# Work order P4.D175 — bug 128's `memories` realtime topic (`4a9be9878`), bug 132's generated-image labels (`78b381a96`: the two writers, the prompt-first `describe_image` ladder with `stored_description`, the data heal), the whole `help/**` re-vendor at `78b381a96` (122 → 123), and the six NO-PORT ratifications — v4 `78b381a96`

**Status: CLOSED** (unified 2026-09-10 — the `78b381a96` twelve-commit drift
catch-up round). Every Tier-1/Tier-2 item landed (bug 128's server half, bug
132's two writers + the reader ladder + the boot heal with its ledger row and
family, the `help/**` re-vendor at the tip — BYTE-IDENTICAL, all 123 blobs,
verified at unification — the six NO-PORT ratifications, the walk annotation,
the `image_generation_tier3` regen). **Landed at unification from the §3
review:** the heal's `links_table_usable` probe is swallowed on BOTH legs as
v4's `try` swallows it (an unreadable mount index DEGRADED the sweep in v4 and
FAILED THE BOOT in v5 — unit-tested under an exclusive lock); the family now
compares `migrations_metadata`; five stale help counts; the walk's table
un-broken. **Measured, not changed:** the host heal test's 600 ms sleep is
not a vacuous sync — `seed_built_ins` JOINS its thread, so the heal completes
inside `Host::start`. **OPEN, named:** the memory-gate logging family (v4's
two `[MemoryGate] … complete` lines with `durationMs`, the two neighbour-set
WARNs, and `4a9be9878`'s `[Memories API] Deleted every memory for a chat`
debug — the last needs no clock and is the cheap first unit); the four
heals' shared `migrations_state`-without-`migrations_metadata` boot shape
(v4-faithful; a one-word hardening). The upstream filing (the third writer,
`— outfit preview`) stays OWED. 💸 as recorded in the lane record. Round
record: `status-log.md`.

## Preamble

- **v4 oracle baseline: `25f534c0b`.** The round absorbs the TWELVE commits
  past it (§R.1); this lane absorbs the SERVER half of **`4a9be9878`** ("Fix
  bugs 127–128: stale memory count and escaped markup (#58)", 2026-09-08 —
  ⚠ a SQUASH of two PR commits; the second, `ba89e0caa`, REMOVED the route's
  own publish that the first added, so `git show 4a9be9878` is the NET and
  the tip state is the spec — a lane reading the first commit's shape ports
  code v4 deleted), the whole of **`78b381a96`** ("Fix bug 132: describe_image
  returned a generated image's label, not a description", 2026-09-09, 664
  insertions / 17 files), the **`help/**` re-vendor obligation of ALL
  TWELVE commits** (11 files, +177/−22, one addition — `help/chat-gallery.md`
  — 122 → 123; one lane, one sha, one `help_tree_equivalence` regen: the
  P4.D168 precedent), and RATIFIES the six riders **`07eee4f4c`**,
  **`9fc664c94`**, **`5fb6bedd6`**, **`df1a075e8`**, **`c0f9232af`**,
  **`d3f0ed133`** (NO-PORT with the evidence tabled below). Spec reads: the
  hunks (`git diff 25f534c0b..78b381a96 -- lib/schemas/realtime.types.ts
  lib/realtime/ lib/memory/memory-gate.ts app/api/v1/memories/route.ts
  lib/background-jobs/handlers/story-background.ts lib/background-jobs/
  handlers/character-avatar.ts lib/tools/handlers/doc-edit/photo-handlers.ts
  lib/tools/describe-image-tool.ts migrations/scripts/clear-generated-image-
  placeholder-descriptions.ts help/`) — **never the commit messages** (ledger
  §5.3). Bug 127's CONVERGENCE half is P4.D177's (a spec note; no oracle
  trips).
- **Run the drift ledger's §2 freshness probe at lane start** with the §R.2
  expected values. **Any mismatch: STOP and report.** Re-run before every
  regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** This lane's pin is
  **`/tmp/qt-v4-pin-p4d175-78b381a96`**; the neutrality pin
  `/tmp/qt-v4-pin-p4d175-25f534c0b`. The help re-vendor is taken from the
  `78b381a96` pin's `help/` tree by `cp` (byte copy), never by hand.
- **Sibling lanes:** P4.D171 → {P4.D172 ∥ P4.D173} (the schema moves + the
  turn manager + the route trail — none of yours; `host.rs` is shared with
  P4.D171 by FENCE: your bug-132 heal goes in its own `// === P4.D175 ===`
  fence in the mount-aware block after the P4.D152 block at `host.rs:1375`;
  P4.D171's two ensures go in the main-partition chain — different regions,
  both fenced; `db/mod.rs` likewise fenced-append); P4.D174 (the gallery
  server — it shares `photos::save_image_to_album` / `db/files.rs` with
  your bug-132 writers and is FORBIDDEN from changing their signatures; you
  own `tools/photo.rs` outright and P4.D174 does not open it); P4.D176 /
  P4.D177 (the SPA lanes — P4.D177 owns the SPA half of bug 128 and the
  bug-127 spec note behind `P4D175_SERVER_LANDED`).
- **Worktree:** per the `carryout` skill; `git worktree add <path> -b
  claude/p4-d175-bugs-128-132-vendor-<hash> main`; edit inside it;
  `CARGO_INCREMENTAL=0`.

## Mandate

(A) Port bug 128's server half so that the completion of the four
chat-scoped memory job types publishes a chat-scoped `memories` hint, a
`MEMORY_HOUSEKEEPING` completion and every delete through the two gate
twins publish it collection-wide, `REPOSITORY_TOPICS` does NOT gain a
`memories` row, and the by-chat delete route publishes NOTHING of its own —
tier 1 exact over the existing `realtime_topics_equivalence` (which flips
ten rows RED-FIRST at the pin with zero corpus edits) plus the publish-site
census. **v5's client never had bug 128's defect** (measured: the sidebar
renders no memory count at all — `edit-section.ts:16-29` is a loud tier-3
deferral); the surface the topic un-stales in v5 is the Salon list card's
memory badge (`chat-card.ts:198`, `chat_enrichment.rs:709-715`), which
P4.D177 maps the topic to. (B) Port bug 132 whole: the two writers stop
stamping a caption into `files.description` (and omit it from the
Scriptorium/vault link call), `describe_image` answers prompt → stored →
vision with a stored description riding along as `stored_description` +
the `\n\nOn file: ` tail, and the boot heal clears the two label shapes
already on disk under v4's exact predicates with a `migrations_state` ledger
row (the P4.D140/P4.D152 shape) — **the heal is load-bearing, not
cosmetic**: `auto_describe_precheck:145-152` answers already-described on
every generated image while the caption sits in the column, so the vision
tier is unreachable without it. **v5 measurably HAS bug 132** at
`story_background_job.rs:839`, `character_avatar_job.rs:365` and
`tools/photo.rs:964-971` (stored FIRST). (C) Re-vendor `help/**` at
`78b381a96` and move the two live count literals 122 → 123. (D) Ratify the
six riders with evidence in the lane record. This order does NOT touch
`db/chats*.rs`, `db/chats_messages*.rs`, `services/provider_failover.rs`,
`services/turn_orchestrator.rs`, `api/chat_media.rs` (VERIFY-ONLY — its
`ensure_image_description` treats the LINK description as a CACHE at
`:1465`, so clearing it re-runs vision there: THE HEAL'S BLAST RADIUS, named
below), `api/images.rs`, `photos/chat_gallery.rs`, or `apps/web/**`.

⚠ **A v4-side gap, measured — decide it explicitly, do not let it pass
unnamed:** a THIRD writer stamps a caption v4's own fix misses.
`app/api/v1/wardrobe/preview-avatar/route.ts:160,:185` writes `` `${
character.name} — outfit preview` `` with `source='GENERATED'`;
`78b381a96` does not touch it (`git show --stat 78b381a96 -- <that file>`
is EMPTY), and the heal's `LIKE '% — wardrobe portrait'` does not match it.
v5 has the identical writer at `api/wardrobe.rs:1112` (`:1128`, `:1150`).
**The ruling for this lane: reproduce v4 EXACTLY (keep the third writer,
keep the narrow predicate) and FILE it upstream as a v4 bug** (the reader
reorder masks it for `describe_image` since those rows carry a
`generationPrompt`; the residual harm is a stale `On file:` tail and the
already-described gate) — record the filing in the lane record with the
measured lines; the convergence pin is the divergence note in the heal
module's header.

## Survey-verified starting points (fresh survey 2026-09-09 at `78b381a96` — do not re-derive)

### (A) Bug 128 — v4 at the tip

- `lib/schemas/realtime.types.ts:39-44` — `'memories'` appended LAST
  (index 6, the 7th entry; count 6 → 7) with the docblock *"The Commonplace
  Book's rows. Scoped by chat id rather than by memory id…"*.
- `lib/realtime/topic-map.ts:35` — `queryKeys.memories.all` appended LAST to
  `ALL_REALTIME_PREFIXES` (7 → 8; `jobs` contributes two).
  `queryKeysForTopic`'s `memories` arm → `[queryKeys.memories.chatCount(id)]`
  when an id is present, else `[queryKeys.memories.all]`; `lib/query/
  keys.ts` gains `queryKeys.memories.{all, chatCount(chatId)}`.
- `lib/realtime/job-topics.ts:127-132` — `TOPIC_ID_FIELDS.memories = []`
  with the comment verbatim: *"Deliberately empty, and `memories` is
  deliberately absent from REPOSITORY_TOPICS: a memories hint is scoped by
  chat id, while `firstIdArg` would hand back `memories.delete(memoryId)`'s
  positional memory id. Every chat-scoped subscriber would then filter that
  hint out — coverage that reaches nobody, which is worse than none."*
  (`firstIdArg` `:141-152` returns `args[0]` whenever it is a non-empty
  string BEFORE consulting `fields`.) `topicsForCompletedJob` `:73-84`,
  inserted between the five-way `chats` arm (ends `:71`) and
  `CONVERSATION_RENDER` (`:86`): `case 'MEMORY_EXTRACTION': case
  'INTER_CHARACTER_MEMORY': case 'CARINA_MEMORY_EXTRACTION': case
  'MEMORY_REGENERATE_CHAT': return [{ topic: 'memories', id: str(payload,
  'chatId') }]` (ONE shared return; `str` `:25-28` coerces a non-string/
  empty value to `undefined`, so a payload missing `chatId` degrades to a
  collection-wide hint); `case 'MEMORY_HOUSEKEEPING': return [{ topic:
  'memories' }]` (character-scoped, collection-wide by necessity).
  **`MEMORY_REGENERATE_ALL` gets NO arm.**
- `lib/memory/memory-gate.ts:28` imports `publishRealtime`;
  `deleteMemoryWithUnlink` (`:509`) publishes at `:548-553`: `if (deleted)
  publishRealtime('memories')` (collection-wide — "this takes a memory id,
  not a chat id, so there is no narrower hint to give. A no-op from the job
  child by design — the parent chokepoints announce a child's deletions");
  `deleteMemoriesWithUnlinkBatch` (`:570`) at `:633-637`: `if (deleted > 0)
  publishRealtime('memories')`. The silence rule's four guards: `:553`
  `if (deleted)`; `:637` `if (deleted > 0)`; `:514-516` the already-gone
  `return false` + `:571` empty list `return 0`; `memory-service.ts:1619-
  1622` `deleteMemoriesByChatIdWithVectors` returns zeros without calling
  the gate when `findByChatId` is empty — **commit 2's exact edge**.
- `app/api/v1/memories/route.ts` `handleDeleteByChatId` (`:1073`) — the
  "No publish here" comment at `:1086-1092` (verbatim in the survey: the
  gate already announces collection-wide; `useRealtimeTopic`'s filter `if
  (id && event.id && event.id !== id) return` lets a collection-wide event
  reach every chat-scoped subscriber; a second hint costs a duplicate
  refetch and would fire on the one case the gate is right to stay silent).
  **Net at the tip: exactly FOUR publish sites** — `job-topics.ts:79`, `:84`,
  `memory-gate.ts:553`, `:637`. The route publishes nothing.
- Client (P4.D177's, for the record): `useChatData.ts` `useRealtimeTopic(
  'memories', chatId)` + `cache: 'no-store'`; `useMemoryActions.ts`
  disabled at zero + a toast + a server re-read before the confirmation;
  `ChatSidebar.tsx` (+7). `help/character-progressions.md` (+6/−?) and
  `help/chat-participants.md` (+11) — part of (C).

### (A) Bug 128 — v5

| v4 | v5 | edit |
|---|---|---|
| `REALTIME_TOPICS` | `crates/quilltap-core/src/realtime/types.rs:31-38` `const REALTIME_TOPICS: [RealtimeTopic; 6]` + the enum `:15-28` + `as_str` `:42-52` | `[; 7]`; variant `Memories` LAST; `as_str` arm `"memories"` |
| `topicsForCompletedJob` | `realtime/job_topics.rs:50-99` (the `chats` arm `:81-88`, `CONVERSATION_RENDER` `:92-95`; `str_field` `:37-43` is v4's `str`) | the two arms BETWEEN them |
| `REPOSITORY_TOPICS` | `job_topics.rs:108-117`; `extract_topic_id` `:134-153` takes `args[0]` first (v4's `firstIdArg`) | **MUST NOT gain a row** — a negative corpus row pins it |
| `TOPIC_ID_FIELDS` | `job_topics.rs:123-132 fn topic_id_fields` (a match) | a `Memories => &[]` arm carrying v4's comment verbatim |
| the gate's publishes | `db/memories.rs:541 delete_with_unlink` (already-gone guard `:541-545`), `:573 delete_many_with_unlink` (empty guard `:573-576`); `publish_realtime(topic, id)` at `realtime/bus.rs:188` | **the publish HOME is a design choice: the repo twin (`db/memories.rs`, v4's layering) or the service (`services/memory_service.rs:108`, `:203`) — v4 publishes from the GATE, whose v5 twin is the repo method; put it there, `if deleted` / `if deleted > 0`, and pin that ALL EIGHT callers (`api/memories.rs:663`; `memory_service.rs:108`, `:203`; `character_archive/service.rs:1242`; `cascade_delete.rs:352`; `memory_dedup.rs:399`; `delete_all.rs:354`; `housekeeping.rs:504`) reach it |
| the route's non-publish | `api/memories.rs:677 memory_delete_by_chat` | **VERIFY-ONLY: must NOT publish**; `memory_service.rs:180-182` is the v4 `:1619` twin (returns default without the gate) |
| the forward loop | `services/job_runner.rs:483 publish_realtime(hint.topic, hint.id.as_deref())` | nothing — the five job arms arrive free once `job_topics.rs` grows them |
| the census | `crates/quilltap-harness/tests/realtime_publish_sites_guard.rs` (`CENSUS` `:23+`, 22 non-test sites today) | the two gate sites added as counted rows |
| the family | `realtime_topics_equivalence.rs` (floors `:110-117`: `completed >= 2*23`, `batch >= 10`) ← `harness/oracle/cases/realtime-topics.ts` (iterates `BackgroundJobTypeEnum.options` `:60-67`, full payload + no payload) | **TEN existing rows flip `[]` → `[{topic:"memories",…}]` on a regen at the pin — the free red-first proof**; add the `memories.delete` write-batch NEGATIVE row v4 added |

All four chat-scoped types exist in v5's enum (`api/system_data.rs:631-655`
`JOB_TYPES`; `services/activity_kinds.rs:82-87`); `MEMORY_EXTRACTION`'s
payload carries `chatId` at `services/queue_service.rs:403` — confirm the
other three the same way (the differential proves it).

### (B) Bug 132 — v4 at the tip

- Writers: `story-background.ts` (+5/−2) — `description: null` on the
  `files` row and the label OMITTED from `writeLanternBackgroundToMountStore`'s
  options, a why-comment naming bug 132; `character-avatar.ts` (+2/−2) —
  likewise through `writeCharacterAvatarToVault`.
- `handleDescribeImage` (`photo-handlers.ts:505-604`): the `respond`
  closure `:536-561` — `respond(description, source, storedDescription?)`;
  `logger.info('Described image for character', {fileEntryId, characterId,
  source, descriptionLength, hasStoredDescription: Boolean(
  storedDescription)})`; `formattedText = storedDescription ? `${
  originalFilename}:\n\n${description}\n\nOn file: ${storedDescription}` :
  `${originalFilename}:\n\n${description}``; result `{...base, description,
  source, ...(storedDescription ? { stored_description } : {})}` — **the
  key is ABSENT, not undefined**. The ladder `:563-596`: `stored =
  entry.description?.trim() || undefined` (a whitespace-only column never
  triggers the tail); (1) `prompt = entry.generationRevisedPrompt?.trim()
  || entry.generationPrompt?.trim()` → `respond(prompt,
  'generation-prompt', stored)` — the ONLY call passing a third argument;
  (2) `stored` → `respond(stored, 'stored-description')`; (3)
  `autoDescribeChatImageAttachment` → `'vision-call'`; the race arm
  (`skipReason === 'already-described'` → re-read → `'stored-description'`)
  UNCHANGED, no third argument. Source-order comments `:565-571`,
  `:575-576`, `:579-580` (the renumbering "lands in case 2").
- `DescribeImageOutput` (`describe-image-tool.ts:55-75`) gains `stored_
  description?: string` with its docblock; **the tool's JSON schema is
  INPUT-ONLY** — no catalog change.
- `lib/chat/file-attachment-fallback.ts` `runGenerateImageDescription` —
  ALREADY prompt-first (confirmed; v5's `services/file_fallback.rs:1097-
  1132` likewise — VERIFY-ONLY).
- The migration `clear-generated-image-placeholder-descriptions.ts` (NEW,
  199 lines): id `clear-generated-image-placeholder-descriptions-v1`,
  `introducedInVersion: '4.10.0'`, `dependsOn: ['sqlite-initial-schema-v1']`,
  no rollback/validate; `PLACEHOLDER_PREDICATE = ("description" LIKE 'Story
  background for: %' OR "description" LIKE '% — wardrobe portrait')`;
  `FILES_WHERE = "source" = 'GENERATED' AND <predicate>` → `UPDATE files SET
  description = NULL`; `LINKS_WHERE = "originalMimeType" LIKE 'image/%' AND
  <predicate>` → `UPDATE doc_mount_file_links SET description = ''` (the
  link side has no source column; opened via `openMountIndexDbIfPresent`);
  counts reported; `prettify.ts` label. Its 202-line test's cases are the
  heal family's seed; `doc-edit-handler-photos.test.ts` (+23, ONE new case:
  a described image WITH a prompt answers the prompt and carries the stored
  text).

### (B) Bug 132 — v5

- Writer 1: `services/story_background_job.rs:837-845` formats the label →
  `:881` `description` on `StoryWriteInput` (`:1012`, field `:1024`) →
  `write_story_file` `:1032-1119` → `FilesRepository::create` `:1089` with
  `description: Some(…)` at **`:1109`** (`source: "GENERATED"` `:1097`,
  `category: "IMAGE"` `:1098` — the heal's anchors) and, on the
  project-less/Lantern branch, `write_lantern_background_to_mount_store(…,
  Some(&input.description))` at **`:1080`** → `image_job_storage.rs:209` →
  `store_blob_to_mount` `:242` → `LinkBlobInput.description` `:276` →
  `db/doc_mount_file_links.rs:974` → `:1083` `input.description.clone().
  unwrap_or_default()` (`None` → `''`, the column default). Edit: drop the
  `format!`, `Option`/`None` at `:881`, `:1024`, `:1109`; `None` at `:1080`.
- Writer 2: `services/character_avatar_job.rs:365` → `:401` on
  `AvatarWriteInput` (`:550`, field `:564`) → `write_avatar_file` `:572-664`
  → `files` INSERT `:635` field **`:655`**; the vault link `:596-604`
  `write_character_avatar_to_vault(…, Some(&input.description))` at
  **`:603`**. Same edit shape. `image_job_storage.rs:97/:209/:242/:276`
  keep their `Option<&str>` params (VERIFY-ONLY).
- The reader: **v5 has NO single `handle_describe_image`** — it is split at
  the async vision seam: `handle_describe_image_precheck` `tools/photo.rs:
  940-945` → `DescribeImageStep::{Done, NeedsVision}` `:928-936`;
  `handle_describe_image_after_vision` `:1000-1005`. Today's order stored
  (`:964-971`) → prompt (`:973-990`) → vision (`:1008-1010`); the race arm
  `:1017-1022`. **No `DescribeImageOutput` struct** — the row is a
  hand-ordered `serde_json::Map` in `fn describe_respond` **`:887-925`** —
  the single edit surface (a `stored: Option<&str>` param, the
  `stored_description` key inserted at v4's position, the tail, the
  `hasStoredDescription` log field). Catalog `tools/definitions/data.rs:
  30-33` GENERATED, input-only — no change; `tools_inventory.rs:89/:393` no
  change; dispatch `tools/executor.rs:1269-1358 run_describe_image`.
- `photos/auto_describe_attachment.rs:130-155` `auto_describe_precheck` —
  a GATE (not-found → not-image → no-sha → already-described `:145-152`),
  correct as is; VERIFY-ONLY. **`api/chat_media.rs:1455-1545`
  `ensure_image_description` treats the LINK description as a CACHE
  (`:1465`)** — the heal clearing `''` onto links re-runs vision there on
  the next attach: NAME this in the heal module header and the lane record
  (blast radius, faithful to v4's migration which clears the same rows).
- The heal: NEW `db/generated_image_placeholder_heal.rs` modelled on
  `db/files_sha256_realign_heal.rs` (`realign_file_entry_sha256(main,
  Option<mount_index>, now_iso)` `:167` — takes BOTH conns; skip guard
  `:174-177`; ledger write `:312-338`; the `migrations_state` table `id,
  completedAt, quilltapVersion, itemsAffected, message` + `migrations_
  metadata` upsert — **hand-duplicated verbatim per heal, there is NO
  shared helper**; a pass that changes NOTHING returns early and writes NO
  ledger row — `host.rs:1168-1171` states why); registered `db/mod.rs`
  (neighbours `:35`, `:68`, `:100`) and called in `host.rs`'s mount-aware
  block (`:1331 if let Some(mi) = ws.mount_index()`) immediately after the
  P4.D152 block at `:1375`, in a `// === P4.D175 ===` fence, with `(main,
  Some(mount_index), &now_iso)`. The v4 ledger id is the once-only guard
  (cross-app in both directions: a v4 boot that already ran it → v5 heals
  nothing and writes nothing).
- Families that MOVE: **`photo_tools_equivalence`** (`.rs:564-628`
  `describe_corpus` = 11 ops mirrored 1:1 at `cases/photo-tools.test.ts:
  249-278`; fixture `build-photo-tools-fixture.ts` + `photo-tools.json` —
  **`describe_stored` (fixture key `described`: description + BOTH prompts)
  flips RED-FIRST with zero edits** — it IS v4's new case; **a COVERAGE
  HOLE: no row has a stored description and NO prompt, so after the reorder
  arm 2 is UNREACHABLE by the corpus** — add a `storedonly` image (spec
  field `:68/:72`, patch `:269/:270`, the JSON entry), a new `DescribeOp`,
  the mirrored oracle op, the completeness assert `:1210-1217` bumped;
  convert `describe_whitespace_stored` (`blankdesc`) into the
  no-`On file:`-tail arm); `avatar_job_tier3_equivalence` (12 cases, dumps
  `files` — every case reds until regenerated at the pin);
  `story_background_job_tier3_equivalence` (15, same);
  `image_generation_tier3_equivalence` (29 — check whether its
  `avatar_fire`/`avatar_autonomous` cases reach the writer). **Neither
  caption string appears in ANY committed fixture** — only regenerated
  NDJSONs change. NEW family: `generated_image_placeholder_heal_equivalence`
  (+ `harness/oracle/cases/<…>-heal.test.ts`, v4's REAL migration `run()`
  over a planted pair) modelled on `files_sha256_realign_heal_equivalence`.
- The 2026-08-24 walk row A2 (`dogfood-walks/2026-08-24-vision-round-pass.md:
  75`) proved `stored-description` on a REAL story background — the WRONG
  behaviour recorded as PASS; annotate it (A3 `:76` and A5 `:78` stay
  valid) and re-bank a genuinely upload-described image as the 💸 item.

### (C) The `help/**` re-vendor

v5's vendored tree is at the REPO ROOT (`help/`, 122 files, blob-identical
to `25f534c0b`), NOT under `crates/`. Delta `25f534c0b..78b381a96`: 11
files, +177/−22 — `character-progressions.md` 3/3, **`chat-gallery.md`
71/0 (A — the ONLY addition)**, `chat-message-actions.md` 2/0,
`chat-multi-character.md` 3/1, `chat-participants.md` 16/5,
`chat-turn-manager.md` 25/10, `chats.md` 35/0, `connection-profiles.md`
7/0, `dangerous-content.md` 8/0, `keep-image-tools.md` 5/3,
`photo-gallery.md` 2/0. Count literals: **TWO live** —
`crates/quilltap-harness/tests/help_tree_embed_guard.rs:29
VENDORED_FILE_COUNT = 122` (+ its docblock `:25-28`) and
`crates/quilltap-host/tests/host_help_docs_boot.rs:88` (+ `:82` comment);
`quilltap-web/tests/help_web_routes.rs:101` DERIVES;
`help_tree_equivalence.rs` retired its literal; **`help_content.rs:17-18`
is ALREADY STALE at 120** — fix it or derive it. Re-vendor-SENSITIVE
families: `help_tree_equivalence` (jest, `cases/help-tree-sync.test.ts`,
header `:18-30`) and `help_tree_embed_guard` (disk/compile pin) +
`help_web_routes`; INSENSITIVE (synthetic fixture trees): `help_doc_ensure`,
`help_doc_sync`, `help_doc_sync_guards`, `embedding_remainder`. The host
embed needs a host REBUILD (bump `quilltap-host`).

### (D) The six riders — the evidence to record

`07eee4f4c` (2 files, 99 insertions, all `docs/`; the bug-127 file MOVES to
`bugs/fixed/` in `4a9be9878` — mirror at the NEW path); `9fc664c94`
(docs only — the route-trail design of record, later MOVED to
`features/complete/`); `5fb6bedd6` (docs only — bug 128's plan, 231 lines);
`df1a075e8` (docs only — the gallery plan, later MOVED); `c0f9232af` (56
files, +132/−110: 12 RENAMES `docs/developer/features/*.md` →
`features/complete/*.md` (the list is in the survey and `git show
c0f9232af --name-status -M --format= | grep '^R'`), 30 non-doc SOURCE files
each a comment-only `See docs/…` path rewrite — **RE-VERIFIED at planning
with two independently-shaped greps, both empty; re-run one and quote it**:
`git show c0f9232af -- app components lib migrations __tests__ | grep -E
'^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-]\s*(//|\*|/\*)' | grep
-v 'version'` → empty); `d3f0ed133` (4 files: README badge,
`package.json`, `packages/quilltap/package.json`, the lock's two lines —
FROM `4.10.0-claude-salon-chat-gallery-6a5awy.19` TO `4.10.0-dev.18`; both
version fields agree on `main`, hazard 6 stays closed). **All twelve
retired specs sit at the TOP LEVEL of v5's `docs/v4/developer/features/`
and none in `complete/` — the unifier's mirror must MOVE them** (name that
in your record as the unifier's wire; you do not touch `docs/v4/`).

## Tiered deliverables

### Tier 1 — must land, in this order

1. **Bug 128 (RED FIRST)** — regenerate `realtime_topics_equivalence` at
   the pin BEFORE any edit and record the ten flipped rows; then the enum/
   array/`as_str`, the two `topics_for_completed_job` arms, the
   `topic_id_fields` arm with v4's comment, the negative `memories.delete`
   write-batch corpus row, the two gate publishes in `db/memories.rs` under
   the existing guards, the census rows, and a unit pin that
   `memory_delete_by_chat` publishes NOTHING (a counting bus seam: one
   collection-wide hint from the gate, zero from the route; a chat with no
   memories → zero hints). Mutation: add the route publish (the count goes
   to two); add a `memories` `REPOSITORY_TOPICS` row (the negative row
   reds).
2. **Bug 132's writers (RED FIRST)** — regenerate `avatar_job_tier3` +
   `story_background_job_tier3` at the pin before the edit (every case red
   on `files.description`), then both writers → `None`, the bridge calls →
   `None`, why-comments naming bug 132; green after.
3. **The reader** — `describe_respond`'s third argument + the key at v4's
   position + the tail + the log field; the ladder reordered prompt →
   stored → vision (the race arm unchanged); `photo_tools_equivalence`
   RED-FIRST on `describe_stored`, then the `storedonly` row + op +
   completeness bump, the `blankdesc` conversion; the doc-comment
   renumbering.
4. **The heal + ledger row** — the module (both predicates verbatim incl.
   the em dash, main + mount legs, counts, the skip-if-row-exists guard,
   NO row on a clean pass), `db/mod.rs`, the `host.rs` fence, the NEW
   family over a planted pair (a matching `GENERATED` file; a matching
   `UPLOADED` file that must SURVIVE; a matching image link; a matching
   non-image link that must SURVIVE; the `— outfit preview` row that
   SURVIVES on both sides — the upstream-filing pin; idempotence; the
   cross-app arm: a pre-written v4 ledger row → nothing touched, nothing
   written). The blast-radius note (`chat_media.rs:1465`).
5. **The help re-vendor** — `cp` the pin's 11 files (incl. the new one),
   the two literals 122 → 123 with their docblocks, `help_content.rs:17-18`
   corrected, `help_tree_equivalence` + `help_tree_embed_guard` +
   `help_web_routes` green at the pin (and RED against the `25f534c0b`
   pin — record both), a host bump.
6. **The six ratifications** in the lane record with the tabled evidence
   (quote the re-run grep for `c0f9232af`).

### Tier 2 — should land

7. The dogfood walk row A2 annotated (the wrong-behaviour PASS) with the
   replacement 💸 item named.
8. `image_generation_tier3` regenerated and its avatar cases checked for the
   writer (record either way).

### Tier 3 — explicit deferrals (loud, typed, never silent)

- The third writer (`api/wardrobe.rs:1112`) is KEPT and the predicate KEPT
  NARROW — v4-faithful; the upstream filing is the deliverable, with the
  heal family's `— outfit preview` arm as the convergence tripwire (it goes
  red the day v4 widens the predicate, by design).
- v4's migration pretty label — NO-PORT (standing).
- Bug 128's client half — P4.D177's; v5 builds NO Delete-Memories count
  (measured absent; the `edit-section.ts` deferral stands).

## The differential requirement (restated)

Tier 1 for bug 128 (`realtime_topics_equivalence` — its recipe is
self-contained pure `tsx` in the Rust header `:19-27`); tier 3 for the two
job families + `photo_tools` (their headers; the photo-tools fixture is
built from scratch by `build-photo-tools-fixture.ts`); tier 2 for the heal
(v4's REAL migration `run()` over a planted pair — the closest recipe is
`files_sha256_realign_heal_equivalence`'s header); the help families as
committed. Every regen from the `78b381a96` pin via the sweep driver; grep
the fresh NDJSONs for `"topic":"memories"` (ten rows) and `stored_description`
before believing a diff.

## Fixtures

- CONSUMES: `photo-tools.json` + its builder (extend with `storedonly`);
  the `avatar-job` / `story-background-job` / `image-generation` fixtures
  (NO edit — captions are runtime-derived); `help/` at the pin.
- DELIVERS: the NEW heal family's planted-pair builder (in prose in the
  recipe header) — a `/tmp` pair, not committed; the re-vendored `help/`
  tree; NO committed DB pair regenerated (§R.11).

## §C — the round-wide wire contract (BINDING — verbatim-identical in P4.D171, P4.D172, P4.D173, P4.D174, P4.D175, P4.D176 and P4.D177)

Every name below is a name, not a suggestion: the unifier runs a
name-for-name diff across the producing crate and the consuming SPA file
(the P4.D165 precedent). A lane that needs a name not listed here STOPs and
flags it in its lane record; the unifier reconciles. Measured at the tip
`78b381a96` on 2026-09-09.

### C.1 The route trail (P4.D171 carries the column; P4.D173 writes it; P4.D177 renders it)

- **Column:** `chat_messages.routeTrail` — nullable JSON `TEXT`. NULL
  whenever nothing failed (nearly every message); no backfill.
- **Wire key:** `routeTrail` on the chat-GET message projection (between
  `modelName` and `targetParticipantIds`; `null` when absent OR null —
  v4's `event.routeTrail || null`, never omitted), on the SSE `done` frame
  (after `modelName`, before `isSilentMessage`; `null` emitted when nothing
  failed), and on the `.qtap` message record (per the vendored schema;
  `profileId` is NOT remapped on import).
- **Entry** (`RouteAttempt`, camelCase on the wire, absent keys ABSENT not
  null): `{ profileId: string(uuid), profileName: string, provider: string,
  modelName: string, via: 'primary'|'retry'|'concierge'|'understudy'|'tier-pick',
  outcome: 'answered'|'failed'|'refused', trigger?: 'auth'|'rate-limit'|
  'network'|'model-missing'|'provider-error'|'empty-response'|
  'moderation-refusal', evidence?: 'finish-reason'|'inferred', detail?:
  string (≤ 200 UTF-16 units; 199 + U+2026 when truncated) }`. The trail
  is `[...failures, answered]`; the answering entry carries no `trigger`/
  `evidence`/`detail` and ALWAYS agrees with the row's `provider`/
  `modelName`. The Concierge's PRE-call reroute writes no row (only
  `via: 'concierge'` on the answering entry).
- **Display** (P4.D177): adjacent rows with the same `profileId` collapse
  to one; glyphs `❌` (`failed`) / `🚫` (`refused`); the list's ONLY theme
  hook is `[aria-label="Models tried for this reply"]`; a one-row trail
  renders pixel-identical to the plain provider/model badge.

### C.2 The drawn rotation (P4.D171 carries + projects the column; P4.D172 writes it; P4.D177 reads it)

- **Column:** `chats.cycleOrderParticipantIds TEXT DEFAULT '[]'` — a
  JSON-encoded array of PARTICIPANT ids, the seats yet to speak this cycle
  in order; `'[]'` reads as "no rotation on file"; a one-character chat
  stores nothing. The migration DDL and the generateDDL shape AGREE.
- **Chat GET:** `cycleOrderParticipantIds: string` (the RAW JSON string, as
  v4 spreads the row) on the chat payload, after `impersonatingParticipantIds`.
- **`?action=turn` (`chatTurnAction`) response:** `state: { queue: string[],
  cycleOrder: string[] }` (the parsed post-resolve list); `turn.
  nextSpeakerName` / `participant.name` resolve from the WHOLE-ROOM map (a
  user-driven seat is named, never `null`/`"Unknown"`); a `query` action
  MAY write the chats row (the resolve is unconditional).
- **Reason + explanation strings:** `TurnSelectionResult.reason` gains
  `'cycle_order'`; `getSelectionExplanation('cycle_order')` = `Next in this
  cycle's drawn rotation`. On that arm the debug block carries
  `weights: {}` and NO `randomValue` key.
- **Client mechanism (RULED for this round):** v5's SPA has no
  `calculateTurnStateFromHistory`; it sets `turnState.cycleOrder` from the
  chat GET's raw string (parsed with `parseCycleOrder`'s exact lenience)
  and from `state.cycleOrder` on every turn response. The participants
  list sorts by rotation rank first (`turn-order.ts` step 4), then the old
  talkativeness sort. Recorded as a documented mechanism divergence.
- **Log lines (P4.D172, byte-exact):** `[Turn Manager] Cycle order
  persisted` `{chatId, how: 'drawn'|'extended', order}` (debug); `[Turn
  Manager] Failed to persist cycle order` `{chatId, how, error}` (warn — the
  write failure is SWALLOWED); `[Turn Manager] Room characters loaded`
  `{seats, requested, resolved}` (debug); `[Turn Manager] Room seats with no
  readable character` `{requested, missing}` (warn).

### C.3 The Salon chat gallery (P4.D174 server ↔ P4.D176 client)

- **Verb `chatGallery`** — request `{ chatId: string }`; response the BARE
  object `{ entries: ChatGalleryEntry[], counts: Record<ChatGallerySource,
  number>, total: number }` (never nested under `data`); a missing chat →
  404 `Chat not found`; an enumerator throw → 500 `Failed to list chat
  gallery` (NOT an empty roll).
- **`ChatGallerySource`** = `'attachment' | 'generated' | 'story-background'
  | 'avatar' | 'portrait' | 'kept' | 'inline'`; **chip order** (and the
  `counts` key order, all seven ALWAYS present): `story-background, avatar,
  portrait, generated, attachment, kept, inline`. Chip labels: `Backgrounds
  / Avatars / Portraits / Generated / Attached / Kept / Inline`.
- **`ChatGalleryEntry`** = `{ id: string, idKind: 'file'|'link', url:
  string, filename: string, mimeType: string, size: number, width?: number,
  height?: number, sha256?: string, createdAt: string (ISO — the sort key,
  newest first, portraits carry the CHARACTER's createdAt), source:
  ChatGallerySource, characterId?: string, characterName?: string,
  messageId?: string, isCurrent: boolean, deletable: boolean (= !isCurrent
  && source ∈ {attachment, generated, story-background, avatar}),
  linkSummary?: PhotoLinkSummary }`. Deletion is `chatFileDelete` and needs
  `idKind === 'file'`.
- **Verb `chatSaveGalleryImage`** — request `{ chatId: string, fileId:
  string (a `files.id` OR a `doc_mount_file_links.id` — NOT uuid-gated),
  mountPointId: string, caption?: string, tags?: string[] }`; refusal
  ladder in order: schema → 400 (`fileId is required` / `mountPointId is
  required`, joined `; `); no chat → 404 `Chat not found`; not in the
  gallery → 400 `Image is not in this chat`; already saved → **409** `{
  error, code: 'ALREADY_SAVED', relativePath, keptAt }`; other save errors
  → 400 with the error's message; else 500 `Failed to save image`. Success
  `{ saved: true, mountPoint, relativePath, linkId, keptAt, fileId, sha256
  }`. The message-scoped `messageSaveImage` keeps 400 for `ALREADY_SAVED`
  and shares the body schema (its former uuid sentences are GONE).
- **`?download=1`** (or `download=true`) on `GET /api/v1/files/{id}`,
  `GET /api/v1/files/proxy/{key}` and `GET /api/v1/mount-points/{id}/blobs/
  {path}` (BOTH arms) → `Content-Disposition: attachment; …` with every other
  header unchanged; anything else → `inline`.
- **Bug 130:** `chatId?: string (uuid)` on the images COLLECTION generate
  body, folded into `linkedTo` through a `Set` (first wins) beside the tag
  ids. The web edge forwards it.
- **Realtime:** the gallery query key sits UNDER v5's `['chat', id]` prefix
  so the existing `chats` topic refreshes it; no new topic.

### C.4 The `memories` realtime topic (P4.D175 server ↔ P4.D177 client)

- **Topic string:** `memories`, appended LAST to `REALTIME_TOPICS` (7th).
  Published chat-scoped (`{topic: 'memories', id: <chatId>}`) on the
  completion of `MEMORY_EXTRACTION`, `INTER_CHARACTER_MEMORY`,
  `CARINA_MEMORY_EXTRACTION`, `MEMORY_REGENERATE_CHAT` (the payload's
  `chatId`; a missing/empty one degrades to collection-wide);
  collection-wide (`{topic: 'memories'}`) on `MEMORY_HOUSEKEEPING` and on
  every delete through the two gate twins (`if deleted` / `if deleted >
  0`); `MEMORY_REGENERATE_ALL` gets no arm; `REPOSITORY_TOPICS` gets NO
  `memories` row; the by-chat delete route publishes NOTHING of its own.
- **Client mapping (RULED for this round, measured):** v5 has no per-chat
  memory count reader, so `queryKeysForTopic('memories', id?)` → the Salon
  LIST's keys (`chatKeys.all`) in both arms — the list card's memory badge
  is the v5 surface the hint un-stales. `ALL_REALTIME_PREFIXES` gains the
  list prefix once (7 entries vs v4's 8 — the target-less `mountPoints`
  row is the standing difference).

### C.5 Bug 132's tool wire (P4.D175, server-only — no SPA consumer)

`describe_image` answers `source: 'generation-prompt' | 'stored-description'
| 'vision-call'` in THAT order of preference; when the answer is the prompt
and a non-blank stored description exists, the result gains
`stored_description: string` (absent otherwise) and `formattedText` ends
`\n\nOn file: <stored>`; the log line `Described image for character` gains
`hasStoredDescription: boolean`.

### C.6 Gate constants (P4.D176 / P4.D177 spec files, ACTIVATE-AT-UNIFY)

`P4D172_SERVER_LANDED = false` (the rotation beats), `P4D173_SERVER_LANDED =
false` (the route-trail beats), `P4D174_SERVER_LANDED = false` (the gallery
beats), `P4D175_SERVER_LANDED = false` (the `memories` beat). Named
constants, never capability probes.

### C.7 What no order invents

No new `qt-*` class (the gallery chips reuse `qt-tab-group`/`qt-tab`/
`qt-tab-active`; the trail's hook is the aria-label); no REST edge for
`?action=gallery` / `?action=save-image`; no Delete-Memories count; no
memo comparator; no third DDL shape for either column; no `memories` row in
`REPOSITORY_TOPICS`; no hostname/RNG seam beyond the ordered draw source; no
widening of the bug-132 heal predicate (the third writer is filed upstream,
not fixed here).

## §R — the round-wide meeting points (BINDING — identical in every order of this round)

Round: **the `78b381a96` twelve-commit drift catch-up round** — P4.D171
(the two schema moves, the substrate) → { P4.D172 (the turn manager from
the tip) ∥ P4.D173 (the route trail) } both stacked on P4.D171's finished
tip ∥ P4.D174 (the gallery server) ∥ P4.D175 (bugs 128/132 + the vendored
artifacts + the riders) ∥ P4.D176 (the gallery SPA) ∥ P4.D177 (the Salon
smalls SPA). Ordered 2026-09-09.

1. **Baseline and drift.** Oracle baseline **`25f534c0b`** (drift-ledger
   §1). v4 `main` HEAD **`78b381a96`** ("Fix bug 132: describe_image
   returned a generated image's label, not a description", `4.10.0-dev.21`)
   — TWELVE commits past the baseline, the ledger's twelve §3 rows, now
   `ORDERED(…)` per row: `07eee4f4c`, `9fc664c94`, `5fb6bedd6`,
   `df1a075e8`, `c0f9232af`, `d3f0ed133` (the six riders — ratification
   rides P4.D175); `4a9be9878` (bug 128 → P4.D175 ∥ P4.D177; bug 127's
   convergence → P4.D177); `5841a8c62` (the route trail → P4.D171 ∥
   P4.D173 ∥ P4.D177); `86d59660c` (the gallery → P4.D174 ∥ P4.D176; its
   help file → P4.D175); `2aca73ad6` + `d14da3a56` (the turn manager, read
   at the TIP → P4.D171 ∥ P4.D172 ∥ P4.D177); `78b381a96` (bug 132 →
   P4.D175). v4 `bugfix` tip **`1a2b2164c`**, unmoved (behind `main`,
   nothing unabsorbed); v4 `release` tip **`8fbf2afe0`** (4.9.2), unmoved.
   The round's catch-up TARGET is `78b381a96`; the unifier moves the
   baseline there.
2. **The freshness probe at lane start** (ledger §2), expected values:
   checkout on **`main`**, HEAD `78b381a96`, the tree carrying EXACTLY the
   recorded bug-133 dirt (ledger §1, re-probed at ordering: 8 modified + 1
   untracked — `story-background.ts`, `appearance-resolution.ts`,
   `image-generation-handler.ts`, `help/dangerous-content.md`, two tests,
   the CHANGELOG, `bugs.md`, `bugs/fixed/bug-133-…md`), `git log --oneline
   78b381a96..main` EMPTY, `git log --oneline 1a2b2164c..bugfix` EMPTY.
   **ONE pre-authorized exception, written at ordering (ledger §1):** if
   the ONLY change is that predicted bug-133 edit having LANDED — the tree
   now CLEAN and `git log --oneline 78b381a96..main` showing EXACTLY ONE
   commit whose subject names bug 133 and whose `--stat` is the recorded
   nine files — the lane PROCEEDS on its `78b381a96` pin and records that
   sha in its lane record as next-round drift. **Any other mismatch: STOP
   and report** — a lane never writes the ledger. Re-run the probe
   before every regen batch; the checkout has gone dirty MID-LANE before
   (ledger §5.1) and was switched to `release` mid-unify once (P4.D91). v4
   ships a commit every few hours this week — expect the probe to fail
   mid-round; a failing probe is a STOP, not a reason to pin at whatever
   HEAD is.
3. **Regen rule: PIN REQUIRED, TWO pin shas, lane-unique paths, all three
   symlink classes** (ledger §5.1), every regen and fixture build through
   `harness/tools/recipe_sweep.py --v4 <pin> --run <family>`; never two
   sweeps concurrently.
   - **`78b381a96`** for every family this round MOVES — the catch-up
     target (the P4.D119 precedent). Paths `/tmp/qt-v4-pin-p4d17N-78b381a96`
     per lane.
   - **`25f534c0b`** for any family the round does NOT move (neutrality
     regens). Paths `/tmp/qt-v4-pin-p4d17N-25f534c0b` per lane.
   - **Verify each pin by a marker**: `lib/photos/chat-gallery.ts`,
     `lib/chat/turn-manager/cycle-order.ts`, `lib/chat/turn-manager/
     room-characters.ts`, `lib/services/chat-message/route-trail.ts` and
     `help/chat-gallery.md` exist ONLY in the `78b381a96` worktree;
     `cycleOrderParticipantIds` appears in `lib/schemas/chat.types.ts` and
     `'memories'` in `lib/schemas/realtime.types.ts` ONLY there; `git
     ls-tree -r --name-only <pin> -- help/ | wc -l` is 123 vs 122;
     `public/schemas/qtap-export.schema.json` is 92,797 bytes at the tip
     vs 89,769 (md5 `430bb227c7029550f773134f42a0c6eb`) at the baseline.
     Check every NDJSON is non-empty before believing any diff (the
     empty-file trap).
   - Committed recipe headers keep saying `cd ~/source/quilltap-server` and
     never name a `/tmp` pin (`recipe-header-conventions`). The oracle
     `node_modules` are the LIVE tree (`zod` 4.5.4 — `zod_version_guard`
     fails the gate if it moves; the twelve commits change no dependency,
     re-measured 2026-09-09). Standing hazard (5): v4's jest maps
     `@google/genai` to a manual mock — the job-family oracles this round
     regenerates (`avatar_job`, `story_background_job`, `image_generation`)
     are on the fourteen-case list; they were proven at the last pin.
   - **`qtap_schema_embed_guard` is RED against the live checkout until
     P4.D171 lands its re-vendor.** Until then every full `cargo test
     --workspace` needs `QT_V4_ROOT=<a 25f534c0b pin>` (the lanes from
     `main`: P4.D174, P4.D175) or the fail-fast run loses its tail; the
     stacked lanes (P4.D172, P4.D173) run with `QT_V4_ROOT=<a 78b381a96
     pin>` because their base carries the re-vendor. P4.D175's help
     re-vendor flips `help_tree_embed_guard` the same way: green at the
     `78b381a96` root, red at `25f534c0b` — its gate uses the tip pin for
     `QT_V4_ROOT` after that commit.
4. **Stacking and resumption.** P4.D171 (the substrate) branches from
   `main` and finishes FIRST — a small, mechanical lane. P4.D172 and
   P4.D173 each `git worktree add <path> -b claude/p4-d17N-… <P4.D171's
   lane-branch TIP>` (never from `main`), in SEPARATE worktrees, in
   parallel, once P4.D171's gate is green and its record names the tip
   sha. **After that tip, P4.D171's public surface is FROZEN**: neither
   stacked lane edits `db/chats.rs`, `db/chats_read.rs`, `db/chats_messages_
   read.rs`, `provisioning/fresh_schema.json`, `qtap_export/schema-key-
   order.json`, `generators/qtap-export.schema.json`, the two ensure
   modules, or `api/salon.rs`'s two projections — a needed change is a
   STOP-and-flag landed as a P4.D171-labelled commit on P4.D171's branch
   (the D164 precedent), recorded, and both stacked lanes rebase onto it.
   P4.D174, P4.D175, P4.D176, P4.D177 branch from `main`. The unifier
   cherry-picks P4.D171, then P4.D172, then P4.D173, then P4.D174, then
   P4.D175, then P4.D176, then P4.D177.
5. **Fenced-append and function-fenced shared files.**
   - `crates/quilltap-host/src/host.rs::seed_built_ins` — P4.D171 appends
     TWO ensure calls in a `// === P4.D171 ===` fence after the P4.D135
     block (the main-partition chain); P4.D175 appends ONE heal call in a
     `// === P4.D175 ===` fence in the mount-aware block after the P4.D152
     block. No one else opens `host.rs`.
   - `crates/quilltap-core/src/db/mod.rs` — `pub mod` lines: P4.D171 (two)
     and P4.D175 (one), each in its own fence.
   - `crates/quilltap-core/src/lib.rs` — P4.D172's new crate-root modules
     (`cycle_order`, `weighted_random`, `room_characters`) in a
     `// === P4.D172 ===` fence. `crates/quilltap-core/src/services/mod.rs`
     — P4.D173's `pub mod route_trail;` in a `// === P4.D173 ===` fence.
     `crates/quilltap-core/src/photos/mod.rs` — P4.D174's two modules.
   - `crates/quilltap-core/src/api/types.rs` + `api/engine.rs` — **P4.D174's
     ALONE this round** (the two gallery verbs, inside `// === P4.D174 ===`
     fences). No other lane adds a `Request`/`Response` variant; P4.D172's
     `?action=turn` change rides `TurnActionResult` in
     `services/turn_orchestrator.rs` and the `Value` envelope in
     `api/salon.rs`, not a typed variant.
   - `crates/quilltap-core/src/services/message_finalizer.rs` — fenced BY
     FUNCTION: `finalize_message_response` (the persistence block + the
     done-frame literal) is P4.D173's; `calculate_next_speaker`,
     `read_speaker_character`, `to_message_views`, `to_speaker_participant`
     are P4.D172's. Neither opens the other's functions.
   - `crates/quilltap-core/src/services/orchestrator.rs` — fenced BY
     FUNCTION: the `StreamingState` literal (`:1392-1396`) and
     `to_effective_profile` (`:4304`) are P4.D173's;
     `maybe_pause_for_user_seat_turn` (`:775-840`) and the turn-skip twin
     (`:3474-3480`) are P4.D172's.
   - `crates/quilltap-core/src/api/salon.rs` — P4.D171 owns the two
     projections (`assemble_chat_get`'s turn block + the message
     projection); P4.D172 owns the `?action=turn` envelope + `resolve_
     active_llm_name`; P4.D173 and P4.D174 do NOT open it.
   - `crates/quilltap-core/src/db/chats_messages.rs` — P4.D171 (the INSERT
     column + the row field) then P4.D172 (`update_chat_metadata`'s strike),
     stacked; P4.D173 sets the field from `primary_stream.rs` and does NOT
     open this file. `services/chat_continuation.rs` is P4.D172's; P4.D173's
     "not copied" proof is harness-side and its comment line is a UNIFIER
     wire.
   - `apps/web/src/app/core/core-contract.ts` — fenced-append (`// ===
     P4.D176 ===` / `// === P4.D177 ===`). `apps/web/src/app/core/realtime-
     topic-map.ts` — P4.D177's, with ONE P4.D176 fenced-append line (or a
     comment) for the gallery key. `apps/web/src/app/chat/message-row.ts` —
     fenced BY REGION: the desktop-avatar template regions (`:72`, `:90`,
     `:385`) + the badge import are P4.D177's; the save-image target
     construct sites (`:262-268`, `:445`, `:611-616`) are P4.D176's.
     `apps/web/src/app/screens/salon/salon-conversation.ts` — fenced BY
     REGION: the turn-state signal + `applyTurnResponse` + the SSE `done`
     fold are P4.D177's; the gallery/detail modal wiring + `scrollToMessage`
     are P4.D176's. Their spec files likewise: new `describe` blocks only,
     appended, never an existing case edited by both.
   - `Cargo.toml`/`Cargo.lock` at the workspace root: version lines only;
     a dependency add is a STOP-and-flag in the lane record (no lane
     expects one — P4.D172's draw source is a closure, not a `rand` crate).
6. **`apps/web/**` is SPLIT between P4.D176 and P4.D177** (see the
   Ownership table for the file lists). P4.D171–P4.D175 finish with `git
   diff main -- apps/web/` EMPTY. The four gate constants (§C.6) flip at
   unification; the §C name-for-name diff runs then.
7. **Frozen files.** `docs/developer/porting/drift-ledger.md` (lanes never
   write it); `help/**` is P4.D175's alone — ALL ELEVEN files the twelve
   commits touched, INCLUDING `help/chat-gallery.md` (P4.D174's feature's
   help page rides P4.D175's re-vendor — one lane, one sha, one
   `help_tree_equivalence` regen, one count bump); `crates/quilltap-core/
   src/tools/photo.rs` is P4.D175's alone (P4.D174 reads it, never edits;
   P4.D174 may NOT change `photos::save_image_to_album`'s signature,
   `SaveImageToAlbumInput`, `SaveImageErrorCode`, `db/files.rs`'s
   `FileCreate`, or `FileBytesStore`); `crates/quilltap-core/src/api/
   chat_media.rs` is P4.D174's (P4.D175 verify-only); `crates/quilltap-core/
   src/enclave/announce.rs` is NOBODY's (v4 still calls the deprecated alias
   there); `crates/quilltap-harness/tests/turn_pause_filters_equivalence.rs`
   stays (it pins the alias); `crates/quilltap-core/src/generators/**` is
   P4.D171's ONLY for the schema re-vendor (no generator code moves);
   `apps/web/src/app/shell/shell.ts` and `apps/web/src/app/workspace/
   workspace-contract.ts` (no new tab kinds — the gallery is a modal, the
   badge a component, the rotation a sort); `docs/v4/**` (the mirror
   refresh — incl. MOVING the twelve retired specs into
   `docs/v4/developer/features/complete/` and mirroring the two new bug
   files + the two design docs at their NEW paths — is a UNIFIER wire).
8. **Version bumps** — bump every crate whose files (tests included) the
   commit touches. P4.D171: `quilltap-core`, `quilltap-harness`,
   `quilltap-host` (the ensures + the DDL tests). P4.D172: `quilltap-core`,
   `quilltap-harness`, `quilltap-host` (`spine.rs`'s draw source +
   `host_cadence`). P4.D173: `quilltap-core`, `quilltap-harness` (+
   `quilltap-web` ONLY if a web test moves — expected none). P4.D174:
   `quilltap-core`, `quilltap-harness`, `quilltap-web`. P4.D175:
   `quilltap-core`, `quilltap-harness`, `quilltap-host` (the heal + the
   help embed), `quilltap-web` (the help count test derives, but its
   siblings' tests may move — measure). P4.D176 / P4.D177:
   `apps/web/package.json` each. Per commit. The unifier RECOUNTS as base
   + total bumps — identical bumps merge silently (`commit-checklist`; the
   playbook's silent-auto-merge trap; four identical bumps across five
   lanes last happened at the `f699da6f6` round).
9. **e2e.** Port 4319 is repo-wide — ONE Playwright at a time across ALL
   lanes; only P4.D176 and P4.D177 run it, and each runs ONLY its own spec
   file(s) during the lane (the full suite is the unifier's). New spec
   files only, plus the named riding specs re-gestured; every
   server-dependent beat guarded ACTIVATE-AT-UNIFY on the §C.6 constants
   — never a capability probe (a DEFINED-but-refusing verb defeats a
   probe; `round-plan-takeaways` §Planning). The unifier flips the
   constants after the producing lanes land and runs the gated beats as
   their first execution — an activation is a measurement, not a
   formality (four gesture defects last round).
10. **Append-only for everyone:** `docs/CHANGELOG.md`,
    `docs/developer/porting/status-log.md`.
11. **Fixture ownership.** P4.D171 regenerates NO committed DB pair; it
    widens the `/tmp` builders of the families the two columns move and
    re-dumps the three committed ARTIFACTS (`fresh_schema.json`,
    `schema-key-order.json`, `qtap-export.schema.json`). P4.D172 delivers
    two NEW JSON corpora (`cycle-order`, `room-characters`) and widens the
    turn-family `/tmp` builders; the committed `characters-{main,mount}.db`
    is READ-ONLY. P4.D173 delivers the NEW `route-trail-compose` corpus and
    widens the finalizer/primary-stream/orchestrator `/tmp` builders.
    P4.D174 delivers the NEW committed `chat-gallery-{main,mount}.db` pair
    (+ builder in prose) and MIGRATES `courier-images-*.db` IN PLACE (the
    Playwright seeder reads it — never rebuild); `images-*.db`, `photos-*`,
    `files-*`, `salon-*` are READ-ONLY. P4.D175 extends `photo-tools.json`
    + its builder and delivers the heal family's `/tmp` planted-pair
    builder; the three job fixtures are NOT edited (captions are
    runtime-derived). No lane regenerates a committed DB pair another lane
    reads. P4.D176/P4.D177 seed through the running server's verbs, never
    SQL (a store-overlay property cannot be SQL-seeded without moving every
    hash beside it — `dogfood-findings.md` standing notes).
12. **Census guards move when a verb is added or a divergence closes.**
    `dispatch_wrong_type_census.rs:2459`'s `429` is P4.D174's ALONE this
    round (the only lane adding `Request` variants); `realtime_publish_
    sites_guard`'s census is P4.D175's; the help count literals
    (`help_tree_embed_guard.rs:29`, `host_help_docs_boot.rs:88`) are
    P4.D175's; `qtap_schema_embed_guard.rs:24`'s `VENDORED_BYTES` is
    P4.D171's; `web_edge_body_parse_guard`'s per-file counts must NOT move
    (P4.D174 parses through the shared reader). Every Rust lane's gate is
    `cargo test --workspace`, never `-p` (`a-new-verb-moves-the-dispatch-
    wrong-type-census`, `closing-a-divergence-moves-the-censuses`).
13. **Cross-app hazards this round.** Both schema ensures and the bug-132
    heal are CROSS-APP with v4 on the shared Friday instance: v4's
    migration runner will have ADDED both columns and RUN the heal there
    before any dogfood copy is taken — the ensures must be no-ops on a
    column that exists, and the heal must honour a pre-written
    `migrations_state` row by touching nothing and writing nothing (the
    P4.D140/P4.D152 rule). A dogfood copy taken after v4 ran them proves
    the cross-app leg for free; the positive leg needs a planted pair.
14. **What is LIVE-CRITICAL.** P4.D171's ensures are load-bearing for any
    pre-4.10 instance — without them P4.D172's `set_col!` arm 500s on
    EVERY turn and the message INSERT 500s; that is why the substrate lands
    first and alone. Bug 132's heal is the fix's user-visible half (the
    vision tier is unreachable while the caption sits in the column).

## Ownership (BINDING — identical table in every order of this round)

| Lane | Owns (edit freely) | Fenced-append / function-fenced (§R.5) | Must not touch |
| --- | --- | --- | --- |
| **P4.D171** (the two schema moves — the substrate, from `main`, FIRST) | `crates/quilltap-core/src/services/provisioning/fresh_schema.json` (the D23 re-dump); `services/qtap_export/schema-key-order.json` (regenerated); `generators/qtap-export.schema.json` (re-vendored) + `crates/quilltap-harness/tests/qtap_schema_embed_guard.rs`; NEW `db/chat_messages_route_trail_repair.rs` + NEW `db/chats_cycle_order_repair.rs`; `db/chats.rs`; `db/chats_read.rs`; `db/chats_messages.rs` (the INSERT column + the row field; NOT `update_chat_metadata`); `db/chats_messages_read.rs`; `api/salon.rs` (ONLY `assemble_chat_get`'s turn block + the message projection); the export/import/restore carry sites (`services/qtap_export/records.rs`, `services/quilltap_import/**` except `reconcile.rs`, the restore spine); every reduced hand-rolled `chats`/`chat_messages` DDL in `crates/*/tests/**` and `crates/*/src/**` (`host_boot.rs`, `host_cadence.rs`, `enclave/lifecycle.rs`, `enclave/step.rs`'s DDLs, `chats_messages_read.rs:351`, `harness/tests/common/mod.rs`, the web venue); the moved families' cases/builders (`provisioning`, `chats_read`, `chats_tier2`, `chats_messages_*`, `salon_reads`, `system_export/import/backup/restore`, `qtap_import`, `qtap_schema_validate`, `backup_uuid_remap`, `restore_vintage_state`, `chat_create_capstone`); this order's header | `crates/quilltap-host/src/host.rs` (two ensure calls, `// === P4.D171 ===`); `crates/quilltap-core/src/db/mod.rs` (two `pub mod` lines) | every service file; `select_speaker.rs`; `turn_state.rs`; `photos/**`; `tools/**`; `realtime/**`; `help/**`; `apps/web/**`; `reconcile.rs` (pin only) |
| **P4.D172** (the turn manager from the tip, stacked on P4.D171) | NEW `crates/quilltap-core/src/{cycle_order,weighted_random,room_characters}.rs`; `select_speaker.rs`; `turn_state.rs`; `turn_order.rs`; `participant_filters.rs` (a `get_present_character_seats` home; the alias STAYS); `services/turn_orchestrator.rs`; `services/participant_resolver.rs`; `services/chat_continuation.rs`; `services/chat_create.rs` (the third weighted pick only); `db/chats_messages.rs::update_chat_metadata` (the strike); `db/vault_read_overlay.rs:374-380` (the drop lines); `db/characters_read.rs` (only if `find_by_ids` needs chunking); `enclave/step.rs` (the room handler + its own test DDL if it moves again); `enclave/lifecycle.rs` (consume only — P4.D171 widened its DDL); `api/salon.rs` (ONLY the `?action=turn` envelope `:984-1018` + `resolve_active_llm_name`); `crates/quilltap-host/src/spine.rs` (the draw source) + `crates/quilltap-host/tests/host_cadence.rs` (the draw-count arm); the moved families' cases/builders (`select_speaker`, `turn_state`, `turn_order`, `turn_orchestrator_tier2`, `participant_resolver_tier2`, `message_finalizer_tier3`, `orchestrator_tier3`, `enclave_step_tier3`, `regenerate_swipe_tier3`, `chats_messages_tier2`, `chat_create_capstone`, `salon_mutations`, `salon_skip`, `chat_activity`/`chat_continuation`); NEW `cycle_order_equivalence` + `room_characters_equivalence` (+ cases + JSON corpora); this order's header | `crates/quilltap-core/src/lib.rs` (`// === P4.D172 ===`, the three modules); `services/message_finalizer.rs` (BY FUNCTION: `calculate_next_speaker` + the speaker helpers ONLY); `services/orchestrator.rs` (BY FUNCTION: `maybe_pause_for_user_seat_turn` + the turn-skip twin ONLY) | P4.D171's frozen files; `provider_failover.rs`; `primary_stream.rs`; `chat_events.rs`; `enclave/announce.rs`; `turn_pause_filters_equivalence.rs:185`; `photos/**`; `tools/photo.rs`; `realtime/**`; `api/types.rs`/`engine.rs`; `help/**`; `apps/web/**`; `host.rs` |
| **P4.D173** (the route trail, stacked on P4.D171) | NEW `crates/quilltap-core/src/services/route_trail.rs`; `services/provider_failover.rs`; `services/primary_stream.rs` (`EffectiveProfile.name`, `StreamingState`, `save_assistant_message`, the preserve-partial write); `services/chat_events.rs`; the twelve `EffectiveProfile` construct sites (`text_tool_loop.rs`, `help_chat/orchestrator.rs`, `dangerous_content/provider_routing.rs`, `brahma_console/{mod,orchestrator}.rs`, the test helpers) for the ONE added field; the moved families' cases/builders (`message_finalizer_tier3`, `primary_stream_tier3`, `orchestrator_tier3`'s route-via arms, `salon_swipe_generate`, `salon_mutations`' done-frame arms, `qtap_schema_validate`'s 201-char arm, the `chat_continuation` family's read-only arm); NEW `route_trail_compose_equivalence` (+ case + corpus); this order's header | `services/mod.rs` (`// === P4.D173 ===`); `services/message_finalizer.rs` (BY FUNCTION: `finalize_message_response` ONLY); `services/orchestrator.rs` (BY FUNCTION: the `StreamingState` literal + `to_effective_profile` ONLY) | P4.D171's frozen files; `api/salon.rs`; `db/**`; `chat_continuation.rs` (harness pin only); `select_speaker.rs`; `turn_state.rs`; `turn_orchestrator.rs`; `participant_resolver.rs`; `enclave/**`; `photos/**`; `tools/**`; `realtime/**`; `generators/**`; `help/**`; `apps/web/**`; `host.rs` |
| **P4.D174** (the gallery server, from `main`) | NEW `crates/quilltap-core/src/photos/chat_gallery.rs` + NEW `photos/save_attribution.rs`; `api/chat_media.rs`; `api/images.rs`; `content_disposition.rs`; `crates/quilltap-web/src/{files_routes,images_routes,query}.rs`; `crates/quilltap-web/tests/{query_param_semantics_equivalence,dispatch_wrong_type_census,binary_routes,images_edge_routes,chats_collection_route}.rs`; `crates/quilltap-harness/tests/web_edge_body_parse_guard.rs` (verify unmoved); the moved families' cases/builders (`courier_images_routes` + `build-courier-images-web-fixture.ts` MIGRATED IN PLACE, `images_generate_route`, `images_routes`, `files_routes`, `chat_export`/`markdown_transcript`/`almanack_tier2` regen-only); NEW `chat_gallery_equivalence` + `harness/oracle/cases/chat-gallery.test.ts` + `build-chat-gallery-fixture.ts` + the committed `crates/quilltap-web/tests/fixtures/chat-gallery-{main,mount}.db`; this order's header | `photos/mod.rs` (two modules); `api/types.rs` + `api/engine.rs` (`// === P4.D174 ===` — the two verbs; the ONLY lane adding variants); `crates/quilltap-web/src/lib.rs` (only if a route registration moves — expected none) | `tools/photo.rs`; `photos/auto_describe_attachment.rs`; `photos/save_image_to_album.rs` (READ — no signature change); `db/files.rs`; `services/story_background_job.rs`; `services/character_avatar_job.rs`; `api/salon.rs`; `db/chats*.rs`; `realtime/**`; `help/**` (incl. `help/chat-gallery.md` — P4.D175's); `apps/web/**`; `host.rs`; `images-*.db`/`photos-*.db`/`files-*.db`/`salon-*.db` (read-only) |
| **P4.D175** (bugs 128/132 + the vendored artifacts + the riders, from `main`) | `crates/quilltap-core/src/realtime/{types,job_topics}.rs`; `db/memories.rs` (the two gate publishes); `services/story_background_job.rs`; `services/character_avatar_job.rs`; `tools/photo.rs` (`describe_respond` + the ladder); NEW `db/generated_image_placeholder_heal.rs`; `help/**` (ALL ELEVEN files at `78b381a96`, incl. NEW `help/chat-gallery.md`); `crates/quilltap-harness/tests/help_tree_embed_guard.rs` + `crates/quilltap-host/tests/host_help_docs_boot.rs` (the count) + `crates/quilltap-core/src/services/help_chat/help_content.rs:17-18` (the stale 120); `crates/quilltap-harness/tests/realtime_publish_sites_guard.rs`; the moved families' cases/builders (`realtime_topics`, `photo_tools` + `build-photo-tools-fixture.ts` + `photo-tools.json`, `avatar_job_tier3`/`story_background_job_tier3`/`image_generation_tier3` regen-only, the four help families); NEW `generated_image_placeholder_heal_equivalence` (+ case + `/tmp` builder in prose); `docs/developer/porting/dogfood-walks/2026-08-24-vision-round-pass.md:75` (the annotation); this order's header | `crates/quilltap-host/src/host.rs` (ONE heal call, `// === P4.D175 ===`, mount-aware block); `crates/quilltap-core/src/db/mod.rs` (one `pub mod` line) | `api/chat_media.rs` (VERIFY-ONLY — the description-cache blast radius is NAMED, not edited); `api/memories.rs` (VERIFY-ONLY — no publish); `api/wardrobe.rs:1112` (KEPT — filed upstream); `photos/auto_describe_attachment.rs`, `services/file_fallback.rs`, `services/image_job_storage.rs`, `db/doc_mount_file_links.rs` (VERIFY-ONLY); `tools/definitions/data.rs` (GENERATED — no change); `photos/chat_gallery.rs`; `api/images.rs`; `db/chats*.rs`; every turn/route-trail file; `apps/web/**`; `docs/v4/**` (the mirror is the unifier's) |
| **P4.D176** (the gallery SPA, from `main`) | `apps/web/src/app/images/**` (`photo-gallery-modal`, `chat-gallery-image-view-modal`, `save-image-dialog`, `image-modal` + specs); `apps/web/src/app/chat/sidebar/organize-section.ts` (+ spec); `apps/web/src/app/chat/chat-keys.ts`; `apps/web/src/app/core/download-utils.ts` (+ spec); NEW `apps/web/oracle/download-utils.ts` + `src/testing/fixtures/download-utils.ndjson`; NEW `apps/web/e2e/salon-chat-gallery-flow.spec.ts`; the riding specs `salon-image-detail-flow`, `salon-courier-images-flow`, `workspace-gallery-modal-flow` (re-gestured only); `docs/developer/porting/m6-screen-parity.md` (its rows); `apps/web/package.json`; this order's header | `apps/web/src/app/core/core-contract.ts` (`// === P4.D176 ===`); `apps/web/src/app/core/realtime-topic-map.ts` (ONE line/comment for the gallery key); `apps/web/src/app/chat/message-row.ts` (BY REGION: the save-image target construct sites ONLY) + its spec (new `describe` blocks only); `apps/web/src/app/screens/salon/salon-conversation.ts` (BY REGION: the gallery/detail modal wiring + `scrollToMessage` ONLY) + its spec | `crates/**`; `help/**`; `harness/**`; `chat/route-trail-*`; `chat/turn-order.ts`; `chat/message-list.ts`; `chat/sidebar/chat-sidebar.ts`; `chat/sidebar/edit-section.ts`; `core/realtime.types.ts`; `progressions/**`; `shell.ts`; `workspace-contract.ts`; any other EXISTING e2e spec |
| **P4.D177** (the Salon smalls SPA, from `main`) | NEW `apps/web/src/app/chat/route-trail-display.ts` + NEW `chat/route-trail-badge.ts` (+ specs); `apps/web/src/app/chat/message-list.ts`; `apps/web/src/app/chat/turn-order.ts` (+ spec); `apps/web/src/app/chat/sidebar/chat-sidebar.ts` (+ spec); `apps/web/src/app/chat/sidebar/edit-section.ts` (doc note only); `apps/web/src/app/core/realtime.types.ts`; `apps/web/src/app/core/realtime-topic-map.ts` (+ spec); `apps/web/src/app/progressions/progressions-section.{ts,spec.ts}` (the two comments); NEW `apps/web/oracle/{route-trail-display,turn-order-rotation}.ts` + their `src/testing/fixtures/*.ndjson`; NEW `apps/web/e2e/salon-route-trail-flow.spec.ts`; the riding specs `salon-sidebar-flow` (the rotation beat), `salon-streaming-avatar-flow` (only if the avatar markup moves); `docs/developer/porting/m6-screen-parity.md` (its rows); `apps/web/package.json`; this order's header | `apps/web/src/app/core/core-contract.ts` (`// === P4.D177 ===`); `apps/web/src/app/chat/message-row.ts` (BY REGION: the desktop-avatar template regions + the badge import ONLY) + its spec (new `describe` blocks only); `apps/web/src/app/screens/salon/salon-conversation.ts` (BY REGION: the turn-state signal, `applyTurnResponse`, the SSE `done` fold ONLY) + its spec | `crates/**`; `help/**`; `harness/**`; `images/**`; `chat/sidebar/organize-section.ts`; `chat/chat-keys.ts`; `core/download-utils.ts`; `shell.ts`; `workspace-contract.ts`; any other EXISTING e2e spec |

Everyone: `docs/CHANGELOG.md` + `status-log.md` append-only; the drift
ledger frozen; `shell.ts` / `workspace-contract.ts` frozen;
`enclave/announce.rs` nobody's; `docs/v4/**` the unifier's. `Cargo.toml` /
`Cargo.lock`: version lines only (a dependency add is a STOP-and-flag). The
§C name-for-name diff, the four gate flips, the `chat_continuation.rs`
comment line (P4.D173's wire), the `docs/v4/` mirror refresh (incl. the
twelve MOVES into `features/complete/`), and the baseline move to
`78b381a96` are UNIFIER wires.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
   -D warnings` in BOTH feature sets (default and `--features
   quilltap-core/native-transport`); `cargo build --workspace`; the release
   build (`webBinary()` prefers `target/release`).
2. `cargo test --workspace` (background, full log + sentinel — NEVER
   `| tail`, never a pgrep loop — `background-gate-latency-fix`,
   `pgrep-until-loops-match-themselves`) with this lane's env block (every
   family it moves, by its recipe var) AND `QT_V4_ROOT` set to the pin §R.3
   names for this lane; grep the log for `SKIP:` — every family this lane
   moves must RUN (confirm by per-binary duration or `-- --nocapture`); the
   exit code read directly, never after a pipe
   (`gate-exit-code-after-a-pipe`).
3. Every family this lane moves regenerated FRESH from the `78b381a96` pin
   through `harness/tools/recipe_sweep.py --v4 <pin> --run <family>` (never
   two sweeps concurrently), re-run by name with zero SKIP, the changed
   bytes grepped in the NDJSON (§R.3's markers), and the NDJSON non-empty.
   The RED-FIRST items shown red against the pre-edit tree in the lane
   record (run them before the first source commit — a `git stash`-free
   proof).
4. The mutation proofs named in this order, run and restored by string
   edit (`mutation-proof-revert-by-file-backup`), each reddening exactly the
   named test.
5. `git diff main -- apps/web/` EMPTY; `git diff main -- help/` EMPTY
   (P4.D175 excepted); the Ownership table's "must not touch" column
   honoured (`git diff main --stat` read against it).
6. Commits per `.claude/commands/commit.md` (the crate bumps §R.8 names, on
   every commit that moves the crate; CHANGELOG every commit); the lane
   record appended to `status-log.md` with the red-first evidence, the
   per-family regen lines, the mutation table, every measured deviation
   from this order (`work-order-facts-need-the-same-verification-as-commit-
   prose`), and the "spotted, not mine" list for the unifier.
