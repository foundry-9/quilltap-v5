# Work order P4.D141 — the Concierge four-state per-chat control (v4 `60e3c4a0a`)

**Status:** CLOSED — UNIFIED on main at the round-2 drift catch-up
unification (2026-09-01). All six units landed; the unifier applied the
wires: the `[conciergeOverride]` binding on `<qt-chat-sidebar>` in
`salon-conversation.ts` (Salon-spec pinned), `P4D141_SALON_WIRE_LANDED`
flipped live, the three `LAST_MESSAGE_AT_PENDING_P4D140` masks DELETED
(P4.D140 landed on the same branch; plain equalities returned), the
`generate_image.rs` §E comment. **§C measured as a NO-OP with evidence:**
v4 `SalonView.tsx:1496` flips the `VirtualizedMessageList → MessageRow →
bubble` `dangerous` prop, and v5's `qt-message-list` has NO danger input —
message-bubble danger styling was never ported (a pre-existing gap, recorded
as a candidate; when it is ported it binds `shouldShowDangerStyling(chat)`,
never the raw label); the sidebar's raw `isDangerousChat` stays raw as v4's
own `:1883` does. §3 at unification fixed: **the sidebar select held a
permanent optimistic latch** (after the first successful pick a refetch, a
classifier auto-flip or another tab could never win, and the control could
disagree with the header badge) — now derived from the two stored fields
alone as v4's `ChatSidebar.tsx:1123` is, re-applied to the element after each
render (the P4.D115 controlled-select idiom), spec-pinned both directions;
the invented same-value short-circuit removed (v4 fires unconditionally);
the Flagged pill's no-category title byte; and
**`post_office_writers_tier3_equivalence` was BROKEN by the kind rename**
(the fixture still drove `manual-off-duty`/`manual-on-duty`, `from_wire`
panicked; the lane record's "pins the five sentences through the real
writer" claim named the WRONG family) — renamed + `manual-uncensored` added,
regenerated at the pin, 22 rows. Recorded follow-ups: the
`precompute_equivalence` blindness (an uncensored profile seeded into
`episodic-recall-*.db` + `allProfiles` threaded); no `PUT /api/v1/chats/{id}`
REST edge exists in `quilltap-web` (the verb is dispatch-only, as before).

## Preamble

- **v4 oracle baseline: `7fb668263`** (drift-ledger §1, probe re-run
  2026-09-01). **This lane's target commit is `60e3c4a0a`** — the per-chat
  Concierge control grows three states → four: NULL (classifier decides
  Monitored/Flagged), `'OFF'` relabeled **Vouched Safe**, NEW
  `'UNCENSORED'` (operator-asserted uncensored routing). Both operator
  states preserve `isDangerousChat` underneath. v4's design doc
  `docs/developer/features/complete/concierge-four-state.md` may be read
  for orientation — the survey confirmed doc and hunks agree — but the
  hunks are the spec (ledger §5.3).
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 checkout
  on `main`, tree clean, `git log 4622411fd..main` empty and
  `git log 3a76b17df..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED** (ledger §1). Every oracle/fixture regen
  runs from a lane-unique detached worktree pinned at **`60e3c4a0a`**
  (`/tmp/qt-v4-pin-p4d141-60e3c4a0a`, three symlink classes, ledger §5.1;
  the post-office family runs with `TZ=UTC`). Families this lane does not
  move regen at the baseline `7fb668263` if needed.
- **Sibling lanes:** P4.D138/P4.D139 (LoRA — §E gives this lane exactly
  two flip lines + imports in two D138-owned files), P4.D140 (bug 112 —
  no server overlap: v4's chat-enrichment hunk here was TYPE-ONLY and
  v5's `concierge_override` is already `Option<String>` at
  `chat_enrichment.rs:593`, so this lane does NOT open that file),
  P4.D142, P4.66 (§C — the salon-conversation flip is a unification
  wire). §Ownership/§Shared contracts binding, identical.
- **Worktree:** per the `carryout` skill. `CHANGELOG`/`status-log.md`
  append-only. **No Playwright** (P4.66 owns port 4319) — this lane's
  four-state walk beat is authored gated (ACTIVATE-AT-UNIFY).

## Mandate

Port the four-state feature whole, server and SPA: the reshaped
`chat_override` predicate family (delete-don't-repoint), the manual-flip
kinds, the resolver's two operator arms, every predicate call site
threaded (20 expressions / 14 v4 files — the v5 map below), the new
Concierge sentences byte-exact, the API `conciergeState` widening AND the
closing of v5's long-named `conciergeState` PUT deferral
(`api/salon.rs:1216/:1293`), the SPA four-state control built from
scratch in its marked slot, the header badge rewrite (fixing the
pre-existing two-pill divergence), the `_chat.css` badge
parameterization, and the client predicate twin per §C. Must NOT touch:
P4.D140's files (chat_enrichment.rs included), any P4.D138 region of
`tools/generate_image.rs`/`story_background_job.rs` beyond the §E flip
lines, `salon-conversation.ts` (P4.66's — §C wire), chat-card.ts /
salon-list.ts quick-hide (their raw `isDangerousChat` reads are
v4-FAITHFUL — v4's commit did not touch ChatCard or quick-hide; do not
"fix" them).

## Survey-verified starting points (2026-09-01, from the shipped hunks — ledger §5.3)

1. **The state model.** Storage unchanged (TEXT, no CHECK — v5's four
   test-DDL strings need nothing). Zod widens to
   `z.enum(['OFF','UNCENSORED']).nullable().optional()` in BOTH
   `ChatMetadataSchema` and `ChatMetadataBaseSchema` with the identical
   replacement doc (verbatim in the hunks). API enum:
   `conciergeState: z.enum(['monitored','flagged','vouched','uncensored'])`
   (was `['safe','flagged','off']`) + the four-line doc. v4's
   `widen-concierge-override-domain-v1` migration is LEDGER-ONLY (run()
   executes NO SQL, itemsAffected 0) — **v5 non-port, recorded
   deliberately** (no runner; nothing to heal). v4's
   `qtap-export.schema.json` enum hunk has no v5 twin (no schema JSON) —
   note against the standing flag; `schema-key-order.json` already lists
   `conciergeOverride` (ordering only, no change).
2. **`chat_override.rs` reshape** (current v5: `:24` enum
   {Safe,Flagged,Off}, `:50` `is_concierge_off_duty`, `:59`
   `get_concierge_state`, `:78` `is_chat_active_dangerous`). Post-state
   API (v4 names, transcribe semantics exactly):
   `ConciergeState { Monitored, Flagged, Vouched, Uncensored }` with
   `as_str` → `"monitored"|"flagged"|"vouched"|"uncensored"`;
   `get_concierge_state` (UNCENSORED → uncensored; OFF → vouched; else
   isDangerousChat ? flagged : monitored — the None-chat guard DISAPPEARS:
   None → monitored); `should_use_uncensored_route` (flagged ||
   uncensored); `should_show_danger_styling` (=== flagged ONLY — the exact
   old `is_chat_active_dangerous` semantics under a new name);
   `is_classifier_on_duty` (monitored || flagged — **true for None**,
   pinned by v4's "nothing has taken the classifier off the case" test).
   **DELETE `is_concierge_off_duty` and `is_chat_active_dangerous` — v4
   deleted, not re-pointed** ("every call site should be forced to state
   which question it is asking"). Carry the module header with the 2×2
   table verbatim. Truth table (override × label) is in the survey/hunks —
   the two UNCENSORED rows are the previously-unreachable corner.
3. **`manual_flip.rs`.** Kinds: `manual-vouched` (was `manual-off-duty`),
   `manual-resumed` (was `manual-on-duty`, scope WIDENED: fires for
   monitored requested from vouched OR uncensored), NEW
   `manual-uncensored`; `manual-flagged`/`manual-safe` unchanged
   (`manual-safe` = monitored requested from flagged). The new
   `Uncensored` arm mirrors the `Off` arm's standalone-write shape
   (`UPDATE chats SET "conciergeOverride" = 'UNCENSORED' WHERE id = ?1`,
   no `updatedAt` — the standing convention at `:15-23`); both operator
   arms carry v4's preserve-isDangerousChat comments. The Safe arm's kind
   selection becomes `current ∈ {vouched, uncensored} ? manual-resumed :
   manual-safe`, and the Monitored arm keeps the clear-classification
   metadata + its comment. Fix the `:42-43`/`:63-65` docs that say
   "four" wire strings (now five).
4. **`resolver.rs`.** `DangerSource` gains `ChatVouched`
   (`"chat-vouched"`, renamed from ChatOffDuty/`"chat-off-duty"`) +
   `ChatUncensored` (`"chat-uncensored"`); constant renamed
   `vouched_safe_dangerous_content_settings` (contents unchanged; new doc
   "deliberately carries no uncensored profile IDs"). Branch order:
   exempt (returns the vouched-safe settings) → **uncensored** →
   **vouched** → global → default; the exempt-beats-uncensored order is
   test-pinned. Uncensored arm: spread the global (falling back to
   defaults when None — v5's narrower `Option<DangerousContentSettings>`
   signature stays), force `mode: AUTO_ROUTE`, `threshold: 1.0`, all
   three scans false, `showWarningBadges: false` — carry the inline
   comments ("the operator has already returned the verdict", "Forcing
   AUTO_ROUTE even under a global OFF is deliberate").
5. **Call sites — the v5 map** (v4: 20 expressions / 14 files; every line
   verified 2026-09-01): via the shared helper →
   `should_use_uncensored_route`: `orchestrator.rs:1246` (uncensored
   fallback options), `pre_compute.rs:236`, `story_background_job.rs:365`
   (§E), `title_update_job.rs:259`, `memory_extraction_job.rs:294`,
   `carina_memory_extraction.rs:237`, `pascal/llm_consult.rs:260`,
   `tools/generate_image.rs:1337` (§E). Local duplicate:
   `context_summary.rs:483/:614` — DELETE the private
   `is_chat_active_dangerous` in favour of the shared helper. Raw gates →
   `!is_classifier_on_duty`: `danger_scan.rs:113` (+ v4's new
   "nothing may reclassify a chat out from under the operator" comment),
   `dangerous_content/gatekeeper_job.rs:129` (v4's bail comment).
   `message_finalizer.rs:1465`'s raw sticky `isDangerousChat` check —
   LEAVE (v4 parity; v4 keeps the same raw check after the helper gate).
   v4 deliberately left five stale "Off-duty" comments at ported sites —
   mirror the staleness (conservative call) or clean with a note; decide
   once, record it. `api/salon.rs:474-476` chat projection carries the
   raw field — unchanged.
6. **The `conciergeState` PUT arm.** v5 has NO production caller of
   `apply_concierge_flip` (only the harness drives it) —
   `api/salon.rs:1293` names `conciergeState` a deferral and
   `chat_update` is at `:1299`. This lane CLOSES the deferral: the PUT's
   `conciergeState` key dispatches through `apply_concierge_flip` with
   the `RealConciergeAnnouncer`, honouring v4's route shape (v4 client
   PUTs `{conciergeState: next}` to `/api/v1/chats/[id]`; the four-value
   enum 400s on anything else — decode through the Request enum, never a
   hand-built variant; the `double_option` tri-state lesson applies).
7. **The writer.** `concierge_notifications.rs:267` enum grows
   `ManualUncensored`; renames follow the wire strings
   (`manual-vouched`/`manual-resumed`/`manual-uncensored` — from_wire/
   as_wire updated; unknown → None stays). Sentences: `manual-vouched`
   persona copy REWORDED ("…takes the afternoon off. No moderation, no
   rerouting, no quiet interventions; the ordinary desks remain in
   service, on the operator's own recognizance."), `manual-resumed`
   KEEPS the old "returns to his post" copy under the renamed kind, NEW
   `manual-uncensored` ("…the uncensored door stands open. Nothing is to
   be examined, nothing softened; the conversation and its errands go
   henceforth to the frank desk, entirely on the operator's own
   recognizance."). Advisories: `manual-vouched` EXTENDED with final
   sentence "Ordinary providers still apply."; NEW `manual-uncensored`
   advisory; `manual-resumed` advisory unchanged. **Byte-exact matters:
   the vouched advisory keeps U+2019 in `operator’s`.** (Correction to
   the ledger row: the uncensored-door sentence is NOT shared by
   manual-resumed — what is shared is the TRANSITION trigger, both
   vouched→monitored and uncensored→monitored emitting manual-resumed.)
8. **SPA.** `conversation-header.ts` — replace the two independent `@if`
   pills (:52-64; a pre-existing divergence: an off-duty+flagged chat
   renders BOTH where v4's ternary renders one) with a single
   `conciergeState()` computed rendering v4's post-state table: flagged
   (`qt-danger-badge`, alert-triangle, "Flagged", categories title) /
   vouched (`qt-danger-badge qt-danger-badge-muted`, check-circle,
   "Vouched Safe", the vouched title sentence) / uncensored
   (`qt-danger-badge qt-danger-badge-info`, eye-off, "Uncensored", the
   uncensored title sentence) / monitored → NO badge ("the pill means
   'something other than the default is set'"). `chat-section.ts` —
   BUILD the four-state control from scratch in its marked slot
   (:75-77/:103-105 name the deferral to retire): the `<select>` with two
   optgroups (`The Concierge decides` → Monitored/Flagged; `You decide` →
   Vouched Safe/Uncensored), the four helper texts, the four state icons
   (eye/qt-text-success, alert-triangle/qt-text-danger,
   check-circle/qt-text-muted, eye-off/qt-text-info), the four toasts
   (`The Concierge is on watch` / `Marked as flagged` / `You have vouched
   for this chat` / `The uncensored door stands open`), the PUT wiring +
   v4's error path (:1074/:1090-1095 in v4 ChatSidebar). NEW
   `chat/concierge-state.ts` client twin per §C (the header + sidebar +
   the §C wire all read it). `core-contract.ts` :2574/:2891 unions widen
   (§D). `_chat.css` :2968 — parameterize `.qt-danger-badge` over
   `--qt-concierge-badge-color` + add `-muted` (comment "Vouched Safe —
   no colour: the Concierge isn't in the room.") and `-info` ("Uncensored
   — the eye you closed yourself.") recolors; rewrite the rule doc
   ("a recolor, not four rules"). v4's theme-storybook mirror has no v5
   twin (no storybook — record).
9. **The gated beat.** Author the four-state walk from v4's
   `concierge-four-state-test.sh` shape: the 10-transition table
   (each asserting the `(conciergeOverride, isDangerousChat)` pair + the
   announcement phrase — `thrown the switch` / `stands down for the
   moment` / `takes the afternoon off` / `returns to his post` ×2 /
   `uncensored door stands open` ×2), the CT-2 preserve pairs
   (`OFF,1` and `UNCENSORED,1`), driven through the UI control.
   ACTIVATE-AT-UNIFY behind a named constant; no Playwright run in-lane.

## Tiered deliverables

- **Tier 1 (must land):** points 1–7 — the predicate family, flips,
  resolver, all call sites, the PUT arm closing the deferral, the writer
  sentences, with every differential below.
- **Tier 2 (should land):** point 8 (the whole SPA surface + client twin
  + CSS) and point 9 (the gated beat).
- **Tier 3 (explicit refusal-arm deferrals):** none planned. The
  `help/dangerous-content.md` hunk banks to `p4.9i2`. The v4 e2e
  `--arm`/`--recheck` scan-tick guard (10-minute wait) is NOT reproduced
  as a beat — its substance (operator states skip classification) is
  pinned at the danger-scan differential instead; record that decision.

## The differential requirement (restated)

Every unit drives v4's REAL code from the `60e3c4a0a` pin:

- `danger_resolver_equivalence` — §1 pure rows grow the full four-state
  truth table over BOTH stored fields (v4's chat-override test table is
  the corpus shape; the oracle drives the real module); ⚠ its
  `state_from_str` at `:186-192` hard-codes `"safe"|"flagged"|"off"` and
  panics on anything else — it must gain the four new strings; §2 tier-2
  (manual-flip over the real fixture with the un-mocked announcer — both
  the `chats` row and the bubble are diffed) grows all 12 ordered
  transitions + the four no-op cases + the preserve pins (v4's
  `expect(update).not.toHaveProperty('isDangerousChat')` becomes a
  column-level assert). Red-first where v5 measurably lacks the state.
- `danger_scan_tier2_equivalence` + `danger_gatekeeper_tier3_equivalence`
  — the two raw gates through `is_classifier_on_duty`, with an
  UNCENSORED-chat fixture row proving the skip (mutation: revert one gate
  → exactly that family reddens).
- `danger_routing_equivalence` — the resolver's two operator arms incl.
  forces-AUTO_ROUTE-under-global-OFF and exempt-beats-uncensored.
- `post_office_concierge_lantern_suparna_equivalence` (`TZ=UTC`) — the
  five manual kinds' sentences byte-exact (the U+2019 pin).
- The chat verbs: the routes family covering PUT `conciergeState` —
  four valid values + an invalid one 400ing with v4's envelope (measure
  v4's actual refusal: `.parse` vs `safeParse` decides FLAT
  `Validation error` vs the detailed body — the standing note).
- Spine coverage: `orchestrator_tier3`/`build_context` ops that carry a
  chat now exercise an UNCENSORED chat where the corpus allows; at
  minimum one op proving `should_use_uncensored_route` reaches the
  uncensored-selection branch (grep the regenerated NDJSON for the
  changed bytes, ledger §5.2).
- SPA: parity spec for `concierge-state.ts` transcribed 1:1 from v4's
  chat-override tests; header-badge and control specs pin the strings +
  the single-pill fix (red-first against the current two-pill render).

## Fixtures

- Consumes: `danger-manual-flip.json` + its builder, `danger-scan.json`,
  `danger-gatekeeper.json`, `danger-routing.json` (grow rows for the new
  states — builders re-run from the pin), the settings/chat routes
  fixtures.
- Delivers: the widened danger fixtures + regenerated oracles; the gated
  four-state beat.
- Regenerating a committed fixture ⇒ re-run every sibling family that
  reads it, by name, zero SKIP.

## Shared contracts (binding, identical across all six orders)

**§A — the LoRA wire (P4.D138 ↔ P4.D139).**
- `GET /api/v1/image-profiles?action=list-models` response gains
  `loraSupport: Record<modelId, ImageLoraSupport>` between `source` and the
  conditional `fetchError`; models resolving no support are ABSENT from the
  map (the editor's "offer no LoRA rows" signal).
- NEW `GET /api/v1/image-profiles?action=options-schema&provider=…[&model=…]`
  → `{ provider, model: model ?? null, optionsSchema: ProviderOptionsSchema
  | null, loraSupport: ImageLoraSupport | null }` — `null`, never a zero-cap
  object. 400s: `Provider is required`; `` Provider ${provider} is not
  available ``.
- NEW `POST /api/v1/image-profiles?action=lora-metadata`, body
  `{ source: string, hfToken?: string }` → HTTP **200** carrying the
  `HuggingFaceLookupResult` verbatim (`{ok:true,facts}` |
  `{ok:false,reason,repoId,url,detail?}`; reasons `not-a-repo-id |
  missing-or-private | not-found | rate-limited | timeout | network |
  http`). Guards in order, exact sentences: bad JSON or non-object body →
  400 ``A JSON body with a `source` is required``; blank/non-string source
  → 400 `A LoRA source is required`; non-string hfToken → 400
  `hfToken must be a string when supplied`. POST-not-GET because hfToken is
  a credential (carry the comment).
- The stored `loras` entry: `{ source: string (trimmed, min 1), scale?:
  number (finite, 0..10), triggerPhrase?: string, label?: string }`.
  Write-side rejection is the Zod envelope
  `{ error: 'Validation error', details: issues }`, HTTP 400 — never a
  bespoke sentence. No server-side cap.
- `ImageLoraSupport = { maxLoras: number, scale?: {min,max,default,step?},
  sourceKinds: ('url'|'hf-repo'|'provider-id')[],
  supportsPrivateWeightsToken?: boolean }`;
  `DEFAULT_LORA_SCALE = {min:0,max:2,default:1,step:0.05}`.

**§B — `qt-range` (P4.D139 ↔ P4.D142).** D142 defines `.qt-range`
(`_interactive.css`) + `--qt-range-accent`/`--qt-range-focus-ring`
(`_variables.css`). D139's new LoRA editor slider carries the literal
`class="qt-range w-full"` (v4 `LoraListEditor.tsx:145`) and ships no slider
CSS of its own. Landing order is irrelevant: the class is inert-until-both,
never broken, and the check-qt-classes guard does not police bare component
names.

**§C — the salon danger-styling flip (P4.D141 ↔ P4.66 → the unifier).** v4
`SalonView.tsx:1496` switched the message-list danger prop to
`shouldShowDangerStyling(chat)`; the v5 twin line is
`salon-conversation.ts:311`, a file P4.66 owns. Neither lane edits the
other's file: P4.D141 ships the client predicate module
`apps/web/src/app/chat/concierge-state.ts` (exports `getConciergeState`,
`shouldUseUncensoredRoute`, `shouldShowDangerStyling`, `isClassifierOnDuty`
— transcribed 1:1 from v4 `chat-override.ts` with a parity spec), and the
ONE-LINE binding flip at `salon-conversation.ts:311` is a NAMED UNIFICATION
WIRE (PENDING_CROSS_LANE, P4.D122 precedent) applied by the unifier with a
spec pin. Both P4.D141 and P4.66 record it loudly in their lane records.

**§D — `core-contract.ts` region scoping.** Three lanes touch it in
disjoint regions: P4.D139 adds the image-profile/LoRA/options-schema DTOs
(a new block, §A names verbatim), P4.D140 adds `createdAt` to the
`RecentChat` DTO, P4.D141 widens the two `conciergeOverride` unions
(:2574, :2891) to `'OFF' | 'UNCENSORED' | null`. No lane touches another's
region; the unifier diffs the contract name-for-name (playbook §D).

**§E — server same-file region split (P4.D138 ↔ P4.D141).**
`tools/generate_image.rs` and `services/story_background_job.rs` belong to
P4.D138 EXCEPT the dangerous-content predicate flips — one import line plus
one call each (`generate_image.rs:1337`, `story_background_job.rs:365`) —
which P4.D141 owns and must edit to keep its branch compiling. The unifier
cherry-picks **P4.D138 before P4.D141** and hand-resolves the import-block
unions; neither lane touches the other's regions in those two files.

## Ownership (binding, identical across all six orders)

| Area | Owner |
|---|---|
| `quilltap-core`: `image_gen.rs`, `image_gen_data.rs`, `model/image.rs`, `model/image_dialects.rs`, `api/image_profiles.rs`, `db/image_profiles.rs`, `services/image_job_common.rs`, `services/character_avatar_job.rs`, `services/image_scene_tasks.rs`, `api/wardrobe.rs` (preview region), the NEW lora/params/HF modules; `quilltap-host`: `avatar_preview.rs`; `tools/generate_image.rs` + `services/story_background_job.rs` per §E | **P4.D138** |
| SPA: `screens/settings/images/**` (modal, form, api, card, picker + specs), `screens/settings/providers/{provider-options-panel.ts,provider-options-schema.ts}` (+ specs), NEW LoRA editor/query components, `core-contract.ts` §D region | **P4.D139** |
| `quilltap-core`: NEW `chat_activity` module, `db/chats_messages.rs`, `db/chats_messages_read.rs`, `services/chat_enrichment.rs`, `services/home.rs`, `api/characters.rs`, `api/projects.rs`, `api/brahma.rs`, `tools/self_inventory.rs`, `services/backup/restore/orchestrator.rs`, NEW recompute-heal module (+ `db/mod.rs` line); `quilltap-host`: `host.rs` (the boot repair chain block); SPA: `screens/salon/chat-card.ts`, `chat/merge-conversation-modal.ts`, `screens/home/recent-chat-item.ts`, `screens/characters/view/tabs/character-conversation-card.ts`, `brahma/brahma-wire.ts`, `e2e/support/seed-archived-character.ts`, `core-contract.ts` §D region (+ the touched specs) | **P4.D140** |
| `quilltap-core`: `services/dangerous_content/**`, `services/concierge_notifications.rs`, `services/context_summary.rs`, `services/danger_scan.rs`, `services/orchestrator.rs`, `services/pre_compute.rs`, `services/title_update_job.rs`, `services/memory_extraction_job.rs`, `services/carina_memory_extraction.rs`, `pascal/llm_consult.rs`, `api/salon.rs` (the conciergeState arm), the §E flip lines in `tools/generate_image.rs` + `services/story_background_job.rs`; SPA: `chat/conversation-header.ts`, `chat/sidebar/chat-section.ts`, NEW `chat/concierge-state.ts`, `styles/qt-components/_chat.css`, `core-contract.ts` §D region (+ specs) | **P4.D141** |
| SPA: `styles/qt-components/{_interactive.css,_variables.css,_surfaces.css}`, the class-string edits in the eight slider host files (`chat/sidebar/participant-card.ts`, `memory/housekeeping-dialog.ts`, `memory/memory-editor.ts`, `screens/settings/chat/context-compression-settings.ts`, `screens/settings/chat/dangerous-content-settings.ts`, `screens/settings/memory/memory-dedup-card.ts`, `screens/settings/providers/profile-modal.ts`, `screens/settings/system/tasks-queue-card.ts`), `apps/web/scripts/check-qt-classes.mjs` (the host-class guard extension) + its spec/self-test | **P4.D142** |
| SPA: `screens/salon/salon-conversation.ts` (+ spec), `chat/message-list.ts` (+ spec), the NEW mid-turn e2e beat | **P4.66** |
| Playwright (port 4319) | **P4.66's branch only**; every other lane's beats are authored gated (ACTIVATE-AT-UNIFY) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it |

**Version bumps:** this lane bumps `quilltap-core`, `quilltap-harness`,
`quilltap-web` (if its route tests move), and the SPA per commit; the
unifier recounts at unification.

## Verification gate before declaring done

`cargo fmt --check`; `cargo clippy` both feature sets; full
`cargo test --workspace` with the lane's oracle env vars
(`run_in_background`, full log + sentinel — NEVER a pgrep loop, NEVER
`| tail`); the lane's families by name through the sweep driver, zero
SKIP, from the `60e3c4a0a` pin with changed-bytes greps; mutation proofs
verified-applied; `npm test` + `npm run build` in `apps/web`; NO
Playwright (the beat is gated). Per-commit rule:
`.claude/commands/commit.md`.
