# Work order P4.6ar — the llm-logs + system-aesthetics server lane (lane A): the LLM-Inspector read surface and the `system/image-aesthetics` routes

> **STATUS: OPEN.**
>
> Lane A of the THREE-lane P4.6ar ∥ P4.6as ∥ P4.6at round (the LLM-Inspector +
> Default-Aesthetics + minHeight round, planned 2026-07-15). **v4 reference
> baseline: HEAD `02865bdb`** (`~/source/quilltap-server`) — run
> `git log 02865bdb..HEAD --oneline` at lane start; **if HEAD moved, STOP and
> report.** Sibling lanes: `p4.6as-llm-inspector-spa.md` (the Salon inspector
> SPA consumer of §1 of the Shared contract),
> `p4.6at-aesthetics-card-minheight.md` (the Settings Default-Aesthetics card,
> consumer of §2, plus the no-server-contact minHeight rider). The **Shared
> contract** and **Ownership** sections below are binding and reproduced
> verbatim in all three orders. You run in a worktree (own Rust `target/`, per
> the parallel-round disk arrangement); the unification pass integrates the
> lanes onto main.

## Mandate

Deliver the two server surfaces the round's SPA lanes consume: **(1)** the
llm-logs read/delete surface — the eight repo read methods v4's
`/api/v1/llm-logs` routes use (v5's `db/llm_logs.rs` today has only
create/update/delete/find_last_by_chat_id and the usage sums), the three
dispatch verbs (`llmLogsList` / `llmLogGet` / `llmLogDelete`), and the REST
edges — and **(2)** the `system/image-aesthetics` GET/PUT surface (two verbs +
the REST edge), a thin sibling of the already-ported project aesthetic pair.
This lane owns `crates/**` and `harness/oracle/**`; it must NOT touch
`apps/web/**`. Every unit ships with its differential against v4's real code —
no exceptions. There are no refusal arms in this lane: the whole surface is
portable now (no LLM calls, no codecs, no fs watchers).

## Survey-verified starting points (2026-07-15 @ `02865bdb` — do not re-derive)

### Unit 1 — the llm-logs repo reads

**v4 repo** — `lib/database/repositories/llm-logs.repository.ts` (690 lines).
The read methods the routes use, with their v4 line numbers:

- `findByMessageId(messageId)` — :113
- `findByChatId(chatId)` — :134
- `findAllForChat(chatId, messageIds, limit = 500)` — :159 (the
  `includeMessages=true` arm: logs by chatId UNION logs linked via the chat's
  message ids)
- `findByCharacterId(characterId)` — :188
- `findStandalone(userId, limit = 50)` — :210 (no entity associations)
- `findByType(userId, type, limit = 50)` — :241
- `findRecent(userId, limit = 20)` — :265
- `findById(id)` — used by the item routes (find its line at lane start)

Port into `crates/quilltap-core/src/db/llm_logs.rs` (which already holds
`create`/`update`/`delete`/`find_last_by_chat_id` and the usage sums — follow
its row-mapping conventions). The log row deserializes the JSON columns
(`request`, `response`, `usage`, `cacheUsage`) — **key order matters** on the
way back out ([[json-column-key-order]]: typed structs in schema order, never a
sorted `Value`). Verify each method's `ORDER BY` from the v4 SQL rather than
assuming DESC (the SPA panel reverses client-side, which implies DESC, but the
differential is the proof).

### Unit 2 — the llm-logs routes → dispatch verbs + REST edges

**v4 list route** — `app/api/v1/llm-logs/route.ts` (92 lines), GET only.
Branch order (first match wins): `messageId` → `chatId` (ownership check via
`repos.chats.findById`, missing → `notFound('Chat')`; then
`includeMessages === 'true'` picks `findAllForChat` over the chat's message
ids, else `findByChatId`) → `characterId` (ownership check, missing →
`notFound('Character')`) → `standalone === 'true'` → `type` (cast, **not
validated** — any string reaches `findByType`) → default `findRecent`.

Quirks to carry faithfully (the differential enforces them):

- **Pagination is applied post-fetch**: `total` = pre-slice `logs.length`,
  then `offset > 0` slices, then `logs.length > limit` slices; `count` =
  post-slice length. The entity branches (`messageId`/`chatId`/`characterId`)
  fetch UNBOUNDED and only the route-level slice limits them, while
  `standalone`/`type`/recent pass `limit` into the repo AND get the
  route-level slice.
- **The limit parse**: `Math.min(parseInt(param || default, 10), cap)` with
  default 50 / cap 100 — or default 500 / cap 500 when
  `includeMessages === 'true'`. `parseInt('garbage')` is `NaN`, and
  `Math.min(NaN, cap)` is `NaN`, and `logs.length > NaN` is `false` — **a
  garbage limit disables the slice entirely**. Port the parse with the Phase-1
  JS-number-fidelity helpers so the NaN arm survives; the differential should
  include a garbage-limit case.
- `standalone` and `includeMessages` are strict `=== 'true'` string compares.
- Errors wrap in `serverError('Failed to list LLM logs')` via the
  try/catch.

**v4 item route** — `app/api/v1/llm-logs/[id]/route.ts` (67 lines). GET:
`findById` → `notFound('LLM Log')` or `successResponse(log)` (the raw log
object, not `{log}`). DELETE: `findById` → notFound; `delete(id)` → false is
`serverError('Failed to delete log')`; success is
`{ success: true, deletedId: logId }`. **Neither has an ownership check** —
any log reachable by id, any user. Faithful port; bank the observation in the
status-log record.

**Dispatch + engine plumbing**: the verbs read the llm-logs PARTITION. Survey
`api/engine.rs` for how existing verbs reach partitions (`ready_db`; the `Db`
carries `llm_logs` as an `Option` — the cost-background differential passes
`llm_logs: None`, so decide and pin the missing-partition behavior: v4 always
has the partition, so in v5 a missing partition on these verbs is a plain
engine error, not a v4-diffed arm). REST edges land in `quilltap-web` beside
the other route families — remember the P4.6ah rule: **REST legs must UNWRAP
the dispatch envelope** before writing the HTTP body.

### Unit 3 — the system image-aesthetics routes

**v4 route** — `app/api/v1/system/image-aesthetics/route.ts` (61 lines).
GET: `parseAestheticKind` (`lib/image-gen/aesthetic.ts:221` — `kind` must be
the literal `lantern` or `aurora`, else
`badRequest('Query param "kind" must be "lantern" or "aurora"')`); no general
mount (`getGeneralMountPointId` from `lib/instance-settings`) →
`successResponse({ content: '' })`; else `readAesthetic(mountId, kind)`
(:233 — absent/read-error coalesce to `''`).
PUT: same kind gate; no general mount →
`serverError('Quilltap General document store is not available')`;
`req.json().catch(() => ({}))` then
`aestheticContentSchema.safeParse(body).data?.content ?? ''` — **a malformed
PUT body parses to `{}`, yields `''`, and DELETES the file** (empty content
deletes, restoring fallback). Response `{ success: true }`. Carry that arm
faithfully.

**v5 already has almost everything**: `get_general_mount_point_id`
(`db/instance_settings.rs:80`), and the whole read/write plumbing inside
`api/projects.rs::project_aesthetic_get`/`project_aesthetic_set` (:847/:880)
over `services/aesthetics.rs`. The system pair is the same body with the
general-mount lookup swapped in for `get_project_official_mount_point_id`.
The filenames are `lantern-aesthetics.md` / `aurora-aesthetics.md`
(`lib/image-gen/aesthetic.ts:33-34`); files live in the Quilltap General
store's doc-mount tables. Factor shared helpers rather than duplicating the
project pair (SRP/DRY), but do not change the project verbs' behavior — their
existing differential must stay green untouched.

## Tiered deliverables

**Tier 1 (must land):**

1. The eight llm-logs repo read methods in `db/llm_logs.rs`, row mapping
   byte-faithful to v4's (JSON columns typed in schema order).
2. `LlmLogsList` / `LlmLogGet` / `LlmLogDelete` dispatch verbs (Shared
   contract §1) with v4's route bodies ported into the handlers (branch
   order, clamps, NaN quirk, post-fetch pagination, ownership checks on
   chat/character branches only).
3. REST edges: `GET /api/v1/llm-logs`, `GET/DELETE /api/v1/llm-logs/:id`
   (envelope unwrapped).
4. `SystemImageAestheticsGet` / `SystemImageAestheticsSet` verbs (Shared
   contract §2) + `GET/PUT /api/v1/system/image-aesthetics`.
5. The new committed `inspector-{main,mount,llm}.db` fixture family + its
   checked-in generator (see Fixtures).
6. Two route differentials over fresh jest oracles:
   `llm_logs_routes_equivalence` (≥ 18 cases — every list branch incl. both
   includeMessages defaults, the clamp above cap, a garbage limit, offset,
   offset+limit, chat/character notFound, item GET found/notFound, DELETE
   found/notFound, the cross-user-id read proving no ownership check) and
   `image_aesthetics_routes_equivalence` (≥ 8 cases — invalid kind on GET and
   PUT, GET absent file → `''`, GET existing file, PUT write → GET
   round-trip, PUT empty → delete → GET `''`, malformed-body PUT → delete,
   and the unprovisioned-store arms if the fixture can stage them without a
   second fixture family; if not, refuse loudly in the order's status header
   and bank it).

**Tier 2 (should land):** none — the tier-1 list is the whole surface.

**Tier 3 (explicit refusal-arm deferrals):** none. This lane must not create
any new refusal arms.

## The differential requirement (restated)

Every ported unit ships with an equivalence test driving v4's REAL code. Both
differentials here are tier-2 route diffs: a jest oracle in
`harness/oracle/` imports v4's real route handlers + repositories, runs the
case list against a copy of the fixture family, and emits NDJSON; the Rust
side (`crates/quilltap-harness/tests/`) replays the same cases through the
`quilltap-web` router over its own fixture copy and diffs field-by-field
(timestamps/minted UUIDs normalized per the standing remap machinery).
**Nearest recipes:** `cost_background_routes_equivalence` +
`text_replacements_web_routes` (route-level diff shape),
[[jest-real-db-oracle]] (resetModules + doMock past jest.setup's DB mocks;
Node 24), [[p4.6s-memories-server]] (SPLIT oracle files if jest contamination
bites), [[oracle-regen-from-worktree-path]] (tsx must import the WORKTREE
case file — verify by grepping the NDJSON for a new row),
[[oracle-jest-testtimeout]]. Regenerate oracles FRESH at `02865bdb` and run
the differentials by name before declaring done.

## Fixtures

This lane DELIVERS the round's one new fixture family:
`crates/quilltap-web/tests/fixtures/inspector-{main,mount,llm}.db`, generated
by a checked-in generator following the cost-background family's recipe
(pinned ids via `create(data, {id})` — [[p4.6k-groups-projects-server]]; the
mount sibling will exist whether you plan it or not, because
`repos.characters.create` mints vault mounts — the P4.6ao lesson). Contents:

- FIXTURE_USER + one chat with ≥ 3 messages + one character.
- llm-logs rows covering **all twelve v4 log types** (the
  `LLMInspectorEntry` badge table enumerates them: CHAT_MESSAGE,
  TOOL_CONTINUATION, MEMORY_EXTRACTION, TITLE_GENERATION, SUMMARIZATION,
  CONTEXT_COMPRESSION, IMAGE_PROMPT_CRAFTING, IMAGE_DESCRIPTION,
  APPEARANCE_RESOLUTION, DANGER_CLASSIFICATION, CHARACTER_WIZARD, AI_IMPORT),
  with: message-linked rows, standalone rows, one error-response row, rows
  with and without `usage`/`cacheUsage`/`durationMs`, and one row owned by a
  SECOND user id (the no-ownership-check case).
- The Quilltap General store provisioned with `general_mount_point_id` set,
  one pre-existing `lantern-aesthetics.md`, and NO aurora file (so both GET
  arms are covered without mutation ordering).

No existing oracle depends on this family (it is new). If you instead extend
an existing family, you own regenerating EVERY dependent oracle — prefer the
new family.

## Shared contract (BINDING — reproduced verbatim in all three orders)

Five new `Request` variants land in `crates/quilltap-core/src/api/types.rs`
(lane A owns the Rust side). The SPA lanes define matching LOCAL request types
in their own api modules (cast at the call site) — **neither SPA lane touches
`core-contract.ts`**; the unifier folds the types into the `CoreRequest`
union at unification (the P4.6ao precedent). Wire encoding is the standing
one: requests internally tagged (`#[serde(tag = "type", rename_all =
"camelCase")]`, fields camelCase); responses adjacently tagged
(`{"type": …, "data": …}`).

### §1 — the llm-logs verbs (lane A ⇄ lane B)

```rust
/// v4 `GET /api/v1/llm-logs` (all filter branches).
#[serde(rename_all = "camelCase")]
LlmLogsList {
    #[serde(default)] message_id: Option<String>,
    #[serde(default)] chat_id: Option<String>,
    #[serde(default)] character_id: Option<String>,
    /// v4's `type` query param. Wire name `logType` — a field literally
    /// named `type` would collide with the internally-tagged envelope key.
    /// The REST edge maps `?type=` onto it. NOT validated (v4 casts).
    #[serde(default)] log_type: Option<String>,
    #[serde(default)] standalone: bool,
    #[serde(default)] include_messages: bool,
    /// Raw query strings; the HANDLER ports v4's parseInt/Math.min body
    /// (including the NaN no-slice quirk) so dispatch and REST agree.
    #[serde(default)] limit: Option<String>,
    #[serde(default)] offset: Option<String>,
},
/// v4 `GET /api/v1/llm-logs/[id]`.
#[serde(rename_all = "camelCase")]
LlmLogGet { id: String },
/// v4 `DELETE /api/v1/llm-logs/[id]`.
#[serde(rename_all = "camelCase")]
LlmLogDelete { id: String },
```

Response payloads (the `data` member):

- `llmLogsList` → `{ "logs": [...], "count": n, "total": n, "limit": n,
  "offset": n }` — v4's exact envelope: `total` pre-slice, `count`
  post-slice.
- `llmLogGet` → the raw log object itself (v4 `successResponse(log)`, no
  wrapper key).
- `llmLogDelete` → `{ "success": true, "deletedId": "<id>" }`.

Lane B's client sends exactly what v4 `useLLMLogs` sends:
`{ type: "llmLogsList", chatId, includeMessages: true }` — nothing else.
Response-variant Rust names follow the existing camelCase-tag convention so
the wire tags read `llmLogsList` etc.; lane B mocks against these tags and
the unifier's activation must not need renames.

### §2 — the system image-aesthetics verbs (lane A ⇄ lane C)

```rust
/// v4 `GET /api/v1/system/image-aesthetics?kind=lantern|aurora`.
#[serde(rename_all = "camelCase")]
SystemImageAestheticsGet { kind: String },
/// v4 `PUT /api/v1/system/image-aesthetics?kind=…`; empty or absent
/// content deletes the file (restores fallback).
#[serde(rename_all = "camelCase")]
SystemImageAestheticsSet {
    kind: String,
    #[serde(default)] content: Option<String>,
},
```

Responses mirror the `ProjectAestheticGet`/`Set` pair byte-for-byte:
get → `{ "content": "<markdown or ''>" }`; set → `{ "success": true }`.

Pinned error semantics (both transports): invalid kind → badRequest
`'Query param "kind" must be "lantern" or "aurora"'`; GET with no general
store → success `{content: ''}`; PUT with no general store → serverError
`'Quilltap General document store is not available'`; malformed PUT body →
content `''` → deletes the file.

## Ownership (BINDING — reproduced verbatim in all three orders)

| Lane | Sole writer of | Must NOT touch |
|---|---|---|
| A (P4.6ar) | `crates/**`, `harness/oracle/**`, the new `inspector-*` fixture family | `apps/web/**` |
| B (P4.6as) | `apps/web/src/app/chat/**`, `apps/web/src/app/ui/slide-over-panel.ts` (new), `apps/web/src/app/screens/salon/**`, `apps/web/e2e/global-setup.ts`, the salon/m4 e2e specs + its own new spec | `crates/**`, `harness/**`, `screens/settings/**`, `screens/characters/**`, `screens/prospero/**`, `src/app/memory/**`, `core-contract.ts`, `e2e/settings-flow.spec.ts` |
| C (P4.6at) | `apps/web/src/app/screens/settings/images/**`, `apps/web/src/app/ui/aesthetic-editor-field.ts` (new), `screens/prospero/cards/project-aesthetic-field.ts`, `screens/characters/new/new-character.ts`, `screens/characters/edit/details-tab.ts`, `src/app/memory/memory-editor.ts` (each + its spec), `apps/web/e2e/settings-flow.spec.ts` | `crates/**`, `harness/**`, `src/app/chat/**`, `screens/salon/**`, `core-contract.ts`, `e2e/global-setup.ts`, the salon e2e specs |

- `docs/CHANGELOG.md` + `docs/developer/porting/status-log.md`: append-only
  for everyone; union-merge at unification.
- `core-contract.ts`: NOBODY this round — the SPA lanes keep local request
  types + casts in their api modules; the unifier folds them (P4.6ao
  precedent).
- Version bumps: lane A bumps `quilltap-core`, `quilltap-web`,
  `quilltap-harness` (leave `quilltap-host` alone — no new host seams this
  round). Lanes B and C each bump the SPA `apps/web/package.json` patch
  version; the unifier accumulates and syncs the lockfile's own version
  fields (the P4.6an lesson).
- Playwright contention: lanes B and C must not run the e2e suite
  concurrently (shared port 4319 — the P4.6af finding). Stagger, or treat
  the unifier's full run as the joint proof.
- ACTIVATE-AT-UNIFY: lane B's inspector beat and lane C's aesthetics beat
  mock their new verbs at the Playwright route layer, tagged
  `ACTIVATE-AT-UNIFY`; the unifier deletes the mocks and runs them live over
  lane A's routes.

## Verification gate (before declaring the lane done)

1. `cargo fmt --all --check` clean.
2. `cargo clippy --workspace --all-targets -- -D warnings` on BOTH feature
   sets (default and `quilltap-core/native-transport`).
3. `cargo test --workspace` — zero failures, including the no-env harness
   self-test.
4. Both new differentials run BY NAME over oracles regenerated fresh from
   `~/source/quilltap-server` at `02865bdb`, with the regen commands recorded
   in the status-log entry. Run them `--nocapture` and grep for SKIP (the
   P4.d3 lesson) — a skipped differential proves nothing.
5. Every commit follows `.claude/commands/commit.md` (version bumps per the
   Ownership section; CHANGELOG + status-log entries).
