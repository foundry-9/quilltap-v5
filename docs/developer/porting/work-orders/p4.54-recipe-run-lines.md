# Work order P4.54 — run lines for the `nothing_to_run` families — maintenance

**Status:** CLOSED (2026-08-21) — all Tier-1 and Tier-2 deliverables landed on
`claude/recipe-run-lines-porting-e2e17c`; the Tier-3 handoff (P4.D97's four
envelope families) is named in the lane record and in the artifact's
`p4_54_classification` block.

## Preamble

- **v4 baseline for this round: `12fe3e6f`** (v4 main, HEAD at planning
  2026-08-21 evening). This lane ports NO v4 behavior — it repairs the
  vacuous-green debt P4.53 measured and committed. It should need NO
  oracle regen; if a family's run genuinely cannot execute without a
  fresh oracle, pin a detached v4 worktree at `12fe3e6f` for that regen
  (all three symlink classes) and record why.
- **Drift-check at lane start** anyway (`git log 12fe3e6f..main
  --oneline` + the bugfix diff) — if v4 moved, report with the lane
  close; this lane need not STOP (nothing here tracks v4 bytes), but
  the round's unifier must know.
- **Sibling lanes (disjoint ownership):** P4.D97 (server drift —
  owns the four request/response envelope families and the files its
  order names), P4.D98 / P4.D99 (SPA — no harness files). See
  Ownership.
- **Worktree:** per the `carryout` skill; edit inside the worktree,
  not main.

## Mandate

Retire the `nothing_to_run` refusal across the P4.53 inventory: give
every family that should have one a scoped, self-contained run line in
its recipe header, and give every family that correctly has none an
explicit recorded disposition. Do NOT modify the sweep driver's logic
(P4.53 closed; only its committed artifact grows a successor), do NOT
touch any family P4.D97 owns, and do NOT weaken any recipe (a run line
is additive; recording stages stay non-runnable by the driver's
design).

## The inventory (committed, measured 2026-08-20 at `b8449b3e`)

`harness/tools/sweep-results/2026-08-20-b8449b3e-p4.53-nothing-to-run.json`
— 39 families under `nothing_to_run` (plus `brahma_console_routes_
equivalence` ok). CLAUDE.md's P4.53 bullet estimates ~34 want a scoped
`cargo test` run line; the estimate is not the deliverable — the
classification is.

**EXCLUDED from this lane (P4.D97 owns their files this round):**
`request_builder_equivalence`, `request_builder_google_equivalence`,
`request_builder_google_wire_equivalence`, `response_parse_equivalence`
— their recipes live in/around `record-request-envelopes.mjs` /
`regenerate-request-envelopes.sh`, which P4.D97 edits. Additionally:
before editing ANY family's recipe file, check it is not named in
P4.D97's Ownership section; if it is, skip the family and record the
skip in the lane record (the unifier will queue it).

**In scope (35):** `p4_6ao_wire_contract`, `p4_6ar_wire_contract`,
`p4_9g1_wire_contract`, `p4_9g6_seam_contract`, `p4_d10_wire_contract`,
`p4_6ay_workbench_wire_contract`, `web_search_runner_wire`,
`binary_routes`, `change_passphrase_archive_sweep`,
`characters_action_route`, `characters_export_route`,
`characters_import_route`, `characters_photos_routes`,
`characters_reset_builtins_route`, `characters_wardrobe_route`,
`chat_create_end_to_end`, `chat_send_smoke`,
`chat_settings_composer_web_routes`, `contract`,
`file_content_missing_404`, `files_write_routes`,
`mount_multipart_routes`, `photos_web_routes`, `profile_web_routes`,
`terminal_ws`, `text_replacements_web_routes`, `cli_differential`,
`image_dialects_equivalence`, `moderation_wire_equivalence`,
`restore_vintage_state`, `stream_decoders_equivalence`,
`streaming_composer_equivalence`, `tool_wire_call_site`,
`tool_wire_equivalence`, `web_search_wire_equivalence`.

## Survey-verified starting points

- The driver: `harness/tools/recipe_sweep.py` — recipes are classified
  by INDENTATION (P4.45), checkout aliases are unforgeable (P4.53),
  `--run` refuses empty-stage families by name with exit 2 (P4.53),
  recording stages are never run and tracked-fixture writes warn
  (P4.47). `--self-test` must stay green. **Do not change driver
  logic.** Locate each family's recipe home the way the driver does —
  its classification is the ground truth for where the header lives.
- The conventions: the recipe-header-conventions memory note (read
  BEFORE editing any header), the harness-recipes-are-runnable rule
  (a run line must be executable verbatim from a fresh checkout —
  self-contained env: `QT_FIXTURE_*`/`QT_ORACLE_*` vars inline on the
  line, repo-relative paths, no `/tmp` pins, no reliance on a sibling
  recipe's staging), and the sweep-recipes-write-into-the-repo caution
  (some recipes MUTATE committed fixtures — a run line must not).
- The known shapes in the inventory: many are `quilltap-web` test
  binaries (`cargo test -p quilltap-web --test <file> [filter]` — the
  workspace-suite copies of some families SKIP silently without their
  `QT_FIXTURE_*` vars, the 0.00s tell; a run line must carry the vars
  so the run is REAL, and where a family can run vacuously, pair the
  line with a `--nocapture` row-count or `test result:` check per the
  b8449b3e-round memory), several are harness differentials with
  committed corpora (`cargo test -p quilltap-harness --test <name>`
  plus their env), `cli_differential` is the Tier R runner,
  `terminal_ws`/`contract` are web integration suites, and the
  `p4_*_wire_contract` scripts are name-for-name contract diffs whose
  "run" may be a node/tsx invocation rather than cargo — classify
  honestly; a non-cargo run line is fine if the driver can execute it,
  and a genuinely non-runnable contract check (pure documentation
  diff) is a DISPOSITION, not a forced line.

## Tiered deliverables

**Tier 1 (must land):**

1. A classification row for all 35: run-line-added |
   correctly-headerless (with the reason) | skipped-for-ownership.
2. The added run lines, each PROVEN by an actual
   `recipe_sweep.py --run <family>` execution from the lane worktree
   — exit 0, and for every family capable of a silent skip, evidence
   the tests actually ran (row counts / `test result:` lines, never
   a bare exit code).
3. `--self-test` green after all edits.
4. A successor artifact committed to `harness/tools/sweep-results/`
   (dated, labeled, with the classification and per-family results),
   same shape as the P4.53 artifact.

**Tier 2 (should land):**

5. Where a run line exposes a REAL standing red (not a
   missing-oracle artifact), do not fix the family in this lane —
   record it loudly in the lane record as new debt with the measured
   evidence (the P4.D32 sweep-debt precedent).

**Tier 3 (explicit loud deferrals):**

6. The four excluded envelope families → named handoff to the
   unifier (P4.D97's regen already exercises them this round; their
   run lines can ride a later maintenance pass).

## The differential requirement, restated

This lane ports nothing; its proof obligation is EXECUTION, not
equivalence: every added run line demonstrably runs the family's real
diff (zero SKIP, non-vacuous), and no recipe's oracle/recording stages
are altered. Any oracle regen it is forced into uses the `12fe3e6f`
pin through the driver.

## Fixtures

Consumes committed fixtures only; must not mutate any (watch the
recipes that write into the repo — a run line that triggers such a
stage is wrong by construction).

## Shared contract

**This lane meets its siblings only procedurally** (binding, in all
four orders): it never edits files P4.D97's Ownership names; the four
envelope families are excluded here; `CHANGELOG` and `status-log.md`
are append-only (union-merge).

## Ownership

- **This lane owns:** the recipe HEADER lines of the 35 in-scope
  families (wherever the driver resolves them), the new sweep-results
  artifact.
- **Must NOT touch:** `harness/tools/recipe_sweep.py` logic (comment
  fixes allowed only if `--self-test` still passes and the diff is
  comment-only), any file in P4.D97's Ownership list, anything under
  `apps/web/**`, any committed fixture or corpus, any test's
  assertion logic.
- **Version bumps:** none expected (headers + artifact are not
  compiled code). If a `.rs` test-file header edit forces a rebuild,
  that alone does not warrant a bump; bump `quilltap-harness` only
  if an actual code change proves necessary — and record why.

## Verification gate (before declaring done)

- `recipe_sweep.py --self-test` → 0 failures.
- Every added run line executed from the lane worktree, exit 0,
  non-vacuous (evidence in the artifact).
- `cargo test --workspace` only if any compiled file changed
  (background + sentinel per the background-gate memory; NEVER a
  pgrep poll loop).
- Per-commit rule: `.claude/commands/commit.md`.
