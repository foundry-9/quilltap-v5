# Work order P4.76 — the P4.73 remainder: `POST /api/v1/images?action=generate` end-to-end, P4.62(a)'s FILES leg, and the five recorded review items

**Status:** OPEN (ordered 2026-09-05, the `p4.9i2` help/HelpChat round:
P4.9I2A ∥ P4.9I2B ∥ P4.76 ∥ P4.77). The unifier updates this header.

## Preamble

- **v4 oracle baseline: `d883a5ee1`** (drift-ledger §1). v4 `main` HEAD is
  `c2232cd9a`, four docs/version-only commits past (ledger §3, all
  `ORDERED(P4.77)`); `app/api/v1/images/route.ts` and everything under
  `lib/` are byte-identical between the baseline and HEAD.
- **Freshness probe at lane start** (ledger §2): checkout on **`bugfix`**
  (recorded; never switch it), tree clean, `git -C ~/source/quilltap-server
  log --oneline c2232cd9a..main` empty, `git … log --oneline
  2b49f51aa..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED at `d883a5ee1`.** Every regen from
  **`/tmp/qt-v4-pin-p476-d883a5ee1`** (three symlink classes, ledger §5.1)
  through `recipe_sweep.py --v4 <pin> --run <family>`. The images oracle
  loads provider plugins (`createImageProvider`) — the third symlink class
  (`plugins/dist/*/node_modules`) is load-bearing here.
- **Sibling lanes:** P4.9I2A (the help server — you share three fenced
  files, §S.1; it owns `quilltap-web/src/lib.rs` and `quilltap-host/src/
  spine.rs`, you touch neither), P4.9I2B (SPA — no overlap), P4.77
  (maintenance — it consolidates the 19 `CaptureLayer` copies; none of them
  sit in your files, and your new capture tests use your own local layer
  which the unifier folds). §S and the Ownership table are binding and
  identical across all four orders.
- **Worktree:** per the `carryout` skill (branch `claude/p4-76-…`); edit by
  absolute worktree path; `cd <worktree>` before every cargo command
  (memory note `p4.9i1a-brahma-server-lane`'s cwd trap). **No Playwright**
  (§S.5). ⚠ `activity_span_sites_guard`'s absence check is a naked
  substring search for `handle_generate_image` over EVERY `.rs` under
  `crates/` — its census row moves in the SAME commit that introduces the
  name (the P4.73 lane record's trap; memory note
  `the-load-bearing-site-may-not-be-the-obvious-one`).

## Mandate

Close P4.73 whole. Tier 1 item 3 of that order — `POST /api/v1/images?
action=generate` — was never started; both its tripwires are still ARMED on
purpose and the edge answers a NAMED refusal. Land the route as v4's own
route-level implementation (NOT a call into the Salon's `generate_image`
tool — P4.73's instruction stands), over a new erased image-GENERATION host
seam; then P4.62(a)'s FILES leg in its CORRECTED shape; then the five items
the P4.73 unification review recorded. **Must NOT touch:** `apps/web/**`,
P4.9I2A's files (`api/help_*`, `services/help_chat/**`, `help_routes.rs`,
`lib.rs`, `spine.rs`, `files_store.rs`, the host `build.rs`), the Salon
`generate_image` tool's behaviour (`tools/generate_image.rs` — a visibility
widening of `build_cheap_llm_selection` is the ONLY permitted edit there,
and only if you choose widening over duplication; say which and why), and
the `images_routes_equivalence` rows P4.73 landed (append arms; never
re-shape a green row).

## Survey-verified starting points (2026-09-05, from the P4.73 lane record + the shipped v4 file — do not re-derive)

Full survey: `status-log.md` → "Lane record — P4.73" → "Tier 1 item 3 …
survey" (`:103770-103805` at ordering). Restated with the anchors:

- **v4 `handleGenerateImage`** — `app/api/v1/images/route.ts:177-408` at the
  pin. Its own Zod body (read the hunk — count/size/format/prompt; NOT the
  image-profile route's schema P4.70 landed and NOT the tool's schema). Its
  Concierge gate is **`scanImagePrompts` with NO chat**; its reroute picks
  **the first `isDangerousCompatible` image profile** rather than consulting
  the Concierge desk; it resolves **NO orientation** (`params_builder`'s
  `orientation: None` arm exists for exactly this caller); filenames are
  `generated_<Date.now()>_<index>_<sha8>.webp` — **the oracle must FREEZE
  `Date`** or every row is unstable; the written file lands through
  `write_lantern_background_to_mount_store` (subfolder `tool/`) with
  `get_inherited_tags`; the api key through the resolver.
- **What v5 is missing (measured by P4.73):**
  1. **An erased image-GENERATION seam.** `ErasedImageDiscovery`
     (`model/image.rs:253`) exposes only `available_models`;
     `ErasedImageGeneration` (`tools/generate_image.rs:437`) runs the whole
     tool. Add an object-safe wrapper over `ImageProvider::generate_image`
     in `model/image.rs` in the `ErasedImageDiscovery` shape, wired in the
     host over `RealImageProvider` (a new host file + a fenced registration
     in `host.rs`, §S.1).
  2. **The Concierge stack from a route handler.** `classify_content`
     (`dangerous_content/gatekeeper.rs:413`) takes `ModerationProvider` +
     `CompletionProvider` + a `CheapLlmSelection`; today only the tool
     runner holds them. `build_cheap_llm_selection`
     (`generate_image.rs:1422`) is a private three-line `fn` — widen to
     `pub(crate)` or duplicate; decide deliberately and record.
  3. **The oracle:** `createImageProvider` mocked with a recorder (the
     seams at `__tests__/unit/app/api/v1/images/image-generate-route.test.ts:
     212-284`), the classification canned, `logLLMCall` no-op'd, `Date`
     frozen.
- **Everything else exists:** `build_image_gen_params` (with the
  `orientation: None` arm), `write_lantern_background_to_mount_store`,
  `get_inherited_tags`, the api-key resolver, `images_routes.rs` (P4.73's
  edge — `?action=generate` currently answers the named refusal; the two
  ARMED tripwires are named in that file's header and in
  `activity_span_sites_guard`), the committed `images-{main,mount}.db` pair
  and `images_routes_equivalence` (27 + 5 cases).
- **P4.62(a)'s FILES leg — the CORRECTED shape** (P4.73 lane record, "Tier 2
  item 8"): the order's raw-carry premise was refuted on both legs; the
  files leg needs `Request::FileUpload.tags` widened, `save_file_entry`
  (`api/files.rs:1200`) validating as `create_file_conns` now does, and
  v4's **500 `Failed to upload file`** (its route catches where the images
  route does not — `app/api/v1/files/route.ts` at the pin; measure the exact
  arm).
- **The five recorded items** (P4.73 unification header):
  (a) the `{id}` DELETE's orphan cleanup writes `characters` rows raw where
  v4's `validateCharacterArchivePatch` would THROW on an ARCHIVED character
  (the fixture seeds none — add one); (b) `zod_url_ok` refuses
  authority-less schemes that v4's `new URL()` accepts (measure v4 with a
  `mailto:`/`data:` import URL — what does the route DO with it? the fetch
  fails; pin the status + sentence); (c) v4's `[Images v1] Image uploaded` /
  `imported from URL` info lines are unported (capture-layer presence pins;
  memory note `differential-blind-to-a-log-only-fix`); (d) the JSON body
  read is unbounded (v4's Next body limit — measure and match, or record the
  divergence with the number); (e) the oracle's `cannedFetch` is
  order-dependent across cases (make it keyed).

## Tiered deliverables

### Tier 1 — must land

1. **The erased generation seam** in `model/image.rs` + the host wiring
   (new `crates/quilltap-host/src/image_generation.rs` or the tree's
   equivalent; the `host.rs` registration inside the `// === P4.76 ===`
   fence). A `Request::ImagesGenerate` dispatched with no seam registered
   answers a NAMED typed refusal.
2. **`Request::ImagesGenerate { … }`** (fenced append in `api/types.rs`,
   the fields v4's body schema reads, decoded THROUGH the enum — memory note
   `edge-must-decode-through-the-request-enum`), the handler in
   `api/images.rs` transcribed from `route.ts:177-408` (every sentence,
   every status, the guard ORDER — Zod first or 404 first, measured), the
   `engine.rs` arm (fenced), the `?action=generate` arm in
   `images_routes.rs` replacing the refusal, **both tripwires retired + the
   `activity_span_sites_guard` census row moved in the same commit**, the
   `api.v1.images.generate` activity anchor landing where the guard expects
   it.
3. **`images_generate_route_equivalence` (tier 3, NEW):** the oracle at the
   pin with the recorder seams above, `Date` frozen; arms: a plain
   1-image generate (params + the written file's name/tags/subfolder +
   the `files` row + the receipt); `count: 3` (the `_index_` filenames);
   the Concierge gate refusing (canned classification → v4's status +
   sentence); the reroute to the first `isDangerousCompatible` profile
   (params recorded on the SECOND provider); a provider throw → v4's
   sentence; the Zod refusals (count over cap; bad size; bad format; empty
   prompt; wrong-type body); no image profile configured. Comparands: the
   ordered provider `recordings`, the `files` rows (path-keyed, minted ids
   remapped), the mount-store blob rows, the response body.
4. **P4.62(a)'s FILES leg** — `Request::FileUpload.tags` widened,
   `save_file_entry` validating as `create_file_conns`, v4's 500 sentence;
   arms appended to the family that already covers the images leg (or a
   `files_upload_tags_*` sibling) — the raw-`tagId` / invalid-uuid / the
   500 arm, red-first.

### Tier 2 — should land

5. The five recorded items (a)–(e), each with its pin (a: an ARCHIVED
   character in the fixture + the DELETE arm answering v4's throw shape;
   b: the measured status; c: capture-layer presence; d: the bound or the
   recorded divergence; e: the keyed `cannedFetch` with a two-case
   order-swap proof).
6. `handler-logging-inventory.md` rows for the generate route's log lines.

### Tier 3 — explicit refusal-arm deferrals

- None expected. If the Concierge classification cannot be reached from a
  route handler without a seam the locked boundary forbids, STOP and flag
  before inventing one — the P4.73 record says the stack is reachable
  (`classify_content`'s three inputs are all constructible host-side).

## The differential requirement (restated)

Every ported unit ships with an equivalence test driving v4's REAL code. The
generate route is tier 3 (the image provider mocked BELOW v4's real route
handler — memory note `tier3-mock-level-vs-provider-internal`; the canned
classification per `tier3-corpus-provider-throw-via-canned-failure` for the
throw arm); the files leg and the five items extend the existing tier-2
`images_routes_equivalence` idiom (real-DB jest route oracle; memory note
`jest-real-db-oracle`). Run by NAME from the pin, `--nocapture`, zero
`SKIP:`. Mutation proofs: the frozen clock un-frozen (must redden every
generate row — proves the comparand sees filenames); the reroute reading
the DESK instead of the first compatible profile; the `orientation` arm
flipped to `Some`; the tags validation removed on the files leg.

## Fixtures

- **Consumes and EXTENDS** the committed `images-{main,mount}.db` pair
  (P4.73's): add ≥ 2 image profiles (one `isDangerousCompatible`), an
  ARCHIVED character owning a file (item a), and whatever the generate arms
  need. You own regenerating every oracle that consumes the pair
  (`images_routes_equivalence` + the new family). Widen in place (memory
  note `fixture-rebuild-vs-migrate-in-place`); the P4.73 arms must stay
  green unchanged.

## §S — the round-wide meeting points (BINDING — identical in all four orders)

1. **Fenced-append shared files.** `crates/quilltap-core/src/api/types.rs`,
   `crates/quilltap-core/src/api/engine.rs`, and
   `crates/quilltap-host/src/host.rs` are edited by BOTH Rust lanes
   (P4.9I2A, P4.76) — each lane appends ONLY inside its own fence
   (`// === P4.9I2A ===` / `// === P4.76 ===`, opened once per file, new
   variants/arms/registrations added at the END of the relevant block,
   never interleaved with existing lines). `crates/quilltap-web/src/lib.rs`
   is P4.9I2A's alone (P4.76 registers nothing new — `?action=generate` is
   an action on an existing edge). `crates/quilltap-host/src/spine.rs` is
   P4.9I2A's alone. `Cargo.toml`/`Cargo.lock` at the workspace root: version
   lines only (§S.4); a dependency add is a STOP-and-flag. P4.77 touches
   none of these.
2. **Frozen files.** `docs/developer/porting/drift-ledger.md` (lanes never
   write it), `apps/web/src/app/core/core-contract.ts` (folded at unify —
   §S.3), `apps/web/src/app/shell/shell.ts` (UNIFIER-ONLY: the P4.9I2B
   `<qt-help-entry />` mount lands there at unification, BEFORE
   `<qt-brahma-entry />` — v4's footer order `sidebar-footer.tsx:203` then
   `:213`), `apps/web/src/app/workspace/workspace-contract.ts` (v4's help is
   a dialog, not a tab kind — no registry row).
3. **`core-contract.ts` folds at unify.** P4.9I2B declares the §B DTOs in
   `apps/web/src/app/help/help-wire.ts`; the unifier folds them and runs the
   name-for-name wire diff against `api/types.rs`.
4. **Version bumps.** P4.9I2A: `quilltap-core`, `quilltap-harness`,
   `quilltap-web`, `quilltap-host` per commit. P4.76: `quilltap-core`,
   `quilltap-harness`, `quilltap-web`, and `quilltap-host` (its seam).
   P4.77: `quilltap-harness` (+ `quilltap-core` for the test helper). P4.9I2B:
   `apps/web/package.json` per commit. The unifier RECOUNTS — identical
   bumps merge silently (memory note `commit-checklist`; the "silent
   auto-merge trap" in the playbook).
5. **e2e.** Only P4.9I2B runs Playwright and it adds NEW spec files only
   (`help-*.spec.ts`), with beats that depend on the server verbs or the
   shell mount shipped as guarded ACTIVATE-AT-UNIFY probes that report their
   guarded state loudly. The three other lanes: `git diff main -- apps/web/`
   empty. Port 4319 is repo-wide — one Playwright at a time.
6. **Append-only for everyone:** `docs/CHANGELOG.md`,
   `docs/developer/porting/status-log.md`.
7. **The regen rule for every lane:** PIN REQUIRED at `d883a5ee1`, a
   LANE-UNIQUE pin path (`/tmp/qt-v4-pin-<order>-d883a5ee1`), every regen
   through `recipe_sweep.py --v4 <pin>`; the checkout stays on `bugfix`
   untouched. The unifier moves the baseline to `c2232cd9a` (a pure
   ratification, P4.77's evidence) and the rule returns to PIN NOT REQUIRED
   only once the human has put the checkout back on `main`.
8. **The `p4.9i2` bank discharge:** P4.9I2A's vendored `help/` at the pin
   discharges every banked help-copy note (phase-4.md's five `### p4.9i2
   bank` sections and the m6 §1.6 row-11 notes); P4.9I2B's Guide tab
   discharges the P4.D77 bank's half (b). The unifier marks them.

## Ownership (BINDING — identical table in all four orders)

| Lane | Owns (edit freely) | Fenced-append (§S.1) | Must not touch |
| --- | --- | --- | --- |
| **P4.9I2A** | `help/**` (new, vendored); `crates/quilltap-host/build.rs` + `Cargo.toml` (host crate only) + `src/files_store.rs` + a new `src/help_content.rs`; `crates/quilltap-core/src/services/help_chat/**` (new); `crates/quilltap-core/src/api/{help_docs,help_chats}.rs` (new); NEW functions appended to `db/help_search.rs` / `db/help_docs.rs`; `crates/quilltap-web/src/help_routes.rs` (new) + `lib.rs`; `crates/quilltap-host/src/spine.rs`; new harness families + oracle cases + the `help-chat-*` fixture; `handler-logging-inventory.md` rows; this order's header | `api/types.rs`, `api/engine.rs`, `host.rs` | `apps/web/**`; P4.76's files; `services/brahma_console/**`; the 19 `CaptureLayer` sites |
| **P4.9I2B** | `apps/web/src/app/help/**` (new); `_surfaces.css`'s `qt-help-*` block (extend only); new e2e specs + `e2e/support/seed-help-fixture.ts`; `apps/web/package.json`; this order's header | — | `crates/**`; `shell.ts`; `core-contract.ts`; `workspace-contract.ts`; any existing spec |
| **P4.76** | `crates/quilltap-core/src/api/images.rs`; `api/files.rs` (the upload path: `save_file_entry` + `FileUpload.tags`); `model/image.rs`; `tools/generate_image.rs` (visibility widening only); `crates/quilltap-web/src/{images_routes,files_routes}.rs`; a new host image-generation seam file; `crates/quilltap-harness/tests/{images_routes_equivalence,activity_span_sites_guard,web_edge_body_parse_guard}.rs` (+ a new generate family); `harness/oracle/cases/images-*.ts`; the `images-*` fixture; this order's header | `api/types.rs`, `api/engine.rs`, `host.rs` | `apps/web/**`; P4.9I2A's files; `lib.rs`; `spine.rs` |
| **P4.77** | `crates/quilltap-harness/tests/zod_version_guard.rs` (new); `docs/v4/releases/4.9.0.md`; a new core test-support module + the 19 named `CaptureLayer` sites (listed at lane start, none in another lane's files); `crates/quilltap-core/src/pascal/custom_tools.rs` (`render_template` log lines only); this order's header | — | everything in the other three lanes' columns; `api/types.rs`; `engine.rs`; `host.rs` |

Everyone: `docs/CHANGELOG.md` + `status-log.md` append-only; the drift ledger
frozen; `core-contract.ts`/`shell.ts` frozen.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
   -D warnings` in BOTH feature sets; `cargo build --workspace`; the release
   build.
2. `cargo test --workspace` (full log + sentinel — never `| tail`) with this
   lane's env block (`QT_ORACLE_IMAGES_*` + `QT_FIXTURE_IMAGES_*` incl. the
   new generate family; the `activity_span_sites_guard` and
   `web_edge_body_parse_guard` censuses green); grep the log for `SKIP:` —
   zero for the named families.
3. The families by NAME through `recipe_sweep.py --v4 /tmp/qt-v4-pin-p476-
   d883a5ee1 --run <family>`, oracles fresh at the pin, zero SKIP.
4. The mutation proofs named above, each in the lane record with the arm it
   reddened.
5. `git diff main -- apps/web/ crates/quilltap-web/src/lib.rs
   crates/quilltap-host/src/spine.rs` empty.
6. Commits per `.claude/commands/commit.md` (core + harness + web + host
   bumps; CHANGELOG every commit); the lane record appended to
   `status-log.md` (💸 the generate route spends real money once a real
   provider is configured — flag it); P4.73's status header updated to
   CLOSED WHOLE (you own that header this round) and this order's header.
