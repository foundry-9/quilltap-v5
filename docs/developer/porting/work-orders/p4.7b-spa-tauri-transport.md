# Work order P4.7b — the SPA Tauri transport (lane B): the D14 seam made real

> **STATUS: CLOSED — UNIFIED on main (2026-07-16).** ALL tier-1 + tier-2
> deliverables LANDED (SPA 0.5.122 → 0.5.126): the `CoreTransport` split
> under `CoreClient` (HTTP frozen byte-for-byte; `interpretHealth` +
> `parseEventData` shared so the transports cannot fork), the Tauri
> transport (§1 invoke dispatch/health, §2 listen-THEN-attach with the
> backlog-ordering spec, resync → `resyncCounter`), bootstrap selection
> via `isTauri()` (the ONLY static `@tauri-apps/api` import; everything
> else rides the lazy `loadTauriIpc()` gateway — the built main bundle
> greps ZERO `__TAURI_INTERNALS__`), the §3 `apiUrl(path)` resolver
> adopted at EVERY raw REST/byte site in the closed inventory, and the §4
> `TerminalStreamTransport` seam (WS lifecycle byte-for-byte; the Tauri
> pipe routes an attach failure into the SAME service reconnect path as a
> 1006 close). Unit tier: +43 specs over `mockIPC(cb, {shouldMockEvents:
> true})` (the full event plumbing works in jsdom — no hand-rolled listen
> seam needed at 2.11.1). Frozen-path proof: full Playwright 63/63 zero
> skips, no locator edits. At unification the §1–§4 names diffed clean
> against lane A's commands; the §4 live pairing rides **the staged human
> M5 walk** (`~/qt-m5-instance`) — until walked, the pane's Tauri path is
> proven against mocks only. Tier-3 deferrals stand as ordered.
>
> **Original status (for reference): OPEN.**
>
> Lane B of the TWO-lane P4.7a ∥ P4.7b round (the Tauri round, planned
> 2026-07-15 — milestone **M5**). **v4 reference baseline: HEAD `02865bdb`**
> (`~/source/quilltap-server`) — run `git log 02865bdb..HEAD --oneline` at lane
> start; **if HEAD moved, STOP and report.** Sibling lane:
> `p4.7a-tauri-shell.md` (the Rust shell exposing every seam this lane
> consumes). The **Shared contract** and **Ownership** sections below are
> binding and reproduced verbatim in both orders. You run in a worktree; the
> unification pass integrates the lanes onto main and runs the cross-lane M5
> walk.

## Mandate

Make D14's promise true: **the one `CoreClient` seam (and the handful of raw
HTTP touchpoints around it) gains a Tauri IPC implementation, selected at
bootstrap, with components never knowing which transport they're on.** The
browser/HTTP path is FROZEN behavior — this lane is a refactor-plus-addition,
and the full existing Playwright suite (63/63, zero skips) is the frozen-path
proof. All Tauri-side behavior is unit-tested against `@tauri-apps/api`
mocks (`mockIPC`); the live cross-lane proof is the unifier's M5 walk.

This lane owns `apps/web/**` only; it must NOT touch `crates/**` or the root
Cargo files. No new server verbs, no `CoreRequest`/`CoreResponse` changes —
the dispatch surface is frozen this round.

**What this lane must NOT do:** no visual/UX changes; no reworking of
TanStack Query usage or the stream reducer (they sit ABOVE the seam and must
not notice); no speculative abstraction beyond the four seams below (YAGNI —
the seam inventory is closed, survey-verified).

## Survey-verified starting points (2026-07-15 @ `02865bdb` — do not re-derive)

### The seam inventory (closed — everything the SPA does over HTTP)

1. **`CoreClient`** (`apps/web/src/app/core/core-client.ts`) — the ONE D14
   injectable; its three raw touchpoints are `fetch('/api/dispatch')`
   (`dispatch()`, lines 75–98), `fetch('/health')` (`fetchHealth()`,
   lines 191–222, interpreting 200/423/409/503), and the single
   `EventSource('/api/events')` (`connect()`/`disconnect()`, with
   `connection` + `resyncCounter` signals and `parseEventData`). Its class
   comment already names this round: "the Tauri shell later swaps this one
   class for an IPC-backed implementation." Everything else in the class
   (dispatchExpect/dispatchData/the typed helpers) sits above the seam.
2. **Raw REST/byte sites** (each keeps its path; only the ORIGIN becomes
   transport-dependent, per §3):
   - `apps/web/src/app/images/image-urls.ts` — `fileUrl`, `thumbnailUrl`
     (the `<img src>` builders; `image-modal.ts` and
     `screens/files/file-preview-modal.ts` fetch these URLs for bytes).
   - `apps/web/src/app/screens/scriptorium/scriptorium.api.ts` —
     `buildMountFileItemUrl` (fetched with PUT/GET), `buildMountBlobUrl`.
   - `apps/web/src/app/files/file-manager-transport.ts` — the
     `?action=write-file` POST.
   - `apps/web/src/app/chat/chat-files.api.ts` — the chat-file multipart
     POST.
   - `apps/web/src/app/screens/characters/characters.api.ts` — the photos
     multipart POST, the `?action=import` POST, `?action=reset-builtins`
     POST (+ `list/character-import-dialog.ts`'s import POST).
   - `apps/web/src/app/terminal/terminal-api.ts` — the four terminal REST
     fetches (`/api/v1/terminals*`).
   (The many other `fetch()` grep hits are TanStack Query `.fetch()`
   refetches — not HTTP. The inventory above is the complete raw surface.)
3. **The terminal stream** —
   `apps/web/src/app/terminal/terminal-session.service.ts` builds
   `ws(s)://<host>/api/v1/terminals/<id>/stream` (line ~161), speaks the
   frozen unions in `terminal-protocol.ts` (`WsClientMessage`:
   `input`/`resize`/`ping`; `WsServerMessage`:
   `output`/`exit`/`meta`/`pong`/`chat-update`), pings every
   `PING_INTERVAL_MS`, and reconnects with backoff. The union types do NOT
   change — only the pipe (§4).
4. **Bootstrap** — the app config wires `CoreClient` via `providedIn:
   'root'`; the transport choice must be made before first injection.
   Detection: `isTauri()` from `@tauri-apps/api/core` (checks
   `__TAURI_INTERNALS__`) — add `@tauri-apps/api` ^2 as a dependency (lane
   B owns `apps/web/package.json`).

### Facts that will bite (from the memory notes + this survey)

- `parseEventData`'s skip rules (`[DONE]`/`{}`/parse-swallow) apply to SSE
  chunking artifacts; the Tauri event payload arrives pre-parsed as a JSON
  value — do NOT re-stringify-and-reparse to reuse the helper; route both
  transports into one "frame accepted" path AFTER their native decode.
- `resyncCounter` semantics: HTTP bumps it on EventSource reopen-after-error;
  under Tauri it bumps on a received `quilltap://resync` (§2). Consumers
  (the refetch-on-resync sites) must not care which.
- The unit-test substrate: `ng test` (the `@angular/build:unit-test`
  builder), NOT bare vitest (the P4.6e lesson); `resetTestingModule` FIRST in
  multi-render helpers (P4.6t/P4.6as lessons); no prettier config in the repo
  — do not reformat existing files (P4.6as lesson).
- `@tauri-apps/api` v2 ships `mockIPC`/`clearMocks` in
  `@tauri-apps/api/mocks` — use them for the Tauri-transport unit tier
  (mock `invoke`; for `listen`, mock at the module seam you wrap it in).
- The full Playwright suite runs against the axum server on port 4319 —
  nothing Tauri-related can run there; the suite's job in this lane is
  proving the HTTP path did not move. Run it ALONE (port-contention lesson,
  P4.6af).

## Tiered deliverables

### Tier 1 — must land

1. **The transport split under `CoreClient`.** Extract the three raw
   touchpoints behind an internal transport boundary (an injected
   implementation pair — HTTP and Tauri; exact shape is yours), keeping
   `CoreClient`'s public API and all HTTP behavior byte-for-byte (the
   synthetic-error messages, the health vocabulary, the connection-state
   transitions, `parseEventData` skips). Selection at bootstrap via
   `isTauri()`.
2. **The Tauri `CoreClient` transport** per §1/§2: `invoke('dispatch',
   {request})` resolving the envelope verbatim (map an invoke REJECTION —
   which per §1 should never happen for typed errors — to the same
   synthetic-internal envelope the HTTP path uses for network failure);
   `invoke('health')` → interpret `{status, body}` with the SAME branch
   logic as today (share the interpreter, don't fork it);
   `listen('quilltap://event')` THEN `invoke('events_attach')` (the §2
   ordering — attach only after the listener is live, so the Green-Room
   backlog replay isn't dropped), `quilltap://resync` → `resyncCounter`
   bump; `connection` signal semantics: 'connecting' until attach resolves,
   then 'open'; `disconnect()` unlistens.
3. **§3 — the origin resolver.** One exported resolver (e.g.
   `apiUrl(path)`) that is identity in the browser and prepends the `qtap`
   origin under Tauri (`qtap://localhost` on macOS/Linux,
   `http://qtap.localhost` on Windows), adopted at EVERY site in the seam
   inventory item 2 — URL builders and raw fetches alike. Paths do not
   change. Multipart bodies (FormData) are unchanged — the protocol carries
   full HTTP requests.
4. **Unit tier.** Specs for: the Tauri transport (mockIPC round trips —
   dispatch envelope pass-through, health branch parity with the HTTP
   interpreter over the same fixture statuses, the listen-before-attach
   ordering, resync bumping), the resolver (both origins, all builder
   sites), and the bootstrap selection. The HTTP-path specs stay untouched
   and green.
5. **The frozen-path proof.** `ng test` green, `ng build` clean, and the
   FULL Playwright suite **63/63, zero skips** — unchanged counts, no
   locator edits (nothing user-visible moved).

### Tier 2 — should land (matches lane A's tier 2)

6. **§4 — the terminal stream transport.** Extract the WS lifecycle in
   `terminal-session.service.ts` behind a stream-transport seam (open /
   send / message-callback / close), keeping the WS implementation
   byte-for-byte (ping cadence, reconnect/backoff, state transitions), and
   add the Tauri implementation: `terminal_attach` with a `Channel`
   callback, `terminal_send`, `terminal_detach` (§4 names, frozen unions
   from `terminal-protocol.ts`). Reconnect semantics under Tauri: an attach
   failure or channel drop routes into the SAME reconnect path the WS close
   handler uses. If lane A's tier 2 slips, ship the seam with the Tauri
   implementation behind it anyway (unit-tested against mocks) and record
   the pairing as DEFERRED-live in the status header — the pane degrades to
   its existing 'reconnecting' state under Tauri until unification pairs
   them.

### Tier 3 — explicit deferrals (loud, named)

- Anything Tauri-native in the UI (window chrome awareness, menu
  accelerators, drag-region, file-drop via the Tauri API) — progressive
  enhancement, later rounds (D14's one-seam rule).
- Offline/latency affordances specific to in-process transport (e.g.
  skipping the health retry loop because the engine is local) — the startup
  gate keeps its existing behavior on both transports this round.

## The verification requirement (restated)

The SPA has no v4 oracle (tier 4 #5): the discipline here is (a) the
**frozen-path proof** — the untouched Playwright suite at full count over
the HTTP transport — and (b) the **contract-pinned unit tier** — every §1–§4
name and payload shape in the Tauri transport specs is written against the
Shared contract text, so a lane-A rename surfaces as a spec diff at
unification, not a runtime mystery. The live IPC proof is the unifier's M5
walk (below); do NOT attempt to drive a Tauri webview from Playwright
(tauri-driver does not support macOS — this is a known platform limit, not a
gap in the lane).

## Fixtures

Consumes nothing new; delivers nothing new. The Playwright suite keeps its
existing committed fixture instance and `global-setup.ts` untouched.

## Shared contract (BINDING — reproduced verbatim in both orders)

The IPC surface, name-level. Lane A implements; lane B consumes. Payload
shapes are the EXISTING wire shapes — no new DTOs.

### §1 — dispatch + health commands

- Command **`dispatch`**, argument key **`request`**: the value is the same
  internally-tagged `CoreRequest` JSON the HTTP body carries, verbatim. The
  command ALWAYS **resolves** (never rejects) with the `Response` enum
  serialized verbatim — `{type, data}` — including typed error envelopes.
  IPC carries no HTTP status; the envelope alone is authoritative (the SPA
  already treats non-2xx bodies as typed envelopes, so nothing is lost).
  Boot-failure arm: resolves `{type:'error', data:{kind:'internal',
  message}}` (the `dispatch.rs` 503 arm's body). Malformed request: resolves
  the BadRequest envelope (the `dispatch.rs` 400 arm's body).
- Command **`health`**, no arguments → `{ status: <number>, body: <the
  GET /health JSON body verbatim> }` where `status` is exactly the HTTP
  status `health.rs` would set (200 healthy / 423 locked / 409 lock-conflict
  / 503 unhealthy), so the SPA's interpreter branches identically.

### §2 — the global event stream

- Tauri event name **`quilltap://event`**; payload = the `Event` JSON exactly
  as the SSE `data:` payload (same serde serialization, no `id:`/framing
  wrapper).
- Command **`events_attach`**, no arguments: (re)starts the forwarder for
  this webview session — subscribe to the core broadcast FIRST, then emit the
  Green-Room `active_snapshot` backlog as `quilltap://event` frames in order,
  then forward live frames (the `events.rs` ordering rule). Idempotent:
  a second attach (webview reload) replaces the previous forwarder.
- On broadcast lag (`RecvError::Lagged`, drop-oldest) the shell emits
  **`quilltap://resync`** (payload ignored); the SPA bumps `resyncCounter` —
  the IPC analogue of the SSE reopen signal.

### §3 — the custom protocol

- Scheme **`qtap`**, registered via `register_uri_scheme_protocol`,
  delegating the full `http::Request` (method + path + query + headers +
  body) into the reused `quilltap-web` router over the same booted state;
  the response is buffered back. CORS preflight answered permissively.
- SPA-side origin rule (per Tauri 2 platform behavior):
  `qtap://localhost` on macOS/Linux, `http://qtap.localhost` on Windows.
  The SPA prepends this origin to the SAME paths it uses today
  (`/api/v1/...`); paths do not change.
- Excluded surfaces (never called via protocol): `GET /api/events` (replaced
  by §2), the terminal WS upgrade (replaced by §4), static serving (the
  webview loads bundled `frontendDist` assets).

### §4 — the terminal stream (tier 2, both lanes)

- Command **`terminal_attach`**, args `sessionId: string`, `onMessage:
  Channel` — the channel carries `WsServerMessage` JSON verbatim
  (`output`/`exit`/`meta`/`pong`/`chat-update`, the frozen union). Attach
  semantics (what is sent on open) mirror the frozen WS route exactly.
- Command **`terminal_send`**, args `sessionId: string`, `message:
  <WsClientMessage>` (`input`/`resize`/`ping`, verbatim).
- Command **`terminal_detach`**, args `sessionId: string`.
- The terminal REST verbs (`/api/v1/terminals*` list/get/spawn/kill) ride §3
  unchanged.

## Ownership (BINDING — reproduced verbatim in both orders)

- **Lane A owns:** `crates/**` (the new `crates/quilltap-tauri/`, plus
  reuse-driven edits inside `crates/quilltap-web/` and — only if genuinely
  needed — `crates/quilltap-host/`), the root `Cargo.toml`/`Cargo.lock`.
  Lane A must NOT touch `apps/web/**`.
- **Lane B owns:** `apps/web/**` (including `package.json`/lockfile — the
  `@tauri-apps/api` dependency is lane B's). Lane B must NOT touch
  `crates/**` or the root Cargo files.
- **Version bumps:** lane A bumps `quilltap-web` (and `quilltap-host` if
  touched) per the usual per-commit rule and introduces `quilltap-tauri` at
  `0.0.1`; lane B bumps the SPA version. The unifier only accumulates.
- **Append-only for both:** `CHANGELOG`, `docs/developer/porting/status-log.md`.
- **At unification:** the unifier builds the SPA, builds the Tauri debug
  bundle, launches it against a fixture/copy instance, and runs the **M5
  walk** (unlock → salon list → open chat → send → streamed reply) with the
  human — the cross-lane proof neither lane can run alone. No
  ACTIVATE-AT-UNIFY test mocks are expected this round (lane B's unit tier
  stays mocked by design; the M5 walk is the live proof).

## Verification gate (before declaring the lane done)

1. `ng test` — zero failures (existing counts + the new specs; record the
   totals).
2. `ng build` clean (the Tauri transport must tree-shake cleanly out of the
   browser bundle or be lazily imported — a browser build must not require
   `@tauri-apps/api` at runtime; verify the built app still boots in a plain
   browser).
3. **Full Playwright — the existing suite at full count (63/63 as of round
   start), zero skips, run alone on 4319.** Any count change is a frozen-path
   violation — stop and fix.
4. `cargo fmt --all --check` clean (no Rust should have been touched — this
   is the no-`crates/**`-diff tripwire; also check
   `git diff --name-only main...HEAD` shows `apps/web/**` + the two
   append-only docs only).
5. Every commit through `.claude/commands/commit.md`. Close with a status-log
   entry recording the seam shapes chosen, the mockIPC recipe, and every
   tier-2/3 item's disposition (LANDED / DEFERRED-loud).
