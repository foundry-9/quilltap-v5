# Work order P4.9f2 — the wardrobe lane (lane F2): the wardrobe control dialog, the item editor, the entry points, and the stub that retires


> **STATUS: CLOSED — UNIFIED on main (2026-07-19, the consult-wire +
> image-detail + wardrobe round).** Tier 1 whole (5/5) and tier 2 whole
> (3/3), across seven units. The dialog service with the
> `${characterId}|${chatId}` remount key, the control dialog in BOTH modes
> (in-chat staging + the one-shot `set_all` flush on Done; out-of-chat
> fitting room that fires NO equip route — asserted as "the dispatch log
> holds only reads"), the tier-routed item editor with all five save paths,
> three of four entry points, and the retirement of the disabled
> `wardrobe-tab.ts` stub. Pure SPA — zero crate changes.
>
> **§2 contract fidelity: VERIFIED CLEAN at unification.** Every §1 verb
> name and payload F2 sends matches F1's implementation exactly, including
> the seven-mode union being byte-identical in membership and order. No
> round-breaking divergence.
>
> **The ACTIVATE-AT-UNIFY beat SELF-ACTIVATED**: `wardrobe-flow.spec.ts`'s
> in-chat staging + `set_all` flush walk (probe-guarded on an "unknown
> variant" dispatch error) runs green over F1's server — full Playwright
> 86/86, zero skips.
>
> **Deferred (loud, named):** the import-from-image modal — deliberately NO
> button shipped, since its `analyze-image` verb is refusal-armed in F1
> (§4); the `asTab` `WardrobeView` (it belongs to `p4.9j` workspace tabs);
> and the in-chat participant-card entry point, because **v5 has no
> participant card at all** — the lane declined to invent a panel to hang
> it on, and the entry travels with that unported surface.
> **Inherited gap:** the out-of-chat Preview button reaches F1's typed
> render refusal until the `avatar_preview` host wire lands (blocked on the
> WebP codec seam — see P4.9f1's header).
>
> *(The original status header follows for history.)*
> **STATUS: OPEN — the SPA half of the wardrobe pair.**
>
> Lane **F2** of the FOUR-lane **P4.6bd ∥ P4.9a2 ∥ P4.9f1 ∥ P4.9f2** round
> (the consult-wire + image-detail + wardrobe round, planned 2026-07-18).
> Siblings: `p4.6bd-consult-wire.md` (BD), `p4.9a2-image-detail-modals.md`
> (A2), `p4.9f1-wardrobe-server.md` (F1 — **your pair**).
> **v4 reference baseline: HEAD `616930db`** (4.8.0-dev.75,
> `~/source/quilltap-server`) — run `git log 616930db..HEAD --oneline` at
> lane start; **if HEAD moved, STOP and report.** You run in a worktree
> with its own Rust binaries, built once for the e2e server (the P4.6af
> memory).
>
> **This is a PURE-SPA lane — it must NOT touch `crates/**`.** It adds no
> wire surface of its own (§1): it codes against lane **P4.9f1**'s verbs
> behind its own LOCAL typed module + an `as unknown as CoreRequest` cast
> (the established `home.api.ts` / `photos.api.ts` pattern), exactly as if
> they existed. **Any e2e beat that needs those verbs live is
> ACTIVATE-AT-UNIFY, with a skip message naming lane P4.9f1** (§2). Neither
> lane may rename a §1 verb or move a payload field without the other's
> order being edited in the same commit — a divergence there is a
> round-breaking defect, not a merge conflict.
>
> **THE STUB THIS LANE RETIRES.**
> `apps/web/src/app/screens/characters/view/tabs/wardrobe-tab.ts` (31 lines)
> today renders v4's intro prose plus a permanently `disabled` button
> reading "Open wardrobe for {name}", titled "The wardrobe dialog is not yet
> available in this vertical". Its docstring at `:3-7` says the dialog "is
> its own future work order here." **This is that work order.** When the
> lane closes, that button opens the real dialog and the comment is gone.

## Mandate

Deliver v4's global wardrobe control dialog family as a v5 Angular
vertical: the dialog *service* (v4's React context — `open(ctx)` / `close`
/ the `${characterId}|${chatId}` remount key), the control dialog itself in
**both** of its modes (in-chat "wearing now" with client-side staging and a
one-shot `set_all` flush; out-of-chat fitting room that never fires an
equip call), the tier-routed item editor with its component picker, the
transfer dialog, the avatar-generation pane, and the entry points that
reach them. v4's strings, layout order, and microcopy carry verbatim. This
lane must NOT touch `crates/**`, `screens/prospero/wardrobe/**`,
`apps/web/src/app/images/**`, or any sibling-owned file. It IS the only
lane touching `shell/shell.ts` this round.

## Survey-verified starting points (2026-07-18 @ `616930db` — do not re-derive)

Every line reference below was read at `616930db`. When you transcribe a
behavior, cite the v4 `file:line` you copied it from in the code comment
AND in the spec that pins it (see "The differential requirement").

### 1. `components/wardrobe/wardrobe-control-dialog.tsx` — 1391 lines

**Three exported shells.**

- `WardrobeControlDialog()` `:87` — no props; reads `useWardrobeDialog()`
  `:88`; renders nothing when closed. **It keys the inner component on
  `${characterId}|${chatId}`** (`:93` supplies `initialCharacterId`; the
  key is the load-bearing part) — a context change REMOUNTS the inner and
  discards all staged state. Reproduce that remount deliberately in
  Angular; do not "optimize" it into an in-place update.
- `WardrobeView({characterId})` `:105` — the left-rail workspace-tab
  variant (`asTab`, `chatId: null`, browse/edit only, no "wearing now").
  **Tier 3 — do not port** (it belongs to `p4.9j` workspace tabs).
- `WardrobeControlDialogInner(InnerProps)` `:166`, with `InnerProps` `:116`
  = `{initialCharacterId: string|null; chatId: string|null; onClose: () =>
  void; asTab?: boolean}`.

Plus `WardrobeShell` `:128` (the modal/bare wrapper — modal chrome when
`asTab` is false, bare when true) and `AvatarGenerationPane` `:1291`
(props interface `:1278`).

**Its direct calls (all become dispatch verbs in v5).**

| v4 call | Line | Body / notes |
|---|---|---|
| `GET /api/v1/characters` | `:253` | the header character dropdown → `CharacterList` |
| `GET /api/v1/image-profiles` | `:279` | the avatar-pane profile picker → `ImageProfileList` |
| toggle-default `PUT /api/v1/characters/{cid}/wardrobe/{id}` **or** `PUT /api/v1/wardrobe/{id}` | `:361`/`:362` | body `{isDefault}`; the pair choice is the item's tier |
| `DELETE` on the same pair | `:387`/`:388` | same tier choice |
| duplicate `POST /api/v1/characters/{cid}/wardrobe` | `:421` | `{title, description, types, appropriateness, isDefault, componentItemIds, replace}` |
| flush `POST /api/v1/chats/{chatId}?action=equip` | `:664` | `{characterId, mode: 'set_all', slots}` |
| `wearFitting` — same route | `:703` | `{characterId, mode: 'set_all', slots: fittingSlots}` |
| `POST /api/v1/chats/{chatId}?action=regenerate-avatar` | `:770` | `{characterId, equippedSlots, imageProfileId?}` → `{queued: true, message}` |
| `POST /api/v1/wardrobe/preview-avatar` | `:788` | `{characterId, equippedSlots, imageProfileId?}` → `{fileId, url, mimeType, prompt}` |

**Its hooks (port them as Angular services/computeds — the reads are the
contract).**

- `useCharacterWardrobeItems(selectedCharacterId, {chatId})` `:174` →
  `lib/hooks/use-character-wardrobe-items.ts` (122 lines): `:69`
  `GET /api/v1/chats/{chatId}` **solely to resolve `projectTierId`**, then
  the three tier reads `:81` `GET /api/v1/characters/{cid}/wardrobe`,
  `:83` `GET /api/v1/projects/{pid}/wardrobe` (only when a project tier
  resolved), `:85` `GET /api/v1/wardrobe`. It also returns
  `projectId` — the dialog threads it into the editor's project-scope arm.
- `useOutfit(chatId, characterIdsForOutfit)` **`:244`** →
  `lib/hooks/use-outfit.ts` (481 lines): `:131`/`:132` the wardrobe reads,
  `:224` `GET /api/v1/chats/{chatId}?action=outfit` (`cache: 'no-store'`),
  and the four mutating call sites `:300` (`mode: 'wear'`), `:340`
  (`'replace'`), `:377` (`'add_to_slot'`), `:416`
  (`'remove_from_slot'` | `'clear_slot'`).

**THE CHAT-AWARE BEHAVIOR — the subtlest thing in this lane. Read twice.**

`isInChat = chatId !== null` `:203`. `rightTab` defaults to `'live'` in
chat and `'builder'` out of chat `:204`.

- **In chat** a "Live / wearing now" tab exists, and **every live mutation
  STAGES client-side** into `liveStagedByChar` `:236` instead of firing per
  click: `handleEquipItem` `:476`, `handleAddToSlot` `:484`,
  `handleSlotAdd` `:499`, `handleSlotRemove` `:514` are ALL pure state
  updates. On **Done**, `flushStagedLiveOutfits` `:648` fires **ONE**
  `?action=equip` with `mode: 'set_all'` per dirty character (`:664`) —
  which is what keeps it to one Aurora announcement and one avatar
  regeneration each. "Try on" → `wearFitting` `:700`, the same `set_all`
  shape, then closes.
- **Out of chat** there is no live tab and **NO equip route EVER fires**:
  `useFittingActions` is forced true at `:730`
  (`!isInChat || rightTab === 'builder'`), rows mutate only the transient
  `fittingSlots` `:200` (seeded from the character's default items), and
  avatar generation goes to the **non-persisting** `preview-avatar` `:788`,
  which explicitly does not touch `avatarOverrides` or any chat.

Getting this wrong in either direction is a data bug (a stray `set_all`
out of chat rewrites a chat's outfit; a per-click equip in chat spams
announcements and regenerations). Pin both arms with unit specs.

### 2. `components/providers/wardrobe-dialog-provider.tsx` — 74 lines

`WardrobeDialogContext {characterId?, chatId?}` `:22` (with v4's own
field docs at `:23`/`:26` — carry them); the context value
`{isOpen, context, open(ctx?), close}` `:31`; the provider `:39`;
`useWardrobeDialog` `:62`; `useWardrobeDialogOptional` `:72` (the
optional accessor is why v4's entry buttons render `disabled={!wardrobeDialog}`
— in v5 an injected root service is always present, so the disabled arm
collapses; record that as a deliberate divergence). **No API calls.**
Mounted at `components/layout/app-layout.tsx:126-138`, wrapping the app and
rendering `<WardrobeControlDialog />` as a sibling of the content — the v5
equivalent is a single host mounted once in the shell.

### 3. `components/wardrobe/WardrobeTransferDialog.tsx` — 239 lines

Props `:23` = `{isOpen, mode: TransferMode, item: WardrobeItem,
sourceCharacterId, sourceProjectId: string|null, onClose, onTransferred}`.
`GET /api/v1/wardrobe/transfers` `:73` →
`{destinations: {general: {available, label}, projects[], groups[], users[]}}`;
`POST` `:104` with
`{action: 'move'|'copy', itemId, sourceCharacterId, sourceProjectId,
destination: {scope, id?}}` → `{wardrobeItem, action}`.

### 4. `components/wardrobe/import-from-image-modal.tsx` — 518 lines

Props `:26` = `{characterId, onClose, onImported}`.
`POST /api/v1/wardrobe/analyze-image` `:129` with
`{image (base64), mimeType: 'image/jpeg'|'image/png'|'image/webp'|'image/gif',
guidance?}` → `{proposedItems: [{title, description, types,
appropriateness}], provider, model}`; then `POST
/api/v1/characters/{cid}/wardrobe` `:192` in a per-accepted-item loop.
**Tier 3 — do NOT ship this UI**: its server verb is refusal-armed in F1
by §4 (the DARK-consult standing note), so a shipped entry point would be
a dead button.

### 5. `components/wardrobe/wardrobe-item-editor.tsx` — 709 lines

Plus the subdirectory `components/wardrobe/wardrobe-item-editor/`:
`WardrobeComponentPicker.tsx` (251), `WardrobeModeChangePrompt.tsx` (72),
`constants.ts` (29), `types.ts` (17) — port all four; the constants file is
the slot/appropriateness vocabulary and the mode-change prompt is the
leaf↔composite confirmation.

Props `:25` = `{characterId, item?, isShared?, projectId?,
initialComponentItemIds?, initialMode?, autoFocusTitle?, onClose, onSave}`;
`WardrobeCreateScope = 'character' | 'global' | 'project'` `:23`. Composite
candidate loading is three parallel wardrobe GETs `:145-147` (character,
project when `projectId`, global).

**The save route is chosen BY TIER — transcribe this branch exactly**
(`:358-369`):

```
isEditing && isShared    → PUT  /api/v1/wardrobe/{item.id}                      :360
isEditing && !isShared   → PUT  /api/v1/characters/{cid}/wardrobe/{item.id}     :361
createScope==='project'
  && projectId           → POST /api/v1/projects/{projectId}/wardrobe           :363
createScope==='global'   → POST /api/v1/wardrobe                                :365
otherwise                → POST /api/v1/characters/{cid}/wardrobe               :367
method = isEditing ? 'PUT' : 'POST'                                             :369
```

### The four entry points (v4 → the v5 file this lane owns)

1. **The left-rail footer button.** `components/layout/left-sidebar/sidebar-footer.tsx`
   — `:239` matches the URL against `SALON_CHAT_PATH_RE` (`:39`, a strict
   UUID salon path); no match → plain `wardrobeDialog.open()`; match →
   `resolveDefaultCharacterForChat(chatId)` (`:60`) then `:244`
   `open(characterId ? {chatId, characterId} : {chatId})`. Button
   `title="Wardrobe"`, icon `wardrobe`. The `inWorkspace` arm above it
   (`workspace.openTab('wardrobe')`) is `p4.9j`'s — **do not port it**.
   → v5 `apps/web/src/app/shell/shell.ts`, in
   `.qt-left-sidebar-footer-actions` (which today holds `qt-user-menu`,
   the theme switcher, and Settings). **This lane is the ONLY one touching
   `shell.ts` this round.**
2. **Character detail.** `app/aurora/[id]/view/CharacterDetailView.tsx:229`
   — `open({characterId: id})`. → v5
   `apps/web/src/app/screens/characters/view/character-detail.ts` (its
   Wardrobe tab is rendered at `:157-158` via `qt-character-wardrobe-tab`;
   the stub component is the thing that goes live).
3. **Character edit.** `app/aurora/[id]/edit/CharacterEditView.tsx:324` —
   `open({characterId: id})`. → v5
   `apps/web/src/app/screens/characters/edit/character-edit.ts`, whose
   docstring at `:44-45` currently declares wardrobe out of scope "(the
   global Wardrobe dialog owns it in v4 too)" — that note updates here.
4. **The in-chat participant card.** `components/chat/ParticipantCard.tsx:337`
   — `open({characterId, ...(chatId ? {chatId} : {})})`. **⚠ SURVEY
   FINDING: v5 HAS NO PARTICIPANT CARD.** The salon conversation renders
   no cast panel (its child components are the header, message list,
   composer, turn controls, the document/terminal panes, and the image
   dialogs — there is no `qt-participant-*` anywhere in the SPA). So
   **entry point 4 is a NAMED TIER-3 DEFERRAL** that travels with the
   unported participant-card surface. **Do NOT invent a participants panel
   to hang it on.** The Ownership block grants you "the salon
   participant-card entry file" against the possibility that one existed;
   the survey says it does not, and the deferral is the correct answer.

### The v5 wardrobe surface that already exists (what NOT to disturb)

- `apps/web/src/app/screens/prospero/wardrobe.api.ts` (125 lines) —
  `WARDROBE_SLOT_TYPES` `:19`, `WardrobeCreateInput` `:27`,
  `WardrobeUpdateInput` `:39`, `WardrobeResult` `:42`, `WardrobeMutator`
  `:44`, `projectWardrobeMutator` `:61` dispatching
  `projectWardrobeList`/`Create`/`Update`/`Delete` at `:70`/`:84`/`:101`/
  `:116`. It is **PROJECT TIER ONLY**. **Reuse its types and slot
  vocabulary rather than re-deriving them** — a second slot enum is a
  future drift bug. Generalizing it (e.g. character/global mutators
  alongside the project one) is in scope; changing project-tier behavior
  is not.
- `screens/prospero/wardrobe/project-wardrobe-manager.ts` (385 lines) +
  its spec — **Ownership forbids touching it.** Its `:50` note ("does NOT
  use the character wardrobe-control dialog family") **STANDS and must
  still be accurate when this lane closes.** If your port makes it false,
  you have over-reached.
- `screens/prospero/cards/project-wardrobe-card.ts` (43 lines).
- There is **no** v5 equivalent yet of the transfer dialog, the
  import-from-image modal, or the character/global item editor.

### Landed verbs F2 may consume directly (no §1 dependency)

`CharacterWardrobeList` / `Create` / `Get` / `Update` / `Delete`
(`crates/quilltap-core/src/api/types.rs:532,536,541,546,552`),
`ProjectWardrobeList` / `Create` / `Get` / `Update` / `Delete`
(`:1045,1050,1056,1062,1069`), `CharacterList` (`:331`),
`ImageProfileList` (`:787`). Everything else in the table above comes from
lane P4.9f1's §1 verbs and rides the §2 hand-off.

### Tests: there are NONE to port

`grep -rln 'WardrobeControlDialog|WardrobeTransferDialog|ImportFromImageModal|WardrobeItemEditor'`
across v4's `*.test.ts(x)` returns **zero files**. v4 has no RTL/jest
coverage for any of these five components. **There is nothing to port
case-for-case, and fidelity therefore rests ENTIRELY on the specs this lane
writes.** That is the reason for the citation rule below: every
transcribed behavior must name the v4 `file:line` it copied.

The adjacent v4 tests that constrain the *server* side (F1's problem, but
read them — they document payload shapes you will render):
`__tests__/unit/wardrobe-image-analysis-api.test.ts`,
`__tests__/unit/lib/wardrobe/wardrobe-frontmatter-replace.test.ts`,
`__tests__/unit/lib/tools/handlers/wardrobe-handlers.test.ts`,
`__tests__/unit/lib/tools/handlers/wardrobe-handler-shared.test.ts`.

## Tiered deliverables

### Tier 1 — must land

1. **The wardrobe dialog service** (`apps/web/src/app/wardrobe/`): the v5
   equivalent of v4's provider — `isOpen`, `context`, `open(ctx?)`,
   `close`, as signals on a root-provided service, plus the
   `${characterId}|${chatId}` **remount key** honoured (a context change
   discards staged state, v4 `:93`). One dialog host mounted once (v4
   `app-layout.tsx:126-138`).
2. **The wardrobe control dialog** with **BOTH** modes:
   - **In chat** — the "Live / wearing now" tab (default `rightTab`,
     `:204`), all four staging handlers as pure state
     (`:476`, `:484`, `:499`, `:514`) writing `liveStagedByChar` (`:236`),
     and the one-shot `set_all` flush per dirty character on Done
     (`:648`/`:664`); "Try on" → `wearFitting` (`:700`/`:703`) then close.
   - **Out of chat** — the fitting room: no live tab,
     `useFittingActions` forced true (`:730`), transient `fittingSlots`
     seeded from defaults (`:200`), and **not one equip call, ever.**
   - Plus the header character dropdown (`CharacterList`), the three-tier
     item load (`use-character-wardrobe-items.ts:69,81,83,85`), and the
     item row actions: toggle-default (`:361`/`:362`), delete
     (`:387`/`:388`), duplicate (`:421`).
3. **The item editor** with all five tier-routed save paths transcribed
   exactly (`:358-369`), the composite candidate load (`:145-147`), the
   component picker, the mode-change prompt, and the `constants.ts` /
   `types.ts` vocabulary aligned with `prospero/wardrobe.api.ts`'s
   existing `WARDROBE_SLOT_TYPES`.
4. **The entry points**: the shell footer button with v4's
   chat-path/default-character resolution (`sidebar-footer.tsx:239-244`),
   character detail (`:229`), character edit (`:324`).
5. **Retiring the stub**:
   `screens/characters/view/tabs/wardrobe-tab.ts` loses `disabled` and its
   `:3-7` "future work order" comment; the button opens the dialog with
   `{characterId}`. The intro prose stays verbatim.

### Tier 2 — should land

6. **The transfer dialog** over F1's `wardrobeTransferDestinations` /
   `wardrobeTransferApply` (v4 `:73`/`:104`), reachable from an item row
   in both move and copy modes.
7. **`AvatarGenerationPane`** (`:1291`, props `:1278`) over F1's
   `wardrobePreviewAvatar` (out of chat, non-persisting — `:788`) and
   `chatRegenerateAvatar` (in chat, `{queued: true, message}` — `:770`),
   with the `ImageProfileList` picker (`:279`). **Beats touching these two
   verbs are ACTIVATE-AT-UNIFY per §2, skip message naming lane P4.9f1.**
8. **A live Playwright walk** in this lane's OWN new spec file: boot →
   unlock → character detail → Wardrobe tab → the (now enabled) button
   opens the dialog → create an item through the editor → it appears in
   the character tier → toggle default → close. The out-of-chat arm needs
   only landed verbs, so it runs LIVE in this lane; the in-chat staging +
   flush arm needs F1's `chatOutfitGet`/`chatEquip` and is
   ACTIVATE-AT-UNIFY.

### Tier 3 — explicit deferrals (loud, named)

- **The import-from-image modal** (`import-from-image-modal.tsx`) — its
  server verb `analyze-image` is refusal-armed in F1 by §4. Port no UI and
  **ship no entry point**; a dead button is worse than an absent one.
  Record it by name.
- **`WardrobeView`'s `asTab` left-rail variant** (`:105`) and the
  `inWorkspace` branch of the footer button — they belong to `p4.9j`
  (workspace tabs). Do not port the tab host.
- **The in-chat participant-card entry** (v4 `ParticipantCard.tsx:337`) —
  no v5 participant card exists (survey above); it travels with that
  unported surface.
- Anything else the survey shows unported — name it in the lane record
  rather than stubbing it.

## The differential requirement, restated

Pure-SPA (tier 4): the proof is `ng test` + the live Playwright walk. The
server half this lane consumes is differential-pinned by lane **P4.9f1**;
nothing here regenerates an oracle.

Because v4 ships **zero component tests** for this family (survey above),
**every transcribed v4 behavior gets a unit spec that names the v4
`file:line` it copied.** At minimum: the remount key (`:93`); the
`isInChat` tab default (`:203`/`:204`); each of the four staging handlers
being write-free (`:476`, `:484`, `:499`, `:514`); the one-`set_all`-per-
dirty-character flush (`:648`/`:664`); `wearFitting` (`:700`); the
out-of-chat **no-equip** invariant (`:730` + `:200`) asserted as "the
dispatch spy recorded no `chatEquip`"; the five editor save routes
(`:358-369`); the three-tier item load
(`use-character-wardrobe-items.ts:69,81,83,85`); the footer button's
chat-path resolution (`sidebar-footer.tsx:239-244`).

**Standing SPA traps — these are rules, not advice:**

- **`[selected]` per option, never `[value]` on a `<select>`** whose
  options are async or `@if`-gated (the dogfood-#6 rule, re-confirmed by
  P4.6bc). This dialog is full of such selects (character dropdown, image
  profile, slot pickers) — expect to hit it.
- **Template comments cannot contain backticks** (the P4.6bb / P4.6bc
  trap). Your v4 citations are full of paths — keep them in `/** */`
  docblocks, not `<!-- -->`.
- **Effects must not latch signals they write** (P4.6bb) — the staging
  map and the fitting-slots seed are exactly the shape that tempts it.
- **A load must not emit an 'edit'** (the `markdown-field-load-must-not-emit`
  memory) — directly relevant to the item editor's description field:
  mounting the editor over async-loaded content must not mark the form
  dirty.
- **Prefer v4's own names in locators** so a new affordance doesn't make a
  sibling spec's `getByRole` ambiguous (the
  `added-affordance-breaks-sibling-by-name-locators` memory). "Open
  wardrobe for {name}" and "Wardrobe" are v4's own strings — use them.
- **Never assert absolute counts a sibling spec can grow** (the P4.6ap
  memory).

## Fixtures

- **Consumes:** the committed e2e locked instance. `global-setup` copies it
  FRESH each run (the `e2e-fixture-before-run-order` memory — read the
  fixture before blaming run order). Seed any extra wardrobe data **via
  dispatch after unlock** (the P4.6z rule), never by editing the committed
  DB.
- **Delivers:** none. No committed fixture, no oracle regeneration owed.
- If lane **P4.9f1**'s new fixture family needs e2e seeding for the
  ACTIVATE-AT-UNIFY beats, **that seeding rides F1's family and is wired at
  unification** — do not add it here.

## Shared contract (BINDING — identical in all four orders)

The round is **P4.6bd ∥ P4.9a2 ∥ P4.9f1 ∥ P4.9f2** (the consult-wire +
image-detail + wardrobe round, planned 2026-07-18). Lane letters below:
**BD** = `p4.6bd-consult-wire.md`, **A2** = `p4.9a2-image-detail-modals.md`,
**F1** = `p4.9f1-wardrobe-server.md`, **F2** = `p4.9f2-wardrobe-dialog-spa.md`.

### §1 — the new wire verbs (fold-at-unify)

Each verb-adding lane keeps a LOCAL typed module + an
`as unknown as CoreRequest` cast (the established `home.api.ts` /
`photos.api.ts` pattern); the unifier folds them into `CoreRequest` in
`apps/web/src/app/core/core-contract.ts` and runs the name-for-name wire
diff against `crates/quilltap-core/src/api/types.rs`. Names + payloads are
pinned here and MUST NOT be renamed by a lane:

**P4.6bd: NO new wire verbs.** It changes no request or response shape. It
wires seams behind three EXISTING verbs (`ChatCustomToolRun`,
`CustomToolPreview`, and the `run_custom` tool path). The only observable
change is that a definition carrying an `llm` block now consults instead of
failing soft with `"no LLM invoker was available in this context"`
(`crates/quilltap-core/src/pascal/custom_tools.rs:1142`).

**P4.9a2: `imageInfoGet`** — v4 `GET /api/v1/images/{id}`
(`app/api/v1/images/[id]/route.ts:39-128`). Full payload verbatim:
`{id, userId, filename, filepath, mimeType, size, width, height, source,
generationPrompt, generationModel, createdAt, updatedAt,
tags[{tagId,tagType}], characterGalleryLinks[{characterId,characterName,linkId}],
_count:{charactersUsingAsDefault, chatAvatarOverrides}}`.
Invariants: `source` remapped `UPLOADED→upload` / `IMPORTED→import` /
`GENERATED→generated` (`route.ts:62-64`); `tagType` DERIVED not stored —
`CHARACTER` if the tagId is a known character id else `THEME`
(`route.ts:66-74`); `characterGalleryLinks` built ONLY when `sha256` is set
and ONLY from linkers with `mountStoreType === 'character' && isPhotoAlbum`
(`route.ts:78-99`); 404 arms = missing file (`:43-45`) and category not
`IMAGE`/`AVATAR` (`:48-50`). REST edge: `GET /api/v1/images/{id}`.

**P4.9f1:** tier 1 — `chatOutfitGet` (v4 `GET /api/v1/chats/{id}?action=outfit`);
`chatEquip` (v4 `POST /api/v1/chats/{id}?action=equip`, **all seven modes,
in v4's own enum order**: `wear`, `replace`, `equip`, `add_to_slot`,
`remove_from_slot`, `clear_slot`, `set_all` — **`equip` is a DEPRECATED
ALIAS for `wear`** (v4's comment, `outfit.ts:37-39`: "`wear` honors the
item's `replace` flag; `replace` force-swaps the slots it covers. `equip` is
a deprecated alias for `wear`") and is the easy-to-miscount seventh: port it
faithfully, do NOT drop it and do NOT collapse it into `wear` — schema
`app/api/v1/chats/[id]/actions/outfit.ts:37-79`, handler
`handleEquipSlot:193`, response `:329`); `wardrobeTransferDestinations` +
`wardrobeTransferApply` (v4 `GET|POST /api/v1/wardrobe/transfers`); the
GLOBAL archetype tier `wardrobeList` / `wardrobeCreate` / `wardrobeUpdate` /
`wardrobeDelete` (v4 `GET|POST /api/v1/wardrobe`, `PUT|DELETE
/api/v1/wardrobe/{itemId}`). Tier 2 — `wardrobePreviewAvatar` (v4 `POST
/api/v1/wardrobe/preview-avatar`) and `chatRegenerateAvatar` (v4 `POST
/api/v1/chats/{id}?action=regenerate-avatar`). REST edges mirror the v4 URLs.

**P4.9f2: NO new wire verbs.** It consumes P4.9f1's verbs above plus the
LANDED `CharacterWardrobeList/Create/Get/Update/Delete`
(`api/types.rs:532,536,541,546,552`), `ProjectWardrobe*`
(`:1045,1050,1056,1062,1069`), `CharacterList` (`:331`), and
`ImageProfileList` (`:787`).

### §2 — the wardrobe pair's hand-off (F1 ↔ F2, BINDING)

F2 codes against §1's F1 verbs behind its own local typed module + cast,
exactly as if they existed. Any F2 e2e beat that needs a live server for
those verbs is **ACTIVATE-AT-UNIFY**, with a skip message naming lane
P4.9f1. F1 does not write SPA code; F2 does not write Rust. Neither lane
may rename a §1 verb or change a payload field without the other's order
being edited in the same commit — a divergence here is a round-breaking
defect, not a merge conflict.

### §3 — the `jsnum` canonicalization (P4.6bd only, SCOPED)

P4.6bd lifts the duplicated JS `Number()` ports into
`crates/quilltap-core/src/jsnum.rs` as `number_from_str`, taking
`crates/quilltap-core/src/tools/llm_number.rs:109`
(`js_number_from_str` — the most fully documented port) as the canonical
source, with `tools/text_block_parser.rs:489-531` and
`model/tool_wire/text_parsers.rs:328` rewired as consumers. **Do NOT widen
`text_parsers`' deliberately narrower grammar** (it rejects hex/`Infinity`
as a documented seam) — preserve it as an explicit narrowing wrapper over
the canonical fn, carrying its seam comment forward.
`crates/quilltap-web/src/photos_routes.rs:63-100`'s twin belongs to lane
P4.9a2's file set this round and is **NOT touched by either lane**; that
one call-site swap is DEFERRED LOUDLY as a follow-up rider (record it in the
status-log entry). The `js_number_to_json` SERIALIZATION family (the ~9
copies the survey found) is a SEPARATE rider and is explicitly out of scope.

### §4 — the DARK-consult standing note

After P4.6bd lands, the three consult entrances are live and the workbench
`{live:true}` bench arm plus the composer `chatCustomToolRun` path become
REAL SPEND. No other lane in this round may add a `CompletionProvider`, wire
a provider seam, or un-refuse an LLM-backed verb. P4.9f1's `analyze-image`
stays refusal-armed for exactly this reason (tier 3).

## Ownership (BINDING — identical in all four orders)

- **P4.6bd:** `crates/quilltap-core/src/pascal/llm_consult.rs`,
  `crates/quilltap-core/src/tools/{run_custom.rs, executor.rs,
  llm_number.rs, text_block_parser.rs}`,
  `crates/quilltap-core/src/model/tool_wire/text_parsers.rs`,
  `crates/quilltap-core/src/jsnum.rs`, `crates/quilltap-host/src/spine.rs`,
  `crates/quilltap-host/src/host.rs`, and the THREE dark dispatch sites in
  `crates/quilltap-core/src/api/engine.rs` (`:2732-2760`, `:2770-2800`) plus
  `api/custom_tools.rs` if a signature must move; harness: its own
  differential files + oracle cases. MUST NOT touch `api/photos.rs`,
  `quilltap-web/src/photos_routes.rs`, or any SPA file. Version bumps:
  quilltap-core, quilltap-host, quilltap-harness (+`Cargo.lock`).
- **P4.9a2:** `crates/quilltap-core/src/api/photos.rs`, the `=== P4.9a ===`
  fenced block in `api/engine.rs`, `crates/quilltap-web/src/photos_routes.rs`;
  harness: `harness/oracle/fixtures/build-photos-fixture.ts`,
  `harness/oracle/fixtures/photos-web.json`, the committed
  `photos-{main,mount}.db` family + sidecar,
  `crates/quilltap-web/tests/photos_routes_equivalence.rs`. SPA:
  `apps/web/src/app/images/**` (incl. `image-modal.ts`,
  `photo-gallery-modal.ts`, and the new image-detail files) EXCEPT
  `generate-image-dialog.ts` / `standalone-generate-image-dialog.ts` /
  `image-profile-picker.ts`, `screens/characters/view/tabs/gallery-tab.ts`,
  `screens/photos/**`, its own e2e spec file. Version bumps: quilltap-core,
  quilltap-web, quilltap-harness (+`Cargo.lock`), SPA `package.json`.
- **P4.9f1:** NEW `crates/quilltap-core/src/api/wardrobe.rs` and
  `crates/quilltap-core/src/api/chat_outfits.rs` (or one module — the lane
  chooses, but nothing else in `api/` moves), their arms in `api/engine.rs`
  + `api/types.rs` + `api/mod.rs`, NEW
  `crates/quilltap-web/src/wardrobe_routes.rs` + its registration in
  `crates/quilltap-web/src/lib.rs`; harness: its NEW fixture family +
  builders + oracle cases + differential files. MUST NOT touch
  `api/photos.rs`, `photos_routes.rs`, `tools/**`, `pascal/**`, or any SPA
  file; MUST NOT edit `quilltap-host/**` except a single named seam wire if
  tier-2 preview-avatar needs one — **flag that loudly in the lane record if
  it happens**, since P4.6bd owns `spine.rs`/`host.rs` this round (coordinate
  by deferring the wire to unification rather than editing the file).
  Version bumps: quilltap-core, quilltap-web, quilltap-harness
  (+`Cargo.lock`).
- **P4.9f2:** SPA only: `apps/web/src/app/wardrobe/**` (new — the dialog,
  the item editor, the transfer dialog, their specs),
  `screens/characters/view/tabs/wardrobe-tab.ts` (the stub it replaces —
  `:3-7`'s "its own future work order" comment retires here),
  `screens/characters/view/character-detail.ts` and the character-edit entry
  file, the salon participant-card entry file, and `shell/shell.ts` (the
  footer entry — **P4.9f2 is the ONLY lane touching `shell.ts` this round**),
  plus its own e2e spec file. MUST NOT touch
  `screens/prospero/wardrobe/**` (the landed project manager — its
  `project-wardrobe-manager.ts:50` note STANDS and stays accurate),
  `apps/web/src/app/images/**` (P4.9a2's), or any Rust file. Version bump:
  SPA `package.json`.
- **The round's KNOWN Rust conflict set:** P4.6bd, P4.9a2, and P4.9f1 all
  add ADDITIVE arms to `crates/quilltap-core/src/api/engine.rs`,
  `api/types.rs`, `api/mod.rs`, and `crates/quilltap-web/src/lib.rs` (route
  registration). Additions only, §1-pinned, in distinct regions (BD edits the
  three EXISTING dark sites; A2 the `=== P4.9a ===` block; F1 new fenced
  blocks). The unifier reconciles (temp-branch cherry-pick + one full gate —
  the parallel-round-reconciliation memory). No other Rust file is shared.
- **Append-only for everyone:** `docs/CHANGELOG.md`,
  `docs/developer/porting/status-log.md` (union-merge at unification).
  Version bumps union at unify — the unifier RECOUNTS from the COMMITS
  (identical bumps merge silently — the P4.6aw/ax memory).
- **Nobody** edits `CLAUDE.md`, `phase-4.md`, `m6-screen-parity.md`, or a
  sibling lane's files; the unifier updates the phase docs.

## Verification gate (run before declaring done)

- `cargo fmt --all --check` clean.
- `cargo clippy --workspace --all-targets -- -D warnings` clean on **BOTH**
  feature sets.
- `cargo test --workspace --no-fail-fast` — main's baseline is **353 test
  binaries / 1,444 tests / 0 failed**; your lane's total must be ≥ that, and
  the lane's OWN differentials must be run **BY NAME** with `--nocapture`
  and grepped for `SKIP` (the `p4d8` memory: `cargo test` hides `eprintln`,
  so an unnamed run's SKIP is invisible). Use `--no-fail-fast` — the first
  failing BINARY otherwise aborts and hides the rest.
- Oracles regenerate from the v4 checkout at `616930db`. **If the v4 tree is
  dirty, regenerate from a pinned detached worktree** (the
  `oracle-regen-pinned-v4-worktree` memory: `git worktree add --detach`,
  symlink BOTH `node_modules`). Grep the emitted NDJSON for a row you expect
  — an erroring builder silently leaves the OLD fixture in place (the
  `oracle-regen-silent-stale-pass` memory).
- SPA lanes: `ng test` green and `ng build` clean, both run FROM `apps/web`;
  full Playwright green, run ALONE on port 4319 with THIS worktree's own
  Rust binaries (the P4.6af memory). A source mutation proves nothing until
  `ng build` rewrites the served bundle (the
  `e2e-mutation-check-needs-a-rebuilt-dist` memory).
- No `[value]` binding on any `<select>` with async or `@if`-gated options —
  bind `[selected]` per option (the standing dogfood-#6 rule, re-confirmed
  by P4.6bc).
- Don't run `ng build` and a `quilltap-tauri` release build concurrently —
  the dist embedding races (the `616930db`-round gate note).
- Every commit follows `.claude/commands/commit.md` (equivalence test with
  the port, version bump, CHANGELOG entry, status-log append).
