# Work order P4.6bc — the `616930db` llm-consult drift re-port, SPA half (the consulted-oracle Workbench surfaces, containment chips, the schema asset, the Inspector consult type)

> **STATUS: CLOSED — UNIFIED on main (2026-07-18).** All tier-1 units
> and tier 2 landed (4 commits): the browser schema twin (with its own
> `NUMERIC_COMPARATOR_KEYS` split and 52 captured-arm rows), the
> tool-draft bijection (v4's +12+3 cases ported case-for-case), the
> Workbench consult surfaces + the byte-copied schema asset + the
> Inspector consult type, and both e2e beats — beat 6 (the scripted
> oracle through preview) SELF-ACTIVATED at unification over D8's
> server, as designed. §B/§C folded at unify (the two casts dropped;
> corpus counts 159/10/149; the drift map emptied). Notable lane
> finding: FOUR committed corpus rows had drifted in v4 itself at
> `616930db` — handled by a replay-verified drift map until the §C
> regen superseded it. One order expectation corrected: v4's
> `llm-logs-card` has NO v5 counterpart (unported surface) — the
> Inspector's own map carries the label. Full record: the status log
> ("P4.6bc — the `616930db` llm-consult drift re-port, SPA half" + the
> round record).
>
> Lane BC of the THREE-lane `616930db` drift-catch-up + P4.9a-resume
> round (planned 2026-07-18). **v4 reference baseline for this round:
> `616930db` (4.8.0-dev.75).** At lane start, run in
> `~/source/quilltap-server`:
>
> ```bash
> git log 616930db..HEAD --oneline
> ```
>
> **If HEAD moved, STOP and report.** The v4 tree is clean at planning
> time; if it goes dirty mid-round, any oracle regen runs from a PINNED
> detached worktree at `616930db` (the `oracle-regen-pinned-v4-worktree`
> memory) — though this lane expects to regenerate nothing (§C: the
> corpus NDJSON is lane D8's).
>
> Sibling lanes: `p4.d8-llm-consult-server.md` (lane D8 — the Rust
> server half; the §A/§B/§C meeting points below), and the RESUMED
> `p4.9a-photos-view.md` (lane A — disjoint from you in `apps/web/`;
> `app.routes.ts` is lane A's alone this round). The **Shared
> contract** and **Ownership** sections below are binding and
> reproduced verbatim in `p4.d8-llm-consult-server.md`. You run in a
> worktree (own Rust `target/`, own Rust binaries for the e2e server —
> the P4.6af memory; mind the disk-pressure memory).
>
> **The port discipline applies.** The SPA half is tier 4 (`ng test` +
> Playwright), but every transcription gets a unit spec pinning the v4
> behavior with file:line, and v4's own new test suites port
> case-for-case (the P4.6bb precedent: v4's 408-line tool-draft suite).
> The facts under "Survey-verified starting points" are from a
> **2026-07-18 survey at `616930db` (v4) / main `0c6c79ae` (v5)** — if
> a unit's fresh survey contradicts this order, **STOP and report**.

## Mandate

Re-port the client half of v4 `a2d9a3c8` + `616930db` onto the ported
Workbench/Pascal SPA: the browser twin of the definition schema (the
`llm` block + contains/ncontains in `pascal/custom-tool-types.ts`), the
tool-draft bijection (llm fields, llm/llm-ok condition chips, the
containment audit), the Workbench screens (BuilderForm's consulted-
oracle card, OutcomesSection's comparator menus + operand widget,
ProvingBench's oracle card, the library badge), the `workbench.api.ts`
§B payload mirror, the byte-copied published JSON Schema asset, and the
CUSTOM_TOOL_CONSULT type in the LLM Inspector + llm-logs card — plus
the `PascalMeta.llm` wire-mirror type. This lane owns the pascal +
custom-tools + inspector SPA files; it must NOT touch `apps/web`
photos/images/screens files (lane A's), `app.routes.ts` (lane A's this
round), the committed corpus NDJSON (lane D8's, §C), or any
`crates/**` file. User-facing strings are v4's verbatim ("The
consulted oracle", "When the oracle is silent", "Consults an LLM
mid-run; outcomes may test its answer", `'contains'` /
`"doesn't contain"`, …).

## Survey-verified starting points (2026-07-18: v4 @ `616930db`, v5 @ main `0c6c79ae` — do not re-derive)

### v4 — the client-side drift

- **`lib/pascal/custom-tool.types.ts`** — the schema the browser twin
  mirrors (full server-side survey in `p4.d8-llm-consult-server.md`;
  the browser port needs the same constants
  `MAX_LLM_PROMPT_LENGTH`/`MAX_LLM_OUTPUT_LENGTH`/`MAX_LLM_OUTPUT_CEILING`,
  `CustomToolLlmSchema` :420-448 with its three verbatim describe
  texts, `LlmComparatorSchema` :297-311 with the refine message
  `` must test something: a comparator on the answer, or `ok` ``,
  `COMPARATOR_KEYS` → 8 :186, `CONTAINMENT_KEYS` :192,
  `StringOperandSchema` :214 with `'the substring to look for must not
  be empty'`, containment on Param/Metadata/Llm but NOT Numeric
  (:238-240), the narrow six-key vs wide eight-key at-least-one
  messages :230-234, the `validateComparator` containment load-checks
  :706-757, the llm-without-block rejection :626-634, `'llm'` in
  `KNOWN_TOP_LEVEL_KEYS` :832).
- **`lib/pascal/tool-draft.ts`** (+159/+32):
  - `CONTAINMENT_COMPARATORS` exported (:55); `ComparatorKey` widens
    via `COMPARATOR_KEYS`.
  - `ConditionSubject` gains `{kind:'llm'}` (:100) and
    `{kind:'llm-ok'}` (:102 — serializes to the comparator's `ok` key).
  - `ToolDraft` gains `llmEnabled: boolean` (:141), `llmPrompt` (:143),
    `llmErrorMessage` (:145), `llmMaxOutput: string` (:147 — loose
    numeric text, `''` = default cap); `newDraft()` defaults
    false/''/''/'' (:188-191); fields KEPT while disabled (like roll).
  - Definition→draft (:342-345; `conditionsFromWhen` :249-279):
    `when.llm.ok` → ONE `llm-ok` chip `{comparator:'eq', operand:
    boolean}` (:263-276); the rest via `comparatorToConditions({kind:
    'llm'}, …)` (:277).
  - Draft→definition (`whenFromConditions` :407-455): `'llm'` →
    `llm[comparator] = operand` (:425-427); `'llm-ok'` → `llm.ok =
    comparator === 'neq' ? !operand : operand` (:428-434 — "succeeded
    ≠ true" serializes as `ok: false`); `definitionFromDraft`
    (:510-540): `llmEnabled` → `doc.llm = {prompt, errorMessage}` +
    `maxOutput` only when finite (:533-539 — omitted while blank: "the
    default cap is the runtime's, not the file's"); `'llm'` in
    `KNOWN_KEY_ORDER` between `roll` and `outcomes` (:360-370).
  - Validation: `DraftIssue.where` gains `{section:'llm', field:
    'prompt'|'errorMessage'|'maxOutput'}` (:577); the llm section
    (:935-962): `'the consult needs a prompt'`, `'the prompt is over
    4000 characters'`, `"the consult needs an error message — the
    tool's own words for when the model cannot answer"`, over-1000
    error, `'the answer cap must be a whole number of characters, at
    least 1'`, `'the answer cap tops out at 100,000 characters'`
    (`toLocaleString()`); `validateLlmPromptPlaceholders` (:667-693):
    same families minus `{{llm}}` — `'{{llm}} is not available here —
    the consult cannot quote its own answer'` (warn), `{{dice}}`
    outside dice form warns, unknown params warn;
    `validateMessagePlaceholders`: `'{{llm}} renders as written unless
    the LLM consult is enabled'` (:636-643); `validateCondition`
    (:710-755): llm/llm-ok chip while disabled → err `'tests the LLM
    consult, but the consult is not enabled'`; llm-ok with ordering OR
    containment → `'whether the consult succeeded can only be tested
    with = or ≠'`; the containment audit (:737-755, early-return to
    avoid double-reporting): `` `${comparator} searches a
    ${subjectType}, and only text can contain text` ``, `` `${comparator}
    needs text to look for` ``, `'the substring to look for must not
    be empty'`, `` `compares against "…", which is not declared` ``,
    `` `${comparator} needs text to look for; "${name}" is ${type}` ``.
  - `subjectValueType`: `'llm'` → null (unknowable), `'llm-ok'` →
    `'boolean'` (:820-825); `conditionSlotKey`: `` `llm:${comparator}` ``
    + comparator-free `'llm-ok'` (:841-846 — one succeeded-chip per
    row); `renameParameterEverywhere` rewrites `{{params.x}}` in
    `llmPrompt` (:1073); `findParameterReferences` reports `'the
    consult prompt renders it'` (:1106-1108).
- **`components/custom-tools/BuilderForm.tsx`** (+111): "The consulted
  oracle" card — enable toggle ("Consult an LLM"), prompt textarea
  with char counter vs 4000, errorMessage field labeled "When the
  oracle is silent", the answer-cap input, inline llm-section
  issues from `validateDraft`; `{{llm}}` joins the insert menu.
- **`components/custom-tools/OutcomesSection.tsx`** (+147 net):
  `llm`/`llm-ok` subject options gated on `llmEnabled`; slot-key
  dedupe messages for both; the `typeOfSubject` comparator-menu logic —
  string subjects offer eq/neq/contains/ncontains, booleans eq/neq,
  unknowable (metadata/llm) all eight, numbers no containment;
  comparator labels `'contains'` / `"doesn't contain"`; a text-only
  operand widget (switching subject/comparator coerces incompatible
  comparator→`eq`, number/boolean operands→text); `$param` operand
  choices filtered to STRING params under containment.
- **`components/custom-tools/ProvingBench.tsx`** (+96): "The oracle"
  card, only when `llmEnabled`: `BenchOracle = scripted | fail |
  live`; `previewOracle()` → the route's `{output}/{fail:true}/
  {live:true}`; **`auditOracle()` never returns `{live}`** — scripted
  answer or silence.
- **`components/custom-tools/WorkbenchLibrary.tsx`** (+5): an `oracle`
  outline badge, title "Consults an LLM mid-run; outcomes may test its
  answer", when `tool.llm` (:224-228).
- **Inspector/logs**: `LLMInspectorEntry.tsx` (+2) — CUSTOM_TOOL_CONSULT
  badge class `'qt-bg-info/15 qt-text-info'`, label `'Consult'`;
  `LLMInspectorPanel.tsx` (+2) — `'CUSTOM_TOOL_CONSULT'` joins the
  `other` filter group; `components/tools/llm-logs-card.tsx` (+1) —
  type label `'CUSTOM_TOOL_CONSULT': 'Custom Tool'`.
- **`public/schemas/qtap-custom-tool.schema.json`** (net +68 over both
  commits): top-level `llm` property (required `["prompt",
  "errorMessage"]`, additionalProperties false, bounds mirrored,
  "Default 8000"); `when.llm` `$ref` → new `$defs/LlmComparator` (8
  keys + `ok`, fail-soft descriptions incl. the trailing-'.'/'!'
  wording and "'west door' finds 'the West Door'"); new
  `$defs/StringOperand`; ParamComparator/MetadataComparator gain
  contains/ncontains (documented case-sensitive; metadata ncontains:
  "as with neq, absence is not a miss"). The JSON Schema is
  **deliberately weaker than Zod** in two pinned spots (cannot reject
  llm-test-on-blockless-tool, nor containment against a numeric
  parameter).
- **The v4 test bases**: `tool-draft.test.ts` +12+3 cases (the llm
  block in the draft; containment round-trips + chip audit + reject
  containment on llm-ok); `workbench-llm.test.tsx` NEW, 148 lines, 9
  cases (BuilderForm fields shown/hidden/toggle/inline issues;
  OutcomesSection renders loaded llm chips + consult subjects offered
  only while enabled; ProvingBench scripted/silence/live modes when
  consulting, no card otherwise, scripted answer sent with a preview
  roll).

### v5 — the anchors

- **`apps/web/src/app/pascal/custom-tool-types.ts`** (1,212 lines) —
  the browser Zod twin; exports `COMPARATOR_KEYS`, `safeParse`,
  `formatDefinitionIssues`, `displayTitle`, `collectUnknownKeys`.
- **`apps/web/src/app/pascal/tool-draft.ts`** (1,054 lines) —
  `ConditionSubject` :98, `ConditionOperand` :105, `DraftCondition`
  :111, `ORDERING_COMPARATORS` :50; `tool-draft.spec.ts` is v4's
  suite ported case-for-case (extend it the same way).
- **`apps/web/src/app/pascal/custom-tool-types.corpus.spec.ts`** (111
  lines) — imports the committed NDJSON as text (angular.json `.ndjson`
  text loader :28-33), asserts row counts **115/10/105 at :76-78**
  (§C: these constants get the unifier marker), byte-compares verdict +
  `JSON.stringify(data)` + unknownKeys + the full
  `formatDefinitionIssues` sentence per row.
- **`apps/web/src/app/screens/custom-tools/`**: `builder-form.ts`
  (667 — validity-by-construction; the oracle card lands here),
  `outcomes-section.ts` (1,039 — chips via `DraftCondition`;
  `COMPARATOR_LABELS: Record<ComparatorKey,string>` :39-46 needs the
  two new labels + the typeOfSubject menu logic), `proving-bench.ts`
  (515 — preview :444, audit :464, "Deal a thousand hands" :225; fact
  sheet dual mode :38; renders server error strings verbatim),
  `workbench-library.ts` (451 — `TIER_ADVISORY` :36-39),
  `workbench.api.ts` (168 — the four verbs via `core.dispatchData`,
  structural reads; §B mirror lands here), plus `workbench-editor.ts`
  (763), `destination-picker.ts`, `number-or-param-field.ts`,
  `custom-tools-page.ts`, `entry-points.spec.ts`.
- **`apps/web/public/schemas/qtap-custom-tool.schema.json`** —
  byte-copy of v4's published schema ("Kept in sync by hand…").
- **Inspector**: `apps/web/src/app/chat/llm-inspector-entry.ts` (445;
  `TYPE_LABELS` :34-47, `TYPE_STYLES` :20-31) +
  `llm-inspector-panel.ts` (211; `FILTER_GROUPS` :27-37; unlisted
  types fall back to `typeLabel` :380) + specs. The llm-logs settings
  card holds its own type-label map (find the P4.6an-era file under
  `screens/settings/`).
- **The wire mirror**: `core-contract.ts` `PascalMeta` (`pascalMeta?:
  PascalMeta | null` :1761) — grows the §A `llm` field (and verify
  `metadataTested` is present; add it if the d68638b4 round missed
  the mirror). The Salon bubble (`chat/message-row.ts` :347-355,
  `system-message-labels.ts` :97-99) renders only
  `toolTitle`/`tool` — v4 renders nothing new in the bubble either;
  NO new bubble display this round (verified parity).
- Versions on main: SPA 0.5.169 (bump target); crates untouched by
  this lane.

## Tiered deliverables

### Tier 1 — must land

1. **The browser schema twin** (`pascal/custom-tool-types.ts`): every
   §A-relevant schema change mirrored — the llm block (describe texts
   + messages verbatim), LlmComparator + `ok`, StringOperand, the 6→8
   comparator widening, containment placement (Param/Metadata/Llm, NOT
   Numeric), both at-least-one messages, the containment load-checks,
   the llm-without-block rejection, KNOWN_TOP_LEVEL_KEYS. **Porting
   Zod = porting its abort/hoist rules** (the P4.6ay units-1-3
   memory). In-lane proof: the OLD 115-row committed corpus stays
   green (old behavior is unchanged — a red old row is a porting bug
   here), plus hand-written specs pinning every NEW arm (verdict +
   message text) mirroring v4's 28 new definition tests. The §C
   marker comment goes on the corpus-spec count constants.
2. **The tool-draft bijection** (`pascal/tool-draft.ts`): all the
   surveyed changes — fields, chips, serialization (incl. "succeeded
   ≠ true" → `ok: false`, maxOutput omitted while blank, KNOWN_KEY_ORDER
   position), the validation arms with verbatim strings, the
   containment audit with its early-return, slot keys, rename/
   references. Port v4's +12+3 tool-draft tests case-for-case into
   `tool-draft.spec.ts`.
3. **The Workbench screens**: BuilderForm's consulted-oracle card
   (toggle, prompt + counter, "When the oracle is silent", answer cap,
   inline issues, `{{llm}}` in the insert menu); OutcomesSection's
   llm/llm-ok subjects (gated on `llmEnabled`), the typeOfSubject
   comparator menus, the two labels, the text-only operand widget with
   its coercions, string-param filtering under containment;
   ProvingBench's oracle card (scripted/fail/live for preview,
   scripted/fail ONLY for audit — §B); the library `oracle` badge.
   Mirror v4's `workbench-llm.test.tsx` 9 cases as Angular specs
   (beware the template-backtick traps and effect-latch rules — the
   P4.6bb memory).
4. **`workbench.api.ts`**: the §B body additions on preview/audit
   (typed locally; no new verbs — §D).
5. **The schema asset**: byte-copy v4 `616930db`'s
   `qtap-custom-tool.schema.json` into `apps/web/public/schemas/`
   (diff against v4's file must be empty — byte-for-byte, the P4.6bb
   discipline).
6. **The Inspector + logs card**: CUSTOM_TOOL_CONSULT label/style in
   `TYPE_LABELS`/`TYPE_STYLES`, the `other` filter group, the
   llm-logs-card label `'Custom Tool'` — each pinned by a spec.
7. **The wire mirror**: `PascalMeta.llm` per §A in `core-contract.ts`
   (+ `metadataTested` if missing). Type-only; no rendering change.

### Tier 2 — should land

8. **An e2e beat, ACTIVATE-AT-UNIFY**: extend this lane's own
   workbench e2e spec with a consulted-oracle walk — author a tool
   with an llm block in the editor, see the oracle card, run a
   scripted preview through the bench, see the answer-gated outcome.
   The server half is lane D8's, so the beat is written against the §B
   contract and marked ACTIVATE-AT-UNIFY (in-lane it runs only as far
   as the pre-server steps allow; never let it silently skip — the
   guarded-skip pattern with a loud marker). Everything editor-side
   (draft round-trip, validation issues) walks live in-lane.

### Tier 3 — explicit deferrals (loud, named)

- **Salon bubble display of `pascalMeta.llm`** — v4 shows nothing in
  the bubble (Inspector-only); verified parity, not a gap.
- **The Insert-Announcement dialog** (`979aec66`) — the whole surface
  is unported (lane D8's disposition table banks it).
- **A live-consult bench walk** — real provider spend; scripted covers
  the UI.

## The differential requirement, restated

The SPA half is tier 4: `ng test` + the Playwright walk, with v4's own
new test suites ported case-for-case and every transcription pinned by
a unit spec carrying v4 file:line. The schema-twin work additionally
rides the §C corpus differential (the committed NDJSON — regenerated by
lane D8, reconciled at unification; **mutate-check the corpus spec if
it passes first try** — the P4.6bb memory). Recipes:
`p4.6bb-workbench-spa-lane`, `p4.6ay-units-1-3-zod-and-js`,
`e2e-mutation-check-needs-a-rebuilt-dist`,
`added-affordance-breaks-sibling-by-name-locators`.

## Fixtures

- **Consumes:** the committed 115-row corpus NDJSON (unchanged
  in-lane; §C at unification), the committed e2e locked instance +
  the P4.6ay-era `seedPascalToolsFixture` seed.
- **Delivers:** nothing new committed beyond specs; if the e2e beat
  needs an llm-block tool seeded, extend the Pascal seed helper it
  already owns the pattern for (never assert absolute counts a sibling
  spec can grow).

## Shared contract (BINDING — identical in `p4.d8-llm-consult-server.md`; P4.9a carries §E by reference)

### §A — the `pascalMeta.llm` record

Shape and key order (v4 `chat.types.ts:378-385` / `custom-tools.ts
LlmConsultResult`): `{ ok: boolean, output: string, prompt: string,
reason?: string, provider?: string, model?: string }` — optionals
OMITTED when absent, never null. It sits inside pascalMeta at v4's
declaration position (after `metadataTested`, before `invokedBy`).
`output` is the model's trimmed-capped-retrimmed answer on success, or
the author's `errorMessage` on failure; `reason` is the technical cause
and never appears in the fiction. Lane D8 owns the Rust struct; lane BC
mirrors it as the `PascalMeta.llm` type in
`apps/web/src/app/core/core-contract.ts` (type-only; no new rendering).

### §B — the workbench payload additions

`POST /api/v1/custom-tools?action=preview` body gains
`llm?: {live: true} | {output: string} | {fail: true}` (nullish;
`output` 1–100,000 chars). `?action=audit` body gains
`llm?: {output: string} | {fail: true}` (nullish — **no live arm;
audits never call live, enforced by shape**). The preview RESPONSE is
the whole run result including the §A record (scripted runs carry
`provider: 'bench', model: 'simulated'`); the audit response shape is
unchanged. The library entry gains `llm: boolean`. Lane D8 owns the
server types + `p4_6ay_workbench_wire_contract`; lane BC mirrors the
bodies in `workbench.api.ts` (structural reads as today — no pinned
response variants).

### §C — the definition corpus wire (unifier-applied)

The definition corpus lives in
`harness/oracle/cases/pascal-custom-tool-definition.ts` (lane D8's
file). Lane D8 extends it with the new-feature rows, regenerates, and
COMMITS `apps/web/src/testing/fixtures/pascal-custom-tool-definition.oracle.ndjson`
(+ README provenance) in its own lane, recording the new
total/accept/reject counts in its status header. Lane BC does NOT touch
the NDJSON: in-lane it keeps the 115-row corpus green (the old rows'
behavior is unchanged — a red old row means a BC porting bug) and pins
every new parse arm with its own hand-written specs; its corpus-spec
count constants (`custom-tool-types.corpus.spec.ts:76-78`) carry a
`// §C: unifier updates to D8's recorded counts` marker. At
unification, the unifier sets the constants to D8's recorded counts and
the corpus spec must pass byte-for-byte — a failure there is a BC parser
bug, not a corpus problem.

### §D — no new dispatch verbs

Neither drift lane adds or renames a wire verb. The four §W workbench
verbs and the chat custom-tools pair keep their names; only the §B
payload fields are added. (Lane A's photos verbs are its own order's
§3 contract, unchanged.)

### §E — the round's KNOWN Rust conflict set (with lane A)

Lane D8 and lane A (P4.9a) both touch
`crates/quilltap-core/src/api/types.rs`, `api/engine.rs`, `api/mod.rs`,
and `crates/quilltap-web/src/lib.rs` — D8 with §B payload additions to
EXISTING variants, A with its NEW photos verbs/routes. Additions only;
the unifier reconciles (temp-branch cherry-pick + one full gate — the
parallel-round-reconciliation memory). No other Rust file is shared.
Lane BC and lane A are disjoint in `apps/web/` (BC: pascal/,
screens/custom-tools/, chat/llm-inspector*, core-contract.ts §A region,
settings llm-logs card; A: screens/photos/, images/, gallery-tab,
its §1 route block). `app.routes.ts` is lane A's alone this round.

## Ownership (BINDING — identical in `p4.d8-llm-consult-server.md`)

- **P4.d8 (lane D8):** `crates/quilltap-core/src/pascal/**` (incl.
  the new `llm_consult.rs`), `crates/quilltap-core/src/tools/run_custom.rs`,
  `crates/quilltap-core/src/api/custom_tools.rs`,
  `crates/quilltap-core/src/db/chats_messages.rs` +
  `chats_messages_read.rs` (the pascalMeta regions),
  `crates/quilltap-core/src/services/llm_logging.rs` +
  `cheap_llm_exec.rs` (additive), `services/pascal_writer.rs`,
  `crates/quilltap-web/src/custom_tools_routes.rs`; the §E conflict-set
  files (additive); harness: all ten pascal differentials + their
  oracle cases + the shared corpora + both pascal fixture families;
  `apps/web/src/testing/fixtures/pascal-custom-tool-definition.oracle.ndjson`
  + that dir's README (§C — its ONLY `apps/web` files). Version bumps:
  quilltap-core, quilltap-harness, quilltap-web (+Cargo.lock).
- **P4.6bc (this lane):** `apps/web/src/app/pascal/**`
  (custom-tool-types.ts, tool-draft.ts + specs, corpus spec — NOT the
  NDJSON), `apps/web/src/app/screens/custom-tools/**`,
  `apps/web/src/app/chat/llm-inspector-entry.ts` +
  `llm-inspector-panel.ts` (+ specs),
  `apps/web/src/app/core/core-contract.ts` (the §A PascalMeta region —
  exclusively this round), the settings llm-logs card file,
  `apps/web/public/schemas/qtap-custom-tool.schema.json` (byte-copy),
  its own e2e spec file. Version bump: SPA `package.json`.
- **P4.9a (lane A):** per its own order's Ownership section (photos
  Rust + SPA surfaces; §E conflict set additive; its §1 route block;
  bumps core/harness/web/SPA). Its resume addendum carries the round's
  new baseline and sibling list.
- **Append-only for everyone:** `docs/CHANGELOG.md`,
  `docs/developer/porting/status-log.md` (rebuild unions from lane
  appends at unification — the M6-round memory). Version bumps union
  at unify; the unifier RECOUNTS (identical bumps merge silently).
- **Nobody** edits `CLAUDE.md`, `phase-4.md`, `m6-screen-parity.md`, or
  a sibling lane's files; the unifier updates the phase docs and moves
  the oracle-baseline paragraph to `616930db`.

## Verification gate (run before declaring done)

- `ng test` green (from `apps/web`), incl. the untouched 115-row
  corpus spec, the extended tool-draft suite, and the new component
  specs; `ng build` clean.
- Full Playwright green, run ALONE on port 4319 from `apps/web` with
  THIS worktree's own Rust server binaries (the P4.6af memory); the
  editor-side walks live; the §B-dependent bench step marked
  ACTIVATE-AT-UNIFY (loud, never a silent skip).
- The schema asset byte-diffs empty against v4 `616930db`.
- `cargo test --workspace` untouched-baseline check (no `crates/**`
  edits — confirm the diff shows none).
- No `[value]` binding on any `<select>` with async options (the
  standing dogfood-#6 rule); mind
  `added-affordance-breaks-sibling-by-name-locators` when adding the
  oracle badge/card.
- Every commit follows `.claude/commands/commit.md` (SPA version bump
  per Ownership; CHANGELOG entry; status-log append at lane close).
