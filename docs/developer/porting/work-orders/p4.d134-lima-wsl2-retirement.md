# Work order P4.D134 — the Lima/WSL2 retirement (v4 `1560bd43b`) + the About Discord-link rider (`7fb668263`)

**Status:** CLOSED (2026-08-31; unified 2026-09-01). All of Tier 1 and Tier 2
landed on `claude/p4-lima-wsl2-retirement-894ad2` (six commits + the gate
record): the host env/lock retirement with Tier R red-first (212 → 214/0), the
data-dir `isVM` wire deletion with the two renamed deletion pins, the
host-rewrite collapse, the self-inventory/almanack retirements (almanack
measured as pure doc convergence), the SPA About/footer/profile mirrors with
the Discord rider, the base-path disjunct drop, and the grep census (zero
production Lima/WSL2 consultation; the four surviving hit kinds enumerated).
Tier 3 discharged as ordered: 2 help rows banked to `p4.9i2`; `7819afb1d` and
`3c3432ae9` CONFIRMED NO-PORT with file lists (stamped in the ledger at the
round-1 unification). **The one follow-up this lane opens:** the host-side
gateway resolver is unported (v5 has never rewritten a localhost URL —
measured, named in `rewrite.rs`'s header; wants its own order). The §3
unification review found no blocking findings in this lane (one note: a stale
test name, renamed at unification).

## Preamble

- **v4 oracle baseline: `b121ac77f`** (drift-ledger §1, checked 2026-08-31).
  **This lane's target commit is `1560bd43b`** — v4 drops the managed Lima
  (macOS) and WSL2 (Windows) VM modes; Docker is now the only sandbox and a
  self-managed VM opts in by env var. The lane also carries the one ported
  hunk of **`7fb668263`** (the About Discord link) as a rider, and
  **ratifies with evidence** the two adjacent NO-PORT? rows `7819afb1d`
  (CI-only restore-key test plumbing) and `3c3432ae9` (4.9.0 release-notes
  prose) so the unifier can move the baseline past them.
- **Freshness probe at lane start** (drift-ledger §2). Pass = checkout on
  `main`, tree clean, no commits past `60e3c4a0a` on main and none past
  `3a76b17df` on bugfix. **Any mismatch: STOP and report.**
- **Regen rule in force: PIN REQUIRED** (ledger §1 — v4 HEAD is fifteen
  commits past the baseline). Every oracle/fixture regen this lane runs
  comes from a lane-unique detached worktree pinned at the TARGET commit
  **`1560bd43b`**: `/tmp/qt-v4-pin-p4d134-1560bd43b`, all three symlink
  classes, per ledger §5.1. Verify the pin (e.g. `lib/paths.ts` in the pin
  must NOT contain `isLimaEnvironment` — the deletion is the marker).
  Families this lane does NOT move regen only if needed, at the baseline
  `b121ac77f`.
- **Sibling lanes:** P4.D135 → P4.D136 (one stacked branch —
  `p4.d135-provider-fallback-chains.md`,
  `p4.d136-reroute-and-cheap-llm-budgets.md`) and P4.D137
  (`p4.d137-doc-edit-guards-and-typographic-folding.md`). §Ownership below
  is binding and identical across all four orders. The lanes meet nowhere
  in source. **This lane runs `ng` tests but NOT Playwright** — the
  D135/D136 branch is the round's only Playwright runner (port 4319 is
  repo-wide).
- **Worktree:** per the `carryout` skill; edit inside the worktree, not
  main. `CHANGELOG` and `status-log.md` append-only.

## Mandate

Retire Lima and WSL2 from every v5 surface that carries them, matching v4's
`1560bd43b` hunk-for-hunk: the environment/lock family in the host and CLI,
the data-dir service and its wire (the `isVM` key is DELETED), the host-URL
rewrite collapse to two strategies, the base-path-availability containment
probe, the Almanack `runtimeType`, the self-inventory runtime modes and
labels, and the SPA About/footer/profile mirrors — plus the `7fb668263`
About Discord-link hunk. The lane must NOT touch the chat spine, the
cheap-LLM/provider files, or the doc-edit modules (sibling ownership), and
must not invent behavior for the deleted branches: this is a deletion port,
and the differential proof is that the deleted arms are GONE, not stubbed.

## Survey-verified starting points (2026-08-31, from the shipped hunks — ledger §5.3: never port from the commit prose)

v4 `git show 1560bd43b` — the ported hunks and their v5 twins:

1. **`lib/database/backends/sqlite/instance-lock.ts` (−42/+…):**
   `EnvironmentType` drops `'lima'`/`'wsl2'`; detection drops the
   lima-before-docker probe; the display label set collapses to ONE
   containerized label (`Docker container`) and the cross-host heartbeat
   arm narrows to `environment === 'docker'`. → `quilltap-host/src/env.rs`
   (`EnvironmentType::{Lima,Wsl2}`, `is_lima_environment()`, the probe
   order) + `quilltap-host/src/lock.rs` (the `is_vm` `matches!`, the
   `"Lima VM"`/`"WSL2 instance"` labels at 4 sites, the stale-reason arm).
   ⚠ These are **user-visible sentences** the Tier R lock arms compare —
   expect Tier R reds first, then green after the port.
2. **`packages/quilltap/{bin/quilltap.js,lib/lock-helpers.js}`:** the CLI
   lock display + `detectEnvironmentType` lose the same arms. →
   `quilltap-cli/src/db_cmd.rs` `is_vm_environment()` and the CLI's lock
   display strings. Tier R drives v4's REAL launcher from the pin, so the
   arms self-verify.
3. **`lib/paths.ts` (−34/+…):** `isLimaEnvironment()` deleted; the Lima
   branches in `getPlatform()` and `getHostDataDir()` gone. →
   `quilltap-core/src/services/data_dir.rs` (`is_lima_environment`, the
   Lima-first `getPlatform`, the `LimaContainer` env capture).
4. **`app/api/v1/system/data-dir/route.ts` (−5):** the **`isVM` response
   key is DELETED** — a wire AND key-order change. →
   `quilltap-core/src/api/data_dir.rs` (the response builder and its
   key-order list) + harness family **`data_dir_paths_equivalence`** (its
   `isVM` comparand must go, red-first) + SPA
   `apps/web/src/app/screens/profile/profile.api.ts` / `profile.spec.ts`.
5. **`lib/host-rewrite.ts` (−127/+…):** five gateway strategies → two
   (`QUILLTAP_HOST_IP`, then `host.docker.internal` in Docker);
   **`isVMEnvironment()` changes meaning** to
   `isDockerEnvironment() || QUILLTAP_HOST_IP is set` (a self-managed VM
   opts in by env var — carry v4's why-comment: the `/proc/net/route`,
   `/etc/resolv.conf` and `/etc/hosts` fallbacks existed only for Lima and
   were actively wrong for Docker). →
   `quilltap-core/src/provider_manifest/rewrite.rs`.
   (`packages/plugin-utils/src/host-rewrite.ts` carries the same change —
   NO-PORT, v5 ships no plugin SDK, but diff it against the lib copy to
   confirm they agree before ratifying.)
6. **`lib/mount-index/base-path-availability.ts` (−6/+…):**
   `isContainerized()` drops the Lima disjunct. → the P4.D67 bug-56 port
   (grep `quilltap-core/src` for `is_containerized` / the bug-56 module).
7. **`lib/tools/almanack/{types,phase1-premises}.ts`:** `runtimeType`
   drops `'lima'`. → `quilltap-core/src/almanack/{types,phase1_premises}.rs`
   + `quilltap-host/src/almanack_services.rs` → family
   `almanack_tier2_equivalence`. **MEASURE first** (the ledger row's note):
   v5 may already never emit `'lima'`, making this pure doc-comment
   convergence — record which it was.
8. **`lib/tools/self-inventory-tool.ts` +
   `handlers/self-inventory/{builders,formatters}.ts`:**
   `SelfInventoryRuntimeMode` drops `'vm'`/`'electron-vm'` and the two
   `RUNTIME_MODE_LABELS` rows (`"VM (Lima/WSL2)"`, `"Electron + VM"`) —
   **prompt-visible bytes**. → `quilltap-core/src/tools/self_inventory.rs`
   + the self-inventory family. ⚠ Bug 112 (`735d9408c`, round 2) also
   lands in this file — do NOT absorb any `735d9408c` hunk here.
9. **SPA:** `components/footer-wrapper.tsx` (`BackendMode` loses the VM /
   Electron+VM badges) → v5's footer twin; `app/about/AboutView.tsx`
   (three prose blocks + the tech list now describe the shell's three back
   ends: Direct, Docker, Remote) →
   `apps/web/src/app/screens/about/about-page.ts` + `about.spec.ts`
   (strings spec-pinned, the P4.D128 precedent). **Rider:** `7fb668263`'s
   one AboutView hunk (the updated Discord link) lands in the same file —
   take the LINK from `7fb668263`'s post-commit file so the two hunks
   compose, and note the rider in the lane record.
10. **`help/{chat-settings,the-almanack}.md`** → bank the rows to
    `p4.9i2` (list them in the lane record).
11. **NO-PORT remainder** (ratify in the lane record with the file list):
    `lima/`, `scripts/build-rootfs.mjs`, `Dockerfile.ci`,
    `.github/workflows/release.yml`, `docker/entrypoint.sh`,
    `docs/WINDOWS.md`, README/DEPLOYMENT/DDL/DEVELOPMENT,
    `packages/plugin-utils` + `plugins/dist/qtap-plugin-mcp`, v4's own
    test deletions, `.gitattributes`, `.claude/commands/*`.

**The two NO-PORT? ratifications** (evidence, not subject lines):
`7819afb1d` — verify zero `lib/`/`app/` hunks (jest mock factory + README
+ CHANGELOG + version bumps only; the surface it tests is P4.D133's Tier R
domain, already driven against v4's real launcher). `3c3432ae9` — verify
one file, `docs/releases/4.9.0.md`. Record both verdicts + file lists in
the lane record for the unifier to stamp NO-PORT-RATIFIED.

## Tiered deliverables

**Tier 1 (must land):**
1. The host env/lock retirement (survey items 1–2) with the Tier R lock
   arms red-first where the sentences moved, → green.
2. The data-dir service + wire (items 3–4): `isVM` deleted from the
   response AND the key-order list; `data_dir_paths_equivalence`
   regenerated at the pin, red-first on the dropped key.
3. The host-rewrite collapse (item 5) with v4's why-comment carried.
4. The self-inventory + almanack retirements (items 7–8), each measured
   (did v5 ever emit the dropped value?) and family-verified.
5. The SPA About/footer/profile mirrors (item 9) spec-pinned, including
   the Discord-link rider.
6. The base-path-availability disjunct drop (item 6).

**Tier 2 (should land):**
7. A grep-census proving zero remaining `lima`/`wsl2`/`Lima`/`WSL2`
   occurrences in v5 source outside comments that cite the sha (the
   deletion port's cheap completeness check); record hits that stay and
   why.

**Tier 3 (explicit deferrals, loud):**
8. Help rows → the `p4.9i2` bank.
9. The NO-PORT ratification evidence for `7819afb1d`/`3c3432ae9` (the
   unifier stamps the ledger).

## The differential requirement, restated

Every moved family regenerates from the `1560bd43b` pin and re-runs
red-first where bytes moved: `data_dir_paths_equivalence`,
`almanack_tier2_equivalence`, the self-inventory family, and the Tier R
lock/display arms (Tier R drives v4's REAL launcher from the pin — the
`-- --nocapture` case-count rule applies). Grep each regenerated NDJSON
for the CHANGED bytes (ledger §5.2 — a green regen is not coverage): the
deleted `isVM` key must be ABSENT, the single `Docker container` label
PRESENT. A deletion that no family can see gets a unit pin instead —
name it.

## Fixtures

No committed DB fixture changes expected. If `data_dir_paths_equivalence`
or the almanack family turn out to consume a committed fixture that bakes
an `isVM`/`lima` value, this lane owns regenerating it at the pin and
re-running every family that consumes it (name them in the lane record).

## Shared contract

None — this lane meets no sibling in source. (Binding statement, identical
in all four orders: the only shared surfaces this round are the append-only
`CHANGELOG` and `status-log.md`, and the drift ledger, which lanes never
write.)

## Ownership (binding, identical across all four orders)

| Area | Owner |
|---|---|
| `crates/quilltap-host/src/{env.rs,lock.rs}`, `crates/quilltap-cli/**` | **P4.D134** |
| `quilltap-core`: `services/data_dir.rs`, `api/data_dir.rs`, `provider_manifest/rewrite.rs`, `almanack/**`, `tools/self_inventory.rs`, the base-path-availability module | **P4.D134** |
| SPA: `screens/about/**`, `screens/profile/**`, the footer twin | **P4.D134** |
| `quilltap-core`: `services/{provider_failover,primary_stream,orchestrator,turn_orchestrator,cheap_llm_exec,file_fallback,compression*,context_summary*,memory_processor,title_update_job,story_background_job,carina_memory_extraction}.rs`, `services/scene_state_tracking*`, `services/dangerous_content/provider_routing.rs`, `files/{image_transport,attachment_support}.rs`, the NEW fallback-engine + message-attachment-adapter modules, `db/{connection_profiles,chat_settings}.rs`, `connection_profile_legacy_fields.rs`, `services/backup/uuid_remap*.rs`, `services/quilltap_import/reconcile.rs`, `services/provisioning/fresh_schema.json`, `services/qtap_export/schema-key-order.json`, the profile/settings verb edges (`api/**` hunks they need), `quilltap-web` route hunks | **P4.D135 → P4.D136** (one stacked branch) |
| SPA: `screens/settings/providers/**`, `core/chat-stream.reducer.ts`, the streaming status display | **P4.D135 → P4.D136** |
| `quilltap-core`: `doc_edit/diacritics.rs`, the NEW typographic-folding module, `tools/doc_edit/{text,shared}.rs`, the doc_grep literal path | **P4.D137** |
| Playwright (port 4319) | **P4.D135 → P4.D136 branch only** |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |

**Version bumps:** this lane bumps `quilltap-core`, `quilltap-harness`,
`quilltap-host`, `quilltap-cli`, and the SPA per commit; the unifier
recounts at unification (the silent-auto-merge trap is on record).

## Verification gate before declaring done

`cargo fmt --check`; `cargo clippy` both feature sets; full
`cargo test --workspace` with the lane's oracle env vars
(`run_in_background`, full log + sentinel — NEVER a pgrep loop, NEVER
`| tail`); the lane's families by name, zero SKIP, from the `1560bd43b`
pin with changed-bytes greps; Tier R with `-- --nocapture` and the case
count read; `npm test` + `npm run build` in `apps/web` (no bare `ng`);
NO Playwright (sibling owns the port). Per-commit rule:
`.claude/commands/commit.md`.
