# Work order P4.D165 — character subprompts, the SPA half: the shared picker + editor modal + query hook, the Subprompts section on the Aurora System Prompts tab, the pickers in both New-Chat forms and on the Salon participant card, the realtime key, the thirteenth field hint, parity specs, and the gated e2e walk

**Status: LANE COMPLETE — every Tier 1 and Tier 2 item landed; awaiting
unification.** Ordered 2026-09-07, the `2f4254b42` character-subprompts round.
Branch `claude/p4-d165-character-subprompts-af51e1`, six commits (`c2495495`,
`3ac62acd`, `edc1c4dc`, `e6962cf5`, `c2c15d02`, `8461c193`); lane record in
`status-log.md`.

**What the unifier still owes this order:** flip `P4D163_SERVER_LANDED` to
`true` in `e2e/character-subprompts-flow.spec.ts:33` once P4.D163/P4.D164 have
landed, and run its three beats as their first execution (the file's header
carries the recipe — the character is **Bram**, nothing is pre-seeded, and the
New-Chat beat tears down what it writes). The §C name-for-name diff should read
this lane's spellings out of the status-log record's table.

**Two Tier 3 deferrals are recorded, not silent:**
`encodeURIComponent(subpromptId)` (v5 builds no URL — the id rides as a JSON
field) and the Lexical `namespace` prop (no ProseMirror twin).

**Three order premises were corrected by measurement** — the hint table moves
thirteen → fourteen keys, not twelve → thirteen; `subpromptErrorMessage` is NOT
`coreErrorMessage` (v4 ships two helpers with different tails, and they disagree
visibly on a blank-message error); and v4's single-character New-Chat picker has
no v5 home because only v4's unported `NewChatModal` ever renders it, while v4's
PAGE — this screen's counterpart — renders nothing there either. All three are
written up in the lane record.

## Preamble

- **v4 oracle baseline: `f699da6f6`;** this order absorbs the CLIENT half of
  **`2f4254b42`** (eleven client files, ~640 lines — the ledger's one row,
  `ORDERED(P4.D163 ∥ P4.D164 ∥ P4.D165)`). Spec reads from `git show
  2f4254b42:<path>` and the hunks — never the commit message.
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2 values).
  **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED (§R.3).** This lane runs NO Rust oracle regen;
  its one v4-side comparand is the client module read at the pin
  (`/tmp/qt-v4-pin-p4d165-2f4254b42` — the P4.D103 idiom of EXECUTING v4's
  `field-hints.ts` at the sha to diff the hints table, and the
  `useNewChat.request-body` / `topic-map` jest suites transcribed as
  parity specs).
- **Sibling lanes:** P4.D163 → P4.D164 (the server — you meet them ONLY at
  §C; until they land, every verb you call answers `unknown request type`,
  so your specs mock the `CoreClient` and your beats gate), P4.9K1/K2-
  resumed (Rust only; `apps/web/**` is yours alone — §R.6).
- **Worktree:** `git worktree add <path> -b claude/p4-d165-subprompts-spa-
  <hash> main`; `npm ci` inside it (`spa-worktree-node-modules` — never
  `cp -a`); edit inside it. Port 4319 is repo-wide — coordinate the ONE
  Playwright run (§R.9).

## Mandate

Port v4's subprompts client whole: the shared `components/subprompts/`
trio (the query hook, the inline picker, the editor modal) as Angular
standalone components over the §C verbs; the `SubpromptsSection` beneath the
prompt list on the character editor's System Prompts tab; the picker under
the system-prompt select in BOTH New-Chat forms (the single-character
shortcut and the multi-character picker panel) with the create body carrying
`selectedSubpromptIds` only when non-empty and LLM-controlled; the picker on
the Salon participant card with the `Subprompts updated` round-trip through
`chatUpdateParticipant`; the `characters` realtime arm gaining the subprompts
key; the thirteenth `PROMPT_FIELD_HINTS` entry byte-exact; v4's three jest
suites as parity specs; and a gated e2e walk. Nothing in `crates/**`; no
existing e2e spec edited.

## Survey-verified starting points (fresh survey 2026-09-07 at `2f4254b42` — do not re-derive)

### v4 — the shared trio (`components/subprompts/`, NEW)

- **`useCharacterSubprompts.ts`** (101 lines): `SubpromptRecord` (§C.1),
  `SubpromptInput {title, content}`; `listUrl = /api/v1/characters/<id>/
  subprompts`, `itemUrl = listUrl/encodeURIComponent(id)`; `subpromptError
  Message(err, fallback)` reads `info.error` off an `ApiFetchError`, else
  `err.message`, else the fallback; the hook takes `(characterId, {enabled})`
  — `enabled = !!characterId && (options.enabled ?? true)`; query key
  `queryKeys.characters.subprompts(id)` = `['characters', id, 'subprompts']`
  (v5's `characterKeys` idiom is `['characters', 'prompts', id]` — add
  `subprompts: (id) => ['characters', 'subprompts', id]` in `characters.
  api.ts:53`'s shape, recorded as the spelling divergence the file already
  carries for `prompts`); `refetchInterval = useRealtimeRefetchInterval(
  60_000)` (v5: the realtime-gated fallback poll in `core/realtime.
  service.ts` — the P4.D125 idiom the regenerate cards use); three mutations
  POST / PUT / DELETE each invalidating the key; returns `{subprompts:
  data?.subprompts ?? [], isLoading: enabled && query.isLoading, error,
  refetch, create, update, remove}`. **v5 calls the §C dispatch verbs, not
  the REST URLs** (every SPA op reads through `CoreClient.dispatchData` —
  `characters.api.ts:3-6`); the `encodeURIComponent` has no v5 twin and is
  recorded.
- **`SubpromptPicker.tsx`** (156 lines) — props `{characterId,
  characterName?, selectedIds, onChange(nextIds), disabled?, size?: 'sm'|
  'md', defaultOpen?}`; state `open` (from `defaultOpen`), `editorOpen`;
  the hook is `enabled: open || selectedIds.length > 0`; `activeCount` =
  known subprompts ∩ selected; `toggle(id, on)` no-ops when unchanged, else
  appends / filters; `handleCreated(saved, mode)` ticks a `created` record
  on when not already selected; summary text: `None on file` when loaded
  and empty, `Loading…` while loading and empty, else `${activeCount} of
  ${subprompts.length} in play`; the disclosure button (`aria-expanded`,
  `aria-controls={listId}`, `title="Subprompts in play for this chat"`)
  renders `Subprompts` + ` · ${summary}` and a `chevron-down`/`chevron-right`
  icon; two class sets by size (`sm`: `qt-select qt-select-sm w-full flex
  items-center justify-between gap-2 text-left`; `md`: the bordered
  `rounded-lg` button — copy the exact class strings; the qt-class guard
  runs in `npm run lint`); the open panel: an empty line `${characterName ??
  'This character'} has no subprompts yet.`; one `<label>` per subprompt
  (`qt-checkbox`, `aria-label="Subprompt ${title}"`, `title={content.slice(0,
  200)}`, the title `truncate`d); **the missing-id rows** — ids ticked on
  that match no known record, shown ONLY when not loading, checked,
  `line-through`, `aria-label="Missing subprompt ${id}"`, `title="This
  subprompt no longer exists in the vault"`, untick → `toggle(id, false)`;
  the `New subprompt…` ghost button (`plus` icon) opening the editor. The
  modal is rendered by the picker itself with `onSaved={handleCreated}`.
- **`SubpromptEditorModal.tsx`** (147 lines) — props `{isOpen, characterId,
  characterName?, editing?: SubpromptRecord | null, onClose, onSaved?(saved,
  mode)}`; **keyed on `editing?.id ?? 'new'` so each opening mounts a fresh
  form** (no reset effect); state `title` / `content` seeded from `editing`;
  `saving = create.isPending || update.isPending`; `disabled = !title.trim()
  || !content.trim() || saving`; save: edit → `update({id, title: title.
  trim(), content})` + toast `Subprompt updated`; create → `create({title:
  title.trim(), content})` + toast `Subprompt created`; then `onSaved`, then
  `onClose`; catch → error toast `subpromptErrorMessage(err, editing ?
  'Failed to update subprompt' : 'Failed to create subprompt')`. Footer
  `Cancel` (secondary, disabled while saving) + `Saving...` / `Update` /
  `Create` (primary). Title text `Edit Subprompt — ${name}` / `New
  Subprompt for ${name}` (the em-dash and `for` forms; name optional).
  Body: `qt-label` `Title` + required star; `<input id="subprompt-title"
  maxLength={100} placeholder="e.g., Keep it brief, Speak in verse, No
  spoilers" className="qt-input">`; when editing, the note `Kept on file as
  <code>{path}</code>. The file name stays put when the title changes, so
  chats that have this subprompt in play keep it.`; then `PromptFieldLabel
  hint={PROMPT_FIELD_HINTS.subprompt} label="Instruction" required helper=
  {`${hint.helper} Markdown is supported, and {{char}} / {{user}} substitute
  the character and user names.`}` (v5: `qt-prompt-field-label`,
  `ui/prompt-field-label.ts:35`) and `MarkdownLexicalEditor` with
  `remountKey={editing?.id ?? 'new'}`, `ariaLabel="Subprompt instruction"`,
  `minHeight="10rem"` (v5: `qt-markdown-field`, `editor/markdown-field.ts:74`
  — the P4.D138/#107 host-class rule applies; give the field its height).
  `BaseModal maxWidth="2xl"` (v5's modal idiom: the `qt-prompt-modal` on the
  same tab is the sibling to copy).
- **`index.ts`** re-exports the three + the two types + `subpromptError
  Message`.

### v4 — the Aurora section (`components/characters/system-prompts-editor/SubpromptsSection.tsx`, 143 lines, + `index.tsx:+4`)

Mounted by `SystemPromptsEditor` right after `<PromptList …/>` and before the
`PromptModal`, with `characterId` + `characterName`. Container `space-y-4
pt-6 border-t qt-border-default`; header row: `<h3 class="qt-heading-4
text-foreground">Subprompts</h3>` + the `qt-text-small` paragraph (`Smaller
instructions ${characterName} may carry into a particular chat. Each lives as
a Markdown file in the vault's <code>Subprompts/</code> folder, and is
switched on or off per conversation from the New Chat dialog or the
Participants drawer. Write them to the character in the second person, as
you would a system prompt.` — the `&rsquo;` is a typographic apostrophe) and
the `+ Add Subprompt` primary button; states: `Loading subprompts...`;
empty → a `qt-card` with `No subprompts yet. Add one to have it on offer
when a chat begins.` + `Create First Subprompt`; list → one `qt-card
hover:bg-accent/50 transition` per record with the title (`qt-text-primary
truncate`), the path (`text-xs qt-text-secondary truncate`, `title={path}`),
a `line-clamp-2` preview of `content` cut at 150 chars + `...`, an Edit
icon-button (`pencil`, `aria-label="Edit subprompt ${title}"`) and a Delete
icon-button (`trash`, `hover:qt-text-destructive`, `aria-label="Delete
subprompt ${title}"`) whose click toggles an INLINE confirm popover
(`absolute right-0 top-full mt-1 … min-w-[220px]`, `Delete this subprompt?
Any chat with it in play drops it.`, `Delete` destructive-sm disabled while
`remove.isPending`, `Cancel` secondary-sm). Delete → toast `Subprompt
deleted` / error toast `subpromptErrorMessage(err, 'Failed to delete
subprompt')`; `finally` clears the confirm. One editor modal for both create
and edit (`isOpen={creating || editing !== null}`). v5 home:
`screens/characters/edit/system-prompts-tab.ts` (`CharacterSystemPromptsTab`,
`:147`; the prompt list + modals end at `:146`; inputs `characterId` /
`characterName`).

### v4 — New Chat (`components/new-chat/`)

- `types.ts:115` — `SelectedCharacter.selectedSubpromptIds?: string[]`. v5:
  `screens/new-chat/new-chat.types.ts:96 NewChatSelectedCharacter`.
- `hooks/useNewChat.ts:751-768` — the create body's participant carries
  `selectedSubpromptIds` ONLY when `sc.controlledBy === 'llm' && length >
  0`, else the key is OMITTED. v5: `screens/new-chat/new-chat.logic.ts:189
  buildCreateRequest` (the spread idiom already used for
  `selectedSystemPromptId`).
- `CharacterPickerPanel.tsx:143-149, :307-317` — `handleSubpromptsChange(
  characterId, ids)` maps the selection; the picker renders under the
  prompt select `{sc.controlledBy !== 'user' && (<div class="mt-2"> …
  `disabled={disabled}` …)}`. v5: `screens/new-chat/character-picker-panel.
  ts` (`onPromptChange` at `:225` is the sibling handler; the prompt select
  at `:120-131`).
- `NewChatForm.tsx:278-286, :511-522` — the single-character shortcut:
  `{showSingleCharacterControls && singleLlm && (<div><label class="mb-2
  block text-sm qt-text-primary">Subprompts</label><SubpromptPicker …
  disabled={creating}/></div>)}` placed AFTER the system-prompt block and
  BEFORE the `Play As` (`!isAutonomous && playAsOptions.length > 0`) block.
  v5: `screens/new-chat/new-chat-form.ts` (`singleLlm` computed at `:407`;
  find the single-character system-prompt block and the play-as block to
  place it between).
- Reset semantics: v5's `new-chat.state.ts:406-417, :534` seeds
  `selectedSystemPromptId` on pick / user-flip — the new field seeds `[]`
  (absent) in the same three places, and flipping a seat to user-controlled
  keeps the ids in state but the body omits them (v4 keeps them too —
  measure `useNewChat`'s user-flip handler: it does NOT clear
  `selectedSubpromptIds`).

### v4 — the Salon (`components/chat/ParticipantCard.tsx:+21`, `ChatSidebar.tsx:+2`, `app/salon/[id]/*`)

- `ParticipantData.selectedSubpromptIds?: string[]` (`:63`);
  `onSubpromptsChange?(participantId, ids)` (`:102`); render at `:487-499`
  — `{isCharacter && !isUserParticipant && !isUserControlledCharacter &&
  entity && onSubpromptsChange && (<div class="mt-1"><SubpromptPicker
  characterId={entity.id} characterName={name} selectedIds={participant.
  selectedSubpromptIds ?? []} onChange={(ids) => onSubpromptsChange(
  participant.id, ids)} size="sm"/></div>)}` — placed AFTER the system-
  prompt row and BEFORE the talkativeness slider. v5: `chat/sidebar/
  participant-card.ts` (the prompt row `:196-232`, the talkativeness block
  follows; `systemPromptChange` output at `:454` is the sibling).
- `useParticipants.ts:137` projects `selectedSubpromptIds: p.selected
  SubpromptIds ?? []` from the enriched participant; `types.ts:195`.
- `useChatControls.ts:464-493 handleSubpromptsChange(participantId, ids)`:
  `POST /api/v1/chats/${chatId}?action=update-participant` with
  `{updateParticipant: {participantId, selectedSubpromptIds}}`; non-OK →
  throw `errorData.error || 'Failed to update subprompts'`; success toast
  `Subprompts updated` + `fetchChat()`; catch → error toast (`err.message`
  or `Failed to update subprompts`). v5: `screens/salon/salon-conversation.ts
  :2422-2435` (`handleSystemPromptChange` is the sibling) through
  `chat/chat-cast.api.ts:107 updateParticipant` (add the field to its
  input type `:88` region) → `chatUpdateParticipant`.
- `ChatSidebar.tsx:166, :843` threads the callback; `SalonView.tsx:1817`
  binds `chatControls.handleSubpromptsChange`.

### v4 — the smalls

- `components/prompt-fields/field-hints.ts:84-89` — the `subprompt` entry
  (`label: 'Subprompt'`; helper `A smaller instruction the character may
  carry into a particular chat, switched on or off per conversation. Written
  to the character directly, in the second person, exactly as a system
  prompt is — it lands immediately after the system prompt and is read by
  nobody else.`; example `You keep every reply under three sentences unless
  asked for more.`), inserted AFTER `systemPrompt` and BEFORE
  `physicalDescription`. v5: `ui/prompt-field-hints.ts:84` — the P4.D103
  table proven byte-identical to v4's module by EXECUTING it at the sha
  (`ui/prompt-field-hints.spec.ts`); regenerate that comparand at
  `2f4254b42` (twelve → thirteen keys, ORDER asserted).
- `lib/query/keys.ts:31` + `lib/realtime/topic-map.ts:79` — the
  `characters` topic with an id fans out to `detail`, `prompts`,
  **`subprompts`**, `photos` in THAT order. v5: `core/realtime-topic-map.ts:
  90-93` + `characterKeys`; the parity spec `realtime-topic-map.spec.ts`
  gains v4's `topic-map.test.ts` +1 line (`queryKeys.characters.subprompts(
  'c9')` in the expected list).
- `__tests__/unit/components/workspace/tab-activation-refetch.test.tsx`
  +1 (`queryKeys.characters.subprompts('char-1')` among the characters
  keys the re-activation refetch expects) — v5's `workspace/core/tab-
  refetch.ts` kind→prefix map: measure whether the `characters` prefix
  already covers the new key (it is a prefix match on `['characters']` —
  if so, the spec gains the assertion and no code moves).
- `__tests__/unit/lib/services/chat-enrichment.service.test.ts` +1
  (`selectedSubpromptIds: []` in the expected enriched participant) — the
  server's; the SPA's `EnrichedParticipantDetail` twin (`core-contract.ts:
  2867` region) gains `selectedSubpromptIds: string[]`.
- `components/new-chat/__tests__/useNewChat.request-body.test.tsx:127-171`
  — three cases: omitted when none ticked; verbatim `['terse','verse']` for
  an LLM seat; never sent for a user seat even when its state carries
  `['stray']`. Transcribe as three specs over `buildCreateRequest`.

### v5 — where the counterparts live (verified 2026-09-07)

| v4 | v5 | note |
|---|---|---|
| `useCharacterSubprompts` | NEW `apps/web/src/app/subprompts/subprompts.api.ts` (the five verbs) + the query in the components (TanStack Angular, `injectQuery` idiom of `system-prompts-tab.ts:163`) | `characterKeys.subprompts` added in `characters.api.ts` |
| `SubpromptPicker` | NEW `apps/web/src/app/subprompts/subprompt-picker.ts` (`qt-subprompt-picker`) | `size` input; `selectedIds` input + `selectionChange` output (the full next set) |
| `SubpromptEditorModal` | NEW `apps/web/src/app/subprompts/subprompt-editor-modal.ts` (`qt-subprompt-editor-modal`) | keyed remount = `@if (open()) { <qt-subprompt-editor-form [key]…> }` — a fresh component instance per opening; never a reset effect |
| `SubpromptsSection` | NEW `apps/web/src/app/subprompts/subprompts-section.ts` (`qt-subprompts-section`), hosted at `system-prompts-tab.ts` after the prompt list | the inline confirm popover |
| `CharacterPickerPanel` / `NewChatForm` / `useNewChat` | `screens/new-chat/character-picker-panel.ts`, `new-chat-form.ts`, `new-chat.logic.ts`, `new-chat.types.ts`, `new-chat.state.ts` | the omit-when-empty rule |
| `ParticipantCard` + the salon hooks | `chat/sidebar/participant-card.ts`, `screens/salon/salon-conversation.ts`, `chat/chat-cast.api.ts`, the enriched participant type in `core-contract.ts` | `size="sm"` |
| `field-hints.ts` | `ui/prompt-field-hints.ts` + spec | thirteen keys |
| `keys.ts` + `topic-map.ts` | `characters.api.ts` + `core/realtime-topic-map.ts` + spec | order `detail, prompts, subprompts, photos` |
| `m6-screen-parity.md` | add rows for the three new components + the four host sites (`SubpromptsSection` under the System Prompts tab row; the two New-Chat pickers; the participant-card picker) | |

## Tiered deliverables

### Tier 1 — must land

1. The wire file + `characterKeys.subprompts` + the §C DTOs in
   `core-contract.ts` (yours to edit this round — §R.6): `SubpromptRecord`,
   the five request/response types, `selectedSubpromptIds` on
   `ChatUpdateParticipantRequest`, on the create-body participant type
   (`core-contract.ts:779` region) and on the enriched participant type;
   `subprompts.api.ts` with the five ops through `dispatchData` and
   `subpromptErrorMessage` (v5's `coreErrorMessage` idiom — measure which
   field the core error carries and record the mapping).
2. The three components with v4's copy, classes, ARIA and states byte-exact
   (incl. the missing-id line-through rows, the `enabled` gating, the
   keyed remount, the `Saving...` label, the two toasts per action).
3. The Aurora section on the System Prompts tab (position: after the prompt
   list, before the modals).
4. Both New-Chat pickers + the `buildCreateRequest` rule + the state seeds.
5. The Salon participant-card picker + `handleSubpromptsChange` + the
   `Subprompts updated` toast + the enriched-participant projection read.
6. The realtime arm + `characterKeys.subprompts`; the thirteenth hint.
7. Parity specs: the three `buildCreateRequest` cases; the topic-map line;
   the hints table regenerated at the pin (thirteen keys, order asserted);
   the picker (summary text in all three states, toggle no-op, missing-id
   row shown only when loaded, created-record auto-tick), the modal
   (fresh form per opening, disabled rule, both toasts, error fallback
   sentences), the section (empty/loading/list, confirm toggle, delete
   toast), the participant card (renders ONLY for an LLM-controlled
   character seat), the tab-refetch coverage; every spec over a mocked
   `CoreClient` (`core-client.testing.ts`).
8. The gated e2e walk `character-subprompts-flow.spec.ts` —
   `P4D163_SERVER_LANDED = false`, ACTIVATE-AT-UNIFY, written COMPLETE (the
   "X would walk today" rule): (a) Aurora → a fixture character → System
   Prompts tab → `+ Add Subprompt` → create `Be terse` → the card shows
   `Subprompts/be-terse.md` → edit the title → the path is UNCHANGED →
   delete via the inline confirm → `Subprompt deleted`; (b) New Chat with
   that character → the picker summary reads `None on file` → `New
   subprompt…` in the picker → created record auto-ticked (`1 of 1 in
   play`) → create the chat → `chatGet` shows the seat's
   `selectedSubpromptIds` with the new id; (c) the Salon sidebar's
   participant card → untick → `Subprompts updated` toast → `chatGet`
   shows `[]`; (d) a user-controlled seat shows NO picker. Seed through the
   server's verbs; use a fixture character nobody asserts prompts on (the
   `e2e-playwright-traps` shared-fixture coupling note). The rendered-prompt
   assertion belongs to the Rust differentials, not here.

### Tier 2 — should land

9. The realtime refetch: a spec proving the list query re-fires on a
   `characters/<id>` hint through `realtime.service.ts` (the P4.D125
   `realtime_hint_wire` idiom, client-side).
10. `m6-screen-parity.md` rows.

### Tier 3 — explicit deferrals (recorded, never silent)

- `encodeURIComponent(subpromptId)` — no REST URL is built by v5's client
  (dispatch verbs); recorded as a NO-COUNTERPART with the verb named.
- The Lexical editor's `namespace` prop — no ProseMirror twin; recorded.

## The differential requirement (restated)

This lane's oracle is v4's client at the pin: the hints table EXECUTED at
`2f4254b42` and diffed (the P4.D103 mechanism in `prompt-field-hints.spec.ts`
— extend its comparand, do not hand-copy); the three jest suites transcribed
1:1 as parity specs with v4's file:line cited in each `it`; every copy string
pasted from `git show 2f4254b42:<file>` and diffed by eye against the spec's
literal. The e2e beats are the wire proof at unification.

## Fixtures

- Consumes the e2e fixture's characters and the running server's verbs for
  seeding; delivers no Rust fixture. Never touches `harness/**`.

## §C — the subprompts wire contract (BINDING — verbatim-identical in P4.D163, P4.D164 and P4.D165)

Every name below is the wire. Server lanes implement it; the SPA lane codes
against it (mocked until unification); the unifier runs the name-for-name
diff across the three orders' implementations.

### C.1 The record

`SubpromptRecord` — v4 `Subprompt` (`lib/subprompts/subprompts.ts:52-63`),
keys in THIS order on the wire:

```
{ id: string, path: string, title: string, content: string, updatedAt: string }
```

`id` = the vault file name sans `.md` (one path segment; stable across title
edits); `path` = `Subprompts/<id>.md`; `title` = frontmatter `title`, falling
back to the id; `content` = the body with frontmatter stripped, leading
newlines removed, `trimEnd`; `updatedAt` = the file's `lastModified` as an ISO
8601 string (`new Date(mtime).toISOString()`).

### C.2 The verbs (dispatch `type` literals; the v5 idiom of the
`characterPrompt*` family — every field camelCase on the wire, snake_case in
`api/types.rs`)

| verb | request | success body | v4 route |
|---|---|---|---|
| `characterSubpromptList` | `{ characterId }` | `{ subprompts: SubpromptRecord[] }` (title-sorted; `[]` for no vault / no folder) | `GET /api/v1/characters/[id]/subprompts` |
| `characterSubpromptGet` | `{ characterId, subpromptId }` | `{ subprompt: SubpromptRecord }` | `GET …/subprompts/[subpromptId]` |
| `characterSubpromptCreate` | `{ characterId, title, content }` | `{ subprompt }` (REST status **201**) | `POST …/subprompts` |
| `characterSubpromptUpdate` | `{ characterId, subpromptId, title?, content? }` | `{ subprompt }` | `PUT …/subprompts/[subpromptId]` |
| `characterSubpromptDelete` | `{ characterId, subpromptId }` | `{ success: true }` | `DELETE …/subprompts/[subpromptId]` |

Update and delete each run the fan-out (C.5) AFTER the write and BEFORE
answering; the fan-out's `{chatsTouched, seatsRecompiled}` is folded into the
info log line only, never into the body.

### C.3 The participant field

`selectedSubpromptIds?: string[]` on the participant record — v4
`ChatParticipantBaseSchema` / `ChatParticipantSchema` (`lib/schemas/
chat.types.ts:606,649`): `z.array(z.string().min(1).max(120)).optional()`,
placed in schema order **directly after `selectedSystemPromptId`, before
`displayOrder`**. Absent or empty → none.

- `chatCreate.participants[].selectedSubpromptIds?: string[]` — v4
  `createParticipantSchema` (`app/api/v1/chats/route.ts:97`):
  `z.array(z.string().min(1).max(120)).max(100).optional()`; the stored value
  is `isUserControlled ? [] : (data.selectedSubpromptIds ?? [])` — a
  CREATED participant ALWAYS carries the key (an array, possibly empty).
  The client OMITS the key when nothing is ticked or the seat is
  user-controlled, so a plain create stays byte-identical.
- `chatUpdateParticipant.selectedSubpromptIds?: string[]` — v4
  `updateParticipantSchema` (`app/api/v1/chats/[id]/schemas.ts:57`), same
  bounds; PRESENT replaces the whole set; ABSENT leaves it untouched (a
  plain `Option<Vec<String>>` — this field is NOT nullish; an explicit
  `null` is a Zod `invalid_type` 400, measured on v4 before pinning).
  The seat recompiles when the set changes by an ORDER-INSENSITIVE compare
  (`sameIdSet`), independently of the `selectedSystemPromptId` trigger.
- The chat-GET enrichment (`EnrichedParticipantDetail`) projects
  `selectedSubpromptIds: string[]` — ALWAYS present, `participant.
  selectedSubpromptIds ?? []`, placed directly after `selectedSystemPromptId`.
- `.qtap` export/import carries the key inside the participants blob (no
  writer change; `public/schemas/qtap-export.schema.json` documents it).

### C.4 Error envelopes (v4's `lib/api/responses.ts` sentences, byte-exact)

| condition | status | body `error` |
|---|---|---|
| character missing (`findByIdRaw`, overlay-free — an ARCHIVED character still resolves for reads) | 404 | `Character not found` |
| subprompt missing (get / update / delete-returned-false) | 404 | `Subprompt not found` |
| `subpromptId` fails `isValidSubpromptId` (get / delete; update after the body parse) | 400 | `Invalid subprompt id` |
| create/update body fails the Zod schema (`title` min 1 max 100 [code points]; `content` min 1; update: both optional) | 400 | v4's `validationError` envelope (`error: 'Validation error'` + `details`), as the P4.62 web edges emit it |
| the service's own validation (`validateTitle` / `validateContent` — UTF-16 `.length` rules, AFTER Zod) | 400 | `A subprompt needs a title` / `A subprompt title is at most 100 characters` / `A subprompt needs some instruction text` |
| character archived, on a WRITE | 409 | `Character is archived; subprompts cannot be added` / `… cannot be edited` / `… cannot be deleted` |
| any other failure | 500 | `Failed to list subprompts` / `Failed to read subprompt` / `Failed to create subprompt` / `Failed to update subprompt` / `Failed to delete subprompt` |

Guard order, as v4 wrote it and to be MEASURED before pinning (the
`a6870c5a` guard-order class): POST and PUT parse the body FIRST (a bad body
on a missing character is 400, not 404); GET and DELETE validate the id first,
then the character, then the subprompt.

### C.5 Fan-out and realtime

- Every successful write publishes `characters/<characterId>` on the
  realtime bus. The SPA topic map's `characters` arm gains
  `characterKeys.subprompts(id)` beside `detail`/`prompts`/`photos`.
- `fanOutSubpromptChange(characterId, subpromptId, {removeSelection})` over
  `chats.findByCharacterId`: for every seat of that character that is
  `controlledBy !== 'user'`, `status !== 'removed'`, and whose selection
  contains the id CASE-INSENSITIVELY — with `removeSelection`, strip the id
  from the persisted selection first (case-insensitive), then
  `compileIdentityStackForParticipant`; per-seat failures warn and fall
  through; `chats/<chatId>` published once per touched chat.

### C.6 The rendered block (P4.D164 owns the bytes; recorded here so the
SPA lane's copy and help text agree with them)

```
\n## Additional Instructions\nThe following also apply to you in this conversation.\n<### {title}\n{processTemplate(content)}> joined by \n\n
```

pushed directly after the base system prompt in `buildIdentityStack` (before
the author-carried blocks); the greeting appends the same text with `\n\n`;
a seat with nothing selected renders NOTHING (byte-identical to before the
feature — `IDENTITY_STACK_BUILDER_VERSION` stays 2, `PROMPT_CACHE_STRUCTURE_
VERSION` stays 4). The green room's user-content note is the SAME per-item
shape but with `content.trim()` and NO template processing.

### C.7 What every order must NOT invent

No verb, key, sentence, status, or heading outside this section and the v4
hunks at `2f4254b42`. A gap is recorded per the order's Tier 3 rules, never
patched with a v5-only shape. The SPA gate constant is `P4D163_SERVER_LANDED`
(§R.9).

## §R — the round-wide meeting points (BINDING — identical in every order and addendum of this round)

Round: **the `2f4254b42` character-subprompts round** — P4.D163 → P4.D164
(stacked, one server lane) ∥ P4.D165 (the SPA lane) ∥ P4.9K1-resumed ∥
P4.9K2-resumed. Ordered 2026-09-07.

1. **Baseline and drift.** Oracle baseline **`f699da6f6`** (drift-ledger §1).
   v4 `main` HEAD **`2f4254b42`** ("feat: character subprompts — per-chat
   optional instructions from the vault", `4.10.0-dev.1`) — ONE commit past
   the baseline, the ledger's one UNPROCESSED row, now
   `ORDERED(P4.D163 ∥ P4.D164 ∥ P4.D165)`. v4 `bugfix` tip **`1a2b2164c`**,
   unmoved, nothing unabsorbed. The `15573c3a1` (bug 119) row stays
   `ORDERED(P4.9K1)` — its runner half rides P4.9K1's resumption.
2. **The freshness probe at lane start** (ledger §2), expected values:
   checkout on **`main`**, tree CLEAN, `git log --oneline 2f4254b42..main`
   EMPTY, `git log --oneline 1a2b2164c..bugfix` EMPTY. **Any mismatch: STOP
   and report** — a lane never writes the ledger. Re-run the probe before
   every regen batch; the checkout has gone dirty MID-LANE before (ledger
   §5.1).
3. **Regen rule: PIN REQUIRED, TWO pin shas, lane-unique paths, all three
   symlink classes** (ledger §5.1), every regen and fixture build through
   `harness/tools/recipe_sweep.py --v4 <pin> --run <family>`; never two
   sweeps concurrently.
   - **`2f4254b42`** for every family the subprompts feature MOVES and for
     the four help families — the round's catch-up target (the P4.D119
     precedent: a drift lane pins at the drift sha it absorbs). Paths:
     `/tmp/qt-v4-pin-p4d163-2f4254b42` (serves the whole stacked lane) and
     `/tmp/qt-v4-pin-p4d165-2f4254b42`.
   - **`f699da6f6`** for P4.9K1 / P4.9K2 and for any family the feature does
     NOT move (neutrality regens). Paths: `/tmp/qt-v4-pin-p49k1-f699da6f6`,
     `/tmp/qt-v4-pin-p49k2-f699da6f6`. Verified at planning by PATH: `git
     show --stat 2f4254b42` touches no `character-optimizer`,
     `external-prompt-generator`, `character-rename`, `character-wizard`,
     `ai-character-import` or `characters/[id]/handlers` file.
   - **Verify each pin by a marker**: `Additional Instructions` MUST appear
     in a `2f4254b42`-pinned `system-prompt` oracle and MUST NOT appear in
     an `f699da6f6`-pinned one; `help/character-subprompts.md` exists only
     in the `2f4254b42` worktree. Check the NDJSON is non-empty before
     believing any diff (the empty-file trap).
   - Committed recipe headers keep saying `cd ~/source/quilltap-server` and
     never name a `/tmp` pin (`recipe-header-conventions`). The oracle
     `node_modules` are the LIVE tree (`zod` 4.5.4 — `zod_version_guard`
     fails the gate if it moves; every Zod length rule this round transcribes
     uses the `jsstr::zod_len_*` code-point helpers).
4. **Stacking and resumption.** P4.D164 branches its worktree FROM P4.D163's
   lane-branch tip (never from `main`), runs in the SAME worktree by the same
   agent after P4.D163's gate is green, and the unifier cherry-picks P4.D163
   first. P4.9K1 and P4.9K2 resume from **`main`** (their unit-1 commits are
   on main; the old lane branches `claude/p4-9k1-generators-detail-a1` /
   `claude/p4-9k2-generators-wizard-import-b1` are preserved history, NOT
   bases): `git worktree add <path> -b claude/p4-9k1-resume-<hash> main` /
   `… -b claude/p4-9k2-resume-<hash> main`. P4.D165 branches from `main`.
5. **Fenced-append shared files.** `crates/quilltap-core/src/api/types.rs`,
   `crates/quilltap-core/src/api/engine.rs`, `crates/quilltap-core/src/lib.rs`
   (module lines), `crates/quilltap-host/src/host.rs`,
   `crates/quilltap-host/src/spine.rs`, `crates/quilltap-web/src/lib.rs`
   (module + route lines): each Rust lane appends ONLY inside its own fence
   (`// === P4.D163 ===` … `// === end P4.D163 ===`; the K lanes REUSE the
   `// === P4.9K1 ===` / `// === P4.9K2 ===` fences their round-1 units
   opened where they exist, and open them once where they do not), new
   variants/arms/registrations at the END of the relevant block, never
   interleaved. `crates/quilltap-web/src/characters_routes.rs` is fenced BY
   FUNCTION exactly as in round 1 — `characters_action_post` is P4.9K1's,
   `characters_collection_post` is P4.9K2's — and **P4.D163 never touches
   that file**: its REST edges live in a NEW `crates/quilltap-web/src/
   subprompts_routes.rs`. `crates/quilltap-web/src/system_data_routes.rs`
   stays P4.9K2's. `crates/quilltap-web/src/chats_routes.rs` is P4.D163's
   this round (the `update-participant` carry, if the edge needs a hand
   edit at all — measure; the P4.67 reader decodes the `Request` enum, so
   a serde-carried key may need nothing). `Cargo.toml`/`Cargo.lock` at the
   workspace root: version lines only; a dependency add is a STOP-and-flag
   in the lane record.
6. **`apps/web/**` is P4.D165's alone** (the ONLY SPA lane this round —
   it owns `core-contract.ts`, `characters.api.ts`, the New-Chat, Salon
   sidebar and characters-edit screens outright). P4.D163, P4.D164, P4.9K1
   and P4.9K2 finish with `git diff main -- apps/web/` EMPTY. **The K lanes'
   SPA follow-ups are UNIFIER wires, not lane work:** flipping the six
   `P49K1_SERVER_LANDED` / `P49K2_SERVER_LANDED` beats (their first-run
   recipes are in the spec headers), folding the six §B generator DTOs into
   `core-contract.ts` and retiring the `as unknown as CoreRequest` casts,
   and deduping the two SPA fold helpers onto K3's `mintProgressId` /
   `isGeneratorProgressEvent`. Each K lane's record names its verbs'
   FINAL wire shapes so the unifier can run the §B name-for-name diff.
7. **Frozen files.** `docs/developer/porting/drift-ledger.md` (lanes never
   write it); `help/**` is P4.D163's alone (the re-vendor at `2f4254b42`,
   unit 0 of that order) — no other lane touches it; `apps/web/src/app/
   shell/shell.ts` and `apps/web/src/app/workspace/workspace-contract.ts`
   (no new tab kinds — every subprompts surface is a section, a picker or a
   modal INSIDE an existing screen, as in v4).
8. **Version bumps.** P4.D163 + P4.D164 (one stacked lane): `quilltap-core`,
   `quilltap-harness`, `quilltap-web`, `quilltap-host` (host on every commit
   that moves `help/**` or any host file). P4.9K1 and P4.9K2:
   `quilltap-core`, `quilltap-harness`, `quilltap-web`, `quilltap-host`.
   P4.D165: `apps/web/package.json`. Per commit. The unifier RECOUNTS as base
   + total bumps — identical bumps merge silently (`commit-checklist`; the
   playbook's silent-auto-merge trap).
9. **e2e.** Port 4319 is repo-wide — ONE Playwright at a time across all
   lanes, and only P4.D165 runs it. P4.D165 adds NEW spec files only, with
   every server-dependent beat guarded ACTIVATE-AT-UNIFY on the NAMED
   constant **`P4D163_SERVER_LANDED = false`** (never a capability probe —
   a DEFINED-but-refusing verb defeats a probe; `round-plan-takeaways`
   §Planning). The unifier flips it after P4.D163/P4.D164 land and runs the
   beats as their first execution.
10. **Append-only for everyone:** `docs/CHANGELOG.md`,
    `docs/developer/porting/status-log.md`.
11. **Fixture ownership.** P4.D163 delivers the NEW committed
    `crates/quilltap-harness/fixtures/subprompts-{main,mount}.db` pair (+ its
    builder `harness/oracle/fixtures/build-subprompts-fixture.ts`, v4's REAL
    repositories at the `2f4254b42` pin, pinned ids); P4.D164 may EXTEND that
    pair ONCE, in the same lane, and every family over it is regenerated
    ONCE after both orders' edits are in (the P4.D119 rule). The `/tmp`
    identity-compiler fixture builder (`build-identity-compiler-fixture.ts`)
    is P4.D164's to widen; the `/tmp` chats-participants builder is
    P4.D163's. `characters-{main,mount}.db` is READ by P4.9K1 and by K4's
    live beats and REGENERATED BY NOBODY. P4.9K2 delivers
    `character-generators-{main,mount}.db`. P4.D165 seeds through the
    running server's verbs (`e2e/support/seed-help-fixture.ts` precedent),
    never SQL — a store-overlay file cannot be SQL-seeded without moving
    every hash beside it (`dogfood-findings.md` standing notes).
12. **The bug-119 obligation** (ledger §3, `15573c3a1`): P4.9K1 lands the
    runner's `run_sub_step` / `run_sub_step_core` containment and the two
    log lines, capture-pinned; the pure half is already on main.
13. **Census guards move when a verb is added.** Adding any dispatch verb
    with an `*_id` field, or closing a recorded divergence, moves the
    executable censuses in OTHER crates (`a-new-verb-moves-the-dispatch-
    wrong-type-census`, `closing-a-divergence-moves-the-censuses`) — every
    Rust lane's gate is `cargo test --workspace`, never `-p`.

## Ownership (BINDING — identical table in every order and addendum of this round)

| Lane | Owns (edit freely) | Fenced-append (§R.5) | Must not touch |
| --- | --- | --- | --- |
| **P4.D163** (stacked lane, first) | NEW `crates/quilltap-core/src/subprompts/**`; NEW `crates/quilltap-core/src/api/subprompts.rs`; NEW `crates/quilltap-web/src/subprompts_routes.rs`; `crates/quilltap-core/src/db/chats.rs` (the `ChatParticipant` field), `db/chats_participants.rs`, `db/chats_read.rs`; `services/chat_participants.rs`, `services/chat_enrichment.rs`, `services/chat_create.rs` (the schema arm + the `json!` carry + the candidate carry ONLY — P4.D164 takes the greeting call site); the `ChatUpdateParticipant` variant's new field in `api/types.rs` (an existing variant — the one in-place edit outside a fence); `crates/quilltap-web/src/chats_routes.rs`; `help/**` (the four re-vendored files); NEW harness families `subprompts_helpers_equivalence`, `subprompts_storage_tier2_equivalence`, `subprompts_routes_equivalence` + their oracle cases; NEW committed `subprompts-{main,mount}.db` + builder + corpus JSON; the widened `/tmp` chats-participants builder; the moved families' recipes (`chats_participants_tier2`, `participant_resolver_tier2`, `chat_create_capstone`, the chat-GET family, the four help families); this order's header | `api/types.rs`, `api/engine.rs`, `core/src/lib.rs`, `web/src/lib.rs` | `apps/web/**`; `characters_routes.rs`; `generators/**`; `system_prompt.rs`, `system_prompt_compiler.rs`, `build_context.rs`, `chat_initialize.rs`, `outfit_selections.rs` (P4.D164's); `characters-*.db`; `character-generators-*.db` |
| **P4.D164** (stacked on P4.D163) | `crates/quilltap-core/src/system_prompt.rs`, `services/system_prompt_compiler.rs`, `services/build_context.rs`, `services/chat_initialize.rs`, `services/outfit_selections.rs`, the greeting call site in `services/chat_create.rs` (`build_chat_context` call + the opener resolve — that ONE region); `harness/oracle/cases/{system-prompt,identity-compiler,initial-greeting,build-context-tier3,chats-outfits-tier2}.*` + the outfit-llm-choose tier-3 case + their fixtures JSON / `/tmp` builders; the ONE extension of `subprompts-{main,mount}.db` (§R.11); NEW family `subprompts_prompt_tier2_equivalence`; this order's header | `api/types.rs`, `api/engine.rs`, `core/src/lib.rs` (only if a new `pub mod` is needed) | `apps/web/**`; `subprompts/**` (consume, never edit — a needed change is a STOP-and-flag to be landed as a P4.D163-labelled commit in the same lane, recorded); `characters_routes.rs`; `generators/**`; `help/**` |
| **P4.D165** | `apps/web/**` whole — incl. `core-contract.ts`, `screens/characters/characters.api.ts`, `screens/characters/edit/system-prompts-tab.ts`, `screens/new-chat/**`, `chat/sidebar/participant-card.ts`, `chat/chat-cast.api.ts`, `screens/salon/salon-conversation.ts`, `core/realtime-topic-map.ts`, `ui/prompt-field-hints.ts`, NEW `apps/web/src/app/subprompts/**`, NEW e2e spec `character-subprompts-flow.spec.ts`, `apps/web/package.json`; `docs/developer/porting/m6-screen-parity.md` (its rows); this order's header | — | `crates/**`; `help/**`; `harness/**`; `shell.ts`; `workspace-contract.ts`; any EXISTING e2e spec |
| **P4.9K1-resumed** | as its round-1 Ownership row (`p4.9k1-generators-server-detail.md` §Ownership): `generators/{optimizer,external_prompt,rename,refresh_archive}.rs`, `api/generators_detail.rs`, `characters_routes.rs::characters_action_post`, its three families + cases + fixtures JSON, `host/generators_detail_driver.rs`; its order's header + addendum | `api/types.rs`, `api/engine.rs`, `host/host.rs`, `host/spine.rs`, `web/src/lib.rs` (the `// === P4.9K1 ===` fence) | `apps/web/**`; `subprompts/**`; `api/subprompts.rs`; `subprompts_routes.rs`; `chats_routes.rs`; `help/**`; `generators/{wizard,ai_import}.rs`; `characters_collection_post`; `system_data_routes.rs`; `character-generators-*.db`; `subprompts-*.db` |
| **P4.9K2-resumed** | as its round-1 Ownership row: `generators/{wizard,ai_import}.rs`, `api/generators_wizard.rs`, `characters_routes.rs::characters_collection_post`, `system_data_routes.rs`, `query_param_semantics_equivalence` + its case (the `ai-import-stream` row), NEW `character-generators-{main,mount}.db` + builder, its two families + cases + fixtures JSON, `host/generators_wizard_driver.rs`; its order's header + addendum | `api/types.rs`, `api/engine.rs`, `host/host.rs`, `host/spine.rs`, `web/src/lib.rs` (the `// === P4.9K2 ===` fence) | `apps/web/**`; `subprompts/**`; `api/subprompts.rs`; `subprompts_routes.rs`; `chats_routes.rs`; `help/**`; `generators/{optimizer,external_prompt,rename,refresh_archive}.rs`; `characters_action_post`; `characters-*.db`; `subprompts-*.db` |

Everyone: `docs/CHANGELOG.md` + `status-log.md` append-only; the drift
ledger frozen; `shell.ts` / `workspace-contract.ts` frozen. `Cargo.toml` /
`Cargo.lock`: version lines only (a dependency add is a STOP-and-flag). The
K lanes' SPA follow-ups are unifier wires (§R.6).

## Verification gate (before declaring done)

1. `npm run lint` (incl. the qt-class guard — every class you paste must
   exist in the sheet; a v4 `hover:` variant must be hand-written,
   `qt-hover-variant-must-be-hand-written`); `npm test` (`ng-run.mjs`,
   never a bare `ng`); `npm run build` (the only real type gate —
   `spa-tsc-does-not-typecheck-app-sources`).
2. The new spec file runs GREEN with its beats SKIPPED by the named gate
   (`--grep` proves nothing for a gated file — run the file); the full
   Playwright suite ONCE against a release build, coordinated on port 4319,
   to prove no existing beat moved (expect the standing count + your
   skips).
3. `git diff main -- crates/ help/ harness/` EMPTY; no existing e2e spec
   in the diff.
4. Commits per `.claude/commands/commit.md` (SPA bump per commit; CHANGELOG
   every commit); the lane record appended to `status-log.md` with the
   FINAL verb/field spellings for the unifier's §C name-for-name diff and
   the beats' first-run recipe (which fixture character, what the seeds
   are, the gate constant's line).
