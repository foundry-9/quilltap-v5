# Work order P4.52 — widen the memories fixture pair; retire the ruled vintage row — v4 `b8449b3e`

**Status:** OPEN (planned 2026-08-20).

## Preamble

- **v4 baseline for this round: `b8449b3e`** (v4 main, HEAD at planning
  2026-08-20, tree CLEAN and the checkout on `main`). This lane ports no
  v4 commit — it discharges the maintenance order the `c8a3cf77` round
  recorded: the committed `memories-{main,mount}.db` fixture pair
  predates the v4 4.8.2 `chat_settings` columns, so
  `housekeeping_config_set` has been a silent standing red pinned as a
  RULED VINTAGE ROW with a repair tripwire. The sibling P4.D96 lane is
  the round's drift catch-up; its two commits touch nothing in the
  memories families, so this lane regens safely at the same pin.
- **Drift-check at lane start.** `git log b8449b3e..main --oneline` AND
  `git diff main bugfix -- lib/ app/ packages/` in the v4 checkout. If
  v4 moved, **STOP** and report.
- **Verify the v4 checkout's branch before ANY oracle regen**; pin a
  detached worktree at `b8449b3e` (`recipe_sweep.py --v4 <pin-path>`;
  ALL THREE symlink classes). Note the pin includes v4's new
  `--no-sparkplug` jest launch (bug 83) — regens inherit it, which only
  REDUCES worker-SIGSEGV flakes.
- **Sibling lanes (disjoint ownership):** P4.D96 (the anti-chorus drift
  — owns `skip_signal.rs`, `build_context.rs`, `message_context.rs`,
  the SPA skip-signal twins, `harness/oracle/cases/skip-signal.ts`) and
  P4.53 (the sweep-driver follow-ups — owns
  `harness/tools/recipe_sweep.py` and the five brahma/carina case
  HEADER lines). This lane must not touch those files.
- **Worktree:** per the `carryout` skill; edit inside the worktree, not
  main.

## Mandate

Widen the committed `memories-{main,mount}.db` fixture pair to the
current v4 schema vintage so v4's whole-row `updateForUser` survives
against it, retire the RULED VINTAGE ROW in
`memories_routes_equivalence.rs` back to a plain both-sides equality
(the row's own tripwire fires the moment the fixture widens — that
firing is the signal, not a failure), and regenerate every oracle the
pair feeds, fresh at the pin, zero SKIP. Do NOT change any v5 source
behavior — this is a fixture + harness lane; if a regen surfaces a real
v5 divergence the widened fixture can now see, that is a FINDING to
record (and fix only if it is unambiguous and small; otherwise escalate
in the lane record). Do not touch the sweep driver or any sibling-owned
file.

## Survey-verified starting points (2026-08-20, fresh)

- **The fixture pair:**
  `crates/quilltap-web/tests/fixtures/memories-main.db` and
  `memories-mount.db`. Consumers found by grep (verify at lane start —
  enumerate before touching):
  - `crates/quilltap-harness/tests/memories_routes_equivalence.rs`
    (the family with the ruled row),
  - `harness/oracle/cases/memories-routes.test.ts` (recipe header at
    the top: copies the pair via `QT_FIXTURE_MEM_MAIN`/`_MOUNT`, /tmp
    mirror venue, jest `--roots` composition),
  - `harness/oracle/cases/memories-config.test.ts` (same pair),
  - any `quilltap-web` tests referencing the fixtures directory (the
    grep found none by filename — check for path-constant indirection
    before declaring the consumer set closed).
- **The ruled vintage row + tripwire:**
  `crates/quilltap-harness/tests/memories_routes_equivalence.rs:756–801`
  — the `housekeeping_config_set` branch asserts v4's fixture-artifact
  500 (`no such column: composerEmoji` inside `updateForUser`) AND v5's
  200 explicitly, and `:777–781` is the tripwire that errors loudly
  ("the fixture-vintage ruling has EXPIRED") once the oracle answers
  non-500. The file's header comment (`:17`) records the repair intent.
- **What's missing from the fixture:** at minimum the three 4.8.2
  `chat_settings` columns (the P4.D73 D23 re-dump; `composerEmoji` is
  the one v4's error names). **Measure, don't assume three**: diff the
  fixture's actual schema (`sqlite3 … '.schema'` via a decrypted copy —
  the pair is test-pepper-keyed) against v4's current `generateDDL`
  shape at the pin (the committed `fresh_schema.json` / the D23
  machinery) for EVERY table the pair carries, both partitions. Columns
  that arrived after the fixture was minted (4.8-era: `pascalMeta`,
  `customTools`, hair-slot-era rows, `multiCharacterPrefill`, the three
  archive columns, `doc_mount_file_links.linkGroupId`, …) may or may not
  be present — the fixture postdates some of these; the diff is the
  truth.
- **Widening method — the lane decides, with these constraints:** the
  case files bake dozens of row UUIDs (see the constants block in
  `memories-routes.test.ts:34+`), so the seeded DATA must survive
  byte-identically. Two sanctioned routes:
  1. **In-place `ALTER TABLE … ADD COLUMN`** with exactly v4's DDL
     defaults for each missing column (transcribed from `generateDDL`
     output, never hand-invented — the D23 rule's spirit applies to
     fixtures too), or
  2. **Replay v4's real migration chain** over the pair (the P4.D41
     migration-vintage fixture precedent — `restore-archive` round
     record shows the shape).
  Route 1 is cheaper and adequate if the schema diff is columns-only;
  route 2 is required if the diff shows index/table-shape changes.
  Either way, note that v5's boot "ensure" passes must ALSO accept the
  result (run the memories families' v5 side against the widened pair
  before regenerating oracles — a v5-side refusal is a finding).
- **Committed-fixture rules:** the sweep driver never runs recording
  stages and warns on tracked-fixture writes
  (`sweep-recipes-write-into-the-repo` memory) — stage the widening as
  a deliberate, reviewed commit of the two `.db` files (plus any
  `.db.meta.json` sidecars if the pair has them — the help-drift round's
  driver fix exists because sidecars got dropped once).

## Tiered deliverables

**Tier 1 (must land):**

1. The schema diff (fixture vs v4 `generateDDL` at the pin), recorded in
   the lane record table-by-table — the measured column list, not a
   guess.
2. The widened pair committed, seeded rows byte-preserved (prove it:
   dump the pre/post row contents of every seeded table and diff —
   the only delta must be the new columns' defaults).
3. The RULED VINTAGE ROW retired: `housekeeping_config_set` back to a
   plain both-sides equality; the tripwire and the ruling comment
   removed; the family header's repair note updated.
4. Fresh oracle regen for every consumer family at the pin through the
   sweep driver (run from THIS lane's worktree with the unchanged
   driver), zero SKIP, fresh-marker-verified NDJSONs; the whole
   `memories_routes` family green including the formerly ruled row.

**Tier 2 (should land):**

5. A one-line vintage stamp in the fixture's provenance comment (the
   case-file header or a sibling `README`/comment if one exists) naming
   the v4 pin the schema now matches, so the NEXT vintage gap is datable.
6. Re-run the OTHER families that the `c8a3cf77` round record grouped as
   "the memories/memory families" IF the lane's consumer enumeration
   (item 1 of the survey) shows they actually read the pair — the grep
   at planning found only the three files above, but the round record's
   wording implies more; resolve the contradiction by measurement and
   record which reading was right.

**Tier 3 (explicit loud deferrals):**

7. Widening any OTHER committed fixture pair that may carry the same
   vintage gap (salon, brahma, embedding-profiles, almanack, …) — OUT OF
   SCOPE; if the schema diff tooling built here generalizes, note it as
   a candidate maintenance sweep in the lane record instead.

## The differential requirement, restated

This lane's "port" is the fixture itself, so the differential IS the
regen: every consumer family regenerated fresh at the `b8449b3e` pin and
green by name, with the formerly ruled row now a plain equality proven
in BOTH directions (v4 200 + body, v5 200 + body, byte-compared).
Mutation-prove the retirement: temporarily re-narrow one added column
(drop it from a scratch copy) and confirm the family goes red the same
way the ruling documented — then restore. The closest recipe precedents:
the family's own header in `memories-routes.test.ts`, and the
`jest-real-db-oracle` + `oracle-regen-pinned-v4-worktree` memory notes.

## Fixtures

This lane DELIVERS the widened `memories-{main,mount}.db` pair (and owns
regenerating every dependent oracle). It consumes v4's `generateDDL`
output at the pin as the schema truth. No other committed fixture moves.

## Shared contract

**The three lanes of this round meet nowhere.** No shared verbs, no
gated beats, no ACTIVATE-AT-UNIFY constants. Procedural interactions
only: all three regen at the same `b8449b3e` pin; P4.53's driver edits
land at unification (this lane runs the UNCHANGED driver from its own
worktree). Binding on all three orders.

## Ownership

- **This lane owns:**
  `crates/quilltap-web/tests/fixtures/memories-{main,mount}.db` (+ any
  sidecars), `crates/quilltap-harness/tests/memories_routes_equivalence.rs`,
  and — ONLY if a consumer-set finding requires it — comment-level
  provenance edits in `harness/oracle/cases/memories-routes.test.ts` /
  `memories-config.test.ts` (no behavioral case changes).
- **Must NOT touch:** `harness/tools/recipe_sweep.py` and the five
  brahma/carina case headers (P4.53); `skip_signal.rs`,
  `build_context.rs`, `message_context.rs`, the SPA skip-signal files,
  `harness/oracle/cases/skip-signal.ts` (P4.D96); any v5 production
  source (a needed source fix = a recorded finding first, per the
  Mandate).
- **Version bumps:** this lane bumps `quilltap-harness`, and
  `quilltap-web` only if a web-side test/fixture reference moves. All
  three lanes bump harness — the unifier ACCUMULATES.
- `CHANGELOG` and `status-log.md` are append-only (union-merge).

## Verification gate (before declaring done)

- `cargo fmt --all --check`; `cargo clippy --workspace --all-targets`
  plain AND with `--features quilltap-core/native-transport`.
- `cargo test --workspace` full (background + sentinel; never a pgrep
  poll loop; never `| tail`).
- The consumer families by name through the sweep driver at the pin,
  zero SKIP, fresh markers verified; the mutation proof recorded.
- SPA untouched → no ng run required unless web tests moved.
- Per-commit rule: `.claude/commands/commit.md`.
