# Work order P4.47 — maintenance smalls: the settings Zod-collapse arms, the google-wire header asserts, and the sweep driver's staging-dependency class — v4 `24633026`

**Status:** CLOSED — unified on main (2026-08-14, the help-drift round; lane
`claude/p4-47-maintenance-smalls-9f83c8`, records in `status-log.md`). All
three units landed, two of them bigger than banked: **(A)** the three sibling
settings arms answer v4's real `ZodError.message` 400 bodies (`settings_zod`
family, 27 cases after the lane's own correction — the "falsy non-member
slips the manual guard" arm — with the zod key-order-per-issue-code table and
the cheap-LLM parse deferred to the END of the assignment walk, both measured
from v4); still open, named at the deferral site: the repo-level validate over
`imageDescriptionProfileId`/`uncensoredImageDescriptionProfileId`/
`contextCompressionSettings`/`thinkingDisplay` past their manual guards.
**(B)** became a REAL v5 FIX, not just an assert: v5 sent google's api key as
a `?key=` query param where v4's genai SDK sends `X-Goog-Api-Key` (three
confirmations: SDK source, all 18 corpus rows, v5's own image path already
using the header); `manifests/google.json` now `kind: "header"`, both
composition-level unit tests flipped (the pair-of-tests lesson), the auth
transport pinned in BOTH directions beside the subset loop, coverage floor
`header_rows == count`, and the corpus provably current (SDK/Node markers
match installed versions — not regenerated). **(C)** the staging-dependency
class landed with direction as a property of the COMMAND (builder/case split
structural, `unknown` warns-not-fails); the P4.45 candidate list resolved 5
true / 3 false (its "last four" guess missed `reset_builtins`); four recipes
repaired with family-suffixed /tmp paths, proof artifact committed;
`help_doc_sync_guards` was flagged and HANDED OFF per Shared contract rule 3
— repaired at unification (the wires commit).

## Preamble

- **v4 baseline for this round: `24633026`** (v4 main HEAD at planning,
  2026-08-14). Drift-check at lane start per the two-branch rule
  (`git log 24633026..main`; `git diff main bugfix -- lib/ app/
  packages/` — measure with `diff`, never the bugfix commit list). STOP
  and report if HEAD moved or the tree is dirty; verify the checkout's
  branch before any regen, else pin a detached worktree
  (`recipe_sweep.py --v4 24633026`).
- Sibling lanes: **P4.D77** (the help-embeddings drift, server),
  **P4.D65-remainder** (archive coverage arms), **P4.9L** (composer
  toolbar, SPA). Ownership disjoint — see Shared contract.
- Work in your own worktree on a lane branch; repo-root paths exist in
  BOTH trees (`edit-inside-the-worktree-not-main`).

## Mandate

Clear three banked smalls, each small alone but each a standing debt
with its record already written: (A) the P4.D73-banked sibling settings
arms that still collapse Zod throws to fixed sentences, (B) the
P4.D76-banked google-wire recorded-not-asserted headers, and (C) the
P4.45-unit-4-banked `recipe_sweep.py` staging-dependency class. The lane
must NOT touch: any help/embedding family or its recipes (P4.D77 owns
them — see Shared contract rule 3), the archive/participant families
(P4.D65-remainder), or `apps/web/**` (P4.9L).

## Survey-verified starting points (2026-08-14)

- **(A) The D73 bank** (verbatim from
  `p4.d73-composer-settings-columns-server.md`'s status header): "the
  sibling `answerConfirmationSettings`/cheap-LLM/dangerous-content arms
  still collapse Zod throws to fixed sentences (no corpus case exercises
  them; `ZodInvalidTypeIssue` + `zod_error_message` now sit ready in the
  same file)." Locate the file by grepping those two identifiers; the
  D73 lane landed them beside the three composer-column arms whose PUT
  legs now answer v4's whole `ZodError.message` 400 bodies. The work:
  wire the three sibling arms through the same machinery, and add the
  corpus cases that exercise a present-but-invalid value on each (the
  `settings_routes_equivalence` family + its stale-oracle count guard —
  update the guard when the count grows). ⚠ The D57/D73 lesson stands:
  present-but-invalid must 400 at the handler, never collapse at the web
  edge (`Option<Option<T>>` where applicable); and the Taboo §3 lesson —
  don't let a hand-built web-edge variant make the differential blind.
- **(B) The D76 bank**: the P4.D76 SDK wire re-check recorded the
  google-wire corpus's request HEADERS without asserting them (the
  4.8.2 round's deferred-loud list names it: "the google-wire
  recorded-not-asserted headers"). Read the P4.D76 order + lane record
  (`status-log.md`) for the exact gap, then make the recorded headers
  comparands in the google-wire family the way the P4.44 provider
  request-header pin did it (post-`apply_auth` subset + a coverage
  floor; memory note `provider-request-header-pin`). If regen is
  needed, remember the D76 trap: corpora SELF-DATE — a green
  request-builder diff does not prove a refreshed corpus
  (`p4.d76-provider-sdk-wire-recheck-lane`).
- **(C) The P4.45 unit-4 bank** (`status-log.md`, "Lane record — P4.45
  unit 4"): a warning class for recipes that READ another family's
  staged /tmp artifacts. The recorded blocker is honest direction
  ambiguity — `QT_FIXTURE_X=/tmp/x.db $N/node build-x-fixture.ts`
  WRITES the path and `QT_FIXTURE_X=/tmp/x.db $N/npx jest …` READS it,
  and a half-wrong class trains authors to ignore it. The banked
  candidate list: `builtin_mounts`, `builtin_templates`,
  `embedding_profiles_routes`, `ensure_character_metadata_file`,
  `help_doc_sync_guards`, `metadata_vault_roundtrip`, `reset_builtins`,
  `seed_avatars` — "the last four look like true 'leans on another
  family's staging' cases by inspection." The work: solve the
  attribution honestly (classify per line: a path is self-staged iff an
  earlier line of the SAME recipe writes it — via the existing
  indentation-classified run lines; the P4.45 driver contract,
  `recipe-sweep-driver` + the driver's `--self-test`), extend
  `--self-test` with both directions, run the classifier over all
  families, and repair the true positives' recipes so each family
  stages its own inputs (the P4.45 "given its own staged mirror"
  precedent). **Exception: `help_doc_sync_guards` — record, don't
  repair** (Shared contract rule 3; P4.D77 owns help recipes this
  round).

## Deliverables

### Tier 1 — must land

1. (A) The three sibling settings arms answer v4's exact `ZodError.message`
   400 bodies on present-but-invalid input; `settings_routes_equivalence`
   grows one case per arm (fresh oracle at the pin), count guard
   updated; mutation-proven (revert one arm → its case reddens).
2. (C) The `external_tmp_input`-successor class lands in
   `recipe_sweep.py` with honest read/write attribution + `--self-test`
   arms; the full-sweep classification run is recorded in the lane
   record (per-family verdicts); true positives OUTSIDE the help
   families get self-staging recipe repairs, re-proven runnable.

### Tier 2 — should land

3. (B) The google-wire header asserts, with the corpus provably
   refreshed if touched (fresh marker in the NDJSON/corpus, not a green
   diff).

### Tier 3 — explicit loud deferrals

- If (B)'s survey shows the gap needs a corpus REGENERATION against the
  live SDK matrix beyond this lane's budget, record the measured shape
  and defer loud in the lane record — do not assert stale bytes.
- If (C)'s attribution cannot be made honest for some family, the class
  must WARN-not-fail for that family with the ambiguity named — the
  P4.45 rule about half-wrong warnings stands.

## The differential requirement, restated

(A) and (B) are corpus/differential work by nature — every new arm runs
against an oracle regenerated fresh at `24633026` through the sweep
driver, zero SKIP, fresh-marker-checked. (C) changes the driver itself:
its proof is `--self-test` plus a full `--run-all`-class verification
that no previously-green family regressed (the P4.45 committed-artifact
precedent). Never run two sweeps concurrently.

## Fixtures

- Consumes: the settings-routes staging, the google-wire corpus, every
  family's committed recipes (read-only except the true-positive
  repairs).
- Delivers: repaired recipes for (C)'s true positives; any settings
  corpus additions. No committed DB fixture changes expected; if one
  becomes necessary, extend by mutation, never rebuild.

## Shared contract (BINDING — identical in spirit across P4.D77, P4.D65-remainder, P4.47, P4.9L)

1. v4 baseline `24633026`; every lane drift-checks at start and STOPS on
   movement/dirt; regen only from a verified-clean `main` checkout or a
   pinned worktree.
2. Ownership is disjoint: **P4.D77** owns `quilltap-core` help/embedding
   modules + `db/mod.rs` + the schema dumps + the boot-ensure site + all
   `help_*`/`embedding_*`/`cold_chunk_*` harness families and their
   recipes/fixtures. **P4.D65-remainder** owns the archive/participant
   families + `character-archive-*`/`system-data-*` fixture mutations.
   **P4.47** owns `harness/tools/recipe_sweep.py`, the google-wire
   corpus asserts, and the settings Zod-collapse file + its
   `settings_routes_equivalence` arms. **P4.9L** owns `apps/web/**` +
   its e2e specs. No two lanes edit the same file. `CHANGELOG` and
   `status-log.md` are append-only for everyone.
3. If P4.47's driver extension flags `help_doc_sync_guards` (it is on
   the candidate list), it records the finding and hands the recipe
   repair to P4.D77/the unifier — it does not edit help recipes.
4. Version bumps: P4.D77 → core/host/harness; P4.D65-rem → harness;
   P4.47 → harness (+web only if the Zod fix lands there); P4.9L → the
   SPA package. Several lanes bump `quilltap-harness` in their own
   trees; the unifier accumulates the UNION and must not silently lose
   one (the 4.8.2 round's three silent version-bump losses).
   `quilltap-sqlite3mc-sys` is never bumped.
5. No lane touches CLAUDE.md's status section; the unifier moves the
   baseline to `24633026` at unification.

## Verification gate (before declaring done)

- `cargo fmt --check`; clippy BOTH feature sets (if any crate source
  changed); full workspace tests with this lane's env vars; the touched
  families by name, zero SKIP, fresh oracles at the pin.
- The driver's `--self-test` green; the classification run recorded.
- Spelling guard. Per-commit rule: `.claude/commands/commit.md`.
