# Work order P4.D127 — the provider/cheap-LLM drift: bug 104's Z.AI vision-list drop (`964ffb959`), the compression budget + cheap-task failure warn (`8872d7efc`), and the coalesce-trace drop (`21f573039`)

**Status:** OPEN — not started (ordered 2026-08-26).

## Preamble

- **v4 baseline for this round: `f3892158d`** (v4 main). The round absorbs
  the FOURTEEN commits past it (drift-ledger §3); this lane owns three
  PORT rows: `964ffb959` (bug 104 — Z.AI's private vision list deleted),
  `8872d7efc` (per-task cheap-LLM budgets + the failure warn),
  `21f573039` (the realtime coalesce trace dropped). At planning
  (2026-08-26) the v4 checkout was on `main`, tree CLEAN, HEAD
  `8872d7efc`.
- **Run the drift ledger's §2 freshness probe at lane start**
  (`docs/developer/porting/drift-ledger.md`). If it fails — **STOP and
  report**; a lane never writes the ledger.
- **Regen rule in force: pin required** (ledger §1). Build ONE lane-unique
  detached worktree (all THREE symlink classes — recipe in drift-ledger
  §5.1; the plugins/dist symlink class is LOAD-BEARING here, the Z.AI
  plugin regen dies without it; suggested path
  `/tmp/qt-v4-pin-p4d127-8872d7efc`) pinned at **`8872d7efc`**. ⚠ The pin
  tree also contains `dcab791c2`'s claimed-neutral provider-wire dedup
  (`@quilltap/plugin-utils` 2.5.0 sharing one `buildRequestBody`,
  nanogpt 1.1.1, ollama 1.0.46) — so this lane's corpus regen doubles as
  part of that sweep's neutrality evidence: **assert every pre-existing
  row NOT moved by `964ffb959` is byte-identical**, and escalate any
  other byte to P4.D129 rather than absorbing it silently. Spec reads per
  commit via `git show <sha> -- <path>` (ledger §5.3). The committed
  request-envelopes corpus is regenerated DELIBERATELY via
  `harness/oracle/providers/regenerate-request-envelopes.sh` run from the
  pin — never through the sweep driver (it refuses recording stages since
  the P4.9L incident). Never two sweeps concurrently (§Shared contract
  §C).
- **Sibling lanes:** P4.D126 (`p4.d126-memory-wipe-batch-bug103.md`),
  P4.D128 (`p4.d128-qt-utilities-cli-about.md`), P4.D129
  (`p4.d129-dedup-neutrality-ratify.md`). §Shared contract and
  §Ownership below are binding and identical in all four orders.
- **Worktree:** per the `carryout` skill; edit inside the worktree, not
  main. `CHANGELOG.md` and `status-log.md` are append-only for everyone.

## Mandate

Port the three provider-side commits whole: (1) the Z.AI request builder
sheds its private vision-model list — **v5 measurably HAS bug 104**
(`is_zai_vision_model` at `chat_completions.rs:54-57`, confirmed in
source 2026-08-26), so a `glm-5.3-flash` attachment is dropped with the
refusal sentence while `supportsImageUpload` has already suppressed the
describe-fallback; (2) the cheap-LLM deadline machinery grows a per-task
override table (the three compression task types → 75 s), threads the
task type into `provider_budget_for`, keeps the local 180 s checked
FIRST, and warns on every failed cheap task; (3) the realtime bus drops
its per-publish coalesce trace while keeping the `coalesced` counter the
flush line reads. Red-first everywhere a measurement can go red.

**Must NOT touch:** anything under `apps/web/` or `crates/quilltap-cli/`
(P4.D128); `db/memories.rs`, `services/delete_all.rs`,
`services/backup/**`, `services/quilltap_import/**` (P4.D126). v4's
bug-104 register/changelog rows are v4-repo bookkeeping — NO-PORT.

## Survey-verified starting points (fresh survey 2026-08-26)

⚠ Port from the shipped hunks, never the commit message (ledger §5.3).

### Unit 1 — `964ffb959` (bug 104; plugin 1.1.24)

v4 `plugins/dist/qtap-plugin-z-ai/provider.ts` (+39/−42 across .ts/.js):

- DELETED: `VISION_MODEL_PATTERNS` (`/^glm-\d+(\.\d+)?v/i`, `/^glm-5v/i`,
  `/^autoglm-phone/i`), `isVisionModel()`, and `buildUserContent`'s
  `modelSupportsVision` refusal branch (the sentence `'Selected Z.AI
  model does not support image input. Use a vision model such as
  glm-4.5v or glm-4.6v.'`). The MIME check
  (`Z_AI_SUPPORTED_MIME_TYPES`) and the missing-data check REMAIN the
  only failure arms. `formatMessages` loses its now-unused `model`
  parameter; both call sites (send + stream) drop the second argument.
- v4's replacement doc-comment (carry its substance): the host has
  already made the call — attachments only reach the builder when the
  profile's `supportsImageUpload` is set; when it isn't, the
  describe-fallback replaced the bytes long before. One question, one
  answer — bug 91's rule, the shape NanoGPT took at P4.D106.
- v5 twins (verified in source at the drift check):
  `crates/quilltap-core/src/model/request_builder/chat_completions.rs` —
  `is_zai_vision_model` at `:54-57`, the refusal sentence at `:106`, the
  gating branch at `:864`. All three GO; the MIME and missing-data arms
  stay. Check for a `formatMessages`-style model param twin to drop.
- **Corpus work (the lane's real proof):** grep
  `harness/oracle/fixtures/request-envelopes/request-envelopes.recorded.ndjson`
  for the refusal sentence to find the affected rows; regenerate the
  corpus from the pin (the deliberate script, above) — the vision-refusal
  rows FLIP red-first (attachment now reaches the wire as `image_url`),
  every other row byte-identical. Add the **`glm-5.3-flash` + attachment
  row** — the drift ledger's §1 note retires the banked 💸 "Z.AI refusal
  sentence" dogfood item (the sentence can no longer be provoked) and
  names this row's live twin as the replacement proof: record that
  retirement + the new owed proof in the lane record. Corpus-provider
  choice trap: `v4-attachment-tiers-disagree.md`.

### Unit 2 — `8872d7efc` (per-task budgets + the failure warn)

v4 `lib/memory/cheap-llm-tasks/core-execution.ts` (+63/−14):

- NEW `CHEAP_LLM_TASK_TIMEOUT_OVERRIDES_MS`:
  `compress-conversation-history` / `compress-system-prompt` /
  `compress-memories` → `75_000`. Carry v4's why-comment whole (the
  Friday measurement: 13 of 34 near-budget calls, mean 24.4 s ≈ 2.5× the
  cheap-task mean; kept short of doubling because the uncached
  compression path is a synchronous inline wait).
- NEW exported `deadlineFor(selection, taskType?)`: **local FIRST**
  (`if (selection.isLocal) return CHEAP_LLM_TASK_TIMEOUT_LOCAL_MS`) —
  a per-task override can never shrink the local budget — then
  `OVERRIDES[taskType ?? ''] ?? CHEAP_LLM_TASK_TIMEOUT_MS`.
- `providerBudgetFor(selection, taskType?)` =
  `deadlineFor(...) - PROVIDER_BUDGET_HEADROOM_MS` (5 s); `sendToProvider`
  threads `taskType` into `requestTimeoutMs`.
- `runCheapLLMTask`'s duplicate local `deadlineFor` closure DELETED; the
  primary attempt AND the uncensored retry both call the module-level
  `deadlineFor(selection, taskType)` (fresh budget per attempt — the
  comment stays).
- NEW `logger.warn('[CheapLLM] Task failed', { taskType, chatId,
  characterId, provider, model, error })` in the catch arm — v4's
  rationale: a provider giving up on its OWN budget arrived as an
  ordinary provider error, invisible to a server-log grep.
- v4's tests pin: the three 75 s budgets; the 45 s default for SIX named
  non-compression tasks (so the override cannot silently become a global
  bump); the unknown/absent-task fallback; the local exemption.
- v5 twins (verified at the drift check): `services/cheap_llm_exec.rs` —
  `CHEAP_LLM_TASK_TIMEOUT_MS = 45_000` at `:143`, local 180 s at `:149`,
  `cheap_llm_deadline_for` at `:160`, `provider_budget_for` at `:176`
  (both currently take only the selection — the signature grows a
  task-type argument; check whether v5 also carries a duplicate closure
  twin to delete). ⚠ **The timeout MESSAGE bytes**
  (`cheap_llm_timeout_message`, pinned at `:1056-1065`): a compression
  timeout must now read `75000ms` — flip those pins red-first. Mirror
  v4's four test groups; the warn line is log-only, pinned by a capturing
  tracing layer (P4.61 idiom;
  `a-process-global-test-seam-must-be-thread-scoped.md`).
- Also regen/re-run `cheap_model_equivalence` and any tier-3 family whose
  recipe stages cheap-LLM budgets, at the pin, asserting nothing else
  moved.

### Unit 3 — `21f573039` (the coalesce trace)

- v4 `lib/realtime/bus.ts`: ONE deleted line — the
  `'Realtime publish coalesced'` debug print. The `coalesced` counter
  STAYS (the flush line reads it).
- v5 twin (verified at the drift check):
  `crates/quilltap-core/src/realtime/bus.rs:202-203` — delete the emit,
  KEEP the counter; the flush read at `:238` is untouched. Log-only: no
  differential can see it (`differential-blind-to-a-log-only-fix.md`) —
  the pin is a capturing tracing layer asserting SILENCE at the publish
  site and presence of the flush line (P4.61's silence-pin idiom;
  beware `coalescing-hides-its-own-mutation-proofs.md` when
  mutation-proving).

## Tiered deliverables

**Tier 1 (must land):**
1. Unit 1 whole — the three deletions + the doc-comment, the corpus
   regenerated with red-first flips, the `glm-5.3-flash` row added,
   pre-existing rows asserted byte-identical.
2. Unit 2 whole — overrides table, `deadline_for`/`provider_budget_for`
   task-type threading with local-first, the retry path on the shared
   fn, the timeout-message pins flipped red-first, v4's four test groups
   mirrored, the warn line with its capturing-layer pin.
3. Unit 3 — the trace deletion with the silence pin.

**Tier 2 (should land):**
4. A mutation pass per unit (override-key typo, local-check reordered
   AFTER the override, the warn line's field set, the counter deleted
   with the trace).
5. The 💸 bookkeeping: retire the Z.AI-refusal dogfood item, bank the
   `glm-5.3` live-wire proof + the 75 s compression budget on a real
   fold as new dogfood-queue rows (named in the lane record).

**Tier 3 (explicit deferrals, loud):**
6. Nothing anticipated; any discovered adjacent gap gets a named refusal
   or a phase-4.md candidate row, never a silent stub.

## The differential requirement, restated

Unit 1's proof is the committed-corpus differential
(`request_builder_equivalence` over the regenerated envelopes — the
recipe is the corpus family's own header; the regenerate script runs v4's
REAL plugin from the pin through the plugins/dist symlink,
`jest-oracle-plugin-factory-is-mocked.md` names the trap when a route
family is involved instead). Unit 2's observable half is unit-tier
(deadlines are internal), with the message bytes as the corpus-visible
edge; the warn and trace are log-only and pinned by capturing layers —
state that in each case header (`differential-blind-to-a-log-only-fix`).
A green regen is not coverage: grep the regenerated NDJSON for the
changed bytes (`image_url` present on the flipped rows, the refusal
sentence ABSENT).

## Fixtures

Consumes/regenerates: the request-envelopes committed corpus (owner,
§Shared contract §A). No DB fixtures move. If `response_parse_equivalence`
or the stream-decoder corpora prove untouched by these hunks, record the
verification (one grep each), don't regen idly.

## Shared contract (BINDING — identical text in all four orders)

- **§A Family ownership for this round's regens:** P4.D126 owns
  `system_delete_data_equivalence`, `system_restore_equivalence`,
  `system_restore_state`, `restore_vintage_state`,
  `qtap_import_equivalence`, `memory_delete_tier2_equivalence`,
  `memory_cascade_tier2_equivalence`. P4.D127 owns
  `request_builder_equivalence` + the committed request-envelopes corpus
  (`harness/oracle/fixtures/request-envelopes/`),
  `response_parse_equivalence`, `cheap_model_equivalence`. P4.D128 owns
  the CLI Tier R (`cli_differential.rs`) + `completion_behavior.rs`. No
  other lane regenerates or edits these. P4.D129's bulk neutrality sweep
  EXCLUDES every family in this section by name and hands any red in
  them to the owning lane.
- **§B Pins:** the round's target baseline is **`8872d7efc`**. Each lane
  regenerates its OWN (§A) families from a lane-unique worktree pinned at
  `8872d7efc`; regens of families unrelated to any lane's commits stay
  pinned at `f3892158d` — except P4.D129, whose deliverable is exactly
  the `8872d7efc` regen of the non-§A families.
- **§C Sweep serialization:** `recipe_sweep.py` bulk runs (`--run-all`)
  are single-flight repo-wide. Lanes 126–128 run per-family regens only.
  P4.D129 runs its bulk sweep AFTER lanes 126–128 have closed their
  branches (execution arrangement in the round report); if forced to run
  earlier it must not touch §A families or their /tmp staging.
- **§D The AboutView split:** `8440b6391`'s `app/about/AboutView.tsx`
  hunk (provider list + the NEW "Live interface" bullet) is ported by
  P4.D128; the commit's docs remainder is ratified NO-PORT by P4.D129.
  Neither lane touches the other's half.

## Ownership (BINDING — identical table in all four orders)

| Path | Owner |
|---|---|
| `crates/quilltap-core/src/services/delete_all.rs`, `services/backup/restore/**`, `services/quilltap_import/**`, `db/memories.rs`, the new legacy-fields helper module | P4.D126 |
| §A families for P4.D126 + any new/changed restore archive fixture | P4.D126 |
| `crates/quilltap-core/src/model/request_builder/chat_completions.rs`, `services/cheap_llm_exec.rs`, `crates/quilltap-core/src/realtime/bus.rs` | P4.D127 |
| §A families for P4.D127 + the request-envelopes committed corpus | P4.D127 |
| `apps/web/**` (utilities CSS, component class sweep, `about-page.ts`), `crates/quilltap-cli/**` (completion templates, `completion_behavior.rs`, Tier R cases) | P4.D128 |
| No v5 source by default; MAY add (loudly, in its record): a harness pin for `help_doc_chunks`' blob-column re-assert, vestigial-twin removals of the knip-deleted exports (wardrobe helper modules) | P4.D129 |
| `CHANGELOG.md`, `docs/developer/porting/status-log.md` | append-only, all lanes |
| `docs/developer/porting/drift-ledger.md` | NOBODY (lanes never write it) |

Version bumps: each lane bumps the crates/SPA it touches per
`.claude/commands/commit.md`; the unifier recounts at the wire.

## Verification gate (before declaring done)

1. `cargo fmt --check`; `cargo clippy` BOTH feature sets.
2. `cargo test --workspace` (full log captured; background +
   notification, never a pgrep loop, never `| tail`).
3. This lane's §A families regenerated fresh at the pin and re-run BY
   NAME, zero SKIP; the corpus diff summary (rows flipped / rows
   byte-identical / rows added) in the lane record.
4. The red-first evidence recorded (the pre-fix refusal rows, the
   pre-fix 45 s message pins).
5. Commits per `.claude/commands/commit.md`; lane record appended to
   `status-log.md`.
