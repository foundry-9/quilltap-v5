# Work order P4.68 — the participant-status parser consolidation + the cheap-LLM / failover remainder (P4.D135/D136/D141/D146 carried follow-ups)

**Status:** OPEN — not started (ordered 2026-09-02, the follow-ups round:
P4.67 ∥ P4.68 ∥ P4.69 ∥ P4.70 ∥ P4.71). Written from `phase-4.md`
candidate 2 at the `6d2a50382` unification (the eight private
participant-status parsers), the P4.D135 lane record's named gaps
(`CheapLlmTaskExecutor::new()`'s missing chain; the empty-response
failover legs' `llm_logs` rows; the three chain-walk corpus blind spots)
and P4.D141's measured `precompute_equivalence` blindness.

## Preamble

- **v4 oracle baseline: `6d2a50382`** (drift-ledger §1, probe re-run
  2026-09-02 at ordering: v4 checkout on `main`, tree clean, both logs
  empty, §3 EMPTY). Not a drift lane; every item is a v5-side debt with
  its v4 twin already in the tree.
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 checkout
  on `main`, tree clean, `git log 6d2a50382..main` empty and `git log
  3a76b17df..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: NO PIN NEEDED** (ledger §1 as of 2026-09-02). If the probe
  finds either fact changed, §5.1's pinned worktree at `6d2a50382` is
  required for every regen. All regens through the sweep driver
  (`harness/tools/recipe_sweep.py --run <family> --v5w <worktree>`);
  header edits follow `recipe-header-conventions`.
- **Sibling lanes (this round):** P4.67 (the REST-edge query-parameter
  sweep — owns `quilltap-web/**` + three named `api/*.rs` regions), P4.69
  (the SPA follow-ups — owns Playwright), P4.70 (image fidelity + the
  `system-data-*` fixture), P4.71 (the host gateway resolver — **§E: it
  owns every provider construction site**; this lane's executor census
  RECORDS host-side findings, never edits `spine.rs`). §Shared contracts
  / §Ownership are binding and identical across all five orders.
- **Worktree:** per the `carryout` skill. `CHANGELOG` / `status-log.md`
  append-only. **No Playwright** (P4.69 owns port 4319); no SPA surface.
- **Long gates:** `run_in_background: true` + full log + sentinel; NEVER a
  `pgrep` loop; NEVER `| tail`. Never edit source during a workspace gate.
  Mutation proofs revert by file backup, never `git checkout <file>`.

## Mandate

(1) Consolidate every private participant-status parser onto
`chat_predicates::participant_status_from_str` / `is_participant_present`
— **behaviour-neutral by measurement at every site** (a site whose v4 twin
uses a DIFFERENT unknown-string rule stays separate, with the v4 line
cited), proven by the existing families re-run by name. (2) Thread the
failover log context at the orchestrator's empty-response recovery call so
every failover leg logs its `CHAT_MESSAGE` row as v4's `restreamInto` does,
and close the three chain-walk corpus blind spots. (3) Measure the
`CheapLlmTaskExecutor::new()` "missing chain" gap against today's tree and
close it by wiring or by an executable census. (4) Make
`precompute_equivalence` discriminating on the uncensored reroute. Must NOT
touch: any `quilltap-web` file (P4.67), `tools/generate_image.rs` or
`image_gen/**` (P4.70), the host crate or `model/completion_provider.rs`
(P4.71), the SPA (P4.69), the `system-data-*` fixture (P4.70, §D).

## Survey-verified starting points (2026-09-02)

1. **The canonical home.** `crates/quilltap-core/src/chat_predicates.rs:87`
   `participant_status_from_str(s: Option<&str>) -> ParticipantStatus`:
   `None`/`"active"` → Active, `"silent"` → Silent, `"removed"` → Removed,
   **anything else → Absent**; `:105` `json_participant_is_present`;
   `:39-44` `is_participant_present` (Active | Silent). Its doc comment
   (`:80-86`) already names the eight sites and calls the sweep
   "behaviour-neutral across eight files no single lane owns".
2. **The seven `ParticipantStatus`-returning copies:**
   `enclave/announce.rs:215 parse_status`,
   `services/commonplace_notifications.rs:453 parse_status`,
   `services/user_identity_resolver.rs:68 parse_status`,
   `services/fold_episode_pass.rs:79 parse_status`,
   `services/turn_orchestrator.rs:192 parse_status` (+ its unit test
   `:844 parse_status_defaults_and_unknown` — keep it, retarget it at the
   canonical), `services/participant_resolver.rs:153 parse_status`,
   `services/message_finalizer.rs:1672` (the inline match inside
   `to_speaker_participant`). **Verify each one's unknown arm and `None`
   default against the canonical BEFORE replacing it** — a copy whose
   `_ =>` arm differs is a measurement, not a typo, and its v4 twin
   decides (cite `git -C ~/source/quilltap-server grep -n "status ===" lib/…`
   at the twin).
3. **The two `build_context.rs` parsers are NOT the same rule:**
   `:3515 parse_sys_status(&str) -> Option<system_prompt::ParticipantStatus>`
   (unknown → `None`) and `:3525 parse_attr_status(&str) ->
   chat_predicates::ParticipantStatus` (**unknown → Active**, the opposite
   of the canonical's Absent). Neither may be silently unified: read v4's
   twin at each call site (`lib/chat/build-context*.ts` / the attribution
   name lookup) and record which rule v4 applies; consolidate only a site
   whose v4 rule IS the canonical one; leave the rest with a one-line
   comment naming the v4 line that fixes its rule.
4. **The three string-level tool sites** (the doc comment's "at string
   level"): `tools/self_inventory.rs:815 matches!(status, "active" |
   "silent")`, `tools/whisper.rs:113`, `tools/doc_edit/shared.rs:320`
   (`status == "active" || status == "silent"`) — each is exactly
   `is_participant_present(participant_status_from_str(Some(status)))`
   for every input (unknown → Absent → false). ⚠ `services/
   host_notifications.rs:70-72` and `services/chat_participants.rs:666-668`
   are LABEL tables (status → prose), not parsers — leave them.
5. **The failover log gap.** `services/provider_failover.rs:150-170`
   records it in-code: v4's `restreamInto` calls `streamMessage({…,
   userId, messageId, chatId, characterId})`
   (`provider-failover.service.ts:266`) so each retry leg that reaches its
   terminal chunk logs a `CHAT_MESSAGE` row through the same wrapper as the
   primary stream (`streaming.service.ts:407`); v5's orchestrator spine
   calls the no-logging entry `attempt_empty_response_recovery` because its
   `AttemptEmptyResponseRecoveryOptions` carries no `db` /
   `preGeneratedAssistantMessageId`. The differential drives
   `attempt_empty_response_recovery_with_log` to prove the row shape; the
   SPINE is unpinned. Fix shape (the record's own): thread `FailoverLogCtx
   {db, message_id}` at `services/orchestrator.rs`'s recovery call. The
   hard-error failover site DOES thread the log already — read it for the
   idiom.
6. **The three chain-walk corpus blind spots** (P4.D135's record, and the
   P4.D136 §3 review): a mid-chain EMPTY answer that continues the walk;
   the no-key `auth` attempt's `no-api-key-configured` reason bytes;
   fail-then-recover. Rows for the provider-failover family (its oracle
   case under `harness/oracle/cases/`; find the family by grepping the
   tests dir for `provider_failover`).
7. **`CheapLlmTaskExecutor::new()`'s "missing chain" — measure first.**
   `services/cheap_llm_exec.rs:506 new()` builds `fallback: None`; `:513
   with_logging(CheapLlmLogConfig)` populates the `CheapFallbackHandle`
   from the log config's `Db` + user id. P4.D135's record named two
   PRODUCTION bare sites (`tools::generate_image`'s prompt expansion, one
   `enclave::step` leg). **The 2026-09-02 grep finds every `::new()`
   construction past its file's `#[cfg(test)]` marker** —
   `tools/generate_image.rs:2614` (marker `:2534`), `enclave/step.rs:1810-
   1811` (marker `:1427`), `services/compression.rs:441/529/565/605/639`,
   `services/answer_confirmation.rs:1148/1280` (markers unverified —
   check) — and the host constructs every production executor through
   `with_logging` (`quilltap-host/src/spine.rs:660/735/813/1164/1259/1634/
   1641/1890/2210/2273`). If zero production bare sites remain, the gap
   closes BY MEASUREMENT: land an executable census in the
   `db_error_key_guard` idiom (per-file exact counts of `::new()` outside
   `#[cfg(test)]`, mutation-proven) so a future bare production executor
   cannot land silently; a host-side site, if found, is RECORDED for the
   unifier (§E), not edited here. If a production site DOES remain, give it
   a `Db` — the record's own fix.
8. **`precompute_equivalence`'s measured blind spot** (its header
   `:21-36`): the case `dangerous-chat-reroute-runs` passes `allProfiles:
   []` on both sides, so `resolve_uncensored_cheap_llm_selection` has
   nothing to swap to and the emitted row never carries the selection;
   forcing `should_use_uncensored_route` to `false` leaves the family
   GREEN. Fix = seed an uncensored cheap-LLM profile into the committed
   `episodic-recall-*` pair through `harness/oracle/fixtures/
   build-episodic-recall-fixture.ts` (+ `episodic-recall.json`; the
   `migrate-fixtures-episodic-columns.ts` sibling is the vintage tool) and
   thread `allProfiles` through BOTH sides of
   `harness/oracle/cases/precompute.test.ts`, then add a comparand that
   SEES the selection (the reroute's chosen profile id, or the prompt
   bytes the swap changes). Red-first: the forced-false mutation must go
   RED after. The pair is shared — RE-RUN `recall_replay_equivalence` and
   `vault_conv_search_equivalence` zero SKIP after the widening.

## Tiered deliverables

**Tier 1 (must land):**

1. The seven `ParticipantStatus` copies + the three string-level sites
   consolidated per items 2/4, each site's v4 rule recorded in the lane
   record (a table: site → v4 twin line → rule → consolidated Y/N), the
   canonical's doc comment updated to name any site deliberately left.
2. The failover log thread at the orchestrator's recovery call (item 5),
   pinned RED-FIRST by `orchestrator_tier3`'s `llm_logs` comparand (the
   `QT_ORACLE_LLM_LOGS` env split — memory note
   `llm-logs-key-order-and-preserve-order`): a case whose primary stream
   answers empty and whose failover leg completes must show the leg's
   `CHAT_MESSAGE` row with `characterId` set.
3. The bare-executor measurement + census (item 7).

**Tier 2 (should land):**

4. The `build_context.rs` pair adjudicated per item 3 (consolidate or
   record with the v4 line).
5. The three chain-walk corpus rows (item 6), each a discriminating arm
   (a mutation that skips the continuation / alters the reason bytes must
   redden exactly its row).
6. `precompute_equivalence` made discriminating (item 8), with the
   forced-false mutation proof recorded both ways.

**Tier 3 (explicit deferrals, loud):** `resolve_provider_for_dangerous_
content`'s missing per-arm log lines (pre-existing; the handler-logging
sweep) — record, do not port here. Any site whose v4 rule is not the
canonical stays a NAMED non-consolidation with its line.

## The differential requirement (restated)

The consolidation is behaviour-neutral, so its proof is **every family
that drives the touched files, re-run by name through the sweep driver
after the edit, zero SKIP** — find them by grepping
`crates/quilltap-harness/tests` for each touched module (`skip_signal_`,
`orchestrator_tier3`, `build_context_tier3`, `post_office_writers_tier3`,
the announce / commonplace / fold-episode / user-identity /
participant-resolver / message-finalizer / self-inventory / whisper /
doc-edit families, at least). A regen-twice proves only that v4 did not
move (memory note `no-behavior-change-lane-needs-v5-rows`) — the V5 rows
are the evidence, and one deliberate wrong-arm mutation at one site must
redden a named family. Items 5/6/8 are ordinary red-first corpus work
against v4's REAL code (the tier-3 oracles under `harness/oracle/cases/`).

## Fixtures

Consumes and MAY WIDEN the committed `episodic-recall-*` pair (§F — this
lane's only fixture; re-run its two other consumers). Consumes the tier-3
fixtures its families already build. Touches nothing under
`system-data-*` (§D).

## Shared contracts (binding, identical across all five orders)

- **§A — well-formed actions never move.** P4.67 may change the answer
  to `?action=` (empty), an UNKNOWN action, and DUPLICATED keys only. The
  status + body of every well-formed `?action=<served>` request and every
  single-valued query is byte-identical before and after (the existing
  web families are the pin). P4.69's SPA callers (`image-profiles.api.ts`
  `?action=options-schema|list-models`, the wardrobe/characters edges)
  therefore need NO change and P4.69 makes none.
- **§B — participant-status parsing has ONE home:**
  `quilltap_core::chat_predicates::participant_status_from_str` (+
  `is_participant_present` / `json_participant_is_present`). P4.68 owns
  the consolidation; no other lane adds a private status match, and no
  lane changes the canonical unknown-string rule (`None`/`"active"` →
  Active, `"silent"` → Silent, `"removed"` → Removed, anything else →
  Absent).
- **§C — danger styling is a client predicate.** `shouldShowDangerStyling
  (chat) = getConciergeState(chat) === 'flagged'` lives ONLY in the SPA
  (`chat/concierge-state.ts:127`, v4 `chat-override.ts:110`); P4.69 binds
  it to the message list; no server payload changes (the §A list wire of
  the `6d2a50382` round stands).
- **§D — the `system-data-*` committed fixture is P4.70's this round.**
  P4.70 widens it (additively — every existing row byte-preserved,
  `system-data.json` regenerated by the shipped builder) and re-runs all
  nine consuming families without editing them (except
  `system_import_state`'s discriminating leg, which P4.70 owns). P4.67
  consumes the committed pair at its base. The unifier re-runs P4.67's
  three consuming families over the widened pair before the gate.
- **§E — the gateway seam.** P4.71 threads `with_localhost_gateway` from
  the host into every provider construction site and adds the ONE missing
  seam (`model/completion_provider.rs`'s hard `None`). No other lane
  constructs a wire provider or touches `quilltap-host/src/spine.rs`,
  `providers.rs`, or `host.rs`; P4.68's executor census is core-only and
  RECORDS any host-side finding for P4.71/the unifier instead of editing.
- **§F — `precompute_equivalence` and the `episodic-recall-*` pair are
  P4.68's.** P4.70's fixture work is the `system-data-*` pair only; the
  two lanes' fixtures are disjoint by name.

## Ownership (binding, identical across all five orders)

| Area | Owner |
|---|---|
| `crates/quilltap-web/src/**` (every `*_routes.rs`, `log_file.rs`, the NEW shared query reader), `crates/quilltap-web/tests/**` (NEW `query_param_semantics_equivalence`; arms added to `system_body_guards_equivalence` / `files_body_guards_equivalence`); `quilltap-core`: `api/types.rs` (the `FileUpload.tags` widening ONLY), `api/system_data.rs` (the `jobs_enqueue` payload gate ONLY), `api/chat_media.rs` (the `chat_file_link` guard ONLY); harness: `system_jobs_collection_equivalence` (the `payload: []` arm), NEW `harness/oracle/cases/query-param-semantics.test.ts` | **P4.67** |
| `quilltap-core`: `chat_predicates.rs`, `enclave/announce.rs`, `services/{commonplace_notifications, user_identity_resolver, fold_episode_pass, turn_orchestrator, participant_resolver, message_finalizer, build_context, orchestrator, provider_failover, cheap_llm_exec}.rs`, `tools/{self_inventory, whisper}.rs`, `tools/doc_edit/shared.rs`, the NEW bare-executor census guard; harness: `precompute_equivalence` + `harness/oracle/cases/precompute.test.ts` + `harness/oracle/fixtures/build-episodic-recall-fixture.ts` + the committed `episodic-recall-*` pair (RE-RUN `recall_replay_equivalence`, `vault_conv_search_equivalence`), the failover / chain-walk families (`orchestrator_tier3`, the `provider_failover` family) + their oracle cases | **P4.68** |
| SPA: `chat/{message-list, message-row, streaming-message, tool-message}.ts` (+specs), `screens/salon/salon-conversation.ts`, `quick-hide/quick-hide.service.ts` (+spec), `screens/settings/images/image-profile-modal.ts` (+spec), the twelve `qt-range` hosts' SPECS (suffix pins only), `styles/qt-components/_chat.css` (only if a rule is missing); e2e: `salon-optimistic-bubble-reconcile.spec.ts`, `workspace-search-documents-flow.spec.ts`, `wardrobe-flow.spec.ts`, NEW danger-avatar beat, `global-setup.ts` if the transfer un-park needs it | **P4.69** |
| `quilltap-core`: `tools/generate_image.rs`, `image_gen/lora_support.rs`, `image_gen/params_builder.rs`; harness: `image_generation_tier3_equivalence`, `image_gen_leaves_equivalence`, `image_generate_route_equivalence` (re-run), `projects_routes_equivalence` (+ its oracle case), `system_import_state` (the discriminating leg), `harness/oracle/fixtures/build-system-data-fixture.ts` + the committed `system-data-{main,mount,llmlogs}.db` + `system-data.json`, `harness/oracle/cases/image-generation.test.ts`, `image-gen-leaves.test.ts`; the other eight `system-data` consumers RE-RUN, not edited | **P4.70** |
| `quilltap-host`: `env.rs`, `providers.rs`, `spine.rs`, `host.rs`, `avatar_preview.rs`, NEW `host_gateway.rs`; `quilltap-core`: `model/completion_provider.rs` (the gateway seam), `model/transport.rs` (doc only), `provider_manifest/rewrite.rs` (header + the pure resolver if placed here), `model/provider_models_api.rs`, `api/provider_actions.rs`, `model/image_dialects.rs` (a base-URL seam ONLY if v4 rewrites there — measure); harness: NEW `host_gateway_equivalence` + `harness/oracle/cases/host-gateway.test.ts`; `Dockerfile`, `docs/developer/running.md` | **P4.71** |
| Playwright (port 4319) | **P4.69's branch only**; every other lane's beats are authored gated (ACTIVATE-AT-UNIFY) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it |


**Version bumps:** this lane bumps `quilltap-core` and `quilltap-harness`
per commit (base: core 0.0.750, harness 0.0.642); the unifier recounts
as base + total bumps (four lanes bump core and harness).

## Verification gate before declaring done

`cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
-D warnings` on BOTH feature sets; full `cargo test --workspace` with the
lane's oracle env vars (`run_in_background`, full log + sentinel, every
`test result:` line read); every family over a touched module re-run by
name through the sweep driver, zero SKIP, plus the widened
`episodic-recall` consumers; the mutation proofs verified-applied then
reverted by file backup; `cargo build --workspace --release`. No SPA run,
no Playwright. Per-commit rule: `.claude/commands/commit.md`. Lane record
→ `status-log.md` ("Lane record — P4.68"); this file's status header
updated at close.
