# Work order P4.D63 — the archive schema (D23), write guards, chokepoint, crypto + re-encrypt, and wipe options (server) — v4 `d553f72a`

**Status:** ✅ CLOSED (2026-08-11) — **unit 7's re-encrypt wire landed in the
resumed P4.D65 lane** (`claude/p4-archive-service-round2-8850e5`): the sweep
runs at the `ChangePassphrase` dispatch arm, the response carries
`{success, archives}`, the quilltap-web edge renders it, and the owed tier-2
differential over planted ARCHIVE files exists as
`archive_reencrypt_tier2_equivalence` (6 cases, mutation-proven) plus a live
web-edge wire test. Note the wire's first differential run caught a real
defect: the sweep blocked on the writer channel and would have panicked
inside the async dispatch arm; it is now `async`. The unit-8 kept/swept arms
and the four archived-character arms from the resume list below remain OPEN
under **P4.D65 item 5**, not here. The rest of this header is kept for
history.

**Superseded note:** OPEN at unit 7 only — units 1-6 + 8-11 UNIFIED on main
(2026-08-11, the P4.D62∥P4.D63∥P4.D64 round-1 unification); **unit 7's wire
is the resume point** and the listed differential arms are OWED (resume list
below — natural round-2 items, since round 2 owns the archive verbs that
make bundles exist). **The §3 unification review fixed four things in this
lane's code on the unify branch:** (1) the one-default rule had leaked into a
SIXTH site — help-doc sync, where v4 KEEPS the `|| profiles[0]` fallback
(`help-doc-sync.ts:359`); split into `default_or_first_profile_id` with a
resolver-split pinning test; (2) `PassphraseSource`'s `Debug` derive could
print the live passphrase — now a redacting manual impl; (3) the
characters-read tolerance probed only `archivedAt` — now per-column, so a
table interrupted between ALTERs still reads; (4) `reencrypt_one` downloaded
via the RAW backend, which would have leaked P4.D62's Bug-55 SOH sentinel
verbatim into the change-passphrase `failures[].reason` once unit 7 wires —
now routed through the manager (v4's own structure). Also recorded by review,
NOT fixed (unpinned edges for the resume): the four decrypt error-class
divergences on forged/corrupt-but-valid-JSON headers (v5 answers
`ArchiveFormatError` where v4 reaches Integrity/PassphraseMismatch — real
users cannot construct these inputs), and unit 5's repo-wrapper
`{chat, oldStatus}` shape having no tier-2 case of its own (round 2's flips
are its first caller).

### Landed (differential-proven, see the lane record in `status-log.md`)

1. **Unit 1** — the D23 re-dump (three columns, no index change), the
   `enclave/step.rs` DDL mirror, `db/character_archive_repair.rs` + its host
   boot wire, `characters_read`'s pragma-guarded tolerance, the struct
   adoption, and the list-DTO `archivedAt`.
2. **Unit 2** — `DbError::CharacterArchived` + `validate_character_archive_patch`
   at v4's placement, the wardrobe-write throw, the read-overlay comments.
3. **Unit 3** — the `archived=` chokepoint, the participant/group projections,
   both add-member refusals, the archived-export 400.
4. **Unit 4** — `SpeakerCharacter` + the archived turn filter, the
   participant-resolver throw, the Carina probe, the character resolver, the
   self-inventory filters, the four mail refusals, the doc-edit resolver, the
   conversation-summary skip. (The v4 backfill skip is a recorded exclusion:
   v5 has no startup-backfill subsystem.)
5. **Unit 5** — the `setParticipantStatus` repo wrapper. (v4's user-scoped
   override is a recorded exclusion: v5 has no user-scoped repository layer.)
6. **Unit 6** — `services/character_archive/crypto.rs` byte-exact over a
   17-arm tier-1 differential, and the engine-held runtime passphrase cache
   (four deposit sites + lock-clears) with its lifecycle test.
8. **Unit 8** — `DeleteUserDataOptions` + the summary keys + the preview shape
   + the replace-mode restore threading + `RestoreOptions`, wired through
   `SystemDeleteData` / `SystemRestoreExecute` and both web edges.
9. **Unit 9** — `CharacterArchive` / `CharacterRehydrate` defined and refusing
   loudly BY NAME (round 2 named in the sentence); the files `category=`
   exact-match filter.
10. **Unit 10** — the one-default embedding rule at all five v5 sites, with
    v4's new "No **default** embedding profile configured" wording.
11. **Unit 11** — the two feature docs mirrored under
    `docs/v4/developer/features/`. The migration pretty-label is recorded
    NO-PORT (v5 surfaces no migration labels); the five `help/*.md` edits join
    the `p4.9i2` bank.

### STILL OPEN — resume here

- **Unit 7, the wire half.** `services/character_archive/reencrypt.rs` IS
  ported (never-abort, the result bag, the `total: -1` shape, the mismatch
  reason, the empty-leg sentinel handled by the caller) and shape-tested, but
  it has **no caller yet**: the engine's `ChangePassphrase` arm still answers
  `Ack`, so the web edge still returns `{"success":true}` without `archives`.
  To finish: run the sweep at the ENGINE DISPATCH ARM (not inside
  `change_passphrase`, which holds the non-reentrant state mutex) — that is
  also v4's own structure, where phase two lives in the ROUTE — then add the
  response variant carrying `{success, archives}` and widen the web edge.
  Its tier-2 differential over planted ARCHIVE files is owed with it.
- **Unit 8's kept/swept differential arms.** The code is landed and the
  family is neutrality-green, but the arms need ARCHIVE-category `files` rows
  in the `system-data-*` fixture, which **P4.D62 owns** (the Fixtures
  section's cross-lane coordination). Write them against the widened family,
  or plant into a /tmp copy for a lane gate.
- **Archived-character arms** for `mail_carina_tools`, `self_inventory`,
  `post_office_routes`, `participant_resolver_tier2`. All four are
  regenerated and NEUTRALITY-green, and the guards they cover are the same
  `is_archived` predicate proven in the characters/select-speaker families,
  but each needs its own fixture extended to exercise its refusal directly.

## Preamble

- **v4 baseline for this round: `d553f72a`** (v4 4.8.0-dev, HEAD at planning
  2026-08-10, tree CLEAN — verified at planning). This is **round 1 of 2**
  of the character-archive drift catch-up (v4 `f6eac168..d553f72a`): this
  lane ports the **schema + everything that reads it** — the three new
  `characters` columns (D23 re-dump), the write guards and refusal arms,
  the `archived=` list chokepoint, the participant-status repo method, the
  archive crypto + passphrase-change re-encrypt sweep, the wipe/restore
  spare-bundles options, and the one-default embedding rule. The archive
  **service itself** (prune/bundle/rehydrate/API actions/CLI) is
  **round 2**; this lane DEFINES its two verbs refusal-armed.
- **Drift-check at lane start.** In `~/source/quilltap-server`, run
  `git log d553f72a..HEAD --oneline`. If v4 moved, **STOP** and report.
  Regenerate every oracle this lane touches at `d553f72a` (pin a detached
  worktree on drift — `oracle-regen-pinned-v4-worktree`, or
  `recipe_sweep.py --v4 d553f72a`). Oracles outside this lane's families
  keep their `f6eac168`-or-earlier vintage; the baseline moves at
  unification.
- **Sibling lanes this round (disjoint ownership):**
  - **P4.D62** — the export/import/file-storage substrate (preserveIds,
    Bug 52/54/55). It owns `services/qtap_export/**`,
    `services/quilltap_import/**`, `services/file_storage.rs`,
    `files_routes.rs`, `files_store.rs` — do not touch those. Its fixtures
    deliberately stay OLD-schema (Shared contract rule 9).
  - **P4.D64** — the whole SPA surface against the Shared contract, beats
    gated. Pure `apps/web`.
- **Worktree:** carry the lane out on its own branch per the `carryout`
  skill.

## The drift, classified (surveyed 2026-08-10, fresh line numbers at `d553f72a`)

| v4 file | what changed |
|---|---|
| `migrations/scripts/add-character-archive-fields.ts` + `lib/schemas/character.types.ts:199–204` | Three nullable columns on `characters`: `archivedAt` TEXT, `archiveFileId` TEXT, `archivedAvatarFileId` TEXT (no new indexes). generateDDL picks them up from the Zod schema. DDL.md comments: "ISO timestamp when the character was archived into a tombstone." / "File row holding the archived character bundle export." / "File row holding the archived avatar thumbnail copy." **`archivedAvatarFileId` is vestigial** under §4.2a (never written by the shipped service; rehydrate clears it) — port the column faithfully. Migration pretty label (`lib/startup/prettify.ts:103`): "Preparing archive tombstones for your characters" |
| `lib/database/repositories/characters.repository.ts` | `CharacterArchivedError` (`:21`) — message `` `Character ${characterId} is archived: this character is archived; rehydrate it to continue` ``; `validateCharacterArchivePatch` (`:42`) — when `existing.archivedAt`, the ONLY sanctioned patch is the single-key `{archivedAt: null}`; wired at the top of `update()` (`:320–327`) via `findByIdRaw`, BEFORE the write overlay, so one guard covers every sub-array mutator; `delete()` stays unguarded (escape hatch). **⚠ The `archived=` filter is NOT here** — repo reads still include archived rows (§4.3); the chokepoint is the API list handler |
| `lib/database/repositories/chats.repository.ts:421–427` + `lib/repositories/user-scoped.ts:205–213` | New `setParticipantStatus(chatId, participantId, newStatus)` → `{chat, oldStatus}`; user-scoped override ownership-checks via `findById` (throws `'Chat not found or access denied'`). `ParticipantStatus` = active\|silent\|absent\|removed; `isParticipantPresent` = active\|silent. (The flip FUNCTIONS live in archive-service — round 2; the repo method lands now) |
| `lib/database/repositories/vault-overlay/read-overlay.ts` | **No archived short-circuit, deliberately** — comments at `hydrateOne` (`:136`) and `applyDocumentStoreOverlayOne` (`:379`) pin the §4.2a reversal: archived characters hydrate exactly like live ones. Carry the comments |
| `lib/database/repositories/vault-overlay/wardrobe-writes.ts:81–83` | `resolveWardrobeMount` THROWS `CharacterArchivedError` when archived (a null return would fall through to the legacy DB write and silently mutate) |
| `lib/chat/turn-manager/selection.ts:50–54` | `selectNextSpeaker`'s active-participant filter also drops a seat whose character (from the pre-existing `characters` map param) has `archivedAt`. New jest case at `turn-manager.test.ts:219`: an archived seat that somehow stayed `active` still yields `nextSpeakerId: null, reason: 'user_turn'` |
| `lib/services/chat-message/participant-resolver.service.ts:198–200` | After the character load: `if (character.archivedAt) throw new CharacterArchivedError(character.id)` |
| `lib/services/chat-message/orchestrator.service.ts:892` | Carina probe: `anyCanBeCarina = rawCharacters.some(c => !c.archivedAt && c.canBeCarina === true)` |
| `lib/services/character-resolver.ts:30,:58` | Name matches skip archived; **an exact id match still resolves an archived character** (callers give the named refusal); a live namesake wins |
| `lib/tools/handlers/self-inventory/builders.ts:678,:683` | Carina section: `selfEnabled` requires `!archivedAt`; the reachable-others pool filters archived |
| `lib/tools/handlers/list-email-handler.ts:53`, `send-mail-handler.ts:56,:64` | Tool refusals: `That character is archived; rehydrate it to continue.` (list-email + send-mail sender); `That recipient is archived; rehydrate them to continue.` (send-mail recipient) |
| `app/api/v1/chats/[id]/actions/mailbox.ts:43`, `send-mail.ts:48,:55` | The same two sentences as 400s at the chat-action edge |
| `app/api/v1/chats/[id]/helpers.ts:66` | `getEnrichedCharacter` projection gains `archivedAt: charData.archivedAt ?? null` (participant chips badge archived seats) |
| `lib/doc-edit/path-resolver.ts:65–67` | `resolveSelfVaultMountPointId` returns **null** for an archived character (debug-logged) — the doc tools degrade with their own sentence |
| `lib/file-storage/conversation-summary-vault-bridge.ts:212` | Summary WRITE skips archived participants (raw read); the removal path stays unguarded |
| `lib/startup/backfill-character-vaults.ts:63` | Skips archived rows (progress still ticks) |
| `app/api/v1/characters/handlers/get.ts:28–33,:88` | **The `archived=` chokepoint**: `only` / `include` / anything-else-or-absent = exclude; stacks with `npc` + `controlledBy`; each list item carries `archivedAt ?? null` |
| `app/api/v1/characters/[id]/handlers/get.ts:67` | `?action=export` for an archived character → 400 `This character is archived; rehydrate them to export, or use their archive bundle.` |
| `app/api/v1/characters/[id]/handlers/post.ts:372,:396` + `route.ts:25–26` | `action=archive` / `action=rehydrate` registered — **this lane defines the verbs refusal-armed; round 2 fills them** (shapes + the passphrase-400 sentence in the Shared contract) |
| `app/api/v1/files/handlers/get.ts:17–23` | `?category=` exact-match filter (the ChangePassphraseCard's courtesy count) |
| `app/api/v1/groups/[id]/actions/group-crud.ts:66,:148` | Members projection gains `archivedAt`; add-member refuses archived → 400 `That character is archived; rehydrate them before adding them to a group.` Membership edges survive archiving |
| `app/api/v1/projects/[id]/actions/roster.ts:77` | Add refuses archived → 400 `That character is archived; rehydrate them before adding them to a roster.` Removal unguarded |
| `lib/characters/archive-crypto.ts` (NEW, 261 ln) | PBKDF2-SHA256 600 000 iters, 32-byte salt, 32-byte key; AES-256-GCM, IV 16, tag 16; `CIPHER_CHUNK_BYTES = 8 MiB`. Layout v1: magic `QTAPARC1` (8 ascii bytes); uint32 **BE** header length; UTF-8 JSON header `{version:1, algorithm, kdf:'pbkdf2', kdfIterations, kdfDigest, salt(hex), iv(hex), keyHash(hex sha256 of derived key)}`; ciphertext; 16-byte GCM tag. Fresh salt+IV per bundle; decrypt reads KDF params FROM the header. Errors: `ArchivePassphraseMismatchError` ("This archive predates your passphrase change: …", thrown on keyHash mismatch BEFORE touching ciphertext), `ArchiveIntegrityError` (GCM failure after hash match), `ArchiveFormatError`, `ArchiveKeyUnavailableError` ("Archive encryption needs the instance passphrase, which this process has not seen…"). Exports `isEncryptedArchive(buf)`, `encryptArchive`, `decryptArchive` |
| `lib/startup/passphrase-cache.ts` (NEW) + `lib/startup/dbkey.ts` | In-memory runtime passphrase cache; `resolveArchivePassphrase()` = cache → else `INTERNAL_PASSPHRASE` when `!hasUserPassphrase` → else throw `ArchiveKeyUnavailableError`. NEVER the pepper. Deposit chokepoints: `setupDbKey`, `unlockDbKey`, `changePassphrase`, `storeEnvPepperInDbKey` (which also sets the has-user-passphrase flag); `lockDbKey` clears |
| `lib/characters/archive-reencrypt.ts` (NEW, 124 ln) + `app/api/v1/system/unlock/route.ts:349–380` | `reencryptArchiveBundles(old, new)` on passphrase change (empty passphrase legs map to `INTERNAL_PASSPHRASE`): iterates `files.findByCategory('ARCHIVE')`; per file download → decrypt-or-passthrough (pre-encryption bundles gain a header) → encrypt(new) → `uploadRaw` in place → `files.update({size})`. Never aborts the sweep; result `{total, reencrypted, failures:[{fileId, filename, reason}]}`; passphrase-mismatch reason: `'does not open with the old passphrase — it predates an earlier passphrase change'`. Route response becomes `{success: true, archives}`; a wholly-failed sweep reports `total: -1` and does NOT fail the change |
| `lib/backup/restore/delete-service.ts` + `restore.ts:62` + `lib/backup/types.ts:446` | `DeleteUserDataOptions.keepArchivedCharacterBundles` — default TRUE (`!== false`); `isArchiveBundle` = category `ARCHIVE`; spares rows+bytes in the files loop, logs `archiveBundlesKept`, reports `files: files.length - archiveBundlesKept`; `DeleteSummary` gains `archiveBundles` + `archiveBundlesKept`; preview reports on-hand bundles with `archiveBundlesKept: true`. Character ROWS are not spared — the survivor is a LOOSE bundle (importable, not rehydratable). Replace-mode restore threads the option into the wipe; `RestoreOptions.keepArchivedCharacterBundles?` |
| `lib/mount-index/embedding-scheduler.ts:78`, `reindex.ts:231`, `lib/background-jobs/handlers/conversation-render.ts:88`, `lib/startup/reconcile-conversation-rendering.ts:181`, `lib/scriptorium/cold-chunk-reembed.ts:75` | **The one-default embedding rule**: every default-profile resolution drops the `\|\| profiles[0]` fallback — only the marked default counts; with none marked, chunks WAIT (warn "No **default** embedding profile configured…"; reindex throws `'No default embedding profile configured'`) |
| `lib/schemas/file.types.ts:23` | `FileCategoryEnum` gains `'ARCHIVE'` (shared knowledge with P4.D62; no v5 enum exists — plain TEXT) |
| `docs/developer/features/{character-archive-and-export-fidelity,character-archive-spec}.md` | Mirror both under `docs/v4/developer/features/` (docs rider). The five `help/*.md` edits → the `p4.9i2` bank (v5 syncs help docs from disk; no action beyond the record) |

NOT this lane's: the archive service / rehydrate bodies / participant-flip
functions / files-delete `ARCHIVE_BUNDLE_HELD` guard / export-picker
archived filter / CLI (round 2); the export/import substrate (P4.D62); the
SPA (P4.D64). Bug 53 (file-storage reconciliation) is MOOT — the subsystem
is unported in v5 (`files_sync` refuses loudly); banked in P4.D62's record.

## Mandate

Adopt the three columns per D23 (re-dump, never by hand), make every
reader/guard/chokepoint/refusal arm match v4 at `d553f72a` byte-for-byte,
port the archive crypto + re-encrypt sweep with the runtime-passphrase
plumbing v5 has never had, land the wipe/restore spare-bundles options and
the one-default embedding rule, and DEFINE the two round-2 verbs
refusal-armed — each unit with its differential regenerated fresh at the
pin. File ownership per the Shared contract's Ownership section.

## Survey-verified starting points (2026-08-10)

- **D23**: `services/provisioning/fresh_schema.json` (32,147 bytes);
  re-dump recipe in `harness/oracle/provision/dump-fresh-schema.ts`'s
  header (`QT_SCHEMA_OUT=… npx tsx …`). Tripwire:
  `crates/quilltap-harness/tests/provisioning_equivalence.rs`. **Check the
  hand-written DDL mirrors** — the D23 rule is NOT fresh_schema alone
  (`d23-redump-is-not-only-fresh-schema` memory; the P4.D49 round found an
  eleventh mirror in the web test venue). Grep the harness + web test
  venues for `CREATE TABLE "characters"` twins.
- **Existing instances**: v5 defers the migration runner, so follow the
  established boot-ensure precedent (P4.d7 NOCASE; P4.D41 `linkGroupId`) —
  a boot repair pass over the main partition adds the three columns when
  absent, so any instance v5 boots is usable. Old-schema committed
  fixtures must keep READING cleanly: v5's `characters_read.rs` uses named
  columns, so either route reads through the boot-ensured connection or
  add pragma-guarded tolerance (the P4.D49 llm-logs precedent) — pick per
  what the read paths actually do; document the choice.
- **Struct adoption**: `db/characters.rs` (`CharacterUpdate` — the guard's
  home is `update()`; mirror v4's placement BEFORE the overlay apply, via
  the raw read) + `db/characters_read.rs` (projections; `find_by_id_raw`
  exists). **Enumerate every reader of the character bag** — the P4.D53
  lesson: a column is not adopted when the spine has it; independent
  projections (list DTO enrichment `services/character_enrichment.rs`,
  chat participant enrichment in `api/salon.rs`, self-inventory, export —
  P4.D62's, leave it) each need the field or a documented exclusion.
- **The chokepoint**: `api/characters.rs` `character_list` `:112–166`
  already applies `npc` + `controlled_by` as in-memory `retain` after
  `find_by_user_id`, before the createdAt-desc sort — `archived` slots in
  as a third retain (exclude = default). `Request::CharacterList` at
  `api/types.rs:345–351` (the block's own comment says it is BINDING with
  the SPA order); engine arm `engine.rs:1578`.
- **The two refusal-armed verbs**: `engine.rs` had a shared
  "recognized but not yet available" arm pattern (P4.9G1;
  `SystemRestorePreview`/`Execute` were its last tenants before P4.9G5) —
  reintroduce it scoped to `CharacterArchive`/`CharacterRehydrate`,
  refusing loudly BY NAME with round 2 named. REST: the characters POST
  action table in `api/characters.rs` + the web edge passes through
  dispatch (`characters_routes.rs` is P4.D62's — the action legs ride the
  core dispatch, not the multipart REST edge; verify the current
  action-verb routing before adding routes).
- **setParticipantStatus**: `db/chats_participants.rs` holds the
  participants ops; `db/chats.rs` the repo. Return shape `{chat,
  oldStatus}`; the user-scoped ownership check mirrors v4's
  (`'Chat not found or access denied'`).
- **select_speaker**: `select_speaker.rs` `select_next_speaker` (the
  active-filter site; the `characters` map param already exists — P4.D60
  landed `select_next_speaker_after_user_message` which delegates into it,
  so the archived filter covers both). Tier-1 family
  `select_speaker_equivalence.rs` / `QT_ORACLE_SELECT_SPEAKER` /
  `harness/oracle/cases/select-speaker.ts`.
- **Wardrobe-writes guard**: v5 `db/vault_wardrobe_write.rs` (the
  `resolveWardrobeMount` analog); the read-overlay no-short-circuit
  comments go in `db/vault_read_overlay.rs` (`hydrateOne` analog) — carry
  v4's why-comments verbatim.
- **Tools**: `tools/self_inventory.rs` `build_carina_section` `:1675`;
  `tools/{send_mail,list_email}.rs`; the chat-action refusals in
  `api/chat_post_office.rs` (`chat_mailbox_list` `:576` + the send-mail
  action leg).
- **doc-edit path resolver**: v5's `resolveSelfVaultMountPointId` analog
  lives in the doc-edit path-resolver module (P4.6bg landed
  `safe_realpath` + the resolver branches — locate `resolve_self_vault`
  by grep). Conversation-summary vault bridge: the v5 module that mirrors
  `conversation-summary-vault-bridge.ts` (P4.D51 touched it — grep
  `conversation_summary_vault`).
- **Crypto**: new `services/character_archive/` module (crypto + reencrypt
  submodules). The dependency stack EXISTS for `.dbkey`
  (`aes-gcm`/`pbkdf2`/`sha2`/`hex` in core's Cargo.toml `:50–67`), and
  `dbkey.rs:24–35` already defines the custom 16-byte-nonce
  `Aes256Gcm16` type — v4's archive IV is 16 bytes, so REUSE it. Header
  JSON: key order matters if diffed — write the header via a typed struct
  in v4's key order (`json-column-key-order` memory). uint32 BE length;
  8 MiB chunked cipher feed (Rust's AEAD is one-shot over a Vec — match
  v4's OUTPUT bytes, not its chunking mechanics; assert equality on real
  vectors).
- **The passphrase cache**: v5 has NO runtime passphrase cache (verified —
  no analog anywhere; the host holds only the pepper). The four v4 deposit
  chokepoints map to `engine.rs`: `unlock` `:4917`, `setup`, `store_pepper`
  (which also sets has-user-passphrase), `change_passphrase` `:5057`; lock
  clears. Hold it on the engine's state (next to `has_user_passphrase`,
  `engine.rs:410/:420`), NOT a process global — v5's engine is the
  process-boundary analog of v4's `global.__quilltapRuntimePassphrase`.
  The `ArchiveKeyUnavailableError` sentence is byte-contractual.
- **Re-encrypt + unlock route**: web edge
  `system_data_routes.rs` `system_unlock_post` `:520` (only
  `change-passphrase` is a REST action; fields `oldPassphrase`/
  `newPassphrase` `:538–539`); core `Request::ChangePassphrase`
  `types.rs:86` → `engine.rs:5057`. The sweep needs file bytes:
  re-encrypt runs where the `StorageBackend` is reachable — thread it the
  way `cleanup_thumbnails` (P4.44) reached the backend, and keep the
  never-abort + `total: -1` semantics. Empty-passphrase legs map to the
  `INTERNAL_PASSPHRASE` sentinel (`dbkey.rs:39`).
- **Wipe options**: `services/delete_all.rs` (`delete_user_data`,
  `delete_all_user_data`, `preview_delete_all_user_data`); restore
  threading in `services/backup/restore/orchestrator.rs` (`restore()`
  `:74`; replace-mode wipe call). Differential
  `system_delete_data_equivalence.rs` / `QT_ORACLE_SYSTEM_DELETE_DATA`
  diffs `{success, summary}` + a full three-partition row-count map — the
  new arms need ARCHIVE-category `files` rows planted (fixture note
  below).
- **One-default embedding rule**: v5 sites — `services/
  conversation_render_reconcile.rs` (`reconcile_conversation_rendering`
  `:178`), the embedding scheduler + reindex modules (P4.d27 territory),
  the cold-chunk module (P4.D25), the conversation-render job handler.
  Grep for the `profiles[0]`-fallback analog (`is_default` selection);
  families: `embedding_generate_jobs`, `embedding_remainder`,
  `conversation_chunks_tier2`, `cold_chunk_reembed_tier2`,
  `maintenance_sweep_tier2` — regen the touched ones, add a
  no-default-marked arm.
- **Chat helpers enrichment**: v5's enriched-character projection for
  participants lives in `api/salon.rs` (the chat-GET assembly) — the
  `archivedAt ?? null` key joins it (P4.D64's participant badge reads it).

## Tiered deliverables

**Tier 1 (must land):**

1. **The D23 re-dump** (three columns; regenerate `fresh_schema.json` from
   v4's live generateDDL at the pin, never by hand) + every hand-written
   DDL mirror + the boot ensure for existing instances + read tolerance so
   old-schema committed fixtures keep passing. `provisioning_equivalence`
   green over the fresh dump. Struct adoption across `db/characters*.rs` +
   the enumerated reader list (each reader carries the field or a
   documented exclusion).
2. **The write guard**: `CharacterArchivedError` (exact message) +
   `validate_character_archive_patch` at the top of the repo `update()`
   (single-key unarchive is the ONLY sanctioned patch; delete unguarded),
   + the wardrobe-writes throw, + the read-overlay no-short-circuit
   comments. Tier-2 cases: guarded update refused with the exact sentence;
   the unarchive patch passes; delete passes.
3. **The chokepoint + projections**: `archived=` on `characterList`
   (exclude default, `include`/`only`), `archivedAt ?? null` on list items
   + detail (`archiveFileId` too) + the chat-participant enrichment +
   group-members projection; the groups/projects add-member refusals; the
   archived `?action=export` 400. Extend the characters-routes fixture
   with a tombstoned character (below); regen
   `characters_{reads,mutations,actions,subresources}` + the groups/
   projects routes families as touched.
4. **The turn/spine guards**: the `select_next_speaker` archived filter
   (mirror v4's new jest case as a tier-1 row: archived-but-active seat →
   `null`/`user_turn`), the participant-resolver `CharacterArchivedError`
   throw, the Carina probe filter, the character-resolver name-skip/
   id-still-resolves semantics, the self-inventory Carina filters, the
   four mail refusal arms (two tools + two chat actions, sentences
   byte-exact), the doc-edit path-resolver null arm, the
   conversation-summary write skip, the backfill skip. Regen:
   `select_speaker`, `participant_resolver_tier2`, `self_inventory`,
   `mail_carina_tools`, `post_office_routes` (as touched) — each with an
   archived-character arm, mutation-proven.
5. **`setParticipantStatus`** (repo + user-scoped override, exact return
   shape + error sentence) with a tier-2 case.
6. **The crypto module**: byte-exact bundle format (magic, BE length,
   header key order, salt/IV/keyHash, tag), the four typed errors with
   v4's sentences, `is_encrypted_archive`/`encrypt_archive`/
   `decrypt_archive`, `resolve_archive_passphrase` over the NEW
   engine-held runtime passphrase cache (four deposit sites + lock-clears
   + the internal-passphrase leg). **Tier-1 differential against v4's
   REAL `archive-crypto.ts`** with `crypto.randomBytes` mocked for
   deterministic salt/IV (`jest-crypto-randombytes-mock` memory):
   encrypt vectors byte-identical both ways; decrypt each other's output;
   every error arm (wrong passphrase pre-ciphertext, corrupt tag, bad
   magic, truncated header).
7. **The re-encrypt sweep + unlock phase two**: never-abort semantics,
   the result bag, `total: -1`, the mismatch reason sentence, empty-leg →
   sentinel; the web response `{success, archives}`. Tier-2 differential
   over planted ARCHIVE files (encrypted with v4's real crypto at the
   pin): sweep re-encrypts, a planted old-passphrase bundle lands in
   `failures` with the exact reason, a pre-encryption plaintext bundle
   gains a header. Extend `settings_routes_equivalence` (or a dedicated
   family) — the unlock web edge currently has NO test file; give the
   change-passphrase leg coverage.
8. **The wipe/restore options**: `keepArchivedCharacterBundles` through
   `delete_all` (default-true `!== false`, rows+bytes spared, summary
   keys) + the replace-mode restore threading + `RestoreOptions`. Extend
   `system_delete_data_equivalence` with kept/swept arms over planted
   ARCHIVE rows (fixture note below); the restore threading pins via the
   existing restore families (verify which dump the wipe summary).
9. **The two refusal-armed verbs** (`characterArchive` /
   `characterRehydrate` + the REST action legs) answering the loud typed
   not-yet-available refusal naming round 2; the files `category=` filter
   (list handler + REST + a routes case).

**Tier 2 (should land):**

10. **The one-default embedding rule** across the five v5 sites, with the
    no-default-marked arm added to the touched families and regens run.
11. The docs mirror rider (the two feature docs under
    `docs/v4/developer/features/`; the help-docs `p4.9i2` bank note in the
    lane record). The migration pretty-label analog only if v5 surfaces
    migration labels anywhere (it does not today — record as NO-PORT if
    so, the `231be14c` precedent).

**Tier 3 (explicit loud deferrals):**

- The archive service, rehydrate, participant-flip functions, the
  files-delete `ARCHIVE_BUNDLE_HELD` guard, the export-picker archived
  filter, the tombstone three-key export carry, and the CLI subcommands —
  ALL round 2, named in the refusal arms where reachable.
- `archivedAvatarFileId` never gains a writer (vestigial; column + read
  tolerance only).

## The differential requirement, restated

Every ported unit ships with an equivalence test driving v4's REAL code at
`d553f72a`: tier-1 exact for `select_speaker` and the crypto module
(deterministic via the randomBytes mock), tier-2 structural for the repo
guard / chokepoint / projections / wipe / re-encrypt sweep, tier-3 only
where a family already runs mocked-LLM (orchestrator Carina probe — add
the archived-answerer arm to the existing family rather than a new one).
First-run-green cases must be mutation-proven (the D24 rule). Grep every
regenerated NDJSON for a fresh marker. TZ pins per the standing rule where
a family is distill-transitive.

## Fixtures

- **Consumes** the committed `characters-{main,mount}.db` +
  `harness/oracle/fixtures/characters.json`, `system-data-*` (via the
  delete-data family — coordinate: P4.D62 also extends that family;
  ownership split below), the mail/post-office fixtures, the self-inventory
  built fixture.
- **Owns** extending the characters family with a tombstoned character
  (archivedAt + archiveFileId set, participants flipped absent in a chat,
  an ARCHIVE-category `files` row) — **rebuild or mutate per the
  committed-fixture rule**, and since a rebuild at `d553f72a` carries the
  new columns, this family (unlike P4.D62's) SHOULD carry them; **re-run
  every characters-family sibling differential against the widened
  fixture** (the P4.9G3 lesson), including P4.D62-adjacent ones by name at
  the unified gate.
- **Delete-data fixture coordination**: P4.D62 owns the `system-data-*`
  widening (vault-bearing character, old schema). This lane's
  kept/swept-bundle arms need ARCHIVE `files` rows in that same family —
  hand P4.D62 the row spec (an `files` row with category `ARCHIVE`,
  folderPath `/archives`, bytes on disk) as part of its widening, and
  write this lane's arms against the widened family. If the round runs
  fully parallel, plant the rows in a /tmp-mutated copy for the lane gate
  and re-run against the committed widened family at unification (name
  this in the lane record).
- **Owns** the crypto vectors (committed small bundles built by v4's real
  `encryptArchive` at the pin, salts/IVs pinned via the mock) and the
  re-encrypt fixture rows.
- **Owns** regenerating every family its fixtures or code touch, each
  NDJSON grepped fresh.

## Shared contract (BINDING — identical in P4.D62 / P4.D63 / P4.D64)

Names are law; v4 shapes at `d553f72a`.

1. `characterList` gains `archived?: "include" | "only"` (absent/other =
   exclude, v4's default) — applied at the API list handler (the single
   chokepoint), never in the repository. List items gain
   `archivedAt: string|null` (ALWAYS present, `?? null`). Detail responses
   gain `archivedAt: string|null` + `archiveFileId: string|null`.
   **Owner: P4.D63** (P4.D64 mirrors).
2. NEW dispatch verbs `characterArchive { id }` / `characterRehydrate
   { id }` + REST `POST /api/v1/characters/{id}?action=archive|rehydrate`
   — DEFINED in P4.D63 answering the loud typed not-yet-available refusal
   (the P4.9G1 pattern); round 2 fills them. Round-2 success shapes,
   pinned now: archive → `{ archived: bool, archiveFileId: string|null,
   pruneComplete: bool }`; rehydrate → `{ rehydrated: bool, archived:
   bool, archiveBundleFileId: string|null, restored?: { memories,
   documents, blobs }, warnings: string[] }`. The passphrase-unavailable
   400 sentence: `The archive cannot be sealed: your passphrase has not
   been entered since Quilltap started. Unlock once (or restart and
   unlock), then archive again.`
3. The files list gains a `category` exact-match filter (REST
   `GET /api/v1/files?category=`; dispatch mirror). **Owner: P4.D63.**
4. `systemDeleteData` gains `keepArchivedCharacterBundles?: bool`
   (effective default TRUE — `!== false`); `DeleteSummary` gains
   `archiveBundles: number` + `archiveBundlesKept: boolean`; the preview
   reports `archiveBundles` with `archiveBundlesKept: true`.
   `systemRestoreExecute` gains `keepArchivedCharacterBundles?: bool`
   (replace-mode wipe only; accepted in both modes). **Owner: P4.D63.**
5. The change-passphrase response gains `archives: { total, reencrypted,
   failures: [{fileId, filename, reason}] }`; `total: -1` = the sweep
   itself failed; a failed sweep does NOT fail the passphrase change.
   **Owner: P4.D63.**
6. Export preview (`characters` type) gains optional `vaults: { stores,
   documents, blobs, estimatedBytes }`, present only when `stores > 0`.
   **Owner: P4.D62.**
7. Group-member projections gain `archivedAt`; add-member refusals are 400
   `That character is archived; rehydrate them before adding them to a
   group.` / `…before adding them to a roster.` The chat-participant
   enrichment projects `archivedAt ?? null`. **Owner: P4.D63.**
8. Error sentences are byte-contractual across all lanes — the
   `CharacterArchivedError` message, the tool/action refusal sentences,
   the three archive-crypto sentences, the preserveIds collision shapes,
   and the 404 body `{"error":"File content not found"}`.
9. **Cross-lane fixture rule:** the committed `system-data-*` fixtures
   keep their OLD schema this round (P4.D62 extends by mutation, never
   rebuild). v4 reading an old-schema DB emits NO archive keys on exported
   character records (Zod-optional, absent column → absent key), so
   P4.D62's families stay honestly green without P4.D63's columns. The
   three-key export carry on character records is ROUND 2's, covered by
   the archive-service differential (a bundle IS an export).
10. SPA gating: `CHARACTER_ARCHIVE_SERVER_LANDED = false` in P4.D64's e2e
    spec — flipped in ROUND 2, not at this round's unification. Beats that
    need only a tombstoned ROW (roster toggle/badges, read-only detail,
    group members line, participant badge) ride a fixture-seeded archived
    character, ACTIVATE-AT-UNIFY behind `ARCHIVE_TOMBSTONE_SEEDED` (the
    unifier seeds it per P4.D64's seeding note and flips the constant).

## Ownership (identical across the three orders)

- **P4.D62**: `services/qtap_export/**`, `services/quilltap_import/**`,
  `services/file_storage.rs`, `crates/quilltap-web/src/files_routes.rs`,
  `crates/quilltap-host/src/files_store.rs`, `db/doc_mount_*` create-path
  id-honor only, its harness families + the `system-data-*` extension +
  the preserveIds bundle fixture. **Bumps core, harness, web, host.**
- **P4.D63 (this lane)**: `api/types.rs`, `api/engine.rs`,
  `api/characters.rs`, `api/files.rs`, `api/salon.rs` (participant
  enrichment), `api/chat_post_office.rs` (mailbox/send-mail refusals),
  `db/characters*.rs`, `db/chats*.rs` (setParticipantStatus),
  `db/vault_wardrobe_write.rs`, `db/vault_read_overlay.rs` (comments),
  `db/character_resolver.rs`, `select_speaker.rs`,
  `services/{orchestrator,participant_resolver}.rs` (probe + refusal
  arms), `tools/{self_inventory,send_mail,list_email}.rs`, the doc-edit
  path-resolver + conversation-summary vault-bridge modules,
  `services/delete_all.rs`, `services/backup/restore/orchestrator.rs`
  (+ backup types) for the wipe option, the five embedding one-default
  sites, NEW `services/character_archive/`, `dbkey.rs` + the engine-held
  passphrase cache, `services/provisioning/fresh_schema.json` + the boot
  ensure, `crates/quilltap-web/src/{system_data_routes,backup_routes}.rs`.
  **Bumps core, harness, web.** (Both Rust lanes bump core/harness/web —
  identical bumps auto-merge silently; the unifier recounts as base +
  total bumps.)
- **P4.D64**: `apps/web/**` only. **Bumps the SPA version.**
- `CHANGELOG` and `status-log.md` are append-only for all lanes
  (union-merge).

## Verification gate (before declaring done)

- `cargo fmt --all` clean; `cargo clippy --workspace --all-targets` clean
  on BOTH feature sets.
- `cargo test --workspace` green with this lane's env block (the
  characters-routes families, `QT_ORACLE_SELECT_SPEAKER`,
  `QT_ORACLE_PARTRES`, `QT_ORACLE_SELFINV` + fixture vars,
  `QT_ORACLE_MAIL`/`QT_ORACLE_CARINA` — note the two-oracles-one-var
  trap: split invocations, never one env block —
  `QT_ORACLE_SYSTEM_DELETE_DATA`, `QT_ORACLE_SETTINGS_ROUTES`, the new
  crypto/re-encrypt families, the touched embedding families, and
  `provisioning_equivalence`) — every family BY NAME with `--nocapture`,
  zero SKIP lines.
- The widened characters fixture re-run: every characters-family sibling
  differential by name.
- `cargo build --release` clean. No `apps/web` change in this lane.
- Per-commit rule: follow `.claude/commands/commit.md`; end commit
  messages with the Co-Authored-By trailer.
