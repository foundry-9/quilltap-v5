# Work order P4.83 — prompt templates: the `promptTemplateList` verb + `GET /api/v1/prompt-templates`, the built-in sample-prompt seeding v5 never had, and the Import-from-Template catalogue on both character hosts

**Status (unification, 2026-09-07 — the generator follow-ups + prompt-templates
round): CLOSED — UNIFIED on main; nothing OPEN.** Tier 1 items 1–5 and Tier 2
items 6–7 landed whole (the vendored 21-prompt catalogue + generator +
`builtin_prompt_templates_guard`, the repo reads, `seed_sample_prompts` at v4's
lazy site, all five verbs + v4's REST edges, the 31-case
`prompt_templates_routes_equivalence`, both SPA hosts with v4's two open
semantics, the `prompt-templates-flow` beat); Tier 3's three refusals recorded.
The lane corrected three order premises by measurement (the seeded `name` is the
registry DISPLAY name, `MODERN General`; the READ wire OMITS a null column while
the CREATE 201 sends `null`; the `*_PT_ROUTES` env suffix) and found v4's lazy
`ensureCollection` on the beat's first live run (the read paths ensure the table
at v4's site). **The §3 unification review's catch, fixed on the unify branch:**
the CREATE path lacked that ensure — `POST /api/v1/prompt-templates` on an
instance that had never held the table answered `no such table` 500 where v4's
`_create` answers 201 — fixed + pinned by a new bare-instance wire arm that
POSTs before any list (22 after the seed). The review also corrected two
mutation-count claims in the lane record (five and six cases redden, not four
and five). **Unifier wires:** the §B DTOs folded into `core-contract.ts` with
the measured `?: T | null` record shape (a deliberate §B deviation, recorded)
and the wire file's cast retired; the sample-prompt catalogue's re-vendor hazard
added to the drift ledger's §1 list; the `9c01fa99` ratification premise
correction recorded in the round record. Recorded, not changed: a malformed
JSON body folds to `{}` → Zod 400 where v4's `req.json()` throws 500 (the
`subprompts_routes.rs` precedent — a pre-existing class); `find_built_in` has no
production caller yet (the `.qtap` exporter is its named entry). Lane record:
`status-log.md` → "Lane record — P4.83".

## Preamble

- **v4 oracle baseline: `2f4254b42`** (drift-ledger §1); **zero drift**.
  This lane closes the `p4.9k` round's recorded divergence "ImportModal's
  catalogue is always empty — the missing `promptTemplateList` verb"
  (P4.9K3's lane record; `import-modal.ts`'s header; both hosts show v4's
  own "No templates available" copy today).
- **A ratification premise this lane corrects:** the `d123658d` round
  dispositioned v4 `9c01fa99` ("modernize sample prompts, add the MODERN
  trio") NO-PORT on the claim that "v5 consumes both plugin content and
  help markdown from the instance at runtime". For HELP that became true
  when `p4.9i2` vendored the tree; for the sample prompts it was never
  true — v5 has NO system-prompt plugin registry and NO seeding, so a
  v5-only instance has zero built-in templates and a shared instance lists
  whatever v4 seeded. This lane vendors the catalogue (the `help/**` /
  `builtin_templates` pattern) and records the correction in its lane
  record; the unifier adds the re-vendor hazard to the ledger's §1 list.
- **Freshness probe at lane start** (ledger §2): checkout on **`main`**,
  tree clean, both log ranges EMPTY. **Any mismatch: STOP and report.**
- **Regen rule: NO PIN REQUIRED** — regens from the live checkout after a
  passing probe, through `recipe_sweep.py --run <family>`.
- **Sibling lanes:** P4.82 (the backfill job — `host.rs`/`spine.rs`/
  `queue_service.rs`; shares `api/types.rs`, `engine.rs`, `core/lib.rs`,
  `services/mod.rs`, `web/lib.rs` with you BY FENCE), P4.84 (the other SPA
  lane — `generators/wizard/generation-step.ts`, `optimizer/**`,
  `ai-import/**`, the Salon, `details-tab.ts`; NEVER your three SPA files),
  P4.85, P4.86 (Rust only).
- **Worktree note:** own worktree + lane branch
  (`claude/p4-83-prompt-templates-<hash>` from `main`);
  `CARGO_INCREMENTAL=0`; `npm ci`; ONE Playwright at a time — run only your
  own spec file by name.

## Mandate

Land v4's prompt-templates listing end to end so the Import-from-Template
modal on the character EDIT host and the NEW-character host shows what v4
shows: the 21 built-in "Sample Prompts" plus the user's own templates.
Server: `find_all_for_user` / `find_built_in` on the existing
`db/prompt_templates.rs` repo, the LAZY built-in seeding at v4's own site
(first listing, insert-if-absent by `(name, isBuiltIn)`, NEVER an update —
the modernized `9c01fa99` content never reaches an instance that already
holds the row, and neither must v5's), the `promptTemplateList` verb
answering v4's `{ templates, count }` body, and `GET /api/v1/prompt-
templates` — over a real-DB routes family whose fixture is a clean
provisioned instance. Tier 2 finishes the family (`POST` create + the
`[id]` GET/PUT/DELETE with the built-in refusals). SPA: the fetch-on-open on
both hosts with v4's two DIFFERENT open semantics, and a live e2e beat that
imports a built-in. What this lane must NOT touch: the roleplay-templates
seeder (a different table with different semantics), `core-contract.ts`
(frozen — declare the DTOs in your own wire file), any P4.84 SPA file, any
other lane's Rust file.

## Survey-verified starting points (2026-09-07 @ v4 `2f4254b42` — do not re-derive)

### The route (`app/api/v1/prompt-templates/route.ts`, 66 lines)

- `GET` → `repos.promptTemplates.findAllForUser(user.id)` →
  `NextResponse.json({ templates, count: templates.length })` (200; the
  `templates` element shape is the FULL row: `id, userId, name, content,
  description, isBuiltIn, category, modelHint, tags, createdAt, updatedAt`
  in the Zod schema's order — `lib/schemas/template.types.ts:270-282`;
  `userId` is `null` for built-ins; `tags` an array).
- `POST` → `createTemplateSchema.parse(body)` (`name` string 1..100,
  `content` string min 1, `description` string max 500 optional,
  `category` / `modelHint` string optional — Zod 4.5 code-point lengths);
  `repos.promptTemplates.create({ userId, name, content, description:
  validatedData.description || null, isBuiltIn: false, category: … ||
  null, modelHint: … || null, tags: [] })`; `info` `[Prompt Templates v1]
  Template created` `{templateId, userId, name}`; `201 { template }`. A
  ZodError is the context handler's `validationError` 400 body (the
  P4.D71 shape — measure the exact `{error, details}` bytes on v4).
- `[id]/route.ts`: `GET` (`findById` → 404 — measure the sentence), `PUT`
  (`:53-109`: `findById` 404; `existingTemplate.isBuiltIn` → a refusal at
  `:69` — measure status + body), `DELETE` (`:111-…`: the same refusal at
  `:124`). The repo's `update`/`delete` ALREADY refuse built-ins in v5
  (`db/prompt_templates.rs` module doc) — the route's refusal sits ABOVE
  the repo's; measure whether v4 answers the route's sentence or the repo's
  silent `null`/`false` for a built-in (they are two guards; the route's
  fires first).

### The repository (`lib/database/repositories/prompt-templates.repository.ts`)

- `findAllForUser(userId)` (`:221-236`): `await this.seedSamplePrompts()`
  THEN `findByFilter({ $or: [{isBuiltIn: true}, {userId}] })`, under
  `safeQuery` (a throw → `[]`, logged). `findBuiltIn()` (`:204-217`): seed
  then `findByFilter({isBuiltIn: true})`. **Measure the row ORDER
  `findByFilter` returns** (the collection layer's default — rowid /
  insertion order, or an `ORDER BY`?) — the modal lists in that order.
- `seedSamplePrompts()` (`:34-47`): single-flight (`seedingPromise`) →
  `_doSeedSamplePrompts()` (`:49-148`): `registryPrompts =
  systemPromptRegistry.isInitialized() ? systemPromptRegistry.getAll() :
  []`; if non-empty: for each prompt, `collection.findOne({ name:
  prompt.name, isBuiltIn: true })`; ABSENT → insert `{ id: generateId(),
  userId: null, name, content, description: \`${category} prompt optimized
  for ${modelHint} models\`, isBuiltIn: true, category, modelHint, tags:
  [], createdAt: now, updatedAt: now }` through `this.validate(...)` (Zod)
  and `info` `Sample prompt template seeded from plugin` `{templateId,
  name, promptId, modelHint, category}`; PRESENT → nothing (no update, no
  log). Registry EMPTY → the deprecated filesystem fallback
  (`lib/prompts/sample-prompts-loader.ts`, `prompts/` dir under
  `process.cwd()`; absent on installs → `[]`, `warn` `Prompts directory
  does not exist`).
- The `promptId` in the seed log is the registry id
  (`default-system-prompts/<NAME>`); the description string is byte-exact
  `${category} prompt optimized for ${modelHint} models`.

### The catalogue (`plugins/dist/qtap-plugin-default-system-prompts/`)

- `index.ts` `loadPrompts()`: `readdirSync(prompts/).filter(.md).sort()`
  (JS default sort — UTF-16 code-unit order); `name = basename without
  .md`; `parsePromptFilename`: split on `_`, LAST part = `category`, the
  rest joined by `_` = `modelHint` (`< 2` parts → `{modelHint: baseName,
  category: 'GENERAL'}`); `content = readFileSync(utf-8)` verbatim.
  Plugin `version 1.1.0`, `pluginId default-system-prompts`.
- 21 files (sorted): `CLAUDE_COMPANION`, `CLAUDE_ROMANTIC`,
  `DEEPSEEK_COMPANION`, `DEEPSEEK_ROMANTIC`, `GEMINI_COMPANION`,
  `GEMINI_ROMANTIC`, `GENERIC_COMPANION`, `GENERIC_ROMANTIC`,
  `GPT4O_COMPANION`, `GPT4O_ROMANTIC`, `GPT5_COMPANION`, `GPT5_ROMANTIC`,
  `GROK_COMPANION`, `GROK_ROMANTIC`, `MISTRAL_COMPANION`,
  `MISTRAL_ROMANTIC`, `MODERN_GENERAL`, `MODERN_PLATONIC`,
  `MODERN_ROMANTIC`, `OLLAMA_COMPANION`, `OLLAMA_ROMANTIC`. Seed order =
  this order.
- Vendor via a generator (`harness/oracle/provision/dump-prompt-templates.ts`
  driving the REAL plugin module's `loadPrompts` — import the built
  `index.js` or the registry after `initializePlugins` — emitting
  `builtin_prompt_templates.json` with `name/content/modelHint/category/
  promptId` in seed order; the `dump-builtin-templates.ts` +
  `byte-exact-static-data-transcription` pattern: ship the generator, add a
  byte-identity guard test that re-reads the 21 `.md` files from the v4
  checkout and compares).

### The SPA (v4 → v5)

- Editor host: `useSystemPrompts.ts:265-268` `openImportModal = () => {
  fetchTemplates(); setShowImportModal(true) }` — ALWAYS refetches;
  `fetchTemplates` (`:92-107`) sets `loadingTemplates` true, `fetch('/api/
  v1/prompt-templates')`, `res.ok` → `setTemplates(data.templates || [])`,
  a throw → `console.error('Error fetching templates', …)` with templates
  UNCHANGED, `finally` loading false. v5 `edit/system-prompts-tab.ts:158`
  mounts `<qt-character-prompt-import-modal>`.
- New-character host: `NewCharacterView.tsx:93-108` `openTemplateImport`:
  open FIRST, then fetch ONLY `if (templates.length === 0)` (the same
  loading/`|| []`/console.error shape). v5 `new/new-character.ts:297`.
- The modal (`ImportModal.tsx`, 113 lines) is already ported 1:1
  (`import-modal.ts`, its `PromptTemplate` interface = v4's
  `types.ts:14-22`); `onImport(template.content, template.name)`.
- v4 has no jest suite for these hooks (measure `__tests__` — none found
  at survey); parity specs pin the two open semantics + the failure arm.

### The v5 side today

- `db/prompt_templates.rs`: `create` / `update` / `delete` / `find_by_name`
  over `PromptTemplatesRepository<'c>`; the built-in read-only guard; NO
  `find_all_for_user`, NO `find_built_in`, NO `find_by_id`, NO seeding (its
  header says seeding "is a startup concern … out of scope" — v4's site is
  the READ, lazy, not startup: reproduce v4's site).
- No `promptTemplate*` verb in `api/types.rs`; no REST edge in
  `quilltap-web`; the `.qtap` importer/exporter and the settings
  import/export type `'prompt-templates'` already read/write the table.
- DDL (`fresh_schema.json`): `id TEXT PK, userId TEXT, name TEXT NOT NULL,
  content TEXT NOT NULL, description TEXT, isBuiltIn INTEGER DEFAULT 0,
  category TEXT, modelHint TEXT, tags TEXT DEFAULT '[]', createdAt,
  updatedAt` — `isBuiltIn` marshals 0/1 → JSON boolean, `tags` JSON text →
  array.
- `services/builtin_templates.rs` is the ROLEPLAY seeder (every boot,
  updates in place) — a different contract; do not reuse its update path.

## Tiered deliverables

### Tier 1 — must land

1. **Repo reads:** `find_all_for_user(conn, user_id)`, `find_built_in(conn)`,
   `find_by_id(conn, id)` marshalling the full row (JSON key order = v4's
   Zod schema order; `userId` null for built-ins; `tags` parsed).
2. **The seeding:** `services::builtin_prompt_templates::seed_sample_prompts
   (conn, now_iso, mint_id)` — insert-if-absent by `(name, isBuiltIn=1)`
   through the repo's validating `create`, NEVER update, the seed `info`
   line per inserted row (v4's field bag), the vendored
   `builtin_prompt_templates.json` + generator + byte-identity guard.
   Called at v4's site: inside `find_all_for_user` / `find_built_in`'s verb
   path BEFORE the read (the writer — the read verb takes the writer once
   when the seed has rows to insert; measure the cheapest shape that keeps
   the read on the pool when nothing is missing).
3. **`promptTemplateList` verb** → `Response::PromptTemplates({ templates,
   count })` (fenced in `api/types.rs`/`engine.rs`) and NEW
   `crates/quilltap-web/src/prompt_templates_routes.rs` serving `GET
   /api/v1/prompt-templates` (route line fenced in `web/lib.rs`) with the
   exact body bytes.
4. **SPA:** `prompts-editor/prompt-templates.api.ts` (the DTO verbatim from
   §B below + the client), the fetch-on-open in BOTH hosts with v4's two
   semantics (editor: always refetch; new-character: only when empty),
   `loading` during, `templates` unchanged on failure + the console error;
   parity specs for each arm; the e2e beat (below).
5. **Differential:** `prompt_templates_routes_equivalence` (below).

### Tier 2 — should land

6. `POST /api/v1/prompt-templates` (`promptTemplateCreate`: the Zod arms
   with v4's 400 bodies + the 201 body + the info line) and the `[id]`
   trio (`promptTemplateGet` / `Update` / `Delete`) with the route-level
   built-in refusals — arms in the same family; the SPA does not consume
   them this round (the ImportModal only lists), so no client work.
7. The list ORDER pinned by a case with interleaved built-in and user
   rows (whatever v4's `findByFilter` order measures as).

### Tier 3 — explicit refusal-arm deferrals (loud, typed, never silent)

- The filesystem `prompts/` fallback — NO-COUNTERPART (deprecated in v4,
  absent on installs); recorded.
- The registry-EMPTY arm (a v4 install with the plugin missing seeds
  nothing; v5 always holds the vendored table) — a RECORDED divergence in
  v5's favour, pinned by a note, not a tripwire.
- The `systemPromptRegistry` itself (plugin loading, `getAll`, the
  `default-system-prompts/<NAME>` ids) — not ported; the vendored table
  stands in; the `promptId` field of the seed log carries the SAME id
  string, computed.

## The differential requirement (restated)

- **`prompt_templates_routes_equivalence` (tier 2, real DB, real HTTP
  handlers):** a jest oracle driving v4's REAL `GET`/`POST`/`[id]` handlers
  (the `settings_routes` / `settings_wire_actions` precedent — v4's
  `createContextHandler` over a fixture the recipe builds from a CLEAN
  provisioned instance in `/tmp`, so the first list SEEDS) with
  `systemPromptRegistry` REAL (`initializePlugins` over the dist plugins,
  as `character-optimizer-tier3.test.ts` initializes the provider
  registry — measure the init call for the system-prompt registry) and
  `Date` + `generateId` pinned (the seeded ids/timestamps are minted on
  both sides → the shared-cross-db-id-map remap + timestamp placeholders,
  OR pin them through the repo's `CreateOptions` on v5 and a mocked
  `crypto.randomUUID` on v4 — pick the shape the family's normalizer
  already supports and record). Cases: first list (21 seeded — the whole
  body bytes after remap; the 21 seed log lines); second list (nothing
  seeded, same body); a user template present (both rows, the order);
  a PRE-EXISTING built-in with OLD content (stays — the never-update pin,
  mutation-proven by making v5 update); the Tier-2 arms (create 201, the
  Zod 400s incl. a 101-code-point astral name, get 404, PUT/DELETE
  built-in refusals). Comparands: status, body bytes (remapped), the
  `prompt_templates` table dump, the captured log lines.
- **The vendored table:** `builtin_prompt_templates_guard` re-reads the 21
  `.md` files + `parsePromptFilename` from the v4 checkout and asserts the
  JSON byte-identical (the `help_tree_embed_guard` shape) — this is the
  tripwire that fires when v4 changes a prompt (a re-vendor obligation).
- Recipe header per `recipe-header-conventions`; registered in the sweep
  driver's table.

## Fixtures

- The family builds its own pair in `/tmp` inside the recipe from a clean
  provisioned instance (v5's provisioning + v4's real repos for the user /
  the user template / the OLD-content built-in plant); commits NOTHING
  under `fixtures/`.
- The e2e beat runs on the shared Playwright fixture (`e2e/support`) —
  measure whether it already carries built-in rows (v4-seeded fixtures do
  not; the first open of the modal on a fresh v5 instance SEEDS, which is
  itself the beat's proof): open Import Template on the EDIT host → the
  "Sample Prompts" section lists the 21 names → click `MODERN_GENERAL` →
  the prompt form holds its content + suggested name; then on the NEW
  host with the same list.

## §B — the prompt-templates wire contract (BINDING — this lane's only new verb family; declared verbatim in `prompts-editor/prompt-templates.api.ts`, folded into `core-contract.ts` by the unifier)

```ts
/** v4 `GET /api/v1/prompt-templates` → `{ templates, count }`. */
export interface PromptTemplateRecord {
  id: string;
  userId: string | null;      // null for built-ins
  name: string;
  content: string;
  description: string | null;
  isBuiltIn: boolean;
  category: string | null;
  modelHint: string | null;
  tags: string[];
  createdAt: string;
  updatedAt: string;
}
export interface PromptTemplateListRequest  { type: 'promptTemplateList' }
export interface PromptTemplateListResponse { type: 'promptTemplates'; data: { templates: PromptTemplateRecord[]; count: number } }
// Tier 2 (server-only this round; no SPA consumer):
//   { type: 'promptTemplateCreate', name, content, description?, category?, modelHint? } → { type: 'promptTemplate', data: { template } }  (201 on the edge)
//   { type: 'promptTemplateGet',    id }                                                 → { type: 'promptTemplate', data: { template } }
//   { type: 'promptTemplateUpdate', id, ...patch }                                        → { type: 'promptTemplate', data: { template } }
//   { type: 'promptTemplateDelete', id }                                                 → { type: 'promptTemplateDeleted', data: { success: true } }
```

The Rust `Request`/`Response` variants carry the same spellings
(`PromptTemplateList` → `Response::PromptTemplates`, …). Nothing invents a
field v4's body does not carry.

## §R — the round-wide meeting points (BINDING — identical in every order of this round)

Round: **the generator follow-ups + prompt-templates round** — P4.82 ∥ P4.83
∥ P4.84 ∥ P4.85 ∥ P4.86 (P4.86 CONDITIONAL on the human's dependency
ruling — see its preamble). Ordered 2026-09-07. The first non-drift round
since the follow-ups round 2 (2026-09-04): nothing to absorb, every lane is
banked follow-up work from the `p4.9k` and `2f4254b42` rounds.

1. **Baseline and drift.** Oracle baseline **`2f4254b42`** (drift-ledger §1,
   "feat: character subprompts", `4.10.0-dev.1`). v4 `main` HEAD **AT the
   baseline** and v4 `bugfix` tip **`1a2b2164c`** unmoved; the ledger's §3 is
   EMPTY — **zero drift**. No ledger row is ordered by this round.
2. **The freshness probe at lane start** (ledger §2), expected values:
   checkout on **`main`**, tree CLEAN, `git log --oneline 2f4254b42..main`
   EMPTY, `git log --oneline 1a2b2164c..bugfix` EMPTY. **Any mismatch: STOP
   and report** — a lane never writes the ledger and never builds its own
   pin; the planner re-points the round. Re-run the probe before EVERY regen
   batch (the checkout has gone dirty and moved MID-LANE before — ledger
   §5.1; a lane that discovers drift mid-way finishes nothing that depends
   on a regen and reports).
3. **Regen rule: NO PIN REQUIRED** (ledger §1) — every regen runs from the
   live `~/source/quilltap-server` checkout after a passing probe, through
   `harness/tools/recipe_sweep.py --run <family>` (the sanctioned path; it
   refuses empty-stage families and unknown names); never two sweeps
   concurrently. Committed recipe headers keep saying `cd
   ~/source/quilltap-server` and never name a `/tmp` pin
   (`recipe-header-conventions`). The oracle `node_modules` are the LIVE
   tree (`zod` 4.5.4 — `zod_version_guard` fails the gate if it moves; every
   Zod length rule transcribed this round uses the `jsstr::zod_len_*`
   code-point helpers, never `utf16_len`). Check every NDJSON is non-empty
   before believing a diff (the empty-file trap; a family's env var is not
   its regen var — `QT_ORACLE_<FAMILY>` reads it, `QT_ORACLE_OUT` writes it).
4. **Branching.** Every lane branches from `main` into its own worktree:
   `git worktree add <path> -b claude/p4-8N-<slug>-<hash> main`;
   `CARGO_INCREMENTAL=0`; `npm ci` (never `cp -a`) for the SPA lanes. No
   stacking this round. P4.86 does not start unless the human has granted
   the dependency ruling its preamble names.
5. **Fenced-append shared files.** `crates/quilltap-core/src/api/types.rs`,
   `crates/quilltap-core/src/api/engine.rs`, `crates/quilltap-core/src/lib.rs`
   (module lines), `crates/quilltap-core/src/services/mod.rs`,
   `crates/quilltap-core/src/generators/mod.rs`,
   `crates/quilltap-core/src/services/queue_service.rs`,
   `crates/quilltap-host/src/host.rs`, `crates/quilltap-host/src/spine.rs`,
   `crates/quilltap-web/src/lib.rs` (module + route lines): each Rust lane
   appends ONLY inside its own fence (`// === P4.82 ===` … `// === end P4.82
   ===`, opened once per file, new variants/arms/registrations at the END of
   the relevant block, never interleaved). `crates/quilltap-web/src/
   characters_routes.rs` and `system_data_routes.rs` are touched by NOBODY
   this round (P4.85's wire tests live in `crates/quilltap-web/tests/`).
   `Cargo.toml`/`Cargo.lock` at the workspace root and in every crate:
   version lines only — **except P4.86, the ONE lane authorized to add a
   dependency (`jsonschema`), and only once the human's ruling is recorded
   in this round's report.** Any other dependency add is a STOP-and-flag.
6. **`apps/web/**` is split between TWO lanes by file:** P4.83 owns
   `screens/characters/generators/prompts-editor/import-modal.ts`, its NEW
   `prompts-editor/prompt-templates.api.ts`, `screens/characters/edit/
   system-prompts-tab.ts`, `screens/characters/new/new-character.ts` and
   its NEW e2e spec; P4.84 owns `screens/characters/generators/wizard/
   generation-step.ts` (+ its NEW preview-markdown module), `generators/
   optimizer/**`, `generators/ai-import/**`, `chat/cast/add-character-
   dialog.ts`, `screens/salon/salon-conversation.ts`, `screens/characters/
   view/tabs/details-tab.ts`, `ui/prompt-field-label.ts` (+ a NEW
   `ui/prompt-field-example.ts` if the example line is extracted) with
   `screens/characters/edit/scenario-editor.ts` ONLY to adopt that
   extraction, `workspace/chrome/link-interceptor.spec.ts` (spec only), and
   the three existing generator e2e specs. `apps/web/src/app/core/
   core-contract.ts` is FROZEN: P4.83 declares its DTO in its own wire file
   verbatim from its §B and the unifier folds it (the name-for-name diff).
   The Rust-only lanes (P4.82, P4.85, P4.86) finish with `git diff main --
   apps/web/` EMPTY.
7. **Frozen files.** `docs/developer/porting/drift-ledger.md` (lanes never
   write it); `help/**`; `apps/web/src/app/shell/shell.ts` and
   `apps/web/src/app/workspace/workspace-contract.ts` (no new tab kinds).
   `crates/quilltap-web/tests/fixtures/character-generators-{main,mount}.db`
   is READ by P4.82 (if it reuses it), P4.85 and P4.86 and REGENERATED BY
   NOBODY.
8. **Version bumps.** P4.82: `quilltap-core`, `quilltap-harness`,
   `quilltap-host`. P4.83: `quilltap-core`, `quilltap-harness`,
   `quilltap-web`, `apps/web/package.json`. P4.84: `apps/web/package.json`.
   P4.85: `quilltap-core`, `quilltap-harness`, `quilltap-web`. P4.86:
   `quilltap-core`, `quilltap-harness` (+ the root `Cargo.lock` for the
   dependency). Per commit. The unifier RECOUNTS as base + total bumps —
   identical bumps merge silently (`commit-checklist`; the playbook's
   silent-auto-merge trap), and a `Cargo.toml` conflict is NOT version-only
   when P4.86 is in the round (the `p4.9i2` §3 catch: a `--ours` resolution
   dropped a feature block).
9. **e2e.** Port 4319 is repo-wide — ONE Playwright at a time across all
   lanes. P4.83 and P4.84 each run ONLY their own spec files by name (never
   the full suite — that is the unifier's), and never kill a server they did
   not start (`pkill -f` can kill a sibling lane's gate; read `pgrep -fl`
   argv first). A held port means WAIT, not force. A beat's mock must speak
   the runner's transport (the optimizer and AI-import runners call the
   NON-streaming `send_message`; the shared `support/mock-llm.ts` speaks
   only SSE — see the spec headers' recipes).
10. **Append-only for everyone:** `docs/CHANGELOG.md`,
    `docs/developer/porting/status-log.md`.
11. **Fixture ownership.** P4.82 delivers a NEW committed
    `crates/quilltap-harness/fixtures/headshoulders-{main,mount}.db` pair (+
    its builder under `harness/oracle/fixtures/`, v4's REAL repositories,
    pinned ids) unless it can prove an existing pair carries every shape it
    needs (record the measurement either way). P4.83 builds its fixture in
    `/tmp` from a clean provisioned instance inside its recipe (the
    `settings_wire_actions` precedent) and commits NOTHING under
    `fixtures/`. P4.85 and P4.86 EXTEND the four generator tier-3 oracle
    cases (three P4.85's, the AI-import one P4.86's) — the unifier
    regenerates all four ONCE at unification after both lanes' edits are in
    (the P4.D119 rule). P4.84 seeds through the running server's verbs,
    never SQL.
12. **Census guards move when a verb is added.** P4.83 adds dispatch verbs;
    adding any verb, or closing a recorded divergence (P4.86 retires
    `VALIDATION_UNAVAILABLE`'s both-directions pins), moves the executable
    censuses in OTHER crates (`a-new-verb-moves-the-dispatch-wrong-type-
    census`, `closing-a-divergence-moves-the-censuses`) — every Rust lane's
    gate is `cargo test --workspace`, never `-p`, with the full log captured
    to a file + a sentinel (never `| tail`, never a `pgrep` poll loop).
13. **Log-line pins** use the thread-scoped capture seam (`a-process-global-
    test-seam-must-be-thread-scoped`); a `%x` field renders unquoted, a bare
    or `?x` field quoted (`tracing-percent-field-renders-unquoted`) — read
    v4's `logger.<level>(msg, bag)` bag and pin the LEVEL, the sentence and
    the field NAMES.

## Ownership (BINDING — identical table in every order of this round)

| Lane | Owns (edit freely) | Fenced-append (§R.5) | Must not touch |
| --- | --- | --- | --- |
| **P4.82** | NEW `crates/quilltap-core/src/services/headshoulders_backfill_job.rs`; NEW `crates/quilltap-core/src/services/headshoulders_backfill_enqueue.rs`; `crates/quilltap-core/src/generators/wizard.rs` ONLY for the `generate_field` / `WizardCallCtx` visibility edit (one fenced block, no behaviour change); NEW harness families `headshoulders_backfill_tier3_equivalence` + `headshoulders_backfill_enqueue_equivalence` + their oracle cases + the fixture builder + the committed pair; NEW host tests under `crates/quilltap-host/tests/` | `services/queue_service.rs`, `services/mod.rs`, `host/host.rs`, `host/spine.rs` | `api/**`, `apps/web/**`, every other generator file |
| **P4.83** | `crates/quilltap-core/src/db/prompt_templates.rs`; NEW `crates/quilltap-core/src/api/prompt_templates.rs`; NEW `crates/quilltap-core/src/services/builtin_prompt_templates.rs` + NEW `crates/quilltap-core/src/services/builtin_prompt_templates.json`; NEW `harness/oracle/provision/dump-prompt-templates.ts`; NEW `crates/quilltap-web/src/prompt_templates_routes.rs` (+ its wire test); NEW harness family `prompt_templates_routes_equivalence` + oracle case; SPA: `prompts-editor/import-modal.ts`, NEW `prompts-editor/prompt-templates.api.ts`, `edit/system-prompts-tab.ts`, `new/new-character.ts`, NEW `e2e/prompt-templates-flow.spec.ts` | `api/types.rs`, `api/engine.rs`, `core/lib.rs`, `services/mod.rs`, `web/lib.rs` | `core-contract.ts` (frozen), every P4.84 SPA file, every other Rust file |
| **P4.84** | SPA only: `generators/wizard/generation-step.ts` + NEW `generators/wizard/generation-preview-markdown.ts`; `generators/optimizer/**`; `generators/ai-import/**`; `chat/cast/add-character-dialog.ts`; `screens/salon/salon-conversation.ts`; `screens/characters/view/tabs/details-tab.ts`; `ui/prompt-field-label.ts` (+ NEW `ui/prompt-field-example.ts`) and `edit/scenario-editor.ts` ONLY to adopt the extraction; `workspace/chrome/link-interceptor.spec.ts`; the existing `e2e/character-wizard-flow.spec.ts`, `e2e/character-optimizer-flow.spec.ts`, `e2e/ai-import-flow.spec.ts` | — | every Rust file; `import-modal.ts`, `system-prompts-tab.ts`, `new-character.ts` (P4.83's); `core-contract.ts` |
| **P4.85** | `crates/quilltap-core/src/api/salon.rs`; `crates/quilltap-core/src/api/generators_detail.rs`; `crates/quilltap-core/src/api/image_profiles.rs`; `crates/quilltap-web/src/generator_sse.rs`; `crates/quilltap-web/tests/generator_sse_wire.rs` + `crates/quilltap-web/tests/characters_generators_routes.rs`; harness `character_optimizer_tier3_equivalence.rs`, `character_wizard_tier3_equivalence.rs`, `external_prompt_tier3_equivalence.rs` + their oracle cases (`character-optimizer-tier3.test.ts`, `character-wizard-tier3.test.ts`, `external-prompt-tier3.test.ts`) + their per-family fixture JSONs; the family that covers `image-profiles?action=generate` (+ a new astral-prompt arm) | — | `api/generators_wizard.rs`, `generators/ai_import.rs`, `ai_import_tier3_equivalence.rs`, `ai-import-tier3.test.ts` (P4.86's); `apps/web/**` |
| **P4.86** (conditional) | `crates/quilltap-core/Cargo.toml` (the ONE `jsonschema` dependency line) + the root `Cargo.lock`; NEW `crates/quilltap-core/src/generators/qtap_schema.rs` + NEW vendored `crates/quilltap-core/src/generators/qtap-export.schema.json`; `crates/quilltap-core/src/generators/ai_import.rs`; `crates/quilltap-core/src/api/generators_wizard.rs`; harness `ai_import_tier3_equivalence.rs` + `harness/oracle/cases/ai-import-tier3.test.ts` + `harness/oracle/fixtures/ai-import-tier3.json`; NEW harness family `qtap_schema_validate_equivalence` + its oracle case + recorded corpus | `generators/mod.rs` | every other file; `apps/web/**` |

Everyone: `docs/CHANGELOG.md` + `status-log.md` append-only; the drift
ledger frozen; `core-contract.ts` / `shell.ts` / `workspace-contract.ts`
frozen; `characters_routes.rs` / `system_data_routes.rs` untouched.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; clippy both feature sets; `cargo build
   --workspace`; the release build.
2. `cargo test --workspace` (full log + sentinel; never `| tail`) with the
   family's oracle var; `SKIP:` zero for it; the by-name `--nocapture`
   run; the dispatch/web-edge censuses re-counted (a new verb moves them).
3. The family by NAME through `recipe_sweep.py --run
   prompt_templates_routes_equivalence` after a passing probe; `--show`
   the header; the vendored-table guard green against the live checkout.
4. Mutation proofs: the seed made to UPDATE a present row (the OLD-content
   case reddens); the `isBuiltIn` filter dropped from the seed lookup (a
   user template named like a built-in makes the seed skip — reddens);
   the `count` hard-coded; the editor's always-refetch replaced by the
   new-host's empty-only guard (the parity spec reddens); the vendored
   JSON hand-edited (the guard reddens).
5. `npm run lint` (incl. `check-qt-classes`); `npm test`; `npm run build`;
   `e2e/prompt-templates-flow.spec.ts` by name against release binaries
   (ONE Playwright at a time — wait for port 4319, never kill).
6. Commits per `.claude/commands/commit.md` (core + harness + web +
   `apps/web/package.json`; CHANGELOG every commit); the lane record
   appended to `status-log.md` (the `9c01fa99` premise correction + the
   re-vendor hazard for the unifier to add to the ledger; the measured
   list order; the built-in refusal shape); this order's status header
   updated.
