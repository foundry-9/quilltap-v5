# Work order P4.40 — the maintenance sweep: the two unregenerable tier-3 oracles, the job_runner tracing race, the recipe-rot tail, the three full-suite deflakes

> **STATUS: OPEN — not started.**

This is phase-4.md candidate 4 — the escalations the `f7f1a956` round
banked ("Escalated to a maintenance lane", status-log "Standing after
the round"), plus the deflake trio that round's gate observed. Zero
new ported surface: every item is harness hygiene, a recorded
pre-existing v5 gap, or test hardening. That is exactly why it can
run beside the two porting lanes — but the discipline still applies:
a repaired oracle is re-proven by regen + re-run BY NAME, and a
deflake never weakens an assertion.

**v4 baseline for this lane's regens: `f7f1a956` — PINNED.** v4 HEAD
is `3adefeba` (release-notes docs atop `7df7de8e`, the Taboo feature
sibling lane **P4.D50** is absorbing this same round); do NOT stop
for either, and do NOT regenerate anything at them. Regenerate from a
detached worktree pinned at `f7f1a956` (recipe
`oracle-regen-pinned-v4-worktree`). If v4 moves PAST `3adefeba`:
classify in the lane record; STOP only if a new commit lands on
`lib/chat/context-summary.ts`, `lib/memory/memory-processor*`, or
another file this lane's two families import.

**Drift-check at lane start**: `git log f7f1a956..HEAD --oneline` +
`git status --short` in `~/source/quilltap-server` — expect exactly
`44e2e4fe` + `7df7de8e` + `3adefeba` (all three classified in the
round-planned record; the third is release-notes docs, NO-PORT);
anything else gets classified before work starts.

**Siblings (same round):** the resumed P4.37 (Almanack server
remainder) and P4.D50 (the Taboo catch-up). See Ownership — this
lane's files are disjoint from both by construction; keep it that
way.

**Worktree note:** own branch, own checkout; no `git stash`/
`git worktree`.

## Mandate

Close the escalated debt: make `context_summary_service_tier3` and
`memory_processor_tier3` regenerable at the pinned baseline again
(the P4.36 stale-mock class — fix v4-SIDE oracle hygiene, never v5
behavior), fix the `failed_job_emits_a_tracing_event`
thread-local race without weakening what it proves, work the
recipe-rot tail down with the sweep driver, and give the three
full-suite-only Playwright intermittents the deflake-round
treatment. (The round plan originally carried the image-path
`durationMs` zeros as this lane's item 2 — that fix landed on main
as `a892b153` DURING round planning, unit pins included; it is NOT
this lane's, and `image_job_common.rs`/`generate_image.rs` are NOT
this lane's files.) Do NOT touch: any file in P4.37's or P4.D50's Ownership
lists; `api/types.rs` / `api/engine.rs` at all; any committed
fixture DB another family owns, except via the paired-artifact rule.

## Survey-verified starting points (2026-08-05)

1. **The two unregenerable oracles.** `harness/oracle/cases/
   context-summary-service-tier3.test.ts` and
   `memory-processor-tier3.test.ts` fail **v4-side** at `f7f1a956`
   with `no such table: llm_logs` (escalated by P4.D49; the P4.36
   stale-mock class — a jest-venue fixture/mock that starves a
   partition v4's code now writes: `0cde7fbc` added llm_logs writes
   with profile attribution on paths these services take). The fix
   shape is P4.36's (read its order + the
   `p4.36-context-summary-unmock-lane` memory): give the oracle
   venue the partition/mocks v4's REAL code needs — never mock the
   service itself back to silence. Consumers:
   `crates/quilltap-harness/tests/
   context_summary_service_tier3_equivalence.rs` and
   `memory_processor_tier3_equivalence.rs`. Done = both oracles
   regenerate cleanly at the `f7f1a956` pin (fresh-baseline marker
   grepped in the NDJSON, [[oracle-regen-silent-stale-pass]]) and
   both differentials run green BY NAME with `--nocapture`, zero
   SKIP. If the regenerated oracle moves a comparand, that is a
   FINDING (possible real v5 divergence hidden behind the stale
   mock — the P4.20/P4.36 precedent runs both ways); diagnose,
   don't paper.
2. **The job_runner tracing race.**
   `crates/quilltap-core/src/services/job_runner.rs:687`
   `failed_job_emits_a_tracing_event` flakes under the integrated
   suite (thread-local tracing capture vs. sibling tests). The
   playbook's OnceLock precedent applies: keep the durable effect
   as the assertion (the emitted event/row), scope the capture
   per-test (a dedicated subscriber guard) or relax exact counts to
   monotonic — never delete the test or gate it off.
3. **The recipe-rot tail.** P4.34 repaired the bulk and left the
   driver durable (`harness/tools/recipe_sweep.py --run-all
   --results`, `--self-test`; memories [[recipe-sweep-driver]] +
   [[harness-recipes-are-runnable]]). The `f7f1a956` round's
   records name the remaining debt (the D42 sweep's stale-red tail;
   P4.D49's recipe repairs may have left siblings). Run the sweep
   from the pin, classify every non-ok row, repair what's
   mechanical, and ESCALATE (don't fix) any row whose repair means
   changing a family a sibling lane owns this round — the
   settings-routes + prompt-transitive families are P4.D50's, the
   almanack families P4.37's; skip them by name and record the
   skip.
4. **The deflake trio** (this round's gate, all
   full-suite-only, each isolation-green): the two terminal-pane
   beats — `apps/web/e2e/salon-documents-flow.spec.ts` (stacking)
   and `workspace-flow.spec.ts` (terminal pop-out) — and the
   `salon-dialogs-flow.spec.ts` Rename Chat beat. The treatment is
   the deflake round's, exactly ([[deflake-followup-round]] +
   [[e2e-playwright-traps]]): reproduce deterministically with
   injected delays (the un-awaited page-initiated-refetch shape has
   explained every one so far), harden the beat, weaken NOTHING,
   and keep the deflake comments load-bearing. If one won't
   reproduce even with delays, record the attempt and leave the
   spec alone — a speculative "fix" of an unreproduced flake is
   noise.

## Tiered deliverables

- **Tier 1:** items 1 (both families), 2, and 4 (all three beats
  reproduced-or-recorded).
- **Tier 2:** item 3 (the sweep run + mechanical repairs + the
  escalation list).
- **Tier 3 (loud):** anything item 3 surfaces that needs a ruling or
  a sibling's file — named in the lane record and the order header,
  never quietly absorbed.

## The differential requirement, restated

This lane ports nothing, so its obligation inverts: every repaired
oracle must be shown to still be an oracle — regenerated fresh at
the `f7f1a956` pin with hard failure checks, fresh-baseline markers
grepped, and the consuming differential run BY NAME with
`--nocapture`, zero SKIP. Any comparand that moves under a repaired
mock is investigated as a possible masked divergence before the
family is declared green.

## Fixtures

No committed fixture changes expected. If item 1's repair requires
extending an oracle venue's DB (adding the llm-logs partition),
prefer the venue-local /tmp copy per the committed-fixture rule; if
a COMMITTED fixture must gain a partition, regenerate its paired
oracle in the SAME invocation (the P4.D49 paired-artifact rule now
applies to /tmp families too) and re-run every family over that
fixture by name.

## Ownership (BINDING)

THIS lane owns: `harness/oracle/cases/
context-summary-service-tier3.test.ts`,
`memory-processor-tier3.test.ts` (+ any venue-support files those
two need), `crates/quilltap-harness/tests/
context_summary_service_tier3_equivalence.rs`,
`memory_processor_tier3_equivalence.rs`,
`crates/quilltap-core/src/services/job_runner.rs` (the test module
only), `harness/tools/recipe_sweep.py` +
recipe headers it repairs (EXCEPT families owned by P4.37/P4.D50 —
skip and escalate), and the three named e2e specs. It must NOT
touch `api/types.rs`, `api/engine.rs`, any chat-spine or almanack
file, or `apps/web/src/**` (specs under `apps/web/e2e/` only).
`CHANGELOG` and `status-log.md` are append-only union-merge files.
Version bumps: bump every crate you touch per
`.claude/commands/commit.md`; the unifier recounts.

## Verification gate (before declaring done)

`cargo fmt --all --check`; `cargo clippy --workspace --all-targets
-- -D warnings` on BOTH feature sets; `cargo test --workspace
--no-fail-fast` with the lane's oracle env vars; the two repaired
families BY NAME with `--nocapture`, zero SKIP, over fresh pinned
oracles; the sweep's `--run-all --results` artifact committed-or-
recorded; for the deflakes, each hardened spec green in isolation
AND in one full Playwright run (background-run rule: launch with
`run_in_background` and wait for the completion notification —
never a pgrep loop). Per-commit rules:
`.claude/commands/commit.md`. Lane record appended to
`status-log.md` before handoff.
