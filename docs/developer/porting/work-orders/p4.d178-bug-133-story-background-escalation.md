# P4.D178 — Bug 133: a moderated chat's story background never escalates to the uncensored provider (the `cc65d6bfc` drift catch-up)

**Status:** OPEN (ordered 2026-09-10 — the `cc65d6bfc` bug-133 catch-up +
`78b381a96`-round remainders round). The unifier updates this header.

## Preamble

- **v4 oracle baseline: `78b381a96`.** This lane absorbs the ONE commit past
  it — **`cc65d6bfc`** ("Fix bug 133: a moderated chat's story background
  could escalate to the uncensored provider", 2026-09-09 15:51 -0500,
  `4.10.0-dev.22`) — the drift ledger's one §3 row, `ORDERED(P4.D178)`. The
  commit the previous round's ledger predicted from the human's dirty tree,
  pre-authorized as that round's probe exception, and left UNPROCESSED by
  design. Spec reads are the hunks and the post-commit files: `git show
  cc65d6bfc -- lib/background-jobs/handlers/story-background.ts lib/image-
  gen/appearance-resolution.ts lib/tools/handlers/image-generation-handler.
  ts help/dangerous-content.md` and `git show cc65d6bfc:<path>` — **never
  the commit message** (ledger §5.3; the message is accurate here but
  wider than the hunks in one place named in the survey: it says the
  re-craft "is deleted", and the hunks ALSO delete the `uncensoredLLMSelection
  ?? cheapLLMSelection` crafter choice and the step-9b re-enumeration that
  rode it — v5's `CandidRecraft` carries all three).
- **Run the drift ledger's §2 freshness probe at lane start** with the §R.2
  expected values. **Any mismatch: STOP and report.** Re-run before every
  regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** This lane's pins are
  **`/tmp/qt-v4-pin-p4d178-cc65d6bfc`** (every family this lane MOVES — the
  catch-up target) and **`/tmp/qt-v4-pin-p4d178-78b381a96`** (neutrality
  regens). The help re-vendor is a `cp` from the `cc65d6bfc` pin's
  `help/dangerous-content.md`, never a hand edit.
- **Sibling lanes:** P4.87 (the turn-manager + route-trail harness
  remainders) and P4.88 (the substrate + heal + gallery + logging
  remainders) — both from `main`, neither touches any file of yours (§R.5,
  the Ownership table). No stacking.
- **Worktree:** per the `carryout` skill; `git worktree add <path> -b
  claude/p4-d178-bug-133-<hash> main`; edit inside it
  (`edit-inside-the-worktree-not-main`); `CARGO_INCREMENTAL=0`.

## Mandate

Port bug 133 whole, so that on v5 exactly as on v4 at `cc65d6bfc`: (a) the
appearance sanitizer's fourth parameter answers "will THIS scene's dangerous
appearance text actually be generated by the uncensored image provider", not
"is such a profile configured" — story backgrounds pass `uncensoredImageTarget`
and the image tool passes `mode === 'AUTO_ROUTE' && Boolean(uncensoredImageProfileId)`,
so a moderated chat's raw appearance text is sanitized even when an uncensored
profile exists elsewhere in settings; (b) the story-background job's post-hoc
moderation reroute is barred for a chat the operator left moderated
(`rerouteAllowed = moderationRejection && isDangerousChat`) — a moderated
chat whose backdrop is refused FAILS the job with `Image generation failed:
<msg>` and produces no image; (c) the candid re-craft before the reroute is
DELETED (a flagged chat's prompt was crafted candidly up front and is resent
as-is) — the P4.D94 `RerouteRecraft` seam retires at source, because v4 now
re-crafts nowhere; (d) the two handlers' reroute-path log lines that v5 never
carried land with v4's bags (the story failure bag gaining `rerouteAllowed` +
`isDangerousChat`); and (e) `help/dangerous-content.md` is the tip's bytes.
The avatar path's behaviour does NOT change (v4's `character-avatar.ts` is
untouched by the commit: its reroute stays gated on the moderation error
alone). This lane does NOT touch the turn/route-trail/gallery/memory
surfaces, adds no verb, moves no schema, and edits no SPA file.

## Survey-verified starting points (fresh survey 2026-09-10 at `cc65d6bfc` — do not re-derive)

### (A) v4 at the tip — the four behaviour files

- **`lib/image-gen/appearance-resolution.ts`** — `sanitizeAppearancesIfNeeded`
  at `:378-386`; the fourth parameter is now `routesDangerousToUncensored:
  boolean` (was `hasUncensoredImageProvider`). The five-rule doc block at
  `:346-372` (rule 2 "chat is marked dangerous AND the scene routes
  uncensored → unchanged"; rule 4 "dangerous AND the scene routes uncensored
  → unchanged (it goes to a provider that accepts it, so accuracy beats a
  draped sheet)"; rule 5 "Otherwise → sanitize"; then the bug-133 paragraph
  explaining why existence is the wrong question). Rule 2 at `:393-396`
  (`isDangerousChat && routesDangerousToUncensored`); the classification
  log bag at `:427-433` carries `routesDangerousToUncensored` (renamed key);
  rule 4 at `:436-439` (`if (routesDangerousToUncensored) return
  appearances`). The function body is otherwise unchanged — a RENAME plus
  the callers' new derivations.
- **`lib/tools/handlers/image-generation-handler.ts`** — `:978-983`:
  `const routesDangerousToUncensored = dangerSettings.mode === 'AUTO_ROUTE'
  && Boolean(dangerSettings.uncensoredImageProfileId);` (the comment: under
  DETECT_ONLY a dangerous appearance stays on the moderated provider and
  must be sanitized). Passed at `:986-994`. `Boolean('')` is false — v5's
  existing `!s.is_empty()` derivation already matches that half.
- **`lib/background-jobs/handlers/story-background.ts`** — the sanitizer
  call at `:404-416` now passes `uncensoredImageTarget` (the existing
  `isDangerousChat && hasUncensoredImageProvider` local — unchanged) with
  the comment "Story backgrounds never route up front". The reroute block
  at `:684-728`: `const moderationRejection = isImageModerationError(error);
  const rerouteAllowed = moderationRejection && isDangerousChat; const
  reroute = rerouteAllowed ? await resolveUncensoredImageProfileForReroute
  (imageProfile.id, dangerSettings, job.userId) : null;` — then, if no
  reroute, `logger.error('[StoryBackground] Image generation failed', {
  context: 'background-jobs.story-background', jobId, error: errorMessage,
  moderationRejection, rerouteAllowed, isDangerousChat,
  hasUncensoredImageProvider }, error)` and `throw new Error(\`Image
  generation failed: ${errorMessage}\`)`; else `logger.info('[StoryBackground]
  Image provider rejected for content moderation, rerouting through
  Concierge uncensored profile', { context, jobId, originalProfileId,
  originalProvider, fallbackProfileId, fallbackProvider, originalError })`,
  and `const rerouteBasePrompt = finalPrompt!;` — the 46-line candid
  re-craft block (the `craftStoryBackgroundPrompt` call with
  `uncensoredImageTarget: true` under `uncensoredLLMSelection ??
  cheapLLMSelection`, its `appendMissingCharacterEnumerations` re-run, and
  its three log lines "Re-crafted prompt candidly…" / "…returned nothing,
  reusing the concealed prompt" / "…failed, reusing the concealed prompt")
  is DELETED. Everything after (`createImageProvider(reroute.profile.
  provider)`, the rebuild, the reroute attempt, the after-reroute failure)
  is unchanged.
- **`lib/background-jobs/handlers/character-avatar.ts`** — UNTOUCHED by
  the commit. Its reroute at `:273` is `isImageModerationError(error) ?
  await resolveUncensoredImageProfileForReroute(…) : null`; its failure log
  at `:278-286` `logger.error('[CharacterAvatar] Image generation failed',
  { …, moderationRejection: isImageModerationError(error),
  hasUncensoredImageProvider })`; its rerouting info at `:287`; its
  after-reroute failure at `:368` (`'[CharacterAvatar] Image generation
  failed (Concierge reroute also failed)'`). Read all three bags at the
  tip: **v5 carries NONE of these six log lines** (see (B)).
- **`help/dangerous-content.md`** — hunk `@@ -141,9 +141,13 @@`: the "same
  holds on a reroute" paragraph rewritten ("but only for a chat already
  marked … with the prompt exactly as drafted"), a NEW bold paragraph "**A
  moderated chat is never carried through that door.**" (with the Concierge
  "He has been spoken to." line), a NEW "If a moderated chat's backdrop is
  refused …" remedy paragraph, and the closing "Concealment applies as
  before to every chat that is not headed for the uncensored profile …"
  paragraph replacing "If no uncensored image profile is configured …".
  The file count stays **123** (an edit, not an addition) — no count
  literal moves (`help_tree_embed_guard.rs:30`, `host_help_docs_boot.rs`,
  `help_content.rs`).
- **The tests (the oracle's shape, not ours to run):**
  `__tests__/unit/image-gen/appearance-resolution.test.ts` renames its
  describe/it titles and adds `'should sanitize a moderated scene even when
  an uncensored profile exists elsewhere'` (moderated chat, routes=false,
  classification dangerous → `mockSanitizeAppearance` called,
  `wasSanitized: true`); NEW `__tests__/unit/lib/background-jobs/handlers/
  story-background-uncensored-target.test.ts` (124 lines): four
  `uncensoredImageTarget` cases (ordinary → false; dangerous + no profile
  → false; dangerous + profile → true; operator-Uncensored under global OFF
  → true via the REAL resolver, `source: 'chat-uncensored'`, mode forced
  AUTO_ROUTE, `scanImagePrompts` false), two sanitization-gate cases
  (moderated + profile configured → the fourth arg is FALSE; flagged +
  profile → TRUE), three reroute cases (moderated + moderation rejection
  → rejects `/Image generation failed/`, `resolveUncensoredImageProfileFor
  Reroute` NOT called, ONE craft with target false; dangerous + no profile
  → rejects; flagged + rejection → ONE craft made candidly, the reroute
  provider receives the CANDID prompt unchanged).

### (B) v5 on `main` (`e5ff4be0`) — where each hunk lands

- **`crates/quilltap-core/src/services/appearance_resolution.rs`** —
  `sanitize_appearances_if_needed` at `:326-341` (doc: "the FIVE-step gate
  IN ORDER"); the eighth parameter is `has_uncensored_image_provider: bool`;
  rule 2 at `:349-351`; the classification at `:353-373` through
  `classify_content`; rule 4 at `:376-379`; rule 5 from `:381`. Port: the
  parameter RENAME to `routes_dangerous_to_uncensored`, v4's new doc block
  carried as the why-comment, and — measure first — whether v5 emits v4's
  `[AppearanceResolution] … classified dangerous` info line with its bag;
  if it does, the bag key renames too.
- **`crates/quilltap-core/src/tools/generate_image.rs`** — the call site at
  `:1890-1907` derives `has_uncensored_image_provider` from the profile id
  ALONE (`.map(|s| !s.is_empty()).unwrap_or(false)`) and passes it. The
  mode string is on `danger_settings.mode` (`:1798` `!= "OFF"`, `:1823`
  `== "AUTO_ROUTE"`). Port: `routes_dangerous_to_uncensored =
  danger_settings.mode == "AUTO_ROUTE" && <the existing non-empty check>`
  with v4's comment. **⚠ Corpus-blind:** `image_generation_tier3`'s oracle
  keeps danger OFF and sends no messages (`harness/oracle/cases/image-
  generation.test.ts:256-257` — "so no classify/resolve/sanitize"), so this
  derivation is invisible to its family; the unit pin in Tier 1 item 5 is
  its proof, and Tier 2 names the widening.
- **`crates/quilltap-core/src/services/story_background_job.rs`** —
  `has_uncensored_image_provider` at `:369-374`, `uncensored_image_target`
  at `:379` (with the `[decd8ef9]` comment); the sanitizer call at
  `:541-552` passes `has_uncensored_image_provider` (v4 now passes
  `uncensoredImageTarget` — the one-token port, with v4's comment); the
  `CandidRecraft` struct + its `common::RerouteRecraft` impl at `:186-270`
  (the two `tracing::info!`/`warn!` lines at `:247` and `:258` — v4's
  deleted trio, all to go); the `recraft` construct at `:754-782`
  (`enabled: !uncensored_image_target`); the `generate_with_reroute` call
  at `:784-806` passing `&danger_settings.mode`,
  `danger_settings.uncensored_image_profile_id.as_deref()`, and `&recraft`.
  No `tracing` line anywhere for the reroute or its failure — v5's whole
  reroute path is SILENT (measured: `ggrep -n 'warn!\|info!\|error!'` on
  `image_job_common.rs` returns nothing; the story file's only nearby line
  is `:656`'s craft-failure warn).
- **`crates/quilltap-core/src/services/image_job_common.rs`** — the
  `RerouteRecraft` trait at `:278-287` + `NoRerouteRecraft` at `:289-296`;
  `generate_with_reroute` at `:304-330` (parameters `danger_mode: &str`,
  `uncensored_image_profile_id: Option<&str>`, …, `recraft: &R`);
  `reroute_or_fail` at `:431-470`: the gate is `is_image_moderation_error
  (&error.message)` → `resolve_uncensored_image_profile_for_reroute(conn,
  api_keys, &current_id, &mode, uncensored, &uid)` (the mode/AUTO_ROUTE
  check lives INSIDE the resolver — v4's shape too), `None` → `Err(format!
  ("{fail_prefix}: {msg}"))`; the recraft call at `:472-483`
  (`recraft.recraft(&reroute.profile.provider).await.unwrap_or_else(…)`).
  Port: the seam gains a per-caller `chat_reroute_allowed: bool` (the story
  passes `is_dangerous_chat`, the avatar passes `true` — v4's avatar gate
  is the moderation error alone), the `recraft` parameter + trait + no-op
  impl are DELETED (nothing in v4 re-crafts at the tip; a seam with one
  no-op implementor is dead code, and P4.D94's lane record already
  predicted this retirement path), and the handler-specific log lines
  arrive as `&'static str` tags or a small per-handler `RerouteLogNames`
  struct alongside the existing `log_context`/`reroute_log_context`
  literals — read v4's two handlers for the exact six bags. `is_image_
  moderation_error` is computed ONCE and threaded into the failure bag
  (v4's `moderationRejection` local).
- **`crates/quilltap-core/src/services/character_avatar_job.rs`** — the
  call at `:306-330` passes `&common::NoRerouteRecraft` with the
  `[decd8ef9]` comment; it drops that argument and gains `true` for the
  chat gate, plus its three log names. Behaviour unchanged;
  `avatar_job_tier3_equivalence` regen at the `cc65d6bfc` pin is expected
  BYTE-IDENTICAL to the baseline's (the neutrality proof — grep the
  `cannedImageFailure` rows to confirm the reroute cases still ran).
- **`help/dangerous-content.md`** at the repo root — v5's current copy is
  byte-identical to the `78b381a96` pin's (verify with `cmp` before the
  copy; if it is NOT, STOP — something else moved it). `crates/quilltap-
  host/build.rs` embeds `help/**` (→ host bump).
- **The families:** `story_background_job_tier3_equivalence` (15 chat cases
  in `harness/oracle/fixtures/story-background-job.json` `chats`; the
  danger set `danger_candid`, `danger_no_uncensored_image`,
  `moderation_recraft`, `moderation_recraft_fails`,
  `moderation_already_candid`, `uncensored_override_candid`; the oracle's
  completion mock at `story-background-job.test.ts:190-253` branches on
  the user prompt's TAIL — derive / resolve / craft (the craft branch reads
  the SYSTEM message's candid marker `'The target image provider accepts
  adult content'`) — and **THROWS `unexpected completion task` on anything
  else (`:245`)**; the image mock throws `content policy violation on this
  prompt` for `params.model === 'blocked-model'` (`:266`) — image profile
  `e5000000-…-0003` "Blocked Images"). `moderation_recraft` and
  `moderation_recraft_fails` are MODERATED chats (no `isDangerousChat`)
  with `characterIds: []` on the blocked profile — under bug 133 both now
  THROW with one craft and no second image (the re-craft arms they were
  built to exercise no longer exist); `moderation_already_candid` is the
  dangerous chat on the blocked profile — it still reroutes, now resending
  its already-candid prompt (its rows should be byte-identical: the prompt
  was candid before too). `image_generation_tier3_equivalence` (corpus
  blind, above). `avatar_job_tier3_equivalence` (has `blocked-model`
  reroute cases; neutrality). `help_tree_equivalence` + `help_tree_embed_
  guard` + the two other help families (`help_docs_*`) — regen at the tip
  pin after the re-vendor. **No harness family and no oracle case drives
  `sanitizeAppearancesIfNeeded` directly** (measured: zero hits for either
  spelling under `crates/quilltap-harness/tests` and `harness/oracle/
  cases`) — Tier 1 item 6 closes that.
- **The classify + sanitize prompt markers** (for the story oracle's new
  branches, Tier 1 item 7): v4's cheap-LLM classification sends `system:
  CLASSIFICATION_SYSTEM_PROMPT` + `user: 'Classify the following content:\n\n
  <content>'` (`lib/services/dangerous-content/gatekeeper.service.ts:388-
  391`) after a null moderation provider; v5 mirrors it at
  `services/dangerous_content/gatekeeper.rs:541-552`. The sanitize task is
  v4 `lib/memory/cheap-llm-tasks.ts` `sanitizeAppearance` (called from
  `appearance-resolution.ts:453`); v5 `services/image_scene_tasks.rs:501`
  `sanitize_appearance`. Read both prompt tails before writing the branch
  predicates. The story oracle must mock the moderation registry to null
  the way the avatar oracle does (`avatar-job.test.ts:24`) — check whether
  it already does (jest.setup may) before adding a classify branch, or the
  classification never reaches the completion mock.

## Tiered deliverables

### Tier 1 — must land, in this order

1. **RED-FIRST measurement.** Regenerate `story_background_job_tier3` from
   the `cc65d6bfc` pin against the UNEDITED v5 tree and record which rows
   red: expected `moderation_recraft` + `moderation_recraft_fails` (v4 now
   throws; v5 still reroutes + re-crafts), and NOTHING else. Then regen
   `avatar_job_tier3` and `image_generation_tier3` at the same pin —
   expected GREEN (neutral). Regen `help_tree_equivalence` at the tip pin —
   expected RED (the vendor is stale by one file). Record all four.
2. **The sanitizer rename** (`appearance_resolution.rs`): parameter →
   `routes_dangerous_to_uncensored`; v4's new doc block; the log-bag key if
   the line exists. Zero behaviour change inside the function (a rename) —
   say so in the commit.
3. **The two callers' derivations.** `generate_image.rs`: `mode ==
   "AUTO_ROUTE" && <non-empty id>`, v4's comment. `story_background_job.rs`:
   pass `uncensored_image_target` (v4's comment "Story backgrounds never
   route up front").
4. **The reroute gate + the re-craft deletion** (`image_job_common.rs` +
   both handlers): `moderation_rejection` computed once; `reroute_allowed =
   moderation_rejection && chat_reroute_allowed`; the resolver called only
   when allowed; `CandidRecraft`, `RerouteRecraft`, `NoRerouteRecraft` and
   the `recraft` parameter DELETED; `reroute_base_prompt = final_prompt`.
   The avatar passes `true`. The story-side locals the deleted block used
   and nothing else uses (`uncensored_selection` for the crafter choice?
   — measure; v4 keeps `uncensoredLLMSelection` for the appearance retry)
   are removed only if genuinely dead.
5. **The six reroute-path log lines** (the #103/#110 class — v4 announces,
   v5 was silent): per handler, the failure ERROR (`… Image generation
   failed` with v4's bag — the story bag `{context, jobId, error,
   moderationRejection, rerouteAllowed, isDangerousChat,
   hasUncensoredImageProvider}`; the avatar bag `{…, moderationRejection,
   hasUncensoredImageProvider}` — read `:278-286`), the rerouting INFO
   (`… Image provider rejected for content moderation, rerouting through
   Concierge uncensored profile` with the profile/provider quartet +
   `originalError`), and the after-reroute ERROR (read `:368` and the story
   twin). `target:` per the existing story/avatar tracing targets; `jobId`
   from the `job_id: Option<&str>` already threaded. Capture-layer tests
   (the thread-scoped rig — `a-process-global-test-seam-must-be-thread-
   scoped`; `capture-layer-target-assert-is-a-prefix-match`;
   `tracing-percent-field-renders-unquoted`): one per line asserting the
   bag's keys AND the values for the two bug-133 keys, plus a silence arm
   (a non-moderation failure emits the ERROR with `moderationRejection:
   false` and NO info line). **Plus a unit pin for `generate_image.rs`'s
   derivation** — three arms (DETECT_ONLY + profile → false; AUTO_ROUTE +
   profile → true; AUTO_ROUTE + empty-string profile → false) — the
   corpus-blind half's only proof until Tier 2.
6. **NEW `appearance_sanitize_gate_tier3_equivalence`** — the family the
   survey found missing. Oracle: a jest case importing v4's REAL
   `sanitizeAppearancesIfNeeded` (never a mock of it), with `@/lib/llm`'s
   `createLLMProvider` canned by the recording pattern (the story oracle's
   shape: record `provider|model|temperature|messages` → response) and the
   moderation registry null, over a JSON corpus enumerating mode {OFF,
   DETECT_ONLY, AUTO_ROUTE} × `isDangerousChat` {t,f} × `routesDangerousTo
   Uncensored` {t,f} × classification {safe, dangerous} (24 rows; the OFF
   rows short-circuit; a `customClassificationPrompt` row is a cheap extra
   arm). Comparands per row: the returned appearances (`wasSanitized`,
   `clothingDescription`/`physicalDescription`), the NUMBER of completion
   calls and their recorded keys (which proves WHICH rule fired — rule 2
   makes zero calls, rule 3-safe one, rule 5 two), and the `llm_logs` rows
   if v4's classification logs (`DANGER_CLASSIFICATION` — the family
   provisions a fresh instance for the `db` the Rust side takes). Rust:
   `sanitize_appearances_if_needed` with `CannedCompletionProvider` +
   `NoModerationProvider`. Mutation proofs: (a) rule 2's `&&` → `||`
   (reds the moderated-chat rows), (b) rule 4 inverted (reds the
   dangerous+routes rows), (c) rule 5's sanitize skipped (reds every
   sanitizing row) — each named in the lane record with the exact rows it
   reddened. Recipe header per `recipe-header-conventions` (`--show` it
   back; `V5W=${V5W:-…}`; no `/tmp` pin in the committed header).
7. **The story corpus's bug-133 arms** (`story-background-job.json` +
   the case file): (a) NEW `moderated_sanitize_profile_configured` — a
   MODERATED chat (no `isDangerousChat`, no override), `characterIds:
   [Fern]`, `dangerousContentSettings` AUTO_ROUTE with
   `uncensoredImageProfileId` set to the Uncensored Images profile, on the
   OpenAI profile; the sanitizer reaches rule 3, so the completion mock
   gains a `Classify the following content:` branch answering v4's
   dangerous-classification JSON (read the gatekeeper's parse for the exact
   keys — `isDangerous`/`score`/`categories`) and a sanitize-task branch
   answering a draped rewrite; pre-fix v5 returns unchanged at rule 4, v4
   sanitizes — the sanitized clothing then reaches the crafter's user
   prompt → the recorded craft key → the image key → both `llm_logs`
   projections, so the row diffs RED before the fix and GREEN after. (b)
   NEW `flagged_no_profile_moderation` — `isDangerousChat: true`, NO
   uncensored profile, the blocked profile → throws on both sides (v4's
   second reroute test). (c) The two flipped cases keep their labels (the
   `_recraft` name is now historical — add a `$comment` OUTSIDE the case
   map, `corpus-comment-inside-a-case-map-is-parsed-as-a-case`). Every
   pre-existing row other than the two flipped ones must be BYTE-IDENTICAL
   between the baseline-pin and tip-pin oracles — assert it in the lane
   record by diffing the two NDJSONs with the two labels excluded.
8. **The help re-vendor:** `cp` from the `cc65d6bfc` pin; `cmp` against
   `git show cc65d6bfc:help/dangerous-content.md`; regen the four help
   families at the tip pin (green); the count literals untouched (123).
9. **Ratify the riders** in the lane record: the two tests (oracle-side),
   `docs/CHANGELOG.md`, `docs/developer/bugs.md`, `bugs/fixed/bug-133-…md`
   (mirror = unifier), and the four version-bump files — NO-PORT with
   evidence, inside the same row (no ledger change; the unifier marks the
   row ABSORBED).

### Tier 2 — should land

10. **Widen `image_generation_tier3`** with ONE DETECT_ONLY case: danger
    mode DETECT_ONLY, an uncensored profile configured, a chat with one
    character and at least one message so `resolveAppearances` runs, the
    classify branch answering dangerous — pre-fix v5 skips the sanitizer
    (rule 4 on the old derivation), v4 sanitizes. Needs the classify/
    resolve/sanitize branches in the image-generation oracle's completion
    mock (its header at `:256` says why the corpus has none — that reason
    is now a blind spot with a live divergence behind it). If it does not
    land, record the blind spot LOUD in the family header and the lane
    record, naming item 5's unit pin as the standing proof.
11. **`llm_logs` projection check** for the flipped cases: v4 logs the
    failed attempt (`logLLMCall` with `error`) and nothing after; confirm
    v5's `log_image_gen_job` row count matches (the family already
    compares `llm_logs` — say so).

### Tier 3 — explicit deferrals (loud, typed, never silent)

12. v4's own note "the concealment prompting itself is unchanged and
    remains permissive … tracked separately" — a v4-side item, nothing to
    port; record it.
13. The `docs/v4/` mirror refresh (the bug-133 file + `bugs.md` + the help
    page) — a UNIFIER wire.

## The differential requirement (restated)

Every ported unit ships with an equivalence test driving v4's REAL code
(jest oracle → NDJSON → Rust diff). This lane's proof is tier 3 (mocked
LLM + canned image provider) for the story job and the new sanitizer-gate
family, unit-tier for the log lines and the tool derivation, and the
content oracle for the help page. Closest recipes: the story family's own
header (`story_background_job_tier3_equivalence.rs:1-40`, the oracle's
recipe block, the P4.D94 lane record for how the `[decd8ef9]` danger cases
were added — memory: the tier-3 oracle mechanics rows in the lane-records
index), the avatar family for the moderation-throw shape, and `danger_
gatekeeper_tier3_equivalence` for canning a classification. The jest
oracle traps that bite here: `jest-oracle-empty-provider-registry`,
`jest-setup-llm-logging-service-mocked` (requireActual it back if the new
family compares `llm_logs`), `jest-oracle-character-vault-bridge-is-mocked`
(the story oracle un-mocks it; the new gate family needs no vault).

## Fixtures

Consumes: the story `/tmp` pair (`build-story-background-job-fixture.ts` —
rebuild from the pin; widen ONLY if the new cases need a profile/character
the fixture lacks — they should not: Fern + the four image profiles cover
both), the avatar and image-generation `/tmp` pairs (regen-only), the help
tree (embedded). Delivers: the widened `story-background-job.json` + case
file; the NEW `appearance-sanitize-gate.json` corpus + case; the re-vendored
`help/dangerous-content.md`. No committed DB pair moves. Dependent-oracle
ownership: the story/avatar/image-gen/help families are all this lane's.

## §R — the round-wide meeting points (BINDING — identical in every order of this round)

Round: **the `cc65d6bfc` bug-133 catch-up + `78b381a96`-round remainders
round** — P4.D178 (bug 133: the moderated chat's story background never
escalates) ∥ P4.87 (the turn-manager + route-trail harness remainders) ∥
P4.88 (the substrate + heal + gallery + logging remainders). Ordered
2026-09-10. Three lanes, all from `main`, no stacking.

1. **Baseline and drift.** Oracle baseline **`78b381a96`** (drift-ledger
   §1). v4 `main` HEAD **`cc65d6bfc`** ("Fix bug 133: a moderated chat's
   story background could escalate to the uncensored provider", 2026-09-09
   15:51 -0500, `4.10.0-dev.22`) — **ONE commit past the baseline**, the
   ledger's one §3 row, now `ORDERED(P4.D178)`. Thirteen files: the four
   behaviour files (`lib/background-jobs/handlers/story-background.ts`,
   `lib/image-gen/appearance-resolution.ts`, `lib/tools/handlers/image-
   generation-handler.ts`, `help/dangerous-content.md`), two tests, three
   docs (`docs/CHANGELOG.md`, `docs/developer/bugs.md`, `docs/developer/
   bugs/fixed/bug-133-moderated-chat-image-escalation.md`), and the four
   version-bump files — the docs and bumps are riders of the same commit,
   no separate row. v4 `bugfix` tip **`1a2b2164c`**, unmoved; v4 `release`
   tip **`8fbf2afe0`** (4.9.2), unmoved. The round's catch-up TARGET is
   `cc65d6bfc`; the unifier moves the baseline there.
2. **The freshness probe at lane start** (ledger §2), expected values:
   checkout on **`main`**, HEAD **`cc65d6bfc`**, tree **CLEAN**, `git log
   --oneline cc65d6bfc..main` EMPTY, `git log --oneline 1a2b2164c..bugfix`
   EMPTY. **Any mismatch: STOP and report** — a lane never writes the
   ledger. There is NO pre-authorized exception this round. Re-run the
   probe before every regen batch; the checkout has gone dirty MID-LANE
   before (ledger §5.1) and was switched to `release` mid-unify once
   (P4.D91). v4 ships a commit every day or two — expect the probe to fail
   mid-round; a failing probe is a STOP, not a reason to pin at whatever
   HEAD is.
3. **Regen rule: PIN REQUIRED, lane-unique paths, all three symlink
   classes** (ledger §5.1), every regen and fixture build through
   `harness/tools/recipe_sweep.py --v4 <pin> --run <family>`; never two
   sweeps concurrently.
   - **P4.D178 pins `cc65d6bfc`** for every family it MOVES (the catch-up
     target; path `/tmp/qt-v4-pin-p4d178-cc65d6bfc`) and **`78b381a96`**
     for neutrality regens (`/tmp/qt-v4-pin-p4d178-78b381a96`).
   - **P4.87 and P4.88 pin `78b381a96`** for EVERY regen (`/tmp/qt-v4-pin-
     p487-78b381a96`, `/tmp/qt-v4-pin-p488-78b381a96`): neither moves v4
     behaviour, and bug 133 touches none of their surfaces — verified BY
     PATH at ordering (`git show --stat=200 cc65d6bfc` names no
     turn-manager, failover, orchestrator, continuation, gallery,
     memory-gate, memories-route, export/import, heal or `chats`-repo
     file).
   - **Verify each pin by a marker**: `routesDangerousToUncensored` appears
     in `lib/image-gen/appearance-resolution.ts` and `lib/tools/handlers/
     image-generation-handler.ts` ONLY in the `cc65d6bfc` worktree;
     `rerouteAllowed` appears in `lib/background-jobs/handlers/story-
     background.ts` ONLY there; `git diff --stat 78b381a96 cc65d6bfc --
     help/dangerous-content.md` is non-empty (the `78b381a96` pin's copy
     is byte-identical to v5's CURRENT `help/dangerous-content.md`; the
     tip's is not). Check every NDJSON is non-empty before believing any
     diff (the empty-file trap).
   - Committed recipe headers keep saying `cd ~/source/quilltap-server` and
     never name a `/tmp` pin (`recipe-header-conventions`). The oracle
     `node_modules` are the LIVE tree (`zod` 4.5.4 — `zod_version_guard`
     fails the gate if it moves; `cc65d6bfc` changes no dependency — the
     lock's two lines are the version bump, re-measured 2026-09-10).
   - **`QT_V4_ROOT` for the full workspace gate:** P4.87 and P4.88 set it
     to their `78b381a96` pin; P4.D178 sets it to its `cc65d6bfc` pin from
     its help re-vendor commit onward (before that commit,
     `help_tree_embed_guard` is green only against the baseline root).
     `qtap_schema_embed_guard` is green at BOTH pins (bug 133 moves no
     schema — `public/schemas/qtap-export.schema.json` is untouched by
     `cc65d6bfc`).
4. **Stacking and resumption.** None. All three lanes branch from `main`
   in SEPARATE worktrees and run in parallel. The unifier cherry-picks
   P4.D178, then P4.87, then P4.88.
5. **Frozen and single-owner files.**
   - `crates/quilltap-core/src/api/types.rs` + `api/engine.rs` — **FROZEN
     for every lane** (no lane adds a `Request`/`Response` variant; the
     dispatch wrong-type census constant does not move).
   - `crates/quilltap-host/src/host.rs` — **FROZEN** (no new ensure, no new
     heal; P4.88's boot-shape hardening lives INSIDE the four heal modules).
   - `crates/quilltap-core/src/services/{story_background_job,character_
     avatar_job,image_job_common,appearance_resolution,image_scene_tasks}.
     rs`, `tools/generate_image.rs`, `help/**` — **P4.D178's alone**.
   - `crates/quilltap-core/src/services/{orchestrator,provider_failover,
     chat_continuation,message_finalizer,primary_stream,route_trail}.rs`,
     `harness/oracle/lib/pinned-draws.ts` — **P4.87's alone**.
   - `crates/quilltap-core/src/photos/chat_gallery.rs`, `db/memories.rs`,
     `api/memories.rs`, the four heal modules (`db/{chat_activity_recompute_
     heal,files_sha256_realign_heal,generated_image_placeholder_heal,
     thinking_prefill_retire_heal}.rs`), `db/chats_read.rs`, `test_support.
     rs`, `crates/quilltap-harness/tests/common/mod.rs` — **P4.88's alone**.
   - `Cargo.toml`/`Cargo.lock` at the workspace root and per crate: version
     lines only; a dependency add is a STOP-and-flag in the lane record (no
     lane expects one).
   - `crates/quilltap-core/src/enclave/announce.rs` is NOBODY's; `docs/v4/
     **` is the unifier's; `docs/developer/porting/drift-ledger.md` is
     frozen (lanes never write it).
6. **`apps/web/**` is FROZEN for every lane** — this round has no SPA half.
   Every lane finishes with `git diff main -- apps/web/` EMPTY.
7. **Version bumps** — bump every crate whose files (tests included) the
   commit touches. P4.D178: `quilltap-core`, `quilltap-harness`,
   `quilltap-host` (its `build.rs` embeds `help/**` — the re-vendor changes
   the host artifact). P4.87: `quilltap-core`, `quilltap-harness`. P4.88:
   `quilltap-core`, `quilltap-harness`, `quilltap-host` (`tests/host_boot.rs`
   moves to the shared ensure helper), `quilltap-web` ONLY if the web test
   venue's hand-rolled DDL moves (measure). Per commit. The unifier
   RECOUNTS as base + total bumps — identical bumps merge silently
   (`commit-checklist`; the playbook's silent-auto-merge trap fired again at
   the `f699da6f6` round). Base at ordering: core 0.0.874, harness 0.0.766,
   host 0.0.123, web 0.0.136, cli 0.0.20, tauri 0.0.7, SPA 0.5.695.
8. **e2e.** No lane runs Playwright (no SPA change, no seeded fixture
   moves). The full suite is the unifier's.
9. **Append-only for everyone:** `docs/CHANGELOG.md`,
   `docs/developer/porting/status-log.md`.
10. **Fixture ownership.** P4.D178 widens `harness/oracle/fixtures/story-
    background-job.json` + `harness/oracle/cases/story-background-job.test.
    ts` (+ its `/tmp` builder if a new profile/character is needed),
    delivers the NEW `appearance-sanitize-gate` oracle case + JSON corpus,
    re-vendors `help/dangerous-content.md`, and regens `image-generation` /
    `avatar-job` / the four help families AS-IS (neutrality — no case edit
    unless Tier 2 lands). P4.87 widens the `enclave-step-tier3` and
    `orchestrator-tier3` case files, `harness/oracle/lib/pinned-draws.ts`,
    and the orchestrator/enclave `/tmp` builders; delivers the NEW
    continuation family's case + `/tmp` builder. P4.88 widens the
    export/import `/tmp` builders it names, adds a plant-probe arm to
    `harness/oracle/cases/chat-gallery.test.ts` that plants in the per-case
    COPY (the committed `chat-gallery-{main,mount}.db` pair is READ-ONLY —
    never rebuilt), and adds unit/capture tests with no fixture. **No lane
    regenerates a committed DB pair**, and no two lanes touch one case
    file.
11. **Census guards.** Every Rust lane's gate is `cargo test --workspace`,
    never `-p` (`a-new-verb-moves-the-dispatch-wrong-type-census`,
    `closing-a-divergence-moves-the-censuses`, `a-new-harness-test-can-
    trip-a-source-census`). `db_error_key_guard`'s per-file counts must not
    move; P4.D178's deleted `RerouteRecraft` seam may move a
    `capture-layer` rig count — measure and record.
12. **Cross-app hazards this round.** None new: bug 133 adds no schema
    move and no heal. P4.88's heal boot-shape hardening must stay a no-op on
    a partition where v4 already created both tables (the P4.D140/P4.D152
    rule) — the Friday copy is that partition.
13. **Upstream filings are PREPARED, never committed, by a lane.** A v4
    commit moves v4's HEAD and fails every sibling's probe. P4.87 drafts the
    `summary_fold` `lastTurnParticipantId` filing text in its lane record;
    P4.D175's owed `— outfit preview` filing stays with the human. The
    human files both after unification.

## Ownership (BINDING — identical table in every order of this round)

| Lane | Owns (edit freely) | Must not touch |
| --- | --- | --- |
| **P4.D178** (bug 133, from `main`) | `crates/quilltap-core/src/services/story_background_job.rs`; `services/character_avatar_job.rs`; `services/image_job_common.rs`; `services/appearance_resolution.rs`; `services/image_scene_tasks.rs` (only if the sanitize task's prompt marker needs a doc note); `tools/generate_image.rs`; `help/dangerous-content.md` (byte copy from the `cc65d6bfc` pin); `harness/oracle/cases/story-background-job.test.ts` + `harness/oracle/fixtures/story-background-job.json` (+ `build-story-background-job-fixture.ts` if widened); NEW `harness/oracle/cases/appearance-sanitize-gate.test.ts` + NEW `harness/oracle/fixtures/appearance-sanitize-gate.json` + NEW `crates/quilltap-harness/tests/appearance_sanitize_gate_tier3_equivalence.rs`; `crates/quilltap-harness/tests/{story_background_job_tier3,image_generation_tier3,avatar_job_tier3}_equivalence.rs` (the story family's new arms; the other two regen-only unless Tier 2 lands); the four help families' regen; `crates/quilltap-core/src/services/help_chat/help_content.rs` (only if a count moves — expected none); this order's header | `services/orchestrator.rs`; `provider_failover.rs`; `chat_continuation.rs`; `photos/**`; `db/**`; `api/**`; `host.rs`; `apps/web/**`; `docs/v4/**`; `harness/oracle/lib/pinned-draws.ts`; `test_support.rs`; `tests/common/mod.rs` |
| **P4.87** (the turn-manager + route-trail harness remainders, from `main`) | `crates/quilltap-core/src/services/orchestrator.rs` (the two unguarded classify-before-reset sites — tests + any guard they need); `services/provider_failover.rs` (the failover log BAGS — values only; text/level are already byte-faithful); `services/chat_continuation.rs` (doc/pin only unless the family finds a defect); `harness/oracle/lib/pinned-draws.ts`; `harness/oracle/cases/{enclave-step-tier3,orchestrator-tier3}.test.ts` + their JSON corpora + `/tmp` builders; NEW `harness/oracle/cases/chat-continuation-tier2.test.ts` + NEW `crates/quilltap-harness/tests/chat_continuation_tier2_equivalence.rs` (+ `/tmp` builder); `crates/quilltap-harness/tests/{enclave_step_tier3,orchestrator_tier3,route_trail_continuation_guard}*.rs`; this order's header | every P4.D178 file; `photos/**`; `db/**`; `api/**`; `host.rs`; `apps/web/**`; `docs/v4/**`; `test_support.rs`; `tests/common/mod.rs`; `message_finalizer.rs`/`primary_stream.rs`/`route_trail.rs` (READ — no edit expected; a needed edit is a STOP-and-flag) |
| **P4.88** (the substrate + heal + gallery + logging remainders, from `main`) | `crates/quilltap-core/src/photos/chat_gallery.rs`; `db/memories.rs`; `api/memories.rs`; `db/{chat_activity_recompute_heal,files_sha256_realign_heal,generated_image_placeholder_heal,thinking_prefill_retire_heal}.rs`; `db/chats_read.rs` (the alignment census TEST only — `marshal_row` itself does not move); `test_support.rs`; `crates/quilltap-harness/tests/common/mod.rs`; the hand-rolled ensure sites (`crates/quilltap-host/tests/host_boot.rs`, `crates/quilltap-core/src/enclave/lifecycle.rs` + `enclave/step.rs` TEST DDLs only, `db/chats_messages_read.rs:351`'s test DDL, the web test venue's DDL); `harness/oracle/cases/chat-gallery.test.ts` (the plant-probe arm) + `crates/quilltap-harness/tests/chat_gallery_equivalence.rs`; the export/import/restore families' `/tmp` builders + cases it widens (name them in the lane record); NEW capture tests for the memory-gate lines; this order's header | every P4.D178 file; every P4.87 file; `api/**` (except `api/memories.rs`); `host.rs`; `apps/web/**`; `docs/v4/**`; the committed `chat-gallery-{main,mount}.db` (READ-ONLY); `enclave/step.rs`/`lifecycle.rs` PRODUCTION code (test DDLs only) |

Everyone: `docs/CHANGELOG.md` + `status-log.md` append-only; the drift
ledger frozen; `api/types.rs`/`api/engine.rs`/`host.rs`/`apps/web/**`
frozen; `enclave/announce.rs` nobody's; `docs/v4/**` the unifier's.
`Cargo.toml` / `Cargo.lock`: version lines only. The `docs/v4/` mirror
refresh (the bug-133 file at its `bugs/fixed/` path + the two moved help
paragraphs), the baseline move to `cc65d6bfc`, and the ledger's §3 →
ABSORBED are UNIFIER wires.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
   -D warnings` in BOTH feature sets (default and `--features
   quilltap-core/native-transport`); `cargo build --workspace`; the release
   build.
2. `cargo test --workspace` (background, full log + sentinel — NEVER
   `| tail`, never a pgrep loop — `background-gate-latency-fix`,
   `pgrep-until-loops-match-themselves`, `a-gate-piped-through-tail-
   undercounts-binaries`) with this lane's env block (every family it moves,
   by its recipe var — `a-family-env-var-is-not-its-regen-var`) AND
   `QT_V4_ROOT` set per §R.3; grep the log for `SKIP:` — every family this
   lane moves must RUN (confirm by per-binary duration or `-- --nocapture`);
   the exit code read directly, never after a pipe
   (`gate-exit-code-after-a-pipe`).
3. Every family this lane moves regenerated FRESH from the pin §R.3 names
   through `harness/tools/recipe_sweep.py --v4 <pin> --run <family>` (never
   two sweeps concurrently), re-run by name with zero SKIP, the changed
   bytes grepped in the NDJSON (§R.3's markers), and the NDJSON non-empty.
   The RED-FIRST items shown red against the pre-edit tree in the lane
   record (run them before the first source commit — a `git stash`-free
   proof).
4. The mutation proofs named in this order, run and restored by string
   edit (`mutation-proof-revert-by-file-backup`; `cargo-fmt-silently-voids-
   a-string-edit-mutation` — assert the replacement count), each reddening
   exactly the named test.
5. `git diff main -- apps/web/` EMPTY; `git diff main -- help/` EMPTY
   (P4.D178 excepted, and there ONLY `help/dangerous-content.md`); the
   Ownership table's "must not touch" column honoured (`git diff main
   --stat` read against it).
6. Commits per `.claude/commands/commit.md` (the crate bumps §R.7 names, on
   every commit that moves the crate; CHANGELOG every commit); the lane
   record appended to `status-log.md` with the red-first evidence, the
   per-family regen lines, the mutation table, every measured deviation
   from this order (`work-order-facts-need-the-same-verification-as-commit-
   prose`), and the "spotted, not mine" list for the unifier.
