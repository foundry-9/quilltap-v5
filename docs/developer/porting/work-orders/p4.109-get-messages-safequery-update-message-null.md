# Work order P4.109 — P4.105's owed edge, done at the right depth: `get_messages` takes v4's `safeQuery` fallback (`[]` + ERROR `Failed to get messages for chat`) — which is where v4's count/find/replace actually swallow, so search & replace PREVIEW answers v4's 200-with-zeros instead of v5's 500 — with a `get_messages_strict` sibling for every caller whose v4 counterpart rethrows, chosen by a 76-site caller census; `update_message` answers v4's `null` (`Ok(false)`) + ERROR `Failed to update message in chat` with its nine callers audited against v4; the ui-search poisoned-table regression arm — maintenance, from `main`

**Status: OPEN** (ordered 2026-09-23).

## Preamble

- **v4 oracle baseline: `a2db63da7`** (drift-ledger §1). No drift row is
  absorbed. Every v4 file cited here is byte-identical at `a2db63da7`,
  `00c290c9a` and `f45a517a9` (`git diff` empty for
  `chats-messages.ops.ts`, `chats-search.ops.ts`, `safe-query.ts`,
  `base.repository.ts`, `strict-failures.ts`, `app/api/v1/ui/search/
  route.ts`, `lib/search-replace/`, `app/api/v1/search-replace/`).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED (§R.3) — BASELINE ONLY**:
  **`/tmp/qt-v4-pin-p4109-a2db63da7`**.
- **Sibling lanes:** P4.D215 (owns `context_summary.rs`,
  `title_update_job.rs`, `chat_admin.rs`, `image_profile_resolution.rs`,
  NEW `auto_title.rs` — your contract reaches them through calls; a site
  there needing the strict variant is RECORDED, not edited, §R.10(c)),
  P4.107, P4.108, P4.110 (owns `services/quilltap_import/**` and
  `crates/quilltap-web/tests/common/mod.rs` — same rule).
- **Worktree:** `git worktree add <path> -b claude/p4-109-get-messages-
  safequery-<hash> main`; `CARGO_INCREMENTAL=0`.
- **Read before the work:** `work-orders/p4.105-update-message-fts-
  update.md` (status header + its lane record in `status-log.md` — the
  owed edge and "finding 1", the NULL-content row); v4
  `lib/database/repositories/safe-query.ts:51-72`,
  `chats-messages.ops.ts:272-366,517-571`, `chats-search.ops.ts:113-351`,
  `chats.repository.ts:95-129`; memory notes
  `a-v4-mock-is-not-v4-in-a-route-family`,
  `a-result-comparand-row-needs-an-unanswerable-request`,
  `capture-layer-target-assert-is-a-prefix-match`,
  `v4-turns-every-null-cell-into-undefined-on-read`.

## Mandate

Put v4's swallow where v4 has it. v4's count/find/replace never reach
their own `safeQuery` arms on SQLite: they call `getMessages`, which is
itself a FALLBACK `safeQuery` answering `[]`. So the faithful move is
`get_messages` answering `Ok(vec![])` + v4's ERROR line, with a strict
sibling for every call site whose v4 counterpart does NOT go through
`getMessages` — chosen site by site from a caller census, never by blanket
rule. Then `update_message` answers `Ok(false)` + v4's ERROR where v4
answers `null`, with each of its nine callers checked against v4's
`null` handling. Must NOT touch: `db/chat_message_fts.rs`, P4.D215's and
P4.110's files (record, don't edit), any committed fixture pair (plant on
per-run COPIES), `apps/web/**`, the drift ledger.

## Survey-verified starting points (fresh survey 2026-09-23 on `main` `8d31a30c` and v4 `00c290c9a` — do not re-derive)

### v4

1. **`safeQuery` (the standalone helper, `safe-query.ts:51-72`)** — the one
   the chats ops files import (`chats-search.ops.ts:16`,
   `chats-messages.ops.ts:20`). Modes by rest-param length (`:69`):
   rethrow / fallback / silent. Catches everything; logs
   `logger.error(errorMessage, { ...context, error:
   extractErrorMessage(error), ...(strict ? { strictFailures: true } :
   {}) })` (`:64-68`; `extractErrorMessage` `:23-25` = `error.message` or
   `String(error)`); returns the fallback only when one was given AND
   strict mode is off. Strict mode (`strict-failures.ts:40-55`,
   AsyncLocalStorage) is entered only by the importer (bug 79); v5 has no
   port of it. **The standalone helper adds NO `collection` field** (the
   base-repo method `base.repository.ts:84-95` does; the chats ops files
   do not use it).
2. **Every use on these paths:**

| v4 method | file:line | mode / fallback | ERROR message `{fields}` |
|---|---|---|---|
| `getMessages` | `chats-messages.ops.ts:315-366` (`:366`) | fallback `[]` | `Failed to get messages for chat` `{chatId, error}`; per-row skip `:346` WARN `Skipping corrupted chat message` |
| `countMessagesWithText` | `chats-search.ops.ts:113-133` | fallback `0` | `Failed to count messages with text` — **UNREACHABLE on SQLite** (it only calls `getMessages`, `:123`) |
| `findMessagesWithText` | `:141-168` | fallback `[]` | `Failed to find messages with text` — UNREACHABLE (`:154`) |
| `searchMessagesGlobal` | `:177-271` | fallback `[]` | `Failed to search messages globally` `{chatCount, error}` — PORTED by P4.105 (`rawQuery`, reachable) |
| `replaceInMessages` | `:280-351` | **rethrow** | `Failed to replace text in messages` — but its `getMessages` swallows first, so it answers `0` |
| `updateMessage` | `chats-messages.ops.ts:517-571` (`:570`) | fallback `null` | `Failed to update message in chat` `{chatId, messageId, error}` — REACHABLE (`findOne` `:525`, `ChatEventSchema.parse` `:529`, `updateOne` `:531-534`) |
| `announceTranscriptChange` | `:272-284` | fallback `false` | already ported (v5 swallows, `chats_messages.rs:436`) |

3. **⚠ The warm-up trap.** `ChatsRepository.ensureMessagesCollection
   Initialized` (`chats.repository.ts:95-112`) runs `CREATE TABLE IF NOT
   EXISTS "chat_messages"` (`schema-translator.ts:404`) the FIRST time a
   repository instance touches the messages collection. If a plant renames
   the table BEFORE that first touch, v4 silently re-creates an EMPTY table
   and answers `0`/`[]` with NO error — the right value for the wrong
   reason. `searchMessagesGlobal` escaped this (it goes through
   `rawQuery`). The chats-search oracle poisons right after `new
   ChatsRepository()` (`harness/oracle/cases/chats-search.ts:163-164`) —
   NOT warmed; the route oracles `jest.resetModules()` per case — NOT
   warmed. **Every count/find/update/preview/execute arm must WARM the
   collection on the v4 side first (one `getMessages` on the same repo
   instance, then the rename) and must PROVE the ERROR line fired** (a
   `0` without the line is the trap, not a pass).
4. **The route edges.** `POST /api/v1/search-replace` preview
   (`lib/search-replace/search-replace-service.ts:90`,
   `getSearchReplacePreview`) → **200 `{messageMatches: 0, …,
   affectedChats: 0}`** on a poisoned table (after warm-up, one ERROR per
   chat). `GET /api/v1/ui/search` (`route.ts:410-417` catch-all → 500
   `Search failed`) → **200** with the messages type absent on a poisoned
   table (measure: another read in the route could touch `chat_messages`).

### v5

5. **`get_messages`** (`crates/quilltap-core/src/db/chats_messages_read.rs:
   298-310`): `prepare(...)?` and `r?` per row — `Err` where v4 answers
   `[]` + ERROR; one NULL `content` cell fails the whole read
   (`marshal_message`'s `row.get::<_, CompressedText>(3)?` — P4.105's
   finding 1) where v4 skips the row with WARN. **76 call sites in 50
   files** under `quilltap-core/src`.
6. **`count_messages_with_text` / `find_messages_with_text`**
   (`db/chats_search.rs:168-194`, `:197-229`): the over-length warn, then
   `get_messages(self.conn, chat_id)?`. `find_messages_with_text` has NO
   production caller in either tree; count's ONE caller is
   `services/search_replace.rs:196` (preview; `inner()` maps `Err` → 500
   "Internal server error", `:230-233`). `replace_in_messages`
   (`chats_search.rs:391`, execute) also reads through `get_messages`.
   P4.105's `search_messages_global` wrap (`:243-262`, `LOG_CONTEXT =
   "db.chats-search"` `:86`, `tracing::error!(target: "quilltap::db",
   context = LOG_CONTEXT, chatCount = …, error = %err, …)`) is the house
   precedent for the ERROR line's shape; its tests
   `a_scan_that_throws_answers_empty_and_logs_once` (`:602-642`) and the
   silence leg (`:646-667`).
7. **`update_message`** (`db/chats_messages.rs:555-583`, `Result<bool,
   DbError>`): `find_event_value` (`:694-699`) reads the whole chat
   through `get_messages` and filters (not v4's `findOne`);
   `serde_json::from_value::<ChatEventInput>` → `DbError::Internal(
   "updateMessage parse: …")`; `update_statement(...)?`; `execute(...)?`.
   **Every failure → `Err`** where v4 answers `null`. ⚠ `MessageEventInput.
   role` is a plain `String` (`:87-91`), so `role: "BOGUS"` passes v5 and
   fails only v4's Zod — a red-first plant must fail BOTH sides (`updates:
   {"content": 42}` or `{"createdAt": 5}` — a type mismatch in serde AND
   Zod). ⚠ `find_event_value` must read through the STRICT variant so a
   read failure inside `update_message` logs v4's `Failed to update
   message in chat`, not `Failed to get messages for chat`.
8. **The nine production `update_message` callers**: `api/salon.rs:1058`
   (`message_edit` — `Ok(false)` → 404, `Err` → 500, `:1061-1065`),
   `api/files.rs:669`, `generators/rename.rs:762`,
   `services/carina_memory_extraction.rs:303`,
   `services/regenerate_swipe.rs:895`, `services/courier_transport.rs:635`
   (`?`), `services/message_reattribute.rs:116`,
   `services/memory_extraction_job.rs:367`, `services/orchestrator.rs:1807`
   (+ tests `realtime/publish_sites.rs:1198,1218`).
9. **The families:** `chats_search_equivalence` (repo tier; `POISON_SQL`
   `:115` byte-identical to the oracle's `:95`; `run_poisoned` `:360-403`
   asserts `op.kind == "searchMessagesGlobal"` at `:380-383`; spec
   `harness/oracle/fixtures/chats-search.json:498-…`, venue
   `QT_VENUE=poisoned`; env `QT_ORACLE_CHSEARCH`, `QT_FIXTURE_CHSEARCH`,
   `QT_FIXTURE_CHSEARCH_FTS`); `search_replace_equivalence` (v4's REAL
   route over a fresh copy of the committed `chat-dialogs-*` pair per
   case — widened at `a2db63da7`, green; oracle `chat-dialogs-search-
   replace.test.ts`; env `QT_ORACLE_SEARCH_REPLACE`, `QT_FIXTURE_CD_MAIN`,
   `_MOUNT`; `TZ=UTC`); `chats_messages_ops_tier2_equivalence` (update;
   `.expect("update_message")` at `:263`; the NDJSON records final-state
   dumps only, not return values; case `chats-messages-ops-tier2.ts:97-99`
   ignores the return); `ui_search_equivalence` (`crates/quilltap-harness/
   tests/ui_search_equivalence.rs`, 440 lines; jest `ui-search.test.ts`, 28
   + 7 snippet cases; spec `ui-search.json`; builder
   `build-ui-search-fixture.ts`; env `QT_ORACLE_UI_SEARCH`,
   `QT_FIXTURE_UI_SEARCH_MAIN`, `_MOUNT`; ONE shared `fresh_db` on the Rust
   side `:180` — a poisoned case needs its own; the oracle copies per case
   `runCase` `:240-271`; the case list hard-coded both sides with a count
   gate). **There is no `ui_search_routes` test** — `quilltap-web/src/
   ui_search_routes.rs` is a 49-line pass-through; the "edge" is the core
   handler `api/ui_search.rs:403` (`search_body`; `Err` → `Error during
   search` + `ErrorKind::Internal` "Search failed", `:356-366`).

## Tiered deliverables

### Tier 1 — must land

1. **The caller census first** (lane record, before any source edit): all
   76 `get_messages` call sites, each with its v4 counterpart (file:line)
   and the verdict **fallback** (v4's counterpart reads through
   `getMessages` — takes the new swallowing `get_messages`) or **strict**
   (v4's counterpart rethrows or reads another way — repointed to
   `get_messages_strict`). `find_event_value` is `strict` (item 3). A site
   in P4.D215's or P4.110's files that needs `strict` is RECORDED for the
   unifier (§R.10(c)), not edited. A site whose v4 counterpart you cannot
   find is a STOP on that site — record it, leave it on `strict` (today's
   behaviour), and name it.
2. **`get_messages` → v4's fallback**: `get_messages_strict` = today's
   body (renamed); `get_messages` = strict + on `Err` ERROR `Failed to get
   messages for chat` with `chatId` and `error` (P4.105's shape: `target:
   "quilltap::db"`, its module's `context`), answering `Ok(vec![])`. The
   signature keeps its `Result` (the P4.105 precedent). The census's
   `strict` sites repointed.
3. **`update_message` → `Ok(false)` + ERROR `Failed to update message in
   chat` `{chatId, messageId, error}`** on any inner failure
   (`update_message_inner` + the wrap, P4.105's pattern). **The nine
   callers audited** (lane record: each caller, its v4 counterpart's
   `null` handling, the observable change, and the hunk if one is needed —
   e.g. `message_edit`'s 500 → 404 is v4's behaviour iff v4's route maps
   `null` to 404; verify, don't assume). `courier_transport.rs:635`'s `?`
   and every other `Err`-only branch re-examined.
4. **The red-first arms** (each warmed on the v4 side, each proving the
   ERROR line fired — item §Survey 3):
   - `chats_search_equivalence`: `poisonedReads` grows one
     `countMessagesWithText` and one `findMessagesWithText`; `run_poisoned`
     dispatches by kind (the oracle's `runRead` already does). v4 `0`/`[]`;
     v5 today `Err`. Capture pin: the line is `Failed to get messages for
     chat` `{chatId}` — NOT the count/find sentence — with its silence leg.
   - `search_replace_equivalence`: `preview_poisoned` (v4 200 zeros; v5
     today 500) and `execute_poisoned` (v4's `0`; v5 today `Err`).
   - `chats_messages_ops_tier2_equivalence`: an `updateMessage` op with
     `updates: {"content": 42}`; the case and the Rust side record each
     op's RETURN (`null` vs the event) and compare it (`Ok(false)` ↔
     `null`); the final state unchanged both sides; v5 today panics at
     the `.expect` (`:263`). Capture pin for the update line with its
     silence leg.
5. **The ui-search regression arm**: a `messages_poisoned` case in
   `ui_search_equivalence` (its own `fresh_db`; `POISON_SQL` planted on
   both sides; queries `?q=airship` and `?q=airship&types=messages`) —
   GREEN on arrival (P4.105 already swallows); its proof is the mutation
   in item 7.

### Tier 2 — should land

6. **P4.105's finding 1**: `get_messages` skips a row whose marshal fails
   (a NULL `content`) with v4's WARN `Skipping corrupted chat message`
   (read v4's `:346` for its fields) instead of failing the read; a
   planted NULL-content row in `chats_search_equivalence` or the ops
   family, red-first.
7. **Mutation proofs**: revert `search_messages_global`'s wrap →
   `messages_poisoned` red (500 vs 200); make `get_messages` rethrow →
   the count/find/preview arms red; read `find_event_value` through the
   swallowing variant → the update capture pin red (wrong line); drop the
   `update_message` wrap → the ops return-row red.
8. **`get_messages_caller_census`** (NEW harness guard, with the lexer the
   census files use — or a plain `rust_sources` scan if P4.110's shared
   module has not landed; say which): every production `get_messages(`
   and `get_messages_strict(` call site listed with its verdict, so a new
   caller must choose.

### Tier 3 — explicit deferrals (loud, typed, never silent)

9. v4's strict-failures mode (bug 79's AsyncLocalStorage) — not ported
   anywhere in v5; the importer path keeps v5's `Err`. Named.
10. v4's backend-level `SQLite find error` / `findOne error` / `Raw query
    failed` ERROR lines — never ported; named.

## The differential requirement, restated

Every behaviour change lands red-first in a family that drives v4's REAL
code: the repo tier (`chats_search`, `chats_messages_ops_tier2`) at tier 2,
the routes (`search_replace`, `ui_search`) through v4's real handlers. The
warm-up is part of the arm, and the ERROR line's presence in v4's capture
is part of the proof. The caller census is the audit that the contract
change moves nothing v4 does not.

## Fixtures

Consumes: the `/tmp`-built `chats-search`, `chats-messages-ops`, and
`ui-search` fixtures (their builders at the pin) and per-run COPIES of the
committed `chat-dialogs-*` pair (never the pair itself). Delivers: no
committed fixture; spec/corpus growth only.

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `00c290c9a` bug-163/164 drift catch-up + maintenance round: **P4.D215 ∥
P4.107 ∥ P4.108 ∥ P4.109 ∥ P4.110**, ordered 2026-09-23. Five lanes, five
worktrees, five lane branches, **every one cut from `main` (`8d31a30c` or
the commit that lands these orders)** — no keystone, no SPA lane, no server
contract crossing a lane boundary. The unifier cherry-picks in the order
**P4.D215 → P4.107 → P4.109 → P4.108 → P4.110** (the drift first; the heal
before any family reading a widened pair is re-run; the `get_messages`
contract before the lanes that only re-run families over it; the codec
decline beside them; the DRY lane LAST because it moves shared test
helpers (`tests/common/mod.rs`, a new `tests/source_census/mod.rs`) and
`execute_import`'s signature, which every other lane's tree compiled
against), gates ONCE from the new baseline pin, and `merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`a2db63da7`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`00c290c9a`**
  ("fix(titles): route every auto-title through applyAutoTitle (bugs 163,
  164)", `4.10.0-dev.65`, 2026-09-23 00:20) — **ONE commit past the
  baseline**, `ORDERED(P4.D215)` in the ledger's §3. v4 `bugfix` tip
  `1a2b2164c` and `release` tip `8fbf2afe0` are unmoved; still no `release:
  4.10.0` squash. The checkout sat on `main`, CLEAN, at the planning probe
  (2026-09-23 — the §2 probe PASSED; nothing was re-derived). Spec reads:
  `git show 00c290c9a:<path>` for post-commit files and `git show 00c290c9a
  -- <path>` for the hunks — **never the commit message** (§R.4). **At
  unification the baseline MOVES to `00c290c9a`**; the row retires
  `ABSORBED(…)`.
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log 00c290c9a..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Pass = branch `main`, HEAD `00c290c9a`, both logs EMPTY, tree
  CLEAN.** Any mismatch — a moved HEAD (a new commit is a NEW drift row,
  not a pass), a non-empty log, ANY dirty path, the checkout off `main` —
  is a STOP: report it and do nothing else. A lane never writes the
  ledger. A lane resumes ONLY on a waiver the human records in the
  ledger's §1 (the `89fcc3c0d` round's mechanism), never on its own reading
  of the dirt.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1), absolutely. HEAD
  is one commit past the baseline and it rewrites the fold's title path
  (`context-summary.ts`), whose families regenerate routinely. Every regen
  runs from a LANE-UNIQUE detached worktree per ledger §5.1 (`git -C
  ~/source/quilltap-server worktree add --detach /tmp/qt-v4-pin-<order>-
  <sha> <sha>`, the THREE symlink classes — root `node_modules`,
  `packages/quilltap/node_modules`, every `plugins/dist/*/node_modules` —
  then `cd` into it; never stash or checkout-switch the human's repo; `git
  worktree list` before each batch; **verify the pin by `git -C "$PIN"
  rev-parse HEAD` AND by `ls -ld` of the path** — build every pin path from
  a quoted literal, never an unsplit zsh variable; then grep the fresh
  NDJSON for a marker only one tree has; check the file is non-empty before
  believing a diff). **The DRIFT lane (P4.D215) pins the TARGET
  `00c290c9a`** (`/tmp/qt-v4-pin-p4d215-00c290c9a`) for every family its
  port MOVES **and the BASELINE `a2db63da7`**
  (`/tmp/qt-v4-pin-p4d215-a2db63da7`) for its neutrality legs and for the
  `chat-admin-main.db` widen. **The four MAINTENANCE lanes (P4.107, P4.108,
  P4.109, P4.110) pin the BASELINE `a2db63da7` ONLY**
  (`/tmp/qt-v4-pin-<order>-a2db63da7`) — their families must not move at
  the baseline; a family that ALSO moves at `00c290c9a` (the fold / title
  families P4.D215 names) is P4.D215's to re-record and the UNIFIER's to
  re-run from the new baseline pin with every lane's growth present. Pin
  verification marker: the target-pinned `help_tree_equivalence` NDJSON
  carries `the Lantern's cue that the scene has shifted`
  (`help/story-backgrounds.md` at `00c290c9a`); the baseline-pinned one
  does not (`grep -c` both). The sweep driver is the sanctioned per-family
  path (`python3 harness/tools/recipe_sweep.py --run <family> --v4 "$PIN"
  --v5w <ABSOLUTE worktree>`); never run two sweeps concurrently; a family
  that reports `SKIP` in 0.00 s did not run. Ledger §5.2's
  silent-stale-pass discipline applies to every regen (`rm -f` the output
  first; read the builder's last line; `grep -c` the changed bytes in the
  fresh NDJSON — MUST be > 0 where a change is predicted). Every lane
  stages its regen outputs under a LANE-PRIVATE directory (`/tmp/<order>/…`
  — several families' committed recipes write FIXED `/tmp/oracle-*.ndjson`
  paths; re-derive into the lane's directory before any `cargo test
  --workspace`) and leaves the committed recipe headers canonical (a
  committed recipe NEVER names a `/tmp` pin — the driver's `--v4` is the
  pin). Every jest oracle writes ZERO `llm_logs` rows (`jest.setup.ts`
  mocks the logging service) — never predict an `llm_logs` diff from a
  jest family; count the canned-LLM calls instead. `QT_NODE` in any env
  block is a REAL path (`~/.nvm/versions/node/v24.13.1/bin/node`), never
  `$N/node`.
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). Measured
  at planning: (a) `00c290c9a`'s message says the chokepoint "writes only a
  changed title" — the HUNK also writes `extraPatch + updatedAt` on the
  refused arms (`writeExtraOnly`, `auto-title.ts:65-69`): the title check's
  cursor, regenerate's `isManuallyRenamed:false`; the fold has no patch and
  writes nothing. (b) The message is silent on logs; the hunks REMOVE
  `[Title Update] Updated title for chat …`, ADD `[Auto Title] Chat
  retitled`, RE-PREFIX the two enqueue lines `[Title Update]` → `[Auto
  Title]` and DROP their `context` field, add three debug lines, and give
  the fold and regenerate info lines an `outcome` field. (c) The title
  check's UNCHANGED title now writes only the cursor and queues NO
  background (before, it wrote and queued) — a behaviour change the
  message never names. (d) The old help text's "(both automatic and
  manual)" trigger claim was FALSE, not stale: before the commit only the
  title-check handler ever queued. (e) In the PLAN: `phase-4.md`'s heal
  list named `almanack-llmlogs-legacy.db` as a gap — it is DELIBERATE (the
  builder drops the two profile columns so `almanack_tier2`'s
  `data_approximate` case exercises approximate attribution); and it read
  the migrator's `:461` error on `wardrobe-instructions-*` as a schema
  problem — it is a PEPPER miss (P4.107). (f) The animated-input ruling's
  wording ("a WebP whose VP8X chunk carries the animation bit") is refined
  by measurement: sharp counts FRAMES, and v4's only animated sharp call is
  the blob transcode (P4.108). Re-verify even where an order flags the trap
  — the order's paragraph is the same kind of prose. Carry a v5-side code
  comment naming the sha where the prose misleads.
- **§R.5 — Designed reds and pre-existing reds: measure, don't assume**
  (ledger §5.4). No convergence pin trips this round (bugs 163/164 are v4's
  own filings). The DESIGNED reds: **P4.D215** — `title_update_tier3` red
  at the target on its grown unchanged-title and mid-flight-rename arms and
  on the three capture tests that pin the removed/renamed lines;
  `chat_regenerate_title_tier3` red on the un-widened `chat-admin-main.db`
  (fixture vintage — green after P4.D215's widen at the BASELINE pin; the
  pre-widen count is the measurement) and then red-first at the target on
  its grown overrule/background arms; `context_summary_service_tier3` red
  at the target on the grown hand-renamed-fold arm; the three fold-reaching
  families (`memory_pipeline_jobs_tier3`, `orchestrator_tier3`,
  `context_summary_service_tier3`'s existing corpus) predicted NEUTRAL at
  the target (no fixture enables story backgrounds or marks a chat
  renamed) — re-run and record, never assume. **P4.107** — the five named
  standing reds (`photos_routes`' five save rows, `wardrobe_routes`' ~20
  outfit/equip 500s, `llm_logs_routes`' four `list_by_chat*` cases,
  `almanack_tier2`'s generate chain) RED from the pin on the un-widened
  pairs and GREEN after the widen. **P4.108** — the animated-GIF rows
  red-first on the un-declined tree (v5 writes a still `image/webp`; the
  ruled shape is the original bytes), pinned as a RULED DIVERGENCE in both
  directions after. **P4.109** — the poisoned count/find/preview/execute
  arms and the `updateMessage` type-mismatch arm red-first (v5 `Err`/500
  where v4 answers `0`/`[]`/200/`null`); the ui-search regression arm
  GREEN on arrival (its proof is a mutation). **P4.110** — no red: every
  census count unchanged, every moved helper's callers green; its proofs
  are mutations. A red in a family a lane did NOT touch is a NEW finding
  to classify at both pins before any source is touched, and it is
  probably not that lane's. **The `/tmp/qt-imggen-*` staging collision** is
  the one recorded standing intermittent in `cargo test --workspace`
  (green by name).
- **§R.6 — The differential requirement stands for every unit.** A ported
  unit lands WITH an equivalence test that drives v4's REAL code at the
  right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest` is
  shared) and its silence leg; a fixture change lands with the regen of
  every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that SURVIVES
  as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`). A v4 unit test that
  MOCKS a repository (both of `00c290c9a`'s new tests do) is mirrored as a
  real-DB arm with PLANTED rows through the existing jest oracle, never as
  the same mock (`a-v4-mock-is-not-v4-in-a-route-family`). A ruled
  divergence is pinned in BOTH directions (the `EXPECTED_DIVERGENCES`
  shape, `markdown_transcript_equivalence.rs:40,213-233,256-261`): a
  "divergence VANISHED" failure if v4 == v5, a "WRONG SHAPE" failure if
  the difference is not the ruled one, and an assert that every declared
  divergence was exercised.
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane. Cross-order header edits allowed
  this round (one sentence each, naming this round): P4.108 → P4.104's
  header (the animated divergence closed as ruled); P4.109 → P4.105's
  header (its owed edge closed); P4.110 → P4.104's header ONLY for the
  `execute_import(…, None)` latent item — P4.108 and P4.110 both touch
  P4.104's header, so each APPENDS its own sentence and the unifier
  resolves the adjacency by hand (union, never `--theirs`).
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed on each commit per `.claude/commands/commit.md`; the unifier
  RECOUNTS as base + the sum of every lane's bumps (the silent-auto-merge
  trap: identical bumps merge as ONE — it fires every round). Base at
  planning: core 0.0.1002, harness 0.0.913, host 0.0.147, web 0.0.176, cli
  0.0.27, tauri 0.0.7, fixture-sanitizer 0.0.4, SPA 0.5.748. Expected:
  **core** by P4.D215, P4.108 (module docs), P4.109, P4.110; **harness** by
  P4.D215, P4.108, P4.109, P4.110; **web** by P4.D215 (the `chat-admin`
  pair lives in `crates/quilltap-web/tests/fixtures/`), P4.107 (the
  pairs), P4.110 (`tests/common/mod.rs`), P4.109 ONLY if it adds a web
  test — say so; **host** by P4.108 ONLY; **cli**, **tauri**,
  **fixture-sanitizer**, **sqlite3mc-sys** and **the SPA** by NOBODY. No
  lane touches the workspace `Cargo.toml` dependency block; every lane's
  `Cargo.toml` deltas must be version-only — the unifier audits each before
  any resolution (the `--theirs` clobber trap).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's: each
  lane pre-lists, in its lane record with byte counts at `00c290c9a`, the
  mirror paths its rows move. This round's mirror rules in one place:
  `00c290c9a` → `docs/v4/developer/bugs/fixed/bug-163-fold-rename-no-
  background.md` (NEW, 4,621), `docs/v4/developer/bugs/fixed/bug-164-fold-
  overwrites-manual-title.md` (NEW, 2,867), `docs/v4/developer/bugs.md`
  (306,475 → 308,274). **The unifier ALSO clears the recorded mirror lag
  in the same commit** (measured at planning): `bug-146-banner-passes-the-
  composers-seat.md` STALE (9,398 → 13,717), and MISSING `bug-147-chat-get-
  omits-the-rotation.md` (10,799), `bug-148-tool-defaults-outrank-image-
  profile.md` (6,157), `bug-149-orientation-default-erases-size.md`
  (5,048), `bug-150-generation-dialog-dead-endpoint.md` (3,969),
  `bug-154-default-prompt-star-dead-action.md` (7,612) — every one
  byte-copied from `00c290c9a` and md5-checked. `docs/v4/CHANGELOG.md`'s
  older lag (~385 lines) stays a named housekeeping item, not widened
  here. `docs/CHANGELOG.md`, `README.md`, `CLAUDE.md`, every
  `package.json`/`package-lock.json` stamp → NO-PORT, the standing class.
  The `help/` count does NOT move (`help/story-backgrounds.md` is a
  byte-copy by P4.D215, 8,220 → 8,976; 126 stays 126 in BOTH literals —
  `help_tree_embed_guard.rs` and `host_help_docs_boot.rs`);
  `help_tree_equivalence` at the target pin is the proof the tree is v4's.
- **§R.10 — Fences, ownership edges and the pre-declared spills.** (a)
  **`api/types.rs` and `api/engine.rs`:** NOBODY adds a variant this round
  (no new verb). P4.D215 may edit the BODY of the existing
  `ChatRegenerateTitle` arm only if the chokepoint's clock needs threading
  — one hunk, said in the lane record. (b) **`db/chats_messages*.rs` +
  `db/chats_search.rs`:** P4.109's. (c) **`services/context_summary.rs`,
  `services/title_update_job.rs`, `services/chat_admin.rs`,
  `services/image_profile_resolution.rs`, NEW `services/auto_title.rs`:**
  P4.D215's. P4.109's `get_messages` contract change REACHES the first
  three through calls it does not edit; if P4.109's caller census finds
  one of THEIR `get_messages` calls must take the strict variant, it
  RECORDS the site for the unifier and does not edit the file. (d)
  **`services/quilltap_import/**` + every `execute_import(` caller:**
  P4.110's. (e) **`crates/quilltap-host/src/image_codec.rs` +
  `services/mount_index/normalize_blob_image.rs`:** P4.108's. (f)
  **`crates/quilltap-web/tests/common/mod.rs`:** P4.110's alone; P4.D215
  and P4.109 add web tests (if any) as NEW files with local helpers, never
  a `common` edit. (g) **Committed fixture pairs:** P4.D215 widens
  `chat-admin-main.db` ONLY; P4.107 widens its five; nobody else touches a
  pair. (h) **`crates/quilltap-harness/tests/blob_write_sites_census.rs` +
  `compressed_column_write_sites_census.rs`:** P4.110's (the lexer lift);
  P4.108 does not move them (the helper is host-side, the census scans
  core only). (i) **Pre-declared loud out-of-mandate edits** (the
  `a-required-field-on-a-shared-struct-crosses-lane-ownership` rule: do the
  edit, keep it to the hunk, mark it `// P4.xxx OUT-OF-MANDATE — <owner>
  preserves`, audit with `git diff main -- <path>` at lane close): **P4.109
  → the nine `update_message` callers** (`api/salon.rs`, `api/files.rs`,
  `generators/rename.rs`, `services/{carina_memory_extraction,
  regenerate_swipe,courier_transport,message_reattribute,
  memory_extraction_job,orchestrator}.rs`) — the call-site hunk only, as
  its audit rules; **P4.110 → `api/system_qtap.rs` +
  `services/character_archive/service.rs`** (the `execute_import(` call
  hunk only). If a lane finds it needs another lane's file beyond these, it
  STOPs on that item and records the shape for the unifier.
- **§R.11 — No SPA lane this round.** Nothing under `apps/web/**` moves.
  `00c290c9a` has no client hunk, the regenerate-title response body is
  unchanged (`{success, title}`), and the SPA reads only `data.title`
  (`chat-admin.api.ts:70-73`). The unified gate still runs the full
  Playwright suite once against the release binary (P4.109's
  `get_messages` contract and P4.D215's chokepoint both reach live
  surfaces); the documented intermittents and the standing parks stand as
  named in the previous round's gate (326 passed / 5 failed / 6 skipped —
  four reds green by file alone, the fifth the avatar-rolls re-seed, since
  fixed).
- **§R.12 — Committed fixtures: widen, never rebuild.** P4.D215 widens
  `chat-admin-main.db`; P4.107 widens `photos-main.db`, `inspector-main.db`,
  `inspector-nostore-main.db`, `wardrobe-routes-main.db`,
  `almanack-main.db` — both through v4's own migration statements via
  `harness/oracle/fixtures/migrate-memories-fixture-columns.ts` from the
  BASELINE pin (P4.103's method). Nobody REBUILDS a pair (a rebuild at the
  pin re-encodes every ≥ 512-byte compressed cell and retires the P4.D203
  Class-3 neutrality proofs). Name only MAIN files in an apply run:
  `--report-only` does not report index additions, so an apply over an
  llm-logs partition silently adds the two `idx_llm_logs_*` indexes.
  `almanack-llmlogs-legacy.db` is DELIBERATELY un-widened — never name it.
  P4.108 commits new binary image fixtures under `harness/oracle/fixtures/
  normalize-blob-image/` WITH their generator. `.db-journal` files are
  never fixtures — delete strays (the migrator leaves one per apply).

## §S — Shared contract (binding; byte-identical in every order of this round)

### §S.1 — Nothing crosses a lane boundary this round

No new `Request`/`Response` variant, no new SPA contract, no wire-shape
change. The three places lanes meet are ownership edges, not contracts,
and §R.10 rules each: P4.109's `get_messages` contract is observed by
P4.D215's and P4.110's code through calls neither edits (a site needing
the strict variant is RECORDED, not edited); P4.110's `execute_import`
signature change lands LAST in the cherry-pick order and repoints every
caller itself; P4.D215 and P4.107 widen disjoint fixture pairs. A lane
that discovers it needs a contract STOPs on that item and records the
shape for the unifier.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D215** — bugs 163/164: the auto-title chokepoint | NEW `crates/quilltap-core/src/services/auto_title.rs` + its `mod` line; `services/context_summary.rs` (the fold's title block); `services/title_update_job.rs`; `services/chat_admin.rs` (`chat_regenerate_title`); `services/image_profile_resolution.rs` (`queue_story_background_if_enabled` MOVES OUT); `api/engine.rs` the EXISTING `ChatRegenerateTitle` arm body ONLY if a clock is threaded (§R.10(a)); `help/story-backgrounds.md` (byte-copy); `crates/quilltap-web/tests/fixtures/chat-admin-main.db` (WIDENED in place; delete the stray journal); `title_update_tier3_equivalence.rs`, `chat_regenerate_title_tier3_equivalence.rs`, `context_summary_service_tier3_equivalence.rs` + their cases/builders/corpora (grown); `story_background_job_tier3`, `memory_pipeline_jobs_tier3`, `orchestrator_tier3`, `chat_admin_routes`, `chat_rebuild_summary` — RE-RUN ONLY (edit only if a capture pin names a moved line; say so); this order's header | `db/**` (P4.109), `services/quilltap_import/**` (P4.110), `crates/quilltap-host/**`, `crates/quilltap-web/tests/common/mod.rs` (P4.110), every other committed fixture pair, `apps/web/**`, the drift ledger |
| **P4.107** — the fixture-vintage heal, next list | `crates/quilltap-web/tests/fixtures/{photos,inspector,inspector-nostore,wardrobe-routes,almanack}-main.db` (WIDENED in place); `harness/oracle/fixtures/migrate-memories-fixture-columns.ts` (the recipe header + the pepper list + an index line in `--report-only` — Tier 2); `harness/tools/sweep-results/<this run>.json`; this order's header | Every `.rs` file (a red after the widen is a FINDING, recorded, not fixed here), every test file's logic, `fresh_schema.json`, `chat-admin-*.db` (P4.D215), `almanack-llmlogs-legacy.db` (deliberate), every llm-logs and mount partition, every other committed fixture, `apps/web/**`, the drift ledger |
| **P4.108** — the animated-input decline at the host codec | `crates/quilltap-host/src/image_codec.rs` (the helper, the two gated call sites, the module doc `:26-29`, host unit tests); `crates/quilltap-core/src/services/mount_index/normalize_blob_image.rs` (module doc `:56-72` ONLY — the closed record); `normalize_blob_image_equivalence.rs` + `harness/oracle/cases/normalize-blob-image.ts` (grown); NEW binary fixtures under `harness/oracle/fixtures/normalize-blob-image/` + their generator (committed beside them); P4.104's status header (one sentence, §R.7); this order's header | Every core trait signature (`PixelCodec`, `WebpTranscoder`, `ImageTranscoder`), `services/file_storage.rs`, `files/**`, the non-animated codec methods' behaviour (`convert_to_webp`, `shrink_to_webp`, `resize_step`, `thumbnail_webp`, `transcode`), both census files (P4.110), every committed fixture pair, `apps/web/**`, the drift ledger |
| **P4.109** — `get_messages`' `safeQuery` fallback + `updateMessage`'s `null` + the ui-search regression arm | `crates/quilltap-core/src/db/chats_messages_read.rs` (`get_messages` + a strict sibling), `db/chats_messages.rs` (`update_message`'s wrap), `db/chats_search.rs` (count/find/replace comments; no new log lines — §Survey A1), `services/search_replace.rs` (ONLY if the preview/execute arms need a hunk after the repo moves — say so); the nine `update_message` callers' call-site hunks (§R.10(i)); every OTHER `get_messages` caller whose census row says "strict" EXCEPT P4.D215's and P4.110's files (§R.10(c)); `chats_search_equivalence.rs`, `search_replace_equivalence.rs`, `chats_messages_ops_tier2_equivalence.rs`, `ui_search_equivalence.rs` + their cases/specs/builders (grown); NEW `crates/quilltap-harness/tests/get_messages_caller_census.rs` (Tier 2); P4.105's status header (one sentence, §R.7); this order's header | `db/chat_message_fts.rs`, P4.D215's four service files + `auto_title.rs`, `services/quilltap_import/**`, `crates/quilltap-web/tests/common/mod.rs`, every committed fixture pair (plant on per-run COPIES), `apps/web/**`, the drift ledger |
| **P4.110** — the DRY smalls: the shared census lexer, `materialize_salon_instance`, `execute_import`'s required codec | NEW `crates/quilltap-harness/tests/source_census/mod.rs`; `blob_write_sites_census.rs` + `compressed_column_write_sites_census.rs` (lexers DELETED, repointed); the ten `rust_sources`/`core_src_root` copies (Tier 2, listed in §Survey); `crates/quilltap-web/tests/common/mod.rs` (+ `materialize_salon_instance`); `chat_informs_rest_routes.rs`, `messages_swipe_sse_route.rs`, `chat_informs_dispatch_wire.rs` (the local copies DELETED, callers repointed); `crates/quilltap-core/src/services/quilltap_import/{mod,reset,seed}.rs`; the `execute_import(` call hunks in `api/system_qtap.rs` + `services/character_archive/service.rs` (§R.10(i)); every harness test calling `execute_import(` (`system_import_state.rs`, `seed_avatars_equivalence.rs`, `qtap_import_equivalence.rs`); P4.104's status header (one sentence, §R.7); this order's header | `stream_watchdog_wrap_census.rs`'s and `zod_issues_home_guard.rs`'s deliberately different scanners, `db/**` (P4.109), P4.D215's service files, `crates/quilltap-host/**` (P4.108), every committed fixture pair, `apps/web/**`, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix, beyond the pre-declared spills in §R.10(i) — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note). Budget 50–70 GB of disk per Rust lane
(`df -h ~` showed 115 GB free at planning; five worktrees is five
`target/` dirs — run at most THREE Rust lanes concurrently and reclaim
`target/debug/incremental` between waves; P4.107 builds only what its
readers need).

1. **§R.2 probe** — PASS (opening, and again before every regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in BOTH
   feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0
   where the order predicts a change; a recorded `0` where it predicts
   none), and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp` / by-label diff) where the family is
   deterministic across regens; where it is not
   (`regenerate-at-both-pins-and-cmp-is-not-universal`), compare the
   normalized rows by case and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`
   (a widened `.db` is the ONE exception — the pair is the artifact).
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast -- --nocapture` with the lane's
   env block (its own families' vars; every other family's var WITHHELD
   so it SKIPs honestly rather than failing on a deleted `/tmp` path):
   every binary's `test result:` line read, zero failures bar the
   standing reds §R.5 names (recorded by name if their vars are in the
   block — P4.107 expects its five GREEN, P4.D215 expects
   `chat_regenerate_title_tier3` GREEN), the lane's families confirmed RUN
   by non-zero duration.
9. **Source censuses and guards** the change moves or must not move, re-run
   by name with any constant recounted and the arithmetic in the comment:
   `help_tree_embed_guard` + `host_help_docs_boot` (P4.D215 — UNMOVED at
   126, run to prove it); `blob_write_sites_census` +
   `compressed_column_write_sites_census` (P4.110 — counts UNCHANGED; P4.108
   — run to prove it does not move); `dispatch_wrong_type_census` (every
   lane — UNMOVED at 446; no verb lands); the NEW
   `get_messages_caller_census` (P4.109, if Tier 2 lands).
10. **Tier R** (`crates/quilltap-cli/tests/cli_differential.rs`) at the
    baseline pin for P4.109 and P4.110 (both reach code the CLI links —
    266/0 expected; run it, do not assert it).
11. **Commit per `.claude/commands/commit.md`** (one unit per commit where
    the order tiers them; CHANGELOG entry + version bumps per §R.8; the
    status-log lane record appended with the regen commands AS RUN, the
    mutation table, the pre-fix counts, the measurements, and every
    finding outside the lane's ownership named for the unifier). The lane
    closes by updating its own order's status header with what landed,
    what was deferred, and what it measured that the order got wrong.
