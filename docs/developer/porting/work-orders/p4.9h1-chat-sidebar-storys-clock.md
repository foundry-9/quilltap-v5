# Work order P4.9H1 — **the ChatSidebar SPA vertical + the Story's Clock (the episodic round-3 SPA switch)** — round baseline `8bf3cb5f`

> **STATUS: CLOSED — UNIFIED ON MAIN (2026-07-22, the episodic round-3
> unification).** Tier 1 and tier 2 complete: the sidebar scaffold + the
> collapsed strip, the participants section, **the Story's Clock**, the Chat /
> Visibility / Organize drawers, the four-affordance reconciliation with the
> header + turn bar, and two live Playwright beats. The per-chat Core-whisper
> override (F3) and the chat-tier State-Editor opener both landed, closing the
> state-cascade round's deferral. Tier-3 deferrals are enumerated in the lane
> record (`status-log.md`): every participant-mutation control, the Concierge
> tri-state, Agent Mode, avatar-gen, Tools / Run Tool, Rename, Continue
> Elsewhere, Merge In, Export, and the whole Edit Content section. ⚠ The lane
> also found a **v4 route bug** — the chat GET never projects `timelineMode` (or
> three sibling columns) — ported faithfully and banked for v4.
>
> This is the M6 backlog's `p4.9h` **split**: P4.9H1 = the ChatSidebar
> vertical (this order). The rest of the `p4.9h` bucket — the Settings
> memory-tab cards (embedding profiles / dedup / regen summaries), the
> prompt library + Core Whisper global card, template tag pickers, the
> memory-creation dialog — stays BANKED as `p4.9h2` (unwritten; see
> `m6-screen-parity.md`). Do not scope-creep into it.

## Preamble

**v4 reference baseline for this round: `8bf3cb5f` (4.9-dev) —
verified at planning (2026-07-22): v4 HEAD == `8bf3cb5f`, tree clean.**
At lane start, run in `~/source/quilltap-server`:

```bash
git log 8bf3cb5f..HEAD --oneline
```

**If HEAD moved, STOP and report.**

**Sibling lanes (run in parallel, own worktrees):** P4.d14
(creation-side + fold, Rust) and P4.d15 (recall-on-reference part 2,
Rust). **They own `crates/**` — this lane makes NO Rust/server
changes.** If a sidebar section needs a server verb v5 lacks, it
becomes a tier-3 loud deferral in this order, never a server edit.

You run in a worktree. E2e: this lane OWNS the in-round Playwright run
(the server lanes defer theirs to the unifier). Build the SPA against
**main's release binaries** (the `p4.6bi` memory: reuse main's RELEASE
bins for e2e; the `e2e-mutation-check-needs-a-rebuilt-dist` memory:
mutations prove nothing until `ng build` rewrites the bundle). Port
4319 is yours in-round; the `e2e-port-4319-cross-worktree-kill` memory
applies only if a sibling accidentally runs the suite — they are
ordered not to.

**Why this lane exists in the episodic round:** the campaign's SPA
deliverable is v4's **"The Story's Clock"** timeline-mode switch —
and its v4-faithful home is the Salon chat sidebar
(`components/chat/ChatSidebar.tsx`), which v5 has never ported (the
M6 doc routes it to `p4.9h`; the state-cascade round's "chat-tier
State-Editor opener" deferral also rides it). Rather than invent a
divergent placement for one select, this lane ports the sidebar
vertical itself.

## Mandate

Deliver the Salon chat sidebar as a v5 Angular vertical over the
FROZEN server surface on main: the collapsible/resizable sidebar
scaffold with v4's section model, the participants section, the
chat-settings section — **with the Story's Clock select (v4 copy
verbatim, steampunk register) wired to the ported chat-PUT
`timelineMode` arm** — and the section-by-section reconciliation with
the affordances v5 previously parked elsewhere. Live Playwright beats
prove the Story's Clock round-trip and the sidebar walk. **No server
changes; no `crates/**` edits.**

## Survey-verified starting points (fresh, 2026-07-22)

### v4 (all at `8bf3cb5f`; line numbers verified)

`components/chat/ChatSidebar.tsx` — 1,689 lines. Map:

| What | Where |
|---|---|
| Collapse/width/resize/narrow machinery (localStorage-backed) | :105–:400 (`getInitialCollapsedState` :241, width :246, narrow strip :254/:399) |
| Section model (`SectionId`, default open `'participants'`) | :242 |
| Aurora Core-whisper per-chat tri-state override state | :191 (+ cadence control :1425) |
| Settings section — the Concierge tri-state | :1100 (`handleConciergeStateChange` :1037) |
| Agent Mode | :1116 |
| Roleplay Template select | :1129 (`handleRoleplayTemplateChange` :936) |
| **The Story's Clock** (label, two options "Real time"/"Story time", the two steampunk helper paragraphs — copy VERBATIM) | **:1147–1165**; `handleTimelineModeChange` :1011–1035 — `PUT /api/v1/chats/[id]` body `{ chat: { timelineMode: value } }` :1017 |
| Project entry | :1166 |
| Image Provider select | :1179 (`handleImageProfileChange` :959) |
| Announce Generated Images tri-state | :1200 (`handleAlertImagesChange` :981) |
| Auto-generate Character Avatars toggle | :1218 (`handleAvatarGenToggle` :1065) |
| Tools (settings modal) entry | :1230 |
| Run Tool entry | :1243 |
| Regenerate Story Background entry | :1256 |
| Whisper-visibility toggle (show/hide private whispers) | :1337–:1361 |
| Export | :1510 (`handleExport`) |
| Plumbing | `app/salon/[id]/SalonView.tsx:1737` passes `timelineMode`; `app/salon/[id]/types.ts:259` (`timelineMode?: 'realtime' | 'narrative' | null`) |

The episodic drift's whole SPA delta inside the sidebar is +53 lines
(the Story's Clock block + saving state + handler) — the REST of the
sidebar is pre-`8bf3cb5f` v4 surface being ported for the first time.
The per-chat Core-whisper override chain is the M6 doc's **F3
finding** (three inheritance levels; v5 has only the character level).

### v5 (verified 2026-07-22)

- The chat-PUT `timelineMode` accept arm is LIVE on main (round 2,
  P4.d13 tier-2 item 9: set / explicit-null / invalid-400 — the
  salon-mutations family pins it). The chat read shape carries
  `timelineMode` (round 1 data layer).
- **Existing v5 placements that overlap sidebar entries** (reconcile,
  don't duplicate): the All-Whispers toggle (P4.6ba, Salon header —
  overlaps :1337), the Regenerate Background entry (P4.6ao/ap —
  overlaps :1256), the Edit-Enclave header entry (P4.6ae rider), the
  custom-tools composer popup (P4.6ay/ba — adjacent to :1230/:1243).
  Rule: v4's sidebar is the faithful home; where v5 already surfaced
  an affordance elsewhere, either move it into the sidebar (if v4 has
  it ONLY there) or keep both (if v4 has both) — decide per entry
  from v4's actual layout and DOCUMENT each call in the lane record.
- **Survey duties at lane start (server-verb availability per
  section)** — verify each against the dispatch surface and tier
  accordingly: participants add/remove/play-as (the
  `QT_ORACLE_CHATSPARTS` family suggests the repo ops are ported —
  confirm the dispatch verbs), the per-chat Concierge / agent-mode /
  image-profile / alertImages / avatar-gen PUT keys (the nested-bag
  chat PUT applies Zod-PARSE semantics — the `p4.6c` memory), the
  per-chat Core-whisper override keys, chat export, Run Tool.
- The banked v4 drift `8e4b00d4` (whisper-visibility client fix +
  `whisper-visibility.ts` helper + tests) named ChatSidebar's toggle
  surface as its port target — absorb it here (check whether P4.6ba
  already ported the helper; if so, just record closure).

## Tiered deliverables

### Tier 1 — must land

1. The sidebar scaffold: collapse/expand, resize with persisted width,
   the narrow-viewport strip, the section model (v4's localStorage
   keys — check the quick-hide precedent for key fidelity,
   `p4.9d` memory).
2. **The Story's Clock select** — v4 copy verbatim (:1147–1165
   including both helper paragraphs), wired to `chatUpdate`
   `{ chat: { timelineMode } }`, saving-state disable, and the chat
   read-shape seed.
3. The participants section (v4's default-open section) at whatever
   depth the ported server surface supports — read/list at minimum;
   mutation affordances per the survey.
4. The settings selects whose PUT keys exist (roleplay template at
   minimum — the listing surface is ported, P4.6p).
5. Angular component specs (via `ng test` — the `p4.d11` memory: not
   raw vitest) + **live Playwright beats**: the Story's Clock
   PUT round-trip (flip to Story time → reload → persisted; flip
   back), plus the sidebar open/collapse walk. Locator hygiene: the
   `added-affordance-breaks-sibling-by-name-locators` memory.

### Tier 2 — should land

6. The remaining settings entries whose verbs exist (image profile,
   alert images, avatar-gen, Concierge, agent mode; the
   Tools/Run-Tool/Regenerate-Background/Edit-Enclave entries
   reconciled with their existing v5 homes).
7. The whisper-visibility toggle reconciliation (+ the `8e4b00d4`
   helper closure record).
8. The per-chat Core-whisper override (F3) IF the PUT keys exist
   server-side; else tier 3.
9. The project entry (navigate to the ported Prospero surface).

### Tier 3 — explicit loud deferrals (enumerate in the lane record)

- Any section whose server verb is missing (likely candidates per the
  survey: chat export, Run Tool modal depth, the Core-whisper override
  if keyless) — visible affordance may render disabled with a
  loud hint ONLY if v4 has an equivalent degraded state; otherwise
  omit and enumerate.
- The rest of the `p4.9h` bucket (`p4.9h2` — settings memory cards,
  prompt library, memory-creation dialog): out of scope by
  definition.
- The chat-tier State-Editor opener (the state-cascade deferral) —
  absorb ONLY if v4's sidebar hosts it (survey; the v4 State Editor
  entries were modal-based). If it doesn't fit tier 2, enumerate.

## The differential requirement (restated)

SPA lanes verify by (a) porting v4's component behavior with Angular
specs mirroring v4's tests where they exist (`NewChatForm.test.tsx`
precedent from round 1), (b) the live Playwright beats against the
real server, and (c) copy fidelity — the Story's Clock strings must be
byte-identical to v4's (steampunk register is load-bearing; the
`writing-voice` convention). No server differential is owed (no server
change is permitted).

## Fixtures

Consumes the committed e2e instance fixtures on main as-is. If a beat
needs seeded state (e.g. a narrative-mode chat), seed via the API in
the beat, not by editing committed fixtures
(`e2e-fixture-before-run-order` memory: global-setup copies fresh each
run).

## Shared contract (binding)

- **This lane owns `apps/web` exclusively; it edits nothing under
  `crates/**` or `harness/**`.** The server lanes (P4.d14/P4.d15) are
  ordered not to touch `apps/web`.
- The server surface this lane rides is FROZEN ON MAIN: the chat GET
  shape's `timelineMode` field and the chat-PUT nested bag
  `{ chat: { timelineMode: 'realtime' | 'narrative' | null } }`
  (invalid values → 400 `Validation error`). No sibling lane changes
  it; if it seems wrong, STOP and report.
- Version bumps: this lane owns the `apps/web` SPA version
  (per-commit); it bumps no crates.
- `CHANGELOG` + `status-log.md` append-only for everyone.

## Verification gate (before declaring done)

- `ng test` full (all files green) and `ng build` clean, from
  `apps/web` (the `p4.6au-av-7c` memory: playwright runs FROM
  `apps/web`).
- **Full Playwright** from a fresh dist + main's release binaries:
  green, zero skips, the new beats ACTIVE.
- `git diff <branch-base>..HEAD -- crates harness` = 0 lines.
- Per-commit rule: `.claude/commands/commit.md`. Append lane records
  to `status-log.md`; update this order's status header at lane end.
