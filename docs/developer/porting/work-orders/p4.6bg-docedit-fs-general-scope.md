# Work order P4.6bg — the fs seam (lane BG): the doc-edit host-filesystem branches, the `general` scope, and store conversion

> **STATUS: CLOSED with ONE loud tier-2 deferral — units 3/4/5/6-part-1
> UNIFIED on main (2026-07-21, the P4.6bg-remainder unification); tier 1 is
> COMPLETE.** Landed on top of unit 1: the tool-site fs I/O (shared /
> document_ui / file_management / text — all six file-management verbs,
> the new-blank-document path, the doc_grep/doc_list_files fs walks); the
> NEW dedicated `doc_fs_equivalence` differential family (21 fs-scoped ops
> + a byte-exact fs-tree diff over `QT_ORACLE_DFS`; the readdir-order and
> uuid-collapsed-sort normalizations documented in the lane record); the
> engine `files_dir` wire (`Some(base_dir/files)` through all 11 doc-verb
> dispatch arms) + the Document-Mode operator fs surface; the
> `workspace-document-standalone-flow` beat flipped to the LIVE
> general-scope round-trip; and **P4.6bf's inherited blob-WebP wire
> (unit 6 part 1)** — the handlers take the S1 `webp` param, the engine
> passes `ReadyEngine.blob_webp`, the `#[allow(dead_code)]` is gone, and
> the scriptorium WebP beat SELF-ACTIVATED (portrait.png → portrait.webp).
>
> **One DELIBERATE v5 divergence (documented in the lane record):**
> `resolve_general_path` does an idempotent `create_dir_all(<files>/_general)`
> before resolving — v4 never creates `_general`, so its general new-blank
> THROWS on a fresh instance (a latent v4 quirk of safeRealpath's
> reversed-tail join). Inert in every differential (fixtures pre-create
> `_general`).
>
> **STILL OPEN (tier 2, loud): unit 6 part 2 — the conversion port.**
> `services/mount_index/conversion.rs` (v4 `lib/mount-index/conversion.ts`,
> filesystem↔database convert/deconvert) + un-refusing
> `api/mount_files.rs::mount_convert`/`mount_deconvert` (guards already
> LIVE) + its tier-2 differential (exact DB + tree diff; ≥1 write-lock
> guard case). Resume with a fresh v4 drift-check — **v4 drifted to
> `e2eb3d21` during this lane** (New-Chat picker: components/help/versions
> only, ZERO lib/ — no oracle family affected; the owed item is a P4.d
> SPA re-port of the New-Chat picker behavior on the ported P4.6q
> vertical).
>
> Tier-3 deferrals stand unchanged: the chokidar-equivalent watcher,
> `filesSync`, cleanup-stale disk keys, the `quilltap docs` CLI,
> `DocumentTextExtractor`, the FilePreviewText rich-stack deferrals. The
> flagged P4.d7 dup-name divergence remains a spawned follow-up.
>
> *(The unit-1 partial header follows for history.)*
> **STATUS: OPEN (partial) — unit 1 UNIFIED on main (2026-07-21, the codec
> + fs seam round unification); RESUME AT UNIT 3.** Landed and on main: the
> path-resolver host-filesystem branches (general / fs-mount /
> legacy-project-fallback; `safe_realpath` walk-up with v4's reversed-tail
> quirk reproduced; `verify_path_is_within_base`; byte-exact
> codes/messages), the S2 `files_dir` thread (`resolve_doc_edit_path`
> trailing param + `DocEditToolContext`/`DocumentAccessContext` fields —
> **every call site currently passes `None`**, so behavior is
> byte-identical to pre-lane), and the `doc_edit_path_resolver_equivalence`
> fs extension (10 new cases, 25 resolve + 6 uri, canonical-scratch
> `__ROOT__` sentinel recipe; green over a fresh `7e6d13e5` oracle). The
> lane branch/worktree were absorbed and deleted — resume FROM MAIN.
>
> **Still OPEN (the resume list):** unit 3 (the tool-site fs I/O —
> `shared.rs` read/write dispatch, `document_ui` new-blank + `fs.stat`,
> `file_management` fs create/rename/delete/move, `text` fs walks), unit 4
> (the doc_fm/doc_ui/doc_text fs differential extensions), unit 5 (the
> engine `files_dir` wire flipping `None` → `Some(base_dir/files)` +
> `api/documents` general arms + the `workspace-document-standalone-flow`
> beat flip), tier-2 unit 6 (`conversion.rs` + un-refusing
> convert/deconvert + the S1 `store_mount_file` signature change).
> **Unit 6 also carries P4.6bf's inherited AT-UNIFY item:** once the
> handlers take `webp: Option<Arc<dyn WebpTranscoder>>`, wire the engine.rs
> call sites to pass `EngineAssembly.blob_webp.clone()`, drop the
> `#[allow(dead_code)]` on `ReadyEngine.blob_webp`, and the probe-gated
> scriptorium WebP beat self-activates.
>
> **⚠ Flagged out-of-scope divergence (spawned as a follow-up, not this
> lane's):** since the P4.d7 NOCASE drift, v4's `findByName` counts
> OVERLAID (disambiguated) names while v5's `db/doc_mount_points.rs
> find_by_name` reads the raw column — dup-named stores diverge on
> name-vs-id `qtap://` form. See the unit-1 lane record in the status log.
>
> *(The original status header follows for history.)*
> **STATUS: OPEN — lane BG of the TWO-lane P4.6bf ∥ P4.6bg codec + fs seam
> round** (planned 2026-07-20). **v4 reference baseline: HEAD `7e6d13e5`**
> (4.8.0-dev.92, `~/source/quilltap-server`) — run
> `git log 7e6d13e5..HEAD --oneline` at lane start; **if HEAD moved, STOP and
> report** (⚠ v4 is mid-release-checklist: the 4.8.0 tag/version commit may
> land any day — that is drift, stop). At planning the v4 tree was CLEAN at
> exactly `7e6d13e5`, so oracles regenerate from the checkout directly (pin a
> detached worktree only if that changes — the
> `oracle-regen-pinned-v4-worktree` memory).
>
> Sibling lane: `p4.6bf-avatar-preview-blob-codec-wire.md`. The **Shared
> contract**, **Ownership**, and **Verification gate** sections below are
> BINDING and reproduced verbatim in both orders. You run in a worktree with
> its own Rust binaries (built once — the P4.6af memory); don't use
> `git stash`.
>
> ⚠ All fs work runs on temp trees you create (the harness target dir or
> `std::env::temp_dir()`); **never** point anything writable at real
> instance data.

## Mandate

Close the standing **FsSeam** deferral: the doc-edit tool surface's
host-filesystem branches, deferred since the d3a path-resolver port because
the differential corpus was all-`database`. Since the workspace-tabs
remainder round this deferral has a **user-visible affordance**: the
standalone Document Mode picker's top-level "New blank document" targets
the `general` scope, which `doc_edit/path_resolver.rs:194` refuses on
EVERY host (`DocEditScope::General => Err(FsSeam)`) — pinned loudly by the
`workspace-document-standalone-flow` beat. After this lane: the `general`
scope works end-to-end (create → edit → autosave round-trip on
`<files>/_general`), `filesystem`/`obsidian` mounts are reachable by the
doc-edit tools, and (tier 2) the Scriptorium's convert/deconvert verbs come
off their refusal arms. **Every fs branch ships with fs-backed differential
coverage — the corpus finally exercising the seam is the point of the
lane, not an accessory.**

**Must NOT touch:** engine.rs outside the S3 region; `quilltap-host` (lane
BF); the wardrobe/codec surfaces; the `WebpTranscoder` trait or its
consumers beyond the S1 signature change; any SPA component; the
mount-file-ops READ/LIST/mutation surface itself (P4.6v/y — frozen; you
consume it in conversion, you don't edit it).

## Survey-verified starting points (surveyed 2026-07-20, v4 @ `7e6d13e5`)

- **v5 seam sites (complete list — every `FsSeam` mention):**
  `crates/quilltap-core/src/doc_edit/path_resolver.rs` (`:15` module docs
  naming the three deferred branches; `:94` the variant; `:194` the General
  arm; `:340`/`:382`/`:389` the fs-mount + legacy-fallback early returns);
  `tools/doc_edit/shared.rs` (`:24`, `:233-241` the surfaced message,
  `:656`+ the read/write dispatch — database-only today);
  `tools/doc_edit/document_ui.rs` (`:21-26` the new-blank-document module
  header, `:108`, `:157-172` the `fs.stat` arms);
  `tools/doc_edit/file_management.rs` (`:8-9`, `:228` fs-dest detection,
  `:750` the FsSeam message); `tools/doc_edit/text.rs` (`:596`, `:686`,
  `:831-907` the fs walks, `:971-973` the `_general` walk skip).
- **v4 oracle code:** `lib/doc-edit/path-resolver.ts` (838 lines) —
  `safeRealpath` `:175` (realpath with the walk-up-to-deepest-existing-
  ancestor fallback; carry v4's iCloud-symlink why-comment), 
  `verifyPathIsWithinBase` `:203` (normalize + separator-suffixed prefix
  check), the `general` arm `:569-600` (base =
  `path.join(getFilesDir(), '_general')`; escape → the exact
  `Path escapes general storage boundary` error — port codes/messages
  byte-exact), the legacy project fallback `:477`/`:541-542`
  (`<filesDir>/<projectId>`, taken only when no official mount), the
  fs-mount realpath checks `:443-451`/`:514-517`/`:545-550`.
  `getFilesDir()` = `<base>/files` (`lib/paths.ts:310`). The tool-side fs
  arms live in v4's doc-edit tool implementations the existing jest oracles
  already import — extend those cases, don't write new harnesses.
- **The differential families to EXTEND (all exist):**
  `doc_edit_path_resolver_equivalence` (`QT_ORACLE_DPR`, tsx case
  `harness/oracle/cases/doc-edit-path-resolver.ts`),
  `doc_fm_equivalence` (`QT_ORACLE_DFM`, jest `doc-fm.test.ts`),
  `doc_ui_equivalence` (`QT_ORACLE_DUI`, jest `doc-ui.test.ts`),
  `doc_text_equivalence` (jest `doc-text.test.ts`). Recipes: the
  `doc-edit-tool-differential-recipe` and `jest-real-db-oracle` memories.
- **The fs-in-fixture recipe:** the P4.6v basePath **sentinel rewrite**
  (the `p4.6v-mount-file-ops-lane` memory) — the corpus materializes a
  temp tree from a committed manifest and both sides rewrite base-path
  sentinels to the temp dir at run time. Reuse it for the fs-mount cases
  AND the `_general` tree.
- **The beat to flip:**
  `apps/web/e2e/workspace-document-standalone-flow.spec.ts:65-104` pins
  the general-scope refusal (`This document could not be opened.`). Flip it
  to the create → edit → flush-on-blur autosave → reload round-trip the
  spec's own header sketches. Because you thread `files_dir` at your own
  engine.rs call sites (S2/S3), this activates **in-lane** — no AT-UNIFY
  gate.
- **Tier-2 conversion:** v4 `lib/mount-index/conversion.ts` —
  filesystem↔database store conversion; preserves every `doc_mount_files`
  row and its `doc_mount_chunks` children (embeddings untouched — no
  re-embed), only `source` flips and bytes move;
  `ConvertResult { mountPointId, filesMigrated, documentsWritten, blobsWritten, filesSkipped, errors[] }`,
  `DeconvertResult { mountPointId, filesWritten, blobsWritten, bytesWritten, errors[] }`.
  The v5 refusal arms: `api/mount_files.rs:863` (`?action=convert`) /
  `:899` (`?action=deconvert`) — the capability guards
  (`derive_mount_capabilities`, incl. the mid-conversion write-lock at
  `mount_points.rs:79`) are already LIVE; only the conversion body refuses.
  No codec is involved (bytes move verbatim, sha256 preserved), so tier-2
  DB diffs can be exact.

## Tiered deliverables

**Tier 1 (must land):**

1. **The path-resolver fs branches**, byte-exact codes/messages:
   `safe_realpath` (walk-up port, why-comment carried),
   `verify_path_is_within_base`, the `general` resolution, the
   `filesystem`/`obsidian` mount resolution, the legacy project fallback.
   `ResolveError::FsSeam` remains ONLY for the `files_dir: None` case (the
   refusal text unchanged); update the module headers that call the seam
   "deferred".
2. **The tool-site fs arms**: `shared.rs` read/write dispatch on fs-backed
   resolved paths; `document_ui.rs` new-blank-file + `fs.stat`;
   `file_management.rs` fs create/rename/delete/move + fs-dest detection;
   `text.rs` fs walks including the `_general` walk. All behind the S2
   `files_dir` thread; `None` keeps today's refusals exactly.
3. **The corpus extension + regen**: fs-mount + `general`-scope cases in
   all four families above (temp tree from a committed manifest, sentinel
   rewrite both sides), regenerated at `7e6d13e5` and re-run BY NAME with
   the NDJSON grepped (the `oracle-regen-silent-stale-pass` memory —
   an erroring builder leaves the old fixture). Cover at minimum: general
   create/read/write/rename/delete, traversal escape (`../`) on both
   general and fs-mount bases, the symlink-ancestor case `safeRealpath`
   exists for, the legacy project fallback, and a new-blank-document
   create.
4. **The S2/S3 engine thread** at your own doc-verb dispatch call sites
   (verify the config accessor at lane start), and the
   `workspace-document-standalone-flow` beat flipped to the live
   round-trip in-lane.
5. `api/documents.rs`: the general-scope arms of the standalone verbs
   (P4.6w shapes are FROZEN — the `body: Value` field shapes are pinned by
   the P4.6w handler differentials; you're adding the scope's reachability,
   not new shapes).

**Tier 2 (should land):**

6. **`services/mount_index/conversion.rs`** (new): the convert/deconvert
   port per the survey above, un-refusing `api/mount_files.rs:863`/`:899`
   behind the live guards (take the S1 signature change on the two
   `store_mount_file` handlers in the same file while you're the owner —
   BF's assembly field is its half; the engine connection is the
   unifier's). Tier-2 differential: drive v4's REAL conversion on a
   fixture copy + temp tree; diff the DB structurally AND assert the
   resulting tree (paths + byte-identical contents — no codec runs).
   Mind the conversion-status write-lock arms (`converting`/
   `deconverting`) — pin at least one guard case.

**Tier 3 (explicit loud deferrals — never silent):**

- The chokidar-equivalent fs watcher + the db-store-event emitter chain
  (host cadence — its own future round, per P4.6y); `filesSync`;
  cleanup-stale disk keys; the `quilltap docs` CLI;
  `DocumentTextExtractor` (a NAMED EXCLUSION this round — see lane BF
  tier 3 for why); FilePreviewText and the other P4.6af rich-stack
  deferrals (untouched).

## The differential requirement, restated

Every fs branch this lane opens must be exercised by an oracle case that
drives v4's REAL `lib/` code over the same materialized tree
(`.claude/commands/commit.md` §1). The four families above are the
harnesses; extending them is tier 1, not optional. Normalize only what the
tiers allow (timestamps, minted UUIDs via remap, the temp-dir sentinel);
paths and error strings diff exact.

## Fixtures

You own: the committed fs-tree MANIFEST(s) the harness materializes, any
new rows added to existing doc-edit fixture DBs, and regenerating every
oracle family you extend. The e2e salon fixture is a consumer
(`e2e-fixture-before-run-order` memory) — the standalone beat's general
scope needs NO new fixture rows (it creates its own document), but verify
against a fresh copy, not a warmed one.

## Shared contract (BINDING — identical in both orders)

- **S1 — the blob-WebP seam.** Lane BF adds
  `pub blob_webp: Option<std::sync::Arc<dyn crate::services::mount_index::blob_transcode::WebpTranscoder>>`
  to `EngineAssembly` (default `None`; production host = the live codec).
  Lane BG changes the two `store_mount_file` dispatch handlers
  (`api/mount_files.rs`, the functions containing `:595`/`:725` at
  planning) to take a trailing
  `webp: Option<std::sync::Arc<dyn WebpTranscoder>>`, using it when `Some`,
  else the existing `RefusingWebpTranscoder` (behavior unchanged on
  `None`). **AT-UNIFY:** the unifier edits the engine.rs call sites of
  those two handlers to pass the assembly's `blob_webp.clone()`.
- **S2 — the doc-edit files-dir thread (lane BG).** The `api/documents.rs`
  + `tools/doc_edit` entry points that reach the host filesystem gain a
  trailing `files_dir: Option<&std::path::Path>` (`None` preserves today's
  FsSeam refusal). v4 semantics: general base = `<files_dir>/_general`,
  legacy project fallback base = `<files_dir>/<projectId>`
  (`lib/paths.ts:310` — files dir = `<base>/files`). Lane BG passes
  `Some(&self.inner.config.base_dir.join("files"))` at its own dispatch
  call sites (verify the exact config accessor against `engine.rs:2971`'s
  `system_data_dir(&self.inner.config.base_dir)` idiom at lane start).
- **S3 — engine.rs REGION SPLIT.** Both lanes edit
  `crates/quilltap-core/src/api/engine.rs` in named disjoint regions ONLY:
  lane BF touches the `EngineAssembly` struct + its defaults/builders +
  assembly-construction plumbing; lane BG touches ONLY the doc-verb
  dispatch call-site lines (the documents/doc-edit match arms). Any merge
  conflict resolves by taking both edits; the unifier verifies the regions
  stayed disjoint.
- **S4 — version-bump ownership.** BF bumps `quilltap-host` +
  `quilltap-core`; BG bumps `quilltap-core` + `quilltap-harness`; either
  lane bumps the SPA only for e2e-spec changes. Identical bumps merge
  silently — the unifier RECOUNTS from the commit history (the P4.6aw
  memory).
- **S5 — the differential/live split.** Committed differentials keep
  canned/refusing seams (the byte-exact fallback arms stay pinned); live
  wires are proven by host tests + Playwright beats. A lane that extends a
  corpus owns regenerating it (BF: wardrobe tier 2; BG: the doc-edit
  families).

## Ownership (BINDING — identical in both orders)

| Path | Owner |
|---|---|
| `crates/quilltap-host/**` | **BF** |
| `crates/quilltap-core/src/api/engine.rs` | REGION SPLIT per S3 |
| `crates/quilltap-core/src/api/wardrobe.rs` | **BF** (docs/comments only — the contract is frozen) |
| `crates/quilltap-core/src/services/mount_index/blob_transcode.rs` | **BF** (doc updates; trait FROZEN) |
| `crates/quilltap-core/src/services/sillytavern.rs` | **BF** (tier 2 only) |
| `crates/quilltap-core/src/api/mount_files.rs`, `api/documents.rs` | **BG** |
| `crates/quilltap-core/src/doc_edit/**`, `src/tools/doc_edit/**` | **BG** |
| `crates/quilltap-core/src/services/mount_index/conversion.rs` (new) | **BG** |
| `crates/quilltap-harness/tests/wardrobe_routes_equivalence.rs` + `harness/oracle/cases/wardrobe-routes*` | **BF** |
| `crates/quilltap-harness/**` (everything else) + `harness/oracle/cases/**` (doc-edit families) | **BG** |
| `apps/web/e2e/wardrobe-flow.spec.ts`, `scriptorium-flow.spec.ts` | **BF** |
| `apps/web/e2e/workspace-document-standalone-flow.spec.ts`, `salon-documents-flow.spec.ts` | **BG** |
| `apps/web/src/**` (components) | **NEITHER** — STOP and report if a component change seems needed |
| `docs/CHANGELOG.md`, `docs/developer/porting/status-log.md` | append-only, both (union-merge) |

## Verification gate (run before declaring done)

1. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets
   -D warnings` AND with `--features quilltap-core/native-transport`.
2. `cargo test --workspace` — zero failures; your differentials re-run BY
   NAME over freshly regenerated oracles, output grepped for SKIP
   (`--nocapture`; the P4.d3 memory).
3. If e2e specs changed: `ng build` clean; Playwright for YOUR changed
   specs in isolation from the worktree's own binaries + fresh dist. The
   FULL serialized suite is the unifier's gate if port-4319 contention
   with the sibling lane makes a clean in-lane run unreachable (the
   `e2e-port-4319-cross-worktree-kill` memory + the workspace-remainder
   precedent) — say so loudly in the lane record if you defer it.
4. Every commit per `.claude/commands/commit.md` (equivalence-test check,
   invariants, version bumps per S4, CHANGELOG). Append the lane record to
   `status-log.md` at lane close, including any AT-UNIFY table.
