# Work order P4.57 — tri-state decode-once for `taboo` + `brahma-console` (the P4.56 recorded lead)

**Status:** OPEN

## Preamble

- **v4 baseline for this round: `a14a1811`** (v4 main, HEAD at planning
  2026-08-23). This is a **v5-internal consolidation lane** — no v4
  drift to absorb; the baseline matters only for oracle regens. The two
  commits between `f8973813` and `a14a1811` are NO-PORT (`65f3476e`
  dispositioned; `718c9ada` pure CI/bundler); `a14a1811` is the sibling
  lanes' business.
- **Drift-check at lane start.** `git log a14a1811..main --oneline` AND
  `git diff main bugfix -- lib/ app/ packages/`. If v4 moved, **STOP**
  and report (a drift commit could land on the settings surface this
  lane rewires).
- **Sibling lanes:** P4.D106/P4.D107/P4.D108 (server, image surfaces),
  P4.D109 (SPA). Fully disjoint domains — no shared contract needed;
  see Ownership. This lane is the round's ONLY owner of
  `api/{types,engine,settings}.rs` and
  `quilltap-web/src/text_replacements_routes.rs`.
- **Worktree:** per the `carryout` skill; edit inside the worktree, not
  main. `docs/CHANGELOG.md` + `status-log.md` append-only.

## The scope (P4.56's recorded lead, surveyed 2026-08-23)

P4.56 built the decode-once shape for data-retention and recorded:
*"taboo/brahma still re-derive the tri-state at three call sites each —
the data-retention edge demonstrates the decode-once alternative."* The
Taboo §3 lesson (a hand-built edge variant collapsed absent/null/value
and made the differential blind — the `double_option` dispatch-leg bug)
is the class this lane retires structurally.

**The reference implementation** (data-retention, P4.56):
- Type: `crates/quilltap-core/src/api/types.rs:1908-1910` —
  `DataRetentionSettingsUpdate { stale_chat_days }` with
  `#[serde(default, rename, deserialize_with = "double_option")]`; the
  helper at `:3036-3041`.
- Edge: `crates/quilltap-web/src/text_replacements_routes.rs:189-256`
  `data_retention_settings_put` — **decodes THROUGH `CoreRequest`
  itself** (retain the key, inject `"type"`, `from_value::<CoreRequest>`).
  The comment at `:203-211` names the exact defect this prevents.
- Engine: `api/engine.rs:3934-3949` (still rebuilds a `bag` — the
  second derivation). Handler: `api/settings.rs:2832-2860` (re-derives
  via `bag.get(...)` — the third).

**The two adoption targets, three sites each:**

- **Taboo** (P4.D50): edge `text_replacements_routes.rs:244-269`
  (`taboo_settings_put` — the hand-rolled
  `json_body.get("phrases").map(Null => None | other => Some)` at
  `:264-267` is the exact re-implementation the P4.56 comment calls
  out); engine `api/engine.rs:3957-3970`; handler
  `api/settings.rs:2888-2917` (`bag.get("phrases")` at `:2897-2900`).
  Type: `types.rs:2951-2980` + the tri-state serde test
  `taboo_update_phrases_tristate_survives_serde` `:3896-3915`.
- **Brahma Console** (P4.D57): edge `:286-320`; engine
  `:3977-3991`; handler `api/settings.rs:2942-2972`. Type:
  `types.rs:2959-2994` (`max_agent_turns` with `double_option` at
  `:2994`).

⚠ The P4.56 landmine already documented at
`text_replacements_routes.rs:75-83`: `CoreResponse::BrahmaConsole` was
missing from `unwrap_to_http`'s success match for two rounds (both
edges 500'd on every success). The `web-edge-unwrap-variant-list-rots`
memory is the standing lesson — this lane's rewire must not
re-introduce the class, and the live web tests must pin the success
arms.

**Behavior must NOT move.** This is a consolidation: each PUT's
observable contract (absent-keeps / null-clears-or-400s / value-writes,
the exact 400 bodies, the echo shapes) is already pinned by
`settings_routes_equivalence` (128 cases) and the taboo/brahma arms.
The deliverable is one decode, three readers collapsed to one, with the
pinned behavior byte-identical before and after.

## Mandate

Adopt the decode-once shape for the taboo and brahma-console update
verbs: the web edge decodes through the `Request` enum (no hand-built
variants — the `edge-must-decode-through-the-request-enum` memory), the
engine passes the decoded struct through without rebuilding a bag, and
the handler consumes the typed tri-state directly. Zero observable
behavior change, proven by the settings families regenerated at the pin
and byte-identical. It must NOT touch any image-surface file (the
sibling lanes'), `services/**` outside the settings handlers' own file,
or `apps/web/**`.

## Tiered deliverables

### Tier 1 — must land

1. **Taboo decode-once**: edge → engine → handler on the data-retention
   pattern; the hand-rolled edge match retired; the
   `double_option`-typed struct is the single decode; the handler's
   three-arm engine match (absent/null/value) consumes it directly.
   `settings_routes_equivalence` regenerated at `a14a1811` and re-run —
   byte-identical rows (this is a refactor pin, not a new-arm pin), the
   explicit-null-400 arm confirmed still red-capable by mutation
   (revert the validate-first order → the family must go red).
2. **Brahma-console decode-once**: same shape; the
   `unwrap_to_http` success arm pinned by the existing live web test
   (extend if the P4.56 test doesn't already cover both edges'
   success).
3. **The serde pins kept**: `taboo_update_phrases_tristate_survives_
   serde` and the brahma `double_option` test survive (extended if the
   struct moves), never weakened.

### Tier 2 — should land

4. A short survey of OTHER hand-built edge variants in
   `text_replacements_routes.rs` and siblings (the P4.D85
   `restore_cleared_nulls` class lead): enumerate, don't fix — the list
   goes in the lane record as future-order fodder.
5. The banked P4.56-era smalls IF they sit inside this lane's owned
   files and are zero-risk: the memories float-literal echo nit and the
   shared-`baseUrl`-helper cleanup (measure first; skip either that
   crosses ownership).

### Tier 3 — explicit deferrals, loud

6. The groups-side cleared-null pin and any settings surface outside
   taboo/brahma/data-retention: named, not touched.
7. 💸 Nothing new for the dogfood queue expected (no observable change);
   if any behavior is found to move, STOP — that is a finding, not a
   rider.

## The differential requirement (restated)

The consolidation is proven by regenerating `settings_routes_equivalence`
(and the brahma-console routes family) FRESH at `a14a1811` and re-running
BY NAME — all rows byte-identical to the pre-refactor run (capture both
runs' NDJSONs and diff them, not just green-vs-green: the
`green-regen-is-not-coverage` rule applies doubly to a
no-behavior-change lane). The web-edge tests
(`crates/quilltap-web/tests/`) run live against the rewired edges.

## Fixtures

Consumes: the committed settings fixtures. Delivers: nothing new. No
corpus or DB fixture moves expected; if one proves necessary, STOP and
report (a no-behavior-change lane should not need one).

## Ownership

This lane owns:
- `crates/quilltap-core/src/api/{types,engine,settings}.rs`
- `crates/quilltap-web/src/text_replacements_routes.rs` + the web tests
  it extends (`crates/quilltap-web/tests/**` for these edges)
- Harness: `settings_routes_equivalence`, the brahma-console routes
  family (regen + byte-diff only)
- Version bumps: `quilltap-core`, `quilltap-harness`, `quilltap-web`
  (the unifier accumulates)

It must NOT touch: `services/**` (all sibling-owned this round),
`files/**`, `model/**`, `tools/**`, `photos/**`,
`provider_manifest/**`, `apps/web/**`, `db/instance_settings.rs`
(read-only — the persistence layer does not move).
`docs/CHANGELOG.md` + `status-log.md` append-only.

## Verification gate (before declaring done)

- `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D
  warnings` plain AND `--features quilltap-core/native-transport`.
- `cargo test --workspace` with the lane's oracle env block — full-log +
  sentinel + background notification per the standing rule.
- `settings_routes_equivalence` + the brahma family regenerated FRESH at
  `a14a1811` through `harness/tools/recipe_sweep.py` (one sweep at a
  time), re-run BY NAME, zero SKIP; the pre/post NDJSON byte-diff clean.
- The mutation pin (validate-first order reverted → red) demonstrated
  and reverted.
- SPA: no edits (P4.D109's file set).
- Per-commit: `.claude/commands/commit.md`.
