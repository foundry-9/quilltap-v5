# Work order P4.D155 — the `0506517d3` collapse's seven behaviour corrections on the server + the Pascal placeholder classifier on BOTH sides (cheap-LLM priority-5 params, export preview exclusion, the document-delete 404 body, the API-key sentence, placeholder edge cases, the self-inventory catch)

**Status:** OPEN (ordered 2026-09-05, the `d883a5ee1` drift catch-up round:
P4.D153 ∥ P4.D154 ∥ P4.D155 ∥ P4.D156 ∥ P4.D157 ∥ P4.D158).

## Preamble

- **v4 oracle baseline: `0b0617fee`** (drift-ledger §1). **This lane's target
  commit is `0506517d3`** — "refactor: release-checklist 3 — collapse the 4.9
  diff's duplicates onto single owners" (2026-09-04): 256 files,
  +8,241/−6,847, a DRY/SRP/chokepoint pass whose author states behaviour is
  unchanged EXCEPT for seven named corrections. **This lane ports the seven
  corrections (a)–(g) — NOT the refactor.** The neutrality claim over the
  other 249 files is P4.D158's bulk sweep. Read the CHANGELOG hunk (`git -C
  ~/source/quilltap-server show 0506517d3 -- docs/CHANGELOG.md`) for the
  seven + the "Left alone on purpose" list, then the code hunks per site —
  the prose was verified at the hunk level for the ledger row, but the port
  is from the hunks.
- **Freshness probe at lane start** (drift-ledger §2): v4 on `main`, tree
  clean, `git log d883a5ee1..main` empty, `git log 3a76b17df..bugfix` empty.
  **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED** (§B). Every regen from
  `/tmp/qt-v4-pin-p4d155-d883a5ee1` through the sweep driver. Only
  `0506517d3` touches this lane's surfaces among the fifteen; verify each
  regenerated NDJSON for the ONE changed byte class the item predicts.
- **Sibling lanes:** P4.D153 (owns `build_context.rs`, `carina_query.rs`),
  P4.D154 (owns `message_context.rs`, `chat_files.rs`), P4.D156 (owns
  `apps/web/src/app/screens/**` — the three CLIENT corrections (f) are ITS,
  not this lane's; this lane's SPA scope is `apps/web/src/app/pascal/**`
  only), P4.D157 (owns `message_formatter.rs`, `host_notifications.rs`,
  `cheap_model.rs` — NOT `cheap_llm.rs`, which is this lane's), P4.D158.
  §A–§H + Ownership binding and identical across all six orders.
- **Worktree:** per the `carryout` skill (branch `claude/p4-d155-…`).
  `CHANGELOG`/`status-log.md` append-only. **No Playwright** (§E) — the
  Pascal client twin is proven by parity specs, not beats.

## Mandate

Land the five server-side corrections and the one both-sides correction,
each red-first against v4's REAL code: **(a)** the priority-5 cheap-LLM
selections carry `profile_parameters` and derive `is_local` from the
provider; **(b)** the export PREVIEW's files count uses
`is_file_excluded_from_export`; **(c)** the chat-scoped document delete's
404 body is `File not found`, not `File not found not found`; **(d)** the
Brahma one-shot API-key sentence is the shared capitalised form; **(e)** the
Pascal placeholder classifier is ONE function on each side (a bare family
prefix is `unknown`, `{{params.toString}}` cannot render prototype source);
**(g)** the self-inventory builder's standing-instructions call has no empty
catch (measure — v5 may already be this shape). Must NOT touch: the three
client corrections (f) (P4.D156), `build_context.rs`, `message_context.rs`,
`lib.rs`, any v4 refactor that is not one of the seven (v4's own "Left alone
on purpose" list names what it did NOT collapse — never "finish" one).

## Survey-verified starting points (2026-09-05, from the shipped hunks — ledger §5.3)

### (a) cheap-LLM priority-5 selections — `lib/llm/cheap-llm.ts`, `core-execution.ts`
1. v4 post-fix `selectionFromProfile(profile, {localBaseUrlFallback?})`
   (`cheap-llm.ts:119-134`): `isLocal = provider === 'OLLAMA'`, `baseUrl =
   profile.baseUrl || (isLocal && localBaseUrlFallback ? OLLAMA_DEFAULT_BASE_URL
   : undefined)`, `profileParameters: profileParams(profile)`. Eight
   hand-built literals replaced (the hunk's `-` blocks): the local cheap
   profile + the Ollama profile (both `{localBaseUrlFallback: true}`), the
   Ollama "use current model" (plain), the "current profile → cheapest
   model" (`{...selectionFromProfile(currentProfile), modelName:
   cheapModel}` — **this one had `isLocal: false` and NO params**), the two
   uncensored picks in `cheap-llm.ts` (`localBaseUrlFallback: true`), and
   `core-execution.ts:417-424`'s answer-confirmation/cheap-task uncensored
   selection (had `isLocal: false` + params → now provider-derived).
2. v5 sites reproducing the pre-fix shape: `crates/quilltap-core/src/
   cheap_llm.rs:319-320` (Ollama use-current: `is_local: true,
   profile_parameters: None` — params dropped), `:334-335` (current →
   cheapest: `is_local: false, profile_parameters: None` — both), and
   `services/cheap_llm_exec.rs:122-138` (the uncensored selection with the
   comment "v4 hardcodes `isLocal: false` on this path" — now provider-
   derived). `cheap_llm.rs:225 selection_from_profile` exists WITHOUT the
   `localBaseUrlFallback` option (`:218-220` has the base-URL fallback as a
   separate helper); `:288-289`/`:303-304` (the local/ollama picks) and
   `:357-368` (the uncensored pick) already carry params. Reshape onto ONE
   `selection_from_profile(profile, local_base_url_fallback: bool)` and
   route all eight v5 twins through it, exactly as v4 did — the port IS the
   collapse here, because the corrections fall out of it.
3. Families: `cheap_llm_fallback_equivalence` (the fallback chain — measure
   whether its rows reach priority 5), `answer_confirmation_tier3_
   equivalence` (`QT_ORACLE_ANSWER_CONFIRMATION`, `QT_FIXTURE_AC_*` — the
   uncensored selection's `is_local` is only VISIBLE if a case's uncensored
   profile is OLLAMA; if none is, add one), and the family driving
   `get_cheap_llm_provider` (grep `crates/quilltap-harness/tests` for the
   ladder; if it is unit-tested only, the tier-1 pin is the ladder's own
   `selection` test module — say which). `profile_parameters` reaching the
   wire on priority 5 is the P4.D83 corpus class: a `profileParameters`
   bag on the CURRENT profile must appear in the priority-5 request body.

### (b) export preview count — `lib/export/quilltap-export-service.ts`
4. v4: `(await repos.files.findAll()).filter(f => !isFileExcludedFromExport(f))`
   for the preview's `files` count. v5 `services/qtap_export/preview.rs:193-215`
   carries the PRE-fix inline two-clause filter with a comment that names
   this exact divergence ("⚠ NOT the excluded-files predicate … carried
   verbatim … `preview_files_all` is the case that says so"). Port: route
   through `super::is_file_excluded_from_export` (`excluded_files.rs:45`,
   `pub(super)` already); `system_export_equivalence`
   (`QT_ORACLE_SYSTEM_EXPORT`) goes red-first on `preview_files_all` IF the
   fixture holds an excluded (archive-bundle) file — measure; plant one if not.

### (c) the document-delete 404 body — `lib/documents/operator-doc-http.ts:312-326`
5. v4: `notFound('File')` → `lib/api/responses.ts:150-156` renders
   `${resource} not found` → "File not found" (was `notFound('File not
   found')` → "File not found not found"). v5 `api/documents.rs:1060`
   `not_found("File not found")` with `not_found` at `:63` appending
   ` not found` — the bug reproduced; `:1748` (the standalone delete) already
   says `not_found("File")`. One-token fix + a red-first arm:
   `documents_routes_equivalence` has `chat_delete_document` (an EXISTING
   file) but NO missing-file delete case — add `chat_delete_document_missing`
   to `documents-routes.test.ts` + the runner (a `chatDocumentId` that is not
   in the fixture), byte-comparing the body.

### (d) the API-key sentence — Brahma one-shot
6. v4: `'no API key configured for this connection profile'` (Brahma
   one-shot) and `'No API key configured'` (help chat) → the shared
   `'No API key configured for this connection profile'`; `'API key not
   found'` unchanged. v5: `services/brahma_console/mod.rs:359` carries the
   lowercase PRE-fix form; `orchestrator.rs:288` already the capital form
   (the exact two-site divergence v4 collapsed, reproduced). Fix `mod.rs:359`;
   the help-chat copy is `p4.9i2`'s (unported surface — record, no port).
   Families: `brahma_console_tier3_equivalence` / `brahma_console_routes_
   equivalence` — an arm whose profile has no key must exist or be added
   (measure).

### (e) the placeholder classifier — NEW `lib/pascal/placeholders.ts`
7. v4 (read the file whole, 120 lines): `PLACEHOLDER_PATTERN =
   /\{\{([^}]+)\}\}/g`; `classifyPlaceholder(key)` → `value|roll|dice|llm`
   exact, then `params.`/`metadata.`/`state.` prefixes each guarded by
   `key.length > PREFIX.length` (a bare prefix is `{kind:'unknown', key}`),
   else `unknown`; `scanPlaceholders(text)` with a fresh `RegExp` per call
   (never shares `lastIndex`). Seven readers collapsed onto it: the template
   renderer, the effect-expression resolver, the grammar's reference check,
   the vocabulary scanner, three Workbench audits. The `{{params.toString}}`
   correction: the renderer must look params up as OWN properties (v4's fix
   is in the renderer hunk — read it; JS's `obj[key]` reached the prototype).
8. v5 server: the prefix logic is spelled at least FIVE times —
   `pascal/tool_vocabulary.rs:48-50` (the three `*_PREFIX` consts),
   `pascal/expressions.rs:129` (a `for prefix in [...]` loop),
   `pascal/custom_tool_types.rs:818/:834/:2734` (`strip_prefix`),
   `pascal/custom_tools.rs:1224-1234` (`strip_prefix` ×3). Rust `strip_prefix`
   on `"params."` yields `Some("")` for a bare prefix — whether each site then
   treats `""` as a missing NAME (v4's old bug) or as `unknown` (v4's new
   rule) is a per-site measurement; the port is ONE `classify_placeholder`
   in a new `pascal/placeholders.rs` with every reader routed through it
   (`custom_tool_types.rs:3207-3211` already pins `parse_effect_target(
   "state.")` as an error — keep that meaning, verify it matches v4's
   post-fix arm). Rust has no prototype chain, so `{{params.toString}}`
   cannot leak function source — pin it anyway (a `serde_json::Map` lookup
   of `toString` is `None`) so the corpus row exists on both sides.
9. v5 client (`apps/web/src/app/pascal/`): `tool-draft.ts:921`
   `PLACEHOLDER_PATTERN` with a SHARED `lastIndex` reset by hand at
   `:929/:990/:1030` (the very hazard v4's `scanPlaceholders` removes), and
   `startsWith('params.')` tests at `tool-draft.ts:956/:1007/:1047`,
   `custom-tool-types.ts:516/:532/:1923`. Port v4's module as a
   character-for-character TS twin (`apps/web/src/app/pascal/placeholders.ts`)
   with the parity spec grown from v4's test file for it, and route the six
   client readers through it — **this is where `{{params.toString}}` CAN
   leak** (the Workbench renders in the browser): the render preview must
   use own-property lookup, spec-pinned with `toString` and `constructor`.
10. Families: `pascal_tool_vocabulary_equivalence`, `pascal_expressions_
    equivalence`, `pascal_custom_tool_definition_equivalence`,
    `pascal_workbench_equivalence`, `pascal_simulate_equivalence`,
    `pascal_run_custom_*` — each gains the bare-prefix + `toString` rows
    where its surface renders/validates placeholders (red-first per family;
    a family whose rows do not move on the mutation is not covering it).

### (g) the self-inventory catch — `lib/tools/handlers/self-inventory/builders.ts:518`
11. v4 dropped an empty `try/catch` around `resolveStandingInstructionsSection`
    (which already fails soft). v5 `tools/self_inventory.rs:1515` calls
    `resolve_standing_instructions_section(...)` — read the lines that follow:
    if v5 already propagates/soft-fails identically there is nothing to
    change (record the measurement + the v4 sha in a comment); if v5 swallows
    an `Err` into `None`, match v4's post-fix propagation and say which.

## Tiered deliverables

**Tier 1 (must land):** (a) items 1–3 whole (the eight twins through one
`selection_from_profile`, the three pre-fix sites fixed, the OLLAMA-uncensored
+ priority-5-params arms red-first); (b) item 4; (c) item 5 with the missing
arm; (d) item 6; (e) items 7–10 — the server classifier + every reader, the
client twin + parity spec + the six client readers, the bare-prefix and
`toString` rows on both sides; (g) item 11 measured and dispositioned.

**Tier 2 (should land):** a doc comment at each collapsed v5 site naming
`0506517d3` and which correction it carries (the ledger's §5.3 rule: the
finding travels in a code comment).

**Tier 3 (explicit deferrals, loud):** the help-chat API-key sentence
(unported surface — `p4.9i2`); v4's "Left alone on purpose" list is NOT this
lane's to finish (NanoGPT/DeepSeek/Z.AI base re-implementations, the two
`core-execution.ts` task maps, `executeCheapLLMTask`'s positional params, the
wardrobe dialog loaders).

## The differential requirement (restated)

Every item lands red-first on a family driving v4's REAL code at the
`d883a5ee1` pin, then green after the port; each family regenerated through
the sweep driver and run by name with zero SKIP. Mutation-proven per item:
(a) hard-code `is_local: false` back on the uncensored pick → the
OLLAMA-uncensored arm reddens, drop `profile_parameters` on priority 5 → the
params-on-the-wire arm reddens; (b) restore the inline filter → `preview_
files_all` reddens; (c) `"File not found"` back → the missing arm reddens;
(d) lowercase back → the no-key Brahma arm reddens; (e) drop the
`len > PREFIX.len` guard in the Rust classifier → the bare-prefix rows
redden, and in the TS twin → the parity spec reddens; render params by
`obj[key]` in the client → the `toString` spec reddens.

## Fixtures

- Consumes: the answer-confirmation, brahma, pascal, documents and
  system-export fixture pairs (§A) — widened by BUILDER only where an arm
  needs a seed (an OLLAMA uncensored profile; an excluded archive-bundle file
  row; a key-less Brahma profile), each widening recorded with before/after
  rowcounts.
- Delivers: the new corpus rows + `apps/web/src/app/pascal/placeholders.ts`
  + its spec; no new committed `.db` expected.

## Shared contracts (BINDING — identical text in all six orders)

**§A Family ownership for this round's regens.** Each family below is
regenerated and edited by ONE lane; P4.D158's bulk neutrality sweep
EXCLUDES every family in this section by name and hands any red in them to
the owning lane.

- **P4.D153:** `memory_injector_equivalence` (+ `harness/oracle/cases/
  memory-injector.ts`), `build_context_tier3_equivalence` (+ its builder
  `build-context-tier3.test.ts` and the `QT_FIXTURE_BC_*` pair),
  `carina_query_tier3_equivalence` (+ its fixture builder),
  `announcer_tier3_equivalence`.
- **P4.D154:** `message_context_leaves_equivalence` (+ `harness/oracle/
  cases/message-context-leaves.ts`), `orchestrator_tier3_equivalence` (+
  `orchestrator-tier3.test.ts` and the `QT_FIXTURE_ORCH_*` pair),
  `file_attachment_tier3_equivalence` (+ its fixture pair).
- **P4.D155:** `documents_routes_equivalence` (+ `documents-routes.test.ts`),
  `system_export_equivalence`, `answer_confirmation_tier3_equivalence`,
  `cheap_llm_fallback_equivalence`, `brahma_console_tier3_equivalence`,
  `brahma_console_routes_equivalence`, `brahma_orchestrator_tier3_equivalence`,
  every `pascal_*_equivalence` + `p4_6ay_workbench_wire_contract`, the
  self-inventory families, and whichever family the lane identifies as
  driving `cheap_llm::get_cheap_llm_provider` (named in its record).
- **P4.D156:** the CLI Tier R (`crates/quilltap-cli/tests/cli_differential.rs`)
  + `completion_behavior.rs`; the SPA unit specs under its paths.
- **P4.D157:** the seven families the dead-code sweep takes dark:
  `cheap_model_equivalence`, `model_selection_equivalence`,
  `llm_errors_equivalence`, `message_formatter_equivalence`,
  `post_office_host_equivalence`, `chat_timestamp_equivalence`,
  `token_estimation_equivalence` (+ their seven oracle cases).
- **P4.D158:** `request_builder_equivalence` + the committed request-envelopes
  corpus and its recorder, `request_builder_google_wire_equivalence`,
  `request_builder_google_equivalence`, `response_parse_equivalence`,
  `tool_wire_call_site`, the stream fixtures, `openrouter_sdk_pricing_equivalence`,
  the provider manifests generator + `provider_manifest/manifests/*.json`;
  the restore families for the `2edd823c0` arms (`system_restore_equivalence`,
  `system_restore_state`, `restore_vintage_state`,
  `system_restore_guards_equivalence`, the `build-restore-archive*` builders
  and any NEW committed archive); and the bulk neutrality sweep over every
  family NOT listed above.

**§B Pins.** The round's target baseline is **`d883a5ee1`** (v4 `main` HEAD
at ordering, 2026-09-05). Every lane regenerates from a LANE-UNIQUE detached
worktree pinned at `d883a5ee1` (`/tmp/qt-v4-pin-<order>-d883a5ee1`, the
three symlink classes, ledger §5.1) — with ONE exception: P4.D157's
frozen-family evidence regenerates at **`0b0617fee`** (the last sha at which
those seven cases link), from its own second pin. **PIN REQUIRED stands for
every lane until `/unify` moves the baseline** — the checkout's HEAD is the
target, but the checkout can go dirty mid-lane (§5.1), and a pin is the only
tree whose contents are known. Verify each pin by grepping a fresh NDJSON for
a marker only one tree has (e.g. `About another character` appears in a
`d883a5ee1` memory-injector oracle only once P4.D153's targeted cases exist,
and never in a `0b0617fee` one).

**§C Sweep serialization.** `recipe_sweep.py --run-all` is single-flight
repo-wide and belongs to P4.D158 alone; it runs AFTER P4.D153–P4.D157 have
closed their branches (execution arrangement in the round report). Lanes
153–157 run per-family `--run` regens only, and never against another
lane's §A families or their `/tmp` staging.

**§D Source-tree seams (who edits what shared file).**

- `crates/quilltap-core/src/services/mod.rs`: **only P4.D153** edits it (one
  line: `pub mod memory_subject;`). `crates/quilltap-core/src/lib.rs`:
  **nobody**. P4.D157 deletes FUNCTIONS and their tests inside files it owns
  but never removes a module declaration or a `pub use` in `lib.rs`; a module
  it empties is recorded for the unifier, not deleted.
- `services/message_context.rs` + `services/chat_files.rs` (the
  `MessageContextSeams` trait, its `Noop` and `Real` impls): **only
  P4.D154**. The trait gains ONE sibling method beside `load_lantern_images`
  for the USER-side loader (name it `load_user_attachments`; signature shape
  `(&self, file_ids: &[String], provider: &str) -> impl Future<Output =
  UserAttachmentLoad> + Send`, default no-op, `UserAttachmentLoad` a struct
  the lane defines beside `LanternLoad`).
- `db/characters_read.rs`: **only P4.D153** (`find_names_by_ids`, RAW path).
- `cheap_llm.rs`, `services/cheap_llm_exec.rs`: **only P4.D155**.
- `model/request_builder/anthropic.rs`: **only P4.D158** (one regex entry +
  the three comment corrections).
- SPA: `apps/web/src/app/pascal/**` → **P4.D155**; `apps/web/src/app/screens/**`
  and `apps/web/src/styles/**` → **P4.D156**. Nobody else edits `apps/web`.
- `crates/quilltap-cli/**` → **P4.D156** only.

**§E Playwright.** Port 4319 is P4.D156's for the round (it may run the
existing suite once at close; it authors no new beat unless its record says
why). Every other lane authors NO Playwright and runs none; the unifier runs
the full suite at the gate.

**§F The drift ledger.** Lanes never write `drift-ledger.md`. The fourteen
§3 rows were marked `ORDERED(p4.d15x)` at ordering; `/unify` marks them
ABSORBED / NO-PORT-RATIFIED when the baseline moves to `d883a5ee1`. The
fifteenth row (`15573c3a1`, bug 119) stays UNPROCESSED by the ledger's own
instruction — it belongs to the unported `p4.9k` surface.

**§G The `p4.9i2` help bank.** The help-file hunks in this drift
(`e288ae2ec` → `help/file-uploads.md` "A word on company"; `d883a5ee1` →
`help/memory-recall-relevance.md` "Whose Life Is It, Anyway?"; `e9a9c538e` →
four `help/` files incl. `homepage.md`'s In-Chat Navigation section and three
corrected `url:` values) are banked ONCE, by P4.D158, in its lane record under
a `p4.9i2 bank` heading; the unifier carries them into `phase-4.md`'s
candidate row. No lane ports help content.

**§H Versions.** Each lane bumps only the crates / the SPA it touches, per
`.claude/commands/commit.md` §6; the unifier recounts as base + total bumps
(the silent auto-merge trap). Base at ordering: core 0.0.776, harness 0.0.671,
web 0.0.114, host 0.0.96, cli 0.0.17, tauri 0.0.7, SPA 0.5.642.

## Ownership (BINDING — identical table in all six orders)

| Path | Owner |
|---|---|
| `crates/quilltap-core/src/memory_injector.rs`, NEW `services/memory_subject.rs` (+ the one `services/mod.rs` line), `db/characters_read.rs`, `services/build_context.rs`, `services/carina_query.rs`, `services/announcer/character_voiced.rs`; §A families for P4.D153 | P4.D153 |
| `crates/quilltap-core/src/services/message_context.rs`, `services/chat_files.rs`; §A families for P4.D154 | P4.D154 |
| `crates/quilltap-core/src/cheap_llm.rs`, `services/cheap_llm_exec.rs`, `api/documents.rs`, `services/brahma_console/**`, `services/qtap_export/**`, `tools/self_inventory.rs`, `crates/quilltap-core/src/pascal/**`, `apps/web/src/app/pascal/**`; §A families for P4.D155 | P4.D155 |
| `apps/web/src/app/screens/**`, `apps/web/src/styles/**`, `crates/quilltap-cli/**`; the CLI Tier R + `completion_behavior.rs` | P4.D156 |
| `crates/quilltap-core/src/cheap_model.rs`, `pricing.rs`, `services/llm_errors.rs`, `message_formatter.rs`, `services/host_notifications.rs`, `chat_timestamp.rs`, `token_estimation.rs` (function-level deletions/pins only); the seven §A families + their oracle cases | P4.D157 |
| `crates/quilltap-core/src/model/request_builder/anthropic.rs` (one regex line + comments); `harness/oracle/providers/**` + `harness/oracle/fixtures/request-envelopes/**`, `response-bodies/**`, the stream fixtures; `crates/quilltap-core/src/provider_manifest/manifests/*.json`; the restore-archive builders + any NEW committed archive + §A restore families; `docs/v4/**`; `harness/tools/sweep-results/<new artifact>` | P4.D158 |
| `docs/CHANGELOG.md`, `docs/developer/porting/status-log.md` | append-only, all lanes |
| `docs/developer/porting/drift-ledger.md`, `crates/quilltap-core/src/lib.rs` | NOBODY |

Every other path is unowned this round: a lane that believes it must touch
one STOPS and reports (the `carryout` rule 3), naming the file and the
sibling it suspects.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D
   warnings` in BOTH feature sets; `cargo build --workspace`.
2. `cargo test --workspace` (full log + sentinel, never `| tail`) with the
   lane's env block (every §A family's `QT_ORACLE_*`/`QT_FIXTURE_*`), each
   confirmed RUN, zero `SKIP:`.
3. Each §A family by name through `recipe_sweep.py --v4 <pin> --run
   <family>`, oracles fresh at `d883a5ee1`, the changed bytes grepped per
   item.
4. SPA: `cd apps/web && npm test` (never bare `ng`) and `npm run build`
   clean; `npm run lint` (the `check-qt-classes` guard) clean.
5. The mutation proofs above, each named in the lane record with the arm it
   reddened.
6. Commits per `.claude/commands/commit.md` (core + harness + SPA bumps);
   the lane record appended to `status-log.md`; this order's status header
   updated.
