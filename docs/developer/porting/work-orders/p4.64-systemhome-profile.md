# Work order P4.64 — profile the 7.5 s `systemHome` dashboard (measure first, then decide)

**Status:** OPEN (planned 2026-08-27).

## Preamble

- **v4 baseline: `8872d7efc`** (drift ledger §1, checked 2026-08-27). v4
  main HEAD at planning is `aec86a613` — one portable commit past the
  baseline, owned by the sibling P4.D130; it does not touch this lane.
- **Freshness probe at lane start** (ledger §2). Pass = checkout on `main`,
  clean, no commits past `aec86a613` (main) / `3a76b17df` (bugfix). Any
  mismatch: **STOP and report.**
- **Regen rule in force: PIN REQUIRED** — if this lane regenerates any
  oracle (only needed if a fix lands and its family must re-prove), use a
  lane-unique detached worktree at **`8872d7efc`**
  (`/tmp/qt-v4-pin-p464-8872d7efc`, three symlink classes, ledger §5.1).
- **Siblings:** P4.D130, P4.62, P4.63 — disjoint; §Ownership binding. No
  Playwright in this lane.
- **Worktree:** per `carryout`; edit inside the worktree. `CHANGELOG` and
  `status-log.md` append-only.
- ⚠ **Real-data measurements run against a COPY of Friday only** (the
  standing rule; `dogfood-friday-setup` memory — NEVER reverse the rsync,
  never point anything writable at live data). If no current copy exists in
  the environment, profile against the largest committed fixture and say so
  loudly — a fixture-scale profile that shows the shape is still evidence;
  a claim about the 7.5 s needs the real-scale copy.

## Mandate

Answer, with measurements, why `systemHome` costs a steady **7.5 s** on the
real instance (dogfood 2026-08-26: 7.50 s and 7.70 s on back-to-back WARM
dispatches; 859 chats, 32 live characters, 8 projects, 45 vaults — correct
output, no panic, **no v4 comparison was run, so this is NOT filed as a
divergence**), and then either land a v5-local fix that changes the COST
without changing the PAYLOAD, or close the item with the evidence that v4
pays the same and the answer is to leave it alone. The phase-4 candidate is
explicit: **first step is to profile** — per-repo timings around the four
loads — and nothing in the standing hypothesis was measured per-query.

⚠ v4 composes the same `findAll` shape, so any "fix" that changes what the
dashboard SHOWS is a divergence, not a fix. The target is the cost of
producing the SAME payload. If the profile points at a cost that can only
move by changing the payload or the schema, STOP and escalate with the
numbers.

**Must NOT touch:** anything a sibling owns; the SPA; the repos' shared
read paths in ways that alter any OTHER caller's behavior (a narrower
projection must be a new read path or provably payload-identical for every
caller — the `canChooseOutfit` omitting-reader incident is the cautionary
precedent).

## Survey-verified starting points (fresh, 2026-08-27)

- The verb lives in `crates/quilltap-core/src/services/home.rs`
  (`get_home_data`), dispatched from `api/engine.rs`/`api/system.rs`.
- The standing hypothesis (phase-4 candidate 2, a hypothesis NOT a
  profile): `get_home_data` loads the world to render a handful of cards —
  `chats_read::find_by_user_id` (all 859), `ProjectsRepository::find_all()`,
  `characters_read::find_by_user_id`, `FilesRepository::find_all()` — and
  the projects and characters reads take the **mount-index connection**, so
  each entity hydrates through the document-store / vault overlay before
  the in-memory stats pass trims to 12 recent chats, 8 projects, and a few
  characters.
- Plausible outcomes the profile should discriminate: (a) the overlay
  hydration dominates (N vault reads per entity) → a batched overlay read
  or a dashboard-specific read path that still yields byte-identical
  payload; (b) one repo dominates linearly (859 chats) → the trim can move
  below the read only if the payload provably cannot change; (c) the cost
  is spread evenly and v4 pays it too → measure v4 (`time` the v4 route on
  the same copy) and close with both numbers.

## Tiered deliverables

**Tier 1 (must land):** the profile itself — wall-clock per repo load (and
per overlay hydration where that is the shape), at real scale, recorded in
the lane record with the method (instrumentation diff or tracing spans;
tear instrumentation back out unless it graduates to a real tracing span
worth keeping). Plus the DECISION, argued from the numbers: fix v5-locally,
or leave-alone with the v4 comparison measured.

**Tier 2 (should land):** the fix, if the profile supports one —
payload-identical by proof (the existing home differential family, if one
exists, re-run green over an unchanged oracle; otherwise a before/after
byte-compare of the dispatch response on the same fixture), with the
before/after timing in the lane record.

**Tier 3 (explicit deferrals, loud):** anything requiring a payload or
schema change (escalate); a v4-side filing if the measurement shows v4
pays the same cost and it is worth an upstream note.

## The differential requirement

This lane's port-discipline obligation is **payload identity**: whatever
moves, the dispatch response for `systemHome` on a fixed fixture must be
byte-identical before and after (normalized only by the standing
timestamp/uuid rules). Check `ls crates/quilltap-harness/tests/ | grep -i
home` for an existing family first; if none exists and a fix lands, a
before/after self-diff on the committed fixture plus the workspace suite
is the minimum bar — say which you used.

## Fixtures

Consumes existing committed fixtures for shape-checks; the real-scale
numbers come from the Friday COPY (read-only dispatches; the engine's
writable open is fine on the copy, never on live). Delivers no fixture.

## Shared contract

None — the lanes meet nowhere. §Ownership is the whole contract.

## Ownership (binding, identical across the four orders)

| Lane | Exclusive ownership |
|---|---|
| **P4.D130** | `apps/web/src/app/wardrobe/**` (new `composed-outfits.*`, new `outfit-quick-pick.*`, `outfit-composer.*`, `equipped-slot-row.*` + specs), `apps/web/e2e/wardrobe-flow.spec.ts`, `apps/web/e2e/global-setup.ts`, `harness/oracle/fixtures/build-salon-fixture.ts`, `crates/quilltap-web/tests/fixtures/salon-*.db`, `crates/quilltap-core/src/services/builtin_mounts.rs`, `screens/new-chat/outfit-selector.ts` (comment only). Bumps: **SPA**; core + harness ONLY if the builtin_mounts fix lands. Sole Playwright runner. |
| **P4.62** | `crates/quilltap-web/src/system_data_routes.rs`, `files_routes.rs`, `llm_logs_routes.rs`; `crates/quilltap-harness/tests/web_edge_body_parse_guard.rs`; the system-data / files / llm-logs routes differential families + their oracle cases; any NEW parser module it creates in `quilltap-core` (own file only). Bumps: **web + harness** (+core if a parser lands there). |
| **P4.63** | `crates/quilltap-harness/tests/{system_import_state.rs, attach_mount_file_equivalence.rs, embedding_blob_binding_guard.rs}`; their oracle cases + `harness/oracle/fixtures/build-attach-file-fixture.ts` + the `attach-file-*` fixtures; `crates/quilltap-core/src/services/cheap_llm_exec.rs` (the one test assert only). Bumps: **harness** (+core for the assert edit). |
| **P4.64** | `crates/quilltap-core/src/services/home.rs` + any profiling scaffolding in its own new files. Bumps: **core** only if a fix lands. |

`docs/CHANGELOG.md`, `status-log.md`: append-only. Version conflicts at
unification: recount as base + total bumps.

## Verification gate (before declaring done)

- If any source changed: `cargo fmt --all --check`; clippy both feature
  sets `-D warnings`; `cargo test --workspace` (background + notification,
  full log); the payload-identity proof.
- If nothing changed (leave-alone verdict): the lane record carries the
  profile table, the v4 comparison number, and the closing argument — that
  IS the deliverable.
- Per-commit rule: `.claude/commands/commit.md`. Lane record to
  `status-log.md`.
