# Work order P4.9G4 — the `.qtap` export / import family

> **STATUS: OPEN — PARTIAL (2026-07-24). Resume at the import EXECUTE half.**
> Lane 2 of 3 in the "finish P4.9G1" round (P4.9G3 ∥ P4.9G4 ∥ P4.9G5).
>
> **LANDED (branch `claude/p4-9g4-qtap-export-import-e31d89`, 2 commits):**
> - Tier 1 items **1, 2, 3** — the whole export writer (`services/qtap_export/**`,
>   all ten types), `systemExportEntities` + `systemExportPreview` (their GET edges
>   were already on main), and the streaming `POST ?action=export` leg with v4's
>   three headers verbatim. **Export is LIVE end-to-end.**
> - Tier 1 item **5, read half** — the `.qtap` upload reader
>   (`services/quilltap_import/ndjson.rs`: `peekFormat` / `readNdjsonLines` /
>   `collectLegacyJson` / `assembleExportFromStream` / `loadQtapFromUpload`),
>   `previewImport` (`services/quilltap_import/preview.rs`), the
>   `systemImportPreview` verb, and the multipart `POST ?action=import-preview`
>   leg. **Import is LIVE up to and including the preview.**
> - Tier 1 item **6** — BOTH differentials, fresh at `e646f58b`, zero SKIP:
>   `system_export_equivalence` (42 cases, **byte-exact line by line**) and
>   `system_import_equivalence` (19 cases, exact).
> - Tier 2 — the e2e `export → import round-trip` beat is fleshed out and LIVE
>   (real download → re-upload → live preview; it stops before pressing Import).
>
> **STILL OPEN — tier 1 item 4 + item 5's write half: `executeImport`.** It
> refuses BY NAME at every entrance (`SystemImportExecute` and
> `POST ?action=import-execute`), never silently. The full resume list — the
> orchestrator's ten id maps and step order, the four per-entity importers, the
> legacy-preset fold, the reconcile pass, the four conflict strategies + the
> route's `'replace'` → `'overwrite'` remap, the multipart `options` part, and the
> tier-2 four-strategy DB-state differential — is in the lane record in
> `status-log.md` (search "DEFERRED, LOUD AND NAMED"), along with two v4 quirks
> banked from the survey that a faithful port must reproduce.
>
> **⚠ A v4 BUG the export/import differential exposed, owed a human ruling:**
> `assembleExportFromStream`'s `every()` over a SPARSE array means v4 cannot
> round-trip a document-store blob larger than the 3 MB chunk size. v5 reproduces
> it verbatim (pinned in both directions). Details in the lane record.
>
> **v4 baseline: `e646f58b`** (v4 HEAD, clean tree, drift-verified 2026-07-24
> at round planning). **Drift-check at lane start** — `git log e646f58b..HEAD
> --oneline` in `~/source/quilltap-server`; if v4 moved, STOP and report before
> porting anything.
>
> **Siblings (run in parallel, disjoint ownership — see §Ownership):**
> - **P4.9G3** — delete-all + the REST remainders.
> - **P4.9G5** — backup/restore.
>
> This lane has **no cross-lane code dependency** (the only one in the round is
> G5 → G3, Shared contract §2). It is the round's most self-contained heavy
> lane.
>
> This lane, plus its two siblings, discharges the OPEN remainder of
> `p4.9g1-data-system-server.md` (whose tasks-queue + jobs family and all
> sixteen §1 wire variants are already on main). P4.9G1 stays open as the
> historical record until all three land; **do not work P4.9G1 directly.**

## Mandate

Deliver the **Import / Export** server family — the streaming NDJSON `.qtap`
writer, the export listing/preview verbs, and the import preview/execute
pipeline with all four conflict strategies — as dispatch verbs + v4-parity REST
edges + the streaming and multipart web-edge legs, each differential-verified
against v4's real `lib/export/**` and `lib/import/quilltap-import/**`.

The SPA half is **already built and waiting on main** (P4.9G2 units 8 —
`export-dialog.ts` / `import-dialog.ts` / `import-export-card.ts`), so this
lane's output goes live the moment it lands. Read that unit's record in
`status-log.md` before starting: it documents exactly which legs the client
calls (the client always holds a `File`, so the multipart legs are the live
path and the JSON dispatch legs are the fallback) and carries v4's quirky
`getTotalSteps` behaviour that your response shapes must satisfy.

**Must NOT touch:** the delete-all family (P4.9G3: `services/delete_all.rs`,
`api/system_data.rs`), the backup/restore family (P4.9G5: `services/backup/**`
and the backup/restore routes), and
`crates/quilltap-core/src/api/types.rs` (**FROZEN this round** — all sixteen §1
variants are already defined and pinned by `p4_9g1_wire_contract`; a needed
change is a STOP-and-report). §Ownership binds the rest.

## Survey-verified starting points (2026-07-24 @ v4 `e646f58b` — do not re-derive)

### v4 source map

| File | Lines | Role |
| --- | --- | --- |
| `lib/export/ndjson-writer.ts` | 784 | the whole streaming writer |
| `lib/export/quilltap-export-service.ts` | 268 | `previewExport` :63 |
| `lib/export/types.ts` | 687 | `ExportEntityType` :31, the manifest + counts + per-entity exported shapes |
| `lib/import/ndjson-reader.ts` | 241 | `peekFormat` :54, `readNdjsonLines` :129, `collectLegacyJson` :218 |
| `lib/import/quilltap-import-stream.ts` | 439 | `assembleExportFromStream` :56, `buildExportDataForType` :397 — this is `loadQtapFromUpload`'s engine |
| `lib/import/quilltap-import-service.ts` | 48 | the thin facade |
| `lib/import/quilltap-import/validation.ts` | 65 | `parseExportFile` :15, `validateExportFormat` :33 — **the exact error strings are part of the contract** |
| `lib/import/quilltap-import/preview.ts` | 183 | `previewImport` :20 (count-only, no writes) |
| `lib/import/quilltap-import/execute.ts` | 412 | `executeImport` :41 — `IdMappingState` (10 maps) + the imported/skipped count bags |
| `lib/import/quilltap-import/import-characters.ts` | 298 | |
| `lib/import/quilltap-import/import-entities.ts` | 461 | |
| `lib/import/quilltap-import/import-profiles.ts` | 225 | |
| `lib/import/quilltap-import/import-document-stores.ts` | 242 | |
| `lib/import/quilltap-import/reconcile.ts` | 369 | the post-write relationship pass |
| `lib/import/quilltap-import/legacy-presets.ts` | 76 | |
| `lib/import/quilltap-import/types.ts` | 132 | `AnyExportData` :34, `getExportData` :60 |

**Explicitly OUT of scope:** `lib/import/sillytavern-import-service.ts` (781
lines) — the ST import is already ported (P4.6i/P4.6m). Do not touch it.

### The ten export entity types (`lib/export/types.ts:31`)

`characters`, `chats`, `roleplay-templates`, `connection-profiles`,
`image-profiles`, `embedding-profiles`, `tags`, `projects`, `groups`,
`document-stores`. Each export carries exactly ONE type — no mixed exports.

### The NDJSON stream (`ndjson-writer.ts`)

- `streamExportRecords` :658 — the authoritative sequence: `resolveExportIds`
  (:617) → yield the **`__envelope__`** record `{kind, format:'qtap-ndjson',
  version:1, manifest: buildManifest(options, {})}` **with an EMPTY counts
  object** → the per-type generator → yield **`__footer__`** `{kind, counts}`
  with the authoritative counts. Carry that envelope-counts-are-empty quirk
  exactly.
- Per-type generators: `streamCharacters` :113, `streamChats` :197,
  `streamRoleplayTemplates` :308, `streamConnectionProfiles` :329,
  `streamImageProfiles` :347, `streamEmbeddingProfiles` :365, `streamTags`
  :383, `streamProjects` :397, `streamGroups` :430, `streamDocumentStores` :480.
- Inline resolvers: `resolveTagNames` :44, `resolveApiKeyLabel` :61,
  `sanitizeProfile` :74 (strips `apiKeyId`), `buildManifest` :85, `bump` :105.
- `resolveExportIds` :617 — `scope==='selected'` returns `selectedIds ?? []`;
  otherwise per-type `findAll().map(id)`, **except `roleplay-templates`, which
  filters `!t.isBuiltIn && t.userId === userId`** (:634-636), and
  `document-stores`, which uses `globalRepos.docMountPoints.findAll()`.
- `createNdjsonStream` :731 wraps it: one `JSON.stringify(record) + '\n'` per
  record. `QTAP_NDJSON_CONTENT_TYPE = 'application/x-ndjson'` :784.
- Unknown type → `throw new Error(\`Unknown export type: ${options.type}\`)`
  (both in `streamExportRecords` and `resolveExportIds`).

### The v4 routes this lane reproduces

| METHOD PATH (+query) | v4 handler | Notes |
| --- | --- | --- |
| `GET /api/v1/system/tools?action=export-entities&type=` | `route.ts:440` (GET dispatch :1192) | missing `type` → `badRequest('Missing type parameter')`; returns `{id, name, memoryCount?}[]` + `totalMemoryCount` |
| `GET /api/v1/system/tools?action=export-preview` | `route.ts:559` | → `previewExport` :63 |
| `POST /api/v1/system/tools?action=export` `{type,scope,selectedIds,includeMemories}` | `route.ts:396` | streams; headers **verbatim**: `Content-Type: application/x-ndjson`, `Content-Disposition: attachment; filename="quilltap-<sanitizedType>-<YYYY-MM-DD>.qtap"` where `sanitizedType = (type \|\| 'data').replace(/_/g,'-')` and the date is `new Date().toISOString().split('T')[0]`, `Cache-Control: no-store` |
| `POST /api/v1/system/tools?action=import-preview` | `route.ts:655` (POST dispatch :1216) | multipart `file` → `loadQtapFromUpload`, ELSE JSON `{exportData}`; missing file → `'No file provided'`; missing field → `'Missing required field: exportData'`; invalid → `'Invalid export file format. Expected quilltap-export v1.0 format.'` |
| `POST /api/v1/system/tools?action=import-execute` | `route.ts:718` | multipart `file` + `options` (a JSON **string** part; bad JSON → `'Invalid JSON: Failed to parse options'`), ELSE JSON `{exportData, options}`. Validates `conflictStrategy ∈ {skip,replace,overwrite,duplicate}` → else `'Invalid conflictStrategy. Must be one of: skip, replace, overwrite, duplicate'`. **Then remaps `'replace'` → `'overwrite'` at :780** before calling `executeImport`, which also receives `includeMemories: importMemories \|\| false`, `includeRelatedEntities: false`, `selectedIds`. |

### v5 anchors

- **The refusal to replace** — `api/engine.rs:736-746` holds ONE contiguous
  "recognized but not yet available" arm covering all nine unlanded verbs. See
  Shared contract §3 for the region discipline that keeps three lanes out of
  each other's way.
- **The existing `services/quilltap_import/`** (`characters.rs`, `memories.rs`,
  `mod.rs`, `reconcile.rs`, `reset.rs`, `seed.rs`, `seed_assets.rs`) is the
  **P4.4u4 seed subset** — it handles characters + memories with `skip` only and
  refuses everything else. This lane extends it into the full importer; the
  seed path (`seed.rs`/`seed_assets.rs`/`reset.rs`) must keep working
  byte-identically — its differentials are already committed, so re-run them.
- Multipart machinery: the P4.6m precedents in `quilltap-web` (photo-upload,
  ST-PNG, chat-file upload) — reuse, don't re-derive.
- Streaming byte responses: the qtap-target byte route (P4.6w) and the fs raw
  read (P4.6y) precedents.
- The `?action=` dispatch in `quilltap-web/src/system_data_routes.rs` (GET :71,
  POST :132, `unknown_action` :57). Per §3 this lane adds a labelled block to
  each match and puts its handler bodies in its own new
  `quilltap-web/src/qtap_routes.rs`.
- Fixture: `crates/quilltap-web/tests/fixtures/system-data-{main,mount,llmlogs}.db`
  (committed) + builder `harness/oracle/fixtures/build-system-data-fixture.ts`
  and `harness/oracle/fixtures/system-data.json`.
- JSON key order is **insertion order** (`preserve_order` is on) — see the
  memory note `llm-logs-key-order-and-preserve-order`. The NDJSON diff is
  line-by-line, so key order is load-bearing here.

## Tiered deliverables

### Tier 1 — must land

1. **The export writer** — `services/qtap_export/**`: `resolve_export_ids`,
   `build_manifest`, the ten per-type record generators with their inline
   `_tagNames` / `_apiKeyLabel` / `_participantInfo` / blob-chunk resolution,
   the envelope/footer discipline, and the `sanitizeProfile` `apiKeyId` strip.
   Rust has no async generators — an iterator/`Vec<Value>`-per-entity shape is
   fine, but **the emitted line sequence and each line's key order must be
   byte-identical to v4's**.
2. **`systemExportEntities` + `systemExportPreview`** dispatch verbs +
   their `?action=export-entities` / `?action=export-preview` GET edges.
3. **`POST /api/v1/system/tools?action=export`** — the streaming `.qtap`
   web-edge leg, with the three headers reproduced verbatim (filename
   derivation included).
4. **The import pipeline** — extend `services/quilltap_import/**` to the full
   v4 surface: `validate_export_format` (the exact error strings),
   `preview_import` (count-only, no writes), `execute_import` with the ten-map
   `IdMappingState`, the imported/skipped/warnings bags, all four conflict
   strategies, the per-entity importers (characters, entities, profiles,
   document-stores), legacy presets, and the `reconcile` relationship pass.
5. **`systemImportPreview` + `systemImportExecute`** dispatch verbs (the JSON
   legs) **and** the two multipart web-edge legs, including the `'replace'` →
   `'overwrite'` remap at the route boundary and `loadQtapFromUpload`'s
   NDJSON-vs-legacy-monolith sniff (`peekFormat` → `readNdjsonLines` /
   `collectLegacyJson` → `assembleExportFromStream`).
6. **The differentials** (both fresh at `e646f58b`, run by name, zero SKIP):
   - **`system_export_equivalence`** — drive v4's REAL `createNdjsonStream` /
     `streamExportRecords` over the fixture and diff the Rust writer's output
     **line by line**. Cover all ten entity types × `scope: all | selected` ×
     `includeMemories: true | false` where meaningful, plus the unknown-type
     error, plus `previewExport`. Normalize only legitimately-nondeterministic
     fields (the manifest timestamp) — everything else is exact.
   - **`system_import_equivalence`** — tier-2 DB-state diff after
     preview + execute for **each of the four conflict strategies**, over the
     same uploaded export, using the minted-UUID positional remap for the
     `duplicate` arm (recipe: `tool-handler-differential-minted-remap`).
     Include the validation-failure arms (bad format, bad version, bad
     conflictStrategy) asserting v4's exact error strings.

### Tier 2 — should land

- **Flesh out the e2e export→import round-trip beat** at
  `apps/web/e2e/settings-data-system-flow.spec.ts:176` (currently a shallow
  stub that stops at the dialog's first step — the comment there says "fleshed
  out when the web-edge legs land"). Drive a real export download → re-import →
  summary. If the lane runs out of budget, leave it and say so — the unifier is
  authorized to finish it.
- Round-trip self-consistency check: a v5-written `.qtap` re-imported by v5
  reaching the same DB state as v4's round-trip.

### Tier 3 — explicit deferrals (loud, named)

- Anything that cannot land ships as a `not_available`-style **loud typed
  refusal** naming exactly what is missing (e.g. a single entity type whose
  generator is unported) — never a silent stub, and never an export that
  quietly emits fewer records than v4.
- The SillyTavern importer stays where it is; not this lane.

## The differential requirement, restated

Every ported unit ships with an equivalence test driving v4's **REAL** code
(jest/tsx oracle → NDJSON → Rust diff). Closest recipes, in the memory notes:
**`jest-real-db-oracle`** (`resetModules` + `doMock` past `jest.setup`; Node 24),
**`oracle-jest-testtimeout`** (`testTimeout=120000` — the import cases are slow),
**`oracle-regen-silent-stale-pass`** (an erroring builder leaves the OLD fixture
— grep the NDJSON after every regen), **`tool-handler-differential-minted-remap`**
(positional UUID/timestamp normalization for the `duplicate` strategy),
**`byte-exact-static-data-transcription`**, and
**`llm-logs-key-order-and-preserve-order`** (key order is insertion order —
build typed structs in schema order, never sort). P4.9G1's
`system_jobs_routes_equivalence` is the in-repo exemplar over this same fixture;
its full regen recipe is in `status-log.md` under "Lane record — P4.9G1". Run
every differential **by name** with `--nocapture`, zero SKIP, per-case OK lines
visible.

## Fixtures

- **Consumes** the committed `system-data-{main,mount,llmlogs}.db` family. The
  export differential needs at least one row of every exportable type; if the
  graph is thin for a type (groups, document-stores), extend it via
  `build-system-data-fixture.ts` + `system-data.json`.
- **If you change that fixture, this lane owns regenerating and re-running
  `system_jobs_routes_equivalence`** (it rides the same family) — and must
  flag the change in its lane record so the unifier re-runs G3's and G5's
  differentials too.
- Import execute writes: run each case against a **fresh copy** of the fixture,
  never the committed file.
- Journal files are never fixtures — delete stray `.db-journal` after any regen
  (the standing P4.15 rule).

## Shared contract (BINDING — identical in all three orders)

### §1 — the wire surface is FROZEN

All sixteen §1 CoreRequest variants are already on main (`api/types.rs:2293-2375`)
and pinned by `crates/quilltap-harness/tests/p4_9g1_wire_contract.rs`. The SPA
(P4.9G2, CLOSED) is fully built against them. **No lane may change a variant's
name, field name, or field type.** If a port reveals a genuine mismatch with
v4's route body, STOP and report — it is a round-level decision, not a lane one.

The web-edge-only legs (no dispatch verb; the SPA reaches them raw via
`apiUrl()`), assigned by lane:

| Leg | Lane |
| --- | --- |
| `POST /api/v1/system/tools?action=export` — the NDJSON `.qtap` stream | **G4** |
| `POST /api/v1/system/tools?action=import-preview\|import-execute` (multipart) | **G4** |
| `GET /api/v1/system/backup/{id}` — the single-use zip download | **G5** |
| `POST /api/v1/system/restore?action=upload` — octet-stream → `{uploadId}` | **G5** |

### §2 — the cross-lane Rust seam: `delete_user_data` (G3 delivers, G5 consumes)

**P4.9G3** delivers, in `crates/quilltap-core/src/services/delete_all.rs`,
exactly:

```rust
/// v4 `lib/backup/restore/delete-service.ts:90` `deleteUserData(userId)` —
/// the full per-user wipe, ALSO used by restore's `replace` mode.
pub async fn delete_user_data(db: &Db, user_id: &str) -> Result<(), DbError>;
```

**P4.9G5** consumes it as restore's `mode: 'replace'` pre-step (v4
`restore/restore.ts:52`). Because the two lanes run in parallel, **G5 writes
that call site as a loud typed refusal** carrying the marker comment

```rust
// ACTIVATE-AT-UNIFY (P4.9G5 ← P4.9G3 §2): replace mode calls
// services::delete_all::delete_user_data(&db, user_id).await?
```

and ships the `system_restore_equivalence` differential covering
`mode: 'new-account'` only. **The unifier** flips the call site and adds the
`replace`-mode case to that differential (both lanes' orders say so; the recipe
is in G5's §Fixtures). No other cross-lane Rust dependency exists in this round;
if a lane finds one, STOP and report rather than inventing a second seam.

### §3 — shared-file regions and the conflict discipline

Four files are unavoidably touched by more than one lane. Each lane confines
itself to a **labelled region** so the cherry-pick conflicts stay local and
mechanically resolvable:

1. **`crates/quilltap-core/src/api/engine.rs`** — the single "not yet
   available" arm at `:736-746`. Each lane (a) **deletes only its own variant
   names** from that arm's `|` list, and (b) appends its real arms immediately
   ABOVE the arm, wrapped in `// ── P4.9G<n> arms ──` … `// ── end P4.9G<n> ──`
   markers. When all three land the residual list is empty and the refusal arm
   is deleted — **the last lane to be cherry-picked removes it**; the unifier
   verifies no orphan remains.
2. **`crates/quilltap-web/src/system_data_routes.rs`** — the `?action=` match
   arms in `system_tools_get` (:71) and `system_tools_post` (:132). Each lane
   adds its own actions in a `// ── P4.9G<n> ──` labelled block at the END of
   each match, before `other => unknown_action(other)`.
3. **`crates/quilltap-web/src/lib.rs`** — `build_router`. Only **G5** registers
   new paths (`/api/v1/system/backup`, `/api/v1/system/backup/{id}`,
   `/api/v1/system/restore`); **G3** registers `/api/v1/system/jobs` and the
   unlock alias; **G4** registers nothing (its legs ride the existing
   `/api/v1/system/tools` route). Add `pub mod` lines at the end of the
   existing block.
4. **`apps/web/e2e/settings-data-system-flow.spec.ts`** — per-lane blocks only:
   **G3** owns the `DELETE_ALL_SERVER_LANDED` constant (`:36`) and the
   `Delete All Data (destructive; last)` describe (`:193`); **G4** owns the
   `export → import round-trip` test body (`:176`); **G5** appends a NEW test
   inside the main describe, at the end. No lane reformats or reorders the file.
   `apps/web/e2e/global-setup.ts` is **G5-exclusive**; a lane needing a seed
   there must say so in its lane record instead of editing it.

`crates/quilltap-core/src/services/mod.rs` is **APPEND-ONLY SHARED** — each lane
adds its own `mod` declaration lines; the unifier union-merges. `docs/CHANGELOG.md`
and `docs/developer/porting/status-log.md` are append-only for everyone.

## Ownership (BINDING — identical in all three orders)

| Lane | Owns (exclusive) | Bumps |
| --- | --- | --- |
| **P4.9G3** (delete-all + REST remainders) | `services/delete_all.rs` (new), `api/system_data.rs`, the delete + jobs-collection + unlock-alias edges, `system_delete_data_equivalence` + its oracle case, the e2e delete-all describe + its gate constant | core, harness, web |
| **P4.9G4** (`.qtap` export/import) | `services/qtap_export/**` (new), `services/quilltap_import/**` (the existing seed-subset module — extends it), new `api/system_qtap.rs` (name indicative), the export/import REST + streaming + multipart legs in a new `quilltap-web/src/qtap_routes.rs`, `system_export_equivalence` + `system_import_equivalence` + their oracle cases, the e2e export→import test body | core, harness, web |
| **P4.9G5** (backup/restore) | `services/backup/**` (new, incl. the restore + uuid-remap submodules), new `api/system_backup.rs` (name indicative), new `quilltap-web/src/backup_routes.rs` + the `lib.rs` backup/restore registrations, any new host seam in `quilltap-host/src/{spine.rs,host.rs}`, `system_backup_equivalence` + `system_restore_equivalence` + their oracle cases, `apps/web/e2e/global-setup.ts`, its new e2e beat | core, harness, web, host |

Shared/append-only: `api/engine.rs`, `system_data_routes.rs`, `lib.rs`,
`services/mod.rs`, the e2e spec (all per §3); `CHANGELOG` + `status-log.md`.
**FROZEN for everyone: `api/types.rs`.**

⚠ All three lanes bump `core`, `harness` and `web` off the same base.
**Identical bumps merge SILENTLY at cherry-pick** — the unifier RECOUNTS as
base + total bumps (the standing gotcha; do not try to pre-sequence).

## Verification gate (run before declaring done)

1. `cargo fmt --all --check`
2. `cargo clippy --workspace --all-targets -- -D warnings` — plain **AND**
   `--features quilltap-core/native-transport`
3. `TZ=UTC cargo test --workspace` with this lane's env vars — zero failures.
   Remember `cargo test --workspace` is **fail-fast per binary**; use
   `--no-fail-fast` to see real blast radius.
4. This lane's differentials **by name**, `--nocapture`, zero SKIP, per-case OK
   lines visible — **plus** the already-committed seed-import differentials
   (P4.4u4), since this lane extends `services/quilltap_import/`.
5. Oracles regenerated fresh from `~/source/quilltap-server` at `e646f58b`
   (drift-check FIRST; if v4 moved, STOP and report).
6. `cargo build --release` (the web bin is affected).
7. If tier 2's e2e beat was fleshed out: full Playwright over a fresh
   `ng build` dist + rebuilt debug bins.
8. Per-commit rule: follow `.claude/commands/commit.md` (CHANGELOG entry +
   version bumps per §Ownership + the full gate on every commit).

**Disk:** export `CARGO_INCREMENTAL=0` for the whole lane. Three parallel
worktrees at default settings will not fit (`target/debug/incremental` alone hit
64 GB in one past lane); with it off a worktree target stays in the 10–20 GB
range. Check `df -h ~` before starting and again mid-lane.
