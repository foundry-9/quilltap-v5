# Work order P4.85 — the Rust follow-ups the generator round recorded: the `[Chats v1] Impersonation stopped` line, the K0 re-framer's `Closed` arm made loud, the `.expect("no issues")` parse path and the `unwrap_or(Null)` 200 in `generators_detail.rs`, the route-level "starting" lines pinned, `image_profiles.rs`'s UTF-16 count, and P4.9K1/K2's Tier-2 items 9/10 for the three non-import families

**Status:** OPEN (ordered 2026-09-07, the generator follow-ups +
prompt-templates round: P4.82 ∥ P4.83 ∥ P4.84 ∥ P4.85 ∥ P4.86-conditional).
The unifier updates this header.

## Preamble

- **v4 oracle baseline: `2f4254b42`** (drift-ledger §1); **zero drift**.
  Every item is a "recorded, not fixed" row from the `p4.9k` or the
  `2f4254b42` round record, or the two K orders' OPEN Tier-2 items 9/10.
- **Freshness probe at lane start** (ledger §2): checkout on **`main`**,
  tree clean, both log ranges EMPTY. **Any mismatch: STOP and report.**
- **Regen rule: NO PIN REQUIRED** — the three oracle cases this lane
  EXTENDS regenerate from the live checkout after a passing probe through
  `recipe_sweep.py --run <family>`; the unifier regenerates all four
  generator tier-3 families ONCE at unification (P4.86 extends the fourth).
- **Sibling lanes:** P4.86 owns `api/generators_wizard.rs`,
  `generators/ai_import.rs` and the AI-import family — **every AI-import
  item in the round records is P4.86's, including that file's ten
  `.expect("no issues")` sites and its `[Characters v1] AI Wizard starting`
  line.** P4.82 owns `generators/wizard.rs`, `host.rs`, `spine.rs`; P4.83
  the prompt-templates verb + edge; P4.84 the SPA.
- **Worktree note:** own worktree + lane branch
  (`claude/p4-85-rust-generator-smalls-<hash>` from `main`);
  `CARGO_INCREMENTAL=0`. No SPA work: `git diff main -- apps/web/` EMPTY.

## Mandate

Discharge the Rust-side follow-ups the generator round banked, each with
its pin: (1) v4's `[Chats v1] Impersonation stopped` info line at v4's site
in `chat_stop_impersonate`; (2) the K0 SSE re-framer's two `RecvError::
Closed` arms made LOUD (a v5-only divergence — v4's per-route
`ReadableStream` cannot close under a running run); (3) `api/generators_
detail.rs`'s seven `.expect("no issues")` sites, its silent sibling at
`:504-507`, and the `serde_json::to_value(result).unwrap_or(Value::Null)`
200 — one typed refusal shape, pinned; (4) the two route-level `[Characters
v1] … starting` lines in that file capture-pinned with v4's field bags; (5)
`api/image_profiles.rs:655`'s `utf16_len` → the `zod_len_*` code-point
helpers `api::images::images_generate` already uses, with an astral-prompt
arm in the family that covers `?action=generate`; (6) K1/K2 Tier-2 item 9
(the SSE edge proven BYTE-exact against the oracle's recorded stream) for
the optimizer and wizard stream edges, and item 10 (the `llm_logs` row
counts compared per case) for the optimizer, external-prompt and wizard
families. What this lane must NOT touch: `api/generators_wizard.rs`,
`generators/ai_import.rs`, the AI-import oracle/family/fixture (P4.86's);
`characters_routes.rs` / `system_data_routes.rs` (nobody's); the SPA.

## Survey-verified starting points (2026-09-07 @ v4 `2f4254b42` — do not re-derive)

### 1. The impersonation line

- v4 `app/api/v1/chats/[id]/actions/participants.ts:126`:
  `logger.info('[Chats v1] Impersonation stopped', { chatId, participantId,
  characterName })` — AFTER the optional profile reassignment (`:113-121`)
  and the `resolveParticipantCharacterName` read (`:124`), BEFORE the
  response. v5 `api/salon.rs:1838-1927` `chat_stop_impersonate` has the
  same sequence (the `remove_impersonation` write, the optional
  `update_participant`, the `character_name` read) and NO log line. Add it
  at v4's position with the three fields (`%chat_id`, `%participant_id`,
  `%name` — the `%` sigil renders unquoted; pin the rendered bytes), on
  the salon target; a capture-layer test over the committed pair the
  P4.80 family drives (`stop_impersonate_*` rows) asserting the line
  fires exactly once on success and NOT on the 404 / failed-write arms.

### 2. The re-framer's `Closed` arms (`crates/quilltap-web/src/generator_sse.rs`)

- `:121` (the pre-commit race): `Err(RecvError::Closed) => break None` —
  the event channel closed before the first frame or the run's resolution:
  the loop exits with `finished = None` and an empty `pending`, so the
  caller commits to a stream that then… measure exactly what the client
  receives (an empty `text/event-stream`? a status?) — the run itself
  keeps executing on the engine. `:188` (the pump): `Err(RecvError::Closed)
  => break` → the drain + close. Both are engine-shutdown-only today
  (the broadcast sender lives for the engine's life), so no user can hit
  them — but a silent arm is the #103/#110/#116 class. Make both LOUD: a
  `tracing::warn!` naming `progress_id` and the arm (the `Lagged` arm's
  spelling at `:113-119` is the model), and record the v5-only divergence
  in the module doc beside the `Lagged` one. Pin with a unit test that
  drops the broadcast sender mid-race and asserts the warn + the response
  shape (whatever it measures as — write it down).

### 3. The parse path in `api/generators_detail.rs`

- Seven `.expect("no issues")` sites (grep at planning: 7 in this file,
  10 in `generators_wizard.rs` — the latter P4.86's). Sound today (every
  `None` branch pushes an issue) but the same file answers the same
  impossible state SILENTLY at `:504-507` (the `scenario_id` arm returns
  `None` where its siblings push). Choose ONE shape for the whole file:
  a `debug_assert!`-free typed arm that answers the route's generic 500
  sentence (measure v4's: an uncaught non-Zod throw in the handler is the
  context middleware's `{ error: 'Internal server error' }`-class body —
  read `lib/api/middleware.ts` for the exact sentence and status) — and a
  unit test per site class proving the arm is reached when an issue list
  is empty while a field is missing (construct the state directly).
- `serde_json::to_value(result).unwrap_or(Value::Null)` in this file (grep
  it — the round record names the pattern for the detail routes): a
  serialization failure answers 200 + `null`; v4's `NextResponse.json`
  throwing is the middleware 500. Make it the same typed 500 arm.

### 4. The route-level "starting" lines

- `generators_detail.rs:555` `[Characters v1] External prompt generation
  starting` and `:763` `[Characters v1] Character optimizer starting
  (streaming)` — "compared by nothing" (round record). v4's sites:
  `app/api/v1/characters/[id]/handlers/post.ts` — measure each line's
  LEVEL and field bag (`characterId`, `profileId`, the mode/options keys)
  and pin with the thread-scoped capture seam over the committed
  `character-generators` pair (the families' fixtures), one test per line,
  mutation-proven (drop a field → red).

### 5. `image_profiles.rs:655`

- `let prompt_units = crate::jsstr::utf16_len(prompt); if !(1..=4000).
  contains(&prompt_units) …` — Zod 4.5 `z.string().min(1).max(4000)`
  counts CODE POINTS (the `d883a5ee1` round's finding; `zod_version_guard`
  pins 4.5.4); `api::images::images_generate` uses `jsstr::zod_len_min_ok`
  / `zod_len_max_ok` for the identical schema, so the two sibling routes
  disagree on astral prompts. Switch, and add a 4000-code-point prompt
  containing astral characters (4001+ UTF-16 units) to the family covering
  `POST /api/v1/image-profiles/{id}?action=generate` (P4.73 landed it over
  an erased image-generation seam with a frozen-clock 37-case oracle —
  find it by grep for `action=generate` in `crates/quilltap-harness/tests`)
  — v4 answers 200 (the tool runs), pre-fix v5 answers 400; red-first.

### 6. K1/K2 Tier-2 items 9 and 10 (three families)

- **Item 9 — the SSE bytes.** The oracle cases parse the stream into
  events (`character-wizard-tier3.test.ts:270-289`: `buffered` is the raw
  text, split on `\n`, `data:` payloads `JSON.parse`d into `events`);
  the raw bytes are DISCARDED. Extend the optimizer and wizard oracle
  cases (`character-optimizer-tier3.test.ts`, `character-wizard-tier3.
  test.ts`) to emit `rawSse: buffered` per case (keep `events`); v4's
  framing is `data: ${JSON.stringify(event)}\n\n` per event (measure at
  `post.ts:120-142` and the collection handler's stream) with headers
  `Content-Type: text/event-stream`, `Cache-Control: no-cache`,
  `Connection: keep-alive` (`post.ts:137-141`). Then a web wire test per
  edge (`crates/quilltap-web/tests/generator_sse_wire.rs` — it already
  asserts `cache-control` + `connection` at `:70-71`; `characters_
  generators_routes.rs:258-262` asserts only `no-cache`): drive `POST
  /api/v1/characters/{id}?action=optimize-stream` and `POST /api/v1/
  characters?action=ai-wizard-stream` with the canned driver over the
  committed pair for ONE recorded case each and assert the response body
  bytes EQUAL the oracle's `rawSse` (after the same minted-id/timestamp
  normalization the dispatch family applies to `events`), and all three
  headers. The K0 re-framer's own per-event bytes (`frame(inner)`) are
  the port under test. External-prompt is JSON, not SSE — item 9 does not
  apply (record).
- **Item 10 — the `llm_logs` counts.** The oracles keep `logLLMCall` REAL
  and write the scratch data dir's llm-logs DB, "not dumped (recorded)".
  Dump `SELECT type, COUNT(*) … GROUP BY type` from that DB per case into
  the row (`llmLogCounts`), for all three families; v5 compares its own
  `llm_logs` counts after each case (P4.79's shape — the help-chat
  family's `llm_logs` rows). Measure v4's actual `type` strings per
  runner (`CHARACTER_WIZARD` for `generateField`; the optimizer's and the
  external prompt's — find their `logLLMCall` sites in
  `character-optimizer.service.ts` / `external-prompt-generator.service.
  ts`) — the SPA's `llm-logs.api.ts:42` lists `CHARACTER_OPTIMIZER` as a
  purpose. Red-first where v5 writes a different count.

## Tiered deliverables

### Tier 1 — must land

1. The impersonation line + its capture test.
2. The two loud `Closed` arms + the unit test + the module-doc record.
3. The `generators_detail.rs` parse path on one typed 500 arm (incl.
   `:504-507` and the `unwrap_or(Null)` site), unit-pinned.
4. The two "starting" lines capture-pinned with v4's bags.
5. `image_profiles.rs` on the code-point helpers + the astral arm, red-first.
6. Item 9 for the optimizer and wizard edges (oracle `rawSse` + the two
   byte-equal wire tests); item 10 for all three families (oracle
   `llmLogCounts` + the compares).

### Tier 2 — should land

7. The `generator_sse_wire.rs` header assertions unified across both
   edges (all three headers on both).
8. A recorded-vs-served header census for the K1 JSON arms (`rename`,
   `refresh-archive`, `generate-external-prompt`): `content-type` only
   (JSON), as v4's `NextResponse.json`.

### Tier 3 — explicit refusal-arm deferrals (loud, typed, never silent)

- v4's `Plugin system initialization failed` arm and v5's v5-only
  `pdf-parse not available` line (round record) — these belong to the
  wizard/import files (P4.86's); NOT this lane.
- The five `is_none_or` ownership gates (unreachable — the profile row
  marshals a required `String`): recorded again, not changed, unless the
  parse-path rewrite touches them naturally.

## The differential requirement (restated)

- Items 1, 3, 4: capture-layer / unit pins over the committed pairs the
  existing families drive (`chat-delete-*` / `character-generators-*`);
  the log pins follow `tracing-percent-field-renders-unquoted` and the
  thread-scoped seam.
- Item 5: the existing `?action=generate` family gains the astral arm —
  regenerate its oracle through the sweep driver (its recipe header names
  the family), red-first.
- Item 6: the three tier-3 families (`character_optimizer_tier3_
  equivalence`, `character_wizard_tier3_equivalence`, `external_prompt_
  tier3_equivalence`) gain the `rawSse` / `llmLogCounts` comparands; the
  oracle cases regenerate from the live checkout (their headers carry the
  jest recipe — `--roots` + the `/tmp` mirror; the P4.9K1 `character-
  optimizer-tier3.test.ts:33-46` recipe); mutation proof: alter one byte of
  the re-framer's frame → the wire test reddens; drop a `log_llm_call` →
  the count reddens.

## Fixtures

- Reads `character-generators-{main,mount}.db` and the P4.80 `chat-delete-*`
  pair; regenerates NEITHER. Extends the three per-family oracle fixture
  JSONs (`character-optimizer-tier3.json`, `character-wizard-tier3.json`,
  `external-prompt-tier3.json`) only if a case needs a new shape.
- The unifier regenerates all four generator families once after P4.86's
  AI-import edits are in.

## §R — the round-wide meeting points (BINDING — identical in every order of this round)

Round: **the generator follow-ups + prompt-templates round** — P4.82 ∥ P4.83
∥ P4.84 ∥ P4.85 ∥ P4.86 (P4.86 CONDITIONAL on the human's dependency
ruling — see its preamble). Ordered 2026-09-07. The first non-drift round
since the follow-ups round 2 (2026-09-04): nothing to absorb, every lane is
banked follow-up work from the `p4.9k` and `2f4254b42` rounds.

1. **Baseline and drift.** Oracle baseline **`2f4254b42`** (drift-ledger §1,
   "feat: character subprompts", `4.10.0-dev.1`). v4 `main` HEAD **AT the
   baseline** and v4 `bugfix` tip **`1a2b2164c`** unmoved; the ledger's §3 is
   EMPTY — **zero drift**. No ledger row is ordered by this round.
2. **The freshness probe at lane start** (ledger §2), expected values:
   checkout on **`main`**, tree CLEAN, `git log --oneline 2f4254b42..main`
   EMPTY, `git log --oneline 1a2b2164c..bugfix` EMPTY. **Any mismatch: STOP
   and report** — a lane never writes the ledger and never builds its own
   pin; the planner re-points the round. Re-run the probe before EVERY regen
   batch (the checkout has gone dirty and moved MID-LANE before — ledger
   §5.1; a lane that discovers drift mid-way finishes nothing that depends
   on a regen and reports).
3. **Regen rule: NO PIN REQUIRED** (ledger §1) — every regen runs from the
   live `~/source/quilltap-server` checkout after a passing probe, through
   `harness/tools/recipe_sweep.py --run <family>` (the sanctioned path; it
   refuses empty-stage families and unknown names); never two sweeps
   concurrently. Committed recipe headers keep saying `cd
   ~/source/quilltap-server` and never name a `/tmp` pin
   (`recipe-header-conventions`). The oracle `node_modules` are the LIVE
   tree (`zod` 4.5.4 — `zod_version_guard` fails the gate if it moves; every
   Zod length rule transcribed this round uses the `jsstr::zod_len_*`
   code-point helpers, never `utf16_len`). Check every NDJSON is non-empty
   before believing a diff (the empty-file trap; a family's env var is not
   its regen var — `QT_ORACLE_<FAMILY>` reads it, `QT_ORACLE_OUT` writes it).
4. **Branching.** Every lane branches from `main` into its own worktree:
   `git worktree add <path> -b claude/p4-8N-<slug>-<hash> main`;
   `CARGO_INCREMENTAL=0`; `npm ci` (never `cp -a`) for the SPA lanes. No
   stacking this round. P4.86 does not start unless the human has granted
   the dependency ruling its preamble names.
5. **Fenced-append shared files.** `crates/quilltap-core/src/api/types.rs`,
   `crates/quilltap-core/src/api/engine.rs`, `crates/quilltap-core/src/lib.rs`
   (module lines), `crates/quilltap-core/src/services/mod.rs`,
   `crates/quilltap-core/src/generators/mod.rs`,
   `crates/quilltap-core/src/services/queue_service.rs`,
   `crates/quilltap-host/src/host.rs`, `crates/quilltap-host/src/spine.rs`,
   `crates/quilltap-web/src/lib.rs` (module + route lines): each Rust lane
   appends ONLY inside its own fence (`// === P4.82 ===` … `// === end P4.82
   ===`, opened once per file, new variants/arms/registrations at the END of
   the relevant block, never interleaved). `crates/quilltap-web/src/
   characters_routes.rs` and `system_data_routes.rs` are touched by NOBODY
   this round (P4.85's wire tests live in `crates/quilltap-web/tests/`).
   `Cargo.toml`/`Cargo.lock` at the workspace root and in every crate:
   version lines only — **except P4.86, the ONE lane authorized to add a
   dependency (`jsonschema`), and only once the human's ruling is recorded
   in this round's report.** Any other dependency add is a STOP-and-flag.
6. **`apps/web/**` is split between TWO lanes by file:** P4.83 owns
   `screens/characters/generators/prompts-editor/import-modal.ts`, its NEW
   `prompts-editor/prompt-templates.api.ts`, `screens/characters/edit/
   system-prompts-tab.ts`, `screens/characters/new/new-character.ts` and
   its NEW e2e spec; P4.84 owns `screens/characters/generators/wizard/
   generation-step.ts` (+ its NEW preview-markdown module), `generators/
   optimizer/**`, `generators/ai-import/**`, `chat/cast/add-character-
   dialog.ts`, `screens/salon/salon-conversation.ts`, `screens/characters/
   view/tabs/details-tab.ts`, `ui/prompt-field-label.ts` (+ a NEW
   `ui/prompt-field-example.ts` if the example line is extracted) with
   `screens/characters/edit/scenario-editor.ts` ONLY to adopt that
   extraction, `workspace/chrome/link-interceptor.spec.ts` (spec only), and
   the three existing generator e2e specs. `apps/web/src/app/core/
   core-contract.ts` is FROZEN: P4.83 declares its DTO in its own wire file
   verbatim from its §B and the unifier folds it (the name-for-name diff).
   The Rust-only lanes (P4.82, P4.85, P4.86) finish with `git diff main --
   apps/web/` EMPTY.
7. **Frozen files.** `docs/developer/porting/drift-ledger.md` (lanes never
   write it); `help/**`; `apps/web/src/app/shell/shell.ts` and
   `apps/web/src/app/workspace/workspace-contract.ts` (no new tab kinds).
   `crates/quilltap-web/tests/fixtures/character-generators-{main,mount}.db`
   is READ by P4.82 (if it reuses it), P4.85 and P4.86 and REGENERATED BY
   NOBODY.
8. **Version bumps.** P4.82: `quilltap-core`, `quilltap-harness`,
   `quilltap-host`. P4.83: `quilltap-core`, `quilltap-harness`,
   `quilltap-web`, `apps/web/package.json`. P4.84: `apps/web/package.json`.
   P4.85: `quilltap-core`, `quilltap-harness`, `quilltap-web`. P4.86:
   `quilltap-core`, `quilltap-harness` (+ the root `Cargo.lock` for the
   dependency). Per commit. The unifier RECOUNTS as base + total bumps —
   identical bumps merge silently (`commit-checklist`; the playbook's
   silent-auto-merge trap), and a `Cargo.toml` conflict is NOT version-only
   when P4.86 is in the round (the `p4.9i2` §3 catch: a `--ours` resolution
   dropped a feature block).
9. **e2e.** Port 4319 is repo-wide — ONE Playwright at a time across all
   lanes. P4.83 and P4.84 each run ONLY their own spec files by name (never
   the full suite — that is the unifier's), and never kill a server they did
   not start (`pkill -f` can kill a sibling lane's gate; read `pgrep -fl`
   argv first). A held port means WAIT, not force. A beat's mock must speak
   the runner's transport (the optimizer and AI-import runners call the
   NON-streaming `send_message`; the shared `support/mock-llm.ts` speaks
   only SSE — see the spec headers' recipes).
10. **Append-only for everyone:** `docs/CHANGELOG.md`,
    `docs/developer/porting/status-log.md`.
11. **Fixture ownership.** P4.82 delivers a NEW committed
    `crates/quilltap-harness/fixtures/headshoulders-{main,mount}.db` pair (+
    its builder under `harness/oracle/fixtures/`, v4's REAL repositories,
    pinned ids) unless it can prove an existing pair carries every shape it
    needs (record the measurement either way). P4.83 builds its fixture in
    `/tmp` from a clean provisioned instance inside its recipe (the
    `settings_wire_actions` precedent) and commits NOTHING under
    `fixtures/`. P4.85 and P4.86 EXTEND the four generator tier-3 oracle
    cases (three P4.85's, the AI-import one P4.86's) — the unifier
    regenerates all four ONCE at unification after both lanes' edits are in
    (the P4.D119 rule). P4.84 seeds through the running server's verbs,
    never SQL.
12. **Census guards move when a verb is added.** P4.83 adds dispatch verbs;
    adding any verb, or closing a recorded divergence (P4.86 retires
    `VALIDATION_UNAVAILABLE`'s both-directions pins), moves the executable
    censuses in OTHER crates (`a-new-verb-moves-the-dispatch-wrong-type-
    census`, `closing-a-divergence-moves-the-censuses`) — every Rust lane's
    gate is `cargo test --workspace`, never `-p`, with the full log captured
    to a file + a sentinel (never `| tail`, never a `pgrep` poll loop).
13. **Log-line pins** use the thread-scoped capture seam (`a-process-global-
    test-seam-must-be-thread-scoped`); a `%x` field renders unquoted, a bare
    or `?x` field quoted (`tracing-percent-field-renders-unquoted`) — read
    v4's `logger.<level>(msg, bag)` bag and pin the LEVEL, the sentence and
    the field NAMES.

## Ownership (BINDING — identical table in every order of this round)

| Lane | Owns (edit freely) | Fenced-append (§R.5) | Must not touch |
| --- | --- | --- | --- |
| **P4.82** | NEW `crates/quilltap-core/src/services/headshoulders_backfill_job.rs`; NEW `crates/quilltap-core/src/services/headshoulders_backfill_enqueue.rs`; `crates/quilltap-core/src/generators/wizard.rs` ONLY for the `generate_field` / `WizardCallCtx` visibility edit (one fenced block, no behaviour change); NEW harness families `headshoulders_backfill_tier3_equivalence` + `headshoulders_backfill_enqueue_equivalence` + their oracle cases + the fixture builder + the committed pair; NEW host tests under `crates/quilltap-host/tests/` | `services/queue_service.rs`, `services/mod.rs`, `host/host.rs`, `host/spine.rs` | `api/**`, `apps/web/**`, every other generator file |
| **P4.83** | `crates/quilltap-core/src/db/prompt_templates.rs`; NEW `crates/quilltap-core/src/api/prompt_templates.rs`; NEW `crates/quilltap-core/src/services/builtin_prompt_templates.rs` + NEW `crates/quilltap-core/src/services/builtin_prompt_templates.json`; NEW `harness/oracle/provision/dump-prompt-templates.ts`; NEW `crates/quilltap-web/src/prompt_templates_routes.rs` (+ its wire test); NEW harness family `prompt_templates_routes_equivalence` + oracle case; SPA: `prompts-editor/import-modal.ts`, NEW `prompts-editor/prompt-templates.api.ts`, `edit/system-prompts-tab.ts`, `new/new-character.ts`, NEW `e2e/prompt-templates-flow.spec.ts` | `api/types.rs`, `api/engine.rs`, `core/lib.rs`, `services/mod.rs`, `web/lib.rs` | `core-contract.ts` (frozen), every P4.84 SPA file, every other Rust file |
| **P4.84** | SPA only: `generators/wizard/generation-step.ts` + NEW `generators/wizard/generation-preview-markdown.ts`; `generators/optimizer/**`; `generators/ai-import/**`; `chat/cast/add-character-dialog.ts`; `screens/salon/salon-conversation.ts`; `screens/characters/view/tabs/details-tab.ts`; `ui/prompt-field-label.ts` (+ NEW `ui/prompt-field-example.ts`) and `edit/scenario-editor.ts` ONLY to adopt the extraction; `workspace/chrome/link-interceptor.spec.ts`; the existing `e2e/character-wizard-flow.spec.ts`, `e2e/character-optimizer-flow.spec.ts`, `e2e/ai-import-flow.spec.ts` | — | every Rust file; `import-modal.ts`, `system-prompts-tab.ts`, `new-character.ts` (P4.83's); `core-contract.ts` |
| **P4.85** | `crates/quilltap-core/src/api/salon.rs`; `crates/quilltap-core/src/api/generators_detail.rs`; `crates/quilltap-core/src/api/image_profiles.rs`; `crates/quilltap-web/src/generator_sse.rs`; `crates/quilltap-web/tests/generator_sse_wire.rs` + `crates/quilltap-web/tests/characters_generators_routes.rs`; harness `character_optimizer_tier3_equivalence.rs`, `character_wizard_tier3_equivalence.rs`, `external_prompt_tier3_equivalence.rs` + their oracle cases (`character-optimizer-tier3.test.ts`, `character-wizard-tier3.test.ts`, `external-prompt-tier3.test.ts`) + their per-family fixture JSONs; the family that covers `image-profiles?action=generate` (+ a new astral-prompt arm) | — | `api/generators_wizard.rs`, `generators/ai_import.rs`, `ai_import_tier3_equivalence.rs`, `ai-import-tier3.test.ts` (P4.86's); `apps/web/**` |
| **P4.86** (conditional) | `crates/quilltap-core/Cargo.toml` (the ONE `jsonschema` dependency line) + the root `Cargo.lock`; NEW `crates/quilltap-core/src/generators/qtap_schema.rs` + NEW vendored `crates/quilltap-core/src/generators/qtap-export.schema.json`; `crates/quilltap-core/src/generators/ai_import.rs`; `crates/quilltap-core/src/api/generators_wizard.rs`; harness `ai_import_tier3_equivalence.rs` + `harness/oracle/cases/ai-import-tier3.test.ts` + `harness/oracle/fixtures/ai-import-tier3.json`; NEW harness family `qtap_schema_validate_equivalence` + its oracle case + recorded corpus | `generators/mod.rs` | every other file; `apps/web/**` |

Everyone: `docs/CHANGELOG.md` + `status-log.md` append-only; the drift
ledger frozen; `core-contract.ts` / `shell.ts` / `workspace-contract.ts`
frozen; `characters_routes.rs` / `system_data_routes.rs` untouched.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; clippy both feature sets; `cargo build
   --workspace`; the release build.
2. `cargo test --workspace` (full log + sentinel; never `| tail`) with the
   three families' oracle vars + the `?action=generate` family's + the
   committed-pair fixture vars; `SKIP:` zero for them; by-name
   `--nocapture` runs (Tier R-style hidden counts need `--nocapture`).
3. The four families by NAME through `recipe_sweep.py --run <family>`
   after a passing probe; `--show` each header; grep each NDJSON for
   `rawSse` / `llmLogCounts` before believing a green.
4. Mutation proofs (each reddening exactly its rows): the impersonation
   line removed; a `Closed` warn removed; a `.expect` restored on one
   site with an empty issue list constructed; the `utf16_len` restored
   (the astral arm reddens); one frame byte altered; one `log_llm_call`
   dropped.
5. Commits per `.claude/commands/commit.md` (core + harness + web
   versions; CHANGELOG every commit); the lane record appended to
   `status-log.md` (the measured `Closed`-arm client shape, the middleware
   500 sentence, v4's `type` strings per runner); this order's status
   header updated; the K1 and K2 order headers' Tier-2 item 9/10 notes
   updated for the families this lane closed (P4.86 updates the
   AI-import note).
