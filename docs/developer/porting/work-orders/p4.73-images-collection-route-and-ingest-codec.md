# Work order P4.73 — the never-ported `/api/v1/images` collection route (list / upload / import-from-URL / `?action=generate` / the `{id}` DELETE) + the host codec on every dispatch-side ingest path (P4.D152's named candidate) + P4.62(a) + the `ChatCreate` wrong-type trio

**Status: CLOSED WHOLE (P4.76, 2026-09-05).** The two items this order left
OPEN — Tier 1 item 3 (`?action=generate`) and Tier 2 item 8's FILES leg — both
landed in the P4.76 lane (`claude/p4-76-images-generate-files-405522`), along
with all five of the "Recorded, not fixed" items below. Nothing under this order
remains open; the record is `status-log.md` → "Lane record — P4.76".

**Status (unification, 2026-09-04):** UNIFIED on main at the follow-ups round 2
unification — **PARTIAL at the time** (Tier 1 item 3 `?action=generate` and
Tier 2 item 8's FILES leg were OPEN; both are closed by P4.76, above).
**The §3 review's headline catch, fixed on the unify branch:**
`create_file_conns`' dedup-with-growth arm bypassed the new UUID refusal and
answered 201 with a raw id merged where v4's `repos.files.update` re-validates
BEFORE any write and answers 400 — and the two dedup arms the order's Tier 1
item 5 required were never written. Fixed + five arms added to
`images_routes_equivalence` on both sides (`upload_dedup_existing_notag` /
`_grows` / `_raw_tagid` over F_INUSE's passthrough WebP bytes, the two-step
`upload_dedup_orphan_cleanup`, `import_tags_extra_keys`), mutation-proven.
Also fixed at unification: `add_raw_tags`' raw `UPDATE files SET tags` writer
retired (a false claim about v4's `addTag`); the import receipt echoes Zod's
rebuilt `{tagType, tagId}`; the edge's malformed JSON / multipart bodies answer
v4's unhandled-error 500 (pinned by the NEW `images_edge_routes` web test,
which also replaces the family header's reference to a driver that did not
exist); `roleplayTemplateId`'s wrong-type refusal hoisted beside
`conciergeState` (`rt_wrong_type_before_404`); the codec wiring pin's
files-row half had NEVER run (`data.id` behind an `if`; the receipt is
`{file:{id}}`) — now a hard floor; the fourth `z.uuid()` copy folded; the
`validateImageFile` unit tests the doc comment promised; both tripwires' prose;
the `images_collection_post` row landed in `query_param_semantics_equivalence`
(the lane's coordination-only item). **Recorded, not fixed at THIS unification — all five FIXED by P4.76:** the
DELETE's orphan cleanup writes `characters` rows raw where v4's
`validateCharacterArchivePatch` would throw on an ARCHIVED character (the
fixture seeds none); `zod_url_ok` refuses authority-less schemes v4's
`new URL()` accepts; v4's `[Images v1] Image uploaded` / `imported from URL`
info lines are unported; the JSON body read is unbounded; the oracle's
`cannedFetch` is order-dependent across cases.

_(The lane's own close header follows.)_

**Status:** **PARTIAL — five units landed, the generate leg DEFERRED**
(worked 2026-09-04 on `claude/p4-73-images-collection-ingest-7e6c3f`; full
record in `status-log.md` → "Lane record — P4.73").

LANDED: Tier 1 items 1 (`GET`), 2 (upload + import-from-URL, with the host
fetch seam), 4 (the host codec threaded into chat uploads + its composition
wiring pin) and 5 (the NEW `images_routes_equivalence`, 27 core-driven cases
over a NEW committed `images-{main,mount}.db` pair); Tier 2 items 7 (the
`{id}` DELETE, retiring `image_delete_not_available`), 9 (the `ChatCreate`
wrong-type trio) and 10 (the `core-contract.ts` mirror).

**OPEN — Tier 1 item 3, `?action=generate`.** Not started; no stub, and BOTH
its tripwires stay ARMED on purpose (retiring them without the route would put
an `api.v1.images.generate` anchor in `combined.log` with nothing behind it).
The edge answers a NAMED refusal rather than falling through to upload. The
lane record carries the full survey — the missing erased image-GENERATION
provider seam, the Concierge stack's reachability from a route handler, and the
oracle's frozen-clock requirement — so the next lane starts from measurement.

**OPEN — Tier 2 item 8, P4.62(a)'s FILES leg**, with its shape CORRECTED: the
order's premise (a raw carry) was REFUTED by measurement on both legs. See the
lane record.

⚠ **Three of this order's stated premises were wrong** and are corrected in the
lane record: (a) the ingest engine did NOT need building — `create_file_conns`
has existed since P4.1b; (b) v4 does NOT carry a raw non-string `tagId`,
it REFUSES (400 on the images leg, 500 on the files leg) and leaves an orphaned
blob; (c) `import_no_dot_filename` cannot discriminate v4's extension rule.
 Written from `phase-4.md`'s candidates 3
and 5 at the `0b0617fee` unification (the host codec into chat uploads; the
`POST /api/v1/images?action=generate` route v5 never ported — "v4's ninth
`[Image LoRA]` anchor AND the one image call site that rewrites a
`baseUrl`"), P4.67's OPEN P4.62(a), the `0b0617fee` round record's
wrong-type note, and the three v5 source sites that name the gap
(`image-gallery.ts:19`, `avatar-picker.ts:22`, `create-npc-dialog.ts:41`).

## Preamble

- **v4 oracle baseline: `0b0617fee`** (drift-ledger §1; probe re-run
  2026-09-03 at ordering: v4 checkout on `main`, tree CLEAN, `git log
  15573c3a1..main` empty, `git log 3a76b17df..bugfix` empty). The ledger's
  one §3 row (`15573c3a1`, bug 119, the unported character optimizer) is
  not this lane's.
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 checkout
  on `main`, tree clean, `git log 15573c3a1..main` empty and `git log
  3a76b17df..bugfix` empty. **Any mismatch: STOP and report.** Re-run
  before every regen batch.
- **Regen rule: PIN REQUIRED at `0b0617fee`** (ledger §1). Lane-unique
  pin `PIN=/tmp/qt-v4-pin-p473-0b0617fee` per §5.1 (all three symlink
  classes — the images route imports provider plugins through
  `createImageProvider`, so the `plugins/dist/*/node_modules` links are
  load-bearing); every regen via `recipe_sweep.py --run <family> --v5w
  <worktree> --v4 "$PIN"`. Committed recipes never name the pin.
- **Sibling lanes (this round):** P4.72 (the P4.67 remainder + the
  dispatch wrong-type census — owns `quilltap-web/src/**` except `lib.rs`
  / the NEW `images_routes.rs` / the §F/§G regions, and
  `api/chat_media.rs`), P4.74 (core smalls — owns
  `services/{answer_confirmation,file_fallback,cost_events}.rs` + named
  harness families), P4.75 (SPA smalls — owns the SPA + Playwright).
  §Shared contracts / §Ownership are binding and identical across all four
  orders.
- **Worktree:** per the `carryout` skill. `CHANGELOG` / `status-log.md`
  append-only. **No Playwright** (P4.75 owns port 4319); this lane's only
  SPA edit is the type-only `core-contract.ts` §1 mirror, gated by `ng
  build` + `ng test`.
- **Long gates:** `run_in_background: true` + full log + sentinel; NEVER a
  `pgrep` loop; NEVER `| tail`; never edit source during a workspace gate;
  fail-fast per binary — read the whole log. Budget disk (memory note
  `parallel-round-playbook` §B).

## Mandate

Port v4's images COLLECTION endpoint whole — `GET /api/v1/images` (the
tagged list projection), `POST /api/v1/images` (multipart upload / JSON
import-from-URL), `POST /api/v1/images?action=generate` (the synchronous
generate-and-persist path with its own Concierge classification + reroute),
and the `{id}` DELETE (orphan-aware, in-use-refusing) that today answers a
loud refusal — as dispatch verbs + thin REST edges, each with its
differential against v4's REAL route module, and retire the two tripwires
that were built to fire the day this lands (`lora_log_anchor_guard`'s ninth
anchor, `activity_span_sites_guard`'s NO_V5_SURFACE row 9). Thread the
production pixel codec (`Engine::qtap_pixel_codec()`) into the chat-upload
dispatch arm and the new images arms so v5 transcodes on ingest exactly as
v4's sharp does — a NEW convergence with its own byte-changing-codec
differential. Close P4.62(a) (`FileUpload.tags` raw carry) and the
`ChatCreate` wrong-type trio (`conciergeState` / `roleplayTemplateId` /
`timestampConfig` present-but-wrong-typed → v4's flat `Validation error`,
transport-agnostic). Must NOT touch: any `quilltap-web/src/*_routes.rs`
other than the NEW `images_routes.rs` and the two §F/§G regions, `query.rs`,
`api/chat_media.rs` (P4.72), `services/{answer_confirmation,file_fallback,
cost_events}.rs` (P4.74), the SPA beyond `core-contract.ts` (P4.75), or any
committed fixture other than a NEW `images-*` pair of its own.

## Survey-verified starting points (2026-09-03)

1. **v4's collection route:** `app/api/v1/images/route.ts` (508 lines).
   - `GET` (`:64-153`): `searchParams.get('tagId')` filter; `repos.files.
     findByCategory('IMAGE')` filtered to the user, sorted `createdAt`
     DESC; per image: `charactersUsingAsDefault` (characters whose
     `defaultImageId === img.id`), `chatAvatarOverrides` (the sum over
     every character's `avatarOverrides[].imageId === img.id`), `tags`
     re-typed by membership in the character-id set (`CHARACTER` else
     `THEME` — never `CHAT`, a v4 quirk to carry), `source` mapped
     `UPLOADED→'upload' / IMPORTED→'import' / GENERATED→'generated' / else
     'upload'`, `filepath = storageKey ? '/api/v1/files/<id>' :
     originalFilename`, `url = source==='IMPORTED' ? description : null`,
     and the field order `id,userId,filename,filepath,url,mimeType,size,
     width,height,source,generationPrompt,generationModel,createdAt,
     updatedAt,tags,_count` (KEY ORDER is the comparand — memory note
     `json-column-key-order`). Error: `serverError('Failed to fetch
     images')` after a `[Images v1] Error fetching images` log.
   - `POST` (`:159-171`): `getActionParam` → `'generate'` runs
     `trackActivity('image', () => handleGenerateImage(...))`; ANY other
     value (including unknown and `''`) falls to `handleUploadOrImport` —
     this route is the FIRST-shape (no `withActionDispatch`, no unknown
     envelope): an unknown action is silently the upload leg. Carry that;
     P4.72's family gets this endpoint's six rows from THIS lane
     (§B: the `?action=` read goes through `query::action`).
   - `handleGenerateImage` (`:177-408`): `generateImageSchema.parse(body)`
     (`prompt` 1..4000, `profileId` `z.uuid()`, optional `tags[{tagType
     enum, tagId string}]`, optional `options{n int 1..10, size, quality
     'standard'|'hd', style 'vivid'|'natural', aspectRatio}`) → a ZodError
     is the flat `Validation error` 400; profile lookup →
     `badRequest('Connection profile not found')`; the Concierge block
     (`:191-256`, whole thing inside a try whose catch logs `[Images v1]
     the Concierge classification failed, continuing normally` and NEVER
     blocks): `resolveDangerousContentSettings(chatSettings)`, gate
     `mode !== 'OFF' && scanImagePrompts`, `getCheapLLMProvider(default
     profile = isDefault || [0])`, `classifyDangerousContent(prompt, sel,
     userId, settings)`; on `isDangerous` the `[Images v1] Front page image
     prompt classified as dangerous` info, and under `AUTO_ROUTE` the
     reroute to the first `isDangerousCompatible === true && id !==
     profile.id` profile (`[Images v1] Rerouted to uncensored connection
     profile`) or the `[Images v1] No uncensored connection profile
     available, using original` warn; api key via `findApiKeyById` →
     `key_value` else `''`; `createImageProvider(profile.provider,
     profile.baseUrl ?? undefined)` — a throw is `badRequest('<provider>
     provider does not support image generation')`; `buildImageGenParams({
     profile, prompt, overrides:{n,size,quality,style,aspectRatio},
     logContext:{context:'api.v1.images.generate', profileId}})` — **the
     ninth `[Image LoRA]` anchor**; `provider.generateImage(params,
     decryptedKey)`; per image: `data || b64Json` else throw `Generated
     image has no data`; `providerMime || 'image/png'` → ext (`jpeg`→`jpg`);
     `convertToWebP` (sharp; the host codec here); `sha256` of the STORED
     bytes; `filename = generated_<Date.now()>_<index>_<sha8>.webp`;
     `getLanternBackgroundsStore()` else throw `Lantern Backgrounds mount is
     not provisioned; cannot persist generated image.`;
     `writeLanternBackgroundToMountStore({filename, content, contentType,
     subfolder:'tool'})`; `getInheritedTags(linkedTo, userId)`;
     `repos.files.create({... category 'IMAGE', source 'GENERATED',
     linkedTo, generationPrompt, generationModel: profile.modelName,
     generationRevisedPrompt, tags: inherited, storageKey}, {id})`; the
     per-image response `{id, filename, filepath, url: filepath, mimeType,
     size, revisedPrompt, tags: tags || []}`; the envelope `{data,
     metadata:{prompt, provider, model, count}}` **201**; the `[Images v1]
     Image generation complete` info. v5 already has every building block:
     `tools/generate_image.rs` (the Lantern write under `tool/`, the
     `get_inherited_tags` twin at `:1047`, the tool's own reroute),
     `services/dangerous_content/gatekeeper.rs:413 classify_content`,
     `image_gen/params_builder.rs`, `services/image_job_storage.rs:209
     write_lantern_background_to_mount_store`, the api-key resolver, the
     `ErasedImageGeneration` runner seam `image_profile_generate` uses
     (`api/image_profiles.rs:631`). **Port the ROUTE's own sequence, not a
     call into the tool** — v4's `handleGenerateImage` is a separate
     route-level implementation (`activity_span_sites_guard.rs:110` says
     exactly that), with its own classification gate (`scanImagePrompts`,
     no chat), its own reroute rule (first compatible profile, not the
     Concierge desk), and no orientation.
   - `handleUploadOrImport` (`:414-506`): dispatch on the request
     `content-type` — `application/json` → `importFromUrlSchema.parse`
     (`url: z.url()`, optional tags) → `importImageFromUrl(url, userId,
     linkedTo)` → `repos.files.addTag(id, tagId)` per tag → the
     `{id,userId,filename,filepath,url,mimeType,size,width,height,
     source:'import',createdAt,updatedAt,tags}` 201 (`createdAt`/`updatedAt`
     are `new Date().toISOString()` at the ROUTE, not the row's — normalize
     as timestamps); `multipart/form-data` → `file` (else `badRequest('No
     file provided')`), `tags` JSON string (unparseable → `badRequest('
     Invalid tags JSON')`; NO schema — the raw `tagId` carry, P4.62(a)'s
     twin) → `uploadImage(file, userId, linkedTo)` → addTag per tag → the
     same envelope with `url: null`, `source:'upload'`; anything else →
     `badRequest('Invalid content type')`.
   - `lib/images-v2.ts` (371 lines): `ALLOWED_IMAGE_TYPES` (jpeg/jpg/png/
     gif/webp/avif/svg+xml), `MAX_FILE_SIZE` 10 MiB, `validateImageFile`
     (the two `Invalid file type. Allowed types: …` / `File size exceeds
     maximum allowed size of 10 MB` throws — the route has NO try around
     them, so they surface as the context handler's 500), `getImageDimensions`
     (sharp metadata, never throws), `createFile` (`:96-217`: non-SVG
     `image/*` → `convertToWebP` (buffer/mime/filename replaced when
     converted); `sha256`; `findBySha256` → if the existing file EXISTS in
     storage, union `linkedTo` and return the existing row (an `update`
     when the union grew) — else `Cleaning up orphaned file metadata before
     re-upload` + delete and fall through; `randomUUID` id;
     `writeUserUploadToMountStore({filename, content, contentType,
     subfolder:'images'})` (v5 twin `services/file_storage.rs:1172`);
     `getInheritedTags` + `mergeTags`; `repos.files.create` with `{id}`),
     `importImageFromUrl` (`fetch(url)` — `!ok` → `Failed to fetch image
     from URL: <statusText>`; content-type gate against the allow-list;
     size gate; filename from the URL path's last segment with the mime's
     ext appended when it has no dot; `description: 'Imported from <url>'`;
     `source 'IMPORTED'`). **The fetch needs a host transport seam** —
     core has no HTTP; follow the P4.D138 HuggingFace precedent
     (`ErasedLoraMetadata` + the host transport, the canned wire carried
     per corpus row) so the oracle and the Rust side both drive a canned
     fetch.
   - `app/api/v1/images/[id]/route.ts` `DELETE` (`:134-233`): `findById`
     → `notFound('Image')`; category not `IMAGE`/`AVATAR` → `notFound(
     'Image')`; a storage-existence probe (a throwing backend counts as
     orphaned — read `:148-170` for the exact arms); orphaned → `[Images
     v1] Cleaning up references to orphaned image` + reference cleanup and
     allow; in use → `badRequest('Image is in use', {…})` (read the body
     shape at `:195`); success `{success: true}`. The `POST` actions
     (`add-tag` / `remove-tag`, `IMAGE_POST_ACTIONS`, `isValidAction` AFTER
     the `findById` → `notFound('Image')`; the envelope is the
     string-interpolated `Unknown action: <x>. Available actions: add-tag,
     remove-tag` — the THIRD dispatch shape) are Tier 3 here.
   - v4's tests: `__tests__/unit/images-generate.test.ts` (route-level,
     mocks `createImageProvider`, `writeLanternBackgroundToMountStore`,
     `getInheritedTags`, the repos), `__tests__/unit/lib/images-v2.test.ts`
     + `images-v2-dedup.test.ts`. Read them for the mock seams the oracle
     must un-mock (memory notes `jest-oracle-empty-provider-registry`,
     `jest-oracle-plugin-factory-is-mocked`, `oracle-mock-must-let-the-
     gate-be-reached`).
2. **v5 today:** `quilltap-web/src/lib.rs:564` serves `/api/v1/images/{id}`
   GET (`photos_routes::image_info_get`, P4.9a2) + DELETE →
   `photos_routes::image_delete_not_available` (`:161-167`, a 500 with a
   named sentence). No collection route, no `Images*` verb in
   `api/types.rs`. `lora_log_anchor_guard.rs:80-97` and
   `activity_span_sites_guard.rs:108-117` both hold tripwires that FAIL
   when `api.v1.images.generate` / the generate span appear — retire each
   by moving its row into the positive list (the guards say exactly how).
3. **The codec seam.** `Engine::qtap_pixel_codec()` (`api/engine.rs:762`)
   returns the backup host's codec when Ready (`quilltap-host/src/
   backup_services.rs:133` → `HostImageCodec`, the production `PixelCodec`
   over `image` + `webp`; D19: policy parity, not byte parity with sharp).
   The chat-upload dispatch arm (`engine.rs:4111-4121`) deliberately passes
   `Arc::new(NotConfiguredPixelCodec)` with a comment naming THIS lane;
   `services/chat_files.rs:705` and `api/files.rs:1116` carry the matching
   notes; `dogfood-findings.md` "Standing notes" (2026-09-03, A8) records
   the live measurement: a 265-byte PNG uploaded through
   `POST /api/v1/chats/{id}/files` came back `image/png` with the row's
   sha equal to the served bytes. ⚠ `qtap_pixel_codec()` takes the state
   lock — the arm already holds `db` from `ready_db()`; read the lock
   discipline before calling it inside the arm (fetch the codec BEFORE
   entering the arm's match, as the `.qtap` importer at `:1136` does).
   `file_upload` (`api/files.rs:1120`) is a DOCUMENT-category path and
   never transcodes — leave its codec note as is; the images arms are the
   ingest paths that need the codec (v4 `createFile` transcodes; the
   generate arm transcodes). Also `api/almanack.rs:289` and
   `services/quilltap_import/mod.rs:1020` pass `NotConfiguredPixelCodec` —
   measure whether v4's twin transcodes there and record (do not widen
   this lane's mandate on a guess).
4. **P4.62(a)** (P4.67's survey item 5(a)): `files_routes.rs:965-1002`
   (`files_upload_post`) — v4 has no schema: `tags.map(t => t.tagId)`
   carries the RAW value (`5` → a number in `linkedTo`/`tags`; `{}` →
   `undefined` → serialized `null`); v5's edge `filter_map`s to strings
   (`:988`) and drops non-strings. Shape: `Request::FileUpload.tags:
   Option<Vec<serde_json::Value>>` (`api/types.rs:2190`), the engine arm
   (`:4362`) and `api/files.rs:1126 file_upload(link_ids)` widened to carry
   raw values into `linkedTo` AND the `tags` column exactly as v4 does; the
   edge (§G region) passes raw values; arms in
   `files_body_guards_equivalence` over the DB-backed venue (they reach
   `saveFileEntry`). The images upload leg has the SAME raw carry — one
   helper, two callers.
5. **The `ChatCreate` wrong-type trio.** v5 has no `POST /api/v1/chats`;
   `Request::ChatCreate` is decoded by serde at
   `quilltap-web/src/dispatch.rs:72` (`Invalid request: <serde text>`,
   `ErrorKind::BadRequest`) and by the Tauri IPC transport identically. v4
   `app/api/v1/chats/route.ts:993 createChatSchema.parse(body)` —
   `conciergeState: z.enum(['monitored','flagged','vouched','uncensored']).
   optional()` (`:146`), `timestampConfig: TimestampConfigSchema.optional()`
   (`:127`), `roleplayTemplateId` (read its line) — throws a ZodError → the
   context handler's flat `{"error":"Validation error"}` 400. The
   `0b0617fee` round recorded `conciergeState: 42` as a divergence
   (`chat_create_capstone` / the create-time arms — find the recorded row
   and flip it). Ordered shape: the D57 idiom — carry the three as
   `Option<serde_json::Value>` (or `Option<Option<Value>>` where absent vs
   null differ in v4 — measure each against the Zod schema: `.optional()`
   refuses `null`), validate in the handler with the flat sentence, so BOTH
   transports answer v4's bytes. Keep the well-formed path byte-identical
   (the capstone corpus is the pin). Name the handler file in the lane
   record at start (`api/chats.rs` or `services/chat_create.rs` — measure;
   it is this lane's for the trio only).

## Tiered deliverables

**Tier 1 (must land):**

1. **`GET /api/v1/images`** as `Request::ImagesList { tag_id: Option<String> }`
   → `api/images.rs::images_list` with v4's projection byte-for-byte (key
   order, the CHARACTER/THEME re-typing, `_count`), the REST edge in the
   NEW `images_routes.rs` (`?tagId=` through `query::first`), the fixed
   500 sentence.
2. **`POST /api/v1/images`** upload + import-from-URL as
   `Request::ImageUpload { filename, content_type, data(b64), tags:
   Option<Vec<Value>> }` and `Request::ImageImportFromUrl { url, tags }`
   over an `images_ingest` module carrying `createFile`'s whole sequence
   (validate → transcode through the HOST codec → sha256 → dedup-or-cleanup
   → user-uploads store write → inherited tags → create with id); the
   import's fetch behind a NEW erased transport seam wired in
   `quilltap-host` (the HuggingFace precedent); the edge dispatching on the
   request content-type exactly as v4 does (`Invalid content type` for
   anything else; `No file provided`; `Invalid tags JSON`); the addTag loop.
3. **`POST /api/v1/images?action=generate`** as `Request::ImagesGenerate
   { prompt, profile_id, tags, options }` → `api/images.rs::images_generate`
   carrying the route's own sequence (item 1's third bullet): Zod-exact
   validation (flat `Validation error`; `z.uuid()` via the `zod_uuid` gate),
   the Concierge block with its NEVER-blocks catch and its four log lines
   (capture-pinned), the api-key resolve, the provider construction with
   v4's `<provider> provider does not support image generation` refusal,
   `build_image_gen_params` with the **`api.v1.images.generate` anchor**,
   the generate call over the injected `ErasedImageGeneration` runner /
   the real image dialect over a canned wire (the `image_generate_route_
   equivalence` mold), per-image transcode-through-the-host-codec → sha256
   → the Lantern `tool/` write → inherited tags → `files.create` with id,
   the 201 envelope with `metadata`, the `trackActivity('image')` span
   (retiring `activity_span_sites_guard`'s row 9). Retire
   `lora_log_anchor_guard`'s ninth-anchor tripwire by moving the anchor
   into the positive list.
4. **The host codec threaded into the chat-upload dispatch arm**
   (`engine.rs:4121` → `qtap_pixel_codec()` with the not-configured
   fallback when Locked/unassembled) and into every new images arm, with a
   **WIRING pin** at engine level: dispatch `ChatFileUpload` through a
   `CoreEngine` assembled with a byte-CHANGING `PixelCodec` (the harness's
   `PrefixingPixelCodec`, or a test `BackupHost` returning it) and assert
   the stored bytes are the codec's output and the row's `sha256`/`mimeType`
   name THEM — a differential cannot see this cutover (memory note
   `a-chokepoint-cutover-is-differential-invisible`), so the pin is a
   composition-level test that reddens when the arm reverts to
   `NotConfiguredPixelCodec`. Update the three source notes
   (`engine.rs:4111`, `chat_files.rs:705`, `api/files.rs:1116`) and the
   `dogfood-findings.md` standing note to say which side of the divergence
   v5 is now on.
5. **The NEW family `images_routes_equivalence`** (tier 2, real DB) over a
   NEW jest oracle `harness/oracle/cases/images-routes.test.ts` driving
   v4's REAL `app/api/v1/images/route.ts` + `[id]/route.ts` handlers with
   the DB stack un-mocked (the `cheap_llm_fallback` oracle's un-mock idiom),
   the image provider canned on BOTH sides (the `image_generate_route`
   mold: reverse-map each oracle image to the SDK wire shape), the fetch
   canned per row, and a byte-changing codec on the Rust side vs REAL
   sharp on v4's (D19: compare mime/extension/sha-of-stored-bytes POLICY
   and the within-tree `files.sha256 == sha256(stored blob)` BOOLEAN, never
   the WebP bytes). Comparands: status + body (key order; minted ids and
   route-level timestamps normalized), the post-mutation `files` /
   `doc_mount_files` / blob dumps, the list projection over a fixture that
   HAS characters with `defaultImageId` and `avatarOverrides` (else
   `_count` is vacuous). Cases at minimum: list (empty / tagged / untagged /
   the IMPORTED-url arm), upload (png → webp; svg passthrough; a
   disallowed type; over-size; the dedup-existing arm; the dedup-orphaned
   cleanup arm; raw non-string `tagId`), import (ok; `!ok` status; wrong
   content-type; over-size; no-dot filename), generate (ok 201 with the
   Lantern write; validation refusals incl. `count`/`n` 11 and a 4001-unit
   prompt; missing profile; unsupported provider; danger OFF vs AUTO_ROUTE
   reroute vs no-compatible-profile warn; the Concierge catch arm; no
   Lantern mount → the thrown sentence), delete (missing; wrong category;
   orphaned cleanup; in use; ok). Red-first where v5 has a pre-existing
   shape (the DELETE refusal is the obvious one).
6. **The `?action=` rows for this endpoint** into
   `query_param_semantics_equivalence` — this route is the FIRST shape (an
   unknown action IS the upload leg); coordinate by name in the lane
   record so P4.72's grown `ENDPOINTS` list and this lane's edge agree at
   unification (the two lengths are compared; the unifier adds the row if
   the lanes cannot).

**Tier 2 (should land):**

7. **`DELETE /api/v1/images/{id}`** for real (item 1's DELETE bullet) as
   `Request::ImageDelete { id }`, retiring `image_delete_not_available`
   (§F: the ONLY `photos_routes.rs` edit) and re-pointing `lib.rs:564`; its
   arms in the new family.
8. **P4.62(a)** (item 4) — the raw `tagId` carry on BOTH upload legs, arms
   in `files_body_guards_equivalence` + the new family.
9. **The `ChatCreate` wrong-type trio** (item 5) — red-first against the
   recorded divergence row, both transports proven (a `dispatch.rs`-level
   web test AND a direct `Request` construction through the engine).
10. **The §1 `core-contract.ts` mirror** of the five new verbs (type-only;
    no SPA consumer this round — the avatar picker's `ImageUploadDialog`
    leg, the gallery's host, and the NPC dialog's upload-first sequence are
    the NEXT SPA lane's, recorded by name in the lane record with the
    contract they consume).

**Tier 3 (explicit deferrals, loud):** the `{id}` POST `add-tag` /
`remove-tag` actions (record v4's interpolated envelope + the
`verifyTaggedEntity` arms; if not ported, the edge answers a NAMED refusal
carrying the two action names — never a silent 405); the `almanack.rs` /
`quilltap_import` codec sites (measured, recorded); the `activity`
registry's remaining no-surface rows (unchanged); `help/images.md` (if v4
has one) → the `p4.9i2` bank.

## The differential requirement (restated)

Every verb ships with its case in `images_routes_equivalence` driving
v4's REAL route handlers (never a reimplementation) over a FRESH copy of
the fixture per case on both sides; the codec cutover ships with the
composition-level wiring pin; the trio ships red-first in the capstone
family. Mutation proofs: revert the engine arm to `NotConfiguredPixelCodec`
(the wiring pin reddens), drop the `api.v1.images.generate` anchor (the
guard reddens), narrow the list projection's `tags` re-typing (its rows
redden). Regens through the sweep driver from the pin; grep the fresh
NDJSON for the changed bytes (`Image is in use`, `Invalid content type`,
`api.v1.images.generate`, `Lantern Backgrounds mount is not provisioned`).
Recipe header per `recipe-header-conventions` (indented commands, `--test
<stem>`, `V5W=${V5W:-$HOME/source/quilltap-v5}`, regen and run naming the
SAME NDJSON path, a lane-UNIQUE stage dir — never `/tmp/qt-oracle-stage`).

## Fixtures

This lane may commit a NEW `images-{main,mount}.db` pair + its builder
under `harness/oracle/fixtures/` if no existing pair carries characters
with `defaultImageId` / `avatarOverrides`, IMAGE-category files with
tags, a provisioned Lantern Backgrounds mount AND a user-uploads store —
measure the `image-generation` two-DB pair (`qt-imggen-*`, which has the
Lantern store + profiles) and the `files` fixture first; **trim a new
builder to this family's question** (memory note
`a-new-fixture-built-from-a-grown-shared-fixture`). It widens NO existing
committed fixture. `files_routes_equivalence`'s P4.D152 chat-upload arms
stay as they are (they drive `chat_file_upload` directly with the
prefixing codec); the engine wiring pin is NEW and separate.

## Shared contracts (binding, identical across all four orders)

- **§A — well-formed actions never move.** P4.72 may change the answer to
  `?action=` (empty), an UNKNOWN action, and DUPLICATED keys only; the
  status + body of every well-formed `?action=<served>` request and every
  single-valued query is byte-identical before and after (the existing web
  families are the pin). No SPA caller changes.
- **§B — `crates/quilltap-web/src/query.rs` is the ONE query reader**
  (`first` / `all`, v4's `searchParams.get` / `getAll`). P4.73's new
  `images_routes.rs` reads its `?action=` and `?tagId=` through it and adds
  nothing to it; P4.72 changes no signature in it.
- **§C — participant-status parsing has ONE home:**
  `quilltap_core::chat_predicates::participant_status_from_str`. P4.74
  retires the two stray copies; no lane adds a private status match.
- **§D — the pixel codec has ONE accessor:** `Engine::qtap_pixel_codec()`
  (`api/engine.rs:762`). P4.73 threads it into the chat-upload arm and the
  new images arms; no other lane constructs a `PixelCodec`.
- **§E — the `Request`/`Response` enums are P4.73's this round.** Only
  P4.73 adds variants (`ImagesList`, `ImageUpload`, `ImageImportFromUrl`,
  `ImagesGenerate`, `ImageDelete` — append-only, at the end of the enum,
  `serde(rename_all = "camelCase")`) and only P4.73 changes an existing
  variant's field types (`FileUpload.tags`, the `ChatCreate` trio). P4.72's
  census READS `api/types.rs`; it never edits it.
- **§F — `photos_routes.rs` same-file exception.** P4.73 edits that file
  ONLY to delete `image_delete_not_available` (`:161-167`) once the real
  DELETE lands in `images_routes.rs`; P4.72 edits it anywhere else (the
  `q`/`tag` duplicate rows). Both lanes name the region in their commit
  message so the unifier's hand-merge is trivial.
- **§G — `files_routes.rs` same-file exception.** P4.73 edits that file
  ONLY inside `files_upload_post` (`:943-1010`, the `tags` parse block that
  P4.62(a) widens); P4.72 edits it anywhere else (the thumbnail GET, the
  chat-files POST, the `force` duplicate row). Same commit-message rule.
- **§H — the streaming avatar is client-only.** P4.75 reads
  `respondingParticipantId` (already on `ChatStreamState`), the chat's
  participants and `avatarDisplayMode`; no server payload changes and no
  lane touches the stream frames.

## Ownership (binding, identical across all four orders)

| Area | Owner |
|---|---|
| `crates/quilltap-web/src/**` EXCEPT `lib.rs`, the NEW `images_routes.rs`, and the two §F/§G regions; `crates/quilltap-web/tests/**` (the grown `query_param_semantics_equivalence`, the NEW dispatch wrong-type census guard, arms in the existing body-guard families); `quilltap-core`: `api/chat_media.rs` (the `chat_file_link` guard ONLY); harness: `harness/oracle/cases/query-param-semantics.test.ts` | **P4.72** |
| `quilltap-core`: `api/types.rs` (append-only new variants + the `FileUpload.tags` and `ChatCreate` trio field widenings), `api/engine.rs` (the new dispatch arms, the `ChatFileUpload` codec line `:4121`, the `FileUpload` arm `:4362`), `api/files.rs` (`file_upload`'s `link_ids` widening), the NEW `api/images.rs`, the file that validates `Request::ChatCreate`'s body (named in the lane record at start — `api/chats.rs` or `services/chat_create.rs`, measured), `services/file_storage.rs` (a codec seam ONLY if the images arms need one); `quilltap-web`: `lib.rs` (route registration), the NEW `images_routes.rs`, the §F/§G regions; `quilltap-host`: `host.rs` (the import-fetch seam wiring), a NEW `image_import_fetch.rs`; harness: NEW `images_routes_equivalence` + `harness/oracle/cases/images-routes.test.ts`, a NEW committed `images-{main,mount}.db` pair + builder if needed, `files_routes_equivalence` (the codec WIRING arm), `chat_create_capstone` (the trio arms), `lora_log_anchor_guard` (the ninth anchor moves); SPA: `core-contract.ts` (the §1 type-only mirror of the new verbs) | **P4.73** |
| `quilltap-core`: `services/answer_confirmation.rs` (the status match ONLY), `services/file_fallback.rs` (log fields ONLY), `services/cost_events.rs` (the swallowed-write warn ONLY); harness: `message_attribution_equivalence.rs`, `primary_stream_tier3_equivalence.rs` + `harness/oracle/cases/primary-stream-tier3.test.ts` + its fixture builder (additive), the three shared-stage recipe headers (`precompute_equivalence.rs`, `mail_carina_tools_equivalence.rs`, `photo_tools_equivalence.rs` + their `.test.ts` twins), the NEW handler-logging inventory doc | **P4.74** |
| SPA: `chat/{streaming-message,message-list}.ts` (+specs), `documents/open-document-from-search.ts` + `search/search-results.ts` + whatever workspace file the root cause names (+specs), the `title=` census script under `apps/web/scripts/`, the residue hosts' files (each named in the lane record), `screens/files/folder-picker.ts` (`#move-folder` only if fixed); e2e: `workspace-search-documents-flow.spec.ts`, NEW streaming-avatar beat | **P4.75** |
| Playwright (port 4319) | **P4.75's branch only**; every other lane's beats are authored gated (ACTIVATE-AT-UNIFY) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it |

**Version bumps:** this lane bumps `quilltap-core`, `quilltap-web`,
`quilltap-harness`, `quilltap-host` and the SPA `package.json` per commit
that touches each (base: core 0.0.768, web 0.0.105, harness 0.0.662, host
0.0.95, SPA 0.5.631); the unifier recounts as base + total bumps (three
lanes bump core and harness, two bump web, two bump the SPA — memory note
`parallel-round-playbook` §C, and a Cargo.toml conflict is never
version-only: this lane may add a dependency for the fetch seam).

## Verification gate before declaring done

`cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
-D warnings` on BOTH feature sets; full `cargo test --workspace` with the
lane's oracle env vars (`run_in_background`, full log + sentinel, every
`test result:` line read, zero `SKIP:` for the lane's families — incl. the
retired tripwires now GREEN in their positive form); the new family + the
touched families (`files_routes_equivalence`, `files_body_guards_
equivalence`, `chat_create_capstone`, `image_generate_route_equivalence`
as a neutrality re-run) by name through the sweep driver from the pin,
zero SKIP, changed bytes grepped; mutation proofs applied-then-reverted by
file backup; `cargo build --workspace --release`; `npm run build` + `npm
test` in `apps/web` for the contract mirror (no Playwright). Per-commit
rule: `.claude/commands/commit.md`. Lane record → `status-log.md` ("Lane
record — P4.73"), the status header here updated at close, and the two
`dogfood-findings.md` standing notes this lane discharges (the A8
chat-upload note; the `image-gallery.ts` / `avatar-picker.ts` "no images
list verb" comments) rewritten to say what is now true. 💸 the live
proofs this lane banks for the next dogfood pass: a real chat-upload PNG
stored as WebP with the row's sha naming the WebP bytes; a real
`?action=generate` on the Friday copy (image spend — human-authorized);
an import-from-URL against a real public image.
