# Work order P4.D119 — the `b86bb1a5` per-tier dressing-instructions drift, server half: the cascade module, the projection preserve option, the reader skip, the outfit-prompt thread, the four `?action=instructions` surfaces

**Status:** OPEN (written 2026-08-25 by /setupphase). Stacked lane: the same
agent/worktree carries out THIS order first, then
`p4.d120-archive-entries-server.md` on the same branch — v4 landed
`b86bb1a5` as the explicit prerequisite of `d25dacc1` (same four routes,
the `preserveFileNames` option, the same dialog), and the archive order
builds on this one's patterns.

## Preamble

- **v4 baseline for this round: `8f910137`** (v4 main). The round absorbs
  the FIVE commits past it (drift-ledger §3): `b86bb1a5` (THIS order's
  server half; client half in sibling P4.D121), `d25dacc1` (P4.D120,
  stacked after this order in the same lane; client half in P4.D121), the
  two docs-only specs `a47d3e03`/`2417cbed1` (NO-PORT? — ratified at
  unification), and `b220999d` (sibling P4.D122, the Documents-search
  vertical). At planning the v4 checkout was on `main`, tree CLEAN, HEAD
  `b220999d`.
- **Run the drift ledger's §2 freshness probe at lane start**
  (`docs/developer/porting/drift-ledger.md`). If it fails — v4 HEAD moved
  past `b220999d`, the checkout switched branches, or the tree dirtied —
  **STOP and report**; a lane never writes the ledger.
- **Regen rule in force: pin required** (ledger §1). Build ONE lane-unique
  detached worktree pinned at **`d25dacc1`** (all THREE symlink classes —
  recipe in drift-ledger §5.1; `/tmp/qt-v4-pin-p4d119-d25dacc1`) and run
  BOTH stacked orders' oracle regens and fixture builds from it —
  `d25dacc1` contains everything in `b86bb1a5` plus the archive feature,
  so one pin serves the whole lane, and regenerating a family this order
  moves at `d25dacc1` bakes in the sibling archive deltas ONLY where the
  P4.D120 half will land in the same lane before the gate (regenerate each
  family ONCE, after both orders' edits to its surface are in). Spec reads
  for THIS order come from `git show b86bb1a5:<path>` and the hunks
  `git show b86bb1a5 -- <path>` — never the later tree, so the two
  features' deltas stay attributable. Regens of families UNRELATED to this
  lane stay at the `8f910137` baseline pin. Sweep runs through
  `harness/tools/recipe_sweep.py --v4 <pin>`; never two sweeps
  concurrently.
- **Sibling lanes:** P4.D121 (`p4.d121-instructions-archive-spa.md` — the
  Angular client half of BOTH stacked orders; meets this lane ONLY at the
  §Shared contract), P4.D122 (`p4.d122-documents-search-vertical.md` —
  fully disjoint; see §Ownership). §Ownership below is binding and
  identical in all four orders.
- **Worktree:** per the `carryout` skill; edit inside the worktree, not
  main (memory: `edit-inside-the-worktree-not-main.md`).
  `docs/CHANGELOG.md` and `status-log.md` are append-only for everyone.

## Mandate

Port the server half of v4 `b86bb1a5` ("per-tier dressing instructions for
characters who dress themselves"): the **new wardrobe-instructions module**
(the four-tier cascade resolver + the read/write helpers over
`Wardrobe/instructions.md`), the **`preserve_file_names` option** on the
vault projection sweep (with `project_vault_wardrobe` the only passing
caller), the **shared wardrobe reader's skip** of the instructions file
(one reader, all four tiers), the **outfit-selection prompt thread** (the
system-prompt bullet + the `instructionsNote` + the cascade resolution at
BOTH of v5's `llm_choose` entrances), the **Almanack items-count
exclusion**, and the **four `?action=instructions` GET/POST surfaces** as
dispatch verbs per §Shared contract. Every unit ships with its
differential against v4's REAL code.

**Must NOT touch:** anything under `apps/web/` (P4.D121 owns the whole
client half — the component, the hook, the field hint, both mount points);
the P4.D122-owned files (`api/ui_search.rs`, the doc-mount search repos,
`doc_edit/qtap_uri.rs`, `doc_edit/uri_producers.rs`,
`ui_search_routes.rs`). v4's `help/wardrobe.md` / `help/groups.md` /
`help/project-wardrobe.md` deltas go to the **`p4.9i2` bank** — record the
rows, port nothing. `docs/developer/API.md` got no v4 update (recorded gap
— nothing to mirror).

## Survey-verified starting points (fresh survey 2026-08-25, at `b86bb1a5`)

Read the commit whole: `git show b86bb1a5` in `~/source/quilltap-server`
(35 files, +1484/−70). ⚠ Port from the shipped hunks, never the commit
message (ledger §5.3).

### v4 — the new module (`lib/wardrobe/wardrobe-instructions.ts`, 129 lines)

- `WARDROBE_INSTRUCTIONS_FILENAME = 'instructions.md'`,
  `WARDROBE_INSTRUCTIONS_PATH = 'Wardrobe/instructions.md'`,
  `isWardrobeInstructionsFileName(name) = name.toLowerCase() ===
  WARDROBE_INSTRUCTIONS_FILENAME` — all in `character-vault.ts:34-44`
  (re-exported through `vault-overlay/schema.ts`, pure re-export).
- `resolveWardrobeInstructions({characterMountPointId,
  groupMountPointIds, projectMountPointIds})`:
  `getGeneralMountPointId()` awaited UNCONDITIONALLY FIRST (even when the
  character tier will win — keep the read); probe list character → group →
  project → general, where character and general use **JS truthiness**
  (`''` skipped like `null`), and the group/project tiers pass through
  `deterministicMounts` = `[...new Set(ids)].sort()` — dedupe then
  **UTF-16 code-unit sort** (v5's Decision-B `str::cmp` idiom,
  `vault_read_overlay.rs:452` carries the note). Sequential `for…of`, no
  parallelism. Per tier: `readVaultTextFile` → `raw?.trim() ?? ''` —
  **a file that trims empty CONTINUES to the next tier** (blank ≠ "unset
  here, stop"). Hit → debug log + return `{content: TRIMMED, tier,
  mountPointId}`; miss everywhere → `null`. NO try/catch — a failing read
  degrades to "not found" because `readVaultTextFile` already logs+nulls.
- `readWardrobeInstructionsFile(mountPointId)` — no cascade; blank and
  missing indistinguishable (`null`).
- `writeWardrobeInstructionsFile(mountPointId, instructions)`: trim; empty
  → delete the file swallowing ONLY `NOT_FOUND` (any other error
  rethrows), debug `'…cleared'`, NO folder ensure on the clear path;
  non-empty → `ensureFolderPath(…, 'Wardrobe')` then write the **trimmed**
  string (no trailing newline, no frontmatter — a bare Markdown body,
  unlike garment files), debug `'…written'`.
- Trim is JS `String.prototype.trim` — use v5's `jsstr::js_trim`.

### v4 — projection + reader (the shared-substrate changes)

- `projectArrayIntoVaultFolder` gains `opts?.preserveFileNames`:
  (1) `preserved` = lowercased set; (2) `seen` is SEEDED with `preserved`
  — so a garment titled `Instructions` disambiguates to
  `instructions-1.md`; (3) the sweep loop skips when the path's FILE
  SEGMENT (`slice(lastIndexOf('/')+1)`, lowercased — degrades to the
  whole path when no slash) is preserved. Default = byte-identical to
  before (v4 pins that omitting the option still sweeps
  `instructions.md`). **Only `projectVaultWardrobe` passes the option**;
  the four managed-fields call sites (Prompts/Scenarios ×2 files) do NOT.
- `readCharacterVaultWardrobe` filters
  `!isWardrobeInstructionsFileName(doc.fileName)` **BEFORE** the
  `itemDocs.length > 0` branch — a folder holding only `instructions.md`
  falls through to the legacy `wardrobe.json` branch (pinned by v4's
  test). One reader covers all four tiers (its callers:
  character-properties overlay, wardrobe-sync, wardrobe-writes,
  shared-wardrobe → general/project/group).

### v4 — the outfit-selection thread

- `OUTFIT_SELECTION_PROMPT` gains a 4th bullet (LAST in the opening list,
  after `- The character's personality`), **unconditionally** — every
  `llm_choose` call now carries it:
  `- The character's own dressing instructions, when provided — these
  describe what the character prefers to wear and under what
  circumstances; weigh them heavily, above general appropriateness
  guesses` (em dash U+2014, no terminal period).
- `chooseLLMOutfit` gains a 7th POSITIONAL param `dressingInstructions:
  string | null` inserted BETWEEN `scenarioText` and `selection`.
  `instructionsNote` = guard `truthy && trim().length > 0` (null/''/
  whitespace → `''`, byte-identical message to pre-commit — v4 pins this
  byte-identity); else
  `` `\nDressing Instructions (addressed to ${characterName} in the second person — "you" is ${characterName}):\n${dressingInstructions.trim()}` ``
  — leading `\n`, no trailing newline, STRAIGHT quotes around `"you"`, em
  dash before it, name interpolated twice. Position: LAST in the note
  chain, after `scenarioNote` (which always emits), immediately before
  the blank line and `Available Wardrobe Items:`. The empty-pool
  short-circuit returns before any message is built.
- `apply-outfit-selections.ts` `llm_choose` arm (inside
  `if (defaultProfile)`, after `wardrobeStart`, before `startedAt`):
  a SECOND, redundant `sharedWardrobeTiersForCharacter(characterId, [])`
  call (the pool build already ran it with the real project ids — carry
  the redundancy note), `.catch(() => ({groupMountPointIds: []}))`;
  `resolveWardrobeInstructions({characterMountPointId ?? null,
  groupMountPointIds ?? [], projectMountPointIds})` — the OUTER
  `projectMountPointIds` (`context?.projectMountPointIds ?? []`), NOT the
  tiers call's — `.catch(() => null)`; a debug log when in play.
  Instructions never block an outfit.

### v4 — routes (all four via the pre-existing action dispatcher)

Shared Zod schema, defined separately in all four files:
`z.object({ instructions: z.string().nullable() })` — nullable, **NOT
optional**: `{}` fails → the middleware's flat `Validation error` 400
(memory: `v4-refusal-envelope-parse-vs-safeparse.md`). Success body on
every arm: raw `{"instructions": string|null}`. `cleared = !instructions
|| instructions.trim().length === 0` (truthiness — `''` clears like
`null`); the POST echo is the TRIMMED string.

| Scope | GET | POST | Error arms (exact sentences) |
|---|---|---|---|
| character | 404 `Character not found`; vault-less → `{instructions:null}` with NO read; **NOT tombstoned** — an archived character's GET answers 200 (v4 asymmetry, deliberate-looking, untested; reproduce it) | via `resolveWardrobeMount`: archived → **409** `Character is archived; dressing instructions cannot be edited`; no vault + cleared → 200 no-op; no vault + content → **500** `Character has no vault to hold dressing instructions` | logs `[Wardrobe v1] …` |
| group | 404 `Group not found`; `ensureGroupOfficialStore` fail → **500** `Failed to ensure group document store` | same arms + explicit `ensureGroupWardrobeFolder` before the write (redundant with the helper's ensure — carry it) | logs `[Groups v1] …` with `context: 'wardrobe'` |
| project | byte-copy of group with Project spellings; **500** `Failed to ensure project document store` | idem | logs `[Projects v1] …` |
| General | no 404 arm; unprovisioned General on GET → `{instructions:null}` | unprovisioned + cleared → 200 no-op (the "harmless no-op" comment); unprovisioned + content → **500** `Quilltap General store is not provisioned yet` | logs `[Wardrobe Archetypes v1] …` |

Unknown action → 400 `{"error":"Unknown action: <x>",
"availableActions":["instructions"]}` (the dispatcher's, pre-existing).

### v4 — Almanack

`countLinksInFolder` gains `excludeRelativePath?` appending
`' AND lower("relativePath") != ?'` + the lowercased param; call site
passes `WARDROBE_INSTRUCTIONS_PATH` for the `items` figure ONLY — **the
sibling `archived` LIKE-count got NO exclusion** (an instructions file
containing the literal `archived: true` still counts as an archived
garment; v4-faithful, carry the asymmetry with a comment).

### v4 — gaps the commit did NOT close (reproduce, don't fix; record)

The startup migration `move-shared-wardrobe-to-general.ts` dedupe set is
NOT seeded with `instructions.md` (a legacy archetype titled
"Instructions" clobbers the General file at first start) — check whether
v5 ported that migration and reproduce/record; a v4-side filing candidate.
No integration test proves the wiring end-to-end in v4 (the three
apply-outfit-selections suites just mock the module to null) — v5's
differential must do better (see §Differential). Project and General
route arms ship with ZERO v4 tests — the differential corpus is the floor
regardless.

### v5 — where the counterparts live (verified 2026-08-25)

- New module home: `crates/quilltap-core/src/wardrobe_instructions.rs`
  (sibling of `wardrobe_tiers.rs`). Substrate: `read_vault_text_file`
  (`services/memory_processor.rs:73` — the documented v4 port; may need a
  `&Connection`-flavoured sibling), `get_general_mount_point_id`
  (`db/instance_settings.rs:80`), `write_database_document` /
  `delete_database_document` (`db/doc_mount_file_links.rs:661`/`:1195` —
  delete is already NOT_FOUND-tolerant returning `false`; check the
  swallow-only-NOT_FOUND mapping), `ensure_folder_path` (`:1358`),
  `jsstr::js_trim`.
- v5 has FIVE separate `WARDROBE_FOLDER` consts and no shared module
  (`almanack/phase4_scriptorium.rs:33`, `db/archetype_wardrobe.rs:41`,
  `db/vault_read_overlay.rs:58`, `db/vault_wardrobe_write.rs:30`,
  `services/character_archive/service.rs:277`) — the new instructions
  constants live ONCE in the new module and are `use`d; do not
  consolidate the five in this lane.
- Projection: `db/vault_wardrobe_write.rs:37`
  `project_array_into_vault_folder` — seed `seen` at :56, skip in the
  sweep at :76-82. v5 has no `opts` bag: add
  `preserve_file_names: &[&str]` and update ALL FIVE call sites (the four
  empty passers: `vault_character_write.rs:323`/`:338`,
  `vault_character_update.rs:269`/`:286`; the passer:
  `vault_wardrobe_write.rs:102` in `project_vault_wardrobe`).
- Reader: `db/vault_read_overlay.rs:439` `read_character_vault_wardrobe`
  — the filter goes BETWEEN :447 (the folder read) and :449 (the
  `is_empty` branch). v4 filters on `doc.fileName`; check whether v5's
  row carries `file_name` or the last path segment must be derived.
- Outfit selection: `services/outfit_selections.rs` —
  `OUTFIT_SELECTION_PROMPT` :66 ("byte-exact" marked), `build_outfit_messages`
  :289 (`scenario_note` :307, the `format!` :353-355), `choose_llm_outfit`
  :755 (+ its call :783). ⚠ **v5 split v4's one entrance into TWO:**
  `resolve_llm_choose` :933 (chat create, connection-bound) AND
  `run_llm_choose_via_db` :875 (add-participant + merge, via
  `OutfitLlmChooseRunner`/`OutfitLlmChooseRequest` :851-869). The cascade
  resolution must land at BOTH — or be pushed down into
  `choose_llm_outfit`; decide, record, and cover both entrances in the
  differential. `shared_wardrobe_tiers_for_character`
  (`wardrobe_tiers.rs:116`) is sync + infallible — v4's `.catch()` arm has
  no analogue; drop it with a recorded note.
- Routes: v5's SPA rides dispatch verbs, not `?action=` URLs. Verb homes:
  `api/characters.rs` (`character_wardrobe_list` :813 the sibling),
  `api/groups.rs` (`group_wardrobe_list` :1140,
  `ensure_group_wardrobe_mount` :1101, `resolve_group_wardrobe_mount`
  :1122), `api/projects.rs` (`ensure_project_wardrobe_mount` :1227),
  `api/wardrobe.rs` (General; `not_found`/`internal`/`bad_request` helpers
  :60-68, the PASS/FAIL-must-match validation precedent :15-20). The
  archived refusal: `resolve_wardrobe_mount`
  (`db/vault_wardrobe_public.rs:192`) raises `DbError::CharacterArchived`
  — **the 409 sentence is route-local in v4** (`Character is archived;
  dressing instructions cannot be edited`), NOT `DbError`'s text; add it
  at the handler. REST edges today: `wardrobe_routes.rs` has GET+POST for
  General; `characters_routes.rs:355` GET only; groups/projects have
  none — see Tier 3.
- Almanack: `almanack/phase4_scriptorium.rs:119` `count_links_in_folder`
  (+ the `params_with(&ids, &extra)` param-building idiom — append the
  exclusion to `extra`); call site :560 (`items:`), the untouched
  `archived:` sibling :561.
- Harness families on this ground (regen owners — see §Fixtures):
  `vault_wardrobe_read_equivalence` (+ `vault-wardrobe-read.ts`),
  `vault_legacy_wardrobe_equivalence`, `vault_wardrobe_write_equivalence`
  (+ `vault-wardrobe-write.ts` — needs new fixtures: an `instructions.md`
  present + an item titled `Instructions`),
  `vault_character_write_equivalence` (the non-passing call sites'
  neutrality), `vault_wardrobe_public_equivalence`,
  `wardrobe_public_read_equivalence`, `wardrobe_tier2_equivalence`,
  `outfit_llm_choose_tier3_equivalence`
  (+ `chat-dialogs-llm-choose-tier3.test.ts` — the prompt bytes),
  `chats_outfits_tier2_equivalence`, `outfit_hash_equivalence`,
  `almanack_render_equivalence` / `almanack_tier2_equivalence` /
  `scriptorium_tools_equivalence`, `group_wardrobe_routes_equivalence`
  (+ `group-wardrobe.test.ts`), and the web-crate
  `wardrobe_routes_equivalence` / `characters_wardrobe_route` over the
  committed `wardrobe-routes-*.db` fixtures.

## Tiered deliverables

**Tier 1 (must land):**

1. `crates/quilltap-core/src/wardrobe_instructions.rs`: the three
   constants + the filename predicate, `resolve_wardrobe_instructions`
   (the cascade with every quirk above: unconditional General read first,
   truthiness skips, dedupe+code-unit sort, sequential probes,
   blank-continues, trimmed content in the result),
   `read_wardrobe_instructions_file`, `write_wardrobe_instructions_file`
   (trim-then-branch, NOT_FOUND-only swallow, ensure-on-write-only,
   trimmed bytes on disk). v4's why-comments carried.
2. The projection option: `preserve_file_names` on
   `project_array_into_vault_folder` (preserved-set lowering, `seen`
   seeding, file-segment sweep skip), all five call sites updated, only
   `project_vault_wardrobe` passing
   `&[WARDROBE_INSTRUCTIONS_FILENAME]`.
3. The reader skip in `read_character_vault_wardrobe`, placed BEFORE the
   emptiness branch (the legacy-json fallthrough preserved).
4. The outfit-selection thread: the 4th system-prompt bullet
   (unconditional), the `instructions_note` byte-exact with the
   null/blank guard, the new `choose_llm_outfit` parameter, and the
   cascade resolution wired at BOTH v5 entrances (`resolve_llm_choose` +
   `run_llm_choose_via_db`) with the outer-project-ids quirk and the
   soft-fail semantics.
5. The four instructions verb pairs per §Shared contract (dispatch:
   `api/types.rs` variants + `api/engine.rs` arms + the four handlers
   with v4's exact guard orders, sentences, status codes, and the
   trimmed echo), including the character GET's un-tombstoned read and
   the General POST's harmless-no-op arm.
6. The Almanack `count_links_in_folder` exclusion param + the `items`
   call site (and ONLY that one — the `archived` sibling untouched, with
   the carried comment).
7. The differentials for all of the above (see §Differential).

**Tier 2 (should land):**

8. REST edges for the four `?action=instructions` pairs ONLY where a v5
   consumer speaks the URL today (survey `quilltap-web`'s registered
   routes + the CLI): General already has GET+POST route files — extend
   them with the action dispatch (the `chat_action_get/post` precedent in
   `wardrobe_routes.rs:192/:268`, unknown-action pointer included);
   characters has GET only. Where no consumer exists, defer loudly with
   the survey evidence (the P4.D112/P4.D115 dispatch-only precedent) —
   record the decision per scope.
9. The startup-migration clobber check: does v5 port
   `move-shared-wardrobe-to-general`? If yes, verify/record whether its
   dedupe set has the same hole; either way record the v4-side filing
   candidate in the lane record.

**Tier 3 (explicit loud deferrals, never silent stubs):**

10. The `p4.9i2` bank rows for the three help files.
11. Anything the archive order (P4.D120) supersedes on the same surfaces
    — do not pre-implement archive semantics here; this order lands
    `b86bb1a5` exactly, and P4.D120 moves the shared surfaces afterward
    in the same lane.

## The differential requirement, restated

Every ported unit ships with an equivalence test driving v4's REAL code —
jest/tsx oracle → NDJSON → Rust diff, per tier. For this lane (regens at
the lane pin `d25dacc1`, each family regenerated ONCE after both stacked
orders' edits to its surface):

- **The cascade module** is PORT-NEW with no existing family: add a
  tier-2 case family driving v4's REAL `resolveWardrobeInstructions` /
  read/write helpers over a staged fixture (the
  `dbfree-route-guard-oracle.md` / vault-wardrobe recipes are the
  pattern; probe order pinned by v4's own
  `wardrobe-instructions.test.ts` — `['m-a','m-b','m-y','m-z',
  'm-general']` for deduped/sorted multi-mount tiers). Corpus floor:
  v4's 179-line unit suite + each tier winning; blank-file
  fall-through; empty-string character mount skipped; unprovisioned
  General absent from probes; write→trimmed bytes; clear-on-blank with
  the NOT_FOUND swallow; clear when file absent.
- **The projection option:** extend `vault_wardrobe_write_equivalence`
  with the three v4-pinned shapes (sweep exemption any casing; the
  `Instructions`-titled item routed to `-1`; omission still sweeps).
  The four non-passing call sites are proven NEUTRAL by regenerating
  `vault_character_write_equivalence` byte-identical.
- **The reader skip:** extend `vault_wardrobe_read_equivalence` (+ the
  legacy-json fallthrough family) with an `instructions.md`-bearing
  fixture, any casing, and the only-instructions folder falling through.
- **The prompt thread:** `outfit_llm_choose_tier3_equivalence` regenerated
  RED-FIRST (the new bullet moves every case), plus new cases: with
  instructions (each tier), null vs whitespace byte-identity, and the
  wiring at BOTH entrances (v4 never integration-tested this — v5's
  tier-3 op must make the instructions text reach the recorded prompt
  through the production path, both the create spine and the
  add-participant path; mock-branches-on-the-system-message is the
  recipe if discrimination is needed). Mutation-proof: remove the
  cascade call at ONE entrance — exactly one case reddens (memory:
  `fixture-side-mutation-proves-nothing.md`).
- **The verbs:** a routes-family per the P4.D112 recipe
  (`group_wardrobe_routes_equivalence` is the closest model) driving
  v4's REAL route handlers at the pin over a real fixture pair. Corpus
  floor per scope: GET present/absent/blank-file; POST set/echo-trim/
  clear-by-null/clear-by-empty/clear-by-whitespace; `{}` → flat
  `Validation error`; wrong-type collapse per P4.60's conventions;
  404s; the character 409 (archived) + the archived-GET-still-200
  asymmetry; the two 500 sentences; General's unprovisioned arms.
  Writes verified through DB state (the instructions file's bytes),
  not just bodies.
- **The Almanack exclusion:** extend the almanack family with an
  instructions-file-bearing fixture — `items` excludes it, `archived`
  still LIKE-counts a planted `archived: true` instructions file (the
  asymmetry pinned).
- A green regen is not coverage: grep every regenerated NDJSON for the
  changed bytes (`Dressing Instructions`, `instructions.md`, the 409
  sentence) — drift-ledger §5.2.

## Fixtures

- Consumes/extends the vault-wardrobe staged fixtures (their builders are
  recipe-run — memory: `sweep-recipes-write-into-the-repo.md` for which
  recipes mutate committed fixtures) and the committed
  `wardrobe-routes-*.db` pair in `crates/quilltap-web/tests/fixtures/`
  (a rebuild requires regenerating dependent oracles — this lane owns
  that).
- New fixture content this order introduces (instructions files at each
  tier, the `Instructions`-titled garment) must be added through the
  families' own builders, at the lane pin, remembering store-overlay
  properties cannot be SQL-seeded (memory:
  `store-overlay-properties-cannot-be-sql-seeded.md`).
- This lane owns regenerating every oracle its changes touch, through the
  sweep driver at the lane pin, changed-bytes grep on every regenerated
  NDJSON.

## Shared contract (BINDING — identical text in `p4.d121-instructions-archive-spa.md`; §A of that order's two-part contract)

1. **Eight dispatch verbs** (serde camelCase over the Request enum),
   named after the existing per-scope wardrobe verb families:
   - `characterWardrobeInstructionsGet { characterId }` /
     `characterWardrobeInstructionsSet { characterId, instructions }`
   - `groupWardrobeInstructionsGet { groupId }` /
     `groupWardrobeInstructionsSet { groupId, instructions }`
   - `projectWardrobeInstructionsGet { projectId }` /
     `projectWardrobeInstructionsSet { projectId, instructions }`
   - `wardrobeInstructionsGet {}` / `wardrobeInstructionsSet
     { instructions }` (Quilltap General)
   `instructions` is `string | null` and REQUIRED (v4's
   `z.string().nullable()`, not optional — an absent key fails
   validation; decode through the Request enum, never a hand-built
   variant; memories: `edge-must-decode-through-the-request-enum.md`,
   `dispatch-verb-ignores-unknown-fields.md`).
2. **Success body** on every arm: `{ instructions: string | null }` —
   the SET echo is the TRIMMED string, `null` when cleared.
3. **Error arms** (v4's envelopes): invalid body → flat
   `Validation error` 400; 404 `Character not found` / `Group not found`
   / `Project not found`; character SET on an archived character → 409
   `Character is archived; dressing instructions cannot be edited`
   (GET is NOT tombstoned — 200); vault-less character SET with content
   → 500 `Character has no vault to hold dressing instructions`
   (cleared → 200 no-op); group/project store-ensure failure → 500
   `Failed to ensure group document store` / `Failed to ensure project
   document store`; unprovisioned General SET with content → 500
   `Quilltap General store is not provisioned yet` (cleared → 200
   no-op).
4. **The client field hint** (P4.D121 lands it in
   `prompt-field-hints.ts`, byte-exact, between `groupInstructions` and
   `roleplayTemplatePrompt`): label `Dressing Instructions`; helper and
   example verbatim from v4's `field-hints.ts:103-109` including the
   curly quotes (U+201C/U+201D), curly apostrophes (U+2019), and the em
   dash — the lane transcribes from `git show b86bb1a5:components/
   prompt-fields/field-hints.ts`, never from this order.
5. **UI strings** (P4.D121): `Dressing Instructions`, `Consulting…` /
   `On file` / `None on file` (U+2026), `Save Instructions` / `Saving…`,
   toasts `Dressing instructions saved` / `Dressing instructions
   cleared` / `Failed to save dressing instructions`.

## Ownership (BINDING — identical table in all four orders)

| Lane | Owns (exclusive) | Version bumps |
|---|---|---|
| P4.D119+P4.D120 (one stacked lane) | `crates/quilltap-core/**` EXCEPT the P4.D122 files below (incl. `api/types.rs`, `api/engine.rs`, all wardrobe/scenario/vault-overlay/export modules), `crates/quilltap-web/**` EXCEPT `ui_search_routes.rs`, the wardrobe/scenario/export/Almanack families in `harness/oracle/**` + `crates/quilltap-harness/**` | core, harness, web |
| P4.D121 | `apps/web/src/app/**` EXCEPT `search/**` and the P4.D122 files below (incl. `core-contract.ts`, wardrobe/scenario/new-chat components), its e2e specs | SPA |
| P4.D122 | `crates/quilltap-core/src/api/ui_search.rs`, `db/doc_mount_file_links.rs`, `db/doc_mount_chunks.rs`, `db/doc_mount_points.rs`, the NEW like-escape + document-text-search modules, `doc_edit/qtap_uri.rs`, `doc_edit/uri_producers.rs`, `crates/quilltap-web/src/ui_search_routes.rs`, `apps/web/src/app/search/**`, NEW open-from-search client files under `apps/web/src/app/documents/`, the ui-search + qtap-uri + doc-mount families in `harness/oracle/**` + `crates/quilltap-harness/**`, its e2e spec | core, harness, web (only if touched), SPA |

⚠ One boundary note: P4.D122 owns `db/doc_mount_file_links.rs` (its search
query lands there), and THIS lane's module CALLS that file's existing
`write_database_document` / `delete_database_document` /
`ensure_folder_path` — calling is fine; **editing that file from this lane
is not**. If an edit there proves necessary, record a
`PENDING_CROSS_LANE` block for the unifier.

`docs/CHANGELOG.md` + `status-log.md` append-only for everyone; nobody
edits `drift-ledger.md` from a lane. core/harness/SPA are bumped by more
than one lane: the unifier recounts (the standing silent-auto-merge trap —
playbook).

## Verification gate (before declaring done — run ONCE for the stacked lane, after P4.D120)

- `cargo fmt --check`; `cargo clippy` both feature sets; full
  `cargo test --workspace` (background, full log capture, sentinel —
  NEVER `| tail`, never a pgrep loop).
- Both stacked orders' differentials by name, regenerated fresh at the
  lane pin through the sweep driver, zero SKIP, changed-bytes grep on
  every regenerated NDJSON.
- The neutrality regens (`vault_character_write_equivalence`, and any
  family the projection-signature change touches without behavior
  change) explicitly re-run and recorded.
- Per-commit rule: `.claude/commands/commit.md`. Lane record appended to
  `status-log.md` (one record per order, both in this lane's section).
