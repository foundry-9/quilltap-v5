# Work order P4.53 — the sweep driver's three recorded follow-ups — v4 `b8449b3e`

**Status:** CLOSED (2026-08-20, branch
`claude/p4-53-sweep-driver-followups-3ad7b1`; unified 2026-08-21). All
three units landed with red-first proofs: (1) the five headers repaired to
the sanctioned `V5W=${V5W:-…}` convention — the LIVE
`brahma_console_routes` clobber reproduced pre-fix by marker probe
(staged from MAIN, exit 0) and proven fixed ALONE before the backstop
existed; **the root was two defects**: the header's cross-alias
assignment AND the injection regex never matching `${VAR:-…}` (both
fixed); (2) the `nothing_to_run` refusal (exit 2, REFUSED not FAILED,
raised before any stage; `--run-all` rows + a whole-checkout artifact key)
— **the measured vacuous-green debt is 39 families** (5 exempt, 12
committed-corpus, 22 no_oracle; judged not hard rot — ~34 could gain a
scoped `cargo test` run line; the next maintenance pass's inventory,
committed as
`harness/tools/sweep-results/2026-08-20-b8449b3e-p4.53-nothing-to-run.json`);
(3) `normalize()` neutralizes every checkout-alias assignment
(`CHECKOUT_ALIASES`, `WT` joining; announced only when the rewrite
CHANGES something — tree-wide measurement: 173/412 scripts changed, zero
non-alias deltas, zero announcements after the settled-shape refinement)
+ the `CROSS_ALIAS_DEFAULT` `--self-test` header pin (mutation-proven).
Also touched, flagged: `harness/tools/README.md` (the driver's own doc).
Tier-3 deferrals recorded loud in the lane record (the 39-family
inventory; `--show` naming the class; the committed-corpus `--run-all`
widening question). Versions: harness 0.0.512→514 (lane-local;
accumulated at unification). Lane record: `status-log.md` → "P4.53".

## Preamble

- **v4 baseline for this round: `b8449b3e`** (v4 main, HEAD at planning
  2026-08-20, tree CLEAN and the checkout on `main`). This lane ports no
  v4 commit — it discharges the three follow-ups the P4.51 lane recorded
  at the `c8a3cf77` unification (status-log → the P4.51 record's closing
  list). The sibling P4.D96 lane is the round's drift catch-up.
- **Drift-check at lane start.** `git log b8449b3e..main --oneline` AND
  `git diff main bugfix -- lib/ app/ packages/` in the v4 checkout. If
  v4 moved, **STOP** and report.
- **Oracle regens in this lane are proof-runs of the driver itself** —
  still pin a detached worktree at `b8449b3e` for any that execute
  (`recipe_sweep.py --v4 <pin-path>`; ALL THREE symlink classes).
- **Sibling lanes (disjoint ownership):** P4.D96 (the anti-chorus drift)
  and P4.52 (the memories fixture widening). Both RUN the sweep driver
  from their own worktrees — they run the UNCHANGED copy; this lane's
  changes take effect for everyone at unification.
- **Worktree:** per the `carryout` skill; edit inside the worktree, not
  main.

## Mandate

Close the three P4.51-recorded driver defects: (1) the LIVE
`brahma_console_routes` restored-recipe `W=` self-clobber (plus the four
cosmetic twins), (2) the `nothing_to_run` vacuous-green class, and
(3) make `normalize()` neutralize any header alias assignment so the
whole `W=`/`V5=`/`V5W=` class is unforgeable rather than
convention-dependent. Extend the driver's `--self-test` to pin each fix.
This lane owns the driver and the five case-file HEADER lines only — no
case bodies, no fixtures, no v5 source, no harness `.rs` files.

## Survey-verified starting points (2026-08-20, fresh)

The driver: `harness/tools/recipe_sweep.py` (1,701 lines; python3.13;
`normalize()` at `:799`; the alias-recognition regexes at `:210`, `:456`,
`:464`, `:533`, `:789`, `:815`; `no_oracle` note at `:404`). The P4.51
record (status-log, "the c8a3cf77 round") states the three items
verbatim:

1. **The live clobber:**
   `harness/oracle/cases/brahma-console-routes.test.ts:25` carries
   `W=${V5W:-$HOME/source/quilltap-v5}` in its EXECUTED recipe header —
   so a sweep run from a worktree stages that family's case file AND
   both `QT_FIXTURE_BRAHMA_*` DBs from MAIN, not the worktree. This is
   the only header where the alias is live (`ok_restored` extraction).
   The four cosmetic twins (their `.rs` families extract from the
   correct `.rs` header, so the `.ts` alias is dormant — fix them
   anyway, they are the same landmine):
   - `harness/oracle/cases/brahma-orchestrator-tier3.test.ts:38`
   - `harness/oracle/cases/brahma-console-tier3.test.ts:33`
   - `harness/oracle/cases/carina-query-tier3.test.ts:42`
   - `harness/oracle/cases/carina-memory-extraction-tier3.test.ts:36`
   The header-convention rule (the `recipe-header-conventions` memory):
   headers default `V5W=${V5W:-$HOME/source/quilltap-v5}` so they are
   copy-paste-safe AND overridable by the driver's injected alias — the
   fix is to make these five headers use that same convention (no
   second alias variable that shadows the injected one).
2. **`nothing_to_run`:** `--run <family>` on a family whose recipe
   extracts to EMPTY stages (every `no_oracle` family, every `exempt`
   pin) prints `OK: … recipe ran end-to-end` and exits 0 having run
   nothing. Wants a distinct `nothing_to_run` refusal — nonzero exit
   (follow the precedent of the unknown-family refusal P4.51 built:
   exit 2 + a naming message), never a green sentence. `--run-all` and
   the report statuses must distinguish it too (a `nothing_to_run` row
   is not `ok`).
3. **Unforgeable headers:** teach `normalize()` (`:799`) to rewrite any
   `^(V5W|V5|W)=` assignment line into the driver's injected alias, so
   a future header regression cannot re-clobber even if item 1's class
   reappears. Keep the rewrite visible in the driver's verbose output
   (a silent rewrite hides the very rot it neutralizes — print what was
   rewritten, once per family).

Interlock between items 1 and 3: after item 3, item 1's live bug is
neutralized mechanically — fix the headers ANYWAY (item 1) so the
committed recipes read true, and keep item 3 as the backstop. The
mutation proof must show the backstop alone catches a planted `W=`
line.

**P4.51 precedents to follow (same lane, one round back):** the
marker-probe method for proving worktree-vs-main staging (plant a
marker in the worktree copy, run, assert the staged copy carries it);
the `--self-test` harness (six arms today — extend, don't fork); the
refuse-before-any-stage placement the §3 review pinned (validation
runs before ANY stage executes).

## Tiered deliverables

**Tier 1 (must land):**

1. The five headers fixed to the sanctioned convention; the LIVE one
   proven by marker probe in BOTH directions (pre-fix: the clobber
   reproduced from a scratch worktree, staging from main; post-fix:
   staging from the worktree).
2. The `nothing_to_run` refusal: distinct status, nonzero exit on
   `--run`, distinct row in `--run-all`/report output; proven against a
   real `no_oracle` family and a real `exempt` pin.
3. `normalize()` alias-line neutralization + the visible rewrite notice;
   mutation-proven (plant a fresh `W=` clobber line in a scratch copy of
   a case header → the driver stages from the worktree anyway and prints
   the rewrite).
4. `--self-test` arms covering all three fixes (extend the existing
   suite; all arms green).

**Tier 2 (should land):**

5. A `--run-all` proof artifact refreshed (the durable artifact P4.45
   established) showing the new statuses over the full family list, run
   at the `b8449b3e` pin — expect the formerly vacuous greens to surface
   as `nothing_to_run` rows; enumerate them in the lane record (that
   list is the measured size of the vacuous-green debt this fix
   retires).

**Tier 3 (explicit loud deferrals):**

6. Any BEHAVIORAL repair to families the new refusal exposes (a
   `nothing_to_run` row that should have a runnable recipe is recipe
   rot — record the list, do not fix them here; they are the next
   maintenance pass's inventory).

## The differential requirement, restated

No v4 oracle applies — the driver is v5-side tooling. The equivalence
standard here is the P4.51 one: every fix ships with (a) a
`--self-test` arm and (b) a mutation proof (the planted-defect
reproduction, recorded in the lane record with the exact commands). A
fix without its red-first reproduction is not done.

## Fixtures

None consumed, none delivered. The marker probes use scratch copies
under the worktree/scratchpad — never the committed fixtures (the
driver's tracked-fixture write warning must stay silent throughout;
if it fires, that is itself a finding).

## Shared contract

**The three lanes of this round meet nowhere.** No shared verbs, no
gated beats, no ACTIVATE-AT-UNIFY constants. Procedural interactions
only: siblings run the UNCHANGED driver from their worktrees; this
lane's changes take effect at unification — the unifier should expect
formerly vacuous greens to answer `nothing_to_run` at the unified gate
(by design, not a regression). Binding on all three orders.

## Ownership

- **This lane owns:** `harness/tools/recipe_sweep.py`; the HEADER lines
  only of the five files listed in the survey (no case-body edits).
- **Must NOT touch:** any committed fixture; any harness `.rs` family;
  any v5 production source; the P4.D96 files (`skip_signal.rs`,
  `build_context.rs`, `message_context.rs`, SPA skip-signal,
  `harness/oracle/cases/skip-signal.ts`); the P4.52 files
  (`memories-*.db`, `memories_routes_equivalence.rs`).
- **Version bumps:** this lane bumps `quilltap-harness` (the driver
  lives under the harness's tooling umbrella; follow P4.51's precedent).
  All three lanes bump harness — the unifier ACCUMULATES.
- `CHANGELOG` and `status-log.md` are append-only (union-merge).

## Verification gate (before declaring done)

- `recipe_sweep.py --self-test` all arms green; the three mutation
  proofs recorded with commands.
- The `brahma_console_routes` family regenerated end-to-end from the
  lane worktree at the `b8449b3e` pin (the post-fix marker probe IS this
  run), zero SKIP.
- `cargo fmt --all --check`; `cargo clippy --workspace --all-targets`
  both feature sets; `cargo test --workspace` (the driver is python, but
  the gate stays whole — the spelling guard and any harness tests that
  shell to the driver must still pass).
- Per-commit rule: `.claude/commands/commit.md`.
