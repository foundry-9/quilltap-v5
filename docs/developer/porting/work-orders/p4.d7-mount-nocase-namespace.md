# Work order P4.d7 — the `0a0419f5` case-insensitive mount-namespace drift re-port (NOCASE indexes, the boot repair pass, case-preserving ops, the 409 name arms)

> **STATUS: OPEN.**
>
> Lane D7 of the FOUR-lane d68638b4 drift-catch-up round (planned
> 2026-07-17 — triggered by the drift check that found v4 ten commits past
> `e3593f75`, including the predicted landing of the custom-tools +
> character-metadata feature). **v4 reference baseline for this round:
> `d68638b4` (4.8.0-dev.72).** At lane start, run in
> `~/source/quilltap-server`:
>
> ```bash
> git log d68638b4..HEAD --oneline
> ```
>
> **If HEAD moved, STOP and report.** Every oracle in this lane generates
> at `d68638b4` — not at `e3593f75`, not at whatever HEAD happens to be.
> If the v4 tree is dirty at regen time, regenerate from a PINNED detached
> worktree at `d68638b4` (recipe: the `[[oracle-regen-pinned-v4-worktree]]`
> memory note + the P4.d5 ∥ P4.6ay round record in the status log —
> symlink BOTH node_modules).
>
> Sibling lanes: `p4.6ay-pascal-custom-tools-server.md` (lane AY — the
> Pascal server surface; disjoint from you except it CONSUMES the
> `database_store` read/list functions you touch — it must not edit them),
> `p4.6az-character-metadata-vault.md` (lane AZ — you share ONE file with
> it, `db/character_vault.rs`, region-pinned in §3 of the Shared
> contract), and `p4.6ba-pascal-salon-spa.md` (lane BA — `apps/web/` only,
> fully disjoint). The **Shared contract** and **Ownership** sections
> below are binding and reproduced verbatim in all four orders. You run in
> a worktree; a unification pass integrates the lanes onto main.
>
> **This lane IS a differential port.** Every unit arrives with an
> equivalence test against v4's real code at `d68638b4`. The facts under
> "Survey-verified starting points" are from a **2026-07-17 survey at
> `d68638b4`** (both repos) — if a unit's fresh survey contradicts this
> order, **STOP and report** before proceeding.

## Mandate

Re-port v4 `0a0419f5` ("fix: vault names and paths are one
case-insensitive namespace") across the already-ported v5 mount-index /
document-store surface: the two unique indexes become **COLLATE NOCASE
under new names** (a D23 `fresh_schema.json` re-dump — the second
human-sanctioned schema follow of the port), a new **boot repair pass**
(`mount-index-case-repair`), case-insensitive + case-preserving folder
and file-link resolution, case-only renames, the same-path copy guard,
case-insensitive store-name uniqueness at the write chokepoints (the 409
arms + ` (N)` suffixing), and the import-seed naming change.

Also disposition, in the status log, the two non-port commits this lane
absorbs: `7d5e6ab3` (v4 CI test infra only — no lib behavior, NO PORT)
and the three docs commits (`a5e66da0`, `554cb26b`, `23b7edad` — already
mirrored under `docs/v4/` at round planning).

**Explicitly NOT this lane:** anything Pascal (`lib/pascal/*`,
`run-custom*` — lane AY), the metadata.json vault surface (lane AZ), the
SPA (lane BA), and `scaffold_character_mount` (lane AZ's region of the
shared file). Do not touch `apps/web/`.

### The schema ruling (D23, applied a second time — do not re-litigate)

v4's fresh `generateDDL` now emits the two indexes as:

```sql
CREATE UNIQUE INDEX "idx_doc_mount_folders_mp_parent_name_nocase"
  ON "doc_mount_folders" ("mountPointId", COALESCE("parentId", ''), "name" COLLATE NOCASE)
CREATE UNIQUE INDEX "idx_doc_mount_file_links_mp_path_nocase"
  ON "doc_mount_file_links" ("mountPointId", "relativePath" COLLATE NOCASE)
```

v5 **FOLLOWS v4** (CLAUDE.md D23): re-dump `fresh_schema.json` from v4's
live `generateDDL` at `d68638b4`, never hand-edit it. **Verify the
re-dump diff is EXACTLY the two index statements** (lines 103 and 107 of
the current file) — the sibling commits add NO columns (metadata.json is
a vault file, not a DB column; the Workbench adds no DDL). If the diff
shows anything else, STOP and report. There is deliberately **no DB
index on store names** (backup restore must recreate legacy rows
verbatim); name uniqueness is app-level + the boot repair.

Existing instances get the new indexes from the **repair pass**, exactly
as v4 does — the migration runner stays unported (standing deferral).

---

## Survey-verified starting points (2026-07-17 @ v4 `d68638b4` / v5 main `01ae43a4` — do not re-derive)

### The v4 repair module — `lib/database/repositories/mount-index-case-repair.ts` (NEW, 354 lines)

Git shows it as binary (it contains non-ASCII `Ärger` strings); it is
plain TypeScript — read it from the working tree. Module doc lines 1–43
carries the invariant + the ASCII-only caveat; carry those why-comments.

- `nocaseUniqueIndexIsValid(db, name)` (`:64-71`) — reads the index's
  `sql` from `sqlite_master`; **trusts it only if the uppercased CREATE
  contains both `UNIQUE` and `NOCASE`** (existence alone is not trusted —
  a hand-made stand-in with the right name but the wrong definition is
  replaced).
- `nextSuffixedName(takenLower, base)` (`:85-91`) — `` `${base} (${n})` ``,
  n starts at **2**; chosen lowercase added to the set so successive
  losers stay mutually unique.
- `nextSuffixedFileName(takenLowerRel, dir, fileName)` (`:94-106`) —
  **suffix before the extension**: `` `${stem} (${n})${ext}` ``; tests
  the full lowercased relative path.
- `renameFolderRow` (`:131-165`) — UPDATE name+path; **descendant walk
  via `parentId`, NOT path-prefix** (prevents exact-duplicate-path rows
  cross-contaminating — carry the comment); then link-row repair:
  `relativePath = ${node.path}/${link.fileName}` matched by `folderId`.
- `repairFolderCaseCollisions` (`:167-204`) — group key
  `` `${mountPointId} ${parentId ?? ''} ${name.toLowerCase()}` ``;
  keep-oldest via `createdAt.localeCompare || id.localeCompare`;
  `siblingLower` seeded from ALL rows sharing `(mountPointId, parentId)`;
  `logger.warn('Mount-index case repair: renamed folder that collided
  with a sibling except for casing', …)`.
- `ensureFolderNocaseUniqueIndex` (`:214-224`) — **repair FIRST,
  unconditionally** (non-ASCII collisions slip past the ASCII-only NOCASE
  index and are caught by the JS scan); then trust-or-recreate: DROP both
  `idx_doc_mount_folders_mp_parent_name` (legacy) and the nocase name,
  CREATE the nocase index.
- `repairLinkCaseCollisions` (`:241-281`) — group key
  `` `${mountPointId} ${relativePath.toLowerCase()}` ``; loser renamed
  `stem (N).ext` in place (`relativePath` + `fileName` UPDATEd);
  `logger.warn('Mount-index case repair: renamed file that collided with
  a sibling except for casing', …)`.
- `ensureLinkNocaseUniqueIndex` (`:291-300`) — same shape; legacy
  `idx_doc_mount_file_links_mp_path` dropped on the way.
- `repairMountPointNameCollisions` (`:319-353`) — returns 0 if the table
  is absent; group by `name.trim().toLowerCase()` (**trim applies to
  vault names only** — folders/links are not trimmed); keep-oldest;
  losers → `nextSuffixedName(takenLower, loser.name.trim())`;
  `logger.warn('Mount-index case repair: renamed document store whose
  name collided with a peer except for casing', …)`.
- **v4 call sites:** each repo's `getCollection()` table-init — folders
  repo `:66`, file-links repo `:246`, mount-points repo `:88` — i.e.
  effectively once per process per startup.

### The v4 case-preserving resolution changes

- `doc-mount-file-links.repository.ts`: `ensureLinkFolderId` (`:67-121`)
  — segment walk with `path=? COLLATE NOCASE ORDER BY (path=?) DESC
  LIMIT 1` (**exact match wins, NOCASE fallback**), continues under the
  folder's STORED casing, returns `canonicalDir`;
  `findByMountPointAndPath` (`:369-385`) — `LOWER(relativePath) =
  LOWER(?)`; `linkBlobContent` (`:579-754`) + `linkDocumentContent`
  (`:760-890`) — **case-preserving upsert**: NOCASE probe on the
  canonical path; found → UPDATE in place keeping stored casing ("a
  write to `NOTES.md` updates the row stored as `notes.md` and keeps its
  casing" — carry it); `linkFilesystemFile` (`:899-1011`) — **ADOPTS the
  scanned casing** (disk is the source of truth there; comment
  `:945-948`).
- `doc-mount-folders.repository.ts`: `findByMountPointAndPath`
  (`:170-191`) — exact-match fast path, then lowercased scan fallback.
- `folder-paths.ts` `ensureFolderPath` (`:108-189`) — case-insensitive
  case-preserving walk; memo key `${mountPointId}:${path.toLowerCase()}`;
  memo value now `{id, path}` (`job-folder-cache.ts` retype — **v5 has
  NO job-folder-cache counterpart; documented absence, not a port**);
  after each segment `currentPath = folder.path` (stored casing).
- `file-ops.ts`: `copyFile` (`:287-416`) — **same-path guard moved
  BEFORE the force-overwrite branch** and made case-insensitive
  (`:296-305`): force-copying a file onto a case-variant of its own path
  must not delete the source; error `Source and destination are the same
  path: ${destRel}` kind `INVALID_PATH`. `moveFile` (`:418-537`) —
  `caseOnlyRename` (`:435-436`): dest-exists check skipped when
  same-mount + lowercase-equal (the `:424` exact-equal guard still
  rejects true no-ops, so `notes.md → Notes.md` passes).
- `folder-ops.ts` `moveFolder` (`:80-142`) — filesystem branch
  `caseOnlyRename` (`:114`), dest probe skipped, `fs.rename` handles it.
- `database-store.ts`: `moveDatabaseDocument` (`:209-268`) —
  `caseOnlyRename = fromRel !== toRel && lower-equal` (`:235`), conflict
  check skipped; `moveDatabaseFolder` (`:418-532`) — dest-conflict
  allows the source itself (`destFolder.id !== sourceFolder.id`,
  `:440-446`), destination **canonicalised under the parent's stored
  path** (`:466-472`), descendants + links prefix-rewritten.
- `character-vault.ts` `ensureCharacterVault` (`:105-184`) — when
  minting a fresh vault: `finalVaultName =
  nextUniqueMountPointName(allStoreNames, vaultName)` (`:147-148`) —
  same-named characters get distinct ` (N)`-suffixed vaults; adoption
  still keys off the **un-suffixed base name** via `findByName` filtered
  to `storeType==='character'` (`:116-131`).
- `unique-mount-point-name.ts` `nextUniqueMountPointName` (`:33-40`) —
  `takenLower` from `.trim().toLowerCase()`; returns `desired` unchanged
  if free; else ` (N)` from 2.
- `lib/import/quilltap-import/import-document-stores.ts` — store names
  now via `nextUniqueMountPointName(takenNames, …)` with
  `takenNames.add(name)` per store (cross-run + intra-payload dedup;
  the `(imported)` duplicate-strategy base name feeds through it).
- `doc-edit/file-management-handlers.ts` — three case-only-rename skips:
  `handleMoveFile` DB branch (`:181-184`) + fs branch (`:230-233`),
  `handleMoveFolder` fs branch (`:857-860`); error strings unchanged
  (`Destination already exists: ${input.new_path}. Move will not
  overwrite existing files.` / `…existing folders.`).

### The v4 409 arms (both routes; body `{ "error": <message> }` via `conflict()`)

- POST `app/api/v1/mount-points/route.ts:162-176` (create) and PATCH
  `[id]/route.ts:102-119` (rename, clash excludes self; only when `name`
  present). Match: `mp.name.trim().toLowerCase() ===
  desired.trim().toLowerCase()`. **Identical message, verbatim:**

  ```
  A document store named "<clash.name>" already exists. Names are matched without regard to case — please choose a different name.
  ```

  (em dash, ASCII double quotes; status 409.)

### The v5 counterpart map

- File-links repo: `crates/quilltap-core/src/db/doc_mount_file_links.rs`
  — `find_by_mount_point_and_path` :1247, `link_exists_at_path` :1221,
  `relative_paths_lower` :1204, `write_database_document` :518,
  `link_document_content` :553, `link_blob_content` :736,
  `link_filesystem_file` :1059, `delete_with_gc` :949,
  `ensure_folder_path` :991, `ensure_link_folder_id` :1533,
  `normalise_relative_path` :1604 (second copy at
  `services/mount_index/path_utils.rs:235`).
- Folders repo: `db/doc_mount_folders.rs` —
  `find_by_mount_point_and_path` :112, `create` :162, `update` :183.
- Mount-points repo: `db/doc_mount_points.rs` — `create` :165, `update`
  :205, `find_all_names` :420, `find_by_name` :486, `count_by_name`
  :688.
- Database store: `db/database_store.rs` — `read_database_document`
  :133, `write_database_document` :175, `move_database_document` :242,
  `list_database_files` :351, `create_database_folder` :424,
  `move_database_folder` :473, existence guards :595/:607/:621; its
  inline test DDL at :693-705 is a SECOND transcription of the two
  tables' shape — update it with the indexes if it carries them.
- Ops: `services/mount_index/file_ops.rs` — `copy_file` :591,
  `move_file` :723, `dest_exists` :219, `delete_at_dest` :487;
  `folder_ops.rs` — `move_folder` :80.
- Naming leaf: `db/ensure_official_store.rs` —
  `next_unique_mount_point_name` :49 (consumed :119).
- Shared file (§3): `db/character_vault.rs` — YOUR region is
  `ensure_character_vault` :174 / `link_character_to_vault` :284 /
  `create_character` :325 (the unique-vault-name change only).
- Import seed: `services/quilltap_import/seed.rs` (+ `seed_assets.rs`) —
  no dedicated import-document-stores file; locate the store-naming site
  by fresh survey.
- Doc-edit handlers: `tools/doc_edit/file_management.rs`.
- Routes: `api/mount_points.rs` — `mount_point_create` :164
  (`validate_create` :247), `mount_point_update` :302; dispatch wired at
  `api/engine.rs:1493`; the file-op verbs in `api/mount_files.rs` (409
  mapping already at :30). Web edge `quilltap-web/src/files_routes.rs` —
  expected UNTOUCHED (the 409s are core-side).
- **Init hooks for the repair pass:** provisioning replay
  (`services/provisioning/mod.rs:128-158`, replays `fresh_schema.json`
  incl. the new indexes) AND the adopt path
  `services/builtin_mounts.rs::ensure_mount_index_tables` :114 (today
  creates only the fast-path `idx_doc_mount_folders_mp_path` at
  :133-134), called from boot `quilltap-host/src/host.rs:604`
  (`seed_built_ins`). **v4's per-repo-init cadence collapses to this
  boot hook in v5's architecture — same effective once-per-startup
  cadence; record that as a documented (non-)divergence with the
  why-comment.**
- `fresh_schema.json` lines: file-links index **:103**, folders index
  **:107** (both plain, old names — the re-dump replaces them).
- No v5 `mount_index_case_repair` module exists (fresh survey confirmed)
  — greenfield file `db/mount_index_case_repair.rs`.
- No v5 job-folder-cache exists — v4's memo retype is a **documented
  absence**.

### v4 oracle-corpus templates

`__tests__/unit/lib/database/repositories/mount-index-case-repair.test.ts`
(270 lines — real in-memory better-sqlite3 required by absolute path;
`freshDb()` at :45-81 creates the tables **plus the legacy case-sensitive
indexes**; the test list in the survey covers folder-subtree+link repair,
scoped uniqueness, idempotence, non-ASCII `Ärger`/`ärger`, the
tampered-index replacement, link suffix-before-extension, vault-name
trim+keep-oldest, and case-insensitive `nextUniqueMountPointName`) and
`__tests__/unit/lib/mount-index/folder-paths.test.ts` (`reuses a
case-variant folder and continues under its stored casing`, :274-311).

---

## Deliverables (tiered)

### Tier 1 — must land

1. **The schema re-dump.** `dump-fresh-schema.ts` re-run at `d68638b4` →
   replaces `fresh_schema.json`; `build-provision-oracle.ts` re-run;
   `provisioning_equivalence`, `builtin_mounts_equivalence`,
   `builtin_templates_equivalence` green against the regenerated file —
   **atomically** (they read `fresh_schema.json` directly). Verify the
   diff is exactly the two index statements.
2. **`db/mount_index_case_repair.rs`** (new) — the full module:
   `nocase_unique_index_is_valid`, `next_suffixed_name`,
   `next_suffixed_file_name`, `rename_folder_row` (parentId walk + link
   repair), `repair_folder_case_collisions`,
   `ensure_folder_nocase_unique_index`, `repair_link_case_collisions`,
   `ensure_link_nocase_unique_index`,
   `repair_mount_point_name_collisions`. Exact suffix formats, exact
   keep-oldest comparator, exact trust rule, warn logs. Wired into
   `ensure_mount_index_tables` (+ the boot path) per the survey note.
3. **Case-insensitive + case-preserving resolution** across
   `doc_mount_file_links.rs` (NOCASE probes, case-preserving upserts,
   `link_filesystem_file` adopting disk casing, `ensure_link_folder_id`
   exact-wins-then-NOCASE walk), `doc_mount_folders.rs`
   (`find_by_mount_point_and_path` fallback), `ensure_folder_path`
   (stored-casing continuation).
4. **Case-only renames + the copy guard**: `file_ops.rs` (`copy_file`
   guard-before-force; `move_file`), `folder_ops.rs` (`move_folder`),
   `database_store.rs` (`move_database_document`,
   `move_database_folder` canonicalisation), `doc_edit/file_management.rs`
   (the three skips).
5. **Store-name uniqueness**: `next_unique_mount_point_name`
   case-insensitive; `api/mount_points.rs` create/update 409 arms with
   the verbatim message; `ensure_character_vault` unique vault names
   (§3 region); the quilltap-import store-naming change.

### Tier 2 — should land

6. The `database_store.rs` inline test-DDL transcription updated to the
   new index shape (if it carries the indexes at all).
7. A migration-vintage regression: open a fixture DB built with the
   LEGACY indexes + planted collisions, boot the repair, assert the
   renames + the index swap (the dogfood-findings "migration-vintage"
   class, scoped to this table pair).

### Tier 3 — DEFERRED, loud (enumerate in the status log)

- **The job-folder-cache retype** — v5 has no job-folder-cache
  subsystem; documented absence.
- **The migration runner** — standing deferral; existing instances are
  served by the repair pass, exactly as v4 serves them.
- `7d5e6ab3` — v4 CI test infra; NO PORT (disposition it).

---

## The differential requirement (restated)

**Every unit ships against v4's REAL code at `d68638b4`. Fix the port,
not the diff.**

- **The repair module** → a tier-2 structural differential templated on
  v4's own `mount-index-case-repair.test.ts` corpus: a jest real-DB
  oracle (`[[jest-real-db-oracle]]`, `[[jest-oracle-mechanism]]`)
  driving v4's REAL `ensureFolderNocaseUniqueIndex` /
  `ensureLinkNocaseUniqueIndex` / `repairMountPointNameCollisions` over
  planted-collision fixtures (legacy indexes + colliding rows), dumping
  post-repair rows + `sqlite_master` index sql → NDJSON → the Rust diff
  runs the ported repair over the same planted fixture. Include the
  non-ASCII, tampered-index, and suffix-before-extension arms.
- **Resolution/ops/naming** → extend the EXISTING families at the new
  baseline: `doc_mount_file_links_tier2` / `doc_mount_folders_tier2` /
  `mount_ops` / `mount_write` / `mount_points_routes` /
  `scriptorium_tools` — add case-variant rows (case-only rename,
  case-variant write-updates-in-place, copy-onto-case-variant guard,
  ensureFolderPath case-variant reuse, 409 clashes, vault-name
  suffixing). Regenerate each family you touch at `d68638b4`; leave
  untouched families alone.
- Memory notes to read first: `[[oracle-regen-from-worktree-path]]`,
  `[[oracle-regen-silent-stale-pass]]`, `[[p4d3-db-size-reduction-drift]]`
  (run `--nocapture`, grep SKIP), `[[e2e-fixture-before-run-order]]`.

## Fixtures

This lane DELIVERS the planted-collision repair fixture family (legacy
indexes + folder/link/vault case collisions incl. one non-ASCII pair and
one tampered index) and REGENERATES: the provision oracle +
`fresh_schema.json` (atomic with the two `builtin_*` tests), and every
existing mount-family fixture/oracle its own edits invalidate. All
regeneration at v4 `d68638b4`, Node 24
(`~/.nvm/versions/node/v24.13.1/bin`); regen recipe in each
differential's header.

---

## Shared contract (binding)

> Reproduced verbatim and identically in all four orders of this round:
> `p4.d7-mount-nocase-namespace.md` (lane D7),
> `p4.6ay-pascal-custom-tools-server.md` (lane AY),
> `p4.6az-character-metadata-vault.md` (lane AZ),
> `p4.6ba-pascal-salon-spa.md` (lane BA).

### §1 — `fresh_schema.json` + the provision oracle (OWNER: lane D7)

Lane D7 owns `crates/quilltap-core/src/services/provisioning/` (the
`fresh_schema.json` re-dump at `d68638b4` — expected diff: EXACTLY the
two mount-index index statements) and `harness/oracle/provision/`.
**P4.6ay's former ownership of `harness/oracle/provision/` is RESCINDED**
(its unit 10 closed). No other lane touches provisioning or the provision
oracle this round. The sibling commits add NO columns (metadata.json is a
vault file, not a DB column; the Workbench adds no DDL) — if D7's re-dump
diff shows more than the two index lines, the ROUND premise is wrong:
STOP and report.

### §2 — `character.metadata` (OWNER: lane AZ — CONSUMER: lane AY)

The field: `metadata` on the HYDRATED character — a flat JSON object
(string keys, any JSON values). **No DB column** (the sole source of
truth is the vault file `metadata.json`); absent/unparseable hydrates
`{}` for a vault-linked character; `null`/absent and `{}` are equivalent.
Lane AZ delivers the hydration (read overlay), the guarded write
projection, the PUT arm, and the scaffold seed. Lane AY consumes it ONLY
through the character read path (the hydrated find-by-id surface) — it
must NOT parse vault files itself. Lane BA never reads it directly
(server DTOs only carry `pascalMeta.metadataTested`).

**Slippage rule:** if lane AZ slips the round, lane AY's handler/route
read `character.metadata` → absent → `{}` — which is v4's own
no-fact-sheet arm, so `run_custom` still lands and every metadata test
declines to the catch-all. Lane AY marks its metadata-HYDRATION
differential rows deferred; its execution-core rows (metadata injected
directly) are unaffected. The unifier does NOT withhold `run_custom` for
AZ slipping.

### §3 — the region-pinned shared file `crates/quilltap-core/src/db/character_vault.rs` (D7 ∥ AZ)

| Region | Owner |
|---|---|
| `ensure_character_vault` (:174) + `link_character_to_vault` (:284) + `create_character` (:325) — the unique-vault-name change ONLY | **D7** |
| `scaffold_character_mount` (:105) + the NEW `ensure_character_metadata_file` | **AZ** |

Neither lane may reformat, reorder, or re-wrap the file; both cite this
table in the commit message of any commit touching it. The unifier takes
both hunks. **Any other same-file collision anywhere in the round is a
planning bug — stop and report rather than resolving it in-lane.**

### §4 — the SPA wire contract (OWNER: lane AY — CONSUMER: lane BA)

- **Dispatch verbs** (added to the CoreRequest/CoreResponse unions on
  both sides, name-for-name):
  - `{ type: 'chatCustomToolsList', chatId }` → data
    `{ tools: CustomToolListing[], errors: CustomToolLoadError[], droppedForCap?: string[] }`
  - `{ type: 'chatCustomToolRun', chatId, tool, parameters?, private?, asCharacterId? }`
    → data `{ messages: MessageDto[], result: { tool, value, state, message, whispered } }`
  - The REST legs (`GET`/`POST /api/v1/chats/{id}/custom-tools`) land
    with them; the verbs are the SPA's path.
- `CustomToolListing` = v4 `route.ts:57` fields **plus `mountPointId`**
  (the d68638b4 addition): `{name, title, description, parameters,
  defaultVisibility, sourceTier, characterLabel?, asCharacterId,
  definitionPath, mountName, mountPointId}` — never the roll spec or the
  outcome table.
- **SSE:** the stream frame gains `pascalResult` carrying the posted
  Pascal message, beside `carinaAnswer` / `hostAnnouncement`.
- **`MessageDto`:** gains `pascalMeta`, transcribed field-for-field from
  v4 `lib/schemas/chat.types.ts:342-370` (incl. `toolTitle`,
  `invokedBy`, `metadataTested`); `systemSender` union gains
  `'pascal'`; `systemKind` values `'custom-tool-result'` /
  `'custom-tool-error'`. Both lanes transcribe from the SAME v4 lines;
  neither invents fields.
- **Settings:** the chat-settings key is `customTools` (boolean,
  `?? true`).
- **Slippage rule:** lane BA ships spec-verified components over mocked
  transport; live e2e beats ACTIVATE-AT-UNIFY. If lane AY slips, the
  unifier holds BA's composer entry points dark and enumerates them.

## Ownership (binding)

> Reproduced verbatim and identically in all four orders. `CHANGELOG` and
> `docs/developer/porting/status-log.md` are **append-only for every
> lane** and union-merge at unification (standing rule).

### Lane D7 — `p4.d7-mount-nocase-namespace.md`

- `crates/quilltap-core/src/db/`: `mount_index_case_repair.rs` **(new)**,
  `doc_mount_file_links.rs`, `doc_mount_folders.rs`,
  `doc_mount_points.rs`, `database_store.rs`, `ensure_official_store.rs`
- `crates/quilltap-core/src/services/`: `mount_index/` (whole dir),
  `builtin_mounts.rs`, `provisioning/` (per §1),
  `quilltap_import/seed.rs` + `seed_assets.rs` (the store-naming change
  only)
- `crates/quilltap-core/src/tools/doc_edit/file_management.rs`
- `crates/quilltap-core/src/api/`: `mount_points.rs`, `mount_files.rs`
- `crates/quilltap-host/src/host.rs` (the repair wiring only)
- `harness/oracle/provision/`; the mount/doc-mount/scriptorium oracle
  cases + fixtures + their `crates/quilltap-harness/tests/` counterparts;
  the new case-repair oracle family

### Lane AY — `p4.6ay-pascal-custom-tools-server.md`

- `crates/quilltap-core/src/pascal/` (all of it, incl. `dice.rs` now —
  the former lane-A split is over), `workbench.rs` **(new, under
  `pascal/`)**
- `crates/quilltap-core/src/tools/`: `run_custom.rs` **(new)**,
  `executor.rs`, `capabilities_report.rs`, `definitions/data.rs`
- `crates/quilltap-core/src/services/`: `pascal_writer.rs` **(new)**,
  `prospero_notifications.rs`, `tool_build.rs`, `tool_execution.rs`,
  `orchestrator.rs`, `chat_events.rs`
- `crates/quilltap-core/src/api/`: `salon.rs`, `settings.rs`, `mod.rs`,
  `engine.rs`, `types.rs` (the new verbs), the new custom-tools api
  module(s)
- `crates/quilltap-web/` (the custom-tools routes)
- `harness/oracle/cases/`: the `pascal-*` + `tool-definitions*` families;
  their `crates/quilltap-harness/tests/` counterparts; the new
  chat-custom-tools route + workbench families

### Lane AZ — `p4.6az-character-metadata-vault.md`

- `crates/quilltap-core/src/vault_overlay.rs`
- `crates/quilltap-core/src/db/`: `vault_read_overlay.rs`,
  `vault_character_write.rs`, `vault_character_update.rs`,
  `characters.rs`, `characters_read.rs` (if the survey shows the
  hydrated field needs it), `character_vault.rs` (per §3 — its region
  ONLY)
- `crates/quilltap-core/src/api/characters.rs`
- `crates/quilltap-core/src/services/quilltap_import/characters.rs`
- `harness/oracle/cases/` + `crates/quilltap-harness/tests/`: the
  `vault-*` and `characters-*` families it touches; the new
  metadata-roundtrip family

### Lane BA — `p4.6ba-pascal-salon-spa.md`

- `apps/web/` — everything it needs there (no other lane touches
  `apps/web/`)

### Version-bump ownership

Lanes D7, AY, AZ bump `quilltap-core` + `quilltap-harness` per their own
commits; lane AY additionally owns any `quilltap-web` bump; lane D7 owns
any `quilltap-host` bump; lane BA owns the SPA version. **Identical first
bumps off a shared base merge SILENTLY** — the unifier recounts
`base + total bumps` (`[[p4.6aw-ax-8-round-plan]]`); no lane may assume
its number survives.

---

## Verification gate (before declaring the lane done)

1. `cargo fmt` — clean.
2. `cargo clippy -D warnings` — BOTH feature sets (default AND
   `native-transport`).
3. `cargo test --workspace --no-fail-fast` — every differential env var
   set.
4. This lane's differentials re-run BY NAME with `--nocapture`, grep'd
   for SKIP — zero SKIP.
5. Every oracle this lane touches regenerated FRESH at `d68638b4`
   (pinned worktree if the v4 tree is dirty), incl. the
   `fresh_schema.json` re-dump with `provisioning_equivalence` +
   `builtin_mounts_equivalence` + `builtin_templates_equivalence` green
   against the regenerated file.
6. Every Tier-3 item enumerated in the status log as a loud refusal or a
   documented absence.
7. Per-commit rule: `.claude/commands/commit.md` (version bump, CHANGELOG
   every commit, status-log append). CLAUDE.md is summary-only.

## Ground rules

The standing set: fix the port, not the diff; never accept unverified
Rust; no stubs or `TODO` beyond the enumerated Tier-3 refusals; the
cipher is frozen; the schema moves ONLY by the §1 re-dump (D23 — an
exception, not a precedent); synthetic test-pepper fixtures only;
database writes only against copies. **If a unit's fresh v4 survey
contradicts this order, STOP and report before proceeding.**
