# Work order P4.20 — the enclave-step proactive pre-compute windowing divergence

> **STATUS: OPEN — not started.**
>
> **v4 baseline for this round: `dcd9440a`** (v4 HEAD, 2026-07-30; adopted at
> planning — the one-commit drift past `5cc76688` is P4.D29's, a
> store-overlay surface this lane never touches). **Drift-check at lane
> start**: `git log dcd9440a..HEAD --oneline` + `git status --short` in
> `~/source/quilltap-server`. If v4 moved past `dcd9440a`, STOP and report.
> v4's tree was CLEAN at `dcd9440a` at planning; oracles regenerate straight
> from the checkout (pin a detached worktree only on drift/dirty —
> `oracle-regen-pinned-v4-worktree`).
>
> **Siblings (same round, disjoint files):** P4.D29 (store-overlay read
> hardening, `db/document_store_overlay.rs`), P4.21 (image-attachment wire,
> `model/**` + `services/orchestrator.rs`), P4.9P (page-toolbar vertical,
> `apps/web` + `api/**`). **⚠ `services/orchestrator.rs` belongs to P4.21
> this round.** If this lane's diagnosis demands a change at the pre-compute
> CALL SITE (which lives in `orchestrator.rs`), STOP that unit, record the
> exact diff + the evidence in the lane record, and leave the edit to the
> unifier as a unification wire. Do NOT edit the file. See "Shared contract +
> Ownership" — binding and identical across all four orders.
>
> **Worktree note:** own branch, own checkout; no `git stash`/`git worktree`.

## Mandate

Close the STANDING RED: `enclave_step_tier3_equivalence` has been excluded
from the pass/fail count since the `5cc76688` round because v5 runs the P4.19
proactive pre-compute distill where v4's `proactiveRecallTask` bails — **one
extra cheap-LLM call + one error `llm_logs` row per affected autonomous turn,
real money in production**. Diagnose exactly why v4 bails (v4-side
instrumentation in a scratch worktree), mirror the behavior in v5's
`services/pre_compute.rs`, pin it with a dedicated differential case, and
return `enclave_step_tier3_equivalence` to the normal green gate. Do NOT
touch: `services/orchestrator.rs` / `build_context.rs` (P4.21's / STOP-rule
above), `model/**`, `api/**`, the SPA, any fixture another family owns.

## Survey-verified starting points (2026-07-30)

**The divergence, as proven at the `5cc76688` unification** (full diagnosis:
`status-log.md` → "⚠ `enclave_step_tier3_equivalence` is RED — pre-existing,
NOT this lane's"; it includes a three-way proof table):

- In the fixture's `fold_fires` case (the "Fold room", 11 alternating
  ASSISTANT seeds), v5 runs the proactive pre-compute distill with a
  1-message window (`Fold seed line 11.`) where v4's `proactiveRecallTask`
  NEVER calls the cheap LLM. The harness has no canned entry for a call v4
  never made → v5's call fails → the failure is logged as an `llm_logs`
  error row (the ruled P4.13 divergence — that ruling stands; do not touch
  it) → and because pre-compute returned nothing, v5 ALSO runs
  build_context's fallback distill (the 11-message window v4 uses). One
  extra call, one extra row, one cascade. **v4 emits exactly 12 distill
  calls; v5 emits 13.**
- The fixture's other 20 chats carry ≤1 seeded message, so pre-compute's
  window and buildContext's last-12 window render IDENTICALLY there — the
  fold room is the ONLY case where the two windows differ, which is why it
  is the only case that exposes anything. **This blindness is itself a
  finding: the precompute family needs a case where the windows differ.**
- **UNDETERMINED at planning, and this lane's first job:** whether v4's
  `proactiveRecallTask` runs-and-bails in the enclave path or is never
  reached with the same inputs. Both of its data guards READ as satisfied
  (`cheapLLMSelection` non-null — the same chat's buildContext distill runs;
  Byron, the verified responder on both sides, last spoke at seed line 10
  with Ada's line 11 after it, so `messagesSinceLastSpoke` should be
  non-empty). Something else differs — settle it empirically, not by
  reading.

**v4 (at `dcd9440a`):**

- `lib/services/chat-message/pre-compute.service.ts` —
  `runPreContextPreCompute` (:88-95) runs `compressionTask` ∥
  `proactiveRecallTask`; `proactiveRecallTask` (:172-240) guards, in order:
  1. `!cheapLLMSelection || !character.id` → undefined;
  2. `characterMessages.length === 0` where `characterMessages` filters
     `role === 'ASSISTANT' && m.participantId === characterParticipant.id`
     — **keys on the PARTICIPANT id**;
  3. window = events after the last such message, filtered USER/ASSISTANT,
     plus the just-saved user `content` when `!isContinueMode && content`;
     empty window → undefined.
- The ONE call site: `lib/services/chat-message/orchestrator.service.ts:1021`
  — the enclave/autonomous turn goes through this same orchestrator, so v4
  DOES reach `runPreContextPreCompute` on autonomous turns; the bail (if it
  is a bail) is inside `proactiveRecallTask`. Candidate suspects to check
  under instrumentation: what `isContinueMode` / `content` /
  `characterParticipant.id` / `existingMessages` actually are on the
  autonomous turn (an autonomous turn may run continue-mode with empty
  content, or resolve a different participant row than the fixture's seeds
  carry).

**v5 (main, core 0.0.411):**

- `crates/quilltap-core/src/services/pre_compute.rs` —
  `messages_since_last_spoke` (:113-182; participant-scoped, the
  is_continue/content append, never-spoke/empty-window short-circuits) and
  `proactive_recall_task` (~:210+). Its unit tests (:428+) replicate v4's
  jest windowing/guard cases (`pre-compute.service.test.ts:138-264`).
- Call sites (NOT this lane's files): `services/orchestrator.rs` (the spine
  call, mirroring v4 :1021) and `services/build_context.rs` (the
  pre-searched-head suppression of the fallback distill). If the fix is a
  guard/windowing change INSIDE `pre_compute.rs`, land it here; if it is an
  input the call site passes, STOP per the preamble rule.
- The enclave entrance: `crates/quilltap-core/src/enclave/step.rs` (this
  lane's file) — `step()` composes over `process_message`; check what the
  autonomous turn passes for continue-mode/content/participant.

**The differential machinery (this lane's files):**

- `crates/quilltap-harness/tests/enclave_step_tier3_equivalence.rs` — the
  19-call tier-3 family; its recipe header pins **TZ=UTC (REQUIRED since
  P4.d26** — the step passes the instance zone as `server_tz`; never
  regenerate without it). Fixture builds into `/tmp` via
  `harness/oracle/fixtures/build-enclave-step-fixture.ts`; oracle case
  `harness/oracle/cases/enclave-step-tier3.test.ts`
  (`--testTimeout=120000`, the `oracle-jest-testtimeout` rule).
- `crates/quilltap-harness/tests/precompute_equivalence.rs` + oracle case
  `harness/oracle/cases/precompute.test.ts` — the 8-case tier-3 family
  P4.19 delivered; TZ=UTC pinned since P4.d26.

## Tiered deliverables

### Tier 1 — must land

1. **The empirical diagnosis.** In a scratch v4 worktree (never the real
   checkout — and note the pinned-worktree symlink recipe if the tree goes
   dirty), instrument `proactiveRecallTask` (entry + each guard + the
   computed window) and re-run the enclave-step oracle. Record, for the
   `fold_fires` turn: whether the task runs, which guard bails (or what the
   window is), and the actual `isContinueMode` / `content` /
   `characterParticipant.id` / message-event `participantId`s. The lane
   record carries the transcript — this is the round's provenance for
   whatever the fix turns out to be.
2. **The v5 fix, confined to this lane's files** (`pre_compute.rs` and/or
   `enclave/step.rs`). If the diagnosis shows the divergence lives at the
   orchestrator call site instead, STOP this unit and record the exact diff
   for the unifier (preamble rule). No speculative guard changes: the fix
   must reproduce v4's observed decision, not merely silence the extra call.
3. **The dedicated differential case.** Extend the `precompute` family with
   at least one case where pre-compute's since-last-spoke window and
   buildContext's last-12 window DIFFER (the fold-room shape: a multi-seed
   chat where the responder last spoke mid-history) — the case class whose
   absence let this divergence ship green in P4.19. Regenerate the
   precompute oracle at `dcd9440a` (TZ=UTC) and prove the new case
   red-without-the-fix / green-with-it (the D24 mutation rule).
4. **The family returns to the gate.** Regenerate the enclave-step fixture +
   oracle fresh at `dcd9440a` (TZ=UTC pinned) and run
   `enclave_step_tier3_equivalence` GREEN — 12 distill rows both sides, no
   error row, no fallback cascade. Remove the standing-red exclusion
   language from the family's header if any was added; the unifier will
   strike the CLAUDE.md/phase-4.md standing-red notes (do not edit those
   files here — record the exact wording to strike in the lane record).

### Tier 2 — should land

5. A `pre_compute.rs` unit test mirroring whatever v4 jest case (existing or
   newly implied by the diagnosis) covers the bail arm — so the guard is
   pinned at the module level as well as the differential level.

### Tier 3 — explicit loud deferrals

- The P4.13 failed-cheap-call error-row ruling is OUT OF SCOPE — v5
  deliberately logs failed cheap calls where v4 does not; the fix here is
  that the failing CALL disappears, not the ruling.
- If the diagnosis surfaces v4-side dead code or a v4 bug (the
  runs-and-bails question has room for one), do NOT fix v4 — record it for
  the post-5.0 v4-side list in `dogfood-findings.md`, per precedent.

## The differential requirement, restated

Every behavior change ships against v4's REAL code: the enclave-step family
drives v4's real `handleAutonomousRoomTurn`/`handleAutonomousRoomScheduleTick`
UNFORKED with only the model boundaries mocked (`tier3-completion-oracle` —
pin completions by oracle-RECORDED keys); the precompute family drives the
real `runPreContextPreCompute`. Recipe headers in both harness files are
current — follow them verbatim, including TZ=UTC and Node 24. Grep fresh
NDJSON for a new-baseline marker (`oracle-regen-silent-stale-pass`).

## Fixtures

Both families build fixtures into `/tmp` (nothing committed). This lane owns
`build-enclave-step-fixture.ts` and the precompute case/fixture files, and
owns regenerating both families' oracles at `dcd9440a`. If the new
window-differing precompute case needs fixture-builder changes, they live in
this lane's builder files only.

## Shared contract + Ownership (binding; identical across all four orders)

**File ownership this round — no lane edits another lane's files:**

| Lane | Owns (exclusively) |
|---|---|
| P4.D29 | `crates/quilltap-core/src/db/document_store_overlay.rs`; harness tests + oracle cases/fixture builders for the groups/projects/vault-overlay families |
| P4.20 (this) | `crates/quilltap-core/src/services/pre_compute.rs`; `crates/quilltap-core/src/enclave/**`; `crates/quilltap-harness/tests/{enclave_step_tier3,precompute}_equivalence.rs`; `harness/oracle/cases/{enclave-step-tier3,precompute}.test.ts` + their fixture builders |
| P4.21 | `crates/quilltap-core/src/model/**`; `crates/quilltap-core/src/services/{orchestrator,regenerate_swipe,tool_call_threading,file_fallback,message_context,chat_files}.rs`; `crates/quilltap-host/src/**`; the request-envelope corpus + recorder + `request_builder*_equivalence.rs` + `file_attachment_tier3_equivalence.rs` / `attach_mount_file_equivalence.rs` |
| P4.9P | `apps/web/**`; `crates/quilltap-core/src/api/**` (incl. `types.rs` + `engine.rs`); `crates/quilltap-web/src/**`; its new ui-search harness/oracle files |

- **`services/orchestrator.rs` belongs to P4.21.** P4.20 STOPS and records
  rather than edit it (see Mandate).
- **`api/types.rs` + `engine.rs` belong to P4.9P** — no other lane opens
  them.
- `CHANGELOG.md` and `docs/developer/porting/status-log.md` are append-only
  for everyone (union-merge at unification).
- Version-bump ownership: every lane bumps the crates it touches per
  `.claude/commands/commit.md`; the unifier recounts and collapses
  (`parallel-round-playbook`).
- All four lanes pin **v4 baseline `dcd9440a`** and drift-check at lane
  start. The distill-transitive tier-3 recipes keep their TZ=UTC pins
  (P4.d26 rule).
- P4.D29 QUEUES the baseline move for the unifier; the other three lanes do
  not write baseline wording at all.

## Verification gate before declaring done

- `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D
  warnings` on BOTH feature sets.
- `cargo test --workspace --no-fail-fast` with this lane's env vars set
  (`cargo-test-workspace-is-fail-fast`: per-binary fail-fast — use
  `--no-fail-fast` to see real blast radius).
- `enclave_step_tier3_equivalence` + `precompute_equivalence` re-run BY NAME
  with `--nocapture`, zero SKIP, GREEN, over oracles regenerated fresh at
  `dcd9440a` with TZ=UTC (marker-grepped).
- No `apps/web` change → no SPA gate owed. Say so in the lane record.
- Per-commit rule: follow `.claude/commands/commit.md`.
