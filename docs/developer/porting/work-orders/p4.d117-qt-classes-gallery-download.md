# Work order P4.D117 — the client-fixes drift pair: inert qt-* utilities made real (bugs 100/102) + the gallery download and modal stacking fix (bug 99)

**Status:** OPEN — not started (written 2026-08-25).

## Preamble

- **v4 baseline for this round: `f6a10055`** (v4 main). The round absorbs the
  FIVE commits past it (drift-ledger §3); this lane carries `309aaa97`
  (bugs 100/102 — the qt-* utility sheet + call-site rewrites + the lint
  guard) and `8018c487` (bug 99 — the embedded-gallery download + the
  ImageDetailModal portal). Siblings carry the rest. At planning the v4
  checkout was on `main`, tree CLEAN, HEAD `8f910137`; `bugfix` has no
  unabsorbed content beyond the tests-only `009c49b2`.
- **Run the drift ledger's §2 freshness probe at lane start**
  (`docs/developer/porting/drift-ledger.md`). Probe failure = **STOP and
  report**; a lane never writes the ledger.
- **Regen rule in force: pin required** for any oracle/recorder run (ledger
  §5.1). This lane is client-side; its v4 spec reads come from
  `git show 309aaa97:<path>` / `git show 8018c487:<path>` — never the moved
  checkout.
- **Sibling lanes:** P4.D115 (server, disjoint), P4.D116 (the scenario SPA
  lane — the §Ownership dir split is the fence; **coordinate Playwright:
  port 4319 is repo-wide, two lanes cannot run e2e at once**), P4.D118
  (disjoint).
- **Worktree:** per the `carryout` skill; edit inside the worktree. SPA
  worktree `node_modules` via `npm ci`, never `cp -a`. `docs/CHANGELOG.md`
  and `status-log.md` append-only.

## Mandate

Port v4's two client fixes. **Bugs 100/102:** v5 ported the qt-* utility
sheet at P4.D34 and measurably inherits the inert names — `hover:qt-*`
variants, the missing opacity steps, and the `qt-text-on-*` /
`qt-text-*-foreground` family appear across 20+ v5 template files while
`src/styles/qt-components/_utilities.css` defines none of them (verified at
planning by grep). Land v4's +490-line utilities expansion, map v4's ~45
call-site rewrites onto their v5 counterparts, sweep v5's OWN inert usages
(v5 has more sites than v4 — Angular templates accumulated their own), and
port the `check-qt-classes` build guard so the class of bug can never ship
silently again. **Bug 99:** the character-gallery download button + blob
download hook, and the image-detail modal freed from the workspace's
stacking trap — v5's `.qt-workspace` carries the same `isolation: isolate`
(`_workspace.css:34`) and its modal may render in place; **measure first,
don't assume** (the ledger row's instruction): v5's galleries got their
download buttons from `af1bc479`'s port (P4.D114), so the surfaces may
differ from v4's pre-fix state in either direction.

**Must NOT touch:** `crates/**`; `apps/web/src/app/screens/new-chat/**`,
`apps/web/src/app/chat/sidebar/**`, `apps/web/src/app/scenario/**`,
`apps/web/src/app/core/core-contract.ts` (all P4.D116 — if the guard finds
inert names in those dirs, RECORD them for the unifier instead of editing).
v4's `help/character-gallery.md` delta → the `p4.9i2` bank row.

## Survey-verified starting points (fresh survey 2026-08-25)

⚠ Port from the shipped hunks, never the commit message (ledger §5.3). Read
v4's own bug write-ups first — they are the mechanism notes:
`git show 309aaa97:docs/developer/bugs/bug-100-qt-text-foreground-classes-inert.md`,
`...bug-102-qt-utility-variants-and-opacity-inert.md`,
`git show 8018c487:docs/developer/bugs/bug-99-gallery-modal-controls-under-toolbar.md`.

### v4 — `309aaa97` (bugs 100/102)

- **`app/styles/qt-components/_utilities.css` (+490)**. The additions, by
  family: `qt-bg-secondary`, `qt-bg-input`; the opacity steps as escaped
  class names over `color-mix(in srgb, var(--color-X) N%, transparent)`
  (`.qt-bg-muted\/20` … `\/80`, `.qt-bg-card\/50` `\/60` …, and the rest —
  read the full hunk); a **STATE VARIANTS** section writing every
  `hover:`/`focus:`-form utility out BY HAND with its own escaped selector
  and pseudo-class (Tailwind generates variants only for utilities it knows;
  anything declared inside `@layer utilities` is invisible to it, so
  `hover:qt-bg-muted` was an undefined class, not "qt-bg-muted on hover");
  and the `qt-text-on-*` family (the `qt-text-*-foreground` names bug 100
  found inert). Transcribe byte-faithfully; v5's sheet is
  `apps/web/src/styles/qt-components/_utilities.css` (819 lines at
  planning) — diff it against v4's PRE-commit sheet first to know exactly
  which of the 490 lines v5 already has under other spellings.
- **~45 component files with call-site rewrites** (the commit stat lists
  them: wardrobe dialogs/rows/editor, LLMInspectorEntry, LLMLogViewerModal,
  AIImportWizard, StartupProgress, memory-* tool cards, GalleryImage,
  DocumentPickerModal, TabooSettings, api-keys dialogs, ChatCard,
  FileConflictDialog, InsertAnnouncementDialog, instance-lock/version-guard
  gates, DataDirectorySection, tasks-queue, ChatCreationProgressModal, and
  more). For each, find the v5 counterpart (most exist — the planning grep
  found `hover:qt-`/`qt-text-on-` hits in `wardrobe-item-row.ts`,
  `wardrobe-control-dialog.ts`, `llm-inspector-entry.ts`,
  `llm-log-viewer-modal.ts`, `rng-dropdown.ts`, `tool-settings-content.ts`,
  `scriptorium-badge.ts`, `insert-announcement-dialog.ts`, memory/almanack
  cards, and ~10 more) and apply v4's post-commit spelling. v5-only usages
  with no v4 counterpart get the nearest v4-faithful spelling and a recorded
  note in the lane record.
- **`scripts/check-qt-classes.mjs` (NEW)** — the build guard. Scope: the
  four *utility* families (`qt-bg-`, `qt-text-`, `qt-border-`, `qt-shadow-`)
  plus ANY `qt-*` token carrying a variant prefix; deliberately does NOT
  police bare component classes (theme hooks are meant to have no rule —
  the allowlist would rot). Escape hatch: a line containing
  `qt-class-exception` is skipped. v4 wires it into `npm run lint`. **Port
  it adapted to v5's reality:** class strings live in Angular inline
  templates inside `.ts` files and in `.html` files; the CSS it validates
  against is `src/styles/qt-components/*.css`. Wire it into the SPA's lint
  or test gate so the workspace gate runs it (name the wiring in the lane
  record). Carry v4's header comment — it is the mechanism note for why no
  other layer can catch this class of bug.

### v4 — `8018c487` (bug 99)

- **`components/images/embedded-gallery/GalleryImage.tsx`** — a hover
  Download button (`!isMissingImage` gate, `title`/`aria-label`
  `"Download image"`, download icon) alongside the existing controls.
- **`.../hooks/useGalleryData.ts`** — `handleDownloadImage(image)`:
  `src = image.url || filepath (with leading-/ normalization)` → `fetch` →
  `!res.ok` throws `Failed to fetch image (${status})` → `blob` →
  `triggerDownload(blob, image.filename)`; catch →
  `showErrorToast('Failed to download image')` + console.error with
  `{error, entityId, imageId}`.
- **`.../EmbeddedPhotoGallery.tsx` / `GalleryGrid.tsx` / `types.ts`** — the
  `onDownloadImage` threading with `e.stopPropagation()`.
- **`components/images/image-detail/ImageDetailModal.tsx`** — the actual
  bug: rendered in place, the overlay lands inside `.qt-workspace`, whose
  `isolation: isolate` traps its `z-[60]` in the workspace's own stacking
  context, so the sticky `.qt-page-toolbar` (z-30) paints over the
  top-right Download/Copy/Close controls. Fix = `createPortal(...,
  document.body)` + the `typeof document === 'undefined'` guard. **The v5
  measurement comes first:** v5 has `src/app/images/image-detail-modal.ts`
  (+spec), `gallery-tab.ts` (`src/app/screens/characters/view/tabs/`),
  `photo-gallery-modal.ts`, `image-actions.ts`, and the same
  `isolation: isolate` on `.qt-workspace`. Determine (a) whether v5's modal
  renders inside the workspace pane or already escapes it (CDK
  overlay/body-appended = already fixed by construction), and (b) whether
  the embedded character gallery already grew a download control from
  P4.D114's `af1bc479` port. Port exactly the deltas v5 measurably lacks;
  record what it already had. The Angular equivalent of the portal is a
  body-appended host — do NOT reach for z-index escalation (v4's fix
  explicitly rejects that; the workspace isolation is load-bearing).

### v5 — starting points (verified 2026-08-25)

- `apps/web/src/styles/qt-components/_utilities.css` (819 lines),
  `_workspace.css:34` (`isolation: isolate`).
- Inert-name usage sweep seed: `ggrep -rc "hover:qt-\|qt-text-on-"
  apps/web/src/app` — 20+ files at planning (list in the planning session's
  survey; regenerate it fresh, and widen the pattern to every family the
  guard covers once the guard exists — the guard IS the sweep tool).
- Angular pitfalls: `angular-custom-element-host-is-inline.md` (if a new
  host element is introduced for the portal fix),
  `porting-a-react-component-to-angular.md`.

## Tiered deliverables

**Tier 1 (must land):**

1. The `_utilities.css` expansion, byte-faithful to v4's post-commit sheet
   (modulo the diff against what v5 already carries — documented).
2. The call-site sweep: v4's ~45 rewrites mapped onto v5 counterparts + the
   guard-found v5-only fixes (excluding P4.D116's dirs — recorded, not
   edited).
3. The `check-qt-classes` guard ported and wired into the SPA gate, GREEN on
   this lane's end state (excluding-dirs misses recorded for the unifier).
4. Bug 99, measured then ported: the download button/hook wherever v5's
   embedded gallery measurably lacks it, and the modal freed from the
   stacking trap wherever v5's modal measurably has it. The measurement
   itself (a real-browser or e2e stacking probe — jsdom does no cascade, so
   a unit spec CANNOT see this bug; v4's guard header says exactly that)
   goes in the lane record with numbers/screenshots.
5. An e2e beat for whichever bug-99 half lands: open the character gallery →
   open the detail modal → the Download/Close controls are clickable
   (visible AND hit-testable above the toolbar), and the hover download
   downloads under the expected filename.

**Tier 2 (should land):**

6. The `qt-class-exception` escape-hatch convention documented in v5's sheet
   header alongside the guard pointer.

**Tier 3 (explicit loud deferrals):**

7. `qt-image-gallery`'s missing v5 host (E5, pre-existing — v4's two mount
   sites are unported and `GET /api/v1/images?tagType&tagId` has no v5
   verb). NOT this lane's scope; leave the existing header note, extend it
   only if this lane's changes touch that component's classes.
8. The `p4.9i2` bank row for `help/character-gallery.md`.

## The differential requirement, restated

CSS and class-string fidelity is proven by transcription (byte-diff the
utilities additions against v4's sheet; the guard proves closure — every
qt-* utility/variant name used in v5 resolves to a rule). Bug 99's fix is
proven by the e2e beat (the stacking bug is invisible to every non-browser
layer) — red-first where v5 measurably has the bug: demonstrate the
unreachable control in the beat BEFORE the fix, then land the fix and watch
it green. The download hook gets a spec pinning the error toast sentence and
the filename source. No jest/tsx oracle applies to this lane except any
recorder it chooses for string tables — none is expected.

## Fixtures

- Consumes the committed e2e fixture's character gallery (images exist —
  the gallery-tab beats already run). If a seeded image is missing for the
  modal beat, extend the beat to create one through the UI/API rather than
  the fixture.

## Shared contract

None with P4.D115/P4.D118. With P4.D116: the qt-class seam only — P4.D116
transcribes v4's class strings verbatim (its v4 source `44a8137e` postdates
v4's qt fix, so its spellings are already the fixed ones); this lane's guard
runs over the union at unification and residual inert names in P4.D116's
dirs are fixed by the unifier, never by this lane editing those dirs
mid-round.

## Ownership (BINDING — identical table in all four orders)

| Lane | Owns (exclusive) | Version bumps |
|---|---|---|
| P4.D115 | `crates/quilltap-core/**`, `crates/quilltap-web/**` (only if the Tier-3 REST edge lands), `harness/oracle/cases/**` + `crates/quilltap-harness/**` for the chat families it touches | core, harness, web (only if touched) |
| P4.D116 | `apps/web/src/app/screens/new-chat/**`, `apps/web/src/app/chat/sidebar/**`, NEW `apps/web/src/app/scenario/**`, `apps/web/src/app/core/core-contract.ts`, its e2e specs | SPA |
| P4.D117 | `apps/web/src/styles/**`, NEW `apps/web/scripts/check-qt-classes.mjs` + lint wiring, `apps/web/src/app/images/**`, the qt-class sweep over `apps/web/src/app/**` EXCLUDING P4.D116's three dirs above | SPA (unifier recounts) |
| P4.D118 | `crates/quilltap-cli/**`, the Tier R files in `crates/quilltap-harness/**` | cli, harness (Tier-R files) |

`docs/CHANGELOG.md` + `status-log.md` append-only for everyone; nobody edits
`drift-ledger.md` from a lane. Both SPA lanes bump the SPA version for their
own commits; the unifier recounts (the standing silent-auto-merge trap).

## Verification gate (before declaring done)

- `npm test` full SPA suite green; `npm run build` clean; the new guard
  green and demonstrably RED under a planted inert class (mutation-proof the
  guard itself — plant `hover:qt-bg-nonexistent` in one owned file, watch it
  fail, remove it).
- Full Playwright green locally, including the new beat(s) (coordinate the
  4319 port with P4.D116).
- Per-commit rule: `.claude/commands/commit.md`. Lane record appended to
  `status-log.md`.
