# Work order P4.9K2 — the character-generators SERVER lane, the creation pair: the AI Wizard (streaming + non-streaming) and Summon From Lore / the AI character import — verbs, host driver, the new committed `character-generators-*` fixture, REST/SSE edges, differentials

**Status:** OPEN (ordered 2026-09-07, the `p4.9k` character-generators round:
P4.9K0 → P4.9K1 ∥ P4.9K2 ∥ P4.9K3 ∥ P4.9K4 ∥ P4.80 ∥ P4.81). **STACKED on
P4.9K0's branch tip** (§S.1) — do not start until K0's header reads LANE
COMPLETE. The unifier updates this header at unification.

## Preamble

- **v4 oracle baseline: `f699da6f6`** (drift-ledger §1). v4 `main` HEAD is
  the baseline; `bugfix` carries no unabsorbed content; no drift to absorb.
  The `40d507cc` generators-taxonomy + `4423ad10` hair edits banked for
  "the future generators lane" (`status-log.md:75297`, `phase-4.md:5147`)
  are IN the two service files at the pin — port the files as they are and
  cite both shas in the module docs so the banked riders close.
- **Freshness probe at lane start** (ledger §2): checkout on **`bugfix`**
  (do NOT switch it), tree clean, both `log` ranges empty. **Any mismatch:
  STOP and report.**
- **Regen rule: PIN REQUIRED at `f699da6f6`**; pin path
  **`/tmp/qt-v4-pin-p49k2-f699da6f6`**; every regen, every jest run, and
  the NEW fixture build through `recipe_sweep.py --v4 <pin>` (the fixture
  builder runs v4's REAL repositories from the pin, `build-characters-
  fixture.ts:29-35` is the invocation shape). Oracle `node_modules` LIVE
  (`zod` 4.5.4 — `wizardRequestSchema` uses `z.uuid()`, the Zod-4 spelling;
  transcribe from Zod 4's own regex as P4.62 did).
- **Sibling lanes:** P4.9K1 (the OTHER server lane, stacked beside you on
  K0 — fences on the shared Rust files; `characters_routes.rs` by FUNCTION,
  yours is `characters_collection_post`; `system_data_routes.rs` and
  `query_param_semantics_equivalence` are YOURS); P4.9K3 (the wizard's SPA
  half — bound by §B.3, gated on `P49K2_SERVER_LANDED`) and P4.9K4 (the AI
  import's SPA half — same gate); P4.80, P4.81 independent.
- **Worktree note:** `git worktree add <path> -b claude/p4-9k2-generators-
  wizard-import-<hash> <K0 branch tip>`; edit inside it;
  `CARGO_INCREMENTAL=0`.

## Mandate

Port `lib/services/character-wizard.service.ts` (1,213 lines: the
non-streaming `runCharacterWizard` `:694` AND the streaming
`runCharacterWizardStreaming` `:954` — v4 duplicates ~250 lines between
them; port both faithfully, share what v4 shares and record where v4 itself
diverges between the twins) and `lib/services/ai-import.service.ts` (1,262
lines: `runAIImportStreaming` `:788` + the pure `assembleWardrobeItems`
`:426`, `assembleQtapExport` `:462`, `restampStructuralFields` `:696`) as
§B.3's three verbs with ONE host-composed driver for both (they need the
same bundle: completion provider, vision/describe for image sources, the
files store for source files and document/image inputs, profile + api-key
resolution), the REST arms (`POST /api/v1/characters?action=ai-wizard`
JSON + `?action=ai-wizard-stream` SSE on `characters_collection_post`;
`POST /api/v1/system/tools?action=ai-import-stream` SSE on
`system_tools_post`, retiring `ai-import-stream` from the
`UNSERVED_KNOWN_ACTIONS` divergence in `query_param_semantics_equivalence`),
the NEW committed `character-generators-{main,mount}.db` fixture pair, and a
tier-3 differential per service. Must NOT touch: `generators/{optimizer,
external_prompt,rename,refresh_archive}.rs`, `characters_action_post`,
`characters-{main,mount}.db`, `apps/web/**`.

## Survey-verified starting points (2026-09-07 @ v4 `f699da6f6` — do not re-derive)

### The wizard (`character-wizard.service.ts`)

- Types `:43-115` (`WizardRequest` — `primaryProfileId`, `visionProfileId?`,
  `sourceType` ∈ existing|upload|gallery|document|skip, `imageId?`,
  `documentId?`, `characterName`, `existingData?{…}`, `background`,
  `fieldsToGenerate[]` of the 13 field names, `characterId?`;
  `GeneratedPhysicalDescription` (7 strings); `WizardResult {success,
  generated, errors?}`; the event union `start|field_start|field_complete|
  field_error|done` with `{field?, snippet?, fullContent?, errors?, error?}`).
- Prompts `:118-305`: `FIELD_PROMPTS` (13 keys, several built from K0's
  `field_semantics` exports), `PROPERTIES_PROMPT` (`:225`, `${PROPERTIES_
  SEMANTICS}…`), `HEAD_AND_SHOULDERS_PHYSICAL_PROMPT` (`:244`, "maximum 500
  characters"), `buildContextPrompt` (`:306`). v4's own parity pins:
  `__tests__/unit/lib/services/character-wizard-prompts.test.ts` (the
  vantage-point rules per prompt — transcribe as unit pins on the constants).
- Call sites: `generateField` (`:387`, one completion — `[system context,
  user fieldPrompt]`, `maxTokens` default 500), `generateImageDescription`
  (`:463`, the vision call — through `profileSupportsMimeType` and the
  optional `visionProfileId` fallback, throws `Vision profile required for
  image analysis` / `Vision profile not found` / `Image not found`),
  `generatePhysicalDescriptions` (`:574`), `generateWardrobeItems` (`:631`),
  `generateProperties` (`:661`, K0's `generated_properties` +
  `sanitize_pronouns`). The non-streaming runner `:694-952` and the
  streaming runner `:954-1213` — profile (`Primary profile not found`) →
  api key → image or document source (`repos.files.findById` `:735/:775`,
  `:1000/:1040`; how the document's TEXT is read is the survey item — find
  the storage read and its size cap) → `name` first when requested (`:808`,
  `:1074`) → per-field loop with `field_start`/`field_complete{snippet}`/
  `field_error` → `physicalDescription` (`:846/:1115`) → `wardrobeItems`
  (`:857/:1128`) → `properties` (`:868/:1141`) → the raw-content fields
  (`:884/:1163`) → `done {fullContent, errors}`. Measure what the streaming
  twin emits when the runner THROWS before any field (the terminal shape
  §B.3 must carry).
- Route: `app/api/v1/characters/handlers/post.ts` — `wizardRequestSchema`
  `:89-136` (transcribe whole, incl. `existingData.scenarios[]`'s
  `{id,title,content}` and `pronouns` nullable), `CHARACTERS_POST_ACTIONS`
  `:139` (`ai-wizard, ai-wizard-stream, import, quick-create, reset-builtins`
  — v5 serves `import` + `reset-builtins` and refuses the rest,
  `characters_routes.rs:529-537`), `handleAiWizard` `:518-534` (parse →
  `logger.info('[Characters v1] AI Wizard starting', {userId, characterName,
  fieldsToGenerate, sourceType})` → `runCharacterWizard` → the result as
  JSON), `handleAiWizardStream` `:536-575` (parse → `…starting (streaming)`
  → the SSE stream). A Zod failure: the route's generic 400 (measure).
- Consumers you do NOT port: `lib/background-jobs/handlers/character-
  headshoulders-backfill.ts:34` imports the wizard's physical-description
  generator — v5's backfill handler (P4.D116 territory) is out of scope;
  record the import as a NAMED follow-up if the v5 handler carries a
  narrowed copy.

### The AI import (`ai-import.service.ts`)

- Types `:46-170` (`AIImportRequest`, the 12 step names, the event union
  `start|step_start|step_complete|step_error|done` with `{step?, snippet?,
  result?, stepResults?, errors?, error?}`, `AIImportStepResults` keyed by
  step). `CHARACTER_BASICS_PROMPT` `:170` (`${FULL_FIELD_SEMANTICS}…`) and
  the per-step prompts that follow; v4's pins `__tests__/unit/lib/services/
  ai-import-character-basics.test.ts` (the four vantage-point rules +
  `assembleQtapExport`'s identity handling) and `ai-import.service.test.ts`
  (`stripCodeFences`/`parseLLMJson` — K0 owns those; the rows are its
  corpus).
- `runAIImportStreaming` `:788-1160`: `start` → profile (`Connection profile
  not found`) → api key → provider → `llmOpts` (`profileParams(profile)`
  — the P4.D80 consolidation) → `shouldRunStep` (`:842`: in
  `regenerateSteps` ⇒ run; present in `existingResult` ⇒ skip) → the
  source context (files' text + `sourceText`; `SOURCE_ANALYSIS_THRESHOLD`
  gates the `analyzing` step `:866`) → the step chain `:866-1095`
  (`character_basics`, `first_message`, `system_prompts`,
  `physical_descriptions`, `wardrobe_items`, `pronouns`, `memories` (if
  `includeMemories`), `chats` (if `includeChats`)), each `step_start` →
  call → parse → `step_complete{snippet}` or `step_error{error}` (nine
  `step_error` sites `:885-1095`) → `assembly` (`:1101-1116`:
  `assembleQtapExport(stepResults, includeMemories, includeChats,
  appVersion)` — `crypto.randomUUID()` for character/user ids `:469-470`,
  `new Date().toISOString()` — both fixed on the harness clock; the
  `appVersion` is v4's package version — measure what v5 stamps and pin
  the divergence if it cannot be v4's) → `validation` / `repair` (read
  `:1116-1160`: what validates — the import preview? — and what `repair`
  re-asks the model) → `done {result, stepResults, errors}`.
- Route: `app/api/v1/system/tools/route.ts:1190-1260` `handleAIImportStream`
  (the hand-rolled body read `:1196-1204` with its defaults; the two 400s
  `:1206-1212`; `logger.info('[System Tools v1] AI Import stream starting',
  {userId, profileId, sourceFileCount, hasSourceText, includeMemories,
  includeChats})`; the SSE stream; the catch `logger.error('[System Tools
  v1] AI Import stream failed', {userId, error})` + `serverError(message)`).
  The client's final step is `POST /api/v1/system/tools?action=import-execute`
  with `{exportData: result, options: {conflictStrategy: 'duplicate',
  importMemories: true}}` (`useAIImport.ts:332-340`) — ALREADY served in v5
  (`SystemImportExecute`, `api/system_qtap.rs:208`); you port nothing there
  but your differential's `result` must be a body that verb accepts
  (a positive arm: feed one case's `result` to v5's `import_execute` and
  assert the character lands — the cross-family proof that the assembled
  export is importable).

### The v5 side today

- `system_data_routes.rs:63-108`: `TOOLS_POST_ACTIONS` carries
  `ai-import-stream` in the 400's tail and `tools_unknown_action` refuses it
  as "not served on this route; it rides POST /api/dispatch" — that
  refusal RETIRES for `ai-import-stream` (the row in
  `query_param_semantics_equivalence`'s `UNSERVED_KNOWN_ACTIONS` moves to
  a served arm; `capabilities-report-progress` stays). `system_tools_post`
  is at `:395`.
- `characters_routes.rs:529-537`: the collection POST refuses everything but
  `import`/`reset-builtins` with a loud sentence — your two arms join the
  match; the sentence names the served set.
- Seams: `CompletionProvider` (canned key for the differential), the
  describe/vision seam (`chat_media::ImageDescribeDriver`, `engine.rs:292`;
  `image_transport` predicates for `profileSupportsMimeType` — P4.D106's
  registry→static→unknown-true pair), the files store read (`StorageBackend`
  + the text-attachment reader P4.D154 built for user-side attachments —
  find the shared "read a file's text with a cap" helper before writing a
  new one), `resolve_connection_profile_api_key`, `profile_params` (P4.D80),
  `services::quilltap_import::execute_import` (`mod.rs:954`).
- The driver: ONE trait in `api/generators_wizard.rs` serving both verbs
  (the host composes the bundle once), impl in
  `quilltap-host/src/generators_wizard_driver.rs`, the `EngineAssembly`
  field in your fence, `None` → named refusal.

## Tiered deliverables

### Tier 1 — must land

1. **`generators::wizard`** — types, the 13 prompts + `buildContextPrompt`
   byte-exact (unit pins from v4's prompts suite), the five generators,
   BOTH runners (the shared body factored ONCE where v4's two copies are
   identical; every place they differ recorded with line pairs), the
   streaming events through K0's emitter.
2. **`generators::ai_import`** — types, prompts, `shouldRunStep`, the step
   chain, `assemble_wardrobe_items`, `assemble_qtap_export`,
   `restamp_structural_fields`, `validation`/`repair` as measured, the
   events.
3. **The NEW committed fixture** `character-generators-{main,mount}.db` via
   `harness/oracle/fixtures/build-character-generators-fixture.ts` (v4's
   REAL repositories at the pin; pinned ids): one user, an OPENAI_COMPATIBLE
   profile (+ key) that does NOT accept images and a second VISION-capable
   profile (a provider the static table marks image-capable), ONE text
   `files` row (a `.md`/`.txt` with known bytes — the wizard's `document`
   source and the import's `sourceFileIds`), one IMAGE `files` row (the
   `gallery`/`upload` source), one existing character with the
   `existingData` shape (scenarios, pronouns, aliases), NO help docs. Trim
   to the question (`a-new-fixture-built-from-a-grown-shared-fixture`).
4. **The three verbs** (§B.3) — `characterWizard`, `characterWizardStream`,
   `aiImportStream` — types fence, handlers in `api/generators_wizard.rs`
   with the Zod/hand-rolled parses and v4's sentences BEFORE any frame,
   engine arms, the driver trait + assembly field.
5. **The host driver** wired LIVE (⚠ real spend on a real click — CHANGELOG
   says so).
6. **The REST arms**: `ai-wizard` (JSON) + `ai-wizard-stream` (SSE via K0's
   re-framer) on `characters_collection_post`; `ai-import-stream` (SSE) on
   `system_tools_post`; the `UNSERVED_KNOWN_ACTIONS` row retired for
   `ai-import-stream` with the family re-run by name (red-first: the
   pre-change arm must go red when the sentence changes).
7. **Two differentials** (below), mutation-proven.

### Tier 2 — should land

8. The cross-family importability proof (the `result` → `import_execute`
   arm above).
9. Both SSE edges byte-proven against the oracle's recorded event
   sequences (the K1 item-9 shape).
10. `llm_logs` rows per call if v4's plugin logs them (`AI_WIZARD` /
    `AI_IMPORT` purposes — find v4's `logLLMCall` sites; pin counts).

### Tier 3 — explicit refusal-arm deferrals (loud, typed, never silent)

- The head-and-shoulders backfill handler's wizard import (out of scope —
  NAMED follow-up with the v5 handler's line).
- Chat generation inside the import (`includeChats` → the `chats` step
  `:1077`) ports WHOLE; if a downstream v5 twin is missing (e.g. a
  transcript writer the assembled export needs), the arm answers
  `step_error` with a NAMED v5 sentence recorded as a divergence — never
  a silent `[]`.

## The differential requirement (restated)

- **`character_wizard_tier3_equivalence`** — jest oracle driving v4's REAL
  `runCharacterWizard` + `runCharacterWizardStreaming` over the new fixture,
  `createLLMProvider` → canned `sendMessage` recording `provider|model|
  temperature|messages` (the `help-chat-orchestrator-tier3.test.ts` header
  is the mechanism); `describeImage`/vision canned per case; the fixed
  clock. Corpus (≥ 14 cases): `skip` source with every field; `existing`
  with `existingData`; `document`; `gallery` on the non-vision primary with
  `visionProfileId` (the fallback), without it (`Vision profile required`),
  with a missing image (`Image not found`); `name` requested (measure the
  order — name first); `properties` (pronouns + aliases through K0's
  helpers); `physicalDescription` + `wardrobeItems`; a field whose call
  throws (`field_error`, the rest continue); the non-streaming twin on the
  same inputs (its `WizardResult` vs the streaming `done.fullContent` —
  equal where v4 makes them equal, recorded where not); a Zod refusal.
  Comparands: the recorded keys (prompts), the event sequence, the result.
- **`ai_import_tier3_equivalence`** — same mechanism over v4's REAL
  `runAIImportStreaming` (≥ 12 cases): `sourceText` only; `sourceFileIds`
  only; both; over the analysis threshold (the `analyzing` step fires);
  `includeMemories`/`includeChats` on/off; `existingResult` + `regenerateSteps`
  (skip vs re-run per step); a step whose answer is unparseable
  (`step_error`, chain continues); the two route 400s; a missing profile.
  Comparands: keys, events, and the `done.result` QuilltapExport with its
  minted uuids/timestamps normalized (the `<minted-N>` remap; the
  `appVersion` stamp per the survey's ruling).

Recipes per `recipe-header-conventions`; the fixture builder's invocation
folded into both regen stages (`A /tmp fixture the recipe READS, the recipe
must BUILD` does not apply — the pair is COMMITTED; the builder header
carries the regen line and the sweep driver warns on tracked-fixture
writes).

## Fixtures

Delivers `crates/quilltap-web/tests/fixtures/character-generators-{main,
mount}.db` + the builder + the two corpus JSONs + two oracle cases. Reads
nothing else committed. Regenerates nothing that exists.

## §B — the generators + chat-delete wire contract (BINDING — verbatim-identical in every order of this round)

Every name below is a **dispatch verb** (`Request` variant in
`crates/quilltap-core/src/api/types.rs`, `#[serde(tag = "type", rename_all =
"camelCase")]`) or an **Event** kind. Field names are the v4 JSON body keys
verbatim (camelCase on the wire; `snake_case` in Rust under
`#[serde(rename_all = "camelCase")]`). Response bodies are v4's route bodies
byte-for-byte unless a line says otherwise. The unifier diffs this block
name-for-name against `api/types.rs` and every SPA wire file at `/unify`.

### B.1 Streaming shape (the one mechanism for every generator)

v4 streams each generator as an HTTP SSE response of `data: <JSON>\n\n`
frames (`handleOptimizeStream`, `handleAiWizardStream`,
`handleAIImportStream` — all three are the same nine-line `ReadableStream`).
v5's boundary streams **only on the Event channel** (`api-boundary.md`), so:

- Each streaming verb carries a client-minted **`progressId: string`**
  (`crypto.randomUUID()` client-side; the `CreationProgressFrame` precedent
  at `core-contract.ts:4187-4220` / `services/creation_progress.rs`).
- Every v4 `onProgress(event)` becomes ONE Event:
  `{ "type": "generatorProgress", "progressId": <id>, "generator": <G>,
  "event": <the v4 progress event object, key order and bytes verbatim> }`
  where `<G>` ∈ `"optimizer" | "wizard" | "aiImport"`.
- The dispatch call resolves when the run ends server-side with
  `{ "terminal": <the LAST event v4 would emit, verbatim> }` — for the
  optimizer that is the `done` or `error` event; for the wizard `done`; for
  the AI import `done`. A refusal BEFORE the run starts (v4's Zod parse /
  route-level `badRequest`) is a dispatch `CoreError` (`badRequest`,
  message = v4's sentence, `details` = v4's Zod issues where v4 answers
  them) and emits NO frame.
- The REST SSE edges (Tier 2 in the server lanes) re-frame the same Events
  as `data: <JSON.stringify(event)>\n\n` with headers `Content-Type:
  text/event-stream`, `Cache-Control: no-cache`, `Connection: keep-alive`,
  and close after the terminal frame — byte-identical to v4's stream.

### B.2 Verbs (K1 — the per-character trio + refresh-archive)

- `characterOptimize { characterId, progressId, connectionProfileId,
  maxMemories?, searchQuery?, useSemanticSearch?, sinceDate?, beforeDate?,
  outputMode? }` — v4 `optimizeStreamSchema` (`app/api/v1/characters/[id]/
  handlers/post.ts:60-68`; defaults 30 / '' / true / null / null / 'apply',
  bounds `maxMemories` int 5..200, `searchQuery` max 500, `outputMode`
  enum). Terminal = v4's `done` (`{type, analysis, suggestions,
  suggestionsFilePath?}`) or `error` (`{type, error}`) event.
- `characterGenerateExternalPrompt { characterId, connectionProfileId,
  systemPromptId, scenarioId?, maxTokens }` — v4
  `generateExternalPromptSchema` (`post.ts:70-75`; uuids; `maxTokens` int
  1000..20000). Response `{ prompt, tokensUsed }`; a failed generation is a
  500 whose message is `result.error || 'Generation failed'` (v4
  `post.ts:328-330`).
- `characterRename { characterId, primaryRename?, additionalReplacements?,
  dryRun? }` — v4 `renameSchema` (`post.ts:77-87`; pairs `{oldValue,
  newValue, caseSensitive}` with the two `min(1)` sentences `Find value is
  required` / `Replace value is required`; `caseSensitive` default false;
  `additionalReplacements` default `[]`; `dryRun` default true; the
  route-level 400 when neither a primary nor an additional pair is present —
  `post.ts:429-431`, sentence verbatim). Response = v4's
  `RenamePreviewResponse` (`{characterId, characterName, dryRun,
  replacements[], summary{characterFields, physicalDescriptions, memories,
  chatTitles, chatMessages, total}}`); the catch arm is 500 `Failed to
  process rename request`.
- `characterRefreshArchive { characterId }` — v4 `post.ts:335-…`. Response
  `{ queued: <n> }`.

### B.3 Verbs (K2 — the wizard pair + the AI import)

- `characterWizard { …WizardRequest }` — the NON-streaming twin (v4
  `?action=ai-wizard` → `runCharacterWizard`). Body = v4 `wizardRequestSchema`
  (`app/api/v1/characters/handlers/post.ts:89-136`) verbatim. Response =
  v4's `WizardResult` `{ success, generated, errors? }`.
- `characterWizardStream { progressId, …WizardRequest }` — the streaming
  twin (`runCharacterWizardStreaming`). Terminal = v4's `done` event
  (`{type, fullContent, errors?}` — measure the throw arm's shape).
- `aiImportStream { progressId, profileId, sourceFileIds, sourceText,
  includeMemories, includeChats, existingResult?, regenerateSteps? }` — v4
  `handleAIImportStream`'s hand-rolled body read (`app/api/v1/system/tools/
  route.ts:1196-1212`: `sourceFileIds || []`, `sourceText || ''`,
  `includeMemories ?? true`, `includeChats ?? false`; the two 400 sentences
  `Missing required field: profileId` and `Must provide at least one source
  file or source text` BEFORE any frame). Terminal = v4's `done` event
  (`{type, result, stepResults, errors?}`); the route's catch arm is a 500
  whose message is the thrown error's (`route.ts:1254-1260`).

### B.4 Verb (P4.80 — chat delete)

- `chatDelete { chatId }` — v4 `handleDelete` with NO action
  (`app/api/v1/chats/[id]/handlers/delete.ts:48-63`): 404 `Chat not found`
  (v4 `notFound('Chat')`), else `repos.chats.delete(chatId)` →
  `{ "success": true }`; the catch arm is 500 `Failed to delete chat`.
  Engine: `services::conversation_summary_vault_bridge::
  delete_conversation_with_vault_sweep(db, chat_id, true)` — ALREADY ported,
  zero callers today.

### B.5 The Event kind (K0 owns the variant; K1/K2 emit; K3/K4 consume)

```
Event::GeneratorProgress { progress_id: String, generator: GeneratorKind, event: serde_json::Value }
  → wire: { "type": "generatorProgress", "progressId", "generator", "event" }
GeneratorKind = Optimizer | Wizard | AiImport  → "optimizer" | "wizard" | "aiImport"
```

The SPA subscribes to `CoreClient.events$`, filters on `type ===
'generatorProgress' && progressId === mine`, and folds `event` exactly as
v4's hook folds each parsed SSE line (`useCharacterOptimizer.ts`,
`useAIWizard.ts:308-…`, `useAIImport.ts`). No SPA lane reads the REST SSE
edges.

### B.6 What every order must NOT invent

No verb, field, Event kind, or response key beyond this block. A lane that
finds this block wrong (a v4 field the survey missed, a shape v4 measures
differently) STOPs on that item, records the measurement in its lane record,
and delivers everything else — the unifier reconciles contracts, lanes do
not.

## §S — the round-wide meeting points (BINDING — identical in every order of this round)

1. **Stacking.** P4.9K0 (the substrate) runs FIRST and alone. P4.9K1 and
   P4.9K2 branch their worktrees FROM P4.9K0's lane branch tip (the
   `P4.D119→P4.D120` stacked precedent), never from `main`; the unifier
   cherry-picks K0 first. P4.9K3, P4.9K4, P4.80 and P4.81 branch from `main`
   and are independent of K0.
2. **Fenced-append shared files.** `crates/quilltap-core/src/api/types.rs`,
   `crates/quilltap-core/src/api/engine.rs`, `crates/quilltap-core/src/lib.rs`
   (module lines), `crates/quilltap-host/src/host.rs`,
   `crates/quilltap-host/src/spine.rs`, `crates/quilltap-web/src/lib.rs`
   (module + route lines): each Rust lane appends ONLY inside its own fence
   (`// === P4.9K1 ===` … `// === end P4.9K1 ===`, opened once per file, new
   variants/arms/registrations at the END of the relevant block, never
   interleaved). P4.81 is the one lane that EDITS EXISTING lines in
   `spine.rs` — exactly the two sites its order names (`map_create_error`'s
   `details: None` and the `CreationProgressEmitter::from_id` call's
   position) and nothing else in that file.
   `crates/quilltap-web/src/characters_routes.rs` is fenced BY FUNCTION:
   `characters_action_post` (the per-id POST dispatcher) is P4.9K1's;
   `characters_collection_post` (the collection POST dispatcher) is
   P4.9K2's; neither touches the other's function or the file's header.
   `crates/quilltap-web/src/system_data_routes.rs` is P4.9K2's alone.
   `crates/quilltap-web/src/chats_routes.rs` is P4.80's alone.
   `Cargo.toml`/`Cargo.lock` at the workspace root: version lines only; a
   dependency add is a STOP-and-flag in the lane record.
3. **Frozen files.** `docs/developer/porting/drift-ledger.md` (lanes never
   write it); `apps/web/src/app/core/core-contract.ts` (folded at unify —
   every SPA lane declares its §B DTOs in its OWN wire file, verbatim from
   §B; identical duplicate `interface` declarations across lanes are
   expected and folded by the unifier, who runs the name-for-name diff);
   `apps/web/src/app/screens/characters/characters.api.ts` (each SPA lane
   adds its verbs in its own new `*.api.ts` beside its feature);
   `apps/web/src/app/shell/shell.ts`; `apps/web/src/app/workspace/
   workspace-contract.ts` (no new tab kinds this round — every surface is a
   modal or a tab INSIDE an existing screen, as in v4).
4. **Version bumps.** P4.9K0: `quilltap-core`, `quilltap-harness`,
   `quilltap-web`. P4.9K1 and P4.9K2: `quilltap-core`, `quilltap-harness`,
   `quilltap-web`, `quilltap-host`. P4.9K3 and P4.9K4: `apps/web/package.json`.
   P4.80: `quilltap-core`, `quilltap-harness`, `quilltap-web`,
   `apps/web/package.json`. P4.81: `quilltap-host`, `quilltap-core`,
   `quilltap-harness`, `apps/web/package.json`. Per commit. The unifier
   RECOUNTS as base + total bumps — identical bumps merge silently
   (`commit-checklist`; the playbook's silent-auto-merge trap).
5. **e2e.** Port 4319 is repo-wide — ONE Playwright at a time across all
   lanes. P4.9K3, P4.9K4 and P4.80 add NEW spec files only; beats that need
   a server verb another lane ships are guarded ACTIVATE-AT-UNIFY on a NAMED
   per-lane constant (`P49K1_SERVER_LANDED`, `P49K2_SERVER_LANDED`), never a
   capability probe (`round-plan-takeaways` §Planning). P4.81 may edit
   EXISTING specs (its item 7 restores what a spec changed). The Rust lanes
   run no Playwright: `git diff main -- apps/web/` empty for K0/K1/K2.
6. **Append-only for everyone:** `docs/CHANGELOG.md`,
   `docs/developer/porting/status-log.md`.
7. **The regen rule for every lane:** PIN REQUIRED at **`f699da6f6`** — a
   LANE-UNIQUE pin path (`/tmp/qt-v4-pin-<order>-f699da6f6`, all three
   symlink classes per ledger §5.1), every regen and every fixture build
   through `harness/tools/recipe_sweep.py --v4 <pin> --run <family>`; the
   v4 checkout stays on `bugfix`, untouched (ask the human, never switch
   it). Committed recipe headers say `cd ~/source/quilltap-server` and never
   name a `/tmp` pin (`recipe-header-conventions`). The oracle
   `node_modules` are the LIVE tree (`zod` 4.5.4 — the `zod_version_guard`
   fails the gate if it moves; every Zod-transcribing schema in §B uses the
   `jsstr::zod_len_*` helpers for string lengths).
8. **Fixture ownership.** `characters-{main,mount}.db` is READ by K1 and by
   K4's beats and REGENERATED BY NOBODY this round. K2 delivers the NEW
   committed `character-generators-{main,mount}.db` pair (its own builder);
   P4.80 delivers `chat-delete-{main,mount}.db` if the survey shows the
   committed pairs cannot express its cascade. K3/K4 seed through the
   running server's verbs (`seed-help-fixture.ts` precedent), never SQL.
9. **The bug-119 obligation** (drift-ledger §3, `15573c3a1`, `ORDERED(P4.9K1)`):
   K1 ports the POST-fix optimizer shape — `coerceSuggestionArray`'s ordered
   key list byte-for-byte, the `runSubStep`/`runSubStepCore` containment,
   the two log lines — and never transcribes the pre-fix inline `.filter`.

## Ownership (BINDING — identical table in every order of this round)

| Lane | Owns (edit freely) | Fenced-append (§S.2) | Must not touch |
| --- | --- | --- | --- |
| **P4.9K0** | NEW `crates/quilltap-core/src/generators/{mod,llm_json,field_semantics,generated_properties,sanitize_pronouns}.rs`; NEW `crates/quilltap-core/src/services/generator_progress.rs`; NEW `crates/quilltap-web/src/generator_sse.rs`; NEW harness family `crates/quilltap-harness/tests/generators_leaf_equivalence.rs` + `harness/oracle/cases/generators-leaf.ts` + its fixture JSON; this order's header | `api/types.rs` (the `Event::GeneratorProgress` variant + `GeneratorKind`), `core/src/lib.rs`, `web/src/lib.rs` (module line) | everything else |
| **P4.9K1** | NEW `crates/quilltap-core/src/generators/{optimizer,external_prompt,rename,refresh_archive}.rs`; NEW `crates/quilltap-core/src/api/generators_detail.rs`; `crates/quilltap-web/src/characters_routes.rs::characters_action_post` (that function only); NEW harness families `character_optimizer_tier3_equivalence`, `external_prompt_tier3_equivalence`, `character_rename_equivalence` (+ `refresh_archive` arms) with their oracle cases + fixtures JSON; a new host driver file `crates/quilltap-host/src/generators_detail_driver.rs`; this order's header | `api/types.rs`, `api/engine.rs`, `host/host.rs`, `host/spine.rs`, `web/src/lib.rs` | `generators/{wizard,ai_import}.rs`; `characters_collection_post`; `system_data_routes.rs`; `query_param_semantics_equivalence` (record, unifier adjusts); `character-generators-*.db`; `apps/web/**` |
| **P4.9K2** | NEW `crates/quilltap-core/src/generators/{wizard,ai_import}.rs`; NEW `crates/quilltap-core/src/api/generators_wizard.rs`; `crates/quilltap-web/src/characters_routes.rs::characters_collection_post` (that function only); `crates/quilltap-web/src/system_data_routes.rs`; `crates/quilltap-web/tests/query_param_semantics_equivalence.rs` + its oracle case (the `ai-import-stream` row); NEW committed `crates/quilltap-web/tests/fixtures/character-generators-{main,mount}.db` + `harness/oracle/fixtures/build-character-generators-fixture.ts`; NEW harness families `character_wizard_tier3_equivalence`, `ai_import_tier3_equivalence` + oracle cases + fixtures JSON; a new host driver file `crates/quilltap-host/src/generators_wizard_driver.rs`; this order's header | `api/types.rs`, `api/engine.rs`, `host/host.rs`, `host/spine.rs`, `web/src/lib.rs` | `generators/{optimizer,external_prompt,rename,refresh_archive}.rs`; `characters_action_post`; `characters-{main,mount}.db`; `apps/web/**` |
| **P4.9K3** | NEW `apps/web/src/app/screens/characters/generators/{wizard,rename,prompts-editor}/**` (+ its own `edit-generators.api.ts` + wire file); `apps/web/src/app/screens/characters/edit/**`; `apps/web/src/app/screens/characters/new/new-character.ts`; NEW e2e specs `character-wizard-flow.spec.ts`, `character-rename-flow.spec.ts`; `apps/web/package.json`; this order's header | — | `crates/**`; `screens/characters/view/**`; `screens/characters/list/**`; `chat/cast/**`; `core-contract.ts`; `characters.api.ts`; any existing spec |
| **P4.9K4** | NEW `apps/web/src/app/screens/characters/generators/{optimizer,external-prompt,ai-import}/**` (+ its own `detail-generators.api.ts` + wire file); `apps/web/src/app/screens/characters/view/character-detail.ts`, `view/character-header.ts`, `view/tabs/details-tab.ts` (the reverse-user entry only); `apps/web/src/app/screens/characters/list/characters-list.ts`; `apps/web/src/app/chat/cast/add-character-dialog.ts`; NEW e2e specs `character-optimizer-flow.spec.ts`, `character-external-prompt-flow.spec.ts`, `ai-import-flow.spec.ts`; `apps/web/package.json`; this order's header | — | `crates/**`; `screens/characters/edit/**`; `new/**`; `view/tabs/conversations-tab.ts`; `view/tabs/character-conversation-card.ts`; `core-contract.ts`; `characters.api.ts`; any existing spec |
| **P4.80** | NEW `crates/quilltap-core/src/api/chat_delete.rs`; `crates/quilltap-web/src/chats_routes.rs`; NEW harness family `chat_delete_equivalence` + oracle case (+ the optional `chat-delete-{main,mount}.db` pair and its builder); `apps/web/src/app/screens/salon/chat-card.ts`, `salon-list.ts`; `apps/web/src/app/screens/characters/view/tabs/conversations-tab.ts`, `view/tabs/character-conversation-card.ts`; its own `chat-delete.api.ts` + wire file; NEW e2e spec `chat-delete-flow.spec.ts`; `apps/web/package.json`; this order's header | `api/types.rs`, `api/engine.rs`, `web/src/lib.rs` (the DELETE registration on the existing `/api/v1/chats/{id}` route line — ONE `.delete(...)` chained onto it) | `generators/**`; `characters_routes.rs`; `screens/characters/{edit,new,list}/**`, `view/character-detail.ts`, `view/character-header.ts`; `core-contract.ts` |
| **P4.81** | `crates/quilltap-host/src/spine.rs` (the TWO named existing sites only); `crates/quilltap-harness/tests/chat_create_capstone_equivalence.rs` (retire the `p4_78_host_wire_details_carry_is_deferred` tripwire → its positive twin); the orchestrator file that holds `execute_turn_chain` (three log lines only); `harness/oracle/fixtures/help-chat-orchestrator-tier3.json` + `harness/oracle/cases/help-chat-orchestrator-tier3.test.ts` (the `text_block_turn` case only); `docs/v4/WINDOWS.md` + `docs/v4/help/database-protection.md` (deletion); `apps/web/README.md`; `apps/web/src/app/screens/salon/salon-conversation.ts` + its spec (the composer predicate only); one EXISTING e2e spec (item 7's restore); NEW e2e beat file `composer-active-seat.spec.ts`; this order's header | — | `generators/**`; `api/types.rs`; `api/engine.rs`; every file in another lane's Owns column |

Everyone: `docs/CHANGELOG.md` + `status-log.md` append-only; the drift
ledger frozen; `core-contract.ts` / `characters.api.ts` / `shell.ts` /
`workspace-contract.ts` frozen.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; clippy both feature sets; `cargo build
   --workspace`; the release build.
2. `cargo test --workspace` (full log + sentinel) with K0's leaf var + your
   two families' oracle vars + `QT_FIXTURE_CHARACTER_GENERATORS_{MAIN,
   MOUNT}` + the `query_param_semantics` var; grep `SKIP:` — zero for
   those; by-name `--nocapture` runs showing counts.
3. Each family by NAME through `recipe_sweep.py --v4
   /tmp/qt-v4-pin-p49k2-f699da6f6 --run <family>` incl.
   `query_param_semantics_equivalence` (its oracle regenerated at the pin)
   and K0's leaf family (neutrality); `--show` each new header; the
   fixture builder's run recorded with `git status` showing exactly the
   two new `.db` files (`.db-journal` strays deleted).
4. Mutation proofs named in the lane record: a prompt byte; the
   `shouldRunStep` skip inverted; the `field_error` containment removed;
   the vision fallback removed; the `assembly` uuid minting made
   deterministic on ONE side (the remap must still hold — proves the
   normalizer is not vacuous); the `analyzing` threshold moved.
5. `git diff main -- apps/web/` empty; `git diff <K0 tip> -- crates/
   quilltap-core/src/generators/{llm_json,field_semantics,
   generated_properties,sanitize_pronouns}.rs` empty.
6. Commits per `.claude/commands/commit.md` (core + harness + web + host
   bumps; CHANGELOG every commit, the LIVE-spend note); the lane record
   appended to `status-log.md` (the twin-runner divergence list, the
   `appVersion` ruling, the backfill follow-up, 💸 a live wizard + a live
   Summon on the Friday copy); this order's status header updated.
