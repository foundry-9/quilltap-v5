# Work order P4.D142 — `qt-range` (v4 `5f56f7a7d`) + finding #107, the inline-host family

**Status:** CLOSED — UNIFIED on main at the round-2 drift catch-up
unification (2026-09-01). Tier 1 whole (`.qt-range` + tokens byte-identical,
all twelve v5 range hosts adopted, finding #107's `.qt-markdown-field` rule)
and Tier 2 at the ordered NARROW scope (the host-class guard). §3 at
unification fixed: the guard's `COMPONENT_HEADER` regex missed one-line
`@Component({ …, template: '' })` headers (two spec hosts) and could
attribute the NEXT component's `host:` block to the wrong site — bounded on
`\btemplate:`; **the ordered spec/self-test had not landed** — a
`--self-test` mode with five red-first shapes now runs ahead of `npm test`
and `npm run lint`; the inlined `qt-markdown-field` frame names the three
utility rules it copies. Recorded follow-ups: the dozen residue hosts with
no display rule at all (named in the script header); the v4-faithful class
SUFFIX bytes are unpinned on six sliders; `definedClasses()` counts a
`.qt-*` spelled inside a CSS comment (the `_variables.css` RANGE banner does
exactly that — documented at the script, never "fixed" by rewording v4's
text).

## Preamble

- **v4 oracle baseline: `7fb668263`** (drift-ledger §1, probe re-run
  2026-09-01). **Targets:** v4 commit **`5f56f7a7d`** (the `.qt-range`
  class + 15-slider adoption) and **dogfood finding #107** (2026-08-29,
  recorded not fixed): `qt-markdown-field` is an Angular custom-element
  host class with NO CSS rule anywhere — the THIRD instance of the
  inline-host family (after #97's `qt-tab-view` and the Almanack walk's
  `qt-entity-tabs`) — plus the guard the findings file proposes.
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 checkout
  on `main`, tree clean, `git log 4622411fd..main` empty and
  `git log 3a76b17df..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED in force round-wide** (ledger §1) — this
  lane regenerates no oracles; its v4 spec is the shipped hunks read by
  sha (`git show 5f56f7a7d -- <path>`), which are pinned by construction.
- **Sibling lanes:** P4.D138/P4.D139 (§B — the LoRA editor's slider uses
  this lane's class), P4.D140, P4.D141 (owns `_chat.css` — this lane must
  NOT touch it), P4.66. §Ownership/§Shared contracts binding, identical.
- **Worktree:** per the `carryout` skill; `npm ci` for node_modules.
  `CHANGELOG`/`status-log.md` append-only. **No Playwright** (P4.66 owns
  port 4319).

## Mandate

Land `.qt-range` with v4's CSS and both load-bearing comments, adopt it on
all twelve v5 sliders, fix #107 (`qt-markdown-field` gets a real rule —
display AND v4's frame), and extend the host-class guard so the
inline-host family is mechanically closed rather than fixed one instance
at a time. Must NOT touch: `_chat.css` (P4.D141's), any sibling-owned
component file, `qt-markdown-field`'s 21 call-site files (the #107 fix is
CSS + the declaring component's spec pin, never call-site edits).

## Survey-verified starting points (2026-09-01, from the shipped hunks — ledger §5.3)

1. **The CSS (verbatim in the survey/hunks).** `.qt-range` →
   `apps/web/src/styles/qt-components/_interactive.css`, insertion point
   **line 317** (after `.qt-radio:focus`, before the TABS banner):
   `@apply focus-visible:outline-none focus-visible:ring-2; @apply
   disabled:cursor-not-allowed; cursor: pointer; accent-color:
   var(--qt-range-accent);` + `.qt-range:focus-visible { --tw-ring-color:
   var(--qt-range-focus-ring); }` — with the full comment block: native
   rendering deliberate (accent-color paints filled track AND thumb;
   `appearance: none` would discard the filled bar in Chrome/Safari) and
   **deliberately no disabled:opacity** (several call sites sit inside
   `opacity-50` wrappers; a second 50% compounds to 25%). Tokens →
   `_variables.css`, insertion point **line 171** (after RADIO, before
   NAVBAR): `--qt-range-accent: var(--color-primary); --qt-range-focus-ring:
   var(--color-ring);` + the full RANGE banner comment ("Do NOT 'improve'
   this into `appearance: none`…"). **Preserve the `focus-visible:`
   (not `focus:`) asymmetry vs checkbox/radio** — a mouse-dragged slider
   must not flash a ring.
2. **The twelve v5 sliders** (all `type="range"` in `apps/web/src` —
   grep-verified; nothing else exists). Old → `qt-range w-full` (+ any
   layout suffix v4 kept):
   - `chat/sidebar/participant-card.ts:241` (`qt-input w-full h-1
     rounded-lg appearance-none cursor-pointer accent-primary`) and
     `:258` (disabled twin, `cursor-not-allowed`) → `qt-range w-full`
   - `memory/housekeeping-dialog.ts:80` and `memory/memory-editor.ts:88`
     (`w-full h-2 qt-bg-muted rounded-lg appearance-none cursor-pointer`)
     → `qt-range w-full`
   - `screens/settings/chat/context-compression-settings.ts:99,131,168`
     (`w-full cursor-pointer`) → `qt-range w-full`
   - `screens/settings/chat/dangerous-content-settings.ts:83`
     (`w-full max-w-xs`) → `qt-range w-full max-w-xs`
   - `screens/settings/memory/memory-dedup-card.ts:46`
     (`w-full accent-primary`) → `qt-range w-full`
   - `screens/settings/providers/profile-modal.ts:416,444` (`w-full`) →
     `qt-range w-full`
   - `screens/settings/system/tasks-queue-card.ts:63`
     (`w-full cursor-pointer mt-2`) → `qt-range w-full mt-2`
   Three v4 hosts have NO v5 counterpart (ExternalPromptDialog and
   CharacterOptimizerModal are unported surfaces; LoraListEditor is
   CREATED this round by P4.D139, which carries the class per §B) —
   record them as deliberately out of scope so a future port picks
   `qt-range` up. Specs select via `input[type="range"]`
   (`participant-card.spec.ts`, `tasks-queue-card.spec.ts`) — the class
   swap is safe, but add one spec pin asserting the class on at least
   one host per idiom (the v4-faithful bytes are the deliverable).
   **`qt-range` needs NO check-qt-classes change** — the guard's
   GUARDED_FAMILIES is `^qt-(bg|text|border|shadow)-` by design.
3. **#107 — the fix.** `apps/web/src/app/editor/markdown-field.ts:77`
   declares `host: { class: 'qt-markdown-field' }`; **no rule for it
   exists anywhere** (grep-verified across styles/, styles.css, public/),
   so the host renders `display: inline` — which is why the formatting
   toolbar overflowed its column on New Chat's scenario field. The survey
   found the defect is DOUBLE: v4's `MarkdownLexicalEditor.tsx:194-206`
   wraps the editor in an explicitly framed block div —
   `rounded-lg border qt-border-default qt-bg-card qt-shadow-sm
   overflow-hidden` + `focus-within:outline-none focus-within:ring-2
   focus-within:ring-ring` — and v5 renders NONE of that (no call site
   supplies it). One rule closes both: `.qt-markdown-field { @apply block
   rounded-lg border qt-border-default qt-bg-card qt-shadow-sm
   overflow-hidden focus-within:outline-none focus-within:ring-2
   focus-within:ring-ring; }` placed in **`_surfaces.css`** next to the
   inline-host family rules (:1146-1174 — `qt-collapsible-card` carries
   the canonical bug-class comment; extend that comment's family list),
   NOT in `_chat.css` (P4.D141's file). Verify against v4's New Chat
   scenario field (`NewChatForm.tsx:606-613` ↔ v5
   `new-chat-form.ts:233-239`) that the frame matches; `minHeight` rides
   the existing inputs. Pin with the host-class spec idiom
   (`standalone-document-view.spec.ts:94-106` precedent — jsdom computes
   no layout, so the class/rule pair is the reachable pin).
4. **The guard.** The findings file proposes "every `host: { class:
   'qt-…' }` must have a rule" — the survey measured that this catches
   exactly one live defect and NEITHER historical one (both were fixed by
   element rules / child classes). **Build the guard to the real
   invariant instead:** every Angular component host must have an
   explicit display, satisfied by any of (a) a `qt-` host class with a
   matching rule in `apps/web/src/styles/` (census today: `qt-tooltip-
   anchor` ✓ `_surfaces.css:1100`, `qt-rich-editor-host` ✓
   `_chat.css:1166`, `qt-markdown-field` ✗ → fixed by point 3), (b) a
   Tailwind display utility in the host class string (the 17
   utility-string hosts the survey lists — `block`, `flex`, `inline-flex`,
   `absolute` all qualify), or (c) a bare element rule in the
   `_surfaces.css` family block (`qt-collapsible-card`,
   `qt-almanack-card`, `qt-entity-tabs`, `qt-settings-providers`).
   A component with NO host entry and no covering element rule is the
   finding class — the guard lists it. Implement beside/inside
   `apps/web/scripts/check-qt-classes.mjs` (its header SCOPE paragraph
   must be rewritten honestly if extended — it currently disclaims bare
   component classes by design), wire into `npm run lint` + ahead of
   `npm test` as the existing guard is, and prove it red-first: run it
   against the pre-#107-fix tree and it must name `qt-markdown-field`;
   run it with the fix and it must pass. ⚠ Do not trip on the two
   imperatively-set ProseMirror classes (`qt-rich-editor-content`,
   `qt-rich-editor-placeholder` — both have rules, and neither is a host
   class); exemptions follow the existing `qt-class-exception` idiom.
   If the census turns up hosts the guard cannot classify mechanically,
   narrow the guard honestly and record the residue — never a silent
   allowlist.

## Tiered deliverables

- **Tier 1 (must land):** points 1–3 — the class + tokens + comments
  byte-faithful, the twelve adoptions, the #107 rule with its spec pin.
- **Tier 2 (should land):** point 4 — the guard at the real-invariant
  scope, red-first proven.
- **Tier 3 (explicit refusal-arm deferrals):** if the real-invariant
  guard proves mechanically unreachable in the budget, land the narrow
  host-qt-class-must-have-a-rule form WITH its honestly-stated scope
  (catches #107's class only) and record the wider invariant as the
  named follow-up. v4's theme-storybook/create-quilltap-theme hunks:
  NO-PORT (v5 ships neither package); `help/themes.md` +
  `THEME_PLUGIN_DEVELOPMENT.md` hunks bank to `p4.9i2`.

## The differential requirement (restated)

This lane is CSS + class strings + a guard script — no oracle families
move. The equivalence discipline is byte-fidelity to the shipped hunks:
transcribe the two CSS blocks and every class string from
`git show 5f56f7a7d`, then diff your transcription against the hunks
mechanically (the byte-exact-static-data-transcription rule). The #107
frame classes are transcribed from `MarkdownLexicalEditor.tsx:194-206`
the same way. Spec pins: one per slider idiom + the markdown-field
host/rule pair + the guard's red-first proof (the guard run against a
tree with the rule deleted must fail naming the class — that IS its
mutation proof).

## Fixtures

None consumed or delivered.

## Shared contracts (binding, identical across all six orders)

**§A — the LoRA wire (P4.D138 ↔ P4.D139).**
- `GET /api/v1/image-profiles?action=list-models` response gains
  `loraSupport: Record<modelId, ImageLoraSupport>` between `source` and the
  conditional `fetchError`; models resolving no support are ABSENT from the
  map (the editor's "offer no LoRA rows" signal).
- NEW `GET /api/v1/image-profiles?action=options-schema&provider=…[&model=…]`
  → `{ provider, model: model ?? null, optionsSchema: ProviderOptionsSchema
  | null, loraSupport: ImageLoraSupport | null }` — `null`, never a zero-cap
  object. 400s: `Provider is required`; `` Provider ${provider} is not
  available ``.
- NEW `POST /api/v1/image-profiles?action=lora-metadata`, body
  `{ source: string, hfToken?: string }` → HTTP **200** carrying the
  `HuggingFaceLookupResult` verbatim (`{ok:true,facts}` |
  `{ok:false,reason,repoId,url,detail?}`; reasons `not-a-repo-id |
  missing-or-private | not-found | rate-limited | timeout | network |
  http`). Guards in order, exact sentences: bad JSON or non-object body →
  400 ``A JSON body with a `source` is required``; blank/non-string source
  → 400 `A LoRA source is required`; non-string hfToken → 400
  `hfToken must be a string when supplied`. POST-not-GET because hfToken is
  a credential (carry the comment).
- The stored `loras` entry: `{ source: string (trimmed, min 1), scale?:
  number (finite, 0..10), triggerPhrase?: string, label?: string }`.
  Write-side rejection is the Zod envelope
  `{ error: 'Validation error', details: issues }`, HTTP 400 — never a
  bespoke sentence. No server-side cap.
- `ImageLoraSupport = { maxLoras: number, scale?: {min,max,default,step?},
  sourceKinds: ('url'|'hf-repo'|'provider-id')[],
  supportsPrivateWeightsToken?: boolean }`;
  `DEFAULT_LORA_SCALE = {min:0,max:2,default:1,step:0.05}`.

**§B — `qt-range` (P4.D139 ↔ P4.D142).** D142 defines `.qt-range`
(`_interactive.css`) + `--qt-range-accent`/`--qt-range-focus-ring`
(`_variables.css`). D139's new LoRA editor slider carries the literal
`class="qt-range w-full"` (v4 `LoraListEditor.tsx:145`) and ships no slider
CSS of its own. Landing order is irrelevant: the class is inert-until-both,
never broken, and the check-qt-classes guard does not police bare component
names.

**§C — the salon danger-styling flip (P4.D141 ↔ P4.66 → the unifier).** v4
`SalonView.tsx:1496` switched the message-list danger prop to
`shouldShowDangerStyling(chat)`; the v5 twin line is
`salon-conversation.ts:311`, a file P4.66 owns. Neither lane edits the
other's file: P4.D141 ships the client predicate module
`apps/web/src/app/chat/concierge-state.ts` (exports `getConciergeState`,
`shouldUseUncensoredRoute`, `shouldShowDangerStyling`, `isClassifierOnDuty`
— transcribed 1:1 from v4 `chat-override.ts` with a parity spec), and the
ONE-LINE binding flip at `salon-conversation.ts:311` is a NAMED UNIFICATION
WIRE (PENDING_CROSS_LANE, P4.D122 precedent) applied by the unifier with a
spec pin. Both P4.D141 and P4.66 record it loudly in their lane records.

**§D — `core-contract.ts` region scoping.** Three lanes touch it in
disjoint regions: P4.D139 adds the image-profile/LoRA/options-schema DTOs
(a new block, §A names verbatim), P4.D140 adds `createdAt` to the
`RecentChat` DTO, P4.D141 widens the two `conciergeOverride` unions
(:2574, :2891) to `'OFF' | 'UNCENSORED' | null`. No lane touches another's
region; the unifier diffs the contract name-for-name (playbook §D).

**§E — server same-file region split (P4.D138 ↔ P4.D141).**
`tools/generate_image.rs` and `services/story_background_job.rs` belong to
P4.D138 EXCEPT the dangerous-content predicate flips — one import line plus
one call each (`generate_image.rs:1337`, `story_background_job.rs:365`) —
which P4.D141 owns and must edit to keep its branch compiling. The unifier
cherry-picks **P4.D138 before P4.D141** and hand-resolves the import-block
unions; neither lane touches the other's regions in those two files.

## Ownership (binding, identical across all six orders)

| Area | Owner |
|---|---|
| `quilltap-core`: `image_gen.rs`, `image_gen_data.rs`, `model/image.rs`, `model/image_dialects.rs`, `api/image_profiles.rs`, `db/image_profiles.rs`, `services/image_job_common.rs`, `services/character_avatar_job.rs`, `services/image_scene_tasks.rs`, `api/wardrobe.rs` (preview region), the NEW lora/params/HF modules; `quilltap-host`: `avatar_preview.rs`; `tools/generate_image.rs` + `services/story_background_job.rs` per §E | **P4.D138** |
| SPA: `screens/settings/images/**` (modal, form, api, card, picker + specs), `screens/settings/providers/{provider-options-panel.ts,provider-options-schema.ts}` (+ specs), NEW LoRA editor/query components, `core-contract.ts` §D region | **P4.D139** |
| `quilltap-core`: NEW `chat_activity` module, `db/chats_messages.rs`, `db/chats_messages_read.rs`, `services/chat_enrichment.rs`, `services/home.rs`, `api/characters.rs`, `api/projects.rs`, `api/brahma.rs`, `tools/self_inventory.rs`, `services/backup/restore/orchestrator.rs`, NEW recompute-heal module (+ `db/mod.rs` line); `quilltap-host`: `host.rs` (the boot repair chain block); SPA: `screens/salon/chat-card.ts`, `chat/merge-conversation-modal.ts`, `screens/home/recent-chat-item.ts`, `screens/characters/view/tabs/character-conversation-card.ts`, `brahma/brahma-wire.ts`, `e2e/support/seed-archived-character.ts`, `core-contract.ts` §D region (+ the touched specs) | **P4.D140** |
| `quilltap-core`: `services/dangerous_content/**`, `services/concierge_notifications.rs`, `services/context_summary.rs`, `services/danger_scan.rs`, `services/orchestrator.rs`, `services/pre_compute.rs`, `services/title_update_job.rs`, `services/memory_extraction_job.rs`, `services/carina_memory_extraction.rs`, `pascal/llm_consult.rs`, `api/salon.rs` (the conciergeState arm), the §E flip lines in `tools/generate_image.rs` + `services/story_background_job.rs`; SPA: `chat/conversation-header.ts`, `chat/sidebar/chat-section.ts`, NEW `chat/concierge-state.ts`, `styles/qt-components/_chat.css`, `core-contract.ts` §D region (+ specs) | **P4.D141** |
| SPA: `styles/qt-components/{_interactive.css,_variables.css,_surfaces.css}`, the class-string edits in the eight slider host files (`chat/sidebar/participant-card.ts`, `memory/housekeeping-dialog.ts`, `memory/memory-editor.ts`, `screens/settings/chat/context-compression-settings.ts`, `screens/settings/chat/dangerous-content-settings.ts`, `screens/settings/memory/memory-dedup-card.ts`, `screens/settings/providers/profile-modal.ts`, `screens/settings/system/tasks-queue-card.ts`), `apps/web/scripts/check-qt-classes.mjs` (the host-class guard extension) + its spec/self-test | **P4.D142** |
| SPA: `screens/salon/salon-conversation.ts` (+ spec), `chat/message-list.ts` (+ spec), the NEW mid-turn e2e beat | **P4.66** |
| Playwright (port 4319) | **P4.66's branch only**; every other lane's beats are authored gated (ACTIVATE-AT-UNIFY) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it |

**Version bumps:** this lane bumps the SPA per commit; the unifier
recounts at unification.

## Verification gate before declaring done

`npm test` + `npm run build` in `apps/web` (no bare `ng`); `npm run lint`
green with the extended guard in the chain; the guard's red-first proof
recorded; the transcription diffs against the v4 hunks clean; NO
Playwright; no crate edits. Per-commit rule:
`.claude/commands/commit.md`.
