# Work order P4.d10 — the state-cascade drift re-port, server half (the four-tier state cascade, Pascal `$state`, the general-state seed, the KaTeX system-prompt note) — round baseline `7e6d13e5`

> **STATUS: CLOSED — UNIFIED ON MAIN (2026-07-20, the state-cascade
> round unification).** The §C wire applied (corpus count literals
> 175 = 10 title + 165 definition, both consumers green); the round
> pins retired at cleanup. The lane record follows.
>
> **STATUS AT LANE CLOSE: LANE COMPLETE (2026-07-20, branch
> `claude/p4-d10-state-cascade-703889`).** Tier 1 units 1–9 all landed;
> tier 2 items 1–6 all closed (1 zero-diff, 2 mirrored, 3/4/5
> dispositioned no-op, 6 corpus-extended); the tier-3 deferrals stand as
> written. Unit records in `status-log.md` (six "P4.d10 …" entries).
>
> **For the unifier — the §C corpus wire:** the regenerated committed
> corpus `apps/web/src/testing/fixtures/pascal-custom-tool-definition.oracle.ndjson`
> is **175 rows total = 10 title + 165 definition (58 accept / 107
> reject)** — update the count literals in
> `apps/web/src/app/pascal/custom-tool-types.corpus.spec.ts` and re-run
> both consumers.
> **Rebuilt committed fixtures:**
> `crates/quilltap-web/tests/fixtures/pascal-run-custom-{main,mount}.db`
> (+ its `.meta.json` — new vault ids) — every differential over them
> already re-ran green this lane;
> `harness/oracle/fixtures/request-envelopes/request-envelopes.recorded.ndjson`
> (31 → 34, the Anthropic boundary rows). Extended committed spec/corpus
> files: `state-sql-tools.json` + `build-state-sql-tools-fixture.ts`
> (groups/general-mount/union-chat/user-row — the state-sql DBs are
> BUILT AT RUN TIME, not committed), `workbench-route-cases.json` (44 →
> 50). The round pin `/private/tmp/qt-v4-pin-7e6d13e5` stays alive until
> unification (per-plugin `plugins/dist/*/node_modules` symlinks were
> added to it — the regen recipes need them).

> **STATUS (planning): OPEN. Re-baselined 2026-07-20** — originally planned
> 2026-07-19 against `c53510c7`; v4 then moved TWELVE more commits (the
> 4.8.0 release sweep) before this lane started, so the round was
> re-baselined and extended (units 8–9, tier-2 items 4–6, the second
> drift table below; sibling lane D11 added).
>
> Lane D10 of the re-baselined full-drift catch-up round (the catch-up
> the p4.9j workspace-tabs round left OWED). **v4 reference baseline
> for this round: `7e6d13e5` (4.8.0-dev.92), tree clean.** At lane
> start, run in `~/source/quilltap-server`:
>
> ```bash
> git log 7e6d13e5..HEAD --oneline
> ```
>
> **If HEAD moved, STOP and report.** (v4 is mid-release-checklist; a
> 4.8.0 tag/version commit may land — report it either way.) Every
> oracle in this lane generates at `7e6d13e5`. Create a PINNED
> detached worktree at lane start and regenerate from it even though
> the checkout is clean today (the `oracle-regen-pinned-v4-worktree`
> memory — symlink BOTH `node_modules`: root +
> `packages/{quilltap,plugin-types,plugin-utils}`):
>
> ```bash
> cd ~/source/quilltap-server
> git worktree add --detach /private/tmp/qt-v4-pin-7e6d13e5 7e6d13e5
> ln -s ~/source/quilltap-server/node_modules /private/tmp/qt-v4-pin-7e6d13e5/node_modules
> for p in quilltap plugin-types plugin-utils; do
>   ln -s ~/source/quilltap-server/packages/$p/node_modules \
>         /private/tmp/qt-v4-pin-7e6d13e5/packages/$p/node_modules
> done
> ```
>
> The old pin `/private/tmp/qt-v4-pin-b8b12695` stays ALIVE until
> unification (the unifier retires it, along with this round's pin).
>
> Sibling lanes: `p4.6be-state-cascade-spa.md` (lane BE — **already
> CARRIED OUT** on branch `claude/p4-6be-state-cascade-spa-bd6ade` at
> the old `c53510c7` baseline; its v4 reference surfaces were verified
> byte-unchanged across `c53510c7..7e6d13e5`, so its work stands — see
> its status header) and `p4.d11-release-sweep-spa.md` (lane D11 —
> the release sweep's SPA slice: single-dollar math promotion +
> workbench backdrops; `apps/web/` files disjoint from both BE's
> branch and your §C carve-out). The **Shared contract** and
> **Ownership** sections below are binding and reproduced verbatim in
> lane BE's order. You run in a worktree; a unification pass
> integrates the lanes onto main.
>
> **This lane IS a differential port.** Every unit arrives with an
> equivalence test against v4's real code at `7e6d13e5`. The facts
> under "Survey-verified starting points" are from a **2026-07-19
> survey at `c53510c7` (v4) / main `7145b23f` (v5)** — verified
> 2026-07-20 to survive the re-baseline: NO state-cascade or Pascal
> feature file changed in the release sweep, but the logging prune
> (`28e89f51`) removed lines from `general-state.ts`,
> `run-custom-handler.ts`, `custom-tools.ts`, `workbench.ts`, and
> `llm-consult.ts`, so **some v4 line numbers below shift by a few
> lines at `7e6d13e5`** — re-anchor locally; STOP and report only on a
> substantive contradiction, not a line drift.

## The drift, classified (the disposition of record)

Four v4 commits between `b8b12695` and `c53510c7`:

| Commit | What | Disposition |
|---|---|---|
| `70baaa74` | docs: the cascading-state feature plan (`docs/developer/features/state-cascade.md`) | **MIRROR ONLY** (tier 2 — refresh the `docs/v4/` mirror) |
| `f48f34dc` | **feat: cascading state (chat → project → group → general) with Pascal `$state`** | **RE-PORT** (this lane: everything server-side; lane BE: the SPA) |
| `77fa8c73` | chore: version bump | no action |
| `c53510c7` | **feat: universal system-prompt note on KaTeX `$$…$$` math delimiters** | **RE-PORT** (this lane, unit 7 — server-only; the SPA renderer already handles `$$` since P4.d9) |

Also dispositioned, no action required: the `help/*.md` edits
(`chat-state.md`, `custom-tools.md`, `groups.md` — v5 loads help from
`<cwd>/help` at runtime and commits no copy; banked for `p4.9i2` with the
rest of the help surface); `public/schemas/qtap-export.schema.json`
(+4: `fileType` enum gains `json`/`jsonl` — v5 ships no export schema
asset, the P4.d8 documented-absence disposition stands; but see tier 2
item 3 for the *import-side* acceptance check it implies);
`docs/developer/DDL.md` (+2 — doc prose; **no schema change is in the
feature**: chat state is the pre-existing `chats.state` column,
project/group state are store-resident `state.json` documents, general
state is a mount-root document. Tier 2 verifies with a zero-diff D23
re-dump). The two schema mirrors that DO matter:
`qtap-custom-tool.schema.json` (+55, `$defs/StateRef`) is lane BE's
byte-copy; the §C corpus regen is yours.

## The release-sweep extension, classified (`c53510c7..7e6d13e5`, dispositioned 2026-07-20)

Twelve commits — the 4.8.0 release landing. v4's "no functional
change" claims are **verified mechanically by this lane's units 8–9**,
never taken on faith:

| Commit | What | Disposition |
|---|---|---|
| `f48f34dc`→`c53510c7` | (the original four — see the table above) | unchanged |
| `f495c9c9` | chore: npm dep updates. Two non-infra riders: **`lib/llm/pricing-fetcher.ts`** rewritten for `@openrouter/sdk` 0.13's paginated `models.list()` (same observable model list, SDK-internal paging); **katex 0.18.0 → 0.18.1** | pricing: **tier-2 item 5** (disposition check — v5 fetches the OpenRouter REST endpoint directly, no JS SDK); katex: **lane D11** |
| `a4ad1e83` | chore: openrouter plugin dist bump | **NO-PORT** (v4 plugin dist; v5 ports `lib/`, not `plugins/dist/`) |
| `60572405` | chore: release-checklist Claude commands | **NO-PORT** (v4 infra) |
| `d0a13d9a` | doc: release notes for 4.8.0 | **MIRROR** (tier-2 item 2's sweep, per the mirror convention) |
| `8ee56f6e` | test: regression coverage for 4.8 fixes that lacked tests (messages/[id] edit+delete, wardrobe itemId vault-aware lookup, run_custom listing, file-attachment fallback, **Anthropic model-family params**) | **NO DRIFT** (the fixes are pre-baseline, already absorbed) — **tier-2 item 6** banks the five families as corpus seeds + spot-checks the Anthropic one |
| `5915b04e` | **fix: single-dollar LaTeX promotion in `lib/markdown/math.ts`** (+ the `help/math-notation.md` rewrite) | **RE-PORT — lane D11** (SPA). Your unit 9 proves the seven renderer-transitive oracle families stay output-neutral |
| `93604767` | **refactor: deduplicate shared logic** across ~19 ported `lib/` files (wardrobe handlers, skip-signal, turn-manager state, memory-weighting, rng-tool, message-formatter, cheap-llm, vault-overlay parsers, database-store, whisper, image-gen, participants) — "no functional change" | **VERIFY, DON'T PORT — unit 8** (the neutrality tripwire sweep). v5 keeps its own structure; adopt v4's new shared-helper *names* only where a future port would otherwise drift |
| `bfc2c08c` | chore: remove the superseded `QtapDocLink` qtap:// chain (SPA dead-code) | **NO-PORT** (v5 never ported the chain — verified 2026-07-20; v5 already matches post-sweep v4). Mirror `DEAD-CODE-REPORT.md` per convention |
| `28e89f51` | chore: prune release-scaffolding debug logging (~29 files, logger.debug removals only — verified by diff inspection 2026-07-20) | **VERIFY, DON'T PORT — unit 8** (rides the same sweep; log lines never reach oracle payloads) |
| `8f24ccc9` | style: workbench dialog backdrops → `qt-dialog-overlay` | **RE-PORT — lane D11** (2-line swap) |
| `2363c518` | fix: strip ephemeral Commonplace Book state (`commonplaceRecallHistory`/`commonplaceSceneCache`) from the .qtap **export** writer + a `chat.types.ts` doc-comment | **NO-PORT** (the export writer is unported — the P4.d8 documented absence stands). One check at lane start: confirm `quilltap_import::parse_export_file` doesn't REQUIRE either dropped field on chat records (fields absent from newer exports can only break a parser that demands them — near-certain no-op; record it) |
| `3dfcb4b4` / `7e6d13e5` | docs: CLI README file-verify; API.md all-routes + README | **MIRROR** (tier-2 item 2's sweep) |

## Mandate

Re-port v4 `f48f34dc` + `c53510c7` across the already-ported v5 server
surface: the new pure `state_paths`/`state_cascade` modules, the
general-state mount-root document (`ensure`/`read`/`write` + the
idempotent host-boot seed), the rewritten four-tier state tool handler
(with the `group` ref param, the `characterId` scope wire, and the
updated tool-definition bytes), the nine new state dispatch verbs
(chat/group/general tiers) with the enriched chat get-state cascade
response, Pascal `$state` end-to-end (`StateRefSchema` in the definition
schema, `resolveStateValue` + threading through params/rolls/operands/
templates/simulate, the per-entrance cascade resolution in the
`run_custom` handler and the chat custom-tool run, the workbench
preview/audit `state` param), and the universal math-notation note in
`build_system_prompt` — each with its regenerated or extended
differential at `7e6d13e5`. This lane owns the Rust workspace and the
harness; it must NOT touch `apps/web/**` EXCEPT the §C corpus NDJSON
fixture file it owns. User-facing strings (schema `describe` texts,
error messages, the tool definition, the math note) are v4's verbatim
bytes — they are wire payload, not prose to improve.

## Survey-verified starting points (2026-07-19: v4 @ `c53510c7`, v5 @ main `7145b23f` — do not re-derive)

### v4 — the new behavior (file:line at `c53510c7`)

- **`lib/state/state-paths.ts`** (NEW, 158 lines) — pure path helpers
  extracted VERBATIM from the old state handler (which re-exports them
  for back-compat; the one deliberate difference: the root-set guard
  here throws a plain `Error`, not the handler's typed `StateError`,
  :9-14). Exports: `parsePath` (:27 — regex `/(\w+)|\[(\d+)\]/g`; a
  segment is a `\w+` word (string) or `[N]` (number via `parseInt`);
  empty/whitespace/undefined → `[]`; **known limitation :16-18: keys
  with spaces/dots/hyphens are unreachable** — shared with Pascal
  `$state`, port it faithfully); `getAtPath` (:56 — empty path returns
  the object; `undefined` past any null/primitive); `setAtPath` (:84 —
  empty path = root-set, value MUST be a plain object else
  `throw new Error('Cannot set root state to non-object value')`
  :89-95; intermediates minted by next-key type: number → `[]` else
  `{}`; primitives mid-path overwritten; mutates + returns);
  `deleteAtPath` (:124 — root → `false`; missing/non-object mid-path →
  `false`; array + numeric last key → `splice` (reindexes) else
  `delete`; returns whether deleted). NO underscore logic here.
- **`lib/state/state-cascade.ts`** (NEW, 297 lines) — the single
  four-tier merge implementation, consumed by the state tool handler,
  the chat get-state route, and both Pascal entrances. Exports:
  `GroupScope` (:45-48) = `{kind:'character';characterId}` |
  `{kind:'participants-union'}` | `{kind:'none'}`; `GroupCandidate
  {id;name}`; `GroupTierStatus = 'none'|'single'|'ambiguous'`;
  `GroupTier {status; candidates; appliedGroupId?}` (:64-68);
  `StateCascadeResult` (:70-78) = `{chatState, projectState,
  groupState, generalState, merged, groupTier, projectId?}`;
  `StateGroupResolutionCode` (:81-85) = `'GROUP_NOT_FOUND' |
  'GROUP_AMBIGUOUS' | 'NO_GROUPS' | 'GROUP_REF_REQUIRED'`;
  `StateGroupResolutionError` (:91-100, carries `code` +
  `candidates`); `resolveGroupCandidates(chat, scope)` (:147 —
  fail-soft PER GROUP: a store that throws is warn-logged and
  dropped); `resolveStateCascade({chat, groupScope})` (:173);
  `resolveGroupForContext({groupRef?, candidates})` (:249, sync,
  throws). **The merge (:219-225): `merged = { ...generalState,
  ...groupState, ...projectState, ...chatState }`** — shallow,
  top-level, narrowest wins. Tier loading: chat =
  `asStateObject(chat.state)` (no I/O, :180); project =
  `chat.projectId → repos.projects.findById → asStateObject(.state)`,
  any throw → `{}` + warn (:183-198); group = candidates then the
  **exactly-one rule** (:206-214): 0 → status `none` + `{}`; 1 →
  that group's state + `{status:'single', appliedGroupId}`; 2+ →
  `{status:'ambiguous'}` and **merges NOTHING**; general =
  `readGeneralState()` (:217). `collectGroupIds` (:117-141):
  `character` scope → `repos.groupCharacterMembers.findByCharacterId`;
  `participants-union` → union over `chat.participants` where
  `p.type==='CHARACTER' && p.characterId && p.status!=='removed'` —
  **deliberately NOT filtered by `controlledBy`** (:130-131, carry the
  why-comment); dedup via Set. `resolveGroupForContext` policy +
  VERBATIM error strings (:242-297): 0 candidates → `NO_GROUPS`
  `"This character does not belong to any group, so there is no group
  state to reach."`; ref omitted + 1 → it; ref omitted + 2+ →
  `GROUP_REF_REQUIRED` `` `More than one group applies; specify one by
  name or id: ${formatCandidates}.` ``; ref === id → it; else
  case-insensitive EXACT name match among candidates only (1 → it;
  2+ → `GROUP_AMBIGUOUS` `` `More than one group is named "${ref}";
  specify one by id: …` ``; 0 → `GROUP_NOT_FOUND` `` `No group
  matching "${ref}" among this character's groups: …` ``).
  `formatCandidates` renders `"Name" (id)` joined by `, `.
  `asStateObject` coerces non-object/array → `{}`.
- **`lib/mount-index/general-state.ts`** (NEW, 115 lines) — the
  bottom tier: a `state.json` document at the root of the singleton
  "Quilltap General" mount (no entity row). Exports:
  `GENERAL_STATE_JSON_PATH = 'state.json'` (:30);
  `ensureGeneralStateFile()` (:42 — falsy `getGeneralMountPointId()` →
  `false`; existence checked by
  `repos.docMountDocuments.findByMountPointAndPath(id,'state.json')`
  — **presence, never parse — a hand-corrupted body is the user's
  state, never heal it** (:37-41); absent → write the exact 2 bytes
  `{}` and return `true`); `readGeneralState()` (:68 — `{}` on
  unprovisioned / NOT_FOUND / corrupt JSON (warn) / non-object or
  array body (warn); **never throws**); `writeGeneralState(state)`
  (:101 — unprovisioned → **throws** `'Quilltap General mount has not
  been provisioned yet'`; serializes `JSON.stringify(state ?? {},
  null, 2)`, wholesale overwrite).
- **`lib/tools/handlers/state-handler.ts`** (rewritten, 451 lines) —
  ops `fetch|set|delete`, contexts `chat|project|group|general`.
  `StateToolContext` gains `characterId` (:45-57 — scopes the group
  tier `{kind:'character'}`; absent → `{kind:'none'}` → no group
  tier, :155-157). Validation failure envelope (:107-121):
  `{success:false, operation, error:'Invalid input: operation is
  required and must be "fetch", "set", or "delete"'}`. Chat fetch +
  ownership (:127-139): `!chat || chat.userId !== userId` →
  `'Chat not found or permission denied'`. Project resolution
  `context.projectId || chat.projectId` (:142-147). `resolveGroupOrError`
  (:161-173) converts `StateGroupResolutionError` → tool error string,
  never throws. **Underscore protection (:175-193): SET and DELETE
  only — fetch of `_` keys is allowed**; message `` `Keys starting
  with underscore are user-only and cannot be ${verb} by AI` `` (verb
  `modified`|`deleted`). fetch routing (:197-249): explicit tier reads
  that tier (project absent → `'Chat is not part of a project'`; group
  via resolveGroupOrError; general via readGeneralState); NO context →
  `resolveStateCascade` + `getAtPath(merged, …)`. set (:251-313):
  `targetContext = stateContext || 'chat'`; clones the tier object then
  `setAtPath`; persists via `repos.chats.update(id,{state})` /
  `repos.projects.update` / `repos.groups.update` /
  `writeGeneralState`; returns `previousValue`. delete (:315-388):
  same routing over `deleteAtPath`, persists only `if (deleted)`,
  returns `previousValue`. Output `StateToolOutput` keys exactly
  `{success, operation, context?, path?, value?, previousValue?,
  error?}`. `formatStateResults` (:420-451): `State Error: …`;
  `(root)` pathDisplay; ` [${context}]` suffix; `(not set)` /
  pretty-JSON value / `set … to:` / `updated …: prev → new` /
  `deleted … (was: …)` arms — v5 already mirrors the two-tier version
  of this in `format_state_results`.
- **`lib/tools/state-tool.ts`** (+30) — `StateContext` gains
  `'group'|'general'`. The `context` enum is now 4-valued, description:
  `"Which tier to operate on: \"chat\" (this conversation), \"project\"
  (its project), \"group\" (a group you belong to — see the \"group\"
  parameter), or \"general\" (instance-wide). For fetch without
  context, returns the merged cascade where narrower tiers win (chat
  over project over group over general). Set/delete without context
  default to \"chat\"."` NEW optional `group` string param:
  `'Group name or ID; required with context "group" when the character
  belongs to more than one group.'`. The definition description's
  middle sentence is replaced with: `'State cascades across four tiers,
  narrowest winning: chat → project → group → general. '` + `'A fetch
  with no context returns the merged view; set/delete with no context
  default to the chat tier. '` (the rest — "Manage persistent state
  for games…", paths, underscore — is unchanged). The snapshot
  `lib/tools/__tests__/__snapshots__/tool-definitions-snapshot.test.ts.snap`
  reflects the same (ignore the stale copies under v4's
  `.claude/worktrees/`).
- **`lib/chat/tool-executor.ts`** (+1, ~:698) — the `state` branch's
  `StateToolContext` literal gains `characterId,` (already in scope) —
  this is the whole group-tier scope wire.
- **`instrumentation.ts`** (+21, :760-780) — a new companion seed at
  the tail of startup Phase 3 (after `ensureGeneralScenariosFolder`,
  before the Phase-3.5 schedulers): dynamic import of
  `general-state`, `await ensureGeneralStateFile()`; `true` →
  `logger.info('Seeded general state.json in the Quilltap General
  mount', …)`; try/catch → warn + continue. Idempotency lives in
  `ensureGeneralStateFile` itself.
- **Chat get-state** (`app/api/v1/chats/[id]/actions/state.ts`, 66
  lines): `handleGetState` now delegates to `resolveStateCascade` with
  `groupScope: {kind:'participants-union'}`. **Response (verbatim,
  :49-58):** `{ success:true, state: cascade.merged, chatState,
  projectState: keys>0 ? … : undefined, groupState: keys>0 ? … :
  undefined, generalState: keys>0 ? … : undefined, groupTier,
  projectId: cascade.projectId }` — the three inherited tiers are
  **JSON-OMITTED when empty** (back-compat convention); `state` +
  `chatState` + `groupTier` always present; `projectId` is
  `chat.projectId || undefined`. `handleSetState`/`handleResetState`
  are UNCHANGED factory handlers (`entityName:'Chat'`,
  `useOwnershipCheck:false`); set body `stateBodySchema =
  z.object({ state: z.record(z.string(), z.unknown()) })`
  (`lib/api/state-handlers.ts:47-49`); set → `{success:true, state}`;
  reset → `{success:true, previousState}`. 404 `notFound('Chat')`;
  catch `serverError('Failed to get state')`.
- **Group state actions** (NEW `app/api/v1/groups/[id]/actions/state.ts`,
  52 lines): action strings `get-state`/`set-state`/`reset-state`
  (identical to chat). `useOwnershipCheck:false` (groups are
  instance-global). **get-state returns OWN state only — NO cascade**:
  `{ success:true, state: group.state ?? {} }` (:41-44). set/reset =
  the shared factory shapes. Wiring: `handlers/get.ts` action list
  gains `'get-state'`; `handlers/put.ts` gains an action switch
  routing `?action=set-state`; `handlers/delete.ts` gains
  `'reset-state'`. **No state on the default GET; no bespoke delete
  lifecycle** (group state rides the group's store-resident
  `state.json` through the overlay — it dies with the store).
- **General state route** (NEW
  `app/api/v1/settings/general-state/route.ts`, 62 lines): GET →
  `{success:true, state}` (readGeneralState); PUT → `stateBodySchema`
  parse, failure `validationError`, else write →
  `{success:true, state: parsed.state}`; DELETE → read previous, write
  `{}` → `{success:true, previousState}`. Errors:
  `serverError('Failed to read general state' | 'Failed to update
  general state' | 'Failed to reset general state')`.
- **Pascal `$state`** —
  `lib/pascal/custom-tool.types.ts`: NEW `StateRefSchema =
  z.strictObject({ $state: z.string().min(1).describe('Dot/bracket
  path into merged state, e.g. "player.health" or
  "inventory[0].name".'), fallback: z.union([z.number().finite(),
  z.string(), z.boolean()]).describe('Required. Used whenever the path
  is absent or holds a value of a different type.') })` +
  `isStateRef` guard (`'$state' in value`). Added to the unions:
  `NumberOrParamRefSchema` (roll fields), parameter `default`,
  `NumberOperandSchema`, `AnyOperandSchema` (eq/neq),
  `StringOperandSchema` (contains/ncontains). Validation: a
  parameter's declared type is checked against the **fallback**
  (`isStateRef(d) ? d.fallback : d`; integer default must satisfy
  `Number.isInteger`); `validateRollRefs` issue: `` `roll.${field}
  uses a $state reference whose fallback is ${typeof} rather than a
  number` ``; `resolveOperandType` types a `$state` operand by
  `typeof fallback`. **`persist` stays deferred in v4 itself** —
  `$state` is read-only; the top-level schema tolerates unknown keys
  to reserve room (docs/developer/features/pascal-custom-tools.md:195).
  `lib/pascal/custom-tools.ts`: `type CustomToolState =
  Record<string,unknown>`; **`resolveStateValue(ref, state)`** — pure,
  total: `getAtPath(state ?? {}, parsePath(ref.$state))`; returns the
  found value iff `typeof found === typeof fallback` (a found number
  must also be finite, else fallback); else the fallback. Threading
  (every site takes `state: CustomToolState = {}`): `resolveParams`/
  `coerceParam` (omitted param with `$state` default →
  `resolveStateValue`); `resolveRollField` (a `$state` roll field that
  resolves non-finite **throws** `CustomToolRunError` `` `roll.${field}
  $state reference resolved to … rather than a finite number` ``);
  `rollRange`; `resolveOperand` (**never throws** — metadata
  doctrine) → `matchesComparator`/`matchesMetadataComparator`/
  `matchesLlmComparator`/`matchesWhen`; `OutcomeSubjects.state?`;
  `renderTemplate` gains the `{{state.path}}` arm — strips `state.`,
  `getAtPath`; renders primitives, **leaves the placeholder verbatim**
  for absent/non-primitive (+ debug log); `executeCustomTool` overrides
  gain `state?` (default `{}`), threaded into resolveParams, rollRange,
  BOTH renderTemplate calls (llm prompt + outcome message), and
  `subjects`; `simulateOutcomes` gains trailing
  `state: CustomToolState = {}` (7th arg, after `llm`).
  `lib/tools/handlers/run-custom-handler.ts` (+27): resolves the
  cascade at the entrance — `groupScope: context.characterId ?
  {kind:'character', characterId} : {kind:'none'}` — fail-soft
  try/catch → `toolState = {}`; passes `state: toolState` into
  `executeCustomTool`.
  `app/api/v1/chats/[id]/custom-tools/route.ts` (+25): `handleRun`
  resolves the cascade per-entrance with `body.asCharacterId ?
  {kind:'character',…} : {kind:'none'}` (NOT participants-union),
  fail-soft → `{}`, passes `state`.
  `app/api/v1/custom-tools/route.ts` (+8): `PreviewBodySchema` and
  `AuditBodySchema` both gain `state: z.record(z.string(),
  z.unknown()).nullish()`; `handlePreview` → `executeCustomTool(…,
  {…, state: body.value.state ?? {}})`; `handleAudit` →
  `simulateOutcomes(…, body.value.state ?? {})`.
- **The math note** (`lib/chat/context/system-prompt-builder.ts`, +21):
  `MATH_FORMATTING_INSTRUCTION` (:22, a JS template literal — after
  escape resolution the LaTeX is single-backslash):

  ```
  [FORMATTING: MATHEMATICAL NOTATION]
  Responses render as Markdown with KaTeX math support. To display a formula, wrap the LaTeX in DOUBLE dollar signs — $$...$$ — which renders correctly both inline within a sentence and as its own centered block. Do NOT use single dollar signs ($x$), single quotes, or backticks for math; only $$...$$ is recognized.
  - Inline example: The area of a circle is $$A = \pi r^2$$ for radius r.
  - Block example:
  $$
  \int_0^1 x^2 \, dx = \frac{1}{3}
  $$
  ```

  Pushed UNCONDITIONALLY at :281 — after the roleplay-template push,
  before the tool-instructions push; parts joined `'\n\n'` + trim
  (:299). v4's cache-determinism golden hash moved `cd1a16cf903c655a`
  → `bd27b1ca407d9901` (v5 has no golden-hash mirror; the tier-1
  system-prompt differential is the proof).
- **v4 test families (corpus seeds):** `__tests__/unit/lib/state/
  state-paths.test.ts` (90), `state-cascade.test.ts` (254),
  `__tests__/unit/lib/tools/handlers/state-handler.test.ts` (257),
  `__tests__/unit/app/api/v1/chats/[id]/actions/state-get.test.ts`,
  `…/groups/[id]/actions/state.test.ts`,
  `…/settings/general-state/route.test.ts`,
  `…/custom-tools/route-state.test.ts`,
  `__tests__/unit/lib/pascal/custom-tools-state.test.ts`,
  `__tests__/unit/lib/mount-index/general-state.test.ts`. Mirror
  their case families into the corpora below.

### v5 — the anchors (main `7145b23f`)

- **State tool:** `crates/quilltap-core/src/tools/state.rs` (1045
  lines, two-tier). Chat tier = `chats.state` column via
  `writers.main().chats().update(chat_id, ChatUpdate{state})`
  (`write_chat_state`, :813 — partial SET, no `updatedAt` mint).
  Project tier = the `state.json` overlay via
  `ProjectsRepository::new(main, mount).update(id, {state})`
  (`write_project_state`, :827); degraded (no mount-index) reads as
  "no project" (:529-530). Entry `execute_state_tool` (:444) →
  `run_state` (:459) → `run_fetch` (:585) / `run_set` (:629) /
  `run_delete` (~:735). `StateToolContext{user_id, chat_id,
  project_id}` (:45) — gains `character_id` this round.
  `StateToolOutput` (:66) with hand-written `Serialize` (:78) pinning
  field order — extend, don't reorder. `format_state_results` (:846).
  Executor call site: `crates/quilltap-core/src/tools/executor.rs:1035-1040`.
- **Tool definition bytes:**
  `crates/quilltap-core/src/tools/definitions/data.rs:235-237` — the
  `json` string IS the v4 bytes; splice the new definition verbatim.
- **Dispatch verbs precedent:** `ProjectStateGet`/`Set`/`Reset` at
  `crates/quilltap-core/src/api/types.rs:945-958`; handlers
  `project_state_get`/`set`/`reset` at
  `crates/quilltap-core/src/api/projects.rs:621-690`; engine dispatch
  `crates/quilltap-core/src/api/engine.rs:1631-1639`. Groups verbs in
  `api/groups.rs` (:92-622), settings in `api/settings.rs`, chat verbs
  in `api/salon.rs`. The web edge is the single `POST /api/dispatch`
  (`crates/quilltap-web/src/dispatch.rs:52`) — new verbs need NO REST
  edge.
- **Host boot seed:** `crates/quilltap-host/src/host.rs` —
  `seed_built_ins(db)` (:389/:609) runs `seed_built_in_templates`,
  `ensure_builtin_mounts`, `ensure_general_scenarios_folder`
  (:617-621); the general-state ensure lands HERE (after
  `ensure_general_scenarios_folder`, mirroring v4's phase order).
  "Quilltap General" mount id:
  `crates/quilltap-core/src/db/instance_settings.rs` —
  `get_general_mount_point_id` (:80). Mount provisioning:
  `crates/quilltap-core/src/services/builtin_mounts.rs` (:70/:84/:229).
  Mount-root document writes:
  `crates/quilltap-core/src/services/mount_index/file_ops.rs`
  (`write_file` :952) + the `db/doc_mount_*` repos.
- **Pascal:** `crates/quilltap-core/src/pascal/` —
  `custom_tool_types.rs` (2285 — the definition schema/Zod port),
  `custom_tools.rs` (1526 — `execute_custom_tool`, `render_template`,
  `resolve_params`, `matches_when`), `workbench.rs` (530 — preview/
  audit param plumbing), `roster.rs`, `llm_consult.rs`, `dice.rs`,
  `js_value.rs`. Run-custom handler:
  `crates/quilltap-core/src/tools/run_custom.rs`.
- **System prompt:** `crates/quilltap-core/src/system_prompt.rs` (717)
  — `build_system_prompt` (:403); roleplay-template push :451-455;
  tool-instructions push :457-461; **the note inserts between them**;
  join rule `js_trim(&parts.join("\n\n"))` (:478).
- **Differentials + oracles (all env-var driven; committed artifacts
  called out):**
  - `state_sql_tools_equivalence.rs` — jest oracle
    `harness/oracle/cases/state-sql-tools.test.ts`, spec fixture
    `harness/oracle/fixtures/state-sql-tools.json` (committed), DB
    builder `build-state-sql-tools-fixture.ts` (committed; DBs built
    to `QT_FIXTURE_TMP_{MAIN,MOUNT,LLM}` at run time). Envs
    `QT_ORACLE_STATE_SQL` + the three fixture tmps. Diffs
    `resultJson`, `formatted`, a `chats` dump, a project-state
    read-back.
  - `tool_definitions_equivalence.rs` — `QT_ORACLE_TOOL_DEFINITIONS`
    (+`_CANONICAL`), cases `tool-definitions.ts` /
    `tool-definitions-canonical.ts`. Byte-compares every `def_json`.
  - `tool_wire_equivalence.rs` — committed
    `harness/oracle/fixtures/tool-wire/tool-wire.recorded.ndjson` uses
    SYNTHETIC tools only (`state` absent, verified) — re-run as a
    guard, no regen needed.
  - Pascal: `pascal_custom_tool_definition_equivalence.rs`
    (`QT_ORACLE_PASCAL_DEFINITION`, case
    `pascal-custom-tool-definition.ts` — the §C generator),
    `pascal_custom_tools_execution_equivalence.rs`
    (`QT_ORACLE_PASCAL_EXECUTION`),
    `pascal_run_custom_handler_equivalence.rs` (committed
    `crates/quilltap-web/tests/fixtures/pascal-run-custom-{main,mount}.db`
    + builder `build-pascal-run-custom-fixture.ts`),
    `pascal_workbench_equivalence.rs` +
    `pascal_workbench_route_equivalence.rs` (committed
    `workbench-{main,mount}.db`, `workbench.json`,
    `workbench-route-cases.json`), `pascal_simulate_equivalence.rs`,
    `pascal_roster_equivalence.rs`,
    `pascal_custom_tools_route_equivalence.rs`,
    `pascal_build_tools_roster.rs`, `pascal_llm_consult_equivalence.rs`.
  - `system_prompt_equivalence.rs` — `QT_ORACLE_SYSTEM_PROMPT`, case
    `harness/oracle/cases/system-prompt.ts`, run under `TZ=UTC`, no
    committed fixture.
  - Routes precedent: groups (`groups_routes_equivalence.rs` over
    `groups-projects.json`), settings (`settings_routes_equivalence.rs`,
    `QT_ORACLE_SETTINGS_ROUTES` + `QT_FIXTURE_SETTINGS`). Per the
    P4.d7 memory: **direct-drive differentials beat jest route
    oracles** — drive v4's exported handlers, not HTTP.
- **Blast radius (verified 2026-07-19):** NO committed fixture embeds
  the composed system prompt or the state tool description (the
  `"Manage persistent state"` string exists ONLY in `data.rs`; the
  reinforcement clause exists ONLY in `system_prompt.rs`; the tier-3
  fixtures' `systemPrompt` keys are template INPUTS, not composed
  output). The regen story is: fresh `/tmp` oracles for every family
  you touch + the committed artifacts you explicitly change (the §C
  corpus, the fixture builders/DBs you extend).

## Tiered deliverables

### Tier 1 — must land

1. **`state_paths` + `state_cascade` modules** (suggest
   `crates/quilltap-core/src/state/{paths,cascade}.rs`, mirroring v4's
   `lib/state/` extraction; `tools/state.rs` re-uses them the way v4's
   handler re-exports). Port the path grammar EXACTLY (regex-equivalent
   segmentation, the unreachable-keys limitation, the root-set error
   string, the splice semantics), the cascade resolver (merge order,
   exactly-one rule, participants-union filter with the
   `controlledBy` why-comment, per-tier fail-soft), and
   `resolve_group_for_context` with the four codes + verbatim
   messages. **Differential:** a new tier-1/tier-2 oracle case
   (`harness/oracle/cases/state-cascade.test.ts`, jest — it needs
   repos) direct-driving v4's REAL `parsePath`/`getAtPath`/`setAtPath`/
   `deleteAtPath`/`resolveStateCascade`/`resolveGroupForContext` over a
   corpus mirroring v4's three unit-test families (paths: parse/get/
   set/delete incl. root-set throw + array splice; cascade: precedence,
   0/1/2-group, participants-union incl. removed/non-character skips,
   degradation; group-ref: all four codes + case-insensitive name +
   id-first).
2. **The general-state document + host seed.** A
   `general_state` module (suggest under
   `crates/quilltap-core/src/services/mount_index/` beside its v4
   home): `GENERAL_STATE_JSON_PATH`, ensure (presence-check,
   never-heal, seeds literal `{}`), read (all the `{}` fail-softs),
   write (pretty 2-space, unprovisioned throw). Wire the ensure into
   `quilltap-host::host::seed_built_ins` after
   `ensure_general_scenarios_folder` (log-on-seed, warn-and-continue
   on error). **Differential:** cases in the unit-1 oracle mirroring
   v4's `general-state.test.ts` family (no-op unprovisioned, seed,
   never-heal, corrupt/array/NOT_FOUND reads, write round-trip).
3. **The four-tier state tool.** Extend `tools/state.rs`: contexts
   `group`/`general`, the `group` ref param, `character_id` on
   `StateToolContext` + the executor wire (executor.rs:1035-1040),
   underscore protection on the new tiers (set/delete only), the
   merged fetch via the cascade, group set/delete via
   `repos.groups.update`-equivalent overlay write, general set/delete
   via the general-state module, all output shapes + error strings
   verbatim, `format_state_results` unchanged arms intact. Update the
   state definition bytes in `definitions/data.rs` to v4's new JSON
   verbatim. **Differentials:** extend the state-sql fixture builder
   (groups ×2 + memberships + a character-scoped chat + the general
   mount `state.json`) and the oracle case with v4's
   `state-handler.test.ts` families (merged fetch 0/1/2 groups; group
   context by-sole/by-id/by-name/ambiguous; general round-trip;
   underscore refusals on group set + general delete; project-absent
   back-compat error) — keep the existing 20 state cases green;
   regenerate + re-run `tool_definitions` (+canonical) and re-run
   `tool_wire` + `pseudo_tool_prompts` as guards.
4. **The nine state dispatch verbs** per Shared contract §A (chat:
   enriched cascade get-state + set + reset; group: own-state get +
   set + reset; general: get + set + reset), engine dispatch arms, and
   the round's wire-contract pin (a `p4_d10_wire_contract`-style test
   naming the new tags, per the P4.6ar precedent). **Differential:** a
   new `state-routes` jest oracle direct-driving v4's route handlers
   (`handleGetState`(chat) incl. the omit-when-empty + groupTier
   enrichment + ambiguous case, group get/set/reset, general-state
   GET/PUT/DELETE incl. the malformed-body arm) against v5 dispatch
   over the same extended fixture family as unit 3.
5. **Pascal `$state` end-to-end.** `custom_tool_types.rs`:
   `StateRefSchema` + `is_state_ref` + the five union insertions + the
   fallback-typing validation rules + `validateRollRefs` /
   `resolveOperandType` messages (Zod-faithful — remember the
   P4.6ay-units-1-3 abort/hoist rules and the Zod-4 no-received-clause
   note). `custom_tools.rs`: `CustomToolState`, `resolve_state_value`
   (typeof-match + finite guard), threading through
   params/rolls/operands/templates/subjects/simulate exactly as
   surveyed (roll throws, operands never throw, template placeholder
   left verbatim). `run_custom.rs`: the entrance cascade
   (character/none scope, fail-soft `{}`). The chat custom-tool run
   verb handler: the `asCharacterId`-scoped cascade (same fail-soft).
   `workbench.rs` + the workbench dispatch verbs: the optional `state`
   field (§B), threaded into preview's `execute_custom_tool` and
   audit's `simulate_outcomes` (new trailing param). **Differentials:**
   extend the §C generator with v4's `custom-tools-state.test.ts`
   schema families and REGENERATE the corpus (§C — you own the file);
   extend the execution oracle with the resolution/threading/template/
   simulate families; rebuild + recommit
   `pascal-run-custom-{main,mount}.db` (the entrance cascade needs
   groups/state seeds) and extend its oracle; extend
   `workbench-route-cases.json` with mock-state preview/audit cases;
   re-run the whole pascal differential family by name over fresh
   `7e6d13e5` oracles.
6. **The math note.** `MATH_FORMATTING_INSTRUCTION` as a const in
   `system_prompt.rs` (v4's resolved bytes — single-backslash LaTeX),
   pushed unconditionally between the roleplay-template and
   tool-instructions pushes. **Differential:** regenerate + re-run
   `system_prompt_equivalence` (the note must appear on BOTH sides);
   re-run the tier-3 turn families (`orchestrator_tier3`,
   `primary_stream_tier3`, `native_tool_loop_tier3`,
   `text_tool_loop_tier3`, `message_finalizer_tier3`) over fresh
   oracles as guards.
7. **The full-suite regen discipline.** Every differential this lane
   touches runs over oracles regenerated from the `7e6d13e5` pin, by
   name, `--nocapture`, grep for SKIP (the P4.d3 memory; guessed
   `QT_ORACLE_*` names silently SKIP — the P4.d9 memory).
8. **The release-sweep neutrality tripwire (`93604767` + `28e89f51`).**
   v4 claims both commits are behavior-neutral; prove it the
   differential way, not by inspection. Enumerate every oracle family
   whose v4 transitive inputs include a file from the two commits'
   file lists (grep the oracle cases' imports — the P4.d9
   seven-family precedent), regenerate each at the pin, and re-run BY
   NAME, `--nocapture`, zero SKIPs. At minimum that sweep includes:
   memory-weighting (`QT_ORACLE_WEIGHTING`), the dice/rng family, the
   skip-signal + turn-manager families (incl. the 106-row P4.d4
   differential), the wardrobe tool-handler differentials, whisper /
   core-whisper, the image-generation handler family, the
   message-formatter consumers (the five tier-3 turn guards — unit 6
   already re-runs them), the vault-overlay parser families
   (characters/metadata/wardrobe vault reads), the mount/database-store
   families, the salon participants/merge families, answer-confirmation
   (cheap-llm), the document-mode (operator-doc-actions) family, and
   the home differential. **All green = the neutrality claim holds and
   the sweep is DONE — no v5 source change for these two commits. Any
   red = the claim failed for that family: STOP and report the diff
   (that's the tripwire firing as designed — the fix is a classified
   re-port, not a test edit).**
9. **The renderer-transitive neutrality re-proof (`5915b04e`).** The
   math promotion sits inside v4's renderer, which seven committed
   oracle families import transitively (salon-reads / -mutations /
   -skip / -swipe-generate, text-replacements-routes,
   cost-background-routes, courier-images-routes). P4.d9 proved
   `renderedHtml` never reaches the diffed payloads; re-prove it at
   this baseline the same way: regenerate the seven at the pin, re-run
   by name, expect green with committed oracles behavior-unchanged.
   (The SPA-side re-port itself is lane D11's.)

### Tier 2 — should land

1. **The D23 zero-diff re-dump.** Re-dump `fresh_schema.json` from v4
   `7e6d13e5`'s live `generateDDL` and verify ZERO diff (neither the
   feature nor the release sweep touches the schema — verified no
   `lib/database` DDL file changed; prove it, don't assume it).
2. **The `docs/v4/` mirror refresh** for the drift's doc changes
   (`docs/developer/features/state-cascade.md` NEW,
   `pascal-custom-tools.md`, `API.md` — now +364 with the all-routes
   documentation, `DDL.md`, `docs/releases/4.8.0.md`,
   `DEAD-CODE-REPORT.md` — mirror whatever subset the existing mirror
   convention covers).
3. **Import-side fileType acceptance.** v4's export schema now admits
   `fileType: json|jsonl` documentStore documents (general state rides
   exports). Verify `quilltap_core::services::quilltap_import`'s seed
   subset does not REJECT such documents; if it constrains fileType to
   markdown/txt, widen to v4's acceptance (with a differential case if
   any behavior actually changes; if v5 already passes fileType
   through untyped, record the verification in the status log and move
   on).
4. **The `2363c518` import-parse check** (from the release-sweep
   table): confirm `parse_export_file` tolerates chat records missing
   `commonplaceRecallHistory`/`commonplaceSceneCache`; record the
   disposition.
5. **The OpenRouter pricing disposition.** v4's `pricing-fetcher.ts`
   moved to `@openrouter/sdk` 0.13's paginated `models.list()`
   (`f495c9c9`). v5's pricing fetch hits the OpenRouter REST endpoint
   directly (no JS SDK) — verify its model-list path still matches
   v4's observable output (same fields, full list), and record that
   the SDK pagination is v4-internal. Only if the REST endpoint
   itself paginates against v5's request is there real work — then
   STOP and report (that's new scope, not this order's).
6. **The `8ee56f6e` corpus bank.** The five new v4 regression
   families document pre-baseline fixes. Spot-check the one with the
   sharpest port risk — the Anthropic model-family params behavior
   (new-gen models omit sampling params + adaptive thinking; exact
   prefix boundary `claude-opus-4-6` vs `-4-8`) — against v5's
   Anthropic request-builder differential corpus; if the boundary
   cases are uncovered, extend the corpus (behavior should already
   match — a mismatch is a banked pre-existing gap: STOP and report).
   Bank the other four families as corpus-enrichment notes in the
   status log.

### Tier 3 — explicit deferrals (loud, named)

- **`persist` (writing state back from a roll):** deferred in v4
  itself — nothing to arm; carry v4's unknown-key tolerance so a
  future `persist` file parses.
- **The chat-tier state editor UI opener:** v4's opener lives in the
  unported `ChatSidebar` — lane BE ships the modal's chat mode
  dormant; the opener lands with the ChatSidebar slice (already a
  named p4.9j follow-up).
- **help/*.md, qtap-export.schema.json:** dispositioned above (no
  action; documented absences stand).

## The differential requirement, restated

Every ported unit ships with an equivalence test driving v4's REAL code
at `7e6d13e5` (jest oracle → NDJSON → Rust diff; the
`jest-real-db-oracle` + `jest-oracle-mechanism` memory recipes; fixture
DBs via the committed builders; direct-drive route handlers per the
P4.d7 memory). New/extended oracle cases must import from the PIN
worktree path when regenerating (the `oracle-regen-from-worktree-path`
memory: tsx must import the WORKTREE case file). Watch the
`oracle-fire-and-forget-tail` and `oracle-regen-silent-stale-pass`
memories when extending mutating cases.

## Fixtures

- **Consumes:** `state-sql-tools.json` + `build-state-sql-tools-fixture.ts`
  (extends both), `pascal-run-custom-{main,mount}.db` (rebuilds +
  recommits via its builder), `workbench-{main,mount}.db` (unchanged
  unless mock-state cases need seeds — prefer spec-file cases),
  `workbench-route-cases.json` (extends), `groups-projects.json`
  (read-only; do NOT extend — state route cases ride the state fixture
  family).
- **Delivers:** the extended state fixture family (builder + spec),
  the regenerated §C corpus (§C below), the rebuilt
  pascal-run-custom DBs, the extended workbench route cases.
- **Owns regenerating** every oracle its units touch, from the
  `7e6d13e5` pin.

## Shared contract (BINDING — identical in `p4.6be-state-cascade-spa.md`)

### §A — the state dispatch verbs (names + payloads + response bodies)

Nine new CoreRequest variants (serde `rename_all="camelCase"` tags in
comments); response BODIES are v4's route JSON verbatim (envelope per
the existing dispatch convention):

```rust
/// v4 `GET /api/v1/chats/[id]?action=get-state` (participants-union cascade).
ChatStateGet { chat_id: String },                              // "chatStateGet"
/// v4 `PUT /api/v1/chats/[id]?action=set-state`.
ChatStateSet { chat_id: String, state: serde_json::Value },    // "chatStateSet"
/// v4 `DELETE /api/v1/chats/[id]?action=reset-state`.
ChatStateReset { chat_id: String },                            // "chatStateReset"
/// v4 `GET /api/v1/groups/[id]?action=get-state` (OWN state, no cascade).
GroupStateGet { group_id: String },                            // "groupStateGet"
GroupStateSet { group_id: String, state: serde_json::Value },  // "groupStateSet"
GroupStateReset { group_id: String },                          // "groupStateReset"
/// v4 `/api/v1/settings/general-state` GET / PUT / DELETE.
GeneralStateGet {},                                            // "generalStateGet"
GeneralStateSet { state: serde_json::Value },                  // "generalStateSet"
GeneralStateReset {},                                          // "generalStateReset"
```

Response bodies (exact keys):

- `chatStateGet` → `{ "success": true, "state": <merged>, "chatState":
  <obj>, "projectState"?: <obj>, "groupState"?: <obj>,
  "generalState"?: <obj>, "groupTier": <GroupTier>, "projectId"?:
  <string> }` — projectState/groupState/generalState **OMITTED when
  the tier object has zero keys**; projectId omitted when the chat has
  none; `state`/`chatState`/`groupTier` always present.
- `chatStateSet` / `groupStateSet` / `generalStateSet` →
  `{ "success": true, "state": <obj> }`.
- `chatStateReset` / `groupStateReset` / `generalStateReset` →
  `{ "success": true, "previousState": <obj> }`.
- `groupStateGet` / `generalStateGet` → `{ "success": true, "state": <obj> }`.
- `GroupTier` = `{ "status": "none"|"single"|"ambiguous",
  "candidates": [{ "id": string, "name": string }],
  "appliedGroupId"?: string }` (appliedGroupId only when single).
- Set bodies validate v4's `stateBodySchema` (`state` must be a JSON
  object of unknowns); 404s use the existing not-found envelope
  (`Chat not found` / `Group not found`).

The SPA mirrors these name-for-name in `core-contract.ts`; lane D10's
wire-contract test pins the tags.

### §B — the workbench payload additions

The workbench preview and audit dispatch verbs each gain ONE optional
field, name `state`, type: JSON object or null/absent (absent/null →
`{}`): mock merged state for `$state` refs and `{{state.path}}`
templates; audit holds it fixed across every draw. No other workbench
payload change. The chat custom-tool RUN verb's payload is UNCHANGED
(state resolution is server-side off the existing `asCharacterId`).

### §C — the definition corpus wire (unifier-applied)

The committed corpus
`apps/web/src/testing/fixtures/pascal-custom-tool-definition.oracle.ndjson`
(159 rows = 10 title + 149 definition at planning time) is owned by
**lane D10**, which extends its single generator
(`harness/oracle/cases/pascal-custom-tool-definition.ts`) with the
`$state` schema families and regenerates the corpus at `7e6d13e5` —
the ONE file lane D10 may touch under `apps/web/`. Lane BE ports the
SPA schema against the same v4 behavior but does NOT edit the corpus
or its row-count guard; **the unifier updates the count literals in
`apps/web/src/app/pascal/custom-tool-types.corpus.spec.ts` to lane
D10's final counts (recorded in lane D10's status header at lane
close) and re-runs both consumers.**

### §D — the StateRef JSON shape

Everywhere both sides see it (definitions, drafts, the schema asset):
`{ "$state": <non-empty string path>, "fallback": <number | string |
boolean> }` — strict (no extra keys), `fallback` REQUIRED and typing
the ref. Draft-layer representations are lane BE's internal concern;
the wire/file shape is this.

### §E — version-bump ownership

Lane D10 bumps `quilltap-core`, `quilltap-harness`, `quilltap-host`,
`quilltap-web` (as touched, per commit) and keeps `Cargo.lock` in
step; lane BE bumps `apps/web/package.json` only. `CHANGELOG` and
`status-log.md` are append-only for both (union-merge at unification).

## Ownership (BINDING — identical in `p4.6be-state-cascade-spa.md`)

- **Lane D10 owns:** `crates/**`, `Cargo.toml`/`Cargo.lock`,
  `harness/oracle/**`, `docs/v4/**`, plus the single carve-out
  `apps/web/src/testing/fixtures/pascal-custom-tool-definition.oracle.ndjson`.
- **Lane BE owns:** `apps/web/**` (everything EXCEPT that corpus
  file), including `apps/web/public/schemas/qtap-custom-tool.schema.json`.
- **Both append-only:** `docs/CHANGELOG.md`,
  `docs/developer/porting/status-log.md`.
- **Neither touches:** `CLAUDE.md` status (the unifier's),
  `docs/developer/porting/phase-4.md`, the other lane's files.

## Verification gate (run before declaring done)

1. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets
   -- -D warnings` for BOTH feature sets (default +
   `quilltap-core/native-transport`).
2. `cargo test --workspace --no-fail-fast` — zero failures; note the
   suite/test counts.
3. Every differential this lane touched, run BY NAME over oracles
   freshly regenerated from `/private/tmp/qt-v4-pin-7e6d13e5`,
   `--nocapture`, output grepped for SKIP (zero SKIPs): the state
   family (paths/cascade/general-state/state-sql/state-routes), the
   tool-definition pair + tool_wire + pseudo_tool_prompts guards, the
   full pascal family, system_prompt, and the five tier-3 turn guards.
4. `cargo test -p quilltap-harness` with NO env vars (the harness
   self-test).
5. The per-commit rule: follow `.claude/commands/commit.md` for every
   commit (equivalence test present, version bumps per §E, CHANGELOG
   every commit, status-log unit records).
6. At lane close: record in the status header the final §C corpus row
   counts (total / title / definition / accept / reject) for the
   unifier's §C wire, and the list of rebuilt committed fixtures.
