# Work order P4.D120 — the `d25dacc1` archive-instead-of-delete drift, server half: the scenario `archived` frontmatter (all four scopes), the wardrobe `archived`→`archivedAt` controls, `includeArchived` end-to-end, the Green Room pins, the Almanack column

**Status:** CLOSED — UNIFIED on main (2026-08-26, the P4.D119 stacked lane,
unified on `unify/b220999d-round`). Tier 1 items 1–8 ALL LANDED (the
scenarios chokepoint with default suppression; the character-scenario half
including the `build_scenario_file` rewrite — **v5's description-drop bug
proven RED-FIRST**; `includeArchived` on the seven scenario + four wardrobe
list verbs AND the nine scenario mutate verbs' fresh-list returns, with the
two formerly-hard-coded-`true` wardrobe reads proven red-first; the
`archived_patch` idempotence quartet with the two new 404 sentences; the
system-prompt guard at exactly v4's three sites; the Almanack scenario
column; the pin suites incl. never-auditions and `archived_wearer_read_
guard`). Tier 2 item 9 landed on the two existing edges; item 10's v4-side
filing candidates recorded. Tier 3: the `qtap-export.schema.json` file port
stays a NAMED standalone flag (v5 has never shipped the file); the seven
help files banked to `p4.9i2`. **The §3 unification review caught the
`archived: null` silent-keep on these surfaces, fixed on the unify branch
(`73fa6ea8` + `ac0dfd75`):** an explicit null answered 200-and-preserve
where v4's `z.boolean().optional()` refuses — fixed with Zod 4's measured
sentences on the bag arms and a doubled Option on the two
character-scenario verbs (parse-before-404 as v4 orders it THERE), with
oracle arms on both the file-backed and character corpora; the sibling
name/description/isDefault null-tolerance is a recorded PRE-EXISTING lead
of the same class (not this round's field). Lane record: `status-log.md` →
"P4.D120".

## Preamble

- **v4 baseline for this round: `8f910137`**; the round's five-commit drift
  map, the freshness-probe rule, the sibling list, and the worktree note are
  in the P4.D119 preamble and apply verbatim here. **Lane pin:
  `/tmp/qt-v4-pin-p4d119-d25dacc1` (shared with P4.D119 — same lane).**
  Spec reads for THIS order come from `git show d25dacc1:<path>` and
  `git show d25dacc1 -- <path>`; regenerate each affected family ONCE,
  after both stacked orders' edits to its surface are in. ⚠ Port from the
  shipped hunks, never the commit message (ledger §5.3) — **this commit's
  message is wrong in two named places** (survey §E: `unarchive` gained NO
  caller, and an archived character-default CAN still be auto-selected).
- **Sibling lanes:** P4.D121 (the client half of both stacked orders — meets
  this order at §Shared contract part B), P4.D122 (disjoint). §Ownership is
  the binding table in P4.D119/P4.D122, identical here by reference.

## Mandate

Port the server half of v4 `d25dacc1` ("archive entries instead of deleting
them"): the **`archived` frontmatter** through the scenarios chokepoint
(parse/serialize/list + the default-suppression rule) and its three scope
wrappers, the **character-scenario half** through the vault overlay
(including v4's `description` round-trip fix — **v5 replicates that bug
verbatim today**), the **`includeArchived` option** on every scenario and
wardrobe list surface (dispatch fields; URL readers only on the REST edges
that exist), the **wardrobe `archived: boolean` → `archivedAt` patch
semantics** on all four item-update surfaces, the **server-side filtering**
replacing the two hard-coded `true` reads, the **Green Room / outfit-LLM
pins** (the exclusion already exists in v5's `wearable_pool.rs` — the work
is pinning it), the **system-prompt `firstActiveScenarioContent` guard**,
the **Almanack scenario archived column**, and the export-carry pins. Every
unit ships with its differential against v4's REAL code.

**Must NOT touch:** anything under `apps/web/` (P4.D121);
`services/scenario_selection.rs` and `services/chat_scenario.rs` behavior —
**they deliberately ignore `archived`** (the "a chat keeps its scenario"
guarantee; carry v4's new doc-comment onto `resolve_scenario_body`, change
no logic); the P4.D122-owned files. v4's seven help-file deltas bank to
**`p4.9i2`**; `docs/developer/API.md` is v4-side docs.

## Survey-verified starting points (fresh survey 2026-08-25, at `d25dacc1`; 84 files, +2411/−253)

### v4 — the scenarios chokepoint (`lib/mount-index/scenarios-common.ts`, +53)

- `ParsedScenario.archived: boolean` ("absence means active");
  `isArchivedFrontmatter` truthy ONLY for real `true` or the string
  `"true"` (the `isDefault` coercion mirrored); **exported**
  `isScenarioContentArchived(content)` reading the flag off raw content —
  used by all three PUT handlers so an unmentioned `archived` survives the
  whole-file rewrite.
- `listScenariosInFolder(mount, folder, {includeArchived?})`: filter
  `archived && !includeArchived → continue` BEFORE default resolution;
  inside the default loop, an archived scenario is force-set
  `isDefault: false` and `continue`d **even when listed** (it can never be
  the warning's "winner" either; `rawIsDefault` untouched — the file's own
  claim still surfaces; the warning text unchanged). The default-SET
  writers are untouched — nothing server-side stops writing
  `isDefault: true` onto an archived file; the READ path neutralizes it
  (survey §E.9 — reproduce, don't guard).
- `buildScenarioFileContent` gains `archived?: boolean`: "Omission means
  active — never write `archived: false`"; emitted frontmatter key order
  `name, description, isDefault, archived`.
- The three wrappers (`general-scenarios.ts:66`, `group-scenarios.ts:64`,
  `project-scenarios.ts:52`) just forward `options`; each module header
  gains the `archived: true # hidden from lists unless "Show archived"`
  doc line.

### v4 — the query-param reader (`lib/api/query-params.ts`, NEW)

`readIncludeArchived(req)`: `raw === 'true' || raw === ''` — literal
`true` OR the bare valueless `?includeArchived`; everything else
(`1`, `TRUE`, `yes`) is false; unparseable URL falls CLOSED. Eleven v4
routes share it. **In v5 this becomes a `#[serde(default)]
include_archived: bool`-style field on the list verbs** (dispatch has no
URL); the exact string spelling matters only at the REST edges that
exist (below).

### v4 — character scenarios (the projection landmine)

- `CharacterScenarioSchema.archived: z.boolean().optional()` with the
  eight-line why-comment: the vault read must NOT filter, because the
  write overlay projects `character.scenarios` back over `Scenarios/` and
  deletes every file not in the array.
- `parseScenarioFile` reads `parsed.data?.archived === true || === 'true'`
  and spreads `...(archived && { archived: true })` — **the key is ABSENT
  for active scenarios** (v4's API.md example claiming `"archived": false`
  is a doc/behavior mismatch; follow the code).
- The route GET filter is a **response filter only**
  (`all.filter(s => s.archived !== true)` unless includeArchived).
- `addScenario` spreads `...(data.archived === true && {archived: true})`;
  `updateScenario` recomputes `data.archived ?? existing.archived` and
  re-adds ONLY when `=== true` — a restore genuinely DELETES the key.
- **`buildScenarioFile` — the fix v5 needs for its own sake** (v4 hunk
  verbatim in the survey): frontmatter block emitted ONLY when there is
  something to put in it (`description` first — trimmed, `escapeYaml`'d —
  then `archived: true`), hand-built `---\n…\n---\n\n` + `# title`;
  files using neither key don't churn; the id is a path-hashed UUID so
  archiving must never rename the file. **v5's
  `vault_overlay.rs:697-699` is the pre-fix one-liner
  (`format!("# {title}\n\n{content}")`) — v5 measurably HAS the
  description-drop bug**; callers `vault_character_write.rs:347`,
  `vault_character_update.rs:295`.

### v4 — routes (exact shapes; NO new error sentences on the scenario side)

All five per-scope route methods (list GET, create POST, update PUT,
rename action, DELETE) honour `?includeArchived` on their fresh-list
returns; the union route forwards it per group. Zod additions verbatim:
`archived: z.boolean().optional()` on create AND update (update's
doc-comment: omit to leave the state alone — the serializer rewrites the
whole file). ⚠ **Collection-POST quirk (survey §E.4, reproduce):** the
three file-backed create POSTs refresh their returned list with
`{includeArchived: validated.archived === true}` — the BODY, not the
query param — so creating an active scenario with "Show archived" ticked
returns an archived-free list. Item-level PUT/POST/DELETE read the query
param correctly.

Wardrobe item PUTs: `lib/wardrobe/archived-patch.ts` (NEW, 26 lines) —
`archivedPatch(currentArchivedAt, archived, now)`: no-op `null` when
state already matches (idempotent — the original stamp survives),
`{archivedAt: now}` / `{archivedAt: null}` otherwise; `now` is
`new Date().toISOString()` at every call site. General/character routes
use the already-loaded `existing`; project/group do an extra
`read*Wardrobe(mount, true)` **only when `archived !== undefined`**, with
the two NEW 404 sentences `Project wardrobe item not found` /
`Group wardrobe item not found` (reachable only with `archived` in the
body). Collection GETs: General `findArchetypes(includeArchived)`;
character `findByCharacterId(id, includeArchived)` + the `?scope=group`
arm `findArchetypesInMounts(ids, includeArchived)`; **project and group
replace hard-coded `true` with the param** — the "server-side filtering
replaces the client-side pass". No v4 repository code changed (the flags
pre-existed).

### v4 — Green Room / prompts / Almanack / export

- **No source change on the candidate pool** — `mergeWearablePool`
  already filtered; the commit PINS it: the 237-line
  `archived-never-auditions` suite (all four tiers, no override, no
  parameter), the two outfit-selection cases ("drops an ARCHIVED id a
  model conjured", "would not accept an archived garment even if one
  leaked into the pool" — documenting the pool as the ONLY guard), the
  `resolve-equipped` opposite-direction pin (the equip read MUST pass
  `true` or a garment archived mid-chat loses its title), and the
  `archived-survives-projection` suite (including the failure shape:
  filter first and the file IS swept).
- `lib/characters/active-scenarios.ts` (NEW): `activeScenarios` /
  `firstActiveScenarioContent`; converted call sites are EXACTLY three —
  `system-prompt-builder.ts:161` + `:373`, help-chat `:50`. ⚠ Three
  sites were NOT converted (scene-state-tracking :220, Carina
  `resolveDefaultScenario` :119 — which also honours an archived
  `defaultScenarioId` — and the SillyTavern export): **convert only what
  v4 converted**; record the three misses as v4-side filing candidates.
- Almanack: `ScenarioTierRow` gains `archived` (v5
  `almanack/types.rs:803-806` — compare `WardrobeTierRow` :748 which
  already has it); `phase4-scriptorium` adds
  `countLinksMatchingContent(ids, SCENARIOS_FOLDER, '%archived: true%')`
  (v5 helper exists, used for wardrobe at `phase4_scriptorium.rs:561`);
  `render` grows the `| Tier | Scenarios | Archived |` column — the
  `*No scenarios*` empty-state still tests `count === 0` only.
- Export: **no writer/reader code changed in v4** — both fields already
  rode along structurally; v4 only declared them in
  `public/schemas/qtap-export.schema.json` (+12; the two description
  strings in the survey) and pinned round-trip with a validator test.
  **v5 has NOT ported that schema file at all** (only
  `qtap-custom-tool.schema.json` ships) — record the flag; do NOT invent
  the schema-file port in this lane. v5's
  `qtap_export/schema-key-order.json` already lists `scenarios` and
  `archivedAt`. The v5 deliverable is the round-trip PIN (an archived
  scenario + archived garment survive export→import) in the existing
  qtap families.

### v4 — quirks/gotchas to reproduce faithfully (survey §E)

`repos.wardrobe.unarchive` stays DEAD (do not port it as a live path —
all four routes patch `archivedAt` directly); the bare `?includeArchived`
spelling; the absent-when-false response key; the project/group
O(folder) pre-read; the unguarded default-SET write path. The New-Chat
character-default hole and the NewChatForm keep-selected-visible
exception are CLIENT facts — P4.D121's to reproduce.

### v5 — where the counterparts live (verified 2026-08-25; survey §F)

- Scenarios chokepoint: `db/scenarios.rs` — `ParsedScenario` :129-148,
  `parse_scenario_doc` :195-248 (the `isDefault` coercion :218 is the
  model), `list_scenarios_in_folder` :256, `build_scenario_file_content`
  :397-429 (fifth arg — signature change, 3+ call sites),
  `resolve_scenario_body` :51 (doc-comment only),
  `read_scenario_by_path` :318. New `is_scenario_content_archived` via
  `crate::markdown::parse_frontmatter`.
- API bags: `api/scenarios.rs:76` `collect_bag_issues` + :124-140
  `extract_fields` — needs `archived` as a TRI-STATE `Option<bool>` to
  reproduce preserve-on-omission (memories:
  `edge-must-decode-through-the-request-enum.md`,
  `harness-spec-typed-option-collapses-tristate.md`). Scope handlers:
  `api/scenarios.rs` `list_body` :166, `api/projects.rs`
  `project_scenario_*`, `api/groups.rs` `group_scenario_*` +
  `group_scenarios_union`.
- Character scenarios: `vault_overlay.rs` `parse_scenario_file` :914-980
  (parses `description` :929-932, no `archived`) +
  **`build_scenario_file` :697-699 (the bug)**;
  `db/vault_character_arrays.rs` `add_scenario` :251 / `update_scenario`
  :280 (need the tri-state); the GET response filter at
  `api/characters.rs:777-779` (`sub_array_read` needs a scenario-specific
  filter or bypass); create/update handlers :2068/:2094.
- System prompt: `system_prompt.rs:167-175` holds the
  `scenarioText || scenarios[0].content || ''` chain; `ScenarioEntry`
  :63-64 is content-only — needs `archived`. Convert the identity-stack +
  system-prompt + help-chat sites only.
- Wardrobe: NEW `archived_patch` fn (home per lane's judgment);
  `api/wardrobe.rs` `wardrobe_update` ~:300 over `parse_archetype_body`
  :97 (the Zod PASS/FAIL port — extend it); `api/characters.rs`
  `character_wardrobe_update`; `api/projects.rs` `project_wardrobe_update`
  ~:1400 and `api/groups.rs` `group_wardrobe_update` ~:1200 over
  `db::vault_wardrobe_write::update_*`. Collection reads:
  `api/wardrobe.rs:207` (already-false default — thread the param),
  `api/characters.rs:815-855` (:836/:847), and the two hard-coded `true`s
  at **`api/projects.rs:1247-1255`** and **`api/groups.rs:1140-1148`**.
  The repo plumbing (`db/wardrobe_read.rs:57,96`,
  `db/archetype_wardrobe.rs:62-265`) is ALREADY fully ported.
  `wearable_pool.rs:18/:72` already excludes — pins only.
  `api/chat_outfits.rs:106` already documents `includeArchived: true`.
- Verbs to extend (`api/types.rs`): `CharacterScenarioList` :547,
  `CharacterScenarioCreate` :551, `CharacterScenarioUpdate` :557,
  `GroupScenarioList` :760, `GroupScenariosUnion` :829, `ScenarioList`
  :835, `ProjectScenarioList` :1269; `CharacterWardrobeList` :593,
  `GroupWardrobeList` :799, `ProjectWardrobeList` :1306, `WardrobeList`
  :2412. The four item-update verbs take `item: Value` — `archived`
  arrives free; the HANDLING is the port.
- REST edges: `wardrobe_routes.rs:67` `wardrobe_get` (no Query extractor
  today — add one with v4's exact `'true'`/`''` spelling) and
  `characters_routes.rs:355` `characters_wardrobe_get` (has one, for
  `scope`). Project/group wardrobe and all scenario surfaces are
  dispatch-only — no new REST edges (the P4.D112/D115 precedent; record
  per surface).
- Almanack: `almanack/types.rs:803-806`, `phase4_scriptorium.rs:596-602`
  (+ the existing `count_links_matching_content`), `render.rs:1639-1649`.
- Harness families (survey §F.5 carries the full table with roles):
  primary homes are `scenarios_routes_equivalence` (the archived
  frontmatter + includeArchived + default-suppression corpus),
  `characters_subresources_equivalence` (character-scenario flag),
  `vault_character_write_equivalence` (the description fix + the
  archived-survives-projection shape), `wardrobe_routes_equivalence` +
  `characters_wardrobe_route` (web crate, committed
  `wardrobe-routes-*.db`), `group_wardrobe_routes_equivalence` (v4
  itself added two cases here this commit — mirror them),
  `projects_routes_equivalence` (the project hard-coded-true site),
  `wardrobe_tier2_equivalence` (the archivedAt write shape),
  `outfit_llm_choose_tier3_equivalence` (never-auditions),
  `chats_outfits_tier2_equivalence` (equip-reads-archived),
  `scenario_resolvers_equivalence` + `chat_scenario_routes_equivalence`
  (archived-still-resolves arms), `almanack_render_equivalence` +
  `almanack_tier2_equivalence`, `wardrobe_transfers_tier2_equivalence`
  (archivedAt survives a transfer), qtap export/import families (the
  round-trip pin).

## Tiered deliverables

**Tier 1 (must land):**

1. The scenarios chokepoint: `archived` through `ParsedScenario` /
   `parse_scenario_doc` (the `"true"`-string coercion) /
   `is_scenario_content_archived` / `list_scenarios_in_folder`
   (`include_archived` + BOTH filter arms, default-suppression exactly
   placed) / `build_scenario_file_content` (omission-means-active, the
   frontmatter key order), the three wrappers, v4's doc-comments carried
   (incl. the `resolve_scenario_body` deliberately-ignorant note).
2. The character-scenario half: schema + parser (`absent-when-false`
   spread), **the `build_scenario_file` rewrite** (description-first
   frontmatter, only-when-needed, `escapeYaml`, no-rename invariant),
   `add_scenario`/`update_scenario` tri-state semantics
   (restore-deletes-the-key), the GET response filter.
3. `includeArchived` on the seven scenario verbs + four wardrobe list
   verbs (`#[serde(default)]`), threaded to the readers; the two
   hard-coded `true`s replaced; the fresh-list returns honouring it at
   every item-level scenario mutation — and the collection-POST
   body-not-param quirk reproduced.
4. The wardrobe `archived_patch` module + the four item-update surfaces
   (idempotence, the conditional project/group pre-read with the two new
   404 sentences, the log-line `archivedAt` rider).
5. The system-prompt guard: `active_scenarios` /
   `first_active_scenario_content` at exactly v4's three converted sites;
   `ScenarioEntry.archived`.
6. The Almanack scenario archived column (types + count + render).
7. The pin suites: never-auditions (all four tiers, at the tier-3
   outfit-LLM family), the leaked-pool documentation case, equip-reads-
   archived, archived-survives-projection (incl. the failure shape), the
   qtap export→import round-trip arms, and the REST-edge query readers'
   exact `'true'`/`''` spelling.
8. The differentials for all of the above (see §Differential).

**Tier 2 (should land):**

9. The `?includeArchived` URL reader on the two existing wardrobe REST
   edges (`wardrobe_get`, `characters_wardrobe_get`) with v4's spelling;
   dispatch-only decisions recorded for every other surface.
10. The three unconverted-`scenarios[0]` sites and the unguarded
    default-SET write recorded as v4-side filing candidates in the lane
    record (with the survey's line references).

**Tier 3 (explicit loud deferrals, never silent stubs):**

11. The `qtap-export.schema.json` file port (v5 has never shipped it —
    a named standalone flag for a future order, NOT this lane).
12. The `p4.9i2` bank rows for the seven help files.

## The differential requirement, restated

Every ported unit ships with an equivalence test driving v4's REAL code at
the lane pin. Corpus floors (v4's own new suites are the baseline —
`archived-never-auditions` 237 lines, `archived-survives-projection` 160,
the scenario chokepoint suite, the qtap validator case):

- **Scenarios routes:** extend `scenarios-routes.test.ts` per scope:
  archived file hidden by default / listed with the flag / bare
  `?includeArchived` spelling at any REST edge that speaks URLs /
  `"true"`-string frontmatter coercion / archived+isDefault loses
  default resolution AND cannot be the warning's winner / create-with-
  archived / update-omission-preserves (via `is_scenario_content_archived`)
  / restore-deletes-the-key / the collection-POST body-not-param refresh
  quirk / DELETE+rename fresh lists honouring the flag. Writes proven
  through file bytes (frontmatter key order included), not just bodies.
- **Character scenarios:** extend `characters_subresources` +
  `vault_character_write`: the description round-trip RED-FIRST (v5's
  builder measurably drops it today — prove the red before the fix),
  frontmatter-only-when-needed (an untouched file's bytes identical),
  archived survives the projection sweep, absent-when-false in the GET.
- **Wardrobe routes:** the archivedPatch idempotence quartet
  (archive/re-archive keeps the stamp/restore/re-restore) per scope, the
  two new 404s, collection GETs default-hidden vs flagged (the two
  formerly-hard-coded-`true` scopes RED-FIRST — v5 measurably
  over-returns today), the `?scope=group` arm.
- **The pins:** never-auditions at `outfit_llm_choose_tier3` (each tier;
  mutation: remove the pool filter — exactly the new cases redden);
  equip-reads-archived at `chats_outfits_tier2`; the transfer-carries-
  `archivedAt` arm.
- **Neutrality regens:** `scenario_resolvers` + `chat_scenario_routes`
  (archived-still-resolves — v4 changed no logic there; expect
  byte-identical plus the new arms), `outfit_hash` (an archived-but-worn
  garment does not change the hash preimage).
- Mutation-proof the load-bearing arms per
  `fixture-side-mutation-proves-nothing.md`; grep every regenerated
  NDJSON for the changed bytes (`archived: true`, `archivedAt`,
  `Project wardrobe item not found`) — ledger §5.2.

## Fixtures

- Extends the scenarios/wardrobe families' staged fixtures through their
  own builders at the lane pin (archived scenario files at each scope, an
  archived garment per tier, an archived+isDefault file); the committed
  `wardrobe-routes-*.db` pair and `group-wardrobe`/`chat-scenario`
  committed families follow the committed-fixture rule — this lane owns
  regenerating every dependent oracle when one changes.
- Remember: store-overlay properties cannot be SQL-seeded; fixture-side
  mutations prove nothing.

## Shared contract (BINDING — identical text in `p4.d121-instructions-archive-spa.md`; §B of that order's two-part contract)

1. **List verbs gain `includeArchived: boolean`** (serde camelCase,
   `#[serde(default)]` — absent = false): `characterScenarioList`,
   `groupScenarioList`, `groupScenariosUnion`, `scenarioList`,
   `projectScenarioList`, `characterWardrobeList` (also honoured by its
   `scope: 'group'` arm), `groupWardrobeList`, `projectWardrobeList`,
   `wardrobeList`.
2. **Scenario create/update bags gain `archived?: boolean`**
   (update = tri-state: omitted preserves, `true` archives, `false`
   restores — restore DELETES the key from the file). Wardrobe item
   update bags gain `archived?: boolean` mapped through `archivedPatch`
   (idempotent; `archivedAt` ISO string | null).
3. **DTO shapes:** file-backed scenario list rows carry
   `archived: boolean` (always present); character-scenario objects carry
   `archived: true` ONLY when archived (absent = active) and now also
   round-trip `description`; wardrobe items keep their existing
   `archivedAt: string | null`.
4. **Default suppression:** an archived scenario is never `isDefault` in
   any list response (its `rawIsDefault` may still be true).
5. **Error arms:** the two new 404 sentences
   `Project wardrobe item not found` / `Group wardrobe item not found`
   (only reachable when `archived` is present in the body); NO other new
   error sentences anywhere on these surfaces.
6. **Client strings** (P4.D121 lands them; transcribed from
   `git show d25dacc1:<path>`, never from this order): `Show archived`
   checkbox everywhere v4 has one (scenarios manager, project wardrobe
   card, wardrobe dialog, ChatScenarioControl, NewChatForm — the last
   only when its callback prop is supplied); `Archive` / `Restore` /
   `Restore from archive`; badges `Archived` / lowercase `archived`;
   `ARCHIVED_OPTION_SUFFIX = ' (archived)'` appended after the default
   marker, before the ` — description`; the disabled-radio title
   `Archived scenarios cannot be the default`; the three
   Restore/Hide-from-pickers title pairs; `Scenario not found in current
   list`; the CharacterBasicInfo help paragraph. NO confirm dialogs —
   archive/restore is single-click, deliberately.
7. **Faithful client quirks** (P4.D121 reproduces, does not fix): the
   character default-scenario seed is UNGUARDED (an archived character
   default auto-selects, rendering with the suffix via the
   keep-selected-visible exception); the outfit composer surface gets NO
   toggle (an archived garment must never audition); the container
   resolution pool always fetches General archetypes WITH archived (a
   composite may bundle one).

## Ownership

The binding table lives identically in P4.D119 and P4.D122 (this order is
the same lane as P4.D119 — one worktree, one branch, one gate).

## Verification gate

Run ONCE for the stacked lane after this order — the gate text in
P4.D119 §Verification applies verbatim, plus: the two RED-FIRST proofs
(the description round-trip; the two formerly-hard-coded-`true` reads)
explicitly recorded with their pre-fix failure output.
