# Work order P4.72 — the P4.67 remainder (the other seventeen `?action=` sites, the per-site duplicate-key rows, the `chat_file_link` guard tidy) + the dispatch-level wrong-TYPE census

**Status:** **CLOSED for Tier 1 items 1–4 and Tier 2 item 5** (lane
`claude/p4-72-query-param-census-0c8180`, 2026-09-03). Every Tier-1 deliverable
landed; Tier 2's warn-line pins landed too. Tier 3 is discharged as recorded
deferrals, listed below.

**Landed:** the seventeen remaining `?action=` edges into
`query_param_semantics_equivalence` (the family now covers all thirty-one; 54
byte-compared refusal rows, was 20) with three hand-rolled gates added to the
refusal classifier (1); the per-site duplicate-key rows for `q`/`limit`/`offset`
on photos, `kind` on the image-aesthetics GET and `filePath` on the qtap-target
GET, each asserting BOTH halves (2); P4.62(c), the `chat_file_link` guard tidy
(3); `dispatch_wrong_type_census` — 237 rows over 124 variants, mechanically
derived and 127 of them DRIVEN (4); and the two `actionLogger.warn` lines with
a three-test thread-scoped capture pin (5).

**One v5 behavior change beyond the tests:** `POST /api/v1/system/unlock`
refused v4's four KNOWN-but-unaliased sibling actions as `Unknown action: <x>`
— a claim about v4 that is false, since v4 dispatches them. They now carry the
mount-point edge's loud pointer (the shape P4.67's §3 review restored
elsewhere), pinned in `UNSERVED_KNOWN_ACTIONS`.

**Deferred loudly, each with its measured reason** (details in the lane
records):

- the `force` duplicate row on `DELETE /api/v1/files/{id}` — v5 refuses an
  unforced delete only on a REAL association (a character image or a message
  attachment); `?action=link` writes only `linkedTo`, which both trees treat as
  stale, so the two values answer the same 200. Needs a venue whose seeded file
  is referenced by a character or a message.
- `scope` and `mountPoint` on the qtap-target route — neither answers
  differently until a chat AND a resolvable mount exist, and the route's
  chat-404 precedes both.
- the census's nested request structs (`PendingToolResult`,
  `ChatCreateParticipant`, `AnnouncerSenderWire`, …) — walking `Request`'s own
  fields is what makes the mechanical half total; the `ChatCreate` trio is
  carried explicitly because the order names it.
- the fixes the census names — next round, by row.

**TWO ORDER PREMISES REFUTED BY MEASUREMENT** (§6's `ChatCreate` trio): the
trio does NOT answer `dispatch.rs:72`'s envelope — `Request::ChatCreate`
carries a raw `Map`, so the typed decode is in the host driver
(`quilltap-host/src/spine.rs:1871`, `invalid chatCreate request: …`), which
widens the fix past the `api/types.rs` the order named; and `timestampConfig`
is already an `Option<Option<Value>>` carrier, so it is a HANDLER question, not
a decode one. Both are asserted in the census, not merely written down.

**A pre-existing vacuity found on the way:** `files_write_routes`' chat-file
`?action=link` and `DELETE ?force=true` legs had never run — the committed
`chat-send` fixture carries no `files` table at all. The table is now built in
the test from v4's own `generateDDL` shape and one row seeded.

_Original status: OPEN — not started (ordered 2026-09-03, the follow-ups round 2:
P4.72 ∥ P4.73 ∥ P4.74 ∥ P4.75). Written from P4.67's status header ("Still
OPEN — the P4.67 remainder"), `phase-4.md`'s candidate 2 at the `0b0617fee`
unification, and the `0b0617fee` round record's §3 review note on the
dispatch-level wrong-type class (`conciergeState` `42` → the decode envelope
where v4 answers the flat `Validation error`)._

## Preamble

- **v4 oracle baseline: `0b0617fee`** (drift-ledger §1; probe re-run
  2026-09-03 at ordering: v4 checkout on `main`, tree CLEAN, `git log
  15573c3a1..main` empty, `git log 3a76b17df..bugfix` empty). The ledger's
  §3 holds ONE row (`15573c3a1`, bug 119 — the unported character
  optimizer, `p4.9k`); it is not this lane's and touches nothing this lane
  reads.
- **Freshness probe at lane start** (drift-ledger §2 — the four read-only
  commands). Pass = v4 checkout on `main`, tree clean, `git log
  15573c3a1..main` empty and `git log 3a76b17df..bugfix` empty. **Any
  mismatch: STOP and report** — a lane never writes the ledger. Re-run the
  probe before every regen batch (the checkout has gone dirty mid-lane
  before — ledger §5.1).
- **Regen rule: PIN REQUIRED at `0b0617fee`** (ledger §1 — v4 HEAD is one
  commit past the baseline). Every oracle regen runs from a lane-unique
  detached worktree per ledger §5.1 (`PIN=/tmp/qt-v4-pin-p472-0b0617fee`,
  the three symlink classes) through the sweep driver: `python3
  harness/tools/recipe_sweep.py --run <family> --v5w <worktree> --v4
  "$PIN"`. **A committed recipe never names a `/tmp` pin** (the driver
  refuses `stale_v4_pin_path`; memory note `recipe-header-conventions`) —
  the recipe says `cd ~/source/quilltap-server`, the pin is the driver's.
  Verify each pin by grepping the fresh NDJSON for a marker only one tree
  has.
- **Sibling lanes (this round):** P4.73 (the `/api/v1/images` collection
  route + the host codec on every dispatch-side ingest — owns `api/types.rs`,
  `api/engine.rs`, `api/files.rs`, the NEW `api/images.rs`, the NEW
  `quilltap-web/src/images_routes.rs`, `quilltap-web/src/lib.rs`, the
  `quilltap-host` seams, `core-contract.ts`), P4.74 (core smalls — owns
  `services/{answer_confirmation,file_fallback,cost_events}.rs` and the
  named harness families), P4.75 (SPA smalls — owns the SPA + Playwright).
  §Shared contracts / §Ownership are binding and identical across all four
  orders.
- **Worktree:** per the `carryout` skill (`git worktree` off `main`;
  `apps/web/node_modules` via `npm ci`, never `cp -a`). `CHANGELOG` /
  `status-log.md` append-only. **No Playwright** (P4.75 owns port 4319);
  this lane has no SPA surface.
- **Long gates:** `run_in_background: true` + full log + sentinel; NEVER a
  `pgrep` loop; NEVER `| tail`. Never edit source during a workspace gate.
  `cargo test --workspace` is fail-fast per binary — read the whole log.
  A `QT_ORACLE_*` var pointing at a deleted `/tmp` path is a FAILURE that
  reads like a regression — `ls` the env block's paths first.

## Mandate

Finish P4.67: bring the **other seventeen** `?action=`-reading REST edges
into `query_param_semantics_equivalence` with the six shapes each, add the
per-site **duplicated-key** rows for the non-action keys the shared reader
classified, and land P4.62(c) (the `chat_file_link` `fileId is required`
guard tidy). Then **measure and record** the dispatch-level wrong-TYPE
class: for every `/api/dispatch` verb whose v4 twin runs a Zod `.parse` on
the request body, which present-but-wrong-typed fields collapse into the
serde decode envelope (`dispatch.rs:72` `Invalid request: <serde text>`)
where v4 answers the flat `Validation error` — an executable census with
the ordered per-verb shape, **not the fix** (the fix crosses three lanes'
ownership: `api/types.rs`, `api/engine.rs`, the handler files; the
`ChatCreate` trio is P4.73's Tier 2 because P4.73 owns those files this
round). Must NOT touch: `api/types.rs`, `api/engine.rs`, `api/files.rs`,
`quilltap-web/src/lib.rs`, the NEW `images_routes.rs`, any `services/**`
file, the host crate, the SPA, or the two same-file exception regions in
§F/§G.

## Survey-verified starting points (2026-09-03)

1. **v4's dispatcher is unchanged since P4.67's survey.**
   `lib/api/middleware/actions.ts` last moved at `55752ad45` (2026-07-30);
   `getActionParam` = `searchParams.get('action')` (FIRST value; `''` for
   `?action=`), `withActionDispatch` gates on JS truthiness; the three
   hand-rolled readers (`v1/system/unlock/route.ts:76`,
   `v1/scenarios/[scenarioPath]/route.ts:163`, `v1/chats/[id]/files/
   route.ts:43`) still carry their own truthiness. P4.67's survey items 1–4
   stand verbatim; re-read them from its order.
2. **The family today covers FOURTEEN endpoints** (`crates/quilltap-web/
   tests/query_param_semantics_equivalence.rs` `endpoints()` mirrored
   entry-for-entry by the oracle's `ENDPOINTS` in
   `harness/oracle/cases/query-param-semantics.test.ts:90-194`):
   `system_tools_get/post`, `system_data_dir_post`, `user_profile_get/put/
   patch`, `brahma_item_patch`, `chats_collection_get`,
   `embedding_profile_item_post`, `mount_point_action_post`,
   `custom_tools_collection_get/post`, `chat_custom_tools_post`,
   `character_item_post`. The two lengths are compared, so every endpoint
   added on one side must be added on the other in the same commit.
3. **The seventeen sites still outside the family** (P4.67's header; the
   v5 census today is 55 `query::action` / `query::first` reads across 15
   route files — `backup_routes.rs` 1, `brahma_routes.rs` 1,
   `characters_routes.rs` 4, `chats_routes.rs` 4, `custom_tools_routes.rs`
   6, `embedding_profiles_routes.rs` 8, `files_routes.rs` 5,
   `llm_logs_routes.rs` 2, `photos_routes.rs` 2, `profile_routes.rs` 3,
   `qtap_target_route.rs` 3, `system_data_routes.rs` 7,
   `text_replacements_routes.rs` 2, `tools_routes.rs` 2,
   `wardrobe_routes.rs` 5): the restore POST (`backup_routes.rs`),
   `characters_wardrobe_get` / `characters_get` / `characters_import_post`,
   the embedding-profiles collection GET, the files thumbnail GET, the
   chat-files POST (both arms — v4's hand-rolled `chats/[id]/files/
   route.ts:43` truthiness, measured not assumed), both text-replacements
   edges, the two conversation-summaries edges, the jobs POST,
   `system/unlock` (v4's hand-rolled `:76` — the `Missing action parameter`
   class the classifier already names), the four wardrobe edges, and
   `chat_action_get`. Each gets the six rows (`?action=`,
   `?action=&action=<known>`, `?action=<known>&action=<unknown>`,
   `?action=<unknown>`, bare, and the served `<known>` as the §A pin).
   Where v5 serves a SUBSET of v4's actions, keep the v4-KNOWN-unserved loud
   refusal pinned v5-side via `UNSERVED_KNOWN_ACTIONS` (the P4.67 §3
   precedent) — never a third shape. **Recorded, unchanged:**
   `characters_get` folds `?action=` onto a 400 where v4 serves the
   character (a P4.D66 narrowing) — put it on the record as a
   both-directions pin, not a silent skip.
4. **The classified non-action keys and their v4 readers** (P4.67 Tier 2
   item 5, READS landed, one duplicate pin only): `q`/`tag` on photos
   (`getAll` — ALL wins; `photos_routes.rs`), `kind` on llm-logs
   (`llm_logs_routes.rs`), `filePath`/`scope`/`mountPoint` on the qtap
   target route (`qtap_target_route.rs`), `force` on files delete
   (`files_routes.rs`), `limit`/`offset` wherever read (v4
   `searchParams.get` ×9/×8 — FIRST wins). A discriminating row is one
   whose two values would answer DIFFERENTLY (`?limit=1&limit=5` that
   changes the page size); a row that answers the same either way is
   vacuous (memory note `a-green-mutation-means-a-non-discriminating-arm`).
   Send `?k=` explicitly where the arm's point is the empty string (memory
   note `axum-absent-and-empty-query-param-collapse`).
5. **P4.62(c)** — `api/chat_media.rs`: the `?action=link` edge sends an
   invalid `fileId` through as `""` to run the core's chat-404 gate. Tidier
   shape: a three-line `fileId is required` guard in `chat_file_link` AFTER
   its chat-404 (behaviour identical), the edge's `""` pass-through retired.
   Proven by the existing files routes families staying green — and by a
   NEW arm only if the current families cannot see the guard (measure
   first; do not add a vacuous row).
6. **The dispatch wrong-type class.** v5 has NO `POST /api/v1/chats` REST
   edge (`lib.rs:399` serves only the collection GET) — chat creation is
   `Request::ChatCreate` over `/api/dispatch`, decoded by serde at
   `crates/quilltap-web/src/dispatch.rs:72`, so a non-string
   `conciergeState` / `roleplayTemplateId` / non-object `timestampConfig`
   answers `ErrorKind::BadRequest` `Invalid request: <serde text>` where
   v4's `createChatSchema.parse(body)` (`app/api/v1/chats/route.ts:993`;
   `conciergeState: z.enum([...]).optional()` at `:146`,
   `timestampConfig: TimestampConfigSchema.optional()` at `:127`) throws a
   ZodError → the context handler's flat `{"error":"Validation error"}`
   400. **The Tauri IPC transport dispatches the same `Request` enum**, so a
   web-edge-only mapping would leave the desktop app answering the serde
   envelope — the transport-agnostic shape is the D57 idiom (the field
   carries `Option<Value>` / `Option<Option<Value>>` and the HANDLER refuses
   with v4's sentence), which is why the FIX belongs to the lane owning
   `api/types.rs` (P4.73 this round, for the `ChatCreate` trio only). This
   lane's deliverable is the CENSUS: every `Request` variant whose v4 route
   twin is a `.parse` (not `safeParse` — memory note
   `v4-refusal-envelope-parse-vs-safeparse`), which of its typed fields
   would serde-reject a wrong JSON type, and what v4 answers for that
   shape — recorded as an executable source census in the
   `db_error_key_guard` idiom (per-variant rows, the ordered shape named,
   mutation-proven by editing one row) so the next round can fix them by
   name. The P4.60/P4.62 adjudication tables (`web_edge_body_parse_guard`)
   are the precedent for the REST edges; this census is their dispatch-side
   twin.
7. **The v5 web test venue** (`crates/quilltap-web/tests/common::
   serve_instance`) and the DB-free jest oracle idiom (memory note
   `dbfree-route-guard-oracle`) stand as P4.67 used them. ⚠ memory note
   `web-test-venue-chat-send-lacks-tables`: an arm that reaches a DB read
   dies as a 500 before the guard unless the venue serves the tables — pick
   fixtures per route family (the restore POST / the jobs POST need the
   `system-data-*` pair; the wardrobe edges the `wardrobe-*` pair; the
   conversation-summaries edges whichever fixture carries
   `conversation_summaries`).

## Tiered deliverables

**Tier 1 (must land):**

1. **The seventeen sites into `query_param_semantics_equivalence`** (both
   sides, same commit), each with the six rows; the oracle's `ENDPOINTS`
   grown entry-for-entry; red-first — run the grown family BEFORE any v5
   change and record which rows red (P4.67's rewrite already moved every
   site onto the reader, so the expected reds are the subset-edge and
   hand-rolled-truthiness shapes; a zero-red run is itself a measurement
   worth recording, not a reason to skip the rows).
2. **The per-site duplicated-key rows** for the classified non-action keys
   (item 4) — one DISCRIMINATING row per site, in the family proper where
   v4's answer is cross-comparable and in the oracle-free v5-side test
   where it is a payload over the fixture (the family's within-tree BOOLEAN
   idiom, `firstWins`/`allWins`).
3. **P4.62(c)** the `chat_file_link` guard tidy.
4. **The dispatch wrong-type census** (item 6) as an executable guard in
   `crates/quilltap-web/tests/` (it reads `api/types.rs` and the v4 route
   files' `.parse` sites by path — a SOURCE census, no oracle needed), with
   the `ChatCreate` trio's rows marked `ORDERED(P4.73)` and every other row
   carrying its ordered shape.

**Tier 2 (should land):**

5. The two v4 `actionLogger.warn` lines (`Unknown action requested` / `No
   action param and no default handler`) with a thread-scoped capture-layer
   pin (memory notes `a-process-global-test-seam-must-be-thread-scoped`,
   `capture-layer-target-assert-is-a-prefix-match`) — P4.67's Tier 3, now
   cheap because the family enumerates every site that fires them. If not
   landed, hand the two lines to P4.74's handler-logging inventory by name.

**Tier 3 (explicit deferrals, loud):** any v4 `searchParams` read shape not
covered by FIRST/ALL (record it, do not guess); the fixes the census names
(next round, by row).

## The differential requirement (restated)

Every added endpoint ships red-first in `query_param_semantics_equivalence`
against v4's REAL middleware + route modules (the DB-free jest idiom; arms
that reach a repo use `serve_instance` over the right fixture). Compare
status AND whole body with key order (`preserve_order`; memory note
`serde-json-map-remove-is-swap-remove`). Mutation proofs: revert ONE
site's reader to `HashMap` semantics (memory note
`mutation-proof-revert-by-file-backup`) and show exactly its rows redden;
for the census, edit one row and show the guard names it. Regenerate the
oracle through the sweep driver from the pin; grep the fresh NDJSON for
the changed bytes (`Unknown action:`, `Action parameter required`,
`availableActions`, and one per-site sentence) before believing green.

## Fixtures

Consumes the committed fixtures the existing web families already serve
(`characters-*`, `system-data-*`, `wardrobe-*`, `files`/`chat-send`
venues) **at this lane's base**, unchanged. **No lane widens a committed
fixture this round except P4.73 (a NEW `images-*` pair, if it needs one)
and P4.74 (the `primary-stream` builder, additively) — neither touches
anything this lane serves.** If an arm needs a row a fixture lacks, build
it in the test (the `instance_settings` materialization precedent) and say
so.

## Shared contracts (binding, identical across all four orders)

- **§A — well-formed actions never move.** P4.72 may change the answer to
  `?action=` (empty), an UNKNOWN action, and DUPLICATED keys only; the
  status + body of every well-formed `?action=<served>` request and every
  single-valued query is byte-identical before and after (the existing web
  families are the pin). No SPA caller changes.
- **§B — `crates/quilltap-web/src/query.rs` is the ONE query reader**
  (`first` / `all`, v4's `searchParams.get` / `getAll`). P4.73's new
  `images_routes.rs` reads its `?action=` and `?tagId=` through it and adds
  nothing to it; P4.72 changes no signature in it.
- **§C — participant-status parsing has ONE home:**
  `quilltap_core::chat_predicates::participant_status_from_str`. P4.74
  retires the two stray copies; no lane adds a private status match.
- **§D — the pixel codec has ONE accessor:** `Engine::qtap_pixel_codec()`
  (`api/engine.rs:762`). P4.73 threads it into the chat-upload arm and the
  new images arms; no other lane constructs a `PixelCodec`.
- **§E — the `Request`/`Response` enums are P4.73's this round.** Only
  P4.73 adds variants (`ImagesList`, `ImageUpload`, `ImageImportFromUrl`,
  `ImagesGenerate`, `ImageDelete` — append-only, at the end of the enum,
  `serde(rename_all = "camelCase")`) and only P4.73 changes an existing
  variant's field types (`FileUpload.tags`, the `ChatCreate` trio). P4.72's
  census READS `api/types.rs`; it never edits it.
- **§F — `photos_routes.rs` same-file exception.** P4.73 edits that file
  ONLY to delete `image_delete_not_available` (`:161-167`) once the real
  DELETE lands in `images_routes.rs`; P4.72 edits it anywhere else (the
  `q`/`tag` duplicate rows). Both lanes name the region in their commit
  message so the unifier's hand-merge is trivial.
- **§G — `files_routes.rs` same-file exception.** P4.73 edits that file
  ONLY inside `files_upload_post` (`:943-1010`, the `tags` parse block that
  P4.62(a) widens); P4.72 edits it anywhere else (the thumbnail GET, the
  chat-files POST, the `force` duplicate row). Same commit-message rule.
- **§H — the streaming avatar is client-only.** P4.75 reads
  `respondingParticipantId` (already on `ChatStreamState`), the chat's
  participants and `avatarDisplayMode`; no server payload changes and no
  lane touches the stream frames.

## Ownership (binding, identical across all four orders)

| Area | Owner |
|---|---|
| `crates/quilltap-web/src/**` EXCEPT `lib.rs`, the NEW `images_routes.rs`, and the two §F/§G regions; `crates/quilltap-web/tests/**` (the grown `query_param_semantics_equivalence`, the NEW dispatch wrong-type census guard, arms in the existing body-guard families); `quilltap-core`: `api/chat_media.rs` (the `chat_file_link` guard ONLY); harness: `harness/oracle/cases/query-param-semantics.test.ts` | **P4.72** |
| `quilltap-core`: `api/types.rs` (append-only new variants + the `FileUpload.tags` and `ChatCreate` trio field widenings), `api/engine.rs` (the new dispatch arms, the `ChatFileUpload` codec line `:4121`, the `FileUpload` arm `:4362`), `api/files.rs` (`file_upload`'s `link_ids` widening), the NEW `api/images.rs`, the file that validates `Request::ChatCreate`'s body (named in the lane record at start — `api/chats.rs` or `services/chat_create.rs`, measured), `services/file_storage.rs` (a codec seam ONLY if the images arms need one); `quilltap-web`: `lib.rs` (route registration), the NEW `images_routes.rs`, the §F/§G regions; `quilltap-host`: `host.rs` (the import-fetch seam wiring), a NEW `image_import_fetch.rs`; harness: NEW `images_routes_equivalence` + `harness/oracle/cases/images-routes.test.ts`, a NEW committed `images-{main,mount}.db` pair + builder if needed, `files_routes_equivalence` (the codec WIRING arm), `chat_create_capstone` (the trio arms), `lora_log_anchor_guard` (the ninth anchor moves); SPA: `core-contract.ts` (the §1 type-only mirror of the new verbs) | **P4.73** |
| `quilltap-core`: `services/answer_confirmation.rs` (the status match ONLY), `services/file_fallback.rs` (log fields ONLY), `services/cost_events.rs` (the swallowed-write warn ONLY); harness: `message_attribution_equivalence.rs`, `primary_stream_tier3_equivalence.rs` + `harness/oracle/cases/primary-stream-tier3.test.ts` + its fixture builder (additive), the three shared-stage recipe headers (`precompute_equivalence.rs`, `mail_carina_tools_equivalence.rs`, `photo_tools_equivalence.rs` + their `.test.ts` twins), the NEW handler-logging inventory doc | **P4.74** |
| SPA: `chat/{streaming-message,message-list}.ts` (+specs), `documents/open-document-from-search.ts` + `search/search-results.ts` + whatever workspace file the root cause names (+specs), the `title=` census script under `apps/web/scripts/`, the residue hosts' files (each named in the lane record), `screens/files/folder-picker.ts` (`#move-folder` only if fixed); e2e: `workspace-search-documents-flow.spec.ts`, NEW streaming-avatar beat | **P4.75** |
| Playwright (port 4319) | **P4.75's branch only**; every other lane's beats are authored gated (ACTIVATE-AT-UNIFY) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it |

**Version bumps:** this lane bumps `quilltap-web`, `quilltap-core` and
`quilltap-harness` per commit (base: web 0.0.105, core 0.0.768, harness
0.0.662); the unifier recounts as base + total bumps (three lanes bump
core and harness — memory note `parallel-round-playbook` §C).

## Verification gate before declaring done

`cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
-D warnings` on BOTH feature sets (plain + `--features
quilltap-core/native-transport`); full `cargo test --workspace` with the
lane's oracle env vars (`run_in_background`, full log + sentinel, read
every `test result:` line, zero `SKIP:` lines for the lane's families);
the grown family by name through the sweep driver from the pin, zero
SKIP, with the changed bytes grepped in the regenerated NDJSON; mutation
proofs verified-applied then reverted by file backup; `cargo build
--workspace --release`. No SPA run, no Playwright. Per-commit rule:
`.claude/commands/commit.md` (the CHANGELOG entry + the version lines).
Lane record → `status-log.md` ("Lane record — P4.72"), the status header
of this file AND of `p4.67-query-param-semantics-sweep.md` updated at
close (P4.67's OPEN list closes here).
