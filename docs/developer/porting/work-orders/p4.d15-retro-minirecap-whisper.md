# Work order P4.d15 — the episodic-recall drift catch-up, **round 3 lane B: recall-on-reference PART 2 (workstream C) — the scoped mini-recap, the `retrospective-recall` whisper, the spam guard, and the recall-history retro machinery** — round baseline `8bf3cb5f`

> **STATUS: CLOSED** (planned 2026-07-22; carried out 2026-07-22 on branch
> `claude/p4-d15-retro-minirecap-c67001`). Tier-1 units 1–3 all landed;
> tier-2 items 5 and 6 landed inside unit 3; tier 3 is the unchanged
> explicit-deferral list. Lane record: `status-log.md` ("P4.d15 — the
> round-3 lane-B record"). Two families were found RED **outside this
> lane** and reported rather than fixed across the boundary:
> `context_summary_service_tier3` (P4.d14's — v4's new five-section
> `FOLD_SUMMARY_PROMPT`) and `orchestrator_tier3` (neither lane's; red
> with this lane's diff reverted — v5 omits a memory-recap block v4
> emits, upstream of `build_context`; needs a dedicated follow-up).

## Preamble

**v4 reference baseline for this round: `8bf3cb5f` (4.9-dev, "Unify
episodic recall + character outfit work") — verified at planning
(2026-07-22): v4 HEAD == `8bf3cb5f`, tree clean.** At lane start, run in
`~/source/quilltap-server`:

```bash
git log 8bf3cb5f..HEAD --oneline
```

**If HEAD moved, STOP and report.** (v4 is mid-4.8/4.9 development — a
version/tag commit may land; report it either way.) v4's checkout is
clean at the baseline, so oracles regenerate directly from
`~/source/quilltap-server`; pin a detached worktree only on drift/dirty
(recipe: the `oracle-regen-pinned-v4-worktree` memory — symlink BOTH
`node_modules`: root + `packages/{quilltap,plugin-types,plugin-utils}` +
`plugins/dist/*`).

**Sibling lanes (run in parallel, own worktrees):**

- **P4.d14** (`p4.d14-episodic-creation-fold.md`) — round 3 lane A:
  the creation-side extraction + fold-episode pass +
  stop-destroying-episodes. It owns `memory_tasks.rs` (+
  `prompt_text`), `services/memory_processor.rs`, `memory_gate.rs`,
  `services/memory_service.rs`, `services/context_summary/**`,
  `services/fold_episode_pass.rs` (new), `services/housekeeping.rs`,
  `services/mod.rs` — **you must not touch those files.**
- **P4.9H1** (`p4.9h1-chat-sidebar-storys-clock.md`) — the ChatSidebar
  SPA vertical (the Story's Clock switch). It owns `apps/web` — **you
  must not touch `apps/web`.**

You run in a worktree; a unification pass integrates onto main.
Worktree gotchas: `p4.9i1a-brahma-server-lane` (oracle-gen `cd` resets
cwd to MAIN), `parallel-round-disk-pressure`
(`CARGO_INCREMENTAL=0`), `e2e-port-4319-cross-worktree-kill` (you do
NOT run full Playwright in-lane — see the gate).

**This is the campaign's final round.** Round 2 (P4.d13) deliberately
ported buildContext **part 1 only** (the turn-recall-signals thread,
the retro search options, the RETRO head sizing) and kept part 2
provably inert via the no-matching-vault-summaries fixture guard —
`retrospectiveRecallContent` stays `''` when no vault conversation
summary matches the probe. **This lane lands part 2: the fourth
cadence completes.** On a retrospective turn, v5 will emit the scoped,
dated, drillable mini-recap; post it as its own
`retrospective-recall` whisper; guard against spam via the
recall-history retro-signature ring; and give the round-2 vault-summary
`time_range` its first production caller (both v5 call sites currently
pass `None`).

## Mandate

Deliver, verified against v4 `8bf3cb5f`: the scoped mini-recap block in
`build_context` (the dated Relevant Past Conversations list — cap 5,
`#### {title} ({firstMessageAt:date}) (\`conversationId\`)` — closed by
the existing `READ_CONVERSATION_CALL_NOTE`, riding the LLM recall text);
the `retrospective-recall` whisper post (its own message, NEVER inside
the consolidated whisper body, swept with the consolidated sweep); the
spam guard (same-signature suppression within
`RETRO_SIGNATURE_TURNS = 3` + conversation-ID dedup against the on-fold
relevant-conversations whisper); the recall-history
`retroSignatures` machinery (`parseRetroSignatures` beyond the round-2
preservation carrier, `appendRetroSignature`, the persist);
and the commonplace writer's `retrospectiveRecall` part + systemKind.
Each unit ships with its differential; the `QT_ORACLE_BUILD_CONTEXT`
fixture is EXTENDED with matching vault conversation summaries so the
mini-recap arm runs live in the diff (keeping a no-match arm as the
inert-path regression guard).

**Must NOT touch:** P4.d14's files (list above); `apps/web`; the
schema; the CLI (`recall-replay` is frozen — re-run its family only).

## Survey-verified starting points (fresh, 2026-07-22)

Authority: `git diff 7e6d13e5..8bf3cb5f -- lib/chat/context-manager.ts
lib/memory/recall-history.ts lib/services/commonplace-notifications/writer.ts`
(writer +27, recall-history part of the memory diff), minus what round 2
already ported (part 1 — the boundary section of
`p4.d13-episodic-retrieval-tools-replay.md` pins it file-by-file; read
it before coding).

### v4 (all at `8bf3cb5f`; line numbers verified 2026-07-22)

| What | Where |
|---|---|
| Mini-recap entry cap const | `lib/chat/context-manager.ts:33` |
| The fourth-cadence doc comment | `context-manager.ts:391` |
| Part-1 boundary (ALREADY PORTED round 2 — do not re-port) | `context-manager.ts:1112–1290` (retro head sizing, search options) |
| The scoped mini-recap build (vault-summary search scoped by `timeRange`/`entities`, dated render, cap, call note, signature compute, suppressed-log :2007, non-fatal warn :2013) | `context-manager.ts:1944–2030` |
| Mini-recap rides the LLM recall text; posted as its OWN whisper | `context-manager.ts:2026–2027` (comment pins the rule) |
| Consolidated-whisper sweep (the `retrospective-recall` whisper is swept by THIS sweep; the fold-posted `relevant-conversations` whisper is NOT — :2111) | `context-manager.ts:2055–2140` |
| Spam-guard append into recall history (rides the same persist as the F4 ring) | `context-manager.ts:2087–2094` |
| The whisper post (kind `'retrospective-recall'` :2159; failure warn :2162) | `context-manager.ts:2147–2165` |
| `RETRO_SIGNATURE_TURNS = 3` / `retroSignatures?: string[]` / `parseRetroSignatures` / `appendRetroSignature` | `lib/memory/recall-history.ts:25, 36, 63–82, 91–94` |
| Writer: the `'retrospective-recall'` systemKind (doc :41–46) + `CommonplaceParts.retrospectiveRecall` (:81–87) + its handling in `buildCommonplacePersonaWhisper` | `lib/services/commonplace-notifications/writer.ts:36–100+` |
| Dated conversation render + `READ_CONVERSATION_CALL_NOTE` ` (YYYY-MM-DD)` (round 2 ported the search side) | `lib/memory/conversation-summary-search.ts:219–226` — re-use, don't duplicate |
| The compression flip (context for §4 of the Shared contract — NO port) | `lib/memory/cheap-llm-tasks/compression-tasks.ts:79+` (`MEMORY_COMPRESSION_PROMPT`: KEEP gains "Dates attached to events" :88; the DROP "Exact dates/times…" line is deleted) |

### v5 landing sites (verified 2026-07-22)

| v4 counterpart | v5 file |
|---|---|
| buildContext part 2 | `crates/quilltap-core/src/services/build_context.rs` (the distill/part-1 region ~:1844+; the whisper assembly + sweep + recall-history persist — round 2 fixed the persist to `{turns}` and ported `parse_retro_signatures` as a PRESERVATION CARRIER only) |
| recall-history retro machinery | `crates/quilltap-core/src/recall_history.rs` |
| the commonplace writer | `crates/quilltap-core/src/services/commonplace_notifications.rs` |
| vault-summary `time_range` (round 2, NO caller yet — both call sites pass `None`) | `crates/quilltap-core/src/services/memory_recap/mod.rs` (`search_vault_conversation_summaries` :123) |
| `compressMemories` tracked deferral (comment update ONLY — §4) | `build_context.rs:2264–2275` |

### Round-2 carry-ins this lane resolves (from the P4.d13 lane record)

- The vault-summary `timeRange` gets its first production caller (the
  mini-recap) — flip the two `None` call sites where v4 passes the
  distilled window.
- `parse_retro_signatures` graduates from preservation carrier to live
  spam-guard reader.
- The round-2 fixture guard INVERTS: round 2's retro arm deliberately
  had NO matching vault summaries. This lane ADDS matching summaries to
  the build-context tier-3 fixture (and keeps a no-match arm so the
  inert path stays regression-guarded — design-doc §3 acceptance).

## Tiered deliverables

### Tier 1 — must land (suggested sequence; each unit = one commit + differential)

1. **The recall-history retro machinery** — `RETRO_SIGNATURE_TURNS`,
   the live `parseRetroSignatures`, `appendRetroSignature`, persist
   shape. Regenerate `QT_ORACLE_RECALL_HISTORY` at `8bf3cb5f`; add
   signature-append/trim/parse cases.
2. **The writer part** — `retrospectiveRecall` on the parts struct +
   the `'retrospective-recall'` systemKind + persona-whisper handling.
   Covered by the commonplace writer family
   (`QT_ORACLE_PO_COMMONPLACE`) — regenerate as touched.
3. **The mini-recap + whisper + spam guard in buildContext** — the
   scoped vault-summary search (passing the distilled
   `timeRange`/`entities` — the `time_range` caller goes live), the
   dated render + cap + call note, the signature compute + suppression
   + conversation-ID dedup, the recall-text ride, the whisper post, the
   sweep membership. Regenerate `QT_ORACLE_BUILD_CONTEXT` at
   `8bf3cb5f` over the EXTENDED fixture: (a) a retro arm with matching
   vault summaries → the mini-recap emits, the whisper row lands, the
   signature persists; (b) the retro re-emit arm → suppressed by the
   spam guard; (c) the no-match arm → byte-inert (the round-2 guard,
   kept). Diff the whisper ROW (kind, content) and the recall-history
   persist, not just the context bytes.
4. **Transitive re-runs** — `QT_ORACLE_DISTILL`,
   `QT_ORACLE_RECALL_REPLAY`, `QT_ORACLE_VAULT_CONV`, and the salon
   families as touched (the replay tables must be UNAFFECTED — part 2
   is outside the replay's retrieval scope; if a replay diff moves,
   STOP and understand why before regenerating).

### Tier 2 — should land

5. **The `compressMemories` deferral-note update** (this lane owns
   `build_context.rs`): record that the port target moved to
   `8bf3cb5f` and now includes the keep/drop date flip (Shared
   contract §4). Comment-only.
6. The seam-note sweep: v5 comments saying "part 2 / mini-recap is
   round 3" get updated (`p4.6aw-rust-riders-hint-lane` memory).

### Tier 3 — explicit loud deferrals (enumerate, never stub silently)

- **`compressMemories` itself** stays the pre-existing tracked
  deferral (Phase-3 vintage; not part of the drift).
- Any SPA rendering nuance for the `retrospective-recall` whisper:
  v4 made NO SPA change for it (verified: the `8bf3cb5f` components
  diff touches no whisper renderer) — the whisper renders through the
  existing generic path. If v5's salon needs anything, that's P4.9H1's
  tree; report, don't edit.

## The differential requirement (restated)

Every ported unit ships with an equivalence test driving v4's REAL code
(jest/tsx oracle → NDJSON → Rust diff). The build-context tier-3
recipe is the round-2 one (its case + fixture builder landed in
P4.d13 — see the lane record's regen table in `status-log.md`):
freeze the WHOLE `Date` constructor (`FakeDate`), `TZ=UTC`, 750ms
oracle settle on migrated fixtures, run `--nocapture` and grep SKIP
(`oracle-regen-silent-stale-pass`). The whisper-row diff follows the
established tier-2 structural pattern (timestamp/UUID normalization
via the minted-values remap).

## Fixtures

- This lane owns `harness/oracle/fixtures/build-context-tier3.json` +
  its builder (extending with matching vault conversation summaries +
  the re-emit arm) and regenerates every family consuming it — that
  fixture feeds ONLY the build-context family (verified in the round-2
  record).
- The committed `episodic-recall-{main,mount}.db` web fixture: re-run
  its consumers (`QT_ORACLE_RECALL_REPLAY`, `QT_ORACLE_VAULT_CONV`);
  extend only if a replay/vault case genuinely needs a matching
  summary — if extended, THIS lane regenerates both consumers.
- Do not touch P4.d14's gate/processor/memory-tasks/context-summary
  fixtures.

## Shared contract (binding — identical text in P4.d14 and P4.d15)

1. **File ownership.** P4.d14 owns `memory_tasks.rs` +
   `memory_tasks/prompt_text.rs`, `services/memory_processor.rs`,
   `memory_gate.rs`, `services/memory_service.rs`,
   `services/context_summary/**`, NEW `services/fold_episode_pass.rs`,
   `services/housekeeping.rs`, `services/mod.rs`. P4.d15 owns
   `services/build_context.rs`, `recall_history.rs`,
   `services/commonplace_notifications.rs`. Neither lane edits the
   other's files, ever. `CHANGELOG` + `status-log.md` are append-only
   for everyone. `apps/web` belongs to P4.9H1 alone.
2. **Oracle-family ownership.** P4.d14 regenerates: `QT_ORACLE_GATE`
   (un-SKIP), `QT_ORACLE_PROCESSOR`, `QT_ORACLE_MEMORY_TASKS`,
   `QT_ORACLE_CARINA_MEM`, the context-summary/chat-tasks families,
   `QT_ORACLE_MEMHOUSEKEEPING`, the NEW fold-episode family, + re-runs
   of extract-novel-details/episodic/weighting/injector/memories
   families as touched. P4.d15 regenerates: `QT_ORACLE_BUILD_CONTEXT`
   (fixture extended), `QT_ORACLE_RECALL_HISTORY`, + re-runs of
   `QT_ORACLE_DISTILL`/`QT_ORACLE_RECALL_REPLAY`/`QT_ORACLE_VAULT_CONV`
   and the salon families as touched. **A red family in the OTHER
   lane's set = STOP and report — never "fix" across the boundary.**
3. **The seam between the lanes is the gate/service write API** as it
   exists on main today: P4.d15's mini-recap is retrieval-only and
   consumes NO new P4.d14 output; P4.d14's fold pass posts NO whisper
   (v4's fold pass writes memories only). If either lane finds v4
   violating this (a fold-pass whisper, a mini-recap write), STOP and
   report — the split was planned on it.
4. **The compression keep/drop flip needs NO round-3 code:** v4's
   change is +3 lines inside `MEMORY_COMPRESSION_PROMPT`
   (`compression-tasks.ts:79+`), and v5 has never ported
   `compressMemories` (tracked no-op deferral,
   `build_context.rs:2264`). P4.d15 (file owner) updates that
   deferral comment to record that the port target moved to
   `8bf3cb5f` (flip included). Neither lane ports the task.
5. **Version bumps.** Both server lanes bump `quilltap-core` +
   `quilltap-harness` per-commit per `.claude/commands/commit.md`; the
   unifier recounts (the `p4.6aw-ax-8-round-plan` memory: identical
   bumps merge silently — recount, don't trust the merge).

## Verification gate (before declaring done)

- `cargo fmt --all --check`; `cargo clippy --workspace --all-targets
  -D warnings` in BOTH feature sets (default +
  `--features quilltap-core/native-transport`); `cargo build
  --workspace --release`.
- `cargo test --workspace` with this lane's full env set; **re-run
  every family this lane owns BY NAME with `--nocapture` and grep for
  SKIP.** Recount ok-lines on any failure.
- `apps/web` untouched: `git diff <branch-base>..HEAD -- apps/web`
  = 0 lines.
- **Full Playwright: DEFERRED TO THE UNIFIER** (three parallel lanes
  share e2e port 4319; the SPA lane owns the in-round Playwright run).
  Note: the `retrospective-recall` whisper is a LIVE production write
  path once wired — flag in the lane record that the unifier's
  Playwright run and the next dogfood pass are the live proofs.
- Per-commit rule: `.claude/commands/commit.md`. Append lane records to
  `status-log.md`; update this order's status header at lane end.
