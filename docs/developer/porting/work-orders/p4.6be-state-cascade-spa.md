# Work order P4.6be — the `c53510c7` drift re-port, SPA half (the four-entity State Editor, the General State card, Workbench mock-state, tool-draft `$state`)

> **STATUS: LANE CARRIED OUT — awaiting unification.** Executed
> 2026-07-19/20 on branch `claude/p4-6be-state-cascade-spa-bd6ade`
> (worktree `.claude/worktrees/p4-6be-state-cascade-spa-fc7b0b`), six
> commits, units 1–5 landed with the lane gate recorded in the status
> log. **PRESERVE the branch until unification** (the
> `unify-a-partial-round` memory).
>
> **Re-baseline addendum (2026-07-20):** v4 moved twelve commits past
> `c53510c7` (the 4.8.0 release sweep) to `7e6d13e5` before lane D10
> started, and the round was re-baselined there. **This lane's
> completed work STANDS:** every v4 surface it ported or byte-copied
> (`components/state/**`, `lib/pascal/tool-draft.ts`,
> `custom-tool.types.ts`, `public/schemas/qtap-custom-tool.schema.json`,
> `lib/query/keys.ts`, the settings/groups/prospero components) was
> verified byte-unchanged across `c53510c7..7e6d13e5` — the sole
> exceptions, the two 2-line workbench dialog backdrop stylings
> (`8f24ccc9`), belong to the NEW sibling lane
> `p4.d11-release-sweep-spa.md` (which touches only files this lane's
> branch did not). Lane D10 now regenerates the §C corpus at
> `7e6d13e5` (output-identical for the §C generator: no post-`c53510c7`
> commit changes Pascal behavior). No rework here; the body below is
> the historical order as executed at `c53510c7`.
>
> Lane BE of the (originally two-lane, now re-baselined) drift
> catch-up round (planned 2026-07-19 — the catch-up the p4.9j
> workspace-tabs round left OWED). **v4 reference baseline at
> execution time: `c53510c7`.** At lane start, run in
> `~/source/quilltap-server`:
>
> ```bash
> git log c53510c7..HEAD --oneline
> ```
>
> **If HEAD moved, STOP and report.** Anything you verify against v4
> (component behavior, byte-copied assets, the tool-draft diff) reads
> at `c53510c7`; if you need a pinned tree, lane D10's order has the
> recipe for the round pin.
>
> Sibling lane: `p4.d10-state-cascade-server.md` (lane D10 — the Rust
> workspace + harness + the §C corpus carve-out). The **Shared
> contract** and **Ownership** sections below are binding and
> reproduced verbatim in that order. You run in a worktree; a
> unification pass integrates the lanes onto main. Your live e2e beats
> that need lane D10's dispatch verbs ship gated and ACTIVATE AT
> UNIFICATION (the established pattern).
>
> **This lane IS a differential port** where a differential applies:
> the SPA schema port is proven by the §C byte corpus (unifier-applied,
> §C), the tool-draft port by v4's spec suite carried case-for-case,
> and the components by v4-faithful unit specs + live beats. The facts
> under "Survey-verified starting points" are from a **2026-07-19
> survey at `c53510c7` (v4) / main `7145b23f` (v5)** — if a fresh
> survey contradicts this order, **STOP and report** before
> proceeding.

## The drift, classified (this lane's slice)

One v4 commit carries the whole SPA surface: `f48f34dc` (cascading
state). The `c53510c7` math-note commit is server-only (the SPA
renderer has handled `$$…$$` since P4.d9) — nothing for this lane.

## Mandate

Re-port v4 `f48f34dc`'s client surface into the Angular SPA: the
StateEditorModal generalized to the four entity types (chat / project /
group / general) with the inherited-layers cascade note, the Group
State entry on the group editor, the General State card at v4's exact
Chat-tab position, the Workbench mock-state field + read-only `$state`
pills across the builder family, the `$state` additions to the SPA
custom-tool schema port and the tool-draft round-trip suite, and the
re-byte-copied `qtap-custom-tool.schema.json` asset — with unit specs
mirroring v4's new test families and gated live e2e beats. This lane
owns `apps/web/**` EXCEPT the §C corpus NDJSON (lane D10's carve-out);
it must NOT touch `crates/**` or `harness/**`. User-facing strings
keep v4's register and, where v4 supplies them, v4's exact words
(button labels, card copy, validation messages, pill text).

## Survey-verified starting points (2026-07-19: v4 @ `c53510c7`, v5 @ main `7145b23f` — do not re-derive)

### v4 — the new client behavior (file:line at `c53510c7`)

- **`components/state/StateEditorModal.tsx`** (400 lines) —
  `StateEntityType = 'chat' | 'project' | 'group' | 'general'`. Props:
  `{ isOpen, onClose, entityType, entityId? (ignored for 'general'),
  entityName?, onSuccess? }`. `endpointConfig(entityType, entityId)`
  → per-type `{queryKey, getUrl, setUrl, resetUrl, label}`: chat →
  `/api/v1/chats/${id}?action=get-state|set-state|reset-state`, label
  `'Chat'`; project → the projects actions, label `'Project'`; group
  → the groups actions, label `'Group'`; general → all three =
  `/api/v1/settings/general-state` (method distinguishes), label
  `'General'`. GET via useQuery (`enabled: isOpen`); save `PUT
  {state}`; reset `DELETE`. **Inherited-layers UI renders ONLY for
  `entityType==='chat'`**: reads `projectState/groupState/
  generalState/groupTier` off the response; `inheritedLayers` built
  from the non-empty tiers (label + `Object.keys`); the note box:
  "The merged state below layers the cascade (chat over project over
  group over general — narrower tiers win)" + per-layer "Inherited
  from {label}: {keys}"; when `groupTier?.status === 'ambiguous'` →
  "{N} groups apply — group state is not merged here. Edit each
  group's state from its own page." Save validation rejects
  non-object/array with `'State must be a JSON object'`; help text
  notes underscore-prefixed keys are user-only. Client `GroupTier` =
  `{status:'none'|'single'|'ambiguous', candidates:{id,name}[],
  appliedGroupId?}`.
- **v4 entry points (four):** Salon chat — `app/salon/[id]/components/
  ChatModals.tsx:414` opened via `useModalState`'s `openStateEditor`,
  whose button lives in **`components/chat/ChatSidebar.tsx`**
  (:206/:1489) — **ChatSidebar is UNPORTED in v5** (a named p4.9j
  follow-up), so the chat-mode OPENER is out of scope (the modal's
  chat mode still ships, dormant). Prospero —
  `app/prospero/[id]/components/SettingsCard.tsx` (v5 has this).
  Group — `app/aurora/groups/[id]/GroupDetailView.tsx` (+17): a
  **"Group State"** secondary button in the form's action row
  **immediately after "Save Changes"**, modal with
  `entityType="group" entityId={groupId} entityName={group.name}`.
  General — the settings card below.
- **`components/settings/chat-settings/GeneralStateSettings.tsx`**
  (NEW, 37 lines): intro paragraph + an **"Edit General State"**
  button opening the modal with `entityType="general"` (no entityId).
  `components/settings/tabs/ChatTabContent.tsx`: one new
  `<CollapsibleCard title="General State" description="Instance-wide
  persistent state shared by every chat (bottom tier of the state
  cascade)" sectionId="general-state">` — **card #11 of v4's Chat
  tab, immediately after Custom Tools (#10) and before Agent Mode
  (#12)**.
- **`lib/query/keys.ts`** (+5): `groups.state(id) =
  ['groups',id,'state']`; `settings.generalState =
  ['settings','general-state']` (chat/project state keys pre-exist).
- **Workbench mock-state (`components/custom-tools/`):**
  `ProvingBench.tsx` (+62): `mockStateText` state (init `'{}'`);
  `testsState` detection = any operand `kind==='state'` ∨ any range
  field `kind==='state'` ∨ any param `isStateRef(defaultValue)` ∨
  `/\{\{\s*state\./` in messages; `mockStateError` (must parse to a
  single JSON object); `benchState()` → parsed object or `undefined`;
  **both run bodies gain `state: benchState()`** (the route's
  preview/audit `state` param — §B); `benchDisabled` also gates on
  `mockStateError !== null`; new UI section **"Card 2¾ — Mock
  state"** (`<h3>Mock state</h3>`, textarea `aria-label="Mock merged
  state (JSON object)"`, 3 rows) between the fact-sheet card and
  "Card 2½ — The oracle"; hint "The merged state (chat → project →
  group → general) a roll resolves `$state` references against.";
  when `testsState` and mock is `{}` → note that every `$state`
  reference will use its fallback. `BuilderForm.tsx` (+21):
  `rangeReadout` renders `state:${path}` parts; a `$state` parameter
  default renders a READ-ONLY pill `$state: {path} → {fallback}`
  (title "Edit $state references in the raw JSON").
  `NumberOrParamField.tsx` (+11): `kind==='state'` → read-only pill;
  toggle title "Replace this $state reference with a literal number."
  `OutcomesSection.tsx` (+12): `showTypePicker` excludes
  `operand.kind==='state'`; state operands render the read-only pill.
  **Doctrine: the builder round-trips `$state` but never AUTHORS it —
  pills are read-only; editing happens in raw JSON.**
- **`lib/pascal/tool-draft.ts`** (v5 ports this suite case-for-case):
  `DraftParameter.defaultValue: string | boolean | StateRef`;
  `NumberOrParamValue` gains `{ kind:'state'; path: string; fallback:
  string }` (fallback is NUMERIC TEXT); `ConditionOperand` gains
  `{ kind:'state'; path: string; fallback: number|string|boolean }`.
  Round-trips: `rollFieldToValue` (`$state` → kind state, fallback
  `String(...)`); `operandToDraft` (fallback carried typed);
  `parameterToDraft` (StateRef default carried VERBATIM);
  `valueToRollField` (kind state → `{$state: path, fallback:
  parseNumberText(fallback)}` only if finite); `operandFromDraft`;
  `parameterFromDraft`. `validateNumberOrParam`: kind state valid iff
  `Number.isFinite(parseNumberText(fallback))`, issue message
  `` `${field} $state fallback must be a number` ``.
- **`lib/pascal/custom-tool.types.ts`** (the SPA mirrors the same
  schema changes lane D10 ports in Rust): `StateRefSchema =
  z.strictObject({ $state: z.string().min(1).describe('Dot/bracket
  path into merged state, e.g. "player.health" or
  "inventory[0].name".'), fallback: z.union([z.number().finite(),
  z.string(), z.boolean()]).describe('Required. Used whenever the
  path is absent or holds a value of a different type.') })` +
  `isStateRef` (`'$state' in value`); added to
  `NumberOrParamRefSchema`, parameter `default`,
  `NumberOperandSchema`, `AnyOperandSchema`, `StringOperandSchema`;
  parameter-default typing checks the **fallback** (integer default →
  `Number.isInteger`); `validateRollRefs` issue `` `roll.${field}
  uses a $state reference whose fallback is ${typeof} rather than a
  number` ``; `resolveOperandType` types a `$state` operand by
  `typeof fallback`.
- **`public/schemas/qtap-custom-tool.schema.json`** (+55): new
  `$defs/StateRef` (`required: ["$state","fallback"]`,
  `additionalProperties: false`, fallback `oneOf`
  number/string/boolean) referenced from `parameter.default`,
  `NumberOrParamRef`, `AnyOperand`, `StringOperand`; `llm.prompt` and
  outcome `message` descriptions mention `{{state.path}}`. Re-copy
  the whole file byte-for-byte — never hand-edit.
- **v4 client test families to mirror:** the tool-draft suite's new
  `$state` cases (in v4's tool-draft tests — carry case-for-case);
  `custom-tools-state.test.ts` schema acceptance/rejection rows
  (these land in the §C corpus, which proves your schema port).

### v5 — the anchors (main `7145b23f`)

- **Existing state editor (project-only):**
  `apps/web/src/app/screens/prospero/state-editor-modal.ts`; call
  site `apps/web/src/app/screens/prospero/cards/
  project-settings-card.ts:6,70` (`stateOpen` signal :86). Generalize
  into a shared component (suggest `apps/web/src/app/ui/` or a
  `shared/state/` home mirroring v4's `components/state/`), keep the
  Prospero card working via `entityType="project"`.
- **Groups screens:** `apps/web/src/app/screens/groups/` —
  `group-editor.ts` (the Group State button's home), `groups.api.ts`
  (dispatch calls — add the §A group verbs).
- **Settings Chat tab:** `apps/web/src/app/screens/settings/chat/
  chat-tab.ts` (252 lines; currently SEVENTEEN cards in v4's order —
  v4 now has eighteen). Current sectionId order anchors:
  `custom-tools` (:162), `agent-mode` (:171). **Insert
  `general-state` between them** (v4 position #11). Card components
  live beside the tab (e.g. `memory-cascade-settings.ts`); shell:
  `settings-card.ts`, `../../../ui/collapsible-card`.
- **Workbench components:** `apps/web/src/app/screens/custom-tools/`
  — `workbench-editor.ts` (host; imports :32-35/:68),
  `builder-form.ts`, `outcomes-section.ts`, `proving-bench.ts`,
  `number-or-param-field.ts`, `workbench.api.ts` (the preview/audit
  dispatch calls — add the §B `state` field), specs alongside each.
- **Pascal ports:** `apps/web/src/app/pascal/custom-tool-types.ts`
  (the schema port), `custom-tool-types.corpus.spec.ts` (the §C
  consumer — row-count guards 159/10/149 at :~96; **do NOT update the
  counts — unifier-applied, §C**), `tool-draft.ts` (1284 lines) +
  `tool-draft.spec.ts` (680 lines).
- **Schema asset:** `apps/web/public/schemas/qtap-custom-tool.schema.json`.
- **Core client:** `apps/web/src/app/core/core-client.ts`
  (`dispatchData`), `core-contract.ts` (add the §A request types
  name-for-name), per-vertical `*.api.ts` layers.
- **E2E:** `apps/web/e2e/` — `settings-chat-cards-flow.spec.ts` (the
  General State card beat), `groups-flow.spec.ts` (the Group State
  beat), `workbench-flow.spec.ts` (the mock-state beat); support/
  seeding conventions in `apps/web/e2e/support/` (fixtures.ts, env.ts,
  seed scripts). The e2e instance already has groups and the builtin
  mounts. **Remember:** e2e mutation checks need a rebuilt dist (the
  `e2e-mutation-check-needs-a-rebuilt-dist` memory); new aria-labels
  can break sibling by-name locators (the
  `added-affordance-breaks-sibling-by-name-locators` memory); keyed
  `@for` tracks remount like React keys (the P4.9f2 memory).

## Tiered deliverables

### Tier 1 — must land

1. **The shared four-entity `qt-state-editor-modal`.** Generalize the
   Prospero modal: `entityType` input (`chat|project|group|general`),
   per-type verb wiring per §A (dispatchData — general takes no
   entityId), the inherited-layers note UI (chat mode only, v4's copy
   verbatim incl. the ambiguous-group note), save validation
   (`'State must be a JSON object'`), the underscore help text, reset
   flow returning to the fetched state. The Prospero
   `project-settings-card` keeps working through the shared component
   (entityType project) — no behavior change there. Unit specs: per
   entity type (verb payloads, general's no-id), the inherited-layers
   rendering from a §A-shaped enriched response (incl. omit-when-empty
   handling), the ambiguous note, validation arms. The chat mode
   ships DORMANT (no opener — tier 3).
2. **The Group State entry.** The "Group State" secondary button on
   `group-editor.ts` in the action row immediately after Save (v4's
   placement + label), opening the modal with the group's id/name.
   `groups.api.ts` gains the three §A group verbs. Unit spec for the
   open/save/reset round-trip against a mocked dispatch.
3. **The General State card.** A `general-state-settings` component
   (v4's 37-line card: intro + "Edit General State" button → modal,
   entityType general) inserted in `chat-tab.ts` as sectionId
   `general-state` between `custom-tools` and `agent-mode`, title
   "General State", description "Instance-wide persistent state
   shared by every chat (bottom tier of the state cascade)". Unit
   spec: the card renders at the right ordinal + opens the modal.
   (Remember the closed-collapsible-cards-render-no-content memory
   when testing.)
4. **Workbench mock-state.** `proving-bench.ts`: the mock-state card
   (v4's "Mock state" heading, `aria-label="Mock merged state (JSON
   object)"`, 3-row textarea, the hint + fallback note, `testsState`
   detection incl. the `/\{\{\s*state\./` message probe,
   `mockStateError` single-object validation, `benchState()`), the
   `state` field on BOTH preview and audit dispatch bodies (§B), the
   disabled gate. `builder-form.ts` / `number-or-param-field.ts` /
   `outcomes-section.ts`: the read-only `$state` pills + titles +
   `rangeReadout` `state:` parts + type-picker exclusion — the
   round-trip-never-author doctrine. Unit specs mirroring v4's
   behaviors (pill rendering, testsState arms, invalid-JSON gate,
   bodies carrying `state`).
5. **The `$state` schema + draft ports.** `custom-tool-types.ts`:
   `StateRefSchema` + `isStateRef` + the five union insertions + the
   fallback-typing rules + the two issue-message formats (Zod-faithful
   — the corpus will catch any drift). `tool-draft.ts` + spec: the
   three draft-shape extensions and six round-trip functions
   case-for-case from v4's diff, incl. the
   `${field} $state fallback must be a number` validation arm. **Do
   not touch the §C corpus file or its spec's count literals** (§C —
   your port is proven when the unifier drops in lane D10's
   regenerated corpus and both consumers run green).
6. **The schema asset re-copy.** Byte-copy v4 `c53510c7`'s
   `public/schemas/qtap-custom-tool.schema.json` over
   `apps/web/public/schemas/qtap-custom-tool.schema.json` (whole
   file, no hand edits) and re-run whatever spec pins it.
7. **E2E beats (gated — ACTIVATE AT UNIFY, they need lane D10's
   verbs):** (a) settings-chat-cards: the General State card opens
   the modal, saves a key, reloads, sees it; (b) groups-flow: Group
   State button round-trip; (c) workbench-flow: a draft with a
   `$state` operand + mock state steers a preview outcome (scripted
   oracle — no spend). Ship them skip-gated on a probe for the §A/§B
   server surface (the established ACTIVATE-AT-UNIFY pattern) so the
   unifier flips them live.

### Tier 2 — should land

1. **The core-contract types** for all nine §A verbs + the §B field,
   name-for-name (the unifier diffs them against lane D10's wire
   contract).
2. **Query-key hygiene** mirroring v4's new keys where the SPA caches
   (group state, general state) — match the existing v5 caching
   idiom rather than inventing one.

### Tier 3 — explicit deferrals (loud, named)

- **The chat-tier opener:** v4's lives in the unported `ChatSidebar`
  — the modal's chat mode ships dormant; the opener lands with the
  ChatSidebar slice (already a named p4.9j follow-up). Note it in the
  status log at lane close.
- **The §C corpus counts:** unifier-applied (§C) — never this lane's
  edit.

## The differential requirement, restated

Where this lane ports v4 logic (the schema, tool-draft), the proof is
v4's own artifacts: the §C byte corpus (unifier-applied) and v4's spec
suite carried case-for-case. Components are proven by v4-faithful unit
specs + the gated live beats. Anything ambiguous: read v4 at
`c53510c7`, never guess.

## Fixtures

- **Consumes:** the committed e2e instance fixtures (read-only — its
  groups + builtin mounts serve the beats); the §C corpus (read-only;
  lane D10 regenerates it).
- **Delivers:** no committed fixture changes (the schema asset is a
  byte mirror, not a fixture).

## Shared contract (BINDING — identical in `p4.d10-state-cascade-server.md`)

### §A — the state dispatch verbs (names + payloads + response bodies)

Nine new CoreRequest variants (serde `rename_all="camelCase"` tags in
comments); response BODIES are v4's route JSON verbatim (envelope per
the existing dispatch convention):

```rust
/// v4 `GET /api/v1/chats/[id]?action=get-state` (participants-union cascade).
ChatStateGet { chat_id: String },                              // "chatStateGet"
/// v4 `PUT /api/v1/chats/[id]?action=set-state`.
ChatStateSet { chat_id: String, state: serde_json::Value },    // "chatStateSet"
/// v4 `DELETE /api/v1/chats/[id]?action=reset-state`.
ChatStateReset { chat_id: String },                            // "chatStateReset"
/// v4 `GET /api/v1/groups/[id]?action=get-state` (OWN state, no cascade).
GroupStateGet { group_id: String },                            // "groupStateGet"
GroupStateSet { group_id: String, state: serde_json::Value },  // "groupStateSet"
GroupStateReset { group_id: String },                          // "groupStateReset"
/// v4 `/api/v1/settings/general-state` GET / PUT / DELETE.
GeneralStateGet {},                                            // "generalStateGet"
GeneralStateSet { state: serde_json::Value },                  // "generalStateSet"
GeneralStateReset {},                                          // "generalStateReset"
```

Response bodies (exact keys):

- `chatStateGet` → `{ "success": true, "state": <merged>, "chatState":
  <obj>, "projectState"?: <obj>, "groupState"?: <obj>,
  "generalState"?: <obj>, "groupTier": <GroupTier>, "projectId"?:
  <string> }` — projectState/groupState/generalState **OMITTED when
  the tier object has zero keys**; projectId omitted when the chat has
  none; `state`/`chatState`/`groupTier` always present.
- `chatStateSet` / `groupStateSet` / `generalStateSet` →
  `{ "success": true, "state": <obj> }`.
- `chatStateReset` / `groupStateReset` / `generalStateReset` →
  `{ "success": true, "previousState": <obj> }`.
- `groupStateGet` / `generalStateGet` → `{ "success": true, "state": <obj> }`.
- `GroupTier` = `{ "status": "none"|"single"|"ambiguous",
  "candidates": [{ "id": string, "name": string }],
  "appliedGroupId"?: string }` (appliedGroupId only when single).
- Set bodies validate v4's `stateBodySchema` (`state` must be a JSON
  object of unknowns); 404s use the existing not-found envelope
  (`Chat not found` / `Group not found`).

The SPA mirrors these name-for-name in `core-contract.ts`; lane D10's
wire-contract test pins the tags.

### §B — the workbench payload additions

The workbench preview and audit dispatch verbs each gain ONE optional
field, name `state`, type: JSON object or null/absent (absent/null →
`{}`): mock merged state for `$state` refs and `{{state.path}}`
templates; audit holds it fixed across every draw. No other workbench
payload change. The chat custom-tool RUN verb's payload is UNCHANGED
(state resolution is server-side off the existing `asCharacterId`).

### §C — the definition corpus wire (unifier-applied)

The committed corpus
`apps/web/src/testing/fixtures/pascal-custom-tool-definition.oracle.ndjson`
(159 rows = 10 title + 149 definition at planning time) is owned by
**lane D10**, which extends its single generator
(`harness/oracle/cases/pascal-custom-tool-definition.ts`) with the
`$state` schema families and regenerates the corpus at `7e6d13e5` —
the ONE file lane D10 may touch under `apps/web/`. Lane BE ports the
SPA schema against the same v4 behavior but does NOT edit the corpus
or its row-count guard; **the unifier updates the count literals in
`apps/web/src/app/pascal/custom-tool-types.corpus.spec.ts` to lane
D10's final counts (recorded in lane D10's status header at lane
close) and re-runs both consumers.**

### §D — the StateRef JSON shape

Everywhere both sides see it (definitions, drafts, the schema asset):
`{ "$state": <non-empty string path>, "fallback": <number | string |
boolean> }` — strict (no extra keys), `fallback` REQUIRED and typing
the ref. Draft-layer representations are lane BE's internal concern;
the wire/file shape is this.

### §E — version-bump ownership

Lane D10 bumps `quilltap-core`, `quilltap-harness`, `quilltap-host`,
`quilltap-web` (as touched, per commit) and keeps `Cargo.lock` in
step; lane BE bumps `apps/web/package.json` only. `CHANGELOG` and
`status-log.md` are append-only for both (union-merge at unification).

## Ownership (BINDING — identical in `p4.d10-state-cascade-server.md`)

- **Lane D10 owns:** `crates/**`, `Cargo.toml`/`Cargo.lock`,
  `harness/oracle/**`, `docs/v4/**`, plus the single carve-out
  `apps/web/src/testing/fixtures/pascal-custom-tool-definition.oracle.ndjson`.
- **Lane BE owns:** `apps/web/**` (everything EXCEPT that corpus
  file), including `apps/web/public/schemas/qtap-custom-tool.schema.json`.
- **Both append-only:** `docs/CHANGELOG.md`,
  `docs/developer/porting/status-log.md`.
- **Neither touches:** `CLAUDE.md` status (the unifier's),
  `docs/developer/porting/phase-4.md`, the other lane's files.

## Verification gate (run before declaring done)

1. `ng test` green (run from `apps/web` — the
   `p4.6au-av-7c-round-plan` memory), zero failures; note the
   file/spec counts.
2. `ng build` clean.
3. Full Playwright from `apps/web` against a rebuilt dist — the
   pre-existing suite byte-green, the three new beats present and
   correctly GATED (skip cleanly on main-without-D10, run live in a
   tree where the §A/§B surface exists if you can stage one; either
   way the gate condition is a server-surface probe, not a hardcoded
   skip).
4. Zero edits to `crates/**`, `harness/**`, the §C corpus, or its
   count literals (`git diff --stat` proves it).
5. The per-commit rule: follow `.claude/commands/commit.md` for every
   commit (SPA version bump per §E, CHANGELOG every commit,
   status-log unit records).
