# P4.D181 — In Their Own Words, the SPA half + bug 134: the gate, the review dialog, the Composer-card toggle, the inspector labels, and the live-settings measurement (the `686954937` + `f4ad2c8d1` drift catch-up, lane 3 of 3)

**Status: OPEN** (ordered 2026-09-10 — the `f4ad2c8d1` In-Their-Own-Words
drift catch-up round, P4.D179 ∥ P4.D180 ∥ P4.D181). The unifier rewrites
this header.

## Preamble

- **v4 oracle baseline: `cc65d6bfc`.** This lane absorbs the CLIENT half of
  **`686954937`** ("feat(salon): restate an impersonated line in the
  character's own voice", 2026-09-10 22:31 -0500) and the WHOLE of
  **`f4ad2c8d1`** ("Fix bug 134: a chat setting changed while a Salon tab
  is open never reached it", 22:47 -0500) except its `help/settings.md`
  hunk (P4.D179's re-vendor) — the ledger's two §3 rows, `ORDERED(P4.D179,
  P4.D180, P4.D181)` and `ORDERED(P4.D181)`. Spec reads are the hunks and
  the post-commit files: `git show 686954937 -- app/salon/ components/`
  and `git show f4ad2c8d1 -- app/salon/` plus `git show f4ad2c8d1:<path>`
  — **never the commit message** (ledger §5.3). Bug 134's message
  describes a v4 mechanism (a mount-only `useState` fetch) that v5 NEVER
  HAD; port from measurement, not from the sentence (§Mandate).
- **Run the drift ledger's §2 freshness probe at lane start** with the §R.2
  expected values. **Any mismatch: STOP and report.** Re-run before any
  recorder run.
- **Regen rule: PIN REQUIRED (§R.3).** This lane builds no Rust oracle; its
  SPA recorders run from **`/tmp/qt-v4-pin-p4d181-f4ad2c8d1`** (the tip):
  the `parseCarinaQuery` vector corpus and the gate's transcription check
  (`spa-oracle-recorders-live-outside-src` — recorders under `apps/web/
  oracle/`, fixtures under `src/testing/fixtures/*.ndjson`, the line count
  pinned).
- **Sibling lanes:** P4.D179 (the settings column — this lane's toggle
  card writes a key the server does not yet accept; the settings beat is
  GATED on `P4D179_SERVER_LANDED`) and P4.D180 (the verb — the dialog
  dispatches a verb the engine does not yet answer; the dialog beat is
  GATED on `P4D180_SERVER_LANDED`). Both consume §B; neither touches
  `apps/web/**`. No stacking. Unit specs mock the verb per §B's exact
  shapes.
- **Worktree:** per the `carryout` skill; `git worktree add <path> -b
  claude/p4-d181-voice-spa-<hash> main`; edit inside it; SPA `node_modules`
  via `npm ci`, never `cp -a` (`spa-worktree-node-modules`). Read
  `e2e-playwright-traps`, `e2e-port-4319-is-repo-wide`, `spa-spec-cannot-
  read-source` (**v4's bug-134 source-scan tests cannot be transcribed —
  pin the same facts structurally**), `in-place-mutation-invisible-to-a-
  computed`, `removing-a-select-option-makes-a-spec-vacuous`, `spa-has-no-
  zod`, `qt-hover-variant-must-be-hand-written`, `qt-class-guard-false-
  positives`, `a-picker-on-a-card-pulls-realtimeservice-into-that-injector`,
  and the Angular/SPA traps section of the memory index before the first
  edit. `prettier --write <dir>` is never a no-op — format files
  individually.

## Mandate

Port the client half of In Their Own Words with **v4's client code as the
oracle** (the `impersonation-overlay-spa-gaps` convention: transcribe, then
prove with a parity spec or a recorded vector corpus), so that on v5 exactly
as on v4 at `f4ad2c8d1`: (a) the pure gate `shouldRehearseImpersonatedLine`
answers v4's five rules — instance setting, an impersonated CHARACTER seat
that is not `controlledBy: 'user'`, non-blank prose, not a Carina address
(via a client-safe `parseCarinaQuery` twin recorded against v4's real
`lib/chat/carina-parser.ts`), not a bypass-once resubmit; (b) the Salon
intercepts the composer's submit when the gate fires — **nothing is cleared
and nothing is sent** — stashes `{seed, attached files, pending tool
results}`, opens the dialog, and auto-runs the preview; (c) the dialog
offers Send (the editable proposal; Cmd/Ctrl+Enter), Regenerate (carrying
edits to the draft), Edit original (back to the composer, draft intact,
cursor restored), Send as written (the draft verbatim, bypass once), and
Cancel — with the last two NEVER disabled after a failed preview; the
profile picker over the connection-profiles list defaulting to "Their own
voice (…)", the prompt picker only for 2+ prompts, both re-running on
change; the proposal scrolled into view on arrival; the "Nothing came
back…" line; (d) either Send path posts through the ordinary send with the
stashed attachments and pending results and leaves the composer empty
exactly as a normal send does; (e) the composer's Send title and the
speaking-as portrait's quill badge announce the armed gate (informational
only); (f) the Composer card gains the toggle in v4's slot with v4's copy;
(g) the LLM inspector and Wire Records label `VOICE_REWRITE`; (h) the
shared `VoiceRewriteReviewPanel` is extracted from the announcement dialog
MECHANICALLY (no behaviour change there). **And bug 134, measured then
ported:** every Salon settings read is proven LIVE against a cache flip
(the query-based reads v5 already has, pinned structurally), the
memory-cascade "remember my choice" write side is measured — v5's dialog
carries no remember arm today (`chat/memory-cascade-dialog.ts`, grep
2026-09-10) — and ported with its invalidation ONLY if v4's dialog at the
tip has it, and the `types.ts` consolidation is recorded NO-COUNTERPART
(v5 has ONE `ChatSettingsDto`). This lane touches NO crate file, NO
`help/**`, and NO committed fixture.

## Survey-verified starting points (fresh survey 2026-09-10 at `f4ad2c8d1` and on `main` `a0c95230` — do not re-derive)

### (A) v4 at the tip — the client hunks of `686954937`

- **`app/salon/[id]/hooks/useImpersonationVoice.ts`** (NEW, 362 lines) —
  `RehearsalSeat { id; type: 'CHARACTER'; controlledBy?: 'llm'|'user' }`;
  `ShouldRehearseArgs { enabled; seat; impersonatingParticipantIds; text;
  hasAttachmentsOnly; bypassOnce }`; **`shouldRehearseImpersonatedLine`**
  in THIS order of early returns: `!enabled` → `bypassOnce` → `!seat` →
  `seat.type !== 'CHARACTER'` → `seat.controlledBy === 'user'` → `!ids.
  includes(seat.id)` → `text.trim().length === 0` → `hasAttachmentsOnly` →
  `parseCarinaQuery(text)` truthy → `true`. `RehearsalTarget {
  participantId; characterName; characterTitle?; avatarSrc?; profileName?;
  modelName?; systemPrompts?: {id,name,isDefault?}[]; selectedSystemPromptId?
  }`. State: `pending: {seed, attachedFiles, pendingToolResults} | null`
  (mirrored in a ref), `target`, `stage: 'idle'|'generating'|'review'`,
  `proposal`, `profileOverride`, `systemPromptOverride`, `resolvedVoice:
  {profileName, modelName} | null`, `bypassOnceRef`. `runPreview(seed,
  profileId, systemPromptId, participantId)`: `setStage('generating')`,
  `setProposal('')`, POST `?action=impersonation-voice-preview` with
  `{participantId, seedMarkdown: seed.trim(), connectionProfileId:
  profileId || undefined, systemPromptId: systemPromptId || undefined}`;
  `!res.ok` → `showErrorToast(err.message || err.error || \`Failed (HTTP
  ${status})\`)` and `setStage('review')` (an EMPTY proposal keeps the two
  doors open); ok → `setProposal(String(proposedMarkdown || '').trim())`,
  `setResolvedVoice` when either name is present, `setStage('review')`;
  a throw → `showErrorToast(error.message || 'Failed to restate the line
  in character')` + `setStage('review')`. `intercept(e, args)`:
  `hasAttachmentsOnly = text.trim().length === 0 && (attachedFiles.length
  > 0 || pendingToolResults.length > 0)`; the gate with `bypassOnce:
  bypassOnceRef.current`; **`bypassOnceRef.current = false` ALWAYS, before
  the armed check** ("consumed by the submit it was set for, whether or
  not anything else would have fired"); `!armed || !seatTarget` → `false`;
  else `e.preventDefault()`, stash, `setTarget`, reset overrides +
  `resolvedVoice`, keep `sendArgs`, `runPreview(text, null, null,
  participantId)`, `true`. `close()` clears EVERYTHING incl. the bypass
  ref. `send(final)`: `bypassOnceRef.current = true`, `sendMessage(null,
  final, setInput, stash.attachedFiles, stash.pendingToolResults, …)`,
  `close()`. `sendAsWritten()` = `send(stash.seed)`. `regenerate()` /
  `changeProfile(id)` / `changeSystemPrompt(id)` re-run on the LIVE draft
  (the ref) with the current overrides. `editOriginal()` / `cancel()` =
  `close()` + `focusComposer()`.
- **`app/salon/[id]/hooks/__tests__/useImpersonationVoice.test.ts`** (122
  lines at the tip after `f4ad2c8d1`) — the five-rule suite (14 `it`s:
  fires; setting off; no seat; not in overlay; owner even when in overlay;
  empty; whitespace; attachment-only; `@Evangeline: …`; `@Evangeline? …`;
  `I look at @Evangeline and say nothing.` STILL fires; bypass once) + the
  bug-134 source-scan describe (`readFileSync` of `SalonView.tsx` /
  `useChatData.ts` — NOT transcribable in vitest; pin the facts
  structurally, §B item 8). Transcribe the fourteen 1:1 as the parity spec.
- **`app/salon/[id]/SalonView.tsx`** (+99/−21 across both commits) —
  `useChatSettingsQuery()` as the ONE settings source (`:120` after bug
  134); `impersonationVoiceEnabled = chatSettings?.impersonationVoice
  Rewrite ?? false`; `rehearsalTarget` memo from `speakingSeat` (`null`
  when `!p?.character`; carries `p.connectionProfile?.name/modelName`,
  `p.character.systemPrompts ?? []`, `p.selectedSystemPromptId`);
  `focusComposer` → `inputRef.current?.focus()`; `impersonationVoiceArmed =
  enabled && speakingSeat && type === 'CHARACTER' && controlledBy !==
  'user' && impersonatingIds.includes(seat.id)` (the text-free half —
  drives the cue only); the composer's `onSubmit` resolves the text, builds
  `sendArgs`, calls `impersonationVoice.intercept(e, {...})` and RETURNS
  when it answers true, else the normal send; `voiceRehearsalArmed` passed
  to `ChatComposer`; `impersonationVoice` passed to `ChatModals`.
- **`app/salon/[id]/components/ChatComposer.tsx`** — new prop
  `voiceRehearsalArmed?: boolean` (default false) → `SpeakingAsAvatar
  voiceRehearsal={…}`; the Send button's title ladder: `!hasActiveCharacters
  ? "Add a character to start chatting" : (streaming||waiting) ?
  "Generating..." : voiceRehearsalArmed && speakingAs ? \`Sends your draft
  to ${speakingAs.name} to say in their own words first\` : "Send message"`.
- **`app/salon/[id]/components/SpeakingAsAvatar.tsx`** — `voiceRehearsal?:
  boolean`; wrapper gains `relative`; title ladder: `voiceRehearsal ?
  \`Speaking as ${name} — your draft goes to ${name} to say in their own
  words first\` : canType ? \`Speaking as ${name}\` : \`Speaking as ${name}
  — waiting for the room\`` (aria-label UNCHANGED); the badge `<span
  class="absolute bottom-0.5 right-0.5 qt-bg-muted qt-text-secondary p-0.5
  flex items-center justify-center" style="border-radius: var(--radius-sm)"
  aria-hidden="true"><Icon name="thinking" class="w-3 h-3"/></span>` when
  armed.
- **`app/salon/[id]/components/ChatModals.tsx`** — `{voice.isOpen &&
  voice.target && <ImpersonationVoiceDialog …/>}` — a FRESH mount per open
  (the announcement dialog relies on the same); props: `characterName/
  Title/avatarSrc` from the target, `profileName/modelName` from
  `resolvedVoice ?? target`, `systemPrompts`, `selectedSystemPromptId`,
  `seed`/`onSeedChange=setSeed`, `proposal`/`onProposalChange=setProposal`,
  `generating = stage === 'generating'`, the two overrides, the seven
  callbacks.
- **`components/chat/ImpersonationVoiceDialog.tsx`** (NEW, 297 lines) —
  `FloatingDialog` title `` `In ${characterName}'s own words` ``,
  `storageKey="quilltap:impersonation-voice-geometry"`, 640×720, min
  420×460, `onClose={generating ? () => {} : onCancel}`; on open, fetch
  `/api/v1/connection-profiles` → `{id, name, modelName, isDefault}` cards
  (`showErrorToast(\`Failed to load connection profiles: ${msg}\`)` on
  failure); `proposalRef.scrollIntoView({block: 'nearest', behavior:
  'smooth'})` whenever `generating` flips false; body: seat header
  (avatar / initial, name, title, `voiceLine` = `` `Spoken through
  ${profileName} — ${modelName}` `` or `` `Spoken through ${profileName}`
  ``, null when both absent); "Your draft" editor (`disabled={generating}`,
  namespace `ImpersonationVoiceDialogSeed`, ariaLabel `Your draft`);
  "How should they say it?" `<select>` with `<option value="">Their own
  voice{profileName ? \` (${profileName})\` : ''}</option>` then `{name} —
  {modelName}{isDefault ? ' (default)' : ''}` per profile, `onChange →
  onChangeProfile(value || null)`; "System prompt" `<select>` ONLY when
  `systemPrompts.length > 1`, value `systemPromptOverride ??
  selectedSystemPromptId ?? ''`, options `{name}{isDefault ? ' (default)'
  : ''}`; the `VoiceRewriteReviewPanel` (namespace `ImpersonationVoice
  DialogPreview`, ariaLabel `` `What ${characterName} will say` ``) wrapped
  in a div with `onKeyDown` → Cmd/Ctrl+Enter sends when `canSend`; the
  empty-proposal line `Nothing came back. Send your own words as written,
  or go back and rewrite them.` when `!generating && proposal.trim() ===
  ''`; footer buttons in order: **Cancel** (`disabled={generating}`) ·
  **Edit original** (`disabled={generating}`) · **Send as written**
  (`disabled={generating}`) · **Regenerate** (`disabled={generating ||
  seed.trim().length === 0}`) · **Send** (primary, `disabled={!canSend}`,
  label `generating ? 'Rehearsing…' : 'Send'`), `canSend = !generating &&
  proposal.trim().length > 0`.
- **`components/chat/VoiceRewriteReviewPanel.tsx`** (NEW, 60 lines) — the
  label `What {characterName} will say`, the generating box (`qt-border-
  primary border rounded p-6 flex flex-col items-center justify-center
  gap-3 min-h-32` + `QuillAnimation size="lg"` + `Generating in
  character…`), else the editable proposal editor; props `characterName,
  generating, value, onChange, disabled?, namespace, ariaLabel`.
  **`components/chat/InsertAnnouncementDialog.tsx`** (−26/+10) swaps its
  inline preview block for the panel — MECHANICAL, no behaviour change
  (the same label, same markup).
- **`components/settings/chat-settings/ImpersonationVoiceSettings.tsx`**
  (NEW, 34 lines) — `SettingsToggleRow` heading `Impersonated lines in the
  character's own words`; the body copy verbatim: "When you have taken a
  character's seat with the Impersonate button, your draft is handed
  first to that character — their own model, their own voice — and
  returned for your inspection before a syllable reaches the room. Send
  the restatement, have it attempted afresh, retire to the composer and
  rewrite, or send your own words exactly as typed. Nothing posts until
  you say so. Speaking as yourself is untouched, as are sends that carry
  only attachments or tool results."; `enabled = settings.impersonation
  VoiceRewrite ?? false`. Mounted in `components/settings/tabs/ChatTab
  Content.tsx:107-111` INSIDE the Composer card AFTER `ComposerUnicode
  Settings`. `hooks/useChatSettings.ts:447-455` — `handleImpersonation
  VoiceRewriteChange` → `patchChatSettings({impersonationVoiceRewrite:
  value}, 'Failed to update impersonated-line voice setting', same)`.
  `types.ts:95-96` the optional field.
- **`components/chat/LLMInspectorEntry.tsx:28,45`** — `TYPE_BADGE_CLASSES.
  VOICE_REWRITE = 'qt-bg-info/15 qt-text-info'`, `TYPE_LABELS.VOICE_REWRITE
  = 'Voice'`; **`LLMInspectorPanel.tsx:18`** — `other: [..., 'VOICE_
  REWRITE']`; **`components/tools/llm-logs-card.tsx:44`** — `'VOICE_
  REWRITE': 'Voice Rewrite'`.
- **`app/salon/[id]/hooks/useSSEStreaming.ts:722-734`** — `sendMessage`'s
  first parameter becomes `React.FormEvent | null` with `e?.preventDefault()`
  — the ONE streaming-hook change; **v5 has no such seam** (the composer
  emits a payload, the Salon's `runTurn` takes options) — NO-COUNTERPART,
  recorded.
- **`app/salon/[id]/types.ts:350-351`** — the optional field on the (then)
  salon-local `ChatSettings` — superseded by `f4ad2c8d1` (below).

### (A′) v4 at the tip — the bug-134 hunks of `f4ad2c8d1`

- **`SalonView.tsx:111-125`** — `useChatData` no longer returns
  `chatSettings`/`fetchChatSettings`; `const { data: chatSettings } =
  useChatSettingsQuery()` is the ONE source for all nine reads (the
  effect at `:864-867` drops `fetchChatSettings()`).
- **`hooks/useChatData.ts`** — `chatSettings` state + `fetchChatSettings`
  (a `useState` filled once from the mount effect, with a hard-coded
  fallback row on failure) DELETED.
- **`components/VirtualizedMessageList.tsx:40-41`** — `chatSettings:
  ChatSettings | null | undefined` ("`undefined` while the settings query
  is in flight; every read below defaults").
- **`hooks/useMessageActions.ts:4,9,34,142-146`** — after the memory-
  cascade "remember my choice" PUT, `await queryClient.invalidateQueries
  ({queryKey: queryKeys.settings.chat})` ("the remembered choice has to be
  published there too — otherwise 'don't ask me again' holds for this
  dialog and is forgotten by the next one until something else
  refetches").
- **`types.ts:305-326`** — the salon-local `ChatSettings` + four sub-shapes
  become `export type { … } from '@/components/settings/chat-settings/
  types'` (one declaration; the copy's `tagStyles` had no reader).
- **`useImpersonationVoice.test.ts:33-52`** — the source-scan describe
  reshaped: `SalonView` contains `const { data: chatSettings } =
  useChatSettingsQuery()` and `chatSettings?.impersonationVoiceRewrite`;
  `SalonView` does NOT contain `fetchChatSettings`; `useChatData.ts`
  contains neither `chatSettings` nor `/api/v1/settings/chat`.

### (B) v5 on `main` (`a0c95230`) — where each lands, and what is already there

- **The settings source, ALREADY query-based:** `apps/web/src/app/screens/
  salon/salon-conversation.ts:1053-1060` — `settingsQuery = injectQuery(()
  => ({ queryKey: ['chatSettings'], queryFn: dispatchExpect({type:
  'chatSettings'}) }))`; `settings = computed(() => settingsQuery.data() ??
  null)` (`:1063`); every reader takes it as an input (`chat/message-list.
  ts:184`, `message-row.ts:432`, `conversation-header.ts:121` — all
  `input<ChatSettingsDto | null>(null)`); `salon-list.ts:111` the same key.
  **The write side:** `screens/settings/chat/chat-settings.api.ts:60-89` —
  `ChatSettingsCard.save` PUTs, `setQueryData(chatSettingsKeys.all, updated)`
  then `invalidateQueries(chatSettingsKeys.all)` (v4's `mutateSettings`
  twin). **Tab re-activation:** `workspace/core/tab-refetch.ts:94` sweeps
  BOTH spellings `['chatSettings']` / `['chat-settings']`. **So v4's bug
  134 mechanism — a mount-only snapshot — has no v5 counterpart; the
  measurement (Tier 1 item 8) is what the lane ships, not the deletion.**
- **The memory-cascade dialog:** `apps/web/src/app/chat/memory-cascade-
  dialog.ts` (v4 `MemoryCascadeDialog`, microcopy verbatim) — **no
  "remember" arm at all** (grep `remember|checkbox` → nothing);
  `salon-conversation.ts:3603-3628` `onCascadeConfirm(action)` dispatches
  `messageDelete` with `memoryAction` + `skipConfirmation: true` and never
  PUTs a preference. **Measure v4's dialog at the pin** (`components/chat/
  MemoryCascadeDialog.tsx` and `useMessageActions.ts:120-150`): if v4's
  dialog offers "remember my choice" and writes `memoryCascadePreferences`,
  that whole arm is a PRE-EXISTING v5 gap this lane closes WITH the bug-134
  invalidation (`queryClient.invalidateQueries({queryKey: chatSettingsKeys.
  all})` after the PUT); if v4's remember arm lives elsewhere, record
  where and port the invalidation at THAT site.
- **`ChatSettingsDto`:** ONE declaration, `core/core-contract.ts:3528`
  (`autoScrollOnResponseComplete?`, `textReplacementsEnabled?` …) — add
  `impersonationVoiceRewrite?: boolean` with v4's doc comment; the
  `types.ts` consolidation is NO-COUNTERPART (v5 never had a second copy).
- **The composer:** `chat/chat-composer.ts` — `ComposerSend { content;
  fileIds }` (`:70`); `speakingAs = input<SpeakingAsSeat | null>` (`:563`);
  `send = output<ComposerSend>()` (`:575`); `canSend` (`:635`); `sendTitle`
  (`:661-663`, two arms today — the four-arm ladder lands here, needing a
  `busy`/streaming arm and the armed arm; measure which v5 signals carry
  v4's `streaming || waitingForResponse`); **`submit()` (`:835-863`) CLEARS
  the editor, the source text, `text`, and the attachment tray ITSELF
  right after `send.emit(...)`** — v4's composer clears NOTHING (only
  `sendMessage` does). This is the lane's one structural decision: "Edit
  original" and "Cancel" require the draft and the tray to SURVIVE an
  intercepted submit. Restructure so the clear is owned by the Salon's
  send path (the composer exposes a public `clearAfterSend()` — it already
  exposes `addAttachedFile`; `submit()` emits and stops clearing; the
  Salon's `send()` clears on the direct path and both dialog Send paths
  clear through the same call; Edit original / Cancel never call it). The
  draft persistence (`restoredDraft()` / the draft store) must follow the
  same rule — measure where the draft is cleared today and move it with
  the composer clear. Pin the invariant with specs at BOTH levels: the
  composer no longer clears on emit; the Salon clears on send and not on
  intercept.
- **The Salon send seam:** `salon-conversation.ts:480` `(send)="send
  ($event)"`; `:3003-3009` `send(payload)` snapshots + CLEARS
  `pendingToolResults` BEFORE `runTurn` (v4 `useSSEStreaming:606-612`) —
  with the intercept, the snapshot moves AFTER the gate (an intercepted
  submit keeps the pending results in state, as v4 does); `:3015` `runTurn
  ({content, fileIds, continueMode, respondingParticipantId, nudge,
  pending})` → the optimistic bubble + `chatSend`. The intercept sits at
  the TOP of `send()`: gate → stash `{seed: payload.content, fileIds,
  pending: this.pendingToolResults()}` → open → preview; else the existing
  body.
- **The speaking seat:** `salon-conversation.ts:2042` `speakingSeat =
  computed<ParticipantDetail | null>` (v4's `findActiveUserParticipant`
  twin; bug 123 split it from `speakingAsSeat` `:2058` — the composer's
  `SpeakingAsSeat` view model); `:149-157` the `impersonatingIds` sync;
  `:357` `[impersonatingParticipantIds]="impersonatingIds()"`. Measure
  whether `ParticipantDetail` carries `character.systemPrompts`,
  `character.title`, and `connectionProfile.{name, modelName}` — the
  `RehearsalTarget` needs them; if the chat GET's participant enrichment
  lacks `systemPrompts`, the prompt picker needs a character read (record
  the divergence; do not invent a verb).
- **`chat/speaking-as-avatar.ts`** (73 lines) — `name`, `avatarUrl`,
  `canType` inputs; `titleText` two arms; no badge. Add `voiceRehearsal =
  input(false)`, the three-arm title, the badge (`qt-icon name="thinking"`;
  the wrapper needs `position: relative` — measure whether `relative` /
  `absolute` / `bottom-0.5` / `right-0.5` / `p-0.5` are live utilities in
  v5's stylesheet (`styles/qt-components/_chat.css:1278` owns
  `.qt-speaking-as-avatar`); if not, add the rule to that CSS block rather
  than an inert class name — `qt-class-guard-false-positives`,
  `qt-hover-variant-must-be-hand-written`).
- **The Carina twin:** NONE in the SPA (`parseCarinaQuery` appears nowhere
  under `apps/web/src`; the server twin is `crates/quilltap-core/src/
  carina_parser.rs`). Transcribe v4's `lib/chat/carina-parser.ts` at the
  pin as `chat/carina-parser.ts` (client-safe, no imports) and record a
  vector corpus against v4's REAL function with the `apps/web/oracle/`
  recorder pattern (≥ 40 vectors: the two markers `:` / `?`, leading
  whitespace, a name with spaces/apostrophes/Unicode, `@name` mid-text,
  `@` alone, empty, the case rules, whatever the real regex admits — read
  the source, then write the corpus; a `pinned line count` guard).
- **The announcement dialog:** `chat/post-office/insert-announcement-
  dialog.ts` (imports `Modal, MarkdownField, QuillAnimation`; the preview
  block at `:355-362` — the label `What {{ name }} will say`, the
  `qt-quill-animation size="lg"` box with `Generating in character…`, the
  editor) — extract into `chat/impersonation-voice/voice-rewrite-review-
  panel.ts` (`qt-voice-rewrite-review-panel`) and use it from BOTH dialogs;
  the announcement dialog's existing specs must stay green UNCHANGED (the
  extraction is mechanical). **The dialog host:** v5's announcement dialog
  is a `Modal`, v4's a `FloatingDialog` (geometry + storageKey) — follow
  the sibling (`Modal`), record the divergence in the m6 class doc as the
  announcement dialog did.
- **The verb client:** `chat/post-office/post-office.api.ts:144-172`
  `previewAnnouncement` (the `|| undefined` omission, the `.trim()`) — the
  sibling `previewImpersonationVoice(core, {chatId, participantId,
  seedMarkdown, connectionProfileId?, systemPromptId?})` returning the
  four-key body (`ChatImpersonationVoicePreviewResponse`). The §B request
  interface beside `ChatAnnouncementPreviewRequest` (`core-contract.ts:
  214-227`). Connection profiles for the picker: reuse the existing
  connection-profiles read the announcement dialog uses.
- **Labels:** `chat/llm-inspector-entry.ts:31,48`, `chat/llm-inspector-
  panel.ts:34`, `screens/settings/system/llm-logs-card.ts:20` — the three
  `CUSTOM_TOOL_CONSULT` lines are the insertion points.
- **The toggle card:** `screens/settings/chat/composer-emoji-settings.ts`
  is the shape (extends `ChatSettingsCard`; `qt-settings-toggle-row`;
  `enabled` computed with the default; `save({key: value}, 'Failed to …')`);
  mount in `chat-tab.ts:105-117` inside the Composer card AFTER
  `<qt-composer-unicode-settings />` (v4's order). The e2e
  `settings-chat-cards-flow.spec.ts:81` walks the `Composer` card.
- **Existing beats to extend/mirror:** `e2e/salon-dialogs-flow.spec.ts:385`
  (the impersonation overlay beat — its seeded chat and gesture are the
  precedent for impersonating a seat), `e2e/composer-active-seat.spec.ts`
  (the speaking-as portrait), `e2e/settings-chat-cards-flow.spec.ts`.

## Tiered deliverables

### Tier 1 — must land

1. **The gate + the Carina twin** — `chat/impersonation-voice/gate.ts`
   (`shouldRehearseImpersonatedLine`, `RehearsalSeat`, `ShouldRehearseArgs`,
   v4's early-return order) + `chat/carina-parser.ts` (the client-safe
   transcription) + the recorded vector corpus (`apps/web/oracle/carina-
   parser-recorder.ts` → `src/testing/fixtures/carina-parser.ndjson`, the
   line count pinned) + the fourteen-case parity spec transcribed 1:1 from
   v4's suite. Mutation: reorder `bypassOnce` after `!seat` — no case
   reddens (record it — v4's order is unobservable on the corpus and pinned
   only by transcription) ; drop the `controlledBy === 'user'` arm → the
   owner case reddens.
2. **The state service** `chat/impersonation-voice/impersonation-voice.
   state.ts` (the hook's twin: signals for `pending`/`target`/`stage`/
   `proposal`/overrides/`resolvedVoice`, a plain field for the bypass
   latch; `intercept(...)` → boolean with the ALWAYS-consume bypass rule;
   `runPreview` with the three toast sentences and the stay-in-review
   rule; `send`/`sendAsWritten`/`regenerate`/`changeProfile`/
   `changeSystemPrompt`/`editOriginal`/`cancel`/`close` per §A) — provided
   at the Salon component level (NOT `providedIn: 'root'` — the #105
   NG0201 lesson), with specs for every transition and the bypass
   consumption (set → consumed by the next intercept → cleared on close).
3. **The composer clear restructure** (§B) + the Salon intercept at the
   top of `send()` — the invariant specs at both levels; the pending
   snapshot moved behind the gate.
4. **The dialog** `chat/impersonation-voice/impersonation-voice-dialog.ts`
   (`qt-impersonation-voice-dialog`) + **the shared panel** extracted from
   the announcement dialog — every string, control, disabled rule, and
   order per §A; Cmd/Ctrl+Enter; the scroll-into-view on arrival (an
   `afterRenderEffect` keyed on `generating` flipping false); the
   connection-profiles read; the fresh-mount-per-open host in the Salon's
   modals region. Specs: the five footer doors incl. the two that survive
   a failed preview; the prompt picker hidden at ≤ 1 prompts (`removing-
   a-select-option-makes-a-spec-vacuous` — assert the select's ABSENCE,
   not a value); the profile change dropping the proposal and re-running.
5. **The cue** — `voiceRehearsalArmed` on the composer (the four-arm send
   title) and `voiceRehearsal` on the avatar (the three-arm title + the
   badge, CSS-backed if the utilities are inert); `impersonationVoiceArmed`
   computed in the Salon from `settings()`, `speakingSeat()`, and
   `impersonatingIds()`. Specs on both components.
6. **The Composer-card toggle** `screens/settings/chat/impersonation-voice-
   settings.ts` with v4's heading + copy byte-for-byte, mounted after the
   unicode toggle; `ChatSettingsDto.impersonationVoiceRewrite?`; a spec in
   the chat-settings spec-harness idiom (`chat-settings.spec-harness.ts`).
7. **The labels** — the three insertion points; a spec asserting the
   `VOICE_REWRITE` badge text/class, the `other` group membership, and the
   Wire Records label.
8. **Bug 134, measured and pinned:** (a) a Salon spec that seeds
   `['chatSettings']` in the `QueryClient`, renders, flips the cache via
   `setQueryData`, and asserts the DERIVED reads follow — `llmLoggingEnabled`
   (the inspector button), `showChatTotals`, the auto-scroll flag, thinking
   display, story backgrounds, `impersonationVoiceRewrite` — the
   structural twin of v4's source-scan (the spec cannot read source); (b)
   a spec that a settings-card `save` results in the Salon's `settings()`
   reflecting the PUT echo (the `setQueryData` + invalidate path); (c) the
   memory-cascade remember arm: MEASURE v4 at the pin, port the arm + the
   invalidation if present (with a spec), else record; (d) the `types.ts`
   consolidation + `useChatData` deletion + `sendMessage(null)` recorded
   NO-COUNTERPART in the lane record and the m6 class doc.
9. **Two e2e beats, AUTHORED and PARKED by NAME** (`round-plan-takeaways`:
   a named constant, never a capability probe): (a) `settings-chat-cards-
   flow` gains the Impersonated-lines toggle round trip, gated on
   `P4D179_SERVER_LANDED = false`; (b) a `salon-impersonation-voice-flow`
   spec: impersonate the seeded LLM seat (the `salon-dialogs-flow` gesture),
   flip the toggle through the UI, type a line, Enter → the dialog opens
   with the draft intact and the composer NOT cleared → the mock provider's
   canned reply appears as the proposal → Edit original returns the draft
   untouched → resend → Send as written posts the ORIGINAL bytes (assert
   the newest bubble's content) → a second line → Send posts the PROPOSAL;
   plus the attachment-only bypass; gated on `P4D180_SERVER_LANDED = false`.
   Their first execution is the unifier's (§R.8) — write them to the
   fixture's seeds you can see today, and read `e2e-assertion-can-read-
   the-pre-click-state` / `a-host-beat-must-wait-on-the-effects-partition`
   before writing a waiter.

### Tier 2 — should land

10. The m6 class doc rows for the two recorded divergences (`Modal` vs
    `FloatingDialog`; the composer-clears-on-emit → Salon-clears-on-send
    restructure) and the three NO-COUNTERPARTs.
11. A `check-qt-classes`-clean pass plus a computed-style probe spec for
    the badge's positioning (the inline-host / inert-utility class the
    #97/#107/#108 findings keep producing).

### Tier 3 — explicit deferrals (loud, typed, never silent)

- If `ParticipantDetail` lacks `character.systemPrompts`, the prompt picker
  is rendered from what the target carries (possibly never — ≤ 1 prompts)
  and the gap is RECORDED for the chat-GET enrichment owner, not patched
  with a new verb.
- No help doc edits (P4.D179's re-vendor carries `help/impersonation-
  voice.md` and the four siblings).

## The differential requirement (restated)

The SPA's oracle is v4's client code: the gate's fourteen cases transcribed
1:1 (a parity spec), the Carina parser proven by a recorded vector corpus
over v4's REAL `lib/chat/carina-parser.ts` from the pinned worktree (the
`apps/web/oracle/` recorder pattern — `spa-oracle-recorders-live-outside-
src`), the copy strings byte-compared against the pinned files, and the
bug-134 facts pinned structurally. Closest precedents: the P4.D58/P4.D60
impersonation client twins (`isUserDrivenSeat` parity spec), the P4.D96
vocative-regex transcription, the P4.9L2 toolbar's v4-side jsdom recorder,
and `p4.d72-wardrobe-staged-replay-spa.md`'s mandate paragraph.

## Fixtures

- Delivers: `src/testing/fixtures/carina-parser.ndjson` (+ its recorder).
- Consumes READ-ONLY: the committed e2e instance (seeds via
  `e2e/support/**` — a needed seed change is a STOP-and-flag; the
  `salon-dialogs-flow` impersonation beat proves the fixture already holds
  an impersonatable LLM seat with a mock-served profile).
- Touches NO crate fixture and NO committed DB pair.

## §R — the round-wide meeting points (BINDING — identical in every order of this round)

Round: **the `f4ad2c8d1` In-Their-Own-Words drift catch-up round** — P4.D179
(the `chat_settings` column + the `VOICE_REWRITE` log type + the Almanack
ledger + the `help/**` re-vendor, server) ∥ P4.D180 (the in-scene voice
rehearsal: the shared core, the new service, the verb, the host wire, server)
∥ P4.D181 (the SPA half of both commits + bug 134). Ordered 2026-09-10. Three
lanes, all from `main`, no stacking.

1. **Baseline and drift.** Oracle baseline **`cc65d6bfc`** (drift-ledger §1).
   v4 `main` HEAD **`f4ad2c8d1`** — **TWO commits past the baseline**, the
   ledger's two §3 rows, now `ORDERED`:
   - **`686954937`** ("feat(salon): restate an impersonated line in the
     character's own voice", 2026-09-10 22:31 -0500, 60 files) —
     `ORDERED(P4.D179, P4.D180, P4.D181)`. PORT-NEW with PORT riders: the new
     `chat_settings."impersonationVoiceRewrite"` column (migration
     `add-impersonation-voice-rewrite-field-v1`), the `VOICE_REWRITE` log
     type + the `mapTaskTypeToLogType` rows (**v5 measurably carries v4's
     pre-fix silent default** — `announcement-rewrite` files as
     `SUMMARIZATION` today, `services/llm_logging.rs:392-418`), the
     `character-voiced.ts` → `voice-rewrite-core.ts` extraction (read as
     behaviour-neutral, TO BE PROVEN by regen at both pins), the new
     `in-scene-voiced.ts` service + the `impersonation-voice-preview` chat
     action, the Almanack `featureConfig` row, `help/**` 123 → 124, and the
     whole Salon/Settings client half.
   - **`f4ad2c8d1`** ("Fix bug 134: a chat setting changed while a Salon tab
     is open never reached it", 2026-09-10 22:47 -0500, 14 files) —
     `ORDERED(P4.D181)`, with its `help/settings.md` hunk riding P4.D179's
     re-vendor. PORT, **measured before ported**: v5's Salon already reads
     settings through the `['chatSettings']` TanStack query
     (`salon-conversation.ts:1053`), the settings card's save writes and
     invalidates that key (`chat-settings.api.ts:86-89`), and tab
     re-activation sweeps both spellings (`tab-refetch.ts:94`) — the
     staleness bug 134 fixes may be structurally absent in v5. The lane
     measures each of v4's hunks and ports only what measures present.
   v4 `bugfix` tip **`1a2b2164c`** unmoved; v4 `release` tip **`8fbf2afe0`**
   (4.9.2) unmoved. The round's catch-up TARGET is `f4ad2c8d1`; the unifier
   moves the baseline there.
2. **The freshness probe at lane start** (ledger §2), expected values:
   checkout on **`main`**, HEAD **`f4ad2c8d1`**, tree **CLEAN**, `git log
   --oneline f4ad2c8d1..main` EMPTY, `git log --oneline 1a2b2164c..bugfix`
   EMPTY. **Any mismatch: STOP and report** — a lane never writes the
   ledger. There is NO pre-authorized exception this round. Re-run the probe
   before every regen batch; the checkout has gone dirty MID-LANE before
   (ledger §5.1) and was switched to `release` mid-unify once (P4.D91). v4
   ships a commit every day or two — expect the probe to fail mid-round; a
   failing probe is a STOP, not a reason to pin at whatever HEAD is.
3. **Regen rule: PIN REQUIRED, lane-unique paths, all three symlink
   classes** (ledger §5.1), every regen and fixture build through
   `harness/tools/recipe_sweep.py --v4 <pin> --run <family>`; never two
   sweeps concurrently.
   - **P4.D179 pins `f4ad2c8d1`** for every family it MOVES
     (`/tmp/qt-v4-pin-p4d179-f4ad2c8d1`) and **`cc65d6bfc`** for neutrality
     regens (`/tmp/qt-v4-pin-p4d179-cc65d6bfc`). The help re-vendor is a
     `cp` of the five moved files from the `f4ad2c8d1` pin's `help/`,
     never a hand edit.
   - **P4.D180 pins `f4ad2c8d1`** for every family it MOVES or CREATES
     (`/tmp/qt-v4-pin-p4d180-f4ad2c8d1`) and **`cc65d6bfc`**
     (`/tmp/qt-v4-pin-p4d180-cc65d6bfc`) for the announcer neutrality
     proof: `announcer_tier3_equivalence`'s NDJSON regenerated at BOTH pins
     must be byte-identical (that is the proof that v4's extraction moved
     nothing), and v5's extraction must be green against both.
   - **P4.D181** builds NO Rust oracle; its two SPA recorders (the
     `parseCarinaQuery` corpus and the `shouldRehearseImpersonatedLine`
     transcription check) run from `/tmp/qt-v4-pin-p4d181-f4ad2c8d1`
     (`spa-oracle-recorders-live-outside-src`).
   - **Verify each pin by a marker**: `impersonationVoiceRewrite` appears in
     `lib/schemas/settings.types.ts` ONLY in the `f4ad2c8d1` worktree;
     `VOICE_REWRITE` in `lib/schemas/llm-log.types.ts` ONLY there;
     `lib/services/announcer/in-scene-voiced.ts` EXISTS only there;
     `git diff --stat cc65d6bfc f4ad2c8d1 -- help/` lists exactly six paths
     (`chat-participants.md`, `chat-settings.md`, `impersonation-voice.md`,
     `insert-announcement.md`, `settings.md` — five from the two commits,
     `settings.md` counted once). Check every NDJSON is non-empty before
     believing any diff (the empty-file trap).
   - Committed recipe headers keep saying `cd ~/source/quilltap-server` and
     never name a `/tmp` pin (`recipe-header-conventions`). The oracle
     `node_modules` are the LIVE tree (`zod` 4.5.4 — `zod_version_guard`
     fails the gate if it moves; neither commit changes a dependency —
     both `package-lock.json` deltas are the version bump, re-measured
     2026-09-10).
   - **`QT_V4_ROOT` for the full workspace gate:** P4.D179 sets it to its
     `f4ad2c8d1` pin from its help re-vendor commit onward (before that
     commit, `help_tree_embed_guard` is green only against a `cc65d6bfc`
     root — build the neutrality pin first and use it until then). P4.D180
     sets it to its `cc65d6bfc` pin for EVERY gate (its embedded help tree
     stays the 123-file one until unification). `qtap_schema_embed_guard`
     is green at BOTH pins (`public/schemas/qtap-export.schema.json` is
     untouched by both commits — measured).
4. **Stacking and resumption.** None. All three lanes branch from `main` in
   SEPARATE worktrees and run in parallel. The unifier cherry-picks
   P4.D179, then P4.D180, then P4.D181.
5. **Frozen and single-owner files.**
   - `crates/quilltap-core/src/api/types.rs` + `api/engine.rs` — **P4.D180's
     alone** (the one new `Request`/`CoreResponse` pair; P4.D179 adds NO
     settings mirror — measured 2026-09-10: no per-column field for
     `composerUnicode` exists in `api/types.rs`, the settings bag rides raw).
   - `crates/quilltap-host/src/host.rs` — **P4.D179's alone** (the ONE new
     boot-ensure call after the P4.D73 block at `:1097-1108`).
   - `crates/quilltap-host/src/spine.rs` — **P4.D180's alone** (the new
     driver's construction beside `HostAnnouncementPreviewRunner`,
     `:657-720` + `:3593-3597`).
   - `crates/quilltap-core/src/services/announcer/**`,
     `api/chat_post_office.rs`, `services/system_prompt_compiler.rs` (the
     `read_current_stacks` visibility + the per-participant reader twin
     ONLY) — **P4.D180's alone**.
   - `crates/quilltap-core/src/db/chat_settings.rs`, the NEW
     `db/chat_settings_impersonation_voice_repair.rs`, `api/settings.rs`,
     `services/llm_logging.rs`, `almanack/**`, `services/provisioning/**`
     (via generators), `help/**` + every vendored-count literal — **P4.D179's
     alone**.
   - `apps/web/**` — **P4.D181's alone**; both server lanes finish with
     `git diff main -- apps/web/` EMPTY.
   - `Cargo.toml`/`Cargo.lock` at the workspace root and per crate: version
     lines only; a dependency add is a STOP-and-flag in the lane record (no
     lane expects one).
   - `docs/v4/**` is the unifier's; `docs/developer/porting/drift-ledger.md`
     is frozen (lanes never write it).
6. **§B — the shared contract (name-level, binding; the unifier diffs it
   name-for-name across `api/types.rs`, the web renderer, and
   `core-contract.ts` at the wire).**
   - **The settings key** `impersonationVoiceRewrite`: boolean, default
     `false`. Column `chat_settings."impersonationVoiceRewrite" INTEGER
     DEFAULT 0` at the position v4's live `generateDDL` puts it (the D23
     dump decides — never hand-placed; the Zod schema declares it after
     `composerUnicode` and before `textReplacementsEnabled`,
     `lib/schemas/settings.types.ts:645-646`). The chat-settings GET echoes
     it; the PUT accepts the single-key body `{"impersonationVoiceRewrite":
     true}`; a present-but-non-boolean value — an explicit `null` included
     — answers 400 `{"error":"Invalid impersonationVoiceRewrite value (must
     be boolean)"}` (v4 throws that sentence and the route's
     `includes('Invalid') ? 400 : 500` split makes it a 400; the
     `Option<Option<bool>>` / raw-bag shape so the web edge cannot collapse
     the null — the Taboo §3 lesson). The SPA reads it from the existing
     `chatSettings` verb and saves through `ChatSettingsCard.save`.
   - **The verb** `chatImpersonationVoicePreview` (P4.D180 defines; P4.D181
     consumes; no REST edge — see below). Request, wire keys exactly:
     `{ "type": "chatImpersonationVoicePreview", "chatId", "participantId",
     "seedMarkdown", "connectionProfileId"?, "systemPromptId"? }` — the two
     optionals are OMITTED when unset, never `null` (the SPA mirrors
     `previewAnnouncement`'s `|| undefined`; Rust: `Option<String>` with
     `#[serde(default, skip_serializing_if = "Option::is_none")]`). Success
     data, key order as v4 `NextResponse.json`: `{ "success": true,
     "proposedMarkdown": string, "profileName": string, "modelName": string
     }`, carried as `CoreResponse::ChatImpersonationVoicePreview(Value)`.
     Errors, byte-exact: 404 `Chat` / `Participant` / `Character` through
     the existing `not_found(..)` sentences; 400 `Only a character seat can
     be spoken for.` · `That character is not present in this chat.` ·
     `That seat is not being impersonated.` · `No connection profile to
     rewrite with` · the service's own `error` string, or `Failed to restate
     the line in character.` when it is empty (JS falsy); a Zod refusal
     through the existing `validation_error()` envelope. **Guard order is
     v4's** (the dispatcher's chat 404 precedes the body parse; the parse
     precedes every participant check) — P4.D180 measures it on v4 before
     transcribing.
   - **The log type**: `llm_logs.type = "VOICE_REWRITE"` for BOTH task types
     `announcement-rewrite` and `impersonation-voice-rewrite`
     (`log_type::VOICE_REWRITE`, P4.D179); P4.D180 spells the new task type
     as the string constant `"impersonation-voice-rewrite"` and does NOT
     touch the map. **The live proof that a rehearsal's row lands as
     `VOICE_REWRITE` needs both lanes on one branch** — P4.D180's web-venue
     wire test asserts the row's type through a NAMED constant
     `EXPECTED_REHEARSAL_LOG_TYPE = "SUMMARIZATION"` with an
     ACTIVATE-AT-UNIFY comment; the unifier flips it to `"VOICE_REWRITE"`
     and the test must go green on the flip (red-first on the unify branch
     if it does not).
   - **The SPA labels** (P4.D181): `VOICE_REWRITE` → inspector badge text
     `Voice` with class `qt-bg-info/15 qt-text-info`, inspector filter group
     `other` (after `CUSTOM_TOOL_CONSULT`), Wire Records label `Voice
     Rewrite`.
   - **No REST edge this round.** v5 serves NO `POST /api/v1/chats/{id}?
     action=` REST family — `announcement-preview` is dispatch-only
     (measured 2026-09-10: the string appears nowhere in
     `crates/quilltap-web/src`; the CLI never calls either action). The new
     action follows its sibling; the absence is RECORDED in P4.D180's lane
     record, not invented.
7. **Version bumps** — bump every crate whose files (tests included) the
   commit touches. P4.D179: `quilltap-core`, `quilltap-harness`,
   `quilltap-host` (the ensure call + its `build.rs`-embedded `help/**`),
   `quilltap-web` ONLY if its test venue's hand-rolled `chat_settings` DDL
   moves (measure — `crates/quilltap-web/tests/chat_settings_composer_web_
   routes.rs` drops columns by name). P4.D180: `quilltap-core`,
   `quilltap-harness`, `quilltap-host`, `quilltap-web` (the web-venue wire
   test). P4.D181: the SPA `package.json` only. Per commit. The unifier
   RECOUNTS as base + total bumps — identical bumps merge silently
   (`commit-checklist`; the silent-auto-merge trap fired at the `f699da6f6`
   and `cc65d6bfc` rounds). Base at ordering: core 0.0.886, harness
   0.0.775, host 0.0.125, web 0.0.136, cli 0.0.20, tauri 0.0.7, SPA
   0.5.695.
8. **e2e.** Only P4.D181 touches Playwright. It AUTHORS the two new beats
   gated by NAMED constants (`P4D179_SERVER_LANDED = false` for the
   settings-toggle round trip, `P4D180_SERVER_LANDED = false` for the
   dialog flow) and runs the FULL suite on its branch with them parked —
   never a capability probe (`round-plan-takeaways`: a DEFINED verb defeats
   a probe). The unifier flips both constants and runs the full suite;
   **activating a beat is its first execution** — expect gesture fixes.
9. **Append-only for everyone:** `docs/CHANGELOG.md`,
   `docs/developer/porting/status-log.md`.
10. **Fixture ownership.** P4.D179 owns `harness/oracle/fixtures/
    settings.json` + `build-settings-fixture.ts` + `cases/settings-
    routes.test.ts` (the new `impersonation_voice` family rows),
    `fixtures/chat-settings-tier2.json` + `build-chat-settings-fixture.ts`
    + `cases/chat-settings-tier2.ts`, the provisioning dumps
    (`fresh_schema.json`, `chat_settings_seed.json`, `schema-key-order.json`
    — via the shipped generators only), the NEW `cases/task-type-log-
    mapping.ts` + its corpus, and the four help families' regen. P4.D180
    owns the NEW committed `in-scene-voiced-{main,mount}.db` pair (built
    from a `/tmp`-staged builder described in PROSE in the family header —
    `a-committed-fixture-builder-belongs-in-prose`), the NEW
    `cases/in-scene-voiced-tier3.test.ts` + corpus, and `cases/announcer-
    tier3.test.ts`'s regen AS-IS (neutrality — no case edit). The committed
    `post-office-{main,mount}.db` pair is READ-ONLY for everyone. P4.D181
    owns `apps/web/oracle/**` recorders + `src/testing/fixtures/*.ndjson`
    it adds, and touches NO committed DB pair. **No lane regenerates a
    committed DB pair it does not own**, and no two lanes touch one case
    file.
11. **Census guards.** Every Rust lane's gate is `cargo test --workspace`,
    never `-p` (`a-new-verb-moves-the-dispatch-wrong-type-census`,
    `closing-a-divergence-moves-the-censuses`, `a-new-harness-test-can-trip-
    a-source-census`). P4.D180's verb carries three `*_id` fields — the
    `dispatch_wrong_type_census.rs:2464` constant (`EXCLUDED_BY_THE_ROUTE_
    IDENTIFIER_RULE = 431`) WILL move; measure, don't guess, and record the
    new count. `db_error_key_guard`'s per-file counts must not move (use
    `DbError::Internal`, never `Key`, at every new site). P4.D179's help
    re-vendor moves every vendored-count literal — the memory note
    `a-vendored-count-is-hard-coded-in-several-crates` (FOUR literals in
    THREE crates last time; `help_tree_embed_guard.rs:30` and
    `host_help_docs_boot.rs:82-89` are two of them; find the rest by one
    `--workspace` run).
12. **Cross-app hazards.** (a) The Friday instance is SHARED with v4's dev
    build, which ran `add-impersonation-voice-rewrite-field-v1` on it the
    day it shipped: P4.D179's boot ensure MUST be an idempotent no-op on a
    table that already carries the column, with v4's byte-exact `INTEGER
    DEFAULT 0` on the add path so a v4 later opened on a v5-added column
    sees the same default (the P4.D73 rule). (b) `llm_logs.type` is plain
    TEXT; a v5-written `VOICE_REWRITE` row is readable by v4 at the tip
    (its enum now admits it) — no hazard, recorded. (c) No `PROMPT_CACHE_
    STRUCTURE_VERSION` / `IDENTITY_STACK_BUILDER_VERSION` move: the
    rehearsal READS the stored compiled stack, it never writes one.
13. **Upstream filings are PREPARED, never committed, by a lane.** None is
    expected this round; a P4.D181 bug-134 measurement that finds v4 still
    stale somewhere is drafted in its lane record for the human.
14. **The turn-time compiled-stack reader stays UNPORTED** (the standing
    P4.D103/P4.D163 deferral: `orchestrator.rs:4151` passes
    `precompiled_identity_stack: None`; the tier-3 oracles stub v4's
    `getCompiledIdentityStack` to null). P4.D180's rehearsal service reads
    the stored stack ITSELF, exactly as v4's `in-scene-voiced.ts:219` does
    — through a per-participant reader twin over the compiler's
    `read_current_stacks` — because the rehearsal is a NEW surface with no
    deferral debt, and v4's `subprompts: precompiledIdentityStack ? null :
    subprompts` arm is only measurable with a real stack in the fixture.
    The orchestrator's deferral is NOT lifted by this round.

## Ownership (BINDING — identical table in every order of this round)

| Lane | Owns (edit freely) | Must not touch |
| --- | --- | --- |
| **P4.D179** (the column + log type + Almanack + help, from `main`) | `crates/quilltap-core/src/db/chat_settings.rs`; NEW `db/chat_settings_impersonation_voice_repair.rs` (+ its `db/mod.rs` line); `crates/quilltap-host/src/host.rs` (the ONE ensure call, after the P4.D73 block); `api/settings.rs`; `services/llm_logging.rs`; `almanack/{types,phase3_ledgers,render}.rs`; `services/provisioning/{fresh_schema.json,chat_settings_seed.json,schema-key-order.json}` (via generators); `help/**` (byte copies from the `f4ad2c8d1` pin) + every vendored-count literal (`crates/quilltap-harness/tests/help_tree_embed_guard.rs`, `crates/quilltap-host/tests/host_help_docs_boot.rs`, any other the workspace gate reddens); `harness/oracle/{cases,fixtures}/settings-routes*`, `chat-settings-tier2*`, `build-settings-fixture.ts`, `build-chat-settings-fixture.ts`, `provision/**`; NEW `harness/oracle/cases/task-type-log-mapping.ts` + corpus + NEW `crates/quilltap-harness/tests/task_type_log_mapping_equivalence.rs`; `crates/quilltap-harness/tests/{settings_routes,chat_settings_tier2,provisioning,almanack_tier2,almanack_render,help_tree,memories_routes}_equivalence.rs` (the settings/help families' new rows + regen; `memories_routes` regen-only); `crates/quilltap-web/tests/chat_settings_composer_web_routes.rs` (a fourth wire arm, if landed there rather than in a new file); this order's header | `api/types.rs`; `api/engine.rs`; `api/chat_post_office.rs`; `services/announcer/**`; `system_prompt_compiler.rs`; `spine.rs`; `apps/web/**`; `docs/v4/**`; the committed `post-office-*` / `in-scene-voiced-*` DB pairs |
| **P4.D180** (the rehearsal service + verb + host wire, from `main`) | `crates/quilltap-core/src/services/announcer/{mod,character_voiced}.rs` + NEW `voice_rewrite_core.rs` + NEW `in_scene_voiced.rs`; `api/chat_post_office.rs` (the new handler + driver seam beside the announcement-preview ones); `api/types.rs` + `api/engine.rs` (the ONE new `Request`/`CoreResponse` pair, the seam field, the arm); `services/system_prompt_compiler.rs` (ONLY `read_current_stacks` visibility + a `get_compiled_identity_stack(chat, participant_id)` twin); `crates/quilltap-host/src/spine.rs` (the driver construction + assembly); NEW `crates/quilltap-web/tests/fixtures/in-scene-voiced-{main,mount}.db` (+ `.meta.json` sidecars); NEW `harness/oracle/cases/in-scene-voiced-tier3.test.ts` + NEW `harness/oracle/fixtures/in-scene-voiced-tier3.json`; NEW `crates/quilltap-harness/tests/in_scene_voiced_tier3_equivalence.rs`; `crates/quilltap-harness/tests/announcer_tier3_equivalence.rs` (regen + the both-pins neutrality assertion only); NEW web-venue wire test `crates/quilltap-web/tests/impersonation_voice_preview_wire.rs`; `crates/quilltap-web/tests/dispatch_wrong_type_census.rs` (the moved constant + the new verb's rows); this order's header | `db/chat_settings.rs`; `api/settings.rs`; `services/llm_logging.rs` (READ the map; never edit); `almanack/**`; `provisioning/**`; `host.rs`; `help/**`; `apps/web/**`; `docs/v4/**`; `message_attribution.rs` / `message_formatter.rs` / `dangerous_content/**` / `standing_instructions.rs` / `participant_resolver.rs` / `user_identity_resolver.rs` / `subprompts/**` / `cheap_llm.rs` (READ — no edit expected; a needed edit is a STOP-and-flag); the committed `post-office-*` pair (READ-ONLY) |
| **P4.D181** (the SPA half + bug 134, from `main`) | `apps/web/**` — the Salon (`screens/salon/salon-conversation.ts`), the composer (`chat/chat-composer.ts`), `chat/speaking-as-avatar.ts`, NEW `chat/impersonation-voice/**` (the gate, the state service, the dialog, the review panel), `chat/post-office/insert-announcement-dialog.ts` (the panel extraction ONLY), `chat/llm-inspector-{entry,panel}.ts`, `screens/settings/system/llm-logs-card.ts`, `screens/settings/chat/{chat-tab.ts, NEW impersonation-voice-settings.ts}`, `core/core-contract.ts` (the §B DTOs + the settings key), NEW `chat/carina-parser.ts` client twin + `apps/web/oracle/**` recorder + `src/testing/fixtures/*.ndjson`, `chat/memory-cascade-dialog.ts` (ONLY if the bug-134 measurement finds the remember arm owed), the e2e specs it names; this order's header | every `crates/**` file; `help/**`; `docs/v4/**`; the committed DB pairs; `e2e/support/**` fixture SEEDS (read; a needed seed change is a STOP-and-flag) |

Everyone: `docs/CHANGELOG.md` + `status-log.md` append-only; the drift
ledger frozen; `docs/v4/**` the unifier's. `Cargo.toml` / `Cargo.lock`:
version lines only. The `docs/v4/` mirror refresh (the feature doc at
`docs/developer/features/complete/impersonation-voice-rewrite.md`, the
bug-134 file at its `bugs/fixed/` path, `bugs.md`, `API.md`, `DDL.md`,
`CHANGELOG.md`), the baseline move to `f4ad2c8d1`, the ledger's two §3
rows → ABSORBED, the two gated beats' flips, the
`EXPECTED_REHEARSAL_LOG_TYPE` flip, and the §B name-for-name diff are
UNIFIER wires.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
   -D warnings` in BOTH feature sets (default and `--features
   quilltap-core/native-transport`); `cargo build --workspace`; the release
   build. (P4.D181: `npm run lint` incl. the `check-qt-classes` guard,
   `npm test`, `npm run build` — never a bare `ng`.)
2. `cargo test --workspace` (background, full log + sentinel — NEVER
   `| tail`, never a pgrep loop — `background-gate-latency-fix`,
   `pgrep-until-loops-match-themselves`, `a-gate-piped-through-tail-
   undercounts-binaries`) with this lane's env block (every family it moves,
   by its recipe var — `a-family-env-var-is-not-its-regen-var`) AND
   `QT_V4_ROOT` set per §R.3; grep the log for `SKIP:` — every family this
   lane moves must RUN (confirm by per-binary duration or `-- --nocapture`);
   the exit code read directly, never after a pipe
   (`gate-exit-code-after-a-pipe`).
3. Every family this lane moves regenerated FRESH from the pin §R.3 names
   through `harness/tools/recipe_sweep.py --v4 <pin> --run <family>` (never
   two sweeps concurrently), re-run by name with zero SKIP, the changed
   bytes grepped in the NDJSON (§R.3's markers), and the NDJSON non-empty.
   The RED-FIRST items shown red against the pre-edit tree in the lane
   record (run them before the first source commit — a `git stash`-free
   proof).
4. The mutation proofs named in this order, run and restored by string
   edit (`mutation-proof-revert-by-file-backup`; `cargo-fmt-silently-voids-
   a-string-edit-mutation` — assert the replacement count), each reddening
   exactly the named test.
5. The Ownership table's "must not touch" column honoured (`git diff main
   --stat` read against it); the server lanes' `git diff main -- apps/web/`
   EMPTY; P4.D180's and P4.D181's `git diff main -- help/` EMPTY.
6. P4.D181 only: the full Playwright suite on its branch (port 4319 is
   repo-wide — one run at a time; `cargo build --release` first because
   `webBinary()` prefers `target/release`), the two new beats PARKED by
   name, every existing beat green (the documented P4.D161 pause-toast
   intermittent excepted, re-proven green by spec file in isolation).
7. Commits per `.claude/commands/commit.md` (the crate bumps §R.7 names, on
   every commit that moves the crate; CHANGELOG every commit); the lane
   record appended to `status-log.md` with the red-first evidence, the
   per-family regen lines, the mutation table, every measured deviation
   from this order (`work-order-facts-need-the-same-verification-as-commit-
   prose`), and the "spotted, not mine" list for the unifier.

