# Work order P4.D97 — thinking-turn prefill scoping + the DeepSeek strip (v4 bugs 85+86, server half) — v4 `12fe3e6f`

**Status:** OPEN

## Preamble

- **v4 baseline for this round: `12fe3e6f`** (v4 main, HEAD at planning
  2026-08-21 evening, tree CLEAN and the checkout on `main`). This lane
  ports the SERVER half of v4 **`97d2fcb5`** ("fix(llm): scope
  prefill-hostility to thinking models, not providers (bug 85)") and the
  whole of **`12fe3e6f`** ("fix(deepseek): decide thinking from the
  model, not the request body (bug 86)"). The two docs-only filings
  (`e04405a5`, `c0984bdf`) are NO-PORT beyond the `docs/v4` mirror.
  `bugfix` measured by `diff` at planning: HEAD still `3a76b17d` (4.8.4
  branch-start); nothing unabsorbed beyond the test-only `009c49b2`.
- **Drift-check at lane start.** `git log 12fe3e6f..main --oneline` AND
  `git diff main bugfix -- lib/ app/ packages/` in the v4 checkout. If
  v4 moved, **STOP** and report.
- **Verify the v4 checkout's branch before ANY oracle regen** (it has
  sat on `bugfix` and `release` before). Pin a detached worktree at
  `12fe3e6f` for every regen (`recipe_sweep.py --v4 <pin-path>`; ALL
  THREE symlink classes: root `node_modules`,
  `packages/quilltap/node_modules`, `plugins/dist/*/node_modules`). At
  unification the oracle baseline MOVES to `12fe3e6f`.
- **Sibling lanes (disjoint ownership):** P4.D98 (bug 85 SPA client —
  the profile editor, `core-contract.ts`), P4.D99 (bug 84 client —
  salon/reducer), P4.54 (recipe run-lines on OTHER families). See
  Ownership.
- **Worktree:** per the `carryout` skill; edit inside the worktree, not
  main.

## The drift, classified (surveyed 2026-08-21, fresh at `12fe3e6f`)

**Bug 85 (`97d2fcb5`), server surfaces:**

1. **NEW `lib/llm/thinking-turn.ts`** — the one pure evaluator:
   `evaluateThinkingTurn({rule, parameters, model})`. `isUnset` =
   absent / null / `''` (the empty string is how options-schema enums
   spell "(model default)"). Rule-keyed explicit choice wins —
   `disabledValues` checked BEFORE `enabledValues`; else
   `model?.thinksByDefault === true`; else `false`.
2. **Plugin types** (`packages/plugin-types` 2.5.8): `ModelInfo` gains
   `supportsThinking?` / `thinksByDefault?`; `TextProviderPlugin` gains
   `thinkingTurnRule?: ThinkingTurnRule`
   (`{optionKey, enabledValues?, disabledValues?}`). Declarative on
   purpose — it serialises through `/api/v1/providers`.
3. **Declaring plugins (only two):** DeepSeek 1.0.20
   (`optionKey:'thinking'`, enabled `['enabled']`, disabled
   `['disabled']`; STATIC_MODELS both gain
   `supportsThinking:true, thinksByDefault:true` — flash observed,
   pro assumed, "the safe direction to be wrong in") and Ollama 1.0.45
   (`optionKey:'enable_thinking'`, enabled `[true]`, disabled
   `[false]`; Ollama contributes NO thinksByDefault models — its
   models are whatever the user pulled).
4. **`providerRegistry.profileRunsThinkingTurn(provider, modelName,
   parameters)`** — joins the plugin's declared rule + its model
   table's `thinksByDefault` to the shared evaluator. "Never re-derive
   the answer at a call site."
5. **`defaultMultiCharacterPrefill(provider, runsThinkingTurn = false)`**
   — returns `false` first when `runsThinkingTurn`; then the Anthropic
   provider rule; else `true`. `profileUsesNamePrefill(profile,
   runsThinkingTurn = false)` threads it; a stored boolean still
   outranks every default (the tri-state is intact — the editor warns,
   never vetoes). The `PREFILL_HOSTILE_PROVIDERS` doc gains the
   "Resist adding a provider here…" warning.
6. **Call sites:** `context-builder.service.ts:909-921` — the anchor
   call now passes `profileRunsThinkingTurn(provider, modelName,
   parameters)`; `connection-profiles/route.ts` create — the
   omitted-field default resolves with the join.
7. **Routes:** `/api/v1/providers` serves
   `thinkingTurnRule: plugin.thinkingTurnRule ?? null` (positioned
   AFTER `optionsSchema`); `/api/v1/models` (the fetch POST) spreads
   `supportsThinking: staticInfo?.supportsThinking,
   thinksByDefault: staticInfo?.thinksByDefault` onto each
   modelsWithInfo row (undefined drops the key). **Measure v4's GET
   leg too** before assuming it's POST-only.
8. **Migration `retire-prefill-on-thinking-profiles-v1`**
   (`migrations/scripts/retire-prefill-on-thinking-profiles.ts`):
   clears stored `multiCharacterPrefill=1` on DeepSeek/Ollama rows
   that run a thinking turn. Carries DELIBERATE FROZEN copies of the
   two rules + `FROZEN_THINKS_BY_DEFAULT = {DEEPSEEK:
   {deepseek-v4-flash, deepseek-v4-pro}}` ("a migration describes the
   world it ran in — do not 'fix' it to follow the plugins later").
   Selection: `WHERE multiCharacterPrefill = 1 AND upper(provider) IN
   ('DEEPSEEK','OLLAMA')`, then per-row `evaluateThinkingTurn` with
   parsed parameters (unparseable → `{}`). `shouldRun` = SQLite ∧
   table exists ∧ column exists; once-only-ness comes from v4's
   migration LEDGER (`migrations_state` table: `id` PK,
   `completedAt`, `quilltapVersion`, `itemsAffected`, `message`;
   checked via `isMigrationCompleted`, `migrations/index.ts:126`;
   file fallback `data/settings/migrations.json` on non-SQLite
   backends only). v4's own coverage:
   `__tests__/integration/retire-prefill-on-thinking-profiles.
   integration.test.ts` (263 lines, added in `12fe3e6f`) — clears
   thinking-active DeepSeek/Ollama rows; leaves alone: thinking-off
   profiles, uncatalogued models, other providers, rows at 0, stored
   nulls, unparseable parameters.

**Bug 86 (`12fe3e6f`), the DeepSeek plugin:**

9. `isThinkingEnabled(body)` → **`willRunThinkingTurn(body)`**:
   explicit `thinking.type === 'enabled'` → true, `'disabled'` →
   false; else exact-id lookup of `body.model` in STATIC_MODELS →
   `thinksByDefault === true`; no model / uncatalogued → false. The
   strip (`temperature, top_p, frequency_penalty, presence_penalty`)
   now fires on a default-state profile on a V4 model. The DeepSeek
   options-schema helpText is REWRITTEN ("The V4 models reason before
   answering by default, so "(model default)" means thinking is ON —
   …" — the full sentence in the v4 snapshot delta; transcribe, never
   retype). v4's coverage:
   `__tests__/unit/plugins/deepseek-thinking-params.test.ts` (130
   lines) through sendMessage.

**Also in the commits, dispositions owed:** `lib/startup/prettify.ts`
(+1 migration pretty-label — rides the heal's message strings if v5
surfaces one, else NO-PORT with evidence), README/API.md/help docs →
mirror + the `p4.9i2` bank, package/lock churn → NO-PORT.

## Mandate

Land the server half whole: the evaluator, the manifest substrate
growth (model facts + the rule), the registry join, the prefill
second-arg with both consumers, the create-route default, the two wire
serializations, the bug-86 strip predicate, and the once-only heal —
every unit with its differential at the `12fe3e6f` pin. Do NOT touch
`apps/web/**` (P4.D98/P4.D99 own the SPA this round) and do not edit
the recipe headers of families P4.54 owns (its order lists them; the
four request/response envelope families are THIS lane's).

## Survey-verified starting points (2026-08-21, fresh)

**The prefill twin (one Rust def, one default call site):**

- `crates/quilltap-core/src/services/multi_character_prefill.rs` (114
  lines; registered `services/mod.rs:124`):
  `default_multi_character_prefill(provider: Option<&str>) -> bool`
  `:50-56`; `profile_uses_name_prefill` `:64-69`;
  `profile_uses_name_prefill_value(profile: &Value) -> bool` `:74-82`.
  All three gain the `runs_thinking_turn: bool` threading (mirror v4's
  default-false semantics; Rust has no default args — pick the
  spelling that keeps every call site explicit).
- Default call site: `crates/quilltap-core/src/api/settings.rs:
  1250-1260` in `connection_profile_create` (`:1239`) — the `None` arm.
  `provider` is read at `:1248`; the raw `bag` (with `modelName`,
  `parameters`) is in scope — v4 passes
  `profileRunsThinkingTurn(provider, modelName, parameters)` here.
  Present-but-not-boolean (incl. explicit `null`) stays a 400; absent
  is resolved AND STORED (create never writes tri-state NULL).
  The PUT arm (`:1731-1741`) is explicit-only — v4 did not change it;
  verify and leave alone.
- `profile_uses_name_prefill_value` consumers:
  `crates/quilltap-core/src/services/orchestrator.rs:2288` and
  `crates/quilltap-core/src/services/regenerate_swipe.rs:344` (both
  feed `MessageContextParams::use_prefill`,
  `message_context.rs:516-521`). Both now need the registry join over
  the profile row's provider/modelName/parameters — mirroring v4's
  context-builder edit. `apply_multi_character_turn_anchor` itself is
  UNTOUCHED by this drift.

**The evaluator + join (net-new):**

- New module suggested: `crates/quilltap-core/src/services/
  thinking_turn.rs` — the pure evaluator (transcribe v4's
  `lib/llm/thinking-turn.ts` semantics incl. the `isUnset` trio and
  disabled-before-enabled order; carry the why-comments). The join
  (v5's `profile_runs_thinking_turn`) reads the manifest registry —
  v5's registry equivalent is `crates/quilltap-core/src/
  provider_manifest/mod.rs` `Registry` (`:416-420`, accessors
  `:422-509`); add an accessor beside them.
- **Verified absent:** no `supportsThinking` / `thinksByDefault` /
  `thinkingTurnRule` / `evaluateThinkingTurn` symbol exists anywhere
  in v5 today.

**The manifest substrate (the lane's design center #1):**

- `Manifest` struct `provider_manifest/mod.rs:257-320`;
  `options_schema` (opaque `Value` carry) `:292-295`;
  `fallback_models: Vec<String>` `:308-310` — **v5 has NO
  ModelInfo-equivalent**: the generator collapses
  `plugin.getModelInfo()` to `models.map(m => m.id)`
  (`gen-provider-manifests.mjs:403`). The facts need a home: add an
  OPTIONAL manifest field (e.g. `models: [{id, supportsThinking?,
  thinksByDefault?}]` — carry only what v4's wire uses, serde-default
  so all nine committed manifests stay parseable) plus
  `thinkingTurnRule` as an optional top-level field beside
  `options_schema` (opaque or typed — typed is better here since the
  evaluator reads it). `SUPPORTED_SCHEMA_VERSION` stays 1 (additive
  optional fields; record the reasoning).
- **THE GENERATOR:**
  `harness/oracle/providers/gen-provider-manifests.mjs` (419 lines;
  deepseek aug `:124-133`, ollama `:154-163`; `buildManifest`
  `:328-405`; `optionsSchema` at `:365`; `fallbackModels` at `:403`).
  Teach IT the new fields (transcribed from `plugin.getModelInfo()`
  and `plugin.thinkingTurnRule`) and REGENERATE all nine manifests —
  never hand-edit a committed manifest (the augmentation-rot memory;
  P4.D78's generator-reverts-the-hand-fix incident). The DeepSeek
  helpText rewrite arrives via this same regen (v4's
  `getProviderOptionsSchema` changed).
- Committed manifests: `crates/quilltap-core/src/provider_manifest/
  manifests/*.json` (nine); `deepseek.json:31+` optionsSchema
  (`thinking` key `:38`), `ollama.json:31+` (`enable_thinking` +
  `thinking_effort`).

**The wire serializations:**

- `provider_list` — `crates/quilltap-core/src/api/settings.rs:
  2161-2211`; the hand-built `json!` row `:2190-2208` with
  `"optionsSchema"` at `:2206`. **Key insertion order is
  wire-visible** (`preserve_order`) and
  `providers_listing_equivalence` compares `optionsSchema` as
  re-serialized strings (`providers_listing_equivalence.rs:91-99`) —
  position `thinkingTurnRule` exactly where v4's route emits it
  (after `optionsSchema`) and move manifest + generator + oracle
  together, red-first.
- `model_list` `settings.rs:2220-2237` (serves `provider_models`
  cache rows verbatim); `model_fetch` `settings.rs:2258-2337` (cache
  rows built `:2304-2326`, echo body `:2333-2337`); the live fetcher
  emits BARE IDS today
  (`crates/quilltap-core/src/api/provider_actions.rs:356`). v4's POST
  merge adds the two facts from staticInfo per exact id — **measure
  v4's models route at the pin (BOTH legs, GET and POST)** and mirror
  where v4 actually emits them; the static facts come from the new
  manifest field.

**Bug 86 (design center #2):**

- `crates/quilltap-core/src/model/request_builder/
  chat_completions.rs`: DeepSeek section `:576+`;
  `DEEPSEEK_PROFILE_ALLOWLIST` `:580-587`; the converged applier
  `apply_profile_params_with` `:609-634` (DO NOT fork it — Z.AI/OAC/
  Ollama share it); `deepseek_normalize_profile_param` `:641-648`;
  **`strip_thinking_incompatible` `:650-665`** — currently body-only,
  called from `build_deepseek_body` at `:690`. v4's replacement reads
  `body.thinking` (explicit enabled/disabled) then `body.model`
  against the static catalogue. `RequestInput`
  (`request_builder/mod.rs:91-130`) carries `model: String` — the
  body already carries `model` too, so the predicate can stay
  body-shaped exactly like v4's. The thinksByDefault facts inside a
  pure builder: v4's plugin reads its own STATIC_MODELS; the faithful
  v5 shape is a module-local const in the DeepSeek section carrying
  the two ids' facts, WITH a unit test asserting the const agrees
  with the committed `deepseek.json` manifest's models field (the
  consistency pin that keeps the two homes from rotting apart —
  v4 has the same duplication plugin-side).

**The heal (design center #3 — the once-only mechanism needs a
decision):**

- Precedent: `crates/quilltap-core/src/db/
  connection_profiles_prefill_repair.rs` (P4.D79; boot-wired at
  `crates/quilltap-host/src/host.rs:893-905` inside
  `seed_built_ins`, on the writer thread). Its once-only guard is
  COLUMN ABSENCE — **which does not transfer**: this pass is
  data-only, no schema delta to key off. Re-running it would clobber
  a user's re-ticked `true` on a thinking profile — NOT idempotent.
- **Ordered shape (verify by measurement before building; STOP and
  escalate if the measurement contradicts it):** interoperate with
  v4's OWN ledger. v4 tracks completion in the `migrations_state`
  table (created lazily via `CREATE TABLE IF NOT EXISTS` by
  `migrations/state.ts`; row = `id` PK, `completedAt`,
  `quilltapVersion`, `itemsAffected`, `message`) and consults it via
  `isMigrationCompleted` (`migrations/index.ts:126`). v5's heal
  should: (a) skip when the `migrations_state` row
  `retire-prefill-on-thinking-profiles-v1` exists; (b) after
  healing, write that row with the same DDL and row shape v4 writes
  (measure `recordCompletedMigration` for the exact column values —
  timestamps ISO, `quilltapVersion` = measure what v4 writes and
  decide what v5 stamps, recorded either way). This is
  D23-compatible: the table and row are v4's own shapes, created
  exactly as v4 creates them — and it is the ONLY shape that
  prevents the cross-app double-run in both directions (v5 heals →
  v4 skips; v4 migrated → v5 skips). An
  `instance_settings`-marker alternative
  (`crates/quilltap-core/src/db/instance_settings.rs`
  `read_setting:59`/`write_setting:69`, both private today;
  `NON_PORTABLE_INSTANCE_SETTING_KEYS:689-695`) does NOT stop v4
  from re-running the migration later and clobbering a re-ticked
  `true` — name this hazard in the lane record if the ledger shape
  proves infeasible.
- The heal itself transcribes v4's FROZEN tables + selection +
  per-row evaluation verbatim (frozen copies stay frozen — carry
  v4's "describes the world it ran in" comment), runs AFTER
  `ensure_connection_profiles_prefill_column` in `seed_built_ins`
  (on a pre-4.9 instance the column doesn't exist until that call),
  and slots at `host.rs:905` (`// === end P4.D79 ===`).
- `sqlite-initial-schema` / fresh instances: v4's fresh path never
  needs the heal (create resolves and stores); v5's
  `fresh_schema.json` has no `migrations_state` table and the D23
  rule forbids inventing one AT PROVISIONING — the ledger row/table
  appears only on the heal's write path against an existing
  instance, exactly as v4's lazy CREATE does. A fresh v5 instance
  that never had thinking rows at 1 has nothing to heal; decide (and
  record) whether the heal still stamps the ledger on first boot the
  way v4's runner stamps every migration — **measure what v4 does on
  a fresh instance** (its runner runs and records all migrations) and
  match the observable outcome.

## Tiered deliverables

**Tier 1 (must land):**

1. The evaluator module + the registry join (+ manifest accessor).
2. The manifest substrate: the two new optional fields, the generator
   taught, ALL NINE manifests regenerated at the pin (byte-diff review
   — expect deepseek/ollama gaining the rule + models facts and
   deepseek's helpText rewrite; the other seven should move only if
   v4 moved them — investigate any surprise).
3. The prefill second-arg threading: the three functions, the create
   default, both `use_prefill` producers.
4. The two wire serializations (`thinkingTurnRule` in provider_list;
   the model facts where v4 emits them).
5. Bug 86: the strip predicate + the manifest-consistency pin.
6. The heal + its ledger interop, mutation-proven (a second run must
   be a no-op; a re-ticked `true` must survive both a second v5 boot
   AND — by the ledger row — a hypothetical v4 run).
7. Differentials (below), each red-first where the surface exists.

**Tier 2 (should land):**

8. `docs/v4/` mirror refresh (bugs.md, the two filings, API.md,
   PROVIDER_PLUGIN_DEVELOPMENT.md, README model table).
9. The `prettify.ts` label disposition (NO-PORT with evidence
   expected — v5 has no migration-runner UI; record it).

**Tier 3 (explicit loud deferrals):**

10. `help/connection-profiles.md` + `help/chat-multi-character.md`
    deltas → the `p4.9i2` bank (P4.D98 banks them too — the bank
    list is append-only; coordinate at unification, duplicate
    entries are harmless).
11. 💸 live proofs join the dogfood queue: a real DeepSeek
    thinking-turn completing multi-character turns (the bug-85 repro
    chat shape), and the heal firing once on a Friday-vintage copy
    with a stored-1 thinking row.

## The differential requirement, restated

Every unit ships with an equivalence test driving v4's REAL code at
the `12fe3e6f` pin, through the sweep driver (never two sweeps
concurrently):

- **NEW evaluator family** (tier 1): oracle case importing v4's real
  `evaluateThinkingTurn` (tsx; follow
  `harness/oracle/cases/multi-character-prefill.ts`'s shape — that
  family is this lane's closest recipe). Corpus: rule
  present/absent × parameters {absent, null, `''`, enabled-value,
  disabled-value, unrelated key, non-string-coercion shapes
  (`true`/`false` for Ollama, numbers)} × model
  {absent, null, thinksByDefault true/false, supportsThinking-only}.
  Pin the disabled-before-enabled order and the `''`-is-unset arm
  with mutation proofs.
- **`multi_character_prefill_equivalence`** (exists;
  `QT_ORACLE_MULTI_CHAR_PREFILL`): the second arg changes the corpus
  SHAPE — extend the oracle case + corpus with `runsThinkingTurn`
  legs (v4's `multi-character-prefill.test.ts` +40 delta is the
  case model); red-first.
- **`settings_routes_equivalence`** (`connection_profiles` family;
  jest, `QT_ORACLE_SETTINGS_ROUTES` + `QT_FIXTURE_SETTINGS`): new
  arms — create with the field ABSENT on a DeepSeek
  thinksByDefault-model profile (default now false), same with
  explicit `thinking:'disabled'` parameters (default stays true),
  an Ollama `enable_thinking:true` profile, and a stored-boolean
  override surviving. Existing prefill arms (`settings-routes.
  test.ts:905-974`) stay green. The fixture may need a DeepSeek
  profile row — the fixture is THIS family's own (check the recipe
  header before widening; the committed-fixture rules apply).
- **`connection_profiles_tier2_equivalence`**: the `:295`
  key-ABSENT measurement op — verify whether its provider makes the
  new default observable; add a DeepSeek-model op if not; red-first.
- **`providers_listing_equivalence`**
  (`QT_ORACLE_PROVIDERS_LISTING`): goes red at the v4 pin BEFORE the
  v5 manifest/serializer move (v4 now emits `thinkingTurnRule`) —
  record the red, land, green. The 8-of-9-schemas shape assertion
  and the string-order comparison both bind.
- **`request_builder_equivalence`** (committed corpus
  `harness/oracle/fixtures/request-envelopes/
  request-envelopes.recorded.ndjson`; recorder
  `record-request-envelopes.mjs`, deepseek corpus `:278-298`;
  regen via `regenerate-request-envelopes.sh` — the driver never
  runs recording stages, run the recorder deliberately at the pin):
  bug 86 CHANGES recorded bytes (the strip now fires for
  default-state V4-model rows — `profile-params` `:284` and
  `profile-params-skips` `:290` should drop the four params;
  `thinking-strip` `:283` unchanged). Add rows: explicit
  `thinking:'disabled'` keeps the params; an uncatalogued model id
  keeps them. All non-deepseek rows must stay byte-identical —
  assert that in the lane record.
- **The heal family** (NEW, tier 2): drive v4's REAL migration
  (`retire-prefill-on-thinking-profiles.ts` `run()`) via the
  jest-real-DB oracle recipe (the memory note; v4's own
  `retire-prefill-on-thinking-profiles.integration.test.ts` shows
  the harnessing) over a fixture with the full leave-alone matrix
  (thinking-active DeepSeek + Ollama rows, thinking-off, uncatalogued
  model, other provider, already-0, stored NULL, unparseable
  parameters); diff the resulting `connection_profiles` rows AND the
  `migrations_state` row against v5's heal on a copy of the same
  fixture. Mutation-prove the second-run no-op.
- **Tier-3 spine regen:** regenerate the round's affected families
  through the driver at the pin and let reds name themselves
  (candidates: `orchestrator_tier3` — its profiles carry stored
  booleans so expect NO movement; a red here means a fixture profile
  was relying on the old default — investigate, don't paper).
  Zero SKIP; grep every regenerated NDJSON for a fresh marker.

## Fixtures

Consumes the settings/connection-profiles fixtures (widening rules
per their recipe headers — this lane owns regenerating dependent
oracles if it widens one). Delivers: the heal family's fixture DB
(committed, test-pepper-keyed per the sanitizer conventions if built
from real data — prefer a /tmp-built synthetic per the
settings-fixture precedents), and the regenerated
request-envelopes corpus + nine manifests.

## Shared contract (BINDING — identical text in P4.D97 and P4.D98)

The wire shapes, exactly as v4 serves them at `12fe3e6f`:

- `GET /api/v1/providers` → each provider entry gains
  `"thinkingTurnRule": { "optionKey": string, "enabledValues"?:
  (string|number|boolean)[], "disabledValues"?:
  (string|number|boolean)[] } | null` — `null` when the plugin declares
  none; the key is ALWAYS present in v4's listing (`?? null`).
- The models listing (`modelsWithInfo` entries) gains
  `"supportsThinking"?: boolean` and `"thinksByDefault"?: boolean` —
  omitted (not null) when the static catalogue has no entry, exactly
  as v4's spread of `staticInfo?.…` serializes (undefined drops the
  key).
- The evaluator semantics both sides run, verbatim from v4
  `lib/llm/thinking-turn.ts`: explicit rule-keyed choice wins
  (unset = absent/null/`''`; `disabledValues` checked BEFORE
  `enabledValues`); else `model?.thinksByDefault === true`; else
  `false`.
- Only DeepSeek (`optionKey: 'thinking'`, enabled `['enabled']`,
  disabled `['disabled']`) and Ollama (`optionKey: 'enable_thinking'`,
  enabled `[true]`, disabled `[false]`) declare rules at this pin.
- The e2e gate constant `P4D97_THINKING_WIRE_LANDED` (in the e2e spec,
  owned by P4.D98) flips true at unification.

## Ownership

- **This lane owns:** `crates/quilltap-core/src/services/
  multi_character_prefill.rs`, the new `thinking_turn.rs`,
  `crates/quilltap-core/src/api/settings.rs`,
  `crates/quilltap-core/src/api/provider_actions.rs`,
  `crates/quilltap-core/src/services/orchestrator.rs`,
  `regenerate_swipe.rs`, `crates/quilltap-core/src/provider_manifest/**`
  (struct + all nine manifests), `crates/quilltap-core/src/model/
  request_builder/chat_completions.rs`, the new heal module under
  `crates/quilltap-core/src/db/` + its `host.rs` wiring,
  `harness/oracle/providers/gen-provider-manifests.mjs`,
  `record-request-envelopes.mjs`, `regenerate-request-envelopes.sh`,
  the request-envelopes committed corpus, the settings-routes /
  connection-profiles / providers-listing / prefill oracle cases +
  harness tests, and the two NEW families (evaluator, heal).
- **Must NOT touch:** anything under `apps/web/**` (P4.D98, P4.D99);
  the recipe headers of P4.54's families (its order lists them —
  P4.54 in turn excludes this lane's four envelope families and any
  file named here).
- **Version bumps:** this lane bumps `quilltap-core`,
  `quilltap-harness`, `quilltap-host`, and `quilltap-web` if a web
  test moves. The unifier ACCUMULATES (identical-bump trap known; do
  not pre-compensate).

## Verification gate (before declaring done)

- `cargo fmt --all --check`; `cargo clippy --workspace --all-targets`
  plain AND with `--features quilltap-core/native-transport`.
- `cargo test --workspace` full (background + sentinel per the
  background-gate memory; NEVER a pgrep poll loop; never `| tail`).
- The lane's families by name through the sweep driver against the
  `12fe3e6f` pin, zero SKIP, fresh markers verified, and the
  changed-bytes grep (the green-regen-is-not-coverage memory): the
  regenerated NDJSONs must positively contain `thinkingTurnRule` /
  the stripped-params shape where the change lands.
- No SPA edits ⇒ no ng gate; if one becomes unavoidable, STOP —
  renegotiate with the sibling orders first.
- Per-commit rule: `.claude/commands/commit.md`.
