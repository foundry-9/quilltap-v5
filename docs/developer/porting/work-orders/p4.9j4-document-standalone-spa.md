# Work order P4.9J4 — the standalone-document lane: the chat-less Document Mode surface over the existing standalone verbs

> **STATUS: CLOSED — unified onto main 2026-07-20** (the workspace-tabs
> remainder round). Every AT-UNIFY row below was performed at unification:
> the `document-standalone` registry swap to `StandaloneDocumentView` (the
> NotWiredPane scaffold retired entirely with the round), the
> `<qt-documents-rail-entry />` shell mount (after Wardrobe, v4 order), and
> the §W.3 fold of the seven `document*` request types + `StandaloneScope`
> into `core-contract.ts` (name-for-name wire diff green; both casts
> dropped). The guarded beats ran live for the first time on the unifier's
> Playwright run — **P4.9J2's tier-2 item 7 and the `doc_focus`-beats fold
> CLOSE with this lane.** Tier-3 deferrals stand as recorded: `doc_focus`
> stays chat-scoped (v4-faithful); the FilePreviewText/rich-stack preview
> deferrals are untouched. SPA bumps 0.5.222–0.5.225 (accumulated at
> unification).
>
> The original lane-progress header follows for history.
>
> **Lane progress (branch `claude/document-standalone-spa-69384f`).** All four
> tiered deliverables LANDED on the branch (tier 1 items 1–4 + tier 2 items 5–6
> both fall out of the units below; tier 3 deferrals recorded). SPA-only —
> `git diff main -- crates/ Cargo.toml Cargo.lock` empty; drift-check at lane
> start clean (v4 HEAD == `7e6d13e5`). Gate: `ng test` 193 files / 2374 green,
> `ng build` clean. Commits: unit 1 (wire), unit 2 (screen), unit 3 (picker
> variant + rail entry), unit 4 (e2e beats + this table).
>
> **AT-UNIFY table (the unifier performs these; §W.2/§W.3):**
> 1. **`workspace/chrome/tab-registry.ts`** — replace
>    `'document-standalone': refusal('document-standalone')` with
>    `{ component: StandaloneDocumentView, inputs: (tab) => ({ payload: tab.payload as DocumentStandaloneTabPayload }) }`
>    (import `StandaloneDocumentView` from `../../documents/standalone-document-view`;
>    `DocumentStandaloneTabPayload` is already a workspace-contract type).
> 2. **`workspace/chrome/not-wired-pane.ts`** — drop the `document-standalone`
>    `headline`/`detail` arms (only `wardrobe` + `brahma` remain, per their lanes).
> 3. **`shell/shell.ts`** — import `DocumentsRailEntry` from
>    `../documents/documents-rail-entry`, add it to `imports`, and mount
>    `<qt-documents-rail-entry />` in the footer actions right AFTER the Wardrobe
>    button (v4 `sidebar-footer.tsx` order: Wardrobe → Document Mode → Settings).
> 4. **`core/core-contract.ts` (§W.3)** — fold the seven `document*` request
>    interfaces from `documents/standalone-wire.ts` into the `CoreRequest` union,
>    run the name-for-name wire diff vs `api/types.rs:1565-1595`, then retire the
>    `as unknown as CoreRequest` casts in `standalone-wire.ts` (2 sites) and
>    re-export the interfaces from `core-contract.ts` (the P4.9a2/P4.9f1
>    fold precedent).
> 5. **e2e:** no shared `global-setup`/`workspace-flow.spec.ts` change. The
>    `workspace-document-standalone-flow.spec.ts` beats self-activate once the
>    rail entry mounts (their rail-button probe flips) and the registry swap
>    renders the live editor instead of the not-wired pane.

> **Preamble.** v4 reference baseline: **`7e6d13e5`** (4.8.0-dev.92). At lane
> start run `git log 7e6d13e5..HEAD --oneline` in `~/source/quilltap-server`.
> ⚠ v4 is **mid-release-checklist** — a 4.8.0 tag/version commit may land any
> day. If HEAD moved, **STOP and report** — do not absorb surprise drift
> mid-lane.
>
> **Sibling lanes:** `p4.9i1a-brahma-server.md` (the round's only Rust lane),
> `p4.9i1b-brahma-spa.md`, `p4.9j3-wardrobe-tab-riders.md`. This lane touches
> ONLY `apps/web/` (plus the append-only docs) — **the server surface it
> consumes ALREADY EXISTS** (the P4.6w standalone verbs; survey below). Run
> in your OWN worktree on your own branch; Playwright runs ALONE on a
> dedicated port with THIS worktree's own Rust binaries built once (the
> `p4.6af-files-family-spa` memory).

## Mandate

Port v4's standalone (chat-less) Document Mode surface: the
`document-standalone` workspace tab — v4's `StandaloneDocumentView` with
its inline per-document mechanics (autosave debounce, flush-on-blur,
absorb-first-serialization baseline, 409 conflict reload) — over the SEVEN
standalone document verbs the Rust engine already serves, plus the
document-picker opener (the rail Documents entry). This closes lane
P4.9J2's tier-2 item 7 and the `doc_focus`-beats deferral that folded into
it. The tab kind swaps from its not-wired pane AT UNIFY (§W.2).

Must NOT touch: any Rust source (**no server verbs are missing** — if you
believe one is, STOP and flag; do not add one), `core-contract.ts` (§W.3 —
your wire types go in the lane-local `documents/standalone-wire.ts`), the
§W frozen files, the chat-scoped document machinery's behavior
(`documents/document-mode.ts` stays chat-bound — the standalone surface is
a SIBLING, not a refactor of it), `brahma/**` (lane P4.9I1B's), or the
characters/wizard files (lane P4.9J3's).

## Survey-verified starting points (2026-07-20 @ v4 `7e6d13e5` — do not re-derive)

### The v4 surface

- **Tab kind + payload:** `lib/workspace/types.ts:38` (kind), `:66-86`
  (payload: `docKey`, `scope: 'document_store' | 'general'`, `mountPoint?`,
  `filePath?` — unset while a blank is being created, `targetFolder?`,
  `displayTitle?`), `:93-103` (`standaloneDocKey`: existing files key by
  `${scope}:${mountPoint}:${filePath}`; a blank mints a fresh uuid; stable
  across renames).
- **The screen:** `components/workspace/TabView.tsx:109-110` →
  `components/workspace/StandaloneDocumentView.tsx`. It does NOT use
  `useDocumentMode`; it re-implements per-document mechanics inline:
  **30 s autosave debounce, flush-on-blur, absorb-first-serialization
  baseline, 409 → reload**. It renders ONE `DocumentPane` (import line
  20, from the salon components).
- **Its I/O:** `postDocumentsAction(action, body)` →
  `fetch('/api/v1/documents?action=…')` (lines 31-37). Actions used:
  `open-document` :136, `write-document` :180, `read-document` :195 (the
  409 reload), `rename-document` :281, `delete-document` :321. The
  surface is **file-scoped** (`filePath`/`scope`/`mountPoint`), never
  chatId-scoped; the server records opens under `STANDALONE_CHAT_ID`
  (`'00000000-0000-0000-0000-000000000000'`,
  `lib/chat-documents/constants.ts:23`) purely for cross-chat recents.
- **The v4 route** (`app/api/v1/documents/route.ts`): handlers
  `handleAccessibleStores:98`, `handleRecentDocuments:118`,
  `handleOpenDocument:166`, `handleReadDocument:228`,
  `handleWriteDocument:275`, `handleRenameDocument:315`,
  `handleDeleteDocument:395`; all standalone-context, no Librarian
  announcements, no chat_documents rows beyond the recents sentinel.
- **The opener:** `components/layout/left-sidebar/sidebar-footer.tsx` —
  the `DocumentPickerModal` (dyn import :31-32; trigger button :257;
  mount + `onSelectDocument` :313-318). `handleSelectDocument` (:163-197)
  builds the payload with `standaloneDocKey(...)` (:178) and calls
  `workspace!.openTab('document-standalone', payload, { title })` (:186);
  the legacy (non-workspace) shell falls back to a
  `?open=document-standalone&scope=…` URL intent (:192-196). The SAME
  picker modal is used chat-side (`SalonView.tsx:351,1546`) —
  Scriptorium/files rows are NOT openers in v4.
- **`doc_focus` is NOT part of this surface** — it is an LLM UI tool
  (`lib/tools/doc-focus-tool.ts`) consumed by the CHAT-scoped panes; the
  standalone view does not consume it. v4-faithful = leave it chat-scoped.

### The v5 side — what EXISTS (P4.6w) vs what's missing

**The server surface is COMPLETE.** All seven standalone verbs exist and
are engine-wired (`crates/quilltap-core/src/api/types.rs:1565-1595`;
`engine.rs:2364-2391`; handlers `api/documents.rs:1309-1721`, all
`DocumentAccessContext::standalone()`, no Librarian):
`DocumentStores` (v4 `accessible-stores` — returns
`{ stores, projectLibrary: null }`), `DocumentsRecent`, `DocumentOpen`
(records under `STANDALONE_CHAT_ID` best-effort), `DocumentRead`,
`DocumentWrite` (mtime-checked — the 409 arm), `DocumentRename` (syncs
recents via `chat_documents.rs:524` `rename_file_path_in_store`),
`DocumentDelete`. There is intentionally NO standalone
`DocumentClose`/`DocumentResolve` (v4 has none either — the tab is its
own open record). The qtap-target byte route also exists
(`quilltap-web/src/qtap_target_route.rs:50`, `lib.rs:169-172`).

**What's missing is entirely SPA-side:**

1. `core-contract.ts` has ONLY the `chatDocument*` types (union
   :3682-3688) — no `document*` request interfaces; the SPA cannot
   dispatch the standalone verbs yet. (Your wire module fixes this via
   the §W.3 fold.)
2. `documents/document-api.ts` is entirely chat-scoped (:85-220);
   `documents/document-mode.ts` is hard-bound to `chatId` (:133,
   :176-177, early-returns :286,401,437,456,480,503). No file-scoped
   client exists.
3. `documents/document-pane.ts` is reusable but its sole consumer is the
   chat-scoped `salon-conversation.ts:491,500`.
4. No opener: `document-standalone` appears only under `workspace/`
   (contract :45,:73-93,:172-180; intent arm `workspace-intent.ts:32,
   77-96` — the only payload constructor today; `tab-meta.ts:48`;
   `persistence.ts:64,216`); the registry renders the not-wired pane
   (`tab-registry.ts:146`, `not-wired-pane.ts:48-49,61-62` — both
   unifier-only, §W.2).
5. No e2e `doc_focus`/standalone beats exist (grep-verified clean).

Locate the existing v5 document PICKER component (the P4.6x deliverable —
under `apps/web/src/app/documents/`) and reuse it with a standalone
variant, exactly as v4 reuses one modal for both entries.

## Tiered deliverables

### Tier 1 — must land

1. **The wire module** — `apps/web/src/app/documents/standalone-wire.ts`:
   the seven `document*` request types, **name-for-name against
   `api/types.rs:1565-1595`** (the unifier folds them into
   `core-contract.ts` and runs the wire diff, §W.3), + a
   `StandaloneDocumentApi` dispatch client (one method per verb; response
   DTOs mirror the Rust handlers' JSON name-for-name).
2. **The screen** — `qt-standalone-document-view` (`StandaloneDocumentView`)
   hosting the existing `document-pane`: port v4's inline mechanics
   faithfully — the 30 s autosave debounce, flush-on-blur,
   absorb-first-serialization baseline (the first serialization after
   load is absorbed as the baseline, not treated as an edit — the
   `markdown-field-load-must-not-emit` memory is the same trap), the
   409 → `read-document` reload, rename (updates the payload's
   `filePath`, the `docKey` does NOT change), and delete (closes the
   tab via the self-close seam). Payload-refresh once the server names a
   new blank document (contract :84-88 semantics).
3. **The opener** — a self-contained rail Documents entry component
   (v4 `sidebar-footer.tsx:257` semantics; mounted in `shell.ts` by the
   UNIFIER, §W.2) driving the picker's standalone variant; selection
   builds the payload with the contract's `standaloneDocKey` (:172-180)
   and calls `openTab('document-standalone', payload, { title })`, with
   the `?open=` URL fallback in routed mode (v4 :192-196 semantics; the
   v5 intent arm `workspace-intent.ts:77-96` already parses it).
   Reopening the same file must FOCUS the existing tab (docKey identity —
   the reducer already dedupes; spec it).
4. **Specs + beats** — component specs for the mechanics (debounce timing
   under fake clocks, absorb-baseline, 409 reload, rename keeps docKey);
   Playwright beats as guarded ACTIVATE-AT-UNIFY probes (the registry
   swap happens at unification): picker → open → edit → autosaved write
   round-trip → rename → reopen-focuses-same-tab → delete-closes-tab.

### Tier 2 — should land

5. **Blank-document creation** (`targetFolder`, the unset-`filePath`
   payload refresh once the server names it) end-to-end.
6. **The picker's recents view** over `DocumentsRecent` (v4's
   `recent-documents` — cross-chat, standalone-sentinel-aware).

### Tier 3 — explicit refusal-arm deferrals

- `doc_focus` stays chat-scoped (v4-faithful — the standalone surface
  does not consume it; record this so it isn't re-reported as a gap).
- FilePreviewText math + the rich-stack preview deferrals (P4.6af-era)
  are untouched by this lane.

## The differential requirement, restated

The server behavior is already differential-backed (P4.6w's
document-mode differentials over the standalone verbs) — this lane adds
NO Rust and NO new oracle. Your fidelity obligations: (a) the wire module
is name-for-name against `api/types.rs:1565-1595` (the unifier's wire
diff is the check); (b) the screen's mechanics are ported against the
pre-surveyed v4 `StandaloneDocumentView.tsx` anchors with specs pinning
each (debounce interval, absorb baseline, 409 reload, docKey stability
across rename); (c) the editor content path reuses the ported
`qt-rich-editor` dialect bridge untouched — byte fidelity there is
already gated by the 28-entry round-trip corpus.

## Fixtures

- Consumes the committed e2e instance's document stores/mounts (the
  P4.6s-era seeded mounts and the document beats' fixtures). If the
  standalone beats need a dedicated file seeded, add it in this lane's
  OWN spec setup (not the shared e2e global setup); if a shared-fixture
  change is genuinely unavoidable, note it in the AT-UNIFY table for the
  unifier instead of editing shared setup (§W.5).

## §W — the round-wide workspace meeting points (BINDING — identical in all four orders)

1. **`apps/web/src/app/workspace/workspace-contract.ts` is FROZEN.** All
   three tab payloads are already declared (`WardrobeTabPayload` :98-100,
   `DocumentStandaloneTabPayload` :73-93, payload-less `brahma` :40). No
   lane edits it; a believed defect in it is a STOP-and-flag.
2. **`workspace/chrome/tab-registry.ts`, `workspace/chrome/not-wired-pane.ts`,
   and `shell/shell.ts` are UNIFIER-ONLY this round.** Each lane ships
   self-contained components and a named AT-UNIFY table in its order; the
   unifier performs the registry row swaps and the shell mount insertions.
   AT-UNIFY rows: `wardrobe` → lane P4.9J3's tab component;
   `document-standalone` → lane P4.9J4's `StandaloneDocumentView`;
   `brahma` → lane P4.9I1B's `BrahmaConsoleView`. Shell mounts: lane
   P4.9J4's documents-picker entry; lane P4.9I1B's brahma entry.
3. **`core-contract.ts` folds at unify.** Lanes adding wire types
   (P4.9I1B: the §B brahma DTOs in `brahma/brahma-wire.ts`; P4.9J4: the
   seven `document*` request types in `documents/standalone-wire.ts`)
   declare them lane-locally; the UNIFIER folds them into
   `core-contract.ts` and runs the name-for-name wire diff against
   `api/types.rs`. No lane edits `core-contract.ts`.
4. **`app.routes.ts` and `workspace/workspace-redirect.guard.ts`: lane
   P4.9J3 single-owner.**
5. **e2e:** each lane adds NEW spec files only; `e2e/workspace-flow.spec.ts`
   is lane P4.9J3's single-owner file. Beats that depend on another lane's
   landing (or on the registry swap) ship as guarded ACTIVATE-AT-UNIFY
   probes, self-activating when the wire lands.
6. **Version bumps:** lane P4.9I1A owns the `quilltap-core` /
   `quilltap-web` / `quilltap-harness` bumps (`quilltap-host` only if a
   genuinely new seam lands — flag first); the three SPA lanes each bump
   `apps/web/package.json` per commit; the unifier accumulates (recount —
   identical bumps merge silently, the `p4.6aw-ax-8-round-plan` memory).
7. **Append-only for everyone:** `docs/CHANGELOG.md`,
   `docs/developer/porting/status-log.md` (they union-merge at
   unification).

## Ownership

This lane's files: `apps/web/src/app/documents/standalone-wire.ts` (new),
`documents/standalone-document-api.ts` (new), the
`qt-standalone-document-view` component (+ spec), the rail Documents entry
component (+ spec), the picker's standalone variant (in the existing
picker file under `documents/` — this lane owns that file this round),
its new e2e spec files, and this order's status header.
`apps/web/package.json` bumps per §W.6. Nothing under `crates/**` or the
sibling lanes' files.

## Verification gate (before declaring done)

- `ng test` fully green (the existing chat-scoped document specs green
  UNCHANGED); `ng build` clean.
- Full Playwright ALONE on a dedicated port with this worktree's own
  binaries; the entire pre-existing suite green; this lane's beats
  visibly report their guarded ACTIVATE-AT-UNIFY state.
- `git diff main -- crates/ Cargo.toml Cargo.lock` empty.
- Per-commit rule: follow `.claude/commands/commit.md`.
