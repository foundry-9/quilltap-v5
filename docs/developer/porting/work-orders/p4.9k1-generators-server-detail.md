# Work order P4.9K1 — the character-generators SERVER lane, per-character trio: the Character Optimizer ("Refine from Memories", bug 119's post-fix shape), the external-prompt generator, the bulk Rename/Replace service, and `refresh-archive` — verbs, host driver, REST/SSE edges, differentials

**Status: OPEN — PARTIAL (tier-1 item 1 of 8 landed).** Ordered 2026-09-07, the
`p4.9k` character-generators round. **STACKED on P4.9K0's branch tip** (§S.1),
which reads LANE COMPLETE.

- **Branch:** `claude/p4-9k1-generators-detail-a1` (from K0's tip `f1031c0f`).
- **LANDED:** tier-1 item 1 — the optimizer's PURE arms (types, constants,
  `coerce_suggestion_array` in bug 119's post-fix shape, all seven prompt
  builders) with the new `character_optimizer_prompts_equivalence` family and
  five mutation proofs. Gate green at the tip.
- **OPEN:** tier-1 items 2–8, all of tier 2, both tier-3 recordings. §S.9 is
  discharged for the pure half only; the RUNNER's `runSubStep`/`runSubStepCore`
  containment and the two bug-119 log lines are still owed.
- **Read the lane record first** — `status-log.md` → "P4.9K1 … (2026-09-07) —
  PARTIAL, unit 1 only". It carries the completed survey for item 2 (both
  P4.D82/D83 riders CONFIRMED at the pin; tier-2 item 10 ANSWERED — v4 DOES log
  an `EXTERNAL_PROMPT` row), the three measured JS-fidelity findings for item 3
  and the ruling they produce, and a structural note that every remaining unit
  is runner-sized because v4 exports no helpers.

The unifier updates this header at unification.

## Preamble

- **v4 oracle baseline: `f699da6f6`** (drift-ledger §1). v4 `main` HEAD is
  the baseline; `bugfix` carries no unabsorbed content. The ledger's one §3
  row, **`15573c3a1` (bug 119, "a non-array sub-step answer no longer kills
  the run") is `ORDERED(P4.9K1)`** — this lane ports the POST-fix optimizer
  (§S.9); the pre-fix inline `.filter` never existed in v5 and must never
  be transcribed. Read v4's write-up first:
  `docs/developer/bugs/fixed/bug-119-optimizer-substep-non-array.md` (186
  lines) — its "v5 status: Applies" row states the design lesson.
- **Freshness probe at lane start** (ledger §2): checkout on **`bugfix`**
  (do NOT switch it), tree clean, both `log` ranges empty. **Any mismatch:
  STOP and report.**
- **Regen rule: PIN REQUIRED at `f699da6f6`** (the checkout is on
  `bugfix`). Pin path **`/tmp/qt-v4-pin-p49k1-f699da6f6`**, every regen and
  every jest run through `recipe_sweep.py --v4 <pin>`; the oracle
  `node_modules` are LIVE (`zod` 4.5.4 — `optimizeStreamSchema`'s
  `.max(500)` is a code-point rule: `jsstr::zod_len_*`).
- **Sibling lanes:** P4.9K2 (wizard + AI import — the OTHER server lane,
  stacked beside you on K0; you share `api/types.rs`/`engine.rs`/`host.rs`/
  `spine.rs`/`web/lib.rs` by fence and `characters_routes.rs` by FUNCTION —
  yours is `characters_action_post`); P4.9K4 (the SPA half of YOUR three
  dialogs — bound to you by §B.2; it mocks until unify and gates its beats
  on `P49K1_SERVER_LANDED`); P4.9K3, P4.80, P4.81 independent.
- **Worktree note:** `git worktree add <path> -b claude/p4-9k1-generators-
  detail-<hash> <K0 branch tip>`; edit inside it; `CARGO_INCREMENTAL=0`.

## Mandate

Port the three LLM/DB services v4 mounts on `POST /api/v1/characters/[id]?
action=` and v5 has never had — `lib/services/character-optimizer.service.ts`
(1,399 lines, POST-bug-119), `lib/services/external-prompt-generator.service.ts`
(304 lines, carrying the P4.D82/P4.D83 riders), `lib/services/
character-rename.service.ts` (445 lines) — plus the small `refresh-archive`
arm (`post.ts:335-…`), as `Request` verbs per §B.2 with a host-composed
driver for the two model-calling services (the `help_chat_send` /
`brahma_console_send` precedent: `engine.rs:295-301`, `spine.rs:1652-1700`,
`spine.rs:2205-2220`), the REST edge arms on `characters_action_post`
(JSON for rename / external-prompt / refresh-archive; SSE via K0's
`generator_sse::stream_generator` for `optimize-stream`), and a differential
per unit driving v4's REAL service functions. Streaming rides §B.1's
`GeneratorProgress` Event; no per-route SSE in core. Must NOT touch:
`generators/{wizard,ai_import}.rs`, `characters_collection_post`,
`system_data_routes.rs`, `apps/web/**`, the committed
`characters-{main,mount}.db` (read-only; copy per case).

## Survey-verified starting points (2026-09-07 @ v4 `f699da6f6` — do not re-derive)

### The route arms (`app/api/v1/characters/[id]/handlers/post.ts`)

- `CHARACTER_POST_ACTIONS` (`:89`) = `favorite, avatar, add-tag, remove-tag,
  toggle-controlled-by, toggle-carina, set-default-partner, optimize-stream,
  generate-external-prompt, refresh-archive, rename, archive, rehydrate`. v5
  serves `archive`/`rehydrate` on the REST edge (`characters_routes.rs:316-
  317`) and refuses the rest with a loud pointer to `/api/dispatch`
  (`:321`). **Measure the guard order** before the action map runs: the
  character-existence check and the archived-refusal that
  `characters_action_post` already applies (P4.D63's `archived=` chokepoint
  — the archive lanes recorded which actions v4 refuses on an archived
  character; the optimizer and rename are `isArchived ? undefined :` gated
  CLIENT-side at `CharacterDetailView.tsx:415`, so measure the server's
  answer for each of your four actions on the fixture's archived shape and
  pin it).
- `optimize-stream` (`:92-140`): Zod `optimizeStreamSchema.parse(body)`
  (`:60-68`) → `logger.info('[Characters v1] Character optimizer starting
  (streaming)', {userId, characterId, connectionProfileId, maxMemories,
  searchQuery: searchQuery || '(none)', useSemanticSearch, sinceDate,
  beforeDate, outputMode})` → the SSE stream over
  `runCharacterOptimizer(characterId, connectionProfileId, user.id, repos,
  enqueue, optimizerOptions)`. A Zod failure is the route's generic 400
  (measure the body — the `validationError` envelope P4.62 transcribed).
- `generate-external-prompt` (`:316-333`): `generateExternalPromptSchema`
  (`:70-75`) → `logger.info('[Characters v1] External prompt generation
  starting', {userId, characterId, connectionProfileId, maxTokens})` →
  `generateExternalPrompt(id, validated, user.id, repos)` → `!result.success`
  ⇒ `serverError(result.error || 'Generation failed')` else
  `{ prompt, tokensUsed }`.
- `rename` (`:425-444`): `renameSchema` (`:77-87`) → the neither-pair 400
  (`:429-431`, sentence verbatim) → `runCharacterRename(character, {…},
  user.id, repos)` (NOTE: takes the already-loaded `character`, so the
  existence 404 precedes the parse — measure) → the response body → catch
  `logger.error('[Characters v1] Error processing rename', …)` +
  `serverError('Failed to process rename request')`.
- `refresh-archive` (`:335-…`): `repos.chats.findByCharacterId(id)` → `0`
  chats ⇒ `{ queued: 0 }`; else per chat `enqueueConversationRender(…)`
  counting successes → `{ queued }` (read the whole arm incl. its catch and
  log lines; v5 twins: `db::chats_read::find_by_character_id` and
  `services::queue_service::enqueue_conversation_render` `:1232`).

### The optimizer, anatomically (`character-optimizer.service.ts`)

- Types `:41-135` (transcribe `OptimizerSuggestion` incl. `subId` doc,
  `wardrobeItem?: GeneratedWardrobeItem`, the event union, `OptimizerSubStep
  {kind,label,index,total}`, `OptimizerOptions`). Constants `:146` onward
  (`MAX_MEMORIES_FOR_ANALYSIS = 30`, `SYSTEM_MESSAGE`, `SUGGESTIONS_FOLDER`,
  temperatures/maxTokens per call — read them all; they are wire bytes).
- Pure prompt builders `:182-660` — `coerceSuggestionArray` (`:182`, the
  bug-119 table: array passthrough; non-object/nullish → `[]`; FIRST
  array-valued property among `['suggestions','items','results','data',
  'amendments']` in that order; a lone object with a string `field` → one-
  element array; else `[]`), `buildCharacterContext` (`:200`, wardrobe
  items optional), `buildMemoryContext` (`:284`), `getAnalysisPrompt`
  (`:300`), the six suggestion prompts (`:360-660`), and the one model call
  (`:618-651`: `provider.sendMessage({model, messages:[system SYSTEM_MESSAGE,
  user \`${characterContext}\n\n---\n\n${memoryContext}\n\n---\n\n${instruction}\`],
  maxTokens, temperature, cacheKey: buildCharacterCacheKey(characterId),
  profileParameters}, apiKey)` → `response.content.trim()`; empty content
  throws `No response from model`).
- `runCharacterOptimizer` (`:661-1160`): `start` → `step_start loading` →
  character (`Character not found` when missing or another user's) → the
  memory pipeline (`:697-770`: search → date filter → rank → reinforcement
  filter → limit; semantic search via `isEmbeddingAvailable` +
  `generateEmbeddingForUser(searchQuery, userId, undefined, {priority:
  'background'})` + `getCharacterVectorStore(characterId).search(embedding,
  500)` ∩ `repos.memories.findByCharacterAboutCharacter(characterId,
  characterId)`, with the text-search fallback and its warn line; the
  self-reference-only rule is a why-comment to carry) → the `step_complete
  loading` event carrying `memoryCount`/`filteredCount` (`:757-780`) →
  `step_start analyzing` → ONE model call → `parseLLMJson<OptimizerAnalysis>`
  (`:834`) → `step_complete analyzing` with `analysis` (`:872`) →
  `step_start generating` → the sub-step list (`:889-901`: general, one per
  existing scenario `Scenario: ${title}`, one per existing prompt `System
  prompt: ${name}`, physicalDescription, wardrobe, properties → label
  `Aliases`, newSystemPrompts) → `runSubStepCore` (`:907-1035`: `substep_start`
  with `{kind,label,index,total}`; the model call; `parseLLMJson<unknown>` +
  `coerceSuggestionArray` (`:944-945`) with the bug-119 warn `[CharacterOptimizer]
  Sub-step answered with a non-array; coerced` `{characterId, subStep,
  parsedType, recovered}`; the pre-existing `LLM call failed; continuing` and
  `unparseable JSON; skipping` arms each emitting `substep_complete` with
  `partialSuggestions: []` (`:938`); ids minted per suggestion — measure
  where) wrapped by `runSubStep` (`:1036-1050`: catch → `logger.error(
  '[CharacterOptimizer] Sub-step failed unexpectedly; continuing',
  {characterId, subStep: label}, err)` + `substep_complete` with `[]`) →
  the seven `await runSubStep(…)` calls in order (`:1052-1098`) →
  `step_complete generating` → `outputMode === 'suggestions-file'`
  (`:1110-1128`: requires `characterDocumentMountPointId` else throws
  `Suggestions-file mode requires the character to be linked to a
  document-store vault.`; `writeSuggestionsFileToVault` `:1173-1300` writes
  `${SUGGESTIONS_FOLDER}/refinement-${stamp}.md` — the stamp is
  `toISOString()` with `:` stripped, fraction dropped, `T`→`-`; the markdown
  body ends with the `_Generated by Quilltap's Character Optimizer in
  suggestions-file mode…_` line `:1292`) → `suggestions_file_written` →
  the completion `logger.info` → `done {analysis, suggestions,
  suggestionsFilePath}`; the outer catch → `logger.error('[CharacterOptimizer]
  Optimization failed', {characterId, userId, error})` + `error {error}`.
  **The v4 clock:** `new Date()` for the stamp and `Date.now()` for
  durations — both sides run under the harness's fixed clock.
- v4's unit tests (parity seeds for your pure arms):
  `__tests__/unit/lib/services/character-optimizer-helpers.test.ts` (the
  bug-119 six + `buildCharacterContext`/`buildMemoryContext` rows) and
  `character-optimizer-regression.test.ts` (undefined/null/empty
  `frequency`; talkativeness edges).

### The external-prompt generator (`external-prompt-generator.service.ts`)

- Types `:25-37`; `META_SYSTEM_PROMPT` `:43-58` (bytes); `generateExternalPrompt`
  `:63-…`: profile (`Connection profile not found`) → api key → character
  (`Character not found`) → system prompt by id (`System prompt not found`)
  → optional scenario → the assembled user message (read `:110-300` whole:
  which character fields, physical description, wardrobe, the scenario
  weave, the token budget sentence) → ONE model call → `{success, prompt,
  tokensUsed}`. Every refusal is a `{success:false, error}` RESULT, not a
  throw — the route turns it into the 500.
- **The two carried riders** (P4.D82's Tier-3 record, `status-log.md:73676`):
  `f933ba9c` (a bare `getModelContextLimit` where a profile is in hand →
  the profile-first `resolveContextWindow`) and `d89babc4` (the sampling
  read via `resolveSamplingParams`/`profileParams`). Both are IN the file
  at the pin — port the file as it is; cite both shas in the module doc so
  the drift ledger's rider closes.

### The rename service (`character-rename.service.ts`)

- Types `:35-73`; `escapeRegex` `:75`; `rewriteField` and the case-flag
  regex (read `:76-156` — a JS `RegExp` with `g` + optional `i`; the
  `js-regex-to-rust-regex-fidelity` axes apply: case folding is the one
  that bites, pin an astral/Turkish-i row).
- `runCharacterRename` (`:157-445`): ordered replacements (primary first);
  1. eight scalar fields (`name, title, identity, description, manifesto,
  personality, firstMessage, exampleDialogues`) with `location = \`Character:
  ${character.name}\``; 2. aliases; 3. scenarios (`title/content/description`,
  location `Scenario: ${title}`); 4. system prompts; 5. physical
  descriptions (summary counter `physicalDescriptions`); 6. memories
  (`repos.memories.findByCharacterId` `:316`); 7. chats
  (`repos.chats.findByCharacterId` `:356` → titles; `getMessages` `:365` →
  messages, **Staff messages skipped** — measure the predicate); then, when
  `!dryRun`: `repos.characters.update(characterId, characterUpdates)`
  (`:400`), `repos.memories.update` per memory (`:404`), `repos.chats.update
  (chatId, {title})` (`:409`), `repos.chats.updateMessage(chatId, messageId,
  {content})` (`:412`), `enqueueConversationRender(userId, {chatId,
  fullReembed: true})` per touched chat (`:420`); the response with `summary.
  total`. v4's suite `__tests__/unit/lib/services/character-rename.service.
  test.ts` (dry-run counts / Staff skip / execute writes + re-render /
  no-match writes nothing / case sensitivity) is the corpus seed — transcribe
  its five scenarios into your fixture-backed cases.

### The v5 side today

- Nothing under `generators/` beyond K0's substrate. `api/characters.rs`
  (3,019 lines) hosts every other character verb — do NOT add to it; §B.2's
  four verbs live in the NEW `api/generators_detail.rs` (K0 registered the
  `generators` module; you register `api::generators_detail` in your
  `api/mod.rs` fence).
- Seams you compose the driver from (all exist): `model::completion::
  CompletionProvider::send_message` (`completion.rs:206`, canned by
  `CannedKeyMessage` `:292` — `provider|model|temperature|messages`),
  `model::embedding::EmbeddingProvider` + the character vector store
  (`memory` family), `services::queue_service::enqueue_conversation_render`
  (`:1232`), the character read/update repos (`db/characters*.rs`, the
  vault writers for the suggestions file — the document-store overlay
  writers the wardrobe/instructions lanes used), `db::memories` (update),
  `db::chats` (`update`, `update_message`), `chats_read::find_by_character_id`.
  The api-key + profile resolution: `resolve_connection_profile_api_key`
  (P4.D93) — the optimizer/external-prompt read `apiKeyId` → `key_value`
  exactly as Brahma does.
- The host composes the driver exactly as `HelpChatSendDriver` is composed
  (`spine.rs:2205-2220` impl; `EngineAssembly.help_chat_send: Option<Arc<dyn
  …>>` `engine.rs:301`; `None` → the verb's NAMED refusal). Your driver
  trait lives in `api/generators_detail.rs`; its impl in the new
  `quilltap-host/src/generators_detail_driver.rs`; the field appended in
  `EngineAssembly` inside your fence; the construction in `spine.rs` inside
  your fence at the END of the file's impl blocks.

## Tiered deliverables

### Tier 1 — must land

1. **`generators::optimizer`** — the whole service POST-fix (types, prompt
   builders, `coerce_suggestion_array`, `run_character_optimizer` with the
   `run_sub_step`/`run_sub_step_core` containment, both bug-119 log lines
   capture-pinned, the suggestions-file writer). Model calls through
   `CompletionProvider`; embeddings through the embedding seam; the
   progress callback = K0's `GeneratorProgressEmitter`.
2. **`generators::external_prompt`** — types, `META_SYSTEM_PROMPT`, the
   assembly, the one call, the result shape; the two riders cited.
3. **`generators::rename`** — the regex rewrite + `run_character_rename`
   over v5's repos, dry-run proven write-free by table dump.
4. **`generators::refresh_archive`** — the arm.
5. **The four verbs** (§B.2) in `api/types.rs` (your fence), handlers in
   `api/generators_detail.rs` with v4's parse-then-work guard order as
   MEASURED (item 0 of your lane record: the archived/404/400 ladder per
   action), engine arms in your `engine.rs` fence, the driver trait +
   `EngineAssembly` field (`None` → `ErrorKind::Unavailable` named refusal
   for the two model-calling verbs; rename + refresh-archive need no
   driver — they are DB-only and dispatch in the engine).
6. **The host driver** (`generators_detail_driver.rs` + the `spine.rs`
   fence): composes the completion/embedding/repo bundle and runs the
   optimizer + external prompt; wired LIVE (⚠ real model spend on a real
   click — say so in the CHANGELOG entry).
7. **The REST arms** on `characters_action_post`: `rename`,
   `generate-external-prompt`, `refresh-archive` as JSON edges (v4's
   status + body per arm); `optimize-stream` through
   `generator_sse::stream_generator` (Tier 1 for the ARM, Tier 2 for the
   byte-exact SSE proof below). The dispatcher's refusal sentence updates
   to name the served set (measure what
   `query_param_semantics_equivalence` records for this edge; you do NOT
   edit that family — record the row change for the unifier, §S.2).
8. **Three differentials** (below), each red-first where a v5 defect is
   predicted, each with mutation proofs named in the lane record.

### Tier 2 — should land

9. The SSE edge proven byte-exact: a web wire test driving `POST
   /api/v1/characters/{id}?action=optimize-stream` on the fixture with the
   canned driver, asserting the frame bytes equal the oracle NDJSON's
   recorded event sequence re-framed as `data: …\n\n` (the oracle records
   the `onProgress` events in order — the same rows the dispatch family
   compares).
10. `llm_logs` rows: measure whether v4's plugin `sendMessage` logs a
    `CHARACTER_OPTIMIZER` / `EXTERNAL_PROMPT` row per call (the SPA's
    `llm-logs.api.ts:42` lists `CHARACTER_OPTIMIZER` as a purpose — find
    v4's `logLLMCall` site for it) and, if so, pin the row count per case
    (P4.79's shape).

### Tier 3 — explicit refusal-arm deferrals (loud, typed, never silent)

- The `pendingExternalPrompt` courier carry (`services/courier_transport.
  rs:241-242`) is already ported and untouched here; the `generate-external-
  prompt` → courier hand-off (if v4 has one — measure) is recorded, not
  built.
- Any `characterDocumentMountPointId`-less character in `suggestions-file`
  mode answers v4's thrown sentence as the `error` event (that IS the port),
  not a v5 refusal.

## The differential requirement (restated)

- **`character_optimizer_tier3_equivalence`** — a jest oracle case
  (`harness/oracle/cases/character-optimizer-tier3.test.ts`) driving v4's
  REAL `runCharacterOptimizer` over fresh copies of the committed
  `characters-{main,mount}.db` (Aria: 2 scenarios + 2 prompts + 2 wardrobe
  items + 2 memories → 9 sub-steps; Echo: the broken vault; Dax/Bram: no
  memories), mocking ONLY the model boundaries the Rust port injects as
  seams — `createLLMProvider` → a canned `sendMessage` keyed
  `provider|model|temperature|messages` that RECORDS the key (the
  `help-chat-orchestrator-tier3.test.ts` header is the mechanism, cited by
  line in your case header; `chat-dialogs-llm-choose-tier3` is the
  non-streaming precedent), `isEmbeddingAvailable`/`generateEmbeddingForUser`
  canned per case (`carina-query-tier3` is the semantic-search precedent),
  the fixed clock. Corpus (≥ 12 cases): apply mode happy path; a
  `searchQuery` with semantic on / off / semantic-throws-fallback; date
  window; `maxMemories` cap; **the bug-119 arms** — a sub-step answering
  `{suggestions:[…]}`, `{items:[…]}` (order proof: both keys present,
  first wins), a lone `{field:…}` object, a number, a string, prose (the
  `unparseable JSON; skipping` arm), a sub-step whose call THROWS (the
  wrapper's containment: later sub-steps still run, `partialSuggestions:
  []` emitted); `suggestions-file` mode (diff the written vault file
  bytes through the store reader — fixed stamp); the vault-less
  suggestions-file error; missing character; another user's character.
  Comparands: the recorded canned keys (the prompts are the port), the
  event sequence verbatim (minted suggestion ids remapped by position — the
  `<minted-N>` normalizer), the vault write, the log lines (capture layer,
  thread-scoped: `a-process-global-test-seam-must-be-thread-scoped`).
- **`external_prompt_tier3_equivalence`** — same mechanism, ≥ 8 cases
  (happy; scenario present/absent; wardrobe/physical present/absent;
  missing profile / character / system prompt; empty model answer; a
  `maxTokens` at each bound) — comparands: the recorded key (the whole
  assembled user message is the unit), the result bag, the log lines.
- **`character_rename_equivalence`** — tier 2 over the same fixture pair,
  both sides on fresh copies: the response body + post-mutation dumps of
  the character's vault projection (through the store reader), `memories`,
  `chats`, `chat_messages`, `background_jobs`; ≥ 10 cases from v4's suite +
  yours (dry-run vs execute for the same pairs → the dry-run dump equals
  the pre-state; the primary-then-additional ORDER proof where an alias
  contains the name; case-sensitive vs insensitive; a Staff message
  untouched; the neither-pair 400; no-match → nothing written, `total: 0`;
  the astral-case row; `refresh-archive` on a character with 0 / N chats →
  `background_jobs` rows). Every writing case names its minted keys
  (`background_jobs.id`, `updatedAt`) in a `WRITING_CASES` set as
  `chat_scenario_routes_equivalence` does; the no-write cases keep them
  fully diffed.

Recipes follow `recipe-header-conventions` (indented, `--test`-scoped,
regen/run paths identical, no `/tmp` pin in a committed header; jest cases
mirrored to `/tmp/<family>/cases` because jest ignores `.claude/`).

## Fixtures

Consumes `crates/quilltap-web/tests/fixtures/characters-{main,mount}.db`
READ-ONLY (a fresh copy per case; never regenerated — if it cannot express
a case, the case goes on K2's NEW `character-generators-*.db` in the NEXT
round; record it, do not widen). Delivers the three oracle case files +
their corpus JSONs under `harness/oracle/fixtures/`.

## §B — the generators + chat-delete wire contract (BINDING — verbatim-identical in every order of this round)

Every name below is a **dispatch verb** (`Request` variant in
`crates/quilltap-core/src/api/types.rs`, `#[serde(tag = "type", rename_all =
"camelCase")]`) or an **Event** kind. Field names are the v4 JSON body keys
verbatim (camelCase on the wire; `snake_case` in Rust under
`#[serde(rename_all = "camelCase")]`). Response bodies are v4's route bodies
byte-for-byte unless a line says otherwise. The unifier diffs this block
name-for-name against `api/types.rs` and every SPA wire file at `/unify`.

### B.1 Streaming shape (the one mechanism for every generator)

v4 streams each generator as an HTTP SSE response of `data: <JSON>\n\n`
frames (`handleOptimizeStream`, `handleAiWizardStream`,
`handleAIImportStream` — all three are the same nine-line `ReadableStream`).
v5's boundary streams **only on the Event channel** (`api-boundary.md`), so:

- Each streaming verb carries a client-minted **`progressId: string`**
  (`crypto.randomUUID()` client-side; the `CreationProgressFrame` precedent
  at `core-contract.ts:4187-4220` / `services/creation_progress.rs`).
- Every v4 `onProgress(event)` becomes ONE Event:
  `{ "type": "generatorProgress", "progressId": <id>, "generator": <G>,
  "event": <the v4 progress event object, key order and bytes verbatim> }`
  where `<G>` ∈ `"optimizer" | "wizard" | "aiImport"`.
- The dispatch call resolves when the run ends server-side with
  `{ "terminal": <the LAST event v4 would emit, verbatim> }` — for the
  optimizer that is the `done` or `error` event; for the wizard `done`; for
  the AI import `done`. A refusal BEFORE the run starts (v4's Zod parse /
  route-level `badRequest`) is a dispatch `CoreError` (`badRequest`,
  message = v4's sentence, `details` = v4's Zod issues where v4 answers
  them) and emits NO frame.
- The REST SSE edges (Tier 2 in the server lanes) re-frame the same Events
  as `data: <JSON.stringify(event)>\n\n` with headers `Content-Type:
  text/event-stream`, `Cache-Control: no-cache`, `Connection: keep-alive`,
  and close after the terminal frame — byte-identical to v4's stream.

### B.2 Verbs (K1 — the per-character trio + refresh-archive)

- `characterOptimize { characterId, progressId, connectionProfileId,
  maxMemories?, searchQuery?, useSemanticSearch?, sinceDate?, beforeDate?,
  outputMode? }` — v4 `optimizeStreamSchema` (`app/api/v1/characters/[id]/
  handlers/post.ts:60-68`; defaults 30 / '' / true / null / null / 'apply',
  bounds `maxMemories` int 5..200, `searchQuery` max 500, `outputMode`
  enum). Terminal = v4's `done` (`{type, analysis, suggestions,
  suggestionsFilePath?}`) or `error` (`{type, error}`) event.
- `characterGenerateExternalPrompt { characterId, connectionProfileId,
  systemPromptId, scenarioId?, maxTokens }` — v4
  `generateExternalPromptSchema` (`post.ts:70-75`; uuids; `maxTokens` int
  1000..20000). Response `{ prompt, tokensUsed }`; a failed generation is a
  500 whose message is `result.error || 'Generation failed'` (v4
  `post.ts:328-330`).
- `characterRename { characterId, primaryRename?, additionalReplacements?,
  dryRun? }` — v4 `renameSchema` (`post.ts:77-87`; pairs `{oldValue,
  newValue, caseSensitive}` with the two `min(1)` sentences `Find value is
  required` / `Replace value is required`; `caseSensitive` default false;
  `additionalReplacements` default `[]`; `dryRun` default true; the
  route-level 400 when neither a primary nor an additional pair is present —
  `post.ts:429-431`, sentence verbatim). Response = v4's
  `RenamePreviewResponse` (`{characterId, characterName, dryRun,
  replacements[], summary{characterFields, physicalDescriptions, memories,
  chatTitles, chatMessages, total}}`); the catch arm is 500 `Failed to
  process rename request`.
- `characterRefreshArchive { characterId }` — v4 `post.ts:335-…`. Response
  `{ queued: <n> }`.

### B.3 Verbs (K2 — the wizard pair + the AI import)

- `characterWizard { …WizardRequest }` — the NON-streaming twin (v4
  `?action=ai-wizard` → `runCharacterWizard`). Body = v4 `wizardRequestSchema`
  (`app/api/v1/characters/handlers/post.ts:89-136`) verbatim. Response =
  v4's `WizardResult` `{ success, generated, errors? }`.
- `characterWizardStream { progressId, …WizardRequest }` — the streaming
  twin (`runCharacterWizardStreaming`). Terminal = v4's `done` event
  (`{type, fullContent, errors?}` — measure the throw arm's shape).
- `aiImportStream { progressId, profileId, sourceFileIds, sourceText,
  includeMemories, includeChats, existingResult?, regenerateSteps? }` — v4
  `handleAIImportStream`'s hand-rolled body read (`app/api/v1/system/tools/
  route.ts:1196-1212`: `sourceFileIds || []`, `sourceText || ''`,
  `includeMemories ?? true`, `includeChats ?? false`; the two 400 sentences
  `Missing required field: profileId` and `Must provide at least one source
  file or source text` BEFORE any frame). Terminal = v4's `done` event
  (`{type, result, stepResults, errors?}`); the route's catch arm is a 500
  whose message is the thrown error's (`route.ts:1254-1260`).

### B.4 Verb (P4.80 — chat delete)

- `chatDelete { chatId }` — v4 `handleDelete` with NO action
  (`app/api/v1/chats/[id]/handlers/delete.ts:48-63`): 404 `Chat not found`
  (v4 `notFound('Chat')`), else `repos.chats.delete(chatId)` →
  `{ "success": true }`; the catch arm is 500 `Failed to delete chat`.
  Engine: `services::conversation_summary_vault_bridge::
  delete_conversation_with_vault_sweep(db, chat_id, true)` — ALREADY ported,
  zero callers today.

### B.5 The Event kind (K0 owns the variant; K1/K2 emit; K3/K4 consume)

```
Event::GeneratorProgress { progress_id: String, generator: GeneratorKind, event: serde_json::Value }
  → wire: { "type": "generatorProgress", "progressId", "generator", "event" }
GeneratorKind = Optimizer | Wizard | AiImport  → "optimizer" | "wizard" | "aiImport"
```

The SPA subscribes to `CoreClient.events$`, filters on `type ===
'generatorProgress' && progressId === mine`, and folds `event` exactly as
v4's hook folds each parsed SSE line (`useCharacterOptimizer.ts`,
`useAIWizard.ts:308-…`, `useAIImport.ts`). No SPA lane reads the REST SSE
edges.

### B.6 What every order must NOT invent

No verb, field, Event kind, or response key beyond this block. A lane that
finds this block wrong (a v4 field the survey missed, a shape v4 measures
differently) STOPs on that item, records the measurement in its lane record,
and delivers everything else — the unifier reconciles contracts, lanes do
not.

## §S — the round-wide meeting points (BINDING — identical in every order of this round)

1. **Stacking.** P4.9K0 (the substrate) runs FIRST and alone. P4.9K1 and
   P4.9K2 branch their worktrees FROM P4.9K0's lane branch tip (the
   `P4.D119→P4.D120` stacked precedent), never from `main`; the unifier
   cherry-picks K0 first. P4.9K3, P4.9K4, P4.80 and P4.81 branch from `main`
   and are independent of K0.
2. **Fenced-append shared files.** `crates/quilltap-core/src/api/types.rs`,
   `crates/quilltap-core/src/api/engine.rs`, `crates/quilltap-core/src/lib.rs`
   (module lines), `crates/quilltap-host/src/host.rs`,
   `crates/quilltap-host/src/spine.rs`, `crates/quilltap-web/src/lib.rs`
   (module + route lines): each Rust lane appends ONLY inside its own fence
   (`// === P4.9K1 ===` … `// === end P4.9K1 ===`, opened once per file, new
   variants/arms/registrations at the END of the relevant block, never
   interleaved). P4.81 is the one lane that EDITS EXISTING lines in
   `spine.rs` — exactly the two sites its order names (`map_create_error`'s
   `details: None` and the `CreationProgressEmitter::from_id` call's
   position) and nothing else in that file.
   `crates/quilltap-web/src/characters_routes.rs` is fenced BY FUNCTION:
   `characters_action_post` (the per-id POST dispatcher) is P4.9K1's;
   `characters_collection_post` (the collection POST dispatcher) is
   P4.9K2's; neither touches the other's function or the file's header.
   `crates/quilltap-web/src/system_data_routes.rs` is P4.9K2's alone.
   `crates/quilltap-web/src/chats_routes.rs` is P4.80's alone.
   `Cargo.toml`/`Cargo.lock` at the workspace root: version lines only; a
   dependency add is a STOP-and-flag in the lane record.
3. **Frozen files.** `docs/developer/porting/drift-ledger.md` (lanes never
   write it); `apps/web/src/app/core/core-contract.ts` (folded at unify —
   every SPA lane declares its §B DTOs in its OWN wire file, verbatim from
   §B; identical duplicate `interface` declarations across lanes are
   expected and folded by the unifier, who runs the name-for-name diff);
   `apps/web/src/app/screens/characters/characters.api.ts` (each SPA lane
   adds its verbs in its own new `*.api.ts` beside its feature);
   `apps/web/src/app/shell/shell.ts`; `apps/web/src/app/workspace/
   workspace-contract.ts` (no new tab kinds this round — every surface is a
   modal or a tab INSIDE an existing screen, as in v4).
4. **Version bumps.** P4.9K0: `quilltap-core`, `quilltap-harness`,
   `quilltap-web`. P4.9K1 and P4.9K2: `quilltap-core`, `quilltap-harness`,
   `quilltap-web`, `quilltap-host`. P4.9K3 and P4.9K4: `apps/web/package.json`.
   P4.80: `quilltap-core`, `quilltap-harness`, `quilltap-web`,
   `apps/web/package.json`. P4.81: `quilltap-host`, `quilltap-core`,
   `quilltap-harness`, `apps/web/package.json`. Per commit. The unifier
   RECOUNTS as base + total bumps — identical bumps merge silently
   (`commit-checklist`; the playbook's silent-auto-merge trap).
5. **e2e.** Port 4319 is repo-wide — ONE Playwright at a time across all
   lanes. P4.9K3, P4.9K4 and P4.80 add NEW spec files only; beats that need
   a server verb another lane ships are guarded ACTIVATE-AT-UNIFY on a NAMED
   per-lane constant (`P49K1_SERVER_LANDED`, `P49K2_SERVER_LANDED`), never a
   capability probe (`round-plan-takeaways` §Planning). P4.81 may edit
   EXISTING specs (its item 7 restores what a spec changed). The Rust lanes
   run no Playwright: `git diff main -- apps/web/` empty for K0/K1/K2.
6. **Append-only for everyone:** `docs/CHANGELOG.md`,
   `docs/developer/porting/status-log.md`.
7. **The regen rule for every lane:** PIN REQUIRED at **`f699da6f6`** — a
   LANE-UNIQUE pin path (`/tmp/qt-v4-pin-<order>-f699da6f6`, all three
   symlink classes per ledger §5.1), every regen and every fixture build
   through `harness/tools/recipe_sweep.py --v4 <pin> --run <family>`; the
   v4 checkout stays on `bugfix`, untouched (ask the human, never switch
   it). Committed recipe headers say `cd ~/source/quilltap-server` and never
   name a `/tmp` pin (`recipe-header-conventions`). The oracle
   `node_modules` are the LIVE tree (`zod` 4.5.4 — the `zod_version_guard`
   fails the gate if it moves; every Zod-transcribing schema in §B uses the
   `jsstr::zod_len_*` helpers for string lengths).
8. **Fixture ownership.** `characters-{main,mount}.db` is READ by K1 and by
   K4's beats and REGENERATED BY NOBODY this round. K2 delivers the NEW
   committed `character-generators-{main,mount}.db` pair (its own builder);
   P4.80 delivers `chat-delete-{main,mount}.db` if the survey shows the
   committed pairs cannot express its cascade. K3/K4 seed through the
   running server's verbs (`seed-help-fixture.ts` precedent), never SQL.
9. **The bug-119 obligation** (drift-ledger §3, `15573c3a1`, `ORDERED(P4.9K1)`):
   K1 ports the POST-fix optimizer shape — `coerceSuggestionArray`'s ordered
   key list byte-for-byte, the `runSubStep`/`runSubStepCore` containment,
   the two log lines — and never transcribes the pre-fix inline `.filter`.

## Ownership (BINDING — identical table in every order of this round)

| Lane | Owns (edit freely) | Fenced-append (§S.2) | Must not touch |
| --- | --- | --- | --- |
| **P4.9K0** | NEW `crates/quilltap-core/src/generators/{mod,llm_json,field_semantics,generated_properties,sanitize_pronouns}.rs`; NEW `crates/quilltap-core/src/services/generator_progress.rs`; NEW `crates/quilltap-web/src/generator_sse.rs`; NEW harness family `crates/quilltap-harness/tests/generators_leaf_equivalence.rs` + `harness/oracle/cases/generators-leaf.ts` + its fixture JSON; this order's header | `api/types.rs` (the `Event::GeneratorProgress` variant + `GeneratorKind`), `core/src/lib.rs`, `web/src/lib.rs` (module line) | everything else |
| **P4.9K1** | NEW `crates/quilltap-core/src/generators/{optimizer,external_prompt,rename,refresh_archive}.rs`; NEW `crates/quilltap-core/src/api/generators_detail.rs`; `crates/quilltap-web/src/characters_routes.rs::characters_action_post` (that function only); NEW harness families `character_optimizer_tier3_equivalence`, `external_prompt_tier3_equivalence`, `character_rename_equivalence` (+ `refresh_archive` arms) with their oracle cases + fixtures JSON; a new host driver file `crates/quilltap-host/src/generators_detail_driver.rs`; this order's header | `api/types.rs`, `api/engine.rs`, `host/host.rs`, `host/spine.rs`, `web/src/lib.rs` | `generators/{wizard,ai_import}.rs`; `characters_collection_post`; `system_data_routes.rs`; `query_param_semantics_equivalence` (record, unifier adjusts); `character-generators-*.db`; `apps/web/**` |
| **P4.9K2** | NEW `crates/quilltap-core/src/generators/{wizard,ai_import}.rs`; NEW `crates/quilltap-core/src/api/generators_wizard.rs`; `crates/quilltap-web/src/characters_routes.rs::characters_collection_post` (that function only); `crates/quilltap-web/src/system_data_routes.rs`; `crates/quilltap-web/tests/query_param_semantics_equivalence.rs` + its oracle case (the `ai-import-stream` row); NEW committed `crates/quilltap-web/tests/fixtures/character-generators-{main,mount}.db` + `harness/oracle/fixtures/build-character-generators-fixture.ts`; NEW harness families `character_wizard_tier3_equivalence`, `ai_import_tier3_equivalence` + oracle cases + fixtures JSON; a new host driver file `crates/quilltap-host/src/generators_wizard_driver.rs`; this order's header | `api/types.rs`, `api/engine.rs`, `host/host.rs`, `host/spine.rs`, `web/src/lib.rs` | `generators/{optimizer,external_prompt,rename,refresh_archive}.rs`; `characters_action_post`; `characters-{main,mount}.db`; `apps/web/**` |
| **P4.9K3** | NEW `apps/web/src/app/screens/characters/generators/{wizard,rename,prompts-editor}/**` (+ its own `edit-generators.api.ts` + wire file); `apps/web/src/app/screens/characters/edit/**`; `apps/web/src/app/screens/characters/new/new-character.ts`; NEW e2e specs `character-wizard-flow.spec.ts`, `character-rename-flow.spec.ts`; `apps/web/package.json`; this order's header | — | `crates/**`; `screens/characters/view/**`; `screens/characters/list/**`; `chat/cast/**`; `core-contract.ts`; `characters.api.ts`; any existing spec |
| **P4.9K4** | NEW `apps/web/src/app/screens/characters/generators/{optimizer,external-prompt,ai-import}/**` (+ its own `detail-generators.api.ts` + wire file); `apps/web/src/app/screens/characters/view/character-detail.ts`, `view/character-header.ts`, `view/tabs/details-tab.ts` (the reverse-user entry only); `apps/web/src/app/screens/characters/list/characters-list.ts`; `apps/web/src/app/chat/cast/add-character-dialog.ts`; NEW e2e specs `character-optimizer-flow.spec.ts`, `character-external-prompt-flow.spec.ts`, `ai-import-flow.spec.ts`; `apps/web/package.json`; this order's header | — | `crates/**`; `screens/characters/edit/**`; `new/**`; `view/tabs/conversations-tab.ts`; `view/tabs/character-conversation-card.ts`; `core-contract.ts`; `characters.api.ts`; any existing spec |
| **P4.80** | NEW `crates/quilltap-core/src/api/chat_delete.rs`; `crates/quilltap-web/src/chats_routes.rs`; NEW harness family `chat_delete_equivalence` + oracle case (+ the optional `chat-delete-{main,mount}.db` pair and its builder); `apps/web/src/app/screens/salon/chat-card.ts`, `salon-list.ts`; `apps/web/src/app/screens/characters/view/tabs/conversations-tab.ts`, `view/tabs/character-conversation-card.ts`; its own `chat-delete.api.ts` + wire file; NEW e2e spec `chat-delete-flow.spec.ts`; `apps/web/package.json`; this order's header | `api/types.rs`, `api/engine.rs`, `web/src/lib.rs` (the DELETE registration on the existing `/api/v1/chats/{id}` route line — ONE `.delete(...)` chained onto it) | `generators/**`; `characters_routes.rs`; `screens/characters/{edit,new,list}/**`, `view/character-detail.ts`, `view/character-header.ts`; `core-contract.ts` |
| **P4.81** | `crates/quilltap-host/src/spine.rs` (the TWO named existing sites only); `crates/quilltap-harness/tests/chat_create_capstone_equivalence.rs` (retire the `p4_78_host_wire_details_carry_is_deferred` tripwire → its positive twin); the orchestrator file that holds `execute_turn_chain` (three log lines only); `harness/oracle/fixtures/help-chat-orchestrator-tier3.json` + `harness/oracle/cases/help-chat-orchestrator-tier3.test.ts` (the `text_block_turn` case only); `docs/v4/WINDOWS.md` + `docs/v4/help/database-protection.md` (deletion); `apps/web/README.md`; `apps/web/src/app/screens/salon/salon-conversation.ts` + its spec (the composer predicate only); one EXISTING e2e spec (item 7's restore); NEW e2e beat file `composer-active-seat.spec.ts`; this order's header | — | `generators/**`; `api/types.rs`; `api/engine.rs`; every file in another lane's Owns column |

Everyone: `docs/CHANGELOG.md` + `status-log.md` append-only; the drift
ledger frozen; `core-contract.ts` / `characters.api.ts` / `shell.ts` /
`workspace-contract.ts` frozen.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
   -D warnings` in BOTH feature sets; `cargo build --workspace`; the release
   build.
2. `cargo test --workspace` (full log + sentinel, never `| tail`) with
   K0's `QT_ORACLE_GENERATORS_LEAF` + your three families' oracle vars +
   `QT_FIXTURE_CHARACTERS_{MAIN,MOUNT}`; grep for `SKIP:` — zero for the
   four; the by-name runs with `-- --nocapture` showing case counts.
3. Each family by NAME through `recipe_sweep.py --v4
   /tmp/qt-v4-pin-p49k1-f699da6f6 --run <family>` (K0's leaf family too —
   a neutrality check on the substrate you stack on); `--show` each new
   header.
4. The mutation proofs named in the lane record: the `coerce` key ORDER
   swapped; the `run_sub_step` catch removed (the throw case aborts the
   run); the `None`-content throw removed; a prompt byte changed; the
   dry-run guard inverted; the Staff-skip predicate dropped; the primary-
   first ordering reversed.
5. `git diff main -- apps/web/` empty; `git diff <K0 tip> -- crates/
   quilltap-core/src/generators/{llm_json,field_semantics,
   generated_properties,sanitize_pronouns}.rs` empty (you consume, never
   edit, the substrate — a needed change is a K0-fence STOP-and-flag).
6. Commits per `.claude/commands/commit.md` (core + harness + web + host
   bumps; CHANGELOG every commit, the LIVE-spend note on the driver
   commit); the lane record appended to `status-log.md` (guard-order
   measurements per action, the `query_param_semantics` row change for the
   unifier, the riders' closure, 💸 the live optimizer run on the Friday
   copy for the dogfood queue); this order's status header updated; the
   drift ledger UNTOUCHED (the unifier retires the bug-119 row).
