# P4.D180 — In Their Own Words, the rehearsal: the shared `voice_rewrite_core`, the new `in_scene_voiced` service, the `chatImpersonationVoicePreview` verb, the host wire (the `686954937` drift catch-up, lane 2 of 3)

**Status: CLOSED** (unified 2026-09-11 — the `f4ad2c8d1` In-Their-Own-Words
drift catch-up round, P4.D179 ∥ P4.D180 ∥ P4.D181; lane branch
`claude/p4-d180-voice-rehearsal-3522a0`, six commits). Every Tier-1 item landed
at the tier ordered, plus both Tier-2 items: the RED-FIRST neutrality baseline
(the announcer NDJSON byte-identical at BOTH pins — v4's extraction proven
neutral on v4's own code; a pre-existing P4.D171 vintage red on the announcer
family healed on the per-case copy), `voice_rewrite_core.rs` + the
`character_voiced.rs` refactor (green against both pins), `in_scene_voiced.rs`
function for function (the filter ORDER whisper → history → presence from the
FULL event list; UTF-16 `max_tokens_for_seed`; `REWRITE_INSTRUCTION` pinned
against its own eight-line construction), the `get_compiled_identity_stack`
twin (the orchestrator's turn-time deferral NOT lifted, §R.14), the verb with
v4's ladder in v4's MEASURED order, the LIVE host wire (💸), the NEW committed
`in-scene-voiced-{main,mount}.db` pair + the 27-case family (8 service + 19
action rows; `maxTokens` a first-class comparand) — **whose first run caught a
real defect inspection missed** (the played filter tested `systemSender` as a
BOOLEAN where the column is TEXT — every Staff bubble stayed in the transcript),
the web-venue wire test proving the driver WIRED and the `llm_logs` row landing,
the census 431 → 435 (FOUR `*_id` fields, not the order's +3). **Three measured
deviations recorded:** no new `CoreResponse` variant (the sibling returns
`Response::ChatPostOffice(Value)`), the census delta, and the hand-built
five-field `RouteProfile` projection (`route_profile_from_value` is private). An
authorised four-site out-of-ownership edit to `host.rs`. Tier 3 all loud: no
REST edge (re-measured), the turn-time reader unported, the vault-unavailable
carry reused. **The §3 unification review fixed on the unify branch:** the
in-code claim that the formatter's `name` reaches the wire through a content
prefix was FALSE on name-supporting providers — v5's `CompletionMessage`
carries no `name`, the same shape its whole turn path has — now RECORDED as
`[CHEAP_LLM_NAME_FIELD_GAP]` pinned in BOTH directions (the oracle records
`name`; 136 v4 rows carry it, 0 v5 rows; the wider turn-path measurement is a
phase-4 candidate); `systemPromptId` renders `null` not `<null>`; the two Tier-2
log pins dropped an `env_or_skip` guard on an oracle variable they never read
(they had SKIPped silently in every gate without it); the `host.rs` note counts
four sites. The `EXPECTED_REHEARSAL_LOG_TYPE` flip went green at the wire.
Round record: `status-log.md`.

## Preamble

- **v4 oracle baseline: `cc65d6bfc`.** This lane absorbs the SERVICE +
  ACTION halves of **`686954937`** ("feat(salon): restate an impersonated
  line in the character's own voice", 2026-09-10 22:31 -0500) — the
  ledger's first §3 row, `ORDERED(P4.D179, P4.D180, P4.D181)`. Spec reads
  are the hunks and the post-commit files: `git show 686954937 -- lib/
  services/announcer/ app/api/v1/chats/[id]/actions/impersonation-voice-
  preview.ts app/api/v1/chats/[id]/schemas.ts app/api/v1/chats/[id]/
  handlers/post.ts app/api/v1/chats/[id]/actions/index.ts` and `git show
  686954937:<path>` — **never the commit message** (ledger §5.3). Where the
  message and the design doc are WIDER than the hunks: the doc's plan said
  the preview "re-reads the chat" — the shipped handler RECEIVES the
  already-loaded `chat` from `handlers/post.ts` (the dispatcher's 404
  precedes it); the doc's "normalizeWhisperRoles" step is NOT used (the
  transcript excludes every `systemSender` message outright); the doc's
  640×600 became 640×720 (SPA, not yours). Port the hunks.
- **Run the drift ledger's §2 freshness probe at lane start** with the §R.2
  expected values. **Any mismatch: STOP and report.** Re-run before every
  regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** This lane's pins are
  **`/tmp/qt-v4-pin-p4d180-f4ad2c8d1`** (the new family, the announcer
  regen at the tip) and **`/tmp/qt-v4-pin-p4d180-cc65d6bfc`** (the announcer
  regen at the baseline — the both-pins neutrality proof — and `QT_V4_ROOT`
  for EVERY workspace gate, because this lane's embedded help tree stays
  the 123-file one).
- **Sibling lanes:** P4.D179 (the column, the `VOICE_REWRITE` map, the
  Almanack, the help re-vendor — you READ its map, never edit it; the
  ACTIVATE-AT-UNIFY log-type constant in your wire test is the meeting
  point) and P4.D181 (the SPA — consumes §B's verb). Neither touches any
  file of yours (§R.5). No stacking.
- **Worktree:** per the `carryout` skill; `git worktree add <path> -b
  claude/p4-d180-voice-rehearsal-<hash> main`; edit inside it
  (`edit-inside-the-worktree-not-main`); `CARGO_INCREMENTAL=0`.
- **Read before the oracle work:** `announcer_tier3_equivalence.rs`'s header
  + `harness/oracle/cases/announcer-tier3.test.ts:1-60` (the canned-
  completion recorder over the committed `post-office-{main,mount}.db`
  pair — your new family is that shape over a NEW pair); the jest-oracle
  leak traps (`jest-oracle-character-vault-bridge-is-mocked` — every vault
  read lists an EMPTY folder unless you `requireActual` as the announcer
  case does; `jest-setup-llm-logging-service-mocked` — zero `llm_logs`
  rows on the v4 side, so the log-type is NOT a tier-3 comparand;
  `jest-oracle-lazy-help-ensure-resyncs-the-fixture`); `oracle-case-clock-
  freeze-defeats-the-sentinel-normalizer` (the clock freeze belongs in the
  BUILDER); `a-committed-fixture-builder-belongs-in-prose`;
  `web-test-venue-has-no-spine-factory` (register `ProductionSpineFactory`
  to prove the driver wired); `a-new-verb-moves-the-dispatch-wrong-type-
  census`.

## Mandate

Port the in-scene voice rehearsal whole, so that on v5 exactly as on v4 at
`f4ad2c8d1`: (a) the two rehearsals share ONE core — the Commonplace recall
against the seed (limit 20 / minImportance 0.3 / maxEntries 12, failure
tolerated with v4's warn), the cheap-LLM call, the never-throws result shape
— and the OFF-SCENE announcement rehearsal's assembled bytes DO NOT MOVE
(proven: `announcer_tier3_equivalence` byte-identical at both pins and green
against both); (b) a NEW `generate_in_scene_voiced_line` composes the seat's
per-turn system prompt (identity stack read from the STORED compiled stacks
when present at the current version, else built fresh; roleplay template with
project/global fallback; Taboo, read-tolerant; standing instructions;
subprompts ONLY when no precompiled stack; NO tool instructions), the last
12 played messages shaped for THIS seat (played filter → whispers → history
access → presence windows when `!hasHistoryAccess` → `[Name]` attribution
with bystander vault failures warned-and-skipped → provider formatting),
the recall block, v4's byte-exact `REWRITE_INSTRUCTION`, and a `max_tokens`
of `clamp(floor(utf16len(seed)/2), 1024, 4096)`; (c) the
`chatImpersonationVoicePreview` verb reproduces v4's handler ladder, its
profile chain (override → seat → character default → instance default),
its uncensored-route reroute (`shouldUseUncensoredRoute(chat)` AND
`mode === 'AUTO_ROUTE'` AND `!profile.isDangerousCompatible` → the
`resolveProviderForDangerousContent` choice, api key discarded), its system
prompt chain (override → seat → character default → `isDefault` → first),
its subprompt resolution, its byte-exact 404/400 sentences, its info line,
and its `{success, proposedMarkdown, profileName, modelName}` body; (d) the
host assembles the driver LIVE (💸 one cheap-LLM call per rehearsal) with a
per-request logging executor so the call lands on an `llm_logs` row. This
lane does NOT touch the settings column, the log-type map, the Almanack,
`help/**`, or any SPA file, and does NOT lift the orchestrator's turn-time
compiled-stack deferral (§R.14).

## Survey-verified starting points (fresh survey 2026-09-10 at `f4ad2c8d1` and on `main` `a0c95230` — do not re-derive)

### (A) v4 at the tip — the three service files and the action

- **`lib/services/announcer/voice-rewrite-core.ts`** (NEW, 144 lines):
  `RECALL_LIMIT = 20`, `RECALL_MIN_IMPORTANCE = 0.3`, `RECALL_MAX_ENTRIES =
  12`; `interface VoiceRewriteResult { success; proposedMarkdown; error? }`;
  `formatNameList(names)` (moved here VERBATIM from `character-voiced.ts`);
  `recallForSeed(character, seedMarkdown, profile, userId, chatId,
  logContext)` — `searchMemoriesSemantic(character.id, seed, {userId, limit,
  minImportance})` → `[]` → `''`; `buildMemorySubjectContext(character.id,
  memories)` → `formatDynamicMemoryHead(results, profile.provider, subject,
  {maxEntries})` → `!formatted.content` → `''` → `buildCommonplaceLLMContext
  ({relevant})`; the catch logs `logger.warn(\`${logContext} Memory recall
  failed; proceeding without\`, {chatId, characterId, error})` and returns
  `''`; `executeVoiceRewrite({selection, messages, userId, taskType, chatId,
  characterId, maxTokens})` — `executeCheapLLMTask<string>(selection,
  messages, userId, trim, taskType, chatId, undefined, undefined, maxTokens,
  characterId)`; `!success || !result` → `{success:false, proposedMarkdown:
  '', error: llmResult.error || 'The LLM returned no content.'}`.
- **`lib/services/announcer/character-voiced.ts`** (refactor, −117/+104
  across the commit): imports the three from the core; `CharacterVoiced
  AnnouncementResult = VoiceRewriteResult`; `ANNOUNCEMENT_MAX_TOKENS =
  2048`; `LOG_CONTEXT = '[CharacterVoicedAnnouncement]'`; the inline recall
  block → `recallForSeed(character, seedMarkdown, profile, userId, chatId,
  LOG_CONTEXT)`; the inline execute → `executeVoiceRewrite({…, taskType:
  TASK_TYPE, maxTokens: ANNOUNCEMENT_MAX_TOKENS})`; the catch's message →
  `\`${LOG_CONTEXT} Unexpected failure\``. Everything about the framing —
  the system prompt (`buildSystemPrompt({character, selectedSystemPromptId})`
  only), the roster/audience block, the "Below is your own rough draft…"
  instruction — is UNCHANGED. v4's own snapshot test (`__tests__/character-
  voiced.test.ts` + its `.snap`) asserts the composed messages before and
  after; **your proof is the announcer differential at BOTH pins.**
- **`lib/services/announcer/in-scene-voiced.ts`** (NEW, 347 lines):
  - `IN_SCENE_REWRITE_WINDOW = 12` (exported), `TASK_TYPE =
    'impersonation-voice-rewrite'`, `LOG_CONTEXT = '[InSceneVoicedLine]'`,
    `MIN_MAX_TOKENS = 1024`, `MAX_MAX_TOKENS = 4096`; `maxTokensForSeed(seed)
    = min(4096, max(1024, floor(seed.length / 2)))` — **`seed.length` is
    UTF-16 units** (`jsstr::utf16_len`), and the seed passed is the TRIMMED
    seed (`:315` `maxTokensForSeed(seedTrimmed)`).
  - `REWRITE_INSTRUCTION` = eight lines `.join(' ')` — port as ONE string
    literal, byte-exact, including the em dashes, the backtick-quoted
    `` `@Name` ``, and `scene\'s` (a plain apostrophe).
  - `InSceneVoicedLineParams { chat, participant, character, profile,
    seedMarkdown, systemPromptId: string|null, subprompts: readonly
    SubpromptForPrompt[]|null, userId }`.
  - `buildTranscriptForSeat(events, participant)`: `all` = every
    `type === 'message'` event mapped to `MessageWithParticipant` (role,
    content, id, participantId ?? null, createdAt, targetParticipantIds ??
    null, hostEvent ?? null); `played` = the message events with
    `!systemSender && (role USER|ASSISTANT) && typeof content === 'string'
    && content.trim().length > 0` (NO `hostEvent` on these); `shaped =
    filterWhisperMessages(played, participant.id)` → `filterMessagesBy
    HistoryAccess(shaped, participant)` → if `!participant.hasHistoryAccess`:
    `computePresenceWindowsForParticipant(all, participant)` then `filter
    MessagesByPresenceWindows(shaped, windows)` (windows from the FULL list
    — the Host status announcements that define them are themselves
    whispers, gone from `played`); `return shaped.slice(-12)`.
  - `generateInSceneVoicedLine`: `selectionFromProfile(profile)`;
    `chatSettings = repos.chatSettings.findByUserId(userId)`;
    `resolveUserIdentity(repos, userId, chat, null)` → `userCharacter =
    {name, description}` (**null active speaker on purpose** — `{{user}}`
    is the OWNER persona, the overlay does not change it);
    `getRoleplayTemplate(repos, chat, chatSettings ? {defaultRoleplay
    TemplateId: chatSettings.defaultRoleplayTemplateId ?? undefined} :
    null)`; Taboo via `getTabooSettings()` in a try/catch that warns
    `\`${LOG_CONTEXT} Failed to read Taboo settings — continuing without
    them\`` `{chatId, error}` and continues with `[]`; `resolveStanding
    InstructionsSection({projectId: chat.projectId ?? null, characterId})`;
    `precompiledIdentityStack = getCompiledIdentityStack(chat,
    participant.id)`; `buildSystemPrompt({character, userCharacter,
    roleplayTemplate, selectedSystemPromptId: systemPromptId, scenarioText:
    chat.scenarioText ?? undefined, precompiledIdentityStack, tabooPhrases,
    standingInstructions, subprompts: precompiledIdentityStack ? null :
    subprompts})` — **no `toolInstructions`**; `events = repos.chats.
    getMessages(chat.id)`; the shaped transcript; `participantCharacters`
    map: for each participant with a `characterId` not yet seen — the
    rehearsing seat's character is the ALREADY-LOADED `character`
    (no re-read), every other via `repos.characters.findById` in a
    try/catch that warns `\`${LOG_CONTEXT} Could not load a participant
    character for attribution\`` `{chatId, characterId, error}` and skips;
    `attributeMessagesForCharacter(shaped, participant.id,
    participantCharacters, chat.participants)`; `formatMessagesForProvider
    (attributed, profile.provider, character.name)` mapped to `{role,
    content, ...(name ? {name} : {})}`; `recallForSeed(character, seed,
    profile, userId, chat.id, LOG_CONTEXT)`; `seedTrimmed = seed.trim()`;
    `userParts = [recallText?, REWRITE_INSTRUCTION, 'Draft:', seedTrimmed]
    .join('\n\n')`; `messages = [{system}, ...transcript, {user}]`;
    `maxTokens = maxTokensForSeed(seedTrimmed)`; the `debug` bag
    `{chatId, participantId, characterId, profileId, transcriptMessages,
    systemPromptLength, hasRecall, hasTemplate, tabooPhrases,
    usedPrecompiledStack, seedLength, maxTokens}`; `executeVoiceRewrite`.
    The catch: `logger.error(\`${LOG_CONTEXT} Unexpected failure\`,
    {chatId, participantId, characterId, error}, error)` → `{success:
    false, proposedMarkdown: '', error: getErrorMessage(error)}`.
- **`app/api/v1/chats/[id]/schemas.ts:230-243`** — `impersonationVoice
  PreviewSchema = z.object({ participantId: z.uuid(), seedMarkdown:
  z.string().min(1), connectionProfileId: z.uuid().optional(),
  systemPromptId: z.uuid().optional() })`. Zod 4.5.4 — `z.uuid()` is the
  RFC-shape gate the harness already transcribes (`zod_uuid`).
- **`app/api/v1/chats/[id]/actions/impersonation-voice-preview.ts`** (NEW,
  175 lines) — signature `(req, chatId, chat, ctx)`; the ladder, IN ORDER:
  `req.json()` + `schema.parse` (a Zod failure → the middleware's
  `validationError` envelope) → `chat.participants.find(id)` else
  `notFound('Participant')` → `participant.type !== 'CHARACTER' ||
  !participant.characterId` → `badRequest('Only a character seat can be
  spoken for.')` → `!isParticipantPresent(participant.status)` →
  `badRequest('That character is not present in this chat.')` →
  `!(chat.impersonatingParticipantIds ?? []).includes(participant.id)` →
  `badRequest('That seat is not being impersonated.')` → `debug` "seat
  verified" → `repos.characters.findById(characterId)` (a
  `CharacterVaultUnavailableError` propagates to the middleware — v5: the
  `CoreError` entity carry / the store-unavailable 503, the P4.23 shape)
  else `notFound('Character')` → the profile chain with `profileSource` ∈
  {override, participant, character-default, instance-default} via
  `repos.connections.findById` ×3 / `findDefault(user.id)` → none →
  `badRequest('No connection profile to rewrite with')` → `debug` "profile
  resolved" → `if (shouldUseUncensoredRoute(chat))`: `chatSettings =
  repos.chatSettings.findByUserId(user.id)`; `dangerSettings =
  resolveDangerousContentSettings(chatSettings, chat).settings`; `if
  (dangerSettings.mode === 'AUTO_ROUTE' && !profile.isDangerousCompatible)`:
  `routeResult = resolveProviderForDangerousContent(profile, '',
  dangerSettings, user.id)`; `if (routeResult.rerouted)` → `profile =
  routeResult.connectionProfile`, `profileSource = 'uncensored-route'`,
  `debug` "rerouted to uncensored profile" → the system-prompt chain
  `validated.systemPromptId ?? participant.selectedSystemPromptId ??
  character.defaultSystemPromptId ?? characterPrompts.find(isDefault)?.id
  ?? characterPrompts[0]?.id ?? null` → `resolveSelectedSubprompts(character
  .id, participant.selectedSubpromptIds ?? [])` → `debug` "prompt resolved"
  → the service → `!success` → `badRequest(result.error || 'Failed to
  restate the line in character.')` → `logger.info('[Chats v1]
  Impersonation voice preview generated', {chatId, participantId,
  characterId, profileId, seedLength, proposedLength})` → `NextResponse.
  json({success: true, proposedMarkdown, profileName: profile.name,
  modelName: profile.modelName})`. Registered at `actions/index.ts:50` and
  in `handlers/post.ts` (`CHAT_POST_ACTIONS` `:98`; the dispatch `:168`
  passes `chat`).
- **What the commit did NOT do here:** no persistence of any kind; no
  streaming; no per-chat override; no tool instructions; no
  `normalizeWhisperRoles`; the announcement POST path (`actions/
  announcement.ts`, `writer.ts`) untouched; `character-avatar` /
  `story-background` untouched.

### (B) v5 on `main` (`a0c95230`) — the twins and the seams

- **`crates/quilltap-core/src/services/announcer/`**: `character_voiced.rs`
  (500 lines; `TASK_TYPE` `:59`; params `:62-80`; the inline recall +
  execute you extract; `precompiled_identity_stack: None` at `:332` — the
  OFF-SCENE prompt has no template/stack by design, unchanged), `mod.rs`
  (18 lines — add the two modules), `audience.rs`, `writer.rs`. Pinned by
  `announcer_tier3_equivalence` (eight audience arms + the base cases over
  the committed `post-office-{main,mount}.db`, the canned completion keyed
  by `provider|model|temperature|messages` — **`max_tokens` is NOT in that
  key**; see Tier 1 item 4).
- **The verb precedent, `crates/quilltap-core/src/api/chat_post_office.rs:
  295-470`**: `AnnouncementPreviewDriver` (a `Send + Sync` trait returning
  `Pin<Box<dyn Future<Output = Result<Outcome, String>>>>`),
  `CharacterVoicedRequest` / `CharacterVoicedOutcome`, the ready-made
  `AnnouncementPreviewRunner<C, E>` (db + completion + embedding +
  `Arc<CheapLlmTaskExecutor>` + `now_ms: Option<f64>` for the
  differential's frozen clock), and `chat_announcement_preview(db, driver,
  user_id, chat_id, …)` — `load_chat` 404 → the Zod gate (`min1`,
  `is_uuid`) → `validation_error()` → the reads → the not-assembled
  refusal (`internal("… is not available: the host has not assembled …")`)
  → `driver.run(..)` → the `badRequest(result.error || default)` arm with
  the JS-falsy note. Your handler is this file's sibling
  `chat_impersonation_voice_preview` with its own `InSceneVoiceDriver` /
  `InSceneVoicedRequest` / runner.
- **`api/types.rs:2884-2898`** — `Request::ChatAnnouncementPreview` (the
  serde attributes to mirror); **`api/engine.rs:221-231`** (the seam field
  `announcement_preview: Option<Arc<dyn AnnouncementPreviewDriver>>`),
  `:578-580`, `:1237-1260` (the dispatch arm + `ready_db_and_announcement_
  preview()`), `:5768`. Add the sibling field + arm; the `CoreResponse`
  variant carries the raw JSON body (the `ChatAdmin(Value)` idiom the web
  renderer already serves).
- **`crates/quilltap-host/src/spine.rs:657-720`** —
  `HostAnnouncementPreviewRunner<C, E>` rebuilds a LOGGING
  `CheapLlmTaskExecutor::with_logging(CheapLlmLogConfig { db, user_id,
  chat_id: Some(..), message_id: None, ctx: LogContext::none() })` per
  call (so the row lands on `llm_logs`); assembled at `:3593-3597`
  (`announcement_completion` / `announcement_embedding`). Your runner is
  its sibling over the same two providers; assembled beside it.
- **The twins the service composes** (READ-ONLY; a needed edit is a
  STOP-and-flag): `cheap_llm.rs` `selection_from_profile`;
  `db/chat_settings.rs` `find_by_user_id`; `services/user_identity_
  resolver.rs` `resolve_user_identity`; `services/participant_resolver.rs`
  `get_roleplay_template`; `db/instance_settings.rs` `get_taboo_settings`;
  `standing_instructions.rs` `resolve_standing_instructions_section`;
  `system_prompt.rs:486` `BuildSystemPromptOptions.precompiled_identity_
  stack: Option<&str>` (+ `subprompts`, `taboo_phrases`,
  `standing_instructions`, `tool_instructions` — leave `None`);
  `message_attribution.rs` `filter_whisper_messages`, `filter_messages_by_
  history_access`, `compute_presence_windows_for_participant` (`:109`),
  `filter_messages_by_presence_windows` (`:161`), `attribute_messages_for_
  character`, and its `MessageWithParticipant` shape; `message_formatter.
  rs` `format_messages_for_provider`; `subprompts/storage.rs` `resolve_
  selected_subprompts`; `chat_predicates.rs:40` `is_participant_present
  (ParticipantStatus)` (the CANONICAL one — `tools/self_inventory.rs:816`
  and `tools/doc_edit/shared.rs:324` carry private twins; use the
  canonical); `db/connection_profiles.rs:926` `find_default`;
  `dangerous_content/chat_override.rs:146` `should_use_uncensored_route
  (chat: Option<&Value>)`, `resolver.rs:107` `resolve_dangerous_content_
  settings(global, chat)`, `provider_routing.rs:204` `resolve_provider_for_
  dangerous_content(conn, api_keys, original_profile: &RouteProfile,
  original_api_key, mode, uncensored_text_profile_id, …)` — v4 passes `''`
  as the key and discards the returned key; measure the v5 signature's
  remaining parameters and thread exactly what the Brahma/turn sites
  thread (`api/brahma*` and `danger_orchestrator` are the call-site
  precedents); the profile's `isDangerousCompatible` read.
- **The compiled-stack reader:** `services/system_prompt_compiler.rs:215`
  `read_current_stacks(chat) -> Option<Map>` is PRIVATE (strict version
  equality against `IDENTITY_STACK_BUILDER_VERSION`, legacy/older/newer →
  `None`). v4's `getCompiledIdentityStack(chat, participantId)` is
  `readCurrentStacks(chat)?.[participantId] ?? null` (verify at the pin in
  `lib/services/system-prompt-compiler/compiler.ts`). Add the
  per-participant twin `pub fn get_compiled_identity_stack(chat: &Value,
  participant_id: &str) -> Option<String>` in that file (its ONLY edit;
  §R.14 — the orchestrator's `None` at `orchestrator.rs:4151` stays).
- **The web edge:** NONE — `crates/quilltap-web/src` serves no `POST
  /api/v1/chats/{id}?action=` family; `announcement-preview` is reached
  only through `POST /api/dispatch` (measured). Record; do not invent.
- **The census:** `crates/quilltap-web/tests/dispatch_wrong_type_census.rs:
  2464` `EXCLUDED_BY_THE_ROUTE_IDENTIFIER_RULE: usize = 431` — your three
  `*_id` fields move it; the census's per-verb rows want the new verb's
  wrong-type shapes (a non-string `seedMarkdown` etc.) adjudicated
  FAITHFUL/DIVERGENT against v4's Zod, as P4.62 did.
- **The web test venue:** `crates/quilltap-web/tests/chat_send_smoke.rs:190`
  and `chat_create_end_to_end.rs:196` construct the spine with
  `announcement_preview: None` — your new seam field gets a `None` there
  too (the compile will tell you); the LIVE-wired proof needs
  `ProductionSpineFactory` (the memory note).

## Tiered deliverables

### Tier 1 — must land, in this order

1. **RED-FIRST + the neutrality baseline, before any source edit:** (a)
   regenerate `announcer_tier3_equivalence` from BOTH pins and diff the two
   NDJSONs — **byte-identical is the finding that v4's extraction moved
   nothing** (a difference is a STOP-and-flag: the order's neutrality
   premise would be wrong); run v5 against the tip's NDJSON — green on the
   unchanged tree. (b) Build the new fixture pair + the new oracle case
   (items 2–3) and run the new family against v5's tree — RED on every row
   (no verb, no service).
2. **The NEW committed fixture pair** `crates/quilltap-web/tests/fixtures/
   in-scene-voiced-{main,mount}.db` (+ `.meta.json` sidecars — `fixture-
   isolation-must-copy-the-meta-sidecars`), built from a `/tmp`-staged
   builder described in PROSE in the family header, seeds pinned, clock
   frozen IN THE BUILDER. It must carry, at minimum: ONE chat with an
   impersonated LLM character seat (`impersonatingParticipantIds` set;
   `controlledBy: 'llm'`; `connectionProfileId` set; `selectedSystemPrompt
   Id` set; `selectedSubpromptIds` naming a real vault subprompt), a
   compiled stack at `IDENTITY_STACK_BUILDER_VERSION` for that seat AND a
   second chat where the stack is absent/legacy (the `subprompts:
   precompiled ? null : subprompts` arm is only measurable with BOTH), a
   second CHARACTER seat (`[Name]` attribution) whose character is
   deliberately BROKEN in the vault (the warned-and-skipped arm), a
   `hasHistoryAccess: false` seat with a Host status announcement mid-
   transcript (presence windows), ≥ 14 played messages (the `-12` slice is
   measurable), at least one whisper NOT addressed to the seat, at least
   one `systemSender` message, an empty-content message, a project with
   standing instructions, Taboo phrases in `instance_settings`, a
   scenario text, a roleplay template on the chat, a `controlledBy: 'user'`
   seat, an ABSENT seat, a chat flagged `UNCENSORED` with an `AUTO_ROUTE`
   danger bag + a non-`isDangerousCompatible` profile + a configured
   uncensored text profile (the reroute arm) and a profile that IS
   dangerous-compatible (the no-reroute arm), a character with two named
   system prompts (the `isDefault`/first chain), and memories for the
   rehearsing character so the recall block is non-empty on at least one
   row. Sanitize per the fixture-sanitizer discipline; the pair is
   test-pepper keyed.
3. **The NEW oracle case** `harness/oracle/cases/in-scene-voiced-tier3.
   test.ts` (jest, the announcer recorder's shape: real DB via
   `requireActual` of the cipher driver, the canned completion keyed by
   the RECORDED call, a recording wrapper so a prompt mismatch is a
   readable diff) + `harness/oracle/fixtures/in-scene-voiced-tier3.json`,
   in TWO sections: **service rows** driving v4's REAL
   `generateInSceneVoicedLine` (the assembled system prompt, the transcript
   messages with their roles/names, the user message, AND `maxTokens`
   captured from the recording wrapper as its own comparand, the result
   shape, the recall bump on `lastAccessedAt`), and **action rows** driving
   v4's REAL `handleImpersonationVoicePreview` through a `NextRequest`
   (status + body per arm: Zod refusal, missing participant, non-character
   seat, absent seat, not-impersonated, missing character, no profile,
   each profile-chain source, the reroute arm and the no-reroute arm, the
   system-prompt chain, the success body's four keys in order, the empty-
   error default sentence). Coverage asserted by shape both ways.
4. **`voice_rewrite_core.rs`** (the three constants, `VoiceRewriteResult`,
   `format_name_list` MOVED here, `recall_for_seed(...)` with v4's warn
   bytes, `execute_voice_rewrite(...)`) + **the `character_voiced.rs`
   refactor** onto it (`ANNOUNCEMENT_MAX_TOKENS`, `LOG_CONTEXT`, the catch
   message) — then `announcer_tier3_equivalence` GREEN against BOTH pins'
   NDJSONs with the assembled bytes unmoved. **`max_tokens` must become a
   comparand in the announcer family too** if it is not already (the
   canned key omits it; a wrong ceiling is invisible today — measure, and
   if invisible, add the captured option as a row field on both sides).
5. **`in_scene_voiced.rs`** — `IN_SCENE_REWRITE_WINDOW`, `TASK_TYPE`,
   `LOG_CONTEXT`, `max_tokens_for_seed` (UTF-16), `REWRITE_INSTRUCTION`
   byte-exact, `build_transcript_for_seat`, `generate_in_scene_voiced_line`
   per §A — every log line at v4's level with v4's bag keys (the #103/#110
   class: a dropped warn IS a port divergence), capture-pinned for the two
   warn arms + the debug bag. `get_compiled_identity_stack` twin in the
   compiler. The service family GREEN.
6. **The verb** — `Request::ChatImpersonationVoicePreview { chat_id,
   participant_id, seed_markdown, connection_profile_id: Option<String>,
   system_prompt_id: Option<String> }` (§B serde attributes),
   `CoreResponse::ChatImpersonationVoicePreview(Value)`, the engine seam
   field + `ready_db_and_impersonation_voice_preview()` + the arm, the
   handler `chat_impersonation_voice_preview` in `chat_post_office.rs`
   with v4's ladder IN v4's ORDER (**measure the order on v4 first**: the
   dispatcher's chat 404 precedes the body parse — `handlers/post.ts` loads
   the chat before dispatching; the parse precedes the participant find),
   the driver seam `InSceneVoiceDriver` + `InSceneVoicedRequest` (chat +
   participant + character + profile + seed + system_prompt_id +
   subprompts + user_id) + the ready-made runner with `now_ms:
   Option<f64>`, the not-assembled refusal sentence, the profile chain via
   `find_by_id` ×3 / `find_default`, the reroute through the three
   dangerous-content twins with the api key discarded, the prompt chain,
   `resolve_selected_subprompts`, the info line. The action rows GREEN.
   `DbError::Internal` at every new site (§R.11).
7. **The host wire** — `HostInSceneVoiceRunner` in `spine.rs` beside the
   announcement one (the per-request LOGGING executor, `chat_id:
   Some(..)`, `message_id: None`), assembled LIVE. **The web-venue wire
   test** `crates/quilltap-web/tests/impersonation_voice_preview_wire.rs`
   over `serve_instance` + `ProductionSpineFactory` with a canned
   completion server: dispatch the verb on a seeded impersonated seat →
   200 + the four-key body → ONE new `llm_logs` row whose `type` equals
   `EXPECTED_REHEARSAL_LOG_TYPE` (`"SUMMARIZATION"` on this branch, an
   ACTIVATE-AT-UNIFY comment naming P4.D179; the unifier flips it to
   `"VOICE_REWRITE"`), plus the not-impersonated 400 through the same
   wire. The two `None` seam sites in the existing web tests updated.
8. **The census** — `dispatch_wrong_type_census.rs`: the moved constant
   (measured), the new verb's rows adjudicated against v4's Zod.
9. **Mutation proofs** (each reddening exactly the named rows): the slice
   `-12` → `-11` (a service row with ≥ 13 shaped messages); drop the
   presence-window leg (the `hasHistoryAccess: false` row); read the
   rehearsing seat's own character from the vault instead of the loaded
   value (no row should move — record that it is unobservable, and pin
   the "no second read" by a read-count on the vault seam if cheap);
   `subprompts` passed alongside a present precompiled stack (the
   with-stack row); `max_tokens` computed over `chars().count()` (the
   astral-seed row — add one); the reroute gate reading `is_dangerous_
   compatible` inverted (the reroute row); the profile chain skipping the
   `character-default` source (its row); the ladder with the parse AFTER
   the participant find (the "non-uuid participantId on a missing seat"
   row, which v4 answers with the Zod envelope, not 404); the empty-error
   default sentence dropped (the empty-error row).

### Tier 2 — should land

10. A unit test in `chat_post_office.rs` pinning the info line's bag keys
    (`chatId, participantId, characterId, profileId, seedLength,
    proposedLength`) via a capture layer (`capture-layer-target-assert-is-
    a-prefix-match`).
11. The `%error` vs `?error` rendering check on every new log line
    (`tracing-percent-field-renders-unquoted`).

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **No REST edge** for the new action — recorded (§B), consistent with
  `announcement-preview`; a lane finding that the CLI or any v4 client
  calls the REST form is a STOP-and-flag.
- **The orchestrator's turn-time compiled-stack reader stays unported**
  (§R.14) — name it in the lane record; do NOT wire the new twin into
  `orchestrator.rs`.
- The v4 `CharacterVaultUnavailableError` propagation: v5's existing
  store-unavailable 503 carry (`ErrorKind::Unavailable`, P4.23) is the
  twin — reuse it; if the vault read path in `chat_post_office.rs` cannot
  reach it, refuse LOUDLY with a typed `CoreError` and record.

## The differential requirement (restated)

Every unit ships with an equivalence test driving v4's REAL code at the
`f4ad2c8d1` pin: the service and the action through the NEW jest case over
the NEW committed pair (the announcer recorder's exact mechanism — read
`announcer-tier3.test.ts:1-60` and `announcer_tier3_equivalence.rs`'s
header first), the refactor through the EXISTING announcer family at BOTH
pins. Tier 3 = mocked-LLM: the same canned completion both sides, then the
recall-bump write diffed. The recipe headers follow `recipe-header-
conventions` (no `/tmp` pin in a committed header; `cd ~/source/quilltap-
server`; `TZ=UTC`); regen through the sweep driver; every NDJSON grepped for
a fresh marker (the `REWRITE_INSTRUCTION`'s first sentence is a good one).

## Fixtures

- Delivers: the NEW committed `in-scene-voiced-{main,mount}.db` pair (+
  sidecars) + its prose builder; the NEW oracle case + JSON corpus.
- Consumes READ-ONLY: `post-office-{main,mount}.db` (the announcer family —
  regen AS-IS, no case edit).
- Touches NO `harness/oracle/fixtures/settings*` / provisioning artifact
  (P4.D179's).

## §R — the round-wide meeting points (BINDING — identical in every order of this round)

Round: **the `f4ad2c8d1` In-Their-Own-Words drift catch-up round** — P4.D179
(the `chat_settings` column + the `VOICE_REWRITE` log type + the Almanack
ledger + the `help/**` re-vendor, server) ∥ P4.D180 (the in-scene voice
rehearsal: the shared core, the new service, the verb, the host wire, server)
∥ P4.D181 (the SPA half of both commits + bug 134). Ordered 2026-09-10. Three
lanes, all from `main`, no stacking.

1. **Baseline and drift.** Oracle baseline **`cc65d6bfc`** (drift-ledger §1).
   v4 `main` HEAD **`f4ad2c8d1`** — **TWO commits past the baseline**, the
   ledger's two §3 rows, now `ORDERED`:
   - **`686954937`** ("feat(salon): restate an impersonated line in the
     character's own voice", 2026-09-10 22:31 -0500, 60 files) —
     `ORDERED(P4.D179, P4.D180, P4.D181)`. PORT-NEW with PORT riders: the new
     `chat_settings."impersonationVoiceRewrite"` column (migration
     `add-impersonation-voice-rewrite-field-v1`), the `VOICE_REWRITE` log
     type + the `mapTaskTypeToLogType` rows (**v5 measurably carries v4's
     pre-fix silent default** — `announcement-rewrite` files as
     `SUMMARIZATION` today, `services/llm_logging.rs:392-418`), the
     `character-voiced.ts` → `voice-rewrite-core.ts` extraction (read as
     behaviour-neutral, TO BE PROVEN by regen at both pins), the new
     `in-scene-voiced.ts` service + the `impersonation-voice-preview` chat
     action, the Almanack `featureConfig` row, `help/**` 123 → 124, and the
     whole Salon/Settings client half.
   - **`f4ad2c8d1`** ("Fix bug 134: a chat setting changed while a Salon tab
     is open never reached it", 2026-09-10 22:47 -0500, 14 files) —
     `ORDERED(P4.D181)`, with its `help/settings.md` hunk riding P4.D179's
     re-vendor. PORT, **measured before ported**: v5's Salon already reads
     settings through the `['chatSettings']` TanStack query
     (`salon-conversation.ts:1053`), the settings card's save writes and
     invalidates that key (`chat-settings.api.ts:86-89`), and tab
     re-activation sweeps both spellings (`tab-refetch.ts:94`) — the
     staleness bug 134 fixes may be structurally absent in v5. The lane
     measures each of v4's hunks and ports only what measures present.
   v4 `bugfix` tip **`1a2b2164c`** unmoved; v4 `release` tip **`8fbf2afe0`**
   (4.9.2) unmoved. The round's catch-up TARGET is `f4ad2c8d1`; the unifier
   moves the baseline there.
2. **The freshness probe at lane start** (ledger §2), expected values:
   checkout on **`main`**, HEAD **`f4ad2c8d1`**, tree **CLEAN**, `git log
   --oneline f4ad2c8d1..main` EMPTY, `git log --oneline 1a2b2164c..bugfix`
   EMPTY. **Any mismatch: STOP and report** — a lane never writes the
   ledger. There is NO pre-authorized exception this round. Re-run the probe
   before every regen batch; the checkout has gone dirty MID-LANE before
   (ledger §5.1) and was switched to `release` mid-unify once (P4.D91). v4
   ships a commit every day or two — expect the probe to fail mid-round; a
   failing probe is a STOP, not a reason to pin at whatever HEAD is.
3. **Regen rule: PIN REQUIRED, lane-unique paths, all three symlink
   classes** (ledger §5.1), every regen and fixture build through
   `harness/tools/recipe_sweep.py --v4 <pin> --run <family>`; never two
   sweeps concurrently.
   - **P4.D179 pins `f4ad2c8d1`** for every family it MOVES
     (`/tmp/qt-v4-pin-p4d179-f4ad2c8d1`) and **`cc65d6bfc`** for neutrality
     regens (`/tmp/qt-v4-pin-p4d179-cc65d6bfc`). The help re-vendor is a
     `cp` of the five moved files from the `f4ad2c8d1` pin's `help/`,
     never a hand edit.
   - **P4.D180 pins `f4ad2c8d1`** for every family it MOVES or CREATES
     (`/tmp/qt-v4-pin-p4d180-f4ad2c8d1`) and **`cc65d6bfc`**
     (`/tmp/qt-v4-pin-p4d180-cc65d6bfc`) for the announcer neutrality
     proof: `announcer_tier3_equivalence`'s NDJSON regenerated at BOTH pins
     must be byte-identical (that is the proof that v4's extraction moved
     nothing), and v5's extraction must be green against both.
   - **P4.D181** builds NO Rust oracle; its two SPA recorders (the
     `parseCarinaQuery` corpus and the `shouldRehearseImpersonatedLine`
     transcription check) run from `/tmp/qt-v4-pin-p4d181-f4ad2c8d1`
     (`spa-oracle-recorders-live-outside-src`).
   - **Verify each pin by a marker**: `impersonationVoiceRewrite` appears in
     `lib/schemas/settings.types.ts` ONLY in the `f4ad2c8d1` worktree;
     `VOICE_REWRITE` in `lib/schemas/llm-log.types.ts` ONLY there;
     `lib/services/announcer/in-scene-voiced.ts` EXISTS only there;
     `git diff --stat cc65d6bfc f4ad2c8d1 -- help/` lists exactly six paths
     (`chat-participants.md`, `chat-settings.md`, `impersonation-voice.md`,
     `insert-announcement.md`, `settings.md` — five from the two commits,
     `settings.md` counted once). Check every NDJSON is non-empty before
     believing any diff (the empty-file trap).
   - Committed recipe headers keep saying `cd ~/source/quilltap-server` and
     never name a `/tmp` pin (`recipe-header-conventions`). The oracle
     `node_modules` are the LIVE tree (`zod` 4.5.4 — `zod_version_guard`
     fails the gate if it moves; neither commit changes a dependency —
     both `package-lock.json` deltas are the version bump, re-measured
     2026-09-10).
   - **`QT_V4_ROOT` for the full workspace gate:** P4.D179 sets it to its
     `f4ad2c8d1` pin from its help re-vendor commit onward (before that
     commit, `help_tree_embed_guard` is green only against a `cc65d6bfc`
     root — build the neutrality pin first and use it until then). P4.D180
     sets it to its `cc65d6bfc` pin for EVERY gate (its embedded help tree
     stays the 123-file one until unification). `qtap_schema_embed_guard`
     is green at BOTH pins (`public/schemas/qtap-export.schema.json` is
     untouched by both commits — measured).
4. **Stacking and resumption.** None. All three lanes branch from `main` in
   SEPARATE worktrees and run in parallel. The unifier cherry-picks
   P4.D179, then P4.D180, then P4.D181.
5. **Frozen and single-owner files.**
   - `crates/quilltap-core/src/api/types.rs` + `api/engine.rs` — **P4.D180's
     alone** (the one new `Request`/`CoreResponse` pair; P4.D179 adds NO
     settings mirror — measured 2026-09-10: no per-column field for
     `composerUnicode` exists in `api/types.rs`, the settings bag rides raw).
   - `crates/quilltap-host/src/host.rs` — **P4.D179's alone** (the ONE new
     boot-ensure call after the P4.D73 block at `:1097-1108`).
   - `crates/quilltap-host/src/spine.rs` — **P4.D180's alone** (the new
     driver's construction beside `HostAnnouncementPreviewRunner`,
     `:657-720` + `:3593-3597`).
   - `crates/quilltap-core/src/services/announcer/**`,
     `api/chat_post_office.rs`, `services/system_prompt_compiler.rs` (the
     `read_current_stacks` visibility + the per-participant reader twin
     ONLY) — **P4.D180's alone**.
   - `crates/quilltap-core/src/db/chat_settings.rs`, the NEW
     `db/chat_settings_impersonation_voice_repair.rs`, `api/settings.rs`,
     `services/llm_logging.rs`, `almanack/**`, `services/provisioning/**`
     (via generators), `help/**` + every vendored-count literal — **P4.D179's
     alone**.
   - `apps/web/**` — **P4.D181's alone**; both server lanes finish with
     `git diff main -- apps/web/` EMPTY.
   - `Cargo.toml`/`Cargo.lock` at the workspace root and per crate: version
     lines only; a dependency add is a STOP-and-flag in the lane record (no
     lane expects one).
   - `docs/v4/**` is the unifier's; `docs/developer/porting/drift-ledger.md`
     is frozen (lanes never write it).
6. **§B — the shared contract (name-level, binding; the unifier diffs it
   name-for-name across `api/types.rs`, the web renderer, and
   `core-contract.ts` at the wire).**
   - **The settings key** `impersonationVoiceRewrite`: boolean, default
     `false`. Column `chat_settings."impersonationVoiceRewrite" INTEGER
     DEFAULT 0` at the position v4's live `generateDDL` puts it (the D23
     dump decides — never hand-placed; the Zod schema declares it after
     `composerUnicode` and before `textReplacementsEnabled`,
     `lib/schemas/settings.types.ts:645-646`). The chat-settings GET echoes
     it; the PUT accepts the single-key body `{"impersonationVoiceRewrite":
     true}`; a present-but-non-boolean value — an explicit `null` included
     — answers 400 `{"error":"Invalid impersonationVoiceRewrite value (must
     be boolean)"}` (v4 throws that sentence and the route's
     `includes('Invalid') ? 400 : 500` split makes it a 400; the
     `Option<Option<bool>>` / raw-bag shape so the web edge cannot collapse
     the null — the Taboo §3 lesson). The SPA reads it from the existing
     `chatSettings` verb and saves through `ChatSettingsCard.save`.
   - **The verb** `chatImpersonationVoicePreview` (P4.D180 defines; P4.D181
     consumes; no REST edge — see below). Request, wire keys exactly:
     `{ "type": "chatImpersonationVoicePreview", "chatId", "participantId",
     "seedMarkdown", "connectionProfileId"?, "systemPromptId"? }` — the two
     optionals are OMITTED when unset, never `null` (the SPA mirrors
     `previewAnnouncement`'s `|| undefined`; Rust: `Option<String>` with
     `#[serde(default, skip_serializing_if = "Option::is_none")]`). Success
     data, key order as v4 `NextResponse.json`: `{ "success": true,
     "proposedMarkdown": string, "profileName": string, "modelName": string
     }`, carried as `CoreResponse::ChatImpersonationVoicePreview(Value)`.
     Errors, byte-exact: 404 `Chat` / `Participant` / `Character` through
     the existing `not_found(..)` sentences; 400 `Only a character seat can
     be spoken for.` · `That character is not present in this chat.` ·
     `That seat is not being impersonated.` · `No connection profile to
     rewrite with` · the service's own `error` string, or `Failed to restate
     the line in character.` when it is empty (JS falsy); a Zod refusal
     through the existing `validation_error()` envelope. **Guard order is
     v4's** (the dispatcher's chat 404 precedes the body parse; the parse
     precedes every participant check) — P4.D180 measures it on v4 before
     transcribing.
   - **The log type**: `llm_logs.type = "VOICE_REWRITE"` for BOTH task types
     `announcement-rewrite` and `impersonation-voice-rewrite`
     (`log_type::VOICE_REWRITE`, P4.D179); P4.D180 spells the new task type
     as the string constant `"impersonation-voice-rewrite"` and does NOT
     touch the map. **The live proof that a rehearsal's row lands as
     `VOICE_REWRITE` needs both lanes on one branch** — P4.D180's web-venue
     wire test asserts the row's type through a NAMED constant
     `EXPECTED_REHEARSAL_LOG_TYPE = "SUMMARIZATION"` with an
     ACTIVATE-AT-UNIFY comment; the unifier flips it to `"VOICE_REWRITE"`
     and the test must go green on the flip (red-first on the unify branch
     if it does not).
   - **The SPA labels** (P4.D181): `VOICE_REWRITE` → inspector badge text
     `Voice` with class `qt-bg-info/15 qt-text-info`, inspector filter group
     `other` (after `CUSTOM_TOOL_CONSULT`), Wire Records label `Voice
     Rewrite`.
   - **No REST edge this round.** v5 serves NO `POST /api/v1/chats/{id}?
     action=` REST family — `announcement-preview` is dispatch-only
     (measured 2026-09-10: the string appears nowhere in
     `crates/quilltap-web/src`; the CLI never calls either action). The new
     action follows its sibling; the absence is RECORDED in P4.D180's lane
     record, not invented.
7. **Version bumps** — bump every crate whose files (tests included) the
   commit touches. P4.D179: `quilltap-core`, `quilltap-harness`,
   `quilltap-host` (the ensure call + its `build.rs`-embedded `help/**`),
   `quilltap-web` ONLY if its test venue's hand-rolled `chat_settings` DDL
   moves (measure — `crates/quilltap-web/tests/chat_settings_composer_web_
   routes.rs` drops columns by name). P4.D180: `quilltap-core`,
   `quilltap-harness`, `quilltap-host`, `quilltap-web` (the web-venue wire
   test). P4.D181: the SPA `package.json` only. Per commit. The unifier
   RECOUNTS as base + total bumps — identical bumps merge silently
   (`commit-checklist`; the silent-auto-merge trap fired at the `f699da6f6`
   and `cc65d6bfc` rounds). Base at ordering: core 0.0.886, harness
   0.0.775, host 0.0.125, web 0.0.136, cli 0.0.20, tauri 0.0.7, SPA
   0.5.695.
8. **e2e.** Only P4.D181 touches Playwright. It AUTHORS the two new beats
   gated by NAMED constants (`P4D179_SERVER_LANDED = false` for the
   settings-toggle round trip, `P4D180_SERVER_LANDED = false` for the
   dialog flow) and runs the FULL suite on its branch with them parked —
   never a capability probe (`round-plan-takeaways`: a DEFINED verb defeats
   a probe). The unifier flips both constants and runs the full suite;
   **activating a beat is its first execution** — expect gesture fixes.
9. **Append-only for everyone:** `docs/CHANGELOG.md`,
   `docs/developer/porting/status-log.md`.
10. **Fixture ownership.** P4.D179 owns `harness/oracle/fixtures/
    settings.json` + `build-settings-fixture.ts` + `cases/settings-
    routes.test.ts` (the new `impersonation_voice` family rows),
    `fixtures/chat-settings-tier2.json` + `build-chat-settings-fixture.ts`
    + `cases/chat-settings-tier2.ts`, the provisioning dumps
    (`fresh_schema.json`, `chat_settings_seed.json`, `schema-key-order.json`
    — via the shipped generators only), the NEW `cases/task-type-log-
    mapping.ts` + its corpus, and the four help families' regen. P4.D180
    owns the NEW committed `in-scene-voiced-{main,mount}.db` pair (built
    from a `/tmp`-staged builder described in PROSE in the family header —
    `a-committed-fixture-builder-belongs-in-prose`), the NEW
    `cases/in-scene-voiced-tier3.test.ts` + corpus, and `cases/announcer-
    tier3.test.ts`'s regen AS-IS (neutrality — no case edit). The committed
    `post-office-{main,mount}.db` pair is READ-ONLY for everyone. P4.D181
    owns `apps/web/oracle/**` recorders + `src/testing/fixtures/*.ndjson`
    it adds, and touches NO committed DB pair. **No lane regenerates a
    committed DB pair it does not own**, and no two lanes touch one case
    file.
11. **Census guards.** Every Rust lane's gate is `cargo test --workspace`,
    never `-p` (`a-new-verb-moves-the-dispatch-wrong-type-census`,
    `closing-a-divergence-moves-the-censuses`, `a-new-harness-test-can-trip-
    a-source-census`). P4.D180's verb carries three `*_id` fields — the
    `dispatch_wrong_type_census.rs:2464` constant (`EXCLUDED_BY_THE_ROUTE_
    IDENTIFIER_RULE = 431`) WILL move; measure, don't guess, and record the
    new count. `db_error_key_guard`'s per-file counts must not move (use
    `DbError::Internal`, never `Key`, at every new site). P4.D179's help
    re-vendor moves every vendored-count literal — the memory note
    `a-vendored-count-is-hard-coded-in-several-crates` (FOUR literals in
    THREE crates last time; `help_tree_embed_guard.rs:30` and
    `host_help_docs_boot.rs:82-89` are two of them; find the rest by one
    `--workspace` run).
12. **Cross-app hazards.** (a) The Friday instance is SHARED with v4's dev
    build, which ran `add-impersonation-voice-rewrite-field-v1` on it the
    day it shipped: P4.D179's boot ensure MUST be an idempotent no-op on a
    table that already carries the column, with v4's byte-exact `INTEGER
    DEFAULT 0` on the add path so a v4 later opened on a v5-added column
    sees the same default (the P4.D73 rule). (b) `llm_logs.type` is plain
    TEXT; a v5-written `VOICE_REWRITE` row is readable by v4 at the tip
    (its enum now admits it) — no hazard, recorded. (c) No `PROMPT_CACHE_
    STRUCTURE_VERSION` / `IDENTITY_STACK_BUILDER_VERSION` move: the
    rehearsal READS the stored compiled stack, it never writes one.
13. **Upstream filings are PREPARED, never committed, by a lane.** None is
    expected this round; a P4.D181 bug-134 measurement that finds v4 still
    stale somewhere is drafted in its lane record for the human.
14. **The turn-time compiled-stack reader stays UNPORTED** (the standing
    P4.D103/P4.D163 deferral: `orchestrator.rs:4151` passes
    `precompiled_identity_stack: None`; the tier-3 oracles stub v4's
    `getCompiledIdentityStack` to null). P4.D180's rehearsal service reads
    the stored stack ITSELF, exactly as v4's `in-scene-voiced.ts:219` does
    — through a per-participant reader twin over the compiler's
    `read_current_stacks` — because the rehearsal is a NEW surface with no
    deferral debt, and v4's `subprompts: precompiledIdentityStack ? null :
    subprompts` arm is only measurable with a real stack in the fixture.
    The orchestrator's deferral is NOT lifted by this round.

## Ownership (BINDING — identical table in every order of this round)

| Lane | Owns (edit freely) | Must not touch |
| --- | --- | --- |
| **P4.D179** (the column + log type + Almanack + help, from `main`) | `crates/quilltap-core/src/db/chat_settings.rs`; NEW `db/chat_settings_impersonation_voice_repair.rs` (+ its `db/mod.rs` line); `crates/quilltap-host/src/host.rs` (the ONE ensure call, after the P4.D73 block); `api/settings.rs`; `services/llm_logging.rs`; `almanack/{types,phase3_ledgers,render}.rs`; `services/provisioning/{fresh_schema.json,chat_settings_seed.json,schema-key-order.json}` (via generators); `help/**` (byte copies from the `f4ad2c8d1` pin) + every vendored-count literal (`crates/quilltap-harness/tests/help_tree_embed_guard.rs`, `crates/quilltap-host/tests/host_help_docs_boot.rs`, any other the workspace gate reddens); `harness/oracle/{cases,fixtures}/settings-routes*`, `chat-settings-tier2*`, `build-settings-fixture.ts`, `build-chat-settings-fixture.ts`, `provision/**`; NEW `harness/oracle/cases/task-type-log-mapping.ts` + corpus + NEW `crates/quilltap-harness/tests/task_type_log_mapping_equivalence.rs`; `crates/quilltap-harness/tests/{settings_routes,chat_settings_tier2,provisioning,almanack_tier2,almanack_render,help_tree,memories_routes}_equivalence.rs` (the settings/help families' new rows + regen; `memories_routes` regen-only); `crates/quilltap-web/tests/chat_settings_composer_web_routes.rs` (a fourth wire arm, if landed there rather than in a new file); this order's header | `api/types.rs`; `api/engine.rs`; `api/chat_post_office.rs`; `services/announcer/**`; `system_prompt_compiler.rs`; `spine.rs`; `apps/web/**`; `docs/v4/**`; the committed `post-office-*` / `in-scene-voiced-*` DB pairs |
| **P4.D180** (the rehearsal service + verb + host wire, from `main`) | `crates/quilltap-core/src/services/announcer/{mod,character_voiced}.rs` + NEW `voice_rewrite_core.rs` + NEW `in_scene_voiced.rs`; `api/chat_post_office.rs` (the new handler + driver seam beside the announcement-preview ones); `api/types.rs` + `api/engine.rs` (the ONE new `Request`/`CoreResponse` pair, the seam field, the arm); `services/system_prompt_compiler.rs` (ONLY `read_current_stacks` visibility + a `get_compiled_identity_stack(chat, participant_id)` twin); `crates/quilltap-host/src/spine.rs` (the driver construction + assembly); NEW `crates/quilltap-web/tests/fixtures/in-scene-voiced-{main,mount}.db` (+ `.meta.json` sidecars); NEW `harness/oracle/cases/in-scene-voiced-tier3.test.ts` + NEW `harness/oracle/fixtures/in-scene-voiced-tier3.json`; NEW `crates/quilltap-harness/tests/in_scene_voiced_tier3_equivalence.rs`; `crates/quilltap-harness/tests/announcer_tier3_equivalence.rs` (regen + the both-pins neutrality assertion only); NEW web-venue wire test `crates/quilltap-web/tests/impersonation_voice_preview_wire.rs`; `crates/quilltap-web/tests/dispatch_wrong_type_census.rs` (the moved constant + the new verb's rows); this order's header | `db/chat_settings.rs`; `api/settings.rs`; `services/llm_logging.rs` (READ the map; never edit); `almanack/**`; `provisioning/**`; `host.rs`; `help/**`; `apps/web/**`; `docs/v4/**`; `message_attribution.rs` / `message_formatter.rs` / `dangerous_content/**` / `standing_instructions.rs` / `participant_resolver.rs` / `user_identity_resolver.rs` / `subprompts/**` / `cheap_llm.rs` (READ — no edit expected; a needed edit is a STOP-and-flag); the committed `post-office-*` pair (READ-ONLY) |
| **P4.D181** (the SPA half + bug 134, from `main`) | `apps/web/**` — the Salon (`screens/salon/salon-conversation.ts`), the composer (`chat/chat-composer.ts`), `chat/speaking-as-avatar.ts`, NEW `chat/impersonation-voice/**` (the gate, the state service, the dialog, the review panel), `chat/post-office/insert-announcement-dialog.ts` (the panel extraction ONLY), `chat/llm-inspector-{entry,panel}.ts`, `screens/settings/system/llm-logs-card.ts`, `screens/settings/chat/{chat-tab.ts, NEW impersonation-voice-settings.ts}`, `core/core-contract.ts` (the §B DTOs + the settings key), NEW `chat/carina-parser.ts` client twin + `apps/web/oracle/**` recorder + `src/testing/fixtures/*.ndjson`, `chat/memory-cascade-dialog.ts` (ONLY if the bug-134 measurement finds the remember arm owed), the e2e specs it names; this order's header | every `crates/**` file; `help/**`; `docs/v4/**`; the committed DB pairs; `e2e/support/**` fixture SEEDS (read; a needed seed change is a STOP-and-flag) |

Everyone: `docs/CHANGELOG.md` + `status-log.md` append-only; the drift
ledger frozen; `docs/v4/**` the unifier's. `Cargo.toml` / `Cargo.lock`:
version lines only. The `docs/v4/` mirror refresh (the feature doc at
`docs/developer/features/complete/impersonation-voice-rewrite.md`, the
bug-134 file at its `bugs/fixed/` path, `bugs.md`, `API.md`, `DDL.md`,
`CHANGELOG.md`), the baseline move to `f4ad2c8d1`, the ledger's two §3
rows → ABSORBED, the two gated beats' flips, the
`EXPECTED_REHEARSAL_LOG_TYPE` flip, and the §B name-for-name diff are
UNIFIER wires.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
   -D warnings` in BOTH feature sets (default and `--features
   quilltap-core/native-transport`); `cargo build --workspace`; the release
   build. (P4.D181: `npm run lint` incl. the `check-qt-classes` guard,
   `npm test`, `npm run build` — never a bare `ng`.)
2. `cargo test --workspace` (background, full log + sentinel — NEVER
   `| tail`, never a pgrep loop — `background-gate-latency-fix`,
   `pgrep-until-loops-match-themselves`, `a-gate-piped-through-tail-
   undercounts-binaries`) with this lane's env block (every family it moves,
   by its recipe var — `a-family-env-var-is-not-its-regen-var`) AND
   `QT_V4_ROOT` set per §R.3; grep the log for `SKIP:` — every family this
   lane moves must RUN (confirm by per-binary duration or `-- --nocapture`);
   the exit code read directly, never after a pipe
   (`gate-exit-code-after-a-pipe`).
3. Every family this lane moves regenerated FRESH from the pin §R.3 names
   through `harness/tools/recipe_sweep.py --v4 <pin> --run <family>` (never
   two sweeps concurrently), re-run by name with zero SKIP, the changed
   bytes grepped in the NDJSON (§R.3's markers), and the NDJSON non-empty.
   The RED-FIRST items shown red against the pre-edit tree in the lane
   record (run them before the first source commit — a `git stash`-free
   proof).
4. The mutation proofs named in this order, run and restored by string
   edit (`mutation-proof-revert-by-file-backup`; `cargo-fmt-silently-voids-
   a-string-edit-mutation` — assert the replacement count), each reddening
   exactly the named test.
5. The Ownership table's "must not touch" column honoured (`git diff main
   --stat` read against it); the server lanes' `git diff main -- apps/web/`
   EMPTY; P4.D180's and P4.D181's `git diff main -- help/` EMPTY.
6. P4.D181 only: the full Playwright suite on its branch (port 4319 is
   repo-wide — one run at a time; `cargo build --release` first because
   `webBinary()` prefers `target/release`), the two new beats PARKED by
   name, every existing beat green (the documented P4.D161 pause-toast
   intermittent excepted, re-proven green by spec file in isolation).
7. Commits per `.claude/commands/commit.md` (the crate bumps §R.7 names, on
   every commit that moves the crate; CHANGELOG every commit); the lane
   record appended to `status-log.md` with the red-first evidence, the
   per-family regen lines, the mutation table, every measured deviation
   from this order (`work-order-facts-need-the-same-verification-as-commit-
   prose`), and the "spotted, not mine" list for the unifier.

