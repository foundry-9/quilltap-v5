# Work order P4.D143 — chat lists carry the derived Concierge state, server half (v4 `c43d3b1b4`, PR #46)

**Status:** OPEN (ordered 2026-09-02).

## Preamble

- **v4 oracle baseline: `4622411fd`** (drift-ledger §1, probe re-run
  2026-09-02). **This lane's target commit is `c43d3b1b4`** — *"Chat lists
  and Quick-hide follow Concierge state, not raw danger label (#46)"*, the
  round's largest row (40 files, +1296/−199). Its plan doc `f3351d54f`
  (`docs/developer/features/complete/concierge-list-marks.md` after the
  move) may be read for rationale — it records v4's decision that
  `isDangerousChat` keeps its meaning as the classifier's tri-state label
  rather than being redefined as the route — but the hunks are the spec
  (ledger §5.3): `git -C ~/source/quilltap-server show c43d3b1b4 --
  <path>`. **This lane also RATIFIES the two NO-PORT? siblings** with
  their file lists in its lane record: `f3351d54f` (docs-only — the plan
  doc, a CHANGELOG entry, one line in `.claude/commands/update-
  documentation.md`) and `6d2a50382` (version files only — `README.md`,
  `package.json`, `package-lock.json`, `packages/quilltap/package.json`;
  v5's About version string is derived, not transcribed).
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 checkout
  on `main`, tree clean, `git log 6d2a50382..main` empty and
  `git log 3a76b17df..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED** (ledger §1). Every oracle/fixture regen
  runs from a lane-unique detached worktree pinned at **`c43d3b1b4`**
  (`/tmp/qt-v4-pin-p4d143-c43d3b1b4`, three symlink classes, ledger
  §5.1), through the sweep driver (`harness/tools/recipe_sweep.py --run
  <family> --v4 "$PIN" --v5w <worktree>`); the orchestrator/build-context
  families run with `TZ=UTC`. ⚠ **This pin carries ALL the round's
  sibling drift** — `a5df98b3f` (bug 114 folders), `a00e18f0d`
  (client-only) and `70505745a` (absent participants + the project
  background mode) are its ancestors. Regenerate ONLY this lane's
  families from it; where one of them also carries a sibling's arm
  (`projects_routes_equivalence` — its `backgroundDisplayMode` update
  arm moves with `70505745a`, §D), measure the shape FIRST and mask it
  only as the named `BACKGROUND_MODE_PENDING_P4D146` tripwire with a
  shape assertion + mutation proof (the P4.D141 precedent; memory note
  "a lane pin can contain a sibling lane's drift"). Families this lane
  does not move regen at the baseline if needed.
- **Sibling lanes (this round):** P4.D144 (this feature's SPA half — §A,
  §B, §H bind the two), P4.D145 (bug 114 folders), P4.D146 (absent
  participants + background mode — shares `api/projects.rs` [§D] and
  `projects_routes_equivalence`), P4.D147 (the Move-to-Project folder
  picker — owns Playwright). §Ownership / §Shared contracts are binding
  and identical across all five orders.
- **Worktree:** per the `carryout` skill. `CHANGELOG`/`status-log.md`
  append-only. **No Playwright** (P4.D147 owns port 4319); this lane is
  server-only.

## Mandate

Port the server half of `c43d3b1b4` whole: every chat-LIST payload
carries the derived pair `conciergeState` + `dangerCategories` IN PLACE
of the raw `isDangerousChat` (and `conciergeOverride` where a list
carried it) — the enriched Salon summary, the home `RecentChat`, the
character conversations rows and the project chats rows — with key order
preserved exactly; `concierge_state_uses_uncensored_route` as THE one
place naming the uncensored row with `should_use_uncensored_route`
delegating to it; the per-turn `CHAT_DANGER_CLASSIFICATION` enqueue
gated on `is_classifier_on_duty` (the real wasted-work fix — the exact
behaviour the 2026-08-27 dogfood pass saw as "completed six times in
four minutes"); and the `has-dangerous` probe v4 re-based onto the
uncensored route, which v5 never had (§H). Must NOT touch: the SPA
(P4.D144's), the single-chat GET projection (`api/salon.rs:472-480`
keeps the raw trio on purpose — v4's detail view is untouched), the
presentation strings (they have NO server consumer in v4 either — §B),
P4.D146's `api/projects.rs` background region (§D), any folders/story
file.

## Survey-verified starting points (2026-09-02, from the shipped hunks — ledger §5.3)

1. **The predicate delegation** (v4 `chat-override.ts:86-100`): NEW
   `conciergeStateUsesUncensoredRoute(state) = state === 'flagged' ||
   state === 'uncensored'` with its doc (*"the state-only twin … for
   callers that already hold a derived state (list payloads carry
   `conciergeState` rather than the raw pair) … THE one place that says
   which states take the uncensored route"*); `shouldUseUncensoredRoute`
   becomes `conciergeStateUsesUncensoredRoute(getConciergeState(chat))`;
   one header line added. v5 `services/dangerous_content/chat_override.rs`:
   `ConciergeState` enum `:63-94` (hand-rolled `as_str`/`from_wire`),
   `get_concierge_state` `:104-123`, `should_use_uncensored_route`
   `:132-135` (inlines the disjunction today), `should_show_danger_styling`
   `:143-145`, `is_classifier_on_duty` `:154-157`; the truth-table tests
   `:159-286`. `concierge_state_uses_uncensored_route` exists NOWHERE in
   v5 — net-new, and the `danger_resolver_equivalence` §1 rows (which
   already assert all three predicates per row, `:143-158`) gain its arm.
2. **The five payload sites — an IN-PLACE swap, key order otherwise
   untouched** (that property is what keeps the key-order pins green):
   - `EnrichedChatSummary` (v4 `chat-enrichment.service.ts:209-233`,
     built `:599-616`): `isDangerousChat` + `conciergeOverride` BOTH
     dropped; `conciergeState: getConciergeState(chat)` +
     `dangerCategories: chat.dangerCategories ?? []` at the same slot.
     Post-state key order: `id, title, contextSummary, createdAt,
     updatedAt, lastMessageAt, participants, tags, project,
     storyBackground, conciergeState, dangerCategories, chatType,
     scriptoriumStatus, _count, _allTagIds`. v5
     `services/chat_enrichment.rs:579-602` (the `Serialize` struct —
     declaration order IS wire order), fields `:592-593`, built
     `:735-755` (`:746-750`). Carry v4's two field docs.
   - `RecentChat` (v4 `home-data.service.ts:67-100`, `:73-74`): a
     straight pass-through from the enriched summary — NO
     `getConciergeState` call, NO `?? []` (already defaulted upstream).
     Order: `id, title, createdAt, updatedAt, lastMessageAt,
     conciergeState, dangerCategories, storyBackgroundUrl, participants,
     _count`. v5 `services/home.rs:52-70` struct, projected `:446-464`
     (field `:453`) — today it drops `conciergeOverride` at
     `map_recent_chat`; the P4.65 batching keeps the summary whole so the
     pass-through is available.
   - characters `?action=chats` (v4 `characters/[id]/handlers/get.ts:
     209-232`, `:223-224`): `conciergeState: getConciergeState(chat)`,
     `dangerCategories: chat.dangerCategories ?? []`. Order: `id, title,
     createdAt, updatedAt, lastMessageAt, character, project,
     storyBackground, messages, tags, conciergeState, dangerCategories,
     _count, scriptoriumStatus`; envelope `{chats, total}`. v5
     `api/characters.rs:482` `character_chats`, `json!` at `:686-700`
     (field `:697`, derived `:684`) — order already identical to v4's.
   - project chats (v4 `projects/[id]/actions/chats.ts:96-108`,
     `:103-104`): same pair. Order: `id, title, messageCount,
     participants, tags, storyBackground, conciergeState,
     dangerCategories, lastMessageAt, updatedAt, createdAt`; envelope
     `{chats, pagination{total,offset,limit,hasMore}}`. v5
     `api/projects.rs:666` `project_chat_list`, `json!` at `:743-754`
     (field `:750`) — **the §D chats region**.
   - `GET /api/v1/chats?action=has-dangerous` (v4 `chats/route.ts:
     898-905`): `hasDangerous = allChats.some(c =>
     shouldUseUncensoredRoute(c))` (was `c.isDangerousChat === true`;
     the `: any` cast gone) with the doc *"The toggle hides whatever
     takes the uncensored route — Flagged … and Uncensored … so the
     affordance appears on exactly that set, not on every chat carrying
     a preserved label."* **v5 has NO counterpart** (zero hits for
     `hasDangerous`/`has-dangerous` in `crates/`; the SPA records the
     omission at `quick-hide.service.ts:72-77`). Port it as §H.
   - `dangerCategories` reaches exactly ONE v5 payload today
     (`salon.rs:476`, the detail GET); adding it to four list payloads is
     net-new everywhere. `dangerousChatOverride` never existed in v5 —
     the spelling is `conciergeOverride` throughout. `conciergeState` is
     currently INPUT-only (`Request::ChatUpdate.concierge_state`,
     `api/types.rs:284-300`) — no serializer emits it; the
     `ConciergeState::as_str` wire spelling (`monitored|flagged|vouched|
     uncensored`) is what the payloads carry.
3. **The enqueue guard** (v4 `memory-trigger.service.ts:157-180`):
   immediately after the chat lookup and BEFORE the settings lookup,
   `if (!isClassifierOnDuty(chat)) return` with the comment *"Once the
   operator has spoken — Vouched Safe or Uncensored — the classifier is
   off the case, and the handler would discard the job at its own guard.
   Bail here so an Uncensored chat stops enqueueing a doomed job on every
   turn."*; the sticky `isDangerousChat === true` check after it is
   unchanged; two "Off-duty" comments rewritten. v5
   `services/message_finalizer.rs:1446-1495`
   `trigger_chat_danger_classification(db, chat_id, user_id,
   connection_profile_id, danger_mode_off)`: gate order today is
   `danger_mode_off` (`:1455`, caller-computed, BEFORE the chat read) →
   chat lookup `:1460` → sticky `:1465` → already-classified `:1469` →
   no summary `:1478` → enqueue `:1487`. **It asks no on-duty
   predicate**; the only cover is the resolver collapse, whose
   `ChatUncensored` arm yields AUTO_ROUTE (`resolver.rs:126-151`) —
   exactly v4's bug, so v5 measurably HAS it (red-first). Land the guard
   right after the chat read (`:1462`), before the sticky check.
   **Recorded shape divergence:** v5's `danger_mode_off` is computed by
   the two producers (`orchestrator.rs:3127-3134` via
   `FinalizerChatSettings.danger_mode_off`, `courier_transport.rs:
   669-692`) BEFORE the call, so v4's "bails before any settings lookup"
   assertion (`chat-danger-trigger.test.ts:139-153`, `chatSettings.
   findByUserId` not called) is a NO-COUNTERPART property — the
   observable is the enqueue itself. `is_classifier_on_duty` is already
   wired at the two OTHER sites (`danger_scan.rs:116-118`,
   `gatekeeper_job.rs:132-134`) — the job's own discard guard v4's
   comment names; leave them.
4. **Fixture blindness to close.** `harness/oracle/fixtures/
   build-home-fixture.ts` knows only `isDangerousChat` (`:77` type, `:337`
   emit) and `home-web.json:357` seeds ONE `isDangerousChat: true` — the
   home differential cannot see four states until the builder learns
   `conciergeOverride` + `dangerCategories` (exactly the widening P4.D141
   did for the story-background builder). The committed
   `home-{main,mount}.db` pair (`crates/quilltap-web/tests/fixtures/`)
   regenerates ⇒ every family reading it re-runs by name. The salon,
   characters and projects fixtures need the same check: at least one
   chat per state (`NULL/0`, `NULL/1`, `OFF/1`, `UNCENSORED/0`) with a
   non-empty `dangerCategories` on the flagged one, or the arms are
   vacuous.
5. **Key-order pins that will catch a misplaced field:**
   `home_routes_equivalence::check_key_order` (`:8-14`, against v4's
   `JSON.stringify` bytes) and `salon_reads_equivalence:324` (`list_all`).
   A field added at the wrong slot reddens them — that is the pin doing
   its job, not a normalizer to loosen.

## Tiered deliverables

- **Tier 1 (must land):** points 1–3 with their differentials, red-first
  on the enqueue guard and on each payload's derived pair over widened
  fixtures (point 4).
- **Tier 2 (should land):** §H — the `has-dangerous` probe as a dispatch
  verb + the REST edge `GET /api/v1/chats?action=has-dangerous`
  (`CHAT_GET_ACTIONS`), answering `{ hasDangerous: boolean }` over
  `should_use_uncensored_route` on every chat of the user; the NEW
  `danger_trigger_equivalence` family (v4's REAL
  `triggerChatDangerClassification` over mocked repos, the DB-free
  route-guard-oracle idiom — memory note — emitting the enqueue calls per
  case; v5 over a seeded `chats` table dumping `background_jobs`), so
  v4's trigger corpus (nine cases + the two new operator arms) is a
  differential rather than an inference from the spine dumps; the two
  NO-PORT ratifications (preamble) with file lists in the lane record.
- **Tier 3 (explicit refusal-arm deferrals):** none planned. The three
  help files bank to `p4.9i2`; `features/concierge-list-marks.md` +
  `concierge-four-state.md`'s "Resolved (4.9)" note → the `docs/v4/`
  mirror at its next refresh.

## The differential requirement (restated)

Every unit drives v4's REAL code from the `c43d3b1b4` pin:

- `danger_resolver_equivalence` (`QT_ORACLE_DANGER_RESOLVER` via
  `cases/danger-resolver.ts`, which imports the four `chat-override`
  exports at `:11-21`) — §1 rows gain `conciergeStateUsesUncensoredRoute`
  as a fourth per-row assertion, driven both directly on the state and
  through `getConciergeState(chat)` (v4's new `it.each(TABLE)`,
  `chat-override.test.ts:65-76`).
- `home_routes_equivalence` (**quilltap-web**; `QT_ORACLE_HOME`,
  `QT_FIXTURE_HOME_{MAIN,MOUNT}`; recipe `cases/home-routes.test.ts:
  29-41`) — the `RecentChat` pair over the widened fixture, key order
  byte-checked; red-first (v5 emits `isDangerousChat` today).
- `salon_reads_equivalence` (`QT_ORACLE_SALON_READS`,
  `QT_FIXTURE_SALON_{MAIN,MOUNT}`) — the enriched summary's pair + the
  `list_all` key-order pin; the single-chat GET's raw trio UNCHANGED
  (assert it stays — v4's detail view keeps it).
- The characters routes family that drives `character_chats` and
  `projects_routes_equivalence` for `project_chat_list` — the pair in v4's
  slot, envelopes unchanged. ⚠ `projects_routes_equivalence` from this
  pin also carries P4.D146's `backgroundDisplayMode` movement — §D
  tripwire discipline (measure, mask by name, mutation-prove, record the
  retirement step for the unifier).
- `message_finalizer_tier3_equivalence` and `orchestrator_tier3_
  equivalence` (`TZ=UTC`) — they diff `background_jobs` dumps, so an
  enqueue that stops happening IS visible: add (or widen) an op on an
  `UNCENSORED`-override chat with `isDangerousChat = 0` and a fresh
  context summary, proving NO `CHAT_DANGER_CLASSIFICATION` row lands;
  red-first (v5 enqueues today); a `VOUCHED` op proves the same through
  the pre-existing OFF collapse (no movement expected — say so). Grep the
  regenerated NDJSONs for the changed bytes (ledger §5.2).
- §H: the chats routes family (or `salon_reads`) gains `has-dangerous`
  arms — none / vouched-only (false) / flagged (true) / uncensored (true)
  — v4's REAL handler over the same fixture.
- Mutation proofs: revert the guard → the finalizer op reddens; swap the
  pair's slot → the key-order pins redden; drop `?? []` → the
  no-categories row reddens; inline the disjunction back into
  `should_use_uncensored_route` → the resolver arm stays green (record
  that it is a delegation, not a behaviour — the fourth assertion is
  what pins the new function).

## Fixtures

- Consumes: `home-{main,mount}.db` (+ `build-home-fixture.ts`,
  `home-web.json`), `salon-{main,mount}.db`, the characters and
  groups-projects routes fixtures, the orchestrator/finalizer tier-3
  pairs, the danger fixtures.
- Delivers: the widened home builder + regenerated committed pair; any
  widened salon/characters/projects fixture; the NEW
  `danger_trigger_equivalence` spec + case (Tier 2). Regenerating a
  committed fixture ⇒ re-run every sibling family that reads it, by
  name, zero SKIP; name them in the record.

## Shared contracts (binding, identical across all five orders)

**§A — the list-payload wire (P4.D143 ↔ P4.D144).** Every chat-LIST shape
v4 `c43d3b1b4` touches carries the derived pair **`conciergeState:
'monitored'|'flagged'|'vouched'|'uncensored'`** (the `ConciergeState::
as_str` wire spelling) and **`dangerCategories: string[]`** (always
present — `?? []`), IN PLACE of the raw field(s) it replaces, key order
otherwise untouched:
- `EnrichedChatSummary` (the `chatList` verb / `GET /api/v1/chats`):
  `isDangerousChat` AND `conciergeOverride` both DROPPED → `… project,
  storyBackground, conciergeState, dangerCategories, chatType,
  scriptoriumStatus, _count, _allTagIds`.
- `RecentChat` (the home dashboard): `isDangerousChat` DROPPED → `id,
  title, createdAt, updatedAt, lastMessageAt, conciergeState,
  dangerCategories, storyBackgroundUrl, participants, _count`.
- character conversations rows (`characters/{id}?action=chats`):
  `isDangerousChat` DROPPED → `… tags, conciergeState, dangerCategories,
  _count, scriptoriumStatus`; envelope `{chats, total}` unchanged.
- project chats rows (`projects/{id}?action=chats`): `isDangerousChat`
  DROPPED → `… storyBackground, conciergeState, dangerCategories,
  lastMessageAt, updatedAt, createdAt`; envelope `{chats, pagination}`
  unchanged.
- The single-chat GET (`ChatDetail`) KEEPS `isDangerousChat`,
  `dangerCategories`, `conciergeOverride` — v4's detail view is untouched
  (the sidebar control needs the raw pair).
D143 emits exactly these; D144 types exactly these in `core-contract.ts`
(+ `home.api.ts`'s `RecentChat`). A key present in one lane's record and
absent from the other's is a unification defect; the unifier diffs the
contract name-for-name against the Rust shapes.

**§B — the Concierge presentation table lives ONCE, in the SPA
(P4.D144).** v4's `concierge-state-presentation.ts` has no server
consumer (its readers are ChatSidebar, SalonView and ConciergeMark), so
v5 ports it as `apps/web/src/app/chat/concierge-state-presentation.ts`
only — P4.D143 builds NO Rust twin and emits NO presentation string. The
two lanes share ONE predicate name instead: `conciergeStateUsesUncensored
Route(state)` = `state === 'flagged' || state === 'uncensored'`, landed
as `concierge_state_uses_uncensored_route` in `chat_override.rs` (D143,
`should_use_uncensored_route` delegating) AND in `chat/concierge-state.ts`
(D144, `shouldUseUncensoredRoute` delegating), each pinned by its own
transcription of v4's `it.each(TABLE)`. The four `detail` sentences, the
four labels, the icon names (`eye`, `alert-triangle`, `check-circle`,
`eye-off`), the tones (`success`, `danger`, `muted`, `info`), the suffix
rule (`''`/`-muted`/`-info`, success → `''`), the text classes and the
hint sentence are byte-exact to v4's module and mechanically diffed
against it by a D144 spec.

**§H — the `has-dangerous` probe (P4.D143 ↔ P4.D144), Tier 2 in both.**
v5 never had v4's `GET /api/v1/chats?action=has-dangerous`; v4
`c43d3b1b4` re-based it onto the uncensored route. D143 lands the verb
**`chatsHasDangerous`** (`{ type: 'chatsHasDangerous' }` → `{
hasDangerous: boolean }`, true iff any chat of the user satisfies
`should_use_uncensored_route`) plus the REST edge under `CHAT_GET_
ACTIONS`; D144 consumes it as the third arm of `hasQuickHideFeatures`
and gates the footer's quick-hide section on it exactly as v4's
`sidebar-footer.tsx:145` does, behind the same ACTIVATE-AT-UNIFY
constant as its beats. Until both land, the SPA's third arm stays the
recorded omission; neither lane fakes the other's half.

**§C — the `story_background_job.rs` region split (P4.D145 ↔
P4.D146).** The legacy-folder ensure (`ensure_legacy_folder` `:1425-1449`
+ its private `find_folder_by_path` `:1451-1471`, both DELETED in favour
of the `ensure_by_path` chokepoint) belongs to P4.D145; the back-fill's
participant exclusion set (`:706-728`) belongs to P4.D146. Neither lane
edits the other's region. The unifier cherry-picks **P4.D145 before
P4.D146** and hand-resolves any import-block union. Pin awareness:
D145's pin `a5df98b3f` has `70505745a` as an ancestor, so a D145 regen of
`story_background_job_tier3_equivalence` or `title_update_tier3_
equivalence` carries D146's absent-participant predicate against a v5
that has not ported it — D145 measures the shape FIRST (the committed
story fixture seeds every participant `active`, so the predicate may not
even discriminate) and masks only as a named
`ABSENT_PARTICIPANTS_PENDING_P4D146` tripwire with a shape assertion +
mutation proof (the P4.D141 / `LAST_MESSAGE_AT_PENDING_P4D140`
precedent), retired by the unifier once both lanes sit on one branch.

**§D — `api/projects.rs` region split (P4.D143 ↔ P4.D146).** The
project-chats serializer (the `?action=chats` shape, §A) belongs to
P4.D143; the background-mode twins (`project_background_get`
`:939-1022`, the `PROJECT_UPDATE_SCHEMA` `backgroundDisplayMode` rule
`:398-425`) belong to P4.D146. Neither lane edits the other's region; the
unifier resolves import unions. **The same split applies to
`projects_routes_equivalence` (+ `cases/projects-routes.test.ts` + its
fixture):** D143 owns the project-chats arms, D146 the background-mode /
update-schema arms; each lane appends its own cases in its own region and
regenerates the oracle from ITS pin — D143's pin (`c43d3b1b4`) also
moves D146's arms, so D143 masks them by the named
`BACKGROUND_MODE_PENDING_P4D146` tripwire; D146's pin (`70505745a`)
predates D143's change, so its regen leaves the chats arms as they are.
The unifier regenerates once at the new baseline and deletes the
tripwire.

**§E — `core-contract.ts` region scoping.** P4.D144 owns the chat-list DTO
regions (§A) + the `ConciergeState` union + any `ConciergeMark` input
types; P4.D146 owns the two project `backgroundDisplayMode` unions
(`:1504`, `:3622`); P4.D147 touches the file NOT AT ALL (every verb it
needs exists); P4.D143/P4.D145 are server-only. The unifier diffs the
contract name-for-name against the Rust `CoreResponse` shapes (playbook
§D).

**§F — the restore orchestrator region split (P4.D145 ↔ P4.D146).**
`services/backup/restore/orchestrator.rs`: the §17 Folders loop
(`:855-880` — the quiet duplicate-drop arm) belongs to P4.D145; the
`project_properties` carry (`:2130-2145` — whether `backgroundDisplayMode`
is normalized on restore, measured against v4) belongs to P4.D146.
Neither lane edits the other's region; the unifier cherry-picks D145
first and resolves any import union.

**§G — the folders route wire (P4.D145 ↔ P4.D147).** P4.D145 changes the
INTERNALS of `filesFolderCreate` / the parent-ensure (the `ensure_by_path`
chokepoint) but the wire shape and statuses are UNCHANGED (201
`{folder, alreadyExists: false, message: 'Folder created successfully'}`;
200 `{folder, alreadyExists: true, message: 'Folder already exists'}`; no
409 arm). P4.D147's picker consumes `filesList` + `filesFoldersList` +
`filesFolderCreate` exactly as the library picker does today and must
not depend on any D145 change; if D147's beat sees a duplicate-path 201
carrying a pre-existing row id, that is D145's recorded semantic shift,
not a defect.

## Ownership (binding, identical across all five orders)

| Area | Owner |
|---|---|
| `quilltap-core`: NEW `services/dangerous_content/concierge_state_presentation.rs`, `services/dangerous_content/chat_override.rs`, `services/chat_enrichment.rs`, `services/home.rs`, `api/salon.rs` (the chat-list serializer), `api/characters.rs` (the conversations serializer), `api/projects.rs` §D chats region, `services/message_finalizer.rs` (the `trigger_chat_danger_classification` guard ONLY), the §H `chatsHasDangerous` verb (`api/types.rs` variant + `api/engine.rs` dispatch + the `quilltap-web` chats REST edge); harness: `home_routes_equivalence`, the salon chat-list / characters / projects routes families, the chat-override + danger-trigger families (+ their oracle cases and fixtures) | **P4.D143** |
| SPA: NEW `chat/concierge-mark.ts` (+spec), `chat/concierge-state.ts` (+spec), `quick-hide/**`, `screens/home/recent-chats-section.ts` + `recent-chat-item.ts`, `screens/salon/salon-list.ts` + `chat-card.ts`, `chat/sidebar/chat-section.ts`, `screens/prospero/cards/project-chats-section.ts`, `screens/characters/view/tabs/conversations-tab.ts` + `character-conversation-card.ts`, `chat/conversation-header.ts` (the pill reads the shared table), `styles/qt-components/_chat.css` (the `.qt-concierge-mark` family), `core-contract.ts` §E regions, the gated quick-hide/marks beats | **P4.D144** |
| `quilltap-core`: `db/folders.rs`, NEW `db/folders_unique_path_repair.rs` (or the lane's name, + `db/mod.rs` line), NEW `db/sqlite_errors.rs` (or wherever the unique-constraint predicate lands), `services/character_avatar_job.rs` (the legacy-folder site), `services/story_background_job.rs` §C folder region, `api/files.rs` (both create paths), `services/quilltap_import/files.rs`, `services/backup/restore/orchestrator.rs` §F folders region, `services/builtin_mounts.rs` / `services/provisioning/mod.rs` (the ensure call), `write_partition.rs` (the one assertion); `quilltap-host`: `host.rs` (the boot repair chain block); harness: NEW `folders_collapse_heal_equivalence`, `folders_tier2_equivalence`, `files_routes_equivalence`, `qtap_import_equivalence`, the `system_restore_*` folder arms + the NEW duplicate-folders archive, the avatar/story tier-3 legacy-folder arms, `provisioning_equivalence` (run, not changed); `docs/v4/` mirror rows for `DDL.md` | **P4.D145** |
| `quilltap-core`: `chat_predicates.rs` (the str→status helper), `services/image_profile_resolution.rs`, `services/story_background_job.rs` §C participant region, `services/title_update_job.rs`, `api/chat_media.rs`, `db/projects.rs`, `api/projects.rs` §D background region, `services/backup/restore/orchestrator.rs` §F project-properties region; harness: `story_background_job_tier3_equivalence` + its builder, `title_update_tier3_equivalence` + the `cost-background` pair, `cost_background_routes_equivalence`, `projects_routes_equivalence`, `projects_tier2_equivalence`, the retired-mode restore archive if built; SPA: `screens/prospero/cards/project-image-generation-card.ts` (+spec), `core-contract.ts` §E region, the gated two-option beat | **P4.D146** |
| SPA: `screens/files/move-to-project-dialog.ts` (+spec), NEW `screens/files/folder-picker.ts` (+spec), the LIVE Move-to-Project beat | **P4.D147** |
| Playwright (port 4319) | **P4.D147's branch only**; every other lane's beats are authored gated (ACTIVATE-AT-UNIFY) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it |

**Version bumps:** this lane bumps `quilltap-core`, `quilltap-harness`,
and `quilltap-web` (the home family + the §H edge) per commit; the
unifier recounts at unification.

## Verification gate before declaring done

`cargo fmt --check`; `cargo clippy` both feature sets; full
`cargo test --workspace` with the lane's oracle env vars
(`run_in_background`, full log + sentinel — NEVER a pgrep loop, NEVER
`| tail`); the lane's families by name through the sweep driver, zero
SKIP, from the `c43d3b1b4` pin with changed-bytes greps
(`"conciergeState"`, `"dangerCategories"`, the absent
`CHAT_DANGER_CLASSIFICATION` row, `"hasDangerous"`); mutation proofs
verified-applied and reverted by file backup; `provisioning_equivalence`
run once at the pin to prove the sibling folders commit did not move the
fresh surface (it should not — record the result). No SPA gate (no SPA
file touched — say so). Per-commit rule: `.claude/commands/commit.md`.
