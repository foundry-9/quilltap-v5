# Work order P4.D212 — bug 161, v4 `e7821606f`: the fold transcript names its speakers — ONE `speaker_names` resolver shared by the context-summary fold and the fold-episode pass (v5 reproduces v4's defect VERBATIM: `role.to_uppercase()` under a prompt whose last line orders the model to use names), `FOLD_SUMMARY_PROMPT`'s new last sentence, the `[Context Summary] Resolved speaker names for fold` debug line, and the PORT-NEW `?action=rebuild-summary` verb (`Request::ChatRebuildSummary`) with v4's refusal order, its single clearing update, its enqueue, its realtime publish and its two log lines; `help/chats.md` re-vendored; the `4e1a8e061` docs commit ratified with its mirrors — from `main`

**Status: UNIFIED on main at the `a2db63da7` round (2026-09-23) — CLOSED WHOLE (Tier 3 items 10–11 stand as deferred by name).** The §3 review (one reader + the unifier's own reads) verified the resolver, the fold render, the regenerated prompt (1,585 bytes identical to v4's template literal), the verb's refusal order and single update, the fences, the census, the help byte-copy and the §S.1 contract name-for-name — NO blocking finding. **Fixed at unification:** finding 1's handoff — `orchestrator_tier3`'s `lastTurnParticipantId` CONVERGENCE pin was still armed on the union (P4.106 owned the file and did not retire it), so the family would have panicked at the new baseline; retired, the field now compares raw. **Also fixed at unification:** finding 3's `courier_images_routes` red — the raw compressed read was on BOTH sides (the harness dump AND the oracle case, which serialized a Node Buffer); both now go through `qt_text()`, green. **Recorded, not fixed:** finding 2's `chat_regenerate_title_tier3` red is the un-widened `chat-admin-main.db` (the heal's next list). _Lane close:_ **LANE CLOSED 2026-09-22 on `claude/speaker-names-rebuild-summary-269bbd` — Tier 1 and Tier 2 LANDED WHOLE; Tier 3 items 10–11 deferred as ordered (no REST arm, recorded in the verb's doc comment; the broken-vault swallow pinned by a v5 unit test over a table-less connection). Awaiting unification.** Measured against the order: (a) moving the resolution after the empty-turns return is UNOBSERVABLE (that return is dead on both sides), so no order leg can exist; (b) dropping `!n.is_empty()` alone does NOT redden `fold_episode_tier3` (v4's `speakerLabel` also falls through on an empty name), and the empty-name line had to live in `episode_pass`, not a no-episode run; (c) v4 ACCEPTS an empty character name on create; (d) `orchestrator_tier3`'s `lastTurnParticipantId` convergence pin TRIPS at `a2db63da7` (v4's own row moved), which is P4.106's to retire; (e) the `chat-admin-*` pair is fixture-vintage (not among P4.103's six) and `chat_regenerate_title_tier3` is red on it; (f) `courier_images_routes` is red on clean `main` (a raw read of a compressed `llm_logs.request`). Lane record: `status-log.md`.

## Preamble

- **v4 oracle baseline: `f45a517a9`** (drift-ledger §1). This lane absorbs
  the ledger's `e7821606f` row ("fix(summary): name the speakers in the fold
  transcript (bug 161)", 2026-09-21 22:07, `4.10.0-dev.62`, 27 files,
  +991/−48 — `ORDERED(P4.D212 ∥ P4.D213)`) and ratifies the `4e1a8e061`
  row ("docs: plan the summarizer name fix, file bugs 161 and 162",
  docs-only — `ORDERED(P4.D212)`, NO-PORT? with a mirror). Spec reads:
  `git -C ~/source/quilltap-server show e7821606f -- <path>` for the hunks
  and `git show e7821606f:<path>` for the post-commit file — never the
  commit message (§R.4).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** This lane pins the TARGET
  **`/tmp/qt-v4-pin-p4d212-a2db63da7`** for every family its port MOVES
  (`context_summary_service_tier3_equivalence`,
  `fold_episode_tier3_equivalence`, `orchestrator_tier3_equivalence`,
  `memory_pipeline_jobs_tier3_equivalence`, `courier_images_routes_
  equivalence`, the NEW `speaker_names_equivalence`, the NEW
  `chat_rebuild_summary_equivalence`, `chat_admin_routes_equivalence`,
  `help_tree_equivalence`) and the BASELINE
  **`/tmp/qt-v4-pin-p4d212-f45a517a9`** for the neutrality legs
  (`chat_regenerate_title_tier3_equivalence`, `chat_tasks_equivalence`,
  `context_summary_equivalence`, `danger_trigger_equivalence`,
  `salon_reads_equivalence`, `salon_mutations_equivalence`). Pin marker:
  the target-pinned `help_tree_equivalence` NDJSON carries `Rebuilding the
  Running Summary` (`help/chats.md` at `e7821606f`); the baseline-pinned
  one does not.
- **Sibling lanes:** P4.D213 (the SPA half — consumes §S.1; owns
  `apps/web/**` whole), P4.D214 (bug 162 — `crates/quilltap-cli/**`),
  P4.103 (the fixture-vintage heal — owns the six committed pairs; touch
  none), P4.104 (the image seam), P4.105 (`update_message`), P4.106 (the
  Inform coverage remainder — owns `orchestrator_tier3`'s FILES; you only
  RE-RUN that family, §R.10(d)).
- **Worktree:** `git worktree add <path> -b claude/p4-d212-speaker-names-
  <hash> main`; `CARGO_INCREMENTAL=0`.
- **Read before the work:** the ledger's `e7821606f` row whole; `crates/
  quilltap-core/src/services/context_summary.rs:489-520` (`turns_to_chat_
  messages` — `role.to_lowercase()`, no speaker), `:562` (`generate_context_
  summary_with_seams`), `:615-665` (the chat row, `rows_by_id`, the fold
  range, `new_turns_content`); `services/context_summary/tasks.rs:67-100`
  (`fold_chat_summary` renders `format!("{stamp}{}: {}", m.role.to_
  uppercase(), m.content)` at `:82`); `services/context_summary/prompt_
  text.rs:1-8` (GENERATED — "no byte transcribed by hand … re-running the
  session extraction against the v4 checkout"), `:20` (the last paragraph
  ending `Use character names, not roles.`); `services/fold_episode_pass.
  rs:107-118` (the chat read + `participants`), `:150-190` (the duplicate
  seat-name loop v4 deleted — note `:161-166` pushes the name whenever the
  `name` field is PRESENT, an empty string included); `services/chat_admin.
  rs:716-800` (`chat_regenerate_title` — the profile choice at `:750-767`
  is the one rebuild-summary copies); `services/queue_service.rs:1172`
  (`enqueue_context_summary(db, user_id, chat_id, connection_profile_id,
  force_regenerate, priority)`); `api/chat_informs.rs:559-563` (the
  `publish_realtime(RealtimeTopic::Chats, Some(chat_id))` precedent);
  `db/chats.rs:494-500` (`ChatUpdate.context_summary: Option<Option<
  String>>`) + `:570-580` (the fold-cursor columns); `api/types.rs:3134-
  3139` (`Request::ChatRegenerateTitle { chat_id }`) + `:4402-4412`
  (`Response::ChatAdmin(Value)` — the body-is-what-§1-freezes rule);
  `api/engine.rs:1462-1465` (the arm); `crates/quilltap-web/src/wardrobe_
  routes.rs:381-460` (the REST edge serves ONLY equip / regenerate-avatar /
  inform / cancel-inform — every other chat action rides `POST /api/
  dispatch`; `regenerate-title` itself is dispatch-only); `crates/quilltap-
  web/tests/dispatch_wrong_type_census.rs:2586-2588` (445); memory notes
  `js-nullish-chain-is-or-else-not-filter`, `capture-layer-target-assert-
  is-a-prefix-match`, `tracing-percent-field-renders-unquoted`,
  `jest-oracle-instanceof-across-resetmodules`, `two-homes-identical-
  bodies-different-helpers`.

## Mandate

Make the fold transcript name its speakers exactly as v4 now does, from ONE
resolver, and give the operator v4's remedy for a summary that is already
wrong. Deliver: NEW `services/speaker_names.rs` (v4 `lib/chat/speaker-
names.ts` — `resolve_speaker_names` over EVERY participant, raw character
reads, the `if (character?.name)` gate, per-seat swallow; `speaker_label`
with the `USER` → `User` / else `Character` fallback); the fold-episode pass
repointed onto it with its private loop DELETED; the fold task's turn type
carrying a REQUIRED `speaker` rendered as `{stamp}{speaker}: {content}`;
`FOLD_SUMMARY_PROMPT` regenerated mechanically with v4's new last sentence;
`generate_context_summary` resolving names BEFORE the empty-turns return and
logging the debug line with v4's four fields; `Request::ChatRebuildSummary`
+ `services::chat_admin::chat_rebuild_summary` (404 → 409 running room → 400
no profiles → the ONE update → enqueue → publish → info log → `{success,
jobId}`; the catch arm's error log + 500 sentence); the census row;
`help/chats.md`; the mirrors pre-listed. Must NOT touch: `apps/web/**`
(P4.D213), `crates/quilltap-cli/**` (P4.D214), any committed fixture pair
(P4.103), `orchestrator_tier3`'s files (P4.106 — re-run only), the drift
ledger.

## Survey-verified starting points (fresh survey 2026-09-22 at `a2db63da7` and on `main` `fc63905e` — do not re-derive)

### v4 (post-commit files at `e7821606f`; line numbers are the post-commit file's)

1. **`lib/chat/speaker-names.ts` (NEW, 71 lines).** `export type
   SpeakerNames = ReadonlyMap<string, string>` (`:25` — participantId →
   display name; "Seats with no resolvable name are absent"); `export type
   SpeakerNamesChat = Pick<ChatMetadata, 'participants'>` (`:28`).
   `resolveSpeakerNames(chat)` (`:42-57`):
   ```ts
   const names = new Map<string, string>()
   const repos = getRepositories()
   for (const p of chat.participants) {
     if (!p.characterId || names.has(p.id)) continue
     try {
       const character = await repos.characters.findByIdRaw(p.characterId)
       if (character?.name) names.set(p.id, character.name)
     } catch {
       // Name stays role-labelled. A broken vault costs a label, not a fold.
     }
   }
   return names
   ```
   — ALL participants, NO status filter (removed and silent seats included);
   the persona is an ordinary `CHARACTER` seat; dedup by `p.id` (two seats
   on one `characterId` = two reads); reads SEQUENTIAL; `findByIdRaw` (the
   DB row, never the vault); the swallow is per seat and silent.
   `speakerLabel(m, names)` (`:64-71`): `names.get(m.participantId)` when
   the id is set AND the resolved value is truthy (an EMPTY name falls
   through), else `m.role === 'USER' ? 'User' : 'Character'` — every
   non-`USER` role (ASSISTANT, SYSTEM, TOOL, …) is `Character`.
2. **`lib/chat/context-summary.ts`.** `:23` the import; `turnsToChatMessages
   (turns, names)` (`:292-316`) pushes `{ speaker: speakerLabel(m, names),
   role: m.role.toLowerCase(), content: m.content, createdAt: m.createdAt
   ?? null }` — KEY ORDER `speaker, role, content, createdAt`. Inside
   `generateContextSummary` (`:396-407`), right after `turnsToFold` is
   sliced and BEFORE the `newTurnsContent.length === 0` early return:
   ```ts
   const speakerNames = await resolveSpeakerNames(chat)
   const unresolvedParticipantIds = chat.participants
     .filter(p => !speakerNames.has(p.id))
     .map(p => p.id)
   logger.debug('[Context Summary] Resolved speaker names for fold', {
     chatId,
     seatCount: chat.participants.length,
     resolvedCount: speakerNames.size,
     unresolvedParticipantIds,
   })
   const newTurnsContent = turnsToChatMessages(turnsToFold, speakerNames)
   ```
   — DEBUG level; `seatCount` counts EVERY participant (seats without a
   `characterId` included); `unresolvedParticipantIds` is every id absent
   from the map (so it includes the no-`characterId` seats); the resolution
   and the log run even when the empty-turns return then fires. `chat` is
   `repos.chats.findById(chatId)` (`:337`). Nothing else in the file moved.
3. **`lib/memory/cheap-llm-tasks/chat-tasks.ts`.** `FOLD_SUMMARY_PROMPT`
   (`:591-601`): the ONLY change is the last paragraph's third sentence —
   OLD `Use character names, not roles.` → NEW `Refer to each speaker by
   the name on their turns. If a turn is labelled only by a role, keep
   that label; never invent a name for anyone.` (the paragraph otherwise
   byte-identical: `Rewrite the five sections in plain prose (the Timeline
   as its dated list). Be concise. Don't transcribe — synthesize. … Output
   only the five sections under their labels; no preamble, no closing
   remarks.`). `FoldSummaryInput.newTurns` (`:618`) becomes
   `Array<ChatMessage & { speaker: string; createdAt?: string | null }>`
   — `role` STAYS on the shape ("for callers that want it, but a bare LLM
   role never reaches the model"). The render (`:634-637`): `const stamp =
   m.createdAt ? `[${m.createdAt.slice(0, 10)}] ` : ''` then
   `${stamp}${m.speaker}: ${m.content}`, still joined by `'\n\n'`.
4. **`lib/memory/fold-episode-pass.ts`.** `:28` the import; the private
   loop (`new Map` → `for p of chat.participants` → `findByIdRaw` → `if
   (character) speakerNames.set(p.id, character.name)`) DELETED and the
   inline `?? (m.role === 'USER' ? 'User' : 'Character')` fallback with it;
   NEW `:90-95` `const speakerNames = await resolveSpeakerNames(chat)` …
   `speaker: speakerLabel(m, speakerNames)`. ⚠ **The one behaviour delta
   on this side:** the old gate was `if (character)`, the shared one is
   `if (character?.name)` — a character row whose `name` is the EMPTY
   STRING used to produce an empty label (`""` is not nullish, so `??`
   never fell through) and now produces the role fallback. v5's loop at
   `fold_episode_pass.rs:161-166` has the SAME pre-fix shape (`if let
   Some(name) = str_field(&character, "name")` pushes `""`, and the
   `.map(|(_, n)| n.clone())` at `:177` yields `Some("")`, so `unwrap_or_
   else` never runs) — **v5 matches PRE-fix v4 on an empty-name character.
   That is the red-first arm for the episode side** (§4 below).
5. **`app/api/v1/chats/[id]/actions/rebuild-summary.ts` (NEW, 94 lines).**
   `handleRebuildSummary(chatId, chat, { user, repos })` — whole body in a
   `try`. Refusals IN ORDER: (a) the 404 is NOT here — `handlePost`
   answers `notFound('Chat')` (`{ "error": "Chat not found" }`) before any
   action runs; (b) `:38-40` `if (chat.chatType === 'autonomous' &&
   chat.runState === 'running') return conflict('Pause the room before
   rebuilding its summary.')` — 409; a PAUSED room passes; (c) `:42-45`
   `availableProfiles = await repos.connections.findByUserId(user.id)`,
   empty → `badRequest('No connection profiles available')` — 400. ⚠ It
   does NOT check the cheap-LLM settings (regenerate-title does, `title.
   ts:25-27` — v5's `chat_admin.rs:731-739`; DO NOT carry that arm over).
   Profile choice (`:50-54`, the same RESULT as `title.ts:35-43`): the
   first `CHARACTER` participant's `connectionProfileId` when it names an
   available profile, else `availableProfiles[0]`. The update (`:64-69`,
   ONE call):
   ```ts
   await repos.chats.update(chatId, {
     contextSummary: null,
     summaryAnchorMessageIds: [],
     lastSummaryTurn: 0,
     updatedAt: new Date().toISOString(),
   });
   ```
   — `lastFullRebuildTurn` deliberately UNTOUCHED (`API.md`: zeroing it
   would route any chat past turn 50 into the single-shot `forceRegenerate`
   path). Enqueue (`:71-75`): `enqueueContextSummary(user.id, { chatId,
   connectionProfileId: connectionProfile.id, forceRegenerate: false })` —
   NO options object, so NO explicit priority (measure v4's
   `enqueueJob` default and pass v5's `enqueue_context_summary` the SAME
   number; the danger scan passes `-2`, this caller must not); job type
   `CONTEXT_SUMMARY`; no dedupe. `:77` `publishRealtime('chats', chatId)`.
   `:79-83` `logger.info('[Chats v1] Context summary cleared for rebuild',
   { chatId, jobId, connectionProfileId: connectionProfile.id })`. `:85`
   `NextResponse.json({ success: true, jobId })` — 200, key order
   `success, jobId`. Catch (`:87-92`): `logger.error('[Chats v1] Failed to
   rebuild context summary', { chatId }, error)` then `serverError('Failed
   to rebuild the summary')` — 500; when the update throws, the enqueue
   and the publish never run.
6. **Registration.** `actions/index.ts:9` `export { handleRebuildSummary }
   from './rebuild-summary'`; `handlers/post.ts` `CHAT_POST_ACTIONS` gains
   `'rebuild-summary'` as its SECOND entry (`:63`, between
   `'regenerate-title'` and `'add-tag'` — the position is visible in v4's
   `Unknown action: X. Available actions: …` sentence; v5's REST edge
   does not render that list, see §v5 item 7) and the arm `:136`
   `'rebuild-summary': () => handleRebuildSummary(chatId, chat, ctx)`.
7. **`docs/developer/API.md`** (`:2273`, after regenerate-title, before
   add-tag): the new `#### POST /api/v1/chats/[id]?action=rebuild-summary`
   section — quote it into the verb's doc comment (its `lastFullRebuild
   Turn` paragraph is the why-comment the port must carry). **`help/chats.
   md`** (`:378-400`, `### Rebuilding the Running Summary`, four
   paragraphs, between the Merge section and `## Advanced Features`):
   38,039 → **39,682 bytes** — BYTE-COPY at the target pin.
8. **v4's tests, as differential arms** (jest names; the port mirrors
   them, never transcribes the files): `speaker-names.test.ts` —
   `resolves both seats of a two-seat chat, persona included`, `resolves a
   seat that has since been removed from the chat`, `resolves a silent
   seat`, `skips a seat with no characterId without reading anything`,
   `leaves a seat unnamed when the character row is gone`, `falls back
   without throwing when a read blows up on a broken vault`, `reads each
   seat once`; `speakerLabel`: `uses the resolved name when the seat is
   known`, `falls back to User for an unresolvable USER turn` (unknown id
   AND `null` participantId), `falls back to Character for everything
   else`, `never returns a bare LLM role`. `context-summary-speaker-names.
   test.ts` (mocks the factory, the cheap-LLM, the tasks, the pass — a
   pure-shape test): `labels every line of a two-seat chat with its
   character name`, `never hands the fold a bare LLM role as a speaker`,
   `falls back to a role label when a line has no seat to resolve`
   (assistant `participantId: null` → `Character`), `names a seat that
   has since been removed from the chat`. `fold-chat-summary.test.ts`:
   `renders an unresolvable seat by its role fallback label, not its
   role` (`User: who is there` / `Character: nobody`), `tells the model to
   keep a role label rather than invent a name (bug 161)`. `rebuild-
   summary.test.ts` (mocks `enqueueContextSummary` + `publishRealtime`):
   `clears the summary, its anchors and the fold cursor in one update`,
   `leaves lastFullRebuildTurn alone so the rebuild stays on the bounded
   fold path`, `enqueues exactly one summary job, and not the single-shot
   path`, `falls back to the first profile when the cast has none of its
   own`, `publishes the chats topic so the summary panel re-reads`,
   `refuses an autonomous room that is currently running`, `allows a
   paused autonomous room`, `refuses when there is no connection profile
   to source a summariser from`, `does not enqueue when the clearing
   update fails`.
9. **The docs commit `4e1a8e061`** (six files, none under `lib/`, `app/`,
   `components/`, `packages/`, `help/`, `public/schemas/`, `migrations/`):
   NO-PORT-RATIFIED with a mirror at the FINAL paths `e7821606f` leaves
   them at — `docs/developer/features/complete/context-summary-speaker-
   names.md` (9,712 bytes at `a2db63da7`; status line `**complete** —
   landed in v4.10-dev on 2026-09-21`; carries a paragraph on the
   `useSummaryActions` deviation) and `docs/developer/bugs/fixed/bug-161-
   summary-invents-a-name.md` (6,100). The bug-162 file is P4.D214's
   mirror. `docs/developer/bugs.md` (302,517 → 306,475 at `a2db63da7` —
   carries BOTH bugs' rows; the unifier mirrors it ONCE) and `docs/
   developer/API.md` (219,745 → 221,285) — pre-list all four in the lane
   record with byte counts (§R.9).

### v5 (on `main` `fc63905e`)

1. `services/context_summary.rs:489-520` `turns_to_chat_messages(turns,
   rows_by_id) -> Vec<ChatMessage>` — `{ role: to_lowercase, content,
   created_at }`; NO participant id is read from the row. `:615-665` the
   chat row is already in hand (`chat`), `rows_by_id` carries the whole
   message row (so `participantId` is available without another read),
   and the empty-turns return sits at `:664`. **The v5 call order must
   match v4's:** resolve + log BEFORE that return.
2. `services/context_summary/tasks.rs:53-100` `fold_chat_summary(executor,
   completion, prior_summary, new_turns: &[ChatMessage], selection)` —
   `ChatMessage { role, content, created_at }` is SHARED with the title
   tasks (`chat_admin.rs:790-800` builds it for `titleChat`). v4 keeps
   `ChatMessage` unchanged and types the fold input as an intersection;
   the port's equivalent is a fold-only turn type (`FoldTurn { speaker:
   String, role: String, content: String, created_at: Option<String> }`
   — `speaker` REQUIRED, not `Option`) so the title path cannot pass a
   turn without one and the fold cannot render a role. Say which shape
   you chose and why in the lane record.
3. `services/context_summary/prompt_text.rs` is GENERATED (`:1-8`). Do
   NOT hand-edit the sentence: re-run the extraction against the target
   pin (the header says how) and diff — the ONLY byte delta must be the
   one sentence in `FOLD_SUMMARY_PROMPT`; the four title prompts unchanged
   (`chat_tasks_equivalence`/`chat_regenerate_title_tier3` are the
   neutrality proof).
4. `services/fold_episode_pass.rs:107-118` reads the chat and its
   `participants`; `:150-190` is the duplicate loop (present-character
   filter at `:121-130` is UNRELATED — the episode set — keep it).
   Callers: `context_summary.rs` (the fold-side pass) and
   `context_summary_job.rs`.
5. `services/chat_admin.rs:716` `chat_regenerate_title` — the profile
   choice `:750-767` and the `Response::ChatAdmin(json!({success, title}))`
   answer; `load_chat`/`not_found("Chat")`/`bad_request`/`internal` are
   the file's helpers. The rebuild verb is a sibling in this file. ⚠ Two
   arms of regenerate-title are NOT rebuild-summary's: the cheap-LLM-
   settings 400 (`:731-739`) and the empty-transcript 400 (`:783-786`).
   409 needs a `conflict(...)` helper — grep the file/crate for the
   existing `Response::Error { kind: "conflict" …}` idiom (P4.D205's
   `Pause the room…`-class refusals) before adding one.
6. `services/queue_service.rs:1172` `enqueue_context_summary(db, user_id,
   chat_id, connection_profile_id, force_regenerate, priority: f64)`.
   Measure v4's `enqueueJob` default priority for a call with no options
   (`queue-service.ts:785-791` and the enqueue helper it calls) and pass
   that number — the corpus diff on `background_jobs.priority` is the
   pin.
7. **The REST edge.** `crates/quilltap-web/src/wardrobe_routes.rs:381-460`
   `chat_action_post` serves equip / regenerate-avatar / inform /
   cancel-inform and answers every other action with the loud pointer at
   `:451-457` ("…the other chat actions ride POST /api/dispatch"). The
   regenerate-title precedent is DISPATCH-ONLY; rebuild-summary follows
   it — NO new REST arm, the pointer sentence unchanged. Record the
   decision in the verb's doc comment.
8. `api/types.rs:3134-3139` the `ChatRegenerateTitle { chat_id }` shape
   (`rename_all = "camelCase"`); `:4402-4412` `Response::ChatAdmin(Value)`
   — the rebuild verb answers through the SAME variant (`{success: true,
   jobId}`), so NO new `Response` variant and NO `unwrap_to_http` change
   (the `a-rest-unwrapper-500s-on-every-new-variant` lesson does not bite
   here — say so). `api/engine.rs:1462` the dispatch arm precedent; the
   new arm needs the writer `Db` + the realtime bus only (no driver — the
   enqueue is a DB write; the JOB runs on the host's pump, exactly as the
   danger scan's enqueue does).
9. `crates/quilltap-web/tests/dispatch_wrong_type_census.rs:2588`
   `EXCLUDED_BY_THE_ROUTE_IDENTIFIER_RULE = 445` → **446** (`chat_id`
   is a `*_id` field); recount in the comment.
10. The realtime publish: `crate::realtime::bus::publish_realtime(
    RealtimeTopic::Chats, Some(chat_id))` (`api/chat_informs.rs:559-563`).
11. `help/chats.md` on main: 38,039 bytes (the `help_tree_embed_guard`
    count stays 126 — a byte-copy moves no count).
12. Fixtures the moving families use: `context-summary-service-tier3.json`
    (1,541 lines; six chats, ONE `CHARACTER` seat each on `aa…0001`…
    `aa…0006`; only `aa…0001` has a vault; **NO message carries a
    `participantId`** — so at the target EVERY fold line renders `User:` /
    `Character:` and the NAME arm is unexercised until the corpus grows,
    §4); `fold-episode-tier3.json` (characters `Aria`/`Bram`/`Cass`;
    seats `active`/`silent`/`absent`; messages with `participantId`
    `null`, a resolving id, a second resolving id and a DANGLING id
    `9z…z9` — the fallback arm exists; NO empty-name character).

## Tiered deliverables

### Tier 1 — must land

1. **NEW `crates/quilltap-core/src/services/speaker_names.rs`** — v4
   `lib/chat/speaker-names.ts` line for line: `pub fn resolve_speaker_
   names(conn, participants: &[Value]) -> Vec<(String, String)>` (or a
   `BTreeMap` — the ORDER of v4's `Map` is insertion order, which
   `unresolvedParticipantIds` does not depend on but `resolvedCount` and
   any future iteration does; keep insertion order) over EVERY
   participant, `characterId` present-and-non-empty AND `p.id` not yet
   seen, `characters_read::find_by_id_raw`, `if let Some(name) …
   .filter(|n| !n.is_empty())` (the `?.name` truthiness gate — an empty
   name is ABSENT), a per-seat `Err` swallowed with v4's comment carried;
   `pub fn speaker_label(participant_id: Option<&str>, role: &str, names)
   -> String` with the `USER` → `User`, else `Character` fallback and the
   empty-resolved-name fall-through. Carry v4's module doc ("the one
   place a participantId becomes a display name").
2. **The fold-episode pass repointed** (`fold_episode_pass.rs:150-190`
   DELETED in favour of the shared resolver; the rendered `speaker` via
   `speaker_label`). Red-first on the NEW empty-name arm (§4).
3. **The fold task carries `speaker`** (`tasks.rs`): the fold-only turn
   type with a REQUIRED `speaker`; `fold_chat_summary` renders `{stamp}
   {speaker}: {content}`; `FOLD_SUMMARY_PROMPT` REGENERATED (item v5-3).
   `turns_to_chat_messages(turns, rows_by_id, names)` reads
   `participantId` off the row and labels it; key order in any JSON-
   rendered form `speaker, role, content, createdAt`.
4. **`generate_context_summary_with_seams`** resolves the names from the
   chat row it already holds, computes `unresolvedParticipantIds` as v4
   does (every seat NOT in the map — no-`characterId` seats included),
   and emits `tracing::debug!(chat_id = %…, seat_count = …, resolved_
   count = …, unresolved_participant_ids = ?…, "[Context Summary]
   Resolved speaker names for fold")` — the array on the tree's JSON
   `…Json` file-layer convention (P4.91's `fileIds` precedent — the
   thread-scoped capture sees the RAW field), BEFORE the empty-turns
   return; capture-pinned with a silence leg AND an order leg (resolution
   happens even when the fold then returns `No content in turns to
   fold`).
5. **`Request::ChatRebuildSummary { chat_id }`** inside a `// === P4.D212
   ===` fence at the END of the `Request` enum + the engine arm at the END
   of the dispatch match; `services::chat_admin::chat_rebuild_summary(db,
   user_id, chat_id, now_iso) -> Response`: `load_chat` → `not_found(
   "Chat")`; `chatType == "autonomous" && runState == "running"` → the
   409 with v4's sentence `Pause the room before rebuilding its summary.`;
   `connection_profiles::find_by_user_id` empty → `bad_request("No
   connection profiles available")`; the profile choice as `:750-767`;
   ONE `ChatUpdate { context_summary: Some(None), summary_anchor_message_
   ids: Some(vec![]), last_summary_turn: Some(0.0), updated_at: Some(now)
   }` (whatever the struct's spellings are — `lastFullRebuildTurn`
   UNTOUCHED, with v4's why-comment from `API.md`); `enqueue_context_
   summary(…, false, <v4's default priority>)`; `publish_realtime(Chats,
   Some(chat_id))`; `tracing::info!(chat_id, job_id, connection_profile_
   id, "[Chats v1] Context summary cleared for rebuild")` (capture-pinned
   + silence); `Response::ChatAdmin(json!({"success": true, "jobId":
   job_id}))`. The catch arm: any `Err` after the refusals → `tracing::
   error!(chat_id, error = %e, "[Chats v1] Failed to rebuild context
   summary")` + `internal`-class 500 whose message is v4's `Failed to
   rebuild the summary` (measure the tree's `serverError` twin — the
   status-and-message shape `chat_admin_routes_equivalence` already pins
   for sibling verbs); an update failure must leave NO job row and fire
   NO publish (a mutation proof, §4).
6. **The census row** (`dispatch_wrong_type_census.rs` 445 → 446 with the
   arithmetic) and the NEW **dispatch WIRE test** `crates/quilltap-web/
   tests/chat_rebuild_summary_dispatch_wire.rs` through `POST /api/
   dispatch` (the `message_swipe_stream_dispatch_wire.rs` shape): the
   happy body's two keys, the 409 on a running room, the 400, the 404 —
   and a wrong-type `chatId` (the census's own contract).
7. **`help/chats.md`** byte-copied at the target pin (39,682); the count
   literals UNTOUCHED (126).

### Tier 2 — should land

8. `chat_admin_routes_equivalence` re-recorded at the target and grown
   with the rebuild verb's REST-side rows through v4's REAL route (the
   404 / 409 / 400 / 200 bodies) — v5's side dispatching the verb
   directly (the family's existing convention for dispatch-only verbs).
9. The `4e1a8e061` ratification: the lane record's mirror pre-list with
   byte counts at `a2db63da7` for the four paths in v4 item 9 (the
   unifier mirrors; a lane never writes `docs/v4/`).

### Tier 3 — explicit deferrals (loud, typed, never silent)

10. **No REST `?action=rebuild-summary` arm** (dispatch-only, the
    regenerate-title precedent) — recorded in the verb's doc comment and
    the lane record, NOT stubbed; the REST edge's pointer sentence already
    names the rule.
11. The "broken vault" swallow arm cannot be planted in a real-DB oracle
    (`findByIdRaw` reads the row, not the vault — the only throw is a DB
    error); pin it as a unit test over a poisoned connection on the v5
    side and record that the v4 unit test's mock is the only v4-side
    evidence.

## The differential requirement, restated

- **NEW `speaker_names_equivalence` (tier 2, a tsx case importing v4's REAL
  `resolveSpeakerNames` + `speakerLabel` over a fixture built by v4's real
  repos at the pin):** seats — two named seats (one the persona), a
  `removed` seat, a `silent` seat, a seat with NO `characterId`, a seat
  whose `characterId` names NO row, an EMPTY-NAME character, two seats on
  ONE character; labels — a resolving id, an unknown id with role `USER`,
  `null` with role `USER`, `null` with role `ASSISTANT`, an unknown id with
  role `SYSTEM`/`TOOL`. Diff the map as an ordered pair list + every
  label. The jest `findByIdRaw` call COUNT (`reads each seat once`) is a
  v4-side assertion the case records into the NDJSON and the v5 side
  matches with a counting connection wrapper or a `read_main` counter.
- **`context_summary_service_tier3` at the target, RED-FIRST** on
  `USER:`/`ASSISTANT:` → `User:`/`Character:` across every fold op — then
  GROWN: at least one op whose ASSISTANT turns carry the seat's
  `participantId` (resolving to `aa…0001`'s name), one with a seat
  REMOVED from `participants` after its messages were written (the
  `names a seat that has since been removed` arm needs the seat still
  LISTED with status removed — v4 resolves over all participants; a seat
  DELETED from the list is the `null`/unknown arm), one with a dangling
  `characterId`. The prompt sentence is part of the diff (the SYSTEM
  prompt is compared verbatim — the family's own rule). The debug line
  capture-pinned on the v5 side per op.
- **`fold_episode_tier3` at the target:** predicted NEUTRAL on the
  existing corpus (measure: `cmp` at both pins where the family is
  deterministic — it mints, so compare by case) and RED-FIRST on the
  grown empty-name arm (add a fourth character with `name: ""` and a seat
  on it: pre-port v5 renders `: content`, v4 post-fix renders
  `Character: content`).
- **`orchestrator_tier3` and `memory_pipeline_jobs_tier3` RE-RUN at the
  target** (both reach the REAL fold — the orchestrator oracle
  `requireActual`s the cheap-llm tasks at `:546`; the jobs oracle runs
  `generateContextSummary` with its real handler at `:35`): predicted to
  MOVE on every case whose fold fires (the prompt sentence + the labels).
  You do not edit their files (P4.106 owns `orchestrator_tier3`'s); you
  regenerate at the target, run, and record — a red that is NOT the
  predicted movement is a finding. `courier_images_routes` routes the
  fold through a canned provider (`:168`) — same rule.
- **Neutrality legs at the baseline pin:** `chat_regenerate_title_tier3`
  (the title prompts unmoved; the shared `ChatMessage` untouched),
  `chat_tasks_equivalence`, `context_summary_equivalence` (the pure
  partition family), `danger_trigger` (mocks the summary check — neutral
  by construction, say so), `salon_reads`/`salon_mutations` (`contextSummary`
  is a column they carry, not a path they run).
- **NEW `chat_rebuild_summary_equivalence` (tier 2 over a jest case driving
  v4's REAL `handleRebuildSummary` through the real chat route, over a
  built fixture — the `chat-admin-routes.test.ts` shape, NOT the mocked
  unit test):** the 200 body + the `chats` row (`contextSummary` NULL,
  `summaryAnchorMessageIds` `[]`, `lastSummaryTurn` 0, `lastFullRebuild
  Turn` UNMOVED from a seeded non-zero, `updatedAt` minted → normalized)
  + the `background_jobs` row (`type`, `payload` bytes `{chatId,
  connectionProfileId, forceRegenerate:false}` in that order, `priority`,
  `maxAttempts`); the 409 on a `running` room and the 200 on a `paused`
  one; the 400 with no profiles; the profile fallback (a cast whose
  `connectionProfileId` names no profile → `profiles[0]`; one that does →
  that one); the 404. ⚠ Measure whether `jest.setup.ts` mocks the queue
  service — if it does, the case `requireActual`s it (every jest oracle
  writes ZERO `llm_logs` rows; the JOB row is what this family needs).
- **Log pins:** the debug line (fields, level, target prefix, ORDER vs the
  empty-turns return) and the two `[Chats v1]` lines, each with a silence
  leg.
- **Mutation proofs (each must redden exactly its test):** drop the
  `!n.is_empty()` filter (the empty-name row reddens in BOTH new families
  and `fold_episode_tier3`'s grown arm); resolve only `active` seats (the
  removed-seat row reddens); render `m.role` instead of `speaker` (every
  fold op reddens); move the resolution AFTER the empty-turns return (the
  order leg reddens); clear `lastFullRebuildTurn` too (the seeded-non-zero
  row reddens); pass `forceRegenerate: true` (the payload row reddens);
  carry the cheap-LLM-settings 400 over (a fixture with NULL
  `cheapLLMSettings` reddens — plant one); publish before the update
  (the update-failure proof reddens: fire the failure with a poisoned
  `chats` table and assert NO job row + NO publish); swap the 409 and 400
  order (a running room with no profiles answers 409 in v4 — plant it).

## Fixtures

Consumes: the target pin's jest-built `/tmp` DBs (`build-context-summary-
service-fixture.ts`, `build-fold-episode-fixture.ts`, `build-orchestrator-
fixture.ts`, the memory-pipeline builder) — every one carries compressed
cells at the pin (the tree has S). Delivers: the TWO new builders/cases
(`build-speaker-names-fixture.ts` or a case-local seed; `chat-rebuild-
summary.test.ts` over the `chat-admin-*` pair's per-case COPY — never the
committed pair itself; §R.12) and the grown JSON specs. Widens NO committed
pair. Dependents: every family named above — yours to re-run.

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `a2db63da7` bug-161/162 drift catch-up + maintenance round: **P4.D212 ∥
P4.D213 ∥ P4.D214 ∥ P4.103 ∥ P4.104 ∥ P4.105 ∥ P4.106**, ordered
2026-09-22. Seven lanes, seven worktrees, seven lane branches, **every one
cut from `main` (`fc63905e`)** — no keystone this round: S and P4.D204 are
already on main, which is what makes the fixture-vintage heal possible at
last. The unifier cherry-picks in the order **P4.D212 → P4.D213 → P4.D214 →
P4.103 → P4.105 → P4.104 → P4.106** (the drift first, its SPA half after
the contract it consumes, the CLI beside them; the heal before every
family that reads a widened pair is re-run; `update_message` before the
seam that shares nothing with it but the gate; the coverage lane LAST
because its grown corpora are re-recorded from the NEW baseline pin with
P4.D212's fold present), gates ONCE from the new baseline pin, and
`merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`f45a517a9`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`a2db63da7`** ("Fix
  bug 162: CLI raw-SQL path opens without qt_text() (#63)", `4.10.0-dev.
  63`, 2026-09-21 22:20) — **THREE commits past the baseline**, all three
  out of ONE live Friday chat: `4e1a8e061` (docs: bugs 161/162 filed +
  the summarizer-name design — NO-PORT? → P4.D212 with mirrors),
  `e7821606f` (bug 161 + the PORT-NEW rebuild-summary — PORT → P4.D212 ∥
  P4.D213), `a2db63da7` (bug 162 — PORT → P4.D214). Every row now
  `ORDERED(…)`. Spec reads: `git show <sha>:<path>` for the post-commit
  files and `git show <sha> -- <path>` for the hunks — **never the commit
  message** (§R.4). v4 `bugfix` tip `1a2b2164c` and `release` tip
  `8fbf2afe0` are unmoved; still no `release: 4.10.0` squash. The checkout
  sat on `main`, CLEAN, at the planning-start AND planning-close probes
  (2026-09-22 — the §2 probe PASSED twice; the ledger stood and nothing
  was re-derived). **At unification the baseline MOVES to `a2db63da7`**;
  the two code rows retire `ABSORBED(…)` and the docs row `NO-PORT-
  RATIFIED(…)` on P4.D212's file list.
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log a2db63da7..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Pass = branch `main`, HEAD `a2db63da7`, both logs EMPTY, tree
  CLEAN.** Any mismatch — a moved HEAD (a new commit is a NEW drift row,
  not a pass), a non-empty log, ANY dirty path, the checkout off `main` —
  is a STOP: report it and do nothing else. A lane never writes the
  ledger. (v4 landed three commits in one evening out of one chat this
  week; expect the probe to fail mid-round, and expect the human to
  record a waiver in the ledger's §1 the way the `89fcc3c0d` round did —
  a lane resumes ONLY on a recorded waiver, never on its own reading of
  the dirt.)
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1), absolutely.
  HEAD is three commits past the baseline and one of them rewrites the
  fold/episode paths whose families regenerate routinely. Every regen
  runs from a LANE-UNIQUE detached worktree per ledger §5.1 (`git -C
  ~/source/quilltap-server worktree add --detach /tmp/qt-v4-pin-<order>-
  <sha> <sha>`, the THREE symlink classes — root `node_modules`,
  `packages/quilltap/node_modules`, every `plugins/dist/*/node_modules` —
  then `cd` into it; never stash or checkout-switch the human's repo;
  `git worktree list` before each batch; **verify the pin by `git -C
  "$PIN" rev-parse HEAD` AND by `ls -ld` of the path** — build every pin
  path from a quoted literal, never an unsplit zsh variable; then grep
  the fresh NDJSON for a marker only one tree has; check the file is
  non-empty before believing a diff). **The two DRIFT lanes (P4.D212,
  P4.D214) pin the TARGET `a2db63da7`** (`/tmp/qt-v4-pin-<order>-
  a2db63da7`) for every family their port MOVES **and the BASELINE
  `f45a517a9`** (`/tmp/qt-v4-pin-<order>-f45a517a9`) for their neutrality
  legs; P4.D213 records no Rust oracle (a pinned worktree only if it
  records a client string). **The four MAINTENANCE lanes (P4.103, P4.104,
  P4.105, P4.106) pin the BASELINE `f45a517a9` ONLY** — their families
  must not move at the baseline; a family that ALSO moves at `a2db63da7`
  (`orchestrator_tier3`, `memory_pipeline_jobs_tier3`, `courier_images_
  routes` — the three that reach the real fold) is P4.D212's to re-record
  and the UNIFIER's to re-run from the new baseline pin with every lane's
  growth present. Pin verification markers: the target-pinned
  `help_tree_equivalence` NDJSON carries `Rebuilding the Running Summary`
  (`help/chats.md` at `e7821606f`); the baseline-pinned one does not
  (`grep -c` both). The target pin's fixtures are compressed exactly as
  the baseline's (P4.D203's S is on main — every lane's tree reads them).
  The sweep driver is the sanctioned per-family path (`harness/tools/
  recipe_sweep.py --run <family> --v4 "$PIN" --v5w <ABSOLUTE worktree>`);
  never run two sweeps concurrently; a family that reports `SKIP` in
  0.00 s did not run. Ledger §5.2's silent-stale-pass discipline applies
  to every regen (`rm -f` the fixture first; read the builder's last
  line; `grep -c` the changed bytes in the fresh NDJSON — MUST be > 0).
  Every lane stages its regen outputs under a LANE-PRIVATE directory
  (`/tmp/<order>/…`) and leaves the committed recipe headers canonical (a
  committed recipe NEVER names a `/tmp` pin — the driver's `--v4` is the
  pin). Every jest oracle writes ZERO `llm_logs` rows (`jest.setup.ts:379`
  mocks the logging service). `QT_NODE` in any env block is a REAL path
  (`~/.nvm/versions/node/v24.13.1/bin/node`), never `$N/node` — the
  `f45a517a9` gate's `cli_differential` artifact.
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). Three
  measured misdescriptions this round, one per drift lane: (a)
  `e7821606f`'s body says the fold-episode pass "was already getting this
  right" — true of NAMED characters only; the consolidation moves the
  gate from `if (character)` to `if (character?.name)`, so an EMPTY-NAME
  character's label changes on that side too, and v5's loop matches the
  PRE-fix gate (P4.D212's red-first episode arm). (b) The same body says
  the API "refuses a running autonomous room" and stops; the HUNK also
  refuses with 400 when the user has NO connection profiles, and does
  NOT check cheap-LLM settings the way regenerate-title does — carry the
  hunk's two refusals, not the sibling's three (P4.D212). (c)
  `a2db63da7`'s title says the raw-SQL path "opens without qt_text()" —
  v5's path ALREADY registers it (P4.D203); what the hunk moves is the
  OPENER (one, not two) and the failure STRINGS (per-target friendly
  names; bare messages on two arms), a Tier R comparand (P4.D214). And
  one in the PLAN: `phase-4.md:6659`'s "the widen brings the FTS
  triggers" is FALSE — the migrator is ALTER-only; the pairs stay
  trigger-free on both sides (P4.103). Re-verify even where an order
  flags the trap — the order's paragraph is the same kind of prose, and
  so is a previous lane's record or a doc comment in the tree. Carry a
  v5-side code comment naming the sha where the prose misleads.
- **§R.5 — Designed reds and pre-existing reds: measure, don't assume**
  (ledger §5.4). No convergence pin trips this round (both bugs are v4's
  own filings — ledger §3, the negative §5.4 check). The DESIGNED reds:
  **P4.D212** — `context_summary_service_tier3` red at the target on
  every fold op's speaker labels (`USER:` → `User:` etc.) AND on the
  prompt's last sentence; `fold_episode_tier3` neutral on the existing
  corpus and red-first on the grown empty-name arm; `orchestrator_tier3`
  / `memory_pipeline_jobs_tier3` / `courier_images_routes` MOVE at the
  target on every case whose fold fires (re-run and record; you edit none
  of their files); the NEW families red-first on unported main;
  `dispatch_wrong_type_census` red until 446. **P4.D214** — Tier R RED at
  the target on the grown wrong-key cases (`Cannot open database:` vs
  `Cannot open main database:`) and on the bare-message arms until the
  opener moves; the SAME cases RED at the baseline pin after the move
  (the movement is v4's — record both). **P4.103** — the SEVEN standing
  fixture-vintage reds (`subprompts_routes`, `subprompts_prompt_tier2`,
  `chat_delete`, `character_wizard_tier3`, `search_replace`, `profile_
  routes`, `projects_routes`) RED from the pin on the un-widened pairs
  and GREEN after the widen — the pre-widen counts are the measurement;
  the "six earlier ones" the previous lists carried are NOT red (P4.94
  closed them — a stale carry-over, corrected). **P4.104** — every image
  row it plants is red-first on the un-wired tree (`image/png` where v4
  answers `image/webp`); the readback unit test red-first. **P4.105** —
  the new family red-first on the map row's `ftsId` under DELETE+INSERT;
  `chats_messages_ops_tier2` + `salon_mutations` NEUTRAL (trigger-free
  fixtures). **P4.106** — each Inform arm red-first where v5 can be made
  to disagree (the lane lists which; "the existing corpus stays green" is
  not a proof); the SDK guard green at planning (every installed version
  matches the recordings — measured 2026-09-22). A red in a family a lane
  did NOT touch is a NEW finding to classify at both pins before any
  source is touched, and it is probably not that lane's. **The seven
  fixture-vintage reds are P4.103's to close this round** — every other
  lane whose family reads one of the six pairs records the red by name
  at lane start and expects it green at unification; nobody else widens
  a pair.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with
  a capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest`
  is shared) and its silence leg; a fixture change lands with the regen
  of every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that
  SURVIVES as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`). A dispatch WIRE
  test through `POST /api/dispatch` is the only instrument that sees a
  serde collapse; a family that calls the handler directly cannot. A new
  verb lands with its `dispatch_wrong_type_census` row and the census
  recount in the comment. A NEW pure module lands with a tier-1 corpus
  recorded from v4's REAL module through a tsx case, never a
  transcription of v4's test file. A v4 unit test that MOCKS a repository
  is mirrored as a real-DB arm with PLANTED rows, never as the same mock
  (`a-v4-mock-is-not-v4-in-a-route-family`).
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane. P4.D214 ALSO updates P4.D210's
  status header (its declared gap closed) and P4.106 updates P4.D205's,
  P4.D208's, P4.D209's and P4.D211's (the items they closed) — those
  headers are the ONE cross-order edit each lane may make, one sentence
  each, naming this round.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed on each commit per `.claude/commands/commit.md`; the unifier
  RECOUNTS as base + the sum of every lane's bumps (the silent-auto-merge
  trap: identical bumps merge as ONE — it fires every round). Base at
  planning: core 0.0.992, harness 0.0.890, host 0.0.146, web 0.0.170,
  cli 0.0.25, tauri 0.0.7, fixture-sanitizer 0.0.4, SPA 0.5.747.
  Expected: **core** by P4.D212, P4.104, P4.105 (and P4.106 ONLY if a
  `cfg(test)` seam lands in core — say so); **harness** by every Rust
  lane (P4.103 only if `test_support` moves — it should not);
  **web** by P4.D212 (the census + the wire test), P4.103 (the fixtures
  live in `crates/quilltap-web/tests/fixtures/`), P4.106 (the wire test);
  **host** by P4.104 (only if `image_codec.rs`/`host.rs` moves — say so);
  **cli** by P4.D214 ONLY; **the SPA** (`apps/web/package.json`) by
  P4.D213 ONLY; **tauri** and **fixture-sanitizer** by NOBODY. No lane
  touches the workspace `Cargo.toml` dependency block; every lane's
  `Cargo.toml` deltas must be version-only — the unifier audits each
  before any resolution (the `--theirs` clobber trap).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  each lane pre-lists, in its lane record with byte counts at
  `a2db63da7`, the mirror paths its rows move. This round's mirror rules
  in one place: `4e1a8e061` + `e7821606f` → `docs/v4/developer/features/
  complete/context-summary-speaker-names.md` (9,712 — the FINAL path
  `e7821606f` renames to, never the birth path), `docs/v4/developer/bugs/
  fixed/bug-161-summary-invents-a-name.md` (6,100), `docs/v4/developer/
  API.md` (219,745 → 221,285), `docs/v4/developer/bugs.md` (302,517 →
  306,475 — carries BOTH bugs' rows; mirrored ONCE); `a2db63da7` →
  `docs/v4/developer/bugs/fixed/bug-162-cli-raw-sql-no-qt-text.md`
  (6,486), `docs/v4/packages-quilltap-README.md` (34,707 → 34,898 — the
  two-line `qt_text()` note; measured as the ONLY delta). `docs/
  CHANGELOG.md`, `README.md`, `CLAUDE.md`, `.claude/commands/update-
  documentation.md`, every `package.json`/`package-lock.json` stamp →
  NO-PORT, the standing class; Tier R at the pin for the `packages/
  quilltap` stamp (P4.D214 runs it). The `help/` count does NOT move
  this round (`help/chats.md` is a byte-copy; 126 stays 126 in BOTH
  literals — `help_tree_embed_guard.rs:65` and `host_help_docs_boot.rs:
  91`); `help_tree_equivalence` at the target pin is the proof the tree
  is v4's.
- **§R.10 — Fences, ownership edges and the pre-declared spills.** (a)
  **`api/types.rs` and `api/engine.rs`:** P4.D212 adds `Request::
  ChatRebuildSummary` inside a `// === P4.D212 ===` … `// === end P4.D212
  ===` fence appended at the END of the `Request` enum and its dispatch
  arm inside the same fence at the END of the engine match (NO new
  `Response` variant — it answers through `Response::ChatAdmin`). P4.104
  edits the BODIES of EXISTING dispatch arms in `engine.rs` (threading
  the codec argument) and adds NO variant, NO fence; it never touches
  `types.rs`. Nobody else touches either file. The unifier picks P4.D212
  first; the hunks are distant. (b) **`crates/quilltap-web/tests/
  dispatch_wrong_type_census.rs`:** P4.D212 alone moves it (445 → 446,
  `chat_id`); P4.106 adds a wire TEST FILE, not a verb, so the census
  stays. (c) **`orchestrator_tier3` / `memory_pipeline_jobs_tier3` /
  `courier_images_routes`:** P4.106 OWNS the orchestrator family's files
  (corpus, builder, case, test); P4.D212 RE-RUNS all three at its target
  pin and edits none of them — a movement it predicts is recorded, a red
  it cannot explain is a finding for P4.106's owner to read at
  unification. (d) **`salon_mutations`:** P4.106 owns its files; P4.105
  re-runs it (neutral); P4.D212 lists it as a neutrality leg (re-run
  only). (e) **`chat_export` + `chat-dialogs-*`:** P4.103 owns the PAIR
  (the `.db` files); P4.106 owns the family's CASE (it plants informs on
  the per-run COPY); at unification the grown case runs over the widened
  pair from the new baseline pin. (f) **`qtap_import`:** P4.106 owns it
  whole (the two-link-blob bundle row); P4.104 does not touch it (the
  import's `normalize_images: false` is the ONE untouched site). (g)
  **`db/doc_mount_file_links.rs` + `db/doc_mount_blobs.rs`:** P4.104's;
  P4.105's is `db/chats_messages.rs` + `db/chats_search.rs`; P4.D212's is
  `services/context_summary*` + `services/fold_episode_pass.rs` +
  `services/chat_admin.rs` + NEW `services/speaker_names.rs`. (h)
  **`crates/quilltap-cli/**`:** P4.D214's alone. (i) **`apps/web/**`:**
  P4.D213's alone. (j) **Pre-declared loud out-of-mandate edits** (the
  `a-required-field-on-a-shared-struct-crosses-lane-ownership` rule: do
  the edit, keep it to the hunk, mark it `// P4.xxx OUT-OF-MANDATE —
  <owner> preserves`, add a guard test where the owner must carry it,
  audit with `git diff main -- <path>` at lane close): **P4.104 →
  `api/engine.rs`** (existing arms' bodies, above); **P4.105 →
  `crates/quilltap-harness/tests/compressed_column_write_sites_census.rs`**
  (its `:46` note names the DELETE+INSERT — the census moves with the
  port; P4.D203's file by history, nobody's this round); **P4.106 →
  `services/message_context.rs`** ONLY if the tier-1 `is_record_only_
  message` family needs the function `pub(crate)` → `pub` (a visibility
  widening, one line). If a lane finds it needs another lane's file
  beyond these, it STOPs on that item and records the shape for the
  unifier.
- **§R.11 — SPA behaviour moves in ONE lane.** P4.D213 is the only lane
  touching `apps/web/**`. It consumes ONE server contract it cannot run
  against until unification (§S.1); it builds against the pinned shape
  with the SPA's mocked transport, adds its live beat gated behind the
  NAMED constant the UNIFIER flips (`P4D212_SERVER_LANDED` — the `P49K*_
  SERVER_LANDED` precedent; a DEFINED-but-unimplemented verb defeats a
  capability probe), and flips nothing itself. The unified gate runs the
  full Playwright suite once against the release binary with the flipped
  beat's first live run as its own step; the documented intermittents and
  the standing parks stand as named in the previous round's gate (329
  passed / 1 failed / 6 skipped, the one red the pre-existing P4.66
  bubble beat, 2/2 by file). **The help re-vendor table:** `help/chats.md`
  → P4.D212 (a byte-copy at the target pin, 39,682 bytes; the count
  unmoved). After the round, `help/**` is 126 files and `help_tree_
  equivalence` at the target pin is the proof (the embed guard compares
  embedded-vs-DISK and cannot see a stale re-vendor).
- **§R.12 — Committed fixtures: P4.103 WIDENS six pairs; nobody else
  touches a committed pair, and nobody REBUILDS one** (a rebuild at the
  pin re-encodes every ≥ 512-byte `chat_messages`/`conversation_chunks`
  cell and retires the P4.D203 Class-3 neutrality proofs over `chat-
  dialogs-*`/`salon-*`/`system-data-*`; a WIDEN through v4's ALTER
  statements does neither). The ONE rebuild this round: P4.103's
  `chat-compressed-mount.db` (the 0-byte file — an empty mount-index
  schema carries no cell). The ONE new committed artifact: P4.106's
  `qtap-import-two-link-blob.qtap` bundle. Every other lane's fixtures
  are the pin's jest-built `/tmp` DBs or per-run COPIES of a committed
  pair. `.db-journal` files are never fixtures — delete strays (the
  migrator leaves one per apply).

## §S — Shared contract (binding; byte-identical in every order of this round)

### §S.1 — `chatRebuildSummary` (P4.D212 provides; P4.D213 consumes)

v4 `POST /api/v1/chats/[id]?action=rebuild-summary` (`e7821606f`,
`app/api/v1/chats/[id]/actions/rebuild-summary.ts`). Dispatch-only on v5
(the `regenerate-title` precedent — the REST edge's pointer sentence is
unchanged).

```
Request  (POST /api/dispatch):  { "type": "chatRebuildSummary", "chatId": "<uuid>" }
Response (200):                 { "success": true, "jobId": "<uuid>" }        — Response::ChatAdmin(Value), key order success, jobId
Refusals, in v4's order:
  404  { "error": "Chat not found" }                                          — the chat read
  409  { "error": "Pause the room before rebuilding its summary." }           — chatType == "autonomous" && runState == "running" (a PAUSED room passes)
  400  { "error": "No connection profiles available" }                        — the user has no connection profiles (NO cheap-LLM-settings check)
  500  { "error": "Failed to rebuild the summary" }                           — any failure after the refusals; no job row, no publish when the update fails
Side effects on 200, in order:
  chats.update(id, { contextSummary: null, summaryAnchorMessageIds: [], lastSummaryTurn: 0, updatedAt: <now> })   — ONE update; lastFullRebuildTurn UNTOUCHED
  background_jobs ← CONTEXT_SUMMARY { chatId, connectionProfileId, forceRegenerate: false } at priority 0, maxAttempts 3 (v4's enqueueJob defaults; NO dedupe)
  realtime: topic `chats`, id = chatId   (the SPA's existing chatKeys.detail invalidation re-reads the chat — no new key)
  log INFO  "[Chats v1] Context summary cleared for rebuild"   { chatId, jobId, connectionProfileId }
  log ERROR "[Chats v1] Failed to rebuild context summary"     { chatId } + the error   (the 500 arm)
Profile choice: the first CHARACTER participant's connectionProfileId when it names an available profile, else the user's first profile.
```

Rust (P4.D212, inside the `// === P4.D212 ===` fence at the END of `Request`):

```rust
/// Discard the chat's running context summary and let the fold cadence
/// rebuild it from turn 1 (v4 `POST …?action=rebuild-summary`, `e7821606f`).
#[serde(rename_all = "camelCase")]
ChatRebuildSummary {
    chat_id: String,
},
```

TypeScript (P4.D213, `apps/web/src/app/core/core-contract.ts`, beside
`ChatRegenerateTitleRequest`; added to the `CoreRequest` union):

```ts
export interface ChatRebuildSummaryRequest {
  type: 'chatRebuildSummary';
  chatId: string;
}
```

SPA gate constant (P4.D213, `apps/web/e2e/salon-dialogs-flow.spec.ts`):
`const P4D212_SERVER_LANDED = false;` — flipped to `true` by the UNIFIER.

### §S.2 — Nothing else crosses a lane boundary this round

P4.D214's opener is CLI-internal; P4.103 changes bytes no test reads as a
contract; P4.104's codec parameter is core-internal (the engine's existing
`blob_webp`); P4.105's UPDATE is repository-internal; P4.106 adds tests.
A lane that discovers it needs a second contract STOPs on that item and
records the shape for the unifier.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D212** — bug 161 server + the rebuild-summary verb + the `4e1a8e061` ratification | NEW `crates/quilltap-core/src/services/speaker_names.rs` + its `mod` line; `services/context_summary.rs`; `services/context_summary/{tasks,prompt_text}.rs` (`prompt_text.rs` REGENERATED, never hand-edited); `services/fold_episode_pass.rs`; `services/chat_admin.rs` (the new verb + a `conflict` helper if none exists); `api/types.rs` + `api/engine.rs` INSIDE its `// === P4.D212 ===` fence ONLY; `help/chats.md` (byte-copy); `crates/quilltap-web/tests/dispatch_wrong_type_census.rs` (445 → 446); NEW `crates/quilltap-web/tests/chat_rebuild_summary_dispatch_wire.rs`; `context_summary_service_tier3_equivalence.rs` + `fold_episode_tier3_equivalence.rs` + their cases/builders/corpora; NEW `speaker_names_equivalence.rs` + `chat_rebuild_summary_equivalence.rs` + their cases/builders/corpora; `chat_admin_routes_equivalence.rs` + its case (grown); this order's header | `apps/web/**` (P4.D213), `crates/quilltap-cli/**` (P4.D214), every committed fixture pair (P4.103), `orchestrator_tier3`/`memory_pipeline_jobs_tier3`/`courier_images_routes`/`salon_mutations`'s files (re-run only — P4.106 owns the first and last), `db/**`, the REST edge (`wardrobe_routes.rs` — no new arm), the drift ledger |
| **P4.D213** — bug 161's SPA half | `apps/web/**` whole: `core/core-contract.ts` (§S.1), `chat/chat-admin.api.ts` (+ spec), `chat/sidebar/organize-section.ts` (+ spec), `screens/salon/salon-conversation.ts` (the handler; + spec), `e2e/salon-dialogs-flow.spec.ts` (the gated beat), `apps/web/package.json`; this order's header | Everything outside `apps/web/**`; the drift ledger |
| **P4.D214** — bug 162: the ONE CLI opener, the Tier R comparand, P4.D210's live rows | `crates/quilltap-cli/src/dbopen.rs` (widened), `src/db_cmd.rs` (the private opener DELETED; the friendly names; the bare catch), `src/{db_characters,docs_cmd,restore_key,sync_cmd}.rs` ONLY if the repointing needs a call-site edit (strings unchanged — Tier R says so); `crates/quilltap-cli/tests/cli_differential.rs` (the grown master DDL, the pre-hooks, the new `db` cases, the canned-stub `sync` cases); the P4.D210 order's status header (one sentence); this order's header | `crates/quilltap-core/**` (the UDF's home — call it, never edit it), `apps/web/**`, every committed fixture pair, the drift ledger |
| **P4.103** — the fixture-vintage heal | `crates/quilltap-web/tests/fixtures/{subprompts,chat-delete,character-generators,chat-dialogs,profile,groups-projects}-*.db` (widened in place), `chat-compressed-main.db` (widened), `chat-compressed-mount.db` (REBUILT), the three `chat-compressed-*.meta.json` sidecars, `chat-delete-llmlogs.db`/`chat-compressed-llmlogs.db` (the `llm_logs` recipes), `inspector-llm.db` ONLY if Tier 2 item 6 plants without a rebuild; `harness/oracle/fixtures/migrate-memories-fixture-columns.ts` ONLY to append this run's recipe to its header; `harness/tools/sweep-results/<this run>.json`; this order's header | Every `.rs` file (a red after the widen is a FINDING, recorded, not fixed here), every test file's logic, `fresh_schema.json`, every other committed fixture, `apps/web/**`, the drift ledger |
| **P4.104** — the image-normalization seam | `crates/quilltap-core/src/db/doc_mount_blobs.rs` (the readback fix + the codec pass-through), `db/doc_mount_file_links.rs` (the `:570-577` doc comment ONLY — the constructors stand), `services/mount_index/normalize_blob_image.rs` (the OPEN note → the closed record), the ten write sites (`tools/doc_edit/blob.rs`, `photos/{character_gallery_service,user_gallery_service,save_image_to_album}.rs`, `services/image_job_storage.rs`, `services/mount_index/{file_ops,store_file}.rs`, `services/file_storage.rs`, `tools/generate_image.rs`) and the API handlers that call them (`api/{photos,chat_media,mount_files,…}.rs` — the lane's census names them), `api/engine.rs` EXISTING arms' bodies (§R.10(j)), `crates/quilltap-host/src/image_codec.rs` ONLY if a trait impl is missing; NEW `crates/quilltap-harness/tests/blob_write_sites_census.rs`; the families its census names + their corpora (+ image files under `harness/oracle/fixtures/<family>/`); `normalize_blob_image_equivalence.rs` (grown); this order's header | `services/mount_index/sync/**` (already wired), `services/quilltap_import/**` (the `false` site — P4.106 owns `qtap_import`), `api/types.rs`, the P4.D212 fence, `db/chats_*` (P4.105), every committed fixture pair, `apps/web/**`, the drift ledger |
| **P4.105** — `update_message` as an UPDATE + the search `safeQuery` arm | `crates/quilltap-core/src/db/chats_messages.rs` (`update_message` + the shared column-list helper; `insert_event`'s BYTES unchanged), `db/chats_search.rs` (the `safeQuery` arm), `crates/quilltap-harness/tests/compressed_column_write_sites_census.rs` (§R.10(j)); NEW `chat_message_update_fts_tier2_equivalence.rs` + its case + the builder arm (`build-chats-messages-ops-fixture.ts`'s FTS variant or a sibling) + corpus; `chats_search_equivalence.rs` + its case (the poisoned-fixture row); this order's header | `db/chat_message_fts.rs` (v4's DDL verbatim), every caller of `update_message`, `salon_mutations`'s files (re-run only), `db/doc_mount_*` (P4.104), every committed fixture pair, `apps/web/**`, the drift ledger |
| **P4.106** — the Inform coverage remainder + the three smalls | `build_context_tier3_equivalence.rs`, `orchestrator_tier3_equivalence.rs`, `primary_stream_tier3_equivalence.rs`, `regenerate_swipe_tier3_equivalence.rs`, `turn_transcript_equivalence.rs`, `salon_mutations_equivalence.rs`, `chat_export_equivalence.rs`, `qtap_import_equivalence.rs`, `system_backup_equivalence.rs`, `system_restore_equivalence.rs` (+ `_state`, `_guards`), `system_import_state.rs` + ALL their cases/builders/corpora; NEW `is_record_only_message_equivalence.rs` + case + corpus; NEW `crates/quilltap-web/tests/chat_informs_dispatch_wire.rs`; NEW `scenario_seeded_summary_heal_equivalence.rs` + case + spec; NEW `provider_sdk_version_guard.rs`; NEW `harness/oracle/fixtures/qtap-import-two-link-blob.qtap` (+ its builder); `services/message_context.rs` ONLY per §R.10(j); a `cfg(test)` counting seam in `test_support.rs` if item 7 needs one; the P4.D205 / P4.D208 / P4.D209 / P4.D211 status headers (one sentence each); this order's header | Every production `db/**`, `services/**`, `api/**` file bar the §R.10(j) line, `apps/web/**`, `crates/quilltap-cli/**`, every committed fixture PAIR (plant on COPIES), the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix, beyond the pre-declared spills in §R.10(j) — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note). Budget 50–70 GB of disk per long lane
(`df -h ~` showed 147 GB free at planning; seven worktrees is seven
`target/` dirs — run at most FOUR Rust lanes concurrently and reclaim
`target/debug/incremental` between waves).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean (Rust lanes); `npm run lint` clean
   incl. the qt-class guard (P4.D213).
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`)
   (Rust lanes).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0
   where the order predicts a change; a recorded `0` where it predicts
   none), and the red-first steps recorded with the pre-fix counts.
   P4.D213: `npm test` whole plus its new/changed spec files by name.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp` / by-label diff) where the family is
   deterministic across regens; where it is not (`regenerate-at-both-
   pins-and-cmp-is-not-universal`), compare the normalized rows by case
   and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`
   (P4.103's `.db` reverts are the ONE exception — the pair is the
   artifact).
7. `cargo build --workspace --release` — clean (Rust lanes; the Playwright
   venue prefers `target/release`); `npm run build` clean (P4.D213).
8. `cargo test --workspace --no-fail-fast -- --nocapture` with the lane's
   env block (its own families' vars; every other family's var WITHHELD
   so it SKIPs honestly rather than failing on a deleted `/tmp` path):
   every binary's `test result:` line read, zero failures bar the seven
   standing fixture-vintage reds named in §R.5 (recorded by name if their
   vars are in the block — P4.103 expects them GREEN), the lane's
   families confirmed RUN by non-zero duration (Rust lanes).
9. **Source censuses** that the change moves (`dispatch_wrong_type_census`
   for P4.D212; `compressed_column_write_sites_census` for P4.105; the
   NEW `blob_write_sites_census` for P4.104; `help_tree_embed_guard` +
   `host_help_docs_boot` for P4.D212 — expected UNMOVED at 126, run to
   prove it; `zod_version_guard` + the NEW `provider_sdk_version_guard`
   for P4.106; `chat_informs_swipe_handle` for P4.106) re-run by name and
   their constants recounted with the arithmetic in the comment.
10. **Tier R** (`crates/quilltap-cli/tests/cli_differential.rs`) at BOTH
    pins for P4.D214 (the comparand; 244 → 244 + N; the designed reds
    closed at the target, recorded at the baseline); at the baseline pin
    for P4.D212 (the verb is not a CLI surface — 244/0 expected; run it,
    do not assert it).
11. **Commit per `.claude/commands/commit.md`** (one unit per commit where
    the order tiers them; CHANGELOG entry + version bumps per §R.8; the
    status-log lane record appended with the regen commands AS RUN, the
    mutation table, the pre-fix counts, the measurements, and every
    finding outside the lane's ownership named for the unifier). The lane
    closes by updating its own order's status header with what landed,
    what was deferred, and what it measured that the order got wrong.
