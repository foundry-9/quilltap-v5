# Work order P4.9E3C — the ChatModals dialog family SPA (`p4.9e3`)

> **STATUS: OPEN — not started (written 2026-07-27).** Lane 3 of 4 in the
> "embedding repair + chat-dialog family" round. **SPA-only: this lane owns
> all of `apps/web/**` and touches nothing else** (plus the docs it must
> correct — see Records).
>
> **v4 baseline for this round: `e8a49597`** (v4 HEAD, 2026-07-27,
> 4.8.0-dev.108 — REVISED mid-planning: v4 shipped the Bug 5 fix, pascal
> drift owned by the round's fourth lane P4.D24; nothing in this lane's v4
> survey sits in the drifted file). **Drift-check at lane start**:
> `git log e8a49597..HEAD --oneline` + `git status --short` in
> `~/source/quilltap-server`; if v4 moved, STOP and report.
> One possible handoff INTO this lane: if P4.D24's verification finds
> `custom-tools-popup.ts` does not render a `characterLabel` on a
> single-variant row, that one-line render fix arrives here (this lane owns
> `apps/web/**`) — check its lane record at unification.
>
> **Siblings (run in parallel, disjoint ownership — see §Ownership):**
> - **P4.9E3B** — the chat-dialog server remainder
>   (`p4.9e3b-chat-dialog-server-remainder.md`). This lane consumes **every
>   §1 verb and both REST edges** from it. Build against the §1 contract;
>   beats that need E3B's verbs are **ACTIVATE-AT-UNIFY** (the standing
>   pattern) — write them live, gate them on a named constant, and the
>   unifier flips them when the branches meet.
> - **P4.6BL** — the EMBEDDING_GENERATE handler
>   (`p4.6bl-embedding-generate-handler.md`). No dependency either way.
> - **P4.D24** — the `e8a49597` operator-perspective drift re-port
>   (`p4.d24-operator-perspective-drift.md`). Rust-only; see the possible
>   popup-label handoff above.
>
> **Worktree note:** run on your own branch in your own checkout. No
> `git stash`/`git worktree` in the repo.

## Mandate

Port v4's in-chat dialog family — the `p4.9e3` backlog `m6-screen-parity.md`
§2.2 tracks — over the chat-admin verbs P4.9E3A landed (2026-07-26) and the
§1 verbs P4.9E3B lands this round. Eleven surfaces plus the agent-mode badge
(dogfood finding #34's untracked hole) and a new Edit Content sidebar
section. This closes the last "server is live, no UI can reach it" gaps in
the chat surface — including `ChatRegenerateTitle`, which is unreachable in
v4 itself except through `ChatRenameModal`'s "Use automatic naming" checkbox.

**This lane also carries the round's full-Playwright obligation**, and with it
the P4.d23-owed item: **execute `zzz-restore-destructive.spec.ts` unmodified
and record the result** (an owed re-run, not an extension — if the spec needs
a change, escalate, don't edit it beyond what the standing instruction
allows).

**Must NOT touch:** anything outside `apps/web/**` except the docs updates in
Records. Server gaps found mid-lane are cross-lane escalations, not fixes.

## Survey-verified starting points (2026-07-27 — do not re-derive)

> Surveyed at v4 `c1507f47` / v5 `d2097503`. Naming corrections from the
> survey are baked in below — the old planning names (ChatMergeModal,
> ReattributeModal, BulkReplaceModal, SelectLLMProfileModal) were wrong.

### The v4 family — host, files, LOC, server calls

The host is `app/salon/[id]/components/ChatModals.tsx` (**465 LOC**, 17
dialogs) with open/close state in `app/salon/[id]/hooks/useModalState.ts`
(135 LOC). ⚠ **`MergeConversationModal` is NOT in the host** — it is rendered
by `SalonView.tsx:1586–1597`, mounted only while open so its state resets.
v5 has no `useModalState` analog and doesn't need one: dialogs are local
signals on `screens/salon/salon-conversation.ts` (2150 LOC — the dialog block
is :414–:564; new dialogs go there).

| Modal | v4 file (components/…) | LOC | Server call(s) |
| --- | --- | --- | --- |
| **ChatRenameModal** | `chat/ChatRenameModal.tsx` | 231 | Save: `PUT /chats/{id}` `{chat:{title, isManuallyRenamed}}` (:100–109). **"Use automatic naming"** (:186–197): on *check* → `?action=regenerate-title`, no body (:52–54), sets title from `data.title`, toasts "Title regenerated", auto-closes; on error reverts the toggle; unchecking fires nothing; Save hidden while checked (:148). |
| **MergeConversationModal** | `chat/MergeConversationModal.tsx` | 373 | `GET /chats` (:98), `GET /chats/{sourceId}?action=outfit-summary` (:106, step 2), `POST …?action=merge-conversation` `{sourceChatId, characterIds, outfitSelections}` (:172–181). Two steps (`pick` → `configure` with per-character checkboxes + OutfitSelector). Filters out the current chat and `chatType === 'autonomous'` (:117). Hand-rolled overlay, not BaseModal (:206–212). |
| **ReattributeMessageDialog** | `chat/ReattributeMessageDialog.tsx` | 258 | `POST /messages/{id}?action=reattribute` (:82) — E3B's `MessageReattribute`. Opened from the message action bar, **not** the sidebar. |
| **BulkCharacterReplaceModal** | `chat/BulkCharacterReplaceModal.tsx` | 376 | `?action=bulk-reattribute` `{sourceParticipantId, targetParticipantId, roleFilter}` (:151–158); the `UNASSIGNED_USER` sentinel → `sourceParticipantId: null`; response `{messagesUpdated, memoriesDeleted}` drives the toast (:172–179). |
| **RunToolModal** | `chat/RunToolModal.tsx` | 409 | `GET /api/v1/tools?chatId=…&includeSchemas=true` (:80) then `?action=run-tool` `{toolName, arguments, characterId, private}` (:134–142). Details below. |
| **SearchReplaceModal** | `tools/search-replace/SearchReplaceModal.tsx` | 228 (+types) | `POST /search-replace?action=preview|execute` (`hooks/useSearchReplace.ts:75/:133`) — E3B's pair — plus `GET /characters` in the scope step. Initial scope `{type:'chat', chatId}`. |
| **AllLLMPauseModal** | `chat/AllLLMPauseModal.tsx` | 148 | **Zero fetches.** Continue = client close; Stop = plain chat PUT `{chat:{isPaused:true}}` (`useChatControls.ts:175–191`); Take Over = the impersonate verb. Counter from `chat.allLLMPauseTurnCount`. Threshold math is the pure `lib/chat/turn-manager/all-llm-pause.ts` (108 LOC: `INITIAL_PAUSE_INTERVAL = 3` :16, `getNextPauseInterval` :25, `shouldPauseForAllLLM` :41, `getCurrentPauseThreshold` :67 — sequence 3, 6, 12, 24, 48…). |
| **SelectLLMProfileDialog** | `chat/SelectLLMProfileDialog.tsx` | 234 | `GET /connection-profiles` (:66); confirm delegates up to stop-impersonation (`useImpersonation.ts:115–142` → `{participantId, newConnectionProfileId}`). v5's `ChatStopImpersonate` (engine.rs:1297) is the verb — **v4's client HTTP method is a v4 bug (DELETE vs POST); port the semantics, not the method** (recorded in E3B's order). |
| **LibraryFilePickerModal** | `chat/LibraryFilePickerModal.tsx` | **616** | Six endpoints: projects, mount-points, `?action=photo-albums`, `?action=group-stores`, `files?action=attach-mount-file`, `files?action=link`, plus character/user photos. Delegates browsing to `FileBrowser` (v5 has the legacy FileBrowser from P4.6af). |
| **ChatProjectModal** | `chat/ChatProjectModal.tsx` | 180 | `GET /projects` + `PUT /chats/{id}` `{chat:{projectId}}` (:68–71); `projectId: null` detaches. |
| **ChatToolSettingsModal** | `chat/ChatToolSettingsModal.tsx` | 171 | `GET /api/v1/tools?chatId=…` (no schemas) then `?action=update-tool-settings` `{disabledTools, disabledToolGroups}` (:78–85). Body rendering is the shared **`ToolSettingsContent`** (below). |
| **Export** | *(no modal)* | — | One sidebar button → `window.location.href = '/api/v1/chats/{id}?action=export'` (`ChatSidebar.tsx:1510–1512`, button :1578–1586). E3B lands the byte route at that exact URL. |

**`ToolSettingsContent`** (`components/tools/tool-settings/`, 565 LOC + types
74 + utils 184): a three-level tri-state tree (group → subgroup → tool,
`CheckState`, all expanded by default, per-group collapse :36–59). Built-in
groups toggle by writing every member into `disabledTools`; plugin groups
write **glob patterns** into `disabledToolGroups`
(`makePluginGroupPattern`/`makeSubgroupPattern`, :105–135 + utils).
`showAvailability` surfaces per-chat `available`/`unavailableReason`. The
modal adds a warning box when any LLM participant's connection profile has
`allowToolUse === false` (`ChatModals.tsx:392`). Budget this component as its
own unit — it is bigger than its host modal. (It also backs v4's
`ProjectToolSettingsModal` — NOT this lane's scope; note it for a later
Prospero rider.)

**RunToolModal details:** filters to `userInvocable !== false` (:85). Phase 1:
search over name/description/id (:162–168), grouping by `category` via the
`CATEGORY_INFO` emoji map (:30–40; unknown categories title-case + ⚙️ :283);
unavailable tools disabled at 50% opacity with `unavailableReason` (:292–312);
plugin chip :314–318. Phase 2: description, "Run as character" select over
active CHARACTER participants (:337–352), "Private (whisper)" checkbox
(:354–371), `JsonSchemaForm` from `tool.parameters` (:378–387), a collapsible
JSON preview labelled `(valid)`/`(incomplete)` (:391–398); defaults
pre-populated from schema `default`s (:99–111); empty args stripped before
POST (:127–132). v5 already has a JSON-schema-driven form family in the
Workbench (builder-form, P4.6bb) — reuse before writing a new one.

### The agent-mode badge (finding #34's untracked hole)

v4: `ChatSidebar.tsx:1116–1127`, in the Chat section **between the Concierge
tri-state and the Roleplay Template select** — a `monitor` icon +
`Agent On`/`Agent Off`, classes `qt-tool-palette-badge`(`-on`/`-off`), title
"Enable/Disable agent mode". Handler `useChatControls.ts:367–395`: computes
`newEnabled = agentModeEnabled === null || !agentModeEnabled` (:369), posts
toggle-agent-mode, reads back
`data.resolvedAgentModeEnabled ?? data.agentModeEnabled` (:383).

⚠ **v5's `ChatToggleAgentMode` variant now carries the FULL tri-state**
(`types.rs:2587–2599`, `enabled: Option<Option<bool>>`, widened at the
chat-action-round unification — the old "no `enabled` field" note is stale).
**v4's badge is a two-state toggle over that tri-state column and never sends
`null`** — a faithful port sends only the two arms v4 sends. The v5 note at
`chat-section.ts:71–76` already records this as "the cheapest item left in
`p4.9e3`".

### v5 current state — what exists, what's missing

- **`apps/web/src/app/core/core-contract.ts` mirrors only `chatRng` (:335)
  and `chatUpdateToolSettings` (:357).** The other nine P4.9E3A verbs
  (`types.rs:2517–2610`) must be mirrored first, plus E3B's six §1 verbs.
  Diff the mirror name-for-name against `types.rs` — the P4.19-round §1 wire
  diff caught a live-breaking field-name drift exactly here.
- **Sidebar:** `chat/sidebar/chat-section.ts` (494 LOC; deferral block
  :67–89 — Tools… refuses BY NAME :77–83 with a spec pin at
  `chat-section.spec.ts:278–282`; Run Tool… :84–85; Project REDUCED
  :86–89), `organize-section.ts` (90 LOC; deferrals :15–33 — Rename :21–26,
  Continue Elsewhere :27 [NOT this round — the continue-chat flow is a
  separate unported subsystem; leave the note], Merge In… :28–29, Export
  :30–31). ⚠ **There is no `edit-section.ts`** — `SidebarSectionId` declares
  `'edit'` (`chat-sidebar.ts:53`) but nothing renders it; Replace and Bulk
  Replace need a NEW section component in v4's order (Replace :1624–1634,
  Bulk Replace :1636–1646, icon `swap`, title "Bulk re-attribute messages
  between characters").
- **Dialog conventions:** `qt-modal` (`ui/modal.ts`, 68 LOC — v4's BaseModal:
  `maxWidth` md…3xl, `closeOnBackdrop`, footer slot) for simple forms;
  hand-rolled `qt-dialog-overlay` for multi-step/nested chrome (the
  `add-character-dialog.ts` precedent, 591 LOC — its :60–64 nested-dialog
  click-outside suppression is directly relevant to Merge's OutfitSelector
  and RunTool's two phases). House docstring style: v4 file+LOC citation, a
  "ported exactly, and why" list with v4 line refs, then deferrals.
  Location by feature (`chat/…`, not a dialogs folder); standalone
  components, OnPush, signals.
- **Entry points:** Chat section gets Tools… (un-refuse the existing entry),
  Run Tool…, the agent-mode badge, and Project upgraded from the REDUCED
  nav-link to `ChatProjectModal`. Organize gets Rename, Merge In…, Export.
  The new Edit Content section gets Replace + Bulk Replace. The composer
  gutter gets Library file (`ComposerGutterTools.tsx:93` analog — v5's
  composer gutter already hosts RNG/announcement/mail). The message action
  bar gets Reattribute. AllLLMPause + SelectLLMProfile are event-driven
  (pause threshold trip; stop-impersonation), not menu items.
- **Workspace hosting: all eleven are overlay-only.** None appears in
  `workspace-intent.ts:20–45`'s tab kinds; no `asTab` work. They render
  inline in `salon-conversation.ts`'s dialog block gated on local signals.
- **AllLLMPause dependencies to verify at lane start:** that v5's chat read
  projects `allLLMPauseTurnCount` and that the turn machinery increments it
  (v4 writes it via `chats-impersonation.ops.ts:131–133`). If v5's server
  never increments it, the modal can never trip — that is a cross-lane
  escalation to record, not a fix to make here. Port the pure threshold
  helpers (108 LOC) with unit tests either way.

### e2e layout (for the beats)

Flat specs in `apps/web/e2e/`, filename-ordered, `workers: 1`,
`fullyParallel: false`; `foundation.spec.ts` first, `zz-…`/`zzz-…`
destructive specs last — do not add a spec that sorts after
`zzz-restore-destructive.spec.ts`. Support helpers in `e2e/support/`
(`openSidebarSection` in `sidebar.ts` — use it). The gating precedent is a
module-level named constant + `test.skip`
(`zz-delete-all-destructive.spec.ts:34–40`, `DELETE_ALL_SERVER_LANDED`) —
use the same shape for the ACTIVATE-AT-UNIFY beats (e.g.
`TOOLS_INVENTORY_LANDED`, `SEARCH_REPLACE_LANDED`, `CHAT_EXPORT_LANDED`),
`false` on the lane branch, flipped by the unifier.

⚠ **Real-spend guard:** the e2e instance has no API keys by design.
Regenerate Title is one cheap-LLM call per press in production — the beat
pins the no-key arm (error toast + toggle revert, zero spend), the
avatar-preview precedent. Same for any beat that could reach `llm_choose`.

## Tiered deliverables

### Tier 1 — must land (live verbs, no E3B dependency)

1. **The contract mirror**: the nine missing P4.9E3A request interfaces +
   union members in `core-contract.ts`, name-for-name against
   `types.rs:2517–2610` (including the tri-state `enabled`), plus E3B's six
   §1 verbs from the §1 block below.
2. **The agent-mode badge** — v4's two-state toggle, exact classes/copy,
   optimistic flip with read-back.
3. **`ChatRenameModal`** — including the automatic-naming checkbox
   (regenerate-title finally reachable), error-revert, auto-close.
4. **`MergeConversationModal`** — both steps; the outfit-summary read is
   E3B's `ChatOutfitSummary` (ACTIVATE-AT-UNIFY for the step-2 beat; the
   dialog itself can land with the pick step live since the merge verb is
   already live).
5. **`BulkCharacterReplaceModal`** + the new **Edit Content section**.
6. **`ChatProjectModal`** — and retire the REDUCED Project nav-link note.
7. **The Export button** — navigation to E3B's byte URL
   (`?action=export`), ACTIVATE-AT-UNIFY beat asserting content-type +
   first JSONL line.
8. **`AllLLMPauseModal` + `SelectLLMProfileDialog`** — the pure threshold
   helpers ported with unit tests; wired to chat-PUT `isPaused`, the
   impersonate/stop-impersonate verbs, `ConnectionProfileList`.
9. **`ReattributeMessageDialog`** on the message action bar —
   ACTIVATE-AT-UNIFY over E3B's `MessageReattribute`.
10. **The full-suite obligations**: the whole Playwright run green, including
    executing `zzz-restore-destructive.spec.ts` unmodified (the P4.d23 owed
    re-run — record pass/fail in the lane record).

### Tier 2 — should land (over E3B's §1 verbs; ACTIVATE-AT-UNIFY beats)

11. **`ToolSettingsContent`** (its own unit: tri-state tree, glob patterns,
    availability display) + **`ChatToolSettingsModal`** (un-refuse the
    Tools… entry and its spec pin) + the `allowToolUse` warning box.
12. **`RunToolModal`** — both phases, reusing the Workbench schema-form
    family where it fits.
13. **`SearchReplaceModal`** — both steps over
    `SearchReplacePreview`/`Execute`.

### Tier 3 — explicit deferrals (loud, named)

- **`LibraryFilePickerModal`** (616 LOC, six endpoints) — land it if the
  round has room, with the `attach-mount-file` leg degrading loudly if E3B's
  tier-2 item 8 stays deferred; otherwise defer the whole picker by name
  (the composer-gutter entry stays absent, the m6 row stays MISSING with the
  reason).
- **Continue Elsewhere** (organize-section :27) — a separate unported
  subsystem; not this round; keep the note.
- **SummonFromLoreModal** — stays deferred (blocked on `AIImportWizard`,
  703 LOC; already recorded in m6).

## The differential requirement, restated

SPA lanes verify by **unit tests + live e2e beats** rather than oracle
differentials (the P4.9E1B/P4.9E2B precedent): every dialog gets component
tests pinning v4's behaviors (the checkbox side-effects, the sentinel
`UNASSIGNED_USER` → null, the threshold sequence 3/6/12/24/48, the two-state
badge over the tri-state verb), and each user-reachable flow gets a Playwright
beat. Mutation-check the load-bearing assertions (`p4.9e1a` lesson: an
assertion that passes with the check disabled proves nothing). Memory notes
that apply: `e2e-playwright-traps` (read BEFORE diagnosing any e2e failure),
`markdown-field-load-must-not-emit` (if any dialog hosts a markdown field).

## Fixtures

- **Consumes:** the committed e2e instance (`apps/web/e2e` fixtures). If a
  beat needs seeded data (a multi-participant chat with reattributable
  messages; an equipped outfit for merge step 2), extend the e2e seed
  helpers in `e2e/support/` — do NOT touch `crates/**` fixture families.
- **Delivers:** no Rust fixtures.

## Shared contract (BINDING)

### §1 — the wire surface (written verbatim and identically in P4.9E3B and P4.9E3C)

The six request variants below are P4.9E3B's to implement and this lane's to
consume; the TS mirror must match them name-for-name.

```rust
/// The tool inventory (v4 `GET /api/v1/tools`, app/api/v1/tools/route.ts:429).
#[serde(rename_all = "camelCase")]
ToolsList {
    #[serde(default)] chat_id: Option<String>,
    #[serde(default)] include_schemas: Option<bool>,
},
/// Chat transcript export as SillyTavern JSONL (v4 `GET …/chats/{id}?action=export`).
#[serde(rename_all = "camelCase")]
ChatExport { chat_id: String },
/// Per-character equipped-outfit summary (v4 `GET …/chats/{id}?action=outfit-summary`).
#[serde(rename_all = "camelCase")]
ChatOutfitSummary { chat_id: String },
/// Search & replace dry-run (v4 `POST /api/v1/search-replace?action=preview`).
/// `scope` is v4's discriminated union: {"type":"chat","chatId":…} |
/// {"type":"character","characterId":…}. The two include flags default TRUE
/// (v4 prefaults) — absent means true, not false.
#[serde(rename_all = "camelCase")]
SearchReplacePreview {
    scope: serde_json::Value,
    search_text: String,
    replace_text: String,
    #[serde(default)] include_messages: Option<bool>,
    #[serde(default)] include_memories: Option<bool>,
},
/// Search & replace execute (v4 `POST /api/v1/search-replace?action=execute`).
#[serde(rename_all = "camelCase")]
SearchReplaceExecute {
    scope: serde_json::Value,
    search_text: String,
    replace_text: String,
    #[serde(default)] include_messages: Option<bool>,
    #[serde(default)] include_memories: Option<bool>,
},
/// Re-attribute ONE message (v4 `POST /api/v1/messages/{id}?action=reattribute`).
/// Also deletes every memory sourced from the message (v4 route.ts:342+).
#[serde(rename_all = "camelCase")]
MessageReattribute { message_id: String, new_participant_id: String },
```

**Binding notes.**
- **Two REST edges are part of the contract**:
  `GET /api/v1/tools?chatId=…&includeSchemas=true` (query-param names exactly
  as written) and `GET /api/v1/chats/{chat_id}?action=export` (byte download,
  `application/x-ndjson`, `Content-Disposition: attachment;
  filename="{name}_chat_{ms}.jsonl"`). The Export button navigates via
  `apiUrl()` to the second.
- Response bodies are not pinned; consume what the E3B branch's differential
  proves (read its lane record at unification if a body shape is unclear —
  do not guess field names into the mirror).
- If any shape must change, it is a cross-lane escalation — record it, do not
  change it unilaterally.

### §2 — shared files

None — this lane owns `apps/web/**` outright. `CHANGELOG` and
`status-log.md` are append-only and union-merge.

## Ownership (BINDING — identical in all three orders)

| Lane | Owns exclusively |
| --- | --- |
| **P4.6BL** | `services/embedding_generate_job.rs`, `db/embedding_status.rs`, the `update_embedding` additions, its memory-service files, `engine.rs:2661–2668`, its spine region, its harness families + fixture family |
| **P4.9E3B** | the tools-inventory + search-replace + export + outfit-summary + message-reattribute server code, `services/chat_participants.rs`, `services/chat_merge.rs`, `services/outfit_selections.rs`, `db/chats.rs`, `api/salon.rs`, `quilltap-web`'s `wardrobe_routes.rs` + new `tools_routes.rs`, its §1 regions + spine region, its harness families, `chat-dialogs-{main,mount}.db` |
| **P4.9E3C** | **all of `apps/web/**`** |
| **P4.D24** | `api/custom_tools.rs`, `pascal_custom_tools_route_equivalence` + its oracle case/fixture builder, the `CLAUDE.md`/`phase-4.md` baseline paragraphs |

**Version bumps.** This lane bumps the SPA version (`apps/web/package.json`)
per the commit checklist; no crate bumps.

> Per `.claude/commands/commit.md` every commit carries a version bump for
> what it touches.

## Verification gate (run before declaring done)

1. `npx ng test` (the full run, headless) — all new component/unit tests in.
2. `npx ng build` clean.
3. **Full Playwright** — zero unexplained skips; the ACTIVATE-AT-UNIFY beats
   present and gated on their named constants (`false` on the lane branch);
   `zzz-restore-destructive.spec.ts` executed unmodified with the result
   recorded.
4. No Rust touched → no cargo gate owed; if you believe you need a Rust
   change, that is a cross-lane escalation.

Per-commit rule: follow `.claude/commands/commit.md`.

## Records to leave behind

- A lane record in `status-log.md`: what landed, per-dialog v4 fidelity notes,
  every deferral by name with its v4 file:line, which beats are
  ACTIVATE-AT-UNIFY and their constants, and the restore-beat result.
- **`m6-screen-parity.md` corrections** (this lane's docs duty, surveyed
  2026-07-27): strike the five DUPLICATED rows at §2.2 :470–474
  (MergeConversation/Reattribute/BulkReplace/RunTool/ChatToolSettings each
  appear twice); fix the WRONG annotation on ReattributeMessageDialog (its
  server half is `MessageReattribute`, NOT `ChatBulkReattribute`); then move
  each row this lane lands to DONE, and update the §1.2 agent-mode row.
- Update `chat-section.ts`/`organize-section.ts` deferral comments as entries
  go live (the finding-#34 lesson: stale comments understate remaining work).
- Update this order's status header.
