# Work order P4.D198 — v4 `bcd7e4852` (bug 151, "send a model the picture, not the archive of it"), the LOADER half: the transport-vs-storage image budget as ONE core module (`files/llm_image_budget.rs` — `LLM_TRANSPORT_MAX_EDGE = 1024`, `LLM_TRANSPORT_TARGET_BASE64 = 500 KiB`, the `[78, 65, 55, 45]` WebP quality ladder, `LANTERN_IMAGE_BASE64_BUDGET = 2 MiB`, `shrink_image_for_llm_transport` never throwing and never refusing), the shrink primitive added to the EXISTING `ImageTranscoder` seam (a `Result`, so "threw" and "nothing to do" never collapse), the host codec implementing it over `image`/`webp`, the shrink run FIRST at BOTH attachment loaders in `services/chat_files.rs` with `resize_image_for_provider` kept as the backstop, v4's WARN + DEBUG lines capture-pinned, a NEW tier-1 family over v4's REAL `shrinkImageForLlmTransport` with `sharp` scripted below it, and `file_attachment_tier3` grown to SEE the loaders' shrink — from `main`

**Status: OPEN.** Ordered 2026-09-17 as part of the `bcd7e4852` bug-151 drift
catch-up + follow-ups round (P4.D198 ∥ P4.D199 ∥ P4.96 ∥ P4.97). The lane
updates this header at lane close; the unifier rewrites it at unification.

## Preamble

- **v4 oracle baseline: `5f0a57dc4`.** This lane absorbs the LOADER half of
  **`bcd7e4852`** ("fix(images): send a model the picture, not the archive of
  it (bug 151)", 2026-09-17 10:15, `4.10.0-dev.45`): `lib/files/llm-image-
  budget.ts` (NEW, 191), `lib/chat-files-v2.ts` (+48/−?: both load paths),
  and the 211-line `__tests__/unit/lib/files/llm-image-budget.test.ts`
  (transcribed as corpus rows where its shape allows — see Tier 1 item 6).
  Its `lib/services/chat-message/context-builder.service.ts` hunk (the
  Lantern walk's per-turn budget) and its `help/connection-profiles.md` hunk
  are **P4.D199's**; its `CLAUDE.md` rule line, `README.md` badge, the
  version stamps (`package.json`, `package-lock.json`, `packages/quilltap/
  package.json`), `docs/developer/bugs.md`, `docs/developer/bugs/fixed/bug-
  151-unbudgeted-image-bytes-on-the-wire.md`, `docs/CHANGELOG.md` and
  `.claude/commands/update-documentation.md` are P4.D199's ratification list.
  The ledger row is `ORDERED(P4.D198, P4.D199)`. Spec reads: `git show
  bcd7e4852:<path>` for the post-commit files and `git show bcd7e4852 --
  <path>` for the hunks — **never the commit message** (§R.4; the message
  says "caps the long edge at 1024 and steps a quality ladder down to a 500
  KB base64 ceiling", but the FILE also carries the early return for an
  already-small input, the grow-discard rule, the provider-ceiling `min`,
  and the `mimeType.startsWith('image/') || !canResizeImage` pass-through —
  only the file says which arm is which).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** Pin **`/tmp/qt-v4-pin-p4d198-
  bcd7e4852`** (the TARGET — v4 HEAD) for every family this port MOVES:
  the NEW `llm_image_budget_equivalence` (its oracle imports the new
  module, which does not exist at the baseline) and `file_attachment_tier3`.
  Pin **`/tmp/qt-v4-pin-p4d198-5f0a57dc4`** (the BASELINE) for the
  neutrality leg (`file_attachment_tier3`'s PRE-EXISTING rows — every corpus
  image is small, so the shrink's early return keeps them byte-identical;
  prove it by regenerating at BOTH pins and diffing the pre-existing rows).
  Pin verification: the target-pinned `file_attachment_tier3` NDJSON carries
  the new shrink rows' `image/webp` output and the baseline one cannot
  (`grep -c` the new row labels: > 0 at the target, 0 at the baseline).
  ⚠ All three symlink classes (ledger §5.1) — the case imports the real
  provider registry.
- **Sibling lanes:** P4.D199 (the WALK half — it budgets the attachments
  your loaders return, reading ONLY the `data` string's length; it owns
  `services/message_context.rs`, the `orchestrator_tier3` /
  `build_context_tier3` families, and `help/**`; §S.1), P4.96 (the
  image-profile generate route's five fields — `api/types.rs`,
  `api/engine.rs`, the web edge; you touch none of those), P4.97 (the
  tool-unsupported retry + the request-envelopes cache-key pin —
  `model/primary_stream.rs` and the recorded provider corpora; you touch
  none of those).
- **Worktree:** `git worktree add <path> -b claude/p4-d198-llm-image-
  budget-<hash> main`; `CARGO_INCREMENTAL=0`. No SPA work.
- **Read before the work:** `crates/quilltap-core/src/files/image_
  processing.rs` WHOLE (the `ImageTranscoder` trait `:70-89` — `metadata`
  + `resize_step`, INFALLIBLE by shape; `NotConfiguredTranscoder` `:93-110`;
  `can_resize_image` `:112`; `calculate_base64_size` `:119` —
  `(len*4 + 2)/3`, which equals v4's `Math.ceil(len*4/3)` for every `len`
  — assert it in the new module's tests rather than trusting this
  sentence; `get_provider_max_base64_size` `:131`; `resize_image_for_
  provider` `:182-188` + `ImageResizeResult` `:163-172`; the `HalvingFake`
  unit fake `:342`); `crates/quilltap-core/src/services/chat_files.rs`
  `:51-66` (`LoadChatFilesOptions` + `with_provider`), `:73-127`
  (`read_file_as_base64` — PRIVATE, `(String, String)` return, the
  `wasResized` drop recorded at `:73-77`), `:134-259` (`load_mount_file_as_
  attachment` — PRIVATE, returns a `Value` with `size` = the POST-resize
  `buffer.len()` at `:255`), `:275-355` (`load_chat_files_for_llm` — the
  `auto_resize` collapse at `:288-292`), `:469-527` (`load_lantern_images`
  — the per-file fallback loop P4.D199 budgets AFTER; you do not touch its
  ordering), `:586-630` (`RealMessageContextSeams` — both methods ignore the
  seam's `provider` and re-read the profile's, a recorded shape; keep it),
  `:701-796` (the P4.73 upload transcode — the `PixelCodec` precedent for a
  host-side pixel op behind a core policy); `crates/quilltap-host/src/
  image_codec.rs` WHOLE (`HostImageCodec` — ONE type implementing THREE
  traits incl. `ImageTranscoder` at `:103+`; the module doc `:1-34` records
  the degradations: `resize_step` returns the ORIGINAL bytes on failure
  because the trait shape is infallible — the exact collapse this order
  forbids for the shrink); `crates/quilltap-harness/tests/file_attachment_
  tier3_equivalence.rs` WHOLE + `harness/oracle/cases/file-attachment-
  tier3.test.ts` WHOLE (the `downloadFile` byte mock, the "No `sharp` mock"
  note at `:25-27` that this lane retires by adding a SCRIPTED one) +
  `harness/oracle/fixtures/build-file-attachment-fixture.ts` +
  `file-attachment.json`; `crates/quilltap-harness/tests/chat_upload_codec_
  wiring.rs` (the composition-pin idiom for "which codec does production
  actually reach"); `crates/quilltap-harness/src/lib.rs:217-238`
  (`PrefixingPixelCodec` — the harness's byte-changing codec idiom);
  `services/file_fallback.rs:940-963` (the `try_downsize` NOT-PORTED warn —
  the `Option` collapse of "threw" and "nothing to do" that
  `handler-logging-inventory.md` records; the shrink seam returns a
  `Result` precisely so this order does not repeat it).

## Mandate

Land bug 151's per-image transport budget end to end on the server: the
pure decision (`shrink_image_for_llm_transport` — the early return, the
ceiling `min`, the ladder walk, the grow-discard, the never-throw arms) as
ONE core module byte-faithful to `lib/files/llm-image-budget.ts`; the pixel
primitive it needs (fit-inside resize to a max edge, never enlarging, WebP
re-encode at a given quality, FALLIBLE) as a new method on the EXISTING
`ImageTranscoder` seam with a default `Err` so every existing implementor
compiles; the host codec's implementation over `image` + `webp`; the shrink
run FIRST at both loaders in `chat_files.rs` with the provider-ceiling
resize kept behind it as the backstop exactly as v4's hunks lay it out; the
two log lines; a NEW tier-1 family over v4's REAL function with `sharp`
scripted below it; and `file_attachment_tier3` grown so the loaders' shrink
is a comparand. This lane does NOT touch `services/message_context.rs`
(P4.D199's — the per-turn `LANTERN_IMAGE_BASE64_BUDGET` walk; you DEFINE the
constant in the budget module because v4 defines it there, and P4.D199
imports it — §S.1), `help/**` (P4.D199's), `api/types.rs` / `api/engine.rs`
/ `crates/quilltap-web/**` (P4.96's), `model/primary_stream.rs` and the
recorded provider corpora (P4.97's), `services/orchestrator.rs` (nobody's
this round — if the wiring pin in Tier 1 item 7 shows the production
transcoder reaching `RealMessageContextSeams` is NOT `HostImageCodec`, STOP
on that item and record the shape for the unifier; do not edit that file),
or the drift ledger.

## Survey-verified starting points (fresh survey 2026-09-17 at `bcd7e4852` and on `main` `78884832` — do not re-derive)

### v4 (at `bcd7e4852`)

- **`lib/files/llm-image-budget.ts`** (NEW, 191 lines — read it WHOLE; the
  module doc is the port's why-comment). Exports: `LLM_TRANSPORT_MAX_EDGE =
  1024`; `LLM_TRANSPORT_TARGET_BASE64 = 500 * 1024`; `LLM_TRANSPORT_QUALITY_
  LADDER = [78, 65, 55, 45] as const`; `LANTERN_IMAGE_BASE64_BUDGET = 2 *
  1024 * 1024`; `interface LlmImageShrinkResult { buffer, mimeType,
  wasShrunk, originalSize, finalSize, width?, height? }`; `async function
  shrinkImageForLlmTransport({ buffer, mimeType, provider?, filename? })`.
  The arms, in order: (1) `!mimeType.startsWith('image/') ||
  !canResizeImage(mimeType)` → `unchanged` (no log); (2) `ceiling = provider
  ? min(TARGET, getProviderMaxBase64Size(provider)) : TARGET`; (3) inside
  `try`: `metadata()` → `longestEdge = max(width ?? 0, height ?? 0)`; (4)
  `longestEdge > 0 && longestEdge <= MAX_EDGE && calculateBase64Size(buffer)
  <= ceiling` → `unchanged` (no log); (5) the ladder: for each quality,
  `resize({width: 1024, height: 1024, fit: 'inside', withoutEnlargement:
  true}).webp({quality}).toBuffer()`, `best = encoded`, break when
  `calculateBase64Size(encoded) <= ceiling` — so `best` is the LAST rung
  tried, and an image that never fits is sent at rung 45; (6) `!best` →
  `unchanged` (unreachable with a non-empty ladder — note it, do not
  invent a case); (7) `best.length >= originalSize && longestEdge <=
  MAX_EDGE` → `unchanged` (the grow-discard; NOTE the second conjunct — an
  oversize-edge input is ALWAYS taken even when the encode grew); (8)
  `finalMeta = metadata(best)`; `logger.debug('Image shrunk for LLM
  transport', { module: 'files:llm-image-budget', filename, provider,
  originalSize, finalSize: best.length, originalDimensions: `${w}x${h}`,
  finalDimensions: `${fw}x${fh}`, ceiling })`; return `{ buffer: best,
  mimeType: 'image/webp', wasShrunk: true, originalSize, finalSize:
  best.length, width: finalMeta.width, height: finalMeta.height }`; (9)
  `catch` → `logger.warn('Could not shrink image for LLM transport; sending
  stored bytes', { module: 'files:llm-image-budget', filename, provider,
  mimeType, error })` → `unchanged`. A thrown `metadata()` (undecodable
  bytes) lands in (9); so does a thrown encode.
- **`lib/chat-files-v2.ts`** — `readFileAsBase64(fileId, mimeType,
  provider?)` `:463-`: after `downloadFile`, `if (provider &&
  mimeType.startsWith('image/'))` → `shrinkImageForLlmTransport({ buffer,
  mimeType, provider, filename: entry.originalFilename })`; `if
  (shrunk.wasShrunk) { buffer = shrunk.buffer; outputMimeType =
  shrunk.mimeType; wasResized = true }`. THEN the pre-existing backstop,
  now gated on `outputMimeType` (`if (provider && outputMimeType.startsWith
  ('image/') && canResizeImage(outputMimeType))`) and passing `mimeType:
  outputMimeType` into `resizeImageForProvider`. `loadMountFileAsAttachment
  (mountFileId, options)` `:561-`: `if (autoResize && provider &&
  outputMimeType.startsWith('image/'))` → the same shrink with `filename:
  mountLink.originalFileName ?? mountLink.fileName`; then the backstop
  gated on `outputMimeType`. NOTE the gate asymmetry v4 keeps: the shrink
  gate has NO `canResizeImage` (the module checks it internally and
  returns `unchanged`); the backstop gate DOES. `loadChatFilesForLLM`
  `:685-` passes `autoResize ? provider : undefined` into
  `readFileAsBase64` (`:700`) — so `autoResize: false` reaches the legacy
  path as `provider = undefined` and the mount path as `autoResize =
  false`; both skip the shrink AND the backstop.
- **`lib/files/image-processing.ts`**: `DEFAULT_MAX_BASE64_SIZE = 4 * 1024 *
  1024` `:18`; `RESIZABLE_MIME_TYPES` `:53` (jpeg/jpg/png/webp/gif);
  `canResizeImage` `:64` (lowercased membership); `calculateBase64Size` `:72`
  (`Math.ceil(buffer.length * 4 / 3)`); `getProviderMaxBase64Size` `:81`
  (`getAttachmentSupport(provider)?.maxBase64Size ?? DEFAULT`) — UNCHANGED
  by the drift.
- **`__tests__/unit/lib/files/llm-image-budget.test.ts`** (211 lines) runs
  against REAL sharp on noise images (`sharp({create: {…, noise: {type:
  'gaussian', mean: 128, sigma: 70}}}).webp({quality: 90})`) and mocks
  `getAttachmentSupport` to `undefined` (so every provider ceiling is the
  4 MB default) — its eight cases: the unfixed-path pin (`resizeImageFor
  Provider` on a 1024x1536 avatar reports `wasResized: false`), portrait →
  683x1024, landscape → 1024x683, ≤ 500 KiB, two shrunk avatars ≤ 2 MiB,
  small-image `unchanged` with `buffer` identity, SVG pass-through, junk
  bytes pass-through, and a below-target provider ceiling (`maxBase64Size:
  8 * 1024`) driving the ladder to its bottom. **These are dimension/byte
  MEASUREMENTS of a specific encoder, not oracle rows** — see Tier 1 item 6
  for which become corpus rows (scripted-encoder rows) and which become a
  host-side unit test over the real `image`/`webp` crates.
- **The `docs/developer/bugs/fixed/bug-151-…md`** measurement table: two
  1024x1536 WebP avatars at 1,757,654 B (2.23 MB base64) and 1,796,172 B
  (2.28 MB base64), each under the 4 MB per-image cap, 4.52 MB together;
  the fix took the first to ~98 KB of base64.

### v5 (on `main` `78884832`)

- **`crates/quilltap-core/src/files/image_processing.rs`** (393 lines):
  `pub trait ImageTranscoder { fn metadata(&self, buffer: &[u8]) ->
  ImageMetadata; fn resize_step(&self, buffer: &[u8], target_width: i64,
  format: OutputFormat, quality: i64) -> Vec<u8>; }` `:70-89`;
  `ImageMetadata { width: Option<i64>, height: Option<i64>, has_alpha: bool
  }` `:55-62`; `NotConfiguredTranscoder` `:93-110` (no dims, returns input);
  `can_resize_image` `:112`; `calculate_base64_size(len: usize) -> i64`
  `:119`; `get_provider_max_base64_size(provider: &str) -> i64` `:131`
  (reads `provider_manifest::Registry::built_in().attachment_support
  (provider).max_base64_size`); `resize_image_for_provider(provider,
  buffer, mime_type, quality, transcoder) -> ImageResizeResult` `:182-188`
  with the two early returns `:192-215` and the ×0.8 / 10-iteration loop
  from `:217`; `DEFAULT_QUALITY = 85` `:175`. **No `tracing::` call in the
  file.** Core has NO image crate (`quilltap-core/Cargo.toml` — measured);
  the doc at `:4-8` records the host-seam rule.
- **`crates/quilltap-core/src/services/chat_files.rs`** (1183 lines):
  `read_file_as_base64(db, bytes: &dyn FileBytesStore, transcoder: &dyn
  ImageTranscoder, file_id, mime_type, provider: Option<&str>) ->
  Result<(String, String), String>` `:78-127` — the resize gate `:105-121`
  calls `resize_image_for_provider(provider, &buffer, mime_type,
  DEFAULT_QUALITY, transcoder)` at `:111-117`; `load_mount_file_as_
  attachment(db, transcoder, mount_file_id, options: &LoadChatFilesOptions)
  -> Option<Value>` `:134-259` — the gate `:222-241`, the call `:230-236`,
  `size` = post-resize `buffer.len()` `:255`; `load_chat_files_for_llm(db,
  bytes, transcoder, file_ids, options)` `:275-355` — the ONLY caller of
  both (`:297-304`, `:326`); its three production callers are IN THIS FILE
  (`load_and_process_files:417`, `load_lantern_images:476`,
  `load_user_attachments:537`) plus the harness family at
  `file_attachment_tier3_equivalence.rs:732`. **The transcoder the loaders
  receive already flows in from the caller** — so adding the shrink
  primitive to `ImageTranscoder` needs NO signature change anywhere.
  The one `tracing::warn!` in the file (`:1097-1103`) uses a `context =
  "chat-files-v2"` FIELD, not a `target:`; the image-adjacent house style
  is `target: "quilltap::image_fallback"` + a `[Bracketed]` message
  (`file_fallback.rs`, 13 sites).
- **`crates/quilltap-host/src/image_codec.rs`**: `pub struct
  HostImageCodec;` `:53`; `impl PixelCodec` `:74-101`; `impl
  quilltap_core::files::image_processing::ImageTranscoder` from `:103`
  (aliased `ResizeTranscoder`); `impl quilltap_core::model::image::
  ImageTranscoder` too. Crates: `image = "0.25"`, `webp = "0.3"` (default
  features; lossy WebP ENCODE is the `webp` crate — libwebp bindings, the
  same library sharp links; animated GIF → first frame; AVIF/HEIC decode
  not wired). The module doc `:20-34` records that `resize_step` returns
  the ORIGINAL bytes on any failure because the trait is infallible.
  Existing host tests at `:229`, `:362-398` call `resize_image_for_
  provider` against the real codec — the precedent for a real-bytes unit
  test.
- **Production wiring of the loaders' transcoder:** `RealMessageContextSeams`
  (`chat_files.rs:586-630`) is constructed at `services/orchestrator.rs:
  2512-2513` from the orchestrator's deps; `load_and_process_files` and the
  two seam methods pass `deps.transcoder`. Which concrete type reaches it in
  production is a MEASUREMENT this lane makes (Tier 1 item 7) — the
  `chat_upload_codec_wiring.rs` precedent proves the upload arm reaches
  `HostImageCodec`; nothing today pins the LOADER arm's transcoder.
- **Harness:** `file_attachment_tier3_equivalence.rs` — test `file_
  attachment_matches_oracle` `:313`, env `QT_ORACLE_FILE_ATTACHMENT` +
  `QT_FIXTURE_FILE_ATTACH_{MAIN,MOUNT}`, the family calls
  `load_chat_files_for_llm` with `NotConfiguredTranscoder` (`:74`); the
  oracle case's `downloadFile` mock serves `spec.files[].bytes` per fileId;
  corpus `harness/oracle/fixtures/file-attachment.json` (every image
  `image/png`, all small). Harness fakes: `PrefixingPixelCodec`
  (`quilltap-harness/src/lib.rs:217-238`, a `PixelCodec`); **no
  harness-visible fake `ImageTranscoder` exists** (`HalvingFake` is
  core-test-private) — you add one. **No tier-1 family drives
  `image_processing` today** (the resize decision is unit-tested only);
  `image_transport_equivalence` is the transport PREDICATE, a different
  subject.
- **Log capture idiom:** `quilltap_core::test_support::captured_with` (the
  thread-scoped rig) — see `file_fallback.rs`'s capture tests and the P4.93
  lane record for the firing + silence leg shape.

## Tiered deliverables

### Tier 1 — must land

1. **The seam primitive.** Add to `ImageTranscoder` (`image_processing.rs`):
   `fn shrink_to_webp(&self, buffer: &[u8], max_edge: i64, quality: i64) ->
   Result<Vec<u8>, String>` — "fit inside `max_edge`×`max_edge`, never
   enlarge, re-encode as WebP at `quality`"; **a default body returning
   `Err("image transcoder not configured")`** so `NotConfiguredTranscoder`,
   every harness fake and every existing `impl` compile unchanged. The
   `Result` is the point: v4's `catch` is ONE arm for "sharp threw" and the
   port must be able to distinguish "threw" from "did nothing" (the
   `try_downsize` collapse at `file_fallback.rs:940-963` is the recorded
   anti-pattern). `metadata()` stays as it is (v4's thrown `metadata()`
   maps to v5's `ImageMetadata { width: None, height: None }` → `longest_
   edge = 0` → the early return's `longestEdge > 0` conjunct fails → the
   ladder runs → the FIRST `shrink_to_webp` `Err` lands in the warn arm;
   record this mapping in the module doc — it is the one place v4's
   control flow and v5's differ in mechanism while agreeing in outcome).
2. **The host implementation.** `HostImageCodec::shrink_to_webp` in
   `quilltap-host/src/image_codec.rs`: decode with `image`, `resize` with
   `fit: inside` semantics (`image::imageops::FilterType::Lanczos3` is
   sharp's default kernel — say which you used and why; D19 stands: byte
   parity of encoded pixels is NOT required), skip the resize when both
   edges are already ≤ `max_edge` (v4's `withoutEnlargement: true` +
   `fit: 'inside'`), encode with `webp::Encoder::from_image(...).encode
   (quality)`. A decode or encode failure is `Err(message)` — NEVER the
   input bytes. A host unit test over a synthesized 1024×1536 noise image
   (the v4 test's shape, `image` can build it) asserts: `wasShrunk`, the
   long edge = 1024, 683×1024, base64 ≤ 500 KiB (or the ladder bottom
   reached — assert whichever the real encoder gives and RECORD the
   measured sizes in the lane record beside v4's 98 KB), and the landscape
   1536×1024 → 1024×683 twin. Plus v4's "two shrunk avatars ≤ 2 MiB" as a
   host test (it is the bug's own reproduction).
3. **The core module** `crates/quilltap-core/src/files/llm_image_budget.rs`
   (+ `pub mod` in `files/mod.rs`): the four constants with v4's doc
   comments carried as why-comments; `pub struct LlmImageShrinkResult {
   buffer: Vec<u8>, mime_type: String, was_shrunk: bool, original_size:
   usize, final_size: usize, width: Option<i64>, height: Option<i64> }`;
   `pub fn shrink_image_for_llm_transport(transcoder: &dyn ImageTranscoder,
   buffer: &[u8], mime_type: &str, provider: Option<&str>, filename:
   Option<&str>) -> LlmImageShrinkResult` — the nine arms above IN v4's
   ORDER, `unchanged` returning the input bytes (a clone is fine; pin
   `was_shrunk == false` + `final_size == original_size`), `ceiling` as
   `i64` through `get_provider_max_base64_size`, the ladder over the
   `const` slice, `best` = the LAST rung tried, the grow-discard with BOTH
   conjuncts, and the two log lines at v4's levels with v4's field bags
   (`module = "files:llm-image-budget"` as a FIELD since v4 logs it as one;
   `originalDimensions`/`finalDimensions` as the `"{w}x{h}"` strings —
   v4 renders `undefined` as the literal `undefinedxundefined` when
   metadata lacks a dimension; reproduce with `to_js_string`-style
   rendering, pinned). Capture-pin both lines with a firing leg AND a
   silence leg each (four tests; the silence legs need a scripted
   transcoder that answers `metadata` — with `NotConfiguredTranscoder` the
   warn fires by design, because "not configured" IS the unresizable arm;
   say so in the test's doc).
4. **The two loaders** (`chat_files.rs`): the shrink FIRST, in v4's exact
   gate shapes — legacy path `if let Some(provider) = provider { if
   mime_type.starts_with("image/") { … } }` (no `can_resize_image` on the
   shrink gate; the module checks it), mount path `if auto_resize { if let
   Some(provider) … if output_mime_type.starts_with("image/") … }`; on
   `was_shrunk` replace `buffer` and `output_mime_type` (`image/webp`);
   then the EXISTING backstop re-gated on `output_mime_type` and passing
   `output_mime_type` into `resize_image_for_provider` (v4's `:521` hunk).
   `filename` for the log: `entry.originalFilename` on the legacy path;
   `mountLink.originalFileName ?? mountLink.fileName` on the mount path —
   find v5's twins of those two columns in the same function and use
   them. The mount path's `size` stays the post-processing `buffer.len()`
   (`:255`) — now post-shrink; confirm v4's `:663` region does the same
   (it does at the baseline; re-read at the target).
5. **The NEW tier-1 family `llm_image_budget_equivalence`** — `harness/
   oracle/cases/llm-image-budget.test.ts` (jest, because `sharp` must be
   `jest.doMock`ed BELOW v4's REAL `shrinkImageForLlmTransport`; a tsx
   oracle cannot mock a module import) + `crates/quilltap-harness/tests/
   llm_image_budget_equivalence.rs`, env `QT_ORACLE_LLM_IMAGE_BUDGET`. The
   scripted encoder is the whole design: the case's `sharp` mock reads a
   per-case SCRIPT — `metadata()` answers `{width, height}` from the script
   (or throws, for the undecodable arm); `.resize(...).webp({quality})
   .toBuffer()` answers a buffer of the script's `encodedLen[quality]`
   bytes filled with a deterministic pattern (`(quality + i) & 0xff`); the
   mock RECORDS the sequence of qualities asked for. The Rust side's
   `ScriptedTranscoder` (in `quilltap-harness/src/lib.rs`, beside
   `PrefixingPixelCodec`) answers the SAME script through `metadata` /
   `shrink_to_webp` (and `resize_step`, unused here but required by the
   trait) — so `buffer` bytes are byte-comparable on both sides. Rows
   (labels are yours; the SHAPES are binding): non-image mime; image mime
   outside `RESIZABLE_MIME_TYPES` (`image/svg+xml`); small-and-under-
   ceiling early return (`buffer` identity — pin `was_shrunk: false` and
   the bytes); small-but-over-ceiling (edge ≤ 1024, base64 > 500 KiB →
   the ladder runs; a `width` of 1000 and `encodedLen` that fits at rung
   65 → qualities asked `[78, 65]`, `best` = the 65 encode); oversize edge
   fitting at the FIRST rung (`[78]`); oversize edge never fitting (all
   four rungs asked, `best` = the 45 encode, `was_shrunk: true`); the
   grow-discard (edge ≤ 1024, over ceiling, every rung's `encodedLen >=
   originalSize` → `unchanged`); the grow-but-oversize-edge (edge > 1024,
   every rung grows → STILL shrunk, the second conjunct); undecodable
   (`metadata` throws → `unchanged`, the warn — on the v5 side
   `ScriptedTranscoder::metadata` answers `None`s and `shrink_to_webp`
   `Err`s; the OUTCOME is the comparand, the mechanism difference is
   recorded in item 1); encode-throws-at-rung-2 (v4: `toBuffer` rejects on
   the second call → `catch` → `unchanged`; v5: `shrink_to_webp` `Err` on
   the second call → `unchanged` — pin that a PARTIAL ladder never leaks
   `best`); the provider ceiling: two REAL providers — one whose manifest
   declares `attachmentSupport.maxBase64Size` and one that does not
   (measure the eleven manifests under `crates/quilltap-core/src/provider_
   manifest/manifests/*.json` and v4's `plugins/dist/*/manifest`; both
   sides read the SAME registry, so use real names, no mock) — and
   `provider: undefined` (the TARGET alone). The below-500-KiB provider
   ceiling that v4's unit test drives with a mock is NOT reachable through
   the real registry — record which providers declare a ceiling and its
   value, and cover the `min` arm with the declared one if any is under
   500 KiB, else with a Rust unit test over a fake registry ONLY if the
   function takes the registry injectably (it does not today — do not
   add a parameter for a test; record the gap). Comparands, exact:
   `wasShrunk`, `mimeType`, `originalSize`, `finalSize`, `width`, `height`,
   the base64 of `buffer` (byte-exact — both scripts produce it), and the
   recorded quality sequence. Count guard `>= 12`. Recipe header per
   `recipe-header-conventions` (indented commands, `--test` scope, the
   regen and run naming ONE NDJSON path, `cd ~/source/quilltap-server`
   NEVER a `/tmp` pin — the pin is the driver's `--v4`).
6. **`file_attachment_tier3` grown to SEE the loaders' shrink.** The oracle
   case gains a `jest.doMock('sharp', …)` scripted exactly as item 5's (a
   shared helper under `harness/oracle/lib/` is fine), keyed by the
   fixture's per-file script; the Rust family swaps `NotConfiguredTranscoder`
   for the `ScriptedTranscoder` over the SAME per-file script. New corpus
   rows in `file-attachment.json` (both the `loadChatFilesForLLM` legacy
   path AND the mount-blob path — v4's hunk says the mount path is "where
   every character avatar actually travels", so it is the primary arm):
   (a) an oversize `image/png` on the legacy path with a script that fits
   at rung 65 → the attachment's `mimeType` is `image/webp`, `data` is the
   base64 of the rung-65 script bytes, byte-exact; (b) the same on the
   mount path, with `size` = the shrunk length; (c) a big image whose script
   never fits → rung 45's bytes, `image/webp`; (d) the SAME oversize file
   loaded with `autoResize: false` (`LoadChatFilesOptions { provider:
   Some(..), auto_resize: false }`) → stored bytes and stored mime,
   verbatim, on BOTH paths — the arm v4's comments name; (e) a big image
   whose script makes the shrink FAIL (metadata throws) → stored bytes
   pass through, and — because it is under the 4 MB backstop — no resize
   either (pin `data` == stored). Keep every pre-existing row's script
   "small" (`metadata` under 1024, `encodedLen` irrelevant) so they are
   byte-identical at both pins — that diff is the neutrality proof AND
   the proof that the early return fires where v4's fires. The `describe`
   / vision rows are untouched (their canned keys carry the FILENAME, not
   the bytes — verify no canned row moves; if one does, the shrink
   changed a filename-keyed lookup and that is a finding).
7. **The production wiring pin.** A composition test in `quilltap-harness`
   (the `chat_upload_codec_wiring.rs` idiom — boot a real `CoreEngine`
   over a seeded instance) proving the transcoder that reaches
   `RealMessageContextSeams` / `load_and_process_files` in production is
   one whose `shrink_to_webp` is NOT the default `Err`: assemble with a
   backup host whose transcoder is byte-CHANGING and drive one turn
   carrying an oversize-scripted image through the real dispatch (or, if
   the orchestrator's deps take the transcoder from a host trait method
   like `pixel_codec()`, drive that method) and assert the stored request's
   attachment is the changed bytes. If the measurement shows the loader
   arm hands `NotConfiguredTranscoder` in production (the P4.73 class —
   "every production call site had the not-configured codec"), that is a
   FINDING: fix it ONLY if the fix lives in a file you own (`quilltap-host`
   or `chat_files.rs`); if it lives in `orchestrator.rs` or `engine.rs`,
   STOP on this item and record the exact wire for the unifier (§S.3).
8. **Neutrality:** `file_attachment_tier3`'s pre-existing rows regenerated
   at BOTH pins and diffed row-for-row (the corpus grows, so diff by label);
   `attach_mount_file_equivalence` (its header says the resize early-returns
   before the codec — confirm the shrink does too, by running it at the
   target pin: it must stay green with ZERO source change on its side);
   `cargo test -p quilltap-core files::` and the host's `image_codec` tests
   green.
9. **Mutation proofs** (each reddens exactly its test; revert by file
   backup): M1 drop the grow-discard's second conjunct (`&& longest_edge
   <= MAX_EDGE`) → the grow-but-oversize row reds; M2 `break` on the FIRST
   rung regardless of fit → the never-fits and fits-at-65 rows red; M3 run
   the shrink AFTER the backstop in the legacy loader → row (a) reds only
   if the backstop's output differs — if it does not, RECORD that the
   order of the two stages is corpus-invisible and pin it with a Rust
   unit test that counts calls (a scripted transcoder that records
   `resize_step` vs `shrink_to_webp` order); M4 shrink on `auto_resize:
   false` → row (d) reds; M5 the warn on `Ok` / the debug on `unchanged` →
   the capture legs red; M6 `shrink_to_webp` returning the input on `Err`
   in the host impl → the host failure test reds (this is the collapse the
   order forbids — prove the test sees it).

### Tier 2 — should land

10. **The `ImageResizeResult`-style `was_resized` flag on the legacy
    loader** — v4 sets `wasResized = true` when shrunk; v5 dropped the
    flag at `:73-77` as unread. Measure whether any v4 caller reads
    `wasResized` at `bcd7e4852` (`grep -n wasResized lib/`); if still
    unread, leave v5's drop as it is and record the re-measurement.
11. **`calculate_base64_size` parity pin:** a Rust unit test asserting
    `(len*4 + 2)/3 == ceil(len*4/3)` for `len` in `0..=10_000` and the
    three bug-151 sizes (1,757,654 / 1,796,172 / 3,553,826) — cheap, and
    the ceiling comparison depends on it.
12. **`combined.log` look:** in the lane record, the exact bytes the two
    lines render to through the file layer (P4.91's `…Json` convention
    applies to no field here — say so after checking).

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **The per-turn `LANTERN_IMAGE_BASE64_BUDGET` spend** is P4.D199's; this
  lane DEFINES the constant (item 3) and nothing else about it. Do not add
  a budget to `load_lantern_images`.
- **The `help/connection-profiles.md` re-vendor** is P4.D199's.
- **A real-bytes differential** (v4's sharp vs v5's `image`/`webp` on the
  same input) is NOT ordered — D19 stands (operations ported, encoded
  bytes not parity-required). The host unit test in item 2 records the
  measured sizes; the 💸 live proof (a real Lantern avatar reaching a real
  vision seat at ~1/20th the bytes, visible in `combined.log`'s debug
  line) goes to the dogfood queue.

## The differential requirement, restated

Every ported unit lands with an equivalence test that drives v4's REAL code:

- Tier 1 exact: `llm_image_budget_equivalence` — jest, `jest.doMock('sharp')`
  scripted BELOW v4's real `shrinkImageForLlmTransport` (the `jest-oracle`
  memory notes: `doMock` survives `resetModules`; register the mock BEFORE
  the first import of `@/lib/files/llm-image-budget`; the provider registry
  is REAL — no `provider-registry` mock, unlike v4's own unit test). Closest
  recipe: `harness/oracle/cases/image-transport.ts` is tsx and cannot mock;
  use the jest shape of `file-attachment-tier3.test.ts` (resetModules +
  doMock) WITHOUT its DB (this family opens no database). NDJSON: one row
  per case with the result fields + the recorded qualities; the Rust side
  replays the script and diffs exactly.
- Tier 3 mocked-codec: `file_attachment_tier3` (the existing real-DB jest
  recipe in its header; add the `sharp` doMock beside the `downloadFile`
  one). The canned vision rows are unaffected — assert it.
- Composition: item 7 (no oracle — v5's own wire).

## Fixtures

- Consumes `harness/oracle/fixtures/file-attachment.json` + `build-file-
  attachment-fixture.ts` (this lane EDITS them — new rows + a per-file
  `shrinkScript` key; the builder plants the big files' rows in `files` /
  `doc_mount_blobs` with byte counts that match the script's
  `originalSize` — the `size` column must agree with the served bytes'
  length or the mount path's `size` comparand lies). The `/tmp/qt-fa-*.db`
  pair is rebuilt by the recipe (never committed). Stage under
  `/tmp/p4.d198/` (§R.3's lane-private rule) and keep the committed recipe
  header canonical.
- Delivers: the NEW `harness/oracle/cases/llm-image-budget.test.ts` + the
  script helper + `crates/quilltap-harness/tests/llm_image_budget_
  equivalence.rs` + the `ScriptedTranscoder` in `quilltap-harness/src/
  lib.rs`; the NEW host test(s); the composition pin.
- Nobody else regenerates these families this round (P4.D199 regenerates
  `orchestrator_tier3` / `build_context_tier3`, which run the loaders with
  `NotConfiguredTranscoder` and junk bytes — the shrink `Err`s → unchanged
  on BOTH sides, so P4.D199's rows are unaffected by your landing; the
  unifier re-runs them over the union to prove it).

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `bcd7e4852` bug-151 drift catch-up + follow-ups round:
**P4.D198 ∥ P4.D199 ∥ P4.96 ∥ P4.97**, ordered 2026-09-17. Four lanes, four
worktrees, four lane branches, ALL from `main` (no stacking — every lane's
ownership is disjoint by file; the one cross-lane constant is handed off by
a marked local definition, §R.10(a)). The unifier cherry-picks in the order
**P4.D198 → P4.D199 → P4.96 → P4.97** (the loader half before the walk half
that reads its output; the two maintenance lanes after the drift), gates
ONCE, and `merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`5f0a57dc4`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`bcd7e4852`**
  ("fix(images): send a model the picture, not the archive of it (bug
  151)", `4.10.0-dev.45`, 2026-09-17 10:15) — ONE commit past the
  baseline, a PORT on ported surfaces: `lib/files/llm-image-budget.ts`
  (NEW), `lib/chat-files-v2.ts` (both attachment loaders), `lib/services/
  chat-message/context-builder.service.ts` (the Lantern walk's per-turn
  byte budget), `help/connection-profiles.md` (+12), plus tests, docs,
  v4's CLAUDE.md rule line and version stamps. The ledger's one §3 row is
  `ORDERED(P4.D198, P4.D199)`. v4 `bugfix` tip `1a2b2164c` and `release`
  tip `8fbf2afe0` are unmoved; there is still no `release: 4.10.0` squash.
  The checkout sat on `main`, CLEAN, at the planning probe (2026-09-17
  afternoon — the §2 probe PASSED, so the ledger stood and nothing was
  re-derived). **At unification the baseline MOVES to `bcd7e4852`**;
  `bcd7e4852` retires `ABSORBED(P4.D198, P4.D199)` with its non-lib files
  ratified on P4.D199's list.
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log bcd7e4852..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Pass = branch `main`, HEAD `bcd7e4852`, both logs EMPTY, tree
  CLEAN.** Any mismatch — a moved HEAD, a dirty `lib/`/`app/`/`components/`/
  `packages/`/`plugins/`/`help/`/`public/schemas/` tree, the checkout off
  `main` — is a STOP: report it and do nothing else. A lane never writes
  the ledger.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1). HEAD is past the
  baseline and `bcd7e4852` moves `lib/` and `help/`. Every regen runs from
  a LANE-UNIQUE detached worktree per ledger §5.1 (`git -C ~/source/
  quilltap-server worktree add --detach /tmp/qt-v4-pin-<order>-<sha>
  <sha>`, the THREE symlink classes — root `node_modules`, `packages/
  quilltap/node_modules`, every `plugins/dist/*/node_modules` — then `cd`
  into it; never stash or checkout-switch the human's repo; `git worktree
  list` before each batch; **verify the pin by `git -C "$PIN" rev-parse
  HEAD` AND by `ls -ld` of the path** — the last unification's two pins
  were created at a path with a trailing space because zsh does not
  word-split `set -- $pin`; build every pin path from a quoted literal,
  never an unsplit variable; then grep the fresh NDJSON for a marker only
  one tree has; check the file is non-empty before believing a diff).
  Which sha:
  - **P4.D198 and P4.D199 pin the TARGET `bcd7e4852`** for every family
    their port MOVES (P4.D198: the NEW `llm_image_budget_equivalence`,
    `file_attachment_tier3`; P4.D199: `orchestrator_tier3`, `build_
    context_tier3` if its seams are real, `help_tree_equivalence` and the
    `help_*` content families that read `connection-profiles.md`) and the
    BASELINE `5f0a57dc4` for their neutrality legs. The target-pinned
    help-tree oracle MUST carry `A travelling portrait packs light` and
    the baseline one MUST NOT — that pair is the pin verification.
  - **P4.96 and P4.97 pin the BASELINE `5f0a57dc4`** — their v4 surfaces
    are untouched by the drift (each lane records `git -C ~/source/
    quilltap-server diff --stat 5f0a57dc4..bcd7e4852 -- <its v4 paths>`
    as a version stamp or EMPTY in its lane record before the first
    regen). A pin at the baseline is still REQUIRED, not optional: the
    checkout can go dirty mid-lane (ledger §5.1), and a bare-checkout
    regen is unrepeatable.
  The sweep driver is the sanctioned per-family path (`harness/tools/
  recipe_sweep.py --run <family> --v4 "$PIN" --v5w <ABSOLUTE worktree>`);
  never run two sweeps concurrently; a family that reports `SKIP` in
  0.00 s did not run (`a-family-env-var-is-not-its-regen-var`). Ledger
  §5.2's silent-stale-pass discipline applies to every regen (`rm -f`
  the fixture first; read the builder's last line; `grep -c` the changed
  bytes in the fresh NDJSON — MUST be > 0). **Shared `/tmp` output paths
  are a live hazard** (P4.92's mid-lane clobber; the `/tmp/qt-imggen-*`
  pair collision): every lane stages its regen outputs under a
  LANE-PRIVATE directory (`/tmp/<order>/…`) by pointing the family's env
  vars there, and leaves the committed recipe headers canonical (a
  committed recipe NEVER names a `/tmp` pin — the driver's `--v4` is the
  pin).
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). The spec
  for every item is `git show <sha> -- <path>` plus `git show <sha>:<path>`
  at `bcd7e4852` (the drift lanes) or `5f0a57dc4` (the maintenance
  lanes); a commit message describes the bug as its author understood it
  and the hunks are often narrower. Re-verify even where an order flags
  the trap — the order's paragraph is the same kind of prose, and so is
  a previous lane's record or a doc comment in the tree (P4.D196's
  `Option<String>` paraphrase; P4.95's "no case reaches the retry" — both
  corrected by this round's surveys). Carry a v5-side code comment naming
  the sha where the prose misleads.
- **§R.5 — Designed trips and pre-existing reds: measure, don't assume**
  (ledger §5.4). No convergence pin trips this round (v5 filed nothing
  bug 151 answers). The drift lanes' pre-fix reds are DESIGNED: P4.D199's
  budget arms red on unported `main` (v5 sends every unseen image) and
  P4.D198's shrink rows red (v5 sends stored bytes) — record the pre-fix
  counts. A red that appears in a family a lane did NOT touch is a NEW
  finding to classify at both pins before any source is touched, and it
  is probably not that lane's.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest`
  is shared) and its silence leg; a fixture change lands with the regen
  of every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that
  SURVIVES as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`). A scripted
  encoder on BOTH sides (P4.D198) is a legitimate tier-1 instrument when
  the real encoders cannot be byte-compared (D19); it must be the SAME
  script, and the family must say so in its header.
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed (and `apps/web/package.json` when it touches the SPA) on each
  commit per `.claude/commands/commit.md`; the unifier RECOUNTS as base +
  the sum of every lane's bumps (the silent-auto-merge trap: identical
  bumps merge as ONE — it fires every round). Base at planning: core
  0.0.942, harness 0.0.834, web 0.0.150, host 0.0.137, cli 0.0.22, tauri
  0.0.7, SPA 0.5.730. Expected: **core** by all four lanes; **harness** by
  all four; **host** by P4.D198 ONLY (the codec implementation is host
  SOURCE; P4.D199's `help/` re-vendor changes the embedded table but no
  host source — the P4.D194 rule: no host bump for help alone); **web**
  by P4.96 ONLY (the census test and the dispatch wire test live in the
  web crate's `tests/`); **SPA** by P4.96 ONLY (the one contract type);
  **cli/tauri** by NOBODY.
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  P4.D199 pre-lists the three paths `bcd7e4852` moves (`docs/v4/CHANGELOG.
  md`, `docs/v4/developer/bugs.md`, the NEW `docs/v4/developer/bugs/fixed/
  bug-151-unbudgeted-image-bytes-on-the-wire.md`) with byte counts at the
  pin in its lane record; no lane copies them.
- **§R.10 — Cross-lane handoffs, named:** (a) **The budget constant.**
  P4.D198 DEFINES `pub const LANTERN_IMAGE_BASE64_BUDGET: usize = 2 *
  1024 * 1024` in the NEW `crates/quilltap-core/src/files/llm_image_
  budget.rs` (v4's home); P4.D199 CONSUMES it but its worktree cannot see
  the module, so it defines a PRIVATE twin in `services/message_context.
  rs` beside `ASSISTANT_IMAGE_LOOKBACK` with a `// P4.D199 → P4.D198
  handoff … §R.10(a)` marker line; the unifier replaces the private twin
  with `use crate::files::llm_image_budget::LANTERN_IMAGE_BASE64_BUDGET`
  after both picks (one-line wire; the marker is the grep). (b) **The
  loaders' output is the walk's input.** P4.D199 budgets whatever
  `LanternLoad.attachments[i]["data"]` (the base64 STRING) the seam
  returns; P4.D198 makes that string the POST-shrink base64. In each
  lane's worktree the other half is absent and both differentials pass
  with junk bytes / `NotConfiguredTranscoder` (the shrink `Err`s →
  unchanged; v4's sharp throws → unchanged); the unifier re-runs
  `orchestrator_tier3` and `file_attachment_tier3` over the UNION at the
  new baseline to prove the composition and records the counts. (c)
  **`api/types.rs` / `api/engine.rs` / the dispatch wrong-type census are
  P4.96's alone this round** — the unifier asserts `git diff main...
  <lane> -- crates/quilltap-core/src/api/types.rs crates/quilltap-core/
  src/api/engine.rs crates/quilltap-web/tests/dispatch_wrong_type_census.
  rs` is EMPTY for P4.D198, P4.D199 and P4.97 before picking. (d) **The
  `/tmp/qt-imggen-*` pair collision** (`image_generate_route` ↔ `image_
  generation_tier3`): P4.96 stages under `/tmp/p4.96/`; at the unified
  gate the tier-3 family is regenerated LAST and P4.96's family is run by
  name against its own snapshot with its oracle var withheld from the
  workspace block (the `53294163f` round's recorded protocol). (e) **The
  recorded provider corpora are P4.97's alone this round** (the
  request-envelopes re-record + the google recorder if touched); P4.D198's
  families are SDK-free. (f) **The primary-stream family's side-channel
  widening** (P4.97 item 1) and **the orchestrator family's side-channel
  widening** (P4.D199 item 4(b), ordered attachment filenames) are two
  DIFFERENT families' mocks — no shared file; if either lane finds the
  widening belongs in a shared helper under `harness/oracle/lib/`, it
  creates a NEW file rather than editing an existing shared one.
- **§R.11 — No SPA behaviour moves this round.** P4.96's one contract
  type is additive and optional; no beat is added or flipped; the unified
  gate runs the full Playwright suite once against the release binary as
  regression only (the documented intermittents and the standing parks
  stand as named in the previous round's gate).

## §S — Shared contract (binding; byte-identical in P4.D198 and P4.D199)

- **§S.1 — The seam between the loader half and the walk half is the
  EXISTING `LanternLoad { prefix: String, attachments: Vec<Value> }`
  returned by `MessageContextSeams::load_lantern_images(&file_ids,
  provider)`** (`services/message_context.rs:614-617`, `:650-685`), and
  NOTHING about it moves: the attachment `Value`'s keys stay `id, filepath,
  filename, mimeType, size, data, url` (`chat_files.rs:250-258`); `data`
  is the base64 STRING of the bytes the turn will send (POST-shrink once
  P4.D198 lands; the stored bytes today); `size` is the byte length of
  those same bytes on the mount path; the attachments arrive in
  CHRONOLOGICAL order with only the raw-kept ones present (the fallback
  pass has already run — a described image contributes to `prefix`, not
  to `attachments`). P4.D199 reads `data.len()` and reorders/drops
  attachments; it never re-reads bytes, never calls a codec, and never
  edits `prefix`. P4.D198 never budgets, never reorders, and never drops
  an attachment for size (the provider backstop resize is a RESIZE, not a
  drop — v4 has none either).
- **§S.2 — The one constant with two homes until unification:**
  `LANTERN_IMAGE_BASE64_BUDGET = 2 * 1024 * 1024` (bytes of BASE64, spent
  against `data.len()`), public in P4.D198's `files/llm_image_budget.rs`,
  private in P4.D199's `message_context.rs` until §R.10(a)'s wire.
- **§S.3 — Nothing else on the wire moves from the drift.** No new
  dispatch verb, no new REST route, no `core-contract.ts` change from
  P4.D198 or P4.D199; a drift lane that finds it needs one STOPs on that
  item and records the shape for the unifier. (P4.96's five optional
  fields on `Request::ImageProfileGenerate` — camelCase `size`, `quality`,
  `style`, `aspectRatio`, `negativePrompt`, each `Option<serde_json::
  Value>` RAW-crossing, refused in the handler with v4's `Validation
  error` envelope — are that lane's own contract, mirrored into `core-
  contract.ts` by the same lane; no other lane sends them.)

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D198** — bug 151, the LOADER half | `crates/quilltap-core/src/files/image_processing.rs` (the `ImageTranscoder` trait's new `shrink_to_webp` default method + doc; nothing else in the file unless a helper is needed — say which); NEW `crates/quilltap-core/src/files/llm_image_budget.rs` + the `pub mod` line in `files/mod.rs`; `crates/quilltap-core/src/services/chat_files.rs` — `read_file_as_base64` and `load_mount_file_as_attachment` ONLY (the shrink-then-backstop sequence, the log filenames) — NOT `load_lantern_images` / `load_user_attachments` / `RealMessageContextSeams`; `crates/quilltap-host/src/image_codec.rs` (the `shrink_to_webp` impl + tests) and `crates/quilltap-host/Cargo.toml` ONLY if a feature flag is needed (say so); `crates/quilltap-harness/src/lib.rs` — the NEW `ScriptedTranscoder` (additive); NEW `harness/oracle/cases/llm-image-budget.test.ts` + a NEW script helper under `harness/oracle/lib/`; NEW `crates/quilltap-harness/tests/llm_image_budget_equivalence.rs`; `crates/quilltap-harness/tests/file_attachment_tier3_equivalence.rs`; `harness/oracle/cases/file-attachment-tier3.test.ts`; `harness/oracle/fixtures/file-attachment.json` + `build-file-attachment-fixture.ts`; NEW `crates/quilltap-harness/tests/lantern_loader_transcoder_wiring.rs` (the composition pin — or a test module in an existing host test file you own; say which); `crates/quilltap-core/src/files/mod.rs`; this order's header | `services/message_context.rs`, `services/orchestrator.rs`, `api/**`, `crates/quilltap-web/**`, `help/**`, `model/primary_stream.rs` (P4.97's — note the file is under `services/`), every recorded provider corpus under `harness/oracle/fixtures/{request-envelopes,google-*,image-dialects,…}/`, every other harness family, `apps/web/**`, the drift ledger |
| **P4.D199** — bug 151, the WALK half + help + ratifications | `crates/quilltap-core/src/services/message_context.rs` — section K (`:1337-1361`) + the private budget constant + the new warn + its `#[cfg(test)]` pins; `help/connection-profiles.md` (byte copy at the target pin); `crates/quilltap-harness/tests/help_tree_embed_guard.rs` — the history doc comment ONLY (124 stays 124); `crates/quilltap-harness/tests/orchestrator_tier3_equivalence.rs`; `harness/oracle/cases/orchestrator-tier3.test.ts`; `harness/oracle/fixtures/orchestrator-tier3.json` + `build-orchestrator-fixture.ts`; `crates/quilltap-harness/tests/build_context_tier3_equivalence.rs` + `harness/oracle/cases/build-context-tier3.test.ts` + `harness/oracle/fixtures/build-context-tier3.json` ONLY if item 4(c)'s measurement shows real seams (say so); this order's header | `services/chat_files.rs` (incl. `load_lantern_images` — READ ONLY), `files/**`, `services/orchestrator.rs`, `api/**`, `crates/quilltap-web/**`, `crates/quilltap-host/**` (no host bump — §R.8), every other `help/*.md`, `model/`/`services/primary_stream.rs`, every recorded provider corpus, every other harness family, `apps/web/**`, the drift ledger |
| **P4.96** — the generate route's five fields | `crates/quilltap-core/src/api/image_profiles.rs` (`image_profile_generate` + its doc); `crates/quilltap-core/src/api/types.rs` — the `ImageProfileGenerate` variant + its doc ONLY; `crates/quilltap-core/src/api/engine.rs` — the `ImageProfileGenerate` arm (`:2928-2951`) ONLY; NEW `crates/quilltap-core/src/image_gen/style.rs` (or a `const` in `image_gen/quality.rs`) + the repoints at `tools/generate_image.rs:213` and `api/images.rs:1472` ONLY if Tier-2 item 9 lands (say so); `crates/quilltap-web/tests/dispatch_wrong_type_census.rs`; NEW `crates/quilltap-web/tests/image_profile_generate_dispatch_wire.rs` (the dispatch-path pin); `crates/quilltap-harness/tests/image_generate_route_equivalence.rs`; `harness/oracle/cases/image-generate-route.test.ts`; `harness/oracle/fixtures/image-generation.json` + `build-image-generation-fixture.ts` ONLY if a row needs a profile the fixture lacks (say so — `image_generation_tier3` shares them); `apps/web/src/app/core/core-contract.ts` — the `imageProfileGenerate` request type ONLY; `apps/web/package.json` version; this order's header | `crates/quilltap-core/src/model/**` (incl. `image_dialects.rs`, `openai_image_options.rs`), `tools/generate_image.rs` beyond the one repoint, `crates/quilltap-web/src/**` (NO new REST edge), `services/**`, `files/**`, `help/**`, every other harness family (incl. `image_generation_tier3` and its oracle/fixture outputs), every other SPA file, the drift ledger |
| **P4.97** — the retry option bag + the envelope cache-key pin | `crates/quilltap-core/src/services/primary_stream.rs` — the retry block (`:1251-1309`) + its `#[cfg(test)]` module; `crates/quilltap-harness/tests/primary_stream_tier3_equivalence.rs`; `harness/oracle/cases/primary-stream-tier3.test.ts` (+ `openai-chaining-fallback-tier3.test.ts` ONLY if the mock widening is shared — say so); `harness/oracle/fixtures/primary-stream-tier3.json` + `build-primary-stream-fixture.ts`; `harness/oracle/providers/record-request-envelopes.mjs` + `regenerate-request-envelopes.sh` (+ `record-google-request.mjs` / `regenerate-google-wire.sh` ONLY if item 6's google arm records — say so); `harness/oracle/fixtures/request-envelopes/request-envelopes.recorded.ndjson` (+ `google-request.recorded.ndjson` under the same condition); `crates/quilltap-harness/tests/request_builder_equivalence.rs`; this order's header | `model/stream.rs` (the canned key), `model/request_builder/**` (READ ONLY — a builder red is a finding), `services/orchestrator.rs`, `services/provider_failover.rs`, `services/native_tool_loop.rs`, `services/text_tool_loop.rs`, `model/streaming_provider.rs`, every other recorded corpus (`image-dialects/`, `response-bodies/`, `stream-fixtures/`, `tool-wire/`, `moderation-wire/`, `web-search-wire/`), `api/**`, `crates/quilltap-web/**`, `services/chat_files.rs` / `message_context.rs` / `files/**`, `help/**`, every other harness family, `apps/web/**`, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0),
   and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp` / by-label diff) where the family is
   deterministic across regens; where it is not (`regenerate-at-both-
   pins-and-cmp-is-not-universal`), compare the normalized rows by case
   and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast` with the lane's env block
   (its own oracle vars + `QT_V4_ROOT`/`QT_V4_CHECKOUT` at the right pin +
   `QT_NODE` = the node BINARY): report **binaries / passed / failed /
   ignored** from the per-binary lines and the exit code, and the lane's
   families confirmed RUN by non-zero duration. The `/tmp/qt-imggen-*`
   collision: a lane that is not P4.96 withholds `QT_ORACLE_IMGGEN_ROUTE`
   from its block (the family SKIP-passes and is P4.96's to prove by
   name); P4.96 withholds `QT_ORACLE_IMGGEN` (the tier-3 family's) and
   proves its own by name on its private staging.
9. **SPA (P4.96 only):** `npm run lint`, `npm test` (spec files / tests /
   0 failed), `npm run build` clean — no e2e beat is added this round;
   the full Playwright run is the unified gate's.
10. **Ownership check:** `git diff --stat main...HEAD` — every path is in
    the lane's Owns column; every MUST-NOT-TOUCH path absent; for the
    three non-P4.96 lanes, `git diff main...HEAD -- crates/quilltap-core/
    src/api/types.rs crates/quilltap-core/src/api/engine.rs crates/
    quilltap-web/tests/dispatch_wrong_type_census.rs` is EMPTY (§R.10(c)).
11. **Per commit:** `.claude/commands/commit.md` in full — the equivalence
    test is non-negotiable (§1), the invariants (§2), real-instance safety
    (§3), the harness regen (§4), build/test/lint (§5), the CHANGELOG
    entry + version bumps (§7). Commit on the lane branch only; never
    merge to `main`; never `git stash`; never a worktree inside a worktree.
12. **Lane close:** update this order's status header (what landed, what
    was refuted by measurement, what stays OPEN with its named cause),
    append the lane record to `status-log.md` (the regen commands as run,
    the counts, the mutation table, every finding outside the lane's
    ownership for the unifier), and stop. The final gate runs in the
    FOREGROUND of the lane's last turn — a backgrounded gate whose
    notification arrives after the lane reports done is a lane that is
    NOT done (`a-backgrounded-sleep-does-not-gate-your-turn`).
