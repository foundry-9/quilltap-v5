# Work order P4.6as — the LLM-Inspector SPA lane (lane B): the slide-over panel, the toolbar button, and the per-message log entry

> **STATUS: CLOSED — UNIFIED on main (2026-07-15).** Tier 1 AND tier 2
> LANDED: `ui/slide-over-panel.ts` (generic, inline-rendered — the portal
> divergence documented), the Inspector panel/entry/api trio (badge +
> filter tables verbatim, all fallback chains + duration inconsistency
> carried), the header toolbar button (inspector BEFORE cost summary; the
> `enabled !== false` gate specced across all five absence cases),
> Cmd+Shift+L, the per-message cpu icon (title pinned to
> `MessageActionBar.tsx:153` with rationale), the reconcile-point
> `refreshLogs()` (tier 2), the global-setup llm-logs seed (3 rows via the
> CLI `--llm-logs` flag, `SINGLE_USER_ID` up front — the partition is
> outside the userId-rewrite loop's reach), and the two-beat
> `llm-inspector-flow.spec.ts` (mocked in-lane; ACTIVATED LIVE at
> unification — mock deleted). **Two deliberate divergences, both
> documented + specced:** the closed slide-over declares `role="dialog"`/
> `aria-modal` only while OPEN and carries `inert` closed (v4's
> always-mounted panel is a permanent phantom modal — it broke two sibling
> beats' honest `getByRole('dialog')` assertions); the courier spec's
> chat-discovery guard now WAITS for its selector instead of instant
> sampling (a race this lane's failing in-lane dispatches exposed; the race
> predated the lane). ng test 968 → 1094 in-lane; SPA 0.5.118 in-lane
> (accumulated to 0.5.122 at unification).
>
> **Original status (for reference): OPEN.**
>
> Lane B of the THREE-lane P4.6ar ∥ P4.6as ∥ P4.6at round (the LLM-Inspector +
> Default-Aesthetics + minHeight round, planned 2026-07-15). **v4 reference
> baseline: HEAD `02865bdb`** (`~/source/quilltap-server`) — run
> `git log 02865bdb..HEAD --oneline` at lane start; **if HEAD moved, STOP and
> report.** Sibling lanes: `p4.6ar-llm-logs-aesthetics-server.md` (delivers
> the `llmLogsList`/`llmLogGet`/`llmLogDelete` verbs this lane consumes —
> Shared contract §1), `p4.6at-aesthetics-card-minheight.md` (settings/forms
> SPA — disjoint files). The **Shared contract** and **Ownership** sections
> below are binding and reproduced verbatim in all three orders. You run in a
> worktree (own `node_modules` + Rust binaries per the P4.6af recipe); the
> unification pass integrates the lanes onto main.

## Mandate

Deliver the whole LLM-Inspector client vertical the P4.6ao round left as a
named deferral: the slide-over panel + entry components, the llm-logs query
module, the conversation-header toolbar button (which
`conversation-header.ts:16` already documents as deferred), the Cmd+Shift+L
shortcut, and the per-message "View LLM logs" action icon. This lane owns the
listed `apps/web` chat/salon files ONLY (see Ownership); it must NOT touch
`crates/**`, the settings screens, or `core-contract.ts` (local request types
+ casts; the unifier folds). Every component ships with v4-faithful specs
pinned to v4 line numbers, plus a live e2e beat (mocked `ACTIVATE-AT-UNIFY`
until lane A's routes exist on main).

## Survey-verified starting points (2026-07-15 @ `02865bdb` — do not re-derive)

### The v4 sources, one v5 target each

| v4 | lines | v5 target |
|---|---|---|
| `components/ui/SlideOverPanel.tsx` | 145 | `apps/web/src/app/ui/slide-over-panel.ts` (new) |
| `components/chat/LLMInspectorPanel.tsx` | 161 | `apps/web/src/app/chat/llm-inspector-panel.ts` (new) |
| `components/chat/LLMInspectorEntry.tsx` | 354 | `apps/web/src/app/chat/llm-inspector-entry.ts` (new) |
| `app/salon/[id]/hooks/useLLMLogs.ts` | 83 | `apps/web/src/app/chat/llm-logs.api.ts` (new) |
| `SalonView.tsx:990-1027` (toolbar effect) | — | `apps/web/src/app/chat/conversation-header.ts` |
| `SalonView.tsx:796-811` (keyboard shortcut), `:1355-1356` + `:1696-1705` (panel mount + props) | — | the salon conversation host (`screens/salon/salon-conversation.ts`) |
| `MessageDesktopActions.tsx:69-77` + `MessageActionBar.tsx:149-157` | — | `apps/web/src/app/chat/message-row.ts` |

**The CSS is already ported.** `qt-slide-over-overlay/-panel/-header`,
`qt-inspector-entry`, `qt-inspector-entry-highlight`, and the
`qt-inspector-highlight` keyframes all sit in
`apps/web/src/styles/qt-components/_surfaces.css:1067-1126`. Zero stylesheet
work; if a class seems missing, re-read the file before adding anything.

### SlideOverPanel (v4 `SlideOverPanel.tsx`)

Portal-based right-edge panel; props `isOpen/onClose/title/width
(default 'min(480px, 85vw)')/ariaLabel/headerActions`; saves and restores
focus on open/close; overlay + panel carry `data-open` for the CSS
transitions. Port as a generic reusable (`qt-slide-over-panel` selector) —
the inspector is its only consumer today, but v4 keeps it generic and so do
we. Read the remaining v4 body (Escape handling, click-outside, focus trap)
at lane start — the survey read the first 40 lines.

### useLLMLogs → `llm-logs.api.ts`

- Query: key `llmLogs.byChat(chatId)`, fetch
  `/api/v1/llm-logs?chatId=${chatId}&includeMessages=true` — in v5, the
  dispatch verb `{ type: 'llmLogsList', chatId, includeMessages: true }`
  (Shared contract §1; local request type + cast, NOT core-contract.ts).
  `enabled: messages.length > 0`.
- Derived `messagesWithLogs: Set<messageId>` from rows with a `messageId`.
- Panel state: `inspectorOpen`, `inspectorScrollToMessageId`;
  `handleViewLLMLogs(messageId)` opens scrolled; `toggleInspector` clears the
  scroll target ONLY when opening (v4 :50-58); `closeInspector` clears both;
  `refreshLogs` refetches.

### LLMInspectorPanel

- Logs arrive DESC; the panel reverses to chronological (v4 :55) and filters
  client-side by `FILTER_GROUPS` — carry the table VERBATIM (all / chat
  [CHAT_MESSAGE, TOOL_CONTINUATION] / memory [MEMORY_EXTRACTION] / system
  [TITLE_GENERATION, SUMMARIZATION, CONTEXT_COMPRESSION] / image
  [IMAGE_PROMPT_CRAFTING, IMAGE_DESCRIPTION, APPEARANCE_RESOLUTION] / safety
  [DANGER_CLASSIFICATION] / other [CHARACTER_WIZARD, AI_IMPORT]) with
  `FILTER_LABELS` copy exactly.
- Header actions: entry count (`N entries` / `1 entry`), the filter
  `<select>` (dogfood-#6 audit applies: static options, plain `[value]` is
  safe here but justify it in the spec), the refresh button
  (`title="Refresh logs"`).
- Scroll-to-message: find the target log by `messageId` in FILTERED results,
  300 ms delay, `querySelector([data-log-id])` + smooth scrollIntoView
  center; the matching entry gets the highlight animation.
- Empty states, in priority order: loading spinner → logging disabled
  (`ban` icon, "LLM logging is disabled." / "Enable it in Chat Settings to
  record API interactions.") → no entries (`file` icon, "No log entries
  yet." / "Send a message to start recording LLM interactions.").

### LLMInspectorEntry

- Carry `TYPE_BADGE_CLASSES` and `TYPE_LABELS` verbatim (twelve types;
  unknown type falls back to `qt-bg-muted qt-text-secondary` + the raw type
  string).
- Collapsed row: timestamp (`toLocaleTimeString` 2-digit h/m/s), badge,
  `provider/modelName`, spacer, token summary
  `promptTokens → completionTokens` (the arrow is `→`, both
  `toLocaleString()`), duration `(ms/1000).toFixed(1)s`, a `chat` icon when
  `messageId` (title "Linked to a message"), an `alert-circle` error icon
  titled with the error text, chevron rotating 180° when expanded.
- Expanded: request/response/usage tabs, lazy-rendered. Request tab:
  provider/model/type block; messageCount/temperature ('default' when
  null)/maxTokens ('default')/toolCount block; per-message blocks with
  role + `contentLength` chars + '(attachments)', body
  `content || contentPreview || ''`. Response tab: error block or
  "Request completed successfully"; body fallback chain
  `content || fullContent || contentPreview || ''` (backward compat — keep
  the why-comment); "(N chars)" heading. Usage tab: 3-col
  prompt/completion/total grid; cacheUsage rows (each only when its key is
  defined); duration `toFixed(2)` (note: 1 decimal collapsed, 2 in the usage
  tab — v4 is inconsistent on purpose, carry it); the all-absent fallback
  copy "No usage data available for this log".
- `UI_TRUNCATE_LENGTH = 500` show-more/show-less on both message blocks and
  the response body ("Show all N chars" with `toLocaleString`).

### The toolbar button + shortcut + gating

- Gate: `chatSettings?.llmLoggingSettings?.enabled !== false` — **default
  TRUE when the bag or key is absent** (the settings parse already defaults
  it server-side; the client re-derives the same way).
- Button (v4 `SalonView.tsx:998-1013`): icon `code`, title
  `"LLM Inspector (Cmd+Shift+L)"`, aria-label "Toggle LLM Inspector",
  active-state classes swap when the panel is open. **Order in the header's
  right cluster: inspector button BEFORE the cost summary** (v4 :995-1024).
  `conversation-header.ts` already renders the cost summary and already
  names this deferral in its doc comment (line 16) — update the comment.
- Shortcut (v4 :796-811): Cmd/Ctrl+Shift+L, `e.key === 'L'` (uppercase — the
  shift is held), `preventDefault`, listener only attached while the gate is
  true. Lives in the salon conversation host, not the header.

### The per-message action icon

Both v4 action bars render it for `hasLLMLogs && role === 'ASSISTANT'`:
icon `cpu`, but the titles DIFFER — desktop hover bar
(`MessageActionBar.tsx:153`) says `"View LLM request/response logs"`, the
mobile/desktop-actions variant (`MessageDesktopActions.tsx:73`) says
`"View LLM logs"`. v5's `message-row.ts` has ONE action bar
(`qt-chat-message-action-bar`, :148) — map it to the bar it visually
corresponds to and pin the title choice in a spec with the v4 line cited.
`messagesWithLogs` and the open-scrolled callback thread from the salon host
through `message-list` inputs the same way the existing action callbacks do.

### e2e

- The e2e instance's llm-logs partition (`salon-llm-logs.db`) is already
  committed (P4.6ab/ac/ad round). **Peek before assuming it has rows**
  ([[e2e-fixture-before-run-order]] — better-sqlite3 from the v4 checkout);
  if empty, seed log rows in `global-setup.ts` by hand-DDL
  ([[p4.6ak-al-am-round-plan]]: migration tables need hand-DDL), remembering
  the e2e userId rewrite (`llm_logs` rows are user-scoped — the THIRD
  recurrence of the P4.6s lesson was `image_profiles`; do not make llm_logs
  the fourth).
- New spec `llm-inspector-flow.spec.ts`: open via toolbar button → entries
  render with badges → expand one → tabs → filter → per-message cpu icon
  scrolls+highlights → Cmd+Shift+L closes. Route-mock the `llmLogsList`
  dispatch tag, tagged `ACTIVATE-AT-UNIFY` (the mock's envelope must match
  the serde adjacent-tag encoding exactly — the P4.6ao mocks did and
  activation was free).

## Tiered deliverables

**Tier 1 (must land):** everything in the survey — the four new files, the
header button + gating, the shortcut, the per-message icon, `ng test` specs
for each (v4 line-pinned), the e2e spec (mocked, ACTIVATE-AT-UNIFY), and the
global-setup llm-logs seeding if the peek finds the partition empty.

**Tier 2 (should land):** the panel's refresh-after-streaming hook — v4
refreshes logs when a stream completes (find the `refreshLogs` call site in
`SalonView`/`useSSEStreaming` at lane start); wire the v5 equivalent if the
seam is already exposed, defer loudly (doc comment + status-log) if it needs
files outside this lane's ownership.

**Tier 3 (explicit refusal-arm deferrals):** none — but name in the
status-log entry anything v4 renders that v5 cannot yet (e.g. if any entry
field depends on a log shape the ported writers never produce).

## The differential requirement (restated)

SPA lanes prove equivalence by: (1) `ng test` specs asserting v4's exact
copy, class hooks, gating, and branch behavior, each citing the v4 file:line
it pins; (2) the live e2e beat over the real fixture instance. The server
shapes this lane consumes are pinned by lane A's route differentials — do
not restate them, but DO make the route mock byte-match the Shared contract
so unify-activation is a deletion, not a rewrite. Nearest recipes:
[[p4.6ap-token-cost-salon-spa]] (header-cluster work, whitespace-stripped
DOM assertions, never assert absolute counts a sibling spec can grow),
[[p4.6ac-courier-images-salon-spa]] (message-row action work),
[[p4.5-spa-foundation]] (Playwright locked-fixture recipe).

## Fixtures

Consumes the committed e2e salon instance (incl. `salon-llm-logs.db`); may
seed rows into it via `global-setup.ts` (this lane is that file's sole owner
this round). Does NOT touch the `crates/quilltap-web/tests/fixtures/`
families — the new `inspector-*` family is lane A's.

## Shared contract (BINDING — reproduced verbatim in all three orders)

Five new `Request` variants land in `crates/quilltap-core/src/api/types.rs`
(lane A owns the Rust side). The SPA lanes define matching LOCAL request types
in their own api modules (cast at the call site) — **neither SPA lane touches
`core-contract.ts`**; the unifier folds the types into the `CoreRequest`
union at unification (the P4.6ao precedent). Wire encoding is the standing
one: requests internally tagged (`#[serde(tag = "type", rename_all =
"camelCase")]`, fields camelCase); responses adjacently tagged
(`{"type": …, "data": …}`).

### §1 — the llm-logs verbs (lane A ⇄ lane B)

```rust
/// v4 `GET /api/v1/llm-logs` (all filter branches).
#[serde(rename_all = "camelCase")]
LlmLogsList {
    #[serde(default)] message_id: Option<String>,
    #[serde(default)] chat_id: Option<String>,
    #[serde(default)] character_id: Option<String>,
    /// v4's `type` query param. Wire name `logType` — a field literally
    /// named `type` would collide with the internally-tagged envelope key.
    /// The REST edge maps `?type=` onto it. NOT validated (v4 casts).
    #[serde(default)] log_type: Option<String>,
    #[serde(default)] standalone: bool,
    #[serde(default)] include_messages: bool,
    /// Raw query strings; the HANDLER ports v4's parseInt/Math.min body
    /// (including the NaN no-slice quirk) so dispatch and REST agree.
    #[serde(default)] limit: Option<String>,
    #[serde(default)] offset: Option<String>,
},
/// v4 `GET /api/v1/llm-logs/[id]`.
#[serde(rename_all = "camelCase")]
LlmLogGet { id: String },
/// v4 `DELETE /api/v1/llm-logs/[id]`.
#[serde(rename_all = "camelCase")]
LlmLogDelete { id: String },
```

Response payloads (the `data` member):

- `llmLogsList` → `{ "logs": [...], "count": n, "total": n, "limit": n,
  "offset": n }` — v4's exact envelope: `total` pre-slice, `count`
  post-slice.
- `llmLogGet` → the raw log object itself (v4 `successResponse(log)`, no
  wrapper key).
- `llmLogDelete` → `{ "success": true, "deletedId": "<id>" }`.

Lane B's client sends exactly what v4 `useLLMLogs` sends:
`{ type: "llmLogsList", chatId, includeMessages: true }` — nothing else.
Response-variant Rust names follow the existing camelCase-tag convention so
the wire tags read `llmLogsList` etc.; lane B mocks against these tags and
the unifier's activation must not need renames.

### §2 — the system image-aesthetics verbs (lane A ⇄ lane C)

```rust
/// v4 `GET /api/v1/system/image-aesthetics?kind=lantern|aurora`.
#[serde(rename_all = "camelCase")]
SystemImageAestheticsGet { kind: String },
/// v4 `PUT /api/v1/system/image-aesthetics?kind=…`; empty or absent
/// content deletes the file (restores fallback).
#[serde(rename_all = "camelCase")]
SystemImageAestheticsSet {
    kind: String,
    #[serde(default)] content: Option<String>,
},
```

Responses mirror the `ProjectAestheticGet`/`Set` pair byte-for-byte:
get → `{ "content": "<markdown or ''>" }`; set → `{ "success": true }`.

Pinned error semantics (both transports): invalid kind → badRequest
`'Query param "kind" must be "lantern" or "aurora"'`; GET with no general
store → success `{content: ''}`; PUT with no general store → serverError
`'Quilltap General document store is not available'`; malformed PUT body →
content `''` → deletes the file.

## Ownership (BINDING — reproduced verbatim in all three orders)

| Lane | Sole writer of | Must NOT touch |
|---|---|---|
| A (P4.6ar) | `crates/**`, `harness/oracle/**`, the new `inspector-*` fixture family | `apps/web/**` |
| B (P4.6as) | `apps/web/src/app/chat/**`, `apps/web/src/app/ui/slide-over-panel.ts` (new), `apps/web/src/app/screens/salon/**`, `apps/web/e2e/global-setup.ts`, the salon/m4 e2e specs + its own new spec | `crates/**`, `harness/**`, `screens/settings/**`, `screens/characters/**`, `screens/prospero/**`, `src/app/memory/**`, `core-contract.ts`, `e2e/settings-flow.spec.ts` |
| C (P4.6at) | `apps/web/src/app/screens/settings/images/**`, `apps/web/src/app/ui/aesthetic-editor-field.ts` (new), `screens/prospero/cards/project-aesthetic-field.ts`, `screens/characters/new/new-character.ts`, `screens/characters/edit/details-tab.ts`, `src/app/memory/memory-editor.ts` (each + its spec), `apps/web/e2e/settings-flow.spec.ts` | `crates/**`, `harness/**`, `src/app/chat/**`, `screens/salon/**`, `core-contract.ts`, `e2e/global-setup.ts`, the salon e2e specs |

- `docs/CHANGELOG.md` + `docs/developer/porting/status-log.md`: append-only
  for everyone; union-merge at unification.
- `core-contract.ts`: NOBODY this round — the SPA lanes keep local request
  types + casts in their api modules; the unifier folds them (P4.6ao
  precedent).
- Version bumps: lane A bumps `quilltap-core`, `quilltap-web`,
  `quilltap-harness` (leave `quilltap-host` alone — no new host seams this
  round). Lanes B and C each bump the SPA `apps/web/package.json` patch
  version; the unifier accumulates and syncs the lockfile's own version
  fields (the P4.6an lesson).
- Playwright contention: lanes B and C must not run the e2e suite
  concurrently (shared port 4319 — the P4.6af finding). Stagger, or treat
  the unifier's full run as the joint proof.
- ACTIVATE-AT-UNIFY: lane B's inspector beat and lane C's aesthetics beat
  mock their new verbs at the Playwright route layer, tagged
  `ACTIVATE-AT-UNIFY`; the unifier deletes the mocks and runs them live over
  lane A's routes.

## Verification gate (before declaring the lane done)

1. `ng test` — full suite, zero failures (record the new count).
2. `ng build` clean.
3. Its own e2e spec green over the locked fixture (mocked legs tagged), plus
   the salon specs it touched; the FULL Playwright suite if the port-4319
   window allows, otherwise say so in the status-log entry and leave the
   full run to the unifier.
4. No `crates/**` diffs (`git status` proof) — this lane compiles no Rust.
5. Every commit follows `.claude/commands/commit.md` (SPA version bump per
   the Ownership section; CHANGELOG + status-log entries).
