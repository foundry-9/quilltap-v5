# Work order P4.82 — the `CHARACTER_HEADSHOULDERS_BACKFILL` job handler + its boot-time enqueuer: the last named-refusal job type, unblocked by `generators::wizard`

**Status (unification, 2026-09-07 — the generator follow-ups + prompt-templates
round): CLOSED — UNIFIED on main; nothing OPEN.** Every Tier-1 and Tier-2
deliverable landed as the lane's close says (the handler, the queue helper, the
boot scan, the host wiring, both differentials over the NEW committed
`headshoulders-{main,mount}.db` pair, both wiring pins; Tier 3's three refusal
arms dispositioned by measurement — the `setImmediate` yield a NO-COUNTERPART,
v4's `Failed to select cheap LLM` catch proven DEAD and kept as
`SELECT_FAILED_UNREACHABLE`, the wizard log-failure warn needing no port because
v5's `log_llm_call` narrates its own failure one layer down). **The §3
unification review fixed three should-fixes on the unify branch:** a failing
`chat_settings` read FAILED the job where v4's fallback-mode `safeQuery`
swallows it to the default config (the P4.48 class — matched, with v4's `Error
finding chat settings by user ID` line); the dedupe-lookup `Err` arm rendered a
warn v4 cannot produce (its `catch` is dead: both reads are `safeQuery(…, [])`)
— it now renders `safeQuery`'s own `Error finding background jobs by user ID`
and falls through the same way, the dead sentence kept as
`DEDUPE_LOOKUP_WARN_UNREACHABLE`; and the boot scan runs INSIDE the JOINED
`seed_built_ins` thread, so it is ON v5's boot path where v4's `.then` is not —
three comments claimed the opposite and are corrected, the mechanism divergence
RECORDED (bounded: one indexed read wherever v4's flag already sits), and the
v5-invented `Head-and-shoulders backfill scan complete` host line dropped (v4
logs nothing after the `await`). Two recorded deviations stand from the lane
close: the committed pair lives under `crates/quilltap-web/tests/fixtures/` and
the queue helper is the blocking shape (`an-orders-prescribed-shape-may-fight-
the-files-idiom`). Follow-up named, not ordered: detach the scan from the joined
thread (a small host change; the boot test's determinism currently rests on the
join). Lane record: `status-log.md` → "Lane record — P4.82".

## Preamble

- **v4 oracle baseline: `2f4254b42`** (drift-ledger §1); **zero drift** —
  v4 `main` HEAD is the baseline, `bugfix` tip `1a2b2164c` unmoved, the
  ledger's §3 EMPTY. Nothing to absorb; this lane is P4.9K2's named Tier-3
  deferral ("the head-and-shoulders backfill handler's wizard import —
  out of scope — NAMED follow-up with the v5 handler's line"), recorded
  UNBLOCKED at the `2f4254b42` round's unification once `generators::
  wizard` landed. The `2f4254b42`-round dogfood pass found no v5 defect on
  this surface because the surface does not exist: every
  `CHARACTER_HEADSHOULDERS_BACKFILL` job v4 has ever enqueued on the shared
  Friday instance is answered by v5's "recognized but not yet available"
  loud fallback (`job_runner.rs:164`).
- **Freshness probe at lane start** (ledger §2): checkout on **`main`**,
  tree clean, `2f4254b42..main` and `1a2b2164c..bugfix` both EMPTY. **Any
  mismatch: STOP and report.**
- **Regen rule: NO PIN REQUIRED** (ledger §1) — regens from the live
  checkout after a passing probe, through `recipe_sweep.py --run <family>`;
  re-probe before every regen batch.
- **Sibling lanes:** P4.83 (prompt templates — the other lane touching
  `api/types.rs` / `engine.rs` / `web/lib.rs`, by fence), P4.84 (SPA
  smalls — no Rust), P4.85 (Rust smalls — `api/generators_detail.rs`,
  `api/salon.rs`, `image_profiles.rs`, `generator_sse.rs`), P4.86 (the
  AI-import validation engine — `generators/ai_import.rs`,
  `api/generators_wizard.rs`). **You are the only lane in `generators/
  wizard.rs`, `services/queue_service.rs`, `host.rs` and `spine.rs`.**
- **Worktree note:** own worktree + lane branch
  (`claude/p4-82-headshoulders-backfill-<hash>` from `main`);
  `CARGO_INCREMENTAL=0`. No SPA work: finish with `git diff main --
  apps/web/` EMPTY.

## Mandate

Port v4's head-and-shoulders portrait-prompt backfill end to end: the
`CHARACTER_HEADSHOULDERS_BACKFILL` job handler
(`lib/background-jobs/handlers/character-headshoulders-backfill.ts`, 159
lines), the dedupe-on-enqueue queue helper
(`lib/background-jobs/queue-service.ts:604-651`), and the one-time startup
scan that enqueues a job per eligible character
(`lib/startup/enqueue-headshoulders-backfill.ts`, 140 lines, chained LAST in
`instrumentation.ts`'s vault-backfill `.then` at `:627-638`) — each with its
differential against v4's REAL code, the handler wired LIVE in the host's
spine bundle beside `TitleUpdateJobHandler`, and the enqueuer wired into the
host's boot-repair chain. The handler generates ONE cheap-LLM completion
through the wizard's `generateField` and writes the COMPLETE merged
`physicalDescription` back through the vault overlay. **Cross-app hazard,
by design:** the enqueuer's gate is the SHARED `instance_settings` key
`headshoulders_backfill_enqueued_v1` — v4 has already set it on every
instance it has booted since the feature shipped (the Friday copy included),
so a v5 boot on such an instance MUST scan nothing and write nothing; the
dogfood pass proves that cross-app leg. What this lane must NOT touch: any
`api/**` file, the SPA, the AI-import or optimizer modules, the committed
`character-generators-{main,mount}.db` pair (read-only if reused).

## Survey-verified starting points (2026-09-07 @ v4 `2f4254b42` — do not re-derive)

### The handler (`lib/background-jobs/handlers/character-headshoulders-backfill.ts`)

- Payload `{ characterId }` (`CharacterHeadShouldersBackfillPayload`,
  `queue-service.ts:607-609`). Logger context string
  `background-jobs.headshoulders-backfill` (`CONTEXT`, `:38`); every line
  carries `context` + `jobId`.
- `repos.characters.findById(payload.characterId)` — the OVERLAY-AWARE read
  (v5 `characters_read::find_by_id(main, mount, id)`). Not found → `info`
  `[HeadShouldersBackfill] Character not found, skipping` `{context, jobId,
  characterId}` and return (the job SUCCEEDS).
- `pd = character.physicalDescription`; absent → return SILENTLY (no log).
- Idempotence: `pd.headAndShouldersPrompt && pd.headAndShouldersPrompt.trim()`
  truthy → return silently.
- `seedText = (pd.mediumPrompt || pd.shortPrompt || pd.longPrompt ||
  pd.completePrompt || pd.fullDescription || '').trim()` — JS `||` (an empty
  string falls through), THEN one trim of the winner; empty → return
  silently.
- `chatSettings = await repos.chatSettings.findByUserId(job.userId) ??
  undefined`; `allProfiles = await repos.connections.findByUserId(job.userId)`
  — **the profile read stays OUTSIDE the try on purpose** (a failing read is
  the job's failure to report; only a failing selection is skipped over —
  the v4 comment at `:83-85`, carry it).
- `resolved = selectCheapLLMFromProfiles(allProfiles, buildCheapLLMConfig(
  chatSettings))` inside try/catch → `warn` `[HeadShouldersBackfill] Failed
  to select cheap LLM, skipping` `{context, jobId, error}` + return.
  **Measure whether v4's `getCheapLLMProvider` can throw at all** (v5's
  `image_job_common::build_cheap_llm_selection` returns a value); if it
  cannot, the warn arm is unreachable — record, do not invent a throw.
  `resolved === null` (no profiles) → `warn` `… No connection profile
  configured, skipping` `{context, jobId, characterId}` + return.
- `apiKey = await getApiKeyForCheapLLMSelection(selection, job.userId)`;
  `=== null` → `warn` `… No API key for cheap LLM selection, skipping`
  `{context, jobId}` + return. v5 twin: `api_key_service::
  get_api_key_for_cheap_llm_selection(conn, &selection, user_id)` (:82 —
  `Some("")` for a local model, `None` for no profile / no key).
- `provider = await createLLMProvider(selection.provider, selection.baseUrl)`;
  `contextPrompt = buildContextPrompt(character.name, '', undefined,
  seedText)` (v5 `generators::wizard::build_context_prompt`, :76 — the seed
  text rides the image-description slot, "physical-appearance-only
  grounding"); `content = await generateField(provider, apiKey,
  selection.modelName, contextPrompt, HEAD_AND_SHOULDERS_PHYSICAL_PROMPT,
  350, job.userId, payload.characterId, selection.provider)` — **the tenth
  argument `profileParameters` is NOT passed** (undefined → the wire carries
  no profile parameters; v5's `WizardCallCtx.profile_parameters` = `None`).
  `generateField` (`character-wizard.service.ts:387-460`): `temperature 0.8`,
  `maxTokens 350`, throws `No response from model` on an empty answer, logs
  a `CHARACTER_WIZARD` `llm_logs` row fire-and-forget (`userId &&
  profileProvider` — both present here, so the row is written; its
  `characterId` = the payload's), returns `content.trim()`. A thrown error
  propagates — the JOB FAILS (the runner's retry; `maxAttempts: 3` from the
  enqueuer).
- v5's `generate_field` is PRIVATE (`generators/wizard.rs:415`, keyed on the
  private `WizardCallCtx<'a, CMP>` at `:398-409`: `db`, `completion`,
  `provider`, `base_url`, `model_name`, `profile_parameters`, `user_id`,
  `character_id`). Expose it for this handler with ONE implementation (a
  `pub(crate)` constructor for the ctx, or a `pub(crate) async fn` taking
  the ten v4 arguments) inside a `// === P4.82 ===` fence; NO behaviour
  change to the wizard's own use, and the wizard families stay green at the
  gate as the proof.
- `headAndShouldersPrompt = content.substring(0, 500).trim()` — `substring`
  counts UTF-16 units (`jsstr::utf16_prefix`, as the wizard's own
  `"headAndShoulders"` arm does at `wizard.rs:644`); empty → `warn` `…
  Model returned empty text, skipping write` `{context, jobId,
  characterId}` + return.
- `repos.characters.update(id, { physicalDescription: { ...pd,
  headAndShouldersPrompt, updatedAt: new Date().toISOString() } })` — the
  COMPLETE merged object, because `renderPhysicalPromptsJson` re-renders
  every key (a partial write would null the other tiers — the v4 header's
  own warning; carry the comment). v5: `vault_character_update::
  update_character(main, mount, id, &patch)` (:371) with the patch
  `{"physicalDescription": <merged>}` — the overlay routes it to the vault
  file. Then `info` `… Populated head-and-shoulders prompt` `{context,
  jobId, characterId, length}` (`length` = the JS string length = UTF-16
  units).
- Activity kind: `image` (`activity-kinds.ts:67`; v5 `activity_kinds.rs:109`
  already carries it). Realtime topic hint: v5 `job_topics.rs:76` already
  scopes it. `system_data.rs:653` lists the type for export — untouched.

### The queue helper (`queue-service.ts:604-651`)

`enqueueCharacterHeadShouldersBackfill(userId, payload, options?)`: dedupes
against `findByUserId(userId, 'PENDING')` ∪ `findByUserId(userId,
'PROCESSING')` for the same type + `payload.characterId` → `{jobId:
existing.id, isNew: false}`; a THROW from the lookup → `warn`
`[HeadShouldersBackfill] Failed to check for existing jobs during enqueue`
`{error}` and FALL THROUGH to enqueue; else `enqueueJob(userId,
'CHARACTER_HEADSHOULDERS_BACKFILL', payload, {...options, priority:
options?.priority ?? -1})` → `{jobId, isNew: true}`. v5 `services/
queue_service.rs` has the sibling shapes (`enqueue_character_avatar_
generation` :578, `enqueue_memory_regenerate_chat`'s dedupe) — add
`enqueue_character_headshoulders_backfill` in a `// === P4.82 ===` fence,
the same dedupe reads, `priority -1`, `maxAttempts` from options.

### The startup enqueuer (`lib/startup/enqueue-headshoulders-backfill.ts`)

- Logger: `createServiceLogger('Startup:HeadShouldersBackfill')` — measure
  the rendered context/prefix v4 emits (the P4.49 file-logging shape) and
  pin the sentences: `info` `Head-and-shoulders backfill scanning` `{total}`;
  `warn` `Failed to enqueue head-and-shoulders backfill` `{characterId,
  error}`; `info` `Head-and-shoulders backfill enqueue complete` `{scanned,
  enqueued, skipped}`; `warn` `Failed to read head-and-shoulders backfill
  flag; treating as not-yet-run` `{error}`; `warn` `Failed to record
  head-and-shoulders backfill flag` `{error}`.
- Gate: `hasRun()` reads `SELECT "value" FROM "instance_settings" WHERE
  "key" = ?` for `headshoulders_backfill_enqueued_v1`; `=== 'true'` → result
  `{scanned:0, enqueued:0, skipped:0, alreadyDone:true}` and NO scan log; a
  read error → warn + treated as not run; no raw DB → false.
- Scan: `repos.characters.findAll()` (overlay-aware; broken vaults DROPPED,
  not thrown — v5 `characters_read::find_all(main, mount)`; measure that its
  broken-vault behaviour matches the batch overlay's drop). Per character,
  in order: no `characterDocumentMountPointId` → `skipped++`; no
  `physicalDescription` → `skipped++`; `headAndShouldersPrompt` present
  and `.trim()` truthy → `skipped++`; `hasSeed = Boolean(mediumPrompt ||
  shortPrompt || longPrompt || completePrompt || fullDescription)` — **NOT
  trimmed here** (a whitespace-only prompt counts as a seed for the
  enqueuer but NOT for the handler, which trims — reproduce both, pin the
  asymmetry with a whitespace-only case); else enqueue with `{maxAttempts:
  3}` → `isNew ? enqueued++ : skipped++`; a throw → the warn above and
  continue; then `await new Promise(resolve => setImmediate(resolve))` — a
  NO-COUNTERPART yield (record).
- `markRun()` runs EVEN IF zero were enqueued (`INSERT … ON CONFLICT("key")
  DO UPDATE SET "value" = excluded."value"`, `'true'`); a write error →
  warn, never a failure. The complete-line fires after.
- Call site (`instrumentation.ts:627-638`): chained LAST after
  `backfillCharacterVaults → migrateVaultPhysicalFiles → refreshVaultWardrobe
  → moveSharedWardrobeToGeneral` inside the `.then`; the chain's catch warns
  `Error during character vault backfill or physical-file migration`. v5
  has NO twin of that chain as a boot stage (measured: `host.rs` carries no
  vault/wardrobe boot stage; those v4 migrations were absorbed elsewhere) —
  the v5 slot is the boot-repair thread `seed_built_ins` (`host.rs:1029`,
  called at `:473` from `assemble`), AFTER every repair in it, on the same
  spawned thread (the enqueuer needs the writer and the job table, and it
  must not block the loading screen — the thread already runs off the boot
  path). Wire it there in a `// === P4.82 ===` fence; the result's counts
  go to an `info` line on the host's target.

### The v5 side today

- `job_runner.rs:164`: the type sits in `KNOWN_JOB_TYPES` → the
  "recognized but not yet available" loud fallback; the runner needs no
  change (a registration replaces the fallback).
- Registration: the handler needs a completion wire, so it is a spine-bundle
  handler (`host/spine.rs:3420-3470` `job_handlers`), NOT a `host.rs`
  seam-free one. Shape: `TitleUpdateJobHandler { wire }` / `AvatarJobHandler
  { wire }` (`spine.rs:3003-3037`) — a wrapper constructing the core generic
  handler per job with `self.wire.completion(db)` (`spine.rs:381`) and
  `now_ms` at job time.
- Cheap-LLM twins: `image_job_common::build_cheap_llm_selection(
  &all_profiles, cheap_settings)` (`pub(crate)`, :123 — `isDefault` else
  first, `None` when no profiles; the `cheapLLMSettings` bag from the
  user's `chat_settings` row exactly as `character_avatar_job.rs:244-251`
  reads it), `api_key_service::get_api_key_for_cheap_llm_selection`.
- Reads/writes: `characters_read::find_by_id` / `find_all`;
  `vault_character_update::update_character`; the `llm_logs` row through
  `generate_field`'s existing `log_llm_call`.

## Tiered deliverables

### Tier 1 — must land

1. **`services::headshoulders_backfill_job`** — `HeadShouldersBackfillPayload`
   (`from_json`), the generic `CharacterHeadShouldersBackfillHandler<CMP:
   CompletionProvider>` (+ `now_ms`) implementing `JobHandler`, the eleven
   arms above in v4's order with v4's sentences/levels/field names; the
   `generate_field` exposure in `generators/wizard.rs` (one implementation).
2. **`queue_service::enqueue_character_headshoulders_backfill`** with the
   dedupe + warn-and-fall-through + `priority -1`.
3. **`services::headshoulders_backfill_enqueue::enqueue_headshoulders_backfill
   (main, mount, …)`** — the flag gate, the scan, the per-character
   classification, the enqueue, `markRun`, the result struct, the five log
   lines. Runs on the boot-repair thread (§ above).
4. **Host wiring:** the `HeadShouldersBackfillJobHandler { wire }` wrapper +
   its `job_handlers` row (fenced, at the END of the vec); the enqueuer
   call in `seed_built_ins` after the last repair (fenced).
5. **Differentials** (below): `headshoulders_backfill_tier3_equivalence`
   (the handler, tier 3) and `headshoulders_backfill_enqueue_equivalence`
   (the enqueuer + the queue helper, tier 2).
6. **Wiring pins:** a host test proving the handler is REGISTERED (a seeded
   PENDING job on the committed pair runs through a `ProductionSpineFactory`
   with a canned completion — the `web-test-venue-has-no-spine-factory`
   trap: every LIVE driver is `None` under the plain venue) and a boot test
   proving the enqueuer RUNS on `assemble` (a fresh boot over the pair →
   the `background_jobs` rows + the flag row; a second boot → nothing; a
   pre-set flag → nothing and no scan line). Mutation-proven: unregister
   the handler → the wiring test reddens; skip the enqueuer call → the boot
   test reddens.

### Tier 2 — should land

7. The `llm_logs` row per happy-path case compared (type `CHARACTER_WIZARD`,
   `characterId` set, `provider` = the selection's) — P4.79's shape.
8. The two whitespace asymmetries pinned by cases: a whitespace-only
   `mediumPrompt` (enqueuer enqueues; handler skips silently), and a
   present-but-whitespace `headAndShouldersPrompt` (both treat as absent).
9. A `substring(0, 500)` boundary case whose 500th/501st UTF-16 unit is an
   astral pair (the port must split the pair exactly as `substring` does —
   `utf16_prefix`'s contract; assert the stored bytes).

### Tier 3 — explicit refusal-arm deferrals (loud, typed, never silent)

- The `setImmediate` yield — NO-COUNTERPART (the Rust scan runs on its own
  thread); recorded in the module doc.
- The `Failed to select cheap LLM, skipping` arm if measurement shows v4's
  selection cannot throw — recorded UNREACHABLE with the v4 evidence, the
  sentence kept as a constant for the record.
- `generateField`'s `Failed to log character wizard LLM call` warn — v5's
  `generate_field` already discards the log error (`let _ =`); measure
  whether it warns; if not, port the warn in the SAME fenced edit (it is
  the shared implementation) or record.

## The differential requirement (restated)

Every ported unit ships with an equivalence test driving v4's REAL code.

- **The handler — tier 3** (`headshoulders_backfill_tier3_equivalence`): a
  jest oracle (`harness/oracle/cases/headshoulders-backfill-tier3.test.ts`)
  driving v4's REAL `handleCharacterHeadShouldersBackfill` over a FRESH copy
  of the committed pair per case, `createLLMProvider` canned by CALL INDEX
  (a string = the answer, `{throws}` = a thrown error; record
  `provider|model|temperature|maxTokens|profileParameters|messages` — the
  prompts are the port), `Date` frozen (`updatedAt`), the logger wrapped for
  the six `[HeadShouldersBackfill]` lines. Dump per case: threw/ok, the
  character's `physicalDescription` as the overlay READS it back AND the
  vault store's rendered physical file bytes (key order is the comparand —
  v4's `renderPhysicalPromptsJson`), the `llm_logs` `(type, count)` rows,
  the captured log lines. Closest recipes: `harness/oracle/cases/
  title-update-tier3.test.ts` (a cheap-LLM job handler, canned provider,
  frozen Date, processor off) and `avatar-job.test.ts` (the vault-write
  dump shape with the shared cross-db id map). Cases: not-found; no `pd`;
  prompt already present; present-but-whitespace prompt; no seed; a
  whitespace-only seed; no profiles; no API key (a non-local selection with
  no key row); a local selection (`Some("")`); the provider THROW (job
  fails — compare the thrown message); the empty-text answer; the happy
  path (the merged object written whole — the OTHER tiers survive,
  mutation-proven by dropping the spread); the astral boundary.
- **The enqueuer + queue helper — tier 2** (`headshoulders_backfill_enqueue_
  equivalence`): an oracle driving v4's REAL `enqueueHeadShouldersBackfill`
  (the module's exported function; `getRawDatabase` + `getRepositories` over
  the fixture copy) with cases: the flag already `'true'` (nothing, no scan
  line); a fresh run over the pair (one character without a vault pointer,
  one without `pd`, one with the prompt, one with no seed, one with a
  whitespace-only seed, two eligible — one of them already PENDING, so the
  dedupe answers `isNew:false` → `skipped`); a second run (flag set → no-op).
  Dump `background_jobs` (minted ids remapped, timestamps placeholdered,
  `type`, `payload` bytes, `priority -1`, `maxAttempts 3`, `status`), the
  `instance_settings` flag row, the captured log lines with their bags.
  Closest recipes: `background_jobs_tier2_equivalence` (the jobs-table
  dump + remap) and the P4.D140 boot-heal families for the "second boot
  writes nothing" shape.
- Both families register in the sweep driver's recipe table with headers
  per `recipe-header-conventions` (`cd ~/source/quilltap-server`, staging
  OUTSIDE any `.claude/` path — jest ignores it).

## Fixtures

- Deliver `crates/quilltap-harness/fixtures/headshoulders-{main,mount}.db`
  built by a NEW `harness/oracle/fixtures/build-headshoulders-fixture.ts`
  through v4's REAL repositories at the live checkout (pinned ids, one
  user, one connection profile flagged default + one cheap-LLM
  `chatSettings` row, the seven characters above with vaults where the case
  needs them) — UNLESS a measurement shows `character-generators-{main,
  mount}.db` already carries every shape (it has ONE `headAndShouldersPrompt`
  and `mediumPrompt`s; it lacks the no-vault and whitespace shapes — so the
  new pair is the expected outcome; record the measurement).
- The pair is consumed by both families and the two host tests. Nobody else
  reads it this round.
- `character-generators-{main,mount}.db` is regenerated by NOBODY.

## §R — the round-wide meeting points (BINDING — identical in every order of this round)

Round: **the generator follow-ups + prompt-templates round** — P4.82 ∥ P4.83
∥ P4.84 ∥ P4.85 ∥ P4.86 (P4.86 CONDITIONAL on the human's dependency
ruling — see its preamble). Ordered 2026-09-07. The first non-drift round
since the follow-ups round 2 (2026-09-04): nothing to absorb, every lane is
banked follow-up work from the `p4.9k` and `2f4254b42` rounds.

1. **Baseline and drift.** Oracle baseline **`2f4254b42`** (drift-ledger §1,
   "feat: character subprompts", `4.10.0-dev.1`). v4 `main` HEAD **AT the
   baseline** and v4 `bugfix` tip **`1a2b2164c`** unmoved; the ledger's §3 is
   EMPTY — **zero drift**. No ledger row is ordered by this round.
2. **The freshness probe at lane start** (ledger §2), expected values:
   checkout on **`main`**, tree CLEAN, `git log --oneline 2f4254b42..main`
   EMPTY, `git log --oneline 1a2b2164c..bugfix` EMPTY. **Any mismatch: STOP
   and report** — a lane never writes the ledger and never builds its own
   pin; the planner re-points the round. Re-run the probe before EVERY regen
   batch (the checkout has gone dirty and moved MID-LANE before — ledger
   §5.1; a lane that discovers drift mid-way finishes nothing that depends
   on a regen and reports).
3. **Regen rule: NO PIN REQUIRED** (ledger §1) — every regen runs from the
   live `~/source/quilltap-server` checkout after a passing probe, through
   `harness/tools/recipe_sweep.py --run <family>` (the sanctioned path; it
   refuses empty-stage families and unknown names); never two sweeps
   concurrently. Committed recipe headers keep saying `cd
   ~/source/quilltap-server` and never name a `/tmp` pin
   (`recipe-header-conventions`). The oracle `node_modules` are the LIVE
   tree (`zod` 4.5.4 — `zod_version_guard` fails the gate if it moves; every
   Zod length rule transcribed this round uses the `jsstr::zod_len_*`
   code-point helpers, never `utf16_len`). Check every NDJSON is non-empty
   before believing a diff (the empty-file trap; a family's env var is not
   its regen var — `QT_ORACLE_<FAMILY>` reads it, `QT_ORACLE_OUT` writes it).
4. **Branching.** Every lane branches from `main` into its own worktree:
   `git worktree add <path> -b claude/p4-8N-<slug>-<hash> main`;
   `CARGO_INCREMENTAL=0`; `npm ci` (never `cp -a`) for the SPA lanes. No
   stacking this round. P4.86 does not start unless the human has granted
   the dependency ruling its preamble names.
5. **Fenced-append shared files.** `crates/quilltap-core/src/api/types.rs`,
   `crates/quilltap-core/src/api/engine.rs`, `crates/quilltap-core/src/lib.rs`
   (module lines), `crates/quilltap-core/src/services/mod.rs`,
   `crates/quilltap-core/src/generators/mod.rs`,
   `crates/quilltap-core/src/services/queue_service.rs`,
   `crates/quilltap-host/src/host.rs`, `crates/quilltap-host/src/spine.rs`,
   `crates/quilltap-web/src/lib.rs` (module + route lines): each Rust lane
   appends ONLY inside its own fence (`// === P4.82 ===` … `// === end P4.82
   ===`, opened once per file, new variants/arms/registrations at the END of
   the relevant block, never interleaved). `crates/quilltap-web/src/
   characters_routes.rs` and `system_data_routes.rs` are touched by NOBODY
   this round (P4.85's wire tests live in `crates/quilltap-web/tests/`).
   `Cargo.toml`/`Cargo.lock` at the workspace root and in every crate:
   version lines only — **except P4.86, the ONE lane authorized to add a
   dependency (`jsonschema`), and only once the human's ruling is recorded
   in this round's report.** Any other dependency add is a STOP-and-flag.
6. **`apps/web/**` is split between TWO lanes by file:** P4.83 owns
   `screens/characters/generators/prompts-editor/import-modal.ts`, its NEW
   `prompts-editor/prompt-templates.api.ts`, `screens/characters/edit/
   system-prompts-tab.ts`, `screens/characters/new/new-character.ts` and
   its NEW e2e spec; P4.84 owns `screens/characters/generators/wizard/
   generation-step.ts` (+ its NEW preview-markdown module), `generators/
   optimizer/**`, `generators/ai-import/**`, `chat/cast/add-character-
   dialog.ts`, `screens/salon/salon-conversation.ts`, `screens/characters/
   view/tabs/details-tab.ts`, `ui/prompt-field-label.ts` (+ a NEW
   `ui/prompt-field-example.ts` if the example line is extracted) with
   `screens/characters/edit/scenario-editor.ts` ONLY to adopt that
   extraction, `workspace/chrome/link-interceptor.spec.ts` (spec only), and
   the three existing generator e2e specs. `apps/web/src/app/core/
   core-contract.ts` is FROZEN: P4.83 declares its DTO in its own wire file
   verbatim from its §B and the unifier folds it (the name-for-name diff).
   The Rust-only lanes (P4.82, P4.85, P4.86) finish with `git diff main --
   apps/web/` EMPTY.
7. **Frozen files.** `docs/developer/porting/drift-ledger.md` (lanes never
   write it); `help/**`; `apps/web/src/app/shell/shell.ts` and
   `apps/web/src/app/workspace/workspace-contract.ts` (no new tab kinds).
   `crates/quilltap-web/tests/fixtures/character-generators-{main,mount}.db`
   is READ by P4.82 (if it reuses it), P4.85 and P4.86 and REGENERATED BY
   NOBODY.
8. **Version bumps.** P4.82: `quilltap-core`, `quilltap-harness`,
   `quilltap-host`. P4.83: `quilltap-core`, `quilltap-harness`,
   `quilltap-web`, `apps/web/package.json`. P4.84: `apps/web/package.json`.
   P4.85: `quilltap-core`, `quilltap-harness`, `quilltap-web`. P4.86:
   `quilltap-core`, `quilltap-harness` (+ the root `Cargo.lock` for the
   dependency). Per commit. The unifier RECOUNTS as base + total bumps —
   identical bumps merge silently (`commit-checklist`; the playbook's
   silent-auto-merge trap), and a `Cargo.toml` conflict is NOT version-only
   when P4.86 is in the round (the `p4.9i2` §3 catch: a `--ours` resolution
   dropped a feature block).
9. **e2e.** Port 4319 is repo-wide — ONE Playwright at a time across all
   lanes. P4.83 and P4.84 each run ONLY their own spec files by name (never
   the full suite — that is the unifier's), and never kill a server they did
   not start (`pkill -f` can kill a sibling lane's gate; read `pgrep -fl`
   argv first). A held port means WAIT, not force. A beat's mock must speak
   the runner's transport (the optimizer and AI-import runners call the
   NON-streaming `send_message`; the shared `support/mock-llm.ts` speaks
   only SSE — see the spec headers' recipes).
10. **Append-only for everyone:** `docs/CHANGELOG.md`,
    `docs/developer/porting/status-log.md`.
11. **Fixture ownership.** P4.82 delivers a NEW committed
    `crates/quilltap-harness/fixtures/headshoulders-{main,mount}.db` pair (+
    its builder under `harness/oracle/fixtures/`, v4's REAL repositories,
    pinned ids) unless it can prove an existing pair carries every shape it
    needs (record the measurement either way). P4.83 builds its fixture in
    `/tmp` from a clean provisioned instance inside its recipe (the
    `settings_wire_actions` precedent) and commits NOTHING under
    `fixtures/`. P4.85 and P4.86 EXTEND the four generator tier-3 oracle
    cases (three P4.85's, the AI-import one P4.86's) — the unifier
    regenerates all four ONCE at unification after both lanes' edits are in
    (the P4.D119 rule). P4.84 seeds through the running server's verbs,
    never SQL.
12. **Census guards move when a verb is added.** P4.83 adds dispatch verbs;
    adding any verb, or closing a recorded divergence (P4.86 retires
    `VALIDATION_UNAVAILABLE`'s both-directions pins), moves the executable
    censuses in OTHER crates (`a-new-verb-moves-the-dispatch-wrong-type-
    census`, `closing-a-divergence-moves-the-censuses`) — every Rust lane's
    gate is `cargo test --workspace`, never `-p`, with the full log captured
    to a file + a sentinel (never `| tail`, never a `pgrep` poll loop).
13. **Log-line pins** use the thread-scoped capture seam (`a-process-global-
    test-seam-must-be-thread-scoped`); a `%x` field renders unquoted, a bare
    or `?x` field quoted (`tracing-percent-field-renders-unquoted`) — read
    v4's `logger.<level>(msg, bag)` bag and pin the LEVEL, the sentence and
    the field NAMES.

## Ownership (BINDING — identical table in every order of this round)

| Lane | Owns (edit freely) | Fenced-append (§R.5) | Must not touch |
| --- | --- | --- | --- |
| **P4.82** | NEW `crates/quilltap-core/src/services/headshoulders_backfill_job.rs`; NEW `crates/quilltap-core/src/services/headshoulders_backfill_enqueue.rs`; `crates/quilltap-core/src/generators/wizard.rs` ONLY for the `generate_field` / `WizardCallCtx` visibility edit (one fenced block, no behaviour change); NEW harness families `headshoulders_backfill_tier3_equivalence` + `headshoulders_backfill_enqueue_equivalence` + their oracle cases + the fixture builder + the committed pair; NEW host tests under `crates/quilltap-host/tests/` | `services/queue_service.rs`, `services/mod.rs`, `host/host.rs`, `host/spine.rs` | `api/**`, `apps/web/**`, every other generator file |
| **P4.83** | `crates/quilltap-core/src/db/prompt_templates.rs`; NEW `crates/quilltap-core/src/api/prompt_templates.rs`; NEW `crates/quilltap-core/src/services/builtin_prompt_templates.rs` + NEW `crates/quilltap-core/src/services/builtin_prompt_templates.json`; NEW `harness/oracle/provision/dump-prompt-templates.ts`; NEW `crates/quilltap-web/src/prompt_templates_routes.rs` (+ its wire test); NEW harness family `prompt_templates_routes_equivalence` + oracle case; SPA: `prompts-editor/import-modal.ts`, NEW `prompts-editor/prompt-templates.api.ts`, `edit/system-prompts-tab.ts`, `new/new-character.ts`, NEW `e2e/prompt-templates-flow.spec.ts` | `api/types.rs`, `api/engine.rs`, `core/lib.rs`, `services/mod.rs`, `web/lib.rs` | `core-contract.ts` (frozen), every P4.84 SPA file, every other Rust file |
| **P4.84** | SPA only: `generators/wizard/generation-step.ts` + NEW `generators/wizard/generation-preview-markdown.ts`; `generators/optimizer/**`; `generators/ai-import/**`; `chat/cast/add-character-dialog.ts`; `screens/salon/salon-conversation.ts`; `screens/characters/view/tabs/details-tab.ts`; `ui/prompt-field-label.ts` (+ NEW `ui/prompt-field-example.ts`) and `edit/scenario-editor.ts` ONLY to adopt the extraction; `workspace/chrome/link-interceptor.spec.ts`; the existing `e2e/character-wizard-flow.spec.ts`, `e2e/character-optimizer-flow.spec.ts`, `e2e/ai-import-flow.spec.ts` | — | every Rust file; `import-modal.ts`, `system-prompts-tab.ts`, `new-character.ts` (P4.83's); `core-contract.ts` |
| **P4.85** | `crates/quilltap-core/src/api/salon.rs`; `crates/quilltap-core/src/api/generators_detail.rs`; `crates/quilltap-core/src/api/image_profiles.rs`; `crates/quilltap-web/src/generator_sse.rs`; `crates/quilltap-web/tests/generator_sse_wire.rs` + `crates/quilltap-web/tests/characters_generators_routes.rs`; harness `character_optimizer_tier3_equivalence.rs`, `character_wizard_tier3_equivalence.rs`, `external_prompt_tier3_equivalence.rs` + their oracle cases (`character-optimizer-tier3.test.ts`, `character-wizard-tier3.test.ts`, `external-prompt-tier3.test.ts`) + their per-family fixture JSONs; the family that covers `image-profiles?action=generate` (+ a new astral-prompt arm) | — | `api/generators_wizard.rs`, `generators/ai_import.rs`, `ai_import_tier3_equivalence.rs`, `ai-import-tier3.test.ts` (P4.86's); `apps/web/**` |
| **P4.86** (conditional) | `crates/quilltap-core/Cargo.toml` (the ONE `jsonschema` dependency line) + the root `Cargo.lock`; NEW `crates/quilltap-core/src/generators/qtap_schema.rs` + NEW vendored `crates/quilltap-core/src/generators/qtap-export.schema.json`; `crates/quilltap-core/src/generators/ai_import.rs`; `crates/quilltap-core/src/api/generators_wizard.rs`; harness `ai_import_tier3_equivalence.rs` + `harness/oracle/cases/ai-import-tier3.test.ts` + `harness/oracle/fixtures/ai-import-tier3.json`; NEW harness family `qtap_schema_validate_equivalence` + its oracle case + recorded corpus | `generators/mod.rs` | every other file; `apps/web/**` |

Everyone: `docs/CHANGELOG.md` + `status-log.md` append-only; the drift
ledger frozen; `core-contract.ts` / `shell.ts` / `workspace-contract.ts`
frozen; `characters_routes.rs` / `system_data_routes.rs` untouched.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; clippy both feature sets (`--all-targets --
   -D warnings`, and with `--features test-support` where the workspace
   defines it — copy the exact two lines from the last round record);
   `cargo build --workspace`; the release build.
2. `cargo test --workspace` (full log to a file + a sentinel; NEVER `| tail`,
   NEVER a `pgrep` poll loop) with BOTH families' oracle vars + fixture
   vars; `SKIP:` zero for them; the by-name `--nocapture` runs.
3. Both families by NAME through `recipe_sweep.py --run <family>` after a
   passing §2 probe; `--show` the headers.
4. Mutation proofs (each reddening exactly its rows): the `...pd` spread
   dropped (the other tiers vanish); the `.trim()` on the seed dropped
   (the whitespace case); `priority` left default; the dedupe read
   skipped; `markRun` skipped on the zero-enqueued case; the handler
   unregistered; the enqueuer call removed from `seed_built_ins`.
5. Commits per `.claude/commands/commit.md` (core + harness + host
   versions; CHANGELOG every commit); the lane record appended to
   `status-log.md` (name the fixture measurement, the unreachable-arm
   evidence, the cross-app flag note for the dogfood pass); this order's
   status header updated.
