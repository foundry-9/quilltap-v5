# Work order P4.D111 — the bug-97 OpenRouter vision convergence + the docblock-note retirement (v4 `7a6716b5` + `0ba942b1`)

**Status:** OPEN (planned 2026-08-23).

## Preamble

- **v4 baseline for this round: `0ba942b1`** (v4 main, HEAD at planning
  2026-08-23, tree CLEAN, checkout on `main`). The round absorbs all three
  commits past the old `a14a1811` baseline: `3c041e46` (bug 96 — the sibling
  P4.D110 lane), `7a6716b5` (the bug-97 filing: docs + the comment-only
  `moderation-finish-reason.ts` docblock correction — THIS lane's item 6),
  `0ba942b1` (the bug-97 fix — THIS lane). `bugfix` measured at planning:
  HEAD `3a76b17d`, nothing unabsorbed beyond the tests-only `009c49b2`.
- **Drift-check at lane start.** `git log 0ba942b1..main --oneline` AND
  `git diff main bugfix -- lib/ app/ packages/`. If v4 moved, **STOP** and
  report. Note which branch the checkout occupies before any regen.
- **Regen pin:** v4 HEAD == the round baseline at planning, so regens may run
  from the live checkout while it stays clean AND on `0ba942b1`; the moment
  it dirties or moves, build a **lane-unique** detached worktree
  (`/tmp/qt-v4-pin-d111-0ba942b1`, ALL THREE symlink classes — this lane
  loads built plugins, so the `plugins/dist/*/node_modules` class is
  load-bearing, not optional) and re-run every regen already done. ⚠ The
  prepared `/tmp/qt-v4-a14a1811` worktree is at the OLD baseline — it
  predates the fix this lane converges on; never regen from it.
- **Sibling lanes:** P4.D110 (`p4.d110-title-verdict.md`, bug 96 — fully
  disjoint files), P4.58 (`p4.58-corpus-maintenance-smalls.md` — fully
  disjoint files). §C below is round facts only; no code contract is shared.
- **Worktree:** per the `carryout` skill; edit inside the worktree, not main.
  `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` are
  append-only (union-merge at unification).

## The drift, classified (surveyed 2026-08-23, fresh at `0ba942b1`)

This is the **pre-announced convergence round** for this port's own upstream
filing. P4.D106 pinned v4's registry/static transport contradiction as a
faithful reproduction with the note "v5's pins converge on the manifest regen
at the drift round after v4 fixes it" — v4 fixed it at `0ba942b1`, so the
pins retire to plain equalities and v5's OpenRouter registry-tier answer
flips true. **v5 source changes are small and enumerated; most of the lane is
regen + pin retirement + red-first proof.**

### What v4 shipped at `0ba942b1` (survey 2026-08-23)

1. **`plugins/dist/qtap-plugin-openrouter/index.ts`** (plugin 1.0.58 →
   1.0.59): the `attachmentSupport` block becomes
   ```ts
   const attachmentSupport = {
     supportsAttachments: true as const,
     supportedMimeTypes: SUPPORTED_IMAGE_MIME_TYPES,
     description: 'Images (JPEG, PNG, GIF, WebP) — requires a vision-capable routed model',
     notes:
       'OpenRouter proxies to 100+ models with varying capabilities. The plugin forwards images for any model and lets the host decide: attachments only reach a profile whose "Supports image attachments" flag is set, and the describe-fallback substitutes text otherwise.',
   };
   ```
   with a long doc comment naming bug 97 and the single-source rule. The
   built `index.js` carries the same change.
2. **`plugins/dist/qtap-plugin-openrouter/provider.ts`**: the module-local
   MIME list is now `export const SUPPORTED_IMAGE_MIME_TYPES: string[] =
   ['image/jpeg', 'image/png', 'image/gif', 'image/webp']` — the single
   source of truth the declaration imports.
3. **`lib/chat/file-attachment-fallback.ts`** (inside
   `describeImageWithProfile`, ~:250): a new 7-line keep-in-step comment
   tying the recommendation list to `PROVIDER_ATTACHMENT_CAPABILITIES` and
   naming bug 97 + the new test; and the guard sentence's list changes by
   exactly one entry — `NanoGPT` inserted between OpenRouter and Z.AI:
   `…Pick a describer on a provider that does (OpenAI, Anthropic, Google,
   Grok, OpenRouter, NanoGPT, Z.AI).`
4. **NEW `__tests__/unit/lib/llm/image-transport.test.ts`** (197 lines):
   loads every bundled plugin's BUILT `index.js` (the object the registry
   hands the predicate) and asserts each declaration answers the same as the
   static mirror; regressing the built declaration to false fails two cases,
   both naming OPENROUTER.
5. Non-lib: `PROVIDER_PLUGIN_DEVELOPMENT.md` +23, bugs.md/bug file updates,
   `help/connection-profiles.md` +2 (banked — see Tier 2).
6. **`7a6716b5`'s one lib line**: `lib/llm/moderation-finish-reason.ts`
   docblock "(bug 94)" → "bug 93" — comment-only; v5's rider is item 6
   below.

## Survey-verified v5 convergence sites (2026-08-23)

| # | Site | Action |
|---|------|--------|
| 1 | `crates/quilltap-core/src/provider_manifest/manifests/openrouter.json:85–93` — the pre-fix `attachment` block (`supportsAttachments: false`, empty MIME list, old sentences) | Regen via `harness/oracle/providers/gen-provider-manifests.mjs` (recipe in its header ~:47–52; it loads the BUILT plugin at :199 and derives the attachment block mechanically at :448–455 — attachment fields are NOT in the augmentation table, so the regen picks up the flip with zero generator change; verify the nine sibling manifests come back byte-identical) |
| 2 | `crates/quilltap-core/src/files/image_transport.rs:84` — `assert!(!provider_can_transport_images("OPENROUTER"))` under the reproduced-faithfully comment (:81–83) | Flip the assert; move OPENROUTER into the transporting loop at :75 (`OPENAI/ANTHROPIC/GOOGLE/GROK/Z_AI/NANOGPT`) |
| 3 | `image_transport.rs:29–37` — the module-header divergence pin ("a v4 bug to file upstream") | Rewrite as a converged note (filed as v4 bug 97, fixed at `0ba942b1`, v5 converged this lane) |
| 4 | `crates/quilltap-core/src/files/attachment_support.rs:21–26` — the header narrating the registry/static disagreement with OPENROUTER as the example | Retire the example; the OPENROUTER row itself (:55, four image types) is already correct — DO NOT touch the data rows |
| 5 | `crates/quilltap-core/src/services/file_fallback.rs:449–453` — the guard sentence | Insert `NanoGPT, ` between OpenRouter and Z.AI (byte-exact to v4's new sentence); carry v4's keep-in-step comment adapted to v5's map location (this is the ONLY copy — repo-grep for "Pick a describer" confirms) |
| 6 | `crates/quilltap-core/src/moderation_finish_reason.rs:15–17` — the mis-number discrepancy note | Retire (v4 corrected its docblock at `7a6716b5`; the note is now wrong about v4) |
| 7 | The oracle families (below) | Regen fresh at the pin, red-first |
| 8 | The P4.D106 lane-record OPEN row (`status-log.md` ~:83132–83135) + the memory note `v4-attachment-tiers-disagree.md` | The status-log row closes via this lane's append-only record; the MEMORY note is amended at unification by the supervisor, not by this lane — say so in the lane record |
| 9 | `help/connection-profiles.md` +2 | Bank verbatim to the `p4.9i2` pool (Tier 2 item) |
| 10 | SPA `apps/web/src/app/screens/settings/providers/attachment-support.ts` | **NO change** — OPENROUTER already lists the four image types at :47 (v4's static map was correct throughout and `0ba942b1` did not touch it). Leave the row comment as v4 leaves its own. |

## Mandate

Converge every v5 pin of v4 bug 97 onto the fixed upstream: the manifest
regen, the predicate-test flip, the two stale header narratives, the guard
sentence's NanoGPT entry, and the moderation docblock-note retirement — with
the three affected families regenerated red-first at the round pin. **Do NOT
touch:** the static capability rows in `attachment_support.rs`, the SPA
attachment table, any P4.D110/P4.58-owned file, the nine sibling provider
manifests (regen must prove them byte-identical — a diff there is a STOP,
per the manifest-generator-augmentation-rot memory).

## Tiered deliverables

**Tier 1 (must land):**

1. The manifest regen (site 1), with the nine non-OpenRouter manifests
   proven byte-identical (`git diff --stat` over the manifests dir must show
   ONLY `openrouter.json`). If any sibling moved, stop and diagnose (the
   generator's augmentation table has reverted hand-fixes before).
2. Sites 2–5: the predicate-test flip + both header rewrites + the guard
   sentence. The sentence must be byte-exact against v4's new literal —
   diff it against `git show 0ba942b1:lib/chat/file-attachment-fallback.ts`
   output, not against this order's transcription.
3. Site 6: the docblock-note retirement.
4. **Red-first proof, per family** (the convergence discipline — the whole
   point of both-direction pins): regenerate each family's oracle fresh at
   the pin and run it against UNCHANGED v5 first; `image_transport_equivalence`
   and `provider_registry_equivalence` must RED on the manifest,
   `file_attachment_tier3_equivalence` must RED on the guard sentence. Then
   land the changes and re-run green. Record both runs in the lane record.
5. The lane record notes that the fixture's deliberate Z_AI describer pick
   (P4.D106's tiers-disagree workaround) is now robust under either tier for
   OPENROUTER too — no fixture change required or wanted.

**Tier 2 (should land):**

6. Bank v4's `help/connection-profiles.md` +2 paragraph verbatim into the
   `p4.9i2` pool (the phase-4.md bank list), preserving the house voice
   text exactly.

**Tier 3 (explicit deferrals — refuse loudly, never stub):**

- None. This lane has no refusal-armed surface; everything in scope lands or
  the lane stops and reports.

## The differential requirement

The three families and their recipes (verified 2026-08-23; the sweep driver
`harness/tools/recipe_sweep.py` is the sanctioned regen path — run by name,
zero SKIP):

- **`image_transport_equivalence`** —
  `crates/quilltap-harness/tests/image_transport_equivalence.rs`
  (`image_transport_predicates_match_v4`, :54; env
  `QT_ORACLE_IMAGE_TRANSPORT`). Oracle `harness/oracle/cases/image-transport.ts`
  (three sections static / full_uninit / full_init; `full_init` loads all ten
  real dist bundles — the section that flips). Recipe in the test header
  :29–34.
- **`provider_registry_equivalence`** —
  `crates/quilltap-harness/tests/provider_registry_equivalence.rs`
  (`provider_registry_matches_oracle`, :164; env
  `QT_ORACLE_PROVIDER_REGISTRY`). Oracle
  `harness/oracle/cases/provider-registry.ts`. This is the family that pins
  the manifest's `attachment` block bytes (the P4.D107 finding — NOT
  `providers_listing`).
- **`file_attachment_tier3_equivalence`** —
  `crates/quilltap-harness/tests/file_attachment_tier3_equivalence.rs`
  (`file_attachment_matches_oracle`, :221; env `QT_ORACLE_FILE_ATTACHMENT` +
  `QT_FIXTURE_FILE_ATTACH_MAIN/MOUNT`). Oracle case
  `harness/oracle/cases/file-attachment-tier3.test.ts` (jest, staged outside
  `.claude/`; builder `build-file-attachment-fixture.ts`; fixture
  `file-attachment.json`). The guard-sentence rows
  (`fb_ollama_describer_guard`, case D at :329–366;
  `fb_autopick_excludes_non_transporting`, case E) carry the recommendation
  list in their `error` bytes — the sentence delta shows regardless of
  registry tier because the literal is fixed.
- Adjacent, NOT moved by this commit (regen only if the sweep driver pulls
  them in): `attachment_anchor_equivalence`, `moderation_finish_reason_equivalence`
  (the sentence TABLE is unchanged; only v5's docblock note moves — a
  comment, invisible to the family).

## Fixtures

- Consumes `harness/oracle/fixtures/file-attachment.json` +
  `build-file-attachment-fixture.ts` — **unchanged** (Tier-1 item 5 records
  why). No fixture this lane owns changes, so no dependent-oracle sweep
  beyond the three named families.

## §C — Shared round facts (identical in all three orders, binding)

- Round baseline: **`0ba942b1`**. Every oracle regen in this round runs at
  it — live checkout only while clean AND at that HEAD; otherwise a
  lane-unique pin worktree with all three symlink classes.
- `/tmp/qt-v4-a14a1811` is the PREVIOUS baseline's pin — stale for every
  family this round touches; never regen from it.
- Never run two sweep-driver invocations concurrently across lanes.
- Version-bump ownership: P4.D110 bumps `quilltap-core` + `quilltap-harness`;
  P4.D111 bumps `quilltap-core` + `quilltap-harness`; P4.58 bumps
  `quilltap-harness` only. Identical bumps off the same base are recounted by
  the unifier (base + total bumps) — bump normally, do not coordinate
  numbers.

## Ownership (disjoint by construction)

This lane owns:
- `crates/quilltap-core/src/files/image_transport.rs`
- `crates/quilltap-core/src/files/attachment_support.rs` (header comment ONLY)
- `crates/quilltap-core/src/services/file_fallback.rs`
- `crates/quilltap-core/src/moderation_finish_reason.rs`
- `crates/quilltap-core/src/provider_manifest/manifests/openrouter.json`
  (via regen)
- `crates/quilltap-harness/tests/{image_transport,provider_registry,file_attachment_tier3}_equivalence.rs`
  (if pin-comment edits are needed)
- The `p4.9i2` bank section in `docs/developer/porting/phase-4.md`
  (append-only addition to the bank list)

It must NOT touch: `context_summary/**`, `title_update_job.rs`, the
title-update oracle family (P4.D110); the photo-tools and settings-routes
oracle families and fixtures (P4.58); `apps/web/**`; the static capability
ROWS of `attachment_support.rs`; the nine sibling manifests.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`.
2. `cargo clippy --workspace --all-targets -- -D warnings`, plain AND
   `--features quilltap-core/native-transport`.
3. `cargo test --workspace` with the lane's oracle env vars — full log
   captured (NEVER `| tail`), per-binary `test result:` lines kept.
4. The three families BY NAME with `--nocapture`, zero SKIP, over oracles
   fresh at the pin — with the red-first runs recorded (Tier-1 item 4), and
   the fresh NDJSONs grepped for the changed bytes (the new `notes` sentence,
   the NanoGPT-bearing guard sentence).
5. `git diff --stat` over the manifests dir showing ONLY `openrouter.json`.
6. No SPA changes → no ng/Playwright run required; state that explicitly.
7. Per-commit rule: follow `.claude/commands/commit.md`; version bumps per
   §C.
