# Work order P4.6al — the D17 editor follow-ons + composer lane (lane B, SPA)

> **STATUS: LANE COMPLETE (branch `claude/p4-6al-editor-followons-63c57e`,
> awaits unification).** All Tier 1 (must) + Tier 2 items 4–5 landed;
> Tier 2 item 6 + all Tier 3 items deferred (loud, enumerated below).
>
> **LANDED:**
> - Item 1 — strikethrough/highlight marks + emphasis-on-type input
>   rules; the round-trip gate grew +8, the keymap spec +9.
> - Item 2 — composition mode composer-side (`submitOnModEnter`,
>   composer toggle + `compositionModeChange`, the Settings Chat
>   "Composition Mode" card). Salon binding is a §3 unification wire.
> - Item 3 — the shared `qt-markdown-field` (+ `qt-formatting-toolbar`,
>   `RichEditor.runCommand`/`inCodeBlock`), adopted in the memory
>   editor + the character edit Details tab (7 fields) + new-character
>   (7 + scenario). `roleplayTemplateId` toolbar delimiters + the
>   source-mode toggle deferred.
> - Item 4 — composer draft persistence (localStorage, 800ms debounce,
>   restore-once, blank-removes, clear-on-send; no expiry).
> - Item 5 — the text-replacement plugin (v4 trigger semantics) + the
>   compiled-rules helper + the TextReplacementSettings CRUD card +
>   the core-contract §2 block + the client api. Composer rules +
>   enabled gate are §3 unification wires.
>
> **DEFERRED (loud):**
> - **Item 6 — further form-field adoptions.** The named sites
>   (ScenariosManager cards, roleplay-template systemPrompt in the
>   Settings Templates tab, prompt modals, wardrobe item description,
>   Prospero settings instructions, the new-chat scenario field) sit in
>   components outside this lane's core ownership — several without a
>   markdown textarea today (e.g. `roleplay-templates-card.ts` has no
>   systemPrompt textarea) — and one ("characters view image-prompt
>   fields" = `characters/view/tabs/details-tab`) is **lane C's**
>   (HANDS-OFF). Deferred to keep the merge surface focused; each is a
>   clean `qt-markdown-field` swap when picked up.
> - **Tier 3** — the GFM table transformer (`prosemirror-tables` +
>   multiline round-trip); consumers whose v5 host does not exist
>   (CreateNPC/ComposeMail/InsertAnnouncement dialogs);
>   `roleplayTemplateId` toolbar awareness; `__bold__` on-type
>   auto-format (typed `__bold__` stays literal, still byte-faithful);
>   the composition-mode + draft + text-replacement live-salon e2e
>   beats (all §3 UNIFICATION deliverables).
>
> **§3 unification wires (NOT in-lane):** the salon binding
> `documentEditingMode` ↔ `[compositionMode]`/`[chatId]` +
> `compositionModeChange` → `chatUpdate`; the salon fetching + compiling
> the text-replacement rules and binding `[textReplacementRules]` /
> `[textReplacementsEnabled]`. Lane C owns `salon-conversation.ts`.
>
> **Original status (for reference): OPEN.**
>
> Lane B of the THREE-lane P4.6ak ∥ P4.6al ∥ P4.6am round (the D17
> editor follow-ons + salon dogfood round, planned 2026-07-14). **v4
> reference baseline: HEAD `02865bdb`** (`~/source/quilltap-server`) —
> run `git log 02865bdb..HEAD --oneline` at lane start; **if HEAD
> moved, STOP and report.** Sibling lanes:
> `p4.6ak-text-replacements-background-server.md` (server),
> `p4.6am-salon-dogfood-slice.md` (salon SPA). The **Shared contract**
> and **Ownership** sections below are binding and reproduced verbatim
> in all three orders. You run in a worktree (it needs its OWN Rust
> binaries for e2e — the P4.6af memory note; run playwright from
> `apps/web` with the local bin); the unification pass integrates the
> lanes onto main.

## Mandate

Deliver the D17 tier-3 editor follow-ons enumerated at the P4.6ag
close (status-log round record, 2026-07-14) plus dogfood finding #8
(composition mode): the emphasis-on-type input rules with the
strikethrough/highlight marks they need, the shared form-field editor
(`MarkdownLexicalEditor`'s v5 analogue) adopted in the highest-value
existing consumers, composition mode end-to-end minus the salon
binding (a unification wire), draft persistence, and the
text-replacement plugin + settings card over lane A's pinned surface.
The byte-round-trip gate (`editor/markdown-round-trip.spec.ts`)
remains the dialect's proof — every dialect change extends its corpus
and the gate stays GREEN.

Do NOT touch `crates/**` (lane A), `screens/salon/**`,
`chat/message-list.*`, `chat/streaming-message.*`,
`core/chat-stream.reducer.*`, `characters/view/tabs/details-tab.*`,
`styles/qt-components/_chat.css`, `e2e/global-setup.ts` (all lane C),
or `chat/render/**` (the standing HANDS-OFF render pipeline —
message DISPLAY of tables/strike is remark-gfm's job in v4 and the
ported pipeline's here, not this lane's). Do not edit existing e2e
spec files except where a change is named below.

## Survey-verified starting points (2026-07-14 @ `02865bdb` — do not re-derive)

### v5 today (what you're extending)

- `apps/web/src/app/editor/` — `rich-editor.ts` (inputs `value`,
  `placeholder`, `disabled`, `submitOnEnter`, `ariaLabel`; outputs
  `contentChange`, `submit`, `imagePaste`; imperative handle `focus()/
  getMarkdown()/setMarkdown()/prependText()`; the component IS the
  handle), `editing-commands.ts` (block input rules only: `> `, `1. `,
  `- `, ``` ``` ```, `#`-headings; formatting keymap Mod-b/i/`;
  deliberately NO single-`*` rule — dialect quirk #6, permanent),
  `markdown-dialect.ts` (schema + parse/serialize),
  `markdown-round-trip.spec.ts` (28-entry gate). Enter handling:
  `rich-editor.ts:169-201` — `submitCmd` fires only when
  `submitOnEnter()`, else falls through to
  `splitListItem`/`newlineInCode`; `Shift-Enter` always inserts
  `hard_break`.
- Adoption sites: `chat/chat-composer.ts:82-92`
  (`[submitOnEnter]="true"`, send reads `getMarkdown()` at submit
  time, emits `ComposerSend { content, fileIds }`, clears via
  `setMarkdown('')`); `documents/document-pane.ts:162-181` (rich
  editor for markdown files behind `USES_RICH_MARKDOWN_EDITOR`, raw
  textarea otherwise).
- The composer has NO mode logic and NO chatId input today; the
  Settings Chat tab (`screens/settings/chat/chat-tab.ts`) has three
  cards and lists "Composition Mode" as dead deferral text at
  `:84-87`.
- Form fields still on textareas: `memory/memory-editor.ts:58-65`
  (content), `screens/characters/edit/details-tab.ts` (7 markdown
  textareas: identity/description/manifesto/personality/firstMessage/
  exampleDialogues/systemPrompt — doc comment at `:15-16` names the
  deferral), `screens/characters/new/new-character.ts:94-262` (same
  fields + scenario).
- There is NO FormattingToolbar component in v5 yet.

### v4 composition mode (dogfood #8)

- Terminology gotcha: the per-chat flag is named
  **`documentEditingMode`** (a `chats` column,
  `lib/schemas/chat.types.ts:610`); the user default is
  **`chat_settings.compositionModeDefault`**, baked into the chat at
  creation (`app/api/v1/chats/route.ts:1145`). **Both are already
  ported server-side** (v5 `chats_read.rs`, `salon.rs:390`,
  `services/chat_create.rs:498`, `api/settings.rs:593-596`) — this is
  a pure SPA feature.
- The key decision logic is `components/chat/lexical/plugins/
  KeyboardPlugin.tsx:113-142`: composition mode → plain Enter falls
  through to paragraph insertion, Cmd+Enter (Mac, `metaKey &&
  !ctrlKey`) / Ctrl+Enter (non-Mac, `ctrlKey && !metaKey`) submits;
  chat mode → bare Enter (no shift/meta/ctrl) submits, everything
  else falls through. `isMac` at `:39-41`. In v5 this maps to:
  `[submitOnEnter]="!compositionMode"` + the NEW `submitOnModEnter`
  input (Shared contract §3).
- The toggle button: `app/salon/[id]/components/ChatComposer.tsx:
  353-367` — a `qt-chat-toolbar-button` with `-active` state and
  titles "Switch to chat mode (Enter to send)" / "Switch to
  composition mode (Cmd|Ctrl+Enter to send)".
- The default-settings card: `components/settings/chat-settings/
  CompositionModeDefaultSettings.tsx` — label "Start New Chats in
  Composition Mode", inside a `CollapsibleCard title="Composition
  Mode" sectionId="composition-mode"` in `ChatTabContent.tsx:71-74`;
  saves `{ compositionModeDefault }` via the settings-chat PUT
  (`useChatSettings.ts:496-525`) — the v5 settings PUT already
  accepts it.
- Toggle persistence (the salon side — the UNIFICATION WIRE, not
  yours): v4 `useChatControls.ts:348-365` PUTs
  `{ chat: { documentEditingMode } }`.

### v4 emphasis-on-type + marks (D17 tier-3 items 1 + 3)

- v4 auto-formats live via Lexical's `MarkdownShortcutPlugin` with
  `COMPOSER_TRANSFORMERS` (`LexicalComposerWrapper.tsx:143`,
  `MarkdownLexicalEditor.tsx:99`): typing the CLOSING delimiter
  converts in place — `_italic_`, `**bold**`, `` `code` ``,
  `~~strike~~`, `==highlight==`. `ITALIC_STAR`/`BOLD_ITALIC_STAR` are
  excluded (`MarkdownBridgePlugin.tsx:60-66`) so `*narration*` NEVER
  auto-formats — that guarantee is permanent in v5 too.
- Marks: `STRIKETHROUGH` (`~~`, `MarkdownBridgePlugin.tsx:35,71`) and
  `HIGHLIGHT` (`==`, `:36,74`) are part of the dialect on EVERY v4
  editing surface. Export protects literal tildes via
  `preserveTildes`/`stripMarkdownEscapes`
  (`MarkdownBridgePlugin.tsx:114-137,147-151`; composer preserve list
  `ComposerSyncPlugin.tsx:41`). Note `==` is an editor-side
  convention — remark-gfm does NOT render it in messages; do not
  touch message rendering.

### v4 form-field consumers (D17 tier-3 item 4)

All v4 consumers embed ONE shared component,
`components/markdown-editor/MarkdownLexicalEditor.tsx` ("Designed for
forms" `:6-8`), which hardwires the composer dialect (`:99-104`) and
mounts RichText/History/List/CheckList/Table plugins + a
`FormattingToolbar` (`:83-107,128-129`) — but NOT
TextReplacementPlugin (composer-only). Key props: `remountKey`
(re-init when the record changes — v5's absorb-once `value` input
needs an equivalent re-key or `setMarkdown` idiom) and
`roleplayTemplateId` (template-aware toolbar delimiters). Consumer
inventory (adopt only where a v5 component EXISTS; the rest are
enumerated deferrals): memory content (`memory/memory-editor.tsx:146`),
wardrobe item description, `AestheticEditorField`, roleplay-template
systemPrompt, prompt modals (settings + characters), scenario editor
modal, new-chat scenario, AddCharacter/CreateNPC/ComposeMail/
InsertAnnouncement dialogs, Prospero settings instructions, Aurora
new/edit character fields (`NewCharacterView.tsx:305-433`,
`CharacterBasicInfo.tsx:341-548`), Aurora view image-prompt fields
(`DescriptionsTab.tsx:230-332`). The toolbar:
`components/chat/FormattingToolbar.tsx` +
`FormattingCommandPlugin.tsx` (`:19-83`), dialect per
`lib/chat/annotations.ts:42-54`.

### v4 TextReplacementPlugin (D17 tier-3 item 5a)

`components/chat/lexical/plugins/TextReplacementPlugin.tsx` — literal
word-boundary replacements ON TYPED INPUT ONLY: KEY_DOWN at LOW
priority (`:173`), only when the caret sits at the very end of a text
node (`:119`), word-start walk (`:121-124`), trigger chars = space,
NBSP, tab, `.` `,` `;` `:` `!` `?` `)` — newline deliberately
excluded (`:16-18,47-58`); no IME composition, no paste (`:16,102`);
the rewrite is one tagged update so a single undo reverts
(`:141-168`). Rules come from lane A's surface (GET
`/api/v1/settings/text-replacements`), compiled into
caseSensitive-first maps (`lib/text-replacement/
useTextReplacementRules.ts:50-83`); the on/off gate is
`chat_settings.textReplacementsEnabled` (already ported). Installed
ONLY in the composer (`LexicalComposerWrapper.tsx:157`) — form fields
do NOT get it. Rule-management UI:
`components/settings/chat-settings/TextReplacementSettings.tsx`,
rendered in `ChatTabContent.tsx:95`.

### v4 draft persistence (D17 tier-3 item 5b)

localStorage key `` `quilltap-draft-${chatId}` ``
(`app/salon/[id]/hooks/useDraftPersistence.ts:25`). Restore once on
mount (`:29-41`); save on an **800 ms** debounce
(`ComposerSyncPlugin.tsx:65,118-128`, written via ref — no
re-render); blank text removes the key (`:47-51`); a successful send
clears it (`useSSEStreaming.ts:574,595`). **No expiry.** In v5 this
is composer-owned, keyed off the NEW `chatId` input (§3); the salon
passes chatId at unification.

### v4 table transformer (D17 tier-3 item 2 — tier 3 here)

`components/chat/lexical/transformers/table-transformer.ts:119-258` —
GFM pipe tables (header + separator required, `:---`/`:---:`/`---:`
alignment parse, escaped `\|`, export pads and always writes LEFT
alignment `:230-232`). Editing surfaces only; messages render tables
via remark-gfm separately.

## Deliverables

### Tier 1 (must land)

1. **Marks + emphasis-on-type input rules:** add `strikethrough`
   (`~~`) and `highlight` (`==`) marks to the dialect schema +
   serializer (with the literal-tilde protection v4 applies), then the
   closing-delimiter input rules for `_italic_`, `**bold**`,
   `` `code` ``, `~~strike~~`, `==highlight==`. Single `*` NEVER
   auto-formats. Extend the round-trip gate corpus for every new
   construct (marks, escaped/literal delimiters, adjacent-mark cases);
   the gate stays GREEN — if a mark cannot round-trip byte-faithfully,
   STOP and record the finding rather than shipping a lossy dialect
   (the D17 discipline).
2. **Composition mode (dogfood #8), composer-side complete:** the
   `submitOnModEnter` input on `qt-rich-editor` (per §3; Mac
   `metaKey && !ctrlKey`, non-Mac `ctrlKey && !metaKey` — port
   KeyboardPlugin's exact branch), the composer `compositionMode`
   input + toggle button (v4 titles + active state) +
   `compositionModeChange` output, and the
   CompositionModeDefaultSettings card in the Settings Chat tab
   (collapsible "Composition Mode", saves `compositionModeDefault`
   through the EXISTING settings-chat client api). Unit-spec the
   composer + editor keymap directly (zoneless spec gotchas: the
   P4.6ag memory — signal Host, drive `.qt-rich-editor-content`).
   Remove the matching dead-deferral line from `chat-tab.ts:84-87`.
3. **The shared form-field editor** — a `qt-markdown-field` component
   (qt-rich-editor + the ported FormattingToolbar + re-key semantics
   for record changes), adopted in: the memory editor content field
   (the P4.6t deferral), `characters/edit/details-tab` (all 7 markdown
   fields), and `characters/new/new-character` (same fields +
   scenario). Preserve each host's save/emit contract exactly (the
   fields keep emitting the same strings). `roleplayTemplateId`-aware
   toolbar delimiters may defer (enumerate) if the template plumbing
   is not already client-side.

### Tier 2 (should land)

4. **Draft persistence** in the composer (key, debounce, restore-once,
   clear-on-send/blank — v4-exact; no expiry), keyed off the new
   `chatId` input; fully unit-specced with a fake localStorage.
5. **The text-replacement plugin + card:** the ProseMirror analogue of
   TextReplacementPlugin (v4's trigger semantics exactly; rules
   injected via an editor/composer input), the compiled-rules helper,
   and the TextReplacementSettings CRUD card in the Settings Chat tab
   over lane A's pinned verbs (client api file + core-contract §2
   block; in-lane against a mocked CoreClient, live at unification).
   Composer-only, gated by `textReplacementsEnabled`.
6. **Further form-field adoptions** where the v5 component exists:
   the scenario editor (ScenariosManager cards), roleplay-template
   systemPrompt (Settings Templates tab), prompt modals, wardrobe item
   description, Prospero settings instructions, the new-chat scenario
   field, characters view image-prompt/appearance fields.

### Tier 3 (explicit deferrals — enumerate in the status header at close)

- The GFM table transformer (`prosemirror-tables` + the multiline
  round-trip) — port only if tiers 1–2 land early; otherwise defer
  with the v4 survey pointer above.
- Consumers whose v5 host component does not exist yet
  (CreateNPC/ComposeMail/InsertAnnouncement dialogs, etc.) — name
  each.
- `roleplayTemplateId` toolbar awareness if deferred from item 3.
- The composition-mode + draft e2e beats over the live salon binding —
  these are UNIFICATION deliverables (§3), not in-lane guarded beats.

## The differential requirement (restated)

This is an SPA lane: the equivalence proofs are (a) the committed
byte-round-trip gate extended for every dialect change — each new
corpus entry traced to a v4 transformer/flag, per the P4.6ag pattern;
(b) unit specs porting v4's decision logic verbatim (KeyboardPlugin
branches, TextReplacementPlugin trigger table, draft debounce timing);
(c) `ng test` + `ng build` + the full Playwright suite green. No Rust
differentials in this lane.

## Fixtures

None new. The e2e instance's chats suffice for the in-lane editor
beats; salon-binding beats land at unification (§3).

## Shared contract (BINDING — identical in all three orders)

### §1 New dispatch variants (lane A implements in Rust; TS mirrors per §2)

Names and shapes are pinned; no lane renames:

- `textReplacementsList` `{}` →
  `{ rules: TextReplacementRule[], count: number }`
- `textReplacementCreate`
  `{ fromText, toText, caseSensitive?, enabled?, sortOrder? }` → the
  created rule (v4 201-created envelope; duplicate
  fromText+caseSensitive → the v4 conflict arm)
- `textReplacementUpdate` `{ id, ...partial rule input }` → the
  updated rule
- `textReplacementDelete` `{ id }` → v4's delete envelope
- `textReplacementsBulkReplace` `{ rules: input[] }` →
  `{ rules, count }` (transactional full-list replace)
- `chatGetBackground` `{ chatId }` →
  `{ backgroundUrl, fileId, filename, sha256, linkSummary }` (all-null
  when the chat has no `storyBackgroundImageId` or the file row is
  missing — v4 `get.ts:182-193`)

`TextReplacementRule` (camelCase serde, exactly these keys):
`{ id, fromText, toText, caseSensitive, enabled, sortOrder, createdAt,
updatedAt }`.

REST edges (lane A): `GET/POST /api/v1/settings/text-replacements`,
`POST /api/v1/settings/text-replacements?action=bulk-replace`,
`PATCH/DELETE /api/v1/settings/text-replacements/:id`,
`GET /api/v1/chats/:id?action=get-background` — each edge UNWRAPS the
dispatch envelope to v4's raw route body (the P4.6ah lesson).

### §2 core-contract.ts delimited append blocks (lanes B + C)

`apps/web/src/app/core/core-contract.ts` is edited by BOTH SPA lanes
this round, engine.rs-style: each lane appends ONE delimited block at
the END of the file (`// --- P4.6al additions ---` /
`// --- P4.6am additions ---`) and touches nothing else in the file.
Lane B's block: the five text-replacement types. Lane C's block:
`chatGetBackground`. The unifier merges and cross-checks name-for-name
against `types.rs`.

### §3 The composer/salon seam (wired at UNIFICATION, not in-lane)

- `qt-chat-composer` (lane B) gains inputs
  `compositionMode: boolean = false` and `chatId: string | null =
  null` (draft persistence), and output
  `compositionModeChange: boolean` (emitted by the toolbar toggle; the
  composer does NOT persist).
- `qt-rich-editor` (lane B) gains input
  `submitOnModEnter: boolean = false` (Cmd/Ctrl+Enter emits `submit`).
- The salon binding — passing the chat's `documentEditingMode` into
  `[compositionMode]`/`[chatId]` and persisting `compositionModeChange`
  via the existing `chatUpdate` dispatch
  (`{ chat: { documentEditingMode } }`, v4 `useChatControls.ts:348-365`)
  — is a **unification wire**. NEITHER SPA lane adds these bindings to
  `salon-conversation.ts` in-lane (lane C owns the file but leaves this
  seam alone; lane B unit-tests the composer directly). e2e beats that
  need the wire land at unification (the P4.6ac precedent: do NOT ship
  unverified-in-lane guarded beats for this).
- Live-data activation for lane B's text-replacement fetch and lane
  C's background fetch happens at unification (in-lane: mocked
  CoreClient / route mocks; the guarded self-activating idiom only
  where the lane can verify the beat green in-lane).

### §4 File ownership (no other lane touches these)

- **Lane A:** `crates/**` (sole core-dispatch writer this round),
  `harness/oracle/**`, new/changed committed fixtures + generators.
- **Lane B:** `apps/web/src/app/editor/**`, `chat/chat-composer.*`,
  `screens/settings/chat/**`, `memory/memory-editor.*`,
  `screens/characters/edit/details-tab.*`,
  `screens/characters/new/new-character.*`, plus any NEW files it
  creates (the qt-markdown-field component, a settings
  text-replacements client api file, new CSS file(s) + their single
  import line in the styles index, new spec/e2e files). New styles go
  in lane B's NEW css file — `_chat.css` is lane C's.
- **Lane C:** `screens/salon/**`, `chat/message-list.*`,
  `chat/streaming-message.*`, `core/chat-stream.reducer.*`,
  `screens/characters/view/tabs/details-tab.*`,
  `styles/qt-components/_chat.css`, `e2e/global-setup.ts`, its own new
  spec/api files.
- Append-only union-merge for everyone: `docs/CHANGELOG.md`,
  `docs/developer/porting/status-log.md`, core-contract.ts per §2.
- **Version bumps:** lane A bumps quilltap-core / quilltap-harness /
  quilltap-web (+ Cargo.lock); lanes B and C EACH bump `apps/web`'s
  version from the 0.5.83 base (the unifier accumulates).

## Verification gate (run before declaring done)

- `ng test` green (all suites, not just this lane's)
- `ng build` clean
- Full Playwright from `apps/web` with the LOCAL playwright bin
  (port-4319 contention + worktree-binaries notes in the P4.6af
  memory; shared-server specs sort after foundation) — the whole
  suite, not just new specs; the existing m4-salon composer drive
  (`.qt-rich-editor-content`) must stay green in default chat mode
- The extended `markdown-round-trip.spec.ts` gate GREEN
- Every commit follows `.claude/commands/commit.md`.
