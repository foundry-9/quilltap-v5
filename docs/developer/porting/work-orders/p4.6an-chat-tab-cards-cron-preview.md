# Work order P4.6an — the Chat-tab settings cards remainder + the cron next-run preview

> **STATUS: OPEN.**
>
> **This round is ONE lane — no siblings.** The scope ("the
> autonomous-rooms deferred cards": the P4.6ad deferral trail) is a
> single vertical: all eleven remaining v4 Chat-tab cards mount into
> the same `chat-tab.ts`, every one of them writes through the same
> already-ported chat-settings PUT, and the cron preview is a small
> rider inside the shared autonomous room card that the same tab
> renders. Splitting would force shared ownership of `chat-tab.ts`
> and yield no honest parallelism.
>
> **v4 reference baseline: HEAD `02865bdb`**
> (`~/source/quilltap-server`) — run
> `git log 02865bdb..HEAD --oneline` at lane start; **if HEAD moved,
> STOP and report** (classify drift, don't plan around it). You run in
> a worktree; the unification pass integrates the lane onto main.
>
> This order closes the two remaining P4.6ad deferrals (the third —
> the Salon Edit-Enclave entry + include-autonomous toggle — landed in
> P4.6af): the client-side next-run cron preview, and the "13 other v4
> Chat-tab cards" of which **eleven remain** (Composition Mode, Text
> Replacement, and Data Retention have since landed via P4.6al and
> P4.d3). Fresh v4 survey before each unit; the facts below are from a
> 2026-07-14 survey at `02865bdb`.

## Mandate

Finish fitting out the Settings → Chat tab: port the eleven remaining
v4 cards — Composer, Auto-Scroll, Token Display, Context Compression,
Memory Cascade, Image Description, Automation, Agent Mode,
Thinking / Reasoning, Answer Confirmation, and Dangerous Content — as
standalone Angular card components over the existing
`chatSettings`/`chatSettingsUpdate` dispatch, mounted in v4's exact
card order with v4's exact titles/descriptions/sectionIds, and retire
the "not yet fitted out" placeholder. Then close the P4.6ad cron
deferral: replace the shape-only cron validation in the shared
autonomous room card with v4's live next-run preview (the `croner`
npm package, v4's own dependency at v4's version range).

This is expected to be an **SPA-only lane**. The server already
Zod-parses every key these cards write (verified — see the survey);
no new dispatch variants, no crate changes, no new committed fixtures.
If a genuine server gap surfaces anyway, it ships with its
differential per the P4.6d settings recipe (below) — never silently.

Do NOT touch `crates/**` unless that gap materializes; do not touch
the Salon message surfaces (`apps/web/src/app/chat/**` render
pipeline) except for the one-attribute spellcheck rider named in
tier 2; do not implement the Salon token/cost *display* (a Salon
slice, named in tier 3); do not edit `enclave/**` or any Rust cron
code — the preview is client-side, exactly as in v4.

## Survey-verified starting points (2026-07-14 @ `02865bdb`, do not re-derive)

### The v4 tab and its card order

- **`components/settings/tabs/ChatTabContent.tsx` lines 70–197** — the
  authoritative 16-card order. Ported already: `composition-mode`,
  `text-replacements`, `data-retention`, `autonomous-rooms`,
  `autonomous-room-schedules`. **The eleven to port, in v4 position**
  (title / description / sectionId):
  1. **Composer** / "Composer behavior and input aids" /
     `composer-spellcheck` (line 78)
  2. **Auto-Scroll** / "Whether the Salon follows new messages to the
     bottom" / `auto-scroll` (line 86)
  3. **Token Display** / "Configure token count and cost display" /
     `token-display` (line 102)
  4. **Context Compression** / "Configure how older messages are
     compressed" / `context-compression` (line 110)
  5. **Memory Cascade** / "Control how memories behave when messages
     change" / `memory-cascade` (line 118)
  6. **Image Description** / "Configure image description generation" /
     `image-description` (line 126)
  7. **Automation** / "Configure automatic detection features" /
     `automation` (line 137)
  8. **Agent Mode** / "Configure iterative tool use with
     self-correction" / `agent-mode` (line 145)
  9. **Thinking / Reasoning** / "Show reasoning models'
     chain-of-thought in chat (display only)" / `thinking-display`
     (line 154)
  10. **Answer Confirmation** / "Vet looked-up answers against what the
      character actually knew this turn" / `answer-confirmation`
      (line 162)
  11. **Dangerous Content** / "Configure content detection, routing,
      and display behavior" / `dangerous-content` (line 170)

  Note v4's interleaving: Composer and Auto-Scroll sit BETWEEN
  Composition Mode and Text Replacement; the nine others sit between
  Text Replacement and Data Retention. The v5 tab must end up in v4's
  full 16-card order, not with the new cards appended.

- **The v4 card components** (`components/settings/chat-settings/`),
  with sizes as a scope gauge: `ComposerSpellcheckSettings.tsx` (42),
  `AutoScrollSettings.tsx` (44), `TokenDisplaySettings.tsx` (60),
  `ContextCompressionSettings.tsx` (271), `MemoryCascadeSettings.tsx`
  (88), `ImageDescriptionSettings.tsx` (99), `AutomationSettings.tsx`
  (55), `AgentModeSettings.tsx` (97), `ThinkingDisplaySettings.tsx`
  (65), `AnswerConfirmationSettings.tsx` (55),
  `DangerousContentSettings.tsx` (359). **None makes a direct
  `fetch`** — every card is pure form controls over props + a handler.

### The save path (v4) and its v5 equivalent

- v4 handlers live in
  `components/settings/chat-settings/hooks/useChatSettings.ts`
  (1002 lines; `ChatSettingsProvider.tsx` is a thin context over it).
  Every handler `PUT /api/v1/settings/chat` with a **partial body**.
  **Nested bags are spread-merged CLIENT-side before sending** — the
  PUT carries the whole updated bag, not a partial nested patch:
  `cheapLLMSettings` (line 234:
  `{ ...settings.cheapLLMSettings, ...updates }`),
  `memoryCascadePreferences` (~325), `tokenDisplaySettings` (~362),
  `contextCompressionSettings` (~399). Scalars go alone
  (`{ autoDetectRng: value }`, `{ composerSpellcheck: value }`, …).
  Reproduce the merge semantics exactly; component specs must assert
  the exact PUT payload per interaction.
- Agent Mode uses TWO handlers (`handleAgentModeDefaultEnabledChange`,
  `handleAgentModeMaxTurnsChange`) writing into `agentModeSettings`;
  Image Description writes two nullable-string scalars
  (`imageDescriptionProfileId`, `uncensoredImageDescriptionProfileId`);
  Dangerous Content drives BOTH `dangerousContentSettings` AND
  `cheapLLMSettings` (it hosts the cheap-LLM provider picker).
- **v5 recipe:** `apps/web/src/app/screens/settings/chat/
  composition-mode-settings.ts` — a standalone component injecting
  `CoreClient`, loading via the `chatSettings` dispatch, saving via
  `chatSettingsUpdate` (a partial merge at the server edge), with
  `loading`/`saving`/`error` signals. `data-retention-settings.ts` and
  `text-replacement-settings.ts` are further examples, including the
  dogfood-#6 error-surfacing rule (a visible `qt-alert-error`, never a
  silent revert).

### Server coverage (verified — no port work expected)

`crates/quilltap-core/src/api/settings.rs` already Zod-parses every
key the eleven cards write: `cheapLLMSettings` (449–463),
`imageDescriptionProfileId` (465–468),
`uncensoredImageDescriptionProfileId` (471–474),
`tokenDisplaySettings` (507), `memoryCascadePreferences` (513–530),
`autoDetectRng` (540–543), `agentModeSettings` (546–548),
`contextCompressionSettings` (558–574), `dangerousContentSettings`
(581–583), `composerSpellcheck` (599–602),
`autoScrollOnResponseComplete` (611–616), `thinkingDisplay` (626–628),
`answerConfirmationSettings` (632–634). The surface is
differential-proven (`settings_routes_equivalence`,
`chat_settings_tier2_equivalence`). Re-verify the specific sub-fields
each card writes round-trip through the parse at lane start (grep, or
a quick live PUT against the dev server) BEFORE building the card.

### The v5 tab file

`apps/web/src/app/screens/settings/chat/chat-tab.ts` — five cards
mounted; the loud-deferral placeholder (lines ~107–121) enumerates
exactly these eleven. When all eleven land, **remove the placeholder
block entirely** and update the header comment. Deep links ride
`?tab=chat&section=<sectionId>` via the existing `forceOpen` binding.

### Selects with async-loaded options — BINDING standing rule

Image Description (two image-profile selects) and Dangerous Content
(the cheap-LLM connection-profile select) render `<select>`s whose
options load async. Per the dogfood-#6 audit rule: bind **`[selected]`
per option**, never `[value]` on the select, and add the regression
spec that sets options/id AFTER first render and asserts the stored
value displays. Both data sources already exist in the SPA contract:
`connectionProfiles` (`core-contract.ts:2338`) and `imageProfileList`
(`core-contract.ts:2908`).

### The cron next-run preview

- **v4:** `components/new-chat/AutonomousRoomCard.tsx` — line 4
  `import { Cron } from 'croner'`; `tryCronNextRun` (~lines 30–50)
  returns `{ ok, next?, error? }` (catch → `'invalid cron'` fallback);
  UI at lines 77–107 renders `Next run: {next.toLocaleString()}`, a
  no-next-run arm, or `Invalid cron: {error}`. Dependency:
  `croner: ^10.0.1` (package.json line 85).
- **v5 insertion point:** `apps/web/src/app/autonomous/
  autonomous-room-card.ts` — the loud deferral is the header comment
  at lines 17–20 and the shape-only five-field validation below it
  (~line 49). Replace with the live preview: add `croner@^10.0.1`
  (v4's exact range) to `apps/web/package.json`, port
  `tryCronNextRun` verbatim, keep v4's exact strings. Browser croner
  over the local `Date` matches v4's client semantics by
  construction — the *server-side* hand-rolled `enclave::cron` (see
  the [u4-enclave-cron-lifecycle] memory) is NOT involved and must
  not be touched. The preview lands everywhere the shared card
  renders (Settings defaults, EditEnclaveModal, New-Chat) for free.

### Consumers of these settings (what's already wired, what isn't)

- **Already consumed in v5** (the card is the only missing piece):
  `autoScrollOnResponseComplete`
  (`apps/web/src/app/chat/message-list.ts:211` + `auto-scroll.ts`),
  `thinkingDisplay.defaultCollapsed`
  (`apps/web/src/app/chat/message-row.ts:329`). All the engine-side
  settings (context compression, memory cascade, agent mode, dangerous
  content, cheap LLM, answer confirmation, autoDetectRng, image
  description profiles) are consumed by the Phase-3-ported engine.
- **Composer spellcheck:** v4 consumes it at
  `components/chat/lexical/LexicalComposerWrapper.tsx:107`
  (`spellCheck` on the editor, default **true**). The v5 composer is
  the ProseMirror `qt-rich-editor`; the tier-2 rider binds the
  `spellcheck` attribute on its contenteditable to the setting with
  v4's default.
- **Token/cost display:** v4 consumes `tokenDisplaySettings` in the
  Salon message surfaces (`app/salon/[id]/components/MessageRow.tsx`,
  `MessageActionBar.tsx`, …). The v5 Salon does not yet render token
  counts/costs at all — that rendering is a **Salon slice, not a
  settings card**, and stays a tier-3 named deferral here. The card
  still ports faithfully (the setting is stored and the note copy
  carries over).

## Deliverables

**Tier 1 (must land):**

1. The eleven card components under
   `apps/web/src/app/screens/settings/chat/` (or `autonomous/` where a
   card is autonomous-shared — none is expected to be), each a
   v4-faithful port: same controls, same labels/descriptions/notes
   (v4 voice, verbatim), same defaults-when-unset
   (`DEFAULT_TOKEN_DISPLAY_SETTINGS` etc. from
   `components/settings/chat-settings/types.ts`), same PUT payloads
   including the client-side spread-merge for nested bags. Each with a
   component spec asserting (a) render from a canned settings row,
   (b) the exact `chatSettingsUpdate` payload per interaction,
   (c) error surfacing on a failed save (dogfood-#6 rule), and — for
   the two async-options selects — (d) the late-options regression.
2. `chat-tab.ts` re-ordered to v4's full 16-card order, sectionId deep
   links working for all new cards, the placeholder block removed, the
   header comment updated.
3. The cron next-run preview in `autonomous-room-card.ts` per the
   survey above, with specs: valid cron → "Next run:" text; invalid →
   v4's error string; blank → no preview; and the deferral comment
   removed.

**Tier 2 (should land):**

4. The composer spellcheck rider: the `qt-rich-editor` contenteditable
   binds `spellcheck` to `composerSpellcheck ?? true`, spec included.
5. e2e beats (extend `settings-flow.spec.ts` or a new
   `settings-chat-cards-flow.spec.ts`, sorted after foundation per the
   shared-server rule): toggle Auto-Scroll → reload → persisted;
   change the Dangerous Content mode → reload → persisted; the cron
   preview beat (type a cron in the Settings defaults card → the
   "Next run:" line appears; type garbage → the invalid arm). Follow
   the e2e gotchas bank: port-4319 contention across worktrees, run
   the LOCAL playwright bin from `apps/web`, a worktree needs its own
   Rust binaries, virtualized count baselines must settle.

**Tier 3 (explicit deferrals — loud, named, NOT this lane):**

- The Salon token/cost display rendering (consumes
  `tokenDisplaySettings`; a Salon slice with its own order).
- The story-background generation subsystem and every other standing
  deferral already named in the status log — unchanged by this lane.
- Anything requiring a new dispatch variant that turns out to be
  load-bearing for a card (STOP and report instead of improvising).

## The differential requirement, restated

This lane is SPA-only, over a server surface that is already
differential-proven (`settings_routes_equivalence` — 
`settingsChat` GET/PUT — and `chat_settings_tier2_equivalence`). The
SPA-side proof is the P4.6e/P4.6r pattern: component specs asserting
exact request payloads + live e2e beats against the real server. If a
server gap DOES surface (a sub-field the Zod parse drops), the fix
ships with the extended `settings_routes_equivalence` case regenerated
from a fresh oracle per the P4.6d recipe (jest oracle → NDJSON → Rust
diff; see the [jest-oracle-mechanism] and
[oracle-regen-from-worktree-path] memory notes — regenerate from the
WORKTREE case file and verify a new row id lands in the NDJSON).

## Fixtures

None new. The e2e instance's existing `chat_settings` row is the
substrate; beats mutate settings through the UI and assert
persistence across reload (no fixture regeneration, no dependent
oracles). Do not add committed fixtures for this lane.

## Ownership + version bumps (single lane)

- **Owns:** `apps/web/src/app/screens/settings/chat/**`,
  `apps/web/src/app/autonomous/autonomous-room-card.ts` (+ its spec),
  the one-attribute spellcheck binding in the `qt-rich-editor`
  composer integration, `apps/web/package.json` + lockfile (the
  `croner` dep), `apps/web/e2e/**` additions.
- **Version bumps:** the SPA version only (0.5.93 → next), one bump
  per commit per the repo convention. No crate bumps expected; if the
  server-gap contingency fires, bump core + harness as usual.
- `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` are
  append-only.

## Verification gate (run before declaring done)

1. `cargo fmt --all --check`; `cargo clippy` both feature sets
   (`-D warnings`); `cargo test --workspace` — expected unchanged
   (proves the lane didn't disturb the workspace), plus the named
   settings differentials if the contingency fired.
2. `ng test` (all specs, including the new card specs) and `ng build`
   clean, from `apps/web`.
3. Full Playwright suite from `apps/web` (local bin), all beats
   ACTIVE — no new skips, the new beats green in isolation AND
   in-suite.
4. Every commit follows `.claude/commands/commit.md`.
