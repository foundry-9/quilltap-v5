# Work order P4.D161 — the Salon says so (v4 bug 123, client half, `fef7ce4f7` → `f699da6f6`): the Skip banner keyed on the seat the composer speaks as, the pause-you-did-not-cause toast, the overlay-aware Skip, and the measured no-counterpart for v4's pause-sync drift

**Status:** OPEN (ordered 2026-09-06, the `f699da6f6` 4.9.x drift catch-up
round (first drafted against `ba34fa367`; repointed the same day when the
4.9.2 squash landed on `main`): P4.D160 ∥ P4.D161 ∥ P4.D162 ∥ P4.78 ∥ P4.79).

## Preamble

- **v4 oracle baseline: `c2232cd9a`** (drift-ledger §1). **This lane's target
  commit is `fef7ce4f7`** (bug 123; on `main` via the 4.9.1 squash; the round's pin is
  `f699da6f6`, §B — the client files are byte-identical between the two). Read the hunks: `git -C ~/source/quilltap-server show fef7ce4f7 --
  app/`, the three new client tests (`__tests__/unit/app/chats/[id]/hooks/
  useTurnManagement.test.ts` +142, `useChatControls.pause-sync.test.tsx`
  +118), and `bugs/fixed/bug-123-silent-pause-sync-drift.md`.
- **Freshness probe at lane start** (ledger §2): checkout on `bugfix`, tree
  clean, `git log f699da6f6..main` empty, `git log 1a2b2164c..bugfix` empty.
  **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED** (ledger §1; §B). This lane runs NO Rust
  oracle; its v4-client oracle is the pinned worktree's source at
  `f699da6f6` (transcribe from `git show f699da6f6:<path>`, never from the
  checkout, which sits on `bugfix`). If a jest client suite is run as a parity
  witness, run it from `/tmp/qt-v4-pin-p4d161-f699da6f6`.
- **Sibling lanes:** P4.D160 (the server half — the §G frame contract binds
  the two; this lane may land and gate BEFORE D160 because the client reads
  an OPTIONAL key), P4.D162, P4.78, P4.79. §A–§H + Ownership binding and
  identical across all five.
- **Worktree:** per `carryout` (branch `claude/p4-d161-…`); `npm ci` in the
  worktree (`spa-worktree-node-modules`); `npm run build` is the only real
  type gate (`spa-tsc-does-not-typecheck-app-sources`); **port 4319 is
  yours** (§E) — one Playwright at a time.

## Mandate

Port the client half of `fef7ce4f7` whole into the Angular Salon: (E) the Skip
banner is offered whenever the composer will take words as a user-driven
seat — owned or impersonated — not only when the rotation has formally landed
on it, gated on the composer being writable and on active characters, worded
for whose turn it is, and Skip honours the impersonation overlay, lifts a
pause first (like a nudge), and withholds the auto-continue when the next
speaker is a seat the human impersonates; (D) a chain that stops with
`paused: true` is announced with v4's two toast sentences unless the user
paused it themselves or the room is all-LLM (which keeps its modal); (C) v4's
`useChatControls` pause-sync drift — MEASURE first, then port or record a
NO-COUNTERPART with the measurement (the ledger's own instruction). Parity
specs for every branch; the two live beats. Must NOT touch: any crate, the
server's frame shape (§G is read-only here), the sidebar Pause button's home
(P4.9H1), or the turn-controls' paused NOTICE copy (dogfood #83's ruling).

## Survey-verified starting points (2026-09-06, from the shipped hunks — ledger §5.3)

1. **v4 (E) — `app/salon/[id]/SalonView.tsx`:** `:531-565` the `speakingAsSeat`
   memo is split: a new `speakingSeat` (resolved via `findActiveUserParticipant(
   participantsAsBase, activeTypingParticipantId, impersonatingParticipantIds)`,
   `seatId = resolved?.id ?? activeTypingParticipantId`, looked up in
   `participantData`, `?? null`) and the avatar-hydrated `speakingAsSeat`
   derived from it. `:1460-1515` the banner: `null` when `streaming ||
   waitingForResponse || sending`; `null` when `!hasActiveCharacters`; `null`
   when no seat or `!isUserDrivenSeat({id, controlledBy: seat.controlledBy ??
   'llm'}, impersonatingParticipantIds)`; `isSeatsTurn =
   turnSelectionResult?.nextSpeakerId === seat.id`; must-speak via
   `computeSkipEligibility` over the SEAT (not the next speaker); wording
   three-way — must-speak `Everyone else has passed — it falls to ${name} to
   say something.` / `isSeatsTurn` → `${name}'s turn — type as them, or skip
   to let someone else respond.` / else `Speaking as ${name} — type, or skip
   to let someone else take the floor.`; Skip → `handleSkipUserTurn(seat.id)`.
   `:706` `useTurnManagement` gains the `impersonation.impersonatingParticipantIds`
   argument.
2. **v4 (E) — `hooks/useTurnManagement.ts`:** `:115-119` the new
   `impersonatingParticipantIds: readonly string[] = []` parameter;
   `:210-229` `handleSkipUserTurn`: guard `!participant || !isUserDrivenSeat(
   participant, impersonatingParticipantIds)` → `showErrorToast('Only a
   character you are speaking as can be skipped.')` (the old sentence `Only
   user-controlled characters can be skipped.` is GONE); then `if (isPaused &&
   onUnpause) await onUnpause()` with v4's comment ("like a nudge, it lifts a
   pause first, or the next speaker it hands the floor to would be refused by
   the pause guard"); `:239` the auto-continue `nextSpeakerId &&
   nextSpeakerControlledBy !== 'user' && !impersonatingParticipantIds.includes(
   nextSpeakerId)`. ⚠ Read what v4's `onUnpause` (`SalonView` passes
   `unpauseChat` from `useChatControls`) DOES — whether it toasts — before
   choosing v5's twin; v5's `onSidebarNudge` (`salon-conversation.ts:2478`)
   lifts a pause through `onTogglePause()`, which toasts `Auto-responses
   resumed`. Match v4's unpause exactly, not the nudge's convenience.
3. **v4 (D) — `hooks/useSSEStreaming.ts`:** `:98` the `SSEEvent` type gains
   `paused?: boolean`; `:369-393` `isPausedRef` (a mirror of `isPaused` so a
   closed-over callback can ask "is this pause NEWS?") and
   `announceChainPause({reason, paused})`: return on `!paused`; return when
   `isPausedRef.current` (the user paused it themselves — they already got
   the toggle's toast); return when `isAllLLMChat(participantsAsBase)` (the
   modal explains); `reason === 'error'` → `showWarningToast("A character's
   turn failed, so auto-responses are paused. Press Resume in the sidebar to
   carry on.")` else `showInfoToast('Auto-responses are paused. Press Resume
   in the sidebar to let the others answer.')`; `:690` `paused: data.paused
   === true` read off the frame; `:956` and `:1132` `announceChainPause(event)`
   called AFTER `await fetchChat()` at BOTH chain-complete sites (send and
   continue-mode). Note the ordering subtlety: the ref still holds the
   PRE-fetch knowledge when the callback runs — "news" is judged against what
   the client believed before the reconcile.
4. **v4 (C) — `hooks/useChatControls.ts:159-174`:** the sync effect re-keyed
   `[chat?.isPaused]` → `[chat]`; `:220-227` `setPauseState` also
   `setChat(prev => ({...prev, isPaused: paused}))`. **v5 measurement so far
   (2026-09-06):** `salon-conversation.ts` holds NO local pause flag — the
   templates read `c.isPaused` (`:353`) and `chat()!.isPaused` (`:449`) off
   the fetched resource; `onTogglePause` (`:2751-2760`) dispatches
   `chatUpdate` + toasts + invalidates `chatKeys.detail`; a grep for
   `userStopped|pausedLocal|isPausedLocal` finds nothing. So the drift v4
   fixed appears to have no v5 counterpart. **Finish the measurement** (any
   other signal derived from `isPaused` and latched? the `allLLMPauseActive`
   computed `:1408`? the sidebar's `[isPaused]` binding?) and record the
   NO-COUNTERPART with the grep lines, per the ledger row's explicit
   instruction; do NOT transcribe a fix for a bug v5 does not have.
5. **v5 today:** `apps/web/src/app/screens/salon/salon-conversation.ts` —
   `speakingAsSeat` `:1998-2011` (resolved exactly v4's way; extend to a
   `speakingSeat` the cue derives from, as v4 restructured); `nextSpeaker`
   `:1990` over `effectiveNextSpeakerId` `:1886` (the Bug-48 override wins);
   `userTurnName` `:2024-2033` and `mustSpeak` `:2036-2070` both keyed on
   `nextSpeaker()` — re-key both on the SEAT, add the `isSeatsTurn` fact;
   `onSkipUserTurn` `:2121-2146` (target `nextSpeaker()`, no overlay guard,
   no unpause, auto-continue `turn.nextSpeakerControlledBy !== 'user'` only);
   `isAllLLM` `:1398`; `hasActiveCharacters` `:1834`; `busy()`;
   `impersonatingIds` `:2601`; `isUserDrivenSeat` already imported. The
   presentational `chat/turn-controls.ts`: `userTurnName` input + `mustSpeak`
   + `bannerText` `:114-119` (two sentences) — it gains the seat's-turn fact
   (an `isSeatsTurn` input, or a small seat object) and the third sentence;
   the Skip emits `skipUserTurn` (void) and the parent resolves the seat.
   `core/core-contract.ts:4106-4111` `ChatStreamFrame` (`chainComplete?`,
   `reason?`, `nextSpeakerId?`, `chainDepth?`) gains `paused?: boolean` per
   §G. `core/chat-stream.reducer.ts:287-299` the chainComplete arm drops
   `reason`/`paused` today — the pure reducer must CARRY them (e.g.
   `chainReason`, `chainPaused` on `ChatStreamState`) so the vertical can
   read them after the fold. `runTurn` `:2790-2900`: v5's ONE reconcile point
   is the `invalidateQueries(chatKeys.detail)` at `:2877` (v4's two
   `fetchChat()` sites collapse onto it — the comment there says so for
   `notifyQueueChange`); the announcer runs right after it, judged against a
   snapshot of `chat()?.isPaused` taken BEFORE the invalidate (v4's
   `isPausedRef` semantics, point 3). Toasts: `ui/toast.service.ts`
   `showWarning` `:83` / `showInfo` `:88` (v4's `showWarningToast` /
   `showInfoToast` twins).
6. **Specs today:** `screens/salon/salon-turn-controls.spec.ts` (7 specs;
   `:251` pins the "'s turn" sentence + the Skip dispatch; `:341` the
   refusal line — which becomes v4's NEW sentence); `core/chat-stream.
   reducer.spec.ts` (if present — else the reducer's spec home). v4's parity
   witnesses: `useTurnManagement.test.ts` (the overlay guard, the unpause-
   first order, the withheld auto-continue) and — for the NO-COUNTERPART —
   `useChatControls.pause-sync.test.tsx` (record why each of its cases has
   no v5 analogue). Angular traps to read first: `angular-class-binding-
   dedups-and-reorders`, `e2e-assertion-can-read-the-pre-click-state`, the
   controlled-select `afterRenderEffect` idiom is NOT needed here.
7. **Beats today:** `e2e/m4b-salon.spec.ts:74-89` (the guarded Skip banner →
   Host turn-pass chip), `e2e/salon-dialogs-flow.spec.ts:348-375` (the
   impersonation overlay round trip; asserts the `Speaking as ` aria cue).
   The toast beat's gesture must make the pause NEWS to the client: a chat
   loaded UNPAUSED, then paused server-side by a route the realtime hints do
   not announce, then a send — or the frame injected at the wire through the
   app's real `EventSource` handler (`e2e-inject-wire-bytes-via-eventsource`;
   the P4.66 idiom) — measure which gesture is deterministic before writing
   it, and remember `toHaveCount` resolves on the first matching poll.

## Tiered deliverables

**Tier 1 (must land):**
1. `speakingSeat` + the banner re-key (point 1/5): the three gates, the
   `isSeatsTurn` fact, the three sentences byte-exact, must-speak over the
   seat, Skip on the seat.
2. `onSkipUserTurn` per point 2: the overlay guard + v4's new refusal
   sentence (the old one deleted), unpause-first in v4's exact shape, the
   auto-continue withheld for an impersonated next speaker.
3. The frame carry (§G): `ChatStreamFrame.paused?`, the reducer carrying
   `reason`/`paused` through to the terminal state, the announcer with v4's
   four gates and two sentences, run after the reconcile against the
   pre-reconcile snapshot.
4. The (C) measurement finished and recorded (NO-COUNTERPART with evidence,
   or the port if a latch is found).
5. Parity specs: banner — seat's turn / speaking-as (not its turn) / hidden
   while sending / hidden with no active characters / hidden for a non-user-
   driven seat / must-speak over the seat; skip — refusal sentence, unpause
   dispatched BEFORE `skipUserTurn`, auto-continue withheld for an
   impersonated next speaker, still fired for an LLM one; reducer — `paused`
   carried; announcer — error→warning, other→info, absent/false→none,
   all-LLM→none, already-paused→none. Each mutation-proven (one spec per
   branch reddens).
6. The two live beats: the "Speaking as" banner on an impersonated seat that
   is NOT next (extend `salon-dialogs-flow`'s overlay beat or `m4b-salon`),
   and the pause toast (point 7). Both run green in the FULL suite, not only
   in isolation (`wardrobe-e2e-beats-are-serial-and-stateful`).

**Tier 2 (should land):**
7. The `salon-dialogs-flow` overlay beat asserting Skip's refusal sentence
   for a seat the human does NOT speak as.

**Tier 3 (explicit deferrals, loud):** none expected. v4's `useChatControls.
pause-sync.test.tsx` is expected to land as a recorded NO-COUNTERPART, not a
spec.

## The differential requirement (restated)

This is a client vertical: the oracle is v4's client source at `f699da6f6`
(sentences byte-copied from the hunks, gates transcribed branch for branch)
and v4's two new jest suites mirrored as vitest parity specs 1:1 where a
counterpart exists. Every branch has a spec that reddens under the obvious
mutation (drop a gate, swap a sentence, reorder unpause after skip); the
beats are the live wire proof. No Rust oracle is regenerated by this lane.

## Fixtures

- Consumes: the salon e2e fixture (unchanged); seeds through the API/UI, not
  SQL, for anything overlay-backed.
- Delivers: none.

## Shared contracts (BINDING — identical text in all five orders)

**§A Family ownership for this round's regens.** Each family below is
regenerated and edited by ONE lane. There is NO bulk neutrality sweep this
round (two substantive v4 commits, both fully delineated in the ledger); the
unifier re-runs the union of these families by name at the gate.

- **P4.D160:** `orchestrator_tier3_equivalence` (+ `harness/oracle/cases/
  orchestrator-tier3.test.ts`, `harness/oracle/fixtures/orchestrator-tier3.json`,
  `harness/oracle/fixtures/build-orchestrator-fixture.ts`, the `QT_FIXTURE_ORCH_*`
  pair), `help_tree_equivalence` (+ `harness/oracle/cases/help-tree-sync.test.ts`),
  and the `chat_events` frame pins inside `quilltap-core`.
- **P4.D161:** the SPA unit specs under `apps/web/src/app/**` and the whole
  Playwright suite (§E). No Rust family.
- **P4.D162:** `help_chat_orchestrator_tier3_equivalence` (+ `harness/oracle/
  cases/help-chat-orchestrator-tier3.test.ts`, `harness/oracle/fixtures/
  help-chat-orchestrator-tier3.json`), `request_builder_google_equivalence` (+
  its oracle case / recorded corpus rows), `request_builder_google_wire_
  equivalence` (regen-only; expected byte-identical; the recorder is run ONLY
  to APPEND rows — never re-record existing ones), and — only if the lane
  rebuilds the `help-chat-{main,mount}.db` pair — its consumers
  `help_chats_routes_equivalence`, `help_docs_routes_equivalence`, and the
  `quilltap-web` help tests, re-run by name.
- **P4.78:** `chat_create_capstone_equivalence` (+ `harness/oracle/cases/
  chat-create-capstone.test.ts`, `harness/oracle/fixtures/chat-create-capstone.json`,
  `harness/oracle/fixtures/build-chat-create-capstone.ts`, the `QT_FIXTURE_CC_*`
  trio).
- **P4.79:** `brahma_console_tier3_equivalence`, `brahma_orchestrator_tier3_
  equivalence` (+ their oracle cases / fixtures), `brahma_console_routes_
  equivalence` (consumer; regen-only).

**§B Pins.** The round's target baseline is **`f699da6f6`** (v4 `main` HEAD
at the second 2026-09-06 drift check — "docs: CHANGELOG update for 4.10";
it sits atop the 4.9.2 release squash `8fbf2afe0`, which carries
`20913d2aa`'s bug-124/125 content onto `main` byte-for-byte — measured:
`git diff 20913d2aa main` over `lib/ app/ packages/ plugins/ help/
jest.config.ts __mocks__/ __tests__/` is EMPTY). **PIN REQUIRED for every
lane** (ledger §1: the checkout sits on `bugfix` AND both branches are past
the baseline). Every lane regenerates from a LANE-UNIQUE detached worktree
(`/tmp/qt-v4-pin-<order>-f699da6f6`, the three symlink classes, ledger
§5.1) — **one pin sha for the whole round, no exceptions** (the earlier
draft's P4.D162 second pin at `20913d2aa` is RETIRED: that content is now on
`main`). Verify each pin by a marker only one tree has: a `f699da6f6`
orchestrator oracle carries `"paused":true` (absent at `c2232cd9a`); a
`f699da6f6` help-tree oracle carries `When a Turn Fails`; a `f699da6f6`
help-chat oracle carries a `toolCallId` inside a recorded slate (absent at
`c2232cd9a` and at `ba34fa367`). ⚠ Ledger §1 hazard 5: from `8fbf2afe0` on,
v4's `jest.config.ts` maps `^@google/genai$` to a manual mock — every lane
whose JEST-run oracle loads the google plugin (through `provider-registry`
or directly) must confirm its regens still match at the pin and say so in
its record; `tsx`/`node` recorders are unaffected. v4 is the human's active
repo: never switch or stash it; re-run the §2 probe before each regen batch.

**§C Sweep serialization.** No `recipe_sweep.py --run-all` this round. Lanes
run per-family `--run <family> --v4 <pin>` only, never against a sibling's §A
families or its `/tmp` staging, and one sweep invocation at a time repo-wide.
Recipes that hard-code a shared `/tmp` staging path (`/tmp/help-orch`,
`/tmp/qt-cc-*`, `/tmp/qt-help-tree-oracle`) are used under LANE-UNIQUE copies
(`/tmp/qt-d160-*`, `/tmp/qt-d162-*`, `/tmp/qt-p478-*`, `/tmp/qt-p479-*`) — the
memory notes `tmp-fixtures-collide-across-parallel-lanes` and
`tmp-fixture-clobber-by-a-sibling-lane`.

**§D Source-tree seams (who edits what shared file).**

- `crates/quilltap-core/src/services/chat_events.rs` (the `ChainCompletePayload`
  `paused` field) and `services/orchestrator.rs`: **only P4.D160**.
- `crates/quilltap-core/src/services/help_chat/**` and
  `model/request_builder/google.rs`: **only P4.D162**.
- `crates/quilltap-core/src/services/tool_call_threading.rs`: **NOBODY** — a
  read-only primitive this round; a lane that believes it needs a change
  STOPs and reports (P4.D162 CALLS it).
- `crates/quilltap-core/src/services/chat_create.rs` + `api/chat_create.rs`:
  **only P4.78**.
- `crates/quilltap-core/src/services/brahma_console/**`: **only P4.79**.
- `crates/quilltap-core/src/services/primary_stream.rs` (`StreamLogCtx`,
  `log_chat_message_call`): **NOBODY** edits; P4.79 CALLS it.
- `apps/web/**`: **only P4.D161**.
- `help/**` (the repo-root vendored tree, embedded by `quilltap-host`'s
  `build.rs`) and `docs/v4/**`: **only P4.D160**.
- `crates/quilltap-web/tests/fixtures/help-chat-*.db` (+ `.meta.json`):
  **only P4.D162**.
- `crates/quilltap-core/src/api/types.rs`, `lib.rs`, `services/mod.rs`,
  `crates/quilltap-web/src/**`, `crates/quilltap-host/src/**`,
  `crates/quilltap-tauri/**`, `crates/quilltap-cli/**`: **NOBODY**. (The
  `chainComplete` frame is a `serde_json::Value` pass-through on every
  transport — measured 2026-09-06: no `chainComplete` schema pin exists in
  `quilltap-web` or `quilltap-tauri`; a lane that finds one STOPs.)

**§E Playwright.** Port 4319 is P4.D161's for the round; it runs the suite at
its close. Every other lane authors NO Playwright and runs none; the unifier
runs the full suite at the gate. (`e2e-port-4319-is-repo-wide`.)

**§F The drift ledger.** Lanes never write `drift-ledger.md`. At ordering the
§3 rows were marked: `fef7ce4f7` → `ORDERED(p4.d160, p4.d161)`; `20913d2aa` →
`ORDERED(p4.d162)`; the six carrier/docs rows `d40497411`, `5eaf98cf1`,
`ba34fa367`, `02b77ab0f`, `8fbf2afe0`, `d489b04a3`, `f699da6f6`, `1a2b2164c`
→ `ORDERED(p4.d160)` (the docs-mirror follow-through + NO-PORT ratification
evidence live in P4.D160's lane record). `15573c3a1` (bug 119) stays
UNPROCESSED by the ledger's own instruction — it belongs to `p4.9k`.
`/unify` moves the baseline to `f699da6f6`; every row above is then an
ancestor of `main` by content (`20913d2aa` through the `8fbf2afe0` squash)
and is marked ABSORBED / NO-PORT-RATIFIED in the ordinary way. The `bugfix`
branch's own bookkeeping commits (`d40497411`, `1a2b2164c`) are ratified by
their file lists; they are not ancestors of `main` and never will be.

**§G The `chainComplete` frame contract (P4.D160 ↔ P4.D161), binding.** The
frame is `{ "chainComplete": true, "reason": <string>, "nextSpeakerId":
<string|null>, "chainDepth": <int>, "paused"?: <bool> }`. The key is spelled
`paused`, a JSON boolean, and is **present on exactly the four
`execute_turn_chain` emits** — the new paused early-return (`reason:
"paused"`, `paused: true`, `chainDepth: 0`, `nextSpeakerId: null`), the
mid-chain decision stop (`paused: decision.reason == "paused"`), the
empty-response stop (`reason: "error"`, `paused: false`), the chain-error
safety stop (`reason: "error"`, `paused: true`) — and **ABSENT** on the
fair-rotation `user_turn` emit (`orchestrator.rs` ≈`:905`, v4
`orchestrator.service.ts:1892`) and on the help-chat orchestrator's
`chainComplete` (v4 `help-chat/orchestrator.service.ts:156`), which v4 did not
touch. The server side therefore carries it as `Option<bool>` with
`skip_serializing_if = "Option::is_none"`; the client reads
`data.paused === true` and treats absent/false alike. Neither lane invents a
second key.

**§H Versions.** Each lane bumps only the crates / the SPA it touches, per
`.claude/commands/commit.md` §6; the unifier recounts as base + total bumps
(the silent auto-merge trap). Base at ordering: core 0.0.806, harness 0.0.695,
web 0.0.119, host 0.0.104, cli 0.0.18, tauri 0.0.7, SPA 0.5.651. Expected
bumpers: P4.D160 core + harness + host (the embedded help tree changes);
P4.D161 SPA only; P4.D162 core + harness; P4.78 core + harness; P4.79 core +
harness.

## Ownership (BINDING — identical table in all five orders)

| Path | Owner |
|---|---|
| `crates/quilltap-core/src/services/orchestrator.rs`, `services/chat_events.rs`; `help/**` (repo root); `docs/v4/**`; §A families for P4.D160 | P4.D160 |
| `apps/web/**` (source, specs, e2e, `package.json` version) | P4.D161 |
| `crates/quilltap-core/src/services/help_chat/**`, `model/request_builder/google.rs`; `crates/quilltap-web/tests/fixtures/help-chat-*.db(.meta.json)` + `harness/oracle/fixtures/build-help-chat-fixture.ts`; `harness/oracle/providers/**` + `harness/oracle/fixtures/request-envelopes/google-*.ndjson` (append-only); §A families for P4.D162 | P4.D162 |
| `crates/quilltap-core/src/services/chat_create.rs`, `api/chat_create.rs`; §A families for P4.78 | P4.78 |
| `crates/quilltap-core/src/services/brahma_console/**`; §A families for P4.79 | P4.79 |
| `docs/CHANGELOG.md`, `docs/developer/porting/status-log.md` | append-only, all lanes |
| `docs/developer/porting/drift-ledger.md`, `crates/quilltap-core/src/lib.rs`, `api/types.rs`, `services/mod.rs`, `services/tool_call_threading.rs`, `services/primary_stream.rs`, `crates/quilltap-web/src/**`, `crates/quilltap-host/src/**` | NOBODY |

Every other path is unowned this round: a lane that believes it must touch
one STOPS and reports (the `carryout` rule 3), naming the file and the
sibling it suspects.

## Verification gate (before declaring done)

1. `npm run lint` (incl. `check-qt-classes`), `npm test` (full file count
   recorded), `npm run build` clean — from the worktree with its own
   `node_modules` (`ng-test-defaults-to-watch-mode`, `ng-test-filter-matches-
   test-names`).
2. The full Playwright suite on port 4319, zero skips, the two new beats
   named with their first-run result (an activated beat's first run IS a
   measurement — `round-plan-takeaways`).
3. The mutation proofs above, each named in the lane record.
4. Commits per `.claude/commands/commit.md` (SPA bump only); the lane record
   appended to `status-log.md`; this order's status header updated.
