# Work order P4.9a2 — the image-detail modal family (lane A2): `imageInfoGet`, the deep detail modals, prev/next navigation, and the character album panel

> **STATUS: OPEN.** Lane A2 of the FOUR-lane **P4.6bd ∥ P4.9a2 ∥ P4.9f1 ∥
> P4.9f2** round (the consult-wire + image-detail + wardrobe round, planned
> 2026-07-18). **v4 reference baseline: HEAD `616930db`** (4.8.0-dev.75,
> `~/source/quilltap-server`) — run `git log 616930db..HEAD --oneline` at
> lane start; **if HEAD moved, STOP and report.** Sibling lanes:
> `p4.6bd-consult-wire.md`, `p4.9f1-wardrobe-server.md`,
> `p4.9f2-wardrobe-dialog-spa.md`. You run in a **worktree** with its **own
> Rust binaries** built once for the e2e server (the P4.6af memory). The
> **Shared contract**, **Ownership**, and **Verification gate** sections
> below are binding and reproduced verbatim in all four orders.
>
> **Why this order is renamed.** It supersedes what `p4.9a-photos-view.md`'s
> status header called "tier 2" — but that name was wrong, and keeping it
> would send the lane hunting for work that does not exist. The deferral
> `p4.9a` actually banked was never about the `/photos` screen; it was the
> deep image-detail modal family that the **P4.6ac** courier/images lane
> deferred and `p4.9a` inherited by adjacency. This order carries that work
> under its own name. **When this lands, the unifier marks `p4.9a` fully
> CLOSED** (no residual tier 2 remains on it) and records the rename in
> `m6-screen-parity.md`.
>
> **⚠ CRITICAL SCOPE CORRECTION — read before you open a single file.**
> A fresh survey (2026-07-18 @ `616930db`) overturned the backlog's framing:
> **`/photos` is already 100% ported. There is NO leftover work on that
> screen.** v4's `app/photos/PhotosView.tsx` (522 lines) opens only a
> **private inline** `PhotoDetailModal` defined at `:383-511` and mounted at
> `:325-332`; v5's `apps/web/src/app/screens/photos/photos-page.ts` (534
> lines) already mirrors it faithfully — inline modal template `:205-320`,
> the `selected` signal `:338`, Escape handling `:65-68`/`:526-528`, delete
> `:513-517`. Its only API calls are `GET /api/v1/photos`
> (`PhotosView.tsx:92`, `:120`) and `DELETE /api/v1/photos/{linkId}`
> (`:172`) — all landed in P4.9a tier 1. **This is an IMAGES-FAMILY lane,
> not a photos lane.** Do not re-derive, re-audit, or "finish" `/photos`.
>
> **Two further scope corrections that REMOVE work the backlog implied:**
> 1. **Image tag editing does not exist in v4's UI.**
>    `components/tags/tag-editor.tsx` (295 lines) has no image arm —
>    `getApiBasePath` (`:38-47`) handles only `character | chat | profile` —
>    and its only three mount sites are
>    `app/aurora/[id]/edit/components/CharacterBasicInfo.tsx:558`,
>    `app/aurora/[id]/view/components/TagsTab.tsx:23`, and
>    `components/settings/connection-profiles/ProfileModal.tsx:421,869`.
>    The image tag endpoints exist server-side
>    (`app/api/v1/images/[id]/route.ts:6-7`, schema `:25-31`) but have **no
>    UI caller**; image tags render read-only, and both galleries hardcode
>    `tags: []` (`PhotoGalleryModal.tsx:141`,
>    `EmbeddedPhotoGallery.tsx:186`). Porting tag editing would be **net-new
>    design, not a port** — it is explicitly OUT of scope (tier 3, named).
> 2. **`PhotoGalleryModal`'s `mode="character"` / `"user-character"`
>    branches have NO live call site.** Only `ChatModals.tsx` mounts the
>    component, always `mode="chat"`. Port the discriminated union
>    faithfully so the type is honest, but **do not invent hosts** for the
>    dead branches; record the finding.

## Mandate

Bring up the deep image-detail modal family v4 layers on top of its
galleries, and the one server read it depends on. Deliver: the
**`imageInfoGet`** verb + its REST edge + the fixture extension + new
differential cases; the **`ImageDetailModal`** equivalent with
`ImageMetadata`'s **character-album panel** ("In Photo Albums", per-character
remove, Set Avatar, "Save to photo album") over that new read-back; **prev/
next navigation** in both of its v4 halves (host index arithmetic + modal
arrows + keyboard) including the **nested-Escape suppression** and the
**z-layering**; the **`ChatGalleryImageViewModal`** equivalent for chat-mode
items; and the four v4 RTL suites ported case-for-case. Tier 2 deepens the
character gallery tab to `EmbeddedPhotoGallery` parity (avatar set/clear, the
link-relink branch) and adds the deleted-image placeholder family. v4's
strings carry verbatim.

**Without `imageInfoGet`, v5's existing save-to-album buttons are
WRITE-ONLY** — they cannot show already-saved state the way
`ChatGalleryImageViewModal.tsx:49-90` does. That verb is the load-bearing
piece of this lane; everything visible depends on it.

## Survey-verified starting points (2026-07-18 @ `616930db` — do not re-derive)

### The v4 component family

| Component | Path | LOC |
|---|---|---|
| `PhotoGalleryModal` | `components/images/PhotoGalleryModal.tsx` | 364 |
| `ImageDetailModal` (re-export shim) | `components/images/ImageDetailModal.tsx` | 9 |
| `ImageDetailModal` (real) | `components/images/image-detail/ImageDetailModal.tsx` | 145 |
| `ImageActions` (nav arrows + toolbar) | `components/images/image-detail/ImageActions.tsx` | 106 |
| `ImageMetadata` (character-album panel) | `components/images/image-detail/ImageMetadata.tsx` | 150 |
| `useImageActions` | `components/images/image-detail/hooks/useImageActions.ts` | 229 |
| types | `components/images/image-detail/types.ts` | 53 |
| `ChatGalleryImageViewModal` | `components/chat/ChatGalleryImageViewModal.tsx` | 361 |
| `useImageNavigation` | `hooks/useImageNavigation.ts` | 81 |
| `EmbeddedPhotoGallery` | `components/images/embedded-gallery/EmbeddedPhotoGallery.tsx` | 197 |
| `useGalleryData` | `components/images/embedded-gallery/hooks/useGalleryData.ts` | 219 |
| `DeletedImagePlaceholder` | `components/images/DeletedImagePlaceholder.tsx` | 76 |
| `image-gallery` (the tile grid) | `components/images/image-gallery.tsx` | 187 |

**Interfaces.** `ImageDetailModalProps` (`image-detail/types.ts:43-50`) =
`{isOpen, onClose, image: ImageData, onPrev?, onNext?, onAvatarSet?}`.
`ImageData` (`:11-35`) — **note the `linkId` vs `id` split documented in the
comment at `:13-19`**: vault photos arrive carrying `{linkId}`, images-v2
files arrive carrying `{fileId}`, and the action layer branches on which is
present. Transcribe that comment into the v5 type; it is the single easiest
thing to silently drop.
`ChatGalleryImageViewModalProps` (`ChatGalleryImageViewModal.tsx:24-35`).
`PhotoGalleryModalProps` is a **discriminated union** on
`mode: 'chat' | 'character' | 'user-character'`
(`PhotoGalleryModal.tsx:40-67`).

### The API calls the deep modals make

- `ImageDetailModal.tsx:43-46` — **parallel** `GET /api/v1/characters` +
  `GET /api/v1/images/{image.id}`.
- `useImageActions.ts:49-53` — `POST /api/v1/characters/{characterId}/photos`,
  body `{linkId}` **or** `{fileId}` (the `ImageData` split above).
- `useImageActions.ts:95-98` —
  `DELETE /api/v1/characters/{characterId}/photos/{linkId}`.
- `useImageActions.ts:130-137` — `POST /api/v1/characters/{entityId}?action=avatar`,
  body `{imageId}`.
- `useImageActions.ts:183-187` — `POST /api/v1/photos`, body `{fileId}`.
- `ChatGalleryImageViewModal.tsx:63` — `GET /api/v1/images/{file.id}`; and at
  `:126`, `:137`, `:165`, `:176` the character-photos POST/DELETE pair
  **twice over** (once for the character, once for the user-character).
- `PhotoGalleryModal.tsx:111` — `GET /api/v1/chats/{chatId}/files`; `:122` —
  `GET /api/v1/characters/{targetId}/photos?limit=200`; `:198` —
  `DELETE /api/v1/chat-files/{fileId}`.

### Prev/next lives in TWO places

Index arithmetic in the **host** (`PhotoGalleryModal.tsx:188-194`,
`EmbeddedPhotoGallery.tsx:52-59`); rendering + keyboard in the **modal**
(`ImageActions.tsx:28-52`, `ChatGalleryImageViewModal.tsx:214-238`, keyboard
via `useImageNavigation.ts:43-60`). The arrow callbacks are handed down as
**conditionally `undefined` at the ends** of the list —
`PhotoGalleryModal.tsx:343-344` and `:358-359` — which is how the arrows
disappear; there is no separate disabled state. Port the `undefined` idiom,
not a boolean.

### ⚠ The subtlest invariant in the family

**`PhotoGalleryModal` suppresses its OWN Escape handler while a detail modal
is open**: it passes `handleEscape: selectedIndex === -1` into
`useImageNavigation` (`PhotoGalleryModal.tsx:170-174`). Without this, one
Escape press closes both layers. Paired with the z-layering: the gallery is
`z-50` (`:299`), both detail modals are `z-[60]`
(`ImageDetailModal.tsx:87`, `ChatGalleryImageViewModal.tsx:208`). Both facts
get their own unit spec citing the file:line.

### The THREE hosts, and how they differ

1. **Chat / Salon** — `app/salon/[id]/components/ChatModals.tsx:161-185`
   mounts `PhotoGalleryModal mode="chat"`, passing `chatId` plus the first
   character and first user-character (`:141-142`, `:165-168`). **Only this
   mode supplies `onImageDeleted`** (`:169-184`), which rewrites the message
   list to append `[attached photo deleted]` and refreshes the photo count.
   Chat mode alone yields `kind: 'chat'` items, and so routes to
   **`ChatGalleryImageViewModal`** (`PhotoGalleryModal.tsx:338-351`) — hard
   delete of the chat file plus the two album toggle buttons.
2. **The character gallery tab** —
   `app/aurora/[id]/view/components/CharacterGallery.tsx:20-30` mounts
   **`EmbeddedPhotoGallery`, NOT `PhotoGalleryModal`**: embedded (no
   overlay), adding upload, set/clear avatar, and per-tile delete via
   `embedded-gallery/hooks/useGalleryData.ts`. It opens **`ImageDetailModal`**
   at `EmbeddedPhotoGallery.tsx:171-196`, is the **only** host passing
   `onAvatarSet` (`:192-195`), and sets `linkId: selectedImage.id` (`:175`)
   so the save action takes the **link-relink branch**.
3. **The `character` / `user-character` modal modes** —
   `PhotoGalleryModal.tsx:120-144`, with `ImageDetailModal` mounted at
   `:353-361`. Reachable by type; **no live caller** (scope correction 2).

### The server gap — the one load-bearing piece

v4 has **no verb literally named `imageInfoGet`**; the surface is the route
`app/api/v1/images/[id]/route.ts`, `GET` at `:39-128`. It calls
`getPhotoLinkSummaryBySha256` from `lib/photos/photo-link-summary.ts`
(invoked `:86`), plus `repos.files.findById` (`:41`),
`repos.characters.findByUserId` (`:53`), and `getFilePath` (`:106`). The
response is `successResponse({ data: { … } })` at `:101-123`. **The full
payload and every invariant are pinned in §1 of the Shared contract below —
that block is the spec; do not paraphrase it from the route.** The
differential demands the **whole** payload even though both v4 consumers read
only `data.characterGalleryLinks` (`ImageDetailModal.tsx:55`,
`ChatGalleryImageViewModal.tsx:71`) — a partial port would pass today's
consumers and fail the next one.

### v5 current state (the SPA side)

- `apps/web/src/app/images/image-modal.ts` (255) — the lightbox. Verbs
  `characterPhotoSaveById` (`:184-185`), `chatFileDelete` (`:240`). Has
  download, copy, save-to-character, save-to-user-character, delete, backdrop
  close.
- `apps/web/src/app/images/photo-gallery-modal.ts` (144) — chat-mode grid
  only; **its header comment `:25-33` names this exact deferral** ("v4's deep
  detail modals (ImageDetailModal / ChatGalleryImageViewModal, tag editing,
  image-navigation) are a named deferral"). Retire that sentence as the work
  lands.
- Specs: `image-modal.spec.ts` (109), `photo-gallery-modal.spec.ts` (93).
- Hosts: `screens/salon/salon-conversation.ts:354` (`qt-photo-gallery-modal`)
  and `:390` (`qt-image-modal`); `screens/generate-image/generate-image-page.ts:15`.
- `screens/characters/view/tabs/gallery-tab.ts` (148) exists as a **minimal**
  grid over `characterPhotoList` + delete + multipart upload (its docstring
  says so) — it is NOT `EmbeddedPhotoGallery` parity: no detail modal, no
  avatar set/clear, no navigation.

**The actual missing delta**, enumerated:

1. **Prev/next** — no arrows, no index arithmetic, no ArrowLeft/ArrowRight,
   and no equivalent of the `handleEscape: false` nested suppression.
2. **The `ImageMetadata` character-album panel** — the "In Photo Albums"
   list, per-character remove, the Set Avatar badge/button, and the "Save to
   photo album" select. **Nothing in v5 corresponds.**
3. **The image-info fetch** — no verb exists (grep `ImageInfoGet` in
   `crates/quilltap-core/src/api/types.rs` and
   `apps/web/src/app/core/core-contract.ts` → zero hits, confirmed).
4. **Set-as-avatar from a modal.**
5. **`DeletedImagePlaceholder` + missing-image detection** — the
   `onError` → `missingImages` set (`PhotoGalleryModal.tsx:81`, `:287`) and
   the placeholder component itself.
6. **Character-mode gallery + the aurora gallery tab depth**
   (`EmbeddedPhotoGallery`) — absent as described above.

v5 **already has** `CharacterPhotoList` / `CharacterPhotoSaveById` /
`CharacterPhotoRemove` (`api/types.rs:571`, `:579`, `:587`) — the album
add/remove half is dispatchable today; only the **read-back** is missing.

### v5 current state (the server side to extend)

- `crates/quilltap-core/src/api/photos.rs` (285): `photo_gallery_list:115`,
  `photo_gallery_save:179`, `photo_gallery_entry_get:236`,
  `photo_gallery_entry_remove:251`. Reusable helpers: `lower():47-70` (the
  user-correctable-vs-internal error split, carrying v4 line citations at
  `:55`/`:62`) and `zod_int_in_range():81-102`. Reuse both; do not fork them.
- Dispatch: `crates/quilltap-core/src/api/engine.rs:2896-2952`, inside the
  `=== P4.9a ===` / `=== end P4.9a ===` fence; `ready_db():2958`.
- HTTP: `crates/quilltap-web/src/photos_routes.rs` (190): `photos_list:130`,
  `photos_save:148`, `photo_entry_get:172`, `photo_entry_delete:182`;
  helpers `unwrap_to_http:36`, `js_number:63`, `number_param:105`,
  `string_param:113`, `all_params:118`. **NOTE: §3 of the Shared contract
  forbids touching this file's `js_number`** — you own the file, but that
  one call-site swap onto `jsnum::number_from_str` is DEFERRED as a rider.

### Fixture + differential machinery (committed, in your file set)

- Fixtures: `crates/quilltap-web/tests/fixtures/photos-main.db`,
  `photos-mount.db`, and the pinned-id sidecar `photos-main.db.meta.json`
  (read at `photos_routes_equivalence.rs:332`).
- Builder: `harness/oracle/fixtures/build-photos-fixture.ts` (553).
- Oracle spec: `harness/oracle/fixtures/photos-web.json` (path computed at
  `photos_routes_equivalence.rs:80-82`).
- Differential: `crates/quilltap-web/tests/photos_routes_equivalence.rs`
  (533) — `fixtures_dir:77`, `spec_path:80`, `env_or_skip:84`, `fresh_db:95`
  (copies **both** `.db` at `:101-102`), the stub embedding seam `:122`, the
  stub image seam `:149-152`, `sorted:158`, `norm:173`, `status_of:177`,
  `success_body:190`, `blank_minted:200`, `check:213`,
  `check_key_order:262`, `decode_bytes:317`, main test
  `photos_routes_equivalence:326`. Per-verb call sites: `:373`, `:446`,
  `:499`, `:524`. Existing case names: `:399-422`, `:426-429`, `:437-442`,
  `:454-486`, `:515-519`.

**⚠ The fixture MUST GAIN an image carrying a `sha256` that is linked into a
CHARACTER photo-album mount.** `characterGalleryLinks` is populated only when
`sha256` is set AND only from linkers with
`mountStoreType === 'character' && isPhotoAlbum` (v4 `route.ts:88`) —
without such a row, every new differential case asserts an empty array and
proves nothing. Extending the fixture is the first task of tier 1, not an
afterthought.

### v4 tests to port case-for-case

There is **no** v4 test for `ImageDetailModal`, `ChatGalleryImageViewModal`,
`useImageActions`, or `ImageMetadata` — those four are uncovered upstream, so
**their fidelity rests entirely on the SPA specs this lane writes**. Say so in
the lane record. What v4 does cover, port case-for-case:

- `__tests__/unit/photo-gallery-modal-deleted-handling.test.tsx` (380) —
  `:78` render + detect missing; `:107` div container for missing; `:136`
  button container for valid; `:157` reload after cleanup; `:221` character
  mode; `:278` user-character mode; `:307` close button; `:328` not rendered
  when `isOpen` false; `:341` body-overflow lock; `:358` zoom in/out.
- `__tests__/unit/hooks/useImageNavigation.test.ts` (350) — `:23` Escape
  closes; `:39` no-op when closed; `:55` `handleEscape:false`; `:72` explicit
  true; `:91` ArrowLeft; `:110` ArrowRight; `:129`/`:146` absent handlers;
  `:163` both nav + escape; `:191` arrows ignored when closed; `:217-:272`
  body-scroll prevention including `preventBodyScroll:false`; `:292` listener
  cleanup; `:324` updated callbacks.
- `__tests__/unit/deleted-image-placeholder.test.tsx` (272) — `:32-:95`
  rendering variants; `:110` onCleanup after delete; `:146` cancel; `:169`
  error toast; `:196` network error; `:220` no-callback; `:247`/`:260` a11y.
- `__tests__/unit/image-gallery-deleted-handling.test.tsx` (254) — `:86`
  `onError` detection; `:104` zero-dimension `onLoad` detection; `:123` no
  placeholder on success; `:143` reload after cleanup; `:177` hide delete
  overlay for missing; `:200` independent per-image state; `:221`/`:231`/
  `:241` loading/error/empty.

Server-side v4 tests that constrain the `imageInfoGet` port (read them; they
pin the summary semantics): `__tests__/unit/lib/photos/photo-link-summary.test.ts`
(217), `character-gallery-service.test.ts`, `save-image-to-album.test.ts`,
`user-gallery-service.test.ts`, `resolve-character-avatar.test.ts`.

## Tiered deliverables

### Tier 1 — must land

1. **`imageInfoGet`** — the verb in `api/photos.rs` (reusing `lower()`), its
   arm inside the `=== P4.9a ===` fence in `api/engine.rs`, and the
   `GET /api/v1/images/{id}` REST edge in `photos_routes.rs`. The payload and
   its five invariants are §1's, verbatim: the `source` remap, the DERIVED
   `tagType`, the sha256 + `mountStoreType === 'character' && isPhotoAlbum`
   gate on `characterGalleryLinks`, and both 404 arms.
2. **The fixture extension + differential cases** — regenerate
   `photos-{main,mount}.db` + sidecar + `photos-web.json` from the builder,
   with the sha256/character-photo-album row described above, and extend
   `photos_routes_equivalence.rs` with cases covering: the populated
   `characterGalleryLinks`, an image with **no** sha256 (empty array), a
   non-character mount linker (empty array), each `source` remap value, both
   `tagType` derivations, the `_count` pair, and both 404 arms. Key order
   asserted via the existing `check_key_order` (`:262`).
3. **The deep `ImageDetailModal` equivalent** with `ImageMetadata`'s
   character-album panel over the new read-back: "In Photo Albums" list,
   per-character remove, the Set Avatar badge/button, and the "Save to photo
   album" select. Both `ImageData` branches (`linkId` vs `fileId`) exercised.
4. **Prev/next** in both halves — host index arithmetic
   (`PhotoGalleryModal.tsx:188-194`), modal arrows (`ImageActions.tsx:28-52`),
   keyboard (`useImageNavigation.ts:43-60`), the conditional-`undefined`
   ends — **including the nested-Escape suppression
   (`PhotoGalleryModal.tsx:170-174`) and the `z-50`/`z-[60]` layering**.
5. **The `ChatGalleryImageViewModal` equivalent** for `kind: 'chat'` items:
   the routing split at `PhotoGalleryModal.tsx:338-351`, the hard chat-file
   delete, and the two album toggle buttons with their already-saved state
   read from `imageInfoGet` (`ChatGalleryImageViewModal.tsx:49-90`).
6. **The four v4 RTL suites ported case-for-case**, each spec pinning the v4
   `file:line` it copied.

### Tier 2 — should land

7. **`EmbeddedPhotoGallery` parity** for the aurora character gallery tab
   (`screens/characters/view/tabs/gallery-tab.ts`, today a minimal grid):
   opening `ImageDetailModal`, `onAvatarSet` (the only host that passes it),
   and the `linkId: selectedImage.id` **link-relink branch**
   (`EmbeddedPhotoGallery.tsx:171-196`).
8. **Set-as-avatar from the modal** (`useImageActions.ts:130-137`).
9. **`DeletedImagePlaceholder` + missing-image detection** — the
   `onError`/zero-dimension-`onLoad` detection, the `missingImages` set
   (`PhotoGalleryModal.tsx:81`, `:287`), the placeholder component
   (`components/images/DeletedImagePlaceholder.tsx`, 76), and the
   reload-after-cleanup behavior.
10. **A live Playwright beat** in this lane's own spec file: boot → unlock →
    open a chat's photo gallery → open a thumbnail into the detail modal →
    arrow to the next image → Escape closes the DETAIL modal only (the
    gallery stays open) → a second Escape closes the gallery.

### Tier 3 — explicit deferrals (loud, named)

- **Image tag editing** — **does not exist in v4's UI**: the endpoints are
  live server-side but have no caller, and both galleries hardcode
  `tags: []`. Porting it would be net-new design, not a port. Deferred with
  that reason stated in the code comment and the lane record.
- **The dead `mode="character"` / `"user-character"` gallery hosts** — the
  union is ported; **no host is invented** for the branches with no live call
  site (`ChatModals.tsx` is always `mode="chat"`).
- **The `photos_routes.rs` `js_number` swap** onto `jsnum::number_from_str` —
  §3's scoped carve-out; P4.6bd owns the canonicalization, this call site is
  a follow-up rider. Record it in the status-log entry.

## The differential requirement, restated

Every ported unit arrives with an equivalence test against the v4 oracle;
"looks right" is not a proof.

- **Tier 1, the new verb: tier-2 structural route diff.** `imageInfoGet` is
  proven by the established `photos_routes_equivalence` recipe — the same
  fixture copied fresh per case, the same `norm` / `blank_minted` /
  `check_key_order` machinery, driven against v4's **real** route at
  `616930db`. The whole payload is asserted, not just the fields today's
  consumers read.
- **The SPA half is tier 4:** `ng test` plus the live Playwright beat. Since
  the four deep-modal components are **uncovered in v4**, every transcribed
  v4 behavior gets a v5 unit spec **pinning the v4 `file:line` it copied** —
  the nested-Escape suppression, the conditional-`undefined` arrow ends, the
  `linkId`-vs-`fileId` branch, the album-panel state derivation, the
  `kind: 'chat'` routing split, the z-layering. That citation trail is the
  only fidelity record these components will have.
- The album add/remove half it drives is already differential-pinned
  (`CharacterPhotoSaveById` / `CharacterPhotoRemove`) — do not re-port it.

## Fixtures

- **Consumes:** the committed `photos-{main,mount}.db` family + its
  `photos-main.db.meta.json` sidecar, and the committed e2e locked instance
  (global-setup copies FRESH each run — the `e2e-fixture-before-run-order`
  memory).
- **Delivers:** the **extended** `photos-{main,mount}.db` family + sidecar
  and a regenerated `harness/oracle/fixtures/photos-web.json`, carrying the
  new sha256 + character-photo-album linkage. The lane owns regenerating any
  oracle that depends on them.
- **The `oracle-regen-silent-stale-pass` rule applies**: an erroring builder
  silently leaves the OLD fixture in place and the suite goes green on stale
  data. After every regen, grep the emitted NDJSON / the rebuilt `.db` for a
  row you expect to be new.
- **Never transcribe a minted id.** Read pinned ids from
  `photos-main.db.meta.json` (the sidecar read at
  `photos_routes_equivalence.rs:332`) — the P4.9a lesson.
- **`apps/web/e2e/support/` is a fixture consumer too** (the `616930db`-round
  lesson): if the e2e instance needs an image row to drive the beat, seed it
  through the support layer, and check nothing there assumes the old fixture
  shape.

## Shared contract (BINDING — identical in all four orders)

The round is **P4.6bd ∥ P4.9a2 ∥ P4.9f1 ∥ P4.9f2** (the consult-wire +
image-detail + wardrobe round, planned 2026-07-18). Lane letters below:
**BD** = `p4.6bd-consult-wire.md`, **A2** = `p4.9a2-image-detail-modals.md`,
**F1** = `p4.9f1-wardrobe-server.md`, **F2** = `p4.9f2-wardrobe-dialog-spa.md`.

### §1 — the new wire verbs (fold-at-unify)

Each verb-adding lane keeps a LOCAL typed module + an
`as unknown as CoreRequest` cast (the established `home.api.ts` /
`photos.api.ts` pattern); the unifier folds them into `CoreRequest` in
`apps/web/src/app/core/core-contract.ts` and runs the name-for-name wire
diff against `crates/quilltap-core/src/api/types.rs`. Names + payloads are
pinned here and MUST NOT be renamed by a lane:

**P4.6bd: NO new wire verbs.** It changes no request or response shape. It
wires seams behind three EXISTING verbs (`ChatCustomToolRun`,
`CustomToolPreview`, and the `run_custom` tool path). The only observable
change is that a definition carrying an `llm` block now consults instead of
failing soft with `"no LLM invoker was available in this context"`
(`crates/quilltap-core/src/pascal/custom_tools.rs:1142`).

**P4.9a2: `imageInfoGet`** — v4 `GET /api/v1/images/{id}`
(`app/api/v1/images/[id]/route.ts:39-128`). Full payload verbatim:
`{id, userId, filename, filepath, mimeType, size, width, height, source,
generationPrompt, generationModel, createdAt, updatedAt,
tags[{tagId,tagType}], characterGalleryLinks[{characterId,characterName,linkId}],
_count:{charactersUsingAsDefault, chatAvatarOverrides}}`.
Invariants: `source` remapped `UPLOADED→upload` / `IMPORTED→import` /
`GENERATED→generated` (`route.ts:62-64`); `tagType` DERIVED not stored —
`CHARACTER` if the tagId is a known character id else `THEME`
(`route.ts:66-74`); `characterGalleryLinks` built ONLY when `sha256` is set
and ONLY from linkers with `mountStoreType === 'character' && isPhotoAlbum`
(`route.ts:78-99`); 404 arms = missing file (`:43-45`) and category not
`IMAGE`/`AVATAR` (`:48-50`). REST edge: `GET /api/v1/images/{id}`.

**P4.9f1:** tier 1 — `chatOutfitGet` (v4 `GET /api/v1/chats/{id}?action=outfit`);
`chatEquip` (v4 `POST /api/v1/chats/{id}?action=equip`, **all seven modes,
in v4's own enum order**: `wear`, `replace`, `equip`, `add_to_slot`,
`remove_from_slot`, `clear_slot`, `set_all` — **`equip` is a DEPRECATED
ALIAS for `wear`** (v4's comment, `outfit.ts:37-39`: "`wear` honors the
item's `replace` flag; `replace` force-swaps the slots it covers. `equip` is
a deprecated alias for `wear`") and is the easy-to-miscount seventh: port it
faithfully, do NOT drop it and do NOT collapse it into `wear` — schema
`app/api/v1/chats/[id]/actions/outfit.ts:37-79`, handler
`handleEquipSlot:193`, response `:329`); `wardrobeTransferDestinations` +
`wardrobeTransferApply` (v4 `GET|POST /api/v1/wardrobe/transfers`); the
GLOBAL archetype tier `wardrobeList` / `wardrobeCreate` / `wardrobeUpdate` /
`wardrobeDelete` (v4 `GET|POST /api/v1/wardrobe`, `PUT|DELETE
/api/v1/wardrobe/{itemId}`). Tier 2 — `wardrobePreviewAvatar` (v4 `POST
/api/v1/wardrobe/preview-avatar`) and `chatRegenerateAvatar` (v4 `POST
/api/v1/chats/{id}?action=regenerate-avatar`). REST edges mirror the v4 URLs.

**P4.9f2: NO new wire verbs.** It consumes P4.9f1's verbs above plus the
LANDED `CharacterWardrobeList/Create/Get/Update/Delete`
(`api/types.rs:532,536,541,546,552`), `ProjectWardrobe*`
(`:1045,1050,1056,1062,1069`), `CharacterList` (`:331`), and
`ImageProfileList` (`:787`).

### §2 — the wardrobe pair's hand-off (F1 ↔ F2, BINDING)

F2 codes against §1's F1 verbs behind its own local typed module + cast,
exactly as if they existed. Any F2 e2e beat that needs a live server for
those verbs is **ACTIVATE-AT-UNIFY**, with a skip message naming lane
P4.9f1. F1 does not write SPA code; F2 does not write Rust. Neither lane
may rename a §1 verb or change a payload field without the other's order
being edited in the same commit — a divergence here is a round-breaking
defect, not a merge conflict.

### §3 — the `jsnum` canonicalization (P4.6bd only, SCOPED)

P4.6bd lifts the duplicated JS `Number()` ports into
`crates/quilltap-core/src/jsnum.rs` as `number_from_str`, taking
`crates/quilltap-core/src/tools/llm_number.rs:109`
(`js_number_from_str` — the most fully documented port) as the canonical
source, with `tools/text_block_parser.rs:489-531` and
`model/tool_wire/text_parsers.rs:328` rewired as consumers. **Do NOT widen
`text_parsers`' deliberately narrower grammar** (it rejects hex/`Infinity`
as a documented seam) — preserve it as an explicit narrowing wrapper over
the canonical fn, carrying its seam comment forward.
`crates/quilltap-web/src/photos_routes.rs:63-100`'s twin belongs to lane
P4.9a2's file set this round and is **NOT touched by either lane**; that
one call-site swap is DEFERRED LOUDLY as a follow-up rider (record it in the
status-log entry). The `js_number_to_json` SERIALIZATION family (the ~9
copies the survey found) is a SEPARATE rider and is explicitly out of scope.

### §4 — the DARK-consult standing note

After P4.6bd lands, the three consult entrances are live and the workbench
`{live:true}` bench arm plus the composer `chatCustomToolRun` path become
REAL SPEND. No other lane in this round may add a `CompletionProvider`, wire
a provider seam, or un-refuse an LLM-backed verb. P4.9f1's `analyze-image`
stays refusal-armed for exactly this reason (tier 3).

## Ownership (BINDING — identical in all four orders)

- **P4.6bd:** `crates/quilltap-core/src/pascal/llm_consult.rs`,
  `crates/quilltap-core/src/tools/{run_custom.rs, executor.rs,
  llm_number.rs, text_block_parser.rs}`,
  `crates/quilltap-core/src/model/tool_wire/text_parsers.rs`,
  `crates/quilltap-core/src/jsnum.rs`, `crates/quilltap-host/src/spine.rs`,
  `crates/quilltap-host/src/host.rs`, and the THREE dark dispatch sites in
  `crates/quilltap-core/src/api/engine.rs` (`:2732-2760`, `:2770-2800`) plus
  `api/custom_tools.rs` if a signature must move; harness: its own
  differential files + oracle cases. MUST NOT touch `api/photos.rs`,
  `quilltap-web/src/photos_routes.rs`, or any SPA file. Version bumps:
  quilltap-core, quilltap-host, quilltap-harness (+`Cargo.lock`).
- **P4.9a2:** `crates/quilltap-core/src/api/photos.rs`, the `=== P4.9a ===`
  fenced block in `api/engine.rs`, `crates/quilltap-web/src/photos_routes.rs`;
  harness: `harness/oracle/fixtures/build-photos-fixture.ts`,
  `harness/oracle/fixtures/photos-web.json`, the committed
  `photos-{main,mount}.db` family + sidecar,
  `crates/quilltap-web/tests/photos_routes_equivalence.rs`. SPA:
  `apps/web/src/app/images/**` (incl. `image-modal.ts`,
  `photo-gallery-modal.ts`, and the new image-detail files) EXCEPT
  `generate-image-dialog.ts` / `standalone-generate-image-dialog.ts` /
  `image-profile-picker.ts`, `screens/characters/view/tabs/gallery-tab.ts`,
  `screens/photos/**`, its own e2e spec file. Version bumps: quilltap-core,
  quilltap-web, quilltap-harness (+`Cargo.lock`), SPA `package.json`.
- **P4.9f1:** NEW `crates/quilltap-core/src/api/wardrobe.rs` and
  `crates/quilltap-core/src/api/chat_outfits.rs` (or one module — the lane
  chooses, but nothing else in `api/` moves), their arms in `api/engine.rs`
  + `api/types.rs` + `api/mod.rs`, NEW
  `crates/quilltap-web/src/wardrobe_routes.rs` + its registration in
  `crates/quilltap-web/src/lib.rs`; harness: its NEW fixture family +
  builders + oracle cases + differential files. MUST NOT touch
  `api/photos.rs`, `photos_routes.rs`, `tools/**`, `pascal/**`, or any SPA
  file; MUST NOT edit `quilltap-host/**` except a single named seam wire if
  tier-2 preview-avatar needs one — **flag that loudly in the lane record if
  it happens**, since P4.6bd owns `spine.rs`/`host.rs` this round (coordinate
  by deferring the wire to unification rather than editing the file).
  Version bumps: quilltap-core, quilltap-web, quilltap-harness
  (+`Cargo.lock`).
- **P4.9f2:** SPA only: `apps/web/src/app/wardrobe/**` (new — the dialog,
  the item editor, the transfer dialog, their specs),
  `screens/characters/view/tabs/wardrobe-tab.ts` (the stub it replaces —
  `:3-7`'s "its own future work order" comment retires here),
  `screens/characters/view/character-detail.ts` and the character-edit entry
  file, the salon participant-card entry file, and `shell/shell.ts` (the
  footer entry — **P4.9f2 is the ONLY lane touching `shell.ts` this round**),
  plus its own e2e spec file. MUST NOT touch
  `screens/prospero/wardrobe/**` (the landed project manager — its
  `project-wardrobe-manager.ts:50` note STANDS and stays accurate),
  `apps/web/src/app/images/**` (P4.9a2's), or any Rust file. Version bump:
  SPA `package.json`.
- **The round's KNOWN Rust conflict set:** P4.6bd, P4.9a2, and P4.9f1 all
  add ADDITIVE arms to `crates/quilltap-core/src/api/engine.rs`,
  `api/types.rs`, `api/mod.rs`, and `crates/quilltap-web/src/lib.rs` (route
  registration). Additions only, §1-pinned, in distinct regions (BD edits the
  three EXISTING dark sites; A2 the `=== P4.9a ===` block; F1 new fenced
  blocks). The unifier reconciles (temp-branch cherry-pick + one full gate —
  the parallel-round-reconciliation memory). No other Rust file is shared.
- **Append-only for everyone:** `docs/CHANGELOG.md`,
  `docs/developer/porting/status-log.md` (union-merge at unification).
  Version bumps union at unify — the unifier RECOUNTS from the COMMITS
  (identical bumps merge silently — the P4.6aw/ax memory).
- **Nobody** edits `CLAUDE.md`, `phase-4.md`, `m6-screen-parity.md`, or a
  sibling lane's files; the unifier updates the phase docs.

## Verification gate (run before declaring done)

- `cargo fmt --all --check` clean.
- `cargo clippy --workspace --all-targets -- -D warnings` clean on **BOTH**
  feature sets.
- `cargo test --workspace --no-fail-fast` — main's baseline is **353 test
  binaries / 1,444 tests / 0 failed**; your lane's total must be ≥ that, and
  the lane's OWN differentials must be run **BY NAME** with `--nocapture`
  and grepped for `SKIP` (the `p4d8` memory: `cargo test` hides `eprintln`,
  so an unnamed run's SKIP is invisible). Use `--no-fail-fast` — the first
  failing BINARY otherwise aborts and hides the rest.
- Oracles regenerate from the v4 checkout at `616930db`. **If the v4 tree is
  dirty, regenerate from a pinned detached worktree** (the
  `oracle-regen-pinned-v4-worktree` memory: `git worktree add --detach`,
  symlink BOTH `node_modules`). Grep the emitted NDJSON for a row you expect
  — an erroring builder silently leaves the OLD fixture in place (the
  `oracle-regen-silent-stale-pass` memory).
- SPA lanes: `ng test` green and `ng build` clean, both run FROM `apps/web`;
  full Playwright green, run ALONE on port 4319 with THIS worktree's own
  Rust binaries (the P4.6af memory). A source mutation proves nothing until
  `ng build` rewrites the served bundle (the
  `e2e-mutation-check-needs-a-rebuilt-dist` memory).
- No `[value]` binding on any `<select>` with async or `@if`-gated options —
  bind `[selected]` per option (the standing dogfood-#6 rule, re-confirmed
  by P4.6bc).
- Don't run `ng build` and a `quilltap-tauri` release build concurrently —
  the dist embedding races (the `616930db`-round gate note).
- Every commit follows `.claude/commands/commit.md` (equivalence test with
  the port, version bump, CHANGELOG entry, status-log append).
