# Work order P4.D149 — the Concierge state chosen at chat creation, SPA half (v4 `303288fb4`)

**Status:** OPEN (ordered 2026-09-02, the `0b0617fee` drift catch-up round:
P4.D148 ∥ P4.D149 ∥ P4.D150 ∥ P4.D151 ∥ P4.D152).

## Preamble

- **v4 oracle baseline: `6d2a50382`** (drift-ledger §1, probe re-run at
  ordering 2026-09-02: v4 on `main` at `0b0617fee`, tree clean, both logs
  empty). **This lane's target commit is `303288fb4`** — the client hunks:
  `components/new-chat/NewChatForm.tsx` (+49), `hooks/useNewChat.ts` (+18),
  `types.ts` (+9), `NewChatModal.tsx` (+5), `providers/new-chat-provider.tsx`
  (+4), `app/salon/[id]/SalonView.tsx` (+4), and two client tests
  (`__tests__/NewChatForm.test.tsx` +52/−?, the NEW
  `__tests__/useNewChat.request-body.test.tsx` 125 lines — v4's CLIENT
  oracle for the body rule). Read the hunks: `git -C
  ~/source/quilltap-server show 303288fb4 -- components/new-chat
  components/providers "app/salon/[id]/SalonView.tsx"`.
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 on `main`,
  tree clean, `git log 0b0617fee..main` empty and `git log
  3a76b17df..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED** (ledger §1). This lane runs NO Rust
  oracle; its v4-client oracles (the two test files above) are READ from
  a lane-unique detached worktree at **`303288fb4`**
  (`/tmp/qt-v4-pin-p4d149-303288fb4`, ledger §5.1) so the transcription is
  taken from the pinned tree, never from the moving checkout.
- **Sibling lanes:** P4.D148 (the server half — §A binds the two; the
  server field does NOT exist on main until unification, so this lane's
  live beat is GATED, §F), P4.D150, P4.D151, P4.D152 (all core; none
  touches `apps/web`). §Shared contracts / §Ownership binding and identical
  across all five orders.
- **Worktree:** per the `carryout` skill (branch `claude/p4-d149-…`);
  `apps/web` needs its own `npm ci` in the worktree (memory note
  `spa-worktree-node-modules` — never `cp -a`). `CHANGELOG`/`status-log.md`
  append-only. **This lane OWNS Playwright port 4319** (§F) — one suite at
  a time.

## Mandate

Port the client half of `303288fb4` whole: the **The Concierge** dropdown
on the New Chat form directly ABOVE "Starting Scenario (Optional)" — the
same four options in the same two optgroups as the sidebar control, the
first option labelled `Monitored (default)` (the sidebar's says
`Monitored`; v4's form deliberately differs), the label carrying the
state's icon in its tone colour, the helper sentence beneath reading the
shared `CONCIERGE_STATE_PRESENTATION[state].detail` (the `hint` is
deliberately NOT shown — the reader is looking at the control that sets
it); `conciergeState` in the form state (default `'monitored'`), the
request-body rule (OMIT the key when `'monitored'` so a plain create stays
byte-identical), and v4's two client tests transcribed 1:1 as specs. Must
NOT touch: any crate, the sidebar control (`chat/sidebar/chat-section.ts`
— unchanged in v4), the presentation table (v4 reads it, changes nothing),
`quick-hide/**`, `concierge-mark.ts`.

## Survey-verified starting points (2026-09-02, from the shipped hunks — ledger §5.3)

1. **The control** (v4 `NewChatForm.tsx:571-599`): a `<div>` block with
   `<label htmlFor="new-chat-concierge" class="mb-2 block text-sm
   qt-text-primary">` wrapping `<span class="flex items-center gap-1.5">The
   Concierge <Icon name={presentation.icon} className="w-3.5 h-3.5
   {conciergeToneTextClass(tone)}"/></span>`; `<select
   id="new-chat-concierge" class="qt-select" disabled={creating}>` with
   `<optgroup label="The Concierge decides">` → `monitored` "Monitored
   (default)", `flagged` "Flagged"; `<optgroup label="You decide">` →
   `vouched` "Vouched Safe", `uncensored` "Uncensored"; then `<p
   class="qt-text-xs qt-text-muted mt-1">{presentation.detail}</p>`.
   Placed between the Roleplay Template block and the Starting Scenario
   block. v5 `screens/new-chat/new-chat-form.ts` — the Roleplay Template
   label is at `:140`, the scenario label at `:179-184` (`for="new-chat-
   scenario"`); insert between. v5's `qt-select` + the icon component the
   sidebar uses (`chat/sidebar/chat-section.ts:150-151` binds
   `conciergeStateIcon().name` / `.className`) are the idioms to reuse;
   `conciergeToneTextClass` is exported from
   `chat/concierge-state-presentation.ts:116`. The select is a CONTROLLED
   select over an `@for`-free static option list — the P4.D115
   `afterRenderEffect` idiom is NOT needed here (options are static), but
   the sidebar's `[value]` binding shape (`:157`) is the precedent; assert
   in the spec that a programmatic state change re-renders the selected
   option (the finding-#108 class).
2. **State + defaults** (v4 `types.ts:145-153`, `useNewChat.ts:114-117`):
   `conciergeState: ConciergeState` in `NewChatFormState`, `INITIAL_STATE.
   conciergeState = 'monitored'`. v5 `screens/new-chat/new-chat.types.ts:
   100-135` (`NewChatFormState` + the pristine constant at `:128-`) — add
   the field beside `imageProfileId` with v4's doc-comment; the state
   reducer in `new-chat.state.ts` gains `setConciergeState(next)` (v4's
   `handleConciergeStateChange` is a `setState(prev => ({...prev,
   conciergeState: next}))`).
3. **The request body** (v4 `useNewChat.ts:775-780`): `if (state.
   conciergeState !== 'monitored') requestBody.conciergeState =
   state.conciergeState` — placed after `imageProfileId` and BEFORE the
   roleplay-template block. v5 `screens/new-chat/new-chat.logic.ts:203-210`
   (`if (form.imageProfileId) body.imageProfileId = …` then the
   `roleplayTemplateId` tri-state) — same slot. `core/core-contract.ts:
   809-835` `ChatCreateRequest` gains `conciergeState?: ConciergeState`
   (§A) after `imageProfileId?` with the §A doc-comment; the `ConciergeState`
   union already exists in the SPA (`chat/concierge-state-presentation.ts`
   keys it) — import, don't redeclare.
4. **v4's client oracles:** `__tests__/useNewChat.request-body.test.tsx`
   (125 lines — the body rule: omitted when monitored, present otherwise,
   never `null`; read it from the pin and transcribe each `it(` 1:1 into
   `new-chat.logic.spec.ts`), and the `NewChatForm.test.tsx` hunk (+52: the
   dropdown renders the four options, the helper sentence follows the
   selection, the scenario test now reaches its select BY ID because the
   form has two `<select>`s — mirror that in `new-chat-form.spec.ts` so the
   existing scenario spec cannot silently bind the wrong select).
5. **Continue Elsewhere seeding** (v4 `SalonView.tsx:1730`
   `initialConciergeState={getConciergeState(chat)}`, threaded through
   `NewChatModal`/`new-chat-provider`/`useNewChat`): **NO v5 COUNTERPART.**
   `chat/sidebar/organize-section.ts:22` records *"Continue Elsewhere (v4
   :1554) — the continue-chat flow is unported"*, and `new-chat-page.ts:
   127-129` exposes only `characterId`/`projectId`/`autonomous` inputs.
   Tier 3: record it by name in the lane record and in the organize-section
   comment (append "and would seed `conciergeState` from the source chat —
   `303288fb4`"), so the future continue-chat lane carries it.
6. **The e2e beat** (`e2e/new-chat-flow.spec.ts` — three beats today at
   `:54/:89/:140`; the template-picker beat at `:140` is the shape to
   copy): pick `Uncensored` on the form → create → the landed chat's
   sidebar `#conciergeSelect` reads `uncensored` AND the transcript's
   second row is the Concierge's manual-uncensored bubble (the sentence the
   four-state walk `salon-concierge-four-state-flow.spec.ts` already
   asserts — reuse its locator). GATED `P4D148_SERVER_LANDED = false` (§F).
   A second, UNGATED beat proves the client rule alone: intercept `POST
   /api/dispatch`, create with the default → the body has NO
   `conciergeState` key; pick Flagged → the body carries `"flagged"` (the
   request never has to succeed for this assertion — but let it, the
   server ignores unknown fields today, memory note
   `dispatch-verb-ignores-unknown-fields`).
7. **Help hunks → the `p4.9i2` bank:** `help/chats.md` (+15, "A Word With
   the Concierge, Before the Doors Open") and `help/dangerous-content.md`
   (two sentences) — append both to the bank section in `phase-4.md` with
   the sha.

## Tiered deliverables

- **Tier 1 (must land):** points 1–4 (control, state, body rule, both
  spec transcriptions), the ungated body-rule beat, point 7.
- **Tier 2 (should land):** the gated create-time beat (point 6) authored
  and proven against a LOCAL build of P4.D148's branch if one is available
  (record whether it was — otherwise the unifier runs it first).
- **Tier 3 (explicit deferrals, loud):** point 5 (Continue Elsewhere
  seeding — no counterpart; recorded in two places). No `hint` line under
  the form control (v4's deliberate omission — say so in the template
  comment).

## The differential requirement (restated)

This lane's oracles are v4's CLIENT tests (the SPA verticals' standing
idiom — P4.D144's `concierge-presentation.mjs` executed the table at the
sha): transcribe `useNewChat.request-body.test.tsx` and the
`NewChatForm.test.tsx` hunk from the `303288fb4` pin 1:1 into the two spec
files, keeping v4's `it(` names as the spec names so the mapping is
greppable. The option labels, optgroup labels and the helper `detail`
sentences are byte-compared against the pinned `NewChatForm.tsx` /
`concierge-state-presentation.ts` (a `node -e` diff over the extracted
strings, recorded in the lane record). Mutation proofs: send the key when
monitored → the body spec reddens; swap the optgroups → the option-order
spec reddens; show `hint` → the helper spec reddens.

## Fixtures

- Consumes: the e2e salon fixture as-is (the four-state walk already seeds
  a chat with each state); no committed fixture changes.
- Delivers: nothing committed beyond the specs and the two beats.

## Shared contracts (binding, identical across all five orders)

**§A — the create-time Concierge wire (P4.D148 ↔ P4.D149).** The
`chatCreate` dispatch verb's request (v5 `ChatCreateRequest`, SPA
`ChatCreateRequest` in `core-contract.ts`) gains ONE optional key:

```
conciergeState?: 'monitored' | 'flagged' | 'vouched' | 'uncensored'
```

(v4 `createChatSchema`: `z.enum([...]).optional()` — spelled out, not derived;
`.optional()` is NOT nullable, so an explicit JSON `null` REJECTS like
`timestampConfig`'s does.) Absent ≡ `'monitored'`: no write, no announcement,
the request AND the created chat byte-identical to today's. Any other value is
applied through the EXISTING `apply_concierge_flip` chokepoint (v4
`applyConciergeFlip(chat.id, requested, chat)` with the fresh row) on ALL
THREE create branches (continuation / autonomous / ordinary), immediately
AFTER the system-prompt message and BEFORE `apply_chat_continuation` /
scenario / staff / greeting, so the Concierge bubble is the first row after
the prompt and the greeting is generated under the chosen state. The
announcement reuses v4's five `manual-*` kinds (`RealConciergeAnnouncer`) —
NO new sentences. The SSE progress status line is exactly
`Briefing the Concierge…` (U+2026), emitted only when a non-Monitored state
is applied, between the branch's existing status and the scene status. An
unknown string → the create schema's validation 400 (the envelope
`tc_bad_value_rejected` already pins). The CLIENT omits the key when the
form holds `'monitored'`. v5 serves NO REST `POST /api/v1/chats` (the SPA
creates through `POST /api/dispatch`; `chats_routes.rs` has the collection
GET only) — recorded, not this round's.

**§B — `CompletionResponse.cache_usage` (P4.D151 owns the widening).**
`model::completion::CompletionResponse` gains
`pub cache_usage: Option<crate::model::stream::StreamCacheUsage>` (v4
`LLMResponse.cacheUsage`), populated from `parsed.cache_usage` in the real
`completion_provider.rs` composition and `None` at every other construction
site. **P4.D151 edits every construction site itself, including the
`cache_usage: None` one-liners in harness tests owned by other families**
(`precompute_equivalence.rs`, `recall_replay_equivalence.rs`,
`title_update_tier3_equivalence.rs`, `settings_wire_actions.rs`,
`attach_mount_file_equivalence.rs`, `chat_regenerate_title_tier3_equivalence.rs`,
`answer_confirmation_tier3_equivalence.rs`, `danger_gatekeeper_tier3_equivalence.rs`,
`file_attachment_tier3_equivalence.rs`, `outfit_llm_choose_tier3_equivalence.rs`).
No other lane edits those harness files this round (P4.D150's pins are
core unit tests, not harness edits) — that is what keeps the pick clean.

**§C — `distill_memory_search` (P4.D150 owns).** The function gains a
trailing `options: CheapLlmTaskOptions` parameter threaded to
`executor.execute(...)` (v4 `latency: CheapLLMLatencyClass = 'background'`
→ `{ latency }`). Its three callers: `build_context.rs` (the dynamic-head
FALLBACK) passes `CheapLlmTaskOptions::interactive()`; `pre_compute.rs` and
`recall_replay.rs` pass `CheapLlmTaskOptions::default()`. No other lane
touches `services/memory_recap/distill.rs`, `services/build_context.rs`,
`services/pre_compute.rs`, or `services/recall_replay.rs`.

**§D — the `files.sha256` invariant (P4.D152 owns).** `files.sha256`,
`mimeType` and `size` ALL describe the bytes actually STORED (post-bridge),
never the writer's input — at chat upload, `.qtap` import, restore replay
AND the carried-store-rows restore branch (which reads the archived
`doc_mount_blobs.sha256` by the parsed blob id). The boot heal carries v4's
migration id `realign-file-entry-sha256-v1` in the P4.D140 ledger shape
(honour an existing `migrations_state` row from either app; write the row
only on a pass that realigned ≥ 1 row). P4.D152 does NOT touch
`services/file_fallback.rs` (the describe tier reads `files` by id, never
by sha) and does NOT edit `attach_mount_file_equivalence.rs` (P4.D151's);
its join proof is the NEW heal family + the state dumps.

**§E — the manifest substrate (P4.D151 owns for this round).** The generated
`provider_manifest/manifests/*.json` are regenerated ONCE, by P4.D151, from
the `0b0617fee` pin through `harness/oracle/providers/gen-provider-manifests.mjs`;
the expected result is BYTE-IDENTICAL (bug 118 is a v4 convergence onto
bytes v5 has carried since P4.D107). No other lane runs the generator.

**§F — Playwright.** Port 4319 is P4.D149's this round. P4.D149's
create-time beat is authored GATED behind `const P4D148_SERVER_LANDED =
false` (ACTIVATE-AT-UNIFY — the unifier flips it after P4.D148 lands and
runs it live); every other lane authors NO Playwright and says so.

**§G — the drift ledger.** Lanes never write `drift-ledger.md`; the five §3
rows were marked ORDERED at ordering, and `/unify` marks them ABSORBED /
NO-PORT-RATIFIED when the baseline moves to `0b0617fee`.

## Ownership (binding, identical across all five orders)

| Area | Owner |
|---|---|
| `quilltap-core`: `services/chat_create.rs` (the request field, `apply_requested_concierge_state`, the three branches, `auto_generate_first_message`'s attempt 0 + the shared uncensored-desk closure, the five log lines); harness: `chat_create_capstone_equivalence` (+ `cases/chat-create-capstone.test.ts`, `fixtures/chat-create-capstone.json`, `fixtures/build-chat-create-capstone.ts`), `initial_greeting_equivalence` + `first_message_context_equivalence` (run, not changed) | **P4.D148** |
| SPA: `screens/new-chat/{new-chat-form.ts, new-chat.types.ts, new-chat.state.ts, new-chat.logic.ts}` (+ specs), `core/core-contract.ts` §A region, `e2e/new-chat-flow.spec.ts` (the gated beat); the `p4.9i2` bank rows for `help/chats.md` + `help/dangerous-content.md` | **P4.D149** |
| `quilltap-core`: `services/memory_recap/distill.rs`, `services/build_context.rs` (the fallback-distill site + the inter-character log line + its unit pins), `services/pre_compute.rs`, `services/recall_replay.rs`; harness: `build_context_tier3_equivalence`, `precompute_equivalence`, `recall_replay_equivalence` (RUN only — no edits, §B) | **P4.D150** |
| `quilltap-core`: `services/file_fallback.rs` whole, `model/completion.rs` (§B), `model/completion_provider.rs` (§B), the `cache_usage: None` one-liners at `services/compression.rs`, `services/cheap_llm_exec.rs`, `services/memory_recap/mod.rs`, `services/outfit_selections.rs` + the harness sites listed in §B, `model/request_builder/chat_completions.rs` (the NanoGPT comment), `provider_manifest/manifests/*.json` (§E regen, expected no-op); harness: `file_attachment_tier3_equivalence` (+ `cases/file-attachment-tier3.test.ts`, `fixtures/file-attachment.json`, its builder), `fallback_engine_equivalence` (+ `cases/fallback-engine.ts`), `attach_mount_file_equivalence` (+ its case); the `p4.9i2` bank rows for `help/file-uploads.md` + `help/chat-settings.md`; the `b448eddd7` NO-PORT ratification | **P4.D151** |
| `quilltap-core`: `services/chat_files.rs`, `services/quilltap_import/files.rs`, `services/backup/restore/orchestrator.rs` (the files region — `restore_one_file` / `carried_store_rows` and the replay branch ONLY), NEW `db/files_sha256_realign_heal.rs` + its `db/mod.rs` line; `quilltap-host`: `host.rs` (one boot-chain block); harness: NEW `files_sha256_realign_heal_equivalence` (+ its oracle case + fixture builder), `files_tier2_equivalence` / `files_routes_equivalence` (the upload sha arm), `qtap_import_equivalence`, `system_restore_state` / `system_restore_equivalence` / `restore_vintage_state` (the sha arms + any NEW committed archive), `character_photo_upload_tier2_equivalence` + `photo_tools_equivalence` (run), a harness-only byte-changing `PixelCodec` twin | **P4.D152** |
| Playwright (port 4319) | **P4.D149's branch only** (§F) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it (§G) |

**Version bumps:** P4.D148 bumps `quilltap-core` + `quilltap-harness`;
P4.D149 bumps the SPA `package.json`; P4.D150 bumps `quilltap-core` (+
`quilltap-harness` only if a harness file changes — §B says none should);
P4.D151 bumps `quilltap-core` + `quilltap-harness`; P4.D152 bumps
`quilltap-core` + `quilltap-harness` + `quilltap-host`. Per commit; the
unifier recounts at unification (the playbook's silent-auto-merge trap).

## Verification gate before declaring done

`npm run lint` (the `check-qt-classes` guard — every class the new block
uses must resolve; `qt-text-xs`/`qt-text-muted` exist, verify rather than
assume), `npm test` (all files, `--filter` matches TEST NAMES not paths),
`npm run build` (the only real type gate — bare `tsc` checks nothing);
the full Playwright suite on port 4319 with the new ungated beat green and
the gated beat SKIPPED by its flag (`run_in_background: true`, full log +
sentinel, never `| tail`); the string byte-diff against the pin recorded.
No Rust gate (no crate touched — say so). Per-commit rule:
`.claude/commands/commit.md` (CHANGELOG entry per commit, SPA
`package.json` bump, lane record appended to `status-log.md` under
"P4.D149").
