# Work order P4.D88 — the hair slot, SPA half — v4 `4423ad10` (client)

**Status:** CLOSED (2026-08-18, branch `claude/p4-wardrobe-hair-spa-101b70`; unified same day; the `P4D87_HAIR_SLOT_LANDED` beat flipped LIVE at unification). The registry + 11-site consolidation + CSS + forward-compat landed; ⚠ the order's empty-hair-SILENCE mandate was REFUTED by measurement (v4's components render an empty Hair row/"nothing" card — `reportWhenEmpty` is a lib/-only rule in v4; v5 renders what v4 renders, pinned both directions). Deferrals dispositioned in the lane record (the Green-Room half of the beat → dogfood; use-outfit twin landed, not deferred). Lane record: `status-log.md` → "P4.D88 — LANE CLOSED".

## Preamble

- **v4 baseline for this round: `979652a9`** (v4 main, HEAD at planning
  2026-08-18, tree CLEAN; the `bugfix` branch measured by `diff` — only the
  test-only `009c49b2` deflake, NO-PORT). This lane ports the CLIENT half of
  v4 commit **`4423ad10`** ("feat(wardrobe): add the 'hair' slot").
- **Drift-check at lane start.** `git log 979652a9..main --oneline` AND
  `git diff main bugfix -- lib/ app/ packages/`. If v4 moved, **STOP** and
  report.
- **Sibling lanes this round (disjoint ownership):**
  - **P4.D87** — the server half. **This lane's live surface depends on the
    server accepting/serving the fifth slot** — any e2e beat that round-trips
    a worn hairdo lands gated **ACTIVATE-AT-UNIFY**
    (`P4D87_HAIR_SLOT_LANDED`); component specs mock the dispatch layer so
    they are green lane-locally. The Shared contract below is binding and
    identical in both orders.
  - **P4.D89** (bugs 76/77 — providers + salon/chat files) and **P4.D90**
    (workspace tab refresh — workspace chrome + hand-rolled view loaders).
    **The one contested file is
    `apps/web/src/app/wardrobe/wardrobe-control-dialog.ts`: THIS lane owns
    it.** P4.D90's ~6-line activation hook for that dialog is an AT-UNIFY
    edit specified in P4.D90 — do not add it here, and expect it to appear
    at unification.
- **Worktree:** per the `carryout` skill; `npm ci`, never `cp -a`
  (`spa-worktree-node-modules`); `edit-inside-the-worktree-not-main`.

## The drift, classified (surveyed 2026-08-18)

v4's client half is a **net deletion**: the real work was consolidating
five local slot-list/label/badge maps onto the new `WARDROBE_SLOT_TYPES` +
`WARDROBE_SLOT_META` registry, after which hair appears automatically in
the wardrobe control dialog's slot filters and equipped rows, the
item-editor's component-picker groups, the outfit selector's
"same as last conversation" preview, and the Green Room's
`OutfitSlotsPreview` — all with a **rose badge**. One forward-compat
guard rides along (`OutfitSlotsPreview.tsx:27` — `slots[key] ?? []` so an
older four-key payload renders with empty hair instead of crashing).
**v5 has no slot-meta registry at all** — every v5 site hard-codes the
four slots — so this is a build-the-registry lane, not a mechanical port.

## Survey-verified starting points (2026-08-18)

**v4 (main @ `979652a9`):** deleted local maps —
`components/wardrobe/equipped-bundle-card.tsx:31-46` (`SLOT_LABEL` +
`TYPE_BADGE_CLASS`), `equipped-slot-row.tsx:29-44` (same pair),
`outfit-selector.tsx:126-135` (`SLOT_LABELS`),
`wardrobe-item-editor/constants.ts:8-24` (`GROUP_LABEL`, `GROUP_ORDER`,
`TYPE_BADGE_CLASS`), `OutfitSlotsPreview.tsx:15-21` (the `SLOTS` array).
`wardrobe-control-dialog.tsx:74` → `SLOT_FILTERS = ['all',
...WARDROBE_SLOT_TYPES]`; `{...EMPTY_EQUIPPED_SLOTS}` spreads →
`makeEmptyEquippedSlots()`/`cloneEquippedSlots()` (`:30-33`, `:197`).
`OutfitSlotsPreview.tsx:27` gains `slots[key] ?? []`.
CSS: the app-side hair additions are
`app/styles/qt-components/_content.css:216` (selector line) +
`:1669-1676` (the rose rule) and `_variables.css:491-492`/`:831-832`
(the token pairs — values pinned in the Shared contract). The +101
theme-storybook hunk has **no v5 target** (v5 ships no storybook package;
its mirror is the app stylesheet, which already carries the full
four-slot family at `apps/web/src/styles/qt-components/_content.css:223-227`,
`:1650-1685` and `_variables.css:486-495`/`:831-840`).

**v5 targets (every four-slot hard-coding in the SPA):**

| v5 file | site |
|---|---|
| `app/core/core-contract.ts` | `:3377` `WardrobeSlotType` union; `:3798-3799` `OutfitPreviewSlots`; `:5789-5790` equipped slots; `:5822` slot param |
| `app/wardrobe/equipped-slots.ts` | `:50-51`, `:56`, `:64-65` (`EMPTY`, factory, clone) |
| `app/wardrobe/equipped-slot-row.ts` | `:8-11` local `SLOT_LABELS` |
| `app/wardrobe/wardrobe-control-dialog.ts` | `:60` `SLOT_FILTERS`; `:942` per-slot loop |
| `app/wardrobe/item-editor/constants.ts` | `:11-14` `GROUP_LABEL`, `:19` `GROUP_ORDER`, `:24-27` `TYPE_BADGE_CLASS` |
| `app/wardrobe/item-editor/types.ts` | `:20` `CandidateGroup` |
| `app/wardrobe/dissolve-bundles.ts` | `:140-141`, `:189` |
| `app/wardrobe/outfit-store.ts` | `:116` `ResolvedSlotItems` |
| `app/screens/new-chat/outfit-selector.ts` | `:58`, `:62-66`, `:74` |
| `app/screens/new-chat/outfit-slots-preview.ts` | `:12-17` the `SLOTS` array — **the Green Room rose-badge target** (consumed by `green-room-dialog.ts:67`); `:20` docstring says "four-slot" — update it |
| `app/screens/prospero/wardrobe.api.ts` | `:22-23` |

Also check `app/wardrobe/{outfit-composer,wardrobe-item-editor,
wardrobe-item-row}.ts`, `app/wardrobe/item-editor/
wardrobe-component-picker.ts`, and `app/screens/prospero/wardrobe/
project-wardrobe-manager.ts` for slot lists/labels the table above may
have missed (v4's commit touched their React twins).

## Mandate

Build the SPA's slot registry (mirroring the Shared contract), consolidate
every hard-coded four-slot site onto it, add the hair badge CSS tokens and
rule, land the forward-compat `?? []` guard, and update the Green Room
preview — so a hairdo item created/worn server-side renders everywhere the
four slots do today, with the rose badge, and NOTHING renders for an empty
hair slot (no filter chip count, no vacancy text — mirror the
reportWhenEmpty contract wherever the SPA lists slots). Do **NOT** touch
`crates/**`, the providers/salon/chat files (P4.D89), or the workspace
chrome + view loaders (P4.D90). `ng test`/`ng build` green is the
baseline; the parity specs are the proof.

## Tiered deliverables

**Tier 1 (must land):**

1. The registry (one module, e.g. `app/wardrobe/slot-meta.ts`): the
   ordered five-slot list + per-slot meta exactly as the Shared contract
   pins, plus `makeEmptyEquippedSlots`/`cloneEquippedSlots` twins — then
   every table row above reads from it (delete the local maps as v4 did).
2. `WardrobeSlotType` + `OutfitPreviewSlots` + equipped-slot types in
   `core-contract.ts` gain `hair`.
3. The CSS: `--qt-badge-wardrobe-hair-{bg,fg}` light + dark token pairs
   (values in the Shared contract), the selector-line addition, and the
   rose rule — in v5's `_variables.css` + `_content.css` mirrors.
4. Forward-compat: the outfit-preview and any equipped-slots reader treat
   an ABSENT `hair` key as `[]` (spec: a four-key payload renders without
   error and without any hair row/vacancy text).
5. Parity specs: slot filters show Hair; the item editor groups/badges a
   hair item; the Green Room preview renders the rose badge for a hair
   item and NOTHING when hair is empty; the equipped row labels from the
   registry. Mutation-prove the empty-hair silence (force a
   `(empty)`/fallback render — a spec must go red).

**Tier 2 (should land):**

6. The gated e2e beat (`P4D87_HAIR_SLOT_LANDED`): create a hair item in
   the wardrobe dialog, wear it, see it in the equipped row + the Green
   Room preview badge on a new chat. Remember the gated-beat first-run
   rot class (`4.8.2-round-unification`) and that an EMPTY state cannot
   be waited on (`wardrobe-set-all-flake-deflaked`) — seed a visible
   pre-state.

**Tier 3 (explicit deferrals — loud, named):**

- v4's `use-outfit.ts:192` hook change rides whichever v5 site consumes
  the equipped-by-slot map — if no v5 twin exists, record that.
- Any generator-adjacent client surface (AI wizard/optimizer components)
  — unported, riders on the future generators lane (P4.D87's preamble
  records the bank).

## The differential requirement, restated

The SPA's oracle is v4's REAL client (`impersonation-overlay-spa-gaps`).
No new v4 client test file shipped with this commit's SPA half, so the
parity specs mirror the v4 component BEHAVIOR at the cited lines (slot
filter list, badge class per slot, preview `?? []`). Byte questions about
rendered text: probe v4's client directly
(`v4-lexical-export-oracle-probe` pattern). No Rust differential is owed
by this lane; the wire shapes are P4.D87's to prove.

## Fixtures

- Consumes: the e2e seed fixture — the gated beat needs a character with
  a wearable hair item; this lane owns extending the seed (remember the
  fixture-missing-TABLE class: verify the seed actually contains what the
  beat waits on).
- Delivers: nothing consumed by the harness.

## Shared contract (binding — identical in P4.D87 and P4.D88)

- **The slot list and order (append LAST, five entries):**
  `top, bottom, footwear, accessories, hair`.
- **The registry rows (v4 `wardrobe.types.ts:77-83`, verbatim semantics):**

  | slot | label | groupLabel | badgeClass | isClothing | reportWhenEmpty | emptyFallback |
  |---|---|---|---|---|---|---|
  | top | Top | Tops | qt-badge-wardrobe-top | true | true | topless |
  | bottom | Bottom | Bottoms | qt-badge-wardrobe-bottom | true | true | bottomless |
  | footwear | Footwear | Footwear | qt-badge-wardrobe-footwear | true | true | barefoot |
  | accessories | Accessories | Accessories | qt-badge-wardrobe-accessories | true | true | no accessories |
  | hair | Hair | Hair | qt-badge-wardrobe-hair | **false** | **false** | — (null) |

- **Wire shapes:** `equippedOutfit` serializes five keys in list order
  (`Slots::to_value`); an ABSENT `hair` key (older payload) reads as `[]`
  on BOTH sides (server parse + SPA `slots[key] ?? []`).
  `OutfitPreviewSlots` (Green Room progress frames) gains `hair`.
  `WardrobeSlotType` in `core-contract.ts` gains `'hair'`.
- **The reportWhenEmpty contract:** every surface that lists slots SKIPS
  an empty unreported slot entirely — no phrase, no label, no `(empty)`.
- **The badge color tokens (this lane lands them):**
  `--qt-badge-wardrobe-hair-{bg,fg}` = `hsl(345 55% 55% / 0.12)` /
  `hsl(345 55% 42%)` light, `hsl(345 55% 58% / 0.25)` /
  `hsl(345 55% 72%)` dark.

## Ownership

- **This lane owns:** `apps/web/src/app/wardrobe/**`,
  `apps/web/src/app/screens/new-chat/outfit-*`,
  `apps/web/src/app/screens/prospero/wardrobe*`, `core-contract.ts` (this
  round), the two qt-components CSS files, its specs + e2e beat + seed
  edits, `CHANGELOG` + `status-log.md` (append-only).
- **Version bumps:** SPA only. Three SPA lanes bump this round — the
  unifier recounts as base + total (`parallel-round-playbook` §C).
- **Must not touch:** `crates/**`, `app/workspace/**`,
  `app/screens/settings/providers/**`, `app/chat/chat-composer.ts`,
  `app/screens/salon/salon-conversation.ts`.

## Verification gate (before declaring done)

- `ng test` (`--filter` matches TEST NAMES, not paths) and `ng build`,
  both clean; the full Playwright suite if any active beat moved.
- The empty-hair-silence spec mutation-proven; the forward-compat spec
  red against a `slots[key]` (no `?? []`) spelling.
- The gated beat committed inert, gesture-script dry-checked against the
  seeded fixture (`e2e-playwright-traps`).
- No pgrep/grep poll loops; background + notification per CLAUDE.md.
- Per-commit rule: `.claude/commands/commit.md`.
