# Work order P4.75 — the SPA smalls: the streaming bubble's missing avatar (+ the danger ring it carries), the `workspace-search-documents` in-chat intermittent's ROOT CAUSE, the SPA-wide `title=` census, the dozen residue hosts, `#move-folder`

**Status:** OPEN — not started (ordered 2026-09-03, the follow-ups round 2:
P4.72 ∥ P4.73 ∥ P4.74 ∥ P4.75). Written from `phase-4.md`'s candidates 4
and 5 at the `0b0617fee` unification (P4.69's own measurement: "v5's
streaming bubble renders NO avatar"; the
`workspace-search-documents-flow.spec.ts:208` suite-context intermittent
"now a NAMED maintenance item — it wants its own root-cause look"; the
carried smalls: the `title=` census 612 v4 / 379 v5, the dozen residue
hosts, `#move-folder`'s single-instance id), P4.69's Tier 3, and
P4.D142's lane record (the residue-host survey list).

## Preamble

- **v4 oracle baseline: `0b0617fee`** (drift-ledger §1; probe re-run
  2026-09-03 at ordering: v4 checkout on `main`, tree CLEAN, `git log
  15573c3a1..main` empty, `git log 3a76b17df..bugfix` empty). The ledger's
  one §3 row (`15573c3a1`, bug 119, the unported character optimizer) is
  not this lane's.
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 checkout
  on `main`, tree clean, both logs empty. **Any mismatch: STOP and report.**
  This lane runs NO server-side oracle regen (its oracle is v4's client
  code, read at the pin); it still probes, because the v4 CLIENT files it
  transcribes must be read from a tree at the baseline — read them from a
  lane-unique pinned worktree (`PIN=/tmp/qt-v4-pin-p475-0b0617fee`, ledger
  §5.1 — no symlinks needed for reading source) and cite the sha.
- **Sibling lanes (this round):** P4.72 (web query remainder + the
  dispatch wrong-type census), P4.73 (the `/api/v1/images` collection route
  + the ingest codec — owns `core-contract.ts` this round), P4.74 (core
  smalls). §Shared contracts / §Ownership are binding and identical across
  all four orders. **This lane owns Playwright (port 4319)** — memory note
  `e2e-port-4319-is-repo-wide`; no sibling runs it.
- **Worktree:** per the `carryout` skill (`apps/web/node_modules` via
  `npm ci`, never `cp -a` — memory note `spa-worktree-node-modules`).
  `CHANGELOG` / `status-log.md` append-only.
- **Long gates:** `npm run build` / `npm test` (a BARE `ng` hangs — memory
  note `ng-test-defaults-to-watch-mode`; `--filter` matches TEST NAMES);
  Playwright via `run_in_background: true` + full log + sentinel; NEVER a
  `pgrep` loop; NEVER `| tail`. Memory notes `e2e-playwright-traps`,
  `e2e-serial-beat-timeout-cascades`, `e2e-tohavecount-resolves-on-first-
  matching-poll`, `false-and-mutation-kills-ts-narrowing`.

## Mandate

(1) Port the streaming bubble's avatar column — v4 `app/salon/[id]/
components/StreamingMessage.tsx:85-97` — so the live assistant row shows
the responding character's avatar under v4's `shouldShowAvatars` gate,
and the danger ring P4.69 could not attach lands there for free; with a
parity spec per v4 line and a mid-turn Playwright beat (the transcript
observed WHILE the turn runs — the P4.66 gesture, not a post-turn assert).
(2) Root-cause the `workspace-search-documents-flow.spec.ts:208` in-chat
intermittent (`.qt-chat-messages-list` resolved-but-HIDDEN after the card
click — the SILENT standalone arm firing with a Salon supposedly focused)
in `documents/open-document-from-search.ts` + the workspace focus/active-tab
order, fix the product defect if it is one (and it fires 1-in-3 in
isolation, so it is not "suite context"), and make the beat deterministic
without weakening its assertion. (3) Run the SPA-wide `title=` census (v4
612 sites vs v5 379) mechanically, classify every gap, and fill the ones
that are a v4 `title` on an element v5 renders without one (never invent
copy; never convert a v4 `title` to a Tooltip — P4.D131 did the nine
action-bar buttons because v4 did). (4) Give each of P4.D142's dozen
residue hosts (component hosts with NO host class, NO host style, NO
bare-element rule) its visual judgment and an explicit display where the
inline default matters, then widen `check-qt-classes.mjs` to the WIDER
invariant only if the residue reaches zero honestly (an allowlist that
rots is worse than the narrow guard — the script header says why). (5)
`#move-folder`: measure whether v4's `folder-picker` id is single-instance
too; if v4 has the same id, record it as v4-faithful and stop; if v4 keys
it per instance, port that. Must NOT touch: the server (nothing here needs
a payload change — §H), `core-contract.ts` (P4.73's this round), any
Rust crate, or a sibling's e2e spec.

## Survey-verified starting points (2026-09-03)

1. **The streaming avatar in v4.** `StreamingMessage.tsx:84-97` (read from
   the pin): the assistant row opens with `{shouldShowAvatars && (<div
   className={`flex-shrink-0 qt-chat-desktop-avatar${isDangerousChat ? '
   qt-chat-avatar-dangerous' : ''}`}><Avatar name={respondingCharacter?.name
   || 'AI'} title={null} src={respondingCharacter} size="chat" showName
   showTitle className="flex flex-col items-center w-32 gap-1" /></div>)}`
   — note the `'AI'` fallback name and `title={null}`. The gate
   `shouldShowAvatars` (`SalonView.tsx:1171-1174`): `!chatSettings ? true :
   chatSettings.avatarDisplayMode === 'ALWAYS'` — **the streaming row uses
   the ALWAYS-only rule, NOT the settled rows' GROUP_ONLY ≥2 rule** (v5's
   `message-list.ts:260-268 showAvatars` is the settled-row rule; the
   streaming row needs its own computed — transcribe both, do not merge
   them). `getRespondingCharacter` (`:1176-1184`): `respondingParticipantId`
   → the chat participant with that id → its `character`, else
   `participantsWithImpersonation.getFirstCharacter() ?? undefined`. Both
   are passed at `VirtualizedMessageList.tsx:375-388` alongside
   `isDangerousChat`.
2. **v5 today.** `chat/streaming-message.ts:16-26` carries the loud note
   (no avatar column, why, and what the port needs);
   `chat/message-list.ts:152-157` hosts `<qt-streaming-message [state]
   [renderingPatterns] [dialogueDetection]>` with NO avatar/danger inputs;
   `respondingParticipantId` lives on `ChatStreamState`
   (`core/chat-stream.reducer.ts:145`, set at `:277` from the frame's
   `participantId`, cleared at `:295`/`:358`); the settled rows' avatar
   markup is `message-row.ts:69-75` / `:87-92` (`qt-chat-desktop-avatar` +
   `[class.qt-chat-avatar-dangerous]="isDangerousChat()"` wrapping
   `<qt-avatar [name] [src] size="chat">`); the danger predicate is
   `shouldShowDangerStyling(chat)` (`chat/concierge-state.ts:127`, §C of
   the follow-ups round). `qt-avatar`'s `showName`/`showTitle` props — check
   `ui/avatar.ts` for the name-under-avatar variant the settled rows use,
   and match v4's `w-32` column.
3. **The mid-turn observation idiom.** `e2e/salon-optimistic-bubble-
   reconcile.spec.ts` (P4.66) captures the app's real
   `EventSource.prototype.onmessage` handler on `window.__qtEsHandler` and
   injects wire bytes through it (memory note
   `e2e-inject-wire-bytes-via-eventsource`), sampling the transcript
   WHILE the turn runs. The avatar beat is the same shape: inject a
   `participantId`-bearing stream frame for a seeded character, assert the
   streaming row's `qt-chat-desktop-avatar` shows that character's name
   (and the ring on a Flagged chat — seed one via the Concierge PUT, the
   `salon-concierge` beats show how), then let the turn settle. Sample
   with a ticking instrument (dogfood standing note: prove the instrument
   before trusting a negative).
4. **The search-documents intermittent.** The beat (`workspace-search-
   documents-flow.spec.ts:208+`): open the workspace → click the salon
   rail → click the `Solo Voyage` card → wait for `.qt-chat-messages-list`
   visible → open the search dialog → click the Documents card → expect
   the in-chat open. The red: after the card click `.qt-chat-messages-list`
   is resolved-but-HIDDEN, i.e. the SILENT standalone arm fired and its
   new tab backgrounded the salon. The decision is
   `open-document-from-search.ts:161-176 open()` → `activeSalon()` →
   `resolveActiveSalon(this.workspace.state(), pathname)` (`:71-91`):
   `state.panes[state.focusedPane].activeTabId` must be a `salon` tab with
   a `chatId`. The candidate causes, in order: (a) the workspace's
   `focusedPane`/`activeTabId` is set a tick AFTER the salon tab renders
   (the beat waits for the message list, not for the tab to become ACTIVE
   — the two can differ); (b) opening the search dialog moves focus
   (`search-dialog` is a portal/body-reparented host — memory note
   `angular-spa-traps`' portal rows); (c) `workspace.state()` is a signal
   read outside a reactive context returning a stale snapshot; (d) the
   `isWorkspaceTabsEnabled()` flag read racing the shell. Instrument
   first: log `resolveActiveSalon`'s inputs at click time in the failing
   run (the beat can expose them on `window`), reproduce ×10 in isolation
   (the round record measured 1-in-3), then fix the CAUSE. If it is (a),
   the product is fine and the BEAT must wait on the tab's active state —
   but read v4's `useOpenDocumentFromSearch` + workspace focus rules at
   the pin first: if v4 resolves the salon from the same state at the
   same moment, a v5-only stale read is a product defect. Record the
   answer either way; `phase-4.md` has carried this three rounds.
5. **The `title=` census.** v4: `ggrep -rn 'title=' app components
   --include='*.tsx' | wc -l` at the pin (the round record said 612); v5:
   the same over `apps/web/src/app --include='*.ts' --include='*.html'`
   (379). Mechanical classification per v4 site: (i) v5 element exists
   with the same `title` → ok; (ii) v5 element exists WITHOUT it → fill
   (byte-exact copy of v4's string; if v4's is an expression, transcribe
   the expression); (iii) v5 has no such element (unported surface —
   `p4.9i2`, `p4.9k`, PB1, the About backdrop…) → list by surface;
   (iv) v4's `title` was converted to a Tooltip by v4 itself (P4.D131's
   nine + any later) → ok. Deliver the census as a committed script
   (`apps/web/scripts/title-census.mjs`, runnable against a v4 path) whose
   output is the classification, so the next drift round can re-run it,
   and fill class (ii). Spec-pin a handful of the fills at their source
   (memory note `angular-class-binding-dedups-and-reorders` is about class
   strings; `title` attributes do not reorder, a plain attribute assert is
   fine).
6. **The residue hosts** (P4.D142's lane record, `status-log.md`
   ~`:97113-97120`): `qt-equipped-slot-row`, `qt-wardrobe-item-row`, three
   selectors in `wardrobe-control-dialog.ts`, `qt-outfit-quick-pick`,
   `qt-rng-dropdown`, `qt-custom-tools-popup`, `qt-library-file-picker-
   modal`, `qt-search-bar`, `qt-photos-page`, `qt-search-dialog`,
   `qt-brahma-model-picker` — twelve, each with NO host class, NO host
   style, NO bare-element rule, so each renders `display: inline`. For each:
   measure the rendered box in the real app (the inline-host family has
   shipped as a bug THREE times — #97, the Almanack `qt-entity-tabs`,
   #107); where the inline default constrains nothing (a host whose only
   child is itself a block that sizes from its parent) record "inline is
   harmless here — because X"; where it does, give the host an explicit
   display via `host: { class: 'qt-…' }` + a rule (the #107 idiom,
   spec-pinned) — never a bare `display:block` in a component style that
   the guard cannot see. Then re-run the WIDE form of `check-qt-classes.mjs`
   (P4.D142 built it and shelved it — its lane record says the parser is
   general enough for either): if the residue is now zero, land the wide
   invariant behind the same `qt-class-exception` escape hatch and prove it
   red-first against one host; if not, keep the narrow guard and update
   its header's residue list to the survivors with each one's reason.
7. **`#move-folder`.** `screens/files/folder-picker.ts:68` hard-codes
   `id="move-folder"`, and `move-to-project-dialog.ts:69` labels `for=
   "move-folder"`. Read v4's `components/files/FolderPicker.tsx` (or
   wherever the picker lives — find it at the pin) for whether the id is
   the same literal (then this is v4-faithful; record and stop) or keyed
   per instance (then port the key). Do not "improve" a single-instance
   id v4 has too.

## Tiered deliverables

**Tier 1 (must land):**

1. The streaming avatar column (item 1) with its own `shouldShowAvatars`
   computed (ALWAYS-only), the `respondingCharacter` resolver (id → cast →
   `character`, else the first character), the `'AI'` fallback, the danger
   ring bound to `shouldShowDangerStyling(chat)`, the settled-row markup
   reused for the column; parity specs named for v4's lines (`:85`, `:86`,
   `:87-95`, `SalonView:1171`, `:1176`); the streaming-message.ts header
   note rewritten to say what is now true.
2. The mid-turn avatar beat (item 3), ACTIVE on this lane's branch.
3. The search-documents root cause (item 4): the diagnosis in the lane
   record (with the ×10 isolation numbers before and after), the fix
   (product or beat, per the measurement), and the beat green ×10 in
   isolation AND in the full suite.

**Tier 2 (should land):**

4. The `title=` census script + the class-(ii) fills (item 5).
5. The twelve residue hosts adjudicated (item 6), each with its measurement
   and disposition in the lane record; the guard's residue list updated.

**Tier 3 (explicit deferrals, loud):** `#move-folder` if v4-faithful
(record only); the wide `check-qt-classes` invariant if the residue is not
zero (record the survivors); class-(iii) `title` sites (they belong to the
unported surfaces' orders — list them by surface in the lane record for
`p4.9i2` / `p4.9k`).

## The differential requirement (restated)

The SPA's oracle is v4's client code at the pin: every ported behaviour
ships with a parity spec transcribed from v4's JSX/hook (assertions named
for the v4 line), and every user-visible surface with a Playwright beat
that drives the real server over the seeded fixture. Pin strings at their
source (memory note `angular-class-binding-dedups-and-reorders`). Mutation
proofs for the avatar: flip the gate to the GROUP_ONLY rule (the ALWAYS
spec reddens), drop the `'AI'` fallback (its spec reddens), unbind the ring
(the Flagged beat reddens). For the intermittent: the fix is proven by the
×10 isolation run turning from ~1-in-3 red to 10/10 green with NO assertion
weakened (a `waitFor` on the state the product actually needs is not a
weakening; a longer timeout is).

## Fixtures

Consumes the committed e2e fixture (`global-setup.ts` seeds) unchanged; a
Flagged chat for the ring beat is set through the Concierge PUT at beat
time (the `salon-concierge` beats' idiom), never by SQL (memory note
`store-overlay-properties-cannot-be-sql-seeded` — and `isDangerousChat`
lives on `chats`, which IS SQL-seedable, but the PUT is what v4 does).
No server fixture changes.

## Shared contracts (binding, identical across all four orders)

- **§A — well-formed actions never move.** P4.72 may change the answer to
  `?action=` (empty), an UNKNOWN action, and DUPLICATED keys only; the
  status + body of every well-formed `?action=<served>` request and every
  single-valued query is byte-identical before and after (the existing web
  families are the pin). No SPA caller changes.
- **§B — `crates/quilltap-web/src/query.rs` is the ONE query reader**
  (`first` / `all`, v4's `searchParams.get` / `getAll`). P4.73's new
  `images_routes.rs` reads its `?action=` and `?tagId=` through it and adds
  nothing to it; P4.72 changes no signature in it.
- **§C — participant-status parsing has ONE home:**
  `quilltap_core::chat_predicates::participant_status_from_str`. P4.74
  retires the two stray copies; no lane adds a private status match.
- **§D — the pixel codec has ONE accessor:** `Engine::qtap_pixel_codec()`
  (`api/engine.rs:762`). P4.73 threads it into the chat-upload arm and the
  new images arms; no other lane constructs a `PixelCodec`.
- **§E — the `Request`/`Response` enums are P4.73's this round.** Only
  P4.73 adds variants (`ImagesList`, `ImageUpload`, `ImageImportFromUrl`,
  `ImagesGenerate`, `ImageDelete` — append-only, at the end of the enum,
  `serde(rename_all = "camelCase")`) and only P4.73 changes an existing
  variant's field types (`FileUpload.tags`, the `ChatCreate` trio). P4.72's
  census READS `api/types.rs`; it never edits it.
- **§F — `photos_routes.rs` same-file exception.** P4.73 edits that file
  ONLY to delete `image_delete_not_available` (`:161-167`) once the real
  DELETE lands in `images_routes.rs`; P4.72 edits it anywhere else (the
  `q`/`tag` duplicate rows). Both lanes name the region in their commit
  message so the unifier's hand-merge is trivial.
- **§G — `files_routes.rs` same-file exception.** P4.73 edits that file
  ONLY inside `files_upload_post` (`:943-1010`, the `tags` parse block that
  P4.62(a) widens); P4.72 edits it anywhere else (the thumbnail GET, the
  chat-files POST, the `force` duplicate row). Same commit-message rule.
- **§H — the streaming avatar is client-only.** P4.75 reads
  `respondingParticipantId` (already on `ChatStreamState`), the chat's
  participants and `avatarDisplayMode`; no server payload changes and no
  lane touches the stream frames.

## Ownership (binding, identical across all four orders)

| Area | Owner |
|---|---|
| `crates/quilltap-web/src/**` EXCEPT `lib.rs`, the NEW `images_routes.rs`, and the two §F/§G regions; `crates/quilltap-web/tests/**` (the grown `query_param_semantics_equivalence`, the NEW dispatch wrong-type census guard, arms in the existing body-guard families); `quilltap-core`: `api/chat_media.rs` (the `chat_file_link` guard ONLY); harness: `harness/oracle/cases/query-param-semantics.test.ts` | **P4.72** |
| `quilltap-core`: `api/types.rs` (append-only new variants + the `FileUpload.tags` and `ChatCreate` trio field widenings), `api/engine.rs` (the new dispatch arms, the `ChatFileUpload` codec line `:4121`, the `FileUpload` arm `:4362`), `api/files.rs` (`file_upload`'s `link_ids` widening), the NEW `api/images.rs`, the file that validates `Request::ChatCreate`'s body (named in the lane record at start — `api/chats.rs` or `services/chat_create.rs`, measured), `services/file_storage.rs` (a codec seam ONLY if the images arms need one); `quilltap-web`: `lib.rs` (route registration), the NEW `images_routes.rs`, the §F/§G regions; `quilltap-host`: `host.rs` (the import-fetch seam wiring), a NEW `image_import_fetch.rs`; harness: NEW `images_routes_equivalence` + `harness/oracle/cases/images-routes.test.ts`, a NEW committed `images-{main,mount}.db` pair + builder if needed, `files_routes_equivalence` (the codec WIRING arm), `chat_create_capstone` (the trio arms), `lora_log_anchor_guard` (the ninth anchor moves); SPA: `core-contract.ts` (the §1 type-only mirror of the new verbs) | **P4.73** |
| `quilltap-core`: `services/answer_confirmation.rs` (the status match ONLY), `services/file_fallback.rs` (log fields ONLY), `services/cost_events.rs` (the swallowed-write warn ONLY); harness: `message_attribution_equivalence.rs`, `primary_stream_tier3_equivalence.rs` + `harness/oracle/cases/primary-stream-tier3.test.ts` + its fixture builder (additive), the three shared-stage recipe headers (`precompute_equivalence.rs`, `mail_carina_tools_equivalence.rs`, `photo_tools_equivalence.rs` + their `.test.ts` twins), the NEW handler-logging inventory doc | **P4.74** |
| SPA: `chat/{streaming-message,message-list}.ts` (+specs), `documents/open-document-from-search.ts` + `search/search-results.ts` + whatever workspace file the root cause names (+specs), the `title=` census script under `apps/web/scripts/`, the residue hosts' files (each named in the lane record), `screens/files/folder-picker.ts` (`#move-folder` only if fixed); e2e: `workspace-search-documents-flow.spec.ts`, NEW streaming-avatar beat | **P4.75** |
| Playwright (port 4319) | **P4.75's branch only**; every other lane's beats are authored gated (ACTIVATE-AT-UNIFY) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it |

**Version bumps:** this lane bumps the SPA `package.json` per commit
(base: SPA 0.5.631); P4.73 also bumps it for the contract mirror — the
unifier recounts as base + total bumps (memory note
`parallel-round-playbook` §C). No crate bumps.

## Verification gate before declaring done

`npm run build` (the only real SPA type gate — memory note
`spa-tsc-does-not-typecheck-app-sources`); `npm test` (files / tests
counted in the lane record); `npm run lint` incl. `check-qt-classes.mjs`
(+ its `--self-test` if the guard changed); the full Playwright suite on
this lane's branch against a fresh build (`run_in_background`, full log +
sentinel, the counts recorded; the search-documents beat ×10 in isolation
recorded separately); mutation proofs applied-then-reverted by file backup.
No cargo gate needed unless the lane touches a crate (it must not). Per-
commit rule: `.claude/commands/commit.md`. Lane record → `status-log.md`
("Lane record — P4.75"), the status header here updated at close, P4.69's
status header gains one line naming where its streaming-avatar deferral
closed, and `dogfood-findings.md`'s standing note on the inline-host family
(#107's sweep suggestion) updated with the residue disposition. 💸 for the
next dogfood pass: the streaming avatar on a real multi-character turn
(does the column show the RESPONDING character, not the first one).
