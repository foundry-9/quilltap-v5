# Work order: P4.6bj — wake the dormant memory pipeline (the CONTEXT_SUMMARY + MEMORY_EXTRACTION job handlers)

**STATUS: CLOSED — UNIFIED ON MAIN (2026-07-22).** All four units landed
(unit 0 the `orchestrator_tier3` stale-RED fix; unit 1
`build_turn_transcript` + the new tier-1 family; units 2–3 both handler
bodies + the new tier-3 `memory_pipeline_jobs` family; unit 4 the host
wire — both handlers registered in `ProductionSpineFactory`, closing
carina's `memory_extraction_limits` deferral). **The extraction/fold
pipeline is LIVE in production.** Deferrals left loud (unchanged from
the order): the other unregistered `KNOWN_JOB_TYPES`; the in-loop
summary check keeps `FoldEpisodePassSeams` (episode pass live; the
Librarian/mirror/refresh/cost arms stay no-op in-loop — the JOB path
runs them all). Live proof owed: the next dogfood pass on the Friday
copy (the e2e instance has no API keys by design). Records: the status
log (lane + unification records).

**Size: one session, single lane** (the two handlers share the fixture
machinery and the host wire; the transcript builder is a small tier-1
prerequisite of the extraction handler). This is phase-4's named
next-candidate #1: three rounds of episodic work (P4.d12–15) left the
whole extraction/fold/gate pipeline **verified but DORMANT** — neither
`CONTEXT_SUMMARY` nor `MEMORY_EXTRACTION` is a registered handler in
`quilltap-host`, so every job the finalizer / danger scan enqueues dies
on the runner's loud fallback and nothing in the running server drives
`process_turn_for_memory` or the job-side `generate_context_summary`.

**Unit 0 — ALREADY LANDED in this lane (before this order was cut):**
the `orchestrator_tier3` stale-RED follow-up. The P4.d15 diagnosis
("v5 omits the memory-recap block") turned out to be ALREADY HEALED by
the round-3 landings; the residual divergence was the fold-time
**episode pass**: `run_summary_check` folded with `NoopSeams` while
v4's in-loop check runs the real `generateContextSummary`
(`runFoldEpisodePass` included, un-mocked in the orchestrator oracle).
Fixed by threading seams through the check
(`check_and_generate_summary_if_needed_with_seams` + the new
`FoldEpisodePassSeams`), orchestrator + ctx-summary families both green
over fresh oracles. Recorded in the status log; do not redo.

v4 sources (surveyed 2026-07-22 at `deab0e5d`; lib-identical to the
`8bf3cb5f` oracle baseline):
- `lib/background-jobs/handlers/memory-extraction.ts` (~245 — the
  whole handler: hydration, transcript, danger resolve, limits,
  timestamp anchor, `processTurnForMemory`, debug-log persist, cost
  event)
- `lib/background-jobs/handlers/context-summary.ts` (~95 — lookups,
  `generateContextSummary`, the danger-classification chain)
- `lib/services/chat-message/turn-transcript.ts` (212 —
  `buildTurnTranscript` :107–212 is UNPORTED; `findTurnOpenerMessageId`
  :67 already lives in `message_finalizer.rs`)
- `lib/background-jobs/handlers/index.ts` :41/:43 (the registrations)
- `lib/background-jobs/queue-service.ts` (`MemoryExtractionPayload` /
  `ContextSummaryPayload` — v5's `queue_service.rs` already enqueues
  both with matching payloads: the finalizer's
  `trigger_turn_memory_extraction` and `danger_scan.rs:249`)

## What to port

1. **`build_turn_transcript`** (pure; new
   `services/turn_transcript.rs`, re-exporting the existing
   `TurnTranscript`/`TurnCharacterSlice` from `memory_tasks`). Walk
   forward from the opener (exclusive), group ASSISTANT messages by
   `participantId`→character, `\n\n`-join repeat contributions, skip
   system/tool/silent/participant-less rows, break on the next
   non-system USER message or after the `extractionAnchorMessageId`.
   Carry the subtle bits v4's comments call out:
   - the **user-controlled opener promotion** (opener's participant is a
     `controlledBy:'user'` CHARACTER → a first-class `isUserControlled`
     slice PREPENDED to the order);
   - `turnOpenerMessageId === null` → scan-all mode (greeting/continue);
   - `lastMessageCreatedAt` / `turnTimestamp` fallback chains
     (`m.createdAt ?? previous`);
   - `latestAssistantMessageId` = last collected assistant row.
   Move `find_turn_opener_message_id` (+ its `is_truthy_silent` read
   coercion) here from `message_finalizer.rs` and re-use it there — one
   source of truth; the silent-read coercion applies to the builder's
   `isSilentMessage` skip too.
   **Differential (tier-1, new family):** an oracle case driving v4's
   REAL `buildTurnTranscript` + `findTurnOpenerMessageId` over a
   committed JSON corpus (messages/participants/characters/options per
   case), NDJSON out, exact diff. Corpus must cover: null opener,
   opener promotion, multi-character grouping + joining, all four skip
   classes, the anchor stop, missing participant/character rows, and
   timestamp fallbacks (absent `createdAt` mid-turn).

2. **The MEMORY_EXTRACTION handler**
   (`services/memory_extraction_job.rs`, v4
   `handleMemoryExtraction`). Payload decode; connection-profile +
   chat-settings lookups (missing → job FAILURE, v4 throws);
   chat-missing → warn + COMPLETE (v4 returns void); message filter to
   `type === 'message'`; participant-character hydration;
   `resolveUserCharacterParticipant` (first `controlledBy:'user'`
   CHARACTER participant); the transcript build; empty-slices →
   COMPLETE; `resolveDangerousContentSettings(chatSettings, chat)` +
   `is_chat_active_dangerous`; `get_memory_extraction_limits` (the
   instance-settings read the carina handler still defers — port the
   parse to `MemoryExtractionLimits` here and CLOSE that deferral for
   both handlers if cheap, else keep carina's `None` and note it);
   the guarded project-description read (broken project store degrades
   to `None`); the `sourceMessageTimestamp` chain (latest-assistant →
   opener → `chat.createdAt`); the
   `TurnMemoryExtractionContext` assembly (`timelineMode ?? 'realtime'`,
   `inAutonomousRoom = chatType === 'autonomous'`,
   `registry_cheapest_for_current: None`); `debugMemoryLogs` persisted
   onto `result.sourceMessageId` (failure swallowed); the cost event
   (`estimateMessageCost` seam + `create_memory_extraction_event`,
   failure swallowed, gated on nonzero usage).
   **Differential (tier-3, new family, modeled ON
   `carina_memory_extraction_tier3`):** jest-real-DB oracle driving
   v4's REAL `handleMemoryExtraction` (repos + processor real; canned
   completion by recorded call key, canned embedding, mocked
   `getMemoryExtractionLimits` + canned `estimateMessageCost`); v5
   replays through the ported handler. Diff `memories`,
   `vector_entries`, `vector_indices`, `chat_messages`, `chats` in the
   shared minted-remap form + the llm-logs partition. Corpus: a
   single-character turn, a multi-character turn (two slices + a
   joined repeat contribution), a user-controlled-opener turn, an
   anchor-bounded autonomous turn (`extractionAnchorMessageId` +
   `witnessedContext`), chat-missing skip, empty-slices skip.

3. **The CONTEXT_SUMMARY handler**
   (`services/context_summary_job.rs`, v4 `handleContextSummary`).
   Payload decode; chat / connection-profile / chat-settings lookups
   (each missing → job FAILURE, v4 throws); available profiles;
   `generate_context_summary_with_seams` with
   **`RealContextSummarySeams`** (the job path is v4's real
   `generateContextSummary` — Librarian re-post, vault mirror,
   relevant-conversations refresh, cost events, episode pass ALL
   live; the ctx-summary family's `generate` ops already pin exactly
   this seam set); `forceRegenerate ?? false`; on
   `!success || !wasGenerated` → warn + COMPLETE (no chain); else the
   danger-classification chain
   (`resolveDangerousContentSettings(chatSettings)` — NO chat arg —
   mode ≠ OFF → `enqueue_chat_danger_classification_with_priority`
   at priority −2, failure swallowed). Note the profile mapping: v4
   hands the FULL connection profile to `generateContextSummary`'s
   `cheapLLM` input — map to `CheapLlmProfile` the way
   `title_update_job` does.
   **Differential:** EXTEND the `context-summary-service` family with
   `handler` ops driving v4's REAL `handleContextSummary` (its case
   already gates the seam mocks per-op — job ops run the writers REAL
   like `generate` ops). Corpus: a fold-firing job, a below-gate job
   (wasGenerated false → no chain), a forceRegenerate job, a
   missing-chat failure, and a danger-OFF job (no chained enqueue).
   Diff adds `background_jobs` (the chained danger row).

4. **The host wire** (`quilltap-host/src/spine.rs`): two decode
   wrappers in `ProductionSpineFactory::build`'s `job_handlers`,
   following the `TitleUpdateJobHandler` / `CarinaMemoryExtractionHandler`
   patterns — per-job `CheapLlmTaskExecutor::with_logging` (user/chat
   from the payload), `ApiEmbeddingProvider` for the extraction +
   Real-seams paths, `PricingMessageCost` for the cost seams,
   `wire.completion(db)` for the cheap-LLM calls. Registration makes
   the finalizer-enqueued MEMORY_EXTRACTION and danger-scan-enqueued
   CONTEXT_SUMMARY jobs actually RUN — **the pipeline goes live and
   costs real money on every closed turn.**

## Gate

Fresh oracles (regen from `~/source/quilltap-server`; drift-check
first — baseline `8bf3cb5f`, HEAD `deab0e5d` is dispositioned
lib-free): the two NEW families + the extended ctx-summary family, and
transitive re-runs by name of `orchestrator_tier3`,
`memory_processor_tier3`, `carina_memory_extraction_tier3`,
`background_jobs_tier2` — all `--nocapture`, zero SKIP. `cargo fmt
--all --check`; clippy both feature sets `-D warnings`; full
`cargo test --workspace`; version bumps (core + harness + host).
`apps/web` untouched.

**Live proof:** the e2e instance has no API keys, so the newly-live
jobs fail loudly at the provider boundary there — the real live proof
is the next dogfood pass on the Friday copy (phase-4 next-candidate
#2); flag it in the record.

## Deferrals to leave loud

- The other unregistered `KNOWN_JOB_TYPES` (INTER_CHARACTER_MEMORY,
  SCENE_STATE_TRACKING, CONVERSATION_RENDER, EMBEDDING_*, …) stay on
  the loud fallback — out of scope.
- The IN-LOOP summary check keeps `FoldEpisodePassSeams` (episode pass
  live; Librarian/mirror/refresh still no-op there) — wiring those
  live in-loop requires un-mocking the orchestrator oracle's four arms
  and re-baking that corpus; the JOB path (this order) runs them all.
- Carina's `memory_extraction_limits: None` — closed here only if the
  shared limits parse lands (item 2); otherwise restate it.
