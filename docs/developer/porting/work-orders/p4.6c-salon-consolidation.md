# Work order P4.6c — Salon consolidation (the carried P4.6 follow-ups)

> One of the THREE parallel lanes of the P4.6c ∥ Settings round. **v4
> reference baseline: HEAD `a7b1398d`** (`~/source/quilltap-server`) — run
> `git log a7b1398d..HEAD` at lane start; if HEAD moved, STOP and report.
> Sibling lanes: `p4.6d-settings-server.md` + `p4.6e-settings-spa.md`. The
> **Ownership** section below is binding across all three; you run in a
> worktree; the unification pass integrates the lanes onto main.
>
> Mixed discipline: the SERVER half is a differential port (v4's real route
> handlers are the oracle — never accept a handler without its diff); the
> SPA half is tier-4 (v4 is the behavioral/visual reference) EXCEPT the
> skip-signal TS port, which is a pure port with fixture tests.

## Mandate

Close the follow-ups the P4.6 round named, making the Salon genuinely
usable for multi-character chats: server-side the four deferred handler
branches with their differentials; SPA-side the tier-2 controls (Skip
banner, Speaking-As, pause/resume, nudge/continue). Small, surgical, no
new screens.

## Deliverables — server half

All handler work lives in `crates/quilltap-core/src/api/salon.rs` (+ the
host spine / chat_send driver seam); you do NOT own `api/engine.rs` or
`api/types.rs` this round (the Settings-server lane does) — see Ownership
and the one unification-wire exception below.

1. **The turn `skipUserTurn` differential case.** The handler is already
   implemented (P4.6a) but excluded from the zero-mint differential because
   it posts a Host turn-pass announcement (minted message id + timestamps).
   Extend `salon_mutations_equivalence` (or add a sibling case file) with a
   minted-values form for this case: drive v4's REAL `handleTurnAction`
   `skipUserTurn` on the group-chat fixture; diff the response + `chats` +
   `chat_messages` with the minted Host row's id/timestamps
   remapped/placeholdered per the established forms. Bank BOTH branches:
   the successful skip (turn-pass row + `spokenThisCycle` fold + queue
   persist) and the `all-others-skipped` refusal (v4's exact copy,
   byte-diffed).
2. **Swipe-generate through dispatch.** The engine arm currently refuses
   (`engine.rs:378` — "swipe generation not yet assembled"). Deliver: a
   driver method for the generate branch (extend the `ChatSendDriver` trait
   in `api/chat_send.rs` — or a sibling trait there if cleaner — so the
   model boundaries stay host-side), the `quilltap-host::spine`
   implementation (compose the ported `regenerate_swipe` over the spine's
   providers, the ChatSpine pattern), and the route guards from v4's
   `handleGenerateSwipe` (ASSISTANT-only, `systemSender` rejected) in the
   handler. Verify with a differential driving v4's REAL
   `handleGenerateSwipe` (jest real-DB, canned completion keyed by the
   oracle-recorded canned key — the `regenerate_swipe_tier3` precedent; the
   Rust side drives your handler + driver directly). **The engine-arm swap
   (refusal → driver call) is a named UNIFICATION wire** — do not edit
   `engine.rs`; note the exact arm + the wiring in your final report.
3. **The `pendingToolResults` orchestrator corpus case.** The threading +
   TOOL-message pre-insertion landed in P4.6a
   (`services/orchestrator.rs`) but no corpus case exercises it. Add one to
   the `orchestrator_tier3` corpus: a send carrying one `pendingToolResults`
   element → the TOOL row lands before the model turn (content shape
   byte-exact vs v4), visible in the recorded stream key. Regenerate that
   oracle (you own the orchestrator corpus this round).
4. **The `processChatUpdates` full field set.** P4.6a ported the
   Salon-minimal subset (`isPaused` + `title`). Survey v4's
   `chatUpdateRequestSchema` + `processChatUpdates` (`chats/[id]/handlers/
   put.ts` + `helpers.ts`) whole and port the remaining field families over
   the ported `chats.update`; STOP-rule (name precisely) anything dragging
   in an unported subsystem. Extend the chat-PUT differential cases
   accordingly.
5. **The two small GET branches deferred by P4.6a:** the single-chat GET's
   attachment-resolution branch (linked `files` + mount-file attachments
   with `sha256`+`linkSummary` — the fixture needs a message with an
   attachment; extend the salon fixture builder, regenerate the committed
   fixture DBs + both salon oracles) and the chat-settings GET's
   default-injection branch (survey what v4's GET injects that the raw read
   doesn't; bank a missing-settings-row case).

## Deliverables — SPA half

All in `apps/web/src/app/chat/**` + `screens/salon/**` (you own those
regions; the pinned P4.6 contract already covers every variant you need —
`chatTurnAction`, `chatSetActiveSpeaker`/`chatImpersonate`/
`chatStopImpersonate`, `chatUpdate`, `chatSend.nudge` — and they are
already in `core-contract.ts`; if you find one missing, STOP and report
rather than editing the mirror, which the Settings-SPA lane owns).

6. **The skip-signal TS port + the Skip banner.** Port v4
   `lib/chat/turn-manager/skip-signal.ts` near-verbatim to TS
   (`apps/web/src/app/chat/skip-signal.ts`) — `computeSkipEligibility` and
   its leaves — with Vitest cases lifted from v4's own skip-signal tests
   (the Rust port `crate::skip_signal` is a second reference). Render v4's
   user-turn banner (SalonView ~1394–1455: copy verbatim) when it's the
   user's turn; the Skip button fires `chatTurnAction skipUserTurn` and
   surfaces the `all-others-skipped` refusal copy; a successful skip
   advances the turn display.
7. **Speaking-As.** Port `SpeakerSelector` (shown when ≥2 user-controlled
   participants) + the `useImpersonation` behaviors: `set-active-speaker`
   on selection, `speakingAsParticipantId` threaded into `chatSend`.
   Impersonate/stop-impersonate menu items only if cheap — else name the
   deferral.
8. **Pause/resume + nudge/continue.** The pause toggle
   (`chatUpdate {isPaused}`) with v4's paused-state banner/copy; Continue
   already exists — add Nudge (a continue-mode send with `nudge: true`,
   v4's `handleNudge` semantics; the turn-queue buttons beyond that stay
   deferred).
9. Component tests for each control over the mocked CoreClient; extend the
   M4 Playwright spec (or a sibling `m4b` spec) with one multi-character
   beat if it can ride the existing fixture: open Group Expedition → the
   user-turn banner shows → Skip → a Host turn-pass chip appears. (The
   group fixture has 2 LLM + 1 user participant — verify eligibility holds;
   if the fixture shape doesn't produce a user turn, note it and keep the
   beat component-tested instead.)

## Verification

- Server: the extended/regenerated differentials green against v4 HEAD
  (`salon_mutations` [+skipUserTurn], the swipe-generate differential,
  `orchestrator_tier3` [+pendingToolResults case], the chat-PUT + GET-branch
  extensions in `salon_reads`/`salon_mutations`), plus re-runs of any
  adjacent differential your files touch.
- SPA: Vitest (skip-signal fixtures + control components), the full
  Playwright suite green.
- The standing gate per `.claude/commands/commit.md` (full workspace tests,
  clippy `-D warnings` default + `native-transport`, fmt, changelog,
  version bumps; SPA version bump).

## Ownership (BINDING for ALL THREE lanes this round)

| Region | Owner |
|---|---|
| `crates/quilltap-core/src/api/salon.rs`, `api/chat_send.rs` | **P4.6c** |
| `crates/quilltap-core/src/api/types.rs`, `api/engine.rs` | P4.6d (Settings server) — P4.6c's one engine-arm swap is a unification wire |
| `crates/quilltap-core/src/services/orchestrator.rs` + the orchestrator oracle corpus | **P4.6c** |
| `crates/quilltap-host/src/spine.rs` | **P4.6c** |
| Other `crates/**` (new settings/theme modules, db additions) + `harness/oracle/**` settings files | P4.6d |
| `harness/oracle/**` salon + orchestrator files + the committed salon fixture | **P4.6c** |
| `apps/web/src/app/chat/**`, `screens/salon/**`, `e2e/m4*` | **P4.6c** |
| `apps/web/src/app/core/core-contract.ts`, `app.routes.ts`, `shell/**`, `screens/settings/**`, `theme/**`, remaining `apps/web/**` | P4.6e (Settings SPA) |
| `docs/CHANGELOG.md`, `CLAUDE.md`, versions | each lane per the commit checklist; union at unification |
| `docs/developer/porting/phase-4.md` + status blocks | no lane (unification) |

If your work genuinely needs a file another lane owns, STOP and report —
the unification pass arbitrates.

## Deferrals (name them in the final report)

- Turn-queue UI beyond nudge (queue/dequeue buttons), `AllLLMPauseModal`,
  `EphemeralMessages`.
- Anything `processChatUpdates` stop-rules out.
- The rest of the standing full-Salon list (Document Mode, terminal pane,
  courier, images, sidebar/modals, Lexical, ToolMessage renderer, token
  badges, virtualization, qtap:// navigation).
