# Work order P4.9d — the quick-hide lane (lane D): the provider service, its consumers, the global tags tab, and ThemePreviewModal

> **STATUS: CLOSED — UNIFIED on main (2026-07-18).** All five units + the
> tier-2 theme preview modal landed, plus the v4-faithful Preview-button
> fix (an invented aria-label dropped). At unification the §2b mount went
> live (`qt-quick-hide-menu-section` inside `qt-user-menu`) and the
> hiding beat's ACTIVATE-AT-UNIFY storage poke was replaced with real
> user-menu toggle clicks. Deferred loud (the lane record has the list):
> the in-chat consumer (v4 `SalonView.tsx:707` → `p4.9e*`), the
> installed-themes tokens fetch (with the themes registry), the
> salon-list header autonomous toggle's recorded placement divergence
> (STANDS; it now binds the shared service). Pure SPA — zero crate
> changes; SPA at the round union 0.5.169. The original order follows.
>
> Lane D of the FOUR-lane P4.9a ∥ P4.9c ∥ P4.9b ∥ P4.9d round (the M6
> backlog items 1–4 round, planned 2026-07-18). **v4 reference baseline:
> HEAD `d68638b4`** (4.8.0-dev.72, `~/source/quilltap-server`) — run
> `git log d68638b4..HEAD --oneline` at lane start; **if HEAD moved, STOP
> and report.** ⚠ The v4 working tree is DIRTY with the in-flight
> llm-consult feature; none of this lane's survey targets are dirty.
> This is a **pure-SPA lane** — it must NOT touch `crates/**` and adds NO
> wire surface (§3): the `tags.quickHide` column is already live in the
> v5 core on both read and write paths, and full tag CRUD verbs exist
> (`TagList`/`TagCreate`/`TagUpdate`/`TagDelete`,
> `crates/quilltap-core/src/api/types.rs:593-613`). Sibling lanes:
> `p4.9a-photos-view.md`, `p4.9c-about-profile.md`,
> `p4.9b-generate-image-screen.md`. The **Shared contract** and
> **Ownership** sections below are binding and reproduced verbatim in all
> four orders. You run in a worktree (own Rust binaries built once for
> the e2e server — the P4.6af memory).
>
> **Scope notes (survey-driven):** (1) v4 mounts its GLOBAL tags manager
> inside Settings → Appearance (`AppearanceTabContent.tsx:8,:49`) — v5's
> appearance tab shipped without it, so this lane ports the whole
> `tags-tab.tsx` (479 lines), not just a checkbox. (2) ThemePreviewModal
> is **RE-BINNED here from p4.9c** because its opener lives in the
> appearance tab this lane owns; the unifier records the re-bin in
> `m6-screen-parity.md`. (3) v4 `SalonView.tsx:707`'s in-chat quick-hide
> consumer feeds the unported `p4.9e*` dialog family — it is deferred
> loudly, NOT ported, so this lane never touches
> `salon-conversation.ts`.

## Mandate

Deliver v4's quick-hide system as a v5 Angular service + its consumers:
the three-key localStorage state (§4), the `shouldHideByIds` +
dangerous-chat filtering across the salon list, both home sections, the
characters roster, character detail/conversations, and the Prospero
cards; the `qt-quick-hide-menu-section` toggle surface (mounted into
lane P4.9c's `qt-user-menu` at unification — §2b); the global tags
manager (with the quickHide authoring column) in Settings → Appearance;
and ThemePreviewModal over v5's bundled themes. The existing salon-list
autonomous toggle refactors onto the shared service (same key, same
placement — the recorded placement divergence stands). This lane must
NOT touch `crates/**`, `shell/shell.ts`, `salon-conversation.ts`,
`home.spec.ts`, or any sibling-owned file. v4's strings carry verbatim.

## Survey-verified starting points (2026-07-18 @ `d68638b4` — do not re-derive)

### The v4 provider (`components/providers/quick-hide-provider.tsx`, 239 lines)

- State (`:51-56`): `quickHideTags` (`{id,name}` of tags flagged
  quickHide), `hiddenTagIds: Set<string>`, `hideDangerousChats`,
  `includeAutonomousRooms`, `loading`, `storageReady`.
- The §4 keys (`:31-33`). Methods: `toggleTag` (`:155`),
  `toggleHideDangerousChats` (`:168`), `toggleIncludeAutonomousRooms`
  (`:172`), `clearAllHidden` (`:176` — deliberately does NOT reset
  includeAutonomousRooms, `:179-180`), `refresh` (= reload the tag list,
  `:222`), `shouldHideByIds` (`:183-196`: false on empty; true if ANY
  passed id ∈ hiddenTagIds).
- **Port hazard — the dead method:** `shouldHideChat` (`:198-209`) reads
  `chat.isDangerous`, but the real payload field is `isDangerousChat`
  and NO consumer calls it — every consumer inlines
  `shouldHideByIds(tags) || (hideDangerousChats && chat.isDangerousChat)`.
  Port the inline consumer pattern with the REAL field name; do not
  replicate the dead method (record the divergence in a comment).
- Tag source (`:37-47`): `GET /api/v1/tags` filtered
  `Boolean(tag.quickHide)` (`:45`); prunes `hiddenTagIds` to still-valid
  ids on refresh (`:71-75`).
- Hydration (`:84-153`): the SSR-mismatch dance (post-mount read,
  `storageReady` write-gate) is a Next.js artifact — an Angular
  client-only service can read localStorage eagerly in the initializer.
  The cross-tab `storage` listener (`:131-153`) does NOT come free —
  port it deliberately (all three keys).
- "Dangerous" is the per-chat `isDangerousChat` classification boolean
  (written by the server-side classifier; surfaced via
  `lib/chat-utils.ts:72,126`) — NOT `dangerousContentSettings`. v5
  already carries the payload field (the home rows render the `*`
  marker) — the filter just reads it; zero server work.

### The v4 consumers (all `useQuickHide` call sites)

| Consumer | Line | Uses | v5 counterpart this lane edits |
|---|---|---|---|
| `app/salon/SalonListView.tsx` | `:37` | `shouldHideByIds, hideDangerousChats, includeAutonomousRooms`; filter `:89-112` (chat tags + all participant character tags → `allTagIds`; dangerous arm `:105`); effective-include math `:44-46` | `screens/salon/salon-list.ts` |
| `components/homepage/RecentChatsSection.tsx` | `:16` | dangerous arm `:23`, participant tags `:30-36` | `screens/home/recent-chats-section.ts` (absence note `:13-17`) |
| `components/homepage/CharactersSection.tsx` | `:24` | `characters.filter(c => !shouldHideByIds(c.tags))` (`:29-32`) | `screens/home/characters-section.ts` (absence note `:34-36`) |
| `app/aurora/AuroraView.tsx` | `:79` | `shouldHideByIds` (roster filter) | `screens/characters/list/characters-list.ts` |
| `app/aurora/[id]/view/CharacterDetailView.tsx` | `:53` | `shouldHideByIds, hiddenTagIds` | `screens/characters/view/character-detail.ts` |
| `components/character/character-conversations-tab.tsx` | `:37` | `shouldHideByIds, hideDangerousChats` | the character Conversations tab file |
| `app/prospero/[id]/components/ChatsSection.tsx` | `:60` | `shouldHideByIds, hideDangerousChats` | the Prospero chats card |
| `app/prospero/[id]/components/CharactersCard.tsx` | `:28` | `shouldHideByIds` | the Prospero characters card |
| `app/salon/[id]/SalonView.tsx` | `:707` | feeds unported `p4.9e*` dialogs | **DEFERRED LOUD — do not port** |
| `components/settings/tags-tab.tsx` | `:42` | `refresh` after authoring | the new global tags tab |
| `components/layout/left-sidebar/sidebar-footer.tsx` | `:106` | badge state (`hasQuickHideFeatures`/`hasAnyHidden`, `:144-145`) | rides the menu section / §2b |
| `components/dashboard/nav-user-menu-quick-hide.tsx` | — | the toggle UI | the new `qt-quick-hide-menu-section` |

### The toggle UI (`nav-user-menu-quick-hide.tsx`, 118 lines)

"Quick Hide Tags" section (`:62-82`): one eye/eye-off button per
`quickHideTags` entry → `toggleTag`. "Content Filters" (`:85-107`):
Dangerous Chats toggle (`:89-96`) + Show Autonomous Rooms toggle
(`:98-106` — note the INVERTED icon polarity: eye when *included*).
Exports `QuickHideIcon` (`:116`) for the menu-entry badge. Mounted at
`sidebar-footer.tsx:302` (above the ProfileMenu at `:309` — the §2b
ordering). The v5 section component carries all three toggles; the
existing salon-list header autonomous toggle stays (placement
divergence recorded in `autonomous-visibility.ts:6-14`) and now binds
the same service signal.

### The global tags tab (`components/settings/tags-tab.tsx`, 479 lines)

Mounted in Settings → Appearance (`AppearanceTabContent.tsx:8,:49`).
Global tag CRUD + the quickHide authoring checkbox ("Enable quick-hide
button", `:370-378`, spinner-gated per-row, subtext "Adds this tag to
the navbar quick-hide controls." `:380`), calling the provider `refresh`
on save (`:42`). The authored bit is SERVER-persisted
(`tags.quickHide`, DDL `docs/developer/DDL.md:1111`); v5 carries it
end-to-end already: `core-contract.ts:659` (update), `:2063` (DTO);
`crates/quilltap-core/src/db/tags.rs:104,:224-294,:375-419`;
`api/characters.rs:2241` (list emit), `:2330` (create default), `:2390`
(update read). Consume `TagList`/`TagCreate`/`TagUpdate`/`TagDelete` —
no new verbs.

### ThemePreviewModal (re-binned here)

`components/settings/appearance/components/ThemePreviewModal.tsx`: props
`:27-37` (`theme|null, isActive, isOpen, onClose, onApply, onUninstall?,
onExport?`); banner with contrast-computed foreground (`:112-211`), live
element preview with Light/Dark toggle (`:227-235`), image gallery
(`:238-250`), icon sheet (`:301-380`). For a NON-default theme it
fetches `GET /api/v1/themes/{id}?action=tokens` (`useThemePreview.ts:94`)
— **v5 has no themes registry and only bundled themes**, so the v5 port
renders from the local bundled theme data (`ThemeService.listThemes`,
`theme.service.ts:253`) with NO server fetch; the installed-theme tokens
arm is deferred loud with the registry (the standing appearance-tab
deferral, `appearance-tab.ts:56`). Opener: a per-theme Preview affordance
in the appearance tab's theme grid (v4 `ThemeSelector.tsx:252,:275-288`
— single instance keyed by `previewThemeId`). v5's appearance tab
(`appearance-tab.ts`, 301 lines) has swatch buttons only — this lane
adds the preview affordance alongside.

## Tiered deliverables

### Tier 1 — must land

1. **The quick-hide service** (`apps/web/src/app/quick-hide/`): signals
   over the three §4 keys (eager localStorage init, cross-tab `storage`
   listener, the still-valid-id prune on refresh, `clearAllHidden`
   sparing includeAutonomousRooms), `quickHideTags` from `TagList`
   filtered on `quickHide`, `shouldHideByIds` as a pure transcribed
   helper. `autonomous-visibility.ts`'s pure math + key move onto/behind
   the service (its spec's shared-key round-trip stays green). Unit specs
   pin every transcription with v4 file:line.
2. **The consumers**: the filter applied in salon-list (chat tags + ALL
   participant character tags + the dangerous arm + the existing
   effective-include math), both home sections, the characters roster,
   character detail + Conversations tab, and both Prospero cards —
   each matching its v4 site's exact tag-collection semantics (the table
   above). Defaults hide nothing, so existing specs stay green; new
   specs live in this lane's own files (NOT `home.spec.ts`).
3. **`qt-quick-hide-menu-section`**: the three-toggle section + per-tag
   eye buttons + `QuickHideIcon` badge state, self-contained over the
   service, selector pinned per §2b (the unifier mounts it in lane
   P4.9c's menu).
4. **The global tags tab** in Settings → Appearance: v4 `tags-tab.tsx`
   ported (list/create/edit/delete + the quickHide authoring checkbox +
   the refresh invalidation so the menu list updates live), placed where
   v4 places it in the appearance tab.
5. **A live Playwright beat** in this lane's OWN new spec file: boot →
   unlock → Settings → Appearance → create a tag, enable quick-hide on
   it → tag a character (or use a seeded tagged character) → toggle the
   tag hidden via the section (drive the section standalone if the menu
   mount is not yet live — the menu-walk variant is ACTIVATE-AT-UNIFY)
   → the salon list / home section hides the row → clear restores it.

### Tier 2 — should land

6. **ThemePreviewModal** over bundled themes + the per-theme Preview
   affordance in the appearance tab (banner, live preview with
   Light/Dark toggle, apply from the modal); the gallery/icon-sheet
   arms included only insofar as bundled themes carry the data — defer
   the rest loudly.

### Tier 3 — explicit deferrals (loud, named)

- **The in-chat consumer** (v4 `SalonView.tsx:707`) — travels with the
  `p4.9e*` dialog family; `salon-conversation.ts` untouched.
- **The installed-themes tokens fetch** (`?action=tokens`) — deferred
  with the themes registry (the standing appearance deferral).
- **The user-menu placement of the section** — §2b's unifier wire; the
  salon-list header autonomous toggle's placement divergence STANDS
  (recorded), it just rebinds to the shared service.

## The differential requirement, restated

Pure-SPA (tier 4): the proof is `ng test` + the live Playwright walk.
Every transcribed v4 behavior (shouldHideByIds semantics, the inline
dangerous arm with the REAL field name, the tag-collection per consumer,
clearAllHidden's autonomous exemption, the prune-on-refresh) gets a unit
spec pinning the v4 file:line it copied. The server side it consumes is
already differential-pinned (the tags CRUD + list payloads).

## Fixtures

- **Consumes:** the committed e2e locked instance (Lorian + Riya carry
  tags; global-setup copies FRESH each run — the
  `e2e-fixture-before-run-order` memory; seed additional tags via
  dispatch after unlock if needed, the P4.6z memory). Never assert
  absolute counts a sibling spec can grow (the P4.6ap memory).
- **Delivers:** none. No oracle regeneration owed.

## Shared contract (BINDING — identical in all four orders)

### §1 — the `app.routes.ts` route blocks

`app.routes.ts` is contract-managed this round (three lanes add routes).
Each lane inserts ONLY its own block(s), immediately BEFORE the
`path: ''` home entry (wildcard stays last), matching the file's lazy
`loadComponent` idiom; update the file's doc comment for your own
route(s) only. The unifier unions the blocks in this order and resolves
the known additive conflicts; no lane touches another lane's block.

- **P4.9a:** `photos` → `./screens/photos/photos-page` → `PhotosPage`
- **P4.9c:** `about` → `./screens/about/about-page` → `AboutPage`;
  `profile` → `./screens/profile/profile-page` → `ProfilePage`
- **P4.9b:** `generate-image` → `./screens/generate-image/generate-image-page`
  → `GenerateImagePage`
- **P4.9d:** no routes.

### §2 — the shell wires (unifier-applied)

- **(a) The photos nav flip.** `shell.ts` NAV_ITEMS `photos` keeps
  `route: null` in every lane; the UNIFIER flips it to `route: '/photos'`
  at unification (the route exists only then). Lane A does not edit
  `shell.ts`; lane C (its owner this round) does not flip it.
- **(b) The user-menu mount chain.** Lane C ships `qt-user-menu` (the
  shell-footer dropdown porting v4 `profile-menu.tsx` +
  `sidebar-footer.tsx:302,309`: quick-hide content ABOVE the
  Profile/About entries) with a pinned marker comment
  `<!-- §2b: qt-quick-hide-menu-section mounts here at unification -->`.
  Lane D ships `qt-quick-hide-menu-section` (standalone, self-contained
  over its own service). The unifier imports + mounts lane D's component
  at the marker. Lane D's menu-walk beats are ACTIVATE-AT-UNIFY.
  (Lane letters here: A = p4.9a, B = p4.9b, C = p4.9c, D = p4.9d.)

### §3 — the new wire verbs (fold-at-unify)

Each verb-adding lane keeps a local typed module + cast (the established
`home.api.ts` pattern); the unifier folds them into `CoreRequest` and
runs the name-for-name wire diff. Names + payloads are pinned:

**P4.9a:** `photoGalleryList` (v4 `GET /api/v1/photos`:
`{q?, tag?, limit?, offset?}` → `{entries, total, hasMore}`, entry shape
verbatim from `listUserGallery`); `photoGallerySave` (v4 POST:
`{fileId, caption?, tags?, chatId?}`; sha256 duplicate → v4's 400);
`photoGalleryEntryGet` (v4 `GET /api/v1/photos/{id}`);
`photoGalleryEntryRemove` (v4 DELETE — link removal + last-link GC);
tier 2 `imageInfoGet` (v4 `GET /api/v1/images/{id}`, the
`characterGalleryLinks` read). REST edges: `GET|POST /api/v1/photos`,
`GET|DELETE /api/v1/photos/{id}` (+ `GET /api/v1/images/{id}` tier 2).

**P4.9c:** `userProfileGet` (v4 `GET /api/v1/user/profile`, default
action ONLY — the `theme-preference` arms are already ported via
`theme.service`/chatSettings, do NOT re-port); `userProfileUpdate` (v4
PUT `{name, email}` incl. the email-uniqueness arm);
`userProfileSetAvatar` (v4 PATCH `?action=set-avatar` `{imageId}`,
category IMAGE/AVATAR validation, stores `image = /api/v1/files/{imageId}`);
`systemDataDir` (v4 `GET /api/v1/system/data-dir`; the `?action=open`
POST is REFUSAL-ARMED loud). Health `version`: the quilltap-web HTTP
`/health` body gains `version` (already-threaded `CARGO_PKG_VERSION`),
and `interpretHealth`/`HealthStatus` (core-transport.ts) gain ONLY an
optional `version` field — no other signature moves; both transports then
agree (the Tauri health command already carries the engine `HealthDto`
version). REST edges: `GET|PUT|PATCH /api/v1/user/profile`,
`GET /api/v1/system/data-dir`.

**P4.9b / P4.9d:** NO new wire surface. P4.9b consumes only
`imageProfileList`, `imageProfileGenerate` (its locked four params),
`charactersList`, `chatAddToolResult`. P4.9d consumes only
`TagList`/`TagCreate`/`TagUpdate`/`TagDelete` and existing payload
fields (`isDangerousChat`, tags). `imageProfileValidateKey`/`ListModels`
STAY refusal-armed; `sortByUserCharacter` on the image-profile list stays
unported (deferred loud in P4.9b).

### §4 — the quick-hide localStorage keys (P4.9d only)

`quilltap.quickHide.activeTags` (JSON id array),
`quilltap.quickHide.hideDangerous` (`'true'`/`'false'`),
`quilltap.quickHide.includeAutonomousRooms` (`'true'`/`'false'` — already
shared with `autonomous-visibility.ts`; one service owns all three after
this round). `clearAllHidden` must NOT reset `includeAutonomousRooms`
(v4 provider `:179-180`). No other lane reads or writes these keys.

## Ownership (BINDING — identical in all four orders)

- **P4.9a:** `crates/quilltap-core/src/photos/user_gallery_service.rs`
  (new) + `crates/quilltap-core/src/api/photos.rs` (new, or the images
  read's natural home), `crates/quilltap-web/src/photos_routes.rs` (new);
  harness: its oracle cases, differential suites, the
  `photos-{main,mount}.db` family + generator. SPA:
  `apps/web/src/app/screens/photos/**` (new),
  `apps/web/src/app/images/**` EXCEPT `generate-image-dialog.ts` and the
  new P4.9b files (P4.9a owns `photo-gallery-modal.ts`, `image-modal.ts`,
  the new deep-modal files, `image-urls.ts` if needed),
  `screens/characters/view/tabs/gallery-tab.ts`, its §1 block, its own
  e2e spec file. Version bumps: quilltap-core, quilltap-harness,
  quilltap-web (+Cargo.lock), SPA `package.json`.
- **P4.9c:** Rust: the user-profile + data-dir dispatch/service files
  (new), `crates/quilltap-web/src/profile_routes.rs` (new),
  `crates/quilltap-web/src/health.rs` (the version field — exclusively);
  harness: its oracle cases + differential suites (+ fixture additions if
  the existing families lack what profile needs). SPA:
  `apps/web/src/app/screens/about/**`, `screens/profile/**` (new),
  `apps/web/src/app/shell/user-menu.ts` (new) and `shell/shell.ts`
  (user-menu mount + the F4 stale-docstring fix at `shell.ts:19-20`; NOT
  the photos flip), `apps/web/src/app/core/core-transport.ts` (the §3
  version carry — exclusively), its §1 blocks, its own e2e spec file.
  Version bumps: quilltap-core, quilltap-harness, quilltap-web
  (+Cargo.lock), SPA (+quilltap-host ONLY if data-dir needs a host seam —
  flag it loudly if so).
- **P4.9b:** SPA only: `apps/web/src/app/screens/generate-image/**`
  (new), `apps/web/src/app/images/standalone-generate-image-dialog.ts`
  (new), `apps/web/src/app/images/image-profile-picker.ts` (new shared)
  and/or `screens/new-chat/image-profile-picker.ts` (generalize),
  `images/generate-image-dialog.ts` (docstring/deferral-note edits only),
  `screens/salon/salon-conversation.ts` + the composer file it must touch
  for the single gutter opener button, `screens/home/quick-actions-row.ts`,
  `screens/home/home.spec.ts` (exclusively — no other lane edits it),
  its §1 block, its own e2e spec file. Version bump: SPA `package.json`.
- **P4.9d:** SPA only: `apps/web/src/app/quick-hide/**` (new: service +
  menu section + specs), `screens/salon/salon-list.ts` (+ refactoring
  `screens/salon/autonomous-visibility.ts` onto the shared service),
  `screens/home/recent-chats-section.ts`,
  `screens/home/characters-section.ts`,
  `screens/characters/list/characters-list.ts`,
  `screens/characters/view/character-detail.ts` + its conversations tab
  file, the Prospero chats/characters card files,
  `screens/settings/appearance/**` (the global tags tab + its
  quickHide column + ThemePreviewModal — RE-BINNED here from p4.9c
  because this lane owns the appearance tab; the unifier records the
  re-bin in `m6-screen-parity.md`), its own e2e spec file(s). MUST NOT
  touch `salon-conversation.ts` (v4 `SalonView.tsx:707`'s in-chat
  quick-hide consumer feeds the unported `p4.9e*` dialog family — defer
  loudly) or `home.spec.ts`. Version bump: SPA `package.json`.
- **The round's KNOWN Rust conflict set:** P4.9a and P4.9c both add
  ADDITIVE arms to `crates/quilltap-core/src/api/types.rs`,
  `api/engine.rs`, `api/mod.rs`, and `crates/quilltap-web/src/lib.rs`
  (route registration). Additions only, §3-pinned; the unifier reconciles
  (temp-branch cherry-pick + one full gate — the parallel-round
  reconciliation memory). No other Rust file is shared.
- **Append-only for everyone:** `docs/CHANGELOG.md`,
  `docs/developer/porting/status-log.md` (union-merge at unification).
  SPA version bumps union at unify — the unifier RECOUNTS (identical
  bumps merge silently — the P4.6aw/ax memory).
- **Nobody** edits `CLAUDE.md`, `phase-4.md`, `m6-screen-parity.md`, or a
  sibling lane's files; the unifier updates the phase docs.

## Verification gate (run before declaring done)

- Rust untouched: `cargo test --workspace` should match main's baseline
  (run it once to prove no accidental drift); fmt/clippy clean.
- `ng test` green (from `apps/web`); `ng build` clean.
- Full Playwright green, run ALONE on port 4319 from `apps/web` with THIS
  worktree's Rust server binaries; the quick-hide beat live in its
  standalone form; the menu-walk variant marked ACTIVATE-AT-UNIFY.
- No `[value]` binding on any `<select>` with async options — bind
  `[selected]` per option (the standing dogfood-#6 rule).
- Every commit follows `.claude/commands/commit.md` (SPA version bump;
  CHANGELOG entry; status-log append at lane close).
