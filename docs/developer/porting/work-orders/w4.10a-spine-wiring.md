# Work order: W4.10a — the spine wiring pass (the three deferred composition-point closures)

**Size: one Opus session.** No new v4 surface is ported here — every piece is
already ported and differential-verified; this pass closes the three deferred
seams at the `process_message` composition point and regenerates the
spine-owned orchestrator oracle with the proof cases. **This order owns the
spine files and the orchestrator corpus this round** (the four spine files:
`services/orchestrator.rs`, `services/message_finalizer.rs`,
`services/message_context.rs`, `services/build_context.rs`; plus
`tools/executor.rs`, `tools/ask_carina.rs`, and the orchestrator
fixture/corpus/oracle). Baseline: v4 HEAD `6b6e39ad` — run the drift check
first (`git -C ~/source/quilltap-server log --oneline -3`); if HEAD moved,
STOP and flag.

Read first: `docs/developer/porting/work-orders/w4.4b-file-attachment.md`
(the two inherited handoffs), `w4.5-carina-query.md` items 8–9 (the spine
closure), CLAUDE.md's Round-4-remainder paragraph (the standing follow-ups),
and the memory notes `[[tier3-completion-oracle]]`,
`[[round3-unification-spine-wiring]]` (the real-registry oracle-regen recipe),
`[[oracle-jest-testtimeout]]`.

## Current-state facts (surveyed 2026-07-08)

- `ProcessMessageInput.model_supports_native_tools: bool` at
  `orchestrator.rs:358`; consumed at `:1166` (into `BuildToolsInput`), read
  back at `:1172`, feeds `check_resolved_tool_mode` at `:1205`. The courier
  arm (`:1134`) hardcodes `false`.
- `PricingFetcher::check_model_supports_tools` at
  `services/pricing_fetcher/mod.rs:625` — **sync** `&self` method
  `(provider, model_name, now_ms, ctx: &PricingContext) -> bool`.
  `PricingContext { profiles: Vec<PricingProfile>, api_keys: HashMap }` at
  `:125`. Only OPENROUTER touches the live cache/fetch seam; every other
  provider answers from `fallback_pricing` (deterministic, no ctx needed).
- `ApiKeyResolver` trait (sync `resolve(&self, api_key_id, user_id) ->
  Option<String>`) + `NoApiKeys` + `ConnApiKeys<'c>(&rusqlite::Connection)`
  all in `services/dangerous_content/provider_routing.rs:63–93`. The
  docstring at `:80` names this wiring. The orchestrator consumes the
  resolver only through `DangerContentRouter<A>` (`:349`), injected as
  `OrchestratorDeps.danger_router` (`orchestrator.rs:398`); the harness
  currently injects `CannedApiKeys` (orchestrator differential `:314`,
  `:521-525`) mirroring the oracle's `findApiKeyByIdAndUserId` monkey-patch.
- `RunCarinaQuery` (async RPITIT, `&mut self`) at `carina_runner.rs:168`;
  injected as the generic `OrchestratorDeps.carina_query: &'a mut CARQ`
  (`orchestrator.rs:406`) and as the finalizer's `carina_query: &mut Q`
  param (`message_finalizer.rs:613`). Harness injects `NoCarina`
  (orchestrator differential `:1004`). The real engine is the free async fn
  `carina_query::run_carina_query(deps: &CarinaQueryDeps<...>, opts)` at
  `carina_query.rs:215`; `CarinaQueryDeps` (`:152`) needs db / embedding /
  streaming / tool_runner / tool_detector / sink / brahma /
  `model_supports_native_tools` / `now_ms: f64`.
- `ask_carina` has NO dispatch arm in `tools/executor.rs` (recognized-names
  list `:144` + comment `:204-207` only → loud fallback). The handler
  `execute_ask_carina<Q, P>` at `tools/ask_carina.rs:93` takes
  `run_query: &mut Q` — but `ToolRunner::run` is `&self`, so the dispatch
  row needs interior mutability (the `Arc<Mutex<…>>`
  pendingWardrobeAnnouncements precedent) or the erased-runner pattern
  (`ImageGenerationRunner` precedent, an `Arc<dyn …>` boxed-future erasure).

## Scope (in order)

1. **`model_supports_native_tools` sourcing.** Read v4's call site first
   (`checkModelSupportsTools(provider, model, userId)` inside
   `orchestrator.service.ts`) and reproduce its position + inputs: drop the
   `ProcessMessageInput.model_supports_native_tools` field and compute it in
   the spine via the real `check_model_supports_tools`. `OrchestratorDeps`
   gains pricing-fetch access (a `&PricingFetcher<F>` field with a new
   `PF: PricingFetch` generic, or an equivalent — keep the fetch itself a
   seam; the harness injects a canned/never-called fetch). Build the
   `PricingContext` the way v4's site does (its data is only exercised on
   the OPENROUTER path; the corpus's ANTHROPIC cases answer from the
   fallback table identically both sides). The courier arm's hardcoded
   `false` stays (v4 skips the tool build there). The oracle side: prefer
   UN-mocking v4's `checkModelSupportsTools` where the corpus providers
   resolve from static data; if the OPENROUTER case (`nonanthropic_scene`)
   forces a live fetch, mock at the FETCH level (`global.fetch` for the
   openrouter pricing URL) and give the Rust `PricingFetch` seam the same
   canned payload — never re-mock the leaf on one side only.
   `CarinaQueryDeps.model_supports_native_tools` (`carina_query.rs:178`)
   gets the spine-computed value for the answerer's resolved profile the
   way v4's carina engine obtains it — read v4's carina site to confirm
   (per-profile recompute vs pass-through) and match.
2. **The real ApiKeyResolver.** The differential composition swaps
   `CannedApiKeys` for the real DB-backed resolution: seed the fixture's
   `api_keys` table with the SYNTHETIC keys the corpus uses (never real
   keys — the column is plaintext), un-monkey-patch
   `findApiKeyByIdAndUserId` on the v4 oracle side (v4 reads its own seeded
   rows), and construct `DangerContentRouter::new(db, <real resolver>)` on
   the Rust side. `ConnApiKeys<'c>` wants a connection reference; if the
   composition needs an owned form, add a small additive `DbApiKeys(Db)`
   impl beside it in `provider_routing.rs` (reads via the read pool /
   `find_by_id_and_user_id`) — additive only, don't touch the existing
   impls. This closes the W4.7d→W4.4b handoff: the key material for the
   danger reroute now comes off the real table end-to-end.
3. **The carina spine closure** (W4.5 item 9). (a) A `RealCarinaQuery`
   adapter struct implementing the frozen `RunCarinaQuery` trait over the
   real engine — it owns/holds the `CarinaQueryDeps` bundle and calls
   `run_carina_query` (the `&mut self`-over-`&deps` mismatch is fine; the
   engine is `&`-only). The Brahma seam stays `UnavailableBrahmaConsole` —
   W4.5b ports the real console IN PARALLEL this round; do NOT wire or
   modify it (the swap is a unification one-liner). (b) The `ask_carina`
   dispatch row in `BuiltInToolRunner`: an ERASED carina-runner seam field
   (defaulted to the current loud-fallback behavior so every existing
   construction site compiles unchanged — **additive builder field only**,
   W4.5b constructs `BuiltInToolRunner` in parallel), dispatching to
   `execute_ask_carina`; the `emitCarinaAnswer`/`onPosted` tool-context
   slot closes here (the engine owns the post + emit — verify against v4's
   ask-carina handler wiring). (c) Construct the real adapter at the
   orchestrator/finalizer composition points in the differential (replacing
   `NoCarina`), with the model boundaries canned per the established
   carina-query recipe.
4. **The live corpus cases + oracle regen.** Extend the orchestrator corpus
   with (a) a live `@Name:` carina-markup case (the finalizer's markup
   runner now hits the REAL engine — inner canned stream key recorded by
   the oracle; keep the recall SMALL per the W4.5 chars-per-token corpus
   constraint) and (b) a live `ask_carina` tool-call case (the primary
   canned stream emits the tool call; the dispatch row runs the real
   engine). Regenerate the orchestrator oracle with the real provider
   registry initialized (the `[[round3-unification-spine-wiring]]` recipe)
   and re-run the full differential. Keep the live cases on a REGULAR
   character answerer (not Brahma — the console is W4.5b's).
5. **`tool_dispatch_equivalence`**: add an `ask_carina` row with the same
   canned seam on both sides (the dispatcher row is now real; v4's
   dispatcher path is drivable with `runCarinaQuery` mocked to the canned
   result).
6. Re-verify the untouched-behavior differentials that share these files:
   `message_finalizer_tier3`, `carina_runner_tier3`, `mail_carina_tools`,
   `tool_build_equivalence`, `regenerate_swipe_tier3` (re-run, regenerate
   only if a signature change forces it).

## Explicitly OUT of scope

- The `with_logging` spine wiring + an orchestrator `llm_logs` dump —
  coupled to W4.10b's primary-stream regen; a post-round step. The
  orchestrator oracle KEEPS its `logLLMCall` mock.
- The real Brahma console (W4.5b, parallel) — keep the default seam.
- W4.10b's seven differentials and their oracles/fixtures; `gatekeeper.rs`;
  `cheap_llm_exec.rs`; `services/carina_query.rs` internals (constructing
  it is fine; changing it is not); `services/brahma_console*` (doesn't
  exist yet — don't create it).

## Watch-outs

- **Parallel contention:** W4.5b (Brahma) and W4.10b (logging regens) run
  in parallel. Your `tools/executor.rs` changes must be ADDITIVE with
  defaults (W4.5b constructs `BuiltInToolRunner` against the existing
  API). Do not touch their files (listed above); they do not touch yours.
- The `RunCarinaQuery` trait, `RunCarinaQueryOptions`, `CarinaResult`, and
  `run_carina_query`'s behavior are FROZEN — adapter + construction only.
- Dropping `ProcessMessageInput.model_supports_native_tools` ripples into
  every harness construction of that struct — mechanical, but check the
  orchestrator self-tests too (`orchestrator.rs:2841+`).
- The live `ask_carina` case's canned inner stream: the engine's
  system-prompt bytes are proven by the recorded canned key — a canned-miss
  IS the diff signal (the memory-processor precedent). Budget corpus time
  for iterating on that.
- jest: `--watchman=false --testTimeout=120000`, Node 24; run the oracle
  from the v4 checkout with `--roots` into the MAIN v5 tree (jest ignores
  `.claude/` worktree paths — stage a copy to /tmp if needed, see
  `[[w4-4b-file-attachment-gotchas]]`).

## Deliverables checklist

- [ ] `model_supports_native_tools` sourced in-spine from the real
      `check_model_supports_tools` (field dropped; fetch stays a seam).
- [ ] Real DB-backed ApiKeyResolver at the danger-router composition
      (fixture-seeded synthetic keys, oracle un-monkey-patched).
- [ ] `RealCarinaQuery` adapter + `ask_carina` dispatch row (additive) +
      real engine at the orchestrator/finalizer composition points.
- [ ] Orchestrator corpus: live `@Name:` + live `ask_carina` cases; oracle
      regenerated with the real registry; differential green.
- [ ] `tool_dispatch_equivalence` ask_carina row; shared-file differentials
      re-verified green.
- [ ] Full workspace `cargo test` / `clippy -D warnings` (default AND
      `--features native-transport`) / `fmt --check`.
- [ ] CHANGELOG, versions (never `quilltap-sqlite3mc-sys`), CLAUDE.md
      (the three handoffs CLOSED; the with_logging/orchestrator-dump item
      restated as the post-round step), memory note.
- [ ] Commit per `.claude/commands/commit.md`, branch `w4.10a-spine-wiring`.

**STOP rules:** fix the port, never the normalization; no stubs/TODOs; if a
closure needs a change to a frozen trait or a W4.5b/W4.10b-owned file, STOP
and flag instead of touching it; synthetic keys only; v4 drift → STOP.
