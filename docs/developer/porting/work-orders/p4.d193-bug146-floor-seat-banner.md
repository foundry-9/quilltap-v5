# Work order P4.D193 — bug 146 whole: `resolveFloorSeatId` as a pure leaf on BOTH sides (the Rust twin in `participant_filters.rs` under a NEW tier-1 family driving v4's REAL export; the client twin in `turn-order.ts` with v4's seven cases as its parity spec), and the Salon's Skip banner re-keyed on the FLOOR — the third sentence (`switch the speaker to them`) behind `composerElsewhere`, the banner / must-speak guard / Skip POST all following the floor seat instead of the composer's, with a deterministic two-user-seat e2e beat — v4 `2075242f9`'s `lib/chat/turn-manager/utils.ts` + `index.ts` + `app/salon/[id]/SalonView.tsx` hunks, from `main`

**UNIFIED on `main` (2026-09-16) — CLOSED WHOLE.** The §3 review found NO blocking issue. **At the wires:** v4's UNCOMMITTED eighth `resolveFloorSeatId` case (the second sighting, 2026-09-16 — the composer holding an impersonated seat while the floor is the owner's) was added to the tsx corpus (53 → 54 rows, regenerated at the target pin) and to the SPA parity spec, so the "seven cases case-for-case" claim does not go stale when v4 commits it; the same branch is v4-1's, so no port risk. Recorded: the Rust doc's "exactly as `is_user_driven_seat`" analogy overstates (that twin HAS server callers; this one has none, as in v4); the parity spec's factory sets three fields where v4's sets thirteen (v4's function reads only those three). Bug 147 ("the chat GET omits the rotation") was found IN FLIGHT in v4's dirty tree by this review — v5's `applyTurnResponse` already reads `state.cycleOrder`, so its client half converges; the P4.D171 both-directions chat-GET pin trips at the next move by design (drift ledger §1). Round record: `status-log.md`.

**Status: CLOSED — every Tier-1 and Tier-2 item landed; NOTHING left OPEN.**
Ordered 2026-09-16, the `2075242f9` bug-145/146 drift catch-up + maintenance
round (P4.D192 ∥ P4.D193 ∥ P4.D194 ∥ P4.89 ∥ P4.90). Lane branch
`claude/floor-seat-banner-bug-848d19`, five commits off `main` `e9173924`. The
lane record is in `status-log.md`; the unifier rewrites this header at
unification.

**Landed:** (1) `participant_filters::resolve_floor_seat_id` with v4's three
prose-invisible shapes; (2) the NEW tier-1 `floor_seat_equivalence` — 52 floor
rows + a `reexport` row pinning `index.ts:83`, over a tsx oracle driving v4's
REAL function at the target pin; (3) the `turn-order.ts` client twin with v4's
own `floor-seat.test.ts` transcribed case-for-case plus the extra shapes; (4)
the banner re-keyed on the floor (`bannerSeat` + `composerElsewhere` + the
fourth sentence + Skip following `bannerSeat()`), five component specs each
asserting the DISPATCH BODY; (5) the live e2e beat
`salon-floor-seat-flow.spec.ts`. Tier 2: item 6 measured and deliberately left
alone with its reasoning recorded; item 7's neutrality legs run at BOTH pins,
byte-identical. Tier 3: no server behaviour changed and `core-contract.ts` is
untouched (the tripwire is empty).

**Three of the order's own premises were REFUTED by measurement** (each recorded
in the lane record, in code comments, and in the changelog):

1. "`find` returns the FIRST id match, so duplicate ids take the first" — it
   scans for the first WHOLE-predicate match. The spec written from the order's
   wording went red; mutation M5 now measures the real rule.
2. "The fair-rotation rule hands the floor from one user seat to the other with
   NO model call" — not with an active LLM seat in the room; the floor is a
   weighted coin flip between the other user seat and the LLM.
3. "The reload case" reaches the fourth sentence — on v5 it does not; the
   turn-follow's latch is a plain instance field, so a reload re-moves the
   composer onto the floor. Only the deliberate speaker pick parts them.

Also refuted, and worth knowing for any future turn beat: the manual turn queue
is invisible to `chatTurnAction query` (`handle_turn_action` rebuilds its
`TurnState` from history and never reads the persisted `turnQueue`).

**One out-of-order finding, fixed in the lane:** the new harness family tripped
P4.68's `participant_status_home_guard` census at the full-workspace gate and
was routed through `chat_predicates::participant_status_from_str`.

## Preamble

- **v4 oracle baseline: `ffb6b3119`.** This lane absorbs the code half of
  **`2075242f9`** ("fix(salon): Skip passes the turn that is outstanding,
  not the composer's seat (bug 146)", 2026-09-15): `lib/chat/turn-manager/
  utils.ts` (+45 — `resolveFloorSeatId`), `lib/chat/turn-manager/index.ts`
  (+1, the re-export), `app/salon/[id]/SalonView.tsx` (+37/−10, the Skip
  banner block) and the NEW `__tests__/unit/lib/chat/turn-manager/floor-
  seat.test.ts` (+84 — the seven cases you transcribe). Its `help/chat-
  turn-manager.md` hunk is P4.D194's. The ledger row is `ORDERED(P4.D193,
  P4.D194)`. Spec reads: `git show 2075242f9 -- lib/chat/turn-manager/
  utils.ts lib/chat/turn-manager/index.ts 'app/salon/[id]/SalonView.tsx'
  __tests__/unit/lib/chat/turn-manager/floor-seat.test.ts` and the
  post-commit files — **never the commit message** (§R.4). ⚠ The message
  says "Skip passes the turn that is outstanding" — the hunks say the
  banner's SEAT is resolved by a new predicate and the existing Skip
  handler is called with THAT seat's id; server acceptance of a skip is
  unchanged (no `lib/` behaviour beyond the export).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** Pin **`/tmp/qt-v4-pin-p4d193-
  2075242f9`** (the new tier-1 oracle imports `resolveFloorSeatId`, which
  does not exist at the baseline — a baseline-pinned run fails to import,
  which is your pin verification). The baseline pin is needed only if you
  re-run `turn_pause_filters_equivalence` / `turn_predicates_equivalence`
  as neutrality legs (they should be byte-identical across pins).
- **Sibling lanes:** P4.D192 (the collapse heal), P4.D194 (CLI + help —
  owns `help/chat-turn-manager.md`), P4.89 (brahma fixtures), P4.90
  (orchestrator + chat_create). None touches `participant_filters.rs`, the
  turn-manager oracles, or the SPA. The SPA is YOURS ALONE this round.
- **Worktree:** `git worktree add <path> -b claude/p4-d193-bug146-floor-
  seat-<hash> main`; `CARGO_INCREMENTAL=0`; `npm ci` in `apps/web` (never
  `cp` a sibling's `node_modules` — `spa-worktree-node-modules`).
- **Read before the work:** `crates/quilltap-core/src/participant_filters.
  rs` (`is_user_driven_seat` `:55`, `find_active_user_participant` `:91`,
  `is_all_llm_chat` `:198`, the `ParticipantView` shape) and `chat_
  predicates.rs:40` (`is_participant_present` = Active | Silent);
  `harness/oracle/cases/turn-pause-filters.ts` (the tsx oracle that drives
  `isUserDrivenSeat` / `isAllLLMChat` — the closest recipe: row kinds,
  `asParts`, the `npx tsx … > /tmp/oracle-*.ndjson` shape) and its consumer
  `crates/quilltap-harness/tests/turn_pause_filters_equivalence.rs`;
  `apps/web/src/app/chat/turn-order.ts:340-420` (`isUserDrivenSeat`,
  `findUserParticipant`, `findActiveUserParticipant` — the client twins
  and their doc comments naming v4 + the core authority) and `turn-order.
  spec.ts:520-560` (the parity block "case-for-case from v4 … `isUserDriven
  Seat` arms"); `apps/web/src/app/chat/turn-controls.ts` whole (the
  presentational banner: inputs `userTurnName` / `mustSpeak` / `isSeatsTurn`,
  `bannerText` = v4's three-way at `SalonView.tsx:1499-1506`, `skipUserTurn`
  emits void); `apps/web/src/app/screens/salon/salon-conversation.ts:2236-
  2360` (`userTurnName`, **`bannerSeat`** — v4's three gates over
  `speakingSeat()`, `hasAnyActiveCharacter`, `isSeatsTurn`, `mustSpeak`
  over the banner's seat) and `:2405-2460` (`onSkipUserTurn` — resolves
  `speakingSeat()` and POSTs `chatTurnAction { action: 'skipUserTurn',
  participantId }`), the binding block `:431-436`; `salon-turn-controls.
  spec.ts:325-350, :460-545` (the banner sentences pinned today; the
  `stubClient` shape with `query: { nextSpeakerId, nextSpeakerControlledBy
  }`); `apps/web/e2e/salon-dialogs-flow.spec.ts:49-75` (`readOwnerSeatAnd
  Turn` — reads `chatGet` + `chatTurnAction query` from the server, the
  independent source the banner beat pins against) and `:395-420` (the
  P4.D161 banner beat); `apps/web/e2e/composer-active-seat.spec.ts:1-70`
  (how a beat builds a chat with a SECOND user-driven seat live: New Chat,
  two characters, the second flipped to "Play As (User)"); memory notes
  `e2e-assertion-can-read-the-pre-click-state`, `e2e-sends-into-a-shared-
  chat-trip-a-title-checkpoint` (build your own chat — never send into
  Group Expedition), `spa-has-no-zod`, `ng-test-defaults-to-watch-mode`,
  `an-assertion-on-the-oracle-cannot-catch-a-v5-regression`.

## Mandate

Port `resolveFloorSeatId` twice — the Rust twin in `participant_filters.rs`
(the differential-proven authority, exactly as `is_user_driven_seat` is for
its client mirror) under a NEW tier-1 family whose tsx oracle imports v4's
REAL `resolveFloorSeatId` from `@/lib/chat/turn-manager`, and the client
twin in `turn-order.ts` with v4's `floor-seat.test.ts` transcribed case-for-
case as its parity spec — then re-key the Salon's Skip banner on the FLOOR:
`bannerSeat` resolves the floor seat (rotation wins when it names a present
seat the human drives; the composer's seat only off-turn), the third
sentence `${name}'s turn — switch the speaker to them to type, or skip to
let someone else respond.` renders behind `composerElsewhere = isSeatsTurn
&& speakingSeat?.id !== seat.id` in v4's four-way order (mustSpeak →
composerElsewhere → isSeatsTurn → speaking-as), and Skip POSTs the FLOOR
seat's id (v5's parent resolves "the same seat it drew the banner from" —
today that is `speakingSeat()`; it becomes `bannerSeat()`). Bug 123's
off-turn affordance and bug 44's overlay are preserved by construction (the
predicate falls back to the composer's seat; it reads `isUserDrivenSeat`
over the overlay). Prove the client half live with a deterministic beat over
a chat with TWO user-driven seats (the fair-rotation rule hands the floor
from one to the other with NO model call). This lane does NOT touch
`help/**`, `core-contract.ts`, the server's turn handlers, or any sibling's
files.

## Survey-verified starting points (fresh survey 2026-09-16 at `2075242f9` and on `main` `4572659b` — do not re-derive)

### v4

- **`resolveFloorSeatId(nextSpeakerId: string | null | undefined,
  participants: ChatParticipantBase[], impersonatingParticipantIds?:
  readonly string[] | null, speakingSeatId?: string | null): string |
  null`** (`utils.ts:219-262`): `if (nextSpeakerId) { onFloor =
  participants.find(p => p.id === nextSpeakerId && isParticipantPresent(p.
  status) && isUserDrivenSeat(p, impersonatingParticipantIds)); if
  (onFloor) return onFloor.id; } return speakingSeatId ?? null;` —
  NOTE three shapes the doc prose does not say: (1) `speakingSeatId` is
  returned VERBATIM, un-validated (a seat not in the room, or an LLM seat,
  comes back as given — the CALLER gates with `isUserDrivenSeat`
  afterwards); (2) an EMPTY-string `nextSpeakerId` is falsy → straight to
  the fallback; (3) `find` returns the FIRST id match — duplicate ids take
  the first. Pin all three in the corpus.
- **`index.ts:83`** — `resolveFloorSeatId` added to the utils re-export
  block (between `isUserDrivenSeat` and `findUserControlledParticipants`).
- **`SalonView.tsx:1531-1596`**: `floorSeatId = resolveFloorSeatId(
  turnSelectionResult?.nextSpeakerId, participantsWithImpersonation.
  participantsAsBase, impersonation.impersonatingParticipantIds,
  speakingSeat?.id ?? null)`; `seat = floorSeatId ? participantData.find
  (pp => pp.id === floorSeatId) ?? null : null`; the existing gate `if
  (!seat || !isUserDrivenSeat({ id: seat.id, controlledBy: seat.
  controlledBy ?? 'llm' }, impersonatingParticipantIds)) return null` is
  UNCHANGED and now runs over the resolved seat; `name`, `isSeatsTurn =
  nextSpeakerId === seat.id` unchanged; NEW `composerElsewhere = isSeatsTurn
  && speakingSeat?.id !== seat.id`; the must-speak block unchanged (it
  already reads `seat`); the sentence four-way: `mustSpeak ? 'Everyone else
  has passed — it falls to ${name} to say something.' : composerElsewhere ?
  '${name}'s turn — switch the speaker to them to type, or skip to let
  someone else respond.' : isSeatsTurn ? '${name}'s turn — type as them,
  or skip to let someone else respond.' : 'Speaking as ${name} — type, or
  skip to let someone else take the floor.'`; the Skip button's handler
  still receives `seat.id` (unchanged code, changed input).
- **v4's `floor-seat.test.ts`** — seven cases over `room = [wahno(llm,
  order 2), charlie(user, order 1), helene(user, order 5)]`: (1) prefers
  the rotation's seat (charlie) over the composer's (helene); (2) keeps
  the composer's seat when the floor is an LLM's (wahno → helene); (3)
  `null`/`undefined` nextSpeakerId → the composer's seat; (4) agreement
  when both name helene; (5) the overlay: lorian (llm) on the floor with
  `['lorian']` impersonating → lorian; with `[]` → charlie; (6) a
  `status: 'removed', isActive: false` user seat on the floor → falls back
  to helene; (7) `null` when neither names a seat (wahno floor + null
  composer; null + null).
- **`help/chat-turn-manager.md` (+3)** — P4.D194's.

### v5

- `participant_filters.rs` — `is_user_driven_seat(participant, impersonating
  _ids)` `:55`, `find_active_user_participant` `:91` (the same
  selected-then-fallback shape — `resolve_floor_seat_id` is its sibling and
  should read like it); `ParticipantView` carries `id`, `controlled_by`,
  `status`. `chat_predicates::is_participant_present(ParticipantStatus)`.
- The Rust twin has NO production caller (v4's consumer is the client) —
  it is a `pub fn` in a lib crate (no dead-code lint) whose purpose is to
  be the differential-proven authority the client mirror cites, exactly
  as the P4.D56/P4.D58 pair did for `is_user_driven_seat`. Say so in its
  doc comment.
- `turn-order.ts:357` `isUserDrivenSeat<Pick<SeatView,'id'|'controlledBy'>>`,
  `:378` `findUserParticipant`, `:403` `findActiveUserParticipant` — the
  new twin sits after `:420` with the same generic `SeatView` bound
  (`status` + `controlledBy` + `id`).
- `salon-conversation.ts:2254` `userTurnName`, `:2273-2295` `bannerSeat`
  (gates: `busy()`, `hasAnyActiveCharacter()`, `speakingSeat()` +
  `isUserDrivenSeat`), `:2316` `isSeatsTurn` (`effectiveNextSpeakerId() ===
  seat.id`), `:2328` `mustSpeak` (over `bannerSeat()` — follows
  automatically), `:2405-2460` `onSkipUserTurn` (reads `speakingSeat()` —
  MUST become the banner's seat), the binding block `:431-436` (`[userTurn
  Name]`, `[mustSpeak]`, `[isSeatsTurn]`, `(skipUserTurn)`). The
  participants list the resolver needs as `ChatParticipantBase[]`: the
  chat's `participants` (each carries `id`, `controlledBy`, `status`);
  `impersonatingIds()` is the overlay list; `effectiveNextSpeakerId()` is
  the rotation's answer (server-authoritative + the P4.D60 `turnOverride`
  layered above it — read its doc comment before deciding which to pass;
  v4 passes `turnSelectionResult?.nextSpeakerId`, the server's answer, so
  the v5 analogue is whatever `isSeatsTurn` already compares against).
- `turn-controls.ts` — the banner is presentational; add a fourth input
  `composerElsewhere` (boolean) and the fourth arm in v4's order; the
  component doc cites `SalonView.tsx:1499-1506` — update the line span to
  the target's `:1588-1596`.
- `salon-turn-controls.spec.ts:346, :472, :483, :539` pin the two existing
  sentences; `stubClient` answers `query: { nextSpeakerId, nextSpeaker
  ControlledBy }`. The third sentence needs a stub whose query names the
  OTHER user seat while the composer's active speaker is this one.
- `salon-dialogs-flow.spec.ts:395-420` pins the banner against
  `readOwnerSeatAndTurn` (ONE user seat in that fixture chat — the third
  sentence is unreachable there; the assertion need not change unless the
  helper's `isItsTurn` logic starts naming a seat the composer is not on).
- `composer-active-seat.spec.ts` — the recipe for building a two-user-seat
  chat live (roster index 0 = LLM seat auto-seeded, index 1 flipped to
  "Play As (User)"); KEEP the LLM seat active for your beat (that spec
  sets it Absent; you do not).
- The e2e fair-rotation fact to lean on (v4 `help/chat-turn-manager.md`,
  the new bullet: "a message from one of them hands the floor straight to
  the other rather than summoning an LLM") — v5's `select_next_speaker_
  after_user_message` (P4.D60) is the server half; MEASURE it before
  writing the beat: send as seat A in a chat with user seats A and B and
  one LLM seat, then `chatTurnAction query` → `nextSpeakerId` must be B
  with ZERO `llm_logs`/stream activity. If the measurement disagrees, the
  beat's discriminator changes (record what you found).

## Tiered deliverables

### Tier 1 — must land

1. **`participant_filters::resolve_floor_seat_id(next_speaker_id:
   Option<&str>, participants: &[ParticipantView], impersonating_ids:
   Option<&[String]>, speaking_seat_id: Option<&str>) -> Option<String>`** —
   v4's shape exactly (survey), incl. the three prose-invisible shapes
   (verbatim fallback; empty-string falsy → JS truthiness, so `Some("")`
   falls through; first-match on duplicate ids). Doc comment: v4's why
   (the two questions), the sha, and "the client mirror in `turn-order.ts`
   cites this as the authority".
2. **NEW tier-1 family `floor_seat_equivalence`**: `harness/oracle/cases/
   floor-seat.ts` (tsx, `@/lib/chat/turn-manager` import — the re-export
   is the surface v4's consumer uses) emitting rows `{ kind: 'floor', id,
   nextSpeakerId: string|null, nextSpeakerUndefined?: true, participants:
   [{ id, controlledBy, status }], impersonating: string[]|null,
   speakingSeatId: string|null, out: string|null }` over v4's seven cases
   PLUS: the empty-string next id; a `silent` user seat on the floor (present
   → wins); an `absent` user seat on the floor (not present → fallback);
   the overlay naming a seat that is ALSO `controlledBy: 'user'`;
   `impersonating: null` vs `[]` vs `undefined`; a `speakingSeatId` not in
   the room (returned verbatim); a `speakingSeatId` naming an LLM seat
   (returned verbatim — the caller gates); duplicate ids (first wins);
   an empty room; ≥ 40 rows. `crates/quilltap-harness/tests/floor_seat_
   equivalence.rs` diffs `out` exactly (tier 1). Mutation proofs: M1 drop
   the presence conjunct → the absent/removed rows red; M2 drop the
   user-driven conjunct → case (2)/(5b) red; M3 validate the fallback
   against the room → the not-in-room row red; M4 treat `Some("")` as a
   name → the empty-string row red.
3. **The client twin** `resolveFloorSeatId` in `turn-order.ts` (same
   signature as v4's, generic over `SeatView`), doc comment citing v4 and
   the core authority; `turn-order.spec.ts` gains v4's `floor-seat.test.ts`
   case-for-case (seven `it`s, the `seat()` factory with v4's field set —
   note v4's factory sets `status: 'active'` + `isActive: true`; the
   removed case sets both) PLUS the four prose-invisible rows from item 2
   as a `resolveFloorSeatId — shapes v4's suite does not ask` block.
4. **The banner re-keyed on the floor.** `bannerSeat` = the three gates
   with gate 3 over the RESOLVED seat: `floorSeatId = resolveFloorSeatId(
   <the rotation's next id>, participants, impersonatingIds(), speakingSeat
   ()?.id ?? null)` → look up the `ParticipantDetail` → the existing
   `isUserDrivenSeat` gate. NEW `composerElsewhere = computed(() => isSeats
   Turn() && speakingSeat()?.id !== bannerSeat()?.id)`; `turn-controls.ts`
   gains the `composerElsewhere` input and the fourth sentence in v4's
   order; the binding block passes it; `onSkipUserTurn` resolves
   `bannerSeat()` (the doc comment already says "the SAME seat it drew the
   banner from" — make the code say it). `mustSpeak` unchanged (already over
   `bannerSeat()`). Update the component/prop doc comments' v4 line spans
   to the target. Specs in `salon-turn-controls.spec.ts`: (a) two user
   seats, query names B, active speaker A → the THIRD sentence names B and
   Skip POSTs `participantId: B` (assert on the dispatch call); (b) query
   names the LLM seat, speaker A → the speaking-as sentence names A and
   Skip POSTs A (bug 123 preserved); (c) query names A, speaker A → the
   second sentence (unchanged pin); (d) the overlay: query names an
   impersonated LLM seat L with `impersonatingParticipantIds: ['L']`,
   speaker A → the third sentence names L, Skip POSTs L; (e) must-speak
   over the floor seat wins over the third sentence. Mutation proofs: M5
   resolve Skip from `speakingSeat()` again → (a) red on the POST body; M6
   drop `composerElsewhere` from the four-way → (a) red on the sentence;
   M7 swap the arm order (isSeatsTurn before composerElsewhere) → (a) red.
5. **The e2e beat** — NEW `apps/web/e2e/salon-floor-seat-flow.spec.ts`
   (serial): build a chat live with an LLM seat + TWO user-driven seats
   (A = the second roster character flipped to "Play As (User)"; B = a
   third, likewise); send one line as A; read the server's `chatTurnAction
   query` — `nextSpeakerId` must be B (the measured fair-rotation fact; if
   the turn-follow has moved the composer to B, the banner reads the
   SECOND sentence naming B — assert it); then pick A in the speaker
   selector (the "deliberate same-turn SpeakerSelector choice" the v4
   comment names) → the banner must read the THIRD sentence naming B; click
   Skip and pin the dispatch body (`page.waitForRequest` on `/api/dispatch`
   whose JSON has `action: 'skipUserTurn'` — ARM the wait BEFORE the click)
   `participantId === B`; then `chatTurnAction query` again → the floor
   has moved off B. Second arm: reload the page (the "reload case") with
   the composer restored to A → the third sentence again. Gate it on
   nothing — it runs live from the start. The Playwright suite's
   documented intermittents are named in the gate; a red in a file you
   did not touch is not yours.

### Tier 2 — should land

6. **`salon-dialogs-flow.spec.ts:412-418`** — teach the P4.D161 beat's
   expected-sentence chooser the third arm IF `readOwnerSeatAndTurn` can
   observe the composer being elsewhere (it cannot today — one user seat;
   record the reasoning if you leave it).
7. **The `turn_pause_filters_equivalence` / `turn_predicates_equivalence`
   neutrality run** at both pins (byte-identical expected; they never
   touch the new export).

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **No server behaviour change** — `chatTurnAction skipUserTurn` already
  accepts any user-driven seat (bug 123); nothing to defer, record that
  the wire is unchanged (no `core-contract.ts` edit — the tripwire is
  `git diff --stat` on that file: EMPTY).
- The weighted-random limitation of the OLD banner beat (P4.D58's note)
  is moot for this feature: the two-user-seat rotation is deterministic.
  Record it in the beat's header.

## The differential requirement, restated

The Rust twin ships with the NEW tier-1 `floor_seat_equivalence` over a tsx
oracle importing v4's REAL `resolveFloorSeatId` at the TARGET pin (closest
recipe: `harness/oracle/cases/turn-pause-filters.ts` + `crates/quilltap-
harness/tests/turn_pause_filters_equivalence.rs` — same `npx tsx <case> >
/tmp/oracle-floor-seat.ndjson` / `QT_ORACLE_FLOOR_SEAT=… cargo test -p
quilltap-harness --test floor_seat_equivalence` shape; write the header
recipe in the sweep driver's indentation form — `recipe-header-conventions`).
The client twin ships with v4's own unit suite transcribed as its parity
spec (the P4.D58 idiom); the Salon re-key ships with component specs that
assert the DISPATCH BODY (not only the sentence) and the live beat.

## Fixtures

- **Consumes:** none committed. The e2e beat builds its own chat live (the
  `composer-active-seat` recipe) — never the shared fixture chats (the
  title-checkpoint hazard).
- **Delivers:** `harness/oracle/cases/floor-seat.ts` (the corpus is inline
  in the case, as `turn-pause-filters.ts` does).
- **Owns regenerating:** `floor_seat_equivalence` (target pin); the two
  neutrality families (both pins).

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `2075242f9` bug-145/146 drift catch-up + maintenance round:
**P4.D192 ∥ P4.D193 ∥ P4.D194 ∥ P4.89 ∥ P4.90**, ordered 2026-09-16. Five
lanes, five worktrees, five lane branches, ALL from `main` (no stacking —
every lane's ownership is disjoint by file, so nothing needs a substrate
commit). The unifier cherry-picks in the order D192 → D193 → D194 → P4.89
→ P4.90 (server-side heals before the fixture widen; the fixture widen
before the family that re-measures over it), gates ONCE, and `merge
--ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`ffb6b3119`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`2075242f9`**
  (`4.10.0-dev.40`) — FOUR commits past the baseline, two carrying code:
  `23abc1ba1` (bug 145 + the bug-144 convergence + closes bug 143 as not
  a defect) and `2075242f9` (bug 146); `064ba85df` and `81e02f7a2` are
  the two docs-only filings this port's own walk wrote. v4 `bugfix` tip
  `1a2b2164c` and `release` tip `8fbf2afe0` are unmoved; there is still
  no `release: 4.10.0` squash. The checkout sat on `main`, CLEAN, at the
  planning probe (2026-09-16 ~08:30 CDT; the §2 probe PASSED, so the
  ledger stood and nothing was re-derived). **At unification the baseline
  MOVES to `2075242f9`** and all four rows retire (two ABSORBED, two
  NO-PORT-RATIFIED).
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log 2075242f9..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Any mismatch — a moved HEAD, a dirty `lib/`/`app/`/`packages/`
  /`plugins/`/`migrations/`/`help/` tree, the checkout off `main` — is a
  STOP: report it and do nothing else.** A lane never writes the ledger.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1). HEAD is past the
  baseline and two commits touch `lib/`, `migrations/`, `packages/` and
  `help/`. Every regen runs from a LANE-UNIQUE detached worktree per
  ledger §5.1 (`git -C ~/source/quilltap-server worktree add --detach
  /tmp/qt-v4-pin-<order>-<sha> <sha>`, the THREE symlink classes, `cd`
  into it; never stash or checkout-switch the human's repo; `git
  worktree list` before each batch; verify the pin by grepping the fresh
  NDJSON for a marker only one tree has; check the file is non-empty
  before believing a diff). Two pins per lane:
  - **the TARGET pin `2075242f9`** — for every family the lane's port
    MOVES (the oracle must import v4's post-fix code: the collapse
    migration, `resolveFloorSeatId`, the CLI's new wording, the two
    re-vendored help pages);
  - **the BASELINE pin `ffb6b3119`** — for every neutrality leg and
    every family the lane merely re-runs, and for `QT_V4_ROOT` /
    `QT_V4_CHECKOUT` on any embed/content guard run BEFORE a re-vendor
    commit lands.
  The sweep driver is the sanctioned per-family path
  (`harness/tools/recipe_sweep.py --run <family> --v4 "$PIN" --v5w
  <ABSOLUTE worktree>`); never run two sweeps concurrently; a family that
  reports `SKIP` in 0.00 s did not run (`a-family-env-var-is-not-its-
  regen-var`). Ledger §5.2's silent-stale-pass discipline applies to
  every regen (`rm -f` the fixture first; read the builder's last line;
  `grep -c` the changed bytes in the fresh NDJSON — MUST be > 0).
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). The
  spec for every item is `git show <sha> -- <path>` plus `git show
  <sha>:<path>`; a commit message describes the bug as its author
  understood it and the hunks are often narrower. Re-verify even where
  this order already flags the trap — the order's paragraph is the same
  kind of prose. Carry a v5-side code comment naming the sha where the
  prose misleads.
- **§R.5 — Convergence rows: measure, don't assume** (ledger §5.4). The
  bug-144 wording pin (`a_fresh_heartbeat_keeps_v4s_false_liveness_claim`)
  trips at the target pin BY DESIGN; dump v4's actual post-fix output
  before retiring it, and expect Tier R's five `lock clean …` cases to
  move together.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig) and its silence leg; a fixture change lands with the
  regen of every family that reads it. Mutation proofs: each named
  proof must redden exactly the test it is written against, and a proof
  that SURVIVES as first written is a finding to fix, not a line to
  delete (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`).
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). This order's status header is the
  lane's to update at lane close; the unifier rewrites it at unification.
  `docs/developer/porting/drift-ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed (and `apps/web/package.json` when it touches the SPA) on each
  commit per `.claude/commands/commit.md`; the unifier RECOUNTS as base +
  the sum of every lane's bumps (the silent-auto-merge trap: four lanes'
  identical `0.0.922` bumps merge as ONE). Expected: core is bumped by
  P4.D192, P4.D193 and P4.90; harness by all five (P4.89 only if it edits
  a harness header); cli by P4.D194; web by P4.89 (the committed fixture
  pair lives under `crates/quilltap-web/tests/fixtures/`); host by
  P4.D192 only if `host_boot_avatar_rolls_collapse.rs` changes; the SPA by
  P4.D193.
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  P4.D194 pre-lists the paths the target moves (with byte counts at the
  pin) in its lane record; no lane copies them.
- **§R.10 — Cross-lane handoffs, named:** (a) P4.D192 widens
  `gc_orphaned_file_row`'s return type — its only callers are in files
  P4.D192 owns (`db/doc_mount_file_links.rs`, `api/mount_points.rs:602`);
  no sibling touches either. (b) P4.D193's `help/chat-turn-manager.md`
  hunk and P4.D192's `help/database-protection.md` hunk are BOTH
  P4.D194's (the one `help/**` owner). (c) P4.89's widened
  `brahma-{main,mount}.db` is read by three families; no sibling
  regenerates them. (d) P4.90's `orchestrator.rs` edit is one statement
  after `:2861`; P4.D189's wrap census counts `watch_stream(` per file and
  must not move — P4.90 adds none. (e) The dogfood-findings row #119 cell
  moves with P4.D194 (the convergence); no other lane edits that file.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D192** — bug 145 server | `crates/quilltap-core/src/db/avatar_rolls_collapse_heal.rs`; `crates/quilltap-core/src/db/doc_mount_file_links.rs` — ONLY (i) `gc_orphaned_file_row`'s return type + its in-file callers/tests and (ii) the deletion of the `is_photos_relative_path` twin + its private `posix_dirname`; `crates/quilltap-core/src/api/mount_points.rs:602` (the one out-of-file GC caller, adapted to the new return); the twin's importers, import-line edits ONLY: `api/chat_media.rs:1716`, `photos/photo_link_summary.rs:13`, `photos/character_gallery_service.rs:17`, `services/maintenance.rs:32,375`; `crates/quilltap-harness/tests/avatar_rolls_collapse_heal_equivalence.rs`; `crates/quilltap-harness/tests/photos_relative_path_equivalence.rs` (import path only); `harness/oracle/cases/avatar-rolls-collapse-heal.test.ts`; `harness/oracle/fixtures/avatar-rolls-collapse-heal.json`; `crates/quilltap-host/tests/host_boot_avatar_rolls_collapse.rs` (only if a two-link arm is added); this order's header | `services/orchestrator.rs`, `services/chat_create.rs`, `participant_filters.rs`, `crates/quilltap-cli/**`, `help/**`, `apps/web/**`, `crates/quilltap-web/tests/fixtures/brahma-*`, every other harness family (regen-and-run ONLY for neutrality — no edits), `docs/v4/**`, the drift ledger |
| **P4.D193** — bug 146 (server twin + SPA) | `crates/quilltap-core/src/participant_filters.rs`; NEW `harness/oracle/cases/floor-seat.ts`; NEW `crates/quilltap-harness/tests/floor_seat_equivalence.rs`; `apps/web/src/app/chat/turn-order.ts` + `turn-order.spec.ts`; `apps/web/src/app/chat/turn-controls.ts`; `apps/web/src/app/screens/salon/salon-conversation.ts` — ONLY `bannerSeat` / `userTurnName` / `isSeatsTurn` / `mustSpeak` / `onSkipUserTurn` and the `<qt-turn-controls …>` binding block (`:431-436`); `apps/web/src/app/screens/salon/salon-turn-controls.spec.ts`; NEW `apps/web/e2e/salon-floor-seat-flow.spec.ts`; `apps/web/e2e/salon-dialogs-flow.spec.ts` ONLY if its banner assertion (`:412-418`) must learn the third sentence; this order's header | every other `crates/**` file, `help/**`, `core-contract.ts` (no wire change in this feature), every other SPA file, the drift ledger |
| **P4.D194** — bug-144 convergence + ratifications + help re-vendor | `crates/quilltap-cli/src/db_cmd.rs`; `crates/quilltap-cli/tests/cli_differential.rs` (only if a Tier R case must be added — none is expected); `help/database-protection.md`; `help/chat-turn-manager.md`; `docs/developer/porting/dogfood-findings.md` — row #119's status cell ONLY; this order's header | every other `crates/**` file, `apps/web/**`, every harness family except the `help_*` content families (regen-and-run at the target pin), `docs/v4/**` (pre-LIST for the unifier, never copy), the drift ledger |
| **P4.89** — the brahma fixture vintage widen | `crates/quilltap-web/tests/fixtures/brahma-main.db`, `brahma-mount.db` (+ any `.meta.json` sidecar found beside them); `harness/oracle/fixtures/migrate-memories-fixture-columns.ts` (EXTEND its target list — the P4.52 idiom); the THREE brahma harness headers (`crates/quilltap-harness/tests/brahma_orchestrator_tier3_equivalence.rs`, `brahma_console_tier3_equivalence.rs`, `brahma_console_routes_equivalence.rs`) — regen-recipe comments and a vintage note ONLY, no assertion changes unless a red is proven fixture-caused; the three jest oracles `harness/oracle/cases/brahma-*.test.ts` ONLY if an oracle-side heal is proven necessary AFTER the widen; this order's header | every `crates/quilltap-core/src/**` file (the P4.50-class prefix finding is MEASURED and RECORDED, never fixed here), `apps/web/**`, `help/**`, every other fixture, the drift ledger |
| **P4.90** — the failover model refresh + the six greeting lines | `crates/quilltap-core/src/services/orchestrator.rs` — ONLY the post-refresh `params` rebuild at `:2861-2863` (and, if measurement says so, the two loop-option sites `:2968-2971` / `:3046-3049`); `crates/quilltap-core/src/services/chat_create.rs` — ONLY the six log lines at their v4 sites + their unit-level capture tests; `crates/quilltap-harness/tests/orchestrator_tier3_equivalence.rs`; `harness/oracle/cases/orchestrator-tier3.test.ts`; `harness/oracle/fixtures/orchestrator-tier3.json` + `build-orchestrator-fixture.ts`; `crates/quilltap-harness/tests/chat_create_capstone_equivalence.rs` (capture pins only, if the pin idiom lives there); this order's header | `services/native_tool_loop.rs`, `services/text_tool_loop.rs`, `services/primary_stream.rs`, `services/provider_failover.rs`, `services/initial_greeting.rs`, `model/**`, every other harness family (regen-and-run only), `apps/web/**`, `help/**`, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0),
   and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp`) where the family is deterministic across
   regens; where it is not (`regenerate-at-both-pins-and-cmp-is-not-
   universal`), run by name and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast` with the lane's env block
   (its own oracle vars + `QT_V4_ROOT`/`QT_V4_CHECKOUT` at the right pin +
   `QT_NODE` = the node BINARY): report **binaries / passed / failed /
   ignored** from the per-binary lines and the exit code, and the lane's
   families confirmed RUN by non-zero duration. The standing withheld
   family (`brahma_orchestrator_tier3` — P4.89's subject) is withheld BY
   NAME in every lane but P4.89, which restores it.
9. **SPA lanes only (P4.D193):** `npm run lint` (incl. the qt-class guard),
   `npm test` (spec files / tests / 0 failed), `npm run build` clean, then
   the lane's e2e beats by FILE against the release binary, and ONE full
   Playwright run at lane close (numbers recorded; the documented
   intermittents — the P4.D161 pause-toast beats, the P4.d17 quill beat —
   are named, re-run by file, and never re-classified as this lane's).
10. **Ownership check:** `git diff --stat main...HEAD` — every path is in
    the lane's Owns column; every MUST-NOT-TOUCH path absent.
11. **Per commit:** `.claude/commands/commit.md` in full — the equivalence
    test is non-negotiable (§1), the invariants (§2), real-instance safety
    (§3), the harness regen (§4), build/test/lint (§5), the CHANGELOG
    entry + version bumps (§7). Commit on the lane branch only; never
    merge to `main`; never `git stash`; never a worktree inside a worktree.
12. **Lane close:** update this order's status header (what landed, what
    was refuted by measurement, what stays OPEN with its named cause),
    append the lane record to `status-log.md` (the regen commands as run,
    the counts, the mutation table, every finding outside the lane's
    ownership for the unifier), and stop. The final gate runs in the
    FOREGROUND of the lane's last turn — a backgrounded gate whose
    notification arrives after the lane reports done is a lane that is
    NOT done (`a-backgrounded-sleep-does-not-gate-your-turn`).
