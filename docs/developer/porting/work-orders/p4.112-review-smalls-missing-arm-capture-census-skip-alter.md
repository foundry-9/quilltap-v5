# Work order P4.112 — the `00c290c9a` round's review smalls: the title-update job's own `missing` arm; `auto_title_capture` consolidated onto `global_capture`; the dead `fold_hand_renamed` title input retired; the `get_messages_caller_census` counting per (file, variant); v5's corrupted-row skip widened to v4's Zod-shape failures (a bad `role`, a non-uuid id, a bad `hostEvent`) with the WARN; the corrupt-second-frame animated input MEASURED against sharp and pinned; the dead reader-side `ALTER` heals over P4.107's five widened pairs removed after a `pragma_table_info` proof — from `main`

**Status:** **UNIFIED on main at the `d1c06cd9d` Scenario Builder round (2026-09-23)** — CLOSED (Tier 1 items 1–6, Tier 2 items 7–8; Tier 3 items 9–11 deferred). **The §3 review fixed one nit here (`43ab1779`):** the folded `global_capture::install` ignored a failed `set_global_default` where the retired `auto_title_capture` rig had `.expect`ed it — a capture that lost the subscriber would pass every silence assertion; restored to a loud `expect`. **Recorded, not fixed:** unit 3's strict skip now also reaches `update_message`'s `find_event_value` (reads through `get_messages_strict`) — on a non-uuid-id / bad-role / bad-hostEvent row v4's raw `findOne` + `ChatEventSchema.parse(merged)` either writes (when the update repairs the field) or ERRORs `Failed to update message in chat`, where v5 WARNs the corrupted row and answers not-found; the P4.105 finding-1 family, widened; and unit 3 still passes `createdAt` / `participantId` shapes v4's Zod rejects. **⚠ AWAITS THE HUMAN: the corrupt-second-frame ruling (item 7)** — sharp throws on a two-frame input whose second frame is corrupt, so v4 stores the ORIGINAL; v5 counts one frame and writes a first-frame still WebP. Pinned as measured both ways; no behaviour change. Lane close: LANE DONE (2026-09-23, branch `claude/cranky-shirley-5d797f`, cut from `main` `602c5f87`) — Tier 1 items 1–6 LANDED, Tier 2 items 7–8 LANDED; Tier 3 items 9–11 deferred as ordered. ⚠ **Item 7 hit the order's STOP condition:** measured at the pin, sharp's transcode THROWS on a two-frame input whose second frame is corrupt, so v4 stores the original (both frames' bytes); v5 counts one frame and writes a first-frame still WebP — v5 drops bytes v4 keeps. NO behaviour change landed; pinned as MEASURED both ways; **the ruling is the human's.** What the order got wrong or could not know: item 1 needed NO `title_update_job.rs` change (v5 already discarded the outcome with no write — measured, pinned); the arm's "cursor unmoved" is UNOBSERVABLE by construction (the row is gone; `update` on a missing row is `Ok(false)` on v5, `null` on v4), so a cursor-write mutation survives — recorded; item 5 moved 10 core unit tests whose hand-rolled rows used non-uuid ids (test-only hunks in `db/chats_messages.rs`, `db/chats_search.rs`, `realtime/publish_sites.rs` — no lane's files); item 6 had exactly one heal to remove (`photos_routes`). Versions: core 0.0.1015, harness 0.0.935, host 0.0.150, web 0.0.180 (web because the removed heal lives in a web test). Record: `status-log.md` → § P4.112.

## Preamble

- **v4 oracle baseline: `00c290c9a`** (drift-ledger §1). This lane absorbs
  NO drift row — it takes the small follow-ups the `00c290c9a` unification
  review recorded (`phase-4.md` → the `00c290c9a` UNIFIED section, Next
  item 3; the round record's §3 findings 5–7; P4.D215's / P4.109's /
  P4.110's status headers). **Deliberately NOT taken** (say so in the lane
  record): P4.110's Tier 3 item 7 (lifting `dispatch_wrong_type_census`'s
  logic onto the shared lexer — two drift lanes recount that file this
  round; §Ownership), `update_chat_metadata`'s importer residual (marked
  in the code as unreachable after a successful INSERT; stays RECORDED),
  the dead heals over P4.111's TEN pairs (they are being widened this
  round; the next smalls).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3) — the BASELINE `00c290c9a` ONLY**
  (`/tmp/qt-v4-pin-p4112-00c290c9a`) for every family this lane grows or
  re-runs (`title_update_tier3`, `chat_regenerate_title_tier3`, `context_
  summary_service_tier3`, `chats_messages_ops_tier2`, `normalize_blob_
  image`, `photos_routes` + the other heal-carrying readers); nothing it
  touches moves at `d1c06cd9d`.
- **Sibling lanes:** the four drift lanes (disjoint files — `services/
  brahma_console/**`, `tool_build.rs`, the executor/search/doc-edit/path-
  resolver files, `api/{types,engine,groups,characters}.rs`, `db/vault_
  character_arrays.rs`, `crates/quilltap-web/src/**`, `help/**`,
  `apps/web/**` are NOT yours), P4.111 (the ten pairs and their readers'
  LOGIC are not yours; `cost-background-main.db` is on ITS list and is
  `title_update_tier3`'s pair — you edit that family's ARMS, it widens the
  pair; the unifier re-runs the family with both; your regens run on the
  un-widened copy from the pin and you record which arms need the widen).
- **Worktree:** `git worktree add <path> -b claude/p4-112-smalls-<hash>
  main`; `CARGO_INCREMENTAL=0`.
- **Read before the work:** the `00c290c9a` round record (`status-log.md`
  → "Round record — the `00c290c9a` …", §3 findings 3–7); P4.D215's order
  (Tier 3 + the "also named, not taken" list in its header); P4.109's Tier
  3 items 9–10 + its header's recorded nits; P4.110's Tier 3; v5
  `services/title_update_job.rs:170-488` (`handle_title_update` — where the
  chokepoint's outcome is discarded), `services/auto_title.rs` (the
  outcomes), the harness `auto_title_capture` rig vs `global_capture`
  (`crates/quilltap-core/src/test_support*`), `harness/oracle/fixtures/
  context-summary-service-ops.json` (`fold_hand_renamed`), `crates/
  quilltap-harness/tests/get_messages_caller_census.rs` (`(76, 8)` — the
  per-file rows), `db/chats_messages_read.rs` (`get_messages`' skip
  predicate + the WARN `Skipping corrupted chat message`), v4 `lib/
  database/repositories/chats-messages*.ts`'s `ChatEventSchema.safeParse`
  arm at `00c290c9a` (the exact Zod shape that fails: `role` enum, `id`
  uuid, `hostEvent` shape — read the schema), `crates/quilltap-host/src/
  image_codec.rs` (the frame counter + `is_multi_frame`; P4.108's record),
  `photos_routes_equivalence.rs:222-237`; memory notes `a-guard-whose-
  other-conjuncts-are-false-is-untested`, `a-mutation-of-a-write-no-arm-
  makes-the-code-read`, `coalescing-hides-its-own-mutation-proofs`,
  `consolidating-capture-layers-can-change-quoting`, `a-process-global-
  test-seam-must-be-thread-scoped`, `an-in-file-source-census-must-strip-
  test-modules-by-braces`, `a-new-harness-test-can-trip-a-source-census`,
  `round-00c290c9a-auto-title-heal-animated-safequery`.

## Mandate

Close the named review follow-ups, each red-first or mutation-proven, with
no behaviour change beyond what v4 does: the job's `missing` outcome
(nothing written, not even the cursor — v4's `applyAutoTitle` never calls
`writeExtraOnly` on that arm) as an arm; ONE capture rig; a dead corpus
input retired; a census that cannot be fooled by a same-file swap; the
corrupted-row skip covering v4's Zod failures with the same WARN; the
corrupt-second-frame case measured against sharp and pinned as whatever it
measures; the dead heals over the five widened pairs removed. Must NOT
touch: `dispatch_wrong_type_census.rs`, any drift lane's file, P4.111's
pairs or their readers' logic, `services/quilltap_import/**`, `apps/web/**`,
the drift ledger.

## Survey-verified starting points (fresh survey 2026-09-23 on `main` `70909d0a` — do not re-derive)

1. **The job's `missing` arm** — v4 `title-update.ts:221-230` discards the
   outcome; `applyAutoTitle`'s `missing` arm (`auto-title.ts:60-63`) writes
   NOTHING (the `extraPatch` cursor included) and DEBUGs `[Auto Title] Chat
   vanished before title could be applied` `{ chatId, source: 'title-
   check' }`. v5 `title_update_job.rs` reaches `apply_auto_title` with the
   job's clock; whether the job then advances its cursor by a separate
   write on `missing` — measure (a divergence if it does).
2. **The two rigs** — `auto_title_capture` duplicates `global_capture`'s
   design with different field rendering (the review's nit); the
   consolidation must keep every existing assertion's BYTES (a quoting
   change is a finding — `consolidating-capture-layers-can-change-quoting`).
3. **`fold_hand_renamed`** — a title INPUT in `context-summary-service-
   ops.json` that the hand-renamed fold arm can never consume (the early
   return skips the title LLM call); dead by construction; retire it and
   prove the arm still asserts zero title calls.
4. **The census** — `get_messages_caller_census.rs` counts per FILE
   `(strict, fallback)`; a swap of two sites' variants inside one file
   passes (the review's nit). Reshape to per-(file, line-anchored call)
   rows or per-(file, variant-by-callee-name) so a swap reddens; recount
   with the arithmetic.
5. **The corrupted-row skip** — v5 skips on a cell decode error or an
   unknown `type`; v4's `ChatEventSchema.safeParse` ALSO fails on a bad
   `role` (outside the enum), a non-uuid `id`, a malformed `hostEvent` —
   each WARNs `Skipping corrupted chat message` with the JS `||` id
   fallback. The review measured NO data-loss divergence (the one whole-
   transcript rewrite); port the three shapes as skip predicates + arms in
   `chats_messages_ops_tier2` (planted rows: `role: 'narrator'`, `id:
   'not-a-uuid'`, `hostEvent: 42`) red-first.
6. **The corrupt second frame** — P4.108's counter counts 1 for a GIF/WebP
   whose SECOND frame is corrupt and the codec encodes the first frame
   (P4.108's record). What sharp does with the same bytes (v4's blob
   transcode is its only animated sharp call): MEASURE with a planted file
   through v4's real `normalize-blob-image` oracle at the pin — then pin
   the measured shape both ways (`EXPECTED_DIVERGENCES` if it differs; an
   equality arm if it does not). A behaviour change only if the measurement
   says v5 loses frames v4 keeps — then a STOP + record (the ruling is the
   human's).
7. **The dead heals** — `photos_routes_equivalence.rs:222-237` (a guarded
   `ALTER TABLE files ADD COLUMN generationKey` over `photos-main.db`, now
   widened) + every sibling P4.107's lane record names over `inspector`,
   `inspector-nostore`, `wardrobe-routes`, `almanack` — each removed AFTER
   a `pragma_table_info` read on the committed pair proves the column
   present; the family still green.

## Tiered deliverables

### Tier 1 — must land

1. **The `missing` arm** in `title_update_tier3`: the chat deleted inside
   the canned LLM's reply hook on both sides → outcome `missing`, NO row
   change (the cursor unmoved — the comparand's `chats` dump byte-
   identical to the pre-job dump), the DEBUG line with `source: 'title-
   check'` capture-pinned; red-first if v5 advances the cursor.
2. **ONE rig**: `auto_title_capture`'s consumers moved onto `global_
   capture`; the rig deleted; every assertion re-run byte-for-byte (a
   diff of the captured lines before/after in the lane record).
3. **The dead input retired** + the zero-title-calls pin re-run.
4. **The census reshaped** (§Survey 4) with a mutation proof: swap two
   sites' variants in one file → RED.
5. **The three Zod-shape skips** + arms (§Survey 5), red-first; the WARN's
   `||` id fallback unchanged.
6. **The dead heals removed** (§Survey 7) with the proofs recorded.

### Tier 2 — should land

7. **The corrupt-second-frame measurement + pin** (§Survey 6).
8. P4.D215's, P4.109's, P4.110's header sentences (§R.7).

### Tier 3 — explicit deferrals (loud, typed, never silent)

9. P4.110's Tier 3 item 7 — NOT taken (the census file is contested this
   round).
10. The dead heals over P4.111's ten — the next smalls.
11. `update_chat_metadata`'s importer residual — recorded, unreachable.

## The differential requirement, restated

Every arm grows an EXISTING family that drives v4's real code from the
baseline pin (`title-update-tier3.test.ts`, `chats-messages-ops` tier 2,
`normalize-blob-image.ts`); the rig consolidation and the census reshape
are proven by byte-for-byte re-runs and mutations, not by inspection.
Recipes: each family's header through the sweep driver with `--v4
/tmp/qt-v4-pin-p4112-00c290c9a`.

## Fixtures

Consumes: the title/fold `/tmp`-built fixtures (their builders at the pin),
the messages tier-2 fixture, `normalize-blob-image/`'s binary fixtures (+ a
NEW corrupt-second-frame file WITH its generator, committed beside them),
the five P4.107-widened pairs (READ ONLY — the `pragma_table_info` proofs).
Delivers: the new binary fixture + generator. No committed pair changes.

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `d1c06cd9d` Scenario Builder drift catch-up + maintenance round:
**P4.D216 → {P4.D217} ∥ P4.D218 ∥ P4.D219 ∥ P4.111 ∥ P4.112**, ordered
2026-09-23. Six lanes, six worktrees, six lane branches. **P4.D216 is the
KEYSTONE** (the substrate every Scenario Builder surface consumes) and is cut
from `main` (`70909d0a` or the commit that lands these orders); **P4.D217 is
STACKED on P4.D216's tip** (`git worktree add <path> -b claude/p4-d217-… <the
P4.D216 lane branch>` once P4.D216's lane record says its Tier 1 is committed;
it rebases onto P4.D216's final tip before its own gate); **P4.D218, P4.D219,
P4.111 and P4.112 are cut from `main`**. The unifier cherry-picks in the order
**P4.111 → P4.D219 → P4.D216 → P4.D217 → P4.D218 → P4.112** (the heal before
any family reading a widened pair is re-run; bug 165 before the SPA that
consumes its id; the keystone before the service that compiles against it;
the SPA after the verbs its beats activate on; the smalls LAST because they
move shared harness rigs), gates ONCE from the new baseline pin, and `merge
--ff-only`s. **P4.D218 is the round's only SPA lane; P4.D216/P4.D217 are the
only lanes that touch `api/types.rs`/`api/engine.rs` (§R.10(a)).**

- **§R.1 — Baseline and target.** The oracle baseline is **`00c290c9a`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`d1c06cd9d`**
  ("feat(scenarios): Scenario Builder — the Host researches and drafts a
  starting scene", `4.10.0-dev.67`, 2026-09-23 09:02 −0500, 73 files,
  +6,010/−436) — **TWO commits past the baseline** with `dff00e98d` (the
  docs-only handoff spec, 4 files, +304) between them. Both rows are
  `ORDERED(…)` in the ledger's §3. v4 `bugfix` tip `1a2b2164c` and `release`
  tip `8fbf2afe0` are unmoved; still no `release: 4.10.0` squash. The
  checkout sat on `main`, CLEAN, at the planning probe (2026-09-23 — the §2
  probe PASSED; nothing was re-derived). Spec reads: `git -C
  ~/source/quilltap-server show d1c06cd9d:<path>` for post-commit files and
  `git show d1c06cd9d -- <path>` for the hunks — **never the commit message
  and never the feature spec's prose** (§R.4; the spec at `dff00e98d` was
  written BEFORE the implementation and `d1c06cd9d` edits it +53 into the
  "design of record" with a five-item "deviations from this spec, as built"
  header — port from the hunks). **At unification the baseline MOVES to
  `d1c06cd9d`**; both rows retire (`ABSORBED(…)` / `NO-PORT-RATIFIED(…)`).
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log d1c06cd9d..main`, `log
  1a2b2164c..bugfix`) at lane start and again before EVERY regen batch.
  **Pass = branch `main`, HEAD `d1c06cd9d`, both logs EMPTY, tree CLEAN.**
  Any mismatch — a moved HEAD (a new commit is a NEW drift row, not a pass),
  a non-empty log, ANY dirty path, the checkout off `main` — is a STOP:
  report it and do nothing else. A lane never writes the ledger. A lane
  resumes ONLY on a waiver the human records in the ledger's §1 (the
  `89fcc3c0d` round's mechanism), never on its own reading of the dirt.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1), absolutely. HEAD is
  two commits past the baseline and `d1c06cd9d` moves the Brahma one-shot's
  log capture, the tool-slate builder, the search/doc-edit handlers, the
  path resolver, the groups collection read and `addScenario` — all of which
  have regenerating families. Every regen runs from a LANE-UNIQUE detached
  worktree per ledger §5.1 (`git -C ~/source/quilltap-server worktree add
  --detach /tmp/qt-v4-pin-<order>-<sha> <sha>`, the THREE symlink classes —
  root `node_modules`, `packages/quilltap/node_modules`, every
  `plugins/dist/*/node_modules` — then `cd` into it; never stash or
  checkout-switch the human's repo; `git worktree list` before each batch;
  **verify the pin by `git -C "$PIN" rev-parse HEAD` AND by `ls -ld` of the
  path** — build every pin path from a quoted literal, never an unsplit zsh
  variable; then grep the fresh NDJSON for a marker only one tree has; check
  the file is non-empty before believing a diff). **The four DRIFT lanes
  (P4.D216, P4.D217, P4.D218, P4.D219) pin the TARGET `d1c06cd9d`**
  (`/tmp/qt-v4-pin-<order>-d1c06cd9d`) for every family their port MOVES
  **and the BASELINE `00c290c9a`** (`/tmp/qt-v4-pin-<order>-00c290c9a`) for
  their neutrality legs and pre-port red counts. **The two MAINTENANCE lanes
  (P4.111, P4.112) pin the BASELINE `00c290c9a` ONLY** — their families must
  not move at the baseline; a family that ALSO moves at `d1c06cd9d` (the
  Brahma / tool-slate / opacity / groups / character-arrays families the
  drift lanes name) is the drift lane's to re-record and the UNIFIER's to
  re-run from the new baseline pin with every lane's growth present. Pin
  verification markers: the target-pinned `tool_definitions` NDJSON carries
  `What to look up in the document stores: a place, its history, a custom,
  an event, a season.` and the baseline-pinned one does not; the
  target-pinned `help_tree_equivalence` NDJSON carries `help/scenario-
  builder.md` and the baseline-pinned one does not (`grep -c` both). The
  sweep driver is the sanctioned per-family path (`python3
  harness/tools/recipe_sweep.py --run <family> --v4 "$PIN" --v5w <ABSOLUTE
  worktree>`); never run two sweeps concurrently; a family that reports
  `SKIP` in 0.00 s did not run. Ledger §5.2's silent-stale-pass discipline
  applies to every regen (`rm -f` the output first; read the builder's last
  line; `grep -c` the changed bytes in the fresh NDJSON — MUST be > 0 where a
  change is predicted). Every lane stages its regen outputs under a
  LANE-PRIVATE directory (`/tmp/<order>/…` — several families' committed
  recipes write FIXED `/tmp/oracle-*.ndjson` paths; re-derive into the
  lane's directory before any `cargo test --workspace`) and leaves the
  committed recipe headers canonical (a committed recipe NEVER names a
  `/tmp` pin — the driver's `--v4` is the pin). Every jest oracle writes
  ZERO `llm_logs` rows (`jest.setup.ts` mocks the logging service) — never
  predict an `llm_logs` diff from a jest family; pin a log TYPE at the
  `streamMessage` option level (record `opts.logType` in the oracle) and
  count the canned-LLM calls. `QT_NODE` in any env block is a REAL path
  (`~/.nvm/versions/node/v24.13.1/bin/node`), never `$N/node`. v4's jest
  ignores `/\.claude/` paths: mirror any new oracle case + spec to a `/tmp`
  sibling pair before running (the `brahma-console-tier3` header's recipe).
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). Measured
  at planning: (a) the commit message calls the Brahma one-shot refactor a
  plain move ("becomes runOneShotToolLoop") — the HUNKS add FIVE debug
  lines the old file never emitted under the `Brahma one-shot` label
  (`: starting`, `: aborted between turns`, `: aborted mid-stream`, `: tool
  turn`, `: finished`) and give the `produced an empty answer` line a
  `turns` field; **the ledger row said "four" — it is five**. (b) `API.md`
  says the build stream has "no `content` chunks: the scene arrives whole"
  — true of the SERVICE (it never calls `encodeContentChunk`), but the loop's
  `processToolCalls` still emits `toolsDetected`/`status`/`toolResult`
  frames and the service's `onReasoning` emits `reasoning` frames: the
  frame set is what v4's REAL encoders write, recorded through them, never
  transcribed from a type (`a-typescript-parameter-type-does-not-strip-
  extra-fields`). (c) The feature spec's own "as built" header lists five
  deviations (the path resolver flattens with `includeParticipants: true`
  ONLY; the request schema lives in `lib/scenario-builder/request-schema.
  ts`; `docToolsMode` + an `extras` bag replaced more positional flags;
  `GET /api/v1/groups?characterIds=` was ADDED for the save dialog; the
  dialog loads lazily) — the spec's §-numbered prose below that header is
  NOT the shipped behaviour. (d) `buildScenarioBuilderUserMessage` tests
  `currentScenario?.trim()` for truthiness but interpolates the UNTRIMMED
  `input.currentScenario`, while the `contextSummary` block interpolates
  the TRIMMED value — an uncommented asymmetry: reproduce it byte-for-byte
  (the tier-1 corpus carries a whitespace-padded scenario) and name it as
  a v4 filing candidate in the lane record; never "fix" it. (e)
  `statusContext.characterId` is the EMPTY STRING `''`, not null. (f) The
  route's cast scoping reads `repos.characters.findById` (the overlay-
  applying, user-scoped read) while the mount pool reads `findByIdRaw`
  (the vault pointer + the archive flag, NO overlay — "a broken vault
  overlay must cost a tier, not the run"): two different reads over the
  same ids, both kept. (g) `GET /api/v1/groups?characterIds=` (present but
  EMPTY) yields ZERO groups — the filter applies on `!== null`, not on a
  non-empty list. (h) `pluginToolAllowlist` ABSENT admits every configured
  plugin tool; `[]` admits none — absent ≠ empty. (i) v4's `webSearch`
  extra only NARROWS (`!!profile.allowWebSearch && extras?.webSearch !==
  false`) — it can never grant search to a profile that forbids it. (j)
  `GET /api/v1/groups` has NO REST edge in v5 (measured: `crates/quilltap-
  web/src/lib.rs` routes no `/api/v1/groups`; the SPA dispatches
  `groupList`), so the filter lands as a dispatch-verb field, not a query
  string. (k) v5 builds NO plugin tools (`tool_build.rs:175` — the plugin-
  only `includePluginTools`/`toolConfigs` are a standing non-port) and has
  no curl plugin: `curlConfigured` is ALWAYS false on v5 and real mode's
  `['curl']` allowlist admits nothing — a RECORDED DIVERGENCE, carried in
  the capabilities body and the lane record, never a stub. Re-verify even
  where an order flags the trap — the order's paragraph is the same kind of
  prose. Carry a v5-side code comment naming the sha where the prose
  misleads.
- **§R.5 — Designed reds and pre-existing reds: measure, don't assume**
  (ledger §5.4). No convergence pin trips this round (bugs 165/166 are v4's
  own filings). The DESIGNED reds: **P4.D216** — `tool_build_equivalence`
  RED at the target on its grown `docToolsMode: 'read'` /
  `documentsOnlySearch` / `webSearch: false` cases (v5 has no read subset,
  no scenario `search` variant, no narrowing); `tool_definitions_
  equivalence` RED on the missing `searchScriptoriumScenario` catalog entry
  (the completeness check); `brahma_console_tier3`'s five new capture pins
  RED on absence; the `mountPool` arms grown in `doc_opacity` /
  `doc_edit_path_resolver` / `search_tools` RED (v5 has no `mount_pool`);
  `groups_routes`' `characterIds` cases RED; the log-type unit pin RED on
  the hard-coded `CHAT_MESSAGE`. **P4.D217** — every NEW family red on
  absence (no v5 counterpart), `help_tree_equivalence` at the target RED on
  the un-vendored tree (126 vs 127) then GREEN after the byte-copy; the
  three other help families that read the tree re-run. **P4.D218** — its
  vitest specs red-first on the absent entry points; its Playwright beats
  GATED on `P4D217_SERVER_LANDED = false` (flipped by the unifier).
  **P4.D219** — the grown vault-backed `add_scenario` arm RED (v5 returns
  the minted id; v4 the projected). **P4.111** — the eleven vintage families
  RED from the baseline pin on the un-widened pairs and GREEN after the
  widen. **P4.112** — no differential red; its capture arms red-first; its
  proofs are mutations. The NEUTRAL re-runs each lane must record (not
  assume): P4.D216 — `brahma_console_tier3` on its EXISTING corpus at the
  target (the refactor claims neutrality; the five new lines are additive),
  `brahma_orchestrator_tier3`, `carina_query_tier3`, `help_chat_
  orchestrator_tier3`, `orchestrator_tier3` (every `build_tools` caller
  maps `true → 'full'` / `false → 'off'` — neutral for every existing
  slate), `groups_tier2`, `scriptorium_tools`, `web_search_tool`. A red in a
  family a lane did NOT touch is a NEW finding to classify at both pins
  before any source is touched, and it is probably not that lane's. **The
  `/tmp/qt-imggen-*` staging collision** is the one recorded standing
  intermittent in `cargo test --workspace` (green by name).
- **§R.6 — The differential requirement stands for every unit.** A ported
  unit lands WITH an equivalence test that drives v4's REAL code at the
  right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest` is
  shared) and its silence leg; a fixture change lands with the regen of
  every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that SURVIVES
  as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`). **A v4 unit test
  that MOCKS the thing under test is a source of CASE NAMES, never an
  oracle**: `scenario-builder.service.test.ts` mocks `buildTools`, the
  loop, the mount pool and the capability probes (a jest oracle over it
  would exercise nothing real); `route.test.ts` mocks `runScenarioBuilder`;
  `mount-pool.test.ts` mocks the repositories; `character-add-scenario-
  projected-id.test.ts` mocks the repository. Mirror each as a real-DB arm
  with PLANTED rows through a jest oracle that drives the REAL module
  (`a-v4-mock-is-not-v4-in-a-route-family`). A ruled divergence is pinned
  in BOTH directions (the `EXPECTED_DIVERGENCES` shape, `markdown_
  transcript_equivalence.rs:40,213-233,256-261`): a "divergence VANISHED"
  failure if v4 == v5, a "WRONG SHAPE" failure if the difference is not the
  ruled one, and an assert that every declared divergence was exercised.
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane. Cross-order header edits allowed
  this round (one sentence each, naming this round): P4.111 → P4.107's
  header (the next list taken); P4.112 → P4.D215's, P4.109's and P4.110's
  headers (the named follow-ups taken; P4.110's Tier 3 item 7 is NOT
  taken — say so); P4.D217 → P4.D216's header ONLY to record what the
  stacked lane found the keystone got wrong (P4.D216 owns its own header
  until its close; P4.D217 appends one sentence after).
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed on each commit per `.claude/commands/commit.md`; the unifier
  RECOUNTS as base + the sum of every lane's bumps (the silent-auto-merge
  trap: identical bumps merge as ONE — it fires every round). Base at
  planning: core 0.0.1013, harness 0.0.930, host 0.0.149, web 0.0.179, cli
  0.0.27, tauri 0.0.7, fixture-sanitizer 0.0.4, sqlite3mc-sys 0.1.0 (NEVER),
  SPA 0.5.748. Expected: **core** by P4.D216, P4.D217, P4.D219, P4.112;
  **harness** by P4.D216, P4.D217, P4.D219, P4.112 (P4.111 ONLY if it edits
  a harness test — it should not); **web** by P4.D216 (the dispatch census
  recount lives in `crates/quilltap-web/tests/`), P4.D217 (the new routes
  file + the file-keyed censuses), P4.111 (the pairs live in
  `crates/quilltap-web/tests/fixtures/`), P4.112 ONLY if it adds a web test
  — say so; **host** by P4.D217 (the spine driver + `host_help_docs_boot`)
  and P4.112 (the codec item, if taken); **the SPA** by P4.D218 ONLY;
  **cli**, **tauri**, **fixture-sanitizer**, **sqlite3mc-sys** by NOBODY.
  P4.D217 stacks on P4.D216: its bumps are counted ON TOP of P4.D216's
  final numbers, and its lane record states both. No lane touches the
  workspace `Cargo.toml` dependency block; every lane's `Cargo.toml` deltas
  must be version-only — the unifier audits each before any resolution
  (the `--theirs` clobber trap).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's: each
  lane pre-lists, in its lane record with byte counts at `d1c06cd9d`, the
  mirror paths its rows move. This round's mirror rules in one place
  (measured at planning, bytes at `d1c06cd9d`): `docs/v4/developer/bugs/
  fixed/bug-165-scenario-create-transient-id.md` (NEW, 3,161),
  `docs/v4/developer/bugs/fixed/bug-166-modal-no-group-scenarios.md` (NEW,
  2,223), `docs/v4/developer/bugs.md` (308,274 → 309,151),
  `docs/v4/developer/API.md` (221,285 → 223,772),
  `docs/v4/developer/PROMPT_ARCHITECTURE.md` (33,334 → 34,137),
  `docs/v4/developer/features/scenario-builder.md` (NEW, 41,021 — the
  `d1c06cd9d` version, which supersedes `dff00e98d`'s; that is how the
  docs-only row is ratified), `docs/v4/developer/features/ROADMAP.md`
  (5,814 → 6,134) — every one byte-copied and md5-checked.
  `docs/v4/CHANGELOG.md`'s older lag (~385 lines) stays a named
  housekeeping item, not widened here. `docs/CHANGELOG.md`, `README.md`,
  `CLAUDE.md`, `.claude/commands/update-documentation.md`, every
  `package.json`/`package-lock.json` stamp → NO-PORT, the standing class.
  **The `help/` tree is NOT the mirror — it is a vendored v5 artifact and
  P4.D217's** (§Ownership): `help/chats.md` (39,682 → 40,848),
  `help/general-scenarios.md` (9,556 → 9,987), `help/project-scenarios.md`
  (9,345 → 9,784), `help/scenario-builder.md` (NEW, 7,375); **126 → 127**
  in BOTH literals (`help_tree_embed_guard.rs:65` `VENDORED_FILE_COUNT` and
  `host_help_docs_boot.rs:95`) — and `grep -rn "\b126\b" crates/
  --include='*.rs'` BEFORE the re-vendor, not after (`a-vendored-count-is-
  hard-coded-in-several-crates`; `help_tree_equivalence.rs:114-117`'s
  comments name the old count too). `help_tree_equivalence` at the target
  pin is the proof the tree is v4's.
- **§R.10 — Fences, ownership edges and the pre-declared spills.** (a)
  **`api/types.rs` and `api/engine.rs`:** P4.D216 adds ONLY `GroupList`'s
  `character_ids` field (§S.2) and the log-type constant; P4.D217 adds the
  three Scenario Builder verbs, the `Response::ScenarioBuilder` variant,
  the `EventPayload::ScenarioBuilderProgress` payload and the driver slot
  (§S.1) — as APPEND-ONLY blocks fenced `// === P4.D217 ===` … `// === end
  P4.D217 ===` at the END of each enum/struct so P4.D216's hunks and
  P4.D217's never share an adjacency; NOBODY ELSE touches either file. (b)
  **`services/tool_build.rs`, `services/tool_execution.rs`,
  `tools/executor.rs`, `tools/search.rs`, `tools/doc_edit/{shared,text,
  blob}.rs`, `doc_edit/path_resolver.rs`, `tools/definitions/data.rs`,
  `services/brahma_console/**`, NEW `services/agent_loop/**`,
  `services/primary_stream.rs` (the log-type parameter ONLY),
  `services/llm_logging.rs`, `api/groups.rs`:** P4.D216's. P4.D217 CONSUMES
  every one of them through calls it does not edit; if P4.D217 finds the
  keystone's shape insufficient it STOPs on that item, records the shape,
  and the unifier (or a re-opened P4.D216) takes it. (c) **NEW
  `services/scenario_builder/**`, NEW `api/scenario_builder.rs`, NEW
  `crates/quilltap-web/src/scenario_builder_routes.rs`, `crates/quilltap-
  host/src/spine.rs` + `host.rs` (the driver impl + the assembly slot —
  the hunks, marked), `help/**` + the two count literals:** P4.D217's. (d)
  **`db/vault_character_arrays.rs` + `api/characters.rs`'s
  `character_scenario_create` arm:** P4.D219's. (e) **`apps/web/**`:**
  P4.D218's alone. (f) **Committed fixture pairs:** P4.111 widens its TEN
  mains; nobody else touches a pair (plants go on per-run COPIES). (g)
  **`crates/quilltap-web/tests/dispatch_wrong_type_census.rs`:** P4.D216
  recounts for `GroupList.character_ids` (446 → 447, "a v4 QUERY key, not a
  route param — recorded honestly"); P4.D217 recounts AGAIN for
  `ScenarioBuilderBuild.run_id` + `ScenarioBuilderAbort.run_id` (a v5 scope
  tag — neither a URL segment nor a v4 body key; say so) — each lane
  records its own delta, the unifier recounts as base + both. (h) **The
  file-keyed censuses** (`tri_state_edges_share_the_decoder.rs`'s per-file
  table + total; `web_edge_body_parse_guard.rs`'s `COLLAPSE_CENSUS`): the
  NEW `scenario_builder_routes.rs` moves both — P4.D217's, with the
  arithmetic in the comment (`a-new-routes-file-moves-file-keyed-censuses`).
  (i) **Pre-declared loud out-of-mandate edits** (the `a-required-field-on-
  a-shared-struct-crosses-lane-ownership` rule: do the edit, keep it to the
  hunk, mark it `// P4.xxx OUT-OF-MANDATE — <owner> preserves`, audit with
  `git diff main -- <path>` at lane close): **P4.D216 → every `build_tools`
  caller** (`services/orchestrator.rs:2058`, `services/carina_query.rs:
  545`, `services/help_chat/orchestrator.rs:767`, `services/brahma_console/
  {mod,orchestrator}.rs`) — the `document_editing_enabled: bool →
  doc_tools_mode` call-site hunk only; **P4.D216 → `services/tool_
  execution.rs`'s `create_tool_context` and every `ToolExecutionContext {
  … }` struct literal in the tree** (`mount_pool: None`) — the field line
  only. If a lane finds it needs another lane's file beyond these, it STOPs
  on that item and records the shape for the unifier.
- **§R.11 — The SPA lane runs from `main` against §S, with its live beats
  GATED.** `P4D217_SERVER_LANDED = false` in NEW `apps/web/e2e/scenario-
  builder-flow.spec.ts` (the `P4D205_SERVER_LANDED` mechanism —
  `salon-inform-flow.spec.ts:36,118-119,154,182`); the unifier flips it and
  that first execution is the beat's first run, not a formality. Its vitest
  specs drive the dialog against a mocked `CoreClient` (`core-client.
  testing.ts`) with the §S.1 frames injected on `events$`. The unified gate
  runs the full Playwright suite once against the release binary + the
  fresh `dist`; the standing parks (6 skipped) and the documented
  intermittents stand as named in the previous round's gate (331 passed /
  0 failed / 6 skipped).
- **§R.12 — Committed fixtures: widen, never rebuild.** P4.111 widens
  `crates/quilltap-web/tests/fixtures/{embedding-remainder,autonomous,
  chat-cast,conversation-summaries-regen,documents,cost-background,
  system-data,episodic-recall,home,text-replacements}-main.db` through v4's
  own migration statements via `harness/oracle/fixtures/migrate-memories-
  fixture-columns.ts` from the BASELINE pin (P4.103's method, P4.107's
  recipe). Nobody REBUILDS a pair (a rebuild at the pin re-encodes every
  ≥ 512-byte compressed cell and retires the P4.D203 Class-3 neutrality
  proofs). Name only MAIN files in an apply run: `--report-only` does not
  report index additions, so an apply over an llm-logs partition silently
  adds the two `idx_llm_logs_*` indexes. `almanack-llmlogs-legacy.db` is
  DELIBERATELY un-widened — never name it. `.db-journal` files are never
  fixtures — delete strays (the migrator leaves one per apply). New
  `/tmp`-built fixtures (P4.D217's scenario-builder fixture, if it does not
  reuse `doc-opacity`'s) are committed as BUILDERS, never as `.db` files.

## §S — Shared contract (binding; byte-identical in every order of this round)

Transcribed at planning from v4 at `d1c06cd9d` — `lib/scenario-builder/
request-schema.ts`, `lib/services/scenario-builder/scenario-builder.service.
ts`, `app/api/v1/scenario-builder/route.ts`, `app/api/v1/groups/route.ts`,
`lib/database/repositories/characters.repository.ts` — and from v5's
streamed-verb precedent (`Event::swipe_progress`, `generator_sse::stream_
frames`, `BrahmaConsoleSendDriver`). **P4.D217 serves §S.1; P4.D216 serves
§S.2; P4.D219 serves §S.3; P4.D218 consumes all three.** Both sides transcribe
independently from the v4 sources named; the unifier diffs the wire
name-for-name (the playbook's §D).

### §S.1 — The Scenario Builder verbs (P4.D217 serves, P4.D218 consumes)

- **`scenarioBuilderBuild`** — dispatch wire `{ type: 'scenarioBuilderBuild',
  runId, body }` where **`runId` is a CLIENT-MINTED uuid** (v5's scope tag;
  v4 has none — its SSE response IS the run) and **`body` is v4's request
  object RAW** (`mode, location, time, details?, connectionProfileId,
  projectId?, characterIds?, chatId?, priorDraft?, revision?`). Rust:
  `Request::ScenarioBuilderBuild { run_id: String, body: serde_json::Value }`
  — the WHOLE body raw so absent / `null` / wrong-typed keys all reach v5's
  Zod twin exactly as v4's `safeParse(raw)` sees them (the tri-state rule
  at object granularity; `body` absent or non-object = the empty-object
  issue list). The twin reproduces `scenarioBuildRequestSchema` verbatim:
  `mode: z.enum(['real','in-world'])`; `location: z.string().trim().min(1).
  max(500)`; `time: z.string().trim().min(1).max(200)`; `details: z.string().
  trim().max(4000).default('')`; `connectionProfileId: UUIDSchema`;
  `projectId: UUIDSchema.nullish()`; `characterIds: z.array(UUIDSchema).
  max(32).default([])`; `chatId: UUIDSchema.nullish()`; `priorDraft: z.
  string().max(20_000).nullish()`; `revision: z.string().trim().max(2000).
  nullish()`; the root `.refine((r) => (r.priorDraft == null) === (r.revision
  == null), { message: 'priorDraft and revision travel together' })` (ONE
  issue at `path: []`); unknown keys STRIPPED (Zod's default). **Refusals,
  in v4's order, ALL before the first frame** (ordinary error responses on
  every transport): 400 `Request body must be JSON` (REST only — a
  non-JSON body); 400 `Validation error` + `details: <the issues array>`
  (the DEBUG `Scenario Builder request failed validation` `{ issues }`);
  404 `Connection profile not found` (a missing profile OR one whose
  `userId` is not the caller's; DEBUG `Scenario Builder profile not found
  for user` `{ profileId }`); 400 `This connection profile has tool use
  switched off, and the Host cannot make enquiries without tools. Choose
  another profile.` (`allowToolUse === false`); 400
  `describeProfileApiKeyFailure(reason)` (the existing api-key service's
  sentence for the reason); **cast scoping is NOT a refusal** — each
  DEDUPED `characterIds` entry is kept only if `characters.findById` finds
  it (a throwing lookup WARNs `Scenario Builder dropped an unreadable cast
  id` `{ characterId, error }` and drops it; a shrunken cast DEBUGs
  `Scenario Builder dropped cast ids the user cannot read` `{ requested,
  kept }`); 404 `Chat not found` (`chatId` given: missing OR foreign); 400
  `The Scenario Builder sets scenes only for Salon chats and autonomous
  rooms.` (`chatType` not `salon`/`autonomous`); then DEBUG `Scenario
  Builder request accepted` `{ mode, castCount, hasProject, inChat,
  revising, profileId }` and the run. A `runId` already in flight → 409
  `A Scenario Builder run with that id is already in flight.` (v5-only,
  before every v4 check).
- **The frames** ride the Event channel as `EventPayload::ScenarioBuilder
  Progress(ScenarioBuilderProgressPayload { frame: Value })` with the
  envelope's `progress_id = runId` — wire `{ type: 'scenarioBuilderProgress',
  progressId, frame }` (the `swipeProgress` shape, `types.rs:5207-5230`),
  `frame` = v4's SSE payload object VERBATIM, key order and bytes untouched
  (`preserve_order`), recorded through v4's REAL encoders: the tool-loop
  frames `processToolCalls` writes (`{"toolsDetected": N, "toolNames":
  [...], "toolArguments": [...]}`, `{"status": {"kind":"status", "stage":
  "tool_executing", "message": …, "toolName": …, "characterName": "The
  Host", "characterId": ""}}` — the `kind`-first rule, `{"toolResult":
  {"index", "success", "result", "error"}}`) exactly as v5's Brahma
  orchestrator already emits them; `{"reasoning": <CUMULATIVE text>}` on
  every reasoning change (REPLACE, never append — `onReasoning` fires with
  the whole turn's reasoning); then EXACTLY ONE terminal frame:
  `{"done":true,"scenario":<trimmed answer>,"provider":<profile.provider>,
  "modelName":<profile.modelName>,"usage":{"promptTokens","completionTokens",
  "totalTokens"},"toolsExecuted":N,"webAvailable":bool}` on success, or
  `{"error":"The Host returned from his enquiries empty-handed, I regret to
  report. Do try again, or try another model.","errorType":"scenario_builder_
  failed","details":<result.detail>}` when the loop returns `ok: false` with
  a detail other than `aborted`, or `{"error":"The Host has been detained by
  circumstances beyond his control — the model would not answer. Do try
  again, or try another model.","errorType":"scenario_builder_failed",
  "details":<error.message>}` when anything THREW (the ERROR `Scenario
  Builder run failed` `{ chatId, error }`), or — the REST edge's own belt
  and braces, unreachable by contract — `{"error":"The Host could not
  complete the enquiry."}`. **An ABORTED run emits NO terminal frame**
  (v4's rule: nothing enqueued; DEBUG `Scenario Builder run aborted by the
  client` `{ chatId }`, or `… (during a throw)` from the catch). **The
  dispatch RESPONSE** resolves AFTER the run: `Response::ScenarioBuilder
  (Value)` (wire `type: "scenarioBuilder"`) carrying the terminal frame's
  object (`done` or `error`) on completion, or `{ aborted: true }` (v5-only)
  after an abort. The SPA subscribes to `events$` BEFORE dispatching
  (client-minted id → no race) and reads the frames; it uses the dispatch
  value only for the pre-stream refusals.
- **`scenarioBuilderAbort`** — `{ type: 'scenarioBuilderAbort', runId }` →
  `Request::ScenarioBuilderAbort { run_id: String }` → `Response::
  ScenarioBuilder({ aborted: bool })` (`true` iff a run with that id was
  live; NEVER an error for an unknown id — the dialog's Stop races the
  finish). Abort semantics = v4's `AbortSignal`: observed BETWEEN turns
  (DEBUG `<label>: aborted between turns` `{ chatId, turn }`) and PER CHUNK
  mid-stream (DEBUG `<label>: aborted mid-stream` `{ chatId, turn }` — v4
  breaks its `for await` on the next chunk, so per-chunk is v4's
  granularity; a stalled provider is the watchdog's business, not the
  abort's); the loop answers `{ ok: false, detail: 'aborted' }`. **The REST
  edge aborts on client disconnect** (v4's `req.signal`): dropping the
  response body trips the same token.
- **`scenarioBuilderCapabilities`** — `{ type: 'scenarioBuilderCapabilities' }`
  → `Response::ScenarioBuilder({ webSearchConfigured: bool, curlConfigured:
  bool })` — v4 `GET /api/v1/scenario-builder?action=capabilities`;
  `webSearchConfigured` = the engine's existing `web_search_configured()`
  (`engine.rs:5963`); **`curlConfigured` is ALWAYS `false` on v5** (§R.4(k);
  v4 answers true only when the curl plugin is installed AND
  `pluginConfigs.findByUserAndPlugin(userId, 'qtap-plugin-curl').config.
  allowedUrlPatterns` is a non-empty array; the WARN `Curl capability lookup
  failed; treating curl as unavailable` has no v5 emitter); the DEBUG
  `Resolved Scenario Builder capabilities` `{ webSearchConfigured,
  curlConfigured }`; 500 `Failed to read Scenario Builder capabilities` (the
  ERROR `Scenario Builder capabilities lookup failed`) on a throw.
- **The REST edges** (P4.D217's Tier 2; the SPA does NOT use them): `POST
  /api/v1/scenario-builder?action=build` answering `text/event-stream` of
  `data: <frame JSON>\n\n` lines (no `event:`, no `id:`, no keep-alives —
  the `generator_sse::stream_frames` re-framer with a `scenario_builder_
  frame` extractor; the route MINTS `runId`), headers `Content-Type:
  text/event-stream`, `Cache-Control: no-cache`, `Connection: keep-alive`;
  the refusals above as JSON with their status BEFORE the stream opens
  (`res.ok` first, the swipe precedent); `GET /api/v1/scenario-builder?
  action=capabilities` → 200 the bare object; an absent/unknown action →
  the existing `withCollectionActionDispatch` sentence (read it from v4's
  middleware — do not invent it). `?action=` on the wrong METHOD is 404/405
  as the middleware answers — measure.
- **The `webAvailable` rule** (both sides): `mode === 'real' && !!profile.
  allowWebSearch && webSearchConfigured`. **The prompt's `now`** is INJECTED
  (a clock seam; the tier-1 family passes a fixed instant) and rendered
  `formatIsoWithOffset` — local ISO-8601 with `±HH:MM`, NEVER `Z`.
- **What the SPA already has** (measured — no server change): `ConnectionProfileDto`
  carries `isDefault: boolean`, `allowToolUse?: boolean`, `allowWebSearch?:
  boolean` (`core-contract.ts:3884-3900`) — the SPA maps them as v4's
  `mapProfiles`: `isDefault === true`, `allowToolUse !== false` (opt-OUT),
  `allowWebSearch === true` (opt-IN). The four save verbs EXIST
  (`scenarioCreate { scenario: ScenarioCreateBag }`, `projectScenarioCreate
  { projectId, scenario }`, `groupScenarioCreate { groupId, scenario }`,
  `characterScenarioCreate { characterId, title, content }` — `core-
  contract.ts:1300,1667,1964,2015`); the save dialog posts through them and
  never through REST.

### §S.2 — `groupList`'s membership filter (P4.D216 serves, P4.D218 consumes)

`{ type: 'groupList', characterIds?: string[] }` → `Request::GroupList {
#[serde(default)] character_ids: Option<Vec<String>> }` (the unit variant
becomes a struct variant; `{"type":"groupList"}` still decodes). v4 `GET
/api/v1/groups?characterIds=<id,id,…>` at `d1c06cd9d` `groups/route.ts:34-52`:
**when the key is PRESENT** (`Some`, even EMPTY — §R.4(g)): for each
requested id in order, `characters.findById` (the user-scoped read; a miss
is SKIPPED silently), then `groupCharacterMembers.findByCharacterId` — the
union of `groupId`s; `groups = groups.filter(g => memberGroupIds.has(g.id))`;
DEBUG `[Groups v1] Filtered groups by character membership` `{ userId,
requested, matched }` — **BEFORE** the existing createdAt-desc sort and the
`_count.members` enrichment. `None` = today's unfiltered list. Body
unchanged: `{ groups }`. The SPA's query key is v4's `groups.byCharacters
(<sorted, comma-joined ids>)`.

### §S.3 — Bug 165: the projected scenario id (P4.D219 serves, P4.D218 consumes)

`characterScenarioCreate` keeps its body `{ scenario }` — but for a
VAULT-BACKED character `scenario.id` is now the vault-PROJECTED id (re-keyed
from `Scenarios/<title>.md` on read), not the minted transient. v4
`characters.repository.ts` `addScenario` at `d1c06cd9d`: `priorIds` = the
ids of `findById(characterId).scenarios` BEFORE the add; the unchanged
`addToSubArray`; if it returned an item: re-read, `fresh` = the scenarios
whose id is not in `priorIds`, `projected = fresh.find(s => s.title ===
data.title) ?? (fresh.length === 1 ? fresh[0] : undefined)`; when `projected
&& projected.id !== added.id` → DEBUG `addScenario: returning the vault-
projected scenario id` `{ characterId, transientId, projectedId }`; returns
`projected ?? added` (a DB-backed character keeps the minted item; a
read-back with nothing fresh keeps the minted item; a failed add returns
`null` with ONE `findById` call, not two). The SPA's `SavedScenarioTarget
{ kind: 'character', characterId, scenarioId, title, content }` takes
`scenarioId` from the response.

### §S.4 — A lane that finds it needs a wire change beyond §S.1–§S.3 STOPs
on that item and records the shape for the unifier rather than inventing a
verb.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D216** — the KEYSTONE: the Scenario Builder substrate | NEW `crates/quilltap-core/src/services/agent_loop/{mod,one_shot_loop}.rs` (+ the `mod` line); `services/brahma_console/{mod,orchestrator,…}.rs` (the one-shot rewritten as a thin wrapper); `services/tool_build.rs` (`DocToolsMode`, `BuildToolsExtras`, the read subset, the three-way `search` pick, the allowlist point); `tools/definitions/data.rs` (`searchScriptoriumScenario`); `services/tool_execution.rs` (`ToolExecutionContext.mount_pool` + the refusal); `tools/executor.rs`, `tools/search.rs`, `tools/doc_edit/{shared,text,blob}.rs`, `doc_edit/path_resolver.rs` (the `mount_pool` arms); `services/primary_stream.rs` (the log-type parameter ONLY); `services/llm_logging.rs` (`SCENARIO_BUILDER`) + every closed copy of `LLMLogTypeEnum` in core (grep `VOICE_REWRITE`); `api/groups.rs` (`group_list`'s filter); `api/types.rs` `GroupList` + `api/engine.rs`'s `GroupList` arm ONLY (§R.10(a)); the `build_tools` call-site hunks + the `mount_pool: None` field lines (§R.10(i)); `tool_build_equivalence.rs`, `tool_definitions_equivalence.rs`, `brahma_console_tier3_equivalence.rs`, `doc_opacity_equivalence.rs`, `doc_edit_path_resolver_equivalence.rs`, `search_tools_equivalence.rs`, `groups_routes_equivalence.rs` + their cases/specs/builders/corpora (grown); `brahma_orchestrator_tier3`, `carina_query_tier3`, `help_chat_orchestrator_tier3`, `orchestrator_tier3`, `groups_tier2`, `scriptorium_tools`, `web_search_tool`, `llm_logs_tier2` — RE-RUN ONLY (edit only if a capture pin names a moved line; say so); `crates/quilltap-web/tests/dispatch_wrong_type_census.rs` (the `GroupList` recount); this order's header | NEW `services/scenario_builder/**`, `api/scenario_builder.rs`, `crates/quilltap-web/src/**`, `crates/quilltap-host/**`, `help/**`, `db/vault_character_arrays.rs`, `api/characters.rs`, every committed fixture pair, `apps/web/**`, the drift ledger |
| **P4.D217** — STACKED on P4.D216: the service + verbs + SSE + help | NEW `crates/quilltap-core/src/services/scenario_builder/{mod,mount_pool,request_schema,system_prompt,capabilities}.rs`; NEW `api/scenario_builder.rs` (the driver seam + the run registry); `api/types.rs` + `api/engine.rs` — the fenced APPEND-ONLY P4.D217 blocks (§R.10(a)); `crates/quilltap-host/src/spine.rs` + `host.rs` (the driver impl + the assembly slot — the hunks, marked); NEW `crates/quilltap-web/src/scenario_builder_routes.rs` + its `lib.rs` route lines + a `generator_sse.rs` frame extractor (append-only); `help/chats.md`, `help/general-scenarios.md`, `help/project-scenarios.md`, NEW `help/scenario-builder.md` (byte-copies) + `help_tree_embed_guard.rs:65` + `host_help_docs_boot.rs:95` (126 → 127); NEW families `scenario_builder_prompts_equivalence`, `scenario_build_request_schema_equivalence`, `scenario_builder_mount_pool_equivalence`, `scenario_builder_tier3_equivalence`, `scenario_builder_routes_equivalence` + their oracle cases/specs/builders (NEW under `harness/oracle/{cases,fixtures}/`); `help_tree_equivalence` + the two other tree-reading `help_*` families — RE-RUN at the target; `crates/quilltap-web/tests/dispatch_wrong_type_census.rs` (its own recount, §R.10(g)), `tri_state_edges_share_the_decoder.rs` + `web_edge_body_parse_guard.rs` (the new file's rows, §R.10(h)); NEW `crates/quilltap-web/tests/scenario_builder_dispatch_wire.rs`; P4.D216's header (one sentence, §R.7); this order's header | Every P4.D216 file beyond a CALL into it (a needed change is a STOP + record, §R.10(b)), `db/**`, `api/characters.rs`, `crates/quilltap-tauri/**` (measured: no per-verb table — the invoke dispatch is generic), every committed fixture pair, `apps/web/**`, the drift ledger |
| **P4.D218** — the SPA: the dialog, the save dialog, the two entry points | `apps/web/src/app/core/core-contract.ts` (the §S.1/§S.2 types, `GroupListRequest.characterIds?`, `ScenarioBuilderProgressEvent` + `isScenarioBuilderProgressEvent`); NEW `apps/web/src/app/scenario-builder/{scenario-builder-dialog,save-scenario-dialog,scenario-builder-run.state,agent-tool-calls,scenario-builder.api,host-activity}.ts` (+ specs); `apps/web/src/app/screens/new-chat/new-chat-form.ts` (+ spec; the entry + `handleUseBuiltScene`/`handleBuiltSceneSaved`), `new-chat.state.ts` (`refetchScenarioTiers`); `apps/web/src/app/chat/sidebar/chat-scenario-control.ts` (+ spec; the entry + `castCharacters`/`projectName` inputs), `chat-sidebar.ts` (the cast threading); `apps/web/src/app/chat/llm-inspector-entry.ts`, `llm-inspector-panel.ts`, `llm-logs.api.ts`, `screens/settings/system/llm-logs-card.ts` (the `SCENARIO_BUILDER` label/badge/filter rows + specs); `apps/web/src/app/scenario/scenario.api.ts` (`groupsByCharacters` query, if it lives there); `_chat.css`/the qt-class tables (guard-clean); NEW `apps/web/e2e/scenario-builder-flow.spec.ts` (gated, §R.11); `apps/web/package.json` (bumps); this order's header | Everything outside `apps/web/**`; the drift ledger |
| **P4.D219** — bug 165: `add_scenario` returns the projected id | `crates/quilltap-core/src/db/vault_character_arrays.rs` (`add_scenario` — the two reads + the projection + the DEBUG line; the `find_by_id` it already uses); `api/characters.rs` ONLY if the arm's body must change (it should not — say so); `characters_arrays_tier2_equivalence.rs` + `scenarios_routes_equivalence.rs` + `chat_scenario_routes_equivalence.rs` + their cases/builders (grown with the five mirrored arms over a VAULT-BACKED and a DB-BACKED character); this order's header | Every other `db/**` file, every P4.D216/P4.D217 file, `apps/web/**`, every committed fixture pair (plants on per-run COPIES), the drift ledger |
| **P4.111** — the fixture-vintage heal, the ten mains | `crates/quilltap-web/tests/fixtures/{embedding-remainder,autonomous,chat-cast,conversation-summaries-regen,documents,cost-background,system-data,episodic-recall,home,text-replacements}-main.db` (WIDENED in place); `harness/oracle/fixtures/migrate-memories-fixture-columns.ts` (the recipe header AS RUN + any pepper the ten need — read from each JSON source, never retyped); `harness/tools/sweep-results/<this run>.json`; P4.107's status header (one sentence, §R.7); this order's header | Every `.rs` file (a red after the widen is a FINDING, recorded, not fixed here), every test file's logic, `fresh_schema.json`, every OTHER committed fixture (incl. the five P4.107 widened and `chat-admin-*`), `almanack-llmlogs-legacy.db` (deliberate), every llm-logs and mount partition, `apps/web/**`, the drift ledger |
| **P4.112** — the review smalls (the `00c290c9a` round's named follow-ups) | `services/title_update_job.rs` (the `missing` arm); `crates/quilltap-harness/tests/{title_update_tier3,chat_regenerate_title_tier3,context_summary_service_tier3}_equivalence.rs` + the `auto_title_capture` rig (consolidated onto `global_capture`) + `harness/oracle/fixtures/context-summary-service-ops.json` (the dead `fold_hand_renamed` input); `crates/quilltap-harness/tests/get_messages_caller_census.rs` (per-(file, variant) rows); `db/chats_messages_read.rs` (the corrupted-row skip's Zod-shape arms) + `chats_messages_ops_tier2_equivalence.rs` (grown); `crates/quilltap-host/src/image_codec.rs` (the corrupt-second-frame item — MEASURE sharp first; a divergence pin, not a behaviour change, unless measured otherwise) + `normalize_blob_image_equivalence` (grown); the dead reader-side `ALTER` heals over P4.107's FIVE widened pairs (`photos_routes_equivalence.rs:222-237` + the siblings P4.107's record names — removed after a `pragma_table_info` proof); P4.D215's, P4.109's, P4.110's headers (one sentence each, §R.7); this order's header | `crates/quilltap-web/tests/dispatch_wrong_type_census.rs` (P4.110's Tier 3 item 7 is NOT taken — two drift lanes recount it), every P4.D216/P4.D217/P4.D219 file, P4.111's ten pairs and their readers' LOGIC, `services/quilltap_import/**` (the `update_chat_metadata` residual stays RECORDED, not taken), `apps/web/**`, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix, beyond the pre-declared spills in §R.10(i) — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note). Budget 50–70 GB of disk per Rust lane
(`df -h ~` showed 139 GB free at planning; six worktrees is five `target/`
dirs plus the SPA's `node_modules` — run at most THREE Rust lanes
concurrently and reclaim `target/debug/incremental` between waves; P4.111
builds only what its readers need; P4.D218 runs no cargo at all).

1. **§R.2 probe** — PASS (opening, and again before every regen batch).
2. `cargo fmt --all --check` — clean (Rust lanes).
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in BOTH
   feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0
   where the order predicts a change; a recorded `0` where it predicts
   none), and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp` / by-label diff) where the family is
   deterministic across regens; where it is not
   (`regenerate-at-both-pins-and-cmp-is-not-universal`), compare the
   normalized rows by case and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`
   (a widened `.db` is the ONE exception — the pair is the artifact).
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast -- --nocapture` with the lane's
   env block (its own families' vars; every other family's var WITHHELD
   so it SKIPs honestly rather than failing on a deleted `/tmp` path):
   every binary's `test result:` line read, zero failures bar the
   standing reds §R.5 names (recorded by name if their vars are in the
   block), the lane's families confirmed RUN by non-zero duration.
9. **Source censuses and guards** the change moves or must not move, re-run
   by name with any constant recounted and the arithmetic in the comment:
   `dispatch_wrong_type_census` (P4.D216 446 → 447; P4.D217 → +2 on top;
   everyone else UNMOVED — run to prove it); `tri_state_edges_share_the_
   decoder` + `web_edge_body_parse_guard` (P4.D217 — the new file's rows;
   everyone else unmoved); `help_tree_embed_guard` + `host_help_docs_boot`
   (P4.D217 → 127; everyone else UNMOVED at 126); `blob_write_sites_
   census`, `compressed_column_write_sites_census`, `get_messages_caller_
   census` (P4.112 reshapes the last; everyone else unmoved);
   `qtap_schema_embed_guard`, `zod_version_guard`, `provider_sdk_version_
   guard`, `public_schemas_vendor_guard` — GREEN.
10. **Tier R** (`crates/quilltap-cli/tests/cli_differential.rs`) at the
    lane's pin for P4.D216, P4.D217, P4.D219 and P4.112 (all reach code
    the CLI links — 266/0 expected; run it, do not assert it).
11. **The SPA** (P4.D218; P4.D217 if it touches `core-contract.ts` — it
    must not): `npm test` whole green with the new spec files confirmed
    RUN; `npm run lint` (the qt-class guard) clean; `npm run build` clean
    (`spa-tsc-does-not-typecheck-app-sources` — the build is the type
    check); `apps/web/package.json` bumped per commit.
12. **Commit per `.claude/commands/commit.md`** (one unit per commit where
    the order tiers them; CHANGELOG entry + version bumps per §R.8; the
    status-log lane record appended with the regen commands AS RUN, the
    mutation table, the pre-fix counts, the measurements, and every
    finding outside the lane's ownership named for the unifier). The lane
    closes by updating its own order's status header with what landed,
    what was deferred, and what it measured that the order got wrong.
