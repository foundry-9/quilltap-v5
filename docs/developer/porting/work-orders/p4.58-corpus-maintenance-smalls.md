# Work order P4.58 — the a14a1811-round corpus-maintenance smalls (photo-tools blind spots + the settings-routes seed-default row)

**Status:** CLOSED (2026-08-23, branch `claude/corpus-maintenance-smalls-c06262`;
unified same day on `unify/0ba942b1-round`). All Tier-1 items + Tier 2 landed
with **zero v5 source change**, and the mutation-proof discipline ran whole
(nine proofs across the two families, each reverted from a file backup).
**Three order premises corrected by measurement:** (1) there are NO committed
photo-tools fixture DBs — the family builds fresh to /tmp per regen, and
`photo_tools_equivalence` is the fixture pair's only consumer; (2) the second
omission site is `attach_image`, not `list_images` (the sha256 sister join
feeds the descriptor; `ListedImage` carries no dimensions in either app); (3)
the suggested fixture-side mutation proves nothing (both sides regenerate from
the same fixture) — v5-source mutations were used instead. Also corrected: v4's
`providerSupportsMimeType` is an alias of the static map (`baseUrl` accepted
and never read) — the registry-tier warning in the order was wrong, and the
case comment records the measured truth. Photo-tools 29 → 34 rows; settings-
routes 141 → 145 with the `>= 26` floor. The #95 status cell corrected. Tier-3
declines stand as written. Lane records: `status-log.md` → "Lane record —
P4.58 unit 1" / "unit 2".

## Preamble

- **v4 baseline for this round: `0ba942b1`** (v4 main, HEAD at planning
  2026-08-23, tree CLEAN, checkout on `main`). The two drift lanes absorb
  the three commits past `a14a1811`; THIS lane is pure corpus maintenance
  against surfaces already ported — its regens still run at the round
  baseline like everyone else's.
- **Drift-check at lane start.** `git log 0ba942b1..main --oneline` AND
  `git diff main bugfix -- lib/ app/ packages/`. If v4 moved, **STOP** and
  report. Note which branch the checkout occupies before any regen.
- **Regen pin:** live checkout only while clean AND at `0ba942b1`; otherwise
  a lane-unique detached worktree (`/tmp/qt-v4-pin-p458-0ba942b1`, all three
  symlink classes). ⚠ `/tmp/qt-v4-a14a1811` is the previous baseline's pin —
  do not regen from it (the drift lanes' commits do not touch these two
  families' surfaces, so the OUTPUT would likely match, but the round rule
  is one pin for everyone; don't create a two-pin round).
- **Sibling lanes:** P4.D110 (`p4.d110-title-verdict.md`), P4.D111
  (`p4.d111-bug97-convergence.md`) — both fully disjoint. §C below is round
  facts only.
- **Worktree:** per the `carryout` skill; edit inside the worktree, not main.
  `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` are
  append-only.

## Mandate

Close the two corpus blind spots the a14a1811 round recorded (phase-4.md
candidate 4) so the arms they cover are pinned through the oracle, not only
by same-file unit tests: (a) the photo-tools width/height-NULL key omission
and the whitespace-only-description quirk; (b) the settings-routes
NANOGPT/Z_AI create-with-omitted-`supportsImageUpload` seed default. Plus one
doc rider (the stale #95 status cell). **Expected v5 source change: ZERO** —
this is corpus/fixture/oracle-case work. If a new row reds against v5, that
is a FINDING: diagnose, and if it is a genuine v5 divergence, fix it only if
the fix lives in files no sibling owns (`tools/photo.rs`,
`photos/auto_describe_attachment.rs`, `api/settings.rs` are all unowned this
round); otherwise stop and escalate.

## Survey-verified starting points (2026-08-23)

### (a) Photo-tools — recorded by the P4.D108 lane

- **The whitespace-only quirk** (`status-log.md:82345–82347`): v4's check is
  truthiness-then-trim — an EMPTY `imageDescription` fails the describe
  check but a WHITESPACE-ONLY one passes and persists `''` after the trim.
  v5: `crates/quilltap-core/src/photos/auto_describe_attachment.rs:296–315`
  (quirk comment :296, truthiness `filter(|d| !d.is_empty())` :300,
  `js_trim` persist :315). Pinned TODAY only by unit tests in the same file
  (:585–586, :657) — no differential row drives the arm through the oracle.
  The stored-tier check (`entry.description && trim().length > 0`, reached
  via :144–148) is also corpus-unpinned for a whitespace-only STORED
  description.
- **The width/height-NULL omission** (`status-log.md:82366–82369`): the
  success row is built in v4's literal key order with absent width/height
  OMITTING the key (`?? undefined`). v5:
  `crates/quilltap-core/src/tools/photo.rs:888–916` (doc comment :888–889,
  conditional inserts :911–915; the sha256 sister join that can leave them
  `None` at :730–739, which also feeds `list_images`). **No fixture image
  has NULL width/height** — one row has a PRESENT `width: 0, height: 0`
  (photo-tools.json:119–120), which does NOT exercise the omit arm (`??`
  keeps a present zero — the js-nullish-chain rule).
- **Family machinery:** oracle case
  `harness/oracle/cases/photo-tools.test.ts` (ops at ~:230–264 — 9
  `describe_image` handler ops + 6 `auto_describe` module ops; module-mock
  vocabulary :163–173 `autoDescribe: 'vision'|'race'|'failed'`,
  `moduleMock: 'description'|'unsupported'|'throws'`;
  `MODULE_RAW_DESCRIPTION` :182 is padded but never whitespace-ONLY).
  Fixture spec `harness/oracle/fixtures/photo-tools.json`; builder
  `build-photo-tools-fixture.ts` (image type :57–58 declares width/height
  REQUIRED and writes them unconditionally :252–253 — the spec/builder must
  widen to allow absent/null). Harness twin
  `crates/quilltap-harness/tests/photo_tools_equivalence.rs` (recipe in
  header ~:37–57; env `QT_ORACLE_PHOTO` + `QT_FIXTURE_PHOTO_MAIN/MOUNT`).
  The mirrored-op lists live at `corpus()` :414 / `describe_corpus()` :549 /
  `module_corpus()` :610, enforced by the completeness assert at
  :1145–1154 — every new oracle op MUST be mirrored or the family errors.
  **The committed fixture DBs must be REBUILT whenever the spec widens**
  (the header's own rule, `status-log.md:82412–82419`).

### (b) Settings-routes — recorded by the P4.D106 lane

- `status-log.md:81829–81835`: the a14a1811 §C1 static-map rows flipped the
  CREATE-time default of `supportsImageUpload` from false→true for NANOGPT
  and Z_AI, and `settings_routes_equivalence` stayed green because "that
  corpus carries no NANOGPT/Z_AI create-with-omitted-flag row" — the flip is
  pinned only by the map rows' own unit tests + the image_transport family.
- v5 seed logic: `crates/quilltap-core/src/api/settings.rs:1345–1349`
  (`bag.get("supportsImageUpload").as_bool()` else
  `files::attachment_support::supports_mime_type(&provider, "image/jpeg")`),
  stored at :1518–1521 (courier forces false). Map rows:
  `crates/quilltap-core/src/files/attachment_support.rs:59–68` (NANOGPT :62,
  Z_AI :68). ⚠ P4.D111 owns `attachment_support.rs` this round — this lane
  READS it, never edits it.
- v4 equivalent: `app/api/v1/connection-profiles/route.ts:191–197`
  (`typeof supportsImageUpload === 'boolean' ? … :
  providerSupportsMimeType(provider, 'image/jpeg', baseUrl || undefined)`),
  stored at :300. **Nuance for the case comment:** v4's route passes
  `baseUrl` into the REGISTRY-AWARE `providerSupportsMimeType` where v5
  calls the static `supports_mime_type`; the oracle runs jest =
  registry-uninitialized (the case's own comment at
  settings-routes.test.ts:124–126), so the new rows pin the STATIC tier
  only — say so in the case comment.
- **Family machinery:** oracle case
  `harness/oracle/cases/settings-routes.test.ts` (connection-profile creates
  at ~:889–1060; NO case anywhere sends or omits `supportsImageUpload`, and
  no create names NANOGPT or Z_AI). Harness twin
  `crates/quilltap-harness/tests/settings_routes_equivalence.rs` (recipe
  header :35–41; it drives cases from the NDJSON — no mirrored case list;
  the `connection_profiles >= 22` count guard at :576–578 should bump to the
  new floor). A create case needs no fixture rows.

### (c) Doc rider

- `docs/developer/porting/dogfood-findings.md` row **#95** (~line 102): the
  status cell still says "FILED as v4 bug 82… retires when the port absorbs
  the fix" — the P4.D93 lane (the `9125f492` round) absorbed exactly that
  fix. One-line status correction.

## Tiered deliverables

**Tier 1 (must land):**

1. **Photo-tools, the NULL-omission arm:** widen the fixture spec + builder
   so an image row may carry absent/null width/height; add such an image to
   `photo-tools.json`; add a `describe_image` op AND a `list_images` op
   hitting it (both join sites :730–739 feed the omit arm) so the success
   rows omit the keys; mirror the ops behind the completeness assert;
   REBUILD the committed fixture DBs; regen at the pin. Per the
   blinded-comparand rule: verify the family's normalizers do not erase key
   PRESENCE (the arm exists to prove omission vs null — if the comparand
   can't tell them apart, fix the comparand first).
2. **Photo-tools, the whitespace-only arms:** (i) a new `moduleMock` (or
   `autoDescribe`) vocabulary value producing a whitespace-only vision
   result, driving the truthiness-then-trim quirk (persists `''`); (ii) a
   fixture image whose STORED description is whitespace-only, with an op
   reading it through the stored-tier check. Mirror both; the persisted `''`
   must be a visible comparand (again, check the normalizers).
3. **Settings-routes:** new oracle-case create rows — NANOGPT and Z_AI with
   `supportsImageUpload` OMITTED (each with `after: 'connProfiles'` so the
   persisted `true` is diffed) + a DEEPSEEK contrast row (defaults false) to
   pin the direction both ways; the registry-tier case comment (see the
   nuance above); bump the `>= 22` count guard to the new floor. Regen at
   the pin.
4. **Red-first honesty check, adapted:** these rows pin arms believed
   CORRECT, so they should be born green — prove they measure something by
   MUTATION instead (temporarily break each arm in v5 — e.g. make the
   builder always-insert width/height keys; flip a map row; break the
   whitespace persist — and confirm the family reds naming the new row;
   revert via file backup, never `git checkout` mid-unit). Record each
   mutation proof in the lane record.
5. The #95 status-cell correction in `dogfood-findings.md`.

**Tier 2 (should land):**

6. If the settings-routes guard-floor bump exposes other stale `>=` floors
   in the same file, bump only those the new rows moved — leave the rest.

**Tier 3 (named and DECLINED for this order — recorded so the round report
can say why):**

- The tri-state web-edge survey sites (P4.57's banked future-order fodder)
  — bigger than corpus rows; wants its own order.
- The `taboo`/`brahma-console` three-call-site tri-state residue (P4.56's
  note) — same.
- The stale `docs/v4/` mirror (~8 files + `bugs/fixed/` rows) — its own
  maintenance pass.
- The upload-time fire-and-forget auto-describe no-op in
  `services/chat_files.rs` (P4.D108 deferred-loud) — adjacent to (a) but a
  BEHAVIOR change, not corpus; stays with its named deferral.
- Dogfood #94 (Almanack "Free Memory: 0 B") — explicitly awaiting the
  human's nod; #98 (the SERPER settings-key registry) — the standing P4.42
  deferral.

## The differential requirement

Both families drive v4's REAL code (the photo-tools jest case over the real
handlers/module; the settings-routes jest case over the real route) — this
lane only widens their corpora. The sweep driver
(`harness/tools/recipe_sweep.py`) is the sanctioned regen path; note the
sweep-recipes-write-into-the-repo rule — the photo-tools recipe REBUILDS
committed fixtures by design, which is exactly what item 1 wants, but run it
deliberately, from the lane worktree, and diff the resulting DBs' seeded
rows against expectations before committing.

## Fixtures

- This lane DELIVERS: the widened `photo-tools.json` + builder + rebuilt
  `photo-tools` committed fixture DBs. Owner of dependent regens: this lane
  — check the spec/DB headers for any OTHER family consuming the photo-tools
  fixture pair and re-run those by name too (report the list in the lane
  record; the survey found none, but the header is the authority).
- The settings fixture (`settings.json`) is CONSUMED unchanged — the new
  rows are oracle-case-only.

## §C — Shared round facts (identical in all three orders, binding)

- Round baseline: **`0ba942b1`**. Every oracle regen in this round runs at
  it — live checkout only while clean AND at that HEAD; otherwise a
  lane-unique pin worktree with all three symlink classes.
- `/tmp/qt-v4-a14a1811` is the PREVIOUS baseline's pin — stale for every
  family this round touches; never regen from it.
- Never run two sweep-driver invocations concurrently across lanes.
- Version-bump ownership: P4.D110 bumps `quilltap-core` + `quilltap-harness`;
  P4.D111 bumps `quilltap-core` + `quilltap-harness`; P4.58 bumps
  `quilltap-harness` only. Identical bumps off the same base are recounted by
  the unifier (base + total bumps) — bump normally, do not coordinate
  numbers.

## Ownership (disjoint by construction)

This lane owns:
- `harness/oracle/cases/photo-tools.test.ts`,
  `harness/oracle/fixtures/photo-tools.json`,
  `harness/oracle/fixtures/build-photo-tools-fixture.ts`, the committed
  photo-tools fixture DBs
- `harness/oracle/cases/settings-routes.test.ts`
- `crates/quilltap-harness/tests/photo_tools_equivalence.rs`,
  `crates/quilltap-harness/tests/settings_routes_equivalence.rs`
- `docs/developer/porting/dogfood-findings.md` (the one status cell)
- CONDITIONAL (finding-only, see Mandate): `tools/photo.rs`,
  `photos/auto_describe_attachment.rs`, `api/settings.rs`

It must NOT touch: anything P4.D110 or P4.D111 owns — in particular
`files/attachment_support.rs` (READ-only here), `files/image_transport.rs`,
`services/file_fallback.rs`, the provider manifests, the title-update
family, `apps/web/**`.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`.
2. `cargo clippy --workspace --all-targets -- -D warnings`, plain AND
   `--features quilltap-core/native-transport`.
3. `cargo test --workspace` with the lane's oracle env vars — full log
   captured (NEVER `| tail`), per-binary `test result:` lines kept.
4. Both families BY NAME with `--nocapture`, zero SKIP, over oracles fresh
   at the pin; the mutation proofs (Tier-1 item 4) recorded red-then-reverted;
   the fresh NDJSONs grepped for the new rows' distinguishing bytes (the
   omitted-key row shape, the persisted `''`, the seeded `true`).
5. No v5 source change expected → if any landed (finding path), name it and
   its own proof explicitly in the lane record.
6. No SPA changes → no ng/Playwright run required; state that explicitly.
7. Per-commit rule: follow `.claude/commands/commit.md`; version bumps per
   §C.
