# Work order P4.d14 — the episodic-recall drift catch-up, **round 3 lane A: creation-side extraction (workstream A-creation) + the fold-episode pass + stop-destroying-episodes (workstream E)** — round baseline `8bf3cb5f`

> **STATUS: CLOSED** (landed 2026-07-22 on
> `claude/p4-episodic-creation-fold-4f22bd`, 5 commits). Every tier-1 item
> (1–8) and the tier-2 item landed; the tier-3 deferrals are the enumerated
> pre-existing ones, unchanged. `QT_ORACLE_GATE` is UN-SKIPPED and green at
> `8bf3cb5f`, and the NEW `QT_ORACLE_FOLD_EPISODE` family exists. The full
> lane record — the 15 regenerated families, the fixtures changed, the
> cross-lane surface, and the loud deferrals — is in `status-log.md`
> ("P4.d14 lane record").

## Preamble

**v4 reference baseline for this round: `8bf3cb5f` (4.9-dev, "Unify
episodic recall + character outfit work") — verified at planning
(2026-07-22): v4 HEAD == `8bf3cb5f`, tree clean.** At lane start, run in
`~/source/quilltap-server`:

```bash
git log 8bf3cb5f..HEAD --oneline
```

**If HEAD moved, STOP and report.** (v4 is mid-4.8/4.9 development — a
version/tag commit may land; report it either way.) v4's checkout is
clean at the baseline, so oracles regenerate directly from
`~/source/quilltap-server`; pin a detached worktree only on drift/dirty
(recipe: the `oracle-regen-pinned-v4-worktree` memory — symlink BOTH
`node_modules`: root + `packages/{quilltap,plugin-types,plugin-utils}` +
`plugins/dist/*`).

**Sibling lanes (run in parallel, own worktrees):**

- **P4.d15** (`p4.d15-retro-minirecap-whisper.md`) — round 3 lane B:
  buildContext PART 2 (the scoped mini-recap + the
  `retrospective-recall` whisper + the spam guard + the recall-history
  retro-signature machinery). It owns `services/build_context.rs`,
  `recall_history.rs`, and `services/commonplace_notifications.rs` —
  **you must not touch those files.**
- **P4.9H1** (`p4.9h1-chat-sidebar-storys-clock.md`) — the ChatSidebar
  SPA vertical (the Story's Clock switch). It owns `apps/web` — **you
  must not touch `apps/web`.**

You run in a worktree; a unification pass integrates onto main.
Worktree gotchas: the `p4.9i1a-brahma-server-lane` memory (oracle-gen
`cd` resets cwd to MAIN — verify which tree your cargo runs against),
`parallel-round-disk-pressure` (pre-clean merged targets;
`CARGO_INCREMENTAL=0` on gates), `e2e-port-4319-cross-worktree-kill`
(you do NOT run full Playwright in-lane — see the gate).

**This is the campaign's final round.** Round 1 (P4.d12 ∥ P4.6bh ∥
P4.6bi) landed the episodic SPINE (the five columns, the pure
`episodic` module, weighting `episodicBonus`, the dated dynamic head).
Round 2 (P4.d13) made retrieval time/entity-aware and shipped the
deep-dive tools + the replay harness. **This lane lands the
creation side: memories are born with anchors** — the clocked
extraction prompts, the EVENT category and `kind`/`when`/`entities`
coercion, the turn-path `occurredAt` stamp, the first-write fallback
anchors, the fold-episode consolidation pass, the fold Timeline, the
gate date guard, and the housekeeping merge guard. The gate tier-3
family (`QT_ORACLE_GATE`) — SKIP since round 1 by design — **goes
green at `8bf3cb5f` in this lane.**

## Mandate

Deliver, verified against v4 `8bf3cb5f`: the creation-side extraction
prompts with the CLOCK block and EVENT machinery
(`renderClockBlock`, `EVENT_INSTRUCTION_BLOCK`, `kind`/`when`/`entities`
candidate coercion, `EVENT_EXTRA_SLOT`/`capCandidates`); the
memory-processor anchor resolution (`resolveCandidateAnchors` over the
already-ported pure `resolve_when_phrase`) and the turn-path
`occurredAt` stamp; the memory-service first-write
`applyEpisodicFallbackAnchors`; the gate date guard + reinforce-time
anchor upgrades + the anchor line in gate/create embedding text; the
NEW fold-episode pass module + its context-summary wiring; the fold
Timeline section; and the housekeeping `mergeSimilar` date guard. Each
unit ships with its differential; the round-3 oracle families this lane
owns regenerate at `8bf3cb5f`.

**Must NOT touch:** `services/build_context.rs`, `recall_history.rs`,
`services/commonplace_notifications.rs` (P4.d15's files); `apps/web`
(P4.9H1's tree); the schema (round 1's D23 re-dump is complete — this
round adds NO columns); the `compressMemories` task (UNPORTED tracked
deferral — see the compression-flip disposition in the Shared
contract).

## Survey-verified starting points (fresh, 2026-07-22)

Authority for every unit: `git diff 7e6d13e5..8bf3cb5f -- <file>` in
the v4 checkout — the whole creation-side delta is inside that diff.
Per-file stats: memory-gate +108, memory-processor +79, memory-service
+268 (round 2 already ported its search half — port the REMAINDER),
context-summary +32, chat-tasks +18, housekeeping +7,
compression-tasks +3, fold-episode-pass NEW (228 lines).
**Zero-diff files (do not port, families regen transitively):**
`carina-memory-extraction.ts`, `turn-transcript.ts`
(`lastMessageCreatedAt` pre-existed).

### v4 (all at `8bf3cb5f`; line numbers verified 2026-07-22)

| What | Where |
|---|---|
| `HARD_CANDIDATE_CAP = 2` / `EVENT_EXTRA_SLOT` / `isEventCandidate` / `capCandidates` | `lib/memory/cheap-llm-tasks/memory-tasks.ts:34–65` (`isEventCandidate` :45–47 — `kind === 'episodic' && (when || entities.length)`) |
| `ExtractionClock` + `WEEKDAYS` + `renderClockBlock` | `memory-tasks.ts:107–141` (the TYPE was round-2 ported for the TODAY line; `renderClockBlock` :124–141 — the CLOCK footer block — is THIS lane's) |
| `EVENT_INSTRUCTION_BLOCK` (verbatim, appended to both extraction bodies) | `memory-tasks.ts:161–180` |
| SELF prompt: EVENT pick-list entry + OUTPUT rows (`kind`/`when`/`entities`) | `memory-tasks.ts:268–310` (OUTPUT `kind` row :310) |
| OTHER prompt: same additions | `memory-tasks.ts:450–498` |
| Candidate coercion: `kind` normalize (`'episodic'` else `'semantic'`), `when`, `entities` | `memory-tasks.ts:600–623` |
| Cap application: per-subject `capCandidates` :684; single-subject :767–768 | `memory-tasks.ts` |
| `FOLD_EPISODE_PROMPT` + `extractEpisodesFromFold` (CLOCK line :1157; 0–2 episode records; per present character) | `memory-tasks.ts:1045–1250` (read the whole block) |
| `runFoldEpisodePass` (NEW module; writes `kind:'episodic'` through the normal gate; `relatedMemoryIds` links to same-window fragments) | `lib/memory/fold-episode-pass.ts` (228 lines — port whole; inputs :36, result :47, entry :56, extraction call :109) |
| Fold-pass wiring + `createdAt` on folded messages | `lib/chat/context-summary.ts` (+32; the call at :519) |
| Fold Timeline section (fifth section, append-only, ~30-line cap, dated one-liners) | `lib/memory/cheap-llm-tasks/chat-tasks.ts:654–663` (`FOLD_SUMMARY_PROMPT`) |
| `resolveCandidateAnchors` + the `occurredAt` stamp | `lib/memory/memory-processor.ts:234–247` (import :46; `ProcessedCandidate.occurredAt` :171; `sourceMessageCreatedAt` doc :215; use sites :257/:277/:307; `slice.lastMessageCreatedAt` :563; observer :672) |
| `applyEpisodicFallbackAnchors` (first-write fallback via `extractNovelDetails`; fires on AUTO/undefined-source proper-noun content) | `lib/memory/memory-service.ts:193` (applied at create :375) |
| Stale-comment cleanup (0.4/0.6 blend comments) | `memory-service.ts:262, 823–829` — verify against the diff |
| Gate date guard: `DATE_GUARD_DAYS`, `occasionsAreDistinct`, SKIP/REINFORCE → INSERT_RELATED downgrade | `lib/memory/memory-gate.ts:52–53, 201–216` (header comment rewrite :6–16) |
| Gate reinforce-time anchor upgrades (fill null `occurredAt`/`narrativeTime`; union `entities`) | `memory-gate.ts:280–330` (:324–325) |
| Anchor line in gate/create/reinforce **embedding text** (`buildMemoryEmbeddingText`) | find the call sites in the memory-gate/memory-service diff — the pure builder is already ported (`episodic.rs:113`); this lane wires it and VERIFIES round 1 didn't already (the gate family's non-inertness says v5 is missing at least the fallback path) |
| Housekeeping `mergeSimilar` date guard | `lib/memory/housekeeping.ts:16` (import) + :303 (`occasionsAreDistinct` → `continue`) |
| v4's own new tests (port case-for-case where a v5 unit test fits) | `lib/memory/__tests__/gate-date-guard.test.ts`, the episodic arms of `lib/memory/__tests__/episodic.test.ts` not yet mirrored |

### v5 landing sites (verified 2026-07-22)

| v4 counterpart | v5 file |
|---|---|
| memory-tasks creation prompts + coercion + caps + `renderClockBlock` + `FOLD_EPISODE_PROMPT`/`extractEpisodesFromFold` | `crates/quilltap-core/src/memory_tasks.rs` + `crates/quilltap-core/src/memory_tasks/prompt_text.rs` |
| memory-processor | `crates/quilltap-core/src/services/memory_processor.rs` |
| memory-gate | `crates/quilltap-core/src/memory_gate.rs` |
| memory-service (creation half) | `crates/quilltap-core/src/services/memory_service.rs` |
| fold-episode pass | NEW `crates/quilltap-core/src/services/fold_episode_pass.rs` (+ the `services/mod.rs` registration — this lane owns `services/mod.rs`) |
| context-summary wiring + fold prompt | `crates/quilltap-core/src/services/context_summary/` (the fold prompt bytes live in `context_summary/prompt_text.rs` — "Active threads" grep hit) |
| housekeeping | `crates/quilltap-core/src/services/housekeeping.rs` (`run_housekeeping` :256) |
| pure episodic helpers (ALREADY PORTED round 1 — consume, don't re-port) | `crates/quilltap-core/src/episodic.rs` (`build_memory_anchor_line` :74, `build_memory_embedding_text` :113, `resolve_when_phrase` :230, `event_reference_time_ms` :369) |
| `extractNovelDetails` (pre-existing pure port) | already ported — family `QT_ORACLE_EXTRACT_NOVEL_DETAILS`; the fallback consumes it |
| `compressMemories` | **UNPORTED tracked deferral** — `services/build_context.rs:2264` (P4.d15's file). No code here; see Shared contract §4 |

### Round-1 boundary findings this lane resolves (do not lose)

1. **The gate family is non-inert on AUTO-source proper-noun content**
   (`applyEpisodicFallbackAnchors` fires on first write). That is
   exactly the behavior this lane ports; after it lands, regenerate
   `QT_ORACLE_GATE` at `8bf3cb5f` and the family MUST go green with
   zero SKIP. If it stays red, the port is wrong — do not re-SKIP.
2. **The turn-path `occurredAt` stamp rides the processor prompt
   port** (its tier-3 pins prompt bytes) — that is why it waited for
   this lane. `turn-transcript.ts` needed no change.

## Tiered deliverables

### Tier 1 — must land (suggested sequence; each unit = one commit + differential)

1. **The clocked creation prompts + EVENT machinery** —
   `renderClockBlock`, `EVENT_INSTRUCTION_BLOCK`, the SELF/OTHER prompt
   additions, the `kind`/`when`/`entities` coercion,
   `EVENT_EXTRA_SLOT`/`capCandidates` (both cap sites). Regenerate the
   memory-tasks CREATION cases (`QT_ORACLE_MEMORY_TASKS`) at
   `8bf3cb5f`; extend the case corpus with EVENT-bearing turns
   (dated/placed candidates, the third-slot soft-raise, coercion edge
   cases — bad `kind`, non-array `entities`).
2. **The processor anchors** — `resolveCandidateAnchors` + the
   `occurredAt` stamp + `narrativeTime` gating + `sourceMessageCreatedAt`
   threading. Processor tier-3 (`QT_ORACLE_PROCESSOR`) regenerates at
   `8bf3cb5f` (prompt bytes + the stamp); add turn-path cases with
   `when` phrases (relative + absolute + unresolvable).
3. **First-write fallback + service cleanup** —
   `applyEpisodicFallbackAnchors` at the create path + the stale-comment
   sweep. Re-run the memories tier-2/routes families (round-1 vintage
   `8bf3cb5f`); add AUTO-source proper-noun create cases if the
   existing corpus doesn't exercise the fallback.
4. **The gate: date guard + reinforce anchors + embedding text** —
   `DATE_GUARD_DAYS`/`occasionsAreDistinct`, the INSERT_RELATED
   downgrade, reinforce-time anchor upgrades, `buildMemoryEmbeddingText`
   at every gate/create/reinforce embedding site. **Un-SKIP
   `QT_ORACLE_GATE`**: regenerate at `8bf3cb5f`, extend the corpus with
   the date-guard arms (same-embedding different-`occurredAt` pairs both
   above and below 7 days), green zero SKIP. Port
   `gate-date-guard.test.ts` case-for-case as unit tests.
5. **The fold-episode pass** — the NEW `fold_episode_pass.rs` module +
   `FOLD_EPISODE_PROMPT`/`extractEpisodesFromFold` + the
   context-summary wiring + `createdAt` on folded messages +
   `relatedMemoryIds` linking. NEW oracle family (tier-3, mocked
   cheap-LLM — canned episode JSON both sides, then tier-2 on the
   written rows; pick an unused env name, e.g. `QT_ORACLE_FOLD_EPISODE`,
   and register it — the `p4d3` memory: guessed names silently SKIP).
6. **The fold Timeline** — the fifth `FOLD_SUMMARY_PROMPT` section.
   Regenerate the context-summary family(ies)
   (`QT_ORACLE_CTXSUM`/`QT_ORACLE_CONTEXT_SUMMARY`/`QT_ORACLE_CHAT_TASKS`
   — identify which pin the fold prompt bytes) at `8bf3cb5f`.
7. **The housekeeping merge guard** — the `mergeSimilar` date-guard
   `continue`. Regenerate `QT_ORACLE_MEMHOUSEKEEPING`; add a
   distinct-occasions merge case.
8. **Transitive regens** — `QT_ORACLE_CARINA_MEM` (zero v4 diff; its
   prompts flow from memory-tasks — regen + green proves the
   threading), `QT_ORACLE_EXTRACT_NOVEL_DETAILS` re-run, and re-runs of
   `QT_ORACLE_EPISODIC`/`QT_ORACLE_WEIGHTING`/`QT_ORACLE_MEMORY_INJECTOR`
   as touched.

### Tier 2 — should land

9. The v4 test-file mirrors that don't fit a differential (episodic
   edge-case unit tests), and the seam-note sweep: any v5 comment that
   says "creation-side episodic is round 3" gets updated
   (`p4.6aw-rust-riders-hint-lane` memory: sweep seam notes when a seam
   closes).

### Tier 3 — explicit loud deferrals (enumerate, never stub silently)

- **`compressMemories`** stays the pre-existing tracked deferral (the
  keep/drop flip rides its eventual port — Shared contract §4; the
  seam-note update is P4.d15's, who owns the file).
- **Pascal `persist`** — deferred in v4 itself; nothing to port.
- The batched re-embed of legacy rows — v4 explicitly did not build it
  (design doc implementation note 4); mixed embedded-text vintages are
  the accepted state. NOT a v5 gap.

## The differential requirement (restated)

Every ported unit ships with an equivalence test driving v4's REAL code
(jest/tsx oracle → NDJSON → Rust diff). Closest recipes: the
`jest-real-db-oracle` + `tier3-completion-oracle` memories (mocked
cheap-LLM tier-3), `oracle-fire-and-forget-tail` (mutating cases),
`chain-depth-frozen-clock-artifact` (+1ms clock ticks), and the
**P4.d13/P4.d12 lane records** in `status-log.md` (freeze the WHOLE
`Date` constructor — `FakeDate` — not just `Date.now`; `TZ=UTC` on
episodic oracles; run differentials `--nocapture` and grep for SKIP —
`oracle-regen-silent-stale-pass`). The gate/processor tier-3 recipes
are in their original W-order records (`danger-differential-recipes`,
`jest-oracle-mechanism` memories).

## Fixtures

- This lane owns the harness-side memory fixtures it extends
  (gate/processor/memory-tasks/context-summary corpora under
  `harness/oracle/fixtures/` + their builders). When a fixture changes,
  THIS lane regenerates every family that consumes it, same commit.
- The committed `episodic-recall-{main,mount}.db` web fixture belongs
  to P4.d15's re-run set — do not touch it.
- No `crates/quilltap-web/tests/fixtures/` changes expected from this
  lane; if one becomes necessary, STOP and coordinate (it likely means
  the boundary is wrong).

## Shared contract (binding — identical text in P4.d14 and P4.d15)

1. **File ownership.** P4.d14 owns `memory_tasks.rs` +
   `memory_tasks/prompt_text.rs`, `services/memory_processor.rs`,
   `memory_gate.rs`, `services/memory_service.rs`,
   `services/context_summary/**`, NEW `services/fold_episode_pass.rs`,
   `services/housekeeping.rs`, `services/mod.rs`. P4.d15 owns
   `services/build_context.rs`, `recall_history.rs`,
   `services/commonplace_notifications.rs`. Neither lane edits the
   other's files, ever. `CHANGELOG` + `status-log.md` are append-only
   for everyone. `apps/web` belongs to P4.9H1 alone.
2. **Oracle-family ownership.** P4.d14 regenerates: `QT_ORACLE_GATE`
   (un-SKIP), `QT_ORACLE_PROCESSOR`, `QT_ORACLE_MEMORY_TASKS`,
   `QT_ORACLE_CARINA_MEM`, the context-summary/chat-tasks families,
   `QT_ORACLE_MEMHOUSEKEEPING`, the NEW fold-episode family, + re-runs
   of extract-novel-details/episodic/weighting/injector/memories
   families as touched. P4.d15 regenerates: `QT_ORACLE_BUILD_CONTEXT`
   (fixture extended), `QT_ORACLE_RECALL_HISTORY`, + re-runs of
   `QT_ORACLE_DISTILL`/`QT_ORACLE_RECALL_REPLAY`/`QT_ORACLE_VAULT_CONV`
   and the salon families as touched. **A red family in the OTHER
   lane's set = STOP and report — never "fix" across the boundary.**
3. **The seam between the lanes is the gate/service write API** as it
   exists on main today: P4.d15's mini-recap is retrieval-only and
   consumes NO new P4.d14 output; P4.d14's fold pass posts NO whisper
   (v4's fold pass writes memories only). If either lane finds v4
   violating this (a fold-pass whisper, a mini-recap write), STOP and
   report — the split was planned on it.
4. **The compression keep/drop flip needs NO round-3 code:** v4's
   change is +3 lines inside `MEMORY_COMPRESSION_PROMPT`
   (`compression-tasks.ts:79+`), and v5 has never ported
   `compressMemories` (tracked no-op deferral,
   `build_context.rs:2264`). P4.d15 (file owner) updates that
   deferral comment to record that the port target moved to
   `8bf3cb5f` (flip included). Neither lane ports the task.
5. **Version bumps.** Both server lanes bump `quilltap-core` +
   `quilltap-harness` per-commit per `.claude/commands/commit.md`; the
   unifier recounts (the `p4.6aw-ax-8-round-plan` memory: identical
   bumps merge silently — recount, don't trust the merge).

## Verification gate (before declaring done)

- `cargo fmt --all --check`; `cargo clippy --workspace --all-targets
  -D warnings` in BOTH feature sets (default +
  `--features quilltap-core/native-transport`); `cargo build
  --workspace --release`.
- `cargo test --workspace` with this lane's full env set; **re-run
  every family this lane owns BY NAME with `--nocapture` and grep for
  SKIP** (`QT_ORACLE_GATE` green zero SKIP is the lane's headline
  proof). Recount ok-lines on any failure (`p4.d12` memory: cargo test
  fail-fasts per TARGET).
- `apps/web` untouched: `git diff <branch-base>..HEAD -- apps/web`
  = 0 lines.
- **Full Playwright: DEFERRED TO THE UNIFIER** (three parallel lanes
  share e2e port 4319 — the cross-worktree-kill hazard; the SPA lane
  owns the in-round Playwright run).
- Per-commit rule: `.claude/commands/commit.md`. Append lane records to
  `status-log.md` as you go; update this order's status header at lane
  end.
