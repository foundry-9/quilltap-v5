# Work order P4.63 — the harness/differential follow-ups from the 4.9.0-push round

**Status:** OPEN (planned 2026-08-27).

## Preamble

- **v4 baseline: `8872d7efc`** (drift ledger §1, checked 2026-08-27). v4
  main HEAD at planning is `aec86a613` — one portable commit past the
  baseline, owned by the sibling P4.D130 (client-only wardrobe; nowhere near
  this lane's files).
- **Freshness probe at lane start** (ledger §2). Pass = checkout on `main`,
  clean, no commits past `aec86a613` (main) / `3a76b17df` (bugfix). Any
  mismatch: **STOP and report.**
- **Regen rule in force: PIN REQUIRED** — lane-unique detached worktree at
  **`8872d7efc`** (`/tmp/qt-v4-pin-p463-8872d7efc`, all three symlink
  classes, ledger §5.1; marker-grep the pin). Regens through
  `recipe_sweep.py --v4 <pin>`; never two sweeps concurrently.
- **Siblings:** P4.D130, P4.62, P4.64 — disjoint files; §Ownership binding.
  No Playwright in this lane.
- **Worktree:** per `carryout`; edit inside the worktree. `CHANGELOG` and
  `status-log.md` append-only.

## Mandate

Discharge the four named harness/differential follow-ups the 4.9.0-push
round recorded (phase-4 candidate 3): the bug-105 oracle-side divergence
tripwire, the `attach_mount_file_equivalence` pre-existing red, the
`a_fired_deadline_warns…` prefix-assert tightening, and the
`embedding_blob_binding_guard` reviewer notes. All four are harness-side
verification debt — **zero v5 production behavior is expected to change.**
If a diagnosis implicates production code (most plausible on the attach-file
red), STOP at the diagnosis, record the ordered shape in the lane record,
and escalate rather than fixing outside ownership.

**Must NOT touch:** anything a sibling owns; v5 production source (the one
sanctioned edit is the test-side assert inside `cheap_llm_exec.rs`'s
`#[cfg(test)]` region).

## Survey-verified starting points (fresh, 2026-08-27)

1. **The bug-105 oracle-side divergence arm** (`system_import_state`,
   `crates/quilltap-harness/tests/system_import_state.rs`). The facts, from
   the P4.D126 lane record (`status-log.md` ~`:89910`): v4's `e000d6bfc`
   seeding helper sits OUTSIDE `importConnectionProfiles`' per-item try and
   `(seeded.provider ?? '').toUpperCase()` throws on a non-string provider —
   one malformed profile aborts a whole v4 import
   (`Import failed: (seeded.provider ?? "").toUpperCase is not a function`),
   where pre-4.9 v4 named the item and continued. **v5 deliberately does
   not reproduce it** (the standing 2026-08-03 backup/restore ruling),
   pinned unit-side by
   `a_non_string_provider_is_named_and_does_not_abort_the_import`
   (`crates/quilltap-core/src/services/quilltap_import/profiles.rs:799`).
   The corpus item that exposed it was moved to a wrong-typed `modelName`
   so the five-named-warnings arm kept measuring. **What is missing:** an
   ORACLE tripwire — a divergence-aware case kind in `system_import_state`
   that (a) drives v4's REAL import with a non-string `provider` and
   records the abort, (b) drives v5 and asserts the named-warning +
   continued-import shape, and (c) is built to flip loudly (in the
   convergence-pin idiom, both directions) the day v4 fixes its bug 105
   (filed upstream as v4 `b6c6d7793`) — retirement by measurement, ledger
   §5.4. Check the family's existing case-kind machinery before inventing
   one; the divergence-aware precedent is the `CHUNK_DIVERGENCES` /
   ruled-row pattern.
2. **`attach_mount_file_equivalence` — the pre-existing red**
   (`crates/quilltap-harness/tests/attach_mount_file_equivalence.rs`,
   fixtures `attach-file-{main,mount,llmlogs}.db` +
   `harness/oracle/fixtures/build-attach-file-fixture.ts`). Recorded by the
   D129 sweep: red at BOTH pins (pre-existing, not the round's), "the
   oracle yields zero canned vision calls". Diagnose it: the likely
   classes, in order — a stale oracle mock starving v4's path (the
   P4.20/P4.36 class), a fixture-vintage gap (the P4.51 class), or venue
   rot in the recipe (P4.45's class). Two-pin attribution first (memory
   `two-pin-attribution-for-a-sweep-red`), then read the oracle case's
   mocks against v4's REAL call path at the pin. Land the repair
   red→green with the changed bytes grepped in the fresh NDJSON; if the
   true cause is a v5 production defect, escalate per the mandate.
3. **The `a_fired_deadline_warns_and_writes_the_ruled_error_row`
   prefix-assert** (`crates/quilltap-core/src/services/cheap_llm_exec.rs:1858`).
   The D129 reviewer note: the capture-layer target assert is a PREFIX
   match (memory `capture-layer-target-assert-is-a-prefix-match`) and this
   one is looser than it needs to be — a sibling target could satisfy it.
   One-line tightening to the exact target; run the test, then
   mutation-check that a wrong-target emission now reddens it.
4. **`embedding_blob_binding_guard` notes**
   (`crates/quilltap-harness/tests/embedding_blob_binding_guard.rs`): the
   whole-file `REGISTRY_ALLOWED` exemption is broader than the sites it
   excuses (narrow it to per-site), and the census has a comment-vacuity
   edge (a match inside a comment counts as a site — make the needle or the
   walk comment-aware, in the `db_error_key_guard` idiom). Both are
   guard-precision fixes; prove each by mutation (introduce the shape the
   guard should catch; it must redden).

## Tiered deliverables

**Tier 1 (must land):** items 3 and 4 (pure guard tightening, small and
closed-ended), and item 1's oracle-side divergence arm with its
convergence tripwire.

**Tier 2 (should land):** item 2 diagnosed to a root cause; landed
red→green when the cause is harness-side (the expected case).

**Tier 3 (explicit deferrals, loud):** item 2's fix if the cause is v5
production code (escalate with the ordered shape); any family whose recipe
header needs repair beyond the owned files — name it for the next
maintenance pass.

## The differential requirement

Item 1's new arm must drive v4's REAL `importConnectionProfiles` (never a
reimplementation) from the pin; the v5 side runs the real service. Items
3/4 are guard work — their proof is the mutation pass (a guard change that
cannot redden on the target defect proves nothing; memory
`mutation-must-be-verified-to-have-applied`). Item 2's repair ends with the
family green over a FRESH pinned oracle, `--nocapture`, zero SKIP, changed
bytes grepped (a canned vision call count > 0 is the discriminator the
red names).

## Fixtures

Consumes `attach-file-*.db` (+ `.meta.json`) — regenerate only through
`build-attach-file-fixture.ts` from the pin with the §5.2 delete-first
discipline, and re-run every consumer by name (check
`harness/tools/sweep-results/` and the recipe headers for who reads the
family's artifacts). `system_import_state`'s corpus: additive only — do not
move existing items (the D126 lesson: moving a corpus item changes what a
sibling arm measures; say what each moved/added item measures).

## Shared contract

None — the lanes meet nowhere. §Ownership is the whole contract.

## Ownership (binding, identical across the four orders)

| Lane | Exclusive ownership |
|---|---|
| **P4.D130** | `apps/web/src/app/wardrobe/**` (new `composed-outfits.*`, new `outfit-quick-pick.*`, `outfit-composer.*`, `equipped-slot-row.*` + specs), `apps/web/e2e/wardrobe-flow.spec.ts`, `apps/web/e2e/global-setup.ts`, `harness/oracle/fixtures/build-salon-fixture.ts`, `crates/quilltap-web/tests/fixtures/salon-*.db`, `crates/quilltap-core/src/services/builtin_mounts.rs`, `screens/new-chat/outfit-selector.ts` (comment only). Bumps: **SPA**; core + harness ONLY if the builtin_mounts fix lands. Sole Playwright runner. |
| **P4.62** | `crates/quilltap-web/src/system_data_routes.rs`, `files_routes.rs`, `llm_logs_routes.rs`; `crates/quilltap-harness/tests/web_edge_body_parse_guard.rs`; the system-data / files / llm-logs routes differential families + their oracle cases; any NEW parser module it creates in `quilltap-core` (own file only). Bumps: **web + harness** (+core if a parser lands there). |
| **P4.63** | `crates/quilltap-harness/tests/{system_import_state.rs, attach_mount_file_equivalence.rs, embedding_blob_binding_guard.rs}`; their oracle cases + `harness/oracle/fixtures/build-attach-file-fixture.ts` + the `attach-file-*` fixtures; `crates/quilltap-core/src/services/cheap_llm_exec.rs` (the one test assert only). Bumps: **harness** (+core for the assert edit). |
| **P4.64** | `crates/quilltap-core/src/services/home.rs` + any profiling scaffolding in its own new files. Bumps: **core** only if a fix lands. |

`docs/CHANGELOG.md`, `status-log.md`: append-only. Version conflicts at
unification: recount as base + total bumps.

## Verification gate (before declaring done)

- `cargo fmt --all --check`; clippy both feature sets, `-D warnings`.
- The owned families by name with `--nocapture`, zero SKIP, fresh pinned
  oracles, changed bytes grepped.
- The mutation proofs for items 1/3/4 recorded in the lane record (which
  mutation, which test reddened).
- `cargo test --workspace` (background + notification, full log).
- Per-commit rule: `.claude/commands/commit.md`. Lane record to
  `status-log.md`.
