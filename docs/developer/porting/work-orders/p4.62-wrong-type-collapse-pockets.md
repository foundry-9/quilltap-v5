# Work order P4.62 — the wrong-type-collapse pockets (P4.60's deferred census rows)

**Status:** OPEN (planned 2026-08-27).

## Preamble

- **v4 baseline: `8872d7efc`** (drift ledger §1, checked 2026-08-27). v4
  main HEAD at planning is `aec86a613` — one portable commit past the
  baseline, **owned by the sibling P4.D130** (client-only wardrobe; it does
  not touch this lane's surfaces).
- **Freshness probe at lane start** (ledger §2). Pass = checkout on `main`,
  clean, no commits past `aec86a613` (main) / `3a76b17df` (bugfix). Any
  mismatch: **STOP and report.**
- **Regen rule in force: PIN REQUIRED** (ledger §1) — every oracle regen and
  fixture build runs from a lane-unique detached worktree at **`8872d7efc`**
  (`/tmp/qt-v4-pin-p462-8872d7efc`, all three symlink classes, ledger §5.1;
  verify the pin by marker-grep). All family regens through
  `harness/tools/recipe_sweep.py --v4 <pin>`; never two sweeps concurrently.
- **Siblings:** P4.D130, P4.63, P4.64 — fully disjoint files; §Ownership
  (identical table in all four orders) is binding. **No Playwright in this
  lane** (P4.D130 is the round's sole runner; port 4319 is repo-wide).
- **Worktree:** per `carryout`; edit inside the worktree. `CHANGELOG` and
  `status-log.md` append-only.

## Mandate

Adjudicate, one by one, the three census pockets P4.60 deferred loud —
`system_data_routes.rs` (13 sites), `files_routes.rs` (the 5 caller-input
closure-form sites of its 7), and `llm_logs_routes.rs` (1 projection) —
exactly by P4.60's method: **read each v4 route first** (none of these is
pre-claimed as a defect), measure v5's current behavior, fix where divergent
by decoding through the `Request` enum (memory
`edge-must-decode-through-the-request-enum`; `double_option` where a
tri-state is load-bearing — the P4.D57/Taboo lesson), pin every verdict —
divergent-fixed arms red-first, faithful verdicts as recorded census
entries — and move `web_edge_body_parse_guard`'s counts to match, replacing
the "NOT adjudicated" prose with the measured verdicts.

The class, restated: `.and_then(Value::as_str)` (and the closure form
`.and_then(|v| v.as_str())`) collapses absent / explicit-null /
present-but-wrong-typed into one `None`. The third is the dangerous one:
v4's Zod refuses it with a 400 (or its handler coerces it deliberately)
while a collapsing edge silently reads "the caller didn't say".

**Guard-order discipline:** where a fix adds validation, match v4's check
ORDER (the `group_update`/`a6870c5a` lesson — a 400 where v4's find-first
answers 404 is a NEW divergence, not a fix; P4.D119's instructions-SET
repeat of the same class). Where v4 itself coerces
(`typeof x === 'number' ? x : default`) the collapse may be FAITHFUL —
record it, don't "fix" it. v4's refusal envelope: `.parse` uncaught = the
flat `Validation error` 400; `safeParse` = joined issue sentences (memory
`v4-refusal-envelope-parse-vs-safeparse`).

**Must NOT touch:** anything a sibling owns; the census rows P4.60 already
adjudicated FAITHFUL (`characters_routes.rs` 6, `qtap_target_route.rs` 3,
`qtap_routes.rs` 2, `custom_tools_routes.rs` 1 — re-flag nothing);
`llm_logs_routes.rs:53–66`'s QUERY parameters (listed by P4.57 only so no
sweep re-flags them).

## Survey-verified starting points (fresh, 2026-08-27)

**`system_data_routes.rs` — the 13 `and_then(Value::as_` sites**, with the
v4 territory each verdict needs (v4 paths from the ported units' own
comments; read the live route from the pin, not the comment):

| v5 site | Key(s) | Context / v4 route to read |
|---|---|---|
| `:149`, `:154` | `content`, `filename` | the Almanack save body (P4.37) — `app/api/v1/system/**` almanack route |
| `:239` | `action` (body, inside `?action=tasks-queue`) | the jobs-queue control body — v4's jobs route |
| `:264` | `progressId` | 36-char + `Uuid::parse_str` gate already mirrors Zod's uuid regex (the comment claims the quirk — verify against v4, then record) |
| `:277` | `reportId` | `capabilities-report-delete` |
| `:294` | `concurrency` (`as_i64`) | `job-concurrency` — the comment says v4 body `{concurrency}` → verb field `maxConcurrentJobs` |
| `:322`, `:333` | `confirm`, `keepArchivedCharacterBundles` (`as_bool`) | `delete-data` — the `d553f72a` absent-vs-explicit-false semantics are load-bearing: absent KEEPS bundles, only explicit `false` wipes. A collapse here may be exactly right or exactly wrong — measure. |
| `:342` | `threshold` (`as_f64`) | `memory-dedup` — v4: `typeof thresholdParam === 'number' ? thresholdParam : 0.80` (a deliberate coercion; likely FAITHFUL) |
| `:497`, `:502`, `:503` | `type`, `priority`, `maxAttempts` | the job-enqueue body — v4: `typeof priority === 'number' ? priority : undefined` |
| `:544` | the `str_field` helper | read every caller of the helper — a shared helper hides N sites behind 1 needle |

**`files_routes.rs` — the 7 closure-form sites** (census prose: FIVE are
caller input, TWO read the server's own response entity to pick 201 vs
200 — adjudicate the five, confirm-and-record the two):

| v5 site | Key(s) | Context |
|---|---|---|
| `:668`, `:677` | `content`, `encoding` | the mount-file write — v4 `mount-points/**` |
| `:826` | `tags[].tagId` | the multipart tags part |
| `:854`, `:857` | `str_field`-style chat-attach reads | v4 `chats/[id]/files/**` |
| `:893` | (entity echo) | 201-vs-200 pick — confirm entity-read, record |
| `:914` | `fileId` | the link leg |

**`llm_logs_routes.rs:127`** — the request-preview `content` projection
(reads `v.get("content")` off a stored request row — likely an entity
read, not caller input; measure and record).

**The census guard** — `crates/quilltap-harness/tests/
web_edge_body_parse_guard.rs`: `COLLAPSE_CENSUS` rows for
`system_data_routes.rs` (13) and `llm_logs_routes.rs` (1);
`CLOSURE_CENSUS` for `files_routes.rs` (7). Every count you change must
land with its verdict prose; a fixed site usually moves into
`PARSER_WIRING` with a wiring needle. ⚠ The closure needle `.as_str())` is
broad — if your fix introduces new legitimate occurrences elsewhere in the
file, the census will tell you; adjudicate, don't widen silently.

## Tiered deliverables

**Tier 1 (must land):** the complete adjudication table for all
13 + 5 + 1 sites — every site a measured verdict (DIVERGENT-FIXED with a
red-first differential arm, or FAITHFUL with the recorded reason), the
census guard updated to match, and the two entity-read `files_routes`
sites + the llm-logs projection confirmed-and-recorded. Zero
DIVERGENT-RECORDED rows without an escalation reason.

**Tier 2 (should land):** same-class neighbours discovered inside the
owned files while reading (P4.60's licence — the confirm-only pass found
two real divergences that way); consumed-case/count guards for any family
that lacks one.

**Tier 3 (explicit deferrals, loud):** anything whose fix belongs outside
the owned files — name the file, the divergence, and the ordered shape in
the lane record; do not reach across ownership.

## The differential requirement

Every DIVERGENT-FIXED verdict lands red-first in the owning family
(`system_data_routes` → the system-data routes family; the files sites →
the files routes family; check `ls crates/quilltap-harness/tests/` for the
exact family names and their recipe headers before inventing anything —
memory `recipe-header-conventions`). The oracle drives v4's REAL routes
(the jest route-harness pattern; the DB-free route-guard oracle — memory
`dbfree-route-guard-oracle` — fits refusal arms that never reach a
service). Keep scenario bodies raw `Value` built by key presence on both
sides (memory `harness-spec-typed-option-collapses-tristate`); a harness
leg that skips serde blinds the very tri-state under test. FAITHFUL
verdicts get a recorded arm only where cheap; the census prose is the
ledger otherwise (P4.60's precedent).

## Fixtures

Consumes the existing routes-family fixtures; must not mutate committed
fixtures except through their builders from the pin. If a family's fixture
must widen (e.g. a jobs row to make a control body reachable), this lane
owns regenerating the dependent oracles from the `8872d7efc` pin and
re-running every consumer by name (ledger §5.2 discipline).

## Shared contract

None — the lanes meet nowhere. §Ownership is the whole contract.

## Ownership (binding, identical across the four orders)

| Lane | Exclusive ownership |
|---|---|
| **P4.D130** | `apps/web/src/app/wardrobe/**` (new `composed-outfits.*`, new `outfit-quick-pick.*`, `outfit-composer.*`, `equipped-slot-row.*` + specs), `apps/web/e2e/wardrobe-flow.spec.ts`, `apps/web/e2e/global-setup.ts`, `harness/oracle/fixtures/build-salon-fixture.ts`, `crates/quilltap-web/tests/fixtures/salon-*.db`, `crates/quilltap-core/src/services/builtin_mounts.rs`, `screens/new-chat/outfit-selector.ts` (comment only). Bumps: **SPA**; core + harness ONLY if the builtin_mounts fix lands. Sole Playwright runner. |
| **P4.62** | `crates/quilltap-web/src/system_data_routes.rs`, `files_routes.rs`, `llm_logs_routes.rs`; `crates/quilltap-harness/tests/web_edge_body_parse_guard.rs`; the system-data / files / llm-logs routes differential families + their oracle cases; any NEW parser module it creates in `quilltap-core` (own file only). Bumps: **web + harness** (+core if a parser lands there). |
| **P4.63** | `crates/quilltap-harness/tests/{system_import_state.rs, attach_mount_file_equivalence.rs, embedding_blob_binding_guard.rs}`; their oracle cases + `harness/oracle/fixtures/build-attach-file-fixture.ts` + the `attach-file-*` fixtures; `crates/quilltap-core/src/services/cheap_llm_exec.rs` (the one test assert only). Bumps: **harness** (+core for the assert edit). |
| **P4.64** | `crates/quilltap-core/src/services/home.rs` + any profiling scaffolding in its own new files. Bumps: **core** only if a fix lands. |

`docs/CHANGELOG.md`, `status-log.md`: append-only. Version conflicts at
unification: recount as base + total bumps.

## Verification gate (before declaring done)

- `cargo fmt --all --check`; clippy both feature sets, `-D warnings`.
- The owned families by name with `--nocapture`, zero SKIP, over oracles
  regenerated fresh from the pin (grep the NDJSONs for the changed bytes —
  a green regen is not coverage).
- `web_edge_body_parse_guard` green with the moved counts.
- `cargo test --workspace` (background + notification, full log captured,
  never `| tail`).
- Per-commit rule: `.claude/commands/commit.md`. Lane record to
  `status-log.md`.
