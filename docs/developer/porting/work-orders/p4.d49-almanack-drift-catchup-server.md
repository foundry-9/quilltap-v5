# Work order P4.D49 — the `0cde7fbc` drift-proper catch-up: the llm_logs profile columns (D23), durationMs at the ported call sites, the token-usage un-zero, the remap additions

> **STATUS: CLOSED — UNIFIED on main (2026-08-05, the `f7f1a956` round).**
> ALL TIERS LANDED (branch `claude/almanack-drift-catchup-server-c2020d`,
> lane records in `status-log.md`): the D23 re-dump (the llm-logs
> partition's first; NO new indexes, as predicted — believed from the
> dump), the 18→20-column write spine with the pragma-guarded READ
> tolerance + the STRICT `create` / TOLERANT `create_for_restore` split
> (a ruled-divergence extension, documented on the methods), the six
> call sites, the token-usage un-zero (the enclave daily budget now
> BINDS — `daily_budget_binds_pre_turn`, mutation-proven, and the
> fixture rework that kept the other 17 cases exercising what they
> exist to prove), the remap additions with the `llm_log_profile_
> attribution` corpus case, the blast-radius sweep (which found and
> widened TEN hand-rolled 18-column DDLs — the §3 unification review
> found an ELEVENTH in `quilltap-web/tests/common/mod.rs`, fixed on the
> unify branch), the `QT_ORACLE_LLM_LOGS` env split, deliverable 9
> taken (the inspector family rebuilt with all three row shapes), and
> the jest-TZ defuse (`jest-zone-globalsetup.cjs` + zone markers both
> jest families). Escalated, NOT this lane's:
> `context_summary_service_tier3` + `memory_processor_tier3` cannot
> regenerate their oracles at `f7f1a956` (`no such table: llm_logs`,
> the P4.36 stale-mock class — a maintenance lane's). Pre-existing,
> recorded at the §3 review with a follow-up chip filed: the image-path
> `durationMs` zeros (`image_job_common.rs:256`,
> `generate_image.rs:2183`) that the new `<ms>` normalization conceals.
> Versions (after the round's recount): core 0.0.474, harness 0.0.403,
> host 0.0.59, web 0.0.61, tauri 0.0.6.
>
> *The original planning header follows for history.* This is
> phase-4.md candidate 0 — the catch-up the `7189a968` round
> record declared OWED when v4's `0cde7fbc` ("feat(almanack): rename
> and rewrite the capabilities report") landed mid-round. This lane
> absorbs the drift's deltas on ALREADY-PORTED surfaces; the Almanack
> report itself (unported surface) is P4.37/P4.38's, running in
> parallel.
>
> **v4 baseline for this round: `f7f1a956`** (v4 HEAD, 2026-08-05,
> clean tree — the CI/test-infra commit atop `0cde7fbc`). The previous
> baseline was `7189a968`; the two-commit drift is classified in the
> "Round planned" record in `status-log.md`: `0cde7fbc` → THIS lane +
> P4.37/P4.38; `f7f1a956` → NO-PORT (CI only; its one v5-relevant
> consequence — the unconditional jest TZ pin — is THIS lane's
> deliverable 8). **The baseline move is the UNIFIER's** — do NOT edit
> `CLAUDE.md` or `phase-4.md` on the branch.
>
> **Drift-check at lane start**: `git log f7f1a956..HEAD --oneline` +
> `git status --short` in `~/source/quilltap-server`. On drift/dirty,
> STOP and regenerate every oracle from a detached worktree PINNED at
> `f7f1a956` (recipe `oracle-regen-pinned-v4-worktree`), and report in
> the lane record. The `7189a968` round record warned v4's tree was
> dirty with in-flight almanack test work — `f7f1a956` landed that
> work, but re-check anyway.
>
> **Siblings (same round):** P4.37 (the Almanack server port — the §2
> shared contract below is BINDING between us), P4.38 (Almanack SPA),
> P4.39 (manifests-generator repair). None of them touch this lane's
> files (see Ownership).
>
> **Worktree note:** own branch, own checkout; no `git stash`/
> `git worktree`.

## Mandate

Absorb every `0cde7fbc` delta on ported v5 surfaces: the
`add-llm-logs-profile-columns-v1` schema change via the D23 re-dump
(**the first re-dump ever to touch the llm-logs partition**), the two
profile-id columns through the write spine (`db/llm_logs.rs`,
`llm_logging.rs`, and all six ported call sites), the durationMs
measurement at the call sites that lacked it, the
`getTotalTokenUsageSince` `$ne: null` un-zero (which makes the enclave
daily token budget bind on real spend for the first time), the
UUID-remap list additions with a measurable corpus, and the
blast-radius oracle regen. Also: disposition `f7f1a956` NO-PORT and
defuse its jest-TZ clobber for the two affected Chicago-leg recipes.
Do NOT touch: `api/types.rs`, `api/engine.rs`,
`services/creation_progress.rs`, any new almanack module (all
P4.37's), `apps/web/**` (P4.38's), the provider manifests (P4.39's).

## Survey-verified starting points (2026-08-05, fresh at `f7f1a956`)

### v4 — the drift anatomy

- **`lib/services/llm-logging.service.ts` (+10):** `LogLLMCallParams`
  gains `connectionProfileId?: string | null` (:48) and
  `imageProfileId?: string | null` (:50) after `modelName`;
  `durationMs?` (:78) pre-existed. The create payload adds
  `connectionProfileId: params.connectionProfileId ?? null` (:226) and
  `imageProfileId: params.imageProfileId ?? null` (:227), between
  `modelName` and `request`.
- **The eight v4 call sites** (six have ported v5 analogs, two are
  unported surface — see the v5 table below):
  - `lib/memory/cheap-llm-tasks/core-execution.ts` — `logCall`
    re-signatured `(response, temperature, durationMs)` with
    durationMs REQUIRED; adds `connectionProfileId:
    selection.connectionProfileId ?? null` (:191); all three send arms
    measure `Date.now()` deltas (:231-233, :238-241, :249-251). This
    shared path covers MEMORY_EXTRACTION, TITLE_GENERATION,
    SUMMARIZATION, CONTEXT_COMPRESSION, SCENE_STATE_TRACKING,
    IMAGE_PROMPT_CRAFTING.
  - `lib/services/chat-message/streaming.service.ts` — one line:
    `connectionProfileId: connectionProfile.id` (:450); durationMs
    already measured.
  - `lib/services/dangerous-content/gatekeeper.service.ts` —
    moderation log gains measured durationMs (:271-273, :296 — NO
    profile id; moderation providers are not connection profiles);
    classifier log gains `connectionProfileId:
    cheapLLMSelection.connectionProfileId ?? null` (:407) + measured
    durationMs (:384, :398, :417).
  - `lib/background-jobs/handlers/character-avatar.ts` —
    `imageProfileId: effectiveImageProfile.id` at both primary arms
    (:252, :272) and `reroute.profile.id` at both Concierge-reroute
    arms (:336, :369); durationMs already present.
  - `lib/background-jobs/handlers/story-background.ts` — same shape
    (:643, :662, :726, :755).
  - `lib/tools/handlers/image-generation-handler.ts` — same shape
    (:398, :421, :482, :510).
  - `lib/services/auto-configure.service.ts` and
    `lib/services/character-optimizer.service.ts` — **UNPORTED v5
    surface** (zero grep hits in crates/). No action here; name them
    so the eventual ports inherit the post-drift shape.
- **`lib/database/repositories/llm-logs.repository.ts` (+241/-8):**
  - `getTotalTokenUsage` (:440) and `getTotalTokenUsageSince` (:485)
    now filter `{ usage: { $exists: true } }` — the `$ne: null` that
    lowered to `usage != NULL` (never matches) is REMOVED, with a
    "must never be added back" why-comment at :445-450. Carry it.
  - `getTotalTokenUsageForChatSince` (:531) and
    `getTotalTokenUsageForRun` (:591): comment-only updates — they
    already used `$exists` alone. No behavior change.
  - **The `?standalone=true` quirk is UNCHANGED**: `findStandalone`
    (:205-228) still filters `$eq: null` (:215-217) → `= NULL` →
    never matches. v5's BROKEN-BUT-EXACT pin at `db/llm_logs.rs`
    :561-577 stays valid — verify, do not change.
  - The new Almanack aggregate methods (:715-925:
    `hasProfileAttributionColumns`, `getStatsByType`,
    `getStatsByProfile`, `getMedianDurationByProfile`,
    `getCacheStats` + three row interfaces) are **P4.37's territory**
    per §2 — this lane does NOT port them.
- **`lib/repositories/user-scoped.ts` (+34):** five userId-currying
  delegates to those aggregates — also P4.37's (sole caller is the
  Almanack).
- **`lib/schemas/llm-log.types.ts` (+8):** `LLMLogSchema` gains
  `connectionProfileId: UUIDSchema.nullable().optional()` and
  `imageProfileId: …` between `modelName` and `request` (after :153).
- **`migrations/scripts/add-llm-logs-profile-columns.ts` (+145):**
  opens the llm-logs partition, `ALTER TABLE ADD COLUMN "<col>" TEXT
  DEFAULT NULL` if missing + `CREATE INDEX IF NOT EXISTS
  "idx_llm_logs_<col>"`. `shouldRun` false on fresh installs.
  **generateDDL: the Zod change feeds it** (`llm-logs.repository.ts`
  :20,:51 → `generateDDL(collection, schema)`;
  `schema-translator.ts:412`), so fresh v4 tables carry both columns.
  **Index caveat:** `extractSchemaMetadata` (:263-280) auto-emits only
  `idx_<name>_userId` + `idx_<name>_createdAt`; the profile-column
  indexes come only from the MIGRATION (as do `idx_llm_logs_type` /
  `autonomousRunId`) — consistent with v5's current
  `fresh_schema.json:116-118` (llm_logs: 18 columns, 2 indexes).
  Expect the re-dump to add the two COLUMNS and (likely) NO new
  indexes — but believe the dump, not this prediction; `docs/
  developer/DDL.md:1415-1432` shows the hand-curated block WITH the
  indexes.
- **`lib/backup/restore/uuid-remap.ts` (+17/-1):** the llm-logs
  `remapFields` list grows from `['id','messageId','chatId',
  'characterId']` to include `'connectionProfileId'` and
  `'imageProfileId'` (profiles are themselves remapped; both nullable;
  `remapFields` only touches string fields so pre-4.9 NULL rows pass
  through).
- **`f7f1a956` (NO-PORT, CI-only) — the one v5-relevant consequence:**
  v4's `jest.config.ts` and `jest.integration.config.ts` now set
  `process.env.TZ = 'UTC'` UNCONDITIONALLY before Jest forks workers.
  An env-passed `TZ=America/Chicago npx jest …` oracle regen is
  clobbered to UTC. See deliverable 8.

### v5 — the landing zones

- **`crates/quilltap-core/src/db/llm_logs.rs`:** 18 columns today.
  `LOG_COLUMNS` (:397-401), INSERT in `create()` (:438-441, doc
  comment :424-426 "in LLMLogSchema field order"), update path
  `durationMs = ?` (:781), in-file test fixtures at :1149, :1162,
  :1408 (all 18-column). `get_total_token_usage_for_run` (:895, NOT
  broken), `get_total_token_usage_since` (:927) with the
  BROKEN-BUT-EXACT annotation :912-914 and unit pins :1258-1286.
  **v5 has no plain `get_total_token_usage`** (never ported — no
  consumer; do not add one).
- **`crates/quilltap-core/src/services/llm_logging.rs`:**
  `LogLlmCallParams` (:115-134) with `duration_ms: Option<f64>`
  (:133), no profile fields; create payload around :310.
- **The six ported call sites:**
  | v4 site | v5 analog | Needs |
  |---|---|---|
  | cheap-llm core-execution | `services/cheap_llm_exec.rs` :319/:390 (both arms `duration_ms: None` at :315/:388) | profile id + measured duration, both arms |
  | streaming.service | `services/primary_stream.rs:935` (`duration_ms: Some(0.0)` under pinned clocks :933) | `connection_profile_id` |
  | gatekeeper | `services/dangerous_content/gatekeeper.rs` :514 (moderation) / :604 (classifier); both `duration_ms: None` | duration both; profile id on classifier only |
  | character-avatar + story-background | consolidated `services/image_job_common.rs` `log_image_gen_job` (:211; called :319/:338/:441/:460; `duration_ms: Some(0.0)` :252) via `character_avatar_job.rs:39` / `story_background_job.rs:44` | `image_profile_id` threading through all four arms |
  | image-generation-handler | `tools/generate_image.rs:2122` | `image_profile_id` |
  Note `crates/quilltap-core/src/services/moderation_wire.rs:51`
  references the moderation-path logLLMCall serialization — in the
  gatekeeper-duration blast radius.
- **Duration measurement under v5's clocks:** v4 measures `Date.now()`
  deltas. v5 measures through the injected time driver (the P4.D42
  lesson: in-core timeouts made the time driver load-bearing) — under
  the harness's pinned clocks a measured delta is deterministic (0 or
  the pinned step); the differential normalization must treat
  `durationMs` as legitimately nondeterministic where live clocks run.
- **`crates/quilltap-core/src/enclave/step.rs`:** the pre-turn budget
  read :528-535 and post-turn re-check :803-805 both carry the
  BROKEN-BUT-EXACT always-zero annotations (which explicitly
  anticipate this fix: "any future fix stays aligned"). The per-run
  sum :768 is NOT broken. **Consequence of the un-zero:**
  `daily_tokens_spent` (:524/:536) and `post_daily_spent` (:800-808)
  stop being constant 0 → `check_budget`'s dailyTokenBudget arm binds
  on real spend for the first time, pre-turn (grace-turn / End
  transitions :545-560) and post-turn. Rooms with a daily budget
  configured begin pausing/ending where they ran unbounded.
- **The D23 machinery:** dump target
  `crates/quilltap-core/src/services/provisioning/fresh_schema.json`
  (llm_logs at :116-118), `include_str!` at `provisioning/*.rs:70`;
  the dumper `harness/oracle/provision/dump-fresh-schema.ts`; the rule
  at `phase-4.md:312-337`; the tripwire `provisioning_equivalence.rs`
  :8-11 (diffs `sqlite_master` across all three partitions, incl. the
  llm-logs one at :245/:345).
- **`crates/quilltap-core/src/services/backup/uuid_remap.rs`
  :211-218:** the llm-logs entry `fields_owned(r, l, &["id",
  "messageId", "chatId", "characterId"], target_user_id)` — add the
  two fields. Corpus: `backup_uuid_remap_equivalence.rs` (sha256-
  pinned per NDJSON line — regen is obligatory, P4.9G6's hash rule).

### Blast radius — harness families touching llm_logs (survey 2026-08-05)

- **Diff the llm_logs table directly:** `enclave_step_tier3_
  equivalence.rs` (:364/:620/:638 — also pins the zero-sum budget
  behavior), `llm_logs_tier2_equivalence.rs` (:373),
  `llm_log_cleanup_equivalence.rs` (:375/:382/:443-444).
- **Route payloads carrying parsed rows** (v4's Zod parse now emits
  the two new keys where columns exist): `llm_logs_routes_
  equivalence.rs`, `courier_images_routes_equivalence.rs`,
  `cost_background_routes_equivalence.rs`,
  `self_inventory_equivalence.rs`.
- **Backup/export/restore/import families carrying llmLogs:**
  `system_backup_equivalence.rs`, `system_export_equivalence.rs`,
  `system_import_equivalence.rs`, `system_import_state.rs`,
  `system_restore_state.rs`, `restore_vintage_state.rs`,
  `system_delete_data_equivalence.rs`,
  `backup_uuid_remap_equivalence.rs`.
- **The schema tripwire:** `provisioning_equivalence.rs` — WILL go red
  at the first regen, by design; this lane's re-dump turns it green.
- **Verify-only sweep set** (fixture families carry an llm-logs
  partition but don't diff the table): `pascal_*`,
  `answer_confirmation`, `attach_mount_file`, `orchestrator_tier3`,
  `system_jobs_*` — regen-and-rerun, expect neutral.

## Deliverables

### Tier 1 — must land

1. **The D23 re-dump.** Run `dump-fresh-schema.ts` against v4 at
   `f7f1a956`; adopt the emitted `fresh_schema.json` verbatim (never
   by hand). Verify the llm_logs block gains exactly the two columns;
   resolve the index question by believing the dump (record which way
   it went in the lane record — the migration-only indexes are
   expected NOT to appear). `provisioning_equivalence` red→green over
   a fresh oracle is the proof.
2. **The write spine.** `db/llm_logs.rs`: 18 → 20 columns
   (`connectionProfileId`, `imageProfileId` between `modelName` and
   `request`, LLMLogSchema field order), through `LOG_COLUMNS`,
   `create()`, the read/row structs, the update path, and the in-file
   test fixtures. `llm_logging.rs`: the two `Option<Option<…>>`-free
   optional fields on `LogLlmCallParams` (both `?? null` coalesced in
   the payload, v4's exact position).
3. **The six call sites** per the table above: profile ids threaded
   from the exact v4 sources (`selection.connectionProfileId ?? null`,
   `connectionProfile.id`, `cheapLLMSelection.connectionProfileId ??
   null`, `effectiveImageProfile.id` / `reroute.profile.id`, etc.) and
   measured durations where v4 added them (cheap-exec both arms,
   gatekeeper both logs). The moderation log gets duration but NO
   profile id — carry v4's reasoning comment.
4. **The token-usage un-zero.** `get_total_token_usage_since` filters
   `usage IS NOT NULL` (v4's `$exists: true` lowering); carry v4's
   :445-450 "must never be added back" why-comment; retire the
   BROKEN-BUT-EXACT annotations in `db/llm_logs.rs` (:912-914, the
   :1258-1286 unit pins move to pinning the FIXED behavior) and
   `enclave/step.rs` (:528-535, :803-805). Do NOT add the plain
   no-since variant (no consumer). The `find_standalone` `$eq: null`
   pin STAYS (v4 didn't fix it) — re-affirm it in the lane record.
5. **The enclave budget consequence, differential-proven.** Regenerate
   `enclave_step_tier3` fresh (TZ=UTC pins apply) and extend the case
   family so the daily-budget arm is EXERCISED: at least one case
   where real summed spend trips the budget (pre-turn grace/End) that
   the old always-zero behavior would have let run. First-run-green
   must be mutation-proven (re-introduce the `$ne` filter → the case
   goes red) per the D24 rule.
6. **The remap additions, measurable.** `uuid_remap.rs` gains the two
   fields; regenerate the sha-pinned corpus at `f7f1a956`. Then make
   the claim MEASURABLE (the P4.D31 lesson — the minted-`<N>`
   normalizer labels correct and wrong ids identically): ensure the
   corpus (and at least one committed restore archive, if the archives
   feed a remap-exercising family) carries llm_logs rows with NON-NULL
   profile ids pointing at remapped profiles, compare those ids
   literally, and mutation-test the which-id-won arm.
7. **The blast-radius regen.** Regenerate + re-run BY NAME
   (`--nocapture`, zero SKIP, grep the NDJSON for a fresh-baseline
   marker) every family in the blast-radius list above, over oracles
   fresh at `f7f1a956`. Route families over pre-migration fixtures
   should stay green (the optional Zod keys are simply absent on
   column-less DBs — verify both sides agree); families over
   post-migration data show the new keys. Run the verify-only sweep
   set last and record neutrality.
8. **The `f7f1a956` disposition + the jest-TZ defuse.** Record the
   NO-PORT (CI/workflows/snapshot only). Then handle the clobber —
   v4's jest configs now force TZ=UTC before workers fork, so of the
   four v5 Chicago-leg recipe families: `day_references_equivalence`
   (tsx, :27-29) and `enclave_cron_equivalence` (tsx, :14-16) are SAFE
   (both validate the recorded zone); `llm_log_cleanup_equivalence`
   (jest — LOUD: the case throws on a TZ/leg mismatch, case :108-109,
   harness check :251-261) and `distill_search_extraction_equivalence`
   (jest — SEMI-SILENT: no TZ marker in the NDJSON; the only
   downstream catch is the `differing >= 5` corpus-sensitivity
   assertion :202-211, which would read as a misleading red). Fix
   both jest recipes so the Chicago leg genuinely runs Chicago (e.g.
   regen from a worktree with the TZ line neutralized, or inject the
   zone by a mechanism the config can't override), add a TZ marker +
   harness-side check to the distill family so a future clobber is
   LOUD, update the recipe headers, and note the `recipe_sweep.py`
   implication (the sweep runs these same recipes). Write the memory
   note.

### Tier 2 — should land

9. **Column-blindness closure for the read families.** The route
   families' committed fixtures predate the migration, so their
   green runs are structurally blind to the new columns. Either
   extend ONE llm-logs-reading family's fixture (the `inspector-*`
   family is purpose-built for log reads) with attributed rows and
   show the parsed keys flow through list/get — or record explicitly
   in the lane record why coverage elsewhere (P4.37's
   exact-attribution fixture arm + this lane's remap corpus) is
   sufficient. Do not leave the question unasked.

### Tier 3 — explicitly out of scope (loud, named)

- The Almanack aggregates + attribution probe + user-scoped delegates
  (P4.37's, per §2).
- `auto-configure` and `character-optimizer` call sites — UNPORTED v5
  surface; named here so their future ports inherit the post-drift
  shape (profile ids + measured durations).
- The plain `getTotalTokenUsage` (no v5 consumer).
- `findStandalone`'s `$eq: null` — v4 did not fix it; the pin stands.

## The differential requirement, restated

Every changed unit ships with its equivalence run against v4's REAL
code at `f7f1a956`: the D23 re-dump is proven by
`provisioning_equivalence`; the write-spine and call-site changes by
the regenerated table-diff families (enclave_step_tier3,
llm_logs_tier2, llm_log_cleanup) whose llm_logs dumps now carry the
new columns and measured durations both sides; the un-zero by the
extended enclave budget case (deliverable 5, mutation-proven); the
remap by the sha-pinned corpus + literal-id assertions (deliverable
6). Closest recipes: the family headers themselves (every family
carries its regen command — `harness-recipes-are-runnable`), plus
memory notes `jest-oracle-mechanism`, `oracle-regen-silent-stale-pass`
(grep every regenerated NDJSON for a fresh marker), and
`sweep-recipes-write-into-the-repo` (run fixture-mutating recipes
against /tmp copies).

## Fixtures

- Consumes (regen only, no rebuild): every committed fixture named in
  the blast-radius list. Point oracles at the COMMITTED DBs; never
  rebuild in place (the standing committed-fixture rule).
- May extend: one llm-logs-reading family's fixture (deliverable 9)
  and the remap corpus/archives (deliverable 6). Whoever extends a
  fixture re-runs EVERY family consuming it, by name.

## §2 Shared contract — the llm_logs profile columns (BINDING; identical in P4.D49 and P4.37)

- The columns: `connectionProfileId`, `imageProfileId` — nullable TEXT
  on `llm_logs` (llm-logs partition), positioned between `modelName`
  and `request` in LLMLogSchema field order.
- **P4.D49 OWNS:** the D23 re-dump (`provisioning/fresh_schema.json` +
  the dumper), `db/llm_logs.rs`, `services/llm_logging.rs`, every
  write/call site, `uuid_remap.rs`, the `$ne: null` fix, and
  `enclave/step.rs`.
- **P4.37 OWNS:** the read-side aggregates + the attribution probe
  (`hasProfileAttributionColumns` = PRAGMA table_info) — these live in
  P4.37's almanack module as raw SQL over the llm-logs connection,
  mirroring v4's fail-soft `aggregate()` helper (v4 repo :715-925),
  NOT in `db/llm_logs.rs`. P4.37 does not touch `db/llm_logs.rs`,
  `llm_logging.rs`, `enclave/step.rs`, or `fresh_schema.json`; P4.D49
  does not touch the almanack module, `api/types.rs`, `api/engine.rs`,
  or `services/creation_progress.rs`.
- The probe makes the lanes runtime-independent: P4.37's collectors
  take the approximate `(provider || '/' || modelName)` arm on any DB
  without the columns (including v5-provisioned instances until this
  lane's re-dump lands) and the exact arm on any DB with them (its own
  v4-built fixtures). Both arms are differential-exercised in P4.37's
  lane regardless of this lane's progress; nothing in either lane
  gates on the other at build time.

## Ownership (binding)

- **This lane owns:** `crates/quilltap-core/src/db/llm_logs.rs`,
  `services/llm_logging.rs`, `services/cheap_llm_exec.rs`,
  `services/primary_stream.rs` (the log call site),
  `services/dangerous_content/gatekeeper.rs`,
  `services/image_job_common.rs`, `tools/generate_image.rs` (the log
  call site), `enclave/step.rs`, `services/backup/uuid_remap.rs`,
  `services/provisioning/fresh_schema.json` (+ dumper-adjacent files),
  and the harness/oracle files of every family in the blast-radius
  list. **Version bumps:** core + harness (host only if a seam
  signature moves — flag it if so).
- **Do NOT touch:** `api/types.rs`, `api/engine.rs`,
  `services/creation_progress.rs`, any `almanack` module,
  `quilltap-web/src/**`, `apps/web/**`, the provider manifests /
  generator.
- `CHANGELOG` + `status-log.md` are append-only (union-merge at
  unification).

## Verification gate before declaring done

1. `cargo fmt --all --check`; clippy `-D warnings` on BOTH feature
   sets; `cargo build --release`.
2. Every blast-radius family BY NAME, `--nocapture`, zero SKIP, over
   oracles regenerated FRESH at `f7f1a956` (NDJSON fresh-marker
   grepped); `provisioning_equivalence` green over the re-dump; the
   mutation proofs (deliverables 5 and 6) recorded with their
   red-fingerprints.
3. `cargo test --workspace --no-fail-fast` with the full env block.
4. No `apps/web` change → no SPA gate owed (state it).
5. Per-commit rule: follow `.claude/commands/commit.md`.
