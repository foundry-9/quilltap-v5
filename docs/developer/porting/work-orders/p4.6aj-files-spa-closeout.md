# Work order P4.6aj — the files SPA close-out (delete-associations dialog + live-path verification)

> **STATUS: OPEN.**
>
> Lane C of the FOUR-lane P4.6ah ∥ P4.6ai ∥ P4.6aj ∥ P4.d4 round (the
> "finish P4.6ae + catch up from v4" round, planned 2026-07-14).
> SPA-ONLY. This lane finishes the client half of the files-family
> story: it consumes the itemized `FILE_HAS_ASSOCIATIONS` envelope lane
> A emits (replacing the current plain-alert fallback with a real
> force/dissociate dialog), and it verifies the two live paths lanes A
> and B unlock — the composer file-attach (`chatFileUpload`) and the
> `imageProfileGenerate` generate dialog.
>
> **v4 reference baseline: HEAD `02865bdb`** — this lane writes NO Rust
> and ports no v4 behavior, but drift-check anyway (`git log
> 02865bdb..HEAD --oneline`) and STOP if HEAD moved. Sibling lanes:
> `p4.6ah-files-write-maintenance-server.md` (emits the envelope + the
> live upload legs), `p4.6ai-image-generation-unrefusal.md` (the live
> generate path), `p4.d4-skip-signal-drift.md`. The **Shared contract**
> and **Ownership** sections below are binding and reproduced verbatim
> in all four orders. You build against the PINNED contract shapes
> (mocking the envelope where needed); the unifier activates the
> fixture-guarded e2e beats once the server lanes land.

## Mandate

Three SPA deliverables, all consuming lanes A/B contracts that are
pinned verbatim below:

1. **The delete-associations dialog.** When `fileDelete` refuses with
   `code: 'FILE_HAS_ASSOCIATIONS'`, the SPA today shows a plain alert.
   Replace it with an itemized dialog listing the associated characters
   (`{id, name, usage}`) and messages (`{chatId, chatName, messageId}`)
   from the envelope's `associations` payload, offering the two v4
   escape hatches: **force** (delete anyway → resend `fileDelete` with
   `force: true`) and **dissociate** (strip the references first →
   `fileDelete` with `dissociate: true`). Keep v4's steampunk register
   on the copy.
2. **Composer file-attach verification.** The composer attach path
   (`chatFileUpload` via `POST /api/v1/chats/{id}/files`) went from a
   404-degraded inline state to live under lane A. Confirm the SPA
   client (`chat-files.api.ts:42-81`, already LOCKED) drives it, and the
   duplicate-conflict flow (`{duplicate, conflictType, existingFile,
   newFile}` → the resolution picker) works end-to-end.
3. **Generate-dialog verification.** `imageProfileGenerate` went live
   under lane B. Confirm `generate-image-dialog.ts:158-174` drives the
   live path and renders the returned images (`{id, filename, filepath,
   mimeType}` + `expandedPrompt`).

Do NOT touch `crates/**` (lanes A/B/D own all Rust),
`apps/web/src/app/files/**` (the Scriptorium `qt-file-manager`, P4.6aa
— NOT this round's general browser), or `apps/web/src/app/chat/render/**`
(the pinned unified-11.0.5 render pipeline). Lane C's screens live under
`apps/web/src/app/screens/files/**`.

## Survey-verified starting points (2026-07-14)

- **The SPA delete flow (today's plain-alert fallback):** the general
  files delete lives under `apps/web/src/app/screens/files/**` (P4.6af's
  `/files` FileBrowser vertical — NOT `app/files/**`). Locate the
  `fileDelete` call site and its current error handling; the
  FILE_HAS_ASSOCIATIONS branch currently degrades to an alert per the
  P4.6ae header ("The SPA delete flow falls back to a plain alert until
  the envelope carries the itemized payload"). Fresh-survey the exact
  component + method at lane start.
- **The composer attach client (LOCKED — do not change its shape):**
  `apps/web/src/app/chat/chat-files.api.ts:42-81` POSTs
  `multipart/form-data` to `/api/v1/chats/{chatId}/files` with fields
  `file`, `resolution`, `conflictingFileId` and expects `{file}` |
  `{duplicate, conflictType, existingFile, newFile}` | `{error}`.
- **The generate dialog (LOCKED shape):**
  `apps/web/src/app/images/generate-image-dialog.ts:158-174` sends
  `{type:'imageProfileGenerate', imageProfileId, prompt, chatId, count:
  1}` and reads `{success, data:[{id,filename,filepath,mimeType}],
  expandedPrompt}`.
- **core-contract.ts / core-client.ts:** lane C is the SOLE author.
  Add the `FileAssociations` type (matching A's wire shape) and wire the
  delete-flow consumption. Most files-family / image client methods
  already ship (P4.6af/ac) — add only what's missing.
- **e2e gotchas carried forward (MEMORY):** shared-server specs sort
  AFTER `foundation.spec.ts` (`[[p4.6af-files-family-spa]]`); glyph
  buttons match by `getByTitle` accname; virtualized/async chip counts
  must SETTLE (two agreeing reads ~450ms apart) before asserting
  (`[[p4.6ae-af-ag-round-plan]]`); a beat's probe-guard must cover the
  seam it SEEDS through; run Playwright from `apps/web` with the local
  binary (not `npx`); zoneless specs need a signal Host and
  `whenStable` ≠ fetch microtasks (`[[p4.6ag-prosemirror-editor]]`,
  `[[p4.6x-document-mode-spa]]`).

## Tiered deliverables

**Tier 1 (must land):**

1. The `FileAssociations` type in `core-contract.ts` + the delete-flow
   consumption in `core-client.ts` (surface `code` +`associations` off
   the error envelope to the caller).
2. The delete-associations dialog component (itemized characters +
   messages; force + dissociate actions re-issuing `fileDelete` with
   the right flag), replacing the plain-alert fallback.
3. A component/unit spec (`ng test`) for the dialog: renders the
   itemized payload; force resends `{force:true}`; dissociate resends
   `{dissociate:true}`; a clean delete (no associations) skips the
   dialog.

**Tier 2 (should land):**

4. An e2e beat exercising the delete-associations dialog over a seeded
   linked file (sorting AFTER `foundation.spec.ts`; the fixture is lane
   A's — the beat self-activates at unification if it needs A's seeded
   rows). Extend the existing files-flow / composer / generate beats to
   assert the now-live paths (composer attach success + duplicate
   picker; generate dialog renders images) — guarded so they activate
   when the server legs land.

**Tier 3 (explicit deferrals — loud, enumerated):**

- Any generate-dialog affordance beyond the LOCKED four params
  (size/quality/style/aspect/negativePrompt) — v4 accepts them but the
  variant/SPA send only the four; NOT this lane's scope.

## The verification requirement

This is a UI lane, not a port — there is no v4 oracle to diff. The
proof is: `ng test` green (incl. the new dialog spec), `ng build`
clean, and the e2e beats (activated at unification) exercising the live
paths without NEW Playwright failures. Build the dialog against the
PINNED envelope shape (mock the `FILE_HAS_ASSOCIATIONS` response in the
unit spec); do not block on lane A's server landing.

## Fixtures

Lane C owns NO server fixture. The delete-associations e2e beat needs a
seeded file linked to a character + a chat message — that seed is in
lane A's `files-{main,mount}.db` (the P4.6ae fixture already carries an
"associations arm" row per its Fixtures section). If a beat needs a seed
A's fixture lacks, request it in the round (lane A owns the fixture);
do not mint a server fixture here. Any `e2e/support/**` seeding helper
lane C adds is SPA-side only.

## Shared contract (BINDING — identical in p4.6ah / p4.6ai / p4.6aj / p4.d4)

### New / going-live Request variants (wire `type` names; Rust mirrors PascalCase)

**Lane A (files write + maintenance):** `fileUpload {filename,
contentType, data (base64), tags?, projectId?, folderPath?}` → `{data:
FileEntry}` (REST leg; 201/200); `chatFileUpload {chatId, filename,
contentType, data (base64), resolution?, conflictingFileId?}` → `{file:
{id, filename, filepath, mimeType, size, url}}` | `{duplicate: true,
conflictType, existingFile, newFile}`; `chatFileLink {chatId, fileId}`
→ `{file}`; `filesGenerateThumbnails {fileIds, size?}` → `{total,
generated, cached, errors}`; `filesCleanupStale {dryRun?}` → `{total,
stale, deleted, dryRun, staleFiles, errors?}`; `filesCleanupOrphans
{mode, dryRun?}` → v4 dry/wet; `fileDelete {fileId, force?,
dissociate?}` → `{success: true}` | the itemized
`FILE_HAS_ASSOCIATIONS` 400 envelope.

**The `FILE_HAS_ASSOCIATIONS` envelope (lane A EMITS, THIS LANE
CONSUMES):** on the un-forced linked-file delete, the error carries
`code: 'FILE_HAS_ASSOCIATIONS'`, `message: 'File is linked to other
items'`, and `associations: {characters: [{id, name, usage}], messages:
[{chatId, chatName, messageId}]}`. `FileEntry` = v4 `serializeFileEntry`
(BOTH `originalFilename` AND `filename`, `filepath` =
`/api/v1/files/{id}`, `folderPath` RESOLVED, `fileStatus` defaulted
`'ok'`).

**REST legs (web edge, lane A):** `POST /api/v1/files?action=upload`
multipart → `{data: FileEntry}` 201/200; `POST /api/v1/chats/{id}/files`
multipart (`file`/`resolution?`/`conflictingFileId?`) → `{file}` |
duplicate envelope | `{error}` (**SPA client LOCKED**,
`chat-files.api.ts:42-81`); the same route with JSON `{action:'link',
fileId}` → `{file}`. Byte GETs FROZEN.

**Lane B (image generation):** `imageProfileGenerate {imageProfileId,
prompt, chatId?, count?}` → `{success: true, data: GeneratedImage[],
expandedPrompt, metadata}` (201). `GeneratedImage` = `{id, filename,
filepath, mimeType}`; `metadata` = `{originalPrompt, provider, model,
count}`. `imageProfileValidateKey` / `imageProfileListModels` STAY
refusal-armed. The generate dialog (`generate-image-dialog.ts:158-174`)
already sends the four params and reads the four-field images +
`expandedPrompt` — THIS LANE verifies the live path, no new client
shape.

**Lane D (skip-signal drift):** no contract changes.

### The two-core-dispatch-writer rule (lanes A + B)

Lanes A and B are the round's ONLY writers to `types.rs` / `engine.rs`
/ `api/mod.rs` (delimited blocks `// === P4.6ah ===` / `// === P4.6ai
===`). Lane A additionally makes the one in-place `CoreError.
associations` + `Response`-helper edit. Lane C writes NO Rust.

### core-contract.ts ownership (single-author rule)

**Lane C OWNS `apps/web/src/app/core/core-contract.ts` +
`core-client.ts`** and is the SOLE author of any files-family /
image-generation client additions there (the `FileAssociations` type +
the delete-flow consumption). Lanes A, B, D do NOT touch either file.

### Name-collision warning

`apps/web/src/app/files/**` is the SCRIPTORIUM `qt-file-manager`
(P4.6aa) — NOT this round's general browser. Lane C's screens live in
`apps/web/src/app/screens/files/**`. Do not confuse the two; nobody
edits the file manager this round. If any name or field must change, ALL
FOUR orders change and the change is reported at unification.

## Ownership (BINDING across all four lanes)

- **Lane A (p4.6ah):** `crates/**` (`api/files.rs`,
  `services/chat_files.rs`, `services/file_storage.rs`, `db/files.rs`,
  `db/folders.rs`, `folder_utils.rs`, `chat_media.rs`, `api/types.rs`
  incl. the `CoreError.associations` field + `Response` helpers + its
  `// === P4.6ah ===` block, `api/engine.rs` its `// === P4.6ah ===`
  block, `api/mod.rs`); `quilltap-web` (`files_routes.rs` new
  POST/DELETE + chat-files multipart/link; `lib.rs`); the
  `files-{main,mount}.db` fixture + builder (+ any courier-images
  extension + oracle regen); `harness/oracle/cases/files-routes*`;
  `files_routes_equivalence.rs`. **Bumps `quilltap-core` +
  `quilltap-web` + `quilltap-harness`.**
- **Lane B (p4.6ai):** `crates/quilltap-core` (`api/image_profiles.rs`,
  `api/types.rs` its `// === P4.6ai ===` block if any, `api/engine.rs`
  the `EngineAssembly.image_generation` field + the in-place generate
  arm, `tools/generate_image.rs` glue, `api/mod.rs`);
  `quilltap-host/src/host.rs` (seam wiring);
  `crates/quilltap-harness/tests/` the generate differential + oracle.
  **Bumps `quilltap-core` + `quilltap-host` + `quilltap-harness`.**
- **Lane C (p4.6aj):** `apps/web` ONLY — `core/core-contract.ts` +
  `core/core-client.ts` (owner), the delete-associations dialog + host
  screen under `screens/files/**` (NOT `app/files/**`), the
  composer-attach + generate-dialog live-path verification, NEW/extended
  e2e beats (sorting AFTER `foundation.spec.ts`), `e2e/support/**`
  additions if seeding needs them. **Bumps the SPA.**
- **Lane D (p4.d4):** `crates/quilltap-core/src/skip_signal.rs`;
  `crates/quilltap-harness/tests/skip_signal_equivalence.rs` +
  `harness/oracle/cases/skip-signal.ts`; OPTIONALLY the tier-3 regen.
  **Bumps `quilltap-core` + `quilltap-harness`.**

Round-wide HANDS OFF: `apps/web/src/app/chat/render/**`,
`apps/web/src/app/files/**` (the Scriptorium file manager), the
byte-GET routes in `files_routes.rs`, `api/salon.rs`, and the frozen
mount/courier/autonomous dispatch surfaces.

Union-merged at unification: CHANGELOG, `status-log.md`, the
`api/mod.rs` + web-router appends, the two lane-delimited blocks in
`types.rs`/`engine.rs`, `package.json` (version). The oracle-baseline
line in CLAUDE.md moves to `02865bdb` when **lane D** unifies;
accumulate the multi-lane bumps per `[[parallel-round-reconciliation]]`.
CLAUDE.md Status is round-completion-only.

## Verification gate (run before declaring done)

- `ng test` green, including the new delete-associations dialog spec.
- `ng build` clean.
- Full Playwright from `apps/web` (local binary, not `npx`): no NEW
  failures. Guarded beats self-activate at unification when the server
  legs land.
- The Rust workspace is untouched by this lane, but a `cargo build`
  sanity check before declaring done confirms the worktree is coherent.
- Commit checklist per `.claude/commands/commit.md` on every commit
  (CHANGELOG entry, SPA version bump).

## Ground rules

No stubs/TODO beyond the loud enumerated deferrals; keep v4's steampunk
register on user-facing strings (the delete-dialog copy); build against
the PINNED envelope shape (mock in the unit spec) — do not block on the
server lanes; do not touch the Scriptorium file manager or the render
pipeline; if the fresh SPA survey contradicts this order, STOP and
report before proceeding.
