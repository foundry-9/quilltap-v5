# Work order P4.77 — maintenance: the `zod` version tripwire, the 4.9.0-release ratification evidence + the mirror refresh (P4.D159), the `CaptureLayer` consolidation, and `render_template`'s missing debug lines

**Status:** OPEN (ordered 2026-09-05, the `p4.9i2` help/HelpChat round:
P4.9I2A ∥ P4.9I2B ∥ P4.76 ∥ P4.77). The unifier updates this header.

## Preamble

- **v4 oracle baseline: `d883a5ee1`** (drift-ledger §1). v4 `main` HEAD is
  `c2232cd9a`, four commits past, every one docs/version-only — the 4.9.0
  release cut (`6cbe2b027` release notes, `b0eea4642` the squash onto
  `release`, `f6794c840` the merge back, `c2232cd9a` the 4.10.0 dev bump).
  The ledger's §3 carries their evidence and marks all four
  `ORDERED(P4.77)`: **this lane produces the ratification record (unit 2,
  "P4.D159") the unifier needs to move the baseline to `c2232cd9a` and set
  `NO-PORT-RATIFIED`.** The `15573c3a1` bug-119 row is NOT yours — it stays
  UNPROCESSED for the unported `p4.9k` surface by the ledger's own
  instruction.
- **Freshness probe at lane start** (ledger §2): checkout on **`bugfix`**
  (recorded; never switch it), tree clean, `git -C ~/source/quilltap-server
  log --oneline c2232cd9a..main` empty, `git … log --oneline
  2b49f51aa..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED at `d883a5ee1`** — this lane regenerates ONE
  family (the `render_template` capture pins need no oracle; the
  `CaptureLayer` consolidation is behaviour-neutral and re-runs the affected
  families' EXISTING oracles as a neutrality check). Pin path
  **`/tmp/qt-v4-pin-p477-d883a5ee1`**; the mirror refresh (unit 2) reads
  `git show 6cbe2b027:docs/releases/4.9.0.md` from the checkout — a
  read-only `git show`, no branch switch.
- **Sibling lanes:** P4.9I2A / P4.76 (Rust — they own every file in their
  Ownership columns; the `CaptureLayer` sites you consolidate must be listed
  at lane start and must contain NONE of their files — as of ordering the 19
  sites are `quilltap-host/src/host_gateway.rs`, `quilltap-core/src/
  vault_overlay.rs`, `image_gen/lora_support.rs`, `realtime/bus.rs`,
  `services/{chat_files,maintenance,scheduled_maintenance,cost_events,
  cheap_llm_exec,file_fallback,job_runner,build_context,message_context,
  memory_subject}.rs`, `services/context_summary/title_verdict.rs` and four
  more — re-grep), P4.9I2B (SPA — no overlap). §S and the Ownership table
  are binding and identical across all four orders.
- **Worktree:** per the `carryout` skill (branch `claude/p4-77-…`); `cd
  <worktree>` before every cargo command. **No Playwright** (§S.5).

## Mandate

Four small, well-bounded items the last three rounds named and deferred,
none of which touches a ported behaviour:

1. **The `zod` version tripwire** (phase-4 candidate 4 from the `d883a5ee1`
   round): v5 hand-rolls Zod semantics in TWO engines
   (`pascal/custom_tool_types.rs` + its SPA twin) and transcribes Zod
   sentences at ~150 edge sites; the oracle `node_modules` resolve the LIVE
   tree, so a v4 dependency bump is a regen event for all of them — and the
   `d883a5ee1` round found one (`6e1a64ea6`, 4.4.3 → 4.5.4) only at a
   family's first red. A committed harness guard that FAILS the gate when
   v4's installed `zod` moves past the recorded version catches the next
   one at ordering time.
2. **P4.D159 — the 4.9.0-release ratification evidence + the mirror
   refresh:** re-measure at the pin what the ledger claims (`git diff
   --stat d883a5ee1 c2232cd9a -- lib/ app/ packages/ plugins/ scripts/
   server.ts public/ types/ hooks/ components/` EMPTY; the all-paths diff =
   the five doc/version files; `package-lock.json`'s hunk = the two version
   lines), write it into the lane record as the ratification evidence, and
   refresh the reference mirror's `docs/v4/releases/4.9.0.md` from
   `6cbe2b027` (the ledger's doc-sync nit). Nothing reads the mirror
   mechanically; the ratification is the deliverable.
3. **The `CaptureLayer` consolidation** (the carried small): 19 copies of
   the same `#[cfg(test)]` tracing capture layer across core/host test
   modules (the candidate list said thirteen; the grep says nineteen — the
   census is yours to fix). One `#[cfg(test)]` helper module in core (and a
   re-export the host can reach), every site migrated, behaviour-neutral —
   with the thread-scope + prefix-match semantics the memory notes pin
   (`a-process-global-test-seam-must-be-thread-scoped`,
   `capture-layer-target-assert-is-a-prefix-match`,
   `coalescing-hides-its-own-mutation-proofs`) PRESERVED, not re-derived.
4. **`render_template`'s four missing `logger.debug` lines**
   (`pascal/custom_tools.rs:1319`; the handler-logging inventory
   `docs/developer/porting/handler-logging-inventory.md` names them): port
   at v4's levels with capture-layer presence + silence pins (memory note
   `differential-blind-to-a-log-only-fix`) — using the NEW shared helper
   from item 3.

**Must NOT touch:** anything in the other three lanes' Ownership columns;
`api/types.rs` / `engine.rs` / `host.rs` (the fenced files are the two
feature lanes'); any ported behaviour (item 3 is a test-only refactor; item
4 adds log lines only); the drift ledger (the unifier marks the rows).

## Survey-verified starting points (2026-09-05 — do not re-derive)

- **`zod` in v4:** `~/source/quilltap-server/node_modules/zod/package.json`
  → **`4.5.4`** at ordering (`node -p "require('./node_modules/zod/
  package.json').version"`). The harness locates the v4 checkout by
  convention (`~/source/quilltap-server` in every recipe header; several
  tests — `fallback_engine_equivalence.rs`, `mount_index_equivalence.rs`,
  `projects_tier2_equivalence.rs` — name it in their docs but read only
  NDJSON). A guard that reads the checkout's filesystem needs an explicit
  locator: an env var (`QT_V4_CHECKOUT`, default `$HOME/source/
  quilltap-server`) with a **loud `SKIP:` line** when the directory is
  absent (CI without a checkout) and a **FAIL** — not a skip — when it is
  present and the version differs from the recorded constant. The failure
  message must name the obligation (regenerate every Zod-transcribing family:
  both engines, the SPA corpus, the ~150 edge sites — the P4.D158 §3 finding
  is the precedent to cite by name). `spelling_guard.rs` is the harness's
  repo-reading guard precedent for shape; `db_error_key_guard.rs` for an
  executable census with a recorded constant.
- **The four rows' evidence** is already in the ledger's §1 and §3 (the
  content-diff commands are quoted there). Re-run them from the pin
  worktree (`git -C $PIN diff --stat d883a5ee1 c2232cd9a -- …` works from
  any worktree of the same repo).
- **`docs/v4/releases/4.9.0.md`** exists in the mirror (pre-final copy,
  refreshed at `d883a5ee1`); `git -C ~/source/quilltap-server show
  6cbe2b027:docs/releases/4.9.0.md` is the final text (+20/-6).
- **`CaptureLayer` sites:** `grep -rln "struct CaptureLayer" crates
  --include='*.rs' | grep -v target` → 19 files at ordering. Read three
  before designing the helper: `services/build_context.rs` (the thread-scoped
  seam), `services/context_summary/title_verdict.rs` (the presence + silence
  idiom), `realtime/bus.rs` (the coalescing trap). Do not unify semantics
  that differ on purpose — if two copies differ, record why before choosing.
- **`render_template`:** `crates/quilltap-core/src/pascal/custom_tools.rs:
  1319`; the inventory row names the four v4 `logger.debug` sites in
  `lib/pascal/**` (read the v4 function at the pin for the exact messages
  and structured fields).

## Tiered deliverables

### Tier 1 — must land

1. `crates/quilltap-harness/tests/zod_version_guard.rs` with the recorded
   constant `4.5.4`, the locator, the loud SKIP, the FAIL with the named
   obligation; a mutation (constant edited to `4.5.3`) reddens it on this
   machine; documented in the harness README/recipe conventions (memory note
   `recipe-header-conventions` — no recipe stage needed, but the sweep driver
   must not classify the file as an empty-stage family: check
   `--self-test`).
2. **P4.D159:** the measured ratification block in the lane record (the
   three commands + their empty/five-file outputs), and the refreshed
   `docs/v4/releases/4.9.0.md` (a `diff` against `git show 6cbe2b027:…`
   empty after the copy).
3. The `CaptureLayer` helper + all 19 sites migrated; the affected families'
   existing tests green; at least three mutation proofs re-run through the
   helper (one per idiom above) to show the semantics survived.
4. `render_template`'s four debug lines + their pins.

### Tier 2 — should land

5. A one-paragraph note in `docs/developer/porting/drift-ledger.md`? **NO —
   lanes never write the ledger.** Instead: the lane record names the
   guard so the next `/driftcheck` can cite it, and `phase-4.md` candidate 4
   is closed by the unifier.

### Tier 3 — explicit refusal-arm deferrals

- None. If the `CaptureLayer` copies turn out to carry three genuinely
  different contracts, land three helpers rather than one and say so.

## The differential requirement (restated)

Nothing here ports behaviour, so no new oracle. Items 3 and 4 are proven by
the EXISTING families staying green (run the families whose test modules
you touched by NAME from the pin, zero SKIP) and by the mutation proofs
named above; item 1 is proven by its own mutation; item 2 by the recorded
measurement.

## Fixtures

- None consumed, none delivered.

## §S — the round-wide meeting points (BINDING — identical in all four orders)

1. **Fenced-append shared files.** `crates/quilltap-core/src/api/types.rs`,
   `crates/quilltap-core/src/api/engine.rs`, and
   `crates/quilltap-host/src/host.rs` are edited by BOTH Rust lanes
   (P4.9I2A, P4.76) — each lane appends ONLY inside its own fence
   (`// === P4.9I2A ===` / `// === P4.76 ===`, opened once per file, new
   variants/arms/registrations added at the END of the relevant block,
   never interleaved with existing lines). `crates/quilltap-web/src/lib.rs`
   is P4.9I2A's alone (P4.76 registers nothing new — `?action=generate` is
   an action on an existing edge). `crates/quilltap-host/src/spine.rs` is
   P4.9I2A's alone. `Cargo.toml`/`Cargo.lock` at the workspace root: version
   lines only (§S.4); a dependency add is a STOP-and-flag. P4.77 touches
   none of these.
2. **Frozen files.** `docs/developer/porting/drift-ledger.md` (lanes never
   write it), `apps/web/src/app/core/core-contract.ts` (folded at unify —
   §S.3), `apps/web/src/app/shell/shell.ts` (UNIFIER-ONLY: the P4.9I2B
   `<qt-help-entry />` mount lands there at unification, BEFORE
   `<qt-brahma-entry />` — v4's footer order `sidebar-footer.tsx:203` then
   `:213`), `apps/web/src/app/workspace/workspace-contract.ts` (v4's help is
   a dialog, not a tab kind — no registry row).
3. **`core-contract.ts` folds at unify.** P4.9I2B declares the §B DTOs in
   `apps/web/src/app/help/help-wire.ts`; the unifier folds them and runs the
   name-for-name wire diff against `api/types.rs`.
4. **Version bumps.** P4.9I2A: `quilltap-core`, `quilltap-harness`,
   `quilltap-web`, `quilltap-host` per commit. P4.76: `quilltap-core`,
   `quilltap-harness`, `quilltap-web`, and `quilltap-host` (its seam).
   P4.77: `quilltap-harness` (+ `quilltap-core` for the test helper). P4.9I2B:
   `apps/web/package.json` per commit. The unifier RECOUNTS — identical
   bumps merge silently (memory note `commit-checklist`; the "silent
   auto-merge trap" in the playbook).
5. **e2e.** Only P4.9I2B runs Playwright and it adds NEW spec files only
   (`help-*.spec.ts`), with beats that depend on the server verbs or the
   shell mount shipped as guarded ACTIVATE-AT-UNIFY probes that report their
   guarded state loudly. The three other lanes: `git diff main -- apps/web/`
   empty. Port 4319 is repo-wide — one Playwright at a time.
6. **Append-only for everyone:** `docs/CHANGELOG.md`,
   `docs/developer/porting/status-log.md`.
7. **The regen rule for every lane:** PIN REQUIRED at `d883a5ee1`, a
   LANE-UNIQUE pin path (`/tmp/qt-v4-pin-<order>-d883a5ee1`), every regen
   through `recipe_sweep.py --v4 <pin>`; the checkout stays on `bugfix`
   untouched. The unifier moves the baseline to `c2232cd9a` (a pure
   ratification, P4.77's evidence) and the rule returns to PIN NOT REQUIRED
   only once the human has put the checkout back on `main`.
8. **The `p4.9i2` bank discharge:** P4.9I2A's vendored `help/` at the pin
   discharges every banked help-copy note (phase-4.md's five `### p4.9i2
   bank` sections and the m6 §1.6 row-11 notes); P4.9I2B's Guide tab
   discharges the P4.D77 bank's half (b). The unifier marks them.

## Ownership (BINDING — identical table in all four orders)

| Lane | Owns (edit freely) | Fenced-append (§S.1) | Must not touch |
| --- | --- | --- | --- |
| **P4.9I2A** | `help/**` (new, vendored); `crates/quilltap-host/build.rs` + `Cargo.toml` (host crate only) + `src/files_store.rs` + a new `src/help_content.rs`; `crates/quilltap-core/src/services/help_chat/**` (new); `crates/quilltap-core/src/api/{help_docs,help_chats}.rs` (new); NEW functions appended to `db/help_search.rs` / `db/help_docs.rs`; `crates/quilltap-web/src/help_routes.rs` (new) + `lib.rs`; `crates/quilltap-host/src/spine.rs`; new harness families + oracle cases + the `help-chat-*` fixture; `handler-logging-inventory.md` rows; this order's header | `api/types.rs`, `api/engine.rs`, `host.rs` | `apps/web/**`; P4.76's files; `services/brahma_console/**`; the 19 `CaptureLayer` sites |
| **P4.9I2B** | `apps/web/src/app/help/**` (new); `_surfaces.css`'s `qt-help-*` block (extend only); new e2e specs + `e2e/support/seed-help-fixture.ts`; `apps/web/package.json`; this order's header | — | `crates/**`; `shell.ts`; `core-contract.ts`; `workspace-contract.ts`; any existing spec |
| **P4.76** | `crates/quilltap-core/src/api/images.rs`; `api/files.rs` (the upload path: `save_file_entry` + `FileUpload.tags`); `model/image.rs`; `tools/generate_image.rs` (visibility widening only); `crates/quilltap-web/src/{images_routes,files_routes}.rs`; a new host image-generation seam file; `crates/quilltap-harness/tests/{images_routes_equivalence,activity_span_sites_guard,web_edge_body_parse_guard}.rs` (+ a new generate family); `harness/oracle/cases/images-*.ts`; the `images-*` fixture; this order's header | `api/types.rs`, `api/engine.rs`, `host.rs` | `apps/web/**`; P4.9I2A's files; `lib.rs`; `spine.rs` |
| **P4.77** | `crates/quilltap-harness/tests/zod_version_guard.rs` (new); `docs/v4/releases/4.9.0.md`; a new core test-support module + the 19 named `CaptureLayer` sites (listed at lane start, none in another lane's files); `crates/quilltap-core/src/pascal/custom_tools.rs` (`render_template` log lines only); this order's header | — | everything in the other three lanes' columns; `api/types.rs`; `engine.rs`; `host.rs` |

Everyone: `docs/CHANGELOG.md` + `status-log.md` append-only; the drift ledger
frozen; `core-contract.ts`/`shell.ts` frozen.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
   -D warnings` in BOTH feature sets; `cargo build --workspace`.
2. `cargo test --workspace` (full log + sentinel — never `| tail`) with the
   env block of every family whose test module you touched (the
   `CaptureLayer` sites' families), zero `SKIP:` for them; the new guard
   present in the binary list.
3. The touched families by NAME through `recipe_sweep.py --v4 /tmp/qt-v4-
   pin-p477-d883a5ee1 --run <family>`, oracles fresh at the pin, zero SKIP
   — a neutrality check (memory note `no-behavior-change-lane-needs-v5-
   rows`: the proof is v5's rows unchanged, not the oracle regenerated).
4. The mutation proofs named above, each in the lane record.
5. `git diff main -- apps/web/ crates/quilltap-core/src/api/ crates/
   quilltap-host/src/host.rs` empty.
6. Commits per `.claude/commands/commit.md` (harness + core bumps; the
   mirror refresh is docs-only); the lane record appended to
   `status-log.md` with the P4.D159 evidence block; this order's status
   header updated.
