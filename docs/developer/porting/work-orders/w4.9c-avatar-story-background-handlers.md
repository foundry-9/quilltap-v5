# Work order: W4.9c — the avatar + story-background job handlers

**Size: one Opus session** (internal parallelization OK: the two scene
tasks, the aesthetics module, and the storage bridges are disjoint from
the handler spines; the two handlers themselves can go to two agents once
the shared pieces land). Reuses the whole W4.9a subsystem — the image
seam, appearance resolution, the Concierge doors, `resolveOrientation`,
`postLanternImageNotification` — plus the W4.8 job runner.

v4 sources (surveyed 2026-07-07 at `6b6e39ad`):
`lib/background-jobs/handlers/character-avatar.ts` (573),
`lib/background-jobs/handlers/story-background.ts` (943),
`lib/wardrobe/avatar-prompt.ts` (171 — REWORKED by `6b6e39ad`: the
bare-top branch + collarbone-crop intro; port from CURRENT source),
`lib/memory/cheap-llm-tasks/image-scene-tasks.ts` (`deriveSceneContext`
:536, `craftStoryBackgroundPrompt` :740 — the two unported functions),
`lib/image-gen/aesthetic.ts`, `lib/file-storage/character-vault-bridge.ts`
+ `lantern-store-bridge.ts`, `lib/background-jobs/queue-service.ts`
(payloads :181–216, enqueues :998/:1144),
`lib/background-jobs/handlers/title-update.ts:219–295`
(`queueStoryBackgroundIfEnabled`).

⚠️ The job-type strings are `CHARACTER_AVATAR_GENERATION` and
**`STORY_BACKGROUND_GENERATION`** (with the `_GENERATION` suffix — both
already in v5's `KNOWN_JOB_TYPES`, currently loud-fallback).

## What to port

1. **The two scene tasks** (extend `services::image_scene_tasks`, prompts
   byte-exact in the generated `prompt_text`):
   - `derive_scene_context`: speaker labels `User`/`Character`/`System`,
     per-message >500-char truncation to `substring(0,500)+'...'`
     (UTF-16), the `contextInfo` block (title + optional existing summary
     + optional characters-present), the parse (trim → strip wrapping
     quotes → strip code fences). NO max-tokens override.
   - `craft_story_background_prompt`: the character section (or the
     no-characters atmospheric line), `lengthGuidance` — ⚠️ provider
     `GROK` gets the 1000-char variant, everyone else 1200 —
     `buildAestheticSection` (already ported private — reuse), and the
     user-message assembly; ⚠️ called WITH the `4000` max-tokens override
     (`deriveSceneContext` is not). Same parse.
2. **The real aesthetics module** (`services::aesthetics` — v4
   `lib/image-gen/aesthetic.ts`): `resolve_aesthetic({kind:
   lantern|aurora, project_official_mount_point_id, max_chars})` — tiered
   project-official → Quilltap General read of
   `{lantern,aurora}-aesthetics.md`, 4000-char default cap, fail-soft to
   None; `get_project_official_mount_point_id` (via the raw projects
   pointer, fail-soft); `resolve_depiction_guidelines(characters,
   max_chars_each=2000)` — the Ariel Clause, each character's own vault
   `depiction-guidelines.md`, skip vault-less, fail-soft. Then wire the
   real module into `tools::generate_image`'s injected-None slots and
   regenerate `image_generation_tier3` with an aesthetic-bearing case
   (STOP-able: if the regeneration balloons, keep generate_image on None,
   note it, and let the handlers consume the module directly — the
   handlers are the primary consumers).
3. **`buildCharacterAvatarPrompt`** (`crate::wardrobe::avatar_prompt` or a
   `services::avatar_prompt`): the physical-text preference chain
   (`headAndShouldersPrompt || medium || short || long || complete ||
   fullDescription`), `topIsBare = leafItemsBySlot.top.is_empty()`, the
   6b6e39ad bare-top branch (accessories-only `describeOutfit` with
   `omit: [top,bottom,footwear]`, else `''` — deliberately routing AROUND
   the "completely naked and unadorned" fallback; carry v4's why-comment),
   the two intros (collarbone-crop vs head-and-shoulders three-quarter),
   the outro, the `[.!?]+$` strip + `.`, the outfit-block spacing
   (`'\n\n'+text+'\n\n'` vs single space), the 600-char aesthetic preamble
   (`Art direction (apply this overall style): `), `hasAppearance` +
   `leafCounts`. `describeOutfit`/`decorateOutfitItems`/
   `genderNounFromPronouns` are ported leaves — compose, don't touch.
4. **The storage bridges**: `write_character_avatar_to_vault` (the
   character's database-backed vault, `kind:'history'` subfolder path per
   v4 `character-vault-bridge.ts` — throw if no vault; compose
   `link_blob_content`) and `write_lantern_background_to_mount_store`
   (the Lantern Backgrounds store, `subfolder:'generated'` — sibling of
   W4.9a's `tool/` writer; refuse-if-unprovisioned). The
   `fileStorageManager.uploadFile` PROJECT branches
   (`/character-avatars/`, `/story-backgrounds/`) are the host FsSeam —
   injected recorded seam, corpus keeps the no-project (database-backed)
   branches primary with one recorded-seam project case. The legacy
   `folders` row find-or-create (only on the non-vault/non-Lantern branch)
   rides the ported folders repo.
5. **The avatar handler** (`handle_character_avatar_generation`): the
   load/guard chain (missing chat/character/profile THROW; missing
   apiKeyId/key/`hasAppearance`/images/data WARN+RETURN — the
   no-op-vs-throw map is load-bearing), `equippedSlotsOverride ??`
   stored-outfit read, ⚠️ aesthetics = **aurora ONLY, no depiction
   guidelines** (the Ariel Clause deliberately does not apply to avatars
   — carry the comment), the Concierge pre-scan door (`mode !== 'OFF' &&
   scanImagePrompts` → classify → AUTO_ROUTE reroute; ALL classification
   errors swallowed), `resolveOrientation(…, 'portrait')` + promptHint
   append, generate + `logLLMCall` type `IMAGE_GENERATION` (both paths,
   durationMs from the injected clock), the post-hoc
   `isImageModerationError` reroute (re-resolve orientation for the
   reroute provider; exact throw strings `Avatar image generation
   failed…` / `…after Concierge reroute…`), base64 → `convertToWebP`
   (the injected transcoder) → sha → the vault-vs-project storage branch
   (branch key: `chat.projectId`), the `files` row (linkedTo
   `[chatId, characterId]`, tags `[characterId]`, description
   `{name} — wardrobe portrait`, generation* fields), the
   `chats.characterAvatars[characterId] = {imageId, generatedAt,
   afterMessageCount}` merge, the `characters.avatarOverrides`
   filter-then-push for this chatId, and `post_lantern_image_notification`
   (kind avatar → sender aurora; pass the prompt IN — v4's
   read-your-writes note; v5's single-writer makes the constraint moot but
   the argument shape stays).
6. **The story-background handler** (`handle_story_background_generation`):
   the load/guards, the scene-state freshness gate (`messageCount -
   updatedAtMessageCount <= 5`, parse failure swallowed), the cheap
   selection + `is_chat_active_dangerous` → uncensored upgrade, the
   last-20 `extract_visible_conversation` window, per-character equipped
   wardrobe → `AppearanceResolutionInput` (errors swallowed), the
   `imagePromptProfileId` uncensored selection (OLLAMA → isLocal,
   baseUrl default `http://localhost:11434`), the PARALLEL
   derive-scene-context (skipped entirely when scene state is fresh) +
   appearance resolution, the sceneContext resolution precedence
   (fresh scene state `{location}. {name: action; …}` → derived → payload
   fallback → chat.title), the appearance uncensored RETRY (only when the
   first ran on the cheap selection and an uncensored one exists), the
   sanitize gate (failure swallowed → unsanitized), the character
   descriptions (genderPrefix + resolved physical + `Wearing: …`, else
   the prompt-chain fallback), ⚠️ aesthetics = **lantern + aurora + the
   Ariel Clause** (depiction guidelines DO apply here), the craft call +
   ⚠️ the empty-but-SUCCESS silent-refusal retry on the uncensored
   selection (empty is NOT an error), `appendMissingCharacterEnumerations`
   over the user's NON-participant characters (longest-name-first `\b`
   scan; `escapeRegex`; appends `. {name}: {genderPrefix + prompt chain}.`),
   `resolveOrientation(…, 'landscape')`, generate + logs + the same
   post-hoc reroute (throw strings `Image generation failed…`), storage
   (branch key: ⚠️ `payload.projectId`, NOT chat.projectId — different
   from avatar), the `files` row (linkedTo `[chatId, ...characterIds]`,
   tags `[]`, description `Story background for: {sceneContext ||
   chat.title}`), `chats.update({storyBackgroundImageId,
   lastBackgroundGeneratedAt})`, the project `backgroundDisplayMode ===
   'latest_chat'` → `projects.update({storyBackgroundImageId})` (a
   store-backed properties RMW — the ported ProjectsRepository), and the
   notification (kind background → sender lantern).
7. **The enqueue side**: `queue_service::enqueue_story_background_generation`
   (dedupe on ANY pending/processing job for the chat, priority **-1**,
   payload `{chatId, imageProfileId, characterIds, sceneContext?,
   projectId?}`) + `queue_story_background_if_enabled` (the
   `storyBackgroundsSettings?.enabled` gate, the autonomous-chat skip,
   `resolveImageProfileForChat`, participant characterIds, sceneContext =
   the new title; errors swallowed). Its v4 call site is INSIDE the
   TITLE_UPDATE handler (fires only after a successful rename, not for
   help chats) — check whether v5 has a TITLE_UPDATE handler registered;
   if yes wire it there, if not port the gate as a public service function
   and document the handler wiring point (do NOT port the whole
   title-update handler here — its own scope). The avatar enqueue +
   dedupe (chatId AND characterId, maxAttempts 3) is already ported.
8. **Registration**: two `JobHandler` impls registered by the harness/host
   (`HandlerRegistry` — the W4.8 pattern; handlers close over the image/
   completion/transcoder/bytes seams), removing both types from the loud
   fallback in effect.

## Differential plan (two jest real-DB tier-3 differentials)

`avatar_job_tier3_equivalence` + `story_background_job_tier3_equivalence`:
drive v4's REAL `handleCharacterAvatarGeneration` /
`handleStoryBackgroundGeneration` over a two-DB fixture (characters +
vaults with physical descriptions + `depiction-guidelines.md` + equipped
outfits; a project with `backgroundDisplayMode:'latest_chat'` + an
official store carrying `{lantern,aurora}-aesthetics.md`; the Lantern
Backgrounds store; chats with scene state fresh AND stale; image + an
uncensored image profile; chatSettings with the Concierge scan flags +
`storyBackgroundsSettings`), mocking ONLY: the image provider (canned by
exact request — proves prompt assembly + orientation + reroute), the
completion boundary (recorded canned keys prove both new task prompts +
classification), Sharp/WebP (deterministic passthrough both sides), the
FSM `uploadFile` (recorded seam), `Date`/`crypto.randomUUID` (frozen/
scripted — v5 injects clock + uuid), and `logLLMCall` if W4.7e hasn't
landed (else live + dump `llm_logs`).

Avatar cases: happy vault write (six-table mount-index dump +
`files`/`chats`/`characters`, shared id-map remap), bare-top with
accessories + bare-top without (the collarbone intro + accessories-only
describeOutfit — the canned image key proves the prompt bytes), clothed
baseline, `equippedSlotsOverride` wins, `hasAppearance:false` no-op,
missing-key no-op, pre-scan reroute, post-hoc moderation reroute, aesthetic
preamble (600-char cap boundary), the project FsSeam case (recorded).
Story cases: fresh-scene-state fast path (derive skipped — proven by
canned-key absence), stale → derive fires, the GROK length guidance, the
empty-craft silent-refusal retry, appearance uncensored retry, sanitize
gate, missing-character enumeration append (a mentioned non-participant),
project `latest_chat` propagation (the projects properties RMW), the
Lantern `generated` write, post-hoc reroute, no-images no-op. Both: diff
result/return + all touched tables (sentinel-aware minted timestamps;
`chunkCount`/`doc_mount_chunks` pinned per the store precedent) + the
notification `chat_messages` rows byte-for-byte. Then a runner-registration
E2E row (enqueue → claim → dispatch → completed) per the W4.8 pattern.

## Watch-outs

- The no-op-vs-throw map is the subtlest surface — copy it exactly (warn+
  return: missing key/apiKeyId, no appearance, no images, no data; throw:
  missing chat/character/profile, save failure, generation failure after
  reroute exhausts).
- Filenames embed `Date.now()` — inject the clock; the differential pins
  it (the photo-trio `keptAt` precedent).
- `describeOutfit` is UNCHANGED by 6b6e39ad — do not touch the ported
  leaf; the bare-top branch routes around it by argument shape.
- The two storage-branch keys differ (avatar: `chat.projectId`; story:
  `payload.projectId`) — same-looking, different source.
- `derive-scene-context` maps to log type `SUMMARIZATION` via the task
  map; `craft-story-background-prompt` → `IMAGE_PROMPT_CRAFTING` (W4.7e's
  table — corpus-relevant only if logging is live).
- `resolveImageProfileForChat` — check for an existing v5 port
  (`avatar_generation.rs` has profile resolution); reuse, don't duplicate.

## Deliverables checklist

- [ ] Scene tasks + aesthetics + avatar prompt + bridges + both handlers +
      the story enqueue/gate ported; both job types dispatched via
      registered handlers; generate_image's aesthetics wired real (or
      STOP-noted).
- [ ] Both tier-3 differentials green; `image_generation_tier3` +
      `wardrobe_tools` re-verified (the trigger corpus now has a real
      handler downstream — confirm inert).
- [ ] Full workspace `cargo test` / `clippy -D warnings` / `fmt --check`.
- [ ] CHANGELOG, versions (never `quilltap-sqlite3mc-sys`), CLAUDE.md,
      `chat-orchestration.md`; memory note for gotchas.
- [ ] Commit per `.claude/commands/commit.md`, branch
      `w4.9c-image-job-handlers`.

**STOP rules:** no image codec crates; the TITLE_UPDATE handler itself is
out of scope (only the gate function); the W4.9a `save_generated_image` is
tool-path-specialized — build the sibling bridges, do not generalize it;
schema never changes; `isImageModerationError` stays in W4.2's module.
