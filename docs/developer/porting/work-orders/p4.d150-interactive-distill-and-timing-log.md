# Work order P4.D150 — the turn-blocking distillation asks for the interactive budget (bug 115, v4 `02d4efa1b`) + the inter-character memory timing log (v4 `c9faa2c74`)

**Status:** OPEN (ordered 2026-09-02, the `0b0617fee` drift catch-up round:
P4.D148 ∥ P4.D149 ∥ P4.D150 ∥ P4.D151 ∥ P4.D152).

## Preamble

- **v4 oracle baseline: `6d2a50382`** (drift-ledger §1, probe re-run at
  ordering 2026-09-02: v4 on `main` at `0b0617fee`, tree clean, both logs
  empty). **This lane's target commits are `02d4efa1b`** (bug 115 — two lib
  hunks: `lib/memory/cheap-llm-tasks/memory-tasks.ts` +12/−? and
  `lib/chat/context-manager.ts:1398` +9; two new v4 tests) **and
  `c9faa2c74`** (one hunk, `lib/chat/context-manager.ts:1755` +7). Both are
  v4's OWN filings/chores — NOT convergences. Read the hunks: `git -C
  ~/source/quilltap-server show 02d4efa1b -- lib/ && git -C
  ~/source/quilltap-server show c9faa2c74 -- lib/`.
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 on `main`,
  tree clean, `git log 0b0617fee..main` empty and `git log
  3a76b17df..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED** (ledger §1). Every regen runs from a
  lane-unique detached worktree pinned at **`c9faa2c74`**
  (`/tmp/qt-v4-pin-p4d150-c9faa2c74`, three symlink classes, ledger §5.1)
  through the sweep driver; the build-context / precompute / recall-replay
  families run with `TZ=UTC`. ⚠ **This pin carries `303288fb4`** (the
  chats route + an `orchestrator.service.ts` COMMENT) — neither touches
  this lane's families; verify by regenerating `build_context_tier3` at the
  pin AND at the baseline and diffing (byte-identical expected — a deadline
  class is invisible to a canned executor, §5.2's "a green regen is not
  coverage" applies with force here, which is why the pins below are unit
  pins).
- **Sibling lanes:** P4.D148/P4.D149 (the Concierge-at-creation halves),
  P4.D151 (bug 116 — owns `CompletionResponse`'s widening, §B: it will add
  `cache_usage: None` one-liners in `precompute_equivalence.rs` and
  `recall_replay_equivalence.rs`; **this lane does not edit those two
  harness files**), P4.D152 (bug 117). §Shared contracts / §Ownership
  binding and identical across all five orders.
- **Worktree:** per the `carryout` skill (branch `claude/p4-d150-…`).
  `CHANGELOG`/`status-log.md` append-only. **No Playwright** (§F).

## Mandate

Port both hunks whole. (1) `distill_memory_search` gains a trailing
`options: CheapLlmTaskOptions` (§C) threaded into `executor.execute(...)`
in place of today's `CheapLlmTaskOptions::default()`; the dynamic-head
FALLBACK call in `build_context.rs` passes `CheapLlmTaskOptions::
interactive()` with v4's nine-line why-comment carried whole; the proactive
pass (`pre_compute.rs`) and the `recall_replay` diagnostic pass the default
— v5 reproduces bug 115 today (up to 90 s + a free retry on a stalled cheap
route, per responding character, with the composer empty). (2) The
`[ContextManager] Inter-character memory retrieval complete` debug line
with `chatId`, `characterId`, `durationMs`, `loadedCount`, `includedCount`
at the end of the inter-character block — a log-only port. Must NOT touch:
`cheap_llm_exec.rs` (the class + options already exist from P4.D136),
`compression.rs`, `memory_recap/mod.rs` (P4.D151's one-liner lives there),
any harness test file (§B), `chat_create.rs`, `file_fallback.rs`.

## Survey-verified starting points (2026-09-02, from the shipped hunks — ledger §5.3)

1. **The parameter** (v4 `memory-tasks.ts:1283-1297` + `:1432-1436`):
   `extractMemorySearchKeywords(…, clock?, latency: CheapLLMLatencyClass =
   'background')` → `executeCheapLLMTask(…, characterId, { latency })`.
   v5 `services/memory_recap/distill.rs:340-352` `distill_memory_search(
   executor, completion, recent_messages, character_name, selection,
   character_id, clock)` → `executor.execute(…, Some("memory-keyword-
   extraction"), CheapLlmTaskOptions::default())` at `:359-368`. Rust has
   no default arguments (the `cheap_llm_exec.rs:200-212` doc says exactly
   this) — every caller names its class. `CheapLlmTaskOptions` /
   `CheapLlmLatencyClass` / `::interactive()` at `cheap_llm_exec.rs:207-232`.
2. **The three callers:** `services/build_context.rs:2339-2349` (the
   fallback — `Some(&clock)` is the last argument today; add
   `CheapLlmTaskOptions::interactive()` with the why-comment from v4
   `context-manager.ts:1401-1409`), `services/pre_compute.rs:288-296`
   (default), `services/recall_replay.rs:275-283` (default). Grep for a
   fourth (`ggrep -rn "distill_memory_search(" crates/`) — the order counts
   three; a fourth is a finding.
3. **The pin idiom — P4.D136's two shapes.** A deadline class never
   reaches an oracle (the canned executor answers instantly), so the
   proof is a UNIT pin in `build_context.rs`'s test module: a
   budget-recording `CompletionProvider` (the `BudgetRecorder` idiom in
   `services/compression.rs:425-458` — it records the `request_timeout_ms`
   the provider was handed) driven through the build-context fallback
   branch (no proactive query in hand → the distill fires) asserting the
   provider saw the INTERACTIVE budget (the interactive attempt deadline
   minus `PROVIDER_BUDGET_HEADROOM_MS` — read the constants, don't
   transcribe numbers from this order), and a second arm driving
   `pre_compute` asserting the BACKGROUND budget (bug 115's "the other
   direction so the proactive pass is not quietly starved" — v4's
   `task-deadline.test.ts` four cases). Plus the compile-time shape:
   P4.D136 pinned `build_context`'s ceiling relations in a `const { … }`
   block (search `build_context.rs` for `const _:` / `const {`); add the
   relation "the fallback distill's attempt deadline < the recap phase
   ceiling" if such a ceiling brackets it — MEASURE first (memory note
   `raising-an-inner-timeout-inverts-outer-bounds`).
4. **The retry half.** v4's `runCheapLLMTask` grants a background pass ONE
   timeout retry and withholds it from an interactive one (bug 107) — v5's
   executor already does (P4.D136, "the timeout-only retry"). The unit pin
   should also count CALLS: the fallback distill on a stalling provider
   makes exactly ONE attempt; the proactive one makes two.
5. **The timing log** (v4 `context-manager.ts:1665-1669`, `:1732`,
   `:1755-1761`): `tInterStart = performance.now()` taken BEFORE the block,
   `interCharacterLoadedCount = interCharacterMemories.length +
   interCharacterRelevance.length` set inside it, the debug line in the
   `if (isMultiCharacter)` block AFTER 2b with `durationMs: Math.round(
   performance.now() - tInterStart)`, `loadedCount`, `includedCount:
   interCharacterMemoriesIncluded`. v5 `build_context.rs:2570-2665`: the
   block has `inter_character_memories_included` (`:2574`, set `:2657`),
   `importance_memories` (`:2611`) and `relevance_results` (`:2617`) — the
   loaded count is `importance_memories.len() + relevance_results.len()`
   (v4's two arrays — NOT `formatted.memories_used`); it is 0 when the
   budget/ids guard skips the block. The line fires whenever
   `is_multi_character` (`:1792`), INCLUDING the `skip_memories` case where
   the block is bypassed (v4's `if (isMultiCharacter)` is outside the
   `!skipMemories` guard — verify against the pinned file and carry
   exactly). `durationMs` is wall-clock: a `std::time::Instant` at the
   block's top (the P4.D49 measured-durations precedent), `.round()` to an
   integer.
6. **The capture pin:** the thread-scoped capture-layer idiom
   (`services/file_fallback.rs:1290-1340` has an in-file example; memory
   notes `a-process-global-test-seam-must-be-thread-scoped`,
   `capture-layer-target-assert-is-a-prefix-match`,
   `differential-blind-to-a-log-only-fix`). Three arms: multi-character
   → the line with all five fields (`loadedCount` equal to the seeded
   count); single-character → SILENCE; multi-character with
   `skip_memories` → the line with `loadedCount: 0` (or whatever point 5's
   measurement says v4 does).

## Tiered deliverables

- **Tier 1 (must land):** the parameter + the three call sites, the two
  budget unit pins (interactive at the fallback, background at the
  proactive pass) + the call-count pins, the timing log + its three
  capture arms, the `build_context_tier3` / `precompute` / `recall_replay`
  regen at the pin (byte-identical to baseline — RECORD that measurement;
  it is the proof the corpus cannot see this and the unit pins are the
  coverage).
- **Tier 2 (should land):** the compile-time ceiling relation (point 3's
  `const` block) if a bracketing ceiling exists.
- **Tier 3 (explicit deferrals, loud):** none expected. v4's two new test
  files are NO-PORT as files (their behaviour is the unit pins).

## The differential requirement (restated)

The three families (`build_context_tier3_equivalence` — `QT_ORACLE_BUILD_
CONTEXT`, `QT_FIXTURE_BC_*`; `precompute_equivalence` — `QT_ORACLE_
PRECOMPUTE`, `QT_FIXTURE_ER_*`; `recall_replay_equivalence` —
`QT_ORACLE_RECALL_REPLAY`, `QT_FIXTURE_ER_*`) regenerate from the
`c9faa2c74` pin and run by name, zero SKIP, with the pin-vs-baseline
NDJSON diff recorded as EMPTY (the expected shape). The behaviour is
pinned by the unit tests above (the P4.D136 idiom), mutation-proven:
revert the fallback site to `default()` → the interactive-budget pin
reddens AND the call-count pin reddens (two attempts); pass
`interactive()` at `pre_compute` → the background pin reddens; delete the
log line → the multi-character capture arm reddens; compute `loadedCount`
from `memories_used` → the count arm reddens (seed more loaded than
included).

## Fixtures

- Consumes: `episodic-recall-*` (P4.68's rebuilt pair — `QT_FIXTURE_ER_*`),
  the build-context tier-3 pair; run only, no changes.
- Delivers: nothing committed beyond the unit pins.

## Shared contracts (binding, identical across all five orders)

**§A — the create-time Concierge wire (P4.D148 ↔ P4.D149).** The
`chatCreate` dispatch verb's request (v5 `ChatCreateRequest`, SPA
`ChatCreateRequest` in `core-contract.ts`) gains ONE optional key:

```
conciergeState?: 'monitored' | 'flagged' | 'vouched' | 'uncensored'
```

(v4 `createChatSchema`: `z.enum([...]).optional()` — spelled out, not derived;
`.optional()` is NOT nullable, so an explicit JSON `null` REJECTS like
`timestampConfig`'s does.) Absent ≡ `'monitored'`: no write, no announcement,
the request AND the created chat byte-identical to today's. Any other value is
applied through the EXISTING `apply_concierge_flip` chokepoint (v4
`applyConciergeFlip(chat.id, requested, chat)` with the fresh row) on ALL
THREE create branches (continuation / autonomous / ordinary), immediately
AFTER the system-prompt message and BEFORE `apply_chat_continuation` /
scenario / staff / greeting, so the Concierge bubble is the first row after
the prompt and the greeting is generated under the chosen state. The
announcement reuses v4's five `manual-*` kinds (`RealConciergeAnnouncer`) —
NO new sentences. The SSE progress status line is exactly
`Briefing the Concierge…` (U+2026), emitted only when a non-Monitored state
is applied, between the branch's existing status and the scene status. An
unknown string → the create schema's validation 400 (the envelope
`tc_bad_value_rejected` already pins). The CLIENT omits the key when the
form holds `'monitored'`. v5 serves NO REST `POST /api/v1/chats` (the SPA
creates through `POST /api/dispatch`; `chats_routes.rs` has the collection
GET only) — recorded, not this round's.

**§B — `CompletionResponse.cache_usage` (P4.D151 owns the widening).**
`model::completion::CompletionResponse` gains
`pub cache_usage: Option<crate::model::stream::StreamCacheUsage>` (v4
`LLMResponse.cacheUsage`), populated from `parsed.cache_usage` in the real
`completion_provider.rs` composition and `None` at every other construction
site. **P4.D151 edits every construction site itself, including the
`cache_usage: None` one-liners in harness tests owned by other families**
(`precompute_equivalence.rs`, `recall_replay_equivalence.rs`,
`title_update_tier3_equivalence.rs`, `settings_wire_actions.rs`,
`attach_mount_file_equivalence.rs`, `chat_regenerate_title_tier3_equivalence.rs`,
`answer_confirmation_tier3_equivalence.rs`, `danger_gatekeeper_tier3_equivalence.rs`,
`file_attachment_tier3_equivalence.rs`, `outfit_llm_choose_tier3_equivalence.rs`).
No other lane edits those harness files this round (P4.D150's pins are
core unit tests, not harness edits) — that is what keeps the pick clean.

**§C — `distill_memory_search` (P4.D150 owns).** The function gains a
trailing `options: CheapLlmTaskOptions` parameter threaded to
`executor.execute(...)` (v4 `latency: CheapLLMLatencyClass = 'background'`
→ `{ latency }`). Its three callers: `build_context.rs` (the dynamic-head
FALLBACK) passes `CheapLlmTaskOptions::interactive()`; `pre_compute.rs` and
`recall_replay.rs` pass `CheapLlmTaskOptions::default()`. No other lane
touches `services/memory_recap/distill.rs`, `services/build_context.rs`,
`services/pre_compute.rs`, or `services/recall_replay.rs`.

**§D — the `files.sha256` invariant (P4.D152 owns).** `files.sha256`,
`mimeType` and `size` ALL describe the bytes actually STORED (post-bridge),
never the writer's input — at chat upload, `.qtap` import, restore replay
AND the carried-store-rows restore branch (which reads the archived
`doc_mount_blobs.sha256` by the parsed blob id). The boot heal carries v4's
migration id `realign-file-entry-sha256-v1` in the P4.D140 ledger shape
(honour an existing `migrations_state` row from either app; write the row
only on a pass that realigned ≥ 1 row). P4.D152 does NOT touch
`services/file_fallback.rs` (the describe tier reads `files` by id, never
by sha) and does NOT edit `attach_mount_file_equivalence.rs` (P4.D151's);
its join proof is the NEW heal family + the state dumps.

**§E — the manifest substrate (P4.D151 owns for this round).** The generated
`provider_manifest/manifests/*.json` are regenerated ONCE, by P4.D151, from
the `0b0617fee` pin through `harness/oracle/providers/gen-provider-manifests.mjs`;
the expected result is BYTE-IDENTICAL (bug 118 is a v4 convergence onto
bytes v5 has carried since P4.D107). No other lane runs the generator.

**§F — Playwright.** Port 4319 is P4.D149's this round. P4.D149's
create-time beat is authored GATED behind `const P4D148_SERVER_LANDED =
false` (ACTIVATE-AT-UNIFY — the unifier flips it after P4.D148 lands and
runs it live); every other lane authors NO Playwright and says so.

**§G — the drift ledger.** Lanes never write `drift-ledger.md`; the five §3
rows were marked ORDERED at ordering, and `/unify` marks them ABSORBED /
NO-PORT-RATIFIED when the baseline moves to `0b0617fee`.

## Ownership (binding, identical across all five orders)

| Area | Owner |
|---|---|
| `quilltap-core`: `services/chat_create.rs` (the request field, `apply_requested_concierge_state`, the three branches, `auto_generate_first_message`'s attempt 0 + the shared uncensored-desk closure, the five log lines); harness: `chat_create_capstone_equivalence` (+ `cases/chat-create-capstone.test.ts`, `fixtures/chat-create-capstone.json`, `fixtures/build-chat-create-capstone.ts`), `initial_greeting_equivalence` + `first_message_context_equivalence` (run, not changed) | **P4.D148** |
| SPA: `screens/new-chat/{new-chat-form.ts, new-chat.types.ts, new-chat.state.ts, new-chat.logic.ts}` (+ specs), `core/core-contract.ts` §A region, `e2e/new-chat-flow.spec.ts` (the gated beat); the `p4.9i2` bank rows for `help/chats.md` + `help/dangerous-content.md` | **P4.D149** |
| `quilltap-core`: `services/memory_recap/distill.rs`, `services/build_context.rs` (the fallback-distill site + the inter-character log line + its unit pins), `services/pre_compute.rs`, `services/recall_replay.rs`; harness: `build_context_tier3_equivalence`, `precompute_equivalence`, `recall_replay_equivalence` (RUN only — no edits, §B) | **P4.D150** |
| `quilltap-core`: `services/file_fallback.rs` whole, `model/completion.rs` (§B), `model/completion_provider.rs` (§B), the `cache_usage: None` one-liners at `services/compression.rs`, `services/cheap_llm_exec.rs`, `services/memory_recap/mod.rs`, `services/outfit_selections.rs` + the harness sites listed in §B, `model/request_builder/chat_completions.rs` (the NanoGPT comment), `provider_manifest/manifests/*.json` (§E regen, expected no-op); harness: `file_attachment_tier3_equivalence` (+ `cases/file-attachment-tier3.test.ts`, `fixtures/file-attachment.json`, its builder), `fallback_engine_equivalence` (+ `cases/fallback-engine.ts`), `attach_mount_file_equivalence` (+ its case); the `p4.9i2` bank rows for `help/file-uploads.md` + `help/chat-settings.md`; the `b448eddd7` NO-PORT ratification | **P4.D151** |
| `quilltap-core`: `services/chat_files.rs`, `services/quilltap_import/files.rs`, `services/backup/restore/orchestrator.rs` (the files region — `restore_one_file` / `carried_store_rows` and the replay branch ONLY), NEW `db/files_sha256_realign_heal.rs` + its `db/mod.rs` line; `quilltap-host`: `host.rs` (one boot-chain block); harness: NEW `files_sha256_realign_heal_equivalence` (+ its oracle case + fixture builder), `files_tier2_equivalence` / `files_routes_equivalence` (the upload sha arm), `qtap_import_equivalence`, `system_restore_state` / `system_restore_equivalence` / `restore_vintage_state` (the sha arms + any NEW committed archive), `character_photo_upload_tier2_equivalence` + `photo_tools_equivalence` (run), a harness-only byte-changing `PixelCodec` twin | **P4.D152** |
| Playwright (port 4319) | **P4.D149's branch only** (§F) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it (§G) |

**Version bumps:** P4.D148 bumps `quilltap-core` + `quilltap-harness`;
P4.D149 bumps the SPA `package.json`; P4.D150 bumps `quilltap-core` (+
`quilltap-harness` only if a harness file changes — §B says none should);
P4.D151 bumps `quilltap-core` + `quilltap-harness`; P4.D152 bumps
`quilltap-core` + `quilltap-harness` + `quilltap-host`. Per commit; the
unifier recounts at unification (the playbook's silent-auto-merge trap).

## Verification gate before declaring done

`cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
-D warnings` on BOTH feature sets; full `cargo test --workspace` with the
three families' env vars (`run_in_background: true`, full log + sentinel —
NEVER a pgrep loop, NEVER `| tail`); the three families by name through
the sweep driver from the `c9faa2c74` pin, zero SKIP, with the
pin-vs-baseline byte-identity recorded; the unit pins' mutations
verified-applied and reverted by file backup. No SPA gate (no SPA file
touched — say so). Per-commit rule: `.claude/commands/commit.md` (CHANGELOG
entry per commit, core bump — harness only if a harness file changed, which
§B says it must not; lane record appended to `status-log.md` under
"P4.D150").
