# Work order P4.D135 — provider/model fallback chains (v4 `65f5021c8`)

**Status:** OPEN (ordered 2026-08-31).

## Preamble

- **v4 oracle baseline: `b121ac77f`** (drift-ledger §1, checked 2026-08-31).
  **This lane's target commit is `65f5021c8`** — the round's PORT-NEW
  centerpiece: a connection profile can name an understudy, and a call that
  fails outright (rejected key, rate limit, network error, missing model,
  5xx, empty response, moderation refusal) hands the turn to the next
  candidate, capped at three attempts (profile → understudy → one
  auto-picked tier replacement), no recursion, no stickiness.
- **Freshness probe at lane start** (drift-ledger §2). Pass = checkout on
  `main`, tree clean, no commits past `60e3c4a0a` on main and none past
  `3a76b17df` on bugfix. **Any mismatch: STOP and report.**
- **Regen rule in force: PIN REQUIRED** (ledger §1). Every oracle/fixture
  regen runs from a lane-unique detached worktree pinned at the TARGET
  commit **`65f5021c8`**: `/tmp/qt-v4-pin-p4d135-65f5021c8`, all three
  symlink classes, per ledger §5.1. Verify the pin
  (`lib/llm/fallback/engine.ts` must exist in it). Families this lane does
  not move regen at the baseline `b121ac77f` if needed.
- **Stacking:** **P4.D136 (`a1d88aa3a`, bugs 106/107) stacks ON THIS
  LANE'S BRANCH** — the two commits share files (`provider-failover`,
  `orchestrator`, `primary-stream`, `fallback/engine`,
  `file-attachment-fallback`, `cheap-llm` core-execution) and the ledger
  orders them together, D-stacked. Finish and close this order, then the
  same or a fresh agent opens P4.D136 from this branch's tip. Do NOT
  absorb any `a1d88aa3a` hunk here — the differential attribution stays
  per-commit.
- **Sibling lanes:** P4.D134 (`p4.d134-lima-wsl2-retirement.md`), P4.D137
  (`p4.d137-doc-edit-guards-and-typographic-folding.md`). §Ownership (in
  P4.D134's order, binding and identical here by reference) assigns this
  branch the spine/provider/profile files and the round's ONLY Playwright
  slot.
- **Worktree:** per the `carryout` skill; edit inside the worktree, not
  main. `CHANGELOG` and `status-log.md` append-only.

## Mandate

Port the whole fallback-chains feature: the two `connection_profiles`
columns through the D23 re-dump, the pure fallback engine, the four
integration sites (Salon hard errors via restream + the new `failing-over`
SSE stage, Salon empty responses as the THIRD recovery after the
same-profile retry and the uncensored reroute, cheap-LLM tasks behind the
new `allowCheapFallback` setting, image description), both id-rewriting
paths, the delete-nulls cascade, the profile CRUD/wire additions, and the
SPA mirrors (understudy picker in the profile modal, the cheap-LLM
setting, the `failing-over` toast). Must NOT touch: P4.D134's files,
P4.D137's doc-edit files, or any `a1d88aa3a` hunk (P4.D136's).

## Survey-verified starting points (2026-08-31, from the shipped hunks — ledger §5.3)

1. **Schema — D23 re-dump REQUIRED.**
   `migrations/scripts/sqlite-initial-schema.ts` adds, at line ~351,
   `"fallbackProfileId" TEXT` + `"allowTierFallback" INTEGER DEFAULT 0`
   **MID-TABLE** (after `multiCharacterPrefill`, before
   `supportsImageUpload`), while the new migration
   `migrations/scripts/add-profile-fallback-fields.ts`
   (`add-profile-fallback-fields-v1`, `introducedInVersion: '4.10.0'`)
   APPENDs them — the standing generateDDL-vs-migration column-order
   disagreement; carry BOTH shapes (generateDDL order in
   `fresh_schema.json`, append order in the boot ensure + per-column read
   tolerance). Re-dump `fresh_schema.json` from the pin's live
   `generateDDL`, never by hand. **The mid-table insert moves exported key
   order** → regenerate `services/qtap_export/schema-key-order.json` with
   the shipped generator (the P4.D65 stale-key-order class);
   `public/schemas/qtap-export.schema.json` grows the two keys.
   Expect `provisioning_equivalence` red until the re-dump lands — that is
   the tripwire firing as designed.
2. **The pure engine** — NEW `lib/llm/fallback/{engine,tier-picker,types,
   index}.ts` (~600 lines). Exports: `classifyFallbackTrigger` (returns
   null for the non-triggers — token/content limits, tool-unsupported, Zod
   errors, unattributed 4xx — so their own recovery paths keep their
   exclusive claim), `buildFallbackChain`, `recordAttempt`,
   `summarizeFallbackAttempts`, `FallbackRepos`; `pickTierCandidate` +
   `tierMatches` (at most one candidate, prefers a different provider);
   the `FallbackAttempt`/`FallbackCandidate`/`FallbackCandidateKind`/
   `FallbackContext`/`FallbackOutcome`/`FallbackTrigger` types. → a NEW
   v5 module (suggested `quilltap-core/src/llm_fallback/`), tier-1
   differential with v4's own 568 test lines
   (`__tests__/unit/lib/llm/fallback/{engine,tier-picker}.test.ts`) as
   the corpus shape — the oracle drives v4's REAL engine from the pin.
3. **The spine.** `lib/services/chat-message/provider-failover.service.ts`
   (+455): `walkFallbackChain` (:538), `attemptHardErrorFailover` (:679),
   `attemptEmptyResponseChainFallback` (:733), `restreamInto` grows
   `resetStreamingBuffersForSwap` (:470); hard-error failover runs **only
   before the first content chunk** (carry v4's why-comment: restreamInto
   appends and SSE has no reset — a mid-stream substitution would glue the
   understudy's answer onto a truncated fragment). `primary-stream.
   service.ts` (+55): the catch-all that used to rethrow unconditionally
   now routes through the chain. `orchestrator.service.ts` (+54),
   `turn-orchestrator.service.ts` (+10). New SSE stage **`failing-over`**
   on the status channel. → `quilltap-core/src/services/
   {provider_failover,primary_stream,orchestrator,turn_orchestrator}.rs`;
   families `primary_stream_tier3_equivalence`,
   `orchestrator_tier3_equivalence`, `turn_orchestrator_tier2_equivalence`.
   **Both chain steps filter on vision when the turn carries images** —
   the tier picker AND (uniquely among the checks) the named understudy;
   every other user choice is honoured as made, danger-compatibility
   included (policy preference vs incompatibility — carry the comment).
4. **Cheap LLM.** NEW `lib/memory/cheap-llm-tasks/{core-execution,
   fallback}.ts` (+139/+137 at THIS commit — `a1d88aa3a` rewrites
   core-execution further; port THIS commit's state, D136 moves it on);
   `lib/llm/cheap-llm.ts` (+2); `allowCheapFallback` on
   `CheapLLMSettings` (default `false`, governs profile-less routes)
   threads `lib/schemas/settings.types.ts`, `lib/auth/single-user.ts`,
   `chat-settings.repository.ts`, `sillytavern-import-service.ts`, and
   `app/api/v1/settings/chat/route.ts` gains the boolean guard
   (`'allowCheapFallback must be a boolean'`). →
   `quilltap-core/src/services/cheap_llm_exec.rs`,
   `db/chat_settings.rs`, the chat-settings verb + web edge; family
   `settings_routes_equivalence` (decode through the Request enum — the
   `double_option`/present-null lessons apply).
5. **Image description** is the fourth site:
   `lib/chat/file-attachment-fallback.ts` (+146 at this commit). →
   `quilltap-core/src/services/file_fallback.rs` (+
   `files/image_transport.rs` only if the hunks reach it — they mostly
   don't until `a1d88aa3a`); family `file_attachment_tier3_equivalence`.
6. **Profiles CRUD + wire.** `app/api/v1/connection-profiles/route.ts`
   (+29) + `[id]/route.ts` (+37);
   `connection-profiles.repository.ts` (+19 — **deleting a profile NULLs
   `fallbackProfileId` on every profile naming it**, a cascade v5 must
   reproduce); `lib/schemas/profile.types.ts` (+26);
   `connection-profile-legacy-fields.ts` (+26). →
   `db/connection_profiles.rs`, `connection_profile_legacy_fields.rs`,
   the profile verbs/edges; families `profile_routes_equivalence`,
   `connection_profiles_tier2_equivalence`,
   `connection_profile_legacy_fields_equivalence`.
7. **Id-rewriting.** `lib/backup/restore/uuid-remap.ts` adds
   `fallbackProfileId` to `remapFields` →
   `services/backup/uuid_remap*.rs`, family
   `backup_uuid_remap_equivalence`;
   `lib/import/quilltap-import/reconcile.ts` (+22) remaps it in the
   reconcile pass (an understudy may appear later in the bundle) →
   `services/quilltap_import/reconcile.rs`, families
   `qtap_import_equivalence`, `system_import_equivalence`/
   `system_import_state`.
8. **SPA.** `ProfileModal.tsx` (+94) + `useProfileForm.ts` +
   `connection-profiles/types.ts` → `apps/web/src/app/screens/settings/
   providers/{profile-modal,profile-form}.ts` + types;
   `CheapLLMSettings.tsx` (+21) + `chat-settings/types.ts` →
   `screens/settings/providers/cheap-llm-card.ts`;
   `useSSEStreaming.ts` (+8 — warn toast now also fires on
   `failing-over`) → `apps/web/src/app/core/chat-stream.reducer.ts` + the
   display site. ⚠ **The reducer-carry note applies** (memory: "A v4
   client fix may need TWO layers in v5") — the pure reducer must carry
   the new stage before any display can read it.
9. **`jest.setup.ts` (+8)** — read the hunk before building oracles: a
   new default mock/repo field here is exactly the stale-oracle-mock
   class (P4.20/P4.36) that starves v4-side behavior; mirror whatever it
   provides into the lane's oracle venue.
10. **`lib/startup/prettify.ts` (+1)** — measure; likely a startup log
    line (log-only fixes are differential-invisible; a capturing pin or a
    recorded NO-PORT with evidence).
11. **Help:** `help/{chat-settings,connection-profiles}.md` → the
    `p4.9i2` bank. **NO-PORT remainder:** README, `docs/**` (the +436
    feature doc `provider-fallback.md` is the design record — read it,
    port from hunks), v4's own test files, `package*.json`.

## Tiered deliverables

**Tier 1 (must land):**
1. The D23 re-dump + boot ensure + read tolerance + schema-key-order regen
   (survey 1), `provisioning_equivalence` green after.
2. The pure engine module with its tier-1 differential (survey 2),
   mutation-proven (each classifier non-trigger arm has a red proof).
3. The spine integration (survey 3) with the three spine families
   regenerated at the pin, red-first where behavior moved, and the
   `failing-over` stage on v5's Event/SSE wire.
4. The cheap-LLM `allowCheapFallback` thread + settings guard (survey 4).
5. The profiles CRUD + delete-nulls cascade + both remap paths
   (surveys 6–7), each family red-first on the new field.
6. The SPA mirrors (survey 8) spec-pinned, reducer carry included.

**Tier 2 (should land):**
7. The image-description site (survey 5) — if its full shape depends on
   `a1d88aa3a`'s adapter, land the `65f5021c8` state here and note the
   D136 handoff explicitly.
8. A live e2e beat for the understudy field round-trip (this branch owns
   Playwright); the `failing-over` toast beat only if it can be driven by
   wire-byte injection (the EventSource injection memory note) — otherwise
   name it a gated deferral.

**Tier 3 (explicit deferrals, loud):**
9. Help rows → `p4.9i2`. 💸 the live proof (a real dead-endpoint
   understudy answering, attributed correctly) joins the dogfood queue —
   v4's own commit verified exactly this against V4test; name the recipe.

## The differential requirement, restated

Every ported unit ships its equivalence test driving v4's REAL code from
the `65f5021c8` pin: the engine tier-1 (v4's `fallback/*.test.ts` shapes
as corpus), the spine tier-3 families (mocked-LLM, the canned failure
injected BOTH sides — the closest recipes are the
`primary_stream`/`orchestrator` rows in the lane-records index), the
routes/settings tier-2 families over real DBs, and the remap families.
Ledger §5.2 discipline throughout: grep regenerated NDJSONs for
`fallbackProfileId`, `allowTierFallback`, `failing-over`,
`allowCheapFallback` — a green regen is not coverage.

## Fixtures

The committed `system-data-*.db` family's vintage stops at
`multiCharacterPrefill` (the standing vacuity, phase-4 candidate 3) — the
two new columns land BEYOND it, so measure whether the profile-import legs
can even see them; if not, either widen the fixture (own the regen + every
consuming family, named) or land the visibility gap as a recorded,
mutation-proven unit pin and leave the widening to the named candidate.
Any fixture this lane regenerates: this lane owns re-running every family
that consumes it.

## Shared contract

**With P4.D136 (binding):** D136 starts from this branch's tip and owns
all further movement of the shared files; this lane leaves
`core-execution`-twin constants at the `65f5021c8` state and does NOT
pre-absorb the budget rewrite. **With the sibling lanes:** none — see the
Ownership table in `p4.d134-lima-wsl2-retirement.md` (binding here by
reference, identical content).

## Ownership

See the binding table in `p4.d134-lima-wsl2-retirement.md` — this branch
owns the spine/provider/profile/remap/schema files, the SPA providers +
reducer files, and the round's only Playwright slot. **Version bumps:**
this branch bumps `quilltap-core`, `quilltap-harness`, `quilltap-web` (if
its route code moves), and the SPA per commit; the unifier recounts.

## Verification gate before declaring done

`cargo fmt --check`; clippy both feature sets; full
`cargo test --workspace` with the lane's oracle env vars
(`run_in_background`, full log + sentinel; never `| tail`, never a pgrep
loop); the lane's families by name, zero SKIP, from the `65f5021c8` pin
with changed-bytes greps; `npm test` + `npm run build` in `apps/web`;
Playwright only for this branch's beats (one runner at a time, port
4319). Per-commit rule: `.claude/commands/commit.md`. Close the order
status header, write the lane record, and hand the branch tip to P4.D136.
