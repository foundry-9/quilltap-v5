# Work order P4.D220 — v4 `ad1c4c37f` (ONE `dispatchAction` primitive) + `68da64d9b` (the chat GET's fourteen actions through it) + the SERVER half of `944127d9a` (`?action=has-dangerous` removed): v4's three-way action rule on every v5 REST edge — absent → the default (or `{error:"Action parameter required", availableActions}` 400 + WARN), known (own-property) → the handler, **unknown OR bare `?action=` → `{error:"Unknown action: X", availableActions}` 400 + WARN `Unknown action requested`** with `availableActions` in v4's thunk-map order — the `query::action()` fold at the root RETIRED, the nine hand-rolled sentence sites + the two core ones (`chat_delete.rs` — a bare `?action=` DELETED the chat; `system_data.rs:465`) onto ONE envelope, the unlock edge's awaited catch, the `Request::ChatsHasDangerous` verb RETIRED end to end (§S.1 with P4.D223), `query_param_semantics_equivalence` re-recorded at the target with its `fold` claim inverted, every old-sentence pin moved — from `main`

**Status:** UNIFIED on main (2026-09-25, the `b0b6656b5` round) — **DONE:** Tier 1 items 1–8 whole; Tier 2 items 9 (`web_edge_action_sites_census`), 10 (avatar-rolls: dispatch-only, commented), 11 (the terminal unknown arm). **Fixed at unification (§3 review):** item 4's proof had posed a corrupt `.dbkey` READ as "the throw" — v4 answers that 401 `No .dbkey file found` with NO catch line (`readDbKeyFile` returns null; only `writeDbKeyFile` throws) — the test now poses the REAL write throw (a read-only file) and pins the corrupt read as v4's 401; core's change-passphrase outcome map made v4's (read-side → 401, the rewrite alone → 500 + the line; `DbKeyError::Write` splits the I/O sides — v5 had answered 400/500); the character GET's lookup moved BEFORE the gate (v4 404s a missing character first — a wire arm in `action_dispatch_edges`); the census gains the typed-extractor door (a `Deserialize` field named `action`, the pre-lane `terminal_routes.rs` shape). **OPEN (recorded, small):** the WARN `path` VALUE (pattern vs pathname) and the `availableActions` Debug render — value-only log divergences; `action_envelope_mismatch` duplicated in two harness families + `envelope()` in the edges test (a shared helper). **Deferred as ordered:** Tier 3 items 12–14 (item 13 measured: `GroupDelete`/`ProjectDelete` carry no action field; item 14 the mirror — done by the unifier). Lane record + round record: `status-log.md`.

## Preamble

- **v4 oracle baseline: `d1c06cd9d`** (drift-ledger §1). This lane absorbs
  the ledger's `ad1c4c37f` row ("Consolidate API action dispatch into single
  primitive (#68)", 2026-09-24 12:36, 107 files, +1,471/−2,376 — the web
  edges + `chat_delete.rs` + `system_data.rs`; the two `lib/` units and the
  four broken oracle cases are **P4.D221's** — `ORDERED(P4.D220, P4.D221)`),
  the `68da64d9b` row ("refactor(api): dispatch GET /api/v1/chats/[id]
  actions through dispatchAction (#69)", 8 files — `ORDERED(P4.D220)`), and
  the SERVER half of `944127d9a` ("Remove unused `GET /api/v1/chats?action=
  has-dangerous` endpoint (#66)" — the client half is P4.D223's;
  `ORDERED(P4.D220, P4.D223)`). Spec reads: `git -C ~/source/quilltap-server
  show <sha> -- <path>` for the hunks and `git show <sha>:<path>` for the
  post-commit file — never the commit message (§R.4 lists FIVE places these
  three mislead).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** This lane pins the TARGET
  **`/tmp/qt-v4-pin-p4d220-b0b6656b5`** for every family its port MOVES
  (`query_param_semantics_equivalence`, `chat_delete_equivalence`,
  `salon_reads_equivalence`, `help_chats_routes_equivalence`,
  `settings_routes_equivalence`, `help_docs_routes_equivalence`,
  `profile_*`, `messages_swipe_*`, `brahma_console_routes`,
  `system_data_*`/`backup_*`/`images_*`/`files_*`/`text_replacements_*`/
  `embedding_profiles_*` route families — MEASURE the list by grepping every
  route family's oracle for `action` probes; the survey's §F list is the
  floor, not the ceiling) and the BASELINE
  **`/tmp/qt-v4-pin-p4d220-d1c06cd9d`** for the pre-port red counts and the
  neutrality legs (every route family whose oracle never probes `?action=`
  — predicted byte-identical; re-run and record). Pin marker: the
  target-pinned `query_param_semantics` NDJSON carries
  `"availableActions"` on a `chats_collection_get__unknown` row; the
  baseline-pinned one carries `Available actions: has-dangerous`
  (`grep -c` both).
- **Sibling lanes:** P4.D221 (the `lib/` riders — owns `write_partition.
  rs`, `services/brahma_console/prompt_text.rs`, `db/fts_query.rs`'s doc,
  `db/characters.rs`/`db/memories.rs`/`db/prompt_templates.rs` method
  retirements, `photos/chat_gallery.rs`, the four broken oracle cases + their
  harness tests, Tier R, the Carina neutrality re-run — NOT yours), P4.D222
  (help reconcile — owns core `services/help_doc_sync.rs`,
  `embedding_generate_job.rs`, `db/help_doc*.rs`, `crates/quilltap-host/**`,
  `help/**`; your `help_routes.rs` hunks must NOT need a core help verb to
  move — if one does, STOP on that item and record), P4.D223 (the quick-hide
  SPA lane — deletes the CLIENT `chatsHasDangerous` reader; §S.1 binds you
  two), P4.D224 (the mention typeahead SPA lane — no contact).
- **Worktree:** `git worktree add <path> -b claude/p4-d220-action-dispatch-
  <hash> main`; `CARGO_INCREMENTAL=0`.
- **Read before the work:** the ledger's three rows whole; v4 at
  `ad1c4c37f`: `lib/api/middleware/actions.ts` whole (244 lines),
  `__tests__/unit/lib/api/middleware/actions.test.ts` whole, every route
  hunk in §Survey B (the `git show ad1c4c37f -- app/api/v1/<route>` for each
  row), `app/api/v1/system/unlock/route.ts:90-109`; v4 at `68da64d9b`:
  `app/api/v1/chats/[id]/handlers/get.ts:45-82` + `get.dispatch.test.ts`;
  v4 at `944127d9a`: `app/api/v1/chats/route.ts:1529-1540` + `route.get.
  test.ts`; v5 `crates/quilltap-web/src/query.rs` whole, every `*_routes.rs`
  site in §Survey B/C, core `api/chat_delete.rs:80-110,:225-245`,
  `api/system_data.rs:455-470`, `api/types.rs:105-125,:4180-4190`,
  `api/salon.rs:90-120`, `api/engine.rs:905-912`,
  `crates/quilltap-web/tests/query_param_semantics_equivalence.rs` whole
  (the endpoint table `:142-370`, `is_dispatcher_refusal` `:405-420`, the
  `RECORDED_EQUALITIES` pins `:816-825`, the claims `:826-861`) +
  `harness/oracle/cases/query-param-semantics.test.ts`; memory notes
  `a-required-field-on-a-shared-struct-crosses-lane-ownership`,
  `closing-a-divergence-moves-the-censuses-that-recorded-it`,
  `a-new-verb-moves-the-dispatch-wrong-type-census`, `a-v4-mock-is-not-v4-
  in-a-route-family`, `capture-layer-target-assert-is-a-prefix-match`,
  `tracing-percent-field-renders-unquoted`, `an-in-file-source-census-
  must-strip-test-modules-by-braces`, `a-tree-wide-census-needs-a-lexer`.

## Mandate

Make every v5 REST edge answer `?action=` the way v4's ONE primitive does.
Deliver: `query::action()` that DISTINGUISHES absent (`None`) from bare
(`Some("")`) — the fold at `query.rs:118-123` is the root defect, so every
consumer that let a bare action reach its default (a DELETE that deleted, a
restore that RAN, an upload that uploaded) refuses it as v4 does; ONE
envelope (`{error: "Unknown action: X", availableActions: [...]}` /
`{error: "Action parameter required", availableActions}` — `error` first,
`availableActions` second, top level, `preserve_order` already on) behind
the existing two helpers, with v4's two WARN lines (`Unknown action
requested` `{action, availableActions, method, path}` / `No action param
and no default handler` `{method, path, availableActions}` — camelCase field
NAMES, the recorded snake_case divergence CLOSED) — every hand-rolled
sentence site and the two core ones rewritten onto it, `availableActions`
carrying v4's FULL list in v4's thunk-map order even where v5 serves only a
subset of the actions on REST (the absent-action arm keeps v5's loud
"rides POST /api/dispatch" pointer where v5 has no REST default — a
recorded, pinned divergence; the unknown/bare arms take v4's bytes); the
own-property rule (`?action=toString` → unknown); the unlock edge's awaited
catch (`Error in database key action` ERROR + 500 on an async throw —
measure v5's current shape first); the chat GET's fourteen-key envelope on
`wardrobe_routes.rs`'s fan-out; `Request::ChatsHasDangerous` +
`Response::ChatsHasDangerous` + `chats_has_dangerous` + the REST arm + the
`chats_collection_route` arms + the `salon_reads` has-dangerous rows
RETIRED (§S.1); `query_param_semantics_equivalence` re-recorded at the
target with the `fold` claim INVERTED (bare ≠ empty everywhere v4 has an
envelope), the `__empty`/`__empty_then_known` rows byte-compared, the
classifier guard re-derived, the dead sentence prefixes retired; every
old-sentence pin in §Survey F moved to the new bytes. Must NOT touch:
`write_partition.rs`, `services/brahma_console/**`, `db/**`, `photos/**`,
the four `lib/`-rider oracle cases (P4.D221); core `services/help_doc_sync.
rs`, `embedding_generate_job.rs`, `crates/quilltap-host/**`, `help/**`
(P4.D222); `apps/web/**` (P4.D223/P4.D224); `api/types.rs`/`engine.rs`
BEYOND the two `ChatsHasDangerous` removals; the drift ledger.

## Survey-verified starting points (fresh survey 2026-09-24 at `ad1c4c37f` / `68da64d9b` / `944127d9a` / `b0b6656b5` and on `main` `ca3e245f` — do not re-derive)

### A. `lib/api/middleware/actions.ts` at `ad1c4c37f`

- `ActionThunkMap<A, R> = Record<A, () => Promise<R>>` (`:62-65`).
  `getActionParam` = `request.nextUrl.searchParams.get('action')`
  (`:73-75`): `null` when absent; `''` for BOTH `?action=` and a key-only
  `?action`.
- `unknownActionResponse(request, action, availableActions)` (`:83-99`):
  WARN `:88` `actionLogger.warn('Unknown action requested', { action,
  availableActions, method: request.method, path: request.nextUrl.
  pathname })`; body `{ error: \`Unknown action: ${action}\`,
  availableActions }`, 400. `actionRequiredResponse(request,
  availableActions)` (`:104-118`): WARN `:108` `'No action param and no
  default handler'` `{ method, path, availableActions }`; body `{ error:
  'Action parameter required', availableActions }`, 400. Logger =
  `logger.child({ module: 'api-action-middleware' })` (`:35`). (`path` now
  from `request.nextUrl.pathname`; messages + bag order unchanged from the
  old helpers — v5's `query.rs:189-197` doc cites `:103,124`, now `:88`/
  `:108`.)
- **`dispatchAction(request, handlers, fallback?)` (`:153-183`):**
  `availableActions = Object.keys(handlers)` (`:169` — object-literal
  insertion order; no integer-like names); ABSENT (`action === null`,
  `:171`) → `fallback()` or the `Action parameter required` envelope; KNOWN
  (`:178`) ONLY if `Object.prototype.hasOwnProperty.call(handlers, action)`
  → `handlers[action]()` (so `?action=toString` is a 400, test `:373-381`);
  **bare `''` OR unknown (`:182`) → the `Unknown action` envelope — a bare
  action renders exactly `"Unknown action: "` (trailing space) and NEVER
  reaches the fallback.**
- `withActionDispatch(actions, defaultHandler?)` (`:209-224`) rebuilt as
  `Object.fromEntries(Object.entries(actions).map(...))` → `dispatchAction`
  (key order preserved). **What changed:** the old `if (action)` truthiness
  gate (`''` → the default or `Action parameter required`) and the
  prototype-chain lookup `actions[action]` are GONE.
  `withCollectionActionDispatch` (`:236-244`) unchanged, wraps it.
- **NO try/catch in `actions.ts`** — a rejecting thunk passes through
  (test `:383-389`). The `return await run(body)` catch is in
  `app/api/v1/system/unlock/route.ts:90-109` `runUnlockAction`: ERROR
  `unlockLogger.error('Error in database key action', { action, error:
  <message|String> })` → `serverError(<message>|'Internal server error')`.
  **Old:** `return dispatchUnlockAction(action, body)` WITHOUT `await`
  inside the `try`, so an async rejection ESCAPED the catch (the log text
  did not change). The same now-awaited-inside-try change lands in
  `connection-profiles/[id]/route.ts:84`, `plugins/[name]/route.ts:218`,
  `themes/[themeId]/route.ts:166`, `themes/route.ts:46,121`,
  `scenarios/[scenarioPath]/route.ts:162`, `lib/mount-index/scenario-item-
  route-factory.ts:218` — dispatch-only in v5 (no REST edge; record).

### B. Routes changed by `ad1c4c37f` WITH a v5 REST counterpart

`badRequest(msg)` = `{error: msg}`. "ENV(list)" = the A-envelope with that
`availableActions`. v4's OLD column is what v5 reproduces today.

| v4 route | v4 old (absent / bare / unknown) | v4 new | v5 file:line | v5 today |
|---|---|---|---|---|
| GET `/chats` (`chats/route.ts:1529-1532`) | (after `944127d9a`) list / `Unknown action: . GET /api/v1/chats takes no actions` / same with X | list / `Unknown action: ` ENV([]) / ENV([]) | `chats_routes.rs:44-67` (`CHAT_GET_ACTIONS` `:36`) | the PRE-`944127d9a` shape: `has-dangerous` served `:45-60`; bare folds to list `:44`; unknown → `Unknown action: X. Available actions: has-dangerous` `:61-67` |
| DELETE `/chats/[id]` (`handlers/delete.ts`) | delete / **delete** / `Unknown DELETE action: X. Available DELETE actions: reset-state, stop-impersonate` + WARN `[Chats v1] Unknown DELETE action, rejecting to prevent data loss` `{chatId, action}` | delete / ENV(`reset-state`,`stop-impersonate`) — **no longer deletes** / ENV + the middleware WARN (the route WARN GONE) | core `chat_delete.rs:88-96` (`Some(a) if !a.is_empty()`; `''` → `Delete`), `:98-104`, `:231-242`; web `chats_routes.rs:141` (`query::first`) | old bytes; **`?action=` still DELETES** |
| POST `/system/restore` | restore ×3 | restore / ENV(`upload`,`preview`) / ENV | `backup_routes.rs:122` (`first().unwrap_or("")`, `_ =>` restore `:135`) | bare AND unknown **RUN a restore** |
| GET `/files/[id]` | download ×3 | download / ENV(`thumbnail`) / ENV — **dispatched BEFORE the file lookup** (v5 404s first) | `files_routes.rs:239` | falls through to download |
| POST `/chats/[id]/files` | upload ×3 (after the chat 404) | upload / ENV(`link`,`attach-mount-file`) / ENV (still after the chat 404) | `files_routes.rs:1113`, `:1136` | falls to upload |
| POST `/settings/text-replacements` | create ×3 | create / ENV(`bulk-replace`) / ENV | `text_replacements_routes.rs:126` | old |
| POST `/images` | upload ×3 | upload / ENV(`generate`) / ENV | `images_routes.rs:141`; module doc `:4-17` + fn doc `:125-133` assert "no envelope" | old; BOTH docs now false |
| GET `/embedding-profiles` | list ×3 | list / ENV(`list-models`,`fetch-models`,`list-providers`) / ENV | `embedding_profiles_routes.rs:88` (`_ =>` list `:101`) | old |
| GET `/help-docs` | list ×3 | list / ENV(`chat-count`,`search`) / ENV | `help_routes.rs:83-93` (module doc `:27-32`) | old |
| GET `/help-chats` | list / list / `Unknown action: X. Available actions: eligibility` | list / ENV(`eligibility`) / ENV | `help_routes.rs:117-126` | old |
| PATCH `/help-chats/[id]` | rename / rename / `… Available actions: update-context` | rename / ENV(`update-context`) / ENV | `help_routes.rs:170-191` | old |
| PATCH `/brahma-console/[id]` | rename / rename / `… Available actions: set-model` | rename / ENV(`set-model`) / ENV | `brahma_routes.rs:103`, `:131-134` | old |
| GET/POST `/system/tools` | `Unknown action: null. Available GET actions: …` / `Unknown action: . Available …` / X | `Action parameter required` ENV / `Unknown action: ` ENV / ENV. GET list = tasks-queue, job-concurrency, delete-data-preview, export-entities, export-preview, capabilities-report-list, capabilities-report-progress, capabilities-report-get, memory-dedup-preview; POST = delete-data, tasks-queue, job-concurrency, export, import-preview, import-execute, capabilities-report-generate, capabilities-report-delete, memory-dedup, ai-import-stream | `system_data_routes.rs:97-118` (`tools_unknown_action`), callers `:390`, `:564`; lists `:68-91` (v4's order) | old |
| POST `/system/jobs/[id]` | `Invalid action. Available actions: pause, resume` ×3 | Required ENV / Unknown ENV(`pause`,`resume`) | `system_data_routes.rs:628` → core `system_data.rs:463-466` | old |
| GET/POST `/system/conversation-summaries` | `Unknown or missing action.` ×3 | Required ENV / Unknown ENV(`regenerate`) | `system_data_routes.rs:580`, `:593` | old |
| POST `/system/unlock` | `Missing action parameter. Use ?action=setup, …, or ?action=lock` (absent AND bare) / `Unknown action: X` (no list) | Required ENV / `Unknown action: ` ENV / ENV(`setup`,`unlock`,`store`,`change-passphrase`,`lock`); async throws now CAUGHT (§A) | `system_data_routes.rs:777-783` (Missing), `:797-805` (v5's loud pointer), `unknown_action` `:120-125` called at `:806` | old |
| GET/PUT `/user/profile` | profile ×3 | profile / ENV(`theme-preference`) / ENV | `profile_routes.rs:89` (GET), `:105` (PUT); `unported_action` `:60-65` | `theme-preference` → v5's `Unknown action: theme-preference. Available actions: (none)` |
| PATCH `/user/profile` | `Unknown action: null. Available actions: set-avatar` / `Unknown action: . …` / X | Required ENV / ENV(`set-avatar`) | `profile_routes.rs:134-145` (`action_of` `:73-75`) | old |
| POST `/messages/[id]` | `Action parameter required: swipe or reattribute` ×3 | Required ENV / ENV(`swipe`,`reattribute`) | `messages_swipe_routes.rs:66`, `:97-110` (doc `:42-51`) | old |
| GET `/characters/[id]` | payload ×3 | payload / ENV(export, chats, cascade-preview, default-partner, get-tags, stats, depiction-guidelines) | `characters_routes.rs:835-840` | v5 pointer (pinned divergence) |
| POST `/characters/[id]` | `Unknown action: null|''|X. Available actions: favorite,…,refresh-archive,rename,archive,rehydrate` | Required ENV / ENV — **list order CHANGED to `…refresh-archive, archive, rehydrate, rename`** | `characters_routes.rs:666`, `_ =>` `:748` (`CHARACTER_ACTION_SERVED`) | v5 pointer |
| POST `/characters` | create ×3 | create / ENV(ai-wizard, ai-wizard-stream, import, quick-create, reset-builtins) | `characters_routes.rs:939-953` | v5 pointer |
| POST `/chats/[id]` | `Unknown action: null|''|X. Available actions: <47>` | Required ENV / ENV(<47>, the old constant's order) | `wardrobe_routes.rs:390`, `_ =>` `:451-457` | v5 pointer |

**`query.rs`:** `action()` `:118-123` folds `Some("")` → `None` (the doc
`:111-117` claims v4's `if (action)` truthiness — no longer true);
`unknown_action_response` `:132-157` — its WARN logs `available_actions`
(snake_case, a RECORDED divergence), body `json!({"error",
"availableActions"})` with `preserve_order` (web `Cargo.toml:26`);
`action_required_response` `:165-187`; in-file tests `:228`, `:245`.
**Every `query::action` caller** (all fold bare into absent — now wrong
wherever v4 has an envelope): `messages_swipe_routes.rs:97`,
`profile_routes.rs:178`, `embedding_profiles_routes.rs:88,:179`,
`images_routes.rs:141`, `brahma_routes.rs:103`, `characters_routes.rs:422,
:779`, `wardrobe_routes.rs:118,:146`, `system_data_routes.rs:777`,
`messages_routes.rs:67`, `chats_routes.rs:44`, `scenario_builder_routes.rs:
102,:131`, `custom_tools_routes.rs:48,:74,:150,:176`, `help_routes.rs:117,
:170`, `files_routes.rs:895,:899`.
**The `withActionDispatch` consumers' bare arm** (v4 file unchanged — the
behaviour moves through the middleware: bare WAS the default or `Action
parameter required`, NOW `Unknown action: ` ENV): avatar-rolls POST
(`characters_routes.rs:422-427`; core `characters.rs:1832-1837` bare
`Unknown action: X` — a dispatch-channel-only sentence, the TENTH site;
measure whether the REST edge reaches it) → today Required; wardrobe GET
(`characters_routes.rs:779-795`) → list; wardrobe GET/POST (`wardrobe_
routes.rs:118-131`/`:146-156`) → list/create; chat custom-tools GET/POST
(`custom_tools_routes.rs:48-55`/`:74-80`) → list/Required; custom-tools
GET/POST (`:150-161`/`:176-183`) → library/Required; embedding-profiles/[id]
POST (`embedding_profiles_routes.rs:177-201`) → Required; messages GET
(`messages_routes.rs:67-100`) → list; mount-points/[id] POST (13 actions;
`files_routes.rs:895-919`) → Required; scenario-builder GET/POST
(`scenario_builder_routes.rs:102-108`/`:131-137`) → Required; system/data-
dir POST (`profile_routes.rs:178-202`) → Required; terminals/[id] POST
(`terminal_routes.rs:244-268`; v4's default is `badRequest('Missing or
invalid action parameter')` — v5 answers that for unknown too, a
PRE-EXISTING divergence to close in passing); chat-files/[id],
autonomous-room, chats/[id]/messages/[messageId], documents,
search-replace → NO v5 REST route (`lib.rs:318-676`).
**The nine hand-rolled sentence sites:** `chats_routes.rs:61-67`; core
`chat_delete.rs:99-104`; `help_routes.rs:122-125`, `:187-190`;
`brahma_routes.rs:131-134`; `system_data_routes.rs:110-117`
(`tools_unknown_action`), `:120-125` (`unknown_action`, unlock);
`profile_routes.rs:60-65` (`unported_action`), `:136-144`. Non-"Unknown"
old sentences: `messages_swipe_routes.rs:66`, `system_data_routes.rs:581,
:594,:780`, core `system_data.rs:465`, `terminal_routes.rs:267`. (`Invalid
action. Must be "start" or "stop"` at `system_data_routes.rs:423` / core
`system_data.rs:387` is a BODY field — not this order's.)

### C. `68da64d9b` — `chats/[id]/handlers/get.ts`

The thunk map (`:60-82`), FOURTEEN keys in this order: `export`,
`export-markdown`, `get-avatars`, `get-state`, `outfit`, `outfit-summary`,
`photo-albums`, `informs`, `group-stores`, `mailbox`, `accessible-stores`
(reads `all === 'true'`), `get-background`, `gallery`, `cost` — the old
if-ladder's order (pre-commit `:48-249`). Fallback `() => handleGetChat(req,
ctx, chatId)` (`:81`; body `:242-411`). **Unknown/bare answer 400 BEFORE
any chat lookup** (the test asserts `findById` NOT called) — an unknown
action on a MISSING chat is 400, not 404. **No handler body changed
observably:** `handleExport` `:88-175`, `handleGallery` `:182-206`,
`handleCost` `:212-236`, `handleGetChat`, `get-background` → `actions/story-
background.ts` `handleGetStoryBackground` (identical: WARN `[Chats v1] Story
background file not found` `{chatId, storyBackgroundImageId}`; ERROR
`[Chats v1] Failed to get story background` / `Failed to get story
background`); unused imports dropped. **v5:** `wardrobe_routes.rs:292-375`
`chat_action_get` uses raw `query::first` (`:300`), serves `outfit`
(`:301`), `outfit-summary` (`:308`), `informs` (`:317`), `export` (`:328`),
`export-markdown` (`:353-372`), ends `_ => chat_get_background(...)` at
`:373`; `text_replacements_routes.rs:287-317` `chat_get_background` serves
`get-background` (`:296`), `cost` (`:297-302`), else the pointer `:306-311`
`"Only the get-background and cost actions are served on this route; the
chat GET rides POST /api/dispatch"`.

### D. `944127d9a` — every v5 `has-dangerous` site

v4 after `944127d9a`: GET `:1531-1540` rejects `action !== null` with
`Unknown action: ${action}. GET /api/v1/chats takes no actions`;
`handleHasDangerous` + `CHAT_GET_ACTIONS` DELETED. After `ad1c4c37f`:
`dispatchAction(req, {}, () => handleList(...))` (`:1529-1532`) — **the
interim sentence never needs porting (§R.4).** v5 core: `api/types.rs:112-
119` (`Request::ChatsHasDangerous`, a UNIT variant — the dispatch census
does not count it), `:4184-4185` (`Response::ChatsHasDangerous(Value)`),
`api/salon.rs:94-118`, `api/engine.rs:909-910`. Web: `lib.rs:431-434`,
`chats_routes.rs:4-9,:36,:45-60`. Tests: `web/tests/chats_collection_route.
rs:11-12,:47-78,:83-87`; `web/tests/query_param_semantics_equivalence.rs:
191-196` (`chats_collection_get`, known = `has-dangerous`); harness
`salon_reads_equivalence.rs:415-445` (four has-dangerous cases) + `:662-679`
(the sentence check); oracle `salon-reads.test.ts:47,:256-259,:374-402` and
`query-param-semantics.test.ts:145-150,:457`. SPA sites → P4.D223. No e2e
reference.

### F. What reds at the target (measured predictions — record the counts)

`query_param_semantics_equivalence.rs`: the endpoint table `:142-370` (32
endpoints × 6 shapes; oracle `:88-370`; fixture venue `system-data-main/
mount/llmlogs.db` `:678-691`). **`fold` (`:830`, `empty` == `bare`)** now
records `false` on every endpoint where bare used to run a default or
`Action parameter required` — v5 folds `''` → absent (`true`), so
`<key>_fold` reds on essentially every non-pinned endpoint (`system_tools_*`
and `user_profile_patch` were already false), incl. `character_item_get`,
`characters_collection_post`, `chat_item_get` (no `RECORDED_EQUALITIES` pin
`:816-825`). The `__empty` / `__empty_then_known` rows become dispatcher
refusals and byte-compare (`chats_collection_get__empty`: v4 400
`{"error":"Unknown action: ","availableActions":[]}` vs v5 200 list).
`chats_collection_get__unknown` reds on body. Every `__bare`/`__unknown` row
whose old sentence was hand-rolled (tools, profile PATCH, brahma, jobs,
conversation-summaries, unlock) reds on body. **The classifier guard
`refusal_rows >= 40 && handler_rows >= 40` (`:857-861`)** likely fails its
`handler_rows` half (~15 `__bare` fallback rows remain) — re-derive the
guard from the new truth, do not weaken it blind. `firstWins` /
`emptyFirstWins` stay green. The dead prefixes in `is_dispatcher_refusal`
(`:410-419`) retire. **Other old-sentence pins that red at the target:**
`chat_delete_equivalence.rs:633` (`action: Some("")`), `:722`, `:744`,
`:756-851`; `web/tests/chat_delete_route.rs:103-107`, `:142-146` (`?action=`
DELETES); `salon_reads_equivalence.rs:415-445`, `:670`;
`chats_collection_route.rs:57-86`; `help_web_routes.rs:178`, `:270`;
`help_chats_routes_equivalence.rs:330`, `:450`; `profile_web_routes.rs:110`,
`:124`; `messages_swipe_sse_route.rs:431`; `settings_routes_equivalence.rs:
367`, `:371` (connection-profile sentinels — v4 is now ENV(`get-tags`) /
ENV(`add-tag`, `remove-tag`, `auto-configure`)).

### H. v4's new tests (mirror as REST arms through the existing route
families — real fixtures, never v4's mocks; §R.6)

`actions.test.ts`: `?action=first&action=second` → `'first'`; `?action=`
→ `''`; known → handler; no action + default → default; unknown, no
default → 400 `{error:'Unknown action: unknown', availableActions:
['favorite','export','avatar']}`; no action, no default → 400 `{error:
'Action parameter required', availableActions:[…]}`; empty map +
`?action=any` → `Unknown action: any`, `[]`; **bare + a default → 400,
the default NOT called**; `dispatchAction`: absent → fallback once; known
→ that thunk only; unknown → 400 with the map's keys, fallback not called;
bare → `'Unknown action: '`; `?action=toString` → 400; a rejecting thunk
rejects through; a plain `Response` passes. `get.dispatch.test.ts`: no
action → 200 `body.chat.id`, no action handler called; `?action=outfit` →
that handler only; `accessible-stores&all=true` → `{all:true}`, without →
`{all:false}`; `?action=cost` → 200; `?action=galery` → 400 with the 14
keys and **`findById` NOT called**; `?action=` → 400 same list.
`route.get.test.ts`: no action → 200 + `findByUserId`; `?action=has-
dangerous`, `?action=`, `?action` → 400 each, `findByUserId` NOT called.
`restore/route.test.ts`: `?action=wipe-everything` → 400 `['upload',
'preview']`, neither restore nor preview called. `groups/[id] delete`: no
action → delete; `removeMember` / `reset-state` → handlers; `?action=
unlinkStore` → 400 `['removeMember','reset-state']`, no delete.
`projects/[id] delete`: `?action=clear-mount-point` → 400 `['remove-
character','remove-chat','remove-file','reset-state']`; `?action=` → 400,
no delete (groups/projects DELETE are dispatch-only in v5 — record which
core handler owns the arm; if a bare `?action=` on the dispatch channel
cannot exist, say so). `scenario-item-route-factory.test.ts`: unknown
`archive` → `{error:'Unknown action: archive', availableActions:
['rename']}` (was `'Unknown action — supported: rename'`).

## Tiered deliverables

### Tier 1 — must land

1. **The root:** `query::action()` returns `Option<&str>` with `Some("")`
   for a bare `?action=` / `?action` and `None` for absent (a NEW helper
   name if the old one's callers must be audited one by one — say which);
   the two response helpers rewritten as v4's `dispatchAction` twin
   (absent → default-or-required; own-property known → handler; bare OR
   unknown → the `Unknown action` envelope with the FULL v4 list in v4's
   order) with v4's two WARNs at v4's field NAMES (camelCase; the snake_case
   divergence CLOSED — recount any census that recorded it); in-file tests
   grown with v4's `actions.test.ts` vectors (§Survey H) incl. `toString`
   and the trailing-space bare message. ONE commit, FIRST.
2. **Every consumer** in §Survey B + the `withActionDispatch` list re-based
   on it (the 24 `query::action` callers + the nine sentence sites + the
   non-"Unknown" sentences + `terminal_routes.rs`'s pre-existing unknown-arm
   divergence), each with v4's FULL `availableActions` in v4's order
   (transcribe from the post-commit thunk maps — incl. the CHANGED order on
   `POST /characters/[id]`), the absent-action arm as v4's default where v5
   serves it and v5's loud pointer where it does not (pinned as a
   DIVERGENCE both ways, per endpoint, in `query_param_semantics`'s
   `RECORDED_EQUALITIES` shape — grow the table, never blanket-skip); the
   `images_routes.rs` module + fn docs corrected; **the four data-affecting
   arms red-first BEFORE the fix**: `chat_delete_route.rs` (a bare
   `?action=` MUST NOT delete — count rows before/after), `backup_routes`
   (a bare/unknown `POST /system/restore` MUST NOT run a restore),
   `files_routes.rs:239` (`thumbnail` dispatched BEFORE the file lookup —
   an unknown action on a missing file is 400, not 404 — measure v4's
   post-commit order from the hunk), `files_routes.rs:1113/:1136` (a bare
   `?action=` MUST NOT upload).
3. **Core:** `api/chat_delete.rs:88-104,:231-242` — `Some("")` refuses
   with the envelope (`reset-state`, `stop-impersonate`), the route-level
   WARN `[Chats v1] Unknown DELETE action, rejecting to prevent data loss`
   GONE (silence leg) and v4's middleware WARN in its place; `api/system_
   data.rs:463-466` the jobs envelope (`pause`, `resume`). `chat_delete_
   equivalence` re-recorded at the target red-first on `:633,:722,:744,
   :756-851`.
4. **The unlock edge:** an async throw inside a known action → ERROR `Error
   in database key action` `{action, error}` + 500 (`serverError(<message>
   | 'Internal server error')`) — measure v5's current shape on a posed
   throw first (a red-first arm on `system_data_routes`' unlock family, or
   a unit pin if the family cannot pose one); the absent/bare/unknown
   envelope with v4's five keys.
5. **The chat GET's fourteen keys:** `wardrobe_routes.rs:292-375` +
   `text_replacements_routes.rs:287-317` — unknown/bare → v4's envelope
   with all fourteen in v4's order, BEFORE any chat read (a missing chat +
   unknown action → 400); the absent arm keeps the pointer (pinned
   divergence); the served subset unchanged. `query_param_semantics`'s
   `chat_item_get` rows re-recorded.
6. **`ChatsHasDangerous` retired (§S.1):** the two variants (`types.rs:
   112-119,:4184-4185`), `salon.rs:94-118`, `engine.rs:909-910`, `lib.rs:
   431-434`'s comment, `chats_routes.rs:4-9,:36,:45-60` → v4's `dispatch
   Action(req, {}, list)` twin (absent → list; bare/unknown → ENV([]));
   `chats_collection_route.rs`'s has-dangerous arms → the three v4
   `route.get.test.ts` vectors; `salon_reads_equivalence.rs:415-445` + the
   oracle's four `hasDangerous` rows RETIRED, `:662-679` re-pinned on the
   envelope; `dispatch_wrong_type_census` re-run (UNMOVED at 449 predicted —
   a unit variant; record the measurement).
7. **`query_param_semantics_equivalence` re-recorded at the target:** the
   `fold` claim INVERTED where v4 has an envelope (a per-endpoint expected
   `fold` value derived from the RECORDED oracle, not asserted blind), the
   `__empty`/`__empty_then_known` rows byte-compared, the classifier guard
   re-derived with the arithmetic in the comment, the dead prefixes retired,
   `chats_collection_get`'s known action → none (`[]`), every
   `RECORDED_EQUALITIES` pin re-measured. **Every other old-sentence pin in
   §Survey F moved** (`help_web_routes`, `help_chats_routes_equivalence`,
   `profile_web_routes`, `messages_swipe_sse_route`, `settings_routes_
   equivalence`'s two sentinels) — each family regenerated at the target
   red-first, the pre-fix count recorded.
8. The `query.rs` docs (`:111-117`, `:189-197`) rewritten to the truth with
   the sha; a v5-side comment at `chats_routes.rs` naming `944127d9a`'s
   interim sentence as never-ported (§R.4).

### Tier 2 — should land

9. A `web_edge_action_sites_census` (or a row in an existing web census):
   every `*_routes.rs` `?action=` read goes through the ONE helper — the
   raw `query::first(…, "action")` reads (`wardrobe_routes.rs:300`,
   `chats_routes.rs:141`, `backup_routes.rs:122`) counted to ZERO after the
   port, with the lexer-based scanner (`source_census/mod.rs`), never a
   naive grep.
10. The dispatch-channel `Unknown action` sentence at core `characters.rs:
    1832-1837` (avatar-rolls): if the REST edge reaches it, onto the
    envelope; if dispatch-only, a one-line comment naming why it stays.
11. `terminal_routes.rs:244-268`: the unknown-action arm as v4's
    (`Missing or invalid action parameter` is v4's DEFAULT; v4's unknown is
    now the envelope) — close the pre-existing divergence if the terminal
    route family can pin it; else record.

### Tier 3 — explicit deferrals (loud, typed, never silent)

12. The seven now-awaited catches in dispatch-only v4 routes (§Survey A's
    list) — NO v5 REST edge; recorded, not ported.
13. `groups`/`projects` DELETE's bare-`?action=` arm (v4 no longer deletes
    the entity) — dispatch-only in v5; measure whether a bare action can
    even reach the core handler over dispatch; record.
14. The `docs/v4/developer/DDL.md` + `concierge-list-marks.md` mirror
    prose — the unifier's (§R.9).

## The differential requirement, restated

Every moved edge lands with its REST family regenerated at the target
(`query_param_semantics` is the tripwire over 32 endpoints × 6 shapes —
its oracle drives v4's REAL route modules through the REAL middleware; the
per-route families cover the bodies) — red-first, the pre-fix count
recorded per family. v4's seven new/changed unit tests MOCK the handlers;
mirror each vector as a REST arm over the real fixture, never the mock
(`a-v4-mock-is-not-v4-in-a-route-family`). The two WARNs land with
capture pins (message + camelCase field names + a silence leg on the
known-action path). Divergences (the absent-arm pointers) are pinned BOTH
ways per endpoint. Recipes: each family's own header; the sweep driver
with `--v4 /tmp/qt-v4-pin-p4d220-b0b6656b5`; `query_param_semantics`'s
jest case needs the pin's `node_modules` symlinks.

## Fixtures

Consumes: `system-data-main/mount/llmlogs.db` (the `query_param_semantics`
venue — UNCHANGED), every route family's committed pair (UNCHANGED —
plants on per-run COPIES). Delivers: no fixture change. A red on a pair
this lane does not widen is a FINDING to classify at both pins.

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `b0b6656b5` ten-commit drift catch-up round: **P4.D220 ∥ P4.D221 ∥
P4.D222 ∥ P4.D223 ∥ P4.D224**, ordered 2026-09-24. Five lanes, five
worktrees, five lane branches, **every one cut from `main` (`ca3e245f` or
the commit that lands these orders)** — no keystone, no stacked lane. Two
are SPA-only (P4.D223, P4.D224 — no cargo), three are Rust (P4.D220,
P4.D221, P4.D222; P4.D222 also touches two SPA files). The unifier
cherry-picks in the order **P4.D221 → P4.D222 → P4.D220 → P4.D223 →
P4.D224** (the `lib/` riders and their retired oracle rows first; the help
tree next so every later regen runs over the 129-file tree; the dispatch
lane before the SPA lane that drops the client half of the same verb; the
mention lane LAST because its one pre-declared hunk lands in P4.D223's
file), gates ONCE from the new baseline pin, and `merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`d1c06cd9d`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`b0b6656b5`** ("Fix
  bug 169: narrow-pane chat sidebar no longer closes dialogs it opens
  (#71)", `4.10.0-dev.78`, 2026-09-24) — **TEN non-merge commits past the
  baseline** plus one clean merge (`f51a44548`, no row): `e3937d7aa`
  (P4.D223), `7ebb74143` (NO-PORT, ratified by P4.D221), `3376b3dfa`
  (P4.D224; help half P4.D222; Rust neutrality P4.D221), `944127d9a`
  (server P4.D220 / client P4.D223), `492771aff` (P4.D222), `94e946728`
  (NO-PORT, ratified by P4.D223), `ad1c4c37f` (web edges + core dispatch
  P4.D220 / `lib/` riders + oracle repairs P4.D221), `68da64d9b`
  (P4.D220), `8aafd595d` (NO-PORT mostly, ratified by P4.D221 with its
  item (1) as a small), `b0b6656b5` (CONVERGENCE, P4.D223) — every row
  `ORDERED(…)` in the ledger's §3. v4 `bugfix` tip `1a2b2164c` and
  `release` tip `8fbf2afe0` are unmoved; still no `release: 4.10.0`
  squash. The checkout sat on `main`, CLEAN, at `b0b6656b5` at the
  planning probe (2026-09-24 — the §2 probe PASSED; nothing was
  re-derived). Spec reads: `git show <sha>:<path>` for post-commit files
  and `git show <sha> -- <path>` for the hunks — **never the commit
  message** (§R.4). **At unification the baseline MOVES to `b0b6656b5`**;
  the rows retire `ABSORBED(…)` / `NO-PORT-RATIFIED(…)`.
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log b0b6656b5..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Pass = branch `main`, HEAD `b0b6656b5`, both logs EMPTY, tree
  CLEAN.** Any mismatch — a moved HEAD (a new commit is a NEW drift row,
  not a pass), a non-empty log, ANY dirty path, the checkout off `main` —
  is a STOP: report it and do nothing else. A lane never writes the
  ledger. A lane resumes ONLY on a waiver the human records in the
  ledger's §1 (the `89fcc3c0d` round's mechanism), never on its own reading
  of the dirt.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1), absolutely. HEAD
  is ten commits past the baseline and the commits rewrite the route
  middleware every REST family imports, the help-sync module, and four
  oracle cases' imports. Every regen runs from a LANE-UNIQUE detached
  worktree per ledger §5.1 (`git -C ~/source/quilltap-server worktree add
  --detach /tmp/qt-v4-pin-<order>-<sha> <sha>`, the THREE symlink classes —
  root `node_modules`, `packages/quilltap/node_modules`, every
  `plugins/dist/*/node_modules` — then `cd` into it; never stash or
  checkout-switch the human's repo; `git worktree list` before each batch;
  **verify the pin by `git -C "$PIN" rev-parse HEAD` AND by `ls -ld` of
  the path** — build every pin path from a quoted literal, never an
  unsplit zsh variable; then grep the fresh NDJSON for a marker only one
  tree has; check the file is non-empty before believing a diff). **Every
  lane pins the TARGET `b0b6656b5`** (`/tmp/qt-v4-pin-<order>-b0b6656b5`)
  for every family its port MOVES and for its neutrality re-runs, **and
  the BASELINE `d1c06cd9d`** (`/tmp/qt-v4-pin-<order>-d1c06cd9d`) for the
  pre-port red counts; the SPA recorders (P4.D223 if any, P4.D224's two,
  P4.D222's help-guide capture) run from the TARGET pin. A family that
  moves under TWO lanes (none is predicted — the fences in §R.10 are
  drawn so that each family has ONE owner; a lane that finds one STOPs on
  it and records) is the UNIFIER's to re-run from the new baseline pin
  with every lane's growth present. Pin verification marker (round-wide):
  the target-pinned `help_tree_equivalence` NDJSON names `chat-settings-
  composer` (a file that exists only at the target); the baseline-pinned
  one does not (`grep -c` both). The sweep driver is the sanctioned
  per-family path (`python3 harness/tools/recipe_sweep.py --run <family>
  --v4 "$PIN" --v5w <ABSOLUTE worktree>`); never run two sweeps
  concurrently; a family that reports `SKIP` in 0.00 s did not run.
  Ledger §5.2's silent-stale-pass discipline applies to every regen
  (`rm -f` the output first; read the builder's last line; `grep -c` the
  changed bytes in the fresh NDJSON — MUST be > 0 where a change is
  predicted). Every lane stages its regen outputs under a LANE-PRIVATE
  directory (`/tmp/<order>/…` — several families' committed recipes write
  FIXED `/tmp/oracle-*.ndjson` paths; re-derive into the lane's directory
  before any `cargo test --workspace`) and leaves the committed recipe
  headers canonical (a committed recipe NEVER names a `/tmp` pin — the
  driver's `--v4` is the pin). Every jest oracle writes ZERO `llm_logs`
  rows (`jest.setup.ts` mocks the logging service) — never predict an
  `llm_logs` diff from a jest family; count the canned-LLM calls instead.
  `QT_NODE` in any env block is a REAL path
  (`~/.nvm/versions/node/v24.13.1/bin/node`), never `$N/node`. **The
  checkout's `node_modules` may LACK `js-tiktoken`** (`492771aff` adds it
  as a devDependency; only the human's `npm install` installs it) — a lane
  that needs it MEASURES `ls ~/source/quilltap-server/node_modules/
  js-tiktoken` and records BLOCKED rather than installing anything in the
  human's checkout.
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). Measured
  at planning: (a) `68da64d9b`'s message says it "adds `dispatchAction`" —
  its diff never touches `lib/api/middleware/actions.ts` (PR #69 branched
  before #68); what ships is the chat GET's ladder → the fourteen-key
  thunk map. (b) `ad1c4c37f`'s message says it "migrates every route" —
  `chats/[id]/handlers/get.ts:45` still reads `getActionParam` at that sha;
  it is migrated only by `68da64d9b`. (c) `8aafd595d`'s message says its
  new test "found `groupDocMountLinks` and `groupCharacterMembers` missing
  … now listed" — the two keys arrived in `ad1c4c37f`'s hunk
  (`write-partition.ts:58-59`); `8aafd595d` changes NO key. (d)
  `944127d9a`'s message says the only consumer "was removed" — that
  removal is `e3937d7aa`'s hunk, not this commit's; and its interim
  sentence `Unknown action: X. GET /api/v1/chats takes no actions` is
  SUPERSEDED by `ad1c4c37f`'s envelope four commits later — it never needs
  porting. (e) `492771aff`'s Phase 3.66 calls `ensureHelpDocsSynced`
  (which wraps `reconcileHelpDocs`), not `reconcileHelpDocs` directly; its
  `HELP_SECTION_EMBEDDING_MAX_TOKENS` lives in the production module but
  is consumed only by the new test; and the ledger's "thirteen help files"
  is TWELVE (two added, ten modified). (f) `e3937d7aa`'s message says
  participant-sidebar avatars hide — they do, but ONLY through
  `ui/Avatar`; no hunk touches `ChatSidebar`/`ParticipantCard`. (g)
  `3376b3dfa`'s `LINE_RE` rebuild is byte-identical (measured `.source` +
  `.flags`); the commit adds NO dependency. Re-verify even where an order
  flags the trap — the order's paragraph is the same kind of prose. Carry
  a v5-side code comment naming the sha where the prose misleads.
- **§R.5 — Designed reds and pre-existing reds: measure, don't assume**
  (ledger §5.4). **ONE convergence row** (`b0b6656b5`, P4.D223): v5 never
  pinned v4's OLD sidebar behaviour, only its own, so no pin trips — the
  wording retires and the residual text-node/null-target difference closes
  by taking v4's twin. The DESIGNED reds: **P4.D220** —
  `query_param_semantics_equivalence` at the target on its `fold` claim
  (nearly every non-pinned endpoint), on every `__empty`/`__empty_then_
  known` row, on `chats_collection_get__unknown`, on every hand-rolled
  `__bare`/`__unknown` sentence row, and probably on the classifier
  guard's `handler_rows >= 40` half; `chat_delete_equivalence` +
  `chat_delete_route` on the bare-`?action=`-deletes arm; `salon_reads` on
  the four has-dangerous cases + the sentence check; `chats_collection_
  route`, `help_web_routes`, `help_chats_routes_equivalence`,
  `profile_web_routes`, `messages_swipe_sse_route`, `settings_routes_
  equivalence` on their old-sentence pins. **P4.D221** — `write_partition_
  equivalence` on the two grown group-key rows (RED on v5 at BOTH pins
  until the keys land — the oracle at either pin classifies them
  mount-index? NO: the baseline oracle lacks the keys too, so the rows red
  ONLY at the target; record both); `brahma_console_tier3` at the target on
  the system-prompt bytes; `fts_query` / `characters_read` / `memories_read`
  / `memories_tier2` — their oracle CASES fail to IMPORT at the target
  (a tsx/jest crash, not a differential red — the empty-file trap, §5.1)
  until repointed/retired. **P4.D222** — `help_tree_equivalence` +
  `help_tree_embed_guard` + `host_help_docs_boot` at 127 vs 129;
  `embedding_generate_jobs` on `hd-happy` (the whole-text call vanishes);
  `help_doc_ensure` on the scenarios whose semantics moved; `help_doc_sync`
  on the `embedding_status` clear; `help_docs_upsert_tier2`'s case fails
  to import (retired). **P4.D223 / P4.D224** — no Rust red; their SPA specs
  are red-first by construction. The `/tmp/qt-imggen-*` staging collision
  remains the one recorded standing intermittent in `cargo test
  --workspace` (green by name); the Salon-streaming e2e timing cluster
  (five untouched files, each green alone) and the `salon-regenerate-
  stream-flow` `2/3` watch item are the recorded Playwright intermittents.
  A red in a family a lane did NOT touch is a NEW finding to classify at
  both pins before any source is touched, and it is probably not that
  lane's.
- **§R.6 — The differential requirement stands for every unit.** A ported
  unit lands WITH an equivalence test that drives v4's REAL code at the
  right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM; the SPA's tier-1 recorders for pure client modules),
  red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest` is
  shared) and its silence leg; a fixture change lands with the regen of
  every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that SURVIVES
  as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`). A v4 unit test that
  MOCKS a repository or a handler (every new test in this round's commits
  does) is mirrored as a real-DB / real-route arm with PLANTED rows through
  the existing oracle, never as the same mock (`a-v4-mock-is-not-v4-in-a-
  route-family`). A ruled divergence is pinned in BOTH directions (the
  `EXPECTED_DIVERGENCES` shape, `markdown_transcript_equivalence.rs:40,
  213-233,256-261`): a "divergence VANISHED" failure if v4 == v5, a "WRONG
  SHAPE" failure if the difference is not the ruled one, and an assert
  that every declared divergence was exercised.
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane; `docs/developer/porting/phase-4.
  md`, `dogfood-findings.md` and the `docs/v4/` mirror are the UNIFIER's
  (a lane pre-lists what moves). Cross-order header edits allowed this
  round: NONE (no lane closes another order's open item).
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed on each commit per `.claude/commands/commit.md`; the unifier
  RECOUNTS as base + the sum of every lane's bumps (the silent-auto-merge
  trap: identical bumps merge as ONE — it fires every round). Base at
  planning: core 0.0.1044, harness 0.0.964, host 0.0.154, web 0.0.191, cli
  0.0.27, tauri 0.0.7, fixture-sanitizer 0.0.4, sqlite3mc-sys 0.1.0
  (PINNED, never bumped), SPA 0.5.761. Expected: **core** by P4.D220
  (`chat_delete.rs`, `system_data.rs`, `types.rs`, `salon.rs`,
  `engine.rs`), P4.D221 (`write_partition.rs`, `prompt_text.rs`,
  `fts_query.rs`, the db retirements, `chat_gallery.rs`), P4.D222 (the
  help/embedding modules); **harness** by all three Rust lanes; **web** by
  P4.D220 (its whole surface), P4.D222 ONLY if a web test moves (say so);
  **host** by P4.D222 ONLY (the boot order + `host_help_docs_boot`);
  **cli** by NOBODY (Tier R is a re-run); **tauri**, **fixture-sanitizer**
  by NOBODY; **the SPA** by P4.D222 (the help-categories capture), P4.D223,
  P4.D224. No lane touches the workspace `Cargo.toml` dependency block or
  `apps/web/package.json`'s dependency block; every `Cargo.toml` /
  `package.json` delta must be version-only — the unifier audits each
  before any resolution (the `--theirs` clobber trap).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's: each
  lane pre-lists, in its lane record with byte counts at `b0b6656b5`, the
  mirror paths its rows move. This round's mirror rules in one place
  (measured at planning against `b0b6656b5`): `docs/v4/developer/bugs/
  fixed/bug-167-help-reindex-fk-rollback.md` (NEW, 3,817),
  `fixed/bug-168-help-doc-embed-overflow.md` (NEW, 2,989),
  `fixed/bug-169-narrow-sidebar-closes-portaled-dialog.md` (NEW, 5,048 —
  v5 has NO copy under `bugs/` either), `docs/v4/developer/bugs.md`
  (309,151 → 311,817), `docs/v4/developer/features/complete/concierge-
  list-marks.md` (24,474 → 24,767), `docs/v4/developer/features/composer-
  typeahead.md` (36,009 → 36,537), `docs/v4/developer/DATABASE_
  ABSTRACTION.md` (13,959 → 15,160), `docs/v4/developer/DDL.md` (125,326 →
  125,835 — prose only). `docs/v4/CHANGELOG.md`'s older lag (83,228 vs
  129,336) stays a named housekeeping item, not widened here.
  `README.md`, `CLAUDE.md`, `.claude/commands/release-checklist-4-api-
  standard.md`, `jest.setup.ts`, every `package.json`/`package-lock.json`
  stamp, `scripts/update_version.sh` → NO-PORT, the standing class. **The
  `help/` count MOVES 127 → 129** — P4.D222 re-vendors the WHOLE tree at
  `b0b6656b5` and moves BOTH literals (`help_tree_embed_guard.rs:69`,
  `host_help_docs_boot.rs:98`); `help_tree_equivalence` at the target pin
  is the proof the tree is v4's. The `dogfood-findings.md` #120 note
  (CONVERGENCE — v4 bug 168 fixed it by section-averaging, a mechanism
  #120 never proposed) is the unifier's, on P4.D222's record.
- **§R.10 — Fences, ownership edges and the pre-declared spills.** (a)
  **`api/types.rs` and `api/engine.rs`:** P4.D220 ONLY, and ONLY the two
  `ChatsHasDangerous` REMOVALS (`types.rs:112-119`, `:4184-4185`;
  `engine.rs:909-910`) — nobody ADDS a variant this round (no new verb).
  `dispatch_wrong_type_census` is expected UNMOVED at 449 (a unit variant
  has no typed field) — P4.D220 runs it and records. (b) **`crates/
  quilltap-web/src/**`:** P4.D220's alone, incl. `help_routes.rs`,
  `query.rs`, `lib.rs`; P4.D222 does NOT touch a web route (a help verb's
  REST arm that must move is RECORDED for the unifier). (c) **Core
  services:** `services/help_doc_sync.rs`, `services/embedding_generate_
  job.rs`, `services/embedding_reindex_job.rs` (re-run only), `db/help_
  docs.rs`, `db/help_doc_chunks.rs`, `db/help_doc_chunks_repair.rs`,
  `embedding_vector.rs` → P4.D222; `write_partition.rs`, `services/brahma_
  console/**`, `db/fts_query.rs`, `db/characters.rs`, `db/memories.rs`,
  `db/prompt_templates.rs`, `photos/chat_gallery.rs` → P4.D221; `api/chat_
  delete.rs`, `api/system_data.rs`, `api/characters.rs:1832-1837` (if
  Tier 2 item 10 lands), `api/salon.rs` (the removal) → P4.D220. (d)
  **`crates/quilltap-host/**`:** P4.D222's alone. (e) **`help/**` (the
  vendored tree) + the two count literals + `apps/web/src/app/help/**` +
  `apps/web/oracle/help-guide-capture.test.tsx`:** P4.D222's alone — the
  two SPA lanes never touch them. (f) **`apps/web/**` otherwise:** P4.D223
  owns `quick-hide/**`, `shell/user-menu*`, `ui/avatar.ts`, `chat/message-
  row.ts`, `chat/streaming-message.ts`, `chat/tool-message.ts`, `chat/
  message-content.ts`, `chat/render/**`, `chat/sidebar/**`, `chat/speaker-
  selector.ts`, `chat/speaking-as-avatar.ts`, `chat/all-llm-pause-modal.
  ts`, `chat/reattribute-message-dialog.ts`, `chat/select-llm-profile-
  dialog.ts`, `chat/cast/**`, `chat/impersonation-voice/**`, `chat/inform-
  dialog.ts`, `chat/post-office/**`, `screens/salon/salon-conversation.ts`,
  `core/core-contract.ts` (the `ChatsHasDangerous` removal), `e2e/{quick-
  hide-flow,concierge-marks-flow,scenario-builder-flow}.spec.ts`; P4.D224
  owns `editor/**`, `chat/chat-composer.ts`, `chat/carina-parser*`, NEW
  `chat/mentions/**`, `src/testing/fixtures/{mention-typeahead,carina-
  parser}.ndjson` + their recorders, NEW `e2e/composer-mention-flow.spec.
  ts`. (g) **Oracle cases:** `write-partition.ts`, `gen-brahma-prompts.
  mjs`, `fts-query.ts`, `characters-read.ts`, `memories-read.ts`,
  `memories-tier2.ts` → P4.D221; every help/embedding case → P4.D222;
  `query-param-semantics.test.ts`, `salon-reads.test.ts`, `chat-delete*`
  and every route-family case with an `?action=` probe → P4.D220. (h)
  **Committed fixtures:** NOBODY widens a pair this round; P4.D222 DELETES
  the `help-docs-upsert` builder/spec/fixture; P4.D224 re-records
  `carina-parser.ndjson`; P4.D222 re-captures `help-guide-tables.json`.
  (i) **Pre-declared loud out-of-mandate edits** (the `a-required-field-
  on-a-shared-struct-crosses-lane-ownership` rule: do the edit, keep it to
  the hunk, mark it `// P4.xxx OUT-OF-MANDATE — <owner> preserves`, audit
  with `git diff main -- <path>` at lane close): **P4.D224 → `screens/
  salon/salon-conversation.ts`** — ONE hunk: the `castCharacterIds`
  computed + the `[mentionPriorityCharacterIds]` binding on `<qt-chat-
  composer>` (§S.2); nothing else in that file. If a lane finds it needs
  another lane's file beyond this, it STOPs on that item and records the
  shape for the unifier.
- **§R.11 — Two SPA lanes, one Rust-lane SPA touch.** P4.D223 and
  P4.D224 are SPA-only and run without cargo; P4.D222 touches exactly two
  SPA files (the help-categories capture) and runs the SPA gate for them.
  The SPA's e2e port 4319 is repo-wide — the three lanes that run
  Playwright coordinate through the human (one suite at a time;
  `e2e-port-4319-is-repo-wide`).

## §S — Shared contract (binding; byte-identical in every order of this round)

### §S.1 — The `chatsHasDangerous` verb RETIRES (P4.D220 ↔ P4.D223)

v4 `944127d9a` deleted `GET /api/v1/chats?action=has-dangerous`, and
`e3937d7aa` deleted its only client reader. In lockstep: **P4.D220
deletes** `Request::ChatsHasDangerous` (`crates/quilltap-core/src/api/
types.rs:112-119`, a unit variant), `Response::ChatsHasDangerous(Value)`
(`:4184-4185`), the `engine.rs:909-910` arm, `api::salon::chats_has_
dangerous` (`salon.rs:94-118`), the REST arm (`chats_routes.rs:45-60`) and
every server-side test/oracle row that names it; **P4.D223 deletes**
`ChatsHasDangerousRequest` (`apps/web/src/app/core/core-contract.ts:66-74`)
and its union member (`:2560`), the service probe
(`quick-hide.service.ts:126-134`), the `hasDangerousChats` signal, the
three-way `quickHideFeaturesVisible`, the `CHATS_HAS_DANGEROUS_VERB_LANDED`
constant and every spec that pins them. Neither lane replaces the verb with
anything. After the round a stray `{ type: 'chatsHasDangerous' }` over
dispatch answers whatever the dispatcher answers ANY unknown `type` today
(P4.D220 records that shape in its lane record; no new pin). The two halves
land in different commits; the union is gated once.

### §S.2 — The composer's `mentionPriorityCharacterIds` (P4.D224 → P4.D223's file)

`<qt-chat-composer>` gains an input **`mentionPriorityCharacterIds:
readonly string[]`** (default `[]`), bound in `screens/salon/salon-
conversation.ts` from a computed **`castCharacterIds`** = the chat's
participants with `status !== 'removed'` and a `character?.id`, mapped to
that id — v4's predicate at `SalonView.tsx:245-251` verbatim. The input
NAME, its default and the predicate are binding; P4.D224 lands the hunk
(§R.10(i)); P4.D223 preserves it through its own edits of the same file.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D220** — the action-dispatch consolidation (web edges + core dispatch + the `has-dangerous` server half + the chat GET's fourteen keys) | `crates/quilltap-web/src/**` (`query.rs`, every `*_routes.rs`, `lib.rs`); `crates/quilltap-core/src/api/chat_delete.rs`, `api/system_data.rs`, `api/characters.rs:1832-1837` (Tier 2), `api/salon.rs` + `api/types.rs` + `api/engine.rs` (the `ChatsHasDangerous` REMOVALS only); `crates/quilltap-web/tests/**` (every route family + `query_param_semantics_equivalence.rs` + `chats_collection_route.rs` + `chat_delete_route.rs` + a NEW action-sites census); `crates/quilltap-harness/tests/{chat_delete,salon_reads,help_chats_routes,settings_routes,help_docs_routes,…}_equivalence.rs` (the `?action=` pins); `harness/oracle/cases/{query-param-semantics.test,salon-reads.test,chat-delete*}.ts` + every route-family case with an action probe; this order's header | `write_partition.rs`, `services/brahma_console/**`, `db/**`, `photos/**` (P4.D221); `services/help_doc_sync.rs`, `embedding_generate_job.rs`, `crates/quilltap-host/**`, `help/**` (P4.D222); `apps/web/**` (P4.D223/P4.D224); any variant ADDED to `api/types.rs`; the drift ledger |
| **P4.D221** — the `lib/` riders + the `8aafd595d` small + two NO-PORT ratifications + the neutrality re-runs | `crates/quilltap-core/src/write_partition.rs`; `services/brahma_console/prompt_text.rs`; `db/fts_query.rs` (doc); `db/characters.rs`, `db/memories.rs`, `db/prompt_templates.rs` (the six method retirements/pins ONLY); `photos/chat_gallery.rs` (Tier 2); `harness/oracle/cases/{write-partition,fts-query,characters-read,memories-read,memories-tier2}.ts` + `gen-brahma-prompts.mjs`; `crates/quilltap-harness/tests/{write_partition,fts_query,characters_read,memories_read,memories_tier2,brahma_console_tier3}_equivalence.rs`; `carina_parser*` + Tier R — RE-RUN ONLY; this order's header | `crates/quilltap-web/**`, `api/**` (P4.D220); the help/embedding modules, `crates/quilltap-host/**`, `help/**` (P4.D222); `apps/web/**`; every committed fixture pair; the drift ledger |
| **P4.D222** — bugs 167/168: help docs by section + the startup reconcile + the WHOLE `help/` tree at `b0b6656b5` | `crates/quilltap-core/src/services/help_doc_sync.rs`, `services/embedding_generate_job.rs` (the HELP_DOC branch), `embedding_vector.rs` (`average_embeddings`), `db/help_docs.rs`, `db/help_doc_chunks.rs`, `db/help_doc_chunks_repair.rs`; `crates/quilltap-host/src/host.rs` (the boot order), `crates/quilltap-host/tests/host_help_docs_boot.rs`; `help/**` (re-vendored WHOLE); `crates/quilltap-harness/tests/help_tree_embed_guard.rs` (the literal) + every `help_*`/`embedding_generate_jobs`/`embedding_reindex_*` family + NEW `average_embeddings_equivalence.rs` + NEW help-section-size test; `harness/oracle/cases/{embedding-generate-jobs.test,help-sync-ensure.test,help-tree-sync.test,help-sync-tier2,help-sync-guards,help-doc-chunking}.ts` + NEW cases; DELETES `help-docs-upsert-tier2.ts` + `help_docs_upsert_tier2_equivalence.rs` + `harness/oracle/fixtures/build-help-docs-upsert-fixture.ts` + `help-docs-upsert-tier2.json`; `apps/web/src/app/help/help-categories.ts` + `help/__fixtures__/help-guide-tables.json` (RE-CAPTURED) + `apps/web/oracle/help-guide-capture.test.tsx`; this order's header | `crates/quilltap-web/src/**` (P4.D220); `write_partition.rs`, `db/characters.rs`, `db/memories.rs`, `photos/**` (P4.D221); every other `apps/web/**` file (P4.D223/P4.D224); every committed `crates/quilltap-web/tests/fixtures/` pair; `dogfood-findings.md`; the drift ledger |
| **P4.D223** — the Salon Images quick-hide + the bug-169 convergence + the `has-dangerous` client half + `94e946728` ratified | `apps/web/src/app/quick-hide/**`; `shell/user-menu.ts` + `user-menu.spec.ts`; `ui/avatar.ts`; `chat/message-row.ts`, `chat/streaming-message.ts`, `chat/tool-message.ts`, `chat/message-content.ts`, `chat/render/**`; `chat/sidebar/**`; `chat/speaker-selector.ts`, `chat/speaking-as-avatar.ts`, `chat/all-llm-pause-modal.ts`, `chat/reattribute-message-dialog.ts`, `chat/select-llm-profile-dialog.ts`, `chat/cast/**`, `chat/impersonation-voice/**`, `chat/inform-dialog.ts`, `chat/post-office/**`; NEW `chat/hidden-image/**` (the two stand-ins); `screens/salon/salon-conversation.ts` (PRESERVING P4.D224's §S.2 hunk); `core/core-contract.ts` (the `ChatsHasDangerousRequest` removal ONLY); `e2e/{quick-hide-flow,concierge-marks-flow,scenario-builder-flow}.spec.ts` + any new e2e seed; `apps/web/package.json` (version only); this order's header | `apps/web/src/app/editor/**`, `chat/chat-composer.ts`, `chat/carina-parser*`, `chat/mentions/**` (P4.D224); `apps/web/src/app/help/**`, `apps/web/oracle/help-guide-capture.test.tsx`, `help/**` (P4.D222); every crate; `phase-4.md`; the drift ledger |
| **P4.D224** — the `@` mention typeahead | `apps/web/src/app/editor/**` (the adapter, the menu's label setter, `rich-editor.ts`'s plugin list, NEW `editor/mentions/**`); `chat/chat-composer.ts` (the input); NEW `chat/mentions/mention-typeahead.ts` + its oracle spec; `chat/carina-parser.ts` + `carina-parser.oracle.spec.ts`; `apps/web/src/testing/fixtures/{mention-typeahead,carina-parser}.ndjson` + their recorders (outside `src/`); `editor/char-insert/*.spec.ts` + `char-typeahead-harness.ts` (grown); NEW `e2e/composer-mention-flow.spec.ts`; the ONE §S.2 hunk in `screens/salon/salon-conversation.ts` (§R.10(i)); `apps/web/package.json` (version only); this order's header | every other P4.D223 file; `apps/web/src/app/help/**`, `help/**` (P4.D222); `e2e/composer-char-insert-flow.spec.ts`'s existing beats (grow a NEW file); every crate; the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` / `apps/web/package.json` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix, beyond the pre-declared spill in §R.10(i) — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md`, `phase-4.md`, `dogfood-findings.md`, `docs/v4/**` are never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note). Budget 50–70 GB of disk per Rust lane
(`df -h ~` showed 99 GB free at planning; three Rust worktrees is three
`target/` dirs — **run at most TWO Rust lanes' full gates concurrently**
and reclaim `target/debug/incremental` between waves; the two SPA lanes
cost no cargo disk).

1. **§R.2 probe** — PASS (opening, and again before every regen batch).
2. `cargo fmt --all --check` — clean (Rust lanes).
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in BOTH
   feature sets (default; `--features quilltap-core/native-transport`)
   (Rust lanes).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the TARGET pin, run with `--nocapture`, the log grepped for `SKIP:`
   (ZERO), the fresh NDJSON grepped for the changed bytes (> 0 where the
   order predicts a change; a recorded `0` where it predicts none), and
   the red-first steps recorded with the pre-fix counts at the BASELINE
   pin.
5. **The neutrality legs** the order names, regenerated at BOTH pins and
   compared (`cmp` / by-label diff) where the family is deterministic
   across regens; where it is not (`regenerate-at-both-pins-and-cmp-is-
   not-universal`), compare the normalized rows by case and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean (Rust lanes).
8. `cargo test --workspace --no-fail-fast -- --nocapture` with the lane's
   env block (its own families' vars; every other family's var WITHHELD so
   it SKIPs honestly rather than failing on a deleted `/tmp` path): every
   binary's `test result:` line read, zero failures bar the standing reds
   §R.5 names (recorded by name if their vars are in the block), the
   lane's families confirmed RUN by non-zero duration (Rust lanes;
   P4.D222 ALSO runs the SPA gate for its two files).
9. **Source censuses and guards** the change moves or must not move, re-run
   by name with any constant recounted and the arithmetic in the comment:
   `help_tree_embed_guard` + `host_help_docs_boot` (P4.D222 — 127 → 129;
   P4.D220/P4.D221 — UNMOVED, run to prove it); `dispatch_wrong_type_
   census` (P4.D220 — UNMOVED at 449 predicted, measured);
   `web_edge_body_parse_guard` (P4.D220 — its collapse census re-run);
   `blob_write_sites_census` + `compressed_column_write_sites_census`
   (every Rust lane — UNCHANGED); `get_messages_caller_census` (P4.D221 —
   UNMOVED at (76, 7) unless a retired method was a caller; say so);
   `spelling_guard` (everyone — the project is Quilltap).
10. **Tier R** (`crates/quilltap-cli/tests/cli_differential.rs`) at the
    target pin for P4.D221 (266/0 expected — run it, do not assert it);
    P4.D220 runs it too if any core file it touches is linked by the CLI
    (`chat_delete.rs` is — say what you measured).
11. **The SPA gate** (P4.D223, P4.D224, and P4.D222 for its two files):
    `npm run lint` clean; `npm test` (vitest — NEVER `ng test`, which never
    exits; `--filter` matches TEST NAMES) with the count recorded (base at
    planning: 448 files / 7,670); `npm run build` clean (the ONLY real
    typecheck — `spa-tsc-does-not-typecheck-app-sources`); the `qt-*`
    class guard; Playwright by FILE, one invocation at a time on 4319, for
    every spec the lane touched or added (base at planning: full suite
    328 passed / 7 failed [the Salon-streaming timing cluster, each green
    alone] / 6 skipped [the standing parks]) — the full suite is the
    UNIFIER's.
12. **Commit per `.claude/commands/commit.md`** (one unit per commit where
    the order tiers them; CHANGELOG entry + version bumps per §R.8; the
    status-log lane record appended with the regen commands AS RUN, the
    mutation table, the pre-fix counts, the measurements, the mirror
    byte-count pre-list (§R.9), and every finding outside the lane's
    ownership named for the unifier). The lane closes by updating its own
    order's status header with what landed, what was deferred, and what
    it measured that the order got wrong.

