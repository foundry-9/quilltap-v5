# Work order P4.D210 — `quilltap sync`, v4 `23da0b322`'s engine, API action and CLI verb: the nine `lib/mount-index/sync/` modules (the PURE planner at tier 1 over its 576-line corpus; the manifest; the sidecar; the two walkers; the two appliers over T's chokepoints; the per-store mutex + the five refusal codes), `Request::MountSync` with v4's six-key schema and its 409/400/500 mapping, the `sync` CLI verb as v4's thin HTTP client with the byte-captured help, the renderer, the three completion templates, Tier R grown, four help pages + `0ecc6067c`'s mirrors — STACKED on P4.D209's T

**Status: UNIFIED on main at the `f45a517a9` round (2026-09-22) — CLOSED, with ONE undeclared scope reduction now DECLARED.** The engine, `Request::MountSync`, the CLI verb, completion, help and Tier R 223 → 244 all verified against v4; §S.3's response is BARE (the lane's correction, ratified). **The §3 review found and fixed on the unify branch:** `in_flight` had no RAII guard (v4's `try/finally`) — a panic left the store refusing `SYNC_IN_PROGRESS` forever; `birthtime_of` dropped v4's `!birth` NaN conjunct. **OPEN, declared by name:** Tier-1 item 4's LIVE Tier R rows (a pre-hook booting `quilltap-web` on a fixture: dry run / clean run / conflict run, exit 0/0/2, with and without `--json`) were NOT delivered — the 21 new cases are the verb's whole OFFLINE surface; the CLI → HTTP → render → exit glue is proven only by `sync_report_equivalence` + `mount_sync_dispatch_wire`. **Recorded for a ruling:** the whole sync run (both walks, every SHA-256, every disk write + fsync, the manifest) runs INSIDE `db.write(...)`, holding the single writer for its duration — the CAS needs the writer; the walks and the disk applier do not. **Upstream filing candidate + 💸 dogfood row:** at `f45a517a9` a synced decodable image IS transcoded by the applier (the lane measured it) and the manifest records neither side, so a real photograph never converges (`create disk` + `create store` every run) — v4's own module doc says the opposite, and v4's byte-round-trip test passes only because its PNG fixture is undecodable. v5 follows v4 (D23).

**Lane status at close (superseded by the unifier's block above): LANE COMPLETE (2026-09-22). Branch
`claude/p4-d210-quilltap-sync-0f5a99`, cut from P4.D209's TIP (`60dc4df1`)
rather than from T (`c91aa240`) — the deviation is recorded in §0 of the lane
record with its reason. All of Tier 1 landed; Tier 2 item 7 landed as Tier R
rows, item 8 is recorded rather than deferred; Tier 3's two deferrals stand as
written.**

### What landed

| item | state |
|---|---|
| 1. the pure modules + `sync_planner_equivalence` (75) + `sync_manifest_sidecar_equivalence` (74) | ✅ |
| 2. the walkers, appliers and engine + `sync_engine_equivalence` (47 scenarios) | ✅ |
| 3. `Request::MountSync` + the handler + the REST arm + `mount_sync_action_equivalence` (36) + the wire test + the census | ✅ |
| 4. the CLI verb + `sync_report.rs` + `sync_report_equivalence` (176) + the captures + the templates + Tier R 223 → **244** | ✅ |
| 5. `help/cli-sync.md` NEW + three re-vendored + both counts (124 → 125; **the unifier recounts to 126**) | ✅ |
| 6. the `§R.9` mirror pre-list with byte counts | ✅ (in the lane record) |
| 7. the `e11a51f44` rules as Tier R rows | ✅ — the concurrent-run refusal is pinned in the engine's own unit test and the keystone conflict is corpus scenario `vault/a-keystone-removed-from-disk-is-a-conflict` |
| 8. the `QUILLTAP_JOB_CHILD` divergence re-recorded | ✅ — at the applier, as the order asked, cross-referencing P4.D209's record at the hoist. (An earlier draft of this header argued it needed no new record because `reindex_file.rs` already carries the argument; the order asked for it AT THE APPLIER, and writing it is cheaper than the argument.) |

### What the order got wrong, measured

1. **§S.3's "200 `{ data: SyncReport }`" is wrong** — `successResponse(data)` is
   `NextResponse.json(data)`, so the report goes on the wire BARE. The CLI reads
   either shape, which is why the mistake is invisible from the client side.
2. **§R.4(b)'s question is answered: the applier does NOT opt out of image
   normalization**, so at the target pin a synced image IS transcoded. v4's own
   "byte for byte, no WebP transcode" test passes for the OTHER reason the order
   offered — its PNG fixture is not a decodable image, so the normalizer hands
   the bytes straight back.
3. **Two of the ten prescribed mutation proofs cannot redden a corpus row** and
   would have survived: `resolve_in_target`'s escape refusal (no walk can
   produce an escaping path) and the CAS (the corpus is a script, so nothing can
   edit the store mid-run). Both are pinned directly instead, with the reason in
   the source.
4. **`§R.9`'s sync feature doc lives at `features/complete/`, and the two bug
   docs' `fixed/` filenames are `bug-155-byte-write-blanks-caption.md` and
   `bug-156-overwrite-leaves-stale-chunks.md`** (the order names the paths but
   not the filenames; they are in the lane record's table).

### For the unifier

- The branch carries P4.D209's OWN commits (it was cut from that lane's tip),
  so `git diff S...<lane>` over P4.D209's files is non-empty BY D209's commits.
  The pick order is unchanged.
- `crates/quilltap-web/tests/mount_multipart_routes.rs` is edited (three index
  assertions) although it is not in this lane's ownership table by name — v4's
  action map is thirteen keys now and the test pinned twelve by index.
- Both help-count literals read **125** on this branch; recount to 126.
- `dispatch_wrong_type_census.rs`'s `EXCLUDED_BY_THE_ROUTE_IDENTIFIER_RULE`
  reads **442** on this branch; recount as base + the sum of the lanes' verbs.
- Tier R reads **244** on this branch (223 + 21); P4.D211 RE-RUNS it for the
  stamp and should expect that number.

## Preamble

- **v4 oracle baseline: `baa85e19b`** (drift-ledger §1). This lane absorbs
  seams (6)–(12) of the ledger's `23da0b322` row ("feat(scriptorium):
  quilltap sync — a store and a directory, kept in step", 2026-09-21 11:27,
  `4.10.0-dev.53` — `ORDERED(P4.D209 → P4.D210)`; seams (1)–(5) are
  P4.D209's T) and ratifies `0ecc6067c` (docs-only: the sync plan + bugs
  155/156 filed — `ORDERED(P4.D210)`; mirror rules §R.9 — the `fixed/`
  paths, never the birth paths). Spec reads: `git show 23da0b322 -- lib/
  mount-index/sync 'app/api/v1/mount-points/[id]/route.ts' packages/
  quilltap/bin/quilltap.js packages/quilltap/lib/sync-command.js packages/
  quilltap/lib/sync-report.js packages/quilltap/lib/completion help` —
  never the commit message (§R.4(b): the applier DOES call the reindex
  helper; and measure whether it normalizes images).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** Pin **`/tmp/qt-v4-pin-p4d210-
  f45a517a9`** (the TARGET) for every NEW family, Tier R (`cli_
  differential.rs` — 223 today; FOUR go red at the target by design: `main
  help`, `completion bash`/`zsh`/`fish`), `completion_behavior.rs`,
  `mount_points_routes_equivalence`, `help_tree_equivalence`; **`/tmp/qt-
  v4-pin-p4d210-baa85e19b`** (the BASELINE) for the neutrality legs
  (`mount_points_routes` minus the new action — the other actions do not
  move; the `docs` CLI family).
- **Sibling lanes:** P4.D209 (T — you CALL `write_dest_bytes`, `link_blob_
  content`, `link_document_content`, `set_link_timestamps`, `update_
  description(…, link_id)`; never edit its files), P4.D203 (S), P4.D205/
  P4.D207 (`api/types.rs`/`engine.rs` by fence — §R.10(b)), P4.D211 (RE-
  RUNS Tier R at the pin for the stamp — your Tier R growth must be in
  BEFORE the unifier's pick order lands it; no conflict by file, but the
  count moves — record 223 → N in your record), P4.D204, P4.D206, P4.D208.
  The `help/` count: you add `cli-sync.md` (+1 in BOTH literals; P4.D205
  adds `inform.md`; the unifier recounts to 126 — §R.10(d)).
- **Worktree:** `git worktree add <path> -b claude/p4-d210-quilltap-sync-
  <hash> <T>`; `CARGO_INCREMENTAL=0`.
- **Read before the work:** the ledger's `23da0b322` row seams (6)–(12) and
  the `e11a51f44` row (4) (the four user-visible rules v4's README names:
  a second concurrent run refuses; character-vault keystones report a
  conflict rather than being deleted; link-vs-copy group semantics; the
  Docker bind planner); `docs/developer/features/complete/cli-document-
  store-sync.md` at `f45a517a9` (the design of record — read it, port the
  HUNKS); `crates/quilltap-core/src/services/mount_index/` (twenty modules
  — `file_ops.rs`, `link_groups.rs`, `reindex_file.rs`, `scanner.rs`,
  `path_utils.rs`, `base_path_availability.rs`, `general_state.rs` (the
  archived-vault set), `list.rs`), `db/database_store.rs:224/:240`, `api/
  mount_points.rs` (`mount_point_list/get/create/update/delete` at `:104-
  502`), `api/types.rs:1266-1282` (the mount-point variants), `api/engine.
  rs:3141-3164`, `crates/quilltap-web/src/lib.rs:338` (`/api/v1/mount-
  points/{id}`), `crates/quilltap-cli/src/main.rs:37` (`SUBCOMMANDS`, 12
  names, no `sync`), `:236-241` (5 of 12 dispatched, `not_yet_available`
  at `:83`), `src/http.rs` (the server leg's transport — the `docs` verb's
  client shape), `docs_cmd.rs` (the closest verb: resolves a store, talks
  HTTP), `src/help/main_help.txt:9` (`docs` with no `sync` after it),
  `src/help/completion/{bash,zsh,fish}.template` (13,862 / 22,226 / 24,645
  bytes, ZERO `sync`), `completion_cmd.rs:15-17`, `tests/completion_
  behavior.rs:276` (`help_sources_cover_every_dispatched_subcommand` —
  FAILS the moment `sync` is dispatched without a help source; drives real
  `bash` and `zsh -n`), `tests/cli_differential.rs:1774` (51 `ctx.case(` +
  167 `ctx.case_with(` + 5 hand-rolled; `main help` at `:1808`; the three
  completion cases `:3764-3766`); memory notes `cli-help-text-capture-not-
  extraction` (the #119/bug-144 lesson — run the oracle BEFORE writing a
  user-facing string), `tier-r-case-needing-prior-state-uses-a-pre-hook`,
  `quilltap-cli-needs-the-dbkey`, `quilltap-cli-json-output`, `node-readdir-
  is-sorted-rust-read-dir-is-not` (the disk walk's ordering!), `a-new-verb-
  moves-the-dispatch-wrong-type-census`.

## Mandate

Deliver the whole verb: the engine as nine modules mirroring v4's file-
for-file (the planner PURE — no SQLite, no fs, no clock — with its decision
table, the keystone refusal list, the older-`createdAt` rule and the
birthtime guard; the manifest with its atomic write and the mismatch
refusal; the sidecar naming and trailing-newline hygiene; the walkers with
dot-segments invisible on BOTH sides and the reserved-suffix conflict; the
disk applier's temp-then-rename and the `..` escape refusal; the store
applier writing ONLY through T's chokepoints with NO metadata opinion and
the compare-and-swap `StoreRaceError`; the per-store `inFlight` mutex and
the five refusal codes), the `mountSync` verb with v4's schema, log lines
and status mapping, the REST arm, the CLI verb as v4's thin client
(resolve the store name/`qtap://` URI against the mount index READ-ONLY,
POST, render, exit 0/1/2) with `sync_help.txt` CAPTURED from v4 at the pin,
the pure renderer, the three completion templates' `sync` hunks, Tier R
grown and the four designed reds closed, the four help pages and the count.

## Survey-verified starting points (fresh survey 2026-09-21 at `f45a517a9` and on `main` `07f4d7ae` — do not re-derive)

### v4 (at `f45a517a9`)

- **`sync/types.ts`:** `SYNC_MANIFEST_FILENAME = '.quilltap-sync.json'`,
  `SIDECAR_SUFFIX = '.description.md'`, `DISK_TEMP_SUFFIX = '.quilltap-
  tmp'`, `MTIME_TOLERANCE_MS = 1000`, `SIDECAR_FILE_TYPES = ['blob','pdf',
  'docx']`, `hasSidecar(fileType)`; `SyncOptionsSchema` (= the route's
  `syncSchema` without the `.optional()` — §S.3); `SyncEntry { relativePath,
  kind: 'file' | 'folder', sha256?, sizeBytes?, lastModified, createdAt:
  string | null, description?, descriptionUpdatedAt?, linkId?, fileId?,
  linkGroupId?, fileType?, folderId? }`; `SyncActionKind = 'create' |
  'modify' | 'delete' | 'touch' | 'describe' | 'mkdir' | 'rmdir' |
  'conflict' | 'skip'`; `SyncSummary { created, modified, deleted, touched,
  described, conflicts, skipped, failed }`; `SyncReport { storeId,
  storeName, targetPath, dryRun, actions, summary, warnings, elapsedMs }`.
- **`sync/planner.ts` (+602, PURE):** `CHARACTER_VAULT_KEYSTONES =
  ['properties.json','identity.md','description.md','manifesto.md',
  'personality.md','example-dialogues.md','wardrobe/instructions.md']`
  (case-insensitive `isKeystone`); `planSync(input: PlanInput) → {actions,
  warnings}`. The decision table (`:119-309`): kind mismatch → `conflict`
  `` `a ${s.kind} in the store, a ${d.kind} on disk` ``; neither side →
  forgotten; store-only, no base → `materialise('disk', s, olderCreatedAt
  (s.createdAt, null))` (+ a `describe` on disk when `hasSidecar` and a
  non-empty description); store-only, base, content changed → `conflict`
  `'edited in the store and deleted on disk'`; store-only, unchanged,
  `!propagateDeletes` → `skip` `'deleted on disk (--no-delete)'`; store-
  only, unchanged, `isCharacterVault && isKeystone` → `conflict` `'a
  character vault keystone cannot be deleted by a sync'`; else `removal
  ('store', s, 'deleted on disk since last sync')`; the disk-only mirror
  (`'edited on disk and deleted in the store'`, `'deleted in the store
  (--no-delete)'`, `'deleted in the store since last sync'`; a disk-only
  `describe` on store when `!isTextPath`); both folders → nothing; both
  files, bytes differ → `chooseWinner` (`--prefer store|disk` absolute;
  `newer` consults the base, else raw clocks with a tie inside `MTIME_
  TOLERANCE_MS` → `conflict` `'both sides changed; --prefer to resolve'`),
  a `modify` on the losing side with reason `` `--prefer ${prefer}` `` or
  `` `${winner} newer by ${humanGap}` ``, the path following the winner's
  CASING, `linkId`/`expectedStoreSha256` for the CAS, store winners into
  `storeRewrites`; both files, bytes equal → `touch` on the stale side(s)
  per `sameInstant` against `newerTimestamp` and `olderCreatedAt`, disk
  `touch` carrying `createdAt` only when `canSetDiskBirthtime`; `plan
  Description` per both-sides file (store caption out as a sidecar on a
  first run; an edited sidecar back in; both moved → conflict; a deleted
  sidecar = a clearing only when the store stood still and not under
  `--no-delete`; trailing whitespace ignored; a TEXT document's description
  reported as unsynced rather than lost quietly); then `planGroupFanOut`
  (a `modify disk` per hard-link sibling of a store rewrite; two members
  edited differently → conflict); ORDERING: parents before children, store
  before disk, folders before files at a depth, deletes children-first and
  last. `olderCreatedAt(a, b)` — a side that cannot report never wins.
  `--direction` narrows: `to-disk` turns store-side work into `skip`s,
  `to-store` disk-side; conflicts stay visible either way.
- **`sync/manifest.ts`:** `ManifestMismatchError(foundStoreId, expected
  StoreId)` message `` `${SYNC_MANIFEST_FILENAME} in this directory belongs
  to store ${found}, not ${expected}. Sync to a different directory, or pass
  --no-manifest to ignore it.` ``; `readManifest(targetPath, storeId,
  warnings)` (null when absent; unparseable/invalid JSON → null + a
  warning — first-run rules); `writeManifest` atomic (`${final}.tmp` →
  `handle.sync()` → rename; body `JSON.stringify(manifest, null, 2) + '\n'`);
  shape `{version: 1, storeId, storeName, lastSyncAt, entries: Record<path,
  {kind, sha256?, lastModified?, createdAt?, descriptionSha256?,
  descriptionUpdatedAt?}>}`; `baseFromManifest` keys case-insensitively.
- **`sync/sidecar.ts`:** `sidecarPathFor(p) = p + '.description.md'` (the
  WHOLE name — `harbour.png.description.md`); `isSidecarPath` (any casing);
  `partnerPathFor`; `renderSidecar(d)` = `d.replace(/\s+$/, '') + '\n'` or
  `''` (an empty description writes NO file); `parseSidecar` strips
  trailing whitespace; `descriptionSha256` over the PARSED text; `descriptions
  Equal` (absent === empty).
- **`walk-disk.ts`:** dot-entries and subtrees invisible; `*.quilltap-tmp`
  skipped; symlinks skipped WITH a warning; text-native files must round-
  trip UTF-8 or go to `unreadable` + a warning; case collisions on a case-
  sensitive fs drop BOTH with a warning; `birthtimeOf(stat)` null when
  `birthtimeMs` is 0/absent or within 1 ms of `ctimeMs`. **`walk-store.
  ts`:** `hasDotSegment`; dot-segment store paths dropped; a store path
  ending in the sidecar suffix → `reservedPaths` (reported as a `conflict`
  "reserved name: the sidecar suffix cannot also be a document").
- **`apply-disk.ts`:** `DiskPathEscapeError` `` `Refusing to touch ${p}: it
  resolves outside the target directory` ``; `resolveInTarget`; temp-then-
  rename; `birthtimeIsSettable() = process.platform === 'darwin'`; the
  sidecar writer. **`apply-store.ts`:** `StoreRaceError` `` `${p} changed
  in the store while the sync was running (expected sha ${expected?.slice
  (0,12) ?? 'none'}…, found ${found?.slice(0,12) ?? 'none'}…)` ``;
  `writeStoreFile(mountPointId, relativePath, bytes, times)`; the `describe`
  action resolves `action.linkId ? findById : findByMountPointAndPath`,
  throws `` `No link to describe at ${p}` ``, calls the THREE-arg
  `updateDescription(blobId, description, link.id)`; **calls `reindexAfter
  DatabaseWrite` at `:99` and `:130`**; NO metadata opinion (which is WHY
  bug 155 went first); ⚠ whether it passes `normalizeImages: false` —
  MEASURE (the ledger says the only `false` in the tree is the `.qtap`
  import; if the applier does not opt out, a synced `.png` IS normalized at
  the target pin and the engine test "stores a .png as a .png, byte for
  byte — no WebP transcode" must be re-read against the hunks: it may
  assert the STORE-side bytes through a path the normalizer does not touch,
  or it may be a v4 test that the later `186eb09cb` made pass for a
  different reason — record which).
- **`sync/index.ts`:** `SyncRefusedError(message, code)`; codes + messages:
  `SYNC_IN_PROGRESS` `` `A sync of "${name}" is already running` ``; `NOT_
  DATABASE_BACKED` `` `"${name}" is a ${mountType} store — it already IS a
  directory (${basePath || 'no base path recorded'}). Sync mirrors database-
  backed stores only.` ``; `CONVERSION_IN_PROGRESS` `` `"${name}" is
  ${conversionStatus}; wait for that to finish` ``; `SCAN_IN_PROGRESS`
  `` `"${name}" is being scanned; wait for that to finish` ``; `CHARACTER_
  ARCHIVED` `` `"${name}" is an archived character's vault and cannot be
  synced. Rehydrate the character first.` `` (guards: `mountType !==
  'database'`; `conversionStatus !== 'idle'`; `scanStatus === 'scanning'`;
  `storeType === 'character'` and in `getArchivedCharacterVaultMountPoint
  Ids()`); `const inFlight = new Set<string>()` around `runSync`; `useManifest
  false` → `emptyBase()`.
- **The route** — §S.3 carries the schema, the two INFO lines, the mapping.
- **The CLI:** `bin/quilltap.js` help line `  sync <store> <path>
  Mirror a database-backed store to a directory` (after `docs`); `SUBCOMMANDS`
  gains `'sync'` (between `docs` and `memories`); the dispatch arm
  `require('../lib/sync-command').syncCommand(subArgs).catch(err => {
  console.error(`Error: ${err.message}`); process.exit(1); })`. `lib/sync-
  command.js`: flags `-d/--data-dir`, `-i/--instance`, `--passphrase`,
  `-p/--port` (`parseInt || 3000`), `--json`, `--dry-run`, `--direction`,
  `--prefer`, `--no-delete`, `--no-manifest`, `-h/--help`; `resolveStoreSpec`
  (a `qtap://` URI must be `document_store` scope with an EMPTY path — "sync
  takes a whole store, not a path inside one" — and not `self`); `require
  Mount(db, spec)` UUID-first else `LOWER(name) = LOWER(?) ORDER BY name
  COLLATE NOCASE` erroring on 0 or >1; opens the mount-index DB READ-ONLY
  and closes it after resolving; POSTs `` `http://localhost:${port}/api/v1/
  mount-points/${encodeURIComponent(id)}?action=sync` `` with `{targetPath,
  dryRun, direction, prefer, propagateDeletes: !noDelete, useManifest:
  !noManifest}`; exit codes: help 0; no positionals → help + 1; missing
  target or >2 positionals → 1; `mountType !== 'database'` → 1; connection
  refused → 1; `!res.ok` → 1; else `exitCodeFor(report.summary)`. **`print
  SyncHelp` — CAPTURE it from v4 at the pin into `sync_help.txt`, byte for
  byte (it is ~45 lines; the planning survey holds a copy, but the ORACLE
  is the source — never type it).** `lib/sync-report.js`: `ACTION_WIDTH =
  8`, `SIDE_WIDTH = 6`, `MAX_PATH_COLUMN = 44`; `formatActionLine` =
  `` `${tint}${pad(kind, 8)}${reset} ${pad(side, 6)} ${shownPath}` `` +
  `` `  (${detail})` `` (dimmed with colour); `side = action.side || '—'`;
  a folder gets a trailing `/`; `formatSummary` (only the categories that
  happened; "nothing to do"; the conditional under `--dry-run`; conflicts
  pluralised; failures LAST); `exitCodeFor`: `failed > 0` → 1, else
  `conflicts > 0` → 2, else 0; `formatBytes` scales; no escape codes when
  colour is off. **Completion:** bash — `sync` in the top commands, `vf_sync
  = " --direction --prefer "`, `--direction` → `both to-disk to-store`,
  `--prefer` → `newer store disk`, the `sync)` block (flags; positional 1
  from `quilltap docs list --names-only`; positional 2 `compgen -d`); zsh
  — `'sync:Mirror a database-backed store to a directory'`, `_quilltap_
  sync()` with `_arguments -C $sync_opts '1: :->store' '2: :->target'`,
  `store) _quilltap_mount_names`, `target) _directories`; fish — the
  top-level `-a 'sync' -d 'Mirror a database-backed store to a directory'`
  and the `# ---------- sync ----------` block. `completion-coverage.test.js`
  `sync: ['lib/sync-command.js', 'printSyncHelp']`.
- **Help:** `help/cli-sync.md` NEW 9,338 bytes; `cli-docs.md` 25,275;
  `mount-points.md` 28,607; `scriptorium.md` 23,140.
- **The corpus sources:** `sync/planner.test.ts` (576 — eleven describe
  groups, ~60 cases; the highest-value tier-1 corpus in the commit),
  `sync/engine.integration.test.ts` (605 — eight groups incl. the
  refusals, `--direction`, "never touches a chunk row itself — only the
  store's own reindex hook does"), `mount-points/sync-action.test.ts` (212
  — the schema, the store, the refusals-as-status), `sync/manifest.test.ts`
  (127), `sync/sidecar.test.ts` (89), `sync-report.test.js` (158 — pure).

### v5 (on `main` `07f4d7ae`)

- No `mount_index/sync` anywhere; `set_link_timestamps` nowhere (T brings
  it); `SUBCOMMANDS` has no `sync`; the three templates have zero `sync`;
  `not_yet_available` at `main.rs:83`; the `docs` verb's HTTP client in
  `http.rs`/`docs_cmd.rs` is the shape; `api/mount_points.rs` ends at
  `mount_point_delete :502`; the mount-index open for the CLI is `dbopen.
  rs` (P4.D203's — you CALL it; `--data-dir` is the INSTANCE root, and a
  live-instance copy is TORN — the memory note).
- Tier R: 223 cases; `main help` (`:1808`) and the three `completion` cases
  (`:3764-3766`) go RED at the target by design; your new section adds the
  `sync --help` capture, `sync` with no args (help + exit 1), a bad URI, a
  filesystem store (exit 1 with v4's sentence), and — through a pre-hook
  that starts the web server on a fixture — a dry run / a clean run / a
  conflict run (exit 0/0/2) with `--json` and without.

## Tiered deliverables

### Tier 1 — must land

1. **The pure modules** — `sync/types.rs`, `sync/planner.rs` (PURE: takes
   `SyncEntry` maps + the base + options + `can_set_disk_birthtime` + `is_
   character_vault`, returns actions + warnings; v4's ordering by a stable
   sort matching its comparator exactly), `sync/manifest.rs` (serde with
   `version: 1`, key order as v4 writes it — `serde_json` with
   `preserve_order`; the `2`-space pretty print + trailing newline byte-
   pinned), `sync/sidecar.rs`, `sync_report.rs` (the renderer in the CLI
   crate). Families: NEW `sync_planner_equivalence` (tier 1 over v4's REAL
   `planSync` — every `planner.test.ts` case as a corpus row: the inputs
   are plain data, so the tsx case builds the same maps and records the
   actions + warnings; exact on kind/side/path/reason/order); NEW `sync_
   manifest_sidecar_equivalence` (the manifest bytes; the mismatch sentence;
   the degrade-to-null arms; the sidecar naming/render/parse/hash cases);
   NEW `sync_report_equivalence` (tier 1 over v4's real `sync-report.js` —
   colour off AND on; the widths; the em dash; the trailing `/`; the
   summary sentences; the exit codes).
2. **The walkers + appliers + the engine** — `walk_disk.rs` (the memory
   note: Node `readdir` is sorted, Rust `read_dir` is not — sort as v4
   does; the birthtime rule; the UTF-8 round-trip check; the collision
   drop), `walk_store.rs`, `apply_disk.rs` (temp-then-rename with the
   `.quilltap-tmp` suffix; the escape refusal; `birthtime_is_settable` =
   macOS only), `apply_store.rs` (EVERY write through T's chokepoints;
   `reindex_after_database_write` after each; the `describe` resolution +
   the three-arg caption write; the CAS against the planner's sha; the
   `normalize_images` decision from the measurement — §R.4(b)), `sync/
   mod.rs` (`sync_mount_point(mount_point, options) → SyncReport`; the
   `in_flight` set; the five refusals in v4's order; `use_manifest false`
   → an empty base; the manifest written after a non-dry run). Families:
   NEW `sync_engine_equivalence` (a jest real-DB + real-tmpdir family over
   v4's REAL `syncMountPoint` — the `engine.integration.test.ts` groups:
   first run materialises; second run converges; edits both ways; deletions
   with the base; bytes and captions incl. bug 155's survival; what the sync
   will not touch (dotfiles, dot-paths, exclude patterns, chunk rows); the
   refusals; `--direction`) — both sides run the SAME plan against a store
   built through T and a tmpdir, comparing the report (actions/summary/
   warnings, `elapsedMs` normalized), the store's link/blob/chunk rows, the
   disk tree (names, bytes, sidecars, the manifest bytes), red-first on
   nothing (it is new) — its mutation proofs are the instrument.
3. **The verb** (§S.3) — `Request::MountSync` in your fence; `api/mount_
   points.rs::mount_point_sync` with the six-key schema (five plain
   optionals with defaults; decode-raw pins per key: absent → default,
   `null` → Zod 400 with the `parsedType` word), the two INFO lines
   capture-pinned, the mapping (409/400/500 with v4's sentences); the REST
   arm on `/api/v1/mount-points/{id}` POST `?action=sync`; `mount_points_
   routes_equivalence` grown with v4's `sync-action.test.ts` cases (the
   schema's seven; the store's two; the seven refusal statuses) over v4's
   REAL handler with the engine MOCKED as v4's test mocks it (measure: it
   mocks `syncMountPoint` — mirror with a v5 seam); the dispatch WIRE test
   + the census row.
4. **The CLI verb** — `sync_cmd.rs` (the flag parser; `resolve_store_spec`
   with the three URI refusals; `require_mount` name-first/UUID-fallback
   through `dbopen.rs` READ-ONLY on the MOUNT-INDEX partition; the POST via
   `http.rs`; the render; the exit codes), `sync_help.txt` CAPTURED at the
   pin, `main_help.txt`'s one line captured likewise, `SUBCOMMANDS` +
   dispatch; the three templates' `sync` hunks (byte-copied at the pin —
   `completion_behavior.rs` drives real `bash`/`zsh -n`); Tier R: the four
   designed reds re-recorded at the target and CLOSED, the new `sync`
   section (a pre-hook boots `quilltap-web` on a fixture copy for the live
   rows — the `tier-r-case-needing-prior-state-uses-a-pre-hook` shape);
   `--json` bytes compared to v4's (the report through `nodefmt`'s
   conventions — measure whether v4 prints the raw report or a shaped one).
5. **Help + counts:** `help/cli-sync.md` NEW + the three DIFFERING pages
   byte-copied at the target; both count literals +1 (125 on your branch;
   126 at unification — say so in the assert's comment); `help_tree_
   equivalence` at the target.
6. **The mirrors pre-listed** (§R.9): the two bug docs at their `fixed/`
   paths, `cli-document-store-sync.md` at `features/complete/`, `docs/
   developer/{API,CLI,DDL}.md` hunks; byte counts.

### Tier 2 — should land

7. The `e11a51f44` README's four user-visible rules banked as Tier R rows
   where the verb reaches them (the concurrent-run refusal; a keystone
   conflict) — the others are P4.D211's docs concern.
8. The `QUILLTAP_JOB_CHILD` divergence re-recorded at the applier (v5's
   in-process runner — the helper always runs; P4.D209 records the same
   at the hoist; cross-reference).

### Tier 3 — explicit deferrals (loud, typed, never silent)

- A SPA surface for sync — v4 has none; none owed.
- `birthtime` setting on Linux — v4 refuses too (`darwin` only); the
  planner's guard covers it; recorded.

## The differential requirement, restated

The planner, manifest, sidecar and report at tier 1 over v4's REAL modules
(tsx cases building v4's plain-data inputs; the report through `node` over
`sync-report.js`); the engine at tier 2 over a jest real-DB + tmpdir family
(the `mount-ops.test.ts` / `mount-write.test.ts` shapes for a store + disk
family; compare the store rows, the disk tree and the report); the verb
through the route family + the wire test; the CLI through Tier R at the
pin with the help CAPTURED. Mutation proofs (each against the family that
sees it): treat a first run as a base (the "never deletes on a first run"
rows redden); tolerate a 1,001 ms gap (the "does not tolerate a difference
past the tolerance" row reddens); drop the keystone check (the vault-
keystone row reddens); hash the RENDERED sidecar instead of the parsed
text (the whitespace row reddens); write the manifest under `--dry-run`
(the dry-run row reddens); let `resolve_in_target` accept `..` (the
escape row reddens); skip the CAS (a planted mid-run store edit's
`StoreRaceError` row reddens); pass a metadata opinion on the byte write
(bug 155's survival row reddens); return exit 1 on a conflict (the exit-
code row reddens); order files before folders at a depth (the ordering
row reddens).

## Fixtures

Consumes: T (P4.D209's contract — never edited); the target pin's jest-
built stores; a fixture pair for the Tier R pre-hook (an EXISTING committed
pair read-only through a per-case copy — §R.12; never rebuilt). Delivers:
the planner/manifest/sidecar/report corpora; the engine family's fixture
builder (a /tmp store + tmpdir per case); `sync_help.txt`; the templates.
Dependents: yours; Tier R shared with P4.D211's RE-RUN (record 223 → N).

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `f45a517a9` thirteen-commit drift catch-up round — the FTS5/compression
hard stop, Inform, `quilltap sync`, the streamed swipe, bugs 155–160 and the
Zod 4.6 move: **P4.D203 → {P4.D204 ∥ P4.D205 ∥ P4.D207 ∥ P4.D208 ∥ P4.D209 →
P4.D210} ∥ P4.D206 ∥ P4.D211**, ordered 2026-09-21. Nine lanes, nine
worktrees, nine lane branches. **This round STACKS on one keystone:**
P4.D203's FIRST commit — the codec + `qt_text` substrate, called **S** below —
is the commit every DB-touching lane branches from (§R.10), because at the
target pin every oracle-built fixture already carries brotli-compressed cells
and a v5 tree without S cannot read them. P4.D206 (the Salon SPA) and P4.D211
(the dependency move) branch from `main` at once; P4.D210 branches from
P4.D209's first commit (its repo-layer contract, called **T**). The unifier
cherry-picks in the order **P4.D203 → P4.D204 → P4.D209 → P4.D210 → P4.D205 →
P4.D207 → P4.D208 → P4.D206 → P4.D211** (substrate first; the Scriptorium
stack; Inform's server half BEFORE the swipe lane that rewrites the file
Inform leaves a handle in; bug 158 after both; the SPA after every server
contract it consumes; the dependency move last because its regens cover
every recorder), gates ONCE, and `merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`baa85e19b`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`f45a517a9`**
  ("feat(search): index chat messages with FTS5, compress the transcript",
  `4.10.0-dev.61`, 2026-09-21 17:25) — **THIRTEEN commits past the
  baseline**, in this order (ledger §3, every row now `ORDERED(…)`):
  `781e3b499` (docs: the Inform plan — NO-PORT? → P4.D205 with a mirror);
  `e7d77bb60` (Inform — PORT-NEW → P4.D205 ∥ P4.D206); `f564b0de3` (the
  swipe as a watched stream — PORT → P4.D207 ∥ P4.D206); `0ecc6067c` (docs:
  the sync plan + bugs 155/156 filed — NO-PORT? → P4.D210 with mirrors);
  `23da0b322` (`quilltap sync` + bugs 155/156 — PORT-NEW + PORT → P4.D209 →
  P4.D210); `0c14fd61f` (bug 157 — PORT → P4.D209); `5e4252898` and
  `e11a51f44` (docs: the CLI reference move — NO-PORT? → P4.D211 with a
  WIDENED mirror); `6b0615807` (the npm update — PORT, the gate moves →
  P4.D211); `da9c4f34f` (bug 158 — PORT → P4.D208); `186eb09cb` (bugs 159
  + 160, the codec — PORT, BLOCKING → P4.D203 ∥ P4.D209); `80a05a4c8`
  (docs: the FTS plan checked — NO-PORT? → P4.D203 with a mirror);
  `f45a517a9` (FTS5 + the four compressed message columns — PORT-NEW +
  PORT, BLOCKING → P4.D203 → P4.D204). Spec reads: `git show <sha>:<path>`
  for the post-commit files and `git show <sha> -- <path>` for the hunks —
  **never the commit message** (§R.4 names six misdescriptions). v4
  `bugfix` tip `1a2b2164c` and `release` tip `8fbf2afe0` are unmoved; there
  is still no `release: 4.10.0` squash. The checkout sat on `main`, CLEAN,
  at the planning probe (2026-09-21 — the §2 probe PASSED, so the ledger
  stood and nothing was re-derived). **At unification the baseline MOVES to
  `f45a517a9`**; the eight code rows retire `ABSORBED(…)` and the five
  docs-only rows `NO-PORT-RATIFIED(…)` on the ratifying lane's file list.
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log f45a517a9..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Pass = branch `main`, HEAD `f45a517a9`, both logs EMPTY, tree
  CLEAN.** Any mismatch — a moved HEAD (a new commit is a NEW drift row,
  not a pass), a non-empty log, ANY dirty path, the checkout off `main` —
  is a STOP: report it and do nothing else. A lane never writes the
  ledger. (v4 landed ten commits in one day this week; expect the probe to
  fail mid-round, and expect the human to record a waiver in the ledger's
  §1 the way the `89fcc3c0d` round did — a lane resumes ONLY on a recorded
  waiver, never on its own reading of the dirt.)
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1), absolutely. HEAD
  is thirteen commits past the baseline and four of them rewrite paths
  whose families regenerate routinely. Every regen runs from a LANE-UNIQUE
  detached worktree per ledger §5.1 (`git -C ~/source/quilltap-server
  worktree add --detach /tmp/qt-v4-pin-<order>-<sha> <sha>`, the THREE
  symlink classes — root `node_modules`, `packages/quilltap/node_modules`,
  every `plugins/dist/*/node_modules` — then `cd` into it; never stash or
  checkout-switch the human's repo; `git worktree list` before each batch;
  **verify the pin by `git -C "$PIN" rev-parse HEAD` AND by `ls -ld` of the
  path** — build every pin path from a quoted literal, never an unsplit zsh
  variable; then grep the fresh NDJSON for a marker only one tree has;
  check the file is non-empty before believing a diff). **Every lane pins
  the TARGET `f45a517a9`** (`/tmp/qt-v4-pin-<order>-f45a517a9`) for every
  family its port MOVES and **the BASELINE `baa85e19b`** (`/tmp/qt-v4-pin-
  <order>-baa85e19b`) for its neutrality legs; the per-lane family lists
  are in each order's differential section. Pin verification markers: the
  target-pinned `help_tree_equivalence` NDJSON carries `How the Message
  Search Reads Your Words` (`help/search.md` at `f45a517a9`); the
  baseline-pinned one does not (`grep -c` both). **⚠ The target pin's
  fixtures are compressed.** `lib/database/manager.ts:125,134-139`
  registers `conversation_chunks.content` and the four `chat_messages`
  columns as compressed at the REPO layer, and `llm-logs.repository.ts:42`
  registers `request`/`response` — so every jest/tsx oracle that writes a
  cell of 512 bytes or more through v4's real repos stores a `0x51 0x01
  0x01` brotli BLOB, and `harness/oracle/lib/tier2.ts:37-40` hexes it in
  the NDJSON. **Measured at planning: `initializeDatabase()` does NOT run
  the migration runner** (`instrumentation.ts:410-450` does), so an
  oracle-built fixture has NO FTS objects and NO triggers unless the case
  calls `ensureChatMessageFtsSchema` itself — v5 can WRITE into such a
  fixture without the UDF, but cannot READ a compressed cell without
  P4.D203's codec. That is the whole reason for the S stacking (§R.10).
  The sweep driver is the sanctioned per-family path (`harness/tools/
  recipe_sweep.py --run <family> --v4 "$PIN" --v5w <ABSOLUTE worktree>`);
  never run two sweeps concurrently; a family that reports `SKIP` in
  0.00 s did not run. Ledger §5.2's silent-stale-pass discipline applies to
  every regen (`rm -f` the fixture first; read the builder's last line;
  `grep -c` the changed bytes in the fresh NDJSON — MUST be > 0). Every
  lane stages its regen outputs under a LANE-PRIVATE directory
  (`/tmp/<order>/…`) and leaves the committed recipe headers canonical (a
  committed recipe NEVER names a `/tmp` pin — the driver's `--v4` is the
  pin). Every jest oracle writes ZERO `llm_logs` rows (`jest.setup.ts:379`
  mocks the logging service) — the `llm_logs` corpora are dumps of an empty
  table and should not move.
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). Six
  measured misdescriptions this round, one per lane family: (a)
  `186eb09cb`'s body says "reading, searching and exporting are unchanged"
  — true of v4 ONLY; v5's `String` binds reject every compressed cell
  (P4.D203). (b) `23da0b322`'s body says "chunks and vectors are never read
  or written by the sync" — false at module level: `sync/apply-store.ts`
  calls `reindexAfterDatabaseWrite` at `:99` and `:130` (P4.D210); and the
  same body's "no transcoding so a `.png` keeps its sha" predates
  `186eb09cb`, which moved image normalization INTO `linkBlobContent` with
  `normalizeImages` defaulting TRUE and only `import-document-stores.ts:326`
  passing `false` — so at the target pin a synced `.png` IS normalized
  unless the applier opts out; measure which the hunks do (P4.D209/P4.D210).
  (c) `f564b0de3`'s body counts "five status beats"; the service emits FOUR
  `kind: 'status'` events (`gathering`/`sending`/`regenerating`/`saving`)
  and the fifth is the route's own `done` frame (P4.D207). (d)
  `6b0615807`'s "No behavior changes intended": the `zod_version_guard`
  FAILS by measurement and all four SDK-bundling plugins gained a terminal-
  SSE flush arm (P4.D211). (e) `e7d77bb60`'s status header says the dialog
  width "was derived from the toolbar's CSS rather than measured" — that
  caveat is DISCHARGED (the human confirmed 2026-09-19 the CSS was tested);
  its OTHER caveat, that live verification against a real instance has not
  been run, STANDS (P4.D205/P4.D206). (f) `da9c4f34f`'s "Behavior
  unchanged" on the three Concierge sites is true of the INPUT TEXT and
  false in two measurable ways — the reported source moves, and a chat
  with a `scenarioText` and no summary now takes the scenario arm
  (P4.D208). Re-verify even where an order flags the trap — the order's
  paragraph is the same kind of prose, and so is a previous lane's record
  or a doc comment in the tree. Carry a v5-side code comment naming the sha
  where the prose misleads.
- **§R.5 — Designed reds and pre-existing reds: measure, don't assume**
  (ledger §5.4). No convergence pin trips this round (none of the thirteen
  credits this port). The DESIGNED reds, predicted by the ledger's
  2026-09-21 measurements: **P4.D203** — every `String`/`Option<String>`
  bind on the seven registered columns is red-first on a target-pinned
  fixture carrying one cell ≥ 512 bytes (`chats_messages_read`,
  `conversation_chunks_tier2`, the backup families), `collapse_stale_chat_
  caches_tier2` on the `compiledIdentityStacks` disjunct, and the brotli
  parity experiment is a MEASUREMENT whose outcome shapes the harness (a
  decode-normalizer if the bytes differ). **P4.D204** — `chats_search_
  equivalence` and `ui_search_equivalence` red by design at the target (the
  oracle switches to token semantics; the `.`-defect rows FLIP from empty to
  populated) and `like_pattern_reproduces_v4_mangling` fails as a matter of
  intent — it is RETIRED, not repaired. **P4.D205** — `provisioning_
  equivalence` red on the missing `chat_informs` DDL until the D23 re-dump
  lands (prove the family's sensitivity BACKWARDS per the memory note, then
  re-dump); `qtap_schema_embed_guard` red until the two schemas are
  re-vendored (the byte count has TWO homes); the NEW `chat_informs`
  families red-first on unported S. **P4.D207** — `regenerate_swipe_tier3`
  red at the target on the three columns v5 writes NULL (`rawResponse`,
  `reasoningContent`, `thoughtSignature`) — DESIGNED; re-record, never
  "fix" v5 back; `stream_watchdog_wrap_census` red-first on the twelfth
  site. **P4.D208** — `chat_create_capstone_equivalence` red on the
  `contextSummary` column; `initial_greeting_equivalence` moves ONLY if a
  case has a non-empty block (measure first, grow if not); `danger_trigger`
  / `danger_scan_tier2` / `danger_gatekeeper_tier3` are GREEN-BY-LUCK (no
  case carries a `scenarioText`) and must be GROWN red-first. **P4.D209** —
  `attach_mount_file_equivalence` and the `mount_blob_update` route family
  are green before AND after bug 157 until the corpus grows a two-link blob
  (§R.4-class trap: comparand-invisible by construction — grow first);
  `doc_mount_file_links_tier2` red-first on bug 155's five columns and bug
  156's `chunkCount`/`doc_mount_chunks` rows; `doc_mount_blobs_tier2` +
  `doc_blob` move on bug 159's normalized bytes wherever the corpus bytes
  are decodable (`sharp` is NOT mocked; `sha256` propagates into `doc_
  mount_files.sha256` and `files.sha256`). **P4.D210** — Tier R RED on four
  cases at the target by design (`main help`, the three `completion`
  templates) until the verb lands; the NEW planner family red-first on
  nothing (it is new) — its mutation proofs are the instrument. **P4.D211**
  — `zod_version_guard` red until the re-measurement lands; `stream_
  decoders_equivalence` re-recorded at the target and COMPARED, red-first
  where the six `nanogpt-cache-*.wire` transcripts without a trailing blank
  line move (reasoning says neutral; measure). A red that appears in a
  family a lane did NOT touch is a NEW finding to classify at both pins
  before any source is touched, and it is probably not that lane's. The
  **ten standing fixture-vintage reds** (`subprompts_prompt_tier2`, `chat_
  delete`, `character_wizard_tier3`, `subprompts_routes` + the six earlier
  ones on committed pairs) are PRE-EXISTING and stay red this round — no
  lane heals them (§R.12); a lane whose family reads one of those pairs
  records the red by name and moves on.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest`
  is shared) and its silence leg; a fixture change lands with the regen
  of every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that
  SURVIVES as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`). A dispatch WIRE
  test through `POST /api/dispatch` is the only instrument that sees a
  serde collapse; a family that calls the handler directly cannot. A new
  verb lands with its `dispatch_wrong_type_census` rows and the census
  recount in the comment. A NEW pure module (P4.D204's `fts_query`,
  P4.D210's planner/manifest/sidecar, P4.D203's codec) lands with a tier-1
  corpus recorded from v4's REAL module through a tsx case, never a
  transcription of v4's test file.
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed on each commit per `.claude/commands/commit.md`; the unifier
  RECOUNTS as base + the sum of every lane's bumps (the silent-auto-merge
  trap: identical bumps merge as ONE — it fires every round). Base at
  planning: core 0.0.966, harness 0.0.857, web 0.0.157, host 0.0.139, cli
  0.0.22, tauri 0.0.7, fixture-sanitizer 0.0.3, SPA 0.5.741. Expected:
  **core** by P4.D203, P4.D204, P4.D205, P4.D207, P4.D208, P4.D209, P4.D210,
  P4.D211 (the custom-tool-types regen); **harness** by every Rust lane;
  **web** by P4.D204 (the `ui_search` route reads), P4.D205 (the chats
  action arms), P4.D207 (the swipe SSE edge), P4.D210 (the mount-point
  `?action=sync` arm); **host** by P4.D204 (the boot reconciler), P4.D205
  (the `chat_informs` boot ensure), P4.D208 (the seeded-summary heal),
  P4.D209 (`image_codec.rs`); **cli** by P4.D203 (`dbopen.rs`/`nodefmt.rs`)
  and P4.D210 (the `sync` verb); **fixture-sanitizer** by P4.D203; **the
  SPA** (`apps/web/package.json`) by P4.D206 and P4.D211 ONLY; **tauri** by
  NOBODY. The workspace `Cargo.toml` dependency block is P4.D203's alone
  (`rusqlite` gains `functions`; `brotli` becomes a direct dependency);
  every other lane's `Cargo.toml` deltas must be version-only — the unifier
  audits each before any resolution (the `--theirs` clobber trap).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's: each
  lane pre-lists, in its lane record with byte counts at the target pin,
  the mirror paths its rows move. The five docs-only rows carry their
  mirror rules HERE so the unifier ratifies them in one pass: `781e3b499`
  → mirror `docs/developer/features/salon-inform.md` at the POST-`e7d77bb60`
  bytes (the version carrying the status header), never the birth version;
  `0ecc6067c` → mirror `docs/v4/developer/bugs/fixed/bug-155-…md` and
  `…/bug-156-…md` at the `fixed/` paths `23da0b322` RENAMES them to
  (`R083`/`R080`), never the birth paths, plus `docs/developer/features/
  cli-document-store-sync.md` — which `23da0b322` also moves to `features/
  complete/` — at its final path; `5e4252898` → mirror `features/complete/
  cli-comprehensive-help.md` at the POST-`e11a51f44` path, never the birth
  path; `e11a51f44` → re-mirror the GUTTED `docs/developer/CLI.md` (28,099 →
  6,542 bytes) AND add `docs/v4/packages-quilltap-README.md` (v5's mirror
  has no `packages/` tree — a straight re-mirror silently shrinks the
  port's CLI reference by 21.5 KB); `80a05a4c8` → mirror `docs/developer/
  features/complete/chat-message-fts5-and-compression.md` (it lives under
  `complete/`, 37,527 bytes at HEAD — the third revision) and `DDL.md`.
  `docs/CHANGELOG.md`, `README.md`, `CLAUDE.md`, `.claude/commands/
  update-documentation.md`, `DEVELOPMENT.md`, every `package.json`/
  `package-lock.json` and every plugin `manifest.json` stamp → NO-PORT, the
  standing class; Tier R at the pin for the `packages/quilltap` stamp
  (P4.D211 runs it).
- **§R.10 — Stacking, fences and the pre-declared spills.** (a) **The
  keystone S.** P4.D203's first commit contains, and ONLY contains: the
  workspace `Cargo.toml` features, NEW `crates/quilltap-core/src/db/text_
  compression.rs` (the codec + the `qt_text` UDF registration function +
  the `FromSql`/`ToSql` helpers), the registration at the three open points
  (`db/runtime.rs:325 open_readonly`, `db/mod.rs:238 Writer::open_
  writable`, `crates/quilltap-cli/src/dbopen.rs:14`), and the read-site
  binds on the seven columns converted so a compressed cell READS. P4.D203
  records S's hash in its lane record and in the status log the moment it
  exists; **P4.D204, P4.D205, P4.D207, P4.D208 and P4.D209 create their
  worktrees FROM S** (`git worktree add <path> -b claude/<lane>-<hash> <S>`),
  not from `main`. P4.D210 creates its worktree from **T** (P4.D209's first
  commit — the `LinkBlobInput`/`LinkDocumentInput` widening, `set_link_
  timestamps`, `drop_chunks_for_links`, the per-field SET) and may start its
  pure modules (planner/manifest/sidecar/report) from S while T is being
  cut. A lane branched from S never edits S's files; a finding in S is
  reported to P4.D203 through the status log. (b) **`api/types.rs` and
  `api/engine.rs` are FENCED, not owned:** P4.D205 (the three Inform verbs
  + the `ChatInform` response types), P4.D207 (`MessageSwipe`'s `stream`
  flag + `EventPayload::SwipeProgress`) and P4.D210 (`Request::MountSync` +
  its response) each add their variants inside their own `// === P4.D2xx
  ===` … `// === end P4.D2xx ===` fenced block appended at the END of the
  relevant enum/struct, and their dispatch arm inside the same fence
  appended at the END of the engine match; nobody edits another lane's
  fence or any existing variant; the unifier resolves the adjacent-add
  conflicts by fence order (D205, D207, D210). The `dispatch_wrong_type_
  census.rs` count moves once per new verb carrying an `*_id` field —
  each lane recounts in the comment and the unifier recounts as base + the
  sum. (c) **`crates/quilltap-host/src/host.rs::seed_built_ins` is
  APPEND-ONLY by fence:** P4.D204 (the FTS reconciler), P4.D205 (`ensure_
  chat_informs_table`) and P4.D208 (the ledger-guarded seeded-summary
  heal) each append ONE fenced call block at the END of the main-partition
  section, bodies in their own core modules; the unifier orders them
  D205 → D204 → D208 (a table before an index-bearing reconciler before a
  data heal). (d) **The `help/` count moves in TWO lanes:** P4.D205 adds
  `inform.md` (124 → 125) and P4.D210 adds `cli-sync.md` (124 → 125); each
  bumps BOTH literals — `crates/quilltap-harness/tests/help_tree_embed_
  guard.rs:56 VENDORED_FILE_COUNT` and `crates/quilltap-host/tests/host_
  help_docs_boot.rs:90` — by exactly +1 with the arithmetic in the comment,
  and the unifier recounts to **126** (grep the NUMBER, not the words:
  `grep -rn "\b124\b" crates/ --include="*.rs"`). The six DIFFERING help
  pages are byte-copied at the target by their lanes (§R.11's table), the
  count untouched. (e) **Pre-declared loud out-of-mandate edits** (the
  `a-required-field-on-a-shared-struct-crosses-lane-ownership` rule: do the
  edit, keep it to the hunk, mark it `// P4.D2xx OUT-OF-MANDATE — <owner>
  preserves`, add a guard test where the owner must carry it, audit with
  `git diff S -- <path>` at lane close): **P4.D205 → `services/regenerate_
  swipe.rs`** (the swipe-group ids computed and passed as `regeneration_of_
  message_ids` into the context build — v4 `regenerate-swipe.service.ts:
  146-188`; P4.D207 owns the file and PRESERVES the handle when it rewrites
  the generation call; the unifier picks D205 first); **P4.D205 →
  `services/backup/collect.rs`** (ONE self-contained `chat_informs`
  collector block — P4.D203 owns the file for the codec binds); **P4.D208 →
  `services/message_finalizer.rs`** (the memory-trigger gate at `:1564-
  1572` ONLY — P4.D205 owns the file for the consumption hunk at the
  assistant-message save) and **→ `services/backup/restore/orchestrator.rs`**
  (the `strip_scenario_seeded_summary` call at the `chats.create` of section
  7, `:481-484` ONLY — P4.D205 owns the file for the `chat_informs` restore
  step); **P4.D209 → every construction site of `LinkBlobInput`** (the new
  `normalize_images` field spills into `db/doc_mount_blobs.rs:308`, `tools/
  doc_edit/blob.rs:243`, `services/quilltap_import/document_stores.rs:573`,
  `photos/character_gallery_service.rs:330`, `photos/save_image_to_album.rs:
  399`, `photos/user_gallery_service.rs:542`, `services/image_job_storage.
  rs:267`, `services/mount_index/file_ops.rs:438` — one line each, the
  `.qtap` import site the ONLY `false`); **P4.D210 → `services/mount_index/
  file_ops.rs::write_dest_bytes`** is NOT a spill — P4.D209 owns `file_ops.
  rs` whole (bug 156's re-chunk lands there) and P4.D210 only CALLS it.
  (f) **Fences by file** (the unifier asserts `git diff S...<lane> --
  <path>` EMPTY for every non-owner before picking): `db/chats_messages_
  read.rs`, `db/chats_messages.rs`, `db/conversation_chunks.rs`, `db/llm_
  logs.rs`, `db/avatar_rolls_collapse_heal.rs`, `services/collapse_stale_
  chat_caches.rs`, `services/backup/collect.rs` (+ `marshal.rs`), `services/
  conversation_render_reconcile.rs`, `almanack/phase6_wire_records.rs`,
  `tools/run_sql.rs`, `crates/quilltap-cli/src/{dbopen,nodefmt}.rs`,
  `crates/quilltap-fixture-sanitizer/**` — **P4.D203's**; `db/chats_search.
  rs`, `db/like_escape.rs`, NEW `db/chat_message_fts.rs`, NEW `db/fts_
  query.rs`, `api/ui_search.rs`, `crates/quilltap-web/src/ui_search_routes.
  rs` — **P4.D204's**; `services/build_context.rs`, `services/message_
  context.rs`, `services/message_finalizer.rs` (bar the D208 gate hunk),
  `services/primary_stream.rs`, `services/orchestrator.rs`, `services/
  announcer/writer.rs`, `services/courier_transport.rs`, `chat_tasks.rs`,
  `services/turn_transcript.rs`, `db/chats_participants.rs`, NEW `db/chat_
  informs.rs`, NEW `api/chat_informs.rs`, `services/backup/{mod,types}.rs`,
  `services/backup/restore/**` (bar the D208 strip hunk), `services/
  quilltap_import/{execute,reconcile,preview,stream}.rs`, `services/
  provisioning/fresh_schema.json`, `generators/qtap_schema.rs` + the two
  vendored `public/schemas/*.json`, `crates/quilltap-web/src/chats_routes.
  rs` — **P4.D205's**; `services/regenerate_swipe.rs` (bar the D205
  handle), `api/salon.rs`'s `message_swipe_generate`, NEW `crates/quilltap-
  web/src/messages_swipe_routes.rs` (or the arm in `messages_routes.rs` —
  the lane says which), `model/stream_watchdog.rs` (if a budget constant
  is added), `crates/quilltap-harness/tests/stream_watchdog_wrap_census.rs`
  — **P4.D207's**; `services/chat_create.rs`, `services/memory_recap/mod.
  rs` (the visibility widening ONLY), NEW `services/scenario_seeded_
  summary.rs`, NEW `db/scenario_seeded_summary_heal.rs`, `services/
  dangerous_content/gatekeeper_job.rs`, `services/danger_scan.rs`,
  `services/quilltap_import/entities.rs` — **P4.D208's**; `db/doc_mount_
  blobs.rs`, `db/doc_mount_file_links.rs`, `services/mount_index/{file_ops,
  reindex_file,link_groups,store_file,blob_transcode,scanner}.rs`, `services/
  file_storage.rs` (the `normalise_blob_relative_path` copies), `files/
  image_processing.rs`, `crates/quilltap-host/src/image_codec.rs`, `api/
  mount_files.rs`, `api/chat_media.rs` (the `:1589` call ONLY), `services/
  quilltap_import/document_stores.rs` (the `:637` call + the `:560-600`
  tri-state widening ONLY) — **P4.D209's**; NEW `services/mount_index/
  sync/**`, `api/mount_points.rs`, `crates/quilltap-web/src/mount_points_
  routes.rs` (or wherever the `/api/v1/mount-points/{id}` POST arm lives —
  the lane says which), `crates/quilltap-cli/src/{main,sync_cmd,completion_
  cmd}.rs` + `src/help/{main_help,sync_help}.txt` + `src/help/completion/*.
  template`, `crates/quilltap-cli/tests/{cli_differential,completion_
  behavior}.rs` — **P4.D210's**; `pascal/custom_tool_types.rs`, `apps/web/
  src/app/pascal/custom-tool-types.ts` + its corpus, `crates/quilltap-
  harness/tests/zod_version_guard.rs`, `harness/oracle/providers/**`,
  `harness/oracle/fixtures/streams/**` + every recorder-owned corpus file,
  `api/user_profile.rs::is_zod_email` (Tier 2) — **P4.D211's**; **every
  other `apps/web/**` path — P4.D206's alone.** If a lane finds it needs
  another lane's file beyond the pre-declared spills, it STOPs on that item
  and records the shape for the unifier.
- **§R.11 — SPA behaviour moves in ONE lane.** P4.D206 is the only lane
  touching `apps/web/**` bar P4.D211's generated `pascal/custom-tool-types.
  ts` + its spec/corpus. P4.D206 consumes TWO server contracts it cannot
  run against until unification (§S.1 Inform, §S.2 the streamed swipe); it
  builds against the pinned shapes with the SPA's mocked transport, adds
  its live beats gated behind NAMED constants the UNIFIER flips
  (`P4D205_SERVER_LANDED`, `P4D207_SERVER_LANDED` — the `P49K*_SERVER_
  LANDED` precedent; a DEFINED-but-unimplemented verb defeats a capability
  probe), and flips nothing itself. The unified gate runs the full
  Playwright suite once against the release binary with each flipped beat's
  first live run as its own step; the documented intermittents and the
  standing parks stand as named in the previous round's gate (321 passed /
  2 failed / 6 skipped, the two reds re-proven green by file). **The help
  re-vendor table** (byte copies at the target pin; the count moves only
  for the two NEW files, §R.10(d)): `help/data-retention.md` → P4.D203;
  `help/search.md` → P4.D204; `help/inform.md` (NEW) + `help/insert-
  announcement.md` → P4.D205; `help/chat-message-actions.md` → P4.D207;
  `help/dangerous-content.md` → P4.D208; `help/cli-sync.md` (NEW) + `help/
  cli-docs.md` + `help/mount-points.md` + `help/scriptorium.md` → P4.D210.
  After the round, `help/**` is 126 files and `help_tree_equivalence` at the
  target pin is the proof (the embed guard compares embedded-vs-DISK and
  cannot see a stale re-vendor).
- **§R.12 — Committed fixtures: NOBODY REBUILDS A COMMITTED PAIR AT THE
  TARGET PIN.** `crates/quilltap-web/tests/fixtures/` holds 124 entries and
  `harness/oracle/fixtures/*.db` more; any pair rebuilt at `f45a517a9`
  silently re-encodes every `chat_messages`/`conversation_chunks` cell over
  512 bytes AND (if the builder runs migrations) acquires the five FTS
  objects, after which a v5 binary without S cannot write to it and every
  Playwright beat over it dies. The ONLY committed fixture work this round:
  P4.D203 and P4.D204 each add ONE NEW pair built at the target for their
  own read/write proofs (`chat-compressed-{main,mount}.db`, `chat-fts-
  {main,mount}.db` — or one shared pair; the two lanes agree in their lane
  records, D204 reads D203's if it exists first), and P4.D205 widens NO
  pair (its `chat_informs` families build their own `/tmp` fixtures from
  the pin; the committed Salon pairs never carry the table — v4's repo
  creates it lazily and the boot ensure supplies it on open, which is
  exactly what the P4.D205 e2e beat proves). **The ten-family fixture-
  vintage heal is DEFERRED by name to the round after this one** — it must
  run on a tree that carries S and P4.D204 (a widen through v4's migration
  DDL at the new pin brings the FTS triggers with it), so it cannot be this
  round's. `.db-journal` files are never fixtures — delete strays.

## §S — Shared contract (binding; byte-identical in every order of this round)

- **§S.1 — The three Inform verbs (P4.D205 serves, P4.D206 consumes;
  transcribed independently by both from v4 `app/api/v1/chats/[id]/actions/
  inform.ts` + `schemas.ts:249-258` at `f45a517a9`).** Dispatch wire, all
  three internally tagged on `type`, `chatId` a string:
  - **`chatInform`** `{ type: 'chatInform', chatId, contentMarkdown, targetParticipantIds }`
    — v4 `POST ?action=inform`, Zod `informSchema = { contentMarkdown:
    z.string().min(1), targetParticipantIds: z.array(z.uuid()).min(1).
    nullable() }` (REQUIRED keys — a missing key is a Zod 400, `null` on
    the second means Everyone). Rust: `Request::ChatInform { chat_id:
    String, content_markdown: Option<Option<Value>>, target_participant_ids:
    Option<Option<Value>> }` — BOTH keys the `double_option` tri-state so
    absent / `null` / wrong-typed each reach v4's flat `Validation error`
    400 with the right `parsedType` word (the P4.60 convention; the P4.98/
    P4.102 shape). Handler order, v4's: 404 `Chat not found` → eligible seats
    = `type === 'CHARACTER' && controlledBy === 'llm' && status !==
    'removed' && !removedAt` → 400 `No LLM-controlled seat to inform.` when
    none → for an explicit list: `resolveAnnouncementAudience` membership
    first, 400 `` `Unknown inform target(s) for this chat: ${ids.join(', ')}` ``,
    then 400 `` `Not an LLM-controlled seat in this chat: ${ids.join(', ')}` ``
    → `coversEveryone = participantIds.length === eligibleIds.length`;
    `recordTargets = coversEveryone ? null : participantIds` (coverage
    decides public vs whisper, NOT how the operator clicked) → the Host
    record through the announcer writer (`postInformRecord`: `role
    ASSISTANT`, `participantId null`, `systemSender 'host'`, `systemKind
    'inform'`, `content` = `opaqueContent` = the TRIMMED body, `targetParticipantIds`
    = `recordTargets`, `customAnnouncer null`; a failed record write WARNS
    and does not block) → `createBatch` with the TRIMMED body, one row per
    target, `recordMessageId` = the record's id or null → **201** `{ success:
    true, batchId, targetParticipantIds: recordTargets, message }` (`message`
    the persisted record or `null`).
  - **`chatInformsList`** `{ type: 'chatInformsList', chatId }` — v4 `GET
    ?action=informs`: 404 `Chat not found`; `findPendingBatches` with each
    batch's `pendingParticipantIds` filtered to seats still current
    (`!removedAt && status !== 'removed'`), a batch left with none dropped
    → **200** `{ batches: PendingInformBatch[] }` where `PendingInformBatch =
    { batchId, contentMarkdown, createdAt, recordMessageId: string | null,
    pendingParticipantIds: string[] }`.
  - **`chatInformCancel`** `{ type: 'chatInformCancel', chatId, batchId }`
    — v4 `POST ?action=cancel-inform`, `cancelInformSchema = { batchId:
    z.uuid() }` (Rust: `batch_id: Option<Option<Value>>`): 404 `Inform batch
    not found` when the batch has no rows; 400 `That inform belongs to
    another conversation.` when any row's `chatId` differs; `anyConsumed =
    rows.some(consumedAt)`; `removed = deletePendingByBatch` (consumed rows
    and their record SURVIVE so a later swipe can re-apply); the Host record
    is deleted ONLY when `!anyConsumed && recordMessageId` (`recordDeleted`);
    the `chats` realtime topic published explicitly → **200** `{ success:
    true, removed, recordDeleted }`.
  - **Realtime:** pending informs ride the EXISTING `chats` topic — the SPA
    twin adds `chatKeys.informs(id)` (`['chats', id, 'informs']`) to the
    row-scoped `chats` entry of `realtime-topic-map.ts`, no new topic, no
    new poll site. **Storage:** `chat_informs` (v4 `ChatInformSchema`: `id,
    chatId, batchId, participantId, contentMarkdown, recordMessageId?,
    createdAt, updatedAt, consumedAt?, consumedByMessageId?`; `participantId`
    is the CHAT PARTICIPANT id, never a character id). **The record's chip
    label** is `out of character` (`system-message-labels.ts` `KIND_DISPLAY_
    OVERRIDES.inform`) keyed on `systemKind === 'inform'`. **The prompt
    block** is v4's exactly: pending bodies joined by `'\n\n---\n\n'`,
    pushed as ONE `{ role: 'system', content, metadata: { isInjected: true } }`
    between system block 2 (identity reinforcement) and system block 3
    (compressed history), NOTHING pushed when nothing is pending, no
    builder-version constant moved; a swipe reads `findConsumedByMessages`
    over its whole swipe group and returns NO row ids (never consumes).
    **Export/import/backup** carry the rows (NDJSON `kind: 'chat_inform'`
    emitted after every `chat_message` and before the annotations; backup
    `data/chat-informs.json` read OPTIONALLY on restore; `remapChatInform`'s
    drop/keep/clear rules verbatim).
- **§S.2 — The streamed swipe (P4.D207 serves, P4.D206 consumes; from v4
  `lib/services/chat-message/regenerate-swipe.service.ts` + `app/api/v1/
  messages/[id]/route.ts` at `f45a517a9`).** The EXISTING `messageSwipe`
  verb (`Request::MessageSwipe { message_id, swipe_index }`) gains ONE
  optional flag — `stream: boolean` (`#[serde(default)]`, v4 `?stream=1`),
  meaningful only on the GENERATE branch (`swipe_index` absent). With
  `stream: true` the engine emits, on the Event channel, a NEW `EventPayload::
  SwipeProgress(SwipeProgressPayload { frame: Value })` per v4 frame, the
  envelope's `progress_id` = the TARGET message id (the swiped message), and
  the `frame` v4's SSE payload object VERBATIM, key order and bytes untouched
  (the `GeneratorProgress` precedent): `{"status":{"stage","message",
  "characterName","characterId"}}` for the FOUR status beats (`gathering`:
  `` `Regenerating — gathering ${name}'s memories and context...` ``;
  `sending`: `` `Regenerating — sending to ${name}...` ``; `regenerating`
  (ONCE, on the first chunk carrying content): `` `Regenerating ${name}'s
  reply...` ``; `saving`: `Regenerating — filing the new line...`), then
  `{"content": delta}` per content chunk (APPEND), `{"reasoning": cumulative}`
  per reasoning chunk (REPLACE — last wins, never concatenated), then
  `{"done": true, "message": <the persisted swipe row>}`, or on a failure
  after the stream opened `{"error": "Failed to generate alternative response",
  "errorType": "regenerate_failed", "details": <message>}`. The dispatch
  RESPONSE is unchanged either way — the same **201** `{ message }` the
  non-streaming call answers, after the generation completes. The three
  refusals stay ordinary responses BEFORE any frame: 404 `Message not found`;
  400 `Only assistant messages can be swiped`; 400 `Staff and system messages
  cannot be regenerated`. **Wire spelling for the SPA:** `{ type:
  'swipeProgress', progressId, frame }` — `SwipeProgressEvent` beside
  `GeneratorProgressEvent` in `core-contract.ts`, narrowed by its own `type`
  tag. **The persisted swipe row** now carries `tokenCount`/`promptTokens`/
  `completionTokens` from the accumulated `chunk.usage`, `rawResponse` =
  the LAST chunk's `rawResponse` (v4 `if (chunk.rawResponse) rawResponse =
  chunk.rawResponse`), `reasoningContent` = the last cumulative reasoning,
  `thoughtSignature` = the last chunk's — retiring v5's tracked deferral
  (`regenerate_swipe.rs:20-25`). **The REST edge** `POST /api/v1/messages/
  {id}?action=swipe&stream=1` answering `text/event-stream` of `data:
  <frame JSON>\n\n` lines (re-framed from the same events, the `generator_
  sse.rs` precedent) is P4.D207's Tier 2 — the SPA does NOT use it (it
  dispatches `messageSwipe { stream: true }` and listens for
  `swipeProgress` events with its `progressId`). **The SPA's four found-
  on-the-way rules:** the row being regenerated shows the `Regenerating...`
  plate (`qt-chat-regenerating-plate`, `role="status"`, `aria-live="polite"`)
  ONLY while no prose has arrived and withdraws the moment there is content
  (no blank gap); the original content dims under `qt-chat-regenerating-
  original`; the message action bar disables (`qt-chat-message-action-bar-
  disabled`); the composer's `disabled` gating becomes `composerLocked =
  disabled || sending` on EVERY control (v4 bug (c)); after `done` the view
  selects the NEW variant (`selectSwipeVariant(newSwipeId)` — v4 bug (b),
  the one v5's `transcript-reconcile.ts` most likely reproduces — measure);
  the response-status strip shows the quill for `regenerating` and `saving`
  (`qt-chat-response-status[data-stage="regenerating"|"saving"]`); one
  regeneration in flight at a time.
- **§S.3 — `Request::MountSync` (P4.D210 alone serves; the CLI is its own
  client; no SPA consumer this round).** `{ type: 'mountSync', mountPointId,
  targetPath, dryRun?, direction?, prefer?, propagateDeletes?, useManifest? }`
  — v4 `POST /api/v1/mount-points/{id}?action=sync`, `syncSchema = {
  targetPath: z.string().min(1), dryRun: z.boolean().optional().default(false),
  direction: z.enum(['both','to-disk','to-store']).optional().default('both'),
  prefer: z.enum(['newer','store','disk']).optional().default('newer'),
  propagateDeletes: z.boolean().optional().default(true), useManifest:
  z.boolean().optional().default(true) }` — the five optionals are PLAIN
  `.optional().default()` (absent and `undefined` both take the default;
  an explicit `null` is a Zod 400), so a decode-raw pin per key is the cheap
  guard; `targetPath` required. Responses: 404 `Mount point not found`; 400
  the per-issue `` `${path} ${message}` `` list; 409 for `SyncRefusedError`
  codes `SYNC_IN_PROGRESS` / `CONVERSION_IN_PROGRESS` / `SCAN_IN_PROGRESS`
  and for `ManifestMismatchError`; 400 for every other `SyncRefusedError`
  (`NOT_DATABASE_BACKED`, `CHARACTER_ARCHIVED`) and for `ENOENT` with v4's
  sentence (`` `${message}. The path is resolved on the server: under Docker
  it must sit inside a bind mount (see 'quilltap docs docker-mounts').` ``);
  500 `` `Failed to sync mount point: ${message}` ``; **200** `{ data:
  SyncReport }` with `SyncReport = { storeId, storeName, targetPath, dryRun,
  actions: SyncAction[], summary: { created, modified, deleted, touched,
  described, conflicts, skipped, failed }, warnings: string[], elapsedMs }`.
  The two INFO lines (`[Mount Points v1] Sync requested` with `mountPointId,
  name, targetPath, dryRun, direction, prefer, userId`; `[Mount Points v1]
  Sync complete` with `mountPointId, targetPath, dryRun, ...summary,
  elapsedMs`) are capture-pinned.
- **§S.4 — A lane that finds it needs a wire change beyond §S.1–§S.3 STOPs
  on that item** and records the shape for the unifier rather than
  inventing a verb.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D203** — the codec + `qt_text` substrate (S) + every read/write site + bug 160 | Workspace `Cargo.toml` (the `rusqlite` feature list + `brotli`); NEW `crates/quilltap-core/src/db/text_compression.rs` + its `mod` line; `db/runtime.rs` (`open_readonly` ONLY); `db/mod.rs` (`Writer::open_writable` ONLY); `db/chats_messages_read.rs`; `db/chats_messages.rs` (the five write statements); `db/conversation_chunks.rs`; `db/llm_logs.rs` (the two reads + the usage aggregate); `db/avatar_rolls_collapse_heal.rs` (the read + write pair); `services/collapse_stale_chat_caches.rs`; `services/backup/collect.rs` + `marshal.rs` (the codec binds; the D205 collector block is a pre-declared spill); `services/conversation_render_reconcile.rs`; `almanack/phase6_wire_records.rs`; `tools/run_sql.rs` (the BLOB rendering) + `services/brahma_console/prompt_text.rs` (the one sentence); `crates/quilltap-cli/src/dbopen.rs` + `nodefmt.rs`; `crates/quilltap-fixture-sanitizer/**`; `help/data-retention.md`; the NEW committed pair `chat-compressed-{main,mount}.db` + builder; NEW `crates/quilltap-harness/tests/text_compression_equivalence.rs` + `harness/oracle/cases/text-compression.ts` + fixture; the harness tier-2 decode-normalizer (if the parity experiment says so); every family named in its differential section; this order's header | `db/chats_search.rs` (P4.D204), `api/**` (no verb moves), `crates/quilltap-host/**` (no boot hook — the two compression migrations are DEFERRED reclamation), `services/mount_index/**`, `db/doc_mount_*` (P4.D209), `apps/web/**`, every other `help/*.md`, every other lane's fence, the drift ledger |
| **P4.D204** — FTS5 message search, stacked on S | NEW `crates/quilltap-core/src/db/chat_message_fts.rs` + NEW `db/fts_query.rs` + their `mod` lines; `db/chats_search.rs` (the rewrite; `like_pattern`/`regex_source_escaped` DELETED); `db/like_escape.rs` (if `escapeLikePattern` folds onto it — say so); `api/ui_search.rs` (`create_snippet` + the fold); `crates/quilltap-web/src/ui_search_routes.rs` (only if the edge moves — say so); `crates/quilltap-host/src/host.rs` — ONE fenced call block in `seed_built_ins` (the reconciler); `help/search.md`; the NEW committed pair `chat-fts-{main,mount}.db` + builder (or D203's shared pair — say which); NEW `crates/quilltap-harness/tests/fts_query_equivalence.rs` + `chat_message_fts_equivalence.rs` + their oracle cases/fixtures; `chats_search_equivalence.rs` + `harness/oracle/cases/chats-search.ts`; `ui_search_equivalence.rs` + `ui-search.test.ts`; this order's header | S's files (report, never edit), `db/chats_messages*.rs`, `api/types.rs`/`engine.rs` (no verb moves — a GET with query params), `services/**`, `apps/web/**`, every other `help/*.md`, the drift ledger |
| **P4.D205** — Inform, the server half + D23 + backup/import + help, stacked on S | NEW `crates/quilltap-core/src/db/chat_informs.rs` + `mod` line; `services/provisioning/fresh_schema.json` (the D23 re-dump — never by hand); NEW `api/chat_informs.rs` + `mod` line; `api/types.rs` + `api/engine.rs` INSIDE its `// === P4.D205 ===` fence ONLY; `services/build_context.rs`; `services/message_context.rs` (`is_record_only_message`); `services/message_finalizer.rs` (bar the D208 gate hunk); `services/primary_stream.rs`; `services/orchestrator.rs` (the `informRowIds` plumbing); `services/announcer/writer.rs`; `services/courier_transport.rs`; `chat_tasks.rs` (`extract_visible_conversation`); `services/turn_transcript.rs` (test only); `db/chats_participants.rs` (`remove_participant`); the chat-delete cascade site (say which file — `chat_informs` has NO FK on the generateDDL surface, measure); `services/backup/{mod,types}.rs`, `services/backup/restore/**` (bar the D208 strip hunk), `services/backup/collect.rs` — the ONE collector block as a pre-declared spill; `services/quilltap_import/{execute,reconcile,preview,stream}.rs` + the export marshal; `generators/qtap_schema.rs` + `public/schemas/qtap-export{,-ndjson}.schema.json` + `qtap_schema_embed_guard.rs::VENDORED_BYTES` (BOTH byte homes); `crates/quilltap-web/src/chats_routes.rs` (the three action arms); `crates/quilltap-host/src/host.rs` — ONE fenced call block (`ensure_chat_informs_table`); `services/regenerate_swipe.rs` — the ONE pre-declared handle hunk; `help/inform.md` (NEW) + `help/insert-announcement.md` + BOTH count literals (+1); `crates/quilltap-web/tests/dispatch_wrong_type_census.rs` (its three rows + recount); NEW harness families + the `context-management`/`inform-block`/`chat-informs-*`/`inform.test.ts` oracle cases; the families its differential section names; this order's header | S's files, every other `api/*.rs` (bar the fence), `services/chat_create.rs` (P4.D208), `services/quilltap_import/{entities,document_stores}.rs`, `db/doc_mount_*`, `crates/quilltap-cli/**`, `apps/web/**`, the drift ledger |
| **P4.D206** — the Salon SPA: Inform's client half + the streamed swipe's client half | `apps/web/**` EXCEPT `apps/web/src/app/pascal/custom-tool-types.ts` + its spec/corpus (P4.D211): `core/core-contract.ts` (the §S.1 request/response types, `MessageSwipeRequest.stream`, `SwipeProgressEvent`), `core/realtime-topic-map.ts` (+ spec), `chat/chat-keys.ts` (`informs`), `chat/chat-composer.ts` (the gutter *i* + the `composerLocked` gating + `PendingInformChips` host), NEW `chat/inform-dialog.ts` + NEW `chat/pending-inform-chips.ts` (+ specs), `chat/system-message-labels.ts`, `chat/message-row.ts`, `chat/message-list.ts`, `chat/transcript-reconcile.ts` (`selectSwipeVariant`), `screens/salon/salon-conversation.ts` (both wirings), `styles/qt-components/_chat.css` (the six classes + the `data-stage` pair), the e2e beats (gated), `apps/web/package.json`; this order's header | Everything outside `apps/web/**`; `apps/web/src/app/pascal/custom-tool-types.ts`; the drift ledger |
| **P4.D207** — the streamed swipe, the server half, stacked on S | `crates/quilltap-core/src/services/regenerate_swipe.rs` (the generation rewritten as a watched stream; the D205 handle PRESERVED); `api/salon.rs` — `message_swipe_generate` ONLY; `api/types.rs` + `api/engine.rs` INSIDE its `// === P4.D207 ===` fence ONLY (`stream`, `SwipeProgress`); `model/stream_watchdog.rs` ONLY if a named budget is added (say so); NEW `crates/quilltap-web/src/messages_swipe_routes.rs` (or the arm in `messages_routes.rs` — say which) + `lib.rs`'s route line; `crates/quilltap-harness/tests/stream_watchdog_wrap_census.rs` (the twelfth row); `regenerate_swipe_tier3_equivalence.rs` + `harness/oracle/cases/regenerate-swipe-tier3.test.ts`; `salon_swipe_generate_equivalence.rs` + its case; `help/chat-message-actions.md`; `crates/quilltap-web/tests/dispatch_wrong_type_census.rs` (its rows + recount); this order's header | S's files, every other `services/*.rs` (Inform's consumption lives in D205's files), every other `api/*.rs`, `apps/web/**`, the drift ledger |
| **P4.D208** — bug 158, stacked on S | `crates/quilltap-core/src/services/chat_create.rs` (the seed line + `build_recent_conversations_block`); `services/memory_recap/mod.rs` (`truncate_gist` → `pub(crate)` ONLY); NEW `services/scenario_seeded_summary.rs` + `mod` line; NEW `db/scenario_seeded_summary_heal.rs` + `mod` line; `services/dangerous_content/gatekeeper_job.rs`; `services/danger_scan.rs`; `services/message_finalizer.rs` — the gate hunk `:1564-1572` ONLY (pre-declared spill); `services/quilltap_import/entities.rs`; `services/backup/restore/orchestrator.rs` — the strip hunk ONLY (pre-declared spill); `crates/quilltap-host/src/host.rs` — ONE fenced call block (the heal); `help/dangerous-content.md`; the families its differential section names + their cases/fixtures (the danger trio GROWN, the capstone + greeting re-recorded, the ingest bundles planted); this order's header | S's files, `services/build_context.rs`/`message_context.rs`/`primary_stream.rs` (P4.D205), `api/**`, `crates/quilltap-web/**`, `apps/web/**`, the drift ledger |
| **P4.D209** — the Scriptorium repo layer: bugs 155/156/157 + bug 159's image half + the timestamps/reindex seams (T), stacked on S | `crates/quilltap-core/src/db/doc_mount_blobs.rs` (`update_description`/`update_extracted_text` — `link_id` REQUIRED, the `LIMIT 1` fallback DELETED; `update_path`'s own `LIMIT 1` LEFT ALONE); `db/doc_mount_file_links.rs` (bug 155's per-field SET, bug 156's `content_changed` + `drop_chunks_for_links` + `chunk_count = 0` + the invalidation, `fan_out_group_file_id`'s `moved` filter + `last_modified` positional, the `last_modified`/`created_at` inputs, NEW `set_link_timestamps`, the `normalize_images` flag + the normalization call BEFORE the sha); `LinkBlobInput`'s eight construction sites (one line each, pre-declared); `services/mount_index/{file_ops,reindex_file,link_groups,store_file,blob_transcode,scanner}.rs` (bug 156's `write_dest_bytes` re-chunk; the post-write helper hoisted as ONE never-throwing block; `is_lossless_webp` + `LOSSLESS_WEBP_REENCODE_MIN_BYTES`); `services/file_storage.rs` (the five `normalise_blob_relative_path` copies → ONE); `files/image_processing.rs` + `crates/quilltap-host/src/image_codec.rs` (the fifth seam); `api/mount_files.rs` (`mount_blob_update`'s `None` → `Some(&meta.link_id)` + the stale comment); `api/chat_media.rs` (`ensure_image_description`'s `:1589` write ONLY); `services/quilltap_import/document_stores.rs` (`:637` + the `:560-600` tri-state widening ONLY; `normalize_images: false` — the ONLY `false`); the families its differential section names + the two-link-blob corpus growth; this order's header | S's files, `services/mount_index/sync/**` (P4.D210), `api/mount_points.rs`, `api/types.rs`/`engine.rs`, `crates/quilltap-cli/**`, `apps/web/**`, the drift ledger |
| **P4.D210** — `quilltap sync`: the engine, the API action, the CLI verb, completion, help; stacked on T | NEW `crates/quilltap-core/src/services/mount_index/sync/{mod,types,planner,manifest,sidecar,walk_disk,walk_store,apply_disk,apply_store}.rs`; `api/mount_points.rs` (`mount_point_sync`); `api/types.rs` + `api/engine.rs` INSIDE its `// === P4.D210 ===` fence ONLY; the `/api/v1/mount-points/{id}` POST `?action=sync` arm in `crates/quilltap-web/src/` (say which file); `crates/quilltap-cli/src/main.rs` (`SUBCOMMANDS` + the dispatch arm), NEW `sync_cmd.rs` + NEW `sync_report.rs`, `src/help/main_help.txt` (the one line) + NEW `src/help/sync_help.txt` (CAPTURED from v4 at the pin, never extracted), `src/help/completion/{bash,zsh,fish}.template`, `completion_cmd.rs` if the coverage map moves; `crates/quilltap-cli/tests/cli_differential.rs` (the four red cases re-recorded + the new `sync` section) + `completion_behavior.rs`; NEW `crates/quilltap-harness/tests/sync_planner_equivalence.rs` + `sync_manifest_sidecar_equivalence.rs` + `sync_report_equivalence.rs` + `mount_sync_action_equivalence.rs` + their cases/fixtures; `help/cli-sync.md` (NEW) + `help/cli-docs.md` + `help/mount-points.md` + `help/scriptorium.md` + BOTH count literals (+1); `crates/quilltap-web/tests/dispatch_wrong_type_census.rs` (its row + recount); this order's header | T's files (P4.D209 — CALL `write_dest_bytes`/`link_blob_content`/`set_link_timestamps`, never edit them), S's files, `crates/quilltap-cli/src/{dbopen,nodefmt,db_cmd}.rs`, `apps/web/**`, the drift ledger |
| **P4.D211** — the `6b0615807` dependency move: Zod 4.6.5, the recorders, Tier R, the CLI-reference mirrors | `crates/quilltap-harness/tests/zod_version_guard.rs`; `crates/quilltap-core/src/pascal/custom_tool_types.rs` + `apps/web/src/app/pascal/custom-tool-types.ts` (+ spec + corpus) — the two hand-rolled engines regenerated; the ~150 Zod-sentence/regex edge sites the re-measurement names (each listed in the lane record; `api/zod_issues.rs` is ONE home now — P4.101); the ~40 `4.5.4` prose claims in `crates/` doc comments; `harness/oracle/providers/*.mjs` (the nine recorders RE-RUN, edited only if a recorder breaks at the new SDK) + every corpus they own (`harness/oracle/fixtures/streams/**`, request-envelopes, response-bodies, tool-wire, image-fixtures, google-request, moderation-wire, web-search-wire, the provider manifests); `stream_decoders_equivalence.rs` and the eight sibling families RE-RUN; `api/user_profile.rs::is_zod_email` (Tier 2); this order's header | Every `db/**`, `services/**`, `api/**` production file bar `user_profile.rs`'s one function, `crates/quilltap-cli/src/**` (Tier R is RE-RUN, never edited), every other `apps/web/**` path (P4.D206), the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix, beyond the pre-declared spills in §R.10(e) — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note). Budget 50–70 GB of disk per long lane
(`df -h ~` before launch; nine worktrees is nine `target/` dirs).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean (Rust lanes); `npm run lint` clean
   incl. the qt-class guard (P4.D206, P4.D211's SPA file).
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`)
   (Rust lanes).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0
   where the order predicts a change; a recorded `0` where it predicts
   none), and the red-first steps recorded with the pre-fix counts.
   P4.D206: `npm test` whole plus its new/changed spec files by name.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp` / by-label diff) where the family is
   deterministic across regens; where it is not (`regenerate-at-both-
   pins-and-cmp-is-not-universal`), compare the normalized rows by case
   and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean (Rust lanes; the Playwright
   venue prefers `target/release`); `npm run build` clean (P4.D206,
   P4.D211).
8. `cargo test --workspace --no-fail-fast -- --nocapture` with the lane's
   env block (its own families' vars; every other family's var WITHHELD
   so it SKIPs honestly rather than failing on a deleted `/tmp` path):
   every binary's `test result:` line read, zero failures bar the ten
   standing fixture-vintage reds named in §R.5 (recorded by name if their
   vars are in the block), the lane's families confirmed RUN by non-zero
   duration (Rust lanes).
9. **Source censuses** that the change moves (`help_tree_embed_guard` +
   `host_help_docs_boot` for D205/D210; `dispatch_wrong_type_census` for
   D205/D207/D210; `stream_watchdog_wrap_census` for D207; `qtap_schema_
   embed_guard` + `the_embedded_schema_compiles` for D205; `zod_version_
   guard` for D211; `public_schemas_vendor_guard` for nobody — say so if it
   moves) re-run by name and their constants recounted with the arithmetic
   in the comment.
10. **Tier R** (`crates/quilltap-cli/tests/cli_differential.rs`) at the
    TARGET pin for P4.D203 (the raw-SQL path now decodes), P4.D210 (the
    verb; 223 → 223 + N, the four designed reds closed) and P4.D211 (the
    stamp; 223/0 expected — run it, do not assert it).
11. **Commit per `.claude/commands/commit.md`** (one unit per commit where
    the order tiers them; CHANGELOG entry + version bumps per §R.8; the
    status-log lane record appended with the regen commands AS RUN, the
    mutation table, the pre-fix counts, the measurements, S's/T's hash
    where the lane cut one, and every finding outside the lane's ownership
    named for the unifier). The lane closes by updating its own order's
    status header with what landed, what was deferred, and what it
    measured that the order got wrong.
