# Work order P4.D55 — the `f4955e0e` provider drift: bug 31 (OpenRouter non-streaming vision), bug 32 (the attachment-capability map), bug 33 (Grok text/PDF arms live), bug 34 (base64 decode round-trip), bug 35 (the Ollama stream splitter)

> **STATUS: OPEN — not started.**

Part of the `f4955e0e` drift catch-up round (v4's "bugs 8–43" batch).
This lane absorbs v4 `43a1b5b1` (session 8) — five provider-plugin
fixes, all **Faithful** provenance (v5 reproduces each defect today),
two of which discharge v4-side findings this port recorded (the
OpenRouter vision refusal, the Grok dead arms). The corpora move; the
`EXPECTED_REFUSALS` table shrinks; one v5 unit test that deliberately
pinned a v4 bug INVERTS.

**v4 baseline for THIS round: `f4955e0e`** (4.8.0-dev.175,
2026-08-06, tree clean at planning). Classification: "Round planned —
the f4955e0e found-bugs convergence round" in `status-log.md`. **The
baseline move is the UNIFIER's** — do NOT edit `CLAUDE.md` or
`phase-4.md`.

**Drift-check at lane start:** `git log f4955e0e..HEAD --oneline` +
`git status --short` in `~/source/quilltap-server`. If HEAD moved:
classify; STOP only if a new commit lands on files this order names;
otherwise pin a worktree at `f4955e0e`
(`oracle-regen-pinned-v4-worktree` — the pin needs
`plugins/node_modules` + per-plugin `plugins/dist/*/node_modules`
symlinks for the recorders, and the installed SDK majors must match
package.json: openai 7, `@openrouter/sdk` 1.2).

**Siblings (same round):** P4.D51, P4.D52, P4.D53, P4.D54, P4.43.
None touches provider files; you touch nothing of theirs.

**Worktree note:** own branch, own checkout; no `git stash`/
`git worktree` games with sibling lanes.

## Mandate

Mirror all five provider fixes byte-for-byte on the wire: the new
OpenRouter direct chat-completions vision path (a THIRD OpenRouter
body shape, replacing v5's `ProviderRefused` arm) with its raw-wire
response parse; the OPENROUTER attachment-capability flip and its
DB-visible default consequence; the widened Grok gate with the
text/PDF arms now reachable; the decode–re-encode–compare base64
wrapper at both call sites; and the cross-push buffered Ollama NDJSON
decoder. Regenerate all affected corpora at the pin, retire the two
refusal entries, and add the new-coverage vectors the fixes make
reachable — a corpus that never exercises a new arm ships it
unverified. Do NOT touch: any sibling lane's files, `apps/web/**`,
`api/types.rs`. (`api/settings.rs` is P4.D53-adjacent but its
`connection_profile_create` default flows from YOUR map with no edit
— do not open it.)

## Survey-verified starting points (2026-08-06, fresh at `f4955e0e`)

### Bug 31 — OpenRouter non-streaming vision (the third body shape)
- v4 `qtap-plugin-openrouter/provider.ts`: early exit at `:151-152` —
  after `collectAttachmentResults`, `hasImageAttachments(params)` →
  `sendViaChatCompletions(params, apiKey, attachmentResults)`.
  `hasImageAttachments` (`:134-138`) = any attachment with a
  supported image mime AND `(a.data || a.url)` — a **no-data image
  does NOT flip the path** (the `attachment-no-data` vector stays on
  the SDK).
- `sendViaChatCompletions` (`:312-470`): POST
  `https://openrouter.ai/api/v1/chat/completions`, `stream:false`,
  JS assignment order = wire order:
  `model, messages, stream, temperature, max_tokens, top_p, stop` →
  `user` (cacheKey, non-empty) → `tools` (RAW passthrough, input key
  order) + `tool_choice:"auto"` → `plugins:[{id:"web",max_results:5}]`
  → `response_format` (`json_schema` → `{type, json_schema:{name,
  strict,schema}}`, `strict ?? true`) → `models` + `route:"fallback"`
  **and `delete body.model`** → `provider` (v4 assignment order:
  `order, allow_fallbacks, require_parameters, data_collection,
  ignore, only` — NOT the SDK's alphabetical) → `reasoning`
  (`{exclude:false}` / `{effort, exclude:false}`). Message shape =
  the STREAM path's (`{role, content}` parts, tool_calls kept,
  tool-role messages carried), NOT the SDK send path's. Abort:
  `buildRequestAbortSignal(params)` (whole-request timeout).
  Predicted vector bodies are in the survey record ("Round planned"
  in `status-log.md`) — verify against the recorder, never trust the
  prediction.
- **Response parse changes on this path** (`:453-470`): raw
  snake_case body read directly — `usage.prompt_tokens_details.
  cached_tokens` DOES produce `cacheUsage` (with cache-read
  subtracted from prompt/total), unlike the SDK path whose
  `usage.cachedTokens` quirk the corpus pins. Same wire body, two
  different `LLMResponse`s depending on whether an image rode along.
- v5 sites: `model/request_builder/chat_completions.rs` —
  `build_openrouter_send_body` (`:760-843`; DELETE the
  `openrouter_has_formattable_images` refusal at `:783`; keep the
  tool-role refusal at `:771` for the image-FREE path),
  `openrouter_send_provider` (`:676-712`, alphabetical — add the
  assignment-order variant for the new arm),
  `openrouter_send_message` (`:657-671` — NOT for the new arm);
  `request_builder/mod.rs:149/:157/:215`
  (`BuildError::ProviderRefused` dispatch);
  `model/response_parse.rs:87-160/:288-300` — add a raw-wire
  OpenRouter flavor beside `openrouter_sdk_normalize`.
- Corpus: `request-envelopes.recorded.ndjson` lines 119/120
  (`openrouter/image-attachment[send]`, `image-attachment-tools
  [send]`) flip refused → body; line 112 (`tool-roundtrip[send]`)
  stays refused. `EXPECTED_REFUSALS`
  (`request_builder_equivalence.rs:183-194`) loses 2 of 3 entries;
  the exact-count assertion (`:322-327`) moves with it — edit both
  IN the regen commit (the recorder keeps only the first line of a
  thrown message, so the old refusal lines vanish entirely).
- **New coverage owed** (every existing cacheKey/tools/websearch/
  response-format/fallback/provider-prefs/reasoning vector is
  image-free, so the new arm is otherwise unreachable): image ×
  each of cache-key, web-search, response-format-schema,
  fallback-models (exercises `delete body.model` + `route`),
  provider-prefs (ZDR + the assignment key order), reasoning-effort,
  and tool-roundtrip-WITH-image (succeeds where image-free refuses).
  Plus a `response-bodies` openrouter case driven WITH an image, to
  pin the raw-wire parse + the now-materialized `cacheUsage` (the
  recorder currently drives image-free params only —
  `record-response-bodies.mjs:165-172`).

### Bug 32 — the attachment-capability map
- v4 `lib/llm/attachment-support.ts:85-91`: OPENROUTER flips to
  `true, ['image/jpeg','image/png','image/gif','image/webp']` (+
  description/notes; the other entries gained comments only).
- v5: `files/attachment_support.rs:42` (`"OPENROUTER" => &[]`) — flip
  it; add an OPENROUTER unit test (none exists — v4's
  `llm-attachment-support.test.ts` diff is the shape).
- **DB-visible consequence, no code edit:** `api/settings.rs:803`
  defaults `resolvedSupportsImageUpload` from
  `supports_mime_type(provider, "image/jpeg")` when the client omits
  it — OPENROUTER creation defaults flip false → true. Grep the
  tier-2 corpora for an OPENROUTER connection-profile create that
  omits `supportsImageUpload` (`connection_profiles_tier2`,
  `settings_routes`) — any such case's expected state moves;
  regenerate those families if hit, and record the check either way.
  `file_fallback.rs` is unaffected for images (profile-flag
  short-circuit) — verify, don't edit.

### Bug 33 — Grok text/PDF arms live
- v4 `qtap-plugin-grok/provider.ts`: new `isHandledMimeType` (`:69-75`
  — supported images OR `text/*` OR `application/pdf`); the gate
  swap (`:149`); the rejection string gains the `", text/*"` suffix
  AFTER the joined list (`:152` — PDF not mentioned); branch order
  after the gate unchanged, so text/PDF now reach the data check
  first (`'File data not loaded'` newly reachable) and PDF reaches
  the Files-API refusal (v5's string at `responses_api.rs:113`
  already matches).
- v5: `model/request_builder/responses_api.rs:20/:45-62/:79-118` —
  the gate + string + the dead-code comments at `:80-85`/`:96-99`
  (rewrite them; the arms are live now).
- Corpus: lines 69/142 (`grok/unsupported-attachment[stream/send]`,
  `TXT_ATT_RAW` = raw `"line one\nline two"`) flip BY DESIGN: the
  body gains the `[File: raw.txt]\n…` input_text block (raw data has
  a newline → `decodeBase64Text` returns it verbatim, bug 34's
  wrapper) and `attachmentResults` moves to sent. **Rename the
  now-lying case** to `text-attachment` and add: `grok/pdf-attachment`
  (Files-API string), `grok/binary-attachment` (e.g. zip — NO current
  vector reaches the new suffixed rejection), `grok/
  text-attachment-no-data`, and a Grok text vector carrying REAL
  base64 (Grok has no charset pre-guard; its round-trip is pinned
  nowhere).

### Bug 34 — base64 decode round-trip
- v4: identical local `decodeBase64Text` in anthropic (`:35-41`,
  applied `:307`; the newline/charset pre-guard at `:305` unchanged)
  and grok (`:33-39`, applied `:177`, NO pre-guard): normalize
  (`\s+` strip, trailing `=` strip), `Buffer.from(data,'base64')`,
  re-encode, compare normalized; match → utf-8 decode, mismatch →
  verbatim. Node-24-verified consequences: `"hello"` → stays
  `"hello"` (was mojibake), `"x=1"` → stays `"x=1"` (was `""`), real
  base64 unchanged.
- v5: `model/request_builder/anthropic.rs:87-105`
  (`anthropic_text_document_data`) and the Grok text arm at
  `responses_api.rs:97`, both over `node_lenient_base64`
  (`responses_api.rs:125-155`) — which STAYS (it is the decode half);
  wrap both sites with the round-trip compare. The
  `node_lenient_base64_matches_node_24` pin (`:517-545`) stays green
  (it pins the raw decoder).
- Corpus: lines 14/87 (`anthropic/text-attachment-mangled-b64`, both
  modes) flip mojibake → `"hello"` BY DESIGN — the corpus vectors
  that pinned the old behavior regenerate. Lines 12/85 and 13/86
  stay byte-identical (assert it). **New coverage:** the `"x=1"`
  vector (absent today; v4's tests cover it).

### Bug 35 — the Ollama stream splitter
- v4 `qtap-plugin-ollama/provider.ts:237+`: a `buffer` string;
  `decoder.decode(value, {stream:true})` mid-stream and a bare
  `decode()` FLUSH at done; `lines = buffer.split('\n'); buffer =
  lines.pop() ?? ''` mid-stream, full flush at done; per-line
  `.trim()` before `JSON.parse` (empty-after-trim skipped);
  done-tail parse failure → `logger.debug`, mid-stream →
  `logger.warn`. THREE distinct changes: cross-read JSON buffering,
  per-line trim (CRLF now parses), cross-read UTF-8 flush.
- v5: `model/decoders/ollama_ndjson.rs` — `push()` (`:158-175`) does
  per-push `from_utf8_lossy` with no cross-push buffer (**will
  corrupt a multi-byte char split across pushes** — `ollama-unicode.
  wire` carries `café`, `日本語 🎉`; use an incremental UTF-8
  decode, not per-push lossy); `finish()` (`:177-182`) must flush;
  the header's "push-boundary-sensitive BY DESIGN (a ported v4 bug)"
  block (`:10-20`) is now wrong; the unit test
  `split_line_across_pushes_loses_content_bug_parity` (`:218-235`)
  **INVERTS** (content must survive) — the pin trips by design.
- Harness: `stream_decoders_equivalence.rs` — remove the ollama
  byte-at-a-time exclusion (`:296-299`) so ollama joins the
  three-chunking equivalence (`:219-234`); rewrite the header note
  (`:25-31`).
- Corpus: the three `ollama_ndjson` wire fixtures are line-aligned
  and newline-terminated, so `ollama.recorded.ndjson` regenerates
  **byte-identical** — regenerate for provenance, expect no diff;
  the RED is Rust-side (the inverted test + the newly-enabled
  chunking until the buffer lands).

### Quirks
- Plugin version bumps are v4-side only (v5 pins no plugin
  versions). The `__mocks__/@openrouter/sdk-chat-compat.ts` +
  jest.config change are jest-only — the recorders run under
  node/tsx from the plugin dir and need NO mock; the
  `openrouter_sdk_pricing_equivalence` jest oracle is unaffected
  (nothing on the pricing path changed).
- The recorders pin `{method, url, body}` (+`attachmentResults`)
  only — the new headers and abort behavior on the vision path are
  invisible to `request_builder_equivalence`. Check whether any
  provider_io/transport family pins headers; if none, note the gap
  in the lane record rather than inventing a new harness shape
  mid-drift.

## Tiered deliverables

### Tier 1 — must land
1. Bug 31 end-to-end: the new builder arm (assignment-order body,
   stream-path message shape, raw tools passthrough, the
   `delete body.model` fallback interplay, the assignment-order
   `provider` variant), the refusal-arm deletion, the raw-wire
   response-parse flavor, the corpus regen + the seven new
   request vectors + the image-driven response-bodies case, the
   `EXPECTED_REFUSALS` shrink with its count assertion.
2. Bug 32: the map flip + the OPENROUTER unit test + the tier-2
   corpus grep/regen check, recorded.
3. Bug 33: the widened gate + string + live arms + the renamed case
   + the four new Grok vectors.
4. Bug 34: the round-trip wrapper at both sites + the regenerated
   mangled-b64 vectors + the `"x=1"` vector + byte-identity
   assertions on the untouched b64/raw vectors.
5. Bug 35: the buffered decoder (incremental UTF-8 + line carry +
   trim + finish-flush) + the inverted unit test + ollama joining
   the byte-at-a-time chunking equivalence + the provenance regen.

### Tier 2 — should land
6. The stale-comment sweep in this lane's files (the responses_api
   dead-code notes, the ollama header block, the request-builder
   refusal notes).

### Tier 3 — explicit loud deferrals
- The header/abort-behavior pinning gap (if no existing family
  reaches headers) — record it, don't build new harness machinery
  in a drift lane.

## The differential requirement, restated

Corpora regenerate with v4's REAL plugin code at the `f4955e0e` pin
via the committed recorders (`record-request-envelopes.mjs`,
`record-response-bodies.mjs`, `record-stream-fixtures.mjs`), from
the plugin directories, with exit codes checked and fresh-baseline
markers grepped ([[oracle-regen-silent-stale-pass]]). Families BY
NAME with `--nocapture`, zero SKIP: `request_builder_equivalence`
(coverage-asserted — the shape assertion moves with the new
vectors, never hand-counted per
[[harness-corpus-shape-constants-rot]]), `response_parse_equivalence`,
`stream_decoders_equivalence`, `tool_wire_call_site` (neutrality),
plus `connection_profiles_tier2`/`settings_routes` if the bug-32
grep hits. Pre-existing vectors that v4 did not touch must
regenerate BYTE-IDENTICAL — assert it, per the P4.D33 precedent.
Changed arms mutation-proven (the D24 rule). Every recorder recipe
stays runnable ([[harness-recipes-are-runnable]]).

## Fixtures

This lane owns: the three provider corpora
(`request-envelopes.recorded.ndjson`, `google-wire` — expect
untouched, verify — and `response-bodies.recorded.ndjson`), the
`ollama_ndjson` stream fixtures + recorded NDJSON, and any new wire
fixture the Grok/base64 vectors need. It must NOT touch any sibling
lane's fixtures.

## Ownership (BINDING)

THIS lane owns: `crates/quilltap-core/src/model/request_builder/
{chat_completions.rs, responses_api.rs, anthropic.rs, mod.rs}`,
`model/response_parse.rs`, `model/decoders/ollama_ndjson.rs`,
`crates/quilltap-core/src/files/attachment_support.rs`, the
harness recorder scripts + corpora above, and
`request_builder_equivalence.rs` / `response_parse_equivalence.rs` /
`stream_decoders_equivalence.rs`. It must NOT touch:
`api/settings.rs`, any sibling lane's files, `apps/web/**`,
`api/types.rs`. `CHANGELOG` and `status-log.md` are append-only
union-merge. Version bumps per `.claude/commands/commit.md`; the
unifier recounts.

## Verification gate (before declaring done)

`cargo fmt --all --check`; clippy both feature sets `-D warnings`;
`cargo test --workspace --no-fail-fast` with the lane's env block
([[cargo-test-workspace-is-fail-fast]]); the lane's differentials BY
NAME with `--nocapture`, zero SKIP, over corpora regenerated fresh
at `f4955e0e` with the byte-identity assertions on untouched
vectors; no SPA gate owed. 💸 note for the round record: the new
OpenRouter vision path's LIVE proof (a real image send on a real
key) joins the owed dogfood queue — no live spend in this lane.
Per-commit rules: `.claude/commands/commit.md`. Lane record appended
to `status-log.md` before handoff.
