# Work order P4.6bi — the episodic-recall drift catch-up, **round 1 / lane 3: the character-outfit SPA + the New-Chat picker re-port** (Aurora Wardrobe-tab `canChooseOutfit` + the outfit-selector Starting-Outfit default + the `/salon/new` full-roster/Play-As drift) — round baseline `8bf3cb5f`

> **STATUS: CLOSED — UNIFIED ON MAIN (2026-07-21, the episodic-recall
> drift round-1 unification).** Tier-1 complete (units 1–4): the
> Wardrobe-tab `canChooseOutfit` card (edit + detail, shared
> `qt-character-choose-outfit-card`); the outfit-selector `canChooseOutfit`
> synchronous seed (`computeSyncInitialMode`); the New-Chat picker
> re-port (`e2eb3d21`: full roster in Select Characters, cast-only
> Play-As, keep-on-revert) + the `8bf3cb5f` new-chat outfit plumbing; a
> live new-chat e2e beat. Specs match v4's post-drift `NewChatForm.test.tsx`.
> The Shared-contract `canChooseOutfit` seam composed with no code wire
> (name-for-name diff clean). **OPEN / deferred (loud):** (1) the
> `help/chats.md` prose is **out-of-repo** — v5 syncs help from a runtime
> `<cwd>/help/` tree; deferred to whoever owns the production `help/`
> tree; (2) the v4 outfit-selector auto-resolve + collapsed mode badges +
> the wardrobe-items `fetched` distinction ride v5's **deferred new-chat
> wardrobe-composer family** (the disabled "Compose" radio is that
> deferral's visible surface). Touched only `apps/web`. SPA 0.5.245.
> Original order preamble follows.
>
> Round-1 lane of the
> episodic-recall drift catch-up campaign. SPA-only (Angular). Delivers
> the SPA half of the character-outfit feature (sibling server: P4.6bh)
> **and** the already-owed New-Chat picker re-port (v4 `e2eb3d21`, plus
> the `8bf3cb5f` outfit plumbing into the new-chat form — they overlap
> the same files, so one lane owns all of `new-chat/`).

## Preamble

**v4 reference baseline for this round: `8bf3cb5f`, tree state verified
at planning.** At lane start, run in `~/source/quilltap-server`:

```bash
git log 8bf3cb5f..HEAD --oneline
```
**If HEAD moved, STOP and report.** This lane is SPA-only (no Rust, no
oracle regen) — its "reference" is v4's React components at `e2eb3d21`
(the picker) and `8bf3cb5f` (the outfit SPA + the new-chat outfit
plumbing), reimplemented in the ported Angular verticals.

**Sibling lanes (this round):** `p4.d12-episodic-spine-foundation.md`
(memory data + schema keystone) and `p4.6bh-character-outfit-server.md`
(the server half of the outfit/wardrobe feature — provides the
`canChooseOutfit` character field this lane consumes). The **Shared
contract** and **Ownership** sections below are binding and reproduced
**verbatim and identically** in P4.6bh's order. You may proceed against
the contract before P4.6bh lands (you depend only on the field
names/types). You run in a worktree; a unification pass integrates.

## Mandate

Three SPA slices, all Angular:

1. **`canChooseOutfit` in the character editor/detail** — a Wardrobe-tab
   checkbox ("Let this character choose their opening outfit") that PUTs
   `{canChooseOutfit}` optimistically (spinner + re-fetch-on-failure),
   in both the edit and view/detail character screens.
2. **The outfit-selector Starting-Outfit default** — seed each cast
   member's Starting Outfit mode from `canChooseOutfit` (+ continuation
   state), with the provisional→auto-resolve behavior once the wardrobe
   loads; collapsed-header mode badges.
3. **The New-Chat picker re-port** (v4 `e2eb3d21`) — the Select
   Characters list shows the **full roster** (incl. default-user
   personas); the Play-As dropdown offers **only the cast**; reverting a
   persona to "Chat as yourself" **keeps** the character in the cast
   under LLM control (profile cleared) instead of removing it. Plus the
   `8bf3cb5f` plumbing: pass `canChooseOutfit` into the outfit-selector
   from the new-chat cast entries.

**Do NOT touch:** any Rust crate, `fresh_schema.json`, or the memory
subsystem. This lane is `apps/web` only.

## Survey-verified starting points (fresh v4 + v5 read, 2026-07-21)

### A. `canChooseOutfit` editor/detail (v4 `8bf3cb5f`)

- v4: `app/aurora/[id]/edit/CharacterEditView.tsx` + `edit/types.ts`
  (adds `canChooseOutfit?: boolean`), `app/aurora/[id]/view/
  CharacterDetailView.tsx` + `view/types.ts` + `view/hooks/
  useCharacterView.ts` (`savingCanChooseOutfit` +
  `handleSaveCanChooseOutfit(enabled)`: PUT the flag, optimistic local
  update, toast, revert-via-refetch on error — mirrors the existing
  `handleSaveCanDressThemselves`/`…CreateOutfits`).
- v5 targets: `apps/web/src/app/screens/characters/edit/` (the
  edit vertical), `apps/web/src/app/screens/characters/view/character-detail.ts`
  + `view/tabs/wardrobe-tab.ts` (the Wardrobe tab — where the checkbox
  goes). Mirror the existing `canDressThemselves`/`canCreateOutfits`
  toggle handling already present in v5's wardrobe tab.

### B. The outfit-selector (v4 `8bf3cb5f`, ~150 lines — biggest UI change)

- v4: `components/wardrobe/outfit-selector.tsx` +
  `lib/hooks/use-character-wardrobe-items.ts`. `OutfitSelectorCharacter`
  gains `canChooseOutfit?: boolean`. `computeSyncInitialMode()` seeds
  each character's Starting Outfit mode synchronously: continuation chat
  → `previous_chat`; LLM-controlled + `canChooseOutfit` → `llm_choose`;
  else provisional `default`. Provisional sections **auto-resolve**
  `default`-vs-`manual` once the wardrobe loads (no usable default outfit
  → `manual`, expanded). Collapsed-header mode badges
  (`MODE_SUMMARY_LABELS`: Defaults / Composed / Dress Themselves /
  Undressed / Same as Last). The wardrobe-items hook adds a `fetched:
  boolean` so the selector distinguishes "not looked yet" from "looked,
  found none".
- v5 targets: `apps/web/src/app/screens/new-chat/outfit-selector.ts`,
  `outfit-slots-preview.ts`, and the v5 wardrobe-items data source (find
  the hook/service equivalent under `apps/web/src/app/wardrobe/` — e.g.
  `outfit-store.ts` / `wardrobe.api.ts`). Add the `fetched` distinction
  if v5 lacks it.

### C. The New-Chat picker (v4 `e2eb3d21` + `8bf3cb5f`)

Precise predicates (from the v4 diffs):

- **Character-selection list (data source).** v4 `hooks/useNewChat.ts`
  (~L234): OLD `loadedCharacters = all.filter(c => c.controlledBy !==
  'user')` → NEW `loadedCharacters = all` (full roster incl.
  `controlledBy==='user'` personas). `loadedUserChars = all.filter(c =>
  c.controlledBy==='user')` unchanged (still tracked, no longer feeds
  Play As).
  - v5: `apps/web/src/app/screens/new-chat/new-chat.state.ts:150` —
    change the `!== 'user'` filter to `all`. (List rendered by
    `character-picker-panel.ts:170-178` off `core().characters()`.)
- **Play-As dropdown option set.** v4 `NewChatForm.tsx` `playAsOptions`:
  OLD `fromCast` ⧺ `fromDefaults(userControlledCharacters not in cast)`
  → NEW cast-only: `selectedCharacters.map(sc => ({id: sc.character.id,
  label: formatCharacterName(sc.character)}))`.
  - v5: `apps/web/src/app/screens/new-chat/new-chat-form.ts:464-478` —
    drop `fromDefaults`; return cast-only. (`userControlledCharacters`
    stays a prop but is no longer read for options.)
- **Play-As transition (`applyPlayAs`).** v4 `handlePlayAsChange`: blanket
  revert — every `controlledBy==='user'` entry → `{controlledBy:'llm',
  connectionProfileId:''}`; `nextId===''` returns the reverted cast
  (nobody removed); else flip the chosen id to `user` (profile cleared).
  No `defaultUserIds` branch, no pull-in of non-cast personas.
  - v5: `apps/web/src/app/screens/new-chat/new-chat.logic.ts:90-131` —
    replace the remove/pull-in body with the blanket-revert; the
    `userControlledCharacters` param is no longer used for the transition.
    Call site `new-chat-form.ts:496-499`.
- **Outfit plumbing (`8bf3cb5f`).** v4 `NewChatForm.tsx` adds
  `canChooseOutfit: sc.character.canChooseOutfit ?? false` to both the
  LLM-cast entry and the `userEntry` when building the cast list;
  `new-chat/types.ts` adds optional `canChooseOutfit?: boolean` to the
  `Character` interface (documented: true → new chat defaults Starting
  Outfit to "let character choose"; absent/false → defaults, or Compose
  when no usable default).
  - v5: thread `canChooseOutfit` from the character list item into the
    `OutfitSelectorCharacter` the new-chat form builds (this is the
    contract meeting point — see Shared contract).

### Specs / e2e (v5)

- `apps/web/src/app/screens/new-chat/new-chat.logic.spec.ts` — the
  `applyPlayAs` block mirrors v4's tests, **including** the "add a
  default-user character" case (~L108) that v4 **deleted** — remove it;
  update the two renamed/changed cases (dropdown now cast-only;
  "Chat as yourself" now keeps Bob under LLM control, `toHaveLength(2)`).
- `apps/web/src/app/screens/new-chat/new-chat-form.spec.ts` — the
  dropdown-options assertion (currently seeds `userControlledCharacters`
  and expects them in options) → cast-only.
- `apps/web/e2e/new-chat-flow.spec.ts` (+ `salon-autonomous-entry.spec.ts`
  which uses the autonomous toggle gated by a user cast member) — update
  to the new roster/Play-As behavior; add a beat that a default-user
  persona now appears in Select Characters and that reverting keeps it in
  the cast.
- **Help doc:** v4 edits `help/chats.md` ("Taking a Character's Chair —
  Play As" — dropdown draws "strictly from the room itself"; the full
  roster now includes personal personas; revert leaves the character "in
  the room" needing a connection profile). v5 has **no `help/chats.md`
  checked in** (help syncs from a runtime `<cwd>/help/` tree). Confirm
  with the unifier/parent where the production `help/` tree lives; if
  there is an in-repo/runtime target, sync the prose; otherwise note it
  as an out-of-repo doc-only edit and skip (do not invent a tree).

## Tiered deliverables

### Tier 1 — must land

1. The Wardrobe-tab `canChooseOutfit` checkbox (edit + detail), optimistic
   PUT with revert-on-error, reading the field from the character
   (Shared contract).
2. The outfit-selector Starting-Outfit default from `canChooseOutfit` +
   the provisional→auto-resolve behavior + the `fetched` distinction +
   collapsed mode badges.
3. The New-Chat picker re-port (full roster, cast-only Play As,
   keep-on-revert) + the `canChooseOutfit` new-chat outfit plumbing.
4. Updated component/logic specs (the deleted case removed; the changed
   assertions) + an updated/added e2e beat. `ng test` and `ng build`
   green; Playwright green.

### Tier 2 — should land

5. The `help/chats.md` prose sync **if** a v5 target exists (see above);
   else documented as out-of-repo.

### Tier 3 — explicit loud deferrals

None expected. If the outfit-selector needs a wardrobe capability the SPA
lacks, surface it visibly (not a silent no-op) and enumerate in the
status header.

## The differential requirement (SPA form)

SPA lanes verify by (a) porting v4's component/logic **tests**
case-for-case (the v4 `NewChatForm.test.tsx` block → v5's
`new-chat.logic.spec.ts` / `new-chat-form.spec.ts`) and (b) a live
Playwright beat over the ported vertical. There is no oracle NDJSON for
this lane. Follow the established SPA-lane recipe (memory notes
`p4.6l-groups-projects-spa.md`, `p4.6j-characters-remainder-spa.md`).
Match v4's post-drift test assertions exactly (the survey lists each
changed/added/deleted case).

## Fixtures

Consumes the e2e instance's committed character fixtures. If a beat needs
a default-user persona in the roster, seed it via the existing e2e
fixture/dispatch path (do not hand-roll DB rows). **Mutation beats prove
nothing until `ng build` rewrites the bundle** (memory note
`e2e-mutation-check-needs-a-rebuilt-dist.md`) — rebuild before the
Playwright run. Watch the shared e2e port (`e2e-port-4319-cross-worktree-kill`)
if running alongside sibling lanes.

## Shared contract (binding — identical in P4.6bh)

The character `canChooseOutfit` field is the meeting point between the
server lane (P4.6bh) and this SPA lane. Name-level, binding both ways:

1. **Character read shape.** The character GET (`/api/v1/characters/:id`)
   and list (`/api/v1/characters`) responses include
   **`canChooseOutfit: boolean`** (never null — default `false` when the
   vault omits it). The SPA reads this field name verbatim.
2. **Character PUT accepts** (all optional; omitted keys unchanged):
   - **`canChooseOutfit: boolean`** — vault-routed (`properties.json`).
   - **`canDressThemselves: boolean | null`** — DB column tri-state;
     explicit `null` is preserved (inherit-global), never stripped.
   - **`canCreateOutfits: boolean | null`** — DB column tri-state; same
     null semantics.
3. **Defaults / hydration.** A character whose vault `properties.json`
   lacks `canChooseOutfit` hydrates as `false` (no migration, no
   backfill). Freshly scaffolded vaults write `canChooseOutfit: false`.

This lane may proceed against the contract before P4.6bh lands (it
depends only on these names/types). The `OutfitSelectorCharacter` input
type (owned by this lane, since this lane owns `outfit-selector.ts`)
gains `canChooseOutfit?: boolean`; the new-chat form populates it from
the character's `canChooseOutfit`.

## Ownership (binding — identical in P4.6bh)

**P4.6bh (server lane) owns and may edit:**
`crates/quilltap-core/src/db/vault_read_overlay.rs`,
`db/vault_character_write.rs`, `db/vault_character_update.rs`,
`crates/quilltap-core/src/api/characters.rs`, the character-response
projection in `crates/quilltap-web`, and the characters-route
differential + its oracle/fixture in `crates/quilltap-harness` /
`harness/oracle`.

**P4.6bi (this lane) owns and may edit:** everything under `apps/web`
(the aurora/character edit + view + Wardrobe tab, `outfit-selector`, the
`new-chat` vertical, the relevant `.spec.ts` + e2e).

**Neither lane touches** `fresh_schema.json` (only P4.d12 does), the
memory subsystem (P4.d12), or the other's files.

**Version-bump ownership:** this lane bumps `apps/web` (SPA
`package.json`). The Rust lanes bump their crates; the unifier
accumulates. Do not coordinate exact integers; bump each commit.

## Verification gate (run before declaring the lane done)

- `ng test` green (the updated/added component + logic specs incl. the
  ported v4 cases).
- `ng build` clean (rebuild the dist before the e2e run).
- Full Playwright green over a freshly rebuilt dist — the new-chat +
  outfit beats active; **run from `apps/web`** (memory note
  `p4.6au-av-7c-round-plan.md`).
- Per-commit discipline per `.claude/commands/commit.md`
  (`CHANGELOG`/`status-log.md` append-only).
- Confirm at hand-off: this lane touched **only** `apps/web`.
