# Work order P4.D83 — the `93ed8abf` drift, provider-params half: `resolveSamplingParams`, the profile-parameters wire (Ollama/OAC/DeepSeek/Z.AI), OAC tool calling, the Ollama request timeout, optionsSchema on the manifests — v4 `93ed8abf`

**Status:** OPEN

## Preamble

- **v4 baseline for this round: `93ed8abf`** (v4 main HEAD at planning,
  2026-08-15 — "fix: local providers send the profile's parameters; OAC
  can call tools (bug 71)"). This lane absorbs `d89babc4` ("send the
  profile's Max Tokens and Top P; Ollama request timeout is a setting")
  and `93ed8abf` (bug 71) — the sibling P4.D82 absorbs `f933ba9c`
  (bug 70). **Drift-check at lane start** per the two-branch rule
  (`git log 93ed8abf..main` AND `git diff main bugfix -- lib/ app/
  packages/ plugins/`; measure bugfix with `diff`, never the commit
  list). **STOP and report** if HEAD moved or the tree is dirty; pin a
  detached worktree at `93ed8abf` for every regen
  (`oracle-regen-pinned-v4-worktree` / `recipe_sweep.py --v4 <pin>`;
  ALL THREE symlink classes).
- **THIS LANE IS STACKED: branch from P4.D82's finished tip, not from
  main.** P4.D82 owns the context-budget spine and finishes first; you
  inherit its committed state, which removes all file contention on
  `orchestrator.rs` / `regenerate_swipe.rs` / `spine.rs`. Do not start
  until P4.D82's status header says its gate ran green. P4.D82 hands you
  a recorded red set for the entangled tier-3 families
  (orchestrator/regenerate/primary-stream) — flipping those green is
  YOUR exit criterion.
- Sibling lanes: **P4.D82** (upstream, see above), **P4.D84** (the SPA
  provider-options machinery, parallel, owns `apps/web/**`; meets you at
  Contracts B and C).
- Work in your own worktree on a lane branch; never `git stash` across
  lanes; edit inside the worktree (`edit-inside-the-worktree-not-main`).
- Per-commit rule: `.claude/commands/commit.md`. Version-bump ownership:
  this lane bumps `quilltap-core`, `quilltap-harness`, `quilltap-host`
  (spine/transport wiring), on top of P4.D82's bumps (sequential —
  recount at unification per the playbook). Do not bump the SPA.

## Mandate

Absorb v4 `d89babc4` + `93ed8abf` whole, server-side:

- `resolveSamplingParams` as the ONE mapper from a profile `parameters`
  bag to the three per-request sampling fields (canonical snake_case,
  camelCase tolerated for hand-edited/imported bags, absent knobs stay
  absent so nothing is invented) — routed through the Salon primary
  stream, regenerate/swipe, the greeting path (incl. the
  Concierge-uncensored branch's borrow-from-outer fallback), and the
  image-description fallback. NOT to be confused with
  `resolve_max_tokens` (`context_budget.rs:57`), which deliberately
  ignores `parameters.max_tokens` because it sizes the response reserve;
  this is the per-request generation cap.
- The allow-listed profile-parameters passthrough generalized to Ollama
  and OPENAI_COMPATIBLE (v5 already has the mechanism for
  DeepSeek/Z.AI): Ollama's widened `options` table + top-level
  `keep_alive` (sentinels `0`/`-1` go out as NUMBERS) + `think` from
  `enable_thinking`×`thinking_effort` (levels low/medium/high/max) +
  control keys never on the wire; OAC's allow-list with
  `reasoning_effort` folded into `chat_template_kwargs` (string-valued
  `chat_template_kwargs` parsed; merge, don't clobber).
- OAC tool calling on BOTH paths: `tools`/`tool_choice` on the body,
  `tool_calls` parsed back, streamed argument fragments accumulated by
  index; the capability stays default-FALSE (a seed, not a clamp).
- The Ollama per-profile `request_timeout_seconds` (default 300 s;
  blank/absent/unparseable falls through; a caller-supplied request
  budget still wins) applied to the streaming FIRST-BYTE timer and the
  non-streaming WHOLE-REQUEST signal.
- optionsSchema carried onto the committed provider manifests (all
  eight declaring plugins) via the GENERATOR, and served on the
  providers listing per Contract B — the server half of P4.D84's panel.

Ownership: `crates/quilltap-core/src/model/**` (request builders,
decoders, transport, stream/completion types),
`crates/quilltap-core/src/provider_manifest/**` + the generator,
`crates/quilltap-core/src/api/settings.rs` (the listing's optionsSchema
serve), `services/chat_create.rs`, `services/file_fallback.rs`, the
sampling-read regions of `services/orchestrator.rs` /
`services/regenerate_swipe.rs` (Contract A), a NEW sampling-params
module, and `quilltap-host/src/spine.rs` transport wiring. Must NOT
touch `apps/web/**` (P4.D84) or rework P4.D82's budget code.

## The drift, classified (surveyed FRESH 2026-08-15 at `93ed8abf`)

### v4 `d89babc4`

- NEW `lib/llm/sampling-params.ts`: `SamplingParams` (`:38`),
  `resolveSamplingParams(parameters)` (`:55`) — snake_case first,
  camelCase tolerated, absent → undefined. 76-line unit suite at
  `lib/llm/__tests__/sampling-params.test.ts` (mine for corpus shapes).
- Call sites: `streaming.service.ts` (Salon primary stream — v4's wire
  previously sent NO max_tokens/top_p; on Ollama that meant provider
  defaults `num_predict 4096`/`top_p 1`), `regenerate-swipe.service.ts`,
  `app/api/v1/chats/route.ts` (greeting, incl. the Concierge-uncensored
  branch: `resolveSamplingParams(uncensoredParams ?? {})` — an
  uncensored profile that only sets a temperature still borrows the
  rest from the outer profile), `lib/chat/file-attachment-fallback.ts`
  (`sampling.temperature ?? 0.7`, `sampling.maxTokens ?? 1000`,
  `sampling.topP` — the reasoning-floor logic unchanged).
- Ollama plugin 1.0.42: `request_timeout_seconds` option;
  `resolveProfileTimeoutMs` (profile-options.ts `:125`;
  `DEFAULT_REQUEST_TIMEOUT_SECONDS = 300` at `:93`); applied to the
  streaming first-byte timer AND the non-streaming whole-request
  signal; a caller-supplied `LLMParams.requestTimeoutMs` still wins
  (the cheap-LLM task deadlines stay hard ceilings).
- v4's verification bytes (reuse as a wire pin): profile
  `temperature 0.42 / max_tokens 777 / top_p 0.33` → Salon turn sends
  `{"temperature":0.42,"num_predict":777,"top_p":0.33,"num_ctx":8192}`.

### v4 `93ed8abf` (bug 71)

- NEW `packages/plugin-utils/src/providers/profile-parameters.ts`:
  `ProfileParamNormalizer` (`:37`), `applyProfileParameters` (`:65`) —
  allow-listed, never spread (model/messages/stream/tools unreachable
  from a profile); undefined, null, and `""` omit the key. Base class
  gains `profileParamAllowlist` (empty default —
  `openai-compatible.ts:263`) + `normalizeProfileParam` (takes the body
  so a value can land under a different key). DeepSeek + Z.AI dropped
  their hand-rolled copies.
- Ollama plugin 1.0.43, `profile-options.ts`:
  `OLLAMA_OPTION_PARAM_ALLOWLIST` (`:26` — the widened `options` keys),
  `OLLAMA_TOP_LEVEL_PARAM_ALLOWLIST = ['keep_alive']` (`:40`),
  `OLLAMA_CONTROL_PARAMS = [enable_thinking, thinking_effort,
  request_timeout_seconds, …]` (`:53` — read by v4, never on the wire),
  `OLLAMA_THINK_LEVELS = ['low','medium','high','max']` (`:85`),
  `resolveThinkSetting` (`:106` — effort + enabled → `think:
  bool|string`), `applyOllamaProfileParameters` (`:166`), the
  keep_alive number-normalization (`:144` — measured against live
  Ollama 0.32.1: an unknown think level is refused outright and
  `keep_alive:"-1"` is refused as a duration while the number is
  honoured, so the sentinels go out as numbers). Keep Model Loaded
  defaults to sending NOTHING (leaves `OLLAMA_KEEP_ALIVE` in charge).
  `thinking_effort` rides its own key rather than widening
  `enable_thinking` into an enum (which would have displayed every
  already-thinking profile as blank).
- OAC plugin 1.0.40, `provider.ts`: first options schema
  (reasoning_effort + top_k/min_p/repeat_penalty/presence_penalty/
  frequency_penalty/seed/cache_prompt); allow-list with
  `chat_template_kwargs` FIRST (`:40`; `reasoning_effort` merges into
  it `:79–:97`; a string value parses); tools: sends
  `tools`/`tool_choice` (`openai-compatible.ts:388`/`:484`), parses
  `tool_calls` back on both paths, accumulates streamed fragments by
  index (`:520–:570`); the capability stays false (a default, not a
  ceiling — the v4 editor seeds the checkbox on new profiles only and
  never disables it).
- Client: `components/settings/connection-profiles/ProfileModal.tsx`
  gains the hint under the tool-use checkbox — P4.D84's (Contract C).
- No migration, no DDL, no export-schema change (`parameters` is
  already free-form JSON and already round-trips) — **no D23 re-dump**.
- v4 tests: `__tests__/unit/plugins/local-provider-params.test.ts`
  (26 cases) + the options-schema snapshot gains an OAC entry.

### v5 starting points (all verified 2026-08-15)

**The headline: v5 has v4's bug WORSE.** The SPA writes the bag
snake_case (`profile-form.ts:155–159`) but the Salon primary stream and
the greeting read camelCase — so a profile saved in v5 today sends NO
Max Tokens and NO Top P on those paths:

| Site | Reads today | Anchor |
|---|---|---|
| Salon primary stream | `temperature`/`maxTokens`/`topP` | `services/orchestrator.rs:2401–2427` (`model_params` `:2401`, `StreamParams{}` `:2406–2427`) |
| Greeting | `temperature`/`maxTokens`/`topP` | `services/chat_create.rs:1571–1573`; bag→`profile_parameters` `:1584`; `base_request` `:1599` |
| Greeting uncensored | `maxTokens`/`topP` + outer fallback | `chat_create.rs:1755–1789` (`attempt_uncensored_reroute`) |
| Regenerate/swipe | `temperature`/`max_tokens`; **no top_p read** | `regenerate_swipe.rs:372–373`, `:415–418`, `:426–438` |
| Image-description fallback | `temperature`/`max_tokens` (0.7/1000 defaults, 4000 reasoning floor) | `services/file_fallback.rs:396–407`, `:432–446`, defaults `:45–46` |

- **No `resolveSamplingParams` twin exists.** The P4.D79/D80 chokepoint
  is bag-SHAPING only: `cheap_llm.rs:136` `profile_params_parts` (+
  adapters `:184`/`:195`; `NUM_CTX_PROVIDER` `:104`) — 900-case pinned;
  READ-ONLY for this lane. Sibling hand-rolled extractions that may want
  the resolver: `services/announcer/character_voiced.rs:199`,
  `services/answer_confirmation.rs:703`, `services/cheap_llm_exec.rs:119`,
  `cheap_llm.rs:223/280/295/359` — MEASURE against v4 before converting
  any (v4 `d89babc4` converted only the four sites above; converting a
  site v4 didn't is a divergence).
- Param types: `StreamParams` (`model/stream.rs:349–374`) has
  temperature/max_tokens/top_p/profile_parameters, **no
  request_timeout_ms**; `CompletionParams` (`model/completion.rs:95–126`)
  has `request_timeout_ms` (`:125`), **no `top_p` at all** — the
  regenerate/vision paths structurally cannot carry Top P today.
- Builders: NOT `providers/` — `model/request_builder/`
  (`chat_completions.rs` 1096 lines serves five providers: OAC `:358`,
  DeepSeek `:440`, Z.AI `:505`, OpenRouter `:598`/`:778`/`:880`, Ollama
  `:1062`; dispatch `mod.rs:207–224`). `apply_profile_params` ALREADY
  EXISTS (`:398–424` — iterate allow-list, skip null, skip `""`,
  DeepSeek thinking normalization `:415–420`): **v4's
  `applyProfileParameters` twin is present; the work is generalizing
  it** — `DEEPSEEK_PROFILE_ALLOWLIST` (`:387–394`),
  `ZAI_PROFILE_ALLOWLIST` (`:466`), `strip_thinking_incompatible`
  (`:425–439`). The Ollama `options` bag today: `:1065–1077` (only
  temperature/num_predict/top_p/stop/num_ctx); `ollama_num_ctx` `:1049`,
  `ollama_enable_thinking` `:1032`, `think` on the body `:1085–1088`.
  `build_openai_compatible_body` (`:358–379`) sends no tools, no
  profile params — its doc comment `:352–357` says so.
- Decoder: `model/decoders/chat_completions_sse.rs` — `Flavor` `:35–56`;
  the accumulator ALREADY exists (`AccTool` `:65`, `tool_mut` `:102–108`,
  the loop `:151–180`); **the OAC block is one flavor guard at `:151`**
  (`if self.flavor != Flavor::OpenAiCompatible`). The real work is the
  OAC terminal `raw_response` shape (SDK flavors assemble at `:256–282`;
  OpenRouter camelCase `:287–306`) — record it from v4, don't invent it.
- Timeout plumbing: `model/transport.rs` —
  `DEFAULT_REQUEST_TIMEOUT_MS = 300_000` (`:103`), `TransportPolicy`
  (`:112–124`), `with_request_budget` (`:139–148` — budget replaces
  timeout AND drops retries to 0); the module header (`:24–44`) already
  documents execute = whole exchange vs execute_stream = time-to-headers
  — exactly v4's contract, no new mechanism. Non-streaming is WIRED
  (`CompletionParams.request_timeout_ms` →
  `quilltap-host/src/spine.rs:271`); **streaming is the gap**:
  `StreamParams` lacks the field and `WireStreamingProvider` uses the
  process-wide policy unconditionally
  (`model/streaming_provider.rs:196`, call sites `:348/:367/:396`;
  constructed `spine.rs:292–312`). No `resolveProfileTimeoutMs` twin
  exists. ⚠ Per the P4.15 ruling (`transport.rs:51–54`): timeouts are
  NOT oracle-checkable — prove at unit tier with stalling-socket
  servers (`transport.rs:429–670` is the pattern).
- Manifests: `provider_manifest/manifests/*.json` (9 files;
  `include_str!` at `mod.rs:340–350`); capabilities types
  `mod.rs:147–176`; **optionsSchema does not exist in v5** —
  `api/settings.rs:1931` (the doc note) + `:1973` (`Value::Null`),
  normalized away at `providers_listing_equivalence.rs:37`. **All
  EIGHT v4 plugins except google declare `getProviderOptionsSchema`**
  (anthropic, deepseek, grok, ollama, openai, openai-compatible,
  openrouter, z-ai) — the machinery lands all eight, not just OAC's
  new one. Generator: `harness/oracle/providers/
  gen-provider-manifests.mjs` — the AUGMENTATION table (`:15`, `:72`)
  and its reversion history (`:35–44`); the standing rule verbatim at
  `:42–43`: *"When a hand-fix lands in a committed manifest, fix this
  table in the same commit."* optionsSchema is getter-derivable
  (`plugin.getProviderOptionsSchema?.()`), so the generator EXTRACTS
  it — per `byte-exact-static-data-transcription`, ship the generator
  change, never hand-transcribe.
- Capability map: OAC `toolUse: false` at
  `manifests/openai_compatible.json:27` — **stays false** (bug 71
  changes the meaning to seed-not-clamp, which is client-side; the
  runtime gate `tool_build.rs:401/:459` reads the PROFILE's
  `allowToolUse`, which is already the v4 shape — MEASURE and confirm,
  then record).
- The `external-prompt-generator` rider: v4's `d89babc4` also touched
  `external-prompt-generator.service.ts`; NO v5 twin exists (unported
  disabled surface) — carry the rider note exactly as P4.D82 does.

## Tiered deliverables

**Tier 1 (must land):**

1. The `resolve_sampling_params` twin (new module; v4
   `lib/llm/sampling-params.ts` 1:1) + the four call-site conversions
   (orchestrator primary stream, regenerate/swipe — `CompletionParams`
   gains `top_p`, threaded to every builder that reads it —,
   chat_create greeting + uncensored borrow, file_fallback). The
   greeting's uncensored-borrows-outer semantics exactly v4's.
2. Ollama: the options table (`OLLAMA_OPTION_PARAM_ALLOWLIST` twin),
   top-level `keep_alive` with number sentinels and absent-by-default,
   `think` from enable_thinking×thinking_effort per
   `resolveThinkSetting`, control keys never on the wire, `num_ctx`
   through the same table. Byte-exact vs v4's recorded envelope rows.
3. OAC: the allow-list + `chat_template_kwargs` fold (string parse,
   merge-don't-clobber), tools/tool_choice on both paths, `tool_calls`
   parse-back non-streaming, the decoder flavor-guard drop + the OAC
   `raw_response` shape recorded from v4.
4. DeepSeek/Z.AI: converge onto the generalized applier exactly as v4
   collapsed theirs — with BYTE-IDENTICAL envelope pins proving the
   collapse is neutral (v4's DeepSeek neutrality gate mirrored).
5. The Ollama `request_timeout_seconds`: `resolveProfileTimeoutMs`
   twin; `StreamParams.request_timeout_ms` + per-call policy override
   at the three `execute_stream` sites (first-byte only); non-streaming
   through the existing `with_request_budget`; caller-supplied budget
   still wins; unit-tier stalling-socket proofs.
6. optionsSchema: the generator extracts all eight plugins' schemas
   into the committed manifests (same commit teaches the augmentation
   table if any hand-adjustment is needed); `api/settings.rs` serves it
   per Contract B; `providers_listing_equivalence.rs:37`'s normalizer
   is REMOVED so optionsSchema becomes a comparand.

**Tier 2 (should land):**

7. Flip P4.D82's recorded entangled-red set green
   (orchestrator-tier3, regenerate-swipe, primary_stream) — regenerate
   at the pin, run by name, zero SKIP.
8. The OAC envelope corpus widened well past its current 4/4 (thinnest
   bucket in the corpus) — tools on/off × options × chat_template_kwargs
   string/object × reasoning_effort; Ollama rows re-recorded (the
   pre-existing 17 change exactly as predicted — enumerate which).
9. The sibling hand-rolled sampling extractions measured against v4
   (convert ONLY what v4 converted; record the rest).

**Tier 3 (explicit refusal-arm deferrals):**

10. Any optionsSchema field type the SPA panel does not render this
    round (`multiEnumSource: fetchedModels`, `affects: modelInput`)
    stays SERVED faithfully server-side regardless — deferral, if any,
    lives in P4.D84, never here. No server-side stubs.

## The differential requirement (restated)

Every unit ships with an equivalence test driving v4's REAL code at the
`93ed8abf` pin. Families this lane owns:

- **NEW `sampling_params` tier-1 family**: oracle case driving v4's real
  `resolveSamplingParams` over a corpus (snake/camel/mixed/absent/null/
  string-numbers/negative/fractional — mine v4's 76-line suite), exact
  compare.
- **`request-envelopes`** (committed corpus,
  `request-envelopes.recorded.ndjson`, 191 rows; recorder
  `record-request-envelopes.mjs` + `regenerate-request-envelopes.sh`):
  re-record at the pin — ollama rows change (options table/keep_alive/
  think), OAC rows change (tools + options), deepseek/z-ai must be
  byte-identical (the collapse-neutrality pin); widen OAC per Tier 2.
  ⚠ The sweep driver never runs recording stages
  (`help-drift-round-unification`) — re-record manually from the pinned
  worktree, stage by name, never `git add -A`.
- **Stream decoders** (`fixtures/streams/chat_completions_sse`, 12
  cases + `cases.json`): new OAC tools `.wire` recordings (fragmented
  arguments across chunks; the three-chunking replay) + the OAC
  `raw_response` terminal shape.
- **`providers_listing`** (`cases/providers-listing.ts`): optionsSchema
  un-normalized; the listing differential now proves all eight schemas
  byte-for-byte (this is P4.D84's server-side proof too).
- **`ollama_think_retry` / `ollama_think_parser`**
  (`QT_ORACLE_OLLAMA_THINK_RETRY`): regen — `resolveThinkSetting` now
  consults `thinking_effort`.
- **Tier-3 closures** (from P4.D82's handoff): `orchestrator-tier3`
  (`QT_ORACLE_ORCHESTRATOR` + fixtures — the P4.D79 corpus already
  records the whole modelParams at the wire, `orchestrator.rs:2394–2397`
  comment block), `regenerate-swipe` (`QT_ORACLE_REGEN`),
  `primary_stream` (`QT_ORACLE_PRIMARY_STREAM` + the P4.41 fallback
  leg's own env), `chat-create-capstone`/`initial-greeting`
  (`QT_ORACLE_CC`/`QT_ORACLE_GREETING` — greeting sampling),
  `file-attachment-tier3` (`QT_ORACLE_FILE_ATTACHMENT`). Check each
  corpus actually EXERCISES the new reads — a bag left `{}` measures
  nothing downstream (`p4.d79-multichar-prefill-lane`); add
  snake_case-bag cases where blind, mutation-prove.
- Neutrality: `profile_params` (900 cases — must NOT change),
  `provider_registry`, `google-wire` + `anthropic`/`openai`/`grok`
  envelope buckets byte-identical (corpora self-date —
  `p4.d76-provider-sdk-wire-recheck-lane`: prove regenerated, not just
  green), `native_tool_loop`/`tool_wire` re-run.
- Unit-tier (per the P4.15 ruling, not oracle-checkable): the timeout
  quartet — streaming first-byte honored / non-streaming whole-request /
  caller budget wins / blank-falls-through-to-300s — against stalling
  sockets.

## Fixtures

- Owns re-recording: `request-envelopes.recorded.ndjson`, the
  `chat_completions_sse` stream fixtures, the provider manifests (via
  generator). Coverage assertions: keep the envelope file's
  per-(provider,mode) floor assertion current; assert SHAPE not hand
  counts.
- Consumes: orchestrator/regen/primary-stream/chat-create/file-attach
  fixture .dbs (regen their oracles at the pin as part of the tier-3
  closures).

## Shared contract A (binding; identical text in P4.D82 and P4.D83)

**The stacking seam.** P4.D82 owns and may edit: `context_budget.rs`,
`model_context.rs`, `services/build_context.rs`, `services/turn_extras.rs`
(new), `services/message_context.rs`, `token_estimation.rs`,
`tools/self_inventory.rs`, `services/orchestrator.rs`,
`services/regenerate_swipe.rs`, `quilltap-host/src/spine.rs`, enclave
files. Within `orchestrator.rs` and `regenerate_swipe.rs`, P4.D82 must
NOT touch the sampling reads (`orchestrator.rs:2400–2427` `model_params`
→ `StreamParams`; `regenerate_swipe.rs:415–438` extraction +
`CompletionParams` construction) — P4.D83 rewrites those on top. P4.D82
must not touch `model/**`, `provider_manifest/**`, `cheap_llm.rs`
(`profile_params_parts` at `:136` is read-only for both lanes — 900-case
pinned), `services/chat_create.rs`, `services/file_fallback.rs`,
`apps/web/**`. P4.D83 branches FROM P4.D82's finished tip and must not
rework P4.D82's budget code beyond what compilation of its own changes
requires (any such touch is named in its lane record). Parameter-key
names are pinned round-wide: the profile `parameters` bag's canonical
sampling keys are `temperature` / `max_tokens` / `top_p` (snake_case;
camelCase tolerated read-side only inside P4.D83's resolver), and the
Ollama control keys are `enable_thinking`, `thinking_effort`,
`keep_alive`, `request_timeout_seconds`, `num_ctx`.

## Shared contract B (binding; identical text in P4.D83 and P4.D84)

**The optionsSchema wire.** The providers listing (v5: the
`providers`-listing verb/route served from `api/settings.rs`; v4:
`app/api/v1/providers/route.ts:25–55`) carries, per provider,
`optionsSchema: ProviderOptionsSchema | null` — `null` exactly when the
plugin declares no schema (google only, at the pin). The shape is v4's
`packages/plugin-types/src/plugins/provider-options.ts`, verbatim:

```
ProviderOptionsSchema = { groups: ProviderOptionGroup[] }
ProviderOptionGroup   = { title?, helpText?, fields: ProviderOptionField[] }
ProviderOptionField   = { key, label, helpText?, type, default?,
                          enumValues?, multiEnumSource?, max?, showIf?,
                          affects?, appliesToModels? }
type ∈ 'boolean'|'enum'|'multi-enum'|'string'|'number'
ProviderOptionEnumValue = { value, label, description? }
ProviderOptionShowIf    = { field, equals }
multiEnumSource ∈ 'fetchedModels';  affects ∈ 'modelInput'
```

Field keys are storage keys inside the profile's `parameters` blob and
must round-trip untouched (the renderer reads/writes the flat bag; the
provider reads the same keys at call time). P4.D83 serves it (manifests
via the generator; the listing differential proves all eight schemas
byte-for-byte); P4.D84 renders it and must not invent, rename, or
reorder fields. The SPA contract type is
`core-contract.ts:3008–3034` (`optionsSchema?: unknown | null`) —
P4.D84 narrows its own client-side type from this contract text, not
from the server code.

## Shared contract C (binding; identical text in P4.D83 and P4.D84)

**toolUse is a seed, never a clamp.** `capabilities.toolUse` seeds
`allowToolUse` on NEW profiles only (v5 today:
`profile-modal.ts:742–756`, seed at `:753`); the checkbox stays
editable whatever the capability says; OAC's capability remains `false`
at the pin. The hint rendered under the tool-use checkbox when
`!capabilities.toolUse`, verbatim v4 (whitespace-normalized around the
`<code>--jinja</code>` span): "This provider does not advertise tool
support, so new profiles start with it off. If your endpoint does speak
native function calling — llama-server with `--jinja`, vLLM, LM Studio
— you may turn it on regardless." P4.D84 owns the rendering; P4.D83
owns keeping the capability value and its manifest byte-stable.

## Verification gate (before declaring done)

- `cargo fmt --all --check`; clippy BOTH feature sets;
  `cargo build --release`.
- `cargo test --workspace` with the round's full env block (yours +
  P4.D82's, since you contain its work) — zero failures, zero `SKIP:`
  in the round's families.
- All owned + inherited-entangled families regenerated fresh at the
  `93ed8abf` pin through the sweep driver and re-run BY NAME
  (`--nocapture`, grep SKIP); P4.D82's recorded red set demonstrably
  flipped green; the neutrality set proven regenerated (self-dating
  markers checked), not just green.
- The committed corpora staged by name; no tracked-fixture writes
  outside the intended files (the sweep-clobber lesson).
- CHANGELOG + status-log appended; version bumps per Preamble; every
  commit per `.claude/commands/commit.md`.
