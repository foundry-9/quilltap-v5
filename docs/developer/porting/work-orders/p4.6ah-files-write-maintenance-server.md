# Work order P4.6ah — the files-family write + maintenance server remainder

> **STATUS: OPEN.**
>
> Lane A of the FOUR-lane P4.6ah ∥ P4.6ai ∥ P4.6aj ∥ P4.d4 round (the
> "finish P4.6ae + catch up from v4" round, planned 2026-07-14). This
> lane completes the OPEN server remainder of
> [`p4.6ae-files-family-server.md`](p4.6ae-files-family-server.md) —
> the file-BYTES legs (chat + general upload), the maintenance verbs,
> the `action=link` leg, and the itemized `FILE_HAS_ASSOCIATIONS`
> envelope + `dissociate` arm. (The general files READ/move/promote/
> delete/folders surface + the loud `filesSync` refusal already landed
> on main under P4.6ae; do NOT re-port them.)
>
> **v4 reference baseline: HEAD `02865bdb`** (`~/source/quilltap-server`,
> v4 `4.8.0-dev.52`) — run `git log 02865bdb..HEAD --oneline` at lane
> start; **if HEAD moved, STOP and report** (classify drift, don't plan
> around it). Sibling lanes: `p4.6ai-image-generation-unrefusal.md`
> (the `imageProfileGenerate` un-refusal + the host image-generation
> seam — the OTHER core-dispatch writer this round),
> `p4.6aj-files-spa-closeout.md` (the SPA delete-associations dialog +
> beat activation, SPA-only), and `p4.d4-skip-signal-drift.md` (the
> `02865bdb` drift re-port — file-disjoint, not a core-dispatch
> writer). The **Shared contract** and **Ownership** sections below are
> binding and reproduced verbatim in all four orders. You run in a
> worktree; the unification pass integrates the lanes onto main and
> activates lane C's fixture-guarded e2e beats.
>
> **Baseline note (2026-07-14):** v4 moved ONE commit since P4.6ae was
> planned — `02865bdb` "strip a trailing 'nothing to add' line" — a
> turn-manager (`skip-signal.ts`) behavior change ONLY. Verified
> file-by-file: it touches NONE of this lane's surface (no
> `api/v1/files`, no `chats/[id]/files`, no `chat-files-v2`, no
> `file-storage`, no `overwrite-utils`, no `get-file-associations`).
> The P4.6ae survey (dated 2026-07-13 @ `6a8a77aa`, re-validated @
> `dd0d9ff5`) therefore stays byte-valid at `02865bdb` for every file
> this lane touches. The drift itself is lane D's re-port.

## Mandate

Make writing file bytes work end-to-end and finish the files
maintenance surface, differential-proven against v4's REAL route
handlers:

1. **The chat-file upload leg** (closes the P4.6ab tier-2 gap): port
   `uploadChatFile` additively into `services/chat_files.rs` over the
   existing `file_storage.rs` write seams, replace the
   `chat_media.rs:799-809` refusal body, and add the web-edge
   `POST /api/v1/chats/{id}/files` multipart route + the JSON
   `action=link` leg. The SPA composer attach currently 404s inline;
   this lands the real path. **The SPA client is LOCKED** — the server
   matches it, never vice versa.
2. **The general upload REST leg:** the `fileUpload` core variant + the
   `saveFileEntry` port (sanitize → sha256 → overwrite-reuse →
   project-vs-uploads-mount branch → 201/200) behind
   `POST /api/v1/files?action=upload`.
3. **The maintenance verbs:** `filesGenerateThumbnails`,
   `filesCleanupStale`, `filesCleanupOrphans` (the DB-provable arms;
   fs-existence legs enumerated per the P4.6w precedent, never silently
   skipped).
4. **The itemized delete envelope:** extend the shared `CoreError` with
   an additive optional `associations` field, emit the
   `FILE_HAS_ASSOCIATIONS` payload on the un-forced linked-file delete,
   and land the `dissociate=true` arm — completing the `fileDelete`
   verb that shipped in P4.6ae with those two pieces deferred.

Do NOT touch `apps/web/**` (lane C owns the SPA; the byte-GET routes in
`files_routes.rs` are frozen and lane-round HANDS OFF), the
image-generation surface (lane B: `api/image_profiles.rs`,
`quilltap-host/src/host.rs`, `tools/generate_image.rs`),
`skip_signal.rs` (lane D), or `api/salon.rs`/the frozen
courier/autonomous/mount dispatch surfaces. Wrap the already-ported
services (`file_storage.rs`, `db/files.rs`, `db/folders.rs`) — never
re-implement or refactor a frozen, differential-proven module.

## Survey-verified starting points (2026-07-14; the P4.6ae survey is the deep reference)

The parent order's "Survey-verified starting points" (P4.6ae, valid at
`02865bdb`) is the authoritative v4 route inventory + v5 gap list for
this lane — read it in full. It already pins: `handlers/get.ts`,
`handlers/post.ts`, `actions/upload.ts` (category hard-coded
`'DOCUMENT'` :64), `actions/generate-thumbnails.ts`,
`actions/cleanup-stale.ts`, `actions/cleanup-orphans.ts`,
`[id]/actions/delete.ts`, `[id]/actions/move.ts`,
`[id]/actions/promote.ts`, `folders/route.ts`, `shared.ts`
(`saveFileEntry` :117, `sanitizeFilename` :217), the chat-file route
(`chats/[id]/files/route.ts`), `lib/chat-files-v2.ts` (`uploadChatFile`
:123-308), `lib/files/get-file-associations.ts:33`,
`lib/files/overwrite-utils.ts:61`. The following are the v5-side edit
points this lane fills, RE-CONFIRMED on main 2026-07-14:

### Already on main (reuse / extend — do NOT re-port)

- `crates/quilltap-core/src/api/types.rs` — the P4.6ae variant block
  (`FilesList`/`FileMove`/`FilePromote`/`FileDelete`/`FilesFoldersList`/
  `FilesFolderCreate`/`FilesFolderRename`/`FilesFolderDelete`) at
  :1670-1745; the parallel handler-fn block at :1895-1900;
  `ChatFileUpload` variant at :1606; `ChatFileUploadInput` at :784-791.
  `CoreError` at :2077 (fields `kind`, `message`, `pepper_state`,
  `code`) — constructed ONLY via `Response::error` :1906 /
  `Response::error_coded` :1917 / `Response::locked` :1933 (three
  struct-literal sites, all in types.rs).
- `crates/quilltap-core/src/api/files.rs` (39 KB, already exists) — the
  general files READ/move/promote/delete/folders handlers. Extend it;
  do not rewrite.
- `crates/quilltap-core/src/api/chat_media.rs` — `chat_file_upload`
  :799-803 is STILL the refusal (`_input` unused); the engine arm
  already calls it (`engine.rs:2364-2388`). Replace the body only.
- `crates/quilltap-core/src/services/file_storage.rs` — the write seams
  (`write_user_upload_to_mount_store` :890, `write_project_file_to_
  mount_store` :979, `RealProjectImageUpload` :1027, `upload_raw`
  :592, `safe_filename` :98, `build_storage_key` :129). **`FileBytesStore`
  is READ-ONLY** — uploads use these write seams via `Db::write`, NOT a
  new EngineAssembly seam.
- `crates/quilltap-core/src/services/chat_files.rs` — only the READ half
  (`load_chat_files_for_llm` :227); the upload/ingest half is this
  lane's additive extension (module header :2-3 flags it as Phase-4
  work).
- `crates/quilltap-web/src/multipart.rs` (`FormData::from_request` :34,
  `file()` :59, `text()` :67, `all_text()` :77); the route mold is
  `mount_blobs_post` (`files_routes.rs:702-744`). Router:
  `lib.rs:build_router` :52, P4.6y legs at :67-75 (append here). The
  byte GETs (`files_proxy` :170, `files_get` :195) are FROZEN.

### v5 gaps this lane fills (the P4.6ae "v5 gaps" list, still open)

- `db/files.rs` MISSING: `find_by_user_id`, `find_general_files`,
  `find_by_filename_in_scope` (overwrite), `find_by_folder`,
  `find_by_filename_in_project`, `remove_link`. **`FileUpdate.project_id`
  cannot express SQL NULL** — use the `clear_project_id` escape hatch.
- `db/folders.rs` has only `create`/`update`; the cleanup/link legs may
  need `find_by_path`/`find_by_user_id`/`has_children` (add only what a
  ported verb needs; the general folders CRUD already landed).
- `folder_utils.rs` completions already landed for the folders CRUD;
  add only what the upload path needs (`normalize`/`derive`/`resolve`
  exist).

### Quirks the port must not miss (the P4.6ae quirk list applies; the write-specific ones)

1. **No size limit and no permission check** on the *collection* upload
   (v4's explicit comment, `upload.ts:48`) — do not "fix" this.
2. The chat-file conflict fields are **`resolution` +
   `conflictingFileId`** (NOT `conflictResolution`) — locked on both v4
   and the shipped SPA client (`chat-files.api.ts:42-81`).
3. `uploadChatFile`: 10 MB cap (size only); `category = image/* ?
   IMAGE : ATTACHMENT`; project-scope conflict detection (content dup
   via `findBySha256` project-filtered + filename dup via
   `findByFilenameInProject`); no-resolution → `{duplicate, conflictType
   (both→content→filename precedence), existingFile (prefers filename
   dup), newFile}`; `skip`→link existing, `replace`+conflictingFileId→
   delete old+upload, `keepBoth`→` (1)`/` (2)` suffix; non-project path
   → sha256 dedup extends `linkedTo` else upload; id `randomUUID` minted
   BEFORE create so metadata id == storage path.
4. `autoDescribeChatImageAttachment` (fire-and-forget image
   auto-describe on chat upload) — host seam; if unported, degrade
   loudly (a named no-op recorded in the status log; v4's
   fire-and-forget makes it behaviorally invisible to the route
   differential).
5. **Overwrite reuses the fileId** and returns 200 (vs 201 new);
   nullable-projectId scoping means a general file collides only with
   general files.
6. `fileDelete`'s associations arm distinguishes REAL associations from
   stale `linkedTo` (stale silently cleared, then delete proceeds);
   `dissociate` strips message attachments + character
   `defaultImageId`/`avatarOverrides` (`shared.ts:64`).
7. cleanup-orphans **rescues** referenced orphans (character-id OR
   sha256 match OR non-empty `linkedTo` → back to `'ok'`) before any
   delete/move; duplicates-vs-unique split; `move` relocates unique to
   `/orphans/`. cleanup-stale: stale = `storageKey` missing on disk (an
   fs-existence leg — enumerate, don't fixture).

## Tiered deliverables

**Tier 1 (must land):**

1. **The chat-file upload leg:** `uploadChatFile` ported additively in
   `services/chat_files.rs` (composed over `file_storage.rs` write
   seams via `Db::write`), the `chat_media.rs:799-809` body
   replacement, and the web-edge `POST /api/v1/chats/{id}/files`
   multipart route (the `mount_blobs_post` mold) + the JSON
   `action=link` leg — matching the LOCKED SPA client name-for-name.
2. **The general upload REST leg:** the `fileUpload` base64 core
   variant + `saveFileEntry` port + `POST /api/v1/files?action=upload`
   multipart route (fields `file`, `tags?` JSON string, `projectId?`,
   `folderPath?`) → `{data: FileEntry}` 201 new / 200 overwrite.
3. **The itemized delete envelope:** the additive `CoreError.associations`
   field (+ a `Response::error_with_associations` helper; the existing
   three helpers gain `associations: None`), the `FILE_HAS_ASSOCIATIONS`
   emit on the un-forced linked-file delete (`get_file_associations` is
   already ported + verified), and the `dissociate=true` arm.
4. **The maintenance verbs:** `filesGenerateThumbnails` (owned+resizable
   filter; count envelope), `filesCleanupStale` (`{dryRun?}`;
   DB-provable arm + enumerated fs leg), `filesCleanupOrphans`
   (`{mode, dryRun?}`; rescue/duplicate/unique partition, dry-run
   counts).
5. Fixture extension + the differential: extend
   `files-{main,mount}.db` for the upload/overwrite/associations/
   cleanup cases and (if the chat-file conflict arms need seeded
   duplicates) `courier-images-{main,mount}.db`; extend
   `files_routes_equivalence.rs` and regenerate BOTH dependent oracles
   (`files_routes` and, if extended, `courier_images_routes`) — lane A
   owns both families.

**Tier 2 (should land):**

6. The chat-file `action=attach-mount-file` leg stays deferred (the
   Librarian announcement walk — TIER 3, named). If time permits,
   confirm the `keepBoth` unique-filename suffix (` (1)`/` (2)`) and the
   non-project sha256-dedup `linkedTo` extension both diff green.

**Tier 3 (explicit deferrals — loud, enumerated):**

- `filesSync` — ALREADY refusal-armed on main (naming the unported
  `lib/file-storage/reconciliation` subsystem); leave it.
- `autoDescribeChatImageAttachment` — the fire-and-forget image
  auto-describe; if its tier-3 service isn't already ported, a named
  no-op recorded in the status log (behaviorally invisible to the route
  differential).
- The chat-file `action=attach-mount-file` announcement walk (the
  Librarian leg) — the P4.6ab bounded deferral continues, named.

## The differential requirement (restated)

Jest real-DB oracles driving v4's REAL route handlers over the shared
baked fixtures — the `groups_routes_equivalence` mold. Recipes:
`[[jest-real-db-oracle]]`, `[[p4.6k-groups-projects-server]]`, the
`/tmp` mirror + `--roots --watchman=false --testTimeout=120000`, Node
24; un-mock `character-vault-bridge`; the P4.6ab entry in
`status-log.md` records this exact family's oracle gotchas (emit
oracle-read bytes; add-tool-result returns the constructed object).
Route reads may omit null nullables while mutations echo them AS null
(the P4.6p pattern) — pin per-case, don't normalize. Multipart legs
diff at the `CoreRequest` layer plus one web-edge integration test per
leg (the P4.6m precedent). **Fix the port, not the diff.**

Case inventory floor (new cases only — the read/move/promote/folders
floor already landed): chat-file upload (non-project happy + sha256
dedup, project conflict → duplicate envelope, all three resolutions,
>10 MB refusal, no-file 400, `action=link`); general upload (201 new,
200 overwrite-reuses-id, invalid `tags` JSON, folder validation);
delete (FILE_HAS_ASSOCIATIONS envelope with itemized
characters+messages, force override, dissociate strips character+message
refs, stale-`linkedTo` clear-then-delete, other-user forbidden);
thumbnails (owned+resizable filter, cached-vs-generated counts);
cleanup-stale (dry-run counts, the enumerated fs leg); cleanup-orphans
(rescue arm, duplicate-vs-unique split, `move` relocation, dry-run).

## Fixtures

This lane EXTENDS the committed `files-{main,mount}.db` (its builder
`harness/oracle/fixtures/build-files-web-fixture.ts`) and MAY extend
the `courier-images-{main,mount}.db` family for the chat-file
upload-conflict cases — **lane A owns both** and regenerates their
dependent oracles in the same commit. The `files` fixture already
carries (from P4.6ae) the general-files rows, the folders tree, a
project + store, and user-B ownership rows; add the seeded duplicate
pairs / linked-into-chat-message+character-default rows the new arms
need if not already present. Real repo/service `create` calls only
(`[[project-store-fixture-needs-real-create]]`,
`[[photo-tools-differential-recipe]]` for the six-table blob dump).
All other fixture families are FROZEN. Synthetic test-pepper fixtures
ONLY — never real Friday data or the real pepper.

## Shared contract (BINDING — identical in p4.6ah / p4.6ai / p4.6aj / p4.d4)

### New / going-live Request variants (wire `type` names; Rust mirrors PascalCase)

**Lane A (files write + maintenance) — new variants + going-live legs:**

- `fileUpload {filename, contentType, data (base64), tags?, projectId?,
  folderPath?}` → `{data: FileEntry}` (behind the REST leg; 201 create
  / 200 overwrite). `FileEntry` is v4 `serializeFileEntry`: BOTH
  `originalFilename` AND `filename` (= originalFilename), `filepath` =
  `/api/v1/files/{id}`, `folderPath` RESOLVED via
  `resolveEffectiveFolderPath`, `fileStatus` defaulted `'ok'`.
- `chatFileUpload {chatId, filename, contentType, data (base64),
  resolution?, conflictingFileId?}` → `{file: {id, filename, filepath,
  mimeType, size, url}}` | `{duplicate: true, conflictType,
  existingFile, newFile}` — the `chat_media.rs:799-809` refusal body is
  REPLACED; the engine arm already exists.
- `chatFileLink {chatId, fileId}` → `{file}` (the JSON `action=link`
  leg; links an existing library file).
- `filesGenerateThumbnails {fileIds, size?}` → `{total, generated,
  cached, errors}`.
- `filesCleanupStale {dryRun?}` → `{total, stale, deleted, dryRun,
  staleFiles, errors?}`.
- `filesCleanupOrphans {mode, dryRun?}` → v4's dry/wet shapes.
- `fileDelete {fileId, force?, dissociate?}` (ALREADY landed, reads
  `force`/`dissociate` now) → `{success: true}` | the itemized 400
  envelope below.

**The `FILE_HAS_ASSOCIATIONS` envelope (lane A EMITS, lane C CONSUMES) —
the exact `CoreError` extension:**

`CoreError` gains ONE additive, optional, `skip_serializing_if =
"Option::is_none"` field:

```rust
/// v4's FILE_HAS_ASSOCIATIONS itemized payload (P4.6ah `fileDelete`):
/// the `getFileAssociations` result — present ONLY on the un-forced
/// linked-file delete refusal, absent everywhere else.
pub associations: Option<FileAssociations>,
```

with `FileAssociations { characters: Vec<FileAssocCharacter>, messages:
Vec<FileAssocMessage> }`, `FileAssocCharacter { id, name, usage }`,
`FileAssocMessage { chatId, chatName, messageId }`. Wire on the
refusal: `{error: 'File is linked to other items', code:
'FILE_HAS_ASSOCIATIONS', associations: {characters: [{id, name,
usage}], messages: [{chatId, chatName, messageId}]}}`. The three
existing `Response` helpers gain `associations: None`; a new
`Response::error_with_associations` sets it.

**REST legs (web edge, lane A):**

- `POST /api/v1/files?action=upload` multipart (fields `file`, `tags?`
  JSON string, `projectId?`, `folderPath?`) → `{data: FileEntry}`,
  status 201 create / 200 overwrite.
- `POST /api/v1/chats/{id}/files` multipart (fields `file`,
  `resolution?`, `conflictingFileId?`) → `{file}` | `{duplicate,
  conflictType, existingFile, newFile}` | `{error}`. **The SPA client
  is ALREADY LOCKED** (`chat-files.api.ts:42-81`, shipped P4.6ac) — the
  server matches it, never vice versa.
- `POST /api/v1/chats/{id}/files` with JSON `{action:'link', fileId}` →
  `{file}`.
- The byte GETs (`/api/v1/files/{id}`, `?action=thumbnail`, the proxy)
  are FROZEN — no lane touches them.

**Lane B (image generation) — going-live variant + new seam:**

- `imageProfileGenerate {imageProfileId, prompt, chatId?, count?}` →
  `{success: true, data: GeneratedImage[], expandedPrompt, metadata}`
  (v4 201). `GeneratedImage` carries `{id, filename, filepath,
  mimeType}` (the SPA reads these + `expandedPrompt`); `metadata` =
  `{originalPrompt, provider, model, count}` (SPA-unread, oracle-diffed
  — emit it). The engine arm currently drops `prompt`/`chatId`/`count`
  — thread them. `imageProfileValidateKey` / `imageProfileListModels`
  STAY refusal-armed.
- NEW `EngineAssembly.image_generation: Option<ErasedImageGeneration>`
  seam (mirror the `courier_resolve` idiom); wired LIVE in the host
  from the W4.7f `Real*Provider`s; spine-less assemblies keep the loud
  refusal.

**Lane C (SPA close-out) — consumes A + B, no server work:**

- Consumes the `FILE_HAS_ASSOCIATIONS` envelope (A's shape) in the
  delete flow → an itemized dialog with force/dissociate choices
  (replacing the current plain-alert fallback).
- The composer-attach live path (`chatFileUpload`) and the general
  files data beat (`fileUpload` REST) self-activate at unification.
- The generate dialog already sends `{type:'imageProfileGenerate',
  imageProfileId, prompt, chatId, count: 1}` and reads
  `{success, data:[{id,filename,filepath,mimeType}], expandedPrompt}`
  (`generate-image-dialog.ts:158-174`) — lane C verifies the live path,
  no new client shape.

**Lane D (skip-signal drift) — no contract changes.** File-disjoint;
not a core-dispatch writer.

### The two-core-dispatch-writer rule (lanes A + B)

Lanes A and B are this round's ONLY writers to `types.rs` / `engine.rs`
/ `api/mod.rs`, each appending ONLY inside its own clearly-delimited
block (`// === P4.6ah === … === end P4.6ah ===` / `// === P4.6ai === …
=== end P4.6ai ===` — the P4.6v/w precedent). The unifier keeps both
blocks; neither lane edits inside the other's block or reorders
existing arms. **The one shared in-place edit is lane A's**: the
`CoreError` struct gains its `associations` field and the three
`Response` helpers gain `associations: None` — lane B and lane D
construct errors ONLY through those helpers, so they are unaffected
(they never edit the struct or the helpers). **Lane B's one in-place
edit** is the existing `imageProfileGenerate` engine arm
(`engine.rs:1438-1443`, currently `..`-dropping the params) + adding
the `EngineAssembly.image_generation` struct field inside its delimited
region — an arm/field no other lane touches. Elsewhere the two lanes
are file-disjoint per their Ownership entries.

### core-contract.ts ownership (single-author rule)

Lane C OWNS `apps/web/src/app/core/core-contract.ts` +
`core-client.ts` and is the SOLE author of any files-family /
image-generation client additions there (most already ship; lane C
adds the `FileAssociations` type + wires the delete-flow consumption).
Lanes A, B, D do NOT touch either file.

### Name-collision warning

`apps/web/src/app/files/**` is the SCRIPTORIUM `qt-file-manager`
(P4.6aa) — NOT this round's general browser. Lane C's screens live in
`apps/web/src/app/screens/files/**`. Do not confuse the two; nobody
edits the file manager this round.

If any name or field must change, ALL FOUR orders change and the change
is reported at unification.

## Ownership (BINDING across all four lanes)

- **Lane A (p4.6ah):** `crates/**` per this list — `api/files.rs`
  (extend), `services/chat_files.rs` (upload port), `services/
  file_storage.rs` (write-seam additions ONLY if a ported verb needs
  one), `db/files.rs` + `db/folders.rs` (the missing methods each
  ported verb needs, carried *why*-comments intact), `folder_utils.rs`
  (upload-path completions only), `chat_media.rs` (:799-809 refusal →
  body), `api/types.rs` (the `CoreError.associations` field + the
  `Response` helper edits + its OWN `// === P4.6ah ===` delimited
  variant/DTO block), `api/engine.rs` (its OWN `// === P4.6ah ===`
  delimited arm block: the `fileUpload`/`chatFileLink`/maintenance
  arms), `api/mod.rs` (P4.6ah appends); `quilltap-web` —
  `files_routes.rs` (the new POST upload + DELETE-associations + the
  chat-files multipart/link legs; the byte GETs FROZEN) + `lib.rs`
  router appends; `harness/oracle/fixtures/build-files-web-fixture.ts`
  + the committed `files-{main,mount}.db` (+ any
  `courier-images-{main,mount}.db` extension and its dependent oracle
  regen — lane A owns that family); `harness/oracle/cases/files-routes*`
  + the upload/delete/maintenance case files;
  `crates/quilltap-harness/tests/files_routes_equivalence.rs`. **Bumps
  `quilltap-core` + `quilltap-web` + `quilltap-harness`.**
- **Lane B (p4.6ai):** `crates/quilltap-core` — `api/image_profiles.rs`
  (the un-refusal), `api/types.rs` (its OWN `// === P4.6ai ===` block
  ONLY IF a new type is needed — the `imageProfileGenerate` params
  already exist), `api/engine.rs` (the `EngineAssembly.image_generation`
  field inside its `// === P4.6ai ===` region + the in-place
  `imageProfileGenerate` arm param-threading), `tools/generate_image.rs`
  (GLUE only — call the W4.9a runner, never rebuild it), `api/mod.rs`
  (P4.6ai appends if any); `quilltap-host/src/host.rs` (the seam
  wiring, mirroring courier/save-image at :459-460/:472-473/:574-575);
  `crates/quilltap-harness/tests/` — the generate-arm differential + its
  oracle case. **Bumps `quilltap-core` + `quilltap-host` +
  `quilltap-harness`.**
- **Lane C (p4.6aj):** `apps/web` ONLY — `core/core-contract.ts` +
  `core/core-client.ts` (owner; the `FileAssociations` type + delete
  consumption), the delete-associations dialog + its host screen under
  `screens/files/**` (or wherever the general-files delete lives, NOT
  `app/files/**`), the composer-attach + generate-dialog live-path
  verification, NEW/extended e2e beats (sorting AFTER `foundation.spec.
  ts`), `e2e/support/**` additions if seeding needs them. **Bumps the
  SPA.**
- **Lane D (p4.d4):** `crates/quilltap-core/src/skip_signal.rs`;
  `crates/quilltap-harness/tests/skip_signal_equivalence.rs` (assert
  additions if any) + `harness/oracle/cases/skip-signal.ts` (new
  `detect` rows); OPTIONALLY the orchestrator/enclave tier-3 regen if it
  adds a trailing-sentinel case. **Bumps `quilltap-core` +
  `quilltap-harness`.**

Round-wide HANDS OFF: `apps/web/src/app/chat/render/**` (the pinned
unified-11.0.5 render pipeline), `apps/web/src/app/files/**` (the
Scriptorium file manager), the byte-GET routes in `files_routes.rs`,
`api/salon.rs`, and the frozen mount/courier/autonomous dispatch
surfaces.

Union-merged at unification: CHANGELOG, `status-log.md`, the
`api/mod.rs` + web-router appends, the two lane-delimited blocks in
`types.rs`/`engine.rs`, `package.json` (version). The oracle-baseline
line in CLAUDE.md moves to `02865bdb` when **lane D** unifies (it is the
drift catch-up); accumulate the multi-lane core + harness + SPA version
bumps per `[[parallel-round-reconciliation]]` (verify version deltas
whole-file). CLAUDE.md Status is round-completion-only.

## Verification gate (run before declaring done)

- `cargo fmt --all` clean.
- `cargo clippy --workspace --all-targets -- -D warnings` on the
  **default** feature set AND `--features
  quilltap-core/native-transport` — both clean.
- `cargo test --workspace` green, including `files_routes_equivalence`
  AND (if extended) `courier_images_routes_equivalence`, each verified
  BY NAME against FRESH v4 oracles generated at `02865bdb` (run the
  differentials `--nocapture` and grep for SKIP to prove no oracle env
  var is unset — `[[p4d3-db-size-reduction-drift]]`).
- `ng test` + `ng build` still green (this lane must not break the SPA
  even though it doesn't edit it — the contract reconcile happens at
  unification).
- Full Playwright: no NEW failures (lane C activates the guarded beats
  at unification).
- Commit checklist per `.claude/commands/commit.md` on every commit
  (CHANGELOG entry, version bumps, full-workspace lint).

## Ground rules

Differential discipline (fix the port, not the diff); no stubs/TODO
beyond the loud enumerated deferrals; schema/cipher frozen; wrap the
frozen services — never refactor a differential-proven module
mid-round; keep v4's steampunk register on user-facing strings;
synthetic test-pepper fixtures only — never real Friday data or the
real pepper; if a unit's fresh v4 survey contradicts this order, STOP
and report before proceeding.
