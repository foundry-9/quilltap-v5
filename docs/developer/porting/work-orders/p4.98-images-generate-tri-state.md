# Work order P4.98 — `Request::ImagesGenerate`'s five body keys as the `Option<Option<Value>>` tri-state (P4.96's §11 finding, confirmed at the `bcd7e4852` review): the collection generate route's `prompt` / `profileId` / `chatId` / `tags` / `options` stop collapsing an explicit JSON `null` into ABSENT on dispatch and Tauri IPC, the engine arm collapses the tri-state, the REST edge mirrors it, a NEW dispatch WIRE test proves v4's `Validation error` envelope on every null arm, the census gains the sibling raw list, and `image_profiles.rs`'s fourth hand-copy of the Zod type renderer retires onto `settings::zod_parsed_type` — from `main`

**Status: CLOSED WHOLE by the lane (2026-09-18)** — every Tier-1 item (1–8)
and both Tier-2 items (9, 10) landed; the Tier-3 deferrals are named in the
lane record. Lane branch `claude/p4-98-images-tri-state-8f131b` from `main`
`5018df46`; three commits — `bd0679f0` (the route family's `zod_prompt_null`
row), `470e828e` (the tri-state across all three transports, the census, the
new dispatch wire test), `6a1a5aa9` (the Zod type-renderer DRY). Versions:
core 0.0.952, harness 0.0.843, web 0.0.153. The unifier updates this header
at unification.

**What the lane measured that the order did not have:**

- **The §R.2 probe FAILED at lane start** — the ledger's recorded thirteen
  dirty paths had grown to seventeen, including a `help/` page, because v4's
  in-flight fix had become bug 154. The lane STOPPED and reported, as ordered,
  and resumed after the human's `/driftcheck` recorded `baa85e19b` as a third
  drift row. **§R.2's paragraph above still names `89fcc3c0d` as the expected
  HEAD and was NOT amended by the lane** (a lane writes neither the ledger nor
  its own §R); the probe of record is the ledger's, which the lane re-ran and
  passed.
- **The two required keys' null arms are GREEN pre-fix, and the order's
  conditional is resolved:** this route drops v4's `details` array (the
  standing `drop_zod_details` deferral), and that array is the only place
  `received null` and `received undefined` differ. v4 DOES differ — measured
  at the pin, quoted in the lane record and in the wire test's header. Three
  arms were red, not five: `chatId`, `tags`, `options`, each answering
  `Connection profile not found` because the null had collapsed to ABSENT and
  v4's parse stage had been SATISFIED.
- **Item 3 landed the PREFERRED shared-decoder shape**, and `Request` already
  derives `PartialEq`, so the `==` assertion needed no new derive.
- **Item 4's "dead socket" is unnecessary on this route** and was not used:
  v4 parses the body BEFORE reading the profile here (the opposite order from
  the profile-id route's 404 gate), so a valid-but-absent `profileId` is a
  complete discriminator — an exact second sentence, with no provider ever
  constructed and no spend possible.
- **The census moved 441 → 441**, recorded before the test was run.
- **The handler did NOT change**, as the order predicted.
- **M1 surfaced a gap in P4.96's sibling list**: a decode-raw test does NOT
  catch a plain-`Option<Value>` regression (a plain `Option` decodes `null`
  fine, it just decodes it wrong). This lane added a distinctness test for its
  own list; `IMAGE_PROFILE_GENERATE_RAW_FIVE` still lacks one — named for the
  unifier, not touched.

## Preamble

- **v4 oracle baseline: `bcd7e4852`** (drift-ledger §1). This is a
  MAINTENANCE lane: its v4 surface (`app/api/v1/images/route.ts` and
  `lib/api/responses.ts`) is untouched by the two commits past the baseline
  (`git diff --stat bcd7e4852..89fcc3c0d -- app/api/v1/images/ lib/api/`
  EMPTY, measured at planning 2026-09-18 — re-record it in the lane record
  before the first regen). Spec reads at `bcd7e4852`: `git show bcd7e4852:
  app/api/v1/images/route.ts` (`generateImageSchema` at `:45-73`;
  `handleGenerateImage` at `:189-`; `generateImageSchema.parse(body)` at
  `:191` — the parse is the FIRST thing the handler does, before the
  profile read) and `git show bcd7e4852:lib/api/responses.ts` (`:101-115`,
  the `validationError` renderer — measure whether the body carries a
  `details` array and what a `received null` issue renders as; the route
  family's existing `zod_*_null` rows are the byte oracle).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** Pin **`/tmp/qt-v4-pin-p4-98-
  bcd7e4852`** (the BASELINE) for `images_generate_route_equivalence`
  (the one family this lane may grow by a row) and for the by-name re-run
  of `images_routes_equivalence` (untouched — a neutrality re-run only).
  ⚠ Both families stage the SAME `/tmp/qt-imgcol-{main,mount}.db` copy of
  the committed `crates/quilltap-web/tests/fixtures/images-{main,mount}.db`
  pair and read `harness/oracle/fixtures/images-collection.json` — the
  `53294163f` round's recorded collision class (§R.10(d)): stage under
  `/tmp/p4.98/` by pointing `QT_FIXTURE_IMGCOL_MAIN`/`_MOUNT`/`QT_ORACLE_
  OUT` there, one family at a time, and leave the committed recipe headers
  canonical.
- **Sibling lanes:** P4.D200 (bugs 152/153 — `doc_edit/**`, `tools/doc_
  edit/**`, `db/tiered_mount_pool.rs`, `help/**`; you touch none), P4.99
  (`services/primary_stream.rs`, `model/request_builder/google.rs`, the
  google corpora, `crates/quilltap-harness/src/lib.rs`; you touch none).
  **`api/types.rs`, `api/engine.rs`, `api/images.rs`, `api/image_
  profiles.rs` and the dispatch wrong-type census are yours ALONE this
  round** (§R.10(a)) — the unifier asserts the other two lanes' diffs
  there are empty.
- **Worktree:** `git worktree add <path> -b claude/p4-98-images-generate-
  tri-state-<hash> main`; `CARGO_INCREMENTAL=0`. No SPA work (§S.1 — the
  SPA never posts to this verb; measured at planning: `apps/web/src/app/
  core/core-contract.ts` carries no `imagesGenerate` request type, and the
  only `?action=generate` client is the image-PROFILE dialog at `:4990`).
- **Read before the work:** `crates/quilltap-core/src/api/types.rs`
  `:3560-3584` (the `ImagesGenerate` variant — five `#[serde(default)]
  Option<serde_json::Value>` fields, the `chat_id` doc naming bug 130 and
  "Crosses RAW like its four siblings; the handler Zod-parses it"), `:1062`
  (the paragraph that cites the "`ChatCreate`-trio / `ImagesGenerate`
  rule" — after this lane the two are the SAME shape; correct the
  paragraph), `:1088-1104` (the `ImageProfileGenerate` variant — the
  `double_option` tri-state P4.96 landed and the `bcd7e4852` review
  finished; THIS is the shape to copy, field for field) and the
  `double_option` helper wherever it lives in the file (grep `fn
  double_option`); `crates/quilltap-core/src/api/engine.rs:5218-5241`
  (the `ImagesGenerate` arm passing `.as_ref()` on each field — after
  the change the arm collapses `v.map(|inner| inner.unwrap_or(Value::
  Null))` FIRST, exactly as the `ImageProfileGenerate` arm at `:2928-2951`
  does; copy that arm's idiom) ; `crates/quilltap-core/src/api/images.rs`
  `:1381-1470` (`parse_generate_body` — `prompt`/`profileId` REQUIRED via
  `and_then(Value::as_str)` so a `Some(Null)` already refuses; `chatId`/
  `tags`/`options` `.optional()`-shaped `match { None => None, Some(v) =>
  … }` so a `Some(Null)` already refuses — the handler is CORRECT today
  and the collapse is entirely at the variant; verify by reading, and
  expect NO handler change), `:1540-1560` (`images_generate` — the
  `track_activity` wrap; the signature takes `Option<&Value>` × 5 — keep
  it, the engine arm is what changes); `crates/quilltap-web/src/images_
  routes.rs:243-281` (the REST edge `images_generate` — hand-builds the
  variant from `map.get(..).cloned()`, so an explicit `null` ALREADY
  crosses as `Some(Value::Null)` there; after the variant changes it must
  build `Some(Some(v))` / `Some(None)` / `None` to mirror `double_option`
  EXACTLY — read the helper before deciding which of `null` → `Some(None)`
  vs `Some(Some(Null))` it produces, and mirror THAT); `crates/quilltap-
  web/tests/images_edge_routes.rs:151-195` (the P4.76 generate leg's
  three arms — an empty body, a 404-shaped body, the census pointer);
  `crates/quilltap-web/tests/image_profile_generate_dispatch_wire.rs`
  WHOLE (the idiom this lane copies: `mod common`, `ProductionSpineFactory`
  booted — `web-test-venue-has-no-spine-factory` — a seeded profile at a
  DEAD SOCKET so the one body that passes validation fails downstream
  with no spend, which is the discriminator; the `chatId` arms the
  review added); `crates/quilltap-web/tests/dispatch_wrong_type_census.rs`
  `:2530-2560` (`EXCLUDED_BY_THE_ROUTE_IDENTIFIER_RULE = 441` + the
  running ledger comment), `:2757-2840` (`IMAGE_PROFILE_GENERATE_RAW_
  FIVE` — the raw sibling list with the per-row `note` prose and the
  explanation of WHY a `Value`-carrying row is not a `CENSUS` row),
  `:2926` (`the_raw_five_list_covers_every_raw_field_on_the_variant`),
  `:2951` (`image_profile_generate_five_decode_raw_so_the_handler_can_
  refuse`), `:2686` (the `ChatCreate` trio's test — the FIRST instance of
  the shape); `crates/quilltap-harness/tests/images_generate_route_
  equivalence.rs` header + `harness/oracle/cases/images-generate-route.
  test.ts:314-535` (the case list: `zod_profile_null` `:482`, `zod_
  options_null` `:523`, `zod_tags_null` `:525`, `generate_chat_id_null`
  `:374` exist; there is NO `zod_prompt_null` row — `grep -c prompt_null`
  = 0 at planning); the memory note `a-plain-option-value-eats-an-
  explicit-null` (the rule, the collapse, the known live instance = this
  variant); `crates/quilltap-core/src/api/image_profiles.rs:676`
  (`generate_parsed_type`) vs `api/settings.rs:665` (`pub(crate) fn
  zod_parsed_type`) — the P4.96 §11 byte-identical twin (Tier 2).

## Mandate

Close the one known live instance of the `a-plain-option-value-eats-an-
explicit-null` class: make `Request::ImagesGenerate`'s five body keys the
same `#[serde(default, deserialize_with = "double_option")] Option<Option<
serde_json::Value>>` tri-state the `ChatCreate` trio and `ImageProfile
Generate` already carry, collapse it in the engine arm so absent stays
absent and an explicit `null` reaches the handler as `Value::Null`, mirror
the tri-state at the REST edge's hand-build so all three transports agree,
prove it at the dispatch WIRE (the only instrument that sees a serde
collapse — the handler-direct family is green on both sides today), grow
the route family by the one null row it lacks so every null arm's envelope
bytes are MEASURED against v4's real route rather than assumed, add the
census's sibling raw list with its coverage test, and retire `image_
profiles.rs`'s hand-copy of the Zod type renderer onto the shared one. This
lane does NOT touch the handler's parse logic unless a measurement says it
must (say so), does NOT add a REST route or an SPA type, does NOT touch
`api/*.rs` beyond the four named files, and does NOT touch the drift
ledger.

## Survey-verified starting points (fresh survey 2026-09-18 at `bcd7e4852` and on `main` `2d720c04` — do not re-derive)

### v4 (at `bcd7e4852`)

- **`app/api/v1/images/route.ts`** — `generateImageSchema` (`:45-73`):
  `prompt: z.string().min(1).max(4000)`, `profileId: z.uuid()`, `chatId:
  z.uuid().optional()` (bug 130's doc at `:48-55`), `tags: z.array(z.
  object({ tagType: z.enum(['CHARACTER','CHAT','THEME']), tagId: z.string()
  })).optional()`, `options: z.object({ n: z.int().min(1).max(10).
  optional(), size: z.string().optional(), quality: imageQualitySchema.
  optional(), style: z.enum(['vivid','natural']).optional(), aspectRatio:
  z.string().optional() }).optional()`. Every `.optional()` is NOT
  `.nullable()`: an explicit `null` on `chatId`/`tags`/`options` is an
  `invalid_type` refusal; `prompt: null` / `profileId: null` refuse as
  `invalid_type … received null` (vs `received undefined` when ABSENT —
  if the envelope carries `details`, those two words are the byte
  difference between "present-null" and "absent", and therefore between
  the pre-fix and post-fix dispatch answers for the two required keys).
  `handleGenerateImage` (`:189-`): `const body = await request.json();
  const { … } = generateImageSchema.parse(body);` FIRST — before
  `repos.connections.findById(profileId)` (`:194`, a miss → `badRequest
  ('Connection profile not found')`, a 400 not a 404 on THIS route). The
  `POST` dispatcher: `action === 'generate'` → `trackActivity('image',
  handleGenerateImage)` (`:177-178`); anything else → upload/import.
- **`lib/api/responses.ts:101-115`** — the Zod refusal renderer (`error:
  'Validation error'` + whatever it puts beside it — MEASURE from the
  existing oracle rows rather than from this paragraph).

### v5 (on `main` `2d720c04`)

- **The variant** (`api/types.rs:3568-3583`): `prompt`, `profile_id`,
  `chat_id`, `tags`, `options` — each `#[serde(default)] Option<serde_json
  ::Value>` (camelCase on the wire via the enum's `rename_all`). `double_
  option` already exists in the file (used at `:1092-1103` by
  `ImageProfileGenerate`, and by the `ChatCreate` trio).
- **The engine arm** (`engine.rs:5218-5241`): `prompt.as_ref(), profile_
  id.as_ref(), chat_id.as_ref(), tags.as_ref(), options.as_ref()` into
  `images_generate(&db, &seams, SINGLE_USER_ID, now_unix_ms(), …)`.
- **The handler** (`images.rs:1381-1470` `parse_generate_body`): already
  refuses `Some(Value::Null)` on all five (required keys through `and_then
  (Value::as_str)`, optional keys through the `match Some(v)` arms) —
  which is why `images_generate_route_equivalence`'s `zod_profile_null` /
  `zod_options_null` / `zod_tags_null` / `generate_chat_id_null` rows are
  GREEN on both sides today: the family calls the handler with `Some(&
  Value::Null)` directly and never crosses serde.
- **The REST edge** (`quilltap-web/src/images_routes.rs:243-281`): builds
  `CoreRequest::ImagesGenerate { prompt: map.get("prompt").cloned(), … }`
  — `null` survives as `Some(Value::Null)` here, which is exactly why the
  edge test's null behaviour is v4-faithful while dispatch's is not. After
  the variant change this construction must mirror `double_option`'s
  output shape for `null` (read the helper; do not guess).
- **The dispatch path** (`quilltap-web/src/dispatch.rs:69` `serde_json::
  from_slice::<Request>`) and Tauri IPC (`crates/quilltap-tauri` — the same
  `Request` decode) both go through serde: `{"chatId": null}` → `None`
  today → the handler sees ABSENT → v4 400s, v5 proceeds. The measured
  live consequence: a `chatId: null` posted over dispatch generates and
  SAVES an image v4 refuses (the P4.96 §11 record).
- **The census** (`quilltap-web/tests/dispatch_wrong_type_census.rs`):
  `EXCLUDED_BY_THE_ROUTE_IDENTIFIER_RULE = 441` (`:2544`) — the five
  `ImagesGenerate` fields are ALREADY `Value`-typed, so `typed_request_
  fields` skips them before the `*_id` rule sees `profile_id`/`chat_id`;
  expect **441 → 441** and RECORD the expectation in the lane record
  before running `census_covers_every_typed_request_field`. The raw
  sibling list `IMAGE_PROFILE_GENERATE_RAW_FIVE` (`:2757-2840`) + its two
  tests (`:2926`, `:2951`) are the shape to duplicate as `IMAGES_GENERATE_
  RAW_FIVE` (or to GENERALIZE into one list keyed by variant — say which;
  a generalization that keeps every existing row byte-identical is
  preferred).
- **The wire test idiom** (`quilltap-web/tests/image_profile_generate_
  dispatch_wire.rs`): boots `ProductionSpineFactory`, seeds a profile by
  SQL into the materialized instance's main partition, posts to `POST
  /api/dispatch`, asserts v4's 400 ENVELOPE vs a serde sentence, and uses
  a dead-socket profile so the well-formed body fails downstream (not
  400) as the discriminator. THIS route reads a CONNECTION profile
  (`connection_profiles::find_by_id`) with image capability — seed one at
  a dead socket (the images fixture pair under `crates/quilltap-web/tests/
  fixtures/` may already carry one; measure — `images_generate_route_
  equivalence` uses `PROFILE_MAIN` from `images-collection.json`).
- **The route family** (`crates/quilltap-harness/tests/images_generate_
  route_equivalence.rs` + `harness/oracle/cases/images-generate-route.
  test.ts`): 37+ cases over a fresh copy of the committed images pair per
  case; coverage asserted by SHAPE (every oracle case name driven and vice
  versa). The null rows present: `zod_profile_null` (`:482`), `zod_options_
  null` (`:523`), `zod_tags_null` (`:525`), `generate_chat_id_null`
  (`:374`). Missing: `zod_prompt_null`.
- **The Zod renderer twins:** `api/image_profiles.rs:676 fn generate_
  parsed_type(v: Option<&Value>) -> &'static str` is `api/settings.rs:665
  pub(crate) fn zod_parsed_type` byte for byte (the `bcd7e4852` review's
  §3 note on P4.96). `run_sql.rs:219 zod_issue` and `settings.rs:680
  zod_error_message` are a DIFFERENT layer (the issue renderer) — NOT
  this lane's (Tier 3).

## Tiered deliverables

### Tier 1 — must land

1. **The variant** (`api/types.rs`): the five fields become `#[serde(
   default, deserialize_with = "double_option")] Option<Option<serde_json
   ::Value>>`, field for field the `ImageProfileGenerate` shape; the
   variant's doc says why (the memory note's rule, in the file's own
   words: absent vs present-null must survive the crossing because v4's
   `.optional()` refuses the second); the `:1062` paragraph corrected so
   it no longer implies the two rules differ.
2. **The engine arm** (`engine.rs:5218-5241`): collapse each field
   `v.map(|inner| inner.unwrap_or(Value::Null))` into a local, then pass
   `.as_ref()` — the `ImageProfileGenerate` arm's idiom, copied. The
   handler signature is UNCHANGED (`Option<&Value>` × 5).
3. **The REST edge** (`images_routes.rs` `images_generate`): the
   hand-build mirrors `double_option`'s output for the three cases
   (absent / `null` / value) EXACTLY — measured by a unit assertion that
   `serde_json::from_value::<Request>(json!({"type":"imagesGenerate",
   "chatId": null, …}))` and the edge's construction are `==` (derive
   `PartialEq` on nothing new — compare the collapsed values the engine
   arm would see, or route the edge THROUGH `serde_json::from_value` on a
   built `Value` so the two transports share one decoder; say which — the
   shared-decoder shape removes the class for good and is preferred if
   the edge's other fields permit it).
4. **The NEW dispatch WIRE test** `crates/quilltap-web/tests/images_
   generate_dispatch_wire.rs` (the P4.96 idiom): FIVE null arms — `{"type":
   "imagesGenerate", …well-formed…, "<key>": null}` for each of the five
   keys, each asserting v4's 400 envelope BYTES (taken from the route
   family's oracle rows for that key — `zod_prompt_null` is item 5's new
   row); FIVE wrong-type arms (`prompt: 42`, `profileId: 7`, `chatId: []`,
   `tags: "nope"`, `options: 7`) asserting the same envelope class (these
   are ALREADY raw and green — they pin the class against a future
   re-typing); ONE well-formed body reaching the handler and failing at
   the dead socket (NOT 400 — the discriminator that the gate let it
   through). **RED-FIRST**: before item 1, record which null arms are red
   — expect `chatId`/`tags`/`options` red (v5 proceeds to the provider
   where v4 400s) and `prompt`/`profileId` red ONLY if the envelope's
   `details` distinguish `received null` from `received undefined`
   (measure; if the envelope carries no `details`, those two arms are
   green pre-fix and the record says so — they still pin the class).
5. **The route family grown by `zod_prompt_null`** on BOTH sides (`{
   profileId: PROFILE_MAIN, prompt: null }` → v4's envelope; the shape
   assert grows with it), regenerated at the baseline pin under `/tmp/
   p4.98/`, green — this row is what makes the wire test's `prompt` arm
   a MEASURED byte comparison rather than a paraphrase.
6. **The REST-edge null arm** in `images_edge_routes.rs`: `?action=
   generate` + `{prompt, profileId, chatId: null}` → v4's 400 envelope
   (green before AND after — the edge already preserves null; it pins
   that the edge rewrite in item 3 did not regress it).
7. **The census**: `IMAGES_GENERATE_RAW_FIVE` (or the generalized list)
   with a `note` per row citing `route.ts:45-73`'s Zod line and the
   `bcd7e4852`-review §11 record; `the_raw_five_list_covers_every_raw_
   field_on_the_variant` extended (or generalized) to walk this variant
   too; `images_generate_five_decode_raw_so_the_handler_can_refuse`
   mirroring `:2951`; the ledger comment's next line stating **441 → 441**
   with the arithmetic (five `Value`-typed fields never reached the walk).
8. **Mutation proofs**: M1 ONE field back to plain `Option<Value>` → that
   field's null arm in the wire test red (201-or-provider vs 400) and the
   census coverage test red; M2 the engine arm's collapse dropped for one
   field (passing `None` for `Some(None)`) → that null arm red; M3 the
   REST edge building `Some(Some(Null))` where `double_option` says
   otherwise (or vice versa) → item 3's `==` assertion red (and, if the
   two shapes collapse differently, the edge null arm red); M4 the new
   `zod_prompt_null` row's expected status flipped → the family red on
   exactly that row.

### Tier 2 — should land

9. **The Zod type-renderer DRY** (the `bcd7e4852` review's §3 note on
   P4.96): retire `api/image_profiles.rs:676 generate_parsed_type` onto
   `settings::zod_parsed_type` after asserting the two bodies are
   byte-identical (`diff <(sed -n …) <(sed -n …)` in the lane record);
   behaviour-neutral — `image_generate_route_equivalence` re-run by name
   at the baseline pin, zero row change (⚠ it shares the `/tmp/qt-imggen-*`
   pair with `image_generation_tier3`; stage privately).
10. **The Tauri IPC leg** RECORDED, not tested: `crates/quilltap-tauri`
    decodes the same `Request` through serde, so the dispatch wire test is
    its proof by construction — say so in the lane record with the decode
    site's path:line, and do NOT add a Tauri test venue (none exists;
    inventing one is out of scope).

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **The issue-renderer DRY** (`run_sql.rs:219 zod_issue`, `settings.rs:680
  zod_error_message`, `generators_wizard.rs`'s twin — "one `zod_issues`
  home"): a cross-module refactor of a different layer; NOT this lane.
  Name it in the lane record as the remaining DRY candidate.
- **`count`'s unsafe-integer edge** (`|n| > 2^53` → v5 `invalid_type`, Zod
  `too_big` + a second issue) on the PROFILE route — recorded by P4.96,
  not this route's; do not chase it here.
- **A REST-edge shared decoder for EVERY hand-built edge** (the class
  item 3 may open): if item 3 lands the shared-decoder shape, record the
  other hand-building edges (grep `CoreRequest::` constructions in
  `quilltap-web/src/*_routes.rs`) as candidates — do not convert them.

## The differential requirement, restated

Every ported unit ships with an equivalence test driving v4's REAL code
(§R.6). Here the fidelity oracle is `images_generate_route_equivalence`
(v4's real `handleGenerateImage` over the committed images pair, tier 3
with the provider and classifier mocked below the route) — grown by the one
null row it lacks so every envelope the wire test asserts is a measured
byte string; the WIRE test itself is the P4.96-class dispatch pin that the
differential structurally cannot be (`a-dispatch-wire-test-sees-what-the-
differential-cannot`). The closest recipes: `image_profile_generate_
dispatch_wire.rs` (the wire pin), `images_generate_route_equivalence.rs`
(the family's staging + the shape assert), the P4.96 lane record in
`status-log.md` (the first run's refutation of a plain `Option<Value>`).

## Fixtures

- **Consumed (READ ONLY, staged under `/tmp/p4.98/`):** `crates/quilltap-
  web/tests/fixtures/images-{main,mount}.db` (+ `.meta.json`) and
  `harness/oracle/fixtures/images-collection.json`; the web test venue's
  materialized instance (`mod common`).
- **Delivered:** none new. The route family's oracle regenerated at the
  baseline pin with the new row.
- **Regen ownership:** this lane regenerates `images_generate_route_
  equivalence`; re-runs `images_routes_equivalence` and `image_generate_
  route_equivalence` by name (neutrality) on private staging; no other
  lane reads these fixtures.

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `89fcc3c0d` opacity-covenant drift catch-up + follow-ups round:
**P4.D200 ∥ P4.98 ∥ P4.99**, ordered 2026-09-18. Three lanes, three
worktrees, three lane branches, ALL from `main` (no stacking — every lane's
ownership is disjoint by file; there is no cross-lane constant and no shared
contract this round, §S). The unifier cherry-picks in the order
**P4.D200 → P4.98 → P4.99** (the drift lane first, then the two maintenance
lanes), gates ONCE, and `merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`bcd7e4852`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`89fcc3c0d`** ("fix
  (scriptorium): a hidden vault is not listed either (bug 153)",
  `4.10.0-dev.47`, 2026-09-17 22:37) — TWO commits past the baseline, both
  PORT on ported surfaces and both on the doc-edit opacity covenant:
  `1065a1f53` (bug 152, 22:23, `4.10.0-dev.46`) — `lib/doc-edit/path-
  resolver.ts` (+79), `lib/mount-index/tiered-mount-pool.ts` (+18),
  `lib/tools/handlers/doc-edit/shared.ts` (+20), `help/character-system-
  transparency.md` (+4), the 269-line `lib/doc-edit/__tests__/path-
  resolver-opacity-group-stores.test.ts`; and `89fcc3c0d` (bug 153) —
  `lib/doc-edit/path-resolver.ts` (+31), `lib/doc-edit/index.ts` (+2),
  `lib/tools/handlers/doc-edit/text-handlers.ts` (+22), `blob-handlers.ts`
  (+20), the same help page (+2), the 345-line `path-resolver-opacity-
  enumeration.test.ts`; plus README/CHANGELOG/bugs.md/`bugs/fixed/` and
  version stamps on both. The ledger's two §3 rows are `ORDERED(P4.D200)`.
  v4 `bugfix` tip `1a2b2164c` and `release` tip `8fbf2afe0` are unmoved;
  there is still no `release: 4.10.0` squash. The checkout sat on `main`,
  CLEAN, at the planning-START probe (2026-09-18 — the §2 probe PASSED,
  so the ledger stood and nothing was re-derived); **at the planning-CLOSE
  probe HEAD was unmoved and both logs empty but the tree had gone DIRTY on
  thirteen paths of a v4 fix in flight** (a characters default-system-
  prompt lockstep — `lib/characters/default-system-prompt.ts` NEW + the
  characters repository, the chat initializer, one route handler, the voice
  preview action, five client files, three tests; the exact list is in the
  ledger's §1). None of this round's regen surfaces is among them, and every
  regen runs from a pin that cannot carry dirt. **At unification the baseline
  MOVES to `89fcc3c0d`**; both rows retire `ABSORBED(P4.D200)` with their
  non-lib files ratified on P4.D200's list.
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log 89fcc3c0d..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Pass = branch `main`, HEAD `89fcc3c0d`, both logs EMPTY, and a
  tree that is either CLEAN or dirty ONLY within the thirteen paths the
  ledger's §1 records from the planning-close probe** (the in-flight v4
  default-system-prompt fix — compare `status --short` against that list
  path by path). Any mismatch — a moved HEAD (the fix landing is a NEW
  drift row, not a pass), a non-empty log, ANY dirty path outside the
  recorded set (`lib/`, `app/`, `components/`, `packages/`, `plugins/`,
  `help/`, `public/schemas/` or anywhere), the checkout off `main` — is a
  STOP: report it and do nothing else. A lane never writes the ledger.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1). HEAD is past the
  baseline and both drift commits move `lib/` and `help/`. Every regen
  runs from a LANE-UNIQUE detached worktree per ledger §5.1 (`git -C
  ~/source/quilltap-server worktree add --detach /tmp/qt-v4-pin-<order>-
  <sha> <sha>`, the THREE symlink classes — root `node_modules`,
  `packages/quilltap/node_modules`, every `plugins/dist/*/node_modules` —
  then `cd` into it; never stash or checkout-switch the human's repo;
  `git worktree list` before each batch; **verify the pin by `git -C
  "$PIN" rev-parse HEAD` AND by `ls -ld` of the path** — build every pin
  path from a quoted literal, never an unsplit zsh variable; then grep the
  fresh NDJSON for a marker only one tree has; check the file is non-empty
  before believing a diff). Which sha:
  - **P4.D200 pins the TARGET `89fcc3c0d`** for every family its port
    MOVES (the NEW `doc_opacity_equivalence`, `tiered_mount_pool_
    equivalence`, `doc_edit_path_resolver_equivalence` if it grows, `help_
    tree_equivalence`) and the BASELINE `bcd7e4852` for its neutrality
    legs (`doc_enum`, `doc_blob`, `doc_text`, `doc_fm`, `doc_fs`, `doc_ui`,
    `scriptorium_tools`, and the pre-existing rows of the two grown
    families). The target-pinned help-tree oracle MUST carry `Nor are they
    so much as named` and the baseline one MUST NOT — that pair is the pin
    verification.
  - **P4.98 and P4.99 pin the BASELINE `bcd7e4852`** — their v4 surfaces
    are untouched by the drift: `git -C ~/source/quilltap-server diff
    --stat bcd7e4852..89fcc3c0d -- app/api/v1/images/ lib/api/
    lib/services/chat-message/ plugins/` is EMPTY (measured at planning,
    2026-09-18; each lane re-records it in its lane record before the
    first regen). A pin at the baseline is still REQUIRED, not optional:
    the checkout can go dirty mid-lane (ledger §5.1), and a bare-checkout
    regen is unrepeatable.
  The sweep driver is the sanctioned per-family path (`harness/tools/
  recipe_sweep.py --run <family> --v4 "$PIN" --v5w <ABSOLUTE worktree>`);
  never run two sweeps concurrently; a family that reports `SKIP` in
  0.00 s did not run (`a-family-env-var-is-not-its-regen-var`). Ledger
  §5.2's silent-stale-pass discipline applies to every regen (`rm -f`
  the fixture first; read the builder's last line; `grep -c` the changed
  bytes in the fresh NDJSON — MUST be > 0). **Shared `/tmp` output paths
  are a live hazard** (the `/tmp/qt-imgcol-*` pair is read by BOTH
  `images_routes_equivalence` and `images_generate_route_equivalence`):
  every lane stages its regen outputs under a LANE-PRIVATE directory
  (`/tmp/<order>/…`) by pointing the family's env vars there, and leaves
  the committed recipe headers canonical (a committed recipe NEVER names
  a `/tmp` pin — the driver's `--v4` is the pin). The recorded provider
  corpora (P4.99's google pair) are regenerated by hand from the pin's
  plugin directory, never by a sweep stage.
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). The spec
  for every item is `git show <sha> -- <path>` plus `git show <sha>:<path>`
  at `89fcc3c0d` (P4.D200) or `bcd7e4852` (P4.98, P4.99); a commit message
  describes the bug as its author understood it and the hunks are often
  narrower. Two measured examples this round: bug 152's message says "the
  self token is now refused explicitly" — the hunk is a CONDITION added to
  the existing self-token gate (`&& !context.hideCharacterVaults`), not a
  new refusal arm; bug 153's message says "all four callers" derive the
  flag — the hunks show the WRITE resolver passes NO `extraCharacterIds`
  while the other three pass the peers. Re-verify even where an order
  flags the trap — the order's paragraph is the same kind of prose, and so
  is a previous lane's record or a doc comment in the tree. Carry a
  v5-side code comment naming the sha where the prose misleads.
- **§R.5 — Designed reds and pre-existing reds: measure, don't assume**
  (ledger §5.4). No convergence pin trips this round (neither v4 commit is
  this port's filing; both are v4-side finds from live Friday use).
  P4.D200's pre-fix reds are DESIGNED and PREDICTED by the ledger's
  2026-09-18 measurement: v5 REPRODUCES both bugs (the builders drop
  `character_id`; the enumerator takes no flag) — the new family's rows
  must be red on unported `main` before the port and the count recorded.
  P4.98's null arms are red at the dispatch WIRE only (the handler-direct
  family is green on both sides today). P4.99's line is a silence pin red.
  A red that appears in a family a lane did NOT touch is a NEW finding to
  classify at both pins before any source is touched, and it is probably
  not that lane's.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest`
  is shared) and its silence leg; a fixture change lands with the regen
  of every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that
  SURVIVES as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`). A dispatch WIRE
  test through `POST /api/dispatch` is the only instrument that sees a
  serde collapse (`a-dispatch-wire-test-sees-what-the-differential-
  cannot`); a family that calls the handler directly cannot.
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed on each commit per `.claude/commands/commit.md`; the unifier
  RECOUNTS as base + the sum of every lane's bumps (the silent-auto-merge
  trap: identical bumps merge as ONE — it fires every round). Base at
  planning: core 0.0.950, harness 0.0.842, web 0.0.152, host 0.0.139, cli
  0.0.22, tauri 0.0.7, SPA 0.5.731. Expected: **core** by all three
  lanes; **harness** by all three; **web** by P4.98 ONLY (the REST edge
  function, the census and the new dispatch wire test); **host, cli,
  tauri and the SPA** by NOBODY (P4.D200's `help/` re-vendor changes the
  embedded table but no host SOURCE — the P4.D194 rule: no host bump for
  help alone; no lane touches `apps/web/**`).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  P4.D200 pre-lists the four paths the two commits move (`docs/v4/
  CHANGELOG.md`, `docs/v4/developer/bugs.md`, the NEW `docs/v4/developer/
  bugs/fixed/bug-152-opacity-hides-group-stores.md` and `bug-153-opacity-
  enumeration-leak.md`) with byte counts at the `89fcc3c0d` pin in its
  lane record; no lane copies them.
- **§R.10 — Cross-lane fences, named:** (a) **`api/types.rs`, `api/
  engine.rs`, `api/images.rs`, `api/image_profiles.rs` and `crates/
  quilltap-web/tests/dispatch_wrong_type_census.rs` are P4.98's alone** —
  the unifier asserts `git diff main...<lane> -- <those five paths>` is
  EMPTY for P4.D200 and P4.99 before picking. (b) **`services/primary_
  stream.rs` and `model/request_builder/google.rs` are P4.99's alone**;
  the same empty-diff assertion for P4.D200 and P4.98. (c) **`crates/
  quilltap-harness/src/lib.rs` is P4.99's alone** (the `CorpusScript`
  DRY) — P4.D200's new family keeps its helpers in its own test file.
  (d) **The `/tmp/qt-imgcol-*` staging pair** (`images_routes_
  equivalence` ↔ `images_generate_route_equivalence` share the committed
  `images-{main,mount}.db` copy and the `images-collection.json`
  fixture): P4.98 stages under `/tmp/p4.98/`; at the unified gate the
  two families are run by name, one after the other, each on its own
  fresh staging. (e) **The recorded google corpora** (`google-request.
  recorded.ndjson`, `google-wire.recorded.ndjson`) and their recorder are
  P4.99's alone; no other lane records. (f) **There is no cross-lane
  constant, no shared type and no handoff this round** — each lane's
  output is consumed by nobody else; if a lane finds it needs another
  lane's file, it STOPs on that item and records the shape for the
  unifier.
- **§R.11 — No SPA behaviour moves this round.** No lane touches
  `apps/web/**`; no beat is added or flipped; the unified gate runs the
  full Playwright suite once against the release binary as regression
  only (the documented intermittents and the standing parks stand as
  named in the previous round's gate: 322 passed / 0 failed / 6 skipped).

## §S — Shared contract (binding; byte-identical in every order of this round)

- **§S.1 — Nothing on the wire moves for any consumer this round.**
  P4.D200 adds no dispatch verb, no REST route and no `core-contract.ts`
  type — the doc-edit opacity covenant is a server-internal rule reached
  only through the `doc_*` tool handlers and the path resolver (v4's two
  commits touch no `app/` or `components/` file). P4.98's tri-state is
  transport-INTERNAL: the five body keys of `Request::ImagesGenerate`
  keep their camelCase wire names (`prompt`, `profileId`, `chatId`,
  `tags`, `options`) and their v4 semantics; only the Rust carry changes
  from `Option<Value>` to `Option<Option<Value>>`, so an explicit JSON
  `null` reaches the handler as `Value::Null` on dispatch and Tauri IPC
  exactly as it already does on the REST edge. The SPA never posts to
  this verb (measured at planning: `core-contract.ts` carries no
  `imagesGenerate` request; the only `?action=generate` client is the
  image-PROFILE dialog). P4.99 adds one log line and two corpus rows; no
  request or response byte changes.
- **§S.2 — The one v4 type shape every lane reads the same way:**
  `hideCharacterVaults?: boolean` on `PathResolutionContext` and on the
  new `AccessibleMountPointsQuery` (v4 `path-resolver.ts:126`, `:848`)
  is P4.D200's to port as a Rust field named `hide_character_vaults`;
  no other lane names it. `includeCharacterTier` on `flattenTierPool`
  (v4 `tiered-mount-pool.ts:352-376`) is P4.D200's to port; the two
  other flatten call sites outside its ownership (`tools/search.rs:440`)
  are adapted by P4.D200 as a signature-only edit (§Ownership).
- **§S.3 — A drift lane that finds it needs a wire change STOPs on that
  item** and records the shape for the unifier rather than inventing a
  verb.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D200** — bugs 152 + 153, the doc-edit opacity covenant | `crates/quilltap-core/src/db/tiered_mount_pool.rs` (the flatten option + doc); `crates/quilltap-core/src/doc_edit/path_resolver.rs` (the context flag, the collector, the self-token gate, the out-of-scope split + its two warn lines, `describe_characters`, the query struct + `get_accessible_mount_points` if it moves here — say which); `crates/quilltap-core/src/db/doc_mount_points.rs` — ONLY an additive `store_type` on `DmpRow` / `find_enabled_for_docedit` if the survey's measurement says the row lacks it (say so); `crates/quilltap-core/src/tools/doc_edit/shared.rs` (the two builders, the opacity helper's warn, `get_accessible_mount_points`); `crates/quilltap-core/src/tools/doc_edit/text.rs` — the grep + list_files enumeration call sites ONLY; `crates/quilltap-core/src/tools/doc_edit/blob.rs` — the two mount resolvers ONLY; `crates/quilltap-core/src/tools/search.rs:440` — the flatten call's SIGNATURE adaptation only; `help/character-system-transparency.md` (byte copy at the target pin); `crates/quilltap-harness/tests/help_tree_embed_guard.rs` — the history doc comment ONLY (124 stays 124); NEW `crates/quilltap-harness/tests/doc_opacity_equivalence.rs` + NEW `harness/oracle/cases/doc-opacity.test.ts` + NEW `harness/oracle/fixtures/build-doc-opacity-fixture.ts` + `doc-opacity.json`; `crates/quilltap-harness/tests/tiered_mount_pool_equivalence.rs` + `harness/oracle/cases/tiered-mount-pool.ts`; `crates/quilltap-harness/tests/doc_edit_path_resolver_equivalence.rs` + `harness/oracle/cases/doc-edit-path-resolver.ts` + its fixture builder/json ONLY if Tier-2 item 13 lands (say so); this order's header | `api/**`, `crates/quilltap-web/**`, `crates/quilltap-host/**`, `services/**` (incl. `primary_stream.rs`), `model/**`, every other `help/*.md`, every recorded provider corpus, `crates/quilltap-harness/src/lib.rs`, every other harness family (the neutrality legs are RE-RUN, never edited), `apps/web/**`, the drift ledger |
| **P4.98** — `Request::ImagesGenerate`'s tri-state | `crates/quilltap-core/src/api/types.rs` — the `ImagesGenerate` variant + its doc and the `:1062` rule paragraph ONLY; `crates/quilltap-core/src/api/engine.rs` — the `ImagesGenerate` arm (`:5218-5241`) ONLY; `crates/quilltap-core/src/api/images.rs` — `parse_generate_body` + the route doc ONLY if a measurement says the handler must change (say so); `crates/quilltap-core/src/api/image_profiles.rs` — the `generate_parsed_type` retirement ONLY (Tier 2); `crates/quilltap-web/src/images_routes.rs` — the `images_generate` edge function ONLY; `crates/quilltap-web/tests/images_edge_routes.rs` (the null arm); `crates/quilltap-web/tests/dispatch_wrong_type_census.rs`; NEW `crates/quilltap-web/tests/images_generate_dispatch_wire.rs`; `crates/quilltap-harness/tests/images_generate_route_equivalence.rs` + `harness/oracle/cases/images-generate-route.test.ts` (the new row); this order's header | every other `api/*.rs`, `crates/quilltap-web/src/**` beyond the one function, `services/**`, `model/**`, `doc_edit/**`, `tools/**`, `db/**`, `help/**`, the committed `images-{main,mount}.db` pair and `images-collection.json` (READ ONLY — a copy is staged), every recorded provider corpus, every other harness family (incl. `images_routes_equivalence` — RE-RUN by name only), `apps/web/**`, the drift ledger |
| **P4.99** — the recovery INFO line + the google cache-key rows + the harness DRY | `crates/quilltap-core/src/services/primary_stream.rs` — the request-limit branch (`:1386-1414`) + its `#[cfg(test)]` pin; `crates/quilltap-core/src/model/request_builder/google.rs` — its `#[cfg(test)]` module ONLY (the ignorer unit pin); `harness/oracle/providers/record-google-request.mjs` + `regenerate-google-wire.sh`; `harness/oracle/fixtures/request-envelopes/google-request.recorded.ndjson` + `google-wire.recorded.ndjson` (re-recorded at the baseline pin); `crates/quilltap-harness/tests/request_builder_google_equivalence.rs` + `request_builder_google_wire_equivalence.rs`; `crates/quilltap-harness/tests/request_builder_equivalence.rs` — the `CACHE_KEY_IGNORED` doc comment ONLY; `crates/quilltap-harness/src/lib.rs` (the `CorpusScript` DRY home, additive); `crates/quilltap-harness/tests/llm_image_budget_equivalence.rs` + `file_attachment_tier3_equivalence.rs` — the DRY repoint + the anchored log asserts ONLY (no row, no fixture, no oracle change); `crates/quilltap-harness/tests/primary_stream_tier3_equivalence.rs` + `harness/oracle/cases/primary-stream-tier3.test.ts` ONLY if Tier-1 item 2's measurement finds a recovery-reaching case whose rig can carry the pin (say so); this order's header | `services/primary_stream.rs` beyond the one branch, `services/recovery.rs`, `services/orchestrator.rs`, `model/request_builder/google.rs` production code, every other recorded corpus (`request-envelopes.recorded.ndjson`, `image-dialects/`, `response-bodies/`, `stream-fixtures/`, `tool-wire/`, `moderation-wire/`, `web-search-wire/`), `api/**`, `crates/quilltap-web/**`, `doc_edit/**`, `tools/**`, `help/**`, every other harness family, `apps/web/**`, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0),
   and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp` / by-label diff) where the family is
   deterministic across regens; where it is not (`regenerate-at-both-
   pins-and-cmp-is-not-universal`), compare the normalized rows by case
   and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean (the Playwright venue
   prefers `target/release`).
8. `cargo test --workspace --no-fail-fast -- --nocapture` with the lane's
   env block (its own families' vars; every other family's var WITHHELD
   so it SKIPs honestly rather than failing on a deleted `/tmp` path):
   every binary's `test result:` line read, zero failures, the lane's
   families confirmed RUN by non-zero duration.
9. **Source censuses** that the change moves (P4.98: the dispatch
   wrong-type census + `EXCLUDED_BY_THE_ROUTE_IDENTIFIER_RULE`; P4.D200:
   `help_tree_embed_guard`, the qtap/help count guards; P4.99: none
   expected — say so if one moves) re-run by name and their constants
   recounted with the arithmetic in the comment.
10. **Commit per `.claude/commands/commit.md`** (one unit per commit where
    the order tiers them; CHANGELOG entry + version bumps per §R.8; the
    status-log lane record appended with the regen commands AS RUN, the
    mutation table, the pre-fix counts, the measurements, and every
    finding outside the lane's ownership named for the unifier). The lane
    closes by updating its own order's status header with what landed,
    what was deferred, and what it measured that the order got wrong.
