# Work order P4.D194 — the bug-144 CONVERGENCE (v4 fixed this port's own filing: `--lock-clean`'s heartbeat arm says what it tested — retire `a_fresh_heartbeat_keeps_v4s_false_liveness_claim` by measurement, `describe_fresh_window` off `FRESH_MS`, Tier R's five `lock clean …` cases moving together), the two docs-only NO-PORT ratifications (`064ba85df` bug 143 filed, `81e02f7a2` bug 144 filed) plus the non-lib files of both code commits, the `help/**` re-vendor (`database-protection.md` +10, `chat-turn-manager.md` +3; 124 stays 124), the `docs/v4/` mirror inventory for the unifier, and dogfood row #119's status cell — v4 `23abc1ba1`'s `packages/quilltap/bin/quilltap.js` + `help/` hunks, `2075242f9`'s `help/` hunk, `064ba85df`, `81e02f7a2`, from `main`

**UNIFIED on `main` (2026-09-16) — CLOSED WHOLE.** The §3 review found NO blocking issue. **At the wires:** `host` bumped for the help embed (P4.D191's precedent — the lane flagged the choice rather than deciding it); the `help_tree_embed_guard` provenance comment carries the `2075242f9` vintage; CLAUDE.md's and the walk doc's "5 of 223" corrected to FOUR alongside the lane's row-#119 edit; and the lane's finding 4 was widened by one grep — SEVEN families (incl. Tier R) spelled their skip notice as prose and are now the `SKIP:` sentinel the gate greps. The `QT_FIXTURE_HELP_MAIN` two-family collision (finding 5) stands as a maintenance item; both families were proven by name in the target-pin sweep. The `docs/v4/` mirror took the seven pre-listed paths from the `2075242f9` COMMIT (the checkout's copies of two of them were dirty with bug 147). Round record: `status-log.md`.

**Status: CLOSED WHOLE at lane close (2026-09-16).** Ordered 2026-09-16, the
`2075242f9` bug-145/146 drift catch-up + maintenance round (P4.D192 ∥ P4.D193
∥ P4.D194 ∥ P4.89 ∥ P4.90). Lane branch
`claude/p4-convergence-ratifications-3c0486` from `main` at `e9173924`; three
commits. The unifier rewrites this header at unification.

**Every Tier 1 and Tier 2 item landed.** Tier 1: (1) both filings and both
commits' non-lib files ratified NO-PORT on `--name-status` file lists;
(2) the convergence red-first — Tier R 223/4 at the target pin before,
**223/0** after, and 223/4 the other way at the baseline pin; (3) the
false-claim pin retired to
`a_fresh_heartbeat_says_what_it_tested_and_offers_waiting` with both
`!contains` guards, plus `the_fresh_window_is_worded_as_v4_words_it` over an
injected window, all three named mutations reddening exactly their targets;
(4) the two `help/**` files re-vendored byte-for-byte (count 124,
`diff -r` empty, `help_tree_equivalence` green at the target / red at the
baseline with the two-path diff measured on the NDJSONs, 15/15 help families
zero SKIP, the embed proven fresh in the compiled binary); (5) the `docs/v4/`
inventory pre-listed with byte counts; (6) dogfood row #119 moved to
CONVERGED. Tier 2 items 7 and 8 both answered "none" / "no v5 site" by
measurement.

**⚠ Two order premises REFUTED by measurement.** (a) The convergence moves
**FOUR** Tier R cases, not five — the order's "+ one — find the fifth by
running" and dogfood #119's "5 of 223" are both unsubstantiated; the
heartbeat-fresh arm reaches exactly the four named cases, and `lock clean
renamed host live pid` plants a LIVE PID so it takes the arm above.
(b) v4's fix moved **BOTH** lines, not only the liveness claim — the old
second line ("Stop the running instance first…") survives only in the
live-process arm, which is why the retirement guards it with a second
`!contains`.

**OPEN: nothing.** Tier 3's live proof (💸 SIGKILL then `--lock-clean`
inside five minutes on the Friday copy) is banked for the next dogfood pass
as the order intended. Two items are recorded for the UNIFIER in the lane
record rather than edited here: `help_tree_embed_guard.rs`'s
`VENDORED_FILE_COUNT` provenance comment is one vintage stale (green
untouched; a MUST-NOT-TOUCH path for every lane), and `quilltap-host` is
deliberately NOT bumped though the re-vendor changes its compiled embed
(§R.8 assigns this lane `cli` alone; P4.D191 bumped host for the same
shape).

## Preamble

- **v4 oracle baseline: `ffb6b3119`.** This lane absorbs FOUR ledger rows
  in part or whole: **`23abc1ba1`**'s `packages/quilltap/bin/quilltap.js`
  hunk (+24/−2 — the CONVERGENCE: v4 fixing bug 144, which this port's
  2026-09-15 walk filed) and its `help/database-protection.md` hunk (+10);
  **`2075242f9`**'s `help/chat-turn-manager.md` hunk (+3); **`064ba85df`**
  and **`81e02f7a2`** whole (docs-only NO-PORT? candidates — ratify on
  FILE LISTS, never subject lines). The ledger rows are `ORDERED(P4.D194)`
  for the two filings and `ORDERED(P4.D192, P4.D194)` / `ORDERED(P4.D193,
  P4.D194)` for the two code commits. Spec reads: `git show 23abc1ba1 --
  packages/quilltap/bin/quilltap.js help/database-protection.md
  __tests__/unit/cli/lock-clean-refusal.test.ts`, `git show 2075242f9 --
  help/chat-turn-manager.md`, `git show --stat --format= --name-status
  064ba85df 81e02f7a2 23abc1ba1 2075242f9` — **never the commit message**
  (§R.4; §R.5 for the convergence).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** Pins **`/tmp/qt-v4-pin-p4d194-
  2075242f9`** (Tier R — the CLI differential drives v4's REAL launcher,
  which must be the post-fix file; the help re-vendor SOURCE; every
  help-content family; `QT_V4_ROOT`/`QT_V4_CHECKOUT` AFTER the re-vendor
  commit) and **`/tmp/qt-v4-pin-p4d194-ffb6b3119`** (Tier R's RED-FIRST
  leg is the other way round — see Tier 1 item 2; `QT_V4_ROOT` for the
  embed guards BEFORE the re-vendor commit).
- **Sibling lanes:** P4.D192 owns the `lib`-side heal of `23abc1ba1`;
  P4.D193 owns bug 146's code; P4.89 / P4.90 never touch the CLI or
  `help/**`. You touch no `crates/quilltap-core/**` file and no SPA file.
- **Worktree:** `git worktree add <path> -b claude/p4-d194-bug144-
  convergence-help-<hash> main`; `CARGO_INCREMENTAL=0`.
- **Read before the work:** `crates/quilltap-cli/src/db_cmd.rs:500-580`
  (`LockAssessment` `:505`, the doc comment `:520-530` that records the
  false sentence and why it stays, `lock_clean_refusal_lines` `:535-561`,
  `FRESH_MS` `:512`, `assess_lock` `:563-575`) and `:905-960` (the
  `lock_clean_wording_tests` module — `V4_FRESH_HEARTBEAT`, `V4_SECOND_
  LINE`, `a_fresh_heartbeat_keeps_v4s_false_liveness_claim` with its
  `assert!(first.contains("its holder is alive"))` ON PURPOSE, `a_live_
  process_outranks_a_fresh_heartbeat`); `crates/quilltap-cli/tests/cli_
  differential.rs:2000-2245` (the `lock clean …` cases; `normalize_
  heartbeat: true` — the seconds display is the one documented
  normalization; the five cases the walk measured moving: `lock clean
  suspect but fresh heartbeat refuses` `:2203`, `… docker fresh refuses`,
  `… retired lima env`, `… foreign fresh local refuses`, + one — find the
  fifth by running); `crates/quilltap-harness/tests/help_tree_embed_
  guard.rs:34` (`VENDORED_FILE_COUNT = 124` — does not move); the P4.D191
  order + lane record (`status-log.md` → "P4.D191": the re-vendor recipe,
  the 16-family help sweep list, the `docs/v4/` inventory shape, the Tier
  R run at the pin — the closest recipe for everything here); `docs/
  developer/porting/dogfood-findings.md:12` (row #119 — the walk's own
  mis-filing, the revert, the pin, "v5 keeps the false sentence until v4
  lands its fix"); memory notes `tier-r-case-count-hidden-on-pass`, `cli-
  help-text-capture-not-extraction`, `a-tier-r-case-needing-prior-state-
  uses-a-pre-hook`, `never-quote-the-misspelling-even-as-the-rule`, `a-
  string-fixed-by-a-dogfood-finding-can-be-made-wrong-by-the-next-port`
  (this row is that note's exact shape, resolved the right way round).

## Mandate

Converge the CLI's `--lock-clean` heartbeat refusal onto v4's post-fix bytes
BY MEASUREMENT: run Tier R at the TARGET pin first and watch exactly the
five fresh-heartbeat cases go red (that is the tripwire firing as designed —
the proof v4 moved), dump v4's actual two lines, then move
`lock_clean_refusal_lines`' heartbeat arm to `Lock heartbeat is still fresh
({N}s ago). Cannot clean.` + `A lock counts as held until its heartbeat is
{describe_fresh_window()} stale, even if its process has gone. Wait it out,
or use --lock-override to force.` with `describe_fresh_window` derived from
`FRESH_MS` exactly as v4 derives it from `HEARTBEAT_FRESH_MS` (seconds when
not a whole minute; `1 minute`; `N minutes`), refusal semantics and branch
ORDER unchanged; retire the deliberate false-claim pin to a pin on the NEW
bytes with a `!contains("its holder is alive")` guard; Tier R back to 223/0
at the target pin. Ratify the two docs-only filings and the non-lib files of
both code commits NO-PORT on file lists (Tier R at the pin is the evidence
the CLI half of `23abc1ba1` beyond the wording is nothing — the new
`__tests__/unit/cli/lock-clean-refusal.test.ts` is v4's own guard, no v5
counterpart needed beyond the unit pin). Re-vendor the two `help/**` files
byte-for-byte from the target pin (count stays 124; every help-content
family regenerated at the target; the whole tree md5-identical to v4 at
`2075242f9`). Pre-list the `docs/v4/` paths the target moves with byte
counts for the unifier (§R.9). Move dogfood row #119's status cell to
CONVERGED. This lane does NOT touch `crates/quilltap-core/**`, the SPA, or
any sibling's files.

## Survey-verified starting points (fresh survey 2026-09-16 at `2075242f9` and on `main` `4572659b` — do not re-derive)

### v4

- **`packages/quilltap/bin/quilltap.js`** (`23abc1ba1`): NEW
  `describeFreshWindow()` after `HEARTBEAT_FRESH_MS` (`:487-498`): `seconds
  = Math.round(HEARTBEAT_FRESH_MS / 1000); if (seconds % 60 !== 0) return
  `${seconds} seconds`; minutes = seconds / 60; return minutes === 1 ? '1
  minute' : `${minutes} minutes``. In `handleLockCommand`'s `--lock-clean`
  chain the `else if (heartbeatFresh)` arm (`:642-654`) now prints
  `Lock heartbeat is still fresh (${ageStr} ago). Cannot clean.` then `A
  lock counts as held until its heartbeat is ${describeFreshWindow()}
  stale, even if its process has gone. Wait it out, or use --lock-override
  to force.` (`ageStr = Math.round(heartbeatAgeMs / 1000) + 's'` —
  unchanged) and `process.exit(1)` — unchanged. The `alive && isNode` arm
  before it and the `alive && !isNode` arm after it are UNCHANGED. The
  comment above the arm names bug 126 (freshness is the deliberate
  fallback where PID checks are unreliable) — carry it.
- **`__tests__/unit/cli/lock-clean-refusal.test.ts`** (NEW, +144): drives
  the REAL binary with a dead PID (`999999`) and an 82 s-old heartbeat;
  reads the output. v4's guard, not a Tier R case — Tier R already covers
  the arm through `lock clean suspect but fresh heartbeat refuses` and its
  four siblings.
- **`help/database-protection.md`** (+10 at `:201-210`): a new paragraph
  "One courtesy of the house deserves mention…" + a fenced example of the
  two new lines (`(82s ago)`, `5 minutes`) + the rationale paragraph.
- **`help/chat-turn-manager.md`** (+3 at `:259` and `:263`): the bold
  "The banner always speaks for whoever holds the floor." paragraph (with
  `Bertram's turn — switch the speaker to them…`) and the "With two of your
  own characters in the room…" bullet.
- **`064ba85df`** (docs(bugs): file bug 143): `docs/developer/bugs.md` + the
  new `docs/developer/bugs/bug-143-…md` — no `lib/`, `app/`, `packages/`,
  `plugins/`, `migrations/`, `help/`, `public/schemas/` delta. **`81e02f7a2`**
  (docs(bugs): file bug 144): `docs/developer/bugs.md` + `docs/developer/
  bugs/bug-144-…md` — likewise. Re-verify both with `git show --stat
  --format= --name-status <sha>` and record the lists.
- **`23abc1ba1`'s non-lib files**: `README.md`, `docs/CHANGELOG.md` (+73),
  `docs/developer/CLI.md` (+25 — the `--lock-clean` freshness paragraph;
  v5 has no CLI reference doc to mirror — the `docs/v4/` mirror takes
  it), `docs/developer/bugs.md`, the bug-143 file MOVED (deleted at its old
  path, +184 at the new one — find both with `--name-status`), the bug-144
  file (+62/−… in place), NEW `docs/developer/bugs/fixed/bug-145-collapse-
  takes-album-photos.md` (+166), `package.json` / `packages/quilltap/
  package.json` / `package-lock.json` (`4.10.0-dev.39`), the migration
  test (+150 — P4.D192's spec source, not a v5 file). **`2075242f9`'s
  non-lib files**: `README.md`, `docs/CHANGELOG.md` (+22), `docs/developer/
  bugs.md` (+1), NEW `docs/developer/bugs/…/bug-146-banner-passes-the-
  composers-seat.md` (+190 — resolve the directory with `--name-status`),
  the three version files (`4.10.0-dev.40`), `floor-seat.test.ts` (P4.D193's
  spec source).

### v5

- `crates/quilltap-cli/src/db_cmd.rs:551-559` — the arm to move; `:512`
  `FRESH_MS: f64 = 5.0 * 60.0 * 1000.0`; `quilltap_core::jsnum::math_round`
  is the existing `Math.round` twin (use it for `seconds` too — v4's
  `Math.round(HEARTBEAT_FRESH_MS / 1000)`; the `%60` on an integer after
  rounding). `:940-952` the pin to retire; `:916` `V4_FRESH_HEARTBEAT`.
- `crates/quilltap-cli/tests/cli_differential.rs` — the five fresh-
  heartbeat `lock clean` cases already exist with `normalize_heartbeat`;
  NO new case is expected. The Tier R count is hidden on PASS (`tier-r-
  case-count-hidden-on-pass`) — read it from a red run or `--nocapture`.
- `help/` at repo root — 124 files, byte-identical to v4 at `ffb6b3119`
  (P4.D191 measured the whole tree). The embed: `crates/quilltap-host/
  build.rs` + `src/help_content.rs`; guards `help_tree_embed_guard`
  (count) and the content families (P4.D191's 16-family list in its lane
  record — `help_tree_equivalence`, `help_doc_sync*`, `help_search*`, the
  help-chat trio, …).
- `docs/developer/porting/dogfood-findings.md:12` — row #119's third
  cell reads "REVERTED to v4's bytes and PINNED BOTH WAYS; filed upstream
  as v4 bug 144 … v5 keeps the false sentence until v4 lands its fix."
- `docs/v4/` mirror: `docs/v4/CHANGELOG.md`, `docs/v4/developer/CLI.md`,
  `docs/v4/developer/bugs.md`, `docs/v4/developer/bugs/fixed/…` (the bug
  files at their v4-relative paths).

## Tiered deliverables

### Tier 1 — must land

1. **Ratify NO-PORT on file lists:** `064ba85df`, `81e02f7a2` whole; the
   non-lib files of `23abc1ba1` and `2075242f9` as listed. Record each
   `--name-status` list in the lane record; note that the ONLY `lib/`-,
   `migrations/`-, `packages/`- or `help/`-touching hunks across the four
   commits are the three this round ports (P4.D192's migration, P4.D193's
   turn-manager + SalonView, yours).
2. **The convergence, red-first (§R.5).** (a) Run Tier R at the TARGET
   pin against the UNCHANGED v5: **exactly the five fresh-heartbeat `lock
   clean …` cases red**, every other case green — record the five names
   and v4's actual two lines from the diff output. (b) Move the arm:
   `lock_clean_refusal_lines`' `heartbeat_fresh` branch to the two new
   sentences with `describe_fresh_window()` (a new `fn` beside `FRESH_MS`,
   v4's three arms), branch order unchanged; rewrite the doc comment at
   `:520-530` (the false sentence is GONE; name `23abc1ba1` and bug 144 as
   the convergence). (c) Tier R at the target pin: **223 cases, 0
   failures** (or the then-current count — record it from a red run or
   `--nocapture`). (d) Tier R at the BASELINE pin: the same five cases
   now red the OTHER way — the proof the port moved for v4's reason and
   not by accident; record, do not keep.
3. **The unit pins.** Retire `a_fresh_heartbeat_keeps_v4s_false_liveness_
   claim` → `a_fresh_heartbeat_says_what_it_tested_and_offers_waiting`:
   `first == "Lock heartbeat is still fresh (82s ago). Cannot clean."`,
   `second == "A lock counts as held until its heartbeat is 5 minutes
   stale, even if its process has gone. Wait it out, or use
   --lock-override to force."`, `!first.contains("its holder is alive")`,
   `!second.contains("Stop the running instance first")` (the SECOND line
   changed too — v4's old second line survives only in the live-process
   arm; pin that the live arm KEEPS it). `describe_fresh_window` unit
   pins over an injected constant (make it `fn describe_fresh_window_ms(ms:
   f64) -> String` with `describe_fresh_window()` = `_ms(FRESH_MS)`): 300 s
   → `5 minutes`; 60 s → `1 minute`; 90 s → `90 seconds`; 0 → `0 minutes`
   (v4: `0 % 60 === 0`, `0 / 60 = 0`, `0 === 1` false → `0 minutes` — pin
   the JS arithmetic, not the English). Mutation proofs: M1 keep the old
   first line → Tier R five red + the unit red; M2 keep the old second
   line → the same five red (Tier R compares BOTH lines — verify it does,
   red-first); M3 `minutes === 1` arm dropped → the 60 s pin red.
4. **The `help/**` re-vendor.** Byte-copy `help/database-protection.md`
   and `help/chat-turn-manager.md` from the TARGET pin; `diff -r
   "$PIN/help" help` EMPTY; `find help -type f | wc -l` = 124;
   `help_tree_embed_guard` green with `QT_V4_ROOT` at the target pin (and
   RED at the baseline pin on exactly those two paths — measured on the
   NDJSON, the P4.D191 way); every help-content family regenerated at the
   target through the sweep driver (P4.D191's list), zero SKIP, with
   `grep -c 'courtesy of the house' oracle-help-tree.ndjson` > 0 and
   `grep -c 'always speaks for whoever holds the floor'` > 0.
5. **The `docs/v4/` inventory** (§R.9): the exact v4-relative paths the
   target moves under `docs/` (CHANGELOG.md, developer/CLI.md, developer/
   bugs.md, the four bug files at their post-commit paths) with byte
   counts at the pin, in the lane record. Do not copy them.
6. **Dogfood row #119** — status cell: `CONVERGED (v4 23abc1ba1, 2026-09-
   15; P4.D194 moved v5 with it — the deliberate pin retired by
   measurement, Tier R 223/0 at 2075242f9)`; keep the walk's mis-filing
   lesson intact.

### Tier 2 — should land

7. **The CLI's `--help`/completion surfaces** — check whether v4's
   `docs/developer/CLI.md` paragraph corresponds to any launcher help
   text or completion string (Tier R would have caught a help-text
   change; `git show 23abc1ba1 -- packages/quilltap/bin/quilltap.js`
   shows none beyond the arm) — record "none".
8. **`quilltap-cli` README / help docs** — if v5's CLI carries its own
   user-facing text mentioning `--lock-clean` (grep `lock-clean` under
   `crates/quilltap-cli/src/help/` and `docs/`), align it to v4's new
   paragraph; else record "no v5 site".

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **The live proof (💸):** SIGKILL the server on the Friday copy, run
  `quilltap db --lock-clean` inside five minutes — the two new lines,
  then after the window a clean. Banked for the dogfood pass (the walk's
  own gesture, now with the right sentence).

## The differential requirement, restated

The CLI wording is proven by Tier R (`crates/quilltap-cli/tests/cli_
differential.rs` — drives v4's REAL launcher `packages/quilltap/bin/
quilltap.js` at the pin; red-first at the target, then 223/0; the reverse
leg at the baseline recorded). The help re-vendor is proven by
`help_tree_embed_guard` + the help-content families at the target pin. The
ratifications are proven by file lists + Tier R. Closest recipe: the
P4.D191 order + lane record (identical shape, one round ago).

## Fixtures

- **Consumes:** none committed (Tier R builds per-case instances; the help
  families read the vendored tree).
- **Owns regenerating:** Tier R (both pins), every help-content family
  (target pin).
- **Delivers:** the two re-vendored help files.

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `2075242f9` bug-145/146 drift catch-up + maintenance round:
**P4.D192 ∥ P4.D193 ∥ P4.D194 ∥ P4.89 ∥ P4.90**, ordered 2026-09-16. Five
lanes, five worktrees, five lane branches, ALL from `main` (no stacking —
every lane's ownership is disjoint by file, so nothing needs a substrate
commit). The unifier cherry-picks in the order D192 → D193 → D194 → P4.89
→ P4.90 (server-side heals before the fixture widen; the fixture widen
before the family that re-measures over it), gates ONCE, and `merge
--ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`ffb6b3119`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`2075242f9`**
  (`4.10.0-dev.40`) — FOUR commits past the baseline, two carrying code:
  `23abc1ba1` (bug 145 + the bug-144 convergence + closes bug 143 as not
  a defect) and `2075242f9` (bug 146); `064ba85df` and `81e02f7a2` are
  the two docs-only filings this port's own walk wrote. v4 `bugfix` tip
  `1a2b2164c` and `release` tip `8fbf2afe0` are unmoved; there is still
  no `release: 4.10.0` squash. The checkout sat on `main`, CLEAN, at the
  planning probe (2026-09-16 ~08:30 CDT; the §2 probe PASSED, so the
  ledger stood and nothing was re-derived). **At unification the baseline
  MOVES to `2075242f9`** and all four rows retire (two ABSORBED, two
  NO-PORT-RATIFIED).
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log 2075242f9..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Any mismatch — a moved HEAD, a dirty `lib/`/`app/`/`packages/`
  /`plugins/`/`migrations/`/`help/` tree, the checkout off `main` — is a
  STOP: report it and do nothing else.** A lane never writes the ledger.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1). HEAD is past the
  baseline and two commits touch `lib/`, `migrations/`, `packages/` and
  `help/`. Every regen runs from a LANE-UNIQUE detached worktree per
  ledger §5.1 (`git -C ~/source/quilltap-server worktree add --detach
  /tmp/qt-v4-pin-<order>-<sha> <sha>`, the THREE symlink classes, `cd`
  into it; never stash or checkout-switch the human's repo; `git
  worktree list` before each batch; verify the pin by grepping the fresh
  NDJSON for a marker only one tree has; check the file is non-empty
  before believing a diff). Two pins per lane:
  - **the TARGET pin `2075242f9`** — for every family the lane's port
    MOVES (the oracle must import v4's post-fix code: the collapse
    migration, `resolveFloorSeatId`, the CLI's new wording, the two
    re-vendored help pages);
  - **the BASELINE pin `ffb6b3119`** — for every neutrality leg and
    every family the lane merely re-runs, and for `QT_V4_ROOT` /
    `QT_V4_CHECKOUT` on any embed/content guard run BEFORE a re-vendor
    commit lands.
  The sweep driver is the sanctioned per-family path
  (`harness/tools/recipe_sweep.py --run <family> --v4 "$PIN" --v5w
  <ABSOLUTE worktree>`); never run two sweeps concurrently; a family that
  reports `SKIP` in 0.00 s did not run (`a-family-env-var-is-not-its-
  regen-var`). Ledger §5.2's silent-stale-pass discipline applies to
  every regen (`rm -f` the fixture first; read the builder's last line;
  `grep -c` the changed bytes in the fresh NDJSON — MUST be > 0).
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). The
  spec for every item is `git show <sha> -- <path>` plus `git show
  <sha>:<path>`; a commit message describes the bug as its author
  understood it and the hunks are often narrower. Re-verify even where
  this order already flags the trap — the order's paragraph is the same
  kind of prose. Carry a v5-side code comment naming the sha where the
  prose misleads.
- **§R.5 — Convergence rows: measure, don't assume** (ledger §5.4). The
  bug-144 wording pin (`a_fresh_heartbeat_keeps_v4s_false_liveness_claim`)
  trips at the target pin BY DESIGN; dump v4's actual post-fix output
  before retiring it, and expect Tier R's five `lock clean …` cases to
  move together.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig) and its silence leg; a fixture change lands with the
  regen of every family that reads it. Mutation proofs: each named
  proof must redden exactly the test it is written against, and a proof
  that SURVIVES as first written is a finding to fix, not a line to
  delete (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`).
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). This order's status header is the
  lane's to update at lane close; the unifier rewrites it at unification.
  `docs/developer/porting/drift-ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed (and `apps/web/package.json` when it touches the SPA) on each
  commit per `.claude/commands/commit.md`; the unifier RECOUNTS as base +
  the sum of every lane's bumps (the silent-auto-merge trap: four lanes'
  identical `0.0.922` bumps merge as ONE). Expected: core is bumped by
  P4.D192, P4.D193 and P4.90; harness by all five (P4.89 only if it edits
  a harness header); cli by P4.D194; web by P4.89 (the committed fixture
  pair lives under `crates/quilltap-web/tests/fixtures/`); host by
  P4.D192 only if `host_boot_avatar_rolls_collapse.rs` changes; the SPA by
  P4.D193.
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  P4.D194 pre-lists the paths the target moves (with byte counts at the
  pin) in its lane record; no lane copies them.
- **§R.10 — Cross-lane handoffs, named:** (a) P4.D192 widens
  `gc_orphaned_file_row`'s return type — its only callers are in files
  P4.D192 owns (`db/doc_mount_file_links.rs`, `api/mount_points.rs:602`);
  no sibling touches either. (b) P4.D193's `help/chat-turn-manager.md`
  hunk and P4.D192's `help/database-protection.md` hunk are BOTH
  P4.D194's (the one `help/**` owner). (c) P4.89's widened
  `brahma-{main,mount}.db` is read by three families; no sibling
  regenerates them. (d) P4.90's `orchestrator.rs` edit is one statement
  after `:2861`; P4.D189's wrap census counts `watch_stream(` per file and
  must not move — P4.90 adds none. (e) The dogfood-findings row #119 cell
  moves with P4.D194 (the convergence); no other lane edits that file.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D192** — bug 145 server | `crates/quilltap-core/src/db/avatar_rolls_collapse_heal.rs`; `crates/quilltap-core/src/db/doc_mount_file_links.rs` — ONLY (i) `gc_orphaned_file_row`'s return type + its in-file callers/tests and (ii) the deletion of the `is_photos_relative_path` twin + its private `posix_dirname`; `crates/quilltap-core/src/api/mount_points.rs:602` (the one out-of-file GC caller, adapted to the new return); the twin's importers, import-line edits ONLY: `api/chat_media.rs:1716`, `photos/photo_link_summary.rs:13`, `photos/character_gallery_service.rs:17`, `services/maintenance.rs:32,375`; `crates/quilltap-harness/tests/avatar_rolls_collapse_heal_equivalence.rs`; `crates/quilltap-harness/tests/photos_relative_path_equivalence.rs` (import path only); `harness/oracle/cases/avatar-rolls-collapse-heal.test.ts`; `harness/oracle/fixtures/avatar-rolls-collapse-heal.json`; `crates/quilltap-host/tests/host_boot_avatar_rolls_collapse.rs` (only if a two-link arm is added); this order's header | `services/orchestrator.rs`, `services/chat_create.rs`, `participant_filters.rs`, `crates/quilltap-cli/**`, `help/**`, `apps/web/**`, `crates/quilltap-web/tests/fixtures/brahma-*`, every other harness family (regen-and-run ONLY for neutrality — no edits), `docs/v4/**`, the drift ledger |
| **P4.D193** — bug 146 (server twin + SPA) | `crates/quilltap-core/src/participant_filters.rs`; NEW `harness/oracle/cases/floor-seat.ts`; NEW `crates/quilltap-harness/tests/floor_seat_equivalence.rs`; `apps/web/src/app/chat/turn-order.ts` + `turn-order.spec.ts`; `apps/web/src/app/chat/turn-controls.ts`; `apps/web/src/app/screens/salon/salon-conversation.ts` — ONLY `bannerSeat` / `userTurnName` / `isSeatsTurn` / `mustSpeak` / `onSkipUserTurn` and the `<qt-turn-controls …>` binding block (`:431-436`); `apps/web/src/app/screens/salon/salon-turn-controls.spec.ts`; NEW `apps/web/e2e/salon-floor-seat-flow.spec.ts`; `apps/web/e2e/salon-dialogs-flow.spec.ts` ONLY if its banner assertion (`:412-418`) must learn the third sentence; this order's header | every other `crates/**` file, `help/**`, `core-contract.ts` (no wire change in this feature), every other SPA file, the drift ledger |
| **P4.D194** — bug-144 convergence + ratifications + help re-vendor | `crates/quilltap-cli/src/db_cmd.rs`; `crates/quilltap-cli/tests/cli_differential.rs` (only if a Tier R case must be added — none is expected); `help/database-protection.md`; `help/chat-turn-manager.md`; `docs/developer/porting/dogfood-findings.md` — row #119's status cell ONLY; this order's header | every other `crates/**` file, `apps/web/**`, every harness family except the `help_*` content families (regen-and-run at the target pin), `docs/v4/**` (pre-LIST for the unifier, never copy), the drift ledger |
| **P4.89** — the brahma fixture vintage widen | `crates/quilltap-web/tests/fixtures/brahma-main.db`, `brahma-mount.db` (+ any `.meta.json` sidecar found beside them); `harness/oracle/fixtures/migrate-memories-fixture-columns.ts` (EXTEND its target list — the P4.52 idiom); the THREE brahma harness headers (`crates/quilltap-harness/tests/brahma_orchestrator_tier3_equivalence.rs`, `brahma_console_tier3_equivalence.rs`, `brahma_console_routes_equivalence.rs`) — regen-recipe comments and a vintage note ONLY, no assertion changes unless a red is proven fixture-caused; the three jest oracles `harness/oracle/cases/brahma-*.test.ts` ONLY if an oracle-side heal is proven necessary AFTER the widen; this order's header | every `crates/quilltap-core/src/**` file (the P4.50-class prefix finding is MEASURED and RECORDED, never fixed here), `apps/web/**`, `help/**`, every other fixture, the drift ledger |
| **P4.90** — the failover model refresh + the six greeting lines | `crates/quilltap-core/src/services/orchestrator.rs` — ONLY the post-refresh `params` rebuild at `:2861-2863` (and, if measurement says so, the two loop-option sites `:2968-2971` / `:3046-3049`); `crates/quilltap-core/src/services/chat_create.rs` — ONLY the six log lines at their v4 sites + their unit-level capture tests; `crates/quilltap-harness/tests/orchestrator_tier3_equivalence.rs`; `harness/oracle/cases/orchestrator-tier3.test.ts`; `harness/oracle/fixtures/orchestrator-tier3.json` + `build-orchestrator-fixture.ts`; `crates/quilltap-harness/tests/chat_create_capstone_equivalence.rs` (capture pins only, if the pin idiom lives there); this order's header | `services/native_tool_loop.rs`, `services/text_tool_loop.rs`, `services/primary_stream.rs`, `services/provider_failover.rs`, `services/initial_greeting.rs`, `model/**`, every other harness family (regen-and-run only), `apps/web/**`, `help/**`, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0),
   and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp`) where the family is deterministic across
   regens; where it is not (`regenerate-at-both-pins-and-cmp-is-not-
   universal`), run by name and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast` with the lane's env block
   (its own oracle vars + `QT_V4_ROOT`/`QT_V4_CHECKOUT` at the right pin +
   `QT_NODE` = the node BINARY): report **binaries / passed / failed /
   ignored** from the per-binary lines and the exit code, and the lane's
   families confirmed RUN by non-zero duration. The standing withheld
   family (`brahma_orchestrator_tier3` — P4.89's subject) is withheld BY
   NAME in every lane but P4.89, which restores it.
9. **SPA lanes only (P4.D193):** `npm run lint` (incl. the qt-class guard),
   `npm test` (spec files / tests / 0 failed), `npm run build` clean, then
   the lane's e2e beats by FILE against the release binary, and ONE full
   Playwright run at lane close (numbers recorded; the documented
   intermittents — the P4.D161 pause-toast beats, the P4.d17 quill beat —
   are named, re-run by file, and never re-classified as this lane's).
10. **Ownership check:** `git diff --stat main...HEAD` — every path is in
    the lane's Owns column; every MUST-NOT-TOUCH path absent.
11. **Per commit:** `.claude/commands/commit.md` in full — the equivalence
    test is non-negotiable (§1), the invariants (§2), real-instance safety
    (§3), the harness regen (§4), build/test/lint (§5), the CHANGELOG
    entry + version bumps (§7). Commit on the lane branch only; never
    merge to `main`; never `git stash`; never a worktree inside a worktree.
12. **Lane close:** update this order's status header (what landed, what
    was refuted by measurement, what stays OPEN with its named cause),
    append the lane record to `status-log.md` (the regen commands as run,
    the counts, the mutation table, every finding outside the lane's
    ownership for the unifier), and stop. The final gate runs in the
    FOREGROUND of the lane's last turn — a backgrounded gate whose
    notification arrives after the lane reports done is a lane that is
    NOT done (`a-backgrounded-sleep-does-not-gate-your-turn`).
