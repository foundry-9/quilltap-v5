# Work order P4.39 — teach `gen-provider-manifests.mjs` the `imageGenerationModels` field

> **STATUS: OPEN — not started.**
>
> This is phase-4.md candidate 2 (the `7189a968` round's "Next
> candidates" list), promoted into the `f7f1a956` Almanack round as its
> small fourth lane. The defect and the measurement are P4.D48's
> (2026-08-05): the generator's own warning header
> (`harness/oracle/providers/gen-provider-manifests.mjs:23-46`) is the
> authoritative statement of the problem — read it first.
>
> **v4 baseline for this round: `f7f1a956`** (v4 HEAD, 2026-08-05,
> clean tree — the CI/test-infra commit atop `0cde7fbc`, the Almanack).
> The previous baseline was `7189a968`; the two-commit drift is
> classified in the "Round planned" record in `status-log.md`:
> `0cde7fbc` → P4.D49 (ported-surface deltas) + P4.37/P4.38 (the
> Almanack port); `f7f1a956` → NO-PORT (CI only; its one v5-relevant
> consequence — the unconditional jest TZ pin — is P4.D49's item 8).
> **The baseline move is the UNIFIER's** — do NOT edit `CLAUDE.md` or
> `phase-4.md` on the branch.
>
> **Drift-check at lane start**: `git log f7f1a956..HEAD --oneline` +
> `git status --short` in `~/source/quilltap-server`. On drift/dirty,
> regenerate from a detached worktree PINNED at `f7f1a956` (recipe
> `oracle-regen-pinned-v4-worktree` — note the pin needs
> `plugins/node_modules` + per-plugin `plugins/dist/*/node_modules`
> symlinks for anything provider-side, P4.D48's lesson), and report in
> the lane record.
>
> **Siblings (same round):** P4.D49 (drift-proper server), P4.37
> (Almanack server), P4.38 (Almanack SPA). None of them touch the
> provider manifests, the generator, or `provider_manifest/` — this
> lane has that surface to itself (the constraint that blocked P4.D48
> from fixing it in-round is gone).
>
> **Worktree note:** own branch, own checkout; no `git stash`/
> `git worktree`.

## Mandate

Make the manifest regen recipe safe to run as written: teach
`harness/oracle/providers/gen-provider-manifests.mjs` to emit the
`imageGenerationModels` field that P4.6p added to the committed
manifests by hand, prove the taught generator reproduces all nine
committed manifests **byte-identical**, and retire the ⚠ RECIPE ROT
warning block. Do NOT touch: any other lane's files (see Ownership),
any manifest **content** (if the regen shows a real content diff
beyond byte-identity, STOP and report — that is v4 drift or a P4.6p
transcription bug, not yours to resolve silently).

## Survey-verified starting points (2026-08-05, at `f7f1a956`)

- The generator: `harness/oracle/providers/gen-provider-manifests.mjs`
  — the AUGMENTATION table pattern, the "MUST run from the
  quilltap-server checkout root" rule, and the ⚠ RECIPE ROT block at
  lines 23-46 recording P4.D48's measurement: a naive regen deletes
  `imageGenerationModels` from exactly five manifests (google, grok,
  openai, openrouter, z_ai); `anthropic.json` + the other three are
  byte-identical.
- The committed manifests:
  `crates/quilltap-core/src/provider_manifest/manifests/*.json`
  (`imageGenerationModels` is `#[serde(default)]` Rust-side, which is
  what made the deletion silent).
- The three built-plugin providers: `plugin.getImageGenerationModels()
  .map(m => m.id)` works on the built bundle for **openai, google,
  openrouter** (P4.D48's finding, restated in the warning header).
- The two source-level providers (verified fresh today — the `.ts`
  sources ship inside `plugins/dist/`, so the generator CAN read them
  from the checkout it already requires):
  - grok: `plugins/dist/qtap-plugin-grok/image-provider.ts:17` —
    `readonly supportedModels = ['grok-imagine-image',
    'grok-imagine-image-pro', 'grok-2-image']`
  - z-ai: `plugins/dist/qtap-plugin-z-ai/image-provider.ts:16` —
    `const SUPPORTED_MODELS = ['cogview-4-250304', 'glm-image']`
- The consuming differential: `provider_registry_equivalence`
  (harness) — the byte-agreement proof for the manifests.
- The transcription discipline: memory note
  `byte-exact-static-data-transcription` ("ship the generator") and
  P4.6p's original source-level transcription precedent.

## Deliverables

### Tier 1 — must land

1. **The field, from the built plugins where possible.** For openai,
   google, openrouter: emit `imageGenerationModels` from
   `plugin.getImageGenerationModels().map(m => m.id)`, in the same
   position/order as the committed manifests carry it.
2. **The field, from source for grok + z-ai.** Prefer reading the
   shipped `plugins/dist/<dir>/image-provider.ts` at generation time
   and extracting the array (a narrow, anchored regex on the two known
   shapes above), failing LOUDLY (non-zero exit, named provider) if
   the pattern no longer matches — so the generator stays
   self-updating instead of hardcoding a second copy that rots. If
   that proves brittle, the fallback is a hardcoded table with a
   verification read that diffs it against the source file and fails
   on mismatch. Either way: no silent staleness.
3. **The byte-identity proof.** Regenerate into a scratch dir and
   `diff -r` against `crates/quilltap-core/src/provider_manifest/
   manifests/` — the diff must be EMPTY for all nine files. Then
   regenerate straight into the committed dir (the recipe as written)
   and show `git status` clean. Any non-empty diff = STOP and report
   (see Mandate).
4. **The mutation proof.** Temporarily disable the new field emission
   and confirm the scratch diff shows exactly the five files and
   exactly that key (reproducing P4.D48's measurement) — proving the
   check bites — then restore. Record the fingerprint in the lane
   record.
5. **Retire the warning.** Replace the ⚠ RECIPE ROT block with a short
   note recording the repair (date, this order) and the grok/z-ai
   source-read mechanism; update the regen recipe comment to drop the
   "carry the five lines across by hand" instruction.
6. **Re-run the consumer**: `provider_registry_equivalence` by name,
   `--nocapture`, zero SKIP, over an oracle regenerated fresh at
   `f7f1a956`.

### Tier 2 — should land

7. The `perl-base` CVE purge on v5's Docker image (P4.D48's caveat,
   phase-4.md candidate-4 pool): if it is genuinely a one-line
   `apt-get remove`/base-image tweak in the Dockerfile with a clean
   `docker build` + the P4.10 container walk still green, take it here
   (no other lane owns the Dockerfile this round). If it turns out
   non-trivial, defer with a note — it is a rider, not the mandate.

### Tier 3 — explicitly out of scope

- Any change to manifest CONTENT or the `ProviderManifest` struct.
- The provider corpora (request-envelopes / google-wire /
  response-bodies) — untouched by this lane.

## The differential requirement

This lane ports no v4 behavior — it repairs a transcription tool. The
equivalence obligations are: (a) the byte-identity regen proof
(deliverable 3), which IS the differential for a generator ("ship the
generator" — the committed artifact must be exactly what the tool
emits); (b) `provider_registry_equivalence` green over a fresh oracle
(deliverable 6).

## Fixtures

None consumed, none delivered. The committed manifests must come out
of this lane byte-identical.

## Ownership (binding)

- **This lane owns:** `harness/oracle/providers/
  gen-provider-manifests.mjs`; `crates/quilltap-core/src/
  provider_manifest/manifests/*.json` (byte-identity expected — see
  Mandate); the Dockerfile (tier 2 only). Version bumps: none expected
  (the generator is not a crate; the manifests should not change). If
  tier 2 lands, no crate bump either (Docker packaging only).
- **Do NOT touch:** `crates/quilltap-core/src/**` outside the
  manifests dir, `apps/web/**`, `api/types.rs`, the llm_logs /
  logging / schema surfaces (P4.D49's), the almanack surfaces
  (P4.37/P4.38's).
- `CHANGELOG` + `status-log.md` are append-only (union-merge at
  unification).

## Verification gate before declaring done

1. `cargo fmt --all --check`; clippy `-D warnings` on both feature
   sets (nothing should change, but the gate is the gate).
2. The scratch-dir byte-identity diff (empty) + the committed-dir
   regen with clean `git status` + the mutation fingerprint.
3. `provider_registry_equivalence` by name, `--nocapture`, zero SKIP,
   fresh oracle at `f7f1a956`.
4. `cargo test --workspace --no-fail-fast` (no env vars needed beyond
   the family's own).
5. If tier 2 taken: `docker build` clean + the P4.10 container walk
   notes re-checked.
6. Per-commit rule: follow `.claude/commands/commit.md`.
