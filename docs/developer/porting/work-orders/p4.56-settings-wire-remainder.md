# Work order P4.56 — the settings-wire remainder: B2 data-retention present-`null` + the P4.55 banked smalls — v4 `f8973813`

**Status:** CLOSED (2026-08-22, branch
`claude/p4-56-settings-wire-remainder-becfb1`; unified same day on
`unify/f8973813-round`). All five Tier-1 items + both Tier-2 items landed, in
the ordered sequence: the harness rewire FIRST (the `dataRetention` leg now
decodes through the `Request` enum's serde exactly as the wire does, with
`seedDataRetention` + the `after` refetch — proven behavior-neutral on the
pre-existing arms before any new arm), then B2 RED-FIRST (`dr_put_null`
measured red: v4 400 `Validation error`, v5 200 silent-keep) and fixed via
`double_option` + the three-arm engine match, pinned by `dr_put_null` +
`dr_put_null_writes_nothing` (family floor 13 → 15, 141 cases). The
`quilltap-web` REST edge landed decoding through the `Request` enum (never a
hand-built variant — the Taboo §3 lesson) **and uncovered two real bugs, both
fixed + pinned**: `CoreResponse::BrahmaConsole` missing from `unwrap_to_http`'s
success arm since P4.D57 (both brahma-console REST edges 500'd on every
SUCCESS), and the two data-retention handlers leaking `DbError` text where v4
answers fixed sentences. The groups cleared-null pin landed with ZERO source
change (fresh oracle measured: v4 echoes `"description": null`, present not
omitted). `settings_wire_actions` builds its own fixture (proven from a clean
/tmp: 5/5). Tier 2: the float-literal fix via `normalize_js_numbers` on both
merged bags, red-first through the new float-SENSITIVE `norm_float_exact`
comparand (the family's own `canon_numbers` would have made the arms vacuous);
the shared `classify_api_key_id`/`classify_base_url` readers extracted with
consequences kept per-site, behavior-neutral (no NDJSON movement) AND
mutation-proven live at all three sites. Tier 3: the SPA survey answered NO
follow-up owed (the card cannot send `null` or omit the key); 💸 the
invalid-config 400s stay on the dogfood queue with the data-retention leg
added. Recorded for a future order: taboo/brahma still re-derive the tri-state
at three call sites each — the data-retention edge demonstrates the
decode-once alternative. A fixture-vintage repair recorded (the `chat-send-*`
fixture lacks `instance_settings`; the web test creates it with v4's DDL, v4
creating it lazily in its version guard). Mid-lane incident: v4's checkout
went DIRTY (later committed as `65f3476e`, NO-PORT) — the whole gate regen ran
from a lane pin at `f8973813`. Lane record: `status-log.md` → "P4.56".

## Preamble

- **v4 baseline for this round: `f8973813`** (v4 main, HEAD at planning
  2026-08-22, tree CLEAN, checkout on `main` — the NanoGPT prompt-caching
  commit, which is the SIBLING lane's surface, not this one's; nothing in
  it touches settings). `bugfix` measured at planning: HEAD `3a76b17d`,
  nothing unabsorbed beyond the tests-only `009c49b2`.
- **Drift-check at lane start.** `git log f8973813..main --oneline` AND
  `git diff main bugfix -- lib/ app/ packages/`. If v4 moved, **STOP**
  and report. Pin a detached worktree at `f8973813` for every regen if
  HEAD has moved (all three symlink classes); verify the checkout's
  branch first.
- **Sibling lane:** P4.D105 (`p4.d105-nanogpt-prompt-caching.md`) —
  fully disjoint domains (provider layer vs settings wire). No shared
  contract is needed; see Ownership.
- **Worktree:** per the `carryout` skill; edit inside the worktree, not
  main. `docs/CHANGELOG.md` and `status-log.md` append-only.
- Every item below was CONFIRMED and recorded by P4.55 (status-log →
  "P4.55", §Tier 3 and "Recorded, not changed") — read that lane record
  before starting; it carries the measurements this order summarizes.

## Mandate

Close the named next-round settings items P4.55 confirmed but did not
own: the B2 data-retention present-`null` collapse (with the harness
edge-mapping rewire that must land FIRST, or the arm is vacuous-green),
the groups-side cleared-null pin, the memories float-literal echo nit,
the shared `apiKeyId`/`baseUrl` helper cleanup, and the
`settings_wire_actions` recipe's self-containment. It must NOT touch the
provider layer (`model/**`, `provider_manifest/**`) or any file P4.D105
owns.

## Survey-verified starting points (re-verified 2026-08-22 at planning)

**B2 — the divergence (CONFIRMED by P4.55, re-verified this planning):**

- `crates/quilltap-core/src/api/types.rs:1899` —
  `Request::DataRetentionSettingsUpdate { stale_chat_days }` carries
  `#[serde(default)] ... Option<serde_json::Value>`, so serde maps an
  explicit `null` to `None` indistinguishably from an absent key.
- `crates/quilltap-core/src/api/engine.rs:3934-3941` — builds `{}` from
  `None` and `data_retention_settings_update` keeps the current value at
  200. v4's Zod `.default()` fires only for `undefined`; an explicit
  `null` is a **400** in v4.
- The fix is the established `double_option` pattern
  (`Option<Option<Value>>` — the Taboo §3 lesson, done right by design
  in P4.D57; `api/types.rs:2950`'s own doc comment cites this exact
  request as the precedent) plus the three-arm match in `engine.rs`:
  absent → `{}` (keep), present-value → pass through, present-`null` →
  v4's 400. Measure v4's exact 400 body (the Zod `ZodError.message`
  shape as the settings-routes family already pins for taboo/brahma)
  before writing the arm.
- **`quilltap-web` has NO data-retention REST edge at all** (grepped
  zero hits at planning, again). v4 serves
  `GET/PUT /api/v1/settings/data-retention` (verify the exact v4 route
  path + handler in `app/api/` at lane start — survey, don't guess).
  Model the v5 edge on the taboo pair
  (`crates/quilltap-web/src/text_replacements_routes.rs`
  `taboo_settings_get/put`, wired at `lib.rs:366-368`) — a thin
  dispatch-through-the-`Request`-enum edge, NEVER a hand-built variant
  (the Taboo `double_option` §3 finding: the hand-built web variant is
  exactly what made the differential blind).

**B2 — the harness rewire (MUST land before the null arm):**

- `crates/quilltap-harness/tests/settings_routes_equivalence.rs:218-221`
  calls `settings::data_retention_settings_get/update(db, body)`
  DIRECTLY, bypassing the `Request` enum's serde entirely. A null arm
  added today would pass green against the broken wire — the exact
  vacuous-green class. Rewire wanted (P4.55's own words): a
  `dataRetention` edge mapping mirroring taboo's / brahma's (through the
  serde `Request` path), a `seedDataRetention`, and an `after` refetch
  map entry. Only THEN the arms: present-`null` → 400 (both sides),
  absent → keep, present-value → set, plus a writes-nothing composite on
  the 400 leg (the P4.D95 §3 precedent). Raise the family's row-driven
  stale-oracle floor for the new arms (floors move with every arm batch).

**The groups-side cleared-null pin (NO defect expected):**

- P4.55 measured the store_backed cleared-null echo NOT divergent on the
  projects side (`db/store_backed.rs` untouched — one generic `update`,
  two `StoreEntity` impls, so groups inherits the verdict by
  construction). The owed arm is an oracle pin on the groups family
  (cleared key → explicit-`null` echo in schema position, or whatever v4
  actually answers — record what the fresh oracle says, don't assume the
  projects answer). Zero source change expected; if the arm reddens,
  STOP and report before "fixing" either side.

**The memories float-literal nit (small fix + arm):**

- Zod's `.int()` accepts `200.0`; v4 stores `200` where v5 stores/echoes
  the float bytes (P4.55 "Recorded, not changed"; the memories-config
  validators, `a6870c5a`-round). Locate the v5 validator (the
  memories-config verbs P4.55 landed), match v4's stored bytes, and pin
  with an arm feeding `200.0`. An input no UI produces — the fix is for
  wire-fidelity completeness; keep it small.

**The shared `apiKeyId`/`baseUrl` helper (DRY cleanup, behavior-neutral):**

- P4.55 left the triplicated `apiKeyId`/`baseUrl` match blocks
  (consistent today) with a shared helper named as the future cleanup.
  Extract it; the three settings families' existing arms are the
  behavior pin — zero NDJSON movement expected. Also note P4.55's §7
  re-record: v4's failure on `{"baseUrl": 5, "isDefault": true}` is
  TERMINAL-but-after-side-effects; v5 refuses before any write. That
  edge divergence is recorded, in v5's favor, and this cleanup must not
  change it.

**`settings_wire_actions` self-containment:**

- The family leans on a sibling recipe's /tmp fixture and was measured
  FAILING 0/4 without it (P4.54 record). Make its recipe self-contained
  (build its own fixture or stage the dependency explicitly in its own
  recipe stages, per `recipe-header-conventions`);
  `recipe_sweep.py --self-test` and an attributable by-name `--run`
  prove it.

## Tiered deliverables

### Tier 1 — must land

1. The `quilltap-web` data-retention REST edge (thin, through the
   `Request` enum), mirrored on the taboo pair, with a live wire test.
2. The harness edge-mapping rewire (dataRetention through serde +
   `seedDataRetention` + `after` refetch) — proven by the EXISTING
   arms still passing through the new path before any new arm lands.
3. The `double_option` fix (`api/types.rs` + the `engine.rs` three-arm
   match) + the present-`null` 400 arms with v4's exact body, the
   writes-nothing composite, and the floor raise. Mutation-prove:
   revert the `double_option` and the null arm must go red.
4. The groups-side cleared-null pin arm (expected zero source change).
5. `settings_wire_actions` self-contained + sweep-provable.

### Tier 2 — should land

6. The memories float-literal fix + `200.0` arm.
7. The shared `apiKeyId`/`baseUrl` helper extraction (behavior-neutral;
   existing arms as the pin).

### Tier 3 — explicit deferrals, loud

8. Any SPA surface for data-retention settings: NOT this lane unless a
   card already exists and reads the changed wire — survey, and if v5
   has a data-retention card that would now see 400s, record the
   client-side follow-up by name rather than patching `apps/web` here.
9. 💸 The invalid-config 400s on live surfaces stay on the standing
   dogfood queue (they gain this lane's data-retention leg).

## The differential requirement (restated)

Every arm drives v4's REAL route/lib code through the settings-routes
jest oracle (the established recipe in
`settings_routes_equivalence.rs`'s header; regen through
`harness/tools/recipe_sweep.py`, one sweep at a time). The B2 arms are
meaningful ONLY through the serde `Request` path — item 2 lands first by
construction. Grep the fresh NDJSON for the new arms by name
(`green-regen-is-not-coverage`); assert recorded-only rows' status where
applicable (the P4.55 §5 precedent).

## Fixtures

Consumes: the settings-routes fixture family (committed) + whatever
`settings_wire_actions` currently borrows. Delivers: the
`seedDataRetention` seeding, `settings_wire_actions`' own staging. If a
fixture widens, grep every consumer family first and re-run them by
name; this lane owns dependent-oracle regeneration for its own fixture
moves.

## Ownership

This lane owns:
- `crates/quilltap-core/src/api/types.rs` (the one request variant)
- `crates/quilltap-core/src/api/engine.rs` (the one dispatch arm)
- `crates/quilltap-core/src/settings/**` (the memories float nit + the
  shared-helper extraction, wherever the triplicated blocks live)
- `crates/quilltap-web/src/**` (the new edge + `lib.rs` wiring)
- Harness: `settings_routes_equivalence.rs`, `settings_wire_actions.rs`
  (+ both recipes), the groups family's one new arm
- Version bumps: `quilltap-core`, `quilltap-harness`, `quilltap-web`
  (this lane's own bumps; the unifier accumulates)

It must NOT touch: `crates/quilltap-core/src/model/**`,
`provider_manifest/**`, `harness/oracle/providers/**`,
`request_builder_equivalence`, `response_parse_equivalence`,
`streaming_composer_equivalence`, or `apps/web/**` (see Tier 3 item 8).
`docs/CHANGELOG.md` + `status-log.md` append-only.

## Verification gate (before declaring done)

- `cargo fmt --all --check`; `cargo clippy --workspace --all-targets
  -- -D warnings` plain AND `--features quilltap-core/native-transport`.
- `cargo test --workspace` with the lane's oracle env block — full-log +
  sentinel + background notification (never `| tail`, never a pgrep
  loop).
- The lane's families regenerated FRESH at `f8973813` through the sweep
  driver and re-run BY NAME, zero SKIP; the new arms grepped present in
  the fresh NDJSONs; `recipe_sweep.py --self-test` clean.
- The web edge's live wire test green.
- SPA: no edits expected (Tier 3 item 8); if any land, `npm test` +
  `npm run build` + the affected beats.
- Per-commit: `.claude/commands/commit.md`.
