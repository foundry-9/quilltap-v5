# SURVEY S1 — `8bd080267` (#73) + `49059fb14` (#74) — fresh survey 2026-09-25 at those shas and on v5 main `2aed9a552`

Scope: SERVER side of both commits (`lib/**`, `app/api/**`, `packages/plugin-types/src/**`, plugin SOURCE under `plugins/dist/*/​*.ts` — the `.ts` beside each rebuilt `index.js` IS the source; there is no `plugins/src`), `migrations/**`, `public/schemas/**`, `help/**`. Client hunks are listed ONLY where they read a NEW wire field (§A.19). Parents: `8bd080267^` = `a8292547a` (the five-phase spec commit, docs only); `49059fb14^` = `8bd080267`. Both v4 trees were read with `git show` only. v4's pre-existing `lib/llm/moderation-finish-reason.ts` (bug 93) is UNTOUCHED by both commits and is imported by the new classifier.

Line numbers below are POST-commit at the named sha (phase-1 files at `8bd080267` unless marked p2 = `49059fb14`).

## A. v4, per file

### A.1 `lib/services/dangerous-content/refusal.ts` — NEW (220 lines, p1; unchanged by p2)

Logger: `createServiceLogger('ConciergeRefusal')` (:33).

Exports:
- `export const MODERATION_REJECTION_CODE = 'MODERATION_REJECTED' as const` (:36); private `MODERATION_REJECTION_NAME = 'ModerationRejectionError'` (:39).
- `export type RefusalEvidence = 'typed-error' | 'provider-code' | 'finish-reason' | 'message-pattern' | 'inferred'` (:41-46).
- `export interface RefusalVerdict { refused: boolean; evidence?: RefusalEvidence; detail?: string /* ≤ 200 chars */ }` (:48-53).
- `export interface RefusalInput { error?: unknown; finishReason?: string | null; emptyBody?: boolean; contentWasFlagged?: boolean }` (:55-60).
- `export function classifyRefusal(input: RefusalInput): RefusalVerdict` (:196-215).
- `export function isModerationRefusal(error: unknown): boolean` = `classifyRefusal({ error }).refused` (:218-220).

Constants VERBATIM:
```
PROVIDER_MODERATION_CODES = new Set(['moderation_blocked','content_policy_violation','content_filter','safety','1301'])   // :70-76, compared LOWER-CASED
REFUSAL_MESSAGE_PATTERNS = ['content moderation','content_policy','content policy','safety system','rejected by content','moderation_blocked','responsible ai','declined to generate','blocked by safety','prompt_blocked','image_safety']   // :84-96 (first six = the old isImageModerationError list, in its order)
DETAIL_MAX = 200   // :98
```
Helpers: `truncate(text)` = trim, then `len > 200 ? slice(0,199) + '…' : text` (:100-103 — JS `slice` counts UTF-16 units); `asRecord` (object & non-null) :105-107; `codeString(v)` = non-empty string → itself, FINITE number → `String(v)`, else null (:109-113); `collectCodes(error)` = `[record.code, record.error.code]` in that order, each through `codeString`, absent dropped (:116-126); `messageOf(error)` = `Error → .message`, `string → itself`, object with string `.message` → it, else `''` (:128-134).

Evidence ranking, `classify()` :136-188, FIRST HIT WINS:
1. `error != null` AND `(record.code === 'MODERATION_REJECTED' || record.name === 'ModerationRejectionError')` → `{refused:true, evidence:'typed-error', detail: truncate(providerReason ? `${message} (${providerReason})` : (message || 'moderation rejection'))}` (:144-151). `providerReason` only if a string.
2. `error != null` AND any collected code whose `.toLowerCase()` ∈ PROVIDER_MODERATION_CODES → `{refused:true, evidence:'provider-code', detail: truncate(`code ${code}${message ? `: ${message}` : ''}`)}` — the ORIGINAL-case code in the detail (:154-161).
3. `isModerationFinishReason(finishReason)` (v4 `moderation-finish-reason.ts`, trims+lowercases; set = sensitive, content_filter, content-filter, refusal, safety, prohibited_content, blocklist, spii, image_safety, recitation) → `{refused:true, evidence:'finish-reason', detail: `finish_reason: ${finishReason}`}` — NOT truncated, raw reason (:165-167). Runs even with no error.
4. `error != null` AND `lowered = messageOf(error).toLowerCase()`, `lowered` truthy (non-empty) AND some pattern `includes` → `{refused:true, evidence:'message-pattern', detail: truncate(message)}` (:170-176).
5. `emptyBody && contentWasFlagged` → `{refused:true, evidence:'inferred', detail:'empty response on content the Concierge had flagged'}` (:179-185).
6. else `{ refused: false }` (:187) — NO evidence/detail keys.

Log lines (every call): `logger.debug('Classified a provider outcome for refusal', { refused, evidence, hasError, finishReason: input.finishReason ?? undefined, emptyBody, contentWasFlagged })` (:199-206); when refused also `logger.info('Provider refused on content-moderation grounds', { evidence, detail })` (:208-212).

### A.2 `lib/services/dangerous-content/understudy.ts` — NEW (240 lines, p1)

Logger `'ConciergeUnderstudy'` (:34). Types: `UnderstudyLookup { userId; settings: DangerousContentSettings; exclude?: string[] }` (:36-41); `TextUnderstudyLookup extends … { turnAttachmentMimeTypes?: string[]; filter?: (p: ConnectionProfile) => boolean }` (:43-57); `TextUnderstudy { profile: ConnectionProfile; apiKey }` (:59-62); `ImageUnderstudy { profile: ImageProfile; apiKey }` (:64-67).

`decryptKey(profile, userId)` (:69-84): `!profile.apiKeyId → null`; `repos.connections.findApiKeyByIdAndUserId(apiKeyId, userId)`; `apiKey?.key_value ? key_value : null`; catch → `logger.warn('Could not decrypt an understudy candidate\'s API key', { profileId, error })` → null. `profileCanCarryTurn(profile, mimeTypes)` = `mimeTypes.every(m => profileCanReceiveAttachment(profile, m))` (:86-88).

**`resolveUncensoredTextUnderstudy(lookup)` :93-172 — decision order (returns `TextUnderstudy | null`; NEVER reads `mode`):**
- `eligible(p) = p.userId === userId && !excluded.has(p.id) && p.transport !== 'courier' && (filter ? filter(p) : true)` (:100-104).
- `explicitId = settings.uncensoredTextProfileId`. If explicitId && NOT excluded: `findById`; if found && eligible: `decryptKey`; key non-null → `logger.debug('Text understudy: the configured uncensored profile', { profileId, profileName, provider, model })` → RETURN; key null → `logger.warn('Configured uncensored text profile has no usable API key; scanning instead', { profileId })`. Else (missing/not-owned/courier/filtered) → `logger.warn('Configured uncensored text profile is missing, not owned, or not eligible; scanning instead', { profileId, found: !!explicit, courier: explicit?.transport === 'courier' })`. Else if explicitId (excluded) → `logger.debug('Configured uncensored text profile is excluded on this call', { profileId })` (:107-133).
- Scan: `all = findAll()`; `compatible = all.filter(p => p.isDangerousCompatible === true && eligible(p))`; `canCarry` / `cannotCarry` partition by `profileCanCarryTurn`; if `turnAttachmentMimeTypes.length > 0 && cannotCarry.length > 0` → `logger.info('Deprioritising uncensored candidates that cannot carry this turn', { turnAttachmentMimeTypes, canCarry: names, cannotCarry: names })` (:137-147). NOTE: no `[DangerousContent]` prefix (the old wrapper's line had one).
- `for profile of [...canCarry, ...cannotCarry]`: first with a non-null key → `logger.debug('Text understudy: a discovered uncensored-compatible profile', { profileId, profileName, provider, model })` → RETURN (:149-160).
- `logger.debug('No uncensored text understudy is available', { userId, excluded: exclude, candidates: compatible.length })` → null (:162-167).
- Whole body in try; catch → `logger.error('Text understudy lookup failed', { error })` → null (:168-171). **The resolver swallows lookup failures.**

**`resolveUncensoredImageUnderstudy(lookup)` :177-240** — same shape over `repos.imageProfiles`: explicit gate is `explicit && explicit.userId === userId` (no courier/filter); lines: `'Image understudy: the configured uncensored profile'` (debug), `'Configured uncensored image profile has no usable API key; scanning instead'` (warn, {profileId}), `'Configured uncensored image profile is missing or not owned; scanning instead'` (warn, {profileId, found}), `'Configured uncensored image profile is excluded on this call'` (debug); scan `p.userId === userId && p.isDangerousCompatible === true && !excluded.has(p.id)` in `findAll()` order, first decryptable → `'Image understudy: a discovered uncensored-compatible profile'` (debug); none → `'No uncensored image understudy is available'` (debug, {userId, excluded, candidates}); catch → `logger.error('Image understudy lookup failed', { error })` → null.

### A.3 `lib/services/dangerous-content/image-failover.ts` — NEW (291 lines p1; 323 lines p2)

Logger `'ConciergeImageFailover'`. Exports: `FailoverProfile { id; name; provider; modelName }` (p2 :49-54); `ImageFailoverContext<P = ImageProfile> { userId; chatId?: string | null; purpose: 'tool'|'lantern'|'avatar'|'dialog'; settings; resolveUnderstudy?: (exclude: string[]) => Promise<{profile: P; apiKey} | null>; profileKind?: 'connection'|'image'; primaryVia?: RouteAttemptVia }` (:56-76); `ImageFailoverOutcome<T, P> { result; profile; apiKey; rerouted; trail: RouteAttempt[] }` (:78-86); `type ConciergeTrailError = Error & { conciergeTrail?: RouteAttempt[] }` (:89); `getConciergeTrail(error): RouteAttempt[] | null` — non-empty array on `error.conciergeTrail` else null (:92-98); `generateImageWithConciergeFailover<T, P>(primary, attempt, ctx)` (p2 :199-323).

Private: `attachTrail(error, trail)` sets `error.conciergeTrail = [...trail]` on an object error (try; a frozen error falls through) else wraps `new Error(getErrorMessage(error))` with the trail (:100-112); `truncate` (same 199+'…' rule, undefined for empty) (:114-121); `row(profile, profileKind, via, outcome, extra)` builds `{ profileId, profileName, provider, modelName, via, outcome, ...(profileKind === 'image' ? { profileKind } : {}), ...(trigger), ...(evidence), ...(detail truncated) }` (:123-143) — **key order: profileKind sits BEFORE trigger/evidence/detail, and connection rows OMIT it**; `announce(ctx, kind, refusing, answeringProfileName?)` — no chatId → `logger.debug('No chat to announce the refusal in', { kind, purpose })` and return; else `postConciergeRefusalAnnouncement({ chatId, kind, details: { refusingProvider: refusing.provider, refusingModel: refusing.modelName, answeringProfileName, purpose } })` (:145-165); p2 `ledger(ctx, refusing, verdict, rerouted)` — no chatId → return; else `recordModerationRefusal({ chatId, kind:'image', purpose: ctx.purpose, refusedProfileId, refusedProfileName, provider, modelName, evidence: verdict.evidence, rerouted })` (:171-191).

**Decision order (p2 line numbers):** `logContext = { purpose, chatId: ctx.chatId ?? undefined, primaryProfileId, primaryProvider, primaryModel }` (:206-212).
1. `attempt(primary.profile, primary.apiKey)`; success → `logger.debug('Image call answered first time', logContext)`; RETURN `{ result, profile: primary, apiKey, rerouted:false, trail: [] }` (:215-220).
2. `verdict = classifyRefusal({ error })`; not refused → `logger.debug('Image call failed for a reason that is not a refusal; rethrowing untouched', { ...logContext, error })`; `throw primaryError` UNTOUCHED (no trail) (:222-229).
3. `trail = [ row(primary, profileKind, primaryVia, 'refused', { trigger:'moderation-refusal', evidence, detail }) ]`; `logger.info('Image provider refused on content grounds', { ...logContext, evidence, detail, mode: ctx.settings.mode })` (:232-244).
4. `ctx.settings.mode !== 'AUTO_ROUTE'` → `logger.info('Refusal not rerouted: the Concierge mode does not permit it', { ...logContext, mode })`; `announce(…,'refusal-not-permitted', primary)`; p2: `ledger(…, verdict, false)`; `throw attachTrail(primaryError, trail)` (:248-255).
5. `exclude = [primary.profile.id]`; `understudy = ctx.resolveUnderstudy ? ctx.resolveUnderstudy(exclude) : resolveUncensoredImageUnderstudy({ userId, settings, exclude })`. Null → `logger.warn('Refusal not rerouted: no uncensored understudy is available', logContext)`; `announce(…,'refusal-no-understudy', primary)`; p2 ledger(false); `throw attachTrail(primaryError, trail)` (:258-272).
6. `logger.info('Rerouting a refused image call to an uncensored understudy', { ...logContext, understudyProfileId, understudyName, understudyProvider, understudyModel })`; `attempt(understudy)`. Success → `trail.push(row(understudy, profileKind, 'concierge', 'answered'))`; `logger.info('Uncensored understudy answered a refused image call', { ...logContext, understudyProfileId, understudyName })`; `announce(…,'refusal-rerouted', primary, understudy.profile.name)`; p2 ledger(true); RETURN `{ rerouted: true, trail }` (:276-298). Failure → `understudyVerdict = classifyRefusal({ error })`; push `row(understudy, kind, 'concierge', 'refused', { trigger:'moderation-refusal', evidence, detail })` if refused else `row(…, 'failed', { trigger: classifyFallbackTrigger(err) ?? 'provider-error', detail: getErrorMessage(err) })`; `logger.error('Uncensored understudy also failed a refused image call', { ...logContext, understudyProfileId, understudyName, understudyRefused, error })`; p2 ledger(false) — **the PRIMARY's verdict, never the understudy's**; `throw attachTrail(understudyError, trail)` (:299-322). **A failed understudy posts NO announcement.**

Ledger timing (p2): written AFTER the announcement on every post-refusal exit, exactly once per primary refusal.

### A.4 `lib/services/dangerous-content/provider-routing.service.ts` — REWRITTEN (329 → 207 lines, p1)

REMOVED: `profileCanCarryTurn`, `decryptProfileApiKey`, `decryptImageProfileApiKey`, `isImageModerationError`, `interface PostHocImageReroute`, `resolveUncensoredImageProfileForReroute`, the `getRepositories` + `profileCanReceiveAttachment` imports. Kept exports: `DangerousProviderRouteResult`, `DangerousImageProviderRouteResult`, `resolveProviderForDangerousContent(originalProfile, originalApiKey, settings, userId, turnAttachmentMimeTypes = [])` (:68-135), `resolveImageProviderForDangerousContent(originalProfile, originalApiKey, settings, userId)` (:150-207).

Text wrapper now: `mode !== 'AUTO_ROUTE'` → `{ rerouted:false, connectionProfile: original, apiKey: original, reason: `Mode is ${mode}, no rerouting` }` (unchanged); else `understudy = resolveUncensoredTextUnderstudy({ userId, settings, exclude: [originalProfile.id], turnAttachmentMimeTypes })`; if found: `configured = understudy.profile.id === settings.uncensoredTextProfileId`; `logger.info('[DangerousContent] Rerouting to uncensored text profile', { profileId, profileName, provider, model, configured })` (ONE line replaces the old two); `reason: configured ? `Rerouted to configured uncensored profile: ${name}` : `Rerouted to uncensored-compatible profile: ${name}``; else the unchanged `logger.warn('[DangerousContent] No uncensored provider available, sending to original profile', { originalProfile, originalProvider })` + reason `'No uncensored provider available - sending to regular provider'`. The catch (`'Routing failed'` reason) is now unreachable in practice (the resolver swallows). **Behaviour deltas:** the original profile is EXCLUDED (the old "rerouted to the same id" no-op case is gone); couriers skipped; an explicit pick that is excluded/courier/keyless now WARNS then scans (old code: keyless explicit → warn then scan; not-owned → warn then scan; same); the old `[DangerousContent] Deprioritising…` info line moved to understudy.ts WITHOUT the prefix.

Image wrapper: same pattern; `logger.info('[DangerousContent] Rerouting to uncensored image profile', { profileId, profileName, provider, configured })` (no `model`); reasons `Rerouted to configured uncensored image profile: X` / `Rerouted to uncensored-compatible image profile: X`; keyless/not-owned explicit now WARNS (old image code fell through silently).

### A.5 `lib/services/chat-message/provider-failover.service.ts` — p1 (+394/−…) and p2 (+49)

Imports (p2 :17-22): `describeModerationRefusal`, `resolveUncensoredTextUnderstudy`, `classifyRefusal, type RefusalEvidence`, `recordModerationRefusal`, `postConciergeRefusalAnnouncement`. **`resolveProviderForDangerousContent` is no longer imported here.**

p2 `recordTextRefusal(chatId, refusing: Pick<ConnectionProfile,'id'|'name'|'provider'|'modelName'>, evidence, rerouted)` (:65-83) → `recordModerationRefusal({ chatId, kind:'text', purpose:'chat', refusedProfileId, refusedProfileName, provider, modelName, evidence, rerouted })`.

**`attemptEmptyResponseRecovery` changes** (p2 :181-336): `connectionProfile` REMOVED from the destructure (the "Both … returned empty" log now names `state.effectiveProfile`); after the opening verdict: `openingProfile = state.effectiveProfile`, `uncensoredRecovered = false`, `turnRefusal = openingVerdict.outcome === 'refused' ? { profile, evidence } : null`, `recordOpeningRefusal()` closure (:184-195). Same-provider retry branch: after the retry verdict, `if (!turnRefusal && retryVerdict.outcome === 'refused') turnRefusal = { profile: state.effectiveProfile, evidence: retryVerdict.evidence }` (:244-246 — the review fix; that branch only runs when `!contentWasFlaggedDangerous`, so this can only be `finish-reason`). Uncensored gate is now `state.fullResponse.trim().length === 0 && dangerSettings.mode === 'AUTO_ROUTE'` — **`uncensoredTextProfileId` is NO LONGER required** (:264); it calls `attemptUncensoredRetry({ …, alreadyTried: triedProfileIds, contentWasFlaggedDangerous, refusalWasStated: openingVerdict.outcome === 'refused', substitute: false })`; `uncensoredRetryAttempted = uncensored.attempted; uncensoredRecovered = uncensored.recovered; if (understudyId) triedProfileIds.push(it)` (:265-285). `await recordOpeningRefusal()` before BOTH returns (:326, :335) — rerouted = `uncensoredRecovered`.

**`attemptUncensoredRetry(opts): Promise<UncensoredRetryResult>` (p2 :393-522) step by step:**
- Options (:337-373): `state, dangerSettings, formattedMessages, modelParams, actualTools, useNativeWebSearch, userId, chatId, character: Pick<Character,'id'|'name'>, controller, encoder, preGeneratedAssistantMessageId?, repos?, alreadyTried: string[], contentWasFlaggedDangerous, refusalWasStated, substitute, stop?`. Result `{ attempted; recovered; understudyId? }` (:375-382).
1. `understudy = resolveUncensoredTextUnderstudy({ userId, settings, exclude: [...alreadyTried, state.effectiveProfile.id], turnAttachmentMimeTypes: collectAttachmentMimeTypes(formattedMessages) })` (:401-408).
2. None → `logger.warn('[DangerousContent] No uncensored understudy to retry this turn with', { chatId, provider, model, refusalWasStated })`; if `refusalWasStated` → `postConciergeRefusalAnnouncement({ chatId, kind:'refusal-no-understudy', details: { refusingProvider, refusingModel, purpose:'text' } })`; return `{ attempted:false, recovered:false }` (:411-429). NOTE `refusalWasStated` is true for an `inferred` opening too (the announcement gate is looser than the ledger's).
3. `logger.warn('[DangerousContent] Attempting uncensored retry', { chatId, originalProvider, originalModel, uncensoredProfileId, uncensoredProvider, uncensoredModel, contentWasFlaggedDangerous, refusalWasStated })` (:433-442); status event `{ stage:'rerouting', message:'Retrying with uncensored provider...', characterName, characterId }` (:444-449).
4. try: `reroutedMessages = repos ? adaptMessagesForProfile(formattedMessages, reroute, repos, userId, { chatId }) : formattedMessages`; `if (substitute) resetStreamingBuffersForSwap(state)`; `restreamInto(state, { connectionProfile: reroute, apiKey, formattedMessages: reroutedMessages, modelParams, actualTools, useNativeWebSearch, userId, chatId, character, controller, encoder, preGeneratedAssistantMessageId, stop })` (:451-476).
5. Non-empty → `state.effectiveProfile = reroute; state.effectiveApiKey = key; setRouteVia(state,'concierge')`; `logger.info('[DangerousContent] Uncensored retry succeeded', { chatId, uncensoredProvider, uncensoredModel, responseLength })`; return `{ attempted:true, recovered:true, understudyId }` (:478-489).
6. Empty → `verdict = classifyEmptyBody(state, contentWasFlaggedDangerous)`; `recordRouteFailure(state, reroute, 'concierge', verdict.outcome, verdict.trigger, verdict.detail, verdict.evidence)`; `logger.error('[DangerousContent] Both safe and uncensored providers returned empty', { chatId, safeProvider: state.effectiveProfile.provider, safeModel, uncensoredProvider, uncensoredModel })` (:495-504).
7. catch → `refusal = classifyRefusal({ error })`; refused → `recordRouteFailure(state, reroute, 'concierge', 'refused', 'moderation-refusal', refusal.detail ?? retryMessage, refusal.evidence)` else `recordRouteFailure(…, 'failed', classifyFallbackTrigger(err) ?? 'provider-error', retryMessage)`; `logger.error('[DangerousContent] Uncensored retry failed', { chatId, error })` (:505-518).
8. `if (substitute) resetStreamingBuffersForSwap(state)`; return `{ attempted:true, recovered:false, understudyId }` (:520-521). **Records nothing on the ledger itself.**

**`attemptHardErrorFailover` (p2 :893-1012)** — `AttemptHardErrorFailoverOptions` gains `dangerSettings?: DangerousContentSettings` (:878-888). Order: classify (null → debug 'Error is not fallback-eligible' :911) → has-started-streaming (:920) → the existing `warn '[Failover] Primary call failed; walking the fallback chain'` (:931) → `openingAttempt = recordAttempt(...)` (:944) → **new branch `trigger === 'moderation-refusal'`** (:946-1008):
  a. `refusal = classifyRefusal({ error })`; `recordRouteFailure(state, state.effectiveProfile, state.routeVia, 'refused', trigger, refusal.detail ?? failureMessage, refusal.evidence)`; `refusingProfile = state.effectiveProfile` (:953-956).
  b. `alreadyTried = [...context.alreadyTried]`; if `opts.dangerSettings?.mode === 'AUTO_ROUTE'`: `attemptUncensoredRetry({ …opts fields…, alreadyTried, contentWasFlaggedDangerous: context.dangerous, refusalWasStated: true, substitute: true, stop })` (:961-978); recovered → p2 `recordTextRefusal(chatId, refusingProfile, refusal.evidence, true)`; return `{ recovered:true, attempts:[openingAttempt], tierPickWasOffered:false }` (:981-984); else push understudyId; p2 `recordTextRefusal(…, false)` (:986); `logger.debug('[Failover] Uncensored retry did not recover a refusal; walking the chain cleared for the content', { chatId, profileId, understudyId, alreadyTried })`; `return walkFallbackChain({ ...opts, context: { ...context, dangerous: true, alreadyTried } }, openingAttempt)` (:991-1000).
  c. Not Auto-Route → `logger.info('[Failover] Refusal not rerouted to an uncensored profile: the Concierge mode does not permit it', { chatId, mode: opts.dangerSettings?.mode })`; p2 `recordTextRefusal(…, false)`; `return walkFallbackChain(opts, openingAttempt)` with the context's OWN `dangerous` flag (:1003-1008). No announcement here.
→ non-refusal path unchanged: `recordRouteFailure(…,'failed', trigger, failureMessage)`; `walkFallbackChain(opts, openingAttempt)` (:1011-1013).

Threading: `primary-stream.service.ts` `RunPrimaryStreamOptions.dangerSettings?: DangerousContentSettings` (:184), destructured (:209), passed to `attemptHardErrorFailover` (:398); `orchestrator.service.ts:1488` passes `dangerSettings` into `runPrimaryStream`. (Only the primary stream passes it — the tool loops' failover calls do NOT, so a thrown refusal on a re-stream gets no uncensored retry.)

### A.6 `lib/services/chat-message/route-trail.ts` (p1)

Import swapped `isModerationFinishReason` → `classifyRefusal, type RefusalEvidence` (:23). `recordRouteFailure(state, profile, via, outcome, trigger, detail?, evidence?: RefusalEvidence)` (:64-71). `EmptyBodyVerdict.evidence?: RefusalEvidence` (:112). `classifyEmptyBody(state, contentWasFlaggedDangerous)` (:128-) now: `finishReason = extractFinishReason(state.rawResponse)`; `refusal = classifyRefusal({ finishReason, emptyBody: true, contentWasFlagged })`; refused → `{ outcome:'refused', trigger:'moderation-refusal', evidence: refusal.evidence, ...(refusal.detail ? { detail } : {}) }`; else the unchanged `'failed'/'empty-response'` arm. Byte-equivalent to before for the two old readings (same details), but every call now emits the classifier's debug/info lines.

### A.7 `lib/chat/route-trail-display.ts` (p1) — shared display module (server-side lib, also imported by the client)

`RouteTrailRow` gains `profileKind: 'connection' | 'image'` (:45) and `label: string` (:53). `collapseRouteTrail` sets `profileKind: attempt.profileKind ?? 'connection'`, `label: attempt.profileKind === 'image' ? attempt.profileName : attempt.modelName` (:89-90). `describeOutcome` refused arm: `evidence === 'inferred' ? ' — inferred' : evidence === 'message-pattern' ? ' — by its wording' : ''` (:128-133) → `refused on content grounds${evidence}${detail ? ` (${detail})` : ''}`.

### A.8 `lib/schemas/chat.types.ts` + `lib/database/repositories/chats-messages.ops.ts` (p1)

`RouteAttemptSchema` (:198): `evidence: z.enum(['typed-error','provider-code','finish-reason','message-pattern','inferred']).optional()` (:221); NEW `profileKind: z.enum(['connection','image']).optional()` (:227) — declared AFTER `evidence`, BEFORE `detail`. `MessageEventSchema.routeTrail` doc widened to image-bearing rows (:291-303). `ChatMessageRowSchema.routeTrail` item (`chats-messages.ops.ts:189-199`): same enum + `profileKind` at :198, `detail` :199. No DDL change (JSON column).

### A.9 `lib/llm/extract-finish-reason.ts` (p1, 47 lines) — order now: `choices[0].finish_reason` (string) → `choices[0].finishReason` (camel, :19-22) → `stop_reason` (:26) → **`promptFeedback.blockReason` (non-empty string, :28-33)** → `candidates[0].finishReason` (:36-41) → `status` (:44) → null.

### A.10 `lib/llm/fallback/engine.ts` (p1) — `classifyFallbackTrigger` (:100): FIRST line `if (classifyRefusal({ error }).refused) return 'moderation-refusal'` (:105), BEFORE the token/content-limit non-triggers. Everything else unchanged. Consequence recorded in the As-built: cheap-LLM chains now walk on a thrown refusal.

### A.11 Tool plumbing (p1)

- `lib/chat/tool-executor.ts`: `ToolResult.metadata.routeTrail?: RouteAttempt[]` (:167); copied from `result.routeTrail` (:448).
- `lib/services/chat-message/types.ts`: `ToolMessage.metadata.routeTrail?: RouteAttempt[]` (:208).
- `lib/tools/image-generation-tool.ts`: `ImageGenerationToolOutput.routeTrail?: RouteAttempt[]` (:113).
- `lib/services/chat-message/tool-execution.service.ts` `saveToolMessages` (:240-251): the TOOL `MessageEvent` gains `...(metadata.routeTrail?.length > 0 ? { routeTrail } : {})` AFTER `attachments`; then `logger.debug('Writing a Concierge route trail on a TOOL message', { chatId, toolMessageId, toolName, trailLength })`.

### A.12 `lib/tools/handlers/image-generation-handler.ts` (p1, 1357 lines)

- `saveGeneratedImage` metadata gains `routeTrail?: RouteAttempt[]` (:128) and passes it to `postLanternImageNotification` for the `character-image` bubble (:204).
- NEW `interface ProviderGenerationResult { images: GeneratedImageResult[]; answeringProfile: { provider; modelName; name; id }; routeTrail: RouteAttempt[] }` (:323-329).
- `generateImagesWithProvider(toolInput, imageProfile, userId, dangerSettings, chatId?, callingParticipantId?, primaryVia: RouteAttemptVia = 'primary'): Promise<ProviderGenerationResult>` (:339-478). `attempt(profile, apiKey)` closure: `rerouted = profile.id !== imageProfile.id`; `createImageProvider(profile.provider)`; `buildImageGenParams({ profile, prompt: toolInput.prompt, overrides: toolInputOverrides(toolInput), orientation: requestedOrientation(toolInput), logContext: { context: rerouted ? 'tools.generate_image.concierge-reroute' : 'tools.generate_image', chatId, profileId } })`; `generateImage`; success `logLLMCall({ …, content: revisedPrompt || `Generated ${n} image(s)${rerouted ? ' (Concierge reroute)' : ''}` })` `.catch(warn '[Image Generation] Failed to log image generation to LLM Inspector')`; failure `logLLMCall({ …, error })` `.catch(() => {})`, `throw error`. Chokepoint: `generateImageWithConciergeFailover({ profile: imageProfile, apiKey: imageProfile.apiKey.key_value }, attempt, { userId, chatId, purpose:'tool', settings: dangerSettings, primaryVia })` (:415-420). Catch: `logger.error('Image generation failed:', { errorMessage, conciergeTrail: trail?.map(a => ({ profileName, outcome })) }, error)`; `throw new ImageGenerationError('PROVIDER_ERROR', trail && trail.length > 1 ? `Image generation failed after Concierge reroute: ${msg}` : `Image generation failed: ${msg}`, error)` (:421-434). Rerouted → `logger.info('[Image Generation] Concierge uncensored reroute succeeded', { originalProfileId, fallbackProfileId, fallbackProvider, fallbackModel })` (:439-444). Saves with `model: activeProfile.modelName, provider: activeProfile.provider, routeTrail: outcome.trail.length > 0 ? trail : undefined` (:451-457); returns `{ images, answeringProfile: { id, name, provider, modelName }, routeTrail: outcome.trail }`.
- `runImageGenerationTool` (:1219-1240): passes `finalProfile.id !== imageProfile.id ? 'concierge' : 'primary'` as `primaryVia`; success `message: `Successfully generated ${n} image(s) using ${answeringProfile.modelName}``, `provider/model` from the ANSWERING profile, `...(routeTrail.length > 0 ? { routeTrail } : {})` LAST. Error path: `failedTrail = getConciergeTrail(error instanceof ImageGenerationError ? error.details : error)`; if any → `errorResponse.routeTrail = failedTrail` (:1252-1257), before the existing `provider/model` from `imageProfile`.
- Removed: the whole old try/catch reroute (`isImageModerationError` + `resolveUncensoredImageProfileForReroute`), its `[Image Generation] Image provider rejected for content moderation, rerouting…` info, `Image generation failed (Concierge reroute also failed):` error, the `after Concierge reroute` ImageGenerationError arm.

### A.13 `lib/background-jobs/handlers/character-avatar.ts` (p1, 553 lines) + `story-background.ts` (p1, 897 lines)

Avatar: imports `resolveImageProviderForDangerousContent` (pre-flight, kept) + `generateImageWithConciergeFailover, getConciergeTrail`; `effectiveApiKey: string` (:233). `attemptPortrait(profile, key)` (:311-369): `params = profile.id === imageProfile.id ? avatarParams : buildImageGenParams({ profile, prompt, overrides:{ n:1, style:'natural' }, orientation:'portrait', logContext:{ context:'background-jobs.character-avatar.concierge-reroute', jobId } }).params` (**any non-requested profile — pre-flight reroute OR understudy — logs the `.concierge-reroute` context**); `rerouted = profile.id !== effectiveImageProfile.id`; `logLLMCall` success `content: revisedPrompt || `Generated ${n} image(s)${rerouted ? ' (Concierge reroute)' : ''}``; failure logs `error`, rethrows. Chokepoint `{ userId: job.userId, chatId: payload.chatId, purpose:'avatar', settings: dangerSettings, primaryVia: effectiveImageProfile.id !== imageProfile.id ? 'concierge' : 'primary' }` (:372-380). Catch: `logger.error('[CharacterAvatar] Image generation failed', { context:'background-jobs.character-avatar', jobId, error, conciergeTrail: trail?.map(a => ({ profileName, outcome })) }, error)`; throw `Avatar image generation failed after Concierge reroute: ${msg}` when `trail.length > 1` else `Avatar image generation failed: ${msg}` (:381-396). `effectiveImageProfile = failover.profile`; rerouted → `logger.info('[CharacterAvatar] Concierge uncensored reroute succeeded', { context, jobId, fallbackProfileId, fallbackProvider, fallbackModel })` (:401-408 — NO `rerouteDurationMs` any more). `postLanternImageNotification({ …, routeTrail: failover.trail })` (:551). Removed lines: `'[CharacterAvatar] Image provider rejected for content moderation, rerouting through Concierge uncensored profile'`, `'…Image generation failed (Concierge reroute also failed)'`, the `moderationRejection` field on the error line.

Story background: `uncensoredImageTarget = isDangerousChat && hasUncensoredImageProvider && dangerSettings.mode === 'AUTO_ROUTE'` (:217-218 — NEW third conjunct; changes prompt CRAFTING, not just the reroute). `attemptBackground(profile, key)` (:627-684): `rerouted = profile.id !== imageProfile.id`; `buildImageGenParams({ profile, prompt: finalPrompt!, overrides:{ n:1, style:'natural' }, orientation:'landscape', logContext:{ context: rerouted ? 'background-jobs.story-background.concierge-reroute' : 'background-jobs.story-background', jobId, chatId } })`; same logLLMCall pair. Chokepoint `{ userId, chatId: payload.chatId, purpose:'lantern', settings: dangerSettings }` (no primaryVia — always 'primary') (:687-691). Catch: `logger.error('[StoryBackground] Image generation failed', { context, jobId, error, isDangerousChat, hasUncensoredImageProvider, dangerMode: dangerSettings.mode, conciergeTrail: […] }, error)`; throw `Image generation failed after Concierge reroute: …` / `Image generation failed: …` (:692-708). `activeImageProfile = failover.profile`; rerouted → `logger.info('[StoryBackground] Concierge uncensored reroute succeeded', { context, jobId, originalProfileId, fallbackProfileId, fallbackProvider, fallbackModel })` (:716-722 — this line was previously the deliberately-UNPORTED one in v5; its bag changed). `routeTrail: failover.trail` on the Lantern bubble (:895). **The bug-133 gate (`rerouteAllowed = moderationRejection && isDangerousChat`) is DELETED**; the error bag's `moderationRejection`/`rerouteAllowed` fields are gone.

### A.14 `app/api/v1/images/route.ts` (p1, 590 lines) — the legacy dialog

Imports `generateImageWithConciergeFailover`, `resolveUncensoredTextUnderstudy`, `supportsImageGeneration`, `ConnectionProfile` (:32-35). Settings (:208-226): `chatSettings = repos.chatSettings.findByUserId(user.id)`; `if (chatId) chatForConcierge = repos.chats.findById(chatId)`; catch → `logger.warn('[Images v1] Could not load Concierge settings; using defaults', { chatId: chatId ?? null, error })`; `dangerSettings = resolveDangerousContentSettings(chatSettings ?? null, chatForConcierge).settings` (**now WITH the chat**); `logger.debug('[Images v1] Generate: resolved Concierge settings', { chatId: chatId ?? null, mode, withChat: !!chatForConcierge })`. The pre-flight classify block no longer resolves settings itself. Provider fail-fast: `primaryProvider = createImageProvider(profile.provider, profile.baseUrl)` catch → `badRequest(`${profile.provider} provider does not support image generation`)` (:305-312). `attempt(candidate: ConnectionProfile, key)` (:318-338): reuses `primaryProvider` when `candidate.id === primaryProfileId` else `createImageProvider(candidate.provider, candidate.baseUrl)`; `buildImageGenParams({ profile: candidate, prompt, overrides:{ n, size, quality, style, aspectRatio }, logContext:{ context:'api.v1.images.generate', profileId: candidate.id } })`; `provider.generateImage(params, key)`. Chokepoint (:340-357): `{ userId, chatId: chatId ?? null, purpose:'dialog', settings: dangerSettings, profileKind:'connection', primaryVia: profile.id !== profileId ? 'concierge' : 'primary', resolveUnderstudy: (exclude) => resolveUncensoredTextUnderstudy({ userId, settings, exclude, filter: (c) => supportsImageGeneration(c.provider) }) }`. Rerouted → `logger.info('[Images v1] Concierge rerouted a refused image request', { userId, originalProfileId, answeringProfileId, answeringProvider })`; `profile = failover.profile` (drives the saved file's provider/model) (:359-367). No trail is persisted anywhere by this route (no message row); errors propagate as before (the route's outer handler).

### A.15 `lib/services/concierge-notifications/writer.ts` — p1 (+131) and p2 (+67); post-p2 500 lines

p1 refusal announcements (p2 :388-500): `type ConciergeRefusalKind = 'refusal-rerouted' | 'refusal-no-understudy' | 'refusal-not-permitted'`; `type ConciergeRefusalPurpose = 'tool'|'lantern'|'avatar'|'dialog'|'text'`; `interface ConciergeRefusalDetails { refusingProvider; refusingModel; answeringProfileName?; purpose }`; `interface ConciergeRefusalAnnouncement { chatId; kind; details }`. `refusalCommission(purpose)` → `{ voiced, plain }`: tool `{ 'the commission for a picture', 'an image request' }`, lantern `{ 'the commission for a new backdrop', 'a story background' }`, avatar `{ 'the commission for a new portrait', 'a character portrait' }`, dialog = tool's, text `{ 'the request for a reply', 'this turn' }` (:402-416).

`buildRefusalContent(kind, details)` (:418-432), `painter = `${refusingProvider} ${refusingModel}``, `house = purpose === 'text' ? "the house's usual correspondent" : "the house's usual painter"`:
- rerouted (p2 wording, the 'attached above' sentence REMOVED at :426): `The Concierge regrets to report that ${house} (${painter}) declined ${voiced} on grounds of propriety; he has taken it across the street to ${answeringProfileName ?? 'a more obliging studio'}, who were happy to oblige.`
- no-understudy: `The Concierge regrets to report that ${house} (${painter}) declined ${voiced} on grounds of propriety, and he knows of no more obliging establishment to take it to. Should you care to name one, tick "Uncensored-compatible" on a suitable profile, or choose one in the Concierge's settings.`
- not-permitted: `The Concierge observes that ${house} (${painter}) declined ${voiced} on grounds of propriety. His present instructions forbid him from taking it elsewhere; were he set to Auto-Route, he would have done so.`

`buildRefusalOpaqueContent` (:434-446), `who = `${refusingProvider} ${refusingModel}``: rerouted `Provider ${who} refused ${plain} on content grounds. The Concierge rerouted it to ${answeringProfileName ?? 'an uncensored profile'}.`; no-understudy `Provider ${who} refused ${plain} on content grounds. No uncensored profile is available to retry it; mark a profile "Uncensored-compatible" or choose one in the Concierge settings.`; not-permitted `Provider ${who} refused ${plain} on content grounds. The Concierge mode does not permit rerouting; Auto-Route would have retried it on an uncensored profile.`

`postConciergeRefusalAnnouncement(params): Promise<MessageEvent | null>` (:448-500): `findById(chatId)`; missing → `logger.debug('[ConciergeNotification] Refusal announcement skipped: chat not found', { context:'concierge-notifications', chatId, kind })` → null; message literal IN ORDER `{ type:'message', id: randomUUID(), role:'ASSISTANT', content, opaqueContent, attachments: [], createdAt: now, participantId: null, systemSender:'concierge', systemKind:'refusal' }` (**a NEW systemKind**); `addMessage`; `logger.info('[ConciergeNotification] Refusal announced', { context, chatId, messageId, kind, purpose, refusingProvider, refusingModel, answeringProfileName })`; catch → `logger.error('[ConciergeNotification] Failed to post refusal announcement', { context, chatId, kind, error }, error)` → null. NO dedupe.

p2 auto-switch (:184-264): `ConciergeManualKind` gains `'auto-flagged-refusals'`; `interface ConciergeAutoFlagDetails { count: number; lastProvider: string; lastModel?: string | null }`; `TIMES_WORDS = ['Never','Once','Twice','Three times',…,'Ten times']` (11 entries), `COUNT_WORDS = ['No','One',…,'Ten']`; `refusalWho(details)` = null when no `lastProvider`, else `lastModel ? `${lastProvider} ${lastModel}` : lastProvider`.
`buildAutoFlagContent(details)`: `count = details?.count ?? 0`; `declined = count === 1 ? `The house's regular staff have declined this conversation on grounds of propriety${who ? ` — ${who}, to be precise` : ''}.` : `${count >= 2 && count < 11 ? `${TIMES_WORDS[count]} now` : 'More than once now'} the house's regular staff have declined this conversation on grounds of propriety${who ? ` — most recently ${who}` : ''}.``; returns `${declined} The Concierge has taken the liberty of moving the whole affair to the uncensored desk; you may move it back from the sidebar whenever you wish.` (count 0 → 'More than once now'). `buildAutoFlagOpaqueContent`: `counted = 1..10 → COUNT_WORDS[count] else String(count)`; `noun = count === 1 ? 'moderation refusal' : 'moderation refusals'`; `${counted} ${noun}${who ? ` (last: ${who})` : ''}. The Concierge switched this chat to Flagged; change it in the sidebar.` `buildManualContent(kind, details?)` / `buildManualOpaqueContent(kind, details?)` gain the case; `ConciergeManualAnnouncement.details?: ConciergeAutoFlagDetails`; `postConciergeManualAnnouncement` threads `details`. The manual post's object literal, log lines and `systemKind: 'danger'` are otherwise unchanged.

### A.16 `lib/services/lantern-notifications/writer.ts` (p1) — `PostParams.routeTrail?: RouteAttempt[] | null` (:50); destructured `routeTrail = null` (:96); message literal gains `...(routeTrail && routeTrail.length > 0 ? { routeTrail } : {})` AFTER `systemKind` (:128); the success log gains `routeTrailLength: routeTrail?.length ?? 0` (:151).

### A.17 p2 — the ledger

**`lib/services/dangerous-content/refusal-ledger.ts` — NEW (250 lines).** Logger `'ConciergeRefusalLedger'`. Imports `getConciergeState` (chat-override), `applyConciergeFlip` (manual-flip), `DEFAULT_AUTO_SWITCH_AFTER_REFUSALS, resolveDangerousContentSettings` (resolver).
- `interface RefusalRecord { chatId; kind:'text'|'image'; purpose:'chat'|'cheap'|'tool'|'lantern'|'avatar'|'dialog'; refusedProfileId; refusedProfileName; provider; modelName?: string|null; evidence: RefusalEvidence | undefined; rerouted: boolean }` (:43-54); `interface RecordRefusalResult { count: number | null; switched: boolean }` (:56-64); `interface LastRefusal { provider; modelName?: string|null }` (:67-70).
- `RECORDABLE_EVIDENCE = Set(['typed-error','provider-code','finish-reason','message-pattern'])` (:73-78); `export function isRecordableRefusalEvidence(evidence)` = `!!evidence && set.has(evidence)` (:80-82).
- `isJobChild()` = `process.env.QUILLTAP_JOB_CHILD === '1'` (:84-86). `switchChecks = new Map<string, Promise<unknown>>()` per-process (:94).
- **`recordModerationRefusal(rec)` (:102-146):** `logContext = { chatId, kind, purpose, refusedProfileId, refusedProfileName, provider, modelName: rec.modelName ?? undefined, evidence, rerouted }`. (1) `!rec.chatId` → `logger.debug('Refusal not recorded: no chat to record it on', logContext)` → `{ count:null, switched:false }`. (2) not recordable → `logger.debug('Refusal not recorded: the evidence is not a stated refusal', logContext)` → same. (3) try: `at = new Date().toISOString()`; `refusedBy = { provider, modelName: rec.modelName ?? null }`; `count = repos.chats.incrementModerationRefusalCount(chatId, at, refusedBy)`; child → `logger.info('Moderation refusal recorded (buffered; the parent decides on the auto-switch)', logContext)` → `{ count:null, switched:false }`; parent → `logger.info('Moderation refusal recorded', { ...logContext, count })`; `{ switched } = maybeAutoSwitchAfterRefusal(chatId, refusedBy)`; return `{ count: typeof count === 'number' ? count : null, switched }`. catch → `logger.error('Failed to record a moderation refusal', { ...logContext, error })` → `{ count:null, switched:false }`. NEVER throws.
- **`maybeAutoSwitchAfterRefusal(chatId, lastRefusal?)` (:157-174):** child → `logger.warn('Auto-switch check refused in the job child; the parent decides', { chatId })` → `{ switched:false }`; else per-chat chaining: `previous = switchChecks.get(chatId) ?? Promise.resolve()`; `current = previous.catch(() => undefined).then(() => runAutoSwitchCheck(chatId, lastRefusal))`; set; `try { return await current } finally { if (switchChecks.get(chatId) === current) delete }`.
- **`runAutoSwitchCheck` (:176-250), in order:** `chat = findById`; missing → debug `'Auto-switch check skipped: chat not found'` {chatId}; `state = getConciergeState(chat)`; `!== 'monitored'` → debug `'Auto-switch check skipped: the chat is not Monitored'` {chatId, state}; `chatSettings = repos.chatSettings.findByUserId(chat.userId)`; `{ settings, source } = resolveDangerousContentSettings(chatSettings, chat)`; `threshold = settings.autoSwitchAfterRefusals ?? 2`; `{ count } = getModerationRefusalLedger(chatId)`; `decision = { chatId, count, threshold, mode: settings.mode, source }`; `threshold <= 0` → debug `'Auto-switch check: the auto-switch is off'` decision; `mode !== 'AUTO_ROUTE'` → debug `'Auto-switch check: the Concierge mode does not permit it'`; `count < threshold` → debug `'Auto-switch check: below the threshold'`; **re-read** `fresh = findById(chatId)`; `!fresh || getConciergeState(fresh) !== 'monitored'` → `logger.info('Auto-switch abandoned: the chat left Monitored during the check', { ...decision, state: freshState })` → false; `result = applyConciergeFlip(chatId, 'flagged', fresh, { by:'concierge', reason:'refusals', refusals: { count, lastProvider: lastRefusal?.provider ?? '', lastModel: lastRefusal?.modelName ?? null } })`; `logger.info('The Concierge switched a chat to Flagged after repeated refusals', { ...decision, changed: result.changed, lastProvider, lastModel: lastRefusal?.modelName ?? undefined })`; return `{ switched: result.changed }`. catch → `logger.error('Auto-switch check failed', { chatId, error })` → false. (Threshold gate precedes the mode gate; `getConciergeState` = `conciergeOverride==='UNCENSORED'→uncensored; 'OFF'→vouched; isDangerousChat===true→flagged; else monitored`, unchanged file.)

**`lib/database/repositories/chats.repository.ts` (p2 :543-628):** the columns are DELIBERATELY absent from `ChatMetadataSchema`/`ChatMetadataBaseSchema` (confirmed: neither declares them; the `chats` collection is created from `ChatMetadataBaseSchema`, :61). `incrementModerationRefusalCount(chatId, at, refusedBy?)` (:567-598): `safeQuery(collection.updateOne({ id }, { $inc: { moderationRefusalCount: 1 }, $set: { lastModerationRefusalAt: at } }) → matchedCount > 0, 'Failed to record a moderation refusal', { chatId }, false)`; not updated → `logger.warn('Moderation refusal not recorded: chat not found', { chatId })` → 0; else `{ count } = getModerationRefusalLedger(chatId)` (a SECOND statement — a read-after-write, not RETURNING); `logger.debug('Moderation refusal recorded on the chat ledger', { chatId, count, at, provider: refusedBy?.provider, modelName: refusedBy?.modelName ?? undefined })`; return count. The SQLite translator renders `$inc` as `"moderationRefusalCount" = "moderationRefusalCount" + ?` (`query-translator.ts:534-538`), so the SQL is `UPDATE chats SET "lastModerationRefusalAt" = ?, "moderationRefusalCount" = "moderationRefusalCount" + ? WHERE …` ($set clauses precede $inc in the translator; `refusedBy` NOT stored). `getModerationRefusalLedger(chatId): { count; lastAt }` (:601-613): `findOne` → `typeof row.moderationRefusalCount === 'number' ? it : 0`, `typeof lastAt === 'string' ? it : null`; safeQuery fallback `{ count:0, lastAt:null }`. `resetModerationRefusalLedger(chatId)` (:619-628): `updateOne({ id }, { $set: { moderationRefusalCount: 0, lastModerationRefusalAt: null } })`, fallback false; `logger.debug('Moderation refusal ledger reset', { chatId })`.

**`lib/background-jobs/host/job-dispatcher.ts` (p2):** `applyWritesUnsafe` ends with `cleanupStagingDirs(writes, jobId); dispatchInvalidations(writes); await runRefusalLedgerChecks(writes, jobId)` (:381-388). `export const REFUSAL_LEDGER_INCREMENT = 'chats.incrementModerationRefusalCount'` (:392). `export function chatsWithRecordedRefusals(writes): Map<string, { provider; modelName? } | null>` (:399-413): for each write with that method, `[chatId, , refusedBy] = args`; skip non-string/empty chatId; `who = refusedBy is object with string provider ? refusedBy : null`; `chats.set(chatId, who ?? chats.get(chatId) ?? null)` — **a later increment WITHOUT a provider does NOT erase an earlier provider** (the code comment "later increments overwrite earlier ones" holds only when the later one names a provider). `runRefusalLedgerChecks` (:422-440): empty → return; `log.debug('Child batch recorded moderation refusals; running the auto-switch check', { jobId, chatIds })`; dynamic `import('@/lib/services/dangerous-content/refusal-ledger')`; `for each → await maybeAutoSwitchAfterRefusal(chatId, lastRefusal)`; catch → `log.error('Refusal-ledger auto-switch check failed after a committed batch', { jobId, error })`. Runs inside the apply chain, after every partition committed, not when a partition failed. The child proxy buffers `increment*` (`child-repositories-proxy.ts:164` — pre-existing prefix list; NOT changed by these commits).

**`lib/memory/cheap-llm-tasks/core-execution.ts` (p2):** `ProviderResponse.finishReason?: string | null` (:29); `sendToProvider` returns `finishReason: response.finishReason` on all three arms (:379, :387, :397). In `runCheapLLMTask` (:580-637): `emptyVerdict = response.content.trim() === '' ? classifyRefusal({ finishReason: response.finishReason, emptyBody: true }) : null` (NO `contentWasFlagged` → an `inferred` verdict is impossible here); `recordCheapRefusal(rerouted)`: `!chatId || !emptyVerdict?.refused → return`; `profile = selection.connectionProfileId ? uncensoredFallback?.availableProfiles.find(id) : undefined`; `recordModerationRefusal({ chatId, kind:'text', purpose:'cheap', refusedProfileId: selection.connectionProfileId ?? '', refusedProfileName: profile?.name ?? `${provider} ${modelName}`, provider: selection.provider, modelName: selection.modelName, evidence, rerouted })`. Placement: uncensored branch — `attempt(uncensoredSelection)` in try; throw → `recordCheapRefusal(false)` then rethrow; retry empty → `recordCheapRefusal(false)` then the existing `Empty response from both…` throw; retry success → `recordCheapRefusal(true)` before `response = retryResponse`; NO uncensored branch → `recordCheapRefusal(false)` (else arm). `shouldAttemptUncensoredFallback` unchanged.

**`lib/schemas/settings.types.ts` (p2):** `DangerousContentSettingsSchema` gains `autoSwitchAfterRefusals: z.number().int().min(0).max(10).default(2)` (:415-416) — declared LAST, after `customClassificationPrompt`; `ChatSettingsSchema`'s `dangerousContentSettings` default object gains `autoSwitchAfterRefusals: 2` (:695). `chat-settings.repository.ts` default row gains `autoSwitchAfterRefusals: 2` (:229, after `showWarningBadges`). `resolver.service.ts`: `export const DEFAULT_AUTO_SWITCH_AFTER_REFUSALS = 2` (:29); `DEFAULT_DANGEROUS_CONTENT_SETTINGS.autoSwitchAfterRefusals: 2` (:42); `VOUCHED_SAFE_… .autoSwitchAfterRefusals: 0` (:60).

**`lib/services/dangerous-content/manual-flip.ts` (p2, 172 lines):** `export interface ApplyConciergeFlipOptions { by?: 'operator'|'concierge'; reason?: 'refusals'|'classifier'; refusals?: ConciergeAutoFlagDetails }` (:48-56); `export const MODERATION_REFUSALS_CATEGORY = 'moderation-refusals'` (:59); `applyConciergeFlip(chatId, requested, chat, options = {})` (:79); `by = options.by ?? 'operator'`. `'flagged'`: `autoByRefusals = by === 'concierge' && options.reason === 'refusals'`; `repos.chats.update(chatId, { conciergeOverride: null, isDangerousChat: true, dangerScore: null, dangerCategories: by === 'concierge' ? ['moderation-refusals'] : [], dangerClassifiedAt: now, dangerClassifiedAtMessageCount: chat.messageCount ?? 0 })`; announce `autoByRefusals ? { kind:'auto-flagged-refusals', details: options.refusals } : { kind:'manual-flagged' }` (:95-120) — note `by:'concierge'` with `reason:'classifier'` stamps the category but posts `manual-flagged`. `'monitored'`: after the existing update, `await repos.chats.resetModerationRefusalLedger(chatId)` (:136), then the existing manual-resumed/manual-safe announcement. Final log: `logger.info(by === 'concierge' ? 'Concierge state flipped by the Concierge' : 'Concierge state flipped manually', { chatId, from, to, by, reason: options.reason })` (:163-169) — the message text is unchanged for operators but the bag gains `by` and `reason` (undefined for operators).

**`lib/services/dangerous-content/resolver.service.ts` (p2)** — see above; nothing else moved.

### A.18 Migration + DDL + export schema

**`migrations/scripts/add-chat-refusal-ledger.ts` — NEW (98 lines, p2).** `id: 'add-chat-refusal-ledger-v1'`, `description: 'Add the Concierge refusal ledger (moderationRefusalCount, lastModerationRefusalAt) to chats'`, `introducedInVersion: '4.10.0'`, `dependsOn: ['add-chat-concierge-override-v1']`. `shouldRun`: SQLite only, `chats` exists, either column missing. `run`: `addColumnIfMissing('chats', 'moderationRefusalCount', 'INTEGER NOT NULL DEFAULT 0')`; `addColumnIfMissing('chats', 'lastModerationRefusalAt', 'TEXT DEFAULT NULL')`; `logger.info('Added the Concierge refusal ledger columns to chats', { context:'migration.add-chat-refusal-ledger', columnsAdded, durationMs })`; result `message: `Added ${n} refusal ledger column(s) to chats table``; catch → `logger.error('Failed to add the Concierge refusal ledger columns', …)`. NO backfill UPDATE. DDL rendered by `addColumnIfMissing`: `ALTER TABLE "chats" ADD COLUMN "moderationRefusalCount" INTEGER NOT NULL DEFAULT 0` / `ALTER TABLE "chats" ADD COLUMN "lastModerationRefusalAt" TEXT DEFAULT NULL`.
`migrations/scripts/index.ts`: import at :315-316 (after `compressChatMessageTextMigration`'s import); appended to the `migrations` array at :823-824 **after `compressChatMessageTextMigration`** (the last entry); named export at :1219-1220. `lib/startup/prettify.ts:121`: `'add-chat-refusal-ledger-v1': "Opening the Concierge's ledger of refusals"` (NO-PORT class). `migrations/README.md` row: `| add-chat-refusal-ledger-v1 | 4.10.0 | Add the Concierge refusal ledger (moderationRefusalCount, lastModerationRefusalAt) to chats | add-chat-concierge-override-v1 |`.
`docs/developer/DDL.md` (p2): two lines inserted after `conciergeOverride` and before `answerConfirmationOverride`: `"moderationRefusalCount" INTEGER NOT NULL DEFAULT 0,  -- …Deliberately absent from ChatMetadataSchema… Not exported in .qtap bundles. Added by add-chat-refusal-ledger-v1.` and `"lastModerationRefusalAt" TEXT DEFAULT NULL, -- …`. (p1 DDL.md: the `chat_messages.routeTrail` comment gains `profileKind?` in its key list.)

**`public/schemas/qtap-export.schema.json` (p1 only; p2 leaves it unchanged — the ledger is NOT exported):** `routeTrail.description` rewritten (image-bearing rows, `profileKind 'image'`, "on an assistant turn those stay authoritative"); `profileId.description` → `"Connection or image profile that was asked. …"`; `evidence.enum` → `["typed-error","provider-code","finish-reason","message-pattern","inferred"]` with the strongest-first description; NEW `"profileKind": { "type": "string", "enum": ["connection","image"], "description": "What kind of profile was asked. Absent means 'connection' (every trail written before image calls had trails); 'image' = an image generation profile, whose modelName is the image model." }` inserted between `evidence` and `detail`. `required` unchanged.

### A.19 `@quilltap/plugin-types` 2.8.0 + plugin SOURCE (p1)

`packages/plugin-types/src/common/errors.ts` (:52-80):
```ts
export class ModerationRejectionError extends ProviderApiError {
  public override readonly code = 'MODERATION_REJECTED';
  constructor(message: string, statusCode?: number, public readonly providerReason?: string, pluginName?: string) {
    super(message, statusCode, undefined, pluginName);
    this.name = 'ModerationRejectionError';
  }
}
```
Exported from `src/index.ts` (:239); `PLUGIN_TYPES_VERSION = '2.8.0'`; package 2.7.1 → 2.8.0; root `package.json` dep `^2.7.0 → ^2.8.0`, version `4.10.0-dev.79 → .81` (p2 → `.83`). Plugin bundles rebuilt: openai 1.0.65, grok 1.0.57, google 1.1.54, openrouter 1.0.65, z-ai 1.1.30 (index.js regenerated; manifest/package bumped; anthropic/deepseek/ollama/nanogpt/… untouched).

Per plugin (image side = what now throws `ModerationRejectionError`; text side = finish reasons):
- **openai** `image-provider.ts`: `MODERATION_CODES = Set(['moderation_blocked','content_policy_violation'])`; `export function toOpenAIImageModerationError(error)`: non-object → as is; `code = err.code (string) ?? err.error?.code (string)`; `message = err.message (string) ?? ''`; `(code ∈ set) || message.toLowerCase().includes('safety system')` → `new ModerationRejectionError(message || 'OpenAI refused this image request on content grounds', typeof err.status === 'number' ? status : undefined, code, 'qtap-plugin-openai')`; else the error unchanged. `generateImage` wraps `client.images.generate` in try/catch; mapped → `logger.info('OpenAI Images API refused the request on content grounds', { context:'OpenAIImageProvider.generateImage', model, providerReason })`; `throw mapped`. `provider.ts`: `buildRawResponse`'s `finish_reason` was `toolCalls.length > 0 ? 'tool_calls' : 'stop'` → now `this.getFinishReason(response)`; new `hasRefusal(response)` = any output item `type === 'refusal'`, or a `message` item whose `content` has a part `type === 'refusal'`; `getFinishReason`: function_call → 'tool_calls'; **hasRefusal → 'refusal'** (new, before status); completed → 'stop'; incomplete → `incomplete_details?.reason || 'length'`; failed → 'error'; else 'stop'. Streaming: `event.type === 'response.completed' || event.type === 'response.incomplete'` both set `finalResponse` (was completed only).
- **grok** `image-provider.ts`: same shape, `MODERATION_CODES` identical, message test `.includes('content moderation')`, default message `'xAI refused this image request on content grounds'`, providerReason `code ?? 'content_moderation'`, plugin `'qtap-plugin-grok'`; info line `'Grok Images API refused the request on content grounds'` { context:'GrokImageProvider.generateImage', model }. `provider.ts`: identical `hasRefusal`/`getFinishReason` refusal arm + `response.incomplete` terminal event + real finish reason in the raw response.
- **google** `image-provider.ts`: `GEMINI_IMAGE_SAFETY_FINISH_REASONS = Set(['IMAGE_SAFETY','SAFETY','PROHIBITED_CONTENT'])`; `isGoogleSafetyMessage(m)`: `lowered.includes('responsible ai') || /safety (filter|system|reasons?|policy)/.test(lowered) || /blocked\b.*\bsafety|\bsafety\b.*\bblocked/.test(lowered)` (deliberately never a bare "safety"). Three throw sites: (1) Gemini non-2xx: `message = error.error?.message || `Gemini API error: ${status}``; safety → `new ModerationRejectionError(message, response.status, error.error?.status, 'qtap-plugin-google')` else `new Error(message)`; (2) Gemini 200 with no image parts: `finishReason = candidate?.finishReason`, `blockReason = data.promptFeedback?.blockReason`; `(finishReason ∈ set) || blockReason` → `logger.warn('Gemini withheld the image on safety grounds', { context:'GoogleImagenProvider.generateWithGemini', finishReason, blockReason })`; `throw new ModerationRejectionError(`Gemini declined to generate this image (${blockReason || finishReason})${textResponse ? `: ${textResponse}` : ''}`, undefined, reason, 'qtap-plugin-google')` — **a NEW message where the old code threw `textResponse || 'No images returned from Gemini API'`**; otherwise the old throw stands; (3) Imagen non-2xx: same as (1) with `Google Imagen API error: ${status}`; (4) Imagen empty/filtered predictions: message unchanged (`Google Imagen rejected prompt by content policy${filterReason ? `: ${filterReason}` : ''}`) but now typed, `providerReason = filterReason ?? undefined`. `provider.ts` (text): `sendMessage` `finishReason = promptFeedback?.blockReason ?? candidates[0]?.finishReason ?? 'STOP'`, with `logger.warn('Google blocked the prompt', { context:'GoogleProvider.sendMessage', model, blockReason })`; streaming keeps the last chunk's `promptFeedback` (any chunk with `promptFeedback.blockReason`) and `withBlockReason(raw, promptFeedback)`: no blockReason → raw unchanged; else `logger.warn('Google blocked the prompt (streaming)', { context:'GoogleProvider.streamMessage', blockReason })` and returns `{ ...raw, promptFeedback: feedback, candidates: raw.candidates?.length ? raw.candidates : [{ finishReason: blockReason }] }`.
- **openrouter** `image-provider.ts`: `REFUSAL_PATTERNS` = the host's eleven patterns verbatim; `export function isOpenRouterRefusalBody(body)`; HTTP non-OK: `isOpenRouterRefusalBody(errorText)` → `new ModerationRejectionError(`OpenRouter API error: ${status} - ${errorText}`, status, 'content refusal', 'qtap-plugin-openrouter')` else the same-message `Error`; text-instead-of-picture: `new ModerationRejectionError(`Model declined to generate an image: ${summary}`, undefined, summary, 'qtap-plugin-openrouter')` (summary = text ≤ 200 chars + '...' rule, unchanged). `provider.ts`: Responses-API path raw response `choices[0]` now carries BOTH `finish_reason` and `finishReason` = `toolCalls?.length ? 'tool_calls' : refused ? 'refusal' : status === 'completed' ? 'stop' : (status === 'incomplete' && incompleteReason) ? incompleteReason : response.status` where `incompleteReason = incompleteDetails?.reason ?? incomplete_details?.reason` and `refused` = the same two shapes (**no `|| 'length'` — an incomplete with no reason yields `'incomplete'`**); chat-completions stream path tracks `streamFinishReason` = the last non-empty `choice.finish_reason` string seen, and writes `finish_reason`/`finishReason` = `toolCalls.length > 0 ? 'tool_calls' : (streamFinishReason ?? 'stop')`.
- **z-ai** `image-provider.ts`: `ZAI_SENSITIVE_CONTENT_CODE = '1301'`; `toZaiImageModerationError`: `rawCode = err.code ?? err.error?.code`; `code = number|string → String(rawCode)`; `code === '1301' || lowered.includes('sensitive content') || lowered.includes('unsafe or sensitive')` → `new ModerationRejectionError(message || 'Z.AI refused this image request as sensitive content', status?, code ?? '1301', 'qtap-plugin-z-ai')`; info `'Z.AI Images API refused the request as sensitive content'` { context:'ZAIImageProvider.generateImage', model }. No text-side change.
- NanoGPT: untouched by design (its filtered-prompt 400 is generic).

### A.20 Help / docs / client-contract readers

Help (list + gist): `help/dangerous-content.md` (p1: "the tick alone is enough" for image understudies; the backdrop reroute now for ANY chat under Auto-Route, never re-drafted; thrown refusals now recognised; new "When a Picture Is Refused" section naming the three notes; a provider must SAY it refused. p2: new "Switch a Chat to Flagged After This Many Refusals" setting text and "When the Concierge Switches a Chat" section with the four particulars; Flagged "arrives in one of three ways"; returning to Monitored clears the ledger). `help/image-generation-profiles.md` (p1: "The provider declined the picture" section; the related-link line). `help/story-backgrounds.md` (p1: uncensored providers need Auto-Route; "When a provider changes its mind" now the SAME prompt, any chat state).
Docs: `docs/developer/DDL.md`, `PROVIDER_PLUGIN_DEVELOPMENT.md` (new "Content refusals: throw ModerationRejectionError" section), `features/complete/message-route-trail.md` (new "Image trails" section), `BACKGROUND_JOBS_CHILD.md` (p2 commit-hook section), `CHANGELOG.md`, the two phase specs, `CLAUDE.md` chokepoints, `.claude/commands/update-documentation.md`, `scripts/concierge-four-state-test.sh` (CT-4 opt-in live check; p2: `qt_text(content) LIKE` in `check_ann` and CT-4).
Client files that READ a NEW wire field (for the contract pin; the client survey owns the port): `app/salon/[id]/components/system-message-labels.ts` — label `refusal: 'provider refusal'`, a content sniff `c.includes('on grounds of propriety') → 'refusal'`, tone map `concierge: { danger:'high', refusal:'high', '*':'high' }` (reads `systemKind: 'refusal'`); `components/chat/ToolMessage.tsx` — reads `message.routeTrail` on TOOL rows and renders `<RouteTrailBadge routeTrail size="xs">` under "Tried:"; `components/ui/RouteTrailBadge.tsx` — `modelName={row.label}` (reads `profileKind` through `collapseRouteTrail`); `components/settings/chat-settings/DangerousContentSettings.tsx` + `types.ts` — reads/writes `autoSwitchAfterRefusals` (number input 0..10, default 2, `Math.min(10, Math.max(0, parsed))`).

## B. v5 counterparts on main `2aed9a552`

- **A.1 classifier → NONE.** Pieces exist: `crates/quilltap-core/src/moderation_finish_reason.rs` (`is_moderation_finish_reason`, `describe_moderation_refusal`, the ten-entry set — v4's file unchanged, so this stays); `services/dangerous_content/provider_routing.rs:430-438` `is_image_moderation_error(message: &str)` = the OLD six-substring list (the doc comment at `model/image_dialects.rs:32-35` reads: "The refusal-keyword GAPs are v4 behavior carried verbatim: Gemini's `textResponse || 'No images returned…'`, OpenRouter's `Model declined…`, and z-ai's total lack of moderation handling never match `is_image_moderation_error`. Never widen the keyword set to 'fix' a gap." — v4 has now widened it itself). No evidence ranking, no code inspection: v5 errors are string-only (`model/image.rs:178` `ImageGenError { message }`; `model/stream.rs:322` `StreamErrorKind { Provider, Stalled{…} }` + `StreamError { message, kind }`; `llm_fallback/types.rs:249` `FallbackError { kind: Option<LlmErrorKind>, name: Option<&str>, message }`; `services/llm_errors.rs:34` `LlmErrorKind { Base, ApiKey, RateLimit, Network, ModelNotFound, InvalidRequest, TokenLimit, ContentLimit }` — no moderation kind, no `code`, no `providerReason`).
- **A.2 understudy → NONE as a module.** `provider_routing.rs:204-315` `resolve_provider_for_dangerous_content(conn, api_keys, original, key, mode, uncensored_text_profile_id, user_id, mime_types)` and `:317-388` `resolve_image_provider_for_dangerous_content` carry the OLD inline order (explicit → scan; NO exclusion of the original; NO courier skip; old two-line logs "Rerouting to configured…"/"…discovered uncensored-compatible…"; the `[DangerousContent] Deprioritising…` line at :279). `:394-427` `resolve_uncensored_image_profile_for_reroute` = the DELETED v4 helper (mode gate, explicit-only, same-id → None). `ApiKeyResolver` seam (`NoApiKeys`/`ConnApiKeys`/`DbApiKeys`) is the key-decrypt twin. `DangerContentRouter` (:443) implements `provider_failover::DangerousContentRouter`.
- **A.3 image chokepoint → NONE.** Three separate reroute sites: `services/image_job_common.rs:518-` `generate_with_reroute` + `:642-` `reroute_or_fail` (shared by avatar/story; `RerouteHandler` enum :281 carries the bug-133 gate as `StoryBackground { is_dangerous_chat }` / `CharacterAvatar`; logs at :368-391 the old "Image provider rejected for content moderation, rerouting…" pair, :403-421 the avatar success line WITH `reroute_duration_ms`, the story success line deliberately UNPORTED (:453-465), :426-449 the "(Concierge reroute also failed)" pair); `tools/generate_image.rs:2566-2640` the tool's inline reroute (`is_image_moderation_error(&error.message)` → `resolve_uncensored_image_profile_for_reroute`, context `tools.generate_image.concierge-reroute`); `api/images.rs:1279-1780` the legacy dialog (settings at :1519-1524 resolved WITHOUT the chat; `generate_image` at :1772; no post-hoc reroute at all). `crates/quilltap-web/src/images_routes.rs` / `quilltap-host/src/images_generate.rs` only build the wires. Reference counts: `grep -rn "isImageModerationError\|resolveUncensoredImageProfileForReroute\|is_image_moderation_error\|resolve_uncensored" crates/` = **64 lines across 18 files** (core: pascal/llm_consult, tools/generate_image, cheap_llm, model/image, model/image_dialects, model/wire, services/image_job_common, context_summary, answer_confirmation, pre_compute, story_background_job, dangerous_content/provider_routing, memory_processor, title_update_job; harness: image_dialects_equivalence, cheap_llm_selection_equivalence, precompute_equivalence, danger_routing_equivalence).
- **A.4 wrappers → `provider_routing.rs`** as above (the inline order IS the pre-p1 wrapper). Callers of the text wrapper (pre-flight, unchanged hunks in v4 but new behaviour via the resolver): `api/chat_post_office.rs:812`, `services/chat_create.rs:2873`, `orchestrator.rs` / `enclave/step.rs` via the `DangerousContentRouter` seam; image wrapper: `character_avatar_job.rs:518`, `generate_image.rs:2081`.
- **A.5 text failover → `services/provider_failover.rs` (2741 lines).** `DangerSettings { mode, uncensored_text_profile_id }` (:60-66); `attempt_empty_response_recovery` (:196-) gates the uncensored retry on `mode == "AUTO_ROUTE" && uncensored_text_profile_id.is_some()` (:404-409 — the OLD gate) and keeps v4's deleted same-id no-op branch (:445-447); `attempt_hard_error_failover` (:1226-1305) has NO moderation-refusal branch — every eligible trigger records `Failed` and walks the chain; no `danger_settings` option; no `attempt_uncensored_retry` factoring (the retry body is inline at :410-520). `restream_into` :771; `reset_streaming_buffers_for_swap` :912.
- **A.6 route trail → `services/route_trail.rs` (507 lines).** `RouteAttemptVia` :57; `RouteAttemptOutcome` :84; **`RouteAttemptEvidence { FinishReason, Inferred }` :103-113 (two variants only)**; `RouteAttempt` :151 (no `profile_kind`); `record_route_failure` :260; `classify_empty_body` :336 (inline `is_moderation_finish_reason` then `inferred` — the pre-p1 body); `compose_route_trail` :397; `build_route_trail` :453. DB-side JSON: `db/chat_messages_route_trail_repair.rs` exists (the routeTrail column ensure); no evidence-string validation found under `db/`.
- **A.7 display → SPA** (`apps/web/src/app/chat/route-trail-badge.ts`, `route-trail-display.spec.ts`) — client survey.
- **A.8 schema → no v5 Zod twin for `RouteAttemptSchema` server-side; `chats_messages` row marshalling is JSON passthrough.** `generators/qtap-export.schema.json:948` still has `"evidence": {"enum": ["finish-reason","inferred"]}` and no `profileKind`; `services/qtap_export/schema-key-order.json:175` lists `routeTrail` as a message key (item keys are not ordered there).
- **A.9 → `crates/quilltap-core/src/finish_reason.rs:23` `extract_finish_reason(raw: &Value)`**: choices[0].finish_reason → stop_reason → candidates[0].finishReason → status. Missing: the camelCase `finishReason` arm and `promptFeedback.blockReason` (grep for `promptFeedback`/`blockReason` in core: ZERO hits).
- **A.10 → `llm_fallback/engine.rs:121` `classify_fallback_trigger(FallbackError)`**: non-triggers first, then the kind ladder, `CheapLLMTimeoutError`, `LLMStreamStalledError`, the regex ladder, `UNATTRIBUTED_4XX → None`, else `ProviderError`. No refusal arm. `FallbackTrigger::ModerationRefusal` exists (`types.rs:30`, `"moderation-refusal"` :45) but is only produced by `classify_empty_body`.
- **A.11 → `services/tool_execution.rs:701` `save_tool_messages`**: builds the TOOL event as `{ id, type, role:'TOOL', participantId, targetParticipantIds, content, createdAt, attachments }` (:738-745); metadata struct at :128 has `expanded_prompt` only; `tools/executor.rs:1699` copies it. No `route_trail`.
- **A.12 → `tools/generate_image.rs`**: output struct :109 (`expanded_prompt`, no route_trail); success message :2018 `"Successfully generated {count} image(s) using {}"` from `final_profile` (the pre-flight profile, not the answerer); the reroute :2566-2640; `ModerationProvider` (gatekeeper) is the pre-flight classifier seam.
- **A.13 → `services/character_avatar_job.rs`** (:582-629 calls `common::generate_with_reroute` with `RerouteHandler::CharacterAvatar`, context `background-jobs.character-avatar.concierge-reroute`) and **`services/story_background_job.rs`** (:304 `uncensored_image_target = is_dangerous_chat && has_uncensored_image_provider` — NO Auto-Route conjunct; :685-711 `generate_with_reroute` with `RerouteHandler::StoryBackground { is_dangerous_chat }` = the bug-133 gate, doc'd at :707-710 and `image_job_common.rs:266-296`). Both post via `services/lantern_notifications.rs:172` `post_lantern_image_notification(db, LanternPostParams { chat_id, file_id, kind, prompt })` (:156-163 — no `route_trail`).
- **A.14 → `api/images.rs:1279-1930`** (settings :1519-1524 without the chat; the `ImagesGenerate` tri-state decoder at `quilltap-web/src/images_routes.rs:248-372`).
- **A.15 → `services/concierge_notifications.rs` (415 lines)**: `ConciergeManualKind` :268 (FIVE variants, `from_wire`/`as_wire` :286-307), `build_manual_content` :310, `build_manual_opaque_content` :327, `post_concierge_manual_announcement(db, chat_id, kind)` :395, `post_concierge_danger_announcement` :407, the shared `post_concierge_message` literal with `systemKind: "danger"` (:378). No refusal kinds, no auto-flag details, no `systemKind: 'refusal'`.
- **A.16 → `lantern_notifications.rs`** as above; the "bug-133 gate" the order named for this file does NOT live here — it is `image_job_common.rs:281-306` + `story_background_job.rs:707-711`.
- **A.17 ledger → NONE.** `db/chats.rs`: `ChatUpdate` (:1014 `update`) is a fixed setter struct; `get_transcript_version` (:1441-1463) is the "read straight off the row, outside the projection" precedent; `db/chats_read.rs:71` `ALL_COLUMNS` (must NOT gain the columns — the index census at :611-624 pins it to the D23 dump, which will not carry them); `db/chats_transcript_version_repair.rs` (220 lines) = the boot-ensure precedent whose module doc already states the rule (generateDDL walks the schema; a column outside the schema can ONLY arrive by boot-ensure). `db/chat_settings.rs:362-376` `DangerousContentSettings` (ten fields, no `auto_switch_after_refusals`); `cheap_llm.rs:94` a two-field subset; `provider_failover.rs:60` `DangerSettings` subset; `dangerous_content/resolver.rs:53-84` the two default builders (no new field). `api/settings.rs:662-745` the hand-rolled Zod-4 twin of `DangerousContentSettingsSchema.parse` for the PUT (declaration-order issues; `showWarningBadges` at :740 is currently last). `services/provisioning/chat_settings_seed.json` `dangerousContentSettings` = `{"mode":"OFF","threshold":0.7,"scanTextChat":true,"scanImagePrompts":true,"scanImageGeneration":false,"displayMode":"SHOW","showWarningBadges":true}` (no `autoSwitchAfterRefusals`; consumed by `provisioning/mod.rs:294`). `services/provisioning/fresh_schema.json`: `moderationRefusalCount` 0 hits, `lastModerationRefusalAt` 0, `transcriptVersion` 0, `conciergeOverride` 1 (as `"conciergeOverride" TEXT,` in the chats CREATE). `services/dangerous_content/manual_flip.rs:95-201` `apply_concierge_flip(db, announcer, chat_id, requested, chat)` — raw `UPDATE`s (`dangerCategories = '[]'` literal at :129/:151), the `ConciergeAnnouncer` seam takes a wire string (`"manual-flagged"` …). `write_apply.rs:150` `apply_writes` ends with `cleanup_staging_dirs` + `dispatch_invalidations` (:168-169; `ApplyHost` trait :133-137) — no ledger hook; v5's job runner is IN-PROCESS (no forked child), so `QUILLTAP_JOB_CHILD` has no analogue. `services/cheap_llm_exec.rs:134` `ProviderResponse { content, usage }` (no finish reason; `CompletionResponse.finish_reason` exists at the provider boundary, `model/completion.rs:180`), `should_attempt_uncensored_fallback` :93-131, the both-empty throw :1109.
- **A.18 → migration runner deferred (locked).** Precedent list under `db/`: `chats_transcript_version_repair.rs`, `chat_messages_route_trail_repair.rs`, `chats_cycle_order_repair.rs`, `files_generation_key_repair.rs`, … (14 repair modules). `qtap_export` guards: `qtap_schema_embed_guard.rs` (byte-compares the vendored schema against the v4 checkout), `qtap_schema_validate_equivalence.rs`, `key_order.rs`.
- **A.19 plugins → v5's native providers.** Image dialects `model/image_dialects.rs` (2949 lines): every rejection path is normalised to v4's EXACT error STRING (:8-11 doc); Imagen filtered predictions manufacture the `content policy` message (:949-957); Gemini no-image path throws `textResponse || 'No images returned from Gemini API'` (:928); OpenRouter text-instead-of-picture `Model declined to generate an image: {summary}` (:1069-1077); OpenRouter/Google HTTP errors via `WireResponse`; the SDK providers (OPENAI/GROK/Z_AI) surface the transport `Err` verbatim (`400 "…rejected by content moderation."` shape, :1281-1290, :2090-2142). No `code`/`providerReason`/`status` fields anywhere; `ImageGenError` is message-only; `CannedImageProvider::with_failure` (`model/image.rs:412`) keys failures by message. Text: `model/response_parse.rs:771-790` `responses_finish_reason` (function_call → tool_calls; completed → stop; incomplete → `incomplete_details.reason` non-empty else `length`; failed → error; else stop) — NO `refusal` arm; `model/decoders/responses_api_sse.rs:72` captures `response.completed` only (no `response.incomplete`); `model/decoders/chat_completions_sse.rs:424-441` hard-codes `finishReason: 'stop'|'tool_calls'` (camelCase only, no `finish_reason`, no streamed reason); Google: no `promptFeedback` handling in `model/request_builder/google.rs`, `decoders/google_parts.rs`, or `response_parse.rs`.
- **A.20 help → `help/` at the repo root** (the vendored tree, 129 files md5-checked by `help_tree_equivalence` / `help_tree_embed_guard`); `docs/v4/` mirror.

## C. Families, oracle cases, fixtures — what MOVES

| v5 test (crate) | oracle case / recipe input | committed fixture(s) | moves because |
|---|---|---|---|
| `fallback_engine_equivalence` (harness) | `harness/oracle/cases/fallback-engine.ts` → `QT_ORACLE_FALLBACK_ENGINE` | none | `classifyFallbackTrigger` now answers `'moderation-refusal'` FIRST for any refusal-shaped input (typed `code`, provider codes, the eleven patterns) — every corpus row whose message contains e.g. "safety system"/"content moderation"/"content policy" flips from `provider-error`/`null`; the corpus must gain `{code}`/`{error.code}`/`name` inputs (v5's `FallbackError` has no `code`). |
| `route_trail_compose_equivalence` (harness) | `route-trail-compose.ts` → `QT_ORACLE_ROUTE_TRAIL` | none | `classifyEmptyBody` delegates (same two readings, byte-identical details) — expected NEUTRAL, but the evidence type widens and the classifier's debug/info lines appear; re-run at the pin to prove neutrality. |
| `finish_reason_equivalence` (harness) | `finish-reason.ts` → `QT_ORACLE_FINISH_REASON` | none | two new arms (`choices[0].finishReason` camel; `promptFeedback.blockReason` BEFORE `candidates`) — corpus gains rows. |
| `moderation_finish_reason_equivalence` | `moderation-finish-reason.ts` | none | UNCHANGED (file untouched) — neutrality pin only. |
| `primary_stream_tier3_equivalence` (harness) | `primary-stream-tier3.test.ts` + `openai-chaining-fallback-tier3.test.ts` → `QT_ORACLE_PRIMARY_STREAM` / `QT_ORACLE_OPENAI_FALLBACK` | `fixtures/primary-stream-tier3.json` (+ builder) | the hard-error `moderation-refusal` branch (refused row with evidence, understudy-first, chain `dangerous: true`, the four new log lines, the `refusal-no-understudy` announcement row in chat_messages, the ledger increment on `chats`); the empty-body path's gate change (no `uncensoredTextProfileId` needed; original excluded) and the `Both safe and uncensored…` bag now reading `state.effectiveProfile`. Fixture needs the two ledger columns for the ledger arms. |
| `danger_routing_equivalence` (harness) | `danger-routing.test.ts` → `QT_ORACLE_DANGER_ROUTING` | `fixtures/danger-routing.json` (+ builder) | **the oracle case imports `isImageModerationError` / `resolveUncensoredImageProfileForReroute`, which no longer exist at the pin — the case itself breaks and must be rewritten** (over `understudy.ts` + the wrappers); log lines renamed/merged; `configured` field; original excluded; courier skip; keyless explicit image pick now warns. |
| `image_dialects_equivalence` (harness) | recorded corpus (no live oracle) | `fixtures/image-dialects/image-dialects.recorded.ndjson` | v4's plugin sources now throw typed errors: Gemini no-image → NEW message `Gemini declined to generate this image (IMAGE_SAFETY)…`; Imagen filtered / OpenRouter declined / OpenRouter refusal-body HTTP / OpenAI+Grok+Z.AI SDK throws → same message text but `code: 'MODERATION_REJECTED'` + `providerReason` + `statusCode`. Re-record at the pin; the comparand must widen from a string to `{message, code?, providerReason?, status?}`; the `:32-35` "never widen" doctrine is retired by v4 itself. |
| `stream_decoders_equivalence` (harness) | recorded streams | `fixtures/streams/*` | OpenAI/Grok terminal `response.incomplete`; the Responses-path `finish_reason: 'refusal'`; OpenRouter chat-completions streamed `finish_reason` (snake + camel) = last non-empty seen; Google `promptFeedback` fold into `rawResponse` (+ synthesized `candidates[0].finishReason`). |
| `image_generation_tier3_equivalence` (harness) | `image-generation.test.ts` → `QT_ORACLE_IMGGEN` | `fixtures/image-generation.json` (+ builder), `photo-lossless.webp` | the tool goes through the chokepoint: TOOL row `routeTrail` (after `attachments`), success message names the ANSWERING model, `provider/model` from the answerer, the `refusal-*` announcement rows, the ledger increment, the log-line census (`Image generation failed:` bag gains `conciergeTrail`; the old reroute pair gone; `Concierge uncensored reroute succeeded` bag changed), `primaryVia: 'concierge'` after a pre-flight reroute, the merely-compatible understudy (no explicit pick). |
| `images_generate_route_equivalence` (harness) + `images_generate_dispatch_wire` (web) | `images-generate-route.test.ts` → `QT_ORACLE_IMAGES_GENERATE` | `fixtures/images-collection.json`, `images-main.db`, `images-mount.db` | settings resolved WITH the chat (a Vouched/Uncensored chat now governs the dialog); the three new `[Images v1]` lines; the connection-profile understudy filtered by `supportsImageGeneration`; `profileKind: 'connection'` rows; announcements into the chat. |
| `story_background_job_tier3_equivalence` (harness) | `story-background-job.test.ts` → `QT_ORACLE_STORY` | `fixtures/story-background-job.json` | bug-133 gate deleted; `uncensoredImageTarget` gains the Auto-Route conjunct (prompt crafting moves for Flagged+DETECT_ONLY); the failure bag (`isDangerousChat, hasUncensoredImageProvider, dangerMode, conciergeTrail`); the success line now PORTED with a new bag; `routeTrail` on the Lantern bubble; announcements; ledger. |
| `avatar_job_tier3_equivalence` (harness) | `avatar-job.test.ts` → `QT_ORACLE_AVATAR` | `fixtures/avatar-job.json` | same shape for Aurora (`purpose: 'avatar'`, `primaryVia` after a pre-flight reroute; `rerouteDurationMs` gone from the success line; `.concierge-reroute` params context for ANY non-requested profile). |
| `post_office_writers_tier3_equivalence` / `post_office_concierge_lantern_suparna_equivalence` (harness) | `post-office-writers-tier3.ts` → `QT_ORACLE_POW`; `post-office-concierge-lantern-suparna.ts` → `QT_ORACLE_PO_CLS` | `fixtures/post-office-writers-tier3.json` | new `postConciergeRefusalAnnouncement` (`systemKind: 'refusal'`), the `auto-flagged-refusals` manual kind + details, `postLanternImageNotification`'s `routeTrail` key and `routeTrailLength` log field. |
| `danger_resolver_equivalence` (harness; also drives manual-flip) | `danger-resolver.ts` → `QT_ORACLE_DANGER_RESOLVER`; `danger-manual-flip.test.ts` → `QT_ORACLE_DANGER_MANUAL_FLIP` | `fixtures/danger-manual-flip.json` (+ builder) | defaults gain `autoSwitchAfterRefusals` (2 / vouched 0); Uncensored spread inherits the global value; `applyConciergeFlip`'s `'monitored'` case now also resets the ledger (the fixture needs the columns or the reset throws in v4), the fourth-argument arms (`by:'concierge'` → `dangerCategories: ["moderation-refusals"]`, kind `auto-flagged-refusals`), the log bag (`by`, `reason`). |
| `chats_tier2_equivalence` (harness) | `chats-tier2.ts` → `QT_ORACLE_CHATS` | `fixtures/chats-tier2.json` (+ builder) | three new repo ops (`incrementModerationRefusalCount` → `$set`+`$inc` SQL, `getModerationRefusalLedger`, `resetModerationRefusalLedger`); the fixture pair must be widened through v4's own ALTERs (the fixture-vintage heal pattern). |
| `chat_settings_tier2_equivalence`, `chat_settings_column_sites_guard`, `chat_settings_composer_web_routes` (harness/web) + `api/settings.rs` twin | `chat-settings-tier2.ts` → `QT_ORACLE_CHAT_SETTINGS` | `fixtures/chat-settings-tier2.json` | the stored default row gains `autoSwitchAfterRefusals: 2`; the PUT parse gains `z.number().int().min(0).max(10).default(2)` (three new issue shapes: not-int, too_small, too_big — measure their `ZodError.message` against real zod 4.6.5); GET projections. |
| provisioning family (`common/mod.rs` readers of `chat_settings_seed.json` / `fresh_schema.json`; `restore_vintage_state`, `transcript_version_isolation_guard`) | v4 fresh instance | the two D23 artifacts | `chat_settings_seed.json` must be RE-DUMPED (v4's default row now carries `autoSwitchAfterRefusals: 2`); `fresh_schema.json` does NOT move (both ledger columns are outside `generateDDL`, §E) — the columns arrive by a NEW boot-ensure + its own isolation guard (the `transcriptVersion` pattern: `ALL_COLUMNS` must not name them; `ChatUpdate` must not carry them). |
| `cheap_llm_fallback_equivalence` (harness) | `cheap-llm-fallback.test.ts` → `QT_ORACLE_CHEAP_FALLBACK` | `fixtures/settings.json` (+ builder) | `finishReason` plumbed through `sendToProvider`; the ledger record on a stated empty-body refusal (with/without the uncensored retry; `refusedProfileName` fallback `${provider} ${modelName}`); cheap chains now walk on a THROWN refusal (via A.10). |
| `write_apply_equivalence` (harness) | `write-apply.test.ts` → `QT_ORACLE_WRITE_APPLY` | `fixtures/write-apply.json` | `runRefusalLedgerChecks` post-commit hook + `chatsWithRecordedRefusals` (once per chat; last provider; skipped on partition failure; best-effort). |
| `qtap_schema_embed_guard`, `qtap_schema_validate_equivalence`, `generation_key_travels_as_is_guard` (harness) | v4 `public/schemas/qtap-export.schema.json` | `generators/qtap-export.schema.json` | RED at the pin until re-vendored (routeTrail descriptions, evidence enum, `profileKind`). The ledger columns do NOT enter the schema. |
| `help_tree_equivalence`, `help_tree_embed_guard` | v4 `help/` | `help/` (129 files) | three files changed (dangerous-content.md by BOTH commits, image-generation-profiles.md, story-backgrounds.md) → re-vendor, count stays 129. |
| `danger_trigger_equivalence`, `moderation_wire_equivalence`, `character_avatar_write_tier2_equivalence` | — | — | expected neutral (gatekeeper trigger / OpenAI moderation wire / avatar write ops untouched); pin. |
| Tier R (`cli_differential`) | v4 `npx quilltap` | — | no CLI hunk in either commit; `quilltap db` already registers `qt_text` (the four-state script's fix is NO-PORT). |

## D. Traps

1. **"isImageModerationError is gone" is true in v4 and false in v5 by 64 references** (§B). The `image_dialects.rs:32-35` doctrine ("never widen the keyword set") was written to preserve v4's gaps; v4 has now closed those gaps itself (eleven patterns + typed errors + codes), so the doctrine is superseded, not violated — the order must say so or a lane will "faithfully" keep the gaps.
2. **The phase-2 SPEC (§1/§3) and the SHIPPED code disagree in three load-bearing places**, and the commit message is the accurate one: (a) the spec declares the columns in both chat schemas and exports them; as-built they are in NEITHER schema and NOT exported (`qtap-export.schema.json` untouched by p2); (b) the spec's `incrementModerationRefusalCount(chatId, at)` "returns the new count" from one UPDATE; as-built it is `$set`+`$inc` then a SEPARATE `findOne` (two statements, and 0 on a missing chat with a warn); (c) the spec's `recordModerationRefusal` returns `count: number`; as-built `number | null`. The phase-1 spec's §5 non-generic chokepoint and §7 `.default('connection')` are likewise overridden by its own As-built (generic + optional `resolveUnderstudy`/`profileKind`/`primaryVia`; optional `profileKind`).
3. **Commit #73 says "Drops the Lantern's bug-133 gate" — it ALSO adds an Auto-Route conjunct to `uncensoredImageTarget`**, which changes PROMPT CRAFTING for a Flagged chat under DETECT_ONLY (concealed draft now), a separate behaviour from the reroute.
4. **Commit #73 describes the text change as "retried on the uncensored understudy before the chain"; the empty-body path ALSO changed silently:** the gate no longer requires `uncensoredTextProfileId`; the original/effective profile is EXCLUDED so v4's "rerouted to the same id" no-op branch (still in v5 at `provider_failover.rs:445-447`) is unreachable; the `connectionProfile` option was dropped from the destructure (the "Both … returned empty" log now names `state.effectiveProfile`, which after a pre-flight reroute is the uncensored one, not the safe one — v4's own naming drift, port as-is).
5. **Two different "stated" gates.** The text `refusal-no-understudy` bubble fires on `refusalWasStated = openingVerdict.outcome === 'refused'`, which is TRUE for an `inferred` verdict; the LEDGER excludes `inferred`. Do not unify them.
6. **Ledger provenance rules that a hurried port will collapse:** the image chokepoint records the PRIMARY's verdict on every post-refusal exit (never the understudy's, never on a non-refusal, never without a chat), AFTER the announcement; the empty-body path records the OPENING verdict (or, if the opening was plain-empty, a `finish-reason` the same-provider retry stated — review fix #3), ONCE per turn, on the way OUT, with `rerouted = uncensoredRecovered`; the hard-error path records in `attemptHardErrorFailover` (rerouted = the understudy recovered), on all three arms incl. not-Auto-Route; `attemptUncensoredRetry` records nothing; cheap records only a finish-reason-stated empty body.
7. **`applyConciergeFlip` stamps `dangerCategories: ['moderation-refusals']` on ANY `by: 'concierge'` flip, but posts `auto-flagged-refusals` ONLY when `reason === 'refusals'`** (`by:'concierge', reason:'classifier'` → category stamped + `manual-flagged` bubble). No caller does that today; pin both arms.
8. **`chatsWithRecordedRefusals`'s comment lies:** "later increments overwrite earlier ones" — only a later increment WITH a provider overwrites; a provider-less later one keeps the earlier provider (`who ?? chats.get(chatId) ?? null`).
9. **Threshold gate before mode gate** in `runAutoSwitchCheck` (log line differs for `threshold <= 0` under DETECT_ONLY); the second `findById` re-read happens only after `count >= threshold`; `switched` reflects `result.changed` (a no-op flip → false).
10. **Announcement arithmetic:** `TIMES_WORDS[count]` only for 2 ≤ count ≤ 10, `'More than once now'` otherwise (INCLUDING count 0); `count === 1` is its own sentence; opaque uses `COUNT_WORDS` for 1..10 else `String(count)`, noun singular only at 1; `who` is null when `lastProvider` is `''` (the dispatcher hook passes `null` → `lastProvider: ''`).
11. **JS→Rust in `refusal.ts`:** `codeString` accepts FINITE numbers via `String(n)` (Z.AI `1301` numeric → "1301"; use JS number formatting for non-integers); codes are compared lower-cased but the ORIGINAL case lands in the detail; `truncate` slices 199 UTF-16 code units + `…` (reuse the existing `truncate_detail` only if it counts the same units); `messageOf` for a non-Error object requires a STRING `.message`; `lowered &&` short-circuits on the empty string; evidence 3 (finish reason) runs with or without an error and BEFORE the message patterns — so an error whose message says "safety system" but whose call ALSO reported `finish_reason: content_filter` is `finish-reason`, not `message-pattern`; evidence 2's `collectCodes` checks `record.code` THEN `record.error.code` — and evidence 1 checks `record.code === 'MODERATION_REJECTED' || record.name === 'ModerationRejectionError'` (name match with NO code).
12. **JS→Rust regex in the Google plugin:** `/safety (filter|system|reasons?|policy)/` and `/blocked\b.*\bsafety|\bsafety\b.*\bblocked/` are run on the LOWER-CASED message; JS `\b` (no `u` flag) is ASCII `[A-Za-z0-9_]` — Rust `regex`'s `\b` is Unicode-aware; use `(?-u:\b)`. `.*` is greedy, no `s` flag (no newline crossing) — Rust default matches.
13. **v5 has no plugins — the "typed-error" evidence can only come from v5's own native dialects/decoders.** v4's `ModerationRejectionError` carries `code`, `statusCode`, `providerReason`, `name`; v5's `ImageGenError`/`StreamError` are strings. The order must give the image dialects (and the text decoders' thrown-error path) a structured refusal (`code: "MODERATION_REJECTED"`, `provider_reason`, `status`) that the classifier ranks as evidence 1 — otherwise every v5 refusal degrades to `message-pattern`/`provider-code` and the trail's `evidence` (a persisted, exported field) diverges. Per provider, v5 must also parse what the SDK gave v4 for free: OpenAI/Grok/Z.AI `err.code`/`err.error.code`/`err.status` come from the SDK's `APIError` (v5 must read the HTTP error BODY's `error.code` + the HTTP status itself); the `message` v4 tests is the SDK's `${status} ${body.error.message}` rendering v5 already reproduces.
14. **Per-provider `providerReason` defaults differ:** OpenAI `code` (may be undefined); Grok `code ?? 'content_moderation'`; Z.AI `code ?? '1301'`; OpenRouter HTTP `'content refusal'`, OpenRouter declined = the ≤203-char summary; Google HTTP `error.error?.status` (e.g. `INVALID_ARGUMENT`), Gemini no-image `blockReason || finishReason`, Imagen `filterReason ?? undefined`. These reach the trail's `detail` through evidence 1's `(${reason})` suffix.
15. **Text finish reasons:** OpenAI/Grok `getFinishReason` gains `hasRefusal → 'refusal'` BETWEEN `tool_calls` and the status map, and the NON-streaming raw response now uses it too; OpenRouter's Responses path yields `'incomplete'` (the raw status) when `incomplete` has no reason — NOT `'length'` like OpenAI's; OpenRouter chat-completions keeps the LAST non-empty `finish_reason` string seen across chunks (`tool_calls` still overrides); Google folds `promptFeedback` into the persisted `rawResponse` and synthesizes `candidates: [{finishReason: blockReason}]` only when there were no candidates — this changes llm_logs/raw comparands, not just the extracted reason.
16. **`extractFinishReason` order trap:** Anthropic `stop_reason` still outranks `promptFeedback.blockReason`; `promptFeedback` outranks `candidates` (a Google response with BOTH a candidate `finishReason: STOP` and a `blockReason` reads the block reason).
17. **Trail row KEY ORDER differs between the two writers.** Image rows (`image-failover.ts row()`): `profileId, profileName, provider, modelName, via, outcome, [profileKind], [trigger], [evidence], [detail]` — `profileKind` BEFORE `trigger`, present only when `'image'`. Text rows (`recordRouteFailure`, unchanged; v5 `route_trail.rs:260`): `…, via, outcome, trigger, [evidence], [detail]` — never `profileKind`. The Zod declaration order (`trigger, evidence, profileKind, detail`) is NEITHER. JSON-column byte comparisons will see this.
18. **Where trails land, three ways:** the TOOL row (after `attachments`); the Lantern/Aurora bubble (after `systemKind`); the tool's error response (`errorResponse.routeTrail`, read from `ImageGenerationError.details`). The legacy dialog persists NO trail. `getConciergeTrail` treats an EMPTY trail as null; the handlers' "after Concierge reroute" wording keys on `trail.length > 1`.
19. **`primaryVia: 'concierge'`** is set by the tool (`finalProfile.id !== imageProfile.id`), the avatar (`effectiveImageProfile.id !== imageProfile.id`) and the dialog (`profile.id !== profileId`) — never by the Lantern (always `'primary'`).
20. **The child/parent split has no v5 analogue** (in-process job runner): `isJobChild()` is constantly false, `recordModerationRefusal` always decides, and the `applyWritesUnsafe` hook only matters where v5 replays `ChildWritePayload` batches (`write_apply.rs` — the P4.D124 twin). A ruling is needed: port `chats_with_recorded_refusals` + the hook for `write_apply_equivalence` parity, or record NO-PORT with the single-entry rule. The per-chat `switchChecks` chaining maps onto the single-writer channel ordering but the "abandon if left Monitored" re-read must still be ported (the test `does not overwrite an operator state set while the check was reading` pins it).
21. **The two review fixes "deferred to phase 2's spec"** (PR #73 Copilot review) both LANDED in `49059fb14`: (a) `buildRefusalContent`'s `refusal-rerouted` string lost its final sentence "The result is attached above." (`writer.ts:426`; the phase-1 spec §8 example and help text were updated); (b) `scripts/concierge-four-state-test.sh` `check_ann` + CT-4 now `qt_text(content) LIKE …`. The phase-2 spec's "Carried over" checklist is ticked. Only (a) is a port item (a v4 wire string); (b) is NO-PORT (script).
22. **`resolveUncensoredTextUnderstudy` applies `eligible()` — incl. the courier skip and the caller's `filter` — to the EXPLICIT pick as well as the scan**, and its `findAll()` is over ALL users' rows filtered by `userId` (v5's `provider_routing.rs` does the same filter). A courier explicit pick logs the "missing, not owned, or not eligible" warn with `courier: true`.
23. **The story handler's `attemptBackground` log context** is `…story-background.concierge-reroute` for any non-primary profile; the avatar's `attemptPortrait` rebuilds params (with the `.concierge-reroute` context) for ANY profile ≠ the REQUESTED one — including the pre-flight-rerouted primary — while the LLM-log suffix `(Concierge reroute)` keys on ≠ the EFFECTIVE primary. Two different comparisons in one closure.
24. **`refusal-not-permitted` fires for Vouched Safe chats** (resolved mode `OFF`) — and for moderation-exempt chat types (Help/Brahma), which the resolver also maps to the vouched settings; the ledger then records (evidence permitting) but `autoSwitchAfterRefusals: 0` + mode OFF stop the switch.
25. **The dialog now resolves settings WITH the chat** — a chat set Uncensored forces `AUTO_ROUTE` even under a global `OFF`, so the legacy dialog can reroute where before it could not.
26. **Spelling/consistency:** the `[Images v1]` warn passes `chatId: chatId ?? null` (null, not undefined) while the failover `logContext` passes `chatId ?? undefined` — capture pins must not "normalise" these.

## E. Open questions

1. **`generateDDL` — LOCATED and RUN.** `lib/database/schema-translator.ts:433` `generateDDL(name, schema)` = `extractSchemaMetadata(name, schema)` → `generateCreateTable` + `generateCreateIndexes`: it WALKS THE ZOD SCHEMA (no column list). The `chats` collection is created from `ChatMetadataBaseSchema` (`chats.repository.ts:61`). Probe from the v4 checkout (`npx tsx -e`, read-only, at working tree = `49059fb14`+docs commits): for BOTH `ChatMetadataBaseSchema` and `ChatMetadataSchema` the CREATE TABLE has 104 lines and **`moderationRefusalCount`: ABSENT, `lastModerationRefusalAt`: ABSENT, `transcriptVersion`: ABSENT** (`dangerClassifiedAtMessageCount` REAL and `answerConfirmationOverride` TEXT present, as anchors). So the D23 re-dump CANNOT carry the ledger columns; they follow the `chats_transcript_version_repair.rs` precedent exactly (boot-ensure over the main partition, no backfill, `INTEGER NOT NULL DEFAULT 0` / `TEXT DEFAULT NULL`, NOT in `ALL_COLUMNS`, NOT in `ChatUpdate`, NOT exported).
   **Anomaly for the D23 owner:** the same probe reports **`conciergeOverride`: ABSENT** from `extractSchemaMetadata` (field count 102) although both schemas declare it (`chat.types.ts:943`, `:1320`) and v5's committed `fresh_schema.json` carries `"conciergeOverride" TEXT,` in the chats CREATE. Either the translator does not unwrap `z.enum().nullable().optional()` under the current zod (4.6.5), or the committed dump was taken from a live table AFTER `add-chat-concierge-override-v1`'s `ALTER` ran. Unresolved here (out of scope, read-only); it does not change the ledger conclusion (undeclared columns are absent either way) but it does mean "re-dump from live generateDDL" may not reproduce the current `fresh_schema.json` byte-for-byte.
2. **v5's structured refusal type** (D.13): which type carries `code`/`provider_reason`/`status` — a new variant on `ImageGenError` and `StreamErrorKind`, or a side-struct the dialects attach? Order-affecting: it decides whether `classify_refusal` takes a trait object or a plain struct, and which of the 18 files' `is_image_moderation_error` call sites move first.
3. **Where the pre-flight wrappers live after the split:** v5's `provider_routing.rs` inline order becomes `understudy.rs` + thin wrappers; the `DangerousContentRouter` seam (`provider_failover.rs:90-108`) and `DangerContentRouter` (:443) must grow an `exclude` list (the effective profile + `alreadyTried`) and the courier skip — that widens a shared trait across the orchestrator, the enclave step, chat_create and the post office (the "required field on a shared struct crosses lane ownership" hazard).
4. **Child-hook ruling** (D.20): port `runRefusalLedgerChecks`/`chats_with_recorded_refusals` for `write_apply_equivalence`, or NO-PORT? If ported, `apply_writes` needs an async hook (the `ApplyHost` trait is sync today).
5. **Row key order for image trail rows** (D.17): pin as v4 writes it (profileKind before trigger) or reuse v5's `RouteAttempt` serializer (which would put it elsewhere) — the tier-2 JSON comparand decides; ask before choosing.
6. **`dangerCategories` write path in `apply_concierge_flip`:** v5 writes the literal `'[]'` via raw UPDATE; the `by:'concierge'` arm must write `'["moderation-refusals"]'` byte-exactly as v4's `chats.update` JSON-serialises it (no spaces) — confirm against the manual-flip oracle at the pin.
7. **The `ConciergeAnnouncer` seam takes a bare wire string** (`post_manual(chat_id, kind: &str)`); `auto-flagged-refusals` needs `details` — widen the seam's signature (touches `manual_flip.rs`, `concierge_notifications.rs`, the danger_resolver family's mock) or add a second method.
8. **Settings twin issue texts:** `z.number().int().min(0).max(10)` produces `invalid_type`/`too_small`/`too_big` issues whose exact `message` strings under zod 4.6.5 must be measured (the ONE Zod-issue home rule) before `api/settings.rs` grows the field; also its position (LAST, after `customClassificationPrompt`) fixes the stored key order.
9. **Which v5 `DangerousContentSettings` grows the field:** `db/chat_settings.rs:362` (must); `cheap_llm.rs:94` and `provider_failover.rs:60` are consumer subsets — the failover subset needs `mode` only now that `uncensored_text_profile_id` no longer gates the retry (a REMOVAL candidate, flagged for the reviewer rather than decided here).
10. **`primary_stream_tier3` / `image_generation_tier3` fixtures lack the ledger columns** — the widen must go through v4's own `ALTER`s (the fixture-vintage heal recipe), and the oracle cases must plant `chatSettings.dangerousContentSettings.autoSwitchAfterRefusals` deliberately (the Zod default 2 will otherwise flip a fixture chat to Flagged on the second refused arm and change every later row).
11. **The tool loops' re-stream failover** gets NO `dangerSettings` in v4 (only `runPrimaryStream` passes it) — port that asymmetry as-is; flag it as a probable v4 follow-up rather than "fixing" it.
12. **Help pages**: `dangerous-content.md` changed in BOTH commits — the re-vendor must be from `49059fb14`, and `help_tree_equivalence` must be run at that pin.
13. **Unresolved ORDER ambiguities, listed:** (a) whether `classify_refusal`'s debug/info lines count as capture-pinned surfaces in every family that now transitively calls it (route_trail_compose, fallback_engine, cheap_llm) — they fire on EVERY classification, so a sorted-field capture will see them everywhere; (b) whether the announcement's `postConciergeRefusalAnnouncement` chat read uses the read pool or the writer in v5 (v4 `findById` then `addMessage` — two round trips, the message can race a concurrent delete); (c) the `image_job_common.rs` story success line, deliberately UNPORTED at P4.93 with a recorded reason — v4 changed its bag, so the NO-PORT record must be re-ruled, not silently ported; (d) the dialog's `resolveUnderstudy` closure uses `resolveUncensoredTextUnderstudy` with `filter: supportsImageGeneration(provider)` — v5's twin is a PRIVATE `fn supports_image_generation(provider: &str)` at `crates/quilltap-core/src/api/settings.rs:174`, so the dialog's filter needs it made `pub(crate)` or re-homed beside `llm/image-capable`'s other twins.

Survey hygiene: the v4 checkout was left untouched (`git status --short` empty after the two `npx tsx -e` probes); the spelling guard finds zero hits in this file; line count is 318 physical lines of long paragraphs (~9k words) rather than the 500–900 short lines the skeleton estimated — every requested section is present.
