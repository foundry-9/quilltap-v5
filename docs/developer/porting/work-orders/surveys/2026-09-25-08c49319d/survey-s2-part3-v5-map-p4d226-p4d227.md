# SURVEY S2 — Part 3 of 4 — §B (v5 counterparts on main `2aed9a552`) + §C (families, oracle cases, fixtures)

## B. v5 counterparts on main `2aed9a552`

### B.0 The substrate gap (read first)
v5 has **NO** port of #73 (`8bd080267`, refusal-driven failover) or #74 (`49059fb14`, refusal ledger): `grep -rl 'moderation_refusal\|moderationRefusalCount\|lastModerationRefusalAt\|refusal_ledger' crates/quilltap-core/src crates/quilltap-host/src` hits only comment/prose in `moderation_finish_reason.rs`, `services/provider_failover.rs` (the "understudy" there is the ordinary fallback chain), `services/orchestrator.rs`. There is no `refusal.rs`, `refusal_ledger.rs`, `understudy.rs`, `image_failover.rs`, `current_state.rs`, `classifier_switch.rs`, no `refusal-*` announcement kinds, no `autoSwitchAfterRefusals` in either `DangerousContentSettings` struct, no ledger columns anywhere (not in `fresh_schema.json` either — and v4's Zod-walking `generateDDL` never emits them, so they need a boot ensure, Part 4 §E). Every "v5 counterpart" below that is NONE for that reason is marked **NONE (#73/#74)**; the P4.D225 lane owns those.

`services/dangerous_content/` on main (2,912 lines): `chat_override.rs` 349, `gatekeeper.rs` 776, `gatekeeper_job.rs` 473, `manual_flip.rs` 201, `mod.rs` 37, `moderation_wire.rs` 262, `prompt_text.rs` 34, `provider_routing.rs` 525, `resolver.rs` 255. Related: `services/chat_create.rs` 4457, `chat_enrichment.rs` 939, `concierge_notifications.rs` 415, `danger_scan.rs` 368, `file_fallback.rs` 1851, `story_background_job.rs` 1409, `appearance_resolution.rs` 504, `cheap_llm_exec.rs` 3602, `memory_processor.rs` 1197, `compression.rs` 713, `context_summary.rs` 1343, `context_summary_job.rs` 230.

v5 has TWO `DangerousContentSettings` types: `db/chat_settings.rs:362` (the full column struct — `mode, threshold, scan_text_chat, scan_image_prompts, scan_image_generation, uncensored_text_profile_id?, uncensored_image_profile_id?, display_mode, show_warning_badges, custom_classification_prompt?`; serialized in that key order) and `cheap_llm.rs:94` (the narrow `{ mode: String, uncensored_text_profile_id }` subset every cheap-LLM path consumes, plus `UncensoredFallbackOptions { danger_settings: &…, available_profiles, is_dangerous_chat }` at :103). Both are named in Part 2's sweep as the type that `ResolvedConciergePolicy` replaces.

### B.1 Per v4 unit → v5 (Part 1 §A + Part 2 §A)
| v4 unit | v5 file(s) : lines | what v5 does NOW |
|---|---|---|
| `chat.types` enums + 3 columns | `db/chats.rs:358-368` (`ChatCreate` carries `concierge_override: Option<String>` at :368, NO mode/setBy/reason), `db/chats.rs:494` `ChatUpdate` (no concierge field at all — the flip writes raw SQL), insert list `:844-849` names `conciergeOverride`; `db/chats_read.rs:71-84` `ALL_COLUMNS` names `conciergeOverride`, `:281` reads it POSITIONALLY as column 58 | four-state on `conciergeOverride`+`isDangerousChat` |
| `chat-override.ts` | `services/dangerous_content/chat_override.rs:43-45` consts, `:65-69` `enum ConciergeState { Monitored, Flagged, Vouched, Uncensored }`, `:74 as_str`, `:86 from_wire`, `:105 get_concierge_state` (override→label derivation), `:135`, `:146`, `:156`, `:167 is_classifier_on_duty` (Monitored\|Flagged; `None` chat → true) | four-state; no provenance/reason/mayFailOver/derive/withLegacy |
| `manual-flip.ts` | `manual_flip.rs:52-82` `ConciergeAnnouncer` trait (`post_manual(chat_id, kind: &str)`), `NoConciergeAnnouncer`, `RealConciergeAnnouncer`; `:95 apply_concierge_flip(db, announcer, chat_id, requested, chat: &Value)` — no-op on match; `Flagged` raw `UPDATE chats SET conciergeOverride=NULL,isDangerousChat=1,dangerScore=NULL,dangerCategories='[]',dangerClassifiedAt=?,dangerClassifiedAtMessageCount=?` + `manual-flagged`; `Monitored` clears the five + `manual-resumed`/`manual-safe`; `Vouched`/`Uncensored` set the override + kind. No options bag, no compare-and-set, no ledger reset, no `by:'concierge'` | four-state, five kinds |
| `classifier-switch.ts`, `current-state.ts` (both fns) | **NONE** (`readCurrentConciergeOnDuty` also NONE) | — |
| `concierge-state-presentation.ts` | SPA only: `apps/web/src/app/chat/concierge-state-presentation.ts` + `.v4.json` recorded copy + spec; `chat/concierge-mark.ts:94,113-128` (`!== 'monitored'`, `describeConciergeState(state, categories)` 2-arg) | four rows incl. `info` tone |
| `resolver.service.ts` | `resolver.rs:23-31` `DangerSource { Global, Default, ChatVouched, ChatUncensored, ChatTypeExempt }` + `as_str`, `:46` `ResolvedDangerousContentSettings { settings, source }`, `:53 default_dangerous_content_settings`, `:73 vouched_safe_dangerous_content_settings`, `:107 resolve_dangerous_content_settings(global: Option<DangerousContentSettings>, chat: Option<&Value>)` (exempt → uncensored → vouched → global/default) | pre-#75 four-state resolver; no policy struct, no `readConciergeSettings` |
| `legacy-concierge-settings.ts` | **NONE** | — |
| `image-failover.ts` | **NONE (#73)** — the image reroute is `provider_routing.rs:394 resolve_uncensored_image_profile_for_reroute` + `:430 is_image_moderation_error` (the pre-#73 W4.2/P4.D178 post-hoc reroute) and `image_job_common.rs:535-672,819,1170` | pre-#73 |
| `provider-failover.service.ts` Locked/on-duty gates | `services/provider_failover.rs:406-407` (`danger_settings.mode == "AUTO_ROUTE" && uncensored_text_profile_id.is_some()` — the empty-response uncensored retry); no hard-error refusal arm, no `refusal-not-permitted`, no `conciergeState` snapshot | pre-#73 |
| `writer.ts` kinds/texts | `services/concierge_notifications.rs:268-303` `ConciergeManualKind { ManualFlagged, ManualSafe, ManualVouched, ManualResumed, ManualUncensored }` + `from_wire`/`as_wire`, `:310 build_manual_content`, `:327 build_manual_opaque_content`, `:395 post_concierge_manual_announcement(db, chat_id, kind)`, `:407 post_concierge_danger_announcement`; `ConciergeDangerDetails` :46. NO `auto-flagged-refusals`, NO `ConciergeAutoFlagDetails`/`buildAutoFlag*`, NO refusal kinds | five kinds |
| `refusal-ledger.ts` | **NONE (#74)** | — |
| `base.repository.patchOnlyFields` / `chats.setConciergeMode` / `setDangerClassification` | no repository abstraction; the danger telemetry write is `gatekeeper_job.rs:321-342` (raw UPDATE of the five columns, no `updatedAt` — "v4's `chats.update` does not mint `updatedAt`"), the announcement `:349-359` from the job itself | — |
| `job-dispatcher.ts` commit hook | **NONE** — v5 has no forked child; `gatekeeper_job.rs` runs in-process (`handle_chat_danger_classification<M,C,An>` :105) | — |
| `chat-danger-classification.ts` | `gatekeeper_job.rs:127-135` (`is_classifier_on_duty` bail), `:213-216` (`resolve_dangerous_content_settings(global, None)`; `mode == "OFF"` → return — NO chat passed), `:351` threshold `danger_settings.threshold` | pre-#76 |
| `scheduled-danger-scan.ts` | `services/danger_scan.rs:53-95` (`DangerScanUserSettings { user_id, dangerous_content_settings }`, `find_all_danger_scan_settings` = `SELECT userId, dangerousContentSettings FROM chat_settings`, `any_user_danger_enabled` = any resolved mode != OFF), `:109-116 chat_needs_classification` (`is_classifier_on_duty`), `:185 run_scheduled_danger_scan`; `quilltap-host/src/host.rs:2054-2068 danger_scan_loop` | mode-based pre-check |
| `chats/route.ts` create-time state | `services/chat_create.rs:255` `concierge_state: Option<Option<Value>>` (raw tri-state), `:298`, `:1111-1112` decode via `ConciergeState::from_wire`, `:1576/1596/1619` call `apply_requested_concierge_state` (`:1889-1910`: `requested == Monitored` → return; `emitter.status("Briefing the Concierge…")`; flip; `debug "[Chats v1] Applied Concierge state at creation"`, returns `()` — the response is NOT re-read) | no `newChatsStartAs`, no off-duty ignore, response carries the as-inserted row |
| greeting desk | `chat_create.rs:2600` attempt 0 `should_use_uncensored_route(chat_row)`; `:2858-2885 generate_via_uncensored_desk`: resolves `dangerousContentSettings` per call, `mode != "AUTO_ROUTE"` → None; attempt 3 has NO `mayFailOver` gate (grep: no `may_fail_over`) | pre-#75 |
| chat GET projection | `api/salon.rs:477-485` emits `isDangerousChat`, `dangerCategories`, `conciergeOverride` (no `conciergeState/SetBy/Reason/RefusalCount`) | pre-#75 |
| chat PUT `conciergeState` | `api/types.rs:298-315` `concierge_state: Option<Option<Value>>` (double_option; `:5401-5440` test pins `"uncensored"` accepted, `"off"`/`null`/`42` → 400); `api/salon.rs:1330,1349-1356` (`from_wire` else `bad_request("Validation error")`), `:1529-1545` flip after `addParticipant` | four-state |
| `reclassify-danger` | `services/chat_admin.rs:365-372 chat_reclassify_danger` (clears five columns) | unchanged by #75 mechanics |
| list payloads (+setBy/reason) | `services/chat_enrichment.rs:762` `concierge_state: get_concierge_state(chat).as_str()`; `api/characters.rs:690`; `api/projects.rs:758`; `services/home.rs:63-66,458` | `conciergeState` + `dangerCategories` only |
| `restore.ts` / `import-entities.ts` `withConciergeModeFromLegacy` | `services/backup/restore/orchestrator.rs:489-490` (`strip_scenario_seeded_summary` then `chats.create`), `:836-856` chat settings via `ChatSettingsRepository::create`; `services/quilltap_import/entities.rs:647 import_chats`, `:769-773` (strips at both sites) | no derivation |
| `uuid-remap.ts` settings | `services/backup/uuid_remap.rs:253-254`, `:625-676 remap_chat_settings` (scalars incl. `uncensoredImageDescriptionProfileId` :632, `cheapLLMSettings` :645, `storyBackgroundsSettings` :666; `dangerousContentSettings` remap present — verify lines) | pre-#76 |
| export schema | `generators/qtap-export.schema.json:659` `conciergeOverride`; `services/qtap_export/schema-key-order.json:109` `conciergeOverride` | pre-#75 |
| `settings.types` / `chat-settings.repository` | `db/chat_settings.rs:122 CheapLlmSettings` (no `image_prompt_profile_id`? — the seed's cheapLLMSettings has none; `api/settings.rs:441 zod_opt_uuid(o, "imagePromptProfileId", …)` accepts it on PUT), `:362 DangerousContentSettings`, `:404 ChatSettingsCreate` (`:594,624,657` bind `dangerousContentSettings`, `uncensoredImageDescriptionProfileId`), `:505 ChatSettingsUpdate` (`:714-716`), `:858 find_by_user_id` (`:912,939,979,1122` hydrate both), `:1384 patch_create_return_shape`, test DDLs `:1475-1646`; `services/provisioning/chat_settings_seed.json:8,35,45,72` (columns + `dangerousContentSettings` default JSON) | pre-#76 |
| settings PUT | `api/settings.rs:683-770 zod_dangerous_content_settings` (hand-rolled Zod twin: MODES OFF/DETECT_ONLY/AUTO_ROUTE, threshold 0–1, three bools, two opt uuids, displayMode, badges, customClassificationPrompt — NO `autoSwitchAfterRefusals`), `:883-886` `uncensoredImageDescriptionProfileId`, `:999-1002`, `:1108-1114` error-precedence notes; GET hydration `db/chat_settings.rs:858` | pre-#76 |
| `help-settings` handler/tool bytes | `tools/help.rs:58 "overview"`, `:94 HELP_SETTINGS_INVALID` (eight categories), `:164 "chat" => fetch_chat`, `:171 overview`, `:210-211 dangerousContentSettings` in `fetch_chat`; `tools/definitions/data.rs:150-152 helpNavigate` (example `"/settings?tab=chat&section=dangerous-content"`), `:160-162 helpSettings` (eight-value enum + old description) | pre-#76; two snapshot moves |
| `help-guide/categories.ts` | SPA `apps/web/src/app/help/help-categories.ts:148` (`'dangerous-content'`), `:153-159` URL map (no concierge row); fixtures `help/__fixtures__/help-guide-tables.json` (1 hit), `label-from-url-vectors.json` (1 hit) | pre-#76 |
| `help/**` tree | `help/` embedded tree 129 files incl. `dangerous-content.md`; no `the-concierge.md` | pre-#76 |
| almanack | `almanack/phase2_machinery.rs:308` (`imagePromptProfileId` under cheapLLMSettings), `phase3_ledgers.rs:581-586,676-683` (`DangerousContentConfig` from `dangerousContentSettings`), `:773` (`uncensoredImageDescriptionProfileId`), `almanack/render.rs:948-964` (`#### The Concierge (Dangerous Content)` block), `almanack/types.rs:387-389,486` | pre-#76 |
| `foundry/subsystem-defaults.ts` | no v5 file matched the grep — locate the SPA's subsystem table (`apps/web/src/app/screens/foundry/*` or `settings` tab list) | — |
| `prettify.ts` labels | no `add-chat-refusal-ledger-v1` label grep hit in core — locate v5's pretty-label map (likely `quilltap-host` boot or `db/migrations_ledger.rs`) | — |
| `chat-utils.ts` / `home-data` | SPA `screens/salon/salon-list.ts:182`, `screens/home/recent-chat-item.ts:48`, `quick-hide/should-hide.ts:57-62`, `screens/characters/view/tabs/*`, `screens/prospero/cards/project-chats-section.ts`, `chat/sidebar/chat-section.ts` (four-state select), `screens/new-chat/new-chat.{types,logic,form}.ts`, `core/core-contract.ts:103-123,895-908` (`ConciergeState` union + `conciergeState?` on update/create) | four-state |

### B.2 Per consumer-sweep row (Part 2 §A.22 #) → the v5 read it must replace
| # | v5 file:line | current read |
|---|---|---|
| 1 | `api/chat_post_office.rs:768-818` | `should_use_uncensored_route` → `resolve_dangerous_content_settings(global, Some(&chat)).settings`; `danger.mode == "AUTO_ROUTE" && !is_dangerous_compatible`; passes `"AUTO_ROUTE"` + `uncensored_text_profile_id` to `resolve_provider_for_dangerous_content` |
| 2 | `api/memories.rs` (dry-run) — no `resolve_dangerous_content_settings` call found by grep; LOCATE how the dry-run builds `memory_processor` ctx (`danger_settings`) | — |
| 3 | `services/courier_transport.rs:692,865-871` | resolve → narrow `{ mode, uncensored_text_profile_id }` |
| 4 | `services/chat_create.rs:2600,2858-2885` | see B.1 greeting |
| 5 | `services/chat_create.rs:1111-1112,1889-1910` | no default seeding |
| 6 | `api/images.rs:1502-1530 read_concierge_inputs` (`ConciergeInputs { settings, all_profiles, cheap_settings }`, resolve with `None` chat), `:1623` (`mode != "OFF" && scan_image_prompts`), `:1649` (`mode == "AUTO_ROUTE"`) | no chat, no direct block |
| 7 | `api/memories.rs:1415-1420` | `settings.dangerousContentSettings.uncensoredTextProfileId` |
| 8 | `services/carina_memory_extraction.rs:205-208,237` | resolve + narrow; `is_dangerous_chat: should_use_uncensored_route` |
| 9 | `services/character_avatar_job.rs:438,502,514,619` (+ test fixtures `:1831-2033` `danger_mode` strings) | `mode != "OFF" && scan_image_prompts`; `mode == "AUTO_ROUTE"`; `uncensored_image_profile_id` |
| 10 | `services/dangerous_content/gatekeeper_job.rs:213-216,351` | `mode == "OFF"` bail (no chat); threshold |
| 11 | `services/context_summary_job.rs:125-138,193` | `:193 chain_settings = resolve(global, None).settings` (mode gate) |
| 12 | `services/memory_extraction_job.rs:207-210,294` | resolve + narrow |
| 13 | `services/scene_state_tracking/` — grep found NO `danger_settings`/`resolve_…` string there; LOCATE (the memory said the subsystem exists; it may build its `UncensoredFallbackOptions` via `cheap_llm_exec`) | — |
| 14 | `services/story_background_job.rs:293-314` (`is_dangerous_chat`, `uncensored_image_profile_id`, narrow settings), `:699` | pre-#76; no pre-route |
| 15 | `services/title_update_job.rs:251-260` | resolve + narrow |
| 16 | `services/danger_scan.rs:74-95`; `quilltap-host/src/host.rs:2055` | `any_user_danger_enabled` = mode != OFF |
| 17 | `services/backup/restore/orchestrator.rs:836-856`; `services/backup/uuid_remap.rs:625-676` | raw copy / old remap list |
| 18 | `services/context_summary.rs:644` (+ `:150 danger_settings: Option<…>`) | `should_use_uncensored_route` → uncensored cheap pick |
| 19 | `services/file_fallback.rs:541-551 get_uncensored_image_description_profile` (reads `uncensoredImageDescriptionProfileId` from chat settings), `:982,1047 chat_id: None`, `:1086 generate_image_description(deps, file)` (no options), `:1230`, `:1468` | no chat, no policy |
| 20 | `db/chat_settings.rs:562-660 create` + `provisioning/chat_settings_seed.json:72` | `dangerousContentSettings` default literal |
| 21 | `services/appearance_resolution.rs:356` (`danger_settings: &DangerousContentSettings`) | `mode == "OFF"` → return |
| 22 | `cheap_llm.rs:94-107,366` | narrow struct; `mode` gate + `uncensored_text_profile_id` |
| 23 | `services/cheap_llm_exec.rs:104-108` (`mode != "AUTO_ROUTE"` → None; `uncensored_text_profile_id`) | as v4 pre-#76 |
| 24 | `services/memory_processor.rs:309-310` (`danger_settings: Option<DangerousContentSettings>`, `is_dangerous_chat`) | narrow |
| 25 | `pascal/llm_consult.rs:43,253-260` | resolve + narrow; `should_use_uncensored_route` |
| 26 | `services/orchestrator.rs:122,1328-1335` (resolve; `is_dangerous_chat = should_use_uncensored_route`), `:1429-1438` (`mode == "AUTO_ROUTE" && !compatible` → reroute), `:2321,2427,3290,3455,3552` (narrow settings into cheap-LLM/failover), `:1605` in `message_finalizer.rs` (`is_classifier_on_duty`) | pre-#76 |
| 27 | `services/message_finalizer.rs:1605` (trigger gate `is_classifier_on_duty`), `:192,286,563,624,663` (`danger_settings`/`is_dangerous_chat` fields) | no `summaryClassification` |
| 28 | `services/orchestrator.rs` memory-recap fallback — LOCATE (`uncensored_fallback` near the recall build; grep `UncensoredFallbackOptions`) | — |
| 29 | `services/provider_failover.rs:406-407,64` (+ tests `:1638-2695`) | `mode == "AUTO_ROUTE" && uncensored_text_profile_id.is_some()` |
| 30 | **NONE (#73)** — `image_job_common.rs:535-672,819,1170`, `provider_routing.rs:394-410` are the pre-#73 reroute | — |
| 31 | `services/dangerous_content/provider_routing.rs:204-230` (`mode: &str` param, `!= "AUTO_ROUTE"`), `:317-340`, `:394-410`, `:443-482 DangerContentRouter` | mode string param |
| 32 | **NONE (#74)** | — |
| 33 | `services/dangerous_content/gatekeeper.rs:122,136,420,461,543` (`settings: &DangerousContentSettings`; `custom_classification_prompt`) | pre-#76 |
| 34 | **NONE (#73)** | — |
| 35 | `almanack/phase2_machinery.rs:308` | `cheapLLMSettings.imagePromptProfileId` |
| 36 | `almanack/phase3_ledgers.rs:581-586,676-683,773`; `render.rs:948-964`; `types.rs:387-389,486` | `DangerousContentConfig` |
| 37 | `tools/help.rs:94,164-171,210-211` | eight categories; `dangerousContentSettings` in `chat` |
| 38 | `tools/generate_image.rs:82,1579,1848-1858,1867,1882,1923,2077,2188,2206,2278-2285,2570,2807-2809` (+ tests `:2831-2985`) | `mode != "OFF" && scan_*`; `mode == "AUTO_ROUTE"`; `uncensored_image_profile_id`; `cheap_settings.imagePromptProfileId` at :2278 |
| 39 | `services/pre_compute.rs:91,236`; `services/build_context.rs:644`; `services/answer_confirmation.rs:458`; `services/compression.rs` (`UncensoredFallbackOptions`); `services/message_finalizer.rs` fields | narrow struct fields |
| — | `enclave/step.rs:1101-1107`, `enclave/lifecycle.rs`, `services/help_chat/summary_check.rs:99-105`, `services/image_job_common.rs:819` | additional v5 resolver callers NOT in v4's sweep (v5-only seams that also read `mode`/`uncensored_text_profile_id`) |
| — | `quilltap-host/src/spine.rs:466` (`danger_settings` from `dangerousContentSettings`), `:4092` (test literal) | host reads the retired bag |

Count: `grep -rln "conciergeOverride\|dangerousContentSettings\|isDangerousChat\|VOUCHED_SAFE\|uncensoredImageDescriptionProfileId\|imagePromptProfileId" crates/ apps/web/src harness/` = **139 files**: `crates/quilltap-core` 40, `crates/quilltap-harness` 14, `crates/quilltap-host` 4 (`tests/host_cadence.rs:39,106,111,174`, `tests/host_llm_log_cleanup.rs:44`, `tests/host_boot.rs:115,120`, `src/spine.rs:466,4092`), `crates/quilltap-web` 0 by that pattern (but `tests/dispatch_wrong_type_census.rs:37,2308,2797-2802` carries `conciergeState`), `apps/web/src` 27, `harness/oracle` 54. Full per-file list in the scratchpad run (`s2/v5-consumers.txt`, 261 lines).

`conciergeOverride` COLUMN binders (non-test): READ — `db/chats_read.rs:84` (`ALL_COLUMNS`, every chats SELECT) + `:281` (positional col 58, so a DROP shifts `sceneState`…`showThinking` too), `api/salon.rs:483-484` (GET projection), `chat_override.rs:107` (derivation); WRITE — `db/chats.rs:849,927` (INSERT column list; `ChatCreate.concierge_override`), `manual_flip.rs:126,148,174,187` (four raw UPDATEs); DDL/schema — `provisioning/fresh_schema.json` (chats), `generators/qtap-export.schema.json:659`, `qtap_export/schema-key-order.json:109`, `enclave/lifecycle.rs:1014` + `enclave/step.rs:1526` + `chat_create.rs:4000,4049` (test DDL/INSERTs), `danger_scan.rs:308` + `resolver.rs:175-242` + `chat_override.rs:238` (unit tests).

### B.3 Schema-side facts
- v5 `fresh_schema.json` `chats` (main partition): `… "isDangerousChat" INTEGER, "dangerScore" INTEGER, "dangerCategories" TEXT DEFAULT '[]', "dangerClassifiedAt" TEXT, "dangerClassifiedAtMessageCount" REAL, "conciergeOverride" TEXT, "answerConfirmationOverride" TEXT, "sceneState" TEXT, …` — no `conciergeMode*`, no ledger columns. `chat_settings`: `… "imageDescriptionProfileId" TEXT, "uncensoredImageDescriptionProfileId" TEXT, "defaultRoleplayTemplateId" TEXT, …, "storyBackgroundsSettings" TEXT DEFAULT '{…}', "dangerousContentSettings" TEXT DEFAULT '{"mode":"OFF","threshold":0.7,"scanTextChat":true,"scanImagePrompts":true,"scanImageGeneration":false,"displayMode":"SHOW","showWarningBadges":true}', "autoLockSettings" …`; `cheapLLMSettings` DEFAULT has no `imagePromptProfileId`.
- Readers of `fresh_schema.json`: `db/chats_read.rs:481,583-586` (`include_str!` + a test that the `ALL_COLUMNS` list matches the DDL's column set — ⚠ this test trips the moment the dump moves), `db/prompt_templates.rs:417-426`, `db/help_doc_chunks_repair.rs:15,117`, `db/connection_profiles.rs:83`, `db/files_generation_key_repair.rs:19`, `realtime/publish_sites.rs:1084`; the provisioning differential family (D23).
- `chat_settings_seed.json` lists column order incl. `uncensoredImageDescriptionProfileId` (:8/:45) and `dangerousContentSettings` (:35/:72).
- Boot-ensure precedent: `quilltap-host/src/host.rs:1271-1285` calls `quilltap_core::db::chats_transcript_version_repair::ensure_chats_transcript_version_column(main)` ("the column's ONLY source on EVERY instance, fresh included: v4 keeps the field out of `ChatMetadataSchema`… `generateDDL` walks the schema"). Sibling ensures in `db/`: `chats_cycle_order_repair.rs`, `chat_settings_composer_repair.rs`, `chat_settings_impersonation_voice_repair.rs`, `files_generation_key_repair.rs`, `help_doc_chunks_repair.rs`, `connection_profiles_{fallback,prefill}_repair.rs`, `fictional_clock_anchor_repair.rs`, `folders_unique_path_repair.rs`, `chat_messages_route_trail_repair.rs`, `migrations_ledger.rs` (v4's `migrations_state` cross-app ledger for DATA passes, e.g. `thinking_prefill_retire_heal`).

## C. Families, oracle cases, fixtures

### C.1 Harness/web families carrying the retired identifiers (env var → oracle case → fixture)
| family (`crates/quilltap-harness/tests/…`) | oracle var | oracle case / fixture | hits |
|---|---|---|---|
| `danger_resolver_equivalence` | `QT_ORACLE_DANGER_RESOLVER`, `QT_ORACLE_DANGER_MANUAL_FLIP` | `oracle/cases/danger-resolver.ts` (24), `danger-manual-flip.test.ts`; `oracle/fixtures/danger-manual-flip.json` (32), `build-danger-manual-flip-fixture.ts` | 6 |
| `danger_trigger_equivalence` | `QT_ORACLE_DANGER_TRIGGER` | `danger-trigger.test.ts` (6) | 4 |
| `danger_gatekeeper_tier3_equivalence`, `danger_routing_equivalence` | — | `danger-gatekeeper.test.ts` / `danger-gatekeeper.json` (5); `danger-routing.test.ts` | (routing: the `mode` param) |
| `chat_create_capstone_equivalence` | `QT_ORACLE_CC` | `chat-create-capstone.test.ts`; `build-chat-create-capstone.ts` (3), `chat-create-capstone.json` (1) | |
| `salon_reads_equivalence` / `salon_mutations_equivalence` | `QT_ORACLE_SALON_READS` / `…_MUTATIONS` | `salon-reads.test.ts` (12), `salon-mutations.test.ts`; `oracle/fixtures/salon.json`; committed `crates/quilltap-web/tests/fixtures/salon-{main,mount}.db`, `salon-long-*`, `salon-llm-logs.db` | 8 |
| `projects_routes_equivalence` | `QT_ORACLE_PROJECTS_ROUTES` | `projects-routes.test.ts` (4); `groups-projects.json`; `groups-projects-{main,mount}.db` | 4 |
| `characters_reads_equivalence` | `QT_ORACLE_CHARACTERS_READS` | `characters-reads.test.ts` (9); `characters.json`; `characters-{main,mount}.db` | 2 |
| `system_restore_state` (+ `restore_vintage_state`) | `QT_ORACLE_SYSTEM_RESTORE` | `system-restore.test.ts` (1); `crates/quilltap-harness/fixtures/restore-archives/*`, `build-restore-archive-bag-keys.ts` (6); `system-data-{main,mount,llmlogs}.db` | 4 |
| `chat_settings_tier2_equivalence` | `QT_ORACLE_CHAT_SETTINGS` | `chat-settings-tier2.ts`; `build-chat-settings-fixture.ts`, `chat-settings-tier2.json` (13) | |
| `settings_routes_equivalence` | `QT_ORACLE_SETTINGS_ROUTES` | `settings-routes.test.ts` (17); `settings.json` | 4 |
| `orchestrator_tier3_equivalence` | `QT_ORACLE_ORCHESTRATOR` | `orchestrator-tier3.test.ts`; `build-orchestrator-fixture.ts` (5), `orchestrator-tier3.json` (5), `oracle/lib/pinned-draws.ts` | 2 |
| `images_generate_route_equivalence` | `QT_ORACLE_IMAGES_GENERATE` | `images-generate-route.test.ts` (6); `images-collection.json`; `images-{main,mount}.db` | 3 |
| `image_generation_tier3_equivalence` | `QT_ORACLE_IMGGEN` | `image-generation.test.ts` (5); `build-image-generation-fixture.ts`, `image-generation.json` (2) | 5 |
| `story_background_job_tier3_equivalence` | `QT_ORACLE_STORY` | `story-background-job.test.ts` (5); `story-background-job.json` (22), `build-story-background-job-fixture.ts` (9) | 6 |
| `appearance_sanitize_gate_tier3_equivalence` | `QT_ORACLE_APPEARANCE_GATE` | `appearance-sanitize-gate.test.ts` (2); `appearance-sanitize-gate.json` (28) | 1 |
| `memory_processor_tier3_equivalence` | `QT_ORACLE_PROCESSOR` | `memory-processor-tier3.test.ts` (2); `memory-processor-tier3.json` (5), `build-memory-processor-fixture.ts` | |
| `message_finalizer_tier3_equivalence` | `QT_ORACLE_MESSAGE_FINALIZER` | `message-finalizer-tier3.json` (4), `build-message-finalizer-fixture.ts` (7) | |
| `compression_tier3_equivalence`, `file_attachment_tier3_equivalence` | `QT_ORACLE_COMPRESSION`, `QT_ORACLE_FILE_ATTACHMENT` | `compression-tier3.test.ts` (1), `file-attachment-tier3.test.ts` (1), `build-file-attachment-fixture.ts` | 1 each |
| `cheap_llm_selection_equivalence` | `QT_ORACLE_CHEAP_LLM_SELECTION` | `cheap-llm-selection.ts` (2); `cheap-llm-selection.json` (6) | |
| `backup_uuid_remap_equivalence` | `QT_ORACLE_UUID_REMAP` | `backup-uuid-remap.test.ts` (5); `uuid-remap-corpus.json` (6, sha256-guarded) | |
| `almanack_render_equivalence`, `almanack_tier2_equivalence` | — | `almanack-render.test.ts`, `almanack-routes.test.ts`; `build-almanack-fixture.ts` (3); `almanack-{main,mount,llmlogs,llmlogs-legacy}.db` | |
| home (`home-routes.test.ts`, `home-web.json` 5, `build-home-fixture.ts` 5; `home-{main,mount}.db`) | | | |
| `avatar_job_tier3_equivalence` (`avatar-job.json` 2), `in_scene_voiced_tier3_equivalence` (`build-in-scene-voiced-fixture.ts` 5), `precompute_equivalence` (`precompute-cases.json` 1; `episodic-recall-*.db`), `courier_transport_tier3_equivalence` (`build-courier-transport-fixture.ts` 1), enclave (`build-enclave-step-fixture.ts` 1), `chats-read-tier2.json` (2), `chats-tier2.json` (1), `help-tools-tier2.json` (3), `build-salon-fixture.ts`, `build-long-chat-fixture.ts`, `build-attach-file-fixture.ts`, `build-courier-images-web-fixture.ts`, `build-context-summary-service-fixture.ts` (4), `build-memory-pipeline-jobs-fixture.ts`, `build-carina-memory-extraction-fixture.ts`, `build-answer-confirmation-fixture.ts`, `build-danger-scan-fixture.ts` (8), `danger-scan-tier2.ts` | | | |
| `quilltap-web/tests/dispatch_wrong_type_census.rs` (449 at baseline; `conciergeState:42` arm :2797) | | | |
| `quilltap-host/tests/host_cadence.rs`, `host_llm_log_cleanup.rs`, `host_boot.rs` | hand-written DDL literals naming `dangerousContentSettings`, `isDangerousChat`, `conciergeOverride` | | |
| `help_tree_equivalence` (`QT_ORACLE_HELP_TREE`, `help-tree-sync.test.ts`) + `help_tree_embed_guard` (129) + `host_help_docs_boot` | file SET moves | | |
| tool-definition snapshots (`tools/definitions/data.rs` helpSettings/helpNavigate; the 59-count guard) | | | |
| SPA: `chat/concierge-state*.spec.ts`, `concierge-mark.spec.ts`, `conversation-header.spec.ts` (10), `sidebar/chat-section.spec.ts` (15), `screens/salon/salon-conversation.spec.ts` (7), `screens/settings/chat/async-select-cards.spec.ts` (8), `connection-profiles-shared-entry.spec.ts`, `core-contract.ts`; e2e `salon-concierge-four-state-flow.spec.ts`, `concierge-marks-flow.spec.ts`, `new-chat-flow.spec.ts`, `salon-danger-avatar-flow.spec.ts` | | | |

Confirmed/extended vs the ledger's prediction: add `danger_gatekeeper_tier3`, `danger_routing`, `appearance_sanitize_gate_tier3`, `memory_processor_tier3`, `message_finalizer_tier3`, `compression_tier3`, `file_attachment_tier3`, `cheap_llm_selection`, `backup_uuid_remap`, `almanack_render`/`almanack_tier2`, home, `avatar_job_tier3`, `in_scene_voiced_tier3`, `precompute`, `courier_transport_tier3`, enclave step/lifecycle, `chats_messages_read`/chats tier2, `help_tools` tier2, `danger_scan` tier2, `host_boot`, `restore_vintage_state`, `salon_fixture_p4d171_ensure`, `system_export_equivalence`, `web_edge_body_parse_guard`, `provider_sdk_version_guard` (SDK-bump regen event, independent). Sweep driver: `harness/tools/recipe_sweep.py` (2,145 lines; families keyed by test name, e.g. `"backup_uuid_remap_equivalence": (` at :167).

### C.2 How v5 builds fresh DBs for tests / boot-ensure / fixture widening
- **Provisioning reads `fresh_schema.json`** (`services/provisioning/`; `db/chats_read.rs:481` embeds it; D23 rule: re-dump from v4's live `generateDDL`, never hand-edit). `generateDDL` (`lib/database/schema-translator.ts:433`) → `extractSchemaMetadata` → walks the Zod object's `shape` (confirmed): a re-dump at `3b463d6b1` carries `conciergeMode`/`conciergeModeSetBy`/`conciergeModeReason` (nullable, NO DEFAULT), DROPS `conciergeOverride`, DROPS `chat_settings.uncensoredImageDescriptionProfileId` and `dangerousContentSettings`, ADDS `conciergeSettings TEXT DEFAULT '<the .default() literal JSON>'`, and does NOT carry `moderationRefusalCount`/`lastModerationRefusalAt` (absent from Zod) → boot ensure needed (P4.D225's), precedent `chats_transcript_version_repair.rs` + `host.rs:1284`.
- **Committed fixture DBs** (test-pepper, all under `crates/quilltap-web/tests/fixtures/`, 110 files): every `*-main.db` baked from v4 ≥ 4.9 carries `conciergeOverride` (v5's `ALL_COLUMNS` SELECT names it; a fixture LACKING a column v5 binds is fatal — see `a-nullable-optional-column-is-fatal-for-v5-not-v4`). After the port, v5 will name `conciergeMode*` in `ALL_COLUMNS`/INSERT and `chat_settings.conciergeSettings` in create/hydrate → EVERY main fixture pair v5 opens for a chats read or chat-settings read needs widening: `salon`, `salon-long`, `characters`, `groups-projects`, `images`, `home`, `system-data`, `almanack`, `chat-send`, `chat-admin`, `chat-cast`, `chat-compressed`, `chat-delete`, `chat-dialogs`, `chat-gallery`, `chat-scenario`, `autonomous`, `avatar-rolls`, `brahma`, `character-archive`, `character-generators`, `conversation-summaries-regen`, `cost-background`, `courier-images`, `documents`, `embedding-*`, `episodic-recall`, `files`, `headshoulders`, `help-chat`, `in-scene-voiced`, `inspector*`, `llm-log-cleanup`, `memories`, `migration-vintage/quilltap.db`, `mounts`, `pascal-run-custom`, `photos`, `post-office`, `profile`, `store-delete`, `subprompts`, `text-replacements`, `wardrobe-*`, `workbench` (i.e. all of them) — and if the DROP is mirrored, `conciergeOverride` must be dropped from each too (or v5's SELECT must stop naming it, which is the same event).
- **Fixture migrator precedent**: `harness/oracle/fixtures/migrate-fixtures-pascal-columns.ts` (P4.6ay unit 10 — runs v4's OWN `compareSchemas` + `generateAlterStatements` over every committed `*.db`, appending columns "the shape production actually has"), `migrate-system-data-schema.ts` (P4.70), `migrate-fixtures-episodic-columns.ts`, `migrate-fixtures-link-group-column.ts`, `migrate-memories-fixture-columns.ts`, `migrate-pascal-run-custom-columns.ts`, `build-migration-vintage-fixture.ts`. Test-side in-place widens: `crates/quilltap-web/tests/common/mod.rs:131,184` (`ALTER TABLE chats ADD COLUMN turnSkippingEnabled INTEGER`), plus `chat_settings_composer_web_routes.rs`, `photos_routes_equivalence.rs`, harness `chat_gallery`, `chat_admin_routes`, `search_replace`, `chat_rebuild_summary`, `post_office_routes`, `chat_regenerate_title_tier3`, `restore_vintage_state`. ⚠ The DROP has no precedent in any migrator: v4's `generateAlterStatements` only ADDs; the drop is a hand `ALTER TABLE "chats" DROP COLUMN "conciergeOverride"` per fixture (SQLite ≥ 3.35 — the migrator runs under v4's better-sqlite3, fine).
- Oracle-side fixtures that seed the retired columns by name (`build-*-fixture.ts` above, `danger-manual-flip.json`, `appearance-sanitize-gate.json` 28, `story-background-job.json` 22, `chat-settings-tier2.json` 13) must be re-recorded at the pin; the `uuid-remap-corpus.json` is sha256-guarded and is a projection of `system-data`'s ids (migrate in place, do not rebuild — `migrate-system-data-schema.ts` header).
