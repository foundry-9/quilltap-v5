# Work order P4.44 — three pinning follow-ups: the chunks-upsert create arm, per-delete `cleanup_thumbnails`, and the vision-path headers pin

**Status:** OPEN — not started.

## Preamble

- **v4 baseline for this round: `f6eac168`** (v4 4.8.0-dev, HEAD at planning
  2026-08-08, tree CLEAN). **Drift-check at lane start:** in
  `~/source/quilltap-server`, run `git log f6eac168..HEAD --oneline`; if v4
  moved, STOP and report. Any oracle this lane regenerates comes from
  `~/source/quilltap-server` at `f6eac168` (pin a detached worktree if HEAD
  drifted — `oracle-regen-pinned-v4-worktree`; the provider-corpus pin needs
  the `plugins/node_modules` + per-plugin symlinks).
- **Sibling lanes this round (disjoint ownership):** **P4.D60** (server
  drift — turn manager / spine / brahma / `api/salon.rs`) and **P4.D61**
  (SPA drift — `apps/web` only). None of this lane's files overlap either.
  This lane and P4.D60 both bump core + harness: **identical version bumps
  auto-merge silently; the unifier recounts as base + total bumps** (the
  parallel-round playbook rule).
- **Worktree:** carry the lane out on its own branch per the `carryout`
  skill.

## Mandate

Close three standing coverage/fidelity debts, each named in a prior round's
deferral list (phase-4.md candidate 3): (1) the conversation-chunks `upsert`
create arm no corpus op reaches, (2) v4's eager per-delete/overwrite
thumbnail cleanup (bug 43 tier 2, deferred at P4.D51), and (3) the
vision-path headers/abort behavior no family pins (deferred at P4.D55).
Items 1 and 3 are pure pinning (corpus + harness machinery); item 2 is a
small behavior port over an existing seam.

**This lane owns (and only touches):**

- `crates/quilltap-core/src/api/files.rs`, `crates/quilltap-core/src/api/
  engine.rs` (the `FileDelete`/`FileUpload` dispatch arms only),
  `crates/quilltap-core/src/api/chat_media.rs` (if the paths ripple),
  `crates/quilltap-core/src/services/file_storage.rs`,
  `crates/quilltap-core/src/services/delete_all.rs` (comment only).
- `crates/quilltap-harness/tests/{conversation_chunks_tier2_equivalence,
  request_builder_equivalence,maintenance_ops_tier2_equivalence}.rs`.
- `harness/oracle/fixtures/conversation-chunks-tier2.json`,
  `harness/oracle/cases/conversation-chunks-tier2.ts`, the provider
  request-envelope recorder + its corpus files, and
  `harness/oracle/fixtures/maintenance-ops.json` if extension is needed.

**Do NOT touch** the turn manager, the spine orchestrator, the brahma
module, `api/salon.rs`, or any `apps/web` file (the sibling lanes').
`api/engine.rs` is shared-by-area with no sibling this round — P4.D60
deliberately does not open it; keep to the two dispatch arms.

## Item 1 — the conversation-chunks `upsert` create arm (surveyed 2026-08-08)

**The deferral** (`status-log.md:61339-61343`, the f4955e0e round's §3): "the
uncovered `upsert` create arm (needs minted-id normalizer machinery — a
named follow-up)".

- `db/conversation_chunks.rs:539` `upsert` — the update arm (`:545-563`) is
  corpus-covered; the create arm (`:565-581`) mints `new_id` + `now_iso` and
  returns `(new_id, true)`, and **every existing corpus upsert hits an
  EXISTING row** (fixture ops at `conversation-chunks-tier2.json:328-379`
  all target seeded interchanges; the oracle case comment at
  `cases/conversation-chunks-tier2.ts:110-115` states the constraint; the
  Rust constants' doc at `conversation_chunks_tier2_equivalence.rs:144-150`
  names the gap).
- **Done =** add create-arm ops on a `(chatId, interchangeIndex)` pair NOT
  in the seed (with and without a supplied `embedding`), extend `normalize()`
  (`:170-190`) to placeholder an unpinned `id` and `createdAt` with the same
  `pinned_literals` test the `updatedAt` leg uses (v4 mints
  UUID + `new Date()`; v5 injects `UPSERT_NEW_ID`/`UPSERT_NOW_ISO`), keep
  dump ordering independent of the minted id, regenerate the family's oracle
  fresh at the pin, and **mutation-prove** (break the create arm's
  `embedding` passthrough at `conversation_chunks.rs:573` → red).
- ⚠ This family's recipe MUTATES its fixture — run against /tmp COPIES
  (`sweep-recipes-write-into-the-repo`).

## Item 2 — per-delete `cleanup_thumbnails` over `StorageBackend` (surveyed 2026-08-08)

**The deferral** (P4.D51 status header + `status-log.md:60776-60784`): the
eager per-delete/overwrite cleanup needs a `StorageBackend` thread; the
landed daily orphan sweep is the interim reaper. The two skip-site comments
name it: `api/files.rs:537-541` (inside `file_delete`, fn at `:468`) and
`:1125-1131` (the upload's overwrite leg; `file_upload` at `:1058`); cross
reference at `services/delete_all.rs:22`.

The seam already exists one level up: `engine.rs:606-611`
`qtap_file_storage() -> Option<Arc<dyn StorageBackend>>`; trait at
`file_storage.rs:510-529` (with the bug-43 `supports_list`/`list`); the
sweep is `sweep_orphaned_thumbnails` at `file_storage.rs:694-745` (its doc
at `:680-693` calls itself "v5's ONLY reaper" because of this deferral —
update it); key helpers `thumbnail_storage_key:170` /
`parse_thumbnail_storage_key:180`; the real backend
`quilltap-host/src/files_store.rs:139`.

- **Done =**
  1. `cleanup_thumbnails(backend, file_id)` in `file_storage.rs` — v4's
     per-file eager delete of every `_thumbnails/<fileId>_<size>.webp`,
     try/catch-and-log, NEVER failing the delete/upload (mirror v4's exact
     behavior — survey v4's implementation before writing).
  2. Thread an optional backend into `file_delete` + `file_upload`, call at
     the two skip sites, delete the deferral comments.
  3. Wire the two engine dispatch arms (`engine.rs:3838` `FileDelete`,
     `:3908` `FileUpload`) via `qtap_file_storage()`, following the existing
     `host.storage().as_ref()` pattern (`:3512`/`:3524`) and the
     `NotConfiguredStorageBackend` fallback (`:3943`) for hosts with no disk
     layer.
  4. Check the chat-media twins (`chat_media.rs:986` / `:1090`) against
     v4 — wire them ONLY if v4's equivalents run the cleanup; record the
     answer either way.
  5. Proof: the side effect is DB-invisible, so the pin is a unit-tier test
     over a fake `StorageBackend` asserting the exact keys deleted (and
     none extra), plus `maintenance_ops_tier2_equivalence` staying green
     (the sweep still finds zero orphans after an eager cleanup).

## Item 3 — the vision-path headers/abort pin (surveyed 2026-08-08)

**The deferral** (P4.D55 status header `:11-13` + Quirks `:230-234`): "the
vision path's headers/abort behavior is unpinned by any family (recorders
capture `{method,url,body}` only)" — confirmed at planning: NO harness
family pins provider HTTP headers.

- The unpinned path: `model/completion_provider.rs:141-150`
  (`transport_headers` + `apply_auth` → `TransportRequest`), the OpenRouter
  non-streaming vision routing at `:165-166`
  (predicate `request_builder/chat_completions.rs:200`);
  `transport_headers` itself at `model/transport.rs:79-94` (User-Agent
  everywhere + OpenRouter's `HTTP-Referer`/`X-Title`), proven today only by
  three in-module unit tests (`transport.rs:394-419`); abort/timeout at
  `transport.rs:218`/`:327-340` (v4's raw `fetch` + `AbortController`, cited
  in the module doc `:33-41`).
- **Done =** widen the v4 recorder feeding `request_builder_equivalence` to
  also capture the outbound headers (and record the abort/timeout arming),
  add a `headers` key to the recorded rows, and compare alongside
  body/method/url (`request_builder_equivalence.rs:288-305`) — normalizing
  the version-bearing `User-Agent` and the `Authorization` secret, and
  asserting at the post-`apply_auth` point (that is where
  `HTTP-Referer`/`X-Title`/auth actually land), not on `built.headers`
  alone. Regenerate the corpus at the pin and verify every PRE-EXISTING
  vector's `{method,url,body}` is byte-identical (the widening must be
  additive — the P4.11/P4.21 corpus precedent). If the abort-arming half
  cannot be recorded without new v4-side instrumentation beyond the
  recorder, land the headers pin and defer the abort half LOUDLY with the
  evidence (do not force a harness shape that lies).
- ⚠ Corpus regen against a pinned worktree needs the plugins symlinks and
  the installed SDK majors matching package.json (openai 7,
  `@openrouter/sdk` 1.2) — see the D33 lane record.

## Tiered deliverables

**Tier 1 (must land):** items 1 and 2 whole.
**Tier 2 (should land):** item 3's headers pin.
**Tier 3 (explicit loud deferrals):** item 3's abort-arming half if the
recorder cannot see it (record the evidence); anything else that can't land
refuses loudly in the lane record — never a silent stub.

## The differential requirement, restated

Item 1 extends an existing tier-2 family (oracle recipe in the case file's
header); item 3 extends the recorded-corpus family (regen recipe in
`request_builder_equivalence.rs`'s header + the D33 record); item 2's pin is
unit-tier by necessity (DB-invisible side effect) plus the existing
maintenance family as the no-regression guard — state that explicitly in the
lane record rather than inventing a fake differential. First-run-green
additions must be mutation-proven (the D24 rule).

## Fixtures

- **Consumes** `conversation-chunks-tier2.json` (extends it),
  `maintenance-ops.json` (read-only unless a gap forces extension — then
  re-run its family), and the request-envelope corpus (widens it).
- **Owns** regenerating the oracles/corpora of every family whose fixture or
  corpus it moves, fresh at `f6eac168`, each grepped for a fresh marker
  (`oracle-regen-silent-stale-pass`).

## Shared contract

None with the sibling lanes (no shared types or wire surfaces). The only
meeting point is version-bump accumulation on core + harness, noted in the
Preamble.

## Ownership

- **This lane (P4.44)** owns the files in the Mandate. **Bumps
  `quilltap-core` and `quilltap-harness`** (P4.D60 also bumps both — the
  unifier recounts). If `quilltap-host` is touched (it should not need to
  be — `LocalStorageBackend` already implements the trait), bump it too and
  say so in the lane record.
- `CHANGELOG` and `status-log.md` are append-only for all lanes
  (union-merge).

## Verification gate (before declaring done)

- `cargo fmt --all` clean; `cargo clippy --workspace --all-targets` clean on
  BOTH feature sets.
- `cargo test --workspace` green with this lane's env vars — the three
  families (`conversation_chunks_tier2`, `request_builder_equivalence`,
  `maintenance_ops_tier2`) confirmed RUN by name with `--nocapture`, zero
  SKIP lines.
- `cargo build --release` clean.
- No `apps/web` change in this lane.
- Per-commit rule: follow `.claude/commands/commit.md`; end commit messages
  with the Co-Authored-By trailer.
