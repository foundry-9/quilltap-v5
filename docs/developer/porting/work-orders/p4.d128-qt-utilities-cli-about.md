# Work order P4.D128 — the client/CLI drift: the qt-* utilities sweep (`97d0b8f8e`), the four completion flags (`57e7b1bc2`), and the About provider list + Live-interface bullet (`8440b6391`'s one code hunk)

**Status:** OPEN — not started (ordered 2026-08-26).

## Preamble

- **v4 baseline for this round: `f3892158d`** (v4 main). The round absorbs
  the FOURTEEN commits past it (drift-ledger §3); this lane owns two PORT
  rows (`97d0b8f8e`, `57e7b1bc2`) plus the single code hunk of
  `8440b6391` (§Shared contract §D — the docs remainder is P4.D129's
  ratification). At planning (2026-08-26) the v4 checkout was on `main`,
  tree CLEAN, HEAD `8872d7efc`.
- **Run the drift ledger's §2 freshness probe at lane start**
  (`docs/developer/porting/drift-ledger.md`). If it fails — **STOP and
  report**; a lane never writes the ledger.
- **Regen rule in force: pin required** (ledger §1). Build ONE lane-unique
  detached worktree (all THREE symlink classes — recipe in drift-ledger
  §5.1; suggested path `/tmp/qt-v4-pin-p4d128-8872d7efc`) pinned at
  **`8872d7efc`** for the Tier R runs (Tier R drives v4's REAL launcher —
  the pinned tree carries the fixed templates, so the red-first flip is
  v5-side only). Spec reads per commit via `git show <sha> -- <path>`
  (ledger §5.3). Never two sweeps concurrently (§Shared contract §C);
  the Playwright port-4319 rule means this lane's e2e runs (if any)
  cannot overlap a sibling's (`e2e-port-4319-is-repo-wide.md`).
- **Sibling lanes:** P4.D126 (`p4.d126-memory-wipe-batch-bug103.md`),
  P4.D127 (`p4.d127-provider-cheapllm-drift.md`), P4.D129
  (`p4.d129-dedup-neutrality-ratify.md`). §Shared contract and
  §Ownership below are binding and identical in all four orders.
- **Worktree:** per the `carryout` skill; edit inside the worktree, not
  main. SPA worktree deps: `npm ci`, never `cp -a`
  (`spa-worktree-node-modules.md`). `CHANGELOG.md` and `status-log.md`
  are append-only for everyone.

## Mandate

Port the round's client and CLI surfaces whole: (1) v4's release-window
Tailwind→qt-* conversion — the two NEW solid hover utilities v5's
stylesheet lacks (any template already spelling them is INERT today),
the 20 class-site conversions mapped onto their v5 twins, the two
deliberately-visible shifts carried exactly, and the two checkbox
conversions; (2) the four documented-but-never-completed CLI flags
byte-copied into v5's three completion templates, Tier R red-first, plus
v4's new token-level coverage guard mirrored; (3) the About page's
provider sentence gains DeepSeek, Z.AI, NanoGPT AND the page gains v4's
new "Live interface" bullet in v4's position.

**Must NOT touch:** any `crates/quilltap-core` source (P4.D126/P4.D127
own the moving files there); the request-envelopes corpus; the restore
fixtures. v4's `help/cli-completion.md` + `packages/quilltap/README.md`
prose → the **`p4.9i2` bank**. v4's `packages/theme-storybook` mirror has
NO v5 analog — record, don't port.

## Survey-verified starting points (fresh survey 2026-08-26)

⚠ Port from the shipped hunks, never the commit message (ledger §5.3).

### Unit 1 — `97d0b8f8e` (qt-* utilities; 15 v4 files, 20 sites)

- v4 `app/styles/qt-components/_utilities.css` (+8): NEW
  `.hover\:qt-bg-primary:hover { background-color: var(--color-primary); }`
  and `.hover\:qt-bg-success:hover { background-color:
  var(--color-success); }` — the missing solid-fill siblings of the
  destructive form (Tailwind generates no variants for `@layer
  utilities`, so an unwritten hover form is inert — v4's own words).
  **Verified 2026-08-26: v5's
  `apps/web/src/styles/qt-components/_utilities.css` has ONLY the
  fractional forms** (`hover\:qt-bg-primary\/10` at :795,
  `hover\:qt-bg-success\/10` at :774 — no solid pair). Add both, in the
  sibling positions v4 uses (primary block ~:830, success block ~:868 of
  v4's file).
- The 20 sites across 15 v4 files (map each to its v5 twin; sixteen are
  same-token semantic fills — `bg-destructive`→`qt-bg-destructive`,
  `bg-success`→`qt-bg-success`, `hover:bg-primary`→`hover:qt-bg-primary`
  — nothing moves on screen): `AuroraView.tsx`, `CharacterHeader.tsx`,
  `ProjectDetailHeader.tsx`, `DeleteProjectDialog.tsx`, `ChatCard.tsx`
  (:268, the odd ternary branch), `FileDeleteConfirmation.tsx`,
  `FilePreviewActions.tsx`, `OrphanCleanupModal.tsx`,
  `DeletedImagePlaceholder.tsx`, `GalleryImage.tsx`,
  `ImageMetadata.tsx`, `image-gallery.tsx`, `memory-recall-card.tsx`,
  `TaskFilters.tsx` (:140, odd ternary branch). Take the hunk list from
  `git show 97d0b8f8e` and sweep the v5 twins — several v5 templates may
  already be qt-* (the P4.D117 sweep) or have no matching class; record
  per-site PORTED / ALREADY-QT / NO-SITE, the P4.D117 census idiom.
- The TWO deliberately-visible shifts (carry, don't normalize away):
  `CharacterHeader.tsx:139` avatar placeholder
  `bg-gray-300/dark:bg-slate-700` → `qt-bg-muted` (matches
  `--qt-avatar-bg`); `image-gallery.tsx:176` overlay `bg-black +
  bg-opacity pair` → `qt-bg-overlay-medium` **dropping the redundant
  bg-opacity pair** (the opacity fade on the same element covers it;
  v4 cites `GenerateImageView.tsx:315` as the precedent). v5 twin found:
  `apps/web/src/app/images/image-gallery.ts` still carries `bg-opacity`
  (verified 2026-08-26 — the only `bg-opacity` hit in the SPA). The
  avatar-placeholder twin has NO `bg-gray-300` hit in v5 — measure what
  v5's character header renders there and converge it on `qt-bg-muted`
  or record NO-SITE with the evidence.
- Both checkboxes in v4 `memory-recall-card.tsx` (:150, :169) →
  `mt-1 qt-checkbox` (replacing
  `mt-1 h-4 w-4 rounded border-input text-primary focus:ring-primary`).
  v5 twin: `apps/web/src/app/screens/settings/memory/memory-recall-card.ts`
  (+ its spec). `qt-checkbox` exists in v5 as a component class
  (variables at `_variables.css:161-163`) — verify the class itself is
  defined in v5's stylesheet before relying on it.
- After the sweep: run `check-qt-classes` (it runs in `npm run lint` and
  ahead of `npm test`; false-positive shapes in
  `qt-class-guard-false-positives.md`). v4's post-change count is 920
  defined classes with every guarded reference resolving — v5's count
  will differ (934 at P4.D117); assert resolution, not the number.
  Class-string traps: `[class]` reorders tokens
  (`angular-class-binding-reorders-tokens.md`); a `[class.foo]` binding
  on a qt-icon host is inert (`qt-icon-class-binding-is-inert.md`).

### Unit 2 — `57e7b1bc2` (the four completion flags)

v4 `packages/quilltap/lib/completion/{bash,zsh,fish}.template`; v5 twins
`crates/quilltap-cli/src/help/completion/{bash,zsh,fish}.template`
(byte-copied templates — the P4.D118 precedent). The four flags
tab-completion never offered, though `--help` documents them:

- **bash** (+9/−2): `--format` appended to `vf_docs` (the value-flag
  list — without it `--format json <TAB>` reads `json` as the verb: bug
  101's exact failure mode); a NEW `--format)` case completing
  `args json`; `--format` appended to `docs_flags` (line-continuation
  block).
- **zsh** (+1): `'--format[For docker-mounts: output shape]:format:(args json)'`
  in `_quilltap_docs`.
- **fish** (+3): `docs --uri` ('Show canonical qtap:// URIs'),
  `docs --base64` ('Base64 transfer for binary files'), and the
  docker-mounts `--format` line (`-x -a 'args json'` via
  `__quilltap_using_subverb`).
- Byte-copy the hunks; then Tier R (`crates/quilltap-cli/tests/
  cli_differential.rs`) red-first — the completion cases comparing v5
  against v4's REAL launcher from the pin should be exactly the flipping
  set (the P4.D118 shape: red-first exactly the changed cases → all
  green).
- v4's new `completion-coverage.test.js` (95 lines) guards: every long
  flag in a subcommand's `--help` is offered by all three templates, and
  bash's `vf_*` value-flag lists cross-check zsh's `:value:` specs —
  **as whole tokens, not substrings** (the review-bot finding: a
  substring match passes for a missing `--max` whenever `--max-nodes`
  has it as a prefix; vf_* lists are space-delimited → Set of tokens;
  template scans use a trailing-boundary match). Mirror this guard in
  `crates/quilltap-cli/tests/completion_behavior.rs` — v5's help text
  and templates are both in-crate, so the guard is self-contained;
  red-prove it against the pre-fix templates.

### Unit 3 — `8440b6391`'s AboutView hunk

- v4 `app/about/AboutView.tsx`: (a) the Multi-provider bullet becomes
  `Anthropic, OpenAI, Google Gemini, Grok, DeepSeek, Z.AI, NanoGPT,
  Ollama, OpenRouter, and OpenAI-compatible APIs`; (b) a NEW bullet
  between "LLM tools" and "Database protection": title **Live
  interface**, body: `a single multiplexed socket tells every open tab
  the moment something changes — queued errands, autonomous rooms,
  generated backdrops — so screens refresh themselves rather than asking
  again every few seconds, and every "4m ago" in the house turns over on
  the same tick` (transcribe the entities from the hunk —
  `&mdash;`/`&ldquo;`/`&rdquo;` render as em-dashes and curly quotes;
  v5's about-page stores plain strings, so carry the RENDERED
  characters).
- v5 twin: `apps/web/src/app/screens/about/about-page.ts` — the
  seven-provider `body:` at ~:398 (verified 2026-08-26) and the
  title/body list the new bullet slots into, in v4's position. ⚠ v5's
  About page reproduces v4's socket-less transport differently (the
  hints ride SSE per the f3892158d round's locked mechanism divergence)
  — port v4's STRING as written anyway; the register is user-facing copy,
  and the round's mechanism-divergence record already covers the
  "socket" word. Spec-pin the two strings (the about-page spec idiom
  from the P4.D68 About release-freshness mirror).

## Tiered deliverables

**Tier 1 (must land):**
1. The two solid hover utilities in v5's `_utilities.css`.
2. The class-site sweep with the per-site PORTED/ALREADY-QT/NO-SITE
   census in the lane record; the two visible shifts and the two
   checkboxes carried; `check-qt-classes` + `npm run lint` green.
3. The three completion templates byte-patched; Tier R red-first
   (exactly the flipping cases named) → green.
4. The AboutView strings, spec-pinned.

**Tier 2 (should land):**
5. The token-level coverage guard mirrored into
   `completion_behavior.rs`, red-proven against the pre-fix templates.
6. A mutation pass (drop one template hunk → the guard and/or Tier R
   redden; drop a hover utility → the guard or a spec catches the inert
   class).

**Tier 3 (explicit deferrals, loud):**
7. `help/cli-completion.md` + `packages/quilltap/README.md` rows →
   `p4.9i2` bank (list them).
8. The 💸 dogfood rows: a real `docs docker-mounts --format <TAB>` in
   all three shells; the About page's new bullet on screen. Name them in
   the lane record.

## The differential requirement, restated

Unit 2's equivalence test is Tier R itself — `cli_differential.rs` drives
v4's REAL `npx quilltap` launcher from the pinned worktree (the P4.D66
recipe; 188 cases at last count — expect growth only if new cases are
added deliberately). Unit 1 and Unit 3 are client-side: the oracle is
v4's source (transcription + spec pins, the P4.D113/P4.D116 v4-client
idiom); where a v5 template diverges structurally, record the divergence
with its reason rather than forcing a false byte-match. `ng test` /
`ng build` run through `npm test` / `npm run build`
(`ng-test-defaults-to-watch-mode.md`).

## Fixtures

None consumed or delivered. Tier R needs no DB; its venue is the pinned
v4 worktree + the built v5 CLI.

## Shared contract (BINDING — identical text in all four orders)

- **§A Family ownership for this round's regens:** P4.D126 owns
  `system_delete_data_equivalence`, `system_restore_equivalence`,
  `system_restore_state`, `restore_vintage_state`,
  `qtap_import_equivalence`, `memory_delete_tier2_equivalence`,
  `memory_cascade_tier2_equivalence`. P4.D127 owns
  `request_builder_equivalence` + the committed request-envelopes corpus
  (`harness/oracle/fixtures/request-envelopes/`),
  `response_parse_equivalence`, `cheap_model_equivalence`. P4.D128 owns
  the CLI Tier R (`cli_differential.rs`) + `completion_behavior.rs`. No
  other lane regenerates or edits these. P4.D129's bulk neutrality sweep
  EXCLUDES every family in this section by name and hands any red in
  them to the owning lane.
- **§B Pins:** the round's target baseline is **`8872d7efc`**. Each lane
  regenerates its OWN (§A) families from a lane-unique worktree pinned at
  `8872d7efc`; regens of families unrelated to any lane's commits stay
  pinned at `f3892158d` — except P4.D129, whose deliverable is exactly
  the `8872d7efc` regen of the non-§A families.
- **§C Sweep serialization:** `recipe_sweep.py` bulk runs (`--run-all`)
  are single-flight repo-wide. Lanes 126–128 run per-family regens only.
  P4.D129 runs its bulk sweep AFTER lanes 126–128 have closed their
  branches (execution arrangement in the round report); if forced to run
  earlier it must not touch §A families or their /tmp staging.
- **§D The AboutView split:** `8440b6391`'s `app/about/AboutView.tsx`
  hunk (provider list + the NEW "Live interface" bullet) is ported by
  P4.D128; the commit's docs remainder is ratified NO-PORT by P4.D129.
  Neither lane touches the other's half.

## Ownership (BINDING — identical table in all four orders)

| Path | Owner |
|---|---|
| `crates/quilltap-core/src/services/delete_all.rs`, `services/backup/restore/**`, `services/quilltap_import/**`, `db/memories.rs`, the new legacy-fields helper module | P4.D126 |
| §A families for P4.D126 + any new/changed restore archive fixture | P4.D126 |
| `crates/quilltap-core/src/model/request_builder/chat_completions.rs`, `services/cheap_llm_exec.rs`, `crates/quilltap-core/src/realtime/bus.rs` | P4.D127 |
| §A families for P4.D127 + the request-envelopes committed corpus | P4.D127 |
| `apps/web/**` (utilities CSS, component class sweep, `about-page.ts`), `crates/quilltap-cli/**` (completion templates, `completion_behavior.rs`, Tier R cases) | P4.D128 |
| No v5 source by default; MAY add (loudly, in its record): a harness pin for `help_doc_chunks`' blob-column re-assert, vestigial-twin removals of the knip-deleted exports (wardrobe helper modules) | P4.D129 |
| `CHANGELOG.md`, `docs/developer/porting/status-log.md` | append-only, all lanes |
| `docs/developer/porting/drift-ledger.md` | NOBODY (lanes never write it) |

Version bumps: each lane bumps the crates/SPA it touches per
`.claude/commands/commit.md`; the unifier recounts at the wire.

## Verification gate (before declaring done)

1. `npm run lint` (includes `check-qt-classes`) + `npm test` + `npm run
   build` in `apps/web` — full logs, background + notification.
2. `cargo fmt --check`; `cargo clippy` BOTH feature sets; `cargo test
   -p quilltap-cli` plus the workspace suite.
3. Tier R + `completion_behavior` re-run BY NAME against the pin, the
   red-first case list recorded.
4. Commits per `.claude/commands/commit.md`; lane record appended to
   `status-log.md`.
