# Work order P4.D129 — the `dcab791c2` dedup-sweep neutrality proof + the round's five NO-PORT? ratifications + the two riders

**Status:** CLOSED — UNIFIED on main (2026-08-27, the 4.9.0-push round;
lane branch `claude/p4-dedup-neutrality-ratify-8b82cb`, unified on
`unify/4.9.0-push-round`). The bulk sweep ran 415 families → 410 GREEN,
1 refused-by-design, 4 reds each dispositioned by a baseline-vs-target
pin sandwich, **not one attributable to `dcab791c2`** — with a recorded
§C scheduling deviation (ran while siblings were open, under the
"forced earlier" clause, with the staging-disjointness proof) and a
recorded exclusion-list slip (`cli_differential` ran; its red was
P4.D128's expected one, handed over untouched). **The headline: the
sweep is NOT neutral on the four title-generation cleaners** — measured
10/76 diverging vectors from the second-trim collapse, v5 carried the
pre-sweep spelling, and no family could see it; ESCALATED and **landed
at unification** (the wires commit: both cleaners second-trim,
red-first unit arms, the `regen_title_quoted_padded_inside` tier-3
case both sides). The scenarios-POST "latent crash" prose was WRONG
about its own diff (no such defect shipped; v5 has no analog by
construction); the wardrobe twin-collapse ratified neutral on every
reachable shape with v5 immune by design. Five NO-PORT ratifications
landed with evidence (incl. the HAIR-dedup 6/6 byte-identical
measurement and the API.md three-path cross-check). Riders: the
blob-registry claim made EXECUTABLE (`embedding_blob_binding_guard`,
mutation-proven three ways) and one vestigial wardrobe twin removed
(`resolve_shared_wardrobe_tiers_for_project`, dead in both trees). Nine
sweep recipes repaired + the driver's `--nocapture` splice root-fixed
(the continued-command bug that disarmed the SKIP-masquerade guard);
the splice gained its self-test regression pin at unification (the §3
review's minor). Also discharged at unification: the finding-#47
web-edge tripwire retired to a plain equality, and the
`backup_uuid_remap` neutrality gap closed by a baseline-vs-target
sandwich (corpus + NDJSON byte-identical across the drift block; the
committed corpus refreshed for pre-existing 4.8.2 default-key
staleness). Lane record + escalation dispositions: `status-log.md` →
"P4.D129".

**Scheduling note (as ordered):** this lane was to run AFTER lanes
P4.D126–P4.D128 closed (§Shared contract §C) — its bulk sweep must not
overlap their regens.

## Preamble

- **v4 baseline for this round: `f3892158d`** (v4 main). The round
  absorbs the FOURTEEN commits past it (drift-ledger §3); this lane owns
  the six NO-PORT?/ratification rows: `dcab791c2` (the 155-file dedup
  sweep — **ratified by NEUTRALITY PROOF, not by reading**; ledger row),
  `487ae57fe` (tests + a claimed structure-only extraction),
  `561466cfe` (knip sweep), `7509c5cfb` (dependency metadata),
  `c0352fdba` (CHANGELOG only), and `8440b6391`'s docs remainder
  (§Shared contract §D — the AboutView hunk is P4.D128's). At planning
  (2026-08-26) the v4 checkout was on `main`, tree CLEAN, HEAD
  `8872d7efc`.
- **Run the drift ledger's §2 freshness probe at lane start**
  (`docs/developer/porting/drift-ledger.md`). If it fails — **STOP and
  report**; a lane never writes the ledger.
- **Regen rule / pins:** build ONE lane-unique detached worktree (all
  THREE symlink classes — drift-ledger §5.1; suggested path
  `/tmp/qt-v4-pin-p4d129-8872d7efc`) pinned at **`8872d7efc`**. This
  lane's deliverable is precisely the thing the standing rule otherwise
  forbids: regenerating the NON-sibling families at the post-drift pin
  and proving them byte-identical/green (§Shared contract §B carves this
  out). The sweep runs through
  `harness/tools/recipe_sweep.py --run-all --v4 <pin>` — single-flight
  (§C), full log captured (never `| tail`), SKIP lines audited
  (`recipe-sweep-driver` SKIP-masquerade; a 0.00 s family without its
  `QT_FIXTURE_*` vars is a silent skip, not a pass).
- **Sibling lanes:** P4.D126 (`p4.d126-memory-wipe-batch-bug103.md`),
  P4.D127 (`p4.d127-provider-cheapllm-drift.md`), P4.D128
  (`p4.d128-qt-utilities-cli-about.md`). §Shared contract and §Ownership
  below are binding and identical in all four orders.
- **Worktree:** per the `carryout` skill; edit inside the worktree, not
  main. `CHANGELOG.md` and `status-log.md` are append-only for everyone.

## Mandate

Ratify the round's six non-porting rows with evidence, at the P4.D31 /
P4.D44 standard: the dedup sweep by a measured neutrality proof over the
regenerated families plus a hunk-level answer for its two
not-obviously-neutral spots; the other five by targeted hunk reads. Two
riders come with the rows (both named in the ledger + phase-4.md
candidate 1): the `help_doc_chunks` blob-column re-assert pin, and the
vestigial-twin check for the knip-deleted exports. This lane changes NO
v5 behavior; any behavior gap it discovers is escalated as a named
finding or a new order candidate, never fixed ad hoc inside this lane.

**Must NOT touch:** every path in §Ownership owned by a sibling; the §A
families and their /tmp staging; committed corpora (P4.D127 owns the
request-envelopes regen). Its own permitted edits are listed in
§Ownership (the two rider artifacts, loudly).

## Survey-verified starting points (fresh survey 2026-08-26)

⚠ Ratify from the shipped hunks, never the commit message (ledger §5.3).

### Unit 1 — `dcab791c2` neutrality proof (the round's largest risk)

155 files, ~2,800 lines removed, spanning route factories, hooks, tool
handlers, the data layer (`lib/database` 12 files, repositories 6),
components, and the provider wire (`@quilltap/plugin-utils` 2.5.0 shares
one `buildRequestBody` between send/stream; nanogpt 1.1.1 single-sources
base URL + image MIME list; ollama 1.0.46 dedups the think-retry and
request build). v4 claims "same strings, status codes, wire bodies, and
log lines" and explicitly pins byte order (the `stream`/`stream_options`
keys SPREAD mid-literal to keep their historical position between `stop`
and `user`) — the tell that this is where a claimed-neutral sweep can
move a byte.

1. **The bulk proof:** `recipe_sweep.py --run-all --v4 <pin>` at
   `8872d7efc`, EXCLUDING the §A sibling-owned families. Expected
   verdict: every run family green with oracles regenerated at the
   post-sweep tree — that IS the neutrality proof for those surfaces
   (the P4.D31 "non-sibling families" method). Any red: first check the
   drift table for which commit owns the surface (a red in a family a
   sibling's commit touches → hand it to that lane, it is not yours);
   an unexplained red = the sweep was NOT neutral there — **escalate
   with the family name, the differing bytes, and the responsible v4
   hunk**; do not patch v5 to chase it inside this lane.
2. **The two named non-neutral hunks** (ledger row):
   - the scenarios POST "latent crash — `repos` referenced without
     destructuring": a JS-only defect in v4's own code. Read the hunk
     (`git show dcab791c2 -- <the scenarios route>`), then confirm v5
     has NO analog (v5's scenario routes were ported at P4.D115 and are
     typed — cite the v5 file in the record).
   - the collapsed twins: `resolveDefaultOutfit` → `buildDefaultOutfit`
     and the help/normal chat-title pair. Collapsing twins is only
     neutral if the twins AGREED — that is a measurement, not a
     reading: diff the two pre-collapse v4 functions (`git show
     dcab791c2~1:<path>`) and record whether any input class got a
     different answer; if the twins disagreed, the collapse picked a
     winner and the affected family regen (wardrobe / title — §A owners
     or the bulk sweep) is where it shows.
3. Record the sweep totals (families run / green / excluded-by-§A /
   skipped-with-reason) in the lane record — no silent caps.

### Unit 2 — the four hunk-read ratifications

- `487ae57fe` (tests + extraction): nine test files + docs; the ONLY
  lib/app hunks extract the bug-77 notice logic from
  `useSSEStreaming.ts` into `useToolExecutionStatus.ts`. Verify the
  extraction hunks are structure-only (same statements, new home). v5
  ported that notice as its own single-door surface (the `979652a9`
  round), so a v4-internal hook extraction has no v5 move — state that
  with the hunk evidence.
- `561466cfe` (knip sweep): pure deletions of 11 unused v4 exports + a
  byte-identical HAIR-guidance dedup (v4 verified no prompt-text change,
  no IDENTITY_STACK_BUILDER_VERSION bump — confirm the version constant
  is untouched in the hunks). Ratify NO-PORT; the vestigial rider is
  Unit 3b.
- `7509c5cfb` (openai-compatible dependency metadata): confirm the
  plugin manifest hunk is the version line alone (1.0.42) and the
  bundle is unchanged; v5's hand-maintained
  `provider_manifest/manifests/*.json` carry no plugin version field —
  nothing reaches a generated manifest (if the manifests generator
  disagrees, that's a finding: `manifest-generator-augmentation-rot.md`).
- `c0352fdba`: `docs/CHANGELOG.md` +23/−0 only — confirm by stat.
- `8440b6391` docs remainder (§D): release notes, API.md, DEVELOPMENT.md,
  README, v4's CLAUDE.md — NO-PORT. API.md's three corrected paths
  document routes v5 already implements — read them as a cross-check
  (30 s each) and record agreement or a finding. The AboutView hunk is
  P4.D128's; do not touch it.

### Unit 3 — the two riders

- **3a. The `help_doc_chunks` blob-column pin:** `487ae57fe`'s new
  `help-doc-chunks.repository` test pins v4's
  registerBlobColumns-re-assert trap. Check whether v5's
  `help_doc_chunks` twin (the P4.D77 substrate,
  `crates/quilltap-core/src/db/` — locate the module) carries an
  equivalent pin; if not, add ONE unit test in v4's shape (this lane's
  only permitted core-adjacent artifact — a test, no source change). If
  the trap is structurally impossible in v5 (no blob-column registry
  twin), record that with the evidence instead.
- **3b. Vestigial twins of the knip-deleted exports:** check whether v5
  carries live twins of `GROUP_WARDROBE_FOLDER` /
  `PROJECT_WARDROBE_FOLDER` / `resolveSharedWardrobeTiersForProject` /
  `noSharedWardrobeTiers` (P4.D71-era wardrobe helpers) that are now
  vestigial (`v5-refactor-vestigial-v4-cruft.md`). Dead-in-both-trees →
  remove (a mechanical deletion, clippy-clean, with the sha cited);
  live-in-v5 → leave and record why. Wardrobe modules are unowned by
  siblings this round — the deletion is safe to land here, loudly.

## Tiered deliverables

**Tier 1 (must land):** Unit 1 whole (bulk proof + the two hunk answers
+ the totals), Unit 2's five ratification records with evidence.
**Tier 2 (should land):** Unit 3a and 3b resolved either way with
evidence.
**Tier 3 (explicit deferrals, loud):** none anticipated; an unexplained
neutrality red is an ESCALATION, not a deferral.

## The differential requirement, restated

This lane's "differential" is the sweep itself: oracles regenerated from
v4's REAL code at the pinned post-sweep tree, diffed against v5
unchanged. The traps that matter: the silent-stale-pass discipline
(ledger §5.2 — `rm` before rebuild, read the tail, grep for a marker),
the SKIP-masquerade audit, `gate-log-failed-grep-lies.md` when grepping
the sweep log, and `a-backstop-hides-the-rot-it-neutralizes.md` if a
red turns out to be recipe rot rather than drift (repair the recipe AND
say so; recipe repairs are within this lane's remit — the P4.45/P4.53
precedent — but never edit a §A family's recipe).

## Fixtures

Consumes everything, delivers nothing. Tracked-fixture writes during the
sweep are refused by the driver since P4.9L — treat any such warning as
a stop-and-read event.

## Shared contract (BINDING — identical text in all four orders)

- **§A Family ownership for this round's regens:** P4.D126 owns
  `system_delete_data_equivalence`, `system_restore_equivalence`,
  `system_restore_state`, `restore_vintage_state`,
  `qtap_import_equivalence`, `memory_delete_tier2_equivalence`,
  `memory_cascade_tier2_equivalence`. P4.D127 owns
  `request_builder_equivalence` + the committed request-envelopes corpus
  (`harness/oracle/fixtures/request-envelopes/`),
  `response_parse_equivalence`, `cheap_model_equivalence`. P4.D128 owns
  the CLI Tier R (`cli_differential.rs`) + `completion_behavior.rs`. No
  other lane regenerates or edits these. P4.D129's bulk neutrality sweep
  EXCLUDES every family in this section by name and hands any red in
  them to the owning lane.
- **§B Pins:** the round's target baseline is **`8872d7efc`**. Each lane
  regenerates its OWN (§A) families from a lane-unique worktree pinned at
  `8872d7efc`; regens of families unrelated to any lane's commits stay
  pinned at `f3892158d` — except P4.D129, whose deliverable is exactly
  the `8872d7efc` regen of the non-§A families.
- **§C Sweep serialization:** `recipe_sweep.py` bulk runs (`--run-all`)
  are single-flight repo-wide. Lanes 126–128 run per-family regens only.
  P4.D129 runs its bulk sweep AFTER lanes 126–128 have closed their
  branches (execution arrangement in the round report); if forced to run
  earlier it must not touch §A families or their /tmp staging.
- **§D The AboutView split:** `8440b6391`'s `app/about/AboutView.tsx`
  hunk (provider list + the NEW "Live interface" bullet) is ported by
  P4.D128; the commit's docs remainder is ratified NO-PORT by P4.D129.
  Neither lane touches the other's half.

## Ownership (BINDING — identical table in all four orders)

| Path | Owner |
|---|---|
| `crates/quilltap-core/src/services/delete_all.rs`, `services/backup/restore/**`, `services/quilltap_import/**`, `db/memories.rs`, the new legacy-fields helper module | P4.D126 |
| §A families for P4.D126 + any new/changed restore archive fixture | P4.D126 |
| `crates/quilltap-core/src/model/request_builder/chat_completions.rs`, `services/cheap_llm_exec.rs`, `crates/quilltap-core/src/realtime/bus.rs` | P4.D127 |
| §A families for P4.D127 + the request-envelopes committed corpus | P4.D127 |
| `apps/web/**` (utilities CSS, component class sweep, `about-page.ts`), `crates/quilltap-cli/**` (completion templates, `completion_behavior.rs`, Tier R cases) | P4.D128 |
| No v5 source by default; MAY add (loudly, in its record): a harness pin for `help_doc_chunks`' blob-column re-assert, vestigial-twin removals of the knip-deleted exports (wardrobe helper modules) | P4.D129 |
| `CHANGELOG.md`, `docs/developer/porting/status-log.md` | append-only, all lanes |
| `docs/developer/porting/drift-ledger.md` | NOBODY (lanes never write it) |

Version bumps: each lane bumps the crates/SPA it touches per
`.claude/commands/commit.md`; the unifier recounts at the wire.

## Verification gate (before declaring done)

1. The sweep's full log captured and audited (totals in the lane
   record; zero unexplained SKIPs; zero unexplained reds).
2. `cargo fmt --check`; `cargo clippy` BOTH feature sets; `cargo test
   --workspace` if any test/deletion artifact landed (Units 3a/3b).
3. The six ratification records + the two rider verdicts in the lane
   record, each with its evidence (hunk citations, diffs, sweep rows).
4. Commits per `.claude/commands/commit.md`; lane record appended to
   `status-log.md`.
