# Work order P4.37 — The Almanack (System Report), server side: the seven-phase collectors, the renderer, the report verbs, the generalized progress bus

> **STATUS: OPEN — PARTIALLY UNIFIED (2026-08-05, the `f7f1a956`
> round). Resume at unit 12 + the held collector commit.**
>
> **Landed on main (verified):** units 1–2 (the pure core — phases /
> types / renderer + `format_time.rs`'s `locale_date{,_time}_us` — with
> the 7-case byte-exact `almanack_render_equivalence`, mutation-proven)
> and unit 10 (the progress generalization: the `phase` frame kind +
> `wire_bytes_unchanged_by_the_phase_kind` / `phase_is_not_terminal`).
>
> **HELD BACK, preserved on branch
> `claude/almanack-server-porting-693d77` (do NOT delete it):** commit
> `a3623ac8` — units 3–9 + 11 (~3,500 lines: `almanack/db.rs` +
> the six phase collector files + the orchestrator + `api/almanack.rs`
> + the four `SystemAlmanack*` verbs + engine arms + the
> `capabilities-report-*` web-edge arms + the `almanack_host: None`
> line) and `80b7c5da` (its rustfmt). Held per the lane's OWN record:
> "landed with NO tier-2 differential behind it … MUST NOT be unified
> until [unit 12] lands" — the port discipline, applied to ourselves.
> The lane records for those units ARE on main (union-merged) and
> describe code that lives only on that branch.
>
> **The resume list (in order):**
> 1. Rebase/cherry-pick `a3623ac8` + `80b7c5da` onto current main
>    (expect only version-file conflicts; the §1 contract was pre-diffed
>    against the landed SPA mirror at unification — name-for-name clean).
> 2. **Unit 12**: the `almanack-*` fixture family + the tier-2
>    data/markdown/route differential, per this order's deliverable 6
>    and the detailed fixture plan in the lane's units-3–9+11 record
>    (fork `build-system-data-fixture.ts`; BOTH attribution arms; the
>    fallback-coverage assertion; /tmp copies; mutation proofs).
> 3. **The host wire**: the `AlmanackHost` impl in `quilltap-host`
>    (paths + runtime facts + passphrase flag + version + clock + the
>    disk storage backend) — until it lands the verbs answer the loud
>    not-assembled refusal and the feature is UNREACHABLE in production.
> 4. **The space-form date stamp** (§3 review finding, 2026-08-05):
>    v4's `formatDate`/`formatDateTime` run through `new Date(...)`,
>    whose V8 legacy fallback parses `"2026-08-05 09:07:03"` (the
>    SQLite `datetime('now')` vintage, present in real instances);
>    v5's `locale_date{,_time}_us` sit on the strict ISO-T
>    `js_date_parse_ms` and render "N/A" for it. Do NOT widen the
>    shared episodic parser — give the almanack formatters the legacy
>    arm (mind TZ semantics: V8 parses the space form as LOCAL time)
>    and pin it with a render-oracle case carrying a space-form stamp.
> 5. Flip `P437_SERVER_LANDED` in
>    `apps/web/e2e/settings-almanack-flow.spec.ts` and run the full
>    Playwright suite (the gated beat: compile → phase bar → viewer →
>    download → delete).
> 6. Re-diff §1 name-for-name against `core-contract.ts` (it was clean
>    at the `f7f1a956` unification; keep it that way).
>
> **ROUND ASSIGNMENT (2026-08-05, the Taboo + maintenance round —
> supersedes the drift-check instruction below for THIS resume):**
> siblings are **P4.D50** (`p4.d50-taboo-drift.md` — the `7df7de8e`
> Taboo catch-up) and **P4.40** (`p4.40-maintenance-sweep.md`). v4
> HEAD is now `3adefeba`, THREE commits past `f7f1a956`; ALL are
> classified in the round-planned record (`44e2e4fe` docs-only
> NO-PORT; `7df7de8e` = P4.D50's; `3adefeba` release-notes docs
> NO-PORT) — **do NOT stop for them.** The
> Taboo commit touches no almanack surface (its lib files are the
> chat spine + instance-settings; verified at planning), so THIS
> lane keeps regenerating every oracle from a detached worktree
> PINNED at `f7f1a956` per the standing rule. STOP only if v4 moves
> PAST `3adefeba` onto almanack/llm-logs surface. §1 contract note
> (BINDING, mirrored in P4.D50): the unifier folds BOTH lanes' arms
> into `api/types.rs`/`api/engine.rs` — this lane's four
> `SystemAlmanack*` variants (already written on the held branch)
> are picked FIRST; P4.D50 adds `TabooSettings` +
> `TabooSettingsUpdate` additively. Ownership deltas vs the original
> lists below: P4.D50 owns the chat-spine files
> (`system_prompt.rs`, `services/build_context.rs`, `cheap_llm.rs`,
> `cache_prefix_hashes.rs`, `db/instance_settings.rs`,
> `api/settings.rs`, `tools/self_inventory.rs`, the settings-routes
> family, and the SPA settings-chat files); P4.40 owns the two
> escalated tier-3 oracle cases, `services/job_runner.rs` (tests),
> and the three named deflake specs. Touch none of those.
>
> *The original planning header follows for history.*
>
> The port-or-defer decision phase-4.md candidate 0 left to the round
> planner is decided: PORT NOW. v4 `0cde7fbc` rewrote the capabilities
> report into The Almanack and DELETED the old
> `lib/tools/capabilities-report.ts` (which v5 never ported — the M6
> parity table row 578 says MISSING). Porting now means porting the
> surface v4 actually ships; deferring means the M6 row rots against a
> moving target. This lane is the server half; P4.38 is the SPA half.
>
> **v4 baseline for this round: `f7f1a956`** (v4 HEAD, 2026-08-05,
> clean tree). The previous baseline was `7189a968`; the two-commit
> drift is classified in the "Round planned" record in
> `status-log.md`. **The baseline move is the UNIFIER's** — do NOT
> edit `CLAUDE.md` or `phase-4.md` on the branch.
>
> **Drift-check at lane start**: `git log f7f1a956..HEAD --oneline` +
> `git status --short` in `~/source/quilltap-server`. On drift/dirty,
> STOP, pin a detached worktree at `f7f1a956`
> (`oracle-regen-pinned-v4-worktree`), and report in the lane record.
>
> **Siblings (same round):** P4.D49 (the drift-proper server lane —
> the §2 shared contract below is BINDING between us), P4.38 (this
> feature's SPA half — the §1 shared contract below is BINDING between
> us), P4.39 (manifests generator).
>
> **Worktree note:** own branch, own checkout; no `git stash`/
> `git worktree`.

## Mandate

Port The Almanack server-side, end to end: the seven-phase collector
family over all three partitions, the pure markdown renderer
(byte-exact), the report save/list/get/delete/download surface on
v4's frozen `capabilities-report-*` action names, and the
operation-progress generalization (the new `phase` event kind on v5's
one global event stream) with `creation_progress`'s existing surface
kept byte-stable. Every unit arrives with its differential over v4's
REAL `lib/tools/almanack` at `f7f1a956`. Do NOT touch:
`db/llm_logs.rs`, `services/llm_logging.rs`, `enclave/step.rs`,
`services/backup/uuid_remap.rs`, `provisioning/fresh_schema.json`
(all P4.D49's per §2), `apps/web/**` (P4.38's), the provider
manifests (P4.39's).

## Survey-verified starting points (2026-08-05, fresh at `f7f1a956`)

### v4 — the module map (`lib/tools/almanack/`, 5,066 lines, 11 files)

- `db.ts` (140) — the three DB doors + soft failure: `mainRows`/
  `mainRow` (main DB via `manager.rawQuery`), `mountRows`/`mountRow`
  (mount index via `getRawMountIndexDatabase()`, sync), 
  `isMountIndexAvailable`, `collect(name, fallback, fn)` (swallows +
  logs any collector failure, returns the fallback), `num()` coercion.
- `phases.ts` (55) — PURE shared data: `ALMANACK_PHASES` (7 entries
  `key`/`label`/`estimatedShare`, shares sum to 1),
  `ALMANACK_PHASE_COUNT`, `ALMANACK_NAME` ("The Almanack"),
  `ALMANACK_TITLE` ("The Almanack (System Report)"). Client-safe by
  design — P4.38 mirrors it (§1).
- `types.ts` (612) — ~40 report interfaces + `AlmanackReportData`
  (:564).
- `phase1-premises.ts` (208) — runtimeEnvironment (os/process;
  `Intl.DateTimeFormat().resolvedOptions().timeZone` at :71),
  databaseSecurity (three DB files + sizes + passphrase),
  backupStatus (three rows), migrationState.
- `phase2-machinery.ts` (526) — 13 collectors: plugins, api-key types,
  providers, models, provider-model cache, api-key usage, the three
  designated profiles (cheapLLM/imagePromptLLM/embedding),
  image/embedding providers, MCP servers, themes.
- `phase3-ledgers.ts` (855) — 12 collectors, main-DB only (18
  `mainRows` calls): databaseStats, chatStats, chatBreakdown,
  autonomousRooms, `collectMemoryBreakdown` (ONE GROUP BY; returns
  `{breakdown, byCharacter: Map}` — the Map feeds phase 5's top-ten),
  characterBreakdown, featureConfig, instanceSettings,
  backgroundJobs, embeddingPipeline, terminal, storageStats.
- `phase4-scriptorium.ts` (596) — `collectScriptorium(userId,
  chatsWithNonEmptyState)` + `emptyScriptorium()`: stores, blobs by
  MIME, hard-link groups, per-document policy, vault keystone health,
  wardrobe by tier, custom tools incl. parse failures, mail, photos,
  scenarios, state cascade. 7 `mountRows` + 4 `mainRows` — the first
  report code to open the mount index.
- `phase5-personae.ts` (289) — top-ten characters/projects/groups with
  tie-breaks and two cross-database app-code joins (character names
  for group members; vault sizes from the mount index). Takes phase
  3's `memoryByCharacter` Map.
- `phase6-wire-records.ts` (212) — llm-logs aggregates via the
  user-scoped repo delegates (`user-scoped.ts:527-560`), plus
  `collectProfileLifetime` off `connection_profiles` (main DB).
  Attribution + denominator-honesty rules in the header. Picks the
  approximate `(provider, modelName)` grouping whenever
  `hasProfileAttributionColumns()` is false; the report prints
  "Attribution is approximate".
- `render.ts` (1,038) — `renderAlmanackMarkdown(data)`: PURE (no
  I/O/clock/globals). Helpers: `formatUSD` (`toFixed(4)`),
  `formatDate`/`formatDateTime`, `formatMs`, `percent` (`toFixed(1)`),
  `cell` (pipe-escape), `countList`, imported `formatBytes`.
- `index.ts` (535) — `generateAlmanackData(userId, progressId?)` walks
  the seven phases SEQUENTIALLY (each phase's collectors in a
  `Promise.all`, each wrapped in `collect()` with an explicit
  fallback), sequential-on-purpose so the progress bar names the
  truly-running phase (:9-13); `announce()` (:83) publishes a `phase`
  event (1-based) on entering each phase. Phase 7 "binding" =
  `generateAndSaveAlmanack` (:467): render → filename
  `almanack-<ISO with :/. → ->.md` → `writeUserUploadToMountStore(
  {subfolder:'diagnostics'})` → caller-supplied `persist()` → returns
  the FILES-ROW id as `reportId` (:462-465 — the 404 fix). Returns
  `{reportId, filename, storageKey, size, content}`;
  `progress.finish()` on success / `fail(msg)` on throw.

### v4 — the route (`app/api/v1/system/tools/route.ts`)

Almanack actions (names deliberately frozen at the capabilities-report
era, :21-23):
- `POST ?action=capabilities-report-generate` (:960) — body
  `{progressId?: uuid}` optional (absent = untracked run, zod at
  :966-970); the persist closure creates the `files` row
  (`category:'DOCUMENT'`, `folderPath:'/reports'`, `source:'SYSTEM'`,
  sha256 of content, `mimeType:'text/markdown'`) and returns
  `fileEntry.id` (:983-1002). Blocking POST; JSON body carries the
  whole rendered report.
- `GET ?action=capabilities-report-list` (:1031) —
  `files.findByCategory('DOCUMENT')` filtered `folderPath ===
  '/reports'`, mapped `{id, filename, storageKey, createdAt, size}`,
  newest first → `{reports}`.
- `GET ?action=capabilities-report-get&reportId=…[&download=true]`
  (:1069) — files row → `fileStorageManager.downloadFile`; the
  download leg is the SAME action with `download=true`
  (`Content-Disposition: attachment`, text/markdown); non-download
  returns `{reportId, filename, content, size}`.
- `POST ?action=capabilities-report-delete` body `{reportId}` (:1128)
  — storage delete + files-row delete → `{success:true}`.
- `GET ?action=capabilities-report-progress&id=…` (:901) — SSE over
  the operation-progress bus (replay-first, terminal-close, 15 s
  keep-alive). **v5 does NOT port this action** — §1's D3-family
  divergence.
- The old `GET ?action=capabilities-report` (hardcoded fake, no
  callers) was DELETED — v5 never had it; nothing to do.
- Unrelated actions still unported in v5 and staying that way:
  `memory-dedup-preview`/`memory-dedup` (standing P4.6s deferral),
  `ai-import-stream`.

### v4 — the progress bus

`lib/progress/operation-progress.ts` (221, NEW): globalThis channel
map keyed by progressId. `CoreProgressEvent` = `status | log | phase
{key,index,total,label} | done | error`, each with `ts` (:43-49).
`publishOperationProgress` (buffered, `MAX_BUFFER=200`, no-op after
terminal), `subscribeOperationProgress` → `{replay, unsubscribe}`
(60 s TTL self-heal), `finish`/`fail`, `createOperationProgressEmitter`
(inert when id absent, :196-210; has a `phase()` method :180).
`lib/chat/creation-progress.ts` is now a THIN FACE: same exports, same
event union (`Extract<CoreProgressEvent, …>` + the two wardrobe
kinds), all bodies one-line delegates; `MAX_BUFFER`/`CLEANUP_TTL_MS`
moved unchanged. The `phase` kind is only ever emitted by the Almanack
run.

### v4 — nondeterminism + locale (the differential's normalization list)

- `render.ts:23` `date.toLocaleDateString()` (no args) and `:29`
  `date.toLocaleString()` (no args) — ambient locale/TZ; under
  en-US/UTC: `"8/5/2026"` and `"8/5/2026, 12:00:00 PM"` (probed live
  on v4's Node). ~35 `Number.prototype.toLocaleString()` sites
  (en-US comma grouping).
- Data-side nondeterminism (renderer itself pure): `generatedAt`,
  `version`/`nodeEnv`, the whole runtimeEnvironment block, on-disk
  DB/backup sizes, `durationMs` averages/medians, the timestamped
  filename, live counts.
- v4's own tests pin TZ=UTC in jest.config.ts (a beforeAll pin is too
  late — ICU caches the zone; render.test.ts:19-22). Every oracle
  recipe here pins `TZ=UTC` explicitly.

### v4 — oracle material

- `__tests__/helpers/almanack/fixture.ts` (603): a COMPLETE
  hand-written `AlmanackReportData` — every field populated
  distinctively, no clock/env — feeding the whole-document snapshot
  (`__snapshots__/render.test.ts.snap`).
- `personae.test.ts`/`premises.test.ts` mock `./db` — unit tests, not
  oracle material.
- Feasibility: (a) tier-1 render oracle — drive the REAL
  `renderAlmanackMarkdown` over exported fixture data under TZ=UTC,
  byte-diff; (b) tier-2 data/route oracle — the established
  `jest-real-db-oracle` pattern (resetModules+doMock) running
  `generateAlmanackData` + the route handlers over fixture DBs.

### v5 — the landing zones

- Dispatch: a NEW `api/almanack.rs` beside `api/system_data.rs` (the
  P4.9G file — tasks_queue :235 … delete-data :735-779); verbs follow
  the standard fold: `api/types.rs` Request variants (cf.
  `SystemExportEntities` at :2337-2342; `Response::System(Value)`
  carrier :2940), `api/engine.rs` dispatch arms (cf. `SystemHome` at
  :3331), web edge action arms in
  `crates/quilltap-web/src/system_data_routes.rs` (`system_tools_get`
  :74 / `system_tools_post` :133 — the five `capabilities-report-*`
  actions currently fall to `unknown_action`), route bound at
  `quilltap-web/src/lib.rs:349-350`.
- The collectors: a NEW `crates/quilltap-core/src/almanack/` module
  mirroring v4's file split (db doors / phases / types / phase1-6 /
  render / orchestrator). Phase-6 aggregates + the attribution probe
  live HERE per §2 (raw SQL over the llm-logs connection, v4's
  fail-soft `aggregate()` shape) — NOT in `db/llm_logs.rs`.
- Progress: `services/creation_progress.rs` already ports the bus
  (`CreationProgressFrame` kind-tagged kebab-case, 200-frame cap,
  60 s TTL, `active_snapshot()` replay) with the D3 divergence
  documented in its header: frames ride the ONE global `/api/events`
  stream scope-tagged by `progress_id` — no per-id SSE route. The
  generalization = add the `phase {key,index,total,label}` frame kind
  + a generalized emitter face (v4's operation-progress), keeping
  every existing kind's wire bytes UNCHANGED (consumers:
  `chat_create.rs`, `outfit_selections.rs`, `api/engine.rs`+`types.rs`
  event plumbing, `quilltap-web/src/events.rs`, quilltap-host).
- Report storage: `writeUserUploadToMountStore` → the "Quilltap
  Uploads" singleton database-backed mount, `diagnostics/<filename>`,
  storage key `mount-blob:{mountPointId}:{blobId}`, plus the main-DB
  `files` row — ALL existing ported machinery (the multipart/blob
  work); the download resolves through the existing mount-blob shim.
- Locale: `src/format_time.rs` — `format_date_short_us` IS
  `toLocaleDateString()` no-args under TZ=UTC/en-US (:101-112). The
  ONE gap: `toLocaleString()` no-args (date + `, ` + 12-hour
  `H:MM:SS AM/PM`) has no existing port — a small byte-exact
  extension of `format_time.rs`. Number grouping already ported
  (`tools/self_inventory.rs:696` `format_number` et al.; canonical
  note at `enclave/announce.rs:31`).
- Closest recipes: `crates/quilltap-harness/tests/
  system_jobs_routes_equivalence.rs` + `harness/oracle/cases/
  system-jobs-routes.test.ts` (fresh /tmp fixture copy per case;
  direct-drive the api free functions; diff `{status, body}`; PIN the
  nondeterministic snapshot identically both sides — the exact
  precedent for pinning runtimeEnvironment/clock);
  `system_export_equivalence.rs` for byte-diff style. Memory notes:
  `jest-real-db-oracle`, `jest-oracle-mechanism`,
  `oracle-worktree-jest-stage`, `sweep-recipes-write-into-the-repo`.

## Deliverables

### Tier 1 — must land

1. **The pure core**: the phase manifest (7 phases, keys/labels/
   shares byte-equal to v4's `phases.ts`, per §1), the report types,
   and `render.rs` — a pure byte-exact `render_almanack_markdown`,
   including the `toLocaleString()`-no-args datetime extension of
   `format_time.rs` and reuse of the existing number-grouping/
   formatBytes ports. **Tier-1 render differential**: an oracle that
   exports v4's committed fixture `AlmanackReportData` as JSON and
   emits v4's real render under TZ=UTC; the Rust side renders the same
   JSON; byte-exact diff. First-run-green must be mutation-proven.
2. **The db doors + the seven phase collector families** over the
   three partitions, with v4's `collect()` fail-soft wrappers and
   EXACT fallback shapes, the phase-3→5 `memoryByCharacter` Map and
   phase-3→4 `chatsWithNonEmptyState` threading, and the phase-6
   aggregates + attribution probe (per §2 — raw SQL in this module;
   the approximate arm's `provider || '/' || modelName` key and the
   "Attribution is approximate" plumbing byte-faithful).
   v5-divergent data sources to handle honestly: the
   runtimeEnvironment block reports v5's OWN runtime (this section
   reports the actual runtime — a documented divergence; normalized in
   the differential, labels byte-matched); `version` is v5's;
   migrationState reads the same main-DB table v4 does and takes the
   fail-soft arm on instances that lack it (the
   `migration-vintage-fixture` reality — record which arm each
   fixture exercises).
3. **The orchestrator**: sequential phases (each phase's collectors
   joined concurrently), the per-phase `phase` announce, and
   `generate_and_save`: render → v4's exact filename scheme →
   the Uploads-mount `diagnostics/` write + main-DB files row
   (`/reports`, DOCUMENT, SYSTEM, sha256, text/markdown) → the
   FILES-ROW id as `reportId`; `finish()`/`fail(msg)` semantics.
4. **The four verbs + web edge** per §1: `SystemAlmanackGenerate/
   List/Get/Delete` request variants, engine arms, and the
   `capabilities-report-*` action arms in `system_data_routes.rs`,
   including the `download=true` web-edge leg (Content-Disposition
   attachment). Envelope shapes byte-faithful to the v4 route.
5. **The progress generalization**: the `phase` frame kind on the
   global event stream, scope-tagged by progressId; a generalized
   emitter face with `creation_progress`'s existing public surface and
   wire bytes UNCHANGED (prove by the existing suites — no existing
   frame kind's serialization moves).
6. **The tier-2 differential** over a NEW committed fixture family
   (this lane's to build and commit, recipe in the header): fixture
   DBs built by v4's REAL create paths at `f7f1a956`, populating
   EVERY report section non-trivially (the P4.D31 "no non-default
   value in any archive" lesson — a section whose fixture data equals
   its fallback shape is structurally blind), with TWO llm-logs
   partition variants: one migrated + attributed rows (the exact-
   attribution arm) and one vintage/column-less (the approximate arm).
   The differential drives v4's real `generateAlmanackData` + route
   handlers vs v5's, diffing: the report DATA (JSON, normalized:
   generatedAt, version/nodeEnv, runtimeEnvironment, disk sizes,
   durationMs aggregates), the rendered MARKDOWN (normalized only at
   the same fields' rendered lines), the route envelopes
   (generate/list/get/delete), and the persisted rows (files row +
   mount blob). Include a fallback-coverage assertion: NO section may
   render its `collect()` fallback in the happy-path cases (the
   `49769ec4` fail-soft-gate lesson — a v5 collector that always
   throws must not pass by coinciding with the oracle's fallback).
   Generate WRITES — run every case over /tmp COPIES of the committed
   fixtures. Mutation-proof first-run-green arms per the D24 rule.

### Tier 2 — should land

7. A settle/`Drop`-clean scratch discipline for the report writes in
   the differential (the P4.9G6/P4.D46 lessons: assert the scratch
   root empty after each case; `settle()` before dumps).
8. A `quilltap` CLI surface is NOT owed (v4's npx CLI has no almanack
   verb — verify and record).

### Tier 3 — explicitly out of scope (loud, named)

- The `capabilities-report-progress` SSE action — the D3-family
  divergence (per §1): v5 serves the global event stream; record the
  divergence in the module header and the lane record.
- `memory-dedup-preview`/`memory-dedup` and `ai-import-stream` —
  pre-existing unported surface; they keep falling to
  `unknown_action`; no new refusal arms invented here.
- The SPA (P4.38's), including the e2e beats (P4.38 writes them gated
  on `P437_SERVER_LANDED`).
- The v4-side `user-scoped.ts` delegate shape — v5 has no user-scoped
  repo layer to mirror; the aggregates are called directly from
  phase 6 (record as a structural no-op).

## The differential requirement, restated

Every unit ships against v4's REAL code at `f7f1a956`: the render via
the tier-1 byte oracle (deliverable 1), the collectors/orchestrator/
routes via the tier-2 family (deliverable 6), the progress
generalization via the unchanged-bytes proof (deliverable 5). Oracle
recipes pin `TZ=UTC` explicitly (v4's jest configs now force it, but
tsx-driven oracles see the ambient zone — pin regardless), live in the
family headers, and are `recipe_sweep.py`-runnable (P4.34's venue
rules: `unstaged_jest_roots` families run with
`--v5w ~/source/quilltap-v5`; prefer the staged-mirror convention).

## Fixtures

- **Delivers:** the new committed almanack fixture family (name it
  `almanack-*`; main + mount + the two llm-logs variants), built by
  v4's real code at `f7f1a956`, recipe in the family header, /tmp-copy
  rule stated. This lane owns regenerating every oracle that consumes
  it (only its own, since the family is new).
- **Consumes:** nothing shared with the siblings (deliberately — the
  `system-data-*` family stays untouched so P4.D49's regen sweep and
  this lane never contend).

## §1 Shared contract — the Almanack wire (BINDING; identical in P4.37 and P4.38)

Dispatch verbs (`api/types.rs` CoreRequest ↔ the SPA
`core-contract.ts` mirror, diffed name-for-name at unification).
Web-edge action strings keep v4's `capabilities-report-*` names
verbatim (v4 froze them deliberately: "renaming them would break
bookmarks to buy nothing").

1. `SystemAlmanackGenerate { progressId?: string (uuid) }`
   → `{ success: true, reportId, filename, storageKey, size, content }`
   (`reportId` = the files-row id — v4's 404-fix shape).
   Web edge: `POST /api/v1/system/tools?action=capabilities-report-generate`.
2. `SystemAlmanackList {}` → `{ reports: [{ id, filename, storageKey,
   createdAt, size }] }`, newest first.
   Web edge: `GET ?action=capabilities-report-list`.
3. `SystemAlmanackGet { reportId }` → `{ reportId, filename, content,
   size }`.
   Web edge: `GET ?action=capabilities-report-get&reportId=…`; the
   `&download=true` leg is WEB-EDGE-ONLY (Content-Disposition
   attachment, text/markdown) — same action, no separate verb.
4. `SystemAlmanackDelete { reportId }` → `{ success: true }`.
   Web edge: `POST ?action=capabilities-report-delete` body
   `{reportId}`.

Progress: NO dispatch verb and NO per-id SSE route (the standing
D3-family divergence — the creation-progress precedent). The server
emits a NEW frame kind on the ONE global event stream, scope-tagged by
`progressId`:

    { kind: 'phase', key, index (1-based), total, label }

alongside the existing `status`/`log`/`done`/`error` kinds (whose wire
bytes must not move). The SPA subscribes `core.events$` filtered by
progressId (the Green-Room pattern, `green-room.state.ts:36-48`); the
POST's own resolution is authoritative. v4's
`GET ?action=capabilities-report-progress` SSE action is NOT ported —
both lanes record it as the D3-family divergence.

The phase manifest (server Rust constant ↔ SPA TS mirror, byte-equal
labels): seven phases, in order, keys `premises`, `machinery`,
`ledgers`, `scriptorium`, `personae`, `wire-records`, `binding`, with
v4's labels and `estimatedShare` values from
`lib/tools/almanack/phases.ts` (shares sum to 1). `ALMANACK_NAME` =
"The Almanack"; `ALMANACK_TITLE` = "The Almanack (System Report)". The
settings-card `sectionId` stays `"capabilities-report"`.

## §2 Shared contract — the llm_logs profile columns (BINDING; identical in P4.D49 and P4.37)

- The columns: `connectionProfileId`, `imageProfileId` — nullable TEXT
  on `llm_logs` (llm-logs partition), positioned between `modelName`
  and `request` in LLMLogSchema field order.
- **P4.D49 OWNS:** the D23 re-dump (`provisioning/fresh_schema.json` +
  the dumper), `db/llm_logs.rs`, `services/llm_logging.rs`, every
  write/call site, `uuid_remap.rs`, the `$ne: null` fix, and
  `enclave/step.rs`.
- **P4.37 OWNS:** the read-side aggregates + the attribution probe
  (`hasProfileAttributionColumns` = PRAGMA table_info) — these live in
  P4.37's almanack module as raw SQL over the llm-logs connection,
  mirroring v4's fail-soft `aggregate()` helper (v4 repo :715-925),
  NOT in `db/llm_logs.rs`. P4.37 does not touch `db/llm_logs.rs`,
  `llm_logging.rs`, `enclave/step.rs`, or `fresh_schema.json`; P4.D49
  does not touch the almanack module, `api/types.rs`, `api/engine.rs`,
  or `services/creation_progress.rs`.
- The probe makes the lanes runtime-independent: P4.37's collectors
  take the approximate `(provider || '/' || modelName)` arm on any DB
  without the columns (including v5-provisioned instances until
  P4.D49's re-dump lands) and the exact arm on any DB with them (its
  own v4-built fixtures). Both arms are differential-exercised in this
  lane regardless of P4.D49's progress; nothing in either lane gates
  on the other at build time.

## Ownership (binding)

- **This lane owns:** the NEW `crates/quilltap-core/src/almanack/`
  module, the NEW `api/almanack.rs`, `api/types.rs` (SOLE toucher
  this round), `api/engine.rs` (sole toucher),
  `services/creation_progress.rs` (sole toucher),
  `src/format_time.rs` (the datetime extension — sole toucher),
  `crates/quilltap-web/src/system_data_routes.rs` +
  `quilltap-web/src/events.rs` if the frame plumbing needs it (sole
  toucher), the new fixture family + its oracle cases + differential
  tests. **Version bumps:** core + harness + web.
- **Do NOT touch:** everything in P4.D49's ownership list (§2),
  `apps/web/**`, the provider manifests/generator, the
  `system-data-*` fixture family.
- `CHANGELOG` + `status-log.md` are append-only (union-merge at
  unification).

## Verification gate before declaring done

1. `cargo fmt --all --check`; clippy `-D warnings` on BOTH feature
   sets; `cargo build --release`.
2. The lane's differentials BY NAME, `--nocapture`, zero SKIP, over
   oracles regenerated FRESH at `f7f1a956` (NDJSON fresh-marker
   grepped); the mutation-proof fingerprints recorded; the
   fallback-coverage assertion green.
3. `cargo test --workspace --no-fail-fast` with the full env block
   (the existing creation-progress consumers prove the bus
   generalization moved nothing).
4. No `apps/web` change → no SPA gate owed (state it). The §1 TS
   mirror is P4.38's file; the unifier diffs the contract
   name-for-name.
5. Per-commit rule: follow `.claude/commands/commit.md`.
