# Work order: W4.11b — the `primary_stream_tier3` logging regen (+ the failover log gap)

**Size: one Opus session.** The deferred step 6 of the W4.7e3 regeneration
plan: regenerate `primary_stream_tier3_equivalence` with `logLLMCall` live
and an `llm_logs` dump — which requires relocating the oracle's model mock
from the service-level `streamMessage` wrapper down to the provider level,
AND fixes the one real faithfulness gap the survey found (the failover path
must log). **Oracle baseline: v4 `6b6e39ad`** (verify; if HEAD moved, STOP
and surface the drift).

## Survey facts (verified 2026-07-08)

- v4 writes the CHAT_MESSAGE row inside the **service-level wrapper**
  `streamMessage` (`streaming.service.ts:312-444`): the log at `:407` fires
  in the terminal `if (chunk.done)` block, gated `if (userId)` (`:402`).
  Payload: `type:'CHAT_MESSAGE'`, `userId/messageId/chatId/characterId`
  (messageId = `preGeneratedAssistantMessageId`), provider/modelName from
  the connection profile, `request.messages = llmMessages.map(({role,
  content, attachments}))` + `temperature`/`maxTokens`/`tools` (tools only
  when non-empty), `response: {content: accumulatedContent, finishReason}`,
  `usage`/`cacheUsage`/`rawProviderUsage` = LAST non-null across chunks,
  `requestHashes = computeRequestPrefixHashes(llmMessages, tools?)`
  (`:404`), `durationMs = Date.now() - startTime` (`:346`/`:403`).
  Fire-and-forget (`.catch`, `:434-439`).
- Wrapper callers: `primary-stream.service.ts:182` (passes userId → logs),
  `:246` (the tool-unsupported retry → logs),
  `provider-failover.service.ts:266` (passes `userId: opts.userId` at
  `:273` → **logs**), `recovery.service.ts:303` (does NOT pass userId →
  does not log).
- The oracle today mocks the WHOLE wrapper
  (`primary-stream-tier3.test.ts:222-282` — per-label attempt cursor,
  canned-key recording, generator yielding content/reasoning/done/throw).
  Relocation target: `jest.doMock('@/lib/llm', ...)` overriding
  `createLLMProvider` (called at `streaming.service.ts:327`) to return
  `{ streamMessage: async function*(params, apiKey) {...} }` — keeping the
  REAL wrapper (and its log) running. The `createLLMProvider` mock pattern
  exists in `compression-tier3.test.ts:165-207` /
  `danger-gatekeeper.test.ts:114-142` — but those mock `sendMessage`
  (non-streaming); the async-generator body + per-label attempt cursor must
  be carried over from the current mock. The provider-level mock no longer
  sees `options.connectionProfile` — key off `params`.
- Rust side already wired (W4.7e3): `StreamLogCtx` (`primary_stream.rs:811-817`)
  built when `user_id` non-empty (`:1051-1058`); `log_chat_message_call`
  (`:826-903`) fires in `consume_stream` on the done chunk (`:969-998`) with
  the **lossy** request/prefix message shape (role + content only,
  `attachments`/`name`/`toolCallId`/`toolCalls` all None) and
  **`duration_ms: Some(0.0)` hard-coded** (`:900`), `LogContext::none()`.
- **The real port gap:** the Rust failover path
  (`provider_failover.rs:281-314`, its own `rx.recv()` drain loop) and
  recovery (`recovery.rs:382-384`) write NO log. v4's failover DOES log
  (userId passed); v4's recovery does NOT (no userId). So
  `empty_same_provider_retry` / `empty_uncensored_failover` / `both_empty`
  will produce v4 rows the port currently doesn't emit. Fix the port:
  the failover retry streams must write the same CHAT_MESSAGE row shape
  (reuse `log_chat_message_call` or a shared helper — do NOT fork the row
  construction). Recovery stays log-free (faithful).
- Harness: `primary_stream_tier3_equivalence.rs` opens
  `DbPaths::main_only` (`:440`), dumps `chat_messages` + `chats`
  (`:634-639`); no `mod common`. The llm-logs pattern to copy is
  `danger_gatekeeper_tier3_equivalence.rs:374-390`
  (`common::materialize_llm_logs` + explicit `DbPaths { llm_logs:
  Some(...) }`), then `common::{dump_llm_logs, oracle_llm_logs}`.
- Corpus (12 calls, 9 stream labels): `clean_stream`,
  `mid_stream_error_preserve`, `tool_unsupported_retry_success/_failure`
  (same canned key re-issued — the attempt cursor matters),
  `token_limit_recovery`, `recovery_static_fallback` (no v4 log),
  the three failover calls, three pure `findPrevResponseId` calls.

## Scope

1. **Relocate the oracle's model mock** to `createLLMProvider` level
   (generator body + attempt cursor preserved; canned-key recording
   unchanged so the Rust `QueuedStreamingProvider` replay keys stay valid).
   Un-mock `logLLMCall` (`requireActual`), set `SQLITE_LLM_LOGS_PATH`,
   drain microtasks before dumping, emit `kind:"llmlogs"` (the
   danger-gatekeeper `:99-100` / `:207-237` pattern).
2. **Prove the event traces survived relocation:** every pre-existing
   case's event trace + `chat_messages`/`chats` dumps must be UNCHANGED
   (the relocation moves the mock boundary, not behavior). If a trace
   changes, that is a finding to investigate, not to normalize away.
3. **Fix the failover logging gap** in `provider_failover.rs`: both retry
   legs write the CHAT_MESSAGE row exactly as v4's wrapper does for them
   (v4 logs per `streamMessage` call — check what messageId/characterId
   the failover call site passes and reproduce; the effective/rerouted
   profile naming follows v4's `connectionProfile` at that call).
4. **durationMs:** v4's row carries `Date.now() - startTime`; the Rust side
   hard-codes 0. Ensure the oracle's clock makes the v4 value 0 (freeze or
   pin Date for this oracle if it isn't already), OR — if the oracle's
   existing clock behavior can't be frozen without disturbing traces —
   placeholder `durationMs` on both sides in the dump with a comment. Do
   NOT change the Rust `Some(0.0)` (a real stream clock is a spine-injected
   follow-up, per the W4.7e3 note).
5. **Harness:** attach the llm-logs partition, dump + diff (id/createdAt/
   updatedAt placeholdered via the shared helper; requestHashes asserted —
   this is the point of step 6: the hash tier fields must match the ported
   `compute_request_prefix_hashes` output at the wire).
6. The lossy `StreamParams.messages` caveat (no attachments/name/toolCalls
   in the request summary): the corpus is plain-message, so v4's mapped
   `request.messages` should equal the lossy shape. If a corpus case DOES
   carry attachments through, verify what v4 stores and surface any
   mismatch rather than widening silently.

## Explicitly OUT of scope

- The orchestrator oracle/harness (W4.11a owns them — it filters
  CHAT_MESSAGE rows from its own dump and points here for the proof).
- The moderation seam (W4.11c).
- `services::llm_logging` (frozen).
- A real stream clock for durationMs (tracked follow-up, host/spine).

## Ownership / contention (this round: W4.11a ∥ W4.11b ∥ W4.11c)

- W4.11b owns: `services/primary_stream.rs`, `services/provider_failover.rs`,
  `services/recovery.rs` (read-only expected),
  `primary_stream_tier3_equivalence.rs`, `primary-stream-tier3.test.ts` +
  its fixture.
- Do NOT touch: the four spine files, `model/*.rs`, `tools/*`
  (W4.11a); `services/dangerous_content/*` (W4.11c);
  `tests/common/mod.rs` changes ADDITIVE only and flagged.

## Verification

- `primary_stream_tier3_equivalence` regenerated + green: all 12 calls,
  event traces + both table dumps unchanged, PLUS the `llm_logs` dump
  byte-exact (CHAT_MESSAGE rows for the primary, tool-retry, and both
  failover legs; none for recovery; requestHashes fields asserted).
- The failover fix's own unit coverage (a drain-loop test proving the row
  writes once per retry leg with the llm-logs partition attached).
- Full workspace `cargo test` / `clippy -D warnings` (default AND
  `--features native-transport`) / `fmt --check`.

## Deliverables checklist

- [ ] Oracle mock relocated below the wrapper; `logLLMCall` live; traces
      proven unchanged.
- [ ] The failover logging gap fixed in the port (shared row construction,
      not forked).
- [ ] `llm_logs` dumped + diffed with requestHashes asserted; durationMs
      strategy documented in the test header.
- [ ] CHANGELOG, versions (never `quilltap-sqlite3mc-sys`), CLAUDE.md (the
      step-6 deferral CLOSED), the W4.7e3 order's regen list updated;
      memory note if a new gotcha surfaced.
- [ ] Commit per `.claude/commands/commit.md`, branch
      `w4.11b-primary-stream-log-regen`.

**STOP rules:** if relocation changes ANY pre-existing event trace or table
dump, STOP and diagnose before normalizing; the `llm_logging` writer is
frozen; fix the port, never the diff; schema never changes.
