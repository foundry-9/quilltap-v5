# Work order P4.D51 — the `f4955e0e` guards + backup + mount-index convergence: bugs 8/18 (corrupt-input guards), 9/11/12 + the delete-all half of 10 (store delete & restore/import integrity), 13/15/38/43 (mount-index & file hygiene)

> **STATUS: OPEN — not started.**

This is the largest lane of the `f4955e0e` drift catch-up round (v4's
"bugs 8–43" batch, eleven commits, nine behavioral). It absorbs v4
commits `13ddc5ee` (session 1), `3bb664f0` (session 2), and `7bcd8515`
(session 3) — EXCEPT bug 16 (the dimension-reconcile wrong-DB fix,
re-binned to P4.D52 because it shares the `embedding_remainder` family
and committed fixture with bug 17) and bug 10's per-chat annotations
sweep (re-binned to P4.D53, which owns `db/chats.rs` and the chats
families). Most of this lane is **convergence**: v4 adopting fixes this
port made first, so the work is retiring both-direction pins that are
now tripping BY DESIGN, regenerating oracles, and absorbing the
genuinely-new arms.

**v4 baseline for THIS round: `f4955e0e`** (4.8.0-dev.175, 2026-08-06,
tree clean at planning). The full drift classification is in the
"Round planned — the f4955e0e found-bugs convergence round" record in
`status-log.md`. **The baseline move `3adefeba` → `f4955e0e` is the
UNIFIER's** — do NOT edit `CLAUDE.md` or `phase-4.md` on the branch.

**Drift-check at lane start:** `git log f4955e0e..HEAD --oneline` +
`git status --short` in `~/source/quilltap-server`. v4 ships daily. If
HEAD moved: classify the new commit(s) in your lane record; STOP only
if one lands on files this order names — otherwise regenerate every
oracle from a detached worktree PINNED at `f4955e0e`
(`oracle-regen-pinned-v4-worktree`).

**Siblings (same round):** P4.D52 (scriptorium/memory/almanack — owns
`embedding_remainder_*`, `embedding_dimension_reconcile.rs`, and the
`embedding-remainder-{main,mount}.db` fixtures; you must NOT touch
them), P4.D53 (chat-API server — owns `db/chats.rs`,
`db/conversation_annotations.rs`, and the chats/salon families),
P4.D54 (SPA), P4.D55 (providers), P4.43 (memory-maintenance
remainder). See Ownership.

**Worktree note:** own branch, own checkout; no `git stash`/
`git worktree` games with sibling lanes.

## Mandate

Absorb v4 sessions 1–3 (minus the two re-binned items) faithfully:
retire every convergence pin the drift trips, mirror the three faithful
fixes v5 still reproduces (the help-sync guard, the `gc_orphaned_file_row`
table guards, the native-text attach fallback), port the two genuinely
new surfaces (the thumbnail sweep with its `StorageBackend` list seam;
nothing else is new), and reconcile the maintenance-summary shape now
that v4 has keys for sweeps v5 invented first. Every retired pin's
family regenerates fresh at `f4955e0e` and re-runs green as a PLAIN
equality (or with the residual pins that survive, named below). Do NOT
touch: P4.D52's files, P4.D53's files, `apps/web/**` (P4.D54's),
provider/request-builder/decoder files (P4.D55's),
`api/memory_maintenance.rs` / `services/memory_dedup*` (P4.43's),
`api/types.rs` (FROZEN all round — no lane adds verbs).

## Survey-verified starting points (2026-08-06, fresh at `f4955e0e`)

Global: **no schema/DDL/migration change in any of the three commits**
— no D23 re-dump owed. The only schema-adjacent edit is a
description-only change to `public/schemas/qtap-export.schema.json`.
v4 deliberately did NOT reconcile the `conversation_annotations`
UNIQUE/FK gap in `generateDDL` ("a separate follow-up") — no v5 action.

### Bug 8 — corrupt `properties.json` clobber (v4 `13ddc5ee`) — CONVERGENCE
- v4: new `readCharacterVaultPropertiesForWrite`
  (`vault-readers.ts:88-170`) throws `CharacterVaultUnavailableError`
  with `"properties.json unparseable: <detail>"` on unreadable/parse/Zod
  failure; `managed-fields.ts:419+` RMW seed swapped to it.
- v5 already correct: `db/vault_character_update.rs:443-486`
  (`read_current_properties` → `OverlayError::Unavailable`), module
  header `:25-35` names the deliberate divergence (finding #47).
- **Pins that trip:** `V4_CLOBBERED_BAG`
  (`characters_update_tier2_equivalence.rs:163`); the oracle
  `corrupt["message"].is_null()` assertion (`:428-434`, whose failure
  message says exactly this: "v4 appears to have landed its finding-#47
  fix; RECLASSIFY"); the module-header prose (`:29-45`).
- Work: regenerate `QT_ORACLE_CHARUPD` at the pin, retire the
  divergence into a plain equality, compare the refusal messages with
  the parse-detail tail elided (the `UNPARSEABLE_MARKER` convention —
  v4's detail is Node/Zod wording, v5's is serde_json; the tails will
  never match and the elision exists for exactly this). Rewrite the
  stale "queued v4-side" comments in `vault_character_update.rs`.

### Bug 18 — whitespace-only help files wipe `help_docs` (v4 `13ddc5ee`) — FAITHFUL, mirror owed
- v4 `help-doc-sync.ts:218-266`: prune now guarded by
  `pathsOnDisk.size === 0 && existingDocs.length > 0` → warn + skip
  (widened from the `files.length === 0` early return).
- v5 reproduces the wipe: `services/help_doc_sync.rs` — early return at
  `:225`, `paths_on_disk` at `:252-261`, the prune loop `:300-330` with
  the "This port reproduces the CODE, not the comment" note.
- The family is a PLAIN equality (no carve-out): after regen,
  `help_doc_sync_guards_equivalence`'s `only-empty-files` case goes red
  the ordinary way (`:134-140` whole-case loop; the `:164-170` boundary
  assertion whose message says "If this ever flips, v4 changed").
- Work: widen the guard, regenerate `QT_ORACLE_HELP_SYNC_GUARDS` (and
  re-check the siblings `help_doc_sync_equivalence` /
  `help_doc_ensure_equivalence` over the shared `help-sync.json` spec +
  the committed `help-sync-guards/{missing-dir,no-markdown,only-empty-files}/`
  trees).

### Bug 9 — store delete leaves orphans (v4 `3bb664f0`) — CONVERGENCE + shape reconciliation
- v4: new `delete-store-cascade.ts` (one transaction, per-table
  `sqlite_master` guards, chunks → links → per-file GC → folders →
  project links → group links → the row) replacing the seven
  un-transacted route calls; new `orphan-store-reaper.ts`
  (`{links, folders, documents}`); repo `sweepOrphanedStoreChildren`;
  boot PHASE 3.3b in `instrumentation.ts:671`; scheduled-maintenance
  step 4 (`orphanedStoreChildrenSwept`).
- v5 already correct (P4.31): `api/mount_points.rs:531-600`
  `cascade_delete`; `db/doc_mount_file_links.rs:2248-2400`
  `sweep_orphaned_store_children` (`ReapedStoreChildren`); boot at
  `services/builtin_mounts.rs:171`; daily step 4b in
  `services/scheduled_maintenance.rs:215-240`.
- **Pins that trip:** `ARMS[].leaked_documents` (7 arms) +
  `ARMS[].leaked_group_links` in `store_delete_equivalence.rs:100-175`
  (assertions `:532-558`, "v4 has CONVERGED — retire this half").
  `PLANTED_ORPHANS` (`:50`) probably survives (v4's reaper doesn't run
  inside the oracle harness) — RE-VERIFY, don't assume. The `reap_orphans`
  arm stays a v5-only op in shape.
- **Shape divergences v4's fix introduced (measure, then decide):** v4's
  reaper returns `{links, folders, documents}` and reaps no chunks; v5's
  reaps `{links, folders, chunks, content}` (strictly broader — heals
  more). v4's reaper is NOT fail-soft on table existence; v5's is
  two-tier. v4 runs store-children BEFORE orphaned files; v5 runs files
  (step 4) before the reaper (4b). These surface in the
  maintenance-summary comparison (below). Default: converge to v4's
  observable shape wherever an oracle family diffs it; keep v5's
  broader healing only behind a both-directions pin with the mutation
  proof (the D24 rule). Escalate in the lane record if neither lands
  cleanly — do not decide a semantic widening silently.

### Bug 10, delete-all half (v4 `3bb664f0`) — CONVERGENCE
- v4 `delete-service.ts:34-46` adds `conversation_annotations` to
  `clearFormat3Entities`' `mainTables`.
- v5 already correct: `services/delete_all.rs:212`
  (`V5_EXTRA_MAIN_TABLES`).
- **Pins that trip:** `ANNOTATION_DIVERGENCE_KEY`
  (`system_delete_data_equivalence.rs:158`, both-direction assertions
  `:232-249` with the "v4 has CONVERGED" message), `WIPING_CASES`
  (`:165-171`), the header ruling prose (`:20-25`). Also the
  `restore_vintage_state.rs:29-37` module doc ("v4 wipes no
  annotations") is now false — prose fix, the suite runs
  unconditionally.
- The per-chat sweep half (`chats.repository.ts:336-350`) is
  **P4.D53's** — do not touch `db/chats.rs`.

### Bug 11 — `.qtap` import store-identity trio (v4 `3bb664f0`) — CONVERGENCE
- v4 `import-document-stores.ts`: overwrite clears folders (`:60-68`);
  identity by id (`byId`, `:45-52`); create preserves the archive id
  when no existing store claims it (`preserveArchiveId`, `:94-126`).
- v5 already correct (P4.33): `services/quilltap_import/
  document_stores.rs:95-130` + `overwrite_clear_mount` `:505-540`.
- **Pins that trip** (all in `system_import_state.rs`):
  `FOLDER_CLEAR_DIVERGENCE` (`:337`; retire messages at `:593`, `:615`,
  `:1576`), `FOLDER_WARNING_PREFIX` (`:340`),
  the `execute_folder_overwrite` arm (`:1379`),
  `STORE_ID_PRESERVED_ON_CREATE` (`:358`; both-directions `:431-470`),
  `store_identity_expectations` + the four `store_identity_*` arms
  (`:1022-1150`, dispatch `:1385`) — each arm's `want_v4` half
  converges to its `want_v5` half. Mind the arm-count assertion at
  `:1400`. Quirk to verify after regen: v4's duplicate-onto-id-clash
  mints via `create(…, undefined)` while v5 mints only when the id is
  taken — the `store_identity_same_name_new_id` arm is the one that
  distinguishes them.
- Stale source comments: `document_stores.rs:105-106`, `:522-523`.

### Bug 12 — second-generation restore loses link ids (v4 `3bb664f0`) — CONVERGENCE + one live difference
- v4: new `carried-store-rows.ts` resolver; the 22a-bis replay skips
  re-ingest on a hit, `repos.files.create({…, storageKey: carried}, {id})`.
  **No phase reorder** — v4 kept 22a-bis; v5 keeps its post-doc-store
  slot per the standing 2026-07-26 ruling.
- v5 already correct (P4.d23): `services/backup/restore/
  orchestrator.rs:1720-1770` `carried_store_rows` → `CarriedBlob`,
  consulted at `:873-874`.
- **Pins that trip** (all in `system_restore_state.rs`): `REPLAY_DEDUPE`
  (6 cases, `:280-292`), `REPLAY_DEDUPE_TABLES` (`:296-304`),
  `REPLAY_DEDUPE_SUMMARY_KEYS` (`:306`), `assert_replay_dedupe`
  (`:1434-1560` — its "v4 must still lose" half fails, by design).
- **`PHASE_ORDER_RESIDUAL` (`:190-215`) does NOT retire** — the ruled
  placement difference stands (legacy disk-key files are still
  re-ingested on both sides). Verify, don't delete.
- **One predicted LIVE difference:** v4's replay-skip carries only the
  remapped `storageKey`; v5's `CarriedBlob` (`:1760-1770`) additionally
  writes `storedMimeType`/`sizeBytes` off the archived blob row, so
  `main.files.mimeType`/`size` may now diff on the dedupe cases.
  Measure it. If real: either converge to v4's field-strip or keep
  v5's post-store truth behind a NEW both-directions pin
  (mutation-proven), citing the standing "backup/restore: FIX v4,
  don't match it" ruling. Record whichever way it lands.
- `V5_STATS_GAP` unaffected but re-verify (the file loop changed on
  both sides).

### Bug 13 — `gcOrphanedFileRow` throws on a blobless index (v4 `7bcd8515`) — FAITHFUL, mirror owed
- v4 `doc-mount-file-links.repository.ts:131-165`: module-level
  `tableExistsSync`; both payload deletes (`doc_mount_documents`,
  `doc_mount_blobs`) guarded; `doc_mount_files` stays unguarded.
- v5 throws identically today: `db/doc_mount_file_links.rs:2153-2175`
  (callers `:837`, `:1062`, `:1280`, `:2232`, `:2378`;
  `api/mount_points.rs:581`). The two-tier fail-soft shape already
  exists in-repo at `:2306-2360` — the guard just never reached
  `gc_orphaned_file_row` itself.
- No pin exists; the sides simply disagree once regenerated. Coverage:
  v4's regression test builds an index WITHOUT `doc_mount_blobs`; no
  committed v5 fixture has that shape — add the arm (a blobless-index
  case in the `doc_mount_file_links_tier2` corpus or a store-delete
  arm mirroring v4's `skips child tables that do not exist`).

### Bug 15 — `reindexLinkGroupSiblings` dead code (v4 `7bcd8515`) — CONVERGENCE
- v4: `l.linkGroupId` added to `queryJoined`'s SELECT + row map. Two
  lines; the pass comes alive.
- v5 correct from the start (`link_groups.rs:48-95`,
  `doc_mount_file_links.rs:673`, `:1823`).
- **Pins that trip:** `CHUNK_DIVERGENCES` (2 entries,
  `doc_mount_file_links_tier2_equivalence.rs:678-693`) +
  `assert_chunk_divergence` (`:695-740`; the v4-direction message at
  `:722-727` says retire). Retiring makes the chunker parity proof
  STRICT on `twins/left.md` / `twins/right.md` for the first time —
  expect real content/tokenCount/headingContext comparisons there.

### Bug 38 — native-text documents 404 on attach (v4 `7bcd8515`) — FAITHFUL, mirror owed
- v4: new `nativeTextAttachmentMime` (`path-utils.ts:101-121` —
  markdown/txt/json|jsonl, deliberately WITHOUT the `; charset=utf-8`
  suffix, because the provider text whitelist and `isTextFile` compare
  exact strings); `handleAttachMountDocument`
  (`chats/[id]/files/route.ts:252-315`, Librarian announcement, URL
  `/api/v1/mount-points/{mp}/files/{path}`, `size: fileSizeBytes`);
  the attach fall-through at `:333-350` (no blob + mime match +
  `docMountDocuments.findByFileId` hit); the GET file-list fallback
  (`:477-510`, `/files/` vs `/blobs/` URLs); `loadMountFileAsAttachment`
  returns the document `content` UTF-8 → base64 (`chat-files-v2.ts:525-550`).
- v5 faithfully 404s: `api/chat_media.rs:1531-1543` ("refusing to
  attach"); `services/chat_files.rs:134-215` requires a blob row.
  `services/mount_index/path_utils.rs:260` has `detect_native_text`
  but no `native_text_attachment_mime`. Precedent for the read side:
  `api/mount_files.rs:815` already falls back to documents for the
  byte-GET route.
- Family: `attach_mount_file_equivalence` (`QT_ORACLE_ATTACH_MOUNT_FILE`)
  over `crates/quilltap-web/tests/fixtures/attach-file-{main,mount,llmlogs}.db`.
  The corpus needs a native-text no-blob document that ATTACHES
  post-fix — check whether the current 404 arm's seed is reusable;
  extend the fixture family if not (you own it).
- Downstream note for the round: the P4.9E4A-era e2e store-attach beat
  deliberately seeds a BINARY file because native-text attach 404'd in
  both apps. That workaround can stay; if P4.D54 wants a native-text
  beat extension it coordinates at unify, not here.

### Bug 43 — orphaned thumbnails never collected (v4 `7bcd8515`) — NEW SURFACE + a v5-only gap
- v4: new `sweep-orphaned-thumbnails.ts` (90 LOC —
  `{scanned, deleted, unparseable}`; lists `THUMBNAIL_KEY_PREFIX`,
  strict-UUID `parseThumbnailStorageKey`
  (`^_thumbnails\/([0-9a-fA-F-]{36})_(\d+)\.webp$` shape —
  transcribe v4's exact regex), batches `findByIds`, `deleteRaw`s
  misses, skips-and-debug-logs unparseable names); `thumbnail-utils.ts:39`
  `THUMBNAIL_KEY_PREFIX = '_thumbnails'`; `manager.ts:484-514` new
  `listRaw(prefix, maxKeys?)` gated on
  `backend.getMetadata().capabilities.list && backend.list`, `[]` when
  unsupported, throws on listing failure; scheduled-maintenance step 7
  (`orphanedThumbnailsSwept`).
- v5 has `build_thumbnail_storage_key` (`services/file_storage.rs:165-167`,
  identical format) and `delete_raw` (`:604`) but NO parse helper, NO
  prefix const, NO `list` on the `StorageBackend` trait (`:484-490`),
  no capability metadata, no sweep. The trait's impls live in
  `quilltap-host` — the list capability is a NEW seam crossing into the
  host crate (follow the existing backend-trait extension precedent;
  local-disk backend lists the directory, unsupported backends report
  no capability and the sweep returns zeros exactly as v4 does).
- The per-delete/overwrite `cleanupThumbnails` v4 always had is a
  **v5-only pre-existing gap**, named at `api/files.rs:537` and `:1123`
  ("best-effort DB-invisible side effects — skipped"). Tier 2 below.

### Cross-cutting — the maintenance-summary collision (v4 `3bb664f0` + `7bcd8515`)
v4's summary after session 3:
`jobs, assets, caches, orphanedStoreChildrenSwept, orphanedFilesSwept,
terminals, orphanedThumbnailsSwept, failures` (failures vocabulary
gains `'orphan-store-children'` + `'orphan-thumbnails'`; store-children
runs BEFORE orphaned files "so a reaped link can drop its file too").
v5's (`services/scheduled_maintenance.rs:73-100`): `orphaned_files_swept`
before `parentless_store_rows_reaped`, no thumbnail key.
- **Pins that trip:** the `got_summary` shape + the
  `parentless_store_rows_reaped == 0` assertion in
  `maintenance_ops_tier2_equivalence.rs:144-172` (its rationale — "v4's
  summary has no key for it" — is now false).
- Work: converge v5's sweep to v4's key names, run order, and failures
  vocabulary wherever the family diffs them; reconcile the
  reaper-shape difference (see bug 9) so the summary VALUES compare;
  re-measure the `safeQuery`-swallow seam note at `:32-40` (v4's two
  new sweeps differ: `sweepOrphanedStoreChildren` is safeQuery-wrapped
  with a zeros default, `sweepOrphanedThumbnails` catches internally
  and is ALSO try/caught by the scheduler).

## Tiered deliverables

### Tier 1 — must land
1. **The convergence sweep**: retire every pin named above (bugs 8, 9,
   10-delete-all, 11, 12, 15) with its family regenerated fresh at
   `f4955e0e` and re-run BY NAME, zero SKIP, now as plain equalities.
   `PHASE_ORDER_RESIDUAL` and (probably) `PLANTED_ORPHANS` survive —
   verify each survivor still fires its both-direction halves
   (mutation-prove any pin you keep; delete only what measurement says
   converged).
2. **Bug 18**: the help-sync prune guard, + regen of all three help
   families.
3. **Bug 13**: the two table guards in `gc_orphaned_file_row`, + a
   blobless-index coverage arm.
4. **Bug 38**: `native_text_attachment_mime` + the attach / file-list /
   load-as-attachment fallbacks, + the extended attach-file corpus
   proving the document arm (announcement row included — the family
   diffs the Librarian post).
5. **Bug 43, the sweep half**: the parse helper + prefix const, the
   `StorageBackend` list capability + host impls, the sweep, scheduled
   step 7, and the maintenance-summary reshape (the cross-cutting
   item) — proven through `maintenance_ops_tier2_equivalence`
   regenerated at the pin, with a thumbnail corpus shaped like v4's
   test (a live entry, an orphan, a garbage name).
6. **The bug-12 live-difference disposition** (measure → converge or
   pin, recorded).
7. **The stale-comment sweep** across every file this lane owns whose
   "queued v4-side" prose the drift discharged (the survey's list:
   `vault_character_update.rs:33`, `document_stores.rs:105/:522`,
   `delete_all.rs:205`, the four harness headers). Do NOT edit
   `dogfood-findings.md` — the discharge sweep there is the UNIFIER's,
   one editor for the whole round.

### Tier 2 — should land
8. **Bug 43, the per-delete half**: `cleanup_thumbnails` at the two
   named `api/files.rs` skip sites (v4-faithful best-effort try/log).
   DB-invisible, so pin it with v5 unit tests over a temp storage dir
   (the `binary_routes.rs` precedent), not a differential.

### Tier 3 — explicit loud deferrals
- None planned. If the `StorageBackend` list seam turns out to need
  more host surface than the local-disk impl (e.g. a capability the
  e2e backend can't express), defer the AFFECTED BACKEND loudly by
  name in the lane record, never silently.

## The differential requirement, restated

Every family this lane touches regenerates from v4's REAL code at
`f4955e0e` (straight from `~/source/quilltap-server` while HEAD holds;
pinned worktree on drift), re-runs BY NAME with `--nocapture`, zero
SKIP, with a fresh-baseline marker grepped in each NDJSON
([[oracle-regen-silent-stale-pass]]). The famiy list:
`characters_update_tier2`, `help_doc_sync_guards` (+ the two help
siblings), `store_delete`, `system_delete_data`, `system_import_state`
(+ re-run `system_import_equivalence`), `system_restore_state` (+
`system_restore_equivalence`, `restore_vintage_state` — no env var,
runs always), `doc_mount_file_links_tier2` (+ `doc_mount_chunks_tier2`,
`doc_fm` — the chunk-count assertions), `maintenance_ops_tier2`,
`attach_mount_file`, and `mount_points_routes` (the DELETE route body
moved). Committed-fixture rule and TZ pins stand
([[sweep-recipes-write-into-the-repo]]); leave every recipe header
runnable for the sweep ([[harness-recipes-are-runnable]]). First-run
green on a changed arm proves nothing — mutation-prove per the D24
rule.

## Fixtures

This lane owns: `attach-file-{main,mount,llmlogs}.db` (+ meta) if bug
38 needs a new seed; the `help-sync-guards/` committed trees (extend
only if a new guard arm needs it); the maintenance-ops fixture builder
(`build-maintenance-ops-fixture.ts`, /tmp-built) + its new thumbnail
corpus; `store-delete-{main,mount}.db` only if `PLANTED_ORPHANS`
re-verification demands it (the builder refuses drift from the
constant — keep that property). It must NOT touch
`embedding-remainder-*` (P4.D52's), `salon-*`/`chat-cast-*` (P4.D53's),
or the provider corpora (P4.D55's). If a fixture this lane regenerates
is consumed by a sibling family it does not own, re-run that sibling
BY NAME and record it.

## Ownership (BINDING)

THIS lane owns: `crates/quilltap-core/src/db/vault_character_update.rs`
(comments), `services/help_doc_sync.rs`, `api/mount_points.rs`,
`db/doc_mount_file_links.rs`, `services/quilltap_import/
document_stores.rs` (comments), `services/delete_all.rs` (comments),
`services/backup/restore/orchestrator.rs`,
`services/scheduled_maintenance.rs`, `services/builtin_mounts.rs`,
`services/file_storage.rs`, the `StorageBackend` impl files in
`crates/quilltap-host` it must extend, `api/files.rs` (tier 2),
`api/chat_media.rs`, `services/chat_files.rs`,
`services/mount_index/path_utils.rs`, and the harness/oracle files of
the families listed above. It must NOT touch: P4.D52's files
(`conversation_markdown.rs`, `conversation_render_reconcile.rs`,
`db/conversation_chunks.rs`, `fold_episode_pass.rs`,
`embedding_dimension_reconcile.rs`, `almanack/**`,
`embedding_remainder_equivalence.rs`), P4.D53's (`api/salon.rs`,
`services/chat_participants.rs`, `api/chat_cast.rs`,
`services/chat_enrichment.rs`, `announcement_attribution.rs`,
`db/chats.rs`, `db/conversation_annotations.rs`,
`content_disposition.rs`), `apps/web/**`, P4.D55's provider files,
P4.43's files, `api/types.rs`. `CHANGELOG` and `status-log.md` are
append-only union-merge. Version bumps: bump every crate you touch per
`.claude/commands/commit.md`; the unifier recounts.

## Verification gate (before declaring done)

`cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
-D warnings` on BOTH feature sets; `cargo test --workspace
--no-fail-fast` with the lane's oracle env vars
([[cargo-test-workspace-is-fail-fast]]); the lane's differentials BY
NAME with `--nocapture`, zero SKIP, over oracles regenerated fresh at
`f4955e0e`; no SPA gate owed (this lane must not touch `apps/web`).
Per-commit rules: `.claude/commands/commit.md`. Lane record appended
to `status-log.md` before handoff, including the bug-12 disposition
and the bug-9/summary shape reconciliation.
