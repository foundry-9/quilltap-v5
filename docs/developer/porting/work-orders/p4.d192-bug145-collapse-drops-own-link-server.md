# Work order P4.D192 — bug 145, the server half: the avatar-roll collapse heal drops the victim's OWN link and frees the bytes only when nothing else holds them (`drop_victim_roll_link` over the three existing chokepoints), the in-pass `protectedKept` census with v4's `unexplainedDuplicateKeys` warn, `keptClause` on both summary sentences and `albumCopiesKept` in the log bag, the family's fixture grown to the two-link shape v4 never modelled — v4 `23abc1ba1`'s `migrations/scripts/collapse-duplicate-avatar-rolls-v1.ts` hunk, from `main`; rider: the `is_photos_relative_path` two-homes consolidation

**Status: CLOSED except Tier 2 item 7** (lane closed 2026-09-16 on branch
`claude/collapse-drops-link-server-262dec`, from `main` `e9173924`). The
`2075242f9` bug-145/146 drift catch-up + maintenance round
(P4.D192 ∥ P4.D193 ∥ P4.D194 ∥ P4.89 ∥ P4.90). The unifier rewrites this
header at unification.

**Landed:** Tier 1 items 1–6 whole (v5 measurably HAD bug 145 — 39 red rows
across 17 of 24 scenarios before the port, 0 after) and Tier 2 item 8 (the
fourth boot arm, red-first against the pre-fix delete).

**OPEN — Tier 2 item 7, the `is_photos_relative_path` consolidation.** Its
premise is REFUTED by measurement: the two homes are NOT byte-identical. The
predicates' bodies are, but their private `posix_dirname` helpers are not —
`db/doc_mount_file_links.rs`'s reproduces Node's algorithm (strips a RUN of
trailing slashes) and `photos/photos_paths.rs`'s strips a single one, so they
disagree on `"photos//"` (Node/twin `"."` → false; survivor `"photos"` →
TRUE) and `"photos///"`. The 13-row corpus is blind to both — it was run
against BOTH homes before the deletion and passed. Deleting the FAITHFUL home
would therefore have been a silent regression on a shape no test can see, so
the consolidation was reverted and the surviving twin now carries a ⚠ doc
comment naming the divergence, the inputs, and Node 24's answers. The
follow-up needs ownership this lane does not have
(`photos/photos_paths.rs` and `harness/oracle/cases/photos-relative-path.ts`);
the ordered shape is in the lane record's §3(b). Tier 1 item 2 is unaffected
and DID land as ordered — the heal imports `photos::photos_paths`, which is
the module v4's own migration imports.

**A second order premise refuted:** two of v4's six new cases seed exactly
what corpus scenario 9 already seeds (so scenario 9 carries them and is one
of the pre-fix reds), and v4's census case takes the EARLY RETURN — so the
conjunct v4's suite never opens is the POST-SUMMARY census exit, not the
early-return one the order proposed as (g). Details and the mutation table:
`status-log.md` → "P4.D192 — bug 145, the server half".

**Out-of-ownership edit, declared for the unifier:**
`crates/quilltap-host/src/host.rs` — `CollapseOutcome::Ran` gaining two fields
is a compile break there (E0027) and Tier 1 item 5 mandates the fields. No
sibling lane in this round owns that file. One hunk, the boot log line.

## Preamble

- **v4 oracle baseline: `ffb6b3119`.** This lane absorbs the `lib`-side
  half of **`23abc1ba1`** ("fix(avatars): the roll collapse no longer
  deletes the photo you kept (bug 145)", 2026-09-15) — the ONE hunk that is
  yours is `migrations/scripts/collapse-duplicate-avatar-rolls-v1.ts`
  (+200/−…); its `packages/quilltap/bin/quilltap.js` and
  `help/database-protection.md` hunks are P4.D194's. The ledger row is
  `ORDERED(P4.D192, P4.D194)`. Spec reads: `git show 23abc1ba1 --
  migrations/scripts/collapse-duplicate-avatar-rolls-v1.ts
  __tests__/unit/migrations/collapse-duplicate-avatar-rolls.test.ts` and
  the post-commit file `git show 23abc1ba1:migrations/scripts/collapse-
  duplicate-avatar-rolls-v1.ts` — **never the commit message** (ledger
  §5.3; §R.4). ⚠ The message's "closes bug 143 as not a defect" is a
  DOCS claim: the code that backs it is the in-pass census + the
  `keptClause` — port those hunks, not the narrative.
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** Pins **`/tmp/qt-v4-pin-p4d192-
  2075242f9`** (the collapse family's jest oracle — it imports v4's REAL
  migration, which must be the post-fix file) and **`/tmp/qt-v4-pin-
  p4d192-ffb6b3119`** (every neutrality leg: the link/GC families below).
- **Sibling lanes:** P4.D193 (turn manager + SPA), P4.D194 (CLI + help),
  P4.89 (the brahma fixture pair), P4.90 (orchestrator + chat_create) —
  none touches `db/avatar_rolls_collapse_heal.rs`, `db/doc_mount_file_
  links.rs`, `api/mount_points.rs`, `photos/**`, `services/maintenance.rs`
  or the collapse family. You touch nothing of theirs (Ownership table).
- **Worktree:** `git worktree add <path> -b claude/p4-d192-bug145-collapse-
  <hash> main`; `CARGO_INCREMENTAL=0`. Verify at start: `git log
  --oneline -1` is at or after `4572659b`.
- **Read before the work:** `crates/quilltap-core/src/db/avatar_rolls_
  collapse_heal.rs` whole (750 lines — `blob_id_from_storage_key` `:123`,
  `protected_blob_ids` `:166`, **`delete_victim_blob` `:214-250` — the
  hunk you replace**, `run_pass` `:456-662`: the survivor/victim walk with
  the protected-roll `continue` at `:520-535`, the early return `:562-575`,
  the delete loop `:590-625`, the summary info `:645`; the ledger write in
  `collapse_duplicate_avatar_rolls` `:364-450`); `db/doc_mount_file_
  links.rs:2400-2500` (`gc_orphaned_file_row` — returns `Result<bool>`;
  v4's returns per-table COUNTS, and the new caller needs `blobs`) and its
  callers `:899`, `:1141`, `:1462`, `:2541`, `:2677`, the in-file tests
  `:2802`, `:2839`, and `api/mount_points.rs:602`; `photos/photos_paths.rs`
  (the canonical `is_photos_relative_path` + `posix_dirname`) vs `db/doc_
  mount_file_links.rs:331-385` (the byte-identical twin — the rider);
  `services/file_storage.rs` `parse_mount_blob_storage_key` (v4's new
  `parseMountBlobKey` ALREADY has a v5 twin returning `(mountPointId,
  blobId)` — use it, do not write a third); `photos/avatar_rolls_service.rs
  :1-60` (the module doc that states the rule this migration now follows —
  "only the roll's own link is ours") and `classify_roll_links` `:235-290`;
  `crates/quilltap-harness/tests/avatar_rolls_collapse_heal_equivalence.rs`
  (header recipe `:1-45`; `build_main`/`build_mount` — the mount DDL has
  NO `relativePath` column today; the dump; the `infos`/`warns` comparand);
  `harness/oracle/cases/avatar-rolls-collapse-heal.test.ts` (`makeMountDb`
  `:217`, the link insert `:257`, the dump `:304-306`); `harness/oracle/
  fixtures/avatar-rolls-collapse-heal.json` (17 scenarios; the `rolls` /
  `chats` / `characters` / `messages` keys); `crates/quilltap-host/tests/
  host_boot_avatar_rolls_collapse.rs` (the three boot arms — none seeds a
  second link); v4's `lib/mount-index/orphan-store-reaper.ts:40-100`
  (`gcOrphanedFileRow` — `null` while a link remains, else `{documents,
  blobs, files}` counts) and `lib/photos/photos-paths.ts:30`; memory notes
  `a-clear-to-null-write-needs-a-seeded-non-null`, `a-guard-whose-other-
  conjuncts-are-false-is-untested`, `serde-json-map-remove-is-swap-remove`
  (not in play here, but the delete loop's ordering IS a comparand),
  `a-fixture-whose-natural-order-matches-the-read-order-hides-the-sort`.

## Mandate

Port the shipped hunk whole: replace `delete_victim_blob` with v4's
`dropVictimRollLink` — pick the roll's OWN link (never a `photos/` path, and
in the mount the storage key names when it names one), delete THAT link's
chunks and the link, then collect the content row through
`gc_orphaned_file_row` (bytes go only once nothing links to them) and report
`{ linkDropped, bytesFreed }` where `bytesFreed` is "the GC removed ≥ 1 blob
row" — which requires `gc_orphaned_file_row` to return v4's per-table counts
instead of a bool; count `blobs_deleted` on `bytesFreed` and
`album_copies_kept` on `linkDropped && !bytesFreed`; record `protected_kept`
at the moment of the keep decision and run v4's `reportCensus`
(`unexplainedDuplicateKeys` → the `Avatar rolls share a generation key this
pass did not choose to double up` warn with `unexplainedCount` + the first
20 `fileIds`) on BOTH exits, including the `victims.is_empty()` early return;
append `keptClause` to BOTH summary sentences (the ledger `message` is a
comparand); add `protectedKept` + `albumCopiesKept` to the summary log bag.
**v5 measurably HAS bug 145** (`delete_victim_blob` deletes by `fileId`
exactly as v4's `deleteVictimBlob` did) — prove it red-first: grow the shared
spec with v4's SIX new test cases as data (the three album cases, the
kept-report case, the two census cases) and watch the family go red on the
album cases against the pre-fix v5 before the port lands. The fixture needs
the two-link shape before the fix can be proven at all — v4's own note
("every test seeded a roll with exactly one link") applies to v5's 17
scenarios verbatim. Rider (Tier 2): `is_photos_relative_path` has two
byte-identical homes; delete the `db/doc_mount_file_links.rs` twin and
repoint its five importers + the harness family at `photos::photos_paths`.
This lane does NOT touch the CLI, `help/**`, the SPA, or any sibling's files.

## Survey-verified starting points (fresh survey 2026-09-16 at `2075242f9` and on `main` `4572659b` — do not re-derive)

### v4 (`git show 23abc1ba1 -- migrations/scripts/collapse-duplicate-avatar-rolls-v1.ts`)

- **Imports added:** `isPhotosRelativePath` from `../../lib/photos/photos-
  paths` and `gcOrphanedFileRow` from `../../lib/mount-index/orphan-store-
  reaper`.
- **`parseMountBlobKey(storageKey)`** → `{ mountPointId, blobId } | null`
  (same shape rule as before: `mount-blob:` prefix, `sep >= 1`, not
  trailing); `blobIdFromStorageKey` now delegates to it. v5 already has
  `services::file_storage::parse_mount_blob_storage_key` → `Option<(String,
  String)>` — reuse it; the module-local `blob_id_from_storage_key` may
  delegate the same way.
- **`dropVictimRollLink(mountDb, blobId, rollMountPointId)`** →
  `{ linkDropped, bytesFreed }`:
  1. `SELECT fileId FROM doc_mount_blobs WHERE id = ?` — none → `{false,
     false}`.
  2. `SELECT id, mountPointId, relativePath FROM doc_mount_file_links WHERE
     fileId = ?` → `links`.
  3. `rollLink = links.find(l => !isPhotosRelativePath(l.relativePath) &&
     (!rollMountPointId || l.mountPointId === rollMountPointId)) ?? null`
     — `Array.prototype.find` = FIRST match in the SELECT's row order (no
     ORDER BY: SQLite rowid order for a plain table — v5 must select in the
     same order; add no ORDER BY that v4 lacks).
  4. If `rollLink`: `DELETE FROM doc_mount_chunks WHERE linkId = ?` then
     `DELETE FROM doc_mount_file_links WHERE id = ?` (that ONE link only).
  5. `collected = gcOrphanedFileRow(mountDb, blob.fileId)`;
     `bytesFreed = (collected?.blobs ?? 0) > 0`; return `{ linkDropped:
     rollLink !== null, bytesFreed }`.
  The migration's own call always passes `parsed.mountPointId` (non-empty
  when parsed), so the `!rollMountPointId` arm is reachable only through
  the function's contract, not the pass — port it anyway (it is the shape
  `classify_roll_links` already carries) and pin it at unit level.
- **The delete loop** (`for victim of victims`): `parsed = parseMountBlobKey
  (victim.storageKey)`; `if (mountDb && parsed)` (was `&& blobId` — same
  truth table) → `{linkDropped, bytesFreed} = dropVictimRollLink(mountDb,
  parsed.blobId, parsed.mountPointId)`; `if (bytesFreed) blobsDeleted++;
  else if (linkDropped) albumCopiesKept++;` — the `catch` arm (warn, leave
  the files row, `continue`) is unchanged; the files-row delete after it is
  unchanged (so a victim whose only surviving link is the album copy still
  loses its `files` row — v4's third test).
- **`protectedKept: string[]`** pushed at the keep decision (right after
  `survivors.push({id: victim.id, key})` in the protected branch), BEFORE
  the `logger.info('Keeping avatar roll still serving as a character
  portrait', …)`.
- **`unexplainedDuplicateKeys(db, keptDeliberately)`**: the SQL selects
  `id, generationKey AS key, createdAt FROM files WHERE generationKey IS NOT
  NULL AND generationKey != '' AND generationKey IN (SELECT generationKey
  FROM files WHERE … GROUP BY generationKey HAVING COUNT(*) > 1)` — NOTE:
  over ALL `files` rows, NOT the avatar predicate (v4's `stowaway` test
  case is a `background_scene.webp` outside the predicate carrying the
  survivor's key, `storageKey NULL`); bucket by key in SELECT order; per
  bucket sort newest-first by `String(createdAt).localeCompare` (ISO
  strings from one clock — v5's existing byte `cmp` rationale at `run_pass
  :470-476` carries; `sort` is stable both sides); `[, ...rest]` — every
  non-newest row whose id is not in `keptDeliberately` is `unexplained`.
  `reportProgress(…)` calls are v4 migration-runner progress — NO-PORT
  (v5 has no progress reporter; record it).
- **`reportCensus(db, protectedKept)`**: returns silently when
  `unexplained.length === 0`; else `logger.warn('Avatar rolls share a
  generation key this pass did not choose to double up', { context:
  'migration.collapse-duplicate-avatar-rolls', unexplainedCount,
  fileIds: unexplained.slice(0, 20) })`. Called on BOTH exits — inside the
  `victims.length === 0` early return (before its `return`) and after the
  summary `logger.info`, before the final `return`.
- **`keptClause(n)`**: `''` for 0; `'; kept 1 roll still serving as a
  character portrait'` for 1; `` `; kept ${n} rolls still serving as
  character portraits` `` otherwise — appended to BOTH messages: `Keyed ${n}
  avatar configurations; nothing to collapse` + clause, and `Collapsed …
  (${blobsDeleted} images freed; repointed …)` + clause.
- **The summary log bag** gains `protectedKept: protectedKept.length` and
  `albumCopiesKept` (between `victimsDeleted` and `blobsDeleted` — key
  order is the oracle's comparand for `infos`).
- **v4's test file** (`__tests__/unit/migrations/collapse-duplicate-avatar-
  rolls.test.ts`): `makeMountDb` gains `"relativePath" TEXT NOT NULL DEFAULT
  ''`; `seedRoll` now writes `character-avatars/avatar_Friday_${id}.webp`
  (mount `mount-1`); NEW `keepInAlbum(id, mountPointId = 'vault-1')` inserts
  link `album-${id}` over `content-${id}` at `photos/kept-${id}.webp`; six
  new cases: **(a)** `keeps the album photo and its bytes when the roll it
  came from is collapsed` (old+new, keepInAlbum(old): `link-old` and its
  chunk gone; `album-old`, `blob-old`, `content-old` stay; files = [new]);
  **(b)** `still frees the bytes of a roll nobody kept` (kept/unkept/new,
  keepInAlbum(kept): `blob-kept` stays, `blob-unkept` gone, message
  contains `1 images freed`); **(c)** `drops the files row of a roll whose
  only surviving link is the album copy` (orphaned+new, keepInAlbum
  (orphaned), then DELETE `link-orphaned` before the run: files = [new],
  `album-orphaned` + `blob-orphaned` stay); **(d)** `reports the rolls it
  kept, not only the ones it collapsed` (portrait+new, character
  `defaultImageId = link-portrait`: message contains `kept 1 roll still
  serving as a character portrait`); **(e)** census `says nothing about a
  duplicate it chose to keep` (same seed, zero census warns); **(f)**
  census `names a duplicate key this pass did not double up on purpose`
  (new + the `stowaway` files row inserted with the survivor's key,
  `storageKey NULL`, `originalFilename 'background_scene.webp'`: ONE warn,
  `unexplainedCount 1`, `fileIds ['stowaway']`).

### v5 (on `main` `4572659b`)

- `db/avatar_rolls_collapse_heal.rs:214-250` `delete_victim_blob` — the
  bug, byte-for-byte v4's old shape (select links by `fileId`, delete every
  chunk, every link, documents, blobs, files). `run_pass:590-625` counts
  `blobs_deleted` on `Ok(true)`. The summary `tracing::info!` at `:645` has
  no `protectedKept`/`albumCopiesKept`; the early return at `:562` never
  reports a census; the ledger `message` is built in `collapse_duplicate_
  avatar_rolls` (`:364-450`) from `CollapseOutcome::Ran` — `CollapseOutcome`
  needs `protected_kept` + `album_copies_kept` fields so the message and
  the bag both read them.
- `db/doc_mount_file_links.rs:2443` `gc_orphaned_file_row(conn, file_id) ->
  Result<bool, DbError>` — `true` when the row was collected. Widen to
  `Result<Option<OrphanedFileRowGc>, DbError>` with `{documents, blobs,
  files}: usize` from `conn.execute`'s changed-row counts (rusqlite returns
  them), `None` while a link remains; every existing caller reads
  `.is_some()` where it read the bool. Behaviour-neutral for them —
  proven by the neutrality legs below.
- `services::file_storage::parse_mount_blob_storage_key(&str) ->
  Option<(String, String)>` — the `parseMountBlobKey` twin (used by
  `classify_roll_links:245`).
- `photos/photos_paths.rs:20` `is_photos_relative_path` + `db/doc_mount_
  file_links.rs:372` the twin (with its own private `posix_dirname` at
  `:331-370`, "Used only by `is_photos_relative_path`"). Importers of the
  TWIN: `api/chat_media.rs:1716` (fully qualified), `photos/photo_link_
  summary.rs:13`, `photos/character_gallery_service.rs:17`, `services/
  maintenance.rs:32` + `:375` (a test module), `crates/quilltap-harness/
  tests/photos_relative_path_equivalence.rs:17`. Everyone else already
  imports `photos::photos_paths`.
- The harness family: `build_mount` DDL (no `relativePath`), the link seed,
  the dump `SELECT id, fileId, mountPointId FROM doc_mount_file_links ORDER
  BY id` — all three grow with the spec; the jest oracle's `makeMountDb`
  (`:217`), insert (`:257`) and dump (`:304-306`) likewise.
- `host_boot_avatar_rolls_collapse.rs` — three arms, one link per roll;
  unchanged unless you add a two-link boot arm (Tier 2).

## Tiered deliverables

### Tier 1 — must land

1. **The spec grows FIRST, red-first.** Extend `avatar-rolls-collapse-
   heal.json` so a roll may carry `relativePath` (default `character-
   avatars/avatar_Friday_<id>.webp`, mount `mount-1`), a scenario may list
   `albumLinks: [{ rollId, mountPointId? (default 'vault-1') }]`
   (`album-<id>` at `photos/kept-<id>.webp` over `content-<id>`),
   `deleteLinks: ['link-<id>']` (pre-run deletions — case (c)), and
   `extraFiles: [{ id, originalFilename, generationKey: 'survivor-of:<id>'
   | literal, storageKey: null, createdAt, prompt, model }]` (case (f) —
   the key must be DERIVED at seed time on both sides via
   `deriveLegacyAvatarCacheKey` / `derive_legacy_avatar_cache_key`, never
   a hand-copied hash). Add v4's six cases (a)–(f) as scenarios 18–23 with
   their expectations as the existing scenarios express them (whole-table
   post-state + `message` + `warns`). Widen BOTH builders' mount DDL with
   `"relativePath" TEXT NOT NULL DEFAULT ''` and BOTH dumps with
   `relativePath`. Regenerate the oracle at the TARGET pin; run the v5
   family against the UNPORTED heal: **cases (a), (b), (c) MUST be red**
   (the album link/blob/content rows gone on v5, present on v4; the
   `images freed` count differs on (b)), **(d)–(f) red on `message`/`warns`**.
   Record the pre-fix red set in the lane record. The 17 pre-existing
   scenarios must stay GREEN across the widen (their `relativePath` column
   is invisible to the old code path) — that is the regression guard on
   the spec change itself.
2. **`drop_victim_roll_link`** replaces `delete_victim_blob` with v4's five
   steps (survey), the row order of the links SELECT unchanged (no ORDER BY),
   `is_photos_relative_path` from `photos::photos_paths`, the mount-point
   conjunct with the `None` arm, the GC through the widened
   `gc_orphaned_file_row`, returning a small `DroppedVictim { link_dropped,
   bytes_freed }`. The delete loop counts `blobs_deleted` on `bytes_freed`
   and `album_copies_kept` on `link_dropped && !bytes_freed`; the `Err` arm
   and the files-row delete are unchanged. Carry v4's why-comment ("Only
   the roll's own link is ours…") and the cross-reference to
   `delete_avatar_roll`.
3. **`gc_orphaned_file_row` widened** to `Result<Option<OrphanedFileRowGc>>`
   (§ survey) with every caller adapted (`.is_some()`), the two in-file
   tests kept meaningful (they assert the bool today — assert
   `is_none()`/`is_some()` AND the counts), plus ONE new unit test: a
   content row with a blob → `Some { blobs: 1, files: 1 }`; document-only
   → `Some { blobs: 0, … }` (the arm `bytesFreed` turns on — v4's
   `(collected?.blobs ?? 0) > 0` is FALSE for a document-only orphan, so a
   document-only victim counts as an album copy kept if its link dropped;
   pin that reading at unit level with a comment naming the sha).
4. **`protected_kept` + the census.** Push at the keep decision;
   `unexplained_duplicate_keys` over ALL `files` (survey — NOT the avatar
   predicate; the `stowaway` arm proves it), stable newest-first sort per
   bucket, `report_census` on BOTH exits with v4's warn (`target:
   "quilltap::migration"`, `context = LOG_CONTEXT`, `unexplainedCount`,
   `fileIds` = first 20, as a JSON array field — check how the family's
   capture normalizes array fields against v4's `warns` rows). The
   `reportProgress` calls are NO-PORT: one comment.
5. **`kept_clause` + the log bag.** `CollapseOutcome::Ran` gains
   `protected_kept` and `album_copies_kept`; the ledger `message` on both
   exits appends the clause; the summary info gains the two fields in v4's
   key position. Mutation proofs (each reddening exactly its scenario):
   M1 drop the clause → (d) red on `message`; M2 count `album_copies_kept`
   on `link_dropped` alone → (b) red on the bag; M3 skip the early-return
   census → a NEW scenario **(g) `census-fires-with-nothing-to-collapse`**
   (a lone roll + a `stowaway` sharing its key: `Keyed 1 …; nothing to
   collapse` AND one warn) red — add (g) to the spec; it is the conjunct
   v4's own suite never opens; M4 select links by `fileId` and delete all
   (the old bug) → (a) red; M5 drop the `!is_photos_relative_path` conjunct
   → (a) red on `album-old` (the album link is FIRST in rowid order only if
   seeded first — seed the album link FIRST in (a) so M5 discriminates;
   record the order dependence); M6 `bytes_freed = link_dropped` → (b)
   red on `images freed`.
6. **The regen + neutrality legs.** The collapse family fresh at the TARGET
   pin (24 scenarios, zero SKIP, `grep -c 'did not choose to double up'` >
   0 and `grep -c 'albumCopiesKept'` > 0 in the NDJSON); the GC-widening
   neutrality: `doc_mount_file_links_tier2_equivalence`, the
   `store_delete_*` family (P4.31's cascade — it reaches `gc_orphaned_file_
   row` through `api/mount_points.rs:602`), `mount_index_orphan_reaper`
   (or whatever name the boot reaper's family carries — find it by
   grepping the harness for `gc_orphaned_file_row`/`reap_orphaned`), and
   `photos_relative_path_equivalence` — all at the BASELINE pin, by name,
   zero SKIP; `host_boot_avatar_rolls_collapse` green.

### Tier 2 — should land

7. **The `is_photos_relative_path` consolidation** (the `31436bae4` round's
   maintenance small): delete the `db/doc_mount_file_links.rs` twin and its
   private `posix_dirname`; repoint the five importers + the harness family
   at `photos::photos_paths`; `photos_relative_path_equivalence` green
   unchanged (the corpus is the proof the two were identical — run it
   BEFORE the deletion against BOTH homes by temporarily importing each,
   record, then delete). Zero behaviour change.
8. **A two-link boot arm** in `host_boot_avatar_rolls_collapse.rs`: the
   planted duplicate's victim carries an album link; after boot the album
   link + blob survive and the `files` row is gone. This is the WIRING
   proof the family cannot give (the family drives the function directly).

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **Real-instance proof (💸):** on the Friday copy v4 has ALREADY run this
  migration (2026-09-11, ledger row present), so v5's boot writes nothing —
  the fix cannot be observed on real data without deleting v4's ledger row
  on the disposable copy and re-running (ledger §5.5: a planted proof is a
  weaker claim). Bank it for the dogfood pass with that recipe; do not run
  it here.
- `reportProgress` — NO-PORT, one comment (v5 has no migration progress
  reporter).

## The differential requirement, restated

Every unit above ships with its equivalence test driving v4's REAL
migration: `avatar_rolls_collapse_heal_equivalence` (tier 2 — whole-table
post-state, `message`, `infos`/`warns`, the second run) over the shared spec,
regenerated at the TARGET pin through the sweep driver (`recipe_sweep.py
--run avatar_rolls_collapse_heal_equivalence --v4 /tmp/qt-v4-pin-p4d192-
2075242f9 --v5w <ABSOLUTE worktree>`). Closest recipes: this family's own
header; the P4.D184 lane record in `status-log.md` (how the 17 scenarios
were built from v4's suite as data; the `infos`/`warns` capture idiom);
`fixture-rebuild-vs-migrate-in-place` does NOT apply (this family builds
in-memory from the spec — no committed `.db`). The GC widening is proven by
re-running its consumers' families at the baseline pin (neutral by
construction; the run is the proof).

## Fixtures

- **Consumes / grows:** `harness/oracle/fixtures/avatar-rolls-collapse-
  heal.json` (17 → 24 scenarios: v4's six + (g)); no committed `.db` pair —
  both sides build in-memory from the spec.
- **Owns regenerating:** `avatar_rolls_collapse_heal_equivalence` (target
  pin); the neutrality families named in Tier 1 item 6 (baseline pin).
- **Delivers:** nothing new on disk beyond the spec.

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `2075242f9` bug-145/146 drift catch-up + maintenance round:
**P4.D192 ∥ P4.D193 ∥ P4.D194 ∥ P4.89 ∥ P4.90**, ordered 2026-09-16. Five
lanes, five worktrees, five lane branches, ALL from `main` (no stacking —
every lane's ownership is disjoint by file, so nothing needs a substrate
commit). The unifier cherry-picks in the order D192 → D193 → D194 → P4.89
→ P4.90 (server-side heals before the fixture widen; the fixture widen
before the family that re-measures over it), gates ONCE, and `merge
--ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`ffb6b3119`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`2075242f9`**
  (`4.10.0-dev.40`) — FOUR commits past the baseline, two carrying code:
  `23abc1ba1` (bug 145 + the bug-144 convergence + closes bug 143 as not
  a defect) and `2075242f9` (bug 146); `064ba85df` and `81e02f7a2` are
  the two docs-only filings this port's own walk wrote. v4 `bugfix` tip
  `1a2b2164c` and `release` tip `8fbf2afe0` are unmoved; there is still
  no `release: 4.10.0` squash. The checkout sat on `main`, CLEAN, at the
  planning probe (2026-09-16 ~08:30 CDT; the §2 probe PASSED, so the
  ledger stood and nothing was re-derived). **At unification the baseline
  MOVES to `2075242f9`** and all four rows retire (two ABSORBED, two
  NO-PORT-RATIFIED).
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log 2075242f9..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Any mismatch — a moved HEAD, a dirty `lib/`/`app/`/`packages/`
  /`plugins/`/`migrations/`/`help/` tree, the checkout off `main` — is a
  STOP: report it and do nothing else.** A lane never writes the ledger.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1). HEAD is past the
  baseline and two commits touch `lib/`, `migrations/`, `packages/` and
  `help/`. Every regen runs from a LANE-UNIQUE detached worktree per
  ledger §5.1 (`git -C ~/source/quilltap-server worktree add --detach
  /tmp/qt-v4-pin-<order>-<sha> <sha>`, the THREE symlink classes, `cd`
  into it; never stash or checkout-switch the human's repo; `git
  worktree list` before each batch; verify the pin by grepping the fresh
  NDJSON for a marker only one tree has; check the file is non-empty
  before believing a diff). Two pins per lane:
  - **the TARGET pin `2075242f9`** — for every family the lane's port
    MOVES (the oracle must import v4's post-fix code: the collapse
    migration, `resolveFloorSeatId`, the CLI's new wording, the two
    re-vendored help pages);
  - **the BASELINE pin `ffb6b3119`** — for every neutrality leg and
    every family the lane merely re-runs, and for `QT_V4_ROOT` /
    `QT_V4_CHECKOUT` on any embed/content guard run BEFORE a re-vendor
    commit lands.
  The sweep driver is the sanctioned per-family path
  (`harness/tools/recipe_sweep.py --run <family> --v4 "$PIN" --v5w
  <ABSOLUTE worktree>`); never run two sweeps concurrently; a family that
  reports `SKIP` in 0.00 s did not run (`a-family-env-var-is-not-its-
  regen-var`). Ledger §5.2's silent-stale-pass discipline applies to
  every regen (`rm -f` the fixture first; read the builder's last line;
  `grep -c` the changed bytes in the fresh NDJSON — MUST be > 0).
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). The
  spec for every item is `git show <sha> -- <path>` plus `git show
  <sha>:<path>`; a commit message describes the bug as its author
  understood it and the hunks are often narrower. Re-verify even where
  this order already flags the trap — the order's paragraph is the same
  kind of prose. Carry a v5-side code comment naming the sha where the
  prose misleads.
- **§R.5 — Convergence rows: measure, don't assume** (ledger §5.4). The
  bug-144 wording pin (`a_fresh_heartbeat_keeps_v4s_false_liveness_claim`)
  trips at the target pin BY DESIGN; dump v4's actual post-fix output
  before retiring it, and expect Tier R's five `lock clean …` cases to
  move together.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig) and its silence leg; a fixture change lands with the
  regen of every family that reads it. Mutation proofs: each named
  proof must redden exactly the test it is written against, and a proof
  that SURVIVES as first written is a finding to fix, not a line to
  delete (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`).
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). This order's status header is the
  lane's to update at lane close; the unifier rewrites it at unification.
  `docs/developer/porting/drift-ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed (and `apps/web/package.json` when it touches the SPA) on each
  commit per `.claude/commands/commit.md`; the unifier RECOUNTS as base +
  the sum of every lane's bumps (the silent-auto-merge trap: four lanes'
  identical `0.0.922` bumps merge as ONE). Expected: core is bumped by
  P4.D192, P4.D193 and P4.90; harness by all five (P4.89 only if it edits
  a harness header); cli by P4.D194; web by P4.89 (the committed fixture
  pair lives under `crates/quilltap-web/tests/fixtures/`); host by
  P4.D192 only if `host_boot_avatar_rolls_collapse.rs` changes; the SPA by
  P4.D193.
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  P4.D194 pre-lists the paths the target moves (with byte counts at the
  pin) in its lane record; no lane copies them.
- **§R.10 — Cross-lane handoffs, named:** (a) P4.D192 widens
  `gc_orphaned_file_row`'s return type — its only callers are in files
  P4.D192 owns (`db/doc_mount_file_links.rs`, `api/mount_points.rs:602`);
  no sibling touches either. (b) P4.D193's `help/chat-turn-manager.md`
  hunk and P4.D192's `help/database-protection.md` hunk are BOTH
  P4.D194's (the one `help/**` owner). (c) P4.89's widened
  `brahma-{main,mount}.db` is read by three families; no sibling
  regenerates them. (d) P4.90's `orchestrator.rs` edit is one statement
  after `:2861`; P4.D189's wrap census counts `watch_stream(` per file and
  must not move — P4.90 adds none. (e) The dogfood-findings row #119 cell
  moves with P4.D194 (the convergence); no other lane edits that file.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D192** — bug 145 server | `crates/quilltap-core/src/db/avatar_rolls_collapse_heal.rs`; `crates/quilltap-core/src/db/doc_mount_file_links.rs` — ONLY (i) `gc_orphaned_file_row`'s return type + its in-file callers/tests and (ii) the deletion of the `is_photos_relative_path` twin + its private `posix_dirname`; `crates/quilltap-core/src/api/mount_points.rs:602` (the one out-of-file GC caller, adapted to the new return); the twin's importers, import-line edits ONLY: `api/chat_media.rs:1716`, `photos/photo_link_summary.rs:13`, `photos/character_gallery_service.rs:17`, `services/maintenance.rs:32,375`; `crates/quilltap-harness/tests/avatar_rolls_collapse_heal_equivalence.rs`; `crates/quilltap-harness/tests/photos_relative_path_equivalence.rs` (import path only); `harness/oracle/cases/avatar-rolls-collapse-heal.test.ts`; `harness/oracle/fixtures/avatar-rolls-collapse-heal.json`; `crates/quilltap-host/tests/host_boot_avatar_rolls_collapse.rs` (only if a two-link arm is added); this order's header | `services/orchestrator.rs`, `services/chat_create.rs`, `participant_filters.rs`, `crates/quilltap-cli/**`, `help/**`, `apps/web/**`, `crates/quilltap-web/tests/fixtures/brahma-*`, every other harness family (regen-and-run ONLY for neutrality — no edits), `docs/v4/**`, the drift ledger |
| **P4.D193** — bug 146 (server twin + SPA) | `crates/quilltap-core/src/participant_filters.rs`; NEW `harness/oracle/cases/floor-seat.ts`; NEW `crates/quilltap-harness/tests/floor_seat_equivalence.rs`; `apps/web/src/app/chat/turn-order.ts` + `turn-order.spec.ts`; `apps/web/src/app/chat/turn-controls.ts`; `apps/web/src/app/screens/salon/salon-conversation.ts` — ONLY `bannerSeat` / `userTurnName` / `isSeatsTurn` / `mustSpeak` / `onSkipUserTurn` and the `<qt-turn-controls …>` binding block (`:431-436`); `apps/web/src/app/screens/salon/salon-turn-controls.spec.ts`; NEW `apps/web/e2e/salon-floor-seat-flow.spec.ts`; `apps/web/e2e/salon-dialogs-flow.spec.ts` ONLY if its banner assertion (`:412-418`) must learn the third sentence; this order's header | every other `crates/**` file, `help/**`, `core-contract.ts` (no wire change in this feature), every other SPA file, the drift ledger |
| **P4.D194** — bug-144 convergence + ratifications + help re-vendor | `crates/quilltap-cli/src/db_cmd.rs`; `crates/quilltap-cli/tests/cli_differential.rs` (only if a Tier R case must be added — none is expected); `help/database-protection.md`; `help/chat-turn-manager.md`; `docs/developer/porting/dogfood-findings.md` — row #119's status cell ONLY; this order's header | every other `crates/**` file, `apps/web/**`, every harness family except the `help_*` content families (regen-and-run at the target pin), `docs/v4/**` (pre-LIST for the unifier, never copy), the drift ledger |
| **P4.89** — the brahma fixture vintage widen | `crates/quilltap-web/tests/fixtures/brahma-main.db`, `brahma-mount.db` (+ any `.meta.json` sidecar found beside them); `harness/oracle/fixtures/migrate-memories-fixture-columns.ts` (EXTEND its target list — the P4.52 idiom); the THREE brahma harness headers (`crates/quilltap-harness/tests/brahma_orchestrator_tier3_equivalence.rs`, `brahma_console_tier3_equivalence.rs`, `brahma_console_routes_equivalence.rs`) — regen-recipe comments and a vintage note ONLY, no assertion changes unless a red is proven fixture-caused; the three jest oracles `harness/oracle/cases/brahma-*.test.ts` ONLY if an oracle-side heal is proven necessary AFTER the widen; this order's header | every `crates/quilltap-core/src/**` file (the P4.50-class prefix finding is MEASURED and RECORDED, never fixed here), `apps/web/**`, `help/**`, every other fixture, the drift ledger |
| **P4.90** — the failover model refresh + the six greeting lines | `crates/quilltap-core/src/services/orchestrator.rs` — ONLY the post-refresh `params` rebuild at `:2861-2863` (and, if measurement says so, the two loop-option sites `:2968-2971` / `:3046-3049`); `crates/quilltap-core/src/services/chat_create.rs` — ONLY the six log lines at their v4 sites + their unit-level capture tests; `crates/quilltap-harness/tests/orchestrator_tier3_equivalence.rs`; `harness/oracle/cases/orchestrator-tier3.test.ts`; `harness/oracle/fixtures/orchestrator-tier3.json` + `build-orchestrator-fixture.ts`; `crates/quilltap-harness/tests/chat_create_capstone_equivalence.rs` (capture pins only, if the pin idiom lives there); this order's header | `services/native_tool_loop.rs`, `services/text_tool_loop.rs`, `services/primary_stream.rs`, `services/provider_failover.rs`, `services/initial_greeting.rs`, `model/**`, every other harness family (regen-and-run only), `apps/web/**`, `help/**`, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0),
   and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp`) where the family is deterministic across
   regens; where it is not (`regenerate-at-both-pins-and-cmp-is-not-
   universal`), run by name and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast` with the lane's env block
   (its own oracle vars + `QT_V4_ROOT`/`QT_V4_CHECKOUT` at the right pin +
   `QT_NODE` = the node BINARY): report **binaries / passed / failed /
   ignored** from the per-binary lines and the exit code, and the lane's
   families confirmed RUN by non-zero duration. The standing withheld
   family (`brahma_orchestrator_tier3` — P4.89's subject) is withheld BY
   NAME in every lane but P4.89, which restores it.
9. **SPA lanes only (P4.D193):** `npm run lint` (incl. the qt-class guard),
   `npm test` (spec files / tests / 0 failed), `npm run build` clean, then
   the lane's e2e beats by FILE against the release binary, and ONE full
   Playwright run at lane close (numbers recorded; the documented
   intermittents — the P4.D161 pause-toast beats, the P4.d17 quill beat —
   are named, re-run by file, and never re-classified as this lane's).
10. **Ownership check:** `git diff --stat main...HEAD` — every path is in
    the lane's Owns column; every MUST-NOT-TOUCH path absent.
11. **Per commit:** `.claude/commands/commit.md` in full — the equivalence
    test is non-negotiable (§1), the invariants (§2), real-instance safety
    (§3), the harness regen (§4), build/test/lint (§5), the CHANGELOG
    entry + version bumps (§7). Commit on the lane branch only; never
    merge to `main`; never `git stash`; never a worktree inside a worktree.
12. **Lane close:** update this order's status header (what landed, what
    was refuted by measurement, what stays OPEN with its named cause),
    append the lane record to `status-log.md` (the regen commands as run,
    the counts, the mutation table, every finding outside the lane's
    ownership for the unifier), and stop. The final gate runs in the
    FOREGROUND of the lane's last turn — a backgrounded gate whose
    notification arrives after the lane reports done is a lane that is
    NOT done (`a-backgrounded-sleep-does-not-gate-your-turn`).
