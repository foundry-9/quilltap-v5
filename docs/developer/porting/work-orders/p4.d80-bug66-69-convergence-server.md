# Work order P4.D80 — the `aa464abf` bug-66/67/69 commit, server side: the archivedAt enrichment, the rehydrate self-heal, and the round's dispositions — v4 `aa464abf`

**Status:** CLOSED (2026-08-15, lane branch
`claude/p4-d80-bug-convergence-8f1945`). Every Tier-1 and Tier-2 deliverable
landed; the Tier-3 items are deferrals by design and stay deferred.

- **Tier 1 ✔** — (1) `archived_at` on `EnrichedCharacterDetail` + both
  `get_character_detail` returns, wire key `archivedAt` (Contract C),
  `api/salon.rs` untouched as predicted; (2) the rehydrate self-heal with
  v4's exact semantics (stored-digest comparison, the byte-exact warning
  sentence, repair-failure tolerance, warnings PREPENDED), differential arms
  (a)/(b)/(c) against v4's REAL `restoreArchiveBundle` on planted fixture
  COPIES, mutation-proven; (3) the three disposition rows
  (`54fdfb43` / `55d63022` / `315e51d2`) with their evidence; (4) the bug-69
  bank (`digest-policy.ts` + the watcher/reconciliation arms) beside the
  P4.D62 bug-53 bank, with the loud Tier-1-when-the-watcher-ports note.
- **Tier 2 ✔** — item 5 landed as a MEASURED comparand rather than a note
  (the clobbered case plants a wrong `size` and both sides must hand it back
  wrong); item 6's stale-lore annotation landed.
- **Tier 3 (still deferred, by design)** — the watcher/reconciliation
  subsystem stays unported (standing refusal, `api/files.rs:1642`); the beat
  flip + record retirements for bugs 66/67 belong to P4.D81 and the unifier;
  💸 a live rehydrate of a REAL clobbered row joins the owed dogfood queue.
- **Fixtures:** none changed. No other family's oracle invalidated.
- **Full record + regen recipe:** `status-log.md` → "P4.D80 — the `aa464abf`
  bug-66/67/69 commit, server side (2026-08-15)".

## Preamble

- **v4 baseline for this round: `aa464abf`** (v4 main HEAD at planning,
  2026-08-15 — "fix: archived-seat badge (66), source-view send (67),
  archive digest clobber (69)"). **Drift-check at lane start** per the
  two-branch rule (`git log aa464abf..main` AND `git diff main bugfix --
  lib/ app/ packages/ plugins/`; measure bugfix with `diff`, never the
  commit list). At planning the v4 tree was CLEAN on `main`; bugfix
  carried nothing portable (tip `3a76b17d` = the 4.8.4 marker;
  `009c49b2` = a test-only deflake). **STOP and report** if HEAD moved
  or the tree is dirty; verify the checkout is on `main` before any
  regen, else pin a detached worktree
  (`oracle-regen-pinned-v4-worktree`, or `recipe_sweep.py --v4 aa464abf`).
- Sibling lanes this round: **P4.D78** (Ollama provider wire),
  **P4.D79** (profile/host half: prefill column + greeting reasoning),
  **P4.D81** (the SPA riders), **P4.48** (the import-preflight
  read-error escalation). Ownership is disjoint — see Shared contract.
- Work in your own worktree on a lane branch; never `git stash` across
  lanes. Repo-root paths exist in BOTH trees — edit inside the worktree
  (`edit-inside-the-worktree-not-main`).

## Mandate

Absorb the server half of v4 `aa464abf` and disposition the round's
three no-port commits. Two code deliverables: **bug 66** — the chat-GET
enrichment finally carries `archivedAt` (v4 converging on the bug THIS
PORT filed; v5 reproduced it faithfully and pinned it with an e2e
beat), and **bug 69** — the rehydrate **self-heal arm** for an archive
row whose plaintext digest was clobbered with a ciphertext digest
(reachable in v5 because v5 opens the same instances v4's pre-4.9
watcher damaged). The watcher/reconciliation/digest-policy halves of
bug 69 do NOT port (v5 has no file-storage watcher — banked
subsystem); they get BANKED loudly. **Bug 67 requires NO v5 code** —
v4 adopted v5's pinned behavior; the record updates ride P4.D81 and
the unifier. The lane owns exactly two core files
(`services/chat_enrichment.rs`,
`services/character_archive/service.rs`) + its harness arms, and must
NOT touch anything else in `services/**`, `db/**`, `api/**`
(P4.D79/P4.48), `model/**` (P4.D78), or `apps/web/**` (P4.D81).

## The drift, classified (surveyed FRESH 2026-08-15 at `aa464abf`)

`aa464abf`, 26 files, +771/−203, plus three disposition commits:

- **Bug 66 (server half).** `lib/services/chat-enrichment.service.ts`:
  `EnrichedCharacterDetail` gains `archivedAt: string | null`;
  `getCharacterDetail` carries `character.archivedAt ?? null` on BOTH
  returns — the avatar-override return (`:322`) and the main return
  (`:348`, with v4's comment "the chat GET renders the sidebar, so
  the archive badge needs this projection here too — not only in the
  participants/PUT enrichment"). The client half (`useParticipants`
  field-by-field rebuild dropping the field) is v4-client-specific —
  P4.D81 verifies the v5 SPA pipeline has no analogous drop.
- **Bug 69.** Three v4 pieces, ONE portable:
  1. `lib/file-storage/digest-policy.ts` (new):
     `preservesContentDigest(category)` — `ARCHIVE` rows carry a
     PLAINTEXT digest of encrypted disk bytes; the watcher and boot
     reconciliation must never re-derive them from disk. **NO v5
     twin possible** — v5 has no `watcher.ts`/`reconciliation.ts`
     twin at all (`api/files.rs:1640-1648` refuses `files_sync`
     loudly; bug 53's guards were BANKED at P4.D62). BANK this file
     verbatim beside them.
  2. `watcher.ts`/`reconciliation.ts` digest-preserve arms — banked
     with (1).
  3. **`archive-service.ts` `restoreArchiveBundle` self-heal — PORTS.**
     On digest mismatch: compute `storedDigest = sha256(raw)` (the
     encrypted bytes as read); if `fileRow.sha256 !== storedDigest` →
     the existing corrupt-bundle refusal, sentence UNCHANGED; if
     EQUAL → the row was clobbered (v4's pre-4.9 watcher), so: warn
     (log), push the fixed warning sentence
     `The bundle's recorded digest had been overwritten with the
     digest of its encrypted bytes; the contents verified against
     the file as stored, and the record has been repaired.` onto
     `clobberWarnings`, attempt `files.update(archiveFileId,
     {sha256: plaintextDigest})` — a repair FAILURE warns and the
     rehydrate CONTINUES — and return
     `warnings: [...clobberWarnings, ...result.warnings]`
     (clobber warnings PREPENDED).
- **Bug 67 — no v5 code.** v4's `composer-source-mode.ts` +
  `SalonView` change adopts exactly the behavior v5 shipped as a
  pinned DELIBERATE DIVERGENCE (`chat-composer.ts:774-786`, the
  P4.9L record). The divergence-record retirement is P4.D81's; this
  lane's record notes the convergence.
- **Dispositions (this lane's record rows, zero code):**
  - `54fdfb43` "re-emit job-child debug and trace logs at their own
    levels" — **NO-PORT** by the standing fork/IPC ruling
    (`w4.8-job-runner.md:14-21`, `[[job-runner-fork-ipc-non-port]]`):
    v5 has no job child and no log replay; `services/job_runner.rs`
    logs in-process at source levels already (`:398-455` maps v4's
    dispatcher lines one-for-one). Record with this evidence.
  - `55d63022` (CHANGELOG move) and `315e51d2` (filing bugs 66/67 —
    the very bugs this port reported) — docs-only, NO-PORT.
- **Help-doc deltas ride the bank:** `help/character-management.md`
  (+8) and `help/chat-settings.md` (+9) go VERBATIM to the `p4.9i2`
  bank, not into this lane.

## Survey-verified v5 starting points (2026-08-15)

- Enrichment twin: `crates/quilltap-core/src/services/
  chat_enrichment.rs` — `EnrichedCharacterDetail` at `:170-179`
  (**no `archived_at`** — the faithful gap), `get_character_detail`
  at `:241` with the avatar-override return inside it, projected into
  the chat GET at `api/salon.rs:206-219` (`assemble_chat_get`) and
  the chat PUT re-enrichment at `api/salon.rs:1513`. (Those `api/`
  call sites project from the struct — the field addition should flow
  without editing `salon.rs`; if a projection edit IS needed there,
  it is a one-hunk exception: record it in the lane record for the
  unifier, since no sibling owns `salon.rs` this round.)
- The OTHER projection already carries the key:
  `services/chat_participants.rs:334-338,444,474` (the v4
  `helpers.ts` twin, reached only from `chat_cast.rs:125`) — that
  asymmetry IS bug 66; leave it as is.
- The pinned beat (P4.D81 flips it):
  `apps/web/e2e/character-archive-flow.spec.ts:192-236` — asserts
  `.qt-badge-absent` count 1 and `Archived` count 0 with the ⚠
  "V4 BUG, REPRODUCED FAITHFULLY" header.
- Archive service: `services/character_archive/service.rs` — digest
  recorded from PLAINTEXT at `:947` (`create_archive_file_record`,
  doc `:935-940`); the rehydrate verify at `:565-574`
  (`ArchiveError::Verification`, guarded by
  `!file_row.sha256.is_empty()`); `sha256_hex` at `:1402`;
  re-encryption preserves the digest (`reencrypt.rs:158`);
  `ARCHIVE_CATEGORY` at `reencrypt.rs:31`. The survey confirmed **no
  v5 code path can clobber the digest row** (the only `files.sha256`
  writers are the same-name upload overwrite and generate_image) —
  the self-heal exists for rows damaged by pre-4.9 v4 on shared
  instances.
- Archive differential: the committed
  `character-archive-{main,mount}.db` family +
  `archive_tier2` / rehydrate arms (P4.D65/P4.D66 era; 8-case
  service differential). The clobbered-row and corrupt-bundle cases
  should be **in-test planted mutations on fixture COPIES** (flip
  the files row's sha256 in the copy) — do NOT regenerate or extend
  the committed fixture (`sweep-recipes-write-into-the-repo`,
  committed-fixture rule).

## Deliverables

### Tier 1 — must land

1. **`archived_at` on `EnrichedCharacterDetail`** + both
   `get_character_detail` returns (`character.archivedAt ?? null`
   semantics, wire key `archivedAt` per Contract C), v4's two
   comments carried. Differential: the archive-fixture chat GET arm —
   drive v4's REAL `getCharacterDetail` (jest real-DB oracle over the
   committed archive fixture with an archived participant) and diff
   the enrichment projection including the new key on both return
   paths (an avatar-override case included).
2. **The rehydrate self-heal arm**: exact v4 semantics per the
   classification (storedDigest comparison, warning sentence
   byte-exact, repair-failure tolerance, warnings PREPENDED).
   Differential arms on fixture copies: (a) clobbered row (recorded
   digest = ciphertext digest) → rehydrate SUCCEEDS, row repaired,
   the warning first in `warnings`; (b) genuinely corrupt (recorded
   digest = neither) → the unchanged refusal sentence; (c) the happy
   path unchanged. Drive v4's REAL `restoreArchiveBundle` on the
   same planted copies for (a) and (b) — both sides must agree on
   all three. Mutation-prove (a): break the repair write, watch the
   row-repaired assert go red while the rehydrate still passes.
3. **The disposition rows** for `54fdfb43`/`55d63022`/`315e51d2`
   with the cited evidence, in the lane record.
4. **The bug-69 bank**: `digest-policy.ts` + the watcher/
   reconciliation arms banked verbatim (survey text + file pointers)
   beside the P4.D62 bug-53 bank, with a loud note that whenever the
   file-storage watcher subsystem ports, the digest policy is part
   of its Tier 1.

### Tier 2 — should land

5. A tolerance note + unit pin: the self-heal's `files.update` repair
   uses the existing repo write; assert it does NOT touch `size`
   (v4 repairs `sha256` alone here).
6. The stale-lore check on `service.rs:949-954` (the v4-inherited
   watcher-race comment): annotate it as v4 lore (one comment line),
   don't delete it — it documents why the row is minted before the
   bytes land, which is still true.

### Tier 3 — explicit loud deferrals

- The watcher/reconciliation subsystem itself: stays unported
  (standing refusal at `api/files.rs:1640-1648`); this lane only
  grows its bank.
- The beat flip + all record retirements for bugs 66/67: P4.D81 and
  the unifier (Contract C).
- 💸 A live rehydrate of a REAL clobbered row (if the Friday copy
  has one) joins the owed dogfood queue — record, don't attempt
  here.

## The differential requirement, restated

Both code deliverables ship with equivalence tests driving v4's REAL
code: the enrichment projection via a jest real-DB oracle
(`jest-real-db-oracle`) over the committed archive fixture, and the
self-heal via v4's real `restoreArchiveBundle` on planted fixture
copies. Recipes in the family `//!` headers, runnable; regen only
against the pinned `aa464abf` worktree; grep NDJSONs for a fresh
marker (`oracle-regen-silent-stale-pass`).

## Fixtures

- Consumes: `character-archive-{main,mount}.db` (committed) —
  READ-ONLY; all planted conditions are in-test mutations on COPIES.
- Delivers: no committed-fixture changes.
- If the archive family's oracle NDJSONs regenerate, this lane owns
  re-running the family by name.

## Shared contract (BINDING — identical across P4.D78, P4.D79, P4.D80, P4.D81, P4.48)

**Contract A — the profile-parameters bag keys (D78 ↔ D79 ↔ D81).**
- Key **`num_ctx`**: INJECTED host-side by P4.D79's widened
  `profile_params` (OLLAMA profiles only, from `maxContext` when it is
  a finite number > 0; an explicit non-null `num_ctx` already in the
  stored blob wins). READ wire-side by P4.D78's `build_ollama_body`:
  number|string coerced (`Number`), finite and > 0, floored →
  `options.num_ctx`; anything else leaves the key off the wire.
- Key **`enable_thinking`**: WRITTEN by the SPA checkbox (P4.D81) as
  a boolean into the profile `parameters` blob; READ wire-side
  (P4.D78) as `value === true || value === "true"` → the top-level
  `think` request field (always present, `false` when off).
- P4.D78 never touches `cheap_llm.rs`, `services/**`, `db/**`,
  `api/**`; P4.D79 never touches `model/**` or `provider_manifest/**`.

**Contract B — the `multiCharacterPrefill` wire (D79 ↔ D81).**
- Read shapes: `multiCharacterPrefill?: boolean | null` on the
  connection-profile DTO (stored value verbatim; `null` = never
  chosen).
- Create bag: field optional; absent → the server stores
  `defaultMultiCharacterPrefill(provider)` (false iff
  `provider.toUpperCase() == "ANTHROPIC"`, true otherwise, true when
  provider is absent); present non-boolean (INCLUDING explicit
  `null`) → 400 with body message exactly
  `multiCharacterPrefill must be a boolean`.
- Update bag: absent → unchanged; boolean → stored; present `null`
  or non-boolean → the same 400. Server-side this REQUIRES the
  double-Option carry (`Option<Option<…>>`, the P4.D57 shape) so an
  explicit null cannot collapse to absent at the web edge (the Taboo
  §3 lesson).
- The SPA's TS twin `defaultMultiCharacterPrefill` has parity-spec
  semantics identical to v4's `lib/llm/multi-character-prefill.ts`.

**Contract C — chat-GET `archivedAt` (D80 ↔ D81).**
- The chat GET (and the chat PUT re-enrichment) character-detail
  projection gains `archivedAt: string | null` (wire key
  `archivedAt`), on BOTH `get_character_detail` return paths (main +
  avatar-override). The SPA contract field already exists
  (`core-contract.ts:2607-2612`).
- The archived-badge e2e beat flip lands in P4.D81 gated behind
  `P4D80_ENRICHMENT_LANDED = false`, flipped at unification; the
  unifier RUNS the beat at first activation (the gated-beat
  first-run rot class).

**Ownership (no two lanes edit the same source file).**
- P4.D78: `crates/quilltap-core/src/model/**`,
  `crates/quilltap-core/src/provider_manifest/**`,
  `harness/oracle/providers/**`,
  `harness/oracle/fixtures/streams/**`, its harness test files
  (stream/request/registry/listing/think-parser families).
- P4.D79: `crates/quilltap-core/src/cheap_llm.rs`,
  `db/connection_profiles.rs`, `api/settings.rs`, these
  `services/` files: `message_context.rs`, `initial_greeting.rs`,
  `chat_create.rs`, `regenerate_swipe.rs`, `file_fallback.rs`,
  `announcer/character_voiced.rs`, `answer_confirmation.rs`,
  `orchestrator.rs`, `cheap_llm_exec.rs`, a new
  `multi_character_prefill.rs`, `provisioning/**` (fresh_schema
  re-dump), `qtap_export/**` (schema-key-order regen), a new
  `db/` ensure-repair module + its one wiring call in
  `quilltap-host/src/host.rs`; its harness families.
- P4.D80: `services/chat_enrichment.rs`,
  `services/character_archive/service.rs`; its harness arms.
- P4.D81: `apps/web/**` ONLY.
- P4.48: `services/qtap_import/**` (mod.rs, entities.rs,
  preview.rs); its harness arms. **Tripwire:** P4.D79's import/restore
  carry of the new column rides the `CpCreate` deserialize
  (`p4.d73-composer-settings-columns-lane` — a `*Create` struct is
  ALSO restore's deserialize); if measurement shows P4.D79 needs an
  edit inside `qtap_import/**`, it STOPS that unit and records the
  ordered edit for the unifier instead of touching P4.48's files.
- `CHANGELOG.md` + `docs/developer/porting/status-log.md`:
  append-only for everyone.
- Version bumps: each lane bumps the crates it touches per
  `.claude/commands/commit.md`; the unifier accumulates (a
  `Cargo.toml` conflict is not version-only — audit deps).

## Verification gate (before declaring done)

- `cargo fmt --check`; clippy BOTH feature sets;
  `cargo build --release`.
- Full workspace tests with this lane's oracle env vars; the lane's
  families by name, zero SKIP, over oracles regenerated fresh at the
  `aa464abf` pin (positively confirm each RAN —
  `cargo-test-workspace-is-fail-fast`).
- No SPA changes → confirm `apps/web` untouched in the diff.
- Spelling guard rides the harness.
- Per-commit rule: follow `.claude/commands/commit.md`.
