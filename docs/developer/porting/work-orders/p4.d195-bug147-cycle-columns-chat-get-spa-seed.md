# Work order P4.D195 — bug 147 whole: the chat GET projects the cycle's two turn columns (`spokenThisCycleParticipantIds` + `cycleOrderParticipantIds`, RAW JSON strings, `'[]'` never `undefined`, after `activeTypingParticipantId` and before `isPaused`) with the P4.D171 "v4 never projects it" pin RETIRED red-first; the SPA's dormant chat-GET seed goes LIVE and grows its spoken half (v4's `calculateTurnStateFromHistory` parse — the sidebar's `spoken` status becomes reachable in v5 for the first time); the `state.cycleOrder` client read MEASURED as a convergence; the `help/chat-turn-manager.md` re-vendor — v4 `1fefadb9a`'s `handlers/get.ts` + `useTurnManagement.ts` + `help/` hunks, from `main`

**Status: CLOSED at lane close (2026-09-16), branch
`claude/cycle-columns-chat-spa-seed-a082a0`, four commits.** Ordered
2026-09-16, the `1fefadb9a` bug-147 drift catch-up + maintenance round
(P4.D195 ∥ P4.91 ∥ P4.92 ∥ P4.93). The unifier rewrites this header at
unification. Full record: `status-log.md` → "P4.D195 — bug 147 whole".

**Every Tier-1 and Tier-2 item landed; nothing is OPEN under this order.**

Refuted or corrected by measurement:

- **The survey's "NOTHING plants `spokenThisCycleParticipantIds`" is WRONG** —
  the committed fixture already carries a non-empty spoken set on BOTH chats,
  so that non-default arm came free. The two arms that were genuinely missing
  (a raw `''` and a raw SQL NULL) landed over a new `setCycleColumnsRaw` plant.
- **The `get`-case KEY-ORDER pin the order asks for ALREADY EXISTS** (P4.D183,
  over every `get_`-prefixed case). M1 proved it does the job rather than
  adding a second one.
- **`chatDetail()` carried NEITHER cycle key**, so the P4.D177 §3 spec's
  `delete` was a no-op on an absent key. It now carries both at `'[]'` and
  that arm is load-bearing.
- **Tier 2 item 8's measurement picked corpus rows** — and opened a real blind
  spot doing it: NOTHING in `select_speaker_equivalence` or
  `turn_state_equivalence` had ever driven the rotation argument (both Rust
  call sites hard-coded `&[]` / `None`). Both corpora now carry it.
- **Mutation M2 SURVIVED** (`Value::Null` for the default) because
  `db::chats_read` coerces first, so the projection's `?? '[]'` is
  unreachable. Kept for v4 fidelity, and the REASON is now executable: the
  null arm asserts the reader's guarantee directly.

⚠ **Found, NOT this lane's, recorded for the unifier:**
`salon_mutations_equivalence` fails nine writing `chat_update` cases with
`sqlite error: no such column: generationKey` — the committed salon fixture
pair predates P4.D182's `files.generationKey`. **Proven pre-existing** (the
identical nine fail with `main`'s untouched `api/salon.rs`), hidden since the
`78b381a96` round behind a deliberately withheld env var. Both that family and
the fixture pair are outside this lane's ownership.

## Preamble

- **v4 oracle baseline: `2075242f9`.** This lane absorbs the WHOLE code
  drift of **`1fefadb9a`** ("fix(salon): send the cycle rotation to the
  client so it stops guessing the turn (bug 147)", 2026-09-16 06:57, v4
  `4.10.0-dev.41`) — the drift ledger's ONE §3 row, `ORDERED(P4.D195)`:
  `app/api/v1/chats/[id]/handlers/get.ts` (+12 — two whitelist keys),
  `app/salon/[id]/hooks/useTurnManagement.ts` (+13/−1 — the
  `TurnActionResponse.state.cycleOrder` declaration + spread),
  `help/chat-turn-manager.md` (+1/−1), and the two test files you use as
  data: `__tests__/unit/app/api/v1/chats/[id]/handlers/get.test.ts` (+41,
  two cases) and the NEW `__tests__/unit/lib/chat/turn-manager/client-
  server-agreement.test.ts` (+156). NO-PORT (ratify on the `--name-status`
  list in your lane record): `README.md`, `docs/**` (the bug-147 filing +
  the amended bug-146 filing + `bugs.md` + `CHANGELOG.md`), the three
  version markers, and the eighth `floor-seat.test.ts` case (ALREADY in
  v5's corpus + parity spec since the `2075242f9` unification wires).
  Spec reads: `git -C ~/source/quilltap-server show 1fefadb9a --
  'app/api/v1/chats/[id]/handlers/get.ts' 'app/salon/[id]/hooks/
  useTurnManagement.ts' help/chat-turn-manager.md` and the post-commit
  files (`git show 1fefadb9a:<path>`) — **never the commit message**
  (§R.4). ⚠ The message says the client "stops guessing the turn"; the
  hunks say the SERVER adds two keys and the CLIENT declares one more
  field on a response it already spreads. v5's client never guessed — it
  asks the server (`chatTurnAction query`) — so the port is the projection
  + the seed's spoken half, NOT a client recompute (§Survey/v5).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** TWO lane-unique pins:
  **`/tmp/qt-v4-pin-p4d195-1fefadb9a`** (the TARGET — every family this
  lane MOVES: `salon_reads_equivalence`, the `help_*` content families,
  the new/grown turn-manager rows) and **`/tmp/qt-v4-pin-p4d195-
  2075242f9`** (the BASELINE — neutrality legs). Pin verification for the
  target: the fresh `salon-reads` NDJSON's `get_*` bodies carry
  `"cycleOrderParticipantIds"` (`grep -c` > 0); a baseline-pinned regen
  carries NONE.
- **Sibling lanes:** P4.91 (the photos-predicate consolidation + the collapse heal's census log shape), P4.92 (the tool loops' `base_params` carry-over class), P4.93 (the harness + logging maintenance smalls). None touches `api/salon.rs`, the
  salon fixture pair, the `salon-reads` oracle, `help/**`, or the SPA. The
  SPA is YOURS ALONE this round.
- **Worktree:** `git worktree add <path> -b claude/p4-d195-bug147-cycle-
  columns-<hash> main`; `CARGO_INCREMENTAL=0`; `npm ci` in `apps/web`
  (never `cp` a sibling's `node_modules` — `spa-worktree-node-modules`).
- **Read before the work:** `crates/quilltap-core/src/api/salon.rs:330-
  400` (`chat_get`'s projection — the P4.D171 NOTE block at `:365-381`
  you retire), `crates/quilltap-core/src/db/chats_read.rs:200-222` (the
  reader ALREADY coerces both columns NULL → `'[]'` — see Survey/v5);
  `crates/quilltap-harness/tests/salon_reads_equivalence.rs` (header
  recipe `:19-24`, `env_or_skip`, `norm`/`first_diff`, the
  `get_route_trail_and_cycle_order` plant `:485-505`, the `list_all`
  key-order pin `:520-540`) + `harness/oracle/cases/salon-reads.test.ts`
  (the case list `:300-412`, `setCycleOrder`, `runCase`); `apps/web/src/
  app/core/core-contract.ts:3331-3341` (the chat DTO's
  `cycleOrderParticipantIds?: string` + its P4.D177 doc); `apps/web/src/
  app/chat/turn-order.ts:36-160` (`TurnState`, the DORMANT paragraph at
  `:47-58`, `parseCycleOrder` `:149`) and `:200-330`
  (`computePredictedTurnOrder` — `spokenSinceUserTurn` read at `:262`,
  the `spoken` bucket at step 6); `apps/web/src/app/screens/salon/salon-
  conversation.ts:2049-2110` (`_turnEffect` + `refreshTurn` +
  `applyTurnResponse`), `:3440-3470` (the turn tail's reconcile —
  `invalidateQueries(chatKeys.detail)` then `settleTranscriptAfterTurn`),
  the ~20 `invalidateQueries({ queryKey: chatKeys.detail… })` sites (every
  one now re-seeds the rotation — the interplay you measure); `salon-
  conversation.spec.ts:335-385` (the P4.D177 §3 spec that pins the
  dormant leg's NO-KEY arm); `apps/web/src/app/chat/sidebar/chat-
  sidebar.ts:530-545` (`computePredictedTurnOrder` over `turnState()`);
  `apps/web/e2e/salon-sidebar-flow.spec.ts:260-380` (the P4D172 rotation
  beat: fresh chat, reload, subsequence assertion — the recipe your new
  arm extends); memory notes `forcing-a-deterministic-turn-in-a-salon-
  e2e-beat`, `e2e-sends-into-a-shared-chat-trip-a-title-checkpoint`,
  `e2e-assertion-can-read-the-pre-click-state`, `a-guard-whose-other-
  conjuncts-are-false-is-untested`, `an-assertion-on-the-oracle-cannot-
  catch-a-v5-regression`, `serde-json-map-remove-is-swap-remove` (you
  INSERT into a `Map` at a POSITION — use the idiom the file already
  uses, never `remove`+`insert`).

## Mandate

Absorb bug 147 whole. Server: `api::salon::chat_get` projects
`spokenThisCycleParticipantIds` and `cycleOrderParticipantIds` as the RAW
JSON strings the row carries, `'[]'` for a null/absent column (v4's `??
'[]'`), placed exactly where v4 places them (after
`activeTypingParticipantId`, before `isPaused`), with the P4.D171
"deliberately NOT projecting" note replaced by one naming `1fefadb9a` — and
`salon_reads_equivalence` regenerated at the target pin FIRST so the
existing `get_*` cases go RED before the port (the P4.D171 both-directions
pin tripping as the ledger predicted), then green, with the non-default
arms (a planted spoken set, a planted `''`) and a `get`-case KEY-ORDER pin
added. SPA: the chat DTO gains `spokenThisCycleParticipantIds?: string`;
the `_turnEffect` seed — dormant since P4.D177 — goes LIVE and seeds BOTH
halves of the turn state from the row (`cycleOrder` via `parseCycleOrder`,
`spokenSinceUserTurn` via v4's `calculateTurnStateFromHistory` parse), so
the participant sidebar's `spoken` status — unreachable in v5 until now, as
it was in v4 — lights on real data; every DORMANT/"v4 never projects"
paragraph (four files) is rewritten to the new truth; the P4.D187 interplay
(a chat refetch mid-turn now re-seeds the rotation) is MEASURED and pinned;
the `TurnActionResponse.state.cycleOrder` read is measured as a convergence
(v5 has read it since P4.D177). `help/chat-turn-manager.md` byte-copied
from the target pin (124 stays 124). This lane does NOT touch the server's
turn handlers, `core-contract.ts` beyond the one key + its doc, or any
sibling's files.

## Survey-verified starting points (fresh survey 2026-09-16 at `1fefadb9a` and on `main` `6e0ba5e8` — do not re-derive)

### v4 (at `1fefadb9a`)

- **`handlers/get.ts:362-373`** — inside the field-by-field `chat` object,
  immediately after `activeTypingParticipantId: chatMetadata.
  activeTypingParticipantId ?? null,` and before `isPaused: chatMetadata.
  isPaused ?? false,`: a ten-line comment, then
  `spokenThisCycleParticipantIds: chatMetadata.spokenThisCycleParticipantIds
  ?? '[]',` then `cycleOrderParticipantIds: chatMetadata.
  cycleOrderParticipantIds ?? '[]',`. **Strings on purpose** ("re-encoding
  it here would put a second shape of the same fact on the wire"). JS
  nullish: `null`/`undefined` → `'[]'`; an EMPTY STRING `''` passes
  through as `''` (measure it on v4 — plant `''` in the fixture copy and
  read the oracle body).
- **`useTurnManagement.ts:40-49`** — `TurnActionResponse.state` gains
  `cycleOrder?: string[]` (doc: "The route has always sent it; the client
  used to declare only `queue` and dropped it"); **`:97-106`** —
  `applyServerResponse` spreads `...(response.state.cycleOrder ? {
  cycleOrder: response.state.cycleOrder } : {})` into `setTurnState`. JS
  truthiness: `[]` is TRUTHY (an empty rotation IS adopted); `null`/
  `undefined` keep the previous list.
- **`SalonView.tsx:790-832`** (UNCHANGED by the commit — the consumer):
  the turn-state effect calls `calculateTurnStateFromHistory({ messages,
  participants, userParticipantId, spokenThisCycleParticipantIds: chat?.
  spokenThisCycleParticipantIds, cycleOrderParticipantIds: chat?.
  cycleOrderParticipantIds })` then `selectNextSpeaker(...)` on every
  change of `messages` / participants / `chat?.lastTurnParticipantId` /
  the two columns.
- **`lib/chat/turn-manager/state.ts:39-79`** (UNCHANGED) —
  `calculateTurnStateFromHistory`: `state.cycleOrder = parseCycleOrder(
  cycleOrderParticipantIds)`; `if (spokenThisCycleParticipantIds) { try {
  parsed = JSON.parse(...); if (Array.isArray(parsed)) state.
  spokenSinceUserTurn = parsed.filter(typeof === 'string') } catch {} }`
  — falsy → `[]`; invalid JSON → `[]`; a non-array → `[]`; non-string
  elements dropped. **Identical semantics to `parseCycleOrder`** (measure
  once, then say so in the twin's doc).
- **`get.test.ts` (+41)** — two cases: `findById` answering
  `cycleOrderParticipantIds: '["part-2","part-1"]'` + `spokenThisCycle
  ParticipantIds: '["part-3"]'` → `body.chat.*` equal those strings; and
  the unset row → both `'[]'`.
- **`client-server-agreement.test.ts` (+156)** — the reported room (five
  seats: ABIGAIL llm talk 1, CHARLIE user, BARAKA/LEILANI/GEN314 llm talk
  0; `impersonating = [LEILANI]`; `spokenBefore = [ABIGAIL, BARAKA,
  GEN314]`, `orderBefore = [LEILANI, CHARLIE]`; history: an ABIGAIL
  assistant line + a USER line as LEILANI); `spokenAfter`/`orderAfter`
  from `computeSpokenThisCycleAfterMessage` / `computeCycleOrderAfter
  Message`; the server leg `selectNextSpeakerAfterUserMessage(participants,
  characters, LEILANI, spokenBefore, '[]', CHARLIE, impersonating,
  orderBefore)` → `CHARLIE`, `user_turn`; the client leg `calculateTurn
  StateFromHistory` + `selectNextSpeaker` → agrees with the columns (both
  pre- and post-post copies), and WITHOUT them answers `weighted_selection`
  → ABIGAIL (the bug, pinned) and re-seats a spoken seat.
- **`help/chat-turn-manager.md:189`** — the sidebar sentence gains "— the
  sidebar and the turn banner both read that one stored running order, so
  what they tell you is what the house will actually do, and not a second
  opinion arrived at independently."

### v5 (on `main` `6e0ba5e8`)

- **`api/salon.rs:330-400` `chat_get`** — the ONE chat-GET projection in
  the tree (measured: `activeTypingParticipantId` is projected nowhere else
  in `crates/*/src` outside `api/salon.rs:383`; the host/CLI/web transports
  pass the verb's payload through). Insert the two keys between the
  `activeTypingParticipantId` insert (`:382-385`) and `isPaused` (`:386`).
  The chat `Value` comes from `db/chats_read.rs`, whose reader ALREADY
  coerces both columns: `:210-213` and `:218-221` — `Value::String(row.get
  ::<_, Option<String>>(24|25)?.unwrap_or_else(empty_arr))` (NULL → `'[]'`,
  always present). So `get_v(key)` is always `Some(String)`; write the
  projection as v4's `?? '[]'` anyway (`.filter(|v| !v.is_null()).unwrap_or
  (json!("[]"))` — the idiom `impersonatingParticipantIds` uses at `:359`)
  so a reader change can never reintroduce `undefined`, and note that the
  `''` arm reaches the wire as `''` on both sides.
- **`salon_reads_equivalence.rs`** — 16 oracle cases, 5 of kind `get`
  (`get_*`); every `get` body is diffed after `norm` (key-sorted), so the
  two NEW v4 keys make every `get_*` case RED at the target pin (the
  designed trip). The `get_route_trail_and_cycle_order` case ALREADY plants
  a rotation (`["b1…02","b1…01"]` on the `solo` chat) on BOTH sides
  (`:495-500`; oracle `setCycleOrder` `:407-410`) — it becomes the
  non-default arm for `cycleOrderParticipantIds` for free. NOTHING plants
  `spokenThisCycleParticipantIds` — add a `setSpokenThisCycle` plant (both
  sides, the `setCycleOrder` idiom) and a `''` plant. The `list_all`
  key-order pin (`check_key_order` via `key_paths`, `:520-540`) covers the
  LIST body only — add the same over ONE `get` case so the two keys' wire
  POSITION (after `activeTypingParticipantId`, before `isPaused`) is
  pinned, not just their presence (`norm` sorts keys away).
- **`core-contract.ts:3331-3341`** — `activeTypingParticipantId?`,
  `impersonatingParticipantIds?`, then `cycleOrderParticipantIds?: string`
  with a P4.D177 doc ("RAW off the row … parse with `parseCycleOrder`").
  Add `spokenThisCycleParticipantIds?: string` beside it (declared
  optional as v4's `types.ts:245,247` does — the server ALWAYS sends both
  from this round; the optionality is the legacy-server arm).
- **`turn-order.ts`** — `TurnState.spokenSinceUserTurn` (`:38`), the
  `cycleOrder` doc paragraph (`:47-58`) that says the chat-GET leg "is
  DORMANT: P4.D171 measured that v4's chat GET never projects the key";
  `parseCycleOrder(raw)` (`:149-158`) — falsy → `[]`, invalid → `[]`,
  non-array → `[]`, non-string elements dropped: the SAME rule v4's spoken
  parse applies. `computePredictedTurnOrder` reads `spokenSinceUserTurn`
  at `:262` (excludes spoken seats from "still to come") and buckets the
  rest as `'spoken'` at step 6 (`:301-311`). **v5 has NEVER seeded
  `spokenSinceUserTurn`** (`createInitialTurnState` `[]`; `applyTurnResponse`'s
  doc `:2095`: "never refreshed client-side") — so the `spoken` status
  has been unreachable in v5 exactly as v4's filing says it was in v4.
- **`salon-conversation.ts:2049-2072` `_turnEffect`** — the seed is gated
  on `chat.cycleOrderParticipantIds !== undefined` and sets ONLY
  `cycleOrder`; the doc paragraph names the dormancy and the §3 catch (an
  unconditional seed wiped the rotation on every `busy()` flip). `:2098-
  2110` `applyTurnResponse` — `cycleOrder: body.state?.cycleOrder ??
  prev.cycleOrder` (v4's truthy spread and v5's `??` agree on `[]`/`null`/
  `undefined` — MEASURE and record the three-row table). The sidebar
  binding `[turnState]="turnState()"` at `:331`; `chat-sidebar.ts:534`
  feeds it to `computePredictedTurnOrder`.
- **The refetch surface** (the interplay to measure): the turn tail
  `:3449` (`invalidateQueries(chatKeys.detail)` then
  `settleTranscriptAfterTurn`), the realtime `chats` hint (P4.D123's
  conditional read), and ~18 more `invalidateQueries(chatKeys.detail…)`
  sites. Every one now re-seeds BOTH halves from the row. The
  `applyTurnResponse` write and the row are the SAME persisted fact after
  `?action=turn`'s resolve-and-persist, so the only divergence window is
  a chat GET issued BEFORE a turn action's write and resolved AFTER it —
  v4 has the identical shape (its effect depends on the same two chat
  fields). Record the measurement; do not invent a guard v4 lacks.
- **`salon-conversation.spec.ts:335-385`** — the P4.D177 §3 spec "keeps
  the rotation … when the chat GET carries no key" DELETES the key from
  the fixture; it stays valid as the legacy-server arm. The fixture
  factory `chatDetail()` evidently carries `cycleOrderParticipantIds`
  (the spec deletes it) — check what it carries and add
  `spokenThisCycleParticipantIds`.
- **`salon-sidebar-flow.spec.ts:260-380`** — the P4D172 rotation beat:
  builds a FRESH chat (the shared fixture's cycle is SPENT — every seat in
  `spokenThisCycleParticipantIds` — so `query` re-draws), reloads, asserts
  the rotation is a subsequence of the displayed cast. Your new arm rides
  the same fresh chat.
- **Core twins already present** (the agreement test's server leg has
  Rust homes): `turn_state.rs:200` `calculate_turn_state_from_history`,
  `:213` `…_with_cycle`, `:324` `compute_spoken_this_cycle_after_message`,
  `:354` `compute_cycle_order_after_message`; `select_speaker.rs:400`
  `select_next_speaker_after_user_message`; `cycle_order.rs:49`
  `parse_cycle_order`. Families: `turn_state_equivalence`,
  `select_speaker_equivalence`, `cycle_order_equivalence`,
  `skip_signal_equivalence`.
- **`help/chat-turn-manager.md`** at the repo root is the vendored tree
  (124 files; `help_tree_embed_guard.rs:22-36` pins the count and names
  every re-vendor — add this one to its doc comment; `quilltap-host`'s
  `build.rs` embeds the tree, so the embedded table moves with the file).

## Tiered deliverables

### Tier 1 — must land

1. **Red-first.** Build the target pin; regenerate `salon-reads` from it
   through the sweep driver (`recipe_sweep.py --run salon_reads_equivalence
   --v4 "$PIN_TARGET" --v5w <ABS>`); run the family against UNCHANGED v5:
   record which cases red (expect all 5 `get_*`) and the `first_diff` line
   (the missing keys). `grep -c '"cycleOrderParticipantIds"' /tmp/oracle-
   salon-reads.ndjson` > 0 is your pin verification.
2. **The projection** in `chat_get` (Survey/v5): two inserts at v4's
   position, v4's `?? '[]'` shape, a comment carrying v4's why (the
   client recomputes from these two columns; strings on purpose; `'[]'`
   never `undefined`) AND the P4.D171 history in one sentence ("v4 did not
   project these until `1fefadb9a`; the earlier NOTE measured that
   correctly and is retired here"). Delete the old NOTE block. Family
   green at the target pin.
3. **The corpus's non-default arms** (both sides, the `setCycleOrder`
   idiom): (a) `get_spoken_and_cycle_columns` — plant `spokenThisCycle
   ParticipantIds = '["b1…01"]'` + a rotation on a chat; (b)
   `get_cycle_columns_empty_string` — plant `''` in BOTH columns and read
   what v4 answers (`'' ?? '[]'` is `''` — pin whatever the oracle says).
   Case count 16 → 18; the header's count guard (if any) bumped. Plus the
   `get` KEY-ORDER pin: `check_key_order` over `get_spoken_and_cycle_
   columns` (positions pinned). Mutation proofs: M1 project the keys at
   the END of the object → the key-order pin red, every other `get` case
   still green (which is WHY the pin is needed); M2 project `Value::Null`
   for a `'[]'` column → (b)/default cases red; M3 drop the spoken key →
   every `get` red.
4. **SPA — the DTO + the live seed.** `core-contract.ts`: `spokenThisCycle
   ParticipantIds?: string` with a doc mirroring the `cycleOrder…` one
   (RAW off the row; parse with the twin). `turn-order.ts`: a
   `parseSpokenThisCycle(raw)` twin of v4's `state.ts:53-62` parse (or,
   if you measure it identical to `parseCycleOrder`, ONE function with
   two documented callers — say which in the doc); rewrite the `:47-58`
   DORMANT paragraph to the new truth (the chat GET is the row's live
   source from v4 `1fefadb9a`; `state.cycleOrder` on turn responses stays
   the between-refetch source). `salon-conversation.ts` `_turnEffect`: the
   seed reads BOTH keys and sets BOTH halves — `cycleOrder: parseCycleOrder
   (chat.cycleOrderParticipantIds)`, `spokenSinceUserTurn: parseSpoken
   ThisCycle(chat.spokenThisCycleParticipantIds)` — REPLACING (not
   merging) as `calculateTurnStateFromHistory` builds a fresh state;
   keep the presence gate for the legacy-server arm (a server that omits
   both keys must not wipe the turn response's rotation — the P4.D177 §3
   catch) and document that the gate is now the LEGACY arm, not the
   dormancy. Rewrite the effect's doc paragraph. Specs (`salon-
   conversation.spec.ts`): (a) a chat GET carrying both keys seeds both
   halves (assert `turnState().spokenSinceUserTurn` AND `cycleOrder`);
   (b) a REFETCH carrying a different rotation + spoken set re-seeds both
   (the interplay pin — the turn response set one list, the row's read
   wins); (c) the existing NO-KEY arm stays green (legacy server); (d)
   invalid JSON / non-array / non-string elements → `[]` / filtered
   (v4's parse, three rows). Mutation proofs: M4 seed only `cycleOrder`
   → (a) red on `spokenSinceUserTurn`; M5 merge instead of replace
   (`[...prev.spokenSinceUserTurn, ...parsed]`) → (b) red; M6 drop the
   presence gate → (c) red.
5. **The sidebar consequence, spec-pinned.** `chat-sidebar.spec.ts` (or
   `turn-order.spec.ts` if the sidebar spec has no turn-state fixture):
   a `turnState` whose `spokenSinceUserTurn` names a present LLM seat →
   that seat's entry is `'spoken'` and sorts AFTER the eligible seats (v4
   step 6). `turn-order.spec.ts:316-353` already pins the pure function;
   what is new is that PRODUCTION can now reach it — pin the WIRING: a
   `SalonConversation` render whose chat GET carries a spoken seat →
   the sidebar's `turnOrder()` marks it `'spoken'` (a component-level
   spec through the `[turnState]` binding, not the pure function).
6. **The e2e arm** — extend `salon-sidebar-flow.spec.ts`'s fresh-chat
   rotation beat (or a sibling beat in the same file; keep the file's
   serial shape): after the reload assertion, force ONE deterministic
   turn (impersonate an LLM seat and send as it — `forcing-a-
   deterministic-turn-in-a-salon-e2e-beat`), read the row's
   `spokenThisCycleParticipantIds` through `chatGet` (the server is the
   independent source), reload, and assert the speaking seat's card
   carries the `spoken` status (measure how `chat-sidebar.ts` renders a
   status — a class, a badge — and assert THAT; `collapsedPositionBadge
   Class(entry.status)` at `:640` is one render site). Mid-turn arm: the
   same mark appears WITHOUT a reload after the turn-boundary GET
   (`settleTranscriptAfterTurn`'s read) — assert it before reloading.
   Never send into the shared fixture chats. Gate it on NOTHING — the
   server half is in the same lane.
7. **`help/chat-turn-manager.md`** byte-copied from the TARGET pin
   (`cmp` against `git show 1fefadb9a:help/chat-turn-manager.md`); the
   `help_tree_embed_guard` doc comment gains the `1fefadb9a` line (124
   stays 124); every `help_*` content family that reads the tree
   regenerated at the target pin and run by name (the list is P4.D194's
   lane record in `status-log.md` — reuse it verbatim; do not re-derive).

### Tier 2 — should land

8. **The agreement test as a Rust parity arm.** v5's SPA has no client
   `selectNextSpeaker` (server-authoritative — the documented P4.D177
   mechanism divergence), so the CLIENT leg of `client-server-agreement.
   test.ts` is a NO-COUNTERPART (record it with that evidence). Its
   SERVER leg and its "client agrees given the columns" leg both have
   Rust homes (Survey/v5 — the twins): transcribe the room as unit tests
   over `calculate_turn_state_from_history_with_cycle` + `select_next_
   speaker` vs `select_next_speaker_after_user_message` (the two
   computations AGREE on CHARLIE/`user_turn`; without the columns the
   first answers `weighted_selection` → ABIGAIL). Put them where the
   twins' existing unit tests live; if a tsx oracle already drives these
   functions (`select_speaker_equivalence` / `turn_state_equivalence`),
   add the room as corpus rows at the target pin instead — measure which
   first and say which you did.
9. **The `state.cycleOrder` convergence, measured.** A three-row table in
   the lane record: `state.cycleOrder` = `[...]` / `[]` / absent (and
   `null` if the wire can carry it) → v4's truthy spread vs v5's `??` —
   expected identical; the `applyTurnResponse` doc gains "v4 `1fefadb9a`
   declared the field the route always sent; v5 has read it since
   P4.D177". Zero code change expected; if the table disagrees on a row,
   fix v5 to v4's shape and spec-pin the row.
10. **Neutrality legs** at the BASELINE pin, by name: `salon_mutations_
    equivalence`, `salon_swipe_generate_equivalence` (they read the same
    fixture pair and NEVER project the two keys — expect byte-identical
    across pins where deterministic; `regenerate-at-both-pins-and-cmp-
    is-not-universal`).

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **Bug 147's "Known residue"** — `turnState.queue` is never seeded from
  the row on load (v4 too: `calculateTurnStateFromHistory` takes no
  queue). v5 mirrors it (`applyTurnResponse` is the queue's only source).
  NO-PORT by fidelity; record it in the `_turnEffect` doc with the v4
  filing's name so the next v4 fix finds its site.
- **The client-side `selectNextSpeaker` recompute** stays unported — the
  P4.D177 mechanism divergence stands (v5 asks the server). Nothing new to
  defer; restate it where the DORMANT prose used to be, because the
  filing's "v5 status: Not yet assessed" row deserves an answer the next
  reader can find: v5 never reproduced bug 147's banner half (the banner
  is server-authoritative) but DID reproduce its sidebar half (the
  `spoken` status unreachable), closed here.
- No `core-contract.ts` change beyond the one key + doc; no server turn
  handler change (tripwire: `git diff --stat main...HEAD -- crates/
  quilltap-core/src/api/turn*.rs crates/quilltap-core/src/select_speaker.
  rs crates/quilltap-core/src/turn_state.rs` — EMPTY, unless Tier 2 item
  8 adds `#[cfg(test)]` rows to `select_speaker.rs`/`turn_state.rs`, in
  which case the production zone is proven unmoved by
  `git diff` showing only `mod tests` hunks).

## The differential requirement, restated

The projection ships with `salon_reads_equivalence` regenerated at the
TARGET pin (v4's REAL `handleGet` over the committed salon fixture) run
RED against unported v5 first, then GREEN, with the two non-default arms
and the `get` key-order pin (closest recipe: the family's own header, run
through `recipe_sweep.py --run salon_reads_equivalence`; the plant idiom
is `setCycleOrder` on the oracle side + the `UPDATE "chats"` in the Rust
test). The SPA seed ships with component specs (the P4.D177 §3 spec's
file) asserting BOTH halves and the refetch re-seed, the sidebar wiring
pinned at component level, and the live e2e arm whose independent source
is the server's `chatGet`. The help re-vendor ships with the content
families at the target. Tier 2 item 8 lands as unit tests transcribed from
v4's suite (the P4.D58 parity-spec idiom) or as corpus rows in an existing
tier-1 family — whichever the measurement picks.

## Fixtures

- **Consumes:** the committed `crates/quilltap-web/tests/fixtures/salon-
  {main,mount}.db` pair (READ ONLY — plants are per-case UPDATEs on a
  fresh copy, both sides; the committed files never change) +
  `harness/oracle/fixtures/salon.json`. The e2e arm builds its own chat.
- **Delivers:** nothing new on disk beyond `help/chat-turn-manager.md`.
- **Owns regenerating:** `salon_reads_equivalence` (target); the `help_*`
  content families (target); Tier 2 item 10's two neutrality legs
  (baseline). No sibling reads the salon pair this round.

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `1fefadb9a` bug-147 drift catch-up + maintenance round:
**P4.D195 ∥ P4.91 ∥ P4.92 ∥ P4.93**, ordered 2026-09-16. Four lanes, four
worktrees, four lane branches, ALL from `main` (no stacking — every lane's
ownership is disjoint by file, so nothing needs a substrate commit). The
unifier cherry-picks in the order P4.D195 → P4.91 → P4.92 → P4.93 (the drift
row first; the harness-wide role-mapper sweep and the env-var rename last,
because they touch the most files and change the gate's env block), gates
ONCE, and `merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`2075242f9`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`1fefadb9a`**
  (`4.10.0-dev.41`, 2026-09-16 06:57) — ONE commit past the baseline, the
  bug-147 code commit, the ledger's one §3 row, now `ORDERED(P4.D195)`.
  v4 `bugfix` tip `1a2b2164c` and `release` tip `8fbf2afe0` are unmoved;
  there is still no `release: 4.10.0` squash. The checkout sat on `main`,
  CLEAN, at the planning probe (2026-09-16; the §2 probe PASSED, so the
  ledger stood and nothing was re-derived). **At unification the baseline
  MOVES to `1fefadb9a`** and the row retires ABSORBED(P4.D195).
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log 1fefadb9a..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Any mismatch — a moved HEAD, a dirty `lib/`/`app/`/`packages/`
  /`plugins/`/`migrations/`/`help/` tree, the checkout off `main` — is a
  STOP: report it and do nothing else.** A lane never writes the ledger.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1). HEAD is past the
  baseline and the commit touches `app/` and `help/`. Every regen runs
  from a LANE-UNIQUE detached worktree per ledger §5.1 (`git -C ~/source/
  quilltap-server worktree add --detach /tmp/qt-v4-pin-<order>-<sha>
  <sha>`, the THREE symlink classes, `cd` into it; never stash or
  checkout-switch the human's repo; `git worktree list` before each batch;
  verify the pin by grepping the fresh NDJSON for a marker only one tree
  has; check the file is non-empty before believing a diff). Which sha:
  - **P4.D195 pins the TARGET `1fefadb9a`** for every family its port
    MOVES (`salon_reads_equivalence`, the `help_*` content families, any
    turn-manager rows it adds) and the BASELINE `2075242f9` for its
    neutrality legs;
  - **P4.91, P4.92 and P4.93 pin the BASELINE `2075242f9`** — their v4
    surfaces are untouched by `1fefadb9a` (each lane records `git -C
    ~/source/quilltap-server diff --stat 2075242f9..1fefadb9a -- <its v4
    paths>` EMPTY in its lane record before the first regen). A pin at
    the baseline is still REQUIRED, not optional: the checkout can go
    dirty mid-lane (ledger §5.1), and a bare-checkout regen is
    unrepeatable.
  The sweep driver is the sanctioned per-family path
  (`harness/tools/recipe_sweep.py --run <family> --v4 "$PIN" --v5w
  <ABSOLUTE worktree>`); never run two sweeps concurrently; a family that
  reports `SKIP` in 0.00 s did not run (`a-family-env-var-is-not-its-
  regen-var`). Ledger §5.2's silent-stale-pass discipline applies to
  every regen (`rm -f` the fixture first; read the builder's last line;
  `grep -c` the changed bytes in the fresh NDJSON — MUST be > 0).
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). The
  spec for every item is `git show <sha> -- <path>` plus `git show
  <sha>:<path>`; a commit message describes the bug as its author
  understood it and the hunks are often narrower. Re-verify even where
  this order already flags the trap — the order's paragraph is the same
  kind of prose. Carry a v5-side code comment naming the sha where the
  prose misleads.
- **§R.5 — Convergence and designed trips: measure, don't assume**
  (ledger §5.4). The P4.D171 chat-GET omission pin (`salon_reads_
  equivalence`'s `get_*` cases) trips at the target pin BY DESIGN; dump
  v4's actual post-fix bodies before porting, and expect every `get`
  case to move together. The `state.cycleOrder` client read is a
  convergence measured with a table, not asserted.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest`
  is shared) and its silence leg; a fixture change lands with the regen
  of every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that
  SURVIVES as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`).
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed (and `apps/web/package.json` when it touches the SPA) on each
  commit per `.claude/commands/commit.md`; the unifier RECOUNTS as base +
  the sum of every lane's bumps (the silent-auto-merge trap: identical
  bumps merge as ONE — it fired again last round). Expected: core is
  bumped by ALL FOUR (P4.D195 `api/salon.rs`; P4.91 `photos/`, `db/`;
  P4.92 `services/orchestrator.rs`; P4.93 `model/completion.rs`, the two
  avatar job files, `db/memories.rs`); harness by ALL FOUR; web by P4.91
  ONLY (the `log_file.rs` visitor, Tier 2) — no other lane touches
  `crates/quilltap-web/src`; the SPA by P4.D195 ONLY; host/cli/tauri by
  NOBODY (a `help/**` re-vendor changes the embedded table `quilltap-
  host`'s `build.rs` produces but no host SOURCE — the P4.D194 precedent:
  no host bump for help alone).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  P4.D195 pre-lists the paths the target moves (with byte counts at the
  pin) in its lane record; no lane copies them.
- **§R.10 — Cross-lane handoffs, named:** (a) P4.93 RENAMES the guards
  family's fixture var (`QT_FIXTURE_HELP_MAIN` → a family-specific name)
  in `help_doc_sync_guards_equivalence.rs`; P4.D195 regenerates and RUNS
  the `help_*` content families under TODAY's names (it edits none of
  them) — the unifier re-runs both help-sync families with the renamed
  block after the pick. (b) P4.93's role-mapper sweep touches every
  hand-rolled `role` match in `crates/quilltap-harness/tests/` EXCEPT
  `orchestrator_tier3_equivalence.rs` (P4.92's file — its mapper already
  carries the `tool` arm); if P4.93 lands a shared inverse in
  `model/completion.rs`, the unifier repoints P4.92's mapper onto it at
  the wire (one line, recorded), never a lane. (c) P4.92 RUNS
  `chat_create_capstone` and `primary_stream_tier3` as neutrality legs
  while P4.93 EDITS their mappers — run vs edit is not a collision; the
  unified gate re-runs both. (d) P4.91 owns `db/avatar_rolls_collapse_
  heal.rs` WHOLE (the import repoint, the `:71-83` comment, and
  `report_census`); P4.93's avatar log lines live in `services/character_
  avatar_job.rs` + `services/image_job_common.rs` — different files.
  (e) P4.D195's Tier 2 item 8 may add `#[cfg(test)]` rows to `select_
  speaker.rs` / `turn_state.rs` or corpus rows to their families; no
  sibling touches either. (f) The `to_completion_messages`-class fix in
  P4.93 must leave every family's canned KEY bytes unchanged — a mapper
  that keys a role differently would silently miss; P4.93 proves each
  edited family green on its pin-fresh oracle by name.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D195** — bug 147 whole (server projection + SPA seed + help) | `crates/quilltap-core/src/api/salon.rs` — ONLY the `chat_get` projection block (`:355-390`); `crates/quilltap-harness/tests/salon_reads_equivalence.rs`; `harness/oracle/cases/salon-reads.test.ts`; `help/chat-turn-manager.md`; `crates/quilltap-harness/tests/help_tree_embed_guard.rs` — the doc-comment vendor list ONLY; `crates/quilltap-core/src/select_speaker.rs` + `turn_state.rs` — `#[cfg(test)]` modules ONLY (Tier 2 item 8), or instead `harness/oracle/cases/select-speaker*.ts` / `turn-state*.ts` + their two harness families (corpus rows only); `apps/web/src/app/core/core-contract.ts` — the ONE new key + its doc beside `cycleOrderParticipantIds`; `apps/web/src/app/chat/turn-order.ts` + `turn-order.spec.ts`; `apps/web/src/app/screens/salon/salon-conversation.ts` — ONLY `_turnEffect`, `refreshTurn`, `applyTurnResponse` and their doc comments; `salon-conversation.spec.ts`; `apps/web/src/app/chat/sidebar/chat-sidebar.spec.ts` (a wiring spec); `apps/web/e2e/salon-sidebar-flow.spec.ts`; `apps/web/package.json` version; this order's header | every other `crates/**` file (incl. the server turn handlers, `chats_read.rs`, `db/**`), the salon fixture PAIR on disk (plants are per-case copies), every other SPA file, `help/**` beyond the one page, the drift ledger |
| **P4.91** — the photos-predicate consolidation + the collapse heal's census log shape | `crates/quilltap-core/src/photos/photos_paths.rs`; `crates/quilltap-core/src/db/doc_mount_file_links.rs` — ONLY the deletion of the `is_photos_relative_path` twin + its private `posix_dirname` (`:332-388`); the SIX importers, import-line edits ONLY: `api/chat_media.rs:1716`, `photos/photo_link_summary.rs:13`, `photos/character_gallery_service.rs:17`, `services/maintenance.rs:32` + `:375` (and its `photos_relative_path_predicate` test if consolidated); `crates/quilltap-core/src/db/avatar_rolls_collapse_heal.rs` — the import + the `:71-83` justification comment + `report_census` (`:535-552`) + its tests; `crates/quilltap-web/src/log_file.rs` — the `FieldVisitor::put` JSON-field convention ONLY (Tier 2) + its tests; `harness/oracle/cases/photos-relative-path.ts`; `crates/quilltap-harness/tests/photos_relative_path_equivalence.rs`; this order's header | every other `crates/**` file (P4.93's avatar job files, `services/route_trail.rs`), `apps/web/**`, `help/**`, every other harness family, the drift ledger |
| **P4.92** — the loops' `base_params` carry-over class | `crates/quilltap-core/src/services/orchestrator.rs` — ONLY the strip after the `:2894` rebuild (or on the three `base_params:` clones at `:3002`/`:3080`/`:3125` — say which) + the `:2150` comment; `crates/quilltap-core/src/model/streaming_provider.rs` — the P4.41 doc block at `:339` ONLY (one sentence); `crates/quilltap-core/src/services/native_tool_loop.rs` + `text_tool_loop.rs` — `#[cfg(test)]` modules ONLY (Case B's unit pin); `crates/quilltap-harness/tests/orchestrator_tier3_equivalence.rs`; `harness/oracle/cases/orchestrator-tier3.test.ts`; `harness/oracle/fixtures/orchestrator-tier3.json` + `build-orchestrator-fixture.ts` + the fixture pair that builder produces; this order's header | every other `crates/**` file (the request builders, `primary_stream.rs`, `provider_failover.rs`), `apps/web/**`, `help/**`, every other harness family (P4.92 RUNS `chat_create_capstone` / `primary_stream_tier3` — never edits them), the drift ledger |
| **P4.93** — the harness + logging maintenance smalls | `crates/quilltap-harness/tests/help_doc_sync_guards_equivalence.rs` + `help_doc_sync_equivalence.rs` (headers + the env var + the fixture-sensitive arm) + `harness/oracle/cases/help-sync-guards.ts` (+ `help-sync-tier2.ts` only if the arm needs an oracle column); `crates/quilltap-core/src/model/completion.rs` — the `CompletionRole` inverse (`from_v4_wire`/`FromStr`) + tests; every hand-rolled role-mapper family in `crates/quilltap-harness/tests/` named in its survey (Class 1 + Class 2 + `external_prompt_tier3_equivalence.rs` + `file_attachment_tier3_equivalence.rs`) EXCEPT `orchestrator_tier3_equivalence.rs`; `crates/quilltap-core/src/db/memories.rs` — the ONE test's capture rig ONLY; `crates/quilltap-core/src/services/character_avatar_job.rs` + `services/image_job_common.rs` — the `[CharacterAvatar]` log lines + their capture tests ONLY; `crates/quilltap-harness/tests/avatar_job_tier3_equivalence.rs` — ONLY if a line-count pin must widen; `harness/tools/recipe_sweep.py` — ONLY if the env rename needs a driver-side alias (say so); this order's header | `orchestrator_tier3_equivalence.rs` (P4.92's), `salon_reads_equivalence.rs` + the help EMBED guard (P4.D195's), `db/avatar_rolls_collapse_heal.rs` (P4.91's), `photos/**`, `apps/web/**`, `help/**`, `crates/quilltap-web/src/**`, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0),
   and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp`) where the family is deterministic across
   regens; where it is not (`regenerate-at-both-pins-and-cmp-is-not-
   universal`), run by name and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast` with the lane's env block
   (its own oracle vars + `QT_V4_ROOT`/`QT_V4_CHECKOUT` at the right pin +
   `QT_NODE` = the node BINARY): report **binaries / passed / failed /
   ignored** from the per-binary lines and the exit code, and the lane's
   families confirmed RUN by non-zero duration. No family is withheld
   this round (P4.89 restored the brahma pair).
9. **SPA lanes only (P4.D195):** `npm run lint` (incl. the qt-class guard),
   `npm test` (spec files / tests / 0 failed), `npm run build` clean, then
   the lane's e2e beats by FILE against the release binary, and ONE full
   Playwright run at lane close (numbers recorded; the documented
   intermittents — the P4.D161 pause-toast beats, the P4.d17 quill beat,
   the `salon-documents-flow` / `workspace-flow` terminal trio — are
   named, re-run by file, and never re-classified as this lane's).
10. **Ownership check:** `git diff --stat main...HEAD` — every path is in
    the lane's Owns column; every MUST-NOT-TOUCH path absent.
11. **Per commit:** `.claude/commands/commit.md` in full — the equivalence
    test is non-negotiable (§1), the invariants (§2), real-instance safety
    (§3), the harness regen (§4), build/test/lint (§5), the CHANGELOG
    entry + version bumps (§7). Commit on the lane branch only; never
    merge to `main`; never `git stash`; never a worktree inside a worktree.
12. **Lane close:** update this order's status header (what landed, what
    was refuted by measurement, what stays OPEN with its named cause),
    append the lane record to `status-log.md` (the regen commands as run,
    the counts, the mutation table, every finding outside the lane's
    ownership for the unifier), and stop. The final gate runs in the
    FOREGROUND of the lane's last turn — a backgrounded gate whose
    notification arrives after the lane reports done is a lane that is
    NOT done (`a-backgrounded-sleep-does-not-gate-your-turn`).
