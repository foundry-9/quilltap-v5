# Work order P4.9b — the Generate Image screen (lane B): `/generate-image` + the standalone dialog + the shared profile picker

> **STATUS: CLOSED — UNIFIED on main (2026-07-18).** All five units
> landed (tier 1 + tier 2): the shared `images/image-profile-picker.ts`
> (+ `provider-icon.ts`, New-Chat consolidated onto it), the
> `/generate-image` screen, the restored homepage quick action (the
> `home.spec.ts:221` omission assert flipped), the standalone in-chat
> dialog + its single composer-gutter opener button, and the live
> Playwright walk (no real provider spend — the error arm is the beat's
> proof). Deferred loud: `sortByUserCharacter` (a future image-profiles
> server rider), the five extra generate params (the P4.6ai narrowing),
> validate-key/list-models (still refusal-armed), the rest of the gutter
> family + DnD (`p4.9e2`), ProviderIcon's plugin/override arms +
> `ProviderBadge`, the workspace-tab mount (`p4.9j`). Pure SPA — zero
> crate changes; SPA at the round union 0.5.169. The original order
> follows.
>
> Lane B of the FOUR-lane P4.9a ∥ P4.9c ∥ P4.9b ∥ P4.9d round (the M6
> backlog items 1–4 round, planned 2026-07-18). **v4 reference baseline:
> HEAD `d68638b4`** (4.8.0-dev.72, `~/source/quilltap-server`) — run
> `git log d68638b4..HEAD --oneline` at lane start; **if HEAD moved, STOP
> and report.** ⚠ The v4 working tree is DIRTY with the in-flight
> llm-consult feature; none of this lane's survey targets are dirty.
> This is a **pure-SPA lane** — it must NOT touch `crates/**` and adds NO
> wire surface (§3). Sibling lanes: `p4.9a-photos-view.md`,
> `p4.9c-about-profile.md`, `p4.9d-quick-hide-provider.md`. The **Shared
> contract** and **Ownership** sections below are binding and reproduced
> verbatim in all four orders. You run in a worktree (own Rust binaries
> built once for the e2e server — the P4.6af memory).
>
> **Survey correction to the M6 seed:** v4's `/generate-image` page does
> NOT use `StandaloneGenerateImageDialog` — it inlines its own form
> (`GenerateImageView.tsx`). The dialog is a separate **in-chat** modal
> whose only v4 opener is a `ComposerGutterTools` button — and the gutter
> family is `p4.9e2`'s scope. This order therefore ports the page (tier
> 1) and the dialog + its single opener button (tier 2), leaving the rest
> of the gutter loudly with `p4.9e2`.

## Mandate

Deliver v4's standalone image-generation surface: the `/generate-image`
route + screen (profile picker, prompt with `{{placeholder}}` inserts +
character search, 1–4 count, in-memory results gallery with
download/preview), the shared `ImageProfilePicker` component, the
un-omission of the homepage **Generate Image** quick action (flipping the
pinned assert at `home.spec.ts:221`), and (tier 2) the in-chat
`StandaloneGenerateImageDialog` with its single composer-gutter opener
button. Everything runs over EXISTING live verbs — `imageProfileList`,
`imageProfileGenerate` (its locked four params), `charactersList`,
`chatAddToolResult`. This lane must not touch `crates/**`,
`shell/shell.ts`, `screens/home/**` other than `quick-actions-row.ts` +
`home.spec.ts`, or any sibling-owned file. v4's strings carry verbatim
(Wodehouse register for anything new).

## Survey-verified starting points (2026-07-18 @ `d68638b4` — do not re-derive)

### The v4 page (`app/generate-image/`)

- `page.tsx:11-12`: `redirectToWorkspaceTab('generate-image')`
  (workspace glue, don't port) then `<GenerateImageView/>`.
- `GenerateImageView.tsx` (13.9 KB, self-contained):
  - Header + back-to-home `Link href="/"` (`:172-176`).
  - `ImageProfilePicker` with `value`/`onChange` ONLY (`:186-189`) — no
    character-sort params on this screen. **No auto-select of the default
    profile** — `selectedProfileId` starts null (`:37`); the picker's
    first option is "No image generation" (empty value); `[Default]` is a
    visual label only. The user must pick — carry this.
  - Prompt textarea (`:198-204`) with the `{{CharacterName}}` hint; a
    character-search insert widget (`:207-256`): filter input over
    `allEntities`, hardcoded quick-inserts `{{me}}` (`:222`) and
    `{{char}}` (`:230`), dropdown capped at 10 (`:241`),
    click-outside close (`:99`).
  - Count `<select>` 1–4 (`:265-275`); Generate disabled unless
    `selectedProfileId && prompt.trim()` (`:280-299`).
  - Results gallery grid (`:305-345`): per-image download (`:321`) +
    preview (`:328`) overlays, revised-prompt tooltip (`:336-339`);
    preview via `ImageModal` (`:349-354`); empty state (`:358-364`).
  - **Gallery is in-memory `useState` only** (`:41`) — lost on
    navigation; NO localStorage anywhere in this flow. Carry that.
  - Download = client-side `fetch(image.filepath)` → blob → anchor
    (`:151-166`). There is no server "save" from this screen; generated
    images auto-persist server-side (the Lantern Backgrounds `tool/`
    mount) with NO album association and, without a chatId, NO
    notification.
  - Data: `GET /api/v1/characters` (`:49-52`) for the insert list;
    `POST /api/v1/image-profiles/{id}?action=generate` body
    `{prompt, count}` — **no chatId** (`:120-124`); reads `data.data`
    (`:134`), "No images were generated" fallback (`:140`); errors via
    toasts reading `data.error || data.message` (`:129`).
  - **The user-facing param surface is prompt + count. Nothing else.**
    `size`/`quality`/`style`/`aspectRatio`/`negativePrompt` exist only in
    the API schema and profile defaults (merged server-side) — v5's
    four-param `imageProfileGenerate` narrowing covers this screen
    exactly. No cost display anywhere.

### The v4 dialogs + picker

- `components/chat/StandaloneGenerateImageDialog.tsx` (tier 2): props
  `:20-26` (`isOpen,onClose,chatId,participants,onImagesGenerated?`);
  `ImageProfilePicker` for explicit profile choice (`:157-161`);
  `useEntitySearch`/`EntitySearchDropdown` for all characters
  (`:41,:199-208`) + participant quick buttons (`:172-196`); count 1–4
  (`:238-249`); POSTs `{prompt, chatId, count}` (`:78-88`); on success
  toasts "…attached to next message" (`:100`), reads `expandedPrompt`
  (`:102`), fires `onImagesGenerated` (`:104-109`). Its mount
  (`app/salon/[id]/components/ChatModals.tsx:269-`) wires
  `onImagesGenerated` → `POST /api/v1/chats/{chatId}?action=add-tool-result`
  with `tool:'generate_image'`, `initiatedBy:'user'`, prompt + image
  `{id, filename}` (`:273`), then refetch.
- **The opener chain** (the only one in v4):
  `useModalState.ts:39,:79` (`openStandaloneGenerateImage`) →
  `SalonView.tsx:1530` → `ChatComposer.tsx:75,:129,:349` →
  `components/chat/ComposerGutterTools.tsx:16,:52` — a gutter button.
  Tier 2 ports THAT ONE BUTTON into the v5 composer as the dialog's
  opener; the rest of the gutter family stays `p4.9e2`'s (defer loud).
- Contrast (do not conflate): `components/chat/GenerateImageDialog.tsx`
  is the chat-profile-fixed dialog (injected `imageProfileId`, no picker,
  hardcoded `count: 1`) — ALREADY ported as v5
  `images/generate-image-dialog.ts`.
- `components/image-profiles/ImageProfilePicker.tsx`: props `:23-29`
  (`value,onChange,characterId,userCharacterId,disabled`); fetches
  `GET /api/v1/image-profiles` (+`?sortByCharacter=`/`?sortByUserCharacter=`
  when given, `:49-54`); reads `data.profiles` (`:60`); options
  `{name} ({modelName}) [Default] ⚠️ No API Key` (`:92-96`), first option
  "No image generation" (`:88`); selected-profile detail card with
  `ProviderIcon` (`:110-129`).

### The v5 targets

- `images/generate-image-dialog.ts` — the ported chat dialog; its
  docstring `:25-38` names this lane's exact deferrals (the standalone
  dialog + picker, the all-characters dropdown). Update those notes as
  they close (docstring edits only — the component behavior is P4.6ai's,
  leave it).
- `imageProfileGenerate` is LIVE end-to-end
  (`crates/quilltap-core/src/api/image_profiles.rs:581`, host-wired; the
  four params `imageProfileId/prompt/chatId/count` — `chatId` optional,
  count 1–4 fits). The five extra v4 params are pinned None
  (`image_profiles.rs:578-580,:599-607`) — a recorded narrowing; do NOT
  try to widen it (that would be Rust, not this lane).
- `imageProfileList` is live (`image_profiles.rs:148`) with
  `sortByCharacter` but **no `sortByUserCharacter`** — the v5 picker
  ports without the user-character sort (defer loud, §3).
- `imageProfileValidateKey`/`ListModels` are refusal-armed
  (`image_profiles.rs:663-668`) — STAY that way; the picker's
  "⚠️ No API Key" flag comes from the list payload, not from validate.
- An `image-profile-picker.ts` already exists under `screens/new-chat/`
  (dispatches `imageProfileList`, `:96-97`) — decide deliberately:
  generalize it into the shared `images/image-profile-picker.ts` v4-shape
  component (this lane owns both files) or build the shared one fresh and
  re-point new-chat; either way ONE picker implementation remains.
- The homepage pins the omission: `quick-actions-row.ts:14-16` (the
  OMITTED note) + `home.spec.ts:221` (the `not.toContain('Generate
  Image')` assert). Flip both: the quick action navigates to
  `/generate-image` per v4's `QuickActionsRow`.
- `ImageModal` reuse: the page's preview mounts the existing
  `images/image-modal.ts` — **import it, don't edit it** (the file is
  lane P4.9a's this round).

## Tiered deliverables

### Tier 1 — must land

1. **The shared `ImageProfilePicker`** (v4-faithful options/labels/detail
   card; `sortByCharacter` supported, `sortByUserCharacter` deferred
   loud), consolidated with the new-chat picker per above.
2. **The `/generate-image` screen** (`screens/generate-image/`): picker +
   prompt + insert widget (`{{me}}`/`{{char}}` + 10-cap character search
   over `charactersList`) + count select + generate over
   `imageProfileGenerate` (no chatId) + the in-memory results gallery
   with client-blob download and `ImageModal` preview + empty state +
   error toasts. Unit specs pin the transcribed logic (disabled-until-
   valid, insert-at-cursor, count bounds, no-auto-default-profile, the
   "No images were generated" arm) with v4 file:line comments.
3. **The quick-action un-omission**: `quick-actions-row.ts` gains v4's
   Generate Image action → `/generate-image`; `home.spec.ts:221` flips
   from asserting the omission to asserting the action navigates.
4. **Its §1 route block**, and a **live Playwright beat** in this lane's
   OWN new spec file: boot → unlock → Home shows Generate Image → click →
   `/generate-image` renders the picker + form → generate stays disabled
   until profile + prompt → (no real generation: the e2e host has no
   image provider — drive the error/refusal arm or stop at the disabled
   gate, per the P4.6ac beat pattern; never spend real provider money).

### Tier 2 — should land

5. **`StandaloneGenerateImageDialog`** as
   `images/standalone-generate-image-dialog.ts` (picker + participants
   quick buttons + all-characters search + count 1–4 + the
   `chatAddToolResult` wire with v4's exact tool-result payload), and
   **its single gutter opener button** in the v5 composer (v4
   `ComposerGutterTools` placement — port ONLY this button; the rest of
   the gutter family + DnD stays `p4.9e2`, say so loudly). A component
   spec pins the add-tool-result payload shape; a salon beat opens the
   dialog from the gutter button.

### Tier 3 — explicit deferrals (loud, named)

- **`sortByUserCharacter`** on the profile list (needs a Rust param —
  out of a pure-SPA lane; name it for the next server rider).
- **The five extra generate params** (`size`/`quality`/`style`/
  `aspectRatio`/`negativePrompt`) — the standing P4.6ai narrowing; no v4
  UI exposes them anyway.
- **`imageProfileValidateKey`/`ListModels`** — stay refusal-armed.
- **The rest of the composer gutter tools + DnD upload** — `p4.9e2`.
- **The workspace-tab mount** (`redirectToWorkspaceTab('generate-image')`)
  — `p4.9j`'s.

## The differential requirement, restated

The SPA has no v4 oracle (tier 4): the proof is `ng test` + the live
Playwright walk over the real server; the wire payloads it sends are
already pinned by the existing `imageProfileGenerate` (4-case) and
image-profiles differentials. Every transcription gets a unit spec
pinning the v4 behavior it copied, with the v4 file:line in a comment.

## Fixtures

- **Consumes:** the committed e2e locked instance (image profiles are
  seeded — `image_profiles` joined the e2e userId rewrite in P4.6ao; the
  characters seed serves the insert list). No fixture changes; no oracle
  regeneration owed.
- **Delivers:** none.

## Shared contract (BINDING — identical in all four orders)

### §1 — the `app.routes.ts` route blocks

`app.routes.ts` is contract-managed this round (three lanes add routes).
Each lane inserts ONLY its own block(s), immediately BEFORE the
`path: ''` home entry (wildcard stays last), matching the file's lazy
`loadComponent` idiom; update the file's doc comment for your own
route(s) only. The unifier unions the blocks in this order and resolves
the known additive conflicts; no lane touches another lane's block.

- **P4.9a:** `photos` → `./screens/photos/photos-page` → `PhotosPage`
- **P4.9c:** `about` → `./screens/about/about-page` → `AboutPage`;
  `profile` → `./screens/profile/profile-page` → `ProfilePage`
- **P4.9b:** `generate-image` → `./screens/generate-image/generate-image-page`
  → `GenerateImagePage`
- **P4.9d:** no routes.

### §2 — the shell wires (unifier-applied)

- **(a) The photos nav flip.** `shell.ts` NAV_ITEMS `photos` keeps
  `route: null` in every lane; the UNIFIER flips it to `route: '/photos'`
  at unification (the route exists only then). Lane A does not edit
  `shell.ts`; lane C (its owner this round) does not flip it.
- **(b) The user-menu mount chain.** Lane C ships `qt-user-menu` (the
  shell-footer dropdown porting v4 `profile-menu.tsx` +
  `sidebar-footer.tsx:302,309`: quick-hide content ABOVE the
  Profile/About entries) with a pinned marker comment
  `<!-- §2b: qt-quick-hide-menu-section mounts here at unification -->`.
  Lane D ships `qt-quick-hide-menu-section` (standalone, self-contained
  over its own service). The unifier imports + mounts lane D's component
  at the marker. Lane D's menu-walk beats are ACTIVATE-AT-UNIFY.
  (Lane letters here: A = p4.9a, B = p4.9b, C = p4.9c, D = p4.9d.)

### §3 — the new wire verbs (fold-at-unify)

Each verb-adding lane keeps a local typed module + cast (the established
`home.api.ts` pattern); the unifier folds them into `CoreRequest` and
runs the name-for-name wire diff. Names + payloads are pinned:

**P4.9a:** `photoGalleryList` (v4 `GET /api/v1/photos`:
`{q?, tag?, limit?, offset?}` → `{entries, total, hasMore}`, entry shape
verbatim from `listUserGallery`); `photoGallerySave` (v4 POST:
`{fileId, caption?, tags?, chatId?}`; sha256 duplicate → v4's 400);
`photoGalleryEntryGet` (v4 `GET /api/v1/photos/{id}`);
`photoGalleryEntryRemove` (v4 DELETE — link removal + last-link GC);
tier 2 `imageInfoGet` (v4 `GET /api/v1/images/{id}`, the
`characterGalleryLinks` read). REST edges: `GET|POST /api/v1/photos`,
`GET|DELETE /api/v1/photos/{id}` (+ `GET /api/v1/images/{id}` tier 2).

**P4.9c:** `userProfileGet` (v4 `GET /api/v1/user/profile`, default
action ONLY — the `theme-preference` arms are already ported via
`theme.service`/chatSettings, do NOT re-port); `userProfileUpdate` (v4
PUT `{name, email}` incl. the email-uniqueness arm);
`userProfileSetAvatar` (v4 PATCH `?action=set-avatar` `{imageId}`,
category IMAGE/AVATAR validation, stores `image = /api/v1/files/{imageId}`);
`systemDataDir` (v4 `GET /api/v1/system/data-dir`; the `?action=open`
POST is REFUSAL-ARMED loud). Health `version`: the quilltap-web HTTP
`/health` body gains `version` (already-threaded `CARGO_PKG_VERSION`),
and `interpretHealth`/`HealthStatus` (core-transport.ts) gain ONLY an
optional `version` field — no other signature moves; both transports then
agree (the Tauri health command already carries the engine `HealthDto`
version). REST edges: `GET|PUT|PATCH /api/v1/user/profile`,
`GET /api/v1/system/data-dir`.

**P4.9b / P4.9d:** NO new wire surface. P4.9b consumes only
`imageProfileList`, `imageProfileGenerate` (its locked four params),
`charactersList`, `chatAddToolResult`. P4.9d consumes only
`TagList`/`TagCreate`/`TagUpdate`/`TagDelete` and existing payload
fields (`isDangerousChat`, tags). `imageProfileValidateKey`/`ListModels`
STAY refusal-armed; `sortByUserCharacter` on the image-profile list stays
unported (deferred loud in P4.9b).

### §4 — the quick-hide localStorage keys (P4.9d only)

`quilltap.quickHide.activeTags` (JSON id array),
`quilltap.quickHide.hideDangerous` (`'true'`/`'false'`),
`quilltap.quickHide.includeAutonomousRooms` (`'true'`/`'false'` — already
shared with `autonomous-visibility.ts`; one service owns all three after
this round). `clearAllHidden` must NOT reset `includeAutonomousRooms`
(v4 provider `:179-180`). No other lane reads or writes these keys.

## Ownership (BINDING — identical in all four orders)

- **P4.9a:** `crates/quilltap-core/src/photos/user_gallery_service.rs`
  (new) + `crates/quilltap-core/src/api/photos.rs` (new, or the images
  read's natural home), `crates/quilltap-web/src/photos_routes.rs` (new);
  harness: its oracle cases, differential suites, the
  `photos-{main,mount}.db` family + generator. SPA:
  `apps/web/src/app/screens/photos/**` (new),
  `apps/web/src/app/images/**` EXCEPT `generate-image-dialog.ts` and the
  new P4.9b files (P4.9a owns `photo-gallery-modal.ts`, `image-modal.ts`,
  the new deep-modal files, `image-urls.ts` if needed),
  `screens/characters/view/tabs/gallery-tab.ts`, its §1 block, its own
  e2e spec file. Version bumps: quilltap-core, quilltap-harness,
  quilltap-web (+Cargo.lock), SPA `package.json`.
- **P4.9c:** Rust: the user-profile + data-dir dispatch/service files
  (new), `crates/quilltap-web/src/profile_routes.rs` (new),
  `crates/quilltap-web/src/health.rs` (the version field — exclusively);
  harness: its oracle cases + differential suites (+ fixture additions if
  the existing families lack what profile needs). SPA:
  `apps/web/src/app/screens/about/**`, `screens/profile/**` (new),
  `apps/web/src/app/shell/user-menu.ts` (new) and `shell/shell.ts`
  (user-menu mount + the F4 stale-docstring fix at `shell.ts:19-20`; NOT
  the photos flip), `apps/web/src/app/core/core-transport.ts` (the §3
  version carry — exclusively), its §1 blocks, its own e2e spec file.
  Version bumps: quilltap-core, quilltap-harness, quilltap-web
  (+Cargo.lock), SPA (+quilltap-host ONLY if data-dir needs a host seam —
  flag it loudly if so).
- **P4.9b:** SPA only: `apps/web/src/app/screens/generate-image/**`
  (new), `apps/web/src/app/images/standalone-generate-image-dialog.ts`
  (new), `apps/web/src/app/images/image-profile-picker.ts` (new shared)
  and/or `screens/new-chat/image-profile-picker.ts` (generalize),
  `images/generate-image-dialog.ts` (docstring/deferral-note edits only),
  `screens/salon/salon-conversation.ts` + the composer file it must touch
  for the single gutter opener button, `screens/home/quick-actions-row.ts`,
  `screens/home/home.spec.ts` (exclusively — no other lane edits it),
  its §1 block, its own e2e spec file. Version bump: SPA `package.json`.
- **P4.9d:** SPA only: `apps/web/src/app/quick-hide/**` (new: service +
  menu section + specs), `screens/salon/salon-list.ts` (+ refactoring
  `screens/salon/autonomous-visibility.ts` onto the shared service),
  `screens/home/recent-chats-section.ts`,
  `screens/home/characters-section.ts`,
  `screens/characters/list/characters-list.ts`,
  `screens/characters/view/character-detail.ts` + its conversations tab
  file, the Prospero chats/characters card files,
  `screens/settings/appearance/**` (the global tags tab + its
  quickHide column + ThemePreviewModal — RE-BINNED here from p4.9c
  because this lane owns the appearance tab; the unifier records the
  re-bin in `m6-screen-parity.md`), its own e2e spec file(s). MUST NOT
  touch `salon-conversation.ts` (v4 `SalonView.tsx:707`'s in-chat
  quick-hide consumer feeds the unported `p4.9e*` dialog family — defer
  loudly) or `home.spec.ts`. Version bump: SPA `package.json`.
- **The round's KNOWN Rust conflict set:** P4.9a and P4.9c both add
  ADDITIVE arms to `crates/quilltap-core/src/api/types.rs`,
  `api/engine.rs`, `api/mod.rs`, and `crates/quilltap-web/src/lib.rs`
  (route registration). Additions only, §3-pinned; the unifier reconciles
  (temp-branch cherry-pick + one full gate — the parallel-round
  reconciliation memory). No other Rust file is shared.
- **Append-only for everyone:** `docs/CHANGELOG.md`,
  `docs/developer/porting/status-log.md` (union-merge at unification).
  SPA version bumps union at unify — the unifier RECOUNTS (identical
  bumps merge silently — the P4.6aw/ax memory).
- **Nobody** edits `CLAUDE.md`, `phase-4.md`, `m6-screen-parity.md`, or a
  sibling lane's files; the unifier updates the phase docs.

## Verification gate (run before declaring done)

- Rust untouched: `cargo test --workspace` should match main's baseline
  (run it once to prove no accidental drift); fmt/clippy clean.
- `ng test` green (from `apps/web`); `ng build` clean.
- Full Playwright green, run ALONE on port 4319 from `apps/web` with THIS
  worktree's Rust server binaries; the generate-image beat live; the
  flipped home assert green.
- No `[value]` binding on any `<select>` with async options — bind
  `[selected]` per option (the standing dogfood-#6 rule; the profile
  picker IS such a select — bind `[selected]` per option from day one).
- No real image-provider spend anywhere in tests or beats.
- Every commit follows `.claude/commands/commit.md` (SPA version bump;
  CHANGELOG entry; status-log append at lane close).
