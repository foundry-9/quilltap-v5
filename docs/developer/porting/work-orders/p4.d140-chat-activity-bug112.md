# Work order P4.D140 — chat activity dates by when a character last spoke (v4 `735d9408c`, bug 112)

**Status:** OPEN.

## Preamble

- **v4 oracle baseline: `7fb668263`** (drift-ledger §1, probe re-run
  2026-09-01). **This lane's target commit is `735d9408c`** — bug 112:
  `lastMessageAt` redefined to character-authored activity. A v4-filed
  human report (NOT a convergence); its v5-status column read "Not
  investigated. The shape applies" — and the survey confirms v5 measurably
  carries the same defect (the single write gate bumps on ANY message row,
  and every sort falls back to `updatedAt`). **It lands on the P4.64/P4.65
  home + Salon-list surfaces this port just rebuilt** — treat those two
  round records as required reading.
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 checkout
  on `main`, tree clean, `git log 4622411fd..main` empty and
  `git log 3a76b17df..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED** (ledger §1). Every oracle/fixture regen runs
  from a lane-unique detached worktree pinned at **`735d9408c`**
  (`/tmp/qt-v4-pin-p4d140-735d9408c`, three symlink classes, ledger §5.1)
  — that tree contains the LoRA commits `84f33ce94`/`648d5c8aa` but NOT
  `2ece98c90`/`60e3c4a0a`; none of this lane's families touch image or
  danger surfaces, so attribution holds. Families this lane does not move
  regen at the baseline `7fb668263` if needed.
- **Sibling lanes:** P4.D138/P4.D139 (LoRA), P4.D141 (Concierge — shares
  NOTHING server-side with this lane; v4's concierge hunk to
  chat-enrichment was type-only and v5's `concierge_override` is already
  `Option<String>`, so `chat_enrichment.rs` is exclusively THIS lane's),
  P4.D142, P4.66. §Ownership/§Shared contracts below binding, identical.
- **Worktree:** per the `carryout` skill. `CHANGELOG`/`status-log.md`
  append-only. **No Playwright** (P4.66 owns port 4319) — this lane's
  display changes are spec-pinned; any beat it wants is authored gated.

## Mandate

Port bug 112 whole: the `chat_activity` chokepoint module (predicate + SQL
mirror + `chatActivityAt`/`chatActivityTime`/`byChatActivityDesc`), the
write-gate split and delete-recompute in the messages repo, the widened
`get_last_played_message_at` predicate, every reader routed through the
chokepoint (Salon/home sort, characters action=chats hand-rolled block
DELETED, projects, brahma + its `createdAt`, self-inventory's
prompt-visible line), the restore re-derivation with v4's warning
sentence, the ai-import predicate filter, the recompute boot heal over
v4's `migrations_state` ledger (P4.D97 precedent shape), and the SPA
display flips to `lastMessageAt ?? createdAt`. Must NOT touch: the
Courier external-reply write site (`services/courier_transport.rs:627` —
v4 deliberately left its third write path ungated; do not "fix" it),
`services/maintenance.rs`'s `updatedAt` staleness fallback (v4 left it),
the projects home-stats `ms_of` path (`home.rs:270` — v4 did not change
it), and any sibling-owned file.

## Survey-verified starting points (2026-09-01, from the shipped hunks — ledger §5.3)

1. **The chokepoint — v4 `lib/chat/chat-activity.ts` (NEW, 90 lines), five
   exports.** `isCharacterAuthoredMessage`: `type === 'message'` AND role
   `USER|ASSISTANT` AND JS-truthy-absent `systemSender` AND
   `customAnnouncer` — **whispers count by omission** (no
   `targetParticipantIds` test); carry the module header + the
   included/excluded doc lists verbatim (the Staff-announcement rationale
   is the bug). `CHARACTER_AUTHORED_MESSAGE_FILTER` — the SQL mirror
   (`type='message' AND role IN ('USER','ASSISTANT') AND systemSender IS
   NULL AND customAnnouncer IS NULL`). ⚠ **The two spellings are not
   exactly equivalent** (truthiness excludes `''`, `IS NULL` includes it)
   — v4 ships both knowingly; mirror BOTH spellings, do not unify (or
   unify only with a recorded decision). `chatActivityAt` =
   `lastMessageAt ?? createdAt` — **never `updatedAt`** (carry the
   fallback doc). `chatActivityTime` — epoch ms, **NaN → 0 so comparators
   stay total** (pinned by v4's test). `byChatActivityDesc` — descending,
   ties resolved by sort stability only. → NEW v5 module (suggested
   `quilltap-core/src/chat_activity.rs`).
2. **Write sites — v5 collapsed v4's two into ONE:**
   `db/chats_messages.rs` `update_chat_metadata` `:530-560` currently
   bumps `last_message_at` + `updated_at` together on `has_actual`. Split:
   `last_message_at` moves only when
   `events.iter().any(is_character_authored_message)` (batch stamped with
   the single minted `now`, as v4's `.some()` is), `updated_at` keeps
   `has_actual`. Rewrite the module doc `:12` and the `:532-534` comment
   (v4's replacement comments are in the hunks). **Delete recompute:**
   `delete_messages_by_ids` `:470-497` — the `removed > 0` arm gains
   `last_message_at: Some(get_last_played_message_at(...))` (can set
   NULL; `updatedAt` still deliberately not bumped — v4's carry the
   "walk backwards" comment). `clear_messages` `:503-517` already
   nulls — no change. **`get_last_played_message_at`**
   (`db/chats_messages_read.rs:288`): widen the SQL with
   `AND role IN ('USER','ASSISTANT') AND customAnnouncer IS NULL`; port
   v4's new doc comment. Its `maintenance.rs:242` consumer's `updatedAt`
   fallback stays (v4 left it).
3. **Readers.** `services/chat_enrichment.rs:768-779`
   `sort_chats_for_list` — swap the `.or_else` key `"updatedAt"` →
   `"createdAt"` (ONE edit covers Salon list AND home; keep the
   single-home comment, note the `:216-244` home ordering comment's
   "RAW chat field" claim stays true). `services/home.rs`: `RecentChat`
   `:52-62` gains `created_at` + `map_recent_chat` `:443-450` carries it
   (a WIRE change — the home family's key-order pin will red until the
   oracle regens; that is the tripwire working). `api/characters.rs`
   `:518-536` — DELETE the hand-rolled max-over-transcript block (v4
   deleted its twin) → `chat_activity_at(&chat)`; the sort `:539-544`
   keys off the new value; note the deleted block's `iso_from_unix_ms`
   round-trip disappears WITH it (v4's does too); rewrite the doc
   `:467-476`. `api/projects.rs:682-696` — swap both key closures (or
   reuse the shared comparator); check the projected object carries
   `createdAt` (it feeds the card). `api/brahma.rs:212-224` — swap
   `updatedAt` → `createdAt` in `effective`; the enriched row `:234-241`
   gains `"createdAt"` between `"title"` and `"updatedAt"` (v4's exact
   placement — key-order matters). `tools/self_inventory.rs:1287-1290` —
   drop the middle `.or_else(updatedAt)` (PROMPT-VISIBLE; the
   self-inventory differential diffs it).
4. **Restore + import.** `services/backup/restore/orchestrator.rs` —
   insert after the per-chat message replay loop closes (`:510`), inside
   the per-chat scope: re-derive via `get_last_played_message_at`, update
   the chat, failure degrades to the warning
   `Failed to restore last-activity date for chat "{title}": {e}` (exact
   sentence; own try, post-write, `updatedAt` preserved by omission —
   carry v4's whole comment block). ai-import twin (find v5's
   `assembleQtapExport` equivalent in the quilltap-import service): the
   last-row read becomes filter-through-the-predicate `.pop()?.createdAt
   ?? null` — **the empty case moves from `now` to `null`** (a behavior
   change beyond the filter; pin it).
5. **The recompute heal.** v4's migration
   `recompute-chat-last-message-at-v1` (169 lines): the drift query uses
   `IS NOT` (NULL counts as different — "the Staff-only chats that must
   be cleared are exactly the rows going to NULL"); rewrite in one
   transaction; `updatedAt` never written; `shouldRun` gates on tables +
   the six columns + drift>0 — **no-drift boots write NO ledger row**
   (retried every boot, cheaply). → v5 heal module in the P4.D97 shape
   (`db/thinking_prefill_retire_heal.rs` is the template: completed-check
   first, `NotApplicable` stamps nothing, lazy `migrations_state`/
   `migrations_metadata` creation, the INSERT shape at `:204`, outcome
   enum); call site in `quilltap-host/src/host.rs` boot repair chain
   (`:986-999` block). Match v4's no-row-on-no-drift semantics exactly —
   a v5 stamp on a clean boot would make a later v4 boot skip a migration
   it thinks it has run. v4's success messages verbatim (`Recomputed
   last-activity for N chat(s) (M with no character-authored messages)` /
   `All chat last-activity timestamps already reflect character-authored
   messages`); the prettify label is v4-only (no v5 runner UI) — record
   as a deliberate non-port.
6. **SPA.** `screens/salon/chat-card.ts:184-188` — currently `updatedAt`
   ONLY with a now-stale comment ("the Salon transform deliberately omits
   lastMessageAt" — FALSE: its input `EnrichedChatSummary` carries both
   `lastMessageAt` and `createdAt`, contract :2562-2570); one-line fix to
   `lastMessageAt ?? createdAt` + comment rewrite.
   `chat/merge-conversation-modal.ts:318` and
   `screens/home/recent-chat-item.ts:88` — `?? updatedAt` → `?? createdAt`
   (recent-chat-item needs the `RecentChat` DTO's new `createdAt`, §D).
   `screens/characters/view/tabs/character-conversation-card.ts:144` —
   same swap (DTO already carries `createdAt`).
   `screens/prospero/cards/project-chats-section.ts` reuses
   `<qt-chat-card>` directly — inherits the fix, NO local edit (v4 needed
   a transform edit; v5 does not — record it). `brahma/brahma-wire.ts` —
   check what the console dialog displays and mirror v4's `createdAt`
   carry. Specs to update: merge-conversation-modal, home, salon-list,
   character-conversation-card, conversations-tab, project-chats-section,
   brahma-console-dialog, quick-hide-consumers.
7. **The e2e fixture landmine (this lane's, even with no Playwright
   run).** `apps/web/e2e/support/seed-archived-character.ts:303-312`
   stamps a wall-clock `lastMessageAt` on a chat whose ONE seeded message
   has OLD `createdAt` — the boot heal will walk the date back and sink
   the chat out of the virtualized render window, breaking the courier
   beats' `openChatWith`. Fix by stamping the message's `createdAt =
   RECENT` too, and verify the copied template row's
   `systemSender`/`customAnnouncer` are NULL. Also review the harness
   scrubbers/fixtures the survey flags: `build-retention-caches-fixture.ts`
   `dressChat` pins post-bump values; `build-maintenance-sweep-fixture.ts:223`
   ("RECENT participant message") — the widened predicate can flip a
   fixture chat's staleness if its recent message is SYSTEM/TOOL-roled;
   `chat_admin_routes_equivalence.rs:524`'s "Host bubbles stamped from the
   wall clock" note describes exactly the behavior this port REMOVES —
   those minted-value normalizations need re-measuring, not assuming.
8. **v4's five test files** (chat-activity predicate table incl. the
   whisper and NaN pins; the write gates incl. the Staff-batch no-bump;
   the migration integration suite incl. idempotence; the renamed +
   two-added enrichment sorts; restore-field-fidelity's re-derivation)
   are the corpus shapes — drive v4's REAL code, not transcriptions.

## Tiered deliverables

- **Tier 1 (must land):** points 1–5 — the module, both DB sites, all six
  server readers, restore + import, the boot heal, each with its
  differential.
- **Tier 2 (should land):** point 6 (the SPA display flips + DTO carry)
  and point 7 (the fixture landmine + scrubber re-measures).
- **Tier 3 (explicit refusal-arm deferrals):** none planned. If the
  ai-import twin's surface turns out unported in v5 (verify before
  assuming), record the no-counterpart with evidence instead of building
  it.

## The differential requirement (restated)

Every unit drives v4's REAL code from the `735d9408c` pin. Families this
lane moves (survey-verified paths):

- NEW tier-1 family for the chokepoint (predicate/activity/comparator) —
  oracle imports the real `lib/chat/chat-activity.ts`; corpus = v4's test
  table + the truthiness-vs-IS-NULL edge (`''` systemSender) pinned in
  whichever direction is measured.
- `chats_messages_tier2` + `chats_messages_ops_tier2` (write gates; the
  ops family's delete case currently asserts `messageCount` only — grow
  it to see the recompute; note its `NORMALIZATION SPEC: NONE`).
  Red-first: a Staff-announcement add must show v5 bumping where v4
  doesn't BEFORE the fix.
- `salon_reads` (sort), `home_routes_equivalence` (⚠ lives in
  **quilltap-web**, has the exact `mutate_null_last_message` case whose
  header comment "falls back to updatedAt" becomes FALSE — fix the
  comment, regen, and the `check_key_order` pin will require the fresh
  oracle once `createdAt` joins `RecentChat`), `characters_reads`
  (the only `action=chats` coverage), `projects_routes`,
  `brahma_console_routes`, `self_inventory`.
- Restore: `system_restore_equivalence` + `system_restore_state` — ⚠ the
  state family normalizes "an ISO timestamp that does not appear anywhere
  in the archive" as minted; post-fix, restored `lastMessageAt` values DO
  appear in the archive and stop being normalized — a normalization-class
  change on both sides; regen and re-measure rather than patching the
  normalizer blind.
- NEW heal family modeled on `thinking_prefill_heal_equivalence.rs`
  (ledger diff on `migrations_state` + the recomputed column; corpus from
  v4's integration suite: walk-back, whisper kept, already-correct
  untouched + nothing-to-do, never-stamped filled, mixed-instance
  idempotence).
- `maintenance_sweep_tier2`/`maintenance_ops_tier2` — re-run; if the
  widened predicate flips a fixture chat's staleness, that is a real
  finding to fix fixture-side with a note, not to normalize away.
- Changed-bytes greps per ledger §5.2 on every regenerated NDJSON
  (`createdAt` present in the home/brahma rows; the enrichment sort
  order actually moving on the null-lastMessageAt case).

## Fixtures

- Consumes: `crates/quilltap-web/tests/fixtures/home-{main,mount}.db` +
  `harness/oracle/fixtures/home-web.json` (pinned `lastMessageAt` rows at
  :316-428 may need re-pinning), the salon-reads fixture, the
  maintenance-sweep fixture, the committed restore archives.
- Delivers: the new chokepoint + heal family recipes (runnable headers),
  any re-pinned fixture JSONs, the repaired e2e seed helper.
- Regenerating a committed fixture ⇒ re-run every sibling family that
  reads it, by name, zero SKIP.

## Shared contracts (binding, identical across all six orders)

**§A — the LoRA wire (P4.D138 ↔ P4.D139).**
- `GET /api/v1/image-profiles?action=list-models` response gains
  `loraSupport: Record<modelId, ImageLoraSupport>` between `source` and the
  conditional `fetchError`; models resolving no support are ABSENT from the
  map (the editor's "offer no LoRA rows" signal).
- NEW `GET /api/v1/image-profiles?action=options-schema&provider=…[&model=…]`
  → `{ provider, model: model ?? null, optionsSchema: ProviderOptionsSchema
  | null, loraSupport: ImageLoraSupport | null }` — `null`, never a zero-cap
  object. 400s: `Provider is required`; `` Provider ${provider} is not
  available ``.
- NEW `POST /api/v1/image-profiles?action=lora-metadata`, body
  `{ source: string, hfToken?: string }` → HTTP **200** carrying the
  `HuggingFaceLookupResult` verbatim (`{ok:true,facts}` |
  `{ok:false,reason,repoId,url,detail?}`; reasons `not-a-repo-id |
  missing-or-private | not-found | rate-limited | timeout | network |
  http`). Guards in order, exact sentences: bad JSON or non-object body →
  400 ``A JSON body with a `source` is required``; blank/non-string source
  → 400 `A LoRA source is required`; non-string hfToken → 400
  `hfToken must be a string when supplied`. POST-not-GET because hfToken is
  a credential (carry the comment).
- The stored `loras` entry: `{ source: string (trimmed, min 1), scale?:
  number (finite, 0..10), triggerPhrase?: string, label?: string }`.
  Write-side rejection is the Zod envelope
  `{ error: 'Validation error', details: issues }`, HTTP 400 — never a
  bespoke sentence. No server-side cap.
- `ImageLoraSupport = { maxLoras: number, scale?: {min,max,default,step?},
  sourceKinds: ('url'|'hf-repo'|'provider-id')[],
  supportsPrivateWeightsToken?: boolean }`;
  `DEFAULT_LORA_SCALE = {min:0,max:2,default:1,step:0.05}`.

**§B — `qt-range` (P4.D139 ↔ P4.D142).** D142 defines `.qt-range`
(`_interactive.css`) + `--qt-range-accent`/`--qt-range-focus-ring`
(`_variables.css`). D139's new LoRA editor slider carries the literal
`class="qt-range w-full"` (v4 `LoraListEditor.tsx:145`) and ships no slider
CSS of its own. Landing order is irrelevant: the class is inert-until-both,
never broken, and the check-qt-classes guard does not police bare component
names.

**§C — the salon danger-styling flip (P4.D141 ↔ P4.66 → the unifier).** v4
`SalonView.tsx:1496` switched the message-list danger prop to
`shouldShowDangerStyling(chat)`; the v5 twin line is
`salon-conversation.ts:311`, a file P4.66 owns. Neither lane edits the
other's file: P4.D141 ships the client predicate module
`apps/web/src/app/chat/concierge-state.ts` (exports `getConciergeState`,
`shouldUseUncensoredRoute`, `shouldShowDangerStyling`, `isClassifierOnDuty`
— transcribed 1:1 from v4 `chat-override.ts` with a parity spec), and the
ONE-LINE binding flip at `salon-conversation.ts:311` is a NAMED UNIFICATION
WIRE (PENDING_CROSS_LANE, P4.D122 precedent) applied by the unifier with a
spec pin. Both P4.D141 and P4.66 record it loudly in their lane records.

**§D — `core-contract.ts` region scoping.** Three lanes touch it in
disjoint regions: P4.D139 adds the image-profile/LoRA/options-schema DTOs
(a new block, §A names verbatim), P4.D140 adds `createdAt` to the
`RecentChat` DTO, P4.D141 widens the two `conciergeOverride` unions
(:2574, :2891) to `'OFF' | 'UNCENSORED' | null`. No lane touches another's
region; the unifier diffs the contract name-for-name (playbook §D).

**§E — server same-file region split (P4.D138 ↔ P4.D141).**
`tools/generate_image.rs` and `services/story_background_job.rs` belong to
P4.D138 EXCEPT the dangerous-content predicate flips — one import line plus
one call each (`generate_image.rs:1337`, `story_background_job.rs:365`) —
which P4.D141 owns and must edit to keep its branch compiling. The unifier
cherry-picks **P4.D138 before P4.D141** and hand-resolves the import-block
unions; neither lane touches the other's regions in those two files.

## Ownership (binding, identical across all six orders)

| Area | Owner |
|---|---|
| `quilltap-core`: `image_gen.rs`, `image_gen_data.rs`, `model/image.rs`, `model/image_dialects.rs`, `api/image_profiles.rs`, `db/image_profiles.rs`, `services/image_job_common.rs`, `services/character_avatar_job.rs`, `services/image_scene_tasks.rs`, `api/wardrobe.rs` (preview region), the NEW lora/params/HF modules; `quilltap-host`: `avatar_preview.rs`; `tools/generate_image.rs` + `services/story_background_job.rs` per §E | **P4.D138** |
| SPA: `screens/settings/images/**` (modal, form, api, card, picker + specs), `screens/settings/providers/{provider-options-panel.ts,provider-options-schema.ts}` (+ specs), NEW LoRA editor/query components, `core-contract.ts` §D region | **P4.D139** |
| `quilltap-core`: NEW `chat_activity` module, `db/chats_messages.rs`, `db/chats_messages_read.rs`, `services/chat_enrichment.rs`, `services/home.rs`, `api/characters.rs`, `api/projects.rs`, `api/brahma.rs`, `tools/self_inventory.rs`, `services/backup/restore/orchestrator.rs`, NEW recompute-heal module (+ `db/mod.rs` line); `quilltap-host`: `host.rs` (the boot repair chain block); SPA: `screens/salon/chat-card.ts`, `chat/merge-conversation-modal.ts`, `screens/home/recent-chat-item.ts`, `screens/characters/view/tabs/character-conversation-card.ts`, `brahma/brahma-wire.ts`, `e2e/support/seed-archived-character.ts`, `core-contract.ts` §D region (+ the touched specs) | **P4.D140** |
| `quilltap-core`: `services/dangerous_content/**`, `services/concierge_notifications.rs`, `services/context_summary.rs`, `services/danger_scan.rs`, `services/orchestrator.rs`, `services/pre_compute.rs`, `services/title_update_job.rs`, `services/memory_extraction_job.rs`, `services/carina_memory_extraction.rs`, `pascal/llm_consult.rs`, `api/salon.rs` (the conciergeState arm), the §E flip lines in `tools/generate_image.rs` + `services/story_background_job.rs`; SPA: `chat/conversation-header.ts`, `chat/sidebar/chat-section.ts`, NEW `chat/concierge-state.ts`, `styles/qt-components/_chat.css`, `core-contract.ts` §D region (+ specs) | **P4.D141** |
| SPA: `styles/qt-components/{_interactive.css,_variables.css,_surfaces.css}`, the class-string edits in the eight slider host files (`chat/sidebar/participant-card.ts`, `memory/housekeeping-dialog.ts`, `memory/memory-editor.ts`, `screens/settings/chat/context-compression-settings.ts`, `screens/settings/chat/dangerous-content-settings.ts`, `screens/settings/memory/memory-dedup-card.ts`, `screens/settings/providers/profile-modal.ts`, `screens/settings/system/tasks-queue-card.ts`), `apps/web/scripts/check-qt-classes.mjs` (the host-class guard extension) + its spec/self-test | **P4.D142** |
| SPA: `screens/salon/salon-conversation.ts` (+ spec), `chat/message-list.ts` (+ spec), the NEW mid-turn e2e beat | **P4.66** |
| Playwright (port 4319) | **P4.66's branch only**; every other lane's beats are authored gated (ACTIVATE-AT-UNIFY) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it |

**Version bumps:** this lane bumps `quilltap-core`, `quilltap-harness`,
`quilltap-web`, `quilltap-host`, and the SPA per commit; the unifier
recounts at unification.

## Verification gate before declaring done

`cargo fmt --check`; `cargo clippy` both feature sets; full
`cargo test --workspace` with the lane's oracle env vars
(`run_in_background`, full log + sentinel — NEVER a pgrep loop, NEVER
`| tail`); the lane's families by name through the sweep driver, zero
SKIP, from the `735d9408c` pin with changed-bytes greps; mutation proofs
verified-applied (incl. one reverting the write-gate split — the
Staff-announcement row must redden); `npm test` + `npm run build` in
`apps/web`; NO Playwright. Per-commit rule: `.claude/commands/commit.md`.
