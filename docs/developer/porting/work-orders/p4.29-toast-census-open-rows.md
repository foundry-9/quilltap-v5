# Work order P4.29 — the toast census's OPEN rows (SPA)

> **STATUS: OPEN — not started.**
>
> **v4 baseline for this round: `40319484`** (v4 HEAD, 4.8.0-dev.147,
> 2026-08-03). The one drift commit is server-side (hard-link groups —
> P4.D41's) and touches NO v4 component this lane ports; the toast
> sentences read the same at `c4d4b0de` and `40319484`, so survey v4 at
> HEAD. Do NOT edit `CLAUDE.md`/`phase-4.md` on the branch.
>
> **Drift-check at lane start**: `git log 40319484..HEAD --oneline` +
> `git status --short` in `~/source/quilltap-server`. If new commits touch
> any `components/`/`app/` file in this lane's rows, STOP on that row and
> report; otherwise proceed and record the drift.
>
> **Siblings (same round):** P4.D41 (hard-link groups, Rust), P4.28
> (restore/backup remainder — OWNS `screens/settings/system/**`, including
> the four data-system toast rows: restore-dialog, backup-dialog,
> delete-data-card, auto-lock-provider — keep out), P4.30
> (roleplay-template rendering — owns `chat/render/**` +
> `salon-conversation.ts`). See "Shared contract + Ownership §X".
>
> **Worktree note:** own branch, own checkout; no `git stash`/`git worktree`.

## Mandate

Take the P4.25 toast census's remaining OPEN rows to zero (outside
`screens/settings/system/**`): every v5 screen that exists and still owes
v4 toasts gains them, sentence-for-sentence from v4's real strings at the
baseline. UNPORTED rows stay untouched (they belong to their future screen
lanes). This is a fidelity lane: no invented copy, no new inline surfaces,
v4's "inline AND toast" rows keep the inline.

**Must NOT touch:** `screens/settings/system/**` (P4.28's), `chat/render/**`
+ `screens/salon/salon-conversation.ts` (P4.30's), any Rust, any existing
e2e spec file. `api/types.rs` is FROZEN.

## Survey-verified starting points (2026-08-03)

The census of record is the P4.25 lane record in `status-log.md`
("THE CENSUS (all 106 v4 toast files, adjudicated)" + the unit-7 addendum
that closed seven rows: state-editor-modal, memory-editor, memory-list,
housekeeping-dialog, edit-enclave-modal, photo-gallery-modal,
data-directory-section). **First step: re-derive the LIVE OPEN list** —
grep the census table for OPEN, drop the unit-7 closures and P4.28's four
system rows, and verify each remaining row against the current code (the
stale-refusal-inventory lesson: rows go stale silently). As of this survey
the expected worklist, by v4 file (call count) → v5 site:

- `app/aurora/AuroraView.tsx` (9 of 10 remaining) →
  `screens/characters/list/*` — delete/favorite/controlled-by/Carina/
  import/reset arms.
- `app/aurora/[id]/edit/CharacterEditView.tsx` (5, BOTH — v4 `:300`
  renders inline AND toasts; keep the block) +
  `app/aurora/[id]/edit/hooks/useCharacterEdit.ts` (6: "Character saved
  successfully!", "Avatar updated!", "Avatar cleared!" + 3 failures) →
  `screens/characters/edit/character-edit.ts`.
- `app/aurora/[id]/view/components/DescriptionsTab.tsx` (5) →
  `screens/characters/view/tabs/details-tab.ts`.
- `app/aurora/[id]/view/hooks/useCharacterView.ts` (9 still OPEN:
  template replace/restore ×4, favorite/controlled-by/Carina ×3, NPC
  toggle ×2) → `character-detail` / `template-display`.
- `app/aurora/groups/[id]/GroupDetailView.tsx` (2) →
  `screens/groups/group-editor.ts`; `useGroupMembers.ts` (4) →
  `screens/groups/group-members-card.ts`.
- `app/aurora/new/NewCharacterView.tsx` (4, BOTH — v4 `:260` inline AND
  toasts) → `screens/characters/new/new-character.ts`.
- `app/prospero/[id]/hooks/useProjectDetail.ts` (20 — the single largest
  block) → `screens/prospero/project-detail.ts` + its nine cards;
  `useProjectChats.ts` (2) → `cards/project-chats-section.ts`.
- `app/salon/[id]/components/DocumentPane.tsx` (1 — "Document URL
  copied") → `documents/document-pane.ts`.
- `app/salon/[id]/terminal/[sessionId]/TerminalPopoutPageClient.tsx`
  (2 — the kill failures) → `screens/salon/terminal-popout.ts`.
- The files family: `FileBrowser.tsx` (13) → `screens/files/
  files-browser.ts`; `FilePreview/hooks/useFileActions.ts` (6) →
  `file-preview-modal.ts`; `CreateFolderModal.tsx` (3) →
  `create-folder-dialog.ts`; `MoveToProjectModal.tsx` (2) →
  `move-to-project-dialog.ts`; `useMountPointBlobUpload.ts` (2) →
  `screens/scriptorium/store-detail.ts` upload leg;
  `useProjectFileUpload.ts` (3) → `prospero/cards/project-files-card.ts`.
- `components/new-chat/hooks/useNewChat.ts` (9 — six validation refusals
  + two creation successes) → `screens/new-chat/*`.
- `components/providers/qtap-link-provider.tsx` (3) → the v5 `qtap://`
  link interceptor.
- `components/settings/api-keys/ApiKeyModal.tsx` (1 — note the 4000 ms
  duration) → `screens/settings/providers/api-key-modal.ts`;
  `connection-profiles/index.tsx` (3, auto-configure) →
  `connection-profiles-card.ts`;
  `chat-settings/DataRetentionSettings.tsx` (2) →
  `screens/settings/chat/data-retention-settings.ts`.
- The memory settings cards (`memory-backfill-card` 2,
  `memory-housekeeping-card` 4, `memory-recall-card` 2,
  `memory-regenerate-card` 2) → `screens/settings/memory/*`.
- `components/tags/tag-editor.tsx` (2) →
  `screens/characters/edit/tag-chip-editor.ts`.

The toast substrate is P4.25's (`--qt-status-*` tokens, the ported
`lib/toast` analogue) — use it as-is; if a needed affordance is missing
from the substrate, that is a finding, not a license to extend it ad hoc.

**The census-verdict trap (the round's own lesson, 2026-07-31):** two
CONVERTED-marked rows were later found missing v4's SUCCESS toasts —
verdicts need v4-sampled review. For every FILE this lane opens, re-check
ALL of v4's call sites in that file against v5 (not just the OPEN-listed
ones), and spot-check the CONVERTED verdict of any sibling file the same
v5 screen imports.

## Tiered deliverables

**Tier 1 (must land):** every row above verified-then-closed — v4's
sentences byte-for-byte (including punctuation and the odd duration
override), success AND failure arms, BOTH-rows keeping their inline
surface — each with a spec pin (`.spec.ts` asserting the toast fires with
v4's exact string on the success and failure paths; mock the transport,
don't invent server behavior). Update the census table rows in
`status-log.md` via an APPEND-ONLY addendum (never edit the original
table — union-merge rule).

**Tier 2 (should land):** one new e2e spec file
(`e2e/toast-open-rows-flow.spec.ts` or similar) walking 2–3
representative beats live (a character save, a project-detail action, a
files action). Keep it additive; do not edit existing specs.

**Tier 3 (explicit deferrals — loud, named):** UNPORTED census rows
(plugins, summon-from-lore, memory-dedup, rename/replace, salon-list
re-extract/re-render, capabilities report, conversation-summary
regenerate, import wizard v4-shape, migration notifier, chat-utils
delete) — each stays with its future screen lane; list them in the lane
record so the census stays honest.

## The differential requirement, restated

SPA-only: the oracle is v4's SOURCE at the baseline — port sentence
tables case-for-case and pin them in specs (the P4.25 shape). Where v4
has component tests covering a toast path, port those cases. No NDJSON
oracle applies; say so in the lane record rather than skipping silently.

## Fixtures

None consumed, none regenerated. The e2e instance is shared — any beat
must tolerate the standing seeded state and clean up after itself.

## Shared contract + Ownership (§X — binding, identical in all four orders)

- **P4.D41 owns:** `crates/quilltap-core/src/db/{doc_mount_file_links.rs,
  doc_mount_documents.rs, doc_mount_blobs.rs, database_store.rs,
  mount_index_case_repair.rs}`, `crates/quilltap-core/src/services/
  {mount_index/**, builtin_mounts.rs, qtap_export/**,
  quilltap_import/document_stores.rs, provisioning/fresh_schema.json}`,
  `crates/quilltap-core/src/tools/doc_edit/shared.rs`,
  `crates/quilltap-cli/**`, `crates/quilltap-harness/tests/
  mount_common/mod.rs` + its listed families, and their oracle cases.
- **P4.28 owns:** `crates/quilltap-core/src/services/{delete_all.rs,
  backup/**}`, `crates/quilltap-core/src/api/system_data.rs`,
  `crates/quilltap-core/src/db/mod.rs` (tolerant-insert helpers) +
  tolerance applications in db files outside P4.D41's list,
  `crates/quilltap-host/src/job_pump.rs` (+ wiring),
  `apps/web/src/app/screens/settings/system/**`, the system/backup/
  restore harness families, `system-data-*` + `restore-archives/**`,
  and `e2e/zzz-restore-destructive.spec.ts` + data-system specs.
- **P4.29 owns:** the census OPEN-row files under `apps/web/src/app/`
  EXCLUDING `screens/settings/system/**`, plus ONE new e2e spec file of
  its own. It edits no existing spec.
- **P4.30 owns:** `apps/web/src/app/chat/render/**`,
  `apps/web/src/app/screens/salon/salon-conversation.ts`, the message
  list/row components it must thread through, its markdown parity
  fixtures, plus ONE new e2e spec file of its own.
- `docs/CHANGELOG.md` + `docs/developer/porting/status-log.md` are
  append-only for everyone. `api/types.rs` is FROZEN this round.
- **Oracle vintage split:** P4.D41 regenerates at `40319484`; P4.28 pins
  at `c4d4b0de`; the unifier regenerates the union at `40319484`.
- **Version bumps:** P4.D41 → core, harness, cli. P4.28 → core, harness,
  host, web, SPA. P4.29/P4.30 → SPA only. The unifier recounts identical
  parallel bumps.

## Verification gate (before declaring done)

`ng test` (full workspace) green; `ng build` clean; full Playwright green
(this lane touches `apps/web`) including its own new spec; `cargo fmt
--all --check` (no Rust expected — state so explicitly). Per-commit rule:
`.claude/commands/commit.md`.
