# Work order P4.D168 — character progressions, the PROMPT PATH: `prompt_section.rs` (the chokepoint), `find_last_own_turn_ms`, the `build_context` trailing section with the ONE memoised event read, the forced greeting and Carina reports, `ContextCharacter.metadata`, and the seven-file `help/` re-vendor at `25f534c0b` — stacked on P4.D167

**Status: DONE — UNIFIED on main 2026-09-09** (the `25f534c0b` progressions +
bug-126 drift catch-up round; lane branch
`claude/p4-d168-progressions-prompt-6daa42`, eight commits stacked on
P4.D167's tip). Every Tier-0/1/2 item landed: the seven `help/` files
re-vendored (121 → 122), the chokepoint + three log lines, the memoised
cadence read, the trailing section, the forced greeting and Carina reports,
the negative cache guarantee, ten families regenerated at the pin. **At
unification:** `CadenceEvents` no longer memoises a FAILED read (v4's `??=`
never assigns on a rejection, so the next cadence retries — the lane's test
pinned the opposite and was flipped). The activated greeting beat's first
live run needed two spec-side gestures (`participants` + a
`connectionProfileId`) and then proved the prompt path end to end. Recorded
gaps stand at their sites: the Suparṇā-ordering comparand is EMPTY (a fixture
op with mail AND progressions closes it — phase-4 candidate); `force` is
inert at both forced call sites; `self_inventory` carries no section (v4's own
stated gap).

## Preamble

- **v4 oracle baseline: `2f4254b42`.** The round absorbs the FOUR commits
  past it (§R.1); this lane absorbs the prompt half of **`0587d1e96`** —
  `lib/progressions/prompt-section.ts` (143 lines, NEW), `lib/chat/context-
  manager.ts` (+56), `lib/chat/context/core-whisper-trigger.ts` (+41),
  `lib/chat/initialize.ts` (+28), `lib/services/carina/carina.service.ts`
  (+17), `lib/schemas/character.types.ts` (the normative comment) — plus
  the `help/**` obligation of the WHOLE round: six files from `0587d1e96`
  (`character-progressions.md` NEW 260 lines; `character-editing.md`,
  `character-system-prompts.md`, `custom-tools.md`, `pascals-workbench.md`,
  `shared-character-vaults.md` edited) and ONE from `25f534c0b`
  (`database-protection.md`, the bug-126 paragraphs). Spec reads: `git show
  25f534c0b:<path>` and the hunks; never the commit message.
- **Run the drift ledger's §2 freshness probe at lane start** with the §R.2
  expected values. **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED (§R.3).** Pin `/tmp/qt-v4-pin-p4d168-25f534c0b`
  for every family you MOVE and for the four help families; a second
  lane-unique `2f4254b42` pin for neutrality regens.
- **Sibling lanes:** P4.D167 (the engine you CONSUME — `progressions/
  {schema,engine}.rs` are FROZEN; a needed change is a STOP-and-flag landed
  as a P4.D167-labelled commit on ITS branch, then both stacks rebase),
  P4.D169 (the Pascal half — you never open `pascal/**`, `tools/
  run_custom.rs`, `api/custom_tools.rs`), P4.D166 (the lock — `help/
  database-protection.md` is YOURS, not its), P4.D170 (the SPA).
- **Worktree:** `git worktree add <path> -b claude/p4-d168-progressions-
  prompt-<hash> <P4.D167 tip sha>`; `CARGO_INCREMENTAL=0`; a host BUMP
  (the help embed) on every commit that moves `help/**`.

## Mandate

Make a v5 turn report a character's timed conditions exactly as v4's does:
the trailing `Time-bound conditions you are carrying, as of this moment:`
block after Suparṇā's mail and before the turn-skip note, skipped in
continue mode, derived from ONE memoised event read shared with the Core-
whisper trigger, FORCED into the greeting head and onto Carina's user
message, and BYTE-FOR-BYTE NOTHING when no progression reports — with
`build_context`'s tier-3 family widened to SEE it (the corpus today cannot:
no fixture character carries a `progressions` key and `ContextCharacter`
has no `metadata`). Re-vendor the seven help files. This order does NOT
touch the engine, the Pascal family, any REST edge, or `apps/web/**`.

⚠ **Two premises to hold onto.** (1) The report NEVER enters system block 1:
`PROMPT_CACHE_STRUCTURE_VERSION` and `IDENTITY_STACK_BUILDER_VERSION` do not
move and both committed golden hashes (`system_prompt_equivalence`'s and the
identity-compiler's cross-implementation hash) must stay green — if one
moves you have put the clock in the cached prefix. (2) The prompt path
performs NO writes — the design doc's constraint; an autonomous turn in the
job runner runs it identically. The greeting's forced report lands in the
greeting's OWN flat builder (`chat_initialize.rs:144`), which is composed
once and never cached — that is why the clock is allowed there.

## Survey-verified starting points (fresh survey 2026-09-08 at `25f534c0b` — do not re-derive)

### v4 — `lib/progressions/prompt-section.ts` (143 lines)

- `CONTEXT = 'progressions.prompt-section'`; `PROGRESSIONS_SECTION_HEADER`
  `:51`. `BuildProgressionsSectionParams` `:53–75`: `character: {id,
  metadata?} | null | undefined`, `loadEvents?: () => Promise<ChatEvent[]>`
  (a THUNK — called only once a character is known to carry ≥ 1
  progression), `respondingParticipantId?`, `nowMs`, `timezone?`, `force?`.
- `buildProgressionsSection` `:82–142`: `!character` → `''`; `parseProgressions`
  with the warn sink; `ids.length === 0` → `''` (the thunk NOT called);
  `lastTurnMs = force || !loadEvents || !respondingParticipantId ? null :
  findLastOwnTurnMs(await loadEvents(), respondingParticipantId)`; for `id
  of ids.sort()`: derive, `force ? {report:true, reason:'first'} :
  shouldReportProgression(...)`, push `- ${render}`; the debug line with
  `decisions`; the `catch` → warn + `''`. Three sentences — §C.3.

### v4 — `core-whisper-trigger.ts` `findLastOwnTurnMs` (`:203–227`)

Walk from the end: `ev.type === 'message'`, `role === 'ASSISTANT'`,
`participantId === id`, `isVisibleConversationalTurn(m, id)`; `createdAt`
may be a `Date` or a string — `Date.parse(String(x))`; a NON-FINITE ms row
is SKIPPED (keep walking), not returned. v5 twin site: `core_whisper.rs`
(`WhisperEvent` `:42–51` has NO `created_at`; `is_visible_conversational_
turn` `:93–109` is private and reusable). The `core_whisper_equivalence`
family (`harness/oracle/cases/core-whisper.ts`) gains a `findLastOwnTurnMs`
op — `WireEvent` gains `createdAt`.

### v4 — `context-manager.ts`

- `:2154–2164` the memo: `let chatEventsForCadence: ChatEvent[] | null =
  null; const loadChatEventsForCadence = async () => { chatEventsForCadence
  ??= await getRepositories().chats.getMessages(chat.id); return … }`; the
  Core-whisper trigger's `getMessages` at `:2176` becomes
  `loadChatEventsForCadence()`. **A read-count change on a ported path** —
  v5's `resolve_core_whisper_llm_context` (`build_context.rs:1374–1490`)
  reads `chats_messages_read::get_messages` at `:1424`; the progressions
  walk must NOT issue a second read for the same rows. The tier-3 family
  cannot see a read count; pin the memo with a unit test over a counting
  `read_main` seam (or a `tracing`-counted arm), mutation-proven by
  deleting the memo.
- `:2588–2609` the section: after `surfaceOperatorMailForChat`, `if
  (!isContinueMode) progressionsLLMContext = await
  buildProgressionsSection({ character, ...(respondingParticipant ?
  {loadEvents} : {}), respondingParticipantId: respondingParticipant?.id ??
  null, nowMs: Date.now(), timezone: options.timezone })`.
- `:2629–2633` trailing order `core, recall, suparna, progressions,
  turnSkip`; `:2644–2656` the `else if (turnSkipInstruction ||
  progressionsLLMContext)` branch joining `[progressions, turnSkip].
  filter(Boolean)` with `\n\n---\n\n`.
- v5: `build_context.rs` — `is_continue_mode` `:513`, the Suparṇā resolve
  `:3408–3428`, the turn-skip build `:3430–3437`, the trailing assembly
  `:3474–3520` (four pushes today at `:3479–3485`; the `else if
  !turn_skip_instruction.is_empty()` at `:3507`). `BuildContextInput.
  character: ContextCharacter` `:477`, `participant_characters` `:490`,
  `timezone: Option<String>` `:496`. The `now_ms` the timestamp whisper
  already injects (frozen in the family — reuse THAT clock for `nowMs`,
  never a second `now_unix_ms()`; the oracle's `FIXED_NOW_MS` is what the
  jest side must hand `buildProgressionsSection` too — mock `Date.now` in
  the case or the rows never agree).
- `ContextCharacter` `:403–409` — `{id, name, character_document_mount_
  point_id, sys}`, NO metadata. Construct sites: `orchestrator.rs:4321
  to_context_character(c: &Value)` (the hydrated character Value carries
  `metadata` — `tools/run_custom.rs:592` reads `character.get("metadata")`
  off the same shape), `orchestrator.rs:4091/4234/4239/4778`,
  `tools/self_inventory.rs:1913`, `build_context.rs:4117/4414` (tests),
  `harness/.../build_context_tier3_equivalence.rs:527/607`.

### v4 — `initialize.ts` (the greeting head) and `carina.service.ts`

- `buildSystemPrompt` becomes `async` (`:174`); after the `## Additional
  Instructions` block (`:218`) and BEFORE `You are roleplaying as`
  (`:240`): `const progressionsSection = await buildProgressionsSection({
  character, nowMs: Date.now(), force: true }); if (progressionsSection)
  prompt += \`\n\n${progressionsSection}\``. v5: `chat_initialize.rs:144–
  205` (`build_system_prompt(character: &Value, …, subprompts)`; the
  subprompts block ends `:203`; `You are roleplaying as` `:205`). The
  greeting is NOT async in v5 — the section needs no event read when forced,
  so a synchronous call is faithful; `now_ms` must be an INJECTED parameter
  (the family freezes it).
- Carina `:583–600`: FORCED section; `questionWithProgressions = section ?
  \`${question}\n\n---\n\n${section}\` : question` as the USER message;
  the system block untouched. v5: `carina_query.rs:456–459`
  (`StreamMessage::user(opts.question.clone())`); `deps.now_ms` exists
  (`:451`).
- Families: `build_context_tier3_equivalence` (header recipe: TZ=UTC on
  BOTH stages; the `/tmp` builder `build-context-tier3-fixture.ts`; the
  corpus `build-context-tier3.json`), `carina_query_tier3_equivalence`
  (`build-carina-query-fixture.ts` + `carina-query-tier3.json`),
  `chat_context_init_equivalence` (the greeting head — find its builder by
  grepping `chat-context-init` under `harness/oracle/`), `core_whisper_
  equivalence`, `chat_create_capstone_equivalence` (the persisted greeting
  head IS the system-prompt row — `chat-create-capstone-a-stored-first-
  message-never-streams`; if the capstone fixture can carry a progression
  cheaply, one arm there pins the greeting end-to-end; otherwise the
  chat-context-init family is the pin).

### v4 — the seven help files

`git show 25f534c0b --stat -- help/` for `25f534c0b` and `git show
0587d1e96 --stat -- help/` for the six. The tree goes 121 → 122 files
(`help_tree_embed_guard.rs:25–27 VENDORED_FILE_COUNT = 121` → 122; the
`help_tree_equivalence.rs:8` "all 120 real files" prose is stale — fix it
while you are there). Four help families regenerate at the pin
(`help_tree_equivalence`, `help_doc_sync_equivalence`,
`help_doc_chunking_equivalence`, `help_docs_tier2_equivalence`); grep the
fresh help-tree NDJSON for `character-progressions` (> 0) and for `not by
what the network was calling the house` (the bug-126 paragraph, > 0).

## Tiered deliverables

### Tier 0 — the help re-vendor (one commit, first)

0. Copy the seven files from the pin over `<repo>/help/`; rebuild host;
   regenerate + re-run the four help families at the pin; bump
   `VENDORED_FILE_COUNT` to 122 and `help_tree_embed_guard` green locally.

### Tier 1 — must land, in this order

1. **`core_whisper::find_last_own_turn_ms`** + `WhisperEvent.created_at:
   Option<String>` (a `Default` field — every construct site compiles;
   `build_context.rs:1430` and the harness `WireEvent` gain the read);
   `core_whisper_equivalence` grows a `findLastOwnTurnMs` op with rows for:
   never spoken; the last own turn is a Staff whisper (skipped → the earlier
   one); a targeted-away whisper; an empty tool-call-only turn; a `Date`-
   typed `createdAt` (the case constructs a `Date` — the wire shows an ISO
   string); an unparseable `createdAt` SKIPPED to the earlier row; a USER
   row with the same participant id (not counted).
2. **`progressions/prompt_section.rs`** (§C.3) + `pub mod prompt_section;`
   in `progressions/mod.rs` inside your fence: the three tracing lines at
   target `progressions.prompt-section` with v4's bags (the `decisions`
   list as a JSON array field), capture-pinned (`quilltap_core::test_
   support::captured`, thread-scoped) — one mutation per line class.
   `load_events` as a boxed closure returning `Result<Vec<WhisperEvent>,
   _>` (the `build_context` read path is sync `read_main`; a thunk is a
   thunk).
3. **`ContextCharacter.metadata: Option<Value>`** threaded at
   `to_context_character` (`c.get("metadata").cloned()`) and every
   construct site; then **`build_context`**: the memoised event read (ONE
   `get_messages` per turn shared with the Core trigger — restructure
   `resolve_core_whisper_llm_context` to take the loaded events / the memo
   rather than reading itself), the section after the Suparṇā resolve,
   skipped when `is_continue_mode`, the trailing order, the `else if`
   union. **`build_context_tier3_equivalence` widened RED-FIRST**: the
   `/tmp` builder seeds the responding character's `metadata.json` with
   `progressions` (a `turn` cannon mid-recharge; a `1h` pregnancy; a
   `once` progression already complete; a malformed entry that must be
   DROPPED with the warn; an entry with `updatedAt` after the last turn) and
   the corpus gains ops: a normal turn (the block in the composed user
   message at the right slot — after Suparṇā's mail when present, before the
   turn-skip note when present), a continue-mode turn (NO block), a
   chained turn with no user message and no turn-skip (the block as its own
   `role: user` message), a chained turn with BOTH (the join order), a
   character with NO progressions (byte-identical to the pre-feature
   corpus), the `once`-silenced case, the `1h` same-bucket skip (the
   fixture's last own turn and the frozen `now` inside one clock hour) and
   cross-bucket report. The jest case freezes `Date.now` to the oracle's
   `FIXED_NOW_MS` for the section (or the rows cannot agree). The
   memoisation pinned by a counting-seam unit test.
4. **The greeting** (`chat_initialize::build_system_prompt` — the forced
   section after the subprompts block, before `You are roleplaying as`;
   `now_ms` injected) — `chat_context_init_equivalence` widened with a
   progression-carrying character (the pending / active / complete wording
   all forced, and the empty case byte-identical); if cheap, one capstone
   arm proving the persisted head carries the block.
5. **Carina** (`carina_query`: the forced section on the USER message;
   `deps.now_ms`) — `carina_query_tier3_equivalence` widened with an
   answerer carrying a progression (the canned-stream key includes the
   messages, so a byte divergence is a canned MISS — the family's own
   tripwire); the `system` block asserted UNCHANGED.
6. **The negative guarantee**: a unit test asserting `PROMPT_CACHE_
   STRUCTURE_VERSION == 4` and the identity-compiler golden UNMOVED with a
   progression-carrying character compiled (v4's `cache-determinism/system-
   prompt.test.ts` +34 is the seed — mirror its assertion).

### Tier 2 — should land

7. The `character.types.ts` normative comment carried onto
   `db/vault_character_write.rs:134–139`'s `metadata` doc ("raw metadata is
   never injected into a prompt; the one sanctioned reader is the derived
   progressions report").
8. `self_inventory` documented as NOT carrying the section (v4's stated gap
   — a doc comment at `tools/self_inventory.rs:1913`'s construct, `metadata:
   None` deliberate).

### Tier 3 — explicit deferrals (loud, typed, never silent)

- The character-voiced announcer, help chat, Brahma: NO section (v4's
  table). Nothing to refuse — record it.
- Story-clock mapping: v4 deferred; nothing to build.

## The differential requirement (restated)

Every moved family regenerated at the `25f534c0b` pin through the sweep
driver with the changed bytes grepped (`Time-bound conditions` > 0 in the
build-context, chat-context-init and carina NDJSONs; `findLastOwnTurnMs`
rows > 0 in the core-whisper NDJSON; `character-progressions` > 0 in the
help NDJSONs); the pre-feature rows BYTE-IDENTICAL (the empty-is-identical
guarantee is a comparand — diff the old and new NDJSON on the untouched
ops). Mutation proofs, each reddening exactly one arm: the section pushed
BEFORE the Suparṇā mail; the continue-mode skip dropped; the `else if` join
order swapped; `force` ignored in the greeting; the section put on Carina's
SYSTEM message; the memo deleted (reddens the counting test only — the
tier-3 corpus is blind to it, say so); `ids.sort()` dropped (reddens the
two-progression row).

## Fixtures

- CONSUMES (widens the `/tmp` builders, regenerates at the pin, never
  rebuilds a committed pair): `build-context-tier3-fixture.ts` +
  `build-context-tier3.json`; `build-carina-query-fixture.ts` +
  `carina-query-tier3.json`; the chat-context-init builder; `harness/oracle/
  cases/core-whisper.ts`. `TZ=UTC` on both stages of every one (the
  distill-transitive rule; the progressions `formatInstant` host fallback
  reads the zone too).
- DELIVERS: `help/**` at `25f534c0b` (seven files); NO new committed DB.
- NEVER touches `characters-*.db`, `subprompts-*.db`, `pascal-run-custom-*`,
  `workbench-*`.

## §C — the progressions wire contract (BINDING — verbatim-identical in P4.D167, P4.D168, P4.D169 and P4.D170)

Every name below is the wire or the one shared home. P4.D167 implements C.2;
P4.D168 implements C.3 against it; P4.D169 implements C.4 against it; P4.D170
codes against C.1/C.4/C.5 (mocked until unification) and transcribes the
client-safe half of C.2 as a TypeScript twin. The unifier runs the
name-for-name diff of every §C spelling at unification. Spec reads are
`git show 25f534c0b:<path>` (post-commit files), never the commit message.

### C.1 The reserved key and the record (v4 `lib/progressions/schema.ts`, 231 lines)

- Storage: ONE reserved top-level key **`progressions`** in the character
  vault's `metadata.json` (`PROGRESSIONS_METADATA_KEY = 'progressions'`);
  the value is an object keyed by id. Every other key stays freeform. A
  missing key means none. **No table, no column, no D23 re-dump, no
  `PROMPT_CACHE_STRUCTURE_VERSION` / `IDENTITY_STACK_BUILDER_VERSION` bump**
  (v4 asserts this as a negative guarantee; v5's committed golden hashes stay
  valid — a lane that finds one moving has ported the section into the
  cached block and must stop).
- Id rule `PROGRESSION_ID_PATTERN = /^[a-z][a-z0-9_-]{0,63}$/` (ASCII —
  no JS-regex fidelity question). Cadence grammar `REPORT_FREQUENCY_PATTERN
  = /^(turn|increment|[1-9]\d{0,4}[smhdw])$/`. `TimeIncrement` enum
  `second|minute|hour|day|week|month|year`; `OnComplete` enum `keep|once`.
- Limits: `MAX_PROGRESSIONS_PER_CHARACTER = 32`, `MAX_REPORT_TEMPLATE_LENGTH =
  500`, `MAX_PROGRESSION_NAME_LENGTH = 80`, `MAX_PROGRESSION_DESCRIPTION_LENGTH
  = 500`, `MAX_QUANTITY_UNIT_LENGTH = 16`; every `min`/`max` is Zod 4.5's
  CODE-POINT length (`jsstr::zod_len_*`).
- The record (a `z.strictObject` — unknown keys REFUSE the entry), in v4's
  declaration order: `name` (string 1–80), `description?` (≤ 500),
  `startTime` (ISO instant), `endTime` (ISO instant, strictly after
  `startTime` — a `superRefine` at path `['endTime']`, message `must be
  strictly after startTime`), `timeIncrement`, `percentageReport` (bool,
  default `true`), `reportFrequency` (string matching the grammar, default
  `'turn'`), `quantity?` (`strictObject { total: finite positive number,
  unit: string 1–16, precision: int 0–6 default 1 }`), `reportTemplate?`
  (1–500), `onComplete` (default `'keep'`), `updatedAt?` (ISO instant).
- `IsoDateTimeSchema`: a string whose `parseIsoInstant` is finite; message
  `must be an ISO-8601 date-time with an offset or Z (e.g.
  2026-08-01T00:00:00Z)`. `parseIsoInstant(value)`: non-string → NaN;
  `trim()`; must match `ISO_INSTANT_PATTERN = /^\d{4}-\d{2}-\d{2}[Tt ]\d{2}:
  \d{2}(:\d{2}(\.\d{1,9})?)?([Zz]|[+-]\d{2}:?\d{2})$/` (a SPACE separator is
  admitted, a zone is REQUIRED); then `Date.parse(trimmed)`. v5: the
  `episodic::js_date_parse_ms` twin (`pub(crate)`, the full V8 ISO subset)
  behind the same regex — the space form needs the `format_time.rs`
  space→`T` treatment before it.
- `ProgressionsSchema` = record keyed by the id pattern, refined to ≤ 32
  entries (message `a character may carry at most 32 progressions`).
  `parseProgressions` is the FAIL-SOFT reader (C.2) and never uses the
  record-level refine — it trims the 33rd and keeps 32.
- `parseProgressKey(key)` → `{ok:true,id,field}` | `{ok:false,reason}`; the
  three reasons, byte-exact: `"<key>" names no field — write "<progression
  id>.<field>", e.g. "cannon.complete"` (no dot); `"<id>" is not a
  progression id — lowercase, starting with a letter, then letters, digits,
  _ or - (at most 64)`; `"<field>" is not a progression field name` (field
  must match `/^[a-zA-Z]+$/`). Split at the FIRST dot.
- `WRITABLE_PROGRESSION_FIELDS`, in order: `name, description, startTime,
  endTime, timeIncrement, percentageReport, reportFrequency, onComplete,
  reportTemplate, quantity.total, quantity.unit, quantity.precision,
  remove`. `updatedAt` is NOT writable (the applier stamps it).

### C.2 The engine — `quilltap_core::progressions` (P4.D167 delivers; frozen at its tip)

Module layout: `crates/quilltap-core/src/progressions/mod.rs` (re-exports),
`schema.rs` (C.1 + the typed parse), `engine.rs` (below). Client-safe in
v4 terms = no DB, no tracing, no I/O in this module; the ONE logging reader
is P4.D168's `prompt_section.rs`, which passes an `on_issue` sink down.
Public names (snake_case twins of v4's exports; the SPA twin keeps v4's
camelCase):

- `parse_iso_instant(&Value) -> Option<i64>` (v4 `NaN` ⇔ `None`).
- `parse_progression(&Value) -> Result<Progression, Vec<ZodIssue>>` where
  `ZodIssue { path: Vec<String>, message: String }` and the issue sentence
  joined for `on_issue` is `` `${path.join('.') || '(root)'}: ${message}` ``
  joined by `'; '` — the sentences are Zod 4.5.4's, MEASURED through the
  corpus (unknown key, wrong type, too small/big, invalid enum, invalid
  format, the custom refine). `Progression` is a typed READ struct; the
  WRITE path (P4.D169's applier) mutates the raw `serde_json::Map` exactly
  as v4 mutates the parsed object, so key order falls out identically —
  no struct round-trip on a write.
- `parse_progressions(metadata: Option<&Value>, on_issue: &mut dyn
  FnMut(&str, &str)) -> Vec<(String, Progression)>` in `Object.entries`
  order, fail-soft: non-object metadata → empty; a non-object
  `progressions` value → `on_issue("*", "the progressions key does not hold
  an object")` (only when the key is PRESENT) → empty; a bad id →
  `on_issue(id, "the id is not a lowercase identifier (a-z, 0-9, _ and -,
  starting with a letter)")`; the 33rd+ → `on_issue(id, "over the ceiling
  of 32 progressions per character")`; a refused body → `on_issue(id,
  <joined issues>)`. Never errors, never `None`.
- `UNIT_MS` (second 1000, minute 60_000, hour 3_600_000, day 86_400_000,
  week 604_800_000, month 2_629_746_000, year 31_556_952_000); the FINER
  unit table (year→month, month→day, week→day, day→hour, hour→minute,
  minute→second, second→none); the period suffixes `s m h d w`.
- `format_span(ms: f64, unit) -> String` (whole units + ONE finer unit;
  `Math.abs`; `"0 <units>"` only when no finer unit or both zero; plural by
  `count === 1`), `format_span_whole(ms, unit)`.
- `derive_progression(id: &str, p: &Progression, now_ms: i64) ->
  DerivedProgression { id, name, start_ms, end_ms, now_ms, elapsed_ms,
  remaining_ms, percent: f64 (UNCAPPED; `spanMs > 0 ? … : 100`),
  percent_clamped, state: Pending|Active|Complete, started, complete,
  quantity_current: Option<f64>, elapsed, elapsed_whole, remaining,
  remaining_whole }` — the display spans differ by state (pending counts
  DOWN to the start; complete counts UP from the end).
- `parse_report_period_ms(&str) -> Option<i64>`.
- `should_report_progression(p, d, last_turn_ms: Option<i64>) ->
  ShouldReportResult { report: bool, reason: ReportReason }` with
  `ReportReason` ∈ `first|updated|transition|silenced|turn|increment|
  period|skip` (`as_str()` gives v4's literal) and v4's SEVEN rules in
  order (`null`/non-finite → first; `updatedAt > lastTurn` → updated;
  state-at-last-turn ≠ state → transition; complete+once → silenced (no
  report); `turn` → turn; `increment` → floor(elapsedAt(now)/unit) ≠
  floor(elapsedAt(last)/unit) ? increment : skip; period → epoch-anchored
  bucket change ? period : skip; an unparseable period → turn).
- `render_progression_report(p, d, &RenderProgressionOptions { timezone:
  Option<&str> }) -> String`: pending → `` `${name}: begins in
  ${remaining}.` ``; complete → `` `${name}: complete; ${elapsed} since it
  finished.` ``; active → `substitute(reportTemplate ??
  default_in_progress_template(p), placeholders)`. `default_in_progress_
  template`: `{{name}}: {{elapsed}} elapsed, {{remaining}} remaining` +
  `, {{percent}}% complete` (if `percentageReport`) + ` ({{quantity}})`
  (if `quantity`) + `.`, prefixed by `description + ' '` when present.
  `progression_placeholders(p, d, opts)` → the eleven keys `name,
  description ('' when absent), elapsed, elapsedWhole, remaining,
  remainingWhole, percent (String(Math.round(percentClamped))), quantity
  (`${current.toFixed(precision)}/${total.toFixed(precision)} ${unit}` or
  ''), start, end, increment`. `substitute` = the plain
  `/\{\{([^}]+)\}\}/g` scan, `trim()` the key, unknown stays VERBATIM.
  ⚠ `start`/`end` = `Intl.DateTimeFormat('en-US', { dateStyle: 'medium',
  timeStyle: 'short' (omitted when the increment is day or coarser),
  timeZone })` → e.g. `Sep 8, 2026, 2:02 PM` / `Sep 8, 2026` — **Node ≥ 20
  renders a U+202F NARROW NO-BREAK SPACE between the minutes and `AM`/`PM`,
  not U+0020; measure the exact bytes on Node 24 and pin them in the
  corpus.** An unresolvable zone falls back to the host's (v5: `jiff::tz::
  TimeZone::get` failure → the host zone). Byte-exact over the corpus.
- `ProgressPrimitive` = Number(f64) | String | Bool (a `serde_json::Value`
  subset). `flatten_progressions(metadata, now_ms, on_issue) ->
  serde_json::Map<String, Value>` (preserve_order): per progression in
  parse order, the twelve keys `<id>.name, <id>.percent, <id>.elapsedMs,
  <id>.remainingMs, <id>.startTime (epoch ms), <id>.endTime (epoch ms),
  <id>.started, <id>.complete, <id>.state, <id>.elapsed, <id>.remaining`
  + `<id>.quantity` only when present.
- `infer_increment(span_ms) -> TimeIncrement` (thresholds 2 min / 2 h /
  2 d / 2 w / 8 w / 2 y).
- `parse_progress_key(&str) -> Result<(String, String), String>` and
  `WRITABLE_PROGRESSION_FIELDS: [&str; 13]` + `is_writable_progression_
  field`.

### C.3 The prompt section (P4.D168 delivers) — `progressions/prompt_section.rs` + the four sites

- `PROGRESSIONS_SECTION_HEADER = "Time-bound conditions you are carrying,
  as of this moment:"`. The block is `header + "\n" + lines.join("\n")`
  where each line is `` `- ${render_progression_report(...)}` ``; ids
  iterate in JS default-sort order (ASCII ids ⇒ byte order); **empty is
  `""` byte-for-byte** and the caller pushes nothing.
- `build_progressions_section(BuildProgressionsSectionParams { character:
  Option<&(id, metadata)>, load_events: Option<thunk>, responding_
  participant_id: Option<&str>, now_ms: i64, timezone: Option<&str>,
  force: bool }) -> String`; the events thunk is called ONLY when the
  character carries ≥ 1 progression AND not `force` AND a participant id is
  present; `force` ⇒ every progression reports with reason `first`. Never
  errors (a failure → `""` + the warn).
- Three log lines at target `progressions.prompt-section`, v4's levels and
  bags: warn `Dropping a malformed character progression` `{characterId,
  progressionId, issue}`; debug `Character progressions evaluated for this
  turn` `{characterId, respondingParticipantId, lastTurnMs, forced,
  emitted, reported, decisions: [{id, state, reason}]}`; warn
  `Progressions section failed to build; the turn continues without it`
  `{characterId, error}`.
- `core_whisper::find_last_own_turn_ms(events, responding_participant_id)
  -> Option<i64>`: walk from the END; `type == "message"`, `role ==
  "ASSISTANT"`, `participantId == id`, `is_visible_conversational_turn`;
  parse `createdAt` (`Date.parse`; a non-finite row is SKIPPED, not
  returned). `WhisperEvent` gains `created_at: Option<String>`.
- `build_context`: ONE memoised event read shared by the Core-whisper
  trigger and the progressions walk (`loadChatEventsForCadence`); the
  section computed AFTER the Suparṇā mail resolve, SKIPPED in continue
  mode, pushed into the trailing sections in the order `[core whisper,
  llm recall, suparna mail, progressions, turn-skip note]`; the no-user-
  message `else if` fires for `turnSkipInstruction || progressions` and
  joins `[progressions, turn_skip]` (that order) with `\n\n---\n\n`.
  `ContextCharacter` gains `metadata: Option<Value>` threaded from the
  hydrated character at `orchestrator::to_context_character`.
- Greeting (`chat_initialize::build_system_prompt`): FORCED; appended as
  `\n\n{section}` after the `## Additional Instructions` block and BEFORE
  `You are roleplaying as`. Carina (`carina_query`): FORCED; rides the USER
  message as `` `${question}\n\n---\n\n${section}` `` — never the system
  block. Announcer, help chat, Brahma, `self_inventory`: NO section.

### C.4 The Pascal wire (P4.D169 server ↔ P4.D170 client)

- Definition JSON: `when.progress: Record<ProgressKey, MetadataComparator>`
  (beside `metadata`; the `testsSomething` OR gains it; refine message
  `` must test something: a comparator on the value, `roll`, `llm`, a
  non-empty `params`, `metadata`, or `progress` `` and the effect-when
  variant with `` `outcome`, `` inserted before `` a non-empty `params` ``);
  `availableWhen`/`withheldWhen`: `metadata` becomes OPTIONAL, `progress:
  Record<ProgressKey, GateComparator>` optional, object-level refine `must
  test at least one metadata key or progress field`; effect `target`
  gains the third family `progress.<id>.<field>` with the four load-time
  refusals (C.1's key parser reasons plus `must name "progress.<progression
  id>.<field>" — e.g. "progress.cannon.endTime"`, `writes progression
  "<id>", which is not a valid id — …`, `writes "<field>", which is not a
  writable progression field — use one of <the 13, comma-joined>`), the
  reserved-key refusal on the METADATA family (`writes the reserved
  "progressions" key through "metadata." — use "progress.<id>.<field>"
  instead, which is validated and rolled back on a bad result` for
  `metadata.progressions` AND `metadata.progressions.<x>`; `metadata.
  progressionsNotes` still fine), and the terminal `must start with
  "state.", "metadata." or "progress."`. Placeholders: `{{now}}` (epoch ms
  at run start, ONE value per run) and `{{progress.<id>.<field>}}` (split
  at the FIRST dot; a half-written key is `unknown`).
- Run-time semantics: `progress` is `flatten_progressions(metadata,
  now_ms)` derived from the SAME hydrated snapshot the gates/tables read,
  against ONE `now_ms` per run (roster resolution, the LLM tool path, the
  manual run, the bench preview/audit); an absent id/field DECLINES the
  comparator (availableWhen fails CLOSED, withheldWhen fails OPEN);
  the two new render-debug sentences `Custom tool message references a
  progression the character cannot render` (`reason: the character carries
  no such progression, or it has no such field`) and `Custom tool message
  references {{now}} with no run clock to render`; the metadata-test debug
  sentence becomes `` `Custom tool ${subject} test did not match` ``.
- The applier: progress writes FOLD into the same `metadataNext` copy
  (ONE character write); create-on-write defaults `name = id, startTime =
  now, endTime = now + 1 h, timeIncrement = inferIncrement(span),
  percentageReport = true, reportFrequency = 'turn', onComplete = 'keep'`;
  a minted `quantity` = `{ total: 1, unit: 'units', precision: 1 }`; time
  fields take epoch ms OR ISO and normalise to ISO; `remove` deletes only
  on `true`; every touched entry stamped `updatedAt = now`; post-
  validation drops a refused entry's writes and restores the pre-run
  entry with warn `Custom tool progress writes dropped — the result would
  not validate` `{chatId, tool, progressionId, issue}`; no empty reserved
  key left behind; the roll stands. Skip reasons: `no rolling character,
  progress effect skipped`, `progress remove effect wrote a value other
  than true`, `progress remove effect names no such progression`,
  `progress time effect wrote neither epoch milliseconds nor an ISO
  instant`. Debug `Custom tool progress effect folded` `{target, previous,
  next}`.
- `ToolVocabulary` (the whole-object wire, `references` on every listing
  row) gains, in v4's declaration order AFTER `metadataWrites`: `progress:
  string[]` (IDS read — from `when.progress`, gates, `{{progress.…}}`),
  `progressWrites: string[]` (ids written), `now: boolean`. Ids, never
  `<id>.<field>` keys. `isEmptyVocabulary` gains the three conjuncts.
- Preview response (`POST /api/v1/custom-tools?action=preview`): `gate`
  evaluated WITH the progress sheet; NEW key `progress` (the flattened
  sheet) present iff non-empty, spread AFTER `gate`. Audit: `simulate_
  outcomes` takes the sheet derived ONCE. No new request body key — the
  bench's `metadata` may carry a `progressions` block.
- `RUN_CUSTOM_PREAMBLE` gains, after the side-effects sentence: `Some tools
  consult, or adjust, the rolling character's timed progressions —
  recharges, gestations, countdowns — server-side; you cannot set one
  yourself.`
- Client strings (P4.D170, v4's bytes): run dialog `{{now}}` → `the moment
  the run began, to the millisecond`; `It consults your timed
  progressions:` + `<code>` ids + `. A progression you are not carrying
  simply does not match.`; writes listed as `progress.<id>`.

### C.5 The SPA's write path (P4.D170) — no new verb

The editor saves by client-side read-modify-write of the hydrated
character's WHOLE `metadata`: re-read `characterGet`, spread every other
key untouched, replace `progressions` (DELETE the key when the last entry
goes), stamp the edited entry's `updatedAt = new Date().toISOString()`,
then `characterUpdate { character: { metadata } }` — v5's vault writer
(`db/vault_character_write.rs:139`, `Option<Value>`, whole-object replace,
absent = no opinion) already carries it; a tombstoned (archived) character's
PUT is refused server-side and the card renders read-only. Invalidate
`characters.detail(id)` + `characters.all`. **No new dispatch verb, no new
REST edge, no `api/types.rs` variant** for the editor.

### C.6 Gate constants (P4.D170 spec files)

`P4D168_SERVER_LANDED = false` guards beats needing the prompt path (the
greeting/turn report); `P4D169_SERVER_LANDED = false` guards beats needing
the Pascal half (a `progress` gate on the bench, a `progress.<id>.<field>`
effect, the vocabulary lines). The editor beats run LIVE from day one.

### C.7 What no order invents

No `progress_set` LLM tool; no scheduling; no story-clock mapping; no
persisted "last reported at"; no per-turn whisper row for the report; no
new table or column; no new dispatch verb for progressions; no
`title="…"` copy not in v4; no toast v4 does not show.

## §R — the round-wide meeting points (BINDING — identical in every order of this round)

Round: **the `25f534c0b` progressions + bug-126 drift catch-up round** —
P4.D166 (bug 126, the instance lock) ∥ P4.D167 (the progressions engine) →
{ P4.D168 (the prompt path) ∥ P4.D169 (the Pascal family) } both stacked on
P4.D167's finished tip ∥ P4.D170 (the SPA, from `main`). Ordered 2026-09-08.

1. **Baseline and drift.** Oracle baseline **`2f4254b42`** (drift-ledger §1).
   v4 `main` HEAD **`25f534c0b`** ("fix: a hostname change no longer makes
   the app kill its own database (bug 126)", `4.10.0-dev.9`) — FOUR commits
   past the baseline, the ledger's four §3 rows, now `ORDERED(…)` per row:
   `d307a4164` (docs — the progressions design of record, ratification +
   `docs/v4/` mirror rides P4.D167), `0587d1e96` (PORT-NEW — P4.D167 ∥
   P4.D168 ∥ P4.D169 ∥ P4.D170), `4097626c6` (version bump — ratification
   rides P4.D166), `25f534c0b` (PORT — P4.D166). v4 `bugfix` tip
   **`1a2b2164c`**, unmoved, nothing unabsorbed (its whole content delta
   against `main` is the four rows above plus the subprompts commit
   reversed). The round's catch-up TARGET is `25f534c0b`; the unifier moves
   the baseline there.
2. **The freshness probe at lane start** (ledger §2), expected values:
   checkout on **`main`**, tree CLEAN, `git log --oneline 25f534c0b..main`
   EMPTY, `git log --oneline 1a2b2164c..bugfix` EMPTY. **Any mismatch: STOP
   and report** — a lane never writes the ledger. Re-run the probe before
   every regen batch; the checkout has gone dirty MID-LANE before (ledger
   §5.1) and was switched to `release` mid-unify once (P4.D91).
3. **Regen rule: PIN REQUIRED, TWO pin shas, lane-unique paths, all three
   symlink classes** (ledger §5.1), every regen and fixture build through
   `harness/tools/recipe_sweep.py --v4 <pin> --run <family>`; never two
   sweeps concurrently.
   - **`25f534c0b`** for every family this round MOVES (the progressions
     families, the lock/Tier R families, the help families) — the catch-up
     target (the P4.D119 precedent: a drift lane pins at the drift sha it
     absorbs). Paths: `/tmp/qt-v4-pin-p4d166-25f534c0b`,
     `/tmp/qt-v4-pin-p4d167-25f534c0b`, `/tmp/qt-v4-pin-p4d168-25f534c0b`,
     `/tmp/qt-v4-pin-p4d169-25f534c0b`, `/tmp/qt-v4-pin-p4d170-25f534c0b`.
   - **`2f4254b42`** for any family the round does NOT move (neutrality
     regens). Paths: `/tmp/qt-v4-pin-p4d16N-2f4254b42` per lane.
   - **Verify each pin by a marker**: `lib/progressions/engine.ts` and
     `help/character-progressions.md` exist ONLY in the `25f534c0b`
     worktree; `isStillOurLock` appears in `lib/database/backends/sqlite/
     instance-lock.ts` ONLY there; `Time-bound conditions you are carrying`
     MUST appear in a `25f534c0b`-pinned build-context oracle whose fixture
     carries a progression and MUST NOT appear in a `2f4254b42`-pinned one.
     Check every NDJSON is non-empty before believing any diff (the
     empty-file trap).
   - Committed recipe headers keep saying `cd ~/source/quilltap-server` and
     never name a `/tmp` pin (`recipe-header-conventions`). The oracle
     `node_modules` are the LIVE tree (`zod` 4.5.4 — `zod_version_guard`
     fails the gate if it moves; every Zod length rule this round transcribes
     uses the `jsstr::zod_len_*` code-point helpers). ⚠ Standing hazard (5):
     v4's jest maps `@google/genai` to a manual mock — irrelevant to this
     round's oracles (none loads the plugin tree) but recorded.
4. **Stacking and resumption.** P4.D167 (the engine) branches from `main`
   and finishes FIRST — a small lane. P4.D168 and P4.D169 each `git
   worktree add <path> -b claude/p4-d16N-… <P4.D167's lane-branch TIP>`
   (never from `main`), in SEPARATE worktrees, in parallel, once P4.D167's
   gate is green and its record names the tip sha. **After that tip,
   P4.D167's public surface is FROZEN**: neither stacked lane edits
   `crates/quilltap-core/src/progressions/{mod.rs,schema.rs,engine.rs}` —
   a needed change is a STOP-and-flag landed as a P4.D167-labelled commit
   on P4.D167's branch (the D164 precedent), recorded, and both stacked
   lanes rebase onto it. P4.D166 and P4.D170 branch from `main`. The
   unifier cherry-picks P4.D166, then P4.D167, then P4.D168, then P4.D169,
   then P4.D170.
5. **Fenced-append shared files.** `crates/quilltap-core/src/lib.rs`
   (module lines — P4.D167 adds `pub mod progressions;` inside a `// === P4.D167 ===`
   fence), `crates/quilltap-core/src/progressions/mod.rs` (P4.D168 appends
   ONE line, `pub mod prompt_section;`, inside a `// === P4.D168 ===` fence —
   the only edit a stacked lane makes in that directory), `crates/quilltap-
   core/src/api/types.rs` and `api/engine.rs` (P4.D169 only, inside a
   `// === P4.D169 ===` fence, and ONLY if the custom-tools verbs' existing
   variants need a new field — measure; the preview response's `progress`
   key is a `CustomToolRunResult`-class type, not a new verb), `crates/
   quilltap-web/src/lib.rs` (no lane expects to touch it — both custom-tools
   REST edges already exist). `crates/quilltap-core/src/services/
   orchestrator.rs` is fenced BY FUNCTION: `to_context_character` (and the
   `ContextCharacter` construct sites it feeds) is P4.D168's; nothing else
   in that file is anyone's this round. `crates/quilltap-host/src/host.rs`
   is P4.D166's outright this round (the heartbeat caller + `on_lock_lost`);
   the help embed needs a host REBUILD (P4.D168's bump), not a host source
   edit. `Cargo.toml`/`Cargo.lock` at the workspace root: version lines
   only; a dependency add is a STOP-and-flag in the lane record.
6. **`apps/web/**` is P4.D170's alone** (the ONLY SPA lane this round —
   it owns `core-contract.ts`, `characters.api.ts`, the characters-edit
   screens, `screens/custom-tools/**`, `chat/tools/**` + `chat/custom-tools-popup.ts` (v4 `CustomToolRunDialog`'s twin), `pascal/**` twins,
   `apps/web/public/schemas/**`, and the NEW `apps/web/src/app/
   progressions/**`). P4.D166–P4.D169 finish with `git diff main --
   apps/web/` EMPTY. The two P4.D170 gate constants flip at unification
   (§R.9); the §C name-for-name diff runs then.
7. **Frozen files.** `docs/developer/porting/drift-ledger.md` (lanes never
   write it); `help/**` is P4.D168's alone — ALL SEVEN files the four v4
   commits touched, INCLUDING `help/database-protection.md` (bug 126's
   help edit rides P4.D168's re-vendor, NOT P4.D166 — one lane, one sha,
   one `help_tree_equivalence` regen); `apps/web/src/app/shell/shell.ts` and
   `apps/web/src/app/workspace/workspace-contract.ts` (no new tab kinds —
   every progressions surface is a section, a modal or a panel INSIDE an
   existing screen, as in v4); `crates/quilltap-core/src/generators/**`
   (nobody's this round).
8. **Version bumps.** P4.D166: `quilltap-host`, `quilltap-cli`,
   `quilltap-harness` (+ `quilltap-tauri`/`quilltap-web` ONLY if a
   lock-lost wire there moves — measure). P4.D167: `quilltap-core`,
   `quilltap-harness`. P4.D168: `quilltap-core`, `quilltap-harness`,
   `quilltap-host` (host on every commit that moves `help/**`). P4.D169:
   `quilltap-core`, `quilltap-harness`, `quilltap-web`. P4.D170:
   `apps/web/package.json` + `quilltap-harness` (the public-schemas
   vendor guard). Per commit. The unifier RECOUNTS as base + total bumps —
   identical bumps merge silently (`commit-checklist`; the playbook's
   silent-auto-merge trap).
9. **e2e.** Port 4319 is repo-wide — ONE Playwright at a time across all
   lanes, and only P4.D170 runs it. P4.D170 adds NEW spec files only, with
   every server-dependent beat guarded ACTIVATE-AT-UNIFY on the NAMED
   constants **`P4D168_SERVER_LANDED = false`** (the prompt-path beats: a
   progression the greeting reports) and **`P4D169_SERVER_LANDED =
   false`** (the Pascal beats: a `progress` gate on the bench, a
   `progress.<id>.<field>` effect through Run Tool) — never a capability
   probe (a DEFINED-but-refusing verb defeats a probe; `round-plan-
   takeaways` §Planning). The editor beats (add / edit / delete a
   progression through the character PUT) run LIVE from day one — the
   server surface they need (`characterUpdate` carrying `metadata` whole)
   is ALREADY on main (`db/vault_character_write.rs:139`). The unifier
   flips the two constants after P4.D168/P4.D169 land and runs the gated
   beats as their first execution.
10. **Append-only for everyone:** `docs/CHANGELOG.md`,
    `docs/developer/porting/status-log.md`.
11. **Fixture ownership.** P4.D167 delivers the committed corpus
    `harness/oracle/fixtures/progressions-engine.json` (read by its `tsx`
    case; no DB). P4.D168 delivers NO new committed DB pair: it WIDENS the
    `/tmp` builders of the families it moves (`build-context-tier3-
    fixture.ts` — a responding character whose `metadata.json` carries
    progressions; the carina-query and chat-context-init builders likewise)
    and regenerates them at the pin. P4.D169 EXTENDS the committed Pascal
    corpora it owns (the custom-tool-types definition corpus, the
    side-effects corpus, the tool-gate / placeholders / vocabulary corpora)
    and the `/tmp` fixtures of the two run-tool tier-3 families; it
    regenerates every family over them ONCE after all its edits are in. No
    lane regenerates a committed DB pair another lane reads:
    `characters-{main,mount}.db`, `subprompts-*.db`, `chat-delete-*`,
    `character-generators-*` are REGENERATED BY NOBODY. P4.D170 seeds
    through the running server's verbs (`characterUpdate` with `metadata`),
    never SQL — a store-overlay file cannot be SQL-seeded without moving
    every hash beside it (`dogfood-findings.md` standing notes).
12. **Census guards move when a verb is added.** Adding any dispatch verb
    with an `*_id` field, or closing a recorded divergence, moves the
    executable censuses in OTHER crates (`a-new-verb-moves-the-dispatch-
    wrong-type-census`, `closing-a-divergence-moves-the-censuses`) — every
    Rust lane's gate is `cargo test --workspace`, never `-p`. P4.D166's
    hostname seam and `LockFileContent` changes move `db_error_key_guard`-
    class censuses ONLY if a new `DbError` site appears — check.
13. **Bug 126 is LIVE-CRITICAL and P4.D166 lands first on main if any
    lane finishes early.** Until it lands, every dogfood or e2e server run
    on a macOS host with `scutil --get HostName` unset can die ~60 s after
    launch (ledger §1). A lane whose long-running server exits with
    `Instance lock lost` should suspect THIS, not its own change.

## Ownership (BINDING — identical table in every order of this round)

| Lane | Owns (edit freely) | Fenced-append (§R.5) | Must not touch |
| --- | --- | --- | --- |
| **P4.D166** (bug 126) | `crates/quilltap-host/src/lock.rs`; `crates/quilltap-host/src/host.rs` (the heartbeat loop, `on_lock_lost`, `HostShutdown`); `crates/quilltap-host/tests/host_cadence.rs`; `crates/quilltap-cli/src/db_cmd.rs`; `crates/quilltap-cli/tests/cli_differential.rs` (the lock cases + `lock_json`); `crates/quilltap-web/src/lib.rs` ONLY the `classify_lock_status` call at `:670–687` if its arms move (measure — likely nothing); this order's header | — | `help/**` (P4.D168 re-vendors `database-protection.md`); `crates/quilltap-core/**`; `apps/web/**`; every other lane's files |
| **P4.D167** (the engine) | NEW `crates/quilltap-core/src/progressions/{mod.rs,schema.rs,engine.rs}`; NEW `crates/quilltap-harness/tests/progressions_engine_equivalence.rs`; NEW `harness/oracle/cases/progressions-engine.ts` + `harness/oracle/fixtures/progressions-engine.json`; this order's header | `crates/quilltap-core/src/lib.rs` (`pub mod progressions;` in a `// === P4.D167 ===` fence) | everything else — in particular `pascal/**`, `services/**`, `core_whisper.rs`, `help/**`, `apps/web/**` |
| **P4.D168** (the prompt path, stacked on P4.D167) | NEW `crates/quilltap-core/src/progressions/prompt_section.rs`; `crates/quilltap-core/src/core_whisper.rs`; `services/build_context.rs` (incl. `ContextCharacter`); `services/orchestrator.rs::to_context_character` + the `ContextCharacter` construct sites (`tools/self_inventory.rs:1913`, the harness build-context fixture constructs); `services/chat_initialize.rs`; `services/carina_query.rs`; `help/**` (ALL SEVEN files at `25f534c0b`); the moved families + their cases/builders (`build_context_tier3`, `core_whisper`, `carina_query_tier3`, `chat_context_init`, `chat_create_capstone` if the greeting arm moves, the four help families); NEW `subprompts`-idiom family `progressions_prompt_tier2_equivalence` if the widened `build_context` fixture cannot carry every arm; this order's header | `crates/quilltap-core/src/progressions/mod.rs` (ONE line, `pub mod prompt_section;`, in a `// === P4.D168 ===` fence) | `progressions/{schema,engine}.rs` (consume only — STOP-and-flag); `pascal/**`; `tools/run_custom.rs`; `api/custom_tools.rs`; `quilltap-web/**`; `apps/web/**`; `quilltap-host/src/**` (a host BUMP for the embed is fine; no host source edit) |
| **P4.D169** (the Pascal family, stacked on P4.D167) | `crates/quilltap-core/src/pascal/**` (all fourteen files); `crates/quilltap-core/src/tools/run_custom.rs`; `crates/quilltap-core/src/api/custom_tools.rs`; `crates/quilltap-web/src/custom_tools_routes.rs`; `services/chat_run_tool.rs` if the manual-run entrance lives there (measure); the Pascal families + their cases/corpora/`/tmp` builders (§R.11); NEW `pascal_side_effects_equivalence` (+ its jest case); this order's header | `api/types.rs`, `api/engine.rs` (`// === P4.D169 ===`, only if measured necessary) | `progressions/**` (consume only — STOP-and-flag); `services/build_context.rs`; `core_whisper.rs`; `chat_initialize.rs`; `carina_query.rs`; `help/**`; `apps/web/**`; `quilltap-host/**`; `pascal-run-custom-*.db` and `workbench-*.db` REGENERATED only through their committed builders if an arm cannot be expressed otherwise (record it; prefer widening the corpus JSON) |
| **P4.D170** (the SPA) | `apps/web/**` whole — incl. `core-contract.ts`, `screens/characters/characters.api.ts`, `screens/characters/edit/system-prompts-tab.ts`, `screens/custom-tools/**`, `chat/custom-tools-popup.ts`, `chat/tools/**`, `pascal/**` (the TS twins), NEW `apps/web/src/app/progressions/**`, `apps/web/public/schemas/**` (the re-vendored `qtap-custom-tool.schema.json` + NEW `qtap-progression.schema.json`), NEW e2e specs `character-progressions-flow.spec.ts` + `workbench-progress-flow.spec.ts`, `apps/web/package.json`; NEW `crates/quilltap-harness/tests/public_schemas_vendor_guard.rs`; `docs/developer/porting/m6-screen-parity.md` (its rows); this order's header | — | `crates/**` except the one new guard file; `help/**`; `harness/oracle/**`; `shell.ts`; `workspace-contract.ts`; any EXISTING e2e spec |

Everyone: `docs/CHANGELOG.md` + `status-log.md` append-only; the drift
ledger frozen; `shell.ts` / `workspace-contract.ts` frozen; `generators/**`
nobody's. `Cargo.toml` / `Cargo.lock`: version lines only (a dependency add
is a STOP-and-flag). The §C name-for-name diff, the two gate flips and the
`docs/v4/developer/features/character-progressions.md` mirror are UNIFIER
wires.


## Verification gate (before declaring done)

1. `cargo fmt --all --check`; clippy both feature sets; `cargo build
   --workspace`; the release build (host embed).
2. `cargo test --workspace` (background, full log + sentinel — NEVER `|
   tail`) with every oracle/fixture var this order names; `SKIP:` zero for
   the moved families; by-name runs with `-- --nocapture` and case counts.
3. Every moved family by NAME through `recipe_sweep.py --v4 /tmp/qt-v4-pin-
   p4d168-25f534c0b --run <family>` (`--show` each changed header first);
   the changed-bytes greps recorded; `system_prompt_equivalence` + the
   identity-compiler family as NEUTRALITY regens at the `2f4254b42` pin
   (byte-identical — the negative guarantee).
4. The mutation proofs above, run and restored by string edit.
5. `git diff main -- apps/web/ crates/quilltap-core/src/pascal
   crates/quilltap-core/src/tools/run_custom.rs crates/quilltap-core/src/
   api/custom_tools.rs crates/quilltap-web crates/quilltap-host/src` EMPTY;
   `git diff <P4.D167 tip> -- crates/quilltap-core/src/progressions/
   {schema,engine}.rs` EMPTY.
6. Commits per `.claude/commands/commit.md` (core + harness + host bumps;
   CHANGELOG every commit); the lane record in `status-log.md` with the
   read-count finding, the slot placement rows, the greeting/Carina arms,
   and the help re-vendor's file list + counts.
