# Work order P4.6aq — the form-field adoptions rider (lane C): the remaining `qt-markdown-field` swaps + the `minHeight` input

> **STATUS: OPEN.**
>
> Lane C of the THREE-lane P4.6ao ∥ P4.6ap ∥ P4.6aq round (the token/cost +
> background-generation + form-fields round, planned 2026-07-15). **v4
> reference baseline: HEAD `02865bdb`** (`~/source/quilltap-server`) — run
> `git log 02865bdb..HEAD --oneline` at lane start; **if HEAD moved, STOP
> and report.** Sibling lanes: `p4.6ao-token-cost-background-server.md`
> (server — no contact with this lane),
> `p4.6ap-token-cost-background-salon-spa.md` (Salon SPA — disjoint files).
> The **Shared contract** and **Ownership** sections below are binding and
> reproduced verbatim in all three orders. You run in a worktree; the
> unification pass integrates the lanes onto main.

## Mandate

Close the P4.6al item-6 deferral: every remaining v5 form field whose v4
counterpart renders `MarkdownLexicalEditor` gets the shared
`qt-markdown-field`, each a mechanical swap plus the one shared
non-mechanical piece — a `minHeight` input on the field component (v4 passes
`minHeight` at every one of these sites; v5 has no equivalent today). This
lane owns `editor/markdown-field.ts` and exactly the enumerated site files.
It must NOT touch `editor/rich-editor.ts` (escalate if a change there seems
needed), `chat/**`, `screens/salon/**`, or `screens/settings/images/**`.

**A correction to the P4.6al deferral text, survey-verified:** the header
mapped "characters view image-prompt fields" to
`characters/view/tabs/details-tab` — that file is a read-only display tab
with no textarea. The actual editable image-prompt fields are in
`screens/characters/edit/appearance-tab.ts` (its own doc comment cites v4
`DescriptionsTab.tsx`) and the guidelines textarea in
`screens/characters/view/tabs/character-appearance-tab.ts:90`. Target those.

## Survey-verified starting points (2026-07-15 @ `02865bdb` — do not re-derive)

### The component

`apps/web/src/app/editor/markdown-field.ts` (`MarkdownField`, selector
`qt-markdown-field`). Inputs: `value`, `placeholder`, `disabled`,
`ariaLabel`, `recordKey` (v4's `remountKey`). Outputs: `contentChange`
(v4's `onChange`), `imagePaste`. v4's
`MarkdownLexicalEditorProps` (`components/markdown-editor/
MarkdownLexicalEditor.tsx:42-64`) additionally has `minHeight` — **the one
missing input**; `roleplayTemplateId` / `showSourceToggle` / `namespace` are
NOT passed by any site in this batch (standing deferrals, untouched).

### The swap sites (v4 → v5, all line refs at `02865bdb`)

| # | v4 site | v4 `minHeight` | v5 target file | state today |
|---|---------|----------------|----------------|-------------|
| 1 | `app/aurora/[id]/edit/CharacterBasicInfo.tsx:482` (the character-edit **scenario** — the ONE CharacterBasicInfo field P4.6al didn't reach) | — | `screens/characters/edit/scenario-editor.ts:63` (`qt-scenario-editor`, hosted by details-tab:281) | plain textarea |
| 2 | `components/scenarios/ScenarioEditorModal.tsx:217` | — | `screens/scenarios/shared/scenario-editor-modal.ts:123` (doc `:32` records the divergence) | plain textarea |
| 3 | `components/settings/roleplay-templates/index.tsx:312` (systemPrompt) | `14rem` | `screens/settings/templates/template-form-modal.ts:86` | plain textarea |
| 4 | `components/characters/system-prompts-editor/PromptModal.tsx:78` | `12rem` | `screens/characters/edit/prompt-modal.ts:58` | plain textarea |
| 5 | `components/wardrobe/wardrobe-item-editor.tsx:674` (item description) | `10rem` | `screens/prospero/wardrobe/project-wardrobe-manager.ts:92` | plain textarea |
| 6 | `app/prospero/[id]/components/SettingsCard.tsx:68` + `SettingsTab.tsx:35` (instructions) | `14rem`/`10rem` | `screens/prospero/cards/project-settings-card.ts:37` (doc `:14` records the deferral) | plain textarea |
| 7 | `components/settings/AestheticEditorField.tsx:119` (aesthetic instructions, embedded by Prospero) | `8rem` | `screens/prospero/cards/project-aesthetic-field.ts:28` (doc `:13` records the deferral) | plain textarea |
| 8 | `components/new-chat/NewChatForm.tsx:642` (starting scenario) | `6rem` | `screens/new-chat/new-chat-form.ts:218` | plain textarea |
| 9 | `app/aurora/[id]/view/DescriptionsTab.tsx:229-331` (6 image/physical prompts) | — | `screens/characters/edit/appearance-tab.ts:82,91,100,109,118,127` | plain textareas |
| 10 | (same v4 tab — guidelines) | — | `screens/characters/view/tabs/character-appearance-tab.ts:90` | plain textarea |

Already adopted (P4.6al — do NOT re-touch): memory-editor, the character
new/edit prose fields (7 each). Sites whose v5 host DOESN'T EXIST stay
deferred — see Tier 3.

### The spec convention (the P4.6al pattern — follow it exactly)

Per `apps/web/src/app/memory/memory-editor.spec.ts` (mirrored in
`new-character.spec.ts`): query the editor handle via
`By.directive(RichEditor)`; assert seeding with `getMarkdown()`; drive edits
imperatively with `setMarkdown()` (never DOM input events); settle with the
zoneless microtask loop; assert the host's EXISTING save/dispatch payload
carries the identical string (the absorb-once seam means no dirty-on-load —
assert that too where the host has a dirty flag).

## Tiered deliverables

**Tier 1 (must land):**

1. The `minHeight` input on `qt-markdown-field` — a CSS min-height on the
   editable content area, implemented entirely within `markdown-field.ts`
   (host style/custom property; NO `rich-editor.ts` change), defaulting to
   today's rendering so the P4.6al-adopted sites are pixel-unchanged. Unit
   spec.
2. Swap sites 1–8 (the single-field sites), each with the v4 `minHeight`
   value from the table where v4 passes one, each with a spec per the
   convention, each proving the host's save payload unchanged.

**Tier 2 (should land):**

3. Swap sites 9–10 (the seven appearance/image-prompt fields) — same
   treatment; these are the highest-count file (`appearance-tab.ts`) so they
   ride second.

**Tier 3 (explicit loud deferrals — restate, all standing from P4.6al; name
them in the status log):**

- Consumers whose v5 host doesn't exist: the CreateNPC / ComposeMail /
  InsertAnnouncement / AddCharacter dialogs, the Settings prompt-library
  screen (`components/settings/prompts/PromptModal.tsx`).
- The source-mode toggle, `roleplayTemplateId` toolbar awareness, the GFM
  table transformer, `__bold__` on-type — none re-scoped here.

## The differential requirement (restated)

This is an SPA rider over an already-differentialed editor (the P4.6ag
28-entry byte-round-trip gate guards the dialect; do not touch it). The
proof per site is the spec-convention pair: seed → `getMarkdown()` equality,
edit → the UNCHANGED host dispatch payload byte-carrying the markdown. No
oracle work in this lane.

## Fixtures

None consumed, none delivered. If a site's spec needs a stored record,
mock `CoreClient.dispatchData` per the convention — no committed fixture
changes from this lane.

## Shared contract (BINDING — reproduced verbatim in all three orders)

**§1 — the `chatGetCost` verb (lane A provides; lane B consumes).**

- Wire request: `{"type": "chatGetCost", "chatId": "<uuid>", "detailed": <boolean, optional — absent means false>}`.
  Rust: `Request::ChatGetCost { chat_id: String, detailed: Option<bool> }`,
  serde camelCase, appended to the union in
  `crates/quilltap-core/src/api/types.rs`.
- Response: `Response::ChatCost(Value)` carrying v4's breakdown object
  VERBATIM.
  - Non-detailed (v4 `getChatCostBreakdown`,
    `lib/services/cost-estimation.service.ts:139-190`):
    `{"totalTokens", "promptTokens", "completionTokens", "estimatedCostUSD"
    (number|null), "priceSource" ("openrouter"|"registry"|"fallback"|
    "openrouter-estimate"|"unavailable")}`.
  - Detailed (`:196-289`): the same five keys plus
    `"messageBreakdown": [{id, promptTokens, completionTokens, totalTokens,
    cost, source}]` and `"systemEventBreakdown": [{id, type, promptTokens,
    completionTokens, totalTokens, cost, source}]`.
- REST edge: `GET /api/v1/chats/{id}?action=cost[&detailed=true]` returns the
  breakdown **RAW** (v4 `NextResponse.json(breakdown)` — NOT the
  successResponse envelope). Missing chat row → 404 `notFound('Chat')`.
- The SPA client type is `ChatGetCostRequest` in a lane-B-owned api file;
  lane B develops against a route mock until unification.

**§2 — the `chatRegenerateBackground` edge (lane A un-refuses; lane B
consumes).**

- The `Request::ChatRegenerateBackground { chat_id }` variant already exists
  (`types.rs:1832-1842`, wire name `chatRegenerateBackground`) — the variant
  and wire name DO NOT change; only the dispatch target does.
- Success (both arms) is v4's successResponse data:
  `{"message": "Story background regeneration queued" |
  "Story background generation already in progress", "queued": true,
  "jobId": "<uuid>"}`.
- The three badRequest strings, verbatim:
  1. `Story backgrounds are not enabled. Enable them in Settings > Chat Settings > Story Backgrounds.`
  2. `No image profile available for story background generation. Configure an image profile in Chat Settings.`
  3. `No characters in chat to generate background for.`
- Failure fallback: serverError
  `Failed to queue story background regeneration`.

**§3 — merge mechanics.**

- Lane B beats that need lane A's live verbs run ROUTE-MOCKED in-lane and are
  marked `ACTIVATE-AT-UNIFY` in the spec; the unifier drops the mocks and
  runs them live (the established P4.6ab/ac pattern).
- `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` are
  append-only for all lanes (union-merge at unification).
- Version bumps: lane A bumps the crates it touches (core, web, host,
  harness) per commit; lanes B and C each bump the SPA
  `apps/web/package.json` patch version per commit; the unifier accumulates
  and syncs the LOCKFILE's own version fields (the P4.6an lesson).

## Ownership (BINDING — reproduced verbatim in all three orders)

- **Lane A (P4.6ao)** owns `crates/**`, `harness/oracle/**`, and the new
  `cost-background-*` fixture family + generator. It does NOT touch
  `apps/web/**`.
- **Lane B (P4.6ap)** owns `apps/web/src/app/chat/**` (message-row,
  conversation-header + their specs), `apps/web/src/app/screens/salon/**`,
  `apps/web/src/app/screens/settings/images/**`, the new shared UI it
  creates (token badge / cost summary / format-tokens util under
  `apps/web/src/app/ui/` or a salon-local dir), its own new e2e spec files,
  and `apps/web/e2e/global-setup.ts` seeding edits. It does NOT touch
  `apps/web/src/app/editor/**`, any lane-C site file, or
  `apps/web/src/app/chat/render/**` (the standing HANDS-OFF render
  pipeline).
- **Lane C (P4.6aq)** owns `apps/web/src/app/editor/markdown-field.ts`
  (+ spec) and exactly its enumerated site files (character-edit
  scenario-editor, scenarios scenario-editor-modal, settings
  template-form-modal, characters prompt-modal, characters edit
  appearance-tab, characters view character-appearance-tab, prospero
  project-wardrobe-manager / project-settings-card /
  project-aesthetic-field, new-chat-form — plus their specs). It does NOT
  touch `apps/web/src/app/chat/**`, `screens/salon/**`,
  `screens/settings/images/**`, or `editor/rich-editor.ts` (a needed
  rich-editor change is an escalation, not a quiet edit).
- Everyone: `docs/CHANGELOG.md` and `status-log.md` append-only.

## Verification gate (before declaring the lane done)

1. `ng test` (from `apps/web`) — zero failures, new specs included; the
   P4.6ag byte-round-trip gate untouched-green.
2. `ng build` — clean.
3. Full Playwright green in the worktree (this lane adds no beats but its
   swaps sit under existing walks — the new-chat and characters flows must
   stay green).
4. Per-commit rule: follow `.claude/commands/commit.md` (version bumps,
   CHANGELOG every commit, status-log append).
