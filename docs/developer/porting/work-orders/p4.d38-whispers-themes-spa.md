# Work order P4.D38 — whispered announcements SPA, whisper-visibility narrowing, themes WCAG, and the flushSync disposition

> **STATUS: OPEN — not started (ordered 2026-08-01).**
>
> **v4 baseline for this round: `c4d4b0de`** (v4 HEAD, 2026-08-01, clean
> tree; ten commits past `ff12f491` — full classification in the "Round
> planned" record in `status-log.md`). This lane owns the SPA halves of
> `a163862c` (whispered announcements) and `424a7381` (whisper-kind
> narrowing + operator-authored announcements), BOTH theme commits
> (`0ec25bdd` light-mode WCAG, `1c55110b` dark-mode WCAG), and the
> `4f7e09fa` flushSync NO-PORT disposition. **The baseline move is the
> UNIFIER's** — do NOT edit `CLAUDE.md` or `phase-4.md`; put replacement
> wording in the lane record.
>
> **Drift-check at lane start**: `git log c4d4b0de..HEAD --oneline` +
> `git status --short` in `~/source/quilltap-server`. On drift/dirty, pin
> `/private/tmp/qt-v4-pin-c4d4b0de` (`oracle-regen-pinned-v4-worktree`)
> and report.
>
> **Siblings (same round):** P4.D37 owns the server halves and the §1
> `api/types.rs` addition this lane mirrors into `core-contract.ts` — the
> §1 block is binding and identical in both orders. P4.D35/D36 (Pascal),
> P4.D39 (wardrobe), P4.D40 (editor). See "Shared contract + Ownership".
> Your live e2e beats need P4.D37's server on the branch under test — the
> beats are ACTIVATE-AT-UNIFY unless you verify against a merged sibling;
> unit/spec coverage is yours regardless.
>
> **Worktree note:** own branch, own checkout; no `git stash`/`git worktree`.

## Mandate

Port the Insert Announcement audience UI ("Who hears it"), the collapsed
chip's whisper tag + border, the whisper-visibility re-port (both commits:
the operator-authored-announcement rule AND the sender→kind narrowing with
its legacy arm), the a163862c CSS block, the theme WCAG value updates, and
record the `4f7e09fa` NO-PORT disposition. Do NOT touch: `crates/**`,
`apps/web/src/app/pascal/**` / `screens/custom-tools/**` /
`custom-tools-popup.ts` / `system-message-labels.ts` (P4.D36's),
`apps/web/src/app/editor/**` (P4.D40's), or the wardrobe SPA files
(P4.D39's).

## Survey-verified starting points (2026-08-01)

**v4 (at `c4d4b0de`):**

- `components/chat/InsertAnnouncementDialog.tsx` (+128): `AudienceCandidate
  {participantId /* CHAT PARTICIPANT id */, name, controlledBy, avatarUrl,
  status}`; `audienceCandidates?` prop (default []); `audience: string[]`
  state (empty = public); the "Who hears it" checkbox group (avatar + name
  + "(you)" for user-controlled + "(silent)"/"(absent)" status tags),
  rendered only when candidates exist; helper text — public: "Everyone
  present hears this…", whisper: "Whispered to {names}. No other character
  receives it in their context." + a "Make it public" reset;
  **`toggleAudience` resets `stage → 'compose'` and clears
  `proposedMarkdown`** (an audience change invalidates the on-screen
  proposal — so does the reset); both POST bodies gain
  `targetParticipantIds: audience.length > 0 ? audience : null`; labels
  flip — "Post Whisper", "Whispered announcement posted" toast, seed label
  "What you want the character to say privately".
- `ChatModals.tsx` (+9): candidates = `chat.participants` filtered
  `p.type === 'CHARACTER' && !p.removedAt && p.status !== 'removed' &&
  p.character`, mapped `{participantId: p.id, name, controlledBy ?? 'llm',
  avatarUrl, status}`.
- `AnnouncementChip.tsx` (+51): `WhisperTag` — `to {names}`
  (`participantNames?.[id] || 'unknown'`, comma-joined), class
  `qt-chat-system-bar-whisper`, `title="Whispered to {names}"`, `sr-only`
  "whispered " prefix; nothing when public. The chip gains class
  `qt-chat-announcement-chip-whisper` when
  `!!message.targetParticipantIds?.length`; the expanded header renders
  the tag between kind and time; `participantNames` threads through
  MessageRow (+12) and VirtualizedMessageList (+5).
- VirtualizedMessageList (+5, a163862c): the overheard-dim predicate gains
  `!isOperatorAuthoredAnnouncement(message)` ("An announcement the
  operator wrote themselves is not overheard by definition").
- `app/salon/[id]/whisper-visibility.ts`:
  - a163862c: `isOperatorAuthoredAnnouncement(msg)` = `msg.systemKind ===
    'announcement'` (set by exactly one writer → the operator-authored
    marker; never hidden, never dimmed); `isMessageVisibleToOperator`
    widens its Pick to include `systemKind` and returns true for it after
    the showAllWhispers check.
  - 424a7381: **`OPERATOR_FACING_WHISPER_SENDERS` (set) is DELETED**;
    replaced by `OPERATOR_FACING_WHISPER_KINDS: ReadonlyMap<SystemSender,
    ReadonlySet<string>>` = `pascal → {'custom-tool-result'}`, `prospero →
    {'tool-run','custom-tool-error','carina-error'}`; private
    `isOperatorFacingStaffWhisper`: no sender → false; sender not in map →
    false; **`!msg.systemKind` → true (legacy rows keep the OLD
    sender-level behavior — the easy port bug)**; else
    `kinds.has(msg.systemKind)`. Effect: Prospero `group-context` whispers
    (the highest-volume whisper) now follow the All Whispers toggle.
  - The final jest suite (both commits) is the pin inventory — port it
    case-for-case into the v5 spec (arms listed in the planning survey:
    incl. the legacy `systemKind: null` pascal arm, the whispered
    announcement shown for Staff signers AND `systemSender: null`, the
    human-authored whisper, group-context-to-human).
- `_chat.css` (+27): `.qt-chat-system-bar-whisper` (color
  `var(--qt-chat-whisper-label-fg)`, italic, ellipsis);
  `.qt-chat-announcement-chip .qt-chat-system-bar-whisper { max-width:
  18ch }`; `.qt-chat-announcement-chip-whisper { border-color:
  var(--qt-chat-whisper-label-fg) }`.
- **Themes (`0ec25bdd` + `1c55110b`)** — v5 carries the OLD failing values
  everywhere except Madman's Box. The exact value table (v5 path:line →
  old → new) is in the planning survey (round record); summary:
  `_variables.css:294` `#7c6fa0`→`#5c447e` (+ WCAG comment), `:855`
  `#a89cc8`→`#b5a9d1`; `art-deco/styles.css:186` (the whisper-bg
  color-mix rework, + why-comment) and `:188` label
  `hsl(270 30% 55%)`→`hsl(270 50% 22%)`; `earl-grey/styles.css:992`
  →`hsl(270 25% 44%)` and base block `:430` →`hsl(270 20% 70%)`;
  `great-estate/styles.css:190` →`hsl(280 35% 26%)`;
  `old-school/styles.css:151` →`hsl(270 45% 36%)`;
  `rains/styles.css:1042` →`hsl(240 18% 43%)` and base block `:457`
  →`hsl(240 18% 68%)`. theme.json bumps to v4's numbers: art-deco
  1.0.11, earl-grey 1.3.7, great-estate 1.0.6, old-school 1.0.12, rains
  1.3.10. Carry v4's comments; `dist/` is build output — never hand-edit.
- `4f7e09fa` (flushSync): **NO-PORT, evidence in hand** — v5 uses TanStack
  Virtual via `@tanstack/angular-virtual` 5.0.7, and its measurement site
  (`src/app/chat/virtual-row.ts:29`, `afterNextRender(() =>
  this.virtualizer().measureElement(...))`) is ALREADY deferred past
  change detection — precisely the shape v4 moved to; Angular has no
  flushSync and the ResizeObserver path is untouched. Record the
  disposition (lane record + a code comment only if a natural anchor
  exists).

**v5 (apps/web/src/app):**

- `chat/post-office/insert-announcement-dialog.ts` (576; `stage`/
  `proposedMarkdown` signals :352-353; input `participantCharacterIds`
  :332; **no audience section**). Mounted at
  `screens/salon/salon-conversation.ts:499-506` (no candidates input —
  build them from the chat per v4's ChatModals filter).
- `chat/post-office/post-office.api.ts` — `postAnnouncement` :97,
  `previewAnnouncement` :117; neither sends the field. Wire mirror:
  `core-contract.ts:169-187` (your named addition, §1).
- `chat/announcement-group.ts` (200; chips render dot/sender/kind/time
  :45-66; expanded body :79-100; **no whisper tag/class**). Chip VM
  carries the full `MessageDto` (`chat-view-model.ts:261-268`) and the
  group has a `chat` input (:112) — audience names resolvable without new
  plumbing (participant id → character name; `message-row.ts:551-557`
  already builds such a map for ordinary whispers — reuse the pattern).
- **v5 divergence with TWO render sites:** `isAnnouncementChip`
  (`chat-view-model.ts:299-305`) requires `systemSender != null`, so a
  character/custom-announcer announcement renders as a normal MessageRow,
  not a chip. The whisper tag must land on BOTH: the chip/group
  (Staff-signed) and the MessageRow path (the whisper label via
  `variant()` engages once `targetParticipantIds` is set — verify, and add
  the audience naming where v4 shows it).
- `chat/whisper-visibility.ts` — carries the OLD pre-drift code
  (`OPERATOR_FACING_WHISPER_SENDERS` set at :23;
  `isMessageVisibleToOperator` :43 without `systemKind`). Spec:
  `whisper-visibility.spec.ts` (5 pre-drift arms — grows to the full v4
  inventory). Consumer: `salon-conversation.ts:66,1490`.
- **Pre-existing v5 gap you inherit:** `.qt-chat-message-whisper-overheard`
  (`_chat.css:79-81`) has ZERO TS appliers — v4's overheard-dim predicate
  was never ported, so a163862c's `!isOperatorAuthoredAnnouncement`
  addition has no landing site. EITHER port the predicate (VirtualizedMessageList.tsx:345-365 at the pin, with the announcement
  exclusion, onto the message list/row) OR defer loudly by name in the
  lane record + order header. Porting it is tier 2 — do not let it block
  tier 1.
- `core-contract.ts:2417,2422` — `MessageDto` already carries
  `targetParticipantIds` and `customAnnouncer`; the server read already
  emits both. Read path needs nothing.
- e2e: `apps/web/e2e/salon-post-office-flow.spec.ts` (P4.9E2B beats),
  `salon-announcement-rendering.spec.ts` (P4.26's audit beat).

## Tiered deliverables

**Tier 1 (must land):**

1. The dialog audience section (candidates input built in
   salon-conversation per v4's filter; checkbox group with the status
   tags; helper text; the proposal-invalidation rule on toggle AND reset;
   the label flips; both API calls carrying `targetParticipantIds` with
   empty→null).
2. The whisper tag on BOTH render sites (chip/group + MessageRow), the
   chip-whisper border class, the `participantNames` threading, and the
   a163862c CSS block (your named `_chat.css` section).
3. `whisper-visibility.ts` re-port: `isOperatorAuthoredAnnouncement` +
   the sender→kind map narrowing with the legacy `!systemKind` arm; the
   spec grown to v4's full case inventory (the legacy arm and the
   `systemSender: null` whispered-announcement arm are the two easy port
   bugs — pin both).
4. The theme WCAG updates (all files in the survey table, with v4's
   comments and theme.json versions).
5. `core-contract.ts` §1 mirror (your named addition).
6. e2e: a whisper-announcement beat (open dialog → pick one hearer → post
   → the chip shows "to {name}" and the whisper border; toggle All
   Whispers off → the whispered announcement STAYS visible to its
   operator author). ACTIVATE-AT-UNIFY if the server half isn't on your
   branch; gate by NAMED constant, never a capability probe.

**Tier 2 (should land):**

7. The overheard-dim predicate port (the pre-existing gap above) with the
   announcement exclusion — or the loud deferral.
8. The `4f7e09fa` NO-PORT disposition recorded (lane record wording ready
   for the unifier's round bullet).

**Tier 3 (explicit deferrals, loud):** the overheard-dim port if deferred
(name it in the status header at close).

## The differential requirement, restated

This is a SPA lane: the equivalence machinery is the case-for-case v4 spec
ports (whisper-visibility especially — v4's final jest suite at the pin is
the oracle inventory), the wire-mirror name-for-name diff at unification,
and the live beats. Where a v4 behavior is pinned by a v4 test, the v5
spec must pin the same arm; mutation-prove any first-run-green spec (break
the map lookup, watch the legacy arm fail, restore).

## Fixtures

- CONSUMES: the committed e2e instance (whisper beats need a
  multi-character chat — the existing salon e2e casts suffice; if a beat
  needs a soft-removed participant, seed it in the beat, not the fixture).
- DELIVERS: none.

## Shared contract + Ownership (binding, identical in all six orders)

- **Round baseline `c4d4b0de`** (v4 HEAD, 2026-08-01, clean tree; ten
  commits past `ff12f491`). Drift-check at lane start (see preamble); on
  drift/dirty, pin `/private/tmp/qt-v4-pin-c4d4b0de` per
  `oracle-regen-pinned-v4-worktree` (incl. `plugins/node_modules` +
  per-plugin `plugins/dist/*/node_modules`). SDK majors: openai 7,
  `@openrouter/sdk` 1.2. TZ pins apply to every distill-transitive family
  (TZ=UTC + the named Chicago legs). Committed-fixture families: point
  oracles at the committed DBs, never a rebuild; families whose recipes
  MUTATE committed fixtures run against /tmp COPIES
  (`recipe_sweep.py --run` handles this).
- **The baseline move (`CLAUDE.md`/`phase-4.md`) is the UNIFIER's.**
- **File ownership (exclusive unless noted):**
  - **P4.D35** → `crates/quilltap-core/src/pascal/**`,
    `crates/quilltap-core/src/tools/run_custom.rs`,
    `crates/quilltap-core/src/services/pascal_writer.rs`,
    `crates/quilltap-core/src/api/custom_tools.rs`,
    `crates/quilltap-core/src/db/chats_messages.rs`, the
    `pascal-run-custom-{main,mount}.db` fixture family (+ sidecar), the
    pascal/tool/workbench oracle families named in its order, and the
    `docs/v4` pascal feature-doc mirror.
  - **P4.D36** → `apps/web/src/app/pascal/**`,
    `apps/web/src/app/screens/custom-tools/**` (incl. `workbench.api.ts`),
    `apps/web/src/app/chat/custom-tools-popup.ts` +
    `custom-tool-params-form.ts`,
    `apps/web/src/app/chat/system-message-labels.ts` (+ spec),
    `apps/web/src/testing/fixtures/pascal-custom-tool-definition.oracle.ndjson`,
    `apps/web/public/schemas/qtap-custom-tool.schema.json`, and the
    workbench/custom-tools e2e specs +
    `apps/web/e2e/support/seed-pascal-tools-fixture.ts`.
  - **P4.D37** → `crates/quilltap-core/src/services/announcer/**` (incl.
    the new audience module), `crates/quilltap-core/src/services/
    message_context.rs`, the new announcement-attribution module,
    `crates/quilltap-core/src/api/chat_post_office.rs`, the
    `post-office-{main,mount}.db` fixture family, the §1 addition named
    below, and the post-office/announcer/context-transitive oracle
    families (orchestrator, salon-swipe, regenerate-swipe, enclave-step,
    file-attachment, salon reads/mutations/skip, message-context leaves).
  - **P4.D38** → `apps/web/src/app/chat/post-office/**`,
    `apps/web/src/app/chat/{announcement-group,whisper-visibility,
    message-row,message-list,virtual-row,chat-view-model}.ts` (+ specs),
    `apps/web/src/app/screens/salon/salon-conversation.ts`,
    `apps/web/src/styles/qt-components/_variables.css`,
    `apps/web/public/themes/**`, and the post-office/announcement e2e
    specs.
  - **P4.D39** → `crates/quilltap-core/src/services/{outfit_selections,
    chat_merge,chat_participants,chat_create}.rs`,
    `crates/quilltap-core/src/db/{wardrobe_read,archetype_wardrobe,
    chats_outfits}.rs`, `crates/quilltap-core/src/wardrobe.rs`,
    `crates/quilltap-core/src/tools/{wardrobe_shared,wardrobe_list}.rs`,
    the new wearable-pool module, `crates/quilltap-core/src/api/
    chat_cast.rs` (call sites only, if needed),
    `apps/web/src/app/wardrobe/{equipped-slots,wardrobe-item-editor}.ts`
    (+ specs), `apps/web/src/app/screens/prospero/wardrobe/
    project-wardrobe-manager.ts`, the `docs/v4` wardrobe feature-doc
    mirror, and the wardrobe/llm-choose/chat-cast/chat-admin/chat-create
    oracle families.
  - **P4.D40** → `apps/web/src/app/editor/**` exclusively (+ its named
    `_chat.css` section).
- **`crates/quilltap-host/src/spine.rs` is SHARED at named sites:** P4.D37
  may touch ONLY the announcement-preview runner impl (`:545` region);
  P4.D39 ONLY the outfit/chat-create sites. No other lane touches it.
- **`api/types.rs` is FROZEN except P4.D37's one named addition:**
  `target_participant_ids: Option<Vec<String>>` (serde
  `targetParticipantIds`, skip-if-None) on `ChatAnnouncementPost` (:2398)
  AND `ChatAnnouncementPreview` (:2406). Any other lane finding a wire
  need: STOP and escalate to the human.
- **`apps/web/src/app/core/core-contract.ts` is additive-only, two named
  additions:** P4.D38 mirrors `targetParticipantIds?: string[] | null`
  onto `ChatAnnouncementPostRequest`/`ChatAnnouncementPreviewRequest`
  (:169-187); P4.D36 adds `chipLabel?: string` + `effects?:
  Array<{target: string; previous?: unknown; next: unknown; tier?:
  string}>` to the message `pascalMeta` type. Disjoint by name; the
  unifier diffs the merged contract name-for-name against `api/types.rs`
  and the server wire.
- **`apps/web/src/styles/qt-components/_chat.css` is append-only, two
  named sections:** P4.D38 appends v4 a163862c's whisper-tag block
  (`.qt-chat-system-bar-whisper`, the chip max-width, the chip-whisper
  border); P4.D40 appends the indent-button sizing rule. Neither edits
  existing rules.
- **The pascal chip/heading label precedence** (`chipLabel → toolTitle →
  tool`) is P4.D36's, implemented in `system-message-labels.ts`. If it
  turns out to require an edit in `announcement-group.ts` (P4.D38's file),
  do NOT edit it — put the exact diff in the lane record for the unifier.
- **Version bumps:** D35/D37/D39 bump `quilltap-core`/`quilltap-harness`
  (+ `quilltap-web`/`quilltap-host` where touched) per commit; D36/D38/D40
  bump the SPA version only. The unifier recounts per the parallel-round
  playbook.
- **`CHANGELOG` + `status-log.md` are append-only** (union-merge at
  unification).

## Verification gate (before declaring done)

- `ng test` green (whisper-visibility, dialog, chip specs by name);
  `ng build` clean.
- Full Playwright suite green, zero unexplained skips; new beats ACTIVE or
  ACTIVATE-AT-UNIFY behind a NAMED constant.
- Per-commit rule: `.claude/commands/commit.md`. **⛔ No `pgrep` poll
  loops** — `run_in_background` + the completion notification only.
- Lane record appended to `status-log.md` (the flushSync NO-PORT
  disposition wording, the overheard-dim decision, the baseline-wording
  slice for the unifier).
