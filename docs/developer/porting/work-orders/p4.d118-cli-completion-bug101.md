# Work order P4.D118 — the `6afacb18` CLI drift: shell completion survives flags on the line (bug 101), plus the `8f910137` NO-PORT evidence

**Status:** CLOSED — UNIFIED on main (2026-08-25, lane branch
`claude/p4-cli-completion-bug-5484ec`, unified on `unify/8f910137-round`).
Both Tier-1 deliverables landed: the three templates re-copied at v4
`6afacb18`'s bytes (byte-identical, verified at unification against v4's
files; neither side substitutes anything — the anticipated Tier-3 deferral
never arose), Tier R red-first (exactly the three completion cases, then
188/0), and the v5-side behavioural guard
(`crates/quilltap-cli/tests/completion_behavior.rs`, the union of BOTH v4
test vintages, 13 bash + 4 zsh arms, red-proven against the pre-fix
templates — v5 measurably HAD bug 101's bash half). The `8f910137` NO-PORT
evidence is in the lane record and was RATIFIED at unification (drift-ledger
§6): CI + tests-only, the +18 test lines absorbed by the guard, no
behavioural hunk under `lib/`/`app/`/`packages/`/`plugins/`. Note: the CLI
Tier R driver lives in `crates/quilltap-cli/tests/`, not the harness crate,
so the harness version did not move. Lane record: `status-log.md` →
"P4.D118".

## Preamble

- **v4 baseline for this round: `f6a10055`** (v4 main). The round absorbs the
  FIVE commits past it (drift-ledger §3); this lane carries `6afacb18`
  (bug 101 — the three completion templates rewritten) and gathers the
  ratification evidence for `8f910137` (the NO-PORT? CI row). Siblings:
  P4.D115/P4.D116 (the scenario feature), P4.D117 (the client fixes) — all
  fully disjoint from this lane. At planning the v4 checkout was on `main`,
  tree CLEAN, HEAD `8f910137`; `bugfix` has no unabsorbed content beyond the
  tests-only `009c49b2`.
- **Run the drift ledger's §2 freshness probe at lane start**
  (`docs/developer/porting/drift-ledger.md`). Probe failure = **STOP and
  report**; a lane never writes the ledger.
- **Regen rule in force: pin required.** Tier R drives v4's REAL launcher —
  run it from a lane-unique detached worktree pinned at `6afacb18` (which
  contains all of `f6a10055`; `8f910137` changes no template), all three
  symlink classes (ledger §5.1). ⚠ The Tier R oracle needs
  `packages/quilltap/node_modules` — the second symlink class — or the
  launcher dies at require time.
- **Worktree:** per the `carryout` skill; edit inside the worktree.
  `docs/CHANGELOG.md` and `status-log.md` append-only.

## Mandate

Re-byte-copy v4's three rewritten completion templates into
`crates/quilltap-cli/src/help/completion/` and prove the copy through Tier R
red-first (the P4.D68/P4.D97 precedent: the templates are byte-copied assets,
and Tier R's comparison against v4's REAL launcher output is the
differential). Mirror v4's new behavioral guard — `completion-behavior.test.js`
drives the bash template FOR REAL (source the script, set
`COMP_WORDS`/`COMP_CWORD`, read `COMPREPLY` back, against a stub `quilltap`
on PATH) — as a v5-side test over v5's SHIPPED template bytes, so the
behavior the templates exist for (completion still fires once flags are on
the line) is pinned on our side too, not only the bytes. Gather the
`8f910137` NO-PORT evidence for `/unify`'s ratification.

**Must NOT touch:** anything outside `crates/quilltap-cli/**` and the
Tier R files in `crates/quilltap-harness/**`.

## Survey-verified starting points (fresh survey 2026-08-25, at `6afacb18`)

⚠ Port from the shipped hunks, never the commit message (ledger §5.3). Read
v4's write-up: `git show 6afacb18:docs/developer/bugs/fixed/bug-101-completion-counts-words.md`
(182 lines — the mechanism note).

### v4 — `6afacb18`

- **`packages/quilltap/lib/completion/bash.template`** (+131/−…): the verb is
  now found by PARSING the words (skipping flags and their values), not by
  counting positions. New helpers to carry whole:
  - `_quilltap_lines_compreply` — fills `COMPREPLY` from newline-separated
    candidates (store/instance names routinely contain spaces —
    `compgen -W` would chop them), `printf '%q'`-escaped, prefix-filtered.
  - `_quilltap_docs_positional` — which docs positionals name a store vs a
    local path; `move|copy|link` take `<srcMount> <srcPath> <dstMount>
    <dstPath>` (store wanted at 2 AND 4); `export`'s arg 3 is a local dir
    (`compgen -d`); the `store_verbs` list.
- **`zsh.template`** (+432/−…): the largest rewrite — same flag-tolerant
  verb lookup in zsh's idiom. **`fish.template`** (+6): the smallest.
  Byte-copy all three post-commit files verbatim
  (`git show 6afacb18:packages/quilltap/lib/completion/<x>.template`);
  verify `8f910137` touched none of them
  (`git diff 6afacb18 8f910137 -- packages/quilltap/lib/completion/` must be
  empty — confirm, don't assume).
- **`packages/quilltap/lib/__tests__/completion-behavior.test.js`** (NEW,
  146 lines; `8f910137` adds +18 more — read BOTH vintages, mirror the
  union): bash driven for real via a stub bin answering
  `instances list --names-only` → `StubInstance` and
  `docs list --names-only` → `Stub Store\nOther Store`; zsh checked
  structurally (its completion system only runs inside a widget). The
  headline case: `quilltap docs --instance Friday <TAB>` must offer verbs.
- **`completion-template.test.js`** (±6): the static coverage test's tweak.

### v4 — `8f910137` (the NO-PORT? row)

`.github/workflows/ci.yml` (+7 — the zsh check gated on zsh being
installed) + `completion-behavior.test.js` (+18, tests-only) + version
bumps. v5 has no zsh CI check and does not run v4's jest suite. Evidence to
record for ratification: the file list, the absence of any `lib/`/`app/`
/`packages/` behavior hunk beyond the test, and the note that the +18 test
lines are absorbed into this lane's mirrored guard (so nothing is lost).

### v5 — starting points (verified 2026-08-25)

- Templates: `crates/quilltap-cli/src/help/completion/{bash,fish,zsh}.template`
  (byte-copied at P4.D68, extended P4.D97). Loader: `completion_cmd.rs`.
  **Survey whether v5 substitutes anything into the templates at
  render time** (P4.D68's port may have carried v4's `{{...}}` markers or
  none) before assuming a pure byte-copy suffices.
- Tier R: the CLI differential in `crates/quilltap-harness` driving v4's
  real `npx quilltap` launcher — 188/0 at last count; the completion
  commands are cases in it (P4.D68 ran them red-first 3-by-name). Find the
  family via `ggrep -rln "completion" crates/quilltap-harness/`.
- The CLI's JSON-output conventions: memory `quilltap-cli-json-output.md`.

## Tiered deliverables

**Tier 1 (must land):**

1. The three templates re-copied at the `6afacb18` bytes.
2. Tier R red-first: the completion cases re-run BEFORE the copy (they must
   go red against the pinned v4 launcher — if they don't, the case corpus
   is not covering the changed bytes; widen it first), then green after.
   Full Tier R suite back to N/0.
3. The v5-side behavioral guard mirroring v4's `completion-behavior.test.js`
   union (both vintages): a Rust `#[test]` (or harness test) that writes the
   shipped bash template to a temp dir, sources it under `bash` with the
   stub `quilltap` on PATH, sets `COMP_WORDS`/`COMP_CWORD`, and asserts
   `COMPREPLY` — including the flags-on-the-line headline case and the
   space-bearing store-name case. Zsh: the structural checks, as v4 does.
   Skip-with-a-loud-name where `bash` is absent (never a silent pass).

**Tier 2 (should land):**

4. The `8f910137` NO-PORT evidence block appended to the lane record for
   `/unify` to ratify (this lane gathers; `/unify` sets the disposition).

**Tier 3 (explicit loud deferrals):**

5. None expected. If v5's template loader turns out to substitute markers
   the new templates moved (survey item above), and the fix is larger than
   this lane, defer loudly with the measurement.

## The differential requirement, restated

The templates are byte-copied assets; the differential is **Tier R against
v4's REAL launcher at the pin** (red-first, then N/0), plus the mirrored
behavioral guard proving the bytes actually complete under a real bash. No
jest/tsx oracle case is written for this lane — v4's own new test is the
model for the v5-side guard, not an oracle to drive (Tier R already drives
the real thing).

## Fixtures

None consumed or delivered. The stub bin is built by the test itself in a
temp dir (v4's `makeStubBin` pattern).

## Shared contract

None — this lane is fully disjoint.

## Ownership (BINDING — identical table in all four orders)

| Lane | Owns (exclusive) | Version bumps |
|---|---|---|
| P4.D115 | `crates/quilltap-core/**`, `crates/quilltap-web/**` (only if the Tier-3 REST edge lands), `harness/oracle/cases/**` + `crates/quilltap-harness/**` for the chat families it touches | core, harness, web (only if touched) |
| P4.D116 | `apps/web/src/app/screens/new-chat/**`, `apps/web/src/app/chat/sidebar/**`, NEW `apps/web/src/app/scenario/**`, `apps/web/src/app/core/core-contract.ts`, its e2e specs | SPA |
| P4.D117 | `apps/web/src/styles/**`, NEW `apps/web/scripts/check-qt-classes.mjs` + lint wiring, `apps/web/src/app/images/**`, the qt-class sweep over `apps/web/src/app/**` EXCLUDING P4.D116's three dirs above | SPA (unifier recounts) |
| P4.D118 | `crates/quilltap-cli/**`, the Tier R files in `crates/quilltap-harness/**` | cli, harness (Tier-R files) |

`docs/CHANGELOG.md` + `status-log.md` append-only for everyone; nobody edits
`drift-ledger.md` from a lane. Harness version: both P4.D115 and P4.D118
bump it for their own commits; the unifier recounts.

## Verification gate (before declaring done)

- `cargo fmt --check`; `cargo clippy` both feature sets; full
  `cargo test --workspace` (background, full log capture, sentinel — never
  `| tail`, never a pgrep loop).
- Tier R by name at the lane pin, red-first evidence recorded, final N/0.
- Per-commit rule: `.claude/commands/commit.md`. Lane record appended to
  `status-log.md`.
