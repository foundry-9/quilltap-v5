# Work order P4.90 — the two pre-existing findings the `ffb6b3119` §3 review surfaced, closed with evidence: (a) the tool loops' re-stream carries the PRE-failover `base_params.model` to the post-failover provider (`orchestrator.rs:2789` built before the primary stream, never rebuilt after the `:2861` `effective_profile` refresh; v4 hands the loops `streaming.effectiveProfile` and its funnel reads `connectionProfile.modelName` per call) — a failover-then-tool-call corpus arm in `orchestrator_tier3` RED-FIRST, then the fix; (b) the SIX `autoGenerateFirstMessage` log lines v5 never carried (`Connection profile is missing its API key`, `Failed to build first message context`, `Failed to build recent-conversations block for greeting`, `Retrying greeting generation without memories`, `Greeting generation succeeded on retry without memories`, `Greeting generation succeeded on final retry`) capture-pinned at v4's sites with their silence legs — the #103/#110 class; no v4 drift; from `main`

**Status: CLOSED (lane complete, awaiting unification).** Ordered 2026-09-16,
the `2075242f9` bug-145/146 drift catch-up + maintenance round (P4.D192 ∥
P4.D193 ∥ P4.D194 ∥ P4.89 ∥ P4.90). Carried out 2026-09-16 on
`claude/p4-90-failover-greeting-lines-ed0fde`, branched from `main`
`e9173924`. The drift-ledger §2 probe PASSED at lane start and again before
every regen batch; the lane never wrote the ledger. **Every Tier-1 item (1–5)
and both Tier-2 items (6–7) landed.** Tier 3's two deferrals stand.

**What the measurements changed.** (a) Tier-1 item 2's question — which
`params` fields follow the refresh — is answered **`model` and nothing else**,
with v4's line numbers carried as a code comment at the fix site: v4's funnel
derives only the provider, the base URL and `connectionProfile.modelName` from
the profile per call, and its `modelParams` bag is computed ONCE before the
primary stream and never recomputed, so v4's own loops carry the PRE-failover
`parameters` and v5 must keep `profile_parameters` + the three sampling knobs
stale to match. The order's `understudy_answers`-shaped premise for the new
case was **REFUTED by measurement**: v4 runs `attemptEmptyResponseRecovery`
*after* the tool loops (`orchestrator.service.ts:1619` vs `:1499`), so an
empty-response failover can never reach a re-stream; the arm is a HARD-ERROR
failover (`attemptHardErrorFailover`, inside `runPrimaryStream`) instead.
(b) The recent-conversations warn is NOT a no-port: v5's helper is infallible
by signature, so the line sits at the swallowed read inside it — the greeting
is the only caller on either side, so the two are one site. All six lines are
unit-pinned; the capstone carries no capture layer (the P4.D190 precedent),
which item 4's "where a case reaches it" clause resolves to.

**Two harness repairs rode along, neither a port bug** (both exposed by the
new arm, both recorded in the lane record): the oracle's `tool` role was filed
as `user` by `to_completion_messages`' catch-all, so the expected canned key
for ANY tool-loop re-stream could never match v5's — the corpus had never
carried a `tool` role; and a canned-stream miss reported only a message COUNT,
naming neither half of the two-step diagnosis it was asked to support (it now
prints the provider, model, temperature and every message's role, length and
content head — which is what turned the second half of this lane's diagnosis
from a guess into a read).

**Tier-2 item 7, discharged:** P4.D190's header records "SIX more v4 log lines
in `autoGenerateFirstMessage` stay unported" — all six are now on this lane's
branch. The unifier updates that header; this lane does not own it.

**Deferred loud (Tier 3, both stand):** a loop-side rebuild is NOT this lane's
(the caller-side rebuild is the ordered shape and it is what landed); the live
💸 proof — a real cross-provider failover followed by a tool call on the
Friday copy, with `combined.log` showing the re-stream against the
understudy's model — is banked for the dogfood pass. **Also deferred, with its
measurement:** the optional `failover_then_text_tool_pass` arm. v5 passes ONE
`params` value to BOTH loop-option sites (`:2971` native, `:3049` text), so
the ordered rebuild is a single statement that the native arm already pins;
a text arm would need a second failover chain onto the text-block profile to
re-prove the same statement. Recorded rather than built.

## Preamble

- **v4 oracle baseline: `ffb6b3119`.** This lane absorbs NO drift row — it
  is candidate 3 (a) + (b) of the `ffb6b3119` round's "What is next"
  (phase-4.md), both recorded by the §3 review as "pre-existing, outside
  every lane's ownership". v4's shape for both is at the BASELINE (nothing
  in `23abc1ba1`/`2075242f9` touches `orchestrator.service.ts`, the tool
  loops, or `app/api/v1/chats/route.ts`), so every regen is at the
  BASELINE pin.
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** Pin **`/tmp/qt-v4-pin-p490-
  ffb6b3119`** for `orchestrator_tier3` (its jest oracle + the fixture
  builder), `chat_create_capstone`, `native_tool_loop_tier3` /
  `text_tool_loop_tier3` (neutrality), `initial_greeting_equivalence`
  (neutrality).
- **Sibling lanes:** P4.D189's wrap census (`stream_watchdog_wrap_census.
  rs`) counts `watch_stream(` per file — you add NONE (the census must not
  move; run it). P4.D192 / P4.D193 / P4.D194 / P4.89 never touch
  `orchestrator.rs`, `chat_create.rs` or the orchestrator/capstone
  families. You never touch `native_tool_loop.rs` / `text_tool_loop.rs` /
  `primary_stream.rs` / `provider_failover.rs` / `initial_greeting.rs` —
  if the fix genuinely needs a loop-side change, STOP and report with the
  measurement (the ordered shape is a caller-side rebuild).
- **Worktree:** `git worktree add <path> -b claude/p4-90-failover-model-
  greeting-lines-<hash> main`; `CARGO_INCREMENTAL=0`.
- **Read before the work:** `crates/quilltap-core/src/services/
  orchestrator.rs:1380-1490` (`effective_profile` born at `:1386` from the
  connection profile; the Concierge reroute at `:1464`), `:2170-2230`
  (`effective_profile_row` re-read when `did_reroute`), `:2760-2800`
  (`model_params` from `effective_profile_row` `:2771`; **`params:
  StreamParams { model: effective_profile.model_name.clone(), …
  profile_parameters: Some(model_params.clone()) }` at `:2786-2796`**),
  `:2810-2830` (`fallback_primary` from `effective_profile_row`; the
  primary stream call), **`:2850-2863` (the refresh: `if let Some(p) =
  streaming_state.effective_profile.as_ref() { effective_profile = p.clone
  (); }` — the `78b381a96` §3 catch, with its comment naming the
  route-trail beat)**, `:2960-2975` (`RunNativeToolLoopOptions { provider:
  effective_profile.provider.clone(), base_url: effective_profile.base_url
  .clone(), … base_params: params.clone() }`) and `:3040-3055` (the text
  pass, same shape); `services/native_tool_loop.rs:181` (`base_params`),
  `:466` + `:578` (`let mut params = base_params.clone()` — the re-stream
  reads `params.model`), `services/text_tool_loop.rs:307`, `:427`;
  v4 `lib/services/chat-message/orchestrator.service.ts:1100-1200`
  (`modelParams = profileParams(streamingState.effectiveProfile)` `:1107`;
  the loop options pass `connectionProfile: streamingState.effectiveProfile`
  `:1184`, `:1369`), `native-tool-loop.service.ts:342`, `:423`
  (`connectionProfile: streaming.effectiveProfile` → the funnel),
  `text-tool-loop.service.ts:392`, and `streaming.service.ts:350-420`
  (`streamMessage` reads `connectionProfile.modelName` at `:403`/`:418` —
  the funnel derives model from the PROFILE per call; note what ELSE it
  derives from the profile — `parameters`? `apiKey`? `baseUrl`? — every
  such field is a candidate for the same staleness in v5's `params`);
  `crates/quilltap-harness/tests/orchestrator_tier3_equivalence.rs`
  (header recipe; the `understudy_answers` case `:2160` in `harness/
  oracle/fixtures/orchestrator-tier3.json` and its `streamLabel`; the
  `stream_calls` / `provider|model|temperature|messages` canned-key
  comparand — the ONE comparand that can SEE a re-stream's model name; the
  P4.68 failover-logging WIRING pin `:1603-1660`); `harness/oracle/cases/
  orchestrator-tier3.test.ts` (how a case scripts a primary failure + an
  understudy + a native tool call: the per-call canned registry keyed by
  `provider|model|…` — a re-stream with the WRONG model has NO canned
  answer, which is exactly how the arm reds); `services/chat_create.rs:
  2530-2560` (the api-key arm: `Ok(Some(k)) => api_key = …` — find the
  `Ok(None)` arm and what it returns today), `:2570-2590` (`if let Ok(fmc)
  = build_first_message_context(…)` — the `Err` arm is SILENT), `:2625-
  2640` (`build_recent_conversations_block` — check whether it returns a
  `Result`; if it is infallible in v5 the warn is a NO-PORT with evidence),
  `:2760-2830` (the ladder: attempt 2's `without memories` warn at `:2812`
  — the retry INFO goes before the call; the success INFO after), `:2840-
  2900` (attempt 4 + the exhaustion line); v4 `app/api/v1/chats/route.ts:
  640-700` and `:875-950` (the six sites with their bags — survey below);
  the P4.D190 order (Tier-1 item 5's capture-pin idiom for the four warns
  it ported; the lane record's mutation table) and the P4.D189 lane
  record's "Recorded, not fixed" (a)/(b); memory notes `a-process-global-
  test-seam-must-be-thread-scoped`, `capture-layer-target-assert-is-a-
  prefix-match`, `tracing-percent-field-renders-unquoted`, `coalescing-
  hides-its-own-mutation-proofs`, `a-guard-whose-other-conjuncts-are-
  false-is-untested`.

## Mandate

(a) Prove the staleness with a corpus arm BEFORE fixing it: a new
`orchestrator_tier3` case where the primary profile (provider P1, model M1)
fails over to an understudy on a DIFFERENT provider and model (P2, M2) whose
reply carries a native tool call — the tool loop's re-stream must then be
keyed `P2|M2|…` on both sides; on the unfixed v5 the re-stream is keyed
`P2|M1|…`, has no canned answer, and the case reds (record the exact
first-diff row). Then the fix, caller-side: after the `:2861` refresh,
rebuild every `params` field v4's funnel derives from the profile — at
minimum `model`; MEASURE whether `profile_parameters` (from `effective_
profile_row` at `:2771`) and any other profile-derived field must follow
(v4's loops build their params from `streamingState.effectiveProfile` via
`profileParams(...)` at `:1107` — read whether that call happens before or
after v4's own refresh, because if v4 ALSO reads a stale bag, v5 must match
the stale bag, and only `model` moves). Neutrality: every other
`orchestrator_tier3` case byte-identical; both tool-loop families and
`chat_continuation_tier2` unchanged. (b) Port the six greeting log lines at
v4's sites with v4's levels, sentences and bags (`context:
'autoGenerateFirstMessage'` on the api-key warn; `characterId` + `error` on
the two build failures; `characterId` + `originalMemoryCount` on the retry
info; `characterId` on both success infos), each with a capture-layer pin
AND its silence leg, mutation-proven. This lane does NOT touch the loop
files, the greeting core, the SPA, `help/**`, or any sibling's files.

## Survey-verified starting points (fresh survey 2026-09-16 at `ffb6b3119` and on `main` `4572659b` — do not re-derive)

### (a) the failover model refresh

- **v5:** `params` (`StreamParams`) is built ONCE at `orchestrator.rs:2786-
  2796` from `effective_profile` (pre-failover) + `model_params` (from
  `effective_profile_row`, `:2771`); the primary stream runs (`primary_
  stream::…`, `:2810-2850`) and may fail over inside `provider_failover`,
  recording the understudy in `streaming_state.effective_profile`; the
  refresh at `:2861-2863` updates the LOCAL `effective_profile` only;
  `:2968-2971` and `:3046-3049` then pass `provider`/`base_url` from the
  REFRESHED profile but `base_params: params.clone()` from the STALE one.
  `native_tool_loop.rs:466`/`:578` and `text_tool_loop.rs:427` clone
  `base_params` and stream with `params.model` — so a native tool call
  after a cross-provider failover streams `M1` to `P2`. The watchdog
  (P4.D189) reports what the request carries.
- **v4:** `orchestrator.service.ts:1184` / `:1369` pass `connectionProfile:
  streamingState.effectiveProfile` to the loops; `native-tool-loop.service.
  ts:342`, `:423` and `text-tool-loop.service.ts:392` forward it to
  `streaming.service.ts`'s `streamMessage`, which reads `connectionProfile.
  modelName` (`:403`, `:418`, `:509`) per call — the understudy's model by
  construction. `modelParams = profileParams(streamingState.
  effectiveProfile)` at `:1107` — determine at which point of v4's flow
  `:1107` runs relative to the failover (if BEFORE, v4's loops also carry
  the primary's `parameters` bag and v5 must keep `profile_parameters`
  stale to match).
- **The corpus:** `orchestrator-tier3.json` already has `understudy_
  answers` (`:2160`, canned `streamLabel: "understudy_answers"`) — a
  failover with a PLAIN reply. The new case pairs a failover with a
  native tool call in the understudy's reply (the fixture builder's
  profile set must offer a P2 on a different provider AND model — check
  `build-orchestrator-fixture.ts` for an existing cross-provider
  understudy profile; add one if the current understudy shares the
  primary's model, since a same-model understudy cannot discriminate).
  The jest side registers canned answers by `provider|model|temperature|
  messages`; the re-stream's key on v4 names `P2|M2`. On v5 the unfixed
  key names `P2|M1` → no canned answer → the case reds on `stream_calls`.

### (b) the six greeting lines (v4 `app/api/v1/chats/route.ts`)

| v4 site | level | sentence | bag | v5 site |
|---|---|---|---|---|
| `:647` | warn | `[Chats v1] Connection profile is missing its API key` | `{ context: 'autoGenerateFirstMessage' }` — then `return NO_GREETING` | `chat_create.rs:2539-2545` — the `Ok(None)` arm of `api_keys::find_by_id` (confirm it returns `GeneratedGreeting::none()`; if it falls through with an empty key, that is a SECOND divergence — measure, record, fix only if the capstone can see it) |
| `:672` | error | `[Chats v1] Failed to build first message context` | `{ characterId, error }` | `:2574` the `Err` arm of `build_first_message_context` (silent today) |
| `:692` | warn | `[Chats v1] Failed to build recent-conversations block for greeting` | `{ characterId, error }` | `:2630-2632` — only if `build_recent_conversations_block` can fail in v5; else NO-PORT with the signature as evidence |
| `:882` | info | `[Chats v1] Retrying greeting generation without memories` | `{ characterId, originalMemoryCount }` | before attempt 2's call (`:2800-2812` region), inside `if participant_memories.len() > 0` |
| `:895` | info | `[Chats v1] Greeting generation succeeded on retry without memories` | `{ characterId }` | attempt 2's `content` success return |
| `:942` | info | `[Chats v1] Greeting generation succeeded on final retry` | `{ characterId }` | attempt 4's `content` success return (`:2870-2890` region) |

  Levels: `warn`/`error`/`info` map to `tracing::warn!/error!/info!` with
  the target the file already uses for the P4.D190 lines (read one). The
  `error` field renders `error instanceof Error ? error.message : String
  (error)` — v5's `%err` (`tracing-percent-field-renders-unquoted`: read
  the sigil the sibling lines use).

## Tiered deliverables

### Tier 1 — must land

1. **The corpus arm, red-first.** Add `failover_then_native_tool_call`
   (and, if the fixture's text-block profile allows it cheaply,
   `failover_then_text_tool_pass`) to `orchestrator-tier3.json` + the jest
   case's canned registry; regenerate at the pin; run v5 UNFIXED: the new
   case RED on `stream_calls` (the re-stream key names the primary's
   model) — record the first-diff row verbatim. Every pre-existing case
   byte-identical (`cmp` the old and new NDJSON minus the new rows).
2. **The measurement v4 forces:** where in v4's flow `profileParams(
   streamingState.effectiveProfile)` (`:1107`) runs relative to the
   failover — cite the line order; decide whether v5's `profile_parameters`
   follows the refresh or stays stale; write the decision as a code
   comment at the fix site naming both line numbers.
3. **The fix** — after `:2861-2863`, rebuild the profile-derived fields of
   `params` from the refreshed `effective_profile` (at minimum `params.
   model = effective_profile.model_name.clone()`; the others per item 2),
   BEFORE the early-return check so the loop-option sites at `:2971` /
   `:3049` receive the rebuilt `params`. The new case GREEN; the P4.68
   failover-logging WIRING pin still green; `stream_watchdog_wrap_census`
   unchanged (2/2). Mutation proofs: M1 revert the rebuild → the new case
   red; M2 rebuild `model` from `connection_profile` (the ORIGINAL) → red;
   M3 rebuild only on `did_reroute` (the Concierge flag, not the failover)
   → red (the failover is not a reroute — the conjuncts differ, pin it).
4. **The six lines** at the v5 sites in the table, v4's bytes, levels and
   bag keys; a capture-layer test per line (`captured_with`, the thread-
   scoped rig) with its silence leg — the api-key warn fires ONLY when the
   profile names a key id that does not resolve (NOT when `apiKeyId` is
   null); the context-build error ONLY on `Err`; the retry info ONLY when
   memories were present; each success info ONLY on ITS attempt (attempt 2
   vs 4 — a mutation swapping the two sentences must redden both). Wire
   each through the capstone where a case reaches it (`gs_*` cases — the
   attempt-2/attempt-4 recoveries exist: `…recovers_on_the_final_retry`);
   the two build-failure lines need a posed failure — if no corpus arm
   can pose a DB failure inside `build_first_message_context`, the
   unit-level capture test over a table-less connection is the pin
   (record which lines are capstone-pinned vs unit-pinned).
5. **Neutrality:** `native_tool_loop_tier3`, `text_tool_loop_tier3`,
   `chat_continuation_tier2`, `chat_create_capstone`, `initial_greeting_
   equivalence`, `primary_stream_tier3` regenerated at the pin and run by
   name, zero SKIP, byte-identical where deterministic (`regenerate-at-
   both-pins-and-cmp-is-not-universal` — record which ones mint).

### Tier 2 — should land

6. **The handler-logging inventory** — `harness/tools/handler_log_
   inventory.py` surveys `lib/background-jobs/handlers/*.ts` only, so these
   six lines never had rows; record in the lane record that
   `app/api/v1/chats/route.ts` is outside its scope (the finding-#110
   standing note names the same gap) — do not widen the tool here.
7. **A P4.D190-header correction:** its "SIX more v4 log lines" list is
   now discharged — note it in THIS order's close-out and in the status
   log; the unifier updates P4.D190's header.

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **A loop-side rebuild** (reading the model from a refreshed profile
  INSIDE the loops, v4's funnel shape) — NOT this lane's; the caller-side
  rebuild is the ordered shape and v5's loops take `base_params` by
  value. If measurement shows the loops re-read something the caller
  cannot rebuild, STOP and report.
- **The live proof (💸):** a real cross-provider failover followed by a
  tool call on the Friday copy (a dead primary endpoint + a DeepSeek
  understudy, which tool-calls reliably — the 2026-08-19 walk's recipe),
  `combined.log` showing the re-stream against the understudy's model.
  Banked for the dogfood pass.

## The differential requirement, restated

(a) is proven by `orchestrator_tier3_equivalence` (tier 3 — canned calls
keyed `provider|model|temperature|messages` on both sides; the new case
red-first, then green; the jest oracle drives v4's REAL orchestrator with
the SDK mocked below it — closest recipe: this family's own header + the
P4.87 lane record's "different-provider understudy" arm). (b) is proven by
capture-layer pins (the P4.D190 Tier-1 item 5 idiom) and, where a corpus
case reaches the site, by the capstone's captured-lines comparand. Log
lines are not a differential comparand on the jest side (`jest.setup` no-ops
logging — `jest-setup-llm-logging-service-mocked`; the v4 logger is mocked),
so the SENTENCE bytes are transcribed from `route.ts` and pinned by capture
— say so in each test's doc comment.

## Fixtures

- **Consumes:** `harness/oracle/fixtures/orchestrator-tier3.json` +
  `build-orchestrator-fixture.ts` (grown by one or two cases; a
  cross-provider understudy profile added if the current one cannot
  discriminate); no committed `.db` (the family builds in /tmp — confirm
  from the header).
- **Owns regenerating:** `orchestrator_tier3` (+ the five neutrality
  families) at the baseline pin.
- **Delivers:** the new case(s); the six capture pins.

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `2075242f9` bug-145/146 drift catch-up + maintenance round:
**P4.D192 ∥ P4.D193 ∥ P4.D194 ∥ P4.89 ∥ P4.90**, ordered 2026-09-16. Five
lanes, five worktrees, five lane branches, ALL from `main` (no stacking —
every lane's ownership is disjoint by file, so nothing needs a substrate
commit). The unifier cherry-picks in the order D192 → D193 → D194 → P4.89
→ P4.90 (server-side heals before the fixture widen; the fixture widen
before the family that re-measures over it), gates ONCE, and `merge
--ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`ffb6b3119`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`2075242f9`**
  (`4.10.0-dev.40`) — FOUR commits past the baseline, two carrying code:
  `23abc1ba1` (bug 145 + the bug-144 convergence + closes bug 143 as not
  a defect) and `2075242f9` (bug 146); `064ba85df` and `81e02f7a2` are
  the two docs-only filings this port's own walk wrote. v4 `bugfix` tip
  `1a2b2164c` and `release` tip `8fbf2afe0` are unmoved; there is still
  no `release: 4.10.0` squash. The checkout sat on `main`, CLEAN, at the
  planning probe (2026-09-16 ~08:30 CDT; the §2 probe PASSED, so the
  ledger stood and nothing was re-derived). **At unification the baseline
  MOVES to `2075242f9`** and all four rows retire (two ABSORBED, two
  NO-PORT-RATIFIED).
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log 2075242f9..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Any mismatch — a moved HEAD, a dirty `lib/`/`app/`/`packages/`
  /`plugins/`/`migrations/`/`help/` tree, the checkout off `main` — is a
  STOP: report it and do nothing else.** A lane never writes the ledger.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1). HEAD is past the
  baseline and two commits touch `lib/`, `migrations/`, `packages/` and
  `help/`. Every regen runs from a LANE-UNIQUE detached worktree per
  ledger §5.1 (`git -C ~/source/quilltap-server worktree add --detach
  /tmp/qt-v4-pin-<order>-<sha> <sha>`, the THREE symlink classes, `cd`
  into it; never stash or checkout-switch the human's repo; `git
  worktree list` before each batch; verify the pin by grepping the fresh
  NDJSON for a marker only one tree has; check the file is non-empty
  before believing a diff). Two pins per lane:
  - **the TARGET pin `2075242f9`** — for every family the lane's port
    MOVES (the oracle must import v4's post-fix code: the collapse
    migration, `resolveFloorSeatId`, the CLI's new wording, the two
    re-vendored help pages);
  - **the BASELINE pin `ffb6b3119`** — for every neutrality leg and
    every family the lane merely re-runs, and for `QT_V4_ROOT` /
    `QT_V4_CHECKOUT` on any embed/content guard run BEFORE a re-vendor
    commit lands.
  The sweep driver is the sanctioned per-family path
  (`harness/tools/recipe_sweep.py --run <family> --v4 "$PIN" --v5w
  <ABSOLUTE worktree>`); never run two sweeps concurrently; a family that
  reports `SKIP` in 0.00 s did not run (`a-family-env-var-is-not-its-
  regen-var`). Ledger §5.2's silent-stale-pass discipline applies to
  every regen (`rm -f` the fixture first; read the builder's last line;
  `grep -c` the changed bytes in the fresh NDJSON — MUST be > 0).
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). The
  spec for every item is `git show <sha> -- <path>` plus `git show
  <sha>:<path>`; a commit message describes the bug as its author
  understood it and the hunks are often narrower. Re-verify even where
  this order already flags the trap — the order's paragraph is the same
  kind of prose. Carry a v5-side code comment naming the sha where the
  prose misleads.
- **§R.5 — Convergence rows: measure, don't assume** (ledger §5.4). The
  bug-144 wording pin (`a_fresh_heartbeat_keeps_v4s_false_liveness_claim`)
  trips at the target pin BY DESIGN; dump v4's actual post-fix output
  before retiring it, and expect Tier R's five `lock clean …` cases to
  move together.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig) and its silence leg; a fixture change lands with the
  regen of every family that reads it. Mutation proofs: each named
  proof must redden exactly the test it is written against, and a proof
  that SURVIVES as first written is a finding to fix, not a line to
  delete (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`).
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). This order's status header is the
  lane's to update at lane close; the unifier rewrites it at unification.
  `docs/developer/porting/drift-ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed (and `apps/web/package.json` when it touches the SPA) on each
  commit per `.claude/commands/commit.md`; the unifier RECOUNTS as base +
  the sum of every lane's bumps (the silent-auto-merge trap: four lanes'
  identical `0.0.922` bumps merge as ONE). Expected: core is bumped by
  P4.D192, P4.D193 and P4.90; harness by all five (P4.89 only if it edits
  a harness header); cli by P4.D194; web by P4.89 (the committed fixture
  pair lives under `crates/quilltap-web/tests/fixtures/`); host by
  P4.D192 only if `host_boot_avatar_rolls_collapse.rs` changes; the SPA by
  P4.D193.
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  P4.D194 pre-lists the paths the target moves (with byte counts at the
  pin) in its lane record; no lane copies them.
- **§R.10 — Cross-lane handoffs, named:** (a) P4.D192 widens
  `gc_orphaned_file_row`'s return type — its only callers are in files
  P4.D192 owns (`db/doc_mount_file_links.rs`, `api/mount_points.rs:602`);
  no sibling touches either. (b) P4.D193's `help/chat-turn-manager.md`
  hunk and P4.D192's `help/database-protection.md` hunk are BOTH
  P4.D194's (the one `help/**` owner). (c) P4.89's widened
  `brahma-{main,mount}.db` is read by three families; no sibling
  regenerates them. (d) P4.90's `orchestrator.rs` edit is one statement
  after `:2861`; P4.D189's wrap census counts `watch_stream(` per file and
  must not move — P4.90 adds none. (e) The dogfood-findings row #119 cell
  moves with P4.D194 (the convergence); no other lane edits that file.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D192** — bug 145 server | `crates/quilltap-core/src/db/avatar_rolls_collapse_heal.rs`; `crates/quilltap-core/src/db/doc_mount_file_links.rs` — ONLY (i) `gc_orphaned_file_row`'s return type + its in-file callers/tests and (ii) the deletion of the `is_photos_relative_path` twin + its private `posix_dirname`; `crates/quilltap-core/src/api/mount_points.rs:602` (the one out-of-file GC caller, adapted to the new return); the twin's importers, import-line edits ONLY: `api/chat_media.rs:1716`, `photos/photo_link_summary.rs:13`, `photos/character_gallery_service.rs:17`, `services/maintenance.rs:32,375`; `crates/quilltap-harness/tests/avatar_rolls_collapse_heal_equivalence.rs`; `crates/quilltap-harness/tests/photos_relative_path_equivalence.rs` (import path only); `harness/oracle/cases/avatar-rolls-collapse-heal.test.ts`; `harness/oracle/fixtures/avatar-rolls-collapse-heal.json`; `crates/quilltap-host/tests/host_boot_avatar_rolls_collapse.rs` (only if a two-link arm is added); this order's header | `services/orchestrator.rs`, `services/chat_create.rs`, `participant_filters.rs`, `crates/quilltap-cli/**`, `help/**`, `apps/web/**`, `crates/quilltap-web/tests/fixtures/brahma-*`, every other harness family (regen-and-run ONLY for neutrality — no edits), `docs/v4/**`, the drift ledger |
| **P4.D193** — bug 146 (server twin + SPA) | `crates/quilltap-core/src/participant_filters.rs`; NEW `harness/oracle/cases/floor-seat.ts`; NEW `crates/quilltap-harness/tests/floor_seat_equivalence.rs`; `apps/web/src/app/chat/turn-order.ts` + `turn-order.spec.ts`; `apps/web/src/app/chat/turn-controls.ts`; `apps/web/src/app/screens/salon/salon-conversation.ts` — ONLY `bannerSeat` / `userTurnName` / `isSeatsTurn` / `mustSpeak` / `onSkipUserTurn` and the `<qt-turn-controls …>` binding block (`:431-436`); `apps/web/src/app/screens/salon/salon-turn-controls.spec.ts`; NEW `apps/web/e2e/salon-floor-seat-flow.spec.ts`; `apps/web/e2e/salon-dialogs-flow.spec.ts` ONLY if its banner assertion (`:412-418`) must learn the third sentence; this order's header | every other `crates/**` file, `help/**`, `core-contract.ts` (no wire change in this feature), every other SPA file, the drift ledger |
| **P4.D194** — bug-144 convergence + ratifications + help re-vendor | `crates/quilltap-cli/src/db_cmd.rs`; `crates/quilltap-cli/tests/cli_differential.rs` (only if a Tier R case must be added — none is expected); `help/database-protection.md`; `help/chat-turn-manager.md`; `docs/developer/porting/dogfood-findings.md` — row #119's status cell ONLY; this order's header | every other `crates/**` file, `apps/web/**`, every harness family except the `help_*` content families (regen-and-run at the target pin), `docs/v4/**` (pre-LIST for the unifier, never copy), the drift ledger |
| **P4.89** — the brahma fixture vintage widen | `crates/quilltap-web/tests/fixtures/brahma-main.db`, `brahma-mount.db` (+ any `.meta.json` sidecar found beside them); `harness/oracle/fixtures/migrate-memories-fixture-columns.ts` (EXTEND its target list — the P4.52 idiom); the THREE brahma harness headers (`crates/quilltap-harness/tests/brahma_orchestrator_tier3_equivalence.rs`, `brahma_console_tier3_equivalence.rs`, `brahma_console_routes_equivalence.rs`) — regen-recipe comments and a vintage note ONLY, no assertion changes unless a red is proven fixture-caused; the three jest oracles `harness/oracle/cases/brahma-*.test.ts` ONLY if an oracle-side heal is proven necessary AFTER the widen; this order's header | every `crates/quilltap-core/src/**` file (the P4.50-class prefix finding is MEASURED and RECORDED, never fixed here), `apps/web/**`, `help/**`, every other fixture, the drift ledger |
| **P4.90** — the failover model refresh + the six greeting lines | `crates/quilltap-core/src/services/orchestrator.rs` — ONLY the post-refresh `params` rebuild at `:2861-2863` (and, if measurement says so, the two loop-option sites `:2968-2971` / `:3046-3049`); `crates/quilltap-core/src/services/chat_create.rs` — ONLY the six log lines at their v4 sites + their unit-level capture tests; `crates/quilltap-harness/tests/orchestrator_tier3_equivalence.rs`; `harness/oracle/cases/orchestrator-tier3.test.ts`; `harness/oracle/fixtures/orchestrator-tier3.json` + `build-orchestrator-fixture.ts`; `crates/quilltap-harness/tests/chat_create_capstone_equivalence.rs` (capture pins only, if the pin idiom lives there); this order's header | `services/native_tool_loop.rs`, `services/text_tool_loop.rs`, `services/primary_stream.rs`, `services/provider_failover.rs`, `services/initial_greeting.rs`, `model/**`, every other harness family (regen-and-run only), `apps/web/**`, `help/**`, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0),
   and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp`) where the family is deterministic across
   regens; where it is not (`regenerate-at-both-pins-and-cmp-is-not-
   universal`), run by name and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast` with the lane's env block
   (its own oracle vars + `QT_V4_ROOT`/`QT_V4_CHECKOUT` at the right pin +
   `QT_NODE` = the node BINARY): report **binaries / passed / failed /
   ignored** from the per-binary lines and the exit code, and the lane's
   families confirmed RUN by non-zero duration. The standing withheld
   family (`brahma_orchestrator_tier3` — P4.89's subject) is withheld BY
   NAME in every lane but P4.89, which restores it.
9. **SPA lanes only (P4.D193):** `npm run lint` (incl. the qt-class guard),
   `npm test` (spec files / tests / 0 failed), `npm run build` clean, then
   the lane's e2e beats by FILE against the release binary, and ONE full
   Playwright run at lane close (numbers recorded; the documented
   intermittents — the P4.D161 pause-toast beats, the P4.d17 quill beat —
   are named, re-run by file, and never re-classified as this lane's).
10. **Ownership check:** `git diff --stat main...HEAD` — every path is in
    the lane's Owns column; every MUST-NOT-TOUCH path absent.
11. **Per commit:** `.claude/commands/commit.md` in full — the equivalence
    test is non-negotiable (§1), the invariants (§2), real-instance safety
    (§3), the harness regen (§4), build/test/lint (§5), the CHANGELOG
    entry + version bumps (§7). Commit on the lane branch only; never
    merge to `main`; never `git stash`; never a worktree inside a worktree.
12. **Lane close:** update this order's status header (what landed, what
    was refuted by measurement, what stays OPEN with its named cause),
    append the lane record to `status-log.md` (the regen commands as run,
    the counts, the mutation table, every finding outside the lane's
    ownership for the unifier), and stop. The final gate runs in the
    FOREGROUND of the lane's last turn — a backgrounded gate whose
    notification arrives after the lane reports done is a lane that is
    NOT done (`a-backgrounded-sleep-does-not-gate-your-turn`).
