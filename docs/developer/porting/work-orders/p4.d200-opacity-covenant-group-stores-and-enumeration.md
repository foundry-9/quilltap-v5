# Work order P4.D200 — v4 `1065a1f53` + `89fcc3c0d` (bugs 152 + 153, the doc-tool opacity covenant): an opaque character KEEPS her own group stores and a hidden vault is not LISTED either — the `hide_character_vaults` flag replacing the drop-the-character shape at both resolution-context builders, the flag through the path resolver's collector and the tiered-pool flatten (`include_character_tier`), the self-token gate's new condition, the `NOT_FOUND` → `ACCESS_DENIED` split for a store that exists but is out of scope (with its two warn lines), the flag on `get_accessible_mount_points` derived at all FOUR enumeration call sites (grep / list_files / the two blob mount resolvers), `help/character-system-transparency.md` re-vendored, the non-lib files ratified, and a NEW real-DB family over v4's two regression suites — from `main`

**UNIFIED on `main` (2026-09-18) — CLOSED WHOLE.** The §3 review found NO
blocking issue; **fixed on the unify branch:** `describe_characters` now skips
an EMPTY acting id as v4's `if (context.characterId)` truthiness does (the
`8f910137`-round audit shape), pinned by two rows; the NOT_FOUND warn pin
asserts its level; the resolver warns gained their missing silence legs — an
enabled store resolving on the pool path asserts NO line at WARN, and the
self-token warn's silence leg lives in `doc_edit_path_resolver_equivalence`'s
two resolving `self` rows over the real fixture (a unit fixture cannot carry
it: the pool's character-tier read applies the vault overlay); the collector's
doc no longer says "participants included"; `help_tree_embed_guard`'s history
comment gains the P4.D200 line; `help_tree_equivalence` was regenerated at the
`89fcc3c0d` pin and proven green with the pin marker present (the lane's gate
had not run it). Recorded, not changed: three corpus rows the reviewer named
as structurally-covered blind spots (an own-vault-by-NAME blob read, a
group-LINKED store, the two-character `characters:` piece seen through the
capture layer) and the oracle's unwrapped `buildWriteResolutionContext` on a
transparent peer-vault write (no current row hits it). Round record:
`status-log.md`.

**Status: CLOSED WHOLE at lane close (2026-09-18), unified the same day.**
Ordered 2026-09-18 as part of the `89fcc3c0d` opacity-covenant drift catch-up
+ follow-ups round (P4.D200 ∥ P4.98 ∥ P4.99). Branch
`claude/p4-d200-opacity-covenant-d2a795`, eight commits. The lane's own
close-out (its measured corrections of the order, the waiver on the bug-154
STOP, the 21/44 red-first count) is preserved in the lane record under
`status-log.md` → "P4.D200 — v4 bugs 152 + 153, the doc-tool opacity covenant
(lane record)". Tier-2 item 13 did not land and is not owed (the new family
covers it more strongly); nothing was banked, stubbed or refused.

## Preamble

- **v4 oracle baseline: `bcd7e4852`** (drift-ledger §1). This lane absorbs
  BOTH rows the ledger's §3 carries, `ORDERED(P4.D200)`:
  **`1065a1f53`** ("fix(scriptorium): an opaque character keeps her own
  group stores (bug 152)", 2026-09-17 22:23, `4.10.0-dev.46`) and
  **`89fcc3c0d`** ("fix(scriptorium): a hidden vault is not listed either
  (bug 153)", 22:37, `4.10.0-dev.47`). One lane, not a stacked pair: the two
  commits share one substrate (`actingCharacterIsOpaqueToVaults` + the
  `hideCharacterVaults` flag + the flatten option), two source files
  (`path-resolver.ts`, `doc-edit/shared.ts`) and one `help/` page, so the
  ownership could not be made disjoint and a stack would serialize without
  parallelism. Spec reads: `git show 89fcc3c0d:<path>` for the post-commit
  files (the tree at bug 153 contains bug 152's hunks) and `git show
  1065a1f53 -- <path>` / `git show 89fcc3c0d -- <path>` for the hunks —
  **never the commit message** (§R.4; two measured misdescriptions are
  named there).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** Pin **`/tmp/qt-v4-pin-p4d200-
  89fcc3c0d`** (the TARGET — v4 HEAD) for every family this port MOVES:
  the NEW `doc_opacity_equivalence`, `tiered_mount_pool_equivalence`
  (the flatten option rows), `doc_edit_path_resolver_equivalence` if
  Tier-2 item 13 lands, and `help_tree_equivalence`. Pin **`/tmp/qt-v4-
  pin-p4d200-bcd7e4852`** (the BASELINE) for the neutrality legs:
  `doc_enum`, `doc_blob`, `doc_text`, `doc_fm`, `doc_fs`, `doc_ui`,
  `scriptorium_tools`, and the PRE-EXISTING rows of the two grown
  families (every existing fixture character is `systemTransparency:
  true` — `build-doc-{enum,text,blob,fm,fs}-fixture.ts` all seed it, and
  the path-resolver + pool fixtures never go through the builders — so
  the flag is `false` on every existing row and the port must move NOTHING
  there; prove it by regenerating at BOTH pins and diffing). Pin
  verification: the target-pinned `help_tree_equivalence` NDJSON carries
  `Nor are they so much as named` and `Vaults, and nothing besides`; the
  baseline one carries neither (`grep -c` both). ⚠ All three symlink
  classes (ledger §5.1) — the jest cases load the cipher driver through
  `packages/quilltap/node_modules`.
- **Sibling lanes:** P4.98 (`Request::ImagesGenerate`'s tri-state —
  `api/**`, the web edge, the census; you touch none of those), P4.99 (the
  recovery INFO line in `services/primary_stream.rs` + the google
  cache-key corpus rows + the harness `CorpusScript` DRY in
  `crates/quilltap-harness/src/lib.rs`; you touch none of those — your new
  family keeps its helpers in its own file).
- **Worktree:** `git worktree add <path> -b claude/p4-d200-opacity-
  covenant-<hash> main`; `CARGO_INCREMENTAL=0`. No SPA work, no host
  work (the help re-vendor changes the embedded table but no host source —
  §R.8).
- **Read before the work:** `crates/quilltap-core/src/db/tiered_mount_
  pool.rs` `:60-100` (`TierContext`, `TierResolveOptions`), `:256-340`
  (`resolve_tiered_mount_pool` — step 6 admits participants only under
  `include_participants`), `:357-410` (`FlattenScope`, `flatten_tier_pool`
  — the `add_character_tier` closure at `:374-383` is where v4's
  `includeCharacterTier` early return lands); `crates/quilltap-core/src/
  doc_edit/path_resolver.rs` `:36` (`SELF_VAULT_TOKEN`), `:40-48`
  (`PathResolutionContext` — no flag), `:67-84` (`PathErrorCode` —
  `AccessDenied` exists), `:119-147` (`resolve_self_vault_mount_point_id`),
  `:149-162` (`resolve_mount_point_ref`), `:387-422` (`collect_accessible_
  mount_point_ids`), `:424-540` (`resolve_document_store_path` — the
  self-token gate at `:459-476` conditioned on `character_id` ONLY; the
  name loop `:479-487`, the id loop `:488-498`, the NOT_FOUND arm `:500-
  505` with no warn and no out-of-scope split; ⚠ the whole file has ZERO
  `tracing` calls — every `logger.warn` v4 has in it is absent, a
  pre-existing gap of the #103/#110 class that Tier 2 item 12 scopes);
  `crates/quilltap-core/src/db/doc_mount_points.rs` `:683`
  (`find_enabled_for_docedit`) and `:848-854` (`DmpRow { id, name,
  enabled, mount_type, base_path }` — NO `store_type`; v4's new
  `findEnabledMountPointByRef` needs `storeType === 'character'`, so
  measure the readers of `DmpRow` and widen it additively or add a
  dedicated read — say which); `crates/quilltap-core/src/tools/doc_edit/
  shared.rs` `:68-82` (`DocEditToolContext`), `:159-167` (`Addressing`),
  `:278-330` (`collect_peer_character_ids_for_reads`), `:334-346`
  (`acting_character_is_opaque_to_vaults` — `None` character → `false`;
  lookup failure → `true` with NO warn where v4 warns), `:536-559` and
  `:562-590` (the two builders — the opaque branch returns `character_id:
  None, character_ids: Vec::new()`, the MEASURED bug), `:635-681`
  (`AccessibleMountPoint` + `get_accessible_mount_points` — five
  positional parameters, `include_participants: true` and `flatten(All,
  true)` hard-coded); `crates/quilltap-core/src/tools/doc_edit/text.rs`
  `:779-785` (grep's call) and `:1155-1161` (list_files's call); `crates/
  quilltap-core/src/tools/doc_edit/blob.rs` `:89-119` (`resolve_blob_
  mount_point_for_read`) and `:121-150` (`resolve_blob_mount_point_for_
  write`) with their callers at `:193`, `:298`, `:388`, `:470`; `crates/
  quilltap-core/src/tools/search.rs:440` (the third flatten call —
  `flatten_tier_pool(&pool, flatten_scope(scope), false)`; signature
  adaptation only); `crates/quilltap-harness/tests/tiered_mount_pool_
  equivalence.rs` WHOLE (the fixture already has a group `d8000000-…-01`
  with an official store `…f1`, a linked store `…f2` and charA's
  membership — `harness/oracle/fixtures/tiered-mount-pool.json:10-12`;
  the Rust side drives `flatten_tier_pool` at `:144-192`) + `harness/
  oracle/cases/tiered-mount-pool.ts`; `crates/quilltap-harness/tests/
  doc_enum_equivalence.rs` WHOLE + `harness/oracle/cases/doc-enum.test.
  ts` WHOLE (the jest mock set at `:98-160` — cipher driver, Librarian
  writer, reindex, embedding scheduler, vector store — is the template
  for the new case; the Rust driver's ONE `DocEditToolContext` per run at
  `:255-262` is what the new family generalizes to a per-op context) +
  `harness/oracle/fixtures/build-doc-enum-fixture.ts` (`:186-207` seeds a
  chat with participants through `repos.chats.create` — the recipe) and
  `harness/oracle/fixtures/build-tiered-mount-pool-fixture.ts` (`:99-110`
  + `:200-205` seed a group through `ensureCollection('groups',
  GroupSchema)`, a raw `INSERT INTO "groups"`, and `repos.
  groupCharacterMembers.addMember` — the recipe); `crates/quilltap-
  harness/tests/doc_blob_equivalence.rs` (the write-op normalizer);
  `crates/quilltap-core/src/test_support.rs:87-140` (`CaptureLayer`,
  `captured_with`); `crates/quilltap-harness/tests/help_tree_embed_guard.
  rs:25-48` (the vendored-count history + `VENDORED_FILE_COUNT = 124`);
  `docs/developer/porting/drift-ledger.md` §3 (both rows carry the
  2026-09-18 measurement of exactly which v5 lines reproduce each bug).

## Mandate

Land bugs 152 and 153 end to end on the server, byte-faithful to the hunks:
the opacity covenant expressed as a SUBTRACTION (`hide_character_vaults`)
rather than by withholding the identity the group tier is keyed on — the
flag on `PathResolutionContext`, set by both resolution-context builders
which now KEEP `character_id`; the flag honoured by the collector through
the pool's new `include_character_tier` flatten option (own vault AND
participants dropped together, group/project/global kept); the self-token
gate's new condition; the out-of-scope split (`find_enabled_mount_point_by_
ref` excluding character vaults so the refusal leaks nothing, v4's
three-sentence `ACCESS_DENIED` message, the two warn lines with
`vaultsHidden`); the same flag on the enumerator (`get_accessible_mount_
points` taking a query struct) derived from `acting_character_is_opaque_
to_vaults` at all four call sites so listing and resolution cannot disagree;
the help page re-vendored; the non-lib files ratified; and a NEW real-DB
differential that drives v4's REAL builders, resolver, enumerator and tool
handlers over the reported shape (an opaque character who belongs to a
group whose store is not project-linked, sharing a chat with a transparent
peer), mirroring v4's two regression suites row for row. **v5 REPRODUCES
both bugs today** (the ledger's measurement); the family must be red-first
and the pre-fix count recorded. This lane does NOT touch `api/**`, `crates/
quilltap-web/**`, `services/**`, `model/**`, `crates/quilltap-harness/src/
lib.rs`, any other `help/*.md`, or the drift ledger; it does not add a
verb, a route or an SPA surface (§S.1).

## Survey-verified starting points (fresh survey 2026-09-18 at `89fcc3c0d` and on `main` `2d720c04` — do not re-derive)

### v4 (at `89fcc3c0d`; the bug-152 hunks are `1065a1f53`'s, the bug-153 hunks `89fcc3c0d`'s)

- **`lib/mount-index/tiered-mount-pool.ts` `flattenTierPool`** (`:352-
  380`, bug 152): the options bag gains `includeCharacterTier?: boolean`
  (default `true`); `addCharacterTier` returns EARLY when it is false —
  so BOTH the own-vault id and the participant ids (which live inside
  `addCharacterTier`) are dropped together; `includeParticipants` is
  unchanged (default `false`). The doc comment is the why: "the group tier
  is derived from `characterId` and from nothing else … dropping the
  character to hide her vault would silently take every group store she
  belongs to with it".
- **`lib/doc-edit/path-resolver.ts`** (bug 152 `+79`, bug 153 `+31`):
  - `PathResolutionContext.hideCharacterVaults?: boolean` (`:114-126`)
    with its doc ("`characterId` must still be supplied alongside it …
    The reserved `self` token is refused while this is set").
  - `describeCharacters(context)` (`:293-300`, PRE-EXISTING): a `Set` of
    `characterId` + every `characterIds` entry, `Array.from(ids).join(',')`
    or `'none'` when empty.
  - `collectAccessibleMountPointIds` (`:321-352`): `vaultsVisible =
    !context.hideCharacterVaults`; the pool is resolved with
    `{ includeParticipants: vaultsVisible }` and flattened with
    `{ includeParticipants: vaultsVisible, includeCharacterTier:
    vaultsVisible }`. The operator-override arm above it is untouched.
  - NEW `findEnabledMountPointByRef(ref)` (`:354-382`): `repos.
    docMountPoints.findEnabled()`; `needle = ref.toLowerCase()`; the match
    is `find(name.toLowerCase() === needle) ?? find(id === ref)` — NAME
    first, then id; `match.storeType === 'character'` → `null` (a vault
    keeps the indistinguishable NOT_FOUND — the covenant must not leak
    through the refusal); any thrown lookup → `null` (fails soft).
  - `resolveDocumentStorePath` (`:395-500`): the self-token gate is now
    `context.characterId && !context.hideCharacterVaults && needle ===
    SELF_VAULT_TOKEN` (`:432`) — with the flag set the token falls
    through to the ordinary name/id loops and ends in the not-found arm;
    the not-found arm (`:471-495`) FIRST calls `findEnabledMountPointByRef
    (context.mountPoint)`: a hit → `logger.warn(`Mount point exists but
    is out of scope: ${name} (${id}) (project: ${projectId ?? 'none'},
    characters: ${describeCharacters(context)}, vaultsHidden:
    ${hideCharacterVaults === true})`)` + throw `ACCESS_DENIED` with the
    message `The document store "${name}" exists but is not reachable
    from this conversation. It is not linked to this project, and it is
    not one of your own group's stores. Retrying with a different
    spelling will not help — use doc_list_files with no path to see the
    stores you can reach.` (three sentences, one string, note the em dash
    and the double-quoted name); a miss → the PRE-EXISTING warn
    `Mount point not found or not accessible: ${mountPoint} (project: …,
    characters: …)` now with `, vaultsHidden: ${… === true}` appended +
    the unchanged NOT_FOUND `Mount point not found or not accessible in
    this context`. Logger: `createServiceLogger('DocEdit:PathResolver')`
    (`:30`).
  - NEW `interface AccessibleMountPointsQuery { projectId?, characterId?,
    extraCharacterIds?, hideCharacterVaults? }` (`:829-848`, bug 153) and
    `getAccessibleMountPoints(query)` (`:855-`): ONE object parameter
    replacing the three positionals; `characterIds: extraCharacterIds &&
    extraCharacterIds.length > 0 ? extraCharacterIds : undefined`
    (unchanged rule); `hideCharacterVaults` passed straight to the
    collector. Its doc: "whatever this lists is exactly what an open will
    accept".
- **`lib/doc-edit/index.ts`** (`+2`, bug 153): re-exports the two types.
  No v5 analogue (v5's items are `pub` in their modules) — ratify.
- **`lib/tools/handlers/doc-edit/shared.ts`** (bug 152 `+20`):
  `actingCharacterIsOpaqueToVaults` (`:163-178`, PRE-EXISTING; doc
  reworded): `!context.characterId → false`; `findById` →
  `character?.systemTransparency !== true`; `catch` → `logger.warn(
  'systemTransparency lookup failed; defaulting to opaque', { characterId,
  error })` → `true`. `buildReadResolutionContext` (`:374-390`): the
  opaque branch returns `{ projectId, characterId: context.characterId,
  hideCharacterVaults: true, mountPoint: input.mount_point,
  operatorOverride }` — NO `characterIds` (peers are never admitted for an
  opaque character); `buildWriteResolutionContext` (`:410-424`): the
  same shape. Both transparent branches are unchanged. Logger:
  `createServiceLogger('DocEdit:Handler')` (`:35`).
- **`lib/tools/handlers/doc-edit/text-handlers.ts`** (bug 153 `+22`):
  `handleGrep` (`:793-805`) and `handleListFiles` (`:998-1010`): after
  `collectPeerCharacterIdsForReads`, `const hideCharacterVaults = await
  actingCharacterIsOpaqueToVaults(context)`; `getAccessibleMountPoints({
  projectId: context.projectId, characterId: context.characterId,
  extraCharacterIds: peerCharacterIds, hideCharacterVaults })`. The
  `mountPointFilter` (`resolveMountPointRef` translating `self` to the
  vault id) is computed BEFORE and unchanged — so `mount_point: "self"`
  under the covenant resolves to a vault id that is simply ABSENT from
  the enumerated set (an empty listing, no refusal sentence).
- **`lib/tools/handlers/doc-edit/blob-handlers.ts`** (bug 153 `+20`):
  `resolveBlobMountPointForRead` (`:57-70`): after `resolveMountPointRef`
  + `collectPeerCharacterIdsForReads`, the flag, then `getAccessibleMount
  Points({ projectId, characterId, extraCharacterIds: peerCharacterIds,
  hideCharacterVaults })`; `resolveBlobMountPointForWrite` (`:87-97`):
  after `assertWriteDoesNotTargetPeerVault`, the flag, then the query
  WITHOUT `extraCharacterIds` (§R.4). `handleListBlobs` (`:252`) goes
  through the READ resolver; `handleReadBlob` the read resolver;
  `handleWriteBlob` + delete the write resolver — no handler body changes.
- **`help/character-system-transparency.md`** (`+4` then `+2`): item 3's
  paragraph gains "Nor will the reserved `self` shorthand find anything
  to point at." and the bold sentence "**Nor are they so much as named.**
  A vault that cannot be opened is not listed either: …"; a NEW indented
  paragraph "**Vaults, and nothing besides.** Group stores, project-
  linked stores, and Quilltap General remain perfectly accessible — …".
  md5 at `89fcc3c0d`: `a70d5054165040970eabfcb61249370f`; v5's current
  file is byte-identical to `bcd7e4852`'s (`4744366850f5c1f69d2af16e32b
  72655`). The tree stays 124 files (both commits MODIFY one, add none).
- **The corpus sources:** `lib/doc-edit/__tests__/path-resolver-opacity-
  group-stores.test.ts` (269 lines, 13 cases: group store by NAME / by
  ID / on the READ path / project store still reachable; own vault hidden
  by name / peer vault hidden and NOT admitted to exist / the `self`
  token refused / the built context keeps `characterId` with the flag set;
  a transparent character reaches her vault AND her group store; an
  out-of-scope stranger store answers ACCESS_DENIED / a nonexistent name
  still NOT_FOUND; two `flattenTierPool` unit rows) and `path-resolver-
  opacity-enumeration.test.ts` (345 lines, 15 cases: `getAccessibleMount
  Points` omits BOTH vault tiers / keeps group + project / lists both
  vaults without the flag / everything listed also opens; `doc_list_files`
  leaves both vault names out / `self` → nothing / own vault by name →
  nothing / a TRANSPARENT character lists both; `doc_grep` omits vault
  hits / `self` → nothing / transparent searches both; the blob tools
  refuse a read from the own vault and from `self` / refuse a listing of a
  peer vault / refuse a write to the own vault / still reach the group
  store). Both run against the REAL tiered pool with the repositories,
  `getGeneralMountPointId` and (153) `listDatabaseFiles` MOCKED. The world
  they build: ACTING `char-leilani` (transparency false, vault `mp-own-
  vault`) and PEER `char-abigail` (transparency true, vault `mp-peer-
  vault`); group `grp-severed` with ONLY the acting character a member and
  official store `mp-group` ("Group Files: Severed") NOT project-linked;
  project `proj-1` linking `mp-project` ("Project Papers"); no General
  store; chat `chat-1` with `allowCrossCharacterVaultReads: true` (153) and
  both as active participants. **This lane's family reproduces that world
  in a REAL two-partition fixture and drives the same real functions** —
  see Tier 1 item 8.
- **Non-lib files (ratify on the file list, §R.9 pre-list):** `README.md`
  (badge), `docs/CHANGELOG.md`, `docs/developer/bugs.md`, NEW `docs/
  developer/bugs/fixed/bug-152-opacity-hides-group-stores.md` (181 lines
  + 6 at bug 153), NEW `bug-153-opacity-enumeration-leak.md` (123),
  `package.json` / `package-lock.json` / `packages/quilltap/package.json`
  (`4.10.0-dev.46` → `.47`), `lib/doc-edit/index.ts` (the two type
  re-exports), and the two `__tests__` files (the corpus sources, not
  ported as files). No `app/`, `components/`, `packages/quilltap/bin`,
  `lib/db/`, `generateDDL` or `public/schemas/` change — no D23 re-dump,
  no Tier R, no SPA, no export-schema guard movement.

### v5 (on `main` `2d720c04`)

- **The measured reproduction** (drift-ledger §3, 2026-09-18): `tools/
  doc_edit/shared.rs:541` and `:568` both return `PathResolutionContext {
  character_id: None, character_ids: Vec::new(), .. }` on the opaque
  branch (bug 152 — the group tier keyed on a `None`); `get_accessible_
  mount_points` (`:646`) takes `(main, mount, project_id, character_id,
  extra_character_ids)` and builds a `TierContext` from `character_id`
  with `include_participants: true` + `FlattenScope::All` + participants
  `true`, and its four callers (`text.rs:779`, `:1155`, `blob.rs:100`,
  `:135`) pass the character unconditionally (bug 153 — the vaults are
  listed). Neither file consults `acting_character_is_opaque_to_vaults`
  on the enumeration path today.
- **`flatten_tier_pool(pool, scope: FlattenScope, include_participants:
  bool)`** (`db/tiered_mount_pool.rs:368`) — a positional bool, THREE
  production call sites (`path_resolver.rs:421`, `tools/doc_edit/shared.
  rs:669`, `tools/search.rs:440`) + the harness family. v4's third option
  makes a struct the honest shape (`TierResolveOptions` at `:85-92` is the
  file's precedent for an options struct with `Default`); a fourth
  positional bool is acceptable if the lane prefers the smaller diff —
  say which, and adapt `search.rs:440` as a signature-only edit either way.
- **`resolve_document_store_path`** (`path_resolver.rs:424-540`) — the
  loops and refusals are v4's minus every log line; `ResolveError::path
  (PathErrorCode::AccessDenied, …)` is the idiom for the new refusal.
  `DocMountPointsRepository::find_enabled_for_docedit` (`db/doc_mount_
  points.rs:683`) returns `DmpRow` WITHOUT `store_type` — the raw table
  carries `storeType`; measure whether widening `DmpRow` disturbs a
  destructuring reader (grep `DmpRow {`) and prefer the additive field;
  if a reader pattern-matches exhaustively, add a sibling read instead.
- **Logging idiom:** this crate logs through `tracing` with snake_case
  fields and v4's sentence as the message (P4.D175 / P4.93; a JSON-valued
  field takes the `…Json` suffix convention from P4.91). v4's two lines
  here are TEMPLATE LITERALS (one rendered string); port them so the
  rendered message is v4's sentence byte-for-byte (the operator greps
  `combined.log` for v4's words) AND carry the pieces as fields
  (`mount_point`, `project_id`, `characters`, `vaults_hidden`). Pin with
  `test_support::captured_with` — the message text, the level (`WARN`),
  and the silence leg.
- **Harness idioms to copy:** the doc-enum jest mock set (`harness/oracle/
  cases/doc-enum.test.ts:98-160`); the doc-blob write-op normalizer
  (positional `<id-N>` UUID remap + `<ts>` collapse + `<mtime>` on write
  outputs); the fixture builders named in the Preamble for the chat and
  the group; `Writer::open_writable` + `execute_doc_edit_tool(main, mount,
  tool, args, &ctx)` + `format_doc_edit_results` (`doc_enum_equivalence.
  rs:245-280`); `build_read_resolution_context(main, &addressing, &ctx)` /
  `build_write_resolution_context(main, mount, &addressing, &ctx)` +
  `resolve_doc_edit_path` (the DPR family's `ctx` closure at `:199-206`
  shows the `PathResolutionContext` literal — after this lane it gains
  `hide_character_vaults`, so every literal in the tree that builds one
  (grep `PathResolutionContext {`) needs the new field: `path_resolver.rs`,
  `shared.rs`, the DPR family, and any test module — count them and list
  them in the lane record).
- **Families that read the moved surfaces (neutrality legs):** `doc_enum`,
  `doc_blob`, `doc_text`, `doc_fm`, `doc_fs`, `doc_ui` (every fixture
  character transparent), `scriptorium_tools` (the search flatten call),
  `tiered_mount_pool` (pre-existing rows), `doc_edit_path_resolver`
  (pre-existing rows). `help_tree_equivalence` MOVES (the page's bytes);
  `help_tree_embed_guard` holds 124; no other `help_*` family's fixture
  or case names `character-system-transparency.md` (grep at planning:
  none) — measure once more and record.

## Tiered deliverables

### Tier 1 — must land

1. **The flatten option** (`db/tiered_mount_pool.rs`): `include_character_
   tier` (default `true`) on `flatten_tier_pool`, dropping BOTH the own
   vault and the participant ids when false (v4's early return inside
   `addCharacterTier`), `scope` and `include_participants` unchanged; v4's
   doc comment carried (the "keyed on `characterId` and nothing else"
   why). The three production call sites and the harness family adapted
   (the shape decision recorded). Unit rows: the two `flattenTierPool`
   cases from the bug-152 suite over a hand-built pool literal — in
   `tiered_mount_pool_equivalence` as oracle-driven rows (the oracle
   emits `flattenTierPool(pool, opts)` for `{includeCharacterTier: false,
   includeParticipants: false}` and `{includeParticipants: true}` over
   v4's literal pool; the Rust side over the same literal) — RED-FIRST
   on the first (v5 has no way to drop the tier).
2. **The context flag** (`doc_edit/path_resolver.rs`): `hide_character_
   vaults: bool` on `PathResolutionContext` with v4's doc; every literal
   in the tree updated (the count in the lane record); `collect_
   accessible_mount_point_ids` resolving with `include_participants:
   vaults_visible` and flattening with both options `vaults_visible`; the
   operator-override arm untouched.
3. **The self-token gate's condition** — `character_id.is_some() &&
   !hide_character_vaults && needle == SELF_VAULT_TOKEN`; with the flag
   set the token falls through to the loops (v4's shape — NOT a new
   refusal arm, §R.4).
4. **The out-of-scope split**: `find_enabled_mount_point_by_ref(mount,
   ref)` over `find_enabled_for_docedit` — name (lowercased) FIRST then
   id, `store_type == "character"` → `None`, a read error → `None`
   (fail-soft, as v4's `catch`); the not-found arm consults it BEFORE
   answering: a hit → the WARN `Mount point exists but is out of scope:
   <name> (<id>) (project: <id|none>, characters: <describe>,
   vaultsHidden: <bool>)` + `ACCESS_DENIED` with v4's three-sentence
   message byte-exact; a miss → the WARN `Mount point not found or not
   accessible: <ref> (project: …, characters: …, vaultsHidden: …)` +
   the unchanged `NOT_FOUND`. `describe_characters(ctx)` ported (a
   `Set`-ordered join — v4's `Set` preserves INSERTION order:
   `characterId` first, then `characterIds` in order, duplicates dropped;
   `none` when empty). Both lines capture-pinned (message bytes, level,
   silence on a resolved store).
5. **The two builders** (`tools/doc_edit/shared.rs`): the opaque branch
   returns `character_id: ctx.character_id.clone(), character_ids: Vec::
   new(), hide_character_vaults: true` (v4's shape — peers never admitted
   for an opaque character, `characterIds` absent); the transparent
   branches unchanged (`hide_character_vaults: false`). v4's why-comment
   carried at both sites naming the sha.
6. **The enumerator's query** (`get_accessible_mount_points`): ONE
   `AccessibleMountPointsQuery { project_id, character_id, extra_character_
   ids, hide_character_vaults }` parameter replacing the three positionals
   (v4's shape — keep the `extra_character_ids.is_empty() → None` rule);
   the flag threaded to the collector so listing and resolution share one
   rule. The FOUR call sites derive it from `acting_character_is_opaque_
   to_vaults(main, ctx)` at v4's positions: grep and list_files AFTER the
   peer collection; the blob READ resolver after `resolve_mount_point_ref`
   + the peer collection WITH peers; the blob WRITE resolver after the
   peer-vault assert WITHOUT peers (§R.4). `mount_point: "self"` under the
   covenant stays an EMPTY listing/search and an unresolved blob mount —
   no new refusal sentence (v4 has none).
7. **The help page** byte-copied from the `89fcc3c0d` pin (`cp` from the
   pin worktree; md5 `a70d5054…` asserted in the lane record);
   `help_tree_embed_guard.rs`'s history comment gains the line (124 stays
   124); `help_tree_equivalence` regenerated at the TARGET pin and green;
   the pin verification grep recorded.
8. **The NEW family `doc_opacity_equivalence`** (Rust) + `harness/oracle/
   cases/doc-opacity.test.ts` (jest, the doc-enum mock set — the
   Librarian writer, reindex, embedding scheduler and vector store
   no-op'd; the cipher driver real; the repositories REAL over the
   fixture, NOT mocked as v4's suites mock them — the point of this family
   is the real pool + real repositories + real DB, which v4's suites
   cannot give) + `build-doc-opacity-fixture.ts` + `doc-opacity.json`.
   **The fixture** (built once per regen, copied to both sides; every
   non-minted id pinned in the json, the two minted vault ids read back
   identically on both sides — the DPR idiom): a users row; Leilani with
   `systemTransparency` ABSENT (v4's default — the opaque arm; do not
   spell it `false`, the absent shape is the one real instances carry)
   and Abigail with `true`, each through the REAL `repos.characters.
   create` (vaults minted); group "Severed" (raw INSERT + `ensure
   Collection`, the pool builder's recipe) with an official database
   store "Group Files: Severed" NOT linked to any project, Leilani AND
   Abigail members (Abigail's membership is what lets the transparent
   control reach the group store the way v4's `transparency: true` mock
   does); project "Papers" via the REAL `repos.projects.create` (its
   official store) plus one linked database store "Project Papers"; a
   "stranger" enabled database store "Someone Elses Papers" linked to
   nothing; the General store; a chat with BOTH as active `llm`
   participants and `allowCrossCharacterVaultReads: true`; one
   `notes.md` ("the quarry stone") in EVERY store incl. both vaults (so a
   grep for `quarry` discriminates by store), and one small blob in
   Leilani's vault and in the group store. **The ops** carry a per-op
   `characterId` (`leilani` | `abigail`) and a `kind`: `resolve_write` /
   `resolve_read` (through the REAL builders → `resolveDocEditPath
   ('document_store', path, ctx)`; the row records `{ok, mountPointId,
   mountPointName}` or `{ok: false, code, message}` — message bytes
   EXACT), `context_shape` (the built context serialized — `hideCharacter
   Vaults`, `characterId`, the ABSENCE of `characterIds`), `accessible`
   (`getAccessibleMountPoints(query)` → the ordered id+name list),
   `agreement` (every listed store re-resolved through the read builder,
   the per-store `mountPointId`), `tool` (`executeDocEditTool` +
   `formatDocEditResults` for `doc_list_files` / `doc_grep` / `doc_read_
   blob` / `doc_list_blobs` / `doc_write_blob` — the doc-enum/doc-blob
   comparand), and `flatten` (the two unit rows, or leave them in the
   pool family — say which). **The matrix mirrors v4's 28 cases** (the
   Preamble lists them): the four group-store resolutions + the project
   control; own vault by name / peer vault by name / `self` / the context
   shape; Abigail's vault + group store; the stranger → ACCESS_DENIED
   (message bytes) / `Group` → NOT_FOUND; the four `getAccessibleMount
   Points` rows (with flag, keeps group+project, without flag for
   Abigail, agreement); `doc_list_files` × 4; `doc_grep` × 3; the blob
   quartet (own-vault read + `self` read, peer-vault listing, own-vault
   write refused, group-store write lands). Normalization: the doc-blob
   normalizer for the write op; everything else EXACT (ids are shared).
   **RED-FIRST on unported `main`:** record which rows are red before any
   source change — the ledger's measurement predicts the group-store
   resolutions, the context shape, the stranger split, the flagged
   enumeration rows, the agreement row and every Leilani listing/grep/
   blob row (expect ≥ 15 of 28); the covenant-unchanged rows (own vault
   hidden, peer vault hidden, `self`) and every Abigail row must be GREEN
   before AND after (they are the "subtraction, not a loosening" proof).
   Count guard `>= 28`. Recipe header per `recipe-header-conventions`
   (indented, `--test`-scoped, `V5W=${V5W:-…}`, the builder invocation
   folded in, the jest filter ANCHORED `"doc-opacity\.test\.ts$"`, staged
   outside `.claude/`, no `/tmp` pin named).
9. **Mutation proofs** (each reddening EXACTLY its rows; revert by file
   backup): M1 the builders back to `character_id: None` → the group-
   store rows + the context-shape row red, the covenant rows green; M2
   `include_character_tier` ignored in the flatten → the own-vault and
   peer-vault rows red (the vaults resolve) + the flatten unit row; M3 the
   self-token gate's new conjunct dropped → the `self` row red; M4 the
   `store_type == "character"` exclusion dropped from `find_enabled_mount_
   point_by_ref` → the peer-vault row red (ACCESS_DENIED naming the vault
   — the leak); M5 ONE enumeration call site passing `false` → exactly
   that tool's Leilani rows red (run it once per site: grep, list_files,
   the read resolver, the write resolver — four runs, four disjoint red
   sets); M6 the `vaultsHidden` piece dropped from either warn → its
   capture pin red; M7 `describe_characters` joining in the wrong order or
   omitting the dedup → the capture pin over a two-character context red.
10. **Neutrality**: `doc_enum`, `doc_blob`, `doc_text`, `doc_fm`, `doc_fs`,
    `doc_ui`, `scriptorium_tools`, and the pre-existing rows of
    `tiered_mount_pool` + `doc_edit_path_resolver` regenerated at the
    BASELINE pin and green with the port applied; where the family mints
    ids across regens, compare normalized rows by case and record.
11. **Ratifications** on the file list (the Preamble's non-lib list) with
    evidence per file, and the §R.9 pre-list with byte counts at the
    `89fcc3c0d` pin.

### Tier 2 — should land

12. **The pre-existing absent warn lines the drift sits beside** — the
   #103/#110 class: `path_resolver.rs`'s other v4 `logger.warn` sites in
   `resolveDocumentStorePath` (`document_store scope requires projectId
   or characterId in context`, `Self-token resolution failed: no
   accessible vault for character` + `{characterId}`, `Attempt to access
   disabled mount point: <id>`) and `shared.ts`'s `systemTransparency
   lookup failed; defaulting to opaque` + `{characterId, error}` in the
   opacity helper. Port them at v4's levels with capture pins (each with
   a silence leg) IF the lane has the budget; otherwise record each as a
   NAMED absence in the lane record for the next logging pass (they are
   outside the drift's hunks, so leaving them is not a fidelity
   regression — silently dropping them from the record would be).
13. **`doc_edit_path_resolver_equivalence` grown** with a `hide-vaults`
    row pair driving `resolve_doc_edit_path` with the flag set directly
    (own vault by name → NOT_FOUND; a project store → resolves) — only if
    the fixture needs no rebuild (charA's `systemTransparency` is `null`
    there but the case bypasses the builders, so the flag is set by hand
    on both sides); otherwise the new family covers it — say which.
14. **The `docs/v4/` mirror pre-list** with byte counts (§R.9) — the
    unifier copies; you list.

### Tier 3 — explicit deferrals (loud, typed, never silent)

- No verb, no route, no SPA surface — the covenant is server-internal and
  v4's commits touch none (§S.1). If the lane finds an SPA surface that
  enumerates stores through this path (the Document Mode picker does not —
  it has its own verbs; measure once, record), it STOPs on that item.
- The `run_sql`/Brahma operator surfaces pass `operator_override`, which
  bypasses the collector entirely — no change, record the measurement.
- `describeCharacters` is otherwise used by NO other v5 line (v4 uses it
  only in these warns) — no wider port.

## The differential requirement, restated

Every ported unit ships with an equivalence test driving v4's REAL code
(§R.6). Here: the NEW `doc_opacity_equivalence` over a REAL two-partition
fixture drives v4's real `buildReadResolutionContext` / `buildWrite
ResolutionContext` / `resolveDocEditPath` / `getAccessibleMountPoints` /
`executeDocEditTool` / `flattenTierPool` (the jest case imports them; the
repositories are the real ones over the fixture — the ONLY mocks are the
side-effect writers the doc-enum case already no-ops); `tiered_mount_pool_
equivalence` grows the flatten-option rows; `help_tree_equivalence` proves
the page bytes. The closest recipes: `doc_enum_equivalence` (the tool-op
comparand + the mock set), `doc_blob_equivalence` (the write-op
normalizer), `doc_edit_path_resolver_equivalence` (the resolve-row shape +
the minted-vault read-back), `tiered_mount_pool_equivalence` (the pool
fixture's group recipe). Tier 2 (structural DB) for the tool ops, tier 1
(exact) for the resolve/enumerate rows.

## Fixtures

- **Consumed:** `harness/oracle/fixtures/tiered-mount-pool.json` +
  `build-tiered-mount-pool-fixture.ts` (grown by the flatten rows' oracle
  only — the fixture is unchanged); every doc-* fixture (READ ONLY, the
  neutrality legs).
- **Delivered:** NEW `harness/oracle/fixtures/doc-opacity.json` +
  `build-doc-opacity-fixture.ts` (minted per regen — NOT committed; the
  builder's invocation folded into the family's recipe); `help/character-
  system-transparency.md` at the target pin.
- **Regen ownership:** this lane regenerates every family it touches and
  every neutrality leg named above. No other lane reads these fixtures.

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `89fcc3c0d` opacity-covenant drift catch-up + follow-ups round:
**P4.D200 ∥ P4.98 ∥ P4.99**, ordered 2026-09-18. Three lanes, three
worktrees, three lane branches, ALL from `main` (no stacking — every lane's
ownership is disjoint by file; there is no cross-lane constant and no shared
contract this round, §S). The unifier cherry-picks in the order
**P4.D200 → P4.98 → P4.99** (the drift lane first, then the two maintenance
lanes), gates ONCE, and `merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`bcd7e4852`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`89fcc3c0d`** ("fix
  (scriptorium): a hidden vault is not listed either (bug 153)",
  `4.10.0-dev.47`, 2026-09-17 22:37) — TWO commits past the baseline, both
  PORT on ported surfaces and both on the doc-edit opacity covenant:
  `1065a1f53` (bug 152, 22:23, `4.10.0-dev.46`) — `lib/doc-edit/path-
  resolver.ts` (+79), `lib/mount-index/tiered-mount-pool.ts` (+18),
  `lib/tools/handlers/doc-edit/shared.ts` (+20), `help/character-system-
  transparency.md` (+4), the 269-line `lib/doc-edit/__tests__/path-
  resolver-opacity-group-stores.test.ts`; and `89fcc3c0d` (bug 153) —
  `lib/doc-edit/path-resolver.ts` (+31), `lib/doc-edit/index.ts` (+2),
  `lib/tools/handlers/doc-edit/text-handlers.ts` (+22), `blob-handlers.ts`
  (+20), the same help page (+2), the 345-line `path-resolver-opacity-
  enumeration.test.ts`; plus README/CHANGELOG/bugs.md/`bugs/fixed/` and
  version stamps on both. The ledger's two §3 rows are `ORDERED(P4.D200)`.
  v4 `bugfix` tip `1a2b2164c` and `release` tip `8fbf2afe0` are unmoved;
  there is still no `release: 4.10.0` squash. The checkout sat on `main`,
  CLEAN, at the planning-START probe (2026-09-18 — the §2 probe PASSED,
  so the ledger stood and nothing was re-derived); **at the planning-CLOSE
  probe HEAD was unmoved and both logs empty but the tree had gone DIRTY on
  thirteen paths of a v4 fix in flight** (a characters default-system-
  prompt lockstep — `lib/characters/default-system-prompt.ts` NEW + the
  characters repository, the chat initializer, one route handler, the voice
  preview action, five client files, three tests; the exact list is in the
  ledger's §1). None of this round's regen surfaces is among them, and every
  regen runs from a pin that cannot carry dirt. **At unification the baseline
  MOVES to `89fcc3c0d`**; both rows retire `ABSORBED(P4.D200)` with their
  non-lib files ratified on P4.D200's list.
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log 89fcc3c0d..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Pass = branch `main`, HEAD `89fcc3c0d`, both logs EMPTY, and a
  tree that is either CLEAN or dirty ONLY within the thirteen paths the
  ledger's §1 records from the planning-close probe** (the in-flight v4
  default-system-prompt fix — compare `status --short` against that list
  path by path). Any mismatch — a moved HEAD (the fix landing is a NEW
  drift row, not a pass), a non-empty log, ANY dirty path outside the
  recorded set (`lib/`, `app/`, `components/`, `packages/`, `plugins/`,
  `help/`, `public/schemas/` or anywhere), the checkout off `main` — is a
  STOP: report it and do nothing else. A lane never writes the ledger.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1). HEAD is past the
  baseline and both drift commits move `lib/` and `help/`. Every regen
  runs from a LANE-UNIQUE detached worktree per ledger §5.1 (`git -C
  ~/source/quilltap-server worktree add --detach /tmp/qt-v4-pin-<order>-
  <sha> <sha>`, the THREE symlink classes — root `node_modules`,
  `packages/quilltap/node_modules`, every `plugins/dist/*/node_modules` —
  then `cd` into it; never stash or checkout-switch the human's repo;
  `git worktree list` before each batch; **verify the pin by `git -C
  "$PIN" rev-parse HEAD` AND by `ls -ld` of the path** — build every pin
  path from a quoted literal, never an unsplit zsh variable; then grep the
  fresh NDJSON for a marker only one tree has; check the file is non-empty
  before believing a diff). Which sha:
  - **P4.D200 pins the TARGET `89fcc3c0d`** for every family its port
    MOVES (the NEW `doc_opacity_equivalence`, `tiered_mount_pool_
    equivalence`, `doc_edit_path_resolver_equivalence` if it grows, `help_
    tree_equivalence`) and the BASELINE `bcd7e4852` for its neutrality
    legs (`doc_enum`, `doc_blob`, `doc_text`, `doc_fm`, `doc_fs`, `doc_ui`,
    `scriptorium_tools`, and the pre-existing rows of the two grown
    families). The target-pinned help-tree oracle MUST carry `Nor are they
    so much as named` and the baseline one MUST NOT — that pair is the pin
    verification.
  - **P4.98 and P4.99 pin the BASELINE `bcd7e4852`** — their v4 surfaces
    are untouched by the drift: `git -C ~/source/quilltap-server diff
    --stat bcd7e4852..89fcc3c0d -- app/api/v1/images/ lib/api/
    lib/services/chat-message/ plugins/` is EMPTY (measured at planning,
    2026-09-18; each lane re-records it in its lane record before the
    first regen). A pin at the baseline is still REQUIRED, not optional:
    the checkout can go dirty mid-lane (ledger §5.1), and a bare-checkout
    regen is unrepeatable.
  The sweep driver is the sanctioned per-family path (`harness/tools/
  recipe_sweep.py --run <family> --v4 "$PIN" --v5w <ABSOLUTE worktree>`);
  never run two sweeps concurrently; a family that reports `SKIP` in
  0.00 s did not run (`a-family-env-var-is-not-its-regen-var`). Ledger
  §5.2's silent-stale-pass discipline applies to every regen (`rm -f`
  the fixture first; read the builder's last line; `grep -c` the changed
  bytes in the fresh NDJSON — MUST be > 0). **Shared `/tmp` output paths
  are a live hazard** (the `/tmp/qt-imgcol-*` pair is read by BOTH
  `images_routes_equivalence` and `images_generate_route_equivalence`):
  every lane stages its regen outputs under a LANE-PRIVATE directory
  (`/tmp/<order>/…`) by pointing the family's env vars there, and leaves
  the committed recipe headers canonical (a committed recipe NEVER names
  a `/tmp` pin — the driver's `--v4` is the pin). The recorded provider
  corpora (P4.99's google pair) are regenerated by hand from the pin's
  plugin directory, never by a sweep stage.
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). The spec
  for every item is `git show <sha> -- <path>` plus `git show <sha>:<path>`
  at `89fcc3c0d` (P4.D200) or `bcd7e4852` (P4.98, P4.99); a commit message
  describes the bug as its author understood it and the hunks are often
  narrower. Two measured examples this round: bug 152's message says "the
  self token is now refused explicitly" — the hunk is a CONDITION added to
  the existing self-token gate (`&& !context.hideCharacterVaults`), not a
  new refusal arm; bug 153's message says "all four callers" derive the
  flag — the hunks show the WRITE resolver passes NO `extraCharacterIds`
  while the other three pass the peers. Re-verify even where an order
  flags the trap — the order's paragraph is the same kind of prose, and so
  is a previous lane's record or a doc comment in the tree. Carry a
  v5-side code comment naming the sha where the prose misleads.
- **§R.5 — Designed reds and pre-existing reds: measure, don't assume**
  (ledger §5.4). No convergence pin trips this round (neither v4 commit is
  this port's filing; both are v4-side finds from live Friday use).
  P4.D200's pre-fix reds are DESIGNED and PREDICTED by the ledger's
  2026-09-18 measurement: v5 REPRODUCES both bugs (the builders drop
  `character_id`; the enumerator takes no flag) — the new family's rows
  must be red on unported `main` before the port and the count recorded.
  P4.98's null arms are red at the dispatch WIRE only (the handler-direct
  family is green on both sides today). P4.99's line is a silence pin red.
  A red that appears in a family a lane did NOT touch is a NEW finding to
  classify at both pins before any source is touched, and it is probably
  not that lane's.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest`
  is shared) and its silence leg; a fixture change lands with the regen
  of every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that
  SURVIVES as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`). A dispatch WIRE
  test through `POST /api/dispatch` is the only instrument that sees a
  serde collapse (`a-dispatch-wire-test-sees-what-the-differential-
  cannot`); a family that calls the handler directly cannot.
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed on each commit per `.claude/commands/commit.md`; the unifier
  RECOUNTS as base + the sum of every lane's bumps (the silent-auto-merge
  trap: identical bumps merge as ONE — it fires every round). Base at
  planning: core 0.0.950, harness 0.0.842, web 0.0.152, host 0.0.139, cli
  0.0.22, tauri 0.0.7, SPA 0.5.731. Expected: **core** by all three
  lanes; **harness** by all three; **web** by P4.98 ONLY (the REST edge
  function, the census and the new dispatch wire test); **host, cli,
  tauri and the SPA** by NOBODY (P4.D200's `help/` re-vendor changes the
  embedded table but no host SOURCE — the P4.D194 rule: no host bump for
  help alone; no lane touches `apps/web/**`).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  P4.D200 pre-lists the four paths the two commits move (`docs/v4/
  CHANGELOG.md`, `docs/v4/developer/bugs.md`, the NEW `docs/v4/developer/
  bugs/fixed/bug-152-opacity-hides-group-stores.md` and `bug-153-opacity-
  enumeration-leak.md`) with byte counts at the `89fcc3c0d` pin in its
  lane record; no lane copies them.
- **§R.10 — Cross-lane fences, named:** (a) **`api/types.rs`, `api/
  engine.rs`, `api/images.rs`, `api/image_profiles.rs` and `crates/
  quilltap-web/tests/dispatch_wrong_type_census.rs` are P4.98's alone** —
  the unifier asserts `git diff main...<lane> -- <those five paths>` is
  EMPTY for P4.D200 and P4.99 before picking. (b) **`services/primary_
  stream.rs` and `model/request_builder/google.rs` are P4.99's alone**;
  the same empty-diff assertion for P4.D200 and P4.98. (c) **`crates/
  quilltap-harness/src/lib.rs` is P4.99's alone** (the `CorpusScript`
  DRY) — P4.D200's new family keeps its helpers in its own test file.
  (d) **The `/tmp/qt-imgcol-*` staging pair** (`images_routes_
  equivalence` ↔ `images_generate_route_equivalence` share the committed
  `images-{main,mount}.db` copy and the `images-collection.json`
  fixture): P4.98 stages under `/tmp/p4.98/`; at the unified gate the
  two families are run by name, one after the other, each on its own
  fresh staging. (e) **The recorded google corpora** (`google-request.
  recorded.ndjson`, `google-wire.recorded.ndjson`) and their recorder are
  P4.99's alone; no other lane records. (f) **There is no cross-lane
  constant, no shared type and no handoff this round** — each lane's
  output is consumed by nobody else; if a lane finds it needs another
  lane's file, it STOPs on that item and records the shape for the
  unifier.
- **§R.11 — No SPA behaviour moves this round.** No lane touches
  `apps/web/**`; no beat is added or flipped; the unified gate runs the
  full Playwright suite once against the release binary as regression
  only (the documented intermittents and the standing parks stand as
  named in the previous round's gate: 322 passed / 0 failed / 6 skipped).

## §S — Shared contract (binding; byte-identical in every order of this round)

- **§S.1 — Nothing on the wire moves for any consumer this round.**
  P4.D200 adds no dispatch verb, no REST route and no `core-contract.ts`
  type — the doc-edit opacity covenant is a server-internal rule reached
  only through the `doc_*` tool handlers and the path resolver (v4's two
  commits touch no `app/` or `components/` file). P4.98's tri-state is
  transport-INTERNAL: the five body keys of `Request::ImagesGenerate`
  keep their camelCase wire names (`prompt`, `profileId`, `chatId`,
  `tags`, `options`) and their v4 semantics; only the Rust carry changes
  from `Option<Value>` to `Option<Option<Value>>`, so an explicit JSON
  `null` reaches the handler as `Value::Null` on dispatch and Tauri IPC
  exactly as it already does on the REST edge. The SPA never posts to
  this verb (measured at planning: `core-contract.ts` carries no
  `imagesGenerate` request; the only `?action=generate` client is the
  image-PROFILE dialog). P4.99 adds one log line and two corpus rows; no
  request or response byte changes.
- **§S.2 — The one v4 type shape every lane reads the same way:**
  `hideCharacterVaults?: boolean` on `PathResolutionContext` and on the
  new `AccessibleMountPointsQuery` (v4 `path-resolver.ts:126`, `:848`)
  is P4.D200's to port as a Rust field named `hide_character_vaults`;
  no other lane names it. `includeCharacterTier` on `flattenTierPool`
  (v4 `tiered-mount-pool.ts:352-376`) is P4.D200's to port; the two
  other flatten call sites outside its ownership (`tools/search.rs:440`)
  are adapted by P4.D200 as a signature-only edit (§Ownership).
- **§S.3 — A drift lane that finds it needs a wire change STOPs on that
  item** and records the shape for the unifier rather than inventing a
  verb.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D200** — bugs 152 + 153, the doc-edit opacity covenant | `crates/quilltap-core/src/db/tiered_mount_pool.rs` (the flatten option + doc); `crates/quilltap-core/src/doc_edit/path_resolver.rs` (the context flag, the collector, the self-token gate, the out-of-scope split + its two warn lines, `describe_characters`, the query struct + `get_accessible_mount_points` if it moves here — say which); `crates/quilltap-core/src/db/doc_mount_points.rs` — ONLY an additive `store_type` on `DmpRow` / `find_enabled_for_docedit` if the survey's measurement says the row lacks it (say so); `crates/quilltap-core/src/tools/doc_edit/shared.rs` (the two builders, the opacity helper's warn, `get_accessible_mount_points`); `crates/quilltap-core/src/tools/doc_edit/text.rs` — the grep + list_files enumeration call sites ONLY; `crates/quilltap-core/src/tools/doc_edit/blob.rs` — the two mount resolvers ONLY; `crates/quilltap-core/src/tools/search.rs:440` — the flatten call's SIGNATURE adaptation only; `help/character-system-transparency.md` (byte copy at the target pin); `crates/quilltap-harness/tests/help_tree_embed_guard.rs` — the history doc comment ONLY (124 stays 124); NEW `crates/quilltap-harness/tests/doc_opacity_equivalence.rs` + NEW `harness/oracle/cases/doc-opacity.test.ts` + NEW `harness/oracle/fixtures/build-doc-opacity-fixture.ts` + `doc-opacity.json`; `crates/quilltap-harness/tests/tiered_mount_pool_equivalence.rs` + `harness/oracle/cases/tiered-mount-pool.ts`; `crates/quilltap-harness/tests/doc_edit_path_resolver_equivalence.rs` + `harness/oracle/cases/doc-edit-path-resolver.ts` + its fixture builder/json ONLY if Tier-2 item 13 lands (say so); this order's header | `api/**`, `crates/quilltap-web/**`, `crates/quilltap-host/**`, `services/**` (incl. `primary_stream.rs`), `model/**`, every other `help/*.md`, every recorded provider corpus, `crates/quilltap-harness/src/lib.rs`, every other harness family (the neutrality legs are RE-RUN, never edited), `apps/web/**`, the drift ledger |
| **P4.98** — `Request::ImagesGenerate`'s tri-state | `crates/quilltap-core/src/api/types.rs` — the `ImagesGenerate` variant + its doc and the `:1062` rule paragraph ONLY; `crates/quilltap-core/src/api/engine.rs` — the `ImagesGenerate` arm (`:5218-5241`) ONLY; `crates/quilltap-core/src/api/images.rs` — `parse_generate_body` + the route doc ONLY if a measurement says the handler must change (say so); `crates/quilltap-core/src/api/image_profiles.rs` — the `generate_parsed_type` retirement ONLY (Tier 2); `crates/quilltap-web/src/images_routes.rs` — the `images_generate` edge function ONLY; `crates/quilltap-web/tests/images_edge_routes.rs` (the null arm); `crates/quilltap-web/tests/dispatch_wrong_type_census.rs`; NEW `crates/quilltap-web/tests/images_generate_dispatch_wire.rs`; `crates/quilltap-harness/tests/images_generate_route_equivalence.rs` + `harness/oracle/cases/images-generate-route.test.ts` (the new row); this order's header | every other `api/*.rs`, `crates/quilltap-web/src/**` beyond the one function, `services/**`, `model/**`, `doc_edit/**`, `tools/**`, `db/**`, `help/**`, the committed `images-{main,mount}.db` pair and `images-collection.json` (READ ONLY — a copy is staged), every recorded provider corpus, every other harness family (incl. `images_routes_equivalence` — RE-RUN by name only), `apps/web/**`, the drift ledger |
| **P4.99** — the recovery INFO line + the google cache-key rows + the harness DRY | `crates/quilltap-core/src/services/primary_stream.rs` — the request-limit branch (`:1386-1414`) + its `#[cfg(test)]` pin; `crates/quilltap-core/src/model/request_builder/google.rs` — its `#[cfg(test)]` module ONLY (the ignorer unit pin); `harness/oracle/providers/record-google-request.mjs` + `regenerate-google-wire.sh`; `harness/oracle/fixtures/request-envelopes/google-request.recorded.ndjson` + `google-wire.recorded.ndjson` (re-recorded at the baseline pin); `crates/quilltap-harness/tests/request_builder_google_equivalence.rs` + `request_builder_google_wire_equivalence.rs`; `crates/quilltap-harness/tests/request_builder_equivalence.rs` — the `CACHE_KEY_IGNORED` doc comment ONLY; `crates/quilltap-harness/src/lib.rs` (the `CorpusScript` DRY home, additive); `crates/quilltap-harness/tests/llm_image_budget_equivalence.rs` + `file_attachment_tier3_equivalence.rs` — the DRY repoint + the anchored log asserts ONLY (no row, no fixture, no oracle change); `crates/quilltap-harness/tests/primary_stream_tier3_equivalence.rs` + `harness/oracle/cases/primary-stream-tier3.test.ts` ONLY if Tier-1 item 2's measurement finds a recovery-reaching case whose rig can carry the pin (say so); this order's header | `services/primary_stream.rs` beyond the one branch, `services/recovery.rs`, `services/orchestrator.rs`, `model/request_builder/google.rs` production code, every other recorded corpus (`request-envelopes.recorded.ndjson`, `image-dialects/`, `response-bodies/`, `stream-fixtures/`, `tool-wire/`, `moderation-wire/`, `web-search-wire/`), `api/**`, `crates/quilltap-web/**`, `doc_edit/**`, `tools/**`, `help/**`, every other harness family, `apps/web/**`, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0),
   and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp` / by-label diff) where the family is
   deterministic across regens; where it is not (`regenerate-at-both-
   pins-and-cmp-is-not-universal`), compare the normalized rows by case
   and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean (the Playwright venue
   prefers `target/release`).
8. `cargo test --workspace --no-fail-fast -- --nocapture` with the lane's
   env block (its own families' vars; every other family's var WITHHELD
   so it SKIPs honestly rather than failing on a deleted `/tmp` path):
   every binary's `test result:` line read, zero failures, the lane's
   families confirmed RUN by non-zero duration.
9. **Source censuses** that the change moves (P4.98: the dispatch
   wrong-type census + `EXCLUDED_BY_THE_ROUTE_IDENTIFIER_RULE`; P4.D200:
   `help_tree_embed_guard`, the qtap/help count guards; P4.99: none
   expected — say so if one moves) re-run by name and their constants
   recounted with the arithmetic in the comment.
10. **Commit per `.claude/commands/commit.md`** (one unit per commit where
    the order tiers them; CHANGELOG entry + version bumps per §R.8; the
    status-log lane record appended with the regen commands AS RUN, the
    mutation table, the pre-fix counts, the measurements, and every
    finding outside the lane's ownership named for the unifier). The lane
    closes by updating its own order's status header with what landed,
    what was deferred, and what it measured that the order got wrong.
