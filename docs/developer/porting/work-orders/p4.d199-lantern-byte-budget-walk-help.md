# Work order P4.D199 — v4 `bcd7e4852` (bug 151), the WALK half + help + the non-lib ratifications: the Lantern unseen-image walk spends `LANTERN_IMAGE_BASE64_BUDGET` (2 MiB) per turn NEWEST-FIRST over the base64 the turn will really send, drops what does not fit with v4's WARN, and restores CHRONOLOGICAL order for the attachments (`services/message_context.rs` section K over the seam's `LanternLoad`), proven by `orchestrator_tier3` arms that can SEE the drop and the order; `help/connection-profiles.md` re-vendored byte-exact (124 stays 124); the commit's non-lib files ratified on their list; the `docs/v4/` mirror paths pre-listed for the unifier — from `main`

**Status: CLOSED WHOLE** (lane close 2026-09-17, branch
`claude/lantern-byte-budget-walk-306c9b`). Ordered 2026-09-17 as part of the
`bcd7e4852` bug-151 drift catch-up + follow-ups round (P4.D198 ∥ P4.D199 ∥
P4.96 ∥ P4.97). The unifier rewrites this header at unification.

**Landed — every Tier-1 and Tier-2 item:**

1. `LANTERN_IMAGE_BASE64_BUDGET` private in `services/message_context.rs` with
   v4's doc and the **§R.10(a) handoff marker** the unifier greps.
2. The newest-first spend over the `LanternLoad`, `>` not `>=`, v4's `?? 0`
   keeping a bodyless attachment, `kept.reverse()` restoring chronological
   order, the prefix used as returned.
3. The WARN, sentence byte-exact, v4's five fields in v4's order — five unit
   pins (firing leg, silence leg, exact fit, bodyless, single-over) and four
   mutation proofs, each reddening exactly its targets.
4. Five `orchestrator_tier3` arms over v4's REAL `buildMessageContext`, with
   the `fsmBytesFill` spec key on all three sides and an optional
   `files.description`. **Red-first recorded.**
5. `help/connection-profiles.md` re-vendored; the whole 124-file tree is
   md5-identical to v4 at `bcd7e4852`, file for file; the guard's history doc
   extended; `help_tree_equivalence` regenerated at the target pin and green,
   red-first proven.
6. The ratifications on the file list, in the lane record.
7. Neutrality: 56 pre-existing orchestrator cases unmoved at both pins;
   `build_context_tier3` regenerated at the target pin and green.
8. Item 8 confirmed from the hunk: v4 adds **no** debug companion here.
9. The `combined.log` rendering note, for the dogfood acceptance grep.
10. The `docs/v4/` mirror pre-list with byte counts and md5s (the unifier
    copies them; this lane copied nothing).

**Measurements that corrected the order:**

* **Item 4(b) needed no mock widening.** The P4.D154 (bug 121) side channel
  already records the WHOLE per-message attachment slate per canned key on both
  sides — filenames, order and the base64 itself. The `carriers == 1` floor was
  rewritten into the ordered filename lists the corpus is supposed to produce.
* **Item 4(c): `build_context_tier3` drives `buildContext`, not
  `buildMessageContext`** — it has no `MessageContextSeams` at all, real or
  Noop. No arm goes there; it is a neutrality leg only.
* **The `help_*` content families that read `connection-profiles.md`: there are
  none.** Only `help_tree_equivalence` (vendored bytes vs v4's at the pin) and
  `help_tree_embed_guard` (embedded table vs disk) read the vendored tree;
  `help_doc_sync`, `help_doc_sync_guards` and `help_doc_ensure` each walk their
  own synthetic fixture tree, and every other `connection-profiles` hit in the
  harness is the `connection_profiles` TABLE.
* **The junk-byte pass-through is measured, not inferred from v4's stderr.**
  `--silent` suppresses v4's `Could not shrink image for LLM transport` warn;
  the proof of record is the recorded base64 lengths (307,200 planted,
  307,200 on the wire) plus the ANTHROPIC 5 MiB ceiling that keeps both sides'
  backstop resize out of it.

**Deviation from the prescribed shape, recorded:** the spend landed as a
private function in `message_context.rs` that section K calls, rather than
inline in section K, because that is the file's idiom (the sibling USER budget
is its own function) and because a unit pin through the whole
`build_message_context` composition would need a DB, embeddings, an executor
and the build-context seams. The section-K wiring is pinned by the corpus arms.

**Recorded divergence for the unifier:** v5 still dispatches the per-file
fallback OLDEST-first (it lives below the seam) where v4 now dispatches it
newest-first. No comparand this port has moves — the prefix bytes, the kept
membership, the kept order and the warn are all identical — but the ORDER of
the fallback's own side effects on a non-vision seat (the per-image describe
calls and their `llm_logs` rows) does. Named in
`spend_lantern_image_budget`'s doc comment with the sha.

**Deferred loud (Tier 3, as ordered):** the post-SHRINK interplay is not
measurable in this worktree and is not claimed — §R.10(b) puts it at the
unifier's re-run over the union; the 💸 live proof joins the dogfood queue; the
USER-side re-hydration budget was not touched.

## Preamble

- **v4 oracle baseline: `5f0a57dc4`.** This lane absorbs the WALK half of
  **`bcd7e4852`** ("fix(images): send a model the picture, not the archive of
  it (bug 151)", 2026-09-17 10:15, `4.10.0-dev.45`): `lib/services/chat-
  message/context-builder.service.ts` (+50/−3 — the per-turn budget inside
  the Lantern unseen-image loop), `help/connection-profiles.md` (+12, the
  "A travelling portrait packs light" section), and the commit's NON-LIB
  files for ratification on their list: `CLAUDE.md` (+1 rule line — v4's
  own CLAUDE.md, not ours), `README.md` (the version badge + a "Last
  updated" date), `package.json` / `package-lock.json` / `packages/
  quilltap/package.json` (the `4.10.0-dev.45` stamps), `docs/CHANGELOG.md`,
  `docs/developer/bugs.md`, `docs/developer/bugs/fixed/bug-151-unbudgeted-
  image-bytes-on-the-wire.md` (NEW, 194), `.claude/commands/update-
  documentation.md`, and the 211-line `__tests__/unit/lib/files/llm-image-
  budget.test.ts` (P4.D198's corpus source — you do not read it for this
  lane). The loader half (`lib/files/llm-image-budget.ts`, `lib/chat-files-
  v2.ts`) is **P4.D198's**. The ledger row is `ORDERED(P4.D198, P4.D199)`.
  Spec reads: `git show bcd7e4852 -- lib/services/chat-message/context-
  builder.service.ts` (the hunks) and `git show bcd7e4852:lib/services/
  chat-message/context-builder.service.ts` `:1120-1216` (the post-commit
  loop) — **never the commit message** (§R.4: the message says "spent
  newest-first: when a budget forces a drop, keep the picture just
  generated", which is right, but only the FILE says that the describe-
  fallback prefix is UNBUDGETED, that the budget counts `data.length` of
  the base64 STRING, that a dropped file still had its fallback run, and
  that the restore is TWO `reverse()`s — one on the prefixes, one on the
  kept attachments).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** Pin **`/tmp/qt-v4-pin-p4d199-
  bcd7e4852`** (the TARGET — v4 HEAD) for every family this port MOVES:
  `orchestrator_tier3` (the new budget arms; v4's REAL walk runs there),
  `build_context_tier3` (ONLY if its seams are real — Tier 1 item 4
  measures), `help_tree_equivalence` (the ONE family that compares the
  vendored tree to v4 — `help-embed-guard-cannot-see-a-stale-revendor`),
  and the two `help_*` content families that read `connection-profiles.md`
  (measure which: `grep -l connection-profiles crates/quilltap-harness/
  tests/help_*`). Pin **`/tmp/qt-v4-pin-p4d199-5f0a57dc4`** (the BASELINE)
  for the neutrality leg: the orchestrator corpus's PRE-EXISTING cases,
  regenerated at both pins and compared BY CASE (this family is NOT
  deterministic across regens — `regenerate-at-both-pins-and-cmp-is-not-
  universal`: minted ids and clocks move; compare the normalized rows the
  Rust side compares, by case label, and record). Pin verification: the
  target-pinned help-tree NDJSON carries `A travelling portrait packs
  light`; the baseline one does not; the target-pinned orchestrator NDJSON
  carries the new case labels. ⚠ All three symlink classes (ledger §5.1).
- **Sibling lanes:** P4.D198 (the LOADER half — it owns `services/chat_
  files.rs` incl. `load_lantern_images` and `files/image_processing.rs`,
  DEFINES the public `LANTERN_IMAGE_BASE64_BUDGET` in the NEW `files/llm_
  image_budget.rs`, and grows `file_attachment_tier3`; §S.1 names the one
  seam you meet it at), P4.96 (`api/types.rs` / `api/engine.rs` / the
  census — you touch none), P4.97 (`services/primary_stream.rs` + the
  recorded provider corpora — you touch none).
- **Worktree:** `git worktree add <path> -b claude/p4-d199-lantern-byte-
  budget-<hash> main`; `CARGO_INCREMENTAL=0`. No SPA work.
- **Read before the work:** `crates/quilltap-core/src/services/message_
  context.rs` `:50-52` (`ASSISTANT_IMAGE_LOOKBACK = 6`), `:408-457`
  (`collect_lantern_image_file_ids_for_character` — walks BACKWARDS, stops
  at the character's own prior response, dedupes, `collected.reverse()` at
  `:455` → returns CHRONOLOGICAL), `:599-604` + `:706-806` (the USER-side
  twin `rehydrate_user_attachments` with its CHAR budget spent OLDEST-first
  — v4's own comment contrasts the two; the `:788-793` warn is the field-
  bag shape to mirror), `:614-617` (`LanternLoad { prefix, attachments }`),
  `:650-685` (`MessageContextSeams` — `load_lantern_images(&ids, provider)`;
  `NoopMessageContextSeams`), `:1337-1361` (section K — the call site you
  extend), `:1384-1388` (the anchor warn beside it), `:1440-1449` (the
  prefix injection); `crates/quilltap-core/src/services/chat_files.rs`
  `:469-527` (`load_lantern_images` — P4.D198's file; READ ONLY: the
  per-file fallback loop, `lantern_prefix.push_str` at `:515`, the
  keep-when-`Unsupported && error.is_none()` filter at `:516-518`,
  attachments pushed in CHRONOLOGICAL order — so the `LanternLoad` you
  receive is v4's `extra` after its fallback pass, in v4's PRE-fix order);
  `crates/quilltap-harness/tests/orchestrator_tier3_equivalence.rs` WHOLE
  (the `CannedBytes` reader `:1040-1042` — `fsm_bytes` / `fsm_bytes_utf8`;
  `NotConfiguredTranscoder` at `:1104`; the P4.92 side-channel recording
  — what the stream mock records per call, and whether attachment
  FILENAMES in ORDER are among the recorded fields) + `harness/oracle/
  cases/orchestrator-tier3.test.ts` (`:480-520` — the `downloadFile` byte
  mock and the "planted files deliberately chosen so nothing model-backed
  runs" note) + `harness/oracle/fixtures/orchestrator-tier3.json` (the
  `files[]` spec keys at `:188-`) + `build-orchestrator-fixture.ts`;
  `crates/quilltap-harness/tests/help_tree_embed_guard.rs` (`VENDORED_
  FILE_COUNT = 124` at `:45`; the re-vendor history doc `:26-44` you
  extend); `crates/quilltap-harness/tests/help_tree_equivalence.rs`;
  `docs/developer/porting/work-orders/p4.d194-bug144-convergence-
  ratifications-help.md` (the ratification-on-file-list idiom + the
  `docs/v4/` pre-listing); `services/file_fallback.rs:438-469`
  (`format_fallback_as_message_prefix` — an `Unsupported` result with no
  error renders NO prefix, which is why a budget-dropped raw image leaves
  no trace but the warn).

## Mandate

Land bug 151's per-turn byte budget on the Lantern walk, byte-faithful in
OUTCOME to v4's loop: the budget is spent newest-first over the base64
string length of each raw attachment the seam kept, the ones that do not
fit are dropped (counted), v4's WARN fires once per turn when anything was
dropped, and the attachments the turn sends are the kept ones in
CHRONOLOGICAL order; the describe-fallback prefix is unbudgeted and its
order is unchanged. Do it in `services/message_context.rs` section K over
the `LanternLoad` the seam returns — NOT inside `load_lantern_images`
(P4.D198's file). Prove it with orchestrator corpus arms that can SEE which
attachments were sent and in what order (junk bytes both sides — no codec
needed, and the shrink is provably a pass-through on junk). Re-vendor the
help page. Ratify the commit's non-lib files on their list. Pre-list the
`docs/v4/` mirror paths. This lane does NOT touch `chat_files.rs`,
`image_processing.rs`, the NEW `llm_image_budget.rs` (P4.D198's), `api/
types.rs` / `engine.rs` / the census (P4.96's), `primary_stream.rs` / the
provider corpora (P4.97's), `orchestrator.rs` (nobody's — it constructs the
seams; you change nothing about construction), `apps/web/**`, or the drift
ledger.

## Survey-verified starting points (fresh survey 2026-09-17 at `bcd7e4852` and on `main` `78884832` — do not re-derive)

### v4 (at `bcd7e4852`)

- **`lib/services/chat-message/context-builder.service.ts`**, inside
  `buildMessageContext`, the section that begins "Additionally surface
  image attachments from Lantern notifications" (`:1113-`): the new
  why-comment `:1126-1135` (carry it); `const ASSISTANT_IMAGE_LOOKBACK = 6`
  `:1136`; `recentAssistantImageFileIds = collectLanternImageFileIdsFor
  Character(...)` `:1139`; `extra = await loadChatFilesForLLM(ids, {
  provider })` (chronological — the collector reverses); then, NEW:
  `let imageBudgetLeft = LANTERN_IMAGE_BASE64_BUDGET; let droppedForBudget
  = 0; const lanternPrefixesNewestFirst: string[] = []; for (const
  fileAttachment of [...extra].reverse()) { … fallbackResult = await
  processFileAttachmentFallback(...); const prefix = formatFallbackAs
  MessagePrefix(fallbackResult); if (prefix) lanternPrefixesNewestFirst.
  push(prefix); if (fallbackResult.type === 'unsupported' &&
  !fallbackResult.error) { const wireBytes = fileAttachment.data?.length
  ?? 0; if (wireBytes > imageBudgetLeft) { droppedForBudget++; continue }
  imageBudgetLeft -= wireBytes; lanternAttachmentsToKeep.push
  (fileAttachment) } }`; then `if (droppedForBudget > 0) logger.warn
  ('Unseen assistant images exceeded the per-turn byte budget; the oldest
  were not sent', { droppedForBudget, kept: lanternAttachmentsToKeep.
  length, budget: LANTERN_IMAGE_BASE64_BUDGET, budgetUsed: LANTERN_IMAGE_
  BASE64_BUDGET - imageBudgetLeft, characterParticipantId:
  characterParticipant.id })`; `lanternImagePrefix += lanternPrefixesNewest
  First.reverse().join('')`; `if (kept.length > 0) mergedAttachmentsToSend
  = [...mergedAttachmentsToSend, ...lanternAttachmentsToKeep.reverse()]`.
  Facts that matter for a post-hoc port: (i) `wireBytes` is the LENGTH OF
  THE BASE64 STRING (`data` is base64), not the stored `size`; (ii) a
  budget-dropped file has already had its fallback RUN (prefix computed)
  — but an `unsupported`-no-error result renders an EMPTY prefix, so the
  drop leaves nothing in the prompt; (iii) the drop applies ONLY inside the
  keep-raw branch — a file whose fallback produced a description (non-
  vision seat) is never budgeted; (iv) `kept` in the warn is the kept
  COUNT; (v) the prefix order after the double reverse equals the PRE-fix
  order (chronological) — so the prefix is order-neutral and only the
  attachments' membership moves; (vi) a budget exactly equal to the
  remaining bytes FITS (`>` not `>=`); (vii) `[...extra].reverse()` on
  ONE file and on ZERO files degenerates to the old behaviour.
- **`lib/files/llm-image-budget.ts:79`** — `LANTERN_IMAGE_BASE64_BUDGET =
  2 * 1024 * 1024` with its doc ("~2 MB is four images at the per-image
  ceiling … Spent newest-first: see the consumer's note").
- **`help/connection-profiles.md`** `:433-444` — the twelve new lines
  ("### A travelling portrait packs light" … "Nothing is re-saved, and
  nothing needs re-generating."), inserted before "## The Understudies:
  Fallback". The vendored tree was md5-identical to v4 at `5f0a57dc4`
  (P4.D197's re-vendor) and lags by exactly this file; 124 files stays 124.
- **The non-lib files** (from `git show --stat bcd7e4852`): `.claude/
  commands/update-documentation.md` (a "Last updated" date), `CLAUDE.md`
  (+1 — the "What an image costs a model…" rule line; v4-side guidance,
  NO-PORT — our CLAUDE.md is not a mirror), `README.md` (badge `dev.44` →
  `dev.45` + the bugs.md index line's date), the three `package*.json`
  stamps, `docs/CHANGELOG.md` (+18), `docs/developer/bugs.md` (+17/−?),
  `docs/developer/bugs/fixed/bug-151-…md` (NEW), the unit test. Tier R
  compares v4's REAL launcher — a `packages/quilltap/package.json` version
  stamp COULD move a `--version` arm: run Tier R once at the target pin
  (the P4.D194 idiom: `223/0` expected; if a case moves, it is the stamp
  and the case is regenerated, not ported).

### v5 (on `main` `78884832`)

- **`crates/quilltap-core/src/services/message_context.rs`** (2124 lines):
  section K `:1337-1361` — `merged` slate at `:1341-1345`, the collector
  call `:1348-1354`, `let lantern = mc_seams.load_lantern_images(&ids,
  params.provider).await` at `:1356`, then the prefix/attachments folded
  into the slate. `LanternLoad { prefix: String, attachments: Vec<Value> }`
  `:614-617`; each attachment `Value` has keys `id, filepath, filename,
  mimeType, size, data, url` (`chat_files.rs:250-258`) — `data` is the
  base64 STRING. `characterParticipant.id`'s twin is in scope at section K
  (the `character_participant_id` field the `:788-793` warn already
  carries — reuse the same binding). **No log line exists in the Lantern
  walk today** (the four warns in the file are the USER-side budget's and
  the anchor's). **No v5 twin of `LANTERN_IMAGE_BASE64_BUDGET` exists**
  (`REHYDRATED_ATTACHMENT_CHAR_BUDGET = 80_000` at `:604` is the USER-side
  CHAR budget — a different quantity; do not reuse it).
- **`RealMessageContextSeams::load_lantern_images`** (`chat_files.rs:591-
  605` → `:469-527`): returns the kept-raw attachments in CHRONOLOGICAL
  order (v4's `extra` order after the fallback pass) and the concatenated
  prefixes in the same order — i.e. exactly v4's PRE-fix output. Your
  post-hoc spend over `lantern.attachments` newest-first + restore
  produces v4's POST-fix `lanternAttachmentsToKeep.reverse()`; the prefix
  needs NOTHING (fact (v) above). Record this equivalence argument in the
  section-K comment, naming the sha and the two `reverse()`s it stands in
  for.
- **Harness:** `orchestrator_tier3_equivalence.rs` — test `orchestrator_
  tier3_matches_oracle` `:827`, env `QT_ORACLE_ORCHESTRATOR` +
  `QT_FIXTURE_ORCH_{MAIN,MOUNT}`, `TZ=UTC` both stages; the `CannedBytes`
  host-byte seam built from `spec.files[].fsm_bytes / fsm_bytes_utf8`
  (`:1040-1042`) — a byte ARRAY in JSON, unusable at 1.5 MB; the loaders
  run with `NotConfiguredTranscoder` (`:1104`) — after P4.D198 lands, the
  shrink's `shrink_to_webp` is the trait DEFAULT `Err` there → `unchanged`,
  and v4's real `sharp` on junk bytes throws → `unchanged`: **the two
  sides agree on junk by construction, which is what makes this lane
  independent of P4.D198.** `build_context_tier3_equivalence.rs` — test
  `build_context_tier3_matches_oracle` `:385`, env `QT_ORACLE_BUILD_
  CONTEXT` + `QT_FIXTURE_BC_{MAIN,MOUNT}`; whether it wires
  `RealMessageContextSeams` or `NoopMessageContextSeams` is a MEASUREMENT
  (Tier 1 item 4).
- **`help/`** lives at the workspace root (`help/connection-profiles.md`);
  embedded by `quilltap-host/build.rs` (`cargo:rerun-if-changed`); guards:
  `help_tree_embed_guard.rs` (`VENDORED_FILE_COUNT = 124` `:45`, the
  history doc `:26-44` — `connection-profiles.md` already named at `:36`
  for bug 141) and `help_tree_equivalence.rs` (`QT_ORACLE_HELP_TREE`, case
  `harness/oracle/cases/help-tree-sync.test.ts`; compares `count` /
  `walkOrder` / `docs` / `chunks` / `jobs`).
- **`docs/v4/`** mirrors `CHANGELOG.md` and `developer/{bugs.md, bugs/
  fixed/…}` among others — the three paths this commit moves are
  `docs/v4/CHANGELOG.md`, `docs/v4/developer/bugs.md`, and the NEW
  `docs/v4/developer/bugs/fixed/bug-151-unbudgeted-image-bytes-on-the-
  wire.md` (v4's `README.md`, `CLAUDE.md`, `.claude/**` are not mirrored).

## Tiered deliverables

### Tier 1 — must land

1. **The budget constant, locally, with the handoff marked.** Until
   P4.D198's `files/llm_image_budget.rs` exists on `main`, define `const
   LANTERN_IMAGE_BASE64_BUDGET: usize = 2 * 1024 * 1024;` PRIVATE in
   `message_context.rs` beside `ASSISTANT_IMAGE_LOOKBACK`, with v4's doc
   comment and a `// P4.D199 → P4.D198 handoff: the public home is
   files::llm_image_budget::LANTERN_IMAGE_BASE64_BUDGET; the unifier
   repoints this to an import (§R.10(a))` line. Do NOT create the module
   yourself.
2. **The spend, in section K.** After `load_lantern_images` returns: walk
   `lantern.attachments` NEWEST-first (iterate `.iter().rev()`), `wire_
   bytes = fa["data"].as_str().map(str::len).unwrap_or(0)` (v4's
   `data?.length ?? 0` — the base64 STRING length; a missing/non-string
   `data` counts 0 and is KEPT, as v4's `?? 0` keeps it), `if wire_bytes >
   budget_left { dropped += 1; continue }`, `budget_left -= wire_bytes`,
   push to `kept`; then `kept.reverse()` (chronological) replaces
   `lantern.attachments` in the slate; the prefix is used AS RETURNED
   (record fact (v)). `>` not `>=` (fact (vi)) — pin it with an exact-fit
   case.
3. **The WARN**, once per turn, only when `dropped > 0`: `tracing::warn!
   (dropped_for_budget = dropped, kept = kept.len(), budget = LANTERN_
   IMAGE_BASE64_BUDGET, budget_used = LANTERN_IMAGE_BASE64_BUDGET -
   budget_left, character_participant_id = %…, "Unseen assistant images
   exceeded the per-turn byte budget; the oldest were not sent")` — the
   sentence byte-exact, the five fields in v4's bag (snake_case per the
   tree's recorded convention; no `…Json` field — nothing here is an
   array), the same target discipline as the `:788-793` sibling (none).
   Capture-pin with a firing leg AND a silence leg (`captured_with`), each
   driven through `build_context` with a seam whose `load_lantern_images`
   answers a scripted `LanternLoad` (a test-local `MessageContextSeams`
   impl — the `NoopMessageContextSeams` shape with a canned answer);
   mutation: swap `>` for `>=` → the exact-fit unit reddens; drop the
   `reverse()` → the order unit reddens; warn on `dropped == 0` → the
   silence leg reddens.
4. **The corpus arms in `orchestrator_tier3`** — the family where v4's
   REAL `buildMessageContext` runs over real fixture rows. First MEASURE:
   (a) which fixture seat can receive raw images (the P4.D106 transport
   predicate + `supportsImageUpload` — `provider_can_transport_images`
   and the profile row; pick or add a seat, e.g. ANTHROPIC with
   `supportsImageUpload: 1`, whose registry entry transports `image/*`),
   (b) whether the stream mock's side-channel (the P4.92 idiom) records
   the ORDERED attachment filenames of each call — if not, add
   `attachmentFilenames` (ordered) to what the jest mock records per call
   AND to what the Rust canned provider records, exactly as
   `previousResponseId`/`stop` were added (do not touch the canned KEY —
   `model/stream.rs:512` is nobody's), (c) whether `build_context_tier3`
   wires real seams (if `Noop`, record it and put every arm in the
   orchestrator family; do NOT rewire that family's seams). Then add to
   `harness/oracle/fixtures/orchestrator-tier3.json` + `build-orchestrator-
   fixture.ts` + the Rust `CannedBytes` reader a **`fsmBytesFill: { byte:
   <0-255>, len: <n> }`** spec key (both sides expand it to `len` copies
   of `byte`; the `files.size` column planted = `len` so the row is
   self-consistent) — the existing `fsmBytes` array cannot carry a
   megabyte. Arms (each a chat where the responding character has NOT
   spoken since the Lantern published N assistant images, each `image/
   webp`, `mimeType` under `RESIZABLE_MIME_TYPES`, planted as ASSISTANT
   messages with `attachments` ids so the collector finds them; every
   byte size chosen so its BASE64 is under the 4 MB provider backstop —
   junk bytes must never reach `resize_image_for_provider` on either
   side):
   - **`lantern_budget_all_fit`** — two images at 300 KiB base64 each;
     nothing dropped; both sent, chronological; silence.
   - **`lantern_budget_drops_oldest`** — three images with base64 lengths
     chronological `[1.5 MiB, 0.3 MiB, 0.5 MiB]`: newest-first spend keeps
     `0.5` then `0.3` (0.8 MiB), the third would reach 2.3 MiB > 2 MiB →
     DROPPED; sent = `[0.3, 0.5]` chronological, `dropped_for_budget = 1`.
     An OLDEST-first spend keeps `[1.5, 0.3]` and drops `0.5` — the
     mutation discriminator.
   - **`lantern_budget_exact_fit`** — two images whose base64 lengths SUM
     to exactly 2,097,152; both kept (`>` not `>=`).
   - **`lantern_budget_single_over`** — one image whose base64 alone
     exceeds the budget; dropped; sent = none; `kept = 0`, `budget_used =
     0`; the turn still runs (the warn is not an error).
   - **`lantern_budget_nonvision_seat_unbudgeted`** — the same three
     images on a seat that CANNOT transport images: every file takes the
     describe fallback (a canned vision call → a recorded canned row on
     both sides, answered identically), NONE is raw-kept, so the budget
     never engages — pin `dropped == 0` and the prefixes present, in
     chronological order. This is fact (iii).
   Comparands: the recorded ordered attachment filenames of the primary
   call (membership AND order), the prompt bytes (the prefix), the DB
   state the family already compares, and the WARN's presence/fields via
   the family's capture channel if it has one (else the unit pin in item
   3 carries the warn). Pre-fix v5 goes RED on `drops_oldest` and
   `single_over` (it sends everything) — record the red-first counts.
   Verify by `grep -c` that the target-pinned NDJSON carries the new
   labels and that v4's `Could not shrink image for LLM transport` warn
   appears in the jest stderr for the junk rows (it proves v4's shrink
   ran and passed the bytes through — the pass-through premise, MEASURED,
   not assumed; the warn is not a comparand).
5. **`help/connection-profiles.md`** byte-copied from the target pin
   (`cp "$PIN/help/connection-profiles.md" help/`; `md5` both; the whole
   tree then md5-identical to v4 at `bcd7e4852` — measure all 124 and
   record); `help_tree_embed_guard.rs`'s history doc extended (124 stays
   124); `help_tree_equivalence` + the `help_*` content families that
   read the page regenerated at the target pin and green.
6. **The ratifications**, on the file list (the P4.D194 idiom): every
   non-lib path of `bcd7e4852` named with its disposition — v4's CLAUDE.md
   rule line NO-PORT (v4-side guidance; the port's rule lives in THIS
   order + P4.D198's module doc), the README/package stamps NO-PORT with
   Tier R run once at the target pin (expected 223/0), the docs NO-PORT
   (mirror paths pre-listed for the unifier), the unit test → P4.D198's
   corpus. Record in the lane record so the unifier can write the
   ledger's `ABSORBED(P4.D198, P4.D199)` row from it.
7. **Neutrality:** every PRE-EXISTING orchestrator case compared at both
   pins BY LABEL over the normalized rows (record which cases are
   non-deterministic across regens at the SAME pin and why, per the
   `ffb6b3119` round's finding); `build_context_tier3` regenerated at the
   target pin and green with zero change on its side.

### Tier 2 — should land

8. **The debug-level companion** v4 does NOT emit here (there is none —
   confirm by reading the hunk; do not invent one).
9. **A `combined.log` rendering note** in the lane record: the exact line
   the warn produces through the file layer (P4.49's shape), for the
   dogfood queue's acceptance grep.
10. **The `docs/v4/` mirror pre-list** with byte counts at the pin:
    `docs/v4/CHANGELOG.md`, `docs/v4/developer/bugs.md`, `docs/v4/
    developer/bugs/fixed/bug-151-unbudgeted-image-bytes-on-the-wire.md`
    (NEW) — the unifier copies them (§R.9); you copy nothing.

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **The post-SHRINK interplay** (a real avatar shrunk by P4.D198's loaders
  and THEN budgeted here) is not measurable in this lane's worktree
  (`NotConfiguredTranscoder` + junk bytes on both sides); it composes by
  construction (`data` is whatever the loader returned) and the unifier
  re-runs this family over the union (§R.10(b)). The 💸 live proof — a
  turn carrying several fresh Lantern portraits on a vision seat, the
  debug/warn lines in `combined.log` — goes to the dogfood queue.
- **The USER-side re-hydration budget** (`rehydrate_user_attachments`) is
  untouched by the drift — v4 kept it OLDEST-first and CHAR-counted; do
  not "harmonize" it.

## The differential requirement, restated

- Tier 3 mocked-LLM: `orchestrator_tier3_equivalence` drives v4's REAL
  `buildMessageContext` + `loadChatFilesForLLM` + `processFileAttachment
  Fallback` under jest with the `downloadFile` byte mock (the case's own
  header recipe; add the `fsmBytesFill` expansion beside the existing
  `fsmBytes` arms on BOTH sides). The canned key cannot see attachment
  order — the ORDERED filenames ride the P4.92 side-channel (measure
  whether it already does). Closest recipe: the P4.D109 attachment-anchor
  arms in the same family + P4.92's side-channel rows.
- Capture pins (item 3) and the help families (item 5) as named.

## Fixtures

- Consumes and EDITS: `harness/oracle/fixtures/orchestrator-tier3.json`,
  `build-orchestrator-fixture.ts` (the `fsmBytesFill` key + the new
  chats/messages/files rows), the Rust `CannedBytes` reader; stages the
  `/tmp/qt-orch-*.db` pair under `/tmp/p4.d199/` (§R.3's lane-private
  rule — this family's shared paths were clobbered by a sibling two rounds
  ago) and keeps the committed recipe header canonical.
- Delivers: `help/connection-profiles.md` (byte copy), the guard doc line,
  the mirror pre-list.
- Nobody else regenerates the orchestrator or help families this round
  (P4.D198 regenerates `file_attachment_tier3` + its new tier-1 family;
  P4.97 regenerates `primary_stream_tier3` and the request-envelopes
  corpus; P4.96 the `image_generate_route` family).

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `bcd7e4852` bug-151 drift catch-up + follow-ups round:
**P4.D198 ∥ P4.D199 ∥ P4.96 ∥ P4.97**, ordered 2026-09-17. Four lanes, four
worktrees, four lane branches, ALL from `main` (no stacking — every lane's
ownership is disjoint by file; the one cross-lane constant is handed off by
a marked local definition, §R.10(a)). The unifier cherry-picks in the order
**P4.D198 → P4.D199 → P4.96 → P4.97** (the loader half before the walk half
that reads its output; the two maintenance lanes after the drift), gates
ONCE, and `merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`5f0a57dc4`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`bcd7e4852`**
  ("fix(images): send a model the picture, not the archive of it (bug
  151)", `4.10.0-dev.45`, 2026-09-17 10:15) — ONE commit past the
  baseline, a PORT on ported surfaces: `lib/files/llm-image-budget.ts`
  (NEW), `lib/chat-files-v2.ts` (both attachment loaders), `lib/services/
  chat-message/context-builder.service.ts` (the Lantern walk's per-turn
  byte budget), `help/connection-profiles.md` (+12), plus tests, docs,
  v4's CLAUDE.md rule line and version stamps. The ledger's one §3 row is
  `ORDERED(P4.D198, P4.D199)`. v4 `bugfix` tip `1a2b2164c` and `release`
  tip `8fbf2afe0` are unmoved; there is still no `release: 4.10.0` squash.
  The checkout sat on `main`, CLEAN, at the planning probe (2026-09-17
  afternoon — the §2 probe PASSED, so the ledger stood and nothing was
  re-derived). **At unification the baseline MOVES to `bcd7e4852`**;
  `bcd7e4852` retires `ABSORBED(P4.D198, P4.D199)` with its non-lib files
  ratified on P4.D199's list.
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log bcd7e4852..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Pass = branch `main`, HEAD `bcd7e4852`, both logs EMPTY, tree
  CLEAN.** Any mismatch — a moved HEAD, a dirty `lib/`/`app/`/`components/`/
  `packages/`/`plugins/`/`help/`/`public/schemas/` tree, the checkout off
  `main` — is a STOP: report it and do nothing else. A lane never writes
  the ledger.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1). HEAD is past the
  baseline and `bcd7e4852` moves `lib/` and `help/`. Every regen runs from
  a LANE-UNIQUE detached worktree per ledger §5.1 (`git -C ~/source/
  quilltap-server worktree add --detach /tmp/qt-v4-pin-<order>-<sha>
  <sha>`, the THREE symlink classes — root `node_modules`, `packages/
  quilltap/node_modules`, every `plugins/dist/*/node_modules` — then `cd`
  into it; never stash or checkout-switch the human's repo; `git worktree
  list` before each batch; **verify the pin by `git -C "$PIN" rev-parse
  HEAD` AND by `ls -ld` of the path** — the last unification's two pins
  were created at a path with a trailing space because zsh does not
  word-split `set -- $pin`; build every pin path from a quoted literal,
  never an unsplit variable; then grep the fresh NDJSON for a marker only
  one tree has; check the file is non-empty before believing a diff).
  Which sha:
  - **P4.D198 and P4.D199 pin the TARGET `bcd7e4852`** for every family
    their port MOVES (P4.D198: the NEW `llm_image_budget_equivalence`,
    `file_attachment_tier3`; P4.D199: `orchestrator_tier3`, `build_
    context_tier3` if its seams are real, `help_tree_equivalence` and the
    `help_*` content families that read `connection-profiles.md`) and the
    BASELINE `5f0a57dc4` for their neutrality legs. The target-pinned
    help-tree oracle MUST carry `A travelling portrait packs light` and
    the baseline one MUST NOT — that pair is the pin verification.
  - **P4.96 and P4.97 pin the BASELINE `5f0a57dc4`** — their v4 surfaces
    are untouched by the drift (each lane records `git -C ~/source/
    quilltap-server diff --stat 5f0a57dc4..bcd7e4852 -- <its v4 paths>`
    as a version stamp or EMPTY in its lane record before the first
    regen). A pin at the baseline is still REQUIRED, not optional: the
    checkout can go dirty mid-lane (ledger §5.1), and a bare-checkout
    regen is unrepeatable.
  The sweep driver is the sanctioned per-family path (`harness/tools/
  recipe_sweep.py --run <family> --v4 "$PIN" --v5w <ABSOLUTE worktree>`);
  never run two sweeps concurrently; a family that reports `SKIP` in
  0.00 s did not run (`a-family-env-var-is-not-its-regen-var`). Ledger
  §5.2's silent-stale-pass discipline applies to every regen (`rm -f`
  the fixture first; read the builder's last line; `grep -c` the changed
  bytes in the fresh NDJSON — MUST be > 0). **Shared `/tmp` output paths
  are a live hazard** (P4.92's mid-lane clobber; the `/tmp/qt-imggen-*`
  pair collision): every lane stages its regen outputs under a
  LANE-PRIVATE directory (`/tmp/<order>/…`) by pointing the family's env
  vars there, and leaves the committed recipe headers canonical (a
  committed recipe NEVER names a `/tmp` pin — the driver's `--v4` is the
  pin).
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). The spec
  for every item is `git show <sha> -- <path>` plus `git show <sha>:<path>`
  at `bcd7e4852` (the drift lanes) or `5f0a57dc4` (the maintenance
  lanes); a commit message describes the bug as its author understood it
  and the hunks are often narrower. Re-verify even where an order flags
  the trap — the order's paragraph is the same kind of prose, and so is
  a previous lane's record or a doc comment in the tree (P4.D196's
  `Option<String>` paraphrase; P4.95's "no case reaches the retry" — both
  corrected by this round's surveys). Carry a v5-side code comment naming
  the sha where the prose misleads.
- **§R.5 — Designed trips and pre-existing reds: measure, don't assume**
  (ledger §5.4). No convergence pin trips this round (v5 filed nothing
  bug 151 answers). The drift lanes' pre-fix reds are DESIGNED: P4.D199's
  budget arms red on unported `main` (v5 sends every unseen image) and
  P4.D198's shrink rows red (v5 sends stored bytes) — record the pre-fix
  counts. A red that appears in a family a lane did NOT touch is a NEW
  finding to classify at both pins before any source is touched, and it
  is probably not that lane's.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest`
  is shared) and its silence leg; a fixture change lands with the regen
  of every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that
  SURVIVES as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`). A scripted
  encoder on BOTH sides (P4.D198) is a legitimate tier-1 instrument when
  the real encoders cannot be byte-compared (D19); it must be the SAME
  script, and the family must say so in its header.
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed (and `apps/web/package.json` when it touches the SPA) on each
  commit per `.claude/commands/commit.md`; the unifier RECOUNTS as base +
  the sum of every lane's bumps (the silent-auto-merge trap: identical
  bumps merge as ONE — it fires every round). Base at planning: core
  0.0.942, harness 0.0.834, web 0.0.150, host 0.0.137, cli 0.0.22, tauri
  0.0.7, SPA 0.5.730. Expected: **core** by all four lanes; **harness** by
  all four; **host** by P4.D198 ONLY (the codec implementation is host
  SOURCE; P4.D199's `help/` re-vendor changes the embedded table but no
  host source — the P4.D194 rule: no host bump for help alone); **web**
  by P4.96 ONLY (the census test and the dispatch wire test live in the
  web crate's `tests/`); **SPA** by P4.96 ONLY (the one contract type);
  **cli/tauri** by NOBODY.
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  P4.D199 pre-lists the three paths `bcd7e4852` moves (`docs/v4/CHANGELOG.
  md`, `docs/v4/developer/bugs.md`, the NEW `docs/v4/developer/bugs/fixed/
  bug-151-unbudgeted-image-bytes-on-the-wire.md`) with byte counts at the
  pin in its lane record; no lane copies them.
- **§R.10 — Cross-lane handoffs, named:** (a) **The budget constant.**
  P4.D198 DEFINES `pub const LANTERN_IMAGE_BASE64_BUDGET: usize = 2 *
  1024 * 1024` in the NEW `crates/quilltap-core/src/files/llm_image_
  budget.rs` (v4's home); P4.D199 CONSUMES it but its worktree cannot see
  the module, so it defines a PRIVATE twin in `services/message_context.
  rs` beside `ASSISTANT_IMAGE_LOOKBACK` with a `// P4.D199 → P4.D198
  handoff … §R.10(a)` marker line; the unifier replaces the private twin
  with `use crate::files::llm_image_budget::LANTERN_IMAGE_BASE64_BUDGET`
  after both picks (one-line wire; the marker is the grep). (b) **The
  loaders' output is the walk's input.** P4.D199 budgets whatever
  `LanternLoad.attachments[i]["data"]` (the base64 STRING) the seam
  returns; P4.D198 makes that string the POST-shrink base64. In each
  lane's worktree the other half is absent and both differentials pass
  with junk bytes / `NotConfiguredTranscoder` (the shrink `Err`s →
  unchanged; v4's sharp throws → unchanged); the unifier re-runs
  `orchestrator_tier3` and `file_attachment_tier3` over the UNION at the
  new baseline to prove the composition and records the counts. (c)
  **`api/types.rs` / `api/engine.rs` / the dispatch wrong-type census are
  P4.96's alone this round** — the unifier asserts `git diff main...
  <lane> -- crates/quilltap-core/src/api/types.rs crates/quilltap-core/
  src/api/engine.rs crates/quilltap-web/tests/dispatch_wrong_type_census.
  rs` is EMPTY for P4.D198, P4.D199 and P4.97 before picking. (d) **The
  `/tmp/qt-imggen-*` pair collision** (`image_generate_route` ↔ `image_
  generation_tier3`): P4.96 stages under `/tmp/p4.96/`; at the unified
  gate the tier-3 family is regenerated LAST and P4.96's family is run by
  name against its own snapshot with its oracle var withheld from the
  workspace block (the `53294163f` round's recorded protocol). (e) **The
  recorded provider corpora are P4.97's alone this round** (the
  request-envelopes re-record + the google recorder if touched); P4.D198's
  families are SDK-free. (f) **The primary-stream family's side-channel
  widening** (P4.97 item 1) and **the orchestrator family's side-channel
  widening** (P4.D199 item 4(b), ordered attachment filenames) are two
  DIFFERENT families' mocks — no shared file; if either lane finds the
  widening belongs in a shared helper under `harness/oracle/lib/`, it
  creates a NEW file rather than editing an existing shared one.
- **§R.11 — No SPA behaviour moves this round.** P4.96's one contract
  type is additive and optional; no beat is added or flipped; the unified
  gate runs the full Playwright suite once against the release binary as
  regression only (the documented intermittents and the standing parks
  stand as named in the previous round's gate).

## §S — Shared contract (binding; byte-identical in P4.D198 and P4.D199)

- **§S.1 — The seam between the loader half and the walk half is the
  EXISTING `LanternLoad { prefix: String, attachments: Vec<Value> }`
  returned by `MessageContextSeams::load_lantern_images(&file_ids,
  provider)`** (`services/message_context.rs:614-617`, `:650-685`), and
  NOTHING about it moves: the attachment `Value`'s keys stay `id, filepath,
  filename, mimeType, size, data, url` (`chat_files.rs:250-258`); `data`
  is the base64 STRING of the bytes the turn will send (POST-shrink once
  P4.D198 lands; the stored bytes today); `size` is the byte length of
  those same bytes on the mount path; the attachments arrive in
  CHRONOLOGICAL order with only the raw-kept ones present (the fallback
  pass has already run — a described image contributes to `prefix`, not
  to `attachments`). P4.D199 reads `data.len()` and reorders/drops
  attachments; it never re-reads bytes, never calls a codec, and never
  edits `prefix`. P4.D198 never budgets, never reorders, and never drops
  an attachment for size (the provider backstop resize is a RESIZE, not a
  drop — v4 has none either).
- **§S.2 — The one constant with two homes until unification:**
  `LANTERN_IMAGE_BASE64_BUDGET = 2 * 1024 * 1024` (bytes of BASE64, spent
  against `data.len()`), public in P4.D198's `files/llm_image_budget.rs`,
  private in P4.D199's `message_context.rs` until §R.10(a)'s wire.
- **§S.3 — Nothing else on the wire moves from the drift.** No new
  dispatch verb, no new REST route, no `core-contract.ts` change from
  P4.D198 or P4.D199; a drift lane that finds it needs one STOPs on that
  item and records the shape for the unifier. (P4.96's five optional
  fields on `Request::ImageProfileGenerate` — camelCase `size`, `quality`,
  `style`, `aspectRatio`, `negativePrompt`, each `Option<serde_json::
  Value>` RAW-crossing, refused in the handler with v4's `Validation
  error` envelope — are that lane's own contract, mirrored into `core-
  contract.ts` by the same lane; no other lane sends them.)

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D198** — bug 151, the LOADER half | `crates/quilltap-core/src/files/image_processing.rs` (the `ImageTranscoder` trait's new `shrink_to_webp` default method + doc; nothing else in the file unless a helper is needed — say which); NEW `crates/quilltap-core/src/files/llm_image_budget.rs` + the `pub mod` line in `files/mod.rs`; `crates/quilltap-core/src/services/chat_files.rs` — `read_file_as_base64` and `load_mount_file_as_attachment` ONLY (the shrink-then-backstop sequence, the log filenames) — NOT `load_lantern_images` / `load_user_attachments` / `RealMessageContextSeams`; `crates/quilltap-host/src/image_codec.rs` (the `shrink_to_webp` impl + tests) and `crates/quilltap-host/Cargo.toml` ONLY if a feature flag is needed (say so); `crates/quilltap-harness/src/lib.rs` — the NEW `ScriptedTranscoder` (additive); NEW `harness/oracle/cases/llm-image-budget.test.ts` + a NEW script helper under `harness/oracle/lib/`; NEW `crates/quilltap-harness/tests/llm_image_budget_equivalence.rs`; `crates/quilltap-harness/tests/file_attachment_tier3_equivalence.rs`; `harness/oracle/cases/file-attachment-tier3.test.ts`; `harness/oracle/fixtures/file-attachment.json` + `build-file-attachment-fixture.ts`; NEW `crates/quilltap-harness/tests/lantern_loader_transcoder_wiring.rs` (the composition pin — or a test module in an existing host test file you own; say which); `crates/quilltap-core/src/files/mod.rs`; this order's header | `services/message_context.rs`, `services/orchestrator.rs`, `api/**`, `crates/quilltap-web/**`, `help/**`, `model/primary_stream.rs` (P4.97's — note the file is under `services/`), every recorded provider corpus under `harness/oracle/fixtures/{request-envelopes,google-*,image-dialects,…}/`, every other harness family, `apps/web/**`, the drift ledger |
| **P4.D199** — bug 151, the WALK half + help + ratifications | `crates/quilltap-core/src/services/message_context.rs` — section K (`:1337-1361`) + the private budget constant + the new warn + its `#[cfg(test)]` pins; `help/connection-profiles.md` (byte copy at the target pin); `crates/quilltap-harness/tests/help_tree_embed_guard.rs` — the history doc comment ONLY (124 stays 124); `crates/quilltap-harness/tests/orchestrator_tier3_equivalence.rs`; `harness/oracle/cases/orchestrator-tier3.test.ts`; `harness/oracle/fixtures/orchestrator-tier3.json` + `build-orchestrator-fixture.ts`; `crates/quilltap-harness/tests/build_context_tier3_equivalence.rs` + `harness/oracle/cases/build-context-tier3.test.ts` + `harness/oracle/fixtures/build-context-tier3.json` ONLY if item 4(c)'s measurement shows real seams (say so); this order's header | `services/chat_files.rs` (incl. `load_lantern_images` — READ ONLY), `files/**`, `services/orchestrator.rs`, `api/**`, `crates/quilltap-web/**`, `crates/quilltap-host/**` (no host bump — §R.8), every other `help/*.md`, `model/`/`services/primary_stream.rs`, every recorded provider corpus, every other harness family, `apps/web/**`, the drift ledger |
| **P4.96** — the generate route's five fields | `crates/quilltap-core/src/api/image_profiles.rs` (`image_profile_generate` + its doc); `crates/quilltap-core/src/api/types.rs` — the `ImageProfileGenerate` variant + its doc ONLY; `crates/quilltap-core/src/api/engine.rs` — the `ImageProfileGenerate` arm (`:2928-2951`) ONLY; NEW `crates/quilltap-core/src/image_gen/style.rs` (or a `const` in `image_gen/quality.rs`) + the repoints at `tools/generate_image.rs:213` and `api/images.rs:1472` ONLY if Tier-2 item 9 lands (say so); `crates/quilltap-web/tests/dispatch_wrong_type_census.rs`; NEW `crates/quilltap-web/tests/image_profile_generate_dispatch_wire.rs` (the dispatch-path pin); `crates/quilltap-harness/tests/image_generate_route_equivalence.rs`; `harness/oracle/cases/image-generate-route.test.ts`; `harness/oracle/fixtures/image-generation.json` + `build-image-generation-fixture.ts` ONLY if a row needs a profile the fixture lacks (say so — `image_generation_tier3` shares them); `apps/web/src/app/core/core-contract.ts` — the `imageProfileGenerate` request type ONLY; `apps/web/package.json` version; this order's header | `crates/quilltap-core/src/model/**` (incl. `image_dialects.rs`, `openai_image_options.rs`), `tools/generate_image.rs` beyond the one repoint, `crates/quilltap-web/src/**` (NO new REST edge), `services/**`, `files/**`, `help/**`, every other harness family (incl. `image_generation_tier3` and its oracle/fixture outputs), every other SPA file, the drift ledger |
| **P4.97** — the retry option bag + the envelope cache-key pin | `crates/quilltap-core/src/services/primary_stream.rs` — the retry block (`:1251-1309`) + its `#[cfg(test)]` module; `crates/quilltap-harness/tests/primary_stream_tier3_equivalence.rs`; `harness/oracle/cases/primary-stream-tier3.test.ts` (+ `openai-chaining-fallback-tier3.test.ts` ONLY if the mock widening is shared — say so); `harness/oracle/fixtures/primary-stream-tier3.json` + `build-primary-stream-fixture.ts`; `harness/oracle/providers/record-request-envelopes.mjs` + `regenerate-request-envelopes.sh` (+ `record-google-request.mjs` / `regenerate-google-wire.sh` ONLY if item 6's google arm records — say so); `harness/oracle/fixtures/request-envelopes/request-envelopes.recorded.ndjson` (+ `google-request.recorded.ndjson` under the same condition); `crates/quilltap-harness/tests/request_builder_equivalence.rs`; this order's header | `model/stream.rs` (the canned key), `model/request_builder/**` (READ ONLY — a builder red is a finding), `services/orchestrator.rs`, `services/provider_failover.rs`, `services/native_tool_loop.rs`, `services/text_tool_loop.rs`, `model/streaming_provider.rs`, every other recorded corpus (`image-dialects/`, `response-bodies/`, `stream-fixtures/`, `tool-wire/`, `moderation-wire/`, `web-search-wire/`), `api/**`, `crates/quilltap-web/**`, `services/chat_files.rs` / `message_context.rs` / `files/**`, `help/**`, every other harness family, `apps/web/**`, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0),
   and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp` / by-label diff) where the family is
   deterministic across regens; where it is not (`regenerate-at-both-
   pins-and-cmp-is-not-universal`), compare the normalized rows by case
   and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast` with the lane's env block
   (its own oracle vars + `QT_V4_ROOT`/`QT_V4_CHECKOUT` at the right pin +
   `QT_NODE` = the node BINARY): report **binaries / passed / failed /
   ignored** from the per-binary lines and the exit code, and the lane's
   families confirmed RUN by non-zero duration. The `/tmp/qt-imggen-*`
   collision: a lane that is not P4.96 withholds `QT_ORACLE_IMGGEN_ROUTE`
   from its block (the family SKIP-passes and is P4.96's to prove by
   name); P4.96 withholds `QT_ORACLE_IMGGEN` (the tier-3 family's) and
   proves its own by name on its private staging.
9. **SPA (P4.96 only):** `npm run lint`, `npm test` (spec files / tests /
   0 failed), `npm run build` clean — no e2e beat is added this round;
   the full Playwright run is the unified gate's.
10. **Ownership check:** `git diff --stat main...HEAD` — every path is in
    the lane's Owns column; every MUST-NOT-TOUCH path absent; for the
    three non-P4.96 lanes, `git diff main...HEAD -- crates/quilltap-core/
    src/api/types.rs crates/quilltap-core/src/api/engine.rs crates/
    quilltap-web/tests/dispatch_wrong_type_census.rs` is EMPTY (§R.10(c)).
11. **Per commit:** `.claude/commands/commit.md` in full — the equivalence
    test is non-negotiable (§1), the invariants (§2), real-instance safety
    (§3), the harness regen (§4), build/test/lint (§5), the CHANGELOG
    entry + version bumps (§7). Commit on the lane branch only; never
    merge to `main`; never `git stash`; never a worktree inside a worktree.
12. **Lane close:** update this order's status header (what landed, what
    was refuted by measurement, what stays OPEN with its named cause),
    append the lane record to `status-log.md` (the regen commands as run,
    the counts, the mutation table, every finding outside the lane's
    ownership for the unifier), and stop. The final gate runs in the
    FOREGROUND of the lane's last turn — a backgrounded gate whose
    notification arrives after the lane reports done is a lane that is
    NOT done (`a-backgrounded-sleep-does-not-gate-your-turn`).
