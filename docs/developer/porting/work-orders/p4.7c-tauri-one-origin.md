# Work order P4.7c — the Tauri one-origin lane (lane C): dogfood finding #12 — images under the Tauri shell

> **STATUS: CLOSED — UNIFIED on main (2026-07-16); the staged human walk
> COMPLETED 2026-07-18.** Final versions after the union: quilltap-tauri
> 0.0.3, SPA **0.5.128** (union with lane B). **The combined M5 +
> finding-#12 walk ran green:** Part A (M5 beats on `~/qt-m5-instance`,
> re-staged at main `2952871a`) passed after surfacing two fixed-in-place
> findings — #14 (Cmd+R dead: no shell menu carried the accelerator;
> fixed `8528072d`, View → Reload) and #15 (the gate screens rendered
> unthemed: `ThemeService` was only constructed post-unlock; fixed
> `b637e2c9`, the App root stamps the theme) — and Part B (the Friday-copy
> image quartet: avatar stacks / chat-card images / in-message + courier
> images / story background) ran clean, closing dogfood finding #12.
> The original close header follows.
>
> **LANE CLOSED (2026-07-16) — the spike ran GREEN on every
> gating check and one-origin is ADOPTED.** The window ships on
> `qtap://localhost/`; the qtap handler serves the embedded
> `frontendDist` for non-API GET/HEAD paths and delegates `/api/*` +
> `/health` into the reused router (no quilltap-web edits — the §2/
> Ownership handoff clause was never needed). The fallback path (b) was
> NOT taken; `avatar-stack.ts` and `markdown-renderer.ts` are untouched.
> `apiUrl()` reconciled to identity on a qtap-origin page (signature
> frozen). Spike record + gating-check results: the status log (P4.7c
> unit 1). Gate: workspace 324 suites / 1354 tests, clippy clean both
> feature sets, `ng test` 126 files / 1152 tests, `ng build` clean
> (main bundle greps zero `__TAURI_INTERNALS__`), full Playwright
> frozen-path run green alone on 4319 with zero locator/count changes,
> debug bundle rebuilt over the real dist and boot-smoked stable
> against `~/qt-m5-instance`. Versions: quilltap-tauri 0.0.3, SPA
> 0.5.127.
>
> **The staged human walk (tier-1 item 5 — the one remaining
> acceptance step, combined with the pending M5 walk):**
>
> 1. **M5 beats** (staged instance): launch
>    `target/debug/bundle/macos/Quilltap.app` with
>    `--args --data-dir ~/qt-m5-instance` (or run
>    `Contents/MacOS/quilltap-tauri --data-dir ~/qt-m5-instance`
>    directly; passphrase `open sesame please`). Unlock → salon list →
>    open chat → send → streamed reply; open the terminal pane (the §4
>    pairing live). The page now lives at `qtap://localhost/` — deep
>    links survive a reload (Cmd+R on any route must land back in the
>    SPA, not 404). *(Walk note 2026-07-18: this recipe asserted a
>    gesture no shell code carried — the shell had no menu, so macOS
>    dropped Cmd+R entirely. Dogfood finding #14; fixed in place —
>    `menu::build_app_menu` wires View → Reload / CmdOrCtrl+R.)*
> 2. **Finding-#12 beats** (the Friday copy — `dogfood-friday-setup`
>    memory: rsync copy at `~/qt-dogfood-friday/Friday`, never the live
>    iCloud data, never reverse the rsync): relaunch with
>    `--data-dir ~/qt-dogfood-friday/Friday`, unlock with the real
>    passphrase (human-held; never in-sandbox). Verify salon-list
>    avatar stacks render, chat-card images render, and in-message/
>    courier images render (finding #12's broken-chip trio). A story
>    background on a chat with one set is the fourth check.
> 3. **Devtools**: right-click → Inspect on the qtap page — the one
>    gating check whose visual half the sandbox could not confirm
>    (`open_devtools()` ran without panic in the spike).
>
> Deferred loud (the standing P4.7 pool, untouched): native niceties,
> turnkey `tauri dev`, updater/signing/release (D21), uniffi/mobile,
> `Last-Event-ID` replay. Windows/Linux one-origin behavior
> (localStorage persistence, custom-scheme quirks) is macOS-verified
> only — re-run the spike checks when those targets first build.
>
> Lane C of the THREE-lane P4.6au ∥ P4.6av ∥ P4.7c round (the homepage +
> Tauri one-origin round, planned 2026-07-16). **v4 reference baseline: HEAD
> `02865bdb`** (`~/source/quilltap-server`) — run
> `git log 02865bdb..HEAD --oneline` at lane start; **if HEAD moved, STOP and
> report.** Sibling lanes: `p4.6au-homepage-server.md`,
> `p4.6av-homepage-spa.md` (bound to this lane only by §2 of the Shared
> contract — no dispatch surface overlap). The **Shared contract** and
> **Ownership** sections below are binding and reproduced verbatim in all
> three orders. You run in a worktree (own Rust `target/`, per the
> parallel-round disk arrangement); the unification pass integrates the
> lanes onto main.

## Mandate

Close **dogfood finding #12** (`docs/developer/porting/dogfood-findings.md`
row 12): under the Tauri shell against real data, every image renders as a
broken chip because **server-supplied RELATIVE URLs** (`avatarUrl` /
`filepath` / `backgroundUrl` DTO fields; `/api/v1/files/…` links inside
server-rendered bodies; the inline blob rewrite in
`markdown-renderer.ts:98`) resolve against the webview's
`tauri://localhost` dist origin and 404. The wire format is v4-faithful and
byte-pinned by differentials — **absolutizing server-side is forbidden.**
Fix it at the client origin: **spike the one-origin approach FIRST** (serve
the SPA itself off `qtap://localhost/` so every relative URL — including
inside pre-rendered HTML — resolves through the qtap protocol into the
reused quilltap-web router), and fall back to render-seam `apiUrl()`
normalization only if the spike goes RED. Browser deployment must remain
byte-for-byte unaffected either way. This lane owns `crates/quilltap-tauri`
and the named SPA transport files only.

## Survey-verified starting points (2026-07-16 @ main `aeaf928` — do not re-derive)

### Where things stand after P4.7a/b

- **The `qtap` protocol already serves the whole HTTP surface:**
  `crates/quilltap-tauri/src/lib.rs:114` registers the asynchronous scheme;
  `src/protocol.rs::handle_qtap_request` (:25) delegates the full
  `http::Request` into the reused quilltap-web `Router`. That router
  already carries **static SPA serving with the index fallback**
  (`crates/quilltap-web/src/lib.rs:190–191`, `static_serve::spa_fallback`)
  — i.e. `qtap://localhost/` can already return the SPA if the window is
  pointed at it and static serving is configured in the embedded router
  build (verify which router-construction path the Tauri shell uses and
  whether static serving is enabled there).
- **The window currently loads the bundled dist**: `tauri.conf.json` —
  `frontendDist: "../../apps/web/dist/quilltap/browser"`, `windows: [...]`
  (:7, :10); page origin `tauri://localhost` (macOS). The protocol.rs
  header comment (:10) documents the current CROSS-origin fetch
  arrangement — the one-origin fix inverts exactly that.
- **`apiUrl()`** (`apps/web/src/app/core/api-url.ts:21`) is the P4.7b
  origin resolver adopted at every client-BUILT raw URL site. Under
  one-origin it should degrade to identity for relative paths — do not
  change its signature (§2).
- **`isTauri()`** checks the `isTauri` GLOBAL (P4.7b memory) — Tauri
  injects it for any window it creates; confirm it still holds under a
  custom-scheme page URL.
- **The known leak sites** (the fallback-path chokepoints, from the
  finding): `normalizeAvatarSrc`-style consumers via
  `apps/web/src/app/ui/avatar-stack.ts`; enriched-image `filepath`
  consumers (`chat_enrichment.rs` / `chat_media.rs` DTO fields rendered by
  the Salon); `apps/web/src/app/chat/render/markdown-renderer.ts:98` (the
  inline store-image blob rewrite bypassing `apiUrl`); courier-rendered
  hrefs inside server-rendered bodies (the hard case — pre-rendered HTML
  can't be fixed at a client seam, which is WHY one-origin is preferred).
- **The tier-4 proof surface:** the 6-test `ipc_contract` suite
  (quilltap-tauri) and the SPA transport specs
  (`tauri-core-transport.spec.ts`, `tauri-terminal-stream-transport.spec.ts`)
  pin every §1–§4 name from the P4.7 round — they must stay green
  untouched-in-meaning.
- **Visual verification limits:** window content is NOT verifiable headless
  on this machine (screencapture + System Events permission-blocked,
  no tauri-driver on macOS — the P4.7 round record). The staged M5
  instance (`~/qt-m5-instance`) has NO avatar images, which is why #12 was
  invisible there; the Friday copy (`~/qt-dogfood-friday/Friday`) surfaces
  it instantly but needs the real pepper — **human-walk territory, never
  in-sandbox.**

## Tiered deliverables

### Tier 1 — must land

1. **The spike, written up either way:** point the Tauri window at
   `qtap://localhost/` (window URL in `tauri.conf.json` / the builder) and
   serve the SPA through the reused router's static serving. The spike's
   gating checks, each recorded GREEN/RED in the lane notes:
   - the SPA boots and Angular ROUTER navigation works under a
     custom-scheme page URL in WKWebView (pushState/history — the known
     custom-scheme risk);
   - `localStorage` (the unlock flow's persistence) is available and
     survives relaunch under the qtap origin;
   - `isTauri()` still selects the IPC transport; `dispatch`/`health`
     invoke fine; the `quilltap://event` pump attaches;
   - relative `/api/v1/…` fetches and `<img src="/api/v1/files/…">` reach
     `handle_qtap_request` (byte routes return bytes);
   - devtools still attach to the debug build.
2. **If the spike is GREEN — adopt it:** the window ships on the qtap
   origin; add `ipc_contract` coverage for `GET /` (index), one hashed
   static asset, and one `/api/v1/files/{id}` byte route through
   `handle_qtap_request`; reconcile `apiUrl()` runtime behavior (identity
   for relative paths when page origin already routes to the core) WITHOUT
   changing its signature; the SPA transport specs stay green.
3. **If the spike is RED — the fallback (b), scoped:** route
   server-supplied paths through `apiUrl()` at the enumerated chokepoints
   (avatar-stack normalization, the enriched-image `filepath` consumers,
   `markdown-renderer.ts:98`, courier-rendered hrefs), each with a unit
   spec; record WHICH check killed the spike, and bank the pre-rendered
   HTML residue (the case (b) cannot fix) as a named finding.
4. **Regression proof for the browser deployment:** the full Playwright
   suite green with ZERO locator/count changes (the frozen-path proof, as
   in P4.7b), and `ng build`'s main bundle still greps zero
   `__TAURI_INTERNALS__`.
5. **The staged human walk recipe**, appended to the status header at
   close: rebuild the debug bundle over a real dist, launch against
   `~/qt-dogfood-friday/Friday` (`dogfood-friday-setup` memory — rsync
   copy, never reverse), and verify salon-list avatar stacks + chat-card
   images + in-message images render. **The human executes it** (sandbox
   cannot see the window or hold the pepper); combine with the pending M5
   walk.

### Tier 2 — should land

6. A `tauri.conf.json`-level note (or build assertion) preventing the dist
   from shipping BOTH as `frontendDist` and via qtap static serving in a
   conflicting way — one canonical serving path, documented in
   `lib.rs`'s module header (which currently documents the cross-origin
   arrangement and must be rewritten to match reality).

### Tier 3 — explicit deferrals (loud, named)

- `Last-Event-ID`-style replay, native niceties, turnkey `tauri dev`,
  updater/signing/release (D21), uniffi/mobile — the standing P4.7 pool,
  untouched by this lane.
- Any server-side URL shape change — FORBIDDEN, not deferred.

## The differential requirement, restated

No v4 oracle applies (v4 has no Tauri shell — tier 4): the proof is the
extended `ipc_contract` suite (Rust, against the real router over the real
encrypted fixtures), the pinned SPA transport specs, the frozen-path
Playwright run for the browser deployment, and the staged human walk for
the visual claim. Where this lane transcribes v4-faithful URL semantics
(the fallback chokepoints), each seam gets a unit spec pinning the
before/after resolution.

## Fixtures

- **Consumes:** the committed quilltap-tauri contract fixtures (real
  encrypted, per P4.7a) and the e2e locked instance for the Playwright
  frozen-path run. If the contract suite needs an image-bearing fixture to
  prove the byte route, EXTEND this lane's own fixture staging — do not
  touch the shared families.
- **Delivers:** none. No oracle regeneration owed.

## Shared contract (BINDING — identical in all three orders)

### §1 — the `systemHome` surface (lane A provides; lane B consumes)

- **Dispatch verb:** wire name `systemHome`, Rust `Request::SystemHome`
  (no parameters — single-user; the engine supplies the user scope as the
  other system verbs do).
- **REST edge:** `GET /api/v1/system/home` → v4 `successResponse` envelope
  around the payload; on internal failure v4's
  `serverError('Failed to load the home dashboard')` body.
- **Payload** (v4 `HomeData`; field names verbatim, omit-vs-null exactly as
  written):

```ts
{
  displayName: string,
  lastChatId: string | null,
  recentChats: Array<{
    id: string, title: string, updatedAt: string,
    lastMessageAt: string | null,
    isDangerousChat?: boolean,
    storyBackgroundUrl?: string | null,
    participants: Array<{
      id: string, type: 'CHARACTER', isActive: boolean, displayOrder: number,
      character?: {
        id: string, name: string,
        defaultImageId?: string,
        defaultImage?: { id: string, filepath: string, url?: string } | null,
        tags?: string[]
      } | null
    }>,
    _count: { messages: number }
  }>,                                   // max 12
  projects: Array<{
    id: string, name: string,
    description?: string | null, color?: string | null, icon?: string | null,
    chatCount: number, lastActivity: string
  }>,                                   // max 12, activity-sorted
  characters: Array<{
    id: string, name: string, title?: string | null,
    defaultImageId: string | null,
    defaultImage: { id: string, filepath: string, url?: string | null } | null,
    tags?: string[],
    isFavorite: boolean, npc: boolean, chatCount: number,
    defaultConnectionProfileId?: string | null
  }>                                    // max 24, favorites→chatCount→name
}
```

- `defaultImage.filepath` / `storyBackgroundUrl` are **server-relative
  paths, v4-faithful** — do NOT absolutize server-side (dogfood finding
  #12; lane C fixes resolution at the client origin).

### §2 — the URL-resolution freeze (lane B ↔ lane C)

- Lane B routes every server-supplied image/file path through the EXISTING
  shared helpers (`apps/web/src/app/ui/avatar-stack.ts` normalization,
  `apps/web/src/app/core/api-url.ts` `apiUrl()`) — no inline URL building
  in the new home components.
- Lane C may change the RUNTIME resolution of those helpers under the Tauri
  origin but must NOT change any exported helper signature
  (`apiUrl(path: string): string` stays) and must not edit lane B's files.

## Ownership (BINDING — identical in all three orders)

- **P4.6au:** `crates/quilltap-core/**`, `crates/quilltap-web/**`,
  `crates/quilltap-harness/**`, `harness/oracle/**`, the new fixture family
  + generator. Version bumps: quilltap-core, quilltap-harness, quilltap-web
  (+ Cargo.lock).
- **P4.6av:** `apps/web/src/app/screens/home/**` (new),
  `apps/web/src/app/app.routes.ts`, `apps/web/src/app/shell/shell.ts`, its
  own new e2e spec file. Version bump: the SPA `package.json`.
- **P4.7c (this lane):** `crates/quilltap-tauri/**` (incl.
  `tauri.conf.json`), `apps/web/src/app/core/tauri-api.ts`,
  `apps/web/src/app/core/core-transport-factory.ts`; ONLY IF the fallback
  path (b) is taken: `apps/web/src/app/core/api-url.ts`,
  `apps/web/src/app/ui/avatar-stack.ts`,
  `apps/web/src/app/chat/render/markdown-renderer.ts`. Version bumps:
  quilltap-tauri (+ the SPA IF apps/web files change — SPA bumps union at
  unify, resolve HEAD+1 per the P4.6ar round memory).

  **Note the constraint this ownership implies:** if the one-origin
  adoption turns out to require edits to `crates/quilltap-web` (e.g. the
  embedded-router static-serving configuration), STOP and report before
  editing — quilltap-web belongs to lane A this round; the unifier can
  take a handoff patch, but two lanes must not both edit the crate.
- **Append-only for everyone:** `docs/CHANGELOG.md`,
  `docs/developer/porting/status-log.md` (union-merge at unification).
- **Nobody** edits `CLAUDE.md`, `docs/developer/porting/phase-4.md`, or a
  sibling lane's files; the unifier updates the phase docs.

## Verification gate (run before declaring done)

- `cargo fmt` clean; `cargo clippy -D warnings` on BOTH feature sets;
  `cargo test --workspace` green with the extended `ipc_contract` run by
  name `--nocapture`.
- `ng test` green; `ng build` clean (zero `__TAURI_INTERNALS__` in the main
  bundle); the full Playwright suite green ALONE on 4319 with zero
  locator/count changes.
- The debug bundle rebuilt over a REAL `ng build` dist (the P4.7 unify
  lesson — never the build.rs-materialized empty dist) and boot-smoked
  headless against the staged locked instance.
- Every commit follows `.claude/commands/commit.md` (quilltap-tauri/SPA
  version bumps as touched; CHANGELOG entry).
