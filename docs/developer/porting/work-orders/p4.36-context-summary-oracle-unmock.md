# Work order P4.36 — un-mock the fold-episode pass in the context-summary oracle (the escalated stale red)

> **STATUS: OPEN — not started.** Written 2026-08-05 at round planning
> (the `7189a968` round).
>
> **v4 baseline for this round: `7189a968`** (v4 HEAD, 2026-08-05, clean
> tree — "feat(export): drop embeddings from exports, reach all fifteen
> types, compact backup"). The previous baseline was `7fe9fe40`; the
> drift is SEVEN commits, classified at planning (see the "Round
> planned" record in `status-log.md`): `7189a968` → P4.D46/P4.D47,
> `be2c9cbb` → P4.D48, the other four are infra/docs (dispositioned in
> P4.D48). **None of the seven touches the context-summary surface**
> (verified at planning: the drift's lib changes are export/import/
> backup/instance-settings/plugin-config only), so this lane's family
> regenerates from the unpinned checkout at `7189a968` without
> absorbing sibling drift by accident. **The baseline move is the
> UNIFIER's** — do NOT edit `CLAUDE.md` or `phase-4.md` on the branch.
>
> **Drift-check at lane start**: `git log 7189a968..HEAD --oneline` +
> `git status --short` in `~/source/quilltap-server`. On drift/dirty,
> regenerate oracles from a detached worktree PINNED at `7189a968`
> (recipe `oracle-regen-pinned-v4-worktree`), and report the drift in
> the lane record.
>
> **Siblings (same round):** P4.D46 (the `7189a968` export/import/backup
> server drift), P4.D47 (its SPA half), P4.D48 (the Anthropic-SDK wire
> check + infra dispositions). See "Shared contract + Ownership" —
> binding and identical across all four orders.
>
> **Worktree note:** own branch, own checkout; no `git stash`/
> `git worktree`. ⚠ This family is one of the 28 carrying
> `unstaged_jest_roots` (P4.34's list): run its recipe with
> `--v5w ~/source/quilltap-v5` (jest ignores `.claude/` venues), or use
> `recipe_sweep.py --run` which guards this for you.

## Mandate

Retire the escalated `context_summary_service_tier3` stale red by
un-mocking the fold-episode pass in the oracle case, per the P4.32
precedent (the doc-edit reindex un-mock). This is a **harness-only**
lane: the adjudicated diagnosis says v5 is faithful, so **zero
production source changes are expected**. If un-mocking exposes a
genuine v5 divergence instead, STOP and escalate with the evidence —
do not "fix" v5 to match a freshly changed oracle without a diagnosis.
Do NOT touch: any `crates/quilltap-core/src/**` production source, any
sibling-owned family (the import/export/backup families are P4.D46's;
the provider corpora are P4.D48's), `api/types.rs`, `apps/web/**`.

## The diagnosis this lane starts from (adjudicated — do not re-litigate without new evidence)

Settled at the `7fe9fe40` round's unification (round record → "The §3
review", item 1, and W3), superseding P4.D45's incorrect lane-record
claim:

- The red is **17-vs-11 rows in `llm_logs`**: the six extras are five
  `SUMMARIZATION` rows carrying the fold-episode extraction prompt plus
  one `TITLE_GENERATION`.
- **v4 folds live in production** — `generateContextSummary` calls
  `runFoldEpisodePass` at `lib/chat/context-summary.ts:519` (verify the
  line at lane start; it may have drifted a few lines).
- The oracle case **suppresses that call**: the case header itself says
  the `check`-op folds run as the internal-fold no-op — see
  `harness/oracle/cases/context-summary-service-tier3.test.ts` (header
  comment around lines 21–36: "the `check`-op folds must NOT post those
  rows"). Meanwhile v5's harness side runs the check ops with
  `NoopSeams`, mirroring the mock, so BOTH sides were suppressing a
  pass that production v5 (P4.6bj / the P4.13-round enclave fix / the
  courier follow-up) runs live with `FoldEpisodePassSeams`.
- So this is the **P4.20 class**: the harness lying about v4, not a
  divergence. Measured zero rows carrying the P4.13 ruled error-row
  signature (P4.34's measurement, confirmed by the unifier's row
  inspection).

## Survey-verified starting points (2026-08-05)

- Oracle case: `harness/oracle/cases/context-summary-service-tier3.test.ts`
  — the fold/title/help-title canned dispatch keys by `(op, promptKind)`
  and RECORDS the exact prompt (line ~13); `logLLMCall` runs REAL
  (W4.10b) so fold + title cheap calls land in a fresh llm-logs DB
  (line ~150). The un-mock means: let v4's REAL `runFoldEpisodePass`
  run inside `generateContextSummary`, with the fold-extraction LLM
  call answered by the same canned `(op, promptKind)` lookup (extend
  the `Kind` union / prompt-kind classifier at lines ~85 and ~185 for
  the fold-episode extraction prompt if its shape isn't already
  classified).
- Rust differential:
  `crates/quilltap-harness/tests/context_summary_service_tier3_equivalence.rs`
  (env var per its header; the recipe is in the file header — the
  standing `harness-recipes-are-runnable` rule). The v5 side's check
  ops must run `FoldEpisodePassSeams` (the production shape from
  `run_summary_check`) instead of `NoopSeams`, with the canned
  completion provider answering the fold-extraction call (key by the
  oracle-RECORDED prompt kind — the `tier3-completion-oracle` memory's
  rule).
- Fixtures: `harness/oracle/fixtures/context-summary-service-ops.json`
  + `context-summary-service-tier3.json` +
  `build-context-summary-service-fixture.ts`. **The committed-fixture
  rule applies**: point the oracle at the committed DBs; never rebuild
  in place (a rebuild mints fresh UUIDs).
- Precedents to follow: **P4.32** (`p4.32-doc-edit-oracle-unmock-reindex.md`
  + the `p4.32-doc-edit-oracle-unmock-lane` memory — enumerate every
  consumer of a mocked module before un-mocking; normalize before/after
  oracle diffs; order the specific arm before the dump equality) and
  **P4.20** (`p4.20-stale-oracle-mock-lane` memory — when a mock is
  retired, `grep -rl` the seam across ALL of `harness/oracle/cases/`
  and fix every case, not just the one you're in; P4.19 fixed
  `orchestrator-tier3`, P4.20 fixed `enclave-step-tier3`, this fixes
  `context-summary-service-tier3` — sweep for any fourth).

## Deliverables

**Tier 1 (must land):**

1. The oracle case runs v4's REAL fold-episode pass on the `check` ops
   (canned model answer both sides), records the fold-extraction
   prompt, and the NDJSON regenerates fresh at `7189a968` (grep the
   output for a new-baseline marker — the
   `oracle-regen-silent-stale-pass` rule).
2. The Rust side's check ops run `FoldEpisodePassSeams` with the
   matching canned answer; the differential goes GREEN with the
   previously-extra six rows now DIFFED (the fold `SUMMARIZATION` rows
   and the `TITLE_GENERATION` row are comparands, not noise).
3. **Mutation proof** (the D24 rule, and P4.20's inert-twin trap:
   line-target the mutation, count occurrences of any constant first):
   suppress the v5 fold pass → the family must go RED on the missing
   rows; restore → GREEN. Record the fingerprint in the lane record.
4. The P4.20 corollary sweep: `grep -r` the fold seam / stub name
   across `harness/oracle/cases/` — every remaining case that stubs a
   pass v5 now runs live is either fixed here or named in the lane
   record with a reason.

**Tier 2 (should land):**

5. Convert this family's recipe off `unstaged_jest_roots` (the P4.34
   staged-mirror shape) so it stops needing `--v5w`; verify with
   `recipe_sweep.py --run context_summary_service_tier3`.
6. Update the Rust test's header recipe + the case-file header comment
   so neither claims the fold is mocked any more.

**Tier 3 (explicit deferrals, loud):** none expected. If the un-mock
surfaces a genuine v5 divergence (see Mandate), the deferral IS the
escalation — record it with row-level evidence and stop.

## The differential requirement, restated

This lane IS differential work: the deliverable is the family running
v4's real code (jest oracle → NDJSON → Rust diff) with one less mock.
No new corpus is needed; the existing fixture family carries the cases.
Sensitivity is proven by the tier-1 mutation, not by first-run green.

## Verification gate before declaring done

- `cargo fmt --all --check`; clippy `-D warnings` on BOTH feature sets.
- The family by name with its env var, `--nocapture`, zero SKIP, over
  an oracle regenerated fresh at `7189a968`.
- `cargo test --workspace --no-fail-fast` (env-less) — no collateral.
- No SPA touched → no `ng test`/Playwright owed.
- Per-commit rule: `.claude/commands/commit.md`. Version bump: see
  Ownership.

## Shared contract + Ownership (binding, identical across P4.D46 / P4.D47 / P4.D48 / P4.36)

**The §1 wire contract for this round belongs to P4.D46 ↔ P4.D47** (the
new export-type identifiers, the compact-backup flag, the preview's
`redactedKeys` surfacing) — pinned verbatim in those two orders. P4.36
and P4.D48 do not touch it and must not edit `api/types.rs`.

**Ownership:**

- **P4.D46** owns `crates/quilltap-core/src/**` (incl. `api/types.rs` —
  the ONLY lane that may edit it), `crates/quilltap-web/src/**` EXCEPT
  `main.rs`, the import/export/backup/restore harness families (tests,
  oracle cases, fixtures), and `docs/v4/**` mirrors for `7189a968`'s
  docs. Bumps: quilltap-core, quilltap-harness, quilltap-web.
- **P4.D47** owns `apps/web/**`. Bump: the SPA version.
- **P4.D48** owns `harness/oracle/providers/**`, the provider corpora
  under `harness/oracle/fixtures/` (`request-envelopes/`,
  `response-bodies/`, `streams/`, `tool-wire/`, `moderation-wire/`,
  `web-search-wire/`), `crates/quilltap-web/src/main.rs`, `Dockerfile`,
  `docs/developer/running.md`. Bumps: quilltap-web (patch; collapses
  with P4.D46's at unification), quilltap-harness only if a corpus
  byte moves (escalation path — see its order).
- **P4.36** (this lane) owns
  `harness/oracle/cases/context-summary-service-tier3.test.ts`,
  `crates/quilltap-harness/tests/context_summary_service_tier3_equivalence.rs`,
  and the `context-summary-service-*` fixture JSONs if extended.
  Bump: quilltap-harness (patch; collapses at unification).
- `CHANGELOG` and `docs/developer/porting/status-log.md` are
  append-only for everyone (union-merge at unification).
