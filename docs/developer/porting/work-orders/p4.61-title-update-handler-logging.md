# Work order P4.61 — the title-update handler's missing log lines (P4.D110's bank) + the docs/v4 mirror refresh

**Status:** OPEN (ordered 2026-08-24).

## Preamble

- **v4 baseline for this round: `0ba942b1`** (v4 main, HEAD at planning
  2026-08-24, tree CLEAN, checkout on `main`). **v4 has NOT moved** — a
  no-drift round. `bugfix` measured at planning: HEAD `3a76b17d`, nothing
  unabsorbed beyond the tests-only `009c49b2`.
- **Drift-check at lane start.** `git log 0ba942b1..main --oneline` AND
  `git diff main bugfix -- lib/ app/ packages/`. If v4 moved, **STOP** and
  report. Note the checkout's branch before any regen; pin a lane-unique
  detached worktree (`/tmp/qt-v4-pin-p461-0ba942b1`, all three symlink
  classes) if the checkout dirties or moves. Regens through the sweep
  driver; never two sweeps concurrently.
- **Sibling lanes:** P4.59 (`p4.59-configured-search-provider.md`) and
  P4.60 (`p4.60-wrong-type-collapse-edges.md`) — fully disjoint files;
  **the lanes meet nowhere.** §Ownership below is the binding split
  (identical table in all three orders).
- **Worktree:** per the `carryout` skill; edit inside the worktree, not
  main. `docs/CHANGELOG.md` and `status-log.md` append-only.

## Mandate

Discharge P4.D110's banked finding: v5's `title_update_job.rs` carries **one
of v4's eight log lines** (the `:196` checkpoint-burned warn, ported with its
capturing-layer pin). Port the other seven byte-faithfully — message text AND
structured fields — so a silent no-cheap-LLM, a failed job, a title update,
and the story-background queue outcomes are all visible in `combined.log`,
which P4.49 made the surface an operator actually reads (the 2026-08-19
dogfood pass proved v4's JSON line shape there). Rider: refresh the stale
`docs/v4/` mirror (~8 differing files + unmirrored `bugs/fixed/` rows —
P4.D105's tier-2 note; `docs/v4/developer/API.md` alone is 253 diff lines
stale) by mechanical copy from the v4 checkout at the baseline.

**Must NOT touch:** anything P4.59 or P4.60 owns; the title-verdict parser
(`context_summary/title_verdict.rs` — P4.D110 closed it; this lane is the
HANDLER's logging only); no cursor/DB-state semantics — this is a log-only
lane, and its proof discipline follows from exactly that.

## Survey-verified starting points (fresh survey 2026-08-24, at `0ba942b1`)

### v4 — `lib/background-jobs/handlers/title-update.ts` (309 lines, 8 log sites)

| Line | Level | Message (port bytes + fields exactly) |
|---|---|---|
| `:89` | warn | `[Title Update] No cheap LLM available` + fields |
| `:152` | warn | `` `[Title Update] Failed for chat ${payload.chatId}: ${result.error}` `` (template string — one rendered sentence) |
| `:185` | error | `[Title Update] Failed to create system event:` + `{}` + the error arg |
| `:196` | warn | `[Title Update] Rename requested with no usable title — checkpoint burned` — **already ported** (P4.D110); do not re-touch, but its existing pin is the pattern to copy |
| `:213` | info | (multi-line call — read the site; the decline/kept-title outcome) |
| `:224` | info | `` `[Title Update] Updated title for chat ${payload.chatId} to: "${result.result.suggestedTitle}"` `` |
| `:294` | info | `[Title Update] Queued story background generation` + fields |
| `:303` | warn | `[Title Update] Failed to queue story background generation` + fields |

Read each site whole (the structured-fields bags matter: v5's tracing
fields must render the same keys). Template-literal interpolation of
non-string values follows JS semantics — the shared `to_js_string` helper
exists for exactly this (the P4.D91 §3 lesson); use it wherever a
`${…}` could carry a non-string.

### v5 — `crates/quilltap-core/src/services/title_update_job.rs` (523 lines)

- Carries only the checkpoint-burned warn today; `handle_title_update` at
  `:170`; the help-chat consider call at `:310`. Map each v4 site to its
  v5 branch — the branches all exist (the P4.D110 lane restructured the
  handler against v4's current shape), so this is line-insertion, not
  restructuring. If any v4 log site's branch does NOT exist in v5, stop
  and re-survey before inventing one — that would mean the handler
  diverges more than the bank recorded.
- The existing capturing-layer pin (P4.D110: a tracing `Layer` over the
  REAL entry point captures the warn because the burned checkpoint's DB
  state is byte-identical to a genuine decline) lives in the
  `title_update_tier3` family — find it and extend, don't parallel it.

### The proof discipline (the whole point of this lane's shape)

Memory `differential-blind-to-a-log-only-fix`: a state dump cannot see a
new `logger.warn`. Every new line is pinned with the capturing tracing
layer over the real `handle_title_update` entry, asserting BOTH presence on
its branch and **silence on the others** (the silence leg is what catches a
line attached to the wrong branch). The wiring class
(`a-backstop-hides-the-rot-it-neutralizes` / the #94
`runtime_facts()` lesson): at least one mutation proof reverts a line and
watches its arm red while everything else stays green.

### The docs/v4 mirror rider

`git diff` the mirror against the v4 checkout's `docs/` at `0ba942b1` to
enumerate the stale set (the recorded ~8 + `bugs/fixed/` rows); copy
mechanically — the mirror is reference-only, never planning material, and
no v5 prose is edited to match. List the refreshed files in the lane
record.

## Tiered deliverables

**Tier 1 (must land):**

1. The seven missing log lines, byte-faithful (sentences + field keys),
   each on its correct branch.
2. Capturing-layer pins: presence + silence legs per line, extending the
   existing `title_update_tier3` machinery; at least one revert-mutation
   proof.

**Tier 2 (should land):**

3. The docs/v4 mirror refresh (mechanical, enumerated in the lane
   record).

**Tier 3 (explicit deferrals — loud):**

- The wider handler-logging parity sweep (other job handlers' log-line
  counts vs v4) — enumerate-only if cheap, else just name it.
- 💸 a live look at the new lines in `combined.log` on a real title cycle
  joins the dogfood queue.

## The differential requirement (restated)

The oracle side already drives v4's REAL handler (`title_update_tier3`,
17 cases since P4.D110 — its recipe header is the regen path). If the
oracle records log output, extend the comparand; if it does not (likely —
the family diffs DB state + the captured warn), the capturing-layer unit
pins ARE the sanctioned proof for log-only changes, per the P4.D110
precedent — but regen the family fresh at the baseline anyway to prove the
lane moved no state (`no-behavior-change-lane-needs-v5-rows`: dump the
harness's own normalized rows pre and post if any doubt arises).

## Fixtures

None new; the family's existing fixtures serve. The mirror rider touches
`docs/v4/**` only.

## Shared contract + Ownership (binding; identical in all three orders)

**No shared code contract this round — the lanes meet nowhere.**

| Lane | Owns (exclusive) |
|---|---|
| **P4.59** | `crates/quilltap-host/src/spine.rs`, `crates/quilltap-host/src/providers.rs`, `crates/quilltap-core/src/tools/web_search.rs`, `crates/quilltap-core/src/api/settings.rs` (the `provider_list` region), `crates/quilltap-core/src/api/engine.rs` (comment-level only), the web-search + providers-listing harness families, `apps/web/src/app/screens/settings/providers/**`, new SPA specs/beats it adds |
| **P4.60** | `crates/quilltap-web/src/custom_tools_routes.rs`, `characters_routes.rs`, `backup_routes.rs`, `brahma_routes.rs`, `embedding_profiles_routes.rs`, `qtap_routes.rs` (confirm-only), their harness families |
| **P4.61** | `crates/quilltap-core/src/services/title_update_job.rs`, the `title_update_tier3` family, `docs/v4/**` (the mirror refresh) |
| All | `docs/CHANGELOG.md` + `status-log.md` append-only; each lane bumps the crates it touches and the unifier recounts |

## Verification gate (before declaring done)

`cargo fmt --all --check`; clippy both feature sets `-D warnings`;
`cargo test --workspace` (background + full-log + sentinel; never `| tail`);
`title_update_tier3` regenerated fresh at the baseline via the sweep
driver, zero unexplained SKIP; no SPA work (none expected). Commits per
`.claude/commands/commit.md`.
