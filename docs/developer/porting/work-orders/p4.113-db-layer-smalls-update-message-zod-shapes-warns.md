# Work order P4.113 — the db-layer review smalls: `update_message` reads ONE raw row and validates the MERGED event as v4 does (repair-writes, ERRORs, and no sibling WARN); the corrupted-row skip gains v4's `createdAt` and `participantId` Zod shapes; v4's `[InstanceSettings] Failed to read setting` WARN; `add_scenario_item`'s `Character not found: Error adding scenario` WARN — maintenance, from `main`

**Status:** OPEN (ordered 2026-09-23, the `d1c06cd9d` review-follow-ups smalls round).

## Preamble

- **v4 oracle baseline: `d1c06cd9d`** (drift-ledger §1). No drift row is
  absorbed (§R.1).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2) and
  before every regen batch. **Any mismatch: STOP and report.**
- **Regen rule: NO PIN REQUIRED at planning — regen from your lane-unique
  pin anyway** (§R.3): **`/tmp/qt-v4-pin-p4113-d1c06cd9d`**.
- **Sibling lanes:** P4.114 (tool/agent loop — it re-runs `scenario_builder_
  tier3`, which reads nothing you change), P4.115 (the web edge; owns
  `test_support.rs` this round — you CONSUME `captured_with`, never edit
  the file), P4.116 (SPA), P4.117 (dead heals — it edits reader files of
  the SYSTEM families, none of yours). You cherry-pick SECOND (§R).
- **Worktree:** `git worktree add <path> -b claude/p4-113-db-smalls-<hash>
  main`; `CARGO_INCREMENTAL=0`.
- **Read before the work:** P4.112's order (its status header's "Recorded,
  not fixed" + its survey item 5) and P4.105's finding 1; v5
  `db/chats_messages.rs:569-623,737-742` and `db/chats_messages_read.rs:
  315-474,682-742`; v4 `lib/database/repositories/chats-messages.ops.ts:
  342-351,517-570` + `safe-query.ts:51-80` + `lib/schemas/chat.types.ts`
  (the three union members) + `lib/schemas/common.types.ts:38,52,55-58` +
  `node_modules/zod/v4/core/regexes.js` (the 4.6.5 datetime + uuid
  regexes); memory notes `a-guard-whose-other-conjuncts-are-false-is-
  untested`, `v4-turns-every-null-cell-into-undefined-on-read`,
  `closing-a-divergence-moves-the-censuses-that-recorded-it`,
  `js-regex-to-rust-regex-fidelity`, `spa-has-no-zod-schema-twins-are-hand-rolled` (for the
  hand-rolled-Zod discipline, server side here).

## Mandate

Close four recorded db-layer divergences, each red-first against v4's real
repository code. `update_message` stops reading the whole chat through the
strict skip and does what v4 does — ONE raw row by `(id, chatId)`, merge
the update, validate the MERGED event against the full `ChatEventSchema`
shape, then write it or ERROR `Failed to update message in chat`. The skip
predicate learns v4's two remaining Zod shapes (`createdAt`,
`participantId`) from ONE datetime-predicate home. Two absent v4 WARN lines
land with capture pins. Must NOT touch: any `tools/**` / `services/**` file,
`api/{types,engine}.rs`, `test_support.rs`, `scenario_builder_capture/
mod.rs`, a committed `.db`, the web crate, the SPA (§Ownership).

## Survey-verified starting points (fresh survey 2026-09-23 on `main` `75af5e08`, v4 at `d1c06cd9d` — do not re-derive)

1. **`update_message`.** v4 `chats-messages.ops.ts:517-570`:
   `messagesCollection.findOne({ id: messageId, chatId })` (`:524` — a raw
   single-row read, `hydrateRow`, NO Zod) → `null` → return `null`; else
   `{ ...existing, ...updates }` → `ChatEventSchema.parse(merged)` (`:530`,
   THROWS on a bad shape) → `updateOne({ id }, { $set: validated })`; the
   whole body under `safeQuery(…, 'Failed to update message in chat',
   { chatId, messageId }, null)` (`:570`) — fallback mode: **ERROR**
   `Failed to update message in chat` `{ chatId, messageId, error }` and
   `null`. v5 `chats_messages.rs:569-589` already has the ERROR on `Err`
   (`Ok(false)`); `update_message_inner` (`:592-623`) returns early at
   `:598` on `find_event_value` → `None`; `find_event_value` (`:737-742`)
   reads EVERY row via `chats_messages_read::get_messages_strict` and
   `find`s the id — so a corrupted target WARNs `Skipping corrupted chat
   message` and answers not-found, and a HEALTHY target in a chat with a
   corrupted sibling emits the SIBLING's WARN (§R.4(c)). The divergence
   table:

   | Planted target row | v4 | v5 today |
   |---|---|---|
   | bad role / non-uuid id / bad hostEvent; the update REPAIRS the field (`{role:'USER'}`, `{hostEvent:null}`) | writes, returns the event | WARN skip, `Ok(false)`, nothing written |
   | same, the update does NOT repair it | ERROR `Failed to update message in chat`, `null` | WARN (not ERROR), `Ok(false)` |
   | healthy target, corrupted sibling | silent, writes | sibling WARN, writes |

   For the non-uuid-id row the op's `messageId` must itself be
   `not-a-uuid` so v4's `findOne` hits it. Measure `find_event_value`'s
   OTHER callers before changing it (`grep -n find_event_value crates/
   quilltap-core/src`) — each caller's v4 counterpart decides whether it
   takes the raw read too; a caller whose v4 twin reads through the Zod
   `getMessages` keeps the strict read.
2. **The two missing Zod shapes.** `createdAt: TimestampSchema` =
   `z.iso.datetime().or(z.date())` on ALL THREE union members (message
   `chat.types.ts:235`, context-summary `:542`, system `:585`);
   `participantId: UUIDSchema.nullable().optional()` (`:257`, message
   only). Zod **4.6.5** at the pin (`package.json:118`): `datetime()`
   accepts `Z` ONLY (no offset, no local) and **REQUIRES seconds**
   (`regexes.js:98-106` — `timeSource({ precision, seconds: true })` →
   `hh:mm:ss(\.\d+)?`, any fraction length). v5's predicate
   `zod_shape_failure` (`chats_messages_read.rs:315-342`): id check `:319`,
   the non-message early return `:322-324`, role `:325-328`, hostEvent
   `:329-340` — `createdAt` must go BEFORE the early return;
   `participantId` after role. `createdAt` is read as `String` (`:201`,
   `:254`, `:278`) so a NULL/non-text cell is already a cell error; a
   malformed STRING passes today. Helpers: `api::zod_issues::zod_uuid_ok`
   (`zod_issues.rs:443`), the local `is_uuid` closure (`:318`).
   **⚠ Trap:** v5's only datetime validator is PRIVATE
   `vault_overlay.rs:287-306` (`ISO_DATETIME_RE`), sourced from "Zod 4.4",
   and makes seconds OPTIONAL — copying it would accept `2024-01-01T10:00Z`,
   which v4 at 4.6.5 rejects. Re-source from the pin's `regexes.js`.
   `db/prompt_templates.rs:348-352` records its own `z.iso.datetime()`
   checks as unported (not yours).
3. **The skip family.** `chats_messages_ops_tier2_equivalence.rs` +
   `harness/oracle/cases/chats-messages-ops-tier2.ts` + `harness/oracle/
   fixtures/chats-messages-ops-tier2.json`. Every `updateMessage` op
   (`:458-489`) runs BEFORE the plants (`plantNullContent` `:581`,
   `plantCell` `:596-643`, the final `getMessages` `:645`); the planted rows
   `e0000090-…-0001` (role `narrator`), `…-0002` (id `not-a-uuid`),
   `…-0003` (hostEvent `42`). The plantable columns are a CLOSED set on
   BOTH sides and must grow: oracle `:71` `PLANTABLE_COLUMNS = new
   Set(['id','role','hostEvent'])`; Rust `:324-327`. The oracle already
   spies `Logger.prototype.error/warn` per op (`:120-155`); the Rust side
   captures per op with `captured_with` (`:288-299`) and compares
   `updateReturns` + the `chats`/`chat_messages` dumps. P4.105's sibling
   family `chat_message_update_fts_tier2` must stay green (the FTS
   triggers fire on the repair write).
4. **`[InstanceSettings] Failed to read setting`.** v4 `lib/instance-
   settings/index.ts:100-114` `readSetting`: `rawQuery('SELECT "value"
   FROM "instance_settings" WHERE "key" = ?', [key])` → `rows[0]?.value ??
   null`; the catch **WARN**s `[InstanceSettings] Failed to read setting`
   `{ key, error: <message> }` and returns `null`. A missing row or a NULL
   `value` is SILENT (no throw). v4's backend ALSO logs ERROR `Raw query
   failed` `{ query, error }` before rethrowing (`backends/sqlite/
   backend.ts:861-886`) — v5 has no `rawQuery` layer; record that line as
   v4-only (pinned as a recorded divergence, not ported). v5 `db/instance_
   settings.rs:59-66` `read_setting`: `.ok()` swallows everything —
   `QueryReturnedNoRows`, the NULL-value `InvalidColumnType`, and real
   failures (`no such table`) alike; its docs at `:10-13` and `:55-58`
   CLAIM "v4 logs a warning" (false today — correct them). The existing
   arm: `scenario_builder_mount_pool_equivalence.rs` + `scenario-builder-
   mount-pool.test.ts` + `fixtures/scenario-builder-mount-pool.json` —
   the plant `settings-table-dropped` (`:299-306`) then `general-read-
   fails` (`:308-318`), whose note already says v5 has no emitter; that
   oracle records only the service logger (`:104-119`) and the Rust side
   captures only `TARGETS = ["quilltap_core::services::scenario_builder::
   mount_pool"]` (`:61`, `:182`) through the SHARED `scenario_builder_
   capture/mod.rs` — which is P4.114's (§R.10(c)).
5. **The JSON-setting parse WARNs (side finding, Tier 2).** v4 `index.ts:
   130-145` `readJsonSetting` WARNs `` `[InstanceSettings] ${key} failed to
   parse — using defaults` `` `{ error }`, used by `memoryExtractionLimits`
   (`:205`), `memoryRecall` (`:223`), `dataRetention` (`:237`),
   `brahmaConsole` (`:255`), `taboo` (`:297`). v5: taboo (`instance_
   settings.rs:339`) and brahmaConsole (`:441`) warn WITHOUT the `error`
   field; `get_memory_recall_settings` (`:141-176`), `get_data_retention_
   settings` (`:208-222` — whose doc claims the warning is "reproduced
   here"; it is not) and `get_memory_extraction_limits` (`:506-518`) fall
   back SILENTLY. Recorded before (`status-log.md` ~145918, ~146243;
   `services/scenario_builder/mount_pool.rs:28-35`).
6. **`add_scenario_item`'s WARN.** v4 `characters.repository.ts:568-594`
   `addToSubArray`, miss arm `:579-583`: `logger.warn(\`Character not
   found: ${errorMsg}\`, { characterId })`; `addScenario` (`:884-925`)
   passes `errorMsg = 'Error adding scenario'` (`:908`) → **WARN**
   `Character not found: Error adding scenario` `{ characterId }` (the
   `title` context rides only `safeQuery`'s ERROR). `addScenario`'s own
   pre-read (`:893`) is silent. Through the route (`app/api/v1/characters/
   [id]/scenarios/route.ts:62-68`) the WARN is reachable only by a race —
   a direct repo call reaches it. v5 `db/vault_character_arrays.rs:436-467`
   `add_scenario_item`, miss arm `:444-446` silent; caller `add_scenario`
   `:376-430`. In-file idiom: `set_default_system_prompt` `:299-308`
   (`tracing::warn!(characterId = %character_id, "…")`). The family:
   `characters_arrays_tier2_equivalence.rs` + `harness/oracle/cases/
   characters-arrays.ts` + `fixtures/characters-arrays-tier2.json`, which
   ALREADY has an absent-character `addScenario` op (`:309-314`,
   `c0ffee00-0000-4000-8000-000000000404`, title `Nowhere`) and already
   wraps each `addScenario` in `captured_with` (`:383-425`); the oracle
   spies no logger yet (its `addScenario` case `:217-245`).

## Tiered deliverables

### Tier 1 — must land

1. **The two Zod shapes** (§Survey 2) — ONE `pub(crate)` datetime predicate
   in `api/zod_issues.rs` beside `zod_uuid_ok`, re-sourced from Zod 4.6.5's
   `regexes.js` at the pin (a unit table covering: `Z` with and without a
   fraction, 1/3/9-digit fractions, NO seconds, an offset, no zone, a
   date-only string, a leap second, `24:00:00`, lowercase `t`/`z` — each
   row's expected value MEASURED by running the pin's `z.iso.datetime()`
   through a tiny tsx probe, never reasoned); `zod_shape_failure` gains
   `createdAt` (all three types) and `participantId` (message; `null`/absent
   pass). Arms: `plantCell` grown to `createdAt` + `participantId` on BOTH
   sides (the closed sets widened identically), planted `2024-01-01T10:00Z`
   (no seconds), `2024-01-01T10:00:00+01:00`, `yesterday` on a message row
   AND on a context-summary or system row, `participantId: 'not-a-
   participant'` — each skipped with the WARN, red-first (count the reds).
2. **`update_message`'s read** (§Survey 1) — the target row read ONCE by
   `(id, chatId)` with no skip; the merged event validated against the
   SAME predicate the skip uses (items 1–2 share ONE home — no second copy
   of the shape rules); a failure is the existing ERROR arm. Arms appended
   to the ops spec AFTER the plants: for each of the three P4.112 shapes
   and the two new ones, a repairing update (v4 writes — the dumps show
   it) and a non-repairing one (ERROR, `null`, nothing written), plus one
   update of a HEALTHY sibling in the planted chat (no WARN at all — a
   silence leg against the sibling's id). Red-first on every repair row.
   Re-run `chat_message_update_fts_tier2` (the FTS row follows the repair
   write). Re-count `get_messages_caller_census` if a call site moves.
3. **`[InstanceSettings] Failed to read setting`** (§Survey 4) — `read_
   setting` matches the error: `QueryReturnedNoRows` and the NULL-value
   type error → `None` SILENT; every other error → WARN `{ key, error }`
   under v4's message bytes. The pin: the mount-pool family's oracle also
   spies the plain `@/lib/logger` WARN filtered to the `[InstanceSettings]`
   prefix; the Rust side adds its OWN capture of `quilltap_core::db::
   instance_settings` inside the family file (do NOT edit the shared
   capture module — §R.10(c)); exactly ONE WARN on `general-read-fails`,
   silence on `general-absent`. The `Raw query failed` ERROR pinned as a
   recorded v4-only line (both directions). Correct the two false doc
   claims.
4. **`add_scenario_item`'s WARN** (§Survey 6) — the miss arm WARNs v4's
   bytes; the oracle gains a `Logger.prototype.warn` spy so the pin is
   v4-sourced; exactly one WARN on the `…0404` op, silence on every other
   `addScenario` op; the existing DEBUG pins unchanged.

### Tier 2 — should land

5. **The JSON-setting parse WARNs** (§Survey 5) — the three silent getters
   WARN and the two existing WARNs gain the `error` field, under v4's
   `` `[InstanceSettings] ${key} failed to parse — using defaults` ``
   bytes; ONE helper (not five copies). NEW family `instance_settings_
   json_warns_equivalence.rs` over a NEW jest oracle `instance-settings-
   json-warns.test.ts` driving v4's REAL getters over a planted malformed
   JSON value per key (and a silence leg per key on a valid value). The
   `error` text is compared by PRESENCE (V8's `JSON.parse` wording vs
   serde's — `v8-json-parse-wording-reaches-the-wire`; do not transcribe).
6. **`vault_overlay.rs`'s stale datetime regex** — measure it against the
   item-1 table. If it disagrees with 4.6.5 on any row, repoint it at the
   new home red-first through the vault-overlay family that reads it
   (name it in the record), and file the finding (P4.D211 called the Zod
   4.6.5 move neutral — this would be the first counter-example); if it
   agrees, record the measurement and leave it.
7. P4.112's header sentence (§R.7).

### Tier 3 — explicit deferrals (loud, typed, never silent)

8. `db/prompt_templates.rs`'s unported `z.iso.datetime()` checks — named,
   not taken (a different repository; its own order).
9. v4's `Raw query failed` ERROR — recorded v4-only (no `rawQuery` layer
   exists in v5), pinned both ways by item 3.

## The differential requirement, restated

Every arm grows an EXISTING family that drives v4's REAL repository code
from the pin (`chats-messages-ops-tier2.ts`, `characters-arrays.ts`,
`scenario-builder-mount-pool.test.ts`), except item 5's new jest family;
the datetime predicate's expected values come from running v4's pinned Zod,
never from reading its regex. Recipes: each family's header through the
sweep driver with `--v4 /tmp/qt-v4-pin-p4113-d1c06cd9d`. Named mutation
proofs: (M1) drop the `createdAt` conjunct → the three datetime plant rows
RED; (M2) make seconds optional in the predicate → the no-seconds row RED;
(M3) revert `update_message` to the whole-chat read → the sibling silence
leg RED; (M4) validate the EXISTING row instead of the merged one → every
repair row RED; (M5) `.ok()` restored in `read_setting` → the WARN pin RED;
(M6) WARN on `QueryReturnedNoRows` → `general-absent`'s silence leg RED.

## Fixtures

Consumes: the tier-2 messages fixture and the characters-arrays / mount-pool
`/tmp`-built fixtures (their builders at the pin). Delivers: spec growth
only. No committed pair changes (§R.10(f)).

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `d1c06cd9d` review-follow-ups smalls round:
**P4.113 ∥ P4.114 ∥ P4.115 ∥ P4.116 ∥ P4.117**, ordered 2026-09-23. Five
lanes, five worktrees, five lane branches, **all cut from `main`** (`75af5e08`
or the commit that lands these orders). No keystone, no stacking, **no wire
change** — no lane adds, removes or reshapes a `Request`/`Response`/`Event`
variant (§R.10). The unifier cherry-picks in the order **P4.117 → P4.113 →
P4.114 → P4.115 → P4.116** (the dead heals first, so every family a later
lane re-runs reads the cleaned readers; the db-layer before the tool layer
that reads through it; the web edge after both core lanes; the SPA last),
gates ONCE from a `d1c06cd9d` pin, and `merge --ff-only`s.

- **§R.1 — Baseline.** The oracle baseline is **`d1c06cd9d`**
  ("feat(scenarios): Scenario Builder — the Host researches and drafts a
  starting scene", v4 main, `4.10.0-dev.67`), drift-ledger §1. At planning
  (2026-09-23) the §2 probe PASSED: v4 `main` HEAD **is** `d1c06cd9d`,
  `bugfix` `1a2b2164c` and `release` `8fbf2afe0` unmoved, the checkout on
  `main`, tree CLEAN. **The ledger's §3 is EMPTY — this round absorbs NO
  drift row**; it takes the follow-ups the `d1c06cd9d` unification review
  recorded (`phase-4.md` → the `d1c06cd9d` UNIFIED section, "Next" item 2;
  P4.D216's, P4.D217's, P4.D218's, P4.111's, P4.112's and P4.110's status
  headers). The baseline does not move at this round's unification.
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: `git -C ~/source/quilltap-server branch
  --show-current`, `status --short`, `log --oneline d1c06cd9d..main`, `log
  --oneline 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Pass = branch `main`, both logs EMPTY, tree CLEAN.** Any mismatch
  — a new v4 commit (a NEW drift row, not a pass), a dirty path, the
  checkout off `main` — is a **STOP**: report it and do nothing else. A lane
  never writes the ledger and resumes only on a waiver the human records in
  the ledger's §1.
- **§R.3 — Regen rule: NO PIN REQUIRED at planning** (drift-ledger §1: v4 AT
  the baseline, clean). **Every lane nevertheless regenerates from a
  LANE-UNIQUE detached pin at `d1c06cd9d`** (`git -C ~/source/quilltap-server
  worktree add --detach /tmp/qt-v4-pin-<order>-d1c06cd9d d1c06cd9d`, the
  THREE symlink classes per ledger §5.1 — root `node_modules`,
  `packages/quilltap/node_modules`, every `plugins/dist/*/node_modules`),
  because five lanes regenerating concurrently from one checkout, and a v4
  commit landing mid-lane, are both the hazards the pin removes; the pin
  also makes the rule's flip to PIN REQUIRED a no-op. Verify the pin by
  `git -C "$PIN" rev-parse HEAD` AND `ls -ld` of the path (build every pin
  path from a quoted literal — `zsh-env-var-does-not-word-split`); `git
  worktree list` before each batch; never stash or checkout-switch the
  human's repo. The sweep driver is the sanctioned per-family path
  (`python3 harness/tools/recipe_sweep.py --run <family> --v4 "$PIN" --v5w
  <ABSOLUTE worktree>`); **never run two sweeps concurrently**; a family
  that reports `SKIP` in 0.00 s did not run. Ledger §5.2's silent-stale-pass
  discipline applies (`rm -f` the output first; read the builder's last
  line; `grep -c` the changed bytes in the fresh NDJSON — > 0 where a change
  is predicted). Stage regen outputs under a LANE-PRIVATE directory
  (`/tmp/<order>/…`) — several committed recipes write FIXED
  `/tmp/oracle-*.ndjson` paths. Committed recipe headers stay canonical
  (never name a `/tmp` pin). Every jest oracle writes ZERO `llm_logs` rows.
  `QT_NODE` is a REAL path (`~/.nvm/versions/node/v24.13.1/bin/node`). v4's
  jest ignores `/\.claude/` paths: mirror a new or edited oracle case + spec
  to a `/tmp` sibling pair before running it (the `brahma-console-tier3`
  header's recipe). The v4 checkout's SQLite binding is ABI-matched to Node
  24 only (`oracle-node-abi-gotcha`).
- **§R.4 — Port from the code, never the prose.** Every item below was
  re-measured at planning (§Survey in each order) against v4 at `d1c06cd9d`
  (`git -C ~/source/quilltap-server show d1c06cd9d:<path>`) — and the
  survey already overturned three recorded descriptions: (a) the Salon
  orchestrator's `tools = ?` line is an **INFO** `Injected tool change
  notification` whose field NAMES also diverge (`chat_id`/`tool_count` vs
  v4's `chatId`/`toolCount`), not a tool-loop DEBUG; (b) v4's
  `normalizeContentBlockFormat` is NOT in `one-shot-loop.ts` — it runs one
  level down, inside `streamMessage` (`streaming.service.ts:449-456`), which
  both tier-3 oracles REPLACE wholesale, so neither side exercises it today;
  (c) `update_message`'s strict read also emits a corrupted SIBLING's WARN
  when a HEALTHY message is updated (v5 reads the whole chat; v4's `findOne`
  is one silent row). Re-verify each claim in your order the same way; a
  work order's paragraph is the same kind of prose as a commit message. A
  finding the order got wrong goes in the lane record and the status
  header.
- **§R.5 — Designed reds and neutrality.** No convergence pin trips (no
  drift is absorbed). Designed reds are each order's red-first arms, named
  in its Tier 1. **Every other family must be NEUTRAL** — a red in a family
  a lane did not touch is a NEW finding to classify before any source is
  touched (and probably not that lane's). Standing reds: the
  `/tmp/qt-imggen-*` staging collision (green by name) and the documented
  Salon-streaming Playwright timing cluster (the previous gate's seven,
  each green by file alone). The previous gate for comparison: the 67-family
  sweep 67/67; `cargo test --workspace` 629 binaries / 3,696 / 0 / 3
  ignored; Tier R 266/0; SPA 447 files / 7,654; Playwright 328 / 7 / 6.
- **§R.6 — The differential requirement stands for every unit.** A changed
  behaviour lands WITH an equivalence arm driving v4's REAL code at the
  right tier (tier 1 exact / tier 2 structural DB diff / tier 3 mocked-LLM),
  red-first where the order says so; a ported log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, or
  `global_capture` where the callsite's `Interest` is shared by un-armed
  tests in the same binary) AND its silence leg, v4-sourced (a `Logger.
  prototype.warn`/`info`/`error` spy in the oracle — the recipe is
  `harness/oracle/cases/chats-messages-ops-tier2.ts:120-155`) wherever a v4
  oracle already runs the path. A ruled or recorded divergence is pinned in
  BOTH directions (the `EXPECTED_DIVERGENCES` shape, `markdown_transcript_
  equivalence.rs`): "VANISHED" if v4 == v5, "WRONG SHAPE" if the difference
  is not the recorded one, and an exercised-count assert. Retiring such a
  pin (P4.114's `OUT_OF_LANE_DIVERGENCES`) is proven by the divergence
  VANISHING, not by deleting the assert first. Mutation proofs: each named
  proof reddens exactly its target; a proof that SURVIVES as first written
  is a finding to fix, never a line to delete. **A v4 unit test that MOCKS
  the thing under test is a source of CASE NAMES, never an oracle.** A
  structured tracing field that v4 logs as an array/object uses the
  `…Json` file-layer convention (`toolsJson = %serde_json::to_string(..)`,
  commit `43ab1779`); v4's camelCase field names are written camelCase in
  the `tracing!` call (the file layer renames only the `…Json` suffix —
  `crates/quilltap-web/src/log_file.rs:1137-1175`).
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (union-merged at unification); a stale claim in an old entry is corrected
  by a NEW record naming it. This order's status header is the lane's to
  update at lane close; the unifier rewrites it. The drift ledger is NEVER
  written by a lane. **Cross-order header edits allowed this round (one
  sentence each, naming this round):** P4.113 → P4.112's header (items
  "recorded, not fixed" taken); P4.114 → P4.D216's and P4.D217's headers
  (the named OPEN items taken); P4.115 → P4.D217's and P4.110's headers;
  P4.116 → P4.D218's header; P4.117 → P4.111's header.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed on each commit per `.claude/commands/commit.md`; the unifier
  RECOUNTS as base + the sum of every lane's bumps (identical bumps
  auto-merge as ONE — the trap fires every round). **Base at planning
  (measured from the `Cargo.toml`s, not CLAUDE.md's bullet, which lags by
  the ruling commit): core 0.0.1027, harness 0.0.948, host 0.0.153, web
  0.0.186, cli 0.0.27, tauri 0.0.7, fixture-sanitizer 0.0.4, sqlite3mc-sys
  0.1.0 (NEVER), SPA 0.5.755.** Expected: **core** by P4.113, P4.114,
  P4.115, P4.117 (the reconcile comment, Tier 2 — only if taken);
  **harness** by P4.113, P4.114, P4.117; **web** by P4.115 ONLY; **host**
  by P4.115 ONLY (`spine.rs`); **SPA** by P4.116 ONLY; **cli**, **tauri**,
  **fixture-sanitizer**, **sqlite3mc-sys** by NOBODY. No lane touches the
  workspace `Cargo.toml` dependency block; every lane's `Cargo.toml` deltas
  are version-only (the unifier audits each before any resolution — the
  `--theirs` clobber trap).
- **§R.9 — The `docs/v4/` mirror** is the unifier's and does not move (no
  drift absorbed). `help/**` is vendored and does not move (127 files).
- **§R.10 — Fences and ownership edges.** (a) **No wire change:**
  `api/types.rs` is touched by NOBODY; `api/engine.rs` by P4.115 ONLY, in
  the `scenarioBuilderBuild` arm's registration hunk (`engine.rs:6436-6498`
  at planning). A lane that finds it needs a wire change STOPs on that item
  and records the shape for the unifier. (b) **`crates/quilltap-core/src/
  test_support.rs`** is P4.115's ONLY (an append-only, fenced
  `// === P4.115 === … // === end P4.115 ===` block for a test-support
  read-only opener); every other lane CONSUMES the existing rigs without
  editing them. (c) **`crates/quilltap-harness/tests/scenario_builder_
  capture/mod.rs`** is P4.114's; P4.113's mount-pool arm builds its own
  capture inside its family file and never edits the shared module. (d)
  **`services/scenario_builder/**`** is P4.115's; P4.114 changes the
  shared one-shot loop and the tool handlers the service CALLS, and
  re-runs `scenario_builder_tier3` as its own family — P4.115 re-runs it as
  a neutrality leg only and reports any red rather than fixing it. (e)
  **`crates/quilltap-harness/tests/get_messages_caller_census.rs`** is
  P4.113's (item 1 may retire or re-key a `get_messages_strict` site; the
  census is re-keyed per (file, fn, variant) — recount with the
  arithmetic). (f) **The committed fixture pairs** are touched by NOBODY
  (P4.117 reads the ten widened pairs to prove deadness; plants go on
  per-run copies). (g) **The web crate's two census files**
  (`dispatch_wrong_type_census.rs`, `tri_state_edges_share_the_decoder.rs`)
  are P4.115's; their counts (449; 115 + `ALLOWED_TRI_STATE_HAND_BUILDS`)
  must NOT move. (h) **Pre-declared loud out-of-mandate edits:** none. If a
  lane finds it needs another lane's file, it STOPs on that item and
  records the shape for the unifier.
- **§R.11 — The SPA lane runs no cargo;** its Playwright beats run against
  a release binary it builds ONLY if the order says so (P4.116 runs the
  scenario-builder beats against `main`'s server — its change is SPA-only).
  Port 4319 is repo-wide: ONE Playwright invocation at a time across all
  lanes (`e2e-port-4319-is-repo-wide`).
- **§R.12 — Committed fixtures: widen, never rebuild** — and this round
  widens none. P4.117 proves each dead heal against the committed pair
  with a `pragma_table_info` read through v4's cipher driver (the P4.111 /
  P4.112 method) on a `/tmp` COPY, recorded in its lane record.

## §S — Shared contract

**None this round.** The five lanes meet at no wire, verb, event, type or
fixture. The only meeting points are the file fences in §R.10 — binding.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.113** — the db-layer smalls: `update_message`'s read, the two missing Zod shapes, the `[InstanceSettings]` WARN, `add_scenario_item`'s WARN | `crates/quilltap-core/src/db/{chats_messages,chats_messages_read,instance_settings,vault_character_arrays}.rs` (the named functions); `crates/quilltap-core/src/api/zod_issues.rs` (ONE `z.iso.datetime()` predicate home, beside `zod_uuid_ok`) + `db/vault_overlay.rs`'s `ISO_DATETIME_RE` (repointed at that home ONLY if item 6 measures it stale); harness `chats_messages_ops_tier2_equivalence.rs`, `chat_message_update_fts_tier2_equivalence.rs`, `characters_arrays_tier2_equivalence.rs`, `scenario_builder_mount_pool_equivalence.rs`, `get_messages_caller_census.rs`, NEW `instance_settings_json_warns_equivalence.rs` (Tier 2); oracle `harness/oracle/cases/{chats-messages-ops-tier2,characters-arrays,scenario-builder-mount-pool.test}.ts` + their `fixtures/*.json` specs + NEW `instance-settings-json-warns.test.ts` | `tools/**`, `services/**`, `api/{types,engine}.rs`, `test_support.rs`, `scenario_builder_capture/mod.rs`, any committed `.db`, `crates/quilltap-web/**`, `apps/web/**` |
| **P4.114** — the tool / agent-loop smalls: `doc_read_file`'s key order, the operator override, the search INFO, the one-shot stream's normalizer + `thoughtSignature`, the Salon tool-change INFO | `crates/quilltap-core/src/tools/doc_edit/text.rs` (`handle_read_file`'s result map); `tools/executor.rs` (`doc_context` + `run_doc_edit`'s `operator_override`); `tools/search.rs` (the INFO); `services/agent_loop/one_shot_loop.rs` (`run_stream`'s normalize + the signature rule); `services/orchestrator.rs` (the ONE `Injected tool change notification` line); harness `scenario_builder_tier3_equivalence.rs` + `scenario_builder_capture/mod.rs`, `brahma_console_tier3_equivalence.rs`, `search_tools_equivalence.rs`, `orchestrator_tier3_equivalence.rs`, `doc_text_equivalence.rs`, `tool_execution_process_tier3_equivalence.rs`; oracle `harness/oracle/cases/{scenario-builder-tier3,brahma-console-tier3,search-tools-search,orchestrator-tier3,doc-text,tool-execution-process-tier3}.test.ts` (or `.ts`) + their specs | `db/**`, `services/scenario_builder/**`, `api/**`, `doc_edit/path_resolver.rs` (consumed, not edited), `model/**` (the Google decoder/builder — item 5 fixes the LOOP's rule only), `test_support.rs`, `crates/quilltap-web/**`, `apps/web/**` |
| **P4.115** — the Scenario Builder edge nits + the web-crate census lift | `crates/quilltap-web/src/{scenario_builder_routes,generator_sse}.rs` (generator_sse changes OPT-IN only); `crates/quilltap-core/src/api/scenario_builder.rs` (the registry); `api/engine.rs` (the build arm's registration hunk ONLY, §R.10(a)); `services/scenario_builder/mod.rs` (`FrameSink` + the terminal-frame gate); `crates/quilltap-host/src/spine.rs` (the publish closure, `:1749-1807` / `:2516-2565` at planning); `test_support.rs` (the fenced block, §R.10(b)); web tests `scenario_builder_{routes_equivalence,disconnect,midstream_failure,dispatch_wire}.rs` + `tests/scenario_builder_spine/**`, `dispatch_wrong_type_census.rs`, `tri_state_edges_share_the_decoder.rs`, NEW `crates/quilltap-web/tests/source_census/mod.rs`; oracle `harness/oracle/cases/scenario-builder-routes.test.ts` + spec | `tools/**`, `services/agent_loop/**`, `db/**`, `api/types.rs`, the swipe/generator/wizard/AI-import route files, every harness test file, `apps/web/**` |
| **P4.116** — the SPA smalls: the builder dialogs portaled, ONE connection-profiles cache shape, the done-frame flash | `apps/web/src/app/scenario-builder/**`; `apps/web/src/app/chat/sidebar/chat-scenario-control*.ts`; `apps/web/src/app/screens/new-chat/new-chat-form*.ts` (only if the portal needs it); every reader of the `['connectionProfiles']` / `['connection-profiles']` query keys (§Survey 2's table) + `apps/web/src/app/workspace/core/tab-refetch.ts` + `screens/characters/characters.api.ts` + `brahma/brahma-console.service.ts`; `apps/web/e2e/scenario-builder-flow.spec.ts`; `apps/web/package.json` (version) | `apps/web/src/app/ui/modal.ts` (the shared modal stays in place — the portal is per-host), `core-contract.ts`, every `crates/**`, every `harness/**` |
| **P4.117** — the dead reader-side heals over P4.111's ten pairs | the 13 heal SITES in §Survey's table ONLY: `crates/quilltap-harness/tests/{title_update_tier3_equivalence,inform_drop_lives_on_the_action,system_import_equivalence,system_export_equivalence,system_backup_equivalence,system_delete_data_equivalence,system_import_state}.rs`; `harness/oracle/cases/{title-update-tier3.test,system-export.test,system-import-execute.test,qtap-schema-validate.test}.ts`; `crates/quilltap-core/src/services/quilltap_import/reconcile.rs` (the stale COMMENT at `:708-712` only, Tier 2) | the helpers (`test_support::ensure_p4d171_columns`/`ensure_p4d182_columns`, `harness/oracle/lib/p4d171-columns.ts` — still used by un-widened readers), every `ensure_chat_informs_table` call (a table, not a column — NOT dead), every committed `.db`, every other reader, `apps/web/**` |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record for the unifier |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — CLAUDE.md's standing rule and the
`background-gate-latency-fix` note). Budget 50–70 GB of disk per Rust lane
(`df -h ~` showed 134 GB free at planning — run at most THREE Rust lanes
concurrently and reclaim `target/debug/incremental` between waves; P4.116
runs no cargo at all).

1. **§R.2 probe** — PASS (opening, and again before every regen batch).
2. `cargo fmt --all --check` — clean (Rust lanes).
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in BOTH
   feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the lane's `d1c06cd9d` pin (§R.3), run with `--nocapture`, the log
   grepped for `SKIP:` (ZERO), the fresh NDJSON grepped for the changed
   bytes, and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated and compared by
   case (where a family is non-deterministic across regens —
   `regenerate-at-both-pins-and-cmp-is-not-universal` — compare normalized
   rows and record).
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, reverted by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast -- --nocapture` with the lane's
   env block (its own families' vars; every other family's var WITHHELD so
   it SKIPs honestly): every binary's `test result:` line read, zero
   failures bar the standing reds §R.5 names, the lane's families
   confirmed RUN by non-zero duration.
9. **Source censuses and guards**, re-run by name, any constant recounted
   with the arithmetic in the comment: `dispatch_wrong_type_census` (449 —
   UNMOVED by every lane, P4.115 included), `tri_state_edges_share_the_
   decoder` (UNMOVED), `get_messages_caller_census` (P4.113 may re-key;
   everyone else unmoved), `blob_write_sites_census`, `compressed_column_
   write_sites_census`, `stream_watchdog_wrap_census` (P4.114 must keep it
   green — the one-shot loop is a wrap site), `help_tree_embed_guard` (127),
   `qtap_schema_embed_guard`, `zod_version_guard`, `provider_sdk_version_
   guard`, `public_schemas_vendor_guard` — GREEN. A new shared test module
   can itself trip a harness census (`a-new-harness-test-can-trip-a-source-
   census`) — only `--workspace` sees it.
10. **Tier R** (`crates/quilltap-cli/tests/cli_differential.rs`) at the
    lane's pin for P4.113, P4.114, P4.115 (all reach code the CLI links —
    266/0 expected; run it, do not assert it).
11. **The SPA** (P4.116 only): `npm test` whole green with the new/changed
    spec files confirmed RUN; `npm run lint` clean; `npm run build` clean
    (the build is the type check — `spa-tsc-does-not-typecheck-app-
    sources`); the four `scenario-builder-flow.spec.ts` beats run LIVE, one
    Playwright invocation at a time; `apps/web/package.json` bumped per
    commit.
12. **Commit per `.claude/commands/commit.md`** (one unit per commit; a
    CHANGELOG entry + version bumps per §R.8; the status-log lane record
    appended with the regen commands AS RUN, the mutation table, the
    pre-fix counts, the measurements, and every finding outside the lane's
    ownership named for the unifier). The lane closes by updating its own
    order's status header with what landed, what was deferred, and what it
    measured that the order got wrong.
