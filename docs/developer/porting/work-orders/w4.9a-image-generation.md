# Work order: W4.9a — the image-generation subsystem (`generate_image`)

**Size: one Opus session** (internal parallelization OK: the three cheap-LLM
tasks + appearance resolution are disjoint from the handler spine). The
long-deferred `generate_image` handler — its W4.2 danger dependency is met;
the pure leaves (`resolveOrientation`, `parsePlaceholders` in
`crate::image_gen`) landed in W4.1d5 part 4. v4 sources (surveyed
2026-07-06): `lib/tools/handlers/image-generation-handler.ts` (1412),
`lib/image-gen/appearance-resolution.ts` (430),
`lib/memory/cheap-llm-tasks/image-scene-tasks.ts` (959 — four of six
functions here; `deriveSceneContext`/`craftStoryBackgroundPrompt` belong to
the story-background follow-up), `lib/wardrobe/avatar-generation.ts` (161).

**Prerequisite / coordination:** the Lantern image-notification writer is
W4.6b. If W4.6b has landed, compose it; if not, keep it an injected recorded
seam with the byte-exact string ("The Lantern, acting upon the instructions
of {requesterName}, has produced the following picture, catalogued under
uuid `{fileId}`…") ported HERE and handed to W4.6b.

## The new model boundary: `model::image`

Port the seam first (the `model::completion` precedent): an
`ImageProvider` trait at v4's `provider.generateImage(params, apiKey)` —
params the merged request (model, prompt, size/aspect/orientation fields,
count), result `{ images: [{ data|b64Json, mimeType, revisedPrompt? }] }` —
plus `CannedImageProvider` keyed by the exact call input, with failure
entries carrying exact message strings so `isImageModerationError` matching
can be driven. The real wire dialects are **W4.7f**. `isImageModerationError`
(the 6 lowercase keywords: "content moderation", "content_policy", "content
policy", "safety system", "rejected by content", "moderation_blocked") is
ALREADY ported in W4.2's `provider_routing` — compose it; do not duplicate.

## What to port

1. **The three cheap-LLM tasks** (`services::image_scene_tasks`, prompts
   byte-exact in generated `prompt_text`): `craftImagePrompt` (weave
   descriptions into the scene prompt; truncation + quote-strip per source),
   `resolveAppearance` (the JSON per-character selection parser),
   `sanitizeAppearance` (explicit → safe rewrite). All over
   `CheapLlmTaskExecutor`.
2. **Appearance resolution** (`services::appearance_resolution`):
   `resolveCharacterAppearances` — the sceneState fast path, the
   trivial-case skip (1 desc / ≤1 item / no messages), the cheap-LLM call
   with dangerous-chat uncensored upgrade (ported `cheap_llm` resolution),
   the default fallbacks; `sanitizeAppearancesIfNeeded` — the FIVE-step gate
   IN ORDER (OFF → pass; dangerous+uncensored-provider → pass; safe → pass;
   dangerous+uncensored available → pass; dangerous+none → sanitize) with
   `wasSanitized` flagged. Wardrobe context via the ported
   `resolve_equipped_outfit_for_character` (imagePrompt preferred over
   title in formatting).
3. **Prompt expansion**: placeholder parse/resolve (ported leaves) + tiers
   + `craftImagePrompt` with the tier-based concatenation FALLBACK chain
   (original → craft → complete/long/medium/short/name) on LLM failure.
4. **The handler spine** (`tools::generate_image`): input validation +
   profile load/validate (`image_profiles` reads ported; API key via the
   `ApiKeyResolver` seam), the Concierge integration — prompt
   classification when `scanImagePrompts`, expanded-prompt classification
   when `scanImageGeneration`, AUTO_ROUTE reroute via the ported
   `resolve_uncensored_image_profile_for_reroute`, and the POST-HOC reroute
   on a provider moderation error (retry once; activeProvider/activeModel
   overwritten for saved metadata) — all composing W4.2, plus
   `resolveOrientation` (ported) mutating the merged params.
5. **`saveGeneratedImage`**: base64 decode → WebP transcode (the injected
   transcoder seam, the doc_blob precedent) → SHA-256 → the Lantern
   Backgrounds store write under `tool/` (compose the ported
   `database_store`/`link_blob_content`; the store's provisioning +
   refuse-if-missing per source — check how the Lantern store is resolved:
   if it's an `instance_settings` pointer like Quilltap General, add the
   scoped read) → the `files` row (sha256, post-transcode mime/size,
   width/height, `linkedTo=[chatId]`, `source='GENERATED'`,
   `category='IMAGE'`, generationPrompt/Model/RevisedPrompt) → tag
   inheritance from linked chats (ported `add_tag`) → the Lantern
   notification (seam or W4.6b).
6. **The avatar trigger** (`services::avatar_generation`):
   `triggerAvatarGenerationIfEnabled` — the `avatarGenerationEnabled` gate,
   the autonomous-chat skip, profile resolution, the
   CHARACTER_AVATAR_GENERATION enqueue (`queue_service` addition), error
   swallow + the structured UI result. This closes the W4.1d2 wardrobe
   deferral (the corpus kept the flag false — now bank it firing).
   The avatar JOB HANDLER itself (573) and STORY_BACKGROUND (943) are the
   tracked follow-up **W4.9c** (they reuse everything here + the scene
   tasks' remaining two functions).
   **W4.9c drift note (v4 `6b6e39ad`, 2026-07-07):** `buildCharacterAvatarPrompt`
   (`lib/wardrobe/avatar-prompt.ts`) gained the bare-top branch — when the
   resolved `leafItemsBySlot.top` is empty, it deliberately routes AROUND
   the ported `describeOutfit`'s "completely naked and unadorned" fallback
   (accessories-only `describeOutfit` call when accessories exist, else
   `''`) and swaps the intro to the collarbone-crop wording ("Close-up
   headshot cropped at the collarbone — only the face, neck, and bare
   shoulders are visible; the chest and torso are outside the frame.") so
   SFW providers stop refusing; port W4.9c from current source and bank
   both branches (bare-top with and without accessories) plus the clothed
   no-change case. `describeOutfit` itself is unchanged — do not touch the
   ported leaf.
7. **Dispatch**: the `BuiltInToolRunner` row for `generate_image` (removing
   it from the loud-fallback set), threading the generated-image paths into
   the already-ported `process_tool_calls` image extraction + the finalizer
   link loop (both banked dormant — now live).

## Differential plan (`image_generation_tier3_equivalence`)

Jest real-DB oracle driving v4's REAL `executeImageGenerationTool` (+
`saveGeneratedImage` via it) over a two-DB fixture (characters + vaults +
equipped outfits + image profiles incl. an uncensored one + chat settings
with the Concierge scan flags), mocking ONLY: the image provider (canned by
exact request — the canned key proves the merged params incl. orientation),
the completion boundary (recorded keys prove all three task prompts), Sharp/
WebP (deterministic fake both sides), and the Lantern notification if
seamed. Cases: plain generation (no placeholders), placeholder + appearance
resolution via sceneState fast path, appearance via cheap-LLM + wardrobe
context, the sanitize gate (dangerous + no uncensored), craft-prompt
fallback chain on LLM failure, prompt-classification reroute, post-hoc
moderation reroute (canned failure message → retry on the uncensored
profile), dedup-by-sha on a repeat save, and the avatar trigger (fire +
autonomous-skip + disabled). Diff: result objects + `files` +
`chat_messages` + the five mount-index tables (shared-cross-db id-map remap;
`[[wardrobe-transfers-remap-gotcha]]` applies — content-addressed bytes
carry minted values) + `background_jobs` for the avatar enqueue. Then
regenerate `tool_dispatch_equivalence` (+1 row) and the finalizer/
orchestrator differentials with a live generated-image case (the image
link-loop order `[firstToolMessageId, assistantMessageId]` banked live).

## Watch-outs

- The five-step sanitize gate ORDER is load-bearing; port with the
  why-comments.
- Non-critical reads (aesthetics/guidance) are error-swallowed in v4 —
  generation proceeds; keep the swallow shape.
- `generationRevisedPrompt` comes from the provider response or null —
  the canned provider must carry it so the row diff proves the plumbing.
- The Lantern store `tool/` subfolder refuse-to-write rule — bank the
  unprovisioned-store failure case.

## Deliverables checklist

- [ ] `model::image` seam + tasks + appearance resolution + handler +
      `saveGeneratedImage` + avatar trigger ported; `generate_image`
      dispatched; the W4.1d2 avatar deferral closed.
- [ ] Differential green; regenerated `tool_dispatch`, finalizer,
      orchestrator differentials green; `wardrobe_tools` re-verified.
- [ ] Full workspace `cargo test` / `clippy -D warnings` / `fmt --check`.
- [ ] CHANGELOG, versions, CLAUDE.md, `chat-orchestration.md`; W4.9c
      (avatar + story-background job handlers) recorded as the follow-up;
      memory note.
- [ ] Commit per `.claude/commands/commit.md`, branch `w4.9a-image-gen`.

**STOP rules:** no image codec crates in the core (transcode is a seam);
`isImageModerationError` lives ONLY in W4.2's module; the real image wire
dialects are W4.7f — the provider stays canned here; schema never changes.
