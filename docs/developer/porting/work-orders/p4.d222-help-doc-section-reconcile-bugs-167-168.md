# Work order P4.D222 — bugs 167 + 168, v4 `492771aff`: help docs indexed by SECTION and reconciled at startup — the HELP_DOC embedding job rewritten over `embed_help_doc_sections` (stored section vectors reused, missing ones embedded, a width rule, a per-section WARN, the doc vector as `average_embeddings` = an f32 accumulator then `normalize_vector`, `guard_skip` dropped for HELP_DOC, the empty-input refusal), NEW `reconcile_help_docs` replacing the divergence gate + the `count() > 0` backfill + `enqueue_missing_help_doc_embeddings` (SHA-256 sync by id, `deleteByEntity('HELP_DOC', id)` on update, NEW `count_by_doc`, the incomplete rule, the `Help docs reconciled` INFO, the once-per-boot memo that never throws), the boot order (3.66 BEFORE the dimension reconcile), `upsert_by_path` + `find_all_needing_embedding` RETIRED with their family, the WHOLE `help/` tree re-vendored at `b0b6656b5` (127 → 129) with the two literals and the SPA's captured category table — from `main`. **Bug 168 IS dogfood #120 — a CONVERGENCE by a mechanism #120 never proposed.**

**Status:** UNIFIED on main (2026-09-25, the `b0b6656b5` round) — **DONE:** Tier 1 items 1–7 and Tier 2 items 8–10 whole (units 5+6 as ONE commit); **dogfood #120 CLOSED by CONVERGENCE** (v4 bug 168's mechanism, recorded in `dogfood-findings.md`); the `docs/v4/` mirror refreshed by the unifier. **Fixed at unification (§3 review):** the `findAll` "propagation" was a NEW divergence presented as closing one — v4's `helpDocs.findAll()` is the base repository's FALLBACK `safeQuery` (ERROR `Error finding all entities` `{collection: 'help_docs', error}` then `[]`), never a throw; both help-sync reads now take that shape (the sync carries on over an empty table; the reconcile finds nothing incomplete), the unit test retargeted; the three fallback ERROR lines (`count_by_doc`, the job's `findByDocId`, `findAll`) carry v4's base-class `collection` field with pins; the reconcile's zero-length doc-vector rule tested at the repo; `help_doc_ensure_equivalence`'s committed recipe header grown to the TWELVE scenarios (the first unification sweep failed its regen on the six-scenario loop — the stale-header trap); its header and `host_help_docs_boot`'s rewritten to the truth (129, the EAGER reconcile CONVERGED); the gate test's false "rolls back" comment corrected. **Deferred as ordered:** Tier 3 items 11–13 (no Rust tokenizer; the #120 note + mirror — done by the unifier; the three mocking jest cases untouched). Lane record + round record: `status-log.md`.

## Preamble

- **v4 oracle baseline: `d1c06cd9d`** (drift-ledger §1). This lane absorbs
  the ledger's `492771aff` row ("fix(help): index help docs by section and
  reconcile them at startup (bugs 167, 168)", 2026-09-24 12:03,
  `4.10.0-dev.72`, 33 files, +1,516/−992 — `ORDERED(P4.D222)`) and the
  `help/` halves of `e3937d7aa`, `3376b3dfa` and `b0b6656b5` (the tree is
  ONE artifact; this lane re-vendors it whole at the target `b0b6656b5`).
  Spec reads: `git -C ~/source/quilltap-server show 492771aff -- <path>` for
  the hunks and `git show 492771aff:<path>` for the post-commit file — never
  the commit message (§R.4).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** This lane pins the TARGET
  **`/tmp/qt-v4-pin-p4d222-b0b6656b5`** for every family its port MOVES
  (`embedding_generate_jobs_equivalence`, `help_doc_ensure_equivalence`,
  `help_doc_sync_equivalence`, `help_doc_sync_guards_equivalence`,
  `help_tree_equivalence`, the NEW `average_embeddings` family, the NEW
  help-section-size family, the SPA `help-guide-capture` recorder) and for
  the re-runs it PREDICTS neutral (`help_doc_chunking_equivalence`,
  `help_docs_tier2_equivalence`, `help_docs_routes_equivalence`,
  `help_chat_orchestrator_tier3_equivalence`, the `embedding_reindex_*`
  families — reindex's `phase_help_docs` now reaches the new
  `deleteByEntity`, so "neutral" is a PREDICTION to measure); and the
  BASELINE **`/tmp/qt-v4-pin-p4d222-d1c06cd9d`** for the pre-port red
  counts and the neutrality legs. Pin marker: the target-pinned
  `help_tree_equivalence` NDJSON carries `chat-settings-composer` (a file
  that exists only at the target); the baseline-pinned one does not
  (`grep -c` both).
- **Sibling lanes:** P4.D220 (the action-dispatch consolidation — owns
  `crates/quilltap-web/src/**` incl. `help_routes.rs`, and `api/types.rs`),
  P4.D221 (the `lib/` riders — owns the `db/characters.rs`/`db/memories.rs`
  method retirements and four oracle cases; NOT the help ones), P4.D223
  (the quick-hide SPA lane — owns `apps/web/**` EXCEPT
  `apps/web/src/app/help/**` and `apps/web/oracle/help-guide-capture.test.tsx`,
  which are YOURS), P4.D224 (the mention-typeahead SPA lane — likewise).
  Nobody consumes anything from you; you consume nothing from them. The
  help REST edges (`help_routes.rs:83/:117-126/:170-190`) are P4.D220's —
  if a help verb's core body must move for the reconcile, record the site
  and STOP on that item (§R.10).
- **Worktree:** `git worktree add <path> -b claude/p4-d222-help-reconcile-
  <hash> main`; `CARGO_INCREMENTAL=0`. The SPA half needs `npm ci` in the
  worktree's `apps/web` (`spa-worktree-node-modules` — never `cp`).
- **Read before the work:** the ledger's `492771aff` row whole; v4 at
  `492771aff`: `lib/help/help-doc-sync.ts` whole (466 lines),
  `lib/background-jobs/handlers/embedding-generate.ts:324-558`,
  `lib/embedding/embedding-service.ts:419-456`,
  `lib/database/repositories/help-doc-chunks.repository.ts:80-101`,
  `instrumentation.ts:895-936`, and the three test files in §Survey H; v5
  `services/help_doc_sync.rs` whole, `services/embedding_generate_job.rs:
  532-750`, `db/help_docs.rs:126-170,:277,:336,:423-470`,
  `db/help_doc_chunks.rs:79-101,:247,:340`, `db/help_doc_chunks_repair.rs`
  (a reader of a retiring symbol — measure which), `embedding_vector.rs:53`,
  `crates/quilltap-host/src/host.rs:495-530,:1047-1085,:1380-1398`,
  `crates/quilltap-host/build.rs:86-110`; memory notes
  `a-process-global-test-seam-must-be-thread-scoped`,
  `capture-layer-target-assert-is-a-prefix-match`,
  `tracing-percent-field-renders-unquoted`,
  `jest-oracle-lazy-help-ensure-resyncs-the-fixture`,
  `help-embed-guard-cannot-see-a-stale-revendor`,
  `jest-setup-llm-logging-service-mocked`, `spa-oracle-recorders-live-
  outside-src`, `a-vendored-count-is-hard-coded-in-several-crates`.

## Mandate

Give v5 v4's section-indexed help embedding and its startup reconcile, so
the five largest help pages (dogfood #120 / v4 bug 168) get a document
vector at last — averaged from their section vectors, never embedded
whole — and a boot repairs whatever is missing (a null doc vector, fewer
embedded sections than sections, a doc with no section rows) instead of
gating on "the table is populated and the tree has not diverged". Deliver:
`average_embeddings` beside `normalize_vector` with a tier-1 family;
`embed_help_doc_sections` + the rewritten `help_doc_try` with every v4 arm
and log line (capture-pinned); `count_by_doc`; the sync shape (update by
id / create with a pinned uuid, the `embedding_status` clear on update, the
DEBUG line); `reconcile_help_docs` + the once-per-boot `ensure_help_docs_
synced` that never throws; the boot reorder (help reconcile awaited BEFORE
the dimension reconcile); `upsert_by_path`, `HdUpsert`, `find_all_needing_
embedding`, the divergence gate, the `count() > 0` backfill and
`enqueue_missing_help_doc_embeddings` RETIRED with their family and fixture;
the WHOLE `help/` tree re-vendored from `b0b6656b5` (129 files, `diff -r`
recorded), both `127` literals → `129`, the SPA's category table
RE-CAPTURED (never hand-edited); every moved family regenerated at the
target red-first, every neutral one re-run and recorded. Must NOT touch:
`crates/quilltap-web/src/**` (P4.D220), `api/types.rs`/`api/engine.rs`
(P4.D220), `db/characters.rs`/`db/memories.rs`/`write_partition.rs`/
`services/brahma_console/**`/`photos/**` (P4.D221), `apps/web/**` outside
`apps/web/src/app/help/**` + `apps/web/oracle/help-guide-capture.test.tsx`
(P4.D223/P4.D224), the drift ledger, `dogfood-findings.md` (the #120
convergence note is the UNIFIER's — §R.9).

## Survey-verified starting points (fresh survey 2026-09-24 at `492771aff` / `b0b6656b5` and on `main` `ca3e245f` — do not re-derive)

### v4 (post-commit files at `492771aff`)

1. **`lib/background-jobs/handlers/embedding-generate.ts`.** Imports
   `averageEmbeddings` from `embedding-service` (`:10`) and
   `helpChunkEmbeddingText` beside `buildHelpDocChunks` (`:14`).
   `HelpDocSection` (`:324-331`): `id?` (absent for an in-memory slice),
   `chunkIndex`, `heading`, `content`, `embedding: Float32Array | null`.
   **`embedHelpDocSections(job, payload, repos, doc)` (`:354-449`)** returns
   `{vectors, embedded, reused, failed}`, IN ORDER:
   - `:360-369` load `helpDocChunks.findByDocId(doc.id)`; a stored row's
     embedding is kept only if `chunk.embedding && chunk.embedding.length >
     0`, else `null`; **no stored rows → `buildHelpDocChunks(doc.content)`
     slices with `embedding: null` and NO `id` — never persisted.**
   - `:371-377` DEBUG `[EmbeddingGenerate] Embedding help doc sections`
     `{context:'handleEmbeddingGenerate', jobId, docId, sections,
     storedRows}`.
   - `embedSection` (`:383-413`): text = `helpChunkEmbeddingText(doc.title,
     heading, content)`; **blank `text.trim()` → return silently, counted
     nowhere**; `generateEmbeddingForUser(text, job.userId,
     payload.profileId, {priority:'background'})` → `section.embedding`;
     `helpDocChunks.updateEmbedding(id, vec)` ONLY when `section.id`;
     `embedded++`. **ONE catch over the provider call AND the chunk write:**
     `failed++`, `lastError = error`, `section.embedding = null` (memory
     only — the DB row is untouched), WARN `[EmbeddingGenerate] Help doc
     section embedding failed — skipping section` `{context, jobId, docId,
     chunkId: section.id, chunkIndex, error}` (`chunkId` undefined for a
     slice). v5's separate `Help doc chunk embedding write failed` arm
     (`:729-735`) has NO v4 counterpart — it folds into this one.
   - `:415-420` `reusedAtStart` = the set with an embedding; every
     embedding-less section embedded in chunk order.
   - `:422-433` **the width rule:** `fresh` = the first section in chunk
     order WITH an embedding and NOT in `reusedAtStart` (evaluated after the
     first pass); `width` = `fresh.length`, else the first section with any
     embedding; every section whose length ≠ `width` leaves `reusedAtStart`
     and is re-embedded through `embedSection` (counted `embedded`, or
     `failed` — and a failed re-embed leaves the OLD wrong-width vector in
     the DB, nulled only in memory).
   - `:435-437` `vectors` = embeddings of length `width`, chunk order.
   - `:439-441` throws `lastError` ONLY when `vectors.length === 0 &&
     lastError` — zero sections or all-blank text does NOT throw.
   - `:443-448` `reused` = sections still in `reusedAtStart` with an
     embedding.
   **`handleHelpDocEmbedding` (`:462-558`):** not-found unchanged
   (`:468-482`, WARN `Help doc not found` → `markAsFailed`); **`skipIfOversize`
   DROPPED for HELP_DOC** (still used by MEMORY `:147`, CONVERSATION_CHUNK
   `:258`, MOUNT_CHUNK `:588`); `:485-486` sections → `averageEmbeddings(
   sections.vectors)`; **`null` (`:488-506`)** → WARN `[EmbeddingGenerate]
   Skipping empty entity` `{context, jobId, entityType:'HELP_DOC',
   entityId: doc.id, title}` + `markAsFailed('HELP_DOC', payload.entityId,
   payload.profileId, 'Empty input — nothing to embed', job.userId)` +
   return (no retry); `:508` `helpDocs.updateEmbedding(doc.id,
   docEmbedding)` (throws `Help doc not found for embedding update: ${id}`
   on no row); `:510-515` `markAsEmbedded`; `:517-527` INFO
   `[EmbeddingGenerate] Help doc embedding generated` `{context, jobId,
   docId, title, dimensions: docEmbedding.length, sectionsAveraged:
   vectors.length, sectionsEmbedded, sectionsReused, sectionsFailed}` (the
   old `chunksEmbedded` is GONE; `dimensions` is the averaged length, not
   the provider's); the catch (`:528-557`) unchanged in shape. **Write
   order:** per-section `help_doc_chunks.updateEmbedding` (null sections in
   chunk order, then the width re-embeds), then `help_docs.updateEmbedding`,
   then `embedding_status.markAsEmbedded`. `help_doc_chunks` takes only the
   BLOB + `updatedAt` (no dimension/status column); `embedding_status` has
   ONE row per doc, none per chunk. **REMOVED:** WARN `Help doc chunk
   embedding failed — skipping chunk` and WARN `Could not embed help doc
   chunks`.
2. **`lib/embedding/embedding-service.ts:432-456` (the ONLY addition):**
   `averageEmbeddings(vectors) → Float32Array | null`: `[]` → `null`;
   `dims = vectors[0].length`; `sum = new Float32Array(dims)`; per vector in
   order (a differing length THROWS `Cannot average embeddings of differing
   dimension (${dims} vs ${v.length})` — unreachable from the handler, which
   pre-filters to `width`), per `i`: `sum[i] += v[i]` — **f64 add, rounded
   to f32 on store = a correctly rounded f32 add**; then `normalizeVector(
   sum)` (`:419-430`, UNCHANGED: f64 `norm += v[i]*v[i]`, `norm === 0` →
   return as is, else `inv = 1/Math.sqrt(norm)`, `v[i] = v[i]*inv` rounded
   to f32). v5's `embedding_vector.rs:53` `normalize_vector` already matches
   bit for bit; **`average_embeddings` does not exist.**
3. **`lib/help/help-doc-sync.ts` (466 lines).** `parseFrontmatter` (`:73`)
   and `extractTitle` (`:89`) now EXPORTED (test-only). `contentHash`
   (`:105-107`) UNCHANGED — SHA-256 hex over `readFileSync(...).trim()`
   (frontmatter included); v5's `hash_content(js_trim(raw))` at
   `help_doc_sync.rs:208,283` already matches. **`syncHelpDocs`
   (`:124-297`):** `:193` `fields = {title, path: relPath, url, content:
   body, contentHash}`; `:195-202` existing → `helpDocs.update(existing.id,
   fields)` (note: `path` is now passed too, same value) / else `docId =
   randomUUID()`, `helpDocs.create(fields, {id: docId})` — **`upsertByPath`
   is GONE**; `:208-210` `replaceForDoc(docId, buildHelpDocChunks(body))`;
   `:213-223` existing → `clearAllEmbeddingsForDoc(docId)` then **NEW
   `embeddingStatus.deleteByEntity('HELP_DOC', docId)`** then `updated++`,
   else `created++`; `:225-231` NEW DEBUG `[HelpDocSync] Synced help doc`
   `{context:'syncHelpDocs', path: relPath, docId, action:
   'updated'|'created', chunks}`; the prune (`:244-288`) unchanged. Write
   order per changed file: update/create → replaceForDoc → clear → delete.
   **`reconcileHelpDocs()` (`:335-377`, NEW):** `sync = syncHelpDocs()`;
   `docs = helpDocs.findAll()` (re-read AFTER the sync); `sectionCounts =
   helpDocChunks.countByDoc()`; per doc: no entry in the map →
   `buildHelpDocChunks(doc.content)`, and ONLY if > 0 chunks:
   `replaceForDoc`, `sectionsBackfilled++`, `counts = {total: n, embedded:
   0}`; **incomplete = `doc.embedding == null || doc.embedding.length ===
   0` OR (`counts !== undefined && counts.embedded < counts.total`)** — a
   doc slicing to zero chunks is judged on its vector alone; `:364-372`
   INFO `[HelpDocSync] Help docs reconciled` `{context:'reconcileHelpDocs',
   created, updated, deleted, unchanged, sectionsBackfilled, incomplete}`;
   `enqueueHelpDocEmbeddings(incompleteIds)`; returns `{sync,
   sectionsBackfilled, incomplete}`. **`ensureHelpDocsSynced` (`:390`):**
   `reconcilePromise ??= reconcileHelpDocs().catch(e => {reconcilePromise =
   null; throw e})`, awaited; **never throws** — failure → WARN
   `[HelpDocSync] Help doc reconcile failed; serving help from the existing
   index` `{context:'ensureHelpDocsSynced', error}`, the NEXT caller retries.
   Still called from `instrumentation.ts:913-914` and `lib/help-search.ts:
   17,42`; `syncHelpDocs` ALSO from `embedding-reindex.ts:202` (so reindex
   now reaches `deleteByEntity`). **REMOVED:** `listHelpDocPathsOnDisk`, the
   old `ensureHelpDocsSynced` (the `existing.length>0 && !diverge` gate, its
   `syncPromise` cleared in `finally`, the trailing
   `enqueueMissingHelpDocEmbeddings`), `backfillHelpDocChunks` (`count() >
   0` gate), `helpDocsDivergeFromDisk`, `enqueueMissingHelpDocEmbeddings`
   (`findAllNeedingEmbedding`); dropped lines `Backfilling help doc
   sections`, `Help doc sections backfilled`, `Help doc section backfill
   failed`, `Help docs on disk diverge from the database`, `Failed to look
   up help docs needing embedding`. **Every log line in the new file, in
   order:** `:60` ERROR `Error reading directory`; `:137` WARN `Help
   directory not found`; `:148` INFO `No Markdown files found in help
   directory`; `:225` DEBUG `Synced help doc` (NEW); `:236` ERROR `Failed
   to sync file`; `:256` WARN `No help docs on disk have usable content but
   the table is populated — skipping the destructive prune`; `:280` ERROR
   `Failed to prune deleted help doc`; `:290` INFO `Sync completed`
   `{context, totalOnDisk, created, updated, unchanged, deleted, failed,
   chunksWritten, changedIds: <count>}`; `:364` INFO `Help docs reconciled`
   (NEW); `:401` WARN `Help doc reconcile failed; …` (NEW); `:425` DEBUG
   `Help docs need embedding but no embedding profile is configured`;
   `:450` INFO `Enqueued help doc embeddings`; `:458` ERROR `Failed to
   enqueue help doc embeddings` (all `[HelpDocSync]`-prefixed).
4. **`help-doc-chunks.repository.ts:80-101` `countByDoc()`** through
   `safeQuery` (failure → empty `Map` + `Error counting help doc chunks by
   doc`):
   ```sql
   SELECT "docId" AS docId,
          COUNT(*) AS total,
          SUM(CASE WHEN "embedding" IS NOT NULL THEN 1 ELSE 0 END) AS embedded
   FROM "help_doc_chunks"
   GROUP BY "docId"
   ```
   → `Map(docId → {total: Number(total), embedded: Number(embedded ?? 0)})`.
   **A zero-length BLOB counts as embedded HERE while the job treats it as
   null** — reproduce both, do not reconcile them. **`help-docs.repository.
   ts` −53:** `upsertByPath` (old `:89-124`) and `findAllNeedingEmbedding`
   (old `:202-217`) DELETED; `create(data, options?)` (`:54-59`) takes the
   pinned id. **`help-doc-chunking.ts`:** NEW exported constant
   `HELP_SECTION_EMBEDDING_MAX_TOKENS = 1000` (`:54`, production module,
   consumed ONLY by `help-doc-size.test.ts`), the docblock rewritten,
   chunking + `HELP_CHUNK_OPTIONS` (`targetMaxTokens: 700`) UNCHANGED.
   `help-search.ts:115` comment-only. `categories.ts:47-48` adds
   `'chat-settings-composer'` + `'chat-settings-ai-services'` directly after
   `'chat-settings'` (`:46`) in the `chats` category. **`instrumentation.ts:
   902-920` Phase 3.66:** AFTER the 3.65 FTS reconcile (ends `:900`), BEFORE
   the 3.7 `reconcileEmbeddingDimensions` (`:923-936`, fire-and-forget) —
   **awaited**: `await ensureHelpDocsSynced()` (NOT `reconcileHelpDocs`
   directly — the message's wording); its catch WARN `Help doc
   reconciliation failed` `{context:'instrumentation.register', error}`
   fires only on an import failure. **NO migration, NO DDL change**
   (`DDL.md:1210,:1216` prose only; no `migrations/` path in the commit).
   `js-tiktoken ^1.0.21` devDependency (test-only).
5. **The `help/` tree at `492771aff`: TWELVE files, not thirteen** (the
   ledger row over-counts by one): ADDED `chat-settings-composer.md` (294
   lines, url `/settings?tab=chat&section=composer-spellcheck`) +
   `chat-settings-ai-services.md` (339 lines, url `/settings?tab=chat&
   section=context-compression`); MODIFIED `chat-settings.md` (800 → 229),
   one link line each in `chats.md`, `connection-profiles.md`,
   `file-uploads.md`, `memory-housekeeping.md`, `memory-regenerate.md`,
   `provider-recommendations.md`, `story-backgrounds.md`, `taboo.md`, and
   `embedding-profiles.md` (+1 bullet, 2 link swaps). `git ls-tree -r
   --name-only 492771aff -- help/ | wc -l` = **129** (parent 127). **At the
   TARGET `b0b6656b5` the tree ALSO carries** `quick-hide.md` +
   `dangerous-content.md` (`e3937d7aa`), `carina.md` + `chats.md`
   (`3376b3dfa`), `chat-participants.md` (`b0b6656b5`) — 129 stays 129. v5's
   tree (127) already differs from `492771aff`'s PARENT in those four files,
   so **re-vendor WHOLE from `b0b6656b5`, never patch by hunk.**

### v5 (on `main` `ca3e245f`)

6. **`services/embedding_generate_job.rs`** is the OLD whole-text design:
   `help_doc_branch` (`:532-566`); `help_doc_try` (`:568-637`) — `:576`
   `format!("{}\n\n{}", title, content)`, `:577-589` `guard_skip` (must GO
   for HELP_DOC), `:590-598` the whole-text call, `:600-614`
   `update_embedding` + `Err("Help doc not found for embedding update: …")`
   on false, `:623` `embed_help_doc_chunks`, `:625` `mark_embedded`,
   `:628-635` INFO with `doc_id, title, dimensions = result.dimensions,
   chunks_embedded`; `embed_help_doc_chunks` (`:654-750`) — `:669` WARN
   `Could not embed help doc chunks`, `:681` skips non-empty embeddings,
   `:729-735` WARN `Help doc chunk embedding write failed` (NOT counted —
   no v4 twin), `:738-745` WARN `Help doc chunk embedding failed — skipping
   chunk`. `HelpDocChunkRow.embedding: Vec<f32>` (empty = none,
   `db/help_doc_chunks.rs:81`); `chunk_index: f64` (`:79`).
7. **`services/help_doc_sync.rs`:** header `:41-54` claims "Not wired at
   startup" — STALE (`host.rs:521` calls it); `:29-30` claims the promise
   guard "does not port" — now WRONG (v4 memoizes once per process).
   `sync_help_docs` (`:240`) uses `repo.upsert_by_path(&HdUpsert{..})`
   (`:295-301`), no `deleteByEntity` on update (`:312-314`), no DEBUG.
   `help_docs_diverge_from_disk` (`:390`); `ensure_help_docs_synced`
   (`:423-446`: gate `:431`, backfill `:435`, sync `:440`, enqueue-missing
   `:444`); `backfill_help_doc_chunks` (`:480`, `count() > 0` at `:487`);
   `enqueue_missing_help_doc_embeddings` (`:543`);
   `enqueue_help_doc_embeddings` (`:575`, KEEP). **Logging here is
   `eprintln!`** (`:342,:526,:560,:616` — "the module's convention"): the
   lines v4 emits through `logger` land on `tracing` at v4's levels with
   capture pins; the pre-existing `eprintln!` lines are Tier 2 (all of them
   move together or none).
8. **`db/help_docs.rs`:** `upsert_by_path` (`:423-470`) + `HdUpsert`
   (`:139-145`) RETIRE; `find_all_needing_embedding` (`:277`) RETIRES;
   **`HelpDocRow` (`:163-170`) has NO embedding field** and `find_all`
   (`:193`) selects none — `reconcile` needs a doc-vector-missing projection
   (null OR zero-length; `qt_text`/BLOB — measure the column); `HdUpdate`
   (`:126-132`) lacks `path` (harmless — same value; add it or record why
   not); `create(&HdCreate, &CreateOptions{id,…})` (`:336`) already takes a
   pinned id. **`db/help_doc_chunks.rs`: NO `count_by_doc`** (only `count()`
   `:340-346`; `update_embedding` `:247`). `db/help_doc_chunks_repair.rs`
   names one of the retiring symbols — measure which and re-home.
9. **Boot:** `host.rs:501` `seed_built_ins(db)?` runs the DIMENSION
   reconcile inside it (`:1393-1398`, the comment at `:1380` "immediately
   behind 3.6"); THEN `host.rs:521` `ensure_help_docs_synced_at_boot(db)`
   (`:1047-1085`: INFO `Help documents synced from the embedded tree` on
   `Some`, nothing on `None`, WARN on error or panic). **v5 runs the phases
   in the WRONG order for the new v4** — 3.66 must be awaited BEFORE 3.7.
   `host_help_docs_boot.rs:98` asserts `127`; `:127-136` asserts a second
   boot touches no row (must still hold under the reconcile when the tree
   is unchanged — a pin worth keeping). `services/embedding_reindex_job.rs:
   425` `phase_help_docs` → `sync_help_docs` inside `db.write` — picks the
   new shape up for free; its family is a NEUTRALITY leg to measure.
10. **The tree:** the workspace-root `help/` is embedded by
    `crates/quilltap-host/build.rs:86-110` into `help_content::EMBEDDED_HELP`
    (`files_store.rs:360` reads it). **No sync script — re-vendored by hand
    per commit; the lane record carries the `diff -r`.** Literals:
    `crates/quilltap-host/tests/host_help_docs_boot.rs:98` and
    `crates/quilltap-harness/tests/help_tree_embed_guard.rs:69`
    (`VENDORED_FILE_COUNT: usize = 127`). SPA: `apps/web/src/app/help/help-
    categories.ts:58` `'chat-settings'` — pinned to `apps/web/src/app/help/
    __fixtures__/help-guide-tables.json:37`, recorded by
    `apps/web/oracle/help-guide-capture.test.tsx` (last at `d883a5ee1`) —
    **RE-CAPTURE at the target pin; never hand-edit either file.**
11. **Families (harness test ↔ oracle case, runner, env):**
    `embedding_generate_jobs_equivalence.rs` ↔ `embedding-generate-jobs.
    test.ts` (jest; `QT_ORACLE_EG`, `QT_FIXTURE_EG_MAIN`, `QT_FIXTURE_EG_
    MOUNT`; spreads the REAL `embedding-service`; HELP_DOC arms `hd-happy` /
    `hd-missing`; the fixture's three chunks: failing first, `[0,1,0…]`
    reused whose composed text equals the old whole-doc text, a fresh one
    → e0 — at the target the doc vector becomes `normalize(e0+e1)` and the
    whole-text call VANISHES); `help_doc_ensure_equivalence.rs` ↔
    `help-sync-ensure.test.ts` (jest; `QT_ORACLE_HELP_ENSURE`, `QT_FIXTURE_
    ENSURE_DIR`, `QT_ENSURE_SCENARIO` ∈ `empty-table`, `in-sync`,
    `in-sync-chunked`, `added-doc`, `deleted-doc`, `no-profile` — the symbol
    survives, the SEMANTICS move); `help_tree_equivalence.rs` ↔ `help-tree-
    sync.test.ts` (jest; 129 at the target); `help_doc_sync_equivalence.rs`
    ↔ `help-sync-tier2.ts` (tsx; `QT_ORACLE_HELP_SYNC`, `QT_FIXTURE_HELP_
    MAIN`; `embedding_status` + the create shape move); `help_doc_sync_
    guards_equivalence.rs` ↔ `help-sync-guards.ts` (tsx); **`help_docs_
    upsert_tier2_equivalence.rs` ↔ `help-docs-upsert-tier2.ts` BREAKS at
    the target (`repo.upsertByPath` `:102`)** — with `harness/oracle/
    fixtures/build-help-docs-upsert-fixture.ts` + `help-docs-upsert-tier2.
    json`; RETIRE all four; `help_doc_chunking_equivalence` (tsx; NEUTRAL);
    `help_docs_tier2_equivalence` (tsx; create/update/delete only —
    NEUTRAL); the three jest cases that MOCK `ensureHelpDocsSynced`
    (`help-docs-routes.test.ts:87`, `search-tools-search.test.ts:193`,
    `help-chat-orchestrator-tier3.test.ts:147`) — unaffected. Nothing in the
    harness names `findAllNeedingEmbedding`.

### v4's changed tests (the vectors to mirror as PLANTED real-DB arms — §R.6)

12. **`embedding-generate-extended.test.ts`** (mocks the provider, REAL
    `averageEmbeddings`): (a) `:132` two null chunks (`Section 0`/`1`,
    `Body 0.`/`Body 1.`) → `[1,0]`, `[0,1]`: two calls, the first text
    `'Welcome › Section 0\n\nBody 0.'`, writes `c0→[1,0]`, `c1→[0,1]`, doc
    `[√½, √½]`, `markAsEmbedded`; (b) `:170` 80 sections (`## Section i\n\n`
    + 40× `'Words about this setting and what it does. '`), NO stored rows:
    the longest call shorter than `huge.length/10`, `updateEmbedding` never
    called, the doc vector written; (c) `:190` `c0 [1,0]` reused, `c1` null,
    `c2 [1,0,0]` stale, every call `[0,1]`: two calls, `c0` NOT rewritten,
    `c2→[0,1]`, doc length 2; (d) `:216` two null chunks, the first rejects
    `'fetch failed'`, the second `[0,1]`: doc `[0,1]`, marked embedded.
13. **`help-doc-size.test.ts` (NEW):** `cl100k_base` over the flat sorted
    `help/*.md`: `trim()` → `parseFrontmatter` → `extractTitle(body,
    'help/'+file)` → `buildHelpDocChunks(body)` → `helpChunkEmbeddingText`
    → token count; asserts > 50 files and no section over
    `HELP_SECTION_EMBEDDING_MAX_TOKENS`; message `help/<file> section <i>
    (<heading|'no heading'>): <n> tokens`. **v5 has no cl100k tokenizer**
    (no tiktoken crate; no `js-tiktoken` in the web/oracle package.json).
14. **`help-doc-sync.test.ts`** (mocks: `create` returns
    `'id-returned-by-a-buffered-write'`, `update` returns `undefined`):
    sync `:125` `create(…, {id})`; `:159` changed doc → `update('existing-
    id', …)`, `clearAllEmbeddingsForDoc`, `deleteByEntity('HELP_DOC',
    'existing-id')`; `:175` chunks keyed to `'existing-id'`; `:187` chunks
    keyed to the `options.id` PASSED to `create`, not the returned id; the
    prune/guard tests `:197-309` carry over. reconcile `:321` an edited page
    → `updated = 1`; `:333` `{total:2, embedded:2}` + a doc vector → 0
    incomplete, no enqueue; `:349` the map holds only `some-other-doc` →
    `sectionsBackfilled = 1`, `replaceForDoc('existing-id', …)`, enqueue
    `{entityType:'HELP_DOC', entityId:'existing-id', profileId:'profile-1'}`;
    `:373` doc vector null → enqueued; `:386` `{total:4, embedded:3}` →
    enqueued, NO re-slice; `:400` no profile → `incomplete: 1`, no enqueue.
    ensure `:420` two concurrent + one later call → ONE `create`; `:430`
    `findAll` rejects once → the first resolves `undefined` with no create,
    the second creates once.

## Tiered deliverables

### Tier 1 — must land

1. **The `help/` tree re-vendored WHOLE from `b0b6656b5`** (`git -C
   ~/source/quilltap-server archive b0b6656b5 help/ | tar -x -C <worktree>`
   over an emptied `help/`; `diff -r` against the pin recorded in the lane
   record; 129 files; md5 of the whole tree in the record); both literals
   `127 → 129` with the arithmetic in the comment; `help_tree_embed_guard`
   + `host_help_docs_boot` green; `help_tree_equivalence` regenerated at the
   target (the proof the tree is v4's); the SPA category table RE-CAPTURED
   through `apps/web/oracle/help-guide-capture.test.tsx` from the target pin
   → `help-guide-tables.json` + `help-categories.ts` (the two slugs land
   after `chat-settings`; `npm test` + `npm run build` green; the capture's
   line pinned per `spa-oracle-recorders-live-outside-src`). ONE commit,
   FIRST — every family below regenerates over this tree.
2. **`average_embeddings(&[Vec<f32>]) -> Option<Vec<f32>>`** in
   `embedding_vector.rs` beside `normalize_vector`: `[]` → `None`; an f32
   accumulator, vectors in order, `sum[i] += v[i]` in f32 (a correctly
   rounded f32 add — assert it against v4's f64-then-store on the corpus);
   the differing-length arm as v4's `Err`/panic-free refusal (the handler
   never reaches it — say which shape you chose); then `normalize_vector`.
   NEW tier-1 family (`average_embeddings_equivalence.rs` ↔ a NEW tsx case
   over v4's REAL `averageEmbeddings` + `normalizeVector`): ≥ 40 rows
   incl. the empty set, one vector, the zero vector (norm 0 → unchanged),
   values that round differently in f32 vs f64 accumulation (e.g. `1e8 + 1
   + 1`), 1536- and 3072-wide random vectors, a subnormal. **Exact f32
   bit-compare**, not 1e-12.
3. **`db/help_doc_chunks.rs::count_by_doc`** — v4's SQL verbatim (the
   quoted identifiers, the `CASE`), the `safeQuery` fallback (an empty map
   + v4's error line) as v5's fallback-`safeQuery` convention (P4.109's
   `get_messages` shape); ONE tier-2 row set in `help-sync-tier2.ts` driving
   v4's REAL `countByDoc` over a fixture holding: a doc with all sections
   embedded, one with some, one with a ZERO-LENGTH BLOB (counts as embedded
   here), one with no rows.
4. **`embed_help_doc_sections` + the rewritten `help_doc_try`** with every
   arm in §Survey 1 in v4's order: the in-memory slices for a doc without
   rows (never persisted), the blank-text silent return, the ONE catch over
   provider + write (the write-failure WARN RETIRED), the width rule with
   `fresh` evaluated after the first pass, the throw-only-when-none-remain
   rule, `guard_skip` dropped for HELP_DOC ONLY (the three other kinds keep
   it — pin one of them unchanged), the empty → `Skipping empty entity` WARN
   + `markAsFailed('Empty input — nothing to embed')` + return, the write
   order, the INFO's five new fields (`chunksEmbedded` GONE), the two
   removed WARNs gone (silence legs). Every new/renamed line
   capture-pinned (`captured_with` / `global_capture`, level + message +
   field names + the `Debug`-vs-JSON rendering convention).
   `embedding_generate_jobs_equivalence` regenerated at the target
   RED-FIRST (`hd-happy` moves: the whole-text call vanishes, the doc vector
   becomes `normalize(e0+e1)`), then GROWN with §Survey 12's four vectors as
   PLANTED rows through the jest oracle (two-null; 80-sections-no-rows;
   reused/null/stale-width; first-fails) — the width re-embed and the
   failed-re-embed-keeps-the-old-row arm included. Count the canned
   provider calls per arm on both sides (the jest oracle writes ZERO
   `llm_logs` rows).
5. **The sync shape** in `help_doc_sync.rs::sync_help_docs`: existing →
   `update(id, fields)` (with `path`) / else `create(fields, id = a minted
   uuid)` (the minted-id remap in the tier-2 diff); `clear_all_embeddings_
   for_doc` then `embedding_status.delete_by_entity("HELP_DOC", id)` on
   update; the DEBUG `Synced help doc` line; `upsert_by_path` + `HdUpsert`
   RETIRED (`db/help_docs.rs`), `help_docs_upsert_tier2_equivalence` + its
   case + builder + spec + fixture DELETED (record the deletion and its
   reason — v4's method no longer exists); `db/help_doc_chunks_repair.rs`
   re-homed. `help_doc_sync_equivalence` regenerated at the target
   red-first (the `embedding_status` row vanishes on an updated doc) and
   GROWN with a planted FAILED `embedding_status` row that the update must
   clear.
6. **`reconcile_help_docs`** replacing the gate + backfill + enqueue-missing
   (all three RETIRED with `find_all_needing_embedding` and
   `help_docs_diverge_from_disk`): the post-sync re-read, `count_by_doc`,
   the section-less slice with the > 0 rule, the incomplete rule (a
   zero-length doc vector counts as MISSING — needs the `HelpDocRow`
   projection, §Survey 8), the INFO with its six fields in order,
   `enqueue_help_doc_embeddings(incomplete)`; **`ensure_help_docs_synced`
   memoized ONCE per boot and NEVER throwing** (WARN `Help doc reconcile
   failed; serving help from the existing index` + the next caller retries)
   — the memo is an OWNED value (a gate the host constructs and threads;
   `help_search`'s lazy call goes through the same gate), NOT a `static`
   in core (`a-process-global-test-seam-must-be-thread-scoped`); say what
   you built. `help_doc_ensure_equivalence` regenerated at the target
   RED-FIRST on the scenarios whose semantics moved (measure which of the
   six; predict `in-sync-chunked` + `added-doc` at least) and GROWN with
   §Survey 14's reconcile vectors as planted arms: an edited page, complete
   (no enqueue), the section-less doc backfilled, the null doc vector, the
   `{4,3}` partial with NO re-slice, no profile, the concurrent/once
   behaviour, `findAll` failing once then succeeding. The module header
   `:29-30` + `:41-54` rewritten to the truth.
7. **The boot order:** the help reconcile awaited BEFORE the dimension
   reconcile (`host.rs` — move the call or split `seed_built_ins`; say
   which), `host_help_docs_boot` grown with an ORDER pin (the help
   reconcile's INFO precedes the dimension reconcile's line in the capture)
   and its second-boot-touches-no-row pin KEPT; the reindex family re-run
   at the target and recorded (neutral predicted unless its fixture updates
   a doc).

### Tier 2 — should land

8. **The help-section-size proof** without a Rust tokenizer: a NEW tsx
   oracle case at the target pin that records, per vendored file and
   section, `{file, index, heading, tokens}` from v4's REAL `js-tiktoken`
   `cl100k_base` + `buildHelpDocChunks` + `helpChunkEmbeddingText`; the
   Rust test re-slices the SAME embedded tree with v5's chunker, asserts the
   section texts match by index, and asserts every recorded `tokens` ≤ 1000
   with v4's failure message shape; `HELP_SECTION_EMBEDDING_MAX_TOKENS =
   1000` as a documented constant in v5's chunking module (its only consumer
   the test, as in v4).
9. **The module's `eprintln!` lines onto `tracing`** at v4's levels with
   capture pins (`:342,:526,:560,:616` and any sibling) — all or none.
10. **`HdUpdate.path`** (v4 passes it on update) — add it, or record why
    the DB-neutral omission stands.

### Tier 3 — explicit deferrals (loud, typed, never silent)

11. A Rust `cl100k` tokenizer dependency — NOT taken (item 8 proves the
    bound through the oracle).
12. The `dogfood-findings.md` #120 note (CONVERGENCE, v4's mechanism) and
    the `docs/v4/developer/bugs/` mirror (`fixed/bug-167-*`, `fixed/bug-
    168-*`, `bugs.md`) — the UNIFIER's (§R.9); pre-list the byte counts in
    the lane record.
13. The three jest cases that mock `ensureHelpDocsSynced` — unchanged; if
    one must move to the real memo, STOP and record.

## The differential requirement, restated

Every unit lands with its equivalence test driving v4's REAL code:
`averageEmbeddings` at tier 1 (a new tsx case, f32 bit-exact); the HELP_DOC
job through the existing jest oracle at tier 3 (canned provider both sides,
tier-2 diff on `help_docs` / `help_doc_chunks` / `embedding_status`);
`countByDoc`, the sync shape and the reconcile at tier 2 through the
existing tsx/jest oracles over planted fixtures; the tree by
`help_tree_equivalence` at the target. v4's three test files mock the
repositories — mirror every vector as a planted real-DB arm, never as the
same mock (§R.6). Recipes: each family's own header, run through the sweep
driver with `--v4 /tmp/qt-v4-pin-p4d222-b0b6656b5`; the jest cases need
the pin's `node_modules` symlinks (§R.3) and `js-tiktoken` is present in
the checkout's `node_modules` only if the human ran `npm install` after
`492771aff` — **measure `ls ~/source/quilltap-server/node_modules/
js-tiktoken` at lane start; absent → item 8 is BLOCKED (record it, ask; do
not `npm install` in the human's checkout).**

## Fixtures

Consumes: `QT_FIXTURE_EG_MAIN`/`_MOUNT` (built by the jest case's own
builder at the pin), `QT_FIXTURE_ENSURE_DIR`, `QT_FIXTURE_HELP_MAIN`,
`QT_FIXTURE_HELP_SYNC_GUARDS_MAIN` — all `/tmp`-built per run under the
lane's directory; the committed `help-guide-tables.json` (RE-CAPTURED —
this lane owns it). Delivers: NOTHING committed under
`crates/quilltap-web/tests/fixtures/`; DELETES `harness/oracle/fixtures/
build-help-docs-upsert-fixture.ts` + `help-docs-upsert-tier2.json` (and any
committed `.db` they name). Plants go on per-run COPIES.

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `b0b6656b5` ten-commit drift catch-up round: **P4.D220 ∥ P4.D221 ∥
P4.D222 ∥ P4.D223 ∥ P4.D224**, ordered 2026-09-24. Five lanes, five
worktrees, five lane branches, **every one cut from `main` (`ca3e245f` or
the commit that lands these orders)** — no keystone, no stacked lane. Two
are SPA-only (P4.D223, P4.D224 — no cargo), three are Rust (P4.D220,
P4.D221, P4.D222; P4.D222 also touches two SPA files). The unifier
cherry-picks in the order **P4.D221 → P4.D222 → P4.D220 → P4.D223 →
P4.D224** (the `lib/` riders and their retired oracle rows first; the help
tree next so every later regen runs over the 129-file tree; the dispatch
lane before the SPA lane that drops the client half of the same verb; the
mention lane LAST because its one pre-declared hunk lands in P4.D223's
file), gates ONCE from the new baseline pin, and `merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`d1c06cd9d`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`b0b6656b5`** ("Fix
  bug 169: narrow-pane chat sidebar no longer closes dialogs it opens
  (#71)", `4.10.0-dev.78`, 2026-09-24) — **TEN non-merge commits past the
  baseline** plus one clean merge (`f51a44548`, no row): `e3937d7aa`
  (P4.D223), `7ebb74143` (NO-PORT, ratified by P4.D221), `3376b3dfa`
  (P4.D224; help half P4.D222; Rust neutrality P4.D221), `944127d9a`
  (server P4.D220 / client P4.D223), `492771aff` (P4.D222), `94e946728`
  (NO-PORT, ratified by P4.D223), `ad1c4c37f` (web edges + core dispatch
  P4.D220 / `lib/` riders + oracle repairs P4.D221), `68da64d9b`
  (P4.D220), `8aafd595d` (NO-PORT mostly, ratified by P4.D221 with its
  item (1) as a small), `b0b6656b5` (CONVERGENCE, P4.D223) — every row
  `ORDERED(…)` in the ledger's §3. v4 `bugfix` tip `1a2b2164c` and
  `release` tip `8fbf2afe0` are unmoved; still no `release: 4.10.0`
  squash. The checkout sat on `main`, CLEAN, at `b0b6656b5` at the
  planning probe (2026-09-24 — the §2 probe PASSED; nothing was
  re-derived). Spec reads: `git show <sha>:<path>` for post-commit files
  and `git show <sha> -- <path>` for the hunks — **never the commit
  message** (§R.4). **At unification the baseline MOVES to `b0b6656b5`**;
  the rows retire `ABSORBED(…)` / `NO-PORT-RATIFIED(…)`.
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log b0b6656b5..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Pass = branch `main`, HEAD `b0b6656b5`, both logs EMPTY, tree
  CLEAN.** Any mismatch — a moved HEAD (a new commit is a NEW drift row,
  not a pass), a non-empty log, ANY dirty path, the checkout off `main` —
  is a STOP: report it and do nothing else. A lane never writes the
  ledger. A lane resumes ONLY on a waiver the human records in the
  ledger's §1 (the `89fcc3c0d` round's mechanism), never on its own reading
  of the dirt.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1), absolutely. HEAD
  is ten commits past the baseline and the commits rewrite the route
  middleware every REST family imports, the help-sync module, and four
  oracle cases' imports. Every regen runs from a LANE-UNIQUE detached
  worktree per ledger §5.1 (`git -C ~/source/quilltap-server worktree add
  --detach /tmp/qt-v4-pin-<order>-<sha> <sha>`, the THREE symlink classes —
  root `node_modules`, `packages/quilltap/node_modules`, every
  `plugins/dist/*/node_modules` — then `cd` into it; never stash or
  checkout-switch the human's repo; `git worktree list` before each batch;
  **verify the pin by `git -C "$PIN" rev-parse HEAD` AND by `ls -ld` of
  the path** — build every pin path from a quoted literal, never an
  unsplit zsh variable; then grep the fresh NDJSON for a marker only one
  tree has; check the file is non-empty before believing a diff). **Every
  lane pins the TARGET `b0b6656b5`** (`/tmp/qt-v4-pin-<order>-b0b6656b5`)
  for every family its port MOVES and for its neutrality re-runs, **and
  the BASELINE `d1c06cd9d`** (`/tmp/qt-v4-pin-<order>-d1c06cd9d`) for the
  pre-port red counts; the SPA recorders (P4.D223 if any, P4.D224's two,
  P4.D222's help-guide capture) run from the TARGET pin. A family that
  moves under TWO lanes (none is predicted — the fences in §R.10 are
  drawn so that each family has ONE owner; a lane that finds one STOPs on
  it and records) is the UNIFIER's to re-run from the new baseline pin
  with every lane's growth present. Pin verification marker (round-wide):
  the target-pinned `help_tree_equivalence` NDJSON names `chat-settings-
  composer` (a file that exists only at the target); the baseline-pinned
  one does not (`grep -c` both). The sweep driver is the sanctioned
  per-family path (`python3 harness/tools/recipe_sweep.py --run <family>
  --v4 "$PIN" --v5w <ABSOLUTE worktree>`); never run two sweeps
  concurrently; a family that reports `SKIP` in 0.00 s did not run.
  Ledger §5.2's silent-stale-pass discipline applies to every regen
  (`rm -f` the output first; read the builder's last line; `grep -c` the
  changed bytes in the fresh NDJSON — MUST be > 0 where a change is
  predicted). Every lane stages its regen outputs under a LANE-PRIVATE
  directory (`/tmp/<order>/…` — several families' committed recipes write
  FIXED `/tmp/oracle-*.ndjson` paths; re-derive into the lane's directory
  before any `cargo test --workspace`) and leaves the committed recipe
  headers canonical (a committed recipe NEVER names a `/tmp` pin — the
  driver's `--v4` is the pin). Every jest oracle writes ZERO `llm_logs`
  rows (`jest.setup.ts` mocks the logging service) — never predict an
  `llm_logs` diff from a jest family; count the canned-LLM calls instead.
  `QT_NODE` in any env block is a REAL path
  (`~/.nvm/versions/node/v24.13.1/bin/node`), never `$N/node`. **The
  checkout's `node_modules` may LACK `js-tiktoken`** (`492771aff` adds it
  as a devDependency; only the human's `npm install` installs it) — a lane
  that needs it MEASURES `ls ~/source/quilltap-server/node_modules/
  js-tiktoken` and records BLOCKED rather than installing anything in the
  human's checkout.
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). Measured
  at planning: (a) `68da64d9b`'s message says it "adds `dispatchAction`" —
  its diff never touches `lib/api/middleware/actions.ts` (PR #69 branched
  before #68); what ships is the chat GET's ladder → the fourteen-key
  thunk map. (b) `ad1c4c37f`'s message says it "migrates every route" —
  `chats/[id]/handlers/get.ts:45` still reads `getActionParam` at that sha;
  it is migrated only by `68da64d9b`. (c) `8aafd595d`'s message says its
  new test "found `groupDocMountLinks` and `groupCharacterMembers` missing
  … now listed" — the two keys arrived in `ad1c4c37f`'s hunk
  (`write-partition.ts:58-59`); `8aafd595d` changes NO key. (d)
  `944127d9a`'s message says the only consumer "was removed" — that
  removal is `e3937d7aa`'s hunk, not this commit's; and its interim
  sentence `Unknown action: X. GET /api/v1/chats takes no actions` is
  SUPERSEDED by `ad1c4c37f`'s envelope four commits later — it never needs
  porting. (e) `492771aff`'s Phase 3.66 calls `ensureHelpDocsSynced`
  (which wraps `reconcileHelpDocs`), not `reconcileHelpDocs` directly; its
  `HELP_SECTION_EMBEDDING_MAX_TOKENS` lives in the production module but
  is consumed only by the new test; and the ledger's "thirteen help files"
  is TWELVE (two added, ten modified). (f) `e3937d7aa`'s message says
  participant-sidebar avatars hide — they do, but ONLY through
  `ui/Avatar`; no hunk touches `ChatSidebar`/`ParticipantCard`. (g)
  `3376b3dfa`'s `LINE_RE` rebuild is byte-identical (measured `.source` +
  `.flags`); the commit adds NO dependency. Re-verify even where an order
  flags the trap — the order's paragraph is the same kind of prose. Carry
  a v5-side code comment naming the sha where the prose misleads.
- **§R.5 — Designed reds and pre-existing reds: measure, don't assume**
  (ledger §5.4). **ONE convergence row** (`b0b6656b5`, P4.D223): v5 never
  pinned v4's OLD sidebar behaviour, only its own, so no pin trips — the
  wording retires and the residual text-node/null-target difference closes
  by taking v4's twin. The DESIGNED reds: **P4.D220** —
  `query_param_semantics_equivalence` at the target on its `fold` claim
  (nearly every non-pinned endpoint), on every `__empty`/`__empty_then_
  known` row, on `chats_collection_get__unknown`, on every hand-rolled
  `__bare`/`__unknown` sentence row, and probably on the classifier
  guard's `handler_rows >= 40` half; `chat_delete_equivalence` +
  `chat_delete_route` on the bare-`?action=`-deletes arm; `salon_reads` on
  the four has-dangerous cases + the sentence check; `chats_collection_
  route`, `help_web_routes`, `help_chats_routes_equivalence`,
  `profile_web_routes`, `messages_swipe_sse_route`, `settings_routes_
  equivalence` on their old-sentence pins. **P4.D221** — `write_partition_
  equivalence` on the two grown group-key rows (RED on v5 at BOTH pins
  until the keys land — the oracle at either pin classifies them
  mount-index? NO: the baseline oracle lacks the keys too, so the rows red
  ONLY at the target; record both); `brahma_console_tier3` at the target on
  the system-prompt bytes; `fts_query` / `characters_read` / `memories_read`
  / `memories_tier2` — their oracle CASES fail to IMPORT at the target
  (a tsx/jest crash, not a differential red — the empty-file trap, §5.1)
  until repointed/retired. **P4.D222** — `help_tree_equivalence` +
  `help_tree_embed_guard` + `host_help_docs_boot` at 127 vs 129;
  `embedding_generate_jobs` on `hd-happy` (the whole-text call vanishes);
  `help_doc_ensure` on the scenarios whose semantics moved; `help_doc_sync`
  on the `embedding_status` clear; `help_docs_upsert_tier2`'s case fails
  to import (retired). **P4.D223 / P4.D224** — no Rust red; their SPA specs
  are red-first by construction. The `/tmp/qt-imggen-*` staging collision
  remains the one recorded standing intermittent in `cargo test
  --workspace` (green by name); the Salon-streaming e2e timing cluster
  (five untouched files, each green alone) and the `salon-regenerate-
  stream-flow` `2/3` watch item are the recorded Playwright intermittents.
  A red in a family a lane did NOT touch is a NEW finding to classify at
  both pins before any source is touched, and it is probably not that
  lane's.
- **§R.6 — The differential requirement stands for every unit.** A ported
  unit lands WITH an equivalence test that drives v4's REAL code at the
  right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM; the SPA's tier-1 recorders for pure client modules),
  red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest` is
  shared) and its silence leg; a fixture change lands with the regen of
  every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that SURVIVES
  as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`). A v4 unit test that
  MOCKS a repository or a handler (every new test in this round's commits
  does) is mirrored as a real-DB / real-route arm with PLANTED rows through
  the existing oracle, never as the same mock (`a-v4-mock-is-not-v4-in-a-
  route-family`). A ruled divergence is pinned in BOTH directions (the
  `EXPECTED_DIVERGENCES` shape, `markdown_transcript_equivalence.rs:40,
  213-233,256-261`): a "divergence VANISHED" failure if v4 == v5, a "WRONG
  SHAPE" failure if the difference is not the ruled one, and an assert
  that every declared divergence was exercised.
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane; `docs/developer/porting/phase-4.
  md`, `dogfood-findings.md` and the `docs/v4/` mirror are the UNIFIER's
  (a lane pre-lists what moves). Cross-order header edits allowed this
  round: NONE (no lane closes another order's open item).
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed on each commit per `.claude/commands/commit.md`; the unifier
  RECOUNTS as base + the sum of every lane's bumps (the silent-auto-merge
  trap: identical bumps merge as ONE — it fires every round). Base at
  planning: core 0.0.1044, harness 0.0.964, host 0.0.154, web 0.0.191, cli
  0.0.27, tauri 0.0.7, fixture-sanitizer 0.0.4, sqlite3mc-sys 0.1.0
  (PINNED, never bumped), SPA 0.5.761. Expected: **core** by P4.D220
  (`chat_delete.rs`, `system_data.rs`, `types.rs`, `salon.rs`,
  `engine.rs`), P4.D221 (`write_partition.rs`, `prompt_text.rs`,
  `fts_query.rs`, the db retirements, `chat_gallery.rs`), P4.D222 (the
  help/embedding modules); **harness** by all three Rust lanes; **web** by
  P4.D220 (its whole surface), P4.D222 ONLY if a web test moves (say so);
  **host** by P4.D222 ONLY (the boot order + `host_help_docs_boot`);
  **cli** by NOBODY (Tier R is a re-run); **tauri**, **fixture-sanitizer**
  by NOBODY; **the SPA** by P4.D222 (the help-categories capture), P4.D223,
  P4.D224. No lane touches the workspace `Cargo.toml` dependency block or
  `apps/web/package.json`'s dependency block; every `Cargo.toml` /
  `package.json` delta must be version-only — the unifier audits each
  before any resolution (the `--theirs` clobber trap).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's: each
  lane pre-lists, in its lane record with byte counts at `b0b6656b5`, the
  mirror paths its rows move. This round's mirror rules in one place
  (measured at planning against `b0b6656b5`): `docs/v4/developer/bugs/
  fixed/bug-167-help-reindex-fk-rollback.md` (NEW, 3,817),
  `fixed/bug-168-help-doc-embed-overflow.md` (NEW, 2,989),
  `fixed/bug-169-narrow-sidebar-closes-portaled-dialog.md` (NEW, 5,048 —
  v5 has NO copy under `bugs/` either), `docs/v4/developer/bugs.md`
  (309,151 → 311,817), `docs/v4/developer/features/complete/concierge-
  list-marks.md` (24,474 → 24,767), `docs/v4/developer/features/composer-
  typeahead.md` (36,009 → 36,537), `docs/v4/developer/DATABASE_
  ABSTRACTION.md` (13,959 → 15,160), `docs/v4/developer/DDL.md` (125,326 →
  125,835 — prose only). `docs/v4/CHANGELOG.md`'s older lag (83,228 vs
  129,336) stays a named housekeeping item, not widened here.
  `README.md`, `CLAUDE.md`, `.claude/commands/release-checklist-4-api-
  standard.md`, `jest.setup.ts`, every `package.json`/`package-lock.json`
  stamp, `scripts/update_version.sh` → NO-PORT, the standing class. **The
  `help/` count MOVES 127 → 129** — P4.D222 re-vendors the WHOLE tree at
  `b0b6656b5` and moves BOTH literals (`help_tree_embed_guard.rs:69`,
  `host_help_docs_boot.rs:98`); `help_tree_equivalence` at the target pin
  is the proof the tree is v4's. The `dogfood-findings.md` #120 note
  (CONVERGENCE — v4 bug 168 fixed it by section-averaging, a mechanism
  #120 never proposed) is the unifier's, on P4.D222's record.
- **§R.10 — Fences, ownership edges and the pre-declared spills.** (a)
  **`api/types.rs` and `api/engine.rs`:** P4.D220 ONLY, and ONLY the two
  `ChatsHasDangerous` REMOVALS (`types.rs:112-119`, `:4184-4185`;
  `engine.rs:909-910`) — nobody ADDS a variant this round (no new verb).
  `dispatch_wrong_type_census` is expected UNMOVED at 449 (a unit variant
  has no typed field) — P4.D220 runs it and records. (b) **`crates/
  quilltap-web/src/**`:** P4.D220's alone, incl. `help_routes.rs`,
  `query.rs`, `lib.rs`; P4.D222 does NOT touch a web route (a help verb's
  REST arm that must move is RECORDED for the unifier). (c) **Core
  services:** `services/help_doc_sync.rs`, `services/embedding_generate_
  job.rs`, `services/embedding_reindex_job.rs` (re-run only), `db/help_
  docs.rs`, `db/help_doc_chunks.rs`, `db/help_doc_chunks_repair.rs`,
  `embedding_vector.rs` → P4.D222; `write_partition.rs`, `services/brahma_
  console/**`, `db/fts_query.rs`, `db/characters.rs`, `db/memories.rs`,
  `db/prompt_templates.rs`, `photos/chat_gallery.rs` → P4.D221; `api/chat_
  delete.rs`, `api/system_data.rs`, `api/characters.rs:1832-1837` (if
  Tier 2 item 10 lands), `api/salon.rs` (the removal) → P4.D220. (d)
  **`crates/quilltap-host/**`:** P4.D222's alone. (e) **`help/**` (the
  vendored tree) + the two count literals + `apps/web/src/app/help/**` +
  `apps/web/oracle/help-guide-capture.test.tsx`:** P4.D222's alone — the
  two SPA lanes never touch them. (f) **`apps/web/**` otherwise:** P4.D223
  owns `quick-hide/**`, `shell/user-menu*`, `ui/avatar.ts`, `chat/message-
  row.ts`, `chat/streaming-message.ts`, `chat/tool-message.ts`, `chat/
  message-content.ts`, `chat/render/**`, `chat/sidebar/**`, `chat/speaker-
  selector.ts`, `chat/speaking-as-avatar.ts`, `chat/all-llm-pause-modal.
  ts`, `chat/reattribute-message-dialog.ts`, `chat/select-llm-profile-
  dialog.ts`, `chat/cast/**`, `chat/impersonation-voice/**`, `chat/inform-
  dialog.ts`, `chat/post-office/**`, `screens/salon/salon-conversation.ts`,
  `core/core-contract.ts` (the `ChatsHasDangerous` removal), `e2e/{quick-
  hide-flow,concierge-marks-flow,scenario-builder-flow}.spec.ts`; P4.D224
  owns `editor/**`, `chat/chat-composer.ts`, `chat/carina-parser*`, NEW
  `chat/mentions/**`, `src/testing/fixtures/{mention-typeahead,carina-
  parser}.ndjson` + their recorders, NEW `e2e/composer-mention-flow.spec.
  ts`. (g) **Oracle cases:** `write-partition.ts`, `gen-brahma-prompts.
  mjs`, `fts-query.ts`, `characters-read.ts`, `memories-read.ts`,
  `memories-tier2.ts` → P4.D221; every help/embedding case → P4.D222;
  `query-param-semantics.test.ts`, `salon-reads.test.ts`, `chat-delete*`
  and every route-family case with an `?action=` probe → P4.D220. (h)
  **Committed fixtures:** NOBODY widens a pair this round; P4.D222 DELETES
  the `help-docs-upsert` builder/spec/fixture; P4.D224 re-records
  `carina-parser.ndjson`; P4.D222 re-captures `help-guide-tables.json`.
  (i) **Pre-declared loud out-of-mandate edits** (the `a-required-field-
  on-a-shared-struct-crosses-lane-ownership` rule: do the edit, keep it to
  the hunk, mark it `// P4.xxx OUT-OF-MANDATE — <owner> preserves`, audit
  with `git diff main -- <path>` at lane close): **P4.D224 → `screens/
  salon/salon-conversation.ts`** — ONE hunk: the `castCharacterIds`
  computed + the `[mentionPriorityCharacterIds]` binding on `<qt-chat-
  composer>` (§S.2); nothing else in that file. If a lane finds it needs
  another lane's file beyond this, it STOPs on that item and records the
  shape for the unifier.
- **§R.11 — Two SPA lanes, one Rust-lane SPA touch.** P4.D223 and
  P4.D224 are SPA-only and run without cargo; P4.D222 touches exactly two
  SPA files (the help-categories capture) and runs the SPA gate for them.
  The SPA's e2e port 4319 is repo-wide — the three lanes that run
  Playwright coordinate through the human (one suite at a time;
  `e2e-port-4319-is-repo-wide`).

## §S — Shared contract (binding; byte-identical in every order of this round)

### §S.1 — The `chatsHasDangerous` verb RETIRES (P4.D220 ↔ P4.D223)

v4 `944127d9a` deleted `GET /api/v1/chats?action=has-dangerous`, and
`e3937d7aa` deleted its only client reader. In lockstep: **P4.D220
deletes** `Request::ChatsHasDangerous` (`crates/quilltap-core/src/api/
types.rs:112-119`, a unit variant), `Response::ChatsHasDangerous(Value)`
(`:4184-4185`), the `engine.rs:909-910` arm, `api::salon::chats_has_
dangerous` (`salon.rs:94-118`), the REST arm (`chats_routes.rs:45-60`) and
every server-side test/oracle row that names it; **P4.D223 deletes**
`ChatsHasDangerousRequest` (`apps/web/src/app/core/core-contract.ts:66-74`)
and its union member (`:2560`), the service probe
(`quick-hide.service.ts:126-134`), the `hasDangerousChats` signal, the
three-way `quickHideFeaturesVisible`, the `CHATS_HAS_DANGEROUS_VERB_LANDED`
constant and every spec that pins them. Neither lane replaces the verb with
anything. After the round a stray `{ type: 'chatsHasDangerous' }` over
dispatch answers whatever the dispatcher answers ANY unknown `type` today
(P4.D220 records that shape in its lane record; no new pin). The two halves
land in different commits; the union is gated once.

### §S.2 — The composer's `mentionPriorityCharacterIds` (P4.D224 → P4.D223's file)

`<qt-chat-composer>` gains an input **`mentionPriorityCharacterIds:
readonly string[]`** (default `[]`), bound in `screens/salon/salon-
conversation.ts` from a computed **`castCharacterIds`** = the chat's
participants with `status !== 'removed'` and a `character?.id`, mapped to
that id — v4's predicate at `SalonView.tsx:245-251` verbatim. The input
NAME, its default and the predicate are binding; P4.D224 lands the hunk
(§R.10(i)); P4.D223 preserves it through its own edits of the same file.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D220** — the action-dispatch consolidation (web edges + core dispatch + the `has-dangerous` server half + the chat GET's fourteen keys) | `crates/quilltap-web/src/**` (`query.rs`, every `*_routes.rs`, `lib.rs`); `crates/quilltap-core/src/api/chat_delete.rs`, `api/system_data.rs`, `api/characters.rs:1832-1837` (Tier 2), `api/salon.rs` + `api/types.rs` + `api/engine.rs` (the `ChatsHasDangerous` REMOVALS only); `crates/quilltap-web/tests/**` (every route family + `query_param_semantics_equivalence.rs` + `chats_collection_route.rs` + `chat_delete_route.rs` + a NEW action-sites census); `crates/quilltap-harness/tests/{chat_delete,salon_reads,help_chats_routes,settings_routes,help_docs_routes,…}_equivalence.rs` (the `?action=` pins); `harness/oracle/cases/{query-param-semantics.test,salon-reads.test,chat-delete*}.ts` + every route-family case with an action probe; this order's header | `write_partition.rs`, `services/brahma_console/**`, `db/**`, `photos/**` (P4.D221); `services/help_doc_sync.rs`, `embedding_generate_job.rs`, `crates/quilltap-host/**`, `help/**` (P4.D222); `apps/web/**` (P4.D223/P4.D224); any variant ADDED to `api/types.rs`; the drift ledger |
| **P4.D221** — the `lib/` riders + the `8aafd595d` small + two NO-PORT ratifications + the neutrality re-runs | `crates/quilltap-core/src/write_partition.rs`; `services/brahma_console/prompt_text.rs`; `db/fts_query.rs` (doc); `db/characters.rs`, `db/memories.rs`, `db/prompt_templates.rs` (the six method retirements/pins ONLY); `photos/chat_gallery.rs` (Tier 2); `harness/oracle/cases/{write-partition,fts-query,characters-read,memories-read,memories-tier2}.ts` + `gen-brahma-prompts.mjs`; `crates/quilltap-harness/tests/{write_partition,fts_query,characters_read,memories_read,memories_tier2,brahma_console_tier3}_equivalence.rs`; `carina_parser*` + Tier R — RE-RUN ONLY; this order's header | `crates/quilltap-web/**`, `api/**` (P4.D220); the help/embedding modules, `crates/quilltap-host/**`, `help/**` (P4.D222); `apps/web/**`; every committed fixture pair; the drift ledger |
| **P4.D222** — bugs 167/168: help docs by section + the startup reconcile + the WHOLE `help/` tree at `b0b6656b5` | `crates/quilltap-core/src/services/help_doc_sync.rs`, `services/embedding_generate_job.rs` (the HELP_DOC branch), `embedding_vector.rs` (`average_embeddings`), `db/help_docs.rs`, `db/help_doc_chunks.rs`, `db/help_doc_chunks_repair.rs`; `crates/quilltap-host/src/host.rs` (the boot order), `crates/quilltap-host/tests/host_help_docs_boot.rs`; `help/**` (re-vendored WHOLE); `crates/quilltap-harness/tests/help_tree_embed_guard.rs` (the literal) + every `help_*`/`embedding_generate_jobs`/`embedding_reindex_*` family + NEW `average_embeddings_equivalence.rs` + NEW help-section-size test; `harness/oracle/cases/{embedding-generate-jobs.test,help-sync-ensure.test,help-tree-sync.test,help-sync-tier2,help-sync-guards,help-doc-chunking}.ts` + NEW cases; DELETES `help-docs-upsert-tier2.ts` + `help_docs_upsert_tier2_equivalence.rs` + `harness/oracle/fixtures/build-help-docs-upsert-fixture.ts` + `help-docs-upsert-tier2.json`; `apps/web/src/app/help/help-categories.ts` + `help/__fixtures__/help-guide-tables.json` (RE-CAPTURED) + `apps/web/oracle/help-guide-capture.test.tsx`; this order's header | `crates/quilltap-web/src/**` (P4.D220); `write_partition.rs`, `db/characters.rs`, `db/memories.rs`, `photos/**` (P4.D221); every other `apps/web/**` file (P4.D223/P4.D224); every committed `crates/quilltap-web/tests/fixtures/` pair; `dogfood-findings.md`; the drift ledger |
| **P4.D223** — the Salon Images quick-hide + the bug-169 convergence + the `has-dangerous` client half + `94e946728` ratified | `apps/web/src/app/quick-hide/**`; `shell/user-menu.ts` + `user-menu.spec.ts`; `ui/avatar.ts`; `chat/message-row.ts`, `chat/streaming-message.ts`, `chat/tool-message.ts`, `chat/message-content.ts`, `chat/render/**`; `chat/sidebar/**`; `chat/speaker-selector.ts`, `chat/speaking-as-avatar.ts`, `chat/all-llm-pause-modal.ts`, `chat/reattribute-message-dialog.ts`, `chat/select-llm-profile-dialog.ts`, `chat/cast/**`, `chat/impersonation-voice/**`, `chat/inform-dialog.ts`, `chat/post-office/**`; NEW `chat/hidden-image/**` (the two stand-ins); `screens/salon/salon-conversation.ts` (PRESERVING P4.D224's §S.2 hunk); `core/core-contract.ts` (the `ChatsHasDangerousRequest` removal ONLY); `e2e/{quick-hide-flow,concierge-marks-flow,scenario-builder-flow}.spec.ts` + any new e2e seed; `apps/web/package.json` (version only); this order's header | `apps/web/src/app/editor/**`, `chat/chat-composer.ts`, `chat/carina-parser*`, `chat/mentions/**` (P4.D224); `apps/web/src/app/help/**`, `apps/web/oracle/help-guide-capture.test.tsx`, `help/**` (P4.D222); every crate; `phase-4.md`; the drift ledger |
| **P4.D224** — the `@` mention typeahead | `apps/web/src/app/editor/**` (the adapter, the menu's label setter, `rich-editor.ts`'s plugin list, NEW `editor/mentions/**`); `chat/chat-composer.ts` (the input); NEW `chat/mentions/mention-typeahead.ts` + its oracle spec; `chat/carina-parser.ts` + `carina-parser.oracle.spec.ts`; `apps/web/src/testing/fixtures/{mention-typeahead,carina-parser}.ndjson` + their recorders (outside `src/`); `editor/char-insert/*.spec.ts` + `char-typeahead-harness.ts` (grown); NEW `e2e/composer-mention-flow.spec.ts`; the ONE §S.2 hunk in `screens/salon/salon-conversation.ts` (§R.10(i)); `apps/web/package.json` (version only); this order's header | every other P4.D223 file; `apps/web/src/app/help/**`, `help/**` (P4.D222); `e2e/composer-char-insert-flow.spec.ts`'s existing beats (grow a NEW file); every crate; the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` / `apps/web/package.json` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix, beyond the pre-declared spill in §R.10(i) — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md`, `phase-4.md`, `dogfood-findings.md`, `docs/v4/**` are never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note). Budget 50–70 GB of disk per Rust lane
(`df -h ~` showed 99 GB free at planning; three Rust worktrees is three
`target/` dirs — **run at most TWO Rust lanes' full gates concurrently**
and reclaim `target/debug/incremental` between waves; the two SPA lanes
cost no cargo disk).

1. **§R.2 probe** — PASS (opening, and again before every regen batch).
2. `cargo fmt --all --check` — clean (Rust lanes).
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in BOTH
   feature sets (default; `--features quilltap-core/native-transport`)
   (Rust lanes).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the TARGET pin, run with `--nocapture`, the log grepped for `SKIP:`
   (ZERO), the fresh NDJSON grepped for the changed bytes (> 0 where the
   order predicts a change; a recorded `0` where it predicts none), and
   the red-first steps recorded with the pre-fix counts at the BASELINE
   pin.
5. **The neutrality legs** the order names, regenerated at BOTH pins and
   compared (`cmp` / by-label diff) where the family is deterministic
   across regens; where it is not (`regenerate-at-both-pins-and-cmp-is-
   not-universal`), compare the normalized rows by case and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean (Rust lanes).
8. `cargo test --workspace --no-fail-fast -- --nocapture` with the lane's
   env block (its own families' vars; every other family's var WITHHELD so
   it SKIPs honestly rather than failing on a deleted `/tmp` path): every
   binary's `test result:` line read, zero failures bar the standing reds
   §R.5 names (recorded by name if their vars are in the block), the
   lane's families confirmed RUN by non-zero duration (Rust lanes;
   P4.D222 ALSO runs the SPA gate for its two files).
9. **Source censuses and guards** the change moves or must not move, re-run
   by name with any constant recounted and the arithmetic in the comment:
   `help_tree_embed_guard` + `host_help_docs_boot` (P4.D222 — 127 → 129;
   P4.D220/P4.D221 — UNMOVED, run to prove it); `dispatch_wrong_type_
   census` (P4.D220 — UNMOVED at 449 predicted, measured);
   `web_edge_body_parse_guard` (P4.D220 — its collapse census re-run);
   `blob_write_sites_census` + `compressed_column_write_sites_census`
   (every Rust lane — UNCHANGED); `get_messages_caller_census` (P4.D221 —
   UNMOVED at (76, 7) unless a retired method was a caller; say so);
   `spelling_guard` (everyone — the project is Quilltap).
10. **Tier R** (`crates/quilltap-cli/tests/cli_differential.rs`) at the
    target pin for P4.D221 (266/0 expected — run it, do not assert it);
    P4.D220 runs it too if any core file it touches is linked by the CLI
    (`chat_delete.rs` is — say what you measured).
11. **The SPA gate** (P4.D223, P4.D224, and P4.D222 for its two files):
    `npm run lint` clean; `npm test` (vitest — NEVER `ng test`, which never
    exits; `--filter` matches TEST NAMES) with the count recorded (base at
    planning: 448 files / 7,670); `npm run build` clean (the ONLY real
    typecheck — `spa-tsc-does-not-typecheck-app-sources`); the `qt-*`
    class guard; Playwright by FILE, one invocation at a time on 4319, for
    every spec the lane touched or added (base at planning: full suite
    328 passed / 7 failed [the Salon-streaming timing cluster, each green
    alone] / 6 skipped [the standing parks]) — the full suite is the
    UNIFIER's.
12. **Commit per `.claude/commands/commit.md`** (one unit per commit where
    the order tiers them; CHANGELOG entry + version bumps per §R.8; the
    status-log lane record appended with the regen commands AS RUN, the
    mutation table, the pre-fix counts, the measurements, the mirror
    byte-count pre-list (§R.9), and every finding outside the lane's
    ownership named for the unifier). The lane closes by updating its own
    order's status header with what landed, what was deferred, and what
    it measured that the order got wrong.

