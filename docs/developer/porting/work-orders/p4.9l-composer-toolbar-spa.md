# Work order P4.9L — the Salon composer formatting toolbar + v4's composer layout (SPA) — v4 `24633026`

**Status:** CLOSED — unified on main (2026-08-14, the help-drift round; lane
`claude/composer-toolbar-spa-porting-5b9810`, records in `status-log.md`).
Tier 1 whole: the toolbar in the Salon composer (v4 button set/order/tooltips
against a NEW v4-side recorder driving v4's REAL FormattingToolbar +
FormattingCommandPlugin in jsdom — `apps/web/oracle/delimiter-transforms.test.ts`,
56 vectors), the delimiter transforms module, v4's composer layout (the
2-column gutter grid; **dogfood #75 CLOSED**, band-aid `937ea82f` retired, the
e2e guard rewritten to v4's geometry), narration delimiters from the Salon's
existing template fetch, five live beats (suite 215 → 219). Tier-2 verdicts:
the source toggle is v4-mounted — PORTED (with the editor kept
mounted-but-hidden); the DocumentPane mount is a NAMED GAP → **`p4.9l2`**
(m6 row 14b) — every component exists, the pane's wiring does not.
**Deliberate divergences, recorded:** v5's send reads the bytes the writer can
see in source mode where v4's send silently discards source edits (v4's own
bug — QUEUED AS A V4-SIDE FILING); the template fetch is the Salon's, not the
toolbar's (one source of truth); Continue keeps its v5-only grid cell. A
pre-existing attach-button title divergence fixed on the way ("Attach file").
💸 owed: the human's wide-pane + narrow-split look on real data (#75's
acceptance check, kept in `dogfood-findings.md`).

## Preamble

- **v4 baseline for this round: `24633026`** (v4 main HEAD at planning,
  2026-08-14). Drift-check at lane start per the two-branch rule; STOP
  and report if HEAD moved or the tree is dirty. This lane's oracle is
  v4's CLIENT code (`impersonation-overlay-spa-gaps`: v4's client is the
  SPA's oracle) — read it at the pin.
- Sibling lanes: **P4.D77** (help-embeddings drift, server), 
  **P4.D65-remainder** (archive coverage arms), **P4.47** (maintenance
  smalls). This is the round's only SPA lane — `apps/web/**` is yours
  alone. Ownership disjoint — see Shared contract.
- Work in your own worktree on a lane branch. ⚠ SPA worktree:
  `npm ci`, never `cp -a` node_modules (`spa-worktree-node-modules`,
  NG0203).

## Mandate

Port v4's composer formatting toolbar (`components/chat/
FormattingToolbar.tsx`, 579 l) into the Salon composer, and with it v4's
real composer layout — the toolbar on its own row and the gutter tools
in the compact 2-column grid beside a dominant editor — retiring the
finding-#75 interim band-aid (`937ea82f`). This closes the standing
`p4.9l` deferral (deferred since the P4.6 era; re-deferred by name at
the 4.8.2 round as "the pickers' composer entrance"; confirmed by a
human walk as finding #86). The lane must NOT touch: any crate
(`crates/**`), the harness, the sweep driver, or the Guide/help
surfaces (unported `p4.9i2`).

## Survey-verified starting points (2026-08-14)

**v4 (the oracle), at `24633026`:**

- `components/chat/FormattingToolbar.tsx` (579 l) — the buttons, in
  order, with byte-contractual tooltips: the markdown format set
  (`handleMarkdownClick`, per-format `title={format.tooltip}`);
  "Outdent list item (Shift+Tab)" / "Indent list item (Tab)"
  (`handleIndentClick`); the code-block toggle (`handleCodeBlockClick`,
  title flips `inCodeBlock ? 'End code block' : 'Insert code block'`);
  the narration-delimiter buttons (`handleDelimiterClick`,
  `getDelimiterTooltip(delimiter)`; `narrationDelimiters?:
  NarrationDelimiters` — array form is `[prefix, suffix]`, string form
  is both; guarded `if (!narrationDelimiters) return`); "Insert emoji
  (or type `:` and a name)"; "Insert a symbol (or type `` \ `` and a
  name)"; the source toggle (`showSource ? 'Switch to rich text editor'
  : 'Edit markdown source'`). Survey the full file for the exact button
  order, icons, separators, and disabled states.
- `components/chat/lexical/plugins/FormattingCommandPlugin.tsx` — the
  editor-side command surface the toolbar drives. v5's counterpart is
  the ProseMirror adapter; commands map, they do not transliterate
  (`p4.d75-char-insert-lane`: the three places the PM adapter must
  differ from Lexical).
- Mount sites: `app/salon/[id]/components/ChatComposer.tsx` (the Salon
  composer — the target) and `components/markdown-editor/
  MarkdownLexicalEditor.tsx` / `app/salon/[id]/components/
  DocumentPane.tsx`. v5's markdown-field already ported the field-level
  toolbar (P4.D75's pickers live there, walked by beat 4 of
  `composer-char-insert-flow.spec.ts`); VERIFY whether v5's Document
  Mode pane already gets a toolbar via qt-markdown-field and record
  NO-PORT for that mount if covered.
- The layout (finding #75's diagnosis, `dogfood-findings.md` row 75): v4
  puts the toolbar on its own row and lays the gutter tools in a compact
  **2-column grid** beside a **dominant** editor. Survey ChatComposer's
  actual grid CSS at the pin — the finding's diagnosis is the map, the
  file is the territory.
- Where `narrationDelimiters` comes from (settings/template — survey the
  ChatComposer call site and port the same source).

**v5 (the target):**

- `apps/web/src/app/chat/chat-composer.ts` + `.spec.ts` — the composer;
  the P4.D75 ProseMirror plugins/popovers/shared panel are the picker
  substrate to REUSE (do not re-implement the pickers; give them the
  composer entrance).
- `apps/web/src/styles/qt-components/_chat.css` — the composer rules;
  the band-aid is `.qt-chat-composer-actions { flex-basis: 100% }`
  (commit `937ea82f`) plus an e2e guard asserting the actions row sits
  below the editor with the input >300px wide. **Retire the band-aid in
  the same change that lands v4's real layout**, and rewrite that guard
  to assert v4's actual geometry (toolbar row + 2-column gutter +
  dominant editor; keep a placeholder-not-clipped assertion — that was
  the user-visible symptom).
- e2e: `composer-char-insert-flow.spec.ts` (the pickers' current beats —
  extend with the composer entrances); add beats for at least one
  format button round-trip to the WIRE (typed bytes are the contract —
  `v4-lexical-export-oracle-probe` for any byte question; remember
  typed ≠ round-trip) and the code-block toggle (interacts with the
  finding-#82 Enter-escape fix — don't regress it).

## Deliverables

### Tier 1 — must land

1. The toolbar component in the Salon composer: every v4 button in v4's
   order with byte-identical tooltips/labels, driving the PM adapter;
   the emoji/symbol buttons open the EXISTING P4.D75 panel (composer
   entrance).
2. v4's composer layout (toolbar row + 2-column gutter + dominant
   editor); the #75 band-aid retired; the e2e layout guard rewritten to
   the real geometry, mutation-proven (re-flattening the gutter reddens
   it).
3. The narration-delimiter buttons wired to the same settings source v4
   reads, with the `getDelimiterTooltip` strings byte-exact; hidden
   exactly when v4 hides them (no `narrationDelimiters`).
4. Parity specs: v4-client-oracle unit specs per button (what command
   fires, what the serialized wire bytes are for a representative
   selection), plus the DOM traps — the toolbar rebuilds must not
   re-fire `mouseenter` on the picker rows
   (`dom-rebuild-refires-mouseenter`), and no root-service signal
   writes from effects (`root-service-signal-write-ng0600`).
5. e2e beats live: a format-button round-trip asserting the wire bytes,
   the composer picker entrances, the code-block toggle + Enter-escape
   still working (#82 non-regression).

### Tier 2 — should land

6. The source-mode toggle in the composer if v4 mounts it there
   (survey; the markdown-field already has source mode — reuse the
   mechanism). If v4's composer has no source toggle, record NO-PORT.
7. The DocumentPane mount verdict (ported already via qt-markdown-field,
   or a gap recorded by name).

### Tier 3 — explicit loud deferrals

- Anything the toolbar exposes that the PM adapter cannot yet do lands
  as a HIDDEN control only if v4 hides it under the same condition;
  otherwise the gap is recorded loudly in the lane record with the v4
  behavior it owes — never a silent dead button.

## The differential requirement, restated

The SPA's oracle is v4's client source at the pin plus the Lexical
export probe for byte questions (`v4-lexical-export-oracle-probe` — jest
swallows console.log; typed ≠ round-trip). Every behavioral claim about
v4 in this order is survey-verified but the BYTES are proven by probe,
not by reading. Unit specs assert against probed bytes; e2e beats
assert the wire. Mutation-prove the load-bearing specs.

## Fixtures

- Consumes: the existing e2e fixture instance and the P4.D75 picker
  datasets (byte-copied corpora — read-only).
- Delivers: no committed fixtures expected. If a beat needs seed data,
  extend the e2e fixture the established way (and remember the
  gated-beat first-run rot class from the 4.8.2 record — run any new
  beat live before declaring it).

## Shared contract (BINDING — identical in spirit across P4.D77, P4.D65-remainder, P4.47, P4.9L)

1. v4 baseline `24633026`; every lane drift-checks at start and STOPS on
   movement/dirt; regen only from a verified-clean `main` checkout or a
   pinned worktree.
2. Ownership is disjoint: **P4.D77** owns `quilltap-core` help/embedding
   modules + `db/mod.rs` + the schema dumps + the boot-ensure site + all
   `help_*`/`embedding_*`/`cold_chunk_*` harness families and their
   recipes/fixtures. **P4.D65-remainder** owns the archive/participant
   families + `character-archive-*`/`system-data-*` fixture mutations.
   **P4.47** owns `harness/tools/recipe_sweep.py`, the google-wire
   corpus asserts, and the settings Zod-collapse file + its
   `settings_routes_equivalence` arms. **P4.9L** owns `apps/web/**` +
   its e2e specs. No two lanes edit the same file. `CHANGELOG` and
   `status-log.md` are append-only for everyone.
3. If P4.47's driver extension flags `help_doc_sync_guards` (it is on
   the candidate list), it records the finding and hands the recipe
   repair to P4.D77/the unifier — it does not edit help recipes.
4. Version bumps: P4.D77 → core/host/harness; P4.D65-rem → harness;
   P4.47 → harness (+web only if the Zod fix lands there); P4.9L → the
   SPA package. Several lanes bump `quilltap-harness` in their own
   trees; the unifier accumulates the UNION and must not silently lose
   one (the 4.8.2 round's three silent version-bump losses).
   `quilltap-sqlite3mc-sys` is never bumped.
5. No lane touches CLAUDE.md's status section; the unifier moves the
   baseline to `24633026` at unification.

## Verification gate (before declaring done)

- `ng test` full (all files), `ng build` clean.
- The FULL Playwright suite green, zero unexplained skips, the new
  beats run LIVE at least once (the gated-beat first-run rot class).
- No `crates/**` changes in the diff.
- Read `e2e-playwright-traps` before ANY e2e diagnosis; never poll with
  `pgrep` loops — background the suite and wait for the notification.
- Per-commit rule: `.claude/commands/commit.md`.
