# Work order P4.55 — the merge-verb silent-keep sweep (the `c8a3cf77` LEAD) + the cleared-null residue

**Status:** CLOSED (2026-08-22) — Tier 1 (A1/A2, B1, E2 + the store_backed
verdict) and Tier 2 (D1/D2/D3 + the stale comment) all landed; Tier 3 stands
deferred as written (B2 and the groups-side cleared-null pin — both recorded
in `phase-4.md` and the lane record in `status-log.md`). Lane branch
`claude/p4-55-merge-verb-silent-f5cfba`, four commits.

## Preamble

- **v4 baseline for this round: `a6870c5a`** (v4 main, HEAD at planning
  2026-08-22). This is a MAINTENANCE lane — it ports no new v4 feature;
  it closes the recorded `c8a3cf77`-round LEAD ("merge-verb
  `unwrap_or(current)` silent-keep vs v4's safeParse-first 400") plus the
  adjacent no-schema-parse and cleared-null residue the survey turned up.
- **Drift-check at lane start** exactly as the sibling orders say; STOP
  and report if v4 moved past `a6870c5a`.
- **Sibling lanes:** P4.D103 (prompts-trio server) owns
  `api/{groups,types}.rs`, `system_prompt.rs`, and the groups harness
  families — the GROUPS half of the no-parse finding (site E1 below) is
  CLOSED BY P4.D103's validator port, not by this lane. P4.D104 owns
  `apps/web/**`. This lane must not touch any file either sibling owns.
- **Worktree:** per the `carryout` skill; edit inside the worktree.

## The lead, resolved to a site table (surveyed 2026-08-22, fresh)

The literal grep class (`unwrap_or(current…)` after a failed parse) is a
CLOSED SET: five hits, all in `crates/quilltap-core/src/api/memories.rs`,
of which three are the already-fixed recall verb (P4.D95 — the template).
v4's mirror (`?? currentSettings.` in `app/api/v1/memories/route.ts`,
three handlers at :752-756 / :792-795 / :828-831, all `safeParse` FIRST)
confirms **two unfixed divergences** plus a wider adjacent ring:

| # | v5 site | verb | v5 today | v4 | verdict |
|---|---|---|---|---|---|
| A1 | `api/memories.rs:975` (pick :990-994) | `memoryHousekeepingConfigSet` | NO validation — garbage values (`"yes"`, `-5`, `null`) are merged and WRITTEN, 200; non-object → 400 `"Invalid housekeeping config body"` | `route.ts:737-739`, schema :107-113 — safeParse-first **400 `{"error":"Validation error","details":[…]}`**, writes nothing | **divergent — worse than recall was: v5 persists garbage** |
| A2 | `api/memories.rs:1111` (pick :1119-1123) | `memoryExtractionLimitsSet` | identical shape to A1 | `route.ts:785-787`, schema :115-120 | **divergent** |
| A3 | `api/memories.rs:1049` | `memoryRecallConfigSet` | validate-first 400, writes nothing | :821-823 | already-faithful (P4.D95) — **the template for A1/A2** |
| B1 | `api/autonomous_rooms.rs:430` (`parse_settings_patch`; verb :378) | `chatAutonomousRoomUpdateSettings` | self-documented leniency (:426-428 — "a present-but-wrong-typed field is dropped"): wrong-typed/out-of-range caps, bogus `runVisibility`, >300-char `title` all silently dropped → 200; only invalid cron 400s | `chats/[id]/autonomous-room/route.ts:178-184`, schema :60-71 — `updateSettingsSchema.parse` in try / ZodError → **400 Validation error**; `.int().positive()` caps, 3-member enum, title max 300, cron max 120 | **divergent — the largest surface (10 fields, 8 Zod-guarded)** |
| B2 | `api/types.rs:1895` + `engine.rs:3929-3934` | `dataRetentionSettingsUpdate` | web-edge state-collapse: `#[serde(default)] Option<Value>` maps a present `null` to absent → `{}` → keep-current 200 (the handler itself 400s every non-null invalid) | `settings/data-retention/route.ts:35-41`; Zod `.default()` fires only for `undefined`, explicit `null` → **400** | **divergent (null arm only) — DEFERRED this round: `api/types.rs` is P4.D103's file. See Tier 3.** |
| D1 | `api/settings.rs:1604-1608` | `connectionProfileUpdate` (`apiKeyId`) | `if null … else if as_str …` — **no else**: a present non-string `apiKeyId` (`5`, `[]`, `{}`) silently dropped, 200 | falls into the lookup → `findApiKeyById(5)` misses → **404 "API key not found"** (likely — measure) | needs measurement |
| D2 | `api/image_profiles.rs:445-472` | `imageProfileUpdate` (`apiKeyId`, `baseUrl`) | same missing else; `baseUrl` non-string collapsed to null where v4 stores a truthy non-string verbatim | `image-profiles/[id]/route.ts:110-124` | needs measurement |
| D3 | `api/embedding_profiles.rs:491-503` | `embeddingProfileUpdate` (`apiKeyId`) | same missing else (the dims clears were §3-fixed; **stale comment at :669 contradicts the fixed code**) | 404 on missing key | needs measurement |
| E1 | `api/groups.rs:241` | `groupUpdate` | zero validation, raw patch passthrough | `updateGroupSchema.parse` → 400, unknown keys stripped | **CLOSED BY P4.D103** (its validator port) — hands off |
| E2 | `api/projects.rs:304` | `projectUpdate` | zero validation, raw patch passthrough | `updateProjectSchema.parse` → 400 Validation error, unknown keys stripped | **divergent — this lane's** (resolve the exact v4 schema at lane start; the survey did not fully chase the export) |

**The P4.D85 cleared-null residue** (v4's `base.repository._update`
answers an IN-MEMORY merge `validate({...existing, ...updateData})`,
never a re-read — quoted at `settings.rs:1841-1846`; a v5 handler that
echoes a DB re-read loses keys the PUT cleared to null):

- **Highest-value candidate:** `db/store_backed.rs:209-221` — the generic
  store-backed `update` does `update_slim` THEN `find_by_id_raw` and
  overlays: a re-read echo. It reaches exactly the two `StoreEntity`
  impls: groups and projects. `slim_row_to_map` (:235-243) DOES emit NULL
  slim columns as `Value::Null`, so the risk is confined to the overlaid
  document/properties keys. **Measure on the PROJECTS side** (this lane's
  file): a `{"description": null}` clear arm in the projects-routes
  family settles it. If the echo diverges, fix in `store_backed.rs`
  (unowned by the siblings) — the groups side inherits the fix by
  construction; a groups-side pin rides the NEXT round (P4.D103 owns the
  groups families this round).
- Already-faithful (verified): `image_profiles.rs:400` (in-memory merged
  echo with explicit nulls), `embedding_profiles.rs:420` (§3-fixed,
  pinned), `characters.rs:913` `merge_update_echo` (built for this),
  `api_key_update` (no nullable clearable columns).

## Mandate

This lane delivers: A1 + A2 rebuilt on the A3 template
(validate-FIRST with v4's exact 400 body, writes-nothing proven); B1's
`parse_settings_patch` made fallible with v4's full Zod-shape refusals;
E2's `updateProjectSchema` ported whole; the D1–D3 measurements with
fixes where v4 diverges (the shared missing-`else` sub-family) and the
stale `:669` comment corrected; the store_backed cleared-null
measurement on the projects side with a fix if divergent. Every fix
ships with red-first invalid-payload oracle arms — the corpus blindness
IS the reason this class survived. It must NOT touch `api/groups.rs`,
`api/types.rs`, `system_prompt.rs` or any P4.D103 surface, nor
`apps/web/**`.

## Survey-verified starting points

All file:line references in the site table above are fresh (2026-08-22).
Additional coverage facts:

- **A1/A2 oracle arms today**: `memories-config.test.ts:372-380` (one
  valid housekeeping arm) and `:490-499` (one valid limits arm) — ZERO
  invalid arms. **A3's arm set is the template to copy**
  (`recall_config_set_invalid_enum`, `recall_config_set_invalid_bool`,
  `recall_config_invalid_writes_nothing` with `storedAfter`, at
  :381-489). The oracle's `runCase` shaper already forwards `storedAfter`
  (:276) — the record-shaper trap is already sprung. Seed housekeeping
  via `repos.chatSettings.updateForUser`; seed limits via
  `set/getMemoryExtractionLimits` from `@/lib/instance-settings`. Mind
  the 750 ms settle note at :283-285. The A1 row's fixture-vintage
  ruling was retired at P4.52 (`memories_routes_equivalence.rs:757-759`)
  — both sides compare plainly.
- **A1/A2 non-object-body messages** (`memories.rs:977`/`:1113`,
  "Invalid housekeeping config body"/"Invalid extraction limits body")
  become v4's uniform `"Validation error"` body — v4's root-level
  `invalid_type` goes through the same `validationError`.
- **B1 arms today**: only `update_invalid_cron`
  (`autonomous-rooms-routes.test.ts:353`) — a service-layer 400, blind to
  the class. Needed: `{budgetMaxTurns:"many"}`, `{budgetMaxTurns:-5}`,
  `{budgetMaxTurns:2.5}`, `{runVisibility:"bogus"}`,
  `{title:'x'.repeat(400)}`, plus a writes-nothing composite over a
  seeded room that already has stored caps (the keep-current arm is then
  measurable). The fix changes `parse_settings_patch`'s return type from
  infallible to `Result` across 10 fields — port v4's schema constraint
  by constraint (`updateSettingsSchema`, route file :60-71).
- **D1–D3**: measure v4 first (what does v4's repo do with `apiKeyId: 5`?
  the survey predicts the lookup-miss 404). Then fix the three
  missing-`else` sites together if confirmed, each with a
  `{apiKeyId: 5}` / `{apiKeyId: {}}` arm (settings-routes,
  image-profiles-routes, embedding-profiles-routes families), plus the
  non-string `baseUrl` arm for D2. If v4 turns out equally lenient at
  any site, record the equivalence and add the pinning arm anyway.
- **E2**: resolve v4's `updateProjectSchema` (export path not fully
  chased — start from the projects `[id]` PUT handler), port it whole
  (constraints + unknown-key strip + the 400 body), arms in
  projects-routes: one invalid-type, one over-max if the schema has one,
  one unknown-key strip probe, one `{"description": null}` cleared-null
  echo arm (double duty with the store_backed question).

## Tiered deliverables

### Tier 1 (must land)

1. A1 + A2 validate-first on the A3 template, v4's 400 body, six new
   oracle arms (two invalid + one `storedAfter` writes-nothing composite
   per verb), mutation-proven red-first.
2. B1: fallible `parse_settings_patch` with v4's constraint set; the
   five invalid arms + the writes-nothing composite; the :426-428
   leniency comment replaced by the ported semantics.
3. E2: `updateProjectSchema` ported whole; the four projects-routes arms
   incl. the cleared-null echo arm; the store_backed verdict recorded
   (fixed if divergent).

### Tier 2 (should land)

1. D1–D3 measured against v4, fixed together if divergent, pinned by new
   arms either way; the stale `embedding_profiles.rs:669` comment
   corrected.

### Tier 3 (explicit deferrals — loud)

- **B2 (data-retention present-null)**: CONFIRMED divergent but its fix
  lives in `api/types.rs` (P4.D103's file this round) plus an
  `engine.rs` three-arm match, AND its harness leg currently bypasses
  the wire (`settings_routes_equivalence.rs` calls the handler with the
  raw body — the null arm would pass green against a broken wire until
  the `dataRetention` edge mapping mirrors taboo/brahma's, plus a
  `seedDataRetention` + the `after` refetch map entry). Record it as a
  named next-round item in the lane record and the phase-4 candidates:
  the fix is the known `double_option` pattern + the edge-mapping
  harness rewire.
- The groups-side cleared-null pin (rides the next round if store_backed
  moved — see above).
- E1 is P4.D103's; verify at unification that its validator port landed,
  and say so in the lane record.

## The differential requirement, restated

Every fix ships red-first: add the oracle arm, watch v5 fail with its
current lenient behavior, then land the fix. Oracles drive v4's REAL
routes (jest real-DB pattern — [[jest-real-db-oracle]],
[[jest-oracle-mechanism]]); regen through
`harness/tools/recipe_sweep.py`, pinned at `a6870c5a` if v4 HEAD moved
([[oracle-regen-pinned-v4-worktree]]). Families: memories
(`memories-config.test.ts` / `memories_routes_equivalence.rs`),
autonomous-rooms (`autonomous-rooms-routes.test.ts` /
`autonomous_rooms_routes_equivalence.rs` — note its oracle's jest
child-fork history, fixed in P4.40), projects
(`projects-routes.test.ts` / `projects_routes_equivalence.rs`),
settings-routes / image-profiles-routes / embedding-profiles-routes for
D1–D3. Writes-nothing composites compare `storedAfter`, not just the
status ([[dogfood-verify-through-persisted-state]] is the same lesson
one layer down).

## Fixtures

- No committed fixture changes expected: the memories pair
  (`memories-{main,mount}.db`, P4.52 vintage) already carries what A1/A2
  need; autonomous-rooms and projects families build /tmp fixtures via
  their builders. If an arm needs a seeded stored value, seed through
  the oracle case (repo calls), not the committed pair. If a committed
  fixture must widen after all, follow the committed-fixture rule and
  flag it loudly.

## Ownership (BINDING across the round)

- **P4.55 (this lane)** owns:
  `crates/quilltap-core/src/api/{memories,autonomous_rooms,settings,image_profiles,embedding_profiles,projects}.rs`,
  `crates/quilltap-core/src/db/store_backed.rs`, and the oracle+harness
  files of the memories / autonomous-rooms / settings-routes /
  image-profiles-routes / embedding-profiles-routes / projects families.
  Version bumps: **quilltap-core, quilltap-harness** (patch each — the
  unifier recounts bumps as base + total, per the playbook).
- **P4.D103** owns `api/{groups,types}.rs`, `system_prompt.rs` and its
  module set, and the groups families. **P4.D104** owns `apps/web/**`.
- `CHANGELOG` and `status-log.md` append-only for everyone.

## Verification gate (before declaring done)

1. `cargo fmt`; `cargo clippy` both feature sets, `-D warnings`.
2. `cargo test --workspace` with the lane's oracle env block (full
   output, background + notification — never a poll loop).
3. The lane's families BY NAME, `--nocapture`, zero SKIP, oracles fresh
   at `a6870c5a` (pinned worktree if v4 moved), every new arm grepped
   present in the NDJSON.
4. `cargo build --release`.
5. Per-commit rule: `.claude/commands/commit.md`.
6. Lane record appended to `status-log.md`; status header here updated.
