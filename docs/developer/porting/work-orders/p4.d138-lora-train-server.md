# Work order P4.D138 — the LoRA train, server half (v4 `84f33ce94` → `648d5c8aa` → `2ece98c90`)

**Status:** OPEN.

## Preamble

- **v4 oracle baseline: `7fb668263`** (drift-ledger §1, probe re-run 2026-09-01).
  **This lane's targets are the THREE stacked LoRA-train commits** —
  `84f33ce94` (LoRA adapters + per-model image options + the five-call-site
  params consolidation), `648d5c8aa` (bugs 110/111 — the discarded
  `lora_preset` and the unlogged image request), `2ece98c90` (the HuggingFace
  LoRA lookup). Port them **in commit order, D-stacked inside this one lane**
  (one branch, per-commit differential attribution — do not blend the bug-110
  fix into the commit-1 port; land commit 1's state first, then move it).
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 checkout on
  `main`, tree clean, `git log 4622411fd..main` empty and
  `git log 3a76b17df..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule in force: PIN REQUIRED** (ledger §1). Every oracle/fixture
  regen runs from a lane-unique detached worktree pinned at the train's tip
  **`2ece98c90`**: `/tmp/qt-v4-pin-p4d138-2ece98c90`, all three symlink
  classes per ledger §5.1 (the third class matters here — the NanoGPT plugin
  oracle dies without `plugins/dist/*/node_modules`). Verify the pin:
  `lib/image-gen/huggingface-lookup.ts` must exist in it. Families this lane
  does not move regen at the baseline `7fb668263` if needed.
- **Docs ratification rider:** `e41fcb12e` (docs(help): the tame-picture
  troubleshooting) is this lane's NO-PORT ratification. Evidence already
  measured 2026-09-01: `docs/CHANGELOG.md` + `help/dangerous-content.md` +
  `help/image-generation-profiles.md`, +34 lines, zero
  lib/app/packages/plugins hunks. Re-verify with `git show --stat e41fcb12e`,
  bank the two help hunks to the `p4.9i2` list, and record the ratification
  in the lane record.
- **Sibling lanes:** P4.D139 (SPA half — §A/§B contracts), P4.D140 (bug 112),
  P4.D141 (Concierge four-state — §E region split on two files this lane
  owns), P4.D142 (qt-range), P4.66 (optimistic bubble). §Ownership below is
  binding and identical across all six orders.
- **Worktree:** per the `carryout` skill; edit inside the worktree, not main.
  `CHANGELOG` and `status-log.md` append-only. **No Playwright** (P4.66 owns
  the port-4319 slot).

## Mandate

Port the whole LoRA-train server side: the model-matcher + LoRA-support +
LoRA-validation + params-builder modules into `quilltap-core`, the five
call-site consolidation (generate_image tool + both reroutes, character
avatar, story background, the images route, the wardrobe preview-avatar),
the NanoGPT wire dialects with bug 110's family-first `applyLoras` and bug
111's error-level request log, the four image-profiles route additions
(`loras` 400 guards, `list-models` loraSupport, the NEW `options-schema` and
`lora-metadata` actions), and the HuggingFace lookup module behind the wire.
Must NOT touch: the dangerous-content predicate lines in
`tools/generate_image.rs` (:1337 region) and `story_background_job.rs`
(:365 region) — those are P4.D141's per §E; nothing in P4.D140/D142/P4.66
files; no SPA files (P4.D139's).

## Survey-verified starting points (2026-09-01, from the shipped hunks — ledger §5.3)

All v4 anchors are post-state at `4622411fd` unless marked as hunks.

1. **`lib/plugins/model-matchers.ts` (NEW, 51 lines, pure, zero imports).**
   `modelMatchesPattern(model, pattern)`: empty pattern never matches → exact
   id → `*` glob (split on `*`, regex-escape each literal part
   `/[.*+?^${}()|[\]\\]/g`, join `.*`, anchor `^…$`) → plain prefix
   (`model.startsWith(pattern)`, comment "a family entry covers the SKUs
   beneath it"). `fieldAppliesToModel(list, model)`: absent/empty list →
   true; **unknown model → true** (carry the "resolves toward showing"
   doc). v4's test pins regex-metachar escaping (`gpt-image-125` must NOT
   match `gpt-image-1.5*`). → a NEW v5 module (suggested
   `quilltap-core/src/image_gen` submodule or `model_matchers.rs`); note
   P4.D139 transcribes a client TS twin — keep names identical.
2. **`lib/image-gen/lora-support.ts` (NEW, 212).** `DEFAULT_LORA_SCALE
   {0,2,1,0.05}`; `resolveLoraSupport(provider, model)` = per-model
   `matchModel(...)?.loraSupport` then provider
   `getImageProviderConstraints(provider)?.loraSupport ?? null` (mirrors
   `resolveOrientation` exactly — "one matcher, two capabilities");
   `readLorasFromParameters` (read-side re-check: non-array warn `[Image
   LoRA] Ignoring a \`loras\` parameter that is not a list`; per-entry drops
   incl. `'(entry with no source)'`; scale kept only finite 0..10 else
   `Dropping an out-of-range scale; the provider default applies`; batched
   `Dropped malformed entries from a profile's stored LoRA list`);
   `capLoras` (null support → `Stripping LoRAs — this provider/model
   declares no LoRA support`; over-cap keeps the LEADING max entries +
   `Capping the LoRA list to the model's limit` naming kept AND dropped
   sources); `loraTriggerPhrases` (case-insensitive dedupe, order
   preserved, blanks skipped) + `joinLoraTriggerPhrases` (`', '`).
   **v5 seam gap:** `image_gen.rs:211-222` `fn match_model` is PRIVATE and
   `ModelInfo` (:118-121) has no `lora_support` field — promote the
   visibility, add `lora_support: Option<ImageLoraSupport>`, and add a
   `lora_data_for`-style table beside `orientation_data_for`
   (`image_gen_data.rs:113-156`; NANOGPT is `(Vec::new(), Some(...))` at
   :153 today — the static image models grow LoRA entries, see point 6).
3. **`lib/image-gen/lora-validation.ts` (NEW, 42).** `validateProfileLoras`:
   non-object/array parameters → `null` ("the caller's own 'parameters must
   be an object' check owns this" — v5's `api/image_profiles.rs:301` guard
   stays FIRST); absent `loras` → null; else Zod
   `z.array(ImageLoraSpecSchema)`. **The 400 body is the Zod envelope
   `{ error: 'Validation error', details: issues }` — never a bespoke
   sentence** (the survey's loud flag). Issue strings: `'LoRA source is
   required'`, `'LoRA scale must be a finite number'`, `'LoRA scale cannot
   be negative'`, `'LoRA scale cannot exceed 10'`. **No cap check on the
   write path — over-cap is kept by ABSENCE of a guard; do not add one
   "for symmetry"** (pinned by v4's e2e narrowing case keeping both rows).
4. **`lib/image-gen/params-builder.ts` (NEW, 298).**
   `HOST_OWNED_PARAMETER_KEYS` exact set/order: `prompt, negativePrompt,
   model, size, aspectRatio, orientation, quality, style, n,
   responseFormat, seed, guidanceScale, steps, loras` — **`hf_api_token` and
   `lora_preset` deliberately absent** (they ride the residual bag; scoping
   is a PLUGIN-side attach decision inside `applyLoras`, never a host-side
   withhold — a host filter breaks both keys). Merge semantics key-for-key:
   `firstNonEmptyString` reproduces `a || b`; model ladder ends
   `?? 'dall-e-3'`; `n: overrides.n ?? asNumber(defaults.n) ?? 1`; absent
   fields OMITTED never `undefined`; seed/guidanceScale/steps via `??
   asNumber`. Orientation only when requested; size/aspectRatio OVERWRITE
   the merge ("the caller asked for a shape, not for a string");
   promptHint appended. LoRA trigger-phrase append: lowercase-`includes`
   test, append only phrases the prompt lacks (carry the "rather than
   thread a flag through five call sites" comment). Residual bag omitted
   when empty. Final `[Image Params] Built image generation parameters`
   debug line — key NAMES only for the bag. Also `resolveProfileLoras`
   (support+loras+joined trigger phrase).
5. **The five consolidated call sites → v5 twins.** v4 deleted the tool's
   local `mergeParameters`+`applyOrientation` → v5 deletes
   `tools/generate_image.rs:391-421` + `:428-445` and routes `:2205-2216`
   (main) + `:2291-2303` (reroute) through the builder; the crafter seam
   `style_trigger_phrase: None` at `:846` gains v4's step-5c
   `resolveProfileLoras` fold (`[Image Generation] LoRA trigger phrases
   routed into prompt expansion`; "resolved against the profile that will
   actually generate — the reroute above may have swapped it";
   `image_scene_tasks.rs:79/:150` already declare/consume the field).
   Avatar + story-background jobs: v5's shared
   `image_job_common.rs:154-183` (`build_job_gen_params` hard-codes the
   drift; `quality_from_parameters` IS the "reads only quality" bug) is
   replaced by the builder — those paths GAIN
   negativePrompt/seed/guidanceScale/steps/loras/bag; the module doc :1-7
   becomes false and must be rewritten. Images route: v4's
   `handleGenerateImage` gains all profile defaults, **no orientation**
   ("this route's caller passes an explicit size and means it"). Wardrobe
   preview: v5 `api/wardrobe.rs:914/:928/:1040-1058` +
   `quilltap-host/src/avatar_preview.rs:77-94` (the literal
   `'1024x1792'` at :87) → builder with `overrides {n:1, style:'natural'},
   orientation:'portrait'` — carry v4's "only OpenAI ever accepted"
   comment; rewrite avatar_preview.rs's :60-76 doc. v4's nine logContext
   strings are the tracing anchors (`'tools.generate_image'`,
   `'…concierge-reroute'`, `'…style-options'`,
   `'background-jobs.character-avatar'` ×2, `'background-jobs.
   story-background'` ×2, `'api.v1.images.generate'`,
   `'api.v1.wardrobe.preview-avatar'`).
6. **NanoGPT dialects (v5 reimplements natively —
   `model/image_dialects.rs:455-496` today writes no LoRA keys).** The
   static family table (ten rows, all `sourceKinds: ['url','hf-repo']`,
   `INDEXED_SCALE {0,4,1,0.1}`): indexed = `flux-2-dev-lora`(4),
   `flux-2-klein-4b`(3), `flux-2-klein-9b`(3),
   `wavespeed-ai/flux-2-klein-base-4b`(3), `…-9b`(3),
   `z-image-turbo-lora`(3), `wavespeed-ai/krea-v2/turbo-lora`(3);
   weights = `pruna-ai/p-image/text-to-image-lora` (1, scale
   {0,4,0.5,0.05}, token) and `…/edit-lora` (1, {0,4,1,0.05}, token);
   url = `flux-lora` (1, {0.1,4,1,0.1} — "fal's lora_strength floor is
   0.1, not 0"). `matchLoraFamily` = longest-prefix-first. Carry the
   "why static" doc verbatim (the catalog's `lora` tag with empty
   `allowed_passthrough_parameters`). Allow-lists:
   `NANOGPT_PASSTHROUGH_KEYS = [num_inference_steps, guidance_scale,
   steps, strength]`; `NANOGPT_LORA_SCOPED_KEYS = [hf_api_token,
   lora_preset]` + the scoping doc verbatim.
   **Bug 110 (port at the stacked second commit):** `applyLoras` resolves
   the family FIRST; unknown family with adapters → warn `LoRA family
   unknown for this model; dropping the adapters rather than guessing a
   dialect` and report `dialect: null`; preset applies whenever the family
   is `url` (moved OUTSIDE the kept-adapters guard), token only alongside
   kept weights — **port the two asymmetry comments** (:283-289
   credential-gated-on-weights, :305-309 preset-stands-alone) so it is not
   consistency-fixed back; a known family reports its dialect even with
   zero keys. **Capping happens TWICE by design** (host `capLoras` + plugin
   `applyLoras`, different log sentences — `the model's` vs
   `this model's`) — do not collapse them; the plugin capper is the
   unknown-family safety net. Live-catalog widening: a `lora`-tagged model
   outside the table gets `{ maxLoras: 1, sourceKinds: ['url','hf-repo'] }`
   and no dialect. **Bug 111:** wrap the NanoGPT generate call; on error
   log at ERROR `NanoGPT image request failed` with `{context:
   'NanoGPTImageProvider.generateImage', model, size, n, loraDialect,
   loraKeys, loraDropped, passthroughKeys, error}` then rethrow unchanged
   — key NAMES only (mirrors the pre-existing debug line field-for-field);
   a log-only fix is differential-invisible → capturing-layer pin
   (thread-scoped seam, per the standing memory notes).
   **`ImageGenParams` (`model/image.rs:27-42`) gains `loras` +
   `profile_parameters` in v4 field order — `to_key_value()` (:48-80) is
   the canned-key serializer; wrong order shifts the harness keys.**
7. **Routes (`api/image_profiles.rs`).** Create: LoRA guard immediately
   after `"Parameters must be an object"` (:301), BEFORE the apiKeyId
   check (:307); warn `[Image Profiles v1] Rejected a profile with a
   malformed LoRA list` with joined `path: message` issues (log field, not
   response field). Update (:403-548): same guard inside the
   `parameters !== undefined` arm, warn `…Rejected an update with a
   malformed LoRA list`. `list-models` (:713-785): `loraSupport` map
   inserted between `source` (:779) and `fetchError` (:781); absent-not-
   empty for no-support models; debug `Resolved LoRA support for the model
   list`. NEW `options-schema` GET arm: `Provider is required` /
   `Provider ${provider} is not available` 400s; plugin hook throw → warn
   + null schema; response `{provider, model: model ?? null, optionsSchema,
   loraSupport}` with **null, not a zero-cap object**. NEW `lora-metadata`
   POST arm per §A (exact guard sentences there). `validate-key` stays a
   loud refusal (:697-699).
8. **HuggingFace lookup (commit 3).** `huggingface-repo-id.ts`
   (pure): `REPO_ID_PATTERN`, URL form via hostname
   `/(^|\.)huggingface\.co$/i` (rejects `nothuggingface.co`), first two
   path segments (deep-link `/resolve/main/…` reason), bare-string form.
   `huggingface-lookup.ts`: 10 s timeout; failure taxonomy
   `not-a-repo-id | missing-or-private (401 AND 403 — the fused-401 doc)
   | not-found | rate-limited | timeout | network | http`; facts
   derivation (`base_model:adapter:` tags, card-first `baseModels` dedupe,
   `isLora` tag, `.safetensors` siblings, `gated` string-or-false,
   trigger phrase from `cardData.instance_prompt` string-or-first-of-
   array); **renders NO compatibility verdict** (carry the doc; v4's test
   asserts the facts object has none of
   compatible/compatibility/verdict/works/supported); token → `Bearer`
   header, logged ONLY as `hasToken`; the five logger lines' exact
   sentences are in the survey/hunks. **This is the repo's second mocked
   non-LLM external HTTP provider — `mock-serper.ts` (P4.42) is the
   precedent for the v5-side test double**; v4's 327-line jest file's
   fetch-mock payloads (REALISM_LORA, shou_xin trigger, list-valued
   base_model, gated 'auto', two-safetensors, 401/404-with-token/429,
   token-wiring URL+header asserts, token-never-logged, no-fetch-on-bad-
   source, TimeoutError, non-JSON) are the corpus shapes.
9. **Schema files.** `lib/schemas/profile.types.ts` gains
   `ImageLoraSpecSchema` + `IMAGE_PROFILE_LORAS_KEY = 'loras'` (transcribe
   into the v5 route-guard); `public/schemas/qtap-export.schema.json`'s
   `parameters` hunk is DESCRIPTION-ONLY (no constraint) — v5 has no
   schema-JSON twin (P4.D141's survey confirms), so record no-counterpart
   against the standing `qtap-export.schema.json` flag. **No DDL change
   anywhere in the train — no D23 re-dump.**

## Tiered deliverables

- **Tier 1 (must land):** points 1–7 above — the four core modules, the
  five-call-site consolidation (all five paths byte-shaped through one
  builder), the NanoGPT dialect family with bugs 110+111 in their stacked
  order, the routes' guards + `list-models` loraSupport + `options-schema`,
  `ImageGenParams` widening, and every differential in the section below.
- **Tier 2 (should land):** point 8 — the HuggingFace module + the
  `lora-metadata` wire (it is commit 3's whole point; drop to Tier 3 only
  if the mocked-provider harness proves larger than budgeted, and then the
  route arm must land as a LOUD typed refusal, `not_available`-style, with
  §A honoured on the refusal shape).
- **Tier 3 (explicit refusal-arm deferrals):** none planned. The
  `e41fcb12e` ratification + `p4.9i2` help bank is a docs rider, not a
  deferral. v4 records its own owed live proof ("Phase 6 dogfood proofs
  against a live NanoGPT key remain outstanding") — bank the same 💸 item
  (a real NanoGPT LoRA request + the bug-111 error log on a real refusal)
  to the dogfood queue in the lane record.

## The differential requirement (restated)

Every ported unit ships with an equivalence test driving v4's REAL code
from the pin. Closest recipes:

- **Tier-1 pure:** model-matchers, lora-support (resolve/cap/read/phrases),
  repo-id extraction, params-builder — a jest/tsx oracle importing the real
  `lib/` modules (v4's own test tables in
  `lib/plugins/__tests__/model-matchers.test.ts`,
  `lib/image-gen/__tests__/params-builder.test.ts` are the corpus shapes;
  drive the REAL functions, not the tables). The
  `thinking-prefill`/`fallback-engine` pure-family recipes are the shape.
- **Routes tier-2:** `image_profiles_routes_equivalence`
  (`crates/quilltap-harness/tests/image_profiles_routes_equivalence.rs`,
  recipe header :1-12) grows the guard arms (create/update malformed-loras
  Zod envelope, options-schema for a schema'd + schema-less provider,
  list-models loraSupport, lora-metadata's four guard sentences + a mocked
  success/failure pair) — red-first where v5 measurably lacks the arm.
  `image_profiles_tier2_equivalence` (recipe :18-29): ⚠ its header :12-16
  constrains the `parameters` corpus to single-key objects for key-order
  reasons — a `loras`-bearing multi-key case trips that documented
  deferral; extend deliberately (preserve_order is on in v5's column) and
  record what you did about the constraint.
- **Wire bytes:** the NanoGPT image request corpus (the P4.D100/D107
  recorded-vector families) grows the three dialect shapes + bug-110's
  preset-without-adapters + unknown-family-writes-nothing rows — every
  pre-existing row must stay byte-identical.
- **Bug 111:** capturing-layer pin (the tracing seam must be
  thread-scoped; the target assert is a prefix match — standing notes).
- **Mutation proofs:** per the standing rule — including one mutation
  reverting the preset outside-the-guard move (must redden the bug-110
  row) and one widening a capper away (must redden the cap row).
- The consolidation is the `a-differential-cannot-see-a-dropped-batch`
  class in reverse: the four paths GAIN fields — grep the regenerated
  NDJSONs for the changed bytes (`negativePrompt`/`seed`/`loras` on an
  avatar-job row) per ledger §5.2.

## Fixtures

- Consumes: `crates/quilltap-web/tests/fixtures/` image-profile fixtures +
  `harness/oracle/fixtures/groups-projects.json` (routes family), the
  image-profiles tier-2 fixture builder
  (`harness/oracle/fixtures/build-image-profiles-fixture.ts`).
- Delivers: widened routes/tier-2 corpora (loras cases), the NanoGPT wire
  corpus rows, the new pure-family NDJSON recipes (runnable headers per
  `recipe-header-conventions`), and the HuggingFace mock module
  (`mock-hf.ts` beside `mock-serper.ts`) if Tier 2 lands.
- Any committed fixture this lane regenerates: this lane re-runs every
  sibling family that reads it (sweep driver by name, zero SKIP).

## Shared contracts (binding, identical across all six orders)

**§A — the LoRA wire (P4.D138 ↔ P4.D139).**
- `GET /api/v1/image-profiles?action=list-models` response gains
  `loraSupport: Record<modelId, ImageLoraSupport>` between `source` and the
  conditional `fetchError`; models resolving no support are ABSENT from the
  map (the editor's "offer no LoRA rows" signal).
- NEW `GET /api/v1/image-profiles?action=options-schema&provider=…[&model=…]`
  → `{ provider, model: model ?? null, optionsSchema: ProviderOptionsSchema
  | null, loraSupport: ImageLoraSupport | null }` — `null`, never a zero-cap
  object. 400s: `Provider is required`; `` Provider ${provider} is not
  available ``.
- NEW `POST /api/v1/image-profiles?action=lora-metadata`, body
  `{ source: string, hfToken?: string }` → HTTP **200** carrying the
  `HuggingFaceLookupResult` verbatim (`{ok:true,facts}` |
  `{ok:false,reason,repoId,url,detail?}`; reasons `not-a-repo-id |
  missing-or-private | not-found | rate-limited | timeout | network |
  http`). Guards in order, exact sentences: bad JSON or non-object body →
  400 ``A JSON body with a `source` is required``; blank/non-string source
  → 400 `A LoRA source is required`; non-string hfToken → 400
  `hfToken must be a string when supplied`. POST-not-GET because hfToken is
  a credential (carry the comment).
- The stored `loras` entry: `{ source: string (trimmed, min 1), scale?:
  number (finite, 0..10), triggerPhrase?: string, label?: string }`.
  Write-side rejection is the Zod envelope
  `{ error: 'Validation error', details: issues }`, HTTP 400 — never a
  bespoke sentence. No server-side cap.
- `ImageLoraSupport = { maxLoras: number, scale?: {min,max,default,step?},
  sourceKinds: ('url'|'hf-repo'|'provider-id')[],
  supportsPrivateWeightsToken?: boolean }`;
  `DEFAULT_LORA_SCALE = {min:0,max:2,default:1,step:0.05}`.

**§B — `qt-range` (P4.D139 ↔ P4.D142).** D142 defines `.qt-range`
(`_interactive.css`) + `--qt-range-accent`/`--qt-range-focus-ring`
(`_variables.css`). D139's new LoRA editor slider carries the literal
`class="qt-range w-full"` (v4 `LoraListEditor.tsx:145`) and ships no slider
CSS of its own. Landing order is irrelevant: the class is inert-until-both,
never broken, and the check-qt-classes guard does not police bare component
names.

**§C — the salon danger-styling flip (P4.D141 ↔ P4.66 → the unifier).** v4
`SalonView.tsx:1496` switched the message-list danger prop to
`shouldShowDangerStyling(chat)`; the v5 twin line is
`salon-conversation.ts:311`, a file P4.66 owns. Neither lane edits the
other's file: P4.D141 ships the client predicate module
`apps/web/src/app/chat/concierge-state.ts` (exports `getConciergeState`,
`shouldUseUncensoredRoute`, `shouldShowDangerStyling`, `isClassifierOnDuty`
— transcribed 1:1 from v4 `chat-override.ts` with a parity spec), and the
ONE-LINE binding flip at `salon-conversation.ts:311` is a NAMED UNIFICATION
WIRE (PENDING_CROSS_LANE, P4.D122 precedent) applied by the unifier with a
spec pin. Both P4.D141 and P4.66 record it loudly in their lane records.

**§D — `core-contract.ts` region scoping.** Three lanes touch it in
disjoint regions: P4.D139 adds the image-profile/LoRA/options-schema DTOs
(a new block, §A names verbatim), P4.D140 adds `createdAt` to the
`RecentChat` DTO, P4.D141 widens the two `conciergeOverride` unions
(:2574, :2891) to `'OFF' | 'UNCENSORED' | null`. No lane touches another's
region; the unifier diffs the contract name-for-name (playbook §D).

**§E — server same-file region split (P4.D138 ↔ P4.D141).**
`tools/generate_image.rs` and `services/story_background_job.rs` belong to
P4.D138 EXCEPT the dangerous-content predicate flips — one import line plus
one call each (`generate_image.rs:1337`, `story_background_job.rs:365`) —
which P4.D141 owns and must edit to keep its branch compiling. The unifier
cherry-picks **P4.D138 before P4.D141** and hand-resolves the import-block
unions; neither lane touches the other's regions in those two files.

## Ownership (binding, identical across all six orders)

| Area | Owner |
|---|---|
| `quilltap-core`: `image_gen.rs`, `image_gen_data.rs`, `model/image.rs`, `model/image_dialects.rs`, `api/image_profiles.rs`, `db/image_profiles.rs`, `services/image_job_common.rs`, `services/character_avatar_job.rs`, `services/image_scene_tasks.rs`, `api/wardrobe.rs` (preview region), the NEW lora/params/HF modules; `quilltap-host`: `avatar_preview.rs`; `tools/generate_image.rs` + `services/story_background_job.rs` per §E | **P4.D138** |
| SPA: `screens/settings/images/**` (modal, form, api, card, picker + specs), `screens/settings/providers/{provider-options-panel.ts,provider-options-schema.ts}` (+ specs), NEW LoRA editor/query components, `core-contract.ts` §D region | **P4.D139** |
| `quilltap-core`: NEW `chat_activity` module, `db/chats_messages.rs`, `db/chats_messages_read.rs`, `services/chat_enrichment.rs`, `services/home.rs`, `api/characters.rs`, `api/projects.rs`, `api/brahma.rs`, `tools/self_inventory.rs`, `services/backup/restore/orchestrator.rs`, NEW recompute-heal module (+ `db/mod.rs` line); `quilltap-host`: `host.rs` (the boot repair chain block); SPA: `screens/salon/chat-card.ts`, `chat/merge-conversation-modal.ts`, `screens/home/recent-chat-item.ts`, `screens/characters/view/tabs/character-conversation-card.ts`, `brahma/brahma-wire.ts`, `e2e/support/seed-archived-character.ts`, `core-contract.ts` §D region (+ the touched specs) | **P4.D140** |
| `quilltap-core`: `services/dangerous_content/**`, `services/concierge_notifications.rs`, `services/context_summary.rs`, `services/danger_scan.rs`, `services/orchestrator.rs`, `services/pre_compute.rs`, `services/title_update_job.rs`, `services/memory_extraction_job.rs`, `services/carina_memory_extraction.rs`, `pascal/llm_consult.rs`, `api/salon.rs` (the conciergeState arm), the §E flip lines in `tools/generate_image.rs` + `services/story_background_job.rs`; SPA: `chat/conversation-header.ts`, `chat/sidebar/chat-section.ts`, NEW `chat/concierge-state.ts`, `styles/qt-components/_chat.css`, `core-contract.ts` §D region (+ specs) | **P4.D141** |
| SPA: `styles/qt-components/{_interactive.css,_variables.css,_surfaces.css}`, the class-string edits in the eight slider host files (`chat/sidebar/participant-card.ts`, `memory/housekeeping-dialog.ts`, `memory/memory-editor.ts`, `screens/settings/chat/context-compression-settings.ts`, `screens/settings/chat/dangerous-content-settings.ts`, `screens/settings/memory/memory-dedup-card.ts`, `screens/settings/providers/profile-modal.ts`, `screens/settings/system/tasks-queue-card.ts`), `apps/web/scripts/check-qt-classes.mjs` (the host-class guard extension) + its spec/self-test | **P4.D142** |
| SPA: `screens/salon/salon-conversation.ts` (+ spec), `chat/message-list.ts` (+ spec), the NEW mid-turn e2e beat | **P4.66** |
| Playwright (port 4319) | **P4.66's branch only**; every other lane's beats are authored gated (ACTIVATE-AT-UNIFY) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it |

**Version bumps:** this lane bumps `quilltap-core`, `quilltap-harness`,
`quilltap-host`, and `quilltap-web` (if its route tests move) per commit;
the unifier recounts at unification (the silent-auto-merge trap is on
record).

## Verification gate before declaring done

`cargo fmt --check`; `cargo clippy` both feature sets; full
`cargo test --workspace` with the lane's oracle env vars
(`run_in_background`, full log + sentinel — NEVER a pgrep loop, NEVER
`| tail`); the lane's families by name through the sweep driver, zero
SKIP, from the `2ece98c90` pin with changed-bytes greps (ledger §5.2);
mutation proofs applied-and-verified; NO Playwright (P4.66 owns the port);
no SPA edits, so no `npm test`. Per-commit rule:
`.claude/commands/commit.md`.
