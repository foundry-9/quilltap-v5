# Work order P4.D67 — the `ed8934f1` (Bug 56) base-path-availability drift catch-up — v4 `ed8934f1`

**Status:** COMPLETE — tier 1 and tier 2 both landed; the lane is closed
pending unification (2026-08-11, branch
`claude/p4-basepath-availability-drift-d1eccb`).

**Landed:**

1. `services/mount_index/base_path_availability.rs` — the whole v4 module
   (check / assert / error type / byte-exact `explain()` sentences /
   docker-OR-lima containerized detection over the existing `DataDirEnv`
   snapshot). `verify_base_path` DELETED; the assert placed between the
   traversal guard and the recursive create, v4's comment carried.
2. Both route arms: the folder-create **409** carrying
   `BasePathUnavailableError.message` (via a typed `CreateFolderError` +
   `FolderCreateRefusal`, replacing the `msg.contains("escapes")` sniff and
   the `__NOT_FOUND__` string sentinel), and the store-create warning
   rewrite — which retires v5's blanket-inaccessible seam, so **a store on
   a real directory now creates with no `warning` key at all**.
3. Planted-fs differential arms in both families, all mutation-proven:
   `mount_points_routes_equivalence` 15 → **19 cases** (missing /
   available / not-a-directory / denied, plus an oracle-rows-vs-driven-cases
   set-equality assertion) and `mount_ops_equivalence` 39 → **42 cases**
   (three folder-create refusals over a planted MP_FS basePath).
4. Both families regenerated FRESH at `ed8934f1`. **The `d553f72a`
   mount-points regen pin is retired** and this lane clears the drift debt;
   the baseline moves to `ed8934f1` at unification.
5. Tier 2: the Docker bind-mount section in `docs/developer/running.md`.

**Two corrections to the order, both recorded in the status log:**

- The `denied` arm cannot be planted with `chmod 000` on the base path.
  `stat`/`fs.stat` take EACCES from the **parent** directory's search
  permission, so a 000 directory stats fine and reads as *available*. Both
  sides plant an unreadable PARENT instead (verified against Node's
  `fs.stat` before either side was written).
- `api/mount_files.rs` needed no new 409 machinery, but it did need the
  refusal to survive the `db.write` closure with its `reason` /
  `containerized` intact — hence the typed refusal enum rather than a
  string.

**Deferred loudly (tier 3, unchanged):** the `quilltap docs docker-mounts`
subcommand + the `start:docker` bind planner, banked by name with the
standing `quilltap docs` CLI deferral (P4.6y) and stated as such in
`running.md` so an operator meeting the 409 is not left hunting for a tool
that does not exist; `help/mount-points.md` + `help/cli-docs.md` → the
`p4.9i2` bank.

**One cosmetic leftover for the unifier, outside this lane's ownership:**
`apps/web/src/app/screens/scriptorium/scriptorium.spec.ts:122` still names
the deleted `verifyBasePath` in a test title. The spec stubs its own
warning string, so nothing about it is stale except the name.

## Preamble

- **v4 baseline for this round: `ed8934f1`** (v4 4.8.0-dev, HEAD at planning
  2026-08-11, tree CLEAN — verified at planning). This lane absorbs the ONE
  commit past the round-1 pin `d553f72a`: "feat(docker): pass filesystem
  document stores through to the container (bug 56)". **This lane clears the
  drift debt; the oracle baseline moves to `ed8934f1` at unification** and
  the mount-points regen pin retires.
- **Drift-check at lane start.** In `~/source/quilltap-server`, run
  `git log ed8934f1..HEAD --oneline`. If v4 moved, **STOP** and report.
- **Sibling lanes this round (disjoint ownership):**
  - **P4.D65** — the archive service + verbs + re-encrypt wire (server).
  - **P4.D66** — the CLI `db characters` family.
  Neither touches mount-index; this lane touches nothing of theirs.
- **Worktree:** carry the lane out on its own branch per the `carryout`
  skill.

## The drift, classified (surveyed FRESH 2026-08-11 at `ed8934f1`)

Port-relevant (this lane):

| v4 file | what changed |
|---|---|
| `lib/mount-index/base-path-availability.ts` (NEW, 154 ln) | The single reachability answer for filesystem-backed stores. `checkBasePathAvailability(basePath)` — **never throws**: `fs.stat` → not-a-directory / ENOENT→`missing` / EACCES\|EPERM→`denied`; result `{available: true}` or `{available: false, reason, containerized, message}`. `assertBasePathAvailable` throws `BasePathUnavailableError` (carries `basePath`, `reason`, `containerized`; `message` = the diagnosis). `isContainerized()` = `isDockerEnvironment() \|\| isLimaEnvironment()` (from `@/lib/paths` — "Lima counts: it is a VM with the same property"). `explain()` sentences are byte-contractual (Shared contract rule 6): denied → `` `The path '${basePath}' exists but cannot be read. Check its permissions${containerized ? ', and note that the container runs as a non-root user' : ''}.` ``; not-a-directory → `` `The path '${basePath}' is not a directory.` ``; missing+containerized → the four-sentence bind-mount/`--recreate` diagnosis; missing → `` `The path '${basePath}' does not exist.` `` |
| `lib/mount-index/scanner.ts` | `createFilesystemFolder` (`:511` region) now runs `await assertBasePathAvailable(basePath)` **before** the recursive mkdir (the mkdir stays), with the load-bearing comment — carry it: without the check, an absent base sends `mkdir -p` walking up to the topmost missing ancestor (observed as `EACCES mkdir '/Users'`), or worse, SUCCEEDS into a fabricated tree. **`verifyBasePath` is DELETED** (replaced by the new module) |
| `app/api/v1/mount-points/[id]/folders/route.ts` | The folder-create arm catches `BasePathUnavailableError` → **409** `errorResponse(folderErr.message, 409)` with a `logger.warn` carrying `{mountPointId, basePath, reason, containerized, userId}` — "a configuration problem rather than a bad request or a server fault. Hand back the diagnosis — it names the remedy, which a bare 500 never could." |
| `app/api/v1/mount-points/route.ts` | Store create replaces `verifyBasePath` with `checkBasePathAvailability`; the warning becomes `` `${availability.message} The store was created, but scanning will fail until the path is reachable.` `` (the old "not currently accessible" sentence is GONE), logged with `{reason, containerized}` |

NOT ported (dispositioned):

- `packages/quilltap/lib/docker-mounts.js` (201 ln) + the `start:docker`
  bind-mount planner + `quilltap docs docker-mounts` — v4 dev-runner/CLI
  packaging. The `quilltap docs` CLI family is a standing loud deferral
  (P4.6y); this subcommand **banks with it, recorded by name** in this
  order's status header at close. See tier 2 for the v5 Docker doc rider.
- `help/mount-points.md` + `help/cli-docs.md` → the `p4.9i2` bank.
- `docs/developer/bugs*`, README, versions, completion templates → v4
  housekeeping, NO-PORT.

## Mandate

Port the base-path-availability module and its three consumer arms into the
v5 mount-index surface, byte-faithful on every operator-facing sentence, and
regenerate the mount families at `ed8934f1`. Touch ONLY
`crates/quilltap-core/src/services/mount_index/**`, `api/mount_points.rs`,
`api/mount_files.rs`, and the harness mount families. Do NOT touch
`api/engine.rs` / `api/types.rs` (no new verbs — both arms change existing
handlers' behavior), the archive lanes' files, or `apps/web/**`.

## Survey-verified starting points — v5 side (2026-08-11)

- `crates/quilltap-core/src/services/mount_index/scanner.rs:434` —
  `verify_base_path` (the deletion target).
- `crates/quilltap-core/src/services/mount_index/folder_ops.rs` — the
  filesystem folder-create analog (find the `create_filesystem_folder`
  path; the assert goes before its recursive create, comment carried).
- `crates/quilltap-core/src/api/mount_points.rs:274` — the OLD warning
  sentence ("Base path '{}' is not currently accessible…") to replace.
- `crates/quilltap-core/src/api/mount_files.rs` — the folder handlers' arm
  (`:145` region maps `FileOpError`/`DatabaseStoreError` → coded); the new
  409 mapping for the unavailable-base error goes here. v5's 409 machinery
  exists (`ErrorKind::Conflict` since P4.d7) — reuse, don't invent.
- Container detection: v4's `isDockerEnvironment`/`isLimaEnvironment` live
  in `lib/paths` — survey them and find v5's analog (P4.D46 landed a
  container-aware `QUILLTAP_TIMEZONE`/`TZ` resolver; check whether a
  detection helper already exists in core/host before writing one). Port
  the detection faithfully (Docker AND Lima).
- Differential families to extend + regenerate at `ed8934f1`:
  `mount_points_routes_equivalence` (the create-warning arm) and
  `mount_ops_equivalence` (the folder-create 409 arm). Both currently pass
  against `d553f72a`-vintage oracles — the regen at `ed8934f1` is itself
  part of clearing the pin.

## Deliverables

**Tier 1 (must land):**
1. `services/mount_index/base_path_availability.rs` (new): the check + the
   assert + the error type + the byte-exact `explain()` sentences + the
   containerized detection. `verify_base_path` deleted; the folder-create
   assert placed before the recursive create with v4's comment carried.
2. The two route arms: folder-create 409 (message = the diagnosis) and the
   store-create warning rewrite.
3. Differential arms with **planted fs conditions**: missing path;
   permission-denied (`chmod 000` a real dir — restore in cleanup);
   file-where-a-directory-should-be; and the available happy path. Both
   sides (oracle and Rust) point at the same planted `/tmp` trees; the
   committed mount fixtures are not rebuilt. ⚠ `containerized` is `false`
   in both test environments — the container-variant sentences are pinned
   by UNIT tests on the message builder, not the differential; say so in
   the case header.
4. Regen of the two mount families fresh at `ed8934f1` (fresh-marker
   check), retiring the `d553f72a` mount-points pin.

**Tier 2 (should land):** a short section in `docs/developer/running.md`
noting that v5's Docker mode has the same bind-mount property (filesystem/
Obsidian stores must be bound into the container at creation; the 409
diagnosis is the runtime symptom) — v5-side documentation, not a port of
v4's planner.

**Tier 3 (explicit loud deferrals, recorded in the status header at
close):** the `quilltap docs docker-mounts` subcommand + the start-script
bind planner (banks with the standing `quilltap docs` CLI deferral); the
two help docs (the `p4.9i2` bank).

## The differential requirement, restated

The oracle drives v4's REAL routes/scanner (the mount-points route
family's existing jest/tsx recipe — see the case headers of
`mount_points_routes_equivalence.rs` / `mount_ops_equivalence.rs` and the
`harness-recipes-are-runnable` memory). Planted-condition cases must be
deterministic and self-cleaning (restore permissions even on failure —
a leaked `chmod 000` dir breaks later suites). First-run-green arms must be
mutation-proven (the D24 rule): break the sentence, watch it go red.

## Fixtures

- The committed mount fixture families are CONSUMED, not rebuilt; planted
  fs conditions live in `/tmp` trees built by the case (recipe in the case
  header). This lane owns regenerating the two named families' oracles at
  `ed8934f1`.

## Shared contract (BINDING — identical in P4.D65 / P4.D66 / P4.D67)

Names are law; v4 shapes at `ed8934f1`.

1. **Verb success shapes** (pinned in round 1, restated): archive →
   `{ archived: bool, archiveFileId: string|null, pruneComplete: bool }`;
   rehydrate → `{ rehydrated: bool, archived: bool, archiveBundleFileId:
   string|null, restored?: { memories, documents, blobs }, warnings:
   string[] }`. REST: `POST /api/v1/characters/{id}?action=archive|rehydrate`.
   The passphrase-unavailable 400 sentence: `The archive cannot be sealed:
   your passphrase has not been entered since Quilltap started. Unlock once
   (or restart and unlock), then archive again.` **Owner: P4.D65.**
2. The change-passphrase response gains `archives: { total, reencrypted,
   failures: [{fileId, filename, reason}] }`; `total: -1` = the sweep itself
   failed; a failed sweep does NOT fail the passphrase change. Passphrase-
   mismatch reason string: `does not open with the old passphrase — it
   predates an earlier passphrase change`. **Owner: P4.D65.**
3. The CLI consumes the REST verbs of rule 1 over HTTP and treats bundle
   bytes as the frozen `QTAPARC1` layout. `services/character_archive/
   crypto.rs` is FROZEN this round — neither lane edits it; P4.D66 links it
   read-only for offline decrypt. **Owner: P4.D65 serves; P4.D66 consumes.**
4. Three-key export carry on character records (`archivedAt`,
   `archiveFileId`, `archivedAvatarFileId`): absent column → absent key;
   NULL cell → present `null`. A bundle IS an export. **Owner: P4.D65.**
5. Files-delete guard envelope: `code: "ARCHIVE_BUNDLE_HELD"` +
   `characterId` + the sentence in P4.D65's survey; `force=true` bypasses.
   **Owner: P4.D65.**
6. Bug-56 sentences are byte-contractual: the three `explain()` messages
   (missing/denied/not-a-directory, with the containerized variants), the
   folder-create 409 carrying `BasePathUnavailableError.message`, and the
   store-create warning `` `${availability.message} The store was created,
   but scanning will fail until the path is reachable.` `` **Owner: P4.D67.**
7. `api/engine.rs` and `api/types.rs` are **P4.D65-only** this round.
8. Fixture ownership: the new `character-archive-*` family + the
   `system-data-*` mutation + the four archived-arm extensions = P4.D65;
   the mount families = P4.D67; the Tier R corpus = P4.D66. Committed
   fixtures extend by mutation, never rebuild.
9. SPA gate flip: **the unifier** flips `CHARACTER_ARCHIVE_SERVER_LANDED`
   in `apps/web/e2e/character-archive-flow.spec.ts` and runs the four
   action beats live. No lane touches `apps/web/**`.
10. Round baseline `ed8934f1`; every round oracle regenerates there;
    drift-check at lane start and STOP on further drift. The baseline moves
    at unification and P4.D67 clears the drift debt.

## Ownership (identical across the three orders)

| Lane | owns (exclusive) | version bumps |
|---|---|---|
| P4.D65 | `crates/quilltap-core/src/services/character_archive/**` (new `service.rs`; `mod.rs`; the reencrypt wire), `api/engine.rs`, `api/types.rs`, `api/characters.rs`, `api/files.rs`, `services/qtap_export/**`, `services/quilltap_import/**`, the quilltap-web change-passphrase edge, the harness archive/system/export families + their fixtures | core, harness, web |
| P4.D66 | `crates/quilltap-cli/**` (src + `tests/cli_differential.rs` + its corpus) | cli |
| P4.D67 | `crates/quilltap-core/src/services/mount_index/**`, `api/mount_points.rs`, `api/mount_files.rs`, the harness mount families | core, harness |

`docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` are
append-only for everyone (union-merge at unification). P4.D65 and P4.D67
both bump `quilltap-core` — the unifier RECOUNTS as base + total bumps
(`parallel-round-playbook` §C).

## Verification gate (before declaring done)

1. `cargo fmt --check`; `cargo clippy` both feature sets; release build.
2. `cargo test --workspace` with this lane's oracle env vars (fail-fast
   per binary).
3. The two mount families BY NAME with `--nocapture`, zero SKIPs, over
   oracles regenerated fresh at `ed8934f1` (fresh-marker check —
   `oracle-regen-silent-stale-pass`).
4. No `apps/web` touch → no SPA gate owed by this lane.
5. Per-commit rule: follow `.claude/commands/commit.md`.
