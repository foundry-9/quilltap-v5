# Work order P4.D152 — `files.sha256` names the bytes actually stored (bug 117) + the `realign-file-entry-sha256-v1` boot heal — v4 `0b0617fee`

**Status:** CLOSED — UNIFIED on main at the `0b0617fee` drift catch-up round
unification (2026-09-03; lane branch `claude/p4-files-sha256-bytes-bug-4be488`).
Tier 1 whole: leg (a) — the chat upload runs the bridge's own transcode FIRST and
records the bridge's `sha256` (+ v4's disagree warn as `resolve_stored_sha256`,
capture-pinned both arms), the codec now a PARAMETER with production passing
`NotConfiguredPixelCodec` byte-for-byte as before; leg (b) import; leg (c) the
restore replay arm + the carried arm reading the archived blob's own hash (JS
truthiness on an empty/NULL hash) inside the 2026-07-26 ruling; leg (d) the
`realign-file-entry-sha256-v1` boot heal in the P4.D140 ledger shape over the
shared `parse_mount_blob_storage_key` with a RECORDED, both-directions-pinned
divergence (v4's `shouldRun()` is presence-not-drift, so v4 stamps a
zero-`itemsAffected` row where v5 stamps nothing); the NEW `files_sha256_
realign_heal_equivalence` over v4's REAL migration + REAL `recordCompletedMigration`
(eight scenarios); the within-tree BOOLEAN comparand on files-routes and
qtap-import with the harness-only `PrefixingPixelCodec` — and the honest
measurement that the join half is non-discriminating by construction, the DEDUP
being the red-first arm; the NEW committed `restore-archive-bug117.zip` +
`qtap-import-bug117.qtap`. Tier 2 whole. Tier 3 (loud): the `PRETTY_LABELS`
sentence (carried in the module header), `DDL.md` prose → the mirror, the
export-schema `description` → the standing flag. **NAMED CANDIDATE, not done:**
threading the HOST codec into chat uploads (a NEW convergence — v4 transcodes
there; v5's own upload rows never had the symptom). **Fixed at the §3
unification review:** the host boot comment that claimed v4 parity where the
lane had pinned a divergence; the heal's blob lookup folded EVERY DB error into
the "orphaned" bucket (v4 only treats a missing row as an orphan and lets a
driver throw escape) — now `QueryReturnedNoRows` → orphan, anything else
propagates; a doc comment spliced onto the wrong harness function; the module
header now says the boot line is conditional on a realigning pass. 💸 the heal on
the Friday copy (measure the population FIRST, ledger §5.5). Lane record:
`status-log.md` → "P4.D152".

## Preamble

- **v4 oracle baseline: `6d2a50382`** (drift-ledger §1, probe re-run at
  ordering 2026-09-02: v4 on `main` at `0b0617fee`, tree clean, both logs
  empty). **This lane's target commit is `0b0617fee`** — **the bug-117 legs
  only**: `lib/chat-files-v2.ts` (+69/−?), `lib/import/quilltap-import/
  import-files.ts` (+7), `lib/backup/restore/restore.ts` (+33), the NEW
  `migrations/scripts/realign-file-entry-sha256.ts` (278) + its
  registration in `migrations/scripts/index.ts` + the `PRETTY_LABELS` line
  in `lib/startup/prettify.ts`, the doc-only rewrite in `repair-files-
  mime-and-size-from-mount-blob.ts`, the `qtap-export.schema.json`
  `description` (no constraint), `docs/developer/DDL.md` prose. Two v4
  tests: `chat-files-v2-stored-sha256.test.ts` (189) and
  `realign-file-entry-sha256.test.ts` (252). Bugs 116/118 are
  **P4.D151's**. Read the hunks: `git -C ~/source/quilltap-server show
  0b0617fee -- lib/chat-files-v2.ts lib/import lib/backup migrations
  lib/startup/prettify.ts`. **No `generateDDL`, no new column, no D23
  re-dump** — the commit's claim, CONFIRMED by the file list at the drift
  check.
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 on `main`,
  tree clean, `git log 0b0617fee..main` empty and `git log
  3a76b17df..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED** (ledger §1). Every regen runs from a
  lane-unique detached worktree at **`0b0617fee`**
  (`/tmp/qt-v4-pin-p4d152-0b0617fee`, three symlink classes, ledger §5.1)
  through the sweep driver. ⚠ **This pin is v4 HEAD and carries ALL FIVE
  drift commits** — regenerate ONLY this lane's families from it; measure
  and mask-by-name any movement on bytes this lane did not port (the
  P4.D143 tripwire discipline). Verify the pin by grepping a fresh
  restore/import NDJSON for a row whose `files.sha256` EQUALS its blob's
  (the within-tree boolean below) on a case that was false at the baseline.
- **Sibling lanes:** P4.D148/P4.D149 (Concierge at creation), P4.D150 (bug
  115), P4.D151 (bug 116 — owns `file_fallback.rs`, the `CompletionResponse`
  widening and `attach_mount_file_equivalence`; **do NOT touch those**, §D).
  §Shared contracts / §Ownership binding and identical across all five
  orders.
- **Worktree:** per the `carryout` skill (branch `claude/p4-d152-…`).
  `CHANGELOG`/`status-log.md` append-only. **No Playwright** (§F).

## Mandate

Port bug 117's four legs whole (§D): (a) chat upload runs the bridge's own
transcode BEFORE anything is hashed and the row records the bridge's
`sha256` (with v4's disagree-warn); (b) `.qtap` import records the bridge's
`sha256` beside the post-bridge `mimeType`/`size` it already takes; (c)
restore's replay branch takes `sha256` from the bridge, and the
carried-store-rows branch resolves the archived `doc_mount_blobs.sha256`
by the parsed blob id; (d) v4's `realign-file-entry-sha256-v1` migration as
a v5 boot heal in the established `db/*_heal.rs` idiom (no migration runner
— the D140 precedent for a v4 migration that writes a `migrations_state`
row). Must NOT touch: `file_fallback.rs` (P4.D151's), `chat_create.rs`,
the distill set, `api/files.rs` (v4's `files/route` upload did not move —
its NotConfigured passthrough note at `:1116` stands), the SPA.

## Survey-verified starting points (2026-09-02, from the shipped hunks — ledger §5.3)

1. **⚠ The codec fact that shapes leg (a).** v4 transcodes chat-uploaded
   bitmaps to WebP through sharp. v5's chat upload path
   (`services/chat_files.rs:833` `let codec = NotConfiguredPixelCodec;`)
   hands the bridges a codec whose every encode FAILS, so the policy layer
   passes the ORIGINAL bytes through (v4's own sharp-unavailable branch —
   the documented pre-existing divergence at `api/files.rs:1116-1118`).
   Consequence: in v5 production a chat upload's input bytes ARE its stored
   bytes, so `files.sha256` already equals `doc_mount_blobs.sha256` for
   every row v5 wrote — **v5 has bug 117's code SHAPE (`:674` hashes
   `data`, `:872` writes it, `:834-860` takes mime/size from the bridge) but
   not its symptom on its own rows.** The symptom IS live on rows v4 wrote
   on the shared instance (118 of 239 on v4's), on rows v5's `.qtap` import
   wrote (the engine threads the HOST codec there — `api/engine.rs:649,
   1136` `qtap_pixel_codec()`), and on rows v5's restore wrote (the host
   codec, `restore/orchestrator.rs:135`). Port the SHAPE at (a) faithfully
   — `transcode_to_webp(&codec, data, mime)` FIRST (the bridge's own
   function, `services/file_storage.rs:463` with `BlobTranscodeResult
   {data, stored_mime_type, size_bytes, sha256}`; a no-op for non-images
   and existing WebP), then dedup + category + the row from
   `stored.sha256` — keeping `NotConfiguredPixelCodec` at that site.
   Threading the host codec into chat uploads would be a NEW convergence
   (v4 transcodes there) beyond this drift row — record it as a named
   candidate in the lane record, do not do it here.
2. **Leg (a) in detail** (v4 `chat-files-v2.ts:134-165` + `:336-425`):
   `detectTextContent`/`getBestMimeType` read the INPUT bytes (`:139-140`
   — "the only thing it can mean"); `transcodeToWebP(inputBuffer,
   inputMimeType)` → `buffer/mimeType/sha256` from the result; category
   from the STORED mime; `uploadFileToProject` takes `storedSha256` from
   the bridge (`uploaded.sha256` / `written.sha256`), warns `Stored-bytes
   hash differs from the pre-upload hash; recording the stored one` with
   `{context:'chat-files-v2', filename, preUploadSha256, storedSha256,
   storedMimeType}` when they disagree, and writes `sha256: storedSha256`.
   v5 `chat_files.rs:662-690` (`upload_chat_file_conn` — the hash at
   `:674`, text detection `:675-676`, the sha-keyed dedup arms `:688-804`)
   and `:819-880` (`upload_file_to_project` — `StoredBlob.sha256` is on
   both bridge results, `file_storage.rs:975-992`; add the warn + write
   the bridge's value at `:872`). v4's dedup residual ("sharp's WebP
   encoding has to be deterministic for `findBySha256` to keep matching
   re-uploads across a version bump") becomes a why-comment at the v5 site
   naming the codec seam.
3. **Leg (b)** (v4 `import-files.ts:236-259` + `:288-294`): `storedSha256`
   from `uploaded.sha256`/`written.sha256`, written as `sha256:
   storedSha256`. v5 `services/quilltap_import/files.rs:342-348` writes
   `sha256: s(file, "sha256")` beside `mime_type: stored.stored_mime_type`
   / `size: stored.size_bytes` — take `stored.sha256`.
4. **Leg (c)** (v4 `restore.ts:477-486`, `:503-511`, `:525-570`): the
   replay branch's `restoredSha256` from the bridge → `sha256:
   restoredSha256`; the carried branch builds `carriedBlobSha256ById` from
   `data.docMountBlobs` and spreads `...(carriedSha256 ? {sha256} : {})`
   after resolving `parseMountBlobStorageKey(carriedStorageKey)?.blobId`.
   v5 `services/backup/restore/orchestrator.rs:1841-1960`:
   `carried_store_rows` (`:1841`) already parses the key
   (`file_storage::parse_mount_blob_storage_key`, `:1849`) and reads
   `storedMimeType, sizeBytes FROM doc_mount_blobs` (`:1863-1866`) — read
   `sha256` in the same SELECT and carry it on `CarriedBlob`;
   `restore_one_file` (`:1905`) writes `sha256: s(file, "sha256")` at
   `:1951` — take the bridge's for the replay arm, the carried blob's for
   the carried arm (v4's `carriedSha256 ? … : {}` — when the archived blob
   row lacks a hash, the ARCHIVE's `files.sha256` stands; keep that arm).
   **Re-read the RULED DIVERGENCE comment at `:994` before touching the
   carried branch** — it names why v5's carried resolution differs from
   v4's, and the sha carry must sit inside that ruling, not around it.
5. **Leg (d) — the heal** (v4 `realign-file-entry-sha256.ts`, whole file
   above in the drift row): walk `files` where `storageKey LIKE
   'mount-blob:%'` in batches of 500 ordered by id, parse `mount-blob:
   <mountPointId>:<blobId>` (malformed → warn `Malformed mount-blob storage
   key; sha256 left untouched` `{fileId, storageKey}`, count), read
   `doc_mount_blobs.sha256` by id from the MOUNT partition (missing → warn
   `Mount blob missing for FileEntry; sha256 left untouched` `{fileId,
   blobId}`, count), skip agreeing rows, UPDATE `files SET sha256 = ?,
   updatedAt = ?` in one transaction per batch, summary info `Scanned
   ${scanned} mount-blob FileEntries; realigned ${realigned} sha256 values;
   ${orphaned} orphaned (no matching blob), ${malformedKey} malformed
   storage keys` with the four counts; idempotent. v5: NEW
   `db/files_sha256_realign_heal.rs` taking BOTH connections (`main`,
   `mount`) — the host chain's mount-aware block is at `quilltap-host/src/
   host.rs:1176` (`ensure_builtin_mounts(main, mount_index)`); the
   main-only blocks are `:1023-1075` (D140's recompute + D145's folders
   collapse) — copy the D140 block's shape (`if let Outcome::Ran {…} =
   heal(main, mount, &now_iso())? { tracing::info!(target:
   "quilltap::boot", …) }`). **Ledger decision (§D): the P4.D140 shape** —
   `migrations_state` row `realign-file-entry-sha256-v1` honoured if
   present (v4 will already have run its migration on a shared instance —
   the free cross-app proof the 2026-09-02 dogfood pass got for bug 112),
   written (with `quilltapVersion`, `itemsAffected`, `message`) only on a
   pass that realigned ≥ 1 row; a no-drift boot writes nothing. Record
   the alternative (the D97 always-stamp shape) and why it was not chosen
   in the module header. `shouldRun`'s `mount-index DB exists` gate maps to
   "the mount partition is open"; its `files` table-exists gate stays.
6. **The corpus is BLIND on both legs (a) and (b) today, and the comparand
   must be a BOOLEAN.** No oracle drives `uploadChatFile` with an image
   (`files-routes.test.ts:110` — "Text uploads never touch sharp"); the
   real-DB oracles use REAL sharp, whose WebP bytes v5's harness cannot
   reproduce, so `files.sha256` can never be compared as a string across
   the trees. Compare the WITHIN-TREE equality instead (memory note
   `within-tree-equality-comparand`): per file row, `files.sha256 ==
   doc_mount_blobs.sha256` (joined through the parsed storage key) as a
   boolean, floor-asserting the true AND false buckets. On the v5 side the
   harness needs a codec that CHANGES bytes deterministically (the
   existing `PassthroughPixelCodec` returns the input → the boolean is
   vacuously true pre-fix): add a harness-only `PrefixingPixelCodec`
   (`encode_webp` = a fixed byte prefix + the input; `measure` fixed) in
   the harness support module, so pre-fix v5 yields `false` on a PNG
   upload and post-fix `true`, while v4 with real sharp yields `true` both
   times — RED FIRST. Text and already-WebP uploads stay `true` on both
   sides (the no-op arms).
7. **Families.** `files_routes_equivalence` (`QT_ORACLE_FILES_ROUTES`,
   chat upload at `chatFilesUpload` `:230-237`; v5 `chat_media::
   chat_file_upload` at `:970`) gains an IMAGE upload arm with the
   boolean comparand + the disagree-warn captured; `qtap_import_
   equivalence` (`QT_ORACLE_QTAPIMPORT`, `QT_FIXTURE_QTAPIMPORT_*`) gains a
   `.qtap` carrying a PNG whose archived row sha is the PRE-transcode hash
   (build it with v4's REAL exporter from a pre-fix worktree, or hand-edit
   the row — say which) → boolean true post-fix; `system_restore_state` /
   `system_restore_equivalence` / `restore_vintage_state`
   (`QT_ORACLE_SYSTEM_RESTORE`) — a NEW committed archive
   `restore-archive-bug117.zip` with (i) a replayed PNG whose row sha is
   pre-transcode and (ii) a carried-store-rows PNG (the gen-2 shape) whose
   archived `docMountBlobs` row carries the stored hash — boolean per
   branch; the NEW `files_sha256_realign_heal_equivalence` — v4's REAL
   migration `run()` (the real-DB jest idiom; `migrations/scripts` imports
   through `../lib/database-utils` — mock what `isSQLiteBackend`/
   `getSQLiteDatabase` need, as the D140/D145 heal oracles did) over a
   planted main+mount pair with v4's seven cases as the corpus (drifted
   row → realigned; agreeing → untouched incl. `updatedAt`; missing blob →
   left + warn; malformed key → left + warn, batch continues; non-mount-
   blob row ignored; second run no-op; > 500 rows walks past the first
   batch), diffing the `files` dump + the `migrations_state` row + the
   four counts. `character_photo_upload_tier2_equivalence` and
   `photo_tools_equivalence` regenerate at the pin and run (no movement
   expected — the photo paths hash after transcode already, per v4's own
   "all 2541 generated images were correct"; say so).
8. **The readers the heal serves** (name them in the module header, they
   are the reason the column matters): `photos/auto_describe_attachment.rs:
   191` (`dmf.find_by_sha256(&entry.sha256)` — a description that never
   reaches the search index), `tools/photo.rs:733,874` (`describe_image` /
   `attach_image` resolving a mount link's sisters), `photos/
   photo_link_summary.rs:32` + its five callers (link summaries at zero).

## Tiered deliverables

- **Tier 1 (must land):** legs (a)–(d) with the boolean comparand RED
  FIRST on the upload arm and the heal family whole; the host boot block;
  the `PrefixingPixelCodec` harness twin; the candidate note for threading
  the host codec into chat uploads (point 1).
- **Tier 2 (should land):** the restore archive (both branches) and the
  `.qtap` import arm; the disagree-warn capture-pinned (thread-scoped
  seam) with a mutation that returns a different hash from the bridge.
- **Tier 3 (explicit deferrals, loud):** the `PRETTY_LABELS` loading-screen
  sentence has no v5 surface (the boot log line carries the counts) —
  record the sentence in the module header for the day a loading screen
  exists; `docs/developer/DDL.md` prose → the `docs/v4/` mirror at its next
  refresh; the export-schema `description` (no constraint) → the standing
  `qtap-export.schema.json` file-port flag.

## The differential requirement (restated)

Every unit drives v4's REAL code from the `0b0617fee` pin (families and
env vars in point 7; recipes in the test headers, new recipes per
`recipe-header-conventions`). Mutation proofs (verified-applied, reverted
by file backup): hash before the transcode → the upload boolean reddens
under `PrefixingPixelCodec`; write `s(file,"sha256")` at import → the
`.qtap` arm reddens; drop the carried-blob SELECT column → the carried
restore arm reddens; skip the `migrations_state` honour check → the
"row already present" heal case reddens (v5 realigns where v4 skips);
stamp on a no-drift pass → the idempotence case reddens (a row appears);
batch size 5000 → the >500 case stays green (record: batching is
unobservable by state — pin it by a query-count seam or say it is
unpinned).

## Fixtures

- Consumes: the files-routes fixture, `qtap-import-*`, the committed restore
  archives (the seven + `restore-archive-*` gen-2), `character-photo-*`,
  `photo-tools-*` (run).
- Delivers: the NEW planted pair for the heal family (built by its recipe,
  /tmp), the NEW committed `restore-archive-bug117.zip` (+ its builder
  invocation recorded), the widened `.qtap` fixture if committed. Any
  committed fixture regen ⇒ every sibling family reading it re-run by name,
  zero SKIP; name them in the record.

## Shared contracts (binding, identical across all five orders)

**§A — the create-time Concierge wire (P4.D148 ↔ P4.D149).** The
`chatCreate` dispatch verb's request (v5 `ChatCreateRequest`, SPA
`ChatCreateRequest` in `core-contract.ts`) gains ONE optional key:

```
conciergeState?: 'monitored' | 'flagged' | 'vouched' | 'uncensored'
```

(v4 `createChatSchema`: `z.enum([...]).optional()` — spelled out, not derived;
`.optional()` is NOT nullable, so an explicit JSON `null` REJECTS like
`timestampConfig`'s does.) Absent ≡ `'monitored'`: no write, no announcement,
the request AND the created chat byte-identical to today's. Any other value is
applied through the EXISTING `apply_concierge_flip` chokepoint (v4
`applyConciergeFlip(chat.id, requested, chat)` with the fresh row) on ALL
THREE create branches (continuation / autonomous / ordinary), immediately
AFTER the system-prompt message and BEFORE `apply_chat_continuation` /
scenario / staff / greeting, so the Concierge bubble is the first row after
the prompt and the greeting is generated under the chosen state. The
announcement reuses v4's five `manual-*` kinds (`RealConciergeAnnouncer`) —
NO new sentences. The SSE progress status line is exactly
`Briefing the Concierge…` (U+2026), emitted only when a non-Monitored state
is applied, between the branch's existing status and the scene status. An
unknown string → the create schema's validation 400 (the envelope
`tc_bad_value_rejected` already pins). The CLIENT omits the key when the
form holds `'monitored'`. v5 serves NO REST `POST /api/v1/chats` (the SPA
creates through `POST /api/dispatch`; `chats_routes.rs` has the collection
GET only) — recorded, not this round's.

**§B — `CompletionResponse.cache_usage` (P4.D151 owns the widening).**
`model::completion::CompletionResponse` gains
`pub cache_usage: Option<crate::model::stream::StreamCacheUsage>` (v4
`LLMResponse.cacheUsage`), populated from `parsed.cache_usage` in the real
`completion_provider.rs` composition and `None` at every other construction
site. **P4.D151 edits every construction site itself, including the
`cache_usage: None` one-liners in harness tests owned by other families**
(`precompute_equivalence.rs`, `recall_replay_equivalence.rs`,
`title_update_tier3_equivalence.rs`, `settings_wire_actions.rs`,
`attach_mount_file_equivalence.rs`, `chat_regenerate_title_tier3_equivalence.rs`,
`answer_confirmation_tier3_equivalence.rs`, `danger_gatekeeper_tier3_equivalence.rs`,
`file_attachment_tier3_equivalence.rs`, `outfit_llm_choose_tier3_equivalence.rs`).
No other lane edits those harness files this round (P4.D150's pins are
core unit tests, not harness edits) — that is what keeps the pick clean.

**§C — `distill_memory_search` (P4.D150 owns).** The function gains a
trailing `options: CheapLlmTaskOptions` parameter threaded to
`executor.execute(...)` (v4 `latency: CheapLLMLatencyClass = 'background'`
→ `{ latency }`). Its three callers: `build_context.rs` (the dynamic-head
FALLBACK) passes `CheapLlmTaskOptions::interactive()`; `pre_compute.rs` and
`recall_replay.rs` pass `CheapLlmTaskOptions::default()`. No other lane
touches `services/memory_recap/distill.rs`, `services/build_context.rs`,
`services/pre_compute.rs`, or `services/recall_replay.rs`.

**§D — the `files.sha256` invariant (P4.D152 owns).** `files.sha256`,
`mimeType` and `size` ALL describe the bytes actually STORED (post-bridge),
never the writer's input — at chat upload, `.qtap` import, restore replay
AND the carried-store-rows restore branch (which reads the archived
`doc_mount_blobs.sha256` by the parsed blob id). The boot heal carries v4's
migration id `realign-file-entry-sha256-v1` in the P4.D140 ledger shape
(honour an existing `migrations_state` row from either app; write the row
only on a pass that realigned ≥ 1 row). P4.D152 does NOT touch
`services/file_fallback.rs` (the describe tier reads `files` by id, never
by sha) and does NOT edit `attach_mount_file_equivalence.rs` (P4.D151's);
its join proof is the NEW heal family + the state dumps.

**§E — the manifest substrate (P4.D151 owns for this round).** The generated
`provider_manifest/manifests/*.json` are regenerated ONCE, by P4.D151, from
the `0b0617fee` pin through `harness/oracle/providers/gen-provider-manifests.mjs`;
the expected result is BYTE-IDENTICAL (bug 118 is a v4 convergence onto
bytes v5 has carried since P4.D107). No other lane runs the generator.

**§F — Playwright.** Port 4319 is P4.D149's this round. P4.D149's
create-time beat is authored GATED behind `const P4D148_SERVER_LANDED =
false` (ACTIVATE-AT-UNIFY — the unifier flips it after P4.D148 lands and
runs it live); every other lane authors NO Playwright and says so.

**§G — the drift ledger.** Lanes never write `drift-ledger.md`; the five §3
rows were marked ORDERED at ordering, and `/unify` marks them ABSORBED /
NO-PORT-RATIFIED when the baseline moves to `0b0617fee`.

## Ownership (binding, identical across all five orders)

| Area | Owner |
|---|---|
| `quilltap-core`: `services/chat_create.rs` (the request field, `apply_requested_concierge_state`, the three branches, `auto_generate_first_message`'s attempt 0 + the shared uncensored-desk closure, the five log lines); harness: `chat_create_capstone_equivalence` (+ `cases/chat-create-capstone.test.ts`, `fixtures/chat-create-capstone.json`, `fixtures/build-chat-create-capstone.ts`), `initial_greeting_equivalence` + `first_message_context_equivalence` (run, not changed) | **P4.D148** |
| SPA: `screens/new-chat/{new-chat-form.ts, new-chat.types.ts, new-chat.state.ts, new-chat.logic.ts}` (+ specs), `core/core-contract.ts` §A region, `e2e/new-chat-flow.spec.ts` (the gated beat); the `p4.9i2` bank rows for `help/chats.md` + `help/dangerous-content.md` | **P4.D149** |
| `quilltap-core`: `services/memory_recap/distill.rs`, `services/build_context.rs` (the fallback-distill site + the inter-character log line + its unit pins), `services/pre_compute.rs`, `services/recall_replay.rs`; harness: `build_context_tier3_equivalence`, `precompute_equivalence`, `recall_replay_equivalence` (RUN only — no edits, §B) | **P4.D150** |
| `quilltap-core`: `services/file_fallback.rs` whole, `model/completion.rs` (§B), `model/completion_provider.rs` (§B), the `cache_usage: None` one-liners at `services/compression.rs`, `services/cheap_llm_exec.rs`, `services/memory_recap/mod.rs`, `services/outfit_selections.rs` + the harness sites listed in §B, `model/request_builder/chat_completions.rs` (the NanoGPT comment), `provider_manifest/manifests/*.json` (§E regen, expected no-op); harness: `file_attachment_tier3_equivalence` (+ `cases/file-attachment-tier3.test.ts`, `fixtures/file-attachment.json`, its builder), `fallback_engine_equivalence` (+ `cases/fallback-engine.ts`), `attach_mount_file_equivalence` (+ its case); the `p4.9i2` bank rows for `help/file-uploads.md` + `help/chat-settings.md`; the `b448eddd7` NO-PORT ratification | **P4.D151** |
| `quilltap-core`: `services/chat_files.rs`, `services/quilltap_import/files.rs`, `services/backup/restore/orchestrator.rs` (the files region — `restore_one_file` / `carried_store_rows` and the replay branch ONLY), NEW `db/files_sha256_realign_heal.rs` + its `db/mod.rs` line; `quilltap-host`: `host.rs` (one boot-chain block); harness: NEW `files_sha256_realign_heal_equivalence` (+ its oracle case + fixture builder), `files_tier2_equivalence` / `files_routes_equivalence` (the upload sha arm), `qtap_import_equivalence`, `system_restore_state` / `system_restore_equivalence` / `restore_vintage_state` (the sha arms + any NEW committed archive), `character_photo_upload_tier2_equivalence` + `photo_tools_equivalence` (run), a harness-only byte-changing `PixelCodec` twin | **P4.D152** |
| Playwright (port 4319) | **P4.D149's branch only** (§F) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it (§G) |

**Version bumps:** P4.D148 bumps `quilltap-core` + `quilltap-harness`;
P4.D149 bumps the SPA `package.json`; P4.D150 bumps `quilltap-core` (+
`quilltap-harness` only if a harness file changes — §B says none should);
P4.D151 bumps `quilltap-core` + `quilltap-harness`; P4.D152 bumps
`quilltap-core` + `quilltap-harness` + `quilltap-host`. Per commit; the
unifier recounts at unification (the playbook's silent-auto-merge trap).

## Verification gate before declaring done

`cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
-D warnings` on BOTH feature sets; full `cargo test --workspace` with the
lane's env vars (`run_in_background: true`, full log + sentinel — NEVER a
pgrep loop, NEVER `| tail`); the lane's families by name through the sweep
driver from the `0b0617fee` pin, zero SKIP, with the boolean buckets'
floors asserted and the heal counts grepped; `provisioning_equivalence`
run once at the pin (no D23 re-dump expected — record it); mutation
proofs verified-applied and reverted by file backup; the host crate's boot
tests by name. No SPA gate (no SPA file touched — say so). Per-commit
rule: `.claude/commands/commit.md` (CHANGELOG entry per commit, core +
harness + host bumps, lane record appended to `status-log.md` under
"P4.D152"). 💸 the heal on the Friday copy (measure the population FIRST
— v4 may already have healed it, ledger §5.5) joins the dogfood queue.
