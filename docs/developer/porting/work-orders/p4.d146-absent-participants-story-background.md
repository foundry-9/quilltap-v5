# Work order P4.D146 — absent characters out of story backgrounds + the project background-mode narrowing (v4 `70505745a`)

**Status:** OPEN (ordered 2026-09-02).

## Preamble

- **v4 oracle baseline: `4622411fd`** (drift-ledger §1, probe re-run
  2026-09-02). **This lane's target commit is `70505745a`** — *"fix(images):
  keep absent characters out of story backgrounds"*: the three
  story-background participant sites gain v4's `isParticipantPresent`
  gate (`active`/`silent` are present; `absent`/`removed` are not), and
  the project `backgroundDisplayMode` enum narrows to
  `latest_chat | theme` with a coercing preprocessor for the two retired
  modes. Not a convergence (v4's bugs doc is untouched). The hunks are the
  spec (ledger §5.3): `git -C ~/source/quilltap-server show 70505745a --
  <path>`.
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 checkout
  on `main`, tree clean, `git log 6d2a50382..main` empty and
  `git log 3a76b17df..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED** (ledger §1). Every oracle/fixture regen
  runs from a lane-unique detached worktree pinned at **`70505745a`**
  (`/tmp/qt-v4-pin-p4d146-70505745a`, three symlink classes, ledger §5.1),
  through the sweep driver (`harness/tools/recipe_sweep.py --run <family>
  --v4 "$PIN" --v5w <worktree>`). `70505745a` is the OLDEST of the round's
  four PORT commits — its ancestors are all absorbed, so this lane's pin
  carries NO sibling drift. Families this lane does not move regen at the
  baseline if needed.
- **Sibling lanes (this round):** P4.D143 (Concierge list marks, server —
  shares `api/projects.rs`, §D), P4.D144 (Concierge marks, SPA), P4.D145
  (bug 114 folders — shares `services/story_background_job.rs`, §C, and
  the restore orchestrator, §F), P4.D147 (the Move-to-Project folder
  picker — owns Playwright). §Ownership / §Shared contracts are binding
  and identical across all five orders.
- **Worktree:** per the `carryout` skill. `CHANGELOG`/`status-log.md`
  append-only. **No Playwright** (P4.D147 owns port 4319) — this lane's
  mode-select beat edit is authored gated (ACTIVATE-AT-UNIFY).

## Mandate

Port `70505745a` whole: the participant-presence gate at the three v5
story-background sites (the auto-trigger enqueue, the manual regenerate
action with its reworded 400 sentence, and the back-fill's exclusion set),
the `normalize_background_display_mode` preprocessor + the narrowed enum
at every v5 read/validate/write of `backgroundDisplayMode` (properties
parse, the update schema, the background GET's dead branches, the restore
carry), and the SPA card's retired options — each red-first over a fixture
that can finally see the change. Must NOT touch: P4.D145's regions of
`story_background_job.rs` (the folder-ensure site + its private `find_folder_by_path`, `:1425-1471`) and the
restore orchestrator (the folders loop), P4.D143's region of
`api/projects.rs` (the `?action=chats` serializer), any Concierge or
folders file, `qtap_export/schema-key-order.json` (key order only — no
change), or the workspace-backdrop reporter (`project-detail.ts:221-227`
is mode-agnostic — verified, no change).

## Survey-verified starting points (2026-09-02, from the shipped hunks — ledger §5.3)

1. **The predicate.** v4 `lib/chat/chat.types.ts:557` gains
   `isParticipantPresent(status) = status === 'active' || status ===
   'silent'`. **v5 already has the canonical twin:**
   `crates/quilltap-core/src/chat_predicates.rs:39-44`
   `is_participant_present(status: ParticipantStatus) -> bool`, consumed by
   `skip_signal.rs`, `message_attribution.rs`, `participant_filters.rs`.
   The three sites below parse participants from JSON `Value`, so each
   needs a str → `ParticipantStatus` step at the boundary
   (`skip_signal.rs:59-64` is the inline-match precedent). Decide once
   (a shared helper in `chat_predicates.rs` is the recommended shape —
   three new sites justify it), record it, and carry v4's comment:
   *"Absent and (soft-)removed participants must never be painted into
   the background … 'silent' counts as present: they are standing there,
   just not speaking."*
2. **Site 1 — the auto-trigger enqueue** (v4 `title-update.ts:276-286`
   `queueStoryBackgroundIfEnabled`, filter `isParticipantPresent(p.status)
   && p.characterId`). **v5's twin is NOT in `title_update_job.rs`** — it
   is `services/image_profile_resolution.rs:168-181`
   `queue_story_background_if_enabled` (called from
   `title_update_job.rs:470`), which collects every `characterId` with no
   status filter. Add the gate there; `title_update_job.rs` itself needs
   only whatever import/comment the ledger row's "title-update" mention
   implied — verify by diff, likely nothing.
3. **Site 2 — the manual regenerate action** (v4
   `app/api/v1/chats/[id]/actions/story-background.ts:38-49`): the same
   filter, AND the empty-list 400 sentence changes from
   `No characters in chat to generate background for.` to
   **`No characters present in chat to generate background for.`** v5:
   `api/chat_media.rs:1340-1354` `chat_regenerate_background` — still the
   pre-fix truthy filter (its own doc at `:1277`/`:1340` says so) AND the
   OLD sentence at `:1353`. Both move; the routes differential must carry
   the new bytes (grep the regenerated NDJSON, ledger §5.2).
4. **Site 3 — the back-fill exclusion set** (v4
   `story-background.ts:573-593`): `excludedIds = payload.characterIds ∪
   {p.characterId | p ∈ chat.participants, !isParticipantPresent(p.status)}`;
   `nonParticipantCharacters = userCharacters.filter(c => !excludedIds.has
   (c.id))`. v5: `services/story_background_job.rs:706-728` builds the
   pool from `payload.character_ids` alone. Widen the set exactly; the
   `[decd8ef9]` HELD-past-this-point comment at `:715-719` stays. **This
   is the §C participant region** — the folder-ensure site at
   `:1425-1471` is P4.D145's; do not touch it.
5. **The enum + preprocessor** (v4 `lib/schemas/project.types.ts`):
   `RETIRED_BACKGROUND_DISPLAY_MODES = ['project','static']`;
   `normalizeBackgroundDisplayMode(value)`: `undefined|null → undefined`;
   `'latest_chat'|'theme'` pass through; **anything else → `'theme'`**
   (retired or unrecognised alike); the schema field is
   `z.preprocess(normalize…, z.enum(['latest_chat','theme'])).default
   ('theme')` and the standalone `BackgroundDisplayModeEnum` narrows the
   same way. v5: `db/projects.rs:26-115` `ProjectProperties.
   background_display_mode` is a **plain `String`** defaulting to
   `"theme"`, `parse_properties` (`:148-153`) is a bare
   `serde_json::from_value` — no enum, no coercion, anything round-trips.
   Land the normalizer as a named function, apply it in the properties
   READ (parse) and in the property WRITE overlay (v4's
   `project-store-write-overlay.test.ts` case *normalizes a retired
   background display mode on the way to disk* pins the write side), and
   keep the absent → default arm (`.default('theme')`) exact.
6. **The update schema + the GET** (v4 `app/api/v1/projects/[id]/
   schemas.ts` + `actions/background.ts`): the update enum narrows to
   `['latest_chat','theme']` (v5 `api/projects.rs:398-425`
   `PROJECT_UPDATE_SCHEMA` still lists four — narrow it; `PROJECT_CREATE_
   SCHEMA` never accepted the field, matches v4, no change); the GET's
   `'static'`/`'project'` resolution branches are **deleted outright** and
   the mode is normalized first (`normalize(...) ?? 'theme'`) — v5
   `api/projects.rs:939-1022` `project_background_get` still carries both
   dead arms at `:964-982`. **This is the §D background region** — the
   chats serializer in the same file is P4.D143's.
7. **The restore carry** (v4's `cutover-projects-to-store.ts:243-249`
   routes a legacy row's mode through the normalizer; v5 has NO cutover
   script — projects were store-backed from the port's start). v5's closest
   analog is `services/backup/restore/orchestrator.rs:2130-2145`
   `project_properties`, which passes `backgroundDisplayMode` through
   verbatim. Route it through the normalizer IF the write path it uses
   does not already pass through the write overlay from point 5 — measure
   with a restore of an archive carrying `'static'` (v4's converged shape
   after `70505745a`: does v4's restore normalize? `git show 70505745a --
   lib/backup/restore/` is empty — so v4 restore writes the raw value and
   the READ normalizes it; match THAT, not a guessed write-side coercion,
   and record it). **This is the §F project-properties region** — the
   folders loop in the same file is P4.D145's.
8. **No-op counterparts, recorded not built:** `qtap-export.schema.json`
   keeps its four-value enum (only a `description` was added — old
   exports must validate; v5 has no local copy); `schema-key-order.json:
   269` is key order only; the workspace-backdrop gate
   (`project-detail.ts:221-227`, `mode !== 'theme'`) is value-agnostic;
   `story_background_job.rs:882-899`'s `latest_chat` write-back is
   untouched by the fix.
9. **SPA** (v4 `ImageGenerationCard.tsx:140-147`, `useProjectDetail.ts:
   256-260`, `types.ts:70`): the `project`/`static` `<option>`s + their
   hint paragraphs deleted, `modeLabels` loses both entries, the type
   narrows. v5: `screens/prospero/cards/project-image-generation-card.ts:
   127-132` (options), `:193-196` (hints), `:238-239` (`modeLabels`);
   `core-contract.ts:1504` + `:3622` (both unions — §E region). The two
   backdrop specs (`workspace-project-backdrop-flow.spec.ts:93,114`,
   `salon-background-flow.spec.ts`) select only `latest_chat`/`theme`, so
   removing the options breaks no beat; a small ACTIVATE-AT-UNIFY beat
   asserting the select has exactly two options is the client pin.
10. **The fixture blind spot — the port's key gap.**
    `harness/oracle/fixtures/build-story-background-job-fixture.ts:303`
    seeds EVERY participant `status: 'active'`, so the committed
    `qt-story-{main,mount}.db` pair cannot discriminate any of the three
    gates (v4 needed two brand-new test files for the same reason). Widen
    the builder with one participant per status (`active`, `silent`,
    `absent`, `removed` — v4's `story-background-present-participants`
    fixture shape) and the back-fill triad (an active participant, an
    absent participant, an unaffiliated named character — v4's
    `backfill-absent` shape); the `chat-cast` builder
    (`build-chat-cast-fixture.ts:452-493`) is the well-documented
    precedent for all three states. `title-update-tier3`'s
    `cost-background-{main,mount}.db` drives the enqueue site and is the
    second candidate to widen. Regenerating a committed fixture ⇒ every
    sibling family reading it re-runs by name, zero SKIP.

## Tiered deliverables

- **Tier 1 (must land):** points 1–7 — the three gates with the reworded
  400, the normalizer at every read/validate/write, the dead GET arms
  removed, the restore carry measured and matched, each red-first over
  the widened fixtures with the differentials below.
- **Tier 2 (should land):** point 9 (the SPA card + contract unions + the
  gated beat) and the `title-update-tier3` fixture widening if the
  enqueue gate cannot be discriminated from the story family alone.
- **Tier 3 (explicit refusal-arm deferrals):** none planned. The
  `help/story-backgrounds.md` hunk banks to `p4.9i2`; the
  `SYSTEM_FLOWCHARTS.md` hunk → the `docs/v4/` mirror at the next refresh.

## The differential requirement (restated)

Every unit drives v4's REAL code from the `70505745a` pin:

- `story_background_job_tier3_equivalence` (`QT_ORACLE_STORY` +
  `QT_FIXTURE_STORY_{MAIN,MOUNT}`) — the back-fill exclusion over the
  widened fixture: the absent participant is NOT enumerated, the
  unaffiliated named character still IS, the present participant is left
  to the crafter (v4's three `backfill-absent` cases as ops). Red-first:
  v5's current pool must measurably enumerate the absent participant
  before the fix.
- `title_update_tier3_equivalence` (`QT_ORACLE_TITLE_UPDATE`) — the
  enqueue's `characterIds` over one-participant-per-status: exactly
  `[active, silent]`; and the all-left case enqueuing NOTHING (v4's
  `present-participants` cases). If the committed `cost-background` pair
  cannot carry the statuses, widen it (ownership: this lane) and re-run
  every family that reads it.
- `cost_background_routes_equivalence` (the family that drives
  `chat_regenerate_background`; env vars per its recipe header) —
  the reworded sentence byte-exact + the present-only enqueue (grep the
  NDJSON for `present in chat`).
- `projects_routes_equivalence` (`QT_ORACLE_PROJECTS_ROUTES`) +
  `projects_tier2_equivalence` (the properties read/write) — the update
  schema refusing `project`/`static` with v4's exact envelope (measure
  `.parse` vs `safeParse` — the standing note), the GET normalizing a
  stored retired mode to `theme` (seed one through the store overlay —
  SQL seeding of an overlay property is invisible, the P4.D92 lesson), the
  properties read defaulting an absent field.
- A tier-1 row set for `normalize_background_display_mode` transcribed
  from v4's `project-background-display-mode.test.ts` (seven cases) —
  driven through v4's real module, not reimplemented.
- The restore family that carries `project_properties` — a
  `'static'`-bearing archive (a new committed archive if none exists)
  proving the read-normalizes/write-raw shape matches v4 exactly.
- SPA: the card spec pins exactly two options + the two surviving hints;
  the contract unions narrowed (compile-time).

Mutation proofs per gate (revert one site → exactly its family reddens),
verified-applied, reverted by file backup.

## Fixtures

- Consumes: `qt-story-{main,mount}.db` (via its builder), the
  `cost-background-{main,mount}.db` pair, the groups-projects fixture,
  the restore archives.
- Delivers: the widened story-background builder + regenerated pair; the
  widened `cost-background` pair if needed; a retired-mode restore archive
  if needed. Regenerating a committed fixture ⇒ re-run every sibling
  family that reads it, by name, zero SKIP, and name them in the record.

## Shared contracts (binding, identical across all five orders)

**§A — the list-payload wire (P4.D143 ↔ P4.D144).** Every chat-LIST shape
v4 `c43d3b1b4` touches carries the derived pair **`conciergeState:
'monitored'|'flagged'|'vouched'|'uncensored'`** (the `ConciergeState::
as_str` wire spelling) and **`dangerCategories: string[]`** (always
present — `?? []`), IN PLACE of the raw field(s) it replaces, key order
otherwise untouched:
- `EnrichedChatSummary` (the `chatList` verb / `GET /api/v1/chats`):
  `isDangerousChat` AND `conciergeOverride` both DROPPED → `… project,
  storyBackground, conciergeState, dangerCategories, chatType,
  scriptoriumStatus, _count, _allTagIds`.
- `RecentChat` (the home dashboard): `isDangerousChat` DROPPED → `id,
  title, createdAt, updatedAt, lastMessageAt, conciergeState,
  dangerCategories, storyBackgroundUrl, participants, _count`.
- character conversations rows (`characters/{id}?action=chats`):
  `isDangerousChat` DROPPED → `… tags, conciergeState, dangerCategories,
  _count, scriptoriumStatus`; envelope `{chats, total}` unchanged.
- project chats rows (`projects/{id}?action=chats`): `isDangerousChat`
  DROPPED → `… storyBackground, conciergeState, dangerCategories,
  lastMessageAt, updatedAt, createdAt`; envelope `{chats, pagination}`
  unchanged.
- The single-chat GET (`ChatDetail`) KEEPS `isDangerousChat`,
  `dangerCategories`, `conciergeOverride` — v4's detail view is untouched
  (the sidebar control needs the raw pair).
D143 emits exactly these; D144 types exactly these in `core-contract.ts`
(+ `home.api.ts`'s `RecentChat`). A key present in one lane's record and
absent from the other's is a unification defect; the unifier diffs the
contract name-for-name against the Rust shapes.

**§B — the Concierge presentation table lives ONCE, in the SPA
(P4.D144).** v4's `concierge-state-presentation.ts` has no server
consumer (its readers are ChatSidebar, SalonView and ConciergeMark), so
v5 ports it as `apps/web/src/app/chat/concierge-state-presentation.ts`
only — P4.D143 builds NO Rust twin and emits NO presentation string. The
two lanes share ONE predicate name instead: `conciergeStateUsesUncensored
Route(state)` = `state === 'flagged' || state === 'uncensored'`, landed
as `concierge_state_uses_uncensored_route` in `chat_override.rs` (D143,
`should_use_uncensored_route` delegating) AND in `chat/concierge-state.ts`
(D144, `shouldUseUncensoredRoute` delegating), each pinned by its own
transcription of v4's `it.each(TABLE)`. The four `detail` sentences, the
four labels, the icon names (`eye`, `alert-triangle`, `check-circle`,
`eye-off`), the tones (`success`, `danger`, `muted`, `info`), the suffix
rule (`''`/`-muted`/`-info`, success → `''`), the text classes and the
hint sentence are byte-exact to v4's module and mechanically diffed
against it by a D144 spec.

**§H — the `has-dangerous` probe (P4.D143 ↔ P4.D144), Tier 2 in both.**
v5 never had v4's `GET /api/v1/chats?action=has-dangerous`; v4
`c43d3b1b4` re-based it onto the uncensored route. D143 lands the verb
**`chatsHasDangerous`** (`{ type: 'chatsHasDangerous' }` → `{
hasDangerous: boolean }`, true iff any chat of the user satisfies
`should_use_uncensored_route`) plus the REST edge under `CHAT_GET_
ACTIONS`; D144 consumes it as the third arm of `hasQuickHideFeatures`
and gates the footer's quick-hide section on it exactly as v4's
`sidebar-footer.tsx:145` does, behind the same ACTIVATE-AT-UNIFY
constant as its beats. Until both land, the SPA's third arm stays the
recorded omission; neither lane fakes the other's half.

**§C — the `story_background_job.rs` region split (P4.D145 ↔
P4.D146).** The legacy-folder ensure (`ensure_legacy_folder` `:1425-1449`
+ its private `find_folder_by_path` `:1451-1471`, both DELETED in favour
of the `ensure_by_path` chokepoint) belongs to P4.D145; the back-fill's
participant exclusion set (`:706-728`) belongs to P4.D146. Neither lane
edits the other's region. The unifier cherry-picks **P4.D145 before
P4.D146** and hand-resolves any import-block union. Pin awareness:
D145's pin `a5df98b3f` has `70505745a` as an ancestor, so a D145 regen of
`story_background_job_tier3_equivalence` or `title_update_tier3_
equivalence` carries D146's absent-participant predicate against a v5
that has not ported it — D145 measures the shape FIRST (the committed
story fixture seeds every participant `active`, so the predicate may not
even discriminate) and masks only as a named
`ABSENT_PARTICIPANTS_PENDING_P4D146` tripwire with a shape assertion +
mutation proof (the P4.D141 / `LAST_MESSAGE_AT_PENDING_P4D140`
precedent), retired by the unifier once both lanes sit on one branch.

**§D — `api/projects.rs` region split (P4.D143 ↔ P4.D146).** The
project-chats serializer (the `?action=chats` shape, §A) belongs to
P4.D143; the background-mode twins (`project_background_get`
`:939-1022`, the `PROJECT_UPDATE_SCHEMA` `backgroundDisplayMode` rule
`:398-425`) belong to P4.D146. Neither lane edits the other's region; the
unifier resolves import unions. **The same split applies to
`projects_routes_equivalence` (+ `cases/projects-routes.test.ts` + its
fixture):** D143 owns the project-chats arms, D146 the background-mode /
update-schema arms; each lane appends its own cases in its own region and
regenerates the oracle from ITS pin — D143's pin (`c43d3b1b4`) also
moves D146's arms, so D143 masks them by the named
`BACKGROUND_MODE_PENDING_P4D146` tripwire; D146's pin (`70505745a`)
predates D143's change, so its regen leaves the chats arms as they are.
The unifier regenerates once at the new baseline and deletes the
tripwire.

**§E — `core-contract.ts` region scoping.** P4.D144 owns the chat-list DTO
regions (§A) + the `ConciergeState` union + any `ConciergeMark` input
types; P4.D146 owns the two project `backgroundDisplayMode` unions
(`:1504`, `:3622`); P4.D147 touches the file NOT AT ALL (every verb it
needs exists); P4.D143/P4.D145 are server-only. The unifier diffs the
contract name-for-name against the Rust `CoreResponse` shapes (playbook
§D).

**§F — the restore orchestrator region split (P4.D145 ↔ P4.D146).**
`services/backup/restore/orchestrator.rs`: the §17 Folders loop
(`:855-880` — the quiet duplicate-drop arm) belongs to P4.D145; the
`project_properties` carry (`:2130-2145` — whether `backgroundDisplayMode`
is normalized on restore, measured against v4) belongs to P4.D146.
Neither lane edits the other's region; the unifier cherry-picks D145
first and resolves any import union.

**§G — the folders route wire (P4.D145 ↔ P4.D147).** P4.D145 changes the
INTERNALS of `filesFolderCreate` / the parent-ensure (the `ensure_by_path`
chokepoint) but the wire shape and statuses are UNCHANGED (201
`{folder, alreadyExists: false, message: 'Folder created successfully'}`;
200 `{folder, alreadyExists: true, message: 'Folder already exists'}`; no
409 arm). P4.D147's picker consumes `filesList` + `filesFoldersList` +
`filesFolderCreate` exactly as the library picker does today and must
not depend on any D145 change; if D147's beat sees a duplicate-path 201
carrying a pre-existing row id, that is D145's recorded semantic shift,
not a defect.

## Ownership (binding, identical across all five orders)

| Area | Owner |
|---|---|
| `quilltap-core`: NEW `services/dangerous_content/concierge_state_presentation.rs`, `services/dangerous_content/chat_override.rs`, `services/chat_enrichment.rs`, `services/home.rs`, `api/salon.rs` (the chat-list serializer), `api/characters.rs` (the conversations serializer), `api/projects.rs` §D chats region, `services/message_finalizer.rs` (the `trigger_chat_danger_classification` guard ONLY), the §H `chatsHasDangerous` verb (`api/types.rs` variant + `api/engine.rs` dispatch + the `quilltap-web` chats REST edge); harness: `home_routes_equivalence`, the salon chat-list / characters / projects routes families, the chat-override + danger-trigger families (+ their oracle cases and fixtures) | **P4.D143** |
| SPA: NEW `chat/concierge-mark.ts` (+spec), `chat/concierge-state.ts` (+spec), `quick-hide/**`, `screens/home/recent-chats-section.ts` + `recent-chat-item.ts`, `screens/salon/salon-list.ts` + `chat-card.ts`, `chat/sidebar/chat-section.ts`, `screens/prospero/cards/project-chats-section.ts`, `screens/characters/view/tabs/conversations-tab.ts` + `character-conversation-card.ts`, `chat/conversation-header.ts` (the pill reads the shared table), `styles/qt-components/_chat.css` (the `.qt-concierge-mark` family), `core-contract.ts` §E regions, the gated quick-hide/marks beats | **P4.D144** |
| `quilltap-core`: `db/folders.rs`, NEW `db/folders_unique_path_repair.rs` (or the lane's name, + `db/mod.rs` line), NEW `db/sqlite_errors.rs` (or wherever the unique-constraint predicate lands), `services/character_avatar_job.rs` (the legacy-folder site), `services/story_background_job.rs` §C folder region, `api/files.rs` (both create paths), `services/quilltap_import/files.rs`, `services/backup/restore/orchestrator.rs` §F folders region, `services/builtin_mounts.rs` / `services/provisioning/mod.rs` (the ensure call), `write_partition.rs` (the one assertion); `quilltap-host`: `host.rs` (the boot repair chain block); harness: NEW `folders_collapse_heal_equivalence`, `folders_tier2_equivalence`, `files_routes_equivalence`, `qtap_import_equivalence`, the `system_restore_*` folder arms + the NEW duplicate-folders archive, the avatar/story tier-3 legacy-folder arms, `provisioning_equivalence` (run, not changed); `docs/v4/` mirror rows for `DDL.md` | **P4.D145** |
| `quilltap-core`: `chat_predicates.rs` (the str→status helper), `services/image_profile_resolution.rs`, `services/story_background_job.rs` §C participant region, `services/title_update_job.rs`, `api/chat_media.rs`, `db/projects.rs`, `api/projects.rs` §D background region, `services/backup/restore/orchestrator.rs` §F project-properties region; harness: `story_background_job_tier3_equivalence` + its builder, `title_update_tier3_equivalence` + the `cost-background` pair, `cost_background_routes_equivalence`, `projects_routes_equivalence`, `projects_tier2_equivalence`, the retired-mode restore archive if built; SPA: `screens/prospero/cards/project-image-generation-card.ts` (+spec), `core-contract.ts` §E region, the gated two-option beat | **P4.D146** |
| SPA: `screens/files/move-to-project-dialog.ts` (+spec), NEW `screens/files/folder-picker.ts` (+spec), the LIVE Move-to-Project beat | **P4.D147** |
| Playwright (port 4319) | **P4.D147's branch only**; every other lane's beats are authored gated (ACTIVATE-AT-UNIFY) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it |

**Version bumps:** this lane bumps `quilltap-core`, `quilltap-harness`,
`quilltap-web` (if a route test moves), and the SPA per commit; the
unifier recounts at unification (three lanes bump the SPA).

## Verification gate before declaring done

`cargo fmt --check`; `cargo clippy` both feature sets; full
`cargo test --workspace` with the lane's oracle env vars
(`run_in_background`, full log + sentinel — NEVER a pgrep loop, NEVER
`| tail`); the lane's families by name through the sweep driver, zero
SKIP, from the `70505745a` pin with changed-bytes greps (`present in
chat`, the status list, `theme`); mutation proofs verified-applied;
`npm test` + `npm run build` in `apps/web`; NO Playwright (the beat is
gated). Per-commit rule: `.claude/commands/commit.md`.
