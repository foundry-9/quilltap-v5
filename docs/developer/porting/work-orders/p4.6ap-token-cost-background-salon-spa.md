# Work order P4.6ap — the token-cost + background-generation Salon SPA lane (lane B): the token/cost display, the Story Backgrounds card, the regenerate entry + polls

> **STATUS: CLOSED — UNIFIED on main (2026-07-15).** All Tier 1 + Tier 2
> LANDED: the format-tokens leaf (37-case table GENERATED from v4's real
> function), the per-message token badge (v4's mount gate; the
> `showPerMessageCost` deadness and `showSystemEvents` inertness ported AS
> dead with why-comments; one pinned divergence — v4's `&&`-chain renders a
> literal "0" for zero-count rows, v5 renders nothing), the compact
> chat-totals header summary (refetch keyed on the CANONICAL message count,
> all four suppression rules), the Story Backgrounds Images-tab card
> (whole-bag PUT, `[selected]`-per-option), the Regenerate Background
> header entry (relocated from v4's unported ChatSidebar; `sparkles` glyph
> — `image` already means the gallery there) + both polls (active 5s×36
> stop-on-change with v4's re-entrancy quirk reproduced; passive 30s gated
> by `enabled`, display unconditional). Four e2e beats — at unification the
> two ACTIVATE-AT-UNIFY beats went LIVE (mocks deleted; the activation
> surfaced the e2e image_profiles ownership gap, fixed in global-setup).
> **Tier-3 deferrals (loud, named):** the LLM-Inspector button, the
> default/boxed summary variant + `detailed=true`, the Default Aesthetics
> Images card, project/workspace backdrop arbitration, the ChatSidebar
> itself.
>
> **Original status (for reference): OPEN.**
>
> Lane B of the THREE-lane P4.6ao ∥ P4.6ap ∥ P4.6aq round (the token/cost +
> background-generation + form-fields round, planned 2026-07-15). **v4
> reference baseline: HEAD `02865bdb`** (`~/source/quilltap-server`) — run
> `git log 02865bdb..HEAD --oneline` at lane start; **if HEAD moved, STOP and
> report.** Sibling lanes: `p4.6ao-token-cost-background-server.md` (provides
> the `chatGetCost` verb and the `regenerate-background` un-refusal — see the
> Shared contract), `p4.6aq-form-field-adoptions.md` (the `qt-markdown-field`
> rider). The **Shared contract** and **Ownership** sections below are
> binding and reproduced verbatim in all three orders. You run in a worktree
> (own Rust binaries for e2e; local playwright bin from `apps/web`; port-4319
> contention across worktrees — the P4.6af memory notes); the unification
> pass integrates the lanes onto main.

## Mandate

Close the round's two named SPA surfaces: **the Salon token/cost display**
(the P4.6an deferral — v4's `MessageRow`/`MessageActionBar` per-message token
badge and the `showChatTotals` header summary over `tokenDisplaySettings`,
whose settings CARD already landed in P4.6an) and **the story-background
generation client surface** (the Story Backgrounds card in the Images
settings tab, the Regenerate Background action, and the two polls — the
display side landed in P4.6ak/P4.6am and is not re-opened). This lane owns
the Salon conversation surface, the chat message components, and the Images
settings tab. It must NOT touch `crates/**` (lane A), `apps/web/src/app/
editor/**` or any lane-C site file (lane C), or `chat/render/**` (the
standing HANDS-OFF render pipeline).

## Survey-verified starting points (2026-07-15 @ `02865bdb` — do not re-derive)

### The `tokenDisplaySettings` shape

Four booleans, all defaulting false — Zod source of truth
`lib/schemas/settings.types.ts:264-273`: `showPerMessageTokens`,
`showPerMessageCost`, `showChatTotals`, `showSystemEvents`. The v5 Chat-tab
"Token Display" card already edits this bag (P4.6an, one shared
`['chatSettings']` query); this lane only CONSUMES it. Two v4 quirks to
carry faithfully:

- **`showSystemEvents` is inert in v4** — declared, parsed, defaulted, read
  by no renderer. Port the inertness (a why-comment where the other three
  are consumed), don't invent a consumer.
- **`showPerMessageCost` is dead in v4** — `MessageActionBar` never passes
  `estimatedCostUSD` to `TokenBadge` (`message-row/MessageActionBar.tsx:
  198-204`), there is no cost field on the Message type, so the cost branch
  never renders per-message. Port the deadness (accept the flag, render
  nothing), carry the why-comment. Do NOT invent a per-message cost source.

### The per-message token badge

- **Placement** — v4 `MessageActionBar.tsx:195-206`: in the
  `qt-chat-message-action-timestamp` row, after the timestamp. Gate:
  `showPerMessageTokens && (message.promptTokens || message.completionTokens)`
  — the badge only mounts when the flag is on AND a count is non-zero
  (JS-truthy). No per-role gating.
- **`TokenBadge`** — `components/chat/TokenBadge.tsx`: returns null if
  `total === 0` or both flags off (`:32-35`). Token text (`:41-48`):
  `<prompt> / <completion> tokens` with `title="Prompt tokens"` /
  `"Completion tokens"` spans.
- **Formatting** — `lib/utils/format-tokens.ts`: `formatTokenCount`
  (`:34-42`): `>=1M → "2.3M"`, `>=1K → "1.5K"`, else the raw integer.
  `formatCostForDisplay` (`:11-29`): `null → "N/A"`, `0 → "Free"`,
  `< $0.01 → toFixed(4)`, `< $1 → toFixed(3)`, else `toFixed(2)` — port both
  as a v5 util with unit specs (exact strings).
- **Data** — already served: v5's message reads emit
  `tokenCount`/`promptTokens`/`completionTokens`
  (`crates/quilltap-core/src/db/chats_messages_read.rs:75-77,164-166`) and
  they're stored ACTUALS from provider usage (v4
  `message-finalizer.service.ts:623-625`), not estimates. No new fetch.
- **v5 landing spot** — `apps/web/src/app/chat/message-row.ts` (`:34` doc
  already names "a timestamp/token line"; the action bar is at `:147-148`).
  Thread `tokenDisplaySettings` from the shared `['chatSettings']` query in
  `screens/salon/salon-conversation.ts` (the same place
  `textReplacementsEnabled` and `composerSpellcheck` come from — no new
  request).

### The chat-totals header summary

- **v4** — `SalonView.tsx:990-1027`: toolbar right-content renders
  `<ChatCostSummary chatId show={showChatTotals} variant="compact"
  refreshKey={messages.length} />` when
  `tokenDisplaySettings.showChatTotals`. (The LLM-Inspector button rendered
  by the same effect is a SEPARATE feature — deferral, below.)
- **`ChatCostSummary`** — `components/chat/ChatCostSummary.tsx`: fetches
  `GET /api/v1/chats/{chatId}?action=cost` on mount and when `refreshKey`
  changes (`:58`). Compact variant (`:106-135`): `"<total> tokens"` (number
  only on mobile) + `"•"` + `formatCostForDisplay(estimatedCostUSD)`;
  `priceSource === 'openrouter-estimate'` → info icon, tooltip `"Cost
  estimated using OpenRouter pricing data. Actual cost may vary."`;
  `'unavailable'` → `*` with `title="Pricing data unavailable"`. Returns
  null when `!show`, while loading (compact), or `totalTokens === 0`
  (`:86-103`). Salon uses compact only — port compact; the default variant
  is a deferral.
- **v5 landing spot** — `apps/web/src/app/chat/conversation-header.ts`
  (`:11` names "the cost/inspector cluster" as the deferral this fills);
  the header already hosts the Edit-Enclave / gallery / copy-id entries.
  Fetch through the Shared-contract §1 `chatGetCost` verb (a lane-B-owned
  `chat-cost.api.ts`, route-mocked until unification), refetch keyed on the
  message count.

### The Story Backgrounds settings card

- **v4** — `components/settings/chat-settings/StoryBackgroundsSettings.tsx`,
  mounted in the **Images tab** (`components/settings/tabs/
  ImagesTabContent.tsx:39-46`), NOT the Chat tab. Two controls only: the
  enable toggle → `storyBackgroundsSettings.enabled` (`:38-43`); the image
  profile `<select>` → `storyBackgroundsSettings.defaultImageProfileId`
  (`:65-77`, "Use default profile" = null). Card copy includes the
  "generated automatically when the chat title is updated" line (`:49-50`).
- **Schema** — `StoryBackgroundsSettingsSchema`,
  `lib/schemas/settings.types.ts:415-420`: `enabled` default false;
  `defaultImageProfileId` nullable-optional UUID.
- **v5 landing spot** — `apps/web/src/app/screens/settings/images/
  images-tab.ts` (today: only the Image Profiles card). Reuse the P4.6an
  `ChatSettingsCard` substrate + the shared `['chatSettings']` key. **PUT
  the whole nested bag, never a partial patch** (the standing nested-bag
  rule), and bind the async-options profile select `[selected]`-per-option,
  never `[value]` (the standing dogfood-#6 rule).

### The Regenerate Background action + the polls

- **v4 button** — lives in the ChatSidebar tool palette
  (`components/chat/ChatSidebar.tsx:1204-1214`): gated
  `storyBackgroundsEnabled && onRegenerateBackgroundClick`, icon `image`,
  label `Regenerate Background`, title
  `"Regenerate story background image"`. **v5 has no ChatSidebar** (the
  god-component's sidebar is unported — `salon-conversation.ts:95`); the
  established v5 placement for relocated sidebar/toolbar actions is the
  conversation-header entry cluster (the P4.6af Edit-Enclave idiom). Gate on
  `chatSettings.storyBackgroundsSettings.enabled` (v4
  `chatControls.storyBackgroundsEnabled`).
- **v4 click handler** — `app/salon/[id]/hooks/useChatControls.ts:397-416`:
  POST `?action=regenerate-background`, toast, then start the ACTIVE poll.
  Read that range for the exact toast copy and error handling; carry it.
- **v4 polls** — `hooks/useStoryBackground.ts`: the PASSIVE poll is a 30s
  `refetchInterval` on the get-background query, enabled only while
  `storyBackgroundsSettings.enabled` (`:68`) — **display stays
  unconditional; the flag gates only polling** (the P4.6am survey rule).
  The ACTIVE poll (`startPolling`, `:95-128`): every 5s, max 36 polls
  (3 min), stops when the resolved URL changes from its captured initial
  value or the budget runs out; on change fires the background-var refresh.
- **v5 landing spot** — `screens/salon/story-background.api.ts` (owns
  `fetchChatBackgroundVar` + `storyBackgroundKeys` today) grows the poll
  machinery; `salon-conversation.ts` already binds
  `--story-background-url` (`:126`) and refetches on the key — extend, don't
  rebuild. The regenerate dispatch uses the EXISTING wire name
  `chatRegenerateBackground` (Shared contract §2) — route-mocked until
  unification.

## Tiered deliverables

**Tier 1 (must land):**

1. The `format-tokens` util (both functions, exact-string unit specs) + the
   token badge in `message-row.ts` with v4's mount gate, threading
   `tokenDisplaySettings` from the shared settings query.
2. The compact chat-totals summary in `conversation-header.ts` over
   `chatGetCost` (§1), with the priceSource markers and the null/loading/
   zero-total suppressions.
3. The Story Backgrounds card in `images-tab.ts` (whole-bag PUT,
   `[selected]`-per-option, v4 copy).
4. The Regenerate Background header entry + toast + the active 5s/36 poll,
   and the passive 30s poll gated by `enabled`.

**Tier 2 (should land):**

5. Live e2e beats (own new spec file(s), shared global-setup server,
   filename sorted after `foundation.spec.ts`): (a) token badge + totals —
   seed token fields on the salon fixture's messages and aggregates on the
   chat row in `global-setup.ts` (probe-guard the seam you seed through;
   settle virtualized counts — the P4.6ae/af lessons); toggle the Token
   Display card flags through the real dispatch, assert badge text and the
   header summary; (b) the Story Backgrounds card round-trip
   (toggle → reload → persisted); (c) the regenerate entry:
   `ACTIVATE-AT-UNIFY` — route-mocked in-lane, live at unification, and
   scoped to the QUEUED response/toast (the e2e host has no image-provider
   key; the job's outcome is out of scope — assert the enqueue, not the
   image).

**Tier 3 (explicit loud deferrals — name them, never stub silently):**

- The LLM-Inspector toolbar button (v4 `SalonView.tsx:990-1027`, gated by
  `llmLoggingSettings.enabled`) — a separate feature, stays deferred.
- `showSystemEvents` (inert in v4) and per-message cost (dead in v4) —
  ported AS inert/dead with why-comments; not deferrals, but say so in the
  status log so they aren't re-reported.
- `ChatCostSummary`'s default (non-compact) variant and the
  `detailed=true` arm (no v4 client calls it).
- Project-page backgrounds / workspace-backdrop arbitration (the standing
  P4.6am deferral).
- The unported ChatSidebar itself — the regenerate entry relocates ONE
  button; the palette stays deferred.

## The differential requirement (restated)

This is an SPA lane: the proof is component/unit specs (payload-asserting,
per the P4.6an card conventions and the P4.6ag zoneless idioms) + the live
e2e beats. Server behavior is lane A's differential surface — do not
duplicate it; consume the Shared-contract shapes and mock them faithfully
(copy the contract JSON verbatim into the mocks so unification can drop them
without rewrites).

## Fixtures

This lane consumes the committed salon e2e fixture family and may EXTEND it
only via `global-setup.ts` seeding (token fields on messages, chat-row
aggregates, a `storyBackgroundsSettings` bag) — the committed fixture files
themselves are not regenerated by this lane. Remember the fixture lessons:
global-setup copies the instance FRESH each run (read the fixture before
blaming run order); normalize starting state, don't assert arrival values.
Lane A's new `cost-background-*` fixture family is NOT consumed here.

## Shared contract (BINDING — reproduced verbatim in all three orders)

**§1 — the `chatGetCost` verb (lane A provides; lane B consumes).**

- Wire request: `{"type": "chatGetCost", "chatId": "<uuid>", "detailed": <boolean, optional — absent means false>}`.
  Rust: `Request::ChatGetCost { chat_id: String, detailed: Option<bool> }`,
  serde camelCase, appended to the union in
  `crates/quilltap-core/src/api/types.rs`.
- Response: `Response::ChatCost(Value)` carrying v4's breakdown object
  VERBATIM.
  - Non-detailed (v4 `getChatCostBreakdown`,
    `lib/services/cost-estimation.service.ts:139-190`):
    `{"totalTokens", "promptTokens", "completionTokens", "estimatedCostUSD"
    (number|null), "priceSource" ("openrouter"|"registry"|"fallback"|
    "openrouter-estimate"|"unavailable")}`.
  - Detailed (`:196-289`): the same five keys plus
    `"messageBreakdown": [{id, promptTokens, completionTokens, totalTokens,
    cost, source}]` and `"systemEventBreakdown": [{id, type, promptTokens,
    completionTokens, totalTokens, cost, source}]`.
- REST edge: `GET /api/v1/chats/{id}?action=cost[&detailed=true]` returns the
  breakdown **RAW** (v4 `NextResponse.json(breakdown)` — NOT the
  successResponse envelope). Missing chat row → 404 `notFound('Chat')`.
- The SPA client type is `ChatGetCostRequest` in a lane-B-owned api file;
  lane B develops against a route mock until unification.

**§2 — the `chatRegenerateBackground` edge (lane A un-refuses; lane B
consumes).**

- The `Request::ChatRegenerateBackground { chat_id }` variant already exists
  (`types.rs:1832-1842`, wire name `chatRegenerateBackground`) — the variant
  and wire name DO NOT change; only the dispatch target does.
- Success (both arms) is v4's successResponse data:
  `{"message": "Story background regeneration queued" |
  "Story background generation already in progress", "queued": true,
  "jobId": "<uuid>"}`.
- The three badRequest strings, verbatim:
  1. `Story backgrounds are not enabled. Enable them in Settings > Chat Settings > Story Backgrounds.`
  2. `No image profile available for story background generation. Configure an image profile in Chat Settings.`
  3. `No characters in chat to generate background for.`
- Failure fallback: serverError
  `Failed to queue story background regeneration`.

**§3 — merge mechanics.**

- Lane B beats that need lane A's live verbs run ROUTE-MOCKED in-lane and are
  marked `ACTIVATE-AT-UNIFY` in the spec; the unifier drops the mocks and
  runs them live (the established P4.6ab/ac pattern).
- `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` are
  append-only for all lanes (union-merge at unification).
- Version bumps: lane A bumps the crates it touches (core, web, host,
  harness) per commit; lanes B and C each bump the SPA
  `apps/web/package.json` patch version per commit; the unifier accumulates
  and syncs the LOCKFILE's own version fields (the P4.6an lesson).

## Ownership (BINDING — reproduced verbatim in all three orders)

- **Lane A (P4.6ao)** owns `crates/**`, `harness/oracle/**`, and the new
  `cost-background-*` fixture family + generator. It does NOT touch
  `apps/web/**`.
- **Lane B (P4.6ap)** owns `apps/web/src/app/chat/**` (message-row,
  conversation-header + their specs), `apps/web/src/app/screens/salon/**`,
  `apps/web/src/app/screens/settings/images/**`, the new shared UI it
  creates (token badge / cost summary / format-tokens util under
  `apps/web/src/app/ui/` or a salon-local dir), its own new e2e spec files,
  and `apps/web/e2e/global-setup.ts` seeding edits. It does NOT touch
  `apps/web/src/app/editor/**`, any lane-C site file, or
  `apps/web/src/app/chat/render/**` (the standing HANDS-OFF render
  pipeline).
- **Lane C (P4.6aq)** owns `apps/web/src/app/editor/markdown-field.ts`
  (+ spec) and exactly its enumerated site files (character-edit
  scenario-editor, scenarios scenario-editor-modal, settings
  template-form-modal, characters prompt-modal, characters edit
  appearance-tab, characters view character-appearance-tab, prospero
  project-wardrobe-manager / project-settings-card /
  project-aesthetic-field, new-chat-form — plus their specs). It does NOT
  touch `apps/web/src/app/chat/**`, `screens/salon/**`,
  `screens/settings/images/**`, or `editor/rich-editor.ts` (a needed
  rich-editor change is an escalation, not a quiet edit).
- Everyone: `docs/CHANGELOG.md` and `status-log.md` append-only.

## Verification gate (before declaring the lane done)

1. `ng test` (from `apps/web`) — zero failures, new specs included.
2. `ng build` — clean.
3. The lane's e2e specs green in isolation AND in-suite (local playwright
   bin from `apps/web`; own Rust binaries in the worktree; the port-4319
   contention note). `ACTIVATE-AT-UNIFY` beats green against their mocks.
4. `cargo test --workspace` untouched-green (you changed no crate — prove
   it).
5. Per-commit rule: follow `.claude/commands/commit.md` (version bumps,
   CHANGELOG every commit, status-log append).
