# Work order P4.9E4B — the LibraryFilePickerModal SPA + the small SPA riders

> **STATUS: OPEN — not started (written 2026-07-27).** Lane 2 of 3 in the
> "library picker + embedding remainder" round. **SPA-only: this lane owns
> all of `apps/web/**` and touches no Rust.**
>
> **v4 baseline for this round: `e8a49597`** (v4 HEAD, 2026-07-27,
> 4.8.0-dev.108). Verified clean at planning; no drift debt.
>
> **Drift-check at lane start**: in `~/source/quilltap-server` run
> `git log e8a49597..HEAD --oneline` and `git status --short`. If v4 moved
> past `e8a49597`, or the tree is dirty, **STOP and report before porting
> anything.**
>
> **Siblings (run in parallel, disjoint ownership — see §Ownership):**
> - **P4.9E4A** — the library-picker server remainder
>   (`p4.9e4a-library-picker-server.md`). Delivers the
>   `attach-mount-file` REST leg this modal's mount-file and gallery picks
>   need. The §Shared contract below is binding and identical in both
>   orders. Until it unifies, gate the attach beats ACTIVATE-AT-UNIFY (the
>   established named-constant pattern) — the legacy `action=link` leg is
>   already live on main and its beats can run now.
> - **P4.6BM** — the embedding-family remainder
>   (`p4.6bm-embedding-remainder.md`). No dependency either way.
>
> **Worktree note:** run on your own branch in your own checkout. Do **not**
> use `git stash` or `git worktree` inside the v5 repo.
>
> **Harness notes inherited from P4.9E3C (both cost real time there):**
> `npx playwright` MUST run with cwd at `apps/web`; a dialog's component host
> element is zero-sized — locate by `getByRole('dialog')`; backticks inside
> an Angular template literal terminate it — write v4 file references in
> template comments bare.

## Mandate

Port v4's **`LibraryFilePickerModal`** (`components/chat/
LibraryFilePickerModal.tsx`, 616 LOC) — the last deferred-by-name dialog in
the chat surface (m6 §2.2 row, item 10's remainder) — plus the composer
gutter entry that is currently ABSENT rather than refusing. Then take three
small riders the survey verified as SPA-only: **ProjectToolSettingsModal**
(its server side is fully landed — verb, storage, contract all live), the
**RNG-dropdown residuals** (the dropdown itself already landed with P4.9E1B;
only click-outside dismissal and the trigger spinner are missing), and the
**`allowToolUse` disposition** (the P4.9E3C escalation is closed as
v4-faithful — record it, don't port it).

**Must NOT touch:** anything outside `apps/web/**` except the docs files
named in §Ownership. The Rust lanes own the crates.

## Survey-verified starting points (2026-07-27 — do not re-derive)

> Surveyed at v4 `e8a49597`, v5 main (SPA 0.5.311).

### 1. The modal — `components/chat/LibraryFilePickerModal.tsx` (616 LOC)

Mount chain: `ChatModals.tsx:246` (props `:60-61`); Salon wiring
`SalonView.tsx:1630-1631`, opener relayed `:1492` →
`ChatComposer.tsx:74,128,348` → **the gutter button**
`ComposerGutterTools.tsx:90-100` (row 2 col 1, icon `file-plus`,
title/aria "Attach file from library"); open state
`useModalState.ts:38,77-78,124-125`.

Props (`:25-50`): `isOpen`, `onClose`, `chatId`,
`onFileLinked(file {id, filename, filepath, mimeType, url})`,
`onMountFileAttached?()`. The two callbacks do DIFFERENT things
(`ChatModals.tsx:250-266`): a linked legacy file goes into the **composer's
pending-attachment tray** (the next send picks it up); an attached mount
file triggers only `fetchChat()` — the Librarian announcement is already a
transcript message, nothing enters the tray.

Two steps (`type Step`, `:103`):
`'scope' | 'browse-project' | 'browse-gallery' | 'browse-mount'`.

**Step 1 — `ScopePicker` (`:418-511`)**, render order:
1. Always: **General** (`projectId=null`, `:450`) and **My Gallery /
   "{Persona}'s Photos"** (`:451`).
2. **Group Files** (`:454-467`) — from `?action=group-stores` (v5 verb
   `ChatGroupStores` is on main and mirrored, `core-contract.ts:565-569` —
   this lane is its FIRST consumer, closing the "mirrored, unconsumed" note).
3. **Projects** (`:473-485`) — `GET /api/v1/projects` (v5 `ProjectList`).
4. **Document Stores** (`:491-504`) — `GET /api/v1/mount-points` (v5
   `MountPointList`), filtered at `:133-135` to
   `enabled && mountType === 'database' && storeType !== 'character'` —
   character vaults are deliberately off-limits to the human composer;
   carry the filter and its why-comment.

Photo albums (`?action=photo-albums`, `:140-145`) are fetched **only** to
resolve the gallery's display name + target character: `userPersonaAlbum`
(`:156-159`) picks `kind==='character' && isUserCharacter && isDefault`,
falling back to the first `isUserCharacter`. They are NOT pickable scopes.

**Step 2:** `browse-project` (`:384-393`) and `browse-mount` (`:395-405`)
delegate to `FileBrowser` with **`showUpload={false}`** (so its
upload/delete/sync legs are unreachable — v5's port needs only the listing
surface); `browse-gallery` (`:407-413`) is a bespoke thumbnail grid
(`GalleryPanel`, `:538-608`) over
`GET /api/v1/characters/{id}/photos?limit=200` (`:557`) or
`GET /api/v1/photos?limit=200` when no persona (`:558`); `LinkingOverlay`
(`:610-616`) seals the panel while `linking`.

**Pick behavior** (`handleFileClick`, `:228-297`): the discriminator is
`isMountFile = !!file.mountPointId && !!file.relativePath` (`:237`;
`FileInfo` fields `components/files/types.ts:26-27`).
- Mount file → `attachMountFile` (`:173-200`) →
  `POST …/files?action=attach-mount-file` `{mountPointId, relativePath}`;
  success → toast `Attached "X" — the Librarian has noted it`,
  `onMountFileAttached()`, `onClose()`.
- Legacy file → `POST …/files?action=link` `{fileId}` (`:248-255`);
  `data.file` projected into `onFileLinked` (`:272-278`), `onClose()`.
- Gallery picks (`:299-320`) **always** go through `attachMountFile` —
  gallery entries are mount files by construction.

### 2. What v5 already has to build on

- The legacy FileBrowser family: `screens/files/` (`files-browser.ts`,
  `file-grid.ts`, `file-list.ts`, `file-model.ts`, `file-thumbnail.ts`,
  `file-preview-modal.ts`) — P4.6af's, with the transitive endpoints live
  (`mountFilesList` `core-contract.ts:4477`, project `list-files`,
  `files?filter=general`, folders). Reuse it in-place if its inputs allow a
  read-only no-upload mode; otherwise port the picker's narrower browse
  panel — record the choice. Scriptorium's `file-table.ts` /
  `directory-picker.ts` are the other candidates.
- The gutter slot is reserved: `chat-composer.ts:58-64` records the fill
  order ("Library file belongs to `p4.9e3`"), template comment `:212-214`.
- The tier-3 note to retire: `screens/salon/salon-conversation.ts:1156-1170`
  ("rendered nowhere, nothing stubbed").
- Photos REST legs live: `characters_routes.rs` via
  `screens/characters/characters.api.ts:112`; `PhotoGalleryList` via
  `screens/photos/photos.api.ts`.
- `ChatFileLink` REST leg live at the edge (`files_routes.rs:859-878`) —
  call it as raw REST through `apiUrl()`, as v4 does.

### 3. Rider A — ProjectToolSettingsModal (server side COMPLETE; SPA-only)

v4: `components/tools/tool-settings/ProjectToolSettingsModal.tsx` (145 LOC),
sole opener `app/prospero/[id]/components/ModelBehaviorCard.tsx:12,169-176`
("Default Tool Settings" row + `toolSummary` + Configure, `:150-164`; seeds
from `project.defaultDisabledTools`/`defaultDisabledToolGroups` `:39-40`).
Reads `GET /api/v1/tools` gated on `isOpen` (`:41`); writes
`POST /api/v1/projects/{id}?action=update-tool-settings`
`{defaultDisabledTools, defaultDisabledToolGroups}` (`:59-67`). Passes
`showAvailability={false}` (`:112`) and the project `footerNote` (`:115`:
"Configure which tools are enabled by default for new chats in this project.
Existing chats are not affected."). Save gated on set-vs-set `hasChanges()`
(`:82-101`).

v5, all verified live: `qt-tool-settings-content`
(`chat/tools/tool-settings-content.ts`, 410 LOC) already takes
`showAvailability`/`footerNote` and honours `showAvailability=false` in the
arithmetic (`:244-248,:259-261`); `listTools` already has optional `chatId`
(`chat/chat-admin.api.ts:169-176`; Rust `ToolsList` `types.rs:2614-2620`);
the write verb `ProjectToolSettingsUpdate` is live end-to-end
(`api/projects.rs:696-733`, `engine.rs:2458-2473`,
`core-contract.ts:1487-1494` — its doc comment "the SPA row is deferred"
gets updated); storage always materializes both arrays
(`db/projects.rs:49-52,133-134`) and `project_get` spreads them out
(`api/projects.rs:275-282`). The stub to replace:
`screens/prospero/cards/project-model-behavior-card.ts:108-121` (hardcoded
"All tools enabled" + a `disabled` Configure button; deferral note
`:19-20`). **One contract gap to close:** `ProjectDetail`
(`core-contract.ts:2982-3004`) does not declare the two fields — they arrive
via the index signature; declare them named.

### 4. Rider B — RNG-dropdown residuals (the dropdown already LANDED)

The phase-4/m6 "the dropdown itself is still owed" note is **stale**: v5's
`chat/rng-dropdown.ts` (279 LOC, landed P4.9E1B `35367af7`) already carries
DICE_TYPES d6/d20 with ± spinners, Flip Coin / Spin the Bottle, the Custom
Roll sub-panel with 2–1000 / 1–100 validation, the error line, and the
preview pending chip. Three residual deltas vs v4
(`components/chat/RngDropdown.tsx`):
1. **No click-outside dismissal** — v4 `useClickOutside` (`:78-83`) closes
   the menu and custom panel; v5 closes only on toggle or after a roll.
2. **No trigger spinner** — v4 swaps the dice icon for a spinner while
   loading (`:187-195`); v5 renders the dice icon unconditionally (`:73`).
3. The header comment at `rng-dropdown.ts:46-48` still *describes* the
   dropped legacy non-preview path as "kept" — correct the comment (the
   `pendingResult` output is unconditional; `variant='palette'`/`onClose`
   were deliberately dropped, there is no second mount in v4 either —
   verified: `ComposerGutterTools.tsx:132-139` is the only live mount).
Fix 1 and 2, correct 3; component-test both behaviors.

### 5. Rider C — the `allowToolUse` disposition (record, do NOT port)

P4.9E3C escalated the missing `allowToolUse` projection as a server gap. The
survey closed it: **v4 does not project it either** —
`getConnectionProfile` (`chat-enrichment.service.ts:354-379`) returns exactly
`{id, name, provider, modelName, apiKey}`, so v4's warning-box condition
(`ChatModals.tsx:392`) compares `undefined === false` and is **dead code in
v4 itself**. v5's box-not-rendered is v4-faithful by outcome. This lane:
rewrite `chat/tools/chat-tool-settings-modal.ts`'s doc block (`:35-45`) from
"cross-lane escalation" to the verified disposition (keep the input and the
gated box — one binding turns it on if v4 ever grows the projection), and
append the finding to `dogfood-findings.md`'s **post-5.0 v4-side items**
list (the box wants either a projection or deleting — same family as the
unreachable `AllLLMPauseModal`).

## Tiered deliverables

**Tier 1 (must land):**

1. `chat/library-file-picker-modal.ts` (one component, signals — the
   P4.9E3C convention): both steps, the four scope sections in v4's render
   order, the mount-type/store-type filter with its why-comment, the
   persona-album resolution, the browse delegation (recorded reuse-vs-port
   decision), the GalleryPanel grid, the LinkingOverlay, both pick legs with
   v4's exact toast copy, and the two DIFFERENT completion callbacks (tray
   vs fetchChat-only).
2. The composer gutter entry at v4's slot (row 2 col 1, `file-plus`,
   "Attach file from library"), replacing the ABSENT slot; the
   salon-conversation tier-3 note retired the way P4.9E3C retired refusal
   pins (assert the absence note is gone).
3. The §1 mirror of `ChatAttachMountFile` into `core-contract.ts`
   name-for-name (per §Shared contract), even though the modal calls the
   REST leg.
4. Component tests: the scope filter (character stores excluded), the
   persona-album fallback chain, the mount-vs-legacy discriminator, the
   gallery-always-attaches rule, both callback behaviors, the sealed-while-
   linking state.
5. e2e: a live beat for the **legacy link leg** (runs now — the leg is on
   main): open picker → General → link a file → assert it lands in the
   composer tray. An **ACTIVATE-AT-UNIFY** beat (named constant, `false` on
   the branch) for the mount-attach leg: pick a store file → assert the
   Librarian announcement renders and the chat file list shows it once.

**Tier 2 (should land):**

6. Rider A: `screens/prospero/project-tool-settings-modal.ts` over the
   shared `qt-tool-settings-content`, the ModelBehaviorCard row un-stubbed
   (real `toolSummary`, enabled Configure), the `ProjectDetail` named
   fields, component tests (set-vs-set hasChanges; the wire body), and an
   e2e beat (open from Prospero → disable a builtin group → read back
   through `projectGet`).
7. Rider B: the two RNG fixes + the comment correction + tests.
8. Rider C: the disposition rewrite + the v4-side list entry.

**Tier 3 (explicit loud deferrals):**

- `FileBrowser`'s upload/delete/sync legs inside the picker — unreachable in
  v4 (`showUpload={false}`); do not build them here.
- `AllLLMPauseModal` stays deferred (unreachable in v4 — P4.9E3C's record
  stands).
- If the browse reuse decision exposes a `files-family` listing gap (e.g. a
  folder view the picker needs that P4.6af never rendered), defer it loudly
  by name in the picker rather than half-rendering.

## The differential requirement, restated

SPA lanes verify by component tests + live e2e beats over the real server
(the established P4.6/P4.9 SPA pattern) — no NDJSON oracle is owed for pure
UI, but every behavior claim above cites the v4 line it reproduces, and the
e2e beats are the equivalence check. Where this lane consumes server verbs,
the shapes are pinned by §Shared contract and the server lane's
differentials.

## Fixtures

- Consumes the committed e2e instance (`apps/web` e2e fixtures). If the
  picker beats need a database store with files/photos on the e2e vault,
  extend the e2e seed the way P4.6ay's `seedPascalToolsFixture` did — in
  THIS lane's spec-fixture files, not the Rust fixture families.
- No Rust fixture ownership.

## Shared contract (binding — identical in P4.9E4A)

**REST, the modal's two write legs** (the SPA calls these as raw REST through
`apiUrl()`, exactly as v4 does — no §1 mirror for either):

- `POST /api/v1/chats/{chatId}/files?action=link`, JSON `{ "fileId": string }`
  → `{ "file": { "id", "filename", "filepath", "mimeType", "size", "url" } }`
  — **already live on main** (`files_routes.rs:859-878`); neither lane
  changes its shape.
- `POST /api/v1/chats/{chatId}/files?action=attach-mount-file`, JSON
  `{ "mountPointId": string, "relativePath": string }` → the v4 response body
  (`file` with `type:"mountFile"` + `announcement {id, createdAt}`; `url` =
  `/api/v1/mount-points/{mountPointId}/blobs/{encodeURI(relativePath)}`).
  Error arms: 400 `mountPointId is required` / `relativePath is required`;
  404 `Chat` / `Mount-point file` / `Mount-point file blob`; 500
  `Failed to post Librarian attachment announcement`.

**§1 (core-contract):** P4.9E4A adds `Request::ChatAttachMountFile`
(serde-tagged `chatAttachMountFile`, fields `chatId`, `mountPointId`,
`relativePath`); this lane mirrors it into `core-contract.ts`
**name-for-name** but MAY consume the REST leg instead (v4 fidelity) — the
mirror is still owed so the §1 diff stays clean. The SPA otherwise consumes
only verbs already on main: `ChatGroupStores`, `ChatPhotoAlbums`,
`MountPointList`, `ProjectList`, `ToolsList`, `ProjectToolSettingsUpdate`,
`ProjectGet` — all frozen; neither lane changes them.

## Ownership

- **This lane owns:** all of `apps/web/**`; plus these docs edits:
  `m6-screen-parity.md` (the LibraryFilePickerModal row + item 10's
  remainder; the stale RNG-dropdown claim in row 9),
  `dogfood-findings.md` (append the `allowToolUse` v4-side item).
- **Does NOT touch:** any crate; `phase-4.md`/`CLAUDE.md` status (the
  unifier's).
- **Version bumps:** this lane bumps the SPA `package.json` version per its
  commits. The unifier accumulates.
- `CHANGELOG` and `status-log.md` are append-only for everyone.

## Verification gate (before declaring the lane done)

- `npx ng test` green; `npx ng build` clean.
- Full Playwright from `apps/web` — all this lane's non-gated beats green;
  gated beats present with named constants `false`.
- The retired absence-note assertions flipped (old note asserted GONE).
- Per-commit: follow `.claude/commands/commit.md`.
