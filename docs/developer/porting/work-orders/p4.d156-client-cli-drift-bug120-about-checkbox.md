# Work order P4.D156 — the client/CLI drift: `instances default --json` (v4 bug 120, `af2023c9a`), the About sentences (`e9a9c538e`'s one code hunk), the cheap-LLM `qt-checkbox` (`bbcb318c6`), and the collapse's three client corrections (`0506517d3` (f))

**Status:** CLOSED WHOLE on the lane branch
`claude/client-cli-drift-checkbox-dd9511` (2026-09-05; ordered the same day,
the `d883a5ee1` drift catch-up round: P4.D153 ∥ P4.D154 ∥ P4.D155 ∥ P4.D156 ∥
P4.D157 ∥ P4.D158). Every Tier-1 deliverable landed and every Tier-2 item with
it; the two Tier-3 deferrals stand as ordered (the wizard half of (f2) —
measured WIDER than the order predicted, see the lane record — and
`af2023c9a`'s docs half, P4.D158's). Awaiting `/unify`. Lane record +
verification gate: `status-log.md` → "Lane record — P4.D156".

**Two order facts corrected by measurement (ledger §5.3):** `instances --help`
is ALREADY a Tier R case (`cli_differential.rs:1811`), so two cases were added
rather than three and the honest arithmetic is **214 → 216**, not 217; and
(f2)'s NO-COUNTERPART is the WHOLE of v4's `saveGeneratedPhysicalDescription`,
not merely its wizard half — both of its callers at the pin are AI-wizard apply
paths.

## Preamble

- **v4 oracle baseline: `0b0617fee`** (drift-ledger §1). **This lane's
  target commits:** `af2023c9a` (bug 120 — CLI Tier R; v4's own
  release-checklist finding, NOT a convergence), `e9a9c538e`'s ONE code
  hunk (`app/about/AboutView.tsx`, three `<span>` bodies — the commit's
  docs remainder is P4.D158's), `bbcb318c6` (two `className` attributes),
  and `0506517d3`'s three CLIENT corrections (f) (the outfit-choice toggle
  toast, the physical-description failure toast, the Answer Confirmation
  row style) — the server corrections are P4.D155's. Read the hunks:
  `git -C ~/source/quilltap-server show af2023c9a -- packages/`, `show
  e9a9c538e -- app/about/AboutView.tsx`, `show bbcb318c6 --
  components/`, `show 0506517d3 -- components/settings/chat-settings/
  AnswerConfirmationSettings.tsx components/wardrobe/CanChooseOutfitToggle.tsx
  "app/aurora/[id]/edit/hooks/useCharacterEdit.ts"
  app/aurora/shared/save-generated-physical-description.ts`.
- **Freshness probe at lane start** (drift-ledger §2): v4 on `main`, tree
  clean, `git log d883a5ee1..main` empty, `git log 3a76b17df..bugfix` empty.
  **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED** (§B). The CLI Tier R runs against v4's REAL
  launcher — point `QT_V4_CHECKOUT` at the lane's pin worktree
  `/tmp/qt-v4-pin-p4d156-d883a5ee1` (with the `packages/quilltap/node_modules`
  symlink, ledger §5.1), never at the live checkout. SPA parity is spec-level
  against v4's client source at the same pin.
- **Sibling lanes:** P4.D155 owns `apps/web/src/app/pascal/**` (not this
  lane's); this lane owns `apps/web/src/app/screens/**`, `apps/web/src/
  styles/**`, and `crates/quilltap-cli/**`. P4.D157 and P4.D158 do not touch
  the SPA or the CLI. §A–§H + Ownership binding and identical across all six
  orders.
- **Worktree:** per the `carryout` skill (branch `claude/p4-d156-…`); SPA
  worktrees need `npm ci`, never `cp -a node_modules` (memory note
  `spa-worktree-node-modules`). `CHANGELOG`/`status-log.md` append-only.
  **Port 4319 is this lane's** (§E) — it may run the existing Playwright
  suite once at close; no new beat is required.

## Mandate

Four small, disjoint ports, each red-first against its oracle. **(1)** the
CLI: `instances default --json` reads AND strips the flag (v5 has the same
defect and then some — `cmd_default` has no `--json` branch at all), the
help verb line, the fish completion block — Tier R red-first. **(2)** the
About page's three sentences (v5 carries the PRE-fix bytes at three sites).
**(3)** the cheap-LLM card's two checkboxes take `qt-checkbox` (v5's inputs
carry NO class at all — neither v4's old `rounded` nor the new one). **(4)**
the three client corrections: (f1) verify the outfit-choice toggle's success
toast on BOTH views (v5 already shares one card between edit and detail —
measure it is v4's post-fix shape and pin the sentences), (f2) the
physical-description failure toast prefers the server's error text, (f3) the
Answer Confirmation row takes the shared `qt-settings-toggle-row` styling.
Must NOT touch: `apps/web/src/app/pascal/**`, any crate other than
`quilltap-cli`, `docs/v4/**`.

## Survey-verified starting points (2026-09-05, from the shipped hunks — ledger §5.3)

### Unit 1 — `af2023c9a` (bug 120, CLI)
1. v4 post-fix `instancesCommand` `default` arm: `const json =
   rest.includes('--json'); cmdDefault(rest.filter(a => a !== '--json'),
   {json});` (with the why-comment: the flag was never READ, so the JSON
   branch was unreachable, and never REMOVED, so `--json` became an instance
   name to set). `cmdDefault(args, opts = {})` (`instances-commands.js:268-288`):
   no args → `opts.json ? console.log(JSON.stringify({defaultInstance:
   current}))` : `current` : `'(none)'`; `--clear`; set. The help line `:33`
   `list [--json]                 List registered instances (default)`
   (column-aligned — byte-copy); `lib/completion/fish.template` gains
   ```
   for verb in list ls
     complete -c quilltap -n "__quilltap_using_subverb instances $verb" -l 'json' -d 'JSON output'
   end
   ```
   immediately after the `for verb in show remove rm delete set-passphrase
   passphrase default rename resto…` block (v4's exact placement — read
   the hunk's `@@ -234` context). Six new v4 cases in
   `packages/quilltap/lib/__tests__/instances-default-json.test.js` drive
   the real binary.
2. v5: `crates/quilltap-cli/src/instances_cmd.rs:48` `"default" =>
   cmd_default(&rest)`; `cmd_default` `:314-332` has NO json branch (so
   `instances default --json` today tries to set an instance named
   `--json`); the `list` arm `:32-35` already reads both flags — the pattern
   to copy. `help/instances_help.txt:7` says `list` (→ `list [--json]`,
   byte-copied incl. the padding); `help/completion/fish.template:236` is the
   `names-only` line — v4's new block goes where v4 put it, not "near" it.
   Tier R (`crates/quilltap-cli/tests/cli_differential.rs`) has four
   `instances default` cases at `:3144-3152` (show none / set / set unknown
   / clear) — add `instances default --json` (none) and `instances default
   --json` (with a default set) and `instances --help`; expect three reds
   before the port and 214 → 217/0 after (`--nocapture` to see the count;
   memory note `tier-r-case-count-hidden-on-pass`). `completion_behavior.rs`
   gains the fish `instances list --json` offer (its bash/zsh siblings have
   no analog in the hunk — verify, don't invent).

### Unit 2 — `e9a9c538e`'s AboutView hunk
3. Three bodies gain a clause (verbatim from the hunk): the Lantern —
   "…derived from chat context, with LoRA adapters and per-model options
   taken from the provider's own advertised capabilities" (the `&apos;` is
   a typographic apostrophe in the rendered DOM — match the CHARACTER v4
   renders, `'` U+0027 from `&apos;`); the Concierge — "…quick-hide
   integration, and a four-state per-chat control (Monitored, Flagged,
   Vouched Safe, Uncensored) settable at creation as well as
   mid-conversation"; Multi-provider — "…and OpenAI-compatible APIs, each
   profile able to name an understudy to take the call when its provider
   falls over". v5: `apps/web/src/app/screens/about/about-page.ts:342`,
   `:346`, `:398` (pre-fix bytes); `about.spec.ts:82-95` pins the provider
   list by `toContain` — grow it to the three full sentences (the mirror has
   been byte-current since P4.D127/P4.D131).

### Unit 3 — `bbcb318c6`
4. v4 `CheapLLMSettings.tsx`: the "Fallback to Local" and "Allow a
   Similar-Tier Stand-In" inputs `className="rounded"` → `"qt-checkbox"`.
   v5 `apps/web/src/app/screens/settings/providers/cheap-llm-card.ts:150-155`
   and `:168-173`: `<input type="checkbox" …>` with NO class. `.qt-checkbox`
   exists (`apps/web/src/styles/qt-components/_interactive.css:290`), so the
   `check-qt-classes` guard stays green; spec-pin the attribute in
   `cheap-llm-card.spec.ts`.

### Unit 4 — `0506517d3` (f)
5. **(f1)** v4 extracted `components/wardrobe/CanChooseOutfitToggle.tsx` and
   gave the EDIT view (`useCharacterEdit.ts:238-258`) the same success toast
   the detail view had: `enabled ? 'New chats will let this character choose
   their own opening outfit' : 'New chats will use this character’s default
   opening outfit'` (U+2019), error `'Failed to update outfit-choice
   setting'`. v5 `apps/web/src/app/screens/characters/choose-outfit-card.ts:66-84`
   already shows both sentences and is hosted by BOTH `character-edit.ts:166`
   and the detail view — **a convergence by construction**: measure (the
   spec `choose-outfit-card.spec.ts` should already pin both sentences; if
   the edit host lacks a spec asserting the toast fires from ITS host, add
   it) and record; no port expected.
6. **(f2)** v4 `save-generated-physical-description.ts:43-45`:
   `showErrorToast(errorData.error || opts.failureMessage)` — the server's
   `error` text wins; callers pass `'Failed to save physical description'`
   (edit view) and `'Character created, but physical description failed to
   save'` (the wizard). v5: `apps/web/src/app/screens/characters/edit/
   appearance-tab.ts:325-328` shows `err instanceof Error ? err.message :
   'Failed to save physical description'` — measure whether the dispatch
   error's `message` IS the server's `error` text (the `coreErrorMessage`
   path); if it is, pin it with a spec whose stub answers a server sentence;
   if not, prefer the server sentence as v4 does. The WIZARD half is a
   NO-COUNTERPART today: v5's `new-character.ts` never saves a generated
   physical description (that is the deferred AI-wizard surface) — record.
7. **(f3)** v4 `AnswerConfirmationSettings.tsx` replaces its hand-built
   `<label className="flex items-start gap-3 p-4 border …">` with
   `<SettingsToggleRow …>` (`components/SettingsToggleRow.tsx:29-41`:
   `<label className="qt-settings-toggle-row">`, `<input className=
   "qt-checkbox mt-1">`, `<div className="flex-1">`, `qt-settings-section-
   heading`, `qt-text-small mt-1`). v5 `apps/web/src/app/screens/settings/
   chat/answer-confirmation-settings.ts:42-52` still carries the hand-built
   label; six sibling cards already use `qt-settings-toggle-row`
   (`composer-emoji-settings.ts:31`, `composer-unicode-settings.ts:31`,
   `custom-tools-settings.ts:32`, `composer-spellcheck-settings.ts:29`,
   `smart-typography-settings.ts:43/:75`) — adopt the same markup (copy a
   sibling, not v4's React); spec-pin the class.

## Tiered deliverables

**Tier 1 (must land):** Unit 1 whole (the strip + read, the JSON shape, the
help line, the fish block, Tier R red-first → green, `completion_behavior`);
Unit 2 (three sentences + the grown spec); Unit 3 (two attributes + spec);
Unit 4 (f2) and (f3) with specs, (f1) measured and recorded.
**Tier 2 (should land):** a comment at `instances_cmd.rs`'s `default` arm
carrying v4's why-comment verbatim; the `lora-list-editor.ts:35` comment
re-pointed at v4's NEW home `lib/image-gen/lora-scale.ts` (P4.D157 checks
the VALUES — `{0, 2, 1, 0.05}` measured identical today; this lane owns the
file).
**Tier 3 (explicit deferrals, loud):** the wizard half of (f2)
(NO-COUNTERPART — the AI wizard is `p4.9k`-class); `af2023c9a`'s docs half
(README / `help/cli-instances.md` / CLI.md — mirror work, P4.D158's §G bank
and `docs/v4/` refresh).

## The differential requirement (restated)

Tier R is the oracle for Unit 1 (v4's REAL launcher from the pin; three
named cases red before the port, 217/0 after, counted with `--nocapture`).
Units 2–4 are client surfaces: the oracle is v4's client source at the pin,
pinned by unit specs that would fail against the pre-fix bytes (run each new
spec against the pre-port tree once and record the red). Mutation-proven:
leave `--json` in `rest` → the `default --json` cases redden; drop one About
clause → the spec reddens; remove one `qt-checkbox` → the card spec reddens;
restore the hand-built label → the row spec reddens.

## Fixtures

- Consumes: the Tier R master fixture (built by the test); no DB fixture.
- Delivers: nothing committed beyond specs and the Tier R cases.

## Shared contracts (BINDING — identical text in all six orders)

**§A Family ownership for this round's regens.** Each family below is
regenerated and edited by ONE lane; P4.D158's bulk neutrality sweep
EXCLUDES every family in this section by name and hands any red in them to
the owning lane.

- **P4.D153:** `memory_injector_equivalence` (+ `harness/oracle/cases/
  memory-injector.ts`), `build_context_tier3_equivalence` (+ its builder
  `build-context-tier3.test.ts` and the `QT_FIXTURE_BC_*` pair),
  `carina_query_tier3_equivalence` (+ its fixture builder),
  `announcer_tier3_equivalence`.
- **P4.D154:** `message_context_leaves_equivalence` (+ `harness/oracle/
  cases/message-context-leaves.ts`), `orchestrator_tier3_equivalence` (+
  `orchestrator-tier3.test.ts` and the `QT_FIXTURE_ORCH_*` pair),
  `file_attachment_tier3_equivalence` (+ its fixture pair).
- **P4.D155:** `documents_routes_equivalence` (+ `documents-routes.test.ts`),
  `system_export_equivalence`, `answer_confirmation_tier3_equivalence`,
  `cheap_llm_fallback_equivalence`, `brahma_console_tier3_equivalence`,
  `brahma_console_routes_equivalence`, `brahma_orchestrator_tier3_equivalence`,
  every `pascal_*_equivalence` + `p4_6ay_workbench_wire_contract`, the
  self-inventory families, and whichever family the lane identifies as
  driving `cheap_llm::get_cheap_llm_provider` (named in its record).
- **P4.D156:** the CLI Tier R (`crates/quilltap-cli/tests/cli_differential.rs`)
  + `completion_behavior.rs`; the SPA unit specs under its paths.
- **P4.D157:** the seven families the dead-code sweep takes dark:
  `cheap_model_equivalence`, `model_selection_equivalence`,
  `llm_errors_equivalence`, `message_formatter_equivalence`,
  `post_office_host_equivalence`, `chat_timestamp_equivalence`,
  `token_estimation_equivalence` (+ their seven oracle cases).
- **P4.D158:** `request_builder_equivalence` + the committed request-envelopes
  corpus and its recorder, `request_builder_google_wire_equivalence`,
  `request_builder_google_equivalence`, `response_parse_equivalence`,
  `tool_wire_call_site`, the stream fixtures, `openrouter_sdk_pricing_equivalence`,
  the provider manifests generator + `provider_manifest/manifests/*.json`;
  the restore families for the `2edd823c0` arms (`system_restore_equivalence`,
  `system_restore_state`, `restore_vintage_state`,
  `system_restore_guards_equivalence`, the `build-restore-archive*` builders
  and any NEW committed archive); and the bulk neutrality sweep over every
  family NOT listed above.

**§B Pins.** The round's target baseline is **`d883a5ee1`** (v4 `main` HEAD
at ordering, 2026-09-05). Every lane regenerates from a LANE-UNIQUE detached
worktree pinned at `d883a5ee1` (`/tmp/qt-v4-pin-<order>-d883a5ee1`, the
three symlink classes, ledger §5.1) — with ONE exception: P4.D157's
frozen-family evidence regenerates at **`0b0617fee`** (the last sha at which
those seven cases link), from its own second pin. **PIN REQUIRED stands for
every lane until `/unify` moves the baseline** — the checkout's HEAD is the
target, but the checkout can go dirty mid-lane (§5.1), and a pin is the only
tree whose contents are known. Verify each pin by grepping a fresh NDJSON for
a marker only one tree has (e.g. `About another character` appears in a
`d883a5ee1` memory-injector oracle only once P4.D153's targeted cases exist,
and never in a `0b0617fee` one).

**§C Sweep serialization.** `recipe_sweep.py --run-all` is single-flight
repo-wide and belongs to P4.D158 alone; it runs AFTER P4.D153–P4.D157 have
closed their branches (execution arrangement in the round report). Lanes
153–157 run per-family `--run` regens only, and never against another
lane's §A families or their `/tmp` staging.

**§D Source-tree seams (who edits what shared file).**

- `crates/quilltap-core/src/services/mod.rs`: **only P4.D153** edits it (one
  line: `pub mod memory_subject;`). `crates/quilltap-core/src/lib.rs`:
  **nobody**. P4.D157 deletes FUNCTIONS and their tests inside files it owns
  but never removes a module declaration or a `pub use` in `lib.rs`; a module
  it empties is recorded for the unifier, not deleted.
- `services/message_context.rs` + `services/chat_files.rs` (the
  `MessageContextSeams` trait, its `Noop` and `Real` impls): **only
  P4.D154**. The trait gains ONE sibling method beside `load_lantern_images`
  for the USER-side loader (name it `load_user_attachments`; signature shape
  `(&self, file_ids: &[String], provider: &str) -> impl Future<Output =
  UserAttachmentLoad> + Send`, default no-op, `UserAttachmentLoad` a struct
  the lane defines beside `LanternLoad`).
- `db/characters_read.rs`: **only P4.D153** (`find_names_by_ids`, RAW path).
- `cheap_llm.rs`, `services/cheap_llm_exec.rs`: **only P4.D155**.
- `model/request_builder/anthropic.rs`: **only P4.D158** (one regex entry +
  the three comment corrections).
- SPA: `apps/web/src/app/pascal/**` → **P4.D155**; `apps/web/src/app/screens/**`
  and `apps/web/src/styles/**` → **P4.D156**. Nobody else edits `apps/web`.
- `crates/quilltap-cli/**` → **P4.D156** only.

**§E Playwright.** Port 4319 is P4.D156's for the round (it may run the
existing suite once at close; it authors no new beat unless its record says
why). Every other lane authors NO Playwright and runs none; the unifier runs
the full suite at the gate.

**§F The drift ledger.** Lanes never write `drift-ledger.md`. The fourteen
§3 rows were marked `ORDERED(p4.d15x)` at ordering; `/unify` marks them
ABSORBED / NO-PORT-RATIFIED when the baseline moves to `d883a5ee1`. The
fifteenth row (`15573c3a1`, bug 119) stays UNPROCESSED by the ledger's own
instruction — it belongs to the unported `p4.9k` surface.

**§G The `p4.9i2` help bank.** The help-file hunks in this drift
(`e288ae2ec` → `help/file-uploads.md` "A word on company"; `d883a5ee1` →
`help/memory-recall-relevance.md` "Whose Life Is It, Anyway?"; `e9a9c538e` →
four `help/` files incl. `homepage.md`'s In-Chat Navigation section and three
corrected `url:` values) are banked ONCE, by P4.D158, in its lane record under
a `p4.9i2 bank` heading; the unifier carries them into `phase-4.md`'s
candidate row. No lane ports help content.

**§H Versions.** Each lane bumps only the crates / the SPA it touches, per
`.claude/commands/commit.md` §6; the unifier recounts as base + total bumps
(the silent auto-merge trap). Base at ordering: core 0.0.776, harness 0.0.671,
web 0.0.114, host 0.0.96, cli 0.0.17, tauri 0.0.7, SPA 0.5.642.

## Ownership (BINDING — identical table in all six orders)

| Path | Owner |
|---|---|
| `crates/quilltap-core/src/memory_injector.rs`, NEW `services/memory_subject.rs` (+ the one `services/mod.rs` line), `db/characters_read.rs`, `services/build_context.rs`, `services/carina_query.rs`, `services/announcer/character_voiced.rs`; §A families for P4.D153 | P4.D153 |
| `crates/quilltap-core/src/services/message_context.rs`, `services/chat_files.rs`; §A families for P4.D154 | P4.D154 |
| `crates/quilltap-core/src/cheap_llm.rs`, `services/cheap_llm_exec.rs`, `api/documents.rs`, `services/brahma_console/**`, `services/qtap_export/**`, `tools/self_inventory.rs`, `crates/quilltap-core/src/pascal/**`, `apps/web/src/app/pascal/**`; §A families for P4.D155 | P4.D155 |
| `apps/web/src/app/screens/**`, `apps/web/src/styles/**`, `crates/quilltap-cli/**`; the CLI Tier R + `completion_behavior.rs` | P4.D156 |
| `crates/quilltap-core/src/cheap_model.rs`, `pricing.rs`, `services/llm_errors.rs`, `message_formatter.rs`, `services/host_notifications.rs`, `chat_timestamp.rs`, `token_estimation.rs` (function-level deletions/pins only); the seven §A families + their oracle cases | P4.D157 |
| `crates/quilltap-core/src/model/request_builder/anthropic.rs` (one regex line + comments); `harness/oracle/providers/**` + `harness/oracle/fixtures/request-envelopes/**`, `response-bodies/**`, the stream fixtures; `crates/quilltap-core/src/provider_manifest/manifests/*.json`; the restore-archive builders + any NEW committed archive + §A restore families; `docs/v4/**`; `harness/tools/sweep-results/<new artifact>` | P4.D158 |
| `docs/CHANGELOG.md`, `docs/developer/porting/status-log.md` | append-only, all lanes |
| `docs/developer/porting/drift-ledger.md`, `crates/quilltap-core/src/lib.rs` | NOBODY |

Every other path is unowned this round: a lane that believes it must touch
one STOPS and reports (the `carryout` rule 3), naming the file and the
sibling it suspects.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D
   warnings` in BOTH feature sets; `cargo build --workspace`; `cargo test
   -p quilltap-cli` including Tier R (`QT_V4_CHECKOUT=<pin> QT_NODE=…`,
   `-- --nocapture`, the case count read) and `completion_behavior`.
2. `cargo test --workspace` (full log + sentinel) — nothing else should
   move; confirm.
3. SPA: `cd apps/web && npm test` (never bare `ng`), `npm run build`, `npm
   run lint`; the existing Playwright suite once (`npm run e2e`, port 4319 —
   zero skips expected; a red on a surface this lane did not touch is
   recorded as the standing intermittent class, not fixed here).
4. The mutation proofs above, each named in the lane record.
5. Commits per `.claude/commands/commit.md` (cli + SPA bumps); the lane
   record appended to `status-log.md`; this order's status header updated.
