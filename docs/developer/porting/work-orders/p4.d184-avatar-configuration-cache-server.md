# Work order P4.D184 — the avatar configuration cache, the SERVER half: the `avatar_cache` key derivation + lookup chokepoint, the avatar job's lookup-before-spend restructure (hit binds and stops; `force` rerolls; every avatar lands in the vault; no legacy `folders` row), the collapse-duplicate-avatar-rolls heal — v4 `7fbf8a55b` (stacked on P4.D182)

**UNIFIED on `main` (2026-09-15) — CLOSED.** Every Tier-1 item is on `main`. **The §3 review's one blocking finding on this lane: Tier-1 item 5 — the capture pins for the five NEW log lines — was neither built nor recorded as deferred.** Landed at the wire: the cache module's four lines pinned on raw connections (`avatar_cache.rs` unit tests: newest-holder sort, the NULL-`storageKey` skip, the cross-character skip, the gone-blob line, the read-failure warn WITHOUT a Miss), and the job's `Reused …` line captured in the tier-3 family on the hit case with its silence on the forced reroll. Should-fixes also landed: the heal's `Failed to collapse duplicate avatar rolls` error arm (v4 logs and BOOTS ON — v5's `?` aborted the boot), the `force` wire's write half pinned through the real enqueue, `leafCounts` on the reuse line, the `…concierge-route` build context on a pre-generation swap, and the `force` comment that contradicted its code. Recorded, not fixed: the lookup-vs-Concierge position is unmeasurable in this corpus (no case classifies at all). Round record: `status-log.md`.

**Status at lane close: CLOSED — every Tier-1 item landed; Tier-2 item 6 recorded rather
than built (see the lane record).** Ordered 2026-09-14, the `31436bae4` drift
catch-up round (P4.D182 → {P4.D183 ∥ P4.D184 ∥ P4.D185} ∥ P4.D186 ∥ P4.D187 ∥
P4.D188). Carried out 2026-09-14 on
**`claude/avatar-config-cache-server-76c2c5`**, branched from P4.D182's tip
(`6ec9c5c1`).

Landed: `services/avatar_cache.rs` (the derivation + lookup chokepoint, with
the NEW tier-1 `avatar_cache_key_equivalence` over a 31-shape corpus); the job
restructure (bind helper, params-once, lookup-unless-`force`, the hit return,
`generation_key` on the new row, vault-always with the project branch and
`ensure_legacy_folder` deleted); `force` end-to-end; and the collapse heal
(`db/avatar_rolls_collapse_heal.rs` + the `host.rs` fenced block + the NEW
tier-2 `avatar_rolls_collapse_heal_equivalence` over 17 scenarios and
`host_boot_avatar_rolls_collapse`). `avatar_job_tier3` 12 → 20 cases.

**Recorded, not built:** the 18 pre-existing `[CharacterAvatar]` handler log
lines v5 never carried (Tier 2 item 6 — a row for the handler-logging
inventory, with its exact size measured). **Recorded corpus gap:** no case can
reach a PRE-generation Concierge profile swap, so keying-on-the-requested-
profile is proven for the post-hoc reroute only. Both are in the lane record.

The unifier updates this header at unification.

## Preamble

- **v4 oracle baseline: `f4ad2c8d1`.** This lane absorbs the BEHAVIOUR of
  **`7fbf8a55b`** ("feat(aurora): cache character avatars per
  configuration", 2026-09-11) — the ledger row `ORDERED(P4.D182, P4.D184)`.
  Spec reads: `git show 7fbf8a55b -- lib/wardrobe/avatar-cache.ts lib/
  background-jobs/handlers/character-avatar.ts lib/wardrobe/avatar-
  generation.ts lib/background-jobs/queue-service.ts app/api/v1/chats/[id]/
  actions/regenerate-avatar.ts migrations/scripts/collapse-duplicate-
  avatar-rolls-v1.ts` and the post-commit files — **never the commit
  message** (ledger §5.3; §R.6(10): the migration's own prose disagrees
  with itself on the counts, and it is NOT transactional). The design doc
  `docs/developer/features/avatar-configuration-cache.md` at the target is
  context, not spec.
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2). **Any
  mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** Pins **`/tmp/qt-v4-pin-p4d184-
  31436bae4`** (every oracle; `QT_V4_ROOT` for every gate) and
  **`/tmp/qt-v4-pin-p4d184-f4ad2c8d1`** (the `avatar_job_tier3` neutrality
  leg on the pre-existing cases).
- **Sibling lanes:** P4.D182 beneath you (FROZEN — `files.generationKey` +
  `find_by_generation_key` are yours to CALL; you are authorised ONE fenced
  `=== P4.D184 ===` block in `host.rs` after its two); P4.D183 and P4.D185
  beside you (P4.D185 READS the key you write — `is_roll` — and owns
  `photos/**`, `api/characters.rs`; nothing of theirs is yours). P4.D188
  consumes nothing of yours directly.
- **Worktree:** `git worktree add <path> -b claude/p4-d184-avatar-cache-
  <hash> <P4.D182 tip>`; `CARGO_INCREMENTAL=0`.
- **Read before the work:** `crates/quilltap-harness/tests/avatar_job_
  tier3_equivalence.rs` + `harness/oracle/cases/avatar-job.test.ts` +
  `harness/oracle/fixtures/build-avatar-job-fixture.ts` + `avatar-job.json`
  (the 12-case tier-3 family you widen — canned image bytes, the mount +
  main pair, the Lantern row); the P4.D140 / P4.D152 lane records
  (`status-log.md` → "P4.D140", "P4.D152": the boot heal in the
  `migrations_state` ledger-row shape, cross-app in both directions, the
  "measure the population FIRST" rule for the Friday copy — ledger §5.5);
  `db/files_sha256_realign_heal.rs` (the heal idiom with a planted
  population and a whole-table census); memory notes `jest-oracle-plugin-
  factory-is-mocked`, `jest-setup-storage-manager-stub` (every
  `downloadFile` answers "mock file content" — a blob-exists probe in the
  oracle must go through the REAL mount store, as `avatar-job.test.ts`
  already does), `byte-exact-static-data-transcription`, `a-guard-whose-
  other-conjuncts-are-false-is-untested`.

## Mandate

Port the cache whole, so that on v5 exactly as on v4 at `31436bae4`: (a)
ONE module (`services/avatar_cache.rs`) derives the v1 key
(`sha256(canonicalJson({v:1, provider, imageProfileId, params}))`) and the
v0 legacy key (`sha256(canonicalJson({v:0, modelName, prompt}))`) with v4's
canonical JSON (keys sorted by ordinary string comparison recursively;
arrays in order; `undefined`-valued keys DROPPED — in Rust, an ABSENT key
and a `None` that serde would omit hash identically — `null` PRESERVED),
and ONE lookup (`lookup_cached_avatar`) tries v1 then v0, never upgrades a
v0 hit, sorts newest-first by `createdAt` string compare, skips a row not
tagged with the character, skips a row with no `storageKey`, treats a row
whose blob is gone as a MISS with v4's info line, and answers `None` on a
read error with v4's warn (never blocks generation); (b) the avatar job
builds the params ONCE from the REQUESTED profile before the Concierge
classification, looks the key up unless `force`, and on a HIT binds the
existing file to `chats.characterAvatars` + `characters.avatarOverrides`
through the SAME helper the fresh path uses and RETURNS — no
classification call, no image call, no transcode, no file write, no
Lantern notification — with v4's info line; on a miss/force it reuses the
same params for the non-reroute generation, writes `generationKey =
cacheKeys.key` on the new `files` row, and binds through the helper; (c)
every avatar lands in the character's vault (`images/history/`) whether or
not the chat has a project — the project-upload branch and the legacy
`folders` find-or-create are DELETED from the avatar path (the vault-
missing refusal becomes unconditional); (d) the manual regenerate action
sets `force: true` and the automatic wardrobe-change trigger never does;
(e) the collapse pass (`collapse-duplicate-avatar-rolls-v1`) runs at boot
under v4's own `migrations_state` gate — an exact no-op on the shared
Friday instance v4 has already collapsed — grouping the SELECTED avatar
rows by the v0 key, keeping the newest, protecting rows backing a
character's `defaultImageId` blob, repointing `chats.characterAvatars`,
`characters.avatarOverrides`, `chat_messages.attachments` + the inline
uuid in `content`/`opaqueContent`, deleting the victims' mount rows then
`files` rows, resumable not atomic, with v4's five log lines. This lane
does NOT touch `photos/**`, `api/characters.rs`, the SPA, or `help/**`.

## Survey-verified starting points (fresh survey 2026-09-14 at `31436bae4` and on `main` `ecdcf9e5` — do not re-derive)

### v4

- **`lib/wardrobe/avatar-cache.ts`** (205 lines): `AvatarCacheKeyInput
  {provider, imageProfileId, params: ImageGenParams}` `:38-45` (taken from
  the PRE-reroute profile — `:31-36`); `LegacyAvatarCacheKeyInput
  {modelName, prompt}` `:47-53`; `canonicalJson` `:60-73` (sort `([a],[b])
  => a<b?-1:a>b?1:0`; arrays kept — "a LoRA list is ordered"; `undefined`
  filtered at `:70`); `sha256Hex` `:75-77` (`createHash('sha256').update(
  input,'utf8').digest('hex')`); `deriveAvatarCacheKey` `:83-92`;
  `deriveLegacyAvatarCacheKey` `:100-108` (`modelName: input.modelName ??
  null`); `deriveAvatarCacheKeys` `:121-129` (legacy from `params.model ??
  null` + `params.prompt`); `lookupCachedAvatar(repos, keys, characterId)`
  `:146-205`: per key → `repos.files.findByGenerationKey` (`:154`; a throw
  → `logger.warn('[AvatarCache] Lookup failed, treating as a miss', …)` +
  `return null` `:155-163`) → sort `String(b.createdAt).localeCompare(
  String(a.createdAt))` `:167-169` → per row: skip `!row.tags?.includes(
  characterId)` `:175-177`; skip `!row.storageKey` `:178-180`; `if (!(await
  mountBlobExists(row.storageKey)))` → `logger.info('[AvatarCache] Cached
  avatar blob is gone, regenerating', {context:'wardrobe.avatar-cache',
  characterId, fileId})` + `continue` `:181-188`; else `logger.debug('[
  AvatarCache] Hit', {context, characterId, fileId, legacy: candidateKey
  === keys.legacyKey})` + return `:190-196`; nothing → `logger.debug('[
  AvatarCache] Miss', {context, characterId})` + `null` `:200-204`. The
  module never writes.
- **`lib/background-jobs/handlers/character-avatar.ts`** (618 lines
  post-commit): `bindAvatarToChat(repos, {chat, character, chatId, fileId})`
  `:53-86` (the `characterAvatars[character.id] = {imageId, generatedAt:
  now ISO, afterMessageCount: chat.messageCount ?? 0}` merge + the
  `avatarOverrides` filter-then-push); steps: load chat `:110`, character
  `:116`, profile `:122`, key `:127-143`; prompt `:151-173`; **the cache
  block `:175-221`** — `buildImageGenParams({profile: imageProfile, prompt,
  overrides: {n:1, style:'natural'}, orientation:'portrait', logContext})`
  `:186-192`; `deriveAvatarCacheKeys({provider: imageProfile.provider,
  imageProfileId: imageProfile.id, params: avatarParams})` `:193-197`;
  `if (!payload.force) { const cached = await lookupCachedAvatar(…); if
  (cached) { await bindAvatarToChat(…fileId: cached.id); logger.info('[
  CharacterAvatar] Reused cached avatar for this configuration', {context,
  jobId, chatId, characterId, fileId: cached.id, leafCounts}); return; } }`
  `:199-221` ("No Lantern notification: nothing was produced"); Concierge
  `:223-299` (unchanged); generation `:301-476` reusing `avatarParams` on
  the non-reroute path `:315-316` (only a reroute rebuilds: `:317-326`,
  `:401-410`); save `:486-594` — vault-always `:514-545` (`getCharacterVault
  Store` → throw on missing `:530-535`; `writeCharacterAvatarToVault({…kind:
  'history'…})` `:539-545`), the deleted `folders` block replaced by the
  four-line comment `:550-553`, `repos.files.create({… generationKey:
  cacheKeys.key (:574), projectId: null, folderPath: null (:579-580)})`;
  bind `:596-602`; completion + Lantern `:604-617`. The pre-commit project
  branch (`7fbf8a55b^:…:414-485`: `fileStorageManager.uploadFile({…
  projectId, folderPath:'/character-avatars/'})` + `repos.folders.ensureBy
  Path(…)`) is GONE. The 19 log lines (level + string) are in the survey
  table — the new one is `Reused cached avatar for this configuration`.
- **Plumbing:** `regenerate-avatar.ts:58-68` `force: true` on `triggerAvatar
  Generation` (the ONLY production setter); `queue-service.ts`
  `CharacterAvatarGenerationPayload.force?: boolean`; `avatar-generation.ts`
  `force?: boolean` on the params, threaded `...(force ? {force: true} : {})`;
  `files.repository.ts` `findByGenerationKey` (P4.D182's).
- **`migrations/scripts/collapse-duplicate-avatar-rolls-v1.ts`** (508):
  selection SQL `:77-88` (`originalFilename LIKE 'avatar\_%' ESCAPE '\' AND
  category = 'IMAGE' AND generationPrompt IS NOT NULL AND generationPrompt
  != ''`); `protectedBlobIds` `:110-134` (characters' `defaultImageId` link
  ids → `doc_mount_file_links` JOIN `doc_mount_blobs` → blob ids, BEFORE
  grouping); grouping by `deriveLegacyAvatarCacheKey({modelName:
  generationModel, prompt: generationPrompt ?? ''})` `:246-258`; survivor =
  newest `createdAt` (`localeCompare` desc) `:266-289`, a protected victim
  becomes a second survivor with the same key + `logger.info('Keeping
  avatar roll still serving as a character portrait', {context:'migration.
  collapse-duplicate-avatar-rolls', fileId})` `:280-283`; `logger.info(
  'Collapsing avatar rolls', {context, avatarRows, configurations, victims})`
  `:291-296`; key the survivors `UPDATE files SET generationKey = ? WHERE
  id = ?` `:298-303`; repoint `chats.characterAvatars` `:319-339` and
  `characters.avatarOverrides` `:342-365` via `repointJsonColumn` (`SELECT
  id, <col> AS payload FROM <table> WHERE <col> IS NOT NULL`, rewrite only
  changed rows); `chat_messages` `:367-427` (`WHERE attachments IS NOT NULL
  AND attachments != '[]'`; swap ids in the array; when any swapped, also
  `content.split(victimId).join(survivorId)` + the same on `opaqueContent`);
  delete `:429-458` — `deleteVictimBlob(mountDb, blobId)` `:141-161`
  (chunks by linkId → links → documents → blobs → files for the blob's
  `fileId`; a throw → `logger.warn('Failed to delete avatar blob, leaving
  its file row in place', {context, fileId, blobId, error})` and the
  `files` row is KEPT), then `DELETE FROM files WHERE id = ?`;
  `logger.info('Collapsed duplicate avatar rolls', {context,
  configurations, rowsKeyed, victimsDeleted, blobsDeleted, chatsChanged,
  charactersChanged, messagesChanged, durationMs})` `:462-472`;
  `logger.error('Failed to collapse duplicate avatar rolls', {context,
  error})` `:490-493`. NOT in one transaction (`:43-45` "Resumable rather
  than transactional"). `shouldRun` = an unkeyed avatar row remains (test
  13). The generic runner persists the result into `migrations_state`
  (`migrations/state.ts:39`, `:108`) — MEASURE at the target whether the
  runner re-runs a migration already in the ledger when `shouldRun` is
  true (expected: applied ids are skipped; `shouldRun` gates only the
  first run) — the answer decides the heal's gate.
- **Oracle case lists:** cache (19) — `deriveAvatarCacheKey` ×7 (`is stable
  across calls with identical input`, `ignores key ordering inside the
  params object`, `treats an explicit undefined the same as an absent
  field`, `changes when the prompt changes`, `changes when the model
  changes`, `changes when the profile changes, even on the same model`,
  `distinguishes LoRA order — an ordered list is part of the
  configuration`), `deriveLegacyAvatarCacheKey` ×3 (`never collides with a
  full-fidelity key over the same prompt and model`, `treats a null model
  and a missing model identically`, `is what deriveAvatarCacheKeys pairs
  with the full key`), `lookupCachedAvatar` ×9 (`returns the row matching
  the full-fidelity key`, `falls back to the legacy key when the full key
  misses`, `prefers the newest holder of a key — a reroll rebinds it`,
  `treats a row whose blob is gone as a miss`, `skips a row whose blob is
  gone and returns the next usable one`, `never hands one character
  another character's face`, `skips a row with no storageKey`, `returns
  null rather than throwing when the read fails`, `returns null when
  nothing matches either key`); collapse (13) — `keeps the newest roll of
  a configuration and deletes the rest`, `keeps configurations apart — a
  different outfit is a different key`, `treats the same prompt on a
  different model as a different configuration`, `keys a lone roll without
  deleting it, referenced or not`, `repoints chat avatars and character
  overrides to the survivor`, `repoints a Lantern announcement and the
  uuid it quotes inline`, `leaves unrelated attachments and messages
  alone`, `takes the deleted roll's chunks, link, blob and content row
  with it`, `never deletes a roll a character still names as its
  portrait`, `ignores rows with no prompt to key on`, `ignores files that
  are not avatars`, `is idempotent — a second pass finds nothing left to
  do`, `shouldRun is true while an unkeyed avatar remains`; regenerate
  (+1 assertion): `force: true` on the trigger args.

### v5

- **The job:** `crates/quilltap-core/src/services/character_avatar_job.rs`
  — `CharacterAvatarPayload {chat_id, character_id, image_profile_id,
  equipped_slots_override}` `:48-53` (NO `force`); `handle_character_
  avatar_generation` `:107`; steps: chat `:128`, character `:134`, profile
  `:146` + key skip `:151-159`, aesthetic `:165-172`, slots + prompt
  `:174-`, **Concierge pre-scan `:221`**, generate `:305`, decode/transcode
  /persist `:345` (`write_avatar_file(main, mount, &write)` at `:404`),
  `chat.characterAvatars` `:409-440`, `avatarOverrides` `:441-467`, Lantern
  `:468-`. `write_avatar_file` `:569` branches on `(project_id,
  project_upload)`: `(None, _)` → the vault (refuse if unprovisioned,
  `write_character_avatar_to_vault`); `(Some(project_id), Some(upload))`
  `:613` → the project store + `ensure_legacy_folder` `:683` (`Folders
  Repository::ensure_by_path` `:690`) — **the branch to DELETE**; measure
  whether `image_job_common::ProjectUploadResult` / the upload helper is
  shared with the story-background job (expected YES — delete only the
  avatar's use, never the helper). The `files` insert inside the vault
  branch is where `generation_key` binds. The cache lookup goes BEFORE
  `:221`; the bind helper (`bind_avatar_to_chat`) is extracted from
  `:409-467` so the hit path and the fresh path share it. The
  `AvatarWriteInput` (`:560-`) loses `project_id`/`project_upload`.
  NO log lines exist today (`grep tracing::` = 0) — the module doc's
  deferral covers `logLLMCall` only; the handler lines v4 has are a
  pre-existing gap: land the NEW ones (`Reused cached avatar …`, the four
  `[AvatarCache]` lines) with capture pins, and record the rest as the
  handler-logging inventory's row.
- **The trigger:** `services/avatar_generation.rs:54` `trigger_avatar_
  generation` (the manual regenerate consumes it), `:68` `_inner`, `:144`
  `trigger_avatar_generation_if_enabled` (automatic). `AvatarGeneration
  Params` gains `force: bool` (default false); the payload gains
  `force: Option<bool>` (serialised only when true — v4's `...(force ?
  {force: true} : {})`, so the job row's payload bytes match).
- **The action:** `api/chat_outfits.rs:841` `chat_regenerate_avatar`
  (`handleRegenerateAvatar` twin) → passes `force: true`.
- **The tag read:** P4.D182 names the tag-indexed `files` read for you
  (the `tags` column — v4's `row.tags?.includes(characterId)`); the blob
  probe is `services/image_job_storage::resolve_character_vault_mount` +
  the mount store's blob-exists read (measure the exact fn; v4's
  `mountBlobExists(storageKey)` parses the `mount-blob:<mountPointId>:
  <blobId>` storage key).
- **The heal home:** NEW `db/avatar_rolls_collapse_heal.rs` in the
  P4.D140 shape (`chat_activity_recompute_heal.rs`) or the P4.D152 shape
  (`files_sha256_realign_heal.rs`, a `migrations_state` gate keyed on v4's
  id `collapse-duplicate-avatar-rolls-v1`, cross-app: a row v4 wrote means
  v5 runs nothing; v5 writes the row after its own run in v4's row shape
  — measure v4's `migrations_state` columns from `migrations/state.ts`).
  Wired in `host.rs` in your ONE fenced block AFTER P4.D182's, and AFTER
  P4.D182's `files` ensure (the pass reads `generationKey`). It needs the
  mount partition (`ws.mount()`) for the blob deletes — the P4.D152 heal
  is the two-partition precedent.
- **Families:** `avatar_job_tier3_equivalence.rs` (12 cases over `avatar-
  job.json`; fresh two-DB copies per case; the fixture builder `build-
  avatar-job-fixture.ts`) — widen: a pre-seeded keyed row (HIT → no
  image call, the bind, no Lantern row, the `files` count unmoved), a
  `force` case (MISS-by-force → a new keyed row, the old key's newer
  holder wins next time), a blob-gone case (the keyed row's blob deleted
  → MISS → regenerate), a legacy-key hit (`generationKey` = the v0 key
  planted, no v1 row), a cross-character keyed row (never returned), a
  project-chat case (the file lands in the VAULT, `projectId: null`, NO
  `folders` row — the pre-existing `folders` assertion flips RED-FIRST),
  the Concierge-reroute case keyed on the ORIGINAL profile. `character_
  avatar_write_tier2` (regen). `seed_avatars` (regen — measure whether the
  seed path writes keys; expected NOT).

## Tiered deliverables

### Tier 1 — must land, in this order

1. **`services/avatar_cache.rs`** — `canonical_json`, `sha256_hex`,
   `derive_avatar_cache_key`, `derive_legacy_avatar_cache_key`,
   `derive_avatar_cache_keys`, `lookup_cached_avatar(main, mount, keys,
   character_id) -> Option<FileEntry>` (v4's four skip rules, the newest-
   first sort by STRING compare of `createdAt`, the v0 fallback with no
   upgrade, the swallowed read error) + the four capture-pinned log lines.
   **NEW tier-1 `avatar_cache_key_equivalence`**: a recorded corpus
   (`harness/oracle/cases/avatar-cache-key.ts` driving v4's REAL
   `deriveAvatarCacheKey`/`deriveLegacyAvatarCacheKey`/`deriveAvatarCache
   Keys` at the pin) over ≥ 20 param shapes incl. the seven key cases
   above, an explicit-`undefined` vs absent pair, a `null`-valued key, a
   nested object with unsorted keys, a LoRA list in two orders, a Unicode
   prompt (UTF-8 hashing), `modelName` null vs missing — hex digests
   compared EXACT. The `params` shape is v5's `build_image_gen_params`
   output: MEASURE that its serde key set equals v4's `buildImageGenParams`
   object for the avatar call (a v5 key v4 lacks — or a `None` v4 would
   carry as an explicit `undefined`, dropped — changes the hash and is a
   real divergence); pin the sameness by feeding BOTH sides the SAME
   recorded params JSON in the corpus AND by a tier-3 arm that hashes each
   side's OWN built params (the arm that can red on a shape drift).
2. **The job restructure** — `bind_avatar_to_chat` extracted; the params
   built ONCE before the Concierge block; the lookup unless `force`; the
   HIT return (no classification, no generation, no transcode, no write,
   no Lantern; the info line); the params reused on the non-reroute
   generation; `generation_key` on the created row; vault-always (the
   project branch + `ensure_legacy_folder` deleted from the avatar path;
   the vault refusal unconditional); `avatar_job_tier3` widened per the
   list above, RED-FIRST on every new case and on the flipped project-chat
   case.
3. **`force`** — `AvatarGenerationParams.force`, the payload's conditional
   key, `chat_regenerate_avatar` passing `true`, the automatic trigger
   NEVER; pinned by a job-row payload-bytes test (the manual action's row
   carries `"force":true`; the automatic one carries no `force` key) and
   by the tier-3 `force` case.
4. **The collapse heal** — NEW `db/avatar_rolls_collapse_heal.rs` with
   v4's algorithm step for step (the selection predicate incl. the
   `ESCAPE '\'` LIKE; the protected-blob set computed BEFORE grouping; the
   v0 grouping through `avatar_cache::derive_legacy_avatar_cache_key` —
   ONE derivation, never a second; newest survivor; the second-survivor
   arm; the three repoint targets incl. the `content`/`opaqueContent`
   `split/join` substitution; the mount-row delete order; the kept-row-on-
   blob-failure arm; NOT one transaction — each statement its own; the
   five log lines byte-exact with v4's `context` field) under the
   `migrations_state` gate in v4's row shape (cross-app both directions:
   v4's row present → v5 runs nothing and writes nothing; absent → v5
   runs and writes ONE row v4's runner will honour — measure v4's runner
   semantics for a present row + `shouldRun` true, item "Survey → v4 →
   collapse", and match). **NEW tier-2 `avatar_rolls_collapse_heal_
   equivalence`**: a jest oracle driving v4's REAL migration module at
   the pin over a built two-DB fixture with the 13 cases' shapes (the
   whole-table census of `files`, `chats.characterAvatars`, `characters.
   avatarOverrides`, `chat_messages.{attachments,content,opaqueContent}`,
   `doc_mount_{files,blobs,file_links,chunks,documents}` after the pass;
   the `migrations_state` row EXCLUDED from the census — its timestamp is
   nondeterministic — but its presence asserted on both sides), RED-FIRST.
   The `host.rs` block + a `host_boot` arm (a planted population is
   collapsed on boot; a second boot changes nothing; a boot on a DB whose
   ledger already carries v4's row changes nothing).
5. **The capture pins** — every new log line (the job's `Reused …`, the
   cache's four, the heal's five) under a thread-scoped capture layer with
   the silence legs (a MISS logs `Miss` not `Hit`; a `force` run logs
   neither; the heal on an already-collapsed DB logs nothing).

### Tier 2 — should land

6. The remaining `[CharacterAvatar]` handler lines v5 never carried
   (the survey's 19-row table) — land them capture-pinned if the lane has
   room; otherwise record the exact list for the handler-logging
   inventory (`docs/developer/porting/handler-logging-inventory.md` or
   wherever P4.74 keeps it).
7. A `seed_avatars` neutrality regen (the seeding path must not start
   writing keys).

### Tier 3 — explicit deferrals (loud, typed, never silent)

- The Avatar Rolls listing/actions — P4.D185's.
- v4's `logLLMCall` for `IMAGE_GENERATION` — the module's standing
  deferral, unchanged.
- The pretty labels — NO-PORT.
- ⚠ **💸 The Friday copy has ALREADY been collapsed by v4** (ledger §5.5)
  — the dogfood pass measures the population first (`SELECT COUNT(*) FROM
  files WHERE generationKey IS NULL AND originalFilename LIKE 'avatar\_%'
  …`); the boot must write nothing there. Record the expectation in the
  lane record for `/dogfood`.

## The differential requirement (restated)

Tier 1 exact for the key derivation; tier 3 (mocked image provider,
canned bytes) for the job through `avatar_job_tier3`; tier 2 census for
the heal. Closest recipes: `avatar_job_tier3`'s own header + `build-avatar-
job-fixture.ts`; P4.D152's `files_sha256_realign_heal_equivalence` (a
migration driven by jest over a planted population, the ledger row
excluded from the census); P4.D97's ledger-row cross-app pins. Mutation
proofs, each reddening exactly one: drop the `undefined` filter in
`canonical_json` (the explicit-undefined corpus row); sort arrays (the LoRA
row); look up AFTER the Concierge block (the reroute-keyed case + the
hit-skips-classification pin — a hit must make ZERO classifier calls);
honour `force` on the automatic trigger (the payload-bytes test); keep
`ensure_legacy_folder` on the project path (the flipped project case);
delete the album's link too in the heal (case 9); make the heal one
transaction whose failure rolls everything back (the blob-failure kept-row
case); write the ledger row on a no-op boot (the cross-app arm).

## Fixtures

- CONSUMES: P4.D182's tip (the column + `find_by_generation_key`); the
  `/tmp` `avatar-job` builder (widen at the pin — plant keyed rows by SQL
  with v0/v1 keys computed by the ORACLE side's real module, never by
  hand).
- DELIVERS: the widened `harness/oracle/fixtures/avatar-job.json` + builder
  (committed spec, `/tmp` DBs); NEW `harness/oracle/cases/avatar-cache-
  key.ts` + `fixtures/avatar-cache-key.json` corpus; NEW `harness/oracle/
  cases/avatar-rolls-collapse.test.ts` + its builder; if the heal family
  needs a committed pair, `crates/quilltap-harness/tests/fixtures/avatar-
  collapse-{main,mount}.db` + sidecars, builder in PROSE.

## §C — the round-wide wire contract (BINDING — verbatim-identical in P4.D182, P4.D183, P4.D184, P4.D185, P4.D186, P4.D187 and P4.D188)

Every name below is a name, not a suggestion: the unifier runs a
name-for-name diff across the producing crate (`api/types.rs`, the handler
bodies, the web renderers) and the consuming SPA files (`core-contract.ts`,
the api helpers) — the P4.D165 precedent. A lane that needs a name not
listed here STOPs and flags it in its lane record; the unifier reconciles.
Measured at the target `31436bae4` on 2026-09-14.

### C.1 `chats.transcriptVersion` (P4.D182 ensures; P4.D183 bumps, reads and projects; P4.D187 reads)

- **Column:** `chats."transcriptVersion" INTEGER DEFAULT 0` — created by
  boot ensure ONLY (`db/chats_transcript_version_repair.rs::
  ensure_chats_transcript_version_column`, wired in `host.rs` after the
  P4.D171 blocks). It is NOT in `fresh_schema.json` (v4's `generateDDL`
  never emits it — §R.5(a)), NOT a field of `ChatUpdate`, NOT read by
  `chats_read::marshal_row` (the 100-column census does not move), NOT in
  any `.qtap` chat record, backup or restore reader, NOT remapped, NOT
  exported. Existing rows read `0`.
- **Writer:** exactly ONE — `ChatMessagesRepository::announce_transcript_
  change(chat_id)` (pub): an atomic `UPDATE chats SET transcriptVersion =
  transcriptVersion + 1 WHERE id = ?` (v4's `$inc`; a failure is
  swallowed with v4's `Failed to bump transcript version` warn and the
  publish still fires), then `tracing::debug!("Transcript change
  announced", chatId)`, then `publish_realtime(RealtimeTopic::Chats,
  Some(chat_id))`. A private `commit_transcript_change(chat_id, chat,
  update)` wraps "the bookkeeping `ChatsRepository::update` when `chat`
  exists AND the patch is non-empty, then ALWAYS announce".
- **Reader:** `ChatsRepository::get_transcript_version(chat_id) -> i64` —
  a raw single-column read, `0` when the row is missing, the cell is
  NULL, or the read fails.
- **Chat GET (`chatGet` → `ChatDetail`):** `transcriptVersion: number`
  (v4 `handlers/get.ts:308` reads it, `:375` places it — after `messages`,
  in the key order the `salon_reads` family compares). SPA type:
  `ChatDetail.transcriptVersion?: number` (optional — a server without
  P4.D183 omits it and the SPA reads `null` = "ask for everything").

### C.2 The transcript verb + the messages edge (P4.D183 ↔ P4.D187)

- **Verb `chatTranscript`** — `Request::ChatTranscript { chat_id: String,
  known_version: Option<serde_json::Value> }` (wire `{ type:
  'chatTranscript', chatId, knownVersion? }`). The handler applies v4's
  gate: `knownVersion` counts only when it is a JSON number that is an
  integer (JS `Number.isInteger`); anything else (absent, null, a float,
  a string) reads as "no known version" — a full read, never an error.
- **Response `CoreResponse::ChatTranscript(Value)`** — the BARE body:
  `{ unchanged: true, version: number }` when `knownVersion === version`
  (nothing else is read or projected — the version is read BEFORE the
  projection, never after); otherwise `{ unchanged: false, version:
  number, messages: [...], offSceneCharacters: [...], count: number }`
  where `messages`/`offSceneCharacters` are byte-identical to the chat
  GET's (ONE projection function feeds both — C.1's neutrality proof).
  Key order as listed.
- **Refusals:** a missing chat → 404 `Chat not found`; a projection
  failure → 500 `Failed to read transcript` (+ v4's `[Messages API v1]
  Error reading transcript` error line, `{chatId}` + the error).
- **REST (NEW `crates/quilltap-web/src/messages_routes.rs`, registered
  at `/api/v1/messages`):** `GET /api/v1/messages?chatId=<id>&action=
  transcript[&knownVersion=N]` → the body above; `chatId` missing → 400
  `Query parameter required: chatId` (before the 404). `knownVersion` is
  read through `query::first` and converted with JS `Number()` semantics
  (`"abc"` → NaN → treated as absent). `GET /api/v1/messages?chatId=<id>`
  with no (or an unrecognised — v4's default handler) `action` → v4's
  `handleListMessages` (the stored message EVENTS — Tier 2 in P4.D183,
  verb `chatMessageEvents`, body measured from v4). `POST /api/v1/
  messages` (v4's SSE send) is NO-COUNTERPART by the locked boundary
  (sends are `chatSend` over dispatch, tokens over `/api/events`) —
  recorded, never a 405 invented as a "port".
- **SPA client (`chat/chat-transcript.api.ts`):** `fetchChatTranscript
  (core, chatId, knownVersion: number | null)` → dispatches the verb;
  `ChatTranscriptDto = { unchanged: boolean; version: number; messages?:
  MessageDto[]; offSceneCharacters?: OffSceneCharacterDto[]; count?:
  number }`.

### C.3 The realtime publish points (P4.D183 server; P4.D187 client)

- **Topic:** the EXISTING `RealtimeTopic::Chats`, chat-scoped —
  `{ v: 1, topic: 'chats', id: <chatId> }` on the existing `Event`
  channel (SSE `/api/events` / the Tauri pump). No new topic, no new
  socket, no new coalescing (the bus's 250 ms trailing edge is v4's).
- **Sites (each announces through C.1's ONE writer):** `add_message`
  (always, even when the chat row is missing — the bookkeeping update
  stays conditional on the row), `add_messages` (same), `update_message`
  (once, after the row write; NOT when the message is not there — v4
  test "says nothing when the message is not there"), `delete_messages_
  by_ids` (only when `removed > 0`), `clear_messages` (UNCONDITIONAL —
  v4's one observable guard change), `ChatSearchRepository::replace_in_
  messages` (v4's `replaceInMessages` — the hunk shows an unconditional
  announce after the loop while v4's test says "says nothing when no
  message matched": MEASURE which is true at the target and port the
  measured behaviour, recording the answer). `ChatsRepository::delete`'s
  raw `DELETE FROM chat_messages` does NOT announce (v4 does not either —
  the row is gone). The maintenance cache collapse does NOT announce
  (measure v4's; expected silent). The P4.9K1 rename execute leg reaches
  the funnel through `update_message`, so it announces for free.
- **Client:** `realtime-topic-map.ts`'s `chats` row is UNCHANGED (the
  `chatKeys.detail(id)` invalidation stays, as v4's topic map keeps its
  detail invalidation); the Salon ADDITIONALLY subscribes with
  `realtime.onTopic('chats', refreshTranscript, chatId)` for the cheap
  conditional read (C.2), which also fires on every reconnect (`onOpen`).

### C.4 `chain_complete.heldUserTurn` (P4.D186 server ↔ P4.D187 client)

- **Frame:** `ChainCompletePayload` gains `held_user_turn: Option<bool>`
  with `#[serde(skip_serializing_if = "Option::is_none")]` → wire key
  `heldUserTurn`, ABSENT on every frame except the one
  `finish_held_user_turn` emits: `{ chainComplete: true, reason:
  'paused', nextSpeakerId: null, chainDepth: 0, paused: true,
  heldUserTurn: true }` — on EVERY held turn, unconditionally (§R.6(1)).
  `heldUserTurn` never appears without `paused: true`.
- **Reducer:** `ChatStreamState.chainHeldUserTurn: boolean` = `frame.
  heldUserTurn === true` (absent and `false` are one answer).
- **Toast (client, once per pause):** `Your remark is in the record. The
  room stays paused — nudge a character for a single turn, or press
  Resume.` (info) — raised by `announceChainPause` BEFORE its
  `pausedBefore` and all-LLM gates (v4 checks `heldUserTurn` first), at
  most once per pause: a latch set on the first announce and cleared
  whenever the chat's `isPaused` reads false.

### C.5 `files.generationKey` (P4.D182 carries; P4.D184 writes; P4.D185 reads)

- **Column:** `files."generationKey" TEXT` (nullable, no default) + index
  `idx_files_generationKey` on `("generationKey")`. The column IS in the
  D23 re-dump (v4's `FileEntrySchema` declares it); the index is created
  by the boot ensure (`db/files_generation_key_repair.rs::ensure_files_
  generation_key_column_and_index`, which also adds the column on a
  pre-4.10 instance).
- **Rust:** `FileCreate.generation_key: Option<String>` (bound on
  INSERT), `FileEntry.generation_key: Option<String>` (marshalled as
  `generationKey`, `null` when absent, at v4's `FileEntrySchema` position
  — the key-order comparand of `files_tier2_equivalence`), `FileFull`
  likewise, `FilesRepository::find_by_generation_key(&str) ->
  Vec<FileEntry>` (a plain indexed read; the hit/miss policy is NOT here).
- **Export/backup/import/restore:** the backup `FILES` const gains
  `("generationKey", F::StrOpt)` (the `.qtap` file records reuse it);
  the value travels AS-IS and is NEVER remapped on import or restore
  (the vendored `qtap-export.schema.json` gained it: "Travels as-is …
  Only avatar rows carry one"). Nothing derives a key on import.
- **Semantics (P4.D184):** written ONLY by the avatar job on a fresh
  generation (`cache_keys.key`, the v1 key of the REQUESTED profile, last
  write wins) and by the collapse heal's backfill (the v0 legacy key).
  Every other file leaves it NULL. **Read (P4.D185):** a roll is
  `generationKey IS NOT NULL AND category = 'IMAGE'` scoped by the
  character TAG — no path predicate.

### C.6 Avatar rolls (P4.D185 server ↔ P4.D188 client)

- **`AvatarRollEntry`** (wire, key order as listed): `{ fileId: string,
  rollLinkId: string | null, albumLinkId: string | null, fileName: string,
  url: string, mimeType: string | null, fileSizeBytes: number, width:
  number | null, height: number | null, createdAt: string,
  generationPrompt: string | null, generationModel: string | null,
  sha256: string, isPortrait: boolean, usedInChatCount: number }`.
- **Verb `characterAvatarRollList`** — `Request::CharacterAvatarRollList
  { character_id: String, limit: Option<i64>, offset: Option<i64> }` →
  `CoreResponse::CharacterAvatarRolls(Value)`, the BARE `{ entries:
  AvatarRollEntry[], total: number, hasMore: boolean }` (limit clamped
  to 1..=200, default 60; offset ≥ 0; newest first; paginated in
  memory). Refusals: 404 `Character not found`.
- **Verb `characterAvatarRollAction`** — `Request::CharacterAvatarRoll
  Action { character_id: String, file_id: String, action: String }`
  with `action ∈ { "save-to-album", "set-avatar" }` → `CoreResponse::
  CharacterAvatarRollAction(Value)`: `{ linkId: string, alreadyInAlbum:
  boolean }` for `save-to-album`, `{ linkId: string, addedToAlbum:
  boolean }` for `set-avatar`. Set-avatar ALWAYS goes through
  save-to-album first (idempotent) and points `defaultImageId` at the
  album LINK id, never a `files` id.
- **Verb `characterAvatarRollDelete`** — `Request::CharacterAvatarRoll
  Delete { character_id: String, file_id: String }` → `CoreResponse::
  CharacterAvatarRollDelete(Value)`: `{ deleted: boolean, blobRemoved:
  boolean, chatsScrubbed: number, keptInAlbum: boolean }`; `deleted:
  false` (not a roll of this character) answers 404 `Avatar roll not
  found` at the edge. Order: scrub `chats.characterAvatars` → the
  character's `defaultImageId` (legacy files-id shape only) +
  `avatarOverrides` → the roll's OWN link via `delete_with_gc` (never
  the album's; never `delete_mount_blob`) → `files.delete`.
- **Error ladder (both action verbs + delete, measured from v4's
  `respondToError`):** message starts with `Character not found` → 404
  `Character not found`; starts with `Avatar roll not found` → 404
  `Avatar roll not found`; contains `no linked database-backed vault` /
  `not an image` / `empty bytes` / `already in` → 400 with the message;
  else 500 with the message + v4's `[Characters/AvatarRolls v1] Avatar
  roll action failed` error line.
- **REST (`characters_routes.rs`):** `GET /api/v1/characters/{id}/avatar-
  rolls?limit=&offset=` (Zod: `limit` int 1..=200, `offset` int ≥ 0;
  an issue list joined `; ` → 400; `!id` → 400 `Missing character id`);
  `POST /api/v1/characters/{id}/avatar-rolls/{fileId}?action=save-to-album
  |set-avatar` (v4's `withActionDispatch` envelope: no action → 400
  `{ error: 'Action parameter required', availableActions: [...] }`;
  unknown → 400 `{ error: 'Unknown action: <x>', availableActions:
  [...] }`); `DELETE /api/v1/characters/{id}/avatar-rolls/{fileId}`
  (`!id`/`!fileId` → 400 `Missing character id` / `Missing avatar roll
  id`). Guard order: auth → id presence → action → service → the ladder.
- **Log lines (info, byte-exact):** `[AvatarRolls] Roll copied into the
  photo album` `{characterId, fileId, linkId}`; `[AvatarRolls] Roll
  promoted to the character portrait` `{characterId, fileId, linkId,
  addedToAlbum}`; `[AvatarRolls] Roll deleted` `{characterId, fileId,
  rollLinkId, keptInAlbum, blobRemoved, chatsScrubbed}`.
- **Album membership (P4.D185, both reads):** `is_character_album_
  relative_path(p)` = `is_photos_relative_path(p) || lower(p) ==
  "images/avatar.webp"` — `images/history/` is NOT album material any
  more. The character GET's `_count` gains `photos` on that predicate
  (v4 `handlers/get.ts:318-356`).
- **SPA (`characters.api.ts`):** `characterKeys.avatarRolls(id) =
  ['characters', id, 'avatar-rolls']`; `fetchAvatarRolls(core, id)`
  (limit 200), `avatarRollAction(core, id, fileId, action)`,
  `deleteAvatarRoll(core, id, fileId)`; every mutation invalidates
  `avatarRolls(id)`, `photos(id)` AND `detail(id)`.

### C.7 Gate constants (ACTIVATE-AT-UNIFY — named, never capability probes)

- `P4D183_SERVER_LANDED` — `apps/web/e2e/salon-transcript-subscribed-
  read.spec.ts` (P4.D187): the cheap read answering `unchanged` and a
  hinted re-read delivering a row no stream carried.
- `P4D186_SERVER_LANDED` — the real-server beats in `apps/web/e2e/salon-
  paused-hold-flow.spec.ts` (P4.D187): a send into a paused room draws
  no reply and raises the C.4 toast once; a Nudge on a paused room draws
  exactly one turn and leaves the pause standing. The injected-frame
  beats in the same file are LIVE from the start.
- `P4D185_SERVER_LANDED` — `apps/web/e2e/character-avatar-rolls-flow.
  spec.ts` (P4.D188).

### C.8 What no order invents

No new realtime topic and no second socket; no `transcriptVersion` on
`ChatUpdate`, in exports, backups or the fresh schema; no `force` on the
automatic wardrobe-change trigger (only the manual regenerate sets it);
no generation-prompt viewer for a roll (v4 has none — §R.6(8)); no
empty-state string for the rolls section (§R.6(9)); no persistence for
"Show shared"; no server-side once-per-pause throttle (§R.6(1)); no
export-side filter for `transcriptVersion` (its absence from every column
list IS the mechanism); no v4 migration runner.

## §R — the round-wide meeting points (BINDING — identical in every order of this round)

- **R.1 The round.** The `31436bae4` drift catch-up round: **P4.D182 →
  {P4.D183 ∥ P4.D184 ∥ P4.D185} ∥ P4.D186 ∥ P4.D187 ∥ P4.D188.** Ordered
  2026-09-14 from `main` `ecdcf9e5`. The oracle baseline is **`f4ad2c8d1`**;
  v4 `main` HEAD is **`31436bae4`** ("fix(salon): a paused chat generates
  nothing on its own (bugs 137-140)", 2026-09-13 22:49 -0500,
  `4.10.0-dev.30`), EIGHT commits past the baseline — the drift ledger's
  eight §3 rows, every one now `ORDERED(...)`. The round absorbs all eight:
  `5029075bb` (the Salon transcript as a subscribed read — P4.D182 substrate,
  P4.D183 server, P4.D187 SPA), `7fbf8a55b` (the avatar configuration cache
  — P4.D182 substrate, P4.D184 server), `4dcbe0d21` (Avatar Rolls — P4.D185
  server, P4.D188 SPA), `055cac45a` ("Show shared" — P4.D188), `8275b3642`
  (bugs 135/136 — P4.D187), `31436bae4` (bugs 137–140 — P4.D186 server,
  P4.D187 SPA), and the two NO-PORT? rows `4dc48283d` + `aecf9de0b`
  (ratified with evidence by P4.D182). The standing rule holds: drift debt
  clears before new scope, so the round IS the catch-up.
- **R.2 The freshness probe (ledger §2), expected values at planning
  (2026-09-14):** `git -C ~/source/quilltap-server branch --show-current`
  → `main`; `git -C ~/source/quilltap-server status --short` → EMPTY;
  `git -C ~/source/quilltap-server log --oneline 31436bae4..main` → EMPTY;
  `git -C ~/source/quilltap-server log --oneline 1a2b2164c..bugfix` →
  EMPTY. Run it at lane start and before EVERY regen batch. **Any
  mismatch: STOP and report** (a lane never writes the ledger). v4 is the
  human's active repo — never stash, checkout, or switch it.
- **R.3 Regen rule: PIN REQUIRED.** v4 HEAD is eight commits past the
  baseline. Every oracle regeneration, fixture build, recorder run and
  `QT_V4_ROOT` gate runs from a LANE-UNIQUE detached worktree built by the
  ledger's §5.1 recipe (all THREE symlink classes). Two pins per lane, named
  `/tmp/qt-v4-pin-<lane>-<sha>`:
  - the **catch-up target `31436bae4`** — the hunks you port, the `help/`
    and `public/schemas/` re-vendor sources, the new families' oracles, and
    `QT_V4_ROOT` for the embed guards on every lane that INCLUDES P4.D182's
    tip (P4.D182 itself after its re-vendor commits; P4.D183/P4.D184/
    P4.D185 stacked on it);
  - the **baseline `f4ad2c8d1`** — the neutrality legs (a family that
    must NOT move regenerates here first, then at the target) and
    `QT_V4_ROOT` for the workspace gate on every lane branched from `main`
    (P4.D186/P4.D187/P4.D188 — their embedded `help/` stays at the
    baseline's 124 files and their vendored `qtap-export.schema.json` at
    the baseline's bytes, so `help_tree_embed_guard` and
    `qtap_schema_embed_guard` are GREEN only against the baseline pin;
    against the live checkout the schema guard is RED by design until
    P4.D182 lands — ledger §1).
  Verify each pin by a marker only one tree has (`paused-hold.ts` exists
  at the target and not at the baseline; `transcriptVersion` appears in
  `lib/database/repositories/chats-messages.ops.ts` only at the target).
  Check the NDJSON is non-empty before believing any diff (§5.1's
  empty-file trap; §5.2's silent-stale-pass trap). Never run two
  `recipe_sweep.py` sweeps concurrently.
- **R.4 Stacking.** P4.D182 is the round's SUBSTRATE and finishes FIRST.
  P4.D183, P4.D184 and P4.D185 branch from P4.D182's recorded TIP (not from
  `main`) and may start only after P4.D182's lane record names that tip;
  after that commit P4.D182's public surface is FROZEN (a further P4.D182
  commit that the stacked lanes need is a STOP-and-flag). P4.D186, P4.D187
  and P4.D188 branch from `main` at `ecdcf9e5`. Every lane: its own
  worktree + lane branch (`claude/p4-d18N-<slug>-<hash>`), edit inside it
  (`edit-inside-the-worktree-not-main`), `CARGO_INCREMENTAL=0`, its own
  `target/` (budget 50–70 GB per long lane — `parallel-round-playbook` §B),
  `npm ci` never `cp -a` for an SPA worktree, ONE Playwright at a time
  repo-wide (port 4319).
- **R.5 The schema facts, measured at planning (do not re-derive).**
  (a) `chats."transcriptVersion" INTEGER DEFAULT 0` is deliberately OUTSIDE
  v4's `ChatMetadataSchema`/`ChatMetadataBaseSchema` (Zod strips it from
  every whole-row write; `$inc` is its only writer), so v4's `generateDDL`
  NEVER emits it — **the D23 re-dump cannot carry it; it arrives by boot
  ensure ONLY** (the P4.D145 bug-114 shape). v4 reads it raw
  (`ChatsRepository.getTranscriptVersion`, `0` when null/missing/failed)
  and projects it on the chat GET by a separate read. It is absent from
  `.qtap` exports and backups because it is absent from the schema — v4
  has NO export-side filter, and v5 must add none: keep it out of every
  column list that feeds `chats_read::marshal_row`, `ChatUpdate`, the
  export writers and the restore readers. (b) `files."generationKey" TEXT`
  (nullable, no default) IS in v4's `FileEntrySchema`, so the D23 re-dump
  DOES carry the column; the index `idx_files_generationKey` is created by
  the migration only (`CREATE INDEX IF NOT EXISTS "idx_files_generationKey"
  ON "files" ("generationKey")`) — measure whether the re-dump emits it and
  carry the boot ensure for BOTH the column and the index either way. It
  travels as-is through `.qtap` (the vendored export schema gained it) and
  is NOT remapped on import. (c) `help/**` stays at 124 files with EIGHT
  modified at the target (`character-gallery`, `chat-multi-character`,
  `chat-participants`, `chat-settings`, `chat-turn-manager`, `chats`,
  `tabbed-workspace`, `wardrobe`). (d) `public/schemas/qtap-export.schema.
  json` gained the `generationKey` property (+4 lines); v5's vendored copy
  is at the baseline bytes (`VENDORED_BYTES = 92_797`).
- **R.6 Prose traps measured at planning (ledger §5.3).** Port the hunks:
  (1) v4's "the first held message of each pause raises a notice" is
  CLIENT-side — the server emits `heldUserTurn: true` on EVERY held turn's
  chain-complete frame; the once-per-pause throttle is a client ref reset
  when the pause lifts. (2) "Nudge and Skip no longer clear the pause;
  `triggerContinueMode`'s `isPaused` guard is deleted" is TWO deletions in
  TWO hooks (the unpause-first legs in `useTurnManagement.handleNudge`/
  `handleSkip`, and the `if (isPaused) return` inside `useSSEStreaming.
  triggerContinueMode`). (3) Bug 138 and bug 139 live in `SalonView.tsx`,
  not `useTurnManagement.ts`. (4) Bug 135's doc says "three writes", its
  commit message "four" — use the doc's enumeration. (5) `transcript-
  projection.ts` is a byte-identical extraction of `handlers/get.ts` —
  MEASURE the neutrality anyway. (6) The publish payload is the bus's
  pre-existing `{v:1, topic:'chats', id}` envelope, not a two-key literal.
  (7) The feature doc's planned "own slot" for the optimistic bubble was
  NOT what shipped — the bubble stays IN the messages array, retired by
  reconciliation. (8) "View it with its generation prompt" (Avatar Rolls)
  is NOT implemented in v4 — the modal's `ImageData` has no prompt field;
  do not build one. (9) `AvatarRollsSection` renders NOTHING when a
  character has zero rolls — there is no empty-state string. (10) v4's
  collapse migration is NOT transactional (resumable by design) and its
  own commit prose disagrees with itself on the counts (1785/888/146 MB vs
  1786/889/141 MB) — the algorithm is the spec.
- **R.7 Fixture ownership.** No two lanes regenerate one committed
  fixture. P4.D182 regenerates NO committed DB pair (the P4.D171 rule);
  P4.D183 owns the `chats-messages-*` / `salon-reads` / messages-edge
  families and any NEW committed pair it needs; P4.D184 owns the
  `avatar_job_tier3` fixture and a NEW committed collapse fixture; P4.D185
  owns a NEW committed `avatar-rolls-{main,mount}.db` pair and the
  `character_get`/photos families; P4.D186 owns the `orchestrator_tier3`
  fixture additions; P4.D187/P4.D188 own no crate fixture (SPA recorders
  under `apps/web/oracle/` + `src/testing/fixtures/` only).
- **R.8 Version-bump ownership.** Per commit, per `.claude/commands/
  commit.md` §6, each lane bumps only the crates it touches: P4.D182 —
  core, harness, host (+ web if a web test venue's reduced DDL moves);
  P4.D183 — core, harness, web (+ host if the host wires anything);
  P4.D184 — core, harness, host; P4.D185 — core, harness, web; P4.D186 —
  core, harness (+ web for the wire pin); P4.D187 — SPA only; P4.D188 —
  SPA only. The unifier recounts as base + total bumps (base: core
  0.0.894, harness 0.0.783, host 0.0.129, web 0.0.142, cli 0.0.20, tauri
  0.0.7, SPA 0.5.707) — identical cross-lane bumps auto-merge SILENTLY.
- **R.9 Append-only docs.** `docs/CHANGELOG.md` (an entry per commit, the
  §7 shape) and `docs/developer/porting/status-log.md` (the lane record,
  appended at the END) are append-only for every lane; they union-merge at
  unification. Nobody edits CLAUDE.md, `phase-4.md`, `drift-ledger.md`,
  `dogfood-findings.md`, `docs/v4/**`, or another lane's order file. Each
  lane rewrites ONLY its own order's status header.
- **R.10 Standing rules that bite here.** Spelling — "Quilltap", never
  the quilt-based misspelling (`harness/tools/check_spelling.py` + the `spelling_guard`
  test). No stubs, no `TODO`s; every deferral is a loud typed refusal
  named in the lane record. Every ported unit ships with its differential
  driving v4's REAL code from the pin (tier 1 exact / tier 2 structural DB
  diff / tier 3 mocked-LLM), plus mutation proofs each reddening exactly
  its named rows, reverted by FILE BACKUP never `git checkout <file>`. A
  guard whose other conjuncts are false in every arm is untested — pin
  each conjunct with the others OPEN. A log-only change is invisible to
  every differential — pin it with a thread-scoped capture layer. A wire
  that no test drives is unpinned — a production-zone census or a wiring
  probe. Every long gate: `run_in_background: true` + full-log capture +
  a sentinel; NEVER a `pgrep`/`grep` poll loop, NEVER `| tail`.
- **R.11 The docs mirror + the ledger are the unifier's.** `docs/v4/**`
  refreshes at unification (the two feature docs, the six bug files,
  `bugs.md`, `API.md`, `DDL.md`, `CHANGELOG.md`); the ledger's eight rows
  move to §6 at the baseline move to `31436bae4`.

## Ownership (BINDING — identical table in every order of this round)

No two lanes edit one source file. `docs/CHANGELOG.md` and
`docs/developer/porting/status-log.md` are append-only for everyone.
`api/types.rs` / `api/engine.rs` / `core-contract.ts` are shared by NAME
only inside the fences named here — a lane needing a variant outside its
fence STOPs and flags it. `crates/quilltap-web/tests/dispatch_wrong_type_
census.rs`'s constant is moved by BOTH P4.D183 and P4.D185 (each lane
records its own delta; the unifier recounts as base + both deltas).

| Lane | OWNS (may edit / create) | MUST NOT TOUCH |
|---|---|---|
| **P4.D182** (the substrate, from `main` — FIRST) | NEW `crates/quilltap-core/src/db/chats_transcript_version_repair.rs`; NEW `db/files_generation_key_repair.rs`; `db/mod.rs` (two `pub mod` lines); `crates/quilltap-host/src/host.rs` (TWO fenced `=== P4.D182 ===` ensure blocks after the P4.D171 blocks, nothing else); `db/files.rs` (the column through `FileCreate`/`FileEntry`/`FileFull`/`FILE_ENTRY_COLUMNS`/the INSERT/the marshal + `find_by_generation_key` + the reduced DDL at `:1081`); `services/backup/collect.rs` (`FILES` const); every restore/import reader of `files` records that must carry the key as-is (measure: `services/backup/restore*`, `services/qtap_import/**`, `services/system_import*`); `services/provisioning/{fresh_schema.json,schema-key-order.json}` (via the generators); `crates/quilltap-core/src/generators/qtap-export.schema.json` + the byte-count doc in `generators/qtap_schema.rs` + `crates/quilltap-harness/tests/qtap_schema_embed_guard.rs` (`VENDORED_BYTES`); `help/**` (eight byte copies from the target pin — count stays 124); the reduced-DDL sweep (`grep -rn 'CREATE TABLE files' crates harness` outside `src/db/`); `harness/oracle/provision/**` + the `files`/export/import/restore families' recipes + regens (`files_tier2`, `files_routes`, `provisioning`, `qtap_schema_validate`, `system_export`, `system_import`, `system_backup`, `system_restore`, `qtap_import`, `backup_uuid_remap`, `restore_vintage_state`); NEW `crates/quilltap-host/tests/host_boot_*` arms for the two ensures; this order's header + the two NO-PORT ratifications (lane record) | `db/chats_messages.rs`, `db/chats_search.rs`, `db/chats.rs`, `db/chats_read.rs` (READ — the census must not move), `api/**`, `services/orchestrator.rs`, `services/character_avatar_job.rs`, `services/avatar_generation.rs`, `photos/**`, `crates/quilltap-web/src/**` (except a reduced test DDL the sweep names), `apps/web/**`, `docs/v4/**`, every committed DB pair |
| **P4.D183** (the transcript server, stacked on P4.D182's tip) | `db/chats_messages.rs` (the announce + commit helpers, the five sites); `db/chats_search.rs` (the replace announce); `db/chats.rs` (`get_transcript_version` ONLY — no `ChatUpdate` field); `api/salon.rs` (the `transcriptVersion` projection + the extraction call); NEW `api/transcript_projection.rs` (`project_chat_transcript` — the message + off-scene half of `assemble_chat_get`, moved not rewritten); NEW `api/chat_transcript.rs` (the verb handler + the events listing); `api/types.rs` + `api/engine.rs` — FENCE: the `ChatTranscript`/`ChatMessageEvents` pairs appended directly after `MessageReattribute` (`types.rs:3217`) and the matching arms; NEW `crates/quilltap-web/src/messages_routes.rs` + the `lib.rs` route registration; `crates/quilltap-core/src/realtime/publish_sites.rs` (the new capturing tests); `crates/quilltap-web/tests/dispatch_wrong_type_census.rs` (its delta); harness: `chats_messages_ops_tier2`, `chats_messages_tier2`, `chats_messages_read`, `salon_reads`, `chat_scenario_routes`, `chat_export` (the NOT-exported pin) + their oracle cases/builders; NEW `crates/quilltap-harness/tests/transcript_route_equivalence.rs` + NEW `harness/oracle/cases/transcript-route.test.ts`; NEW `crates/quilltap-harness/tests/chats_search_announce_equivalence.rs` (or an arm in an existing search family); `crates/quilltap-web/tests/chats_collection_route.rs` (READ — the shape to copy); this order's header | `host.rs`, `db/files.rs`, `db/chats_read.rs` (READ), `services/orchestrator.rs`, `services/chat_events.rs`, `services/character_avatar_job.rs`, `photos/**`, `api/characters.rs`, `characters_routes.rs`, `provisioning/**`, `help/**`, `apps/web/**`, `docs/v4/**` |
| **P4.D184** (the avatar cache server, stacked on P4.D182's tip) | NEW `crates/quilltap-core/src/services/avatar_cache.rs`; `services/character_avatar_job.rs` (the restructure); `services/avatar_generation.rs` (`force` on the params + the payload); `api/chat_outfits.rs` (`chat_regenerate_avatar` passes `force: true` — that one line); `services/image_job_storage.rs` / `image_job_common.rs` ONLY where the avatar's project-upload branch is deleted (a shared helper another job uses stays); NEW `db/avatar_rolls_collapse_heal.rs` (the collapse pass + its `migrations_state` gate); `crates/quilltap-host/src/host.rs` — ONE fenced `=== P4.D184 ===` block AFTER P4.D182's two blocks (the heal call), nothing else; harness: `avatar_job_tier3` (fixture + spec + case additions), `character_avatar_write_tier2` (regen), NEW `avatar_cache_key_equivalence` (+ `harness/oracle/cases/avatar-cache-key.ts` + corpus), NEW `avatar_rolls_collapse_heal_equivalence` (+ its jest oracle over v4's REAL migration + a NEW committed fixture pair); this order's header | `db/files.rs` (READ — P4.D182's column + `find_by_generation_key` are yours to CALL), `db/chats_messages.rs`, `api/salon.rs`, `api/characters.rs`, `api/types.rs`, `api/engine.rs`, `photos/**`, `crates/quilltap-web/src/**`, `apps/web/**`, `help/**`, `docs/v4/**` |
| **P4.D185** (the avatar rolls server, stacked on P4.D182's tip) | NEW `crates/quilltap-core/src/photos/avatar_rolls_service.rs`; `photos/photos_paths.rs` (`is_character_album_relative_path` + `LEGACY_MAIN_AVATAR_PATH`); `photos/character_gallery_service.rs` (the filter swap at `:23-28` + the doc note); `services/character_enrichment.rs` (`_count.photos` on the measured predicate); `api/characters.rs` (three handlers appended after `character_photo_save_by_id`); `api/types.rs` + `api/engine.rs` — FENCE: the three `CharacterAvatarRoll*` pairs appended directly after `CharacterPhotoRemove` (`types.rs:722`) and the matching arms; `crates/quilltap-web/src/characters_routes.rs` + `lib.rs` (the two new sub-routes); `crates/quilltap-web/tests/dispatch_wrong_type_census.rs` (its delta); harness: NEW `avatar_rolls_tier2_equivalence` (+ `harness/oracle/cases/avatar-rolls-tier2.test.ts` + NEW committed `crates/quilltap-web/tests/fixtures/avatar-rolls-{main,mount}.db` + their `.meta.json` sidecars + the builder in PROSE), `characters_reads` (regen + the `_count.photos` arm), `photos_routes_equivalence` + `photos_web_routes` + `characters_photos_routes` (the `images/history/` drop, red-first); this order's header | `db/**` (READ — P4.D182's column is yours to READ; `delete_with_gc` yours to CALL), `services/character_avatar_job.rs`, `services/avatar_generation.rs`, `host.rs`, `api/salon.rs`, `db/chats_messages.rs`, `apps/web/**`, `help/**`, `docs/v4/**` |
| **P4.D186** (the paused hold server, from `main`) | NEW `crates/quilltap-core/src/services/paused_hold.rs`; `services/orchestrator.rs` (the seam, the three `!hold` conjuncts, `finish_held_user_turn`); `services/chat_events.rs` (`held_user_turn`); harness: `orchestrator_tier3` (fixture + spec + case additions), NEW `paused_hold_equivalence` (+ `harness/oracle/cases/paused-hold.ts`), a web-venue or core wire test pinning the frame bytes + the info line (capture layer); this order's header | `db/**`, `api/**` (READ — `SendMessageOptions` is in `orchestrator.rs`, yours), `services/turn_orchestrator.rs` (READ — the bug-123 `paused` emits are NOT this lane's), `host.rs`, `photos/**`, `crates/quilltap-web/src/**`, `apps/web/**`, `help/**`, `docs/v4/**` |
| **P4.D187** (the Salon SPA, from `main`) | `apps/web/src/app/screens/salon/salon-conversation.ts` (+ its specs, `salon-turn-controls.spec.ts`, `salon-settings-live.spec.ts` if touched); NEW `chat/transcript-reconcile.ts` (+ spec + `apps/web/oracle/transcript-reconcile.recorder.ts` + `src/testing/fixtures/transcript-reconcile.ndjson`); NEW `chat/chat-transcript.api.ts`; `chat/chat-view-model.ts` (only if the reconcile supersedes `splitSwipeGroups` — measure); `core/chat-stream.reducer.ts` (`chainHeldUserTurn`) + spec; `core/core-contract.ts` — FENCE: `ChatDetail.transcriptVersion?` + the C.2 DTOs beside the `ChatDetail`/`MessageDto` section; `chat/chat-composer.ts` (+ spec) ONLY if the bug-136 measurement puts the refusal there; `chat/turn-controls.ts` (+ spec) ONLY if the measurement puts a refusal there; e2e: `salon-optimistic-bubble-reconcile.spec.ts` (re-gestured), `salon-chain-pause-toast-flow.spec.ts` (the held-turn injected beat), `m4b-salon.spec.ts` (the skip beat), NEW `salon-transcript-subscribed-read.spec.ts` (gated), NEW `salon-paused-hold-flow.spec.ts` (mixed); `e2e/support/mock-llm.ts` ONLY additively (a slow-stream knob); this order's header | every `crates/**` file; `help/**`; `docs/v4/**`; `screens/characters/**`; `wardrobe/**`; `images/**`; `e2e/support/**` fixture SEEDS (read; a needed seed change is a STOP-and-flag) |
| **P4.D188** (the gallery + wardrobe SPA, from `main`) | `apps/web/src/app/screens/characters/view/tabs/gallery-tab.ts` (+ spec: the section mount); NEW `screens/characters/view/tabs/avatar-rolls-section.ts` (+ spec); `screens/characters/characters.api.ts` (the key + three helpers); `core/core-contract.ts` — FENCE: the C.6 DTOs beside the `CharacterPhoto*` section; `wardrobe/wardrobe-control-dialog.ts` (+ spec); e2e: `wardrobe-flow.spec.ts` (ONE new Show-shared beat, LIVE), NEW `character-avatar-rolls-flow.spec.ts` (gated), `characters-flow.spec.ts` ONLY if the section's presence changes an existing assertion; this order's header | every `crates/**` file; `help/**`; `docs/v4/**`; `screens/salon/**`; `chat/**`; `core/chat-stream.reducer.ts`; `images/photo-gallery-modal.ts` (READ — the Salon gallery is not the character tab); `e2e/support/**` seeds (a needed seed change is a STOP-and-flag) |

Round-wide: nobody edits `CLAUDE.md`, `phase-4.md`, `drift-ledger.md`,
`dogfood-findings.md`, `docs/v4/**`, another lane's order, or a committed
DB pair another lane owns.

## Verification gate (before declaring done — identical in every order of this round)

Run from the lane worktree, `CARGO_INCREMENTAL=0`, `TZ=UTC`, every long
command with `run_in_background: true` + full-log capture + a sentinel
(`background-gate-latency-fix`); never a poll loop, never `| tail`.
`QT_V4_ROOT` = this lane's §R.3 pin for the embed guards (the target pin for
P4.D182-after-re-vendor / P4.D183 / P4.D184 / P4.D185; the baseline pin for
P4.D186 / P4.D187 / P4.D188).

1. **The probe (§R.2)** — passed at lane start and before every regen.
2. `cargo fmt --all --check` clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` clean in BOTH
   feature sets (default and `--features quilltap-core/native-transport`).
4. `cargo build --workspace --release` clean (the e2e binary is the
   RELEASE build — `webBinary()` prefers `target/release`).
5. **This lane's differentials by NAME**, each over an oracle regenerated
   FRESH from the pin through `harness/tools/recipe_sweep.py --v4 <pin>
   --run <family>` (never two sweeps at once), zero `SKIP:` lines, every
   NDJSON non-empty, the CHANGED bytes grepped in the NDJSON (§5.2), and
   the red-first evidence recorded (the pre-port run RED on the new
   arms, then green).
6. **The mutation proofs** named in the order, each reddening exactly its
   rows, each reverted by FILE BACKUP.
7. `cargo test --workspace` with the lane's env block (its own recipe
   vars + `QT_V4_ROOT`; the untouched families' vars deliberately
   withheld) — record `<N> test binaries / <P> passed / 0 failed`, exit
   0, ZERO `SKIP:` lines for the lane's families (confirm each RAN by
   name and non-zero duration — `a-family-env-var-is-not-its-regen-var`).
   `cargo test --workspace` is fail-fast per BINARY — read the whole log.
8. **SPA lanes (P4.D187 / P4.D188) and any lane whose diff touches
   `apps/web/**`:** `npm run lint` (incl. `check-qt-classes`), `npm test`
   (`ng-run.mjs` — never bare `ng test`), `npm run build`, then the FULL
   Playwright suite against the release binary (`npx playwright test`,
   one at a time repo-wide; record passed / failed / skipped; every red
   re-run by FILE in isolation and classified — the documented
   intermittents are the P4.D161 pause-toast beats and the P4.d17 quill).
   A lane that touches no `apps/web/**` file records "no e2e owed".
9. **Ownership honoured:** `git diff main -- <every MUST-NOT-TOUCH path>`
   EMPTY (stacked lanes diff against P4.D182's tip).
10. **Per commit** `.claude/commands/commit.md`: the CHANGELOG entry (H4
    header + versions line), the version bumps (§R.8), the status-log
    lane record appended at the END (units, measurements, the mutation
    table, the gate numbers, "spotted, not mine" items for the unifier,
    the regen recipe), the spelling grep, this order's status header
    rewritten. Push the lane branch; never merge to `main`.
