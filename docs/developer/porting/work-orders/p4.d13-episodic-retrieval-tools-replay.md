# Work order P4.d13 — the episodic-recall drift catch-up, **round 2 / single lane: time+entity-aware retrieval (workstream B) + the deep-dive tools (workstream D) + the §3 replay harness** — round baseline `8bf3cb5f`

> **STATUS: OPEN** (planned 2026-07-21; not started). The unifier updates
> this header at unification.
>
> Round-2 lane of the **episodic-recall drift catch-up campaign** (v4
> `8bf3cb5f` — the largest single drift in the port; round 1 = P4.d12 ∥
> P4.6bh ∥ P4.6bi, UNIFIED 2026-07-21, `b79b659f`). Round 1 landed the
> episodic SPINE (five columns, the pure `episodic` module, weighting
> `episodicBonus`, the dated dynamic head). **This round makes the
> columns start EARNING: retrieval becomes time- and entity-aware, the
> deep-dive tools learn time filters + the anti-confabulation norm, and
> the §3 recall-replay harness lands end-to-end (module + dispatch verb
> + CLI).** Round 3 (creation-side extraction + the fourth cadence
> part 2 + stop-destroying-episodes + the "Story's Clock" SPA) stays
> deferred — see §"The round-2 / round-3 boundary".

## Preamble

**v4 reference baseline for this round: `8bf3cb5f` (4.9-dev, "Unify
episodic recall + character outfit work") — verified at planning
(2026-07-21): v4 HEAD == `8bf3cb5f`, tree clean.** At lane start, run in
`~/source/quilltap-server`:

```bash
git log 8bf3cb5f..HEAD --oneline
```

**If HEAD moved, STOP and report.** (v4 is mid-4.8/4.9 development — a
version/tag commit may land; report it either way.) v4's checkout is
clean at the baseline, so oracles regenerate directly from
`~/source/quilltap-server`; pin a detached worktree only on drift/dirty
(recipe: the `oracle-regen-pinned-v4-worktree` memory — symlink BOTH
`node_modules`: root + `packages/{quilltap,plugin-types,plugin-utils}` +
`plugins/dist/*`).

**Sibling lanes: NONE — this is a deliberate single-lane round.** The
scope is one deep vertical: workstream D's search handler passes the new
search options (`occurredWithin`, `aboutCharacterId`) straight into
`searchMemoriesSemantic`, and the §3 replay's `newPath` table exercises
the live retrieval ranking — so a parallel D or §3 lane could not run
its own differential in-lane. Don't manufacture fake parallelism
(setupphase rule); sequence the units instead. You run in a worktree; a
unification pass integrates onto main. (Single lane ⇒ the e2e port 4319
is yours alone — the `e2e-port-4319-cross-worktree-kill` memory hazard
does not apply unless the human runs something else.)

**⚠ Baseline movement this round:** the families this round touches
regenerate at `8bf3cb5f` (recall-tags, the memory-tasks SEARCH-extraction
subset, search-tools, tool-definitions, chats-search/vault-summary as
touched, build-context re-regen, + the NEW replay family). The deferred
round-3 families **stay at `7e6d13e5`** (gate tier-3 `QT_ORACLE_GATE`
stays un-regenerated/SKIP — v4's first-write
`applyEpisodicFallbackAnchors` is non-inert on AUTO-source proper-noun
content; processor tier-3; the memory-tasks CREATION-prompt cases;
context-summary/fold; carina-extraction; recall-history). Mixed vintages
are the established pattern.

## The round-2 / round-3 boundary (binding — read before coding)

v4's squash mixes the workstreams inside shared files. The cut lines:

- **`lib/memory/cheap-llm-tasks/memory-tasks.ts`** — round 2 ports ONLY
  the search-keyword extraction (`extractMemorySearchKeywords` +
  `MemorySearchExtraction` + the TODAY line + the new system-prompt
  fields). The creation-side changes (the CLOCK block in the SELF/OTHER
  extraction prompts, `EVENT_INSTRUCTION_BLOCK`, `kind`/`when`/`entities`
  candidate coercion, `EVENT_EXTRA_SLOT`/`capCandidates`, the
  `FOLD_EPISODE_PROMPT` + `extractEpisodesFromFold`) are **round 3**.
  The shared `ExtractionClock` type (memory-tasks.ts:107–141) IS round-2
  scope — the search extraction takes it for the TODAY line; the
  creation prompts pick it up in round 3.
- **`lib/chat/context-manager.ts` (`buildContext`)** — round 2 ports
  **part 1 of the fourth cadence**: the turn-recall-signals thread, the
  search-call options (`entityAnchors`, `occurredWithin` gated on
  `fallbackRetro`, the multi-probe list), and the retro head sizing
  (`RETRO_HEAD_TOKEN_BUDGET`/`RETRO_HEAD_SIZE` + the
  `isRetrospectiveTurn` ternaries). **Part 2 is round 3**: the scoped
  mini-recap block (context-manager.ts ~:1940–2030), the
  `retrospective-recall` whisper post, the spam-guard signature, the
  `recall-history.ts` retro-signature machinery, and the commonplace
  writer's `retrospectiveRecall` param. Rationale for pulling part 1
  forward: the replay harness imports `RETRO_HEAD_SIZE` for its newPath
  head selection, and the retrieval call expression is inseparable from
  the sizing ternaries. **Fixture guard:** any retrospective-arm
  build-context/replay fixture must have NO vault conversation summaries
  that match the probe — then v4's part 2 emits nothing
  (`retrospectiveRecallContent` stays `''`) and the deferred half is
  provably inert in the diff.
- **`lib/memory/recall-history.ts`** — the `parseRetroSignatures` /
  `appendRetroSignature` / `RETRO_SIGNATURE_TURNS` additions are round 3
  (the spam guard). `QT_ORACLE_RECALL_HISTORY` does not regenerate this
  round. The replay reads `recentlyWhisperedIdSet` only — pre-existing.
- **`lib/chat/context-summary.ts`** — the fold-episode-pass call +
  `createdAt` on folded messages: round 3. `partitionMessagesIntoTurns`
  is pre-existing (v5 `context_summary.rs:215`) and is what the replay
  consumes — no context-summary changes this round.
- **Untouched round-3 files (do not open):** `memory-gate.ts`,
  `memory-processor.ts`, `fold-episode-pass.ts`, `compression-tasks.ts`,
  `chat-tasks.ts`, `housekeeping.ts` (its +7 is the E protection bump —
  verify: round 1 already landed `episodicBonus`; if the +7 is only that,
  nothing remains), `carina-memory-extraction.ts`,
  `commonplace-notifications/writer.ts`, `ChatSidebar.tsx` (Story's
  Clock SPA), `turn-transcript.ts` (the `occurredAt` stamp anchor —
  rides the round-3 processor work).

**No SPA work this round.** `apps/web` should not change (the replay is
CLI/API-only; the tools are server-side). If you find yourself editing
`apps/web`, stop and re-read this section.

## Mandate

Deliver, verified against v4 `8bf3cb5f`:

1. **Workstream B — time+entity-aware retrieval:** the search-extraction
   signals (`retrospective`/`timeRange`/`entities` + the TODAY line) in
   the distill task; `turnTemporal` made real in the recall-tags
   multiplier loop; `searchMemoriesSemantic`'s `occurredWithin` two-stage
   window (hard filter → soft ×1.3 fallback boost), entity anchoring, and
   retrospective multi-probe; vault conversation-summary date
   filter/boost + dated matches.
2. **Workstream D — the deep-dive tools:** `search` gains
   `since`/`until`/`aboutCharacter` + episodic result fields;
   `read_conversation` gains the interchange range; the v5 `memorySearch`
   catalog entry is DELETED (v4 deleted `memory-search-tool.ts`; the
   58-tool invariant becomes 57); the anti-confabulation orchestration
   prose in both tool-instruction builders.
3. **§3 — the replay harness:** the `recall_replay` module (old-path vs
   new-path candidate tables), the `recall-replay` chat action verb on
   the dispatch surface, and the `quilltap recall-replay` CLI subcommand
   (+ completions/help), each with its differential.

Must NOT touch: the round-3 files/behaviors above; the schema (round 1's
D23 re-dump is complete — this round adds NO columns); `apps/web`.

## Survey-verified starting points (fresh, 2026-07-21)

### v4 (all at `8bf3cb5f`; line numbers verified)

| What | Where |
|---|---|
| `MemorySearchExtraction` (+ episodic fields) | `lib/memory/cheap-llm-tasks/memory-tasks.ts:1182–1204` |
| `extractMemorySearchKeywords` (clock param, TODAY line, parse) | `memory-tasks.ts:1221–1350` — TODAY line ~:1243–1249; retrospective strict-`=== true`; timeRange `YYYY-MM-DD` regex + `Date.parse` finite + date-only → full-day normalization (`T00:00:00.000Z`/`T23:59:59.999Z`) + `from<=to`; entities cap 5 |
| The new prompt fields (system prompt) | `memory-tasks.ts` `MEMORY_KEYWORD_EXTRACTION_PROMPT` ~:725–740 (retrospective / timeRange-resolve-against-TODAY / entities rules + the updated one-line JSON example) |
| `ExtractionClock` + `WEEKDAYS` + `renderClockBlock` | `memory-tasks.ts:107–141` (round 2 needs the type + WEEKDAYS for the TODAY line; `renderClockBlock` itself is only used by creation prompts — port it now ONLY if the search path needs it, which it does not) |
| recall-tags: `turnRetrospective`/`occurredWithin` on `RecallContext` | `lib/memory/recall-tags.ts:100–140` |
| recall-tags: new multipliers 1.15/1.0/1.3 | `recall-tags.ts:164–182` (`temporalPastRetrospective`, `temporalMomentRetrospective`, `occurredWithinWindow`) |
| `temporalMultiplier(tags, retrospective)` — fired labels `past↑retro`/`moment·retro` | `recall-tags.ts:288–320` |
| `occurredWithinMultiplier` (occurredAt ?? createdAt; Date.parse; fired `window↑`) | `recall-tags.ts:322–345` |
| `recentlyWhisperedMultiplier(…, suspended)` | `recall-tags.ts:393–405` |
| `MemoryTagView` gains `occurredAt`/`createdAt` | `recall-tags.ts:214–218` |
| search options: `occurredWithin`/`entityAnchors`/`probes` | `lib/memory/memory-service.ts:754–780` (`aboutCharacterId` at :758 is PRE-EXISTING — v5 already has it) |
| entity-anchor union (cap 3, trim, `searchByContent` literal path) | `memory-service.ts:889–930` |
| multi-probe (cap 2 extras, max cosine per memory) | `memory-service.ts` same hunk region — read the full :837–:986 block |
| two-stage window filter → fallback + `occurredWithin` into the multiplier loop | `memory-service.ts:985–1015` |
| conv-summary: `timeRange` option, `readCap = max(limit*3, 15)`, `tsField` frontmatter reads, `overlapsWindow`, `CONVERSATION_WINDOW_BOOST = 1.3`, dated `VaultConversationMatch` | `lib/memory/conversation-summary-search.ts:36–70, 140–210` |
| `READ_CONVERSATION_CALL_NOTE` render gains ` (YYYY-MM-DD)` | `conversation-summary-search.ts:219–226` |
| buildContext part-1 threading (`turnRecallSignals`, `fallbackRetro`, entity probe, retro limit/head sizing) | `lib/chat/context-manager.ts:1112–1290` (the PART-2 mini-recap at ~:1940–2030 is round 3) |
| `RETRO_HEAD_TOKEN_BUDGET = 600` / `RETRO_HEAD_SIZE = 10` | `lib/chat/context/memory-injector.ts:46–55` |
| pre-compute: `proactiveRecallTask` → `{ memories, signals }`; signals → `recallSignals` thread | `lib/services/chat-message/pre-compute.service.ts:72–120, 250–270`; `context-builder.service.ts:69–72, 491, 640`; `orchestrator.service.ts` (+3) |
| search tool schema `since`/`until`/`aboutCharacter` | `lib/tools/search-scriptorium-tool.ts:55–83`; result fields `occurredAt`/`narrativeTime` :99–106 |
| search handler: ISO normalization, `aboutCharacter` name/alias resolve (unresolved → NO memory hits), `occurredWithin` hard filter (comment: "no recallContext, so no soft fallback"), result fields incl. `kind` + `conversationId` (= `memory.chatId`), conversation span filter (chat `createdAt..updatedAt` overlap, `spanByChat` cache) | `lib/tools/handlers/search-scriptorium-handler.ts:81–210, 257–340` |
| read_conversation schema + description (`interchange_start`/`interchange_end`) | `lib/tools/read-conversation-tool.ts:28–50, 70` |
| read_conversation slicing (regex split on `^## Interchange \d+`, preamble keep, `interchange_end >= interchange_start` error, empty-range error string, `_Showing interchanges X–Y of Z._` footer, counts from SLICED markdown) | `lib/tools/handlers/read-conversation-handler.ts:59–160` |
| `memory-search-tool.ts` + its test: **DELETED** (the `search` collision fix) | v4 diff; audit v4's `buildToolsForProvider` for what routed it before |
| native-tool-prompt rule 6 (never-invent-specifics) | `lib/tools/native-tool-prompt.ts:29–35` |
| simple-json-prompt recall bullet | `lib/tools/simple-json-prompt.ts:249–254` |
| tool snapshot churn | `lib/tools/__tests__/__snapshots__/tool-definitions-snapshot.test.ts.snap` (±60) |
| replay module (rows/result/inputs; read-only; `toRows` head-size selection; the two search invocations) | `lib/memory/recall-replay.ts` (237 lines — read whole; imports `RETRO_HEAD_SIZE`, `partitionMessagesIntoTurns`, `recentlyWhisperedIdSet`, `getMemoryRecallSettings`) |
| replay route (body validation: `turnIndex` int ≥1, `characterId` string, `limit` int 1..100 clamp; cheap-LLM anchor-profile resolution; error strings `Chat settings not found.` / `No connection profiles configured.` / `No cheap LLM provider available.`; catch → 400) | `app/api/v1/chats/[id]/actions/recall-replay.ts` (93 lines) |
| action registration | `app/api/v1/chats/[id]/handlers/post.ts` (`CHAT_POST_ACTIONS` + map), `actions/index.ts`, `route.ts` doc line, `lib/startup/prettify.ts` (+1 route listing) |
| CLI command (flags `--turn/--char/--limit/--port/--json`, help text, ANSI table rendering) | `packages/quilltap/lib/recall-replay-command.js` (201 lines); `bin/quilltap.js:102,1112,1172–1174`; completion templates `lib/completion/{bash,fish,zsh}.template` |
| chat PUT `timelineMode` accept | `app/api/v1/chats/[id]/schemas.ts:22–28` (`z.enum(['realtime','narrative']).nullish()`) |
| v4's own new tests (reference, port case-for-case where a v5 unit test fits) | `lib/memory/__tests__/recall-tags-retrospective.test.ts` (126), `lib/memory/__tests__/episodic-visibility.test.ts` (96) |

### v5 (all under `/Users/csebold/source/quilltap-v5`; verified 2026-07-21)

| v4 counterpart | v5 landing site |
|---|---|
| `extractMemorySearchKeywords` | `crates/quilltap-core/src/services/memory_recap/distill.rs` — `distill_memory_search<C: CompletionProvider>` (parse closure ~L103). NOTE the v5 architecture difference: there is NO separate pre-compute service; the distill is called inline in `services/build_context.rs:1844–1878`. The v4 pre-compute `{memories, signals}` split maps onto that inline call — thread the parsed signals into the same fn's search options. |
| creation-side memory-tasks (round 3 — do NOT touch) | `crates/quilltap-core/src/memory_tasks.rs` + its `prompt_text` sibling |
| recall-tags | `crates/quilltap-core/src/recall_tags.rs` — consts L140–166, `temporal_multiplier` L306, `combine_recall_multipliers`, `TargetingTags` L125 |
| `searchMemoriesSemantic` | `crates/quilltap-core/src/services/memory_service.rs` — `search_memories_semantic<P: EmbeddingProvider>` L302, `SemanticSearchOptions` L278 (`about_character_id` already at L290 — only `occurred_within`/`entity_anchors`/`probes` are new) |
| conversation-summary-search | `crates/quilltap-core/src/services/memory_recap/mod.rs` — `search_vault_conversation_summaries<E>` L123 (chunk search: `db/conversation_search.rs:58`) |
| buildContext | `crates/quilltap-core/src/services/build_context.rs` (distill call site L1844–1878; the head-format call sites) |
| DYNAMIC_HEAD constants (RETRO go beside) | `crates/quilltap-core/src/memory_injector.rs:64–66` |
| `partitionMessagesIntoTurns` | `crates/quilltap-core/src/context_summary.rs:215` |
| search tool | `crates/quilltap-core/src/tools/search.rs` — validation L85, `execute_search_scriptorium<P>` L145, `format_search_scriptorium_results` L606 |
| read_conversation tool | `crates/quilltap-core/src/tools/read_conversation.rs` — validation L62, `execute_read_conversation` L109, `format_read_conversation` L218 |
| tool catalog (the `memorySearch` entry to DELETE) | `crates/quilltap-core/src/tools/definitions/data.rs:180–182` (`key: "memorySearch", name: "search"`); the `len() == 58` invariant `definitions/mod.rs:90–95` → 57. Audit how (whether) v5's tool build/dispatch ever resolves that key before deleting — mirror v4's `buildToolsForProvider` audit. |
| native tool prompt | `crates/quilltap-core/src/tools/native_tool_prompt.rs:5` |
| simple-json prompt | grep `crates/quilltap-core/src/tools/` for the text-tool-loop prompt builder (the text-tool loop family owns it) |
| dispatch (the new verb) | `crates/quilltap-core/src/api/engine.rs` `dispatch_impl` L427 (`Request::ChatTurnAction` arm L601); `api/types.rs` `Request` enum ~L215; the P4.6s memories-action precedent `api/memories.rs` (`not_available` idiom L66). Mirror v4: `recall-replay` is a chat POST action — follow however the existing chat actions marshal (action string vs dedicated variant; pick what the existing chats-routes differential shape dictates). Web edge: rides `POST /api/dispatch` (`crates/quilltap-web/src/lib.rs`) like the other action verbs — add a REST leg only if the chats routes family models one. |
| CLI | `crates/quilltap-cli/src/main.rs` — `SUBCOMMANDS` L30 (add `recall-replay`; note `memory-diff` sits there as a recognized-but-unshipped `not_yet_available` L79 — recall-replay must NOT join that list; it ships), new `recall_replay_cmd.rs` beside `db_cmd.rs`/`docs_cmd.rs`; completions `completion_cmd.rs`; help tree `help/` |
| chat PUT `timelineMode` | **GAP (verified):** `crates/quilltap-core/src/db/chats.rs` has full `timeline_mode` data-layer support (round 1: read L338, update-patch L545/L1000), but `grep -rn timelineMode crates/quilltap-core/src/api/` is EMPTY — the API accept arm is missing. Tier-2 item 10. |

### Known-good recipes + memory notes to reuse (do not invent)

- **JS date semantics:** v5 already pinned V8 `Date.parse` quirks in
  `crates/quilltap-core/src/episodic.rs` (round 1, the P4.d12 lane
  memory). **Reuse its parse helpers** for every `Date.parse` in this
  round (`occurredWithinMultiplier`, timeRange validation, window
  filters, span overlap) — do not hand-roll a second date parser. The
  episodic oracle needed `TZ=UTC`; assume the same for every new oracle
  case doing date math.
- **Tier-3 canned cheap-LLM:** `tier3-completion-oracle.md` (canned
  completions keyed by oracle-RECORDED keys) +
  `answer-confirmation-differential.md` (ONE createLLMProvider mock
  routed by prompt kind) — the distill mock for build-context/replay.
- **Jest real-DB oracle:** `jest-real-db-oracle.md` (resetModules+doMock,
  Node 24) + `oracle-jest-testtimeout.md` + `jest-oracle-bare-package-imports.md`
  (/tmp mirror → symlink node_modules) — the replay + handler oracles.
- **Fixture builders:** `project-store-fixture-needs-real-create.md`
  (provision via REAL repos), `chat-participant-fixture-shape.md` (the
  "user" is a `controlledBy:'user'` participant — the replay's
  participant resolution depends on this), `oracle-fire-and-forget-tail.md`
  (settle stray promises between mutating cases),
  `oracle-regen-silent-stale-pass.md` (grep the NDJSON — an erroring
  builder leaves the stale fixture).
- **CLI Tier R:** `p4.3-quilltap-cli.md` (console.table non-TTY geometry;
  lossy int→double) — the replay table renders with hand-rolled ANSI, not
  console.table, but the stdout-diff recipe is the same.
- **Search-tool defaults:** `p4.6y-mount-file-ops-remainder.md` (JS-falsy
  search defaults) — the search handler's default handling.
- **Fail-fast counting:** the P4.d12 lane memory — `cargo test`
  fail-fasts per TARGET; recount ok-lines on any failure.

## Tiered deliverables

### Tier 1 — must land (sequenced; each unit ships with its differential)

**Unit 1 — the distill signals (B's source).**
`distill.rs`: the `ExtractionClock`-equivalent input (nowIso +
timelineMode; v5 decides the idiomatic shape — a small struct mirroring
v4's, threaded from the chat), the TODAY line (weekday name via the
`WEEKDAYS` table, `timeline mode: {mode}` suffix, the non-finite-date
fallback arm), the new system-prompt bytes (three field rules + the
updated JSON example — byte-exact), and the parse: `retrospective`
strict-`=== true`; `timeRange` with the `^\d{4}-\d{2}-\d{2}` regex,
finite `Date.parse` (V8 semantics — the episodic.rs helpers), date-only
→ full-day normalization, `from <= to` else null; `entities` string
filter capped at 5 (verify v4's exact cap/trim in the tail of the parse —
read `memory-tasks.ts:1330–1360`). **Differential:** the memory-tasks
tier-1 family (`memory_tasks_equivalence.rs` / `QT_ORACLE_MEMORY_TASKS`,
case `harness/oracle/cases/memory-tasks-tier1.test.ts`) — **SPLIT it**:
the search-extraction cases regenerate at `8bf3cb5f` (extended with
retrospective/timeRange/garbled-parse arms + a clock-bearing prompt
case); the creation-prompt cases STAY at `7e6d13e5` (round 3). Precedent
for splitting a family: `p4.6s-memories-server.md` (routes+config
split). Two env vars or one file with vintage-tagged cases — pick
whichever keeps the SKIP semantics honest (a case must never silently
pass against the wrong vintage).

**Unit 2 — recall-tags made turn-aware.**
`recall_tags.rs`: `turn_retrospective` + `occurred_within` on the
context; the three new multiplier consts (1.15 / 1.0 / 1.3) with v4's
why-comments; `temporal_multiplier(tags, retrospective)` with the
`past↑retro`/`moment·retro` fired labels; `occurred_within_multiplier`
(occurredAt ?? createdAt, episodic.rs date parse, `window↑`);
`recently_whispered_multiplier(…, suspended)`; the combine loop wiring
(clamp [0,4] unchanged — ONE multiplier loop, no side-channel math).
Port v4's `recall-tags-retrospective.test.ts` case-for-case as v5 unit
tests. **Differential:** `recall_tags_equivalence.rs` /
`QT_ORACLE_RECALL_TAGS` regenerated at `8bf3cb5f` with the oracle case
EXTENDED (retro flip arms, window in/out/edge, suspend, unparsable-date
arms).

**Unit 3 — `search_memories_semantic` learns time + entities.**
`services/memory_service.rs`: `occurred_within` (two-stage: hard filter
first; if survivors < limit, fall back to the unfiltered pool and hand
`occurredWithin` to the multiplier loop for the soft ×1.3 — never fewer
results than today), `entity_anchors` (cap 3, trim, the literal-boost /
`searchByContent` union path on the injector path), `probes`
(retrospective-only extra embeddings, cap 2 extras, per-memory max
cosine, then blend as today). Read v4 `memory-service.ts:837–1015` in
full before writing — the union/dedup order and where the window filter
sits relative to the multiplier loop are the subtle invariants; carry
the why-comments. **Differential:** primarily proven end-to-end by Unit
7's replay differential (which tables every multiplier); additionally
extend whatever direct search coverage exists in the memories families
if a seam fits naturally — do not force a new standalone family if the
replay + tools coverage pins every branch.

**Unit 4 — vault conversation-summary dates.**
`services/memory_recap/mod.rs`: the `timeRange` option (`readCap =
max(limit*3, 15)`), the frontmatter `firstMessageAt`/`lastMessageAt`
reads (`tsField`), `overlapsWindow` (span-end falls back to start), the
two-stage filter → ×1.3 `CONVERSATION_WINDOW_BOOST`, the dated fields on
the match struct, and the ` (YYYY-MM-DD)` suffix in the rendered
conversation line (`READ_CONVERSATION_CALL_NOTE` render — v5's
counterpart of the `#### {title}…` renderer). **Differential:** the
chats-search family (`chats_search_equivalence.rs` / `QT_ORACLE_CHSEARCH`)
and/or `vault_summary_mirror_tier2_equivalence.rs` — extend with
timeRange cases over dated summaries and regenerate at `8bf3cb5f`.
(Round 2 has no production caller passing `timeRange` — its consumers
are round 3's mini-recap and the D conversation filter is span-based —
that is fine: port + prove the function; the wire arrives with round 3.
Say so loudly in the unit record.)

**Unit 5 — buildContext part-1 threading + the RETRO constants.**
`memory_injector.rs`: `RETRO_HEAD_TOKEN_BUDGET = 600` /
`RETRO_HEAD_SIZE = 10` beside the DYNAMIC constants (carry the tuning
why-comment). `build_context.rs`: thread the distill signals
(`turnRecallSignals`) out of the inline distill call; the fallback
search call gains `entity_anchors` (unconditional — v4 passes
`turnRecallSignals?.entities` on EVERY turn), `occurred_within` gated on
`fallbackRetro`, the entity/paraphrase multi-probe list (retro only),
and the retro-sized `limit`; the head-format call gains the
`isRetrospectiveTurn` budget/entries ternaries. **STOP at part 1** — no
mini-recap, no whisper, no recall-history writes (§boundary).
**Differential:** `build_context_tier3_equivalence.rs` /
`QT_ORACLE_BUILD_CONTEXT` regenerated at `8bf3cb5f`. Round 1 already
rebased this family (the dated head bleeds into the render), so the
committed oracle is at `8bf3cb5f` with non-retro canned responses —
regenerate to pick up the new distill prompt bytes if the tier pins
them (round 1's green regen suggests the canned routing tolerates the
prompt change — CONFIRM which it is and record it), and ADD at least
one retro-arm case: a canned distill response with
`retrospective: true` + `timeRange` + `entities`, fixtures with dated
episodic memories and NO matching vault summaries (the part-2 inertness
guard).

**Unit 6 — the deep-dive tools (D).**
`tools/search.rs`: schema `since`/`until`/`aboutCharacter` (byte-exact
descriptions), the date-only → full-day ISO normalization, the
`aboutCharacter` name/alias resolution over `characters.findAll()`
(case-insensitive trim; unresolved → memory hits EMPTY, not
filter-ignored), `occurred_within` hard-window into the search call
(`from`/`to` sentinels `0000-01-01T00:00:00.000Z` /
`9999-12-31T23:59:59.999Z`), result fields `occurredAt` /
`narrativeTime` / `kind` / `conversationId` (= the memory's chatId),
the conversation span filter (per-chat `createdAt..updatedAt` overlap
with the `spanByChat` cache + the unparsable-span drop), and any
rendered-output changes in `format_search_scriptorium_results` (diff
v4's formatter — the +119 handler hunk has a tail you must read whole).
`tools/read_conversation.rs`: the two range params + updated tool
description; the slicing (split regex, preamble handling, the two error
strings byte-exact, the `_Showing interchanges X–Y of Z._` footer,
counts from the sliced markdown). **The catalog deletion:**
`definitions/data.rs` drops the `memorySearch` entry; the invariant test
57; audit that nothing in v5 routes the key (if something does, mirror
v4's disposition — v4 deleted the tool AND its test outright).
`native_tool_prompt.rs` rule 6 + the text-tool-loop prompt bullet —
byte-exact. **Differentials:** `search_tools_equivalence.rs`
(`QT_ORACLE_SEARCH` + `QT_ORACLE_SEARCH_RW`) — extend the oracle cases
with since/until (date-only + full-ISO), aboutCharacter
(resolved/alias/unresolved), slicing (mid-range, open start, open end,
out-of-range error, end<start error) over the new episodic fixture rows;
regen at `8bf3cb5f`. `tool_definitions_equivalence.rs`
(`QT_ORACLE_TOOL_DEFINITIONS` + `…_CANONICAL`, generator
`harness/oracle/tools/gen-tool-catalog.mjs`) — regen at `8bf3cb5f`
(picks up the two changed definitions + the deletion). **Audit which
other committed corpora embed the changed prompt bytes** (the
request-envelope family pins system prompts; the tool-wire/tool-build
families pin instructions) — regen every family that goes red on the
new rule-6/bullet bytes and list them in the unit record.

**Unit 7 — the replay module + verb (the round's centerpiece).**
`crates/quilltap-core/src/memory/recall_replay.rs` (or the module home
matching v5's layout): `RecallReplayRow`/`RecallReplayResult`/input
struct mirroring v4 name-for-name (wire keys camelCase per the existing
DTO idiom); the run: chat load + participant resolution (first
LLM-controlled non-removed with a characterId), turn partition
(`partition_messages_into_turns`), the replayed turn's clock, the
distill call, then the search run TWICE — old path (episodic signals
inert, head `DYNAMIC_HEAD_DEFAULT_SIZE`) vs new path (retro flip +
window + entity anchors + probes, head `RETRO_HEAD_SIZE` when
retrospective — READ v4's `toRows` calls for the exact head-size
selection); read-only guarantee (recall-history read, never written;
the `lastAccessedAt` bump tolerated — normalize it in the diff). The
dispatch verb `recall-replay` with v4's body validation + error strings
+ the cheap-LLM anchor-profile resolution mirrored onto v5's
cheap-LLM/selection machinery; registered wherever the existing chat
POST actions dispatch (engine arm + web edge). **Differential (NEW
family):** tier-3 mocked-LLM — a jest/tsx oracle driving v4's REAL
`runRecallReplay` (mock ONLY the cheap-LLM with canned distill
responses keyed by case; real repos over the fixture) vs the v5 verb
with the same canned responses, diffing the full result JSON (both
tables: cosine, rawWeight, blendedBefore, multiplier, fired[],
blendedAfter, selected — floats at 1e-12, timestamps normalized). Env
`QT_ORACLE_RECALL_REPLAY`. Cases: a retrospective turn with window hits
(the flip + window + suspend all fire), a non-retrospective turn
(oldPath == newPath modulo nothing — the §3 acceptance regression), an
entity-anchor rescue (verbatim place name below the cosine floor), a
window-starved fallback (soft boost arm), turnIndex/characterId/limit
arms + the error arms. This differential is what proves Units 1/2/3/5
end-to-end — build it against the fixture below.

**Unit 8 — the CLI.**
`crates/quilltap-cli/src/recall_replay_cmd.rs` + `SUBCOMMANDS` +
completions (all three templates) + help: flags
`--turn/--char/--limit/--port/--json`, the help text and the ANSI
old/new table rendering byte-faithful to
`packages/quilltap/lib/recall-replay-command.js` (hand-rolled ANSI, not
console.table — but the p4.3 stdout-diff recipe applies). HTTP mode
wraps the same POST the v4 CLI hits; direct-core mode follows the
established dual-mode pattern. **Differential:** Tier R — v4
`npx quilltap recall-replay` vs the v5 binary over the same running
fixture server (or the established CLI-oracle harness form), stdout
diffed, `--json` and table modes + the help/error arms.

### Tier 2 — should land

9. **The chat-PUT `timelineMode` accept arm** (the verified v5 gap): v4
   `updateChatSchema` takes `z.enum(['realtime','narrative']).nullish()`;
   v5's data layer is ready (`db/chats.rs` patch support) but no API arm
   parses it. Add it Zod-faithfully (explicit-null semantics per the
   `p4.6an-dangerous-content-parse-gap` memory) and extend the chats
   routes differential with set/null/invalid-enum cases (regen that
   family at `8bf3cb5f` — round 1 already rebased it; this is an
   extension).
10. **The `episodic-visibility` port:** v4's
    `lib/memory/__tests__/episodic-visibility.test.ts` (96 lines) —
    where its assertions map onto ported v5 surfaces (search result
    fields, injector line), port case-for-case as unit tests.
11. **Startup route listing** (+1 in v4 `prettify.ts`): if v5's web boot
    banner enumerates routes, add the line; else record n/a.

### Tier 3 — explicit loud deferrals (round 3; enumerate, do not stub)

Unchanged from the campaign plan — round 3 owns: the clocked
creation-extraction prompt + EVENT category + `kind`/`when`/`entities`
output + soft cap 2→3; `resolveWhenPhrase`/`resolveCandidateAnchors` +
the turn-path `occurredAt` stamp (+ `turn-transcript.ts` anchors); the
first-write `applyEpisodicFallbackAnchors` capture (the gate family
`QT_ORACLE_GATE` stays SKIP until then); the fold-episode pass +
context-summary wiring; the fourth-cadence PART 2 (mini-recap +
`retrospective-recall` whisper + spam guard + recall-history retro
signatures + the commonplace writer param); the fold Timeline section;
the compression keep/drop flip; the gate date-guard + housekeeping
merge guard; the Salon "Story's Clock" SPA + any timelineMode SPA
surface. No `not_available` refusals are needed where behavior degrades
to today's (the feature's own design); the CLI/API surfaces this round
ships must be COMPLETE, not stubbed.

## The differential requirement (restated)

Every ported unit ships an equivalence test driving v4's REAL `lib/`
code (tsx/jest oracle → NDJSON → Rust diff): tier 1 exact for the pure
leaves (recall-tags, the distill parse), tier 2 structural for
DB-touching ops, tier 3 mocked-LLM for the distill-dependent paths
(build-context, the replay). Regenerate every touched family FRESH at
`8bf3cb5f`, run BY NAME with `--nocapture`, zero SKIP, and grep the
NDJSON before trusting a green. The recipes are enumerated in
§Survey — reuse, don't invent.

## Fixtures

**This lane BUILDS AND OWNS a new committed fixture family** —
`episodic-recall-{main,mount}.db` (name it per the harness convention;
builder under `harness/oracle/fixtures/`, driving v4's REAL repos):

- a TF-IDF embedding profile (deterministic embeddings both sides — the
  established memorySearch precedent);
- a memory corpus mixing `kind: 'episodic'` rows (varied `occurredAt`
  in/out of the test windows, `narrativeTime`, `entities`, keywords,
  embeddings) and plain semantic rows (null `occurredAt` — the inert
  arm), including a verbatim-place-name row rescued only by entity
  anchoring;
- a chat with ≥4 partitionable turns (a `controlledBy:'user'`
  participant + an LLM character — the replay's participant resolution),
  recall-history state for the suspend arm, and connection
  profiles/chat-settings sufficient for the route's cheap-LLM
  resolution;
- vault conversation summaries WITH dated frontmatter (for Unit 4's
  cases) but engineered NOT to match the retro-arm probes used by
  Unit 5/7 fixtures (the part-2 inertness guard) — if that tension is
  awkward in one fixture, split the conv-summary cases onto their own
  small fixture and say so.

Existing fixtures this lane extends: the search-tools oracle fixture
rows (episodic fields for Unit 6), the chats-routes fixture (the
timelineMode PUT case). When a fixture changes, THIS lane regenerates
every dependent oracle and re-runs the dependent differentials —
nothing is left for the unifier.

**Oracle disposition table (confirm empirically per P4.d12's rule):**

| Family | Env | Round-2 action |
|---|---|---|
| memory-tasks tier-1 | `QT_ORACLE_MEMORY_TASKS` | SPLIT: search-extraction cases regen `8bf3cb5f`; creation cases stay `7e6d13e5` |
| recall-tags | `QT_ORACLE_RECALL_TAGS` | regen + extend at `8bf3cb5f` |
| build-context | `QT_ORACLE_BUILD_CONTEXT` | re-regen at `8bf3cb5f` + retro arm |
| search-tools | `QT_ORACLE_SEARCH`, `QT_ORACLE_SEARCH_RW` | regen + extend at `8bf3cb5f` |
| tool-definitions | `QT_ORACLE_TOOL_DEFINITIONS`, `…_CANONICAL` | regen at `8bf3cb5f` (57 tools) |
| chats-search / vault-summary | `QT_ORACLE_CHSEARCH`, (mirror tier-2) | regen + extend as touched |
| chats routes (timelineMode PUT) | (its existing env) | extend + regen (already `8bf3cb5f`-based) |
| recall-replay (NEW) | `QT_ORACLE_RECALL_REPLAY` | new family |
| request-envelopes / tool-wire / tool-build | (their envs) | regen ONLY those that go red on the prompt-byte changes; list them |
| gate / processor / memory-tasks-creation / context-summary / carina / recall-history / fold | various | **LEAVE at `7e6d13e5`** (round 3) |

## Shared contract (binding — none)

Single lane; no cross-lane contract. **Version-bump ownership:** this
lane bumps `quilltap-core`, `quilltap-harness`, `quilltap-cli`, and
`quilltap-web` (if touched) per the per-commit rules; no SPA bump
(apps/web untouched).

## Ownership

This lane owns: `crates/quilltap-core/src/{recall_tags.rs,
memory_injector.rs (RETRO consts), context wiring in
services/build_context.rs, services/memory_service.rs,
services/memory_recap/*, tools/{search.rs, read_conversation.rs,
native_tool_prompt.rs, definitions/*}, the new recall_replay module,
api/{types.rs additions, engine.rs arm, the chat-action handler}}`,
`crates/quilltap-cli/*`, `crates/quilltap-web` (edge only if needed),
`crates/quilltap-harness` (its tests + oracle cases/fixtures),
`harness/oracle/*` for the touched families, `docs/v4/developer/features/
episodic-recall-overhaul.md` (mirrored at planning — keep in sync if v4
edits it). `CHANGELOG` + `status-log.md` are append-only.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; `cargo clippy` BOTH feature sets
   `-D warnings`; release build.
2. Fresh oracle regen for every family in the disposition table's regen
   set, from the clean v4 checkout at `8bf3cb5f` (drift-check first);
   grep each NDJSON for real content.
3. `cargo test --workspace` with the full differential env set; the
   round's differentials re-run BY NAME `--nocapture`, zero SKIP.
   Recount ok-lines on any failure (fail-fast is per target).
4. `ng build`/`ng test` only to prove apps/web is UNTOUCHED (counts
   identical to main's baseline).
5. Full Playwright against a fresh dist + freshly rebuilt release
   binaries (the tool-prompt bytes ride live system prompts — prove no
   beat regressed). Known flake: `wardrobe-flow` in-chat set_all
   (documented; isolation re-run proves non-regression).
6. Every commit per `.claude/commands/commit.md` (CHANGELOG every
   commit; version bumps on code commits).
7. The lane record in `status-log.md`: per-unit records, the oracle
   vintage moves, the confirmed/adjusted boundary findings, the round-3
   carry-forwards restated.
