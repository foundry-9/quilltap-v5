# Work order P4.9H2A — embedding-profiles management, the server half: the verb/REST family, the PUT trigger matrix, the `EMBEDDING_REAPPLY_PROFILE` handler, memory-dedup, and conversation-summaries regeneration

> **STATUS: CLOSED — UNIFIED on main (2026-08-06, the fallback + wire +
> embedding-profiles round). Tier 1 complete; tier 2 (units 6+7) DEFERRED
> LOUDLY as named refusals.** Landed: the repo ops (`find_by_name`,
> `unset_all_defaults`, the `Option<Option<T>>` nullable clears — tier-2
> family extended, mutation-proven twice), the eleven profile verbs +
> handler + **the PUT trigger matrix** (routes differential 34 cases over
> the new committed `embedding-profiles-{main,mount}.db` family, matrix
> claims proven as `background_jobs` + `embedding_status` STATE,
> mutation-proven), the REST edges, and the `EMBEDDING_REAPPLY_PROFILE`
> handler (byte-identical table diffs on the four main tables; ⚠ the
> MOUNT leg is sandbox-blind on BOTH sides — the production mount reapply
> + encrypted VACUUM-INTO backup is an OWED real-instance proof, flagged
> per the CLAUDE.md crypto rule).
>
> **The §3 unification review fixed, on the unify branch (`a5b0f803`):**
> (1) BLOCKING — the PUT echo dropped `dimensions`/`truncateToDimensions`
> on an explicit-null clear where v4 keeps the key present-as-null; fixed
> + pinned by the new `update_clear_truncate_dims_null` corpus case,
> mutation-proven. (2) Every 500 arm answered the leaked underlying error
> where v4 answers per-route FIXED sentences (the P4.D50 class) — all 25
> arms fixed, real error to tracing. (3) The REST edges' body parsing now
> matches v4 per action (create/PUT throw→500 fixed sentence on
> empty/malformed; reindex guards its parse; refit/reapply never read the
> body). (4) The reapply service regained v4's in-service no-truncation
> throw and persists the RAW backup-failure error. **Recorded, not
> changed (documented divergences):** the degenerate-input arms (PUT
> non-string `apiKeyId` ignored vs v4's 404; non-object PUT body 400 vs
> v4's accidental no-op/500; create `isDefault` coercion vs v4's Zod 500;
> `find_by_name` DB-error 500 vs v4's `safeQuery`-to-null), the
> malformed-payload job-error string, the PUT-echo key order for
> previously-NULL keys (invisible — the differential sorts keys), and the
> provider-list order (manifest order + BUILTIN last; documented in the
> module doc).
>
> **STILL OPEN (the follow-up lane's scope):** units 6+7 — memory-dedup
> (preview/run) and conversation-summaries regeneration + its handler.
> The four §1 verbs refuse loudly by name (`api/memory_maintenance.rs`);
> the SPA cards + gated beats exist (flip `P49H2A_MAINTENANCE_LANDED` in
> `settings-memory-flow.spec.ts` when they land); the deps and the
> fixture's crafted near-duplicate memories are already in place.
> `embeddingProfileFetchModels` stays refusal-armed (tier 3, live
> provider call). Lane record: `status-log.md` → "Lane record —
> P4.9H2A".

This is the top standing pool item (`p4.9h` in phase-4.md's candidate
lists: "embedding-profiles management + the banked PUT trigger
matrix"), split server ∥ SPA with **P4.9H2B** as the sibling SPA lane.
The PUT trigger matrix was banked whole by lane P4.d27 —
`status-log.md` → "Tier-3 bank — the PUT
`/api/v1/embedding-profiles/[id]` trigger matrix → `p4.9h`"
(status-log.md:44020-44060) — and its hard dependency
`EMBEDDING_REAPPLY_PROFILE` (~380 LOC v4 lib) is this lane's to port.
Port target: v4 AT the round baseline (which is at/after `13f0ebd7`,
the D33 bank's floor).

**v4 baseline for this round: `3adefeba`** (v4 HEAD, tree clean at
planning, 2026-08-06 — verified). **Drift-check at lane start**:
`git log 3adefeba..HEAD --oneline` + `git status --short` in
`~/source/quilltap-server`. If v4 moved: classify in your lane record;
STOP if a commit lands on `app/api/v1/embedding-profiles/**`,
`lib/embedding/**`, `lib/tools/memory-dedup.ts`,
`app/api/v1/system/conversation-summaries/**`, or
`app/api/v1/system/tools/route.ts` — otherwise pin a detached worktree
at `3adefeba` for every oracle regen
(`oracle-regen-pinned-v4-worktree`) and proceed.

**Siblings (same round):** P4.9H2B (the SPA half — the §1 contract
below is BINDING between us), P4.42 (the web-search wire — **shares
`quilltap-host/src/host.rs` and possibly `spine.rs` in DISJOINT named
blocks**, see Ownership), P4.41 (collision-free).

**Worktree note:** own branch, own checkout; no `git stash`/
`git worktree` games with sibling lanes.

## Mandate

Port v4's embedding-profiles management surface faithfully: the CRUD
route family with its enrichment (api-key, tags, BUILTIN
`vocabularyStats`, `embeddingStats`), the **PUT trigger matrix**
(full-reindex vs Matryoshka re-apply vs BUILTIN refit — the doctrine
comment carried verbatim), the three `?action=` verbs
(refit/reindex/reapply) with v4's exact guard sentences, the
`EMBEDDING_REAPPLY_PROFILE` job handler (the 435-line
`lib/embedding/reapply-profile.ts` with every subtle invariant), the
memory-dedup preview/run pair (a synchronous inline algorithm — **no
LLM, no jobs**), and the conversation-summaries regeneration pair
(status + enqueue + the `REGENERATE_CONVERSATION_SUMMARIES` handler,
which **re-mirrors existing `contextSummary` values into vaults — it
does NOT regenerate summaries and enqueues no other job**). Every
ported unit ships with its differential (below).

Do NOT touch: `apps/web/**` (P4.9H2B's), the P4.42 Ownership hunks in
`engine.rs`/`host.rs`/`spine.rs`/`executor.rs`/`orchestrator.rs`/
`providers.rs`/`web_search.rs`/`api_keys.rs`,
`model/streaming_provider.rs` (P4.41's), `api/types.rs` and
`api/engine.rs` beyond the §1 arms named below.

## Survey-verified starting points (2026-08-06, fresh at `3adefeba` / main)

### v4 — the route family (NO Zod anywhere; hand-rolled checks, user-visible sentences)

- **Collection** `app/api/v1/embedding-profiles/route.ts` (367 LOC):
  GET list :30/:52-110 (enrichment: `enrichWithApiKey` :58,
  `enrichWithTags` :59, BUILTIN `tfidfVocabularies.findByProfileId` →
  `vocabularyStats` :63-73, `embeddingStatus.getStatsByProfileId` →
  `embeddingStats` attached only when `total > 0`, swallow-all catch
  :77-84; sort default-first then createdAt desc :97-102; response
  `{profiles, count}` :104). `?action=list-providers` :118-126
  (`getEmbeddingProviders()`). `?action=list-models` :198-254 (static
  catalog + a NON-FATAL `providerModels.upsertModelsForProvider`
  cache side-effect :211/:232-242; ⚠ two different return shapes —
  `{provider, models}` with a provider vs a bare
  `Record<provider, models[]>` without). `?action=fetch-models`
  :132-193 (LIVE provider call — tier 3 here). POST create :259-367:
  validation sentences at :277/:281/:285/:289/:297/:301, 404
  `notFound('API key')` :308, ⚠ **hand-rolled 409**
  `{error: 'An embedding profile with this name already exists'}`
  :315-318 (NOT the `conflict()` helper), `unsetAllDefaults` before a
  default create :323, `normalizeL2 ?? true` + `tags: []` :327-339,
  **created-as-default immediately enqueues
  `enqueueEmbeddingReindexAll(user.id, {profileId})`** non-fatal
  :349-350, returns 201 `created({...profile, apiKey})`.
- **Item** `app/api/v1/embedding-profiles/[id]/route.ts` (438 LOC):
  GET :26-48 (`enrichProfile`, `lib/api/middleware/enrichment.ts:180`).
  PUT :53-262 (sentences at :82/:97/:120/:129/:143/:151/:157; 409 via
  `conflict()` :89 — ⚠ the OTHER 409 shape; 404 `notFound('API key')`
  :108). DELETE :267-289 (`messageResponse('Embedding profile deleted
  successfully')`; the `tfidf_vocabularies` FK cascades in DDL).
  POST `?action=refit` :297-327 (BUILTIN gate sentence :307,
  `enqueueEmbeddingRefit({profileId, triggerReindex: true})`,
  `{message: 'Vocabulary refit job enqueued', jobId}`).
  POST `?action=reindex` :329-401 — TWO guard comments to carry
  verbatim: the legacy no-body parse guard (:338-355, default
  `'all'`) and the full-scope-invalidates-up-front comment (:373-376);
  scope-400 sentence :348, mismatched-dim-nothing-to-compare sentence
  :362; `invalidateAllEmbeddings` only when `scope === 'all'` :379;
  response `{message, jobId, scope, invalidatedCount}` with two
  distinct message strings :390-393.
  POST `?action=reapply` :403-436 (no-truncation 400 sentence :412;
  response message names the pre-write DB backup; **no isDefault gate
  — API-reachable from a non-default profile; only the SPA gates**).
- ⚠ **No `userId` scoping on `findById` in the [id] routes** despite
  the "Verify ownership" comments (:57/:271) — carry deliberately.
- **The PUT trigger matrix** — [id]/route.ts:181-250, evaluated AFTER
  the update commits (:170). Carry the doctrine comment :181-186
  verbatim ("There is one embedding standard per instance: the default
  profile's output…"). Condition flags :187-194
  (`providerChanged`/`modelChanged`/`dimensionsChanged`/
  `becameDefault`/`truncateChanged`); `needsFullReindex =
  updatedProfile.isDefault && (provider||model||dimensions||becameDefault)`.
  Branch A (full reindex) :200-226: invalidate, then **BUILTIN →
  `enqueueEmbeddingRefit({triggerReindex: true})`** :215-217 else
  `enqueueEmbeddingReindexAll` (no scope) :223. Branch B
  (truncation-only, `truncateChanged && isDefault`) :227-249: the
  NARROW/WIDEN test :231-234 (`oldEffective = existing.truncateToDimensions
  ?? existing.dimensions ?? null`; `newTarget <= oldEffective` both
  non-null → **`enqueueEmbeddingReapplyProfile`** :240; else — incl.
  clearing to null — invalidate + full reindex :247-248). ⚠ Nothing
  fires on `normalizeL2`-alone or on any non-default profile.
  Response carries `reembeddingTriggered` :255.
- **`lib/embedding/reapply-profile.ts`** (435 LOC; the handler
  `handlers/embedding-reapply-profile.ts:20` is 53 LOC — resolves the
  profile, throws `Embedding profile not found: {id}` on miss :31,
  returns silently when `!truncateToDimensions` :34-39). The port
  must not lose (survey-verified list):
  1. Dimensions from the DECODED vector, never byte length (:191-193
     — legacy raw Float32 vs self-describing quantized);
     `blobToFloat32`/`float32ToBlob`/`EMBEDDING_DIM_SQL`
     (`lib/embedding/float32-conversion.ts`; the DIM_SQL keeps the
     MAX scan un-hydrated :240/:254).
  2. Tables: main `['memories','vector_entries','conversation_chunks',
     'help_docs']` (:46, each behind `tableExists` :346) + mount-index
     `doc_mount_chunks` (:366). Same two statements per table
     (:178/:181). Keep the allowlist shape.
  3. **`VACUUM INTO` backup before any write** (:140-157), name
     `<base>.bak-pre-truncation-<YYYY-MM-DD>.db` + `-<millis>` on
     same-day collision :146-152; backup failure is FATAL both
     partitions (:336-341/:370-375). Empty corpus (`observedMax===0`)
     → clean zero-result, `backupPath: null`, **no backup** :298-312.
  4. **Refuses to grow** (:314-319) — the exact two-sentence error.
  5. No-truncation direct-call throw :289-293; `normalize =
     normalizeL2 !== false` :295; reads/computes OUTSIDE the txn,
     UPDATEs inside, `FLUSH_BATCH = 500` (:43/:162-184);
     degenerate vectors (mag < `ZERO_MAG = 1e-10`) skipped, counted,
     not written :206-209; post-write cache invalidation + `VACUUM`
     both non-fatal (:353-359/:381-387/:398-412 — v5's analogues of
     `invalidateMountChunkCacheAll`/`unloadAll` are documented no-ops,
     per the P4.d27 bank).
  6. ⚠ The sibling startup migration
     `migrations/scripts/apply-embedding-profile-truncation.ts` is
     DELIBERATELY narrower (integer-multiple-only) than the runtime
     job (slices anything longer) — do not unify them by accident;
     the migration is NOT this lane's (v5's migration runner is
     deferred).
- **Memory dedup** — no dedicated route; rides
  `app/api/v1/system/tools/route.ts` (`memory-dedup-preview` in
  GET actions :72, `memory-dedup` in POST actions :85; handlers
  :1168-1200/:1202-1235; threshold default 0.80, 400 sentence
  `'Invalid threshold. Must be a number between 0.5 and 1.0'` in BOTH;
  responses `{success: true, result}`). Service
  `lib/tools/memory-dedup.ts` (446 LOC):
  `deduplicateCharacterMemories` :152, `deduplicateAllMemories` :366.
  **Fully synchronous inline, no LLM, no jobs.** Algorithm: group
  embedded memories BY EMBEDDING LENGTH (:203-210 — never compare
  mismatched dims), pairwise `cosineSimilarity` union at `>= threshold`
  (:226-227), Union-Find with path compression + union-by-rank
  (:70-101), survivor by `scoreMemory` (:113-133 — importance×100 +
  len×0.1 + specificity: capitals ×2 cap 10, numbers ×3 cap 5,
  code-ish ×4 cap 5), `extractNovelDetails` (**heuristic, not LLM** —
  `lib/memory/memory-gate.ts:630`, proper nouns/dates/numbers with
  STOP_WORDS) appended as `[+] ` footnotes (:263-278), writes only
  when `!dryRun` (:306): `updateForCharacter` per survivor +
  **`deleteMemoriesWithUnlinkBatch`** (the chokepoint — :316-319) +
  per-character vector-store removal, non-fatal (:329-340).
  `deduplicateAllMemories` iterates all characters, try/catch per
  character pushing a ZERO-FILLED entry on failure (:406-419).
- **Conversation-summaries regen** —
  `app/api/v1/system/conversation-summaries/route.ts` (74 LOC): GET
  `?action=regenerate` → counts PENDING+PROCESSING jobs of the type
  (:62-72), `{success, inFlight}`; POST → `enqueueRegenerateConversationSummaries`
  (queue-service.ts:742 — dedupes on userId over PENDING+PROCESSING
  :749-756, **`maxAttempts: 1`** :767 with the "idempotent but a
  retry would re-walk every chat" doc, empty payload `{}`), response
  `{success, jobId, message}` forking on `isNew` :49-51; other/missing
  action → `badRequest('Unknown or missing action.')`. Handler
  `handlers/regenerate-conversation-summaries.ts:27`: chats with
  non-empty `contextSummary` → per chat dedupe participant ids,
  `getMessages` + `computeConversationStats` +
  `writeConversationSummaryToVaults` (idempotent by frontmatter-UUID
  replace), best-effort per chat (:61-67). v5 analogues already live:
  the vault mirror ran differential-verified in the P4.6bj
  `memory_pipeline_jobs` family; the vault-summary machinery is the
  `QT_ORACLE_VAULT_CONV` family (P4.d13). The v4 host-RPC
  forked-child shape is a locked v5 non-port
  ([[job-runner-fork-ipc-non-port]]) — v5 writes directly on the
  writer task like every other fold write.
- **Enqueue helpers** (queue-service.ts): `enqueueEmbeddingRefit`
  :939, `enqueueEmbeddingReindexAll` :954,
  `enqueueEmbeddingReapplyProfile` :971 — all default `priority: -1`;
  payload docs :142-173 (the scope doc-comment :155-167 is worth
  porting verbatim).
- **Schema:** `embedding_profiles` DDL
  (`sqlite-initial-schema.ts:385-402`) + the two truncation columns
  from `add-embedding-profile-truncation-fields-v1`
  (`truncateToDimensions INTEGER DEFAULT NULL`,
  `normalizeL2 INTEGER DEFAULT 1`). **No unique constraint on
  `isDefault`** — app-enforced via `unsetAllDefaults`. No
  `?action=set-default` route exists; default flips only through
  create/PUT. `tfidf_vocabularies` FK → `embedding_profiles` ON
  DELETE CASCADE; `embedding_status.profileId` is a SOFT link (no FK).

### v5 — what exists and what's missing

- **Repo** `crates/quilltap-core/src/db/embedding_profiles.rs`
  (differential-verified): `find_by_id` :108, `find_default` :122,
  `EpCreate` :175 / `CreateOptions` :197 / `EpUpdate` :209,
  repository create/update/delete :238/:270/:340,
  `find_full_json_by_id` :418, `find_all_full_json` :436. **Gaps the
  module doc names (:9-12, :47-51): `findByName` and
  `unsetAllDefaults` are UNPORTED, and `EpUpdate` cannot set a
  nullable column to NULL** — both must land here (the tri-state
  `??` fields in §1 depend on the nullable setters).
- `db::embedding_status::mark_all_pending_by_profile_id` :373 (=
  v4 `invalidateAllEmbeddings`'s core);
  `queue_service::enqueue_embedding_reindex_all` :782 whose doc
  :778-781 NAMES this lane as the future `Some(scope)` caller.
  Scope semantics already implemented in
  `services/embedding_reindex_job.rs:113-135`.
- `EMBEDDING_REAPPLY_PROFILE` is already in
  `job_runner::KNOWN_JOB_TYPES` (:146) and `api/system_data.rs`
  JOB_TYPES (:619) — the loud refusal
  ("recognized but its handler is not yet available", job_runner.rs
  :186-191) retires when the handler registers.
  `REGENERATE_CONVERSATION_SUMMARIES` likewise (job_runner.rs:160 /
  system_data.rs:633; the SPA queue-badge label already knows the
  string, `queue-status.logic.ts:63`).
- **Registration site: the SEAM-FREE registry,
  `quilltap-host/src/host.rs:468-510`** (precedents: `EMBEDDING_REFIT`
  :480, `EMBEDDING_REINDEX_ALL` :497, `CONVERSATION_RENDER` :506).
  Both new handlers need only the DB — no LLM wire — so they belong
  here, NOT in `spine.rs`'s `ProductionSpineFactory` vec (:2960-3020).
  Staying out of spine.rs avoids the P4.42 adjacency entirely.
- **No `EmbeddingProfile*` verb exists** (`api/types.rs` grep: zero).
  The structural precedent is the `ImageProfile*` block
  (api/types.rs:801-850, `Response::ImageProfile` :2955, handler
  module `api/image_profiles.rs` with `not_available` :43).
- **No new axum route is strictly needed** for the SPA (everything
  rides `POST /api/dispatch`, `quilltap-web/src/lib.rs:210`), but the
  REST edges ARE deliverables here for v4 parity + the routes
  differential (see tier 1). Stale doc riders to fix in passing:
  `api/types.rs:1276-1277`/:1284-1285 still call
  `MemoryGenerateEmbeddings`/`MemoryRebuildIndex` "recognized-but-
  refused" (they went live in P4.6BL, engine.rs:2860-2884);
  `characters_routes.rs:608` hardcodes `"embeddingProfiles": 0` in an
  export count.
- **Closest recipes:** `crates/quilltap-harness/tests/
  embedding_profiles_tier2_equivalence.rs` (header :1-27 carries the
  full regen incantation; extending its committed op-spec
  `harness/oracle/fixtures/embedding-profiles-tier2.json` + `Op` enum
  covers the new repo ops); `image_profiles_routes_equivalence` for
  the route family; `memory_pipeline_jobs` + `QT_ORACLE_VAULT_CONV`
  for the summaries handler; [[jest-real-db-oracle]] for the reapply
  and dedup oracles. Committed fixture naming precedent:
  `crates/quilltap-web/tests/fixtures/<family>-{main,mount}.db`.

## §1 Shared wire contract (BINDING — identical in P4.9H2A and P4.9H2B)

Request variants (`api/types.rs` name — wire `type` tag — fields,
camelCase on the wire):

| Variant | tag | fields |
| --- | --- | --- |
| `EmbeddingProfileList` | `embeddingProfileList` | — |
| `EmbeddingProfileGet` | `embeddingProfileGet` | `profileId` |
| `EmbeddingProfileCreate` | `embeddingProfileCreate` | `name`, `provider`, `apiKeyId?`, `baseUrl?`, `modelName`, `dimensions?`, `isDefault?` |
| `EmbeddingProfileUpdate` | `embeddingProfileUpdate` | `profileId`, then all optional: `name?`, `provider?`, `apiKeyId??`, `baseUrl??`, `modelName?`, `dimensions??`, `truncateToDimensions??`, `normalizeL2?`, `isDefault?` — `??` = tri-state (absent / explicit null / value) riding `double_option`, per the P4.D50 lesson |
| `EmbeddingProfileDelete` | `embeddingProfileDelete` | `profileId` |
| `EmbeddingProfileRefit` | `embeddingProfileRefit` | `profileId` |
| `EmbeddingProfileReindex` | `embeddingProfileReindex` | `profileId`, `scope?` (`'all'` \| `'mismatched-dim'`; absent = v4's legacy no-body call = `'all'`) |
| `EmbeddingProfileReapply` | `embeddingProfileReapply` | `profileId` |
| `EmbeddingProfileListProviders` | `embeddingProfileListProviders` | — |
| `EmbeddingProfileListModels` | `embeddingProfileListModels` | `provider?` |
| `EmbeddingProfileFetchModels` | `embeddingProfileFetchModels` | `provider`, `baseUrl?` — **REFUSAL-ARMED** (tier 3) |
| `MemoryDedupPreview` | `memoryDedupPreview` | `threshold?` (default 0.80) |
| `MemoryDedupRun` | `memoryDedupRun` | `threshold?` |
| `ConversationSummariesRegenerateStatus` | `conversationSummariesRegenerateStatus` | — |
| `ConversationSummariesRegenerate` | `conversationSummariesRegenerate` | — |

Response arms: `Response::EmbeddingProfile(serde_json::Value)` for the
eleven profile verbs; `Response::MemoryMaintenance(serde_json::Value)`
for the four dedup/summaries verbs. Bodies mirror v4's route JSON
name-for-name: list `{profiles, count}`; get = enriched profile
spread; create = created profile spread + `apiKey` (v4 201); update =
updated profile spread + `reembeddingTriggered`; delete `{message}`;
refit/reindex/reapply `{message, jobId, …}` with v4's exact message
strings (reindex adds `scope` + `invalidatedCount`; reapply adds
`targetDimensions`); dedup `{success, result}`; summaries POST
`{success, jobId, message}` / status `{success, inFlight}`.

Error semantics: v4's 400/404/409 sentences byte-for-byte — including
the two INCONSISTENT 409 shapes (hand-rolled
`{error: 'An embedding profile with this name already exists'}` on
create vs the `conflict()` helper on update); carry both faithfully.
The refusal arm answers the family `not_available` shape naming
`embeddingProfileFetchModels`.

## Tiered deliverables

**Tier 1 (must land):**

1. `db/embedding_profiles.rs`: `find_by_name`, `unset_all_defaults`,
   nullable-to-NULL setters on `EpUpdate`/`update` — with the
   extended `embedding_profiles_tier2_equivalence` ops proving each
   against v4's real repository.
2. The eleven profile verbs + dispatch arms + the new
   `api/embedding_profiles.rs` handler module (model on
   `api/image_profiles.rs`), incl. list enrichment, the create/PUT
   default flip via `unset_all_defaults`, the created-as-default
   reindex enqueue, and **the PUT trigger matrix** with the doctrine
   comment carried. `list-providers`/`list-models` land from v5's
   provider-manifest/static catalogs reproducing v4's output
   (including BUILTIN — ⚠ without disturbing the pinned Almanack
   BUILTIN-manifest omission, which is a DIFFERENT surface;
   `fetch-models` refuses, tier 3). Carry the `list-models`
   `provider_models` cache side-effect non-fatally.
3. The REST edges in `quilltap-web` matching v4 paths:
   `GET/POST /api/v1/embedding-profiles`,
   `GET/PUT/DELETE /api/v1/embedding-profiles/{id}` +
   `POST …/{id}?action=refit|reindex|reapply`. Unknown/missing
   actions answer v4's dispatcher sentences.
4. The `EMBEDDING_REAPPLY_PROFILE` handler
   (`services/embedding_reapply_profile.rs` + the thin job handler),
   preserving every invariant in the survey list (decoded-dim,
   VACUUM-INTO backups + fatal-on-backup-failure, refuse-to-grow,
   degenerate skip, batch txn shape, allowlisted tables, both
   partitions), registered in the **host.rs:468-510 seam-free block**.
5. New oracle-driven differentials (see the differential section) over
   the new committed `embedding-profiles-{main,mount}.db` fixture
   family: the routes family (CRUD + matrix — proving WHICH job types
   land in `background_jobs` and the `embedding_status` invalidation
   counts per matrix branch) and the reapply family (table-level
   vector diffs both partitions + result JSON, normalized for
   duration/backup-path).

**Tier 2 (should land):**

6. Memory-dedup: the ported service (`services/memory_dedup.rs` —
   union-find, dim-grouped cosine, `scoreMemory`,
   `extractNovelDetails` if not already ported with the memory gate;
   writes through v5's delete-with-unlink chokepoint), the two verbs,
   the `system/tools` REST edges for exactly these two actions
   (other v4 system-tools actions refuse loudly by name), and a
   dedup differential driving v4's REAL `deduplicateAllMemories`
   (deterministic — no LLM) over a fixture with embedded memories,
   diffing result JSON + the memories table + related-id scrubbing.
7. Conversation-summaries regen: the two verbs + the REST edge, the
   enqueue helper (dedupe-on-user, `maxAttempts: 1`), the handler
   over v5's existing vault-mirror machinery, registered in the same
   host.rs block; differential per the `memory_pipeline_jobs` /
   `QT_ORACLE_VAULT_CONV` recipes (diff the vault documents +
   `background_jobs`).

**Tier 3 (explicit refusal-arm deferrals — loud, named):**

- `embeddingProfileFetchModels` (live provider `getAvailableModels`
  call) — refuses via the family `not_available`, matching the
  standing `imageProfileValidateKey`/`ListModels` precedent.
- The BUILTIN refit's TF-IDF manifest question is NOT reopened here —
  the refit path already exists (`embedding_refit_job.rs`); this lane
  only enqueues it per the matrix.
- v4's startup migration `apply-embedding-profile-truncation.ts`
  (migration runner deferred repo-wide).

## The differential requirement, restated

Every ported unit ships with an equivalence test driving v4's REAL
code (jest/tsx oracle → NDJSON → Rust diff): the repo ops extend
`embedding_profiles_tier2_equivalence` (recipe in its header :1-27);
the route family gets a new tier-2 routes differential modeled on
`image_profiles_routes_equivalence`, diffing response JSON AND the
`background_jobs` + `embedding_status` state per matrix branch (the
matrix claim is state, not prose); reapply and dedup get
real-DB jest oracles ([[jest-real-db-oracle]]) over the new fixture;
summaries-regen follows the `memory_pipeline_jobs` shape. Regenerate
every oracle FRESH from the pin, grep the NDJSON for a fresh marker
([[oracle-regen-silent-stale-pass]]), run BY NAME with `--nocapture`,
zero SKIP — and **mutation-prove** any first-run-green family (the
D24 rule): at minimum, disable the matrix's refit split and the
NARROW/WIDEN test and watch the differential go red.

## Fixtures

This lane DELIVERS the committed
`crates/quilltap-web/tests/fixtures/embedding-profiles-{main,mount}.db`
family + its builder under `harness/oracle/fixtures/` (profiles incl.
a BUILTIN one with a fitted vocabulary, an OPENAI-shaped default,
truncation-bearing and truncation-free profiles, embedded memories
with TWO distinct dims for the dedup grouping, vectors in all four
main tables + `doc_mount_chunks`, and near-duplicate memory pairs
above/below the 0.80 threshold). It consumes the existing embedding
fixtures untouched. If any existing committed fixture must widen,
this lane owns regenerating every oracle family that reads it
(sweep with `recipe_sweep.py --v4 <pin>`).

## Ownership

This lane owns:

- `crates/quilltap-core/src/api/types.rs` — **ONLY the §1 variants +
  the two response arms** (+ the two stale doc-comment fixes named in
  the survey)
- `crates/quilltap-core/src/api/engine.rs` — **ONLY the §1 dispatch
  arms** (P4.42 owns the assembly-seam hunks; do not touch them)
- `crates/quilltap-core/src/api/mod.rs` (one module line), NEW
  `api/embedding_profiles.rs`, NEW `api/memory_maintenance.rs` (the
  four dedup/summaries verbs — keep out of `api/memories.rs`)
- `crates/quilltap-core/src/db/embedding_profiles.rs`
- NEW `services/embedding_reapply_profile.rs`, NEW
  `services/memory_dedup.rs`, NEW
  `services/conversation_summaries_regen.rs` (+ thin handlers),
  `services/queue_service.rs` (the new enqueue helpers + the
  `Some(scope)` caller its doc predicts)
- `crates/quilltap-host/src/host.rs` — **ONLY the :468-510 seam-free
  registry block** (two `registry.register` lines). P4.42 owns
  :515-560 + :638-776; do not touch those. Stay OUT of `spine.rs`
  entirely (both handlers are seam-free by design — if one turns out
  to need a seam, STOP and coordinate via the unifier rather than
  editing spine.rs).
- `crates/quilltap-web/src/` — the new REST edge module(s) + their
  router lines in `lib.rs`, `characters_routes.rs:608` (the hardcoded
  zero), the new routes-differential test + committed fixtures
- `crates/quilltap-harness` — the extended tier-2 family + the new
  families + `harness/oracle/cases`/`fixtures` files for them
- `CHANGELOG` + `status-log.md`: append-only (union-merge).

**Version bumps this lane owns:** quilltap-core (from 0.0.482),
quilltap-harness (from 0.0.408), quilltap-web (from 0.0.62),
quilltap-host (from 0.0.60), one bump each; the unifier recounts as
base + total bumps.

## Verification gate (before declaring done)

- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets -- -D warnings`, and again
  with `--features quilltap-core/native-transport`
- `cargo test --workspace --no-fail-fast` with the lane's full oracle
  env block — full pass, zero SKIP lines
- Every new/extended family BY NAME with `--nocapture` over oracles
  regenerated FRESH at the pin, zero SKIP; mutation proofs recorded
- Release build
- No `apps/web` touch → no ng/Playwright run owed by this lane (the
  SPA gate is P4.9H2B's); the §1 contract will be diffed
  name-for-name at unification
- Per-commit rule: follow `.claude/commands/commit.md`.
