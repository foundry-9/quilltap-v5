# Work order P4.69 — the SPA follow-ups: message-avatar danger styling, the invented quick-hide warns, the image-profile modal's structured writers, the parked/fragile beats

**Status:** OPEN — not started (ordered 2026-09-02, the follow-ups round:
P4.67 ∥ P4.68 ∥ P4.69 ∥ P4.70 ∥ P4.71). Written from `phase-4.md`
candidates 3/4/6 at the P4.D138 follow-up + the `6d2a50382` unification
(message-bubble danger styling never ported — the round-2 §C
measurement; the `console.warn` with no v4 counterpart; the modal's
structured writers; the temp bubble's seat; `waitForChatRefetch`'s
unscoped match; the workspace-search beat's positional pick; the six
unpinned slider suffixes; the component-transfer beat's precisely-scoped
un-park from P4.D130's lane record).

## Preamble

- **v4 oracle baseline: `6d2a50382`** (drift-ledger §1, probe re-run
  2026-09-02 at ordering: v4 checkout on `main`, tree clean, both logs
  empty, §3 EMPTY). Not a drift lane; the v4 client code is the oracle
  for every item (transcribed parity specs, the P4.D144 idiom).
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 checkout
  on `main`, tree clean, `git log 6d2a50382..main` empty and `git log
  3a76b17df..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: NO PIN NEEDED** (ledger §1 as of 2026-09-02) — this lane
  regenerates no Rust oracle; if a v4-client recorder is used (the P4.9L
  jsdom-recorder precedent), run it from the checkout only while the
  probe passes.
- **Sibling lanes (this round):** P4.67 (REST-edge query semantics —
  **§A: no well-formed call moves, so no SPA client changes**), P4.68
  (core consolidation), P4.70 (image fidelity + fixture), P4.71 (host
  gateway). §Shared contracts / §Ownership are binding and identical
  across all five orders.
- **Worktree:** per the `carryout` skill; `apps/web/node_modules` via
  `npm ci` (memory note `spa-worktree-node-modules`). `CHANGELOG` /
  `status-log.md` append-only. **This lane OWNS Playwright (port 4319)**
  — one suite at a time, repo-wide (memory note
  `e2e-port-4319-is-repo-wide`); every other lane's beats are gated.
- **SPA gates:** `npm test` / `npm run build` (never a bare `ng` — memory
  note `ng-test-defaults-to-watch-mode`; `--filter` matches TEST NAMES).
  The qt-class guard runs inside `npm run lint` and ahead of `npm test`;
  a `hover:` variant must be hand-written (memory note
  `qt-hover-variant-must-be-hand-written`).

## Mandate

Port v4's assistant-side message-avatar danger ring end to end (the
Salon passes `shouldShowDangerStyling(chat)` down the message list to
every assistant row's desktop avatar and to the streaming message —
Flagged only, never Uncensored), retire the two v5-invented
`console.warn`s where v4 is silent, give the image-profile modal
structured parameter writers so the LoRA editor cannot clobber a
mid-edit JSON textarea, pin the six slider suffix strings, and repair the
four fragile/parked beats (the temp bubble's seat, `waitForChatRefetch`,
the workspace-search positional pick, the component-transfer un-park) —
each with a spec or beat that would have caught it. Must NOT touch: any
Rust crate, `core-contract.ts` beyond the options-schema response shape
(Tier 2), the `quick-hide` presentation table or `shouldHideChat`, the
Concierge mark/pill (P4.D144's, live), the `check-qt-classes` guard.

## Survey-verified starting points (2026-09-02)

1. **v4's danger ring, the whole chain.** `app/salon/[id]/SalonView.tsx:1489`
   passes `isDangerousChat={shouldShowDangerStyling(chat)}` (the
   state-only predicate, `lib/services/dangerous-content/chat-override.ts:110`
   — `getConciergeState(chat) === 'flagged'`; an `'uncensored'` chat is
   deliberately NOT painted) into `VirtualizedMessageList.tsx` (`:106`
   prop, `:165` default false, `:368` → every `MessageRow`, `:384` →
   `StreamingMessage`). `MessageRow.tsx` (`:109/:176`) forwards it as
   `dangerous={isDangerousChat}` to `MessageDesktopAvatar` at BOTH
   assistant render sites — the courier row `:234` and the regular row
   `:280` — and its memo comparator re-renders on the flag (`:578`).
   `components/message-row/MessageDesktopAvatar.tsx:19-21`: `className =
   'flex-shrink-0 qt-chat-desktop-avatar' + (dangerous ? '
   qt-chat-avatar-dangerous' : '')` — "assistant side only; user rows pass
   neither". `StreamingMessage.tsx:85` applies the same two classes on its
   own avatar wrapper. The rule: `app/styles/qt-components/_chat.css:2819
   .qt-chat-avatar-dangerous .overflow-hidden {…}`. ⚠ `SalonView.tsx:1876`
   passes the RAW `chat?.isDangerousChat === true` to the sidebar (the
   participant-card tint) — that is a different consumer, already ported
   (`chat/sidebar/participants-section.ts:87`); leave it.
2. **v5 today.** `chat/message-list.ts` (selector `qt-message-list`) has
   NO danger input; `chat/message-row.ts` renders `qt-chat-desktop-avatar`
   at `:70`, `:86`, `:381` (measure which are assistant-side; v4 paints
   ASSISTANT rows only) and `chat/tool-message.ts:223` renders one too
   (check whether v4's tool-message path is a `MessageRow` — if so it
   paints); `chat/streaming-message.ts` has no input; the v5 rule
   `styles/qt-components/_chat.css:2960 .qt-chat-avatar-dangerous
   .overflow-hidden` EXISTS — byte-compare it against v4 `:2819`. The
   client predicate twin is `chat/concierge-state.ts:127
   shouldShowDangerStyling` (transcribed at P4.D144, parity-specced). The
   wiring site is `screens/salon/salon-conversation.ts:416 <qt-message-list`.
   Bind an input named for v4's prop (`isDangerousChat`) computed from the
   chat signal through the predicate; thread it to the row + streaming
   components; add the class exactly where v4 adds it.
3. **The invented warns.** `quick-hide/quick-hide.service.ts:124
   console.warn('Unable to probe for uncensored-route chats', …)` and
   `:206 console.warn('Unable to load quick-hide tags', …)`. v4's probe
   hook `components/hooks/use-has-dangerous-chats.ts:27` is a bare
   `catch {}` — SILENT; measure the tags twin (`components/layout/
   left-sidebar/sidebar-footer.tsx` and the hook it reads) before
   retiring `:206`. Spec: a `console.warn` spy asserting NOT called on the
   failing-fetch path (red-first against today's code).
4. **The modal's structured writers.** `screens/settings/images/
   image-profile-modal.ts:223-229` is a Parameters JSON `<textarea>` — a
   v5 INVENTION (its own doc `:78`, `:409-433`: "v5's legacy arm IS a
   textarea, so the string is the source of truth"); `:238 [support]=
   "loraSupport()"` mounts the LoRA editor, and `:468` shows the pattern
   — a writer re-serializes the WHOLE bag (`JSON.stringify({...bag,
   size}, null, 2)`) into the textarea, so a LoRA write while the textarea
   holds an unparseable mid-edit string either throws at `:433`'s
   `JSON.parse` or silently discards the operator's edit. The round-2
   record's ask: structured writers replacing the mid-edit textarea "once
   `loraSupport` renders without a schema" (a declaring provider with no
   `optionsSchema` — reachable since P4.D138). v4's `ImageProfileParameters
   .tsx` has no textarea — read it to decide the faithful shape (a bag
   held as an object with the textarea as a DERIVED view that refuses to
   clobber while invalid, or v4's exact control set). Whatever lands,
   spec-pin: (a) a LoRA write with a valid textarea merges and preserves
   unknown keys; (b) a LoRA write with an INVALID textarea neither throws
   nor loses the text.
5. **The temp bubble's seat.** `screens/salon/salon-conversation.ts:1338
   optimisticUser`, `:1813` the temp mint, `:1979` the doc ("the server
   attributes a typed message via `findActiveUserParticipant`, honouring
   …"). Round 2 recorded "the temp bubble's seat vs the server's
   resolution" as a follow-up: measure the one shape where the client's
   optimistic seat and the server's attribution can still disagree (the
   impersonation `turnOverride` layered above the server-authoritative
   turn — P4.D61's documented mechanism divergence) against v4's
   `useSSEStreaming.ts` optimistic construction; either converge or record
   with the v4 line. Spec-pin the answer.
6. **The fragile beats.** (a) `e2e/salon-optimistic-bubble-reconcile.spec.ts:114
   waitForChatRefetch` matches `type === 'chatGet' && chatId` on the
   request body — the round-2 record calls the match UNSCOPED and the
   injection hook's degradation SILENT (an `EventSource` handler that is
   not there makes the injected hint a no-op that still "passes"; memory
   note `e2e-inject-wire-bytes-via-eventsource`): scope the match and
   make the hook's absence a hard failure. (b) `e2e/workspace-search-
   documents-flow.spec.ts` picks a chat POSITIONALLY — pick by the seeded
   title (a sibling spec seeding newer chats in a full run is the exact
   shape that broke the marks beat at the last unification). (c)
   **the component-transfer un-park, precisely scoped** (P4.D130's lane
   record, `status-log.md` ~`:92362-92396`): `e2e/wardrobe-flow.spec.ts:683`
   parks on `hasTransferDestinations` (`:1240`) because the salon
   fixture's `characters-main.db` has no `projects`/`groups` tables and
   `enumerate_destinations` fails whole. Materialize BOTH in the spec's
   `beforeAll` (DDL from `crates/quilltap-core/src/services/provisioning/
   fresh_schema.json`; `global-setup.ts` already does it for `groups` on
   the salon instance), then fix the Copy arm's stale `option[value^=
   "character:"]` count (it asserted 0 because the fetch always died —
   the real count is 4 on that fixture) and re-drive the move beat.
   Memory note `lifting-a-park-is-a-measurement`: expect further blockers
   and name each. The `[p4d122]` store-probe park in the documents spec
   (`:112`) is a DIFFERENT park — lift it only if the same
   materialization makes it walk, measured.
7. **The six slider suffix strings.** P4.D142's record (`status-log.md`
   ~`:97128-97150`) names "six unpinned slider suffix strings" across the
   twelve `qt-range` hosts (14 files reference `qt-range`); pin each
   suffix byte in the host's spec against v4's JSX.

## Tiered deliverables

**Tier 1 (must land):**

1. The danger ring end to end (item 1/2): `isDangerousChat` input on the
   list, threaded to `message-row` (both assistant sites, tool rows if v4
   paints them) and `streaming-message`, the CSS rule byte-compared,
   parity specs (a Flagged chat paints; an Uncensored chat with
   `isDangerousChat: true` underneath does NOT; user rows never), and a
   LIVE Playwright beat over a chat seeded through the verbs' real shapes
   (the `salon-concierge-marks` spec's seeding idiom): assert the class on
   assistant avatars, flip the chat to `UNCENSORED` via `chatUpdate`,
   assert it gone.
2. The two warns retired where v4 is silent (item 3), spec-pinned.
3. The fragile-beat trio (item 6 a/b/c) — each repair run RED against the
   pre-fix shape where a red can be posed (the un-park's Copy arm must go
   red on the stale count first).

**Tier 2 (should land):**

4. The modal's structured writers (item 4) with both spec pins; the
   options-schema RESPONSE shape brought inside `core-contract.ts`
   (round 2's "outside the contract" note) — a type-only edit, name-for-
   name against the server's `image_profiles` routes family.
5. The temp bubble's seat (item 5), converged or recorded + specced.
6. The six slider suffix pins (item 7).

**Tier 3 (explicit deferrals, loud):** the dozen residue hosts with no
host class/style (P4.D142's file-header list — each a visual judgment;
record which, if any, this lane's own screens made obvious); the
`#move-folder` single-instance id (record); the SPA-wide `title=` sweep
(v4 612 sites vs v5 379 — a census, not this lane); `help/chats.md` →
the `p4.9i2` bank (note only).

## The differential requirement (restated)

The SPA's oracle is v4's client code: every ported behaviour ships with
a parity spec transcribed from v4's JSX/hook (assertions named for the
v4 line), and every user-visible surface with a Playwright beat that
drives the real server over the seeded fixture. Pin strings at their
source (memory note `angular-class-binding-dedups-and-reorders` — a
transcribed `className` assertion can measure nothing). Mutation-prove
each spec both ways (`false && …` is a BAD mutation — memory note
`false-and-mutation-kills-ts-narrowing`).

## Fixtures

Consumes the committed salon / characters e2e fixtures; the ONLY fixture
edit is the in-spec `beforeAll` materialization of `projects`/`groups`
(never a fixture regen — the P4.D130 precedent spared six harness
families). No Rust fixture touched.

## Shared contracts (binding, identical across all five orders)

- **§A — well-formed actions never move.** P4.67 may change the answer
  to `?action=` (empty), an UNKNOWN action, and DUPLICATED keys only. The
  status + body of every well-formed `?action=<served>` request and every
  single-valued query is byte-identical before and after (the existing
  web families are the pin). P4.69's SPA callers (`image-profiles.api.ts`
  `?action=options-schema|list-models`, the wardrobe/characters edges)
  therefore need NO change and P4.69 makes none.
- **§B — participant-status parsing has ONE home:**
  `quilltap_core::chat_predicates::participant_status_from_str` (+
  `is_participant_present` / `json_participant_is_present`). P4.68 owns
  the consolidation; no other lane adds a private status match, and no
  lane changes the canonical unknown-string rule (`None`/`"active"` →
  Active, `"silent"` → Silent, `"removed"` → Removed, anything else →
  Absent).
- **§C — danger styling is a client predicate.** `shouldShowDangerStyling
  (chat) = getConciergeState(chat) === 'flagged'` lives ONLY in the SPA
  (`chat/concierge-state.ts:127`, v4 `chat-override.ts:110`); P4.69 binds
  it to the message list; no server payload changes (the §A list wire of
  the `6d2a50382` round stands).
- **§D — the `system-data-*` committed fixture is P4.70's this round.**
  P4.70 widens it (additively — every existing row byte-preserved,
  `system-data.json` regenerated by the shipped builder) and re-runs all
  nine consuming families without editing them (except
  `system_import_state`'s discriminating leg, which P4.70 owns). P4.67
  consumes the committed pair at its base. The unifier re-runs P4.67's
  three consuming families over the widened pair before the gate.
- **§E — the gateway seam.** P4.71 threads `with_localhost_gateway` from
  the host into every provider construction site and adds the ONE missing
  seam (`model/completion_provider.rs`'s hard `None`). No other lane
  constructs a wire provider or touches `quilltap-host/src/spine.rs`,
  `providers.rs`, or `host.rs`; P4.68's executor census is core-only and
  RECORDS any host-side finding for P4.71/the unifier instead of editing.
- **§F — `precompute_equivalence` and the `episodic-recall-*` pair are
  P4.68's.** P4.70's fixture work is the `system-data-*` pair only; the
  two lanes' fixtures are disjoint by name.

## Ownership (binding, identical across all five orders)

| Area | Owner |
|---|---|
| `crates/quilltap-web/src/**` (every `*_routes.rs`, `log_file.rs`, the NEW shared query reader), `crates/quilltap-web/tests/**` (NEW `query_param_semantics_equivalence`; arms added to `system_body_guards_equivalence` / `files_body_guards_equivalence`); `quilltap-core`: `api/types.rs` (the `FileUpload.tags` widening ONLY), `api/system_data.rs` (the `jobs_enqueue` payload gate ONLY), `api/chat_media.rs` (the `chat_file_link` guard ONLY); harness: `system_jobs_collection_equivalence` (the `payload: []` arm), NEW `harness/oracle/cases/query-param-semantics.test.ts` | **P4.67** |
| `quilltap-core`: `chat_predicates.rs`, `enclave/announce.rs`, `services/{commonplace_notifications, user_identity_resolver, fold_episode_pass, turn_orchestrator, participant_resolver, message_finalizer, build_context, orchestrator, provider_failover, cheap_llm_exec}.rs`, `tools/{self_inventory, whisper}.rs`, `tools/doc_edit/shared.rs`, the NEW bare-executor census guard; harness: `precompute_equivalence` + `harness/oracle/cases/precompute.test.ts` + `harness/oracle/fixtures/build-episodic-recall-fixture.ts` + the committed `episodic-recall-*` pair (RE-RUN `recall_replay_equivalence`, `vault_conv_search_equivalence`), the failover / chain-walk families (`orchestrator_tier3`, the `provider_failover` family) + their oracle cases | **P4.68** |
| SPA: `chat/{message-list, message-row, streaming-message, tool-message}.ts` (+specs), `screens/salon/salon-conversation.ts`, `quick-hide/quick-hide.service.ts` (+spec), `screens/settings/images/image-profile-modal.ts` (+spec), the twelve `qt-range` hosts' SPECS (suffix pins only), `styles/qt-components/_chat.css` (only if a rule is missing); e2e: `salon-optimistic-bubble-reconcile.spec.ts`, `workspace-search-documents-flow.spec.ts`, `wardrobe-flow.spec.ts`, NEW danger-avatar beat, `global-setup.ts` if the transfer un-park needs it | **P4.69** |
| `quilltap-core`: `tools/generate_image.rs`, `image_gen/lora_support.rs`, `image_gen/params_builder.rs`; harness: `image_generation_tier3_equivalence`, `image_gen_leaves_equivalence`, `image_generate_route_equivalence` (re-run), `projects_routes_equivalence` (+ its oracle case), `system_import_state` (the discriminating leg), `harness/oracle/fixtures/build-system-data-fixture.ts` + the committed `system-data-{main,mount,llmlogs}.db` + `system-data.json`, `harness/oracle/cases/image-generation.test.ts`, `image-gen-leaves.test.ts`; the other eight `system-data` consumers RE-RUN, not edited | **P4.70** |
| `quilltap-host`: `env.rs`, `providers.rs`, `spine.rs`, `host.rs`, `avatar_preview.rs`, NEW `host_gateway.rs`; `quilltap-core`: `model/completion_provider.rs` (the gateway seam), `model/transport.rs` (doc only), `provider_manifest/rewrite.rs` (header + the pure resolver if placed here), `model/provider_models_api.rs`, `api/provider_actions.rs`, `model/image_dialects.rs` (a base-URL seam ONLY if v4 rewrites there — measure); harness: NEW `host_gateway_equivalence` + `harness/oracle/cases/host-gateway.test.ts`; `Dockerfile`, `docs/developer/running.md` | **P4.71** |
| Playwright (port 4319) | **P4.69's branch only**; every other lane's beats are authored gated (ACTIVATE-AT-UNIFY) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it |


**Version bumps:** this lane bumps the SPA (`apps/web/package.json`, base
0.5.623) per commit and no crate; the unifier recounts.

## Verification gate before declaring done

`npm run lint` (the qt-class guard) + `npm test` (full, no `--filter`) +
`npm run build` in `apps/web`; the full Playwright suite against the
final build (`run_in_background`, full log + sentinel, NEVER `| tail`;
read the summary line AND every `✘`) — the new beat LIVE, the repaired
beats green, the un-parked transfer beat green or re-parked on a NAMED
measured blocker; the mutation proofs recorded. No Rust gate (no crate
touched) — if any crate file is touched, STOP: it belongs to a sibling.
Per-commit rule: `.claude/commands/commit.md`. Lane record →
`status-log.md` ("Lane record — P4.69"); this file's status header
updated at close.
