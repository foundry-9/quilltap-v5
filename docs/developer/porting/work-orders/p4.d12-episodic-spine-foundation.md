# Work order P4.d12 — the episodic-recall drift catch-up, **round 1 / lane 1: the episodic-spine foundation** (D23 schema re-dump + the five new columns through the data layer + the inert-path pure-logic ports + the memory-family oracle rebase) — round baseline `8bf3cb5f`

> **STATUS: OPEN (planning, 2026-07-21).** Round-1 lane of the
> **episodic-recall drift catch-up campaign** — the largest single v4
> drift in the port (`8bf3cb5f`, a squash-merge of three feature
> branches). This lane is the campaign KEYSTONE: it lands the schema +
> data layer + the pure/render logic that fires on *inert* inputs, and
> rebases the memory-family oracles onto the new baseline. It deliberately
> does **NOT** port the episodic *behavior* (time-aware retrieval, event
> extraction, the fourth cadence, fold Timeline, gate date-guard,
> deep-dive tool filters) — that is rounds 2 and 3. See §"The campaign"
> below.

## Preamble

**v4 reference baseline for this round: `8bf3cb5f` (4.9-dev, "Unify
episodic recall + character outfit work"), tree state verified at
planning.** At lane start, run in `~/source/quilltap-server`:

```bash
git log 8bf3cb5f..HEAD --oneline
```

**If HEAD moved, STOP and report.** (v4 is mid-4.8/4.9 development; a
version/tag commit may land — report it either way. The prior baseline
was `7e6d13e5`; this round moves it to `8bf3cb5f`.) If v4's checkout is
dirty, or you want reproducibility, create a PINNED detached worktree at
lane start and regenerate from it (the `oracle-regen-pinned-v4-worktree`
memory — symlink BOTH `node_modules`: root +
`packages/{quilltap,plugin-types,plugin-utils}` + `plugins/dist/*`):

```bash
cd ~/source/quilltap-server
git worktree add --detach /private/tmp/qt-v4-pin-8bf3cb5f 8bf3cb5f
ln -s ~/source/quilltap-server/node_modules /private/tmp/qt-v4-pin-8bf3cb5f/node_modules
for p in quilltap plugin-types plugin-utils; do
  ln -s ~/source/quilltap-server/packages/$p/node_modules \
        /private/tmp/qt-v4-pin-8bf3cb5f/packages/$p/node_modules
done
```

**Sibling lanes (this round):** `p4.6bh-character-outfit-server.md`
(the `canChooseOutfit` vault field + the wardrobe-permission PUT
toggles) and `p4.6bi-outfit-newchat-spa.md` (the character/outfit SPA +
the New-Chat picker re-port). **File ownership is disjoint from both:**
this lane owns the memory subsystem + the memory data layer + the
`fresh_schema.json` re-dump; the sibling server lane owns the character
vault-overlay + `api/characters.rs`; the SPA lane owns `apps/web`. The
only shared crate is `quilltap-core` — but on **disjoint files** (see
Ownership). You run in a worktree; a unification pass integrates the
lanes onto main.

**⚠ This lane MOVES the oracle baseline for the memory-row and
memory-pure families to `8bf3cb5f`. It does NOT move it for the deferred
families** (the extraction-prompt, fold, retrieval, and deep-dive tool
families stay at `7e6d13e5` until rounds 2/3 port their behavior). Mixed
oracle vintages are the established pattern (CLAUDE.md: "untouched
families' committed oracles keep their earlier regen vintages") — see
§"Fixtures & oracle rebase" for the exact regen set vs the leave-alone
set. Expect `provisioning_equivalence` to trip after the drift check —
that is the tripwire firing as designed (v4 drift, not a v5 bug).

## The campaign (context — do not implement rounds 2/3 here)

v4 `8bf3cb5f`'s **episodic-recall-overhaul** is a five-workstream feature
(A–E + a §3 replay harness; the design authority is
`~/source/quilltap-server/docs/developer/features/episodic-recall-overhaul.md`,
mirror it under `docs/v4/` if not already). The v5 catch-up is a
**3-round campaign**:

- **Round 1 (THIS lane, P4.d12):** the episodic **spine** — schema
  (D23), the five new columns through the data layer, and the pure/render
  logic that fires on inert inputs (memory-weighting `episodicBonus` +
  event-clock age label; the injector's dated dynamic head; the pure
  `episodic` module). Rebase the memory-row/pure oracle families onto
  `8bf3cb5f`; verify they match via the feature's **inert-path
  guarantee** (§4 of the spec: "degrade to today, never block" — on
  semantic memories with null `occurredAt` and non-retrospective turns,
  v4's new code is byte-identical to old, differing only by the new
  column values in emitted rows).
- **Round 2 (future — workstreams B + D + §3):** time/entity-aware
  retrieval (`searchMemoriesSemantic` `occurredWithin`, entity anchoring,
  multi-probe), `turnTemporal` made real (recall-tags), vault-summary
  date filters, the deep-dive tools (`search` `since`/`until`/
  `aboutCharacter`, `read_conversation` range, the orchestration prose),
  and the `recall-replay` CLI + replay harness.
- **Round 3 (future — workstreams A-creation + C + E):** the clocked
  extraction prompt + EVENT category + `kind`/`when`/`entities` output,
  the fold-time episode pass (new module), recall-on-reference (the
  fourth cadence: enlarged head + scoped mini-recap + the
  `retrospective-recall` whisper), the fold Timeline section, the gate
  date-guard, the compression-prompt keep/drop flip, and the Salon
  "Story's Clock" timeline-mode SPA switch.

The next `/setupphase` plans rounds 2/3 once this lands. **Do not pull
their behavior forward** — it needs new fixtures that exercise the new
inputs, and mixing it in here would blow the round.

## Mandate

Deliver the episodic-spine **data + pure-logic foundation** in
`quilltap-core`, verified against v4 `8bf3cb5f`:

1. The **D23 schema re-dump** adding `chats.timelineMode` and
   `memories.{occurredAt,narrativeTime,entities,kind}` + the
   `idx_memories_occurredAt` index (re-dumped from v4's live
   `generateDDL`, never hand-edited).
2. Those columns threaded through the memories + chats **data layer**
   (SELECT column lists, positional marshal, insert/update structs,
   defaults `entities='[]'` / `kind='semantic'`, and the create-path
   `occurredAt` stamp from the source message's `createdAt`).
3. The pure **`episodic`** module (port all four exports) with its own
   tier-1 differential.
4. The **memory-weighting** deltas (`episodicBonus` in config/result +
   `calculateProtectionScore`; `formatRelativeAge` reading the event
   clock via `eventReferenceTimeMs`).
5. The **memory-injector** dated dynamic head
   (`formatDynamicMemoryHead` prepends the `occurredAt ?? createdAt`
   age label + `narrativeTime` verbatim).
6. The **memory-family oracle rebase** onto `8bf3cb5f` for the
   row-emitting + pure families, each verified to match via the
   inert-path guarantee.

**Do NOT touch:** the character vault-overlay, `api/characters.rs`, or
`apps/web` (sibling lanes own those). Do NOT port the extraction-prompt
changes, the fold-episode pass, the retrieval multipliers
(`turnTemporal`, `occurredWithin`), the fold Timeline, the gate
date-guard, the compression flip, the deep-dive tool filters, or the
recall-replay CLI — those are rounds 2/3 and belong to families this
lane leaves at `7e6d13e5`.

## Survey-verified starting points (fresh v4 + v5 read, 2026-07-21)

### The schema change (v4 `8bf3cb5f`, `docs/developer/DDL.md`)

```
chats:    "timelineMode" TEXT DEFAULT NULL   -- 'realtime' (NULL reads as realtime) | 'narrative'
memories: "occurredAt"    TEXT DEFAULT NULL         -- ISO wall-clock EVENT time (vs createdAt)
          "narrativeTime" TEXT DEFAULT NULL         -- free-text in-story time
          "entities"      TEXT DEFAULT '[]'         -- JSON string[] of proper nouns
          "kind"          TEXT DEFAULT 'semantic'   -- 'semantic' | 'episodic'
index:    CREATE INDEX "idx_memories_occurredAt" ON "memories" ("occurredAt" DESC)
```
All additive, all nullable-or-defaulted; v4 ships a pure-SQL backfill
migration (`migrations/scripts/add-episodic-memory-fields.ts`) — v5's
migration runner stays deferred, so v5 gets the columns via the re-dump
and the create-path stamp, not the migration.

### v5 D23 mechanism (survey-confirmed)

- Committed snapshot: `crates/quilltap-core/src/services/provisioning/fresh_schema.json`
  (per-partition DDL; **committed with no trailing newline** — preserve
  that). Sibling `chat_settings_seed.json` moves with it.
- Read by `crates/quilltap-core/src/services/provisioning/mod.rs:70`
  (`include_str!`), replayed per-partition.
- **Three harness tests read `fresh_schema.json` directly and pin its
  relative path / statement shape** —
  `builtin_templates_equivalence.rs:83`, `builtin_mounts_equivalence.rs:46`,
  `pascal_workbench_route_equivalence.rs:117`. They break if the file's
  statement count/shape shifts; re-run them.
- Tripwire test: `crates/quilltap-harness/tests/provisioning_equivalence.rs`,
  `provisioning_matches_v4_fresh_instance` (L227), env `QT_ORACLE_PROVISION`
  (+ `QT_FIXTURE_V4_FRESH`, `QT_V5_PROVISION_OUT`); byte-exact per-partition
  `sqlite_master` diff at L261. SKIPs when the env var is unset.
- **Re-dump recipe** (drives v4's REAL repositories; the dumper is
  `harness/oracle/provision/dump-fresh-schema.ts` — do NOT hand-edit the
  JSON):
  ```bash
  N=~/.nvm/versions/node/v24.13.1/bin   # or your node bin dir
  W=<this worktree abs path>
  cd ~/source/quilltap-server            # or the pinned worktree
  QT_SCHEMA_OUT=/tmp/qt-fresh-schema.json QT_SEED_OUT=/tmp/qt-chat-settings-seed.json \
    $N/npx tsx $W/harness/oracle/provision/dump-fresh-schema.ts
  # then REPLACE both committed files with the outputs (no trailing newline):
  #   crates/quilltap-core/src/services/provisioning/fresh_schema.json
  #   crates/quilltap-core/src/services/provisioning/chat_settings_seed.json
  QT_ORACLE_PROVISION=/tmp/oracle-provision.json QT_V4_FRESH_OUT=/tmp/qt-v4-fresh \
    $N/npx tsx $W/harness/oracle/provision/build-provision-oracle.ts
  cd $W && QT_ORACLE_PROVISION=/tmp/oracle-provision.json \
    QT_FIXTURE_V4_FRESH=/tmp/qt-v4-fresh QT_V5_PROVISION_OUT=/tmp/qt-v5-provisioned \
    cargo test -p quilltap-harness --test provisioning_equivalence -- --nocapture
  cd ~/source/quilltap-server && QT_FIXTURE_V5_PROVISIONED=/tmp/qt-v5-provisioned \
    $N/npx tsx $W/harness/oracle/provision/verify-v5-provisioned.ts
  ```
- **⚠ The re-dump diff is often larger than predicted** (`generateDDL`
  emits mid-table; a prior round found an unexpected `characters.metadata`
  column). **Diff and grep the regenerated file before trusting a green.**
  Confirm the diff contains ONLY the six expected additions
  (`timelineMode`, the four `memories` columns, the one index) — if it
  carries more, that is additional v4 drift; classify it, and adopt it
  faithfully (it rides the same re-dump) or STOP and report if it looks
  like a different feature.
- A schema adoption is **atomic** and fans out to: `fresh_schema.json`
  (+ `chat_settings_seed.json`), the DB repo column lists, the
  **positional** marshal indices, insert/update structs, the tier-2
  fixtures/build-scripts, and the three `*_equivalence.rs` tests above.
  (Memory notes: `schema-rule-follows-v4-d23.md`,
  `oracle-regen-silent-stale-pass.md`.)

### v5 memory data layer (survey-confirmed edit sites)

- **`memories` write** — `crates/quilltap-core/src/db/memories.rs`:
  INSERT column list L140-146 + `params![…]` L147-169; `MemCreate`
  (L51-70), `CreateOptions` (L74-77), `MemUpdate` (L94-114), `set_col!`
  update block (~L192+); test DDL L666-672. Append the four columns
  (order: after `witnessedContext`, per the DDL) to the struct + insert;
  `entities` default `'[]'`, `kind` default `'semantic'`.
- **`memories` read/marshal** — `crates/quilltap-core/src/db/memories_read.rs`:
  `const COLS` L55-58 (indices 0..=20, schema order) and `marshal_row`
  L109-189 (positional getters — **appending at the end is safest**;
  adding mid-list shifts every later index). `entities` → the
  `json_array_or_empty` path (L85, like `keywords`/`tags`);
  `occurredAt`/`narrativeTime` → `put_nullable_str` (L70); `kind` →
  required-string-with-default.
- **`chats` write** — `crates/quilltap-core/src/db/chats.rs`: INSERT
  list L676-716 (`create`), `ChatUpdate` (L427+) + `set_col!` (L842+).
- **`chats` read** — `crates/quilltap-core/src/db/chats_read.rs`:
  `const ALL_COLUMNS` L51 ("all 96 columns, DDL/SELECT order") + the
  SELECT at L326 + row marshal. Append `timelineMode`.
- **Create-path `occurredAt` stamp** — `crates/quilltap-core/src/api/memories.rs`
  `memory_create` L469 already threads `source_message_timestamp: None`
  (L496) into the create path — that is the hook. v4's rule (spec §A
  "Stamping rules"): `occurredAt` = source turn's message `createdAt`
  (`sourceMessageId` → message `createdAt`), else the memory's own
  `createdAt`. Wire this so a created memory lands with a non-null
  `occurredAt` resolved from `sourceMessageId` when present. (The
  model-emitted `when`-phrase resolution is round 3 — round 1 stamps only
  from the transcript timestamp / createdAt fallback.)

### v5 pure/render modules (survey-confirmed)

- **`episodic` (new)** — v4 `lib/memory/episodic.ts` (pure, I/O-free; 4
  exports: `buildMemoryAnchorLine`, `buildMemoryEmbeddingText`,
  `resolveWhenPhrase`, `eventReferenceTimeMs`). Create
  `crates/quilltap-core/src/episodic.rs` (or a submodule) porting all
  four with a tier-1 differential. `eventReferenceTimeMs` is consumed by
  weighting + injector this round; `buildMemoryEmbeddingText` /
  `buildMemoryAnchorLine` are the canonical embedded-text builder (wire
  them into the gate/create embedding-text sites **only where inert** —
  on null anchors they return `summary\n\ncontent`, byte-identical to
  today); `resolveWhenPhrase` lands as a pure fn but stays **unwired**
  (round 3's extraction is its only caller). Note the `narrativeTime`
  anchor renders as `story time: …` and the date is ISO-`slice(0,10)`
  (see the source).
- **`memory-weighting`** — `crates/quilltap-core/src/memory_weighting.rs`:
  `ProtectionConfig` L31 (add `episodic_bonus`, default `0.10`),
  `ProtectionScore` L100 (add the `episodic_bonus` output field),
  `calculate_protection_score` L109 (add `episodicBonus = kind ==
  'episodic' ? config.episodicBonus : 0`, folded into the `min(1, …)`),
  `format_relative_age` L186 (`referenceTime = eventReferenceTimeMs(
  memory.occurredAt, referenceTimeMs(memory))`). **Decay stays on the
  write clock** — only the human-facing age label follows the event
  clock (v4 comment; carry it).
- **`memory-injector`** — `crates/quilltap-core/src/memory_injector.rs`:
  `format_dynamic_memory_head` L812 — prepend the `[3 days ago]`-style
  label (the one `format_memories_for_context` L465 already uses),
  computed from `occurredAt ?? createdAt`, plus `narrativeTime` verbatim
  when present (v4 `memory-injector.ts:566-577`). `DYNAMIC_HEAD_*`
  budget/size constants stay at their round-1 values (200/5); the
  enlarged head is round 3.

## Tiered deliverables

### Tier 1 — must land

1. **D23 re-dump** — `fresh_schema.json` (+ `chat_settings_seed.json` if
   it moves) regenerated from v4 `8bf3cb5f`; diff-verified to contain
   ONLY the six expected additions; `provisioning_equivalence` green;
   the three direct-reader tests re-run green.
2. **memories data layer** — the four columns through
   `memories.rs` + `memories_read.rs` (struct/insert/marshal/defaults) +
   the create-path `occurredAt` stamp. Verified by the memories tier-2 /
   read / routes differentials regenerated at `8bf3cb5f`.
3. **chats data layer** — `timelineMode` through `chats.rs` +
   `chats_read.rs`. Verified by the chats read/tier-2 differential(s)
   regenerated at `8bf3cb5f`.
4. **`episodic.rs`** — all four pure exports + a new tier-1 differential
   (`QT_ORACLE_EPISODIC`, oracle case `harness/oracle/cases/episodic.ts`
   driving v4's real `lib/memory/episodic.ts`). 1e-12 floats / exact
   strings.
5. **memory-weighting** — `episodicBonus` config/result/calc +
   `formatRelativeAge` event-clock; `QT_ORACLE_WEIGHTING` regenerated at
   `8bf3cb5f`, green. Fixtures must include at least one `kind:'episodic'`
   row so the bonus is exercised (extend the oracle case if needed).
6. **memory-injector** — dated `format_dynamic_memory_head`;
   `QT_ORACLE_MEMORY_INJECTOR` regenerated at `8bf3cb5f`, green.
7. **The memory-family oracle rebase** — see §"Fixtures & oracle rebase"
   for the exact regen set. Every regenerated family green with
   `--nocapture`, zero SKIP.

### Tier 2 — should land

8. Wire `buildMemoryEmbeddingText` as the single-source-of-truth embedded
   text at the gate + create sites **only if it is provably inert**
   (null-anchor → `summary\n\ncontent`) — this pre-positions round 3. If
   it perturbs any `8bf3cb5f`-rebased gate/create differential, back it
   out and leave a loud note for round 3.
9. Stale-comment cleanups the drift calls out (the 0.4/0.6 blend comments
   in `memory-service`, the 0.80/0.70 gate-threshold comments) — only in
   files this lane already touches; do not widen ownership.

### Tier 3 — explicit loud deferrals (rounds 2/3; enumerate, do not stub)

Everything episodic-**behavioral**. Where a round-1 struct/field exists
but is unwired, that is fine (a pure fn with no caller); where a code path
would need the new behavior, it keeps today's behavior (the inert path) —
**no `not_available` refusal is required for round 1**, because the
feature degrades to current behavior by design. Enumerate in the status
header, for rounds 2/3:

- **Round 2:** `searchMemoriesSemantic` `occurredWithin` + entity
  anchoring + multi-probe; `turnTemporal` real (recall-tags
  temporalPast/Moment/suspend-recentlyWhispered); vault-summary date
  filters; deep-dive tool `since`/`until`/`aboutCharacter` +
  `read_conversation` range + orchestration prose; the `recall-replay`
  CLI + §3 replay harness.
- **Round 3:** the clocked extraction prompt + EVENT category +
  `kind`/`when`/`entities` output + soft cap 2→3; `resolveWhenPhrase`
  wiring; first-write `extractNovelDetails` capture; the fold-episode
  pass (new module); recall-on-reference cadence
  (`retrospective-recall` whisper); the fold Timeline section; the gate
  date-guard (>7-day `occurredAt` → INSERT_RELATED); the compression
  keep/drop flip; the anchor-line embedded-text on real writes; the
  Salon "Story's Clock" SPA.

## The differential requirement (restated)

Every ported unit ships an equivalence test driving v4's REAL `lib/` code
(the tsx/jest oracle → NDJSON → Rust diff, per tier). This lane is
tier-1 (episodic + weighting), tier-2 (memories/chats data + provisioning),
and tier-3-adjacent (the gate/processor rebases run their existing mocked
oracles). **Point at the existing recipes, do not invent:** the memory
differential harness (survey §2 below), the D23 re-dump recipe (above),
and the pinned-worktree regen memory (`oracle-regen-pinned-v4-worktree`).
The tier-1 `episodic` oracle follows the `memory-weighting.ts` /
`ranking-blend.ts` pure-leaf oracle pattern in `harness/oracle/cases/`.

## Fixtures & oracle rebase

**The memory differential families (survey-confirmed), and their round-1
disposition.** Each test SKIPs unless its `QT_ORACLE_*` env var points at
a regenerated NDJSON. The **guiding principle:** rebase a family onto
`8bf3cb5f` (regenerate its oracle from the new baseline) **only** when its
v4 change is (a) row-shape (the four new columns appear in emitted rows)
or (b) a round-1 pure/render change (weighting struct, injector label).
On the *existing committed fixtures* (semantic memories, null
`occurredAt`, non-retrospective turns) such a family matches with only
this lane's changes — the inert-path guarantee. **Leave** a family at
`7e6d13e5` (do NOT regenerate) when its v4 change is a prompt-text change
or new-input-only behavior — rounds 2/3 own those.

**REBASE to `8bf3cb5f` this round (regenerate + verify green):**

| Family | Test / env | Why it moves |
|---|---|---|
| weighting | `memory_weighting_equivalence` / `QT_ORACLE_WEIGHTING` | `episodicBonus` field + event-clock age (ported) |
| episodic (new) | new test / `QT_ORACLE_EPISODIC` | new pure module |
| memories read | `memories_read_equivalence` / `QT_ORACLE_MEMREAD` | four new columns in the row |
| memories tier2 | `memories_tier2_equivalence` / `QT_ORACLE_MEM` | new columns in create/read |
| memories routes | `memories_routes_equivalence` / `QT_ORACLE_MEMORIES_ROUTES` | new columns + `occurredAt` stamp |
| memory delete/cascade | `memory_delete_tier2` / `QT_ORACLE_MEMDEL`, `memory_cascade_tier2` / `QT_ORACLE_MEMCASCADE` | **only if** they emit memory rows — verify; regen if red-on-columns |
| injector | `memory_injector_equivalence` / `QT_ORACLE_MEMORY_INJECTOR` | dated dynamic head (ported) |
| housekeeping | `memory_housekeeping_tier2` / `QT_ORACLE_MEMHOUSEKEEPING` | protection score gains `episodicBonus` field (0 on semantic — decision inert; verify) |
| gate | `memory_gate_tier3` / `QT_ORACLE_GATE` (+`QT_ORACLE_OUT`) | emitted rows gain columns; embedding text inert on null anchors — **the date-guard logic is DEFERRED; verify the fixtures are inert** |
| processor | `memory_processor_tier3` / `QT_ORACLE_PROCESSOR` (+`QT_ORACLE_OUT`) | writes gain `kind='semantic'` + stamped `occurredAt` — **verify: if the tier asserts the changed extraction PROMPT bytes, it is NOT inert → move it to the DEFER set and leave at `7e6d13e5`** |

**LEAVE at `7e6d13e5` (do NOT regenerate — rounds 2/3):**

- memory-tasks tier1 (`QT_ORACLE_MEMORY_TASKS`) — extraction prompt text
  changed (EVENT category, clock header): **round 3**.
- recall-tags (`QT_ORACLE_RECALL_TAGS`) — `turnTemporal`: **round 2**
  (v5 unchanged this round; fixtures are non-retrospective).
- ranking-blend (`QT_ORACLE_RANKING`) — blend unchanged (spec §1.7); at
  most a stale-comment cleanup, no oracle move.
- context-summary (`QT_ORACLE_CTXSUM`/`QT_ORACLE_CONTEXT_SUMMARY`) —
  FOLD_SUMMARY Timeline: **round 3**.
- build-context (`QT_ORACLE_BUILD_CONTEXT`) — enlarged head: **round 3**
  (but if the injector date-render bleeds into its rendered head output,
  the tier goes red — in that case regen it too and verify inert).
- carina-memory-extraction (`QT_ORACLE_CARINA_MEM`) — extraction:
  **round 3**.
- watermark (`QT_ORACLE_WATERMARK`), memories-config
  (`QT_ORACLE_MEMORIES_CONFIG`), memory-name-helpers
  (`QT_ORACLE_MEMORY_NAME_HELPERS`), extract-novel-details
  (`QT_ORACLE_EXTRACT_NOVEL_DETAILS`) — unaffected by the data layer /
  new-input logic; leave. (If any emits memory rows and goes red on the
  new columns, promote it to the REBASE set.)

**These lists are the plan; confirm empirically.** After the code
changes, run each REBASE family against its regenerated `8bf3cb5f`
oracle. A family that trips **non-inertly** (a diff beyond the new column
values / ported render) is either an unanticipated inert-path break
(absorb it if it is genuinely data/render; **escalate/report if it is
deferred behavior** you would have to pull forward — do not pull round
2/3 behavior into round 1). Grep every regenerated NDJSON before trusting
a green (`oracle-regen-silent-stale-pass.md` — an erroring builder leaves
the stale fixture in place).

**Committed fixtures this lane may need to extend:** the memory tier-2/
routes DB fixtures (add the four columns to any hand-written schema in
their build scripts), and the weighting oracle case (add a `kind:
'episodic'` row). Any in-crate test DDL that a *touched* test exercises
(`memory_gate.rs:857`, `memory_service.rs:746`, `housekeeping.rs:536`,
`memories.rs:666`, `host_boot.rs:162`, `job_runner.rs:858`) gains the
columns **only if that test reads them** — otherwise leave it.

## Shared contract (binding — none for this lane)

This lane defines **no cross-lane contract**. Its schema re-dump
(`fresh_schema.json`) touches only DB columns the sibling lanes do not
(`chats.timelineMode`, the `memories` columns); `canChooseOutfit` is a
vault properties.json field (no DB column, no re-dump) and
`canDressThemselves`/`canCreateOutfits` are pre-existing columns — the
sibling server lane does **not** re-dump the schema. Confirm at
integration that the two lanes' `fresh_schema.json` histories do not
collide (they should not — only this lane edits it).

## Ownership (binding)

**This lane (P4.d12) owns and may edit:**
- `crates/quilltap-core/src/services/provisioning/fresh_schema.json`
  (+ `chat_settings_seed.json` if it moves) — **sole owner of the
  re-dump this round.**
- `crates/quilltap-core/src/db/memories.rs`, `db/memories_read.rs`,
  `db/chats.rs`, `db/chats_read.rs`.
- `crates/quilltap-core/src/api/memories.rs` (the create-path stamp).
- `crates/quilltap-core/src/memory_weighting.rs`, `memory_injector.rs`,
  and a new `episodic.rs` (+ its `mod` registration).
- The memory-family services **only** where a rebased differential
  forces an inert data/render touch
  (`services/memory_gate.rs`, `services/memory_processor.rs`,
  `services/housekeeping.rs`, `services/memory_service.rs`) — keep edits
  minimal and inert; **do not** port deferred behavior here.
- `crates/quilltap-harness/tests/*` for the memory + provisioning
  families it rebases + the new `episodic` test; `harness/oracle/cases/*`
  + `harness/oracle/provision/*` for their oracles; committed memory
  fixtures it extends.
- The three `fresh_schema.json` direct-reader tests
  (`builtin_templates_equivalence.rs`, `builtin_mounts_equivalence.rs`,
  `pascal_workbench_route_equivalence.rs`) — re-run + adjust ONLY if the
  statement-count assertion needs updating for the new columns/index.

**This lane must NOT touch** (sibling lanes / rounds 2/3):
`db/vault_read_overlay.rs`, `db/vault_character_write.rs`,
`db/vault_character_update.rs`, `db/characters*.rs`, `api/characters.rs`
(P4.6bh); anything under `apps/web` (P4.6bi); the extraction-prompt /
fold / retrieval / deep-dive-tool code paths beyond an inert marshal
(rounds 2/3).

**Version-bump ownership:** this lane bumps `quilltap-core` and
`quilltap-harness` (per-commit, per `.claude/commands/commit.md`). The
sibling server lane also bumps those two crates on disjoint files — the
**unifier accumulates** the version numbers (memory note:
`p4.6aw-ax-8-round-plan.md`, "identical version bumps merge SILENTLY —
recount"). Do not try to coordinate the exact integer with the sibling;
just bump each commit.

## Verification gate (run before declaring the lane done)

- `cargo fmt --all --check` clean.
- `cargo clippy --workspace --all-targets` clean **and**
  `cargo clippy --workspace --all-targets --all-features` clean
  (`-D warnings`).
- `cargo test --workspace` green with the lane's `QT_ORACLE_*` env vars
  set (the rebased families + provisioning + the new `episodic` test) —
  and the three `fresh_schema.json` direct-reader tests green.
- Every rebased differential re-run **by name** with `--nocapture`,
  **zero SKIP**, over oracles freshly regenerated from v4 `8bf3cb5f`.
  Grep each NDJSON for content before trusting green.
- Release build clean (`cargo build --release`).
- Per-commit discipline per `.claude/commands/commit.md` (small units,
  each with its differential; `CHANGELOG` append-only, plain English;
  `status-log.md` append-only).
- **Flag explicitly** in the status header: this lane does not carry the
  real-instance / episodic-behavior proof (rounds 2/3); the crypto/cipher
  paths are untouched; and the exact REBASE-vs-DEFER family set as it
  actually landed (with any family you had to move between the lists and
  why).
