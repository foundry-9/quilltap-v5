# Work order P4.78 — `chatCreate` parses its whole body the way v4 does (dogfood finding #115): v4's `createChatSchema` as a validation stage ahead of any work, one refusing corpus arm per rule, and the Zod `details` envelope measured

**Status:** OPEN (ordered 2026-09-06, the `f699da6f6` 4.9.x drift catch-up
round (first drafted against `ba34fa367`; repointed the same day when the
4.9.2 squash landed on `main`): P4.D160 ∥ P4.D161 ∥ P4.D162 ∥ P4.78 ∥ P4.79).

## Preamble

- **v4 oracle baseline: `c2232cd9a`** (drift-ledger §1); **this lane's pin
  is the round's target `f699da6f6`** (§B) — `app/api/v1/chats/route.ts` is
  unchanged between them (no drift row names it), so the pin is a hygiene
  matter, not a content one. The surface is `dogfood-findings.md` → Standing
  notes → "`chatCreate` lacks v4's whole-body Zod parse (2026-09-06, finding
  #115)": a stored `controlledBy: "LLM"` split the server's and the SPA's
  readers on the Friday copy because v5 never refused it.
- **Freshness probe at lane start** (ledger §2): checkout on `bugfix`, tree
  clean, `git log f699da6f6..main` empty, `git log 1a2b2164c..bugfix` empty.
  **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED** (ledger §1; §B): `/tmp/qt-v4-pin-p478-
  f699da6f6`, three symlink classes; the capstone recipe's `/tmp/qt-cc-*`
  staging under a lane-unique prefix (§C).
- **Sibling lanes:** P4.D160/P4.D161 (bug 123), P4.D162 (bugs 124/125),
  P4.79 (Brahma). §A–§H + Ownership binding and identical across all five.
  `api/types.rs` is NOBODY's — the `ChatCreate { request: Map }` raw-map
  carry STAYS (it is what delivers wrong TYPES to the handler so they can be
  refused there).
- **Worktree:** per `carryout` (branch `claude/p4-78-…`); absolute worktree
  paths; `CHANGELOG`/`status-log.md` append-only. **No Playwright** (§E).

## Mandate

Land v4's whole `createChatSchema` as ONE validation stage at the top of
`handle_create`, running BEFORE the continuation 404, before the progress
emitter, before any read — every rule, every arm, answering v4's `400
Validation error` and writing nothing; subsume the three ad-hoc arms v5
already has (`conciergeState`, `roleplayTemplateId`, `timestampConfig`) into
the stage without changing what they answer; and give the capstone family one
refusing case per rule, each paired with the 200 it would otherwise be, so
the stage is measured arm by arm against v4's REAL route. Then MEASURE the
Zod `details` envelope (the family's long-standing "standing error-envelope
deferral") and close it where the issue bytes reproduce. Must NOT touch: the
dispatch carry, the creation spine below the stage (participants build,
greeting, Concierge flip, continuation replay), `api/types.rs`, or any
sibling's file.

## Survey-verified starting points (2026-09-06)

1. **v4 — `app/api/v1/chats/route.ts:89-175+`** (read to the schema's
   closing brace — the survey stopped at `:175`): `createParticipantSchema`
   `{type: z.literal('CHARACTER'), characterId: z.uuid(), connectionProfileId:
   z.uuid().optional(), imageProfileId: z.uuid().optional() /* legacy,
   ignored */, controlledBy: z.enum(['llm','user']).optional(),
   selectedSystemPromptId: z.uuid().optional()}`; `createChatSchema`
   `{participants: z.array(createParticipantSchema).min(1, 'At least one
   participant is required'), title: z.string().optional(), scenario:
   z.string().optional(), scenarioId: z.string().uuid().optional(),
   projectScenarioPath: z.string().max(500).optional(), generalScenarioPath:
   z.string().max(500).optional(), groupScenarioPath: z.string().max(500)
   .optional(), groupScenarioGroupId: z.uuid().optional(), timestampConfig:
   TimestampConfigSchema.optional(), projectId: z.uuid().optional(),
   imageProfileId: z.uuid().optional(), roleplayTemplateId: z.uuid()
   .nullable().optional(), conciergeState: z.enum(['monitored','flagged',
   'vouched','uncensored']).optional(), outfitSelections: z.array(
   OutfitSelectionSchema).optional(), avatarGenerationEnabled: z.boolean()
   .optional(), continuationFromChatId: z.uuid().optional(), progressId:
   z.uuid().optional(), chatType: z.enum(['salon','autonomous']).optional(),
   scheduleCron: z.string().max(120).optional(), scheduleFreshnessWindowMs:
   z.number().int().positive().optional(), budgetMaxTurns / budgetMaxTokens /
   budgetMaxWallClockMs: z.number().int().positive().optional(), …}` (transcribe
   whatever follows `:175` too). Note the two spellings `z.uuid()` vs
   `z.string().uuid()` (`scenarioId`) — Zod 4 treats them alike for v4
   strings; record it. The parse: `:1084 const validatedData =
   createChatSchema.parse(body)` — the FIRST statement after `req.json()`,
   before `createCreationProgressEmitter(validatedData.progressId)` (`:1090`)
   and the continuation lookup (`:1097`). A `ZodError` is caught by
   `createContextHandler` (`lib/api/middleware/context.ts:166`) →
   `validationError(error)` (`lib/api/responses.ts:108-118`): `{ error:
   'Validation error', details: zodError.issues }`, status 400. Sub-schemas:
   `TimestampConfigSchema` (`lib/schemas/settings.types.ts:88-100+`: `mode`
   enum default `NONE`, `format` enum default `FRIENDLY`, `customFormat`
   nullable optional, `useFictionalTime` boolean default false, the base
   fictional timestamp … — read the whole object; v5 already ports its
   refusal arms), `OutfitSelectionSchema` (`lib/schemas/wardrobe.types.ts:224
   -229`: `characterId: UUIDSchema`, `mode: OutfitSelectionModeEnum`, `slots:
   EquippedSlotsSchema.optional()` — read the two enums and the slots
   schema; the hair slot is a FIFTH slot since P4.D87).
2. **v5 today — `crates/quilltap-core/src/services/chat_create.rs`:**
   `handle_create` `:313`; the existing ad-hoc arms: `conciergeState`
   `:330-347`, `roleplayTemplateId` `:349-375` (its own comment records that
   ONLY the wrong-TYPE arm is measured and that v4's `z.uuid()` would refuse a
   non-UUID string the port does not — that unmeasured note is exactly what
   this lane closes), `timestampConfig` `:555-571`; all answer
   `HandleCreateError::BadRequest("Validation error")` (`:278-291`; `Display`
   is the bare message). `ChatCreateRequest` (`:150-260`) is the typed view
   over the raw map — read which fields it already types loosely (`Value` /
   `Option<Option<Value>>`) and which it would DECODE-FAIL on a wrong type
   before the handler runs (a decode failure is a dispatch error, not v4's
   400 — the P4.D57 `Option<Option<Value>>` lesson; a field that fails the
   decode must be widened to raw so the stage can refuse it with v4's
   sentence). `api/chat_create.rs` is the dispatch arm (its header names
   `handle_create`). Zod-transcription homes already in the tree: the
   `zod_uuid` gate transcribed from Zod 4's own regex (P4.62; grep `fn
   zod_uuid` for the ONE home — `api/{brahma,help_chats,files,memories,
   images}.rs` all reference it), `api/brahma.rs:131-145` `zod_min1_string` /
   `zod_uuid_string`, and the Zod 4.5 **code-point** length rule (P4.D158 §3:
   `max(N)` counts code points, not UTF-16 units — the `api/image_profiles.rs
   :655` UTF-16 count is a RECORDED wrong spelling; do not copy it).
   `z.number().int().positive()`: an integer-valued float passes `int()` in
   Zod (`1.0` is an int; `1.5` is not; `0` and negatives fail `positive()`).
3. **The family — `crates/quilltap-harness/tests/chat_create_capstone_
   equivalence.rs`:** 36 cases (`chat-create-capstone.json`; the builder
   `build-chat-create-capstone.ts`; env `QT_ORACLE_CC` + `QT_FIXTURE_CC_
   {MAIN,MOUNT,LLM}`; `TZ=UTC` pins the cron case). The refusal branch
   `:653-700`: the oracle row's `status` selects it; it compares status +
   `body.error` sentence + WRITES-NOTHING (the four MAIN tables byte-equal to
   the baked fixture) — and its comment names "the Zod `details` array" as
   the standing error-envelope deferral. Existing refusal arms:
   `cs_invalid_value_400`, `cs_null_rejected`, `cs_wrong_type_400`,
   `rt_wrong_type_400`, `rt_wrong_type_before_404`, `rt_unresolvable_id_
   rejected`, `tc_bad_value_rejected`, `tc_explicit_null_rejected`,
   `tc_wrong_type_400`. The oracle case (`chat-create-capstone.test.ts:296-
   336`) already captures `status` + `body` whole — the `details` array is
   ALREADY in every refusal row's `body`; only the Rust comparand ignores it.
4. **Guard order (measured by the existing pins):** parse BEFORE the
   continuation 404 (`rt_wrong_type_before_404`); the P4.D115 / a6870c5a
   guard-order class (doc comments claiming v4's order while the code
   inverted it) is the trap — put the stage FIRST and let one composite case
   prove it (a wrong-typed field PLUS a missing continuation chat → 400).
5. **Arms to cover (one refusing case each, each paired with its accepting
   sibling — the P4.62 shape):** `participants` missing / empty array /
   not-an-array / an element with `type: 'PERSONA'` / a non-UUID
   `characterId` / `controlledBy: 'LLM'` (finding #115's live shape) /
   non-UUID `connectionProfileId` / non-UUID `selectedSystemPromptId`;
   `title` a number; `scenarioId` non-UUID; each of the three `*ScenarioPath`
   at 501 code points (and 500 accepted — use an astral-plane string so the
   code-point rule is what passes it: 500 astral chars = 1,000 UTF-16
   units); `groupScenarioGroupId` / `projectId` / `imageProfileId` /
   `continuationFromChatId` / `progressId` non-UUID; `roleplayTemplateId` a
   non-UUID STRING (the arm the file's own comment names unmeasured);
   `outfitSelections` with a bad `mode` and with a non-UUID `characterId`;
   `avatarGenerationEnabled` a string; `chatType: 'brahma'`; `scheduleCron`
   at 121 code points; `scheduleFreshnessWindowMs` 0 / negative / `1.5` / a
   numeric string; the three budgets likewise; a `timestampConfig` inner
   field wrong-typed (the existing `tc_*` arms cover value/null/type at the
   top). Corpus 36 → ~65; every new refusal row must ALSO show the accepting
   sibling still 201 (a stage that over-refuses is the other failure mode).
6. **The `details` envelope (Tier 2):** v5 has a `CoreError.details` carry
   since P4.D138 (the `loras` write guard answered v4's Zod ENVELOPE through
   it) — read how that lane shaped the issues and reuse the home. Zod 4.5.4
   issue objects (`{code, expected?, received?, path, message, …}`; the
   `zod_version_guard` protects the version); measure each arm's bytes from
   the FRESH oracle and reproduce them; where an arm's issue shape proves
   unreproducible (e.g. a union's nested issues), record it per arm and keep
   the deferral NARROWED to those arms rather than wholesale.

## Tiered deliverables

**Tier 1 (must land):**
1. The validation stage (a private `validate_create_body` over the raw map,
   or the idiom the file already uses — `an-orders-prescribed-shape-may-
   fight-the-files-idiom`) covering every rule in point 1, placed first,
   answering `HandleCreateError::BadRequest("Validation error")`, writing
   nothing; the three ad-hoc arms subsumed (their existing cases stay green,
   their sentences unchanged).
2. Any `ChatCreateRequest` field that would decode-fail on a wrong type
   widened to a raw carry so the stage refuses it (point 2), with the
   dispatch-level proof (a wrong-typed body reaches the handler and answers
   400, not a dispatch error).
3. The corpus arms of point 5, red-first at the pin (record which arms were
   201 on v5 before the stage — that list IS finding #115's measurement),
   the family by name zero SKIP.
4. The composite guard-order case (point 4).

**Tier 2 (should land):**
5. The `details` envelope through the P4.D138 carry with the family's
   comparand widened to compare `body.details` byte-for-byte (point 6); the
   per-arm reproducibility table in the lane record; the deferral comment at
   `:653-656` rewritten to what remains.

**Tier 3 (explicit deferrals, loud):** the SPA's New-Chat dialog needs no
change (it already sends `llm`/`user`); if the stage refuses a body the SPA
sends today, that is a v5 SPA bug — STOP and report it, do not loosen the
stage.

## The differential requirement (restated)

`chat_create_capstone_equivalence` regenerated from the `f699da6f6` pin and
run by name, zero SKIP, every new arm red before the stage and green after;
mutation-proven: drop ONE rule from the stage → exactly its arm reddens (do
this for at least five rules incl. `controlledBy` and a `max(500)`); move the
stage below the continuation lookup → the composite case reddens; count
UTF-16 units in a `max()` → the astral-plane accept case reddens.

## Fixtures

- Consumes and widens: the capstone case json + builder (§A, this lane's);
  the `QT_FIXTURE_CC_*` trio is built to `/tmp` by the recipe (lane-unique
  prefix).
- Delivers: the widened corpus; no new committed `.db`.

## Shared contracts (BINDING — identical text in all five orders)

**§A Family ownership for this round's regens.** Each family below is
regenerated and edited by ONE lane. There is NO bulk neutrality sweep this
round (two substantive v4 commits, both fully delineated in the ledger); the
unifier re-runs the union of these families by name at the gate.

- **P4.D160:** `orchestrator_tier3_equivalence` (+ `harness/oracle/cases/
  orchestrator-tier3.test.ts`, `harness/oracle/fixtures/orchestrator-tier3.json`,
  `harness/oracle/fixtures/build-orchestrator-fixture.ts`, the `QT_FIXTURE_ORCH_*`
  pair), `help_tree_equivalence` (+ `harness/oracle/cases/help-tree-sync.test.ts`),
  and the `chat_events` frame pins inside `quilltap-core`.
- **P4.D161:** the SPA unit specs under `apps/web/src/app/**` and the whole
  Playwright suite (§E). No Rust family.
- **P4.D162:** `help_chat_orchestrator_tier3_equivalence` (+ `harness/oracle/
  cases/help-chat-orchestrator-tier3.test.ts`, `harness/oracle/fixtures/
  help-chat-orchestrator-tier3.json`), `request_builder_google_equivalence` (+
  its oracle case / recorded corpus rows), `request_builder_google_wire_
  equivalence` (regen-only; expected byte-identical; the recorder is run ONLY
  to APPEND rows — never re-record existing ones), and — only if the lane
  rebuilds the `help-chat-{main,mount}.db` pair — its consumers
  `help_chats_routes_equivalence`, `help_docs_routes_equivalence`, and the
  `quilltap-web` help tests, re-run by name.
- **P4.78:** `chat_create_capstone_equivalence` (+ `harness/oracle/cases/
  chat-create-capstone.test.ts`, `harness/oracle/fixtures/chat-create-capstone.json`,
  `harness/oracle/fixtures/build-chat-create-capstone.ts`, the `QT_FIXTURE_CC_*`
  trio).
- **P4.79:** `brahma_console_tier3_equivalence`, `brahma_orchestrator_tier3_
  equivalence` (+ their oracle cases / fixtures), `brahma_console_routes_
  equivalence` (consumer; regen-only).

**§B Pins.** The round's target baseline is **`f699da6f6`** (v4 `main` HEAD
at the second 2026-09-06 drift check — "docs: CHANGELOG update for 4.10";
it sits atop the 4.9.2 release squash `8fbf2afe0`, which carries
`20913d2aa`'s bug-124/125 content onto `main` byte-for-byte — measured:
`git diff 20913d2aa main` over `lib/ app/ packages/ plugins/ help/
jest.config.ts __mocks__/ __tests__/` is EMPTY). **PIN REQUIRED for every
lane** (ledger §1: the checkout sits on `bugfix` AND both branches are past
the baseline). Every lane regenerates from a LANE-UNIQUE detached worktree
(`/tmp/qt-v4-pin-<order>-f699da6f6`, the three symlink classes, ledger
§5.1) — **one pin sha for the whole round, no exceptions** (the earlier
draft's P4.D162 second pin at `20913d2aa` is RETIRED: that content is now on
`main`). Verify each pin by a marker only one tree has: a `f699da6f6`
orchestrator oracle carries `"paused":true` (absent at `c2232cd9a`); a
`f699da6f6` help-tree oracle carries `When a Turn Fails`; a `f699da6f6`
help-chat oracle carries a `toolCallId` inside a recorded slate (absent at
`c2232cd9a` and at `ba34fa367`). ⚠ Ledger §1 hazard 5: from `8fbf2afe0` on,
v4's `jest.config.ts` maps `^@google/genai$` to a manual mock — every lane
whose JEST-run oracle loads the google plugin (through `provider-registry`
or directly) must confirm its regens still match at the pin and say so in
its record; `tsx`/`node` recorders are unaffected. v4 is the human's active
repo: never switch or stash it; re-run the §2 probe before each regen batch.

**§C Sweep serialization.** No `recipe_sweep.py --run-all` this round. Lanes
run per-family `--run <family> --v4 <pin>` only, never against a sibling's §A
families or its `/tmp` staging, and one sweep invocation at a time repo-wide.
Recipes that hard-code a shared `/tmp` staging path (`/tmp/help-orch`,
`/tmp/qt-cc-*`, `/tmp/qt-help-tree-oracle`) are used under LANE-UNIQUE copies
(`/tmp/qt-d160-*`, `/tmp/qt-d162-*`, `/tmp/qt-p478-*`, `/tmp/qt-p479-*`) — the
memory notes `tmp-fixtures-collide-across-parallel-lanes` and
`tmp-fixture-clobber-by-a-sibling-lane`.

**§D Source-tree seams (who edits what shared file).**

- `crates/quilltap-core/src/services/chat_events.rs` (the `ChainCompletePayload`
  `paused` field) and `services/orchestrator.rs`: **only P4.D160**.
- `crates/quilltap-core/src/services/help_chat/**` and
  `model/request_builder/google.rs`: **only P4.D162**.
- `crates/quilltap-core/src/services/tool_call_threading.rs`: **NOBODY** — a
  read-only primitive this round; a lane that believes it needs a change
  STOPs and reports (P4.D162 CALLS it).
- `crates/quilltap-core/src/services/chat_create.rs` + `api/chat_create.rs`:
  **only P4.78**.
- `crates/quilltap-core/src/services/brahma_console/**`: **only P4.79**.
- `crates/quilltap-core/src/services/primary_stream.rs` (`StreamLogCtx`,
  `log_chat_message_call`): **NOBODY** edits; P4.79 CALLS it.
- `apps/web/**`: **only P4.D161**.
- `help/**` (the repo-root vendored tree, embedded by `quilltap-host`'s
  `build.rs`) and `docs/v4/**`: **only P4.D160**.
- `crates/quilltap-web/tests/fixtures/help-chat-*.db` (+ `.meta.json`):
  **only P4.D162**.
- `crates/quilltap-core/src/api/types.rs`, `lib.rs`, `services/mod.rs`,
  `crates/quilltap-web/src/**`, `crates/quilltap-host/src/**`,
  `crates/quilltap-tauri/**`, `crates/quilltap-cli/**`: **NOBODY**. (The
  `chainComplete` frame is a `serde_json::Value` pass-through on every
  transport — measured 2026-09-06: no `chainComplete` schema pin exists in
  `quilltap-web` or `quilltap-tauri`; a lane that finds one STOPs.)

**§E Playwright.** Port 4319 is P4.D161's for the round; it runs the suite at
its close. Every other lane authors NO Playwright and runs none; the unifier
runs the full suite at the gate. (`e2e-port-4319-is-repo-wide`.)

**§F The drift ledger.** Lanes never write `drift-ledger.md`. At ordering the
§3 rows were marked: `fef7ce4f7` → `ORDERED(p4.d160, p4.d161)`; `20913d2aa` →
`ORDERED(p4.d162)`; the six carrier/docs rows `d40497411`, `5eaf98cf1`,
`ba34fa367`, `02b77ab0f`, `8fbf2afe0`, `d489b04a3`, `f699da6f6`, `1a2b2164c`
→ `ORDERED(p4.d160)` (the docs-mirror follow-through + NO-PORT ratification
evidence live in P4.D160's lane record). `15573c3a1` (bug 119) stays
UNPROCESSED by the ledger's own instruction — it belongs to `p4.9k`.
`/unify` moves the baseline to `f699da6f6`; every row above is then an
ancestor of `main` by content (`20913d2aa` through the `8fbf2afe0` squash)
and is marked ABSORBED / NO-PORT-RATIFIED in the ordinary way. The `bugfix`
branch's own bookkeeping commits (`d40497411`, `1a2b2164c`) are ratified by
their file lists; they are not ancestors of `main` and never will be.

**§G The `chainComplete` frame contract (P4.D160 ↔ P4.D161), binding.** The
frame is `{ "chainComplete": true, "reason": <string>, "nextSpeakerId":
<string|null>, "chainDepth": <int>, "paused"?: <bool> }`. The key is spelled
`paused`, a JSON boolean, and is **present on exactly the four
`execute_turn_chain` emits** — the new paused early-return (`reason:
"paused"`, `paused: true`, `chainDepth: 0`, `nextSpeakerId: null`), the
mid-chain decision stop (`paused: decision.reason == "paused"`), the
empty-response stop (`reason: "error"`, `paused: false`), the chain-error
safety stop (`reason: "error"`, `paused: true`) — and **ABSENT** on the
fair-rotation `user_turn` emit (`orchestrator.rs` ≈`:905`, v4
`orchestrator.service.ts:1892`) and on the help-chat orchestrator's
`chainComplete` (v4 `help-chat/orchestrator.service.ts:156`), which v4 did not
touch. The server side therefore carries it as `Option<bool>` with
`skip_serializing_if = "Option::is_none"`; the client reads
`data.paused === true` and treats absent/false alike. Neither lane invents a
second key.

**§H Versions.** Each lane bumps only the crates / the SPA it touches, per
`.claude/commands/commit.md` §6; the unifier recounts as base + total bumps
(the silent auto-merge trap). Base at ordering: core 0.0.806, harness 0.0.695,
web 0.0.119, host 0.0.104, cli 0.0.18, tauri 0.0.7, SPA 0.5.651. Expected
bumpers: P4.D160 core + harness + host (the embedded help tree changes);
P4.D161 SPA only; P4.D162 core + harness; P4.78 core + harness; P4.79 core +
harness.

## Ownership (BINDING — identical table in all five orders)

| Path | Owner |
|---|---|
| `crates/quilltap-core/src/services/orchestrator.rs`, `services/chat_events.rs`; `help/**` (repo root); `docs/v4/**`; §A families for P4.D160 | P4.D160 |
| `apps/web/**` (source, specs, e2e, `package.json` version) | P4.D161 |
| `crates/quilltap-core/src/services/help_chat/**`, `model/request_builder/google.rs`; `crates/quilltap-web/tests/fixtures/help-chat-*.db(.meta.json)` + `harness/oracle/fixtures/build-help-chat-fixture.ts`; `harness/oracle/providers/**` + `harness/oracle/fixtures/request-envelopes/google-*.ndjson` (append-only); §A families for P4.D162 | P4.D162 |
| `crates/quilltap-core/src/services/chat_create.rs`, `api/chat_create.rs`; §A families for P4.78 | P4.78 |
| `crates/quilltap-core/src/services/brahma_console/**`; §A families for P4.79 | P4.79 |
| `docs/CHANGELOG.md`, `docs/developer/porting/status-log.md` | append-only, all lanes |
| `docs/developer/porting/drift-ledger.md`, `crates/quilltap-core/src/lib.rs`, `api/types.rs`, `services/mod.rs`, `services/tool_call_threading.rs`, `services/primary_stream.rs`, `crates/quilltap-web/src/**`, `crates/quilltap-host/src/**` | NOBODY |

Every other path is unowned this round: a lane that believes it must touch
one STOPS and reports (the `carryout` rule 3), naming the file and the
sibling it suspects.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D
   warnings` in BOTH feature sets; `cargo build --workspace`.
2. `cargo test --workspace` (full log + sentinel; never `| tail`) with
   `QT_ORACLE_CC` + the `QT_FIXTURE_CC_*` trio + `TZ=UTC` — the family
   confirmed RUN (grep for `SKIP:`; zero).
3. The family by name through `recipe_sweep.py --v4 <pin> --run
   chat_create_capstone_equivalence`, oracle fresh at `f699da6f6`, the new
   rows counted (`>= 36 + new`).
4. The mutation proofs above, each named in the lane record.
5. Commits per `.claude/commands/commit.md` (core + harness bumps); the lane
   record appended to `status-log.md`; this order's status header updated;
   the finding-#115 standing note in `dogfood-findings.md` is the unifier's
   to close (a lane does not edit that file's standing notes — record the
   closure in the lane record).
