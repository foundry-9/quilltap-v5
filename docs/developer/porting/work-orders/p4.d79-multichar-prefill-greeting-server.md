# Work order P4.D79 — the `aa464abf` drift, profile/host half: `multiCharacterPrefill` (bug 68), `profileParams` consolidation + `num_ctx` injection, greeting reasoning — v4 `aa464abf`

**Status:** OPEN

## Preamble

- **v4 baseline for this round: `aa464abf`** (v4 main HEAD at planning,
  2026-08-15 — "fix: archived-seat badge (66), source-view send (67),
  archive digest clobber (69)"). **Drift-check at lane start** per the
  two-branch rule (`git log aa464abf..main` AND `git diff main bugfix --
  lib/ app/ packages/ plugins/`; measure bugfix with `diff`, never the
  commit list). At planning the v4 tree was CLEAN on `main`; bugfix
  carried nothing portable (tip `3a76b17d` = the 4.8.4 marker;
  `009c49b2` = a test-only deflake). **STOP and report** if HEAD moved
  or the tree is dirty; verify the checkout is on `main` before any
  regen, else pin a detached worktree
  (`oracle-regen-pinned-v4-worktree`, or `recipe_sweep.py --v4 aa464abf`).
- Sibling lanes this round: **P4.D78** (the provider-wire half of the
  Ollama drift), **P4.D80** (bugs 66/69 convergence, server),
  **P4.D81** (the SPA riders), **P4.48** (the import-preflight
  read-error escalation). Ownership is disjoint — see Shared contract.
- Work in your own worktree on a lane branch; never `git stash` across
  lanes. Repo-root paths exist in BOTH trees — edit inside the worktree
  (`edit-inside-the-worktree-not-main`).

## Mandate

Absorb the **host/profile half** of the drift: v4 `23af7146` whole
("fix: per-profile multi-character [Name] prefill (bug 68), greeting
reasoning") plus the host half of `d9c5a1c7` (the `profileParams`
widening + the eight call-site conversions). Deliverables: the
`connection_profiles.multiCharacterPrefill` column through the D23
re-dump + a boot ensure carrying the MIGRATION's backfill semantics,
the resolver module twin, the per-profile multi-character turn anchor,
the create/update route arms, the export/import field carry, the
greeting `reasoningContent` capture persisted onto the first message,
and the `profile_params` consolidation (which also FIXES three
measured v5 divergences). The lane must NOT touch `model/**` or
`provider_manifest/**` (P4.D78), `chat_enrichment.rs` or
`character_archive/**` (P4.D80), `qtap_import/**` (P4.48 — see the
tripwire in Shared contract), or `apps/web/**` (P4.D81).

## The drift, classified (surveyed FRESH 2026-08-15 at `aa464abf`)

`23af7146`, 30 files, +1,103/−52, **plus** the `lib/` host hunks of
`d9c5a1c7`. From the diffs:

- **The column.** `connection_profiles.multiCharacterPrefill INTEGER
  DEFAULT 1` — in BOTH v4 shapes: `sqlite-initial-schema.ts` (the
  generateDDL side → our D23 re-dump) and migration
  `add-profile-multi-character-prefill-field-v1` (ALTER ADD COLUMN
  DEFAULT 1, then `UPDATE … SET 0 WHERE upper(provider)='ANTHROPIC'`,
  then `UPDATE … SET 1 WHERE … IS NULL`). ⚠ **The backfill runs ONCE,
  when the column is added** — a boot ensure that re-ran the Anthropic
  UPDATE every boot would clobber a user's explicit `true` on an
  Anthropic profile. v5's ensure must be: column absent → add +
  backfill; column present → no-op. (The uniform ensure precedent:
  `db/chat_settings_composer_repair.rs:72`.)
- **The resolver** (`lib/llm/multi-character-prefill.ts`, new, 61 l):
  `PREFILL_HOSTILE_PROVIDERS = {'ANTHROPIC'}`;
  `defaultMultiCharacterPrefill(provider)` — `!provider → true`, else
  `!hostile.has(provider.toUpperCase())`;
  `profileUsesNamePrefill(profile)` — an explicit stored boolean wins,
  null/absent falls back to the provider default. The column is
  **tri-state on purpose**: NULL = "never chosen" (pre-migration row,
  or a profile imported from a pre-4.9 bundle) so an old Anthropic
  export cannot come back with the prefill on. Never read the field
  directly — the resolver is the single source of truth.
- **The turn anchor** (`context-builder.service.ts`): the provider
  branch is replaced by `applyMultiCharacterTurnAnchor(
  formattedMessages, characterName, usePrefill)` — prefill route
  pushes the assistant `[Name]` message (thoughtSignature/name
  explicitly undefined), prose route appends v4's UNCHANGED
  instruction sentence to the system message (byte-identical to the
  old Anthropic branch). Call site:
  `applyMultiCharacterTurnAnchor(…, character.name,
  profileUsesNamePrefill(connectionProfile))`.
- **Routes.** Create (`connection-profiles/route.ts`): field optional;
  present non-boolean → 400 `multiCharacterPrefill must be a boolean`;
  absent → `defaultMultiCharacterPrefill(provider)` resolved and
  STORED (so create never writes NULL); the courier create carries it
  too (v4 comment: "Not a tool flag — the Courier renders the same
  assembled context"). Update (`[id]/route.ts`): `!== undefined` +
  non-boolean → the same 400; boolean → `updateData`.
- **Export schema carry**: `qtap-export.schema.json` gains the field
  (`["boolean","null"]`) — v5's analog is
  `qtap_export/schema-key-order.json` (regen, see Fixtures) and the
  profile record stream. **Measure v4's import write for an ABSENT
  field before porting it** — the migration comment says a pre-4.9
  bundle yields NULL, but the DDL default is 1; confirm with a
  real-DB jest probe (`jest-real-db-oracle`) whether v4's import
  INSERT explicitly writes NULL — do not trust the comment
  (`evidence-conditional-rulings`).
- **Greeting reasoning** (`initial-greeting.ts` +
  `app/api/v1/chats/route.ts`): `GreetingResult` gains
  `reasoningContent` — accumulated by ASSIGNMENT, not concatenation
  (providers emit `reasoningContent` CUMULATIVELY), trimmed alongside
  content. `autoGenerateFirstMessage` returns `GeneratedGreeting =
  {content, reasoningContent}` (a `NO_GREETING` constant for all four
  give-up paths) and carries reasoning through ALL FOUR attempts
  (initial, no-memories retry, the Concierge uncensored reroute, the
  final retry). The stored greeting message gains
  `reasoningContent: firstMessageReasoning || null` — DISPLAY ONLY;
  a scripted first message never touched a model, so its reasoning is
  always empty.
- **The `profileParams` widening** (`d9c5a1c7`, host half):
  signature takes `provider` + `parameters` + `maxContext`; base = the
  JS `params && typeof params === 'object'` check (arrays PASS); when
  `provider === 'OLLAMA'` and `maxContext` is a number > 0 and
  `base?.num_ctx == null` → return `{...(base ?? {}), num_ctx:
  maxContext}`. ⚠ Faithful edge: spreading an ARRAY base produces an
  object with index keys — pin it in the tier-1 differential.
  Call sites converted to the shared helper in v4:
  `announcer/character-voiced.ts:62`,
  `answer-confirmation.service.ts:398`, `orchestrator.service.ts:1031`
  (`modelParams = profileParams(effectiveProfile) ?? {}`),
  `regenerate-swipe.service.ts:131`,
  `memory/cheap-llm-tasks/core-execution.ts:295` (uncensored
  fallback), `wardrobe/image-analysis.ts:306` (NO v5 twin — typed
  refusal, record NO-PORT), `chat/file-attachment-fallback.ts:233`
  (`?? {}`), `app/api/v1/chats/route.ts:690` (the greeting site).
- The other three commits are siblings' or dispositions: `aa464abf` →
  P4.D80/P4.D81; `54fdfb43`/`55d63022`/`315e51d2` → P4.D80's
  disposition rows.
- **Help-doc deltas ride the bank:** `help/connection-profiles.md`
  (+4 from `d9c5a1c7`, +17 from `23af7146`) and
  `help/chat-multi-character.md` (+8) go VERBATIM to the `p4.9i2`
  bank, not into this lane.

## Survey-verified v5 starting points (2026-08-15)

- `profile_params`: `crates/quilltap-core/src/cheap_llm.rs:100` —
  currently `fn profile_params(profile: &CheapLlmProfile) ->
  Option<Value>` with the Object|Array match. Internal uses at
  `:124,181,196,260` and `services/cheap_llm_exec.rs:119,435`.
- **The three measured v5 divergences this lane FIXES by
  consolidation** (found in the 2026-08-15 survey):
  `services/regenerate_swipe.rs:362,424` takes `parameters`
  unconditionally defaulting `json!({})` (never `None`);
  `services/file_fallback.rs:393,436` is `is_object()`-only (arrays
  dropped); `services/chat_create.rs:1540,1573` filters
  `is_object()` (arrays dropped). Inlined-but-faithful copies to
  consolidate: `services/announcer/character_voiced.rs:198`,
  `services/answer_confirmation.rs:697`. The orchestrator's
  `modelParams` twin needs locating in `services/orchestrator.rs`
  (v4 `:1031`).
- The turn anchor: `services/message_context.rs` §M at `:830-846`
  (`anthropic_scene_instruction` at `:524`; the prefill push at
  `:838-846`). Grep for "scene block", not "prefill".
- Greeting: `services/initial_greeting.rs:140`
  (`generate_greeting_message`); the ladder
  `services/chat_create.rs:1380` (`auto_generate_first_message`),
  greeting calls at `:1596,1614,1653`, the reroute at `:1667/:1751`.
- Profile verbs (dispatch-only — v5 has no `/api/v1/
  connection-profiles` REST edge, a recorded asymmetry):
  `api/types.rs:128-161`; engine arms `api/engine.rs:793-826`;
  handlers `api/settings.rs:1192` (list), `:1232` (create), `:1490`
  (update); bag parsing `settings.rs:1371,1378-1420` (create),
  `:1516-1533,1617-1621,1688-1691` (update).
- Structs: `db/connection_profiles.rs` — `CpCreate:74`
  (`parameters:89`), `CpUpdate:128` (`parameters:143`), marshal
  `create_return_shape:563`, `find_by_id:623`. ⚠ `CpCreate` is ALSO
  restore's deserialize (`p4.d73-composer-settings-columns-lane`):
  the new field must be `Option<bool>` there so pre-4.9 archives
  deserialize (to NULL, pending the import-write measurement above).
  ⚠ The `parameters` blob is documented as CONSTRAINED to `{}` or a
  single-key object (`connection_profiles.rs:42-49,65`) — the SPA
  will now write multi-key bags (`enable_thinking` beside
  `temperature`); see Tier 2.
- D23 machinery: `services/provisioning/fresh_schema.json`
  (embedded `provisioning/mod.rs:70`), regen
  `harness/oracle/provision/dump-fresh-schema.ts`; the tripwire
  `provisioning_equivalence.rs:237`
  (`provisioning_matches_v4_fresh_instance`) — it goes red FIRST at
  the re-dump, by design; never "fix" v5 back.
- Boot-ensure precedents: `db/chat_settings_composer_repair.rs:72`
  (ALTER template `:83`), wired from `quilltap-host/src/host.rs:889`
  (siblings at `:867,879,903`).
- Export: `qtap_export/mod.rs:94` (`"connection-profiles"` in
  `EXPORT_ENTITY_TYPES`), record stream
  `qtap_export/records.rs:345-360` (`sanitize_profile:358`);
  `qtap_export/schema-key-order.json:283-313` (`connection_profile`,
  29 keys) with regen recipe
  `harness/oracle/fixtures/dump-export-key-order.ts`
  (`key_order.rs:19-21`) — the D65 lesson: this file STALES on D23
  re-dumps; regenerate, never hand-append.
- Differential families: `settings_routes_equivalence.rs` (family
  tag `connection_profiles_routes`, per-row error-status asserts per
  the help-drift lesson), `connection_profiles_tier2_equivalence.rs:178`
  (`QT_ORACLE_CONNECTION_PROFILES`),
  `orchestrator_tier3_equivalence.rs` (the prefill case id is
  `nonanthropic_scene`), `initial_greeting_equivalence.rs:106`
  (`QT_ORACLE_GREETING`), `chat_create_capstone_equivalence.rs:313`,
  `message_context_leaves_equivalence.rs:131`.

## Deliverables

### Tier 1 — must land

1. **The D23 re-dump**: `dump-fresh-schema.ts` at the `aa464abf` pin;
   `provisioning_matches_v4_fresh_instance` back to green over the new
   schema. Then the **boot ensure** (new
   `db/connection_profiles_prefill_repair.rs` on the chat-settings
   precedent + one wiring call in `quilltap-host/src/host.rs`):
   column-absent → ALTER ADD COLUMN `INTEGER DEFAULT 1` + the
   Anthropic-off + NULL-to-1 backfills, column-present → no-op;
   idempotence and the no-reclobber property unit-proven (an explicit
   0/1 on an Anthropic row survives a second boot).
2. **The resolver twin** (new
   `services/multi_character_prefill.rs`): both functions, tri-state
   semantics; tier-1 differential (new family, e.g.
   `multi_character_prefill_equivalence`) driving v4's REAL module
   over a providers × {true,false,null,absent} table.
3. **The turn anchor**: `message_context.rs` §M rewritten to the
   `apply_multi_character_turn_anchor` shape (prose sentence
   byte-unchanged); resolution via the resolver over the chat's
   connection profile. `orchestrator_tier3` grows cases: explicit
   `false` on a non-Anthropic profile → prose; explicit `true` on an
   ANTHROPIC profile → prefill; NULL both ways (provider default).
   Mutation-prove at least one (flip the resolver, watch it go red).
4. **Struct + route arms**: `CpCreate.multi_character_prefill:
   Option<bool>` (restore-tolerant), `CpUpdate` with the
   double-Option carry per Contract B; create resolves the default
   when absent (never stores NULL on create; courier create carries
   it); update 400s on present-null/non-boolean with v4's exact
   sentence; list/read shapes carry the stored value verbatim.
   `settings_routes_equivalence` grows create/update/read arms
   including the 400s (per-row error-status asserts);
   `connection_profiles_tier2` grows the column.
5. **Export/import carry**: `sanitize_profile` + the record stream
   carry the field; `schema-key-order.json` REGENERATED via
   `dump-export-key-order.ts` at the pin; the import/restore leg via
   the `CpCreate` deserialize (measured against v4's real import
   write per the classification note — if the measurement demands an
   edit inside `qtap_import/**`, STOP that unit and record it for the
   unifier; P4.48 owns those files).
6. **Greeting reasoning**: `GreetingResult` gains reasoning
   (assignment-accumulated); `auto_generate_first_message` →
   `GeneratedGreeting` through all four attempts + the reroute; the
   stored first message carries `reasoningContent` (`|| null`).
   `initial_greeting_equivalence` grows a reasoning-carrying mocked
   stream case; `chat_create_capstone` grows an arm asserting the
   persisted greeting row's `reasoningContent` (and that a scripted
   first message stores NULL).
7. **The `profile_params` consolidation**: widen
   `cheap_llm.rs::profile_params` per Contract A (owning the exact
   Rust signature is this lane's call; the SEMANTICS are the
   contract), convert every v5 site listed in the survey — fixing
   the three divergent shapes — and pin the widened function with a
   tier-1 differential vs v4's REAL `profileParams` (cases: object,
   array, null, non-object, OLLAMA + maxContext permutations incl.
   the explicit-num_ctx win and the array-spread edge). The wardrobe
   image-analysis site: NO-PORT note (typed refusal stands).

### Tier 2 — should land

8. A multi-key `parameters` routes case (the single-key constraint at
   `connection_profiles.rs:42-49` predates the SPA writing
   `enable_thinking` beside `temperature`): measure v4's key-order on
   a multi-key update round-trip and either close the seam (expected:
   `preserve_order` already matches JSON.stringify insertion order —
   `llm-logs-key-order-and-preserve-order`) or keep the deferral with
   the measurement recorded.
9. The stale doc-comment sweep: update `connection_profiles.rs`'s
   module doc for the new column + any constraint change.

### Tier 3 — explicit loud deferrals

- The SPA checkbox + TS default twin: P4.D81's (Contract B).
- The Enable Thinking UI: P4.D81's; the wire read: P4.D78's.
- v5 still ships no `/api/v1/connection-profiles` REST edge — the
  standing asymmetry stands; do not add one in this lane (nothing in
  the drift requires it; the dispatch verbs are the ported surface).

## The differential requirement, restated

Every unit ships with an equivalence test driving v4's REAL code:
the resolver + `profile_params` tier-1 families (tsx oracle, exact),
the routes/tier-2 families over regenerated oracles, the
orchestrator/greeting/capstone tier-3 arms (mocked-LLM,
`tier3-completion-oracle`), and the provisioning tripwire. Recipes in
each family's `//!` header, runnable (`harness-recipes-are-runnable`);
regen ONLY against the pinned `aa464abf` worktree; grep NDJSONs for a
fresh marker (`oracle-regen-silent-stale-pass`). `TZ=UTC` on both
stages where the recipe says so (`build_context_tier3` neighbors).

## Fixtures

- Regenerates: `fresh_schema.json` (D23),
  `qtap_export/schema-key-order.json` (via the shipped dumper), the
  settings-routes fixture if its builder needs the column
  (`build-settings-fixture.ts`), the lane's oracle NDJSONs.
- Consumes: `connection-profiles-tier2` fixtures, the capstone
  fixture set — extend, don't fork; keep sibling cases meaningful
  (the P4.D49 fixture-rework precedent).
- Owns regenerating every oracle downstream of a fixture it touches.

## Shared contract (BINDING — identical across P4.D78, P4.D79, P4.D80, P4.D81, P4.48)

**Contract A — the profile-parameters bag keys (D78 ↔ D79 ↔ D81).**
- Key **`num_ctx`**: INJECTED host-side by P4.D79's widened
  `profile_params` (OLLAMA profiles only, from `maxContext` when it is
  a finite number > 0; an explicit non-null `num_ctx` already in the
  stored blob wins). READ wire-side by P4.D78's `build_ollama_body`:
  number|string coerced (`Number`), finite and > 0, floored →
  `options.num_ctx`; anything else leaves the key off the wire.
- Key **`enable_thinking`**: WRITTEN by the SPA checkbox (P4.D81) as
  a boolean into the profile `parameters` blob; READ wire-side
  (P4.D78) as `value === true || value === "true"` → the top-level
  `think` request field (always present, `false` when off).
- P4.D78 never touches `cheap_llm.rs`, `services/**`, `db/**`,
  `api/**`; P4.D79 never touches `model/**` or `provider_manifest/**`.

**Contract B — the `multiCharacterPrefill` wire (D79 ↔ D81).**
- Read shapes: `multiCharacterPrefill?: boolean | null` on the
  connection-profile DTO (stored value verbatim; `null` = never
  chosen).
- Create bag: field optional; absent → the server stores
  `defaultMultiCharacterPrefill(provider)` (false iff
  `provider.toUpperCase() == "ANTHROPIC"`, true otherwise, true when
  provider is absent); present non-boolean (INCLUDING explicit
  `null`) → 400 with body message exactly
  `multiCharacterPrefill must be a boolean`.
- Update bag: absent → unchanged; boolean → stored; present `null`
  or non-boolean → the same 400. Server-side this REQUIRES the
  double-Option carry (`Option<Option<…>>`, the P4.D57 shape) so an
  explicit null cannot collapse to absent at the web edge (the Taboo
  §3 lesson).
- The SPA's TS twin `defaultMultiCharacterPrefill` has parity-spec
  semantics identical to v4's `lib/llm/multi-character-prefill.ts`.

**Contract C — chat-GET `archivedAt` (D80 ↔ D81).**
- The chat GET (and the chat PUT re-enrichment) character-detail
  projection gains `archivedAt: string | null` (wire key
  `archivedAt`), on BOTH `get_character_detail` return paths (main +
  avatar-override). The SPA contract field already exists
  (`core-contract.ts:2607-2612`).
- The archived-badge e2e beat flip lands in P4.D81 gated behind
  `P4D80_ENRICHMENT_LANDED = false`, flipped at unification; the
  unifier RUNS the beat at first activation (the gated-beat
  first-run rot class).

**Ownership (no two lanes edit the same source file).**
- P4.D78: `crates/quilltap-core/src/model/**`,
  `crates/quilltap-core/src/provider_manifest/**`,
  `harness/oracle/providers/**`,
  `harness/oracle/fixtures/streams/**`, its harness test files
  (stream/request/registry/listing/think-parser families).
- P4.D79: `crates/quilltap-core/src/cheap_llm.rs`,
  `db/connection_profiles.rs`, `api/settings.rs`, these
  `services/` files: `message_context.rs`, `initial_greeting.rs`,
  `chat_create.rs`, `regenerate_swipe.rs`, `file_fallback.rs`,
  `announcer/character_voiced.rs`, `answer_confirmation.rs`,
  `orchestrator.rs`, `cheap_llm_exec.rs`, a new
  `multi_character_prefill.rs`, `provisioning/**` (fresh_schema
  re-dump), `qtap_export/**` (schema-key-order regen), a new
  `db/` ensure-repair module + its one wiring call in
  `quilltap-host/src/host.rs`; its harness families.
- P4.D80: `services/chat_enrichment.rs`,
  `services/character_archive/service.rs`; its harness arms.
- P4.D81: `apps/web/**` ONLY.
- P4.48: `services/qtap_import/**` (mod.rs, entities.rs,
  preview.rs); its harness arms. **Tripwire:** P4.D79's import/restore
  carry of the new column rides the `CpCreate` deserialize
  (`p4.d73-composer-settings-columns-lane` — a `*Create` struct is
  ALSO restore's deserialize); if measurement shows P4.D79 needs an
  edit inside `qtap_import/**`, it STOPS that unit and records the
  ordered edit for the unifier instead of touching P4.48's files.
- `CHANGELOG.md` + `docs/developer/porting/status-log.md`:
  append-only for everyone.
- Version bumps: each lane bumps the crates it touches per
  `.claude/commands/commit.md`; the unifier accumulates (a
  `Cargo.toml` conflict is not version-only — audit deps).

## Verification gate (before declaring done)

- `cargo fmt --check`; clippy BOTH feature sets;
  `cargo build --release`.
- Full workspace tests with this lane's oracle env vars; the lane's
  families by name, zero SKIP, over oracles regenerated fresh at the
  `aa464abf` pin (positively confirm each RAN —
  `cargo-test-workspace-is-fail-fast`).
- The provisioning tripwire green AFTER the re-dump; the boot-ensure
  no-reclobber unit test green.
- No SPA changes → confirm `apps/web` untouched in the diff.
- Spelling guard rides the harness.
- Per-commit rule: follow `.claude/commands/commit.md`.
