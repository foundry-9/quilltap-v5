# Work order P4.6av — the homepage SPA lane (lane B): the Home screen at `/`

> **STATUS: LANE COMPLETE (2026-07-16) — awaiting unification.** Tiers 1 AND 2
> both landed (branch `claude/charming-tereshkova-9e4b15`; the lane record is
> in the status log). The home Playwright beat is PROBE-GUARDED
> **ACTIVATE-AT-UNIFY** (record-and-fallback on the `systemHome` dispatch — it
> proves the error arm and skips in-lane, self-activating once lane A's verb
> merges). Recorded divergences: the card Chat action navigates to
> `/salon/new?characterId=` (v4's `NewChatModal`-on-card banked for the M6
> screen-parity review); the whole card is clickable per the finding-#4
> pattern. Tier-3 deferrals as ordered: the **Generate Image** quick action
> OMITTED (`/generate-image` / v4 `GenerateImageView` → the M6 parity pool);
> the tabbed workspace unowed. Additionally deferred loud: quick-hide
> filtering on the home sections (v5 has no quick-hide provider — the standing
> salon-list deferral; the dangerous-chat `*` marker IS ported). Survey
> correction: v4's `QuickActionsRow` has FIVE actions, not four — the fifth,
> **New Project**, is ported (reusing the Prospero `ProjectCreateDialog`).
> Note for the unifier: 16 existing e2e specs' `/`-entries moved to the Salon
> (14 literal `goto('/')` sweeps, salon-scroll's template-literal root,
> setup-flow's Home-nav click → the Chats rail item — the root now mounts
> Home); the `systemHome` request type awaits its fold into `CoreRequest`
> (`home.api.ts` carries the cast + note).
>
> Lane B of the THREE-lane P4.6au ∥ P4.6av ∥ P4.7c round (the homepage +
> Tauri one-origin round, planned 2026-07-16). **v4 reference baseline: HEAD
> `02865bdb`** (`~/source/quilltap-server`) — run
> `git log 02865bdb..HEAD --oneline` at lane start; **if HEAD moved, STOP and
> report.** Sibling lanes: `p4.6au-homepage-server.md` (provides §1 of the
> Shared contract — this lane MOCKS the `systemHome` payload against §1
> until unification; do not depend on lane A's branch),
> `p4.7c-tauri-one-origin.md` (bound to this lane only by §2). The **Shared
> contract** and **Ownership** sections below are binding and reproduced
> verbatim in all three orders. You run in a worktree (own Rust `target/`
> and its own Rust binaries for the e2e server — the P4.6af memory); the
> unification pass integrates the lanes onto main.

## Mandate

Deliver the v5 Home screen: the v4 homepage (`components/homepage/*`,
~890 lines across 10 components) as an Angular screen family mounted at
`/` — welcome greeting, quick-actions row, and the three-column grid
(Recent Chats / Projects / Characters) — fed by §1's `systemHome` payload,
plus the root-route and shell-nav rewires. This lane owns
`apps/web/src/app/screens/home/**`, `app.routes.ts`, and `shell/shell.ts`;
it must NOT touch `crates/**` or any sibling-owned file. The screen keeps
v4's `qt-*` class vocabulary and the Wodehouse register for any new
user-facing string (there should be almost none — v4's strings carry over
verbatim).

## Survey-verified starting points (2026-07-16 @ `02865bdb` — do not re-derive)

### The v4 components (`~/source/quilltap-server/components/homepage/`)

| File | Lines | Notes |
|---|---|---|
| `HomeView.tsx` | 54 | `qt-homepage-container` → Welcome + QuickActions + `qt-homepage-grid` (3 sections). Purely props-driven. |
| `HomeViewContainer.tsx` | 55 | The client-fetch wrapper (workspace tab) — v5's screen replaces it with its own `systemHome` fetch; structure only, don't port the workspace glue. |
| `WelcomeSection.tsx` | 20 | "Welcome back, `{displayName}`!" / "What would you like to do today?" — `qt-heading-1`, `text-primary`. |
| `QuickActionsRow.tsx` | 109 | Four actions: **New Chat** → `/salon/new`; **New Autonomous Room** → `/salon/new?autonomous=1` (title tooltip carried); **Continue Last** → `/salon/{lastChatId}` (disabled "No recent chats" state when null); **Generate Image** → `/generate-image` (UNPORTED target — see tier 3). |
| `RecentChatsSection.tsx` / `RecentChatItem.tsx` | 64/76 | Read v4 for the display count, quick-hide filtering, the story-background-instead-of-avatars branch, and the dangerous-chat marker. |
| `ProjectsSection.tsx` / `ProjectItem.tsx` | 34/55 | Color/icon chip, chat count, relative last-activity. |
| `CharactersSection.tsx` / `CharacterCard.tsx` | 104/92 | Card links to `/aurora/{id}/view` (v5: `/characters/:id`). Whole-card click semantics per dogfood finding #4 — v4's card ignores clicks landing on inner buttons/links (`closest('button')`/`closest('a')` guard); replicate the P4.6f fix pattern, don't reintroduce the bug. The **Chat** button opens v4's `NewChatModal` preloaded with the character — see the tier-1 decision below. `ProviderModelBadge` via `defaultConnectionProfileId` — see tier 2. |
| `types.ts` / `index.ts` | — | The DTO shapes are §1 verbatim. |

CSS: `qt-homepage-container` (flex-fill viewport layout) and
`qt-homepage-grid` come from v4's theme layer — check the already-ported
`qt-*` stylesheet in `apps/web` before writing new CSS; port the missing
rules from v4's `app/globals.css`/`styles/` next to the other ported
`qt-*` blocks.

### The v5 targets

- `apps/web/src/app/app.routes.ts:94` — `{ path: '', redirectTo: 'salon' }`
  becomes the Home screen route (lazy `loadComponent`, matching the file's
  idiom). **Leave the `'**'` wildcard → salon as-is** (existing documented
  divergence). Update the file's doc comment.
- `apps/web/src/app/shell/shell.ts:76` — the collapsed-nav "Home" button
  currently points at `/salon`; repoint to `/`. Check the expanded nav item
  list (`shell.ts:29–58`) for whether v4's sidebar has a Home entry and
  match it.
- New-chat preselection already exists:
  `screens/new-chat/new-chat-page.ts:101–109` reads `?characterId=` and
  `?autonomous=1`. Precedent for navigating instead of modal-ing:
  `screens/prospero/cards/project-header.ts` ("New Chat" link into
  `/salon/new?projectId=`).
- Shared helpers this lane MUST use (never inline): the avatar stack
  (`apps/web/src/app/ui/avatar-stack.ts`) for participant avatars and its
  URL normalization; `apiUrl()` (`core/api-url.ts`) for any raw path (§2).
- The e2e locked instance already seeds Lorian + Riya + chats (P4.4u4 +
  the salon fixture) — the home walk needs no new fixture; remember
  global-setup copies FRESH each run (`e2e-fixture-before-run-order`
  memory) and never assert absolute counts a sibling spec can grow
  (P4.6ap memory).

## Tiered deliverables

### Tier 1 — must land

1. **The Home screen** (`screens/home/`): welcome section, quick-actions
   row, and the three sections, v4-faithful markup/classes, fed by ONE
   `systemHome` fetch through the established query/service pattern the
   other screens use. Loading/empty states per v4 (read
   `HomeViewContainer.tsx` for the fetch-side states).
2. **Routing + nav:** `/` mounts the screen; the shell Home affordance
   points at `/`.
3. **The character-card Chat action:** navigate to
   `/salon/new?characterId={id}` (the established v5 New-Chat-page
   precedent, P4.6q — a DOCUMENTED divergence from v4's `NewChatModal`;
   record it in the status header at close). Whole-card click per the
   finding-#4 pattern, with the unit test proving
   navigate-from-body / no-navigate-from-inner-control.
4. **Unit tests** (`ng test`) for: the sort/slice-blind rendering of §1
   data, the quick-action states (`lastChatId` null branch), the card
   click guard, and the empty-sections arms.
5. **A live Playwright home beat** in this lane's OWN new spec file:
   boot → unlock → `/` renders the greeting + at least one populated
   section over the seeded instance → a quick action navigates to
   `/salon/new` → a character card body-click lands on
   `/characters/:id`. Until unification the `systemHome` payload does not
   exist on main's server — write the beat against the §1 contract as
   record-and-fallback (the P4.6ar round memory: payload-asserting beats
   activate at unification), and mark it ACTIVATE-AT-UNIFY for the unifier.

### Tier 2 — should land

6. The provider badge on character cards (v4 `ProviderModelBadge` +
   `useConnectionProfiles`): reuse whatever badge/profile plumbing the
   ported Salon/characters screens already have; if none exists, defer
   loudly (tier 3) rather than half-porting the hook.
7. The story-background-instead-of-avatars branch on recent-chat items
   (v4 `RecentChatItem` — `storyBackgroundUrl` present ⇒ background strip
   replaces the avatar stack), through the §2 helpers.

### Tier 3 — explicit deferrals (loud, named)

- **The Generate Image quick action** — its target `/generate-image`
  (v4 `GenerateImageView`) is an unported screen. OMIT the button (no dead
  links, no stubs) and name the `/generate-image` screen in the status
  header as an M6-parity pool item.
- **v4's `NewChatModal`-on-card** (superseded by the tier-1 navigation
  divergence) — bank for the M6 screen-parity review.
- **The tabbed workspace** (`HomeViewContainer`'s reason to exist) —
  unported family; nothing owed here.

## The differential requirement, restated

The SPA has no v4 oracle (tier 4, per the phase-4 verification strategy):
the proof is `ng test` + the live Playwright walk over the real server, and
§1 payload fidelity is lane A's differential. Where this lane transcribes
v4 logic (the card click guard, the quick-action states), each transcription
gets a unit spec that pins the v4 behavior it copied, with the v4
file/line in a comment.

## Fixtures

- **Consumes:** the committed e2e locked instance (seeded sample content —
  Lorian + Riya + chats). No fixture changes; no oracle regeneration owed.
- **Delivers:** none.

## Shared contract (BINDING — identical in all three orders)

### §1 — the `systemHome` surface (lane A provides; lane B consumes)

- **Dispatch verb:** wire name `systemHome`, Rust `Request::SystemHome`
  (no parameters — single-user; the engine supplies the user scope as the
  other system verbs do).
- **REST edge:** `GET /api/v1/system/home` → v4 `successResponse` envelope
  around the payload; on internal failure v4's
  `serverError('Failed to load the home dashboard')` body.
- **Payload** (v4 `HomeData`; field names verbatim, omit-vs-null exactly as
  written):

```ts
{
  displayName: string,
  lastChatId: string | null,
  recentChats: Array<{
    id: string, title: string, updatedAt: string,
    lastMessageAt: string | null,
    isDangerousChat?: boolean,
    storyBackgroundUrl?: string | null,
    participants: Array<{
      id: string, type: 'CHARACTER', isActive: boolean, displayOrder: number,
      character?: {
        id: string, name: string,
        defaultImageId?: string,
        defaultImage?: { id: string, filepath: string, url?: string } | null,
        tags?: string[]
      } | null
    }>,
    _count: { messages: number }
  }>,                                   // max 12
  projects: Array<{
    id: string, name: string,
    description?: string | null, color?: string | null, icon?: string | null,
    chatCount: number, lastActivity: string
  }>,                                   // max 12, activity-sorted
  characters: Array<{
    id: string, name: string, title?: string | null,
    defaultImageId: string | null,
    defaultImage: { id: string, filepath: string, url?: string | null } | null,
    tags?: string[],
    isFavorite: boolean, npc: boolean, chatCount: number,
    defaultConnectionProfileId?: string | null
  }>                                    // max 24, favorites→chatCount→name
}
```

- `defaultImage.filepath` / `storyBackgroundUrl` are **server-relative
  paths, v4-faithful** — do NOT absolutize server-side (dogfood finding
  #12; lane C fixes resolution at the client origin).

### §2 — the URL-resolution freeze (lane B ↔ lane C)

- Lane B routes every server-supplied image/file path through the EXISTING
  shared helpers (`apps/web/src/app/ui/avatar-stack.ts` normalization,
  `apps/web/src/app/core/api-url.ts` `apiUrl()`) — no inline URL building
  in the new home components.
- Lane C may change the RUNTIME resolution of those helpers under the Tauri
  origin but must NOT change any exported helper signature
  (`apiUrl(path: string): string` stays) and must not edit lane B's files.

## Ownership (BINDING — identical in all three orders)

- **P4.6au:** `crates/quilltap-core/**`, `crates/quilltap-web/**`,
  `crates/quilltap-harness/**`, `harness/oracle/**`, the new fixture family
  + generator. Version bumps: quilltap-core, quilltap-harness, quilltap-web
  (+ Cargo.lock).
- **P4.6av (this lane):** `apps/web/src/app/screens/home/**` (new),
  `apps/web/src/app/app.routes.ts`, `apps/web/src/app/shell/shell.ts`, its
  own new e2e spec file. Version bump: the SPA `package.json`.
- **P4.7c:** `crates/quilltap-tauri/**` (incl.
  `tauri.conf.json`), `apps/web/src/app/core/tauri-api.ts`,
  `apps/web/src/app/core/core-transport-factory.ts`; ONLY IF the fallback
  path (b) is taken: `apps/web/src/app/core/api-url.ts`,
  `apps/web/src/app/ui/avatar-stack.ts`,
  `apps/web/src/app/chat/render/markdown-renderer.ts`. Version bumps:
  quilltap-tauri (+ the SPA IF apps/web files change — SPA bumps union at
  unify, resolve HEAD+1 per the P4.6ar round memory).

  **Note the constraint this ownership implies:** if the one-origin
  adoption turns out to require edits to `crates/quilltap-web` (e.g. the
  embedded-router static-serving configuration), STOP and report before
  editing — quilltap-web belongs to lane A this round; the unifier can
  take a handoff patch, but two lanes must not both edit the crate.
- **Append-only for everyone:** `docs/CHANGELOG.md`,
  `docs/developer/porting/status-log.md` (union-merge at unification).
- **Nobody** edits `CLAUDE.md`, `docs/developer/porting/phase-4.md`, or a
  sibling lane's files; the unifier updates the phase docs.

## Verification gate (run before declaring done)

- `ng test` green (run via `ng test`, not bare vitest — P4.6e memory);
  `ng build` clean.
- The full Playwright suite green, run ALONE on port 4319 (the 4319
  contention + local playwright bin — P4.6af memory), from THIS worktree's
  own Rust server binaries; the new home beat green in record-and-fallback
  mode, tagged ACTIVATE-AT-UNIFY.
- No `[value]` binding on any `<select>` whose options load async — bind
  `[selected]` per option (the standing dogfood-#6 rule).
- Every commit follows `.claude/commands/commit.md` (SPA version bump on
  SPA-touching commits; CHANGELOG entry).
