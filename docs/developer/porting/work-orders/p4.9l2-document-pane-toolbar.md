# Work order P4.9L2 — the DocumentPane formatting toolbar (m6 §4 row 14b) — v4 `c8a3cf77`

**Status:** CLOSED (2026-08-20, branch
`claude/document-pane-toolbar-port-0b3088`; unified same day). All tier-1
items landed: the toolbar in its `.qt-doc-toolbar` row mirroring v4's
`DocToolbar` 1:1 (NO `narrationDelimiters` binding, asserted directly on the
mounted instance), the source branch over THIS pane's textarea through the
pane's own `onEditorInput` (frontmatter `rawBlock` recombine held), the ONE
`toggleSourceMode()` behind both controls with the leave-flush (v5 flushes by
emitting `blur` — v4's `onFlushSave` IS its `onBlur` at both call sites), and
the template threaded from the Salon as RESOLVED delimiters (the recorded
P4.9L divergence — the Salon already fetches the row; the standalone view
passes nothing, as v4 does). Tier 2: both live beats landed and green (the
Salon beat proves the delimiter press reaches the raw bytes through the
toolbar's own source toggle; the standalone beat proves zero delimiter
buttons with live markdown buttons); m6 row 14b struck; the GAP paragraph
retired. Tier 3: none, as predicted. Specs: 12 cases against the P4.9L
recorder's three vectors; six mutations red — one (the header-bypass) went
GREEN first and exposed a real blind spot, now closed by driving BOTH
controls in both directions. TWO divergences recorded in the pane's class
doc: the v5-invented header source toggle (KEPT per this order's item 3;
ruled at §3 — stays, retirement is a candidate) and the pre-existing
source-mode-shows-body-not-whole-file shape (its own future slice). Lane
record: `status-log.md` → "P4.9L2 unit 1..3".

## Preamble

- **v4 baseline for this round: `c8a3cf77`** (v4 main, HEAD at planning
  2026-08-20, tree CLEAN, checkout on `main`). This is NOT a drift lane:
  it closes the gap P4.9L named and deliberately left
  (`m6-screen-parity.md` §4 row **14b**; the `GAP (named, P4.9L
  2026-08-14)` paragraph in `document-pane.ts`'s class doc). The round's
  one drift commit (`870a57fa`, per-turn conversation summaries) does
  not touch any file this lane owns — verified at planning.
- **Drift-check at lane start.** `git log c8a3cf77..main --oneline` AND
  `git diff main bugfix -- lib/ app/ packages/` in the v4 checkout. If
  v4 moved onto `DocumentPane.tsx`, `FormattingToolbar.tsx`, or the
  Salon pane hosts, **STOP** and report; other drift, report and
  continue (the sibling drift-lane pattern).
- **Sibling lanes (disjoint ownership):** P4.D95 (the per-turn
  conversation-summaries drift — core + harness + the settings/memory
  SPA files) and P4.51 (sweep maintenance — driver + carina recipe
  headers). See the binding Ownership section below.
- **Worktree:** per the `carryout` skill. SPA-only lane — no crate is
  touched, so no oracle regen and no sweep-driver use. Set up
  `apps/web/node_modules` with `npm ci`, never `cp -a`
  ([[spa-worktree-node-modules]]). For the live beats, this lane may
  borrow main's release-build target the way P4.D86 did (a crate-free
  lane can — see [[p4.d86-profile-editor-spa-lane]]).

## Mandate

Mount the existing `qt-formatting-toolbar` in the Document-Mode pane
exactly as v4's `DocumentPane` mounts its `FormattingToolbar`, wire the
pane's own seams (source branch → THIS pane's textarea; the toolbar's
source toggle sharing the header button's existing `showSource` signal —
never a second one), and thread `roleplayTemplateId` from the Salon so
the template's delimiter buttons appear. The standalone document view
passes NO template id (v4 passes none there). This lane touches ONLY the
files in its ownership row; it must not touch the composer, the toolbar
component itself (unless a genuine v4-parity defect is found — record
first), or anything of its siblings'.

## Survey-verified starting points (2026-08-20)

### v4, at `c8a3cf77` (paths in `~/source/quilltap-server`)

- `app/salon/[id]/components/DocumentPane.tsx`:
  - `:44` — `import FormattingToolbar from
    '@/components/chat/FormattingToolbar'`.
  - `:327-355` — the `DocToolbar` wrapper: a `.qt-doc-toolbar` row
    rendering `<FormattingToolbar roleplayTemplateId={…} editor={…}
    disabled={…} showSource={…} sourceTextareaRef={…}
    setInput={onContentChange} onToggleSource={…} />`. **No
    `narrationDelimiters` prop** → the toolbar shows the template's
    delimiter buttons but no synthesized "Nar" button (unlike the
    composer).
  - `:687` — the mount inside the pane body, fed the pane's
    `roleplayTemplateId` prop (`:61` in the props interface).
- The template id's journey: `SalonView.tsx` passes
  `chat?.roleplayTemplateId` at `:1577`, `:1630`, `:1847` (through
  `DocumentPaneBinding.tsx:23/27/48` for the bound pane).
- `components/workspace/StandaloneDocumentView.tsx:381` mounts
  `DocumentPane` with **no** `roleplayTemplateId` — the standalone
  toolbar shows no delimiter buttons.

### v5 (verified 2026-08-20)

- `apps/web/src/app/documents/document-pane.ts` — the pane, with the
  full GAP paragraph at `:48-67` (retire it when the gap closes) and
  the P4.6u #4 stretch-host lesson in the class doc. Companion
  `document-pane.spec.ts`.
- Hosts: `apps/web/src/app/screens/salon/salon-conversation.ts` (the
  Salon pane host — the `roleplayTemplateId` pass site to add; the v5
  chat data has carried `roleplayTemplateId` since P4.D44) and
  `apps/web/src/app/documents/standalone-document-view.ts` (passes
  nothing — match v4).
- Every toolbar piece already exists (landed P4.9L):
  `qt-formatting-toolbar`, `formatCommand`, `applyDelimiterCommand`,
  `applySourceDelimiter`. The P4.9L lane built a **56-vector v4-side
  jsdom recorder** for toolbar behavior — the parity oracle to extend
  if this mount needs new vectors (the no-Nar/with-template shape); see
  the P4.9L record in `status-log.md` and the recipe it committed.
- The pane already has a header source toggle with a `showSource`
  signal — the toolbar's source toggle must SHARE it (the GAP
  paragraph's second wiring requirement).
- Named live beats: `salon-documents-flow` and
  `workspace-document-standalone-flow` (existing e2e specs to extend).
- Prettier churn warning: the Salon files churn hundreds of unrelated
  lines under prettier ([[p4.d89-client-bugs-76-77-lane]]) — keep the
  pass-site edit surgical.

## Tiered deliverables

**Tier 1 (must land):**

1. The toolbar mounted in a `.qt-doc-toolbar` row above the editor,
   props mirroring v4's `DocToolbar` 1:1 — including NO
   narration-delimiters input (no Nar button).
2. The source branch acting on THIS pane's textarea (formatting
   commands reach the source textarea when source mode is on, exactly
   as the composer's source branch does).
3. The toolbar's source toggle sharing the header button's existing
   `showSource` signal — one signal, two controls, always in agreement.
4. `roleplayTemplateId` threaded from the Salon host into the pane (and
   deliberately NOT from the standalone view); the template's delimiter
   buttons appear in the Salon-hosted pane.
5. Unit/parity specs: the mount shape, the shared-signal invariant, the
   delimiter-buttons-present/absent split between the two hosts —
   against the P4.9L recorder vectors where byte behavior is in
   question.

**Tier 2 (should land):**

6. The two live beats extended: `salon-documents-flow` exercises a
   toolbar button on a document (assert the edit landed) + the source
   toggle from the toolbar; `workspace-document-standalone-flow`
   asserts the toolbar present WITHOUT delimiter buttons.
7. Close the paper trail: retire the GAP paragraph in
   `document-pane.ts`, strike row 14b in `m6-screen-parity.md`.

**Tier 3 (explicit deferrals, loud):** none expected — P4.9L already
established that every control the toolbar renders does something in
both views, and v4's template FETCH remains the recorded mechanism
divergence (the Salon fetches instead). If a new dead control is found,
record it, don't stub it.

## The differential requirement (restated)

Client-only lane: the equivalence obligation is the v4-client parity
pattern, not an NDJSON diff. Drive the P4.9L jsdom recorder against
v4's REAL `FormattingToolbar` for any vector the specs compare bytes
against (the with-template/no-Nar shape); mirror v4's behavior 1:1 in
the specs, per the P4.9L recipe. Do not hand-transcribe expected
strings the recorder can produce.

## Fixtures

The e2e fixture instance must carry a chat with a `roleplayTemplateId`
set (the P4.D44 picker beat's seed is the model) so the Salon beat can
see delimiter buttons. Seed through the API/UI, never SQL, for anything
store-overlay-backed ([[store-overlay-properties-cannot-be-sql-seeded]]).

## Shared contracts + Ownership (BINDING — identical in all three orders)

**Shared contracts:** none — the three lanes meet nowhere. No lane may
grow a dependency on another's files mid-flight; if one seems needed,
STOP and escalate.

**Ownership (no two lanes edit the same source file):**

- **P4.D95** owns: `crates/quilltap-core/src/services/{memory_service,
  pre_compute,orchestrator,message_context,build_context,
  commonplace_notifications}.rs`, `services/memory_recap/**`,
  `db/instance_settings.rs`, `api/memories.rs`, mechanical
  call-site touches of `search_memories_semantic` across core;
  `crates/quilltap-harness/tests/{vault_conv_search,recall_replay,
  precompute,build_context_tier3,memories_routes,
  context_summary_service_tier3}_equivalence.rs` + their fixtures and
  `harness/oracle/cases/` recipes; `crates/quilltap-web` ONLY if the
  REST edge projection names the settings keys; SPA:
  `apps/web/src/app/screens/settings/memory/**`,
  `apps/web/src/app/memory/memory.api.ts`,
  `apps/web/src/app/core/core-contract.ts`, its own e2e spec.
- **P4.9L2** owns: `apps/web/src/app/documents/**`, the
  `roleplayTemplateId` pass site in
  `apps/web/src/app/screens/salon/salon-conversation.ts`, the
  `salon-documents-flow` + `workspace-document-standalone-flow` e2e
  specs, `m6-screen-parity.md` row 14b.
- **P4.51** owns: `harness/tools/recipe_sweep.py` and ONLY the recipe
  header comments of
  `crates/quilltap-harness/tests/carina_memory_extraction_tier3_equivalence.rs`
  and `carina_query_tier3_equivalence.rs`.
- `CHANGELOG` and `status-log.md` are append-only for everyone.

**Version bumps:** P4.D95 bumps `quilltap-core`, `quilltap-harness`,
the SPA (`apps/web/package.json`), and `quilltap-web` only if touched.
P4.9L2 bumps the SPA. P4.51 bumps `quilltap-harness`. Harness and SPA
are each bumped by TWO lanes — **the unifier ACCUMULATES bumps; an
identical-bump merge has eaten one before** (the `9125f492` round
record).

## Verification gate (before declaring done)

- SPA: `npm test` + `npm run build` (never a bare `ng` —
  [[ng-test-defaults-to-watch-mode]]).
- The two extended beats green LIVE (read
  [[e2e-playwright-traps]] before any diagnosis; a refetch the beat
  triggers must be awaited).
- No crate touched; no `cargo` gate owed beyond confirming the tree
  builds if any shared type was (unexpectedly) grazed — which would be
  an ownership violation to report, not to ship.
- Per-commit rule: `.claude/commands/commit.md`.
- Lane record appended to `status-log.md`.
