# Work order P4.D148 — the Concierge state chosen at chat creation, server half (v4 `303288fb4`)

**Status:** CLOSED — UNIFIED on main at the `0b0617fee` drift catch-up round
unification (2026-09-03; lane branch `claude/concierge-creation-server-d1d460`).
Tier 1 whole: `conciergeState` on the create request (the double-`Option`
through `de_double_opt_string`, validated against the four wire values at the
top of `handle_create` — an unknown string or an explicit `null` answers
`Validation error` 400 with nothing written), `apply_requested_concierge_state`
through the EXISTING `apply_concierge_flip` chokepoint on ALL THREE branches
right after the system-prompt message (the continuation branch BEFORE the
replay), the greeting ladder's attempt 0 on the uncensored desk with the resolver
asked WITH the fresh chat row, the ONE shared `generate_via_uncensored_desk`
closure, attempt 3's `!uncensored_desk_tried` skip, five log lines byte-copied.
Tier 2 landed IN THE CORPUS (the mock could express a content filter). Tier 3:
none. The capstone corpus 19 → 32 cases red-first, with two NEW comparands
(`message_order` over `rowid`, `stream_calls` over the ordered call trace) and
the `NoApiKeys` → `ConnApiKeys` harness seam fix that made the uncensored desk
reachable at all. **Fixed at the §3 unification review:** the attempt-3 arm's
dead emptiness guard (the desk already answers `Ok(None)` on an empty greeting),
the silent no-op on a missing chat id (v4 has no such arm — the id now comes from
the caller), and the Err-parity doc comment (the helper still `.ok()`-folds the
settings/resolver/routing reads v4's `try` also catches — log-only,
pre-existing). **Recorded, not changed:** a non-string `conciergeState` (e.g.
`42`) answers the dispatch decode envelope where v4 answers the flat
`Validation error` — the P4.60/P4.62 wrong-type-collapse class, shared with
`roleplayTemplateId`/`timestampConfig`; a named lead for that census. Lane
record: `status-log.md` → "P4.D148".

## Preamble

- **v4 oracle baseline: `6d2a50382`** (drift-ledger §1, probe re-run at
  ordering 2026-09-02: v4 checkout on `main` at `0b0617fee`, tree clean,
  both logs empty). **This lane's target commit is `303288fb4`** — *"Choose
  the Concierge state on the New Chat form"* (23 files, +1436/−91; the
  server half is `app/api/v1/chats/route.ts` +244/−91 and a comment-only
  hunk in `orchestrator.service.ts`). Its feature doc
  `docs/developer/features/complete/concierge-default-at-creation.md` (§3
  "Wire contract", §4 "Applying the state", §5 "Greeting routing",
  "Decisions of record") may be read for rationale, but the hunks are the
  spec (ledger §5.3): `git -C ~/source/quilltap-server show 303288fb4 --
  app/api/v1/chats/route.ts`.
- **Freshness probe at lane start** (drift-ledger §2 — the four read-only
  commands). Pass = v4 checkout on `main`, tree clean, `git log
  0b0617fee..main` empty and `git log 3a76b17df..bugfix` empty. **Any
  mismatch: STOP and report** — a lane never writes the ledger.
- **Regen rule: PIN REQUIRED** (ledger §1 — HEAD is five commits past the
  baseline). Every oracle/fixture regen runs from a lane-unique detached
  worktree pinned at **`303288fb4`** (`/tmp/qt-v4-pin-p4d148-303288fb4`,
  the three symlink classes, ledger §5.1) through the sweep driver
  (`python3 harness/tools/recipe_sweep.py --run <family> --v4 "$PIN" --v5w
  <worktree>`); the capstone family runs with `TZ=UTC` (its cron case).
  Verify the pin by grepping a fresh capstone NDJSON for
  `Briefing the Concierge` (present only past the baseline) and for the
  ABSENCE of any bug-115/116/117 marker. This pin carries NO sibling drift
  (`303288fb4` is the first commit past the baseline) — regenerate only
  this lane's families from it.
- **Sibling lanes (this round):** P4.D149 (this feature's SPA half — §A
  binds the two), P4.D150 (bug 115 + the inter-character timing log — owns
  `build_context.rs`; do not touch it), P4.D151 (bug 116 + the manifest
  regen — owns `file_fallback.rs` + the `CompletionResponse` widening),
  P4.D152 (bug 117 — owns `chat_files.rs`, import/restore files regions,
  the boot heal). §Shared contracts / §Ownership are binding and identical
  across all five orders.
- **Worktree:** per the `carryout` skill (`.claude/worktrees/…`, branch
  `claude/p4-d148-…`). `CHANGELOG`/`status-log.md` append-only. **No
  Playwright** (§F). Disk: budget 50–70 GB for the target dir; run gates
  with `CARGO_INCREMENTAL=0` and check `df -h ~` first (89 GB free at
  ordering, five lanes).

## Mandate

Port the server half of `303288fb4` whole: `conciergeState` on the
`chatCreate` request; `apply_requested_concierge_state` at ALL THREE create
branches right after `write_system_prompt_message` (the continuation branch
BEFORE `apply_chat_continuation`, so the bubble precedes the replayed tail);
the greeting ladder's "attempt 0" — `auto_generate_first_message` reads the
FRESH chat row, the reroute body becomes ONE shared closure asked WITH the
chat (`resolve_dangerous_content_settings(global, Some(&chat_row))`), a
Flagged/Uncensored chat generates its greeting on the uncensored desk
FIRST, the content-filter attempt 3 reuses the closure and SKIPS when
attempt 0 ran; the five log lines. Must NOT touch: the SPA (P4.D149's),
`manual_flip.rs` (the chokepoint is unchanged in v4 — a caller is added,
nothing else), `chat_override.rs` / the resolver (v4 changed neither),
`build_context.rs` (P4.D150's), anything under §B/§D/§E.

## Survey-verified starting points (2026-09-02, from the shipped hunks — ledger §5.3)

1. **The schema field** (v4 `route.ts:137-146`): `conciergeState:
   z.enum(['monitored','flagged','vouched','uncensored']).optional()` with
   its doc-comment (carry it). v5 `services/chat_create.rs:150-198`
   `ChatCreateRequest` (serde `default`, `rename_all = "camelCase"`) — add
   `pub concierge_state: Option<ConciergeState>` deserializing from the
   FOUR wire strings (`ConciergeState` lives in
   `services/dangerous_content/chat_override.rs:63-94` with hand-rolled
   `as_str`/`from_wire`; add a serde impl there ONLY if none exists — check
   first; otherwise a `deserialize_with` local to `chat_create.rs`). An
   unknown string must fail deserialization so the dispatch answers the
   create schema's 400 (the same arm `tc_bad_value_rejected` pins for
   `timestampConfig`); an explicit `null` REJECTS too (v4 `.optional()`
   without `.nullable()` — mirror `timestamp_config`'s `de_double_opt_value`
   trick if a bare `Option` would swallow the null; measure).
   `api/types.rs:381`'s `ChatCreate` variant flattens `ChatCreateRequest`
   (the P4.D44 seam — `api/types.rs` need not open).
2. **`applyRequestedConciergeState`** (v4 `route.ts:365-389`): `if
   (!requested || requested === 'monitored') return;
   progress.status('Briefing the Concierge…'); const result = await
   applyConciergeFlip(chat.id, requested, chat); logger.debug('[Chats v1]
   Applied Concierge state at creation', {chatId, requested, changed})`.
   v5: a private `async fn apply_requested_concierge_state(db, emitter,
   chat: &Value, requested: Option<ConciergeState>)` beside
   `write_system_prompt_message` (`chat_create.rs:989`), calling
   `apply_concierge_flip(db, &RealConciergeAnnouncer { db }, &chat_id,
   requested, &chat).await` (the signature at `manual_flip.rs:95-101`;
   today's ONLY production caller is `api/salon.rs:1548` — copy its
   announcer shape). The `chat` Value the create path already holds (step
   10, `compile_all_identity_stacks(main, mount, &chat)`) is v4's
   `ChatMetadata` row from `chats.create` — a FRESH chat's
   `conciergeOverride`/`isDangerousChat` are null/0, so
   `get_concierge_state` reads Monitored and the flip always CHANGES for a
   non-Monitored request. The debug line carries `chatId`, `requested`
   (wire string), `changed`.
3. **The three branches** (v4 `route.ts:1275-1330`): v5 `chat_create.rs:
   725-778` — continuation (`:728-729`: system prompt → **flip** →
   `apply_chat_continuation` → scenario/staff `skip_first_message=true`),
   autonomous (`:745`: system prompt → **flip** → scenario/staff), ordinary
   (`:762`: `emitter.status("Setting the opening scene…")` → system prompt →
   **flip** → scenario/staff). v4 DELETED the `createInitialMessages`
   wrapper (both call sites already write the prompt themselves in v5 —
   nothing to delete; say so). The ordinary branch's "Setting the opening
   scene…" status comes BEFORE the prompt write in both trees; the
   Concierge status sits AFTER it and BEFORE the scenario work (the SSE
   frame order is a capstone comparand — `frames`).
4. **Attempt 0 + the shared closure** (v4 `route.ts:709-812` and
   `:873-890`): v5 `auto_generate_first_message` `:1390` builds the ladder
   at `:1609-1671` (`content_filter_hit`, attempts 1–4) and calls
   `attempt_uncensored_reroute` `:1707-1806`, which resolves with
   `resolve_dangerous_content_settings(global_settings, None)` — **the
   `None` is the bug v4 fixed**: pass the fresh chat row (`chats_read::
   find_by_id(main, chat_id)` read once right after the profile/sampling
   setup, before attempt 0). Reshape `attempt_uncensored_reroute` into the
   closure's Rust twin taking `trigger: &'static str` (`"chat-state"` |
   `"content-filter"`) and `chat_row: Option<&Value>`; the mode gate stays
   `mode != "AUTO_ROUTE" → None` (a Vouched chat collapses to `OFF` inside
   the resolver — never reroutes even under a global AUTO_ROUTE; an
   Uncensored chat resolves to AUTO_ROUTE even under a global `OFF`).
   Attempt 0: `if should_use_uncensored_route(Some(&chat_row))` → set
   `uncensored_desk_tried = true`, call the closure with `"chat-state"`;
   `Some` → return it; `None` → the info line *"Uncensored desk unavailable
   or empty for greeting — using the participant’s own profile"* (U+2019 in
   `participant’s`, fields `characterId`, `chatId`); an Err → the warn
   *"Concierge uncensored greeting attempt failed"* (`characterId`, `error`)
   — v5's closure returns `Option`, so measure whether the Err arm is
   reachable (v4's try/catch wraps `generateGreetingMessage` throwing; v5's
   `generate_greeting_message` returns `Err` — carry it as the warn, don't
   swallow). Attempt 3 becomes `if content_filter_hit && !uncensored_desk_
   tried`, logging *"Content filter detected on greeting — falling back to
   Concierge uncensored provider"* with `characterId` ONLY (the four
   profile fields MOVED into the closure's own info line
   *"Generating greeting on the Concierge uncensored provider"* — fields
   `characterId, trigger, settingsSource, uncensoredProfile,
   uncensoredProvider, uncensoredModel`; and the success line gains
   `trigger`). Byte-copy all five sentences.
5. **The capstone corpus is BLIND to all of this today.** `fixtures/
   chat-create-capstone.json` seeds no `dangerousContentSettings` and no
   uncensored profile (grep: zero hits for `dangerous|uncensored|
   AUTO_ROUTE`), and `build-chat-create-capstone.ts:181-186` writes
   `chat_settings` with only the roleplay default. Widen the builder: an
   `uncensoredTextProfileId` profile (+ its api key) and
   `chat_settings.dangerousContentSettings = {mode:'AUTO_ROUTE', …}`
   seeded per spec (keep every existing case's request byte-identical —
   the global AUTO_ROUTE changes NOTHING for a Monitored chat's greeting
   unless a content filter fires, which no existing case does; PROVE that
   by regenerating the pre-widening cases and diffing). The oracle's
   `createLLMProvider` mock (`chat-create-capstone.test.ts:180-234`)
   records `{kind:'stream', provider, model, …}` per greeting call — the
   RECORDING is how attempt 0 is measured (the uncensored profile's
   provider/model appears FIRST, the participant's never does).
6. **The flip's own family** already exists: `danger_resolver_equivalence`
   carries the manual-flip arms (`QT_ORACLE_DANGER_MANUAL_FLIP` via
   `cases/danger-manual-flip.test.ts` driving the REAL
   `applyConciergeFlip` + REAL announcement writer). Run it at the pin;
   nothing in it moves (v4 did not touch `manual-flip.ts`) — say so.
7. **The `chat_messages` dump** (`chat-create-capstone.test.ts:71`, ordered
   by id) is where the announcement bubble's POSITION is asserted: system
   prompt → Concierge manual bubble → (Prospero/scenario/staff) → greeting.
   v5's `apply_concierge_flip` posts through
   `services/concierge_notifications` — confirm the row lands with the
   same `systemSender`/kind bytes the PUT path already pins.

## Tiered deliverables

- **Tier 1 (must land):** points 1–4 with the widened capstone corpus, RED
  FIRST: new cases `cs_omitted_is_monitored` (no key — every dump
  byte-identical to `single_char_first_message`), `cs_monitored_explicit_
  noop` (`'monitored'` — identical again, no bubble, no status frame),
  `cs_flagged_bubble_after_prompt` (the manual-flagged bubble row second;
  `isDangerousChat=1`, `dangerCategories='[]'`, `dangerClassifiedAt` set),
  `cs_vouched_never_reroutes` (global AUTO_ROUTE, `greetingContent` from
  the participant's profile — the recording shows the participant's
  provider only), `cs_uncensored_attempt0` (the recording's FIRST stream
  call is the uncensored profile's provider/model; the greeting persists
  from it), `cs_uncensored_desk_empty_falls_through` (canned empty content
  for the uncensored call, then the participant's — two recordings, in
  that order; needs a per-provider canned map in the mock: extend
  `greetingContent` to a per-provider table or add
  `uncensoredGreetingContent`), `cs_invalid_value_400`
  (`'spicy'` → 400 envelope, nothing written), `cs_null_rejected` (explicit
  null → 400), `cs_continuation_bubble_before_replay` (continuation branch:
  prompt → bubble → replayed tail), `cs_autonomous_flagged` (the autonomous
  branch, `skip_first_message` — bubble present, no greeting). The `frames`
  comparand carries `Briefing the Concierge…` in the right slot.
- **Tier 2 (should land):** the content-filter arm's `!uncensored_desk_
  tried` skip made measurable — a case whose participant call yields a
  content-filter refusal on an Uncensored chat proves attempt 3 does NOT
  fire a second uncensored call (the mock needs a `contentFilter` arm; if
  the capstone mock cannot express a content-filter result without
  reshaping it, land the skip pinned by a unit test in `chat_create.rs`
  over a fake provider and record the corpus gap by name).
- **Tier 3 (explicit deferrals, loud):** none expected. The `orchestrator.
  service.ts` comment hunk is a NO-PORT (comment only) — record it. Help
  hunks bank via P4.D149 (§Ownership).

## The differential requirement (restated)

Every unit drives v4's REAL code from the `303288fb4` pin:

- `chat_create_capstone_equivalence` (`QT_ORACLE_CC`, `QT_FIXTURE_CC_{MAIN,
  MOUNT,LLM}`; recipe in the test header; `TZ=UTC`) — the REAL `POST`
  handler over the widened fixture; the ten new cases above; grep the
  fresh NDJSON for `Briefing the Concierge` and for the uncensored
  profile's model name in a `kind:"stream"` recording (ledger §5.2).
- `initial_greeting_equivalence` (`QT_ORACLE_GREETING`) and
  `first_message_context_equivalence` (`QT_ORACLE_FMC`, `QT_FIXTURE_FMC_*`)
  — regenerated at the pin and run by name; NO movement expected (v4 did
  not touch `initial-greeting.ts` / `first-message-context.ts`) — say so.
- `danger_resolver_equivalence` (`QT_ORACLE_DANGER_RESOLVER` +
  `QT_ORACLE_DANGER_MANUAL_FLIP`) — run at the pin, unchanged.
- Mutation proofs (each verified-applied, reverted by file backup): move
  the flip BEFORE `write_system_prompt_message` → the bubble-position
  cases redden; pass `None` back to the resolver → `cs_vouched_never_
  reroutes` reddens (the recording shows the uncensored provider); drop
  `uncensored_desk_tried` → the Tier-2 case reddens; drop the status line
  → the `frames` comparand reddens; accept `'monitored'` as a flip → the
  noop case reddens (a bubble appears).

## Fixtures

- Consumes: `chat-create-capstone.json` + `build-chat-create-capstone.ts`
  (WIDENED — this lane owns them), the greeting/fmc pairs (run only).
- Delivers: the widened capstone spec + builder; the three `QT_FIXTURE_CC_*`
  files are /tmp-built by the recipe (never committed). Regenerating the
  builder ⇒ every capstone case re-run; name the count in the record.

## Shared contracts (binding, identical across all five orders)

**§A — the create-time Concierge wire (P4.D148 ↔ P4.D149).** The
`chatCreate` dispatch verb's request (v5 `ChatCreateRequest`, SPA
`ChatCreateRequest` in `core-contract.ts`) gains ONE optional key:

```
conciergeState?: 'monitored' | 'flagged' | 'vouched' | 'uncensored'
```

(v4 `createChatSchema`: `z.enum([...]).optional()` — spelled out, not derived;
`.optional()` is NOT nullable, so an explicit JSON `null` REJECTS like
`timestampConfig`'s does.) Absent ≡ `'monitored'`: no write, no announcement,
the request AND the created chat byte-identical to today's. Any other value is
applied through the EXISTING `apply_concierge_flip` chokepoint (v4
`applyConciergeFlip(chat.id, requested, chat)` with the fresh row) on ALL
THREE create branches (continuation / autonomous / ordinary), immediately
AFTER the system-prompt message and BEFORE `apply_chat_continuation` /
scenario / staff / greeting, so the Concierge bubble is the first row after
the prompt and the greeting is generated under the chosen state. The
announcement reuses v4's five `manual-*` kinds (`RealConciergeAnnouncer`) —
NO new sentences. The SSE progress status line is exactly
`Briefing the Concierge…` (U+2026), emitted only when a non-Monitored state
is applied, between the branch's existing status and the scene status. An
unknown string → the create schema's validation 400 (the envelope
`tc_bad_value_rejected` already pins). The CLIENT omits the key when the
form holds `'monitored'`. v5 serves NO REST `POST /api/v1/chats` (the SPA
creates through `POST /api/dispatch`; `chats_routes.rs` has the collection
GET only) — recorded, not this round's.

**§B — `CompletionResponse.cache_usage` (P4.D151 owns the widening).**
`model::completion::CompletionResponse` gains
`pub cache_usage: Option<crate::model::stream::StreamCacheUsage>` (v4
`LLMResponse.cacheUsage`), populated from `parsed.cache_usage` in the real
`completion_provider.rs` composition and `None` at every other construction
site. **P4.D151 edits every construction site itself, including the
`cache_usage: None` one-liners in harness tests owned by other families**
(`precompute_equivalence.rs`, `recall_replay_equivalence.rs`,
`title_update_tier3_equivalence.rs`, `settings_wire_actions.rs`,
`attach_mount_file_equivalence.rs`, `chat_regenerate_title_tier3_equivalence.rs`,
`answer_confirmation_tier3_equivalence.rs`, `danger_gatekeeper_tier3_equivalence.rs`,
`file_attachment_tier3_equivalence.rs`, `outfit_llm_choose_tier3_equivalence.rs`).
No other lane edits those harness files this round (P4.D150's pins are
core unit tests, not harness edits) — that is what keeps the pick clean.

**§C — `distill_memory_search` (P4.D150 owns).** The function gains a
trailing `options: CheapLlmTaskOptions` parameter threaded to
`executor.execute(...)` (v4 `latency: CheapLLMLatencyClass = 'background'`
→ `{ latency }`). Its three callers: `build_context.rs` (the dynamic-head
FALLBACK) passes `CheapLlmTaskOptions::interactive()`; `pre_compute.rs` and
`recall_replay.rs` pass `CheapLlmTaskOptions::default()`. No other lane
touches `services/memory_recap/distill.rs`, `services/build_context.rs`,
`services/pre_compute.rs`, or `services/recall_replay.rs`.

**§D — the `files.sha256` invariant (P4.D152 owns).** `files.sha256`,
`mimeType` and `size` ALL describe the bytes actually STORED (post-bridge),
never the writer's input — at chat upload, `.qtap` import, restore replay
AND the carried-store-rows restore branch (which reads the archived
`doc_mount_blobs.sha256` by the parsed blob id). The boot heal carries v4's
migration id `realign-file-entry-sha256-v1` in the P4.D140 ledger shape
(honour an existing `migrations_state` row from either app; write the row
only on a pass that realigned ≥ 1 row). P4.D152 does NOT touch
`services/file_fallback.rs` (the describe tier reads `files` by id, never
by sha) and does NOT edit `attach_mount_file_equivalence.rs` (P4.D151's);
its join proof is the NEW heal family + the state dumps.

**§E — the manifest substrate (P4.D151 owns for this round).** The generated
`provider_manifest/manifests/*.json` are regenerated ONCE, by P4.D151, from
the `0b0617fee` pin through `harness/oracle/providers/gen-provider-manifests.mjs`;
the expected result is BYTE-IDENTICAL (bug 118 is a v4 convergence onto
bytes v5 has carried since P4.D107). No other lane runs the generator.

**§F — Playwright.** Port 4319 is P4.D149's this round. P4.D149's
create-time beat is authored GATED behind `const P4D148_SERVER_LANDED =
false` (ACTIVATE-AT-UNIFY — the unifier flips it after P4.D148 lands and
runs it live); every other lane authors NO Playwright and says so.

**§G — the drift ledger.** Lanes never write `drift-ledger.md`; the five §3
rows were marked ORDERED at ordering, and `/unify` marks them ABSORBED /
NO-PORT-RATIFIED when the baseline moves to `0b0617fee`.

## Ownership (binding, identical across all five orders)

| Area | Owner |
|---|---|
| `quilltap-core`: `services/chat_create.rs` (the request field, `apply_requested_concierge_state`, the three branches, `auto_generate_first_message`'s attempt 0 + the shared uncensored-desk closure, the five log lines); harness: `chat_create_capstone_equivalence` (+ `cases/chat-create-capstone.test.ts`, `fixtures/chat-create-capstone.json`, `fixtures/build-chat-create-capstone.ts`), `initial_greeting_equivalence` + `first_message_context_equivalence` (run, not changed) | **P4.D148** |
| SPA: `screens/new-chat/{new-chat-form.ts, new-chat.types.ts, new-chat.state.ts, new-chat.logic.ts}` (+ specs), `core/core-contract.ts` §A region, `e2e/new-chat-flow.spec.ts` (the gated beat); the `p4.9i2` bank rows for `help/chats.md` + `help/dangerous-content.md` | **P4.D149** |
| `quilltap-core`: `services/memory_recap/distill.rs`, `services/build_context.rs` (the fallback-distill site + the inter-character log line + its unit pins), `services/pre_compute.rs`, `services/recall_replay.rs`; harness: `build_context_tier3_equivalence`, `precompute_equivalence`, `recall_replay_equivalence` (RUN only — no edits, §B) | **P4.D150** |
| `quilltap-core`: `services/file_fallback.rs` whole, `model/completion.rs` (§B), `model/completion_provider.rs` (§B), the `cache_usage: None` one-liners at `services/compression.rs`, `services/cheap_llm_exec.rs`, `services/memory_recap/mod.rs`, `services/outfit_selections.rs` + the harness sites listed in §B, `model/request_builder/chat_completions.rs` (the NanoGPT comment), `provider_manifest/manifests/*.json` (§E regen, expected no-op); harness: `file_attachment_tier3_equivalence` (+ `cases/file-attachment-tier3.test.ts`, `fixtures/file-attachment.json`, its builder), `fallback_engine_equivalence` (+ `cases/fallback-engine.ts`), `attach_mount_file_equivalence` (+ its case); the `p4.9i2` bank rows for `help/file-uploads.md` + `help/chat-settings.md`; the `b448eddd7` NO-PORT ratification | **P4.D151** |
| `quilltap-core`: `services/chat_files.rs`, `services/quilltap_import/files.rs`, `services/backup/restore/orchestrator.rs` (the files region — `restore_one_file` / `carried_store_rows` and the replay branch ONLY), NEW `db/files_sha256_realign_heal.rs` + its `db/mod.rs` line; `quilltap-host`: `host.rs` (one boot-chain block); harness: NEW `files_sha256_realign_heal_equivalence` (+ its oracle case + fixture builder), `files_tier2_equivalence` / `files_routes_equivalence` (the upload sha arm), `qtap_import_equivalence`, `system_restore_state` / `system_restore_equivalence` / `restore_vintage_state` (the sha arms + any NEW committed archive), `character_photo_upload_tier2_equivalence` + `photo_tools_equivalence` (run), a harness-only byte-changing `PixelCodec` twin | **P4.D152** |
| Playwright (port 4319) | **P4.D149's branch only** (§F) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it (§G) |

**Version bumps:** P4.D148 bumps `quilltap-core` + `quilltap-harness`;
P4.D149 bumps the SPA `package.json`; P4.D150 bumps `quilltap-core` (+
`quilltap-harness` only if a harness file changes — §B says none should);
P4.D151 bumps `quilltap-core` + `quilltap-harness`; P4.D152 bumps
`quilltap-core` + `quilltap-harness` + `quilltap-host`. Per commit; the
unifier recounts at unification (the playbook's silent-auto-merge trap).

## Verification gate before declaring done

`cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
-D warnings` on BOTH feature sets (plain + `--features
quilltap-core/native-transport`); full `cargo test --workspace` with the
lane's oracle env vars (`run_in_background: true`, full log + sentinel —
NEVER a pgrep loop, NEVER `| tail`); the lane's families by name through
the sweep driver from the `303288fb4` pin, zero SKIP, changed-bytes grepped
(`Briefing the Concierge`, the uncensored model in a stream recording, the
manual-flagged bubble row); mutation proofs verified-applied and reverted
by file backup; `danger_resolver_equivalence` run once at the pin (unchanged
— record it). No SPA gate (no SPA file touched — say so). Per-commit rule:
`.claude/commands/commit.md` (CHANGELOG entry per commit, core + harness
bumps, lane record appended to `status-log.md` under "P4.D148").
