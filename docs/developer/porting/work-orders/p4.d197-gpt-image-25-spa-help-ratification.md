# Work order P4.D197 — v4 `d8d2890ee` (PR #62), the CLIENT half + help + the `53294163f` ratification: the SPA's offline OPENAI model list (eight ids, v4's order), the image-profile modal's stale OPENAI-deferral prose corrected and the schema arm PROVEN over a recorded copy of v4's `getOpenAIImageOptionsSchema` output (a render spec beside `nanogpt-options.spec.ts` + a GATED live beat that flips at unification), the manual-dialog hunks MEASURED for a v5 twin (bug 150's exposure settled by measurement, not by the ledger's guess), the two `help/` pages re-vendored byte-exact (124 stays 124), and `53294163f` ratified NO-PORT on its file list — from `main`

**Status: OPEN** — ordered 2026-09-16, the `53294163f` GPT-Image-2.5 drift
catch-up + maintenance round (P4.94 ∥ P4.D196 ∥ P4.D197 ∥ P4.95). The lane
updates this header at lane close; the unifier rewrites it at unification.

## Preamble

- **v4 oracle baseline: `1fefadb9a`.** This lane absorbs the CLIENT + help
  half of **`d8d2890ee`**: `components/image-profiles/ImageProfileForm.tsx`
  (+2/−2 — `FALLBACK_PROVIDERS[OPENAI].defaultModels` → eight ids),
  `components/image-profiles/ImageProfileParameters.tsx` (+39/−? — the
  OPENAI fallback branch's three selects refreshed to the family UNION with
  a `(model default)` blank first and the `|| 'standard'`/`'vivid'`/
  `'1024x1024'` fallbacks → `|| ''`), `components/images/image-generation-
  dialog.tsx` (+49 — `OPENAI_SIZES` twelve entries, `OPENAI_QUALITIES` nine
  with `''` first, the initial `quality: ''`), `help/image-generation-
  profiles.md` (+28/−4) and `help/provider-recommendations.md` (+2/−2); and
  it RATIFIES **`53294163f`** ("docs: Update version" — `README.md`, three
  `package.json`s, `package-lock.json`, and all fifteen `plugins/dist/*/
  index.js` rebuilt; NO `lib/`, `app/`, `components/`, `packages/*/src`,
  `help/`, `public/schemas/` or `manifest.json` content hunks) NO-PORT on
  its `--name-status` list. The ledger rows are `ORDERED(P4.D196, P4.D197)`
  and `ORDERED(P4.D197)`. Spec reads: `git show 53294163f:<path>` and
  `git show d8d2890ee -- <path>` — **never the commit message** (§R.4).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** Pin **`/tmp/qt-v4-pin-p4d197-
  53294163f`** (the TARGET) for the two `help/` byte-copies, the `help_*`
  content families, the options-schema recording (§S.1 — a tsx script
  importing v4's REAL `getOpenAIImageOptionsSchema` from `plugins/dist/
  qtap-plugin-openai/image-options-schema.ts` at the pin), and the
  `--name-status` lists. Pin verification: the recorded sunburst schema
  carries `xhigh`; a baseline-pinned import of the module FAILS (the file
  does not exist at `1fefadb9a`).
- **Sibling lanes:** P4.D196 (the server half — it delivers the OPENAI arm
  of the `options-schema` action your beat drives; until it lands the
  action answers `optionsSchema: null` for OPENAI and the modal shows the
  JSON textarea — which is WHY your beat is gated; it also records the
  same five schema JSONs, §R.10(c)), P4.94 (the fixture heal — its full
  Playwright run is on the widened e2e seed, yours is on `main`'s), P4.95
  (orchestrator). None touches `apps/web/**` or `help/**`.
- **Worktree:** `git worktree add <path> -b claude/p4-d197-gpt-image-25-
  spa-help-<hash> main`; `CARGO_INCREMENTAL=0`; `npm ci` in `apps/web`
  (never `cp` a sibling's `node_modules` — `spa-worktree-node-modules`).
- **Read before the work:** `apps/web/src/app/screens/settings/images/image-
  profile-modal.ts:60-100` (the header: the P4.D139 schema-driven panel in
  front of the hand-written arms; "v4's legacy arm is `ImageProfile
  Parameters` alone, whose `default:` case renders NOTHING; v5's is the
  size panel OR the JSON textarea, the textarea being a v5 invention"; the
  STILL-DEFERRED list naming `OPENAI` (`:28-83` — Quality, Style, Size,
  Response Format) — that paragraph is what goes stale: after P4.D196 the
  OPENAI plugin DECLARES a schema, so its hand-written branch is reachable
  only on a failed fetch, and v5's stand-in for that case stays the
  textarea — a RECORDED divergence, not a port), `:440-460` (`sizePanel`
  over `PROVIDER_SIZE_PANELS` — Z_AI/NANOGPT only; `sizeValue`'s `||
  '1024x1024'` is v4's `:137`/`:166` for THOSE two panels, which the drift
  did NOT change — leave it), `:595-625` (the options-schema fetch effect
  keyed on `providerKey` + `modelName` + `catalogVersion` — the refetch on
  model change v4's schema depends on already exists; `fetchImageOptions
  Schema` → `imageProfileOptionsSchema`), `:625-660` (the three post-render
  select assignments — the controlled-select class, dogfood #108); `apps/
  web/src/app/screens/settings/images/image-profile-form.ts:1-40`
  (`FALLBACK_PROVIDERS` twin at `:18` — four ids today; `PROVIDER_SIZE_
  PANELS`); `apps/web/src/app/screens/settings/providers/provider-options-
  panel.ts` + `provider-options-schema.ts:20-100` (the renderer and its
  field contract — `type` incl. `'number'`, `enumValues[].description`
  (declared for Ollama's `thinking_effort`), group `helpText`, field
  `helpText`, `default`; v4's OpenAI schema uses `default: ''` on every
  enum and a `number` field with NO min/max — confirm the renderer handles
  both without a change; if it needs one, that is a §S.3 STOP) and
  `nanogpt-options.spec.ts` (the precedent render spec over a transcribed
  schema fixture — yours is RECORDED, not transcribed); `apps/web/src/app/
  core/core-contract.ts:5070-5100` (`ImageOptionsSchemaResponse` — the wire
  shape, UNCHANGED); `apps/web/e2e/settings-image-lora-flow.spec.ts` WHOLE
  (the precedent beat on THIS modal: `openImageProfilesCard`, `provider
  Select`/`modelSelect`/`nameInput`, `pickNanoGptKey`, `deleteProfile`, the
  `maybeUnlock` idiom, the `P4D138_SERVER_LANDED`-style gate it once had,
  the "row 0" dogfood-#108 assertion at `:205-215`), `settings-flow.spec.ts:
  397-560` (the Images-tab beats — the Fetch Models control, the NanoGPT
  picker), `apps/web/e2e/support/seed-courier-fixture.ts` (what the e2e
  instance holds — MEASURE whether an OPENAI `api_keys` row exists; if not
  the beat seeds one through the API, never SQL — `dogfood-findings.md`'s
  standing note); the SPA-side recorder precedent (P4.D187's `reconcile
  Transcript` 38-vector corpus "from v4's real module" — find its recorder
  and put yours beside it; `spa-oracle-recorders-live-outside-src`);
  `crates/quilltap-harness/tests/help_tree_embed_guard.rs:1-45` (the
  vendored count `VENDORED_FILE_COUNT = 124` and the per-round doc list —
  this round MODIFIES two and adds none; the md5 line moves); `crates/
  quilltap-harness/tests/help_tree_equivalence.rs:1-35` (the recipe: the
  jest case over `help/**` at the pin); the P4.D194 lane record (the
  precedent CLI+help lane: the byte-copy, the guard edit, the NO-PORT
  ratification tables); the P4.D102 order (the NanoGPT spec-fixture
  transcription + the `Fetch Models` beats — the SPA half of an image-
  provider drift); memory notes `spa-has-no-zod-schema-twins-are-hand-
  rolled`, `ng-test-defaults-to-watch-mode`, `spa-tsc-does-not-typecheck-
  app-sources`, `e2e-playwright-traps`, `e2e-assertion-can-read-the-pre-
  click-state`, `qt-class-guard-false-positives`, `check-qt-classes-
  ignores-a-bare-component-class`, `a-blanket-flag-guard-is-not-a-scoped-
  one`, `dispatch-verb-ignores-unknown-fields`.

## Mandate

Land the client half of PR #62 as v5 actually needs it: the eight-id
fallback list in v4's order (§S.2); the modal header's OPENAI paragraph
rewritten to the post-`d8d2890ee` truth (schema-served; the hand-written
branch is v4's fetch-failure fallback refreshed to the union; v5's stand-in
stays the JSON textarea — RECORDED, with the sha); a render spec that
mounts `ProviderOptionsPanel` over a RECORDED copy of v4's `getOpenAIImage
OptionsSchema` output for the five §R.10(c) inputs and asserts every field,
label, `(model default)` blank, `description`, group `helpText`, the
`number` field with no bounds, and `style`/`GPT Image Output` presence per
family (mutation-proven: a fixture edit that drops `xhigh` must redden);
a GATED live beat (`P4D196_SERVER_LANDED = false`, flipped at unification)
walking OPENAI → `gpt-image-2.5-sunburst` → the two groups → `dall-e-3` →
Style present, GPT Image Output gone → save `quality: max, background:
transparent, output_format: webp, output_compression: 80, moderation: low`
→ reload → the stored bag carries them (through `imageProfileGet`, never
SQL) — with the dogfood-#108 "row 0" assertion carried; the manual-dialog
hunks MEASURED (does v5 have a twin of `components/images/image-generation-
dialog.tsx`? the survey says NO — `grep -rn 'image-generation-dialog\|
ImageGenerationDialog' apps/web/src` is empty; record NO-COUNTERPART and
settle bug 150's v5 exposure on THAT fact, correcting the ledger row's
reasoning, which named the CHAT dialog's port); the two `help/` pages
byte-copied at the pin with the embed guard's doc list updated and every
`help_*` content family regenerated at the target and green; `53294163f`
ratified NO-PORT with its `--name-status` list in the lane record and the
`docs/v4/` paths the target moves pre-listed for the unifier (§R.9). This
lane does NOT touch `provider-options-panel.ts` (a needed change there is a
§S.3 STOP), any `crates/*/src` file, or the committed fixtures.

## Survey-verified starting points (fresh survey 2026-09-16 at `53294163f` and on `main` `201ae34d` — do not re-derive)

### v4 (at `53294163f`)

- **`ImageProfileForm.tsx:49`** — `{ value: 'OPENAI', label: 'OpenAI (DALL-E
  / GPT Image)', defaultModels: ['gpt-image-2.5-sunburst', 'gpt-image-2.5-
  flare', 'gpt-image-2', 'gpt-image-1.5', 'gpt-image-1', 'gpt-image-1-
  mini', 'dall-e-3', 'dall-e-2'], apiKeyProvider: 'OPENAI' }` — ⚠ NOTE
  this CLIENT list spells `gpt-image-1` BEFORE `gpt-image-1-mini`, the
  OPPOSITE of the plugin table (§S.2's server order has `1-mini` first).
  The fallback list is the CLIENT file's order — byte-copy THIS one for
  `image-profile-form.ts`; §S.2's server order is the manifest's. Record
  the disagreement (it is v4's own; a candidate upstream nit).
- **`ImageProfileParameters.tsx:96-175`** — the `case 'OPENAI'` fallback
  branch: a new comment ("renders only when that fetch fails — which means
  we cannot know the selected model. The lists below are therefore the
  union across families"); Quality `value={parameters.quality || ''}` with
  options `'' (model default)`, `auto`, `low`, `medium`, `high`, `xhigh`,
  `max`, `standard`, `hd` (labels in the hunk) and the help line `Higher
  tiers cost more and take longer`; Style `|| ''` with `(model default)`
  first and `DALL-E 3 only; the GPT Image models ignore it`; Default Size
  `|| ''` with `(model default)`, `auto`, `1024x1024`, `1536x1024`,
  `1024x1536`, `1792x1024`, `1024x1792`, `2560x1440`, `1440x2560`,
  `3840x2160`, `2160x3840` and `Default image dimensions. Sizes above
  1536x1024 need GPT Image 2 or 2.5.` **v5 never ported this branch** (the
  modal header lists it under STILL DEFERRED; v5 shows the JSON textarea
  for OPENAI when no schema arrives) — so the refresh is a NO-PORT on a
  recorded divergence, and the divergence note must now cite `d8d2890ee`.
- **`image-generation-dialog.tsx`** — `GenerationOption.quality` widened to
  the eight + `''`; `OPENAI_SIZES` twelve (`auto` first, `256x256`…
  `2160x3840`); `OPENAI_QUALITIES` nine `{value,label}` with `''`
  `(model default)` first; initial and reset `quality: ''`; the select
  maps `OPENAI_QUALITIES`. This is the dialog reached from `CharacterEdit
  View` and `avatar-selector.tsx` (per bug 150's filing), NOT the chat's
  `GenerateImageDialog` — v5's `images/generate-image-dialog.ts` is the
  CHAT one's port and `standalone-generate-image-dialog.ts` the in-chat
  gutter one; neither carries a quality/size control (grep `quality` in
  both: no hits). MEASURE and record.
- **`help/image-generation-profiles.md`** — the model line (`:153`) gains
  the two 2.5 ids; the OpenAI provider section (`:250-275`) rewrites
  Sizes/Quality and adds two subsections, "The Two Lamps of GPT Image 2.5"
  and "The GPT Image Output Tray" (Wodehouse register — byte-copy).
  `help/provider-recommendations.md:59` — one sentence appended. Both are
  byte-copies at the pin; 124 files stays 124.
- **`53294163f`** — `git show --stat=220 --format= 53294163f`: `README.md`,
  `package.json`, `package-lock.json`, `packages/quilltap/package.json`
  (`4.10.0-claude-amazing-fermi-3hh73g.44` → `4.10.0-dev.43`), fourteen
  `plugins/dist/*/index.js` (−24k lines net — the openai-SDK bundle churn
  settling). Ratify on the list: v5 consumes plugin SOURCES (`index.ts`,
  `manifest.json`) via the generator, never the bundled `index.js`; the
  `--name-status` list goes in the lane record.

### v5 (on `main` `201ae34d`)

- `image-profile-form.ts:18` — `defaultModels: ['gpt-image-2', 'gpt-image-
  1', 'dall-e-3', 'dall-e-2']` (four; v4 had SIX at the baseline — `gpt-
  image-1.5` and `1-mini` were already missing from v5's copy: a pre-
  existing staleness this lane closes by copying the eight).
- `image-profile-modal.ts` — the header paragraphs at `:75-90`; the fetch
  effect `:595-620`; `optionsSchema` signal `:343`; the template's `@if
  (optionsSchema(); as schema)` `:215`. The panel renders any schema the
  server sends — the port's client half is PROOF, not code.
- `provider-options-schema.ts` — `description?: string` on enum values
  (`:28-33`), `helpText?` on fields (`:56`) and groups (`:96`), `type:
  ProviderOptionFieldType` (`:58`, incl. `'number'`), `enumValues?`
  (`:66`). `nanogpt-options.spec.ts` — the render-spec shape to mirror.
- `apps/web/e2e/settings-image-lora-flow.spec.ts:87-150` — the helpers;
  `:151` the first beat's shape (create → reload → assert); `:205-215` the
  row-0 provider assertion. No beat today selects OPENAI in this modal.
- `crates/quilltap-harness/tests/help_tree_embed_guard.rs:13,33-40` — the
  count + the per-round doc list (append the `53294163f` line: "two
  modified, none added — `image-generation-profiles.md`, `provider-
  recommendations.md`").
- `help/image-generation-profiles.md`, `help/provider-recommendations.md`
  at the repo root — the vendored tree (md5-identical to v4 at
  `1fefadb9a`; the ledger records the two-file drift).
- `docs/v4/developer/IMAGE_GENERATION.md` — the mirror path the target
  moves (+88/−?; pre-list its byte count at the pin for the unifier, §R.9).

## Tiered deliverables

### Tier 1 — must land

1. **`image-profile-form.ts:18`** → v4's eight ids in the CLIENT file's
   order (byte-copy `ImageProfileForm.tsx:49`), spec-pinned; the
   server/client order disagreement recorded.
2. **The modal header rewritten** by NEW text: OPENAI moves out of "STILL
   DEFERRED" into a recorded divergence paragraph naming `d8d2890ee` (v4:
   schema-served, hand-written UNION fallback on fetch failure; v5:
   schema-served, JSON-textarea fallback — a v5 invention kept because
   "a provider whose editor offers nothing at all would be worse than a
   slightly stale size list" applies to it too).
3. **The recorded schema fixture** — a tsx recorder at the target pin
   importing v4's REAL `getOpenAIImageOptionsSchema` and writing the five
   §R.10(c) outputs (`gpt-image-2.5-sunburst`, `gpt-image-2`, `dall-e-3`,
   `dall-e-2`, `undefined`) to ONE committed JSON beside the spec; the
   recorder's invocation in the fixture's header (the recipe-header
   convention). The unifier `diff -q`s this against P4.D196's lane-record
   JSON (§R.10(c)).
4. **`openai-image-options.spec.ts`** — `ProviderOptionsPanel` mounted over
   each recorded schema: field keys/labels/order, the `(model default)`
   blank first on every enum, `xhigh`/`max` `description`s present on the
   2.5 schema and ABSENT on `gpt-image-2`'s, the experimental
   `description` on exactly the two >2560×1440 sizes, `style` only on
   `dall-e-3`, the `GPT Image Output` group (with its `helpText`) only on
   the `gptImage` families, `output_compression` rendered as a number
   input with NO min/max attributes, the no-model schema equal to
   sunburst's. Mutation proofs: (M1) drop `xhigh` from the fixture → the
   2.5 assertions redden; (M2) add `min: 0` to the number field → the
   no-bounds assertion reddens; (M3) move `style` into the `gpt-image-2`
   schema → its absence assertion reddens.
5. **The gated live beat** `settings-image-openai-options-flow.spec.ts`
   (`P4D196_SERVER_LANDED = false` at authoring — the standard shape; the
   unifier flips it): unlock → Images tab → Image Profiles card → New →
   provider `OPENAI` (assert the select VALUE after render — the row-0
   class) → an OPENAI key (measure the seed; seed via the API if absent)
   → model `gpt-image-2.5-sunburst` → assert the `Image Parameters` group
   (Quality options include `max`; Default Size 13 + blank) and the `GPT
   Image Output` group (four fields) → switch model to `dall-e-3` → assert
   Style present, `GPT Image Output` absent, Quality has exactly
   `standard`/`hd` + blank → switch back, set the five values, Save →
   reload → open the profile → each control reads back its value AND
   `imageProfileGet` (through the dispatch client, as the LoRA beat reads)
   carries `parameters.quality === 'max'`, `background`, `output_format`,
   `output_compression: 80` (number, not string — measure what the panel
   stores), `moderation` → delete the profile. The beat's first execution
   is the unifier's (§R.10(d)) — write it so a wrong locator fails LOUDLY,
   not vacuously (`a-disjunctive-assertion-can-be-unfalsifiable`).
6. **The two `help/` pages byte-copied** at the pin (`cmp` against `git
   show 53294163f:help/<file>`), the embed guard's doc list appended
   (124 stays 124), every `help_*` content family regenerated at the
   target through the sweep driver and run by name, zero SKIP (the
   P4.D194 record names which move: `help_tree_equivalence` at least;
   record the whole set you ran).
7. **`53294163f` NO-PORT-ratified** — the `--name-status` list in the lane
   record with the one-line reason per class (version markers; bundle
   rebuilds v5 never imports); the `docs/v4/` mirror paths + byte counts
   for the unifier (§R.9).

### Tier 2 — should land

8. **The manual-dialog measurement** (bug 150 / the `image-generation-
   dialog.tsx` hunks): confirm v5 has no twin (the character editor's and
   avatar selector's image-upload dialog in v5 — does it offer a Generate
   tab at all?); record NO-COUNTERPART with the grep and the v5 surface
   inspected; correct the ledger row's reasoning by NEW record ("the
   ledger named `generate-image-dialog.ts`, the chat dialog's port; the
   v4 file is the upload dialog's, which v5 never ported").
9. **The two in-chat generate dialogs** (`generate-image-dialog.ts`,
   `standalone-generate-image-dialog.ts`) measured against their v4
   sources at the pin: `git diff 1fefadb9a..53294163f -- components/chat/
   GenerateImageDialog.tsx <the gutter dialog>` — expected EMPTY; record.
10. A `qt-class` guard run + `npm run lint` clean is part of the gate; if
    the beat or spec needs a new `qt-*` class, STOP — v4's markup carries
    none new here.

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **Porting the OPENAI hand-written fallback branch** — NOT ported; the
  textarea stand-in stays as a recorded divergence (item 2). If the human
  wants v4's union lists instead, that is a separate small order.
- **The server half** — P4.D196's; a beat that needs anything from the
  server beyond the existing `options-schema` action is a §S.3 STOP.
- **`provider-options-panel.ts`** — untouched; a needed change is a STOP
  recorded for the unifier (it would mean v4's schema uses a shape the
  P4.D84 renderer cannot draw — measure first; the survey says it can).
- **The `docs/v4/` mirror** — the unifier's (§R.9).

## The differential requirement, restated

The client half's oracle is v4's REAL `getOpenAIImageOptionsSchema` run at
the pin (item 3 — the recorder is the differential's v4 side; the render
spec is the v5 side, exact), plus the `help_*` content families (v4's real
help loader over the vendored tree at the pin — the P4.D194 recipe). The
live beat is the wiring proof the specs cannot give (a server-declared
schema reaching the modal's `@if` arm and the saved bag). No Rust unit is
ported here; the embed guard's edit is a doc list only. Closest recipes:
the P4.D194 lane record (help byte-copy + guard), P4.D102's SPA half
(the transcribed schema spec fixture — yours is recorded, which is
stronger), P4.D139's gated beats (`P4D138_SERVER_LANDED`).

## Fixtures

Consumes: the e2e instance's seed (`courier-images-*` on `main`'s vintage
— P4.94 widens it in its own lane; your full Playwright run is on `main`'s
copy, the unifier's is on the widened one). Delivers: the committed
recorded schema JSON beside the spec (five inputs). No committed `.db`
fixture is touched.
## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `53294163f` GPT-Image-2.5 drift catch-up + maintenance round:
**P4.94 ∥ P4.D196 ∥ P4.D197 ∥ P4.95**, ordered 2026-09-16. Four lanes, four
worktrees, four lane branches, ALL from `main` (no stacking — every lane's
ownership is disjoint by file, so nothing needs a substrate commit). The
unifier cherry-picks in the order **P4.94 → P4.D196 → P4.D197 → P4.95** (the
fixture heal FIRST so every later family runs over the widened pairs; the
drift's server half before the SPA half that reads its schema; the
orchestrator carry last), gates ONCE, and `merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`1fefadb9a`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`53294163f`** ("docs:
  Update version", `4.10.0-dev.43`, 2026-09-16 21:15) — TWO commits past the
  baseline: `d8d2890ee` (PR #62 — GPT Image 2.5 + the full OpenAI image
  parameter set, carrying v4 bugs 148 + 149, **both of which v5 measurably
  HAS**) and `53294163f` (a version stamp + a rebuild of the fifteen bundled
  `plugins/dist/*/index.js`, a NO-PORT? candidate). The ledger's two §3 rows
  are now `ORDERED(P4.D196, P4.D197)` and `ORDERED(P4.D197)`. v4 `bugfix`
  tip `1a2b2164c` and `release` tip `8fbf2afe0` are unmoved; there is still
  no `release: 4.10.0` squash. The checkout sat on `main`, CLEAN, at the
  planning-START probe (2026-09-16; the §2 probe PASSED, so the ledger
  stood and nothing was re-derived). ⚠ **At the planning-CLOSE probe the
  checkout had gone DIRTY with a v4 bug-150 fix in flight** — HEAD still
  `53294163f`; uncommitted: `components/images/image-generation-dialog.
  tsx` (M), `__tests__/unit/image-generation-dialog.test.ts` → `.tsx`
  (RM), `docs/developer/bugs/bug-150-…md` → `bugs/fixed/` (RM),
  `docs/developer/bugs.md` + `docs/CHANGELOG.md` (M). Recorded in the
  ledger's §1 at ordering. It is CLIENT-only dirt on a file v5 has NO twin
  for (P4.D197 measures exactly that), so no lane's regen imports it and
  no lane chases it; when the human commits it, `/driftcheck` classifies
  the new row (expected NO-COUNTERPART for v5). **At unification the
  baseline MOVES to
  `53294163f`**; `d8d2890ee` retires ABSORBED(P4.D196, P4.D197) and
  `53294163f` NO-PORT-RATIFIED(P4.D197) on its file list.
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log 53294163f..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Pass = branch `main`, HEAD `53294163f`, both logs EMPTY, and a
  tree that is either CLEAN or dirty in EXACTLY the five bug-150 paths the
  ledger's §1 records (§R.1).** Any OTHER mismatch — a moved HEAD, dirt in
  any other `lib/`/`app/`/`components/`/`packages/`/`plugins/`/`help/`/
  `public/schemas/` path, the checkout off `main` — is a STOP: report it
  and do nothing else. (The recorded dirt is the reason the pin is not
  optional this round even for baseline-pinned lanes — a bare-checkout
  regen would import an uncommitted client edit.) A lane never writes the
  ledger.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1). HEAD is past the
  baseline and `d8d2890ee` moves `lib/`, `app/`, `components/`, `plugins/`
  and `help/`. Every regen runs from a LANE-UNIQUE detached worktree per
  ledger §5.1 (`git -C ~/source/quilltap-server worktree add --detach
  /tmp/qt-v4-pin-<order>-<sha> <sha>`, the THREE symlink classes — root
  `node_modules`, `packages/quilltap/node_modules`, every
  `plugins/dist/*/node_modules` — then `cd` into it; never stash or
  checkout-switch the human's repo; `git worktree list` before each batch;
  verify the pin by grepping the fresh NDJSON for a marker only one tree
  has; check the file is non-empty before believing a diff). Which sha:
  - **P4.D196 and P4.D197 pin the TARGET `53294163f`** (v4 HEAD — it carries
    `d8d2890ee`'s code AND the settled plugin bundles the manifests
    generator imports) for every family their port MOVES, and the BASELINE
    `1fefadb9a` for their neutrality legs (the pre-existing image-dialect
    rows, the untouched provider corpora, the `help_*` families' unchanged
    pages). The target-pinned image-dialects oracle MUST carry a
    `gpt-image-2.5` row and the baseline one MUST NOT — that pair is the
    pin verification.
  - **P4.94 and P4.95 pin the BASELINE `1fefadb9a`** — their v4 surfaces are
    untouched by either drift commit (each lane records `git -C
    ~/source/quilltap-server diff --stat 1fefadb9a..53294163f -- <its v4
    paths>` EMPTY in its lane record before the first regen). A pin at the
    baseline is still REQUIRED, not optional: the checkout can go dirty
    mid-lane (ledger §5.1), and a bare-checkout regen is unrepeatable —
    and today a bare-checkout regen imports PR #62's `generate_image`
    definition, whose bytes have moved.
  The sweep driver is the sanctioned per-family path
  (`harness/tools/recipe_sweep.py --run <family> --v4 "$PIN" --v5w
  <ABSOLUTE worktree>`); never run two sweeps concurrently; a family that
  reports `SKIP` in 0.00 s did not run (`a-family-env-var-is-not-its-
  regen-var`). Ledger §5.2's silent-stale-pass discipline applies to
  every regen (`rm -f` the fixture first; read the builder's last line;
  `grep -c` the changed bytes in the fresh NDJSON — MUST be > 0). **Shared
  `/tmp` output paths are a live hazard this round** (P4.92's mid-lane
  clobber): every lane stages its regen outputs under a LANE-PRIVATE
  directory (`/tmp/<order>/…`) and leaves the committed recipe headers
  canonical.
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). The spec
  for every item is `git show <sha> -- <path>` plus `git show <sha>:<path>`
  at `53294163f`; a commit message describes the bug as its author
  understood it and the hunks are often narrower. PR #62's message is
  three squashed commits of prose — the third ("fix(images): share one
  quality schema…") REVISES the first, so read the post-commit FILES, not
  the narrative. Re-verify even where an order flags the trap — the
  order's paragraph is the same kind of prose. Carry a v5-side code
  comment naming the sha where the prose misleads.
- **§R.5 — Designed trips and pre-existing reds: measure, don't assume**
  (ledger §5.4). The tool-definition family (`tool_definitions_
  equivalence`) goes RED at the target pin BY DESIGN (three `default` keys
  removed, two enums widened, three descriptions rewritten) — regenerate
  and copy the bytes, never hand-edit them. The six fixture-vintage reds
  P4.94 heals are PRE-EXISTING (proven by the `1fefadb9a` round's revert
  probes) — a red that SURVIVES the widen is a NEW finding to classify at
  both pins before any source is touched, and it is probably not P4.94's.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest`
  is shared) and its silence leg; a fixture change lands with the regen
  of every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that
  SURVIVES as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`).
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed (and `apps/web/package.json` when it touches the SPA) on each
  commit per `.claude/commands/commit.md`; the unifier RECOUNTS as base +
  the sum of every lane's bumps (the silent-auto-merge trap: identical
  bumps merge as ONE — it fired again two rounds ago). Base at planning:
  core 0.0.935, harness 0.0.827, web 0.0.148, host 0.0.137, cli 0.0.22,
  tauri 0.0.7, SPA 0.5.727. Expected: **core** by P4.D196 and P4.95 ONLY;
  **harness** by P4.D196, P4.D197 (the embed guard's doc list) and P4.95,
  and by P4.94 ONLY if it touches a harness-crate file (expected NOT — its
  fixtures live under `crates/quilltap-web/tests/fixtures/` and its
  migrator under `harness/oracle/fixtures/`, which is not a crate);
  **web** by P4.94 (the committed fixtures are the web crate's) and by
  P4.D196 ONLY if its Tier-2 measurement makes it touch the web edge's
  generate body arm (say so in the lane record); **SPA** by P4.D197 ONLY;
  **host/cli/tauri** by NOBODY (a `help/**` re-vendor changes the
  embedded table `quilltap-host`'s `build.rs` produces but no host
  SOURCE — the P4.D194 precedent: no host bump for help alone).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  P4.D197 pre-lists the paths the target moves (`docs/developer/
  IMAGE_GENERATION.md` at least, with byte counts at the pin) in its lane
  record; no lane copies them.
- **§R.10 — Cross-lane handoffs, named:** (a) **P4.94 widens the committed
  `images-{main,mount}.db` pair that P4.D196's `images_generate_route_
  equivalence` (the `max`-quality row) RUNS over** — run vs edit is not a
  collision; P4.D196 never edits the pair, P4.94 never edits that family's
  sources; the unifier re-runs P4.D196's route families over the widened
  pair after the picks. (b) **P4.D196 owns every recorded provider corpus
  under `harness/oracle/fixtures/`** (its SDK wire re-check regenerates
  them ALL at the target pin); P4.95 therefore pins its wire keys in the
  `orchestrator_tier3` side-channel and NOT in the request-envelopes
  corpus — the corpus pin is P4.95's Tier-3 deferral, recorded with the
  six-key table for a later wire. (c) **P4.D196 produces the OpenAI
  options-schema JSON its differential asserts; P4.D197 records the SAME
  function's output at the SAME pin as a spec fixture** — both from v4's
  REAL `getOpenAIImageOptionsSchema` — and the unifier `diff -q`s the two
  recordings for `gpt-image-2.5-sunburst`, `gpt-image-2`, `dall-e-3`,
  `dall-e-2` and the no-model call (the §S contract). (d) P4.D197's e2e
  beat is GATED by the named constant `P4D196_SERVER_LANDED = false` and
  flipped at unification — its first live run is the unifier's (a beat
  authored while gated has never executed). (e) P4.94 RUNS
  `salon_reads_equivalence` and the `help_*`-free salon families over the
  widened pair; P4.D197 RUNS the `help_*` content families; neither edits
  the other's — the unified gate re-runs both sets. (f) P4.95 stages its
  orchestrator fixture under a lane-private path (`/tmp/p4.95/`) — the
  committed recipe's `/tmp/qt-orch-*.db` paths were clobbered by a sibling
  last round (P4.92 §5); the unified gate regenerates that family itself
  and trusts no artifact either lane left in the shared paths.
- **§R.11 — The e2e seed is a committed fixture this round touches.**
  `apps/web/e2e/support/seed-courier-fixture.ts:124-127` copies the
  committed `courier-images-{main,mount}.db` pair into the Playwright
  instance — so P4.94's widen of that pair changes the base every beat
  runs on. P4.94 runs the FULL Playwright suite at lane close (release
  binary, `npm ci`'d SPA worktree), and the unified gate runs it again.

## §S — Shared contract (binding; byte-identical in P4.D196 and P4.D197)

- **§S.1 — The OpenAI image options schema is v4's `getOpenAIImageOptionsSchema(modelName)` byte-for-byte**, served by the EXISTING `imageProfileOptionsSchema` verb / `GET /api/v1/image-profiles?action=options-schema&provider=OPENAI&model=<id>` edge (P4.D138 unit 6 — the wire shape `{provider, model, optionsSchema, loraSupport}` is UNCHANGED). The schema's fields, in v4's order: group `Image Parameters` → `quality` (enum, `default: ''`, `(model default)` first, then the family's tiers with v4's labels; `xhigh`/`max` carry a `description`), `size` (enum, `default: ''`, `(model default)` then the family's sizes labelled `Square (W×H)` / `Landscape (…)` / `Portrait (…)` with a U+00D7 `×`, `auto` labelled `Auto — the model chooses the shape`, the two >2560×1440 sizes carrying the experimental `description`), and `style` (enum) ONLY when the family `supportsStyle`; then group `GPT Image Output` (with v4's `helpText`) ONLY when the family is `gptImage`: `background` (enum), `output_format` (enum), `output_compression` (`type: 'number'`, NO min/max, `helpText` only), `moderation` (enum). An unknown or absent model gets the FIRST table entry's schema (`gpt-image-2.5-sunburst`). `loraSupport` for OPENAI stays what it is today. Both lanes record this function's output at the target pin for the five inputs named in §R.10(c); the unifier diffs the recordings.
- **§S.2 — `imageProviderList`'s OPENAI `defaultModels` becomes the eight ids in v4's table order**: `gpt-image-2.5-sunburst`, `gpt-image-2.5-flare`, `gpt-image-2`, `gpt-image-1.5`, `gpt-image-1-mini`, `gpt-image-1`, `dall-e-3`, `dall-e-2` — NOTE `gpt-image-1-mini` precedes `gpt-image-1` in v4's table, the reverse of every old v5 list. The server side arrives through the manifests regen (P4.D196 — `openai.json`'s `imageGenerationModels`); the SPA's offline fallback list (`image-profile-form.ts:18`) is P4.D197's and must spell the same eight in the same order.
- **§S.3 — Nothing else on the wire moves.** No new dispatch verb, no new REST route, no `core-contract.ts` type change is expected from this drift; a lane that finds it needs one STOPs on that item and records the shape for the unifier rather than inventing a contract its sibling cannot see.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.94** — the fixture-vintage heal | The committed pairs ON DISK: `crates/quilltap-web/tests/fixtures/{salon-main,salon-mount,salon-llm-logs,salon-long-main,salon-long-mount,chat-gallery-main,chat-gallery-mount,images-main,images-mount,courier-images-main,courier-images-mount,courier-images-llmlogs,pascal-run-custom-main,pascal-run-custom-mount}.db` (the `salon-long-*` and `-llmlogs` members ONLY if the measurement says they need a column; the `.meta.json` sidecars are NOT rewritten); `harness/oracle/fixtures/migrate-memories-fixture-columns.ts` (the `MIGRATIONS` list + the target-pair list + its header); `crates/quilltap-harness/tests/salon_fixture_p4d171_ensure.rs` — its header comment ONLY if the widen makes its premise stale (say so); `harness/tools/sweep-results/<date>-p4.94-*.json`; this order's header | every harness family's `.rs`/`.test.ts` (it RUNS them — P4.D196 owns the image route families' sources), every `crates/*/src/**` file (incl. `test_support.rs` — the per-copy heals STAY as no-ops), `apps/web/**` (it RUNS Playwright), `help/**`, the drift ledger |
| **P4.D196** — the `d8d2890ee` drift, server half | `crates/quilltap-core/src/model/image_dialects.rs`; NEW `crates/quilltap-core/src/model/openai_image_models.rs` (the capability table + `find_image_model` + `check_arbitrary_size` + `mime_type_for_format` + the options-schema builder — or two modules, say which); `crates/quilltap-core/src/model/nanogpt_catalog.rs` — the `image_provider_options_schema` dispatcher (`:376-381`) ONLY (or its relocation to a neutral module — say which); `crates/quilltap-core/src/image_gen_data.rs` — the OPENAI block; NEW `crates/quilltap-core/src/image_gen/quality.rs` (the one quality list); `crates/quilltap-core/src/tools/generate_image.rs`; `crates/quilltap-core/src/tools/definitions/data.rs` — the `generate_image` `ToolDef` ONLY; `crates/quilltap-core/src/api/images.rs` — the `quality` arm at `:1461` ONLY; `crates/quilltap-core/src/api/image_profiles.rs` — `image_profile_generate` ONLY if the Tier-2 measurement requires (say so); `crates/quilltap-web/src/**` — the `?action=generate` body arm ONLY if that measurement requires (say so); `crates/quilltap-core/src/provider_manifest/manifests/*.json` (the generator's output — ALL eleven rewritten, only `openai.json` expected to move); every file under `harness/oracle/fixtures/` that a recorder writes (`image-dialects/`, `request-envelopes/`, `google-wire/`, `response-bodies/`, `stream-fixtures/`, `tool-wire/`, `moderation-wire/`, `web-search-wire/` — the SDK re-check regen) + `harness/oracle/providers/record-image-fixtures.mjs` (new cases); `harness/oracle/fixtures/image-generation.json` + `build-image-generation-fixture.ts` (if a GPT-Image profile is needed); `harness/oracle/cases/{tool-definitions,tool-definitions-canonical,image-generation.test,image-generate-route.test,images-generate-route.test,image-profiles-routes.test,provider-registry}.ts`; `crates/quilltap-harness/tests/{image_dialects,tool_definitions,image_generation_tier3,image_generate_route,images_generate_route,image_profiles_routes,provider_registry}_equivalence.rs`; this order's header | the committed fixture pairs on disk (P4.94's — it COPIES `images-*.db`, never edits), `apps/web/**`, `help/**`, `services/orchestrator.rs` + the tool loops (P4.95's), every other harness family, the drift ledger |
| **P4.D197** — the drift's SPA half + help + the NO-PORT ratification | `apps/web/src/app/screens/settings/images/image-profile-form.ts` (the fallback list); `apps/web/src/app/screens/settings/images/image-profile-modal.ts` + `.spec.ts` (the header's stale OPENAI deferral paragraph; any measured behaviour change); NEW `apps/web/src/app/screens/settings/images/openai-image-options.spec.ts` + its recorded fixture JSON; the SPA-side recorder for that fixture (beside the existing SPA recorders — `spa-oracle-recorders-live-outside-src`); NEW `apps/web/e2e/settings-image-openai-options-flow.spec.ts` (gated); `help/image-generation-profiles.md` + `help/provider-recommendations.md` (byte-copies at the pin); `crates/quilltap-harness/tests/help_tree_embed_guard.rs` — the doc-comment vendor list ONLY; `apps/web/package.json` version; this order's header | every `crates/*/src/**` file, every other harness family (it RUNS the `help_*` content families), every other SPA file (incl. `provider-options-panel.ts` — if the renderer needs a change for v4's schema, STOP and record: that is a §S.3 event), the drift ledger |
| **P4.95** — the `cacheKey` primary-path carry | `crates/quilltap-core/src/services/orchestrator.rs` — the `cache_key:` literal (`:2803`), the `:2952-2956` comment, and the `loop_base_params` closure (`:2957-2962`) ONLY; `crates/quilltap-core/src/services/native_tool_loop.rs` + `text_tool_loop.rs` — the production re-stream / force-final / continuation `params` clones ONLY (per the measured asymmetry) + their `#[cfg(test)]` modules; `crates/quilltap-core/src/model/request_builder/{responses_api,chat_completions}.rs` — `#[cfg(test)]` modules ONLY, and only if an emission pin is missing; `crates/quilltap-harness/tests/orchestrator_tier3_equivalence.rs`; `harness/oracle/cases/orchestrator-tier3.test.ts`; `harness/oracle/fixtures/orchestrator-tier3.json` + `build-orchestrator-fixture.ts`; this order's header | `cheap_llm.rs` (import `build_character_cache_key`, never edit), `streaming_provider.rs`, `primary_stream.rs`, `provider_failover.rs`, every recorded provider corpus (P4.D196's), `apps/web/**`, `help/**`, every other harness family, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0),
   and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp`) where the family is deterministic across
   regens; where it is not (`regenerate-at-both-pins-and-cmp-is-not-
   universal`), run by name and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast` with the lane's env block
   (its own oracle vars + `QT_V4_ROOT`/`QT_V4_CHECKOUT` at the right pin +
   `QT_NODE` = the node BINARY): report **binaries / passed / failed /
   ignored** from the per-binary lines and the exit code, and the lane's
   families confirmed RUN by non-zero duration. **The six fixture-vintage
   families (`salon_mutations`, `chat_gallery`, `images_routes`,
   `courier_images_routes`, `pascal_run_custom_handler`,
   `pascal_custom_tools_route`) are RED on `main` until P4.94 lands** —
   P4.D196 / P4.D197 / P4.95 withhold those six oracle vars from their
   workspace block BY NAME and say so (they SKIP-pass without them);
   P4.94 sets all six and reports them green.
9. **SPA lanes only (P4.D197; P4.94 for Playwright per §R.11):** `npm run
   lint` (incl. the qt-class guard), `npm test` (spec files / tests / 0
   failed), `npm run build` clean, then the lane's e2e beats by FILE
   against the release binary, and ONE full Playwright run at lane close
   (numbers recorded; the documented intermittents — the P4.D161
   pause-toast beats, the P4.d17 quill beat, the `salon-documents-flow` /
   `workspace-flow` terminal trio — are named, re-run by file, and never
   re-classified as this lane's; the standing parks — the five P4.D187
   title-checkpoint beats + the gallery store-probe — stay parked).
10. **Ownership check:** `git diff --stat main...HEAD` — every path is in
    the lane's Owns column; every MUST-NOT-TOUCH path absent.
11. **Per commit:** `.claude/commands/commit.md` in full — the equivalence
    test is non-negotiable (§1), the invariants (§2), real-instance safety
    (§3), the harness regen (§4), build/test/lint (§5), the CHANGELOG
    entry + version bumps (§7). Commit on the lane branch only; never
    merge to `main`; never `git stash`; never a worktree inside a worktree.
12. **Lane close:** update this order's status header (what landed, what
    was refuted by measurement, what stays OPEN with its named cause),
    append the lane record to `status-log.md` (the regen commands as run,
    the counts, the mutation table, every finding outside the lane's
    ownership for the unifier), and stop. The final gate runs in the
    FOREGROUND of the lane's last turn — a backgrounded gate whose
    notification arrives after the lane reports done is a lane that is
    NOT done (`a-backgrounded-sleep-does-not-gate-your-turn`).
