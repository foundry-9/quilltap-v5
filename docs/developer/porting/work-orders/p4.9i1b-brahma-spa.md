# Work order P4.9I1B — the Brahma SPA lane: the console dialog family, the streaming consumer, and the brahma tab

> **STATUS: OPEN** (round planned 2026-07-20). The unifier updates this header.

> **Preamble.** v4 reference baseline: **`7e6d13e5`** (4.8.0-dev.92). At lane
> start run `git log 7e6d13e5..HEAD --oneline` in `~/source/quilltap-server`.
> ⚠ v4 is **mid-release-checklist** — a 4.8.0 tag/version commit may land any
> day. If HEAD moved, **STOP and report** — do not absorb surprise drift
> mid-lane.
>
> **Sibling lanes:** `p4.9i1a-brahma-server.md` (the Brahma server — §B binds
> you two together; its verbs do not exist until unification, so everything
> here lands **inert-in-lane** with guarded ACTIVATE-AT-UNIFY beats),
> `p4.9j3-wardrobe-tab-riders.md`, `p4.9j4-document-standalone-spa.md`. This
> lane touches ONLY `apps/web/` (plus the append-only docs). Run in your OWN
> worktree on your own branch; Playwright runs ALONE on a dedicated port with
> THIS worktree's own Rust binaries built once (the `p4.6af-files-family-spa`
> memory). Remember: e2e mutation checks prove nothing until `ng build`
> rewrites the bundle (the `e2e-mutation-check-needs-a-rebuilt-dist` memory).

## Mandate

Port v4's Brahma Console **UI half** into the Angular SPA: the console
dialog family (`BrahmaConsoleDialog` in both `asTab` and floating modes,
`BrahmaConsoleView`, `BrahmaConsoleMessageList`, `BrahmaToolCall` + the
`brahma-sql-tool-call` parser, `ModelPicker`), the provider-service
analogue (current-chat persistence + eligibility), the streaming consumer
over the shared `chat-stream.reducer`, the `HelpChatComposer` analogue +
the `qt-help-*` style dependency, the lane-local §B wire client, and the
rail entry component. The tab kind `brahma` swaps from its permanent
refusal pane to the real view AT UNIFY (§W.2); the e2e beats are
ACTIVATE-AT-UNIFY over lane P4.9I1A's server.

Must NOT touch: any Rust source, `core-contract.ts` (§W.3 — wire types go
in the lane-local `brahma/brahma-wire.ts`), the frozen files in §W
(workspace contract / tab registry / not-wired pane / shell), the
chat-stream reducer's semantics (`chat-stream.reducer.ts` — its
most-recent-batch `toolResult` bookkeeping is verified-compatible with the
wire; consume it, don't change it), and anything under `screens/**` owned
by the sibling SPA lanes (P4.9J3: characters/wardrobe/wizard files;
P4.9J4: `documents/**`).

## Survey-verified starting points (2026-07-20 @ v4 `7e6d13e5` — do not re-derive)

### The v4 UI family — `components/brahma-console/`

| File | LOC | Notes |
| --- | --- | --- |
| `BrahmaConsoleDialog.tsx` | 325 | the whole console; prop `{ asTab?: boolean }` (L50) |
| `BrahmaConsoleMessageList.tsx` | 239 | reuses the `qt-help-*` bubble styles |
| `BrahmaToolCall.tsx` | 143 | run_sql result cards; other tools render silent |
| `ModelPicker.tsx` | 99 | connection-profile picker |
| `brahma-sql-tool-call.ts` | 83 | the SQL tool-call parser |
| `hooks/useBrahmaConsoleStreaming.ts` | 238 | the SSE reader |
| `BrahmaConsoleView.tsx` | 16 | `<BrahmaConsoleDialog asTab />` — the workspace-tab entry |
| `__tests__/brahma-sql-tool-call.test.ts` | 136 | **port case-for-case** |

- **`asTab` mechanics** (`BrahmaConsoleDialog.tsx:297-309`): when true,
  render the body **bare** with an inline right-aligned header (model
  picker + "new conversation"), no `FloatingDialog`, and stay live
  regardless of the provider's `isOpen` (the past-chats query is
  `enabled: (isOpen || asTab) && !currentChatId`, L113). Otherwise wrap in
  `FloatingDialog` (L311).
- **The provider** (`components/providers/brahma-console-provider.tsx`,
  162 LOC): character-less and NOT page-aware (no pathname tracking —
  the header comment at L4-13 is the spec). State: `isOpen`,
  `currentChatId` (persisted to localStorage — port the EXACT storage
  key from the file), `activeConnectionProfileId` (+ `setModel`, which
  PATCHes `?action=set-model` then updates local state), `profiles` via
  `useConnectionProfiles`, and **`isEligible: profiles.length > 0`**
  (L137) — no other eligibility machinery.
- **`HelpChatComposer`** (`components/help-chat/HelpChatComposer.tsx`,
  76 LOC): textarea + send button on the `qt-help-composer*` classes;
  props `{ onSend, disabled?, placeholder?, inputRef? }`; Enter sends,
  Shift+Enter newline, auto-grow to 120px. The console imports it
  directly (Dialog L20/264/286) — **this lane carries the composer and
  the styles even though HelpChat itself stays deferred (p4.9i2)**.
- **The `qt-help-*` styles** live in v4
  `app/styles/qt-components/_surfaces.css`: `qt-help-avatar`,
  `qt-help-tail`/`-user`/`-assistant`, `qt-help-msg-user`/`-assistant`,
  `qt-help-error`, `qt-help-section-label`, `qt-help-past-chat`,
  `qt-help-composer*`. VERIFY whether v5's ported `_surfaces.css` already
  carries the block; port it if absent (this lane owns that style block).
- **The message list** renders `MessageContent`, `ThinkingBlock`
  (reasoning display), and `BrahmaToolCall`; avatar is
  `Icon name="brahma-console"`.
- **The entry point** (`components/layout/left-sidebar/
  sidebar-footer.tsx:213-226`): `{brahmaConsole && (<button …>)}`;
  onClick: `if (inWorkspace) workspace!.openTab('brahma'); else
  brahmaConsole.openConsole();`. Disabled unless `isEligible`; icon
  `brahma-console`.
- **The streaming hook's batch bookkeeping**
  (`useBrahmaConsoleStreaming.ts:150,163-166`): batch base captured on
  `toolsDetected` (`batchBaseRef.current = toolCallsRef.current.length`),
  then `toolResult` maps `batchBaseRef.current + index` into a flat
  run-wide accumulator — tool cards persist across all agent turns.

### The v5 side today

- **Nothing UI-side exists.** The waiting plumbing: `ui/icon.ts:22`
  (`'brahma-console'` in `IconName`), `styles/qt-components/_icons.css:132`
  (the mask entry → `/images/icons/brahma-console.svg` — VERIFY the SVG
  asset ships in v5's public images; copy it from v4 if missing). Two
  orphan `chatType: 'brahma'` literals in `core-contract.ts` (L1706
  `EnrichedChat`, L1925 `ChatDetail`).
- **The shared stream reducer**
  (`apps/web/src/app/core/chat-stream.reducer.ts`): already models all
  seven frames. Its `toolResult` handling (doc L17, `applyToolResult`
  L367-385) targets `batches[batches.length-1]` and treats `index` as
  batch-local with a name fallback — **same wire semantics as v4,
  different (batch-aware, already-multi-turn-correct) client
  bookkeeping**. Consume it as-is; do NOT port v4's flat
  `batchBaseRef` arithmetic.
- **The workspace side:** contract `:40` declares payload-less `brahma`;
  registry `tab-registry.ts:147` renders the permanent refusal
  (`not-wired-pane.ts:44-45,57-58` names p4.9i1). Both files are
  unifier-only (§W.2) — your view swaps in at unification.
- The e2e instance has a canned LLM provider (the salon send beats run
  live against it) — a live console send beat is feasible once the
  server lane lands.

## Tiered deliverables

### Tier 1 — must land

1. **The wire client** — `apps/web/src/app/brahma/brahma-wire.ts`
   declaring the §B DTOs (lane-local; folded at unify) + a dispatch
   client service (`BrahmaConsoleApi`) with one method per verb. Derive
   response DTOs from the v4 handlers at the §B anchors.
2. **The provider-service analogue** — `BrahmaConsoleService`:
   `isOpen`/open/close (floating mode), `currentChatId` with v4's exact
   localStorage key, `activeConnectionProfileId` + `setModel` (PATCH
   then local update), `profiles` from the existing connection-profiles
   surface, `isEligible = profiles.length > 0`.
3. **The component family** — `qt-brahma-console-dialog` (both modes per
   the asTab mechanics above; floating mode on the established
   `qt-dialog-overlay` pattern), `qt-brahma-console-view` (the asTab
   wrapper the tab registry will mount), the message list on the
   `qt-help-*` styles, `qt-brahma-tool-call` + the `brahma-sql-tool-call`
   parser (port v4's 136-LOC test case-for-case), the model picker, and
   the `HelpChatComposer` analogue (`qt-help-composer`) + the style block
   if absent. Follow the async-options select rule (`[selected]` per
   option, never `[value]` on a select with async options).
4. **The streaming consumer** — subscribe the console to the global
   event stream scope-tagged by `chatId`, folding frames through the
   shared `chat-stream.reducer` (all seven frames; reasoning is
   cumulative-replace; tool cards persist across turns via the reducer's
   batch list).
5. **The rail entry component** — a self-contained
   `qt-brahma-entry` (eligibility-gated, disabled otherwise; workspace
   arm `openTab('brahma')` via the injected workspace handle, routed arm
   opens the floating dialog) — mounted in `shell.ts` by the UNIFIER
   (§W.2 AT-UNIFY).
6. **Specs + beats** — component specs for every unit above;
   ACTIVATE-AT-UNIFY Playwright beats: rail entry → brahma tab renders
   the console; create → send (canned provider) → assistant bubble +
   a run_sql tool card when the canned stream carries one; rename;
   set-model round-trip; delete; past-chats launcher. Guard each beat on
   the server surface being present (the established guarded-probe
   pattern), so they self-activate at unification.

### Tier 2 — should land

7. The past-chats launcher view's full fidelity (section labels,
   `qt-help-past-chat` rows, "new conversation" reset).
8. Reasoning display via the ThinkingBlock-equivalent (v5's existing
   reasoning presentation in the salon — reuse, don't fork).

### Tier 3 — explicit refusal-arm deferrals

- **HelpChat itself stays p4.9i2** (the 9-component family + eligibility/
  context-resolver/categories libs + the help-doc read surface) — this
  lane carries only the composer + styles the console needs. Keep the
  deferral loud in the status header.
- File attachments on the console composer (`fileIds` in send): defer
  loud if v4's console composer doesn't expose them in the UI — verify
  against the dialog before deciding; record the verdict.

## The differential requirement, restated

This is a SPA lane: the server behavior is lane P4.9I1A's differential
obligation. Yours is fidelity at the component tier: port v4's
`brahma-sql-tool-call.test.ts` case-for-case; every component spec
asserts v4's observable behavior (asTab bare render vs floating; the
`enabled: (isOpen || asTab) && !currentChatId` query gate; eligibility
gating; the localStorage persistence key). Where v4 renders markdown
(`MessageContent`), reuse the ported v5 renderer — no new pipeline.

## Fixtures

- Consumes lane P4.9I1A's server at unification only (beats guarded until
  then). The e2e instance's canned provider + a seeded connection profile
  are the send beat's substrate — if the committed e2e fixture lacks a
  connection profile, note it in your AT-UNIFY table; the unifier seeds
  it alongside the registry swap (the `seedPascalToolsFixture` precedent).

## §B — the Brahma wire contract (BINDING — verbatim-identical in P4.9I1A and P4.9I1B)

**Dispatch verbs** (Rust `Request` variant / SPA wire type name):

| Rust variant | SPA type | Fields (camelCase on the wire) | v4 anchor |
| --- | --- | --- | --- |
| `BrahmaConsoleList` | `brahmaConsoleList` | — | `route.ts:37-60` |
| `BrahmaConsoleCreate` | `brahmaConsoleCreate` | `consoleConnectionProfileId?` | `route.ts:65-113` |
| `BrahmaConsoleGet` | `brahmaConsoleGet` | `chatId` | `[id]/route.ts:42` |
| `BrahmaConsoleRename` | `brahmaConsoleRename` | `chatId`, `title` | `[id]/route.ts:69` |
| `BrahmaConsoleSetModel` | `brahmaConsoleSetModel` | `chatId`, + the body field v4 reads at `[id]/route.ts:100` | `[id]/route.ts:100` |
| `BrahmaConsoleDelete` | `brahmaConsoleDelete` | `chatId` | `[id]/route.ts:132` |
| `BrahmaConsoleMessages` | `brahmaConsoleMessages` | `chatId` | `[id]/messages/route.ts:55` |
| `BrahmaConsoleSend` | `brahmaConsoleSend` | `chatId`, `content`, `fileIds?` (v4 `BrahmaConsoleSendOptions`) | `[id]/messages/route.ts:28-53` |

- **Response DTOs mirror v4's route JSON name-for-name** — both lanes
  derive them from the v4 handlers at the anchors above; the tier-2
  differential is the arbiter. A derivation disagreement between lanes is
  a unifier-level event, not something either lane patches around.
- **Send semantics:** the dispatch reply is the typed result of the run;
  the seven stream frames (`content`, `reasoning`, `toolsDetected`,
  `status`, `toolResult`, `done`, `error` — field shapes as surveyed
  above) ride the global Event channel scope-tagged by `chatId`, exactly
  like `ChatSend` (`api/types.rs:270-300`). **`toolResult.index` is
  batch-relative** (v4 `tool-execution.service.ts:161`); v5's shared
  reducer applies it to the most recent batch
  (`chat-stream.reducer.ts:367-385`) — verified-compatible semantics;
  neither side changes them.
- **REST edges** (quilltap-web, v4-path-faithful): `GET|POST
  /api/v1/brahma-console`; `GET|PATCH|DELETE /api/v1/brahma-console/{id}`
  (+ `?action=set-model`); `GET|POST /api/v1/brahma-console/{id}/messages`.
- **Constants:** create's seed title is exactly
  `'A Fresh Audience at the Console'`; `verify_brahma_chat` 404s unless
  owner + `chatType==='brahma'`; eligibility is SPA-side only
  (`profiles.length > 0` — v4 `brahma-console-provider.tsx:137`).
- The SPA lane declares its wire types in a lane-local
  `apps/web/src/app/brahma/brahma-wire.ts`; the UNIFIER folds them into
  `core-contract.ts` and runs the name-for-name wire diff against
  `api/types.rs` (§W.3).

## §W — the round-wide workspace meeting points (BINDING — identical in all four orders)

1. **`apps/web/src/app/workspace/workspace-contract.ts` is FROZEN.** All
   three tab payloads are already declared (`WardrobeTabPayload` :98-100,
   `DocumentStandaloneTabPayload` :73-93, payload-less `brahma` :40). No
   lane edits it; a believed defect in it is a STOP-and-flag.
2. **`workspace/chrome/tab-registry.ts`, `workspace/chrome/not-wired-pane.ts`,
   and `shell/shell.ts` are UNIFIER-ONLY this round.** Each lane ships
   self-contained components and a named AT-UNIFY table in its order; the
   unifier performs the registry row swaps and the shell mount insertions.
   AT-UNIFY rows: `wardrobe` → lane P4.9J3's tab component;
   `document-standalone` → lane P4.9J4's `StandaloneDocumentView`;
   `brahma` → lane P4.9I1B's `BrahmaConsoleView`. Shell mounts: lane
   P4.9J4's documents-picker entry; lane P4.9I1B's brahma entry.
3. **`core-contract.ts` folds at unify.** Lanes adding wire types
   (P4.9I1B: the §B brahma DTOs in `brahma/brahma-wire.ts`; P4.9J4: the
   seven `document*` request types in `documents/standalone-wire.ts`)
   declare them lane-locally; the UNIFIER folds them into
   `core-contract.ts` and runs the name-for-name wire diff against
   `api/types.rs`. No lane edits `core-contract.ts`.
4. **`app.routes.ts` and `workspace/workspace-redirect.guard.ts`: lane
   P4.9J3 single-owner.**
5. **e2e:** each lane adds NEW spec files only; `e2e/workspace-flow.spec.ts`
   is lane P4.9J3's single-owner file. Beats that depend on another lane's
   landing (or on the registry swap) ship as guarded ACTIVATE-AT-UNIFY
   probes, self-activating when the wire lands.
6. **Version bumps:** lane P4.9I1A owns the `quilltap-core` /
   `quilltap-web` / `quilltap-harness` bumps (`quilltap-host` only if a
   genuinely new seam lands — flag first); the three SPA lanes each bump
   `apps/web/package.json` per commit; the unifier accumulates (recount —
   identical bumps merge silently, the `p4.6aw-ax-8-round-plan` memory).
7. **Append-only for everyone:** `docs/CHANGELOG.md`,
   `docs/developer/porting/status-log.md` (they union-merge at
   unification).

## Ownership

This lane's files: `apps/web/src/app/brahma/**` (new), the `qt-help-*` +
console style blocks it ports (in the v5 stylesheet files that mirror v4's
`_surfaces.css` placement), the brahma SVG asset if missing, its new e2e
spec files, and this order's status header. `apps/web/package.json` bumps
per §W.6. Nothing under `crates/**`, `screens/**` files owned by sibling
lanes, or the §W frozen files.

## Verification gate (before declaring done)

- `ng test` fully green (additions only — zero edits to existing specs);
  `ng build` clean.
- Full Playwright ALONE on a dedicated port with this worktree's own
  binaries; the entire pre-existing suite green with zero skips beyond
  this lane's own guarded ACTIVATE-AT-UNIFY beats (which must visibly
  report their guarded state, not silently pass).
- `git diff main -- crates/ Cargo.toml Cargo.lock` empty.
- Per-commit rule: follow `.claude/commands/commit.md`.
