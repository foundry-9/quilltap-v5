# Work order P4.9J1 — the workspace lane (lane J1): the tabbed-workspace core, chrome, and shell cutover

> **STATUS: OPEN** (updated by the unifier when the round lands)

> **Preamble.** v4 reference baseline: **`b8b12695`** (4.8.0-dev.76). At lane
> start run `git log b8b12695..HEAD --oneline` in `~/source/quilltap-server`.
> One commit past baseline is **already dispositioned**: `70baaa74` (docs-only —
> the `state-cascade.md` feature *plan*; no `lib/` change; mirrored under
> `docs/v4/developer/features/`). If HEAD moved beyond `70baaa74`, **STOP and
> report** — the state-cascade plan predicts an in-flight v4 feature, exactly
> like the custom-tools feature before the d68638b4 round; do not absorb
> surprise drift mid-lane. If the v4 tree is dirty, regenerate corpus fixtures
> from a pinned detached worktree (the `oracle-regen-pinned-v4-worktree`
> memory: `git worktree add --detach`, symlink BOTH `node_modules`).
>
> **Sibling lane:** `p4.9j2-screen-hostability.md` (screen adaptation). This is
> a TWO-lane, **pure-SPA** round — **neither lane touches `crates/**` or
> `harness/` Rust**; the only Rust-adjacent artifact is the new Node oracle
> case under `harness/oracle/cases/` (lane J1's), which drives v4's real TS.
> Run in your OWN worktree on your own branch; Playwright runs ALONE on a
> dedicated port with THIS worktree's own Rust binaries built once (the P4.6af
> memory).
>
> **The pre-committed contract.** `apps/web/src/app/workspace/workspace-contract.ts`
> is ALREADY ON MAIN (committed at round setup, typecheck-verified). It is the
> §1 shared contract *as code* — both lanes import it; **neither lane edits
> it**. Lane J1 PROVIDES every token it declares; lane J2 injects them
> optionally. If you believe it is wrong, STOP and flag — do not patch it.

## Mandate

Port v4's tabbed workspace — v4's **default shell** (m6 F1) — into the Angular
SPA: the pure workspace core (reducer / persistence / tab-meta /
route-to-intent) with a **captured-corpus tier-1 differential against v4's
real code**, the workspace store service, the two-pane keep-alive host and all
its chrome (tab strip, divider, drop-zones, backdrop, portal hosts, shortcuts,
link interceptor, `?open=` intent), the workspace-tabs flag with the old-route
redirect guards, and the shell/rail integration. Host in-lane every tab kind
whose v5 screen needs no inputs; the input-driven kinds land as loud
ACTIVATE-AT-UNIFY wires over lane J2's contracted inputs (§2).

Must NOT touch: the contract file, any existing file under
`apps/web/src/app/screens/**` or `apps/web/src/app/chat/**` (lane J2's), any
Rust source. `shell/shell.ts` and `app.routes.ts` are **this lane's**
single-owner files.

## Survey-verified starting points (2026-07-19 @ v4 `b8b12695` — do not re-derive)

The spec is the mirrored feature doc + ADR (both already under
`docs/v4/developer/`): `features/tabbed-workspace.md` (571 lines) and
`decisions/ADR-0001-tabbed-workspace-routing.md` (Option B chosen: single
`/workspace` route + client tab model).

**The pure core (`lib/workspace/`, the tier-1 differential targets):**

- `types.ts` (177 ln) — 22 `TabKind`s, payload interfaces, `WorkspaceState`,
  `DEFAULT/MIN/MAX_SPLIT_RATIO`, `standaloneDocKey`. Already mirrored in the
  contract file — the core port re-exports the contract's types rather than
  duplicating them.
- `workspace-reducer.ts` (441 ln, PURE — no React/crypto/localStorage; every
  id supplied in the action). Actions: `OPEN_TAB` (caller-minted id; de-dupe
  via `tabIdentity`; payload/title refresh on existing incl. the
  `focus:false` arm; right-pane creation; defensive collapsed-right fallback),
  `CLOSE_TAB` (+`homeFallbackId`; child-cascade via `parentTabId`),
  `MOVE_TAB` (same-pane reorder activates the moved tab; cross-pane move with
  right-pane creation, source-active repair via `pickNeighbor`, empty-pane
  collapse + right→left promotion), `SET_ACTIVE`, `SET_FOCUSED_PANE`,
  `UNSPLIT`, `SET_SPLIT_RATIO` (clamp 0.2–0.8, non-finite → 0.5),
  `REPLACE_STATE`. Selectors: `tabIdentity` (salon/terminal by chatId;
  document by chatId+chatDocumentId; document-standalone by docKey;
  character-edit/-view by characterId; custom-tools by mountPointId+path;
  all else singleton by kind), `paneOfTab`, `getPaneState`,
  `isActiveInItsPane`, `isSplit`, `createInitialState`, `createHomeTab`.
  Quirks to carry: `payloadEqual` compares `JSON.stringify(a ?? null)` — key
  order is significant (same keys, different insertion order ⇒ NOT equal ⇒
  payload refresh); `pickActiveAfterRemoval` walks forward from the removed
  active's old index, then backward; `clampIndex` floors and clamps.
- `workspace-persistence.ts` (270 ln, pure) — `WORKSPACE_STORAGE_KEY_BASE =
  'quilltap.workspace.layout'`, `workspaceStorageKey(instanceId?)` appends
  `.{instanceId}`; Zod-shaped validation with the CRITICAL leniency: the
  outer shape parses strictly but **each tab re-validates individually** so a
  single malformed/unknown-kind tab drops only itself, never the whole saved
  layout; `pruneWorkspaceState` (drop salon/terminal/document tabs with
  invalid chats; document tabs lacking `chatDocumentId`; document-standalone
  lacking `docKey`+`filePath`; orphaned children via a FIXPOINT loop for
  chains; rebuild pane orders keeping original order; unplaced survivors
  append to left; active-reference repair; right-collapse; right→left
  promotion; total-loss → fresh home via `homeFallbackId`); `hydrateWorkspaceState`.
  v5 has no Zod — port the validation by hand (the Workbench client-safe
  schema-port precedent); the corpus proves equivalence.
- `tab-meta.ts` (52 ln) — `DEFAULT_TAB_META` per-kind titles/icons (house
  voice: 'The Foundry', 'The Scriptorium', "Pascal's Workbench", …). v5 icon
  names must come from v5's `ui/icon.ts` registry — where a v4 icon name has
  no v5 equivalent, keep the v4 NAME in the ported meta table (the corpus
  diffs the table) and map to the nearest v5 icon at the render site,
  documented.
- `lib/navigation/route-to-intent.ts` (112 ln) — `parseHrefToIntent`: strips
  query/hash/trailing-slash; `/salon/<id>` → salon (`new` → null);
  `/(aurora|characters)/<id>/edit` → character-edit (+`?tab=`);
  `/(aurora|characters)/<id>/view` → character-view; bare detail → null;
  singletons; `/custom-tools` `?mount=&path=&new=1`; `/settings`
  `?tab=&section=`; null for externals/no-equivalent (`/salon` list,
  `/salon/new`). **v5 additions** (v5's route names differ): `/characters` →
  aurora, `/characters/new` → character-new, `/characters/<id>` →
  **character-view** (v5 has no `/view` suffix — its `/characters/:id` IS the
  detail route; documented adaptation), plus v5-only null rows
  (`/characters/groups/<id>`, `/prospero/<id>`, `/scriptorium/<id>`,
  `/salon/<id>/terminal/<sid>` — bare details render standalone, v4-faithful).
  The v4 corpus rows must replay byte-identically; the v5 additions get
  v5-side unit tests.
- `lib/navigation/workspace-redirect.ts` (34 ln) + the **16 redirecting
  routes** (survey-verified list): `/` (→ `home`), `/salon/[id]`, `/aurora`,
  `/aurora/new`, `/aurora/[id]/edit`, `/prospero`, `/scriptorium`, `/files`,
  `/photos`, `/scenarios`, `/settings` (carrying `tab`/`section`),
  `/settings/wizard`, `/about`, `/profile`, `/generate-image`,
  `/custom-tools` (carrying `mount`/`path`/`new`). NOT redirected in v4 (and
  so not in v5): the salon LIST, `/salon/new`, bare detail routes,
  `/aurora/[id]/view`. ⚠ Two v4 docstrings LIE (m6 F4): `workspace-redirect.ts:6-7`
  says "flag off (the default)" and `app/workspace/page.tsx:11-13` calls the
  route dev-only — the flag defaults ON (`feature-flags.ts`: `!== '0'`).

**The chrome (`components/workspace/`, ~1.7 KLOC):**

- `WorkspaceHost.tsx` (170 ln) — one CSS grid; `gridTemplateColumns` from
  `splitRatio` (`minmax(0, Xfr) 8px minmax(0, 1-Xfr)`); a FLAT always-mounted
  list of tab views positioned by `grid-column`, hidden via `display:none` +
  `aria-hidden` (never unmounted); a hidden Salon stays MOUNTED while a child
  terminal/document tab is visible (`childActive`) so it can portal into it;
  `onMouseDownCapture` sets focused pane; empty-pane affordance; unsplit
  drag → center drop-zone ("Release here to split the workspace").
- `TabView.tsx` (145 ln) — kind→view switch + the **lazy-mount latch**
  (`everActive`): a view mounts on first activation, never unmounts after.
- `TabStrip.tsx` (123 ln) — role=tablist/tab, close button
  (`aria-label="Close <title>"`), HTML5 drag reorder/move (strip-level drop =
  append at end; tab-level drop = insert at index), active-tab
  `scrollIntoView({ inline: 'nearest', block: 'nearest' })`.
- `WorkspaceDivider.tsx` (98 ln) — pointer drag over the container rect;
  `role="separator"` with aria-value*; ArrowLeft/Right nudge `KEY_STEP=0.02`;
  Home/Enter/double-click reset to 0.5; body cursor/user-select during drag.
- `workspace-backdrop.tsx` (157 ln) — the arbitration rule: a Salon WITH a
  background wins full-screen (focused pane preferred when both are Salons);
  else unsplit → active tab's; else per-side layers crossfading at
  `--qt-split: <round(ratio*100)>%`.
- `tab-toolbar.tsx` (171 ln) — v4's per-tab page-toolbar registry + bridge.
  **v5 structural difference:** v5 has NO global page toolbar (each v5 screen
  renders its own header inside its own component), so per-tab toolbars come
  free with hosting; port NOTHING here, record it as a structural non-issue
  in the status log (verify while building — if a v5 global header slot
  exists after all, flag before inventing).
- `WorkspaceIntent.tsx` (113 ln) — consumes transient `/workspace?open=…`
  (+`chatId`/`tab`/`section`/`characterId`/`mount`/`path`/`new`/
  standalone-doc params), opens the tab, then strips to a clean `/workspace`
  (Angular: `router.navigate` with `replaceUrl: true`). ⚠ The hydration
  race is real and documented in v4: apply the intent only AFTER the
  localStorage hydrate, or the hydrate's `REPLACE_STATE` clobbers the
  just-opened tab.
- `WorkspaceLinkInterceptor.tsx` (94 ln) — ONE document-level **capture**
  click listener, active only on `/workspace`: left-click, no modifiers, no
  `download`/`target`, href starts `/`; `/salon/new` passes through (v5: the
  New-Chat PAGE navigates — v5 never ported v4's NewChatModal, an already
  documented P4.6q divergence; creation lands on `/salon/:id`, whose redirect
  guard funnels straight back into the workspace); otherwise
  `parseHrefToIntent` → openTab. **Angular adaptation (pin):** v4 relies on
  `preventDefault()` because Next's `<Link>` checks `defaultPrevented`;
  Angular's `RouterLink` does NOT — the capture handler must ALSO
  `stopPropagation()` so RouterLink's own listener never fires. Middle/
  modifier clicks must still pass through untouched.
- `useWorkspaceNavigate` / `useWorkspaceLink` (programmatic + per-link
  arms), `useCloseSelfTab`, `useWorkspaceShortcuts` (96 ln — Ctrl/Cmd+Alt
  namespace: ←/→ cycle w/ wrap, 1–9 jump, W close, `\` split-toggle (split
  only when ≥2 tabs remain), inert in input/textarea/select/contenteditable),
  `workspace-tab-context.tsx` (portal registry + tab id), `TerminalDocumentViews.tsx`
  (58 ln — the child-tab PORTAL HOSTS: a mount div registered under
  `portalKey(...)` + the "Open this conversation in the workspace to bring
  its terminal/document to life here" empty copy), `WorkspaceProviders.tsx`.
- `app/workspace/page.tsx` (32 ln), `app/salon/SalonListView` extraction
  (the salon LIST is NOT a tab kind; rail "Chats" navigates to `/salon`
  standalone — leaving the workspace — and a chat click funnels back in;
  v4-faithful, keep it).
- CSS: `app/styles/qt-components/_workspace.css` (338 ln) — the
  `qt-workspace*` / `qt-tab*` family; ONE master token
  `--qt-workspace-accent` drives active tab / divider / drop-zone; each of
  the six bundled themes sets its own accent. v5's D15 `qt-*` port lives at
  `apps/web/src/styles/qt-components/` — add `_workspace.css` there + the
  per-theme accent values.

**v4's own test suites** (port case-for-case where the corpus doesn't cover):
`__tests__/unit/lib/workspace/workspace-reducer.test.ts` (317 ln, 31 cases),
`workspace-persistence.test.ts` (214 ln, 13), `route-to-intent.test.ts`
(86 ln, 10) + the component suites (keep-alive mount-counter, intent race,
interceptor, shortcuts, backdrop, tab-toolbar, salon-mode-panes — the last
is lane J2's side).

**v5 shell facts:** `shell/shell.ts` renders the rail (`NAV_ITEMS`, all
`routerLink`) + footer + a single `<router-outlet>` inside the finding-#3a
scroller wrapper (`flex-1 min-h-0 overflow-y-auto`, `shell.ts:167`) —
**preserve that wrapper** around the workspace host. `openWardrobe()`
(`shell.ts:195-204`) already carries the comment that its `inWorkspace` arm
belongs to p4.9j — this lane delivers it. No RouteReuseStrategy exists
anywhere in v5; this lane introduces the first keep-alive machinery. The
committed-fixture precedent for the corpus is
`apps/web/src/app/chat/render/__fixtures__/markdown-fixtures.json` (P4.d9).

## Tiered deliverables

### Tier 1 — must land

1. **The corpus + the pure core.** A new oracle case
   `harness/oracle/cases/workspace-core.ts` run from the v4 checkout via
   `npx tsx`, importing v4's REAL `lib/workspace/workspace-reducer.ts`,
   `workspace-persistence.ts`, `tab-meta.ts`, and
   `lib/navigation/route-to-intent.ts`, emitting a deterministic JSON corpus
   committed at
   `apps/web/src/app/workspace/core/__fixtures__/workspace-core-fixtures.json`:
   - **Reducer corpus:** scripted action sequences (every id/homeFallbackId
     supplied in the script — the reducer is fully deterministic), recording
     the COMPLETE state after every action. Cover at minimum: every action
     type, every `tabIdentity` arm, the de-dupe payload-refresh (incl. a
     same-keys-different-order payload pinning the `JSON.stringify`
     semantics), `focus:false`, right-pane creation both ways, child
     cascade, neighbor-pick after removal (forward AND backward arms),
     collapse + promotion, last-tab home reset, clamp edges (NaN/∞/negative
     ratio, out-of-range MOVE index), `UNSPLIT` active-tab preference.
     Target ≥ 60 scripted steps; seed from v4's 31 reducer cases and go
     beyond them.
   - **Persistence corpus:** raw-string inputs → `deserializeWorkspaceState`
     output (valid, garbage, `null`ish, single-bad-tab-dropped,
     unknown-kind-dropped, every-TabKind round-trip) and
     (state, invalid-chat-set, homeFallbackId) → `pruneWorkspaceState`
     output (dead salon/terminal/document chats, missing `chatDocumentId`,
     standalone-doc without filePath, orphan CHAINS exercising the fixpoint,
     dangling active refs + stray order ids + unplaced survivors, total
     loss). Also `workspaceStorageKey` with/without instance id and
     `serialize→deserialize` round-trips.
   - **Route-intent corpus:** ≥ 40 hrefs (v4's 10 test cases + the full
     route table + query/hash/trailing-slash/external edges) → intent or
     null.
   - **Tab-meta corpus:** the full `DEFAULT_TAB_META` table.
   The v5 port lives under `apps/web/src/app/workspace/core/` (re-exporting
   the contract's types; the reducer/persistence/tab-meta/route-to-intent
   modules stay PURE — no Angular imports) with a replay spec that runs
   EVERY corpus row through the port and asserts deep equality (object
   states — deep equal, not byte; strings — exact). Corpus regen recipe goes
   in the order-of-record and the status log; grep the emitted JSON for a
   row you expect (`oracle-regen-silent-stale-pass` memory).
2. **The store service.** `WorkspaceService` (signal-based) wrapping the
   reducer with v4 `workspace-provider.tsx` semantics: uuid minting
   (`crypto.randomUUID`), de-dupe-resolved `openTab` return id, hydrate ONCE
   after construction from localStorage (validator: chat existence via the
   D14 `CoreClient` chats list — mirror v4's `isChatValid` seam), a
   `hydrated` signal gating intent application, debounced persist
   (`PERSIST_DEBOUNCE_MS = 250`) that never runs pre-hydration, and the
   `WorkspaceHandle` implementation (openTab/closeTab/refreshTab) **provided
   under the contract's `WORKSPACE_HANDLE` token**. Storage key: the ported
   `workspaceStorageKey`; v5 passes `null` instanceId for now (no client
   instance id is exposed — record as a named deferral; the scoped form
   stays corpus-proven).
3. **The chrome + the `/workspace` route.** All components surveyed above,
   Angular-idiomatic (standalone, signals, OnPush, `@for` keyed by tab id for
   the flat keep-alive list), with: the lazy-mount latch, the childActive
   salon-stays-mounted rule, per-tab injectors providing `WORKSPACE_TAB_ID`,
   the portal-host registry provided under `WORKSPACE_PORTAL_REGISTRY`, the
   backdrop registry + arbitrated `WorkspaceBackdrop` under
   `WORKSPACE_BACKDROP_REGISTRY`, the divider, the strip with drag
   reorder/move/split, the shortcuts, the intent consumer (hydration-gated,
   URL-stripping), and the link interceptor (capture +
   preventDefault+stopPropagation, `/salon/new` passthrough). CSS:
   `_workspace.css` ported to v5's qt-components with the
   `--qt-workspace-accent` token + six theme accents. Unit specs port v4's
   component suites — the **keep-alive mount-counter spec is mandatory**
   (switch away and back; assert the view instance never re-instantiated).
4. **The flag, the redirects, the shell.** `isWorkspaceTabsEnabled()`:
   `localStorage['quilltap.workspace.tabs'] !== '0'` — default ON, read
   once at bootstrap (documented divergence: v4 uses a build-time
   `NEXT_PUBLIC_WORKSPACE_TABS`; v5 has no per-deploy build env, so the
   supported opt-out becomes a per-browser key with identical semantics).
   A `workspaceRedirectGuard` on exactly the 16 v5 route equivalents
   (§4 list) → `/workspace?open=…` carrying the v4 param names. Shell:
   when the flag is on, the rail items and footer entries go through the
   interceptor/`openTab` path (rail click opens/focuses in the FOCUSED
   pane); `openWardrobe()` gains its `workspace.openTab('wardrobe')` arm;
   the `<router-outlet>` body is preserved for routed mode. `/workspace`
   itself renders the host inside the shell (rail + footer stay, per v4
   AppLayout).
5. **In-lane hosting + loud refusal panes.** Wire `TabView` to the v5
   screens that need no inputs TODAY: `home`, `aurora` (CharactersList),
   `prospero` (ProsperoList), `scriptorium` (ScriptoriumList), `files`,
   `photos`, `scenarios`, `generate-image`, `about`, `profile`,
   `character-new`, `settings-wizard`. Every other kind (`salon`,
   `terminal`, `document`, `settings`, `wardrobe`, `character-edit`,
   `character-view`, `custom-tools`, `document-standalone`) renders a loud
   named not-yet-wired pane and is an **ACTIVATE-AT-UNIFY** wire over lane
   J2's §2 inputs. `brahma` renders a permanent loud refusal pane naming
   `p4.9i1` (the console has no v5 surface).
6. **The e2e dual-mode harness.** A single global opt-out
   (`addInitScript` setting `quilltap.workspace.tabs = '0'`) in the shared
   Playwright setup so the ENTIRE existing suite keeps running v4's
   supported route mode byte-unchanged; a new `workspace-flow.spec.ts` that
   clears the opt-out and walks flag-on beats over the in-lane kinds: `/`
   redirects to `/workspace?open=home` → home tab; rail click opens a
   second tab; de-dupe focuses; drag-to-split (or `splitTo` via keyboard
   `Ctrl+Alt+\`); reload restores the layout; deep-link
   `/settings?tab=…` redirect carries its intent (assert the redirect +
   the stripped URL even while the settings tab is a refusal pane —
   extend the beat at unify). ⚠ New beats that mutate shared fixture
   state are only validated by a FULL-suite run (the P4.d9 memory).

### Tier 2 — should land

- The `workspace-flow` split-drag beat via real HTML5 drag events (the
  keyboard split is the tier-1 floor).
- The empty-pane affordance + drop-zone visual states across all six themes
  (manual screenshot check recorded in the lane log).
- `refreshTab` exercised by a unit spec (lane J2's standalone-document
  payload refresh depends on it at unify).

### Tier 3 — explicit deferrals (loud, named)

- **`brahma` tab content** — refusal pane until `p4.9i1`.
- **Per-instance storage-key scoping** — v5 passes `null` instanceId until a
  client-visible instance id exists (named deferral in the status log).
- **The chat-sidebar narrow-pane overlay** (v4 Phase 8) — lane J2's salon
  territory; if J2 defers it, it stays a NAMED deferral, not silent.
- **The workspace help doc** (`help/tabbed-workspace.md`) — banked for
  `p4.9i2` with the rest of the help family.
- **URL-encoded layouts / >2 panes / DB-backed layout sync** — out of scope
  in v4 too (ADR Option C).

## The differential requirement, restated

Every ported unit ships with an equivalence test driving v4's REAL code.
For this pure-TS-client lane the tier-1 form is the **captured corpus**: the
oracle case imports v4's actual `lib/workspace`/`lib/navigation` modules
(never reimplements them), emits the committed JSON corpus, and the v5 spec
replays every row (the `markdown-fixtures.json` / Workbench schema-port
precedent). Regen: `cd ~/source/quilltap-server && npx tsx
~/source/quilltap-v5/harness/oracle/cases/workspace-core.ts >
<v5>/apps/web/src/app/workspace/core/__fixtures__/workspace-core-fixtures.json`.
Never hand-edit the fixture; regen + grep a known row. Component behavior
(keep-alive, intent race, interceptor) is covered by porting v4's own
component suites case-for-case — name the v4 source suite in each spec
header.

## Fixtures

- **Delivers:** `workspace-core-fixtures.json` (committed; lane J1 owns
  regeneration whenever the v4 baseline moves).
- **Consumes:** the standing e2e instance fixtures (`apps/web/e2e/`
  global-setup provisioning) — unchanged; the new workspace beats reuse the
  provisioned instance. No DB fixture work in this round.

## Shared contract (BINDING — identical in both orders)

### §1 — the contract file (pre-committed)

`apps/web/src/app/workspace/workspace-contract.ts` on main is the contract:
the 22-kind `TabKind`, the payload interfaces, `WorkspaceState`,
`DEFAULT/MIN/MAX_SPLIT_RATIO`, `standaloneDocKey`, `portalKey`,
`OpenTabOptions`, `WorkspaceHandle` + `WORKSPACE_HANDLE`,
`WORKSPACE_TAB_ID`, `WorkspacePortalRegistry` + `WORKSPACE_PORTAL_REGISTRY`,
`WorkspaceBackdropEntry`/`WorkspaceBackdropRegistry` +
`WORKSPACE_BACKDROP_REGISTRY`. Lane J1 provides every token; lane J2 injects
`{ optional: true }` and treats null as routed mode. Neither lane edits the
file.

### §2 — the per-kind hosting table (BINDING)

Lane J2 delivers the inputs; lane J1 (in-lane where possible, else the
unifier) binds them. Inputs are signal `input()`s; a null-injected
`WORKSPACE_TAB_ID` means routed mode and the screen reads its route params
exactly as today.

| TabKind | v5 component | Tab-mode inputs lane J2 delivers | Binding |
| --- | --- | --- | --- |
| home | HomePage | — | in-lane (J1) |
| salon | SalonConversation | `chatId` | AT-UNIFY |
| terminal | (portal host, J1 chrome) | J2: salon-side portal SOURCE + child-tab lifecycle | AT-UNIFY |
| document | (portal host, J1 chrome) | J2: salon-side portal SOURCE + child-tab lifecycle | AT-UNIFY |
| aurora | CharactersList | `inTab` drill (internal detail/group state) | list in-lane; drill at unify |
| prospero | ProsperoList | `inTab` drill | list in-lane; drill at unify |
| scriptorium | ScriptoriumList | `inTab` drill | list in-lane; drill at unify |
| settings | Settings | `tab`, `section` | AT-UNIFY |
| files | FilesBrowser | — | in-lane (J1) |
| photos | PhotosPage | — | in-lane (J1) |
| scenarios | ScenariosPage | — | in-lane (J1) |
| brahma | — (unported) | — | refusal pane (p4.9i1) |
| wardrobe | the wardrobe control surface (P4.9f2) | `characterId?` (rail/browse mode, NO chatId) | AT-UNIFY |
| profile | ProfilePage | — | in-lane (J1) |
| about | AboutPage | — | in-lane (J1) |
| generate-image | GenerateImagePage | — | in-lane (J1) |
| document-standalone | J2's new standalone document surface | `payload: DocumentStandaloneTabPayload` | AT-UNIFY |
| character-new | NewCharacter | self-close upgrade | in-lane; close-seam at unify |
| character-edit | CharacterEdit | `characterId`, `tab?` + self-close | AT-UNIFY |
| character-view | CharacterDetail | `characterId`, `tab?` + self-close/back | AT-UNIFY |
| settings-wizard | WizardScreen | self-close upgrade | in-lane; close-seam at unify |
| custom-tools | CustomToolsPage | `mountPointId?`, `path?`, `create?` | AT-UNIFY |

### §3 — seam semantics (BINDING)

- **Optional injection**: screens inject the tokens `{ optional: true }`;
  null ⇒ routed mode, behavior byte-identical to today. Lane J2's own gate
  is the UNCHANGED existing ng + Playwright suites.
- **Self-close**: a screen whose save/cancel/back should dismiss its tab
  injects `WORKSPACE_HANDLE` + `WORKSPACE_TAB_ID`; when both non-null,
  close the tab instead of navigating (v4 `useCloseSelfTab`).
- **Portals**: child-tab hosts register DOM nodes under
  `portalKey(kind, chatId, docId?)`; the Salon relocates its live pane
  ELEMENT into the registered node (DOM `appendChild` move — the component
  stays in the Salon's logical tree so PTY/editor state survives; v4 uses a
  React portal, the DOM-move is the Angular-idiomatic equivalent and the
  keep-alive spec must prove xterm survives the move). Opening a document /
  terminal in workspace mode spawns the child tab via
  `openTab(kind, payload, { parentTabId })`; closing the child tab toggles
  the mode off; closing the parent Salon tab cascades (reducer-side, J1).
- **Backdrop**: the Salon (and only actual background sources) reports
  `{ url, isSalon }` under its `WORKSPACE_TAB_ID`; clears on null/destroy.
  The host paints per the v4 arbitration rule.
- **`refreshTab`**: J2's blank standalone document uses it to swap in the
  server-named `filePath` (same docKey — identity stable).

### §4 — the flag, keys, redirects, e2e modes (BINDING)

- Flag: `localStorage['quilltap.workspace.tabs'] !== '0'` ⇒ enabled
  (default ON; documented divergence from v4's build-time env var).
- Layout key: `quilltap.workspace.layout` (unscoped form; the scoped form
  stays corpus-proven, deferral named in J1 tier 3).
- Redirecting v5 routes (guard → `/workspace?open=…`): `''`→home,
  `salon/:id`, `characters`, `characters/new`, `characters/:id/edit`,
  `prospero`, `scriptorium`, `files`, `photos`, `scenarios`, `settings`
  (+tab/section), `settings/wizard`, `about`, `profile`, `generate-image`,
  `custom-tools` (+mount/path/new). NOT redirected: `salon` (list),
  `salon/new`, `salon/:id/terminal/:sessionId`, `characters/:id`,
  `characters/groups/:id`, `prospero/:id`, `scriptorium/:id`, `**`.
- e2e: the shared harness sets the opt-out globally (route mode, whole
  existing suite unchanged); workspace beats clear it explicitly.

## Ownership (BINDING — identical in both orders)

- **Lane J1 owns:** `apps/web/src/app/workspace/**` EXCEPT
  `workspace-contract.ts` (read-only for everyone);
  `apps/web/src/app/app.routes.ts`; `apps/web/src/app/shell/shell.ts`;
  `apps/web/src/styles/qt-components/_workspace.css` (new) + the six theme
  accent additions; the Playwright shared setup file(s) it must touch for
  the global opt-out + `apps/web/e2e/workspace-flow.spec.ts` (new);
  `harness/oracle/cases/workspace-core.ts` (new) + the committed corpus
  fixture.
- **Lane J2 owns:** the existing screen/chat component files it adapts
  (enumerated in its order) + its new standalone-document surface files +
  their specs. J2 does NOT touch app.routes.ts, shell.ts, the e2e harness
  setup, or anything under `apps/web/src/app/workspace/` besides importing
  the contract.
- **Version bumps:** BOTH lanes are SPA-only — each bumps
  `apps/web/package.json` per-commit on its own branch (base at round start:
  **0.5.189** — the contract pre-commit was scaffolding-only, no bump); the
  unifier accumulates (max + other lane's commit count). NO crate bumps
  anywhere in this round; `Cargo.lock` untouched.
- **Append-only for everyone:** `docs/CHANGELOG.md`,
  `docs/developer/porting/status-log.md` (union-merge at unification).

## Verification gate (run before declaring done)

- `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D
  warnings` (both feature sets), `cargo test --workspace --no-fail-fast` —
  pure don't-regress guards for this SPA round; main's baseline is **354
  test binaries / 1,450 tests / 0 failed** and your lane must not drop below
  it. No Rust source may have changed (`git diff --stat main -- crates/`
  must be empty).
- The corpus fixture regenerates cleanly from the v4 checkout at
  `b8b12695`/`70baaa74` and the replay spec is green over the FRESH regen
  (not just the committed copy); grep the emitted JSON for an expected row
  (`oracle-regen-silent-stale-pass`).
- `ng test` green and `ng build` clean, both run FROM `apps/web` (main's
  baseline: 172 files / 2,029 specs — grow it); the keep-alive
  mount-counter spec present and green.
- Full Playwright green, run ALONE on a dedicated port with THIS worktree's
  own Rust binaries (the P4.6af memory): the ENTIRE existing suite green in
  route mode (the global opt-out) **plus** the new workspace beats green
  flag-on. A source mutation proves nothing until `ng build` rewrites the
  served bundle (`e2e-mutation-check-needs-a-rebuilt-dist`). Full-suite run
  required — new beats can shift shared fixture state (the P4.d9 memory).
- No `[value]` binding on any `<select>` with async or `@if`-gated options —
  bind `[selected]` per option (the standing dogfood-#6 rule).
- Every commit follows `.claude/commands/commit.md` (equivalence test with
  the port, SPA version bump, CHANGELOG entry, status-log append).
