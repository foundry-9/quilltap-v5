# Work order P4.d17 — the `deab0e5d` + `ab0f175e` thinking-indicator + Madman's Box drift re-port (the themeable waiting quill, the theme refresh to 1.1.7)

> **STATUS: CLOSED — unified on main 2026-07-22.** All six units landed
> (tier 1 AND tier 2): the `thinking` icon (registry name 85), the
> `.qt-thinking-indicator` motion hook (declarations byte-identical to
> v4), `qt-quill-animation`, adoption at v5's analogs of v4's four call
> sites (all inside `streaming-message.ts`), Madman's Box 1.1.5 → 1.1.7
> (`theme.json` byte-identical; the icons-map entry expressed as the
> unlayered `[data-icon]` CSS override — the established v5 mechanism),
> and the two live e2e pins. Tier-3 NO-PORTS as ordered (help doc banked
> for `p4.9i2`; theme-storybook and the icon-css generator have no v5
> equivalents). Lane + round records: `status-log.md`.

Drift-catch-up round, lane B of two (sibling: **P4.d16**, the workspace
deep-links re-port — file ownership is disjoint; see the Shared contract).
This round absorbs the WHOLE outstanding v4 drift — the four commits
`ab0f175e` + `deab0e5d` + `8d86847a` + `e646f58b` — and **the oracle baseline
MOVES to `e646f58b` (v4 HEAD) at unification.**

Run `git log e646f58b..HEAD --oneline` in `~/source/quilltap-server` **at lane
start; if HEAD moved, STOP and report.** The v4 checkout was verified CLEAN at
`e646f58b` at planning (2026-07-22).

Work on a lane branch (`lane/p4.d17-thinking-indicator`) — normal carryout
rules apply. `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md`
are append-only (they union-merge at unification).

**This lane is SPA assets + styles + one component family + docs only. Zero
Rust, zero workspace files.** Both v4 commits are lib-free (dispositioned at
the P4.6bj unification).

## Mandate

Bring v5's Salon "something is happening" indicator to v4's POST-drift form
and refresh the bundled Madman's Box theme pack from 1.1.5 to 1.1.7.

**The planning survey found v5 never ported QuillAnimation at all** — the
status strip shows a hard-coded pulsing dot and the awaiting state a literal
`…`. So this lane is not a diff-application: it ports the indicator system
FRESH, at its post-drift (and far more portable) shape — the `thinking` mask
icon, the `.qt-thinking-indicator` motion class with its five `--qt-thinking-*`
theme hooks, a `QuillAnimation`-equivalent Angular component, and its
adoption at the v5 analogs of v4's call sites. Then the theme refresh:
1.1.6's small-caps headings + `.qt-button` re-key, 1.1.7's own-quill spin +
`thinking` glyph override, translated to v5's theme-CSS icon-override
mechanism.

**Must NOT touch:** anything in P4.d16's ownership (`src/app/workspace/**`,
`app.routes.ts`, `shell.ts`, the workspace corpus, the workspace/terminal/
new-chat e2e specs), any Rust source, `message-list.ts` beyond what indicator
adoption strictly requires (prefer zero — the indicator lives inside
`streaming-message.ts`), the chat composer (v5 has no composer indicator
site — see tier 3).

## Survey-verified starting points (surveyed 2026-07-22; v4 at `e646f58b`, v5 at `91959254`)

### v4 `deab0e5d` (the indicator)

- **`public/images/icons/thinking.svg` (NEW, 5 lines)** — a 24×24 line-art
  quill (`stroke="currentColor"`, stroke-width 2, three paths; the nib sits
  at (3,21)). Byte-copy it.
- **`components/ui/icons/icon-registry.ts`** — `'thinking': { defaultFile:
  '/images/icons/thinking.svg', defaultMode: 'mask', ariaLabel: 'Writing' }`,
  inserted after `wand` with the why-comment: "The rocking quill shown while
  a reply is awaited or streaming. The MOTION is a separate theme hook
  (`.qt-thinking-indicator` in _chat.css) so a theme can change the glyph and
  the animation independently." Deliberately NOT `brand` (a wordmark theme
  must not find its wordmark rocking in the Salon). Mask mode = tints with
  `currentColor`, so the per-stage status colors reach it.
- **`app/styles/qt-components/_chat.css`** — the THINKING INDICATOR section
  (post-drift ~`:2996–3042`): `.qt-thinking-indicator { animation:
  qt-thinking-rock var(--qt-thinking-duration, 1.2s)
  var(--qt-thinking-easing, ease-in-out) infinite; transform-origin:
  var(--qt-thinking-origin, 12.5% 87.5%); }`; `@keyframes qt-thinking-rock`
  (0%/100% `rotate(var(--qt-thinking-angle-rest, -45deg))`, 50%
  `rotate(var(--qt-thinking-angle-lean, 0deg))`); a
  `prefers-reduced-motion: reduce` block (animation none, resting angle
  held). **Carry the whole banner comment** — the glyph-vs-motion split, the
  nib-origin rationale ((3,21) of the 24×24 viewBox = 12.5% 87.5%; a theme
  shipping its own glyph must move the origin to ITS nib or the drawing
  orbits), and the overflow caveat (the feather tip paints ~8px above the
  48px box at the upright extreme — never wrap the indicator in
  `overflow: hidden`).
- **`components/chat/QuillAnimation.tsx` (post-drift, 44 lines)** — renders
  `<span class="inline-flex items-center justify-center {w-12 h-12 | w-4
  h-4}">` wrapping `<Icon name="thinking" title={label ?? undefined}
  class="qt-thinking-indicator {size}">`. Props: `size: 'lg' | 'sm'` (lg
  default), `className`, `label?: string | null` defaulting `'Writing…'` —
  pass `null` where the indicator sits inside an already-labelled live region
  (it was being announced twice). Wrapper div → span (the sm variant renders
  inline inside streamed prose). React only decides mountedness; CSS owns the
  motion.
- **v4 call sites (all four, post-drift):**
  `app/salon/[id]/components/StreamingMessage.tsx:100` (`size="lg"` — the
  awaiting state) and `:122` (`size="sm" className="inline-block ml-2
  qt-text-secondary"` — the inline tail while prose streams);
  `ChatComposer.tsx:241` (status strip, `stage === 'streaming'` →
  `size="sm" label={null}`, other stages keep the pulsing circle);
  `PendingToolCalls.tsx:50` (any pending → `size="sm" label={null}
  className="ml-auto qt-text-secondary"`).
- **Madman's Box 1.1.7**: `theme.json` icons map gains `"thinking":
  "icons/brand.svg"`; `styles.css` gains the THINKING INDICATOR block —
  `@keyframes madmans-box-thinking-turn` (0→360deg) and
  `[data-theme="madmans-box"] .qt-thinking-indicator { --qt-thinking-duration:
  6s; --qt-thinking-origin: center center; animation:
  madmans-box-thinking-turn var(--qt-thinking-duration) linear infinite; }` —
  with the full "maker's mark, turning" banner (the ring is centred on
  (12,12), so the origin RESETS from the nib pivot; the off-axis gap keeps
  the turn legible; reduced motion via the theme's existing global rule).

### v4 `ab0f175e` (the 1.1.6 theme fix)

- `themes/bundled/madmans-box/styles.css` headings block: `letter-spacing
  0.14em → 0.1em`, `text-transform: uppercase` → `font-variant-caps:
  small-caps`, `font-weight 300 → 400` — with the why-comment (Raleway ships
  no `smcp` table; browsers synthesize, and synthesized small caps read
  light, hence 400).
- The button rule re-keys `[data-theme="madmans-box"] button` →
  `[data-theme="madmans-box"] .qt-button`, drops `text-transform: uppercase`
  and the redundant `font-size` — why-comment: most buttons are links wearing
  `.qt-button`; the element selector caught only real `<button>`s.
- `theme.json` version `1.1.5 → 1.1.6` (then `→ 1.1.7` with `deab0e5d`).

### The v5 side (all under `apps/web`)

- **The indicator today**: `src/app/chat/streaming-message.ts` — status strip
  at lines 19–26 (`div.qt-chat-response-status[data-stage]` with a
  hard-coded pulsing dot at :22); the awaiting state renders `…` at :32;
  in-progress tool rows render inline at 48–57 (`toolBatches`, `qt-icon
  name="wrench"`) — **v5 has no separate PendingToolCalls component**; that
  inline block is its analog. Only call site: `message-list.ts:126`. The
  composer (`chat-composer.ts`) has NO indicator — v4's composer `label`
  touch maps onto v5's status strip instead.
- **Per-stage colors**: `data-stage` set at `streaming-message.ts:20`, styled
  at `_chat.css:3035–3064` (six stages pulling
  `--qt-response-status-<stage>-fg`); base strip colors `_chat.css:3019–3025`.
- **Icons**: `src/app/ui/icon.ts` — the `IconName` union (lines 9–23) IS the
  registry ("84-name registry" header comment at :6 — update it); the
  component renders `<span class="qt-icon" data-icon>`. Mask/paint rules:
  `src/styles/qt-components/_icons.css` (per-icon `[data-icon="…"] {
  --_qt-icon-mask: url(…) }`; the shared rule at ~46–51; `brand` is
  image-mode at :136). The file's "generated by icon-registry.ts /
  generate:icon-css" banner is a v4 relic — **no generator exists in v5**;
  hand-edit per existing practice (and correct the banner while there —
  point it at `ui/icon.ts` as the v5 source of truth). **Theme override
  mechanism** documented at `_icons.css:15–19`: a theme's UNLAYERED
  stylesheet re-declares `[data-icon="name"]` and beats the
  `@layer components` defaults — this, NOT a theme.json icons map, is how
  v5 themes swap glyphs (v5's `theme.service.ts` only swaps the
  `styles.css` link + fonts; it reads no icons map).
- **Assets**: `public/images/icons/` (83 SVGs, no `thinking.svg`).
- **CSS homes**: the new indicator block belongs in the RESPONSE STATUS
  INDICATOR section of `src/styles/qt-components/_chat.css` (3014–3074);
  extend the existing `prefers-reduced-motion` block at 3071–3074. Token
  defaults may also be declared in `_variables.css` (thinking/reasoning
  tokens cluster ~302–309, ~849–852) if the house pattern prefers it —
  match v4's placement (v4 keeps the fallbacks inline in `var()`; do the
  same).
- **The theme pack**: `public/themes/madmans-box/` — `styles.css` (headings
  block at 566–573, button block at 628–632 — both hunks apply cleanly),
  `theme.json:7` at `"1.1.5"` (exactly the pre-drift baseline), `tokens.json`,
  `fonts/`, `textures/`. **No `icons/` directory and no `brand.svg` asset
  exist in the v5 pack** — 1.1.7 needs `icons/brand.svg` copied from v4's
  pack and the `thinking` mapping expressed as unlayered CSS in the theme's
  `styles.css` (e.g. `[data-icon="thinking"] { --_qt-icon-mask:
  url("/themes/madmans-box/icons/brand.svg"); }` — mirror the exact
  custom-property shape the core `_icons.css` per-icon rules use). v5's
  `.qt-button` class is in wide use (`_buttons.css` + ui components), so the
  `ab0f175e` re-key lands meaningfully.
- **Specs/e2e**: `streaming-message.spec.ts` (status text assertions, e.g.
  :35), `chat-stream.reducer.spec.ts`; theme specs `theme.service.spec.ts`,
  `appearance-tab.spec.ts`, `theme-preview.spec.ts`; e2e
  `foundation.spec.ts:14–52` (bundled-theme application). No e2e asserts the
  indicator DOM today; the mock-LLM salon streaming beats
  (`m4-salon.spec.ts`, `mock-llm.ts`) are the place to pin it.

## Deliverables

### Tier 1 — must land

1. **The `thinking` icon.** Byte-copy `thinking.svg` into
   `public/images/icons/`; add `'thinking'` to the `IconName` union with v4's
   why-comment; add the `[data-icon="thinking"]` mask rule to `_icons.css`;
   update the count comment (84 → 85) and fix the stale generator banner.
2. **The motion hook.** The `.qt-thinking-indicator` block +
   `qt-thinking-rock` keyframes + reduced-motion branch in `_chat.css`'s
   response-status section, byte-near to v4's (same custom-property names,
   same fallback values, same origin) with the FULL banner comment carried.
3. **The `QuillAnimation` equivalent.** A small standalone component (e.g.
   `src/app/chat/quill-animation.ts`, selector `qt-quill-animation`): `size`
   input (`'lg' | 'sm'`, default lg), `label` input (`string | null`, default
   `'Writing…'`, null = no accessible title), span wrapper, `qt-icon
   name="thinking"` with the `qt-thinking-indicator` class. Match v4's
   accessibility semantics (title/aria on the icon; nothing announced when
   label is null).
4. **Adoption in `streaming-message.ts`** — the v5 mapping of v4's four call
   sites: the awaiting (`…`) state → lg quill (v4 `StreamingMessage:100`);
   the inline streaming tail → sm quill `qt-text-secondary` (v4 `:122` — put
   it where v5 renders the live streaming prose tail; if v5 has no
   equivalent tail position, document the reduction loudly and keep the
   other three); the status strip's `streaming` stage → sm quill with no
   label (v4 `ChatComposer:241`; other stages KEEP the pulsing dot); the
   in-progress tool rows → sm quill with no label when a batch is running
   (v4 `PendingToolCalls:50`). Extend `streaming-message.spec.ts` for each
   state (indicator present/absent, label suppression, dot preserved on
   non-streaming stages).
5. **Madman's Box 1.1.6 + 1.1.7.** Apply the `ab0f175e` hunks to the v5
   pack's `styles.css` (headings 566–573, buttons 628–632, both with v4's
   why-comments); add the 1.1.7 thinking block (the 6s
   `madmans-box-thinking-turn` spin + origin reset, full banner); copy
   `icons/brand.svg` from v4's pack; add the unlayered
   `[data-icon="thinking"]` override per the `_icons.css:15–19` mechanism
   (document in the lane record that this is the v5 translation of v4's
   theme.json `icons` map — the established divergence); bump `theme.json`
   to `1.1.7`. Verify the theme's existing global reduced-motion rule covers
   the spin (v4's does).

### Tier 2 — should land

6. **A live indicator e2e pin.** Extend a mock-LLM salon streaming beat to
   assert `.qt-thinking-indicator` is present mid-stream and gone after
   settle (targeted run only — see the port arbitration). A madmans-box
   visual assertion (e.g. computed `font-variant-caps` on a heading) may ride
   `foundation.spec.ts` if cheap.

### Tier 3 — named NO-PORTS (record in the lane record; do not build)

- v4's `ChatComposer.tsx` / `PendingToolCalls.tsx` `label={null}` line edits —
  no v5 equivalents exist as separate components; subsumed by unit 4.
- `help/appearance-settings.md` ("The Waiting Quill") — no v5 help surface;
  banked for `p4.9i2`.
- `theme-storybook` — no v5 equivalent.
- `ICON_INVENTORY.md` / `THEME_PLUGIN_DEVELOPMENT.md` — refresh the MIRROR
  copies under `docs/v4/developer/` only.
- A v5 `generate:icon-css` generator — out of scope; the hand-edit practice
  stands (banner corrected in unit 1).

## The differential requirement

Both v4 commits are lib-free, so there is no oracle family and no corpus.
The equivalence discipline here is **byte-fidelity where bytes are the
artifact**: `thinking.svg` and `icons/brand.svg` are byte-copies (verify with
`cmp`); the `.qt-thinking-indicator` block, the keyframes, the theme hunks,
and the madmans-box thinking block are transcriptions whose property names,
values, and fallbacks must match v4's exactly — record a side-by-side diff
against `git show` output in the lane record (the
`byte-exact-static-data-transcription` memory note is the spirit). Behavior
above the CSS (the component, the call-site mapping) is pinned by ported
assertions: v4 shipped no new unit tests in these commits, so the v5 specs in
unit 4 are the coverage of record — assert against v4's DOCUMENTED semantics
(label default/suppression, stage gating, reduced motion via
`matchMedia`-independent class presence).

## Fixtures

None consumed, none produced. No oracle regeneration. (The baseline move to
`e646f58b` is recorded by the ROUND, owned by the unifier; P4.d16 owns the
only fixture that moves.)

## Shared contract (BINDING — identical in `p4.d16-workspace-deeplinks-drift.md`)

- **The round**: P4.d16 ∥ P4.d17, both SPA-only, zero Rust. At unification the
  oracle baseline moves `8bf3cb5f` → `e646f58b` and the four-commit drift is
  fully absorbed (`e646f58b` itself is a v4-internal lint chore — NO-PORT).
- **Ownership (no file appears in both lanes):**
  - **P4.d16 owns**: `apps/web/src/app/workspace/**`,
    `apps/web/src/app/app.routes.ts`, `apps/web/src/app/shell/shell.ts`,
    drill-in inputs on the prospero/scriptorium/characters list screens and
    the salon-list/new-chat screen components' tab hosting,
    `harness/oracle/cases/workspace-core.ts` + the corpus fixture,
    `apps/web/e2e/workspace-flow.spec.ts` + `terminal-flow.spec.ts` +
    `new-chat-flow.spec.ts`, `docs/developer/porting/m6-screen-parity.md`.
  - **P4.d17 owns**: `apps/web/src/app/chat/streaming-message.ts` (+spec), the
    new indicator component under `apps/web/src/app/chat/`,
    `apps/web/src/app/ui/icon.ts`,
    `apps/web/src/styles/qt-components/_icons.css`, the `_chat.css` thinking/
    response-status section + `_variables.css` token additions,
    `apps/web/public/images/icons/**`, `apps/web/public/themes/**`,
    `apps/web/e2e/foundation.spec.ts` and any new indicator/theme spec.
  - Append-only for both: `docs/CHANGELOG.md`, `status-log.md`.
- **Playwright port arbitration**: P4.d16 owns port 4319 and full-suite runs.
  P4.d17 runs its TARGETED specs with locally moved ports
  (`e2e-port-4319-cross-worktree-kill` memory note: 4419/45401, revert before
  commit, never commit the move) and defers its full-suite proof to the
  unifier.
- **Version bumps**: each lane bumps `apps/web/package.json` per commit as
  usual; the unifier RECOUNTS and accumulates (identical bumps merge
  silently — `p4.6aw-ax-8-round-plan` memory note). No crate versions move in
  this round.

## Verification gate (before declaring the lane done)

- Drift re-check: `git log e646f58b..HEAD` in the v4 checkout is empty.
- `ng test` full suite green (the new component spec + the extended
  streaming-message spec by name); `ng build` clean.
- Targeted Playwright (the salon streaming beat + `foundation.spec.ts`) green
  on moved ports, moves reverted; the FULL suite is the unifier's.
- `cmp` clean on both copied SVGs; the CSS side-by-side diff recorded.
- No `crates/**` diff and no P4.d16-owned file in `git diff --stat main..`.
- Per-commit: `.claude/commands/commit.md`. Lane record appended to
  `status-log.md`.
