# Work order P4.74 — the core smalls: the failover `auth`/`no-api-key-configured` chain arm, the eleventh + twelfth participant-status copies, the three shared-stage recipes, and the handler-logging rows (`[Image Fallback]` fields, `create_system_event`)

**Status (unification, 2026-09-04):** UNIFIED on main at the follow-ups round 2
unification — CLOSED; the §3 review found nothing blocking and three
should-fixes landed on the unify branch: the two case headers
(`carina-tool.test.ts`, `mail-tools.test.ts`) still carried the UNANCHORED jest
filter the lane had diagnosed and fixed only in the `.rs` twins; the inventory
seed gained `token-tracking.service.ts` (its `Failed to update chat token
aggregates` catch is the sibling of `create_system_event`'s row, three lines
below it in `cost_events.rs`, and was invisible to the inventory — 203 → 205
rows, the new row NO-SITE); the oracle's `profileKey` union no longer
advertises a value the Rust twin panics on. Plus: the `state-cascade.test.ts`
header's `WT=` placeholder → `V5W=`, the script's duplicated docstring and
unused import. Recorded: the thirteen verbatim `CaptureLayer` copies want a
shared `#[cfg(test)]` helper; `pinned()` is a plain grep (a sentence quoted in
a comment would score PORTED-PINNED — none does today).

_(The lane's own close header follows.)_

**Status:** CLOSED — Tier 1 and Tier 2 complete, Tier 3 discharged rather than
taken (lane branch `claude/p4-74-core-smalls-auth-arm-e661ba`, 2026-09-03; four
commits; lane record in `status-log.md` → "Lane record — P4.74"). All four
Tier-1 deliverables and both Tier-2 deliverables landed. The `api-key-not-found`
arm the order deferred to Tier 3 LANDED instead — the fixture carries it in the
same case without disturbing the tier pool. Three order premises were corrected
by measurement and are recorded in the lane record: the shared-stage census is
SIX families rather than three (and the three that were genuinely unsafe are not
the three the round record named); the oracle hands a primary at FIVE sites, not
four, and the fifth is the load-bearing one; and v4's
`file-attachment-fallback.ts` carries SIXTEEN logger calls rather than nine, with
all five describer-chain sentences in that same file. Two `[Image Fallback]`
lines are deliberately NOT ported for structural reasons recorded at their sites
(`:505`, `:426`), and the description-path lines are ported but recorded
UNPINNED — both in the new inventory. Left OPEN for a follow-up order, outside
this lane's ownership: the THIRTEENTH participant-status copy at
`db/chats_messages.rs` (the stale `_ => Active` rule, and unlike the copy retired
here its status IS read) plus five harness-side copies — all named in the new
`participant_status_home_guard` census.

_Original order:_ (ordered 2026-09-03, the follow-ups round 2:
P4.72 ∥ P4.73 ∥ P4.74 ∥ P4.75). Written from `phase-4.md`'s candidate 4 at
the `0b0617fee` unification (the follow-ups round's items 5–6, carried:
P4.68's deferred `auth` arm with its written-out shape; `answer_
confirmation.rs:336`'s eleventh status-parser copy; the harness's twelfth
copy that `panic!`s; the three families still staging into the shared
`/tmp/qt-oracle-stage`; the handler-logging sweep — the `[Image Fallback]`
warn fields "unpinned (none of that file's five log lines are)"), and
P4.61's named `cost_events::create_system_event` sibling.

## Preamble

- **v4 oracle baseline: `0b0617fee`** (drift-ledger §1; probe re-run
  2026-09-03 at ordering: v4 checkout on `main`, tree CLEAN, `git log
  15573c3a1..main` empty, `git log 3a76b17df..bugfix` empty). The ledger's
  one §3 row (`15573c3a1`, bug 119, the unported character optimizer) is
  not this lane's and touches no file this lane reads.
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 checkout
  on `main`, tree clean, `git log 15573c3a1..main` empty and `git log
  3a76b17df..bugfix` empty. **Any mismatch: STOP and report.** Re-run
  before every regen batch.
- **Regen rule: PIN REQUIRED at `0b0617fee`** (ledger §1). Lane-unique
  pin `PIN=/tmp/qt-v4-pin-p474-0b0617fee` per §5.1; every regen via
  `recipe_sweep.py --run <family> --v5w <worktree> --v4 "$PIN"`.
  Committed recipes never name the pin.
- **Sibling lanes (this round):** P4.72 (web query remainder + the
  dispatch wrong-type census), P4.73 (the `/api/v1/images` collection route
  + the ingest codec — owns `api/types.rs`, `api/engine.rs`, `api/files.rs`,
  the `ChatCreate` validator file, the host seams), P4.75 (SPA smalls —
  owns the SPA + Playwright). §Shared contracts / §Ownership are binding
  and identical across all four orders.
- **Worktree:** per the `carryout` skill. `CHANGELOG` / `status-log.md`
  append-only. **No Playwright, no SPA.**
- **Long gates:** `run_in_background: true` + full log + sentinel; NEVER a
  `pgrep` loop; NEVER `| tail`; never edit source during a workspace gate;
  fail-fast per binary — read the whole log. **Never run two sweeps
  concurrently**, and never share a stage dir with a sibling lane (this
  lane is the one retiring the shared stage — see item 3).

## Mandate

Close the four core-side debts the follow-ups round named and did not
land: (1) the third chain-walk blind spot — a chain candidate refused by
the credential gate, recording `auth` with v4's `no-api-key-configured`
reason bytes — as a measured arm in `primary_stream_tier3_equivalence`
using exactly the shape P4.68's lane record wrote out; (2) the two stray
participant-status parsers (`services/answer_confirmation.rs:336` with the
OLD unknown→Active rule; `crates/quilltap-harness/tests/message_
attribution_equivalence.rs:27` which `panic!`s on an unknown status) onto
the ONE home (§C), output-neutral and pinned; (3) the three families whose
committed recipes stage into the SHARED `/tmp/qt-oracle-stage`
(`precompute_equivalence.rs:83`, `mail_carina_tools_equivalence.rs:16`,
`photo_tools_equivalence.rs:40` + their `.test.ts` twins) moved onto
lane-unique stage dirs so two sweeps can never clobber each other; (4) the
two handler-logging rows the round left unpinned — `services/file_
fallback.rs`'s five `[Image Fallback]` lines' FIELDS against v4's
`lib/chat/file-attachment-fallback.ts` context objects (capture-layer
pins, one mutation per line), and `services/cost_events.rs::
create_system_event`'s swallowed write, which returns `None` where v4
`system-events.service.ts:74` logs `Failed to create system event` with
`{chatId, eventType, error}` — plus a WRITTEN inventory of every
handler-logging row still open after this lane, so the "sweep" stops being
a phrase and becomes a list. Must NOT touch: `api/**` (P4.72/P4.73),
`services/chat_create.rs` or whichever file P4.73 names for the trio, any
`quilltap-web` file, the host crate, the SPA, or any committed fixture
other than the `primary-stream` builder's ADDITIVE widening.

## Survey-verified starting points (2026-09-03)

1. **The `auth` arm's family is `primary_stream_tier3_equivalence`**
   (`crates/quilltap-harness/tests/primary_stream_tier3_equivalence.rs`;
   oracle `harness/oracle/cases/primary-stream-tier3.test.ts` — the ONLY
   oracle case with `toConnectionProfile(spec.profile)`, at four sites).
   P4.68's lane record (`status-log.md` ~`:99582-99606`, "⚠ DEFERRED
   LOUDLY — the third blind spot") is the spec: v4
   `provider-failover.service.ts:583-591` — `resolveConnectionProfileApiKey`
   fails → `recordAttempt(understudy, 'auth', new Error(keyResolution.
   reason))`; no `auth` trigger and no `no-api-key-configured` bytes
   appear anywhere in the corpus today. Why the cheap routes fail (both
   MEASURED by P4.68): making the tier spare keyless changes
   `empty_chain_fallback` and `hard_error_chain_exhausted`; adding a
   keyless profile to the tier POOL risks the picker choosing it in
   existing cases. **The shape that works:** a per-call primary override
   (a `profileKey` on the call, resolved at the oracle's four
   `toConnectionProfile(spec.profile)` sites and its Rust twin), plus a
   second primary with `allowTierFallback: false` pointing at a KEYLESS
   understudy, both given `modelClass: null` so they stay out of every
   existing case's tier pool. The fixture builder creates exactly three
   profiles from three named spec keys — widen it ADDITIVELY (two more
   profiles, `modelClass: null`, one keyless), every existing row
   byte-preserved; re-run every consumer of the `primary-stream` pair by
   name (memory notes `a-new-fixture-built-from-a-grown-shared-fixture`,
   `parallel-round-playbook` §D "when a lane BOTH widens a shared fixture
   AND ports against it"). The comparand: the ordered `chainAttempts`
   trigger + reason bytes (the family already diffs the recorded
   per-call trace) — the `auth` row must be red-first on a v5 that
   records anything but `auth` / `no-api-key-configured`, so mutate v5's
   trigger string to prove the row discriminates. Also cover
   `api-key-not-found` (a DANGLING `apiKeyId`) if the fixture can carry it
   without disturbing the tier pool; record if not.
2. **The eleventh copy:** `services/answer_confirmation.rs:336-341` matches
   `"silent"`/`"absent"`/`"removed"`/`_ → Active` by hand — the OLD
   unknown→Active rule, where the canonical
   `chat_predicates::participant_status_from_str` (`chat_predicates.rs:120`)
   answers `None`/`"active"` → Active, `"silent"` → Silent, `"removed"` →
   Removed, ANYTHING ELSE → Absent (P4.68's §B, ratified). Measure whether
   any corpus row carries an unknown status string at this site (the
   answer-confirmation families: `answer_confirmation_equivalence` and the
   tier-3 ops that thread participants); if none does, the swap is
   output-neutral on the corpus and the pin is a UNIT test at the site
   with an unknown string (`"paused"`) asserting the canonical Absent —
   red-first against the old match. One line of production change; the
   `AttributionParticipant` construction around it untouched.
3. **The twelfth copy:** `message_attribution_equivalence.rs:27-35
   parse_status` — `panic!("unknown status {other}")`. A harness-side
   copy with a stricter rule than v4's; retire it onto
   `participant_status_from_str(Some(s))` so a future corpus row carrying
   an unknown status measures v4's Absent instead of aborting the binary.
   Re-run the family by name; it must be byte-neutral (no corpus row is
   unknown today — assert that as a count guard so the retirement is not
   vacuous).
4. **The three shared-stage recipes.** `ggrep -rln qt-oracle-stage
   crates/quilltap-harness/tests` lists TEN files today; the three this
   round named are `precompute_equivalence.rs:83`, `mail_carina_tools_
   equivalence.rs:16` (`STAGE=/tmp/qt-oracle-stage` beside `WT=<worktree>`
   — note the `WT=` alias must become the driver-recognized
   `V5W=${V5W:-$HOME/source/quilltap-v5}` form, memory note
   `recipe-header-conventions`), `photo_tools_equivalence.rs:40`. The other
   seven (`web_search_tool`, `doc_fs`, `doc_enum`, `search_tools`, `doc_text`,
   `state_cascade`, `image_gen_leaves`) are IN SCOPE too if their stage
   line is the shared path — measure each with `recipe_sweep.py --show
   <family>` and fix every one that stages into `/tmp/qt-oracle-stage`
   (the round-record said three; the census may say ten — report the true
   number). Shape: a per-family stage dir (`/tmp/qt-<family>-stage`),
   regen and run naming the SAME NDJSON path, `--show` read back, the
   family `--run` green from the pin, and `--self-test` still 0 failures.
   Do NOT change what any recipe measures.
5. **The `[Image Fallback]` lines.** v5 `services/file_fallback.rs` carries
   five: `:748` (`Describer answered without the image; discarding its
   description`, warn), `:1138` (`Primary profile failed, retrying with
   uncensored fallback`, info), `:1240` (`Could not build a fallback chain
   for the describer`, warn), `:1270` (`Trying a describer stand-in`,
   info, fields `primary_profile_id`/`stand_in_profile_id`/`stand_in_name`/
   `stand_in_provider`/…), `:1292` (`Describer stand-in answered`, info).
   v4 `lib/chat/file-attachment-fallback.ts` carries nine (`:399` reasoning
   maxTokens warn, `:426` resize-failed warn, `:505` failed-to-record-log
   warn, `:519` the without-the-image warn, `:545` empty-response error,
   `:594` suspicious-response warn, `:613` success info, `:632` error) —
   the stand-in lines are v4's `provider-failover`-side twins (find them
   by the sentence bytes in `lib/services/provider-failover.service.ts` or
   wherever the describer chain logs; measure, do not assume they are in
   the same file). For each v5 line: the sentence AND the context object's
   keys byte-faithful to v4's (P4.70's `[Image LoRA]` caller-spread
   precedent — `image_gen/lora_support.rs:409+` `log_context_tests` is the
   idiom: a thread-scoped capture layer, one test per line, one mutation
   per line reddening exactly one test; memory notes
   `a-process-global-test-seam-must-be-thread-scoped`,
   `capture-layer-target-assert-is-a-prefix-match`,
   `coalescing-hides-its-own-mutation-proofs`). Where v4 has a line v5
   lacks (`:399`/`:426`/`:505`/`:545`/`:594`/`:613`/`:632`), port the
   line at v5's analogous site if the site exists, else record it in the
   inventory by v4 line — a log-only port needs no oracle but does need
   the capture pin (memory note `differential-blind-to-a-log-only-fix`).
6. **`create_system_event`** (`services/cost_events.rs:57`): returns
   `Option<String>` and swallows a failed write; v4
   `system-events.service.ts:73-79` catches, logs `logger.error('Failed to
   create system event', {chatId, eventType: event.systemEventType, error})`
   and returns `null`. Port the error line with a capture pin (force the
   failure by breaking the table in the test — memory note
   `force-a-swallowed-catch-by-breaking-the-table`).
7. **The handler-logging inventory** has never been written down as a
   list: P4.61 closed five of eight `[Title Update]` lines and named the
   `create_system_event` sibling; the round records since then each "added
   a row" (P4.68, P4.70, the `[Image Fallback]` fields) without a home.
   Deliver `docs/developer/porting/handler-logging-inventory.md`: one row
   per v4 handler log line still without a v5 twin or without a capture
   pin, with the v4 file:line, the sentence, the v5 site (or NONE), and the
   disposition (PORTED-PINNED / PORTED-UNPINNED / NO-SITE / NO-PORT with
   evidence). Seed it from the six background-job handlers under
   `lib/background-jobs/handlers/` + `lib/chat/file-attachment-fallback.ts`
   + `lib/services/system-events.service.ts` by mechanical grep
   (`logger\.(info|warn|error|debug)\(`), and mark every row this lane
   closes. The next logging order works from this file.

## Tiered deliverables

**Tier 1 (must land):**

1. The `auth` / `no-api-key-configured` arm (item 1), red-first, the
   builder widened additively, every `primary-stream` consumer re-run by
   name.
2. The eleventh and twelfth status-parser copies retired onto
   `participant_status_from_str` (items 2–3), each with its
   non-vacuous pin.
3. Every committed recipe that stages into `/tmp/qt-oracle-stage` moved
   onto a lane-unique stage (item 4), `--show`-verified, `--self-test`
   green, each family `--run` green from the pin.
4. The five `[Image Fallback]` lines' fields capture-pinned (item 5) and
   `create_system_event`'s error line ported + pinned (item 6).

**Tier 2 (should land):**

5. The v4 `[Image Fallback]` lines v5 lacks, ported at their analogous
   sites where a site exists (item 5's second half).
6. `docs/developer/porting/handler-logging-inventory.md` (item 7) with
   this lane's rows marked closed.

**Tier 3 (explicit deferrals, loud):** the `api-key-not-found` arm if the
fixture cannot carry a dangling id without disturbing the tier pool
(record the measurement); any `[Image Fallback]` line whose v5 site does
not exist (inventory row, not a stub); the background-job handlers' own
lines beyond the seeded inventory (the inventory IS the deferral).

## The differential requirement (restated)

The `auth` arm is a corpus row in `primary_stream_tier3_equivalence`
against v4's REAL `provider-failover.service.ts` over the widened fixture,
regenerated from the pin through the sweep driver, red-first with a
v5-source mutation (memory notes `fixture-side-mutation-proves-nothing`,
`mutation-attribution-needs-a-narrowed-corpus` — a first-mismatch family
cannot redden a row appended at the END; narrow the corpus to the new row
for the attribution run, then restore). The two parser retirements are
output-neutral on the corpus (re-run by name, byte-identical NDJSON diff)
and pinned at unit level. The log rows are capture-layer pins (a
differential cannot see them). The recipe moves are proven by `--show` +
`--run` + the NDJSON regenerating byte-identical to the pre-move oracle
(the regen changed WHERE it stages, not WHAT it measures).

## Fixtures

Consumes the committed `primary-stream` pair (widened ADDITIVELY by this
lane through its shipped builder — every existing row byte-preserved,
proven by a cell-by-cell diff of the old rows; every consumer of the pair
re-run by name) and the fixtures the recipe families already build. No
other committed fixture changes. P4.73's NEW `images-*` pair and P4.72's
venues are disjoint by name.

**Ownership rider (this lane only):** if item 4's census finds more than
the three named recipes staging into the shared path, those families'
HEADERS join this lane's ownership for the stage line ONLY — name each in
the lane record; no sibling lane edits a recipe header this round.

## Shared contracts (binding, identical across all four orders)

- **§A — well-formed actions never move.** P4.72 may change the answer to
  `?action=` (empty), an UNKNOWN action, and DUPLICATED keys only; the
  status + body of every well-formed `?action=<served>` request and every
  single-valued query is byte-identical before and after (the existing web
  families are the pin). No SPA caller changes.
- **§B — `crates/quilltap-web/src/query.rs` is the ONE query reader**
  (`first` / `all`, v4's `searchParams.get` / `getAll`). P4.73's new
  `images_routes.rs` reads its `?action=` and `?tagId=` through it and adds
  nothing to it; P4.72 changes no signature in it.
- **§C — participant-status parsing has ONE home:**
  `quilltap_core::chat_predicates::participant_status_from_str`. P4.74
  retires the two stray copies; no lane adds a private status match.
- **§D — the pixel codec has ONE accessor:** `Engine::qtap_pixel_codec()`
  (`api/engine.rs:762`). P4.73 threads it into the chat-upload arm and the
  new images arms; no other lane constructs a `PixelCodec`.
- **§E — the `Request`/`Response` enums are P4.73's this round.** Only
  P4.73 adds variants (`ImagesList`, `ImageUpload`, `ImageImportFromUrl`,
  `ImagesGenerate`, `ImageDelete` — append-only, at the end of the enum,
  `serde(rename_all = "camelCase")`) and only P4.73 changes an existing
  variant's field types (`FileUpload.tags`, the `ChatCreate` trio). P4.72's
  census READS `api/types.rs`; it never edits it.
- **§F — `photos_routes.rs` same-file exception.** P4.73 edits that file
  ONLY to delete `image_delete_not_available` (`:161-167`) once the real
  DELETE lands in `images_routes.rs`; P4.72 edits it anywhere else (the
  `q`/`tag` duplicate rows). Both lanes name the region in their commit
  message so the unifier's hand-merge is trivial.
- **§G — `files_routes.rs` same-file exception.** P4.73 edits that file
  ONLY inside `files_upload_post` (`:943-1010`, the `tags` parse block that
  P4.62(a) widens); P4.72 edits it anywhere else (the thumbnail GET, the
  chat-files POST, the `force` duplicate row). Same commit-message rule.
- **§H — the streaming avatar is client-only.** P4.75 reads
  `respondingParticipantId` (already on `ChatStreamState`), the chat's
  participants and `avatarDisplayMode`; no server payload changes and no
  lane touches the stream frames.

## Ownership (binding, identical across all four orders)

| Area | Owner |
|---|---|
| `crates/quilltap-web/src/**` EXCEPT `lib.rs`, the NEW `images_routes.rs`, and the two §F/§G regions; `crates/quilltap-web/tests/**` (the grown `query_param_semantics_equivalence`, the NEW dispatch wrong-type census guard, arms in the existing body-guard families); `quilltap-core`: `api/chat_media.rs` (the `chat_file_link` guard ONLY); harness: `harness/oracle/cases/query-param-semantics.test.ts` | **P4.72** |
| `quilltap-core`: `api/types.rs` (append-only new variants + the `FileUpload.tags` and `ChatCreate` trio field widenings), `api/engine.rs` (the new dispatch arms, the `ChatFileUpload` codec line `:4121`, the `FileUpload` arm `:4362`), `api/files.rs` (`file_upload`'s `link_ids` widening), the NEW `api/images.rs`, the file that validates `Request::ChatCreate`'s body (named in the lane record at start — `api/chats.rs` or `services/chat_create.rs`, measured), `services/file_storage.rs` (a codec seam ONLY if the images arms need one); `quilltap-web`: `lib.rs` (route registration), the NEW `images_routes.rs`, the §F/§G regions; `quilltap-host`: `host.rs` (the import-fetch seam wiring), a NEW `image_import_fetch.rs`; harness: NEW `images_routes_equivalence` + `harness/oracle/cases/images-routes.test.ts`, a NEW committed `images-{main,mount}.db` pair + builder if needed, `files_routes_equivalence` (the codec WIRING arm), `chat_create_capstone` (the trio arms), `lora_log_anchor_guard` (the ninth anchor moves); SPA: `core-contract.ts` (the §1 type-only mirror of the new verbs) | **P4.73** |
| `quilltap-core`: `services/answer_confirmation.rs` (the status match ONLY), `services/file_fallback.rs` (log fields ONLY), `services/cost_events.rs` (the swallowed-write warn ONLY); harness: `message_attribution_equivalence.rs`, `primary_stream_tier3_equivalence.rs` + `harness/oracle/cases/primary-stream-tier3.test.ts` + its fixture builder (additive), the three shared-stage recipe headers (`precompute_equivalence.rs`, `mail_carina_tools_equivalence.rs`, `photo_tools_equivalence.rs` + their `.test.ts` twins), the NEW handler-logging inventory doc | **P4.74** |
| SPA: `chat/{streaming-message,message-list}.ts` (+specs), `documents/open-document-from-search.ts` + `search/search-results.ts` + whatever workspace file the root cause names (+specs), the `title=` census script under `apps/web/scripts/`, the residue hosts' files (each named in the lane record), `screens/files/folder-picker.ts` (`#move-folder` only if fixed); e2e: `workspace-search-documents-flow.spec.ts`, NEW streaming-avatar beat | **P4.75** |
| Playwright (port 4319) | **P4.75's branch only**; every other lane's beats are authored gated (ACTIVATE-AT-UNIFY) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it |

**Version bumps:** this lane bumps `quilltap-core` and `quilltap-harness`
per commit (base: core 0.0.768, harness 0.0.662); the unifier recounts as
base + total bumps (three lanes bump core and harness — memory note
`parallel-round-playbook` §C).

## Verification gate before declaring done

`cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
-D warnings` on BOTH feature sets; full `cargo test --workspace` with the
lane's oracle env vars (`run_in_background`, full log + sentinel, every
`test result:` line read, zero `SKIP:` for the lane's families); the
touched families by name through the sweep driver from the pin
(`primary_stream_tier3_equivalence` red-first then green with the changed
bytes grepped — `no-api-key-configured`, `"auth"`; `message_attribution_
equivalence` and the answer-confirmation families byte-neutral; every
re-staged recipe family green from `--run`); `python3
harness/tools/recipe_sweep.py --self-test` 0 failures; mutation proofs
applied-then-reverted by file backup; `cargo build --workspace --release`.
No SPA run, no Playwright. Per-commit rule: `.claude/commands/commit.md`.
Lane record → `status-log.md` ("Lane record — P4.74"), the status header
here updated at close, and P4.68's status header gains one line naming
where its deferred arm closed.
