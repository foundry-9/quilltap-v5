# Work order P4.93 — the harness + logging maintenance smalls the `2075242f9` round surfaced, closed with evidence: (A) the `QT_FIXTURE_HELP_MAIN` two-family collision split by name + a fixture-SENSITIVE arm for the thin guards family; (B) the tree-wide `to_completion_messages` tool-role blind spot — ONE inverse for `CompletionRole` in its home and every hand-rolled harness mapper repointed (nine silent catch-alls, one silent-to-`Tool`, twelve loud ones — all but P4.92's); (C) the `db::memories` capture-rig intermittent moved onto `global_capture`; (D) the fifteen `[CharacterAvatar]` handler log lines v5 never carried, byte-exact with capture pins — from `main`

**UNIFIED on `main` (2026-09-16) — CLOSED WHOLE.** The §3 review found nothing that blocked outright, but ONE Tier-1 claim was false and is fixed on the unify branch: the `:589` `Failed to save avatar image` line had only a SILENCE leg (deleting the whole `tracing::error!` survived every test — §R.6's finding shape) and dropped the exception v4 attaches — it now carries `error = %e`, and a `break_files_insert` plant (a BEFORE INSERT trigger on `files`, so the cache lookup's SELECTs still succeed and the job reaches the write) drives a firing pin: ERROR, the common bag, the plant's own sentence in `error=`, the job failing with v4's prefix, the completion line silent. Also fixed: (2) the `:255` `categories` array and the P4.D184 cache-hit `leafCounts` object rendered Rust `Debug` strings where v4 logs structure — both ride P4.91's `…Json` file-layer convention (`categoriesJson`, `leafCountsJson`), landed on this same branch, which the lane could not have known; (3) the `:292` NO-PORT was one branch short — v4's `try` also wraps the routing resolver, and v5 swallowed that read's `Err` with `.ok()`; the arm now logs v4's sentence with the error and carries on unrouted (a silence leg on the classified-as-dangerous test; a FIRING pin is not plantable through the seed because the resolver reads `image_profiles`, which the job's own profile read reaches first — recorded); (4) the memories rig doc's "four" tests are five. Decided: the P4.D184 line's snake_case respelling is a recorded convergence onto the tree's key spelling, not held against the lane — but `combined.log` key case vs v4's camelCase is a standing tree-wide divergence the round names ONCE in its record. Round record: `status-log.md`.

**Status: CLOSED at lane close (2026-09-16), branch
`claude/harness-logging-smalls-b86378`. Tier 1 items 1–7 and Tier 2 items 8–10
ALL landed; nothing is left OPEN under this order.** Four commits. The §R.2
probe PASSED at lane start and again after the regen batch (v4 `main`, clean,
both logs empty), and the §R.3 baseline-vs-target diff for this lane's v4 paths
was EMPTY, so every regen ran from the lane pin `/tmp/qt-v4-pin-p4.93-2075242f9`
at the BASELINE.

**Three of the order's premises were refuted by measurement, and the refutations
are the lane's most useful results:**

1. **M1's named witness cannot be `orchestrator_tier3`** (Tier 1 item 4). §R.10
   (b) forbids this lane from editing that file, so its mapper still hand-rolls
   its own (correct) `"tool"` arm and `from_v4_wire` never runs for it — it
   stays GREEN under M1, measured. The witness is `native_tool_loop_tier3`
   (8 tool rows → RED with the exact "no canned stream queued for key" shape the
   inverse prevents), with `text_tool_loop_tier3` (0 tool rows) as the contrast.
   When the unifier repoints P4.92's file at the wire, M1 will redden it too —
   that is what the census guard's named exemption is for.
2. **The "exposed pair" is not exposed** (Tier 1 item 5). v4 emits `role: 'tool'`
   at exactly ONE site, `lib/services/chat-message/tool-call-threading.ts:94`,
   with four callers — so the only families that can carry a tool row are the
   five that ALREADY had the arm. `enclave_step_tier3`'s stream mock has no
   tool-call channel on either side (`{content}` / `{done,usage,…}` only) and
   its corpus has zero tool-call rows; `regenerate_swipe_tier3` is a single
   non-streaming `sendMessage` with no loop at all. The nine catch-alls were
   LATENT, not live; no corpus case was added, and the reason is recorded.
3. **The `global_capture` rig cannot carry the memories tests** (Tier 1 item 6).
   It is `async` and renders the MESSAGE only, while every assertion there reads
   `key=value` fields. The load-bearing half is the ARMING, which landed in
   `quilltap-host/src/lock.rs`'s shape (same fix, same symptom, same rig), cited
   in the test doc beside the `global_capture` module doc the order named.
   `test_support.rs` is not in this lane's Owns column and was not touched.

**Two Tier 2 items were no-ops with evidence:** item 8 — `avatar_job_tier3_
equivalence.rs:688-711` reads captured lines with `.any(…)`, not an exact count,
so there is no pin to widen and that file is untouched; item 9 — `grep -rn
QT_FIXTURE_HELP_MAIN harness/tools/` finds NOTHING and no committed
`sweep-results/*.json` mentions it (the driver derives every recipe from the
test header), so the rename needed no driver-side alias and
`harness/tools/recipe_sweep.py` is untouched.

**One deliberate deferral, loud and typed:** v4's `[StoryBackground] Concierge
uncensored reroute succeeded` (`story-background.ts:769`) is NOT ported —
recorded as `STORY_REROUTE_SUCCEEDED_UNPORTED` in `image_job_common.rs` with an
explicit no-op arm and a silence leg. **One recorded change outside the fifteen
lines:** the P4.D184 cache-hit line's four camelCase field names respelled
snake_case (sentence, level and target unmoved).

**For the unifier:** (a) delete `role_mapper_inverse_guard.rs`'s
`orchestrator_tier3_equivalence.rs` exemption when P4.92's mapper is repointed
(the guard fails if the exemption outlives its cause); (b) `help_doc_sync_guards_
equivalence` now reads `QT_FIXTURE_HELP_SYNC_GUARDS_MAIN` — the gate's env block
needs both names; (c) THREE pre-existing reds were surfaced by re-running
untouched families and PROVEN pre-existing (identical at `main`'s version of
each file, same oracles): `courier_images_routes` (`no such column:
generationKey` — the P4.D182 `files` vintage gap), `pascal_run_custom_handler`
(`no such column: cycleOrderParticipantIds` — the P4.D171 `chats` vintage gap),
and `pascal_custom_tools_route` (`case 'list' status` 500 vs 200, the same
family class the `49769ec4` round already recorded as Pascal fixture rot). None
is this lane's, none is attributable to the role sweep, and each wants a fixture
heal rather than a port change.

The full lane record — regen commands as run, counts, the mutation tables, and
every measurement above with its citation — is in `status-log.md`.

## Preamble

- **v4 oracle baseline: `2075242f9`.** This lane absorbs NO drift row — it
  is candidate 5 of the `2075242f9` round's "What is next" (phase-4.md
  `:6685-6696`) minus the items this round deliberately leaves out (the
  shared-fixture title-checkpoint hazard, the `SCENE_STATE_TRACKING`
  trigger, the rolls save's 500-vs-400 prose — see the round's planning
  record). v4's code is the spec at the BASELINE pin: `lib/background-
  jobs/handlers/character-avatar.ts` (the 19 log sites) and the help-sync
  fixture builder's spec.
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** ONE lane-unique pin **`/tmp/qt-v4-
  pin-p4.93-2075242f9`** (the baseline — record `git -C ~/source/quilltap-
  server diff --stat 2075242f9..1fefadb9a -- lib/background-jobs/ lib/
  help-docs/ lib/services/help*` EMPTY before the first regen).
- **Sibling lanes:** P4.D195 (bug 147 whole — the chat GET projection + the SPA seed + the help re-vendor), P4.91 (the photos-predicate consolidation + the collapse heal's census log shape), P4.92 (the tool loops' `base_params` carry-over class). P4.92 owns `orchestrator_tier3_
  equivalence.rs` — your mapper sweep SKIPS that file (§R.10 (b)); P4.92
  RUNS `chat_create_capstone` / `primary_stream_tier3` as neutrality legs
  while you edit their mappers (§R.10 (c) — run vs edit); P4.D195 RUNS the
  `help_*` content families under TODAY's env names while you rename the
  guards family's var (§R.10 (a)); P4.91 owns `db/avatar_rolls_collapse_
  heal.rs` — your avatar lines are in the two `services/` files (§R.10
  (d)).
- **Worktree:** `git worktree add <path> -b claude/p4-93-harness-logging-
  smalls-<hash> main`; `CARGO_INCREMENTAL=0`. No SPA.
- **Read before the work:** (A) `crates/quilltap-harness/tests/help_doc_
  sync_equivalence.rs:17-27` (header), `:84-99` (`QT_FIXTURE_HELP_MAIN`
  read, the `help-sync.json` spec), `:128-132` + `:339` (the whole-table
  row diff), `:427-438` (sentinel pins); `help_doc_sync_guards_
  equivalence.rs:20-38` (header — already carries the staging-dependency
  repair note and its FAMILY-SPECIFIC `/tmp/qt-help-sync-guards-main.db`),
  `:74-89`, `:122` (`SELECT id … ORDER BY id`), `:134-149` (the six-field
  summary — the ONLY fixture-content channel is `helpDocIds`), `:165-182`;
  `harness/oracle/fixtures/build-help-sync-fixture.ts` (ONE builder, ONE
  spec → the two `.db`s differ only in page/header noise — WHY each passes
  on the other's); `harness/oracle/cases/help-sync-guards.ts`; memory
  `a-second-family-on-one-table-collides-by-default`, `a-family-env-var-
  is-not-its-regen-var`. (B) `crates/quilltap-core/src/model/completion.
  rs:24-41` (`CompletionRole {System, User, Assistant, Tool}` — ONLY a
  one-way `as_str()`; no `FromStr`), `:284`, `:299` (the canned key
  renders the role — a misfiled role MISSES silently); the fixed exemplar
  `orchestrator_tier3_equivalence.rs:286-300` (P4.90's comment — the why);
  the Class-1 list (silent catch-all → `User`, no `tool` arm):
  `announcer_tier3_equivalence.rs:456-459`, `regenerate_swipe_tier3_
  equivalence.rs:122-125`, `appearance_sanitize_gate_tier3_equivalence.rs:
  272-275`, `courier_images_routes_equivalence.rs:674-677`, `chat_create_
  capstone_equivalence.rs:381-384`, `enclave_step_tier3_equivalence.rs:
  192-195`, `initial_greeting_equivalence.rs:194-197`, `compression_cache_
  tier3_equivalence.rs:117-120`, `in_scene_voiced_tier3_equivalence.rs:
  590-592`; the oddball `external_prompt_tier3_equivalence.rs:261-266`
  (unknown → `Tool`); `file_attachment_tier3_equivalence.rs:612-619` (→
  `StreamMessage::User`); the Class-2 list (loud `panic!`, no `tool` arm):
  `pascal_run_custom_handler_equivalence.rs:158-162`, `pascal_workbench_
  route_equivalence.rs:229-233`, `primary_stream_tier3_equivalence.rs:
  331-335`, `context_summary_service_tier3_equivalence.rs:548-552`,
  `memory_processor_tier3_equivalence.rs:488-492`, `fold_episode_tier3_
  equivalence.rs:307-311`, `memory_pipeline_jobs_tier3_equivalence.rs:
  406-410`, `compression_tier3_equivalence.rs:210-214`, `carina_memory_
  extraction_tier3_equivalence.rs:344-348`, `build_context_tier3_
  equivalence.rs:459-462`, `pascal_custom_tools_route_equivalence.rs:210-
  213`, `message_formatter_equivalence.rs:126-139` (`WireRole`/
  `MessageRole` — a DIFFERENT enum pair; measure before touching); the
  already-correct Class 3 (eleven families with a `tool` arm — the
  template); memory `canned-key-role-catchall-hides-a-tool-role`, `a-
  canned-miss-that-prints-only-a-count`. (C) `crates/quilltap-core/src/db/
  memories.rs:891`, `:921` (the two siblings that reach the callsite with
  nothing capturing) and the failing test `delete_with_unlink_logs_v4s_
  complete_debug`; `crates/quilltap-core/src/test_support.rs` (module doc
  — the `Interest`-cache paragraph; the `global_capture` rig); the
  diagnosis at `status-log.md:124346-124363`; memory `a-process-global-
  test-seam-must-be-thread-scoped`, `capture-layer-target-assert-is-a-
  prefix-match`, `coalescing-hides-its-own-mutation-proofs`. (D) v4
  `lib/background-jobs/handlers/character-avatar.ts` — the 19 sites
  (survey table below); v5 `crates/quilltap-core/src/services/character_
  avatar_job.rs` (ONE `tracing::` today, `:313-321`, the P4.D184 reuse
  line) and `services/image_job_common.rs:343-416` (three shared-path
  lines); `services/headshoulders_backfill_job.rs:95` (the house rule:
  "because it is what an operator greps `combined.log` for"); `crates/
  quilltap-harness/tests/avatar_job_tier3_equivalence.rs:701` (reads
  captured lines — check for an exact-count pin); the P4.61 title-update
  lines lane record (the closest recipe for a byte-exact log-line port
  with capture pins + silence legs + NO-PORT arms with v4-source
  evidence); memory `tracing-percent-field-renders-unquoted`, `a-
  guard-whose-other-conjuncts-are-false-is-untested`.

## Mandate

Four smalls, each closed with evidence. (A) End the `QT_FIXTURE_HELP_MAIN`
collision by NAME: the guards family reads its own variable, both headers
say so, the sweep driver's block follows — and the thin guards family
gains a fixture-SENSITIVE arm (its six-field summary reads only seeded
ids; add a content channel — `contentHash` or `title` of the surviving
rows — on both sides, so a wrong fixture reddens it). (B) Give
`CompletionRole` the inverse it never had (`from_v4_wire(&str) ->
Option<Self>` beside `as_str()`, exhaustive over the four wire spellings,
`None` for anything else), then repoint every hand-rolled harness mapper
onto it — Class 1 keeps its call-site default (`unwrap_or(User)`) so
behavior on today's corpora is UNCHANGED while a `tool` row finally keys
right; Class 2 keeps its `panic!` on `None`; the two oddballs measured
individually — with every edited family re-run by name on a pin-fresh
oracle GREEN (the canned KEY bytes must not move: §R.10 (f)). The two
EXPOSED Class-1 families (`enclave_step_tier3` drives an autonomous-room
turn with `custom_tools: true`; `regenerate_swipe_tier3` is `processMessage`'s
sibling entry) get a corpus case that CARRIES a `tool` row if one is
reachable — measure; if reachable, RED-first on the misfiled key. (C) Move
the ONE intermittent test in `db/memories.rs` onto the `global_capture`
rig `test_support.rs`'s doc already prescribes; nothing else in that file
moves. (D) Port the fifteen absent `[CharacterAvatar]` sentences into
`character_avatar_job.rs` at v4's branch points, byte-exact, v4's levels
and field bags, each with a capture pin and a silence leg (the #103/#110
class — the P4.61 idiom); NO-PORT any site whose v4 branch is
unreachable in v5, with the v4 source cited. This lane does NOT touch
`orchestrator_tier3_equivalence.rs`, `db/avatar_rolls_collapse_heal.rs`,
`photos/**`, `crates/quilltap-web/src/**`, or any sibling's files.

## Survey-verified starting points (fresh survey 2026-09-16 at `2075242f9` and on `main` `6e0ba5e8` — do not re-derive)

### (A) The help-sync collision

- Both families read `QT_FIXTURE_HELP_MAIN` (`help_doc_sync_equivalence.
  rs:84`, `help_doc_sync_guards_equivalence.rs:74`); oracle vars differ
  (`QT_ORACLE_HELP_SYNC` / `QT_ORACLE_HELP_SYNC_GUARDS`); recipe paths
  differ (`/tmp/qt-help-sync-main.db` / `/tmp/qt-help-sync-guards-main.
  db`). The two other `help_doc_*` families do not contend (`help_doc_
  ensure` uses `QT_FIXTURE_ENSURE_DIR`; `help_tree` has no fixture var).
- **Why each passes on the other's fixture:** ONE builder (`build-help-
  sync-fixture.ts`) from ONE spec (`help-sync.json`, read by both tests at
  `:97-99` / `:87-89`) → the two `.db`s are logically identical (the
  "`cmp` parts at byte 1" is page noise). So the record's "at least one
  arm is fixture-insensitive" is TRUE of the guards family specifically:
  its per-scenario compare is `{scenario, totalOnDisk, deleted, failed,
  chunksWritten, helpDocIds}` and `helpDocIds` (`SELECT id … ORDER BY id`)
  is the only content channel — it never reads title/content/hash/
  embedding/timestamps. The sync family diffs the WHOLE `help_docs` table
  row-by-row (`:128-132`, `:339`) + sentinel pins — strongly sensitive.
- The workspace gate today cannot tell the guards family ran on ITS
  fixture; only durations say it ran at all.

### (B) The role-mapper blind spot

- `CompletionRole` has `as_str()` only (`completion.rs:33-40`); the canned
  key renders it (`:284`, `:299`). Every family hand-rolls the inverse.
- Class 1 (nine silent → `User`), the oddball (→ `Tool`), the
  `StreamMessage` variant, Class 2 (twelve loud), Class 3 (eleven correct)
  — line-listed in the Preamble. **Exposure:** `enclave_step_tier3` (drives
  v4's REAL `handleAutonomousRoomTurn`; its own request sets `custom_tools:
  true` at `:328` — a tool loop puts a `tool` row in the re-stream) and
  `regenerate_swipe_tier3` (`processMessage`'s sibling) are LIVE-exposed;
  `courier_images_routes` low (single-shot cheap-LLM); the other six are
  single-shot prompt assemblies — latent. Class 2 cannot mis-key (it
  panics) but would abort a future tool-row corpus.
- Every oracle case records `role` verbatim off v4's `params.messages`
  (e.g. `enclave-step-tier3.test.ts:223`), so a corpus that reaches a
  tool loop WILL carry `"tool"`.

### (C) The `db::memories` intermittent

- `delete_with_unlink_logs_v4s_complete_debug` failed once in a full
  workspace run ("exactly one complete line: []"), passed alone and ×2 in
  its binary. Mechanism (`status-log.md:124346-124363`): `captured`
  installs a THREAD-scoped subscriber; `tracing` caches each callsite's
  `Interest` globally on first use; the two sibling tests at `memories.rs:
  891` / `:921` reach `delete_with_unlink` with nothing capturing and can
  lose the callsite for everyone. The sanctioned fix is the `global_
  capture` rig `test_support.rs`'s module doc names (inventoried at
  `status-log.md:108706`).

### (D) The `[CharacterAvatar]` lines

- v4 `character-avatar.ts` — 19 sites: `:102` Starting avatar generation;
  `:128` Image profile has no API key, skipping; `:138` API key not found
  or invalid, skipping; `:167` No appearance data available, skipping;
  `:211` Reused cached avatar for this configuration (v5 HAS — `character_
  avatar_job.rs:313-321`); `:237` Failed to build cheap LLM selection for
  danger classification; `:255` Avatar prompt classified as dangerous;
  `:274` Rerouted to uncensored image provider; `:282` No uncensored image
  provider available, using original; `:292` Prompt classification failed,
  continuing normally; `:378` Image generation failed (v5 HAS — `image_
  job_common.rs:343-349`); `:387` Image provider rejected for content
  moderation, rerouting through Concierge uncensored profile (v5 HAS —
  `:379-388`); `:439` Concierge uncensored reroute succeeded; `:468` Image
  generation failed (Concierge reroute also failed) (v5 HAS — `:410-416`);
  `:479` No images returned from provider; `:490` Generated image has no
  data; `:583` Avatar image saved; `:589` Failed to save avatar image;
  `:604` Avatar generation completed. **Fifteen absent** (the record's
  "18" was v4's 19 minus the one P4.D184 landed, before the three shared-
  path lines were counted). For EACH: read v4's level (`logger.info/warn/
  error`) and its context bag keys from the post-commit file; find v5's
  branch point; port level + sentence + bag byte-exact; where v5's branch
  cannot be reached (a v4-only guard), NO-PORT with the v4 lines quoted.
- Log records have no oracle (`crates/quilltap-web/src/lib.rs:133-135`);
  `avatar_job_tier3_equivalence.rs:701` reads captured lines — check
  whether it pins an exact count (widen ONLY if it does).

## Tiered deliverables

### Tier 1 — must land

1. **(A) The rename.** `help_doc_sync_guards_equivalence.rs:74` reads
   `QT_FIXTURE_HELP_SYNC_GUARDS_MAIN`; the skip sentence `:79-80` and the
   three header lines `:27,:31,:37` follow; the sync family's header
   `:17-27` gains one sentence naming the split. `recipe_sweep.py`'s
   header validation must still pass (`--self-test`; the driver keys on
   the header lines — if it needs an alias, edit it minimally and say
   so). Both families regenerated at the pin through the driver and run
   by name — GREEN; then the discriminator: run the guards family with
   the SYNC fixture's path under the NEW var → it must still pass TODAY
   (recording that the two files are logically identical), which is what
   item 2 changes.
2. **(A) The fixture-sensitive arm.** The guards family's per-scenario
   JSON gains ONE content column for the surviving rows (`contentHash`
   or `title`, joined in id order — pick the one the oracle case
   `help-sync-guards.ts` can emit with the smallest edit; both sides).
   Then a MUTATION of the fixture, not the code: build a second guards
   fixture with one seeded doc's content altered (through the builder's
   spec, a copy) and run the family against it with the UNCHANGED oracle
   → RED. Record the red; the committed builder/spec unchanged.
3. **(B) The inverse.** `completion.rs`: `impl CompletionRole { pub fn
   from_v4_wire(s: &str) -> Option<Self> }` (four arms, `None` otherwise;
   doc: the canned key renders the role, so a caller's default must be a
   CONSCIOUS choice — P4.90's finding quoted) + a unit test over the four
   spellings + one unknown. No `FromStr` unless a caller needs the trait
   (say why if you add it).
4. **(B) The sweep.** Every Class-1 family: `CompletionRole::from_v4_wire
   (r).unwrap_or(CompletionRole::User)` (behavior on today's corpora
   UNCHANGED; a `tool` row now keys right); the `external_prompt` oddball:
   measure whether v4's external-prompt path can emit a `tool` row (it
   cannot — single-shot; then `unwrap_or(User)` like its siblings, and the
   `Tool` default recorded as an accident); `file_attachment_tier3`'s
   `StreamMessage` mapper gains a `"tool"` arm ONLY if `StreamMessage` has
   a tool variant — measure, else record; Class 2: `from_v4_wire(r).
   unwrap_or_else(|| panic!(…))` with the same message; `message_formatter_
   equivalence.rs:126-139` maps a DIFFERENT enum pair — measure whether
   `WireRole`/`MessageRole` have a tool spelling before touching (record
   the answer either way). Every edited family re-run by name on a pin-
   fresh oracle → GREEN, zero row change (the key bytes unmoved — §R.10
   (f)). Mutation proof M1: make `from_v4_wire("tool")` answer `User` →
   the eleven Class-3 families that key a `tool` row today go RED (P4.92's
   `orchestrator_tier3` among them — run it by name as the proof's
   witness; do not edit it).
5. **(B) The exposed pair, measured.** `enclave_step_tier3`: does any
   existing case run a tool loop (`custom_tools: true` + a canned reply
   that calls one)? If yes, the family was ALREADY mis-keying — expect a
   pre-existing red or a vacuous canned miss; find it. If no, ADD one
   case whose canned reply calls a custom tool (the P4.90 marker idiom
   as the family's oracle allows) — RED-first on unported mappers? No:
   the mapper fix is item 4 and lands first; the case's value is
   COVERAGE — its non-vacuity pin is "the oracle recorded a re-stream row
   carrying `tool`". Same for `regenerate_swipe_tier3`. If neither can
   reach a tool loop through its oracle's mocks, record WHY (the mock
   shape) and leave the pin.
6. **(C) The rig.** `db/memories.rs`: `delete_with_unlink_logs_v4s_
   complete_debug` moves onto `global_capture` (the exact idiom in
   `test_support.rs`); run the memories binary ×3 and the FULL workspace
   gate; the `Interest`-cache paragraph in the test's doc names this
   order. Nothing else in the file moves (`git diff` shows the one test).
7. **(D) The fifteen lines.** In `character_avatar_job.rs` at v4's branch
   points (the P4.61 idiom): each `tracing::<level>!(target: …, <v4's bag
   keys> = …, "[CharacterAvatar] <sentence>")` byte-exact; a capture test
   per line (fires with v4's bag) and a silence leg (the sibling branch
   does not fire it); NO-PORT arms with v4's source quoted where the
   branch is v5-unreachable. Mutation proofs: M2 swap two sentences (e.g.
   `Avatar image saved` ↔ `Avatar generation completed`) → both pins red;
   M3 drop one bag key → its pin red; M4 fire the "skipping" line on the
   success path → its silence leg red.

### Tier 2 — should land

8. **(D) `avatar_job_tier3_equivalence.rs:701`** — if it pins an exact
   line count, widen it to the new count with the list of sentences it
   now expects (never "≥").
9. **(A) The sweep driver's env block** — grep `harness/tools/` and the
   committed `sweep-results/*.json` for `QT_FIXTURE_HELP_MAIN` to find
   every place the OLD name is baked; update the driver-side ones; the
   result artifacts are history (leave them).
10. **(B) A guard against the class returning:** a harness source census
    (the `db_error_key_guard` idiom) asserting NO file under `crates/
    quilltap-harness/tests/` matches a hand-rolled role match (`"assistant"
    => CompletionRole::Assistant` outside `completion.rs`) — every mapper
    goes through the inverse. Exempt P4.92's `orchestrator_tier3_
    equivalence.rs` BY NAME with a comment (the unifier repoints it at
    the wire and deletes the exemption — §R.10 (b)).

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **`SCENE_STATE_TRACKING`** — v5 has the vocabulary (`job_topics.rs:84`,
  `activity_kinds.rs:96`, `llm_logging.rs`, a `services/scene_state_
  tracking/mod.rs`) but NEITHER the handler (`lib/background-jobs/
  handlers/scene-state-tracking.ts`) NOR the trigger (`orchestrator.
  service.ts:230-246` → `enqueueSceneStateTracking`); `spine.rs:1635-1640`
  says so. Lane-sized (a handler + a tier-3 family) — NOT this order's;
  named for its own order in the round's planning record.
- **The rolls save's 500-vs-400** — the STATUS is already converged and
  pinned at the wire; only the body PROSE differs (two engines' own
  storage sentences) — recorded, nothing to do (`status-log.md:125865-
  125872`).
- **The lock-conflict 503-vs-409** — a v5-composition divergence that
  needs the human's RULING (v4's two lock modules disagree with each
  other) — not this order's; flagged in the planning record.

## The differential requirement, restated

(A) ships with both help-sync families regenerated at the pin through the
sweep driver and the guards family's new content column proven
fixture-SENSITIVE by a fixture mutation (RED against an unchanged oracle).
(B) ships with every edited family re-run by name on a pin-fresh oracle,
zero row change, and M1 as the cross-family proof that the inverse is
load-bearing. (C) ships with the workspace gate and ×3 binary runs. (D)
ships with capture pins + silence legs per line (log records have no
oracle — the pin is v4's bytes quoted from the post-commit file) and the
mutation table. Nothing in this lane may change a canned KEY byte.

## Fixtures

- **Consumes:** the help-sync builder/spec (READ ONLY; the sensitivity
  mutation builds a COPY under `/tmp`); every edited family's committed
  fixtures (READ ONLY — re-run, never regenerated by this lane).
- **Delivers:** the guards oracle case's new column.
- **Owns regenerating:** `help_doc_sync_equivalence`, `help_doc_sync_
  guards_equivalence` (pin — note §R.10 (a): P4.D195 also RUNS them under
  the old name); every Class-1/Class-2 family by name (pin, run only).

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `1fefadb9a` bug-147 drift catch-up + maintenance round:
**P4.D195 ∥ P4.91 ∥ P4.92 ∥ P4.93**, ordered 2026-09-16. Four lanes, four
worktrees, four lane branches, ALL from `main` (no stacking — every lane's
ownership is disjoint by file, so nothing needs a substrate commit). The
unifier cherry-picks in the order P4.D195 → P4.91 → P4.92 → P4.93 (the drift
row first; the harness-wide role-mapper sweep and the env-var rename last,
because they touch the most files and change the gate's env block), gates
ONCE, and `merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`2075242f9`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`1fefadb9a`**
  (`4.10.0-dev.41`, 2026-09-16 06:57) — ONE commit past the baseline, the
  bug-147 code commit, the ledger's one §3 row, now `ORDERED(P4.D195)`.
  v4 `bugfix` tip `1a2b2164c` and `release` tip `8fbf2afe0` are unmoved;
  there is still no `release: 4.10.0` squash. The checkout sat on `main`,
  CLEAN, at the planning probe (2026-09-16; the §2 probe PASSED, so the
  ledger stood and nothing was re-derived). **At unification the baseline
  MOVES to `1fefadb9a`** and the row retires ABSORBED(P4.D195).
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log 1fefadb9a..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Any mismatch — a moved HEAD, a dirty `lib/`/`app/`/`packages/`
  /`plugins/`/`migrations/`/`help/` tree, the checkout off `main` — is a
  STOP: report it and do nothing else.** A lane never writes the ledger.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1). HEAD is past the
  baseline and the commit touches `app/` and `help/`. Every regen runs
  from a LANE-UNIQUE detached worktree per ledger §5.1 (`git -C ~/source/
  quilltap-server worktree add --detach /tmp/qt-v4-pin-<order>-<sha>
  <sha>`, the THREE symlink classes, `cd` into it; never stash or
  checkout-switch the human's repo; `git worktree list` before each batch;
  verify the pin by grepping the fresh NDJSON for a marker only one tree
  has; check the file is non-empty before believing a diff). Which sha:
  - **P4.D195 pins the TARGET `1fefadb9a`** for every family its port
    MOVES (`salon_reads_equivalence`, the `help_*` content families, any
    turn-manager rows it adds) and the BASELINE `2075242f9` for its
    neutrality legs;
  - **P4.91, P4.92 and P4.93 pin the BASELINE `2075242f9`** — their v4
    surfaces are untouched by `1fefadb9a` (each lane records `git -C
    ~/source/quilltap-server diff --stat 2075242f9..1fefadb9a -- <its v4
    paths>` EMPTY in its lane record before the first regen). A pin at
    the baseline is still REQUIRED, not optional: the checkout can go
    dirty mid-lane (ledger §5.1), and a bare-checkout regen is
    unrepeatable.
  The sweep driver is the sanctioned per-family path
  (`harness/tools/recipe_sweep.py --run <family> --v4 "$PIN" --v5w
  <ABSOLUTE worktree>`); never run two sweeps concurrently; a family that
  reports `SKIP` in 0.00 s did not run (`a-family-env-var-is-not-its-
  regen-var`). Ledger §5.2's silent-stale-pass discipline applies to
  every regen (`rm -f` the fixture first; read the builder's last line;
  `grep -c` the changed bytes in the fresh NDJSON — MUST be > 0).
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). The
  spec for every item is `git show <sha> -- <path>` plus `git show
  <sha>:<path>`; a commit message describes the bug as its author
  understood it and the hunks are often narrower. Re-verify even where
  this order already flags the trap — the order's paragraph is the same
  kind of prose. Carry a v5-side code comment naming the sha where the
  prose misleads.
- **§R.5 — Convergence and designed trips: measure, don't assume**
  (ledger §5.4). The P4.D171 chat-GET omission pin (`salon_reads_
  equivalence`'s `get_*` cases) trips at the target pin BY DESIGN; dump
  v4's actual post-fix bodies before porting, and expect every `get`
  case to move together. The `state.cycleOrder` client read is a
  convergence measured with a table, not asserted.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest`
  is shared) and its silence leg; a fixture change lands with the regen
  of every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that
  SURVIVES as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`).
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed (and `apps/web/package.json` when it touches the SPA) on each
  commit per `.claude/commands/commit.md`; the unifier RECOUNTS as base +
  the sum of every lane's bumps (the silent-auto-merge trap: identical
  bumps merge as ONE — it fired again last round). Expected: core is
  bumped by ALL FOUR (P4.D195 `api/salon.rs`; P4.91 `photos/`, `db/`;
  P4.92 `services/orchestrator.rs`; P4.93 `model/completion.rs`, the two
  avatar job files, `db/memories.rs`); harness by ALL FOUR; web by P4.91
  ONLY (the `log_file.rs` visitor, Tier 2) — no other lane touches
  `crates/quilltap-web/src`; the SPA by P4.D195 ONLY; host/cli/tauri by
  NOBODY (a `help/**` re-vendor changes the embedded table `quilltap-
  host`'s `build.rs` produces but no host SOURCE — the P4.D194 precedent:
  no host bump for help alone).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  P4.D195 pre-lists the paths the target moves (with byte counts at the
  pin) in its lane record; no lane copies them.
- **§R.10 — Cross-lane handoffs, named:** (a) P4.93 RENAMES the guards
  family's fixture var (`QT_FIXTURE_HELP_MAIN` → a family-specific name)
  in `help_doc_sync_guards_equivalence.rs`; P4.D195 regenerates and RUNS
  the `help_*` content families under TODAY's names (it edits none of
  them) — the unifier re-runs both help-sync families with the renamed
  block after the pick. (b) P4.93's role-mapper sweep touches every
  hand-rolled `role` match in `crates/quilltap-harness/tests/` EXCEPT
  `orchestrator_tier3_equivalence.rs` (P4.92's file — its mapper already
  carries the `tool` arm); if P4.93 lands a shared inverse in
  `model/completion.rs`, the unifier repoints P4.92's mapper onto it at
  the wire (one line, recorded), never a lane. (c) P4.92 RUNS
  `chat_create_capstone` and `primary_stream_tier3` as neutrality legs
  while P4.93 EDITS their mappers — run vs edit is not a collision; the
  unified gate re-runs both. (d) P4.91 owns `db/avatar_rolls_collapse_
  heal.rs` WHOLE (the import repoint, the `:71-83` comment, and
  `report_census`); P4.93's avatar log lines live in `services/character_
  avatar_job.rs` + `services/image_job_common.rs` — different files.
  (e) P4.D195's Tier 2 item 8 may add `#[cfg(test)]` rows to `select_
  speaker.rs` / `turn_state.rs` or corpus rows to their families; no
  sibling touches either. (f) The `to_completion_messages`-class fix in
  P4.93 must leave every family's canned KEY bytes unchanged — a mapper
  that keys a role differently would silently miss; P4.93 proves each
  edited family green on its pin-fresh oracle by name.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D195** — bug 147 whole (server projection + SPA seed + help) | `crates/quilltap-core/src/api/salon.rs` — ONLY the `chat_get` projection block (`:355-390`); `crates/quilltap-harness/tests/salon_reads_equivalence.rs`; `harness/oracle/cases/salon-reads.test.ts`; `help/chat-turn-manager.md`; `crates/quilltap-harness/tests/help_tree_embed_guard.rs` — the doc-comment vendor list ONLY; `crates/quilltap-core/src/select_speaker.rs` + `turn_state.rs` — `#[cfg(test)]` modules ONLY (Tier 2 item 8), or instead `harness/oracle/cases/select-speaker*.ts` / `turn-state*.ts` + their two harness families (corpus rows only); `apps/web/src/app/core/core-contract.ts` — the ONE new key + its doc beside `cycleOrderParticipantIds`; `apps/web/src/app/chat/turn-order.ts` + `turn-order.spec.ts`; `apps/web/src/app/screens/salon/salon-conversation.ts` — ONLY `_turnEffect`, `refreshTurn`, `applyTurnResponse` and their doc comments; `salon-conversation.spec.ts`; `apps/web/src/app/chat/sidebar/chat-sidebar.spec.ts` (a wiring spec); `apps/web/e2e/salon-sidebar-flow.spec.ts`; `apps/web/package.json` version; this order's header | every other `crates/**` file (incl. the server turn handlers, `chats_read.rs`, `db/**`), the salon fixture PAIR on disk (plants are per-case copies), every other SPA file, `help/**` beyond the one page, the drift ledger |
| **P4.91** — the photos-predicate consolidation + the collapse heal's census log shape | `crates/quilltap-core/src/photos/photos_paths.rs`; `crates/quilltap-core/src/db/doc_mount_file_links.rs` — ONLY the deletion of the `is_photos_relative_path` twin + its private `posix_dirname` (`:332-388`); the SIX importers, import-line edits ONLY: `api/chat_media.rs:1716`, `photos/photo_link_summary.rs:13`, `photos/character_gallery_service.rs:17`, `services/maintenance.rs:32` + `:375` (and its `photos_relative_path_predicate` test if consolidated); `crates/quilltap-core/src/db/avatar_rolls_collapse_heal.rs` — the import + the `:71-83` justification comment + `report_census` (`:535-552`) + its tests; `crates/quilltap-web/src/log_file.rs` — the `FieldVisitor::put` JSON-field convention ONLY (Tier 2) + its tests; `harness/oracle/cases/photos-relative-path.ts`; `crates/quilltap-harness/tests/photos_relative_path_equivalence.rs`; this order's header | every other `crates/**` file (P4.93's avatar job files, `services/route_trail.rs`), `apps/web/**`, `help/**`, every other harness family, the drift ledger |
| **P4.92** — the loops' `base_params` carry-over class | `crates/quilltap-core/src/services/orchestrator.rs` — ONLY the strip after the `:2894` rebuild (or on the three `base_params:` clones at `:3002`/`:3080`/`:3125` — say which) + the `:2150` comment; `crates/quilltap-core/src/model/streaming_provider.rs` — the P4.41 doc block at `:339` ONLY (one sentence); `crates/quilltap-core/src/services/native_tool_loop.rs` + `text_tool_loop.rs` — `#[cfg(test)]` modules ONLY (Case B's unit pin); `crates/quilltap-harness/tests/orchestrator_tier3_equivalence.rs`; `harness/oracle/cases/orchestrator-tier3.test.ts`; `harness/oracle/fixtures/orchestrator-tier3.json` + `build-orchestrator-fixture.ts` + the fixture pair that builder produces; this order's header | every other `crates/**` file (the request builders, `primary_stream.rs`, `provider_failover.rs`), `apps/web/**`, `help/**`, every other harness family (P4.92 RUNS `chat_create_capstone` / `primary_stream_tier3` — never edits them), the drift ledger |
| **P4.93** — the harness + logging maintenance smalls | `crates/quilltap-harness/tests/help_doc_sync_guards_equivalence.rs` + `help_doc_sync_equivalence.rs` (headers + the env var + the fixture-sensitive arm) + `harness/oracle/cases/help-sync-guards.ts` (+ `help-sync-tier2.ts` only if the arm needs an oracle column); `crates/quilltap-core/src/model/completion.rs` — the `CompletionRole` inverse (`from_v4_wire`/`FromStr`) + tests; every hand-rolled role-mapper family in `crates/quilltap-harness/tests/` named in its survey (Class 1 + Class 2 + `external_prompt_tier3_equivalence.rs` + `file_attachment_tier3_equivalence.rs`) EXCEPT `orchestrator_tier3_equivalence.rs`; `crates/quilltap-core/src/db/memories.rs` — the ONE test's capture rig ONLY; `crates/quilltap-core/src/services/character_avatar_job.rs` + `services/image_job_common.rs` — the `[CharacterAvatar]` log lines + their capture tests ONLY; `crates/quilltap-harness/tests/avatar_job_tier3_equivalence.rs` — ONLY if a line-count pin must widen; `harness/tools/recipe_sweep.py` — ONLY if the env rename needs a driver-side alias (say so); this order's header | `orchestrator_tier3_equivalence.rs` (P4.92's), `salon_reads_equivalence.rs` + the help EMBED guard (P4.D195's), `db/avatar_rolls_collapse_heal.rs` (P4.91's), `photos/**`, `apps/web/**`, `help/**`, `crates/quilltap-web/src/**`, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0),
   and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp`) where the family is deterministic across
   regens; where it is not (`regenerate-at-both-pins-and-cmp-is-not-
   universal`), run by name and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast` with the lane's env block
   (its own oracle vars + `QT_V4_ROOT`/`QT_V4_CHECKOUT` at the right pin +
   `QT_NODE` = the node BINARY): report **binaries / passed / failed /
   ignored** from the per-binary lines and the exit code, and the lane's
   families confirmed RUN by non-zero duration. No family is withheld
   this round (P4.89 restored the brahma pair).
9. **SPA lanes only (P4.D195):** `npm run lint` (incl. the qt-class guard),
   `npm test` (spec files / tests / 0 failed), `npm run build` clean, then
   the lane's e2e beats by FILE against the release binary, and ONE full
   Playwright run at lane close (numbers recorded; the documented
   intermittents — the P4.D161 pause-toast beats, the P4.d17 quill beat,
   the `salon-documents-flow` / `workspace-flow` terminal trio — are
   named, re-run by file, and never re-classified as this lane's).
10. **Ownership check:** `git diff --stat main...HEAD` — every path is in
    the lane's Owns column; every MUST-NOT-TOUCH path absent.
11. **Per commit:** `.claude/commands/commit.md` in full — the equivalence
    test is non-negotiable (§1), the invariants (§2), real-instance safety
    (§3), the harness regen (§4), build/test/lint (§5), the CHANGELOG
    entry + version bumps (§7). Commit on the lane branch only; never
    merge to `main`; never `git stash`; never a worktree inside a worktree.
12. **Lane close:** update this order's status header (what landed, what
    was refuted by measurement, what stays OPEN with its named cause),
    append the lane record to `status-log.md` (the regen commands as run,
    the counts, the mutation table, every finding outside the lane's
    ownership for the unifier), and stop. The final gate runs in the
    FOREGROUND of the lane's last turn — a backgrounded gate whose
    notification arrives after the lane reports done is a lane that is
    NOT done (`a-backgrounded-sleep-does-not-gate-your-turn`).
