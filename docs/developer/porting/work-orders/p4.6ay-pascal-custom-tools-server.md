# Work order P4.6ay — the Pascal custom-pseudo-tools SERVER surface (+ the forced schema adoption)

> **STATUS: OPEN — PAUSED ON A UNIT BOUNDARY (2026-07-17). Unit 10 is
> UNIFIED ON MAIN; units 1–9 are unstarted. RESUME AT UNIT 1.**
>
> ### The v4 fix addendum (2026-07-17 — the baseline MOVES to `e3593f75`)
>
> The D24 blocker is RESOLVED: **v4 `e3593f75` ("fix: tool validators
> return the parse, so the leniency actually lands", 4.8.0-dev.62)** is
> on v4 main — one commit past `a33ac8b8`, classified at the 2026-07-17
> drift check as a behavior change scoped entirely to the tool-input
> family (lane A's re-port scope). **What it means for THIS lane:**
>
> - **Every v4 tool validator now returns the parsed data
>   (`XInput | null`), not a boolean** — including
>   `validateRunCustomInput` — and `run-custom-handler.ts` destructures
>   the PARSE (`const parsed = validateRunCustomInput(input)` →
>   `const { tool: toolName, parameters, private: isPrivate } = parsed;`).
>   Port unit 4's validator/handler to THAT shape, not the `a33ac8b8`
>   boolean-guard shape the survey below describes. Line references into
>   `run-custom-tool.ts` / `run-custom-handler.ts` may be off by a couple
>   of lines (each validator gained one line).
> - **Nothing else in this lane's scope changed.** The fix touches no
>   Pascal file (`lib/pascal/*` untouched), no schema construction (the
>   published tool-definition bytes — §2's targets — are exactly what
>   they were at `a33ac8b8`), no routes, no repos, no DDL. Unit 10's
>   landed schema adoption is unaffected.
> - **Pascal's own `coerceParam` is still NOT `llmNumber`** — that
>   separation survives the fix unchanged; the fix is about validators
>   returning parses, not about Pascal's parameter coercion.
> - **Every oracle this lane generates now regenerates at `e3593f75`**,
>   not `a33ac8b8`. Where the body of this order says "at `a33ac8b8`",
>   read `e3593f75` — the individual regen/gate lines below have been
>   updated.
>
> **Unification addendum (2026-07-17) — read with the pause note below:**
>
> - Unit 10 is **on main** and green under the full gate (329 suites /
>   1376 tests / 0 failed; Playwright 65/65 zero skips proves the schema
>   adoption + the 33 migrated web fixtures did not break the app).
> - **Unit 10's breakage list was NINE items and the true count was TEN.**
>   `help_settings` (`tools/help.rs`) is a **second, independent reader**
>   of the chat-settings bag and needed `customTools` too; v4 added it at
>   `help-settings-handler.ts:112` in `61ec90bd`. Lane B predicted the
>   red differential and named it as this lane's; the unification wire
>   closed it. **Whoever resumes should assume the same class of miss
>   exists for `pascalMeta`** — enumerate every reader of a bag before
>   calling a column adopted, not just the schema/repo/route spine.
> - **The `run_custom` definition bytes (§2(b)) are NOT on main.** Per the
>   Shared contract's unification note, lane C slipping means §2(b) is
>   withheld so v5 never publishes a tool to the model that answers
>   `Unknown tool: run_custom`. Lane A's §2(a) is withheld with it (unit 3
>   is blocked — see `p4.d5`). **`data.rs` has neither `run_custom` nor
>   the rng `modifier`; that is deliberate and consistent.**
> - **§1 (`pascal::dice`) IS on main** and consumable: `parse_dice_notation`,
>   `roll_notation`, `format_dice_breakdown`, and the `RandomBytes` seam
>   are all live. **The unit-3 sequencing caveat in the preamble is now
>   moot** — no cherry-pick is needed; the dice module is simply there.
>
> Per this header's own pause rule, exactly what landed and what did not:
>
> - **Unit 10 (the schema adoption) — LANDED** (commit `bde4575`, branch
>   `claude/pascal-custom-tools-porting-23d520`). All nine enumerated breakages
>   closed; `fresh_schema.json` re-dumped from v4's live `generateDDL` at
>   `a33ac8b8` (the diff is EXACTLY the two columns); every affected oracle
>   regenerated and green, incl. `provisioning_equivalence`,
>   `builtin_mounts`/`builtin_templates` (atomic with the re-dump), the
>   v4-reads-v5 cross-compat check, and the chat-settings + chats-messages
>   write/read/ops differentials. `cargo test --workspace --no-fail-fast`:
>   1358 passed / 0 failed.
> - **Units 1, 2, 3, 4, 5, 6, 7, 8, 9 — NOT STARTED.** No partial code for any
>   of them exists on the branch.
>
> **Two findings for whoever resumes (full detail: the status-log record
> "P4.6ay unit 10"):**
>
> 1. **⚠ Accepted consequence #2 is SHARPER than this order states.** v5 opening
>    a v4 instance older than 4.8.0 does not merely "lack" the columns — it
>    **cannot read or write messages at all** (`no such column: pascalMeta` from
>    both `insert_message` and the read SELECT). `tolerant_select_list` has only
>    ONE caller (`chat_settings`), so only that half degrades gracefully (absent
>    → v4's Zod `.default(true)`, ported + tested). **The dogfood Friday copy
>    must be migrated to v4 4.8.0's two ALTERs before v5 opens it.** Extending
>    tolerance to the message paths needs a human ruling + its own differential;
>    it was left out of scope.
> 2. **The committed web fixtures needed bringing forward**, which this order did
>    not anticipate (its Fixtures section names only the four tier-2/provision
>    artifacts). 33 `crates/quilltap-web/tests/fixtures/*.db` predate both
>    columns and broke 4 tests. Resolved with v4's OWN migration SQL via a new
>    committed script, `harness/oracle/fixtures/migrate-fixtures-pascal-columns.ts`
>    (idempotent) — NOT by re-running the 14 builders, which would re-bake the
>    substrate through 8 commits of unrelated v4 drift, cannot reach the 3
>    builder-less fixtures (`chat-send-{main,mount}.db`, `salon-llm-logs.db`),
>    and would yield the generateDDL shape where real instances carry the
>    migration shape.
>
> **Resume at unit 1**, per the Tier-1 order below (10 → 1 → 3 → 2 → 5 → 6 → 4
> → 8 → 9 → 7). Re-run the drift check first **against `e3593f75`**
> (`git log e3593f75..HEAD --oneline`; if HEAD moved, STOP and report).
> The unit-3 dice dependency is already satisfied: §1's `pascal::dice` is
> on main.
>
> **Sizing note for the next lane (survey-verified, not a guess):** unit 1 is
> not merely "606 lines of types". Its differential must reproduce **Zod's own
> rejection message strings**, because `formatDefinitionIssues` is the chokepoint
> the corpus compares. v4's own test
> (`__tests__/unit/lib/pascal/custom-tool-definition.test.ts`) only
> `toMatch(/regex/)`s those messages, so it is a weaker bar than a v5-vs-v4
> string diff — decide deliberately whether the differential pins full strings
> (byte-exact Zod built-ins: `Invalid input: expected number, received string`,
> `Unrecognized key: "gt3"`, the union `— or — ` join) or only the
> author-written `ctx.addIssue` messages + the accept/reject verdict.
>
> ---
>
> **Original status header follows.**
>
> **STATUS: OPEN.**
>
> Lane C of the THREE-lane P4.d5 ∥ P4.d6 ∥ P4.6ay round (the
> Pascal-custom-tools round, planned 2026-07-16 — triggered by a v4 drift
> check). **v4 reference baseline for the RESUMED lane: `e3593f75`**
> (see the v4 fix addendum above). At lane start, run in
> `~/source/quilltap-server`:
>
> ```bash
> git log e3593f75..HEAD --oneline
> ```
>
> **If HEAD moved, STOP and report.** Every oracle in this lane generates
> at `e3593f75` — not at `02865bdb`, not at `a33ac8b8`, not at whatever
> HEAD happens to be.
>
> Sibling lanes: `p4.d5-dice-rng-lenient-numbers.md` (lane A — **it
> delivers your dice module (§1) and your `run_custom` definition bytes
> (§2)**; you are the consumer of both) and `p4.d6-help-doc-sync-drift.md`
> (lane B — fully disjoint from you). The **Shared contract** and
> **Ownership** sections below are binding and reproduced verbatim in all
> three orders. You run in a worktree; a unification pass integrates the
> lanes onto main.
>
> **Sequencing (binding).** Lane A commits `crates/quilltap-core/src/pascal/dice.rs`
> as its **unit 1**. Your Form-B (dice) roll path in unit 3 depends on it.
> **Sequence your dice-form work LATE.** If you reach it before A's commit
> is available in the shared history, cherry-pick A's dice commit onto your
> branch (recipe: the `parallel-round-reconciliation` memory note —
> temp-branch cherry-pick, one full gate). **Do NOT write your own dice
> module and do NOT define your own randomness seam** — §1 forbids both.
>
> **This lane IS a differential port.** Every unit below arrives with an
> equivalence test against v4's real code at `a33ac8b8`. Never accept a
> unit without one. Re-survey v4 fresh before each unit; the facts under
> "Survey-verified starting points" are from a **2026-07-16 survey at
> `a33ac8b8`** and are given so the lane never re-derives them — if a
> unit's fresh survey contradicts this order, **STOP and report** before
> proceeding.
>
> **This lane is the long pole of the round.** It is honestly ~10 units.
> It cannot be split: the contract between any two halves would *be* the
> whole 606-line type surface of unit 1, which is worse than the lane
> being large. If it must be paused, **pause on a unit boundary** and
> update this status header with exactly which units landed and which did
> not.

## Mandate

Port the whole **Pascal custom-pseudo-tools SERVER surface** (v4
`61ec90bd` + `31d9be49` + `a33ac8b8`) — the definition format, the
roster discovery, the execution core, the `run_custom` LLM tool + its
handler, the Pascal writer, Prospero's `custom-tool-error`, the
`/api/v1/chats/{id}/custom-tools` route, registration/catalogue/
capabilities, the orchestration + streaming wiring — **including the
schema adoption it forces**.

v4 spec: `docs/developer/features/pascal-custom-tools.md` (412 lines).
User help doc: `help/custom-tools.md` (252 lines). Read both before unit 1.

**Explicitly NOT this lane:** the tools/dice drift (lane A), the
help-docs family (lane B), and **the SPA — scope is SERVER-ONLY. Do not
touch `apps/web/`.** The SPA is the next round.

### Human decisions already taken — do NOT re-litigate

1. **The schema question is RULED: adopt both columns.** v4 added two
   migrations (`pascalMeta` on `chat_messages`, `customTools` on
   `chat_settings`). v5 **re-dumps `fresh_schema.json` from v4 HEAD's live
   `generateDDL`** and adds `customTools` to the seed. **Do NOT port the
   migration runner** — it stays a named Tier-3 deferral.

   **Accepted consequences (recorded here so nobody re-opens them):**
   - A v5-provisioned DB gains two columns a pre-4.8.0 v4 does not know
     about. **Harmless** — v4's repos are column-name-addressed, proven by
     every tier-2 differential we have.
   - A v4 instance older than 4.8.0, opened by v5, **lacks** them (v4's
     migration would have supplied them). This is the price of not porting
     the migration runner.

   **This is an explicit, human-approved exception to CLAUDE.md's "the
   schema does not change during the port" rule, forced by v4 moving.**
   It is an exception, not a precedent: it does not license any other
   schema change.

2. **Scope is SERVER-ONLY. The SPA is next round.** `apps/web/` is out of
   bounds for this lane, full stop.

3. **Forward note for the follow-on SPA round — record, do not act:**
   Pascal gets a **bespoke composer button**, not a ported gutter
   substrate. v5's `chat-composer.ts` has only the paperclip;
   `ComposerGutterTools` / `RngDropdown` / the dropdown shell /
   `useClickOutside` are all unported. **The full gutter stays a named
   deferral.**

---

## Survey-verified starting points (2026-07-16 @ v4 `a33ac8b8` — do not re-derive)

### Unit 1 — the definition format (`lib/pascal/custom-tool.types.ts`, NEW, 606 lines)

**Constants:** `TOOLS_FOLDER='Tools'` (`:22`), `TOOL_FILE_SUFFIX='.tool.json'`
(`:25`), `MAX_PARAMETERS=8`, `MAX_OUTCOMES=32`, `MAX_ROSTER_SIZE=64`,
`MAX_MESSAGE_LENGTH=1000`, `MAX_DESCRIPTION_LENGTH=500`,
`MAX_TITLE_LENGTH=80`, `IDENTIFIER_PATTERN=/^[a-z][a-z0-9_-]{0,63}$/` (`:49`).

**Schemas:** `ParamRefSchema` (strict, `$param`), `NumberOrParamRefSchema`,
`ParameterTypeSchema` (number|integer|string|boolean),
`CustomToolParameterSchema` + `superRefine` (`:99-129` — min/max only on
numerics; min ≤ max; default must match the declared type),
`RollRangeSchema` (strict, `:138`), `RollDiceSchema` (`:155`), `RollSchema`
union, `NumericComparatorSchema` / `ParamComparatorSchema` (strict, `:206`
/ `:215`), `WhenObjectSchema` (strict; bare comparators = value; `roll` =
the raw draw; `params` record; `:241-254`), `WhenSchema = true | object`,
`OutcomeStateSchema` (success|partial|failure|info), `CustomToolOutcomeSchema`
(strict), `VisibilitySchema`, `QtapCustomToolSchema` (**top-level
NON-strict on purpose — the room reserved for v2 keys**, `:301`).

**Load-time rules:** `validateOutcomeOrdering` (`:371` — **a trailing
catch-all is MANDATORY; an earlier catch-all is REJECTED as unreachable**
— this pair of rules is what makes a coverage gap structurally
impossible), `validateReferences` / `validateRollRefs` /
`validateComparator` / `resolveOperandType` (`:414-550` — an undeclared
`$param`, ordering a string, and cross-type `eq` are all rejected **at
load**).

`displayTitle()` (`:352` — derives from `name`: underscores/hyphens →
spaces, each word capitalized). `formatDefinitionIssues()` +
`flattenIssues()` (`:563-582` — **`when` and `roll` are unions, so Zod
reports "Invalid input" at the union and buries the real complaint inside
its branches; this surfaces every branch joined by `" — or — "`. A
rejection nobody can read is barely better than no rejection.** Carry the
why-comment.) `collectUnknownKeys()` (`:603`) with `KNOWN_TOP_LEVEL_KEYS`
(`:585`).

**`a33ac8b8` made the nested objects STRICT** (`when`, the comparators, the
outcome entries, the roll range, `$param` refs). Zod had been silently
stripping unknown keys, so a comparator typo'd as `gt3` loaded fine and
became **a test that never fires — the dead-branch failure this format
exists to prevent.** The published JSON Schema had always claimed
`additionalProperties: false` there; only the loader disagreed.
**Top-level tolerance is untouched.**

v4 has a corpus test `__tests__/unit/lib/pascal/custom-tool-definition.test.ts`
(246 lines) cross-checking Zod against the published JSON Schema — a
natural oracle template.

### Unit 2 — discovery / roster (`lib/pascal/custom-tools.ts:1-368` of 772, NEW)

Types `DiscoveredCustomTool`, `CustomToolLoadError`,
`CustomToolRoster{tools,errors,droppedForCap}`, `RosterContext`.

`TIER_ORDER = ['character','participant','group','project','global']` (`:62`).
`isRootToolFile` (`:119` — **prefix-listing means nested files must be
rejected**), `listToolFilesFromDatabase` (`:127`), `listToolFilesFromDisk`
(`:140` — ENOENT/ENOTDIR → `[]`), `readToolFile` (`:155`),
`loadToolsFromMount` (`:171` — **never throws**; a same-mount duplicate
`name` becomes an error entry, `:251`), `orderedMounts` (`:271` —
**same-tier ties sorted lexicographically by mount id — deterministic**),
`resolveCustomToolRoster` (`:298` — **nearest tier wins; a `disabled`
tombstone suppresses at that tier AND below**; `MAX_ROSTER_SIZE` drop list).

Depends on `resolveTieredMountPool(..., { includeParticipants: true })`,
`listDatabaseFiles`, `readDatabaseDocument`, `docMountPoints.findById`.

**No caching, EVER — the roster is re-resolved per LLM call and per popup
open.** Carry that why-comment.

**The v5 infrastructure this needs is ALREADY PORTED — this unit is
composition, not new plumbing:**
`crates/quilltap-core/src/db/tiered_mount_pool.rs:256` `resolve_tiered_mount_pool`,
`:368` `flatten_tier_pool`, `:415` `classify_mount_tier`, `MountTier`;
`crates/quilltap-core/src/db/database_store.rs:133` `read_database_document`,
`:351` `list_database_files`.

### Unit 3 — execution core (`lib/pascal/custom-tools.ts:370-772`)

`CustomToolRunError`, `ResolvedParams`, `CustomToolRunResult`.

- `resolveParams` (`:410` — **an unknown key is a hard reject**).
- `coerceParam` (`:433` — numeric strings accepted; `integer` →
  `Math.round`, **THEN** clamp; boolean accepts `'true'` / `'false'`).
  **NOTE: this is Pascal's OWN coercion, NOT lane A's `llmNumber`. Do not
  conflate them and do not share an implementation.**
- `clamp` (`:465`), `resolveRollField` (`:473`).
- `cryptoUniform` (`:505` — **6 random bytes / 2^48**). Draws through the
  §1 `RandomBytes` seam.
- `rollRange` (`:514` — **transform order is multiply → offset → round, in
  that order** (`:534-536`); min > max is an error; **min === max
  short-circuits WITHOUT drawing** — byte-stream relevant, so your
  `FixedBytes::consumed()` assertions must pin it).
- `OutcomeSubjects{value,roll,params}` (`:546`), `resolveOperand` (`:563`),
  `requireNumber` (`:581`).
- `matchesComparator` (`:594` — **keys are AND-composed**; `eq`/`neq` use
  strict `!==`/`===`), `matchesWhen` (`:636`).
- `formatValue` (`:662` — **integers plain, else `toPrecision(4)` folded
  through `Number()`**).
- `renderTemplate` (`:673` — `/\{\{([^}]+)\}\}/g`; `{{value}}` / `{{roll}}`
  / `{{dice}}` / `{{params.*}}`; **unknown placeholders are left VERBATIM**).
- `executeCustomTool` (`:703` — **the dice branch: `raw === value ===
  total`; the Form-A transform NEVER applies**; visibility override
  resolution `:751`).

**`a33ac8b8` added the `when` subjects:** `roll` tests the **RAW
pre-transform draw**; `params` tests the call args. All ANDed. Comparator
operands may be a `{"$param":"difficulty"}` reference rather than a
literal — **the opposed check, reusing the ONE indirection the format
already had instead of inventing a second.** `eq`/`neq` on a param accept
strings and booleans. **Bare comparators still mean the value — that is
what keeps every older definition meaning what it meant.** Still no OR, no
nesting, no expression grammar: **the evaluator stays eval-free.**

Rationale for `roll` earning its place (carry it as a why-comment): a
transform moves the value away from what was drawn — a raw draw in the
bottom 2% is a fumble whatever it was later scaled by, and no test on the
value could say so.

### Unit 4 — the LLM-facing tool

**`lib/tools/run-custom-tool.ts` (NEW, 208 lines):**
`runCustomToolInputSchema` (`tool: string`, `parameters: record|null|optional`,
`private: bool|optional`), `RunCustomToolOutput`, `RUN_CUSTOM_PREAMBLE`
(`:74`), `describeRoll` (`:83`), `COMPARATOR_SYMBOLS` (`:100`),
`describeOperand` / `describeComparator` / `describeWhen` (`:110-140`),
**`buildRunCustomDescription(roster)` (`:153` — the roster rides IN the
description, rebuilt every build; `revealOdds:false` contributes
name/description/params only, `:173`)**, `runCustomToolDefinition` (`:194` —
**the EMPTY-ROSTER description; this is exactly what §2 pins**),
`validateRunCustomInput`.

**`lib/tools/handlers/run-custom-handler.ts` (NEW, 244 lines):**
`RunCustomToolContext` (`:38` — incl. `onPosted`), `failure` (`:61`),
`reportFailure` (`:71` — **Prospero, NOT Pascal**), `executeRunCustomTool`
(`:99`), `formatRunCustomResults` (`:238`). Whisper target =
`[callerParticipantId]` (`:164`). Posted-but-null → `reportFailure` (`:201`).

### Unit 5 — the Pascal writer (`lib/services/pascal/writer.ts`, NEW, 160 lines)

`PascalMeta = NonNullable<MessageEvent['pascalMeta']>`.
`buildPascalResultContent({toolTitle,message})` → body
`` `🎲 **${toolTitle}** — ${message.trim()}` `` (`:72`).
**`opaqueContent === content` (`:74`) — deliberate: with Pascal's framing
gone there is no persona to strip, which is exactly where Suparṇā and
Carina already sit.** Carry the why-comment.
`postPascalResult` (`:97`) → `role:'ASSISTANT'`, `participantId: null`,
`systemSender:'pascal'`, `systemKind:'custom-tool-result'`,
`targetParticipantIds` normalized (**empty array → null**, `:125`). Errors
are logged, never thrown; returns `MessageEvent | null`.

**v5's closest analogue / the right template:
`crates/quilltap-core/src/services/carina_runner/writer.rs`** — the same
shape (build content → post a synthetic ASSISTANT message → return
`Option`), and Carina is likewise an `opaqueContent === content` case.

**`a33ac8b8` CUT BACK what a roll says. The order records these as
DELIBERATE REMOVALS, not omissions — do not "restore" them:**

- **Pascal's narration is GONE.** A manual run's announcement is now
  byte-identical to a character's roll, so nothing in the transcript
  records that the operator reached for the tool — `pascalMeta.invokedBy`
  still does.
- **The italic `*(rolled 14)*` suffix is GONE.** What a roll says is the
  author's to decide, via `{{value}}` / `{{dice}}`.
- **A manual run no longer posts a companion USER note.** Its only unique
  contribution was publishing the parameters the operator chose — the
  human's hand on the scale, from which a character could infer the roll
  was arranged.
- `?action=run` returns `messages: [pascalMessage]`.

### Unit 6 — Prospero `custom-tool-error` (`lib/services/prospero-notifications/writer.ts:779-824`, +46)

`buildCustomToolErrorContent` → `` Prospero regrets that the custom tool `X`
could not be run — <reason>. ``
`buildCustomToolErrorOpaqueContent` → `System: The custom tool "X" could not
be run — <reason>.`
`postProsperoCustomToolError` — reason normalized
`.trim().replace(/[.\s]+$/,'')` with a `'the table would not deal'`
fallback; `systemKind:'custom-tool-error'`; whisper target
`[callerParticipantId]`.

**Failures are authored by Prospero, never Pascal.**

### Unit 7 — the route (`app/api/v1/chats/[id]/custom-tools/route.ts`, NEW, 424 lines)

| Method | Path | Query | Body | Response |
|---|---|---|---|---|
| GET | `/api/v1/chats/{id}/custom-tools` | — | — | `{tools: CustomToolListing[], errors: CustomToolLoadError[], droppedForCap?: string[]}` |
| POST | `/api/v1/chats/{id}/custom-tools` | `?action=run` | `{tool, parameters?, private?, asCharacterId?}` | `{messages: [pascalMessage] \| [], result: {tool,value,state,message,whispered}}` |

`CustomToolListing` (`:57`): `{name,title,description,parameters,
defaultVisibility,sourceTier,characterLabel?,asCharacterId,definitionPath,
mountName}` — **the roll spec and the outcome table are NEVER in the
payload (`:29-33`).**

**GET (`handleList`, `:152`):** 404 on a missing chat; no readable CHARACTER
participants → `{tools:[],errors:[]}` (`:168`); **one roster per character
perspective, merged**: an identical `(mountPointId::definitionPath)` → one
unlabelled row (`:199`); differing → one row per variant tagged with
`characterName` (`:210`); errors unioned by `(mount::path)` (`:189`);
sorted by `title` then `characterLabel` (`:228`).

**POST (`:280`):** `runSchema` at `:271`; 404 on a missing chat; 400 on no
character participants (`:300`); 400 on an unknown `asCharacterId` (`:307`);
400 on an unknown tool **with the available list** (`:327`); on a
`CustomToolRunError` → **post the Prospero error AND return 400**
(`:355-362`); **a private run targets `ctx.user.id` (`:340`) — a userId,
NOT a participant id, mirroring `actions/run-tool.ts`**; `invokedBy:'user'`,
**no `callerParticipantId`** in `pascalMeta` (`:385-399`).

Handlers via `createContextParamsHandler` + `withActionDispatch`
(`:422-424`).

### Unit 8 — registration / catalogue / capabilities

- `lib/tools/index.ts:198-212` — re-exports.
- `lib/tools/plugin-tool-builder.ts:208-228` (`customTools?: DiscoveredCustomTool[]`
  option), `:409-419` (**push `run_custom` ONLY when the roster is
  non-empty**; the description is swapped per call), `:337` (a log field).
- `lib/chat/tool-executor.ts:189-199` (`characterMountPointId`,
  `characterIds` on `ToolExecutionContext`), `:225-231` (`emitPascalResult`),
  `:263` (`'run_custom'` added to `BUILT_IN_TOOLS`), `:611-643` (dispatch;
  **the TOOL row persists but renders nothing — `delegatedDisplay`**).
- `app/api/v1/tools/route.ts:32,82,191-197` — the catalogue entry
  `{id:'run_custom', name:'Custom Tools', category:'utility'}`.
  **`31d9be49` fixed this:** `run_custom` was missing from the
  hand-maintained catalogue behind the per-chat tool toggles, so **the tool
  never appeared in any list however valid the definition.** That catalogue
  has **NO drift guard** — it lists 40 of 58 registered definitions, the
  remainder deliberately non-toggleable (agent/console tools such as
  `run_sql`, `terminal_*`, `memory_search`) — which is why it was missed. A
  guard needs an explicit allowlist of the intentional exclusions and is
  **left as a v4-side follow-up, not this lane's work.**
- `lib/tools/capabilities-report.ts:192,979,1601` — `customTools: boolean`
  on `FeatureConfigInfo` + a markdown line under "Pascal the Croupier (RNG)".

**⚠ `delegatedDisplay` is a PERSISTED ROW-SHAPE CONTRACT in v5, not a
rendering detail.** v4's `tool-execution.service.ts` added
`DELEGATED_DISPLAY_TOOLS = new Set(['run_custom'])` and stamps
`delegatedDisplay: true` into the TOOL message's **stored JSON content**.
`grep -rn "delegated_display\|delegatedDisplay"` over v5 returns
**NOTHING**; `crates/quilltap-core/src/services/tool_execution.rs` has
`VAULT_READ_TOOLS` (`:73`) and `save_tool_messages` (`:664`) but no
delegated-display set. **Land it with the bubble or ship double-rendering.**
It is Tier 1.

### Unit 9 — orchestration / streaming wiring

- `lib/services/chat-message/streaming.service.ts:177-191`
  (`customToolContext?: RosterContext|null` param on `buildTools`),
  `:240-264` (**the roster resolves INSIDE `buildTools`, at the last
  possible moment — every early return skips it; a throw is caught and
  `run_custom` withheld**), `:286`, `:782-800` (`encodePascalResultEvent`
  → SSE `{pascalResult: message}`).
- `lib/services/chat-message/orchestrator.service.ts:57,93,873-898` —
  builds `customToolContext` from the **responding character's**
  perspective; **`characterId` is REQUIRED (the group tier silently
  resolves to `[]` without it)**; `customToolsEnabled = chatSettings?.customTools ?? true`;
  **withholding the context is HOW the setting turns the feature off.**
- `lib/services/chat-message/tool-execution.service.ts:+12`;
  `app/api/v1/settings/chat/route.ts:+9`;
  `app/api/v1/chats/[id]/handlers/get.ts:432` (`pascalMeta: event.pascalMeta || null`).

**v5 map:** `buildTools` → `crates/quilltap-core/src/services/tool_build.rs:427`;
the SSE encoders → `crates/quilltap-core/src/services/chat_events.rs`
(`carina_answer` `:453`, `host_announcement` `:461` — **`pascal_result`
slots beside them**).

### Unit 10 — THE SCHEMA ADOPTION (human-ruled: ADOPT — see "Human decisions")

```sql
-- migrations/scripts/add-pascal-message-meta-v1.ts:52
ALTER TABLE "chat_messages" ADD COLUMN "pascalMeta" TEXT DEFAULT NULL
-- id 'add-pascal-message-meta-v1', introducedInVersion '4.8.0',
--   dependsOn ['sqlite-initial-schema-v1']

-- migrations/scripts/add-custom-tools-field.ts:71
ALTER TABLE "chat_settings" ADD COLUMN "customTools" INTEGER DEFAULT 1
-- id 'add-custom-tools-field-v1', introducedInVersion '4.8.0',
--   dependsOn ['sqlite-initial-schema-v1']
```

Both are SQLite-only (`isSQLiteBackend()` guard), registered in
`migrations/scripts/index.ts:+12`.

**Exactly what breaks in v5 — the complete list:**

1. **`crates/quilltap-core/src/services/provisioning/fresh_schema.json` —
   the hard break.** The committed byte-exact DDL a fresh v5 instance
   replays (`include_str!` at `provisioning/mod.rs:70`, executed at `:126`).
   **Line 8** = the `chat_messages` CREATE TABLE — has `carinaMeta`, **no
   `pascalMeta`**. **Line 9** = `chat_settings` — has `autoDetectRng INTEGER
   DEFAULT 1`, **no `customTools`**. The provisioning differential
   (`crates/quilltap-harness/tests/provisioning_equivalence.rs:8-11`)
   asserts v5's `sqlite_master` per partition equals **v4's LIVE
   `generateDDL` schema** — and `harness/oracle/provision/dump-fresh-schema.ts`
   drives v4's REAL repositories to materialize the DDL from the Zod row
   schemas. **Both Zod schemas grew a field, so the moment the oracle
   regenerates at `a33ac8b8` the differential fails on both tables. This is
   exactly the tripwire that test was built to trip. It is v4 drift, not a
   v5 bug.**

2. `crates/quilltap-core/src/services/provisioning/chat_settings_seed.json`
   — the `columns` array (30 entries, `autoDetectRng` at index 17) **AND**
   the `values` map both need `customTools: 1`. `provisioning/mod.rs:241`
   errors `chat_settings_seed.json missing value for {col}` on any mismatch,
   **so the column list and the value map must move together.**

3. `crates/quilltap-core/src/db/chat_settings.rs` — **five** places carry
   the column list: `:61` (doc comment), `:478` (the SELECT list), `:590`
   (the UPDATE assignment builder, `autoDetectRng = ?N`), `:741` and `:844`
   (column-name arrays), `:1184` / `:1194` (the test-fixture DDL + INSERT).
   Mirror `auto_detect_rng` for `custom_tools` at every one.

4. `crates/quilltap-core/src/db/chats_messages.rs` — `system_sender:
   Option<String>` (`:144`) is a bare String, **so `'pascal'` needs no type
   change**; but add `pascal_meta: Option<PascalMetaIn>` beside `carina_meta`
   (`:154`), a `let pascal_meta = opt_json(&m.pascal_meta)?;` beside `:562`,
   and `pascalMeta` appended to the INSERT column list (`:577`) and the bind
   list (`:607-612`).

5. `crates/quilltap-core/src/db/chats_messages_read.rs` — **the SELECT at
   `:79` is POSITIONAL**: `put_opt_json(&mut o, "carinaMeta", row.get(25)?)`
   at `:187`. **Adding `pascalMeta` shifts every index after 25.** Also
   `:34` (doc comment) and `:334` (the test DDL).

6. `crates/quilltap-core/src/api/salon.rs:480` — needs
   `m.insert("pascalMeta".into(), or_null("pascalMeta"));` beside
   `carinaMeta` (v4: `handlers/get.ts:432`).

7. `crates/quilltap-core/src/api/settings.rs:634-637` — the `autoDetectRng`
   `bool_field` block needs a `customTools` sibling.

8. **Committed fixtures carrying column sets** (`harness/oracle/fixtures/`):
   - `chat-settings-tier2.json` + `build-chat-settings-fixture.ts` →
     regenerate with `customTools`;
   - `chats-messages-ops-tier2.json` + `build-chats-messages-ops-fixture.ts`
     → regenerate with `pascalMeta`;
   - `harness/oracle/provision/dump-fresh-schema.ts` → re-run; **the output
     REPLACES `fresh_schema.json`**;
   - `harness/oracle/provision/build-provision-oracle.ts` → re-run.

   **`crates/quilltap-harness/tests/builtin_mounts_equivalence.rs:43-59` and
   `builtin_templates_equivalence.rs` both read `fresh_schema.json` DIRECTLY
   as their DDL source and pin the path — the regeneration must be ATOMIC
   with them.** They go green or red together; do not land a half-regenerated
   tree.

9. **v5 has NO export-schema counterpart.** `qtap-export.schema.json` does
   not exist in v5 (`find` returns nothing outside `node_modules`). v4
   changed it twice (`61ec90bd`: the `systemSender` enum + the whole
   `pascalMeta` object at `public/schemas/qtap-export.schema.json:723-745`;
   `a33ac8b8`: `toolTitle` at `:730`). **A pure gap, not a break — but
   unported drift that will only grow. Name it as a Tier-3 deferral.**

### `a33ac8b8`'s `title` + `toolTitle`

`title` — an optional display name (max 80). **Pascal announces it, the
composer popup lists it, the roster sorts on it. The MODEL NEVER SEES IT**
— it still calls tools by `name`, so there is no second string for it to
pass by mistake, and `pascalMeta.tool` still records the identity. Omitted
→ derived from `name` (`displayTitle()`).

`pascalMeta.toolTitle` — new, optional, for the Salon header chip
("PASCAL · SCAN HAWKING RADIATION"). **Absent on older rows, which fall
back to `tool`; UI only — `systemKind` never reaches a model's context.**
Propagated to the four places carrying the shape: the Zod schema, the
client Message type, `qtap-export.schema.json`, and `DDL.md`.

### The whisper rule — NOT this lane's work, but the order records it

"Any message with a `systemSender` now always renders for the human user
regardless of the show-all-whispers toggle" is implemented **ENTIRELY
CLIENT-SIDE** at `app/salon/[id]/SalonView.tsx:308-324` (the
`visibleMessages` memo — `if (msg.systemSender) return true` at `:316`),
plus a de-dimming guard at `VirtualizedMessageList.tsx:352`.
`showAllWhispers` is `useState` — **not a persisted setting; it never
reaches the server.**

The server-side context-shaping filter
`lib/chat/context/message-attribution.ts:168 filterWhisperMessages` (called
from `context-manager.ts:1654`) was **NOT touched by any of the three
commits, and MUST NOT be** — hiding a private roll from the CHARACTERS is
the whole point; the toggle only ever governed the human's view. **So v5's
ported context shaping is CORRECT and unaffected.**

**This lane's ONLY obligation here:** `pascalMeta` and
`systemSender:'pascal'` must survive the read path (`api/salon.rs:480`) so
the SPA has the field to branch on next round.

### Not this lane's work (named as out-of-scope — do not port)

`lib/startup/prettify.ts` gained two migration pretty-labels. **v5 has no
prettify subsystem at all** (`grep -rln "PRETTY_LABELS"` returns nothing).
The whole subsystem is unported; this is not a Pascal problem.

---

## Deliverables (tiered)

### Tier 1 — must land (10 units)

Land them **in this order**. Unit 10 is FIRST: everything else binds to
those two columns.

**1. Unit 10 — the schema adoption.** All nine breakages above, atomically
with the two `builtin_*` tests that read `fresh_schema.json` directly. Do
**not** port the migration runner (Tier 3).

**2. Unit 1 — the definition format.** `crates/quilltap-core/src/pascal/`
(a new module beside lane A's `dice.rs` — you own everything under
`pascal/` EXCEPT `dice.rs`). The constants, the schema tree with v4's
strictness exactly as `a33ac8b8` left it (**nested strict; top-level
tolerant**), the load-time rules (ordering incl. the mandatory trailing
catch-all + the unreachable-earlier-catch-all rejection; reference /
roll-ref / comparator / operand-type validation), `display_title`,
`format_definition_issues` + `flatten_issues` (the `" — or — "` join),
`collect_unknown_keys` + `KNOWN_TOP_LEVEL_KEYS`.

**3. Unit 3 — the execution core.** `resolve_params` / `coerce_param`
(**Pascal's own — NOT lane A's `llmNumber`**) / `clamp` /
`resolve_roll_field` / `crypto_uniform` (6 bytes / 2^48, through the §1
seam) / `roll_range` (multiply → offset → round; min===max short-circuits
without drawing) / `OutcomeSubjects` / `resolve_operand` / `require_number`
/ `matches_comparator` / `matches_when` (incl. the `roll` and `params`
subjects and `$param` operands) / `format_value` / `render_template` /
`execute_custom_tool` (incl. the dice branch's `raw === value === total`
and the visibility override). **The dice-Form-B path sequences LATE — it
consumes §1's `parse_dice_notation` / `roll_notation` from lane A.**

**4. Unit 2 — discovery / roster.** Composition over the already-ported
`tiered_mount_pool` + `database_store` primitives. `TIER_ORDER`,
`is_root_tool_file` (reject nested), the DB + disk listers (ENOENT/ENOTDIR
→ `[]`), `read_tool_file`, `load_tools_from_mount` (**never returns Err**;
same-mount duplicate name → an error entry), `ordered_mounts` (lexicographic
mount-id tiebreak), `resolve_custom_tool_roster` (nearest tier wins;
`disabled` tombstone suppresses at that tier and below; `MAX_ROSTER_SIZE`
drop list). **Carry the no-caching why-comment.**

**5. Unit 5 — the Pascal writer.** `crates/quilltap-core/src/services/pascal_writer.rs`
(new), templated on `services/carina_runner/writer.rs`. The `🎲 **title** —
message` body, `opaque_content == content` (with the why-comment), the
synthetic ASSISTANT post, `targetParticipantIds` empty → null, errors
logged never thrown → `Option`. **`a33ac8b8`'s four removals are the
target state.**

**6. Unit 6 — Prospero `custom-tool-error`.** In
`services/prospero_notifications.rs`: the content + opaque-content builders,
the reason normalization + `'the table would not deal'` fallback,
`systemKind:'custom-tool-error'`, the `[callerParticipantId]` whisper.

**7. Unit 4 — the `run_custom` tool + handler.**
`crates/quilltap-core/src/tools/run_custom.rs` (new): the input schema, the
preamble, the describe helpers, `build_run_custom_description(roster)`
(roster rides IN the description, rebuilt per build; `revealOdds:false`
contributes name/description/params only), `validate_run_custom_input`; and
the handler (`RunCustomToolContext` incl. `on_posted`, `failure`,
`report_failure` → **Prospero**, `execute_run_custom_tool`,
`format_run_custom_results`, the `[callerParticipantId]` whisper,
posted-but-null → `report_failure`).
**§2 obligation (binding): `build_run_custom_description(&[])` MUST return
bytes IDENTICAL to lane A's pinned `run_custom` description in
`tools/definitions/data.rs`, and this lane ships a test asserting exactly
that against the `data.rs` entry.**

**8. Unit 8 — registration / catalogue / capabilities.** The
`plugin-tool-builder` equivalent (the `custom_tools` option; **push
`run_custom` ONLY when the roster is non-empty**; per-call description
swap), `tools/executor.rs` (`characterMountPointId` / `characterIds` on the
execution context, `emit_pascal_result`, `'run_custom'` into
`BUILT_IN_TOOLS`, the dispatch arm), **and `delegatedDisplay` — the
`DELEGATED_DISPLAY_TOOLS` set + the stamp into the TOOL row's stored JSON
content in `services/tool_execution.rs` (a PERSISTED ROW-SHAPE CONTRACT;
land it or ship double-rendering).**

**9. Unit 9 — orchestration / streaming wiring.** The `custom_tool_context`
param on `build_tools` (`services/tool_build.rs:427`) with **the roster
resolving INSIDE `build_tools` at the last possible moment** (every early
return skips it; a resolve failure is caught and `run_custom` withheld —
carry the why-comment); the `pascal_result` SSE encoder in
`services/chat_events.rs` beside `carina_answer` / `host_announcement`; the
orchestrator's `custom_tool_context` construction from the **responding
character's** perspective (**`characterId` is REQUIRED**) and the
`custom_tools ?? true` gate — **withholding the context is HOW the setting
turns the feature off**; `api/salon.rs:480`'s `pascalMeta` pass-through;
`api/settings.rs`'s `customTools` field.
**⚠ `services/orchestrator.rs` is a DECLARED SHARED FILE — see the
Ownership table. Touch ONLY the `custom_tool_context` construction + the
`build_tools` call site. Do not reformat, reorder, or re-wrap. Cite the
table in the commit message.**

**10. Unit 7 — the route.** `GET` + `POST ?action=run` at
`/api/v1/chats/{id}/custom-tools` per the table above: the listing DTO
(**never the roll spec or the outcome table**), the merged
per-character-perspective roster with the identical-vs-differing
`(mountPointId::definitionPath)` rule and the `characterName` labels, the
error union by `(mount::path)`, the `title`-then-`characterLabel` sort; and
the run path with all four 400 arms, the **post-Prospero-AND-return-400**
behavior, `ctx.user.id` as the private target (**a userId, NOT a
participant id**), `invokedBy:'user'` with **no `callerParticipantId`**.

### Tier 2 — should land

11. The **capabilities-report** line: `customTools: boolean` on
    `FeatureConfigInfo` + the markdown line under "Pascal the Croupier
    (RNG)" (`tools/capabilities_report.rs`).
12. The **`31d9be49` catalogue registration**: the `{id:'run_custom', name:
    'Custom Tools', category:'utility'}` entry in v5's counterpart of
    `app/api/v1/tools/route.ts`. **Without it the tool never appears behind
    the per-chat toggles however valid the definition** — that is the whole
    content of `31d9be49`. The catalogue drift-guard is a v4-side follow-up,
    NOT this lane.

### Tier 3 — DEFERRED, as LOUD typed refusals (never silent stubs)

**Per CLAUDE.md there are NO stubs or `TODO` code without prior agreement.**
Each of these is either an enumerated typed not-available refusal (the
`BuiltInToolRunner` precedent) or a documented absence — and every one is
named in the status log:

- **The migration runner** — human-ruled out of scope; the schema adoption
  replaces it (with the two accepted consequences recorded above).
- **`qtap-export.schema.json`** — v5 has no export-schema counterpart at
  all. A pure gap, not a break; it will only grow.
- **`lib/startup/prettify.ts`** — v5 has no prettify subsystem.
- **The SPA** — next round. Forward note (record, do not act): Pascal gets a
  **bespoke composer button**, not a ported gutter substrate; the full
  gutter (`ComposerGutterTools` / `RngDropdown` / the dropdown shell /
  `useClickOutside`) stays a named deferral.
- **The v4-side catalogue drift-guard** (needs an explicit allowlist of the
  18 intentional exclusions).
- Anything else the lane leaves — enumerate it here before declaring done.

---

## The differential requirement (restated per tier)

**Every unit ships with an equivalence test against v4's real code at
`e3593f75`. Fix the port, not the diff.**

- **Units 1 and 3 are PURE → tier-1 differentials with an INJECTED BYTE
  SOURCE.** Use §1's `FixedBytes` — never the OS source — and assert
  `FixedBytes::consumed()`, which is how `min === max` short-circuiting
  without drawing gets pinned. v4's own new tests are the natural oracle
  templates: `__tests__/unit/lib/pascal/custom-tool-definition.test.ts`
  (246 lines) and `custom-tools-execution.test.ts` (370 lines).
- **Unit 2 (roster) and unit 10 (schema) are tier-2 STRUCTURAL DB DIFFS.**
  v4's `__tests__/unit/lib/pascal/custom-tools-discovery.test.ts` (364
  lines) is the discovery oracle's template. Unit 10's proof is the
  existing `provisioning_equivalence` +
  `builtin_mounts_equivalence` / `builtin_templates_equivalence` going green
  against a FRESHLY regenerated `fresh_schema.json`.
- **Unit 7 is a ROUTE DIFFERENTIAL** (the established
  `salon_reads`/`salon_mutations` shape: a jest real-DB oracle driving v4's
  REAL handler with auth/`NextRequest` mocked, diffed against the Rust
  dispatch arm over a shared baked fixture).

**Point at the CLOSEST EXISTING recipes rather than inventing.** Read
these memory notes before building any oracle:

- `[[jest-real-db-oracle]]` — `resetModules` + `doMock` past `jest.setup`'s
  DB mocks; Node 24.
- `[[jest-oracle-mechanism]]` — v4-jest oracles via extra `--roots`; NDJSON
  out.
- `[[jest-oracle-bare-package-imports]]` — **the `/tmp` mirror recipe BREAKS
  for bare-package imports → the mirror needs a `fixtures/` sibling AND a
  `node_modules` symlink.**
- `[[oracle-jest-testtimeout]]` — tier-3 jest oracles need
  `--testTimeout=120000`.
- `[[tool-handler-differential-minted-remap]]` — positional UUID/timestamp
  normalization on OUTPUT.
- `[[project-store-fixture-needs-real-create]]` — **provision via the REAL
  `repos.*.create`.**
- `[[p4-6k-groups-projects-server]]` — **the fixture pins ids via
  `create(data, {id})`.**
- `[[oracle-regen-from-worktree-path]]` — **tsx must import the WORKTREE
  case file; verify by grepping the NDJSON for a new row.** Critical: this
  lane runs in a worktree.
- `[[p4d3-db-size-reduction-drift]]` — run differentials `--nocapture` and
  **grep for SKIP**.
- `[[byte-exact-static-data-transcription]]` — relevant to the §2 assertion.

---

## Fixtures

**This lane DELIVERS a new Pascal fixture family** and **REGENERATES every
oracle its own changes invalidate.** That obligation is not optional and it
explicitly includes the two `builtin_*` tests that read `fresh_schema.json`
directly.

**New — the Pascal fixture family.** A mount tree carrying
`Tools/*.tool.json` at several tiers, built to exercise the roster branches:

- definitions at the character, participant, group, project, and global
  tiers (so `TIER_ORDER` and nearest-tier-wins are both exercised);
- a **`disabled` tombstone** (suppressing at its tier AND below);
- a **same-tier tie** (two mounts at one tier → the lexicographic mount-id
  tiebreak);
- a **duplicate `name` within one mount** (→ an error entry, not a throw);
- a **malformed definition** (→ an error entry whose message exercises
  `formatDefinitionIssues`' `" — or — "` union flattening);
- a **nested** `Tools/sub/x.tool.json` (→ rejected by `isRootToolFile`);
- enough definitions to exercise the `MAX_ROSTER_SIZE` drop list;
- both roll forms (range + dice), a `title` present and a `title` omitted
  (→ `displayTitle` derivation), and a `when` using `roll`, `params`, and a
  `$param` operand.

Build it via v4's REAL repos (`[[project-store-fixture-needs-real-create]]`),
pinning ids with `create(data, {id})` (`[[p4-6k-groups-projects-server]]`).

**Regenerate (atomically):**

- `harness/oracle/fixtures/chat-settings-tier2.json` +
  `build-chat-settings-fixture.ts` — with `customTools`;
- `harness/oracle/fixtures/chats-messages-ops-tier2.json` +
  `build-chats-messages-ops-fixture.ts` — with `pascalMeta`;
- `harness/oracle/provision/dump-fresh-schema.ts` → **replaces**
  `crates/quilltap-core/src/services/provisioning/fresh_schema.json`;
- `harness/oracle/provision/build-provision-oracle.ts`.

**All regeneration is at v4 `e3593f75`, Node 24
(`~/.nvm/versions/node/v24.13.1/bin`).** Put the regen recipe in each
differential's header (the `[[p4-6c-salon-consolidation]]` recipe block is
the template).

---

## Shared contract (binding)

> Reproduced verbatim and identically in all three orders of this round:
> `p4.d5-dice-rng-lenient-numbers.md` (lane A), `p4.d6-help-doc-sync-drift.md`
> (lane B), `p4.6ay-pascal-custom-tools-server.md` (lane C). Lane B is disjoint
> from both sections below and consumes neither; it reproduces them so the three
> orders stay diffable against each other.

### §1 — `quilltap-core::pascal::dice` (OWNER: lane A — CONSUMER: lane C)

Lane A creates `crates/quilltap-core/src/pascal/dice.rs`, adds `pub mod pascal;`
to `crates/quilltap-core/src/lib.rs`, and creates `crates/quilltap-core/src/pascal/mod.rs`.
Lane C adds its own modules under `pascal/` and **MUST NOT edit `dice.rs`**.

The surface below is **binding**. Lane A may add to it; lane A may not change
these signatures without a round-level renegotiation.

```rust
// crates/quilltap-core/src/pascal/dice.rs — v4 lib/pascal/dice.ts

/// v4's bounds. Out-of-bounds notation is SKIPPED, never clamped.
pub const MIN_DIE_SIDES: u32 = 2;
pub const MAX_DIE_SIDES: u32 = 1000;
pub const MIN_DICE_COUNT: u32 = 1;
pub const MAX_DICE_COUNT: u32 = 100;
pub const MAX_DICE_MODIFIER: i64 = 1000;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct DiceNotation {
    pub count: u32,
    pub sides: u32,
    pub modifier: i64,
}

#[derive(Debug, Clone, PartialEq)]
pub struct DiceRollResult {
    pub count: u32,
    pub sides: u32,
    pub modifier: i64,
    pub results: Vec<i64>,
    pub subtotal: i64,
    pub total: i64,
}

/// v4 `parseDiceNotation` — the STRICT anchored form
/// (`/^\s*(\d+)?d(\d+)(?:\s*([+-])\s*(\d+))?\s*$/i`): whitespace around the
/// sign IS allowed here. `None` when unparseable OR out of bounds.
pub fn parse_dice_notation(s: &str) -> Option<DiceNotation>;

/// v4 `rollNotation` — `subtotal` = sum of faces; `total` = subtotal + modifier.
pub fn roll_notation(n: &DiceNotation, rng: &mut dyn RandomBytes) -> DiceRollResult;

/// v4 `formatDiceBreakdown` — `"3d6+2: [4, 2, 6] + 2 = 14"`;
/// modifier 0 → `"3d6: [4, 2, 6] = 12"`.
pub fn format_dice_breakdown(r: &DiceRollResult) -> String;
```

**The randomness seam.** `RandomBytes` (+ `OsRandomBytes`, `FixedBytes`) is the
**existing** trait at `crates/quilltap-core/src/tools/rng.rs:99-146`. Lane A MAY
relocate it into `pascal::dice` to mirror v4's own move of the crypto into
`lib/pascal/dice.ts` — but **`crate::tools::rng::RandomBytes` MUST continue to
resolve** (re-export if moved). Lane C imports it by whichever path resolves at
merge and **MUST NOT define its own seam**. `FixedBytes::consumed()` is
load-bearing for both lanes' differentials and must survive any relocation.

**The two regexes are NOT interchangeable** (v4 `dice.ts:69` scan vs `:71`
strict): the scan form forbids whitespace around the sign — that is what keeps
`"2d6 - 1 apple"` a plain 2d6 — while the strict form allows it. Lane A owns
both. Lane C consumes **only** `parse_dice_notation` (strict).

### §2 — the `run_custom` tool-definition bytes (OWNER: lane A — CONSUMER: lane C)

Lane A owns `crates/quilltap-core/src/tools/definitions/data.rs`,
`harness/oracle/cases/tool-definitions.ts`, `tool-definitions-canonical.ts`, and
the `gen-tool-catalog.ts` generator. Lane A lands **both** deltas its
tool-definitions differential demands against v4 `a33ac8b8`:

- **(a)** the `rng` entry's new `modifier` property (`default: 0`,
  `minimum: -1000`, `maximum: 1000`, `type: integer`); and
- **(b)** the `run_custom` entry, transcribed **byte-exact** from v4's
  `lib/tools/__tests__/__snapshots__/tool-definitions-snapshot.test.ts.snap`
  (recipe: the `byte-exact-static-data-transcription` memory note).

Lane C **MUST NOT edit `data.rs` or the tool-definitions oracle case.**

**Binding on lane C:** the static `run_custom` description in `data.rs` is v4's
`buildRunCustomDescription([])` — the **empty-roster** form. Lane C's
`build_run_custom_description(&[])` MUST return bytes **identical** to that
pinned description, and lane C ships a test asserting exactly that against the
`data.rs` entry. (v4 `lib/tools/run-custom-tool.ts:194` is the empty-roster
definition; `:153` is the builder. They must not drift apart.)

**Unification note (binding on the unifier):** A's `run_custom` catalogue entry
and C's executor arm **must land together**. `run_custom` reaching `data.rs`
without C's handler would publish a tool to the model that answers
`Unknown tool: run_custom`. **If lane C slips the round, the unifier reverts
§2(b) and its oracle-case rows and lands A's §2(a) alone.**

## Ownership (binding)

> Reproduced verbatim and identically in all three orders. `CHANGELOG` and
> `docs/developer/porting/status-log.md` are **append-only for every lane** and
> union-merge at unification (standing rule).

### Lane A — `p4.d5-dice-rng-lenient-numbers.md`

- `crates/quilltap-core/src/pascal/dice.rs` **(new)**
- `crates/quilltap-core/src/tools/llm_number.rs` **(new)**
- `crates/quilltap-core/src/rng_patterns.rs`
- `crates/quilltap-core/src/tools/rng.rs`
- `crates/quilltap-core/src/tools/definitions/data.rs`
- the lenient-number call sites: `tools/web_search.rs`, `tools/search.rs`,
  `tools/help_search.rs`, `tools/run_sql.rs`, `tools/help.rs`,
  `tools/terminal.rs`, `tools/annotations.rs`, `tools/photo.rs`,
  `tools/doc_edit/text.rs`, `tools/doc_edit/markdown.rs`,
  `tools/doc_edit/shared.rs`, plus the `image_generation` / `memory_search`
  sites the lane's own survey locates
- `crates/quilltap-core/src/services/message_finalizer.rs` (whole file)
- `harness/oracle/cases/`: `rng-patterns.ts`, `rng-executor.test.ts`,
  `tool-definitions.ts`, `tool-definitions-canonical.ts`, + new
  lenient-number case(s)
- `harness/oracle/fixtures/rng-executor.json` + `build-rng-executor-fixture.ts`
- `crates/quilltap-harness/tests/`: `rng_patterns_equivalence.rs`,
  `rng_executor_equivalence.rs`, `tool_definitions_equivalence.rs`, + new
  lenient-number test(s)

### Lane B — `p4.d6-help-doc-sync-drift.md`

- `crates/quilltap-core/src/services/help_doc_sync.rs`
- `crates/quilltap-core/src/db/help_docs.rs`
- `crates/quilltap-core/src/db/help_search.rs`
- `crates/quilltap-core/src/help_doc_slug.rs` **(new — or the module path the
  lane's survey justifies)**
- `crates/quilltap-host/src/files_store.rs`
- `harness/oracle/cases/` + `crates/quilltap-harness/tests/`: the
  `help_doc_sync` / `help_docs_*` / `help_search` families only

### Lane C — `p4.6ay-pascal-custom-tools-server.md`

- `crates/quilltap-core/src/pascal/` — **everything except `dice.rs`**
- `crates/quilltap-core/src/tools/run_custom.rs` **(new)**
- `crates/quilltap-core/src/tools/executor.rs`,
  `crates/quilltap-core/src/tools/capabilities_report.rs`
- `crates/quilltap-core/src/services/`: `pascal_writer.rs` **(new)**,
  `prospero_notifications.rs`, `tool_build.rs`, `tool_execution.rs`
- `crates/quilltap-core/src/services/provisioning/`: `fresh_schema.json`,
  `chat_settings_seed.json`, `mod.rs`
- `crates/quilltap-core/src/db/`: `chat_settings.rs`, `chats_messages.rs`,
  `chats_messages_read.rs`
- `crates/quilltap-core/src/api/`: `salon.rs`, `settings.rs`, `mod.rs`
- `crates/quilltap-web/` (the custom-tools route)
- `harness/oracle/provision/`, and the `chat-settings` / `chats-messages-ops` /
  `pascal-*` oracle cases + their `crates/quilltap-harness/tests/` counterparts

### Declared shared files (region-pinned — the ONLY files two lanes touch)

| File | Lane A's region | Lane C's region |
|---|---|---|
| `crates/quilltap-core/src/services/orchestrator.rs` | the RNG auto-detect execute block **only** (the `rng_input` `json!` and the persisted TOOL-row `arguments`) | the `custom_tool_context` construction + the `build_tools` call site **only** |
| `crates/quilltap-core/src/pascal/mod.rs` | `pub mod dice;` | its own `pub mod` lines |
| `crates/quilltap-core/src/tools/executor.rs` | the `image_generation` `count` validation region **only** (the unit-2 lenient-number site — the last round's undeclared `05d03ab` collision, now declared) | everything else (the `run_custom` dispatch arm, `BUILT_IN_TOOLS`, the execution-context fields) |

Neither lane may reformat, reorder, or re-wrap `orchestrator.rs`; both cite this
table in the commit message of any commit touching it. The unifier takes both
hunks. **Any other same-file collision is a round-planning bug — stop and report
rather than resolving it in-lane.**

### Version-bump ownership

All three lanes bump `quilltap-core` and `quilltap-harness` per their own
commits; lane C additionally owns any `quilltap-web` bump. **Identical first
bumps off a shared base merge SILENTLY** (the `p4.6aw-ax-8-round-plan` memory
note) — so the **unifier recounts `base + total bumps` rather than trusting the
merge result**, and no lane may assume its number survives.

---

## Verification gate (before declaring the lane done)

1. `cargo fmt` — clean.
2. `cargo clippy -D warnings` — on **BOTH feature sets** (default AND
   `native-transport`).
3. `cargo test --workspace` — with **every** differential env var set.
4. **This lane's own differentials re-run BY NAME with `--nocapture` and
   `grep`'d for `SKIP` — zero SKIP.** (`[[p4d3-db-size-reduction-drift]]`: a
   silently skipped differential is an unverified port.)
5. **Every oracle regenerated FRESH from the v4 checkout at `e3593f75`**,
   Node 24 (`~/.nvm/versions/node/v24.13.1/bin`) — including the
   `fresh_schema.json` re-dump and the provision oracle, with
   `provisioning_equivalence`, `builtin_mounts_equivalence`, and
   `builtin_templates_equivalence` all green against the regenerated file.
6. The §2 assertion green: `build_run_custom_description(&[])` byte-identical
   to lane A's `data.rs` `run_custom` description.
7. Every Tier-3 item enumerated in the status log as a **loud typed refusal
   or a documented absence** — no silent stubs, no `TODO` code.
8. Per-commit rule: follow `.claude/commands/commit.md` (version bump,
   CHANGELOG every commit, status-log append). **CLAUDE.md is
   summary-only — do not append unit records there.**

## Ground rules

The standing set: the differential port discipline (**fix the port, not the
diff**); **never accept unverified Rust** — compile + run the tests before
presenting anything as done; no stubs or `TODO` beyond the enumerated Tier-3
refusals; **the cipher is frozen and the schema is frozen EXCEPT for the one
human-ruled adoption in unit 10** (which is an exception, not a precedent);
synthetic test-pepper fixtures only — never real Friday data and never the
real pepper; database writes only against copies. **If a unit's fresh v4
survey contradicts this order, STOP and report before proceeding.**
