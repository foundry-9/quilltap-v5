# Work order P4.D196 — v4 `d8d2890ee` (PR #62, "GPT Image 2.5 + the full OpenAI image parameter set"), SERVER half: the OpenAI image capability table as ONE source (`model/openai_image_models.rs` — eight families, longest-prefix snapshot resolution, `checkArbitrarySize`, `mimeTypeForFormat`), the OPENAI image dialect rewritten over it (per-family quality tiers, arbitrary WxH, the four GPT-Image extras from the residual bag, `n` capped at `maxN`, the returned `mimeType` following the requested format, every dropped parameter WARNED), the per-model OpenAI options schema served through the existing `options-schema` action, v4 bugs 148 + 149 in `generate_image.rs` (v5 measurably HAS both), the `generate_image` tool definition's moved bytes, the ONE quality list at the `?action=generate` route arm, the manifests regen (eight OPENAI image models), and the openai-SDK 7.10 → 7.15 wire re-check over every recorded corpus — from `main`

**Status: OPEN** — ordered 2026-09-16, the `53294163f` GPT-Image-2.5 drift
catch-up + maintenance round (P4.94 ∥ P4.D196 ∥ P4.D197 ∥ P4.95). The lane
updates this header at lane close; the unifier rewrites it at unification.

## Preamble

- **v4 oracle baseline: `1fefadb9a`.** This lane absorbs the SERVER half of
  **`d8d2890ee`** ("Add GPT Image 2.5 models and complete OpenAI image
  parameters (#62)", 2026-09-16 21:14, three squashed commits): `plugins/
  dist/qtap-plugin-openai/{image-models.ts (NEW, 283), image-options-schema.
  ts (NEW, 216), image-provider.ts (+296/−?), index.ts (+49), types.ts (+3)}`,
  `lib/image-gen/quality.ts` (NEW, 61), `lib/tools/image-generation-tool.ts`
  (+17), `lib/tools/handlers/image-generation-handler.ts` (+35),
  `app/api/v1/images/route.ts` (+3), `app/api/v1/image-profiles/[id]/route.
  ts` (+3), `packages/plugin-types/src/providers/image.ts` (+12 — the
  `quality` union widened to eight), `packages/plugin-types/src/plugins/
  provider.ts` (+10 — docs: three host-lifted keys, not two), the snapshot
  `lib/tools/__tests__/__snapshots__/tool-definitions-snapshot.test.ts.snap`
  (+17), and the four NEW tests you transcribe as corpus rows:
  `__tests__/unit/openai-image-provider-params.test.ts` (266),
  `__tests__/unit/lib/tools/image-generation-orientation.test.ts` (39),
  `__tests__/unit/lib/tools/image-generation-tool-defaults.test.ts` (66),
  `__tests__/unit/lib/image-gen/quality-schema-parity.test.ts` (66). Its
  client hunks (`components/image-profiles/ImageProfileForm.tsx`,
  `ImageProfileParameters.tsx`, `components/images/image-generation-dialog.
  tsx`) and its two `help/` files are P4.D197's. The ledger row is
  `ORDERED(P4.D196, P4.D197)`. Spec reads: `git show 53294163f:<path>` for
  every file above, and `git show d8d2890ee -- <path>` for the hunks —
  **never the commit message** (§R.4: the third squashed commit REVISES
  the first — e.g. the message's "quality was previously not sent to GPT
  Image models at all" is true, but the FIX's shape is `resolveQuality`
  returning `undefined` for GPT Image when unset and `'standard'` for
  DALL·E, which only the file says).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** Pin **`/tmp/qt-v4-pin-p4d196-
  53294163f`** (the TARGET — v4 HEAD; it carries the code AND the settled
  plugin bundles) for every family this port MOVES: `image_dialects_
  equivalence` (the recorder drives the REAL `image-provider.ts`),
  `tool_definitions_equivalence` (+ canonical), `image_generation_tier3`,
  `image_generate_route`, `images_generate_route`, `image_profiles_routes`,
  `provider_registry_equivalence`, the manifests generator, and the SDK
  re-check's whole corpus set. Pin **`/tmp/qt-v4-pin-p4d196-1fefadb9a`**
  (the BASELINE) for the neutrality legs (§ below). Pin verification: the
  target-pinned image-dialects NDJSON carries `gpt-image-2.5` rows and the
  baseline-pinned one carries NONE. ⚠ All three symlink classes — the
  recorder runs FROM `plugins/dist/qtap-plugin-openai/` and dies on
  `Cannot find module 'openai'` without the per-plugin `node_modules`
  link.
- **Sibling lanes:** P4.94 (widens the committed `images-*.db` pair your
  two route families COPY — never edit the pair; §R.10(a)), P4.D197 (the
  SPA half — it records the SAME options-schema output you assert, §S.1/
  §R.10(c); it owns `help/**` and `image-profile-form.ts`), P4.95 (the
  orchestrator carry — it needs the request-envelopes corpus you regen
  for the SDK check to stay yours; §R.10(b)).
- **Worktree:** `git worktree add <path> -b claude/p4-d196-gpt-image-25-
  server-<hash> main`; `CARGO_INCREMENTAL=0`. No SPA work.
- **Read before the work:** `crates/quilltap-core/src/model/image_dialects.
  rs` WHOLE (`build_openai` `:170-203` — today's arm: `model_or_default
  (params, "dall-e-3")`, `is_gpt_image_model` = `starts_with("gpt-image-")`
  `:134`, `validate_and_normalize_size` `:139-168` with the three fixed
  lists, `quality`/`style` only when `!is_gpt`; `parse_openai_like(resp,
  "OpenAI", "image/png")` `:405,418` — mimeType HARDCODED; `supported_
  image_models("OPENAI")` `:843-850` — six ids in the OLD order;
  `finalize_models`/`parse_models_page` OPENAI arms `:997,1320` — the
  `/^(dall-e|gpt-image)/` filter, UNCHANGED by the drift; the module
  header's "SDK providers build via the OpenAI SDK" note; the `NanoGptWire
  Extras` seam `:92,478` — the precedent for a builder returning MORE than
  `(url, body)`); `crates/quilltap-core/src/model/image.rs:25-140`
  (`ImageGenParams` — `quality: Option<String>`, `profile_parameters`, the
  two `*_inserted_by_orientation` flags and `to_json`'s key order);
  `crates/quilltap-core/src/image_gen.rs:225-245` (`match_model` — exact
  then LONGEST prefix via `max_by_key`, with the note on v4's sort-desc
  tie rule; `findImageModel` is the SAME rule over a different table —
  reuse the idiom, cite the note) and `:289-320` (`resolve_orientation`);
  `crates/quilltap-core/src/image_gen_data.rs:100-130` (the OPENAI
  `ModelInfo` block — six models, `gpt_image_orientation()` /
  `dalle3_orientation()` / `dalle2_orientation()`; v4 now derives the list
  from the table with `orientationFor(id)`); `crates/quilltap-core/src/
  tools/generate_image.rs:195-210` (`SCHEMA_SIZES` [3] / `SCHEMA_STYLES` /
  `SCHEMA_QUALITIES` [2]), `:300-345` (`validate_image_generation_input` +
  `parse_schema` — the three `.unwrap_or_else(SCHEMA_X[0])` at `:326-331`
  ARE bug 148; the doc comment above `:300` says so in v4's pre-fix words:
  "the defaults are an OVERRIDE and outrank the profile's stored bag, which
  is why a profile storing `quality: 'hd'` still generates `standard`"),
  `:615-635` (`tool_input_overrides` + `orientation_of` — `orientation_of`
  IS bug 149: v4's `input.orientation ?? 'square'` verbatim), `:2456` and
  `:2554` (the two call sites — ordinary + Concierge reroute — both
  passing `Some(orientation_of(original_input))`); `crates/quilltap-core/
  src/api/images.rs:1440-1470` (the `?action=generate` body parser — the
  `quality` arm at `:1461` spells `("standard" | "hd")`; `size`/`aspect
  Ratio` are `z.string()`; the route already passes `orientation: None` —
  "undefined is the builder's documented leave-size-alone, which POST
  /api/v1/images already passes", as v4's fix says); `crates/quilltap-
  core/src/api/image_profiles.rs:631-705` (`image_profile_generate` —
  today it validates ONLY `prompt` + `count` and builds the tool input with
  `size/style/quality/aspect_ratio: None` — MEASURE v4's post-commit
  handler at `app/api/v1/image-profiles/[id]/route.ts:230-300` for
  whether the optional body fields are THREADED into the tool input; if
  they are, v5 has a pre-existing gap this lane records or closes — Tier
  2), `:874-945` (`image_profile_options_schema` — v4's
  `handleOptionsSchema` port; the dispatcher it calls is `model/nanogpt_
  catalog.rs:376-381 image_provider_options_schema`, NANOGPT-only today —
  your OPENAI arm goes beside it or the dispatcher moves to a neutral
  module, say which), `:598-630` (`image_provider_list` reads
  `m.image_generation_models` off the MANIFEST — the manifests regen is
  what delivers §S.2's eight ids); `crates/quilltap-core/src/provider_
  manifest/manifests/openai.json` (`imageGenerationModels` — six ids,
  ONE-LINE inline per the generator's sentinel); `harness/oracle/providers/
  gen-provider-manifests.mjs:1-60, :360-380, :460-470` (the generator: the
  `imageGenerationModels` inline sentinel; the AUGMENTATION table —
  `manifest-generator-augmentation-rot`: diff the WHOLE manifests dir after
  a regen, expect ONLY `openai.json` to move); `harness/oracle/providers/
  record-image-fixtures.mjs` WHOLE (the recorder — runs from the plugin
  dir with `global.fetch` mocked; the per-provider case table you extend;
  the `kind:'dialect'` line shape `{provider, case, model, style, input,
  request:{method,url,body}, mode, wire, outcome, images, thrown,
  isModeration}` — `input` is the `ImageGenParams` the harness rebuilds,
  so it MUST carry `profileParameters` for the extras rows) and
  `regenerate-image-fixtures.sh`; `crates/quilltap-harness/tests/image_
  dialects_equivalence.rs` (how `input` → `ImageGenParams` is rebuilt —
  check `profileParameters` → `profile_parameters` survives); `crates/
  quilltap-harness/tests/tool_definitions_equivalence.rs:1-30` + `harness/
  oracle/cases/tool-definitions.ts` (byte-exact; `preserve_order` round-
  trip; the regen line); `harness/oracle/fixtures/image-generation.json`
  (the tier-3 corpus — rows at `:79`, `:176`, `:221-246` already carry
  `size`/`quality`/`style` inputs incl. `"size": "512x512"` with NO
  orientation at `:246` — those rows MOVE at the target: bug 149 lets the
  size reach the provider); `harness/oracle/cases/image-generate-route.
  test.ts:40-50` (the two-DB /tmp fixture recipe `QT_FIXTURE_IMGGEN_*` +
  `build-image-generation-fixture.ts`) and `images-generate-route.test.ts`
  (COPIES the committed `images-*.db` — P4.94's); `harness/oracle/cases/
  image-profiles-routes.test.ts:420-460` (the existing `options-schema`
  rows — "NanoGPT is the only provider declaring either" is now FALSE);
  the P4.D158 lane record in `status-log.md` ("the packaging trio's wire
  re-check" — the environment-proof table via the `createRequire` walk,
  the regen-every-corpus batch, the "moved only in self-dating markers"
  disposition); the P4.D101/P4.D138 lane records (the LoRA train — the
  `image-dialects` corpus re-recorded at the tip with "exactly the two
  predicted rows moved"; the options-schema action's landing); memory
  notes `byte-exact-static-data-transcription`, `manifest-generator-
  augmentation-rot`, `a-vendored-count-is-hard-coded-in-several-crates`,
  `js-nullish-chain-is-or-else-not-filter`, `js-nan-is-a-number-to-a-
  schema-but-null-on-the-wire`, `a-fidelity-fallback-behind-a-coercing-
  reader-is-unreachable`, `canned-key-renders-the-role-so-a-default-is-a-
  decision`, `oracle-row-option-value-swallows-a-json-null`, `tracing-
  percent-field-renders-unquoted`, `a-process-global-test-seam-must-be-
  thread-scoped`.

## Mandate

Port the server half of PR #62 from the post-commit files at `53294163f`,
red-first wherever a family can see the change: (1) the capability table
as ONE Rust module the dialect, the model list, `image_gen_data`'s OPENAI
block and the options-schema builder all read — none restating a limit;
(2) the OPENAI dialect rewritten over it — `validate_and_normalize_size`
(unknown model → forward untouched; in the family's list → as given;
arbitrary-capable → `checkArbitrarySize` → forward (debug above the
experimental line) or WARN + `1024x1024`; else WARN + `1024x1024`),
`resolve_quality` (unset → GPT Image `None` / DALL·E `"standard"`; unknown
model → as given; in the family's tiers → as given; else WARN + the same
default), `n = caps ? clamp(requestedN, 1, maxN) : requestedN` with the
capping WARN, `style` only where `caps.supportsStyle` (and `!isGptImage`
when caps are unknown), the four extras read off `profile_parameters` by
`readEnum` / `readIntInRange` with v4's drop-with-WARN semantics, the
transparent-needs-png rule, `output_compression` only for jpeg/webp (a
DEBUG line otherwise), the `Calling OpenAI Images API` DEBUG bag, the body
KEY ORDER `model, prompt, n, [response_format], size, [quality], [style],
[background], [output_format], [output_compression], [moderation]`, and
the parsed images' `mimeType` following the (possibly forced) output
format; (3) the per-model options schema per §S.1; (4) bugs 148 + 149 in
`generate_image.rs` — the three defaults become `None` (count keeps 1 with
v4's why), `requested_orientation` at BOTH call sites; (5) the `generate_
image` tool definition's bytes regenerated and copied; (6) the ONE quality
list (`image_gen/quality.rs`, eight values in v4's order) read by the tool
schema and the `api/images.rs:1461` arm, with a unit test playing v4's
compile-time assertion (the list equals the tool definition's enum); (7)
the manifests regenerated (§S.2); (8) the SDK wire re-check over every
recorded corpus at the target pin, dispositioned per the P4.D158 method.
Every dropped-parameter WARN/DEBUG lands as a `tracing` line with a
capture pin and a silence leg (§R.6). This lane does NOT touch `apps/web/
**`, `help/**`, the committed fixture pairs, or the orchestrator.

## Survey-verified starting points (fresh survey 2026-09-16 at `53294163f` and on `main` `201ae34d` — do not re-derive)

### v4 (at `53294163f`)

- **`image-models.ts`** — `OPENAI_IMAGE_MODELS` (newest first): `gpt-image-
  2.5-sunburst`, `gpt-image-2.5-flare` (both `gptImage`, qualities
  `auto,low,medium,high,xhigh,max`, sizes `GPT_IMAGE_WIDE_SIZES` (13: `auto,
  1024x1024, 1536x1024, 1024x1536, 1792x1024, 1024x1792, 1920x1088,
  1088x1920, 2048x2048, 2560x1440, 1440x2560, 3840x2160, 2160x3840`),
  `arbitrarySizes: true`, `supportsStyle: false`, `maxN: 10`); `gpt-image-2`
  (qualities `auto,low,medium,high`, WIDE sizes, arbitrary); `gpt-image-1.5`,
  `gpt-image-1-mini`, `gpt-image-1` (STANDARD sizes `auto, 1024x1024,
  1536x1024, 1024x1536`, NOT arbitrary) — **`1-mini` BEFORE `1`**;
  `dall-e-3` (`gptImage: false`, `standard,hd`, `1024x1024, 1792x1024,
  1024x1792`, `supportsStyle: true`, `maxN: 1`); `dall-e-2` (`standard`
  only, `256x256, 512x512, 1024x1024`, `maxN: 10`). `ARBITRARY_SIZE_RULES
  = {edgeMultiple: 16, maxAspect: 3, maxEdge: 3840, maxPixels: 3840*2160,
  experimentalAbovePixels: 2560*1440}`. `findImageModel(model)`: falsy →
  undefined; exact id; else `filter(startsWith).sort(len desc)[0]`.
  `isGptImageModel(model) = findImageModel(model)?.gptImage ?? (model ??
  '').startsWith('gpt-image-')`. `parseSize`: `/^(\d+)x(\d+)$/` over
  `size.trim()`, integers > 0. `checkArbitrarySize` reasons, in order:
  `not a WIDTHxHEIGHT value`; `both edges must be divisible by 16`;
  `aspect ratio must be between 1:3 and 3:1`; `no edge may exceed 3840px`;
  `total pixels may not exceed 8294400`. `mimeTypeForFormat`: `jpeg` →
  `image/jpeg`, `webp` → `image/webp`, else `image/png`.
- **`image-provider.ts`** — `readEnum(bag, key, allowed)`: `undefined/null/
  ''` → undefined; a string in `allowed` → it; else `logger.warn('Ignoring
  unsupported OpenAI image parameter value', {context: 'OpenAIImageProvider.
  readEnum', key, value: String(raw), allowed: allowed.join(', ')})` →
  undefined (NOTE `String(raw)` — JS `String()` over an object/array/bool;
  use `to_js_string`). `readIntInRange(bag, key, min, max)`: `undefined/
  null/''` → undefined; `typeof raw === 'number' ? raw : Number(raw)` —
  JS `Number()` coercion (`Number(true)` is 1, `Number([])` is 0,
  `Number('80')` is 80, `Number({})` is NaN — find v5's Phase-1 JS-number
  port and reuse it); `!isFinite || !isInteger || <min || >max` →
  `logger.warn('Ignoring out-of-range OpenAI image parameter', {context:
  'OpenAIImageProvider.readIntInRange', key, value: String(raw), min, max})`
  → undefined. `generateImage`: `modelName = params.model ?? 'dall-e-3'`;
  `caps = findImageModel(modelName)`; `isGptImage = isGptImageModel(model
  Name)`; `requestedN = params.n ?? 1`; `n = caps ? Math.min(Math.max(
  requestedN, 1), caps.maxN) : requestedN`, WARN `'Capping image count to
  the model maximum'` `{context, model, requested, capped}` when changed;
  `requestParams = {model: params.model, prompt, n}` (NOTE `model:
  params.model` — the RAW value, undefined stays undefined on the wire
  while validation used `modelName`; v5's `ImageGenParams.model` is a
  `String` — record how the harness rebuilds an absent model); `!isGpt
  Image → response_format: 'b64_json'`; `size = validateAndNormalizeSize
  (params.size, modelName)` (`!size` → `'1024x1024'` — `''` is falsy);
  `quality = resolveQuality(params.quality, modelName)`, set only if
  defined (`!quality` → `isGptImage ? undefined : 'standard'`; `!caps` →
  as given; in tiers → as given; else WARN `'Ignoring quality tier
  unsupported by this OpenAI model'` `{context, model, quality, supported}`
  → the same default); `style` if `caps ? caps.supportsStyle : !isGptImage`
  → `params.style ?? 'vivid'`; the extras ONLY `if (isGptImage)`:
  `outputFormat = readEnum(bag,'output_format',['png','jpeg','webp'])`,
  `background = readEnum(bag,'background',['auto','transparent','opaque'])`,
  `moderation = readEnum(bag,'moderation',['auto','low'])`,
  `outputCompression = readIntInRange(bag,'output_compression',0,100)`;
  `if background !== undefined`: `if background==='transparent' &&
  outputFormat !== undefined && !['png','webp'].includes(outputFormat)` →
  WARN `'Forcing PNG output: a transparent background needs png or webp'`
  `{context, model, requestedFormat}` + `outputFormat = 'png'`; then
  `requestParams.background = background`; `if outputFormat !== undefined
  → requestParams.output_format`; `if outputCompression !== undefined`:
  `outputFormat !== undefined && ['jpeg','webp'].includes(outputFormat)` →
  `requestParams.output_compression` else DEBUG `'Dropping output_
  compression: it applies only to jpeg and webp'` `{context, model,
  outputFormat: outputFormat ?? 'png (default)'}`; `if moderation !==
  undefined → requestParams.moderation`. Then DEBUG `'Calling OpenAI
  Images API'` with `{context, model, size, quality: requestParams.quality
  ?? '(model default)', background ?? '(model default)', outputFormat …,
  outputCompression …, moderation …, n}`. Response: `mimeType = mimeType
  ForFormat(outputFormat)` (the POSSIBLY-FORCED value; undefined → png) on
  every image; `data: img.b64_json || img.url || ''`; `revisedPrompt`.
  The `'Invalid response from OpenAI Images API'` arm unchanged.
  `supportedModels = [...OPENAI_IMAGE_MODEL_IDS]` (eight, table order).
- **`image-options-schema.ts`** — see §S.1; the label table `QUALITY_
  LABELS` (`auto: 'Auto — the model chooses'`, `low: 'Low — fastest and
  cheapest'`, `medium: 'Medium'`, `high: 'High'`, `xhigh: 'Extra High'`,
  `max: 'Max — the finest the model offers'`, `standard: 'Standard'`, `hd:
  'HD — finer detail'`), `QUALITY_DESCRIPTIONS` (`xhigh`, `max` only), the
  two `helpText` variants on `quality` (the `max`-capable one mentions
  "GPT Image 2.5’s premium tiers" with a U+2019), `sizeLabel` (`auto` →
  `Auto — the model chooses the shape`; else `${Square|Landscape|Portrait}
  (${w}×${h})` with U+00D7), `sizeDescription` (>2560×1440 → `Experimental
  resolution — slower, and the model may decline it.`), the size
  `helpText` variants (arbitrary vs not), `STYLE_FIELD`, `BACKGROUND_
  FIELD`, `OUTPUT_FORMAT_FIELD`, `OUTPUT_COMPRESSION_FIELD` (`type:
  'number'`, `helpText` only — no `min`/`max`/`default`), `MODERATION_
  FIELD` (an ASCII apostrophe in `OpenAI's own content filter` but a
  U+2019 in `Auto — OpenAI’s standard filtering`), the group `helpText`.
  `getOpenAIImageOptionsSchema(modelName?)`: `caps = findImageModel(model
  Name) ?? OPENAI_IMAGE_MODELS[0]`. Byte-copy every string from the file.
- **`index.ts`** — `getImageProviderOptionsSchema: (context?) => getOpenAI
  ImageOptionsSchema(context?.modelName)`; `getImageGenerationModels()` →
  `OPENAI_IMAGE_MODELS.map(m => ({id, name, supportedSizes: [...m.sizes],
  orientationSupport: orientationFor(m.id)}))` with `orientationFor`:
  `dall-e-3` → dalle3, `dall-e-2` → dalle2, else the gpt-image trio
  (portrait `1024x1536`, landscape `1536x1024`, square `1024x1024`) —
  UNCHANGED mappings, so the 2.5 models declare the STANDARD trio for
  orientation even though they accept more (v4's comment says why).
- **`lib/image-gen/quality.ts`** — `IMAGE_QUALITY_VALUES = ['auto','low',
  'medium','high','xhigh','max','standard','hd']`; `imageQualitySchema =
  z.enum(IMAGE_QUALITY_VALUES)`; two compile-time `Assert`s pinning it to
  `ImageGenParams['quality']` both ways.
- **`lib/tools/image-generation-tool.ts`** — `size: z.enum(['1024x1024',
  '1792x1024','1024x1792','1536x1024','1024x1536']).describe(…).optional()`
  (NO `.default`), `style: z.enum(['vivid','natural']).describe(…).
  optional()` (NO default), `quality: imageQualitySchema.describe(…).
  optional()` (NO default); `count` keeps `.default(1)` with the why. The
  three new descriptions are in the snapshot hunk — copy from the
  regenerated oracle, not by hand.
- **`image-generation-handler.ts`** — `requestedOrientation(input)`:
  `input.orientation` truthy → it; else `input.size ? undefined :
  'square'`; consumed at BOTH `buildImageGenParams` sites (`:341` ordinary,
  `:436` Concierge reroute). The builder is UNTOUCHED.
- **The two routes** — `quality: imageQualitySchema.optional()` replaces
  `z.enum(['standard','hd']).optional()` in `app/api/v1/images/route.ts:69`
  (inside `options`) and `app/api/v1/image-profiles/[id]/route.ts:26`.
- **`plugin-types`** — `ImageGenParams.quality` widened to the eight; the
  `getImageProviderOptionsSchema` doc: the host lifts `size`, `aspectRatio`
  AND `quality` out of the residual bag onto the named fields (docs only —
  "the behaviour was always this"; MEASURE v5's params builder lifts
  `quality` too — `image_gen/params_builder.rs`).
- **The openai SDK:** `^7.10.0` → `^7.15.0` in the root and every plugin
  that bundles it (fourteen patch-bumped `manifest.json`/`package.json`;
  `builtin-embeddings` untouched). Measure the installed version at the
  pin via the `createRequire` walk (`r.resolve('openai')` then the nearest
  `package.json` — `openai/package.json` is NOT exported).
- **v4's tests** (the corpus seed): `openai-image-provider-params.test.ts`
  — the SDK mocked at `client.images.generate`, `request = mock.calls[0]
  [0]`; `describe('quality tiers')` 7 cases, `describe('size handling')` 7
  (+4 `it.each` failing shapes: `1000x1000` edges, `3200x800` aspect,
  `3856x2160` edge, `3840x3840` pixels), `describe('GPT Image output
  controls')` 6, `describe('family-specific parameters')` 4 (incl. `n: 4`
  → DALL·E 3 capped to 1, sunburst keeps 4; `gpt-image-3-supernova` with
  `size: '4096x4096', quality: 'ludicrous'` forwarded untouched);
  `image-generation-orientation.test.ts` 4; `image-generation-tool-
  defaults.test.ts` 6 (incl. `count` still 1).

### v5 (on `main` `201ae34d`)

- `model/image_dialects.rs` — `build_openai` `:170-203` (the OLD shape
  described in Read-before), `validate_and_normalize_size` `:139`,
  `is_gpt_image_model` `:134`, `parse_openai_like(…, "image/png")` `:405`
  (the mime is a fixed argument — make it the FORMAT-derived value for
  OPENAI; GROK keeps `image/jpeg`), `supported_image_models("OPENAI")`
  `:843` (six, OLD order), the module's unit tests at `:1935-1950`
  (`openai_request_body_order_and_size_default` pins the OLD body bytes
  `{"model":"dall-e-3","prompt":"a cat","n":1,"response_format":"b64_json",
  "size":"1024x1024","quality":"standard","style":"vivid"}` — still true
  after the port for dall-e-3; `gpt_image_omits_response_format_quality_
  style` pins `gpt-image-1` with NO quality — still true when unset).
  `build_image_request_with_extras` `:92` returns `NanoGptWireExtras` — the
  precedent for carrying more than `(url, body)`; the mime type the OPENAI
  parser needs is "the requested output format after forcing" — carry it
  the same way or re-derive it from `params.profile_parameters` in the
  parser (say which; the corpus `images[].mimeType` is the pin).
- `image_gen.rs:228-245 match_model` — exact then longest prefix (`max_by_
  key`; the tie note). `image_gen_data.rs:100-130` — the OPENAI `ModelInfo`
  block (six; `supported_sizes`? — check the struct carries v4's
  `supportedSizes`; if it does not, `getImageGenerationModels`' size lists
  are consumed only by the SPA picker through the MANIFEST and the
  `ModelInfo` block needs only the two new ids + orientation).
- `tools/generate_image.rs:200-206` (the three schema constants), `:326-
  331` (the three defaults — bug 148), `:627-633` (`orientation_of` — bug
  149), `:2456`/`:2554` (its two call sites), `:615-625` (`tool_input_
  overrides` — already `Option`-shaped, unchanged).
- `tools/definitions/data.rs:166-168` — the `generate_image` `ToolDef` (the
  ONLY place in the tree carrying the old description bytes — `grep -rl
  'produces finer details and greater consistency'` hits nothing else: no
  recorded corpus embeds the tool catalog, so ONLY `tool_definitions_
  equivalence` moves on this item).
- `api/images.rs:1461` (`("standard" | "hd")`), `:1467` (`style` —
  unchanged). `api/image_profiles.rs:631-705` (`image_profile_generate` —
  no quality parse today; see the Tier-2 measurement), `:874-945`
  (`image_profile_options_schema`), `:598-630` (`image_provider_list` ←
  manifest). `model/nanogpt_catalog.rs:376-381` (the dispatcher).
- `provider_manifest/manifests/openai.json` — `imageGenerationModels` six
  ids, inline. The generator: `harness/oracle/providers/gen-provider-
  manifests.mjs` (imports v4's BUILT plugin objects — run from the TARGET
  pin, whose bundles `53294163f` settled).
- Harness: `image_dialects_equivalence.rs` ← `harness/oracle/fixtures/image-
  dialects/image-dialects.recorded.ndjson` (the ONLY reader); `tool_
  definitions_equivalence.rs` (+ `tool-definitions-canonical.ts`);
  `image_generation_tier3_equivalence.rs` ← `image-generation.json` over
  the /tmp two-DB fixture (`build-image-generation-fixture.ts`; MEASURE
  which OPENAI model its image profile names — a GPT-Image profile may be
  needed for a bug-149 row where the size survives normalization);
  `image_generate_route_equivalence.rs` (same fixture); `images_generate_
  route_equivalence.rs` (COPIES the committed `images-*.db` — P4.94's);
  `image_profiles_routes_equivalence.rs` (`options-schema` rows at
  `image-profiles-routes.test.ts:420-460`, NANOGPT-only today);
  `provider_registry_equivalence.rs` (`getImageGenerationModels` per
  provider — moves for OPENAI).
- The SDK re-check corpora + their scripts under `harness/oracle/
  providers/`: `regenerate-request-envelopes.sh` → `fixtures/request-
  envelopes/request-envelopes.recorded.ndjson`, `regenerate-google-wire.
  sh`, `regenerate-response-bodies.sh`, `regenerate-stream-fixtures.sh`,
  `regenerate-tool-wire.sh`, `regenerate-image-fixtures.sh`, plus `record-
  moderation-wire.mjs` and `record-web-search-wire.mjs` from their plugin
  dirs, plus the manifests generator — the P4.D158 batch.

## Tiered deliverables

### Tier 1 — must land

1. **`model/openai_image_models.rs`** — the table (eight entries, v4's
   order and every field), `ARBITRARY_SIZE_RULES`, `find_image_model`
   (exact → longest prefix; reuse `match_model`'s idiom and cite its tie
   note), `is_gpt_image_model`, `parse_size`, `check_arbitrary_size` (the
   five reason sentences byte-exact), `mime_type_for_format`, `OPENAI_
   IMAGE_MODEL_IDS`. Unit-pinned against v4's test shapes (dated snapshot
   `gpt-image-2.5-flare-2026-09-08` → flare; `gpt-image-1-mini-…` beats
   `gpt-image-1`; `gpt-image-3-supernova` → None).
2. **The OPENAI dialect rewritten** over the table per the Mandate, RED-
   FIRST over the re-recorded `image-dialects` corpus: extend `record-
   image-fixtures.mjs`'s openai case table with rows transcribing all 24
   `openai-image-provider-params.test.ts` cases (each as a `dialect` row
   whose `input` carries `model`/`size`/`quality`/`style`/`n`/`profile
   Parameters` and whose `wire` answers `OK_RESPONSE`), regenerate at the
   TARGET pin, confirm every pre-existing row is byte-identical to a
   baseline-pin regen (`cmp` the non-openai providers' rows; the openai
   rows that MOVE are the family's existing `gpt-image-*` rows — list them
   with the field that moved), then port until green. The `mimeType` rows
   (`webp`/`jpeg`/unset) are the parser's pin. Every WARN/DEBUG sentence
   and bag lands as `tracing::warn!`/`debug!` with a capture pin + a
   silence leg (§R.6); the corpus proves the DROP, the capture proves the
   SENTENCE.
3. **`supported_image_models("OPENAI")`** and **`image_gen_data.rs`'s
   OPENAI block** derived from the table (eight, table order; the
   orientation trio unchanged; `provider_registry_equivalence` regenerated
   at the target and green — it carries `getImageGenerationModels`).
4. **The options schema** (§S.1) — `image_provider_options_schema
   ("OPENAI", model)` answering v4's `getOpenAIImageOptionsSchema` byte-
   for-byte, proven by `image_profiles_routes_equivalence` rows for the
   five §R.10(c) inputs (`?action=options-schema&provider=OPENAI&model=
   gpt-image-2.5-sunburst` / `gpt-image-2` / `dall-e-3` / `dall-e-2` /
   no model) regenerated at the target — key order included (`key_paths`);
   the `"NanoGPT is the only provider declaring either"` comment in the
   oracle case corrected. Record the JSON for the five inputs in the lane
   record (the unifier `diff -q`s them against P4.D197's).
5. **Bugs 148 + 149 in `generate_image.rs`** — `SCHEMA_SIZES` [5] in v4's
   order, `SCHEMA_QUALITIES` = the shared list, the three `.unwrap_or_else`
   deleted (absent → `None`; `count` keeps 1 with v4's why-comment
   verbatim), the doc comment above `validate_image_generation_input`
   rewritten (it states the BUG as the behaviour), `requested_orientation
   (input) -> Option<Orientation>` replacing `orientation_of` at BOTH call
   sites (explicit → it; `size` truthy → `None`; else `Some(Square)`) —
   RED-FIRST over `image_generation_tier3` regenerated at the target: name
   the rows that move (`:246`'s `"size": "512x512"` no-orientation row at
   least; any row whose profile bag carries a `quality`/`size`/`style` the
   defaults used to override); add rows for the four `requestedOrientation`
   cases and the "profile bag wins when the tool sends nothing" case (a
   profile whose `parameters` carry `quality: 'hd'` + `size: '1792x1024'`
   and a tool call with prompt only — the exact symptom bug 148 describes;
   if the /tmp fixture's profile lacks such a bag, extend `build-image-
   generation-fixture.ts` with a second OPENAI profile and say so). Unit
   pins mirroring v4's two small suites (10 cases).
6. **The tool definition bytes** — regenerate `tool-definitions.ts` +
   `-canonical.ts` at the target, copy the `generate_image` JSON into
   `data.rs:167` byte-exact (the family is the proof; `tool_definitions_
   equivalence` RED at the target before the copy, green after — record
   the three `default` keys gone, `quality.enum` 8, `size.enum` 5).
7. **`image_gen/quality.rs`** — `IMAGE_QUALITY_VALUES: [&str; 8]` (v4's
   order) + `is_image_quality(&str)`; `SCHEMA_QUALITIES` and the `api/
   images.rs:1461` arm both read it; a unit test that the `data.rs` tool
   definition's `quality.enum` equals the list (v4's compile-time
   assertion, as a test); `images_generate_route_equivalence` gains a
   `quality: 'max'` row (200, forwarded) and a `quality: 'ludicrous'` row
   (400 `Validation error`) RED-FIRST at the target (it COPIES P4.94's
   pair — §R.10(a)).
8. **The manifests regen** at the target pin — diff the WHOLE manifests
   dir: ONLY `openai.json`'s `imageGenerationModels` may move (eight ids,
   §S.2 order); any other movement is a finding (`manifest-generator-
   augmentation-rot` — check the augmentation table did not revert a
   sibling's hand-fix).
9. **The SDK wire re-check** (the P4.D158 method): the environment proof
   (openai resolved version at the pin from six plugin dirs + root;
   `@quilltap/plugin-types` 2.7.0; anthropic/google SDKs UNMOVED), then
   every recorded corpus regenerated at the target in one batch; expected:
   `image-dialects` MOVES (items 2–3 — by design), `openai.json` moves
   (item 8), everything else byte-identical or moved only in self-dating
   markers. Anything else is a finding, recorded with the row and field.

### Tier 2 — should land

10. **The profile-id generate route's optional fields** — measure v4's
    `app/api/v1/image-profiles/[id]/route.ts` handler at the pin: does it
    thread `size`/`quality`/`style`/`aspectRatio`/`negativePrompt` from the
    parsed body into `executeImageGenerationTool`'s input? If YES, v5's
    `image_profile_generate` (prompt/count only) has a pre-existing gap:
    close it with the shared quality list at the parse (400 `Validation
    error` on a bad tier) and `image_generate_route_equivalence` rows —
    this may reach the web edge's body arm (`crates/quilltap-web`) and the
    dispatch verb's fields; if it does, say so, bump web, and keep the
    change to that arm. If NO, record the measurement and leave the
    signature.
11. **`profile_parameters` lifting** — v4's docs now say the host lifts
    `quality` beside `size`/`aspectRatio` out of the residual bag onto the
    named params; measure `image_gen/params_builder.rs` does the same
    (the corpus row "profile bag wins" in item 5 is the proof if it does;
    a divergence here is a fix, not a record).
12. The four `it.each` failing-size shapes + the `n` capping + the unknown-
    model passthrough as `image_dialects.rs` unit tests too (fast, no
    corpus), so a future table edit reddens locally before a regen.

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **The SPA, the two `help/` pages, `image-profile-form.ts`, the `docs/v4/`
  mirror** — P4.D197's / the unifier's.
- **The request-envelopes corpus is regenerated here for NEUTRALITY only**
  — no new rows; P4.95's cache-key rows are its own deferral (§R.10(b)).
- **v4 bug 150** (the manual `image-generation-dialog.tsx` posting to a
  dead route) — a CLIENT filing; P4.D197 measures whether v5 has a twin
  (the ledger row's reasoning named `generate-image-dialog.ts`, which is
  the CHAT dialog's port — the wrong file; the measurement is P4.D197's).
- **Widening the tool's `size` enum to arbitrary strings** — v4 deferred it
  on the PR ("it is the model-facing surface"); v5 follows: the enum is
  the five values, recorded.

## The differential requirement, restated

Every unit above lands with its differential driving v4's REAL code at the
target pin: the dialect through the REAL `image-provider.ts` under the
mocked SDK (`record-image-fixtures.mjs` — the W4.7f recipe, extended), the
tool definition through the REAL `zodToOpenAISchema` output (`tool-
definitions.ts`), bugs 148/149 through the REAL `executeImageGenerationTool`
(`image-generation.test.ts` over the /tmp two-DB fixture — the tier-3 mold),
the routes through the REAL Next handlers (`images-generate-route.test.ts`,
`image-generate-route.test.ts`, `image-profiles-routes.test.ts`), the
manifest through `provider-registry.ts`. Red-first is the rule wherever a
family can see the change (items 2, 5, 6, 7); a green regen is not
coverage — grep every fresh NDJSON for the changed bytes (`xhigh`,
`output_compression`, `1536x864`, `image/webp`, `gpt-image-2.5-sunburst`).
Closest recipes: the P4.D138 follow-up lane record (the `image-dialects`
re-record "exactly the two predicted rows moved"; the `options-schema`
family rows) and the P4.D101 record (the model-list discovery rows).

## Fixtures

Consumes: the /tmp image-generation two-DB fixture (built per run —
extend its builder if item 5 needs a second OPENAI profile; say so), the
committed `images-*.db` pair (COPY only — P4.94 widens it; §R.10(a)), the
committed `groups-projects` fixture `image_profiles_routes` copies. Delivers:
the re-recorded `image-dialects.recorded.ndjson`, the regenerated eleven
manifests (only `openai.json` moving), the regenerated tool-definition
bytes in `data.rs`, `image-generation.json`'s new rows, and the neutrality
regens of every other recorded corpus (expected byte-identical). Owner of
dependent regens: this lane regenerates every family it touches; the
unifier re-runs the two route families over P4.94's widened pair.
## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `53294163f` GPT-Image-2.5 drift catch-up + maintenance round:
**P4.94 ∥ P4.D196 ∥ P4.D197 ∥ P4.95**, ordered 2026-09-16. Four lanes, four
worktrees, four lane branches, ALL from `main` (no stacking — every lane's
ownership is disjoint by file, so nothing needs a substrate commit). The
unifier cherry-picks in the order **P4.94 → P4.D196 → P4.D197 → P4.95** (the
fixture heal FIRST so every later family runs over the widened pairs; the
drift's server half before the SPA half that reads its schema; the
orchestrator carry last), gates ONCE, and `merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`1fefadb9a`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`53294163f`** ("docs:
  Update version", `4.10.0-dev.43`, 2026-09-16 21:15) — TWO commits past the
  baseline: `d8d2890ee` (PR #62 — GPT Image 2.5 + the full OpenAI image
  parameter set, carrying v4 bugs 148 + 149, **both of which v5 measurably
  HAS**) and `53294163f` (a version stamp + a rebuild of the fifteen bundled
  `plugins/dist/*/index.js`, a NO-PORT? candidate). The ledger's two §3 rows
  are now `ORDERED(P4.D196, P4.D197)` and `ORDERED(P4.D197)`. v4 `bugfix`
  tip `1a2b2164c` and `release` tip `8fbf2afe0` are unmoved; there is still
  no `release: 4.10.0` squash. The checkout sat on `main`, CLEAN, at the
  planning-START probe (2026-09-16; the §2 probe PASSED, so the ledger
  stood and nothing was re-derived). ⚠ **At the planning-CLOSE probe the
  checkout had gone DIRTY with a v4 bug-150 fix in flight** — HEAD still
  `53294163f`; uncommitted: `components/images/image-generation-dialog.
  tsx` (M), `__tests__/unit/image-generation-dialog.test.ts` → `.tsx`
  (RM), `docs/developer/bugs/bug-150-…md` → `bugs/fixed/` (RM),
  `docs/developer/bugs.md` + `docs/CHANGELOG.md` (M). Recorded in the
  ledger's §1 at ordering. It is CLIENT-only dirt on a file v5 has NO twin
  for (P4.D197 measures exactly that), so no lane's regen imports it and
  no lane chases it; when the human commits it, `/driftcheck` classifies
  the new row (expected NO-COUNTERPART for v5). **At unification the
  baseline MOVES to
  `53294163f`**; `d8d2890ee` retires ABSORBED(P4.D196, P4.D197) and
  `53294163f` NO-PORT-RATIFIED(P4.D197) on its file list.
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log 53294163f..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Pass = branch `main`, HEAD `53294163f`, both logs EMPTY, and a
  tree that is either CLEAN or dirty in EXACTLY the five bug-150 paths the
  ledger's §1 records (§R.1).** Any OTHER mismatch — a moved HEAD, dirt in
  any other `lib/`/`app/`/`components/`/`packages/`/`plugins/`/`help/`/
  `public/schemas/` path, the checkout off `main` — is a STOP: report it
  and do nothing else. (The recorded dirt is the reason the pin is not
  optional this round even for baseline-pinned lanes — a bare-checkout
  regen would import an uncommitted client edit.) A lane never writes the
  ledger.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1). HEAD is past the
  baseline and `d8d2890ee` moves `lib/`, `app/`, `components/`, `plugins/`
  and `help/`. Every regen runs from a LANE-UNIQUE detached worktree per
  ledger §5.1 (`git -C ~/source/quilltap-server worktree add --detach
  /tmp/qt-v4-pin-<order>-<sha> <sha>`, the THREE symlink classes — root
  `node_modules`, `packages/quilltap/node_modules`, every
  `plugins/dist/*/node_modules` — then `cd` into it; never stash or
  checkout-switch the human's repo; `git worktree list` before each batch;
  verify the pin by grepping the fresh NDJSON for a marker only one tree
  has; check the file is non-empty before believing a diff). Which sha:
  - **P4.D196 and P4.D197 pin the TARGET `53294163f`** (v4 HEAD — it carries
    `d8d2890ee`'s code AND the settled plugin bundles the manifests
    generator imports) for every family their port MOVES, and the BASELINE
    `1fefadb9a` for their neutrality legs (the pre-existing image-dialect
    rows, the untouched provider corpora, the `help_*` families' unchanged
    pages). The target-pinned image-dialects oracle MUST carry a
    `gpt-image-2.5` row and the baseline one MUST NOT — that pair is the
    pin verification.
  - **P4.94 and P4.95 pin the BASELINE `1fefadb9a`** — their v4 surfaces are
    untouched by either drift commit (each lane records `git -C
    ~/source/quilltap-server diff --stat 1fefadb9a..53294163f -- <its v4
    paths>` EMPTY in its lane record before the first regen). A pin at the
    baseline is still REQUIRED, not optional: the checkout can go dirty
    mid-lane (ledger §5.1), and a bare-checkout regen is unrepeatable —
    and today a bare-checkout regen imports PR #62's `generate_image`
    definition, whose bytes have moved.
  The sweep driver is the sanctioned per-family path
  (`harness/tools/recipe_sweep.py --run <family> --v4 "$PIN" --v5w
  <ABSOLUTE worktree>`); never run two sweeps concurrently; a family that
  reports `SKIP` in 0.00 s did not run (`a-family-env-var-is-not-its-
  regen-var`). Ledger §5.2's silent-stale-pass discipline applies to
  every regen (`rm -f` the fixture first; read the builder's last line;
  `grep -c` the changed bytes in the fresh NDJSON — MUST be > 0). **Shared
  `/tmp` output paths are a live hazard this round** (P4.92's mid-lane
  clobber): every lane stages its regen outputs under a LANE-PRIVATE
  directory (`/tmp/<order>/…`) and leaves the committed recipe headers
  canonical.
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). The spec
  for every item is `git show <sha> -- <path>` plus `git show <sha>:<path>`
  at `53294163f`; a commit message describes the bug as its author
  understood it and the hunks are often narrower. PR #62's message is
  three squashed commits of prose — the third ("fix(images): share one
  quality schema…") REVISES the first, so read the post-commit FILES, not
  the narrative. Re-verify even where an order flags the trap — the
  order's paragraph is the same kind of prose. Carry a v5-side code
  comment naming the sha where the prose misleads.
- **§R.5 — Designed trips and pre-existing reds: measure, don't assume**
  (ledger §5.4). The tool-definition family (`tool_definitions_
  equivalence`) goes RED at the target pin BY DESIGN (three `default` keys
  removed, two enums widened, three descriptions rewritten) — regenerate
  and copy the bytes, never hand-edit them. The six fixture-vintage reds
  P4.94 heals are PRE-EXISTING (proven by the `1fefadb9a` round's revert
  probes) — a red that SURVIVES the widen is a NEW finding to classify at
  both pins before any source is touched, and it is probably not P4.94's.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest`
  is shared) and its silence leg; a fixture change lands with the regen
  of every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that
  SURVIVES as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`).
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed (and `apps/web/package.json` when it touches the SPA) on each
  commit per `.claude/commands/commit.md`; the unifier RECOUNTS as base +
  the sum of every lane's bumps (the silent-auto-merge trap: identical
  bumps merge as ONE — it fired again two rounds ago). Base at planning:
  core 0.0.935, harness 0.0.827, web 0.0.148, host 0.0.137, cli 0.0.22,
  tauri 0.0.7, SPA 0.5.727. Expected: **core** by P4.D196 and P4.95 ONLY;
  **harness** by P4.D196, P4.D197 (the embed guard's doc list) and P4.95,
  and by P4.94 ONLY if it touches a harness-crate file (expected NOT — its
  fixtures live under `crates/quilltap-web/tests/fixtures/` and its
  migrator under `harness/oracle/fixtures/`, which is not a crate);
  **web** by P4.94 (the committed fixtures are the web crate's) and by
  P4.D196 ONLY if its Tier-2 measurement makes it touch the web edge's
  generate body arm (say so in the lane record); **SPA** by P4.D197 ONLY;
  **host/cli/tauri** by NOBODY (a `help/**` re-vendor changes the
  embedded table `quilltap-host`'s `build.rs` produces but no host
  SOURCE — the P4.D194 precedent: no host bump for help alone).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  P4.D197 pre-lists the paths the target moves (`docs/developer/
  IMAGE_GENERATION.md` at least, with byte counts at the pin) in its lane
  record; no lane copies them.
- **§R.10 — Cross-lane handoffs, named:** (a) **P4.94 widens the committed
  `images-{main,mount}.db` pair that P4.D196's `images_generate_route_
  equivalence` (the `max`-quality row) RUNS over** — run vs edit is not a
  collision; P4.D196 never edits the pair, P4.94 never edits that family's
  sources; the unifier re-runs P4.D196's route families over the widened
  pair after the picks. (b) **P4.D196 owns every recorded provider corpus
  under `harness/oracle/fixtures/`** (its SDK wire re-check regenerates
  them ALL at the target pin); P4.95 therefore pins its wire keys in the
  `orchestrator_tier3` side-channel and NOT in the request-envelopes
  corpus — the corpus pin is P4.95's Tier-3 deferral, recorded with the
  six-key table for a later wire. (c) **P4.D196 produces the OpenAI
  options-schema JSON its differential asserts; P4.D197 records the SAME
  function's output at the SAME pin as a spec fixture** — both from v4's
  REAL `getOpenAIImageOptionsSchema` — and the unifier `diff -q`s the two
  recordings for `gpt-image-2.5-sunburst`, `gpt-image-2`, `dall-e-3`,
  `dall-e-2` and the no-model call (the §S contract). (d) P4.D197's e2e
  beat is GATED by the named constant `P4D196_SERVER_LANDED = false` and
  flipped at unification — its first live run is the unifier's (a beat
  authored while gated has never executed). (e) P4.94 RUNS
  `salon_reads_equivalence` and the `help_*`-free salon families over the
  widened pair; P4.D197 RUNS the `help_*` content families; neither edits
  the other's — the unified gate re-runs both sets. (f) P4.95 stages its
  orchestrator fixture under a lane-private path (`/tmp/p4.95/`) — the
  committed recipe's `/tmp/qt-orch-*.db` paths were clobbered by a sibling
  last round (P4.92 §5); the unified gate regenerates that family itself
  and trusts no artifact either lane left in the shared paths.
- **§R.11 — The e2e seed is a committed fixture this round touches.**
  `apps/web/e2e/support/seed-courier-fixture.ts:124-127` copies the
  committed `courier-images-{main,mount}.db` pair into the Playwright
  instance — so P4.94's widen of that pair changes the base every beat
  runs on. P4.94 runs the FULL Playwright suite at lane close (release
  binary, `npm ci`'d SPA worktree), and the unified gate runs it again.

## §S — Shared contract (binding; byte-identical in P4.D196 and P4.D197)

- **§S.1 — The OpenAI image options schema is v4's `getOpenAIImageOptionsSchema(modelName)` byte-for-byte**, served by the EXISTING `imageProfileOptionsSchema` verb / `GET /api/v1/image-profiles?action=options-schema&provider=OPENAI&model=<id>` edge (P4.D138 unit 6 — the wire shape `{provider, model, optionsSchema, loraSupport}` is UNCHANGED). The schema's fields, in v4's order: group `Image Parameters` → `quality` (enum, `default: ''`, `(model default)` first, then the family's tiers with v4's labels; `xhigh`/`max` carry a `description`), `size` (enum, `default: ''`, `(model default)` then the family's sizes labelled `Square (W×H)` / `Landscape (…)` / `Portrait (…)` with a U+00D7 `×`, `auto` labelled `Auto — the model chooses the shape`, the two >2560×1440 sizes carrying the experimental `description`), and `style` (enum) ONLY when the family `supportsStyle`; then group `GPT Image Output` (with v4's `helpText`) ONLY when the family is `gptImage`: `background` (enum), `output_format` (enum), `output_compression` (`type: 'number'`, NO min/max, `helpText` only), `moderation` (enum). An unknown or absent model gets the FIRST table entry's schema (`gpt-image-2.5-sunburst`). `loraSupport` for OPENAI stays what it is today. Both lanes record this function's output at the target pin for the five inputs named in §R.10(c); the unifier diffs the recordings.
- **§S.2 — `imageProviderList`'s OPENAI `defaultModels` becomes the eight ids in v4's table order**: `gpt-image-2.5-sunburst`, `gpt-image-2.5-flare`, `gpt-image-2`, `gpt-image-1.5`, `gpt-image-1-mini`, `gpt-image-1`, `dall-e-3`, `dall-e-2` — NOTE `gpt-image-1-mini` precedes `gpt-image-1` in v4's table, the reverse of every old v5 list. The server side arrives through the manifests regen (P4.D196 — `openai.json`'s `imageGenerationModels`); the SPA's offline fallback list (`image-profile-form.ts:18`) is P4.D197's and must spell the same eight in the same order.
- **§S.3 — Nothing else on the wire moves.** No new dispatch verb, no new REST route, no `core-contract.ts` type change is expected from this drift; a lane that finds it needs one STOPs on that item and records the shape for the unifier rather than inventing a contract its sibling cannot see.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.94** — the fixture-vintage heal | The committed pairs ON DISK: `crates/quilltap-web/tests/fixtures/{salon-main,salon-mount,salon-llm-logs,salon-long-main,salon-long-mount,chat-gallery-main,chat-gallery-mount,images-main,images-mount,courier-images-main,courier-images-mount,courier-images-llmlogs,pascal-run-custom-main,pascal-run-custom-mount}.db` (the `salon-long-*` and `-llmlogs` members ONLY if the measurement says they need a column; the `.meta.json` sidecars are NOT rewritten); `harness/oracle/fixtures/migrate-memories-fixture-columns.ts` (the `MIGRATIONS` list + the target-pair list + its header); `crates/quilltap-harness/tests/salon_fixture_p4d171_ensure.rs` — its header comment ONLY if the widen makes its premise stale (say so); `harness/tools/sweep-results/<date>-p4.94-*.json`; this order's header | every harness family's `.rs`/`.test.ts` (it RUNS them — P4.D196 owns the image route families' sources), every `crates/*/src/**` file (incl. `test_support.rs` — the per-copy heals STAY as no-ops), `apps/web/**` (it RUNS Playwright), `help/**`, the drift ledger |
| **P4.D196** — the `d8d2890ee` drift, server half | `crates/quilltap-core/src/model/image_dialects.rs`; NEW `crates/quilltap-core/src/model/openai_image_models.rs` (the capability table + `find_image_model` + `check_arbitrary_size` + `mime_type_for_format` + the options-schema builder — or two modules, say which); `crates/quilltap-core/src/model/nanogpt_catalog.rs` — the `image_provider_options_schema` dispatcher (`:376-381`) ONLY (or its relocation to a neutral module — say which); `crates/quilltap-core/src/image_gen_data.rs` — the OPENAI block; NEW `crates/quilltap-core/src/image_gen/quality.rs` (the one quality list); `crates/quilltap-core/src/tools/generate_image.rs`; `crates/quilltap-core/src/tools/definitions/data.rs` — the `generate_image` `ToolDef` ONLY; `crates/quilltap-core/src/api/images.rs` — the `quality` arm at `:1461` ONLY; `crates/quilltap-core/src/api/image_profiles.rs` — `image_profile_generate` ONLY if the Tier-2 measurement requires (say so); `crates/quilltap-web/src/**` — the `?action=generate` body arm ONLY if that measurement requires (say so); `crates/quilltap-core/src/provider_manifest/manifests/*.json` (the generator's output — ALL eleven rewritten, only `openai.json` expected to move); every file under `harness/oracle/fixtures/` that a recorder writes (`image-dialects/`, `request-envelopes/`, `google-wire/`, `response-bodies/`, `stream-fixtures/`, `tool-wire/`, `moderation-wire/`, `web-search-wire/` — the SDK re-check regen) + `harness/oracle/providers/record-image-fixtures.mjs` (new cases); `harness/oracle/fixtures/image-generation.json` + `build-image-generation-fixture.ts` (if a GPT-Image profile is needed); `harness/oracle/cases/{tool-definitions,tool-definitions-canonical,image-generation.test,image-generate-route.test,images-generate-route.test,image-profiles-routes.test,provider-registry}.ts`; `crates/quilltap-harness/tests/{image_dialects,tool_definitions,image_generation_tier3,image_generate_route,images_generate_route,image_profiles_routes,provider_registry}_equivalence.rs`; this order's header | the committed fixture pairs on disk (P4.94's — it COPIES `images-*.db`, never edits), `apps/web/**`, `help/**`, `services/orchestrator.rs` + the tool loops (P4.95's), every other harness family, the drift ledger |
| **P4.D197** — the drift's SPA half + help + the NO-PORT ratification | `apps/web/src/app/screens/settings/images/image-profile-form.ts` (the fallback list); `apps/web/src/app/screens/settings/images/image-profile-modal.ts` + `.spec.ts` (the header's stale OPENAI deferral paragraph; any measured behaviour change); NEW `apps/web/src/app/screens/settings/images/openai-image-options.spec.ts` + its recorded fixture JSON; the SPA-side recorder for that fixture (beside the existing SPA recorders — `spa-oracle-recorders-live-outside-src`); NEW `apps/web/e2e/settings-image-openai-options-flow.spec.ts` (gated); `help/image-generation-profiles.md` + `help/provider-recommendations.md` (byte-copies at the pin); `crates/quilltap-harness/tests/help_tree_embed_guard.rs` — the doc-comment vendor list ONLY; `apps/web/package.json` version; this order's header | every `crates/*/src/**` file, every other harness family (it RUNS the `help_*` content families), every other SPA file (incl. `provider-options-panel.ts` — if the renderer needs a change for v4's schema, STOP and record: that is a §S.3 event), the drift ledger |
| **P4.95** — the `cacheKey` primary-path carry | `crates/quilltap-core/src/services/orchestrator.rs` — the `cache_key:` literal (`:2803`), the `:2952-2956` comment, and the `loop_base_params` closure (`:2957-2962`) ONLY; `crates/quilltap-core/src/services/native_tool_loop.rs` + `text_tool_loop.rs` — the production re-stream / force-final / continuation `params` clones ONLY (per the measured asymmetry) + their `#[cfg(test)]` modules; `crates/quilltap-core/src/model/request_builder/{responses_api,chat_completions}.rs` — `#[cfg(test)]` modules ONLY, and only if an emission pin is missing; `crates/quilltap-harness/tests/orchestrator_tier3_equivalence.rs`; `harness/oracle/cases/orchestrator-tier3.test.ts`; `harness/oracle/fixtures/orchestrator-tier3.json` + `build-orchestrator-fixture.ts`; this order's header | `cheap_llm.rs` (import `build_character_cache_key`, never edit), `streaming_provider.rs`, `primary_stream.rs`, `provider_failover.rs`, every recorded provider corpus (P4.D196's), `apps/web/**`, `help/**`, every other harness family, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0),
   and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp`) where the family is deterministic across
   regens; where it is not (`regenerate-at-both-pins-and-cmp-is-not-
   universal`), run by name and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast` with the lane's env block
   (its own oracle vars + `QT_V4_ROOT`/`QT_V4_CHECKOUT` at the right pin +
   `QT_NODE` = the node BINARY): report **binaries / passed / failed /
   ignored** from the per-binary lines and the exit code, and the lane's
   families confirmed RUN by non-zero duration. **The six fixture-vintage
   families (`salon_mutations`, `chat_gallery`, `images_routes`,
   `courier_images_routes`, `pascal_run_custom_handler`,
   `pascal_custom_tools_route`) are RED on `main` until P4.94 lands** —
   P4.D196 / P4.D197 / P4.95 withhold those six oracle vars from their
   workspace block BY NAME and say so (they SKIP-pass without them);
   P4.94 sets all six and reports them green.
9. **SPA lanes only (P4.D197; P4.94 for Playwright per §R.11):** `npm run
   lint` (incl. the qt-class guard), `npm test` (spec files / tests / 0
   failed), `npm run build` clean, then the lane's e2e beats by FILE
   against the release binary, and ONE full Playwright run at lane close
   (numbers recorded; the documented intermittents — the P4.D161
   pause-toast beats, the P4.d17 quill beat, the `salon-documents-flow` /
   `workspace-flow` terminal trio — are named, re-run by file, and never
   re-classified as this lane's; the standing parks — the five P4.D187
   title-checkpoint beats + the gallery store-probe — stay parked).
10. **Ownership check:** `git diff --stat main...HEAD` — every path is in
    the lane's Owns column; every MUST-NOT-TOUCH path absent.
11. **Per commit:** `.claude/commands/commit.md` in full — the equivalence
    test is non-negotiable (§1), the invariants (§2), real-instance safety
    (§3), the harness regen (§4), build/test/lint (§5), the CHANGELOG
    entry + version bumps (§7). Commit on the lane branch only; never
    merge to `main`; never `git stash`; never a worktree inside a worktree.
12. **Lane close:** update this order's status header (what landed, what
    was refuted by measurement, what stays OPEN with its named cause),
    append the lane record to `status-log.md` (the regen commands as run,
    the counts, the mutation table, every finding outside the lane's
    ownership for the unifier), and stop. The final gate runs in the
    FOREGROUND of the lane's last turn — a backgrounded gate whose
    notification arrives after the lane reports done is a lane that is
    NOT done (`a-backgrounded-sleep-does-not-gate-your-turn`).
