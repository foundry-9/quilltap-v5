# Work order P4.43 — the P4.9H2A maintenance remainder: memory-dedup (preview/run) and conversation-summaries regeneration, un-refusing the last four §1 memory-maintenance verbs

> **STATUS: CLOSED — UNIFIED on main (2026-08-06, the f4955e0e convergence
> round).** Units 6 (memory-dedup) and 7 (conversation-summaries
> regeneration) both shipped differential-verified + mutation-proven;
> the four verbs un-refused; `P49H2A_MAINTENANCE_LANDED` flipped and
> both maintenance beats LIVE — **P4.9H2A is now CLOSED WHOLE.** One
> recorded ownership deviation, accepted at unification: the order's
> "do not touch `api/engine.rs`" premise was wrong (the refusing
> arms discarded the `Db` and `threshold`), so the lane made the
> minimal four-arm dispatch re-signature — no sibling touched the
> file. The dedup's best-effort `CharacterVectorStore` cleanup is
> oracle-unverifiable (v4's store is a file) — noted, not owed. 💸
> The dedup/summaries first live run joins the owed dogfood queue.
> Lane record: `status-log.md` → "Lane record — P4.43".

This is the follow-up lane P4.9H2A's status header names: its units
6+7, deferred loudly at the fallback + wire + embedding-profiles
round (2026-08-06). The four §1 verbs already exist and refuse by
name in `api/memory_maintenance.rs`; the SPA cards exist; the e2e
beats are written and gated behind the named constant
`P49H2A_MAINTENANCE_LANDED`. This lane implements the two services,
un-refuses the verbs, and flips the constant.

**Read `docs/developer/porting/work-orders/
p4.9h2a-embedding-profiles-server.md` FIRST** — its survey (dated
2026-08-06) already maps the v4 anatomy: `lib/tools/memory-dedup.ts`
(446 LOC; `deduplicateCharacterMemories` :152,
`deduplicateAllMemories` :366; no dedicated route — rides
`app/api/v1/system/tools/route.ts` GET `memory-dedup-preview` /
POST `memory-dedup`), the summaries-regen surface + its handler, the
queue-service enqueue dedupe (:742, userId over PENDING+PROCESSING),
and the fixture's crafted near-duplicate memories (two distinct
dims for the grouping, vectors in all four …). This order does not
repeat that survey; it binds the round context.

**v4 baseline for THIS round: `f4955e0e`** (4.8.0-dev.175,
2026-08-06, tree clean at planning). **Verified at planning:** none
of the eleven drift commits (`3adefeba..f4955e0e`) touches
`lib/tools/memory-dedup.ts`, the system tools route,
`queue-service.ts`, `memory-gate.ts`, or `embedding-service.ts` —
the H2A survey's line numbers stand. Regenerate every oracle this
lane creates at `f4955e0e` (straight from the checkout while HEAD
holds; pinned worktree on drift — `oracle-regen-pinned-v4-worktree`).
**The baseline move is the UNIFIER's** — do NOT edit `CLAUDE.md` or
`phase-4.md`.

**Drift-check at lane start:** `git log f4955e0e..HEAD --oneline` +
`git status --short` in `~/source/quilltap-server`; classify any
movement; STOP only if it lands on the H2A-survey files above.

**Siblings (same round):** the five drift lanes P4.D51–P4.D55. TWO
proximity warnings, both verified disjoint at planning but binding:
- P4.D52 owns `services/fold_episode_pass.rs`,
  `services/conversation_markdown.rs`, `db/conversation_chunks.rs`,
  and the `embedding-remainder-*` fixtures. v4's dedup imports
  `memory-gate` helpers (`extractNovelDetails`,
  `deleteMemoriesWithUnlinkBatch`) — NOT the files P4.D52 edits. If
  your implementation turns out to need an edit in any P4.D52 file,
  STOP and escalate to the unifier; do not edit it.
- P4.D54 owns `apps/web/**` EXCEPT the
  `P49H2A_MAINTENANCE_LANDED` constant and the two maintenance
  cards' gated specs, which are YOURS.

**Worktree note:** own branch, own checkout; no `git stash`/
`git worktree` games with sibling lanes.

## Mandate

Land H2A units 6 and 7 exactly as its order specifies them
(deliverables 6–7 and the differential/fixture sections there),
against the `f4955e0e` baseline: the ported dedup service
(`services/memory_dedup.rs` — the synchronous union-find, v4's
per-character try/catch iteration, the preview/run pair), the
conversation-summaries regeneration (+ its job handler, the
dedupe-on-user enqueue helper with `maxAttempts: 1`, and the
`ProductionSpineFactory` registration), the four verbs un-refused
with v4's exact body shapes (dedup `{success, result}`; summaries
POST per the H2A §1), the differentials driving v4's REAL
`deduplicateAllMemories` / summaries path, and the SPA constant
flip that activates the two gated beats. Do NOT touch: any drift
lane's files (each order's Ownership list; in particular nothing
under `model/`, `almanack/`, the chat/salon families, or the
backup/restore family), `api/types.rs` (the verbs already exist).

## Survey addendum (2026-08-06 — what changed since H2A's survey)

- Nothing in the v4 dedup/summaries anatomy (verified per-path
  against `3adefeba..f4955e0e`, above).
- P4.6BL/P4.6BM-era note that still binds: the embedding worker and
  `chatQueueMemories` are live; your enqueue helper must not
  double-mint jobs the reconcile also mints (the H2A survey's
  queue-service dedupe is the v4 mechanism — port it, don't invent).
- The `memories` table now carries the episodic columns
  (`occurredAt`, `entities`, `kind` …). v4's dedup MERGES memory
  rows — its survivor-selection and field-union behavior over the
  episodic columns is part of the byte contract; the differential's
  table diff must include them (the fixture family already carries
  `occurredAt`-bearing rows; extend if the crafted near-duplicates
  don't exercise the episodic fields).

## Tiered deliverables

### Tier 1 — must land
1. Unit 6 per H2A: `services/memory_dedup.rs` + the two verbs
   un-refused + the dedup differential driving v4's REAL
   `deduplicateAllMemories` over the committed
   `embedding-profiles-{main,mount}.db` family (whose crafted
   near-duplicates exist for exactly this), diffing the response
   AND the post-state of `memories` (episodic columns included) +
   any link/unlink side-tables v4's `deleteMemoriesWithUnlinkBatch`
   touches.
2. Unit 7 per H2A: the summaries-regen service + handler + enqueue
   helper (dedupe-on-user, `maxAttempts: 1`) + spine registration +
   the two verbs un-refused + its differential (response + the
   `background_jobs` state, the H2A trigger-matrix precedent:
   matrix claims proven as STATE, not prose).
3. The `P49H2A_MAINTENANCE_LANDED` flip with both gated beats
   ACTIVE in a full Playwright run.
4. Mutation proofs on every first-run-green case (the D24 rule).

### Tier 2 — should land
5. The H2A order's status header updated from "STILL OPEN (units
   6+7)" to closed-by-P4.43 (this is the one sibling-order edit
   this lane owns — the unifier folds it if contested).

### Tier 3 — explicit loud deferrals
- Anything v4's dedup reaches that is still unported in v5 (the
  H2A survey flags `jest incomplete embedding-service` — see
  [[p4.9h2a-embedding-profiles-lane]] for the oracle trap) defers
  as a named refusal INSIDE the service with its arm pinned, never
  a silent stub. If the summaries-regen handler turns out to need
  a fold/context-summary seam P4.D52 is mid-flight on, defer that
  arm loudly and hand the wire to the unifier.

## The differential requirement, restated

Both units ship jest/tsx oracles importing v4's REAL
`lib/tools/memory-dedup.ts` and the real summaries path (per the
H2A order's differential section and the
[[p4.9h2a-embedding-profiles-lane]] memory note — the jest
embedding-service mock trap is documented there), NDJSON → Rust
diff, regenerated at `f4955e0e`, BY NAME with `--nocapture`, zero
SKIP, fresh-baseline markers grepped
([[oracle-regen-silent-stale-pass]]). Committed-fixture rule: point
oracles at the committed `embedding-profiles-*` DBs; run any
fixture-mutating recipe against /tmp copies
([[sweep-recipes-write-into-the-repo]]). Recipes stay runnable
([[harness-recipes-are-runnable]]).

## Fixtures

Consumes the committed `embedding-profiles-{main,mount}.db` family
(H2A's). If the episodic-field or summaries coverage needs a wider
fixture, THIS lane regenerates it and re-runs every H2A family over
it BY NAME (`embedding_profiles_routes` 34 cases,
`embedding_profiles` tier-2, `embedding_reapply`) plus its own new
families, recording the sweep.

## Ownership (BINDING)

THIS lane owns: `crates/quilltap-core/src/api/
memory_maintenance.rs`, the new `services/memory_dedup.rs` and
summaries-regen service/handler files, the
`ProductionSpineFactory` registration lines in
`crates/quilltap-host/src/spine.rs` (no drift lane touches
spine.rs — verified at planning), its new harness/oracle files, the
`embedding-profiles-*` fixtures IF widened, and under `apps/web/`:
the `P49H2A_MAINTENANCE_LANDED` constant + the two gated e2e specs
+ the two maintenance cards ONLY as far as the flip requires. It
must NOT touch: anything in P4.D51–P4.D55's Ownership lists,
`api/types.rs`, `api/engine.rs` (the dispatch already routes to
the refusing handlers — un-refuse inside `memory_maintenance.rs`).
`CHANGELOG` and `status-log.md` are append-only union-merge.
Version bumps per `.claude/commands/commit.md`; the unifier
recounts.

## Verification gate (before declaring done)

`cargo fmt --all --check`; clippy both feature sets `-D warnings`;
`cargo test --workspace --no-fail-fast` with the lane's env block
([[cargo-test-workspace-is-fail-fast]]); the lane's differentials
BY NAME with `--nocapture`, zero SKIP, over fresh `f4955e0e`
oracles; `ng test` + `ng build` clean; the FULL Playwright suite
with both formerly-gated beats ACTIVE and zero skips attributable
to this lane (read [[e2e-playwright-traps]] before diagnosing any
red). Per-commit rules: `.claude/commands/commit.md`. Lane record
appended to `status-log.md` before handoff.
