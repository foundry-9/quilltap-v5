# Work order P4.6ba — the Pascal in-chat SPA (composer popup + run flow, the Pascal bubble, `pascalResult`, the Custom Tools card, the All-Whispers toggle)

> **STATUS: OPEN.**
>
> Lane BA of the FOUR-lane d68638b4 drift-catch-up round (planned
> 2026-07-17). **v4 reference baseline: `d68638b4` (4.8.0-dev.72).** At
> lane start, run in `~/source/quilltap-server`:
>
> ```bash
> git log d68638b4..HEAD --oneline
> ```
>
> **If HEAD moved, STOP and report.**
>
> Sibling lanes: `p4.6ay-pascal-custom-tools-server.md` (lane AY — OWNS
> the wire contract you consume, §4), `p4.d7-mount-nocase-namespace.md`
> (lane D7) and `p4.6az-character-metadata-vault.md` (lane AZ) — both
> fully disjoint from you (**you own `apps/web/` exclusively; touch
> nothing outside it** except your append-only CHANGELOG/status-log
> entries). The **Shared contract** and **Ownership** sections below are
> binding and reproduced verbatim in all four orders. You run in a
> worktree; a unification pass integrates the lanes onto main.
>
> **The differential discipline for SPA lanes** (established across
> P4.6b→P4.6ba): faithful component ports verified by ng unit specs
> (assert v4's exact behaviors, not approximations), the wire contract
> pinned name-for-name against §4, and live e2e beats that
> ACTIVATE-AT-UNIFY once lane AY's server surface is on main. Mocked
> beats are written as real integration tests
> (`[[p4.6ao-ap-aq-round-plan]]` — activating a mocked beat is a real
> integration test). The whisper-visibility helper is a PURE-function
> port with v4's own 92-line test file as the spec template.
>
> The facts below are from a **2026-07-17 survey at `d68638b4` (v4) and
> v5 main `01ae43a4` (SPA)** — if a fresh survey contradicts this order,
> **STOP and report**.

## Mandate

Port the **in-chat** Pascal SPA surface: the composer custom-tools popup
(roster list + parameter form + run flow), the Pascal result bubble
(full-row rendering with the "PASCAL · TOOL" header chip), the
`pascalResult` stream handling, the Settings → Chat **Custom Tools**
toggle card, and — riding the same lane because Pascal's whispers are
what make it visible — the **`8e4b00d4` whisper-visibility re-port**:
the All-Whispers toggle + `isMessageVisibleToOperator` with the
`OPERATOR_FACING_WHISPER_SENDERS` carve-out (pascal, prospero).

**Explicitly NOT this lane:** Pascal's **Workbench** (the `/custom-tools`
vertical, `tool-draft.ts`, builder/bench/library/destination-picker, the
Scriptorium row action, the Settings "Open Workbench" button, the
left-rail entry) — that is **P4.6bb, the next round**, once this round's
server surface is on main. Also not this lane: the full composer gutter
substrate (standing deferral — Pascal gets a **bespoke composer
button**, per the P4.6ay forward note), and anything outside
`apps/web/`.

---

## Survey-verified starting points (2026-07-17 — do not re-derive)

**The SPA has ZERO Pascal surface today**: `pascal`, `pascalMeta`,
`pascalResult`, `customTools` return no hits under `apps/web/src`. The
template throughout is Carina (`systemSender:'carina'` + `carinaMeta`).

### 1. The composer popup

- `apps/web/src/app/chat/chat-composer.ts` — toolbar buttons at
  `:110-200` (composition-toggle, **paperclip `:123-132`**, sparkles,
  book, code, continue, send). The Pascal button is a NEW toolbar button
  in this row (bespoke — no gutter substrate).
- **Popup pattern to copy:** `apps/web/src/app/chat/speaker-selector.ts`
  — anchored `absolute bottom-full` popup (`:35,59-84`), open `signal`
  (`:97`), `toggle()` (`:107`), click-outside via
  `@HostListener('document:click')` + `contains()` (`:120-125`). There
  is NO shared dropdown/click-outside utility; this idiom is the
  convention.
- v4 behaviors to port from `components/chat/CustomToolsDropdown.tsx`:
  roster fetched **fresh on every open** (no caching — carry v4's
  why-comment: the roster is re-resolved per popup open), never shows
  odds/outcome tables, groups/labels per-character variants
  (`characterLabel`), broken-file entries render the loader's verbatim
  reason, the parameter form generated from `parameters` with text →
  declared-type coercion (v4 `CustomToolParamsForm` + 
  `coerceParamValues` — port as a standalone
  `qt-custom-tool-params-form` so P4.6bb's bench can reuse it), private
  toggle, as-character select, run → posts and closes. **The
  d68638b4 "Open on Pascal's Workbench" / "+ new" / repair entries are
  P4.6bb** — omit them and name the omission.
- Run flow: dispatch verbs per §4 (`chatCustomToolsList`,
  `chatCustomToolRun`) via `CoreClient.dispatch`
  (`core-client.ts:87-116`) — the SPA convention over raw REST.

### 2. The Pascal bubble + delegatedDisplay

- Routing decision: `chat-view-model.ts:147-149` `isAnnouncementChip()`
  — `systemSender != null && systemSender !== 'carina'` → chip. **Add
  `'pascal'` to the full-row carve-out** so Pascal results render as
  their own `qt-message-row` (v4 renders them as full messages with a
  header chip), not a collapsed announcement chip.
- `message-row.ts` — Courier early-return at `:50-68`
  (`qt-courier-bubble`) is the branch template; whisper variant at
  `:411-418`. The Pascal header chip ("PASCAL · <toolTitle ?? tool,
  uppercased>" — v4's Salon header chip) is a new branch here or a
  small `pascal-header` fragment; the body is the normal markdown
  pipeline (the 🎲 content is plain markdown from the server).
- DTO additions (`core-contract.ts`): `pascalMeta` on `MessageDto`
  (`:1594-1625`, beside `carinaMeta :1614`), `PascalMeta` interface
  beside `CarinaMeta` (`:1585-1588`), `'pascal'` into the
  `SystemSender` union (`:1559-1571`) — **transcribed from v4
  `chat.types.ts:342-370` per §4**. Sender label into
  `system-message-labels.ts:14-25` ("Pascal"); kind labels for
  `custom-tool-result` / `custom-tool-error` into `:27-75`.
- **delegatedDisplay:** the server stamps it into the TOOL row's stored
  JSON (lane AY unit 8). The SPA `role` union is
  `'USER'|'ASSISTANT'|'SYSTEM'` (`core-contract.ts:1596`) and SYSTEM
  rows are filtered at `chat-view-model.ts:34` — **verify at lane start
  how a persisted TOOL row surfaces in the v5 salon read** (it likely
  never reaches the DTO). If TOOL rows do render, add the suppression
  branch in `splitSwipeGroups`/`buildRenderItems`; if they don't,
  record the verification in the status log and ship no code — do NOT
  add speculative filtering.

### 3. The stream

- `core/chat-stream.reducer.ts` — `carinaAnswer` at `:207-210`,
  `hostAnnouncement` at `:213-215`; add the `pascalResult` branch beside
  them + a `{ kind: 'pascal' }` member in the `StreamMessage` union
  (`:63-83`), deduped by server messageId like the others.
- Frame field: `ChatStreamFrame` (`core-contract.ts:2541-2597`) gains
  `pascalResult?` beside `carinaAnswer :2592` (§4).
- Live rendering: `message-list.ts` `streamMessageToMessageDto`
  (`:285-320` — hardcodes `carinaMeta: null` at `:309`; add
  `pascalMeta` mapping) + `buildStreamRenderItems` (`:329-346`).

### 4. The Settings card

- `screens/settings/chat/chat-tab.ts` — the sixteen
  `qt-collapsible-card`s at `:77-222`; add a **Custom Tools** card in
  v4's position within the tab order (survey v4's
  `ChatSettingsView` order at lane start).
- Template: `composer-spellcheck-settings.ts` (extends
  `ChatSettingsCard`, `chat-settings.api.ts:60-94` — ONE deduped GET,
  `save()` partial PUT). The key is `customTools` (`?? true`), riding
  the `ChatSettingsDto` index signature (`core-contract.ts:1973`) — no
  DTO change needed. v4's card copy + the "Open Pascal's Workbench"
  button: port the toggle + copy; the button is P4.6bb (named
  omission).

### 5. The All-Whispers toggle (the `8e4b00d4` re-port)

- v4: `app/salon/[id]/whisper-visibility.ts` (55 lines) —
  `OPERATOR_FACING_WHISPER_SENDERS = {'pascal','prospero'}` (**keep it
  narrow** — the why-comment about the Commonplace Book leak is
  load-bearing; carry it verbatim) + `isMessageVisibleToOperator(msg,
  {showAllWhispers, userParticipantIds})`: not-a-whisper → true; toggle
  on → true; operator-facing sender → true; author-or-target is a
  user-controlled participant → true; else false. v4 test:
  `__tests__/unit/app/salon/whisper-visibility.test.ts` (92 lines) —
  port as the ng spec, case-for-case.
- v5 insertion (greenfield — NO whisper filtering exists today):
  `salon-conversation.ts:848-859` `displayMessages()` — wrap/replace
  with a computed applying the helper; toggle state as a signal +
  a header toggle control (v4 SalonView's toggle); user participant ids
  from the chat participants (`controlledBy:'user'` —
  `[[chat-participant-fixture-shape]]`). Whisper STYLING
  (`message-row.ts:411-418`) stays as is; v5 has no dimming so v4's
  de-dimming guard has no counterpart (document).

### 6. e2e

- Specs in `apps/web/e2e/` (workers:1, alphabetical, one shared server
  unlocked by `foundation.spec.ts`); composer spec today:
  `salon-composer-modes.spec.ts` (drives
  `.qt-chat-composer-input .qt-rich-editor-content`). Beats: a
  `salon-custom-tools-flow.spec.ts` — open popup → roster renders →
  run a fixture tool → Pascal bubble appears with the header chip →
  toggle All Whispers and assert a whispered result stays visible
  (operator-facing carve-out). **These beats go live only over lane
  AY's server + a Tools-bearing fixture — write them
  ACTIVATE-AT-UNIFY** (annotated + unit-spec-backed until then, the
  `m4b-salon.spec.ts:8-16` pattern). Coordinate the fixture need with
  the unifier: lane AY's route fixture family is the natural instance
  to reuse (its Fixtures section delivers a Tools/ mount tree).

---

## Deliverables (tiered)

### Tier 1 — must land

1. **The wire mirror** (§4, name-for-name): the two CoreRequest verbs +
   response variants, `PascalMeta` + `pascalMeta`, `'pascal'` in
   `SystemSender`, the `pascalResult` frame field, the two systemKind
   labels.
2. **The composer popup**: the toolbar button, the anchored popup
   (speaker-selector idiom), fresh-per-open roster, the standalone
   `qt-custom-tool-params-form` + coercion, private toggle +
   as-character select, the run flow + error surfacing (400 arms render
   the server message; a Prospero error posted by the server arrives as
   a message — do not double-render).
3. **The Pascal bubble**: full-row carve-out, the header chip
   (toolTitle ?? tool fallback for older rows — v4's rule), sender/kind
   labels.
4. **The stream**: reducer branch + StreamMessage kind + live-render
   mapping.
5. **The Settings Custom Tools toggle card** (v4 copy, `?? true`).
6. **The whisper toggle**: the ported helper (spec case-for-case from
   v4's 92-line test), the visibility computed, the toggle control,
   user-participant wiring.

### Tier 2 — should land

7. The delegatedDisplay verification (and the branch ONLY if TOOL rows
   actually surface — see survey §2).
8. The ACTIVATE-AT-UNIFY e2e beats (written, annotated, unit-backed).

### Tier 3 — DEFERRED, loud (enumerate in the status log)

- **Pascal's Workbench SPA — the WHOLE of it** (`/custom-tools`
  vertical, `tool-draft` port, builder/outcomes/bench/destination
  picker, library, Scriptorium row action, Settings button, left-rail
  entry, deep links): **P4.6bb, next round.**
- The full composer gutter substrate (standing).
- Workspace-tab intents (v5 has no workspace tabs — `p4.9j` awaits a
  human ruling).
- v4's popup Workbench entries ("Open on…", "+ new", repair) — ride
  P4.6bb.

---

## The differential requirement (restated)

SPA-lane discipline: ng specs assert v4's EXACT behaviors (fresh-roster
per open; never render odds; the chip fallback `toolTitle ?? tool`; the
whisper helper case-for-case against v4's own test file; the settings
`?? true` default), the §4 wire mirror is diffed name-for-name against
lane AY's Rust unions at unification (the `p4_6ar_wire_contract`
precedent), and every mocked beat is written to ACTIVATE-AT-UNIFY.
`ng test` runs via the project harness (`[[p4.6e-settings-spa]]` — `ng
test`, not bare vitest); zoneless quirks per
`[[p4.6x-document-mode-spa]]`; editor-adjacent e2e drives
`.qt-rich-editor-content` (`[[p4.6ag-prosemirror-editor]]`). New selects
with async options bind `[selected]` per option, never `[value]`
(standing rule).

## Fixtures

Consumes lane AY's chat-custom-tools fixture instance at unification
(the Tools/-bearing mount tree its Fixtures section delivers) for the
live beats; until then, mocked transport in specs. No fixture deliveries
of its own beyond e2e seed helpers under `apps/web/e2e/support/`.

---

## Shared contract (binding)

> Reproduced verbatim and identically in all four orders of this round:
> `p4.d7-mount-nocase-namespace.md` (lane D7),
> `p4.6ay-pascal-custom-tools-server.md` (lane AY),
> `p4.6az-character-metadata-vault.md` (lane AZ),
> `p4.6ba-pascal-salon-spa.md` (lane BA).

### §1 — `fresh_schema.json` + the provision oracle (OWNER: lane D7)

Lane D7 owns `crates/quilltap-core/src/services/provisioning/` (the
`fresh_schema.json` re-dump at `d68638b4` — expected diff: EXACTLY the
two mount-index index statements) and `harness/oracle/provision/`.
**P4.6ay's former ownership of `harness/oracle/provision/` is RESCINDED**
(its unit 10 closed). No other lane touches provisioning or the provision
oracle this round. The sibling commits add NO columns (metadata.json is a
vault file, not a DB column; the Workbench adds no DDL) — if D7's re-dump
diff shows more than the two index lines, the ROUND premise is wrong:
STOP and report.

### §2 — `character.metadata` (OWNER: lane AZ — CONSUMER: lane AY)

The field: `metadata` on the HYDRATED character — a flat JSON object
(string keys, any JSON values). **No DB column** (the sole source of
truth is the vault file `metadata.json`); absent/unparseable hydrates
`{}` for a vault-linked character; `null`/absent and `{}` are equivalent.
Lane AZ delivers the hydration (read overlay), the guarded write
projection, the PUT arm, and the scaffold seed. Lane AY consumes it ONLY
through the character read path (the hydrated find-by-id surface) — it
must NOT parse vault files itself. Lane BA never reads it directly
(server DTOs only carry `pascalMeta.metadataTested`).

**Slippage rule:** if lane AZ slips the round, lane AY's handler/route
read `character.metadata` → absent → `{}` — which is v4's own
no-fact-sheet arm, so `run_custom` still lands and every metadata test
declines to the catch-all. Lane AY marks its metadata-HYDRATION
differential rows deferred; its execution-core rows (metadata injected
directly) are unaffected. The unifier does NOT withhold `run_custom` for
AZ slipping.

### §3 — the region-pinned shared file `crates/quilltap-core/src/db/character_vault.rs` (D7 ∥ AZ)

| Region | Owner |
|---|---|
| `ensure_character_vault` (:174) + `link_character_to_vault` (:284) + `create_character` (:325) — the unique-vault-name change ONLY | **D7** |
| `scaffold_character_mount` (:105) + the NEW `ensure_character_metadata_file` | **AZ** |

Neither lane may reformat, reorder, or re-wrap the file; both cite this
table in the commit message of any commit touching it. The unifier takes
both hunks. **Any other same-file collision anywhere in the round is a
planning bug — stop and report rather than resolving it in-lane.**

### §4 — the SPA wire contract (OWNER: lane AY — CONSUMER: lane BA)

- **Dispatch verbs** (added to the CoreRequest/CoreResponse unions on
  both sides, name-for-name):
  - `{ type: 'chatCustomToolsList', chatId }` → data
    `{ tools: CustomToolListing[], errors: CustomToolLoadError[], droppedForCap?: string[] }`
  - `{ type: 'chatCustomToolRun', chatId, tool, parameters?, private?, asCharacterId? }`
    → data `{ messages: MessageDto[], result: { tool, value, state, message, whispered } }`
  - The REST legs (`GET`/`POST /api/v1/chats/{id}/custom-tools`) land
    with them; the verbs are the SPA's path.
- `CustomToolListing` = v4 `route.ts:57` fields **plus `mountPointId`**
  (the d68638b4 addition): `{name, title, description, parameters,
  defaultVisibility, sourceTier, characterLabel?, asCharacterId,
  definitionPath, mountName, mountPointId}` — never the roll spec or the
  outcome table.
- **SSE:** the stream frame gains `pascalResult` carrying the posted
  Pascal message, beside `carinaAnswer` / `hostAnnouncement`.
- **`MessageDto`:** gains `pascalMeta`, transcribed field-for-field from
  v4 `lib/schemas/chat.types.ts:342-370` (incl. `toolTitle`,
  `invokedBy`, `metadataTested`); `systemSender` union gains
  `'pascal'`; `systemKind` values `'custom-tool-result'` /
  `'custom-tool-error'`. Both lanes transcribe from the SAME v4 lines;
  neither invents fields.
- **Settings:** the chat-settings key is `customTools` (boolean,
  `?? true`).
- **Slippage rule:** lane BA ships spec-verified components over mocked
  transport; live e2e beats ACTIVATE-AT-UNIFY. If lane AY slips, the
  unifier holds BA's composer entry points dark and enumerates them.

## Ownership (binding)

> Reproduced verbatim and identically in all four orders. `CHANGELOG` and
> `docs/developer/porting/status-log.md` are **append-only for every
> lane** and union-merge at unification (standing rule).

### Lane D7 — `p4.d7-mount-nocase-namespace.md`

- `crates/quilltap-core/src/db/`: `mount_index_case_repair.rs` **(new)**,
  `doc_mount_file_links.rs`, `doc_mount_folders.rs`,
  `doc_mount_points.rs`, `database_store.rs`, `ensure_official_store.rs`
- `crates/quilltap-core/src/services/`: `mount_index/` (whole dir),
  `builtin_mounts.rs`, `provisioning/` (per §1),
  `quilltap_import/seed.rs` + `seed_assets.rs` (the store-naming change
  only)
- `crates/quilltap-core/src/tools/doc_edit/file_management.rs`
- `crates/quilltap-core/src/api/`: `mount_points.rs`, `mount_files.rs`
- `crates/quilltap-host/src/host.rs` (the repair wiring only)
- `harness/oracle/provision/`; the mount/doc-mount/scriptorium oracle
  cases + fixtures + their `crates/quilltap-harness/tests/` counterparts;
  the new case-repair oracle family

### Lane AY — `p4.6ay-pascal-custom-tools-server.md`

- `crates/quilltap-core/src/pascal/` (all of it, incl. `dice.rs` now —
  the former lane-A split is over), `workbench.rs` **(new, under
  `pascal/`)**
- `crates/quilltap-core/src/tools/`: `run_custom.rs` **(new)**,
  `executor.rs`, `capabilities_report.rs`, `definitions/data.rs`
- `crates/quilltap-core/src/services/`: `pascal_writer.rs` **(new)**,
  `prospero_notifications.rs`, `tool_build.rs`, `tool_execution.rs`,
  `orchestrator.rs`, `chat_events.rs`
- `crates/quilltap-core/src/api/`: `salon.rs`, `settings.rs`, `mod.rs`,
  `engine.rs`, `types.rs` (the new verbs), the new custom-tools api
  module(s)
- `crates/quilltap-web/` (the custom-tools routes)
- `harness/oracle/cases/`: the `pascal-*` + `tool-definitions*` families;
  their `crates/quilltap-harness/tests/` counterparts; the new
  chat-custom-tools route + workbench families

### Lane AZ — `p4.6az-character-metadata-vault.md`

- `crates/quilltap-core/src/vault_overlay.rs`
- `crates/quilltap-core/src/db/`: `vault_read_overlay.rs`,
  `vault_character_write.rs`, `vault_character_update.rs`,
  `characters.rs`, `characters_read.rs` (if the survey shows the
  hydrated field needs it), `character_vault.rs` (per §3 — its region
  ONLY)
- `crates/quilltap-core/src/api/characters.rs`
- `crates/quilltap-core/src/services/quilltap_import/characters.rs`
- `harness/oracle/cases/` + `crates/quilltap-harness/tests/`: the
  `vault-*` and `characters-*` families it touches; the new
  metadata-roundtrip family

### Lane BA — `p4.6ba-pascal-salon-spa.md`

- `apps/web/` — everything it needs there (no other lane touches
  `apps/web/`)

### Version-bump ownership

Lanes D7, AY, AZ bump `quilltap-core` + `quilltap-harness` per their own
commits; lane AY additionally owns any `quilltap-web` bump; lane D7 owns
any `quilltap-host` bump; lane BA owns the SPA version. **Identical first
bumps off a shared base merge SILENTLY** — the unifier recounts
`base + total bumps` (`[[p4.6aw-ax-8-round-plan]]`); no lane may assume
its number survives.

---

## Verification gate (before declaring the lane done)

1. `ng test` — all green (run via the project harness, not bare
   vitest).
2. `ng build` — clean.
3. The full Playwright suite from `apps/web` — green; new beats present
   (annotated ACTIVATE-AT-UNIFY where server-dependent), zero
   unexplained skips.
4. The §4 wire mirror diffed name-for-name against this order's pinned
   shapes (the Rust-side diff happens at unification against lane AY).
5. Every Tier-3 item enumerated in the status log as a named deferral.
6. Per-commit rule: `.claude/commands/commit.md` (SPA version bump,
   CHANGELOG every commit, status-log append). CLAUDE.md is
   summary-only.

## Ground rules

The standing set: faithful ports, not improvisations (v4's behavior is
the spec — including what it deliberately does NOT show, e.g. odds);
keep v4's user-facing register for any new copy; no stubs or `TODO`
beyond the enumerated Tier-3 deferrals; touch nothing outside
`apps/web/`. **If a fresh survey contradicts this order, STOP and
report before proceeding.**
