# Work order P4.92 — the tool loops' `base_params` carry-over class, closed with evidence: `previous_response_id` (all three re-stream sites) and `stop` (the native loop's two) ride the PRIMARY's `StreamParams` into re-streams where v4's loops pass NEITHER — the orchestrator corpus made able to SEE both fields first (recorded side-channels, never the canned key; a fixture builder that can seed a `resp_` id; a SimpleJson seat), the arms RED against unported v5, then the caller-side strip at the three loop-option sites in P4.90's exact shape; the `cacheKey` carry MEASURED and dispositioned — from `main`

**Status: CLOSED at lane close (2026-09-16), branch
`claude/loop-params-carry-over-docs-91d876`.** Ordered 2026-09-16, the
`1fefadb9a` bug-147 drift catch-up + maintenance round (P4.D195 ∥ P4.91 ∥
P4.92 ∥ P4.93). The unifier rewrites this header at unification.

**Landed — Tier 1 complete.** The corpus can SEE both fields (oracle
side-channel recording, harness comparison per call, a presence guard + three
non-vacuity floors); the fixture builder seeds a `rawResponse`; THREE cases
(the order asked for two) — `openai_chained_then_native_tool_call`,
`simple_json_then_native_tool_call` and `simple_json_text_tool_continuation`;
the caller-side strip at all THREE `base_params:` sites; the `:2150` comment
and the P4.41 doc sentence. All 69 pre-existing canned rows proven
byte-identical across the regen with the two new keys stripped. Tier 2 items 8
and 9 landed.

**Refuted by measurement.** (a) The tool modes are NOT mutually exclusive —
v4 and v5 both run the native loop unconditionally, so the `stop` half is a
corpus case, not the unit pin the order allowed for. (b) The order's preferred
strip placement ("after the `:2894` rebuild, before the three clones") would
have REGRESSED the empty-response chain leg: `params` has a fourth consumer
the survey did not list, and v4 passes it `stop: initialStopSequences`. The
strip is on the three clones. (c) `textblock_mode` is a simple-json case, not
a text-block one.

**A named mutation SURVIVED and was fixed, not deleted.** M2 (delete the text
continuation's own `stop` override) left the family green: every row reaching
the text continuation was the ANTHROPIC text-block seat, whose strategy has no
stop sequences. Case C and its own floor close that.

**Tier 2 item 7 — the `cacheKey` carry: MEASURED and ESCALATED**, disposition
(b). It reaches the wire on six of eleven providers, v5's builders agree key
for key, and the gap is ONE production input (`orchestrator.rs`'s
`StreamParams` literal sends `None`). It is a PRIMARY-path wire divergence
needing the request-envelopes corpus, and matching v4 needs a clear on the
native force-final — loop-internal, which Tier 3 forbids here. The ordered
shape is in the lane record.

**Nothing under this order stays OPEN.** Two findings for the unifier: the
`cacheKey` escalation, and a live cross-lane `/tmp` collision (P4.93's sweep
regenerated this family into the recipe's shared output paths mid-lane) — both
in the lane record.

## Preamble

- **v4 oracle baseline: `2075242f9`.** This lane absorbs NO drift row — it
  is candidate 4 of the `2075242f9` round's "What is next" (phase-4.md
  `:6678-6684`), the P4.90 §3 review's lead, recorded verbatim in that
  round's record (`status-log.md:129009-129014`): "`previous_response_id`
  (and `stop`) ride `base_params` into both tool loops where v4's
  re-streams pass none (on an OPENAI primary with a `resp_` id in history
  v5 chains and P4.41's fallback re-issues; the identical class as the
  fix — wants a corpus arm)". v4's code is the spec at the BASELINE pin:
  `lib/services/chat-message/{primary-stream,native-tool-loop,text-tool-
  loop,streaming,provider-failover}.service.ts` (survey below — the four
  `streamMessage` option sets, diffed).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** ONE lane-unique pin **`/tmp/qt-v4-
  pin-p4.92-2075242f9`** (the baseline — v4's loop code is unchanged by
  `1fefadb9a`; verify with `git -C ~/source/quilltap-server diff --stat
  2075242f9..1fefadb9a -- lib/services/chat-message/` EMPTY and record
  it). Every `orchestrator_tier3` regen from the pin through the sweep
  driver.
- **Sibling lanes:** P4.D195 (bug 147 whole — the chat GET projection + the SPA seed + the help re-vendor), P4.91 (the photos-predicate consolidation + the collapse heal's census log shape), P4.93 (the harness + logging maintenance smalls). None touches `orchestrator.rs`, the two
  loop files, `streaming_provider.rs`, or the orchestrator family/oracle/
  fixture. P4.93's tool-role census sweeps every harness family EXCEPT
  `orchestrator_tier3_equivalence.rs` (yours — its mapper was already
  fixed by P4.90; §R.10).
- **Worktree:** `git worktree add <path> -b claude/p4-92-loop-params-
  carry-<hash> main`; `CARGO_INCREMENTAL=0`. No SPA.
- **Read before the work:** `crates/quilltap-core/src/services/
  orchestrator.rs:2150-2156` (`initial_stop_sequences` — "applied to the
  primary stream", says the comment; nothing enforces it), `:2723-2724`
  (`find_previous_response_id`), `:2787-2804` (the `StreamParams` literal
  — every field set), `:2875-2894` (P4.90's rebuild + its MEASURED
  paragraph — the shape and the comment style you extend), `:2987-3002`
  / `:3066-3080` / `:3111-3125` (the THREE `base_params: params.clone()`
  sites — the order's "two" was one short; there are two `run_text_tool_
  pass` calls); `native_tool_loop.rs:181,220,466-480,578-585` (both
  re-streams override ONLY `messages`); `text_tool_loop.rs:307,427,502,
  676-691` (`stream_continuation` overrides `messages`/`tools`/`web_search_
  enabled`/`stop` — `stop` already v4-faithful there); `model/streaming_
  provider.rs:159-186` (`request_input_from_stream_params` — `stop` `:169-
  174`, `previous_response_id` `:183`), `:315-424` (`stream_message` — the
  P4.41 fallback is PER CALL, gated on `params.previous_response_id.is_
  some()` at `:346`; unit pins `:1053-1095`, `:1435`); `model/request_
  builder/responses_api.rs:513-522` (the OPENAI wire arm);
  `primary_stream.rs:1414` (`find_previous_response_id` — the ONLY
  producer of a `Some`); the harness `crates/quilltap-harness/tests/
  orchestrator_tier3_equivalence.rs:31-40` (P4.90's header rationale),
  `:221-229` (the marker-mocked detector), `:241-271` (`CannedStreamW` —
  the recorded row: `provider, model, temperature, messages, tools,
  attachments, modelParams, sampling, sequences`), `:379` (the key),
  `:1240-1261` (P4.90's non-vacuity pin — the idiom for yours); the
  oracle `harness/oracle/cases/orchestrator-tier3.test.ts:275-304` (the
  `streamMessage` mock destructures NEITHER `previousResponseId` NOR
  `stop`; the key `provider|model|temperature|messages`); the fixture
  `harness/oracle/fixtures/orchestrator-tier3.json:36-79` (the four
  OPENAI profiles incl. `TextBlockModel` `o1-mini` as a PRIMARY seat on
  chat `f2000002…`, and `FailoverUnderstudy` `gpt-stands-in`), `:2314-
  2324` + `:3400-3455` (P4.90's case, streams, detection row) and `build-
  orchestrator-fixture.ts` (grep `rawResponse` → ZERO: the builder cannot
  seed one today); `crates/quilltap-core/src/model/stream.rs:512,571,606`
  (`canned_stream_key`); memory notes `v4-empty-response-failover-is-
  downstream-of-the-tool-loops`, `canned-key-role-catchall-hides-a-tool-
  role`, `a-canned-miss-that-prints-only-a-count`, `a-guard-whose-other-
  conjuncts-are-false-is-untested`, `heredoc-env-var-makes-a-mutation-
  vacuous`, `mutation-proof-revert-by-file-backup`.

## Mandate

Close the carry-over class P4.90 named and left. Make the orchestrator
corpus ABLE TO SEE the two fields first — the oracle's `streamMessage` mock
records `previousResponseId` and `stop` as side-channel fields on the
canned row (NEVER in the key: every pre-existing row stays byte-identical
and every old canned answer still matches), the Rust harness reads them
back into `CannedStreamW` and compares per re-stream, the fixture builder
learns to seed an assistant message carrying `rawResponse.id: "resp_…"`,
and two new cases pose the shapes: an OPENAI primary with a `resp_` id in
history whose reply calls a native tool (v4's re-stream carries NO
`previousResponseId`; unported v5's carries one and — because the canned
transport answers it — the arm reds on the recorded field, not on a miss),
and a SimpleJson-mode seat whose reply calls a text tool (v4's native-mode
re-stream has no `stop`; v5's native loop would carry `SIMPLE_JSON_STOP` if
that mode ever reached the native loop — MEASURE whether it can; if the two
modes are mutually exclusive in v5 as in v4, the `stop` half is a
`native_tool_loop`-side arm over the force-final re-stream with an
explicitly posed non-empty `initial_stop_sequences`, or a recorded
structural impossibility with the code path cited). Then the fix, in
P4.90's exact caller-side shape: strip `previous_response_id` (all three
loop-option sites) and `stop` (the native site) from the `params` handed
to the loops — one statement each after the `:2894` rebuild, a comment
naming v4's four call sites and which options each omits, the `:2150`
comment corrected to be true by construction — and re-run the arms GREEN.
Measure the `cacheKey` carry-over the survey found (v4 derives
`cacheKey = buildCharacterCacheKey(characterId)` per call and passes
`characterId` on the primary + the native re-stream but not the force-final
or the text continuation; v5 sends `cache_key: None` at all four) and
disposition it with evidence — a fix if it reaches any provider's wire, a
recorded no-op if the key is dead on every wire, an ESCALATION with the
measured shape if it is a wire-level divergence wider than one lane. This
lane does NOT touch the loops' internals beyond a test fixture, the
provider builders, `streaming_provider.rs`, or any sibling's files.

## Survey-verified starting points (fresh survey 2026-09-16 at `2075242f9` and on `main` `6e0ba5e8` — do not re-derive)

### v4 (at `2075242f9`; unchanged by `1fefadb9a`)

- **The ONE funnel** `streaming.service.ts:350-412` — options destructured
  at `:363` (`messages, connectionProfile, apiKey, modelParams, tools,
  useNativeWebSearch, userId, messageId, chatId, characterId,
  previousResponseId, stop`); `:393` `const cacheKey = buildCharacterCache
  Key(characterId)`; `:400-412` `provider.streamMessage({ messages, model:
  connectionProfile.modelName, temperature, maxTokens, topP, tools,
  webSearchEnabled, profileParameters: modelParams, cacheKey,
  previousResponseId, stop })`.
- **The four call sites and what each passes:**

  | option | primary `primary-stream.service.ts:197-210` | native re-stream `native-tool-loop.service.ts:340-350` | native force-final `:421-430` | text continuation `text-tool-loop.service.ts:390-400` |
  |---|---|---|---|---|
  | `previousResponseId` | yes | **no** | **no** | **no** |
  | `stop` | yes | **no** | **no** | yes (`strategy.stopSequences`) |
  | `characterId` (→ `cacheKey`) | yes | yes | **no** | **no** |
  | `tools` | `actualTools` | `actualTools` | `actualTools` | `continuationTools` |
  | `useNativeWebSearch` | as-is | as-is | as-is | `continuationUseNativeWebSearch` |
  | `modelParams` | one bag | same | same | same |

- `provider-failover.service.ts:464` — the explicit refusal: "there is
  deliberately no `previousResponseId` here: it is an OpenAI Responses-API
  chaining token, and handing it to a different account — never mind a
  different provider — is meaningless at best." Carry this why into the
  strip's comment.
- `orchestrator.service.ts:1455,1473` — `findPreviousResponseId` computed
  once and passed into `runPrimaryStream` ONLY. `primary-stream.service.
  ts:419` defines it (OPENAI only; newest-first assistant `rawResponse.id`
  starting `resp_`).

### v5 (on `main` `6e0ba5e8`)

- `orchestrator.rs:2787-2804` — `let mut params = StreamParams { messages,
  model, temperature, max_tokens, top_p, tools, web_search_enabled,
  profile_parameters, cache_key: None, previous_response_id, stop:
  initial_stop_sequences.clone(), request_timeout_ms: None }`;
  `previous_response_id` from `:2723` (`primary_stream::find_previous_
  response_id(&effective_profile.provider, &existing_messages)` — the ONLY
  `Some` producer in the tree; every other constructor hard-codes `None`);
  `stop` from `:2150-2156` (`SIMPLE_JSON_STOP` iff `resolved_tool_mode ==
  SimpleJson`, else empty).
- `:2894` `params.model = effective_profile.model_name.clone();` — P4.90's
  rebuild; its MEASURED paragraph (`:2885-2893`) explains why the
  `modelParams`-derived fields stay stale. It says NOTHING about the two
  fields here because they are not `modelParams`-derived — they are
  primary-only OPTIONS on v4's side.
- The three hand-offs: `:3002`, `:3080`, `:3125` — `base_params: params.
  clone()`. Native loop `:466-480` and `:578-585`: `let mut params =
  base_params.clone(); params.messages = …` — everything else rides. Text
  loop `:676-680`: overrides `messages`, `tools`, `web_search_enabled`,
  `stop` (= `strategy.stop_sequences()`, v4-faithful) — `previous_response_
  id` rides.
- The wire: `streaming_provider.rs:183` copies `previous_response_id` into
  the request input; `responses_api.rs:513-522` emits `previous_response_
  id` for OPENAI and switches the body to the CHAINED shape (`input:
  lastUser`) — so a loop re-stream that chains also DROPS the tool-result
  history from the body (the deeper consequence: the model never sees the
  tool result it asked for unless the chained response holds it server-
  side). Record this in the arm's rationale.
- The P4.41 fallback `:339-424` is per call → one dead request per loop
  iteration on a `previous_response_not_found`.
- **The corpus is BLIND to both** — the key is `provider|model|temperature|
  messages` (`orchestrator-tier3.test.ts:304`; `stream.rs:512`); the
  oracle's mock (`:277-281`) never reads `previousResponseId`/`stop`; the
  recorded row (`CannedStreamW` `:241-271`) has no slot for either; `grep
  -c stop` in both files → 0; P4.41's own record says so (`status-log.md:
  63665-63666`). No fixture message has a `rawResponse` (`grep resp_
  orchestrator-tier3.json` → 0) and `build-orchestrator-fixture.ts` cannot
  seed one; no case resolves `SimpleJson`, so `initial_stop_sequences` is
  always empty in the corpus. Both halves are structurally unreachable
  today — the corpus widening is the FIRST deliverable.
- OPENAI seats available: `TextBlockModel` `e0000005…` (`o1-mini`) is a
  PRIMARY seat on chat `f2000002…` (participant `f20a0002…`, driven by the
  `textblock_mode` case `:1928-1938`); `FailoverUnderstudy` `gpt-stands-
  in`; `Courier` `gpt-4o`; `CheapDefault`. A new OPENAI primary seat with
  native tool mode is the cleanest (add a profile + chat + participant in
  the fixture JSON, the P4.90 additions as the template).
- `cache_key: None` at `orchestrator.rs:2798`, `primary_stream.rs:1536`,
  `native_tool_loop.rs:730`, `text_tool_loop.rs:799` (the last two are
  test fixtures). Whether v4's `cacheKey` reaches any wire is NOT known
  from this survey — MEASURE (Tier 2 item 7).

## Tiered deliverables

### Tier 1 — must land

1. **The corpus can see the fields.** Oracle: the `streamMessage` mock
   destructures `previousResponseId` and `stop` and records them on the
   canned row as `previousResponseId: string|null` and `stop: string[]`
   (side-channel, NOT in the key). Rust: `CannedStreamW` gains the two
   fields (serde `default` so old NDJSONs parse); the per-re-stream
   comparison asserts v5's `StreamParams.previous_response_id`/`stop` at
   each canned lookup equal the recorded row's (the canned transport
   already sees the request — add the two fields to whatever it records
   per call; if it records nothing today, record them the way `a-canned-
   miss-that-prints-only-a-count` taught: the whole request head). Regen
   the WHOLE family at the pin: every pre-existing row byte-identical
   apart from the two new keys (diff the old and new NDJSON with the two
   keys stripped — `jq del(.previousResponseId,.stop)` both sides, `cmp`).
2. **The fixture builder seeds a `rawResponse.id`.** Extend `build-
   orchestrator-fixture.ts` so a seeded ASSISTANT message can carry
   `rawResponse: { id: "resp_…" }` (v4's `findPreviousResponseId` reads
   `rawResponse.id`; measure the exact column/JSON path v4 persists it
   under — `chat_messages.rawResponse`? — from the reader at `primary-
   stream.service.ts:419-440`, and seed THAT). Rebuild the committed
   fixture pair per the family's recipe; every existing case green on the
   rebuilt pair before any new case is added (the silent-stale-pass
   discipline: `rm -f` the pair first, read the builder's last line).
3. **Case A — `openai_chained_then_native_tool_call`:** a NEW OPENAI
   primary seat (native tool mode) on a chat whose history holds an
   assistant message with `rawResponse.id: "resp_prior"`; the primary's
   canned reply calls a native tool (P4.90's `read_conversation` +
   detection-marker idiom); the re-stream's canned answer is a plain reply.
   RED-FIRST on unported v5: the recorded re-stream row shows
   `previousResponseId: null` (v4) while v5's re-stream request carried
   `Some("resp_prior")` — and ALSO the primary row shows `"resp_prior"` on
   BOTH sides (the primary DOES chain — the positive control that proves
   the recording is live). Non-vacuity pin in P4.90's `:1240-1261` idiom:
   the oracle recorded an OPENAI primary row WITH a `previousResponseId`
   AND a re-stream row carrying a `tool` message.
4. **Case B — the `stop` half.** MEASURE first: can `resolved_tool_mode ==
   SimpleJson` ever reach the native loop in v5 (`orchestrator.rs` — the
   mode decides WHICH loop runs)? In v4 the native loop runs only under
   native mode, whose `stop` is empty, so v4's omission is invisible on
   the wire unless the modes can mix. If v5 mirrors that exclusivity, the
   `stop` carry is dead-by-construction on the native site: land the strip
   anyway (v4's shape; it costs one statement) and pin it with a UNIT test
   in `orchestrator.rs`'s or `native_tool_loop.rs`'s test module that
   hands a `base_params` with a non-empty `stop` and asserts the re-stream
   request carries none — plus a doc comment recording the measured
   exclusivity. If the modes CAN mix, pose it as a corpus case instead
   (a SimpleJson seat whose canned reply is a text tool block followed by
   a native call) and red-first it like Case A.
5. **The fix.** After `:2894`, in P4.90's shape: `params.previous_response_
   id = None;` and `params.stop = Vec::new();` — with a comment naming the
   four v4 call sites and the omission table above, v4's `provider-
   failover.service.ts:464` why, and the chained-body consequence
   (`responses_api.rs:513-522`). ⚠ The PRIMARY stream must still receive
   both — measure where `params` is consumed by the primary (`:2810-2870`)
   vs handed to the loops (`:3002+`): the strip goes AFTER the primary's
   use and BEFORE the three clones, or on the three clones themselves if
   the primary reads `params` again after them (say which and why). The
   `:2150` comment ("applied to the primary stream") becomes true — reword
   it to say the strip makes it so. Text loop `:680` keeps overriding
   `stop` (v4-faithful) — unchanged. Case A green; Case B's pin green.
   Mutation proofs: M1 revert the `previous_response_id` strip → Case A
   red on the re-stream row; M2 strip `stop` on the TEXT path too (delete
   `:680`'s override) → the existing text-tool cases red (v4 passes
   `strategy.stopSequences`); M3 strip before the primary's use → Case A
   red on the PRIMARY row (the positive control); M4 (Case B unit) hand a
   non-empty `stop` through the unstripped path → red.
6. **P4.41's fallback pins re-read** (`streaming_provider.rs:1053-1095`,
   `:1435`): unchanged — they pin the PRIMARY's chaining. Add one sentence
   to the P4.41 doc block at `:339` naming this order: the loops no longer
   reach this arm.

### Tier 2 — should land

7. **The `cacheKey` carry, measured and dispositioned.** v4 computes
   `cacheKey = buildCharacterCacheKey(characterId)` at the funnel and
   passes `characterId` on the primary + native re-stream only. Trace
   `cacheKey` through v4's plugins (`plugins/dist/*/` + `lib/providers/`):
   which provider bodies carry it and under what wire key (OPENAI
   `prompt_cache_key`? Anthropic none? NanoGPT?). Then v5: `cache_key:
   None` everywhere vs the request builders' consumers of `cache_key`
   (grep `cache_key` in `model/request_builder/`). Disposition: (a) no
   provider ever writes it to the wire → a recorded no-op with the grep
   evidence; (b) some provider does → a pre-existing PRIMARY-path
   divergence too wide for a loop-params lane (it needs the request-
   envelopes corpus): ESCALATE with the measured wire shape and the
   affected providers, in the lane record + this header, ordered next.
   Do not fix (b) here.
8. **The recorded-row widening's own guard:** a harness assertion that
   EVERY canned row in the fresh NDJSON carries the two new keys (a regen
   from a stale oracle case would silently drop them and every arm would
   read `None == None`) — the `a-case-added-only-to-the-oracle-is-never-
   run` class.
9. **Neutrality:** `primary_stream_tier3_equivalence` and `chat_create_
   capstone` by name at the pin (they share the canned-stream machinery;
   expect green with zero row change).

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **No loop-internal change.** The strip is caller-side (P4.90's ruled
  shape); `native_tool_loop.rs`/`text_tool_loop.rs` production zones
  unmoved (tripwire: `git diff main...HEAD -- crates/quilltap-core/src/
  services/native_tool_loop.rs crates/quilltap-core/src/services/text_
  tool_loop.rs` shows only `mod tests` hunks, if any).
- **The `characterId`-per-call asymmetry** (v4 passes it on two of four
  sites; it feeds `cacheKey` and the `llm_logs` `characterId` column) —
  the logging half is P4.D97/P4.79 territory and already differential-
  pinned; the cache half is item 7. Nothing else to defer.
- **The live 💸 proof** — a real OPENAI chained conversation (a `resp_`
  in history) with a native tool call on the Friday copy, `combined.log`
  showing ONE chained primary and ZERO `Conversation chaining failed`
  lines on the loop re-stream — banked for the dogfood pass.

## The differential requirement, restated

The strip ships with the orchestrator tier-3 family (v4's REAL
orchestrator + loops under the mocked provider, the P4.90 recipe) made
able to SEE the two fields (recorded side-channels, never the key), the
fixture builder able to seed the posing history, and Case A run RED
against unported v5 first — the primary row as the positive control that
the recording is live. The `stop` half lands as a corpus case or as a
unit pin with the measured exclusivity recorded, whichever the
measurement selects. Every pre-existing canned row is proven byte-
identical (keys stripped) across the regen. Mutation proofs as named;
a proof that survives as written is a finding.

## Fixtures

- **Consumes and REBUILDS:** `harness/oracle/fixtures/orchestrator-
  tier3.json` (+ the new profile/chat/participant/messages/case/streams/
  detection rows) and the fixture pair `build-orchestrator-fixture.ts`
  produces (per the family's recipe — the pair's path is in the header;
  `rm -f` before every build). No sibling reads this family's fixture.
- **Delivers:** the widened oracle case + harness; the rebuilt pair.
- **Owns regenerating:** `orchestrator_tier3_equivalence` (pin); the two
  neutrality legs (pin).

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `1fefadb9a` bug-147 drift catch-up + maintenance round:
**P4.D195 ∥ P4.91 ∥ P4.92 ∥ P4.93**, ordered 2026-09-16. Four lanes, four
worktrees, four lane branches, ALL from `main` (no stacking — every lane's
ownership is disjoint by file, so nothing needs a substrate commit). The
unifier cherry-picks in the order P4.D195 → P4.91 → P4.92 → P4.93 (the drift
row first; the harness-wide role-mapper sweep and the env-var rename last,
because they touch the most files and change the gate's env block), gates
ONCE, and `merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`2075242f9`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`1fefadb9a`**
  (`4.10.0-dev.41`, 2026-09-16 06:57) — ONE commit past the baseline, the
  bug-147 code commit, the ledger's one §3 row, now `ORDERED(P4.D195)`.
  v4 `bugfix` tip `1a2b2164c` and `release` tip `8fbf2afe0` are unmoved;
  there is still no `release: 4.10.0` squash. The checkout sat on `main`,
  CLEAN, at the planning probe (2026-09-16; the §2 probe PASSED, so the
  ledger stood and nothing was re-derived). **At unification the baseline
  MOVES to `1fefadb9a`** and the row retires ABSORBED(P4.D195).
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log 1fefadb9a..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Any mismatch — a moved HEAD, a dirty `lib/`/`app/`/`packages/`
  /`plugins/`/`migrations/`/`help/` tree, the checkout off `main` — is a
  STOP: report it and do nothing else.** A lane never writes the ledger.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1). HEAD is past the
  baseline and the commit touches `app/` and `help/`. Every regen runs
  from a LANE-UNIQUE detached worktree per ledger §5.1 (`git -C ~/source/
  quilltap-server worktree add --detach /tmp/qt-v4-pin-<order>-<sha>
  <sha>`, the THREE symlink classes, `cd` into it; never stash or
  checkout-switch the human's repo; `git worktree list` before each batch;
  verify the pin by grepping the fresh NDJSON for a marker only one tree
  has; check the file is non-empty before believing a diff). Which sha:
  - **P4.D195 pins the TARGET `1fefadb9a`** for every family its port
    MOVES (`salon_reads_equivalence`, the `help_*` content families, any
    turn-manager rows it adds) and the BASELINE `2075242f9` for its
    neutrality legs;
  - **P4.91, P4.92 and P4.93 pin the BASELINE `2075242f9`** — their v4
    surfaces are untouched by `1fefadb9a` (each lane records `git -C
    ~/source/quilltap-server diff --stat 2075242f9..1fefadb9a -- <its v4
    paths>` EMPTY in its lane record before the first regen). A pin at
    the baseline is still REQUIRED, not optional: the checkout can go
    dirty mid-lane (ledger §5.1), and a bare-checkout regen is
    unrepeatable.
  The sweep driver is the sanctioned per-family path
  (`harness/tools/recipe_sweep.py --run <family> --v4 "$PIN" --v5w
  <ABSOLUTE worktree>`); never run two sweeps concurrently; a family that
  reports `SKIP` in 0.00 s did not run (`a-family-env-var-is-not-its-
  regen-var`). Ledger §5.2's silent-stale-pass discipline applies to
  every regen (`rm -f` the fixture first; read the builder's last line;
  `grep -c` the changed bytes in the fresh NDJSON — MUST be > 0).
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). The
  spec for every item is `git show <sha> -- <path>` plus `git show
  <sha>:<path>`; a commit message describes the bug as its author
  understood it and the hunks are often narrower. Re-verify even where
  this order already flags the trap — the order's paragraph is the same
  kind of prose. Carry a v5-side code comment naming the sha where the
  prose misleads.
- **§R.5 — Convergence and designed trips: measure, don't assume**
  (ledger §5.4). The P4.D171 chat-GET omission pin (`salon_reads_
  equivalence`'s `get_*` cases) trips at the target pin BY DESIGN; dump
  v4's actual post-fix bodies before porting, and expect every `get`
  case to move together. The `state.cycleOrder` client read is a
  convergence measured with a table, not asserted.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest`
  is shared) and its silence leg; a fixture change lands with the regen
  of every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that
  SURVIVES as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`).
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed (and `apps/web/package.json` when it touches the SPA) on each
  commit per `.claude/commands/commit.md`; the unifier RECOUNTS as base +
  the sum of every lane's bumps (the silent-auto-merge trap: identical
  bumps merge as ONE — it fired again last round). Expected: core is
  bumped by ALL FOUR (P4.D195 `api/salon.rs`; P4.91 `photos/`, `db/`;
  P4.92 `services/orchestrator.rs`; P4.93 `model/completion.rs`, the two
  avatar job files, `db/memories.rs`); harness by ALL FOUR; web by P4.91
  ONLY (the `log_file.rs` visitor, Tier 2) — no other lane touches
  `crates/quilltap-web/src`; the SPA by P4.D195 ONLY; host/cli/tauri by
  NOBODY (a `help/**` re-vendor changes the embedded table `quilltap-
  host`'s `build.rs` produces but no host SOURCE — the P4.D194 precedent:
  no host bump for help alone).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  P4.D195 pre-lists the paths the target moves (with byte counts at the
  pin) in its lane record; no lane copies them.
- **§R.10 — Cross-lane handoffs, named:** (a) P4.93 RENAMES the guards
  family's fixture var (`QT_FIXTURE_HELP_MAIN` → a family-specific name)
  in `help_doc_sync_guards_equivalence.rs`; P4.D195 regenerates and RUNS
  the `help_*` content families under TODAY's names (it edits none of
  them) — the unifier re-runs both help-sync families with the renamed
  block after the pick. (b) P4.93's role-mapper sweep touches every
  hand-rolled `role` match in `crates/quilltap-harness/tests/` EXCEPT
  `orchestrator_tier3_equivalence.rs` (P4.92's file — its mapper already
  carries the `tool` arm); if P4.93 lands a shared inverse in
  `model/completion.rs`, the unifier repoints P4.92's mapper onto it at
  the wire (one line, recorded), never a lane. (c) P4.92 RUNS
  `chat_create_capstone` and `primary_stream_tier3` as neutrality legs
  while P4.93 EDITS their mappers — run vs edit is not a collision; the
  unified gate re-runs both. (d) P4.91 owns `db/avatar_rolls_collapse_
  heal.rs` WHOLE (the import repoint, the `:71-83` comment, and
  `report_census`); P4.93's avatar log lines live in `services/character_
  avatar_job.rs` + `services/image_job_common.rs` — different files.
  (e) P4.D195's Tier 2 item 8 may add `#[cfg(test)]` rows to `select_
  speaker.rs` / `turn_state.rs` or corpus rows to their families; no
  sibling touches either. (f) The `to_completion_messages`-class fix in
  P4.93 must leave every family's canned KEY bytes unchanged — a mapper
  that keys a role differently would silently miss; P4.93 proves each
  edited family green on its pin-fresh oracle by name.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D195** — bug 147 whole (server projection + SPA seed + help) | `crates/quilltap-core/src/api/salon.rs` — ONLY the `chat_get` projection block (`:355-390`); `crates/quilltap-harness/tests/salon_reads_equivalence.rs`; `harness/oracle/cases/salon-reads.test.ts`; `help/chat-turn-manager.md`; `crates/quilltap-harness/tests/help_tree_embed_guard.rs` — the doc-comment vendor list ONLY; `crates/quilltap-core/src/select_speaker.rs` + `turn_state.rs` — `#[cfg(test)]` modules ONLY (Tier 2 item 8), or instead `harness/oracle/cases/select-speaker*.ts` / `turn-state*.ts` + their two harness families (corpus rows only); `apps/web/src/app/core/core-contract.ts` — the ONE new key + its doc beside `cycleOrderParticipantIds`; `apps/web/src/app/chat/turn-order.ts` + `turn-order.spec.ts`; `apps/web/src/app/screens/salon/salon-conversation.ts` — ONLY `_turnEffect`, `refreshTurn`, `applyTurnResponse` and their doc comments; `salon-conversation.spec.ts`; `apps/web/src/app/chat/sidebar/chat-sidebar.spec.ts` (a wiring spec); `apps/web/e2e/salon-sidebar-flow.spec.ts`; `apps/web/package.json` version; this order's header | every other `crates/**` file (incl. the server turn handlers, `chats_read.rs`, `db/**`), the salon fixture PAIR on disk (plants are per-case copies), every other SPA file, `help/**` beyond the one page, the drift ledger |
| **P4.91** — the photos-predicate consolidation + the collapse heal's census log shape | `crates/quilltap-core/src/photos/photos_paths.rs`; `crates/quilltap-core/src/db/doc_mount_file_links.rs` — ONLY the deletion of the `is_photos_relative_path` twin + its private `posix_dirname` (`:332-388`); the SIX importers, import-line edits ONLY: `api/chat_media.rs:1716`, `photos/photo_link_summary.rs:13`, `photos/character_gallery_service.rs:17`, `services/maintenance.rs:32` + `:375` (and its `photos_relative_path_predicate` test if consolidated); `crates/quilltap-core/src/db/avatar_rolls_collapse_heal.rs` — the import + the `:71-83` justification comment + `report_census` (`:535-552`) + its tests; `crates/quilltap-web/src/log_file.rs` — the `FieldVisitor::put` JSON-field convention ONLY (Tier 2) + its tests; `harness/oracle/cases/photos-relative-path.ts`; `crates/quilltap-harness/tests/photos_relative_path_equivalence.rs`; this order's header | every other `crates/**` file (P4.93's avatar job files, `services/route_trail.rs`), `apps/web/**`, `help/**`, every other harness family, the drift ledger |
| **P4.92** — the loops' `base_params` carry-over class | `crates/quilltap-core/src/services/orchestrator.rs` — ONLY the strip after the `:2894` rebuild (or on the three `base_params:` clones at `:3002`/`:3080`/`:3125` — say which) + the `:2150` comment; `crates/quilltap-core/src/model/streaming_provider.rs` — the P4.41 doc block at `:339` ONLY (one sentence); `crates/quilltap-core/src/services/native_tool_loop.rs` + `text_tool_loop.rs` — `#[cfg(test)]` modules ONLY (Case B's unit pin); `crates/quilltap-harness/tests/orchestrator_tier3_equivalence.rs`; `harness/oracle/cases/orchestrator-tier3.test.ts`; `harness/oracle/fixtures/orchestrator-tier3.json` + `build-orchestrator-fixture.ts` + the fixture pair that builder produces; this order's header | every other `crates/**` file (the request builders, `primary_stream.rs`, `provider_failover.rs`), `apps/web/**`, `help/**`, every other harness family (P4.92 RUNS `chat_create_capstone` / `primary_stream_tier3` — never edits them), the drift ledger |
| **P4.93** — the harness + logging maintenance smalls | `crates/quilltap-harness/tests/help_doc_sync_guards_equivalence.rs` + `help_doc_sync_equivalence.rs` (headers + the env var + the fixture-sensitive arm) + `harness/oracle/cases/help-sync-guards.ts` (+ `help-sync-tier2.ts` only if the arm needs an oracle column); `crates/quilltap-core/src/model/completion.rs` — the `CompletionRole` inverse (`from_v4_wire`/`FromStr`) + tests; every hand-rolled role-mapper family in `crates/quilltap-harness/tests/` named in its survey (Class 1 + Class 2 + `external_prompt_tier3_equivalence.rs` + `file_attachment_tier3_equivalence.rs`) EXCEPT `orchestrator_tier3_equivalence.rs`; `crates/quilltap-core/src/db/memories.rs` — the ONE test's capture rig ONLY; `crates/quilltap-core/src/services/character_avatar_job.rs` + `services/image_job_common.rs` — the `[CharacterAvatar]` log lines + their capture tests ONLY; `crates/quilltap-harness/tests/avatar_job_tier3_equivalence.rs` — ONLY if a line-count pin must widen; `harness/tools/recipe_sweep.py` — ONLY if the env rename needs a driver-side alias (say so); this order's header | `orchestrator_tier3_equivalence.rs` (P4.92's), `salon_reads_equivalence.rs` + the help EMBED guard (P4.D195's), `db/avatar_rolls_collapse_heal.rs` (P4.91's), `photos/**`, `apps/web/**`, `help/**`, `crates/quilltap-web/src/**`, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0),
   and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp`) where the family is deterministic across
   regens; where it is not (`regenerate-at-both-pins-and-cmp-is-not-
   universal`), run by name and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast` with the lane's env block
   (its own oracle vars + `QT_V4_ROOT`/`QT_V4_CHECKOUT` at the right pin +
   `QT_NODE` = the node BINARY): report **binaries / passed / failed /
   ignored** from the per-binary lines and the exit code, and the lane's
   families confirmed RUN by non-zero duration. No family is withheld
   this round (P4.89 restored the brahma pair).
9. **SPA lanes only (P4.D195):** `npm run lint` (incl. the qt-class guard),
   `npm test` (spec files / tests / 0 failed), `npm run build` clean, then
   the lane's e2e beats by FILE against the release binary, and ONE full
   Playwright run at lane close (numbers recorded; the documented
   intermittents — the P4.D161 pause-toast beats, the P4.d17 quill beat,
   the `salon-documents-flow` / `workspace-flow` terminal trio — are
   named, re-run by file, and never re-classified as this lane's).
10. **Ownership check:** `git diff --stat main...HEAD` — every path is in
    the lane's Owns column; every MUST-NOT-TOUCH path absent.
11. **Per commit:** `.claude/commands/commit.md` in full — the equivalence
    test is non-negotiable (§1), the invariants (§2), real-instance safety
    (§3), the harness regen (§4), build/test/lint (§5), the CHANGELOG
    entry + version bumps (§7). Commit on the lane branch only; never
    merge to `main`; never `git stash`; never a worktree inside a worktree.
12. **Lane close:** update this order's status header (what landed, what
    was refuted by measurement, what stays OPEN with its named cause),
    append the lane record to `status-log.md` (the regen commands as run,
    the counts, the mutation table, every finding outside the lane's
    ownership for the unifier), and stop. The final gate runs in the
    FOREGROUND of the lane's last turn — a backgrounded gate whose
    notification arrives after the lane reports done is a lane that is
    NOT done (`a-backgrounded-sleep-does-not-gate-your-turn`).
