# Work order P4.84 — the SPA follow-ups the `p4.9k` round recorded: the wizard review pane's three missing renders, the continue-mode toasts, `runTemplateSave` onto the shared apply helper, the AI-import wizard's fixed title, the three K4 divergences run to ground, and the interceptor's nested-button census

**Status (unification, 2026-09-07 — the generator follow-ups + prompt-templates
round): CLOSED — UNIFIED on main; nothing OPEN.** All six Tier-1 items landed;
both Tier-2 items landed or discharged by measurement (the `isPaused` continue
guard MEASURED and PINNED, deliberately not ported — the one reachable path is
named). Two order premises refuted by the lane: v4 has no standalone
`AIImportWizard` mount (both wrappers carry the fixed title, so the fix is at
the dialog with no `[title]` input), and the K3 note's "no chat-independent
renderer" claim is wrong twice over — v5 has two, and neither is reusable
because v4's wizard chain carries no plugins at all (a third, bare pipeline).
**The order's author RATIFIES at unification the lane's Tier-3 reading of the
`qtap:` hrefs** (left on a plain anchor — no v5 surface rewrites them; the
`isQtapUri` case-sensitivity lead in `chat/render/qtap-linkify.ts` stands as a
follow-up). **The §3 unification review fixed three should-fixes on the unify
branch:** the optimizer's stream-end arm was gated on `loading` where v4's
`:233-238` runs after EVERY stream end — `settleStreamEnd` is v4's
unconditional arm, pinned for the error-after-suggestions shape (real: the
suggestions-file vault-write failure) and for v4's shipped quirk that a
suggestions-file `done` carrying suggestions ends at `review` over the
file-written pane (reproduced; a v4 filing candidate); the AI-import step
heading takes v4's `qt-heading-2` (the file's other `qt-section-title` headings
are a pre-existing convention, recorded); and the lane record's claim that the
old inline fan-out "never dispatched `characterPromptUpdate`" was false and its
mutation proof vacuous — replaced by the one discriminating pin (an empty error
message takes the helper's fixed sentence). Lane record: `status-log.md` →
"Lane record — P4.84".

## Preamble

- **v4 oracle baseline: `2f4254b42`** (drift-ledger §1); **zero drift**.
  Every item here is a "recorded, not changed" row of the `p4.9k` round
  record's §3 (`status-log.md` → "Round record — the `p4.9k` character-
  generators round unification") or a K3/K4 lane-record divergence, and
  phase-4.md's candidate 3 names them.
- **Freshness probe at lane start** (ledger §2): checkout on **`main`**,
  tree clean, both log ranges EMPTY. **Any mismatch: STOP and report.** No
  Rust, no oracle regen in this lane — the probe still runs (a v4 client
  file this lane transcribes may have moved).
- **Regen rule: NO PIN REQUIRED** (nothing to regenerate here; parity specs
  transcribe v4's client code at the baseline — cite `file:line` at the
  pin in every spec header).
- **Sibling lanes:** P4.83 is the OTHER SPA lane — it owns
  `prompts-editor/import-modal.ts`, `edit/system-prompts-tab.ts`,
  `new/new-character.ts` and `core-contract.ts` stays frozen for both;
  the three Rust lanes never touch `apps/web/**`.
- **Worktree note:** own worktree + lane branch
  (`claude/p4-84-spa-generator-smalls-<hash>` from `main`); `npm ci`; ONE
  Playwright at a time — run only your own spec files by name.

## Mandate

Close the SPA-side divergences the generator round left recorded, each
against v4's client code at the baseline, each spec-pinned, with the
existing generator e2e beats extended where a render is browser-visible.
Deliver: (1) the wizard review pane's three missing renders — the
"Written as:" example above every hinted field, the physical-description
tier panel, and scenarios as title + pre-wrapped content — plus the
`fullDescription` Markdown preview through a plain (non-Salon) pipeline,
refuting the K3 note's premise that v5 has no chat-independent renderer
(it has two: `almanack/almanack-markdown.ts`, `help/help-doc-markdown.ts`);
(2) `useSSEStreaming`'s two continue-mode error toasts at v5's continue
entrance; (3) `runTemplateSave` lifted onto the shared
`applyCharacterFieldUpdates` as v4's `useCharacterView` does; (4) the
"Summon From Lore" fixed title on the Salon mount; (5) the three K4
divergences the round record names only by phrase — located against v4,
then ported or recorded with evidence; (6) a census spec for the link
interceptor's nested-button rule. What this lane must NOT touch: any Rust;
P4.83's three SPA files; `core-contract.ts`; the workspace shell/contract.

## Survey-verified starting points (2026-09-07 @ v4 `2f4254b42` — do not re-derive)

### 1. The wizard review pane (`components/characters/ai-wizard/steps/GenerationStep.tsx`)

- The expanded row (`:330-385`): `hintKey = FIELD_HINT_KEYS[field]`; `hint
  = hintKey ? PROMPT_FIELD_HINTS[hintKey] : undefined`; `voiceExample =
  hint?.example`; on expand with content: `{voiceExample &&
  <PromptFieldExample example={voiceExample} />}` THEN
  `renderFieldPreview(field)`. v5's `generation-step.ts:232-247` records the
  three missing renders verbatim; the "Written as: <em>…</em>" line exists
  in `ui/prompt-field-label.ts:59,80` (the P4.D104 shared label) and is
  DUPLICATED at `edit/scenario-editor.ts:72` — extract ONE
  `qt-prompt-field-example` (or make the label's example line standalone)
  and adopt it in all three places (the scenario editor edit is allowed
  ONLY for that adoption). Measure v4's `FIELD_HINT_KEYS` table + the
  hints' `example` strings — the SPA's twelve-key hints table is already
  byte-identical to v4's module (P4.D104); the field→hint-key map is the
  transcription to add.
- `renderFieldPreview` (`:100-170`): for `physicalDescription` —
  `:103-148`: a collapsed Short-prompt teaser, and expanded: `Short (N
  chars)` / `Medium (N chars)` / `Long (N chars)` / `Complete (N chars)`
  each `<strong>` + `<p class="qt-text-secondary">`, N = `.length` (UTF-16
  units), then `Full Description:` through `<ReactMarkdown components={{a}}>`
  inside `prose prose-sm qt-prose-auto max-w-none mt-1` — the `a` override
  renders `QtapLink` when `isQtapUri(href)` else a plain `<a>`. **NOTE:
  no `remarkPlugins`** — bare ReactMarkdown = CommonMark only, NO GFM (the
  Almanack pipeline adds GFM; the help pipeline adds GFM + math — neither
  is this one). Build `generators/wizard/generation-preview-markdown.ts`:
  `remark-parse → remark-rehype → rehype-stringify` (no GFM) + a rehype
  pass turning `qtap:` hrefs into the SPA's qtap-link shape
  (`chat/render/qtap-linkify.ts` holds the `isQtapUri` twin — reuse, do
  not re-transcribe); raw HTML dropped (the ReactMarkdown default, the
  safety property the two existing pipelines document). Transcribe the
  exact label strings and class lists.
- Scenarios (`:151-163`): `normalizeGeneratedScenarios(generatedData.
  scenarios)` → `<strong>{s.title}</strong>` + `<p class="qt-text-secondary
  mt-0.5 whitespace-pre-wrap">{s.content}</p>` per scenario; v5 renders
  the `**title**\ncontent` join as plain text (`getFieldContent`'s
  scenarios branch, `:54-58`, is the COLLAPSED teaser on both sides —
  keep it).
- Parity source: does v4 have `steps/__tests__/GenerationStep.test.tsx`?
  Measure; if yes, its assertions become the spec's; if not, pin the
  three renders over a recorded `generatedData` fixture with
  `wizard/fixtures/` as the home.
- e2e: `e2e/character-wizard-flow.spec.ts` (LIVE) expands a row today —
  extend the assertion to the tier panel's `Short (N chars)` label and the
  "Written as:" line on a hinted field.

### 2. The continue-mode toasts (`app/salon/[id]/hooks/useSSEStreaming.ts:997-1012`)

- `triggerContinueMode(participantId, nudge = false)`: `if (streaming ||
  waitingForResponse) return; if (isPaused) return;` then `participant =
  participantsAsBase.find(p => p.id === participantId && p.isActive)`;
  none → `showErrorToast('This participant is no longer available in the
  chat.')` + return; `if (!hasActiveCharacters)` →
  `showErrorToast('No characters available. Add a character to continue
  the conversation.')` + return.
- v5 entrances into `runTurn({ continueMode: true, … })`
  (`screens/salon/salon-conversation.ts:2995-3005`): `continueTurn()` (the
  composer's v5-ONLY Continue button — v4 has none there; `chat-composer.
  ts:372-386`, disabled by `!hasActiveCharacters()`), `onNudge()`
  (`:2964-2973`, `respondingParticipantId = turnInfo.nextSpeakerId`), and
  the participant card's per-seat continue (`chat/sidebar/participant-
  card.ts:316`, `qt-chat-continue-button` → the `nudge` output → the
  Salon's `onNudge`). The sidebar Skip (`:2663-2680`) already toasts v4's
  OTHER sentence (`handleContinue`'s `No characters available. Add a
  character to continue.`) — different function, different sentence; keep.
- Port BOTH toasts at `runTurn`'s continue entrance (the participant arm
  only when a `respondingParticipantId` is given — look it up in the
  current roster by id + `isActive`), and measure reachability: with the
  buttons disabled by the same predicate, the arms may be reachable only
  through a stale roster (a participant removed by another tab between
  render and click — the P4.D90 refresh class). Pin each arm at unit
  level (a spec driving `runTurn` with a stale id / an empty roster); an
  e2e beat only if a real gesture reaches it (the transient-toast trap:
  `toHaveCount` resolves on the first matching poll).

### 3. `runTemplateSave` (`app/aurora/[id]/view/hooks/useCharacterView.ts:255-281`)

- v4: `applyTemplateTransform(character, transform)` → `{mainUpdates,
  changedSystemPrompts}`; both empty → `showSuccessToast('No replacements
  needed')` + return; `const { errors } = await applyCharacterFieldUpdates(
  characterId, { mainUpdates, promptUpdates: changedSystemPrompts })`;
  `await fetchCharacter()` (ALWAYS — also on partial failure); `errors.
  length > 0 ? showErrorToast(errors.join(' ')) : showSuccessToast(
  successMsg)`.
- v5: `view/tabs/details-tab.ts:328-360` inlines the fan-out (`generators/
  optimizer/apply-character-field-updates.ts:17` says so); the shared
  helper lives at `apply-character-field-updates.ts:52`. Lift the tab onto
  the helper (both files are this lane's); keep the refetch-before-toast
  order; the existing details-tab specs must stay green and a new one pins
  the partial-failure arm (errors joined by a single space, refetch still
  happens).

### 4. The fixed "Summon From Lore" title

- v4 `components/chat/SummonFromLoreModal.tsx` wraps the shared
  `AIImportWizard` (`components/settings/ai-import/AIImportWizard.tsx`)
  with a fixed chrome title + Close; v5 folded the wrapper into
  `chat/cast/add-character-dialog.ts` (the K4 record's "noted fidelity
  divergence") and `ai-import-wizard.ts` titles by step. Restore v4's
  fixed title on the SALON mount through a thin `[title]` input (measure
  the exact title string and the Close button's placement/copy in the
  wrapper); Aurora's mount keeps whatever v4's `AIImportWizard` shows
  standalone (measure — the step titles may be v4's own there).

### 5. The three K4 divergences (named by phrase only — locate, then port or record)

- **The `imported` count line:** v4 `AIImportWizard.tsx:407,480` —
  `importResult.importedCount` is ONE number from the import result and the
  line `(${importedCount} entities imported)` renders only when `> 1`. v5
  `ai-import-wizard.ts:297-298,686-692` computes `importedCount` from
  something else (its comment at `:686` says v4's is a single number).
  Measure what the server's `imported` frame carries on each side and
  whether v5's derivation can disagree with v4's number; port the direct
  read or record the divergence with the frame shape as evidence.
- **The stale-closure apply banner:** v4 `components/characters/optimizer/
  hooks/useCharacterOptimizer.ts` — find the apply-result banner whose
  handler reads a `useState` value captured by a stale closure (search the
  apply path for a banner/summary set after `applyCharacterFieldUpdates`);
  decide whether the stale read is a v4 BUG (then v5's `optimizer-state.ts`
  stays correct and the record names a v4-first filing candidate with the
  repro) or a v4 behaviour v5 dropped (then port).
- **The error-terminal phase:** v4 `useCharacterOptimizer.ts:220-226` — on
  an `error` frame: `setError(event.error ?? 'An unforeseen calamity has
  befallen the refinement process.')` + `setLoading(false)` with the PHASE
  unchanged; and `:233-238` — a stream that ends WITHOUT `done` →
  `setLoading(false)` + `review` if suggestions exist. v5 `optimizer-
  fold.ts` / `optimizer-state.ts`: measure the fold's terminal state on an
  `error` frame and on an early stream close; pin whichever matches v4,
  fix what does not (the byte-exact calamity sentence included).

### 6. The interceptor's nested-button census

- `workspace/chrome/link-interceptor.ts:46-60`: the v5-only rule passes a
  click THROUGH when `target.closest('button')` is inside the anchor. Its
  safety rests on every action control inside a card's `<a routerLink>`
  being a `<button>`. Write `link-interceptor.spec.ts`'s census: render the
  chat card (`chat/…` / the Salon list card) and the character card
  templates in jsdom and assert every element with a `(click)` binding
  inside the anchor IS a `<button>` (or carries the rule's escape) — a
  future `<span (click)>` reddens it. Measure the current card set from
  `tab-registry.ts:89`'s note.

## Tiered deliverables

### Tier 1 — must land

1. The three review-pane renders + the CommonMark preview module, spec-
   pinned (label strings, char counts in UTF-16 units, the qtap-link arm,
   raw HTML dropped), and the wizard e2e beat extended.
2. The two continue toasts at the `runTurn` continue entrance, unit-pinned
   per arm.
3. `runTemplateSave` on the shared helper; the partial-failure spec.
4. The Salon mount's fixed title.
5. Each K4 divergence dispositioned with evidence (ported + pinned, or
   recorded with the v4 `file:line` and a filing candidate where v4 is
   the one at fault).
6. The nested-button census spec.

### Tier 2 — should land

7. The `isPaused` early return of `triggerContinueMode` measured against
   v5's continue entrance (v5 has no local pause latch — P4.D161's
   NO-COUNTERPART; confirm the server-side pause makes the arm moot and
   record).
8. A `qt-prompt-field-example` adoption in the scenario editor (the
   duplicate line retired).

### Tier 3 — explicit refusal-arm deferrals (loud, typed, never silent)

- v4's `QtapLink` component semantics beyond the href rewrite (its click
  handling lives in the Salon's link layer) — the preview renders the
  SAME anchor shape the Salon renderer emits and delegates; anything
  further is recorded.
- P4.81 item 7 (the flipping shared-default spec) stays MEASURED, NOT
  FIXED — not this lane's.

## The differential requirement (restated)

No Rust unit lands here; the equivalence discipline is the parity spec:
every render/toast/handler transcribed cites v4's `file:line` at
`2f4254b42` in its spec header and asserts v4's strings byte-for-byte.
Where v4 has a jest suite for the surface (measure `__tests__` beside each
v4 file), its assertions are transcribed 1:1 (the P4.D161 precedent);
where v4 has none, the spec drives the rendered control (click the row,
read the DOM — the `2f4254b42` round's lesson: handler-direct specs leave
template bindings unpinned). Every fix is mutation-proven (revert the
render → the spec reddens).

## Fixtures

- No committed fixture. The wizard beat's non-streaming mock already
  answers the generation steps (its header carries the recipe); extend
  its canned physical-description reply so the tier panel has every tier
  populated (Short/Medium/Long/Complete + a Full Description containing a
  `qtap:` link and a raw `<b>` tag to prove the drop).
- Seeding through the running server's verbs only.

## §R — the round-wide meeting points (BINDING — identical in every order of this round)

Round: **the generator follow-ups + prompt-templates round** — P4.82 ∥ P4.83
∥ P4.84 ∥ P4.85 ∥ P4.86 (P4.86 CONDITIONAL on the human's dependency
ruling — see its preamble). Ordered 2026-09-07. The first non-drift round
since the follow-ups round 2 (2026-09-04): nothing to absorb, every lane is
banked follow-up work from the `p4.9k` and `2f4254b42` rounds.

1. **Baseline and drift.** Oracle baseline **`2f4254b42`** (drift-ledger §1,
   "feat: character subprompts", `4.10.0-dev.1`). v4 `main` HEAD **AT the
   baseline** and v4 `bugfix` tip **`1a2b2164c`** unmoved; the ledger's §3 is
   EMPTY — **zero drift**. No ledger row is ordered by this round.
2. **The freshness probe at lane start** (ledger §2), expected values:
   checkout on **`main`**, tree CLEAN, `git log --oneline 2f4254b42..main`
   EMPTY, `git log --oneline 1a2b2164c..bugfix` EMPTY. **Any mismatch: STOP
   and report** — a lane never writes the ledger and never builds its own
   pin; the planner re-points the round. Re-run the probe before EVERY regen
   batch (the checkout has gone dirty and moved MID-LANE before — ledger
   §5.1; a lane that discovers drift mid-way finishes nothing that depends
   on a regen and reports).
3. **Regen rule: NO PIN REQUIRED** (ledger §1) — every regen runs from the
   live `~/source/quilltap-server` checkout after a passing probe, through
   `harness/tools/recipe_sweep.py --run <family>` (the sanctioned path; it
   refuses empty-stage families and unknown names); never two sweeps
   concurrently. Committed recipe headers keep saying `cd
   ~/source/quilltap-server` and never name a `/tmp` pin
   (`recipe-header-conventions`). The oracle `node_modules` are the LIVE
   tree (`zod` 4.5.4 — `zod_version_guard` fails the gate if it moves; every
   Zod length rule transcribed this round uses the `jsstr::zod_len_*`
   code-point helpers, never `utf16_len`). Check every NDJSON is non-empty
   before believing a diff (the empty-file trap; a family's env var is not
   its regen var — `QT_ORACLE_<FAMILY>` reads it, `QT_ORACLE_OUT` writes it).
4. **Branching.** Every lane branches from `main` into its own worktree:
   `git worktree add <path> -b claude/p4-8N-<slug>-<hash> main`;
   `CARGO_INCREMENTAL=0`; `npm ci` (never `cp -a`) for the SPA lanes. No
   stacking this round. P4.86 does not start unless the human has granted
   the dependency ruling its preamble names.
5. **Fenced-append shared files.** `crates/quilltap-core/src/api/types.rs`,
   `crates/quilltap-core/src/api/engine.rs`, `crates/quilltap-core/src/lib.rs`
   (module lines), `crates/quilltap-core/src/services/mod.rs`,
   `crates/quilltap-core/src/generators/mod.rs`,
   `crates/quilltap-core/src/services/queue_service.rs`,
   `crates/quilltap-host/src/host.rs`, `crates/quilltap-host/src/spine.rs`,
   `crates/quilltap-web/src/lib.rs` (module + route lines): each Rust lane
   appends ONLY inside its own fence (`// === P4.82 ===` … `// === end P4.82
   ===`, opened once per file, new variants/arms/registrations at the END of
   the relevant block, never interleaved). `crates/quilltap-web/src/
   characters_routes.rs` and `system_data_routes.rs` are touched by NOBODY
   this round (P4.85's wire tests live in `crates/quilltap-web/tests/`).
   `Cargo.toml`/`Cargo.lock` at the workspace root and in every crate:
   version lines only — **except P4.86, the ONE lane authorized to add a
   dependency (`jsonschema`), and only once the human's ruling is recorded
   in this round's report.** Any other dependency add is a STOP-and-flag.
6. **`apps/web/**` is split between TWO lanes by file:** P4.83 owns
   `screens/characters/generators/prompts-editor/import-modal.ts`, its NEW
   `prompts-editor/prompt-templates.api.ts`, `screens/characters/edit/
   system-prompts-tab.ts`, `screens/characters/new/new-character.ts` and
   its NEW e2e spec; P4.84 owns `screens/characters/generators/wizard/
   generation-step.ts` (+ its NEW preview-markdown module), `generators/
   optimizer/**`, `generators/ai-import/**`, `chat/cast/add-character-
   dialog.ts`, `screens/salon/salon-conversation.ts`, `screens/characters/
   view/tabs/details-tab.ts`, `ui/prompt-field-label.ts` (+ a NEW
   `ui/prompt-field-example.ts` if the example line is extracted) with
   `screens/characters/edit/scenario-editor.ts` ONLY to adopt that
   extraction, `workspace/chrome/link-interceptor.spec.ts` (spec only), and
   the three existing generator e2e specs. `apps/web/src/app/core/
   core-contract.ts` is FROZEN: P4.83 declares its DTO in its own wire file
   verbatim from its §B and the unifier folds it (the name-for-name diff).
   The Rust-only lanes (P4.82, P4.85, P4.86) finish with `git diff main --
   apps/web/` EMPTY.
7. **Frozen files.** `docs/developer/porting/drift-ledger.md` (lanes never
   write it); `help/**`; `apps/web/src/app/shell/shell.ts` and
   `apps/web/src/app/workspace/workspace-contract.ts` (no new tab kinds).
   `crates/quilltap-web/tests/fixtures/character-generators-{main,mount}.db`
   is READ by P4.82 (if it reuses it), P4.85 and P4.86 and REGENERATED BY
   NOBODY.
8. **Version bumps.** P4.82: `quilltap-core`, `quilltap-harness`,
   `quilltap-host`. P4.83: `quilltap-core`, `quilltap-harness`,
   `quilltap-web`, `apps/web/package.json`. P4.84: `apps/web/package.json`.
   P4.85: `quilltap-core`, `quilltap-harness`, `quilltap-web`. P4.86:
   `quilltap-core`, `quilltap-harness` (+ the root `Cargo.lock` for the
   dependency). Per commit. The unifier RECOUNTS as base + total bumps —
   identical bumps merge silently (`commit-checklist`; the playbook's
   silent-auto-merge trap), and a `Cargo.toml` conflict is NOT version-only
   when P4.86 is in the round (the `p4.9i2` §3 catch: a `--ours` resolution
   dropped a feature block).
9. **e2e.** Port 4319 is repo-wide — ONE Playwright at a time across all
   lanes. P4.83 and P4.84 each run ONLY their own spec files by name (never
   the full suite — that is the unifier's), and never kill a server they did
   not start (`pkill -f` can kill a sibling lane's gate; read `pgrep -fl`
   argv first). A held port means WAIT, not force. A beat's mock must speak
   the runner's transport (the optimizer and AI-import runners call the
   NON-streaming `send_message`; the shared `support/mock-llm.ts` speaks
   only SSE — see the spec headers' recipes).
10. **Append-only for everyone:** `docs/CHANGELOG.md`,
    `docs/developer/porting/status-log.md`.
11. **Fixture ownership.** P4.82 delivers a NEW committed
    `crates/quilltap-harness/fixtures/headshoulders-{main,mount}.db` pair (+
    its builder under `harness/oracle/fixtures/`, v4's REAL repositories,
    pinned ids) unless it can prove an existing pair carries every shape it
    needs (record the measurement either way). P4.83 builds its fixture in
    `/tmp` from a clean provisioned instance inside its recipe (the
    `settings_wire_actions` precedent) and commits NOTHING under
    `fixtures/`. P4.85 and P4.86 EXTEND the four generator tier-3 oracle
    cases (three P4.85's, the AI-import one P4.86's) — the unifier
    regenerates all four ONCE at unification after both lanes' edits are in
    (the P4.D119 rule). P4.84 seeds through the running server's verbs,
    never SQL.
12. **Census guards move when a verb is added.** P4.83 adds dispatch verbs;
    adding any verb, or closing a recorded divergence (P4.86 retires
    `VALIDATION_UNAVAILABLE`'s both-directions pins), moves the executable
    censuses in OTHER crates (`a-new-verb-moves-the-dispatch-wrong-type-
    census`, `closing-a-divergence-moves-the-censuses`) — every Rust lane's
    gate is `cargo test --workspace`, never `-p`, with the full log captured
    to a file + a sentinel (never `| tail`, never a `pgrep` poll loop).
13. **Log-line pins** use the thread-scoped capture seam (`a-process-global-
    test-seam-must-be-thread-scoped`); a `%x` field renders unquoted, a bare
    or `?x` field quoted (`tracing-percent-field-renders-unquoted`) — read
    v4's `logger.<level>(msg, bag)` bag and pin the LEVEL, the sentence and
    the field NAMES.

## Ownership (BINDING — identical table in every order of this round)

| Lane | Owns (edit freely) | Fenced-append (§R.5) | Must not touch |
| --- | --- | --- | --- |
| **P4.82** | NEW `crates/quilltap-core/src/services/headshoulders_backfill_job.rs`; NEW `crates/quilltap-core/src/services/headshoulders_backfill_enqueue.rs`; `crates/quilltap-core/src/generators/wizard.rs` ONLY for the `generate_field` / `WizardCallCtx` visibility edit (one fenced block, no behaviour change); NEW harness families `headshoulders_backfill_tier3_equivalence` + `headshoulders_backfill_enqueue_equivalence` + their oracle cases + the fixture builder + the committed pair; NEW host tests under `crates/quilltap-host/tests/` | `services/queue_service.rs`, `services/mod.rs`, `host/host.rs`, `host/spine.rs` | `api/**`, `apps/web/**`, every other generator file |
| **P4.83** | `crates/quilltap-core/src/db/prompt_templates.rs`; NEW `crates/quilltap-core/src/api/prompt_templates.rs`; NEW `crates/quilltap-core/src/services/builtin_prompt_templates.rs` + NEW `crates/quilltap-core/src/services/builtin_prompt_templates.json`; NEW `harness/oracle/provision/dump-prompt-templates.ts`; NEW `crates/quilltap-web/src/prompt_templates_routes.rs` (+ its wire test); NEW harness family `prompt_templates_routes_equivalence` + oracle case; SPA: `prompts-editor/import-modal.ts`, NEW `prompts-editor/prompt-templates.api.ts`, `edit/system-prompts-tab.ts`, `new/new-character.ts`, NEW `e2e/prompt-templates-flow.spec.ts` | `api/types.rs`, `api/engine.rs`, `core/lib.rs`, `services/mod.rs`, `web/lib.rs` | `core-contract.ts` (frozen), every P4.84 SPA file, every other Rust file |
| **P4.84** | SPA only: `generators/wizard/generation-step.ts` + NEW `generators/wizard/generation-preview-markdown.ts`; `generators/optimizer/**`; `generators/ai-import/**`; `chat/cast/add-character-dialog.ts`; `screens/salon/salon-conversation.ts`; `screens/characters/view/tabs/details-tab.ts`; `ui/prompt-field-label.ts` (+ NEW `ui/prompt-field-example.ts`) and `edit/scenario-editor.ts` ONLY to adopt the extraction; `workspace/chrome/link-interceptor.spec.ts`; the existing `e2e/character-wizard-flow.spec.ts`, `e2e/character-optimizer-flow.spec.ts`, `e2e/ai-import-flow.spec.ts` | — | every Rust file; `import-modal.ts`, `system-prompts-tab.ts`, `new-character.ts` (P4.83's); `core-contract.ts` |
| **P4.85** | `crates/quilltap-core/src/api/salon.rs`; `crates/quilltap-core/src/api/generators_detail.rs`; `crates/quilltap-core/src/api/image_profiles.rs`; `crates/quilltap-web/src/generator_sse.rs`; `crates/quilltap-web/tests/generator_sse_wire.rs` + `crates/quilltap-web/tests/characters_generators_routes.rs`; harness `character_optimizer_tier3_equivalence.rs`, `character_wizard_tier3_equivalence.rs`, `external_prompt_tier3_equivalence.rs` + their oracle cases (`character-optimizer-tier3.test.ts`, `character-wizard-tier3.test.ts`, `external-prompt-tier3.test.ts`) + their per-family fixture JSONs; the family that covers `image-profiles?action=generate` (+ a new astral-prompt arm) | — | `api/generators_wizard.rs`, `generators/ai_import.rs`, `ai_import_tier3_equivalence.rs`, `ai-import-tier3.test.ts` (P4.86's); `apps/web/**` |
| **P4.86** (conditional) | `crates/quilltap-core/Cargo.toml` (the ONE `jsonschema` dependency line) + the root `Cargo.lock`; NEW `crates/quilltap-core/src/generators/qtap_schema.rs` + NEW vendored `crates/quilltap-core/src/generators/qtap-export.schema.json`; `crates/quilltap-core/src/generators/ai_import.rs`; `crates/quilltap-core/src/api/generators_wizard.rs`; harness `ai_import_tier3_equivalence.rs` + `harness/oracle/cases/ai-import-tier3.test.ts` + `harness/oracle/fixtures/ai-import-tier3.json`; NEW harness family `qtap_schema_validate_equivalence` + its oracle case + recorded corpus | `generators/mod.rs` | every other file; `apps/web/**` |

Everyone: `docs/CHANGELOG.md` + `status-log.md` append-only; the drift
ledger frozen; `core-contract.ts` / `shell.ts` / `workspace-contract.ts`
frozen; `characters_routes.rs` / `system_data_routes.rs` untouched.

## Verification gate (before declaring done)

1. `npm run lint` (incl. `check-qt-classes` — a class named in prose
   trips it; never a backtick inside an inline-template HTML comment);
   `npm test` (full run — `--filter` matches test NAMES, not paths);
   `npm run build` (the only real SPA type gate).
2. The three generator e2e specs by name against release binaries (ONE
   Playwright at a time; wait for port 4319).
3. Mutation proofs: each of the three renders removed → its spec reddens;
   the toast sentence altered → the arm's spec reddens; the helper call
   replaced by the old inline fan-out → the partial-failure spec reddens;
   a `<span (click)>` planted inside a card anchor → the census reddens.
4. `cargo fmt --all --check` (touched no Rust — prove it: `git diff main
   -- crates/ Cargo.*` EMPTY).
5. Commits per `.claude/commands/commit.md` (`apps/web/package.json`
   bumped per commit; CHANGELOG every commit); the lane record appended
   to `status-log.md` (the K3 premise refutation, each K4 disposition with
   its evidence, any v4-first filing candidate); this order's status
   header updated.
