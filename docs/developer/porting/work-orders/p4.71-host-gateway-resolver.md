# Work order P4.71 — the host gateway resolver (v4 `lib/host-rewrite.ts`: `QUILLTAP_HOST_IP` + Docker `host.docker.internal`), wired into every provider construction site

**Status:** OPEN — not started (ordered 2026-09-02, the follow-ups round:
P4.67 ∥ P4.68 ∥ P4.69 ∥ P4.70 ∥ P4.71). Written from P4.D134's lane record
("**v5 has never had a host gateway resolver** — measured, named in
`rewrite.rs`") carried as `phase-4.md` candidate 5 since the round-1
drift catch-up. This is a functional gap in the first-class Docker
deployment (phase-4 locked decision): a user-configured
`http://localhost:11434` (Ollama, LM Studio) inside the container reaches
the container's own loopback and fails; v4 rewrites it to the host.

## Preamble

- **v4 oracle baseline: `6d2a50382`** (drift-ledger §1, probe re-run
  2026-09-02 at ordering: v4 checkout on `main`, tree clean, both logs
  empty, §3 EMPTY). Not a drift lane — v4's module has been stable since
  `1560bd43b` collapsed five strategies to two (absorbed at P4.D134, the
  PURE rewrite half only).
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 checkout
  on `main`, tree clean, `git log 6d2a50382..main` empty and `git log
  3a76b17df..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: NO PIN NEEDED** (ledger §1 as of 2026-09-02); a failed
  probe flips it to §5.1's pinned worktree. The NEW family's recipe
  header follows `recipe-header-conventions`; regens through the sweep
  driver.
- **Sibling lanes (this round):** P4.67 (REST-edge query semantics),
  P4.68 (core consolidation — **§E: its executor census RECORDS host
  findings for this lane; it never edits `spine.rs`**), P4.69 (SPA —
  owns Playwright), P4.70 (image fidelity + fixture — owns
  `image_gen/**`; this lane may touch `model/image_dialects.rs` ONLY for a
  measured base-URL seam). §Shared contracts / §Ownership are binding and
  identical across all five orders.
- **Worktree:** per the `carryout` skill. `CHANGELOG` / `status-log.md`
  append-only. **No Playwright**; no SPA surface.
- **Long gates:** `run_in_background: true` + full log + sentinel; NEVER a
  `pgrep` loop; NEVER `| tail`. Never edit source during a workspace gate.
  Memory note `core-has-no-tokio-scheduler` — the resolver's cache is a
  plain `OnceLock`, no timers.

## Mandate

Port `lib/host-rewrite.ts` whole into `quilltap-host` — `isVMEnvironment`,
the once-per-process cached `resolveHostGateway` with its two strategies
and three log lines, `rewriteLocalhostUrl`'s `Rewrote localhost URL` debug
line — and inject the resolved gateway at EVERY v5 construction site whose
v4 twin rewrites (`provider-registry.ts`'s five `resolveBaseUrl` sites +
`abstract-provider-registry.ts:201`'s `validateApiKey`), adding the ONE
missing core seam (`model/completion_provider.rs:184` passes `None`
hard). Prove the resolver tier-1 against v4's REAL module and every
injection by a wiring pin; document the Linux `--add-host` requirement
and `QUILLTAP_HOST_IP` in the running guide. Must NOT touch:
`services/**` (P4.68), `image_gen/**` or `tools/generate_image.rs`
(P4.70), any `quilltap-web` file (P4.67), the SPA (P4.69), the pure
rewrite's URL semantics (P4.D134's corpus is the pin — extend, never
alter).

## Survey-verified starting points (2026-09-02)

1. **v4's module, 151 lines** (`lib/host-rewrite.ts`): `LOCALHOST_HOSTS =
   {localhost, 127.0.0.1, [::1], ::1}`; `cachedGatewayHost: string | null
   | undefined` (undefined = unresolved); logger child `{module:
   'host-rewrite'}`. `isVMEnvironment() = isDockerEnvironment() ||
   !!process.env.QUILLTAP_HOST_IP` (`:56-58` — JS `!!`, so an EMPTY env
   value is "unset"). `resolveHostGateway()` (`:66-100`): cached →
   return; strategy 1 `QUILLTAP_HOST_IP` truthy → `info('Host gateway from
   QUILLTAP_HOST_IP', {host})`, cache, return; strategy 2
   `isDockerEnvironment()` → `info('Docker environment detected — using
   host.docker.internal as gateway hostname')`, cache
   `'host.docker.internal'`; else `warn('Could not resolve host gateway —
   localhost URLs will not be rewritten')`, cache `null`. The comment at
   `:78-88` explains WHY no bridge-IP fallback (`172.17.0.1` cannot reach
   the host's loopback) — carry it. `rewriteLocalhostUrl(url)` (`:113-150`):
   no-op unless `isVMEnvironment()`; `new URL(url)` (unparseable →
   unchanged); non-localhost host → unchanged; gateway `null` →
   unchanged; else swap `.hostname`, `toString()`, `debug('Rewrote
   localhost URL', {original, rewritten, gatewayHost})`. Note the ORDER:
   the environment gate runs BEFORE parsing, and resolution (with its
   log line) happens only for a localhost URL. `_resetGatewayCache` is
   exported (the `packages/plugin-utils` copy shows it; check the `lib/`
   module) — the oracle uses it, or `jest.resetModules` + re-import.
2. **v5's pure half is done and pinned.** `crates/quilltap-core/src/
   provider_manifest/rewrite.rs` `rewrite_localhost_url(url, gateway:
   Option<&str>)` reproduces `new URL().toString()` for the bounded space
   (header `:44-57`), corpus-pinned at P4.D134; its header `:38-43` is the
   gap's own record: "Nothing outside this crate calls
   `with_localhost_gateway`, so the injected gateway is `None` on every
   production path". **Do not change its URL semantics**; the header's
   gap paragraph is retired by this lane.
3. **The probe exists in the host.** `crates/quilltap-host/src/env.rs:122
   is_docker_environment()` (`DOCKER_CONTAINER === 'true'` / `/.dockerenv`
   / an `/app` directory — v4 `lib/paths.ts:80`). The resolver is a NEW
   host module (`host_gateway.rs`): `is_vm_environment()`,
   `resolve_host_gateway() -> Option<String>` over a process `OnceLock`,
   and a PURE `resolve_host_gateway_pure(host_ip: Option<&str>, is_docker:
   bool) -> Option<&'static str | String>` the differential drives; the
   three log lines byte-exact through `tracing` with `module =
   "host-rewrite"` fields as v4's child logger carries. Decide where the
   `Rewrote localhost URL` debug line lives (v4 logs INSIDE the rewrite;
   v5's pure core function has no logger — the faithful spot is a host
   wrapper `rewrite_localhost_url_logged` that calls the core function and
   logs on change, so the core stays pure) and pin it by capture.
4. **v4's injection sites** (the twins this lane must reach): `lib/plugins/
   provider-registry.ts:88-89 resolveBaseUrl` used at `:103`, `:133`,
   `:163`, `:261`, `:275` (the LLM registry's factory methods — read each
   to name it: streaming provider / completion / embeddings / model
   listing / …), and `lib/plugins/abstract-provider-registry.ts:201`
   (`validateApiKey(name, apiKey, baseUrl)` for EVERY registry type —
   LLM, image, embedding, search). ⚠ Measure whether the IMAGE and
   EMBEDDING registries' own factory paths rewrite (grep `lib/plugins/`
   for the other registries); if an image-generation call does NOT pass
   through a rewrite in v4, v5's image path must not rewrite either —
   record it. Same for the search registry's `validateApiKey` (P4.59's
   probe).
5. **v5's construction sites** (host, all today injecting nothing):
   streaming — `quilltap-host/src/providers.rs:228 WireStreamingProvider::
   new(…)` (`with_localhost_gateway` exists at `model/streaming_provider.
   rs:225`, never called); embeddings — `spine.rs:2615/2675/2723/2763/
   3087/3266 ApiEmbeddingProvider::new(…)` (`with_localhost_gateway` at
   `services/embedding_provider.rs:177`, never called); completion —
   `spine.rs:363` and `:3573 WireCompletionProvider::new(…)` → core
   `model/completion_provider.rs:172-195 execute_completion` does
   `rewrite_localhost_url(base, None)` at `:184` — **no seam**: add a
   `localhost_gateway: Option<String>` carried by `WireCompletionProvider`
   (builder `with_localhost_gateway`, the streaming twin's shape) and
   passed into `execute_completion`; image — `host.rs:800` and
   `avatar_preview.rs:51 RealImageProvider::with_bytes_fetch(…)` (inject
   ONLY if item 4 measures that v4 rewrites the image path; the seam would
   sit in `model/image_dialects.rs`); model listing — `model/
   provider_models_api.rs` (v4's `:261`/`:275` twins — measure);
   validate-key — `api/provider_actions.rs` (v4 `:201`, must rewrite).
   Every injection reads the resolver ONCE per construction (the
   `OnceLock` makes repeats free).
6. **Docs + packaging.** v4's start scripts add
   `--add-host=host.docker.internal:host-gateway` on Linux
   (`scripts/start-quilltap.sh:156`, `.ps1:207`, `start-quilltap-docker.
   ts:445`) because Linux Docker has no built-in DNS for it; v5's
   `docs/developer/running.md:33/67/115 docker run …` lines carry no such
   flag and never mention `QUILLTAP_HOST_IP`. The root `Dockerfile` —
   check whether it sets `DOCKER_CONTAINER=true` as v4's does (the probe's
   first arm) and whether `/app` is the workdir (its third).

## Tiered deliverables

**Tier 1 (must land):**

1. `host_gateway.rs` per item 3: the pure resolver, the cached process
   resolver, `is_vm_environment`, the three log lines, the logged rewrite
   wrapper; `rewrite.rs`'s gap paragraph retired.
2. The NEW tier-1 family `host_gateway_equivalence` over
   `harness/oracle/cases/host-gateway.test.ts` driving v4's REAL
   `resolveHostGateway` / `isVMEnvironment` / `rewriteLocalhostUrl` with
   `jest.doMock('@/lib/paths', …isDockerEnvironment)` per row and
   `process.env.QUILLTAP_HOST_IP` set/`''`/unset per row, the cache reset
   between rows (memory note `jest-domock-survives-resetmodules`): the
   full matrix {env unset, `''`, `10.0.0.5`, `my-host`} × {docker
   true/false} × URLs {`http://localhost:11434`, `http://127.0.0.1:1234/v1`,
   `http://[::1]:8080/x?y=1#f`, `https://api.openai.com/v1`,
   `not a url`, `http://LOCALHOST:11434`} → the rewritten string (or
   unchanged) + the resolved gateway; v5 side drives the pure resolver +
   the pure rewrite. Mutation: swap the strategy order → red.
3. The injections (item 5) at every site whose v4 twin rewrites, with a
   WIRING pin per site — a canned transport recording the URL hit, the
   gateway forced to `Some("gw.test")`, a `localhost` profile base URL,
   assert the recorded host; revert one site's injection → exactly its
   pin reds (the P4.D108 "structurally unreachable" lesson: a seam
   without its wire is a no-op).
4. `running.md` gains the Linux `--add-host` flag on every `docker run`
   line with v4's one-paragraph why, and a `QUILLTAP_HOST_IP` paragraph
   for self-managed VMs; the `Dockerfile` probe arms verified (item 6).

**Tier 2 (should land):**

5. The image/embedding/search registry measurement (item 4) written up
   as a per-registry table (v4 rewrites Y/N → v5 injects Y/N), any
   measured-faithful NON-injection recorded.
6. The `validateApiKey` twin (`api/provider_actions.rs`) and the model
   listing (`provider_models_api.rs`) injected per the table.

**Tier 3 (explicit deferrals, loud):** 💸 the container walk with a real
Ollama profile at `http://localhost:11434` (the dogfood queue — the P4.10
walk recipe in `status-log.md`, "container walk"); a Tauri-shell
environment answer (bare metal → no-op by construction; record).

## The differential requirement (restated)

The resolver + rewrite are pure once the environment is injected, so the
family is tier-1 EXACT against v4's REAL `lib/host-rewrite.ts` (imported,
never reimplemented) with the environment mocked per row on the v4 side
and passed as arguments on the v5 side. The injections are
differential-invisible (a chokepoint cutover — memory note
`a-chokepoint-cutover-is-differential-invisible`), so each ships with its
wiring pin, mutation-proven. The three log lines are capture-pinned
(thread-scoped seam; prefix-match target assert).

## Fixtures

None committed; the family is DB-free. The wiring pins use canned
transports (the `CannedStreamingProvider` / recording-transport idiom in
`model/*.rs` tests).

## Shared contracts (binding, identical across all five orders)

- **§A — well-formed actions never move.** P4.67 may change the answer
  to `?action=` (empty), an UNKNOWN action, and DUPLICATED keys only. The
  status + body of every well-formed `?action=<served>` request and every
  single-valued query is byte-identical before and after (the existing
  web families are the pin). P4.69's SPA callers (`image-profiles.api.ts`
  `?action=options-schema|list-models`, the wardrobe/characters edges)
  therefore need NO change and P4.69 makes none.
- **§B — participant-status parsing has ONE home:**
  `quilltap_core::chat_predicates::participant_status_from_str` (+
  `is_participant_present` / `json_participant_is_present`). P4.68 owns
  the consolidation; no other lane adds a private status match, and no
  lane changes the canonical unknown-string rule (`None`/`"active"` →
  Active, `"silent"` → Silent, `"removed"` → Removed, anything else →
  Absent).
- **§C — danger styling is a client predicate.** `shouldShowDangerStyling
  (chat) = getConciergeState(chat) === 'flagged'` lives ONLY in the SPA
  (`chat/concierge-state.ts:127`, v4 `chat-override.ts:110`); P4.69 binds
  it to the message list; no server payload changes (the §A list wire of
  the `6d2a50382` round stands).
- **§D — the `system-data-*` committed fixture is P4.70's this round.**
  P4.70 widens it (additively — every existing row byte-preserved,
  `system-data.json` regenerated by the shipped builder) and re-runs all
  nine consuming families without editing them (except
  `system_import_state`'s discriminating leg, which P4.70 owns). P4.67
  consumes the committed pair at its base. The unifier re-runs P4.67's
  three consuming families over the widened pair before the gate.
- **§E — the gateway seam.** P4.71 threads `with_localhost_gateway` from
  the host into every provider construction site and adds the ONE missing
  seam (`model/completion_provider.rs`'s hard `None`). No other lane
  constructs a wire provider or touches `quilltap-host/src/spine.rs`,
  `providers.rs`, or `host.rs`; P4.68's executor census is core-only and
  RECORDS any host-side finding for P4.71/the unifier instead of editing.
- **§F — `precompute_equivalence` and the `episodic-recall-*` pair are
  P4.68's.** P4.70's fixture work is the `system-data-*` pair only; the
  two lanes' fixtures are disjoint by name.

## Ownership (binding, identical across all five orders)

| Area | Owner |
|---|---|
| `crates/quilltap-web/src/**` (every `*_routes.rs`, `log_file.rs`, the NEW shared query reader), `crates/quilltap-web/tests/**` (NEW `query_param_semantics_equivalence`; arms added to `system_body_guards_equivalence` / `files_body_guards_equivalence`); `quilltap-core`: `api/types.rs` (the `FileUpload.tags` widening ONLY), `api/system_data.rs` (the `jobs_enqueue` payload gate ONLY), `api/chat_media.rs` (the `chat_file_link` guard ONLY); harness: `system_jobs_collection_equivalence` (the `payload: []` arm), NEW `harness/oracle/cases/query-param-semantics.test.ts` | **P4.67** |
| `quilltap-core`: `chat_predicates.rs`, `enclave/announce.rs`, `services/{commonplace_notifications, user_identity_resolver, fold_episode_pass, turn_orchestrator, participant_resolver, message_finalizer, build_context, orchestrator, provider_failover, cheap_llm_exec}.rs`, `tools/{self_inventory, whisper}.rs`, `tools/doc_edit/shared.rs`, the NEW bare-executor census guard; harness: `precompute_equivalence` + `harness/oracle/cases/precompute.test.ts` + `harness/oracle/fixtures/build-episodic-recall-fixture.ts` + the committed `episodic-recall-*` pair (RE-RUN `recall_replay_equivalence`, `vault_conv_search_equivalence`), the failover / chain-walk families (`orchestrator_tier3`, the `provider_failover` family) + their oracle cases | **P4.68** |
| SPA: `chat/{message-list, message-row, streaming-message, tool-message}.ts` (+specs), `screens/salon/salon-conversation.ts`, `quick-hide/quick-hide.service.ts` (+spec), `screens/settings/images/image-profile-modal.ts` (+spec), the twelve `qt-range` hosts' SPECS (suffix pins only), `styles/qt-components/_chat.css` (only if a rule is missing); e2e: `salon-optimistic-bubble-reconcile.spec.ts`, `workspace-search-documents-flow.spec.ts`, `wardrobe-flow.spec.ts`, NEW danger-avatar beat, `global-setup.ts` if the transfer un-park needs it | **P4.69** |
| `quilltap-core`: `tools/generate_image.rs`, `image_gen/lora_support.rs`, `image_gen/params_builder.rs`; harness: `image_generation_tier3_equivalence`, `image_gen_leaves_equivalence`, `image_generate_route_equivalence` (re-run), `projects_routes_equivalence` (+ its oracle case), `system_import_state` (the discriminating leg), `harness/oracle/fixtures/build-system-data-fixture.ts` + the committed `system-data-{main,mount,llmlogs}.db` + `system-data.json`, `harness/oracle/cases/image-generation.test.ts`, `image-gen-leaves.test.ts`; the other eight `system-data` consumers RE-RUN, not edited | **P4.70** |
| `quilltap-host`: `env.rs`, `providers.rs`, `spine.rs`, `host.rs`, `avatar_preview.rs`, NEW `host_gateway.rs`; `quilltap-core`: `model/completion_provider.rs` (the gateway seam), `model/transport.rs` (doc only), `provider_manifest/rewrite.rs` (header + the pure resolver if placed here), `model/provider_models_api.rs`, `api/provider_actions.rs`, `model/image_dialects.rs` (a base-URL seam ONLY if v4 rewrites there — measure); harness: NEW `host_gateway_equivalence` + `harness/oracle/cases/host-gateway.test.ts`; `Dockerfile`, `docs/developer/running.md` | **P4.71** |
| Playwright (port 4319) | **P4.69's branch only**; every other lane's beats are authored gated (ACTIVATE-AT-UNIFY) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it |


**Version bumps:** this lane bumps `quilltap-host`, `quilltap-core` and
`quilltap-harness` per commit (base: host 0.0.92, core 0.0.750, harness
0.0.642); the unifier recounts as base + total bumps (four lanes bump
core and harness).

## Verification gate before declaring done

`cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
-D warnings` on BOTH feature sets; full `cargo test --workspace` with the
lane's oracle env vars (`run_in_background`, full log + sentinel, every
`test result:` line read); `host_gateway_equivalence` by name through the
sweep driver, zero SKIP, changed bytes grepped (`host.docker.internal`,
the env-strategy rows); the wiring pins + their mutations
verified-applied then reverted; `cargo build --workspace --release`; a
`docker build` of the root `Dockerfile` if the packaging changed (the
P4.10 exit-gate shape — no push, no registry). No SPA run, no Playwright.
Per-commit rule: `.claude/commands/commit.md`. Lane record →
`status-log.md` ("Lane record — P4.71"); this file's status header
updated at close.
