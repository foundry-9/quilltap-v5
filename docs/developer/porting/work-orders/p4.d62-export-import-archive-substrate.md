# Work order P4.D62 — the export/import/file-storage archive substrate (server) — v4 `d553f72a` (WP A2 + B1, Bugs 52/54/55)

**Status:** OPEN — not started.

## Preamble

- **v4 baseline for this round: `d553f72a`** (v4 4.8.0-dev, HEAD at planning
  2026-08-10, tree CLEAN — verified at planning). This is **round 1 of 2** of
  the character-archive drift catch-up (v4 `f6eac168..d553f72a`, five
  commits): this lane ports the **export/import/file-storage substrate** the
  archive service is built on — v4's WP A2 (export fidelity), WP B1
  (`preserveIds`), Bug 52 (avatar-id remap), Bug 54 (sha256 dedup in the
  skip-if-present preflight), and Bug 55 (`FileContentMissingError` → 404).
  The archive **service itself** (prune/bundle/rehydrate, the API actions,
  the CLI) is **round 2** and depends on this lane's functions.
- **Drift-check at lane start.** In `~/source/quilltap-server`, run
  `git log d553f72a..HEAD --oneline`. If v4 moved, **STOP** and report — the
  round is planned against `d553f72a`. Regenerate every oracle this lane
  touches from `~/source/quilltap-server` at `d553f72a`; if HEAD has drifted,
  pin a detached worktree at `d553f72a` (`oracle-regen-pinned-v4-worktree`,
  or `recipe_sweep.py --v4 d553f72a`). Oracles OUTSIDE this lane's families
  keep their `f6eac168`-or-earlier vintage; the baseline moves at
  unification.
- **Sibling lanes this round (disjoint ownership):**
  - **P4.D63** — the schema (D23 re-dump: three new `characters` columns),
    the write guards / `archived=` chokepoint / participant-flip repo
    method, archive-crypto + re-encrypt, and the wipe/restore spare-bundles
    options. It owns `api/types.rs` / `api/engine.rs` / `api/characters.rs`
    / `api/files.rs` / the `db/characters*` structs — **do not touch those
    files**, and see the Shared contract's cross-lane fixture rule: this
    lane must NOT pull the new columns into its own diffs.
  - **P4.D64** — the whole SPA surface, built against the Shared contract,
    beats gated (`CHARACTER_ARCHIVE_SERVER_LANDED` stays false until
    round 2). Pure `apps/web`.
- **Worktree:** carry the lane out on its own branch per the `carryout`
  skill. Do not use `git stash`/worktrees with agents beyond the lane
  branch.

## The drift, classified (surveyed 2026-08-10, fresh line numbers at `d553f72a`)

This lane's half of `01e481f6` + all of `d553f72a`'s substrate:

| v4 file | what changed |
|---|---|
| `lib/export/ndjson-writer.ts` | `streamOneStore` extracted (`:556`, per-store body of the old `streamDocumentStores` loop; `opts.skipProjectLinks`); `streamCharacters` (`:145`) emits each character's vault inline (`:220–233`, after `character_plugin_data`, before `memory`, try/catch warn `'Failed to export character vault'`); id fields on record shapes (folder `id` FIRST key; document `fileId`/`linkId` between `folderId` and `linkGroupId`; blob `fileId`/`linkId`/`blobId` between `descriptionUpdatedAt` and `extractedText`); manifest settings gain `preserveIds` (after `selectedIds`, `:130`); the two inline BACKUP filters (`:754`, `:959`) become `isFileExcludedFromExport` (debug msg → `'Skipping excluded file in export'`) |
| `lib/export/excluded-files.ts` (NEW) | `EXPORT_EXCLUDED_FILE_CATEGORIES = ['BACKUP','ARCHIVE']`, `EXPORT_EXCLUDED_FOLDER_PATHS = ['/backups','/archives']`, `isFileExcludedFromExport(file)` |
| `lib/export/types.ts` | `QuilltapExportSettings.preserveIds?`; `ExportedDocumentStoreFolder.id?` (new first field); document `fileId?`/`linkId?`; blob `fileId?`/`linkId?`/`blobId?`; `ExportOptions.preserveIds?`; `ExportPreview.vaults?: { stores, documents, blobs, estimatedBytes }` |
| `lib/export/quilltap-export-service.ts` | `summarizeCharacterVaults` (best-effort, warn-per-store; blob bytes counted base64-inflated `Math.ceil(sizeBytes*4/3)`); `previewExport` characters branch attaches `vaults` only when `stores > 0` |
| `lib/import/quilltap-import/types.ts` | `PreserveIdsMode` union (`:138`): `{mode:'refuse-on-collision'}` \| `{mode:'skip-if-present', targetCharacterId, targetVaultMountPointId}`; `ImportOptions.preserveIds?`/`preserveIdsMode?`; `IdMappingState` gains `docMountFileLinks`, `characterVaultMounts`, `skippedCharacterVaults`, `preserveIdsSkips` |
| `lib/import/quilltap-import/execute.ts` | `PreserveIdsCollisionError` (`:48`); `getPreserveIdsCreateOptions` (`:55`, **dead helper — port faithfully per the vestigial-cruft rule**); `preflightPreserveIds` (`:71`, 15 check kinds, two error shapes: `` `Preserve IDs collision for ${kind} ${id} (also seen as ${existingKind})` `` and `` `Preserve IDs collision for ${kind} ${id}` ``, pushed to warnings then thrown; **Bug 54**: file/blob `skippable` classifiers settle by content sha256 FIRST — `carriedContentSha`/`carriedBlobSha` maps at `:117–133`, file arm `:230–235`, blob arm `:262–267`, falling back to `fileLinkedInTargetVault`; same-id/different-bytes still refuses; consulted ONLY in skip-if-present mode); `executeImport` runs the preflight after `getExportData` (`:479`) and returns `{success:false,…}` on throw; doc-store phase filters `skippedCharacterVaults`; `importMemories` gains `options`; the BUILTIN-TF-IDF embedding early-return is GONE — BUILTIN default profiles now get per-row `enqueueEmbeddingGenerate` + debounced `scheduleRefit`; only no-profile-at-all warns (`'…because no default embedding profile is configured; they will be indexed once one is.'`) |
| `lib/import/quilltap-import/import-document-stores.ts` | mount-point skip-if-present arm (`idMap.set(mp.id, mp.id)`, settings untouched); create passes `{id: mp.id}` whenever `preserveIds`; folders gain `folderIdBySourceId` + `folderIdByPath` maps; under preserveIds folders create with `{id}` + verbatim `parentId`; **without preserveIds the parent is now resolved BY PATH** (replaces the always-unremapped-`parentId` quirk — a real behavior change on the ORDINARY import path, which v5 currently carries faithfully at `document_stores.rs:239`); `resolveTargetFolderId` (source-id map first, containing-directory fallback); documents/blobs skip-if-present on `linkId`, carried ids honored only at actual row creation (sha256 dedup wins), `idMaps.docMountFileLinks` recorded either way |
| `lib/import/quilltap-import/import-characters.ts` | `rememberBundleVault` helper; skip-if-present arm at loop head (`idMaps.characters.set(id,id); skipped++` — deliberately NOT the conflict-strategy `skip` branch, NOT `rememberBundleVault`); the `skip` branch now adds the vault to `skippedCharacterVaults`; plain create under preserveIds passes `{...charData, id}` + create-options `{id}` |
| `lib/import/quilltap-import/import-entities.ts` | tags/templates/projects/groups/chats/memories all gain the preserveIds create fork; `importMemories` gains `options` + skip-if-present arm |
| `lib/import/quilltap-import/import-profiles.ts` | connection/image/embedding profiles gain the fork at both the name-conflict `duplicate` arm and the plain arm (the duplicate arm mints `newId` into the idMap but creates with `{id: profile.id}` under preserveIds — carry faithfully; unreachable in practice) |
| `lib/import/quilltap-import/reconcile.ts` | **Bug 52**: `discardScaffoldVault` (`:36`, cascade-delete via `deleteStoreCascade`, canonical-name handback, warning `` `Failed to remove the placeholder vault for an imported character: ${msg}` ``); character loop: skip-if-present short-circuit; vault repoint through `characterVaultMounts`→`mountPoints` remap with scaffold remembered; `defaultImageId` remap through `docMountFileLinks` (`:195–207`, unremapped + not a legacy `files` row → cleared to null + `` `Character "${name}" defaultImageId could not be remapped and was cleared: ${id}` ``); `avatarOverrides` remap (`:209–241`, legacy `files` row kept as-is, unremappable override DROPPED not nulled + `` `Character "${name}" avatar override could not be remapped and was dropped: ${id}` ``); scaffold teardown LAST, after the character update lands (ordering is load-bearing) |
| `lib/import/quilltap-import-stream.ts` | blob assembly carries `fileId`/`linkId`/`blobId` (`:285–287`, `?? null`); `buildExportDataForType` characters arm conditionally includes `mountPoints`/`folders`/`documents`/`blobs`/`projectLinks` (each only when non-empty) |
| `lib/file-storage/errors.ts` (NEW, d553f72a) | `FileContentMissingError` (`:19`) — `storageKey` carried; default message `` `No stored content for storageKey: ${storageKey}` ``; `name = 'FileContentMissingError'`. Row exists, bytes don't; everything else stays generic |
| `lib/file-storage/manager.ts` | `downloadFile`: missing mount-blob throws `FileContentMissingError` (`:389`); the catch passes it through UN-wrapped (`:409`) at warn level `'File content is missing; the row outlived its bytes'` (no `Failed to download file '<name>':` prefix on this arm) |
| `lib/file-storage/backends/local/index.ts` | download's catch: ENOENT → warn `'Download found no file at the storage key'` + `FileContentMissingError(key, `` `No file at storage key '${key}'` ``)` |
| `app/api/v1/files/[id]/actions/download.ts:40`, `files/proxy/[...key]/route.ts:51` | `FileContentMissingError` → **404** `{"error":"File content not found"}` (warn logs); everything else stays 500 `{"error":"Failed to serve file"}` |
| `public/schemas/qtap-export{,-ndjson}.schema.json` | ndjson: description-only (doc-store kinds may appear in a `characters` stream; `project_doc_mount_link` never does). Monolithic: characters block gains optional `mountPoints`/`folders`/`documents`/`blobs`; `document-stores` gains `folders`; manifest settings gain `preserveIds`; `ExportedCharacter` gains the three archive fields; new `$defs/ExportedDocumentStoreFolder`; document/blob defs gain the id fields; `ExportedFile` description covers ARCHIVE + `/archives` |
| `lib/schemas/file.types.ts:23` | `FileCategoryEnum` gains `'ARCHIVE'` (v5 stores category as plain TEXT — update the stale value lists in `db/files.rs` doc comments at `:91`/`:164`) |
| `lib/file-storage/reconciliation.ts` | **MOOT for v5 this round** — v5 has not ported the filesystem reconciliation at all (`files_sync` refuses loudly, `crates/quilltap-core/src/api/files.rs:1594–1602`). Bug 53's guards (system-shelf folderPath suppression, `findAllRaw`, archive-ref preservation) are BANKED for whenever that subsystem is ported; record in the lane record, do not port now |

NOT this lane's: the three `characters` columns and everything that reads
them (P4.D63), the archive service / rehydrate / API actions / CLI
(round 2), the SPA (P4.D64).

## Mandate

Port the export/import/file-storage substrate so v5's `.qtap` machinery
matches v4 at `d553f72a` exactly: the vault-carrying character export, the
carried row ids, the `preserveIds` refuse-on-collision + skip-if-present
import path, the Bug-52 avatar remap, the Bug-54 sha256 dedup arms, and the
Bug-55 typed missing-content 404 — each with its differential regenerated
fresh at the pin. **Everything in this lane is column-free by design**: no
file in this lane reads `archivedAt`/`archiveFileId`/`archivedAvatarFileId`
(those structs are P4.D63's).

**This lane owns (and only touches):**

- `crates/quilltap-core/src/services/qtap_export/{mod,records,preview,entities}.rs`
  + a new `excluded_files.rs` (or module-local equivalent).
- `crates/quilltap-core/src/services/quilltap_import/{mod,characters,document_stores,entities,profiles,memories,reconcile,ndjson}.rs`.
- `crates/quilltap-core/src/services/file_storage.rs`;
  `crates/quilltap-host/src/files_store.rs` (the local backend's ENOENT
  arm); `crates/quilltap-web/src/files_routes.rs` (the two 404 arms).
- `crates/quilltap-core/src/db/doc_mount_*` **create paths only** as needed
  for the carried-id honor (`fileId`/`linkId`/`blobId`/folder `id` at the
  mint sites; existing rows found by sha256 or path keep their own id) —
  coordinate nothing: P4.D63 touches no doc-mount repo file.
- Harness: `crates/quilltap-harness/tests/{system_export_equivalence,system_import_equivalence,system_import_state,qtap_import_equivalence}.rs`;
  oracle cases `harness/oracle/cases/{system-export,system-import,system-import-execute}.test.ts`;
  the fixtures it extends (below). Web-edge: a bug-55 test beside
  `crates/quilltap-web/tests/`.

**Do NOT touch** `api/types.rs`, `api/engine.rs`, `api/characters.rs`,
`api/files.rs`, `db/characters*.rs`, `select_speaker.rs`, `delete_all.rs`,
`services/backup/**` (the backup/restore family is untouched by this half —
the wipe options are P4.D63's), or any `apps/web` file.

## Survey-verified starting points (2026-08-10)

- **v5 export writer**: `services/qtap_export/` — `mod.rs`: `build_manifest`
  `:180` (no `preserveIds` yet), `resolve_export_ids` `:226` (files arm's
  inline BACKUP filter `:268`), `stream_export_records` `:320`,
  `records_to_ndjson` `:395`. `records.rs`: `stream_characters` `:66` (NO
  vault emission today), `stream_document_stores` `:527` (per-id body
  inline — the `streamOneStore` extraction target; folder emit `:593`,
  document `:597–622`, blob `:628–676`, project links `:693+`),
  `stream_files` `:720` (inline BACKUP filter `:787`). `preview.rs`
  `preview_export` `:21` (no `vaults`; files filter `:193`). `entities.rs`
  `export_entities` `:26` (files filter `:134`). **Four** inline
  BACKUP-exclusion sites consolidate into the excluded-files predicate;
  `services/backup/collect.rs:600` is the backup service's own rule — v4
  did NOT touch it, leave it alone.
- **v5 import**: `services/quilltap_import/` — `mod.rs`: `ImportOptions`
  `:61`, `IdMaps` `:250` (ten maps today), `execute_import` `:343` (no
  preflight). `characters.rs` `import_characters` `:166` (creates via
  `db::character_vault::create_character`, which provisions a scaffold
  vault and nulls the incoming FK — comment `:151`). `document_stores.rs`
  `import_document_stores` `:72` (carries the P4.33 store-identity-by-ID
  divergence header — that ruling STANDS and composes with this port;
  folder create `:248–260` mints fresh UUIDs and keeps `parentId`
  unremapped — v4 has now CHANGED the ordinary path to path-based parent
  resolution, so this quirk-carry retires). `reconcile.rs`
  `reconcile_relationships` `:61–142` (remaps six FK families; **no
  `defaultImageId`/`avatarOverrides` handling — v5 reproduces Bug 52
  today, both halves, verified**). `ndjson.rs` blob accumulator has no id
  carry; `build_export_data_for_type` characters arm `:770–775`.
- **v5 file storage**: `services/file_storage.rs` — `StorageBackend` trait
  `:530` (`download → Result<Vec<u8>, String>` — string errors, no typed
  variant), `download_file` `:599` (mount-blob miss is a formatted string;
  the wrapper prefixes EVERYTHING with `Failed to download file '<name>':`,
  including the missing-content case v4 now passes through un-wrapped).
  Design the typed carry to fit the string-error trait (e.g. a sentinel
  prefix constant or a typed error enum on the manager layer — pick the
  smallest change that lets the web edge discriminate; document the choice).
  Host backend `crates/quilltap-host/src/files_store.rs` `download` `:155`
  (no ENOENT distinction today). Web arms: `files_routes.rs` `files_proxy`
  `:143` (500 `"Failed to download file"` at `:160` — note this wording is
  a PRE-EXISTING v5 divergence from v4's `Failed to serve file`; check and
  reconcile while there, with a case), `files_get` `:249+` (500
  `"Failed to serve file"` `:260`). v4's 404 body: `{"error":"File content
  not found"}`.
- **The embedding follow-up rewrite** (execute.ts): v5's equivalent import
  fan-out is `enqueueImportedMemoryEmbeddings`-shaped code inside
  `quilltap_import`. v4's new arm calls a debounced
  `scheduleRefit(userId, profileId)` for BUILTIN profiles — survey whether
  v5 has a refit scheduler (the TF-IDF builtin landed in W4.7e2). If the
  scheduler seam does not exist in v5, port the per-row enqueue + the
  no-profile warning exactly and defer the `scheduleRefit` call LOUDLY
  (named comment + lane record), never a silent stub.
- **Fixture blindness (load-bearing):** the committed `system-data-*`
  fixtures predate carried ids and content-bearing character vaults — the
  new arms are structurally INVISIBLE against them (the D24/P4.11
  one-mode-corpus lesson). This lane must extend them (below).

## Tiered deliverables

**Tier 1 (must land):**

1. The export writer: `stream_one_store` extraction + character-vault
   emission (exact record order: after `character_plugin_data`, before
   `memory`; `skipProjectLinks`; warn-and-continue on a failed vault) + the
   carried id fields at v4's exact key positions + the manifest
   `preserveIds` setting + the excluded-files predicate replacing all four
   inline filters + the preview `vaults` block
   (`summarizeCharacterVaults`'s exact arithmetic incl. the 4/3 base64
   inflation).
2. The reader: blob id-carry in the stream assembler +
   `build_export_data_for_type`'s conditional characters-arm blocks.
3. The `preserveIds` import path whole: types (`PreserveIdsMode`, the four
   new `IdMaps` members), `preflight_preserve_ids` (15 check kinds, both
   error sentences byte-exact, warnings-then-throw, no partial
   application), the per-entity create forks (characters / tags /
   templates / projects / groups / chats / memories / profiles /
   document-stores), skip-if-present arms, `skippedCharacterVaults`
   filtering, the folder maps + **path-based parent resolution on the
   ordinary path** (retire the v5 quirk-carry comment at
   `document_stores.rs:239`), `resolveTargetFolderId`, and the dead
   `getPreserveIdsCreateOptions` helper (faithful, with v4's shape).
4. **Bug 52**: the reconcile avatar remap — `defaultImageId` +
   `avatarOverrides[]` through the new `docMountFileLinks` map, the legacy
   `files.id` dual-shape check (`repos.files.findById` analog), cleared vs
   dropped semantics, both warning sentences byte-exact — plus
   `discardScaffoldVault` with its teardown-LAST ordering and canonical-name
   handback.
5. **Bug 54**: the sha256-first `skippable` classifiers (file + blob arms),
   consulted only in skip-if-present mode.
6. **Bug 55**: the typed missing-content carry end-to-end — local backend
   ENOENT arm (host), manager pass-through un-wrapped at warn, the two web
   404 arms with v4's exact bodies.
7. The differentials (see below), regenerated fresh at `d553f72a`, all
   green, new arms mutation-proven.

**Tier 2 (should land):**

8. The embedding follow-up rewrite in the import fan-out (per the survey
   note — `scheduleRefit` deferral allowed if the seam is unported, loud).
9. The `db/files.rs` stale category-list doc comments updated to include
   `ARCHIVE`; the proxy-arm 500 wording reconciliation (survey note), with
   a case if changed.

**Tier 3 (explicit loud deferrals):**

- Bug 53 / `lib/file-storage/reconciliation.ts` — BANKED whole (the
  subsystem is unported; `files_sync` already refuses loudly by name).
  Record in the lane record with the three guard summaries so the future
  port inherits them.
- The three archive fields on exported character records
  (`archivedAt`/`archiveFileId`/`archivedAvatarFileId`) — **round 2's**,
  with the service (see the Shared contract's cross-lane fixture rule; do
  not add struct fields or a shim here).
- `scheduleRefit` if the seam is unported (tier-2 note above).

## The differential requirement, restated

Every ported unit ships with an equivalence test driving v4's REAL code at
`d553f72a` (jest real-DB oracle recipes in each case file's header;
`jest-oracle-mechanism`, `jest-real-db-oracle`, `oracle-jest-testtimeout`).
The four families:

- `system_export_equivalence` (57 cases today, count asserted at `:275`) —
  extend with: a vault-bearing character export (stores/folders/nested
  folders/documents/blobs, carried ids asserted LITERALLY — the UUID
  normalizer is blind to fixture-baked ids, compare raw;
  `uuid-normalizer-blinds-fixture-baked-ids`), the excluded-ARCHIVE-files
  arm, the preview `vaults` block, `preserveIds` in the manifest.
- `system_import_equivalence` (27) — reader id-carry cases.
- `system_import_state` (23) — the preserveIds arms: refuse-on-collision
  (both error shapes), skip-if-present (target-scoped skips + the
  anywhere-else refusal), the Bug-54 sha256 dedup arm (shared content +
  same-id/different-bytes refusal), the Bug-52 remap cases (dangling
  `defaultImageId` cleared + warning; unremappable override dropped;
  legacy `files.id` kept; scaffold teardown — diff the store tables +
  masked warnings per the family's existing normalizer discipline), and an
  ordinary-path nested-folder case pinning the NEW path-based parent
  resolution (this one would have been green-by-luck before — mutation-prove
  it).
- `qtap_import_equivalence` — re-run; the seed bundle predates the id
  fields so it should be output-neutral (verify, don't assume).
- Bug 55: a `quilltap-web` route test (row-without-bytes → 404 body
  byte-exact; generic failure stays 500) — v4's semantics are pinned by the
  bug doc; no oracle NDJSON needed for a fixed-sentence HTTP arm, but the
  sentences come from v4's `responses.ts`, quoted above.

First-run-green cases must be mutation-proven (the D24 rule). Grep every
regenerated NDJSON for a fresh marker (`oracle-regen-silent-stale-pass`).

## Fixtures

- **Consumes** the committed `system-data-{main,mount,llmlogs}.db` family +
  `harness/oracle/fixtures/system-data.json` + the seed
  `lorian-and-riya.qtap`.
- **Owns extending** the `system-data-*` family with: a character whose
  vault carries folders (nested), documents, and blobs (shared-content
  rows for the Bug-54 arm), plus ARCHIVE-category `files` rows for the
  exclusion arm. **⚠ Extend by MUTATING copies of the committed DBs via v4
  repos at `d553f72a`, then commit the mutated family — do NOT rebuild
  from scratch**: a fresh `d553f72a` build carries the three archive
  columns and drags the sibling-owned three-key export carry into this
  lane's diffs (the Shared contract pins this boundary). The committed
  bytes rule (`sweep-recipes-write-into-the-repo`) applies: run
  fixture-mutating recipes against /tmp copies.
- **Owns** a small committed preserveIds bundle fixture (a `.qtap` with
  carried ids) for the import arms — built by v4's REAL writer at the pin
  (the `restore-archives/` precedent: the fixture's authorship is v4's, so
  the claim never depends on v5's writer).
- **Owns regenerating every oracle of a fixture it moves** — after
  widening `system-data-*`, re-run ALL system-family differentials by name
  (export, import, import-state, backup, restore-state, delete-data — the
  P4.9G3 widened-fixture lesson), not just this lane's.

## Shared contract (BINDING — identical in P4.D62 / P4.D63 / P4.D64)

Names are law; v4 shapes at `d553f72a`.

1. `characterList` gains `archived?: "include" | "only"` (absent/other =
   exclude, v4's default) — applied at the API list handler (the single
   chokepoint), never in the repository. List items gain
   `archivedAt: string|null` (ALWAYS present, `?? null`). Detail responses
   gain `archivedAt: string|null` + `archiveFileId: string|null`.
   **Owner: P4.D63** (P4.D64 mirrors).
2. NEW dispatch verbs `characterArchive { id }` / `characterRehydrate
   { id }` + REST `POST /api/v1/characters/{id}?action=archive|rehydrate`
   — DEFINED in P4.D63 answering the loud typed not-yet-available refusal
   (the P4.9G1 pattern); round 2 fills them. Round-2 success shapes,
   pinned now: archive → `{ archived: bool, archiveFileId: string|null,
   pruneComplete: bool }`; rehydrate → `{ rehydrated: bool, archived:
   bool, archiveBundleFileId: string|null, restored?: { memories,
   documents, blobs }, warnings: string[] }`. The passphrase-unavailable
   400 sentence: `The archive cannot be sealed: your passphrase has not
   been entered since Quilltap started. Unlock once (or restart and
   unlock), then archive again.`
3. The files list gains a `category` exact-match filter (REST
   `GET /api/v1/files?category=`; dispatch mirror). **Owner: P4.D63.**
4. `systemDeleteData` gains `keepArchivedCharacterBundles?: bool`
   (effective default TRUE — `!== false`); `DeleteSummary` gains
   `archiveBundles: number` + `archiveBundlesKept: boolean`; the preview
   reports `archiveBundles` with `archiveBundlesKept: true`.
   `systemRestoreExecute` gains `keepArchivedCharacterBundles?: bool`
   (replace-mode wipe only; accepted in both modes). **Owner: P4.D63.**
5. The change-passphrase response gains `archives: { total, reencrypted,
   failures: [{fileId, filename, reason}] }`; `total: -1` = the sweep
   itself failed; a failed sweep does NOT fail the passphrase change.
   **Owner: P4.D63.**
6. Export preview (`characters` type) gains optional `vaults: { stores,
   documents, blobs, estimatedBytes }`, present only when `stores > 0`.
   **Owner: P4.D62** (this lane).
7. Group-member projections gain `archivedAt`; add-member refusals are 400
   `That character is archived; rehydrate them before adding them to a
   group.` / `…before adding them to a roster.` The chat-participant
   enrichment projects `archivedAt ?? null`. **Owner: P4.D63.**
8. Error sentences are byte-contractual across all lanes — the
   `CharacterArchivedError` message, the tool/action refusal sentences,
   the three archive-crypto sentences, the preserveIds collision shapes,
   and the 404 body `{"error":"File content not found"}`.
9. **Cross-lane fixture rule:** the committed `system-data-*` fixtures
   keep their OLD schema this round (P4.D62 extends by mutation, never
   rebuild). v4 reading an old-schema DB emits NO archive keys on exported
   character records (Zod-optional, absent column → absent key), so
   P4.D62's families stay honestly green without P4.D63's columns. The
   three-key export carry on character records is ROUND 2's, covered by
   the archive-service differential (a bundle IS an export).
10. SPA gating: `CHARACTER_ARCHIVE_SERVER_LANDED = false` in P4.D64's e2e
    spec — flipped in ROUND 2, not at this round's unification. Beats that
    need only a tombstoned ROW (roster toggle/badges, read-only detail,
    group members line, participant badge) ride a fixture-seeded archived
    character, ACTIVATE-AT-UNIFY behind `ARCHIVE_TOMBSTONE_SEEDED` (the
    unifier seeds it per P4.D64's seeding note and flips the constant).

## Ownership (identical across the three orders)

- **P4.D62 (this lane)**: `services/qtap_export/**`,
  `services/quilltap_import/**`, `services/file_storage.rs`,
  `crates/quilltap-web/src/files_routes.rs`,
  `crates/quilltap-host/src/files_store.rs`, `db/doc_mount_*` create-path
  id-honor only, its harness families + the `system-data-*` extension +
  the preserveIds bundle fixture. **Bumps core, harness, web, host.**
- **P4.D63**: `api/types.rs`, `api/engine.rs`, `api/characters.rs`,
  `api/files.rs`, `api/salon.rs` (participant enrichment),
  `api/chat_post_office.rs` (mailbox/send-mail refusals),
  `db/characters*.rs`, `db/chats*.rs` (setParticipantStatus),
  `db/vault_wardrobe_write.rs`, `db/character_resolver.rs`,
  `select_speaker.rs`, `services/{orchestrator,participant_resolver}.rs`
  (probe + refusal arms), `tools/{self_inventory,send_mail,list_email}.rs`,
  the doc-edit path-resolver + conversation-summary vault-bridge modules,
  `services/delete_all.rs`, `services/backup/restore/orchestrator.rs` (+
  types) for the wipe option, the five embedding one-default sites, NEW
  `services/character_archive/` (crypto + reencrypt), `dbkey.rs` +
  engine-held passphrase cache,
  `services/provisioning/fresh_schema.json` (D23 re-dump) + the boot
  ensure, `crates/quilltap-web/src/{system_data_routes,backup_routes}.rs`.
  **Bumps core, harness, web.** (Both Rust lanes bump core/harness/web —
  identical bumps auto-merge silently; the unifier recounts as base +
  total bumps.)
- **P4.D64**: `apps/web/**` only. **Bumps the SPA version.**
- `CHANGELOG` and `status-log.md` are append-only for all lanes
  (union-merge).

## Verification gate (before declaring done)

- `cargo fmt --all` clean; `cargo clippy --workspace --all-targets` clean
  on BOTH feature sets.
- `cargo test --workspace` green with this lane's env block
  (`QT_ORACLE_SYSTEM_EXPORT`, `QT_ORACLE_SYSTEM_IMPORT`,
  `QT_ORACLE_SYSTEM_IMPORT_EXECUTE`, `QT_ORACLE_QTAPIMPORT` + its fixture
  vars) — confirm all four families RAN by name with `--nocapture`, zero
  SKIP lines (`cargo test --workspace` is fail-fast per binary).
- The widened-fixture re-run: every system-family differential by name
  (incl. P4.D63-adjacent `system_delete_data`, backup + restore families)
  green against the extended `system-data-*`.
- `cargo build --release` clean. No `apps/web` change in this lane.
- Per-commit rule: follow `.claude/commands/commit.md`; end commit messages
  with the Co-Authored-By trailer.
