# Work order P4.D46 — the `7189a968` export/import/backup drift, server side: no embeddings in exports, all fifteen types, compact backup

> **STATUS: OPEN — not started.** Written 2026-08-05 at round planning
> (the `7189a968` round). This is the drift catch-up the 2026-08-04
> dogfood walk STOPPED for (phase-4.md candidate 0 — "if it has landed,
> this is the round"; the walk record names the blast radius).
>
> **v4 baseline for this round: `7189a968`** (v4 HEAD, 2026-08-05, clean
> tree — "feat(export): drop embeddings from exports, reach all fifteen
> types, compact backup", 44 files / +4859). The previous baseline was
> `7fe9fe40`; the seven-commit drift is classified in the "Round
> planned" record in `status-log.md`: `7189a968` → THIS lane + P4.D47;
> `be2c9cbb` → P4.D48; four infra/docs commits dispositioned in P4.D48.
> **The baseline move is the UNIFIER's** — do NOT edit `CLAUDE.md` or
> `phase-4.md` on the branch.
>
> **Drift-check at lane start**: `git log 7189a968..HEAD --oneline` +
> `git status --short` in `~/source/quilltap-server`. On drift/dirty,
> regenerate oracles from a detached worktree PINNED at `7189a968`
> (recipe `oracle-regen-pinned-v4-worktree`), and report in the lane
> record. ⚠ P4.D48 will run `npm install` in the v4 checkout (the SDK
> unfreeze verification) — that leaves tracked files clean, but
> coordinate: if `git status` shows a dirty lockfile, that's the
> sibling's install, not drift.
>
> **Siblings (same round):** P4.D47 (this feature's SPA half — the
> §1 shared contract below is binding between us), P4.D48 (SDK wire
> check + infra dispositions), P4.36 (context-summary oracle un-mock).
>
> **Worktree note:** own branch, own checkout; no `git stash`/
> `git worktree`.

## Mandate

Port v4 `7189a968` server-side, end to end: the embedding strip
(writer + reader + the per-memory `EMBEDDING_GENERATE` enqueue), the
five new export types (`files`, `prompt-templates`, `provider-models`,
`plugin-configs`, `instance-settings`) and the exhaustive
entity-listing (+`groups`, +`document-stores` — **note: v4's `projects`
was ALREADY listed pre-drift and v5 already answers it**, so the
listing gains SEVEN arms, not eight), the document-stores-before-
group-links ordering fix, compact backup, the restore-tail additions
(step 24a compact re-index + step 25 embedding reconcile +
`RestoreSummary.embeddingReconcile`), and the plugin-config `enabled`
carry. Every unit arrives with its differential over v4's REAL code at
`7189a968`. Do NOT touch: `apps/web/**` (P4.D47's),
`crates/quilltap-web/src/main.rs`, the provider corpora (P4.D48's),
the context-summary family (P4.36's).

## The v4 anatomy (surveyed fresh 2026-08-05 at `7189a968` — file:line verified)

**The canonical touch-list is v4's own:** `lib/export/types.ts:31-53`
carries a cross-cutting-change checklist naming every layer that must
gain a case — port it verbatim as a doc comment on v5's
`EXPORT_ENTITY_TYPES`. The fifteen types (`types.ts:54-69`):
`characters, chats, roleplay-templates, prompt-templates,
connection-profiles, image-profiles, embedding-profiles, tags,
projects, groups, document-stores, files, provider-models,
plugin-configs, instance-settings`.

**Forward-compat rule** (`types.ts:48-52`): NDJSON envelope stays
`version: 1`; a reader **warns-and-skips** an unknown record `kind` but
**throws** on an unknown `manifest.exportType`. Verify v5's assembler
honors both halves.

### The embedding strip

- Writer: `stripEmbedding` (`ndjson-writer.ts:110-113`), called at the
  two memory emit sites `:215` (characters) and `:329` (chats).
  `QtapMemoryRecord.data = Omit<Memory,'embedding'>` (`types.ts:654`).
- Reader drop site 1 — the NDJSON reassembler
  (`quilltap-import-stream.ts:236-247`): destructures `embedding` off
  and keeps the rest, with the "archives written before that change
  carry it" comment.
- Reader drop site 2 — the legacy monolithic path
  (`import-entities.ts:450-458`): the create-strip gains `embedding`,
  and the function's signature widens to return
  `createdIds: Array<{id, characterId}>` (`:400-406`, pushed `:466`).
- The enqueue: `execute.ts:62-138` `enqueueImportedMemoryEmbeddings`
  (never throws), called AFTER `reconcileRelationships` at `:532`.
  Per-memory `enqueueEmbeddingGenerate(userId, {entityType:'MEMORY',
  entityId, characterId, profileId})` (`:106-111`); dedupe per entity;
  bail-outs with EXACT warning strings: no default profile / BUILTIN
  profile → `"${n} memories were imported without embeddings because
  ${reason}; they will be indexed once an embedding profile is
  configured."` (reason ∈ {`no default embedding profile is
  configured`, `the configured embedding profile is the built-in
  TF-IDF one`}); partial → `"${missing} of ${total} imported memories
  could not be queued for embedding; the next startup sweep will pick
  them up."` Explicit anti-decision at `:56-58`: NOT one
  `EMBEDDING_REINDEX_ALL`.

### The five new types — record shapes (field-for-field, from the survey)

- **files**: `ExportedFolder {id, path, name, parentFolderId?|null,
  projectId?|null}` (writer always emits all five, `ndjson-writer.ts:664-673`);
  `ExportedFile` = `FileEntry` minus `userId`/`storageKey` plus
  `_sourceStorageKey?|null` + `_bytesMissing?` (`types.ts:411-417`);
  byte pair `file_blob {kind, fileId, sha256, sizeBytes, chunkCount}` +
  `file_blob_chunk {kind, fileId, index, total, dataBase64}`
  (`types.ts:751-773`), 3 MiB chunks (mult-of-3 rule — v5's
  `BLOB_CHUNK_BYTES` at `qtap_export/mod.rs:52` already matches).
  Writer `streamFiles` (`ndjson-writer.ts:648-742`): whole folder tree
  regardless of selection, sorted by `path.length`; per file
  metadata → blob → chunks; BACKUP-category/`/backups` excluded
  (`:691`); unreadable bytes → log + `_bytesMissing: true`, blob
  records omitted entirely.
- **prompt-templates**: `{promptTemplates: PromptTemplate[]}`; writer
  filter `:754`: built-ins and foreign-owner rows never travel.
- **provider-models**: instance-global (userId unused); doc note: a
  regenerable cache, exportable for air-gap convenience.
- **plugin-configs**: `ExportedPluginConfig` = `PluginConfig` minus
  `userId` plus `_redactedKeys?: string[]`. Redaction
  (`ndjson-writer.ts:781-833`): manifest unresolvable →
  `_redactedKeys: ['*']` + `config: {}` (never guess); else
  password-typed keys withheld; the `_redactedKeys` key is **omitted,
  not empty**, when nothing was redacted.
- **instance-settings**: `{key, value}` pairs only, from
  `listPortableInstanceSettings()`; selection keyed by `key` (no id
  column). The non-portable exclusion set
  (`lib/instance-settings/index.ts:62-68`), exact strings:
  `lanternBackgroundsMountPointId`, `userUploadsMountPointId`,
  `generalMountPointId`, `lastMaintenanceSweepAt`,
  `highest_app_version`.

### Listing, preview, reader, execute

- `handleExportEntities` (`app/api/v1/system/tools/route.ts:444`,
  switch `:462`) — fifteen cases; name shapes pinned in the §1
  contract below. `document-stores` reads the INSTANCE-scoped repo.
- `previewExport` (`quilltap-export-service.ts:254-332`) five new
  cases; `preview.ts:161-228` six new preview arrays listed FIRST in
  `entities`, each previewed against **the key its importer dedupes
  on** (files by id + `detail: 'contents missing — will be skipped'`
  on `_bytesMissing`; prompt-templates by name + `matchedExistingId`;
  provider-models `exists: false` always; plugin-configs by
  user+plugin with the two `detail` redaction strings; instance-settings
  `exists: false` always). `ImportPreviewEntity.detail?: string`.
- Reassembler (`quilltap-import-stream.ts:96-108, 315-529`): new state
  incl. `fileOrder` (emission order preserved), `fileBlobAccumulators`;
  the **files chunk path uses the counted-arrivals pattern from day
  one** (`:377-389` — "Same trap as the doc-store blob path above");
  files truncation check `:432-441` with its own message;
  `CollectedArrays` names the files-folders field **`fileFolders`** to
  avoid the doc-store `folders` collision — v4 then shares the ONE
  untyped `AnyExportData.folders` slot at the execute boundary
  (`execute.ts:488` cast); **the Rust port uses two distinct typed
  fields** (v4's own hazard note endorses this; behavior identical).
- Importers: `import-files.ts` (321 L — invariants: storageKey never
  transfers; post-bridge mime/size win; `remapLinkedTo` three tiers,
  drop-the-rest because cascade-delete reads `linkedTo`; folders
  parents-first with `findByPath` reuse; project-bound → project-store
  bridge, else user-uploads bridge with NEW subfolder `'imported'`;
  per-file/per-folder warnings verbatim; aggregate dropped-links
  warning). `import-configuration.ts` (229 L — prompt templates
  deduped by NAME with `skip`/`overwrite`/`duplicate`→
  `"${name} (imported)"`; provider models upsert by
  `(provider, modelId)`; plugin configs via `upsertForUserPlugin(...,
  config.enabled)` — merge semantics load-bearing: a redacted key
  doesn't overwrite the local secret — plus the two `_redactedKeys`
  warning strings; instance settings unconditional overwrite,
  conflict strategy does NOT apply).
- **The ordering fix**: `importDocumentStores` moves from step 9 (dead
  last) to step **7c** (`execute.ts:382-407`) — before the group
  member/store-link step (7d, `:409`), after projects. Pre-fix,
  `idMaps.mountPoints` was empty at link time and every group→store
  link silently dropped. New steps: 9 = general file library
  (`:483-498`), 10 = the four configuration importers (`:500-524`).
- `upsertForUserPlugin` (`lib/database/repositories/plugin-config.repository.ts:154-183`)
  gains `enabled?: boolean` — `undefined` leaves the flag UNTOUCHED on
  both branches (tri-state); restore passes it at `restore.ts:301-306`.

### Compact backup + the restore tail

- `compactBackupData` (`backup-service.ts:557-572`): memories keep
  every word, `embedding: null`; six derived collections emptied:
  `conversationChunks, vectorEntries, vectorIndexMetas,
  tfidfVocabularies, embeddingStatus, docMountChunks`. `llmLogs`
  untouched. Staging **omits the six files entirely** (`:672`, `:683`)
  — absent ≡ empty to the optional readers, and absent is what shrinks
  the archive. Manifest gains `compact: true` — **key omitted when
  false** (`:445`).
- Route semantics (`app/api/v1/system/backup/route.ts:23-28`): body
  optional; malformed body treated as ABSENT, not rejected; only
  literal `true` engages.
- Restore step **24a** (`restore.ts:873-907`), gated on
  `manifest?.compact`: resolve default profile →
  `enqueueEmbeddingReindexAll(userId, {profileId, scope:'all'})`
  **BEFORE** the reconcile (so the reconcile's dedupe sees it); three
  warning variants verbatim. Step **25** (`:910-938`):
  `reconcileEmbeddingDimensions()` immediately before the summary is
  built; never throws; result → `RestoreSummary.embeddingReconcile
  {targetDimensions, skippedReason, vectorEntriesDeleted,
  vectorIndexMetaFixed, reindexEnqueued}` (`types.ts:586-592`) + two
  warning branches.

### v4's new tests (the oracle-case menu — survey §13 has the full table)

`compact-backup.test.ts` (file omission, not empty), 
`restore-embedding-reconcile.test.ts` (once-only + ordering:
reindex-before-reconcile), `import-files.test.ts` (both invariants +
the three remap tiers), `import-memory-embeddings.test.ts` (1:1
fan-out, never REINDEX_ALL, both bail-outs, never-throws),
`import-order-group-stores.test.ts` (the call-order assertion),
`portable-settings.test.ts` (the 5-key set is exported and
authoritative), and `ndjson-roundtrip.test.ts`'s new describes (the
embedding strip in BOTH serialized forms; the multi-chunk file blob
round-trip).

## The v5 starting points (surveyed fresh 2026-08-05 on main)

- Export service: `crates/quilltap-core/src/services/qtap_export/`
  — `EXPORT_ENTITY_TYPES` (ten today) at `mod.rs:59-70`; the FIVE
  dispatch tables that must each grow arms: `resolve_export_ids`
  (`mod.rs:189-223`), `stream_export_records` (`mod.rs:264-286`),
  `export_entities` (`entities.rs:36-116` — answers EIGHT today incl.
  projects; the v4-asymmetry comments at `api/system_qtap.rs:34-37`,
  `entities.rs:5-8`, `entities.rs:114` all DIE with this port), plus
  the read side `build_export_data_for_type` (`ndjson.rs:599-660`) and
  `preview_export` (`qtap_export/preview.rs`).
- **v5 writes `embedding` today** at `records.rs:97-104` and
  `:232-241`, sourced from `db/memories_read.rs:149-157` (the
  `JSON.stringify(Float32Array)` object shape, `:99-107`). The
  key-order template pins `embedding` at index 10 of the `memory`
  record — **re-dump `schema-key-order.json` from v4 at `7189a968`**
  via `harness/oracle/fixtures/dump-export-key-order.ts`. Update the
  standing irony comment at `quilltap_import/memories.rs:10-19`.
- Import: `quilltap_import/` — assembler `ndjson.rs:173` (doc-blob
  chunk counting already the CONVERGED counted pattern, `:447-452` —
  the retired sparse-array pin's comment at `:421-451` stays
  historical); execute orchestrator `mod.rs:327/411` with the step
  table in the lane survey (memories step 8 at `:694-701`; document
  stores CURRENTLY step 9 at `:705` — the ordering fix moves it before
  the group-links step at `:635`); `import_memories` at
  `memories.rs:57` is v5's ONLY memory path (v4's two reader drop
  sites collapse to: the assembler arm + a defensive strip in
  `import_memories`, covering the legacy `parse_export_file` path at
  `mod.rs:259` too).
- **The enqueue seam (named here so the lane doesn't re-derive it):**
  `execute_import` runs INSIDE `db.write(...)` over raw connections
  (`api/system_qtap.rs:276-299`), so the async
  `queue_service.rs:176 enqueue_embedding_generate(&Db, …)` CANNOT be
  called from it. Mirror `mount_index/embedding_scheduler.rs:29`'s
  sync connection-level shape as
  `enqueue_memory_embedding(main: &Connection, …)` (same dedupe via
  pending-for-entity + `BackgroundJobsRepository::create`, MEMORY
  priority 10.0, maxAttempts 3, payload key order
  `{entityType, entityId, characterId, profileId}` per
  `api/memories.rs:1428-1447`); default profile via a `_conn` twin
  (`embedding_scheduler::default_profile_id` exists at `:73`). Keep
  v4's call position: after the reconcile, inside the import body.
- Backup/restore: collect `backup/collect.rs:380` (the 38 collections
  already include all five candidate tables; memories collected
  through `memories_read` so they carry the embedding object — compact
  nulls it); staging `backup/staging.rs:62/158` (the six data-file
  omissions); manifest `backup/manifest.rs:23` (`backupFormat: 4` at
  `:94` — compact is a NEW key, not a format bump); restore
  orchestrator `backup/restore/orchestrator.rs:75/166`, phase 15
  plugin-configs at `:693-711` (currently DROPS archived `enabled` —
  the fix passes it through a widened repo upsert), instance settings
  at `:1056`, and the summary built at `:1103` — steps 24a/25 slot
  immediately before it. `reconcile_embedding_dimensions` already
  exists with EXACTLY the right shape:
  `services/embedding_dimension_reconcile.rs:265`
  `(main: &Connection, mount: Option<&Connection>, now_ms) →
  DimensionReconcileResult`, sync, infallible, enqueues its own
  deduped reindex — the boot caller at `quilltap-host/src/host.rs:840`
  is unchanged.
- Repos: `db/plugin_config.rs:242 upsert_for_user_plugin` takes NO
  enabled flag (both arms hard-code `None`, `:285/:297`) — widen with
  `Option<bool>` = untouched (v4's tri-state). `db/instance_settings.rs`
  has NO list-all reader (`read_setting`/`write_setting` private,
  `:58/:70`) — add the portable-list reader + the exclusion-set const
  + a public write for the importer. `db/provider_models.rs` and
  `db/prompt_templates.rs` exist (check upsert-by-(provider,modelId)
  and find-by-name respectively; add what's missing). The user-uploads
  bridge needs the `'imported'` subfolder member.
- Web edge: `quilltap-web/src/qtap_routes.rs` (export `:33`, preview
  `:133`, execute `:184`), `backup_routes.rs:50` (backup POST — gains
  the tolerant `compact` body parse), `system_data_routes.rs:89/100`
  (export-entities/-preview actions). §1 verbs at
  `api/types.rs:2310-2355`: **`SystemBackupCreate` (`:2316`, currently
  fieldless) gains `compact`** — see the contract. Entity types travel
  as strings, so no other §1 change is expected; if one becomes
  necessary, pin it in the lane record and notify P4.D47 via the
  contract's change rule.
- CLI: nothing needed (verified — `db_cmd.rs` backup verb is a loud
  no-ship; `docs_cmd.rs` "export" is the unrelated mount-store verb).

## Divergence pins to re-check at the new baseline (each is a named task, not a vibe)

From the standing inventory (all in this family): `PHASE_ORDER_RESIDUAL`
(`system_restore_state.rs:118-209` — `7189a968` does NOT move v4's
files phase, so it should survive; verify), `V5_STATS_GAP` (`:211-248`),
`REPLAY_DEDUPE` (`:250-300`), the orphan-skip `ORPHAN_LINKS_CASE`
(`:1781-1802`), `FOLDER_CLEAR_DIVERGENCE` +
`STORE_ID_PRESERVED_ON_CREATE` + the four store-identity arms
(`system_import_state.rs:312-360, 987-1060`), `ANNOTATION_DIVERGENCE_KEY`
(`system_delete_data_equivalence.rs:135-158`), the skipped-files
reporting (`api/system_backup.rs:51-66`). The sparse-array pin is
already DISCHARGED (v4 converged at `c1507f47`; the new file-blob path
was born converged). Any pin that trips is a finding — diagnose before
touching it (a convergence retirement is good news; a re-break is a
v4-side item).

## Deliverables

**Tier 1 (must land):**

1. The embedding strip end to end: writer (both emit sites +
   re-dumped key order), assembler drop arm, `import_memories`
   defensive strip + `created_ids` return, the sync enqueue helper +
   the post-reconcile call with v4's exact warning strings.
   Differential: `system_export_equivalence` extended with
   strip arms (an embedding-bearing FIXTURE memory must export
   WITHOUT the field — both serialized forms, per v4's
   ndjson-roundtrip cases); `system_import_state` extended to DIFF
   `background_jobs` for the per-memory `EMBEDDING_GENERATE` rows
   (count, payload shape, dedupe) and the no-profile/BUILTIN warning
   arms.
2. The five new export types + the seven new listing arms + preview
   both sides (export preview + import preview's six arrays with
   `detail`), the assembler's new record kinds (counted chunk pattern,
   emission-order preservation, the two distinct folder fields), the
   `import_files` + `import_configuration` importers with all four
   conflict strategies and every warning string byte-for-byte, the
   `'imported'` uploads subfolder. Differentials:
   `system_export_equivalence` + `system_import_equivalence` +
   `system_import_state` each gain per-type arms (the multi-chunk
   file-blob round-trip is the chunk oracle; prompt-template built-in
   exclusion; plugin-config redaction incl. the manifest-unresolvable
   `['*']` arm; the instance-settings 5-key exclusion asserted from
   the exported CONST, not a copy).
3. The ordering fix (document stores to 7c) — pinned by a
   group→store-link import case that FAILS on the old order
   (mutation-proof: move it back, the link drops).
4. Compact backup + restore tail: `compact` through
   `SystemBackupCreate` → route (tolerant body parse) → collect/staging
   (six files ABSENT, memories `embedding: null`, manifest key omitted
   when false) → restore 24a (reindex-enqueued-BEFORE-reconcile,
   gated on the manifest flag) + 25 (reconcile + summary field +
   warnings). Differentials: `system_backup_equivalence` gains the
   compact case (file-absence asserted, manifest flag, memory rows);
   `system_restore_state`/`system_restore_equivalence` gain the
   24a/25 arms (the `background_jobs` reindex row, its
   enqueue-before-reconcile ordering, `embeddingReconcile` in the
   summary) over a NEW compact archive built by v4's REAL
   `createBackup` (the committed `restore-archives/` rule — a separate
   builder so the existing nine zips stay byte-untouched).
5. Plugin-config `enabled`: the widened tri-state upsert + restore
   phase 15 + import both passing it — `system_restore_state` diffs
   `plugin_configs` row-for-row already; add the switched-off-plugin
   arm to the fixture so the claim is measured.
6. The fixture widening: `system-data-*` gains rows for the new types
   (files WITH bytes incl. a >3 MiB multi-chunk one, a built-in +
   user prompt template, provider models, plugin configs with
   password-typed manifest keys AND one with an unresolvable manifest,
   instance settings incl. non-portable keys, a group with linked
   stores for the ordering case). **The committed-fixture rule**: the
   widened fixture is rebuilt ONCE, committed, and EVERY consuming
   family regenerates over it (export / import / import-execute /
   backup / delete-data / restore-vintage share it — the P4.9G3
   precedent); run fixture-mutating recipes against /tmp copies.
7. Every family in the blast radius regenerated fresh at `7189a968`
   and re-run by name, zero SKIP: `system_export_equivalence`,
   `system_import_equivalence`, `system_import_state`,
   `system_backup_equivalence`, `system_restore_equivalence`,
   `system_restore_state`, `backup_uuid_remap_equivalence`
   (neutrality), `system_delete_data_equivalence` (neutrality),
   `backup_mount_index_coercion_equivalence` (neutrality),
   `qtap_import_equivalence` (neutrality — the seed corpus carries no
   embeddings). First-run-green families get a mutation fingerprint
   (the D24 rule).
8. The divergence-pin re-check (the section above) — each pin's
   verdict recorded in the lane record.
9. Doc mirrors: `import_export_spec.md` (new, 454 L) +
   `import_export_update.md` (moved to `complete/`) under
   `docs/v4/developer/features/`; the DDL "Portability (4.8+)" note if
   `docs/v4` mirrors DDL.md. Help docs need no port (runtime sync —
   verify and say so).

**Tier 2 (should land):**

10. v4's touch-list comment ported onto `EXPORT_ENTITY_TYPES` and the
    forward-compat rule (warn-skip unknown kind / throw unknown
    exportType) explicitly asserted in `system_import_equivalence`
    (a synthetic future-kind line must warn-and-skip on BOTH sides).
11. Update `p4.35-streaming-qtap-import.md`'s "check this before
    starting" header with the measured post-drift reality (the 791 MB
    was 99.7% embeddings; a fresh export of the same library should
    now be ~MBs — re-measure on the dogfood copy is the WALK's job,
    but the header should say the strip landed).

**Tier 3 (explicit refusal-arm deferrals, loud):** none planned. If a
new-type importer turns out to need a seam v5 lacks (e.g. an image
codec path for file bytes), refuse loudly by name with the standing
pattern and enumerate it here in the status header — never a silent
stub.

## The differential requirement, restated

Every unit above ships with its equivalence test driving v4's REAL
code at `7189a968` (jest oracle → NDJSON → Rust diff). The closest
recipes: the family headers themselves (`system-export.test.ts:1-35`
is the canonical shape — /tmp mirror + `QT_FIXTURE_SD_*` +
`QT_ORACLE_OUT`), the `jest-real-db-oracle` + `jest-oracle-mechanism`
memory notes, and `build-restore-archives*.test.ts` for archive
builders. Oracle output freshness per `oracle-regen-silent-stale-pass`
(grep for a new-baseline marker). TZ pins where the recipes carry
them.

## Fixtures

Consumes + widens (owns the rebuild + ALL consumer regen):
`crates/quilltap-web/tests/fixtures/system-data-{main,mount,llmlogs}.db`
(+ `build-system-data-fixture.ts` + `system-data.json`). Delivers: the
new compact restore archive under
`crates/quilltap-web/tests/fixtures/restore-archives/` (own builder
file; existing nine zips byte-untouched — re-proven by
`system_backup_equivalence`). Re-dumps:
`qtap_export/schema-key-order.json`. Untouched: `migration-vintage/`,
`uuid-remap-corpus.json` (neutrality re-run only),
`lorian-and-riya.qtap`.

## Verification gate before declaring done

- `cargo fmt --all --check`; clippy `-D warnings` BOTH feature sets;
  `cargo build --release`.
- The tier-1 item-7 family list by name, `--nocapture`, zero SKIP,
  oracles fresh at `7189a968`.
- `cargo test --workspace --no-fail-fast` with the round's env block —
  no collateral (the `restore_vintage_state` invariant suite runs
  unconditionally; it must stay green over the widened fixture).
- No `apps/web` change → no SPA gate owed (P4.D47 carries it).
- Per-commit rule: `.claude/commands/commit.md`. Version bumps: see
  Ownership.

## Shared contract (BINDING — verbatim-identical in P4.D46 and P4.D47)

**C1 — the fifteen export types, picker order, and labels** (v4
`ExportTypeStep.tsx:20-36` order; labels `types.ts:42-58`):

| # | id (kebab, wire) | label |
|---|---|---|
| 1 | `characters` | Characters |
| 2 | `chats` | Chats |
| 3 | `roleplay-templates` | Roleplay Templates |
| 4 | `prompt-templates` | Prompt Templates |
| 5 | `connection-profiles` | Connection Profiles |
| 6 | `image-profiles` | Image Profiles |
| 7 | `embedding-profiles` | Embedding Profiles |
| 8 | `tags` | Tags |
| 9 | `projects` | Projects |
| 10 | `groups` | Groups |
| 11 | `document-stores` | Document Stores |
| 12 | `files` | Files & Folders |
| 13 | `provider-models` | Provider Models |
| 14 | `plugin-configs` | Plugin Settings |
| 15 | `instance-settings` | Instance Settings |

camelCase preview keys (the `toExportEntityType` additions):
`promptTemplates, projects, groups, documentStores, files,
providerModels, pluginConfigs, instanceSettings`.

**C2 — the compact-backup wire.** `CoreRequest::SystemBackupCreate`
gains exactly one field, wire key **`compact`**, boolean, default
false. Edge semantics (v4 `backup/route.ts:23-28`): request body
optional; a malformed body is treated as ABSENT, not rejected; only
JSON literal `true` engages compact. The SPA sends `{ compact }` on
the existing backup-create call. The manifest key `compact: true` is
omitted when false.

**C3 — the import preview additions.** `entities` gains six OPTIONAL
arrays, serialized in v4's order (listed before characters/chats/
tags): `documentStores, files, promptTemplates, providerModels,
pluginConfigs, instanceSettings`. Every entity item may carry
`detail?: string`; the three detail strings, verbatim:
`contents missing — will be skipped` ·
`all settings withheld — re-enter them here` ·
`secrets withheld: <k1, k2>` (comma-space joined). `promptTemplates`
items may carry `matchedExistingId`; `providerModels` and
`instanceSettings` items always have `exists: false`.

**C4 — the entity-listing name shapes** (server produces, SPA
renders as-is): files → `originalFilename` (BACKUP-category and
`/backups` rows excluded); prompt-templates → `name` (built-ins and
foreign-owner rows excluded); provider-models →
`` `${provider} / ${modelId}` ``; plugin-configs → `pluginName`;
instance-settings → `{ id: key, name: key }`.

**C5 — the backup-dialog copy** (SPA renders; server has no part):
checkbox label `Compact backup`, default OFF; sub-label: "A
considerably slimmer archive — the search indexes are left behind and
rebuilt after restoring. Everything you have written travels either
way."

**Change rule:** if either lane must deviate from C1–C5, it updates
BOTH order files in the same commit and says so in its lane record —
a silent unilateral change is an ownership violation.

## Ownership (binding, identical across P4.D46 / P4.D47 / P4.D48 / P4.36)

- **P4.D46** (this lane) owns `crates/quilltap-core/src/**` (incl.
  `api/types.rs` — the ONLY lane that may edit it),
  `crates/quilltap-web/src/**` EXCEPT `main.rs`, the
  import/export/backup/restore harness families (tests, oracle cases,
  fixtures incl. the system-data widening + the new archive), and
  `docs/v4/**` mirrors for `7189a968`'s docs. Bumps: quilltap-core,
  quilltap-harness, quilltap-web.
- **P4.D47** owns `apps/web/**`. Bump: the SPA version.
- **P4.D48** owns `harness/oracle/providers/**`, the provider corpora
  under `harness/oracle/fixtures/` (`request-envelopes/`,
  `response-bodies/`, `streams/`, `tool-wire/`, `moderation-wire/`,
  `web-search-wire/`), `crates/quilltap-web/src/main.rs`, `Dockerfile`,
  `docs/developer/running.md`. Bumps: quilltap-web (patch; collapses
  with this lane's at unification), quilltap-harness only if a corpus
  byte moves (its escalation path).
- **P4.36** owns
  `harness/oracle/cases/context-summary-service-tier3.test.ts`,
  `crates/quilltap-harness/tests/context_summary_service_tier3_equivalence.rs`,
  and the `context-summary-service-*` fixture JSONs if extended.
  Bump: quilltap-harness (patch; collapses at unification).
- `CHANGELOG` and `docs/developer/porting/status-log.md` are
  append-only for everyone (union-merge at unification).
