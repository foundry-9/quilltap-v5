# Work order P4.D76 — the 4.8.2 dependency-refresh wire re-check — v4 `48396682`

**Status:** OPEN — not started.

## Preamble

- **v4 baseline for this round: `48396682`** (v4 main HEAD at planning
  2026-08-13). This lane dispositions `d339bad8` "chore: refresh npm
  dependencies across app, packages, and plugins": openai `^7.2.0` →
  `^7.4.0`, `@openrouter/sdk` `^1.2.2` → `^1.2.32`, plus 14 plugin
  rebuilds with `manifest.json` versions synced to `package.json` (nine
  had drifted). katex/Next/mammoth/ws are out of scope here (katex is
  P4.D74's; the rest have no v5 wire surface).
- **Drift-check at lane start, BOTH branches** (procedure as in P4.D71's
  preamble; STOP on movement).
- **Sibling lanes:** P4.D71/P4.D72 (wardrobe), P4.D73 (settings),
  P4.D74/P4.D75 (composer SPA), P4.46 (boot lock). None touch the
  provider corpora or oracle provider tooling.
- **Worktree:** own branch per `carryout`. Small lane; suitable for a
  cheaper agent tier with this order as the full spec.

## Mandate

Prove (or refute) that v4's SDK bumps moved the provider wire, by
regenerating the recorded corpora against the NEW SDKs and re-running the
consuming differentials — the P4.D33/P4.D48 precedent, with one
**restated deliverable**: the corpora are now SELF-DATING (P4.44 added
recorded request headers), so a post-bump regen CANNOT be byte-identical.
The claim becomes: **byte-identical outside the version-marker fields.**
**Do NOT touch:** any `crates/quilltap-core/src` provider code (if the
wire actually moved, STOP and escalate — absorbing a wire change is a
drift-port decision, not this lane's).

## Survey-verified starting points (2026-08-13)

- Corpora (committed NDJSON):
  - `harness/oracle/fixtures/request-envelopes/request-envelopes.recorded.ndjson`
    — 163 rows, 8 providers × stream+send. **Version markers measured
    today:** `x-stainless-package-version: 7.2.0` on 80 rows
    (openai 26, grok 18, deepseek 14, z-ai 14, openai-compatible 8);
    `0.115.0` on 38 anthropic rows; the 13 openrouter SDK-path rows date
    via UA `speakeasy-sdk/typescript 1.2.2 …`.
  - `…/google-wire.recorded.ndjson` (18), `…/google-request.recorded.ndjson`
    (9), `harness/oracle/fixtures/response-bodies/response-bodies.recorded.ndjson`
    (30 rows, 9 providers).
- Recipes (runnable, Node 24 + tsx, run from `$V4/plugins/dist/<plugin>`):
  `harness/oracle/providers/regenerate-request-envelopes.sh`,
  `regenerate-google-wire.sh`, `regenerate-response-bodies.sh` (+ the
  recorders beside them).
- Consuming differentials: `crates/quilltap-harness/tests/
  request_builder_equivalence.rs` (header :450-490 — the header check is
  an explicit SUBSET, so `x-stainless-*` changes are invisible to it;
  the OpenRouter UA divergence is whitelisted :471-478),
  `request_builder_google_wire_equivalence.rs`,
  `request_builder_google_equivalence.rs`, `response_parse_equivalence.rs`,
  `tool_wire_call_site.rs`.
- OpenRouter pricing: `crates/quilltap-harness/tests/
  openrouter_sdk_pricing_equivalence.rs` — regen recipe in its header
  :22-43; **both jest overrides are load-bearing**, notably
  `--transformIgnorePatterns "node_modules/(?!(@openrouter/sdk|jose)/)"`
  (without it the suite passes and emits EMPTY scenarios). Pins the SDK
  key remap + the 500-row page loop.
- Precedent memories: `p4.d33-provider-sdk-wire-lane`,
  `p4.d48-anthropic-sdk-wire-check-lane` (the `createRequire`
  version-probe trick for confirming which SDK is actually installed).
- Manifests: the v5 manifest generator was REPAIRED in P4.39
  (byte-identity proven then) — the D48-era "rotted, destructive
  generator" note is stale. The 14 plugin rebuilds (version-only + the
  manifest-version sync) are in its input surface.

## Deliverables

### Tier 1 — must land

1. **Environment proof first.** In the v4 checkout: `npm ci` (or verify
   node_modules matches the committed lock), then prove the INSTALLED
   majors/minors with the `createRequire` probe (openai 7.4.x,
   `@openrouter/sdk` 1.2.32, anthropic unchanged at 0.115.x). A regen
   against stale node_modules proves nothing.
2. **Regenerate all four corpora** via the committed recipes; commit the
   regenerated files. Assert field-level: every row byte-identical
   EXCEPT the version-marker fields (`x-stainless-package-version` /
   `x-stainless-runtime*`-class headers on the openai-family rows, the
   openrouter UA string). The anthropic rows must be byte-identical
   WHOLE (its SDK did not move). Any other delta = the wire moved →
   STOP, report, escalate (that becomes a drift-port item, not a corpus
   update).
3. **Re-run the five consuming differentials + the pricing family BY
   NAME** with `--nocapture`, zero SKIP, against the fresh corpora.
4. **The pricing oracle regen** per its header recipe (the REAL SDK in
   the loop) — 1.2.2 → 1.2.32 is exactly the class that once broke the
   key remap silently; the differential is the tripwire.

### Tier 2 — should land

5. Re-run the manifests generator against the rebuilt v4 plugins and
   diff v5's declarative manifests — expected byte-identical (the plugin
   bumps are version-only); if manifest content moved beyond version
   strings, report it as drift.
6. A short restatement in the lane record correcting the D33/D48
   precedent for future lanes: "byte-identical regen" is now
   "byte-identical outside the self-dating version markers" (and the
   corpora self-date, so no mtime archaeology).

### Tier 3 — explicit loud deferrals

- None. This lane's only legitimate outputs are a neutrality proof or an
  escalation.

## Fixtures

- Owns the four corpus files' regeneration + commit. No DB fixtures.

## Shared contract / Ownership

- **This lane owns:** `harness/oracle/fixtures/request-envelopes/**`,
  `harness/oracle/fixtures/response-bodies/**`, the provider recipe
  scripts if they need venue repairs (repair-and-record per the
  `recipe-header-conventions` memory), the pricing oracle NDJSON.
- Touches NO crate source. **Version bumps:** `quilltap-harness` only
  (if any Rust test text changes; otherwise no bump — fixtures alone do
  not bump crates unless the repo convention says otherwise; follow
  `commit-checklist`).
- `CHANGELOG` + `status-log.md`: append-only.

## Verification gate (before declaring done)

1. The six differentials BY NAME with `--nocapture`, zero SKIP, fresh
   corpora.
2. `cargo test -p quilltap-harness` (no env vars) — the standing
   self-test still green.
3. Per-commit rule: `.claude/commands/commit.md`.
