# Work order: W4.6a — the buildContext feeder closures

**Size: one Opus session** (internal parallelization OK: the feeders are
mostly disjoint new modules; the `BuildContextSeams` closure + differential
regeneration serialized at the end). Closes the READ/COMPUTE half of the
`BuildContextSeams` trait (`services/build_context.rs:533`); the
whisper-POSTING half is W4.6b. v4 sources surveyed 2026-07-06.

## Seam-by-seam plan

1. **`resolve_mount_pool` — CLOSE WITH EXISTING CODE.** The tiered mount
   pool is already ported (`db::tiered_mount_pool`, W4.1d3a). Wire
   `resolve_tiered_mount_pool` in at the composition point; delete the seam
   method. Re-verify `build_context_tier3` (the corpus's own-vault-only
   default must fall out of the real resolver on the same fixture) and add
   one case with a project store so the wider pool is banked live.
2. **`read_memory_recall_settings` — CLOSE WITH EXISTING CODE.** v4
   `getMemoryRecallSettings` reads instance settings;
   `db::instance_settings` exists — add the scoped read, close the seam.
3. **`compute_frozen_archive`** (v4 `lib/memory/frozen-archive-cache.ts`,
   106 — pure + process-local cache): `memories.findMostImportant(char,
   100)` (add the scoped read if missing) → effective-weight re-rank (the
   ported weighting leaves) → top 25 → sort by id. Cache keyed
   `(characterId, compactionGeneration)`, process-global
   (`OnceLock<Mutex<HashMap>>`, the housekeeping-outcome-cache precedent),
   evicted on generation bump.
4. **`distill_memory_search`** (v4 `extractMemorySearchKeywords` cheap-LLM
   task): prompt body byte-exact in a generated `prompt_text` submodule,
   over `CheapLlmTaskExecutor`; parse per source.
5. **`resolve_memory_recap`** (v4 `lib/memory/memory-recap.ts`, 373):
   `findRecentByImportanceTier` ×3 (add scoped reads),
   `findRecentSummarizedByCharacter` (ported),
   `searchVaultConversationSummaries` (semantic search over the
   `Conversation Summaries/` folder — compose `document_search` narrowed by
   path, the Knowledge/ precedent), `buildRecentConversationsBlock` /
   `buildConversationRecallLists` (pure formatters, tier-1-able), and the
   `summarizeMemoryRecap` cheap-LLM task (prompt byte-exact). Relative-age
   labels use the injected clock.
6. **`resolve_core_whisper_context`** (v4
   `lib/services/aurora-notifications/core-whisper.ts`, 442 — the READ +
   ASSEMBLY half): `resolveCoreWhisperConfig` (chat → character → global
   precedence), the `Core/` folder read
   (`find_many_by_mount_points_in_folder`, ported) + group Core via
   `group_character_members`/`groups` (ported), frontmatter strip (ported
   parser), path-sorted rendering into persona-voiced + opaque forms. The
   POST (`postCoreWhisper`) is W4.6b — here return the packet and leave the
   post behind a recorded seam so W4.6b snaps in.
7. **`resolve_suparna_mail_context`** — the READ half: `collect_unalerted_mail`
   (the `Post Office/` folder scan over the ported post_office parsers,
   closing that tracked deferral) + the LLM-context builder; `mark_alerted`
   (frontmatter flip via the ported write primitive) is part of the same
   unit since the read is only correct with the flip (double-announce
   guard). The whisper POST is W4.6b.
8. **`resolve_off_scene_announcement`** — the SCAN half: v4
   `findIntroducedOffSceneCharacterIds` reads prior Host
   `off-scene-characters` messages' `hostEvent.introducedCharacterIds` +
   the mention scan (the Phase-1 `find_mentioned_character_ids` leaf) over
   the tool-stripped corpus. The Host POST is W4.6b.
9. **`persist_scene_cache` / `persist_recall_history` + the scene-state
   read** — port the persistence (a `chats.update` on the scene-state /
   recall-history columns — confirm marshaling exists; extend additively if
   not) and the prior-emission read; `EVERY_N_MINUTES = 10` resolution per
   source. The scene-state TRACKING job (`updateSceneState` cheap-LLM task,
   `scene-state-tracking.ts`, 376) is in scope as a service function (its
   runner dispatch row is W4.8); its prompt bodies byte-exact.
10. **`resolve_live_clothing` — CLOSE WITH EXISTING CODE.**
    `resolve_equipped_outfit_for_character` (W4.1d2) + `describe_outfit`;
    wire per v4's call site.

Also close the finalizer's **`CostTracker` estimation seam** if it falls out
cheaply: `createSystemEvent` is just a SYSTEM `add_message` (ported) + the
ported token aggregates + `estimateMessageCost` over the ported pricing —
verified 2026-07-06 that no new table is involved. If the estimation needs
the pricing fetcher's live data, leave it seamed for W4.7e and note it.

## Differential plan

- Tier-1 differentials for the pure leaves (frozen-archive ranking,
  recap block formatters, core-whisper assembly/rendering, off-scene scan)
  — fresh oracle cases driving v4's REAL exports.
- One tier-3 differential (`context_feeders_tier3_equivalence`) driving
  v4's REAL `buildContext` over a fixture provisioned with: `Core/` files
  (own + group), a `Conversation Summaries/` corpus (baked via v4's real
  vault mirror — coordinate with W4.6b's fixture builder if concurrent),
  unalerted mail, a stale + fresh scene state, and tiered memories — with
  ONLY the model boundaries canned (recorded keys prove the recap/distill
  prompts) and the W4.6b posts mocked/recorded. Bank: recap on chat-start,
  distillation feeding the search query (canned-key content proves it),
  frozen archive injection + generation-bump eviction, core-whisper
  packet content (persona + opaque), mail context + the `mark_alerted`
  flip (double-load = no re-announce), off-scene introduction surfaced in
  the other-participants block, scene cache persist + reread, and the
  live mount pool.
- Regenerate `build_context_tier3_equivalence` — every closed seam's
  pre-existing case must be behavior-identical; the oracle drops the
  corresponding jest mocks one-for-one with the Rust seam closures.

## Watch-outs

- Dependency ordering (from the survey): frozen archive before the
  injector; distillation before semantic search; core-whisper trigger fires
  BEFORE context build; off-scene scan after tool-strip.
- The recap's vault-summary search needs the summaries CORPUS — bake it
  with v4's real `writeConversationSummaryToVaults` in the fixture builder
  (the `[[project-store-fixture-needs-real-create]]` rule generalizes:
  never hand-write store bytes).
- `collect_unalerted_mail` + `mark_alerted` interact with W4.5's mail
  handlers — the post_office module is shared; coordinate if concurrent
  (prefer not concurrent).
- Scene-state columns: `chats.sceneState` marshaling — verify before
  extending.

## Deliverables checklist

- [ ] All ten seams above closed or explicitly re-scoped in the module doc;
      the `BuildContextSeams` trait shrunk to the W4.6b posting methods
      only.
- [ ] New differentials green; regenerated `build_context_tier3`;
      re-verified `knowledge_injector`, `first_message_context`,
      `orchestrator_tier3`.
- [ ] Full workspace `cargo test` / `clippy -D warnings` / `fmt --check`.
- [ ] CHANGELOG, versions, CLAUDE.md, `chat-orchestration.md`; memory note.
- [ ] Commit per `.claude/commands/commit.md`, branch `w4.6a-context-feeders`.

**STOP rules:** schema never changes (scene-state/recall-history columns
must already exist — if not, STOP and flag); the posting half stays seamed
for W4.6b — do not port writers here.
