# Work order P4.D131 — retire the bug-105 divergence arm (the `679e450e3` convergence)

**Status:** OPEN

## Preamble

- **v4 oracle baseline: `aec86a613`** (drift ledger §1, checked 2026-08-27).
  **This lane's target commit is `679e450e3`** — v4's fix for bug 105, the
  first of FOUR portable commits past the baseline (the ledger §3 rows; the
  siblings absorb the other three). This is a **CONVERGENCE** row: v4
  adopting a fix this port made first, so the both-directions pin trips at
  the baseline move **by design**. The governing procedure is drift-ledger
  **§5.4 — measure, don't assume**: regenerate, dump v4's ACTUAL post-fix
  output, and only then retire.
- **Freshness probe at lane start** (drift-ledger §2, four read-only
  commands). Pass = checkout on `main`, tree clean, no commits past
  `b121ac77f` on main and none past `3a76b17df` on bugfix. **Any mismatch:
  STOP and report** — lanes never write the ledger.
- **Regen rule in force: PIN REQUIRED** (ledger §1). Every oracle regen in
  this lane pins a lane-unique detached worktree at **`b121ac77f`** (v4
  main HEAD at planning — it contains all four target commits and is the
  round's intended new baseline): `/tmp/qt-v4-pin-p4d131-b121ac77f`, all
  three symlink classes, per ledger §5.1. Verify the pin by grepping the
  fresh NDJSON for a marker only the post-fix tree has (the abort sentence
  `(seeded.provider ?? "").toUpperCase is not a function` must be ABSENT;
  the named warning `Failed to import connection profile "Bug 105
  Connection": ` must be PRESENT on the v4 leg). Run family regens through
  the sweep driver (`harness/tools/recipe_sweep.py --v4 <pin>`); never two
  sweeps concurrently.
- **Sibling lanes:** P4.D132 (`p4.d132-salon-tooltips-spa.md`), P4.D133
  (`p4.d133-cli-restore-key.md`), P4.65
  (`p4.65-chat-list-preloaded-batching.md`). The lanes meet nowhere;
  §Ownership below is the binding split (identical table in all four
  orders). P4.D132 is the round's ONLY Playwright runner; this lane's gate
  is cargo-only.
- **Worktree:** per the `carryout` skill; edit inside the worktree, not
  main (repo-root paths exist in BOTH trees). `docs/CHANGELOG.md` and
  `status-log.md` append-only.

## Mandate

Absorb v4 `679e450e3` — which fixed v4 bug 105 exactly as this port's
filing described — by retiring the divergence-aware machinery in the
`system_import_state` family to a plain equality, and updating the v5-side
doc comments that still describe the divergence as standing. **Zero v5
production-source behavior change is expected**: v5 never had the bug (the
standing 2026-08-03 backup/restore ruling; the unit pin in `profiles.rs`).
The work is harness + doc-comment surgery, proven by a fresh pinned regen.

**Must NOT touch:** anything a sibling owns (§Ownership); v5's import
implementation beyond the `profiles.rs` doc comment; the drift ledger
(lanes never write it); `system-data-main.db` (the `multiCharacterPrefill`
vintage vacuity stays RECORDED, not fixed — it is phase-4 candidate 5, a
different round, because the fixture is shared with the system-data routes
differentials).

## Survey-verified starting points (fresh, 2026-08-27)

**The v4 commit** (12 files, +138/−34; port from the hunks, never the
prose — ledger §5.3; the commit message itself says *"No v5 change is
owed"* and the survey confirms it):

1. `lib/import/quilltap-import/import-profiles.ts` — the
   `seedLegacyConnectionProfileFields(rawProfile)` call moved INSIDE the
   per-item `try` (post-commit `:41-48`), and the `catch` now reads
   `rawProfile.name` / `rawProfile.id` (`:118`, `:123`) because `profile`
   is block-scoped to the `try`.
2. `lib/llm/connection-profile-legacy-fields.ts:64-65` — the guard widened
   past `??`: `typeof seeded.provider === 'string' ? seeded.provider : ''`.
3. Tests (+53 lines across two suites), the bug file moved to
   `docs/developer/bugs/fixed/`, and **`help/system-import-export.md` +1
   line** (post-commit `:336`) — the help line banks to `p4.9i2`; record
   the bank in the status log, do not port it.
4. The restore path needed nothing (v4's per-profile restore loop already
   try-wraps seeding) — `crates/quilltap-core/src/services/backup/restore/
   orchestrator.rs:289` is NOT a bug-105 site; leave it alone.

**The v5 machinery to retire** (all line numbers verified 2026-08-27):

- `crates/quilltap-harness/tests/system_import_state.rs`:
  - Module-header divergence section `:91-110` (keep the fixture-vacuity
    note at `:100-110` — it is about `multiCharacterPrefill`, not bug 105;
    reword the section so the vacuity note survives the retirement).
  - Case-count assert `:1072` (`ran == 37`) and named-presence assert
    `:1073-1081` — the count stays 37 if the case is retained as a plain
    equality (it should be: the case is now a REGRESSION GUARD that a
    malformed profile is named-and-skipped on both sides).
  - `classify_bug105_seed_abort` `:1457-1566` with its doc `:1398-1456`
    and the three consts `:1467-1472` (`ABORT`/`NAMED`/`SOUND`). The v4
    leg's own failure text (`:1494-1512`) prescribes this retirement.
  - The dispatch `else if` + `skip.insert(("main", "image_profiles"))`
    `:1699-1715`, threaded into `compare_execute` `:1717` and
    `normalize_side`. **Retiring the arm makes `skip` unconditionally
    empty** — the comment at `:1775-1780` says this is the only non-empty
    caller; decide deliberately whether the `skip` parameter machinery
    stays (a harness capability other convergences may want) or goes
    (clippy will flag it dead) — either way say which and why in the lane
    record.
  - The blanked body `:1561-1565`.
- `harness/oracle/cases/system-import-execute.test.ts`:
  - `bug105SeedAbortPayload()` `:1251-1284` + payload doc `:1205-1250` +
    registration `:1972-1978`. The payload STAYS (the malformed
    `provider: 42` profile + the `Bug 105 Survivor` image profile is now
    the equality case's input); the doc comment's divergence framing goes.
- `crates/quilltap-core/src/services/quilltap_import/profiles.rs`:
  - Doc block `:761-799` — `:761-762` still says "TO FILE UPSTREAM" (it
    WAS filed, as v4 `b6c6d7793`, and is now FIXED at `679e450e3`);
    `:792-798` names the oracle tripwire and its retirement condition —
    this order IS that retirement. Rewrite as a convergence record: v5 was
    never affected; v4 converged at `679e450e3`; the family now pins the
    shared behavior as a plain equality. The test fn `:801-838` and its
    assertions are already correct post-convergence — no change.

**Expected post-fix v4 behavior to measure** (from the shipped hunks; the
regen must CONFIRM, not assume — §5.4): `success == true`, exactly one
warning starting `Failed to import connection profile "Bug 105
Connection": `, `imported.connectionProfiles == 0` (the malformed profile
is named and skipped), `imported.imageProfiles == 1` (`Bug 105 Survivor`
lands), and `main.image_profiles` gains exactly the survivor — i.e. v4's
leg becomes byte-for-byte what v5's leg has asserted all along. If the
measured output differs from this in ANY particular (partial convergence —
P4.D51's canonical surprise), reshape the pin instead of deleting it and
record the residue.

**Regen recipe** — `system-import-execute.test.ts:24-36` verbatim, with
`cd ~/source/quilltap-server` replaced by the lane pin. Consumes
`harness/oracle/fixtures/system-data.json` + the three committed
`crates/quilltap-web/tests/fixtures/system-data-{main,mount,llmlogs}.db`.
Replay: `QT_ORACLE_SYSTEM_IMPORT_EXECUTE=/tmp/oracle-system-import-execute.ndjson
cargo test -p quilltap-harness --test system_import_state -- --nocapture`.
Observe the silent-stale-pass discipline (ledger §5.2): delete the old
NDJSON first, check non-empty, grep the changed bytes.

**Doc sites this lane also updates** (v5-side prose that describes the
divergence as standing): the status-log gets the lane record as usual
(append-only — do NOT edit the historical P4.63 records; history stays
verbatim). CLAUDE.md and the drift ledger are the unifier's, not yours.
Note for the unifier (record it in your lane record): the ledger's §3 row
names the class `import_aborts_on_non_string_provider` while the code
name is `execute_bug105_seed_abort` — reconcile the naming when marking
the row ABSORBED.

## Tiered deliverables

- **Tier 1 (must land):**
  1. The fresh pinned regen of `system-import-execute` at `b121ac77f`,
     with the §5.4 measurement recorded in the lane record (v4's actual
     post-fix rows quoted).
  2. `execute_bug105_seed_abort` retired to a plain equality: classifier
     deleted, skip-insert deleted, blanked body deleted, the case retained
     as an ordinary state-compared case, `ran == 37` still exact.
  3. The `profiles.rs` doc rewrite (convergence record, not divergence).
  4. A mutation proof that the retained equality case still discriminates:
     revert the case to comparing nothing (or drop the survivor from one
     side's expectation) and confirm RED, per the fixture-side-mutations-
     prove-nothing rule — mutate the v5 SOURCE or the harness comparand,
     never the fixture.
- **Tier 2 (should land):** the `skip`-parameter disposition (keep with a
  doc comment naming it currently-unused, or remove it cleanly) and the
  module-header reword preserving the `multiCharacterPrefill` vacuity
  note.
- **Tier 3 (explicit deferrals):** the help-line bank to `p4.9i2`
  (record it); the `system-data-main.db` vintage widening (phase-4
  candidate 5 — NOT this lane).

## The differential requirement, restated

Every claim here rests on the regenerated oracle driving v4's REAL import
service from the pinned worktree — never a transcription of the hunks. The
family's own recipe is the closest (and exact) recipe; the P4.63 lane
record (`status-log.md` → "P4.63" §Item 1) documents how the arm was built
and is the map for un-building it.

## Fixtures

Consumes (read-only): `system-data.json`, the three committed
`system-data-*.db`. Delivers: no fixture changes. If a regen appears to
require touching a committed fixture, STOP — that is a sign the
measurement diverged from the plan.

## Shared contract

The four lanes meet nowhere — there is no cross-lane type, verb, or wire
contract this round. The only shared obligations are the §Ownership table
below and the append-only rule for `docs/CHANGELOG.md` and
`docs/developer/porting/status-log.md`.

## Ownership (binding; identical table in all four orders)

| Lane | Exclusive ownership |
|---|---|
| **P4.D131** | `crates/quilltap-harness/tests/system_import_state.rs`; `harness/oracle/cases/system-import-execute.test.ts`; `crates/quilltap-core/src/services/quilltap_import/profiles.rs` (doc/test-comment edits only). Bumps: `quilltap-harness`, `quilltap-core`. |
| **P4.D132** | `apps/web/**` (the whole SPA). Bumps: the SPA version. The round's ONLY Playwright runner. |
| **P4.D133** | `crates/quilltap-cli/**`; `crates/quilltap-core/src/dbkey.rs`. Bumps: `quilltap-cli`, `quilltap-core`. |
| **P4.65** | `crates/quilltap-core/src/db/{files,projects,store_backed,doc_mount_file_links,conversation_chunks,memories_read}.rs`; `crates/quilltap-core/src/services/chat_enrichment.rs`; `crates/quilltap-core/src/services/home.rs`; `crates/quilltap-core/src/api/salon.rs`; `crates/quilltap-harness/tests/salon_reads_equivalence.rs`; `harness/oracle/cases/salon-reads.test.ts`; `harness/oracle/fixtures/salon.json` + `harness/oracle/fixtures/build-salon-fixture.ts`; `crates/quilltap-web/tests/fixtures/salon-{main,mount}.db`. Bumps: `quilltap-core`, `quilltap-harness`, `quilltap-web` (fixtures). |

Multiple lanes bump `quilltap-core`/`quilltap-harness` — identical bumps
auto-merge silently; **the unifier recounts as base + total bumps** (the
standing playbook rule), lanes just bump normally. No lane edits
CLAUDE.md, `phase-4.md`, or the drift ledger.

## Verification gate (before declaring done)

1. `cargo fmt --check`; `cargo clippy` both feature sets.
2. `cargo test -p quilltap-harness --test system_import_state --
   --nocapture` with the fresh pinned oracle — 37 cases, 0 failures,
   zero SKIP, and grep the run output for the case by name.
3. The mutation proof, run and reverted (file-backup revert, never
   `git checkout` mid-unit).
4. `cargo test --workspace` (background, full log, no `tail`, no pgrep
   loops — the CLAUDE.md standing rule).
5. Commit per `.claude/commands/commit.md`; CHANGELOG entry; version
   bumps per §Ownership.
