# Work order P4.6ak — the text-replacements + chat-background server lane (lane A)

> **STATUS: CLOSED — UNIFIED on main (2026-07-14).** All Tier 1 + Tier 2
> LANDED: the `text_replacement_rules` route surface (list / create /
> update / delete / bulk-replace + the conflict arm) over five dispatch
> verbs and the REST edges; `chatGetBackground` (all three arms + the
> chat 404); the `regenerate-background` loud typed refusal (tier 2).
> Proven by the 15-case `text_replacements_routes_equivalence`
> differential + the `text_replacement_rules_tier2` differential (both
> fresh at `02865bdb`) + the live `text_replacements_web_routes`
> web-edge test, over the new committed
> `text-replacements-{main,mount}.db` fixture. Ported v4 quirks:
> REAL-affinity `sortOrder`, the doubled "not found not found" 404
> message. **Tier-3 deferrals (loud, named):** the story-background
> GENERATION subsystem (image-profile prompt build,
> `lastBackgroundGeneratedAt`, the 30s poll — `regenerate-background`
> refuses); project-level `?action=get-background` (the Salon never
> uses it). Versions: core 0.0.221, harness 0.0.200, web 0.0.21.
>
> **Original status (for reference): OPEN.**
>
> Lane A of the THREE-lane P4.6ak ∥ P4.6al ∥ P4.6am round (the D17
> editor follow-ons + salon dogfood round, planned 2026-07-14). **v4
> reference baseline: HEAD `02865bdb`** (`~/source/quilltap-server`) —
> run `git log 02865bdb..HEAD --oneline` at lane start; **if HEAD
> moved, STOP and report** (classify drift, don't plan around it).
> Sibling lanes: `p4.6al-editor-followons-composer.md` (the editor +
> composer SPA lane), `p4.6am-salon-dogfood-slice.md` (the salon SPA
> lane). The **Shared contract** and **Ownership** sections below are
> binding and reproduced verbatim in all three orders. You run in a
> worktree; the unification pass integrates the lanes onto main.
> **This lane is the round's ONLY core-dispatch writer** (the
> two-writer delimited-block rule is moot this round).

## Mandate

Port the two server surfaces the SPA lanes need live data from:

1. **The text-replacement rules surface** — the `text_replacement_rules`
   repository (global, no userId; main partition) and its five verbs
   (list / create / update / delete / bulk-replace) with the REST edge
   legs at `/api/v1/settings/text-replacements` — the data source for
   v4's composer autocorrect (`TextReplacementPlugin`), which lane B
   ports client-side.
2. **`chatGetBackground`** — v4's
   `GET /api/v1/chats/[id]?action=get-background` (the story-background
   resolver), which lane C needs for dogfood finding #9 (chat
   background images don't display).

Every unit ships with a differential against v4's REAL code. Do NOT
touch `apps/web/src/**` (lanes B/C own the SPA), and do not add any
SPA client code — the TS contract mirrors are the SPA lanes' job (see
Shared contract §2).

## Survey-verified starting points (2026-07-14 @ `02865bdb` — do not re-derive)

### The v4 text-replacements surface

- **Schema:** `lib/schemas/text-replacement.types.ts` —
  `TextReplacementRuleSchema` `{ id, fromText (1..100, no
  leading/trailing whitespace), toText (1..1000), caseSensitive,
  enabled, sortOrder (int), createdAt, updatedAt }`. Rules are
  **global — no userId scoping** (route header comment).
- **Table:** created by MIGRATION
  `migrations/scripts/add-text-replacement-rules-table.ts` (introduced
  v4.6.0) — it is NOT in fresh `generateDDL` output; fixtures and the
  e2e instance must materialize it by hand-DDL (the `doc_mount_blobs`
  / `terminal_sessions` / `folders` precedent — see the P4.6ah memory
  note). A second migration
  `add-text-replacements-enabled-field.ts` added the
  `chat_settings.textReplacementsEnabled` flag — **already ported**
  (`crates/quilltap-core/src/db/chat_settings.rs:480,603,745`); no
  work there.
- **Repository:** `lib/database/repositories/
  text-replacement-rules.repository.ts` — `list()` (ordered
  sortOrder then createdAt), `create()`, `update()`, `delete()`,
  `bulkReplace()` (transactional full-list replace), plus
  `TextReplacementRuleConflictError` (duplicate fromText/caseSensitive
  pair).
- **Routes:** `app/api/v1/settings/text-replacements/route.ts` —
  `GET` → `successResponse({ rules, count })` (`:31-45`); `POST` is
  action-dispatched (`:50-61`): default = create one rule (201
  `created`, conflict arm), `?action=bulk-replace` =
  `handleBulkReplace`. The `[id]/route.ts` subroute has `PATCH` (`:23`)
  and `DELETE` (`:60`).
- **Consumer (for reference only, lane B ports it):**
  `components/chat/lexical/plugins/TextReplacementPlugin.tsx` +
  `lib/text-replacement/useTextReplacementRules.ts` (compiles rules
  into caseSensitive/caseInsensitive maps); enable flag read from
  `/api/v1/settings/chat` `textReplacementsEnabled`.

### The v4 get-background handler

- `app/api/v1/chats/[id]/handlers/get.ts:173-211` — on
  `action === 'get-background'`:
  - no `chat.storyBackgroundImageId` → `{ backgroundUrl: null,
    fileId: null, filename: null, sha256: null, linkSummary: null }`
    (`:182-184`);
  - file row missing → same all-null body + a warn log (`:187-193`);
  - else `backgroundUrl = getFilePath(file)` (`:196`) and the 200 body
    `{ backgroundUrl, fileId: file.id, filename:
    file.originalFilename, sha256, linkSummary }` (`:201-206`). Read
    the full handler for the exact `sha256`/`linkSummary` derivations
    and port them exactly — the differential proves the bytes.
- v5 already reads/writes the `chats.storyBackgroundImageId` column
  (`crates/quilltap-core/src/db/chats_read.rs:62,245`) — this unit is
  the resolver verb only, no schema work.
- The SIBLING v4 surfaces this lane does NOT port (refusal-arm or
  leave to the named deferral list): the `regenerate-background` POST
  action (`handlers/post.ts:77` — the story-background *generation*
  subsystem: image-profile prompt build + polling), and the
  project-level `GET /api/v1/projects/[id]?action=get-background`
  (`actions/background.ts` — the Salon never uses it; it only fires
  when `chatId` is null, `hooks/useStoryBackground.ts:50-66`).

### v5 landing points

- Dispatch variants: `crates/quilltap-core/src/api/` (settings.rs for
  the rules verbs — or a new `text_replacements.rs` module in the
  established per-family shape; salon.rs or chats-side for
  `chatGetBackground`, next to the existing chat-GET serialization
  which already carries `documentEditingMode` at `salon.rs:390`).
- REST edges: `crates/quilltap-web` — remember **the P4.6ah lesson:
  the REST edge must UNWRAP the dispatch envelope** to v4's raw route
  body (`core_response_to_http` arm), and any web-edge assertion must
  be seeded so its guard actually fires.
- `compositionModeDefault` is FULLY ported (settings PUT validation
  `api/settings.rs:593-596`; chat-create bake
  `services/chat_create.rs:498`) — listed here so nobody re-ports it.

## Deliverables

### Tier 1 (must land)

1. **`text_replacement_rules` repo** (main partition, single-writer
   discipline): list/create/update/delete/bulkReplace + the conflict
   error as a typed `CoreError` arm matching v4's conflict response.
   JSON column/row serialization in **schema key order** (the
   standing JSON-column rule).
2. **The five dispatch variants + REST edge legs** per the Shared
   contract §1, envelope-unwrapped at the edge.
3. **`chatGetBackground`** dispatch variant + the
   `?action=get-background` leg on the chat GET route, all three arms
   (no-id / file-missing / resolved).
4. **Differentials** for both surfaces (see below), green over a
   fresh oracle at `02865bdb`, run BY NAME in the gate with
   `--nocapture` + grep for SKIP (the P4.d3 lesson).

### Tier 2 (should land)

5. A named loud refusal for `regenerate-background` in the chats POST
   action table (v4 `handlers/post.ts:77`) so the SPA gets a typed
   `not_available` instead of an unknown-action fallback.

### Tier 3 (explicit deferrals — enumerate in the status header at close)

- The story-background *generation* subsystem (image-profile prompt
  build, `lastBackgroundGeneratedAt`, the 30s poll loop).
- Project-level `?action=get-background`.

## The differential requirement (restated)

Every ported unit ships with an equivalence test driving v4's REAL
code. Both surfaces here are route-level: use the **jest real-DB
oracle** recipe (memory notes `jest-real-db-oracle`,
`jest-oracle-mechanism`; the P4.6s split-routes contamination gotcha
and the P4.6p "route reads omit null nullables but create/update echo
AS null" gotcha both apply). Oracle cases live under
`harness/oracle/cases/`; NDJSON out; Rust diff in `quilltap-harness`
behind env vars (tests skip if unset — name them
`QT_ORACLE_TEXT_REPLACEMENTS` and `QT_ORACLE_CHAT_BACKGROUND`, or one
combined var if you fold them into one case file). **Regenerate the
oracle from the WORKTREE case file path** (the
`oracle-regen-from-worktree-path` memory note).

Suggested case coverage: list-empty, create (+ echo shape), create
conflict, patch, delete, delete-missing, bulk-replace (incl. order),
list-after ordering (sortOrder then createdAt); get-background all
three arms.

## Fixtures

Deliver a NEW small committed fixture (suggested name
`text-replacements-main.db`, alongside the existing fixture families)
containing: a few `text_replacement_rules` rows (table materialized by
hand-DDL matching the migration's column affinities), plus a chat row
with `storyBackgroundImageId` set, one with it NULL, and the matching
`files` rows (create chats/files via the REAL repos —
`project-store-fixture-needs-real-create`). A new fixture avoids
regenerating the courier-images differential; if you instead extend an
existing fixture, YOU own regenerating every oracle that reads it.
Commit the fixture generator (the established recipe).

## Shared contract (BINDING — identical in all three orders)

### §1 New dispatch variants (lane A implements in Rust; TS mirrors per §2)

Names and shapes are pinned; no lane renames:

- `textReplacementsList` `{}` →
  `{ rules: TextReplacementRule[], count: number }`
- `textReplacementCreate`
  `{ fromText, toText, caseSensitive?, enabled?, sortOrder? }` → the
  created rule (v4 201-created envelope; duplicate
  fromText+caseSensitive → the v4 conflict arm)
- `textReplacementUpdate` `{ id, ...partial rule input }` → the
  updated rule
- `textReplacementDelete` `{ id }` → v4's delete envelope
- `textReplacementsBulkReplace` `{ rules: input[] }` →
  `{ rules, count }` (transactional full-list replace)
- `chatGetBackground` `{ chatId }` →
  `{ backgroundUrl, fileId, filename, sha256, linkSummary }` (all-null
  when the chat has no `storyBackgroundImageId` or the file row is
  missing — v4 `get.ts:182-193`)

`TextReplacementRule` (camelCase serde, exactly these keys):
`{ id, fromText, toText, caseSensitive, enabled, sortOrder, createdAt,
updatedAt }`.

REST edges (lane A): `GET/POST /api/v1/settings/text-replacements`,
`POST /api/v1/settings/text-replacements?action=bulk-replace`,
`PATCH/DELETE /api/v1/settings/text-replacements/:id`,
`GET /api/v1/chats/:id?action=get-background` — each edge UNWRAPS the
dispatch envelope to v4's raw route body (the P4.6ah lesson).

### §2 core-contract.ts delimited append blocks (lanes B + C)

`apps/web/src/app/core/core-contract.ts` is edited by BOTH SPA lanes
this round, engine.rs-style: each lane appends ONE delimited block at
the END of the file (`// --- P4.6al additions ---` /
`// --- P4.6am additions ---`) and touches nothing else in the file.
Lane B's block: the five text-replacement types. Lane C's block:
`chatGetBackground`. The unifier merges and cross-checks name-for-name
against `types.rs`.

### §3 The composer/salon seam (wired at UNIFICATION, not in-lane)

- `qt-chat-composer` (lane B) gains inputs
  `compositionMode: boolean = false` and `chatId: string | null =
  null` (draft persistence), and output
  `compositionModeChange: boolean` (emitted by the toolbar toggle; the
  composer does NOT persist).
- `qt-rich-editor` (lane B) gains input
  `submitOnModEnter: boolean = false` (Cmd/Ctrl+Enter emits `submit`).
- The salon binding — passing the chat's `documentEditingMode` into
  `[compositionMode]`/`[chatId]` and persisting `compositionModeChange`
  via the existing `chatUpdate` dispatch
  (`{ chat: { documentEditingMode } }`, v4 `useChatControls.ts:348-365`)
  — is a **unification wire**. NEITHER SPA lane adds these bindings to
  `salon-conversation.ts` in-lane (lane C owns the file but leaves this
  seam alone; lane B unit-tests the composer directly). e2e beats that
  need the wire land at unification (the P4.6ac precedent: do NOT ship
  unverified-in-lane guarded beats for this).
- Live-data activation for lane B's text-replacement fetch and lane
  C's background fetch happens at unification (in-lane: mocked
  CoreClient / route mocks; the guarded self-activating idiom only
  where the lane can verify the beat green in-lane).

### §4 File ownership (no other lane touches these)

- **Lane A:** `crates/**` (sole core-dispatch writer this round),
  `harness/oracle/**`, new/changed committed fixtures + generators.
- **Lane B:** `apps/web/src/app/editor/**`, `chat/chat-composer.*`,
  `screens/settings/chat/**`, `memory/memory-editor.*`,
  `screens/characters/edit/details-tab.*`,
  `screens/characters/new/new-character.*`, plus any NEW files it
  creates (the qt-markdown-field component, a settings
  text-replacements client api file, new CSS file(s) + their single
  import line in the styles index, new spec/e2e files). New styles go
  in lane B's NEW css file — `_chat.css` is lane C's.
- **Lane C:** `screens/salon/**`, `chat/message-list.*`,
  `chat/streaming-message.*`, `core/chat-stream.reducer.*`,
  `screens/characters/view/tabs/details-tab.*`,
  `styles/qt-components/_chat.css`, `e2e/global-setup.ts`, its own new
  spec/api files.
- Append-only union-merge for everyone: `docs/CHANGELOG.md`,
  `docs/developer/porting/status-log.md`, core-contract.ts per §2.
- **Version bumps:** lane A bumps quilltap-core / quilltap-harness /
  quilltap-web (+ Cargo.lock); lanes B and C EACH bump `apps/web`'s
  version from the 0.5.83 base (the unifier accumulates).

## Verification gate (run before declaring done)

- `cargo fmt --all --check`
- `cargo clippy` on BOTH feature sets (default + native-transport),
  `-D warnings`
- `cargo test --workspace` — zero failures, with this lane's
  differentials regenerated FRESH at `02865bdb` and run BY NAME with
  `--nocapture`, grepping for SKIP lines
- `cargo build -p quilltap-web -p quilltap-cli` sanity (the SPA lanes
  need coherent binaries at unification)
- Every commit follows `.claude/commands/commit.md`.
