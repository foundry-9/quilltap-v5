# Work order P4.D136 — the uncensored-reroute modality fix + the cheap-LLM budget rewrite (v4 `a1d88aa3a`, bugs 106/107) — STACKED on P4.D135

**Status:** OPEN (ordered 2026-08-31).

## Preamble

- **v4 oracle baseline: `b121ac77f`** (drift-ledger §1, checked
  2026-08-31). **This lane's target commit is `a1d88aa3a`** — v4's fixes
  for its own bugs 106 (the reroute swaps the model but keeps the message
  array built for the original profile, so embedded image bytes reach a
  text-only substitute) and 107 (the cheap-LLM budget wall). **Neither is
  a convergence** — both were v4-filed from its own Salon sitting
  (`97ebfb9fc` registers them; this lane ratifies that row NO-PORT with
  evidence). ⚠ **Bug 106's v5-relevance column reads "Applies"** — expect
  v5 to measurably HAVE the bug; prove it red-first.
- **STACKED:** this lane starts from **P4.D135's branch tip**, not main —
  the two commits share `provider-failover`, `orchestrator`,
  `primary-stream`, `fallback/engine`, `file-attachment-fallback`, and
  the cheap-LLM core. Do not begin until P4.D135's order is CLOSED.
- **Freshness probe at lane start** (drift-ledger §2; same pass condition
  as the siblings — main at `60e3c4a0a`, bugfix at `3a76b17df`, clean).
  **Any mismatch: STOP and report.**
- **Regen rule in force: PIN REQUIRED.** Lane-unique pin at the TARGET
  commit **`a1d88aa3a`**: `/tmp/qt-v4-pin-p4d136-a1d88aa3a`, three
  symlink classes, ledger §5.1. Verify the pin
  (`lib/chat/message-attachment-adapter.ts` must exist in it).
- **Sibling lanes:** P4.D134, P4.D137. The Ownership table in
  `p4.d134-lima-wsl2-retirement.md` is binding; this branch inherits
  P4.D135's rows plus the round's Playwright slot.
- **Worktree:** the P4.D135 worktree/branch continues; edit inside it.
  `CHANGELOG` and `status-log.md` append-only.

## Mandate

Absorb `a1d88aa3a` whole: bug 106's three halves (the danger-routing scan
ordered by the turn's attachment MIME types, the NEW message-attachment
adapter re-running the attachment fallback against the profile actually
being called, `needsVision` read off the ARRAY rather than what the user
uploaded) plus the three-spellings `profileCanReceiveAttachment`
consolidation; and bug 107's budget rewrite (the latency-class split, the
raised ceilings, the timeout-only fresh-socket retry, and
`throwIfLostToTimeout` in the six background-job handlers so a lost task
gets a backed-off retry then DEAD). Also: retire the superseded 75 s
constants and their tests, and update the superseded dogfood C4 row
(ledger §5.5). Must NOT touch P4.D134's or P4.D137's files.

## Survey-verified starting points (2026-08-31, from the shipped hunks — ledger §5.3)

**Bug 106 — the reroute/modality half:**
1. `lib/services/dangerous-content/provider-routing.service.ts` (+49):
   `resolveProviderForDangerousContent` now takes the turn's attachment
   MIME types and **ORDERS** (not filters) its scan by them. →
   `quilltap-core/src/services/dangerous_content/provider_routing.rs`
   (P4.D94/P4.D110), family `danger_routing_equivalence` + the
   dangerous-chat fixture — red-first: build the case where a
   vision-capable primary's reroute lands on a text-only candidate.
2. NEW `lib/chat/message-attachment-adapter.ts` (+183):
   `collectAttachmentMimeTypes` (:72), `adaptMessagesForProfile` (:98 —
   re-runs `processFileAttachmentFallback` against the profile actually
   being called; returns the SAME array reference when nothing changes —
   carry that identity contract, it is load-bearing for callers that
   compare). → a NEW v5 module beside `services/file_fallback.rs`.
3. `lib/chat/file-attachment-fallback.ts` (+10),
   `lib/llm/image-transport.ts` (+27): the **three-spellings
   consolidation** into one `profileCanReceiveAttachment`. v5's P4.D106
   predicate pair spans `files/image_transport.rs`,
   `files/attachment_support.rs`, `services/file_fallback.rs` — the same
   three-site shape; collapse it the same way (the
   consolidating-a-decoder memory note: a consolidation the differential
   uses needs its own mutation proofs so one shared blind spot can't
   hide). Families `file_attachment_tier3_equivalence`,
   `attachment_anchor_equivalence`.
4. Riders on the D135 files: `provider-failover.service.ts` (+26),
   `orchestrator.service.ts` (+7), `primary-stream.service.ts` (+5),
   `lib/llm/fallback/engine.ts` (+4) — `needsVision` off
   `collectAttachmentMimeTypes(messages)`.

**Bug 107 — the budget rewrite:**
5. `lib/memory/cheap-llm-tasks/core-execution.ts` (+250/−…): the new
   constants — `CHEAP_LLM_TASK_TIMEOUT_MS = 90_000` (:59, background),
   `CHEAP_LLM_TASK_TIMEOUT_INTERACTIVE_MS = 45_000` (:75),
   `CHEAP_LLM_TASK_TIMEOUT_LOCAL_MS = 180_000` (:82),
   `PROVIDER_BUDGET_HEADROOM_MS = 5_000` (:91);
   `CheapLLMLatencyClass = 'background' | 'interactive'` (:107) threaded
   from the CALL SITE; the per-task
   `CHEAP_LLM_TASK_TIMEOUT_OVERRIDES_MS` table (:140);
   `deadlineFor(…, latency)` (:150); `CheapLLMTimeoutError` (:195);
   **one retry on a fresh socket, on timeout only, never on the
   interactive path**; `CheapLLMTaskLostError` (:773) +
   `throwIfLostToTimeout` (:802). `compression-tasks.ts` (+21),
   `memory-tasks.ts` (+7), `types.ts` (+13 —
   `CheapLLMTaskResult.timedOut`), `index.ts` (+9).
6. Compression/context call sites keep their interactive numbers:
   `lib/chat/context/compression.ts` (+15 — background 75 s → **120 s**;
   interactive cache-miss stays **75 s**), `context-manager.ts` (+29),
   `context-summary.ts` (+8), `compression-cache.service.ts` (+6),
   `memory-processor.ts` (+28 — `passesLostToTimeout` joins
   `TurnMemoryProcessingResult`). The latency-class split is also what
   stops the raise inverting `MEMORY_RECAP_PHASE_TIMEOUT_MS` — verify
   v5's recap ceiling relationship still holds and pin it.
7. **Six background-job handlers** gain `throwIfLostToTimeout` so
   `markFailed` gives a backed-off retry then DEAD:
   `memory-extraction` (+18), `carina-memory-extraction` (+18),
   `context-summary` (+5), `scene-state-tracking` (+11),
   `story-background` (+7), `title-update` (+5). → v5's job handlers
   (`services/{memory_processor,carina_memory_extraction,
   context_summary_job,story_background_job,title_update_job}.rs`,
   `services/scene_state_tracking*`) + the job-runner retry semantics.
8. `docs/developer/BACKGROUND_JOBS_CHILD.md` (+38) records the
   all-or-nothing extraction-retry rule (buffered child writes are
   discarded on a throw) — read it and verify v5's single-writer buffered
   apply gives the same all-or-nothing shape; record the measurement.

**Supersessions and ratifications:**
9. ⚠ **Ledger §5.5:** the 2026-08-27 dogfood C4 row (75 s budget, "the
   warn needs >75 s, never crossed in 400 calls") is SUPERSEDED — update
   v5's `cheap_llm_exec.rs` constants + deadline tests from the hunks
   (transcribe v4's `cheap-llm-deadlines.test.ts` additions + the new
   `cheap-llm-timeout-retry.test.ts` shapes), and append the supersession
   note to `dogfood-findings.md`'s C4 row (append-only edit).
10. **Ratify `97ebfb9fc` NO-PORT with evidence** (bugs.md + two bug files
    only; read both files' v5-relevance columns and quote them in the
    lane record).
11. **Help:** `help/{chat-settings,dangerous-content,system-tasks-queue}.md`
    → the `p4.9i2` bank. NO-PORT remainder: README, CHANGELOG, bugs docs,
    v4's own tests, `package*.json`.

## Tiered deliverables

**Tier 1 (must land):**
1. Bug 106 red-first: a differential case proving v5 HAS the bug (the
   rerouted profile receiving the primary-built array), then the
   three-part fix (surveys 1–4) turning it green; the consolidation with
   per-site mutation proofs.
2. Bug 107's engine (survey 5) with the deadline unit tests moved to the
   new numbers and the latency class threaded from every v5 call site
   (surveys 6): background raises, interactive holds, retry-on-timeout
   proven with a stalling-socket test (the P4.D42 idiom).
3. The six handlers' `throwIfLostToTimeout` + retry-then-DEAD semantics
   (survey 7), family-verified where a family exists
   (`title_update_tier3_equivalence`, `memory_processor_tier3_equivalence`,
   the compression families) and unit-pinned where only the job runner
   can see it.

**Tier 2 (should land):**
4. The recap-ceiling relationship pin (survey 6) and the single-writer
   all-or-nothing measurement (survey 8).
5. The dogfood C4 supersession note (survey 9).

**Tier 3 (explicit deferrals, loud):**
6. Help rows → `p4.9i2`. 💸 a re-measured live compression row (the old
   75 s proof retired per §5.5) + the reroute-with-image live proof join
   the dogfood queue — name both recipes in the lane record.

## The differential requirement, restated

Oracles from the `a1d88aa3a` pin, driving v4's REAL code:
`danger_routing_equivalence` over the dangerous-chat fixture (grow it for
the MIME-ordering arm), `file_attachment_tier3_equivalence` +
`attachment_anchor_equivalence`, the compression/memory/title tier-3
families, and unit transcriptions of v4's two new test files. Red-first
is the rule of the round: bug 106 and the budget numbers both changed
observable behavior — a family that stays green through the port proves
the family is blind, not that the port is right (ledger §5.2; grep the
NDJSONs for `90_000`-class evidence, the adapter's markers, the ordered
scan).

## Fixtures

The dangerous-chat fixture family grows the modality arm — this lane owns
regenerating it at the pin and re-running every consuming family (name
them). No committed-DB schema movement (the D23 work happened in D135).

## Shared contract

**With P4.D135 (binding):** starts at D135's tip; owns all further
movement of the shared files; D135 must not have pre-absorbed any
`a1d88aa3a` hunk. **With the siblings:** none — the Ownership table in
`p4.d134-lima-wsl2-retirement.md` binds.

## Ownership

Inherits P4.D135's rows in the binding table (one branch). **Version
bumps:** continue the branch's `quilltap-core`/`quilltap-harness`/SPA
bumps per commit; the unifier recounts.

## Verification gate before declaring done

The full gate for the STACKED BRANCH (this close is what the unifier
picks up): fmt; clippy both feature sets; full `cargo test --workspace`
with both lanes' oracle env vars (background, full log + sentinel); both
lanes' families by name, zero SKIP — D135's from its `65f5021c8` pin,
this lane's from the `a1d88aa3a` pin — with changed-bytes greps;
`npm test` + `npm run build` in `apps/web`; the branch's Playwright beats
(sole runner, port 4319). Per-commit rule: `.claude/commands/commit.md`.
