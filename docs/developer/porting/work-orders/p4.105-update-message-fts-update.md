# Work order P4.105 — `update_message` as v4's UPDATE under the FTS triggers: v5's DELETE + re-INSERT (byte-identical before `f45a517a9`, and NOT state-equivalent since — `_ad` + `_ai` renumber the message's `ftsId`, re-index unchanged content past `_au`'s guard, index a newly-eligible row `_au` would not, and move the base rowid) replaced by an UPDATE that sets every member column of the parsed event; a tier-2 family over a fixture that CARRIES the five FTS objects, red-first on the map row; `search_messages_global`'s `Err` where v4's `safeQuery` answers `[]` — maintenance, from `main`

**Status: UNIFIED on main at the `a2db63da7` round (2026-09-23) — CLOSED (Tier 1 items 1–4, Tier 2 item 5); Tier 3 item 6 restated.** The §3 review verified the UPDATE's SET list against v4's `$set: validated` keys (`chatId` excluded, no `updatedAt` column), the shared marshaling, the triggered fixture, the storage form in the comparand, and the `safeQuery` arm's fallback/level/fields — NO blocking finding. **Owed by name (the review's should-fix, not built here):** a `ui_search_routes` arm over the poisoned-table plant — the REST edge (`api/ui_search.rs:403`) was measured by reading only; `count_messages_with_text` / `find_messages_with_text` are `safeQuery` fallbacks in v4 (`0` / `[]`) and still propagate `Err`; v4's `updateMessage` is ITSELF a `safeQuery` (`null` + one ERROR on a parse failure where v5 answers `Err`). **Recorded:** the logged `error` field carries v5's `sqlite error: ` prefix (house style). _Lane close:_ **LANE CLOSED (2026-09-22) — Tier 1 items 1–4 and Tier 2 item 5 LANDED; Tier 3 item 6 restated (no merge).** `update_message` is one UPDATE through the INSERT's own member marshaling; NEW `chat_message_update_fts_tier2` red-first on 7 of 8 ops (op f2 does not move under DELETE+INSERT: the row held the max of both id spaces, so the re-INSERT reused them); `search_messages_global` answers `[]` + v4's ERROR on a throwing scan (a third `poisoned` venue in `chats_search`, red-first). **What the order got wrong, measured:** (1) the NULL-content plant for op (d) cannot run on v5 — v5's chat-wide `get_messages` fails on a NULL-content row where v4 skips it (`Skipping corrupted chat message`), and `find_event_value` walks it, so it is NOT findOne-shaped; op (d) re-planted as a TOOL→USER role change (a finding for `chats_messages_read.rs`, nobody's this round); (2) `DROP TABLE chat_messages_fts` cannot reach the `safeQuery` arm (the FTS path's own fallback catches it) — the plant is a rename of `chat_messages`; (3) the codec is deterministic, so op (c)'s mutation used a plaintext re-store (reddens only the row dump); (4) `message_reattribute` is red on main at the pin — an eighth fixture-vintage red on P4.103's `chat-dialogs-*` pair; (5) `regenerate_swipe_tier3`'s recipe runs zero tests (a stray second `--`), reported "ok" by the driver. Lane record: `status-log.md`.

## Preamble

- **v4 oracle baseline: `f45a517a9`** (drift-ledger §1). This lane is
  MAINTENANCE on an ABSORBED row (`f45a517a9`, FTS5 — ABSORBED(P4.D203 →
  P4.D204) with the `update_message` divergence recorded "for the next
  round" in BOTH order headers) and closes P4.D204's recorded
  `search_messages_global` item. Spec reads: `git -C ~/source/quilltap-
  server show f45a517a9:lib/database/repositories/chats-messages.ops.ts`
  (`:517` `updateMessage`), `:lib/database/backends/sqlite/query-
  translator.ts` (`:683` the UPDATE, `:497-521` the SET clauses),
  `:lib/database/chat-message-fts.ts` (the triggers), `:lib/database/
  chats-search.ts` (or wherever the global search's `safeQuery` sits —
  find it).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED (§R.3).** This lane pins the BASELINE ONLY:
  `/tmp/qt-v4-pin-p4105-f45a517a9`. Every regen from the pin; the NEW
  family's fixture builder runs v4's REAL `ensureChatMessageFtsSchema`
  at the pin (the `build-chats-search-fixture.ts:92` `QT_FIXTURE_FTS=1`
  precedent) so both sides write under the same five objects.
- **Sibling lanes:** P4.D212 (bug 161 — `services/context_summary*`,
  `chat_admin.rs`; no `db/` overlap), P4.106 (owns `salon_mutations`'s
  FILES — you RE-RUN that family for neutrality, never edit it), P4.104
  (`db/doc_mount_*`), P4.103 (the six pairs — the `salon-*` pair you
  re-run over is NOT among them), P4.D213, P4.D214.
- **Worktree:** `git worktree add <path> -b claude/p4-105-update-message-
  fts-<hash> main`; `CARGO_INCREMENTAL=0`.
- **Read before the work:** `crates/quilltap-core/src/db/chats_messages.
  rs:516-563` (`update_message` — the why-comment: "DELETE + re-INSERT of
  the merged event produces the byte-identical row while reusing the
  insert marshaling"; the body: `find_event_value` → overlay → `serde_
  json::from_value::<ChatEventInput>` → `DELETE FROM chat_messages WHERE
  id = ?1` (by id only, no chatId) → `insert_event(conn, chat_id, &event)`
  → `announce_transcript_change(chat_id)`); its NINE production callers
  (`api/salon.rs:1058 message_edit`, `api/files.rs:669`, `generators/
  rename.rs:762`, `services/carina_memory_extraction.rs:303`, `services/
  regenerate_swipe.rs:895`, `services/message_reattribute.rs:116`,
  `services/orchestrator.rs:1807`, `services/courier_transport.rs:635`,
  `services/memory_extraction_job.rs:367`) + the tests (`realtime/
  publish_sites.rs:1198,1218`, `chats_messages_ops_tier2_equivalence.rs:
  263`); `db/chat_message_fts.rs:132-167` (the three triggers VERBATIM —
  `_ai` on INSERT when `type='message' AND role IN ('USER','ASSISTANT')
  AND content IS NOT NULL`: map row + fts row keyed by the map's `ftsId`;
  `_ad` on DELETE when a map row exists: both deleted; `_au` AFTER UPDATE
  OF `content` WHEN `qt_text(new.content) IS NOT qt_text(old.content)`
  AND a map row exists: the fts row rewritten under the SAME `ftsId`);
  `crates/quilltap-harness/tests/compressed_column_write_sites_census.rs:
  46` (records "`update_message` is a DELETE + re-INSERT through `insert_
  event`" — the census moves); `chats_messages_ops_tier2_equivalence.rs`
  (`:263` drives the real `update_message`; `QT_ORACLE_CHATSMSGOPS`,
  `QT_FIXTURE_CHATSMSGOPS`; case `harness/oracle/cases/chats-messages-
  ops-tier2.ts:99-100`; builder `build-chats-messages-ops-fixture.ts` —
  NO FTS); `chat_message_fts_equivalence.rs:369-375` (RAW `UPDATE
  "chat_messages" SET "content" = ? WHERE "id" = ?` after `ensure_chat_
  message_fts_schema` `:498` — the corpus `chat-message-fts.json` has five
  `"op": "update"` rows, but never calls `update_message`); `chats_
  search_equivalence.rs` + `build-chats-search-fixture.ts:92` (the
  `QT_FIXTURE_FTS=1` variant runs v4's real `ensureChatMessageFtsSchema`);
  `db/chats_search.rs` (P4.D204's `search_messages_global` — propagates
  `Err` where v4's `safeQuery` answers `[]`; the P4.D204 header names it);
  memory notes `a-writer-that-rechunks-hides-its-own-defect` (enter
  BELOW the step that overwrites), `a-total-decoder-hides-a-lost-write`
  (put the STORAGE FORM in the comparand — here the map row and the fts
  rowid), `json-column-key-order`, `a-clear-to-null-write-needs-a-
  seeded-non-null`, `widened-sql-predicate-breaks-reduced-test-ddls`.

## Mandate

Make `update_message` an UPDATE, as v4's is, so the FTS triggers see what
v4's see — and prove it on a fixture that has them. Deliver: `update_
message` rewritten as `UPDATE chat_messages SET <col> = ?, … WHERE id = ?`
over EVERY member column of the parsed `ChatEventInput` (v4 `$set:
validated` sets every key of the parsed event — `id` and `content`
included; undefined binds NULL; compressed columns through the codec;
objects as JSON — `query-translator.ts:497-521`), reusing the INSERT's
marshaling helpers so the column list cannot drift from `insert_event`'s
(a census pins the two lists equal); the base rowid PRESERVED; `announce_
transcript_change` unchanged; the why-comment rewritten to say WHY the
DELETE+INSERT was retired (the four consequences, each pinned); a NEW
tier-2 family over a TRIGGERED fixture, red-first on the map row; the
write-sites census updated; `search_messages_global`'s `safeQuery`
fidelity. Must NOT: touch the triggers (`chat_message_fts.rs` is v4's
DDL verbatim), touch `insert_event`'s bytes, touch any caller, touch
`salon_mutations`'s files, touch the drift ledger.

## Survey-verified starting points (fresh survey 2026-09-22 on `main` `fc63905e` and at `f45a517a9` — do not re-derive)

### v4

1. `chats-messages.ops.ts:517 updateMessage` (delegated from `chats.
   repository.ts:492`): `findOne({ id: messageId, chatId })` → `{
   ...existingMessage, ...updates }` → `ChatEventSchema.parse` →
   `messagesCollection.updateOne({ id: messageId }, { $set: validated })`
   → `announceTranscriptChange(chatId)`. ⚠ The find is by `id` AND
   `chatId`; the update's WHERE is by `id` alone.
2. `query-translator.ts:683`: `UPDATE "${table}" SET ${setClauses.join(',
   ')} WHERE ${whereClause.sql}`; `:497-521` each key of `validated`
   becomes `"field" = ?` — an `undefined` value binds NULL, a registered
   compressed column goes through `encodeCompressed`, an object becomes
   JSON. The columns set are EVERY key of the parsed event. The rowid is
   preserved (it is an UPDATE).
3. The triggers (v4's DDL = v5's `chat_message_fts.rs:132-167`): on an
   UPDATE only `_au` can fire, and only when `content` changed by
   decoded text AND a map row exists. So in v4: (1) the `ftsId` is
   STABLE across edits; (2) an edit that leaves `content` unchanged
   touches the index NOT AT ALL; (3) a row that BECOMES eligible through
   an update (NULL content → text; or `type`/`role` — never, per the
   corpus) is NOT indexed until a rebuild/reconcile; (4) the base rowid
   is stable.

### v5

4. `update_message` (`:526`): DELETE by id → `insert_event`. Under the
   triggers (INFERRED at survey, to be MEASURED red-first): `_ad` then
   `_ai` fire; the map row is deleted and re-minted → a NEW `ftsId`; an
   unchanged-content edit still re-indexes; a NULL→text edit gets
   indexed by `_ai`; the base rowid changes. Search RESULTS are
   unaffected (same tokens under a new rowid) — which is why no family
   has seen it.
5. Coverage: `chats_messages_ops_tier2` (real `update_message`, NO
   triggers), `salon_mutations` (`message_edit` → `update_message`, over
   the committed `salon-*` pair, NO triggers), `chat_message_fts_
   equivalence` (raw UPDATEs under triggers — never the repo method),
   `chat_message_fts_reconcile` (inserts only), `chats_search` (reads).
   **No family runs `update_message` over a fixture that carries the
   triggers.** `test_support.rs` has no FTS ensure helper; production
   gets the objects from `host.rs:1461` (`reconcile_chat_message_fts` →
   `chat_message_fts_reconcile.rs:132` ensure).
6. `compressed_column_write_sites_census.rs:46` names the DELETE +
   re-INSERT; the count/notes move.

## Tiered deliverables

### Tier 1 — must land

1. **`update_message` as an UPDATE** over every member column, the
   marshaling SHARED with `insert_event` (one column list, one bind
   list — a `chat_messages_member_columns` const or a helper both use;
   red-first proof that the two lists are the same set), WHERE by `id`;
   `find_event_value` by `id` AND `chat_id` as today (v4's `findOne`
   shape — measure v5's current predicate); the why-comment rewritten
   with the four consequences and the `f45a517a9` sha.
2. **NEW `chat_message_update_fts_tier2_equivalence`** (tier 2): a jest
   case over a fixture built at the pin WITH v4's real `ensureChatMessage
   FtsSchema` (extend `build-chats-messages-ops-fixture.ts` with the
   `QT_FIXTURE_FTS=1` arm, or a sibling builder — say which), driving
   v4's REAL `updateMessage` and v5's `update_message` over the same
   ops; the comparand = the `chat_messages` row (+ its `rowid`), the
   `chat_messages_fts_map` table (`ftsId`, `messageId`), and a dump of
   `SELECT rowid, content FROM chat_messages_fts` (contentless — read
   it back through the map + a `MATCH` per row, or `fts5vocab`; measure
   what is readable on both sides and pin the same shape). Ops, each
   RED-FIRST on the DELETE+INSERT tree: (a) edit `content` on an indexed
   row → same `ftsId`, new fts text; (b) edit a NON-content column
   (`attachments`, `reasoningContent`) on an indexed row → map + fts
   UNTOUCHED (the `_au` guard); (c) edit `content` to the SAME text →
   untouched (the `IS NOT` guard on decoded text — plant a COMPRESSED
   cell ≥ 512 bytes so `qt_text` is what compares, not the BLOB); (d)
   NULL `content` → text on a `message`/`USER` row → NOT indexed (no map
   row) on both sides; (e) an edit on a `system`-type row → nothing; (f)
   two edits in sequence → one `ftsId` throughout; (g) a rowid pin:
   `SELECT rowid FROM chat_messages WHERE id = ?` unchanged across an
   edit.
3. **`compressed_column_write_sites_census.rs`** moved (the UPDATE is a
   write site on the two compressed columns — the count and the note),
   with the arithmetic.
4. **Neutrality legs at the pin:** `chats_messages_ops_tier2` (trigger-
   free — byte-identical rows before and after; the ONLY visible delta a
   rowid, which that family does not read — say so), `salon_mutations`
   (`message_edit` over the trigger-free `salon-*` pair — unchanged),
   the nine callers' families (`message_reattribute`, `regenerate_swipe_
   tier3`, `orchestrator_tier3`, `courier_transport_tier3`, `carina_*`,
   `memory_extraction_*`, `rename_*`, `files_*` — list by grep; re-run
   each from the pin; all predicted neutral).

### Tier 2 — should land

5. **`search_messages_global`'s `safeQuery` fidelity** (`db/chats_search.
   rs`): where v4 wraps the query in `safeQuery` and answers `[]` on a
   thrown query, v5 propagates `Err` → measure v4's exact wrap (which
   arms — the FTS shape, the LIKE fallback, both?) and match it: an
   empty list + the logged line (capture-pinned) on a poisoned fixture
   (a `DROP TABLE chat_messages_fts` on the copy is the plant); a
   `chats_search_equivalence` row RED-FIRST (v5 500s / errors, v4 answers
   `[]`). ⚠ Measure through the REST edge too (`ui_search_routes`) —
   the error today may already be swallowed at the edge; the divergence
   is only real where a caller sees `Err`.

### Tier 3 — explicit deferrals (loud, typed, never silent)

6. `chat_message_fts_equivalence`'s raw-UPDATE rows stay as they are
   (they prove the trigger; the new family proves the repo method) —
   restated, no merge.

## The differential requirement, restated

The new family is the port's proof: v4's REAL `updateMessage` vs v5's
`update_message` over ONE triggered fixture at the pin, with the STORAGE
FORM (map row, fts rowid, base rowid) in the comparand — red-first on
(a)/(b)/(c)/(f)/(g) under DELETE+INSERT, green under the UPDATE. Mutation
proofs (each must redden exactly its op): drop `content` from the SET
list (op a reddens); include a non-member column (the column-list census
reddens); set `content` even when unchanged with a re-encoded BLOB (op c
reddens ONLY if `qt_text` disagrees — so the proof must re-encode with a
DIFFERENT brotli quality; if the codec is deterministic, record that op c
is guard-proven by the trigger family instead); revert to DELETE+INSERT
(a, b, f, g redden); WHERE by `id` AND `chatId` (nothing reddens — record
that v4's WHERE is by id alone and the difference is unobservable); swap
`Err` for `[]` on the search arm without the log (the capture pin reddens).

## Fixtures

Consumes: the pin's jest-built `/tmp` DBs (the ops fixture ± the FTS
arm; the search fixture). Delivers: the new family's builder arm + case +
corpus. Widens NO committed pair. Dependents: the families in Tier 1
item 4 — yours to re-run.

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `a2db63da7` bug-161/162 drift catch-up + maintenance round: **P4.D212 ∥
P4.D213 ∥ P4.D214 ∥ P4.103 ∥ P4.104 ∥ P4.105 ∥ P4.106**, ordered
2026-09-22. Seven lanes, seven worktrees, seven lane branches, **every one
cut from `main` (`fc63905e`)** — no keystone this round: S and P4.D204 are
already on main, which is what makes the fixture-vintage heal possible at
last. The unifier cherry-picks in the order **P4.D212 → P4.D213 → P4.D214 →
P4.103 → P4.105 → P4.104 → P4.106** (the drift first, its SPA half after
the contract it consumes, the CLI beside them; the heal before every
family that reads a widened pair is re-run; `update_message` before the
seam that shares nothing with it but the gate; the coverage lane LAST
because its grown corpora are re-recorded from the NEW baseline pin with
P4.D212's fold present), gates ONCE from the new baseline pin, and
`merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`f45a517a9`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`a2db63da7`** ("Fix
  bug 162: CLI raw-SQL path opens without qt_text() (#63)", `4.10.0-dev.
  63`, 2026-09-21 22:20) — **THREE commits past the baseline**, all three
  out of ONE live Friday chat: `4e1a8e061` (docs: bugs 161/162 filed +
  the summarizer-name design — NO-PORT? → P4.D212 with mirrors),
  `e7821606f` (bug 161 + the PORT-NEW rebuild-summary — PORT → P4.D212 ∥
  P4.D213), `a2db63da7` (bug 162 — PORT → P4.D214). Every row now
  `ORDERED(…)`. Spec reads: `git show <sha>:<path>` for the post-commit
  files and `git show <sha> -- <path>` for the hunks — **never the commit
  message** (§R.4). v4 `bugfix` tip `1a2b2164c` and `release` tip
  `8fbf2afe0` are unmoved; still no `release: 4.10.0` squash. The checkout
  sat on `main`, CLEAN, at the planning-start AND planning-close probes
  (2026-09-22 — the §2 probe PASSED twice; the ledger stood and nothing
  was re-derived). **At unification the baseline MOVES to `a2db63da7`**;
  the two code rows retire `ABSORBED(…)` and the docs row `NO-PORT-
  RATIFIED(…)` on P4.D212's file list.
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log a2db63da7..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Pass = branch `main`, HEAD `a2db63da7`, both logs EMPTY, tree
  CLEAN.** Any mismatch — a moved HEAD (a new commit is a NEW drift row,
  not a pass), a non-empty log, ANY dirty path, the checkout off `main` —
  is a STOP: report it and do nothing else. A lane never writes the
  ledger. (v4 landed three commits in one evening out of one chat this
  week; expect the probe to fail mid-round, and expect the human to
  record a waiver in the ledger's §1 the way the `89fcc3c0d` round did —
  a lane resumes ONLY on a recorded waiver, never on its own reading of
  the dirt.)
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1), absolutely.
  HEAD is three commits past the baseline and one of them rewrites the
  fold/episode paths whose families regenerate routinely. Every regen
  runs from a LANE-UNIQUE detached worktree per ledger §5.1 (`git -C
  ~/source/quilltap-server worktree add --detach /tmp/qt-v4-pin-<order>-
  <sha> <sha>`, the THREE symlink classes — root `node_modules`,
  `packages/quilltap/node_modules`, every `plugins/dist/*/node_modules` —
  then `cd` into it; never stash or checkout-switch the human's repo;
  `git worktree list` before each batch; **verify the pin by `git -C
  "$PIN" rev-parse HEAD` AND by `ls -ld` of the path** — build every pin
  path from a quoted literal, never an unsplit zsh variable; then grep
  the fresh NDJSON for a marker only one tree has; check the file is
  non-empty before believing a diff). **The two DRIFT lanes (P4.D212,
  P4.D214) pin the TARGET `a2db63da7`** (`/tmp/qt-v4-pin-<order>-
  a2db63da7`) for every family their port MOVES **and the BASELINE
  `f45a517a9`** (`/tmp/qt-v4-pin-<order>-f45a517a9`) for their neutrality
  legs; P4.D213 records no Rust oracle (a pinned worktree only if it
  records a client string). **The four MAINTENANCE lanes (P4.103, P4.104,
  P4.105, P4.106) pin the BASELINE `f45a517a9` ONLY** — their families
  must not move at the baseline; a family that ALSO moves at `a2db63da7`
  (`orchestrator_tier3`, `memory_pipeline_jobs_tier3`, `courier_images_
  routes` — the three that reach the real fold) is P4.D212's to re-record
  and the UNIFIER's to re-run from the new baseline pin with every lane's
  growth present. Pin verification markers: the target-pinned
  `help_tree_equivalence` NDJSON carries `Rebuilding the Running Summary`
  (`help/chats.md` at `e7821606f`); the baseline-pinned one does not
  (`grep -c` both). The target pin's fixtures are compressed exactly as
  the baseline's (P4.D203's S is on main — every lane's tree reads them).
  The sweep driver is the sanctioned per-family path (`harness/tools/
  recipe_sweep.py --run <family> --v4 "$PIN" --v5w <ABSOLUTE worktree>`);
  never run two sweeps concurrently; a family that reports `SKIP` in
  0.00 s did not run. Ledger §5.2's silent-stale-pass discipline applies
  to every regen (`rm -f` the fixture first; read the builder's last
  line; `grep -c` the changed bytes in the fresh NDJSON — MUST be > 0).
  Every lane stages its regen outputs under a LANE-PRIVATE directory
  (`/tmp/<order>/…`) and leaves the committed recipe headers canonical (a
  committed recipe NEVER names a `/tmp` pin — the driver's `--v4` is the
  pin). Every jest oracle writes ZERO `llm_logs` rows (`jest.setup.ts:379`
  mocks the logging service). `QT_NODE` in any env block is a REAL path
  (`~/.nvm/versions/node/v24.13.1/bin/node`), never `$N/node` — the
  `f45a517a9` gate's `cli_differential` artifact.
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). Three
  measured misdescriptions this round, one per drift lane: (a)
  `e7821606f`'s body says the fold-episode pass "was already getting this
  right" — true of NAMED characters only; the consolidation moves the
  gate from `if (character)` to `if (character?.name)`, so an EMPTY-NAME
  character's label changes on that side too, and v5's loop matches the
  PRE-fix gate (P4.D212's red-first episode arm). (b) The same body says
  the API "refuses a running autonomous room" and stops; the HUNK also
  refuses with 400 when the user has NO connection profiles, and does
  NOT check cheap-LLM settings the way regenerate-title does — carry the
  hunk's two refusals, not the sibling's three (P4.D212). (c)
  `a2db63da7`'s title says the raw-SQL path "opens without qt_text()" —
  v5's path ALREADY registers it (P4.D203); what the hunk moves is the
  OPENER (one, not two) and the failure STRINGS (per-target friendly
  names; bare messages on two arms), a Tier R comparand (P4.D214). And
  one in the PLAN: `phase-4.md:6659`'s "the widen brings the FTS
  triggers" is FALSE — the migrator is ALTER-only; the pairs stay
  trigger-free on both sides (P4.103). Re-verify even where an order
  flags the trap — the order's paragraph is the same kind of prose, and
  so is a previous lane's record or a doc comment in the tree. Carry a
  v5-side code comment naming the sha where the prose misleads.
- **§R.5 — Designed reds and pre-existing reds: measure, don't assume**
  (ledger §5.4). No convergence pin trips this round (both bugs are v4's
  own filings — ledger §3, the negative §5.4 check). The DESIGNED reds:
  **P4.D212** — `context_summary_service_tier3` red at the target on
  every fold op's speaker labels (`USER:` → `User:` etc.) AND on the
  prompt's last sentence; `fold_episode_tier3` neutral on the existing
  corpus and red-first on the grown empty-name arm; `orchestrator_tier3`
  / `memory_pipeline_jobs_tier3` / `courier_images_routes` MOVE at the
  target on every case whose fold fires (re-run and record; you edit none
  of their files); the NEW families red-first on unported main;
  `dispatch_wrong_type_census` red until 446. **P4.D214** — Tier R RED at
  the target on the grown wrong-key cases (`Cannot open database:` vs
  `Cannot open main database:`) and on the bare-message arms until the
  opener moves; the SAME cases RED at the baseline pin after the move
  (the movement is v4's — record both). **P4.103** — the SEVEN standing
  fixture-vintage reds (`subprompts_routes`, `subprompts_prompt_tier2`,
  `chat_delete`, `character_wizard_tier3`, `search_replace`, `profile_
  routes`, `projects_routes`) RED from the pin on the un-widened pairs
  and GREEN after the widen — the pre-widen counts are the measurement;
  the "six earlier ones" the previous lists carried are NOT red (P4.94
  closed them — a stale carry-over, corrected). **P4.104** — every image
  row it plants is red-first on the un-wired tree (`image/png` where v4
  answers `image/webp`); the readback unit test red-first. **P4.105** —
  the new family red-first on the map row's `ftsId` under DELETE+INSERT;
  `chats_messages_ops_tier2` + `salon_mutations` NEUTRAL (trigger-free
  fixtures). **P4.106** — each Inform arm red-first where v5 can be made
  to disagree (the lane lists which; "the existing corpus stays green" is
  not a proof); the SDK guard green at planning (every installed version
  matches the recordings — measured 2026-09-22). A red in a family a lane
  did NOT touch is a NEW finding to classify at both pins before any
  source is touched, and it is probably not that lane's. **The seven
  fixture-vintage reds are P4.103's to close this round** — every other
  lane whose family reads one of the six pairs records the red by name
  at lane start and expects it green at unification; nobody else widens
  a pair.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with
  a capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest`
  is shared) and its silence leg; a fixture change lands with the regen
  of every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that
  SURVIVES as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`). A dispatch WIRE
  test through `POST /api/dispatch` is the only instrument that sees a
  serde collapse; a family that calls the handler directly cannot. A new
  verb lands with its `dispatch_wrong_type_census` row and the census
  recount in the comment. A NEW pure module lands with a tier-1 corpus
  recorded from v4's REAL module through a tsx case, never a
  transcription of v4's test file. A v4 unit test that MOCKS a repository
  is mirrored as a real-DB arm with PLANTED rows, never as the same mock
  (`a-v4-mock-is-not-v4-in-a-route-family`).
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane. P4.D214 ALSO updates P4.D210's
  status header (its declared gap closed) and P4.106 updates P4.D205's,
  P4.D208's, P4.D209's and P4.D211's (the items they closed) — those
  headers are the ONE cross-order edit each lane may make, one sentence
  each, naming this round.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed on each commit per `.claude/commands/commit.md`; the unifier
  RECOUNTS as base + the sum of every lane's bumps (the silent-auto-merge
  trap: identical bumps merge as ONE — it fires every round). Base at
  planning: core 0.0.992, harness 0.0.890, host 0.0.146, web 0.0.170,
  cli 0.0.25, tauri 0.0.7, fixture-sanitizer 0.0.4, SPA 0.5.747.
  Expected: **core** by P4.D212, P4.104, P4.105 (and P4.106 ONLY if a
  `cfg(test)` seam lands in core — say so); **harness** by every Rust
  lane (P4.103 only if `test_support` moves — it should not);
  **web** by P4.D212 (the census + the wire test), P4.103 (the fixtures
  live in `crates/quilltap-web/tests/fixtures/`), P4.106 (the wire test);
  **host** by P4.104 (only if `image_codec.rs`/`host.rs` moves — say so);
  **cli** by P4.D214 ONLY; **the SPA** (`apps/web/package.json`) by
  P4.D213 ONLY; **tauri** and **fixture-sanitizer** by NOBODY. No lane
  touches the workspace `Cargo.toml` dependency block; every lane's
  `Cargo.toml` deltas must be version-only — the unifier audits each
  before any resolution (the `--theirs` clobber trap).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  each lane pre-lists, in its lane record with byte counts at
  `a2db63da7`, the mirror paths its rows move. This round's mirror rules
  in one place: `4e1a8e061` + `e7821606f` → `docs/v4/developer/features/
  complete/context-summary-speaker-names.md` (9,712 — the FINAL path
  `e7821606f` renames to, never the birth path), `docs/v4/developer/bugs/
  fixed/bug-161-summary-invents-a-name.md` (6,100), `docs/v4/developer/
  API.md` (219,745 → 221,285), `docs/v4/developer/bugs.md` (302,517 →
  306,475 — carries BOTH bugs' rows; mirrored ONCE); `a2db63da7` →
  `docs/v4/developer/bugs/fixed/bug-162-cli-raw-sql-no-qt-text.md`
  (6,486), `docs/v4/packages-quilltap-README.md` (34,707 → 34,898 — the
  two-line `qt_text()` note; measured as the ONLY delta). `docs/
  CHANGELOG.md`, `README.md`, `CLAUDE.md`, `.claude/commands/update-
  documentation.md`, every `package.json`/`package-lock.json` stamp →
  NO-PORT, the standing class; Tier R at the pin for the `packages/
  quilltap` stamp (P4.D214 runs it). The `help/` count does NOT move
  this round (`help/chats.md` is a byte-copy; 126 stays 126 in BOTH
  literals — `help_tree_embed_guard.rs:65` and `host_help_docs_boot.rs:
  91`); `help_tree_equivalence` at the target pin is the proof the tree
  is v4's.
- **§R.10 — Fences, ownership edges and the pre-declared spills.** (a)
  **`api/types.rs` and `api/engine.rs`:** P4.D212 adds `Request::
  ChatRebuildSummary` inside a `// === P4.D212 ===` … `// === end P4.D212
  ===` fence appended at the END of the `Request` enum and its dispatch
  arm inside the same fence at the END of the engine match (NO new
  `Response` variant — it answers through `Response::ChatAdmin`). P4.104
  edits the BODIES of EXISTING dispatch arms in `engine.rs` (threading
  the codec argument) and adds NO variant, NO fence; it never touches
  `types.rs`. Nobody else touches either file. The unifier picks P4.D212
  first; the hunks are distant. (b) **`crates/quilltap-web/tests/
  dispatch_wrong_type_census.rs`:** P4.D212 alone moves it (445 → 446,
  `chat_id`); P4.106 adds a wire TEST FILE, not a verb, so the census
  stays. (c) **`orchestrator_tier3` / `memory_pipeline_jobs_tier3` /
  `courier_images_routes`:** P4.106 OWNS the orchestrator family's files
  (corpus, builder, case, test); P4.D212 RE-RUNS all three at its target
  pin and edits none of them — a movement it predicts is recorded, a red
  it cannot explain is a finding for P4.106's owner to read at
  unification. (d) **`salon_mutations`:** P4.106 owns its files; P4.105
  re-runs it (neutral); P4.D212 lists it as a neutrality leg (re-run
  only). (e) **`chat_export` + `chat-dialogs-*`:** P4.103 owns the PAIR
  (the `.db` files); P4.106 owns the family's CASE (it plants informs on
  the per-run COPY); at unification the grown case runs over the widened
  pair from the new baseline pin. (f) **`qtap_import`:** P4.106 owns it
  whole (the two-link-blob bundle row); P4.104 does not touch it (the
  import's `normalize_images: false` is the ONE untouched site). (g)
  **`db/doc_mount_file_links.rs` + `db/doc_mount_blobs.rs`:** P4.104's;
  P4.105's is `db/chats_messages.rs` + `db/chats_search.rs`; P4.D212's is
  `services/context_summary*` + `services/fold_episode_pass.rs` +
  `services/chat_admin.rs` + NEW `services/speaker_names.rs`. (h)
  **`crates/quilltap-cli/**`:** P4.D214's alone. (i) **`apps/web/**`:**
  P4.D213's alone. (j) **Pre-declared loud out-of-mandate edits** (the
  `a-required-field-on-a-shared-struct-crosses-lane-ownership` rule: do
  the edit, keep it to the hunk, mark it `// P4.xxx OUT-OF-MANDATE —
  <owner> preserves`, add a guard test where the owner must carry it,
  audit with `git diff main -- <path>` at lane close): **P4.104 →
  `api/engine.rs`** (existing arms' bodies, above); **P4.105 →
  `crates/quilltap-harness/tests/compressed_column_write_sites_census.rs`**
  (its `:46` note names the DELETE+INSERT — the census moves with the
  port; P4.D203's file by history, nobody's this round); **P4.106 →
  `services/message_context.rs`** ONLY if the tier-1 `is_record_only_
  message` family needs the function `pub(crate)` → `pub` (a visibility
  widening, one line). If a lane finds it needs another lane's file
  beyond these, it STOPs on that item and records the shape for the
  unifier.
- **§R.11 — SPA behaviour moves in ONE lane.** P4.D213 is the only lane
  touching `apps/web/**`. It consumes ONE server contract it cannot run
  against until unification (§S.1); it builds against the pinned shape
  with the SPA's mocked transport, adds its live beat gated behind the
  NAMED constant the UNIFIER flips (`P4D212_SERVER_LANDED` — the `P49K*_
  SERVER_LANDED` precedent; a DEFINED-but-unimplemented verb defeats a
  capability probe), and flips nothing itself. The unified gate runs the
  full Playwright suite once against the release binary with the flipped
  beat's first live run as its own step; the documented intermittents and
  the standing parks stand as named in the previous round's gate (329
  passed / 1 failed / 6 skipped, the one red the pre-existing P4.66
  bubble beat, 2/2 by file). **The help re-vendor table:** `help/chats.md`
  → P4.D212 (a byte-copy at the target pin, 39,682 bytes; the count
  unmoved). After the round, `help/**` is 126 files and `help_tree_
  equivalence` at the target pin is the proof (the embed guard compares
  embedded-vs-DISK and cannot see a stale re-vendor).
- **§R.12 — Committed fixtures: P4.103 WIDENS six pairs; nobody else
  touches a committed pair, and nobody REBUILDS one** (a rebuild at the
  pin re-encodes every ≥ 512-byte `chat_messages`/`conversation_chunks`
  cell and retires the P4.D203 Class-3 neutrality proofs over `chat-
  dialogs-*`/`salon-*`/`system-data-*`; a WIDEN through v4's ALTER
  statements does neither). The ONE rebuild this round: P4.103's
  `chat-compressed-mount.db` (the 0-byte file — an empty mount-index
  schema carries no cell). The ONE new committed artifact: P4.106's
  `qtap-import-two-link-blob.qtap` bundle. Every other lane's fixtures
  are the pin's jest-built `/tmp` DBs or per-run COPIES of a committed
  pair. `.db-journal` files are never fixtures — delete strays (the
  migrator leaves one per apply).

## §S — Shared contract (binding; byte-identical in every order of this round)

### §S.1 — `chatRebuildSummary` (P4.D212 provides; P4.D213 consumes)

v4 `POST /api/v1/chats/[id]?action=rebuild-summary` (`e7821606f`,
`app/api/v1/chats/[id]/actions/rebuild-summary.ts`). Dispatch-only on v5
(the `regenerate-title` precedent — the REST edge's pointer sentence is
unchanged).

```
Request  (POST /api/dispatch):  { "type": "chatRebuildSummary", "chatId": "<uuid>" }
Response (200):                 { "success": true, "jobId": "<uuid>" }        — Response::ChatAdmin(Value), key order success, jobId
Refusals, in v4's order:
  404  { "error": "Chat not found" }                                          — the chat read
  409  { "error": "Pause the room before rebuilding its summary." }           — chatType == "autonomous" && runState == "running" (a PAUSED room passes)
  400  { "error": "No connection profiles available" }                        — the user has no connection profiles (NO cheap-LLM-settings check)
  500  { "error": "Failed to rebuild the summary" }                           — any failure after the refusals; no job row, no publish when the update fails
Side effects on 200, in order:
  chats.update(id, { contextSummary: null, summaryAnchorMessageIds: [], lastSummaryTurn: 0, updatedAt: <now> })   — ONE update; lastFullRebuildTurn UNTOUCHED
  background_jobs ← CONTEXT_SUMMARY { chatId, connectionProfileId, forceRegenerate: false } at priority 0, maxAttempts 3 (v4's enqueueJob defaults; NO dedupe)
  realtime: topic `chats`, id = chatId   (the SPA's existing chatKeys.detail invalidation re-reads the chat — no new key)
  log INFO  "[Chats v1] Context summary cleared for rebuild"   { chatId, jobId, connectionProfileId }
  log ERROR "[Chats v1] Failed to rebuild context summary"     { chatId } + the error   (the 500 arm)
Profile choice: the first CHARACTER participant's connectionProfileId when it names an available profile, else the user's first profile.
```

Rust (P4.D212, inside the `// === P4.D212 ===` fence at the END of `Request`):

```rust
/// Discard the chat's running context summary and let the fold cadence
/// rebuild it from turn 1 (v4 `POST …?action=rebuild-summary`, `e7821606f`).
#[serde(rename_all = "camelCase")]
ChatRebuildSummary {
    chat_id: String,
},
```

TypeScript (P4.D213, `apps/web/src/app/core/core-contract.ts`, beside
`ChatRegenerateTitleRequest`; added to the `CoreRequest` union):

```ts
export interface ChatRebuildSummaryRequest {
  type: 'chatRebuildSummary';
  chatId: string;
}
```

SPA gate constant (P4.D213, `apps/web/e2e/salon-dialogs-flow.spec.ts`):
`const P4D212_SERVER_LANDED = false;` — flipped to `true` by the UNIFIER.

### §S.2 — Nothing else crosses a lane boundary this round

P4.D214's opener is CLI-internal; P4.103 changes bytes no test reads as a
contract; P4.104's codec parameter is core-internal (the engine's existing
`blob_webp`); P4.105's UPDATE is repository-internal; P4.106 adds tests.
A lane that discovers it needs a second contract STOPs on that item and
records the shape for the unifier.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D212** — bug 161 server + the rebuild-summary verb + the `4e1a8e061` ratification | NEW `crates/quilltap-core/src/services/speaker_names.rs` + its `mod` line; `services/context_summary.rs`; `services/context_summary/{tasks,prompt_text}.rs` (`prompt_text.rs` REGENERATED, never hand-edited); `services/fold_episode_pass.rs`; `services/chat_admin.rs` (the new verb + a `conflict` helper if none exists); `api/types.rs` + `api/engine.rs` INSIDE its `// === P4.D212 ===` fence ONLY; `help/chats.md` (byte-copy); `crates/quilltap-web/tests/dispatch_wrong_type_census.rs` (445 → 446); NEW `crates/quilltap-web/tests/chat_rebuild_summary_dispatch_wire.rs`; `context_summary_service_tier3_equivalence.rs` + `fold_episode_tier3_equivalence.rs` + their cases/builders/corpora; NEW `speaker_names_equivalence.rs` + `chat_rebuild_summary_equivalence.rs` + their cases/builders/corpora; `chat_admin_routes_equivalence.rs` + its case (grown); this order's header | `apps/web/**` (P4.D213), `crates/quilltap-cli/**` (P4.D214), every committed fixture pair (P4.103), `orchestrator_tier3`/`memory_pipeline_jobs_tier3`/`courier_images_routes`/`salon_mutations`'s files (re-run only — P4.106 owns the first and last), `db/**`, the REST edge (`wardrobe_routes.rs` — no new arm), the drift ledger |
| **P4.D213** — bug 161's SPA half | `apps/web/**` whole: `core/core-contract.ts` (§S.1), `chat/chat-admin.api.ts` (+ spec), `chat/sidebar/organize-section.ts` (+ spec), `screens/salon/salon-conversation.ts` (the handler; + spec), `e2e/salon-dialogs-flow.spec.ts` (the gated beat), `apps/web/package.json`; this order's header | Everything outside `apps/web/**`; the drift ledger |
| **P4.D214** — bug 162: the ONE CLI opener, the Tier R comparand, P4.D210's live rows | `crates/quilltap-cli/src/dbopen.rs` (widened), `src/db_cmd.rs` (the private opener DELETED; the friendly names; the bare catch), `src/{db_characters,docs_cmd,restore_key,sync_cmd}.rs` ONLY if the repointing needs a call-site edit (strings unchanged — Tier R says so); `crates/quilltap-cli/tests/cli_differential.rs` (the grown master DDL, the pre-hooks, the new `db` cases, the canned-stub `sync` cases); the P4.D210 order's status header (one sentence); this order's header | `crates/quilltap-core/**` (the UDF's home — call it, never edit it), `apps/web/**`, every committed fixture pair, the drift ledger |
| **P4.103** — the fixture-vintage heal | `crates/quilltap-web/tests/fixtures/{subprompts,chat-delete,character-generators,chat-dialogs,profile,groups-projects}-*.db` (widened in place), `chat-compressed-main.db` (widened), `chat-compressed-mount.db` (REBUILT), the three `chat-compressed-*.meta.json` sidecars, `chat-delete-llmlogs.db`/`chat-compressed-llmlogs.db` (the `llm_logs` recipes), `inspector-llm.db` ONLY if Tier 2 item 6 plants without a rebuild; `harness/oracle/fixtures/migrate-memories-fixture-columns.ts` ONLY to append this run's recipe to its header; `harness/tools/sweep-results/<this run>.json`; this order's header | Every `.rs` file (a red after the widen is a FINDING, recorded, not fixed here), every test file's logic, `fresh_schema.json`, every other committed fixture, `apps/web/**`, the drift ledger |
| **P4.104** — the image-normalization seam | `crates/quilltap-core/src/db/doc_mount_blobs.rs` (the readback fix + the codec pass-through), `db/doc_mount_file_links.rs` (the `:570-577` doc comment ONLY — the constructors stand), `services/mount_index/normalize_blob_image.rs` (the OPEN note → the closed record), the ten write sites (`tools/doc_edit/blob.rs`, `photos/{character_gallery_service,user_gallery_service,save_image_to_album}.rs`, `services/image_job_storage.rs`, `services/mount_index/{file_ops,store_file}.rs`, `services/file_storage.rs`, `tools/generate_image.rs`) and the API handlers that call them (`api/{photos,chat_media,mount_files,…}.rs` — the lane's census names them), `api/engine.rs` EXISTING arms' bodies (§R.10(j)), `crates/quilltap-host/src/image_codec.rs` ONLY if a trait impl is missing; NEW `crates/quilltap-harness/tests/blob_write_sites_census.rs`; the families its census names + their corpora (+ image files under `harness/oracle/fixtures/<family>/`); `normalize_blob_image_equivalence.rs` (grown); this order's header | `services/mount_index/sync/**` (already wired), `services/quilltap_import/**` (the `false` site — P4.106 owns `qtap_import`), `api/types.rs`, the P4.D212 fence, `db/chats_*` (P4.105), every committed fixture pair, `apps/web/**`, the drift ledger |
| **P4.105** — `update_message` as an UPDATE + the search `safeQuery` arm | `crates/quilltap-core/src/db/chats_messages.rs` (`update_message` + the shared column-list helper; `insert_event`'s BYTES unchanged), `db/chats_search.rs` (the `safeQuery` arm), `crates/quilltap-harness/tests/compressed_column_write_sites_census.rs` (§R.10(j)); NEW `chat_message_update_fts_tier2_equivalence.rs` + its case + the builder arm (`build-chats-messages-ops-fixture.ts`'s FTS variant or a sibling) + corpus; `chats_search_equivalence.rs` + its case (the poisoned-fixture row); this order's header | `db/chat_message_fts.rs` (v4's DDL verbatim), every caller of `update_message`, `salon_mutations`'s files (re-run only), `db/doc_mount_*` (P4.104), every committed fixture pair, `apps/web/**`, the drift ledger |
| **P4.106** — the Inform coverage remainder + the three smalls | `build_context_tier3_equivalence.rs`, `orchestrator_tier3_equivalence.rs`, `primary_stream_tier3_equivalence.rs`, `regenerate_swipe_tier3_equivalence.rs`, `turn_transcript_equivalence.rs`, `salon_mutations_equivalence.rs`, `chat_export_equivalence.rs`, `qtap_import_equivalence.rs`, `system_backup_equivalence.rs`, `system_restore_equivalence.rs` (+ `_state`, `_guards`), `system_import_state.rs` + ALL their cases/builders/corpora; NEW `is_record_only_message_equivalence.rs` + case + corpus; NEW `crates/quilltap-web/tests/chat_informs_dispatch_wire.rs`; NEW `scenario_seeded_summary_heal_equivalence.rs` + case + spec; NEW `provider_sdk_version_guard.rs`; NEW `harness/oracle/fixtures/qtap-import-two-link-blob.qtap` (+ its builder); `services/message_context.rs` ONLY per §R.10(j); a `cfg(test)` counting seam in `test_support.rs` if item 7 needs one; the P4.D205 / P4.D208 / P4.D209 / P4.D211 status headers (one sentence each); this order's header | Every production `db/**`, `services/**`, `api/**` file bar the §R.10(j) line, `apps/web/**`, `crates/quilltap-cli/**`, every committed fixture PAIR (plant on COPIES), the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix, beyond the pre-declared spills in §R.10(j) — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note). Budget 50–70 GB of disk per long lane
(`df -h ~` showed 147 GB free at planning; seven worktrees is seven
`target/` dirs — run at most FOUR Rust lanes concurrently and reclaim
`target/debug/incremental` between waves).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean (Rust lanes); `npm run lint` clean
   incl. the qt-class guard (P4.D213).
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`)
   (Rust lanes).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0
   where the order predicts a change; a recorded `0` where it predicts
   none), and the red-first steps recorded with the pre-fix counts.
   P4.D213: `npm test` whole plus its new/changed spec files by name.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp` / by-label diff) where the family is
   deterministic across regens; where it is not (`regenerate-at-both-
   pins-and-cmp-is-not-universal`), compare the normalized rows by case
   and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`
   (P4.103's `.db` reverts are the ONE exception — the pair is the
   artifact).
7. `cargo build --workspace --release` — clean (Rust lanes; the Playwright
   venue prefers `target/release`); `npm run build` clean (P4.D213).
8. `cargo test --workspace --no-fail-fast -- --nocapture` with the lane's
   env block (its own families' vars; every other family's var WITHHELD
   so it SKIPs honestly rather than failing on a deleted `/tmp` path):
   every binary's `test result:` line read, zero failures bar the seven
   standing fixture-vintage reds named in §R.5 (recorded by name if their
   vars are in the block — P4.103 expects them GREEN), the lane's
   families confirmed RUN by non-zero duration (Rust lanes).
9. **Source censuses** that the change moves (`dispatch_wrong_type_census`
   for P4.D212; `compressed_column_write_sites_census` for P4.105; the
   NEW `blob_write_sites_census` for P4.104; `help_tree_embed_guard` +
   `host_help_docs_boot` for P4.D212 — expected UNMOVED at 126, run to
   prove it; `zod_version_guard` + the NEW `provider_sdk_version_guard`
   for P4.106; `chat_informs_swipe_handle` for P4.106) re-run by name and
   their constants recounted with the arithmetic in the comment.
10. **Tier R** (`crates/quilltap-cli/tests/cli_differential.rs`) at BOTH
    pins for P4.D214 (the comparand; 244 → 244 + N; the designed reds
    closed at the target, recorded at the baseline); at the baseline pin
    for P4.D212 (the verb is not a CLI surface — 244/0 expected; run it,
    do not assert it).
11. **Commit per `.claude/commands/commit.md`** (one unit per commit where
    the order tiers them; CHANGELOG entry + version bumps per §R.8; the
    status-log lane record appended with the regen commands AS RUN, the
    mutation table, the pre-fix counts, the measurements, and every
    finding outside the lane's ownership named for the unifier). The lane
    closes by updating its own order's status header with what landed,
    what was deferred, and what it measured that the order got wrong.
