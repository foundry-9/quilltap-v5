# Work order P4.D114 — gallery downloads (`af1bc479`) + the bug-98 projects-create schema (`c93ec7ff`)

**Status:** OPEN

## Preamble

- **v4 baseline for this round: `f6a10055`** (v4 main, HEAD at planning
  2026-08-25, tree CLEAN, checkout on `main`). The round absorbs the FOUR
  commits past `0ba942b1`; this lane owns `af1bc479` (every gallery can
  download the picture on display — MINUS its wardrobe-dialog hunk, which
  is P4.D113's) and `c93ec7ff` (bug 98 — a blank description no longer
  refuses the whole project). `bugfix` at planning: HEAD `3a76b17d`,
  nothing unabsorbed beyond the tests-only `009c49b2`.
- **Drift-check at lane start.** `git log f6a10055..main --oneline` AND
  `git diff main bugfix -- lib/ app/ packages/`. If v4 moved past
  `f6a10055`, **STOP** and report. Pin a lane-unique detached worktree
  (`/tmp/qt-v4-pin-p4d114-f6a10055`, all three symlink classes) whenever
  the checkout dirties or moves. Regens through the sweep driver; never
  two sweeps concurrently.
- **Sibling lanes:** P4.D112 and P4.D113 (the wardrobe-containers pair) —
  **fully disjoint files; this lane meets them nowhere.** §Ownership below
  is the binding split (identical table in all three orders).
- **Worktree:** per the `carryout` skill; `npm ci` in the worktree's
  `apps/web`. `docs/CHANGELOG.md` and `status-log.md` append-only.

## Mandate

Two small v4 commits, absorbed whole. (1) **Downloads:** the My Photos
detail modal gains Download + Copy, the Scriptorium file table's detail
row gains Download, the (unhosted) image-gallery component gains its hover
download, the Generate Image page's hand-rolled anchor converges onto the
shared `core/download-utils.ts`, and the mount-point blob endpoint sends an
**inline `Content-Disposition` with the STORED basename** (not
`originalFileName`, whose extension can mismatch the WebP-transcoded
bytes — v4's why-comment, carry it). (2) **Bug 98:** v4's projects CREATE
gets a real schema (`app/api/v1/projects/schemas.ts`) with
`description`/`instructions`/`color`/`icon` as `.nullable().optional()` —
v5's create today validates ONLY the name, so this lane first MEASURES
v5's per-field behavior, then ports the schema table so create validates
exactly as v4's does; plus the QuickActionsRow success/error toasts.

**Must NOT touch:** anything P4.D112 or P4.D113 owns — in particular
`apps/web/src/app/wardrobe/**` (the wardrobe avatar-preview download hunk
of `af1bc479` is P4.D113's rider, not yours), `api/types.rs`,
`api/engine.rs`, and `wardrobe_routes.rs`. Bug 98 needs no Request-enum
change (the `ProjectCreate` variant exists; validation lives in the
handler) — if you find yourself opening `types.rs`, STOP and report.

## Survey-verified starting points (fresh survey 2026-08-25, at `f6a10055`)

### v4 — read whole: `git show af1bc479` and `git show c93ec7ff`

**`af1bc479` (downloads):**

- `lib/download-utils.ts` **predates the drift** (it existed at
  `0ba942b1`; v5's port `apps/web/src/app/core/download-utils.ts` already
  exists). The commit only routes MORE surfaces through it and adds
  buttons — do not re-port the util.
- `app/photos/PhotosView.tsx`: the detail modal footer becomes a
  three-button row — Download (`fetch(entry.blobUrl)` → blob →
  `triggerDownload(blob, entry.fileName)`, `disabled` while running,
  label `Downloading…`, error toast `'Failed to download photo'`), Copy
  (`copyImageToClipboard(entry.blobUrl)` from `lib/clipboard-utils`,
  toasts `'Image copied to clipboard'` /
  `'Failed to copy image to clipboard'`), Close. Icons `download` /
  `copy`.
- `app/scriptorium/[id]/components/FileTable.tsx`: the detail row gains a
  Download button → `triggerDownload` with error toast.
- `components/images/image-gallery.tsx`: a hover download button on each
  grid tile.
- `app/generate-image/GenerateImageView.tsx`: the hand-rolled anchor is
  replaced by the shared util.
- `app/api/v1/mount-points/[id]/blobs/[...path]/route.ts`: BOTH response
  arms gain `Content-Disposition: buildContentDisposition(name, 'inline')`
  — the blob arm's name is
  `relativePath.split('/').pop() || meta.originalFileName || 'file'`, the
  documents-fallback arm's is `relativePath.split('/').pop() || 'document'`.

**`c93ec7ff` (bug 98):**

- New `app/api/v1/projects/schemas.ts` — `createProjectSchema`:
  `name` `.min(1, 'Name is required').max(100)`;
  `description` `.max(2000).nullable().optional()`;
  `instructions` `.max(10000).nullable().optional()`;
  `allowAnyCharacter` `.optional().prefault(false)`;
  `characterRoster` `z.array(z.uuid()).optional().prefault([])`;
  `color` hex-regex `.nullable().optional()`; `icon`
  `.max(50).nullable().optional()`. The header comment (carry it): the
  create dialogs send `description || null` for a blank field, so `null`
  must validate; the handler already coerces falsy to null. The OLD
  schema (inline in `route.ts`) had these four as plain `.optional()` —
  `null` failed, which is the bug.
- `components/homepage/QuickActionsRow.tsx`: on create success →
  `showSuccessToast('Project created successfully!')`, on failure →
  `showErrorToast('Failed to create project')` (both arms).

### v5 — the counterparts

- **Blob route:** `crates/quilltap-web/src/files_routes.rs` —
  `mount_blob_get` :416 (blob branch via `DocMountBlobsRepository`, then a
  documents-table fallback with `mime_for_document`). Headers today:
  `content-type`, `content-length`, `cache-control`, `x-blob-sha256`
  (:463-470) — **no Content-Disposition**. Helper exists:
  `crates/quilltap-core/src/content_disposition.rs` (already used by
  `mount_file_get` :279 / `files_get` :174). Route test venue:
  `crates/quilltap-web/tests/binary_routes.rs`.
- **Projects create:** `crates/quilltap-core/src/api/projects.rs` —
  `project_create` :156 is hand-rolled: the ONLY guard is
  `name.trim().is_empty()` → `bad_request("Name is required")`;
  `description`/`instructions` pass through `or_null` (:79-84, `""` →
  `None`). The UPDATE side has the P4.55 schema table
  (`PROJECT_UPDATE_SCHEMA` :327-354, `FieldRule`/`field_value_ok`, 400
  `"Validation error"`). **So v5's create likely never 400'd on `null`
  description (bug 98's exact shape may be absent) but also never
  enforced the max lengths / hex color / roster-UUID rules v4 now
  enforces — measure each field first** (the
  `convergence-lane-measure-dont-assume` rule). Differential family:
  `crates/quilltap-harness/tests/projects_routes_equivalence.rs` + its
  oracle case (drive v4's REAL new route + schema at the pin).
- **SPA:**
  - My Photos: `apps/web/src/app/screens/photos/photos-page.ts` — the
    detail modal is INLINE in the page (`@if (selected(); as entry)`
    ~line 205); today it has only a `×` close. Pattern donors for
    Copy: `apps/web/src/app/images/image-detail-modal.ts` has
    `handleCopyToClipboard()`; check whether a shared clipboard util
    exists — if v4's `lib/clipboard-utils` has no v5 twin, create
    `apps/web/src/app/core/clipboard-utils.ts` (transcribe v4's).
  - Scriptorium: `apps/web/src/app/screens/scriptorium/file-detail-row.ts`
    (`FileDetailRow`, exports `isBlobBacked`).
  - Generate Image:
    `apps/web/src/app/screens/generate-image/generate-image-page.ts` —
    `onDownload` :431-445 hand-rolls the anchor; converge onto
    `triggerBlobDownload` exactly as v4 converged onto `triggerDownload`.
  - Image gallery: `apps/web/src/app/images/image-gallery.ts` — ported
    component, **currently has NO v5 host** (its own header says so).
    Port the hover-download change to keep the mirror in sync; note the
    no-host status in the lane record (do NOT invent a host).
  - Quick actions: `apps/web/src/app/screens/home/quick-actions-row.ts`
    (+ `quick-actions-row.spec.ts`) — New Project reuses the Prospero
    create dialog; add v4's two toast arms at the equivalent success/
    failure points.
  - Shared util (consume, don't edit):
    `apps/web/src/app/core/download-utils.ts` — `triggerBlobDownload` /
    `triggerUrlDownload`. The Tauri-native save arm is a recorded
    non-goal (the util's own header); v4's Electron IPC arm has no v5
    counterpart and gets none this round.
- **e2e:** `apps/web/e2e/photos-flow.spec.ts` (127 — modal open/Escape),
  `scriptorium-flow.spec.ts` (268 — FileTable expand walk),
  `generate-image-flow.spec.ts` (173), `home-flow.spec.ts`.

## Tiered deliverables

**Tier 1 (must land):**

1. **The blob route's Content-Disposition** — both arms, v4's exact name
   fallback chains, `inline`, via the existing helper. Pin in
   `binary_routes.rs`: assert the header bytes for (a) a nested path
   (stored basename wins), (b) a name needing RFC 5987 escaping (match
   v4's `buildContentDisposition` output byte-for-byte — read v4's
   `lib/api/content-disposition.ts` and diff against v5's
   `content_disposition.rs` before assuming they agree), (c) the
   documents-fallback arm's `'document'` default.
2. **Bug 98** — the per-field measurement table (current v5 vs old v4 vs
   new v4 for: `null` description, `""` description, >2000 chars, bad
   hex color, `null` color, non-UUID roster entry, >100-char name), then
   the create schema table in `api/projects.rs` mirroring
   `PROJECT_UPDATE_SCHEMA`'s idiom so create validates exactly as v4's
   `createProjectSchema` — remembering Zod counts UTF-16 units
   (`zod-string-check-order-and-length`) and v4's refusal envelope
   (`v4-refusal-envelope-parse-vs-safeparse` — measure which sentence the
   route actually answers). Extend `projects_routes_equivalence` with
   arms for every row of the measurement table, red-first where v5
   diverges today.
3. **My Photos Download + Copy** — v4's labels, busy state, icons, and
   all four toast sentences byte-for-byte; the clipboard util transcribed
   if absent.
4. **FileTable detail-row Download** and **Generate Image convergence**
   onto the shared util.
5. **QuickActionsRow toasts** — both arms, v4's sentences.

**Tier 2 (should land):**

6. The image-gallery hover download (unhosted component — spec-only
   proof).
7. e2e: photos modal shows Download/Copy and a click yields a Playwright
   `download` event with `entry.fileName`; the FileTable detail-row
   button present; generate-image download still works post-convergence
   (the existing beat may already cover it — extend, don't duplicate).

**Tier 3 (explicit deferrals — loud):**

- No Tauri-native save dialog (the standing recorded non-goal; v4's
  Electron `will-download` arm has no counterpart).
- `help/mount-points.md` / `help/photo-gallery.md` /
  `help/profile-avatar.md` deltas → the `p4.9i2` bank.
- The other hand-rolled anchor sites v5 accumulated (image-modal,
  chat-gallery-image-view-modal, image-detail-modal, file-preview-modal,
  characters.api ×2) stay as they are — v4 did NOT converge its
  counterparts of those; converging them would be v5-invented drift.
  Record the census in the lane record for a future v4 convergence.

## The differential requirement, restated

The blob-route header and the create schema are server behavior: prove
them against v4's REAL code at the pin (the routes-family jest oracle for
projects — closest recipe: the family's own committed header + the
`dbfree-route-guard-oracle` memory for pure-validation arms; the header
bytes via a direct comparison against v4's `buildContentDisposition`
outputs). The SPA halves prove via wire/DOM specs mirroring v4's client
code + the e2e beats. Mutation-prove the load-bearing pins (drop the
Content-Disposition → `binary_routes` reds; loosen the description rule →
family reds).

## Fixtures

- Consumes: the `projects_routes_equivalence` fixture (extend through its
  committed builder/recipe; regenerate any dependent oracle NDJSONs
  yourself and re-run the consumers by name).
- The blob-route pins run against `binary_routes.rs`'s own in-test
  fixtures — extend in place.

## Shared contract

This lane has **no cross-lane contract**: it meets P4.D112/P4.D113
nowhere. (The §Ownership table is the only shared text.)

## Ownership (identical table in all three orders)

| Lane | Owns (exclusive) |
|---|---|
| P4.D112 | `crates/quilltap-core/src/{api/types.rs, api/engine.rs, api/groups.rs, api/wardrobe.rs, services/wardrobe_transfers.rs, vault_overlay.rs, db/vault_wardrobe_write.rs, db/vault_wardrobe_public.rs, db/archetype_wardrobe.rs, db/vault_read_overlay.rs}`; `crates/quilltap-web/src/wardrobe_routes.rs` + its tests; harness wardrobe/vault/groups families + their oracle cases + fixture builders. **Bumps `quilltap-core` + `quilltap-harness` versions.** |
| P4.D113 | `apps/web/src/app/wardrobe/**` (the whole folder, incl. `wardrobe.api.ts` and the new `wardrobe-container.ts`), `apps/web/e2e/wardrobe-flow.spec.ts`. **Bumps the SPA version.** |
| P4.D114 | `crates/quilltap-web/src/files_routes.rs` + `crates/quilltap-web/tests/binary_routes.rs`; `crates/quilltap-core/src/api/projects.rs`; `crates/quilltap-harness/tests/projects_routes_equivalence.rs` + its oracle case/fixture; SPA: `apps/web/src/app/screens/photos/**`, `apps/web/src/app/screens/scriptorium/file-detail-row.ts`, `apps/web/src/app/screens/generate-image/**`, `apps/web/src/app/images/image-gallery.ts`, `apps/web/src/app/screens/home/quick-actions-row.ts` (+ their specs), `apps/web/e2e/{photos-flow,scriptorium-flow,generate-image-flow,home-flow}.spec.ts`, a new clipboard util under `apps/web/src/app/core/` if needed. **Bumps `quilltap-web` version ONLY** — its core/harness/SPA edits ride un-bumped; the unifier recounts. |
| Everyone | `docs/CHANGELOG.md`, `docs/developer/porting/status-log.md` — append-only. |

## Verification gate (before declaring done)

- `cargo fmt --check`; `cargo clippy` both feature sets; full
  `cargo test --workspace` (full-log + sentinel — never `| tail`, never a
  pgrep loop).
- `projects_routes_equivalence` + `binary_routes` by name over oracles
  regenerated FRESH at `f6a10055` through the sweep driver, zero SKIP;
  grep the NDJSON for the new validation arms.
- SPA: `npm test` + `npm run build` clean; Playwright — the three touched
  specs green plus the full suite.
- Mutation proofs as listed in §Differential.
- Per-commit rule: `.claude/commands/commit.md`.
