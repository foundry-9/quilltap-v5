# Work order P4.D154 — re-hydrate user attachments so every character sees them (v4 bug 121, `e288ae2ec`): the USER-side attachment walk as a fourth `buildMessageContext` leaf, the re-hydration step before `build_context`, and the seams it needs

**Status:** CLOSED (ordered 2026-09-05, the `d883a5ee1` drift catch-up round:
P4.D153 ∥ P4.D154 ∥ P4.D155 ∥ P4.D156 ∥ P4.D157 ∥ P4.D158; carried out
2026-09-05 on branch `claude/user-attachment-rehydration-bug-9a5151`).

**Every Tier-1 deliverable (1–6) landed.** Tier 2 item 7 (a
budget-discriminating tier-3 row) is deliberately NOT landed under the
order's own escape clause — the ceiling is 80,000 characters, so a
discriminating row would push ~160 KB of generated text through the canned
stream key on both sides and would very likely trip `build_context`'s
compression/trim path, changing the row's shape for reasons unrelated to the
budget. The unit pin of item 4 stands in its place
(`the_budget_skips_a_file_whole_and_the_next_smaller_one_still_lands`, which
drives the real wrapper over a 79,996-character file plus an overflowing one
plus a fitting one, and whose truncate-instead-of-skip mutation reddens it).
Tier 3 is empty as expected. **The lane record — including the answer to the
order's open question about whether the corpus can see the
before-`build_context` ordering (it CAN, through the `llm_logs` row of the
memory-keyword distill `build_context` itself fires) — is in
`status-log.md`.**

## Preamble

- **v4 oracle baseline: `0b0617fee`** (drift-ledger §1). **This lane's target
  commit is `e288ae2ec`** — "fix(chat): re-hydrate user attachments so every
  character sees them (bug 121)" (2026-09-04, v4 `4.9.0-dev.134`). NOT a
  convergence: v4's own filing from a live Friday scene in which one
  character quoted an attached 29 KB transcript and the next said she could
  not read it. Read the hunks, not the message: `git -C
  ~/source/quilltap-server show e288ae2ec -- lib/`, and the 199-line
  `docs/developer/bugs/fixed/bug-121-text-attachment-first-responder-only.md`
  (its `v5 status` is **Applies**; it carries the 13-call table).
- **Freshness probe at lane start** (drift-ledger §2): v4 on `main`, tree
  clean, `git log d883a5ee1..main` empty, `git log 3a76b17df..bugfix` empty.
  **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED** (§B). Every regen from
  `/tmp/qt-v4-pin-p4d154-d883a5ee1` through the sweep driver. ⚠ The pin also
  carries `d883a5ee1` (bug 122) — it edits `context-manager.ts`, not
  `context-builder.service.ts`, and no oracle in this lane's §A imports the
  memory injector; verify by grepping the fresh `message-context-leaves`
  NDJSON for nothing this lane did not add.
- **Sibling lanes:** P4.D153 (bug 122 — owns `build_context.rs`; this lane
  does NOT edit it: the re-hydration runs INSIDE `message_context.rs` before
  the `build_context` call, exactly where v4 puts it), P4.D155 (owns
  `cheap_llm*.rs`, `api/documents.rs`), P4.D156, P4.D157 (owns
  `message_formatter.rs` — not this lane's), P4.D158. §A–§H + Ownership
  binding and identical across all six orders.
- **Worktree:** per the `carryout` skill (branch `claude/p4-d154-…`).
  `CHANGELOG`/`status-log.md` append-only. **No Playwright** (§E).

## Mandate

Port `e288ae2ec` whole: (a) the extracted `is_characters_own_prior_response`
stop rule (verified neutral in v4 — verify it again in v5 by the Lantern
leaf's existing 12 cases staying byte-identical); (b) the pure, exported
`collect_unseen_user_attachments_for_character` walk (the USER-side
counterpart of the Lantern walk); (c) the private `rehydrate_user_attachments`
step — per unseen row, load + fallback-process the files for THIS provider,
keep raw bytes only for a SUCCESSFUL `unsupported` fallback, accumulate
`format_fallback_as_message_prefix` text oldest-first against the 80,000-char
per-turn ceiling that SKIPS a file whole rather than truncating it, the three
log lines; (d) the `build_message_context` rewiring — the shared
`attachment_history_cutoff` hoisted out of the Lantern block, the
re-hydration BEFORE `build_context` so the spliced text is budgeted /
compressed / trimmed like any message body, the prefix spliced per carrying
message id into a `messages_for_conversation` COPY fed to
`build_conversation_messages` (`filtered` untouched and unchanged by
reference when nothing was re-hydrated), and `merged_attachments` seeded from
`attachments_to_send ++ rehydrated_attachments_to_keep` with the Lantern merge
chaining onto it. **Nothing is persisted** — v4 says so deliberately;
existing chats are repaired and the behaviour survives regenerate, swipe,
import and restore. v5 today has the pre-fix shape exactly and reproduces bug
121 whole, on every chat from the second turn onward and on every character
after the first in a multi-character turn. Must NOT touch:
`services/build_context.rs`, `file_fallback.rs` (its two functions are
consumed, not changed), `orchestrator.rs`, `api/**`, any SPA file.

## Survey-verified starting points (2026-09-05, from the shipped hunks — ledger §5.3)

1. **v4 post-fix symbols** (`lib/services/chat-message/context-builder.service.ts`):
   `collectLanternImageFileIdsForCharacter` `:249` (unchanged);
   `isCharactersOwnPriorResponse(msg, characterParticipantId, isMultiCharacter)`
   `:300` — multi → `participantId === characterParticipantId`; single →
   `!(Array.isArray(attachments) && attachments.length > 0)`, byte-for-byte
   the old inline pair; `collectUnseenUserAttachmentsForCharacter(
   existingMessages, characterParticipantId, isMultiCharacter, historyCutoff,
   lookback)` `:336` — reverse tail-walk; `type !== 'message'` skipped
   WITHOUT cost; an ASSISTANT row either BREAKS (own prior response) or
   counts against `scanned` and continues; non-USER rows skipped; a USER row
   counts, then needs a non-empty `attachments` array AND an `id` (no row id
   ⇒ nowhere to splice); `historyCutoff && createdAt < historyCutoff` skips
   (the joining-participant guard, symmetric with the Lantern walk);
   string-typed ids deduped through a `seen` set; returns `{messageId,
   fileIds}[]` REVERSED into chronological order. Constants
   `USER_ATTACHMENT_LOOKBACK = 20` (`:383`, distinct from the Lantern walk's
   `ASSISTANT_IMAGE_LOOKBACK = 6` at `:1124`) and
   `REHYDRATED_ATTACHMENT_CHAR_BUDGET = 80_000` (`:397`).
   `rehydrateUserAttachments` `:411-495`: returns nothing when there is no
   `characterParticipantId`; ONE try/catch around everything —
   `logger.warn('Failed to re-hydrate user attachments from history', {error,
   characterParticipantId})` and the turn proceeds exactly as before; per
   unseen row `loadChatFilesForLLM(fileIds, {provider})` then
   `processFileAttachmentFallback` per file, mirroring `loadAndProcessFiles`'s
   filter: `type === 'unsupported'` keeps the raw bytes only when
   `!fallbackResult.error` (a failed fallback DROPS the file rather than
   tripping the provider's no-image refusal); otherwise the prefix text is
   accumulated oldest-first against the budget, a file that would overflow is
   SKIPPED WHOLE (`skippedForBudget++`), one
   `logger.warn('Re-hydrated attachments exceeded the per-turn budget; some
   were not re-sent', {skippedForBudget, budget, characterParticipantId})`,
   and `logger.debug('Re-hydrated user attachments from history',
   {messagesExpanded, rawAttachmentsKept, charactersUsed,
   characterParticipantId})`. In `buildMessageContext` (`:819`):
   `attachmentHistoryCutoff` `:966` (`isMultiCharacter && characterParticipant
   && !hasHistoryAccess && !hasPriorResponse` → the participant's join time),
   the re-hydration call `:991`, `messagesForConversation` `:997` (a copy
   only when `rehydratedContentByMessageId.size > 0`), fed at `:1006`;
   `mergedAttachmentsToSend = [...attachmentsToSend,
   ...rehydratedAttachmentsToKeep]` `:1125`, the Lantern merge chaining
   `:1169`, the anchor logic `:1186-1203` unchanged.
2. **v5 pre-fix shape** (`crates/quilltap-core/src/services/message_context.rs`):
   `collect_lantern_image_file_ids_for_character` `:408` is the only
   attachment walk; section K `:947-978` computes `has_prior_response`
   (`:950`) / `history_cutoff` (`:955`) INLINE and seeds `merged_attachments`
   from `attachments_to_send` alone (`:947`, re-seeded `:972`); `:871` feeds
   `build_conversation_messages(&filtered, is_multi)` with no re-hydration
   step; the anchor logic `:1001-1013`, `:1058-1074`. No `collect_unseen_*`
   or `rehydrat*` anywhere in `crates/`. The seam:
   `MessageContextSeams::load_lantern_images(&self, file_ids, provider) ->
   LanternLoad { prefix, attachments }` (`:482-491`; `NoopMessageContextSeams`
   `:494`; the real impl `chat_files.rs:526 RealMessageContextSeams`, whose
   loader is `chat_files.rs:469 load_lantern_images`). **The USER-side loader
   is a sibling method on that trait (§D names it `load_user_attachments`)
   — a direct call from the wrapper would make the wrapper untestable**, and
   the tier-3 corpus drives the wrapper through `NoopMessageContextSeams`.
   The parts already exist: `chat_files.rs:275 load_chat_files_for_llm`,
   `chat_files.rs:386 load_and_process_files` (the filter to mirror),
   `file_fallback.rs:1468 process_file_attachment_fallback`,
   `file_fallback.rs:438 format_fallback_as_message_prefix`.
3. **Per-row vs per-file:** v4 loads ONE `loadChatFilesForLLM` call per
   carrying message (its `fileIds`) and formats per file; the budget check is
   per FILE (skip whole), the splice is per MESSAGE (all that row's kept
   prefixes joined in file order). Read the hunk for the join separator and
   for whether the prefix goes BEFORE the user text — carry both exactly.
4. **The differential shape.** The new walk is pure and exported → a FOURTH
   leaf for `message_context_leaves_equivalence` (12 cases today, tier-1 over
   v4's REAL exports; oracle `harness/oracle/cases/message-context-leaves.ts`,
   `QT_ORACLE_MESSAGE_CONTEXT_LEAVES`). v4 shipped TEN cases for it in
   `context-builder.service.test.ts` (+113 lines) — port them as corpus rows:
   empty; the reported second-character shape; no re-delivery to the
   character who already answered; chronological multi-collect; dedupe of a
   re-attached id; the history cutoff; the missing row id; the
   single-character `participantId`-absent stop; the lookback cap; non-message
   rows. The re-hydration itself is tier-3 over `orchestrator_tier3_equivalence`
   (`QT_ORACLE_ORCHESTRATOR`, `QT_FIXTURE_ORCH_*`) — ⚠ **that corpus keeps
   message attachments EMPTY today** (`orchestrator-tier3.test.ts:401-402`
   says so in a comment) and its fixture builder creates NO `files` table
   (grep count 0), so the re-hydration is corpus-BLIND until the builder
   plants a `files` row + a USER message carrying it + a second character
   turn. The two arms a corpus must actually DISCRIMINATE: the budget skip
   (a file larger than the remaining budget is skipped WHOLE, the next
   smaller one still lands) and the `unsupported`-with-error drop. The
   `messages_for_conversation` splice is what the tier-3 diff sees. The
   fallback machinery's own family is `file_attachment_tier3_equivalence`
   (`QT_ORACLE_FILE_ATTACHMENT`, `QT_FIXTURE_FILE_ATTACH_*`) — regenerate at
   the pin; expected byte-identical (v4 did not touch the fallback).
5. **The hoist is a behaviour edge:** v4 computes `attachmentHistoryCutoff`
   ONCE and passes it to BOTH walks; v5's inline `history_cutoff` today is
   computed inside the Lantern block only when `is_multi && !has_history_access
   && !has_prior_response`. Hoist it to the same shape and confirm the Lantern
   walk's 12 leaf cases and the orchestrator corpus stay byte-identical for
   every pre-existing row (the neutrality half of this port).

## Tiered deliverables

**Tier 1 (must land):**
1. `is_characters_own_prior_response` extracted; the Lantern leaf's 12 rows
   byte-identical (the extraction's neutrality proof).
2. `collect_unseen_user_attachments_for_character` (pure, `pub`), the two
   constants, and the TEN-case fourth leaf in the oracle + the Rust runner
   (red-first: the runner must FAIL on a missing `unseen` kind before the port).
3. `MessageContextSeams::load_user_attachments` + `UserAttachmentLoad` (§D),
   the `Noop` default, and the `RealMessageContextSeams` impl in
   `chat_files.rs` mirroring `load_and_process_files`' filter (point 1).
4. `rehydrate_user_attachments` + the `build_message_context` rewiring
   (point 1's (d) + point 5), the three log lines capture-pinned
   (thread-scoped seam; memory notes `a-process-global-test-seam-must-be-
   thread-scoped`, `capture-layer-target-assert-is-a-prefix-match`), the
   budget arithmetic + the skip-whole rule + the `unsupported`-with-error
   drop each under a unit pin driving the wrapper through a canned seam.
5. The orchestrator tier-3 builder widened (a `files` row, a USER message
   carrying it, a second character's turn) so the corpus can SEE the splice;
   `orchestrator_tier3_equivalence` regenerated at the pin with the changed
   bytes grepped (the fallback prefix text appearing in the second
   character's request), every pre-existing row byte-identical.
6. `file_attachment_tier3_equivalence` regenerated at the pin — expected
   byte-identical; record it.

**Tier 2 (should land):**
7. A budget-discriminating tier-3 row (two files where the second overflows
   the 80,000-char ceiling) if the builder can carry it cheaply; otherwise
   the unit pin of item 4 stands and the record says so.

**Tier 3 (explicit deferrals, loud):** none expected. v4's 113 test lines
are NO-PORT as a file (their behaviour is items 2 and 4). The help hunk
(`help/file-uploads.md`) banks via §G (P4.D158).

## The differential requirement (restated)

`message_context_leaves_equivalence` regenerated from the `d883a5ee1` pin
and run by name, zero SKIP, 12 → 22 rows counted; `orchestrator_tier3_
equivalence` at the pin with the new row(s) and a pre/post-port NDJSON diff
recorded (non-empty on the planted rows, empty elsewhere). Mutation-proven:
drop the `id`-required guard → the missing-row-id leaf row reddens; walk
ASSISTANT rows without breaking → the no-re-delivery row reddens; truncate
instead of skip-whole → the budget pin reddens; keep an errored
`unsupported` result → the drop pin reddens; splice into `filtered` instead
of a copy → the unchanged-by-reference pin reddens; move the re-hydration
AFTER `build_context` → the orchestrator planted row's context accounting
reddens (measure that it does; if the corpus cannot see it, say so and pin
it at unit tier).

## Fixtures

- Consumes: the `orchestrator-tier3` and `file-attachment` fixture pairs
  (§A) — widens the orchestrator BUILDER (a `files` row needs the `files`
  DDL the builder lacks today; take it from the `system-data` builder's
  schema, not by hand).
- Delivers: the widened oracle case + the widened builder; no new committed
  `.db` unless the widening cannot be a builder change (record why).

## Shared contracts (BINDING — identical text in all six orders)

**§A Family ownership for this round's regens.** Each family below is
regenerated and edited by ONE lane; P4.D158's bulk neutrality sweep
EXCLUDES every family in this section by name and hands any red in them to
the owning lane.

- **P4.D153:** `memory_injector_equivalence` (+ `harness/oracle/cases/
  memory-injector.ts`), `build_context_tier3_equivalence` (+ its builder
  `build-context-tier3.test.ts` and the `QT_FIXTURE_BC_*` pair),
  `carina_query_tier3_equivalence` (+ its fixture builder),
  `announcer_tier3_equivalence`.
- **P4.D154:** `message_context_leaves_equivalence` (+ `harness/oracle/
  cases/message-context-leaves.ts`), `orchestrator_tier3_equivalence` (+
  `orchestrator-tier3.test.ts` and the `QT_FIXTURE_ORCH_*` pair),
  `file_attachment_tier3_equivalence` (+ its fixture pair).
- **P4.D155:** `documents_routes_equivalence` (+ `documents-routes.test.ts`),
  `system_export_equivalence`, `answer_confirmation_tier3_equivalence`,
  `cheap_llm_fallback_equivalence`, `brahma_console_tier3_equivalence`,
  `brahma_console_routes_equivalence`, `brahma_orchestrator_tier3_equivalence`,
  every `pascal_*_equivalence` + `p4_6ay_workbench_wire_contract`, the
  self-inventory families, and whichever family the lane identifies as
  driving `cheap_llm::get_cheap_llm_provider` (named in its record).
- **P4.D156:** the CLI Tier R (`crates/quilltap-cli/tests/cli_differential.rs`)
  + `completion_behavior.rs`; the SPA unit specs under its paths.
- **P4.D157:** the seven families the dead-code sweep takes dark:
  `cheap_model_equivalence`, `model_selection_equivalence`,
  `llm_errors_equivalence`, `message_formatter_equivalence`,
  `post_office_host_equivalence`, `chat_timestamp_equivalence`,
  `token_estimation_equivalence` (+ their seven oracle cases).
- **P4.D158:** `request_builder_equivalence` + the committed request-envelopes
  corpus and its recorder, `request_builder_google_wire_equivalence`,
  `request_builder_google_equivalence`, `response_parse_equivalence`,
  `tool_wire_call_site`, the stream fixtures, `openrouter_sdk_pricing_equivalence`,
  the provider manifests generator + `provider_manifest/manifests/*.json`;
  the restore families for the `2edd823c0` arms (`system_restore_equivalence`,
  `system_restore_state`, `restore_vintage_state`,
  `system_restore_guards_equivalence`, the `build-restore-archive*` builders
  and any NEW committed archive); and the bulk neutrality sweep over every
  family NOT listed above.

**§B Pins.** The round's target baseline is **`d883a5ee1`** (v4 `main` HEAD
at ordering, 2026-09-05). Every lane regenerates from a LANE-UNIQUE detached
worktree pinned at `d883a5ee1` (`/tmp/qt-v4-pin-<order>-d883a5ee1`, the
three symlink classes, ledger §5.1) — with ONE exception: P4.D157's
frozen-family evidence regenerates at **`0b0617fee`** (the last sha at which
those seven cases link), from its own second pin. **PIN REQUIRED stands for
every lane until `/unify` moves the baseline** — the checkout's HEAD is the
target, but the checkout can go dirty mid-lane (§5.1), and a pin is the only
tree whose contents are known. Verify each pin by grepping a fresh NDJSON for
a marker only one tree has (e.g. `About another character` appears in a
`d883a5ee1` memory-injector oracle only once P4.D153's targeted cases exist,
and never in a `0b0617fee` one).

**§C Sweep serialization.** `recipe_sweep.py --run-all` is single-flight
repo-wide and belongs to P4.D158 alone; it runs AFTER P4.D153–P4.D157 have
closed their branches (execution arrangement in the round report). Lanes
153–157 run per-family `--run` regens only, and never against another
lane's §A families or their `/tmp` staging.

**§D Source-tree seams (who edits what shared file).**

- `crates/quilltap-core/src/services/mod.rs`: **only P4.D153** edits it (one
  line: `pub mod memory_subject;`). `crates/quilltap-core/src/lib.rs`:
  **nobody**. P4.D157 deletes FUNCTIONS and their tests inside files it owns
  but never removes a module declaration or a `pub use` in `lib.rs`; a module
  it empties is recorded for the unifier, not deleted.
- `services/message_context.rs` + `services/chat_files.rs` (the
  `MessageContextSeams` trait, its `Noop` and `Real` impls): **only
  P4.D154**. The trait gains ONE sibling method beside `load_lantern_images`
  for the USER-side loader (name it `load_user_attachments`; signature shape
  `(&self, file_ids: &[String], provider: &str) -> impl Future<Output =
  UserAttachmentLoad> + Send`, default no-op, `UserAttachmentLoad` a struct
  the lane defines beside `LanternLoad`).
- `db/characters_read.rs`: **only P4.D153** (`find_names_by_ids`, RAW path).
- `cheap_llm.rs`, `services/cheap_llm_exec.rs`: **only P4.D155**.
- `model/request_builder/anthropic.rs`: **only P4.D158** (one regex entry +
  the three comment corrections).
- SPA: `apps/web/src/app/pascal/**` → **P4.D155**; `apps/web/src/app/screens/**`
  and `apps/web/src/styles/**` → **P4.D156**. Nobody else edits `apps/web`.
- `crates/quilltap-cli/**` → **P4.D156** only.

**§E Playwright.** Port 4319 is P4.D156's for the round (it may run the
existing suite once at close; it authors no new beat unless its record says
why). Every other lane authors NO Playwright and runs none; the unifier runs
the full suite at the gate.

**§F The drift ledger.** Lanes never write `drift-ledger.md`. The fourteen
§3 rows were marked `ORDERED(p4.d15x)` at ordering; `/unify` marks them
ABSORBED / NO-PORT-RATIFIED when the baseline moves to `d883a5ee1`. The
fifteenth row (`15573c3a1`, bug 119) stays UNPROCESSED by the ledger's own
instruction — it belongs to the unported `p4.9k` surface.

**§G The `p4.9i2` help bank.** The help-file hunks in this drift
(`e288ae2ec` → `help/file-uploads.md` "A word on company"; `d883a5ee1` →
`help/memory-recall-relevance.md` "Whose Life Is It, Anyway?"; `e9a9c538e` →
four `help/` files incl. `homepage.md`'s In-Chat Navigation section and three
corrected `url:` values) are banked ONCE, by P4.D158, in its lane record under
a `p4.9i2 bank` heading; the unifier carries them into `phase-4.md`'s
candidate row. No lane ports help content.

**§H Versions.** Each lane bumps only the crates / the SPA it touches, per
`.claude/commands/commit.md` §6; the unifier recounts as base + total bumps
(the silent auto-merge trap). Base at ordering: core 0.0.776, harness 0.0.671,
web 0.0.114, host 0.0.96, cli 0.0.17, tauri 0.0.7, SPA 0.5.642.

## Ownership (BINDING — identical table in all six orders)

| Path | Owner |
|---|---|
| `crates/quilltap-core/src/memory_injector.rs`, NEW `services/memory_subject.rs` (+ the one `services/mod.rs` line), `db/characters_read.rs`, `services/build_context.rs`, `services/carina_query.rs`, `services/announcer/character_voiced.rs`; §A families for P4.D153 | P4.D153 |
| `crates/quilltap-core/src/services/message_context.rs`, `services/chat_files.rs`; §A families for P4.D154 | P4.D154 |
| `crates/quilltap-core/src/cheap_llm.rs`, `services/cheap_llm_exec.rs`, `api/documents.rs`, `services/brahma_console/**`, `services/qtap_export/**`, `tools/self_inventory.rs`, `crates/quilltap-core/src/pascal/**`, `apps/web/src/app/pascal/**`; §A families for P4.D155 | P4.D155 |
| `apps/web/src/app/screens/**`, `apps/web/src/styles/**`, `crates/quilltap-cli/**`; the CLI Tier R + `completion_behavior.rs` | P4.D156 |
| `crates/quilltap-core/src/cheap_model.rs`, `pricing.rs`, `services/llm_errors.rs`, `message_formatter.rs`, `services/host_notifications.rs`, `chat_timestamp.rs`, `token_estimation.rs` (function-level deletions/pins only); the seven §A families + their oracle cases | P4.D157 |
| `crates/quilltap-core/src/model/request_builder/anthropic.rs` (one regex line + comments); `harness/oracle/providers/**` + `harness/oracle/fixtures/request-envelopes/**`, `response-bodies/**`, the stream fixtures; `crates/quilltap-core/src/provider_manifest/manifests/*.json`; the restore-archive builders + any NEW committed archive + §A restore families; `docs/v4/**`; `harness/tools/sweep-results/<new artifact>` | P4.D158 |
| `docs/CHANGELOG.md`, `docs/developer/porting/status-log.md` | append-only, all lanes |
| `docs/developer/porting/drift-ledger.md`, `crates/quilltap-core/src/lib.rs` | NOBODY |

Every other path is unowned this round: a lane that believes it must touch
one STOPS and reports (the `carryout` rule 3), naming the file and the
sibling it suspects.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D
   warnings` in BOTH feature sets; `cargo build --workspace`.
2. `cargo test --workspace` (full log + sentinel, never `| tail`) with the
   lane's env block: `QT_ORACLE_MESSAGE_CONTEXT_LEAVES`,
   `QT_ORACLE_ORCHESTRATOR` + `QT_FIXTURE_ORCH_*`, `QT_ORACLE_FILE_ATTACHMENT`
   + `QT_FIXTURE_FILE_ATTACH_*` — all three families confirmed RUN, zero
   `SKIP:`.
3. The three §A families by name through `recipe_sweep.py --v4 <pin> --run
   <family>`, oracles fresh at `d883a5ee1`, changed bytes grepped.
4. The mutation proofs above, each named in the lane record with the arm it
   reddened.
5. Commits per `.claude/commands/commit.md` (core + harness bumps); the lane
   record appended to `status-log.md`; this order's status header updated.
