# Work order P4.91 — the `is_photos_relative_path` consolidation done RIGHT (P4.D192's refuted Tier 2 item 7): the 13-row `photos_relative_path_equivalence` corpus grown with the trailing-slash-run shapes FIRST, the family repointed onto `photos::photos_paths` so the growth goes RED (the twin it drives today is already Node-faithful and would go green measuring nothing), `photos_paths`' `posix_dirname` made Node-faithful, THEN the `db/doc_mount_file_links.rs` twin deleted and its SIX importers repointed (the collapse heal is the sixth — the recorded five is stale) — plus the heal's `report_census` `fileIds` rendered as the ARRAY v4 logs, via a `log_file.rs` visitor convention — from `main`

**Status: CLOSED WHOLE by the lane (2026-09-16), awaiting unification.**
Ordered 2026-09-16, the `1fefadb9a` bug-147 drift catch-up + maintenance round
(P4.D195 ∥ P4.91 ∥ P4.92 ∥ P4.93). Branch
`claude/p4-91-photos-predicate-consolidation-ed72a6`, three commits. Every Tier
1 and Tier 2 item landed; the Tier 3 deferrals stand as ordered. Lane record:
`status-log.md` → "Lane record — P4.91".

**Landed:** the family repointed onto `photos::photos_paths` FIRST (GREEN over
13 — the baseline measurement), the corpus grown 13 → **30** with the slash
shapes and RED on **exactly 3** (`photos//`, `photos///`, `PHOTOS//`), Node's
loop MOVED into `photos_paths`, GREEN over 30; the twin + its private
`posix_dirname` deleted and its **SIX** importers repointed with the heal's
justification comment rewritten; the two near-duplicate unit suites
consolidated into one; the writer census recorded (no writer can mint even a
single trailing slash); `report_census` logging the ARRAY through a named
`log_file.rs` `…Json` visitor convention with a census capture pin that did not
previously exist; the ORDER half recorded and escalated; the stale claims
corrected by new record. Eight mutation proofs, each reddening exactly its
target.

**Refuted or corrected by measurement (four order premises):**
- **`photos/sub//` AGREES** — the order asked; it is not one of the red rows.
- **M2's target was wrong.** Skipping no trailing slash does NOT redden the
  control `photos/a.webp/` at the predicate level (the dirname still satisfies
  `startsWith("photos/")`); it reddens `photos/` — and it DOES redden the
  control in the new `posix_dirname` table, which is where that contract lives.
- **No census capture test existed.** The order says "the existing census
  capture test"; nothing pinned that warn at all. Three were written.
- **"`valuable` is not a dependency anywhere" is wrong** — it is in
  `Cargo.lock` as `tracing-core`'s OPTIONAL dep. The true, load-bearing claim
  is that the FEATURE is never selected, which is what shipped.

**Found on the way, beyond the order:** the algorithm being moved was itself
un-Node-faithful on one arm — `has_root && end == 1` answered `"/"` where
Node's `dirname("//photos")` is `"//"`. The predicate is blind to it, so the
corpus can never see it; it is fixed and pinned by the new
`posix_dirname_matches_node` table (mutation M4 reddens that table and leaves
the differential green).

**OPEN for the unifier, NOT this lane's:** `chat_gallery_equivalence` and
`images_routes_equivalence` are RED on a pin-fresh oracle — their committed
fixtures predate P4.D182's `files.generationKey` (2026-09-14), so v5's fixed
column list answers `no such column` on every write arm. Proven not-mine by a
revert-probe that reproduces both failures byte-identically with the pre-lane
helper restored. Neither fixture is in any lane's Owns column this round. Full
evidence in the lane record.

## Preamble

- **v4 oracle baseline: `2075242f9`.** This lane absorbs NO drift row — it
  is candidate 3 of the `2075242f9` round's "What is next" (phase-4.md
  `:6661-6667`) plus the two `report_census` records from the same round's
  §3 review (`status-log.md:129009-129016`: "`report_census`'s `fileIds`
  reaches `combined.log` as a JSON string where v4 logs an array; the
  census's `fileIds` ORDER is SELECT-order over a table the real instance
  indexes"). v4's code is the spec at the BASELINE pin: `lib/photos/
  photos-paths.ts:30-34` (`isPhotosRelativePath` over `path.posix.
  dirname`) and `migrations/scripts/collapse-duplicate-avatar-rolls-v1.ts:
  262-267, 311-320` (`unexplainedDuplicateKeys` + `reportCensus`).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** ONE lane-unique pin **`/tmp/qt-v4-
  pin-p4.91-2075242f9`** (the baseline — `1fefadb9a` touches neither
  `lib/photos/` nor `migrations/`; record `git -C ~/source/quilltap-server
  diff --stat 2075242f9..1fefadb9a -- lib/photos/ migrations/` EMPTY).
- **Sibling lanes:** P4.D195 (bug 147 whole — the chat GET projection + the SPA seed + the help re-vendor), P4.92 (the tool loops' `base_params` carry-over class), P4.93 (the harness + logging maintenance smalls). None touches `photos/**`, `db/doc_mount_
  file_links.rs`, `db/avatar_rolls_collapse_heal.rs`, `log_file.rs`, or the
  photos-path family. P4.93's avatar log lines are in DIFFERENT files
  (`services/character_avatar_job.rs`, `services/image_job_common.rs`) —
  §R.10 (d).
- **Worktree:** `git worktree add <path> -b claude/p4-91-photos-predicate-
  consolidation-<hash> main`; `CARGO_INCREMENTAL=0`. No SPA.
- **Read before the work:** `crates/quilltap-core/src/photos/photos_paths.
  rs:20-29` (`is_photos_relative_path`), `:61-76` (`posix_dirname` — the
  doc at `:61-63` CLAIMS "Matches v4's `path.posix.dirname`"; the code
  strips ONE trailing slash), `:78-92` (the in-file unit test — 8 asserts,
  no trailing-slash input), `:55` (`is_character_album_relative_path`
  delegates through it); `crates/quilltap-core/src/db/doc_mount_file_
  links.rs:332-365` (the Node-faithful `posix_dirname` — the `matched_
  slash`/`end` loop, root handling, skips a RUN), `:373-388` (the twin +
  its ⚠ doc that already names the divergence and this order's shape);
  `crates/quilltap-core/src/db/avatar_rolls_collapse_heal.rs:71-83` (the
  13-line justification for importing the `doc_mount_file_links` twin —
  written by the `2075242f9` §3 review, commit `4954de3a`), `:312` (the
  use), `:474-495` (`unexplained_duplicate_keys`' SELECT — no `ORDER BY`,
  byte-identical to v4's), `:501-513` (the `Map`-insertion-order
  bucketing comment), `:535-552` (`report_census` — `serde_json::to_
  string(&first_twenty)` handed to `tracing::warn!` as `fileIds = file_
  ids.as_str()`); `crates/quilltap-web/src/log_file.rs:1103-1152`
  (`FieldVisitor::put` + the `Visit` impl — `record_str` stores a
  `Value::String`; no `record_value`, no re-parse), `:1168-1194`
  (`on_event`); `crates/quilltap-core/src/services/route_trail.rs:421-436`
  (the OTHER JSON-string log field — the same convention; NOT yours to
  edit, record it); `crates/quilltap-harness/tests/photos_relative_path_
  equivalence.rs` whole (51 lines: imports the TWIN at `:17`, env
  `QT_ORACLE_PHOTOS_PATH`, row `{id, path, out}`, the `>= 13` floor at
  `:49`) + `harness/oracle/cases/photos-relative-path.ts` (imports v4's
  REAL `isPhotosRelativePath`; the 13 inline inputs); `services/
  maintenance.rs:553-568` (`photos_relative_path_predicate` — 10 asserts
  against the twin); the measurement table at `status-log.md:127700-
  127704` and the ordered shape at `:127712-127723`; memory notes
  `mutation-proof-revert-by-file-backup`, `a-fixture-whose-natural-order-
  matches-the-read-order-hides-the-sort`, `tracing-percent-field-renders-
  unquoted`, `an-in-file-source-census-must-strip-test-modules-by-braces`.

## Mandate

Leave ONE `is_photos_relative_path` in the tree, Node-faithful, proven by
the differential family — and get there red-first. Repoint the harness
family onto `photos::photos_paths::is_photos_relative_path` FIRST (today
it drives the twin, which is already faithful, so growing the corpus
against it would go green having measured nothing); grow the corpus with
the trailing-slash-run shapes (`photos//`, `photos///`, `a//`, and the
sibling shapes named below) at the pin and watch the repointed family go
RED on exactly those rows; replace `photos_paths`' `posix_dirname` with
the Node algorithm (the `doc_mount_file_links.rs:332-365` loop, moved —
not re-derived) and go green; THEN delete the twin and repoint its SIX
importers (the collapse heal is the sixth, with the `:71-83` comment
rewritten to the new single-home truth), consolidate the two near-identical
unit tests, and prove production unmoved everywhere a natural writer mints
a path (no writer mints a trailing-slash run — say so with the grep). Then
the heal's census: render `fileIds` as the ARRAY v4 logs by teaching the
`log_file.rs` field visitor a NAMED convention for JSON-valued string
fields (a suffix or prefix the visitor re-parses into a real `Value`), and
retarget `report_census` onto it — the ORDER half (SELECT-order over a
table the real instance indexes) is RECORDED with its measured exposure
and NOT changed: an `ORDER BY` is a deliberate divergence from v4 and
needs the human's ruling. This lane does NOT touch `route_trail.rs`
(record that it is the convention's second candidate), P4.93's avatar
job files, or any sibling's files.

## Survey-verified starting points (fresh survey 2026-09-16 at `2075242f9` and on `main` `6e0ba5e8` — do not re-derive)

### v4 (at `2075242f9`; unchanged by `1fefadb9a`)

- **`lib/photos/photos-paths.ts:30-34`** — `if (!relativePath) return
  false; const folder = path.posix.dirname(relativePath).toLowerCase();
  return folder === PHOTOS_FOLDER || folder.startsWith(`${PHOTOS_FOLDER}/
  `);`. `path.posix.dirname` SKIPS a run of trailing slashes before
  finding the last separator (`"photos//"` → `"."`, `"photos///"` →
  `"."`, `"a//"` → `"."`, `"/photos//"` → `"/"`, `"photos/sub//"` →
  `"photos"`). **There is NO v4 twin** — 21 `isPhotosRelativePath` sites
  in `lib/`+`app/` all import this one module (`grep posix.dirname lib/
  app/` finds five unrelated callers). The v5 twin is a v5 invention.
- **`migrations/scripts/collapse-duplicate-avatar-rolls-v1.ts:262-267`**
  — the census SELECT, byte-identical to v5's, no `ORDER BY`; `:311-320`
  `reportCensus`: `logger.warn('Avatar rolls share a generation key this
  pass did not choose to double up', { context: 'migration.collapse-
  duplicate-avatar-rolls', unexplainedCount: unexplained.length, fileIds:
  unexplained.slice(0, 20) })` — `fileIds` a raw `string[]`; winston
  renders `"fileIds": ["id1","id2"]`.

### v5 (on `main` `6e0ba5e8`)

- **Home A (survivor)** `photos/photos_paths.rs:20-29` + `:64-76`
  (`posix_dirname`: `strip_suffix('/')` ONCE, then `rfind('/')`). Importers
  of Home A: `tools/photo.rs:33` (uses `:356,:450,:714,:793`), `photos/
  user_gallery_service.rs:44` (`:134,:352,:397,:482`), `photos/save_image_
  to_album.rs:42` (`:168`), `photos/chat_gallery.rs:51` (`:899`), and the
  in-module `is_character_album_relative_path` `:55`. **NOT driven by any
  differential family today.**
- **Home B (the twin, to delete)** `db/doc_mount_file_links.rs:332-365`
  (Node-faithful `posix_dirname`) + `:373-388`. Importers of Home B —
  **SIX**, not the recorded five: `api/chat_media.rs:1716` (fully-
  qualified call, no `use`), `photos/photo_link_summary.rs:13` (`:89`),
  `photos/character_gallery_service.rs:17` (`:283`), `services/
  maintenance.rs:32` (`:154`) + `:375` (the test mod's import),
  **`db/avatar_rolls_collapse_heal.rs:83`** (`:312`; the `:71-83` comment
  explains WHY it reads the twin — v4's migration imports `photos-paths`,
  which skips a run; and the runtime roll rule (`classify_roll_links` →
  `photo_link_summary`) reads the twin, so heal and runtime agree), and
  `crates/quilltap-harness/tests/photos_relative_path_equivalence.rs:17`.
- **Unrelated `posix_dirname` copies — do NOT touch** (different
  signatures/semantics): `tools/generate_image.rs:1305`, `db/mount_index_
  case_repair.rs:69`, `photos/save_image_to_album.rs:537`, `services/
  image_job_storage.rs:401`. (Record them; a later consolidation is its
  own question.)
- **The family** drives Home B; env `QT_ORACLE_PHOTOS_PATH`; `>= 13`
  floor at `:49`; the 13 inputs: `photos/a.webp`, `Photos/a.webp`,
  `PHOTOS/a.webp`, `photos/sub/a.webp`, `photos/2026/01/a.webp`, `images/
  a.webp`, `a.webp`, `my-photos/a.webp`, `photosx/a.webp`, `/photos/a.
  webp`, `a/photos/x.webp`, `''`, `null`. Last touched Phase 3 (W4.8).
  Sweep-runnable (`recipe_sweep.py --run photos_relative_path_
  equivalence`); last artifacts `harness/tools/sweep-results/2026-09-16-
  2075242f9-unify-baseline-pin.json`.
- **Unit tests:** `photos_paths.rs:78-92` (8 asserts) and `maintenance.rs:
  553-568` (10 asserts against Home B) — near-duplicates, neither with a
  trailing slash. `grep 'photos//' crates/` → prose only. **No test in
  either repo mints a multi-slash path.**
- **The measured table** (`status-log.md:127700-127704`): `"photos//"` →
  Node `"."`/false, Home B false ✓, Home A `"photos"`/**true** ✗;
  `"photos///"` → Node `"."`, B ✓, A `"photos/"`/**true** ✗; `"a//"` →
  Node `"."`, B ✓, A `"a"` ✗. The disagreement is in the SAFE direction
  (Home A over-includes only on paths no writer mints).
- **`report_census`** `avatar_rolls_collapse_heal.rs:535-552` — `let
  file_ids = serde_json::to_string(&first_twenty)…; tracing::warn!(target:
  "quilltap::migration", context = LOG_CONTEXT, unexplainedCount = …,
  fileIds = file_ids.as_str(), "Avatar rolls share a generation key this
  pass did not choose to double up")`. In `combined.log`: `"fileIds":
  "[\"id1\",\"id2\"]"` (a STRING). The visitor (`log_file.rs:1130-1152`)
  has `record_str`/`record_debug`/ints/bool/f64 — no `record_value`
  (`valuable` is not a dependency anywhere: `grep valuable */Cargo.toml`
  → empty; `tracing = "0.1"` bare). A `Vec<String>` via `?` would render
  the RUST debug string. `route_trail.rs:421-436` already uses the same
  JSON-string convention (`trail = %serde_json::to_string(…)`) — the
  convention's second candidate; NOT yours.
- **The ORDER half:** `unexplained_duplicate_keys` `:474-495` has no
  `ORDER BY` on either side; v5 mirrors v4's `Map` insertion order
  (`:501-513`). The real instance has `idx_files_generationKey` (named as
  a fresh-instance divergence at `status-log.md:125861-125863`), so
  SQLite may satisfy the `IN`-subquery scan via the index and hand back a
  different row order than the un-indexed fixture — `fileIds` becomes
  instance-dependent while the harness pins one order. An `ORDER BY id`
  on BOTH sides is the fix and a deliberate divergence from v4 → RULING.

## Tiered deliverables

### Tier 1 — must land

1. **Repoint the family FIRST.** `photos_relative_path_equivalence.rs:17`
   imports `quilltap_core::photos::photos_paths::is_photos_relative_path`
   (Home A). Regenerate at the pin through the sweep driver; run: GREEN
   over the 13 rows (Home A agrees on every non-slash-run shape — the
   baseline measurement).
2. **Grow the corpus red-first.** `harness/oracle/cases/photos-relative-
   path.ts` gains, at minimum: `photos//`, `photos///`, `a//`, `/photos//`,
   `photos/sub//`, `photos/a.webp/` (single trailing slash — Node and BOTH
   homes agree; the control), `//`, `/`, `photos`, `photos/`, `.`, `./
   photos/a.webp` (Node's dirname of `./photos/a.webp` is `./photos` →
   false — measure), `photos//a.webp` (an INTERIOR run — Node's dirname
   is `photos/` → `startsWith("photos/")` TRUE; measure and pin whatever
   v4 says), `PHOTOS//`. 13 → ≥ 27 rows; the harness floor `:49` bumped
   to the new count (an EXACT `==`, not `>=`, so a stale oracle cannot
   pass). Regenerate at the pin; run against UNCHANGED Home A: RED on
   exactly the run rows (`photos//`, `photos///`, `PHOTOS//` — and
   `photos/sub//`? Home A strips one → `photos/sub` → true; Node → `photos`
   → true; agree — record what actually reds). `grep -c 'photos//'` in the
   fresh NDJSON > 0.
3. **Make Home A Node-faithful.** Replace `photos_paths::posix_dirname`
   (`:64-76`) with the algorithm at `doc_mount_file_links.rs:332-365`,
   MOVED (cut-and-paste with its comments; the doc at `:61-63` rewritten
   to say it IS Node's loop and names this order + the measured table).
   Family GREEN over every row. `photos_paths.rs:78-92`'s unit test gains
   the run shapes.
4. **Delete the twin; repoint the six.** Remove `doc_mount_file_links.rs:
   332-388` (the twin + its `posix_dirname` + the ⚠ doc); repoint `api/
   chat_media.rs:1716`, `photos/photo_link_summary.rs:13`, `photos/
   character_gallery_service.rs:17`, `services/maintenance.rs:32` + `:375`,
   `db/avatar_rolls_collapse_heal.rs:83` onto `photos::photos_paths`.
   REWRITE `avatar_rolls_collapse_heal.rs:71-83`: the heal reads the one
   home, which is v4's own module (`photos-paths.ts`), Node-faithful since
   this order; the runtime roll rule reads the same home, so heal and
   runtime agree by construction, not by choice. Consolidate `maintenance.
   rs:553-568`'s `photos_relative_path_predicate` into `photos_paths.rs`'s
   test (or keep it as a caller-side smoke — say which; do not leave two
   near-identical suites pinning one function from two modules). Mutation
   proofs: M1 revert `posix_dirname` to the one-slash strip → the run rows
   red; M2 make the loop skip NO trailing slash at all → `photos/a.webp/`
   (the control) red; M3 make `is_photos_relative_path` compare without
   `to_lowercase` → the `Photos/`/`PHOTOS/` rows red.
5. **Production unmoved, proven.** (a) `cargo test --workspace` green —
   every family reading a photos path (`photo_link_summary`, `character_
   gallery_service`, `chat_gallery`, `user_gallery`, `maintenance`, the
   avatar-rolls collapse family `avatar_rolls_collapse_heal_equivalence`
   or its name in the tree, `tools/photo.rs`'s families) re-run by name
   at the pin with ZERO row change; (b) the writer census: `grep -rn
   'format!.*photos/' crates/quilltap-core/src/photos` + every
   `relativePath` minting site — NO writer can produce a trailing-slash
   run (record the sites); the consolidation changes the answer ONLY on
   inputs no writer mints.

### Tier 2 — should land

6. **`report_census` logs the ARRAY.** `log_file.rs:1103-1126`
   (`FieldVisitor::put`): a NAMED convention — a field whose name ends in
   a documented suffix (e.g. `Json`) is `serde_json::from_str`-parsed into
   a `Value` and inserted under the name WITHOUT the suffix; a parse
   failure falls back to the raw string under the raw name (never a
   panic in a logging layer). Unit-test the visitor (both arms).
   `report_census` renames its field to `fileIdsJson = file_ids.as_str()`
   → `combined.log` carries `"fileIds": ["id1","id2"]`, byte-shape
   identical to v4's. Capture-pin: the existing census capture test
   asserts the field is a JSON ARRAY of ≤ 20 strings (the P4.D192 pin
   moves from string-compare to array-compare). Record `route_trail.rs:
   421-436` as the convention's second candidate for its owning lane.
7. **The ORDER half, recorded not changed.** In the lane record and the
   `:501-513` comment: the SELECT has no `ORDER BY` on either side; the
   real instance's `idx_files_generationKey` can reorder the scan; an
   `ORDER BY id` (both sides) is the fix and a deliberate divergence
   from v4 — **ESCALATED for the human's ruling** (the P4.D40 evidence-
   conditional shape). Do not add it.
8. **The stale-claim corrections, by NEW record only** (§R.7): the "five
   importers" count (status-log `:127712-127723`, phase-4 `:6745-6746`),
   the "byte-identical homes" claims (`status-log.md:124650, :124890,
   :125868`; phase-4 `:7027-7029`), `docs/CHANGELOG.md:138`'s "what v4's
   migration itself now imports" (contradicted by `:66`) — one paragraph
   in the lane record naming each, so the unifier can decide what the
   round record restates.

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **The four unrelated `posix_dirname` copies** stay (different
  signatures; each has its own caller semantics) — listed in the lane
  record as a later consolidation candidate, not touched.
- **`route_trail.rs`'s JSON-string `trail` field** — the convention's
  second candidate; out of ownership; recorded.
- **The `ORDER BY` ruling** — Tier 2 item 7.

## The differential requirement, restated

The consolidation ships with `photos_relative_path_equivalence` repointed
onto the surviving home and grown red-first at the pin (a tsx oracle over
v4's REAL `isPhotosRelativePath` — the family's own header recipe, run
through the sweep driver), an EXACT row-count floor, and every consumer
family re-run by name with zero row change. The census log shape ships
with a visitor unit test (both arms) and the heal's capture pin moved to an
array compare — log records have no oracle (`crates/quilltap-web/src/lib.
rs:133-135`), so the pin is the byte shape v4's winston writes, quoted in
the test. Mutation proofs as named.

## Fixtures

- **Consumes:** none on disk (the family's corpus is inline in the tsx
  case). The avatar-rolls collapse family's committed fixtures are READ
  by the neutrality re-run only — never regenerated by this lane.
- **Delivers:** the grown `harness/oracle/cases/photos-relative-path.ts`.
- **Owns regenerating:** `photos_relative_path_equivalence` (pin); the
  consumer families by name (pin, neutrality).

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `1fefadb9a` bug-147 drift catch-up + maintenance round:
**P4.D195 ∥ P4.91 ∥ P4.92 ∥ P4.93**, ordered 2026-09-16. Four lanes, four
worktrees, four lane branches, ALL from `main` (no stacking — every lane's
ownership is disjoint by file, so nothing needs a substrate commit). The
unifier cherry-picks in the order P4.D195 → P4.91 → P4.92 → P4.93 (the drift
row first; the harness-wide role-mapper sweep and the env-var rename last,
because they touch the most files and change the gate's env block), gates
ONCE, and `merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`2075242f9`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`1fefadb9a`**
  (`4.10.0-dev.41`, 2026-09-16 06:57) — ONE commit past the baseline, the
  bug-147 code commit, the ledger's one §3 row, now `ORDERED(P4.D195)`.
  v4 `bugfix` tip `1a2b2164c` and `release` tip `8fbf2afe0` are unmoved;
  there is still no `release: 4.10.0` squash. The checkout sat on `main`,
  CLEAN, at the planning probe (2026-09-16; the §2 probe PASSED, so the
  ledger stood and nothing was re-derived). **At unification the baseline
  MOVES to `1fefadb9a`** and the row retires ABSORBED(P4.D195).
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log 1fefadb9a..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Any mismatch — a moved HEAD, a dirty `lib/`/`app/`/`packages/`
  /`plugins/`/`migrations/`/`help/` tree, the checkout off `main` — is a
  STOP: report it and do nothing else.** A lane never writes the ledger.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1). HEAD is past the
  baseline and the commit touches `app/` and `help/`. Every regen runs
  from a LANE-UNIQUE detached worktree per ledger §5.1 (`git -C ~/source/
  quilltap-server worktree add --detach /tmp/qt-v4-pin-<order>-<sha>
  <sha>`, the THREE symlink classes, `cd` into it; never stash or
  checkout-switch the human's repo; `git worktree list` before each batch;
  verify the pin by grepping the fresh NDJSON for a marker only one tree
  has; check the file is non-empty before believing a diff). Which sha:
  - **P4.D195 pins the TARGET `1fefadb9a`** for every family its port
    MOVES (`salon_reads_equivalence`, the `help_*` content families, any
    turn-manager rows it adds) and the BASELINE `2075242f9` for its
    neutrality legs;
  - **P4.91, P4.92 and P4.93 pin the BASELINE `2075242f9`** — their v4
    surfaces are untouched by `1fefadb9a` (each lane records `git -C
    ~/source/quilltap-server diff --stat 2075242f9..1fefadb9a -- <its v4
    paths>` EMPTY in its lane record before the first regen). A pin at
    the baseline is still REQUIRED, not optional: the checkout can go
    dirty mid-lane (ledger §5.1), and a bare-checkout regen is
    unrepeatable.
  The sweep driver is the sanctioned per-family path
  (`harness/tools/recipe_sweep.py --run <family> --v4 "$PIN" --v5w
  <ABSOLUTE worktree>`); never run two sweeps concurrently; a family that
  reports `SKIP` in 0.00 s did not run (`a-family-env-var-is-not-its-
  regen-var`). Ledger §5.2's silent-stale-pass discipline applies to
  every regen (`rm -f` the fixture first; read the builder's last line;
  `grep -c` the changed bytes in the fresh NDJSON — MUST be > 0).
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). The
  spec for every item is `git show <sha> -- <path>` plus `git show
  <sha>:<path>`; a commit message describes the bug as its author
  understood it and the hunks are often narrower. Re-verify even where
  this order already flags the trap — the order's paragraph is the same
  kind of prose. Carry a v5-side code comment naming the sha where the
  prose misleads.
- **§R.5 — Convergence and designed trips: measure, don't assume**
  (ledger §5.4). The P4.D171 chat-GET omission pin (`salon_reads_
  equivalence`'s `get_*` cases) trips at the target pin BY DESIGN; dump
  v4's actual post-fix bodies before porting, and expect every `get`
  case to move together. The `state.cycleOrder` client read is a
  convergence measured with a table, not asserted.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest`
  is shared) and its silence leg; a fixture change lands with the regen
  of every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that
  SURVIVES as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`).
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed (and `apps/web/package.json` when it touches the SPA) on each
  commit per `.claude/commands/commit.md`; the unifier RECOUNTS as base +
  the sum of every lane's bumps (the silent-auto-merge trap: identical
  bumps merge as ONE — it fired again last round). Expected: core is
  bumped by ALL FOUR (P4.D195 `api/salon.rs`; P4.91 `photos/`, `db/`;
  P4.92 `services/orchestrator.rs`; P4.93 `model/completion.rs`, the two
  avatar job files, `db/memories.rs`); harness by ALL FOUR; web by P4.91
  ONLY (the `log_file.rs` visitor, Tier 2) — no other lane touches
  `crates/quilltap-web/src`; the SPA by P4.D195 ONLY; host/cli/tauri by
  NOBODY (a `help/**` re-vendor changes the embedded table `quilltap-
  host`'s `build.rs` produces but no host SOURCE — the P4.D194 precedent:
  no host bump for help alone).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  P4.D195 pre-lists the paths the target moves (with byte counts at the
  pin) in its lane record; no lane copies them.
- **§R.10 — Cross-lane handoffs, named:** (a) P4.93 RENAMES the guards
  family's fixture var (`QT_FIXTURE_HELP_MAIN` → a family-specific name)
  in `help_doc_sync_guards_equivalence.rs`; P4.D195 regenerates and RUNS
  the `help_*` content families under TODAY's names (it edits none of
  them) — the unifier re-runs both help-sync families with the renamed
  block after the pick. (b) P4.93's role-mapper sweep touches every
  hand-rolled `role` match in `crates/quilltap-harness/tests/` EXCEPT
  `orchestrator_tier3_equivalence.rs` (P4.92's file — its mapper already
  carries the `tool` arm); if P4.93 lands a shared inverse in
  `model/completion.rs`, the unifier repoints P4.92's mapper onto it at
  the wire (one line, recorded), never a lane. (c) P4.92 RUNS
  `chat_create_capstone` and `primary_stream_tier3` as neutrality legs
  while P4.93 EDITS their mappers — run vs edit is not a collision; the
  unified gate re-runs both. (d) P4.91 owns `db/avatar_rolls_collapse_
  heal.rs` WHOLE (the import repoint, the `:71-83` comment, and
  `report_census`); P4.93's avatar log lines live in `services/character_
  avatar_job.rs` + `services/image_job_common.rs` — different files.
  (e) P4.D195's Tier 2 item 8 may add `#[cfg(test)]` rows to `select_
  speaker.rs` / `turn_state.rs` or corpus rows to their families; no
  sibling touches either. (f) The `to_completion_messages`-class fix in
  P4.93 must leave every family's canned KEY bytes unchanged — a mapper
  that keys a role differently would silently miss; P4.93 proves each
  edited family green on its pin-fresh oracle by name.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D195** — bug 147 whole (server projection + SPA seed + help) | `crates/quilltap-core/src/api/salon.rs` — ONLY the `chat_get` projection block (`:355-390`); `crates/quilltap-harness/tests/salon_reads_equivalence.rs`; `harness/oracle/cases/salon-reads.test.ts`; `help/chat-turn-manager.md`; `crates/quilltap-harness/tests/help_tree_embed_guard.rs` — the doc-comment vendor list ONLY; `crates/quilltap-core/src/select_speaker.rs` + `turn_state.rs` — `#[cfg(test)]` modules ONLY (Tier 2 item 8), or instead `harness/oracle/cases/select-speaker*.ts` / `turn-state*.ts` + their two harness families (corpus rows only); `apps/web/src/app/core/core-contract.ts` — the ONE new key + its doc beside `cycleOrderParticipantIds`; `apps/web/src/app/chat/turn-order.ts` + `turn-order.spec.ts`; `apps/web/src/app/screens/salon/salon-conversation.ts` — ONLY `_turnEffect`, `refreshTurn`, `applyTurnResponse` and their doc comments; `salon-conversation.spec.ts`; `apps/web/src/app/chat/sidebar/chat-sidebar.spec.ts` (a wiring spec); `apps/web/e2e/salon-sidebar-flow.spec.ts`; `apps/web/package.json` version; this order's header | every other `crates/**` file (incl. the server turn handlers, `chats_read.rs`, `db/**`), the salon fixture PAIR on disk (plants are per-case copies), every other SPA file, `help/**` beyond the one page, the drift ledger |
| **P4.91** — the photos-predicate consolidation + the collapse heal's census log shape | `crates/quilltap-core/src/photos/photos_paths.rs`; `crates/quilltap-core/src/db/doc_mount_file_links.rs` — ONLY the deletion of the `is_photos_relative_path` twin + its private `posix_dirname` (`:332-388`); the SIX importers, import-line edits ONLY: `api/chat_media.rs:1716`, `photos/photo_link_summary.rs:13`, `photos/character_gallery_service.rs:17`, `services/maintenance.rs:32` + `:375` (and its `photos_relative_path_predicate` test if consolidated); `crates/quilltap-core/src/db/avatar_rolls_collapse_heal.rs` — the import + the `:71-83` justification comment + `report_census` (`:535-552`) + its tests; `crates/quilltap-web/src/log_file.rs` — the `FieldVisitor::put` JSON-field convention ONLY (Tier 2) + its tests; `harness/oracle/cases/photos-relative-path.ts`; `crates/quilltap-harness/tests/photos_relative_path_equivalence.rs`; this order's header | every other `crates/**` file (P4.93's avatar job files, `services/route_trail.rs`), `apps/web/**`, `help/**`, every other harness family, the drift ledger |
| **P4.92** — the loops' `base_params` carry-over class | `crates/quilltap-core/src/services/orchestrator.rs` — ONLY the strip after the `:2894` rebuild (or on the three `base_params:` clones at `:3002`/`:3080`/`:3125` — say which) + the `:2150` comment; `crates/quilltap-core/src/model/streaming_provider.rs` — the P4.41 doc block at `:339` ONLY (one sentence); `crates/quilltap-core/src/services/native_tool_loop.rs` + `text_tool_loop.rs` — `#[cfg(test)]` modules ONLY (Case B's unit pin); `crates/quilltap-harness/tests/orchestrator_tier3_equivalence.rs`; `harness/oracle/cases/orchestrator-tier3.test.ts`; `harness/oracle/fixtures/orchestrator-tier3.json` + `build-orchestrator-fixture.ts` + the fixture pair that builder produces; this order's header | every other `crates/**` file (the request builders, `primary_stream.rs`, `provider_failover.rs`), `apps/web/**`, `help/**`, every other harness family (P4.92 RUNS `chat_create_capstone` / `primary_stream_tier3` — never edits them), the drift ledger |
| **P4.93** — the harness + logging maintenance smalls | `crates/quilltap-harness/tests/help_doc_sync_guards_equivalence.rs` + `help_doc_sync_equivalence.rs` (headers + the env var + the fixture-sensitive arm) + `harness/oracle/cases/help-sync-guards.ts` (+ `help-sync-tier2.ts` only if the arm needs an oracle column); `crates/quilltap-core/src/model/completion.rs` — the `CompletionRole` inverse (`from_v4_wire`/`FromStr`) + tests; every hand-rolled role-mapper family in `crates/quilltap-harness/tests/` named in its survey (Class 1 + Class 2 + `external_prompt_tier3_equivalence.rs` + `file_attachment_tier3_equivalence.rs`) EXCEPT `orchestrator_tier3_equivalence.rs`; `crates/quilltap-core/src/db/memories.rs` — the ONE test's capture rig ONLY; `crates/quilltap-core/src/services/character_avatar_job.rs` + `services/image_job_common.rs` — the `[CharacterAvatar]` log lines + their capture tests ONLY; `crates/quilltap-harness/tests/avatar_job_tier3_equivalence.rs` — ONLY if a line-count pin must widen; `harness/tools/recipe_sweep.py` — ONLY if the env rename needs a driver-side alias (say so); this order's header | `orchestrator_tier3_equivalence.rs` (P4.92's), `salon_reads_equivalence.rs` + the help EMBED guard (P4.D195's), `db/avatar_rolls_collapse_heal.rs` (P4.91's), `photos/**`, `apps/web/**`, `help/**`, `crates/quilltap-web/src/**`, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0),
   and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp`) where the family is deterministic across
   regens; where it is not (`regenerate-at-both-pins-and-cmp-is-not-
   universal`), run by name and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast` with the lane's env block
   (its own oracle vars + `QT_V4_ROOT`/`QT_V4_CHECKOUT` at the right pin +
   `QT_NODE` = the node BINARY): report **binaries / passed / failed /
   ignored** from the per-binary lines and the exit code, and the lane's
   families confirmed RUN by non-zero duration. No family is withheld
   this round (P4.89 restored the brahma pair).
9. **SPA lanes only (P4.D195):** `npm run lint` (incl. the qt-class guard),
   `npm test` (spec files / tests / 0 failed), `npm run build` clean, then
   the lane's e2e beats by FILE against the release binary, and ONE full
   Playwright run at lane close (numbers recorded; the documented
   intermittents — the P4.D161 pause-toast beats, the P4.d17 quill beat,
   the `salon-documents-flow` / `workspace-flow` terminal trio — are
   named, re-run by file, and never re-classified as this lane's).
10. **Ownership check:** `git diff --stat main...HEAD` — every path is in
    the lane's Owns column; every MUST-NOT-TOUCH path absent.
11. **Per commit:** `.claude/commands/commit.md` in full — the equivalence
    test is non-negotiable (§1), the invariants (§2), real-instance safety
    (§3), the harness regen (§4), build/test/lint (§5), the CHANGELOG
    entry + version bumps (§7). Commit on the lane branch only; never
    merge to `main`; never `git stash`; never a worktree inside a worktree.
12. **Lane close:** update this order's status header (what landed, what
    was refuted by measurement, what stays OPEN with its named cause),
    append the lane record to `status-log.md` (the regen commands as run,
    the counts, the mutation table, every finding outside the lane's
    ownership for the unifier), and stop. The final gate runs in the
    FOREGROUND of the lane's last turn — a backgrounded gate whose
    notification arrives after the lane reports done is a lane that is
    NOT done (`a-backgrounded-sleep-does-not-gate-your-turn`).
