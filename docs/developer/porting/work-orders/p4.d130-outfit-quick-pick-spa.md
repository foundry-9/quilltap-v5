# Work order P4.D130 — the `aec86a613` outfit pull-down (SPA drift) + the wardrobe e2e-fixture debt

**Status:** CLOSED (2026-08-27, branch `claude/p4-d130-outfit-quick-pick-9a808f`;
unified on `unify/p4d130-round`). All tier-1 units landed: the pool-split twin
with BOTH equivalence checks (v4's 7-case transcription 1:1 + a nine-case
recorded-vector corpus whose ICU collation rows the transcription cannot ask —
mutation-proven), the pull-down component (capture-phase Escape spec-pinned
with a bubble-stub proof; the `hidden`-host renders-nothing divergence
recorded), the composer mount + garments-only slot pickers (the
`allItems`-passed-whole chip-label pin written RED first), and the live
dissolution beat. Tier 2: **debt 1 HALF-closed** — the missing
`instance_settings` table materialized in the spec's `beforeAll` (NOT a
fixture regen — six harness families spared), the "Shared — everywhere"
create-scope beat LIVE; the component-transfer beat re-parked on its REAL
blocker (`characters-main.db` has no `projects`/`groups` tables, so
`enumerate_destinations` fails whole — park now probes
`hasTransferDestinations`; precisely-scoped follow-up recorded). **Debt 2
CLOSED** — the duplicate "Quilltap General" was NOT `builtin_mounts.rs` (the
provisioner measured idempotent): `seedCourierImagesFixture` drags in
pointer-less built-in stores; `global-setup.ts` now reconciles each by what it
holds (unreferenced → drop, referenced → adopt), `sameName=1`, with a standing
one-store tripwire in the beat. Deferred loud: the new-chat host rides the
standing wardrobe-composer family (dated comment on `outfit-selector.ts`);
`help/wardrobe.md` → `p4.9i2`. Mid-lane v4 drift recorded and re-proven
byte-identical from a fresh pin. Zero crate source; SPA 0.5.586 → 0.5.590.
Lane record: `status-log.md` → "P4.D130".

## Preamble

- **v4 baseline for this round: `8872d7efc`** (the last commit of the 4.9.0
  release push; drift ledger §1, checked 2026-08-27). **This lane's target
  commit is `aec86a613`** — v4 main HEAD at planning, ONE portable commit
  past the baseline (the intervening `b6c6d7793` is docs-only, this port's
  own bug-105 filing; it is carried in the ledger as a NO-PORT? row for the
  unifier to ratify at the baseline move — this lane does not touch it).
- **Freshness probe at lane start** (drift-ledger §2, four read-only
  commands). Pass = checkout on `main`, tree clean, no commits past
  `aec86a613` on main and none past `3a76b17df` on bugfix. **Any mismatch:
  STOP and report** — lanes never write the ledger.
- **Regen rule in force: PIN REQUIRED** (ledger §1). This lane's own
  oracle/vector work pins a lane-unique detached worktree at **`aec86a613`**
  (the drift commit IS the spec): `/tmp/qt-v4-pin-p4d130-aec86a613`, all
  three symlink classes, per ledger §5.1. Any regen of a *pre-existing*
  family (e.g. after the salon-fixture widening) pins **`8872d7efc`** — the
  baseline, not the drift commit — so neutrality claims stay claims about
  the baseline. Verify every pin by grepping the output for a marker only
  one tree has. Every family regen goes through the sweep driver
  (`harness/tools/recipe_sweep.py --v4 <pin>`); never two sweeps
  concurrently.
- **Sibling lanes:** P4.62 (`p4.62-wrong-type-collapse-pockets.md`), P4.63
  (`p4.63-harness-differential-followups.md`), P4.64
  (`p4.64-systemhome-profile.md`). The lanes meet nowhere; §Ownership below
  is the binding split (identical table in all four orders). **This lane is
  the round's ONLY Playwright runner** (port 4319 is repo-wide — memory
  `e2e-port-4319-is-repo-wide`); the siblings' gates are cargo-only.
- **Worktree:** per the `carryout` skill; edit inside the worktree, not main
  (repo-root paths exist in BOTH trees). `docs/CHANGELOG.md` and
  `status-log.md` append-only.

## Mandate

Port v4 `aec86a613` — the outfit pull-down above the slot rows, garments
only in the slot pickers — into the v5 SPA, and discharge the two carried
wardrobe e2e-fixture debts that live in the same files (phase-4 candidates
"widen the fixture with a Quilltap General store" and "the duplicate
Quilltap General store collision"). The v4 commit is **client-only**: no
server verb, no schema, no wire change; the equip path is the EXISTING
`onAddToSlot` callback (v5: the `addToSlot` output). Do not invent a new
equip path, and do not touch the deferred **new-chat manual-compose**
surface — v5's Starting Outfit `manual` mode renders loudly disabled by
design (`screens/new-chat/outfit-selector.ts:43`, the standing P4.D39
deferral); the pull-down reaches that host when THAT family lands, not now.
`help/wardrobe.md`'s two new sections bank to `p4.9i2` (record the bank, do
not port them).

**Must NOT touch:** anything a sibling owns (§Ownership); the server
wardrobe code (`crates/quilltap-core/src/**` EXCEPT the
`services/builtin_mounts.rs` diagnosis below); the new-chat outfit-selector
beyond a comment noting the pull-down rides the deferred family.

## Survey-verified starting points (fresh, 2026-08-27)

**The v4 commit's five shipped pieces** (classified from the hunks — ledger
§3 carries the same survey; re-verify against `git show aec86a613` from the
pin, never the commit message, ledger §5.3):

1. **NEW `lib/wardrobe/composed-outfits.ts`** (41 lines) — pure pool split
   on the existing `isBundle` from `lib/wardrobe/dissolve-bundles.ts`:
   `selectComposedOutfits` = `items.filter(isBundle).sort((a, b) =>
   a.title.localeCompare(b.title))`; `selectGarments` = the complement,
   caller's order. Archived items are already gone from the pool the
   composer is handed (`mergeWearablePool`) — nothing re-filters. Shipped
   with `__tests__/unit/lib/wardrobe/composed-outfits.test.ts` (69 lines).
   v5's client `isBundle` twin: `apps/web/src/app/wardrobe/
   dissolve-bundles.ts:64`.
2. **NEW `components/wardrobe/outfit-quick-pick.tsx`** (138 lines) — the
   `Wear an outfit…` pull-down: a `qt-button-secondary qt-button-sm`
   full-width toggle with a rotating `chevron-down`; a `role="listbox"`
   panel with an autofocused `type="search"` box; per-row
   `WARDROBE_SLOT_META[t].label` joins plus a ` · replaces` suffix when
   `outfit.replace`; `No matching outfits.` on an empty filter;
   outside-click close (document `mousedown`); an Escape handler registered
   **in the capture phase with `stopPropagation` + `preventDefault`** so the
   enclosing dialog is not dismissed with the menu. **Renders nothing when
   the pool holds no composites.**
3. **`outfit-composer.tsx`** — mounts `<OutfitQuickPick>` ABOVE the bundle
   cards and slot rows, wiring `onWear` as
   `onAddToSlot(outfit.types[0]!, outfit.id)`. No new equip path: the
   callback already applies `wearItemIntoSlots` across every slot the item
   covers (the `slot` argument names where the gesture started, not where
   the item lands); additive bundles layer, a `replace` bundle sweeps its
   slots first, either way it dissolves as it lands.
4. **`equipped-slot-row.tsx`** — picker candidates become
   `selectGarments(allItems)` BEFORE the existing slot/equipped/search
   filters, and the now-dead `· composite` suffix is deleted. `allItems`
   still passes whole so equipped composite CHIPS keep their labels.
5. Infra/docs riders (help, CHANGELOG, version bumps) — no port; the help
   rows bank to `p4.9i2`.

Note: the commit prose's "a multi-slot LEAF (a dress typed
`["top","bottom"]`) is not a composite" is not a new rule — it falls out of
`isBundle` testing `componentItemIds`, which v5 already ports.

**v5's measured pre-fix shape** (both confirmed today):
`apps/web/src/app/wardrobe/equipped-slot-row.ts:100` renders the
`' · composite'` suffix; its `candidates` computed (`:144` region) filters
only by slot/equipped/search, so composites are still offered per-slot.
`outfit-composer.ts` renders bundle cards then slot rows (`:25–45`) with
outputs `addToSlot: output<{slot; itemId}>` — the `onAddToSlot` twin the
quick-pick wires into. The composer's ONE live v5 host is
`wardrobe-control-dialog.ts` (both dressing columns).

**The two fixture debts** (carried, phase-4 candidates 3/5 of the
`b220999d` list):

- The armed component-transfer beat and the "Shared — everywhere"
  create-scope beat **self-park** on `hasGeneralStore`
  (`apps/web/e2e/wardrobe-flow.spec.ts:516`, `:652`, probe at `:1050`)
  because the committed e2e fixture has no shared General container to move
  into. The e2e instance is copied by `apps/web/e2e/global-setup.ts` from
  `crates/quilltap-web/tests/fixtures/salon-main.db` + `salon-mount.db`,
  built by `harness/oracle/fixtures/build-salon-fixture.ts`. ⚠ Store-overlay
  properties **cannot be SQL-seeded** (memory
  `store-overlay-properties-cannot-be-sql-seeded`) — widen through the
  builder's engine/API path, then regenerate the committed pair from the
  **`8872d7efc`** pin and re-run every family and e2e beat that consumes it
  (the P4.D91 lesson: SQL-seeding is invisible; the unification-wire lesson:
  ask what a widening hides, and re-run siblings' differentials against the
  widened fixture).
- **The duplicate "Quilltap General" store collision** (P4.D122's e2e find):
  the committed fixture serves TWO enabled stores with the name; suspected
  cause `crates/quilltap-core/src/services/builtin_mounts.rs` (the
  ensure-or-adopt creating a second row after the boot repair has run). The
  2026-08-26 dogfood pass narrowed it: the REAL instance has exactly one —
  this is a property of the committed fixture/boot interplay, not of
  instances in the wild. Diagnose to a root cause; fix only what the
  evidence supports (a fixture artifact → fix the builder; a real
  ensure-or-adopt idempotence hole → fix core WITH a regression test and
  say so loudly, since that is a behavior change on a boot path).

## Tiered deliverables

**Tier 1 (must land):**

1. `apps/web/src/app/wardrobe/composed-outfits.ts` — the two selectors as a
   character-for-character client twin of v4's module (keep
   `localeCompare`; carry v4's why-comments), plus
   `composed-outfits.spec.ts` transcribing v4's 69-line unit test **1:1**
   (the client-twin parity discipline — every v4 case, same names, same
   expectations).
2. `apps/web/src/app/wardrobe/outfit-quick-pick.ts` — the Angular port of
   the 138-line component (memory `porting-a-react-component-to-angular`;
   the capture-phase Escape must be a real capture listener —
   `document.addEventListener('keydown', h, true)` — not a template
   binding), with specs covering: renders nothing on a composite-free pool;
   title-sorted rows with slot-label joins and the ` · replaces` suffix;
   the empty-filter sentence; outside-click close; Escape closes the menu
   WITHOUT reaching an enclosing dialog handler (assert `stopPropagation`
   observable — a spec-level dialog stub that must NOT close).
3. `outfit-composer.ts` mounts the quick-pick above the bundle cards,
   `(wear)` → `addToSlot.emit({ slot: outfit.types[0]!, itemId: outfit.id })`.
4. `equipped-slot-row.ts` — candidates through `selectGarments` first;
   delete the `' · composite'` suffix; `allItems` still passed whole
   (spec-pin the chip-label survival on an equipped composite).
5. A live e2e beat in `wardrobe-flow.spec.ts`: seed/verify a composite in
   the pool, open the dialog, wear it through the pull-down, assert the
   dissolved components land in the slots AND that the per-slot `+` picker
   no longer lists the composite. Run it against the pre-fix bundle first
   where cheap (the red-first habit).

**Tier 2 (should land):** the salon-fixture General-store widening
(un-parking both wardrobe beats — flip their park probes to real runs and
watch their FIRST live execution; a beat a lane predicted would pass has
failed on first run before) and the duplicate-General-store diagnosis with
whatever fix the evidence supports.

**Tier 3 (explicit deferrals, loud):** the new-chat host (the standing
manual-compose deferral — leave a dated comment on `outfit-selector.ts`'s
existing deferral block naming `aec86a613` so the future lane knows the
pull-down is part of its scope); `help/wardrobe.md` → the `p4.9i2` bank.

## The differential requirement

Every ported unit ships with an equivalence check driving v4's REAL code.
For this client-only commit that means: (a) the 1:1 transcription of v4's
own `composed-outfits.test.ts` (the pure tier for a client twin — the
closest precedent is the P4.D96 vocative client twin's parity spec), and
(b) where the transcription can't see a behavior (the Escape
capture-phase contract, the renders-nothing gate), spec-pin it against the
v4 source with the file:line cited. If you record vectors from v4's real
module instead of (or beyond) transcription, drive them from the
`aec86a613` pin. Mutation-prove the load-bearing pins (revert the
`selectGarments` filter → the slot-picker spec and the e2e beat must
redden; memory `mutation-proof-revert-by-file-backup`).

## Fixtures

Consumes: `crates/quilltap-web/tests/fixtures/salon-{main,mount}.db` (and
may DELIVER a widened pair — regenerated only through
`build-salon-fixture.ts` from the `8872d7efc` pin, with the ledger §5.2
discipline: delete-first, read the builder's exit, grep the result for the
new store). If the pair changes, THIS lane owns re-running every committed
family and e2e spec that reads it and reporting the by-name list in the
lane record.

## Shared contract

None — the four lanes meet nowhere. The §Ownership table is the whole
contract, identical in all four orders.

## Ownership (binding, identical across the four orders)

| Lane | Exclusive ownership |
|---|---|
| **P4.D130** | `apps/web/src/app/wardrobe/**` (new `composed-outfits.*`, new `outfit-quick-pick.*`, `outfit-composer.*`, `equipped-slot-row.*` + specs), `apps/web/e2e/wardrobe-flow.spec.ts`, `apps/web/e2e/global-setup.ts`, `harness/oracle/fixtures/build-salon-fixture.ts`, `crates/quilltap-web/tests/fixtures/salon-*.db`, `crates/quilltap-core/src/services/builtin_mounts.rs`, `screens/new-chat/outfit-selector.ts` (comment only). Bumps: **SPA**; core + harness ONLY if the builtin_mounts fix lands. Sole Playwright runner. |
| **P4.62** | `crates/quilltap-web/src/system_data_routes.rs`, `files_routes.rs`, `llm_logs_routes.rs`; `crates/quilltap-harness/tests/web_edge_body_parse_guard.rs`; the system-data / files / llm-logs routes differential families + their oracle cases; any NEW parser module it creates in `quilltap-core` (own file only). Bumps: **web + harness** (+core if a parser lands there). |
| **P4.63** | `crates/quilltap-harness/tests/{system_import_state.rs, attach_mount_file_equivalence.rs, embedding_blob_binding_guard.rs}`; their oracle cases + `harness/oracle/fixtures/build-attach-file-fixture.ts` + the `attach-file-*` fixtures; `crates/quilltap-core/src/services/cheap_llm_exec.rs` (the one test assert only). Bumps: **harness** (+core for the assert edit). |
| **P4.64** | `crates/quilltap-core/src/services/home.rs` + any profiling scaffolding in its own new files. Bumps: **core** only if a fix lands. |

`docs/CHANGELOG.md`, `docs/developer/porting/status-log.md`: append-only for
everyone. Version conflicts at unification: recount as base + total bumps
(playbook §C).

## Verification gate (before declaring done)

- `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D
  warnings` both plain and with `--features quilltap-core/native-transport`
  (needed even for an SPA-heavy lane if any crate file moved).
- `cargo test --workspace` if any crate changed (full-log + sentinel, never
  a pgrep loop; `run_in_background: true` and wait for the notification).
- SPA: `npm run lint` (the check-qt-classes guard must stay whole — any new
  qt-* class must exist; memory `qt-hover-variant-must-be-hand-written`),
  `npm test`, `npm run build`, and the **full Playwright suite** (not just
  the new beat) — zero unexplained skips; the store-probe park should be
  GONE if tier 2 landed.
- Per-commit rule: `.claude/commands/commit.md`. Lane record appended to
  `status-log.md`; status header here updated at unification, not by the
  lane.
