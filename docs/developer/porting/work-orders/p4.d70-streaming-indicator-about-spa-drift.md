# Work order P4.D70 — the 4.8.1 SPA drift: the standalone streaming indicator + About release-freshness — v4 `03154b72`

**Status:** CLOSED — tier 1 landed whole and UNIFIED on main (2026-08-12,
the `03154b72` 4.8.1-release drift round). Unit 1 (the standalone
indicator, v4's predicate re-expressed over the reducer's batch offsets,
three spec arms mutation-checked both directions) + unit 2 (the five
About feature-list edits verbatim, spec-pinned incl. the
Workspace-before-Aurora placement). Tier 2's conditional e2e gesture was
correctly NOT taken (the mock LLM has no tool-call support — new
LLM-driving infra is forbidden by this order). The lane also MEASURED the
wardrobe `set_all` flake as pre-existing (bisected against main's files,
1-in-3 in isolation — the standing "green in isolation" note was stale);
that flake was closed the same day by the parallel spec-only deflake
(`status-log.md` → "Follow-up — the `wardrobe-flow` `set_all` beat
deflaked", dogfood finding #78 → v4 Bug 61). One banked observation for
whoever ports v4's offset interleaving: v5 gates the whole streaming
bubble on `content || reasoning`, so a turn whose FIRST event is a tool
call shows the large waiting quill instead of v4's bubble+tools; the
ported predicate already handles that case when the gate widens. Lane
records in `status-log.md`.

## Preamble

- **v4 baseline for this round: `03154b72`** ("merge: 4.8.1 back into main",
  v4 main HEAD at planning 2026-08-12, version `4.9.0-dev.0`). This lane
  absorbs the two client-side drift commits: `fed5b5da` "fix(salon): give
  the streaming thinking indicator room above a tool block" and
  `5fdd7bed`'s `AboutView.tsx` half (the release-freshness sweep).
- **⚠ THE v4 CHECKOUT IS ON THE `bugfix` BRANCH, NOT main** (HEAD
  `eafb5e2e`, `4.8.2-bugfix.0`; app/ content-identical to main at
  planning). Read v4 source for this lane from a worktree pinned at
  `03154b72` (or `git show 03154b72:<path>`) — never trust the checkout's
  working tree this round.
- **Drift-check at lane start.** `git log 03154b72..main --oneline` and
  `git log main..bugfix --oneline -- lib/ app/ packages/` in
  `~/source/quilltap-server`. If either moved, **STOP** and report.
- **Sibling lanes this round (disjoint ownership):**
  - **P4.D68** — dbkey one-file + seeding/lock dispositions (server; owns
    the baseline move).
  - **P4.D69** — the CLI completions drift.
  Neither touches `apps/web/**`; this lane touches nothing of theirs.
- **Worktree:** carry out on its own branch per the `carryout` skill; edit
  inside the worktree. SPA worktree node_modules: `npm ci`, never `cp -a`
  (the `spa-worktree-node_modules` memory — NG0203).

## The drift, classified (surveyed FRESH 2026-08-12 at `03154b72`)

1. **`app/salon/[id]/components/StreamingMessage.tsx` (+14/−1).** When the
   trailing text segment is EMPTY and the part before it is a `tools`
   block, the typing indicator lands directly under the tool block — and
   the quill's feather paints a little above its own layout box, so inline
   placement crowds the two together. v4 now computes
   `standaloneIndicator = part.text.length === 0 && parts[idx - 1]?.kind
   === 'tools'` and renders the trailing quill on a line of its own
   (`<div className="mt-3"><QuillAnimation size="sm" className="ml-2
   qt-text-secondary" /></div>` — note `inline-block` dropped, `ml-2`
   kept) instead of the inline form. v4's why-comment is worth carrying
   verbatim.
2. **`app/about/AboutView.tsx` (+8/−4).** The feature list freshened for
   the 4.8.0 release: the Docker bullet gains "with filesystem document
   stores bound through to the container"; a NEW bullet **The Workspace**
   (two-pane shell of kept-alive tabs …) inserted before Aurora; the
   Aurora bullet gains "and archiving into an encrypted bundle you can
   rehydrate later"; the Pascal bullet rewritten ("persistent state across
   four registers … user-authored custom tools with honest server-side
   rolls, outcome tables, side effects, and a visual workbench to build
   them"); the Foundry bullet gains "and the Almanack: a full system
   report on what this instance can actually do". Every named feature is
   PORTED in v5, so the new text is truthful for v5 too.

Both surfaces are ported v5 territory: the trailing-quill site is
`apps/web/src/app/chat/streaming-message.ts` (the "v4
StreamingMessage:122" analog, ~:80–85 at planning — one of the four
QuillAnimation call sites P4.D17 placed), and the About screen is
`apps/web/src/app/screens/about/about-page.ts` (P4.9c vintage; the
About-backdrop NO-PORT of P4.D59 stands and is untouched by this drift).

## Mandate

Re-port both client changes into the Angular SPA with v4's client code as
the oracle (unit-spec pins, per the `impersonation-overlay-spa-gaps`
memory's convention: v4's CLIENT is the SPA's oracle for render-side
drift). **Do NOT touch:** any crate (`crates/**`), the harness, the
dispatch contract, `docs/v4/**` (P4.D68's mirror rider owns docs). Zero
Rust changes — no oracle regen is owed by this lane.

## Survey-verified starting points (2026-08-12)

- `apps/web/src/app/chat/streaming-message.ts` — the trailing indicator at
  the "small quill trails the live prose" site (`<qt-quill-animation
  size="sm" class="inline-block ml-2 qt-text-secondary" />`, ~:85).
  Establish how v5 models v4's `parts` array (text/tools segmentation and
  the `lastTextIdx` trailing-segment rule) before porting the conditional —
  the predicate must express "empty trailing segment whose predecessor is
  a tools part" in v5's own render-item terms, not a transliterated index
  poke.
- `apps/web/src/app/screens/about/about-page.ts` — the feature-list
  bullets (static template text). Check the existing specs and any e2e
  beat asserting bullet text before editing (the About beat landed with
  P4.9c; workspace-hosted variants exist).
- v4 reference at the pin: `git show 03154b72:'app/salon/[id]/components/StreamingMessage.tsx'`
  and `git show 03154b72:app/about/AboutView.tsx`.

## Deliverables

### Tier 1 — must land

1. **The standalone-indicator port.** The trailing quill renders on its
   own line (v5's analog of `div.mt-3`, `ml-2` kept, inline form dropped)
   exactly when the trailing text is empty and the preceding rendered part
   is a tools block; the inline form is unchanged everywhere else. Carry
   v4's why-comment. **Spec pins, both arms:** (a) prose present → inline
   indicator (the existing behavior, pinned so the port can't overreach);
   (b) empty trailing segment after a tool block → the standalone wrapper
   with the spacing class. Mutation-check: with the conditional inverted,
   (a) or (b) must fail.
2. **The About freshness mirror.** All five bullet edits, v4 text
   verbatim (entities adapted to Angular template syntax the way the
   existing port already does). Update any spec/e2e assertion that pinned
   the old sentences.

### Tier 2 — should land

- If the salon walk has a beat that streams a tool block (the P4.17 tool
  card beats), eyeball whether the standalone arm is reachable in e2e;
  add a gesture only if one already-live beat reaches it cheaply. Do NOT
  build new LLM-driving infrastructure for this — the unit specs are the
  proof (no-provider e2e host precedent).

### Tier 3 — explicit deferrals (loud, named)

- None. v4's version string (4.9.0-dev.0) is NOT mirrored — v5's About
  reports v5's own identity (the P4.9c convention); verify unchanged, do
  not "fix".

## The differential requirement, restated

Render-side client drift: the oracle is **v4's client source at the pin**,
and the proof is spec-level DOM pins (the `salon-turn-serverside-vs-v4-clientside`
/ `impersonation-overlay-spa-gaps` precedents). No server oracle families
are touched; no NDJSON regen is owed.

## Fixtures

None consumed or changed.

## Shared contract

None — no cross-lane types or assets. `docs/CHANGELOG.md` and
`status-log.md` append-only.

## Ownership

- **This lane owns:** `apps/web/**`.
- **Version bumps:** this lane bumps the SPA version
  (`apps/web/package.json`) only.

## Verification gate (before declaring done)

- `ng test` (full, headless) and `ng build` clean.
- **Full Playwright, zero skips** — this lane touches `apps/web`, so the
  round's SPA gate obligation lands here; read the
  `e2e-playwright-traps` memory before diagnosing any red.
- No cargo gate owed beyond confirming no crate was touched.
- Per-commit rule: `.claude/commands/commit.md`. Background long jobs with
  `run_in_background` — never a pgrep poll loop.
