# Work order P4.d8 — the `616930db` llm-consult drift re-port, server half (the `llm` block, the consult seam, contains/ncontains, the CUSTOM_TOOL_CONSULT log type)

> **STATUS: CLOSED — UNIFIED on main (2026-07-18).** All tier-1 units
> (1–7) and both tier-2 items landed (6 commits); every tier-3
> disposition discharged (the `979aec66` announcer BANK recorded; the
> D23 re-dump verified ZERO-DIFF). The lane's 14 differentials green by
> name over fresh `616930db` oracles at lane close AND re-verified at
> unification (§C corpus: 159 rows = 10 title + 149 definition,
> 53 accept / 96 reject — the SPA constants folded at unify).
> **STANDING DEFERRAL (loud, named — the next order's natural first
> item): the consult is DARK end to end.** All three production
> entrances (the `run_custom` executor, the composer `chatCustomToolRun`,
> the bench `{live:true}` preview) pass `None` for the provider —
> nothing at the dispatch layer holds a `CompletionProvider` (the trait
> is not dyn-compatible; wiring it needs an erased adapter threaded
> through `EngineAssembly` in `quilltap-host`) — and unit 3's 60 s
> timeout decorator is unwired with it. Until then a real `llm` tool
> always fails soft into the author's `errorMessage` (reason
> `no LLM invoker was available in this context`); every SCRIPTED path
> is live and differential-checked. Full record: the status log
> ("P4.d8 — the `616930db` llm-consult drift re-port" + the round
> record).
>
> Lane D8 of the THREE-lane `616930db` drift-catch-up + P4.9a-resume
> round (planned 2026-07-18 — triggered by the drift the M6 items 1–4
> unification flagged: v4 moved three commits past `d68638b4` during
> that round and is now CLEAN at `616930db`). **v4 reference baseline
> for this round: `616930db` (4.8.0-dev.75).** At lane start, run in
> `~/source/quilltap-server`:
>
> ```bash
> git log 616930db..HEAD --oneline
> ```
>
> **If HEAD moved, STOP and report.** Every oracle in this lane
> generates at `616930db`. The v4 tree is clean at planning time, so
> oracles may run from the checkout directly; if it goes dirty
> mid-round, regenerate from a PINNED detached worktree at `616930db`
> (the `oracle-regen-pinned-v4-worktree` memory — symlink BOTH
> `node_modules`).
>
> Sibling lanes: `p4.6bc-workbench-llm-spa.md` (lane BC — `apps/web/`
> only, disjoint from you except the §C corpus wire and the §A/§B
> payload pins), and the RESUMED `p4.9a-photos-view.md` (lane A —
> photos surfaces, fully disjoint from the Pascal family; you share
> only the round's KNOWN Rust conflict set, see Ownership). The
> **Shared contract** and **Ownership** sections below are binding and
> reproduced verbatim in `p4.6bc-workbench-llm-spa.md` (P4.9a carries
> them by reference in its resume addendum). You run in a worktree; a
> unification pass integrates the lanes onto main.
>
> **This lane IS a differential port.** Every unit arrives with an
> equivalence test against v4's real code at `616930db`. The facts
> under "Survey-verified starting points" are from a **2026-07-18
> survey at `616930db` (v4) / main `0c6c79ae` (v5)** — if a unit's
> fresh survey contradicts this order, **STOP and report** before
> proceeding.

## The drift, classified (the disposition of record)

Three v4 commits between `d68638b4` and `616930db`:

| Commit | What | Disposition |
|---|---|---|
| `a2d9a3c8` | custom tools can consult an LLM for a generated result | **RE-PORT** (this lane + lane BC) |
| `616930db` | contains/ncontains substring comparators in outcome tests | **RE-PORT** (this lane + lane BC) |
| `979aec66` | Pascal is selectable in the Insert Announcement dialog | **NO PORT NOW** — the ad-hoc announcer surface (`lib/services/announcer/writer.ts`, the `announcement`/`announcement-preview` actions, the dialog) is UNPORTED in v5 (verified 2026-07-18: no announcer service, no dispatch verb, no SPA dialog). BANK IT: when the Insert-Announcement slice is ordered, its staff roster includes `pascal` and the staffId enum carries it. Record the bank in the status log at lane close. |

Also dispositioned, no action required: the `help/*.md` edits (v5 loads
help from `<cwd>/help` at runtime — `quilltap-host/src/files_store.rs:230`
— and commits no copy); `docs/developer/DDL.md` (the pascalMeta COMMENT
changed, but `generateDDL` output is bare DDL — **no D23 re-dump is
needed**; tier 2 verifies this with a zero-diff re-dump);
`public/schemas/qtap-export.schema.json` (a published documentation
artifact; v5 ships no export schema asset — record the documented
absence). The two schema mirrors that DO matter are lane BC's
(`qtap-custom-tool.schema.json` byte asset) and this lane's row/event
schemas.

## Mandate

Re-port v4 `a2d9a3c8` + `616930db` across the already-ported v5 Pascal
server surface: the `llm` block in the definition schema, the
contains/ncontains comparators (with their three deliberately different
per-subject semantics), the async LLM-consult seam through
`execute_custom_tool` and every entrance (LLM tool handler, operator
chat run, workbench preview), the new `quilltap-core::pascal::llm_consult`
invoker module over the cheap-LLM pipeline (with the CUSTOM_TOOL_CONSULT
log type), the `pascalMeta.llm` record end-to-end, the workbench
preview/audit scripted-oracle params, and the extended/regenerated
differential suite — with the round's new committed oracle artifacts.
This lane owns the Pascal Rust family and its harness; it must NOT touch
`apps/web/**` EXCEPT the §C corpus NDJSON fixture file it owns, nor any
photos/profile file (lane A's), nor `shell.ts`. User-facing strings
(schema `describe` texts, error messages, the tool preamble) are v4's
verbatim — they are wire payload, not prose to improve.

## Survey-verified starting points (2026-07-18: v4 @ `616930db`, v5 @ main `0c6c79ae` — do not re-derive)

### v4 — the new behavior (file:line at `616930db`)

- **`lib/pascal/llm-consult.ts`** (NEW, 164 lines). Exports:
  `CONSULT_TIMEOUT_MS = 60_000` (:42); `CustomToolConsultContext
  { userId; chatId?: string | null }` (:45-53 — `chatId: null` is the
  Workbench: "bench runs belong to no room and are never rerouted");
  `consultMaxTokens(maxOutputChars) = min(max(ceil(chars/3), 2048),
  32_768)` (:63-65); `buildCustomToolLlmInvoker(context)` (:156-164) —
  returns `async (prompt, options)` wrapping the private `consult` in
  `withTimeout(…, CONSULT_TIMEOUT_MS)` (timeout message: `` `the consult
  timed out after ${Math.round(ms/1000)}s` ``, :67-84), converting any
  throw to `{ok:false, reason: getErrorMessage(e)}`. Each invocation
  resolves settings/profiles FRESH. `consult` (:86-149): chatSettings +
  connections in parallel; zero profiles → `{ok:false, reason:'no
  connection profiles are configured'}`; `buildCheapLLMConfig` →
  `getCheapLLMProvider(profiles[0], config, profiles)` (:108-109);
  danger reroute :111-115 (`isChatActiveDangerous(chat)` →
  `resolveUncensoredCheapLLMSelection`; null chat ⇒ never rerouted);
  the call :130-142: `executeCheapLLMTask<string>(selection,
  [{role:'user', content: prompt}], userId, identity-parse,
  'custom-tool-consult', chatId ?? undefined, undefined,
  {dangerSettings, availableProfiles, isDangerousChat}, maxTokens)` —
  **the author's rendered prompt is the entire conversation, no system
  framing** (:132-133). Failures: `task.error ?? 'the model returned
  nothing'` (:144-145). Success: `{ok:true, output: task.result,
  provider: selection.provider, model: selection.modelName}` (:148).
  **The author's `errorMessage` never appears in this module** —
  translating failure into the author's words is the execution core's
  job (:13-16).
- **`lib/pascal/custom-tool.types.ts`**: `MAX_LLM_PROMPT_LENGTH = 4000`
  (:46, measured before templating), `MAX_LLM_OUTPUT_LENGTH = 8000`
  (:54), `MAX_LLM_OUTPUT_CEILING = 100_000` (:62).
  `CustomToolLlmSchema` strictObject (:420-448): `prompt` 1–4000,
  `errorMessage` 1–1000 REQUIRED, `maxOutput` int 1–100000 optional —
  the three `describe` texts are wire payload, carry them verbatim.
  Mounted `llm: CustomToolLlmSchema.optional()` (:495-497); `'llm'`
  joins `KNOWN_TOP_LEVEL_KEYS` (:832). `LlmComparatorSchema`
  (:297-311): NUMERIC shape + eq/neq(AnyOperand) +
  contains/ncontains(StringOperand) + `ok: boolean` optional; refine
  message verbatim: `` must test something: a comparator on the answer,
  or `ok` `` (:310). `WhenObjectSchema.llm` (:359-361); the when-object
  refine accepts `when.llm` and its message now names `llm` (:363-371).
  `COMPARATOR_KEYS` → 8 (:186), `CONTAINMENT_KEYS` (:192),
  `StringOperandSchema` = non-empty string (msg `'the substring to look
  for must not be empty'`) | ParamRef (:214). Containment on
  `ParamComparatorSchema` (:254-262) + `MetadataComparatorSchema`
  (alias :278) + Llm; **NOT on `NumericComparatorSchema`** (bare
  value/roll; :238-240 "a number holds no substrings"); the narrow
  at-least-one message keeps the six-key form (:230), the wide form
  lists all eight (:232-234). Load checks `validateComparator`
  (:706-757): containment subject must be string (msg `` `${key}
  searches ${subjectLabel}, which is a ${subjectType} — only a string
  can contain a substring` ``), operand must resolve string (`` `${key}
  looks for a ${operandType} inside ${subjectLabel}, and a substring
  must be a string` ``); metadata/llm get only the `$param` operand
  check (`validateMetadataOperands` :658-669, called :637/:648).
  llm-without-block rejection (:626-634): `` tests the LLM consult, but
  the tool declares no `llm` block `` at `outcomes.i.when.llm`.
- **`lib/pascal/custom-tools.ts`**: seam types :422-468
  (`LlmInvokeResult`, `LlmInvokeOptions{maxOutputChars}` advisory,
  `LlmInvoker = (prompt, options?) => Promise<LlmInvokeResult>`,
  `LlmConsultResult{ok,output,prompt,reason?,provider?,model?}`,
  `LlmSubject = Pick<…,'ok'|'output'>`). **`executeCustomTool` is now
  async** (:1153-1157), overrides gains `llmInvoke?: LlmInvoker`. Run
  order :1191-1210: params → roll → metadata → **consult** (AFTER the
  roll, BEFORE the table; prompt rendered WITHOUT an `llm` var) →
  subjects (`…(llm ? {llm} : {})`) → findIndex(matchesWhen) → message
  rendered WITH `llm`. `resolveLlmConsult` (:1098-1142) never throws:
  every failure → `{ok:false, output: spec.errorMessage, prompt,
  reason}`; reasons: `'no LLM invoker was available in this context'`
  (:1109), invoker throw → getErrorMessage, `result.reason`, and
  empty-after-trim → `'the model returned an empty answer'` (:1125).
  Success output = `result.output.trim().slice(0, maxOutput).trim()`
  (:1124 — trim, cap, re-trim; cap = `spec.maxOutput ?? 8000`;
  errorMessage NEVER capped). `{{llm}}` in `renderTemplate`
  (:1056-1060): `vars.llm.output`, else leave the placeholder as
  written. `matchesWhen` llm arm (:981-990): no `subjects.llm` →
  fail-soft decline. `matchesLlmComparator` (:888-949) — the forgiving
  reconciliation: `ok` mismatch → false; `answer = output.trim()`;
  `numericAnswer = Number.isFinite(Number(answer)) ? Number(answer) :
  null` (empty string excluded); ordering declines unless numericAnswer
  AND numeric operand; `equalsAnswer`: numeric `===` when both numeric,
  else trimmed lowercase compare forgiving a trailing `.` or `!` on the
  answer (NOTHING else); `containsAnswer`:
  `answer.toLowerCase().includes(String(operand).trim().toLowerCase())`
  — no punctuation forgiveness; `$param` resolution failures still
  THROW (:883-885). Containment per subject: **params strict**
  (`matchesComparator` :721-773; `requireString` THROWS
  `CustomToolRunError` `` `${label} cannot be searched — it is … rather
  than a string` `` :708-715; match = case-sensitive untrimmed
  `includes` :762-770); **metadata fail-soft** (:850-861; absent
  key/non-primitive/non-string decline — **including under ncontains:
  "absence is not a miss"** :851-852; else case-sensitive `includes`).
  `simulateOutcomes` gains a 5th param `llm?: LlmSubject` held FIXED
  across every draw (:1258-1270, folded per draw :1303).
  `CustomToolRunResult.llm?: LlmConsultResult` (:503); result spreads
  `…(llm ? {llm} : {})` (:1233).
- **`lib/pascal/workbench.ts`** (+3): `CustomToolLibraryEntry.llm:
  boolean` (:39-40), mapped `entry.definition.llm !== undefined`
  (:189). Nothing else.
- **`lib/tools/run-custom-tool.ts`**: preamble gains ONE sentence
  (~:88): `'Some tools additionally pose a question to a separate model
  mid-run and let its answer steer the outcome; that consult happens
  server-side too, and you never speak for it.'` `COMPARATOR_SYMBOLS`
  + `['contains','contains']`, `['ncontains','does not contain']`
  (:111-120); `describeWhen` appends `describeLlmComparator` (:157,
  :163-171: `ok` → `'the consult succeeded'`/`'the consult failed'`,
  rest via `describeComparator(c, "the consulted answer")`);
  description line `'    Consults a separate model; outcomes may test
  its answer.'` under `revealOdds` only, prompt deliberately NOT
  rendered (:210-214).
- **`lib/tools/handlers/run-custom-handler.ts`**: `await
  executeCustomTool` with `…(entry.definition.llm ? {llmInvoke:
  buildCustomToolLlmInvoker({userId, chatId})} : {})` (:180-188);
  pascalMeta gains `…(result.llm ? {llm: result.llm} : {})` (:231).
- **Row/event schemas**: `chats-messages.ops.ts` row-parse pascalMeta
  gains BOTH `metadataTested` (:134) and `llm` (:139-146 — the
  six-field object) — **on READ, v4's Zod row parse previously
  STRIPPED unknown pascalMeta keys, so this +14 is load-bearing on the
  read path**; `chat.types.ts` MessageEventSchema pascalMeta gains
  `llm` (:378-385; `metadataTested` :369 pre-existed);
  `llm-log.types.ts` `'CUSTOM_TOOL_CONSULT'` appended after
  `'ANSWER_CONFIRMATION'` (:37);
  `lib/memory/cheap-llm-tasks/core-execution.ts:62`
  `'custom-tool-consult': 'CUSTOM_TOOL_CONSULT'`.
- **`app/api/v1/custom-tools/route.ts`** (+76): `LlmSimulationSchema`
  (:87-92) = `{output: string 1–100000}` | `{fail: true}` (bounded by
  the CEILING — "the bench must be able to script anything a tool
  could"); `PreviewBodySchema.llm: union([{live:true},
  LlmSimulationSchema]).nullish()` (:99); `AuditBodySchema.llm:
  LlmSimulationSchema.nullish()` (:109) — **NO live arm on audit; the
  "audits never call live" rule is a schema shape, not a runtime
  check** (:103-106). `handlePreview` (:171-215): `{live:true}` →
  `buildCustomToolLlmInvoker({userId, chatId: null})`; `{output}` →
  canned `{ok:true, output, provider:'bench', model:'simulated'}`;
  `{fail:true}` → `{ok:false, reason:'a simulated failure, as the
  bench requested'}`; absent → seam unwired (fails soft into
  errorMessage). Preview response = the whole run result, `llm` field
  riding verbatim (:207). `handleAudit` (:217-263): scripted `{output}`
  → fixed subject `{ok:true, output: output.trim().slice(0,
  maxOutput).trim()}` (cap = definition's); anything else (fail/absent)
  → `{ok:false, output: definition.llm.errorMessage}`; 5th arg to
  `simulateOutcomes` (:242-248); audit response shape UNCHANGED.
- **`app/api/v1/chats/[id]/custom-tools/route.ts`** (+7): `handleRun`
  awaits with the invoker (chatId = the chat, :370-376); pascalMeta
  gains `llm` (:431).
- **The v4 test files** (oracle bases; case inventory in the planning
  survey): `custom-tool-definition.test.ts` +28 cases (llm subject 8,
  llm block 5, schema-mirror 2, containment 13);
  `custom-tools-execution.test.ts` ~24+8 genuinely new cases across
  five new describes (+ the sync→async churn);
  `custom-tools-simulate.test.ts` +3 (fixed-consult);
  `tool-draft.test.ts` +12+3 (lane BC's);
  `components/custom-tools/workbench-llm.test.tsx` NEW 9 (lane BC's).

### v5 — the anchors (main `0c6c79ae`)

- **`crates/quilltap-core/src/pascal/custom_tool_types.rs`** (1,870
  lines): `COMPARATOR_KEYS: [&str; 6]` :98; Issue model :103-227
  (union hoist :183); `ParamComparator` :409 (eq/neq AnyOperand),
  `MetadataComparator = ParamComparator` :433, `WhenObject` :447-479,
  `KNOWN_TOP_LEVEL_KEYS: [&str; 10]` :1845; parsers
  `parse_numeric_comparator` :769, `parse_param_comparator` :815,
  `parse_when_object` :1102 (known lists :1168, must-test refine
  :1183); `validate_comparator` :1731, `resolve_operand_type` :1776;
  `format_definition_issues` :1810. Every parser/validator/describe-fn
  iterates `COMPARATOR_KEYS` — the 6→8 widening fans out.
- **`crates/quilltap-core/src/pascal/custom_tools.rs`** (1,073 lines):
  **`execute_custom_tool` :774-879 is SYNC and pure** — the only seam
  is `rng: &mut dyn RandomBytes`. `matches_when` :622-690 (params loop
  :658, metadata :679 → `matches_metadata_comparator` :533);
  `matches_comparator` :445 (ordering arm :456, eq/neq :475 — the
  containment arm goes here + :555/:575); `render_template` :721-768
  (`TEMPLATE_RE` :703); `simulate_outcomes` :913-1007 (same file;
  `CustomToolAuditResult` :894-901); `CustomToolRunResult` :112-130
  (has `metadata_tested`).
- **Workbench verbs**: `crates/quilltap-core/src/api/custom_tools.rs`
  — `AUDIT_RUNS = 10_000` :588, `custom_tool_preview` :772
  (`run_result_to_value` :719 pins v4 field order), `custom_tool_audit`
  :806, `parse_definition` :626, `resolve_metadata` :665 (422 via
  `ErrorKind::Unprocessable`); chat pair `chat_custom_tools_list` :205,
  `chat_custom_tool_run` :344 (pascalMeta assembly :490-536). Request
  types `api/types.rs` :1863-1916 (`CustomToolPreview
  {definition,params,private,metadata}` :1896, `CustomToolAudit`
  :1909); responses :2044-2055. Library DTO
  `pascal/workbench.rs:50` (`CustomToolLibraryEntry`), built :332.
- **`crates/quilltap-core/src/tools/run_custom.rs`** (719 lines):
  `RUN_CUSTOM_PREAMBLE` :96, `COMPARATOR_SYMBOLS` :150,
  `describe_param_comparator` :208, `describe_when` :251,
  `build_run_custom_description` :292, handler
  `execute_run_custom_tool` :433-641 (async; execute call :527,
  pascalMeta inline :560-599 with `metadataTested` :589-595). Executor
  dispatch `tools/executor.rs:478` → `run_run_custom` :552-591. Write
  path `services/pascal_writer.rs` (`post_pascal_result` :126,
  `build_pascal_message` :85).
- **pascalMeta struct**: `db/chats_messages.rs:257-298` `PascalMetaIn`
  — field order = v4 declaration order; `metadata_tested` :293; **the
  `llm` sub-object inserts per v4 `chat.types.ts` declaration order
  (after `metadataTested`, before `invokedBy`) — verify against v4
  :341-388 at port time**. Read side `chats_messages_read.rs` column 44
  (:77,:89), emitted after carinaMeta (:195-197), test DDL :344.
  **Check v5's read-path parse discipline**: v4 Zod-parses pascalMeta
  rows on read (stripping unknown keys); if v5 passes raw JSON through,
  the two now differ on rows carrying keys the schema lacks — the
  differential must cover a stored `llm` round-trip.
- **Cheap-LLM**: `services/cheap_llm_exec.rs` — task types are plain
  `Option<&str>` threaded through `CheapLlmExecutor::execute` :357-368;
  uncensored fallback :72-116. **Log types live in
  `services/llm_logging.rs`**: the `log_type` const module :343-358 (a
  `CUSTOM_TOOL_CONSULT` const joins here) and
  `map_task_type_to_log_type` :362-386 (v4 `core-execution.ts:36-63`
  verbatim; the `'custom-tool-consult'` arm goes here — without it the
  consult silently logs as SUMMARIZATION). `db/llm_logs.rs` stores
  `log_type` as plain TEXT — nothing to extend there.
- **Routes**: `crates/quilltap-web/src/custom_tools_routes.rs` (207
  lines): chat pair :39/:50 (body parse :75-87), workbench
  :134/:155 (`?action=preview|audit`, `parseBody` 400 :169,
  `workbench_unwrap` :114).
- **`fresh_schema.json`**
  (`services/provisioning/fresh_schema.json`): bare DDL, no comments —
  `pascalMeta` appears once (`main[5]`, plain `TEXT`). Unchanged by
  this drift (tier-2 zero-diff verification; recipe:
  `harness/oracle/provision/dump-fresh-schema.ts` :29-33 + the D23
  block at `phase-4.md:282-320`).
- **The harness matrix** (tests in `crates/quilltap-harness/tests/`,
  cases in `harness/oracle/cases/`):
  | differential | env var | oracle case | shared corpus/fixtures |
  |---|---|---|---|
  | `pascal_custom_tool_definition_equivalence` | `QT_ORACLE_PASCAL_DEFINITION` | `pascal-custom-tool-definition.ts` (tsx) | corpus lives IN the case file |
  | `pascal_custom_tools_execution_equivalence` | `QT_ORACLE_PASCAL_EXECUTION` | `pascal-custom-tools-execution.test.ts` (jest mirror) | — |
  | `pascal_run_custom_equivalence` | `QT_ORACLE_PASCAL_RUN_CUSTOM` | `pascal-run-custom.ts` (tsx) | — |
  | `pascal_run_custom_handler_equivalence` | `QT_ORACLE_PASCAL_RUN_CUSTOM_HANDLER` | `pascal-run-custom-handler.test.ts` (jest) | `harness/oracle/fixtures/pascal-run-custom.json` + `quilltap-web/tests/fixtures/pascal-run-custom-{main,mount}.db` |
  | `pascal_simulate_equivalence` | `QT_ORACLE_PASCAL_SIMULATE` | `pascal-simulate.ts` (tsx) | — |
  | `pascal_workbench_equivalence` | `QT_ORACLE_PASCAL_WORKBENCH` | `pascal-workbench.test.ts` (jest) | `harness/oracle/fixtures/workbench.json` + `workbench-{main,mount}.db` |
  | `pascal_workbench_route_equivalence` | `QT_ORACLE_PASCAL_WORKBENCH_ROUTE` | `pascal-workbench-route.test.ts` (jest) | `harness/oracle/fixtures/workbench-route-cases.json` |
  | `pascal_custom_tools_route_equivalence` | `QT_ORACLE_PASCAL_CUSTOM_TOOLS_ROUTE` | `pascal-custom-tools-route.test.ts` (jest) | `pascal-run-custom.json` + its fixture DBs |
  | `pascal_writers_equivalence` | `QT_ORACLE_PASCAL_WRITERS` | `pascal-writers.ts` (tsx) | — |
  | `p4_6ay_workbench_wire_contract` | (pure serde pin, no oracle) | — | — |
- The SPA's committed definition-corpus NDJSON:
  `apps/web/src/testing/fixtures/pascal-custom-tool-definition.oracle.ndjson`
  (115 rows at `d68638b4`; regen recipe in that dir's README). **This
  lane owns regenerating and committing it** (§C).
- Versions on main: core 0.0.271, harness 0.0.239, web 0.0.28, host
  0.0.20, tauri 0.0.4, SPA 0.5.169.

## Tiered deliverables

### Tier 1 — must land

1. **The definition schema** (`custom_tool_types.rs`): the three new
   constants, `CustomToolLlm` strictObject parse (describe texts +
   error messages verbatim — they are wire payload), the `llm` mount +
   `KNOWN_TOP_LEVEL_KEYS`, `LlmComparator` (+`ok`), `StringOperand`,
   `COMPARATOR_KEYS` 6→8 with `CONTAINMENT_KEYS`, the narrow-vs-wide
   at-least-one messages, containment on Param/Metadata/Llm but NOT
   Numeric, the `validate_comparator` containment load-checks, the
   llm-without-block superRefine, `validate_llm_operands` for
   metadata/llm `$param` checks. **Differential**: extend the corpus in
   `harness/oracle/cases/pascal-custom-tool-definition.ts` with rows
   mirroring v4's 28 new definition tests (accept + reject + message
   cases), regenerate at `616930db`, extend the Rust suite to match.
   Grep the NDJSON for a new row before trusting green (the stale-pass
   memory).
2. **The execution core** (`custom_tools.rs`): the consult seam. v4
   made `executeCustomTool` async with an injected `llmInvoke`;
   v5's `execute_custom_tool` is sync/pure. **Frozen = behavior/args,
   not sync-ness** (the W4.5 carina memory): pick the seam shape that
   fits v5 — an injected invoker parameter (boxed async closure making
   the fn async, or a sync `FnMut` bridge) — provided (a) the seam is
   injectable per call site, (b) `simulate_outcomes` stays sync and
   takes the fixed `LlmSubject` 5th arg (it NEVER invokes), (c) the
   run order, `resolveLlmConsult` failure semantics (every reason
   string verbatim; trim-cap-retrim; errorMessage never capped),
   `{{llm}}` rendering, the `matchesWhen` fail-soft llm arm, and
   `matches_llm_comparator`'s reconciliation rules land exactly.
   Containment arms: params strict-throw, metadata fail-soft
   (ncontains: absence is not a miss), llm forgiving. **Differential**:
   mirror v4's five new execution describes + the containment cases
   into the jest-mirror oracle case + the Rust suite (canned invokers
   injected identically both sides); +3 simulate fixed-consult cases.
3. **`pascal/llm_consult.rs`** (new module): `CONSULT_TIMEOUT_MS`,
   `consult_max_tokens` (tier-1 exact differential — it's pure),
   the invoker builder over `CheapLlmExecutor` (fresh per-invocation
   resolution, zero-profiles reason, danger reroute with null-chat
   bench semantics, the `'custom-tool-consult'` task type, prompt as
   the entire conversation, the timeout with v4's message string, the
   `'the model returned nothing'` fallback). Plus
   `llm_logging.rs`: the `CUSTOM_TOOL_CONSULT` log-type const + the
   `map_task_type_to_log_type` arm (extend its existing differential
   if one pins the mapping — check `tool-definitions`/logging suites).
   The invoker's plumbing is proven through the handler differential
   (unit 5) with a canned cheap-LLM mock (recipe pointers:
   `answer-confirmation-differential` — one mock routed by prompt
   kind; `danger-differential-recipes`).
4. **pascalMeta.llm end-to-end**: `PascalMetaIn` gains the typed `llm`
   sub-object at v4's declaration position (verify against
   `chat.types.ts:341-388`); the read path round-trips it; **resolve
   the read-parse discipline** (v4 row-parse strips unknown keys — the
   differential must pin a stored-row read both sides); the three
   writers (run-custom handler :231-equivalent, chat run
   :431-equivalent, preview response) attach `…(llm ? {llm} : {})`.
5. **The run_custom tool + handler**: the preamble sentence, the two
   `COMPARATOR_SYMBOLS` rows, `describe_llm_comparator`, the
   "Consults a separate model" description line (revealOdds-gated,
   prompt never rendered), the handler's conditional invoker wiring +
   pascalMeta.llm. **Differential**: regenerate
   `pascal_run_custom_equivalence` (description/preamble changes) and
   extend `pascal_run_custom_handler_equivalence` with llm-consult
   cases — an llm-block tool in the fixture mount + a canned
   cheap-LLM/provider mock identical both sides (fixture family:
   extend `pascal-run-custom-{main,mount}.db` + regenerate
   `pascal-run-custom.json`; pin ids via `create(data,{id})`).
6. **The workbench + routes**: preview body `llm` union
   (`{live}|{output}|{fail}` nullish), audit body (`{output}|{fail}`
   nullish, NO live arm — keep it structurally impossible), the
   preview invoker mapping with v4's exact bench strings
   (`provider:'bench'`, `model:'simulated'`, `'a simulated failure, as
   the bench requested'`), the audit fixed-subject derivation
   (trim-cap-retrim; else `{ok:false, output: errorMessage}`),
   `CustomToolLibraryEntry.llm: bool`, the chat run's invoker + meta.
   **Differentials**: extend `workbench.json` /
   `workbench-route-cases.json` / the jest oracles with
   preview-scripted, preview-fail, preview-absent (fails soft),
   audit-scripted, audit-fail, audit-absent, and llm-in-library cases;
   regenerate `pascal_workbench_equivalence`,
   `pascal_workbench_route_equivalence`,
   `pascal_custom_tools_route_equivalence`; update
   `p4_6ay_workbench_wire_contract` for the payload additions (§B).
   **Live-arm cases run scripted only** — no differential ever spends
   a real LLM call.
7. **The §C corpus NDJSON**: regenerate
   `apps/web/src/testing/fixtures/pascal-custom-tool-definition.oracle.ndjson`
   from the extended case at `616930db`, commit it + the README
   provenance line, and record the new row counts (total/accept/reject)
   in this order's status header at lane close — lane BC's corpus spec
   constants are updated to those counts at unification (§C).

### Tier 2 — should land

8. **The D23 zero-diff verification**: re-dump `fresh_schema.json`
   from v4 `616930db` and diff — expected ZERO change (the DDL.md edit
   was comment-only). If it differs, STOP: that's an unclassified
   schema drift; report before proceeding.
9. **Trip-check the adjacent families**: run
   `tool_definitions_equivalence` / `tool_build_equivalence` /
   `pascal_build_tools_roster` / `pascal_writers_equivalence` /
   `pascal_roster_equivalence` against fresh `616930db` oracles — the
   preamble/description changes may reach them. Regenerate whichever
   trips; record the ones that don't.

### Tier 3 — explicit deferrals (loud, named)

- **`979aec66` (Pascal in Insert Announcement)**: NO PORT — the
  announcer surface is unported; BANKED (see the disposition table).
- **`qtap-export.schema.json`**: documented absence (v5 ships no
  export schema asset).
- **The Salon bubble display of `pascalMeta.llm`**: v4 renders nothing
  new in the bubble either (Inspector-only) — record as verified
  parity, not a deferral.
- **A live-consult e2e beat**: real provider spend — out of scope for
  the harness; the scripted paths cover the seam.

## The differential requirement, restated

Every Rust unit above ships with an equivalence test driving v4's REAL
code at `616930db` (the oracles import `lib/pascal/*`, the real route
handlers, and the real cheap-LLM pipeline with mocks only at the
provider/transport boundary — never reimplementations). Tiers: exact
for the pure pieces (`consult_max_tokens`, the reconciliation table),
jest-mirror/structural for execution and routes, mocked-LLM for the
consult plumbing. Recipes: `jest-real-db-oracle`,
`answer-confirmation-differential`, `tier3-completion-oracle`,
`oracle-regen-silent-stale-pass`, `p4.6ay-unit12` (shared JSON corpora
both oracle and test read), `oracle-jest-testtimeout`.

## Fixtures

- **Consumes + extends:** `pascal-run-custom-{main,mount}.db` (+ meta)
  and `workbench-{main,mount}.db` (+ meta) — this lane owns their
  generators and regenerates dependent oracles when they change;
  the shared corpora `pascal-run-custom.json`, `workbench.json`,
  `workbench-route-cases.json`.
- **Delivers:** the regenerated SPA corpus NDJSON (§C), the extended
  oracle cases, fresh committed oracle artifacts per the round's
  convention.
- No sibling consumes these fixtures (lane BC reads only the NDJSON,
  via §C at unification; lane A is disjoint).

## Shared contract (BINDING — identical in `p4.6bc-workbench-llm-spa.md`; P4.9a carries §E by reference)

### §A — the `pascalMeta.llm` record

Shape and key order (v4 `chat.types.ts:378-385` / `custom-tools.ts
LlmConsultResult`): `{ ok: boolean, output: string, prompt: string,
reason?: string, provider?: string, model?: string }` — optionals
OMITTED when absent, never null. It sits inside pascalMeta at v4's
declaration position (after `metadataTested`, before `invokedBy`).
`output` is the model's trimmed-capped-retrimmed answer on success, or
the author's `errorMessage` on failure; `reason` is the technical cause
and never appears in the fiction. Lane D8 owns the Rust struct; lane BC
mirrors it as the `PascalMeta.llm` type in
`apps/web/src/app/core/core-contract.ts` (type-only; no new rendering).

### §B — the workbench payload additions

`POST /api/v1/custom-tools?action=preview` body gains
`llm?: {live: true} | {output: string} | {fail: true}` (nullish;
`output` 1–100,000 chars). `?action=audit` body gains
`llm?: {output: string} | {fail: true}` (nullish — **no live arm;
audits never call live, enforced by shape**). The preview RESPONSE is
the whole run result including the §A record (scripted runs carry
`provider: 'bench', model: 'simulated'`); the audit response shape is
unchanged. The library entry gains `llm: boolean`. Lane D8 owns the
server types + `p4_6ay_workbench_wire_contract`; lane BC mirrors the
bodies in `workbench.api.ts` (structural reads as today — no pinned
response variants).

### §C — the definition corpus wire (unifier-applied)

The definition corpus lives in
`harness/oracle/cases/pascal-custom-tool-definition.ts` (lane D8's
file). Lane D8 extends it with the new-feature rows, regenerates, and
COMMITS `apps/web/src/testing/fixtures/pascal-custom-tool-definition.oracle.ndjson`
(+ README provenance) in its own lane, recording the new
total/accept/reject counts in its status header. Lane BC does NOT touch
the NDJSON: in-lane it keeps the 115-row corpus green (the old rows'
behavior is unchanged — a red old row means a BC porting bug) and pins
every new parse arm with its own hand-written specs; its corpus-spec
count constants (`custom-tool-types.corpus.spec.ts:76-78`) carry a
`// §C: unifier updates to D8's recorded counts` marker. At
unification, the unifier sets the constants to D8's recorded counts and
the corpus spec must pass byte-for-byte — a failure there is a BC parser
bug, not a corpus problem.

### §D — no new dispatch verbs

Neither drift lane adds or renames a wire verb. The four §W workbench
verbs and the chat custom-tools pair keep their names; only the §B
payload fields are added. (Lane A's photos verbs are its own order's
§3 contract, unchanged.)

### §E — the round's KNOWN Rust conflict set (with lane A)

Lane D8 and lane A (P4.9a) both touch
`crates/quilltap-core/src/api/types.rs`, `api/engine.rs`, `api/mod.rs`,
and `crates/quilltap-web/src/lib.rs` — D8 with §B payload additions to
EXISTING variants, A with its NEW photos verbs/routes. Additions only;
the unifier reconciles (temp-branch cherry-pick + one full gate — the
parallel-round-reconciliation memory). No other Rust file is shared.
Lane BC and lane A are disjoint in `apps/web/` (BC: pascal/,
screens/custom-tools/, chat/llm-inspector*, core-contract.ts §A region,
settings llm-logs card; A: screens/photos/, images/, gallery-tab,
its §1 route block). `app.routes.ts` is lane A's alone this round.

## Ownership (BINDING — identical in `p4.6bc-workbench-llm-spa.md`)

- **P4.d8 (this lane):** `crates/quilltap-core/src/pascal/**` (incl.
  the new `llm_consult.rs`), `crates/quilltap-core/src/tools/run_custom.rs`,
  `crates/quilltap-core/src/api/custom_tools.rs`,
  `crates/quilltap-core/src/db/chats_messages.rs` +
  `chats_messages_read.rs` (the pascalMeta regions),
  `crates/quilltap-core/src/services/llm_logging.rs` +
  `cheap_llm_exec.rs` (additive), `services/pascal_writer.rs`,
  `crates/quilltap-web/src/custom_tools_routes.rs`; the §E conflict-set
  files (additive); harness: all ten pascal differentials + their
  oracle cases + the shared corpora + both pascal fixture families;
  `apps/web/src/testing/fixtures/pascal-custom-tool-definition.oracle.ndjson`
  + that dir's README (§C — its ONLY `apps/web` files). Version bumps:
  quilltap-core, quilltap-harness, quilltap-web (+Cargo.lock).
- **P4.6bc:** `apps/web/src/app/pascal/**` (custom-tool-types.ts,
  tool-draft.ts + specs, corpus spec — NOT the NDJSON),
  `apps/web/src/app/screens/custom-tools/**`,
  `apps/web/src/app/chat/llm-inspector-entry.ts` +
  `llm-inspector-panel.ts` (+ specs),
  `apps/web/src/app/core/core-contract.ts` (the §A PascalMeta region —
  exclusively this round), the settings llm-logs card file,
  `apps/web/public/schemas/qtap-custom-tool.schema.json` (byte-copy),
  its own e2e spec file. Version bump: SPA `package.json`.
- **P4.9a (lane A):** per its own order's Ownership section (photos
  Rust + SPA surfaces; §E conflict set additive; its §1 route block;
  bumps core/harness/web/SPA). Its resume addendum carries the round's
  new baseline and sibling list.
- **Append-only for everyone:** `docs/CHANGELOG.md`,
  `docs/developer/porting/status-log.md` (rebuild unions from lane
  appends at unification — the M6-round memory). Version bumps union
  at unify; the unifier RECOUNTS (identical bumps merge silently).
- **Nobody** edits `CLAUDE.md`, `phase-4.md`, `m6-screen-parity.md`, or
  a sibling lane's files; the unifier updates the phase docs and moves
  the oracle-baseline paragraph to `616930db`.

## Verification gate (run before declaring done)

- `cargo fmt --all --check`; `cargo clippy --workspace --all-targets`
  clean on both feature sets; `cargo test --workspace --no-fail-fast`
  zero failures; this lane's differentials run BY NAME with the oracle
  env vars set, `--nocapture`, output grepped for SKIP (the P4.d3
  memory) — all oracles freshly regenerated at `616930db`.
- The tier-2 trip-check results recorded (which families regenerated,
  which proved untouched).
- No SPA build/test obligations beyond `ng test` on the corpus spec
  still passing against the OLD committed NDJSON is NOT required here
  — the NDJSON this lane commits is consumed at unification (§C);
  do not edit BC's spec.
- Every commit follows `.claude/commands/commit.md` (version bumps per
  Ownership; CHANGELOG entry; status-log append at lane close,
  including the §C counts and the tier-3 dispositions/bank).
