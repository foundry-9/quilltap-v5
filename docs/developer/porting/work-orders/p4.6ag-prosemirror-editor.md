# Work order P4.6ag — the D17 ProseMirror editor decision lane (SPA-only)

> **STATUS: CLOSED (D17 DECIDED — ProseMirror ADOPTED, gate GREEN).**
> The committed byte-round-trip gate ran GREEN, so `qt-rich-editor`
> (ProseMirror over the v4-dialect markdown bridge) shipped and was
> adopted in the Document Mode pane (markdown files) and the chat
> composer. Tiers 0–2 landed (gate; editor + handle; both adoptions;
> input rules + formatting commands; live e2e beats). Deferred loud:
> inline emphasis-on-type input rules, the multiline table transformer,
> strikethrough/highlight marks, the form-field consumers,
> TextReplacementPlugin, and draft persistence (see the status-log round
> record). UNIFIED on main 2026-07-14 (with the P4.6ae partial + P4.6af
> lanes); the phase-4.md D17 note is updated in the same unification
> doc pass.
>
> Lane C of the FOUR-lane P4.6ae ∥ P4.6af ∥ P4.6ag ∥ P4.d3 round (the
> files-family + editor round, drift-amended 2026-07-13). **v4
> reference baseline: HEAD `dd0d9ff5`** (`~/source/quilltap-server`,
> v4 `4.8.0-dev.52`) — run `git log dd0d9ff5..HEAD --oneline` at lane
> start; **if HEAD moved, STOP and report** (classify drift, don't
> plan around it). Sibling lanes: `p4.6ae-files-family-server.md`,
> `p4.6af-files-family-spa.md`, and
> `p4.d3-db-size-reduction-drift.md` (the `dd0d9ff5` drift re-port).
> The **Shared contract** and **Ownership** sections below are
> binding and reproduced verbatim in all four orders. You run in a
> worktree; the unification pass integrates the lanes onto main.
>
> **Drift note (2026-07-13):** the round was first planned at
> `6a8a77aa`; v4 then moved one commit to `dd0d9ff5` (db-size
> reduction — embeddings/maintenance/settings). That drift is lane
> D's re-port and **touches NONE of this lane's surface** (no
> composer, editor, or Document Mode files in the diff — the v4
> dialect facts below are unchanged at `dd0d9ff5`).
>
> **This is a DECISION lane (the D18 mold), not an unconditional
> adoption lane.** D17 asked which rich editor replaces v4's Lexical
> stack in v5. The Lexical spike ran RED (P4.6x, commit `7b0238f` —
> docs+lockfile only; the spike test was NEVER committed, there is no
> harness to inherit). ProseMirror is the named next candidate. The
> make-or-break gate is a **committed byte-round-trip markdown test**
> in v4's composer dialect. If the gate runs RED after honest
> configuration effort, REJECTION IS A VALID DELIVERABLE: record the
> decision (the ngx-explorer precedent — GREEN checks, adoption
> rejected, reasons named), keep the textarea, close D17 with a
> named next candidate or a stand-pat verdict. Do not ship a
> lossy editor to force a GREEN.

## Mandate

Settle D17: build the committed round-trip gate first; if GREEN,
ship the bespoke `qt-rich-editor` (ProseMirror over a v4-dialect
markdown bridge) and adopt it in the two sanctioned surfaces — the
Document Mode pane (markdown files ONLY, v4 parity) and the chat
composer — preserving every existing behavior contract (send
payload, save path, mtime conflict flow, paste-image, focusout
flush). If RED, deliver the decision record and leave both textareas
untouched.

Do NOT touch `crates/**` (lane A), `core/core-contract.ts` /
`core-client.ts`, `screens/**`, `chat/conversation-header.ts` (lane
B), or `chat/render/**` (round-wide HANDS OFF — the pinned
unified-11.0.5 render pipeline). No new dispatch variants: document
writes ride the existing `chatDocumentRead`/`chatDocumentWrite`; the
composer emits the same `ComposerSend`.

## Survey-verified starting points (2026-07-13 @ `6a8a77aa`, re-validated @ `dd0d9ff5` — do not re-derive)

### The v4 dialect (what "byte-faithful" means — the gate's spec)

- **The serializer is `MarkdownBridgePlugin`**
  (`components/chat/lexical/plugins/MarkdownBridgePlugin.tsx`, 269
  lines):
  - `COMPOSER_TRANSFORMERS` (:67-87) **excludes `ITALIC_STAR` and
    `BOLD_ITALIC_STAR`**, includes `ITALIC_UNDERSCORE` +
    `BOLD_STAR` + `BOLD_UNDERSCORE`. Italic is `_text_`. The
    *why*-comment (:60-66): single `*` is a roleplay narration
    delimiter and must survive as literal text. This matches
    `lib/chat/annotations.ts:44` (`MARKDOWN_FORMATS` italic `_`/`_`).
  - `stripMarkdownEscapes(markdown, chars)` (:128-137) strips
    Lexical's auto-escapes on export (regex `\\([*_\`~])` → `$1`);
    `DEFAULT_PRESERVED_MARKDOWN_CHARS = ['*','_','\`','~']` (:120);
    the four `preserve*` props default true (:143-151).
  - Export: `$convertToMarkdownString(COMPOSER_TRANSFORMERS,
    undefined, true)` → `normalizeExportMarkdown`.
- The other v4 plugins (for scoping the port): `ComposerSyncPlugin`
  (143 — decoupled hasContent/draft-debounce; the editor owns live
  text, send reads the handle), `KeyboardPlugin` (153 —
  Enter=submit / Shift+Enter=newline chat mode; Cmd/Ctrl+Enter
  document mode; code-block escape on blank trailing line),
  `ImagePastePlugin` (80 — clipboard image →
  `pasted-image-<ts>.<ext>` → `onImagePaste`),
  `FormattingCommandPlugin` (236 — bold/italic/headings/lists/
  blockquote/code commands + **roleplay delimiters inserted as
  literal text**), `ExternalControlPlugin` (134 — imperative
  focus/scrollIntoView/prependText), `TextReplacementPlugin` (179 —
  word-boundary autocorrect), the custom multiline
  `table-transformer` (`transformers/table-transformer.ts`, 258).
  Stock: RichText, History (undo), List, CheckList,
  MarkdownShortcut (driven by COMPOSER_TRANSFORMERS), placeholder
  via `aria-placeholder` + a `qt-lexical-placeholder` div. **No
  mentions plugin, no slash-command plugin exist** — don't invent
  them.
- **v4 is already selective about byte fidelity** (load-bearing for
  scope): `DocumentPane.tsx:92-97` — only `.md`/`.markdown` get the
  rich editor; **everything else is a plain textarea** (:722-740,
  the *why*-comment: bytes must not round-trip through a markdown
  serializer). Markdown files keep a raw-source textarea toggle
  (`showSource`, :681-692) and frontmatter is split off read-only —
  only the BODY is edited (:482-488). v5 must keep ALL THREE
  concessions.
- v4 also ships `components/markdown-editor/MarkdownLexicalEditor.
  tsx` (242 — the form-field variant used by character fields /
  scenarios / settings). Its v5 consumers are a TIER-3 deferral —
  the memory editor's doc comment (:27-28) already names this lane.

### v5 side — what already exists (the adoption seams)

- **Document pane:** `documents/document-pane.ts` (280,
  `qt-document-pane`) — textarea for every type (:142-150);
  markdown frontmatter split + body-only editing recombining
  `${rawBlock}${body}` (:233-240); **`focusout` not `blur`**
  (:137-140 — the P4.6x standing rule); the D17-RED docblock
  (:24-38).
- **Save path:** `documents/document-mode.ts` —
  `USES_RICH_MARKDOWN_EDITOR = false` (:54); `computeAbsorbNext`
  gated on it (:76 — the absorb-first-serialization baseline goes
  ACTIVE when flipped: verify its behavior with a spec);
  `saveDocument` (:282-348) — mtime rides the write, `conflict` →
  re-read/adopt-or-refresh (:306-330). Dispatch:
  `document-api.ts:170-200` (`chatDocumentWrite` + `mtime` +
  `diffContent`; `WriteDocumentOutcome` :40-43). None of this
  changes — the editor slots ABOVE it.
- **Composer:** `chat/chat-composer.ts` (339, `qt-chat-composer`) —
  textarea (:76-86); Enter/Shift+Enter (:238-243); `submit()` emits
  `ComposerSend {content, fileIds}` (:250-260); attachment chips
  (:41-68); paste-image upload via the chat-files leg (:264-309) +
  `FileConflictDialog`. The doc comment (:26-34) names itself the
  sanctioned MVP substitute. The swap is INTERNAL to this component
  — same inputs/outputs, `salon-conversation.ts` untouched (lane
  B's file).
- **No shared editor abstraction exists** — this lane creates the
  first (`apps/web/src/app/editor/**`, e.g. `qt-rich-editor`).
  House idiom: OnPush standalone, signal `input`/`output`,
  imperative ProseMirror inside (zoneless-safe — PM is
  framework-agnostic and needs no zone patching).
- **Packaging:** no `prosemirror*` deps exist. Needed:
  `prosemirror-model/state/view/markdown/commands/keymap/history/
  inputrules` (+ `prosemirror-tables` ONLY if the tier-2 table scope
  lands). **STATIC imports inside the lazily-loaded chunk** — the
  P4.6u xterm lesson (`[[p4.6u-salon-terminal-pane]]`). Lane C is
  the round's sole `package.json`/`package-lock.json` dependency
  owner.
- **The known serializer work** (why the gate is genuinely at risk):
  `prosemirror-markdown` defaults serialize `em` as `*…*` (must
  override to `_`) and its `esc()` backslash-escapes
  markdown-significant chars in text nodes — the exact lossiness v4
  neutralizes with `stripMarkdownEscapes` + the `preserve*` flags.
  Both are overridable (per-mark `open`/`close`; custom escape /
  post-pass), but that IS the port of v4's preservation bridge —
  budget for it; don't declare RED before attempting it, don't
  declare GREEN without the committed test.

### Quirks the port must not miss

1. The dialect is the CONTRACT: `_italic_`, `**bold**`, literal
   `*`/`_`/`` ` ``/`~` unescaped, v4's transformer scope
   (headings, lists, checklists, blockquote, code fences; tables via
   the custom transformer are tier 2).
2. Non-markdown files NEVER touch the rich editor (v4 parity — the
   byte-fidelity concession is v4's own).
3. Frontmatter stays split; only the body is edited; recombine
   exactly `${rawBlock}${body}`.
4. The raw-source toggle (v4 `showSource`) ships WITH the adoption,
   not after — it's the user's escape hatch when the dialect
   surprises them.
5. Send reads markdown from the editor handle at submit time (v4's
   decoupled ComposerSyncPlugin posture) — no per-keystroke
   serialization; `hasContent` gating only.
6. Roleplay delimiters insert as LITERAL text (v4
   FormattingCommandPlugin) — the editor must not smart-format a
   user-typed `*narration*` into emphasis on input either
   (inputrules must respect the excluded ITALIC_STAR).
7. Paste-image, Enter-vs-Shift+Enter, and the focusout flush are
   behavior contracts with existing specs — they keep passing
   unmodified or the specs are updated in the same commit with the
   reason recorded.
8. `computeAbsorbNext` goes live when `USES_RICH_MARKDOWN_EDITOR`
   flips — the first save after opening a doc may legitimately
   absorb a serializer-normalization diff ONCE; spec that behavior
   (that's what the flag was built for), and byte-noise beyond the
   first absorb is a gate FAILURE, not something to absorb
   repeatedly.

## Tiered deliverables

**Tier 0 — the gate (must land FIRST, committed either way):**

1. `apps/web/src/app/editor/markdown-round-trip.spec.ts` (name
   indicative): the PM schema + parser + serializer configured for
   the v4 dialect, driven over a fixed corpus asserting
   **`serialize(parse(x)) === x` byte-equality**: underscore-italic;
   bold; bold-underscore; literal `*` narration lines; literal
   `_`/`` ` ``/`~` in prose; backslash-escape preservation;
   headings; ordered/unordered/check lists; blockquote; fenced code
   (with language + blank trailing line); mixed nesting; a
   frontmatter-free chapter-like body. Corpus entries mirror v4
   `COMPOSER_TRANSFORMERS` scope. **GREEN → proceed to tier 1. RED
   after honest serializer-bridge effort → STOP adoption, write the
   decision record (status log + this order's header + phase-4.md
   D17 note at unification), and end the lane** — the committed RED
   spec (skipped-with-reason or inverted into a
   documents-the-divergence spec) plus the record IS the
   deliverable.

**Tier 1 (must land if the gate is GREEN):**

2. `qt-rich-editor` (`apps/web/src/app/editor/**`): PM view hosting
   with markdown `value` in / markdown out via an imperative handle
   (get/set/focus/prepend — v4's `ComposerEditorHandle` shape),
   history, base keymap, placeholder, the dialect
   parser/serializer bridge (the v4 preservation semantics), static
   imports, zoneless-safe.
3. Document-pane adoption: markdown files only; frontmatter split
   preserved; the raw-source toggle; `USES_RICH_MARKDOWN_EDITOR` →
   true with the absorb-first-serialization spec; the save/conflict
   path untouched and its specs green.
4. Composer adoption: chat-mode keys (Enter/Shift+Enter),
   send-reads-handle, paste-image preserved, `hasContent` enable
   gating, roleplay-literal input behavior; `ComposerSend`
   unchanged.

**Tier 2 (should land):**

5. Markdown input shortcuts (inputrules) within the dialect scope;
   the formatting command set (bold/italic/headings/lists/
   blockquote/code) as commands + keybindings (v4
   FormattingCommandPlugin semantics — a toolbar is NOT required,
   v5 has none today).
6. The table scope (the v4 custom table transformer's round-trip;
   `prosemirror-tables` only if this lands).
7. e2e: extend `salon-documents-flow.spec.ts` with a rich-edit beat
   (open a markdown doc, type styled content, save, re-read via
   dispatch, assert the dialect bytes) + a composer beat (type
   `_italic_` + a literal-`*` narration line, send, assert the
   message content bytes).

**Tier 3 (explicit deferrals — loud, enumerated):**

- The form-field consumers (memory editor, scenario editor,
  character fields — v4 `MarkdownLexicalEditor` sites): a follow-on
  order once the editor is proven in the two sanctioned surfaces;
  each site keeps its textarea + its existing doc-comment marker.
- `TextReplacementPlugin` (the autocorrect layer) — named, its own
  small unit later.
- Draft persistence (v4 `onPersistDraft` debounce) — v5's composer
  has no draft store yet; defer with the plugin inventory note.
- Document Mode for the STANDALONE editor surfaces beyond the pane
  — nothing changes there this round.

## The differential requirement (restated)

The gate spec IS this lane's differential: byte-equality over the
committed corpus, in-repo, runnable by `ng test` forever (the
Lexical spike's fatal flaw was leaving no committed harness — do
not repeat it). Adoption units prove by unit specs (the handle
contract, the absorb-once behavior, keyboard/paste contracts — the
existing `chat-composer.spec.ts` and `document-pane.spec.ts` suites
keep passing, updated only with recorded reasons) + the live e2e
beats. No oracle run is needed (no server behavior changes), but
the DIALECT is v4's — every corpus entry should trace to a v4
transformer or preserve-flag, cited in a comment. Fix the port, not
the spec.

## Fixtures

None (SPA-only). The e2e beats drive live documents over the
existing shared-instance mounts seeded by global-setup; create any
needed markdown file via dispatch in the spec (after unlock) and
clean up in afterAll. The round-trip corpus lives IN the spec file
(or a sibling `.ts` corpus module), not in a DB fixture.

## Shared contract (BINDING — identical in p4.6ae / p4.6af / p4.6ag / p4.d3)

### New Request variants (wire `type` names; Rust mirrors in PascalCase; lane A builds the files family, lane B consumes it; lane D builds the retention pair)

The general files family (all JSON verbs ride `POST /api/dispatch`):

- `filesList {projectId?, folderPath?, filter?}` → `{files:
  FileEntry[]}`. `FileEntry` is v4 `serializeFileEntry`: BOTH
  `originalFilename` AND `filename` (= originalFilename), `filepath`
  = `/api/v1/files/{id}`, `folderPath` RESOLVED via
  `resolveEffectiveFolderPath`, `fileStatus` defaulted `'ok'`. Sort:
  `createdAt` DESC.
- `fileMove {fileId, folderPath?, filename?, projectId?}` → `{data:
  ManagedFile}` — the managed shape (`filename` only, RAW
  `folderPath`, no `originalFilename`/`width`/`height`/`fileStatus`).
  At least one field required; `projectId` absent keeps, null clears
  (the tri-state — do not collapse).
- `filePromote {fileId, targetProjectId?, folderPath?}` → `{data:
  ManagedFile}`.
- `fileDelete {fileId, force?, dissociate?}` → `{success: true}` |
  the 400 associations envelope (`'File is linked to other items'`,
  code `FILE_HAS_ASSOCIATIONS`, `associations {characters,
  messages}`).
- `filesGenerateThumbnails {fileIds, size?}` → `{total, generated,
  cached, errors}`.
- `filesCleanupStale {dryRun?}` → v4's stale-cleanup shape.
- `filesCleanupOrphans {mode, dryRun?}` → v4's dry/wet shapes.
- `filesFoldersList {projectId?}` → `{folders: Folder[], count}`
  (sort: `path` localeCompare ASC).
- `filesFolderCreate {path, projectId?}` → `{folder, alreadyExists,
  message}` (idempotent; auto-creates the parent chain).
- `filesFolderRename {path, newName, projectId?}` → `{oldPath,
  newPath, foldersUpdated, filesUpdated}`.
- `filesFolderDelete {path, projectId?}` → `{message, path}`; refuses
  non-empty (two DISTINCT 400 messages: files vs subfolders).
- `filesSync {}` → STAYS refusal-armed (loud, names the unported
  `lib/file-storage/reconciliation` subsystem).
- `fileUpload {filename, contentType, data (base64), tags?,
  projectId?, folderPath?}` — the core variant behind the REST leg
  below.

The data-retention pair (lane D builds and consumes; listed here so
names never collide):

- `dataRetentionSettings {}` → `{staleChatDays}` (v4 defaults:
  staleChatDays 30, bounds 1..3650).
- `dataRetentionSettingsUpdate {staleChatDays?}` → the parsed echo
  (v4 merges over current, validates, `validationError` on fail).

REST legs (web edge, lane A):

- `POST /api/v1/files?action=upload` multipart (fields `file`, `tags?`
  JSON string, `projectId?`, `folderPath?`) → `{data: FileEntry}`,
  status 201 create / 200 overwrite.
- `POST /api/v1/chats/{id}/files` multipart (fields `file`,
  `resolution?`, `conflictingFileId?`) → `{file}` | `{duplicate,
  conflictType, existingFile, newFile}` | `{error}`. **The SPA client
  is ALREADY LOCKED** (`chat-files.api.ts:42-81`, shipped in P4.6ac)
  — the server matches it, never vice versa.
- The byte GETs (`/api/v1/files/{id}`, `?action=thumbnail`, the
  proxy) are FROZEN — no lane touches them.

Existing variants going LIVE (lane A):

- `chatFileUpload {chatId, filename, contentType, data (base64),
  resolution?, conflictingFileId?}` — the refusal body at
  `chat_media.rs:799-809` is replaced; the engine arm already exists.
- `imageProfileGenerate {imageProfileId, prompt, chatId?, count?}` →
  `{success: true, data: GeneratedImage[], expandedPrompt, metadata}`
  (v4 201; the SPA reads `id`/`filename`/`filepath`/`mimeType` and
  `expandedPrompt`). `imageProfileValidateKey` /
  `imageProfileListModels` STAY refusal-armed.

The salon riders (lane B consumes, ZERO server work):

- `listChats.includeAutonomous` already exists end-to-end
  (`core-contract.ts:58-63`; `api/salon.rs:67-96` implements v4's
  exact filter incl. the `runVisibility ∈ {household, open}`
  override) — lane B sends the flag; NO lane touches the server
  filter.
- The Edit-Enclave entry reuses `qt-edit-enclave-modal` and the seven
  existing CoreClient autonomous verbs unchanged.

Lane C: NO contract changes. No new dispatch variants; document
writes ride the existing `chatDocumentRead`/`chatDocumentWrite`
(+ the mtime conflict arm) and the composer emits the same
`ComposerSend` payload.

### The two-core-dispatch-writer rule (lanes A + D)

Lanes A and D are this round's ONLY writers to `types.rs`/
`engine.rs`/`api/mod.rs`, each appending ONLY at the end inside its
own clearly-delimited block (`// === P4.6ae === … === end P4.6ae ===`
/ `// === P4.d3 === … === end P4.d3 ===` — the P4.6v/w precedent).
The unifier keeps both blocks; neither lane edits inside the other's
block or reorders existing arms. Elsewhere in `crates/**` the two
lanes are file-disjoint: lane D owns `embedding_blob.rs`,
`db/conversation_chunks.rs`, `db/instance_settings.rs`,
`api/settings.rs`, `api/salon.rs`, the maintenance/cold-reembed
services; lane A owns everything in its Ownership entry. Neither
touches the other's list.

### core-contract.ts ownership (single-author rule)

Lane B OWNS `apps/web/src/app/core/core-contract.ts` +
`core-client.ts` and authors the files-family block. Lane D appends
ONE clearly-delimited data-retention block to each that lane B must
not touch (the P4.6ad precedent). Lane C must NOT touch either file.
Each block has exactly one author (the P4.6pqr lesson).

### The editor serializer rule (lane C)

The editor's output is a markdown STRING in v4's composer dialect:
underscore italic (`_text_`, never `*text*`); literal `*` `_`
backtick `~` survive UNESCAPED (v4 `MarkdownBridgePlugin`'s
`stripMarkdownEscapes` + the four `preserve*` flags,
`MarkdownBridgePlugin.tsx:120-202`); v4 `COMPOSER_TRANSFORMERS`
scope. The unified-11.0.5 render pipeline
(`apps/web/src/app/chat/render/**`) is HANDS OFF for every lane —
the editor serializes, the pipeline renders, they never import each
other.

### Name-collision warning

`apps/web/src/app/files/**` is the SCRIPTORIUM `qt-file-manager`
(P4.6aa) — NOT this round's general browser. Lane B's new screens
live in `apps/web/src/app/screens/files/**`. Do not confuse the two;
nobody edits the file manager this round.

If any name or field must change, ALL FOUR orders change and the
change is reported at unification.

## Ownership (BINDING across all four lanes)

- **Lane A (p4.6ae):** `crates/**` per its list — NEW
  `quilltap-core/src/api/files.rs`; its OWN delimited appends to
  `types.rs`/`engine.rs`/`mod.rs` (incl. the NEW
  `EngineAssembly.image_generation` seam); additive methods on
  `db/files.rs` + `db/folders.rs`; `folder_utils.rs` completions; the
  upload port in `services/chat_files.rs`; the `chat_media.rs`
  :799-809 refusal replacement; `api/image_profiles.rs:568-570`
  un-refusal; `quilltap-host/src/host.rs` seam wiring; `quilltap-web`
  — `files_routes.rs` (the new POST/DELETE handlers + the chat-files
  multipart leg; the byte GETs frozen) + `lib.rs` router appends;
  `harness/oracle/fixtures/build-files-web-fixture.ts` + the
  committed `files-{main,mount}.db` (+ any courier-images fixture
  extension and its dependent oracle regen — lane A owns that
  family); `harness/oracle/cases/files-routes*` + the upload/generate
  case files; `crates/quilltap-harness/tests/
  files_routes_equivalence.rs`. **Bumps `quilltap-core` +
  `quilltap-web` + `quilltap-host` + `quilltap-harness`.**
- **Lane B (p4.6af):** `apps/web` — `core/core-contract.ts` +
  `core/core-client.ts` (owner; lanes C/D append only their own
  delimited blocks per the contract rule), NEW
  `apps/web/src/app/screens/files/**` (browser + preview + dialogs),
  `screens/salon/**` (the list toggle + hint + the Edit-Enclave modal
  wiring), `chat/conversation-header.ts` (the gated button — the ONLY
  `chat/**` file lane B touches), `shell/shell.ts` (the Files nav
  wire), NEW `e2e/files-flow.spec.ts` + NEW
  `e2e/salon-autonomous-entry.spec.ts` (both sort AFTER
  `foundation.spec.ts`), `e2e/support/**` additions if seeding needs
  them. **Bumps the SPA.**
- **Lane C (p4.6ag):** `apps/web` — NEW `apps/web/src/app/editor/**`
  (the qt-rich-editor + the serializer bridge + the committed
  round-trip gate spec), `chat/chat-composer.ts` +
  `chat/chat-composer.spec.ts` (the ONLY `chat/**` files lane C
  touches; `conversation-header.ts` is lane B's), `documents/**` (the
  pane adoption + the `USES_RICH_MARKDOWN_EDITOR` flip),
  `package.json`/`package-lock.json` dependency additions (SOLE owner
  of dependency changes this round), the
  `e2e/salon-documents-flow.spec.ts` extension. **Bumps the SPA.**
- **Lane D (p4.d3):** `crates/quilltap-core` —
  `src/embedding_blob.rs` (the codec), `db/conversation_chunks.rs`
  (`clear_embeddings_for_chat`), `db/instance_settings.rs`
  (dataRetention accessors), `api/settings.rs` (the retention pair),
  `api/salon.rs` (the chat_get third pre-read side effect ONLY),
  `services/maintenance.rs` + `services/scheduled_maintenance.rs` +
  NEW `services/collapse_stale_chat_caches.rs` + NEW
  `services/cold_chunk_reembed.rs`; its OWN delimited appends to
  `types.rs`/`engine.rs`/`mod.rs`; the affected-differential regen
  batch in `crates/quilltap-harness/tests/` + their oracle cases; any
  new `retention-{main,mount}.db` fixture + builder; SPA —
  `screens/settings/**` (the Data Retention card) + its OWN delimited
  blocks in `core-contract.ts`/`core-client.ts`. **Bumps
  `quilltap-core` + `quilltap-harness` + the SPA** (the unifier
  accumulates the multi-lane core and SPA bumps per
  `[[parallel-round-reconciliation]]` — verify version deltas
  whole-file).

Round-wide HANDS OFF: `apps/web/src/app/chat/render/**` (the pinned
unified-11.0.5 render pipeline), `apps/web/src/app/files/**` (the
Scriptorium file manager), the byte-GET routes in `files_routes.rs`,
and the frozen mount/courier/autonomous dispatch surfaces.

Union-merged at unification: CHANGELOG, `status-log.md`, the
`api/mod.rs` + web-router appends, the two lane-delimited blocks in
`types.rs`/`engine.rs`, `package.json` (version). CLAUDE.md Status is
round-completion-only; the oracle-baseline line moves to `dd0d9ff5`
when THIS lane unifies.

## Verification gate (run before declaring done)

- The tier-0 round-trip spec committed and green (or the RED
  decision record committed — see tier 0).
- `ng test` green (all suites — the existing composer/document-pane
  specs keep passing, or their updates carry recorded reasons);
  `ng build` clean (watch bundle-size/CommonJS warnings for the new
  deps; static imports per the P4.6u lesson).
- Playwright: the extended `salon-documents-flow` beats run LIVE
  in-lane (the document surface is fully live on main); the full
  suite runs with no NEW failures.
- `cargo test --workspace` untouched-green (this lane edits no
  Rust; run it once before declaring done to prove it).
- Commit checklist per `.claude/commands/commit.md` on every commit
  (CHANGELOG entry, SPA version bump, full-workspace lint).

## Ground rules

The gate before the adoption — never invert them; rejection with a
record beats a lossy GREEN; no stubs/TODO beyond the loud enumerated
deferrals; the dialect traces to v4 (cite the transformer or
preserve-flag per corpus entry); keep v4's steampunk register on any
user-facing strings; if a unit's fresh v4 survey contradicts this
order, STOP and report before proceeding.
