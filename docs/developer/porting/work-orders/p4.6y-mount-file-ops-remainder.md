# Work order P4.6y — the mount-index file-ops remainder (closes P4.6v: the Scriptorium mutation + indexing surface, the D7 closure, the refresh-seam wire)

> **STATUS: OPEN.**
>
> **This order IS the P4.6v remainder** — the resumption lane for
> `p4.6v-mount-index-file-ops-server.md` (whose units 1–3 landed and were
> unified on main 2026-07-12, `ab6141c`). When this order closes, mark
> **P4.6v CLOSED** in its status header, delete the D7 refusal note in
> `api/mount_points.rs`, and note the D7 closure in
> `p4.6p-listing-surfaces-server.md`'s status header (the standing
> instruction carried from the original order).
>
> **A SINGLE lane — no siblings.** The remainder cannot honestly split:
> `storeMountFile` is the single ingest pipeline (write, blob upload, and
> the scanner all ride it), it fire-and-forget calls `reindexLinks` + the
> embedding enqueue (`store-file.ts:212-225`) so the write surface's
> tier-2 dumps cannot pass without the indexing services, and everything
> lands in one module tree (`services/mount_index/`), one dispatch module
> (`api/mount_files.rs`), one fixture family, and one oracle-suite family.
> **v4 reference baseline: HEAD `a7b1398d`** (`~/source/quilltap-server`)
> — run `git log a7b1398d..HEAD --oneline` at lane start; **if HEAD moved,
> STOP and report** (classify drift, don't plan around it). You run in a
> worktree on your own branch; the unification pass integrates onto main
> and runs the full gate (including the full Playwright suite — the seam
> wire changes live document-write behavior under lane C's activated
> document beats).
>
> The original order `p4.6v-mount-index-file-ops-server.md` remains the
> **survey of record** (2026-07-12 @ `a7b1398d` — the same baseline,
> spot-re-verified 2026-07-12 for this order: every lib export and route
> line number below re-checked). Read it in full before starting: its
> route inventory, lib-layer port order, the ten "Quirks the port must
> not miss", the Shared-contract variant table, and the error-envelope
> rules all still bind. This order records only the delta: what landed,
> what changed shape at unification, and the remainder's tiers.

## Mandate

Finish the mount-point file-operations surface: the mutation pipeline
(`store-file` / `file-ops` / `folder-ops`), the indexing services
(`reindex` / `embedding-scheduler` / `scanner`), semantic search, the blob
variants, the web-edge multipart + raw-read fs legs, and the
`convert`/`deconvert` refusal arms — every unit differential-proven against
v4's real code over the committed `mounts-*` fixture family. Then **wire
`EngineAssembly.mount_refresh` live** (the P4.6w deferral) and **close D7**.

No SPA work: do NOT touch `apps/web/**` (the Scriptorium SPA — D18 — is the
next round, over this lane's then-frozen surface). Do NOT touch
`tools/doc_edit/**` (frozen reference; no dedup refactor this round) or
lane B's `crates/quilltap-core/src/documents/**` except the additive seam
plumbing named in tier 1 item 7.

## What already exists on main (verified 2026-07-12 — reuse, don't duplicate)

- **Landed P4.6v units 1–3** (all differential-proven):
  `services/mount_index/{chunker,path_utils,file_op_error,file_op_status,
  read_file,list}.rs`, plus `file_ops.rs` containing **only**
  `resolve_fs_absolute` (the boundary-escape guard, unit-pinned) — the rest
  of `file-ops.ts` is this order's to port into that same file.
  `api/mount_files.rs` carries `mountFilesList` + `mountFileRead`.
- **The fixture family**: `crates/quilltap-web/tests/fixtures/
  mounts-{main,mount}.db` + `mounts-fs-tree/` (fs `basePath` is the
  sentinel `__MOUNTS_FS_TREE__`, rewritten per side). Builder:
  `harness/oracle/fixtures/build-mounts-fixture.ts` (spec `mounts.json` —
  test pepper, pinned userId, seed timestamp), built via v4's REAL repos +
  `storeMountFile`. **It does NOT yet bake an embedding profile** — see
  Fixtures. Sole dependent oracle: `harness/oracle/cases/mount-read.ts`
  (18 cases → `mount_read_equivalence.rs`).
- **The refresh seam (lane B's, already authored)**:
  `documents/mod.rs:78` (`MountRefreshScheduler`), the loud-skip helper
  `documents/mod.rs:763-770`, call sites `:407`/`:505`;
  `EngineAssembly.mount_refresh: Option<Arc<dyn …>>` at
  `api/engine.rs:105` (default `None` at `:118`, threaded to dispatch via
  `:2024-2026`/`:2445`). **The single production wiring site is
  `crates/quilltap-host/src/host.rs:544`** (`mount_refresh: None` under
  the P4.6w comment block that records the wiring condition — replace
  both when wiring).
- **The D7 note to delete at close**: `api/mount_points.rs:7` (the module
  header's "the twelve mount-point action verbs + semantic-search have NO
  variants" refusal).
- Everything in the original order's "v5 side" survey section: all
  `doc_mount_*` repos, `db/database_store.rs`, `search_document_chunks`,
  `files_routes.rs` (`mount_file_get` database-branch + `mount_blob_get`),
  `multipart.rs` (P4.6m), the `EMBEDDING_GENERATE` job type + handler, the
  `ImageTranscoder` seam, `ErrorKind::{Forbidden,Conflict}`.

### v4 line references re-verified for this order (2026-07-12 @ `a7b1398d`)

`store-file.ts`: `CollisionStrategy` :54, `AssetStorageMode` :65,
`storeMountFile` :127 (fire-and-forget reindex+embed :212-225).
`file-ops.ts`: `FileOpStrategy` :39, `resolveFsAbsolute` :83,
`copyFile` :287, `moveFile` :415, `linkFile` :540, `writeFile` :839,
`deleteFile` :875. `folder-ops.ts`: `deleteFolder` :47, `moveFolder` :80.
`reindex.ts`: `reindexLinks` :98, `enqueueEmbeddingJobsScoped` :204.
`scanner.ts`: `processMountFile` :158, `scanMountPoint` :289,
`listFilesystemFolders` :444, `createFilesystemFolder` :496,
`verifyBasePath` :523. `embedding-scheduler.ts`:
`enqueueEmbeddingJobsForMountPoint` :25. Action dispatch
(`mount-points/[id]/route.ts`): handlers :207–:714, dispatch map :759-771
(12 verbs, order `scan, convert, deconvert, move-file, copy-file,
link-file, write-file, delete-file, delete-folder, move-folder, reindex,
embed`). The catch-all `files/[...path]/route.ts` facts stand as surveyed
in the original order.

## Tiered deliverables

Numbering continues the original order's tier list (units 1–2 landed as the
pure leaves + READ/LIST; the original's item 3 onward is this order's).

**Tier 1 (must land, differential-proven):**

1. **`store_file.rs`** — all three ingest branches (fs-mount disk write
   with the optimistic-mtime `CONFLICT`; database native-text →
   `doc_mount_documents` + fire-and-forget reindex/embed; database blob
   with the image→WebP transcode via the existing `ImageTranscoder` seam
   and pdf/docx extraction via the new `DocumentTextExtractor` seam — tier
   3) + variants `mountFileWrite` (JSON + multipart PUT legs) +
   `mountBlobUpload` + the `?action=write-file` web-edge mapping (P4.6m
   multipart helper). Carry the background-job-child guard's why-comment
   (structural under the single-writer rule).
2. **`file_ops.rs` (completed) + `folder_ops.rs`** — the four strategies
   (`db-link | fs-link | rename | byte-copy`), the sha256 end-to-end
   verify pair, `FileOpResult.strategy` serialized faithfully (contract
   surface); variants `mountFileMove` / `mountFileCopy` / `mountFileLink`
   / `mountFileDelete` / `mountFolderDelete` (non-empty → `CONFLICT`) /
   `mountFolderMove` (same-mount only; fs path rewrites link prefixes +
   emits document-moved).
3. **`mountFileUpdate`** (PATCH rename+description; description is
   blob-only — text doc → 400; rename runs BEFORE description) +
   **`mountFolderCreate`** (`isPathSafe` port, unit-pinned).
4. **`mountSemanticSearch`** — route glue over the ported
   `search_document_chunks`, `includeBlocked:true`.
5. **`reindex.rs` + `embedding_scheduler.rs`** — `reindexLinks`
   synchronous in-request (deliberate, keep it), `shouldProcess`
   (`TEXT_NATIVE` = {markdown, txt, json, jsonl}; `EXTRACTABLE` = +{pdf,
   docx}), `enqueueEmbeddingJobsScoped` async via `EMBEDDING_GENERATE`
   jobs + `enqueueEmbeddingJobsForMountPoint`; variants `mountReindex` +
   `mountEmbed`. Text-native full fidelity; extractables route through
   the `DocumentTextExtractor` seam (loud refusal per file, extraction
   state still bookkept).
6. **The web-edge raw-read fs branch** in `files_routes.rs`
   (`mount_file_get` covers database mounts only today).
7. **Wire `EngineAssembly.mount_refresh` live.** Implement the scheduler
   over this order's reindex + embed services (a small impl in
   `services/mount_index/`, fire-and-forget on the engine's runtime — the
   `EngineAssembly.memory_embedding` precedent), set it at
   `host.rs:544` replacing the `None` + the P4.6w comment block. The
   loud-skip stays for `None` assemblies (tests). **Then re-run the
   P4.6w `documents-routes` differential**: if its oracle mocked or
   never exercised v4's `scheduleDocumentStoreRefresh` side effects, add
   a scoped case proving the refresh parity (chunks written after a
   document-store write) — fire-and-forget must be drainable in tests on
   BOTH sides (await the promise v4-side; expose a flush/completion hook
   v5-side) so tier-2 dumps stay deterministic.
8. **Close D7**: delete the `api/mount_points.rs` header refusal note;
   at unification, note the closure in P4.6p's status header and mark
   P4.6v CLOSED.

**Tier 2 (should land):**

9. **`scanner.rs`** — `scanMountPoint`, `processMountFile`,
   `removeMountFile`, `updateMountPointTotals`, `verifyBasePath`,
   `detectFileType`/`matchesPattern` + the `mountScan` variant; pdf/docx
   per-file through the seam (refusing loudly where v4 would extract).
10. **`mountBlobsList` / `mountBlobDelete` / `mountBlobUpdate`** — the
    documents-table fallback on GET/DELETE preserved.

**Tier 3 (explicit refusal arms / named deferrals — loud, never silent):**

- `mountConvert` / `mountDeconvert` — variants exist, return the
  `not_available`-style typed refusal naming this order (`conversion.ts`
  is a full future unit). Mirror the capability guards
  (`derive_mount_capabilities` already ported) so the refusals sit behind
  the same gates v4 puts the handlers behind.
- **`DocumentTextExtractor`** — a core seam trait (pdf/docx → plaintext);
  the default impl refuses loudly per file, extraction-state bookkeeping
  still runs (so differentials pin the pipeline on text-native corpora).
  The production impl is a named future order; never silently mark files
  skipped.
- The chokidar-equivalent fs watcher — no variant; a host cadence
  deferral (note it in the status header at close).
- The `quilltap docs` CLI subcommands — no CLI work this round.

## The differential requirement (restated)

Every ported unit ships with an equivalence test driving v4's REAL code.
The original order's section stands verbatim; the operative recipes:

- **Routes/services**: tier-2 structural DB diff via the jest real-DB
  oracle (`jest-real-db-oracle` + `jest-oracle-mechanism` +
  `document-store-oracle-gotchas` memory recipes: `resetModules`+`doMock`
  past jest.setup's global DB mocks, real `better-sqlite3-multiple-ciphers`
  by absolute path, Node 24, `--watchman=false`, extra `--roots`,
  `--testTimeout=120000`, NDJSON to a file). Drive v4's REAL route
  handlers (the catch-all, the action-dispatch map, folders/blobs routes)
  over per-side copies of the committed fixture; diff response envelopes
  AND the six-table mount-index dump
  (files/file_links/documents/blobs/chunks/folders) with the established
  normalizations (timestamps, minted UUIDs via the remap, fs mtimes via
  the sentinel/0 normalization already proven in `mount-read.ts`).
- **SPLIT the suites** (the P4.6s cross-case-contamination lesson):
  suggested `mount-write` (store-file + blobs) / `mount-ops`
  (file/folder ops + update/create) / `mount-index` (reindex/embed/scan/
  semantic-search). Record regen commands in each case header.
- **fs-mount cases** run over per-side temp COPIES of the committed fs
  tree (never shared-live — the P4.3 lesson); rewrite the
  `__MOUNTS_FS_TREE__` basePath sentinel per side via
  `Writer::open_writable` UPDATE before `Db::open` (the landed
  `mount_read_equivalence.rs` shows the exact recipe).
- **The P4.6p envelope gotchas stand**: route reads omit null nullables
  but create/update echo them AS null; provider probe no-op'd under
  jest; chokidar's ESM import needs the watcher-mock.
- **`doc_mount_blobs` DDL**: the `data BLOB` table can't be generateDDL'd
  — trigger the repo's lazy table-init
  (`new DocMountBlobsRepository().findByFileId(...)`) before any blob
  insert, both in the fixture builder (already done there) and in any
  oracle-side scratch DB.
- New JSON dispatch variants auto-wire via `POST /api/dispatch` — no
  per-variant web route except the multipart/binary legs (write-file
  multipart, blob upload, the raw-read fs branch). Append variants inside
  the existing `// === P4.6v: mount files ===` delimiter blocks in
  `api/types.rs` / `api/engine.rs` (keep the block discipline even as
  sole writer).

## Fixtures

- **Consumes** `crates/quilltap-web/tests/fixtures/mounts-{main,mount}.db`
  + `mounts-fs-tree/` (the location correction from the P4.6v close: NOT
  `quilltap-harness/fixtures/`).
- **Expected extension (this lane owns it):** bake the builtin TF-IDF
  `embedding_profiles` row (the P4.6s fixture precedent) so
  `mountEmbed`/`mountSemanticSearch` run deterministically, plus whatever
  the write/scan corpora need (e.g. a pdf/docx-named file to pin
  extraction-state bookkeeping through the refusing seam, blob rows with
  real bytes for the blob variants). Extend `build-mounts-fixture.ts` /
  `mounts.json`; ids/timestamps stay pinned.
- **Fixture change ⇒ this lane regenerates every dependent oracle.**
  Today that is exactly one: `mount-read.ts` (18 cases,
  `mount_read_equivalence` — regen command in the memory note
  `p4-6v-mount-file-ops-lane` and the case header). Enumerate any others
  you create in the status header.
- Nothing writes the committed fs tree; tests copy it per side.

## Shared contract (BINDING — carried from the P4.6v/w/x round)

The **variant table in `p4.6v-mount-index-file-ops-server.md` §"Shared
contract"** remains the binding wire contract for every variant this order
lands (`mountFileWrite` … `mountScan`, the refusal-armed
`mountConvert`/`mountDeconvert`) — same wire `type` names, payloads, and
response-body shapes ("lane A pins" resolutions are this order's to pin
from the oracle envelopes and record in the status header at close). The
next round's Scriptorium SPA (D18) builds against these names; do not
rename after landing.

The refresh-seam trait is FROZEN as authored
(`documents/mod.rs:78` — `schedule_refresh(&self, mount_point_id: &str,
path: Option<&str>)`); this order implements and wires it, it does not
reshape it. The error envelope rules (the `{ error, code }` body with the
`FileOpError` ∪ `DatabaseStoreError` code union; reads omit null
nullables, writes echo null) stand as written there.

## Ownership (single lane — these are the frozen zones)

| Path | This lane |
|---|---|
| `crates/quilltap-core/src/services/mount_index/**` | owns (extends) |
| `crates/quilltap-core/src/api/mount_files.rs`, `api/mount_points.rs` (D7 note deletion) | owns |
| `crates/quilltap-core/src/db/doc_mount_*.rs`, `db/database_store.rs` | additive extensions only |
| `api/types.rs` / `api/engine.rs` | append inside the `// === P4.6v ===` delimiter blocks only |
| `crates/quilltap-web/src/{files_routes,multipart,lib}.rs` | additive (raw-read fs branch, multipart legs, router registration) |
| `crates/quilltap-host/src/host.rs` | the seam wire at :544 only |
| `crates/quilltap-core/src/documents/**` | FROZEN except: no edits needed — the seam call sites already exist; if a flush/drain hook for tier-1 item 7 truly requires a touch, keep it additive and record it |
| `harness/oracle/{cases,fixtures}/mount-*`, `crates/quilltap-harness/tests/mount_*`, the `mounts-*` fixture family | owns |
| `crates/quilltap-core/src/tools/doc_edit/**` | FROZEN reference (no dedup refactor) |
| `apps/web/**` | untouched |
| `docs/CHANGELOG.md`, `docs/developer/porting/status-log.md` | append-only |

Version bumps: this lane bumps `quilltap-core` / `quilltap-web` /
`quilltap-harness` / `quilltap-host` per its own commits. Nobody bumps
`quilltap-sqlite3mc-sys`.

## Verification gate (run before declaring done)

1. `cargo fmt --all --check`.
2. `cargo clippy --workspace --all-targets -- -D warnings` on the default
   feature set AND with `--features quilltap-core/native-transport`.
3. `cargo test --workspace` (no env vars — self-tests + skips green).
4. Regenerate YOUR oracles fresh from v4 at `a7b1398d` and run YOUR
   differentials BY NAME with env vars set — the new suites, plus
   `mount_chunker_equivalence` + `mount_read_equivalence` (regen if the
   fixture changed), plus the P4.6w `documents-routes` differential
   re-run after the seam wire — all green; record regen commands in the
   case headers.
5. `cargo build --workspace --release` compiles.
6. Every commit follows `.claude/commands/commit.md` (changelog entry,
   version bumps, status-log append).
7. Flag for the unifier: run the FULL Playwright suite at unification —
   the seam wire changes live document-write behavior under the
   activated document e2e beats.

## Ground rules

- Work in your worktree; never `git stash`; commit at every green unit.
- No stubs or silent TODOs — tier-3 deferrals are loud typed refusals or
  enumerated no-variant deferrals in the status header.
- The schema and cipher do not change. Writes against real instances:
  never (fixtures only).
- Spelling: **Quilltap**, never "Quilttap". User-facing strings keep the
  v4 register — the steampunk `error`-string copy in this family is
  load-bearing for the Scriptorium SPA's error translation; port it
  faithfully.
- If a fact here or in the original order contradicts the code, re-survey
  v4 and note the correction in the status log; don't silently improvise.
