# Work order P4.27 — the D32 sweep debt (projection gap, terminal_tools, recipe runnability)

> **STATUS: CLOSED — UNIFIED on main (2026-07-31), lane E.** All three debts
> cleared. (1) The `canChooseOutfit` gap diagnosed FROM THE RED: the omitting
> reader was a FIFTH site (`db/characters_read.rs::marshal_row` — the raw-read
> managed-defaults block), fixed one-line, mutation-proven;
> `characters_read` + `characters_actions` GREEN fresh at `ff12f491`. **A
> correction worth keeping: `characters_reads` was green both with and
> without the fix — D32's red on THAT family was a sweep hazard, not the
> projection gap.** (2) `terminal_tools` was rot class (a): v4 `e3593f75`
> made the validators return the parse (`| null`) and the oracle case
> recorded the raw return, rotting `valid` to object/null — the case now
> records the verdict (`!== null`); family green from its own header recipe.
> (3) The recipe-runnability pass: the driver COMMITTED
> (`harness/tools/recipe_sweep.py` + README — tier 2 delivered), both D32
> hazards enforced as policy (repo-write refusal + fixture shield;
> per-family scratch suffixing + atomic regen-then-run), the re-derived
> non-runnable list (15 families + 4 corpus headers — the honest successor
> to D32's lost 28) ALL fixed and proven by verbatim execution;
> classification at lane close: 0 non_extractable.
>
> **Unification notes:** lane B's new `llm_log_cleanup_equivalence` header
> (written in parallel) classified `non_extractable (repo_write)` under this
> lane's policy — restructured at unification (the fixture-install cps
> demoted to a deliberate-act note) and re-proven via `--run`; lane A's new
> `store-unavailable` recipe carried literal `<scratch>` placeholders —
> fixed at unification and re-proven via `--run`. The union is back to
> **0 non_extractable**. The gate also surfaced NEW sweep debt:
> `autonomous-rooms-routes.test.ts`'s oracle fails at `ff12f491`/`e1be028b`
> (v4's forked background-jobs child dies on the `@/` alias under jest —
> `ERR_MODULE_NOT_FOUND` from `child-entry.ts` — and a mid-list case then
> reads an empty DB: `no such table: chats`); pre-existing, not
> round-caused (green in D32's pinned-worktree sweep, deterministic today),
> recorded in the round record for the next maintenance pass.
>
> The original order follows unchanged.
>
> **STATUS (original): OPEN, not started (ordered 2026-07-31).** Written from the
> `ff12f491` round's standing items (P4.D32's sweep surfaced all three).
> Phase-4's candidate list carries them as items 2 and 4.
>
> **v4 baseline for this round: `ff12f491`** (v4 HEAD is `e1be028b`, one
> release-infra commit past it — NO-PORT, verified by name). **Drift-check
> at lane start**: `git log ff12f491..HEAD --oneline` + `git status
> --short` in `~/source/quilltap-server` must show exactly `e1be028b` and a
> clean tree; anything more → STOP and report. ⚠ If v4 has landed the
> finding-#47 vault fix (flagged urgent to the human), the characters
> families' oracles move — coordinate with lane A before regenerating them.
>
> **Siblings (same round):** lane A (P4.22 → P4.23 — owns `api/**` and the
> characters-WRITE families), lane B (P4.24 — owns the new llm-log-cleanup
> family), lane C (P4.25), lane D (P4.26). See "Shared contract +
> Ownership" — binding and identical across all six orders.
>
> **Worktree note:** own branch, own checkout; no `git stash`/`git
> worktree`.

## Mandate

Clear the three debts D32's 290-family sweep left behind: (1) the
`canChooseOutfit` read-projection gap — the standing RED in
`characters_read`/`characters_reads`/`characters_actions`; (2) the
`terminal_tools` oracle that did not survive regeneration; (3) recipe
runnability — every harness family header's recipe must actually run
mechanically (`harness-recipes-are-runnable`), with D32's two sweep hazards
fixed as policy. Do NOT touch: `crates/quilltap-core/src/api/**` (lane A —
STOP rule below), lane A's/B's harness files, the SPA.

## Survey-verified starting points (2026-07-31)

### Item 1 — the `canChooseOutfit` projection gap

- **The finding (D32's lane record, status-log ~:45131):** v5's character
  projection omits `canChooseOutfit`, which v4 emits.
  `git log -S canChooseOutfit dcd9440a..ff12f491` is EMPTY — no drift
  commit introduced it; `characters_read` was proven RED at BOTH pins with
  matched oracle+fixture pairs. It is the P4.6bh outfit round's leftover:
  the vault flag landed, the DB read projection didn't.
- **Every site that ALREADY carries the flag (verified today)** — the
  omitting reader is therefore a FURTHER site; this is the playbook's
  "enumerate every READER of the bag" lesson in the flesh:
  - `db/vault_read_overlay.rs:243-253` — hydration projects it;
  - `db/characters.rs:116` — `MANAGED_FIELDS`;
  - `api/characters.rs:1624` — the PUT whitelist (⚠ lane A's file);
  - `services/character_enrichment.rs:126-130` — the list DTO
    (`canChooseOutfit ?? false`).
- **Diagnose from the RED, not by grep**: regenerate the three families'
  oracles fresh at `ff12f491`, run them, and read the diff output — it
  names the op and the missing field's location. Then enumerate the
  remaining readers of the character bag until the omitting projection is
  found. Expected fix sites (this lane's): `db/characters.rs`,
  `db/vault_read_overlay.rs`, `services/character_enrichment.rs`. **If the
  fix must land in `api/characters.rs` (or anywhere under `api/**`), STOP
  that unit and record the exact diff + evidence for the unifier** — that
  file is lane A's this round.
- Done means: `characters_read_equivalence`, `characters_reads_equivalence`
  and `characters_actions_equivalence` GREEN over fresh `ff12f491`
  oracles, back in the normal gate, with the fix mutation-proven.

### Item 2 — the `terminal_tools` oracle

- D32: "its oracle did not survive re-generation; unresolved, and reported
  as unresolved." File: `crates/quilltap-harness/tests/
  terminal_tools_equivalence.rs` + its oracle case.
- Diagnose which of the three rot classes it is: (a) the CASE rotted (a v4
  API it imports moved — then this is a mini drift re-port of the case,
  not the port); (b) the RECIPE rotted (stale paths/placeholders — item
  3's bread and butter); (c) a /tmp collision or committed-fixture mutation
  (D32's two hazards — a red in a broad sweep is not a finding until it
  survives isolation, `sweep-recipes-write-into-the-repo`). Fix
  accordingly; done means the family runs GREEN from its own header recipe,
  fresh oracle, zero SKIP.

### Item 3 — recipe runnability

- **The 28-family list is LOST** (`/tmp/d32-rest-final.json` did not
  survive; the lane's scratchpad is gone). Re-derive it: rebuild D32's
  extraction driver per its lane record (status-log ~:45080) — extract
  each family header's shell block; normalise the accumulated placeholders
  (`$N`, `$V5W`, `<this>`, `<worktree>`, other lanes' stale worktree
  paths); rewrite `cd ~/source/quilltap-server` targets; restore elided
  (`… npx jest -- x`) invocations with an ANCHORED pattern; run each
  family in its OWN clean invocation with the oracle output removed
  first. The 6 compile-time-pin families with no oracle by design are
  exempt (`p4_6ao/p4_6ar/p4_9g1/p4_d10_wire_contract`,
  `p4_9g6_seam_contract`, `settings_wire_actions`).
- **Fix every non-extractable header** so its recipe runs verbatim —
  multi-stage jest stages spelled out, bespoke fixture builders invoked
  explicitly, placeholders replaced with copy-paste-safe forms. The
  standing rule this enforces: `harness-recipes-are-runnable` — every
  family header carries its own regen command.
- **Fix the two D32 hazards as POLICY while there:** (a) no recipe writes
  into the repo — headers that `cp` into `crates/**` or point the oracle
  at a committed `.db` get rewritten to /tmp COPIES (the
  `embedding-generate-*`, `embedding-remainder-*`, `episodic-recall-*`
  class); (b) per-family UNIQUE /tmp paths, so one sweep's families cannot
  clobber each other (five of D32's twelve gate reds were exactly this).
- **Exclusions (binding):** do not edit headers in files lanes A and B own
  this round — the characters-WRITE family lane A extends,
  `groups_routes_equivalence.rs`, `projects_routes_equivalence.rs`, and
  lane B's new llm-log-cleanup family. If one of those headers is broken,
  list it in the lane record for the owning lane/unifier.
- **Commit the driver** (tier 2) so the next sweep doesn't re-derive it —
  e.g. `harness/tools/` — D32's record explicitly says the scripts "are
  worth re-deriving for the next broad sweep"; make that unnecessary.

## Tiered deliverables

### Tier 1 — must land

1. The `canChooseOutfit` diagnosis + fix (STOP rule if it lands in
   `api/**`), the three characters-read families GREEN fresh at
   `ff12f491`, mutation-proven.
2. The `terminal_tools` diagnosis + repair, family GREEN from its own
   header recipe.
3. The re-derived non-runnable-recipe list (the honest successor to D32's
   lost 28) recorded in the lane record, and every header on it fixed —
   except sibling-owned files, listed instead.
4. Proof of runnability: each fixed header's recipe executed verbatim
   end-to-end (regen + family run, zero SKIP, marker-grepped). Fixing a
   header without running it is not done.

### Tier 2 — should land

5. The extraction/run driver committed under `harness/tools/` with a short
   README — the next sweep runs it, not re-derives it.
6. A repo-write refusal in the driver (D32's rule: refuse any `cp`/`mv`
   whose destination is inside the repo).

### Tier 3 — explicit loud deferrals

- Families whose recipes CANNOT be made mechanically runnable (if any
  genuinely resist — e.g. a recipe requiring the real Friday copy or 💸
  spend) get a header note saying exactly what manual step remains and
  why, rather than a silently broken block.
- The `chat_settings` explicit-`null` and `V5_STATS_GAP` tripwired gaps
  stay where they are — not this lane's.

## The differential requirement, restated

Item 1 is a port fix and carries the full discipline: the three families
drive v4's REAL character reads (their existing recipes/oracle cases);
regenerate fresh at `ff12f491`, grep the NDJSON for a fresh marker
(`oracle-regen-silent-stale-pass`), run BY NAME with `--nocapture`, zero
SKIP, and mutation-prove the fix (drop the projection → RED). Items 2–3
are harness maintenance: the "differential" is each recipe executed
verbatim to a green family.

## Fixtures

Consumes the committed characters/terminal fixture families; regenerates
their oracles (this lane owns those regens this round — coordinate with
lane A only if v4 lands the #47 fix mid-round). Delivers no new fixture.
⚠ Recipes that mutate committed fixtures run against /tmp COPIES —
verify `git status` shows every committed `.db` byte-untouched at lane
close (D32's restore precedent).

## Shared contract + Ownership (binding; identical across all six orders)

**File ownership this round — no lane edits another lane's files:**

| Lane | Orders | Owns (exclusively) |
|---|---|---|
| A | P4.22 → P4.23 (one branch, sequential) | `crates/quilltap-core/src/db/vault_character_update.rs`; `crates/quilltap-core/src/api/**` (incl. `types.rs`, `engine.rs`, `projects.rs`, `groups.rs`, `autonomous_rooms.rs`, `characters.rs`); `crates/quilltap-web/src/**`; `apps/web/src/app/core/core-contract.ts`; harness/oracle files for the characters-WRITE (vault-properties) arms it extends + `groups_routes_equivalence.rs` + `projects_routes_equivalence.rs` + any `crates/quilltap-web/tests/**` envelope-arm file |
| B | P4.24 | `crates/quilltap-core/src/db/llm_logs.rs`; `crates/quilltap-core/src/services/{queue_service,job_runner}.rs` + the new `llm_log_cleanup_job.rs`; `crates/quilltap-host/src/**`; the new `llm-log-cleanup` fixture family + harness family + oracle case |
| C | P4.25 | the new toast module files + its new stylesheet + the styles index; every `apps/web` call-site file it converts EXCEPT lane D's files and `core-contract.ts`; its new e2e spec file; dialog/settings e2e specs whose assertions its change moves (named per-file in the lane record) |
| D | P4.26 | `apps/web/src/app/chat/{message-row,announcement-group,message-content,system-message-labels}.ts` + their specs; `apps/web/src/app/chat/render/**`; `apps/web/src/styles/**/_chat.css`; its new e2e spec file; salon/announcement/post-office e2e specs whose assertions its fixes move (named per-file) |
| E | P4.27 | `crates/quilltap-harness/tests/{characters_read,characters_reads,characters_actions}_equivalence.rs` + their oracle cases; `terminal_tools_equivalence.rs` + its oracle case; harness recipe HEADERS in files NOT owned by lanes A/B; the projection-fix site in core as diagnosed (`db/characters.rs` / `db/vault_read_overlay.rs` / `services/character_enrichment.rs`) |

- **`crates/quilltap-core/src/api/**` belongs to lane A.** Lane E STOPS and
  records the exact diff if the `canChooseOutfit` fix must land there.
- Lane C never edits lane D's four chat files, even where a v4 toast call
  site lives in one — record the site for D/the unifier instead.
- Lane D is SPA-only; a divergence traced to the SERVER is recorded, not
  fixed.
- Lanes C and D never edit the SAME e2e spec file; an unavoidable collision
  → STOP and record for the unifier.
- `CHANGELOG.md` and `docs/developer/porting/status-log.md` are append-only
  for everyone (union-merge at unification).
- Version-bump ownership: every lane bumps the crates/SPA it touches per
  `.claude/commands/commit.md`; the unifier recounts and collapses
  (`parallel-round-playbook`).
- All lanes pin **v4 baseline `ff12f491`** and drift-check at lane start
  (expect exactly `e1be028b` past it, tree clean; else STOP). No lane
  writes baseline wording — no baseline move this round.
- Distill-transitive tier-3 recipes keep their TZ pins (P4.d26 rule) —
  ⚠ especially binding on THIS lane: item 3's header fixes must PRESERVE
  every existing TZ=UTC / America/Chicago pin verbatim.

## Verification gate before declaring done

- `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D
  warnings` on BOTH feature sets.
- `cargo test --workspace --no-fail-fast` with this lane's env vars
  (`cargo-test-workspace-is-fail-fast`).
- The three characters-read families + `terminal_tools` re-run BY NAME
  with `--nocapture`, zero SKIP, GREEN, fresh `ff12f491` oracles
  (marker-grepped).
- Every fixed recipe header proven by verbatim execution; every committed
  `.db` byte-untouched at lane close (`git status`).
- No `apps/web` change → no SPA gate owed. Say so in the lane record.
- Per-commit rule: follow `.claude/commands/commit.md`.
