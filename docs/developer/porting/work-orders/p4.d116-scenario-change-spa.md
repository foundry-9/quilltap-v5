# Work order P4.D116 — the `44a8137e` scenario-change drift, SPA half: the shared ScenarioSelect, the in-chat scenario control, the New-Chat refactor

**Status:** OPEN — not started (written 2026-08-25).

## Preamble

- **v4 baseline for this round: `f6a10055`** (v4 main). The round absorbs the
  FIVE commits past it (drift-ledger §3); this lane carries the CLIENT half
  of `44a8137e` (sibling P4.D115 carries the server half). At planning the v4
  checkout was on `main`, tree CLEAN, HEAD `8f910137`; `bugfix` has no
  unabsorbed content beyond the tests-only `009c49b2`.
- **Run the drift ledger's §2 freshness probe at lane start**
  (`docs/developer/porting/drift-ledger.md`). Probe failure = **STOP and
  report**; a lane never writes the ledger.
- **Regen rule in force: pin required** for any jest/tsx oracle this lane
  runs (client-transcription parity recorders included) — lane-unique
  detached worktree, all three symlink classes (ledger §5.1). This lane's v4
  spec reads come from `git show 44a8137e:<path>`; any v4-client recorder it
  drives runs from a pin at `44a8137e` (which contains all of `f6a10055`).
- **Sibling lanes:** P4.D115 (`p4.d115-scenario-change-server.md` — meets
  this lane ONLY at the §Shared contract), P4.D117
  (`p4.d117-qt-classes-gallery-download.md` — also an SPA lane; the
  §Ownership dir split below is the fence, and **coordinate Playwright runs:
  port 4319 is repo-wide, two lanes cannot run e2e at once** — memory:
  `e2e-port-4319-is-repo-wide.md`), P4.D118 (disjoint).
- **Worktree:** per the `carryout` skill; edit inside the worktree. SPA
  worktree `node_modules` via `npm ci`, never `cp -a` (memory:
  `spa-worktree-node-modules.md`). `docs/CHANGELOG.md` and `status-log.md`
  append-only.

## Mandate

Port the client half of v4 `44a8137e`: the **scenario dropdown extracted into
a shared component** (v4's `components/scenario/ScenarioSelect.tsx` +
`types.ts`) with the **New-Chat form refactored onto it**
(equivalence-preserved — the new-chat scenario behavior must not move), the
**new in-chat `ChatScenarioControl`** in the Salon sidebar's Chat drawer
(four tiers + Custom free-text, character tier only when exactly one LLM
character is present, saves through the §Shared-contract verb, opens on the
chat's CURRENT scene via the new GET projection), the **sidebar/SalonView
threading** (`projectId`, `scenarioText`, the LLM-cast derivation), and the
**scenario query keys**. Land the e2e beat gated, activated at unification
once P4.D115's verb is live.

**Must NOT touch:** anything under `crates/**` (P4.D115/P4.D118);
`apps/web/src/styles/**`, `apps/web/scripts/**`, `apps/web/src/app/images/**`
(P4.D117). Write NO new qt-* utility class names that don't already exist in
`src/styles/qt-components/_utilities.css` at lane start — P4.D117 is
rewriting that sheet in parallel; transcribe v4's class strings verbatim from
`44a8137e` and let the unifier's guard run reconcile (see §Ownership note).
v4's `help/**` deltas are P4.D115's bank rows, not yours.

## Survey-verified starting points (fresh survey 2026-08-25, at `44a8137e`)

Read the commit's client files whole: `git show 44a8137e -- components/
app/salon/ lib/query/keys.ts` in `~/source/quilltap-server`. ⚠ Port from the
shipped hunks, never the commit message (ledger §5.3).

### v4 — the client shapes

- **`components/scenario/ScenarioSelect.tsx` (NEW, 144 lines)** — the shared
  dropdown: four preset tiers + "Custom…" which reveals the free-text box.
  Extracted FROM `NewChatForm.tsx` (−168 lines there) so both surfaces render
  identically. **`components/scenario/types.ts` (NEW, 162 lines)** — the
  option/selection types moved out of `components/new-chat/types.ts` (−93).
  Read all four diffs together to see exactly which behavior moved vs which
  is new; the New-Chat dialog's visible behavior does NOT change.
- **`components/chat/ChatScenarioControl.tsx` (NEW, 285 lines)** — the Chat
  drawer control: fetches the tier lists (project tier gated on `projectId`,
  group tier keyed by the comma-joined SORTED LLM-character ids, character
  tier only when `singleLlmCharacterId` is non-null), shows the scene in
  force (from the GET projection), Save posts `?action=scenario`, empty
  Custom clears, and a save that resolves to the scene already in force is a
  visible no-op (server answers `changed:false`).
- **`components/chat/ChatSidebar.tsx` (+41)** — new props `projectId?`,
  `scenarioText?`; derives the LLM cast:
  `participants.filter(p => p.controlledBy !== 'user' && p.status !==
  'removed' && p.character?.id)` → `llmCharacterIds`;
  `singleLlmCharacterId = length === 1 ? [0] : null`. ⚠ Note the
  `controlledBy !== 'user'` read — v5's impersonation overlay (P4.D56/D58)
  means the client-side seat predicate is `isUserDrivenSeat`; check whether
  v4's raw `controlledBy` read here predates or ignores the overlay and
  mirror v4's ACTUAL spelling, recording the tension if the overlay twins
  (`skip-signal-helpers.ts`) would answer differently.
- **`app/salon/[id]/SalonView.tsx` + `types.ts` (+2 each)** — thread
  `projectId` and `scenarioText` from the chat payload to the sidebar.
- **`lib/query/keys.ts` (+13)** — the `scenarios` key family: `all`,
  `general`, `project(projectId)`, `group(characterIdsKey)`,
  `character(characterId)` — cached per SCOPE, not per chat.
- The GET projection (`scenarioText`) is the server half's item; this lane
  READS it. If the chat payload reaches the control through a reducer, check
  the pure reducer actually carries the field — a render-site edit alone can
  be inert (memory: `v4-client-fix-needs-the-reducer-carry.md`).
- The `scenario-change` announcement kind renders through the EXISTING
  announcement render sites; v5 renders host/system announcements on TWO
  sites (the P4.D36 whisper-tag lesson) — verify both accept the new kind,
  spec-pinned, rather than assuming the default arm is right.

### v5 — where the counterparts live (verified 2026-08-25)

- New-Chat: `apps/web/src/app/screens/new-chat/` — `new-chat-form.ts`,
  `new-chat.types.ts`, `new-chat.logic.ts` (+ specs, `new-chat.state.ts`).
  The scenario fields ride the `ChatCreate` flatten seam (P4.D44 —
  `api/types.rs` never opened for the template picker; same here).
- Sidebar: `apps/web/src/app/chat/sidebar/` — `chat-section.ts` is the Chat
  drawer (v4's control lands there), `chat-sidebar.ts` the container;
  `participant-card.ts`/`participants-section.ts` show the cast conventions.
  The client seat twins live in `apps/web/src/app/chat/skip-signal-helpers.ts`.
- The typed verb goes in `apps/web/src/app/core/core-contract.ts`
  (`chatRegenerateBackground` at ~:5463 is the pattern) — THIS lane owns
  that file this round; the shape is pinned by §Shared contract, not
  negotiated.
- Angular porting conventions: memory notes
  `porting-a-react-component-to-angular.md`,
  `angular-custom-element-host-is-inline.md` (dogfood #97/#103 — give the
  new components' hosts real display values),
  `angular-select-cannot-mirror-react-controlled-value.md` (the dropdown
  must not regress the controlled-select lesson),
  `dom-rebuild-refires-mouseenter.md` (if the control rebuilds option rows).

## Tiered deliverables

**Tier 1 (must land):**

1. NEW `apps/web/src/app/scenario/` — the shared `ScenarioSelect` component
   + types, transcribed from v4's new files 1:1 (parity spec against the v4
   source per the established client-oracle transcription pattern).
2. The New-Chat form refactored onto it, with the existing new-chat specs
   green UNCHANGED (the refactor's neutrality proof at the spec tier) plus a
   parity spec for anything v4's refactor genuinely changed.
3. `ChatScenarioControl` in the Chat drawer: tier gating, current-scene
   seeding from `scenarioText`, Save through `chatSetScenario`, clear via
   empty Custom, v4's toast/feedback sentences byte-exact (survey them in
   the component — do not invent).
4. The sidebar/SalonView threading + the LLM-cast derivation (v4's exact
   predicate spelling, with the overlay-tension note from the survey
   recorded either way).
5. The scenario query keys mirrored into v5's query-key home, per-scope.
6. The `scenario-change` kind verified at BOTH announcement render sites
   (spec-pinned).
7. The e2e beat, gated on a `P4D115_SERVER_LANDED`-style constant flipped at
   unification: open a seeded chat → picker shows the seeded scene (GET
   projection) → pick a project scenario → the Host revision bubble appears
   with v4's sentence → re-pick the same → no new bubble → clear → the
   cleared sentence.

**Tier 2 (should land):**

8. The no-op save's UX exactly as v4 renders `changed:false` (survey the
   component's handling; do not invent a toast v4 doesn't show).

**Tier 3 (explicit loud deferrals):**

9. None expected. If the transcript-export UI surfaces the new kind anywhere
   client-side beyond the render sites, record it rather than chasing it.

## The differential requirement, restated

Client lanes prove fidelity by transcription parity specs against v4's real
source at the pin (the established v4-client-oracle pattern — see the
P4.D86/P4.D113 lane records) plus the gated e2e beat driving the REAL server
verb end-to-end at unification. The New-Chat refactor's neutrality is proven
by the untouched existing specs staying green plus a diff-read of v4's own
refactor hunks. Mutation-proof the tier gating (single-vs-several LLM
characters flips the character tier) and the seeding (a chat WITH a scene
must not open on "Custom…" — that is the exact v4 bug being fixed).

## Fixtures

- The e2e beat runs over the committed e2e fixture; it needs a chat with a
  seeded `scenarioText` and at least one project scenario file. Remember
  store-overlay properties cannot be SQL-seeded — seed scenario FILES through
  the API/UI in the beat itself or extend the fixture the sanctioned way
  (memory: `store-overlay-properties-cannot-be-sql-seeded.md`). If the
  committed fixture changes, THIS lane owns regenerating dependent e2e
  expectations; it must not touch harness oracle fixtures (P4.D115's).

## Shared contract (BINDING — identical text in `p4.d115-scenario-change-server.md`)

1. **Verb:** wire name `chatSetScenario`, Rust variant `ChatSetScenario`.
   Request fields, exactly v4's `setScenarioSchema` (all optional AND
   nullable — the absent/null/value tri-state is preserved end-to-end):
   `chatId` (path/id), `scenario: string?`, `scenarioId: uuid?`,
   `projectScenarioPath: string(≤500)?`, `groupScenarioPath: string(≤500)?`,
   `groupScenarioGroupId: uuid?`, `generalScenarioPath: string(≤500)?`.
2. **Success body** (v4 success envelope):
   `{ scenarioText: string|null, changed: boolean, message: 'Scenario
   updated' | 'Scenario cleared' | 'Scenario unchanged' }`.
3. **Error arms:** invalid body → v4's flat `Validation error` 400 envelope;
   unknown chat → v4's `notFound('Chat')` 404; failed write →
   `serverError('Failed to update chat')` 500.
4. **Chat GET** gains `scenarioText: string|null` in its projection.
5. **The SPA reuses the EXISTING scenario list verbs** (general / project /
   group-union / character tiers — already in `api/types.rs` ~:545-838). No
   new list endpoints.
6. **Announcement kind `scenario-change`** rides the existing host-message
   machinery; the client renders it through the existing announcement render
   sites (this lane verifies both sites accept the new kind).

## Ownership (BINDING — identical table in all four orders)

| Lane | Owns (exclusive) | Version bumps |
|---|---|---|
| P4.D115 | `crates/quilltap-core/**`, `crates/quilltap-web/**` (only if the Tier-3 REST edge lands), `harness/oracle/cases/**` + `crates/quilltap-harness/**` for the chat families it touches | core, harness, web (only if touched) |
| P4.D116 | `apps/web/src/app/screens/new-chat/**`, `apps/web/src/app/chat/sidebar/**`, NEW `apps/web/src/app/scenario/**`, `apps/web/src/app/core/core-contract.ts`, its e2e specs | SPA |
| P4.D117 | `apps/web/src/styles/**`, NEW `apps/web/scripts/check-qt-classes.mjs` + lint wiring, `apps/web/src/app/images/**`, the qt-class sweep over `apps/web/src/app/**` EXCLUDING P4.D116's three dirs above | SPA (unifier recounts) |
| P4.D118 | `crates/quilltap-cli/**`, the Tier R files in `crates/quilltap-harness/**` | cli, harness (Tier-R files) |

**The qt-class seam with P4.D117:** this lane transcribes v4's class strings
verbatim (including any `hover:qt-*`/opacity forms v4 `44a8137e` uses —
that commit postdates v4's qt fix, so its class names are already the FIXED
spellings); P4.D117's guard runs over the union at unification and any
residual inert name in this lane's files is fixed there, not by P4.D117
editing this lane's dirs mid-round.

`docs/CHANGELOG.md` + `status-log.md` append-only for everyone; nobody edits
`drift-ledger.md` from a lane.

## Verification gate (before declaring done)

- `npm test` (never bare `ng` — memory: `ng-test-defaults-to-watch-mode.md`)
  full SPA suite green; `npm run build` clean.
- The parity specs red-first where they pin a v4 behavior v5 lacked (the
  Custom…-always-opens bug must be DEMONSTRATED red before the projection
  read fixes it, at spec tier).
- The gated e2e beat compiled and runnable in its gated-off state; the full
  Playwright suite green locally (coordinate the 4319 port with P4.D117).
- Per-commit rule: `.claude/commands/commit.md`. Lane record appended to
  `status-log.md`.
