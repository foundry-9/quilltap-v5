# Work order P4.D151 — verify a describer saw the image (bug 116) + the NanoGPT manifest convergence check (bug 118) — v4 `0b0617fee`, `b448eddd7`

**Status:** OPEN (ordered 2026-09-02, the `0b0617fee` drift catch-up round:
P4.D148 ∥ P4.D149 ∥ P4.D150 ∥ P4.D151 ∥ P4.D152).

## Preamble

- **v4 oracle baseline: `6d2a50382`** (drift-ledger §1, probe re-run at
  ordering 2026-09-02: v4 on `main` at `0b0617fee`, tree clean, both logs
  empty). **This lane's target commit is `0b0617fee`** — *"fix(images):
  verify a describer saw the image; hash the bytes we store (bugs
  116-118)"* (28 files, +1445/−65) — **the bug-116 and bug-118 legs only**
  (`lib/chat/file-attachment-fallback.ts` +124, `plugins/dist/
  qtap-plugin-nanogpt/manifest.json` +13/−3 + its `package.json`,
  `__tests__/unit/lib/chat/file-attachment-fallback.test.ts` +208,
  `__tests__/unit/lib/llm/image-transport.test.ts` +98). Bug 117's legs are
  **P4.D152's**. This lane also RATIFIES `b448eddd7` (docs-only: the three
  bug filings + `bugs.md` + CHANGELOG — zero lib/app/packages/plugins) with
  its file list in the lane record. Read the hunks: `git -C
  ~/source/quilltap-server show 0b0617fee -- lib/chat/
  file-attachment-fallback.ts plugins/dist/qtap-plugin-nanogpt`.
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 on `main`,
  tree clean, `git log 0b0617fee..main` empty and `git log
  3a76b17df..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED** (ledger §1). Every regen runs from a
  lane-unique detached worktree at **`0b0617fee`**
  (`/tmp/qt-v4-pin-p4d151-0b0617fee`, three symlink classes, ledger §5.1)
  through the sweep driver. ⚠ **This pin is v4 HEAD and carries ALL FIVE
  drift commits.** Regenerate ONLY this lane's families from it; none of
  them touches the chats route, the distill, or the files/import/restore
  paths, but if a regenerated NDJSON moves on bytes this lane did not
  port, measure the shape FIRST and mask by name with a tripwire
  (P4.D143's `BACKGROUND_MODE_PENDING_*` precedent; memory note "a lane
  pin can contain a sibling lane's drift"). Verify the pin by grepping a
  fresh file-attachment NDJSON for `did not process the image`.
- **Sibling lanes:** P4.D148/P4.D149 (Concierge at creation), P4.D150 (bug
  115 — do NOT touch `distill.rs`/`build_context.rs`/`pre_compute.rs`/
  `recall_replay.rs`), P4.D152 (bug 117 — owns `chat_files.rs`, the
  import/restore files regions, the boot heal; **do NOT touch them**, and
  §D gives `attach_mount_file_equivalence` to THIS lane). §Shared
  contracts / §Ownership binding and identical across all five orders.
- **Worktree:** per the `carryout` skill (branch `claude/p4-d151-…`).
  `CHANGELOG`/`status-log.md` append-only. **No Playwright** (§F).

## Mandate

Port bug 116 whole: `verify_image_reached_model(response, attachment_id)
-> ImageArrivalVerdict` with the two constants (`MIN_CHARS_PER_TOKEN =
2.5`, `INSTRUCTION_TOKEN_CEILING = ceil(len(IMAGE_DESCRIPTION_INSTRUCTION)
/ 2.5)` — compute it from the constant the way v4 does; do not hard-code
66, but ASSERT it equals 66 in a unit test as the live-call anchor), called
in `describe_image_with_profile` AHEAD of every content check, with the
warn line, the long `type:'unsupported'` sentence and the five
`processingMetadata` keys; the `CompletionResponse.cache_usage` widening
(§B) that the cache-read arm needs, populated from the real composition;
bug 118 as a regen + byte-identity proof (§E) plus the NanoGPT comment at
`chat_completions.rs:46-52` updated to say v4 now agrees. Must NOT touch:
`chat_files.rs` / `quilltap_import/files.rs` / the restore orchestrator
(P4.D152's — §D), the distill/build-context set (P4.D150's), the SPA, the
describe-tier's OTHER arms (the empty/refusal/reasoning checks are
unchanged in v4 — leave their bytes alone).

## Survey-verified starting points (2026-09-02, from the shipped hunks — ledger §5.3)

1. **The verdict function** (v4 `file-attachment-fallback.ts:36-126`):
   order is (a) `attachmentResults.failed` non-empty → pick the entry whose
   `id === attachmentId`, else `failed[0]`; reason `the provider reported
   the attachment as not sent: ${mine.error || 'no reason given'}` (JS
   `||` — an EMPTY error string takes the fallback); (b) `promptTokens`
   not a number or `<= 0` → `{arrived: true}` (SILENCE); (c) `billedInput
   = promptTokens + (cacheUsage.cacheReadInputTokens ?? 0) + (cacheUsage.
   cachedTokens ?? 0)`; `<= INSTRUCTION_TOKEN_CEILING` → `{arrived: false,
   reason: "the model was billed for ${billedInput} prompt tokens, which is
   no more than the ${CEILING} the instruction costs on its own — the
   image was accepted and discarded before it reached the model, and any
   description returned is invented"}`; else arrived. v5 `services/
   file_fallback.rs` — the instruction constant is already there (grep
   `IMAGE_DESCRIPTION_INSTRUCTION`); `describe_image_with_profile` is
   `:421-660`; the success path `:571` reads `resp.content` and goes
   straight to the empty check `:585` and the refusal-keyword check
   `:611` — INSERT the verdict between the `log_description_success` call
   (`:573-583`, which must still run — v4 logs the call before judging)
   and `let trimmed = …` (`:584`).
2. **The response's two proofs in v5.** `model/completion.rs:149-162`
   `CompletionResponse { content, usage: Option<CompletionUsage
   {prompt_tokens,…}>, finish_reason, attachment_results:
   Option<StreamAttachmentResults {sent, failed: Vec<{id, error}>}> }` —
   **NO `cache_usage` field** (v4's `LLMResponse.cacheUsage`). §B: add
   `pub cache_usage: Option<crate::model::stream::StreamCacheUsage>`
   (`model/stream.rs:224-229`: `cached_tokens`, `cache_discount`,
   `cache_creation_input_tokens`, `cache_read_input_tokens`); the real
   composition `model/completion_provider.rs:252-262` builds from
   `parsed` (`response_parse.rs:74` already carries `cache_usage`) — thread
   it; every other construction site gets `cache_usage: None` — the census
   at ordering: core `model/completion.rs:385`, `services/compression.rs:
   417`, `services/cheap_llm_exec.rs:1482,1514,1597,2017,2200`,
   `services/memory_recap/mod.rs:801`, `services/outfit_selections.rs:
   1359`; harness `title_update_tier3_equivalence.rs:144`,
   `settings_wire_actions.rs:99`, `attach_mount_file_equivalence.rs:444`,
   `chat_regenerate_title_tier3_equivalence.rs:114`,
   `answer_confirmation_tier3_equivalence.rs:446`,
   `precompute_equivalence.rs:241`, `danger_gatekeeper_tier3_equivalence.rs:
   345`, `recall_replay_equivalence.rs:120`, `file_attachment_tier3_
   equivalence.rs:339`, `outfit_llm_choose_tier3_equivalence.rs:90`. Re-grep
   at lane start (`ggrep -rn "CompletionResponse {" crates/ | ggrep -v "pub
   struct"`); the compiler finds the rest.
3. **The refusal arm** (v4 `:510-539`): warn `[Image Fallback] Describer
   answered without the image; discarding its description` with `provider,
   model, profileId, filename, reason, promptTokens, contentLength`
   (`response.content?.length ?? 0` — UTF-16 units); the result is
   `type:'unsupported'` with error `Image description profile
   (${provider} ${modelName}) did not process the image — ${reason}. Pick
   a describer on a model that genuinely reads images; a gateway may
   accept an image and route to a model that ignores it.` and
   `processingMetadata {originalFilename, originalMimeType,
   descriptionProfileId, descriptionProvider, descriptionModel}` — v5's
   `FallbackResult::unsupported(...)` + `set_description_metadata(...)`
   (`:119`, `:669`) build exactly that pair; use them. The verdict FAILS
   INTO the existing fallback chain (`describe_via_fallback_chain` `:1056`
   advances to the uncensored describer on an `unsupported` result — v4's
   third new test "advances to the uncensored fallback when the primary
   never saw the image" is a chain-level arm).
4. **The corpus is BLIND today.** `fixtures/file-attachment.json`'s
   `vision[]` entries carry `{provider, modelName, filename, content,
   finishReason, usage}` (`:288-330`); the oracle's canned `sendMessage`
   (`cases/file-attachment-tier3.test.ts:183-229`) returns `{content,
   finishReason, usage}` and records the same; the v5 canned provider
   (`file_attachment_tier3_equivalence.rs:325-350`) builds
   `CompletionResponse { …, attachment_results: None }`. Extend the entry
   shape with OPTIONAL `attachmentResults` and `cacheUsage`, returned by
   the mock AND recorded in the `kind:"canned"` row, and carried into the
   v5 canned response. Every existing entry has `promptTokens` ≥ 820 or
   `usage: null` — both ARRIVE; the new arms need entries at 38 (the live
   bad call), at 66 exactly (the boundary — `<=` refuses), at 67, one with
   `promptTokens: 20` + `cacheUsage.cacheReadInputTokens: 800` (arrives —
   the add-back), one with `attachmentResults.failed: [{id, error: ''}]`
   ("no reason given"), and a chain case (primary refused by verdict →
   uncensored describer answers). ⚠ Note bug 91's class in the fixture: the
   `attachmentId` the verdict matches on is the attachment's `id` — the v5
   canned key carries `id: String::new()` (P4.21); the `failed` entry
   matching is by id, so seed the failed entry's id to the real
   attachment id on BOTH sides or the `?? failed[0]` fallback is what's
   measured (say which).
5. **The pure half as tier 1:** `verifyImageReachedModel` is EXPORTED.
   `fallback_engine_equivalence` (`QT_ORACLE_FALLBACK_ENGINE`,
   `cases/fallback-engine.ts` — a tier-1 pure family) gains a `verdict`
   kind over v4's five unit shapes (`file-attachment-fallback.test.ts`'s
   new `describe('verifyImageReachedModel')` block: long-confident-answer-
   billed-for-instruction, plugin-reported-not-sent, well-above,
   silence, cache-reads-added-back) plus the boundary pair, comparing
   `{arrived, reason}` byte-for-byte.
6. **Bug 118 — v5 does NOT have it (measured at the drift check):**
   `provider_manifest/manifests/nanogpt.json:148-157` already reads
   `supportsAttachments: true` + the four MIME types + `"Images (JPEG, PNG,
   GIF, WebP) — requires a vision-capable routed model"`, because the
   generator regenerates from the plugin's BUILT declaration (the truthful
   one), not `manifest.json`. Deliverable: run
   `harness/oracle/providers/gen-provider-manifests.mjs` from the pin
   (its header carries the invocation) and prove all ten manifests
   byte-identical (`git status` clean after the regen — record it); update
   the `chat_completions.rs:46-52` comment ("the two homes now agree —
   `0b0617fee` flipped v4's `manifest.json` onto these bytes"). If the
   regen is NOT a no-op, that is a finding — measure which key moved
   before touching anything (memory note `manifest-generator-augmentation-
   rot`).
7. **`attach_mount_file_equivalence`** (`QT_ORACLE_ATTACH_MOUNT_FILE`) —
   its canned describer (`:444`) gains the `cache_usage: None` line and
   the family regenerates at the pin; if its describe arm can express a
   low-`promptTokens` canned call, add ONE refusal row there too (the
   attach path reaches `describe_image_with_profile` through the auto-
   describe module); otherwise run-only and say so.
8. **Help → the `p4.9i2` bank:** `help/file-uploads.md` (+10) and
   `help/chat-settings.md` (+3), appended to the bank with the sha.

## Tiered deliverables

- **Tier 1 (must land):** points 1–5 RED FIRST (the 38-token case must
  produce a persisted description on v5 pre-fix and an `unsupported`
  result post-fix — record the red), the §B widening with every site,
  point 6's regen proof + comment, the `b448eddd7` ratification with file
  list, point 8.
- **Tier 2 (should land):** point 7's refusal row on the attach path; the
  boundary pair (66/67) as separate corpus rows.
- **Tier 3 (explicit deferrals, loud):** v4's four test files as files are
  NO-PORT (their behaviour is the corpus + the tier-1 verdict arms).

## The differential requirement (restated)

- `file_attachment_tier3_equivalence` (`QT_ORACLE_FILE_ATTACHMENT`,
  `QT_FIXTURE_FILE_ATTACH_{MAIN,MOUNT}`; recipe in the test header —
  builder `fixtures/build-file-attachment-fixture.ts`) — the widened
  `vision[]` entries + the six new cases, v4's REAL `describeImageWithProfile`
  under the REAL chain; grep the fresh NDJSON for `did not process the
  image` and for `no reason given` (ledger §5.2).
- `fallback_engine_equivalence` — the `verdict` kind, byte-compared.
- `attach_mount_file_equivalence` — regenerated + run (Tier 2 row if
  expressible).
- The manifest generator run (§E) with `git status` proof.
- Mutation proofs: drop the cache add-back → the cache-read case reddens
  (it refuses); swap `<=` for `<` → the 66 row reddens; move the verdict
  AFTER the empty check → a refused-and-empty case reddens (sentence
  order); read `failed[0]` unconditionally → the matching-id case reddens
  (seed two failed entries); revert the composition's `cache_usage` thread
  → the tier-3 cache case reddens ONLY if the canned provider carries the
  value from the corpus — make sure it does, then record that the REAL
  composition's thread is pinned by a unit test in `completion_provider.rs`
  (a stub server returning `usage.prompt_tokens_details.cached_tokens` — or
  whichever key `response_parse` already maps — and asserting
  `cache_usage` on the returned `CompletionResponse`).

## Fixtures

- Consumes: `file-attachment.json` + its builder (WIDENED — this lane owns
  them), the attach-mount-file case, the manifests.
- Delivers: the widened spec; no committed DB changes expected.

## Shared contracts (binding, identical across all five orders)

**§A — the create-time Concierge wire (P4.D148 ↔ P4.D149).** The
`chatCreate` dispatch verb's request (v5 `ChatCreateRequest`, SPA
`ChatCreateRequest` in `core-contract.ts`) gains ONE optional key:

```
conciergeState?: 'monitored' | 'flagged' | 'vouched' | 'uncensored'
```

(v4 `createChatSchema`: `z.enum([...]).optional()` — spelled out, not derived;
`.optional()` is NOT nullable, so an explicit JSON `null` REJECTS like
`timestampConfig`'s does.) Absent ≡ `'monitored'`: no write, no announcement,
the request AND the created chat byte-identical to today's. Any other value is
applied through the EXISTING `apply_concierge_flip` chokepoint (v4
`applyConciergeFlip(chat.id, requested, chat)` with the fresh row) on ALL
THREE create branches (continuation / autonomous / ordinary), immediately
AFTER the system-prompt message and BEFORE `apply_chat_continuation` /
scenario / staff / greeting, so the Concierge bubble is the first row after
the prompt and the greeting is generated under the chosen state. The
announcement reuses v4's five `manual-*` kinds (`RealConciergeAnnouncer`) —
NO new sentences. The SSE progress status line is exactly
`Briefing the Concierge…` (U+2026), emitted only when a non-Monitored state
is applied, between the branch's existing status and the scene status. An
unknown string → the create schema's validation 400 (the envelope
`tc_bad_value_rejected` already pins). The CLIENT omits the key when the
form holds `'monitored'`. v5 serves NO REST `POST /api/v1/chats` (the SPA
creates through `POST /api/dispatch`; `chats_routes.rs` has the collection
GET only) — recorded, not this round's.

**§B — `CompletionResponse.cache_usage` (P4.D151 owns the widening).**
`model::completion::CompletionResponse` gains
`pub cache_usage: Option<crate::model::stream::StreamCacheUsage>` (v4
`LLMResponse.cacheUsage`), populated from `parsed.cache_usage` in the real
`completion_provider.rs` composition and `None` at every other construction
site. **P4.D151 edits every construction site itself, including the
`cache_usage: None` one-liners in harness tests owned by other families**
(`precompute_equivalence.rs`, `recall_replay_equivalence.rs`,
`title_update_tier3_equivalence.rs`, `settings_wire_actions.rs`,
`attach_mount_file_equivalence.rs`, `chat_regenerate_title_tier3_equivalence.rs`,
`answer_confirmation_tier3_equivalence.rs`, `danger_gatekeeper_tier3_equivalence.rs`,
`file_attachment_tier3_equivalence.rs`, `outfit_llm_choose_tier3_equivalence.rs`).
No other lane edits those harness files this round (P4.D150's pins are
core unit tests, not harness edits) — that is what keeps the pick clean.

**§C — `distill_memory_search` (P4.D150 owns).** The function gains a
trailing `options: CheapLlmTaskOptions` parameter threaded to
`executor.execute(...)` (v4 `latency: CheapLLMLatencyClass = 'background'`
→ `{ latency }`). Its three callers: `build_context.rs` (the dynamic-head
FALLBACK) passes `CheapLlmTaskOptions::interactive()`; `pre_compute.rs` and
`recall_replay.rs` pass `CheapLlmTaskOptions::default()`. No other lane
touches `services/memory_recap/distill.rs`, `services/build_context.rs`,
`services/pre_compute.rs`, or `services/recall_replay.rs`.

**§D — the `files.sha256` invariant (P4.D152 owns).** `files.sha256`,
`mimeType` and `size` ALL describe the bytes actually STORED (post-bridge),
never the writer's input — at chat upload, `.qtap` import, restore replay
AND the carried-store-rows restore branch (which reads the archived
`doc_mount_blobs.sha256` by the parsed blob id). The boot heal carries v4's
migration id `realign-file-entry-sha256-v1` in the P4.D140 ledger shape
(honour an existing `migrations_state` row from either app; write the row
only on a pass that realigned ≥ 1 row). P4.D152 does NOT touch
`services/file_fallback.rs` (the describe tier reads `files` by id, never
by sha) and does NOT edit `attach_mount_file_equivalence.rs` (P4.D151's);
its join proof is the NEW heal family + the state dumps.

**§E — the manifest substrate (P4.D151 owns for this round).** The generated
`provider_manifest/manifests/*.json` are regenerated ONCE, by P4.D151, from
the `0b0617fee` pin through `harness/oracle/providers/gen-provider-manifests.mjs`;
the expected result is BYTE-IDENTICAL (bug 118 is a v4 convergence onto
bytes v5 has carried since P4.D107). No other lane runs the generator.

**§F — Playwright.** Port 4319 is P4.D149's this round. P4.D149's
create-time beat is authored GATED behind `const P4D148_SERVER_LANDED =
false` (ACTIVATE-AT-UNIFY — the unifier flips it after P4.D148 lands and
runs it live); every other lane authors NO Playwright and says so.

**§G — the drift ledger.** Lanes never write `drift-ledger.md`; the five §3
rows were marked ORDERED at ordering, and `/unify` marks them ABSORBED /
NO-PORT-RATIFIED when the baseline moves to `0b0617fee`.

## Ownership (binding, identical across all five orders)

| Area | Owner |
|---|---|
| `quilltap-core`: `services/chat_create.rs` (the request field, `apply_requested_concierge_state`, the three branches, `auto_generate_first_message`'s attempt 0 + the shared uncensored-desk closure, the five log lines); harness: `chat_create_capstone_equivalence` (+ `cases/chat-create-capstone.test.ts`, `fixtures/chat-create-capstone.json`, `fixtures/build-chat-create-capstone.ts`), `initial_greeting_equivalence` + `first_message_context_equivalence` (run, not changed) | **P4.D148** |
| SPA: `screens/new-chat/{new-chat-form.ts, new-chat.types.ts, new-chat.state.ts, new-chat.logic.ts}` (+ specs), `core/core-contract.ts` §A region, `e2e/new-chat-flow.spec.ts` (the gated beat); the `p4.9i2` bank rows for `help/chats.md` + `help/dangerous-content.md` | **P4.D149** |
| `quilltap-core`: `services/memory_recap/distill.rs`, `services/build_context.rs` (the fallback-distill site + the inter-character log line + its unit pins), `services/pre_compute.rs`, `services/recall_replay.rs`; harness: `build_context_tier3_equivalence`, `precompute_equivalence`, `recall_replay_equivalence` (RUN only — no edits, §B) | **P4.D150** |
| `quilltap-core`: `services/file_fallback.rs` whole, `model/completion.rs` (§B), `model/completion_provider.rs` (§B), the `cache_usage: None` one-liners at `services/compression.rs`, `services/cheap_llm_exec.rs`, `services/memory_recap/mod.rs`, `services/outfit_selections.rs` + the harness sites listed in §B, `model/request_builder/chat_completions.rs` (the NanoGPT comment), `provider_manifest/manifests/*.json` (§E regen, expected no-op); harness: `file_attachment_tier3_equivalence` (+ `cases/file-attachment-tier3.test.ts`, `fixtures/file-attachment.json`, its builder), `fallback_engine_equivalence` (+ `cases/fallback-engine.ts`), `attach_mount_file_equivalence` (+ its case); the `p4.9i2` bank rows for `help/file-uploads.md` + `help/chat-settings.md`; the `b448eddd7` NO-PORT ratification | **P4.D151** |
| `quilltap-core`: `services/chat_files.rs`, `services/quilltap_import/files.rs`, `services/backup/restore/orchestrator.rs` (the files region — `restore_one_file` / `carried_store_rows` and the replay branch ONLY), NEW `db/files_sha256_realign_heal.rs` + its `db/mod.rs` line; `quilltap-host`: `host.rs` (one boot-chain block); harness: NEW `files_sha256_realign_heal_equivalence` (+ its oracle case + fixture builder), `files_tier2_equivalence` / `files_routes_equivalence` (the upload sha arm), `qtap_import_equivalence`, `system_restore_state` / `system_restore_equivalence` / `restore_vintage_state` (the sha arms + any NEW committed archive), `character_photo_upload_tier2_equivalence` + `photo_tools_equivalence` (run), a harness-only byte-changing `PixelCodec` twin | **P4.D152** |
| Playwright (port 4319) | **P4.D149's branch only** (§F) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it (§G) |

**Version bumps:** P4.D148 bumps `quilltap-core` + `quilltap-harness`;
P4.D149 bumps the SPA `package.json`; P4.D150 bumps `quilltap-core` (+
`quilltap-harness` only if a harness file changes — §B says none should);
P4.D151 bumps `quilltap-core` + `quilltap-harness`; P4.D152 bumps
`quilltap-core` + `quilltap-harness` + `quilltap-host`. Per commit; the
unifier recounts at unification (the playbook's silent-auto-merge trap).

## Verification gate before declaring done

`cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
-D warnings` on BOTH feature sets (the §B widening touches ten harness
binaries — the full workspace build is the proof they all compile); full
`cargo test --workspace` with the lane's env vars (`run_in_background:
true`, full log + sentinel — NEVER a pgrep loop, NEVER `| tail`); the
three families by name through the sweep driver from the `0b0617fee` pin,
zero SKIP, changed-bytes grepped; every harness family whose canned
provider gained the one-liner RUN by name (their recipes' env vars, at the
pin — they must not move); the manifest regen `git status` clean; mutation
proofs verified-applied and reverted by file backup. No SPA gate (no SPA
file touched — say so). Per-commit rule: `.claude/commands/commit.md`
(CHANGELOG entry per commit, core + harness bumps, lane record appended to
`status-log.md` under "P4.D151").
