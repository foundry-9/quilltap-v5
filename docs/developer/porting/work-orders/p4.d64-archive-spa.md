# Work order P4.D64 — the character-archive SPA surface — v4 `d553f72a` (incl. the `d69287d9` button fix)

**Status:** CLOSED — carried out and UNIFIED on main (2026-08-11, the
P4.D62∥P4.D63∥P4.D64 round-1 unification). All six units landed (contract
mirror + data layer, roster, detail + both dialogs, members/participant
badges, the four settings surfaces, the beats + the tombstone seeder); every
quoted string byte-exact; `apps/web/**` only. **The seeder deliberately
diverged from this order's Dax suggestion** — Dax anchors seven post-office
assertions, so the seeder builds a self-contained island (Marchpane / The
Dust Sheets / The Shuttered Wing) instead; rationale in the unit-6 lane
record. At unification `ARCHIVE_TOMBSTONE_SEEDED` flipped TRUE and the six
tombstone-read beats ran live (first-ever seeder execution).
**`CHARACTER_ARCHIVE_SERVER_LANDED` stays FALSE — round 2 flips it** (the
four action beats name round 2 in their skip messages). Tier-3 note carried
in code at `character-header.ts`: the archived gating of
optimize/search-replace/external-prompt is moot while those are disabled
deferrals; whichever lane makes one live must hide it when archived.

## Preamble

- **v4 baseline for this round: `d553f72a`** (v4 4.8.0-dev, HEAD at planning
  2026-08-10, tree CLEAN — verified at planning). This is **round 1 of 2**
  of the character-archive drift catch-up: this lane ports the **whole
  client surface** of `01e481f6` + `d69287d9` into the Angular SPA, built
  against the Shared contract. The archive/rehydrate ACTIONS answer a loud
  refusal until round 2 — the action beats land gated; the tombstone
  READ surfaces (roster/badges/read-only detail/counts) activate at this
  round's unification over a fixture-seeded archived character.
- **Drift-check at lane start.** In `~/source/quilltap-server`, run
  `git log d553f72a..HEAD --oneline`. If v4 moved, **STOP** and report.
- **Sibling lanes this round (disjoint ownership):**
  - **P4.D62** — the export/import/file-storage substrate (server). Its
    export-preview `vaults` block is this lane's wizard-hint dependency.
  - **P4.D63** — the schema/guards/chokepoint/crypto (server). Its
    `archived=` filter, projections, refusal-armed verbs, wipe/restore
    options, and change-passphrase `archives` bag are this lane's wire.
- **Worktree:** carry the lane out on its own branch per the `carryout`
  skill. **`npm ci` in the worktree, never `cp -a` node_modules from main**
  (`spa-worktree-node-modules` — NG0203).

## The drift, classified (surveyed 2026-08-10; strings quoted verbatim — v4's writing voice is byte-contractual)

### Roster (`app/aurora/AuroraView.tsx` → `screens/characters/list/characters-list.ts`)

- A plain **toggle button** (no URL state), local state default OFF, FIRST
  in the toolbar row, `folder` icon. Label `Show Archived` / `Hide
  Archived`; titles `Show characters resting in the archive` / `Tuck the
  archived characters back out of sight`; active styling
  `qt-bg-primary/20 qt-text-primary`, inactive `qt-bg-muted/70
  qt-text-primary hover:qt-bg-muted`.
- Fetch: `archived=include` when on, param omitted when off; the two
  states cache under DISTINCT query keys (`list({archived:'include'})` vs
  `list(undefined)`); mutation invalidation widens to the `characters`
  prefix ("so the other archived-filter variant refreshes too").
- Sort rule 0 (ahead of the existing three): archived last.
- Card: `Archived` badge next to the name (`qt-badge … qt-bg-muted px-2
  py-0.5 text-xs`, title `` `Resting in the archive since
  ${toLocaleDateString(archivedAt)}` ``); the favorite/Carina/controlledBy
  cluster hidden when archived; Chat + export actions replaced by ONE
  inert span — text `Resting in the archive`, title `An archived character
  neither chats nor exports; open their page to rehydrate them.` — Delete
  stays. **v5's card has an extra Export-PNG action v4's lacks: the inert
  span covers Chat + BOTH exports** (v4's intent: archived characters
  neither chat nor export).

### Detail (`CharacterDetailView.tsx` → `screens/characters/view/character-detail.ts`)

- `isArchived = Boolean(archivedAt)`; state: archive dialog open,
  rehydrating, `leftoverBundleFileId`.
- Archived banner above the header (rounded-2xl `qt-bg-muted/60`, 🗄️
  `aria-hidden`): label `{name} rests in the archive.`; body `Their
  effects — memories, correspondence, photographs, summaries — are packed
  into a sealed bundle on the shelf. What you see below is kept for the
  record and may be read but not altered; rehydrate them to pick up the
  pen again. Old conversations keep their words and their face
  throughout.`
- **All tabs render**, wrapped `<fieldset disabled class="opacity-90">` —
  carry v4's comment: "every form control inert, the page still readable.
  The repository write guard is the real lock — this is the courtesy."
  (Angular: a `fieldset[disabled]` wrapper in the tab host — verify it
  actually inerts the qt form fields; where it can't, mirror v4's
  per-control fallbacks.)
- Header callbacks gated: optimize/search-replace/external-prompt pass
  undefined when archived (moot for v5's still-disabled deferrals — note
  it); `onArchive` only live; `onRehydrate` only archived.
- Archive confirm: `POST …?action=archive` (dispatch `characterArchive`).
  Success toasts: pruneComplete false → `` `${name} is archived, but some
  effects resisted packing — archive them again to finish the sweep.` ``;
  else `` `${name} rests in the archive, bundle sealed and shelved.` ``
  (name falls back to `The character`). Error toast: the message or
  `Failed to archive character`. Then refetch + prefix invalidation +
  header-stats refresh.
- Rehydrate: `POST …?action=rehydrate`. Toasts: with restored → `` `${name}
  is awake again — ${memories} memories, ${documents} papers and ${blobs}
  photographs unpacked.` ``; without → `` `${name} is awake again.` ``.
  Non-empty `archiveBundleFileId` opens the RehydrateBundleDialog;
  `onDeleted` toast `The bundle is off the shelf.`

### Header (`CharacterHeader.tsx` → `view/character-header.ts`, incl. `d69287d9`)

- Name-row `Archived` badge (px-2.5, text-sm variant; same title format);
  toggle cluster hidden when archived.
- Actions column forked: archived → single **Rehydrate** button
  (`bg-primary … disabled:opacity-50`, `disabled` while rehydrating, label
  `Rehydrating…`/`Rehydrate`, title `Restore this character from their
  archive bundle and wake them`); live → existing cluster + **Archive**
  button at the END (after v5's disabled deferrals), `folder` icon, title
  `Pack this character's effects into a sealed bundle and set them resting
  in the archive`. **Port the `d69287d9`-fixed classes** —
  `text-foreground`, matching siblings exactly (the original
  `qt-text-secondary` read as disabled): `inline-flex items-center
  justify-center gap-1.5 rounded-lg border qt-border-default qt-bg-card
  px-4 py-2 qt-label text-foreground qt-shadow-sm hover:qt-bg-muted`.

### `ArchiveCharacterDialog` (new)

Confirm dialog (max-w-lg, `max-h-[85vh]` scrollable; `working` disables
both buttons). Title `Set {characterName} resting in the archive?`. Lead:
`Archiving packs the whole of {characterName} — every last letter and
photograph — into a single sealed bundle on the archive shelf, then clears
the heavier effects from the working rooms. Nothing is lost; it is merely
put away, and rehydrating unpacks it all again.` Pruned box (`Packed into
the bundle and cleared away:`): `Their memories (the Commonplace Book
falls silent)` / `Their correspondence — the whole of the mail folder` /
`Every photograph beyond the portrait itself` / `Their conversation
summaries`. Kept box (`Kept in place, exactly as it stands:`): `Who they
are — every character field, still readable on their page` / `Their
portrait, so old conversations keep their face` / `Their wardrobe` /
`Every chat they took part in, word for word` / `What <em>other</em>
characters remember about them — archiving silences the character, not
everyone's memory of them`. Footer: `While archived they take no turns,
receive no letters, and answer no queries. The bundle is sealed with your
passphrase and rests at ` + monospace `files/<id>/character-archive.qtap`
+ `.` Buttons: secondary `Leave Them Be`; primary `Archive` →
`Packing the bundle…`.

### `RehydrateBundleDialog` (new — the keep/discard deviation)

Title `The empty bundle remains on the shelf`. Body 1: `{characterName} is
fully unpacked — memories, correspondence and photographs all back where
they belong. The sealed bundle they travelled in still sits in the file
library, and keeping it costs nothing but shelf space: it is a spare copy
of everything the archive held, exactly as it was.` Body 2: `Discard it
and the spare copy is gone for good — though of course you can always
archive {characterName} afresh, which packs a new bundle from their
current state.` Inline error default `Failed to discard the bundle`.
Buttons: **secondary** `Discard the Bundle` → `Clearing the shelf…`
(DELETE the file; `onDeleted` then close); **primary** `Keep It` (the
polarity is deliberate).

### The rest

- `CharacterDetails` → `view/tabs/details-tab.ts:111–115`: the
  `Edit Character` link hides when archived (carry v4's comment: "A
  disabled fieldset can't inert a Link, so the edit door hides itself…").
- `GroupMembersCard` → `screens/groups/group-members-card.ts:115`: count
  line appends `` ` / ${n - archived} can speak (${archived} archived)` ``
  only when archived > 0 (e.g. `3 members / 2 can speak (1 archived)`);
  per-member `Archived` badge, title `Resting in the archive — still a
  member, but takes no turns until rehydrated`.
- `ParticipantCard` → `chat/sidebar/participant-card.ts:153–158`: an
  `Archived` badge REUSING `qt-badge-absent text-xs`, AFTER the Absent
  badge (both can show), title `Resting in the archive — rehydrate them
  from their character page to let them speak again`; reads the new
  participant-enrichment `archivedAt`.
- `ChangePassphraseCard` → `settings/system/change-passphrase-card.ts`:
  mount-time courtesy count via the files `category=ARCHIVE` list
  (failure swallowed); the warning paragraph (singular/plural, verbatim
  per the survey: `Your 1 archived-character bundle is sealed under this
  passphrase and will be rewritten to open with the new one.` / `Your {N}
  archived-character bundles are sealed…each…` + ` This cannot be
  interrupted halfway without leaving some archives on the old
  passphrase; if that happens, the ones left behind are named below.`);
  the response `archives` bag handling — success append (`Your
  archived-character bundle was rewritten under the new passphrase.` /
  `All {N} archived-character bundles were rewritten…`), failure alert
  (`One archived-character bundle could not be rewritten and still
  expects the old passphrase:` / `{N} archived-character bundles could
  not be rewritten and still expect the old passphrase:` + `{filename} —
  {reason}` bullets).
- `delete-data-card.ts`: `keepArchives` default TRUE, reset on
  open/close; POST body gains `keepArchivedCharacterBundles`; preview row
  `Archived Character Bundles` → `{n} (kept)` while ticked; checkbox
  `Leave the archived-character bundles on the shelf ({n} {file|files})`
  + copy `Their character records perish with everything else, so what
  remains is a loose bundle apiece — each may be imported afresh, but
  none can simply be woken. Untick to sweep the shelf bare as well.`;
  completion note `{n} archived-character {bundle remains|bundles remain}
  on the shelf, ready for import.`; the `personas ?? 0` NaN fix + the
  `max-h-[85vh] flex flex-col` chrome hardening.
- Export wizard (`export-dialog.ts`): `vaultPreview` state; the advisory
  `loadVaultPreview` fired ONLY on entering the options step with
  `characters` selected; the card when `stores > 0`: heading `Every
  character travels with their vault`; body `{n} {vault|vaults} packed
  and labelled` [+ `: ` + `{n} {paper|papers}` / `{n}
  {photograph|photographs}` joined with ` and `] + `. Expect a trunk of
  roughly {formatBytes(bytes)} — the photographs, as ever, are the heavy
  luggage.` (`formatBytes` exists at `src/app/ui/format-bytes.ts`; port
  the `count(n, singular, plural)` helper with its comment). **Reconcile
  the stale contract comment** ("v4 has no export-preview route" — it now
  does).
- Restore dialog (`restore-dialog.ts`): `keepArchiveBundles` default
  TRUE, sent in both modes; the replace-mode-only checkbox `Leave any
  archived-character bundles on the shelf` + copy `The wipe that precedes
  the restore will spare each archived character's .qtap bundle. Their
  character records are replaced with the backup's like everything else,
  so a spared bundle is a loose one — importable afresh, but not simply
  woken. Untick to sweep the shelf bare as well.`
- Contract mirror (`core-contract.ts`): `CharacterListRequest.archived`;
  `archivedAt` on `CharacterListItem` + `CharacterDetail` (+
  `archiveFileId`); `GroupMemberSummary.archivedAt`; participant
  character `archivedAt`; `SystemDeleteDataRequest` /
  `SystemRestoreExecuteRequest.keepArchivedCharacterBundles`; the
  change-passphrase `archives` response bag; the export-preview `vaults`;
  the two new verbs. **Diff name-for-name against P4.D63's `api/types.rs`
  at unification** — the §1 wire diff is the round's standing check.

## Mandate

Port the whole client surface faithfully (strings byte-exact, v4's voice),
against the Shared contract, with unit specs for every behavior fork and
e2e beats split into (a) tombstone-read beats ACTIVATE-AT-UNIFY over a
seeded archived character and (b) action beats gated
`CHARACTER_ARCHIVE_SERVER_LANDED = false` until round 2. Touch ONLY
`apps/web/**`.

## Survey-verified starting points (2026-08-10)

- Roster: `characters-list.ts` (toolbar `:116–148` — the toggle goes
  FIRST; `sortCharacters` `:56` — rule 0; delete formatter `:38`); card
  `character-card.ts`. Data layer `characters.api.ts` — `characterKeys.
  list(filter?)` currently omits the filter object entirely when absent
  (v4 keys `{}`; keep v5's existing convention, just add the
  `{archived:'include'}` variant as a DISTINCT key) and invalidations at
  `characters-list.ts:284/:360–370/:400` narrow to `list()` — widen to
  `characterKeys.all` per v4.
- Detail: `character-detail.ts` (EntityTabs `:145`, nine tabs);
  header `character-header.ts` (actions column `:181–230` — Archive slots
  after the disabled deferrals, `d69287d9` classes); details tab
  `:111–115`.
- Groups: `group-members-card.ts:115` (subtitle); mounted from
  `group-editor.ts:162`. Participant card:
  `chat/sidebar/participant-card.ts:153–158` (`qt-badge-absent` CSS
  exists at `_chat.css:2902`).
- Settings: `change-passphrase-card.ts` (`changePassphrase` verb `:146`),
  `delete-data-card.ts` (`systemDeleteDataPreview` `:242`; local
  `DeleteSummary` `:19`), `export-dialog.ts` (options step `:118`;
  `systemExportEntities` `:351`), `restore-dialog.ts` (replace confirm
  `:141`; `systemRestoreExecute` `:431`).
- Dialog/toast substrate: `ui/modal.ts`, `ui/toast.service.ts`,
  `qt-dialog-overlay` (the workbench precedent for backdrops).
- **Pickers need NO change** — v4's chokepoint defaults to exclude, so
  every existing picker is clean automatically once P4.D63 lands the
  default (verify one picker in a spec anyway).
- **Gating pattern**: constants at the top of the owning e2e spec file
  (never in `src/`), `test.skip(!CONST, '…')` or `describeOrSkip`, JSDoc
  "ACTIVATE-AT-UNIFY behind {@link CONST}" (`settings-almanack-flow.
  spec.ts:36`, `zz-delete-all-destructive.spec.ts:36` precedents).
- **e2e seeding note (for the unifier)**: the e2e instance copies
  `salon-main.db` + partitions, unlocked with a real passphrase
  (`open sesame please`) in global setup — so the archive happy path is
  reachable in round 2, and the passphrase-unavailable 400 arm is NOT
  reachable without a restart (do not write that arm as a beat). For
  round 1's tombstone-read beats, seed an archived character the
  delete-beat way (create a throwaway, then SQL-stamp `archivedAt` — or
  extend the fixture seeder): **Dax is the least-entangled fixture
  character; do NOT archive Aria** (the detail-view anchor of sibling
  beats). Seed `archivedAt` + flip that character's chat participants to
  `absent` to exercise the participant badge; leave `archiveFileId` NULL
  (a pre-bundle tombstone — v4-legal).
- The turn-banner/impersonation beats assert participant states — check
  the seeded tombstone doesn't shift any existing beat's turn rotation
  (the P4.D9 lesson: sends/seeds shift sibling baselines; prefer a chat
  no other beat walks).

## Tiered deliverables

**Tier 1 (must land):**

1. The contract mirror (all Shared-contract keys + the two verbs), the
   API layer (`characterArchive`/`characterRehydrate` calls, files
   `category=` list, the preview `vaults` read), the widened
   invalidations, the distinct-key archived list variant.
2. The roster surface: toggle (state, labels, titles, styling, position),
   sort rule 0, badge, hidden toggles, the inert-span action replacement
   (covering Chat + both exports), with unit specs (toggle refetch under
   the distinct key; sort; badge presence; inert span).
3. The detail surface: banner, fieldset-disabled tabs, header fork
   (Rehydrate/Archive buttons, `d69287d9` classes), gated callbacks, the
   Edit-link hide, both dialogs (all copy byte-exact), the toast set, the
   leftover-bundle flow. Unit specs per fork; the dialog copy pinned by
   spec (string assertions).
4. The group-members count line + badge; the participant-card badge over
   the new enrichment key. Unit specs (the existing `groups.spec.ts:89`
   `'3 members'` assertion extends to the archived variant).
5. The settings surfaces: change-passphrase (courtesy count, warning
   copy, `archives` bag rendering — all three alert shapes), delete-data
   (option, preview row, checkbox copy, completion note, NaN fix),
   export-wizard vault hint (advisory-only semantics), restore checkbox.
   Unit specs; the pre-seeded-DTO false-green trap applies — parity specs
   must re-fire on refetch (`f6eac168-drift-round-unification` memory:
   TanStack structural sharing; bump a per-fetch marker in stubs).
6. e2e: the tombstone-read beats (roster toggle → badge → inert card;
   detail read-only banner + Rehydrate button PRESENCE; group members
   line; participant badge) written ACTIVATE-AT-UNIFY behind
   `ARCHIVE_TOMBSTONE_SEEDED` with the seeding note above; the action
   beats (archive confirm → toast; rehydrate → keep/discard; delete-data
   option; change-passphrase archives alert) behind
   `CHARACTER_ARCHIVE_SERVER_LANDED = false` with round 2 named in the
   skip message.

**Tier 2 (should land):**

7. The stale "v4 has no export-preview route" contract-comment
   reconciliation; the delete-data dialog chrome hardening; a picker
   default-exclude verification spec.

**Tier 3 (explicit loud deferrals):**

- The archived gating of optimize/search-replace/external-prompt header
  callbacks — moot while those remain v5 disabled deferrals; note in the
  lane record so their future lanes inherit the gate.
- Flipping `CHARACTER_ARCHIVE_SERVER_LANDED` — round 2's unifier.

## The differential requirement, restated

This is a client lane: fidelity is pinned by (a) the Shared contract
diffed name-for-name against `api/types.rs` at unification, (b) byte-exact
string assertions in unit specs for every quoted sentence above, (c) the
ng test suite green, and (d) the beats. Where v4 client behavior is
subtle (the distinct-cache-key rule, the advisory-preview
never-blocks rule, the keep/discard polarity), write the spec BEFORE
wiring the behavior and mutation-prove it (the D24 rule applies to specs
that pass first-run).

## Fixtures

- Consumes the e2e instance (salon fixture family). **Owns** the
  tombstone-seeding recipe (written into the spec/JSDoc for the unifier;
  the seeder change itself may land here gated, or as the unifier's wire —
  name which in the lane record). Does not touch committed Rust-side
  fixtures.

## Shared contract (BINDING — identical in P4.D62 / P4.D63 / P4.D64)

Names are law; v4 shapes at `d553f72a`.

1. `characterList` gains `archived?: "include" | "only"` (absent/other =
   exclude, v4's default) — applied at the API list handler (the single
   chokepoint), never in the repository. List items gain
   `archivedAt: string|null` (ALWAYS present, `?? null`). Detail responses
   gain `archivedAt: string|null` + `archiveFileId: string|null`.
   **Owner: P4.D63** (P4.D64 mirrors).
2. NEW dispatch verbs `characterArchive { id }` / `characterRehydrate
   { id }` + REST `POST /api/v1/characters/{id}?action=archive|rehydrate`
   — DEFINED in P4.D63 answering the loud typed not-yet-available refusal
   (the P4.9G1 pattern); round 2 fills them. Round-2 success shapes,
   pinned now: archive → `{ archived: bool, archiveFileId: string|null,
   pruneComplete: bool }`; rehydrate → `{ rehydrated: bool, archived:
   bool, archiveBundleFileId: string|null, restored?: { memories,
   documents, blobs }, warnings: string[] }`. The passphrase-unavailable
   400 sentence: `The archive cannot be sealed: your passphrase has not
   been entered since Quilltap started. Unlock once (or restart and
   unlock), then archive again.`
3. The files list gains a `category` exact-match filter (REST
   `GET /api/v1/files?category=`; dispatch mirror). **Owner: P4.D63.**
4. `systemDeleteData` gains `keepArchivedCharacterBundles?: bool`
   (effective default TRUE — `!== false`); `DeleteSummary` gains
   `archiveBundles: number` + `archiveBundlesKept: boolean`; the preview
   reports `archiveBundles` with `archiveBundlesKept: true`.
   `systemRestoreExecute` gains `keepArchivedCharacterBundles?: bool`
   (replace-mode wipe only; accepted in both modes). **Owner: P4.D63.**
5. The change-passphrase response gains `archives: { total, reencrypted,
   failures: [{fileId, filename, reason}] }`; `total: -1` = the sweep
   itself failed; a failed sweep does NOT fail the passphrase change.
   **Owner: P4.D63.**
6. Export preview (`characters` type) gains optional `vaults: { stores,
   documents, blobs, estimatedBytes }`, present only when `stores > 0`.
   **Owner: P4.D62.**
7. Group-member projections gain `archivedAt`; add-member refusals are 400
   `That character is archived; rehydrate them before adding them to a
   group.` / `…before adding them to a roster.` The chat-participant
   enrichment projects `archivedAt ?? null`. **Owner: P4.D63.**
8. Error sentences are byte-contractual across all lanes — the
   `CharacterArchivedError` message, the tool/action refusal sentences,
   the three archive-crypto sentences, the preserveIds collision shapes,
   and the 404 body `{"error":"File content not found"}`.
9. **Cross-lane fixture rule:** the committed `system-data-*` fixtures
   keep their OLD schema this round (P4.D62 extends by mutation, never
   rebuild). v4 reading an old-schema DB emits NO archive keys on exported
   character records (Zod-optional, absent column → absent key), so
   P4.D62's families stay honestly green without P4.D63's columns. The
   three-key export carry on character records is ROUND 2's, covered by
   the archive-service differential (a bundle IS an export).
10. SPA gating: `CHARACTER_ARCHIVE_SERVER_LANDED = false` in P4.D64's e2e
    spec — flipped in ROUND 2, not at this round's unification. Beats that
    need only a tombstoned ROW (roster toggle/badges, read-only detail,
    group members line, participant badge) ride a fixture-seeded archived
    character, ACTIVATE-AT-UNIFY behind `ARCHIVE_TOMBSTONE_SEEDED` (the
    unifier seeds it per P4.D64's seeding note and flips the constant).

## Ownership (identical across the three orders)

- **P4.D62**: `services/qtap_export/**`, `services/quilltap_import/**`,
  `services/file_storage.rs`, `crates/quilltap-web/src/files_routes.rs`,
  `crates/quilltap-host/src/files_store.rs`, `db/doc_mount_*` create-path
  id-honor only, its harness families + the `system-data-*` extension +
  the preserveIds bundle fixture. **Bumps core, harness, web, host.**
- **P4.D63**: `api/types.rs`, `api/engine.rs`, `api/characters.rs`,
  `api/files.rs`, `api/salon.rs` (participant enrichment),
  `api/chat_post_office.rs` (mailbox/send-mail refusals),
  `db/characters*.rs`, `db/chats*.rs` (setParticipantStatus),
  `db/vault_wardrobe_write.rs`, `db/vault_read_overlay.rs` (comments),
  `db/character_resolver.rs`, `select_speaker.rs`,
  `services/{orchestrator,participant_resolver}.rs`,
  `tools/{self_inventory,send_mail,list_email}.rs`, the doc-edit
  path-resolver + conversation-summary vault-bridge modules,
  `services/delete_all.rs`, `services/backup/restore/orchestrator.rs`
  (+ backup types), the five embedding one-default sites, NEW
  `services/character_archive/`, `dbkey.rs` + the engine-held passphrase
  cache, `services/provisioning/fresh_schema.json` + the boot ensure,
  `crates/quilltap-web/src/{system_data_routes,backup_routes}.rs`.
  **Bumps core, harness, web.**
- **P4.D64 (this lane)**: `apps/web/**` only. **Bumps the SPA version.**
- `CHANGELOG` and `status-log.md` are append-only for all lanes
  (union-merge).

## Verification gate (before declaring done)

- `ng test` green (it is vitest-based — do NOT pass karma flags; no
  `| tail` pipelines masking the exit code). `ng build` clean.
- The new gated e2e specs compile and SKIP with their named constants;
  any beat runnable without server support runs green locally.
- No file outside `apps/web/` changed.
- Per-commit rule: follow `.claude/commands/commit.md`; end commit
  messages with the Co-Authored-By trailer.
