# Work order P4.9a — the My Photos vertical (lane A): the user photo gallery, server + `/photos` SPA + the deep gallery modals

> **STATUS: OPEN — HELD BACK from the round's unification (2026-07-18);
> resume at unit 2 (the fixture family + the stateful differential + the
> §3 verbs).** The lane stopped after ONE commit (`40c7da51` on the
> PRESERVED branch `claude/photos-view-porting-3ee1fc`, worktree
> `.claude/worktrees/photos-view-porting-3ee1fc`): unit 1 — the 811-line
> `quilltap-core::photos::user_gallery_service` port (all four legs) with
> the injected embedding/bytes seams, plus the tier-1
> `photos-prompt-excerpt` table case; its own commit message names the
> unit-2 differential as the next step. NOT on main; nothing of this lane
> unified. **Resume notes:** (1) REBASE the branch onto the unified main
> first — its quilltap-core 0.0.270 bump + Cargo.lock now conflict with
> main's 0.0.271 (accumulate upward, recount at the end); (2) the §2a
> photos nav flip was NOT applied at the round's unification (the nav
> item stays `route: null`) — it lands when THIS lane unifies; (3) the
> §1 route block and §3 folds for this lane are likewise still owed; (4)
> the sibling lanes CLOSED, so the Shared contract's cross-lane
> obligations reduce to this lane's own; (5) ⚠ v4 has drifted to
> `616930db` (the llm-consult feature — pascal/workbench files only,
> ZERO overlap with this lane's targets): keep generating oracles from a
> pinned detached worktree at `d68638b4` until the drift round moves the
> baseline.
>
> Lane A of the FOUR-lane P4.9a ∥ P4.9c ∥ P4.9b ∥ P4.9d round (the M6
> backlog items 1–4 round, planned 2026-07-18). **v4 reference baseline:
> HEAD `d68638b4`** (4.8.0-dev.72, `~/source/quilltap-server`) — run
> `git log d68638b4..HEAD --oneline` at lane start; **if HEAD moved, STOP
> and report.** ⚠ The v4 working tree is DIRTY with the in-flight
> llm-consult feature (pascal/custom-tools/LLM-inspector files +
> `lib/pascal/llm-consult.ts`); none of this lane's survey targets are
> dirty, but **every oracle this lane generates MUST run from a pinned
> detached worktree at `d68638b4`** (the `oracle-regen-pinned-v4-worktree`
> memory: `git worktree add --detach`, symlink BOTH `node_modules`).
> Sibling lanes: `p4.9c-about-profile.md` (shares the round's Rust
> conflict set — see Ownership), `p4.9b-generate-image-screen.md`,
> `p4.9d-quick-hide-provider.md`. The **Shared contract** and
> **Ownership** sections below are binding and reproduced verbatim in all
> four orders. You run in a worktree (own Rust `target/`, own Rust
> binaries for the e2e server — the P4.6af memory; mind the disk-pressure
> memory: per-worktree targets run ~10–20 GB).

> **RESUME ADDENDUM (2026-07-18 — the `616930db` drift-catch-up + P4.9a
> round; supersedes the preamble's baseline paragraphs above where they
> conflict):**
>
> 1. **The v4 baseline for the resumed lane is `616930db`**
>    (4.8.0-dev.75; the v4 tree is CLEAN — the in-flight llm-consult
>    feature landed as `a2d9a3c8`/`979aec66`/`616930db`). Drift-check at
>    lane start with `git log 616930db..HEAD --oneline`; if HEAD moved,
>    STOP and report. The pinned-detached-worktree requirement is
>    LIFTED: the drift touched ONLY the Pascal/announcer family (zero
>    overlap with this lane's survey targets, re-verified at planning),
>    so photos oracles regenerate from the checkout directly. If v4
>    goes dirty mid-round, re-pin at `616930db` per the
>    `oracle-regen-pinned-v4-worktree` memory.
> 2. **The sibling lanes this round are new**: `p4.d8-llm-consult-server.md`
>    (lane D8 — the Pascal server drift re-port) and
>    `p4.6bc-workbench-llm-spa.md` (lane BC — the Workbench SPA drift
>    re-port). The original four-lane Shared contract below still
>    binds THIS lane's own obligations (§1 route block, §2a nav flip as
>    a unifier wire, §3 photos verbs) — the sibling-specific parts (§2b,
>    §4, P4.9b/c/d entries) are CLOSED history. Additionally, §E of the
>    new orders binds here by reference: **lane D8 and this lane share
>    the round's KNOWN Rust conflict set** (`api/types.rs`,
>    `api/engine.rs`, `api/mod.rs`, `quilltap-web/src/lib.rs` — additive
>    only, unifier reconciles); lane BC is fully disjoint from this
>    lane in `apps/web/` — and `app.routes.ts` is THIS lane's alone
>    this round.
> 3. **Rebase before anything else** (per resume note 1 above): main is
>    at core 0.0.271 / harness 0.0.239 / web 0.0.28 / SPA 0.5.169 —
>    accumulate version bumps upward from those, recount at lane close.
>    The preserved worktree's `target/` is ~25 GB — reclaim its
>    `incremental/` before the first build (the disk-pressure memory).
> 4. Everything else in this order (mandate, survey, tiers, gate)
>    stands as written; resume at **unit 2** (the fixture family + the
>    stateful differential + the §3 verbs).

## Mandate

Deliver the **user photo gallery** end-to-end: v4's `lib/photos/`
user-gallery service (the cross-mount `photos/` roll-up with sha256 dedup
and link summaries) as a differential-verified Rust surface with four new
wire verbs + REST edges, and the `/photos` "My Photos" Angular screen
(v4 `PhotosView.tsx`), plus (tier 2) the deep gallery modal family
(`ImageDetailModal` / `ChatGalleryImageViewModal` + prev/next navigation)
that upgrades the existing chat/character galleries. This lane owns the
new `screens/photos/**`, the `images/` modal files, and the photos Rust
surface; it must NOT touch `shell/shell.ts` (lane C's file this round —
the nav flip is a §2a unifier wire), `screens/home/**`,
`screens/salon/salon-conversation.ts`, or any sibling-owned file.
User-facing strings keep v4's Wodehouse register (v4's own strings carry
over verbatim — "My Photos", "Other albums keep their copy", etc.).

## Survey-verified starting points (2026-07-18 @ `d68638b4` — do not re-derive)

### The v4 screen (`app/photos/`)

- `page.tsx:12-15` — thin: `redirectToWorkspaceTab('photos')` (workspace
  glue, don't port) then `<PhotosView />`.
- `PhotosView.tsx` (~19 KB, self-contained — it does NOT use the shared
  modal family; `PhotoCard` at `:337` and `PhotoDetailModal` at `:383`
  are inline components):
  - ONE list endpoint: `GET /api/v1/photos` (`:92` initial, `:120`
    load-more) with `q`, `limit` = `PAGE_SIZE` 60 (`:61`), `offset`
    (`entries.length` on load-more). Response `{entries, total, hasMore}`
    (`:55-59`).
  - Delete: `DELETE /api/v1/photos/${linkId}` (`:172`) behind
    `showConfirmation` (`:169`); optimistic removal + `total--`
    (`:177-178`). The copy: link-only removal — "Other albums keep their
    copy" (`:169`).
  - Infinite scroll: `IntersectionObserver`, `rootMargin: '600px 0px'`
    (`:147-153`); NO virtualization. `fetchGenerationRef` guards stale
    load-mores across query changes (`:77,:83,:97`) + a defensive linkId
    de-dupe on append (`:125-131`). Port both guards.
  - Card (`:337-381`): full `blobUrl` `<img loading="lazy">` (no
    thumbnail route on this screen — a v4 perf quirk, carry it), "🔗 N"
    badge when `linkSummary.count > 1` (`:360-368`), caption =
    `caption || generationPromptExcerpt || fileName`.
  - Detail modal (`:383-511`): Escape-to-close, full image, "Original
    prompt", READ-ONLY tag badges (`:450-458` — v4 has no tag editing
    anywhere in this family), the "Linked in N places" list with
    `mountStoreType === 'character'` → "Vault" else "Album" badge
    (`:460-483`), sha256/linkId identity, one destructive "Remove from
    this album" button. No prev/next on THIS modal.
  - `useSubsystemBackgroundStyle('lantern')` (`:17/:79`) — check whether
    v5 has a subsystem-background equivalent; if not, defer loud (do not
    invent one).
  - localStorage: none. Quick-hide: none.

### The v4 server surface

| Route file | Methods | Backing |
|---|---|---|
| `app/api/v1/photos/route.ts` | GET list (`q`,`tag[]`,`limit`,`offset`; `:36-68`), POST save `{fileId,caption?,tags?,chatId?}` (`:70-104`; sha256 duplicate → 400, `:92-99`) | `lib/photos/user-gallery-service.ts` → `listUserGallery` / `saveToUserGallery` |
| `app/api/v1/photos/[id]/route.ts` | GET entry (`:19-33`), DELETE link + last-link file GC (`:35-53`; docstring `:5-8`: `id` is a `doc_mount_file_links.id`) | `getUserGalleryEntry` / `removeFromUserGallery` |
| `app/api/v1/images/[id]/route.ts` | GET — the `characterGalleryLinks` read both deep modals need (tier 2) | images service |

- `lib/photos/user-gallery-service.ts` (~470 lines) is the core port:
  cross-mount roll-up of every `photos/` folder, **sha256 dedup** (same
  image hard-linked into multiple albums collapses to one entry),
  `linkSummary` (count + linker list), `blobUrl` =
  `/api/v1/mount-points/${mountPointId}/blobs/${encodeURI(relativePath)}`
  (`:390,:470`).
- v5 already serves those bytes: `GET /api/v1/mount-points/{id}/blobs/{*path}`
  (`crates/quilltap-web/src/lib.rs:122`). No new byte route needed.

### The v4 deep modal family (tier 2)

- `components/images/ImageDetailModal.tsx` is a SHIM (`:8-9`); the real
  component is `components/images/image-detail/ImageDetailModal.tsx`
  (145 lines). Props `:11-18`: `isOpen,onClose,image,onPrev,onNext,onAvatarSet`.
  On open fetches `GET /api/v1/characters` + `GET /api/v1/images/{id}` in
  parallel (`:43-46`). Sub-components `ImageActions`/`ImageMetadata`/
  `DeletedImagePlaceholder`; keyboard nav via `useImageNavigation` (`:73`).
- `image-detail/hooks/useImageActions.ts` — the writes:
  `POST /api/v1/characters/{id}/photos` `{linkId|fileId}` (`:46-53`),
  `DELETE .../photos/{linkId}` (`:95-98`), set-avatar
  `POST /api/v1/characters/{id}?action=avatar` `{imageId}` (`:130-136`),
  save-to-user-gallery `POST /api/v1/photos` `{fileId}` (`:183-187`),
  client-side download/copy.
- `components/chat/ChatGalleryImageViewModal.tsx` — props `:24-35`; on
  open `GET /api/v1/images/{id}` for gallery-link toggle state
  (`:63-82`); character/user-character album toggles (`:126-188`); delete
  delegated to parent (`:198-202`); chevrons `:214-238`.
- `components/images/PhotoGalleryModal.tsx` routes detail clicks:
  `kind:'chat'` items → `ChatGalleryImageViewModal` (`:339`), image-mode
  items → `ImageDetailModal` (`:354`); prev/next is index math (`:188-193`).

### The v5 targets

- NO `/photos` screen, NO user-gallery Rust surface, NO `photoGallery*`
  or image-info verbs (verified by grep 2026-07-18). The shell nav item
  is a disabled dead button — `shell.ts:44-50`, `route: null` (§2a).
- `apps/web/src/app/images/photo-gallery-modal.ts` — the chat-mode port;
  its docstring (`:25-33`) names the deep-detail modals as the deferred
  gap this lane's tier 2 closes. `images/image-modal.ts` — the lightbox;
  saves via `characterPhotoSaveById`, deletes via `chatFileDelete`.
- `screens/characters/view/tabs/gallery-tab.ts` — character album grid
  (`characterPhotoList` / `characterPhotoRemove` / multipart upload).
- Existing verbs the deep modals need: `characterPhotoList` /
  `characterPhotoSaveById` / `characterPhotoRemove`
  (`core-contract.ts:623-635`), `CharacterAvatar`
  (`crates/quilltap-core/src/api/types.rs:371-373` — the set-avatar verb
  EXISTS). MISSING: the `GET /api/v1/images/{id}` read → this lane's
  tier-2 `imageInfoGet` verb.
- Rust module home: `crates/quilltap-core/src/photos/` already holds
  `character_gallery_service`, `photo_link_summary`, `photos_paths` — the
  new `user_gallery_service.rs` lands beside them and should REUSE
  `photo_link_summary` / `build_mount_file_url` where v4's service does.

## Tiered deliverables

### Tier 1 — must land

1. **`quilltap-core::photos::user_gallery_service`** — port
   `lib/photos/user-gallery-service.ts` (list / save / get-entry /
   remove): cross-mount `photos/` enumeration, sha256 dedup, linkSummary,
   `q` search, offset pagination, blobUrl minting (server-relative,
   v4-faithful — do NOT absolutize; dogfood-#12 note), save-duplicate
   rejection, link-only delete + last-link file GC. Carry v4's
   why-comments.
2. **The four §3 dispatch verbs** (`photoGalleryList` / `photoGallerySave`
   / `photoGalleryEntryGet` / `photoGalleryEntryRemove`) + the REST edges
   `GET|POST /api/v1/photos`, `GET|DELETE /api/v1/photos/{id}` in a new
   `quilltap-web/src/photos_routes.rs`, envelopes byte-faithful to v4
   (REST legs UNWRAP the dispatch envelope — the P4.6ah round memory).
3. **The differential**: a new committed `photos-{main,mount}.db` fixture
   family + generator (pin ids via `create(data,{id})` — the P4.6k
   memory) with: multiple mounts carrying `photos/` folders, a
   hard-linked duplicate (sha256 dedup case), a multi-link entry
   (linkSummary count > 1), caption/tags/generationPrompt rows, and a
   single-link entry (GC arm). Oracle: **direct-drive v4's real exported
   service functions** (`listUserGallery` etc.) + the two real route
   handlers for the envelope cases (direct-drive beats jest route
   oracles — the P4.d7 memory; jest-real-DB recipe where the route layer
   is needed). Cases must cover: list pagination + `hasMore`, `q` search,
   dedup collapse, linkSummary counts, save + duplicate-400, entry get,
   delete link-only vs last-link GC. Regenerate from the pinned detached
   worktree; grep the NDJSON for a new row before trusting green (the
   stale-pass memory).
4. **The `/photos` screen** (`screens/photos/`): v4-faithful markup +
   `qt-*` classes — header/counter/back-link, search form, the responsive
   grid of PhotoCards, infinite scroll (observer + both stale-guards),
   the inline detail modal (Escape, linker list with Vault/Album badges,
   read-only tags, confirm-gated link-only delete). ONE list verb fetch
   through the established query/service pattern. Unit specs pin the
   transcribed logic (stale-generation guard, de-dupe on append, badge
   arms, caption fallback chain) with v4 file:line comments.
5. **Its §1 route block** (path `photos`), and a **live Playwright beat**
   in this lane's OWN new spec file: boot → unlock → navigate to
   `/photos` directly (the nav item stays disabled until the §2a unifier
   flip — the beat navigates by URL in-lane and gains the nav-click step
   at unification, mark it so) → seeded gallery renders → search narrows
   → detail modal opens → link-only delete round-trips. Seed gallery
   content by dispatch after unlock (the P4.6z memory) or extend the e2e
   locked instance's seed if cleaner — this lane owns that choice; never
   assert absolute counts a sibling spec can grow.

### Tier 2 — should land

6. **`imageInfoGet`** (§3) — v4 `GET /api/v1/images/{id}` — with its REST
   edge + differential cases riding the photos corpus.
7. **The deep gallery modal family**: port
   `image-detail/ImageDetailModal` (+ `ImageActions`/`ImageMetadata`,
   keyboard nav) and `ChatGalleryImageViewModal`; wire prev/next index
   math + the detail hand-off into `images/photo-gallery-modal.ts` (chat
   items → ChatGalleryImageViewModal; image items → ImageDetailModal) and
   `gallery-tab.ts`. Reuse the EXISTING verbs (`characterPhotoSaveById` /
   `characterPhotoRemove` / `CharacterAvatar`) + this lane's
   `photoGallerySave` (the save-to-user-gallery action) + `imageInfoGet`.
   Component specs pin toggle-state derivation and nav wrap-around.

### Tier 3 — explicit deferrals (loud, named)

- **Tag editing** — v4 itself has none (tags are read-only in this whole
  family); record that, don't invent it.
- **`useSubsystemBackgroundStyle('lantern')`** — if v5 has no subsystem
  background machinery, name the omission in the status header.
- **The workspace-tab mount** (`redirectToWorkspaceTab('photos')`) —
  `p4.9j`'s.
- Anything in the deep-modal family that turns out to need an unported
  verb beyond §3 — refuse loudly, name it, don't stub.

## The differential requirement, restated

Every Rust unit above ships with an equivalence test driving v4's REAL
code (the oracle imports `lib/photos/user-gallery-service.ts` and the
real route handlers; tier-2 structural DB/JSON diff with UUID/timestamp
normalization — the minted-remap machinery). The SPA half is tier 4:
`ng test` + the live Playwright walk; every transcription gets a unit
spec pinning the v4 behavior with file:line. Recipes: `jest-real-db-oracle`,
`oracle-regen-pinned-v4-worktree`, `oracle-regen-silent-stale-pass`,
`p4.6ay-unit12` (shared JSON corpus both oracle and test read).

## Fixtures

- **Delivers:** the `photos-{main,mount}.db` fixture family + generator
  (committed), the photos oracle NDJSON (committed per the round's
  convention), and any e2e-instance seed additions for the photos beat.
- **Consumes:** the committed e2e locked instance; the existing
  mount-points blob route.
- Fixture changes are this lane's own; no sibling depends on them.

## Shared contract (BINDING — identical in all four orders)

### §1 — the `app.routes.ts` route blocks

`app.routes.ts` is contract-managed this round (three lanes add routes).
Each lane inserts ONLY its own block(s), immediately BEFORE the
`path: ''` home entry (wildcard stays last), matching the file's lazy
`loadComponent` idiom; update the file's doc comment for your own
route(s) only. The unifier unions the blocks in this order and resolves
the known additive conflicts; no lane touches another lane's block.

- **P4.9a:** `photos` → `./screens/photos/photos-page` → `PhotosPage`
- **P4.9c:** `about` → `./screens/about/about-page` → `AboutPage`;
  `profile` → `./screens/profile/profile-page` → `ProfilePage`
- **P4.9b:** `generate-image` → `./screens/generate-image/generate-image-page`
  → `GenerateImagePage`
- **P4.9d:** no routes.

### §2 — the shell wires (unifier-applied)

- **(a) The photos nav flip.** `shell.ts` NAV_ITEMS `photos` keeps
  `route: null` in every lane; the UNIFIER flips it to `route: '/photos'`
  at unification (the route exists only then). Lane A does not edit
  `shell.ts`; lane C (its owner this round) does not flip it.
- **(b) The user-menu mount chain.** Lane C ships `qt-user-menu` (the
  shell-footer dropdown porting v4 `profile-menu.tsx` +
  `sidebar-footer.tsx:302,309`: quick-hide content ABOVE the
  Profile/About entries) with a pinned marker comment
  `<!-- §2b: qt-quick-hide-menu-section mounts here at unification -->`.
  Lane D ships `qt-quick-hide-menu-section` (standalone, self-contained
  over its own service). The unifier imports + mounts lane D's component
  at the marker. Lane D's menu-walk beats are ACTIVATE-AT-UNIFY.
  (Lane letters here: A = p4.9a, B = p4.9b, C = p4.9c, D = p4.9d.)

### §3 — the new wire verbs (fold-at-unify)

Each verb-adding lane keeps a local typed module + cast (the established
`home.api.ts` pattern); the unifier folds them into `CoreRequest` and
runs the name-for-name wire diff. Names + payloads are pinned:

**P4.9a:** `photoGalleryList` (v4 `GET /api/v1/photos`:
`{q?, tag?, limit?, offset?}` → `{entries, total, hasMore}`, entry shape
verbatim from `listUserGallery`); `photoGallerySave` (v4 POST:
`{fileId, caption?, tags?, chatId?}`; sha256 duplicate → v4's 400);
`photoGalleryEntryGet` (v4 `GET /api/v1/photos/{id}`);
`photoGalleryEntryRemove` (v4 DELETE — link removal + last-link GC);
tier 2 `imageInfoGet` (v4 `GET /api/v1/images/{id}`, the
`characterGalleryLinks` read). REST edges: `GET|POST /api/v1/photos`,
`GET|DELETE /api/v1/photos/{id}` (+ `GET /api/v1/images/{id}` tier 2).

**P4.9c:** `userProfileGet` (v4 `GET /api/v1/user/profile`, default
action ONLY — the `theme-preference` arms are already ported via
`theme.service`/chatSettings, do NOT re-port); `userProfileUpdate` (v4
PUT `{name, email}` incl. the email-uniqueness arm);
`userProfileSetAvatar` (v4 PATCH `?action=set-avatar` `{imageId}`,
category IMAGE/AVATAR validation, stores `image = /api/v1/files/{imageId}`);
`systemDataDir` (v4 `GET /api/v1/system/data-dir`; the `?action=open`
POST is REFUSAL-ARMED loud). Health `version`: the quilltap-web HTTP
`/health` body gains `version` (already-threaded `CARGO_PKG_VERSION`),
and `interpretHealth`/`HealthStatus` (core-transport.ts) gain ONLY an
optional `version` field — no other signature moves; both transports then
agree (the Tauri health command already carries the engine `HealthDto`
version). REST edges: `GET|PUT|PATCH /api/v1/user/profile`,
`GET /api/v1/system/data-dir`.

**P4.9b / P4.9d:** NO new wire surface. P4.9b consumes only
`imageProfileList`, `imageProfileGenerate` (its locked four params),
`charactersList`, `chatAddToolResult`. P4.9d consumes only
`TagList`/`TagCreate`/`TagUpdate`/`TagDelete` and existing payload
fields (`isDangerousChat`, tags). `imageProfileValidateKey`/`ListModels`
STAY refusal-armed; `sortByUserCharacter` on the image-profile list stays
unported (deferred loud in P4.9b).

### §4 — the quick-hide localStorage keys (P4.9d only)

`quilltap.quickHide.activeTags` (JSON id array),
`quilltap.quickHide.hideDangerous` (`'true'`/`'false'`),
`quilltap.quickHide.includeAutonomousRooms` (`'true'`/`'false'` — already
shared with `autonomous-visibility.ts`; one service owns all three after
this round). `clearAllHidden` must NOT reset `includeAutonomousRooms`
(v4 provider `:179-180`). No other lane reads or writes these keys.

## Ownership (BINDING — identical in all four orders)

- **P4.9a:** `crates/quilltap-core/src/photos/user_gallery_service.rs`
  (new) + `crates/quilltap-core/src/api/photos.rs` (new, or the images
  read's natural home), `crates/quilltap-web/src/photos_routes.rs` (new);
  harness: its oracle cases, differential suites, the
  `photos-{main,mount}.db` family + generator. SPA:
  `apps/web/src/app/screens/photos/**` (new),
  `apps/web/src/app/images/**` EXCEPT `generate-image-dialog.ts` and the
  new P4.9b files (P4.9a owns `photo-gallery-modal.ts`, `image-modal.ts`,
  the new deep-modal files, `image-urls.ts` if needed),
  `screens/characters/view/tabs/gallery-tab.ts`, its §1 block, its own
  e2e spec file. Version bumps: quilltap-core, quilltap-harness,
  quilltap-web (+Cargo.lock), SPA `package.json`.
- **P4.9c:** Rust: the user-profile + data-dir dispatch/service files
  (new), `crates/quilltap-web/src/profile_routes.rs` (new),
  `crates/quilltap-web/src/health.rs` (the version field — exclusively);
  harness: its oracle cases + differential suites (+ fixture additions if
  the existing families lack what profile needs). SPA:
  `apps/web/src/app/screens/about/**`, `screens/profile/**` (new),
  `apps/web/src/app/shell/user-menu.ts` (new) and `shell/shell.ts`
  (user-menu mount + the F4 stale-docstring fix at `shell.ts:19-20`; NOT
  the photos flip), `apps/web/src/app/core/core-transport.ts` (the §3
  version carry — exclusively), its §1 blocks, its own e2e spec file.
  Version bumps: quilltap-core, quilltap-harness, quilltap-web
  (+Cargo.lock), SPA (+quilltap-host ONLY if data-dir needs a host seam —
  flag it loudly if so).
- **P4.9b:** SPA only: `apps/web/src/app/screens/generate-image/**`
  (new), `apps/web/src/app/images/standalone-generate-image-dialog.ts`
  (new), `apps/web/src/app/images/image-profile-picker.ts` (new shared)
  and/or `screens/new-chat/image-profile-picker.ts` (generalize),
  `images/generate-image-dialog.ts` (docstring/deferral-note edits only),
  `screens/salon/salon-conversation.ts` + the composer file it must touch
  for the single gutter opener button, `screens/home/quick-actions-row.ts`,
  `screens/home/home.spec.ts` (exclusively — no other lane edits it),
  its §1 block, its own e2e spec file. Version bump: SPA `package.json`.
- **P4.9d:** SPA only: `apps/web/src/app/quick-hide/**` (new: service +
  menu section + specs), `screens/salon/salon-list.ts` (+ refactoring
  `screens/salon/autonomous-visibility.ts` onto the shared service),
  `screens/home/recent-chats-section.ts`,
  `screens/home/characters-section.ts`,
  `screens/characters/list/characters-list.ts`,
  `screens/characters/view/character-detail.ts` + its conversations tab
  file, the Prospero chats/characters card files,
  `screens/settings/appearance/**` (the global tags tab + its
  quickHide column + ThemePreviewModal — RE-BINNED here from p4.9c
  because this lane owns the appearance tab; the unifier records the
  re-bin in `m6-screen-parity.md`), its own e2e spec file(s). MUST NOT
  touch `salon-conversation.ts` (v4 `SalonView.tsx:707`'s in-chat
  quick-hide consumer feeds the unported `p4.9e*` dialog family — defer
  loudly) or `home.spec.ts`. Version bump: SPA `package.json`.
- **The round's KNOWN Rust conflict set:** P4.9a and P4.9c both add
  ADDITIVE arms to `crates/quilltap-core/src/api/types.rs`,
  `api/engine.rs`, `api/mod.rs`, and `crates/quilltap-web/src/lib.rs`
  (route registration). Additions only, §3-pinned; the unifier reconciles
  (temp-branch cherry-pick + one full gate — the parallel-round
  reconciliation memory). No other Rust file is shared.
- **Append-only for everyone:** `docs/CHANGELOG.md`,
  `docs/developer/porting/status-log.md` (union-merge at unification).
  SPA version bumps union at unify — the unifier RECOUNTS (identical
  bumps merge silently — the P4.6aw/ax memory).
- **Nobody** edits `CLAUDE.md`, `phase-4.md`, `m6-screen-parity.md`, or a
  sibling lane's files; the unifier updates the phase docs.

## Verification gate (run before declaring done)

- `cargo fmt --all --check`; `cargo clippy --workspace --all-targets`
  clean on both feature sets; `cargo test --workspace --no-fail-fast`
  zero failures (fail-fast hides blast radius — the memory); this lane's
  differentials run by name with the oracle env vars set, `--nocapture`,
  and grep the output for SKIP (the P4.d3 memory).
- `ng test` green (via `ng test`, from `apps/web`); `ng build` clean.
- Full Playwright green, run ALONE on port 4319 from `apps/web` with THIS
  worktree's own Rust server binaries (the P4.6af memory); the photos
  beat live; any §2-dependent step marked ACTIVATE-AT-UNIFY.
- No `[value]` binding on any `<select>` with async options — bind
  `[selected]` per option (the standing dogfood-#6 rule).
- Every commit follows `.claude/commands/commit.md` (version bumps per
  Ownership; CHANGELOG entry; status-log append at lane close).
