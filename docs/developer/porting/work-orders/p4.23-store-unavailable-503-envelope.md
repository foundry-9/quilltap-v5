# Work order P4.23 — the store-unavailable 503 envelope

> **STATUS: CLOSED — UNIFIED on main (2026-07-31), lane A's second order.**
> Every store-unavailable error on the groups / projects / characters
> surfaces answers v4's contextful 503 (`context.ts:176-205` — NOT `auth.ts`;
> the P4.D29 record's attribution was wrong): `ErrorKind::Unavailable`
> (kebab `unavailable`, separate from `Locked`) at every web-edge status
> match; `CoreError.entity: Option<Box<UnavailableEntity>>` +
> `unavailable_wire_body()` building v4's exact two-key bodies in key order;
> `DbError::StoreUnavailable` carrying the three `overlay_to_db`s and the
> vault read keystone structurally; the shared `db_error_response` terminal
> helper across groups/projects/characters + `require_character`; the SPA
> mirror (`'unavailable'` + `entity` in `core-contract.ts`). Differentials:
> `groups_routes_equivalence` +1 corrupt-store arm (15 cases),
> `projects_routes_equivalence` +2 (41 cases — the GET arm caught v4's
> local-catch fixed 500 `Failed to fetch project`, made faithful),
> the NEW `store-unavailable-routes.test.ts` oracle +
> `crates/quilltap-web/tests/store_unavailable_envelope.rs` (live edge; the
> absent-keystone equality + the corrupt-vault-PUT divergence arm that goes
> red when v4 fixes #47) + the always-on `store_unavailable_wire_pin`.
> Mutation-proven per the D24 rule.
>
> **Recorded residuals (tier 3, unchanged by this close):** dispatch
> surfaces outside groups/projects/characters that read characters via
> `find_by_id` still answer 500 through their own `internal(e)` terminals
> (the standing `DbError::Key` message-leak pool — the structured variant
> now reaches them, so closing it got cheaper); v4 verbs with OTHER local
> catches (delete, list-chats, members-state, …) answer their fixed 500s
> where v5 answers the 503 for this class — unpinned residual, recorded;
> the autonomous-rooms listing keeps its v4-faithful local-catch 500.
> Lane record: `status-log.md` → "Lane record — P4.23".
>
> The original order follows unchanged.
>
> **STATUS (original): OPEN, not started (ordered 2026-07-31).** The P4.D29 unit-4
> escalation, ordered at last: v4 answers a broken document store with a
> deliberate, contextful **503**; v5 answers **500 + a leaked internal
> detail**. Phase-4's candidate list has carried this as "escalated, ordered
> next" since the `dcd9440a` round.
>
> **Lane arrangement — this order runs in LANE A, SECOND, on the SAME branch
> as P4.22** (`p4.22-character-vault-properties-clobber.md`). P4.22 lands the
> character vault's present-but-unparseable write refusal first; this order
> then maps every store-unavailable error — including that new refusal — to
> v4's envelope. Do not start this order until P4.22's tier 1 is committed on
> the lane branch.
>
> **v4 baseline for this round: `ff12f491`** (v4 HEAD is `e1be028b`, one
> release-infra commit past it — NO-PORT, verified by name). **Drift-check at
> lane start**: `git log ff12f491..HEAD --oneline` + `git status --short` in
> `~/source/quilltap-server` must show exactly `e1be028b` and a clean tree;
> anything more → STOP and report. ⚠ **Lane-A special drift risk:** the human
> was asked to land the finding-#47 fix in v4 promptly. If v4 has moved on
> `lib/database/repositories/vault-overlay/managed-fields.ts` or
> `lib/api/middleware/context.ts`, STOP — P4.22 reclassifies from deliberate
> divergence to drift re-port (its header says so) and this order's oracle
> may have moved with it.
>
> **Siblings (same round):** P4.24 (LLM_LOG_CLEANUP, lane B), P4.25 (toast
> subsystem, lane C), P4.26 (announcement rendering, lane D), P4.27 (sweep
> debt, lane E). See "Shared contract + Ownership" — binding and identical
> across all six orders.
>
> **Worktree note:** own branch, own checkout; no `git stash`/`git worktree`.

## Mandate

Make every store-unavailable error answer v4's deliberate contextful 503
instead of v5's current 500 + leaked detail, end to end: the additive
`ErrorKind`, the entity-id carry on `CoreError`, the three `overlay_to_db`
replacements, the character-vault sibling (read keystone AND P4.22's new
write refusal), the web-edge status mapping at every match site, the SPA
contract mirror, and a routes-level envelope differential. Do NOT touch:
lane D's SPA files, lane E's characters-READ families, `db/llm_logs.rs` or
`services/queue_service.rs` (lane B), or any fixture another family owns.

## Survey-verified starting points (2026-07-31)

**v4 (at `ff12f491`) — the envelope lives in
`lib/api/middleware/context.ts:176-205`** (⚠ the P4.D29 lane record said
`auth.ts` — that was wrong; corrected here). The wrapper catches the three
error classes and answers, in this exact key order:

```
{ "error": "Project document store unavailable", "projectId": <id> }     → 503
{ "error": "Group document store unavailable",   "groupId": <id> }       → 503
{ "error": "Character vault unavailable",        "characterId": <id> }   → 503
```

v4's own comment states the intent: "Map each to a deliberate, contextful
503 instead of an opaque 500 so callers and logs can tell store degradation
apart from a generic crash." Anything else falls through to
`serverError('Internal server error')` — v4 does NOT leak `error.message`
for this class.

**v5 (main, core 0.0.425):**

- `db/document_store_overlay.rs:129-137` — `OverlayError::Unavailable
  { entity_label, id, mount_point_id, detail }` already carries everything
  the envelope needs.
- THREE independent `overlay_to_db` collapses, each
  `OverlayError::Unavailable { detail, .. } → DbError::Key(detail)` →
  `ErrorKind::Internal` → 500 + raw detail: `api/projects.rs:64`,
  `api/groups.rs:125`, `api/autonomous_rooms.rs:488`.
- The vault sibling: `db/vault_read_overlay.rs:64` `VaultUnavailable`
  (v4 `CharacterVaultUnavailableError`; its doc comment already says
  "mapped to a 503" — aspirationally). `hydrate_one` (:215) raises it when
  the `properties.json` keystone is ABSENT. Find every place it (or its
  `OverlayOneError::Unavailable` wrapper, :383) is collapsed on the way to
  the edge.
- P4.22's NEW write-path refusal (landed first on this branch) — whatever
  error shape P4.22 chose, it must reach the vault envelope too. Reconcile
  on this branch; you own both sides.
- Precedent for additive `api/types.rs` changes: `ErrorKind::Unprocessable`
  (P4.6ay unit 12) and the `code`/`associations` fields on `CoreError` —
  follow those shapes. `ErrorKind::Locked` already maps to 503; keep
  Unavailable a SEPARATE kind so "vault locked" stays distinguishable from
  "store broken" (the D29 proposal's reasoning).
- Web-edge `ErrorKind` → `StatusCode` match sites (all must gain the arm):
  `crates/quilltap-web/src/dispatch.rs:28`, `characters_routes.rs:667`,
  `files_routes.rs:497`, `text_replacements_routes.rs` (grep for the match;
  there may be more — grep `ErrorKind::Locked` across `quilltap-web/src`
  and cover every site it appears).
- The REST error envelope at the edge must be byte-exact to v4's three
  bodies above, including the per-entity key NAME (`projectId` vs `groupId`
  vs `characterId`). Derive the key from the entity label; do not add all
  three keys.
- SPA mirror: `apps/web/src/app/core/core-contract.ts` (ErrorKind/CoreError
  mirror) + the `coreErrorMessage` helper (P4.D34) — additive, name-for-name
  with the server (the §1 wire diff at unification checks this).

**Reachability facts, established at D29 (do not re-derive):**

- Project/group route-level PUT/GET hit the HYDRATING `find_by_id` before
  any write, so a corrupt `properties.json` throws on the READ path
  (`hydrate_one`'s pre-existing throw) — a routes arm exercises the read
  refusal, which is exactly what v4's middleware maps.
- The character vault differs: corrupt-but-PRESENT `properties.json` is
  fail-soft on read (v4-faithful — finding #47's read half), so the vault's
  v4-comparable 503 arm is the ABSENT-keystone read. P4.22's
  present-but-unparseable WRITE refusal → 503 is part of the **deliberate
  divergence** (v4 clobbers instead) and is asserted via the divergence
  machinery, not a byte-compare.

## Tiered deliverables

### Tier 1 — must land

1. **`ErrorKind::Unavailable`** (additive) mapping to 503 at every web-edge
   match site, plus an optional entity-id carry on `CoreError` (label +
   id — design latitude on the exact field shape, but the WIRE bodies are
   pinned verbatim above).
2. **Replace the three `overlay_to_db` collapses** so
   `OverlayError::Unavailable` reaches the edge as the contextful 503;
   `Db(d)` keeps its current path.
3. **The vault sibling**: `VaultUnavailable` (absent keystone, read path)
   and P4.22's write refusal both answer
   `{ "error": "Character vault unavailable", "characterId": <id> }` 503.
4. **The SPA mirror** (`core-contract.ts` + `coreErrorMessage`), additive,
   name-for-name.
5. **The routes-level envelope differential**: extend
   `groups_routes_equivalence` and `projects_routes_equivalence` with a
   corrupted-store arm (plant malformed bytes through the REAL
   `write_database_document`, drive the route, byte-compare status + body
   against v4's REAL route), and add the vault absent-keystone read arm on
   the characters routes surface (choose the file; name it in the lane
   record). The vault WRITE-refusal envelope is asserted v5-side with an
   `EXPECTED_DIVERGENCES`-style carve-out that goes red the moment v4 lands
   its own #47 fix (the P4.22/sparse-array pattern). **Mutation-prove**
   the arms (the D24 rule): revert the mapping → the arm must go red on
   status AND body.

### Tier 2 — should land

6. A wire-level unit pin on the edge serialization of the three bodies (key
   order included — `preserve_order` makes insertion order load-bearing).

### Tier 3 — explicit loud deferrals

- The standing `DbError::Key` message-prefix leak (the broader
  internal-detail-leak pool item) stays OUT of scope — this order fixes the
  store-unavailable class only.
- The Zod format-validator gap on property bags stays deferred (P4.D29's
  own deferral; P4.22 tier 3 restates it).

## The differential requirement, restated

The envelope arms drive v4's REAL routes (the `jest-real-db-oracle` recipe;
Node 24; oracles straight from `~/source/quilltap-server` while HEAD stays
`e1be028b`). Regenerated NDJSON gets a fresh-baseline marker grep
(`oracle-regen-silent-stale-pass`). Point oracles at COMMITTED fixture DBs
per the standing rule — and since corrupting a store MUTATES the fixture,
run both sides against /tmp COPIES.

## Fixtures

Consumes the existing groups/projects routes fixtures and the characters
fixture family P4.22 already touches (same lane, no cross-lane regen owed).
If an arm needs a dedicated corrupt-store fixture variant, build it in /tmp
from a copy; commit nothing new unless the arm genuinely needs it (name it
in the lane record if so). This lane owns regenerating every family it
extends, fresh at `ff12f491`.

## Shared contract + Ownership (binding; identical across all six orders)

**File ownership this round — no lane edits another lane's files:**

| Lane | Orders | Owns (exclusively) |
|---|---|---|
| A | P4.22 → P4.23 (one branch, sequential) | `crates/quilltap-core/src/db/vault_character_update.rs`; `crates/quilltap-core/src/api/**` (incl. `types.rs`, `engine.rs`, `projects.rs`, `groups.rs`, `autonomous_rooms.rs`, `characters.rs`); `crates/quilltap-web/src/**`; `apps/web/src/app/core/core-contract.ts`; harness/oracle files for the characters-WRITE (vault-properties) arms it extends + `groups_routes_equivalence.rs` + `projects_routes_equivalence.rs` + any `crates/quilltap-web/tests/**` envelope-arm file |
| B | P4.24 | `crates/quilltap-core/src/db/llm_logs.rs`; `crates/quilltap-core/src/services/{queue_service,job_runner}.rs` + the new `llm_log_cleanup_job.rs`; `crates/quilltap-host/src/**`; the new `llm-log-cleanup` fixture family + harness family + oracle case |
| C | P4.25 | the new toast module files + its new stylesheet + the styles index; every `apps/web` call-site file it converts EXCEPT lane D's files and `core-contract.ts`; its new e2e spec file; dialog/settings e2e specs whose assertions its change moves (named per-file in the lane record) |
| D | P4.26 | `apps/web/src/app/chat/{message-row,announcement-group,message-content,system-message-labels}.ts` + their specs; `apps/web/src/app/chat/render/**`; `apps/web/src/styles/**/_chat.css`; its new e2e spec file; salon/announcement/post-office e2e specs whose assertions its fixes move (named per-file) |
| E | P4.27 | `crates/quilltap-harness/tests/{characters_read,characters_reads,characters_actions}_equivalence.rs` + their oracle cases; `terminal_tools_equivalence.rs` + its oracle case; harness recipe HEADERS in files NOT owned by lanes A/B; the projection-fix site in core as diagnosed (`db/characters.rs` / `db/vault_read_overlay.rs` / `services/character_enrichment.rs`) |

- **`crates/quilltap-core/src/api/**` belongs to lane A.** Lane E STOPS and
  records the exact diff if the `canChooseOutfit` fix must land there.
- Lane C never edits lane D's four chat files, even where a v4 toast call
  site lives in one — record the site for D/the unifier instead.
- Lane D is SPA-only; a divergence traced to the SERVER is recorded, not
  fixed.
- Lanes C and D never edit the SAME e2e spec file; an unavoidable collision
  → STOP and record for the unifier.
- `CHANGELOG.md` and `docs/developer/porting/status-log.md` are append-only
  for everyone (union-merge at unification).
- Version-bump ownership: every lane bumps the crates/SPA it touches per
  `.claude/commands/commit.md`; the unifier recounts and collapses
  (`parallel-round-playbook`).
- All lanes pin **v4 baseline `ff12f491`** and drift-check at lane start
  (expect exactly `e1be028b` past it, tree clean; else STOP). No lane
  writes baseline wording — no baseline move this round.
- Distill-transitive tier-3 recipes keep their TZ pins (P4.d26 rule) if any
  lane regenerates one.

## Verification gate before declaring done

- `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D
  warnings` on BOTH feature sets.
- `cargo test --workspace --no-fail-fast` with this lane's env vars
  (`cargo-test-workspace-is-fail-fast`).
- Every family this order extended, re-run BY NAME with `--nocapture`, zero
  SKIP, over oracles regenerated fresh at `ff12f491` (marker-grepped).
- `apps/web` touched (the mirror) → `ng test` + `ng build` clean. The full
  Playwright run rides the unification (types-only SPA change).
- Per-commit rule: follow `.claude/commands/commit.md`.
