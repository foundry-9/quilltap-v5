# Work order P4.6aw — the Rust riders (cost-estimator consolidation + the stale-seam-note sweep) + the depiction-guidelines no-vault hint

> **STATUS: CLOSED — UNIFIED on main (2026-07-16).** All Tier 1 + Tier 2
> LANDED: (1) one trait / one default / one host impl —
> `CarinaCostEstimator`, `NoCarinaCost`, and `PricingCarinaCost` retired;
> both tier-3 differentials green over FRESH `02865bdb` oracles
> regenerated again at unification (title 10 cases, carina 12 cases, zero
> SKIP); (2) the sweep hit 15 files — five sites had described seam #5 as
> a tracked deferral and now record `preserve_order` closed it (the
> unifier reconciled `phase-2-onramp.md`'s present-tense wording; the
> item's RESOLVED marker had been there since 2026-07-01); (3) both
> appearance tabs now suppress the guidelines editor behind v4's verbatim
> no-vault warning, no fetch fired when suppressed — a spec was
> sentinel-corrupted once to prove it actually runs. §1 honored:
> `characterDocumentMountPointId` was ALREADY typed
> (`core-contract.ts:1823`), so even the permitted field-typing allowance
> went unused. **Tier-3 deferrals stand as written:** the boxed
> `ChatCostSummary` + client `detailed=true` (WON'T-PORT verdict rendered
> in `m6-screen-parity.md` §1.2 with the dead-in-v4 evidence); the shared
> component's own `disabledHint` input (no consumer would pass it).
> Final versions: core 0.0.232, host 0.0.19, harness 0.0.209.
>
> **Original status (for reference): OPEN.**
>
> Lane A of the THREE-lane P4.6aw ∥ P4.6ax ∥ P4.8 round (the riders +
> M6-review round, planned 2026-07-16). **v4 reference baseline: HEAD
> `02865bdb`** (`~/source/quilltap-server`) — run
> `git log 02865bdb..HEAD --oneline` at lane start; **if HEAD moved, STOP
> and report.** Sibling lanes: `p4.6ax-editor-riders.md` (the editor SPA
> riders — disjoint files; §2 of the Shared contract is the one visible
> meeting point) and `p4.8-m6-screen-parity-review.md` (docs-only,
> read-only). The **Shared contract** and **Ownership** sections below are
> binding and reproduced verbatim in all three orders. You run in a
> worktree; the unification pass integrates the lanes onto main.

## Mandate

Close three standing-pool items: (1) consolidate the two byte-identical
cost-estimator seams (`MessageCostEstimator` / `CarinaCostEstimator`) into
one — a **behavior-frozen refactor** guarded by the existing differentials;
(2) retire the stale "serde_json sorts keys" rationale comments across
`crates/` (the `preserve_order` decision made the claim false — P4.6ar
unit 4 banked the sweep); (3) port v4's **proactive no-vault suppression**
(the `disabledHint` arm) onto the two v5 depiction-guidelines fields —
today a v5 user on a vault-less character can type and Save and only then
hits the reactive server `bad_request`; v4 suppresses the editor behind a
warning before they start.

This lane owns `crates/**` plus exactly the three SPA files enumerated
under Ownership. It must NOT touch `apps/web/src/app/editor/**`,
`chat/**`, `harness/oracle/**`, or any wire surface (see §1).

## Survey-verified starting points (2026-07-16 @ `02865bdb` — do not re-derive)

### Item 1 — the two seams (same shape, same v4 function)

| Trait | Definition | Default impl | Host (pricing) impl |
|---|---|---|---|
| `MessageCostEstimator` | `crates/quilltap-core/src/services/cost_estimation.rs:54-63` | `NoMessageCost` (`:69`, impl `:71-82`, returns `None`) | `PricingMessageCost` — `crates/quilltap-host/src/spine.rs:1824` (impl `:1829-1850`) |
| `CarinaCostEstimator` | `crates/quilltap-core/src/services/carina_memory_extraction.rs:57-66` | `NoCarinaCost` (`:70`, impl `:72-84`, returns `None`) | `PricingCarinaCost` — `spine.rs:1793` (impl `:1798-1819`) |

Identical method signature both sides: `estimate(&self, provider, model,
prompt_tokens: i64, completion_tokens: i64, user_id) -> impl
Future<Output = Option<f64>> + Send`. **The two host impl bodies are
byte-for-byte identical** (`spine.rs:1807-1817` vs `:1838-1848`):
`build_pricing_context(&self.db, user_id)` →
`self.fetcher.estimate_message_cost(Registry::built_in(), …, now_unix_ms(),
&ctx)` → `result.cost`. Both port v4 `estimateMessageCost`
(`lib/services/cost-estimation.service.ts:62-138`).

Call sites: `MessageCostEstimator` bounds in `title_update_job.rs:172,426`
(handler defaults `E = NoMessageCost` at `:414`, import `:56`); host wire
`spine.rs:1882-1885` (import `:109`). `CarinaCostEstimator` bound in
`carina_memory_extraction.rs:108` (arg `cost: &K` at `:100-101`); host
wire `spine.rs:1927-1930` (import `:97`). The duplication is already
acknowledged in-code: `cost_estimation.rs:51-53` and
`carina_memory_extraction.rs:17`.

**The differentials that pin behavior (the refactor's proof — keep all
green):**

- `quilltap-harness/tests/title_update_tier3_equivalence.rs` — pins the
  **no-cost default** arm (imports `NoMessageCost` `:27`, passes it `:361`,
  `:451`; asserts `estimatedCostUSD` on TITLE_GENERATION `:210`).
- `quilltap-harness/tests/carina_memory_extraction_tier3_equivalence.rs` —
  pins the **cost-producing** arm via `struct CannedCost(f64)`
  (`:138-141`) fed the oracle's mocked cost (`:382`).
- `quilltap-harness/tests/cost_background_routes_equivalence.rs` — the
  READ breakdown (referenced from `cost_estimation.rs:30`).
- `pricing_fetcher_equivalence.rs:190` +
  `quilltap-core/src/services/pricing_fetcher/tests.rs:228,241` — the
  shared `estimate_message_cost` itself
  (`services/pricing_fetcher/mod.rs:714`).

**The design constraint:** the merged seam must still offer BOTH a no-cost
default (title differential) and a swappable cost-producing impl (carina's
`CannedCost`). One trait in `services/cost_estimation.rs`, one default,
one host impl; `carina_memory_extraction` re-uses that trait (an alias or
re-export is acceptable if it keeps call sites readable — lane's call).

### Item 2 — the stale-comment sweep targets

`preserve_order` IS enabled: `crates/quilltap-core/Cargo.toml:32-36`
(`serde_json = { version = "1", features = ["preserve_order"] }`; feature
unification propagates it graph-wide). `Value::Object` is an `IndexMap`
preserving insertion order — every comment claiming serde_json/`BTreeMap`
"sorts keys" as the *reason* for avoiding `Value` is now false.

**GENUINELY STALE (the sweep targets):**
`wardrobe.rs:487`; `canonicalize.rs:44`; `db/chat_settings.rs:30,43,110`;
`db/llm_logs.rs:29-30,44,289,343`; `db/connection_profiles.rs:46`;
`db/character_vault.rs:362`; `db/character_plugin_data.rs:41`;
`db/tags.rs:17`; `db/roleplay_templates.rs:36`;
`db/document_store_overlay.rs:81`; `db/characters.rs:64,141`;
`db/chats.rs:44`; `db/image_profiles.rs:40`; `db/background_jobs.rs:63`;
`db/plugin_config.rs:43,240,274` (all under `crates/quilltap-core/src/`).

**Rewrite rule:** keep the load-bearing guidance ("typed structs in schema
field order" — the `json-column-key-order` convention stands); drop or
correct only the false "because `Value` would sort" justification. The
*code* at every site is correct — only the rationale is stale.

**STILL-CORRECT (do NOT touch):** `cache_prefix_hashes.rs:7,9,203`
(`stableStringify` deliberately sorts, faithfully v4);
`embedding_blob.rs:359` (deliberate numeric key sort);
`services/core_whisper.rs:188,205-206,292` (deliberate `BTreeMap`
reproducing JS `.sort()`); every harness test-normalization `sorted()`
helper.

### Item 3 — the depiction-guidelines no-vault hint

**v4 mechanism** — `components/settings/AestheticEditorField.tsx`:
`disabledHint?: string` (`:29`, destructured `:38`); the load `useEffect`
**short-circuits when set** (`:54-56` — no fetch; dep array `:76`); the
render arm (`:113-114`) replaces the editor + Save entirely with
`<p className="qt-text-small qt-text-warning">{disabledHint}</p>`.

**The only v4 caller** —
`app/aurora/[id]/edit/CharacterEditView.tsx:333-351` (descriptions tab):
passes `disabledHint` at `:343-347` as
`character?.characterDocumentMountPointId ? undefined : '<the warning>'`.
The warning string, **verbatim** (it must survive byte-for-byte, and it
already carries the register):

> This character has no document vault yet, so depiction guidelines cannot
> be stored. Save the character once to provision its vault, then return
> here.

**v5 current state** — the field itself IS ported (as inline
`qt-markdown-field`s, NOT through the shared `aesthetic-editor-field`):

- Edit screen: `apps/web/src/app/screens/characters/edit/appearance-tab.ts`
  — the guidelines block `:165-201` (field `:183-188`, Save `:191-198`,
  save via `characterDepictionGuidelinesUpdate` `:274-278`, load seeded in
  the queryFn `:233-240`). Spec: `appearance-tab.spec.ts:127`.
- View screen:
  `apps/web/src/app/screens/characters/view/tabs/character-appearance-tab.ts`
  (doc `:9-27`), mounted by `view/character-detail.ts:166`.
- **Neither tab checks `characterDocumentMountPointId` or renders any
  hint** — the failure today is reactive (`guidelinesError` surfacing the
  server `bad_request("Character has no document vault to store depiction
  guidelines")`, `crates/quilltap-core/src/api/characters.rs:287-289`).
- The shared `apps/web/src/app/ui/aesthetic-editor-field.ts` docstring
  (`:41-45`) is **half-stale**: it claims the depiction-guidelines field is
  "NOT ported" — the field landed elsewhere; only the `disabledHint`
  behavior is missing. Correct the docstring to record where the arm
  actually lands.

**Server support (complete — nothing to add):** GET/PUT verbs
`types.rs:440,445`, engine `engine.rs:897,905-910`, handlers
`api/characters.rs:235-261,267+`, filename constant
`services/aesthetics.rs:29`; differentials
`characters_reads_equivalence.rs:225-226`,
`characters_mutations_equivalence.rs:631-656`.

**Data-source check (do first):** the suppression needs the character's
`characterDocumentMountPointId` client-side. If the field is absent from
the client type but already present on the wire (verify against the Rust
read DTO), typing the already-sent optional field on the existing
interface is allowed and is NOT a §1 breach (no new verb, no payload
change) — document it. If the server does NOT send it, STOP and escalate
(§1).

## Tiered deliverables

**Tier 1 (must land):**

1. The cost-estimator consolidation — one trait, one no-cost default, one
   host pricing impl; both tier-3 differentials green over FRESH oracles;
   the acknowledged-duplication comments retired with it.
2. The stale-seam-note sweep — every enumerated target rewritten per the
   rewrite rule; the still-correct list untouched; one commit (comment-only
   plus the version bump the commit checklist requires).

**Tier 2 (should land):**

3. The no-vault suppression on BOTH appearance tabs, v4-faithful: when
   `characterDocumentMountPointId` is falsy, the guidelines editor + Save
   are replaced by the verbatim warning paragraph (warning styling per the
   v5 idiom for `qt-text-warning`) and **no guidelines fetch fires** (v4
   `:54-56`). Plus the `aesthetic-editor-field.ts` docstring correction.
   Unit specs per tab: suppressed-when-no-vault (editor absent, warning
   text byte-exact, no fetch), active-when-vaulted (unchanged behavior).

**Tier 3 (explicit loud deferrals — name them in the status log):**

- The default/boxed `ChatCostSummary` variant and the client
  `detailed=true` arm stay UNPORTED — confirmed dead in v4 (the only
  caller, `SalonView.tsx:1014`, passes `variant="compact"` at `:1017`; no
  caller exercises the boxed render `:139-175` or its loading pulse
  `:90-99`). The WON'T-PORT verdict is lane C's to record in the M6
  checklist — do not port them here.
- A `disabledHint` input on the shared `aesthetic-editor-field` component
  itself stays unported (no v5 consumer would pass it — the guidelines
  fields don't use that component); the docstring records this.

## The differential requirement (restated)

Item 1 is a behavior-frozen refactor: **the existing differentials ARE the
equivalence proof.** Regenerate the title-update and carina
tier-3 oracles FRESH from the v4 checkout at `02865bdb` (each test file's
header documents its regen command), run both by name with `--nocapture`,
and grep the output for `SKIP` (the `p4d3-db-size-reduction-drift` lesson —
a skipped differential proves nothing). `cost_background_routes_equivalence`
and the pricing-fetcher tests must pass untouched. Item 2 is comment-only.
Item 3 is client-only UX with no wire change: the proof is the spec pair
per tab plus the verbatim v4 string pinned in the spec.

## Fixtures

None consumed beyond the committed families the existing differentials
already use; none delivered. No fixture regeneration.

## Shared contract (BINDING — reproduced verbatim in all three orders)

**§1 — no new wire surface.** This round adds NO `CoreRequest`/
`CoreResponse` variants, no new REST edges, no `core-contract.ts` union
folds, and no fixture families. Typing an already-transmitted field on an
existing client interface is permitted (document it); a new verb, route,
or payload change is an escalation — STOP and report rather than
improvise a fold.

**§2 — the one cross-lane visible change.** Lane B flips
`qt-markdown-field`'s source-toggle to v4's default-ON
(`MarkdownLexicalEditor.tsx:150`), so at unification EVERY markdown-field
consumer — including the two appearance tabs lane A edits — grows a
source button. Lane A must not assert toolbar button inventories in its
new specs; lane B must keep the toggle self-contained inside the field so
no consumer file needs edits.

**§3 — merge mechanics.**

- `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` are
  append-only for all lanes (union-merge at unification).
- Version bumps: lane A bumps the crates it touches (core, host, harness
  if touched) per commit AND the SPA patch version
  (`apps/web/package.json`) on its SPA commits; lane B bumps the SPA
  patch per commit; lane C bumps nothing (docs-only). The unifier
  accumulates and syncs the lockfile's own version fields (the P4.6an
  lesson).
- Full-Playwright runs contend on the fixed port 4319 — run the suite
  alone, from `apps/web` (the P4.6au round lessons).

## Ownership (BINDING — reproduced verbatim in all three orders)

- **Lane A (P4.6aw)** owns `crates/**` and, on the SPA side, EXACTLY:
  `apps/web/src/app/screens/characters/edit/appearance-tab.ts` (+spec),
  `apps/web/src/app/screens/characters/view/tabs/character-appearance-tab.ts`
  (+spec), the `apps/web/src/app/ui/aesthetic-editor-field.ts` docstring
  (comment-only), and — only if the data-source check requires it —
  typing an already-sent field in `core-contract.ts`. It does NOT touch
  `apps/web/src/app/editor/**`, `chat/**`, `harness/oracle/**`, or any
  other SPA file.
- **Lane B (P4.6ax)** owns `apps/web/src/app/editor/**` (+specs), its new
  committed recorded-vector files, and new `harness/oracle/cases/` files
  for the editor oracles. It does NOT touch `crates/**`, `screens/**`,
  `chat/**`, or `ui/**`.
- **Lane C (P4.8)** is READ-ONLY except for its one new document,
  `docs/developer/porting/m6-screen-parity.md`. No code edits — findings
  are recorded, not fixed.
- Everyone: `docs/CHANGELOG.md` and `status-log.md` append-only.

## Verification gate (before declaring the lane done)

1. `cargo fmt --all --check` clean; `cargo clippy --workspace
   --all-targets -- -D warnings` clean on BOTH feature sets (default +
   `quilltap-core/native-transport`), exit codes checked under `pipefail`
   (the `| tail` gotcha).
2. `cargo test --workspace` — zero failures; the title-update and carina
   tier-3 differentials run by name over FRESH `02865bdb` oracles,
   `--nocapture`, no `SKIP` in the output.
3. `ng test` (from `apps/web`) — zero failures, the new tab specs
   included; `ng build` clean.
4. Full Playwright green in the worktree (no new beats; the characters
   walks must stay green), run alone on 4319.
5. Per-commit rule: follow `.claude/commands/commit.md` (version bumps,
   CHANGELOG every commit, status-log append).
