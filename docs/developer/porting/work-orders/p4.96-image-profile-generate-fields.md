# Work order P4.96 — P4.D196's Tier-2 item 10, closed with evidence: the profile-id generate verb (`imageProfileGenerate` / v4's `POST /api/v1/image-profiles/[id]?action=generate`) carries v4's five optional body fields (`size` / `quality` / `style` / `aspectRatio` / `negativePrompt`) through `Request::ImageProfileGenerate` into the tool input exactly as v4's `generateImageSchema` + `executeImageGenerationTool` do — RAW `Option<Value>` crossing in the `ChatCreate`-trio idiom so both transports answer v4's bytes, `quality` through the ONE quality list and `style` through the two-value enum with v4's 400 envelope, the dispatch wrong-type census moved by exactly the five rows, and `image_generate_route_equivalence` grown a row per field plus the refusal arms; the variant's stale doc corrected — no v4 drift; the `53294163f` round's "What is next" item 2, from `main`

**Status: CLOSED WHOLE at lane close (2026-09-17).** Ordered 2026-09-17 as part
of the `bcd7e4852` bug-151 drift catch-up + follow-ups round (P4.D198 ∥ P4.D199
∥ P4.96 ∥ P4.97). Lane branch `claude/image-profile-generate-fields-1341bb`,
three commits. The unifier rewrites this header at unification; the full lane
record is in `status-log.md` → "P4.96 — the profile-id generate route's five
shaping fields".

**Landed: every Tier-1 item (1–8) and both Tier-2 items (9, 10).** The §2
freshness probe PASSED at lane start and again before the regen batch; every
regen ran from `/tmp/qt-v4-pin-p4-96-5f0a57dc4` (`rev-parse` verified), and the
order's pre-regen surface diff came back a version stamp only, as predicted. No
committed fixture changed, so no sibling family is invalidated.

**Three of the order's prescriptions were refuted by measurement and are
recorded, not silently varied:**

1. **Item 1's `Option<serde_json::Value>` is not enough — the five are
   `Option<Option<serde_json::Value>>` under `double_option`.** Serde collapses
   an explicit JSON `null` into `None` for a plain `Option<T>`, so
   `{"size": null}` reached the handler as an ABSENT key and passed a gate v4
   400s — the exact evidence-destroying collapse the raw crossing exists to
   prevent. Item 5's wire test caught it on its first run; the differential
   could not, because it drives the handler directly and never crosses serde.
   (`CHAT_CREATE_TRIO`, the idiom the order names, already records
   `Option<Option<Value>>` for this reason.)
2. **Item 4's "five new `CENSUS` rows" would redden two existing tests.**
   `CENSUS` is a census of the serde boundary's TYPED fields and asserts set
   equality against a mechanical walk, so a `Value`-carrying row is an `extra`;
   and `body_sourced_rows_are_serde_type_rejected_today` asserts every body
   row's decode REJECTS a wrong type, which is the opposite of what these five
   must do. They landed as a sibling list, `IMAGE_PROFILE_GENERATE_RAW_FIVE`,
   in the `CHAT_CREATE_TRIO` shape the order's own parenthesis names.
   `EXCLUDED_BY_THE_ROUTE_IDENTIFIER_RULE` **stayed 442**, the expectation
   recorded before the test was run.
3. **Item 7's happy `aspectRatio: "3:2"` row is a 400, not a 201.** v4's ROUTE
   declares `aspectRatio: z.string()` while the TOOL declares a five-member
   enum, so `3:2` passes the route, reaches `executeImageGenerationTool` and is
   refused THERE with v4's blanket `Invalid input: prompt is required…`
   sentence. The row was kept as
   `generate_aspect_ratio_off_tool_enum` — it is what forbids the route from
   pre-gating on the tool's enum — and a separate `generate_aspect_ratio_ok`
   row carries the happy `16:9` case.

**Two pre-existing v5 divergences closed on the way**, neither in the order:
`chatId` had no `z.uuid()` gate at all, and `count` had no `z.int()` check, so
`1.5` reached the tool.

**A mutation proof SURVIVED as first written** (§R.6) and was fixed rather than
deleted: deleting a row from the raw-five list reddened nothing, because the
list was only ever iterated. It is now held against a source walk of the
variant by name AND declared shape — which also closed a second gap the next
mutation exposed, the rows' unasserted `rust_type`.

**Nothing stays OPEN under this order.** The Tier-3 deferrals are recorded with
their measurements in `status-log.md` §9.

## Preamble

- **v4 oracle baseline: `5f0a57dc4`.** No drift on this lane's surfaces:
  `bcd7e4852` touches neither `app/api/v1/image-profiles/**`, `lib/tools/
  **`, `lib/image-gen/**`, nor any plugin (measured at planning: `git diff
  --stat 5f0a57dc4..bcd7e4852 -- app/api/v1/image-profiles lib/tools
  lib/image-gen plugins packages` is a version stamp only). The v4 spec is
  `git show 5f0a57dc4:'app/api/v1/image-profiles/[id]/route.ts'` (`:19-32`
  the schema, `:223-290` the POST handler) and `lib/tools/handlers/image-
  generation-handler.ts` (`executeImageGenerationTool` `:1163`; the input
  fields threaded at `:258-263`) — **the shipped code, never the P4.D196
  doc comment's paraphrase** (§R.4; that comment says `Option<String>` on
  the variant, and the tree's LATER-ruled idiom for body-sourced fields
  says otherwise — see Tier 1 item 1).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** Pin **`/tmp/qt-v4-pin-p4-96-
  5f0a57dc4`** (the BASELINE; your v4 surfaces are identical at the
  target — record `git -C ~/source/quilltap-server diff --stat
  5f0a57dc4..bcd7e4852 -- app/api/v1/image-profiles lib/tools lib/image-gen
  plugins packages/plugin-types` in the lane record before the first regen)
  for `image_generate_route_equivalence`. ⚠ This family shares the
  `/tmp/qt-imggen-{main,mount}.db` pair AND the `QT_FIXTURE_IMGGEN_*` env
  names with `image_generation_tier3` (the recorded collision — CLAUDE.md
  Status, the `53294163f` round record): stage YOUR pair under `/tmp/
  p4.96/` by setting the env vars to lane-private paths for every regen
  and run, and never touch `image_generation_tier3`'s oracle or fixture;
  the unifier regenerates the tier-3 family LAST and runs yours by name
  against its own snapshot (§R.10(d)).
- **Sibling lanes:** P4.D198 / P4.D199 (the bug-151 drift — `chat_files.
  rs`, `image_processing.rs`, `message_context.rs`, `help/**`; you touch
  none), P4.97 (`services/primary_stream.rs` + the recorded provider
  corpora; you touch none). **You are the ONLY lane this round that edits
  `api/types.rs`, `api/engine.rs`, or the dispatch wrong-type census** —
  the unifier asserts the other three lanes' diffs are EMPTY on those
  paths (§R.10(c)).
- **Worktree:** `git worktree add <path> -b claude/p4-96-image-profile-
  generate-fields-<hash> main`; `CARGO_INCREMENTAL=0`. SPA work is ONE
  contract type (Tier 1 item 6) — `npm ci` the worktree's `apps/web` for
  the build/test gate (`spa-worktree-node-modules`).
- **Read before the work:** `crates/quilltap-core/src/api/image_profiles.
  rs:620-710` (the doc comment `:620-651` — its measurement paragraph
  `:633-639` is correct, its prescribed shape `:641-651` is superseded by
  item 1; the handler `:652-660`; the 404 gate `:662-666`; the prompt/count
  refusals `:685-690`; the tool input built with five hard `None`s at
  `:694-708`); `crates/quilltap-core/src/api/types.rs:1047-1062` (the
  variant — its doc `:1047-1053` is STALE: "still a loud refusal arm" —
  `engine.rs:2931` refutes it; fix the doc); `crates/quilltap-core/src/
  api/engine.rs:2928-2951` (the arm; `ready_generate_image()` `:2937`);
  `crates/quilltap-core/src/api/images.rs:1361-1475` (`GenerateBody` +
  `parse_generate_body` — the SIBLING route's ruled shape: RAW `Option<
  Value>` fields on `Request::ImagesGenerate` at `types.rs:3524-3539`
  under the `ChatCreate`-trio rule stated at `:3513-3519`, the quality
  gate `:1462-1469` through `is_image_quality`, the style gate `:1470-
  1475`); `crates/quilltap-core/src/image_gen/quality.rs:29-34`
  (`IMAGE_QUALITY_VALUES` — the eight; `is_image_quality`); the style
  duplicate at `tools/generate_image.rs:213` (`SCHEMA_STYLES`) and
  `api/images.rs:1472` (inline); `crates/quilltap-web/tests/dispatch_
  wrong_type_census.rs` (`CENSUS` `:128-2279`; the two `ImageProfile
  Generate` rows `:820-842`; `is_route_identifier` `:2439-2448`; the
  running ledger `:2450-2532` and `EXCLUDED_BY_THE_ROUTE_IDENTIFIER_RULE =
  442` at `:2533`; the test `:2536`; the STALE header figure at `:121` —
  "403 of 643"); `crates/quilltap-harness/tests/image_generate_route_
  equivalence.rs` (the `Case` struct — `name/id/prompt/chat_id/count` ONLY;
  the eight cases `:526-594`; the handler call `:620`) + `harness/oracle/
  cases/image-generate-route.test.ts` (header recipe `:43-52`; it drives
  v4's REAL route handler) + `harness/oracle/fixtures/image-generation.
  json` + `build-image-generation-fixture.ts`; `docs/developer/porting/
  work-orders/p4.62-…` and `p4.73-…` (the wrong-type-collapse rulings —
  present-but-invalid answers v4's 400, never a silent keep and never a
  serde decode error at the edge).

## Mandate

Make the profile-id generate verb carry v4's five optional shaping fields
end to end: parsed where v4 parses them (a validation stage ahead of any
work, answering v4's exact 400 envelope for a wrong type or an unknown
enum value), threaded into the SAME tool-input keys v4 threads
(`size`, `quality`, `style`, `aspectRatio`, `negativePrompt` → the v5
`ImageGenerationToolInput` twins that today are hard `None`), on BOTH
transports (dispatch is the only REST path for this verb in v5 — there is
NO `/api/v1/image-profiles` edge in `quilltap-web`, measured; do not add
one — that is `p4.9k`/the SPA's future, not this order). Move the dispatch
wrong-type census by exactly the rows the five fields add, with the
arithmetic ledger extended. Grow the differential a row per field and per
refusal. This lane does NOT touch `image_dialects.rs`, `generate_image.rs`
(the tool's own schema is P4.D196's — you only READ `SCHEMA_STYLES`), the
`images` collection route's own body parser (`api/images.rs` — a sibling
you MIRROR, not edit, unless Tier 2 item 9 consolidates the style list and
you say so), `chat_files.rs` / `message_context.rs` / `help/**` (the drift
lanes'), `primary_stream.rs` (P4.97's), or the drift ledger.

## Survey-verified starting points (fresh survey 2026-09-17 at `5f0a57dc4` and on `main` `78884832` — do not re-derive)

### v4 (at `5f0a57dc4`)

- **`app/api/v1/image-profiles/[id]/route.ts`**: `generateImageSchema =
  z.object({ prompt: z.string().min(1).max(4000), chatId: z.uuid().optional(), count: z.int().min(1).max(10).optional().prefault(1), size:
  z.string().optional(), quality: imageQualitySchema.optional(), style:
  z.enum(['vivid', 'natural']).optional(), aspectRatio: z.string().
  optional(), negativePrompt: z.string().optional() })` `:21-30`. The
  POST handler `:223-290`: `getActionParam` → `action !== 'generate'` →
  `badRequest('Unknown action: …. Available actions: generate')`; the
  404 (`notFound('Image profile')`) BEFORE the body read; `body = await
  req.json(); validated = generateImageSchema.parse(body)` (a thrown
  ZodError — `lib/api/responses.ts:109-113` renders it as `{ error:
  'Validation error', details: … }`; the exact `details` bytes are what
  you MEASURE through the oracle, never assume — P4.78's `chatCreate`
  arms are the precedent); `executeImageGenerationTool({
  prompt, count, size, quality, style, aspectRatio, negativePrompt },
  { userId, profileId: id, chatId })`; `!result.success` → `badRequest
  (result.error || 'Image generation failed')`; the 201 body `{ success,
  data: result.images, expandedPrompt, metadata: { originalPrompt,
  provider, model, count } }`. Zod 4 semantics to reproduce (the tree's
  hand-rolled Zod twins are 4.5.4 — `a-nullable-optional-column…`,
  `spa-has-no-zod`): `.optional()` accepts ABSENT and `undefined` — a
  JSON `null` for `size` is a wrong type (400), not "absent"; `z.string()`
  rejects a number/bool/object; `z.enum` rejects an unknown string with
  the enum's own issue; `prompt`'s `.max(4000)` counts CODE POINTS (Zod
  4.5's rule, the `d883a5ee1` round) — the existing `astral` cases pin
  it. `count`'s `z.int()` rejects `1.5` and `"2"`.
- **`lib/image-gen/quality.ts`**: `IMAGE_QUALITY_VALUES = ['auto', 'low',
  'medium', 'high', 'xhigh', 'max', 'standard', 'hd']`; `imageQualitySchema
  = z.enum(...)`.
- **`lib/tools/handlers/image-generation-handler.ts`**: the input
  interface's five optional fields and where they are read (`:258-263`
  thread `negativePrompt`, `aspectRatio`, `style`; `size`/`quality` per
  `:227` and the P4.D196-ported `resolveQuality`) — READ ONLY; the tool
  side is ported (P4.D196) and unchanged by this order.

### v5 (on `main` `78884832`)

- **`api/image_profiles.rs`** `image_profile_generate(db, runner, user_id,
  profile_id, prompt, chat_id, count) -> Response` `:652-660`; the tool
  input `:694-708` with `negative_prompt: None, orientation: None, size:
  None, style: None, quality: None, aspect_ratio: None`.
- **`api/types.rs:1047-1062`** — `ImageProfileGenerate { image_profile_id:
  String, prompt: String, #[serde(default)] chat_id: Option<String>,
  #[serde(default)] count: Option<i64> }` (camelCase). `prompt` and
  `count` are serde-TYPED (their census rows are `V4::BodyParse`,
  "serde-type-rejected today" — a recorded pre-existing class you do NOT
  reopen; the five NEW fields follow the newer rule).
- **`api/engine.rs:2928-2951`** — the arm; `runner = self.ready_generate_
  image()?`.
- **`quilltap-web`** — NO `/api/v1/image-profiles` route (`lib.rs`
  registers `embedding-profiles`, `images`, `images/{id}`, `user/profile`;
  the only hits for `image-profiles` in the crate are census prose). The
  HTTP path is `POST /api/dispatch` → `serde_json::from_slice::<Request>`
  (`dispatch.rs:69`).
- **The sibling's ruled shape** (`Request::ImagesGenerate`, `types.rs:
  3524-3539`): every body-sourced optional as `Option<serde_json::Value>`
  (RAW), refused in the HANDLER with v4's envelope, so Tauri IPC and HTTP
  answer the same bytes (`:3513-3519` states the rule; P4.62/P4.73 ruled
  it).
- **The census** `dispatch_wrong_type_census.rs`: `CENSUS` rows carry
  `{variant, field, rust_type, v4 classification}`; the excluded-by-rule
  constant `442` `:2533` with the ledger `:2450-2532`; the enforcing test
  `:2536-2547`; `body_sourced_rows_are_serde_type_rejected_today` `:2648`;
  `chat_create_trio_fixed_by_p4_73_decodes_raw_so_the_handler_can_refuse`
  `:2675` — the shape your five rows join (RAW-crossing, handler-refused).
- **`image_generate_route_equivalence.rs`** — env `QT_ORACLE_IMGGEN_ROUTE`
  + `QT_FIXTURE_IMGGEN_{MAIN,MOUNT}`; `Case { name, id, prompt, chat_id,
  count }`; eight cases; the oracle case posts a JSON body to v4's real
  handler and records status + body. The Rust side calls the handler
  directly (`:620`) — so the FIVE fields must reach the handler's
  signature (or a body struct) for the Rust side to drive them; the
  dispatch path is pinned separately (item 5).
- **`is_image_quality`** `image_gen/quality.rs:34`; the style enum is
  spelled THREE times (`tools/generate_image.rs:213`, `api/images.rs:
  1472`, `model/openai_image_options.rs:166-167`); the wire default
  `"vivid"` at `image_dialects.rs:402`.

## Tiered deliverables

### Tier 1 — must land

1. **The variant.** Five new fields on `Request::ImageProfileGenerate`
   (`api/types.rs`), camelCase on the wire (`size`, `quality`, `style`,
   `aspectRatio`, `negativePrompt`), each `#[serde(default)] Option<serde_
   json::Value>` — the RAW crossing of the `ChatCreate` trio / `Images
   Generate` idiom, NOT `Option<String>`: a serde-typed field turns v4's
   400 `Validation error` into a dispatch decode error at the web edge
   and into a different sentence over Tauri IPC, which is the exact
   divergence P4.62 ruled out. State this in the variant's doc and
   CORRECT the stale "loud refusal arm" paragraph (`:1047-1053`) while
   there (the arm has been live since P4.6ab's un-refusal).
2. **The parse stage**, in `api/image_profiles.rs`, AHEAD of any work and
   in v4's order: 404 first (v4 reads the body AFTER `findById` — a
   missing profile with a garbage body is a 404, pin it), then the whole
   body as ONE validation (P4.78's `chatCreate` shape — a single stage
   that answers v4's Zod envelope; measure the envelope's exact bytes for
   a wrong-type `size`, a `null` `size`, an unknown `quality`, an unknown
   `style`, a non-string `negativePrompt`, and a wrong-type `count`
   through the oracle FIRST, then reproduce: the `details` array if that
   is what `createContextParamsHandler` renders, with Zod 4.5.4's issue
   sentences from the tree's existing twins — never hand-write a Zod
   sentence you have not measured). `quality` through `is_image_quality`;
   `style` through the two-value enum; `size` / `aspectRatio` /
   `negativePrompt` as free strings (any string passes; a non-string
   400s). Hand the five into the tool input's existing keys (`size`,
   `quality`, `style`, `aspect_ratio`, `negative_prompt` — `orientation`
   stays `None`; v4 passes none).
3. **The engine arm** (`engine.rs:2928-2951`): thread the five through to
   the handler — extend the handler's signature with a small body struct
   (`ImageProfileGenerateBody { prompt, chat_id, count, size, quality,
   style, aspect_ratio, negative_prompt }` or the raw `Value`s — say
   which) rather than nine positional parameters; the harness calls the
   handler directly, so the struct is what the differential drives.
4. **The census.** Five new `CENSUS` rows for `ImageProfileGenerate`
   (`size` / `quality` / `style` / `aspectRatio` / `negativePrompt`, each
   `Option<Value>`, the `V4::BodyParse` classification with the
   RAW-crossing shape the P4.73 test asserts); the running ledger gains
   its next line; `EXCLUDED_BY_THE_ROUTE_IDENTIFIER_RULE` moves ONLY if
   the rule's arithmetic says so (none of the five is a route identifier
   — expect 442 → 442 and RECORD the expectation before running the
   test; if it moves, explain the arithmetic in the ledger comment). Fix
   the stale header figure at `:121` while there (it is your file this
   round).
5. **The dispatch-path pin.** A `quilltap-web` wire test (the P4.D65
   REST-edge-live-wire idiom over `POST /api/dispatch`) posting `{ "type":
   "imageProfileGenerate", … , "quality": 7 }` and asserting v4's 400
   ENVELOPE — not a serde decode error — plus a well-formed body reaching
   the handler with all five set (a stub `ErasedImageGeneration` that
   records its input; assert the five keys arrived). Mutation: change one
   field to `Option<String>` → the wrong-type arm's body changes from v4's
   envelope to serde's sentence → the test reddens.
6. **The SPA contract type** — `apps/web/src/app/core/core-contract.ts`
   declares the verb's request body (`type: 'imageProfileGenerate'` at
   `:4998`, measured at planning); add the five as OPTIONAL fields with v4's
   camelCase names so the unifier's §1 name-for-name diff stays clean;
   NO SPA behaviour change, no call site sends them yet (the manual
   image-generation dialog v4 fixed in bug 150 has no v5 twin — P4.D197
   measured). `npm run build` + `npm test` once.
7. **The differential.** `image_generate_route_equivalence` grown: widen
   `Case` (both sides) with the five optionals; rows — a happy row per
   field (`size: "1024x1536"`, `quality: "max"`, `style: "natural"`,
   `aspectRatio: "3:2"`, `negativePrompt: "no hats"`) asserting the 201
   body AND the tool input the stub runner received (the Rust stub
   records it; the oracle records v4's `executeImageGenerationTool` input
   through the mock the case already installs — measure that it exposes
   the input; if not, add the recording in the P4.92 side-channel shape);
   a row with ALL five; refusal rows — `quality: "ultra"`, `style:
   "sketch"`, `size: 1024` (number), `size: null`, `negativePrompt: {}`,
   `aspectRatio: true`, `count: "2"` (string — pins the pre-existing
   serde-typed class AS RECORDED, both sides), and the 404-beats-400
   composite (missing profile + garbage body). Count guard `>= 20`
   (8 + ≥12). RED-FIRST: the happy rows red on unported `main` (the five
   never reached the runner — record the pre-fix count), the refusal rows
   red (v5 answered 201 having ignored the key — the silent-keep class).
8. **Mutation proofs:** M1 drop the `style` gate → the `sketch` row reds;
   M2 thread `quality` but not `size` → the size happy row reds; M3 parse
   AFTER the 404 gate becomes parse BEFORE → the composite reds
   (guard-order class, `a6870c5a`); M4 `Option<String>` on one field → the
   wire test (item 5) reds; M5 the census row deleted → the census test
   reds.

### Tier 2 — should land

9. **The style list's ONE home** — `image_gen/style.rs` (or a `const` in
   `quality.rs` beside `IMAGE_QUALITY_VALUES`, mirroring v4's ONE-list
   precedent) with `tools/generate_image.rs:213` and `api/images.rs:1472`
   repointed to it (`openai_image_options.rs:166-167` is a UI label list
   — leave it). Behaviour-neutral: `image_dialects` / `tool_definitions`
   / `images_generate_route` re-run by name at the pin, byte-identical.
   Say in the lane record which files this expanded your ownership to;
   nobody else touches them this round.
10. **The variant doc + the `image_profiles.rs` doc comment** rewritten to
    describe the landed shape (delete the "ORDERED FOLLOW-UP" paragraph).

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **A `quilltap-web` REST edge for `/api/v1/image-profiles/{id}?action=
  generate`** — v5 has none (dispatch-only); adding one is a new route,
  not this order's; the CLI/SPA reach the verb through dispatch. Record
  the measurement so the next reader does not re-derive it.
- **`orientation`** — v4's route passes none; stays `None`.

## The differential requirement, restated

Tier 2 real-DB route family: `image_generate_route_equivalence` drives
v4's REAL `POST` handler (the case's own header recipe — a jest case over
the shared `image-generation.json` fixture, real-DB-under-jest); the Rust
side drives the handler directly with the same rows and a recording stub
runner. Every new row lands on BOTH sides from ONE case list (the
`a-case-added-only-to-the-oracle-is-never-run` class this very family
suffered last round — the Rust `cases()` list and the oracle's must be
diffed name-for-name in the lane record). The dispatch wire pin (item 5)
has no oracle — it is v5's own composition.

## Fixtures

- Consumes `harness/oracle/fixtures/image-generation.json` + `build-image-
  generation-fixture.ts` (edit ONLY if a row needs a profile the fixture
  lacks — expect not); stages the pair under `/tmp/p4.96/` (the collision
  rule above).
- Delivers: the grown case list (both sides), the census rows, the wire
  test.
- Nobody else regenerates this family this round; the unifier regenerates
  `image_generation_tier3` LAST and re-runs yours by name (§R.10(d)).

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `bcd7e4852` bug-151 drift catch-up + follow-ups round:
**P4.D198 ∥ P4.D199 ∥ P4.96 ∥ P4.97**, ordered 2026-09-17. Four lanes, four
worktrees, four lane branches, ALL from `main` (no stacking — every lane's
ownership is disjoint by file; the one cross-lane constant is handed off by
a marked local definition, §R.10(a)). The unifier cherry-picks in the order
**P4.D198 → P4.D199 → P4.96 → P4.97** (the loader half before the walk half
that reads its output; the two maintenance lanes after the drift), gates
ONCE, and `merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`5f0a57dc4`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`bcd7e4852`**
  ("fix(images): send a model the picture, not the archive of it (bug
  151)", `4.10.0-dev.45`, 2026-09-17 10:15) — ONE commit past the
  baseline, a PORT on ported surfaces: `lib/files/llm-image-budget.ts`
  (NEW), `lib/chat-files-v2.ts` (both attachment loaders), `lib/services/
  chat-message/context-builder.service.ts` (the Lantern walk's per-turn
  byte budget), `help/connection-profiles.md` (+12), plus tests, docs,
  v4's CLAUDE.md rule line and version stamps. The ledger's one §3 row is
  `ORDERED(P4.D198, P4.D199)`. v4 `bugfix` tip `1a2b2164c` and `release`
  tip `8fbf2afe0` are unmoved; there is still no `release: 4.10.0` squash.
  The checkout sat on `main`, CLEAN, at the planning probe (2026-09-17
  afternoon — the §2 probe PASSED, so the ledger stood and nothing was
  re-derived). **At unification the baseline MOVES to `bcd7e4852`**;
  `bcd7e4852` retires `ABSORBED(P4.D198, P4.D199)` with its non-lib files
  ratified on P4.D199's list.
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log bcd7e4852..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Pass = branch `main`, HEAD `bcd7e4852`, both logs EMPTY, tree
  CLEAN.** Any mismatch — a moved HEAD, a dirty `lib/`/`app/`/`components/`/
  `packages/`/`plugins/`/`help/`/`public/schemas/` tree, the checkout off
  `main` — is a STOP: report it and do nothing else. A lane never writes
  the ledger.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1). HEAD is past the
  baseline and `bcd7e4852` moves `lib/` and `help/`. Every regen runs from
  a LANE-UNIQUE detached worktree per ledger §5.1 (`git -C ~/source/
  quilltap-server worktree add --detach /tmp/qt-v4-pin-<order>-<sha>
  <sha>`, the THREE symlink classes — root `node_modules`, `packages/
  quilltap/node_modules`, every `plugins/dist/*/node_modules` — then `cd`
  into it; never stash or checkout-switch the human's repo; `git worktree
  list` before each batch; **verify the pin by `git -C "$PIN" rev-parse
  HEAD` AND by `ls -ld` of the path** — the last unification's two pins
  were created at a path with a trailing space because zsh does not
  word-split `set -- $pin`; build every pin path from a quoted literal,
  never an unsplit variable; then grep the fresh NDJSON for a marker only
  one tree has; check the file is non-empty before believing a diff).
  Which sha:
  - **P4.D198 and P4.D199 pin the TARGET `bcd7e4852`** for every family
    their port MOVES (P4.D198: the NEW `llm_image_budget_equivalence`,
    `file_attachment_tier3`; P4.D199: `orchestrator_tier3`, `build_
    context_tier3` if its seams are real, `help_tree_equivalence` and the
    `help_*` content families that read `connection-profiles.md`) and the
    BASELINE `5f0a57dc4` for their neutrality legs. The target-pinned
    help-tree oracle MUST carry `A travelling portrait packs light` and
    the baseline one MUST NOT — that pair is the pin verification.
  - **P4.96 and P4.97 pin the BASELINE `5f0a57dc4`** — their v4 surfaces
    are untouched by the drift (each lane records `git -C ~/source/
    quilltap-server diff --stat 5f0a57dc4..bcd7e4852 -- <its v4 paths>`
    as a version stamp or EMPTY in its lane record before the first
    regen). A pin at the baseline is still REQUIRED, not optional: the
    checkout can go dirty mid-lane (ledger §5.1), and a bare-checkout
    regen is unrepeatable.
  The sweep driver is the sanctioned per-family path (`harness/tools/
  recipe_sweep.py --run <family> --v4 "$PIN" --v5w <ABSOLUTE worktree>`);
  never run two sweeps concurrently; a family that reports `SKIP` in
  0.00 s did not run (`a-family-env-var-is-not-its-regen-var`). Ledger
  §5.2's silent-stale-pass discipline applies to every regen (`rm -f`
  the fixture first; read the builder's last line; `grep -c` the changed
  bytes in the fresh NDJSON — MUST be > 0). **Shared `/tmp` output paths
  are a live hazard** (P4.92's mid-lane clobber; the `/tmp/qt-imggen-*`
  pair collision): every lane stages its regen outputs under a
  LANE-PRIVATE directory (`/tmp/<order>/…`) by pointing the family's env
  vars there, and leaves the committed recipe headers canonical (a
  committed recipe NEVER names a `/tmp` pin — the driver's `--v4` is the
  pin).
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). The spec
  for every item is `git show <sha> -- <path>` plus `git show <sha>:<path>`
  at `bcd7e4852` (the drift lanes) or `5f0a57dc4` (the maintenance
  lanes); a commit message describes the bug as its author understood it
  and the hunks are often narrower. Re-verify even where an order flags
  the trap — the order's paragraph is the same kind of prose, and so is
  a previous lane's record or a doc comment in the tree (P4.D196's
  `Option<String>` paraphrase; P4.95's "no case reaches the retry" — both
  corrected by this round's surveys). Carry a v5-side code comment naming
  the sha where the prose misleads.
- **§R.5 — Designed trips and pre-existing reds: measure, don't assume**
  (ledger §5.4). No convergence pin trips this round (v5 filed nothing
  bug 151 answers). The drift lanes' pre-fix reds are DESIGNED: P4.D199's
  budget arms red on unported `main` (v5 sends every unseen image) and
  P4.D198's shrink rows red (v5 sends stored bytes) — record the pre-fix
  counts. A red that appears in a family a lane did NOT touch is a NEW
  finding to classify at both pins before any source is touched, and it
  is probably not that lane's.
- **§R.6 — The differential requirement stands for every unit.** A
  ported unit lands WITH an equivalence test that drives v4's REAL code
  at the right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM), red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest`
  is shared) and its silence leg; a fixture change lands with the regen
  of every family that reads it. Mutation proofs: each named proof must
  redden exactly the test it is written against, and a proof that
  SURVIVES as first written is a finding to fix, not a line to delete
  (`a-guard-whose-other-conjuncts-are-false-is-untested`,
  `a-mutation-of-a-write-no-arm-makes-the-code-read`). A scripted
  encoder on BOTH sides (P4.D198) is a legitimate tier-1 instrument when
  the real encoders cannot be byte-compared (D19); it must be the SAME
  script, and the family must say so in its header.
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close; the
  unifier rewrites it at unification. `docs/developer/porting/drift-
  ledger.md` is NEVER written by a lane.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed (and `apps/web/package.json` when it touches the SPA) on each
  commit per `.claude/commands/commit.md`; the unifier RECOUNTS as base +
  the sum of every lane's bumps (the silent-auto-merge trap: identical
  bumps merge as ONE — it fires every round). Base at planning: core
  0.0.942, harness 0.0.834, web 0.0.150, host 0.0.137, cli 0.0.22, tauri
  0.0.7, SPA 0.5.730. Expected: **core** by all four lanes; **harness** by
  all four; **host** by P4.D198 ONLY (the codec implementation is host
  SOURCE; P4.D199's `help/` re-vendor changes the embedded table but no
  host source — the P4.D194 rule: no host bump for help alone); **web**
  by P4.96 ONLY (the census test and the dispatch wire test live in the
  web crate's `tests/`); **SPA** by P4.96 ONLY (the one contract type);
  **cli/tauri** by NOBODY.
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  P4.D199 pre-lists the three paths `bcd7e4852` moves (`docs/v4/CHANGELOG.
  md`, `docs/v4/developer/bugs.md`, the NEW `docs/v4/developer/bugs/fixed/
  bug-151-unbudgeted-image-bytes-on-the-wire.md`) with byte counts at the
  pin in its lane record; no lane copies them.
- **§R.10 — Cross-lane handoffs, named:** (a) **The budget constant.**
  P4.D198 DEFINES `pub const LANTERN_IMAGE_BASE64_BUDGET: usize = 2 *
  1024 * 1024` in the NEW `crates/quilltap-core/src/files/llm_image_
  budget.rs` (v4's home); P4.D199 CONSUMES it but its worktree cannot see
  the module, so it defines a PRIVATE twin in `services/message_context.
  rs` beside `ASSISTANT_IMAGE_LOOKBACK` with a `// P4.D199 → P4.D198
  handoff … §R.10(a)` marker line; the unifier replaces the private twin
  with `use crate::files::llm_image_budget::LANTERN_IMAGE_BASE64_BUDGET`
  after both picks (one-line wire; the marker is the grep). (b) **The
  loaders' output is the walk's input.** P4.D199 budgets whatever
  `LanternLoad.attachments[i]["data"]` (the base64 STRING) the seam
  returns; P4.D198 makes that string the POST-shrink base64. In each
  lane's worktree the other half is absent and both differentials pass
  with junk bytes / `NotConfiguredTranscoder` (the shrink `Err`s →
  unchanged; v4's sharp throws → unchanged); the unifier re-runs
  `orchestrator_tier3` and `file_attachment_tier3` over the UNION at the
  new baseline to prove the composition and records the counts. (c)
  **`api/types.rs` / `api/engine.rs` / the dispatch wrong-type census are
  P4.96's alone this round** — the unifier asserts `git diff main...
  <lane> -- crates/quilltap-core/src/api/types.rs crates/quilltap-core/
  src/api/engine.rs crates/quilltap-web/tests/dispatch_wrong_type_census.
  rs` is EMPTY for P4.D198, P4.D199 and P4.97 before picking. (d) **The
  `/tmp/qt-imggen-*` pair collision** (`image_generate_route` ↔ `image_
  generation_tier3`): P4.96 stages under `/tmp/p4.96/`; at the unified
  gate the tier-3 family is regenerated LAST and P4.96's family is run by
  name against its own snapshot with its oracle var withheld from the
  workspace block (the `53294163f` round's recorded protocol). (e) **The
  recorded provider corpora are P4.97's alone this round** (the
  request-envelopes re-record + the google recorder if touched); P4.D198's
  families are SDK-free. (f) **The primary-stream family's side-channel
  widening** (P4.97 item 1) and **the orchestrator family's side-channel
  widening** (P4.D199 item 4(b), ordered attachment filenames) are two
  DIFFERENT families' mocks — no shared file; if either lane finds the
  widening belongs in a shared helper under `harness/oracle/lib/`, it
  creates a NEW file rather than editing an existing shared one.
- **§R.11 — No SPA behaviour moves this round.** P4.96's one contract
  type is additive and optional; no beat is added or flipped; the unified
  gate runs the full Playwright suite once against the release binary as
  regression only (the documented intermittents and the standing parks
  stand as named in the previous round's gate).

## §S — Shared contract (binding; byte-identical in P4.D198 and P4.D199)

- **§S.1 — The seam between the loader half and the walk half is the
  EXISTING `LanternLoad { prefix: String, attachments: Vec<Value> }`
  returned by `MessageContextSeams::load_lantern_images(&file_ids,
  provider)`** (`services/message_context.rs:614-617`, `:650-685`), and
  NOTHING about it moves: the attachment `Value`'s keys stay `id, filepath,
  filename, mimeType, size, data, url` (`chat_files.rs:250-258`); `data`
  is the base64 STRING of the bytes the turn will send (POST-shrink once
  P4.D198 lands; the stored bytes today); `size` is the byte length of
  those same bytes on the mount path; the attachments arrive in
  CHRONOLOGICAL order with only the raw-kept ones present (the fallback
  pass has already run — a described image contributes to `prefix`, not
  to `attachments`). P4.D199 reads `data.len()` and reorders/drops
  attachments; it never re-reads bytes, never calls a codec, and never
  edits `prefix`. P4.D198 never budgets, never reorders, and never drops
  an attachment for size (the provider backstop resize is a RESIZE, not a
  drop — v4 has none either).
- **§S.2 — The one constant with two homes until unification:**
  `LANTERN_IMAGE_BASE64_BUDGET = 2 * 1024 * 1024` (bytes of BASE64, spent
  against `data.len()`), public in P4.D198's `files/llm_image_budget.rs`,
  private in P4.D199's `message_context.rs` until §R.10(a)'s wire.
- **§S.3 — Nothing else on the wire moves from the drift.** No new
  dispatch verb, no new REST route, no `core-contract.ts` change from
  P4.D198 or P4.D199; a drift lane that finds it needs one STOPs on that
  item and records the shape for the unifier. (P4.96's five optional
  fields on `Request::ImageProfileGenerate` — camelCase `size`, `quality`,
  `style`, `aspectRatio`, `negativePrompt`, each `Option<serde_json::
  Value>` RAW-crossing, refused in the handler with v4's `Validation
  error` envelope — are that lane's own contract, mirrored into `core-
  contract.ts` by the same lane; no other lane sends them.)

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D198** — bug 151, the LOADER half | `crates/quilltap-core/src/files/image_processing.rs` (the `ImageTranscoder` trait's new `shrink_to_webp` default method + doc; nothing else in the file unless a helper is needed — say which); NEW `crates/quilltap-core/src/files/llm_image_budget.rs` + the `pub mod` line in `files/mod.rs`; `crates/quilltap-core/src/services/chat_files.rs` — `read_file_as_base64` and `load_mount_file_as_attachment` ONLY (the shrink-then-backstop sequence, the log filenames) — NOT `load_lantern_images` / `load_user_attachments` / `RealMessageContextSeams`; `crates/quilltap-host/src/image_codec.rs` (the `shrink_to_webp` impl + tests) and `crates/quilltap-host/Cargo.toml` ONLY if a feature flag is needed (say so); `crates/quilltap-harness/src/lib.rs` — the NEW `ScriptedTranscoder` (additive); NEW `harness/oracle/cases/llm-image-budget.test.ts` + a NEW script helper under `harness/oracle/lib/`; NEW `crates/quilltap-harness/tests/llm_image_budget_equivalence.rs`; `crates/quilltap-harness/tests/file_attachment_tier3_equivalence.rs`; `harness/oracle/cases/file-attachment-tier3.test.ts`; `harness/oracle/fixtures/file-attachment.json` + `build-file-attachment-fixture.ts`; NEW `crates/quilltap-harness/tests/lantern_loader_transcoder_wiring.rs` (the composition pin — or a test module in an existing host test file you own; say which); `crates/quilltap-core/src/files/mod.rs`; this order's header | `services/message_context.rs`, `services/orchestrator.rs`, `api/**`, `crates/quilltap-web/**`, `help/**`, `model/primary_stream.rs` (P4.97's — note the file is under `services/`), every recorded provider corpus under `harness/oracle/fixtures/{request-envelopes,google-*,image-dialects,…}/`, every other harness family, `apps/web/**`, the drift ledger |
| **P4.D199** — bug 151, the WALK half + help + ratifications | `crates/quilltap-core/src/services/message_context.rs` — section K (`:1337-1361`) + the private budget constant + the new warn + its `#[cfg(test)]` pins; `help/connection-profiles.md` (byte copy at the target pin); `crates/quilltap-harness/tests/help_tree_embed_guard.rs` — the history doc comment ONLY (124 stays 124); `crates/quilltap-harness/tests/orchestrator_tier3_equivalence.rs`; `harness/oracle/cases/orchestrator-tier3.test.ts`; `harness/oracle/fixtures/orchestrator-tier3.json` + `build-orchestrator-fixture.ts`; `crates/quilltap-harness/tests/build_context_tier3_equivalence.rs` + `harness/oracle/cases/build-context-tier3.test.ts` + `harness/oracle/fixtures/build-context-tier3.json` ONLY if item 4(c)'s measurement shows real seams (say so); this order's header | `services/chat_files.rs` (incl. `load_lantern_images` — READ ONLY), `files/**`, `services/orchestrator.rs`, `api/**`, `crates/quilltap-web/**`, `crates/quilltap-host/**` (no host bump — §R.8), every other `help/*.md`, `model/`/`services/primary_stream.rs`, every recorded provider corpus, every other harness family, `apps/web/**`, the drift ledger |
| **P4.96** — the generate route's five fields | `crates/quilltap-core/src/api/image_profiles.rs` (`image_profile_generate` + its doc); `crates/quilltap-core/src/api/types.rs` — the `ImageProfileGenerate` variant + its doc ONLY; `crates/quilltap-core/src/api/engine.rs` — the `ImageProfileGenerate` arm (`:2928-2951`) ONLY; NEW `crates/quilltap-core/src/image_gen/style.rs` (or a `const` in `image_gen/quality.rs`) + the repoints at `tools/generate_image.rs:213` and `api/images.rs:1472` ONLY if Tier-2 item 9 lands (say so); `crates/quilltap-web/tests/dispatch_wrong_type_census.rs`; NEW `crates/quilltap-web/tests/image_profile_generate_dispatch_wire.rs` (the dispatch-path pin); `crates/quilltap-harness/tests/image_generate_route_equivalence.rs`; `harness/oracle/cases/image-generate-route.test.ts`; `harness/oracle/fixtures/image-generation.json` + `build-image-generation-fixture.ts` ONLY if a row needs a profile the fixture lacks (say so — `image_generation_tier3` shares them); `apps/web/src/app/core/core-contract.ts` — the `imageProfileGenerate` request type ONLY; `apps/web/package.json` version; this order's header | `crates/quilltap-core/src/model/**` (incl. `image_dialects.rs`, `openai_image_options.rs`), `tools/generate_image.rs` beyond the one repoint, `crates/quilltap-web/src/**` (NO new REST edge), `services/**`, `files/**`, `help/**`, every other harness family (incl. `image_generation_tier3` and its oracle/fixture outputs), every other SPA file, the drift ledger |
| **P4.97** — the retry option bag + the envelope cache-key pin | `crates/quilltap-core/src/services/primary_stream.rs` — the retry block (`:1251-1309`) + its `#[cfg(test)]` module; `crates/quilltap-harness/tests/primary_stream_tier3_equivalence.rs`; `harness/oracle/cases/primary-stream-tier3.test.ts` (+ `openai-chaining-fallback-tier3.test.ts` ONLY if the mock widening is shared — say so); `harness/oracle/fixtures/primary-stream-tier3.json` + `build-primary-stream-fixture.ts`; `harness/oracle/providers/record-request-envelopes.mjs` + `regenerate-request-envelopes.sh` (+ `record-google-request.mjs` / `regenerate-google-wire.sh` ONLY if item 6's google arm records — say so); `harness/oracle/fixtures/request-envelopes/request-envelopes.recorded.ndjson` (+ `google-request.recorded.ndjson` under the same condition); `crates/quilltap-harness/tests/request_builder_equivalence.rs`; this order's header | `model/stream.rs` (the canned key), `model/request_builder/**` (READ ONLY — a builder red is a finding), `services/orchestrator.rs`, `services/provider_failover.rs`, `services/native_tool_loop.rs`, `services/text_tool_loop.rs`, `model/streaming_provider.rs`, every other recorded corpus (`image-dialects/`, `response-bodies/`, `stream-fixtures/`, `tool-wire/`, `moderation-wire/`, `web-search-wire/`), `api/**`, `crates/quilltap-web/**`, `services/chat_files.rs` / `message_context.rs` / `files/**`, `help/**`, every other harness family, `apps/web/**`, the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix — record the finding in the lane record and the status log for the unifier instead. `docs/developer/porting/drift-ledger.md` is never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note).

1. **§R.2 probe** — PASS (opening, and again before the regen batch).
2. `cargo fmt --all --check` — clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in
   BOTH feature sets (default; `--features quilltap-core/native-transport`).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the pin §R.3 assigns it, run with `--nocapture`, the log grepped for
   `SKIP:` (ZERO), the fresh NDJSON grepped for the changed bytes (> 0),
   and the red-first steps recorded with the pre-fix counts.
5. **The neutrality legs** the order names, regenerated at the BASELINE
   pin and compared (`cmp` / by-label diff) where the family is
   deterministic across regens; where it is not (`regenerate-at-both-
   pins-and-cmp-is-not-universal`), compare the normalized rows by case
   and record.
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean.
8. `cargo test --workspace --no-fail-fast` with the lane's env block
   (its own oracle vars + `QT_V4_ROOT`/`QT_V4_CHECKOUT` at the right pin +
   `QT_NODE` = the node BINARY): report **binaries / passed / failed /
   ignored** from the per-binary lines and the exit code, and the lane's
   families confirmed RUN by non-zero duration. The `/tmp/qt-imggen-*`
   collision: a lane that is not P4.96 withholds `QT_ORACLE_IMGGEN_ROUTE`
   from its block (the family SKIP-passes and is P4.96's to prove by
   name); P4.96 withholds `QT_ORACLE_IMGGEN` (the tier-3 family's) and
   proves its own by name on its private staging.
9. **SPA (P4.96 only):** `npm run lint`, `npm test` (spec files / tests /
   0 failed), `npm run build` clean — no e2e beat is added this round;
   the full Playwright run is the unified gate's.
10. **Ownership check:** `git diff --stat main...HEAD` — every path is in
    the lane's Owns column; every MUST-NOT-TOUCH path absent; for the
    three non-P4.96 lanes, `git diff main...HEAD -- crates/quilltap-core/
    src/api/types.rs crates/quilltap-core/src/api/engine.rs crates/
    quilltap-web/tests/dispatch_wrong_type_census.rs` is EMPTY (§R.10(c)).
11. **Per commit:** `.claude/commands/commit.md` in full — the equivalence
    test is non-negotiable (§1), the invariants (§2), real-instance safety
    (§3), the harness regen (§4), build/test/lint (§5), the CHANGELOG
    entry + version bumps (§7). Commit on the lane branch only; never
    merge to `main`; never `git stash`; never a worktree inside a worktree.
12. **Lane close:** update this order's status header (what landed, what
    was refuted by measurement, what stays OPEN with its named cause),
    append the lane record to `status-log.md` (the regen commands as run,
    the counts, the mutation table, every finding outside the lane's
    ownership for the unifier), and stop. The final gate runs in the
    FOREGROUND of the lane's last turn — a backgrounded gate whose
    notification arrives after the lane reports done is a lane that is
    NOT done (`a-backgrounded-sleep-does-not-gate-your-turn`).
