# Work order P4.D231 — v4 `08c49319d` ("Scenario Builder on the scenario shelves"): the build request's `groupIds` (Zod twin + the route's dedup-then-exists filter + `groupCount` / `namedGroupCount` on three log lines), named groups joining the group tier FIRST in the mount pool, the NEW `resolve_mount_point_ids_for_group` helper with v4's fail-soft granularity, and the SPA's shelf mode (the Host button on the General page / the project card / a NEW group Scenarios card, the builder's shelf footer + always-sent `groupIds`, the save dialog's `project:<id>` keys + three optgroups + name-sorted groups + everywhere-mode targets) — server + SPA, from `main`

**Status:** OPEN — ordered 2026-09-25.

## Preamble

- **v4 oracle baseline: `b0b6656b5`** (drift-ledger §1). This lane absorbs
  the ledger's `08c49319d` row ("Scenario Builder on the scenario shelves",
  2026-09-25, `4.10.0-dev.92`, 27 files, +617/−71; `ORDERED(P4.D231; help
  P4.D228)`) EXCEPT its four `help/` pages, which P4.D228 re-vendors with
  the whole tree (your four pages are byte-copies — measured md5-identical
  to v4's parent commit today — so nothing of yours is lost by that
  ordering). Spec reads: `git -C ~/source/quilltap-server show 08c49319d:
  <path>` for the post-commit files and `show 08c49319d -- <path>` for the
  hunks — never the commit message (§R.4: it is silent on THREE behaviour
  changes, §Survey D.5).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** This lane pins the round TARGET
  **`/tmp/qt-v4-pin-p4d231-08c49319d`** for the five families it moves
  (`scenario_build_request_schema_equivalence`, `scenario_builder_mount_
  pool_equivalence`, `scenario_builder_tier3_equivalence`,
  `scenario_builder_routes_equivalence`, and `tiered_mount_pool_
  equivalence` as a NEUTRALITY leg) and the BASELINE **`/tmp/qt-v4-pin-
  p4d231-b0b6656b5`** for the pre-port red counts. Pin marker: the
  target-pinned `scenario-build-request-schema` NDJSON carries
  `"groupIds":[]` on every accepting row; the baseline-pinned one carries
  none (`grep -c` both). Families 2, 4 and 6 in §Survey C are MINTED-
  fixture recipes (rebuild → regen → cargo, against the SAME build).
- **Sibling lanes:** P4.D225–P4.D228 (the stacked Concierge chain — you
  share NO file with it; the `dispatch_wrong_type_census` literal that
  the ledger predicted you would collide on does NOT move for you, §Survey
  D.1), P4.D229/P4.D230 (the Concierge SPA lanes — `core/core-contract.ts`
  is shared by NAMED HUNKS: yours is ONE line, §R.10(i)), P4.D232 (no
  contact).
- **Worktree:** `git worktree add <path> -b claude/p4-d231-sb-shelves-
  <hash> main`; `CARGO_INCREMENTAL=0`; `npm ci` in `apps/web`
  (`spa-worktree-node-modules` — never `cp`).
- **Read before the work:** the ledger's row whole; v4 at `08c49319d`:
  `lib/scenario-builder/request-schema.ts` whole (the schema :12-32),
  `app/api/v1/scenario-builder/route.ts:35-201` (`handleBuild`; the group
  block :88-100; the accepted DEBUG :115-123; the pass-through :150-167),
  `lib/services/scenario-builder/scenario-builder.service.ts:52-66,
  132-155`, `lib/scenario-builder/mount-pool.ts:39-126` whole, `lib/
  mount-index/tiered-mount-pool.ts:183-232`, `lib/query/keys.ts:91`,
  `components/scenarios/ScenariosManager.tsx` whole, `components/
  scenario-builder/ScenarioBuilderDialog.tsx:48-65,114-117,161-172,
  222-254`, `components/scenario-builder/SaveScenarioDialog.tsx` whole
  (the target half is a rewrite), `app/aurora/groups/components/
  GroupScenariosCard.tsx` whole (60 lines), `app/prospero/[id]/
  components/ScenariosCard.tsx`, `GroupDetailView.tsx:273-277`,
  `ProjectDetailView.tsx:186`, `app/scenarios/ScenariosView.tsx:53`; the
  four test files for CASE NAMES (§Survey A.11); v5 `crates/quilltap-core/
  src/services/scenario_builder/request_schema.rs` whole (the twin;
  `ScenarioBuildRequest` :92-103, the `characterIds` parse block
  :304-332, `to_value` :129-162, the header table :5-18), `api/scenario_
  builder.rs:292-434` (`scenario_builder_prepare`; the cast scoping
  :377-402 and its "WARN unreachable" comment :388-395; the accepted
  DEBUG :411-420), `services/scenario_builder/mod.rs:123-135,276-311`,
  `services/scenario_builder/mount_pool.rs` whole (210 lines; the tier
  order :101-172; the pool DEBUG :200-208; the repository-ERROR precedent
  :111-129; `push_unique` :65-69), `db/tiered_mount_pool.rs:207-250`,
  `db/groups.rs:165-208`, `crates/quilltap-web/src/scenario_builder_
  routes.rs:133,165`, `crates/quilltap-host/src/spine.rs:2539-2590`;
  `apps/web/src/app/scenario-builder/{scenario-builder-dialog,save-
  scenario-dialog,scenario-builder-run.state,scenario-builder.api}.ts`
  whole, `screens/scenarios/shared/scenarios-manager.ts` whole, `screens/
  scenarios/scenarios-page.ts:42-47`, `screens/scenarios/scenarios.api.
  ts:54,256-330`, `screens/prospero/cards/project-scenarios-card.ts` whole
  + `project-detail.ts:141`, `screens/groups/group-editor.ts:194-208,337`
  + `groups.api.ts:21-28`, `screens/prospero/projects.api.ts:20-21,45`,
  `screens/characters/characters.api.ts:44-46,113`, `scenario/scenario.
  api.ts:34-43`, `chat/sidebar/chat-scenario-control.ts:111-135`,
  `screens/new-chat/new-chat-form.ts:326-336`, `chat/hidden-image/
  images-hidden.ts:28-33`, `core/core-contract.ts:1674-1692,8158-8184`;
  the P4.D217 / P4.D218 orders' §Survey B (verify, do not trust); memory
  notes `a-new-verb-moves-the-dispatch-wrong-type-census` (it does NOT
  fire here — read it to know why), `a-v4-mock-is-not-v4-in-a-route-
  family`, `capture-layer-target-assert-is-a-prefix-match`,
  `a-sorted-field-capture-cannot-see-v4-field-order`,
  `spa-oracle-recorders-live-outside-src`, `a-rollback-is-invisible-once-
  the-refetch-lands`, `e2e-assertion-can-read-the-pre-click-state`.

## Mandate

Make a v5 Scenario Builder run launched from a shelf name its group the
way v4 does, and make every shelf offer the Host. Server: the Zod twin
gains `groupIds` (an array of uuids, max 32, default `[]`, BETWEEN
`characterIds` and `chatId` in both the parsed output and the issue
order; a non-array is a non-continuable `invalid_type` that skips the
refine); `scenario_builder_prepare` gains the group block AFTER the cast
scoping and BEFORE the chat check — dedup (`Set`, insertion order) THEN
existence, an absent id dropped SILENTLY (no "dropped" DEBUG — unlike the
cast), `groupCount` added to the accepted DEBUG between `castCount` and
`hasProject`, `group_ids` threaded to `ScenarioBuilderInput` after
`character_ids`; the run-starting DEBUG gains `namedGroupCount`
(`input.group_ids.len()` — NOT re-deduped; it trusts the route) after
`castCount`; the mount pool gains `group_ids`, computes `namedGroupIds`
(dedup + non-empty filter) BEFORE the cast ids, pushes the named groups'
stores FIRST in the group tier then the cast union (plain array, first-
occurrence dedup keeps a named group's ids ahead of the cast's), and its
pool DEBUG gains `namedGroupCount` (post-dedup) after `liveCastCount`; a
NEW `pub fn resolve_mount_point_ids_for_group(main, mount, group_id) ->
Vec<String>` (official store then linked stores, a LOCAL list, `[]` on
EITHER read's `Err` — v4's NEW granularity, where a failed links read
drops the official store too), called per membership by
`resolve_group_mount_point_ids_for_character` (whose false "is logged"
comment is corrected). SPA: `ScenariosManager` gains a `shelf` input
(`{kind:'general'} | {kind:'project', projectId, projectName?} |
{kind:'group', groupId}`), `shelfSaveTarget`, and the Host button (v4's
`qt-button qt-button-secondary qt-button-sm inline-flex items-center
gap-1.5` with the Host avatar unless images are hidden, text **`Ask the
Host to set the scene`**) before `+ New scenario` inside a `flex
items-center gap-2 flex-wrap` wrapper, mounting the builder with
`cast=[]`, the shelf's project/group, `saveTargets="everywhere"`,
`defaultSaveTarget=shelfSaveTarget(shelf)`, `onSaved → mutator.refresh({
silent: true })`; the General page passes `{kind:'general'}`, the project
card gains `projectName` (from `project-detail.ts:141`) and passes
`{kind:'project', …}`, a NEW group Scenarios card (`Scenarios (N)` /
`Reusable starting scenes offered whenever a member takes a seat` /
scope `group` / the empty message verbatim) after `qt-group-stores-card`,
collapsed by default, over a NEW `groupScenarioMutator` (the three TS
group twins widened with `includeArchived?` the Rust already accepts);
the builder dialog gains `groupIds`, `saveTargets`, `defaultSaveTarget`,
ALWAYS sends `groupIds` (`[]` on New Chat and in-chat — the body bytes
change on every surface), seeds in-world whenever `cast.length > 0 ||
!!projectId || groupIds.length > 0` (New Chat with a project and NO cast
now opens in-world — §R.4), and in shelf mode renders the review footer
as `<span />` / **`Close`** / primary **`Save as scenario…`** with the
builder STAYING OPEN after a save; the save dialog's target key becomes
`project:<id>` in EVERY mode (cast mode included), the `<select>` gains
the three `<optgroup>`s (`Projects` / `Groups` / `Characters`, each only
when non-empty, ALSO in cast mode), groups name-sorted in BOTH modes,
cast-mode characters UNSORTED, everywhere mode listing every project /
every group / every non-archived character through the EXISTING v5
fetchers that already own those query keys (never an envelope under a
shared key), and the preselection flipping from General to the shelf's
home once the list that offers it arrives unless the user already chose.
Must NOT touch: `help/**` (P4.D228); `api/types.rs` (nothing to add —
the body is ONE raw `Value`, §Survey B.1); any Concierge file; `core/
core-contract.ts` beyond the ONE `groupIds?: string[]` line (§R.10(i));
`e2e/scenario-builder-flow.spec.ts`'s existing four beats (grow a NEW
file); the drift ledger.

## Survey-verified starting points (fresh survey 2026-09-25 at `08c49319d` and on `main` `2aed9a552` — do not re-derive)

> **The full survey is committed** at `docs/developer/porting/work-orders/surveys/2026-09-25-08c49319d/` — `survey-sb-shelves-p4d231.md`. Every `§Survey …` reference below points into it; the order carries the load-bearing facts and the survey carries the rest. The survey is planning evidence, dated 2026-09-25 — re-verify a line number before relying on it.

### A. v4 server (post-commit line numbers at `08c49319d`)

- **`lib/scenario-builder/request-schema.ts:12-32`** — `groupIds:
  z.array(UUIDSchema).max(32).default([])` at :23, between
  `characterIds` and `chatId`, doc "Groups named outright — the builder
  launched from a group's Scenarios card." Semantics identical to
  `characterIds`: elements checked first, then `too_big` at 33+, a
  non-array `invalid_type` (non-continuable → the refine is skipped).
- **`app/api/v1/scenario-builder/route.ts` `handleBuild` (:35-201)** —
  refusal order UNCHANGED (JSON :40, Zod :44-48, profile :52-56, tools off
  :57-61, key :63-66, cast scoping :68-86); the group block :88-100:
  `for (const id of [...new Set(body.groupIds)]) { try { const group =
  await repos.groups.findByIdRaw(id); if (group) groupIds.push(id) } catch
  (error) { logger.warn('Scenario Builder dropped an unreadable group id',
  { groupId: id, error }) } }` — NO `.filter(Boolean)`, NO "dropped" DEBUG.
  `repos` is the GLOBAL container (`context.ts:61,116`); groups carry no
  `userId`. `findByIdRaw` → `_findById` (`base.repository.ts:247-258`) is a
  **fallback-mode `safeQuery(…, 'Error finding entity by ID', { id },
  null)`** that ALSO runs `this.validate(result)` — it never throws, so
  **the route's WARN is UNREACHABLE through v4's real code** (the cast
  WARN's class). The accepted DEBUG (:115-123): `'Scenario Builder request
  accepted' { mode, castCount, groupCount, hasProject, inChat, revising,
  profileId }`. The pass-through (:150-167) puts `groupIds` between
  `characterIds` and `chat`.
- **`scenario-builder.service.ts`** — `ScenarioBuilderInput` (:52-66)
  gains `groupIds?: string[]` after `characterIds`; the pool call (:132-137)
  passes `groupIds: input.groupIds ?? []`; the run-starting DEBUG
  (:139-155) `'Scenario Builder run starting' { mode, castCount,
  namedGroupCount: input.groupIds?.length ?? 0, inChat, revising,
  profileId, provider, model, webAvailable, pool: {…} }`.
- **`lib/scenario-builder/mount-pool.ts:39-126`** — `groupIds?` (:43-44);
  `:47 const namedGroupIds = [...new Set((opts.groupIds ?? []).filter(
  Boolean))]` BEFORE `castIds`; the group tier (:79-87): named groups
  first (`resolveMountPointIdsForGroup(groupId)` per named id), then the
  cast union (`resolveGroupMountPointIdsForCharacter` per live cast id),
  into ONE plain array; `dedupeTierTriple` (:102-107, unchanged, first
  occurrence wins); participants excluded against the deduped tiers
  (:108-113); the pool DEBUG (:116-124) `'Resolved Scenario Builder mount
  pool' { castCount, liveCastCount, namedGroupCount, participants, groups,
  projects, hasGlobal }`; the header prose (:7-9) adds the "named outright"
  sentence.
- **`lib/mount-index/tiered-mount-pool.ts:183-206`** — NEW
  `resolveMountPointIdsForGroup(groupId)`: `[]` when falsy; `ids = new
  Set`; `findByIdRaw` → `officialMountPointId`; `groupDocMountLinks.
  findByGroupId` → each `mountPointId`; `[...ids]`; catch → `logger.warn(
  'Group store lookup failed', { groupId, error })` → `[]`. `resolveGroup
  MountPointIdsForCharacter` (:215-232) now calls it per membership; outer
  catch unchanged (`'Group mount lookup failed' { characterId, error }`).
  **WARN wording change**: BEFORE `'Group store lookup failed for
  membership' { groupId, characterId, error }` → AFTER `'Group store lookup
  failed' { groupId, error }`. **Hidden granularity change**: BEFORE the
  official id went into the OUTER set before the links read (a links
  throw KEPT the official store); AFTER both reads sit inside the helper's
  `try` (a links throw drops the official store too). **Neither WARN arm is
  reachable through v4's real code** — `findByIdRaw` and `findByGroupId`
  (`group-doc-mount-links.repository.ts:64-76`) are both fallback-mode
  `safeQuery` (their failure lands in the repository's own ERROR line,
  `Error finding entity by ID` / `Error finding links by group ID`). On
  success the id ORDER is identical before and after.
- **`lib/query/keys.ts:91`** — `groups.list: () => ['groups', 'list']`
  (only consumer: `SaveScenarioDialog`).

### B. v4 client

- **`ScenariosManager.tsx`** — `export type ScenarioShelf = | { kind:
  'general' } | { kind: 'project'; projectId: string; projectName?: string
  | null } | { kind: 'group'; groupId: string }` (:43-47); `shelfSaveTarget`
  (:49-58): general → `'general'`, project → `` `project:${projectId}` ``,
  group → `` `group:${groupId}` ``; prop `shelf?: ScenarioShelf` ("The
  shelf being managed; offers the Host's Scenario Builder when set");
  `refresh` destructured from the mutator; `useImagesHidden()` (defaults
  FALSE outside the Salon's provider — `images-hidden-context.tsx:18`;
  the only provider is `SalonView.tsx:1513` — so the avatar is effectively
  always shown on shelves); the toolbar (:201-221) wraps `+ New scenario`
  in `<div className="flex items-center gap-2 flex-wrap">` preceded, only
  when `shelf` is set, by the Host button (`STAFF_AVATARS.host ??
  '/images/avatars/host-avatar.webp'`, `h-4 w-4 rounded-full`, text `Ask
  the Host to set the scene`); the mount (:253-266): `cast={[]}`,
  `projectId`/`projectName` for a project shelf, `groupIds={[groupId]}`
  for a group shelf, `saveTargets="everywhere"`, `defaultSaveTarget=
  {shelfSaveTarget(shelf)}`, `onSaved={() => void refresh({ silent: true
  })}`, **no `onUse`**. Used by `app/scenarios/ScenariosView.tsx:53`
  (`{kind:'general'}`), `app/prospero/[id]/components/ScenariosCard.tsx`
  (new prop `projectName?: string | null`, doc `Offered as the Host's
  default save home ("Project: <name>")`; `ProjectDetailView.tsx:186`
  passes `projectName={project.name}`), and NEW `app/aurora/groups/
  components/GroupScenariosCard.tsx` (60 lines: `useScenarioMutator(\`/api/
  v1/groups/${groupId}/scenarios\`)`, a collapsible `qt-card` like the
  Members/Linked Stores cards, `ScenariosIcon`; heading `Scenarios
  ({mutator.scenarios.length})`, subtitle `Reusable starting scenes offered
  whenever a member takes a seat`, `scopeLabel="group"`, `shelf={{ kind:
  'group', groupId }}`, `emptyMessage="No scenarios yet. Create one and
  it'll be offered whenever a member of this group joins a new chat."`);
  `GroupDetailView.tsx:273-277` places it AFTER `GroupLinkedStoresCard`,
  last in the cards column, `useState(false)` (collapsed).
- **`ScenarioBuilderDialog.tsx`** — props (:48-65) gain `groupIds?:
  string[]`, `saveTargets?: 'cast' | 'everywhere'`, `defaultSaveTarget?:
  SaveScenarioTargetKey`; `onUse` becomes OPTIONAL ("Omit where there is no
  box"); the mode default (:114-117) `cast.length > 0 || !!projectId ||
  (groupIds?.length ?? 0) > 0 ? 'in-world' : 'real'`; the build body
  (:161-172) adds `groupIds: groupIds ?? []` between `characterIds` and
  `chatId` (ALWAYS sent); `handleUse` early-returns `if (draft === null ||
  !onUse)`; the review footer (:222-254) in shelf mode (`!onUse`): left
  `<span />`, first right button `Close` (`{onUse ? 'Cancel' : 'Close'}`),
  primary `Save as scenario…` (`qt-button-primary`, disabled `!draft?.
  trim()`); with `onUse` the footer is unchanged (left `Save as scenario…`
  secondary, `Cancel`, primary `Use this scene`); the inputs-pane footer
  (`Cancel` / `Set the scene`) untouched; the save mount passes
  `targets`/`defaultTarget`; the builder STAYS OPEN after a save.
- **`SaveScenarioDialog.tsx`** — `SaveScenarioTargetKey = 'general' |
  \`project:${string}\` | \`group:${string}\` | \`character:${string}\``
  (:36-40); props `targets?: 'cast' | 'everywhere'` (default `'cast'`),
  `defaultTarget?` ("Preselected home; falls back to General when it is
  not on offer"); `NamedRow`; `byName = (a, b) => a.name.localeCompare(
  b.name)`; `const [chosenTarget, setTarget] = useState<string | null>(
  null)`. Queries, all gated on `isOpen`: `castGroupData` (`queryKeys.
  groups.byCharacters(castKey)`, enabled `!everywhere && castKey.length >
  0`), `allGroupData` (`groups.list()`, `/api/v1/groups`, `everywhere`),
  `projectData` (`projects.list()`), `characterData` (`characters.list()`,
  `/api/v1/characters` — the list endpoint already excludes archived,
  verified at `characters/handlers/get.ts:24-33`). Lists: `projects` =
  everywhere ? sorted by name : `projectId ? [{ id: projectId, name:
  projectName || 'this project' }] : []`; `groups` = `[...((everywhere ?
  all : cast) ?? [])].sort(byName)` (**cast-mode groups now SORTED**);
  `characters` = everywhere ? sorted : `cast` UNSORTED. `offered` = a Set of
  `general`, `project:<id>`…, `group:<id>`…, `character:<id>`…; `target =
  chosenTarget ?? (defaultTarget && offered.has(defaultTarget) ?
  defaultTarget : 'general')`. Key construction BEFORE: `target ===
  'project' && projectId` → `/api/v1/projects/${projectId}/scenarios`,
  option `value="project"`; AFTER (:164-166, :196-197): `target.startsWith(
  'project:')` → `` `/api/v1/projects/${target.slice('project:'.length)}/
  scenarios` ``, `saved = { kind: 'project', projectId: <slice>, path }`,
  option `` value={`project:${p.id}`} `` — **in EVERY mode**. The `<select>`
  (:277-312): `Quilltap General` bare at the top, then three `<optgroup>`s
  present in cast mode too, each only when non-empty: `label="Projects"`
  (`Project: {p.name}`), `label="Groups"` (`Group: {g.name}`), `label=
  "Characters"` (`{c.name}&rsquo;s scenarios`). Unchanged strings: `File
  this scene as a scenario`, `Name` / `Description (optional)` / `Where it
  lives`, `A character&rsquo;s own scenarios keep a title and a body
  only.`, `A scenario wants a name before it can be filed.`, `Choose where
  the scenario should live.`, `The scenario could not be filed (HTTP
  ${res.status}).`, `The scenario could not be filed.`, the toast
  `“${trimmedName}” has been filed among the scenarios.`, `Cancel`, `Save`
  / `Filing…`. After a save: `invalidateQueries(queryKeys.scenarios.all)`
  → toast → `onSaved(saved)` → `onClose()`.
- **Tests** (case names — v4's are MOCKED; mirror as real-DB/real-route
  arms): route `describe('POST /api/v1/scenario-builder?action=build —
  named groups')`: `passes existing group ids through and drops unknown
  ones`, `defaults to no named groups`; mount-pool: `a group named
  outright joins the group tier — official and linked stores — with no
  cast`, `a named group the cast also belongs to is not counted twice`,
  `an unknown named group contributes nothing and does not throw`; dialog
  `describe('ScenarioBuilderDialog — launched from a scenarios shelf')`:
  `sends the named group ids and defaults to in-world` (body
  `toMatchObject({ mode: 'in-world', characterIds: [], groupIds: ['grp-1']
  })`), `offers no "Use this scene"; Save is the primary action` (exactly
  ONE `Save as scenario…`, plus `Close`), `Save lists every home,
  preselects the shelf, and files it there` (options `Group: Aeronauts
  Club`, `Project: The Estate`, `/Riya.s scenarios/`; select value
  `group:grp-1`; `onSaved({kind:'group', groupId:'grp-1', path:
  'Scenarios/rain.md'})`), `saving to another project posts to that
  project` (default `general`; change to `project:proj-1`). No test for
  `resolveMountPointIdsForGroup`, the WARN rename, `ScenariosManager` or
  `GroupScenariosCard`.

### C. v5 on `main` `2aed9a552`

- **`api/types.rs:3986-4018`** `ScenarioBuilderBuild { run_id, #[serde(
  default = "empty_json_object")] body: serde_json::Value }` — the body is
  ONE raw `Value`; **NOTHING to add.** `crates/quilltap-web/tests/dispatch_
  wrong_type_census.rs:2580` `EXCLUDED_BY_THE_ROUTE_IDENTIFIER_RULE = 449`
  — its P4.D217 entry (:2570-2579) already records the raw body; **449
  stays 449 for this lane** (re-run as a neutrality check).
- **`services/scenario_builder/request_schema.rs`** (415 lines) — needs
  `group_ids: Vec<String>` on `ScenarioBuildRequest` (:92-103), a parse
  block cloned from `characterIds` (:304-332: elements first, then
  `too_big_array(32)`) placed between `characterIds` and `chatId`,
  `to_value` (:129-162) emitting `"groupIds": [...]` right after
  `characterIds`, and the header table (:5-18) grown.
- **`api/scenario_builder.rs:292-434`** `scenario_builder_prepare` — cast
  scoping :377-402 (a failed overlay read is a SILENT miss, with a comment
  saying the WARN is unreachable — the precedent for the group block); the
  chat check :405-424; the accepted DEBUG :411-420 (`mode, castCount,
  hasProject, inChat, revising, profileId`). The group block goes between
  :403 and :405. Existence read candidates: `db::groups::find_official_
  mount_point_id_raw` (`groups.rs:188-208`, ONE column, NO validate) or
  `find_name_and_official_mount_point_id_raw` (:165-185) — **see Tier 1
  item 2 for the ruling on which** (v4's `findByIdRaw` VALIDATES the row).
- **`services/scenario_builder/mod.rs`** — `ScenarioBuilderInput`
  (:123-135) lacks `group_ids`; the run (:276-310) calls `mount_pool::
  resolve_scenario_builder_mount_pool(main, mount, &pool_user, pool_
  project.as_deref(), &pool_cast)`; the run-starting DEBUG (:300-311) is
  `mode, castCount, inChat, revising, profileId, provider, model,
  webAvailable, pool`; in-module fixtures (e.g. :672) need the field.
- **`services/scenario_builder/mount_pool.rs`** (210 lines) — tiers in
  order: cast vaults :101-150, group (cast union only) :152-160, project
  :163, General :166-172, dedup + participant exclusion :176-191; the pool
  DEBUG :200-208; `push_unique` :65-69; **`:111-129` reproduces the
  REPOSITORY fallback ERROR instead of a dead WARN** — the precedent; the
  module header (:38-51, "four log lines") gains v4's sentence.
- **`db/tiered_mount_pool.rs:207-250`** `resolve_group_mount_point_ids_
  for_character` — the per-membership closure `let _ = per_membership();`
  is SILENT; its comment "is logged and skipped" is FALSE; it pushes the
  official id into the OUTER list before the links read (v4's OLD
  granularity); the memberships read's `Err(_) => return Vec::new()` (:227)
  lacks v4's `Group mount lookup failed` (also unreachable in v4 —
  `findByCharacterId` is `safeQuery` too). No v5 grep hit for either WARN
  string.
- **Route edge + host**: `scenario_builder_routes.rs:133` reads raw bytes,
  `:165` forwards `body: raw` — no change; `web_edge_body_parse_guard` has
  NO scenario-builder row — unmoved; `spine.rs:2539-2590` forwards the
  prepared input — no change beyond the struct field.
- **SPA**: `core-contract.ts:8158-8169` `ScenarioBuildRequestInput` (add
  `groupIds?: string[]` after `characterIds` — the ONE contract line);
  `scenario-builder-run.state.ts:114,138` passes `input` through as `body`
  (no change); `scenario-builder-dialog.ts` (548 lines: inputs :387-398
  `cast`/`projectId`/`projectName`/`chatId`, outputs `closed`/`use`/
  `saved`; the mode seed :481-484; the build body :505-517; the review
  footer :318-341; the save mount :347-356; `handleUse` :541-546) — **`use`
  is an Angular `output()` that ALWAYS exists**, so v4's `!onUse` needs an
  explicit input: drive the shelf footer from `saveTargets ===
  'everywhere'` (set exactly when v4 omits `onUse`) — a recorded design
  call; `save-scenario-dialog.ts` (299 lines: target signal :186-187 still
  `'project'`; the `<select>` :104-126 no optgroups; `save()` :220-298 with
  `target === 'project' && projectId` at :243/:273; groups query :199-203);
  `scenario-builder.api.ts` (`groupsByCharactersKey`, `fetchGroupsBy
  Characters`; NO all-groups/projects/characters fetchers — **those exist
  elsewhere and OWN the same keys**: `screens/groups/groups.api.ts:21-28`
  `groupKeys.list()` = `['groups','list']` → `fetchGroups` → `GroupCard
  Model[]`; `screens/prospero/projects.api.ts:20-21,45` `projectKeys.list()`
  → `ProjectCardModel[]`; `screens/characters/characters.api.ts:44-46,113`
  `characterKeys.list()` → `fetchCharacterList` → `CharacterListItem[]`
  (excludes archived by default, P4.D64) — REUSE them; storing v4's
  envelope under those keys corrupts the shared cache, the P4.116 class);
  `scenario/scenario.api.ts:34-43` `scenarioKeys` (the save dialog already
  invalidates `scenarioKeys.all`; the shelf managers are NOT TanStack-
  cached — `makeScenarioMutator` owns signals — so the refresh is the
  explicit `mutator.refresh({ silent: true })` on `ScenarioMutator`,
  `scenarios.api.ts:54`); `scenarios-manager.ts` (190 lines; toolbar
  :47-60; no `shelf`, no builder — the Host button follows `chat-scenario-
  control.ts:111-123` (`HOST_AVATAR` from `scenario-builder/host-avatar`,
  `injectImagesHidden()` whose root default is `false`, `images-hidden.ts:
  28-33`), the `@defer`'d dialog follows `new-chat-form.ts:326-336`);
  `scenarios-page.ts:42-47` (add `[shelf]`); `project-scenarios-card.ts`
  (47 lines; inputs `projectId`, `defaultOpen`; no `projectName`; host
  `project-detail.ts:141` has `project()`); `scenarios.api.ts:256-330` has
  `projectScenarioMutator` + `generalScenarioMutator` — **NO
  `groupScenarioMutator`**; **`screens/groups/` has NO Scenarios card** —
  `group-editor.ts:194-208` cards column (`qt-group-members-card`, then
  `qt-group-stores-card`); the new card goes after `qt-group-stores-card`
  in `project-scenarios-card.ts`'s `qt-collapsible-card` shape with
  `icon="scenarios"`; the server verbs EXIST (`Request::GroupScenario{List,
  Create,Get,Update,Rename,Delete}`, `types.rs:876-920`, all with
  `include_archived` on List/Update/Rename/Delete) **but the TS twins
  `GroupScenarioUpdateRequest` / `GroupScenarioRenameRequest` /
  `GroupScenarioDeleteRequest` (`core-contract.ts:1674-1692`) LACK
  `includeArchived?`** (List has it; the project twins have all four) —
  widen them (three more contract lines, part of the ONE pre-declared
  contract hunk); the SPA oracle recorder `apps/web/oracle/scenario-
  builder.recorder.ts` → `scenario-builder-oracle.json` records ONLY
  `applyAgentStreamEvent` + `describeHostActivity` — **it does NOT show the
  target-key change** (the ledger row's claim is FALSE); the proof is the
  vitest specs. Other builder hosts the mode-default change reaches:
  `new-chat-form.ts:326-336` (`builderProjectId()` with a possibly empty
  cast) and `chat-scenario-control.ts:126-135`.

### D. Families (test → oracle → fixture → what moves)

1. `scenario_build_request_schema_equivalence` — tsx `harness/oracle/
   cases/scenario-build-request-schema.ts`, no fixture, env `QT_ORACLE_
   SCENARIO_BUILD_REQUEST_SCHEMA`; header recipe :20-28. **MOVES:** every
   accepting row gains `"groupIds":[]` — red-first on every success row.
   Grow the corpus with `groupIds` rows mirroring `characterIds`: absent,
   `[]`, valid, 33 entries, a bad element, a 33rd bad element (element
   before size), a non-array (aborts the refine), `null`.
2. `scenario_builder_mount_pool_equivalence` — jest `scenario-builder-
   mount-pool.test.ts` (:185-189 the call), spec `scenario-builder-mount-
   pool.json` (1 plant + 12 arms + 2 plants + 2 arms), the UNCHANGED
   `build-doc-opacity-fixture.ts` fixture (ONE group, Severed `d2000000-
   0000-4000-8000-000000000001`, official `e2000000-…-0000000000f1`, linked
   `…f4`, members Leilani + Abigail); env `QT_ORACLE_SBPOOL`, `QT_FIXTURE_
   SBPOOL_MAIN/_MOUNT`; recipe :39-57 (PIN REQUIRED, MINTED, staged
   `/tmp/qt-oracle-stage-sb-pool`); comparand = the five pool fields + the
   `ScenarioBuilderMountPool` logger's lines as a field SET. **MOVES:** every
   arm's pool line gains `namedGroupCount: 0` (red-first). New arms need
   `groupIds` in the step shape + the call: named with no cast (Severed →
   `[f1, f4]`); named + a member cast (dedup, named first); an unknown id
   (`[]`); dup/empty named ids (`namedGroupCount` post-dedup). **A
   named-group-NOT-in-the-cast arm needs a SECOND planted group** (plant it
   in the spec's plants). The TieredMountPool logger is NOT recorded here.
3. `tiered_mount_pool_equivalence` — tsx `tiered-mount-pool.ts`, fixture
   `build-tiered-mount-pool-fixture.ts` + `tiered-mount-pool.json`, env
   `QT_ORACLE_TMP`, `QT_FIXTURE_TMP_MAIN/_MOUNT`, recipe :16-26. **Expected
   NOT to move** (the success-path order is identical) — regen at the pin
   as a NEUTRALITY proof; the home for a direct `resolve_mount_point_ids_
   for_group` tier-2 arm (no log capture there).
4. `scenario_builder_tier3_equivalence` — jest `scenario-builder-tier3.
   test.ts` (:320-335), spec `scenario-builder-tier3.json` (21 cases incl.
   `no_cast_no_project` = the General-shelf pool), doc-opacity fixture, env
   `QT_ORACLE_SBT3` + `QT_FIXTURE_SBT3_MAIN/_MOUNT`, recipe :42-63 (PIN
   REQUIRED, MINTED); comparand includes the log lines (:638-646). **MOVES:**
   `Scenario Builder run starting` gains `namedGroupCount: 0` (+ the pool
   line) — every case red-first. Add a group-shelf case (`groupIds:
   [Severed]`, empty cast, in-world) and a project-shelf case.
5. `scenario_builder_capture` (`crates/quilltap-harness/tests/scenario_
   builder_capture/mod.rs`) — the shared STRUCTURAL capture (field ORDER
   not compared, :7) — not a family; it is why 2 and 4 cannot see
   `namedGroupCount`'s POSITION (`a-sorted-field-capture-cannot-see-v4-
   field-order`).
6. `scenario_builder_routes_equivalence` (`crates/quilltap-web/tests/`) —
   jest `scenario-builder-routes.test.ts`, spec `scenario-builder-routes.
   json` over the committed `chat-send-{main,mount}.db`, env `QT_ORACLE_
   SB_ROUTES`, recipe :60-72; **log lines compared with context keys IN
   ORDER** (:40-47) — every accepted case red-first on `groupCount` between
   `castCount` and `hasProject`; the recorded run projection (oracle
   :143-153: `mode, characterIds, chat, priorDraft, revision, aborted`;
   Rust :250) lacks `groupIds` — extend BOTH sides. New cases: existing +
   unknown ids (v4's case); duplicated ids (dedup before the check); the
   default `[]`; an UNREADABLE (BLOB-named) group row — the WARN is
   unreachable, so pin its ABSENCE and v4's repository ERROR, the cast
   precedent. **Check the chat-send fixture for a `groups` row first** —
   a plant may be needed.
7. `scenario_builder_dispatch_wire`, `scenario_builder_disconnect`,
   `scenario_builder_midstream_failure` — no movement (the body is raw);
   re-run.
8. `dispatch_wrong_type_census` — stays 449; re-run as neutrality.
9. SPA: `scenario-builder-dialog.spec.ts` (1076 lines) — the `toEqual`
   exact-body pin at :487-497 (and the bare `toMatchObject` :502) WILL
   redden once `groupIds: []` is always sent; the `opens in real mode when
   the cast is empty` row (:380) still holds (no project);
   `SaveScenarioDialog — within the builder` (:747) moves on `project` →
   `project:<id>`; `scenarios-manager.spec.ts` (`describe('ScenariosManager')`
   :163); e2e `scenario-builder-flow.spec.ts` (four beats on New Chat /
   in-chat) and `scenarios-flow.spec.ts` (shelves) are the homes for live
   shelf beats — grow a NEW file. The SPA oracle fixture does NOT move
   (a recorder re-run at the pin is byte-identical except the `source`
   string — a neutrality proof, not a red).

### E. Traps (measured — §R.4)

(a) the commit message never mentions the mode-default change (New Chat
with a project + empty cast now opens in-world); (b) nor that optgroups +
name-sorted groups now appear in cast mode too (cast characters stay
unsorted); (c) "unknown group ids are dropped" hides that dedup precedes
the check, that there is NO dropped-count DEBUG, and that the WARN is
dead; (d) "the builder stays open after a save" is in the spec prose,
not the message; (e) the dialog now sends `groupIds: []` on EVERY surface;
(f) `Cancel` → `Close` only in the review footer, only on a shelf; (g) the
dedup precedes the existence check in both the route and the pool;
`groupCount` counts post-existence, the pool's `namedGroupCount`
post-dedup, the service's `namedGroupCount` trusts the route; (h) v4's
`findByIdRaw` VALIDATES, so a schema-invalid group row answers `null`
(dropped); v5's one-column raw read does NOT — existence semantics differ
on a malformed row (Tier 1 item 2); (i) the preselection timing is v4's:
General until the offering list arrives, then the flip unless the user
chose — `[selected]`-per-option must re-evaluate against the computed
target, and an e2e beat must wait for the option to EXIST before
asserting the select's value (v4's test `waitFor`s).

## Tiered deliverables

### Tier 1 — must land

1. **The Zod twin** — `group_ids` on `ScenarioBuildRequest` between
   `character_ids` and `chat_id`; the parse block cloned from
   `characterIds` (elements first, then `too_big_array(32)`; a non-array
   `invalid_type` non-continuable → the refine skipped); `to_value` emits
   `"groupIds"` right after `characterIds`; the header table row. RED-FIRST:
   `scenario_build_request_schema_equivalence` at the target on every
   accepting row (record the count) with the corpus grown by the eight
   `groupIds` rows in §D.1; GREEN after.
2. **The route's group block** in `scenario_builder_prepare` between the
   cast scoping and the chat check: dedup (insertion order) THEN existence;
   **"exists" = v4's `findByIdRaw` semantics — the row is present AND
   decodes** (plant a BLOB-named group row on BOTH sides and MEASURE: v4
   drops it with its repository ERROR `Error finding entity by ID {…}` and
   no route line; v5 must drop it too — use a row-decoding read, not the
   one-column raw read, or wrap the raw read with the row validation the
   `groups` decoder applies; record which and why); an absent id dropped
   SILENTLY (no DEBUG, no WARN — pin the ABSENCE of `Scenario Builder
   dropped an unreadable group id` with a capture rig, the cast-WARN
   precedent at `:388-395`, and carry the "unreachable in v4" comment
   naming `08c49319d`); `groupCount` between `castCount` and `hasProject`
   on the accepted DEBUG (the routes family compares key ORDER — red-first
   on every accepted case); `group_ids` on `ScenarioBuilderInput`;
   `scenario_builder_routes_equivalence` re-recorded at the target with
   §D.6's four new cases (the BLOB row's failure line on v5's `Err` path
   = the REPOSITORY fallback ERROR per the `mount_pool.rs:111-129`
   precedent, pinned with the capture rig; a plant in the chat-send spec
   if the fixture has no `groups` row), the run projection extended on
   both sides with `groupIds`.
3. **The service + pool** — `namedGroupCount` after `castCount` on the
   run-starting DEBUG (`input.group_ids.len()`, no re-dedup); the pool
   gains `group_ids`, `named_group_ids` (dedup + non-empty) computed BEFORE
   the cast ids, the named groups' stores pushed FIRST in the group tier,
   the pool DEBUG's `namedGroupCount` (post-dedup) after `liveCastCount`;
   the module header's sentence. RED-FIRST at the target:
   `scenario_builder_mount_pool_equivalence` on every arm's pool line and
   `scenario_builder_tier3_equivalence` on every case; then the new arms
   (§D.2's four + a SECOND planted group for the named-not-in-cast arm;
   §D.4's group-shelf + project-shelf cases). Because the capture is a
   field SET, ALSO pin `namedGroupCount`'s POSITION once with a v5-side
   unit test over the rendered line (the `a-sorted-field-capture-cannot-
   see-v4-field-order` rule).
4. **`resolve_mount_point_ids_for_group`** — a NEW `pub fn` in
   `db/tiered_mount_pool.rs`: `[]` on an empty id; official then linked
   into a LOCAL list; `[]` on EITHER read's `Err` (v4's NEW granularity:
   a failed links read drops the official store too — carry the
   why-comment); the failure line = the repository fallback ERROR
   (`Error finding entity by ID` for the group read; `Error finding links
   by group ID {groupId, error}` for the links), NOT the dead WARN (its
   ABSENCE pinned; the comment names `08c49319d` and why both wordings are
   unreachable in v4); `resolve_group_mount_point_ids_for_character` calls
   it per membership (its false "is logged" comment corrected; the outer
   `Group mount lookup failed` recorded as the same dead-line class, NOT
   emitted). A tier-2 arm for the helper in `tiered_mount_pool_
   equivalence` (an official + two linked; official only; unknown id; a
   links-read failure planted as an unreadable link row on both sides —
   MEASURE that v4 returns `[]` for the WHOLE group). The success-path
   neutrality of `tiered_mount_pool_equivalence` at the target (`cmp`).
5. **The SPA contract line** — `groupIds?: string[]` after `characterIds`
   on `ScenarioBuildRequestInput` (`core-contract.ts:8158-8169`) + the
   three `includeArchived?` lines on `GroupScenario{Update,Rename,Delete}
   Request` (`:1674-1692`) — the ONE pre-declared hunk (§R.10(i)),
   marked.
6. **The builder dialog** — inputs `groupIds` (default `[]`),
   `saveTargets` (`'cast' | 'everywhere'`, default `'cast'`),
   `defaultSaveTarget`; the mode seed `cast.length > 0 || !!projectId ||
   groupIds.length > 0`; `groupIds` ALWAYS in the build body between
   `characterIds` and `chatId`; the shelf footer (`<span />` / `Close` /
   primary `Save as scenario…`) driven by `saveTargets === 'everywhere'`
   (the design call, recorded in the file's doc: Angular's `output()`
   always exists); the builder stays open after a save; the save mount
   passes `targets`/`defaultTarget`. RED-FIRST: the `toEqual` body pin at
   `scenario-builder-dialog.spec.ts:487-497`; then v4's four shelf `it`s
   ported by name over v5's harness (the exact strings in §B).
7. **The save dialog** — `chosenTarget: null` until the user picks;
   `offered` computed; `target = chosen ?? (offered.has(default) ? default
   : 'general')`; the `project:<id>` key in EVERY mode (`:243`/`:273`
   rewritten); the three optgroups (only when non-empty, cast mode too);
   groups name-sorted in both modes (`localeCompare` — the ICU4X en-US
   collation twin the SPA already uses where v4 `localeCompare`s; measure
   which helper the existing lists use and reuse it); cast characters
   unsorted; everywhere mode over the EXISTING `fetchGroups` / `fetchProjects`
   / `fetchCharacterList` (their array shapes carry `id` + `name`; never an
   envelope under `['groups','list']`); the option texts verbatim (`Project:
   `, `Group: `, `<name>’s scenarios`). RED-FIRST: the `SaveScenarioDialog —
   within the builder` spec (:747) on the key change.
8. **`ScenariosManager` shelf mode** — the `ScenarioShelf` type,
   `shelfSaveTarget`, the `shelf` input, the Host button (v4's classes,
   avatar unless images hidden, `Ask the Host to set the scene`) inside the
   `flex items-center gap-2 flex-wrap` wrapper BEFORE `+ New scenario`, the
   `@defer`'d builder mount with `cast=[]`, the shelf's project/group,
   `saveTargets="everywhere"`, `defaultSaveTarget`, `onSaved → mutator.
   refresh({ silent: true })`; `scenarios-page.ts` passes `{kind:'general'}`;
   `project-scenarios-card.ts` gains `projectName` (bound from
   `project-detail.ts:141`'s `project().name`) and passes `{kind:'project',
   projectId, projectName}`.
9. **The group Scenarios card** — NEW `screens/groups/group-scenarios-
   card.ts` after `qt-group-stores-card` in `group-editor.ts:194-208`,
   collapsed by default, `qt-collapsible-card` with `icon="scenarios"`,
   heading `Scenarios (N)`, subtitle and empty message verbatim (§B),
   `scopeLabel="group"`, `shelf={kind:'group', groupId}`, over a NEW
   `groupScenarioMutator` in `scenarios.api.ts` mirroring
   `projectScenarioMutator` over the six `groupScenario*` verbs (with
   `includeArchived` threaded on Update/Rename/Delete like the project
   twin — the widened contract of item 5; measure that "Show archived" +
   a mutate refetches the archived list, the project card's behaviour).
10. **Specs + one e2e beat** — the unit specs above red-first then green;
    a NEW `e2e/scenarios-shelf-builder-flow.spec.ts` with three beats
    (General page → Host button → the dialog shows `Close` + `Save as
    scenario…` and no `Use this scene`; a project card → the save select
    preselects `Project: <name>` once its option exists; the group card →
    a save files to `group:<id>` and the shelf shows the new row after the
    silent refresh) against the mock LLM, gated `P4D231_SERVER_LANDED`
    ONLY if you find the server half cannot run in your own worktree (it
    can — this lane owns both halves; run them LIVE).

### Tier 2 — should land

11. The `dispatch_wrong_type_census` (449), `web_edge_body_parse_guard`,
    `scenario_builder_{dispatch_wire,disconnect,midstream_failure}` re-run
    by name as neutrality; the SPA recorder re-run at the pin (`cmp`
    modulo the `source` string).
12. Mutation proofs: **M1** swap the named-first/cast-union order in the
    pool → the named-first arm reds; **M2** drop the dedup before the
    existence check → the duplicated-ids routes case reds (`groupCount`);
    **M3** emit `groupIds` before `characterIds` in `to_value` → the schema
    family reds; **M4** make the helper keep the official id on a links
    `Err` → the planted-failure tier-2 arm reds; **M5** send `groupIds`
    only when non-empty → the dialog's `toEqual` pin reds; **M6** keep the
    `project` bare key in cast mode → the save spec reds.

### Tier 3 — explicit deferrals (loud, typed, never silent)

13. The four `help/` pages — P4.D228's (the whole-tree re-vendor at
    `08c49319d`); record their md5s at the pin in your lane record so the
    unifier can confirm the copy.
14. A per-group shelf `scenarioKeys` entry (v4's `scenarioKeys.group` is
    the New-Chat UNION key, not a per-group shelf key) — NOT needed: the
    shelf is signal-owned; record.

## The differential requirement, restated

Every server unit lands with its family regenerated from the TARGET pin
through the sweep driver (`recipe_sweep.py --run <family> --v4 "$PIN"
--v5w <ABSOLUTE worktree>`), red-first where §Tier 1 says so, with the
pre-port red counts at the BASELINE pin recorded; the three MINTED
families rebuilt → regenerated → tested against the SAME build. Every
absent-line claim (the dead WARNs) is pinned with a capture rig on the
planted row (`captured_with` / the thread-scoped rig) and its silence leg.
The SPA units are proven by vitest specs transcribing v4's four new `it`s
by name plus the reddened existing pins, and by the three LIVE beats. A
v4 unit test that MOCKS a repository (all four here) is mirrored as a
real-DB / real-route arm with PLANTED rows through the existing oracle
(`a-v4-mock-is-not-v4-in-a-route-family`). Ledger §5.2 applies to every
regen.

## Fixtures

Consumes: the doc-opacity pair (UNCHANGED builder; a SECOND group PLANTED
in `scenario-builder-mount-pool.json`'s plants and, if needed, in
`scenario-builder-tier3.json`'s), the committed `chat-send-{main,mount}.db`
(READ ONLY — a `groups` row planted through the routes spec if absent),
the tiered-mount-pool fixture (its builder grown ONLY if the helper arm
needs a links-failure plant the spec cannot express). Delivers: no
committed pair widened. Dependent oracles: the five families in §D, all
re-run here.

## §R — Round-wide meeting points (binding; byte-identical in every order of this round)

The `08c49319d` Concierge-overhaul drift catch-up round: **P4.D225 →
P4.D226 → P4.D227 → P4.D228 (a STACKED server chain) ∥ P4.D229 ∥ P4.D230 ∥
P4.D231 ∥ P4.D232**, ordered 2026-09-25. Eight lanes, eight worktrees,
eight lane branches. **P4.D225, P4.D229, P4.D230, P4.D231 and P4.D232 cut
from `main` (`2aed9a552` or the commit that lands these orders); P4.D226
cuts from P4.D225's lane branch, P4.D227 from P4.D226's, P4.D228 from
P4.D227's — each once the lane it stacks on has written LANE COMPLETE
into its status header.** Two lanes are SPA-only (P4.D229, P4.D230 — no
cargo); P4.D231 is Rust + SPA; the rest are Rust. The unifier
cherry-picks in the order **P4.D232 → P4.D231 → P4.D225 → P4.D226 →
P4.D227 → P4.D228 → P4.D230 → P4.D229** (the riders first so the guard's
constants and `request-envelopes` are in place before the substrate's
`image-dialects` lands; the shelves next — disjoint; then the chain in
its own order; the Settings SPA before the Salon SPA so the Salon lane's
on-duty reads see the settings shape), gates ONCE from the round-target
pin, flips every `P4D2xx_SERVER_LANDED` gate and runs the live beats, and
`merge --ff-only`s.

- **§R.1 — Baseline and target.** The oracle baseline is **`b0b6656b5`**
  (drift-ledger §1). v4 `main` HEAD at planning is **`08c49319d`**
  ("Scenario Builder on the scenario shelves", `4.10.0-dev.92`,
  2026-09-25) — **NINE non-merge commits past the baseline**: `83d0c969b`
  (NO-PORT?, P4.D232), `a8292547a` (NO-PORT?, P4.D232), `8bd080267` (#73
  — P4.D225 / client P4.D229), `49059fb14` (#74 — P4.D225 / client
  P4.D230), `4d370a90f` (#75 — P4.D226 / client P4.D229 / help P4.D228),
  `3b463d6b1` (#76 — P4.D227 / client P4.D230 / help P4.D228),
  `ce2f1dabf` (#77 — P4.D228 / client P4.D229), `6d0f88d65` (REGEN EVENT
  + NO-PORT? code — P4.D232), `08c49319d` (P4.D231 / help P4.D228) — every
  row `ORDERED(…)` in the ledger's §3. v4 `bugfix` tip `1a2b2164c` and
  `release` tip `8fbf2afe0` are unmoved; still no `release: 4.10.0`
  squash. The checkout sat on `main`, CLEAN, at `08c49319d` at the
  planning probe (2026-09-25 — the §2 probe PASSED; nothing was
  re-derived). Spec reads: `git show <sha>:<path>` for post-commit files
  and `git show <sha> -- <path>` for the hunks — **never the commit
  message and never the six `docs/developer/features/concierge-overhaul*`
  specs' prose** (§R.4). **At unification the baseline MOVES to
  `08c49319d`**; the rows retire `ABSORBED(…)` / `NO-PORT-RATIFIED(…)`.
- **§R.2 — The freshness probe.** Run the drift ledger's §2 probe (four
  read-only commands: branch, `status --short`, `log 08c49319d..main`,
  `log 1a2b2164c..bugfix`) at lane start and again before EVERY regen
  batch. **Pass = branch `main`, HEAD `08c49319d`, both logs EMPTY, tree
  CLEAN.** Any mismatch — a moved HEAD (a new commit is a NEW drift row,
  not a pass), a non-empty log, ANY dirty path, the checkout off `main` —
  is a STOP: report it and do nothing else. A lane never writes the
  ledger. A lane resumes ONLY on a waiver the human records in the
  ledger's §1 (the `89fcc3c0d` round's mechanism), never on its own reading
  of the dirt.
- **§R.3 — Regen rule: PIN REQUIRED** (drift-ledger §1), absolutely, and
  **PER-LANE TARGETS inside the chain**: HEAD is nine commits past the
  baseline and the five Concierge commits rewrite the SAME functions in
  sequence, so a stacked lane that regenerated at HEAD would record
  behaviour that belongs to a later lane. Targets: **P4.D225 →
  `49059fb14`**; **P4.D226 → `4d370a90f`**; **P4.D227 → `3b463d6b1`**;
  **P4.D228, P4.D229, P4.D230, P4.D231, P4.D232 → `08c49319d`** (the round
  target). Each stacked lane's "before" for pre-port red counts is the
  lane it stacks on (its pin); the `main`-cut lanes use the BASELINE
  `b0b6656b5`. Every regen runs from a LANE-UNIQUE detached worktree per
  ledger §5.1 (`git -C ~/source/quilltap-server worktree add --detach
  /tmp/qt-v4-pin-<order>-<sha> <sha>`, the THREE symlink classes — root
  `node_modules`, `packages/quilltap/node_modules`, every `plugins/dist/*/
  node_modules` — then `cd` into it; never stash or checkout-switch the
  human's repo; `git worktree list` before each batch; **verify the pin by
  `git -C "$PIN" rev-parse HEAD` AND by `ls -ld` of the path** — build
  every pin path from a quoted literal, never an unsplit zsh variable;
  then grep the fresh NDJSON for a marker only one tree has; check the
  file is non-empty before believing a diff). **The pins' `node_modules`
  are the CHECKOUT's** (openai 7.23.0, `@openrouter/sdk` 1.3.28,
  plugin-types 2.8.0 — measured at planning): every provider corpus
  re-recorded at ANY pin carries the NEW stamps; a "pre-bump" corpus can
  no longer be produced; the guard's constants are P4.D232's alone (§S.4).
  **Node:** every regen under `PATH=$HOME/.nvm/versions/node/v24.13.1/bin:
  $PATH` (a REAL path in every env block — never `$N/node`); the corpora
  carry 290 `v24.13.1` runtime stamps and the PATH also has 24.18, 24.19
  and Homebrew 26. A family that moves under TWO lanes of the CHAIN is
  re-run by the later lane at ITS pin (that is what stacking means: each
  chain lane's first act is re-running the previous lane's whole family
  list at its own pin and recording what moved); a family that moves under
  two PARALLEL lanes is a fence error — STOP on it and record (the fences
  in §R.10 are drawn so it cannot happen; §S.4's `image-dialects` is the
  one declared exception with a declared winner). Pin verification marker
  (round-wide, for the HEAD pin): the target-pinned `help_tree_equivalence`
  NDJSON names `the-concierge` and its `scenario-builder.md` carries
  `## Called to a Shelf`; a `b0b6656b5` pin names `dangerous-content`;
  the chain pins carry their own markers (named in each order). The
  sweep driver is the sanctioned per-family path (`python3 harness/tools/
  recipe_sweep.py --run <family> --v4 "$PIN" --v5w <ABSOLUTE worktree>`);
  never run two sweeps concurrently; a family that reports `SKIP` in
  0.00 s did not run. Ledger §5.2's silent-stale-pass discipline applies
  to every regen (`rm -f` the output first; read the builder's last line;
  `grep -c` the changed bytes in the fresh NDJSON — MUST be > 0 where a
  change is predicted). Every lane stages its regen outputs under a
  LANE-PRIVATE directory (`/tmp/<order>/…` — several families' committed
  recipes write FIXED `/tmp/oracle-*.ndjson` paths; re-derive into the
  lane's directory before any `cargo test --workspace`) and leaves the
  committed recipe headers canonical (a committed recipe NEVER names a
  `/tmp` pin — the driver's `--v4` is the pin). Every jest oracle writes
  ZERO `llm_logs` rows (`jest.setup.ts` mocks the logging service) — never
  predict an `llm_logs` diff from a jest family; count the canned-LLM
  calls instead. **`jest-oracle-instanceof-across-resetmodules`** is THIS
  round's shape: `ModerationRejectionError instanceof` across
  `resetModules()` silently never fires — import the class INSIDE the
  case and PROVE the typed arm with a `grep -c` on the fresh NDJSON.
- **§R.4 — Port from the hunks, never the prose** (ledger §5.3). Measured
  at planning: (a) the phase-2 SPEC declares the ledger columns in both
  chat schemas and exports them — as-built they are in NEITHER and NOT
  exported; it says the increment returns the count from one UPDATE —
  as-built it is `$set`+`$inc` then a SEPARATE read; it says `count:
  number` — as-built `number | null`. (b) #73's message says it "drops the
  Lantern's bug-133 gate" — it ALSO adds an `AUTO_ROUTE` conjunct to
  `uncensoredImageTarget` (prompt crafting moves); it says the text change
  is "retried before the chain" — the EMPTY-BODY path also changed (no
  `uncensoredTextProfileId` needed; the original excluded). (c) #75's
  message names the wire keys correctly (`conciergeSetBy` /
  `conciergeReason` / `conciergeRefusalCount`) — the LEDGER's working names
  (`conciergeStateSetBy` / `conciergeStateReason` / `moderationRefusalCount`)
  are WRONG for the wire. (d) #77's "Try uncensored in the refusal bubble"
  is the LANTERN's `background-refused` bubble ONLY; "hidden on Locked" is
  the ONLY gate; API.md's exclusion list OMITS the same-provider+model
  exclusion; "completes instead of failing" holds only for a refused trail
  with no answer. (e) `08c49319d`'s message is silent on THREE behaviour
  changes (the mode default now in-world with a project and no cast;
  optgroups + name-sorted groups in cast mode too; `groupIds` sent on
  EVERY surface) and its "unreadable id dropped with a WARN" is
  UNREACHABLE through v4's real code (`findByIdRaw` is a fallback
  `safeQuery`). (f) the ledger's "a new `*_ids` field moves the dispatch
  census" for `08c49319d` is FALSE — the build body is ONE raw `Value`;
  the census stays 449 for that lane. (g) `6d0f88d65`'s regen event
  BEGAN at #73 (three bundles already at 7.23.0 there) — no recorder reads
  a bundle, so it changes nothing. (h) `83d0c969b`'s bug file says "v5
  status: Unchecked" — measured NOT AFFECTED (a dispatch verb, no HTTP
  method). Re-verify even where an order flags the trap — the order's
  paragraph is the same kind of prose. Carry a v5-side code comment naming
  the sha where the prose misleads.
- **§R.5 — Designed reds and pre-existing reds: measure, don't assume**
  (ledger §5.4). **No CONVERGENCE row** (v4's `bugs.md` gained only bug
  170). The DESIGNED reds by lane: **P4.D225** — `danger_routing_
  equivalence`'s CASE fails to IMPORT at its pin (two deleted functions —
  rewrite); `image_dialects_equivalence`, `stream_decoders_equivalence`,
  `response_parse_equivalence` on the re-recorded corpora; `fallback_
  engine` on the pattern rows; `primary_stream_tier3`, `image_generation_
  tier3`, `avatar_job_tier3`, `story_background_job_tier3`, `images_
  generate_route`, the two post-office families, `danger_resolver` +
  manual-flip, `chats_tier2`, `chat_settings_tier2`, `cheap_llm_fallback`,
  `write_apply`, `finish_reason` on their new arms; `qtap_schema_embed_
  guard` until the export schema is re-vendored; **`provider_sdk_version_
  guard` RED on its branch** (half 1 as on `main`; half 2 on `image-
  dialects`' new stamps — §S.4). **P4.D226** — `provisioning_equivalence`
  (the D23 tripwire: the mode trio); every chats-READING family until the
  42 pairs are widened (v5's strict `ALL_COLUMNS`); `danger_resolver`,
  `danger_trigger`, `danger_gatekeeper_tier3`, `danger_scan_tier2`,
  `salon_reads` (the chat GET), `salon_mutations` (the three-value enum;
  the P4.D141 refusal arms), `chat_create_capstone`, `projects_routes`,
  `characters_reads`, `chat_cast_routes`, `chat_scenario_routes`,
  `chats_read`/`chats_tier2` (`patchOnlyFields`, `setConciergeMode`),
  `home_routes`, `in_scene_voiced`, `system_restore_state` / `system_
  import_state` (`withConciergeModeFromLegacy`), `qtap_schema_*`
  (`conciergeOverride` deprecated), the post-office writers (the four
  transition kinds), `initial_greeting` (the Locked skip), `message_
  finalizer_tier3`, `appearance_sanitize_gate`, `orchestrator_tier3`,
  `context_summary_service`; `host_boot` / `host_cadence` (hand-written
  DDL mirrors); `dispatch_wrong_type_census` IF `ChatUpdate.concierge_
  state`'s multi-line serde attribute is rewritten (the `strip_noise`
  hazard — measure). **P4.D227** — `provisioning_equivalence` again
  (`conciergeSettings` + the DROP + the seed); `danger_resolver`'s CASE
  fails to IMPORT at its pin (`resolveDangerousContentSettings` deleted —
  rewrite); `chat_settings_tier2`, `chat_settings_column_sites_guard`,
  `settings_routes` (the retired-key 400), `help_tools`, `tool_
  definitions` + the pseudo-tool prompt families (the `helpSettings`
  enum + `helpNavigate` URL bytes; the count 59 UNMOVED), `images_
  generate_route` (WF), `image_generation_tier3`, `image_generate_route`,
  `story_background_job_tier3`, `avatar_job_tier3`, `appearance_sanitize_
  gate`, `orchestrator_tier3`, `primary_stream_tier3`, `answer_
  confirmation_tier3`, `memory_processor_tier3`, `compression_tier3`,
  `cheap_llm_selection`, `precompute`, `context_summary_service`,
  `file_attachment_tier3`, `almanack_tier2` (WF with the `imagePrompt
  ProfileId` move), the carina / memory-pipeline / enclave-step /
  courier-transport builders, `backup_uuid_remap` (the four desk ids),
  `system_restore_*`, `host_llm_log_cleanup` (DDL + WF); every settings-
  reading family until the 23 pairs are widened (v4's own regen dies on
  the `.default()` column — `fixture-vintage-gap-which-columns-are-fatal`).
  **P4.D228** — `query_param_semantics_equivalence`'s `chat_item_post`
  row; `orchestrator_tier3` + `primary_stream_tier3` on the NEW
  direct-route arms; `story_background_job_tier3` on the flips; `help_
  tree_equivalence` until the tree is re-vendored (`help_tree_embed_guard`
  stays GREEN — it cannot see a stale re-vendor). **P4.D229 / P4.D230** —
  no Rust red; their specs are red-first by construction; their gated
  beats SKIP until the unifier flips them. **P4.D231** — `scenario_build_
  request_schema`, `scenario_builder_mount_pool`, `scenario_builder_tier3`,
  `scenario_builder_routes` on the new field/lines; `tiered_mount_pool`
  NEUTRAL. **P4.D232** — `provider_sdk_version_guard` half 1 GREEN once
  the constants move, half 2 RED on `image-dialects` until P4.D225 lands
  (§S.4). **Standing intermittents** (recorded, not this round's): the
  `/tmp/qt-imggen-*` staging collision in `cargo test --workspace` (green
  by name); the Salon-streaming e2e timing cluster and the P4.66 bubble
  beat (each green alone); the `salon-regenerate-stream-flow` `2/3` watch
  item. A red in a family a lane did NOT touch is a NEW finding to
  classify at both pins before any source is touched, and it is probably
  not that lane's — in the chain, it is probably the PREVIOUS lane's
  handoff, which you take and record by name.
- **§R.6 — The differential requirement stands for every unit.** A ported
  unit lands WITH an equivalence test that drives v4's REAL code at the
  right tier (tier 1 exact / tier 2 structural DB diff / tier 3
  mocked-LLM; the SPA's tier-1 recorders for pure client modules),
  red-first where the order says so; a log line lands with a
  capture-layer pin (`quilltap_core::test_support::captured_with`, the
  thread-scoped rig, or `global_capture` where the callsite's `Interest` is
  shared) and its silence leg; a fixture change lands with the regen of
  every family that reads it — **and this round widens fixtures at scale:
  P4.D226 all 42 chats-bearing pairs (migrations 1 + 2), P4.D227 the same
  42 + the 23 settings-bearing pairs (migrations 3 + 4), P4.D225 only the
  four pairs its ledger arms read (migration 1), through a NEW arm of the
  fixture migrator that IMPORTS AND RUNS v4's REAL migration modules from
  the lane's pin against each fixture handle (the ADD-COLUMN twin cannot
  express #75's/#76's JavaScript backfills or #76's DROP), `--report-only`
  first, the three known unguarded `ADD COLUMN` plants in oracle cases
  (`salon-reads.test.ts`, `chat-scenario-routes.test.ts`, `p4d171-columns.
  ts`) checked BEFORE applying, the migration-vintage trio REBUILT at the
  lane's pin (it runs v4's real runner), the three hand-written DDL mirrors
  (`host_boot.rs`, `host_cadence.rs`, `host_llm_log_cleanup.rs`) chased,
  and EVERY reader of a widened pair re-run (a full sweep at the lane's
  pin is the honest gate for P4.D226 and P4.D227). v5 NEVER backfills or
  drops at boot: the ledger columns arrive by the P4.D182-model boot
  ensure (schema-absent), the mode trio and `conciergeSettings` by the D23
  re-dump on fresh instances and by v4's migration on migrated ones, and
  the `conciergeOverride` DROP by v5 simply no longer binding the column
  (either shape opens). Mutation proofs: each named proof must redden
  exactly the test it is written against, and a proof that SURVIVES as
  first written is a finding to fix, not a line to delete. A v4 unit test
  that MOCKS a repository or a handler (every new test in this round's
  commits does — the retry family's in particular) is mirrored as a
  real-DB / real-route arm with PLANTED rows through the existing oracle,
  never as the same mock (`a-v4-mock-is-not-v4-in-a-route-family`). A
  ruled divergence is pinned in BOTH directions (the `EXPECTED_DIVERGENCES`
  shape, `markdown_transcript_equivalence.rs:40,213-233,256-261`).
- **§R.7 — Append-only shared files.** `docs/CHANGELOG.md` and
  `docs/developer/porting/status-log.md` are append-only for every lane
  (they union-merge at unification). A stale claim in an OLD entry is
  corrected by a NEW record that names it, never by editing the old one.
  This order's status header is the lane's to update at lane close (a
  stacked lane reads the header of the lane it cuts from — LANE COMPLETE
  is the signal); the unifier rewrites it at unification. `docs/developer/
  porting/drift-ledger.md` is NEVER written by a lane; `docs/developer/
  porting/phase-4.md`, `dogfood-findings.md` and the `docs/v4/` mirror are
  the UNIFIER's (a lane pre-lists what moves). Cross-order header edits
  allowed this round: NONE.
- **§R.8 — Version bumps.** Each lane bumps every crate whose files it
  changed on each commit per `.claude/commands/commit.md`; the unifier
  RECOUNTS as base + the sum of every lane's bumps (the silent-auto-merge
  trap: identical bumps merge as ONE — it fires every round; in the CHAIN
  the bumps are sequential and carry, so the unifier reads the chain's
  tip once and adds the parallel lanes' sums). Base at planning: core
  0.0.1058, harness 0.0.977, host 0.0.158, web 0.0.196, cli 0.0.27, tauri
  0.0.7, fixture-sanitizer 0.0.4, sqlite3mc-sys 0.1.0 (PINNED, never
  bumped), SPA 0.5.773. Expected: **core** by P4.D225–P4.D228 (the
  chain), P4.D231 (`services/scenario_builder/**`, `db/tiered_mount_pool.
  rs`, `api/scenario_builder.rs`); **harness** by every Rust lane;
  **web** by P4.D225 (`images_routes.rs`), P4.D226 (the chat PUT/POST
  edge), P4.D227 (`settings_routes.rs`), P4.D228 (`wardrobe_routes.rs` +
  the wire tests), P4.D231 ONLY if its route family moves (say so);
  **host** by P4.D225 (the boot ensure's host arm + `images_generate.rs`),
  P4.D226/P4.D227 (the DDL mirrors), P4.D228 (the help boot test if it
  moves), P4.D231 ONLY if the driver struct moves (say so); **cli** by
  NOBODY (Tier R is a re-run); **tauri**, **fixture-sanitizer** by NOBODY;
  **the SPA** by P4.D229, P4.D230, P4.D231. No lane touches the workspace
  `Cargo.toml` dependency block or `apps/web/package.json`'s dependency
  block (the KaTeX pin is a NAMED housekeeping item, not this round's);
  every `Cargo.toml` / `package.json` delta must be version-only — the
  unifier audits each before any resolution (the `--theirs` clobber trap).
- **§R.9 — The `docs/v4/` mirror** is the UNIFIER's, not any lane's:
  each lane pre-lists, in its lane record with byte counts at the ROUND
  target `08c49319d`, the mirror paths its rows move. Measured at
  planning: `docs/v4/developer/bugs/fixed/bug-170-speaker-switch-wrong-
  method.md` (NEW) + `docs/v4/developer/bugs.md` (two hunks) — P4.D232's
  pre-list; `docs/v4/developer/features/concierge-overhaul.md` + the five
  `concierge-overhaul-phase-{1..5}-*.md` (NEW — at the TARGET's bytes,
  never `a8292547a`'s) + `features/ROADMAP.md` (+1) + `features/complete/
  {concierge-default-at-creation,concierge-four-state,concierge-list-
  marks,dangerous,message-route-trail}.md` + `features/scenario-builder.
  md` (the seven differing files) — P4.D232 pre-lists, the chain lanes
  and P4.D231 add any their commits touch (`DDL.md`, `PROVIDER_PLUGIN_
  DEVELOPMENT.md`, `BACKGROUND_JOBS_CHILD.md`, `API.md`, `DATABASE_*`).
  `docs/v4/CHANGELOG.md`'s 48-entry lag stays a named housekeeping item.
  `README.md`, `CLAUDE.md`, `.claude/commands/*`, `jest.setup.ts`, every
  `package.json`/`package-lock.json` stamp, `scripts/*.sh`, `lib/startup/
  prettify.ts` labels → NO-PORT, the standing class. **The `help/` count
  STAYS 129** (`dangerous-content.md` deleted, `the-concierge.md` added,
  17 more modified) — P4.D228 re-vendors the WHOLE tree at `08c49319d`;
  both literals (`help_tree_embed_guard.rs:76`, `host_help_docs_boot.rs`)
  UNMOVED and run to prove it; `help_tree_equivalence` at the HEAD pin is
  the proof the tree is v4's (the embed guard compares embedded vs DISK
  only). The `dogfood-findings.md` notes this round earns (the live-
  instance consequence of the `conciergeOverride` DROP — a Friday copy
  taken after v4's `4.10.0-dev.88` is REQUIRED for the next dogfood, and
  one taken before it CANNOT open after P4.D226 lands) are the unifier's.
- **§R.10 — Fences, ownership edges and the pre-declared spills.** (a)
  **`api/types.rs` and `api/engine.rs`:** the CHAIN's (sequential — each
  lane adds its variants on top of the previous lane's; P4.D226 the
  `conciergeState` enum + the chat GET/list/create shapes; P4.D227 the
  settings shapes; P4.D228 the two retry verbs). P4.D231 adds NOTHING
  there (the build body is a raw `Value` — §R.4(f)). Nobody else touches
  them. `dispatch_wrong_type_census`: 449 at planning; P4.D226 measures
  the `strip_noise` hazard; P4.D228 adds +1 per typed `*_id` on its
  verbs; each chain lane records the literal it left; P4.D231/P4.D232
  run it UNMOVED. (b) **`crates/quilltap-web/src/**`:** the chain's
  (sequential) for `images_routes.rs` (P4.D225), the chat PUT/POST edges
  (P4.D226), `settings_routes.rs` (P4.D227), `wardrobe_routes.rs` +
  `lib.rs` (P4.D228); P4.D231 owns `scenario_builder_routes.rs` ONLY
  (measured: no change needed). (c) **Core services and db:** the chain
  owns EVERYTHING under `services/dangerous_content/**`, `services/
  {provider_failover,route_trail,concierge_notifications,lantern_
  notifications,image_job_common,character_avatar_job,story_background_
  job,tool_execution,queue_service,orchestrator,regenerate_swipe,chat_
  create,chat_enrichment,danger_scan,cheap_llm_exec,...}.rs`, `llm_
  fallback/**`, `model/**` (the dialects, decoders, parsers), `finish_
  reason.rs`, `tools/generate_image.rs`, `tools/help.rs` + `tools/
  definitions/**`, `db/chats*.rs`, `db/chat_settings.rs`, `db/*_repair.rs`,
  `write_apply.rs`, `services/backup/**`, `services/quilltap_import/**`,
  `services/provisioning/**`, `generators/qtap-export.schema.json` +
  `qtap_export/**`, the almanack, `api/{salon,settings,images,chat_media,
  ...}.rs` — sequentially; P4.D231 owns `services/scenario_builder/**`,
  `db/tiered_mount_pool.rs`, `api/scenario_builder.rs` and NOTHING else
  in core; P4.D232 owns NO core file. (d) **`crates/quilltap-host/**`:**
  the chain's (sequential); P4.D231 reads `spine.rs` and changes nothing
  (measured). (e) **`help/**` + the two count literals:** P4.D228's alone
  (the WHOLE tree at HEAD, incl. P4.D231's four pages). (f) **`apps/web/
  **`:** P4.D229 owns `chat/**` (incl. `concierge-*`, `sidebar/**`,
  `conversation-header.ts`, `tool-message.ts`, `route-trail-*`, `system-
  message-labels.ts`, `message-row.ts`, `message-action-bar*`, `message-
  list.ts`, `regeneration.state.ts`, NEW `concierge-retry*`, NEW
  `concierge-off-duty-hint.ts`), `quick-hide/**`, `screens/salon/**`,
  `screens/home/**`, `screens/new-chat/**`, `screens/prospero/cards/
  project-chats-section*`, `screens/characters/view/tabs/{character-
  conversation-card,conversations-tab}*`, `screens/about/**`, `workspace/
  chrome/link-interceptor.spec.ts`, **`core/core-contract.ts` (OWNER)**,
  `styles/qt-components/_chat.css` (the `-info` rules), `testing/
  fixtures/route-trail-display.oracle.ndjson` + `apps/web/oracle/route-
  trail-display.ts` + `harness/oracle/cases/concierge-presentation.mjs` +
  the NEW recorders, e2e `{salon-concierge-*,concierge-marks-flow,salon-
  danger-avatar-flow,salon-streaming-avatar-flow,new-chat-flow,salon-
  route-trail-flow,salon-try-uncensored-flow}.spec.ts`; P4.D230 owns
  `screens/settings/**` (NEW `screens/settings/concierge/**`; `chat/
  dangerous-content-settings.ts` DELETED; `chat/chat-tab.ts`, `chat/
  image-description-settings.ts`, `chat/chat-settings.types.ts`,
  `providers/cheap-llm-card.ts`, `settings.ts`), `help/help-categories.
  ts` + `help/__fixtures__/*` + `help/help-stream*` + `apps/web/oracle/
  help-guide-capture.test.tsx`, e2e `settings-chat-cards-flow.spec.ts` +
  NEW `settings-concierge-flow.spec.ts`; P4.D231 owns `scenario-builder/
  **`, `screens/scenarios/**`, `screens/prospero/cards/project-scenarios-
  card.ts` + `project-detail.ts` (the ONE binding), `screens/groups/**`
  (NEW `group-scenarios-card.ts` + `group-editor.ts`'s ONE mount), NEW
  e2e `scenarios-shelf-builder-flow.spec.ts`. (g) **Oracle cases:** by the
  family lists in each order; `harness/oracle/fixtures/migrate-memories-
  fixture-columns.ts` gains its module-running arm in P4.D225 (the first
  lane to need it — the chain inherits it). (h) **Committed fixtures:**
  §R.6's widening schedule (P4.D225 four pairs; P4.D226 42; P4.D227 42 +
  23 + the vintage trio rebuilt); P4.D228/P4.D231/P4.D232 widen NOTHING;
  P4.D231 PLANTS rows in specs only. (i) **Pre-declared loud out-of-
  mandate edits** (the `a-required-field-on-a-shared-struct-crosses-lane-
  ownership` rule: do the edit, keep it to the hunk, mark it `// P4.xxx
  OUT-OF-MANDATE — <owner> preserves`, audit with `git diff main --
  <path>` at lane close): **P4.D230 → `core/core-contract.ts`** — the
  `ChatSettingsDto` shape (§S.2); **P4.D231 → `core/core-contract.ts`**
  — `groupIds?` on `ScenarioBuildRequestInput` + `includeArchived?` on
  the three `GroupScenario*Request` twins; nothing else in that file by
  either. If a lane finds it needs another lane's file beyond this, it
  STOPs on that item and records the shape for the unifier.
- **§R.11 — SPA lanes, Playwright, and the gated beats.** P4.D229 and
  P4.D230 are SPA-only and run without cargo; P4.D231 runs both gates.
  The SPA's e2e port 4319 is repo-wide — the three lanes that run
  Playwright coordinate through the human (one suite at a time; `e2e-
  port-4319-is-repo-wide`). Every SPA beat that needs the server chain is
  written GATED (`const P4D2xx_SERVER_LANDED = false` + `test.skip(!…,
  GATE_REASON)` — the `P4D217_SERVER_LANDED` mechanism) naming the lane
  it waits on; the unifier flips each gate after the pick and runs the
  beat LIVE for the first time (P4.D218's precedent: 4/4 on the first
  run). A beat that reads stored `conciergeMode` / `conciergeSettings`
  columns through the CLI is gated too — those columns do not exist on
  `main`'s seed.
- **§R.12 — Stacking mechanics.** A stacked lane (P4.D226, P4.D227,
  P4.D228) starts ONLY when the lane it stacks on has (1) committed
  everything, (2) run its gate, (3) written LANE COMPLETE into its order's
  status header, and (4) told the human; it cuts its worktree from that
  lane branch (`git worktree add <path> -b claude/<order>-<hash> <lane
  branch>`), never from `main`. From that moment the lower lane's branch
  is FROZEN: no rebase, no amend, no force-push — a fix it still owes is a
  NEW commit the upper lane cherry-picks or waits for (record which). The
  upper lane's FIRST act is re-running the lower lane's whole family list
  at its OWN pin and recording, family by family, what moved (expected:
  exactly the families its commit rewrites) — a family that moved
  unexpectedly is the lower lane's handoff and is taken by name. The lower
  lane's Tier-1 shapes are the upper lane's contracts; an insufficient
  shape is a STOP-and-record, not a rewrite of the lower lane's file in
  place (the upper lane MAY extend a shape additively). The chain's
  versions carry (§R.8). At most TWO Rust full gates run concurrently
  (114 GB free at planning; a Rust worktree costs 50–70 GB): the chain
  runs one at a time by construction; P4.D231 and P4.D232 are the other
  slot, in turn.

## §S — Shared contract (binding; byte-identical in every order of this round)

### §S.1 — The chat's Concierge shape (P4.D226 serves; P4.D229 consumes; P4.D228 reads)

The chat GET (`chatGet` → `ChatDetail`) carries **`conciergeState:
'moderated' | 'unmoderated' | 'locked'`** (= v4 `getConciergeState(row)`),
**`conciergeSetBy: 'operator' | 'concierge' | null`** (null when Moderated),
**`conciergeReason: 'manual' | 'refusals' | 'classifier' | 'migration' |
null`** (null when Moderated), **`conciergeRefusalCount: number`** (v4:
`repos.chats.getModerationRefusalLedger(chatId).count`), KEEPS
`isDangerousChat` (telemetry) and `dangerCategories`, and DROPS
`conciergeOverride`. The chat PUT (`chatUpdate { chatId, chat: {},
conciergeState }`) and the create (`chatCreate.conciergeState`) take the
THREE values; any retired value (`monitored`/`flagged`/`vouched`/
`uncensored`/`OFF`/`UNCENSORED`) → **400**. Absent on create ⇒
`newChatsStartAs` when on duty, else none; a non-`moderated` value while
OFF DUTY on create ⇒ IGNORED (a WARN, not a 400). The create response
carries the RAW columns (`conciergeMode`, `conciergeModeSetBy`,
`conciergeModeReason`) post-flip. Every list payload (`chatsList`,
`projectChats`, the character's chats, home recents) carries
`conciergeState` (derived) + **`conciergeSetBy`** + **`conciergeReason`** +
`dangerCategories` (`[]` when none) and NO refusal count. **Whether a PUT
with `conciergeState` is refused, no-op'd or applied while OFF DUTY is
P4.D226's to MEASURE against v4's real `processChatUpdates` →
`applyConciergeFlip` and RECORD**; P4.D229's e2e asserts the disabled
select only. The stored columns are `chats.conciergeMode` (default
`'moderated'`), `conciergeModeSetBy`, `conciergeModeReason`; `getConcierge
State` reads the column FIRST then the payload key; the legacy pair is
IGNORED; NULL ⇒ `moderated`.

### §S.2 — The chat settings' `conciergeSettings` (P4.D227 serves; P4.D230 consumes; P4.D229 reads `enabled` + `newChatsStartAs`)

`chatSettings` (GET) carries **`conciergeSettings`** and NO LONGER carries
`dangerousContentSettings`, `uncensoredImageDescriptionProfileId` or
`cheapLLMSettings.imagePromptProfileId` (the server STRIPS them on read as
v4's Zod does). `chatSettingsUpdate` REPLACES `conciergeSettings` wholesale
(the object safeParsed with defaults filled; invalid ⇒ 400 `Invalid
conciergeSettings: <path>: <msg>; …`) and answers **400 `Invalid settings:
<keys joined ', '> was|were replaced by conciergeSettings`** when ANY of
the three retired keys is PRESENT (`typeof !== 'undefined'` — an explicit
`null` counts). The shape, v4's Zod at `3b463d6b1` verbatim:

```ts
DangerousContentDisplayModeEnum = z.enum(['SHOW', 'BLUR', 'COLLAPSE']);
ConciergeNewChatStateEnum = z.enum(['moderated', 'unmoderated']);
ConciergeDisplaySettingsSchema = z.object({
  mode: DangerousContentDisplayModeEnum.default('SHOW'),
  showWarningBadges: z.boolean().default(true),
});
ConciergePreScreenSettingsSchema = z.object({
  enabled: z.boolean().default(false),
  threshold: z.number().min(0).max(1).default(0.7),
  scanTextChat: z.boolean().default(true),
  scanImagePrompts: z.boolean().default(true),
  scanImageGeneration: z.boolean().default(false),
  customClassificationPrompt: z.string().nullable().optional(),
  summaryClassification: z.boolean().default(false),
});
ConciergeSettingsSchema = z.object({
  enabled: z.boolean().default(true),
  uncensoredTextProfileId: UUIDSchema.nullable().optional(),
  uncensoredImageProfileId: UUIDSchema.nullable().optional(),
  uncensoredVisionProfileId: UUIDSchema.nullable().optional(),
  imagePromptProfileId: UUIDSchema.nullable().optional(),
  autoSwitchAfterRefusals: z.number().int().min(0).max(10).default(2),
  newChatsStartAs: ConciergeNewChatStateEnum.default('moderated'),
  display: ConciergeDisplaySettingsSchema.default({ mode: 'SHOW', showWarningBadges: true }),
  preScreen: ConciergePreScreenSettingsSchema.default({
    enabled: false, threshold: 0.7, scanTextChat: true, scanImagePrompts: true,
    scanImageGeneration: false, summaryClassification: false,
  }),
});
// ChatSettingsSchema.conciergeSettings: ConciergeSettingsSchema.default({ enabled: true,
//   autoSwitchAfterRefusals: 2, newChatsStartAs: 'moderated', display: {…}, preScreen: {…} })
```

The client default object = the server's `DEFAULT_CONCIERGE_SETTINGS` (all
profile ids `null`, `customClassificationPrompt: null`, the schema defaults
otherwise). P4.D230 lands the `ChatSettingsDto` hunk in `core-contract.ts`
(§R.10(i)); P4.D229 reads `conciergeSettings?.enabled !== false` (on duty,
default TRUE while loading) and `newChatsStartAs` off the SHARED
`chatSettingsKeys.all` query and changes no key.

### §S.3 — The two retry verbs (P4.D228 serves; P4.D229 consumes)

**`{ type: 'chatRetryImageUncensored', chatId, body }`** where `body` is v4's
JSON body verbatim — `{ toolMessageId: string }` | `{ kind: 'background' }`
— decoded FIRST-BRANCH-WINS with unknown keys STRIPPED (`Request::
ChatRetryImageUncensored { chat_id, body: Value }`, the raw-body precedent).
Picture: **200** `{ toolMessageId, images: GeneratedImage[], routeTrail }`;
404 `Tool message not found`; 400 `Only generate_image pictures can be
retried uncensored`; 502 `{ error, details: { code } }` (`details: {}` when
no code). Background: 200 `regenerate-background`'s own `{ message, queued,
jobId }`. Both: 400 `Expected { toolMessageId } or { kind: "background" }`;
**409** with the error message EXACTLY **`locked`** or **`no-understudy`**
(the REST arm answers the pointer; over dispatch the `CoreDispatchError`'s
`message` is that bare token — P4.D229 maps it by message; P4.D228 records
what `kind` a 409 carries). **`{ type: 'messageRetryUncensored', messageId,
stream }`** (`Request::MessageRetryUncensored { message_id, stream: bool }`,
RPC-only, no REST leg): 404 `Chat not found` / `Message not found`; 400
`Only assistant messages can be retried`; 400 `Staff and system messages
cannot be regenerated`; 409 `locked` / `no-understudy` — every refusal a
dispatch error BEFORE any frame; with `stream: true` the SAME `swipeProgress`
frame family as `messageSwipe` keyed by the target `messageId` (status →
`rerouting`/… frames, content/reasoning deltas, the terminal `{ done: true,
message }` / error frame — the P4.D207 shape; v4 has two independent SSE
streams, v5 one broadcast per message id — a recorded shape divergence,
no server change); without it **201** `{ message: newSwipe }`. The new
swipe's `routeTrail` ends with a `via: 'concierge'`, `outcome: 'answered'`
row; the picture retry files a NEW TOOL row at `original.createdAt + 1 ms`
whose `routeTrail`'s last row carries `profileKind: 'image'`. Neither verb
ever writes the chat's Concierge state. The chat POST action list gains
`retry-image-uncensored` at index 24 (after `regenerate-background`).

### §S.4 — The provider corpora and the guard (P4.D225 ↔ P4.D232)

P4.D232 moves `RECORDED_OPENAI_SDK` → `"7.23.0"` and `RECORDED_OPENROUTER_
SDK` → `"1.3.28"` and re-records **`request-envelopes`** (+ the neutrality
set: the four #73-UNTOUCHED `chat_completions_sse` stream files, the
manifests, `google-wire`, the OpenRouter pricing oracle). P4.D225 re-records
**`image-dialects`**, **`response-bodies`** and the four #73-MOVED stream
files (`responses_api_sse/{openai,grok}`, `chat_completions_sse/openrouter`,
`google_parts/google`) at its pin. On P4.D232's branch the guard's half 2
is RED on `image-dialects` (old stamps vs new constants); on P4.D225's
branch half 1 is RED (installed, as on `main`) and half 2 is RED on
`image-dialects` (new stamps vs old constants) — BOTH recorded by name as
designed; the union is GREEN and the unifier proves it. Neither lane
touches the other's corpus; `image-dialects` has ONE owner (P4.D225).

### §S.5 — The trail row and the system-message kinds (P4.D225 serves; P4.D229 consumes)

`RouteAttempt { profileId, profileName, provider, modelName, via, outcome,
trigger?, evidence?: 'typed-error' | 'provider-code' | 'finish-reason' |
'message-pattern' | 'inferred', profileKind?: 'connection' | 'image' (absent
= connection), detail? (≤ 200) }` on EVERY message row incl. TOOL rows and
the Lantern/Aurora bubbles. System rows the client renders (all
`role: 'ASSISTANT'`, `participantId: null`): `systemSender: 'concierge'` with
`systemKind: 'danger'` for the classifier verdict and the transitions
(`set-moderated` / `set-unmoderated` / `set-locked` / `auto-unmoderated`)
and `systemKind: 'refusal'` for `refusal-rerouted` / `refusal-no-understudy`
/ `refusal-not-permitted`; `systemSender: 'lantern'` with `systemKind:
'background-refused'` (may carry `routeTrail`). Retired kinds (`manual-
flagged`, `manual-safe`, `manual-resumed`, `manual-vouched`, `manual-
uncensored`, `auto-flagged-refusals`) survive in old transcripts as
ordinary `danger` rows and MUST still render.

### §S.6 — The help slug and URL (P4.D227's tool bytes, P4.D228's tree, P4.D230's categories)

`help/dangerous-content.md` is DELETED and `help/the-concierge.md` ADDED
(front matter `url: /settings?tab=concierge`); the `help_settings` tool
gains the `concierge` category and the `help_navigate` example URL becomes
`/settings?tab=concierge` (the tool count stays 59); the SPA's
`content-routing` category lists `['the-concierge', 'story-backgrounds',
'scene-state-tracker']` and `URL_CATEGORY_MAP` gains `/settings?tab=
concierge` after `templates`, before `images`. The three land in different
lanes and meet only at unification; `help_tree_equivalence` (P4.D228),
the tool-definition families (P4.D227) and the help-guide capture
(P4.D230) each prove their own third.

### §S.7 — A lane that finds it needs a wire change beyond §S.1–§S.6 STOPs

…on that item, records the shape it needs and why, and continues with the
rest of its tiers. The unifier decides.

## Ownership (binding; byte-identical in every order of this round)

| Lane | Owns (may create/edit) | Must NOT touch |
|---|---|---|
| **P4.D225** — #73 + #74 server (the refusal substrate + the ledger), from `main` | Everything in §R.10(a)–(d) the chain owns, at the BOTTOM of the chain: `services/dangerous_content/**` (NEW `refusal.rs`, `understudy.rs`, `image_failover.rs`, `refusal_ledger.rs`; `provider_routing.rs`, `manual_flip.rs`, `resolver.rs`), `services/{provider_failover,route_trail,concierge_notifications,lantern_notifications,image_job_common,character_avatar_job,story_background_job,tool_execution,cheap_llm_exec}.rs`, `llm_fallback/**`, `finish_reason.rs`, `model/**`, `tools/generate_image.rs` + `tools/executor.rs`, `db/chats.rs`, `db/chat_settings.rs`, NEW `db/chats_moderation_refusal_ledger_repair.rs`, `write_apply.rs`, `api/{images,settings}.rs`, `services/provisioning/chat_settings_seed.json` (+ the register), `generators/qtap-export.schema.json` + `qtap_export/schema-key-order.json`; `crates/quilltap-web/src/images_routes.rs`; `crates/quilltap-host/src/{host,images_generate}.rs` + NEW `tests/host_boot_p4d225_columns.rs`; the harness families in its §H + three NEW families + NEW `moderation_refusal_ledger_isolation_guard`; the oracle cases + fixtures those name; `harness/oracle/fixtures/migrate-memories-fixture-columns.ts` (the module-running arm); the four pairs it widens; `image-dialects`, `response-bodies`, the four #73 stream files; this order's header | `services/scenario_builder/**`, `db/tiered_mount_pool.rs`, `api/scenario_builder.rs` (P4.D231); `request-envelopes.recorded.ndjson`, the guard's constants, the manifests, the four untouched stream files (P4.D232); `apps/web/**`; `help/**`; `fresh_schema.json` (P4.D226 re-dumps); the drift ledger |
| **P4.D226** — #75 server (the three states), stacked on P4.D225 | The chain's files, sequentially on top of P4.D225: `services/dangerous_content/{chat_override,manual_flip,resolver,gatekeeper_job}.rs` + NEW `classifier_switch.rs`, `current_state.rs`; `services/{chat_create,chat_enrichment,concierge_notifications,danger_scan,initial_greeting,home,...}.rs`; `db/chats.rs` + `db/chats_read.rs` (`ALL_COLUMNS` gains the trio; `patch_only_fields`; `set_concierge_mode`; `set_danger_classification`); `db/base` twins; `api/{salon,types,engine,projects,characters}.rs` (the GET/PUT/POST/list/create shapes); `services/backup/restore/**`, `services/quilltap_import/**`, the export schema (`conciergeOverride` deprecated); `services/provisioning/fresh_schema.json` (the D23 RE-DUMP at its pin + the register + the E.1 anomaly measured); the web chat edges; `crates/quilltap-host/tests/{host_boot,host_cadence}.rs` (the DDL mirrors); the widening of ALL 42 chats-bearing pairs (migrations 1 + 2) + the vintage trio rebuilt; every family in its list; `harness/oracle/cases/concierge-presentation.mjs` is P4.D229's — NOT yours; this order's header | P4.D225's frozen commits (append only); `services/scenario_builder/**` etc. (P4.D231); the provider corpora + the guard (P4.D225/P4.D232); `apps/web/**`; `help/**`; the drift ledger |
| **P4.D227** — #76 server (the Concierge settings + the DROP + the consumer sweep), stacked on P4.D226 | Sequentially on top: `services/dangerous_content/resolver.rs` (the `resolve_concierge_settings` / `read_concierge_settings` policy) + NEW `legacy_concierge_settings.rs`; `db/chat_settings.rs` (+ `conciergeSettings`; the legacy keys STRIPPED on read; the six-sites guard); `api/settings.rs` (the retired-key 400; the whole-object replace); `services/provisioning/{fresh_schema.json,chat_settings_seed.json}` (the second RE-DUMP: `conciergeSettings`, the DROP, the seed); the ~40 consumer sites (`almanack/**`, `cheap_llm*`, `memory_processor.rs`, `pascal/llm_consult.rs`, `photos/auto_describe*`, `appearance_resolution.rs`, `file_fallback.rs`, `context_summary*`, `compression.rs`, scene-state, title-update, carina/memory extraction, `answer_confirmation*`, `build_context.rs`, memory trigger, message finalizer, `orchestrator.rs`, pre-compute, primary stream, `provider_failover.rs`, `gatekeeper*`, `story_background_job.rs`, `generate_image.rs`, doc-edit photo handlers, `text_block_prompt.rs`); `tools/help.rs` + `tools/definitions/data.rs` (the `concierge` category + the URL bytes); `services/backup/uuid_remap.rs` + restore (the legacy translation + the four desk ids); `db/chats_read.rs` (`conciergeOverride` OUT of `ALL_COLUMNS`); `crates/quilltap-web/src/settings_routes.rs`; `crates/quilltap-host/tests/host_llm_log_cleanup.rs`; the widening of the 42 + 23 pairs (migrations 3 + 4) + the vintage trio rebuilt; every family in its list; this order's header | P4.D225/P4.D226's frozen commits; P4.D231's files; the corpora + the guard; `apps/web/**`; `help/**`; the drift ledger |
| **P4.D228** — #77 server (the Salon polish) + the WHOLE `help/` tree at `08c49319d`, stacked on P4.D227 | Sequentially on top: NEW `services/dangerous_content/retry_uncensored.rs`; `services/{regenerate_swipe,lantern_notifications,story_background_job,tool_execution,queue_service,orchestrator}.rs`; `services/dangerous_content/{resolver,image_failover}.rs` (+13 / +16); `tools/generate_image.rs` (+9); `api/{salon,types,engine,chat_media}.rs` (the two verbs); `crates/quilltap-web/src/{wardrobe_routes,lib}.rs` + the wire tests; `crates/quilltap-host/src/spine.rs` (the override + the sync image seam); **`help/**` WHOLE** + `help_tree_embed_guard.rs` / `host_help_docs_boot.rs` (run UNMOVED); NEW `retry_uncensored_tier3_equivalence` + its fixture/builder; every family in its list; this order's header | the frozen chain below it; P4.D231's files (its four help pages are IN your tree copy — copy, don't edit); the corpora + the guard; `apps/web/**`; the drift ledger |
| **P4.D229** — the Concierge SPA, Salon side, from `main` | §R.10(f)'s P4.D229 list; **`core/core-contract.ts` (OWNER)** preserving P4.D230's and P4.D231's marked hunks; `apps/web/package.json` (version only); this order's header | `screens/settings/**`, `help/**` (the SPA's), `apps/web/oracle/help-guide-capture.test.tsx` (P4.D230); `scenario-builder/**`, `screens/scenarios/**`, `screens/groups/**`, `project-scenarios-card.ts` (P4.D231); every crate; the drift ledger |
| **P4.D230** — the Concierge SPA, Settings side, from `main` | §R.10(f)'s P4.D230 list; the ONE `core-contract.ts` hunk (§S.2, marked); `apps/web/package.json` (version only); this order's header | every other `apps/web/**` file; `_chat.css` (P4.D229 deletes the `-info` rules); every crate; the drift ledger |
| **P4.D231** — the Scenario Builder shelves (server + SPA), from `main` | `services/scenario_builder/{mod,mount_pool,request_schema}.rs`, `db/tiered_mount_pool.rs`, `api/scenario_builder.rs`; the five families + their cases/specs (a SECOND group PLANTED in the mount-pool/tier3 specs; a `groups` row planted in the routes spec if absent); §R.10(f)'s P4.D231 SPA list + the ONE `core-contract.ts` hunk (marked); NEW e2e `scenarios-shelf-builder-flow.spec.ts`; `apps/web/package.json` (version only); this order's header | `help/**` (P4.D228); every Concierge file (the chain); `api/types.rs` (nothing to add); every other `apps/web/**` file; every committed pair; the drift ledger |
| **P4.D232** — the SDK-bump regen + the riders, from `main` | `crates/quilltap-harness/tests/provider_sdk_version_guard.rs` (the constants + prose + the optional Node arm); `harness/oracle/fixtures/request-envelopes/request-envelopes.recorded.ndjson` (re-recorded); `crates/quilltap-harness/tests/openrouter_sdk_pricing_equivalence.rs` (the header); the manifests / the four untouched stream files / `google-wire` — RE-RUN ONLY (byte-identical expected); this order's header | EVERY `crates/*/src/**` file; `image-dialects`, `response-bodies`, the four #73 stream files (P4.D225); `apps/web/**`; `help/**`; `docs/v4/**`; the drift ledger |
| **Everyone** | `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md` — APPEND ONLY; own crate `Cargo.toml` / `apps/web/package.json` version lines (§R.8); `Cargo.lock` as cargo rewrites it | Another lane's files, even for a one-line fix, beyond the pre-declared spills in §R.10(i) — record the finding in the lane record and the status log for the unifier instead; in the chain, the lower lane's commits are frozen (§R.12). `docs/developer/porting/drift-ledger.md`, `phase-4.md`, `dogfood-findings.md`, `docs/v4/**` are never written by a lane. |

## Verification gate (binding; byte-identical in every order of this round — run before declaring the lane done)

Run from the lane's worktree with `CARGO_INCREMENTAL=0` and `TZ=UTC`; ONE
logged, sentinel-guarded chain per long step (`run_in_background: true`,
wait for the completion notification; NEVER a `pgrep`/`grep` poll loop;
NEVER `| tail -N` on a gate; capture the FULL log and read the per-binary
`test result:` lines — see CLAUDE.md's standing rule and the
`background-gate-latency-fix` note). Budget 50–70 GB of disk per Rust lane
(`df -h /` showed 114 GB free at planning) — **at most TWO Rust lanes'
full gates concurrently** (§R.12) and reclaim `target/debug/incremental`
between waves; the two SPA lanes cost no cargo disk.

1. **§R.2 probe** — PASS (opening, and again before every regen batch).
2. `cargo fmt --all --check` — clean (Rust lanes).
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean in BOTH
   feature sets (default; `--features quilltap-core/native-transport`)
   (Rust lanes).
4. **The lane's own differentials by NAME**, each regenerated fresh from
   the lane's OWN target pin (§R.3), run with `--nocapture`, the log
   grepped for `SKIP:` (ZERO), the fresh NDJSON grepped for the changed
   bytes (> 0 where the order predicts a change; a recorded `0` where it
   predicts none), and the red-first steps recorded with the pre-fix
   counts at the lane's "before" pin.
5. **The neutrality legs** the order names, regenerated at BOTH pins and
   compared (`cmp` / by-label diff) where the family is deterministic
   across regens; where it is not (`regenerate-at-both-pins-and-cmp-is-
   not-universal`), compare the normalized rows by case and record. **A
   stacked lane's first act (§R.12) is this step over the lower lane's
   whole list.**
6. **Mutation proofs** — every proof the order names, each reddening
   exactly the test it targets, with the revert done by file backup
   (`mutation-proof-revert-by-file-backup`), never `git checkout <file>`.
7. `cargo build --workspace --release` — clean (Rust lanes).
8. `cargo test --workspace --no-fail-fast -- --nocapture` with the lane's
   env block (its own families' vars; every other family's var WITHHELD so
   it SKIPs honestly rather than failing on a deleted `/tmp` path): every
   binary's `test result:` line read, zero failures bar the standing reds
   §R.5 names (recorded by name if their vars are in the block — the guard
   for P4.D225/P4.D232, the `/tmp/qt-imggen-*` collision), the lane's
   families confirmed RUN by non-zero duration (Rust lanes). **P4.D226 and
   P4.D227 ALSO run the FULL sweep** (`recipe_sweep.py --run-all --v4
   "$PIN"`) from their pins — every reader of a widened pair, with the
   collision check first (`--collisions`) and the result file committed
   under `harness/tools/sweep-results/`.
9. **Source censuses and guards** the change moves or must not move, re-run
   by name with any constant recounted and the arithmetic in the comment:
   `help_tree_embed_guard` + `host_help_docs_boot` (everyone — UNMOVED at
   129; P4.D228 proves the tree by `help_tree_equivalence`); `dispatch_
   wrong_type_census` (the chain — measured each lane; P4.D231/P4.D232
   UNMOVED); `web_edge_action_sites_census` + `web_edge_body_parse_guard`
   (P4.D228, P4.D231 — UNMOVED predicted); `chat_settings_column_sites_
   guard` (P4.D225, P4.D227); `qtap_schema_embed_guard` + `public_schemas_
   vendor_guard` (P4.D225 moves the first; P4.D226 again; everyone else
   UNMOVED); `zod_version_guard` (everyone — 4.6.5); `provider_sdk_version_
   guard` (§S.4 — the designed reds by name); `blob_write_sites_census` +
   `compressed_column_write_sites_census` (every Rust lane — UNCHANGED);
   `get_messages_caller_census` (P4.D228 — its picture arm reads messages;
   recount); the NEW `moderation_refusal_ledger_isolation_guard` +
   `transcript_version_isolation_guard` (P4.D225, then the chain);
   `spelling_guard` (everyone — the project is Quilltap).
10. **Tier R** (`crates/quilltap-cli/tests/cli_differential.rs`) at the
    round-target pin for P4.D232 (266/0 expected — run it, do not assert
    it) and for any chain lane that touches a core file the CLI links
    (`db/chats.rs` is — P4.D225/P4.D226 say what they measured).
11. **The SPA gate** (P4.D229, P4.D230, P4.D231): `npm run lint` clean;
    `npm test` (vitest — NEVER `ng test`, which never exits; `--filter`
    matches TEST NAMES) with the count recorded (base at planning: 452
    files / 7,919); `npm run build` clean (the ONLY real typecheck —
    `spa-tsc-does-not-typecheck-app-sources`); the `qt-*` class guard;
    Playwright by FILE, one invocation at a time on 4319, for every spec
    the lane touched or added (the gated beats SKIP by design — record the
    skip count; base at planning: full suite 340 passed / 1 failed [the
    P4.66 bubble intermittent, green alone] / 6 skipped [the standing
    parks]) — the full suite and the gate flips are the UNIFIER's.
12. **Commit per `.claude/commands/commit.md`** (one unit per commit where
    the order tiers them; CHANGELOG entry + version bumps per §R.8; the
    status-log lane record appended with the regen commands AS RUN, the
    mutation table, the pre-fix counts, the measurements, the mirror
    byte-count pre-list (§R.9), and every finding outside the lane's
    ownership named for the unifier or, in the chain, for the lane above).
    The lane closes by updating its own order's status header with what
    landed, what was deferred, what it measured that the order got wrong,
    and — for P4.D225/P4.D226/P4.D227 — the words **LANE COMPLETE** that
    release the lane stacked on it.
