# Work order P4.d16 — the `8d86847a` workspace deep-links drift re-port (salon-list tab, drill-in payloads, the intent-layer catch-up, the corpus recapture)

> **STATUS: OPEN** (planned 2026-07-22). No units landed yet. The unifier
> updates this header.

Drift-catch-up round, lane A of two (sibling: **P4.d17**, the thinking-
indicator + Madman's Box theme re-port — file ownership is disjoint; see the
Shared contract). This round absorbs the WHOLE outstanding v4 drift — the four
commits `ab0f175e` + `deab0e5d` + `8d86847a` + `e646f58b` — and **the oracle
baseline MOVES to `e646f58b` (v4 HEAD) at unification.**

Run `git log e646f58b..HEAD --oneline` in `~/source/quilltap-server` **at lane
start; if HEAD moved, STOP and report.** The v4 checkout was verified CLEAN at
`e646f58b` at planning (2026-07-22); if it is dirty at lane start, regenerate
the corpus from a pinned detached worktree per the
`oracle-regen-pinned-v4-worktree` memory note.

Work on a lane branch (`lane/p4.d16-workspace-deeplinks`) — normal carryout
rules apply. `docs/CHANGELOG.md` and `docs/developer/porting/status-log.md`
are append-only (they union-merge at unification).

**This lane is SPA + workspace corpus + docs only. Zero Rust.** If a change
seems to require touching `crates/**`, STOP and report — the v4 commit's
diffstat contains no `lib/` file any Rust family imports (verified at the
P4.6bj unification, status-log).

## Mandate

Absorb v4 `8d86847a` ("deep links that escaped the tabbed workspace now open
as tabs") into the v5 workspace: the new **`salon-list` tab kind** (the
all-chats list hosted in the workspace; the rail's Chats item and the home
links stop unmounting the workspace), **drill-in payloads** for the
Prospero/Scriptorium/Aurora singleton tabs (`projectId`/`storeId`/`groupId` —
detail deep links open the list tab drilled into the target),
**`character-view` joins the `?open=` intent layer** (v5 currently excludes it
BY SPEC — that exclusion flips), the **terminal-popout funnel** (the popout
route opens the Salon tab plus a child terminal tab instead of the standalone
screen), and the **/salon/new intent** adapted to v5's standing no-modal
divergence. The captured workspace corpus **regenerates at `e646f58b`** and
the replay differential extends to cover every new v4 behavior.

**Must NOT touch:** anything in P4.d17's ownership (the chat streaming
indicator, `ui/icon.ts`, `_icons.css`, the `_chat.css` thinking section,
`public/themes/**`, `public/images/icons/**`), any Rust source, the on-disk
schema, the `qt-rich-editor`.

## Survey-verified starting points (surveyed 2026-07-22; v4 at `e646f58b`, v5 at `91959254`)

### The v4 commit (`git show 8d86847a`, 32 files — lib/ delta is small and exact)

- **`lib/workspace/types.ts`**: `TabKind` gains `'salon-list'` (comment: "the
  all-chats list (`/salon` without a conversation)"); three NEW optional
  drill-in payload interfaces — `ProsperoTabPayload { projectId?: string }`,
  `ScriptoriumTabPayload { storeId?: string }`, `AuroraTabPayload { groupId?:
  string }` — with the why-comment: "A deep link (e.g. `/prospero/<id>`) opens
  the singleton tab with the target preselected; the view renders that detail
  in place. Absent payload = the plain list."
- **`lib/workspace/tab-meta.ts`**: `'salon-list': { title: 'Chats', icon:
  'chat' }` inserted after `salon`.
- **`lib/workspace/workspace-persistence.ts`**: `TAB_KINDS` gains
  `'salon-list'` (same position).
- **`lib/navigation/route-to-intent.ts`** (`parseHrefToIntent`):
  - NEW drill matches (before the character-edit match): `/prospero/<id>` or
    legacy `/projects/<id>` → `{ kind: 'prospero', payload: { projectId } }`;
    `/scriptorium/<id>` → `{ kind: 'scriptorium', payload: { storeId } }`;
    `/aurora/groups/<id>` → `{ kind: 'aurora', payload: { groupId } }`.
  - The path switch gains `case '/salon': case '/chats': return { kind:
    'salon-list' }`.
  - The header comment changes: null is now only for "new-chat — a modal,
    handled by the interceptor — and external links". **`/salon/new` still
    returns null** — the corpus pins that.
- **`components/workspace/WorkspaceIntent.tsx`** (the `?open=` layer):
  - `OPENABLE_KINDS` gains `'salon-list'` and `'character-view'`.
  - NEW first branch: `open === 'new-chat'` pops the NewChatModal with
    `{ projectId?, characterId?, autonomous: searchParams.get('autonomous')
    === '1' }`, then `router.replace('/workspace')` and returns.
  - Payload extraction: `character-view` shares the `character-edit` arm
    (`{ characterId, tab }`); NEW arms for `prospero` (`projectId`),
    `scriptorium` (`storeId`), `aurora` (`groupId`).
  - `missingCharacterId` guard extends to `character-view`.
  - **The terminal two-step**: for `kind === 'terminal' && chatId`, open the
    salon parent FIRST (`const salonTabId = openTab('salon', { chatId })`),
    then `openTab('terminal', { chatId, sessionId }, { parentTabId:
    salonTabId })` — why-comment: "A terminal tab is a portal fed by its Salon
    view — open (and mount) the conversation first, then the terminal as its
    child tab."
  - `character-view` + `?action=chat`: after opening the tab, ALSO pop the
    new-chat modal with the character preselected ("legacy-page parity").
- **`components/workspace/TabView.tsx`**: `salon-list` → `<SalonListView />`;
  the three list views take `initialGroupId` / `initialProjectId` /
  `initialStoreId` from the (optional) payload.
- **Page-level**: `/salon` calls `redirectToWorkspaceTab('salon-list')`;
  `/salon/new`, `/prospero/[id]`, `/scriptorium/[id]`, `/aurora/groups/[id]`,
  `/aurora/[id]/view`, and the terminal popout split into server redirect
  wrappers + `*PageClient` bodies (a Next-ism — see tier 3); bare
  `/aurora/[id]` becomes a server-side redirect to `/view`.
- **v4 test deltas to port case-for-case**:
  `__tests__/unit/lib/workspace/route-to-intent.test.ts` (+27: the salon-list
  mapping incl. legacy `/chats`, the not-mapped new-chat case, the three
  drill-detail mappings incl. legacy `/projects/p1`) and
  `__tests__/components/workspace/workspace-intent.test.tsx` (+73: the
  new-chat modal branch, drill params, character-view + action=chat, the
  terminal two-step).

### The v5 side (all under `apps/web` unless noted)

- **Contract**: `src/app/workspace/workspace-contract.ts` — the 22-kind
  `TabKind` union at lines 28–50; payload interfaces 52–121
  (`CharacterViewTabPayload` 106–110). No drill payloads exist.
- **Core**: `src/app/workspace/core/reducer.ts` (`tabIdentity()` 100–122 —
  prospero/scriptorium/aurora are singletons keyed by kind alone, default arm
  119–120); `core/persistence.ts` (`TAB_KINDS` allowlist 47–70, `satisfies
  readonly TabKind[]`); `core/tab-meta.ts` (`DEFAULT_TAB_META` 31–54);
  `core/route-to-intent.ts` (salon 48–53 with `new` → null at 51;
  character-view `/…/view` 66–73; **v5-only bare `/characters/:id` →
  character-view** 80–85; the path switch 87–132).
- **The corpus**: committed at
  `src/app/workspace/core/__fixtures__/workspace-core-fixtures.json` (6,273
  lines, `_meta.baseline: "b8b12695"` — THIS LANE moves it to `e646f58b`).
  Generator: `harness/oracle/cases/workspace-core.ts` (repo root) — imports
  v4's REAL `lib/workspace/{workspace-reducer,workspace-persistence,tab-meta}`
  + `lib/navigation/route-to-intent`. Regen command in the replay-spec header
  (`core/workspace-core.spec.ts:13–16`). Replay spec sections: reducer 44
  scenarios / 92 step-assertions, tabIdentity 17, persistence
  (deserialize 12 / prune 8 / hydrate 3 / roundTrip 3), routeIntent 44,
  tabMeta byte-for-byte 1; v5-only `/characters*` additions live OUTSIDE the
  corpus at spec lines 163–193 — that is the precedent for every v5-only
  behavior this lane adds.
- **The intent layer**: `src/app/workspace/chrome/workspace-intent.ts` —
  `OPENABLE_KINDS` 15–37 **deliberately omits `character-view`**, and
  `chrome/workspace-intent.spec.ts:18–20` ASSERTS the omission ("character-view
  is not openable"). v4 `8d86847a` flips this — the spec assertion inverts.
- **Host + registry**: `chrome/workspace-host.ts` (providers 65–71, `?open=`
  intent after hydration via `parseOpenIntent` 187/220); `chrome/tab-view.ts`;
  **`chrome/tab-registry.ts` `DEFAULT_TAB_REGISTRY` 77–154** — the single
  kind→component+inputs table (`character-view`→`CharacterDetail` 127–133); a
  new kind needs an entry here + contract + persistence + tab-meta (+ reducer
  identity if de-duped).
- **Guards**: `src/app/workspace/workspace-redirect.guard.ts`
  (`workspaceRedirectGuard(open, params?, bypass?)` 42–59), wired in
  `src/app/app.routes.ts` — 16 guarded routes today. **Un-redirected (the
  deep-link gaps this lane closes):** `/salon` (list, 37–40), `/salon/new`
  (41–44), `/salon/:id/terminal/:sessionId` (45–48), `/characters/groups/:id`
  (67–70), `/characters/:id` (82–86), `/prospero/:id` (97–101),
  `/scriptorium/:id` (113–116). The docstring at 27–34 names these exclusions
  "v4-faithful" — that claim is now STALE and the docstring must be rewritten.
- **Shell/home**: `src/app/shell/shell.ts` — the Chats `NAV_ITEMS` entry
  routes to `/salon` (line 78), which unmounts the workspace today. Home
  links: `screens/home/recent-chats-section.ts:29` (`/salon`), `:35`
  (`/salon/new`); `quick-actions-row.ts:27,35,47`; `recent-chat-item.ts:27`;
  `home-character-card.ts:20–21` (Chat → `/salon/new?characterId=` — the
  standing P4.6q divergence).
- **The standing no-modal divergence**: v5 has NO NewChatModal. New Chat is a
  full screen (`NewChatPage` at `/salon/new`); `?action=chat` arms navigate to
  `/salon/new?characterId=` (ruled in `p4.9j3-wardrobe-tab-riders.md`,
  preamble + item 2). v4's modal arms must be translated, not copied — see
  unit 5.
- **e2e**: `e2e/workspace-flow.spec.ts` (9 beats, incl. the cross-theme accent
  walk at 246). Related: `new-chat-flow.spec.ts`, `terminal-flow.spec.ts`.

## Deliverables

### Tier 1 — must land

1. **The corpus recapture + core adoption.** Adopt `salon-list` +
   the three drill payload interfaces into `workspace-contract.ts`,
   `core/persistence.ts` TAB_KINDS, `core/tab-meta.ts`, and
   `core/route-to-intent.ts` (the three drill matches + the `/salon`+`/chats`
   switch cases + the header-comment change), carrying v4's why-comments.
   Extend the generator's scripted inputs
   (`harness/oracle/cases/workspace-core.ts`) with v4's new behaviors —
   route-intent hrefs `/salon`, `/chats`, `/salon/new` (pins null),
   `/prospero/p1`, `/projects/p1`, `/scriptorium/s1`, `/aurora/groups/g1`;
   tab-meta and persistence pick up `salon-list` automatically if the
   generator enumerates from v4's tables (verify — extend scenarios if the
   corpus scripts kinds by hand). Regenerate the corpus at `e646f58b`
   (`_meta.baseline` moves), extend the replay spec, and port v4's +27
   route-to-intent test cases case-for-case. v5-only additions (the
   `/characters/groups/:id` drill alias mirroring the v4-only
   `/aurora/groups/:id` path) go OUTSIDE the corpus, in the spec's v5-only
   section (lines 163–193 precedent). **`tabIdentity` note:** v4's reducer
   was NOT touched by the drift — prospero/scriptorium/aurora stay singletons
   keyed by kind; a drill-in re-USES the open tab and re-targets its payload.
   Verify how v4's reducer/openTab handles the payload change on an existing
   singleton (read `lib/workspace/workspace-reducer.ts` at `e646f58b`) and
   mirror it exactly; add corpus scenarios covering open-then-redrill if the
   current scripts don't.
2. **The intent layer.** `chrome/workspace-intent.ts`: OPENABLE gains
   `salon-list` + `character-view` (FLIP the exclusion assertion in
   `workspace-intent.spec.ts`); the drill param arms (`projectId` / `storeId`
   / `groupId`); `character-view` payload `{ characterId, tab }` + the
   missing-characterId guard; the terminal two-step (salon parent first, then
   the terminal child with `parentTabId`) with v4's portal why-comment. Port
   v4's +73 intent-layer test cases case-for-case into the spec (translated
   to the v5 harness idiom).
3. **Guards + interception.** New guards in `app.routes.ts`: `/salon` →
   `open=salon-list`; `/salon/:id/terminal/:sessionId` →
   `open=terminal&chatId&sessionId`; `/prospero/:id` →
   `open=prospero&projectId`; `/scriptorium/:id` → `open=scriptorium&storeId`;
   `/characters/groups/:id` → `open=aurora&groupId`; `/characters/:id` →
   `open=character-view&characterId` (with `?tab=` preserved and `?action=`
   forwarded). Rewrite the stale "v4-faithful exclusions" docstring. The
   in-workspace link interceptor needs no new wiring for the drill routes
   (parseHrefToIntent covers them once unit 1 lands) — verify the Chats rail
   item and home `/salon` links now open the salon-list tab when inside the
   workspace, and stay plain navigations outside it.
4. **Drill-in hosting.** `chrome/tab-registry.ts`: the `salon-list` entry
   (hosting the existing `SalonList` screen component) and inputs factories
   passing the drill ids into the three list screens; the prospero /
   scriptorium / characters-roster screens gain an optional initial-drill
   input wired to their EXISTING drill state (drilling is already
   state-not-navigation in v5 — `project-card.ts:47` et al.). Mirror v4's
   `TabView.tsx` prop names in spirit (`initialProjectId` / `initialStoreId`
   / `initialGroupId`).
5. **e2e.** Extend `workspace-flow.spec.ts` (or a sibling spec this lane
   owns): (a) the Chats rail click inside the workspace opens a salon-list
   tab — the workspace does NOT unmount; (b) a `/prospero/:id` deep link
   lands drilled into that project inside a Prospero tab; (c) the terminal
   popout deep link opens the Salon tab + child terminal tab (extend or
   coordinate with `terminal-flow.spec.ts` — this lane owns both).

### Tier 2 — should land

6. **The `/salon/new` funnel (the v5 no-modal translation).** v4 pops the
   NewChatModal via `open=new-chat`; v5 has no modal. Translation: a
   **v5-only `salon-new` tab kind** hosting the existing `NewChatPage` screen
   (registry entry + contract + tab-meta `{ title: 'New Chat', icon: 'chat' }`
   + persistence), a guard `/salon/new` → `open=salon-new` forwarding
   `characterId`/`projectId`/`autonomous`, and an interceptor special-case
   for in-workspace `/salon/new` links. **Do NOT add `/salon/new` to
   `parseHrefToIntent`** — the corpus pins its null return; the special case
   lives in the interceptor exactly as v4's comment describes ("new-chat —
   a modal, handled by the interceptor"). `character-view` + `?action=chat`
   opens the salon-new tab with `{ characterId }` (the v4 modal-pop arm's
   translation). On chat creation, `NewChatPage` navigates to `/salon/:id` —
   inside the workspace that becomes a salon tab via interception; the
   salon-new tab should self-close (the `useCloseSelfTab` idiom is ported —
   P4.9J2). Document all of this as an EXTENSION of the standing no-modal
   divergence in the lane record + the m6 parity file's workspace row.
   If this unit fights back, defer it LOUDLY (named in the status header)
   rather than shipping half — units 1–5 do not depend on it.

### Tier 3 — named NO-PORTS (record in the lane record; do not build)

- The `*PageClient` server-redirect wrapper split and
  `NEXT_PUBLIC_WORKSPACE_TABS=0` legacy rendering — Next-isms; v5's guard
  factory + flag bypass IS that mechanism (guards already no-op when the
  workspace flag is off — verify the new guards inherit it).
- v4's bare `/aurora/[id]` server-side redirect to `/view` — v5's bare
  `/characters/:id` renders/opens the detail directly (same outcome; the
  v5-only route-to-intent arm at `core/route-to-intent.ts:80–85` already
  covers in-workspace links).
- `help/tabbed-workspace.md` — no v5 help surface; banked for `p4.9i2`.
- `docs/developer/features/tabbed-workspace.md` — refresh the MIRROR copy
  under `docs/v4/developer/features/` only.

## The differential requirement

This is a pure-TS-client lane: the tier-1 form is the **captured corpus**
(P4.9J1 precedent — the order of record for the mechanism). The oracle case
`harness/oracle/cases/workspace-core.ts` imports v4's REAL lib (never
reimplements), emits the committed JSON corpus at `e646f58b`, and the replay
spec drives every row through the v5 port asserting deep/exact equality. Every
NEW v4 behavior this lane ports must be pinned by a corpus row (or, where the
behavior lives above the pure core — the intent layer — by a case-for-case
port of v4's own test delta). v5-only additions NEVER go in the corpus; they
get spec rows in the marked v5-only section. On regen, grep the emitted JSON
for the new rows before trusting a green replay (`oracle-regen-silent-stale-
pass` memory note: an erroring generator leaves the old fixture in place).

## Fixtures

- **Consumes/regenerates**: `workspace-core-fixtures.json` (this lane OWNS the
  regen and the `_meta.baseline` move to `e646f58b`).
- **No DB fixtures, no Rust oracle families.** The P4.6bj unification verified
  by import that no Rust-side family touches the drifted files.

## Shared contract (BINDING — identical in `p4.d17-thinking-indicator-theme-drift.md`)

- **The round**: P4.d16 ∥ P4.d17, both SPA-only, zero Rust. At unification the
  oracle baseline moves `8bf3cb5f` → `e646f58b` and the four-commit drift is
  fully absorbed (`e646f58b` itself is a v4-internal lint chore — NO-PORT).
- **Ownership (no file appears in both lanes):**
  - **P4.d16 owns**: `apps/web/src/app/workspace/**`,
    `apps/web/src/app/app.routes.ts`, `apps/web/src/app/shell/shell.ts`,
    drill-in inputs on the prospero/scriptorium/characters list screens and
    the salon-list/new-chat screen components' tab hosting,
    `harness/oracle/cases/workspace-core.ts` + the corpus fixture,
    `apps/web/e2e/workspace-flow.spec.ts` + `terminal-flow.spec.ts` +
    `new-chat-flow.spec.ts`, `docs/developer/porting/m6-screen-parity.md`.
  - **P4.d17 owns**: `apps/web/src/app/chat/streaming-message.ts` (+spec), the
    new indicator component under `apps/web/src/app/chat/`,
    `apps/web/src/app/ui/icon.ts`,
    `apps/web/src/styles/qt-components/_icons.css`, the `_chat.css` thinking/
    response-status section + `_variables.css` token additions,
    `apps/web/public/images/icons/**`, `apps/web/public/themes/**`,
    `apps/web/e2e/foundation.spec.ts` and any new indicator/theme spec.
  - Append-only for both: `docs/CHANGELOG.md`, `status-log.md`.
- **Playwright port arbitration**: P4.d16 owns port 4319 and full-suite runs.
  P4.d17 runs its TARGETED specs with locally moved ports
  (`e2e-port-4319-cross-worktree-kill` memory note: 4419/45401, revert before
  commit, never commit the move) and defers its full-suite proof to the
  unifier.
- **Version bumps**: each lane bumps `apps/web/package.json` per commit as
  usual; the unifier RECOUNTS and accumulates (identical bumps merge
  silently — `p4.6aw-ax-8-round-plan` memory note). No crate versions move in
  this round.

## Verification gate (before declaring the lane done)

- Drift re-check: `git log e646f58b..HEAD` in the v4 checkout is empty.
- The regenerated corpus committed; `ng test` full suite green (the replay
  spec + both ported v4 test deltas by name); `ng build` clean.
- Full Playwright from a fresh `ng build` dist against main's release
  binaries (`p4.6bi` memory note), zero unexplained failures; the new beats
  active; the dual-mode (flag-off) harness still green.
- No `crates/**` diff (`git diff --stat main.. -- crates/` empty).
- Per-commit: `.claude/commands/commit.md`. Lane record appended to
  `status-log.md`.
