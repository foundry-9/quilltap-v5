# Work order P4.D144 — the Concierge mark on every chat list + Quick-hide on the uncensored row, SPA half (v4 `c43d3b1b4`, PR #46)

**Status:** CLOSED — UNIFIED on main at the `6d2a50382` drift catch-up round
unification (2026-09-02; lane branch `claude/concierge-list-marks-spa-eb4ae1`).
Tier 1 whole: the presentation table as the ONE string home
(`chat/concierge-state-presentation.ts`, diffed string-for-string against a
COMMITTED recording of v4's module EXECUTED at the sha —
`concierge-state-presentation.v4.json` via `harness/oracle/cases/
concierge-presentation.mjs`, a deliberate strengthening of the order's
scratch-read spec), `ConciergeMark` over the P4.D132 Tooltip (class string
pinned at its SOURCE because Angular's `[class]` binding de-duplicates
tokens — a transcribed v4 assertion silently disarmed), the three card
sites + the fourth list, the header pill and sidebar reads onto the table
with zero visible string change (the Flagged `title` retired),
`shouldHideChat` as the one rule with the P4.9d non-port ruling retired at
source, the four filters, the §A DTOs, the `.qt-concierge-mark` CSS family.
Tier 2 whole: §H's third arm behind `CHATS_HAS_DANGEROUS_VERB_LANDED` + the
footer gate in `shell/user-menu.ts` (an out-of-ownership edit, flagged and
kept), the three ACTIVATE-AT-UNIFY beats in `concierge-marks-flow.spec.ts`.
Both constants were flipped LIVE at unification. Recorded: the bubble's
`Categories` line is unit-pinned only (nothing a walk can reach writes
`dangerCategories`); arm order behaviour-neutral; theme-storybook NO-PORT.
Lane record: `status-log.md` → "Lane record — P4.D144".

## Preamble

- **v4 oracle baseline: `4622411fd`** (drift-ledger §1, probe re-run
  2026-09-02). **This lane's target commit is `c43d3b1b4`** — *"Chat lists
  and Quick-hide follow Concierge state, not raw danger label (#46)"*.
  The plan doc (`docs/developer/features/complete/concierge-list-marks.md`
  at the sha) is rationale; the hunks are the spec (ledger §5.3):
  `git -C ~/source/quilltap-server show c43d3b1b4 -- <path>` and
  `git show c43d3b1b4:<path>` for the new files.
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 checkout
  on `main`, tree clean, `git log 6d2a50382..main` empty and
  `git log 3a76b17df..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED** (ledger §1). This lane is client-only; its
  oracles are v4's jest corpora read at the sha (`git show c43d3b1b4:
  __tests__/…`) and transcribed 1:1 — no worktree pin is needed unless a
  step needs a v4 run (then a lane-unique pin at `c43d3b1b4`, ledger
  §5.1).
- **Sibling lanes (this round):** P4.D143 (this feature's server half —
  §A, §B, §H bind the two; the SPA codes against the §A contract and its
  beats stay gated until unification), P4.D145 (bug 114 folders), P4.D146
  (absent participants + background mode — shares `core-contract.ts` by
  region, §E), P4.D147 (the Move-to-Project folder picker — owns
  Playwright). §Ownership / §Shared contracts are binding and identical
  across all five orders.
- **Worktree:** per the `carryout` skill (`npm ci` in the worktree's
  `apps/web`, never `cp -a`). `CHANGELOG`/`status-log.md` append-only.
  **No Playwright** (P4.D147 owns port 4319) — this lane's beats are
  authored gated (ACTIVATE-AT-UNIFY behind a named constant, e.g.
  `P4D143_LIST_PAYLOAD_LANDED`).

## Mandate

Port the client half of `c43d3b1b4` whole: the presentation table as the
ONE home of every word, icon and tone the four states wear (the strings
v5 currently holds inline in the sidebar Chat section and the header
pill move there verbatim — §B), `ConciergeMark` (the asterisk on all four
lists, three tones, explained through the P4.D132 Tooltip and never a
native `title`), the header pill and the sidebar helper text re-pointed
at the table (the Flagged pill's native `title` retired as v4 retires
it), `shouldHideChat` as THE quick-hide rule in one place (its recorded
non-port ruling is now obsolete — retire the comment explicitly), the
four inline filters routed through it, the `.qt-concierge-mark` CSS
family, the §A DTO changes, and the specs transcribed 1:1 from v4's four
new/changed jest files. Must NOT touch: any Rust crate (P4.D143's), the
project Image Generation card or the `backgroundDisplayMode` unions
(P4.D146's §E region), the files screens (P4.D147's), the sidebar
`chat-section.ts` control's PUT/toast machinery (only its helper-text
and icon reads move onto the table), or `salon-conversation.ts`.

## Survey-verified starting points (2026-09-02, from the shipped hunks — ledger §5.3)

1. **The presentation module** (v4 NEW `lib/services/dangerous-content/
   concierge-state-presentation.ts`, 142 lines, client-safe): exports
   `ConciergeTone = 'danger'|'muted'|'info'|'success'`,
   `ConciergeStatePresentation {label, icon, tone, detail, hint}`,
   `CONCIERGE_STATE_PRESENTATION: Record<ConciergeState, …>`,
   `conciergeToneSuffix(tone) → '' | '-muted' | '-info'` (**`success`
   falls through to `''`** — the plan doc's `-success` never shipped),
   `conciergeToneTextClass(tone)` (`muted→qt-text-muted`, `info→
   qt-text-info`, `success→qt-text-success`, default `qt-text-danger` —
   spelled one branch at a time so `check-qt-classes` sees each name),
   `ConciergeStateDescription {title, detail, categories, hint}`,
   `describeConciergeState(state, dangerCategories?)` (categories only
   for `flagged` AND non-empty, else `null`; key order `title, detail,
   categories, hint`). **The table, byte-load-bearing (§B carries it
   verbatim):** `monitored` {Monitored, eye, success, "The Concierge
   keeps watch, and will flip the switch himself if the conversation
   calls for it."}, `flagged` {Flagged, alert-triangle, danger, "The
   Concierge has this chat down as dangerous, and routes it through the
   uncensored providers."}, `vouched` {Vouched Safe, check-circle, muted,
   "You have vouched for this chat. The Concierge stops watching; the
   ordinary providers still apply, and may still refuse."}, `uncensored`
   {Uncensored, eye-off, info, "You have sent the Concierge away and
   opened the uncensored door yourself. Nothing is scanned, nothing is
   softened — the risk is yours."} (U+2014 em dash); every row's `hint`
   = `Change it from the Salon sidebar's Chat section.` **v5 home:** NEW
   `apps/web/src/app/chat/concierge-state-presentation.ts` beside the
   P4.D141 client twin `chat/concierge-state.ts` (122 lines — zero
   strings today). Where v5's strings live NOW and must move: the four
   `detail` sentences at `chat/sidebar/chat-section.ts:637-649`
   (`conciergeHelperText`, already byte-exact), the icon/tone pairs at
   `:651-663` (`conciergeStateIcon`), the labels at `chat/conversation-
   header.ts:53-80` (per `@switch` arm) and `chat-section.ts:135-167`
   (`<option>` labels stay — they are the control's own copy in v4 too),
   the suffixes spelled per arm in the header. The `hint` sentence does
   not exist in v5 — net-new. v4's `ChatSidebar.tsx:1132-1140` becomes
   two table reads with zero visible string change — mirror that
   ("the strings move verbatim, so any component test asserting them
   keeps passing").
2. **`ConciergeMark`** (v4 NEW `components/chat/ConciergeMark.tsx`, 97
   lines — read it whole): `monitored` renders NOTHING; else
   `<span role="img" aria-label="Concierge: {label}" class="qt-concierge-
   mark[ qt-concierge-mark{suffix}][ {className}]">*</span>` (flagged's
   class is EXACTLY `qt-concierge-mark` — the base rule is the danger
   tone, never emitted twice) wrapped in `<Tooltip content={<Concierge
   TooltipBody …/>} placement="top">` — only `content` + `placement`, no
   `pinnable`, **no `title`**; deliberately not focusable/pinnable
   because it sits inside a link (carry the comment). `ConciergeTooltip
   Body({title, detail, categories, hint})`: `div.qt-tooltip-body >
   p.qt-tooltip-title, p{detail}, [div.qt-tooltip-section > p.qt-tooltip-
   section-label "Categories", p.qt-tooltip-quote {categories.join(', ')}],
   p.qt-tooltip-hint` — all six classes already exist in v5
   (`_surfaces.css:1109-1136`). v5: NEW `chat/concierge-mark.ts`
   (`qt-concierge-mark` selector is the lane's call — mind the guard's
   host-class rule from P4.D142; the CSS class and the element can share
   the name only if a rule exists for the host) using the P4.D132
   primitive `apps/web/src/app/ui/tooltip.ts` (`qt-tooltip`, inputs
   `content`|`contentTemplate`, `placement`, `delay` 200, `pinnable`,
   `interactive`; trigger projected via `<ng-content>`; its doc at
   `:31-33` forbids a `title` on the wrapped control). v4 passes a JSX
   element → v5 passes `[contentTemplate]` with an `<ng-template>`
   rendering the body (the `confirmation-badge.ts:46` idiom).
3. **The three asterisk sites + the fourth list** (v4 `ChatCard.tsx:
   289-297` with `className="text-sm flex-shrink-0"`, `RecentChatItem.
   tsx:70-79` with no className, inlined after `{n} msgs`; the character
   conversations tab inherits `CharacterChatShape`): v5
   `screens/salon/chat-card.ts:93-100`, `screens/home/recent-chat-item.
   ts:46-52`, `screens/characters/view/tabs/character-conversation-card.
   ts:84-91` — identical `<span class="qt-text-destructive text-sm
   flex-shrink-0" title="Flagged as dangerous" aria-label="Flagged as
   dangerous">*</span>` reading `isDangerousChat` raw; hosts `salon-list.
   ts:93`, `project-chats-section.ts:77`, `recent-chats-section.ts:41`,
   `conversations-tab.ts:90`. Render the mark under `@if (chat().
   conciergeState)` — **a payload with NO state renders no mark** (v4's
   `concierge-mark.test.tsx:147` / `homepage-components.test.tsx:549`),
   which is also what keeps this lane green before P4.D143's wire lands.
4. **The header pill** (v4 `SalonView.tsx:1088-1112`): one table lookup;
   `placement="bottom"`; gains `role="img"` + `aria-label="Concierge:
   {label}"`; the four native `title=` strings RETIRED — the Flagged
   `title` (`The Concierge has flagged this chat[: cats]`) is replaced by
   the drawn bubble with a `Categories` section. v5 `chat/conversation-
   header.ts:53-80` (the `@switch`), `dangerTitle()` `:155-160` (retire),
   spec pins at `conversation-header.spec.ts:227, 238-284` (rewrite to
   the new shape, red-first against the `title`). Keep the pill's
   `.qt-danger-badge` class family (`_chat.css:2971-2987`) — the mark
   gets its own family (point 7).
5. **`shouldHideChat`** (v4 `quick-hide-provider.tsx:205-215`, context
   type `:29-35`): `(chat: {characterTags?: Array<string|null|
   undefined>; conciergeState?: ConciergeState}) => boolean` — tag arm
   FIRST (`shouldHideByIds`), then `hideDangerousChats && chat.
   conciergeState && conciergeStateUsesUncensoredRoute(chat.
   conciergeState)`; doc *"THE quick-hide rule for a chat, in one place.
   "Dangerous Chats" hides whatever takes the uncensored route — Flagged
   … and Uncensored … — never a Vouched Safe chat that merely carries a
   preserved label underneath."* v5: `quick-hide/should-hide.ts:23-36`
   is tags-only, and `:8-15` + `quick-hide.service.ts:30-31` RECORD a
   non-port ruling ("v4's `shouldHideChat` read a non-existent
   `chat.isDangerous`, no consumer called it") — **that premise is now
   obsolete; retire the comment, land the rule on the service, and add
   `conciergeStateUsesUncensoredRoute` to `chat/concierge-state.ts`
   with `shouldUseUncensoredRoute` delegating (the §B client twin of
   P4.D143's server change).** The four inline danger filters, all
   reading `isDangerousChat`: `screens/salon/salon-list.ts:178`
   (filter `:165-183`, tags first), `screens/home/recent-chats-section.
   ts:56` (`:54-67`, danger first), `screens/prospero/cards/project-
   chats-section.ts:116` (`:114-127`), `screens/characters/view/tabs/
   conversations-tab.ts:153` (`:149-156`) — each becomes
   `!shouldHideChat({characterTags, conciergeState})` (the arm-order
   change is behaviour-neutral, both arms pure — record it). Tag-only
   consumers pinned by `quick-hide-consumers.spec.ts:14-21` are
   unaffected. **Behaviour deltas to pin red-first:** a vouched chat
   stops being hidden by "Dangerous Chats"; an uncensored chat starts
   being hidden.
6. **The §A DTOs** (`core-contract.ts`): `EnrichedChatSummary`
   `:2577-2594` (`isDangerousChat` `:2588` + `conciergeOverride` `:2590`
   → `conciergeState` + `dangerCategories`), `CharacterChatSummary`
   `:3173-3188` (`:3185`), `RecentChat` (`screens/home/home.api.ts:28`),
   the project chats DTO; `ChatDetail` (`:2905-2908`) KEEPS the raw trio.
   `ConciergeState` wire type already at `:76`. **`concierge-state.ts:
   36-38`'s note that the raw list reads are "v4-FAITHFUL" goes stale
   the moment this lands** — rewrite it.
7. **CSS** (v4 `_chat.css:2848-2869`, inserted right after
   `.qt-danger-badge-info`): `.qt-concierge-mark { --qt-concierge-mark-
   color: var(--color-destructive, #dc2626); @apply inline-block
   font-bold leading-none; color: var(--qt-concierge-mark-color); }`,
   `.qt-concierge-mark-muted { --qt-concierge-mark-color: var(--color-
   muted-foreground, #6b7280); }` (*"Vouched Safe — no colour: the
   Concierge isn't in the room."*), `.qt-concierge-mark-info { … var(--
   color-info, #2563eb) }` (*"Uncensored — the eye you closed
   yourself."*), with v4's four-line header comment. v5 insertion point:
   `apps/web/src/styles/qt-components/_chat.css` after `:2987`, before
   `.qt-badge-silent` `:2992`. The theme-storybook mirror (de-Tailwinded,
   1.0.69) has no v5 twin — recorded non-port. `check-qt-classes` does
   not police a bare `.qt-concierge-mark` on an ordinary element (its
   scope is the four utility families + variant tokens + component host
   classes, `check-qt-classes.mjs:25-48`) — but it DOES police
   `qt-text-*`, hence the one-branch-per-class spelling in point 1.
8. **The footer affordance** (v4 `sidebar-footer.tsx:107/:145`
   `hasQuickHideFeatures` ORs in `useHasDangerousChats`, whose meaning
   moved with §H): v5 mounts `<qt-quick-hide-menu-section />` at
   `shell/user-menu.ts:69` UNGATED — `hasQuickHideFeatures`
   (`quick-hide.service.ts:78-80`) and `hasAnyHidden` (`:68-70`) are
   computed and consumed by NO template, the third arm omitted "rather
   than faked" at `:72-77`. §H: consume P4.D143's `chatsHasDangerous`
   verb in the third arm and gate the section as v4 does (Tier 2,
   gated on the wire).
9. **Vitest specs that assert today's mark and must move:** `screens/
   home/home.spec.ts:236` (`span[title="Flagged as dangerous"]`),
   `screens/salon/salon-list.spec.ts:80`, `screens/characters/view/tabs/
   conversations-tab.spec.ts:121`, `chat/conversation-header.spec.ts:
   227/238-284`, `chat/concierge-state.spec.ts`, `quick-hide/quick-hide-
   consumers.spec.ts`. v4's corpora to transcribe 1:1: `concierge-state-
   presentation.test.ts` (12 cases incl. the `it.each` tables — the
   sentences asserted byte-exact), `concierge-mark.test.tsx` (the class
   string EXACT per state, no `title`, the fake-timer 250 ms dwell
   tooltip cases, the ChatCard no-state arm), `homepage-components.
   test.tsx` (+194: the RecentChatItem mark + tooltip + "still opens the
   chat when the mark itself is clicked" + the section's uncensored-row
   hide), `quick-hide-provider.test.tsx` (five cases), `chat-utils.
   test.ts` (pass-through + "never puts a raw danger label on the
   card"), the `chat-override.test.ts` `it.each(TABLE)` for the client
   twin.
10. **e2e coverage gap:** `quick-hide-flow.spec.ts` drives the TAG arm
    only — the "Dangerous Chats" toggle is never exercised and no list
    danger filtering is asserted anywhere. Author, GATED: (a) a mark beat
    (seed one chat per state through the API; assert the asterisk's
    class per state, no `title`, and the drawn bubble's title + detail +
    the `Categories` line on the flagged one after the dwell — the
    `salon-tooltips-flow.spec.ts` idiom); (b) a "Dangerous Chats" beat
    (toggle on → the flagged and uncensored rows vanish from the Salon
    list AND the home Recent Chats, the vouched one stays; toggle off →
    all back); (c) the header pill's bubble on `placement="bottom"`.
    Existing `salon-concierge-four-state-flow.spec.ts` locates the pill
    by `qt-conversation-header .qt-danger-badge` (`:265-267`) — keep
    that class so the walk stays green.

## Tiered deliverables

- **Tier 1 (must land):** points 1–7 and 9 — the module, the mark, the
  four lists, the pill, the sidebar reads, `shouldHideChat` + the four
  filters, the DTOs, the CSS, every spec transcribed with the red-first
  runs recorded.
- **Tier 2 (should land):** point 8 (§H's client arm, gated on the
  wire), point 10's three gated beats.
- **Tier 3 (explicit refusal-arm deferrals):** none planned. The help
  hunks bank to `p4.9i2`; theme-storybook NO-PORT recorded.

## The differential requirement (restated)

Client-only: the oracle is v4's REAL jest corpora transcribed 1:1 (the
P4.D132 / P4.D86 / P4.D96 client-parity precedent), with the red-first
runs recorded (the `title` assertions, the `isDangerousChat` filters and
the vouched/uncensored hide deltas must each go red against the pre-lane
code). The presentation table is additionally cross-checked mechanically:
a spec that reads v4's module at the sha (`git show c43d3b1b4:lib/
services/dangerous-content/concierge-state-presentation.ts`, staged to
the scratch dir by the recipe) and diffs the four rows string-for-string
against the v5 table — so a copy edit on either side reddens (the
byte-exact-static-data-transcription rule). Mutation proofs: swap a
sentence → the table spec reddens; emit `qt-concierge-mark` twice for
flagged → the class-EXACT spec reddens; put a `title` back on the mark →
the no-title spec reddens; reorder the two arms in `shouldHideChat` →
stays green (record: behaviour-neutral by construction).

## Fixtures

Consumes the standing Playwright fixture (seeds one chat per state via
the API in the gated beats — SQL-seeding `conciergeOverride` is fine, it
is a plain column, not an overlay property). Delivers no committed
fixture.

## Shared contracts (binding, identical across all five orders)

**§A — the list-payload wire (P4.D143 ↔ P4.D144).** Every chat-LIST shape
v4 `c43d3b1b4` touches carries the derived pair **`conciergeState:
'monitored'|'flagged'|'vouched'|'uncensored'`** (the `ConciergeState::
as_str` wire spelling) and **`dangerCategories: string[]`** (always
present — `?? []`), IN PLACE of the raw field(s) it replaces, key order
otherwise untouched:
- `EnrichedChatSummary` (the `chatList` verb / `GET /api/v1/chats`):
  `isDangerousChat` AND `conciergeOverride` both DROPPED → `… project,
  storyBackground, conciergeState, dangerCategories, chatType,
  scriptoriumStatus, _count, _allTagIds`.
- `RecentChat` (the home dashboard): `isDangerousChat` DROPPED → `id,
  title, createdAt, updatedAt, lastMessageAt, conciergeState,
  dangerCategories, storyBackgroundUrl, participants, _count`.
- character conversations rows (`characters/{id}?action=chats`):
  `isDangerousChat` DROPPED → `… tags, conciergeState, dangerCategories,
  _count, scriptoriumStatus`; envelope `{chats, total}` unchanged.
- project chats rows (`projects/{id}?action=chats`): `isDangerousChat`
  DROPPED → `… storyBackground, conciergeState, dangerCategories,
  lastMessageAt, updatedAt, createdAt`; envelope `{chats, pagination}`
  unchanged.
- The single-chat GET (`ChatDetail`) KEEPS `isDangerousChat`,
  `dangerCategories`, `conciergeOverride` — v4's detail view is untouched
  (the sidebar control needs the raw pair).
D143 emits exactly these; D144 types exactly these in `core-contract.ts`
(+ `home.api.ts`'s `RecentChat`). A key present in one lane's record and
absent from the other's is a unification defect; the unifier diffs the
contract name-for-name against the Rust shapes.

**§B — the Concierge presentation table lives ONCE, in the SPA
(P4.D144).** v4's `concierge-state-presentation.ts` has no server
consumer (its readers are ChatSidebar, SalonView and ConciergeMark), so
v5 ports it as `apps/web/src/app/chat/concierge-state-presentation.ts`
only — P4.D143 builds NO Rust twin and emits NO presentation string. The
two lanes share ONE predicate name instead: `conciergeStateUsesUncensored
Route(state)` = `state === 'flagged' || state === 'uncensored'`, landed
as `concierge_state_uses_uncensored_route` in `chat_override.rs` (D143,
`should_use_uncensored_route` delegating) AND in `chat/concierge-state.ts`
(D144, `shouldUseUncensoredRoute` delegating), each pinned by its own
transcription of v4's `it.each(TABLE)`. The four `detail` sentences, the
four labels, the icon names (`eye`, `alert-triangle`, `check-circle`,
`eye-off`), the tones (`success`, `danger`, `muted`, `info`), the suffix
rule (`''`/`-muted`/`-info`, success → `''`), the text classes and the
hint sentence are byte-exact to v4's module and mechanically diffed
against it by a D144 spec.

**§H — the `has-dangerous` probe (P4.D143 ↔ P4.D144), Tier 2 in both.**
v5 never had v4's `GET /api/v1/chats?action=has-dangerous`; v4
`c43d3b1b4` re-based it onto the uncensored route. D143 lands the verb
**`chatsHasDangerous`** (`{ type: 'chatsHasDangerous' }` → `{
hasDangerous: boolean }`, true iff any chat of the user satisfies
`should_use_uncensored_route`) plus the REST edge under `CHAT_GET_
ACTIONS`; D144 consumes it as the third arm of `hasQuickHideFeatures`
and gates the footer's quick-hide section on it exactly as v4's
`sidebar-footer.tsx:145` does, behind the same ACTIVATE-AT-UNIFY
constant as its beats. Until both land, the SPA's third arm stays the
recorded omission; neither lane fakes the other's half.

**§C — the `story_background_job.rs` region split (P4.D145 ↔
P4.D146).** The legacy-folder ensure (`ensure_legacy_folder` `:1425-1449`
+ its private `find_folder_by_path` `:1451-1471`, both DELETED in favour
of the `ensure_by_path` chokepoint) belongs to P4.D145; the back-fill's
participant exclusion set (`:706-728`) belongs to P4.D146. Neither lane
edits the other's region. The unifier cherry-picks **P4.D145 before
P4.D146** and hand-resolves any import-block union. Pin awareness:
D145's pin `a5df98b3f` has `70505745a` as an ancestor, so a D145 regen of
`story_background_job_tier3_equivalence` or `title_update_tier3_
equivalence` carries D146's absent-participant predicate against a v5
that has not ported it — D145 measures the shape FIRST (the committed
story fixture seeds every participant `active`, so the predicate may not
even discriminate) and masks only as a named
`ABSENT_PARTICIPANTS_PENDING_P4D146` tripwire with a shape assertion +
mutation proof (the P4.D141 / `LAST_MESSAGE_AT_PENDING_P4D140`
precedent), retired by the unifier once both lanes sit on one branch.

**§D — `api/projects.rs` region split (P4.D143 ↔ P4.D146).** The
project-chats serializer (the `?action=chats` shape, §A) belongs to
P4.D143; the background-mode twins (`project_background_get`
`:939-1022`, the `PROJECT_UPDATE_SCHEMA` `backgroundDisplayMode` rule
`:398-425`) belong to P4.D146. Neither lane edits the other's region; the
unifier resolves import unions. **The same split applies to
`projects_routes_equivalence` (+ `cases/projects-routes.test.ts` + its
fixture):** D143 owns the project-chats arms, D146 the background-mode /
update-schema arms; each lane appends its own cases in its own region and
regenerates the oracle from ITS pin — D143's pin (`c43d3b1b4`) also
moves D146's arms, so D143 masks them by the named
`BACKGROUND_MODE_PENDING_P4D146` tripwire; D146's pin (`70505745a`)
predates D143's change, so its regen leaves the chats arms as they are.
The unifier regenerates once at the new baseline and deletes the
tripwire.

**§E — `core-contract.ts` region scoping.** P4.D144 owns the chat-list DTO
regions (§A) + the `ConciergeState` union + any `ConciergeMark` input
types; P4.D146 owns the two project `backgroundDisplayMode` unions
(`:1504`, `:3622`); P4.D147 touches the file NOT AT ALL (every verb it
needs exists); P4.D143/P4.D145 are server-only. The unifier diffs the
contract name-for-name against the Rust `CoreResponse` shapes (playbook
§D).

**§F — the restore orchestrator region split (P4.D145 ↔ P4.D146).**
`services/backup/restore/orchestrator.rs`: the §17 Folders loop
(`:855-880` — the quiet duplicate-drop arm) belongs to P4.D145; the
`project_properties` carry (`:2130-2145` — whether `backgroundDisplayMode`
is normalized on restore, measured against v4) belongs to P4.D146.
Neither lane edits the other's region; the unifier cherry-picks D145
first and resolves any import union.

**§G — the folders route wire (P4.D145 ↔ P4.D147).** P4.D145 changes the
INTERNALS of `filesFolderCreate` / the parent-ensure (the `ensure_by_path`
chokepoint) but the wire shape and statuses are UNCHANGED (201
`{folder, alreadyExists: false, message: 'Folder created successfully'}`;
200 `{folder, alreadyExists: true, message: 'Folder already exists'}`; no
409 arm). P4.D147's picker consumes `filesList` + `filesFoldersList` +
`filesFolderCreate` exactly as the library picker does today and must
not depend on any D145 change; if D147's beat sees a duplicate-path 201
carrying a pre-existing row id, that is D145's recorded semantic shift,
not a defect.

## Ownership (binding, identical across all five orders)

| Area | Owner |
|---|---|
| `quilltap-core`: NEW `services/dangerous_content/concierge_state_presentation.rs`, `services/dangerous_content/chat_override.rs`, `services/chat_enrichment.rs`, `services/home.rs`, `api/salon.rs` (the chat-list serializer), `api/characters.rs` (the conversations serializer), `api/projects.rs` §D chats region, `services/message_finalizer.rs` (the `trigger_chat_danger_classification` guard ONLY), the §H `chatsHasDangerous` verb (`api/types.rs` variant + `api/engine.rs` dispatch + the `quilltap-web` chats REST edge); harness: `home_routes_equivalence`, the salon chat-list / characters / projects routes families, the chat-override + danger-trigger families (+ their oracle cases and fixtures) | **P4.D143** |
| SPA: NEW `chat/concierge-mark.ts` (+spec), `chat/concierge-state.ts` (+spec), `quick-hide/**`, `screens/home/recent-chats-section.ts` + `recent-chat-item.ts`, `screens/salon/salon-list.ts` + `chat-card.ts`, `chat/sidebar/chat-section.ts`, `screens/prospero/cards/project-chats-section.ts`, `screens/characters/view/tabs/conversations-tab.ts` + `character-conversation-card.ts`, `chat/conversation-header.ts` (the pill reads the shared table), `styles/qt-components/_chat.css` (the `.qt-concierge-mark` family), `core-contract.ts` §E regions, the gated quick-hide/marks beats | **P4.D144** |
| `quilltap-core`: `db/folders.rs`, NEW `db/folders_unique_path_repair.rs` (or the lane's name, + `db/mod.rs` line), NEW `db/sqlite_errors.rs` (or wherever the unique-constraint predicate lands), `services/character_avatar_job.rs` (the legacy-folder site), `services/story_background_job.rs` §C folder region, `api/files.rs` (both create paths), `services/quilltap_import/files.rs`, `services/backup/restore/orchestrator.rs` §F folders region, `services/builtin_mounts.rs` / `services/provisioning/mod.rs` (the ensure call), `write_partition.rs` (the one assertion); `quilltap-host`: `host.rs` (the boot repair chain block); harness: NEW `folders_collapse_heal_equivalence`, `folders_tier2_equivalence`, `files_routes_equivalence`, `qtap_import_equivalence`, the `system_restore_*` folder arms + the NEW duplicate-folders archive, the avatar/story tier-3 legacy-folder arms, `provisioning_equivalence` (run, not changed); `docs/v4/` mirror rows for `DDL.md` | **P4.D145** |
| `quilltap-core`: `chat_predicates.rs` (the str→status helper), `services/image_profile_resolution.rs`, `services/story_background_job.rs` §C participant region, `services/title_update_job.rs`, `api/chat_media.rs`, `db/projects.rs`, `api/projects.rs` §D background region, `services/backup/restore/orchestrator.rs` §F project-properties region; harness: `story_background_job_tier3_equivalence` + its builder, `title_update_tier3_equivalence` + the `cost-background` pair, `cost_background_routes_equivalence`, `projects_routes_equivalence`, `projects_tier2_equivalence`, the retired-mode restore archive if built; SPA: `screens/prospero/cards/project-image-generation-card.ts` (+spec), `core-contract.ts` §E region, the gated two-option beat | **P4.D146** |
| SPA: `screens/files/move-to-project-dialog.ts` (+spec), NEW `screens/files/folder-picker.ts` (+spec), the LIVE Move-to-Project beat | **P4.D147** |
| Playwright (port 4319) | **P4.D147's branch only**; every other lane's beats are authored gated (ACTIVATE-AT-UNIFY) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it |

**Version bumps:** this lane bumps the SPA `package.json` per commit and
no crate; the unifier recounts at unification (three lanes bump the SPA).

## Verification gate before declaring done

`npm test` + `npm run build` in `apps/web` (never a bare `ng`;
`check-qt-classes` runs inside both); the transcribed specs by name with
the red-first runs recorded; the mutation proofs verified-applied and
reverted by file backup; the three beats authored gated and their gate
constant named in the lane record for the unifier; NO Playwright run
in-lane. No Rust gate (no crate touched — say so). Per-commit rule:
`.claude/commands/commit.md`.
