# Work order P4.D189 — the stream watchdog, the SERVER substrate + the Salon side: `StreamError`'s stalled kind, `watch_stream` over every Salon-side `stream_message` consumer (ten sites), and `LLMStreamStalledError` → `network` in the fallback classifier — v4 `f90144ac4` bug 141 (the `streaming.service.ts` + `fallback/engine.ts` hunks), from `main`

**Status: CLOSED WHOLE (lane-complete, awaiting unification).** Ordered
2026-09-14, the `ffb6b3119` bug-141 + bug-142 drift catch-up round
(P4.D189 → P4.D190 ∥ P4.D191). Carried out 2026-09-14/15 on branch
`claude/p4-stream-watchdog-salon-b1222c`, from `main` at `f687cc4e` (the
round's own setupphase commit, one past the `d7cab319` the order names).
Every Tier 1 item (1–5) and every Tier 2 item (6–8) landed; the Tier 3
deferrals are recorded loudly in the module doc and the lane record.

**SUBSTRATE COMMIT: `73546827`** (§R.4) — `model/stream_watchdog.rs` +
`StreamError`'s kind and three methods + their unit tests, alone.
**P4.D190 branches from THAT sha.** ⚠ At this lane's start
`git worktree list` already showed
`.claude/worktrees/p4-d190-greeting-stall-ladder-b555d2` sitting at
`f687cc4e` — created BEFORE the substrate existed, so that lane must be
re-based onto `73546827` (or cherry-pick it) before it can compile
against §C.1/§C.2. Flagged for the unifier.

Commits (oldest first): `73546827` (substrate) · `dd1fdbdb` (the ten wrap
sites + the wrap census) · `550c21c4` (the classifier arm, red-first) ·
`5ba1974e` (the `stall` corpus vocabulary + five tier-3 cases) ·
`6fdc1583` (the two wiring probes + the `model_name` fidelity fix).

Gate: 570 test binaries / 3,287 passed / 0 failed / 2 ignored, exit 0,
zero `SKIP:` lines; fmt + clippy (both feature sets) + release build
clean; 15 families regenerated fresh at `/tmp/qt-v4-pin-p4d189-ffb6b3119`,
14 ok and one PRE-EXISTING red proven on main
(`brahma_orchestrator_tier3_equivalence` — a stale committed fixture,
plus a v5 `sqlite error: ` prefix divergence; both recorded for the
unifier, neither in this lane's Ownership). Versions: core 0.0.917,
harness 0.0.808. Full record: `status-log.md` → "P4.D189 — the stream
watchdog". The unifier updates this header at unification.

## Preamble

- **v4 oracle baseline: `31436bae4`.** This lane absorbs the Salon-side
  half of **`f90144ac4`** ("fix(llm): a silent provider no longer wedges
  chat creation (bug 141)", 2026-09-14 — the NEW `lib/llm/stream-
  watchdog.ts`, the `lib/services/chat-message/streaming.service.ts` hunk,
  and the `lib/llm/fallback/engine.ts` hunk + its test) — the ledger row
  `ORDERED(P4.D189, P4.D190, P4.D191)`. Spec reads: `git show f90144ac4:
  lib/llm/stream-watchdog.ts` (139 lines, whole), `git show f90144ac4 --
  lib/services/chat-message/streaming.service.ts lib/llm/fallback/engine.ts
  __tests__/unit/lib/llm/stream-watchdog.test.ts __tests__/unit/lib/llm/
  fallback/engine.test.ts` — **never the commit message** (ledger §5.3;
  §R.6).
- **Run the drift ledger's §2 freshness probe at lane start** (§R.2).
  **Any mismatch: STOP and report.** Re-run before every regen batch.
- **Regen rule: PIN REQUIRED (§R.3).** Pins **`/tmp/qt-v4-pin-p4d189-
  ffb6b3119`** (every moving family; the `fallback-engine.ts` oracle
  IMPORTS `@/lib/llm/stream-watchdog`, which does not exist at the
  baseline) and **`/tmp/qt-v4-pin-p4d189-31436bae4`** (the neutrality
  legs of Tier 1 item 8 and `QT_V4_ROOT` for every embed guard — this
  lane's vendored `help/**` stays at the baseline).
- **Sibling lanes:** P4.D190 (the greeting site + the ladder) is STACKED
  on your substrate commit — commit it FIRST, alone, and name its sha in
  your lane record; after it, `model/stream.rs` and `model/stream_
  watchdog.rs` are FROZEN for you too (a change you need later is a
  STOP-and-flag). P4.D191 (the convergence + ratifications + `help/**`)
  runs from `main` beside you and touches nothing you own.
- **Worktree:** `git worktree add <path> -b claude/p4-d189-stream-
  watchdog-<hash> main`; `CARGO_INCREMENTAL=0`.
- **Read before the work:** `crates/quilltap-core/src/model/transport.rs`
  (module header — v4's bounded-request baseline and WHY the streaming
  bound is headers-only: the gap this lane closes sits exactly where that
  header says "once the headers land the body streams unbounded");
  `model/stream.rs:305-332` (`StreamChunkResult` / `StreamError`);
  `model/stream.rs:449-560` (`CannedStreamingProvider` — never stalls);
  `services/primary_stream.rs:1012-1105` (`consume_stream`),
  `:1300-1330` (the hard-error failover call + `FallbackError::message`);
  `services/provider_failover.rs:767-800` (`restream_into`), `:355-395`,
  `:540-565`, `:1060-1085`, `:1195-1225` (the three classify sites + the
  hard-error entry); `llm_fallback/engine.rs:100-190` + `types.rs:236-283`;
  `crates/quilltap-harness/tests/fallback_engine_equivalence.rs` +
  `harness/oracle/cases/fallback-engine.ts` (the `(name, message)` error
  shape — rows at `:225-260`, `named()` at `:211`, `kind_for` at `:152`);
  `primary_stream_tier3_equivalence.rs` + `harness/oracle/cases/primary-
  stream-tier3.test.ts:275-330` (the `chunk.error` arm you extend with
  `chunk.stall`) + `:340-420` of the Rust file (`QueuedStreamingProvider`);
  the P4.D42 / P4.D83 lane records (`status-log.md` — the request-budget
  shape and the headers-only streaming decision this lane does NOT
  reverse); P4.44's abort-arming deferral (stands); memory notes
  `core-has-no-tokio-scheduler`, `capture-layer-target-assert-is-a-prefix-
  match`, `a-process-global-test-seam-must-be-thread-scoped`,
  `mutation-proof-revert-by-file-backup`, `a-family-env-var-is-not-its-
  regen-var`, `never-edit-source-during-a-workspace-gate`.

## Mandate

Port bug 141's Salon-side half whole, so that on v5 exactly as on v4 at
`ffb6b3119`: (a) a provider stream that answers with headers and then
sends nothing — or goes quiet mid-stream — FAILS inside a per-gap budget
(240 s to the first chunk, 120 s between chunks) instead of holding the
turn open forever, at EVERY Salon-side consumer (v4's one funnel = v5's
ten sites, §R.5(1)); (b) the failure is a NAMED kind
(`StreamErrorKind::Stalled`, v4 `LLMStreamStalledError`) carrying v4's
message bytes, the budget, the chunk count, the provider and the model;
(c) `classify_fallback_trigger` reads it as `network`, beside
`CheapLLMTimeoutError`, so a Salon turn whose primary stalls before its
first chunk reaches its understudy, and a stall on an understudy leg or
a same-provider retry is recorded as `network` in the route trail /
attempts; (d) v4's one `[LLMStream] Abandoned a stalled provider stream`
warn fires once per stall with v4's bag; (e) every existing tier-3
family stays byte-neutral (canned streams never stall). The stalled
request is abandoned, not cancelled — dropping the receiver is v5's
whole answer, and P4.44's abort-arming deferral stands, recorded. This
lane does NOT touch the greeting (`initial_greeting.rs`, `chat_create.rs`
— P4.D190's), the cheap-LLM completion path (v4's watchdog is
streaming-only), the transport's headers-only request budget (it stays;
the watchdog sits ABOVE it, at the consumer), `help/**`, the SPA, or the
DB layer.

## Survey-verified starting points (fresh survey 2026-09-14 at `ffb6b3119` and on `main` `d7cab319` — do not re-derive)

### v4

- **`lib/llm/stream-watchdog.ts`** (NEW, 139 lines; whole file is the
  spec): `DEFAULT_FIRST_CHUNK_TIMEOUT_MS = 240_000`,
  `DEFAULT_IDLE_CHUNK_TIMEOUT_MS = 120_000`; `class LLMStreamStalledError
  extends Error { budgetMs, chunksReceived, provider?, modelName? }` whose
  message is `Provider stream ${where}` with `where` = `never sent a first
  chunk within ${budgetMs}ms` (chunks 0) | `went quiet for ${budgetMs}ms
  after ${chunksReceived} chunk(s)`; `name = 'LLMStreamStalledError'`.
  `withStallWatchdog(source, options)`: `for (;;)` — `budgetMs =
  chunksReceived === 0 ? first : idle`; `withTimeout(iterator.next(),
  budgetMs, () => new LLMStreamStalledError(...))`; on the stalled error:
  `stalled = true`, `logger.warn('[LLMStream] Abandoned a stalled provider
  stream', { ...logContext, provider, modelName, budgetMs, chunksReceived,
  elapsedMs })`, rethrow; any other error rethrown untouched; `if
  (result.done) return; chunksReceived++; yield result.value`. `finally`:
  `iterator.return()` awaited on an early consumer `break`, NOT awaited on
  a stall (the suspended generator's `return()` would reproduce the hang).
  Carry the module doc's four paragraphs and the two budget-constant docs
  as the v5 module doc — they are the *why*.
- **`streaming.service.ts:395-421`** — the ONE wrap: `for await (const
  chunk of withStallWatchdog(provider.streamMessage({...}, apiKey), {
  provider: connectionProfile.provider, modelName: connectionProfile.
  modelName, logContext: { context: 'streaming.service', userId, chatId,
  characterId, messageId } }))` — NO budget override (defaults). The
  comment above it ("A provider that answers with headers and then goes
  silent would otherwise hold this `for await` open forever — the SDK's
  own timeout stops at the headers. The watchdog turns that into an
  ordinary throw, which the fallback engine reads as `network` and routes
  to the understudy.") is carried at every v5 site.
- **`fallback/engine.ts:118-125`** — after `if (name ===
  'CheapLLMTimeoutError') return 'network'`: `// Likewise the stream
  watchdog: the provider took the request, answered with headers and then
  went quiet. A silence is not a refusal, and the understudy is exactly
  what a chain is for.` `if (name === 'LLMStreamStalledError') return
  'network'`. Test (`engine.test.ts:128-133`): `new LLMStreamStalledError(
  240000, 0, 'DEEPSEEK', 'deepseek-v4-flash')` → `'network'`; `new
  LLMStreamStalledError(120000, 37)` → `'network'`.
- **`stream-watchdog.test.ts`** (138 lines) — the eight shapes, with
  budgets `{ firstChunkTimeoutMs: 60, idleTimeoutMs: 40 }`: a healthy
  paced stream passes untouched; first chunk never arrives → throws the
  class; the first-chunk case reports `chunksReceived 0`, `budgetMs 60`,
  message `/never sent a first chunk/`; yields `['a','b']` then throws
  with `chunksReceived 2`, `budgetMs 40`, `/went quiet/`; six gaps of
  25 ms each (total 150 ms, past the 40 ms idle budget) are NOT aborted —
  per-gap, not cumulative; provider/model carried onto the error; an
  early consumer `break` closes the source; a real provider error
  (`429 rate limit exceeded`) arrives as itself, not the class.
- **Who goes through the funnel at the target** (`git grep -ln
  "streaming.service'" ffb6b3119 -- lib app`, tests excluded): `progress/
  operation-progress-sse.ts`, `brahma-console/{one-shot,orchestrator}.
  service.ts`, `carina/carina.service.ts`, `chat-message/{courier-
  transport, danger-orchestrator, index, message-finalizer, native-tool-
  loop, orchestrator, pre-compute, primary-stream, provider-failover,
  recovery, text-tool-loop, tool-execution, turn-orchestrator}.service.ts`,
  `help-chat/orchestrator.service.ts`. MEASURE which of these CALL
  `streamMessage` (vs import a type) and map each caller to its v5 site
  below; record the map. Expected: every v5 site below has a v4 caller
  through the funnel, and no v4 streaming consumer lacks a v5 site.
- **Not in the commit:** no `AbortSignal` on `LLMParams` (the bug doc's
  "Not done here"); no client change; `plugin-utils`' `request-budget.ts`
  untouched.

### v5

- **The seam:** `model/stream.rs:314` `pub struct StreamError { pub
  message: String }` with `new()`, `Display`, `Error`; `:308`
  `StreamChunkResult = Result<StreamChunk, StreamError>`; the trait
  `StreamingCompletionProvider::stream_message(&self, provider, base_url,
  params) -> impl Future<Output = mpsc::Receiver<StreamChunkResult>>`
  (`:399`). Every consumer is `let mut rx = X.stream_message(..).await;
  while let Some(item) = rx.recv().await { … }`.
- **The ten Salon-side sites** (§R.5(1); each becomes `let mut rx =
  watch_stream(X.stream_message(..).await, StallBudgets::default(),
  StallWatchdogContext { context: "streaming.service", … })`, the loop
  body untouched): `services/primary_stream.rs:1035` (`consume_stream` —
  `provider_name`/`base_url` from `state.effective_profile`, `params`
  in hand; `log: Option<&StreamLogCtx>` carries `user_id`/`chat_id`/
  `message_id` — measure the struct and pass what it holds; `character_id`
  is a parameter); `services/provider_failover.rs:797` (`restream_into` —
  `profile.provider`/`profile.model_name`; the same `log` ctx);
  `services/recovery.rs:378`; `services/native_tool_loop.rs:465` + `:558`
  (two re-stream sites); `services/text_tool_loop.rs:659`;
  `services/carina_query.rs:1169` (`ctx.provider`/`ctx.base_url`; the
  Carina ctx's user/chat ids — measure); `services/help_chat/
  orchestrator.rs:1388`; `services/brahma_console/mod.rs:311` +
  `brahma_console/orchestrator.rs:871` (no `characterId` — `None`, as
  v4's wrapper receives `undefined` from those callers and its JSON drops
  it). Where a site holds no `model_name` local, it is `params.model`.
- **The classify sites:** `primary_stream.rs:1324` (`attempt_hard_error_
  failover(…, &FallbackError::message(&err.message), …)` — the PRIMARY's
  error, the site that must route a first-chunk stall to the understudy);
  `provider_failover.rs:379` (`retry_error` — the empty-response
  same-provider retry), `:555` (`reroute_error` — the Concierge reroute),
  `:1075` (`understudy_error` — a chain leg). `attempt_hard_error_
  failover` (`:1198`) leaves a stall that arrives AFTER streaming started
  to the caller (`opts.state.has_started_streaming` → the info line and
  `FallbackChainResult::default()`) — v4 identical; a MID-STREAM stall
  therefore surfaces as the turn's error, not a substitution, and that
  is an arm to pin.
- **`FallbackError`** (`llm_fallback/types.rs:248-283`): `{ kind:
  Option<LlmErrorKind>, name: Option<&str>, message: &str }` with
  `message()` / `typed()` / `named()`; the classifier (`engine.rs:121`)
  tests `error.name == Some("CheapLLMTimeoutError")` at `:150` — the
  new arm goes directly after it. The harness's `kind_for` (`fallback_
  engine_equivalence.rs:152`) maps the eight class names; anything else
  rides `name` — an `LLMStreamStalledError` row needs NO harness change
  beyond the corpus.
- **The primary-stream family:** `harness/oracle/cases/primary-stream-
  tier3.test.ts:275-330` — the mock resolves a `streamLabel` by marker,
  pops `spec.streams[label][attempt]`, and per chunk `if (chunk.error)
  throw new Error(chunk.error)`; the Rust twin `primary_stream_tier3_
  equivalence.rs:340-360` maps `err` → `Err(StreamError::new(err))`. The
  comparands: the SSE frame trace, the result object, the `chat_messages`
  + `chats` dumps, the `llm_logs` rows, and (P4.87/P4.88) the route trail
  + the three `[Failover]` bags as exact key sets. The fixture
  (`primary-stream-tier3.json`) carries `profile`, `uncensoredProfile`,
  `understudyProfile`, `tierSpareProfile`, `apiKeys`, `chats`,
  `character` — enough for every stall arm below without a builder
  change (measure; a needed profile is a builder edit you own).
- **Runtimes:** §R.5(5). `crates/quilltap-core/Cargo.toml:108` (`sync`,
  `time`), `:182` (dev: `test-util` → `start_paused`).

## Tiered deliverables

### Tier 1 — must land, in this order

1. **THE SUBSTRATE COMMIT (alone, first):** `model/stream_watchdog.rs` +
   `model/stream.rs`'s `StreamErrorKind` / `stalled()` / `is_stalled()` /
   `v4_name()` exactly as §C.1/§C.2, with v4's module docs carried, plus
   the unit tests transcribing `stream-watchdog.test.ts`'s eight shapes
   under `#[tokio::test(start_paused = true)]` with budgets `{60, 40}` ms
   (a `paced` provider that sleeps `tokio::time::sleep(gap)` before each
   chunk through a held `Sender` — a real task is needed to pace; use
   `tokio::spawn` in the TEST only, never in the module — and a
   `silent_after(values)` provider whose `Sender` is kept alive and never
   sends): healthy pass-through; first chunk never arrives → `Some(Err)`
   with `is_stalled()`, `chunks_received 0`, `budget_ms 60`, the exact
   `never sent a first chunk within 60ms` bytes; `['a','b']` then silence
   → the two chunks pass, then the stalled `Err` with `chunks_received 2`,
   `budget_ms 40`, `went quiet for 40ms after 2 chunk(s)`; six 25 ms gaps
   NOT aborted; provider/model on the error; a source `Err('429 rate
   limit exceeded')` arrives as itself (`kind == Provider`); after a
   stall every further `recv` is `None` (v5's "asked to unwind" leg);
   a reasoning-only chunk COUNTS (a `reasoning_content`-only chunk then
   silence reports `chunks_received 1`). Plus the warn line
   capture-pinned (`captured` thread-scoped, target prefix
   `quilltap::llm_stream`): present once on a stall with `context`,
   `provider`, `model_name`, `budget_ms`, `chunks_received`, `elapsed_ms`
   and the `Some` ids; ABSENT on a healthy stream and on a provider
   `Err`. Commit; record "Substrate commit: <sha>" in the lane record
   FIRST THING so P4.D190 can branch.
2. **The ten wrap sites** (survey list) — each `watch_stream(…)` with
   `StallBudgets::default()` and `context: "streaming.service"`, the v4
   comment carried once per file, loop bodies untouched. Then the
   **wrap census** — an always-on harness test in the `db_error_key_
   guard` idiom: for each of the eleven production files (the ten +
   `services/initial_greeting.rs`, which P4.D190 wraps — the census
   counts it at ZERO wraps until P4.D190 lands and P4.D190's order tells
   it to move the count to one; record the cross-lane handoff), strip
   test modules by brace balance, count `.stream_message(` and
   `watch_stream(` in the production zone, assert the per-file exact
   pairs (every production `stream_message` call is wrapped), and assert
   the eleven-file list is EXACTLY the set of production files containing
   `.stream_message(` under `crates/quilltap-core/src` (a twelfth site
   added later must be wrapped or explained). Mutation: unwrap ONE site →
   red by file name.
3. **§C.3** — `FallbackError::from_stream_error` + the classifier arm at
   v4's position with v4's comment; the four production classify sites
   through it; `fallback_engine_equivalence` RED-FIRST with ≥ 3 new
   corpus rows in `fallback-engine.ts` constructed from v4's REAL class
   (`import { LLMStreamStalledError } from '@/lib/llm/stream-watchdog'`):
   `stalled-first-chunk` (`new LLMStreamStalledError(240000, 0,
   'DEEPSEEK', 'deepseek-v4-flash')`), `stalled-mid-stream` (`(120000,
   37)` — no provider/model), and `stalled-name-beats-4xx-message` (the
   class with its message REPLACED by `400 Bad Request` — the name is
   tested BEFORE the message probes, so it is still `network`; the
   ladder-order row, the round-1 lesson). The oracle emits the observed
   `(name, message)` as today; the harness needs no code change beyond
   the corpus count.
4. **`primary_stream_tier3` RED-FIRST** with the `stall` vocabulary
   (§C.4) at the mock (`if (chunk.stall) throw new LLMStreamStalledError(
   chunk.stall.budgetMs, chunk.stall.chunksReceived, providerName,
   params.model)` — before the `chunk.error` arm) and its Rust twin, and
   ≥ 5 new cases: (a) **primary stalls before the first chunk** with a
   configured understudy → the walk reaches the understudy, the attempt
   records `network`, the route trail's answering entry names the
   understudy, `llm_logs` carries the primary's row with the stalled
   message as `error` (measure v4's row — the wrapper's `finally` logs
   it) and the understudy's row; (b) **primary stalls MID-STREAM** after
   two content chunks → NO substitution (`has_started_streaming`), the
   error surfaces as v4's does (measure the exact frame + result + row —
   port whatever v4 emits, the recorded `[Failover] … has started
   streaming` info line included); (c) **the understudy leg stalls** →
   `understudy_trigger` `network` in the attempts + trail, the chain
   moves on to the tier pick (or exhausts) exactly as v4; (d) **the
   empty-response same-provider retry stalls** (`retry_error` — a first
   attempt answering empty, the retry posed as a stall) → the trail row
   `Retry`/`Failed`/`network` with the stalled message as `detail`; (e)
   **the Concierge reroute stalls** (`reroute_error`) — the same shape
   via `Concierge`. Each case grepped in the fresh NDJSON for
   `LLMStreamStalledError` / `never sent a first chunk` bytes. Red-first:
   run the new corpus against the pre-item-3 tree — (a)/(c)/(d)/(e) must
   red on `provider-error` vs `network`.
5. **The `[Failover]` bags** the stall reaches (P4.87's three exact key
   sets) — confirm the stall's `error` field carries v4's message bytes
   through them (the existing pins; extend a key set only if v4's bag
   grew — it did not, measure).

### Tier 2 — should land

6. **A wiring probe at the two load-bearing sites** beyond the census: a
   `#[tokio::test(start_paused = true)]` in `primary_stream.rs` running
   `run_primary_stream` against a never-sending provider → returns the
   stalled error within the paused-clock budget and the warn fires with
   `context = "streaming.service"`, `chat_id`, `character_id`; the same
   for `restream_into` (the understudy leg). Mutation: swap a site's
   budgets for `StallBudgets { first_chunk_ms: u64::MAX/4, .. }` → the
   probe reds (proves the site's budgets are the DEFAULTS, which the
   census cannot see).
7. **`elapsed_ms` on the warn is wall-clock** (`std::time::Instant`
   since `watch_stream`, as v4's `Date.now() - startedAt`) — assert
   `>= budget_ms` in the stall probe; under `start_paused` the paused
   clock advances exactly the budget, so assert equality there.
8. **The neutrality sweep** (§R.3, gate item 5): every tier-3 family
   whose v5 consumer you wrapped, regenerated at BOTH pins and
   `cmp`-identical, v5 green: `orchestrator_tier3_equivalence`,
   `enclave_step_tier3_equivalence`, `native_tool_loop_tier3_
   equivalence`, `text_tool_loop_tier3_equivalence`, `carina_query_tier3_
   equivalence`, `brahma_console_tier3_equivalence`, `brahma_orchestrator_
   tier3_equivalence`, `help_chat_orchestrator_tier3_equivalence`,
   `answer_confirmation_tier3_equivalence`, `cheap_llm_fallback_
   equivalence`, `chat_continuation_tier2_equivalence`, `route_trail_
   compose_equivalence` (+ any family the sweep driver names as consuming
   `streaming.service`/`fallback/engine` — measure with `grep -l` over
   `harness/oracle/cases`). Record the byte counts per family. A recipe
   that no longer runs is repaired and the repair recorded (`recipe-
   header-conventions`).

### Tier 3 — explicit deferrals (loud, typed, never silent)

- **Real cancellation** of the stalled socket — v4's own "Not done here"
  (`AbortSignal` on `LLMParams` is a plugin-types contract change); v5's
  P4.44 abort-arming deferral STANDS. Record in the lane record and in
  the module doc that dropping the receiver is the whole v5 answer.
- **The consumer-`break` unwind** (`iterator.return()` awaited) has no
  v5 counterpart beyond the drop — a NO-COUNTERPART recorded in the
  module doc.
- The greeting site + budgets + ladder — P4.D190's. The two `help/`
  files — P4.D191's.
- **No SPA change** (v4 shipped none; the Green Room's own ceiling is
  v4's recorded not-done — a future v4 commit, not a v5 invention).

## The differential requirement (restated)

Tier 1 exact for the classifier (`fallback_engine_equivalence` over v4's
REAL `classifyFallbackTrigger` at the pin — the corpus rows above); tier 3
for the Salon spine (`primary_stream_tier3_equivalence` over v4's REAL
`primary-stream.service.ts` / `provider-failover.service.ts` with ONLY
`createLLMProvider` mocked — the `stall` arm throws v4's REAL error class,
so v4's REAL watchdog is in the loop only insofar as its `catch` rethrows;
the WATCHDOG's timing is unit-tier on both sides by the transport module's
falsifiability ruling, §R.5(4)); the neutrality legs at both pins.
Closest recipes: the two families' own headers (the `TMPO` staging, the
`-- cases/primary-stream-tier3` anchored jest filter — `sweep-driver-
renames-tmpo`), P4.D135's lane record (`status-log.md` — the fallback
engine's oracle mechanics), P4.87/P4.88 (the trail + bag comparands).
Mutation proofs, each reddening exactly its rows, reverted by FILE
BACKUP: delete the `LLMStreamStalledError` arm (rows 3a/3c + cases 4a/c/
d/e); make the budget cumulative (`elapsed since start` instead of per
gap — the six-gap unit test); stop counting reasoning-only chunks (the
reasoning-then-silence unit test); answer the stalled `Err` on every
`recv` instead of once (the after-stall `None` test); unwrap one site
(the census); drop the warn (the capture pin); render `chunk(s)` as
`chunks` (the message-bytes test + corpus row 3b's `message` comparand);
swap a site's budgets (item 6).

## Fixtures

- CONSUMES: `harness/oracle/fixtures/primary-stream-tier3.json` (+
  `build-primary-stream-fixture.ts` only if a stall arm needs a profile
  it lacks — you own it this round, §R.7); the `fallback-engine.ts`
  corpus (in-file).
- DELIVERS: the widened `primary-stream-tier3.json` + case file; the
  widened `fallback-engine.ts`; the NEW wrap-census test. No committed DB
  pair.

## §R — the round-wide meeting points (BINDING — verbatim-identical in P4.D189, P4.D190 and P4.D191)

- **R.1 Baseline and target.** Oracle baseline **`31436bae4`** (drift-ledger
  §1). Target **`ffb6b3119`** — v4 `main` HEAD at planning (2026-09-14
  22:25 CDT): FOUR commits past the baseline, the ledger's four §3 rows,
  every one now `ORDERED(…)`: `f90144ac4` (bug 141, PORT — P4.D189 +
  P4.D190 + the two `help/` files in P4.D191), `85813ddd2` (the ABI heal,
  NO-PORT? — P4.D191 ratifies), `364b04ac4` (the bug-142 filing, NO-PORT? —
  P4.D191 ratifies), `ffb6b3119` (the bug-142 fix, CONVERGENCE — P4.D191
  retires the pin by measurement). v4 `bugfix` tip `1a2b2164c` and
  `release` tip `8fbf2afe0` UNMOVED (still no `release: 4.10.0` squash).
  The checkout on `main`, CLEAN at the planning probe. v5 `main` at
  `d7cab319`; base versions core 0.0.913, harness 0.0.805, host 0.0.134,
  web 0.0.146, cli 0.0.20, tauri 0.0.7, SPA 0.5.721.
- **R.2 The freshness probe.** Run the ledger's §2 probe at lane start and
  before EVERY regen batch — the §1 values it checks are: branch `main`,
  tree clean, `git log --oneline ffb6b3119..main` EMPTY, `git log
  --oneline 1a2b2164c..bugfix` EMPTY. **Any mismatch: STOP and report**
  (a lane never writes the ledger; the human runs `/driftcheck`). A dirty
  tree in `lib/`/`app/`/`packages/`/`plugins/`/`help/` is a mismatch even
  when HEAD has not moved.
- **R.3 Regen rule: PIN REQUIRED.** Every oracle, fixture builder and
  jest case for a MOVING family runs from a lane-unique detached worktree
  at the TARGET: `PIN=/tmp/qt-v4-pin-<lane>-ffb6b3119` (ledger §5.1 —
  the three symlink classes, `cd "$PIN"` for every `tsx`/`jest`; the
  sweep driver with `--v4 "$PIN"`; verify the pin by grepping the fresh
  NDJSON for bytes only the target has). Neutrality legs (a family the
  lane touches whose v4 side is expected NOT to move) run from a SECOND
  lane-unique pin at the BASELINE, `/tmp/qt-v4-pin-<lane>-31436bae4`, and
  the two NDJSONs are diffed byte-for-byte. `QT_V4_ROOT` for the embed
  guards (`help_tree_embed_guard`, `qtap_schema_embed_guard`): P4.D189 and
  P4.D190 branch from `main`, whose vendored `help/**` is at the baseline
  → the `31436bae4` pin; P4.D191 → the `31436bae4` pin BEFORE its
  re-vendor commit and the `ffb6b3119` pin AFTER it. ⚠ The ABI-heal note
  (ledger §3, `85813ddd2`): the `ffb6b3119` pin carries the FIXED
  `jest.global-setup.js`, so the "pinned worktree chains the inert heal"
  trap does NOT apply to this round's target pins; the `31436bae4`
  NEUTRALITY pin still chains the old inert heal — if a real-binding jest
  case dies at setup with `NODE_MODULE_VERSION` after a green-looking
  rebuild line, rebuild `better-sqlite3-multiple-ciphers` by DIRECTORY
  from the main checkout first (memory note `oracle-node-abi-gotcha`).
  Node 24 (`~/.nvm/versions/node/v24.13.1/bin`) for every recipe.
- **R.4 Stacking and worktrees.** P4.D189's FIRST commit is the
  **substrate commit** — `model/stream_watchdog.rs` + the `StreamError`
  kind (§C.1, §C.2) + their unit tests, and NOTHING else — and its lane
  record names the sha under "Substrate commit:". **P4.D190 branches from
  THAT sha** (`git worktree add <path> -b claude/p4-d190-greeting-stall-
  <hash> <substrate-sha>`), not from `main` and not from P4.D189's tip;
  after the substrate commit P4.D189's `model/**` surface is FROZEN (a
  further `model/stream_watchdog.rs`/`StreamError` change P4.D190 needs
  is a STOP-and-flag, reconciled by the unifier). P4.D189 and P4.D191
  branch from `main` at `d7cab319`. Every lane: its own worktree + lane
  branch (`claude/p4-d18N-<slug>-<hash>` / `claude/p4-d19N-…`), edit
  inside it (`edit-inside-the-worktree-not-main`), `CARGO_INCREMENTAL=0`,
  its own `target/` (`parallel-round-playbook` §B), ONE sweep at a time
  per checkout. No SPA worktree is needed — **no lane touches
  `apps/web/**`** (bug 141 has no client hunk; the Green Room's own
  ceiling is v4's recorded not-done).
- **R.5 Planning facts measured at the target (not from prose — do not
  re-derive).** (1) v4 wraps `withStallWatchdog` at exactly TWO
  `provider.streamMessage(` sites (`git grep` at `ffb6b3119`:
  `lib/chat/initial-greeting.ts:174` and `lib/services/chat-message/
  streaming.service.ts:400`), but the second is v4's ONE FUNNEL: every
  Salon-side consumer — primary stream, provider failover, recovery, both
  tool loops, the turn orchestrator, the Courier, the danger orchestrator,
  Carina, both Brahma services, the help-chat orchestrator (nineteen
  importers of `streaming.service` at the target) — goes through it.
  **v5 has NO funnel**: ELEVEN production `.stream_message(` call sites,
  ten Salon-side (`services/recovery.rs:378`, `primary_stream.rs:1035`
  [`consume_stream`], `carina_query.rs:1169`, `native_tool_loop.rs:465`
  + `:558`, `provider_failover.rs:797` [`restream_into`],
  `help_chat/orchestrator.rs:1388`, `text_tool_loop.rs:659`,
  `brahma_console/orchestrator.rs:871`, `brahma_console/mod.rs:311`) +
  the greeting (`initial_greeting.rs:199`). The port is therefore eleven
  wrap sites, not two — every one wears the watchdog. (2) v4's stalled
  MESSAGE (`Provider stream never sent a first chunk within 240000ms` /
  `… went quiet for 120000ms after 37 chunk(s)`) matches NONE of the
  `NETWORK_ERROR_PATTERNS` (`timed? ?out|timeout|econnreset|…|aborted`),
  so without the NAMED classifier arm v5 classifies a stall as the
  unattributed fall-through `ProviderError` — the `LLMStreamStalledError`
  → `network` corpus row is a real discriminator, not a formality.
  (3) Every canned provider on both sides (`CannedStreamingProvider`,
  `QueuedStreamingProvider`, the jest `async function*` mocks) yields
  promptly and CLOSES — a v5 canned sequence is pre-pushed into the
  channel, so `recv()` answers `None`, never pends — so the watchdog is
  expected byte-NEUTRAL on every existing tier-3 family; that neutrality
  is PROVEN by regen at both pins (§R.3), never assumed. (4) A stall can
  only be posed by a NAMED error thrown/queued directly (v4's own
  `route.greeting-stall.test.ts` does exactly that — `mockRejectedValue(
  new LLMStreamStalledError(90000, 0, 'DEEPSEEK', 'deepseek-v4-flash'))`);
  the watchdog's TIMING is unit-tier on both sides (v4's
  `stream-watchdog.test.ts` eight shapes; v5 `#[tokio::test(start_paused
  = true)]` under `tokio/test-util`, a dev-dependency already in core),
  per the transport module's own falsifiability ruling ("a timeout is
  wall-clock behavior no NDJSON corpus can observe"). (5)
  `tokio::time::timeout` compiles in the default core build (`tokio =
  { default-features = false, features = ["sync", "time"] }`) but needs a
  runtime with a TIME DRIVER at the await — every production runtime
  (`host.rs:1044`, `spine.rs:989/2205/2288/2342`, `generator_sse.rs:287`,
  the `#[tokio::main]`s) is `enable_all()`; a test venue that polls a
  stream consumer under a runtime WITHOUT one panics `there is no
  reactor running` — the fix is the venue's runtime (`enable_all()` /
  `#[tokio::test]`), NEVER a watchdog that is optional or skipped
  (memory note `core-has-no-tokio-scheduler`). (6) The v5 greeting
  ladder's three catch arms are SILENT (`Err(_) => { /* swallowed */ }`,
  `chat_create.rs:2739/2757/2802`) where v4 warns at each (`[Chats v1]
  Greeting generation attempt failed {characterId, attempt: 'full
  context' | 'without memories', error}` and `[Chats v1] Final greeting
  generation retry failed {characterId, error}`) and the exhaustion line
  (`[Chats v1] All greeting generation attempts exhausted, falling back
  to static greeting {characterId, contentFilterHit}`) is absent too —
  the #103/#110 silent-arm class, closed by P4.D190 alongside the new
  `Greeting abandoned` warn. (7) `chats_messages_ops_tier2_equivalence`'s
  `DELETE_MISS_DIVERGENCE` (`c0000020-0000-4000-8000-000000000001`: v5
  writes `transcriptVersion` 2, v4 3) is a BOTH-DIRECTIONS pin whose
  `[oracle]` arm FIRES on the first regen at or past `ffb6b3119` — that
  is the tripwire working, not a regression (ledger §5.4): measure v4's
  post-fix rows, then retire. (8) `help/**` stays at **124** files —
  `f90144ac4` MODIFIED two (`chats.md` +2, `connection-profiles.md`
  +16/−1) and added none; `VENDORED_FILE_COUNT` does not move, only its
  provenance comment. (9) `85813ddd2` / `364b04ac4` / `ffb6b3119` touch
  NO `lib/` outside `chats-messages.ops.ts:637-645`, no `help/`, no
  `public/`, no migration (`git show --stat` at planning, file lists in
  P4.D191's survey).
- **R.6 Prose traps (ledger §5.3).** Port the HUNKS: (1) the commit
  message says "applied at both provider.streamMessage call sites" — in
  v5 that is eleven (R.5(1)); (2) "the stalled request is abandoned, not
  cancelled" is a DESIGN statement — v5's counterpart is dropping the
  `Receiver`, which unwinds the host transport pump on its next send;
  nothing new is cancelled here either, and P4.44's abort-arming deferral
  STANDS (record it, do not close it); (3) v4's `withStallWatchdog`
  `finally` awaits `iterator.return()` on an early consumer `break` but
  NOT on a stall — v5 has no `return()`; dropping the receiver is the one
  answer for both legs, and the divergence is a NO-COUNTERPART to record;
  (4) the bug doc's "the Green Room resolves within 90 s of a stall" is
  the greeting's FIRST-chunk budget (a stall after one chunk resolves
  within 60 s) — port the constants, not the sentence; (5) the commit's
  "Also rebuilt the better-sqlite3 native binding" is the human's local
  toolchain, not a `lib/` change, and `85813ddd2` is its follow-up — both
  NO-PORT on the file lists.
- **R.7 Fixture ownership.** No two lanes regenerate one committed
  fixture. P4.D189 owns `primary-stream-tier3.json` (+ its builder if the
  stall arms need a profile) and the `fallback-engine.ts` corpus; P4.D190
  owns `chat-create-capstone.json` + `build-chat-create-capstone.ts` and
  `initial-greeting.json`; P4.D191 owns `chats-messages-ops-tier2.json` +
  its builder (regen only — no shape change expected) and `help/**`. No
  committed DB pair moves this round.
- **R.8 Version-bump ownership.** Per commit, per `.claude/commands/
  commit.md` §6, each lane bumps only the crates it touches: P4.D189 —
  core, harness; P4.D190 — core, harness; P4.D191 — harness, host (the
  embedded help tree is `quilltap-host`'s `build.rs` output). The unifier
  recounts as base + total bumps (base: core 0.0.913, harness 0.0.805,
  host 0.0.134, web 0.0.146, cli 0.0.20, tauri 0.0.7, SPA 0.5.721) —
  identical cross-lane bumps auto-merge SILENTLY (`round-plan-takeaways`).
- **R.9 Append-only docs.** `docs/CHANGELOG.md` (an entry per commit, the
  §7 shape) and `docs/developer/porting/status-log.md` (the lane record,
  appended at the END) are append-only for every lane; they union-merge
  at unification. Nobody edits CLAUDE.md, `phase-4.md`, `drift-ledger.md`,
  `dogfood-findings.md`, `docs/v4/**`, or another lane's order file. Each
  lane rewrites ONLY its own order's status header.
- **R.10 Standing rules that bite here.** Spelling — "Quilltap", never
  the quilt-based misspelling (`harness/tools/check_spelling.py` + the
  `spelling_guard` test). No stubs, no `TODO`s; every deferral is a loud
  typed refusal named in the lane record. Every ported unit ships with
  its differential driving v4's REAL code from the pin (tier 1 exact /
  tier 2 structural DB diff / tier 3 mocked-LLM), plus mutation proofs
  each reddening exactly its named rows, reverted by FILE BACKUP never
  `git checkout <file>`. A log-only change is invisible to every
  differential — pin it with a thread-scoped capture layer
  (`capture-layer-target-assert-is-a-prefix-match`, `a-process-global-
  test-seam-must-be-thread-scoped`). A wire that no test drives is
  unpinned — a production-zone census (the `db_error_key_guard` idiom:
  per-file exact counts, brace-balanced test-module stripping) or a
  wiring probe. A guard whose other conjuncts are false in every arm is
  untested — pin each conjunct with the others OPEN. Every long gate:
  `run_in_background: true` + full-log capture + a sentinel; NEVER a
  `pgrep`/`grep` poll loop, NEVER `| tail`. `QT_NODE` is the node BINARY.
- **R.11 The docs mirror + the ledger are the unifier's.** `docs/v4/**`
  refreshes at unification (`bugs.md`, the bug-141 and bug-142 files,
  `CHANGELOG.md`); the ledger's four rows move to §6 at the baseline move
  to `ffb6b3119`; the `31436bae4`-round §3 catches this round inherits
  nothing.
## §C — the round-wide contract (BINDING — verbatim-identical in P4.D189, P4.D190 and P4.D191)

Every name below is a name, not a suggestion. P4.D189's substrate commit
DEFINES C.1 and C.2; P4.D190 CONSUMES them from that commit and defines
nothing in `model/**`; P4.D191 touches neither. A lane that needs a name
not listed here STOPs and flags it in its lane record. Measured against
`f90144ac4`'s hunks on 2026-09-14.

### C.1 `StreamError` gains a kind (`crates/quilltap-core/src/model/stream.rs` — P4.D189 substrate)

```rust
/// v4 `LLMStreamStalledError` vs everything else. v4 tells the two apart by
/// the error's `name`; v5 has no error-class hierarchy at the stream seam,
/// so the one class that matters to a caller is carried as a kind.
#[derive(Clone, Debug, PartialEq)]
pub enum StreamErrorKind {
    /// A provider failure that reached us as text (every pre-existing site).
    Provider,
    /// The stream watchdog fired (v4 `LLMStreamStalledError`).
    Stalled {
        budget_ms: u64,
        chunks_received: u64,
        provider: Option<String>,
        model_name: Option<String>,
    },
}

#[derive(Clone, Debug)]
pub struct StreamError {
    pub message: String,
    pub kind: StreamErrorKind,
}

impl StreamError {
    pub fn new(message: impl Into<String>) -> Self;   // kind: Provider — every existing call site compiles unchanged
    pub fn stalled(budget_ms: u64, chunks_received: u64, provider: Option<&str>, model_name: Option<&str>) -> Self;
    pub fn is_stalled(&self) -> bool;
    /// v4's `error.name` as the fallback classifier reads it:
    /// `"LLMStreamStalledError"` for `Stalled`, `"Error"` otherwise.
    pub fn v4_name(&self) -> &'static str;
}
```

`StreamError::stalled` renders v4's constructor bytes EXACTLY:
`chunks_received == 0` → `Provider stream never sent a first chunk within
{budget_ms}ms`; otherwise `Provider stream went quiet for {budget_ms}ms
after {chunks_received} chunk(s)`. `Display` stays the bare message. No
other field, no other constructor.

### C.2 The watchdog (`crates/quilltap-core/src/model/stream_watchdog.rs`, NEW — P4.D189 substrate; `pub mod stream_watchdog;` in `model/mod.rs`)

```rust
pub const DEFAULT_FIRST_CHUNK_TIMEOUT_MS: u64 = 240_000;   // v4 DEFAULT_FIRST_CHUNK_TIMEOUT_MS
pub const DEFAULT_IDLE_CHUNK_TIMEOUT_MS: u64 = 120_000;    // v4 DEFAULT_IDLE_CHUNK_TIMEOUT_MS

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct StallBudgets { pub first_chunk_ms: u64, pub idle_ms: u64 }
impl Default for StallBudgets { … }   // the two constants

/// v4's `StallWatchdogOptions.provider/modelName/logContext`, flattened.
#[derive(Clone, Copy, Debug)]
pub struct StallWatchdogContext<'a> {
    pub provider: &'a str,
    pub model_name: &'a str,
    /// v4 `logContext.context`: `"streaming.service"` at every Salon-side
    /// site (v4's one funnel), `"initial-greeting"` at the greeting.
    pub context: &'static str,
    pub user_id: Option<&'a str>,
    pub chat_id: Option<&'a str>,
    pub character_id: Option<&'a str>,
    pub message_id: Option<&'a str>,
}

pub struct WatchedStream<'a> { /* rx, budgets, ctx, chunks_received, started_at, stalled */ }

pub fn watch_stream<'a>(
    rx: tokio::sync::mpsc::Receiver<StreamChunkResult>,
    budgets: StallBudgets,
    ctx: StallWatchdogContext<'a>,
) -> WatchedStream<'a>;

impl WatchedStream<'_> {
    /// The same signature as `Receiver::recv`, so every consumer loop
    /// (`while let Some(item) = rx.recv().await`) is unchanged.
    pub async fn recv(&mut self) -> Option<StreamChunkResult>;
}
```

Semantics (v4 `withStallWatchdog`, per `next()`): the budget for THIS
`recv` is `first_chunk_ms` while `chunks_received == 0`, else `idle_ms` —
per gap, never cumulative. Each `recv` races the inner `rx.recv()` against
`tokio::time::timeout(Duration::from_millis(budget))`. An `Ok(chunk)` from
the source increments `chunks_received` (EVERY yielded chunk counts —
reasoning-only, usage-only, `done` — v4 counts `result.value`s) and is
passed through; an `Err` from the source passes through AS ITSELF (v4: "a
real provider error arrives as itself"); `None` (the source closed) passes
through. On a timeout: ONE `tracing::warn!(target: "quilltap::llm_stream",
"[LLMStream] Abandoned a stalled provider stream")` with fields `context`,
`user_id`, `chat_id`, `character_id`, `message_id` (each an `Option`,
rendered only when `Some` — v4's JSON drops `undefined`), `provider`,
`model_name`, `budget_ms`, `chunks_received`, `elapsed_ms` (wall-clock
since `watch_stream`); the inner receiver is DROPPED (v5's "abandoned, not
cancelled" — the host transport pump's next `send` fails and it unwinds);
the call answers `Some(Err(StreamError::stalled(budget, chunks_received,
Some(provider), Some(model_name))))` exactly once, and every later `recv`
answers `None`. Consumer loops treat that `Err` exactly as any mid-stream
`Err` today (`return Some(e)` / `item?` / `break`) — no consumer branches
on the kind except the greeting ladder (P4.D190) and the fallback
classifier (C.3). Greeting budgets live in `services/initial_greeting.rs`
(P4.D190): `GREETING_FIRST_CHUNK_TIMEOUT_MS: u64 = 90_000`,
`GREETING_IDLE_TIMEOUT_MS: u64 = 60_000`.

### C.3 The classifier arm (`crates/quilltap-core/src/llm_fallback/{types,engine}.rs` — P4.D189)

```rust
impl<'a> FallbackError<'a> {
    /// The production shape for a stream failure: v4 walks `error.name`
    /// before `error.message`, and the one stream-seam name it tests is
    /// `LLMStreamStalledError`.
    pub fn from_stream_error(e: &'a StreamError) -> FallbackError<'a>;
    // == named("LLMStreamStalledError", &e.message) when e.is_stalled(), else message(&e.message)
}
```

`classify_fallback_trigger` gains `if error.name ==
Some("LLMStreamStalledError") { return Some(FallbackTrigger::Network); }`
at v4's position — directly AFTER the `CheapLLMTimeoutError` arm and
BEFORE the message-pattern probes — with v4's comment carried. Every
production site that builds a `FallbackError` from a `StreamError`
(`primary_stream.rs:1324`, `provider_failover.rs:379/:555/:1075`) goes
through `from_stream_error`; `cheap_llm_exec.rs:1232-1234` (the completion
path) is untouched — v4's watchdog is streaming-only. P4.D190 does NOT
call the classifier: the greeting ladder tests `is_stalled()` directly, as
v4's route tests `instanceof LLMStreamStalledError`.

### C.4 The corpus vocabulary for a posed stall (P4.D189 in `primary-stream-tier3`; P4.D190 in `chat-create-capstone` and `initial-greeting`)

A canned stream entry — a `chunk` in `primary-stream-tier3.json`'s
`streams[label][attempt]` list, a `greetingByModel[model]` entry (or one
element of its per-call `attempts` list) in `chat-create-capstone.json`,
and a case in `initial-greeting.json` — may carry EITHER
`stall: { budgetMs: number, chunksReceived: number }` OR `error: string`
(the latter already exists in `primary-stream-tier3`). The jest mock
throws v4's REAL class for `stall` — `throw new LLMStreamStalledError(
budgetMs, chunksReceived, providerName, params.model)` imported from
`@/lib/llm/stream-watchdog` (target pin only — the module does not exist
at the baseline, which is one more reason every regen is pinned) — and a
plain `new Error(error)` for `error`; the Rust side registers
`Err(StreamError::stalled(budget_ms, chunks_received, Some(provider),
Some(model)))` / `Err(StreamError::new(error))` as the sequence's terminal
item. A stall entry with `chunksReceived > 0` is preceded by that many
canned content chunks on BOTH sides (the counts on the error must agree
with what was yielded). Same key names in every corpus; each lane owns its
own corpus file (§R.7).
## Ownership (BINDING — identical table in every order of this round)

No two lanes edit one source file. `docs/CHANGELOG.md` and
`docs/developer/porting/status-log.md` are append-only for everyone.
`model/stream.rs` and `model/stream_watchdog.rs` are P4.D189's and FROZEN
after its substrate commit (§R.4) — P4.D190 consumes them by NAME (§C) and
never edits them.

| Lane | OWNS (may edit / create) | MUST NOT TOUCH |
|---|---|---|
| **P4.D189** (the substrate + the Salon-side watchdog + the classifier, from `main`) | NEW `crates/quilltap-core/src/model/stream_watchdog.rs`; `model/stream.rs` (`StreamErrorKind`, the three new `StreamError` methods — nothing else); `model/mod.rs` (one `pub mod` line); the TEN Salon-side wrap sites: `services/primary_stream.rs`, `services/provider_failover.rs`, `services/recovery.rs`, `services/native_tool_loop.rs`, `services/text_tool_loop.rs`, `services/carina_query.rs`, `services/help_chat/orchestrator.rs`, `services/brahma_console/mod.rs`, `services/brahma_console/orchestrator.rs`; `llm_fallback/types.rs` (`from_stream_error`) + `llm_fallback/engine.rs` (the arm); NEW `crates/quilltap-harness/tests/stream_watchdog_wire_census.rs` (the wrap census — or the census inside an existing always-on harness test the lane names); harness: `fallback_engine_equivalence.rs` + `harness/oracle/cases/fallback-engine.ts`, `primary_stream_tier3_equivalence.rs` + `harness/oracle/cases/primary-stream-tier3.test.ts` + `harness/oracle/fixtures/primary-stream-tier3.json` (+ `build-primary-stream-fixture.ts` only if a stall arm needs a profile the fixture lacks); the neutrality regens of the families named in its Tier 1 item 8 (recipes untouched unless rotten — a repair is recorded); this order's header | `services/initial_greeting.rs`, `services/chat_create.rs`, `services/cheap_llm_exec.rs`, `services/orchestrator.rs` (READ — the primary stream's caller; a needed change is a STOP-and-flag), `db/**`, `api/**`, `help/**`, `chat_create_capstone_*`, `initial_greeting_*`, `chat_context_init_*`, `chats_messages_ops_*`, `help_*` families, `crates/quilltap-web/src/**`, `crates/quilltap-host/src/**`, `apps/web/**`, `docs/v4/**` |
| **P4.D190** (the greeting watchdog + the ladder, stacked on P4.D189's SUBSTRATE commit) | `services/initial_greeting.rs` (the wrap site + the two greeting constants + the `error` on the `llm_logs` row already carried — verify); `services/chat_create.rs` (the ladder: `own_profile_stalled`, `note_own_profile_outcome`, `give_up_on_stall`, the three gates, v4's three pre-existing warn lines + the exhaustion line); harness: `chat_create_capstone_equivalence.rs` + `harness/oracle/cases/chat-create-capstone.test.ts` + `harness/oracle/fixtures/chat-create-capstone.json` + `build-chat-create-capstone.ts` (only if a case needs a profile/memory the fixture lacks), `initial_greeting_equivalence.rs` + `harness/oracle/cases/initial-greeting.test.ts` + `harness/oracle/fixtures/initial-greeting.json`, `chat_context_init_equivalence.rs` ONLY if measurement shows the greeting-resolve family must move (record the measurement either way); this order's header | `model/**` (READ — §C.1/§C.2 are yours to CALL; a missing name is a STOP-and-flag), `llm_fallback/**`, every file in P4.D189's OWNS column, `db/**`, `api/**` (READ — `chat_create` is reached through `api/chat_create.rs`, which does not move), `help/**`, `chats_messages_ops_*`, `help_*` families, `crates/quilltap-web/src/**`, `crates/quilltap-host/src/**`, `apps/web/**`, `docs/v4/**` |
| **P4.D191** (the bug-142 convergence + the two NO-PORT ratifications + the `help/**` re-vendor, from `main`) | `crates/quilltap-harness/tests/chats_messages_ops_tier2_equivalence.rs` (the pin retirement) + its recipe/builder ONLY if rotten (`harness/oracle/cases/chats-messages-ops-tier2.ts`, `fixtures/build-chats-messages-ops-fixture.ts`, `fixtures/chats-messages-ops-tier2.json`); `crates/quilltap-core/src/db/chats_messages.rs` — COMMENTS ONLY (the "FILED UPSTREAM" / bug-142 note beside `delete_messages_by_ids`; zero code change, proven by `git diff` showing only comment lines); `help/chats.md` + `help/connection-profiles.md` (byte copies from the target pin); `crates/quilltap-harness/tests/help_tree_embed_guard.rs` (the `VENDORED_FILE_COUNT` provenance COMMENT — the value stays 124); the help-content families' regens (`help_tree_equivalence`, `help_docs_tier2_equivalence`, `help_chat_orchestrator_tier3_equivalence`, `help_context_resolver_equivalence`, + whichever the sweep's `help_*` list names); the Tier R run (`crates/quilltap-cli/tests/cli_differential.rs`, READ — run only); the two NO-PORT ratifications + the convergence measurement (lane record); this order's header | every `services/**`, `model/**`, `llm_fallback/**` file; `db/**` code (comments only, above); `api/**`; `crates/quilltap-web/src/**`; `crates/quilltap-host/src/**` (the embedded help changes through `build.rs` re-reading `help/**` — no source edit); `apps/web/**`; `docs/v4/**`; every P4.D189/P4.D190 family |

Round-wide: nobody edits `CLAUDE.md`, `phase-4.md`, `drift-ledger.md`,
`dogfood-findings.md`, `docs/v4/**`, another lane's order, or a committed
DB pair (none moves this round).
## Verification gate (before declaring done — identical in every order of this round)

Run from the lane worktree, `CARGO_INCREMENTAL=0`, `TZ=UTC`, every long
command with `run_in_background: true` + full-log capture + a sentinel
(`background-gate-latency-fix`); never a poll loop, never `| tail`.
`QT_V4_ROOT` = this lane's §R.3 pin for the embed guards (the baseline pin
for P4.D189 / P4.D190; for P4.D191 the baseline pin before the re-vendor
commit and the target pin after it).

1. **The probe (§R.2)** — passed at lane start and before every regen.
2. `cargo fmt --all --check` clean.
3. `cargo clippy --workspace --all-targets -- -D warnings` clean in BOTH
   feature sets (default and `--features quilltap-core/native-transport`).
4. `cargo build --workspace --release` clean.
5. **This lane's differentials by NAME**, each over an oracle regenerated
   FRESH from the `ffb6b3119` pin through `harness/tools/recipe_sweep.py
   --v4 <pin> --run <family>` (never two sweeps at once), zero `SKIP:`
   lines, every NDJSON non-empty, the CHANGED bytes grepped in the NDJSON
   (ledger §5.2), and the red-first evidence recorded (the pre-port run
   RED on the new arms, then green). **Neutrality legs** (the families the
   lane touches whose v4 side must NOT move): regenerated at BOTH pins,
   the two NDJSONs `cmp`-identical, v5 green against them — record the
   byte counts.
6. **The mutation proofs** named in the order, each reddening exactly its
   rows, each reverted by FILE BACKUP; a mutation that survives is
   recorded, never deleted (`mutation-proof-traps-index`).
7. `cargo test --workspace` with the lane's env block (its own recipe
   vars + `QT_V4_ROOT`; the untouched families' vars deliberately
   withheld) — record `<N> test binaries / <P> passed / 0 failed`, exit
   0, ZERO `SKIP:` lines for the lane's families (confirm each RAN by
   name and non-zero duration — `a-family-env-var-is-not-its-regen-var`).
   `cargo test --workspace` is fail-fast per BINARY — read the whole log.
   A `there is no reactor running` panic anywhere in it is §R.5(5): fix
   that venue's runtime and record it.
8. **SPA:** no lane touches `apps/web/**` — each lane records "no SPA
   change; no e2e owed" and runs NO Playwright (ONE Playwright at a time
   repo-wide stands for whoever does).
9. **Ownership honoured:** `git diff main -- <every MUST-NOT-TOUCH path>`
   EMPTY (P4.D190 diffs against P4.D189's substrate commit).
10. **Per commit** `.claude/commands/commit.md`: the CHANGELOG entry (H4
    header + versions line), the version bumps (§R.8), the status-log
    lane record appended at the END (units, measurements, the mutation
    table, the gate numbers, "spotted, not mine" items for the unifier,
    the regen recipe with the pin path), the spelling grep, this order's
    status header rewritten. Push the lane branch; never merge to `main`.
