# Work order P4.D32 — the release-refactor neutrality sweep + helper mirrors

> **STATUS: CLOSED — UNIFIED on main (2026-07-31).** All tiers landed
> (branch `claude/p4-d32-release-refactor-7aacba`, five commits). **The
> verdict: the four release-refactor commits are output-neutral** — proven
> by a 290-of-324 non-sibling-family regen-and-re-run at the `ff12f491`
> pin (34 not regenerated: 6 compile-time pins with no oracle by design +
> 28 whose header recipes did not survive mechanical extraction — a stated
> shortfall, enumerated in the lane record). The Rust helper mirrors
> landed (staff display names, `default_timestamp_config`, `event_time_ms`,
> the turn-transcript participant-resolver move); the dead-code follow
> removed v5's `find_cheapest_available_model` (the order's "no v5 analog"
> survey was WRONG — it existed, referenced only by its own differential)
> and found the other six never ported. **`33cca411` was NOT a NO-PORT:**
> the Tier R differential byte-diffs `docs --help` + completions, and was
> 6-of-135 red at the pin until the help/completion re-port landed (now
> 135/135). **The sweep surfaced standing reds that are NOT this drift's:**
> the `canChooseOutfit` character-projection gap (pre-existing, P4.6bh —
> wants its own unit; now in phase-4's candidates), the ruled
> chunk-on-write and error-row divergence reds, and a `terminal_tools`
> oracle that did not survive regen (unresolved). The
> `pricing_fetcher_equivalence` oracle mock was repaired (it had been
> starving v4 since SDK 0.13 — comparing against a starved oracle).
> Lane record: `status-log.md`.
>
> **v4 baseline for this round: `ff12f491`** (v4 HEAD, 2026-07-30, clean
> tree). The previous baseline was `dcd9440a`; the drift is NINETEEN commits,
> classified across this round's five orders (P4.D30–P4.D34). This lane owns
> the four claimed-neutral release-sweep refactors (`0246c6c8` minus its
> pascal slice, `345a858a`, `55752ad4`, `cc2e3c6f`) plus the round's
> NO-PORT dispositions. **The baseline move is the UNIFIER's** — do NOT edit
> `CLAUDE.md` or `phase-4.md` on the branch; put the replacement wording for
> your slice in the lane record.
>
> **Drift-check at lane start**: `git log ff12f491..HEAD --oneline` +
> `git status --short` in `~/source/quilltap-server`. If v4 moved past
> `ff12f491` or the tree is dirty, STOP and report. Regenerate oracles from a
> detached worktree PINNED at `ff12f491`
> (`/private/tmp/qt-v4-pin-ff12f491`, recipe:
> `oracle-regen-pinned-v4-worktree`, incl. per-plugin
> `plugins/dist/*/node_modules` symlinks). ⚠ Verify installed SDK majors
> (openai 7, `@openrouter/sdk` 1.2) before regen; STOP if stale — do not run
> `npm install` in the v4 checkout yourself.
>
> **Siblings (same round, disjoint files):** P4.D30 (Pascal canonical
> reader), P4.D31 (restore memory ids), P4.D33 (provider SDK wire check),
> P4.D34 (SPA drift riders — ALL `apps/web` mirrors of these refactors are
> D34's, not yours). This lane owns the Rust helper mirrors OUTSIDE
> sibling-owned files, and the broad regen-and-rerun sweep EXCLUDING the
> families the other four orders name. See "Shared contract + Ownership" —
> binding and identical across all five orders.
>
> **Worktree note:** own branch, own checkout; no `git stash`/`git worktree`.

## The drift, classified (surveyed 2026-07-30)

Four commits, all claiming "no functional change", all landing on PORTED
surfaces. The D10 precedent applies: a "no functional change" claim is
proven by a regen-and-re-run sweep, never accepted by inspection — and the
D29 correction applies too: routes families load route modules at RUNTIME
(`loadRoute(path)`), so a static import walk saying "family X doesn't import
the drifted file" is UNPROVEN on any routes family. Hence: sweep everything
you own.

1. **`0246c6c8 refactor: collapse duplicated helpers across the release
   diff`** (27 files). The non-pascal, non-SPA slice:
   - `lib/chat/staff-display-names.ts` — NEW client-safe module:
     `STAFF_DISPLAY_NAMES` (11 entries, incl. `host`) +
     `staffDisplayName(sender)` with a raw-tag fallback for unrecognised
     senders. `lib/export/markdown-transcript.ts` drops its private map
     (which had NO raw-tag fallback — `?? msg.systemSender` inline, so
     behavior is identical) and imports the shared one. The Carina
     exception (answers render under the ANSWERER's name) stays at call
     sites.
   - `lib/chat/timestamp-utils.ts` — `DEFAULT_TIMESTAMP_CONFIG` exported;
     `markdown-transcript.ts` drops its identical private `FALLBACK_CONFIG`.
   - `lib/memory/recall-tags.ts` — private `eventTimeMs(memory)` extracted;
     `occurredWithinMultiplier` + `freshEventMultiplier` now share it. ⚠ One
     REAL semantic wrinkle: the old arms returned `{multiplier: 1}` early on
     a MISSING iso string; the new shared helper returns `NaN` and the arms
     rely on NaN comparisons falling through. Outputs are claimed identical
     — the tier-1 weighting/recall-tags families must prove it.
   - `lib/services/chat-message/turn-transcript.ts` — gains the exported
     `resolveUserCharacterParticipant`; the private copies in
     `lib/background-jobs/handlers/memory-extraction.ts` and
     `app/api/v1/chats/[id]/actions/memories.ts` are deleted.
   - `lib/database/backends/sqlite/introspection.ts` — NEW `tableExists(db,
     name)`; `lib/embedding/reapply-profile.ts` (UNPORTED surface — p4.9h's
     dependency) consumes it.
   - `lib/promise-timeout.ts` — NEW shared `withTimeout` (the pascal
     consumer is D30's; no other ported surface consumes it).
   - `lib/schemas/chat.types.ts`, `lib/query/fetcher.ts`,
     `lib/chat/staff-display-names.ts` client use, and the four component
     files — SPA-side, D34's.
2. **`345a858a refactor(api): route responses go through the shared response
   helpers`** — mechanical `NextResponse.json(x)` → `successResponse(x)` /
   `created(x)` across six ported route files (characters wardrobe item,
   chats, messages ×2, mount-points ×2, embedding-profiles).
   `successResponse` IS `NextResponse.json` (verified at planning —
   `lib/api/responses.ts:63-67`), so this is wire-neutral BY CONSTRUCTION;
   the sweep proves it.
3. **`55752ad4 refactor(api): finish the context-middleware rename, drop the
   vestigial auth surface`** — 213 files, 188 of them source: `lib/api/
   middleware/auth.ts` → `context.ts` (legacy aliases deleted:
   `AuthenticatedContext`, `withAuth*`, `createAuthenticated*Handler`,
   `checkOwnership`), `lib/auth/session.ts` + `single-user.ts` dead exports
   dropped, and every route's call sites renamed. Zero behavior; enormous
   oracle surface area. v5 has NO structural analog (its api layer is its
   own design) — sweep-only.
4. **`cc2e3c6f chore(dead-code): remove unreferenced exports found by the
   release sweep`** — deletions in 7 lib files: `highlightMessage`
   (message-navigation), `findCheapestAvailableModel` (pricing-fetcher),
   `getThemeBundleCacheDir` (paths), `MemoryCollection` (export/types),
   `DatabaseBackendFactory` (database/interfaces),
   `RegenerateConversationSummariesPayload` (queue-service),
   `AdoptableStore` (mount-index/ensure-official-store), plus icon-registry
   entries. v4 removed its own dead code — v5 FOLLOWS by removing any
   analogs it faithfully ported (the `v5-refactor-vestigial-v4-cruft` rule's
   happy ending), and records "never ported" for the rest. Surveyed at
   planning: `find_cheapest_available_model` has NO v5 analog (v5's
   `cheap_model.rs::get_cheapest_model` corresponds to v4's still-alive
   `getCheapestModel` — do not confuse them).

**NO-PORT dispositions this lane records (with one-line evidence each):**
`71dcc7e8` + `80cafed5` (v4-side test coverage only), `77c480d0` (release
notes), `ff12f491` (CI/tsconfig), `f46b0554` (sharp 0.35 type-only imports in
`lib/files/image-processing.ts` + a rebuilt pdf.worker — v5's image codec is
native and its pdf-extractor seam is a standing loud deferral; nothing to
port), `0b9320a3` (plugin-dist bundling — oracle-environment only), and
`33cca411` (CLI help text + shell completions — SURVEY FIRST: check whether
v5's `quilltap-cli` mirrors v4's help strings for the memories/docs
subcommands and whether the Tier R differential compares help output; the
`docs` CLI surface is a standing refusal-arm, so the expected outcome is
"bank with that deferral", but verify rather than assume).

## Mandate

Prove the four release-refactor commits output-neutral across every ported
family this lane owns (regen-and-re-run, D10-style), mirror the helper
extractions into v5 where a ported analog exists (structural de-drift, each
proven neutral by its family), follow v4's dead-code removals in v5, and
record the round's NO-PORT dispositions. Do NOT touch:
`crates/quilltap-core/src/pascal/**`, `api/custom_tools.rs`,
`services/backup/**`, `db/memories.rs`, `model/**`,
`quilltap-host/src/providers.rs`, `apps/web/**`, `api/types.rs`, or any
sibling-owned oracle family/corpus.

## Survey-verified starting points (2026-07-30)

- v5's transcript renderer (P4.d28's pure renderer — the 53-row byte
  differential `chat_export_equivalence`) carries the private staff-name
  map + fallback config the v4 extraction just deduplicated. Mirror:
  extract a `staff_display_names` module (Rust side; keep insertion order
  and the raw-tag fallback semantics EXACTLY — the transcript byte
  differential is the proof) and a `DEFAULT_TIMESTAMP_CONFIG` const.
- v5's recall-tags multipliers live in the ported memory-weighting family
  (see `crates/quilltap-core/src/memory_weighting*` / the recall-tags
  module — locate by the `occurred_within`/`fresh_event` fn names from
  P4.d26). Mirror `event_time_ms` and prove neutral via the weighting +
  recall-tags + distill-transitive families (TZ pins!).
- v5's `buildTurnTranscript` port (`turn_transcript` family, P4.6bj) and the
  two extraction handlers (`memory_extraction_job.rs`,
  `carina_memory_extraction.rs` — P4.15 corrected the stale note; both are
  live) may carry duplicate `resolve_user_character_participant` copies —
  mirror the move to the turn-transcript module if so.
- v5 already grew `if_table`/`if_table_opt` (P4.9G3) and `_conn` twins
  (P4.D25) for the introspection question — verify whether a shared
  `table_exists` helper already exists; mirror only if v5 genuinely
  duplicates it today. Do NOT re-signature anything that runs inside
  `write_blocking` (the P4.D25 deadlock lesson).
- Dead-code follow: grep v5 for analogs of each `cc2e3c6f` deletion
  (`highlight_message`, `find_cheapest_available_model`,
  `theme_bundle_cache_dir`, `MemoryCollection`, `DatabaseBackendFactory`,
  `RegenerateConversationSummariesPayload`/its Rust payload enum arm,
  `AdoptableStore`). Remove where present-and-unreferenced; record each
  verdict.

## Tiered deliverables

**Tier 1 (must land):**

1. **The neutrality sweep.** Regenerate FRESH from the `ff12f491` pin and
   re-run BY NAME every committed oracle family in
   `crates/quilltap-harness/tests/` EXCEPT the sibling-owned lists (D30: the
   pascal/tool/workbench families; D31: the backup/restore/remap/import
   families; D33: the request-builder/google/response-parse families and the
   three provider corpora). Every regenerated NDJSON grepped for a
   new-baseline marker (`oracle-regen-silent-stale-pass`); every family with
   a COMMITTED fixture pointed at the committed DBs; every distill-transitive
   family regenerated under its TZ pins (TZ=UTC + the America/Chicago legs
   where the recipe names them). Zero SKIP, zero failures. Report the family
   count in the lane record (the D10 sweep was 53; expect similar).
2. The Rust helper mirrors (staff display names, default timestamp config,
   `event_time_ms`, the turn-transcript participant resolver move), each
   proven neutral by its family re-run — the transcript one at the BYTE
   level. Any mirror that doesn't collapse cleanly in Rust: skip and record
   (structure is the goal, not churn).
3. The dead-code follow with per-item verdicts.
4. The NO-PORT disposition list (above) recorded in the lane record, each
   with its one-line evidence — including the `33cca411` CLI survey result.

**Tier 2 (should land):**

5. A stale-comment sweep: v5 comments citing v4 file paths that `55752ad4`
   renamed (`lib/api/middleware/auth.ts` → `context.ts`; the dropped
   `createAuthenticatedHandler` names). Update citations in v5 doc comments
   so future drift surveys don't chase ghosts. (Grep for `middleware/auth`
   and `createAuthenticated` under `crates/`.)

**Tier 3 (explicit deferrals, loud):** the `reapply-profile` consumer of the
new introspection helper stays with the UNPORTED `EMBEDDING_REAPPLY_PROFILE`
surface (p4.9h's banked dependency) — record, don't port.

## The differential requirement, restated

This lane's product IS the differential evidence: the sweep is the proof the
four commits were what they claimed. No new oracle cases are expected;
the bar is fresh-regen + by-name re-run + marker-grep for every family you
own. Where a mirror lands (tier-1 item 2), the family re-run after the
mirror is its equivalence test. Recipes: the D10 release-sweep record
(status-log), `oracle-regen-pinned-v4-worktree`,
`oracle-regen-silent-stale-pass`, and the committed-fixture regen rule in
CLAUDE.md's baseline section.

## Fixtures

- CONSUMES: every committed fixture its families read (pointed-at, never
  rebuilt).
- DELIVERS: none. If a fixture proves stale-poisoned during the sweep, STOP
  on that family and report rather than rebuilding a committed fixture
  unilaterally.

## Shared contract + Ownership (binding, identical in all five orders)

- **Round baseline `ff12f491`.** All oracle regen from the pinned worktree
  `/private/tmp/qt-v4-pin-ff12f491` (recipe `oracle-regen-pinned-v4-worktree`,
  including per-plugin `plugins/dist/*/node_modules` symlinks). Verify
  installed SDK majors (openai 7, `@openrouter/sdk` 1.2) before regen; STOP
  if stale. TZ pins apply to every distill-transitive family. Committed
  fixtures: point oracles at the committed DBs.
- **File ownership:** P4.D30 → `crates/quilltap-core/src/pascal/**` +
  `crates/quilltap-core/src/api/custom_tools.rs` + the pascal/tool/workbench
  families. P4.D31 → `crates/quilltap-core/src/services/backup/**` +
  `crates/quilltap-core/src/db/memories.rs` + the
  backup/restore/remap/import families. P4.D32 → the release-refactor
  helper mirrors OUTSIDE sibling-owned files + the broad neutrality regen
  sweep EXCLUDING the families the other four orders name. P4.D33 →
  `crates/quilltap-core/src/model/**` + `crates/quilltap-host/src/providers.rs`
  + the three provider corpora + the request-builder/response-parse
  families. P4.D34 → `apps/web/**` EXCLUSIVELY.
- **No lane edits `api/types.rs`.** None of this drift needs a wire change;
  if a lane finds otherwise, STOP and escalate to the human.
- **Version bumps:** D30–D33 bump `quilltap-core`/`quilltap-harness` as they
  touch them (own branch; the unifier recounts per the parallel-round
  playbook). D34 bumps the SPA version only.
- **`CHANGELOG` + `status-log.md` are append-only** (union-merge at
  unification). The baseline move in `CLAUDE.md`/`phase-4.md` is the
  unifier's alone.

## Verification gate (before declaring done)

- `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D
  warnings` on BOTH feature sets; `cargo build --release`.
- `cargo test --workspace --no-fail-fast` with ALL the sweep's env vars set
  — zero failures, zero SKIP lines on the swept families (list them by name
  in the lane record).
- Per-commit rule: follow `.claude/commands/commit.md`.
- Lane record appended to `status-log.md` (sweep family list + counts, the
  mirror verdicts, the disposition list, and the baseline-wording slice for
  the unifier).
