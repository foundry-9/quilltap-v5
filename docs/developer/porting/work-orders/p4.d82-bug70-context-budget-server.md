# Work order P4.D82 — the `93ed8abf` drift, bug-70 half: context budget honors the profile's Max Context (`resolveContextWindow`, `computeSafeInputLimit`, turn-extras token accounting) — v4 `93ed8abf`

**Status:** CLOSED on the lane branch `claude/p4-context-budget-server-7a3db5`
(2026-08-15) — Tier 1 (items 1–5) and Tier 2 (items 6–7) landed; Tier 3
(item 8) recorded as a rider, no code. Three commits; the full record is in
`status-log.md` (four P4.D82 sections — units 1, 2–3, 4, and the close notes
folded into them).

**Carried forward — the RIDER (item 8):** `f933ba9c` also fixed
`lib/services/external-prompt-generator.service.ts`, which has **NO v5 twin**
(the `generate-external-prompt` character action is unported; only the
courier's `pendingExternalPrompt` carry exists,
`services/courier_transport.rs:241–242`). **The future lane that ports
external-prompt-generator must carry the `f933ba9c` AND `d89babc4` edits to
that file.**

**Handed to P4.D83:** the entangled-family red set this order asked for is
**EMPTY** — every named family was regenerated fresh at the `93ed8abf` pin
(which includes `d89babc4`) and ran green; the sampling fields are not in
their compared surfaces. Details + the family list in the unit-4 record.

**Beyond the order, in the same unit (unit 4), with its reason:** the
tool-change notice's SPLICE and the `forceToolsOnNextMessage` clear were a
pre-existing v5 deferral; reserving room for a notice v5 never splices would
have been a NEW divergence, so the deferral closed here. Measured by a new
`orchestrator_tier3` op (mutation-proven on the flag clear).

## Preamble

- **v4 baseline for this round: `93ed8abf`** (v4 main HEAD at planning,
  2026-08-15 — "fix: local providers send the profile's parameters; OAC can
  call tools (bug 71)"). The round absorbs the three-commit drift past
  `aa464abf`: `f933ba9c` (bug 70, THIS lane), `d89babc4` + `93ed8abf`
  (sibling P4.D83). **Drift-check at lane start** per the two-branch rule
  (`git log 93ed8abf..main` AND `git diff main bugfix -- lib/ app/
  packages/ plugins/`; measure bugfix with `diff`, never the commit list).
  At planning the v4 tree was CLEAN on `main`, and bugfix carried nothing
  portable (tip `3a76b17d` is the 4.8.4 branch marker; `009c49b2` is a
  test-only deflake, part of the already-dispositioned 4.8.4 NO-PORT).
  **STOP and report** if HEAD moved or the tree is dirty; verify the
  checkout is on `main` before any regen, else pin a detached worktree
  (`oracle-regen-pinned-v4-worktree`, or `recipe_sweep.py --v4 <pin>`;
  remember ALL THREE symlink classes: root `node_modules`,
  `packages/quilltap/node_modules`, the `plugins/dist/*/node_modules`
  dirs).
- Sibling lanes this round: **P4.D83** (the `d89babc4` + `93ed8abf`
  provider-params/wire half — **STACKED ON THIS LANE'S BRANCH**, it starts
  from this lane's finished tip) and **P4.D84** (the SPA provider-options
  machinery, fully parallel, owns `apps/web/**`). Because P4.D83 stacks on
  you, your lane MUST finish with a clean, gated tip — no WIP commits, no
  uncommitted work.
- Work in your own worktree on a lane branch; never `git stash` across
  lanes. Repo-root paths exist in BOTH trees — edit inside the worktree
  (`edit-inside-the-worktree-not-main`).
- Per-commit rule: follow `.claude/commands/commit.md`. Version-bump
  ownership: this lane bumps `quilltap-core`, `quilltap-harness`, and
  (if `spine.rs` is edited) `quilltap-host`. Do not bump web/cli/tauri/SPA.

## Mandate

Absorb v4 `f933ba9c` ("fix: context budget honors the profile's Max
Context (bug 70)") whole: `resolveContextWindow` as the single source of
the context window (profile `maxContext` wins, name lookup is the
fallback, zero-or-negative falls through), `computeSafeInputLimit` as the
one owner of the builder/validator formula (ContextBudget carries
`safeInputLimit` + `safetyMargin`), and the new turn-extras accounting
(tool schemas + agent-mode instructions + the tool-change notice built
and measured BEFORE the context so the builder reserves room via
`reservedOutgoingTokens`; `countToolSchemaTokens` returns 0 on an
unserializable definition; the no-room-for-history case warns by name).
This lane owns the context/budget spine: `context_budget.rs`,
`model_context.rs`, `services/build_context.rs`, `token_estimation.rs`,
a NEW `services/turn_extras.rs` (or equivalently-placed module),
`services/orchestrator.rs`, `services/regenerate_swipe.rs`,
`services/message_context.rs`, `tools/self_inventory.rs`, and (if the
measurement requires) `quilltap-host/src/spine.rs`. It must NOT touch
`model/**` (request builders, decoders, transport, stream/completion
param types), `provider_manifest/**`, `cheap_llm.rs`,
`services/chat_create.rs`, `services/file_fallback.rs`, or
`apps/web/**` — P4.D83/P4.D84 own those (see Shared contract A).

**MEASURE, don't assume (`convergence-lane-measure-dont-assume`):** v5's
`calculate_max_available` already resolves profile-max-context-first
(`context_budget.rs:96–102`), so PART of v4's commit is v4 converging on
v5's shape. The genuinely-new parts (measured at planning): the
ALLOCATION path (`build_context.rs:1707` budgets from the raw model
window), the safe-input-limit unification, the warning path reading the
unfixed total, and the whole turn-extras accounting (green-field in v5).
Re-measure at lane start; record which arms are convergence vs. new.

## The drift, classified (surveyed FRESH 2026-08-15 at `93ed8abf`)

### v4 `f933ba9c` — what changed

- `lib/llm/model-context-data.ts`: NEW `resolveContextWindow` (`:154`,
  `ContextWindowSource` at `:129`), NEW `CONTEXT_SAFETY_MARGIN_RATIO =
  0.10` (`:171`) + `computeSafeInputLimit(totalLimit, responseReserve)`
  (`:188`); `getSafeInputLimit` (`:205`), `getRecommendedContextAllocation`
  (`:240`), `calculateMaxAvailable` (`:403`) all route through
  `resolveContextWindow`; `resolveMaxTokens` (`:371`) untouched in
  semantics (still deliberately ignores `parameters.max_tokens` — it sizes
  the response reserve, NOT the per-request cap; do not conflate with
  P4.D83's `resolveSamplingParams`).
- `lib/chat/context-manager.ts` (+94): `calculateContextBudget` takes the
  profile; `ContextBudget` carries `safeInputLimit` and `safetyMargin`;
  the builder fills to `computeSafeInputLimit`'s figure and the validator
  reads the SAME number (previously builder `total − reserve` vs validator
  `total − reserve − 10%` — an exactly-as-instructed context could be
  reported as an overage).
- NEW `lib/services/chat-message/turn-extras.ts` (114 lines):
  `extractToolNames` (`:30`), `buildToolChangeNotice` (`:45`),
  `TurnExtrasOptions` (`:53`), `TurnExtras` (`:64`),
  `collectTurnExtras(options)` (`:86`). Built once per turn from
  `{tools, agentMode, toolSettingsChanged, provider}`; returns the
  measured strings + `reservedTokens`; the SAME strings are spliced in at
  the splice sites (never rebuilt).
- `lib/services/chat-message/orchestrator.service.ts` (+53/−…):
  `collectTurnExtras` called before the context build; the context builder
  receives `reservedOutgoingTokens: turnExtras.reservedTokens`; the two
  splice sites (tool-change notice, agent-mode instructions) consume the
  measured strings; the pre-send estimate measures the WHOLE payload
  (messages + tool schemas), not just the message array.
- `lib/tokens/token-counter.ts` (+39): NEW `countToolSchemaTokens`
  (serialized form; returns 0 rather than throwing on an unserializable
  definition; shape is provider-specific — `{type, function:{...}}` for
  OpenAI-style).
- `lib/services/chat-message/context-builder.service.ts` (+11): consumes
  `reservedOutgoingTokens`; the large-character/small-window
  no-room-for-history case warns BY NAME instead of silently sending a
  character with no memory of the exchange.
- `lib/tools/handlers/self-inventory/builders.ts` (+5/−…): routes through
  `resolveContextWindow`.
- `lib/services/external-prompt-generator.service.ts` (+2/−…): same
  blind spot fixed. **v5 disposition: NO TWIN EXISTS** — the
  `generate-external-prompt` character action is an unported, disabled
  surface in v5 (only the courier's `pendingExternalPrompt` carry exists,
  `services/courier_transport.rs:241–242`). Record this as a RIDER in the
  lane record + status header: the future lane that ports
  external-prompt-generator must carry the `f933ba9c` + `d89babc4` edits.
- For the reported case: budget 8192 → 65536, history allowance ~1.5k →
  32768, and the spurious "Conversation is getting long" stops.
- `__tests__/unit/lib/services/chat-message/turn-extras.test.ts` (141
  lines) — mine it for the differential corpus shapes.

### v5 starting points (all verified 2026-08-15)

- `crates/quilltap-core/src/context_budget.rs` (180 lines; ports
  `model-context-data.ts`'s arithmetic half): `DEFAULT_MAX_CONTEXT =
  128000` (`:20`), `DEFAULT_MAX_TOKENS = 8000` (`:22`),
  `MIN_MAX_AVAILABLE = 4096` (`:24`), `resolve_max_tokens` (`:57`),
  `MaxAvailable` (`:76`), `calculate_max_available` (`:90`; the
  profile-first resolve at `:96–102`; the 20 % cap + floor at
  `:106–107`), `get_safe_input_limit(total, max_response)` (`:118` —
  the arithmetic form, **zero production callers**, harness only),
  `has_extended_context` (`:125`), `ContextAllocation` (`:133`),
  `get_recommended_context_allocation(total_limit)` (`:146` — raw total
  only, no profile: **the bug-70 seam's v5 half**).
- `crates/quilltap-core/src/model_context.rs` (174 lines; the lookup
  half): `model_context_override` (`:33`), `default_context_by_provider`
  (`:61`), `id_matches` (`:77`), `get_model_context_limit` (`:91`;
  8192-sentinel check `:127`; terminal `unwrap_or(8192)` `:131`),
  provider/model forms of `has_extended_context` (`:136`) and
  `get_safe_input_limit` (`:154`, duplicating the `:118` arithmetic).
  **No `resolveContextWindow` twin exists in either file — new symbol;
  v4 keeps both halves in ONE file, so v4's new functions straddle these
  two v5 files. Pick placements that keep each file's v4 module citation
  honest, and record the mapping in the module headers.**
- `crates/quilltap-core/src/services/build_context.rs`: the SEAM —
  `:1707` computes `get_recommended_context_allocation(input.
  model_context_limit)` from the raw model window; only `:1840–1847`
  consults `p.max_context` via `calculate_max_available`. So the
  profile's Max Context governs compression/history/memory but NEVER the
  `ContextBudget` allocation. Also: the serialized `ContextBudget` struct
  at `:224–236` (`recent_messages_budget: f64` js-number serialized at
  `:233`) — v4's fix ADDS `safeInputLimit` + `safetyMargin` here;
  `BuildContextInput.model_context_limit` (`:449`),
  `ConnectionProfileInput { max_context, max_tokens, model_class }`
  (`:400–405`), `input.connection_profile` (`:472`);
  `tool_instructions: Option<String>` (`:467`) folded into the system
  prompt at `:1811`; the warning path `:2759–2769` reads
  `budget.total_limit` (the unfixed total) and pushes "Conversation is
  getting long. Consider generating a summary for better context
  management." at `:2765`; the autonomous-room clamp `:1857–1861`.
- `crates/quilltap-core/src/services/orchestrator.rs`: `process_message`
  (`:875`); `build_input` at `:2083–2085`; the `build_context` call at
  `:2249–2262`; `build_context_input(args)` (`:3684`;
  `model_context_limit` set at `:3735`, `connection_profile` at `:3757`);
  `effective_profile_profile` (`:3902–3908` — the
  `maxContext`/`maxTokens`/`modelClass` JSON reads);
  `cheap_llm_profile_from_value` (`:3670–3677`). The native tool schemas
  that go on the wire are `actual_tools_value` (`:2421`) — **never
  token-counted today**; turn-extras is green-field. ⚠ The sampling reads
  at `:2400–2427` (`model_params` → `StreamParams`) are P4.D83's — do
  not touch them (Contract A).
- `crates/quilltap-core/src/services/regenerate_swipe.rs`: the second
  `effective_profile_profile` → `build_context_input` consumer
  (`:245–258`; `model_context_limit` at `:61`/`:135`/`:248`). ⚠ The
  sampling extraction at `:415–438` is P4.D83's.
- `crates/quilltap-core/src/token_estimation.rs`: `estimate_tokens`
  (`:28`), `count_messages_tokens` (`:46`), `DEFAULT_CHARS_PER_TOKEN =
  3.5` (`:15`). `countToolSchemaTokens` twin lands here.
- `crates/quilltap-core/src/tools/self_inventory.rs`: the
  `model_context_limit` wrapper (`:113–120`); the last-turn
  `context_window` + utilization (`:1565–1570`); **`:1634–1638` already
  does the profile-first resolve** (`profile.maxContext ??
  getModelContextLimit(...)`) — route through the new single source per
  v4's builders.ts change.
- `crates/quilltap-host/src/spine.rs`: `registry_inputs()`
  (`:996–1014`) calls `get_model_context_limit`; **`:1013` returns
  `(8192, false)` when profile resolution fails — the only 8192 fallback
  on the live path.** Threaded at `:1085/:1104`, `:1176/:1302/:1376`,
  `:1544/:1654`. Measure whether v4's fix moves this site (v4 resolves
  the window downstream of the equivalent seam); edit only if the
  measurement says so.
- `crates/quilltap-core/src/services/message_context.rs:634` — secondary
  budget consumer; verify against v4's change set.
- Enclave: `enclave/step.rs:163/:699/:1708` carry `model_context_limit` —
  verify the fix reaches the enclave path (v4's buildContext is shared).

## Tiered deliverables

**Tier 1 (must land):**

1. `resolveContextWindow` twin + `ContextWindowSource`, single-sourcing
   the window (profile-first; zero-or-negative falls through; name lookup
   fallback), with `get_recommended_context_allocation`,
   `get_safe_input_limit` (BOTH forms), `calculate_max_available`, and the
   self-inventory builder routed through it. Signature changes ripple to
   `build_context.rs:1707` (the allocation finally sees the profile) and
   the warning path.
2. `computeSafeInputLimit` + `CONTEXT_SAFETY_MARGIN_RATIO`; the
   `ContextBudget` struct gains `safeInputLimit`/`safetyMargin` (serialized
   names per v4 — mind `json-column-key-order` and js-number
   serialization); builder and validator read the ONE number.
3. `turn_extras.rs`: `collect_turn_extras` + `extract_tool_names` +
   `build_tool_change_notice`, byte-exact strings vs v4 (the tool-change
   notice sentences are contractual UI-adjacent strings); orchestrator
   builds it BEFORE the context, passes `reservedOutgoingTokens`, splices
   the measured strings; the whole-payload pre-send estimate.
4. `count_tool_schema_tokens` in `token_estimation.rs` (0 on
   unserializable).
5. The no-room-for-history named warning in the context builder.

**Tier 2 (should land):**

6. The enclave-path verification (does the fixed budget reach
   `enclave/step.rs`'s builds identically to v4's shared buildContext?)
   with a pin if any arm differs.
7. The `spine.rs:1013` measurement + fix-or-record.

**Tier 3 (explicit refusal-arm deferrals):**

8. The external-prompt-generator rider: NO code — record loudly (status
   header + status-log + the future lane's pointer). Never stub it.

## The differential requirement (restated)

Every ported unit ships with an equivalence test driving v4's REAL code
at the `93ed8abf` pin (jest/tsx oracle → NDJSON → Rust diff). Families
this lane owns and must leave green, with recipes per
`recipe-header-conventions` / the sweep driver:

- **`context_budget`** (`harness/oracle/cases/context-budget.ts` →
  `tests/context_budget_equivalence.rs:120`, `QT_ORACLE_CONTEXT_BUDGET`).
  The oracle case's generator signatures CHANGE: `:116`
  `getRecommendedContextAllocation(provider, model)` and `:127`
  `getSafeInputLimit(provider, model, maxResp)` carry no profile — add
  profile fields to the `allocation`/`safeInput`/`hasExtended` row kinds
  (typed `:50–52`) and NEW row kinds for `resolveContextWindow` +
  `computeSafeInputLimit` (cover: profile wins; zero; negative; absent;
  name-lookup fallback; the 8192 sentinel; Infinity/huge values — mind
  the recorded Infinity-maxContext Value-path note from the aa464abf
  round). Mutation-prove the new rows (plant a wrong fallback order).
- **`model_context`** (`cases/model-context.ts` →
  `tests/model_context_equivalence.rs:70`, `QT_ORACLE_MODEL_CONTEXT`).
- **`token-estimation`** (`cases/token-estimation.ts` →
  `tests/token_estimation_equivalence.rs:78`,
  `QT_ORACLE_TOKEN_ESTIMATION`): grow rows for `countToolSchemaTokens`
  (incl. the unserializable→0 arm; mine v4's `turn-extras.test.ts` +
  `token-counter.ts` for shapes).
- **A turn-extras tier-1 family** (new case file driving v4's REAL
  `collectTurnExtras`/`buildToolChangeNotice` over a corpus: 0 tools /
  n tools / agent mode on-off / toolSettingsChanged / provider variants;
  strings compared EXACTLY).
- **`build-context-tier3`** (`cases/build-context-tier3.test.ts` +
  fixture builder, `QT_ORACLE_BUILD_CONTEXT`, `QT_FIXTURE_BC_MAIN/_MOUNT`):
  regenerate at the pin — the corpus sets `model_limit() = 200_000`
  (`build_context_tier3_equivalence.rs:848`) while compression ops give
  `max_context: Some(8000)` (`:577`), and `BuiltContext.budget` is
  compared whole (`:765–774`), so EVERY allocation field shifts from the
  200k tier to the 8k tier when the fix lands — the family is the fix's
  sharpest witness. Also add a case exercising `reservedOutgoingTokens`
  + the named no-room warning.
- **`self-inventory`** (`QT_ORACLE_SELFINV` + three fixtures): regen +
  re-run.
- **Entangled families — DO NOT expect green in this lane:**
  `orchestrator-tier3`, `regenerate-swipe`, `primary_stream`,
  `chats-tokens-tier2`/`chat-context-init` (if budget-sensitive). A
  v4-at-pin oracle carries the `d89babc4` sampling fix this lane does not
  port (P4.D83 does, stacked on you). Regenerate them at the pin, run
  them, and RECORD the red set — every red must be attributable to the
  sampling/params fields (`temperature`/`max_tokens`/`top_p`/
  `profile_parameters` wire reads) and NOTHING else. An unattributable
  red is a lane bug: stop and fix. Hand the recorded red set to P4.D83
  in your status header; P4.D83 flips them green.
- Neutrality: `context-compression`, `message-context-leaves`,
  `context-feeders-leaves` re-run at the pin (budget signature ripples).

Oracle regen only through the sweep driver against a detached worktree
pinned at `93ed8abf` (`recipe_sweep.py --v4 <pin-path>`; python3.13;
never two sweeps concurrently — coordinate with P4.D83's schedule, which
starts after you finish, so no overlap exists by construction).

## Fixtures

- Consumes: `build-context-tier3` fixture pair, `orchestrator`/`regen`
  fixture .dbs, self-inventory fixtures — regenerating dependent oracles
  at the pin is THIS lane's job for the families it owns.
- Delivers: any new corpus rows ride the existing committed corpora
  (`build-context-tier3.json` etc.); assert SHAPE, not hand counts
  (`harness-corpus-shape-constants-rot`); mind
  `blinded-comparand-hides-the-new-arm` — classify every new field into a
  normalizer bucket deliberately, and plant it wrong once.

## Shared contract A (binding; identical text in P4.D82 and P4.D83)

**The stacking seam.** P4.D82 owns and may edit: `context_budget.rs`,
`model_context.rs`, `services/build_context.rs`, `services/turn_extras.rs`
(new), `services/message_context.rs`, `token_estimation.rs`,
`tools/self_inventory.rs`, `services/orchestrator.rs`,
`services/regenerate_swipe.rs`, `quilltap-host/src/spine.rs`, enclave
files. Within `orchestrator.rs` and `regenerate_swipe.rs`, P4.D82 must
NOT touch the sampling reads (`orchestrator.rs:2400–2427` `model_params`
→ `StreamParams`; `regenerate_swipe.rs:415–438` extraction +
`CompletionParams` construction) — P4.D83 rewrites those on top. P4.D82
must not touch `model/**`, `provider_manifest/**`, `cheap_llm.rs`
(`profile_params_parts` at `:136` is read-only for both lanes — 900-case
pinned), `services/chat_create.rs`, `services/file_fallback.rs`,
`apps/web/**`. P4.D83 branches FROM P4.D82's finished tip and must not
rework P4.D82's budget code beyond what compilation of its own changes
requires (any such touch is named in its lane record). Parameter-key
names are pinned round-wide: the profile `parameters` bag's canonical
sampling keys are `temperature` / `max_tokens` / `top_p` (snake_case;
camelCase tolerated read-side only inside P4.D83's resolver), and the
Ollama control keys are `enable_thinking`, `thinking_effort`,
`keep_alive`, `request_timeout_seconds`, `num_ctx`.

## Verification gate (before declaring done)

- `cargo fmt --all --check`; clippy BOTH feature sets; `cargo build
  --release`.
- `cargo test --workspace` with the lane's env block — zero failures,
  zero `SKIP:` lines in the lane's families
  (`cargo-test-workspace-is-fail-fast`).
- The lane's owned families regenerated fresh at the `93ed8abf` pin and
  re-run BY NAME (`--nocapture`, grep for SKIP); the entangled families'
  red set recorded and fully attributed as specified above.
- No SPA work → no ng/Playwright requirement, but do not break the
  existing suites (spot-run `ng build` if any contract type moved — it
  should not).
- CHANGELOG + status-log entries appended (union-merge etiquette);
  version bumps per Preamble; every commit per
  `.claude/commands/commit.md`.
