# Work order P4.D125 — the `664cfca84` + `f3892158d` client half: the chips rework, the realtime hub over v5's event stream, the topic map, the shared clock, the polling-site migrations

**Status:** OPEN.

## Preamble

- **v4 baseline for this round: `b220999da`** (v4 main). This order is
  the Angular client half of BOTH drift commits: `664cfca84` (chips /
  heartbeat / pulse; server half in sibling P4.D123) and `f3892158d`
  (realtime hub / `useNow` / migrations; server half in P4.D124, stacked
  after P4.D123 in one server lane). At planning (2026-08-26) the v4
  checkout was on `main`, tree CLEAN, HEAD `f3892158d`.
- **Run the drift ledger's §2 freshness probe at lane start**
  (`docs/developer/porting/drift-ledger.md`); STOP and report on any
  mismatch — a lane never writes the ledger.
- **Regen rule in force: pin required** (ledger §1). This lane's spec
  reads are `git show 664cfca84:<path>` / `git show f3892158d:<path>`
  per feature (the badges component is rewritten by BOTH commits — port
  its FINAL `f3892158d` state, but attribute cadence/pulse semantics to
  the right commit when recording). If the lane runs any oracle regen
  (unlikely — this is a client lane), it builds its own lane-unique pin
  at `f3892158d` per drift-ledger §5.1; never share the server lane's
  pin (`oracle-regen-pinned-worktree` / P4.d26 deletion trap).
  **Read `docs/developer/features/complete/realtime-updates.md` (inside
  `f3892158d`) first** — decisions 1–8 and the divergence section govern
  the client semantics too.
- **Sibling lanes:** P4.D123+P4.D124 (one stacked server lane; meets
  this lane ONLY at the §Shared contract). §Ownership below is binding
  and identical in all three orders.
- **Worktree:** per the `carryout` skill; edit inside the worktree
  (`edit-inside-the-worktree-not-main.md`). SPA worktree node_modules:
  `npm ci`, never `cp -a` (`spa-worktree-node-modules.md`).

## Mandate

Port the client half of both commits into `apps/web`: the **queue-chip
rework** (TanStack query, adaptive 1.5 s/8 s cadence, `activeByKind` per
§Shared contract §A, the `startedByKind` pulse, the transcribed chip
metadata, retirement of the `QUEUE_CHANGE_EVENT` hand-rolled poller),
the **realtime hub** over v5's existing event stream per §Shared
contract §B (connection status, topic subscriptions, the
invalidate-everything catch-up, gated fallback polling), the **topic →
query-key map** over v5's ACTUAL per-feature keys, the **shared clock**
(the `useNow` twin + `nowMs` params + `formatRelativeAge`), and the
**per-site poller migrations** (tasks queue + toggle rename, story
background, the memory cards, autonomous badges/card, the Salon avatar
watch, the character-conversations Scriptorium watch, StartupProgress).
Every ported unit ships with parity specs against v4's client code and
mutation proofs; e2e beats that need the server half land gated
ACTIVATE-AT-UNIFY.

**Must NOT touch:** anything under `crates/` or `harness/` (the server
lane owns both). No invented UI: strings, cadences, and thresholds come
from v4's files. v4's `help/system-tasks-queue.md` rows are banked by
the server lane; don't double-bank.

## Survey-verified starting points (fresh survey 2026-08-26)

### v4 — the chips, final state (`components/layout/queue-status-badges.tsx` at `f3892158d`, 301→213-line rewrite across the two commits)

- A TanStack query on `queryKeys.system.jobs`; plain GET (NO
  `includeByType` — the chips read `activeByKind`/`startedByKind` only).
- `refetchInterval` in **function form**, reading `query.state.data`:
  busy (any `activeByKind` count > 0) → `BUSY_POLL_INTERVAL = 1_500`;
  idle → `IDLE_POLL_INTERVAL = 8_000`; gated on realtime connection per
  §Shared contract §B.6 (v4: `false` while connected — but note v4
  KEEPS the chips polling…: read the component's actual gating in the
  hunks — the spec's Phase-3 note says the chips retain their adaptive
  cadence as the fallback; port what the code does, not the prose).
- The pulse: compare `startedByKind` against the PREVIOUS read per kind;
  any advance → set a pulse flag for that chip, cleared on a timer
  (`pulseTimersRef`, cleared on unmount); CSS `qt-queue-badge-pulse` =
  `animation: qt-queue-badge-blip 600ms ease-in-out 2` where the
  keyframes swing opacity from `var(--qt-queue-badge-idle-opacity)` to 1
  (transcribe from `app/styles/qt-components/_content.css` hunks).
  First read is a DELTA BASE, never a pulse (server restart zeros the
  totals — treat a DECREASE as a reset, not an advance).
- Chip metadata: transcribe `ACTIVITY_CHIPS` from
  `lib/background-jobs/activity-kinds.ts` at `664cfca84` — five chips in
  render order (Mem/Emb/Sum/Dgr/Img), the NEW title strings, and the
  badgeClass column including the `image`→`qt-queue-badge-story` quirk.
- `coerceCounts` tolerates unknown/absent maps (`?? {}` shapes) — the
  chips must render zeros against an old server.

### v4 — the realtime client legs this lane adapts (at `f3892158d`)

- `lib/realtime/client.ts` (224 lines): one socket per tab,
  `BACKOFF_MIN_MS=1_000` → `BACKOFF_MAX_MS=30_000` jittered
  (`min + rand*(min(max, min*2^attempt) - min)`), `PING_INTERVAL_MS =
  30_000`, visibility-aware (hidden tab doesn't retry; visibility
  handler reconnects), Zod-parsed frames, subscriber set + status
  listeners. **Mechanism divergence (documented):** v5 consumes the
  EXISTING event stream — SSE `GET /api/events` in HTTP mode (browser
  `EventSource` auto-reconnects; frames carry `id:` so a gap =
  missed-events signal), `quilltap://event` + `quilltap://resync` in
  Tauri (pump in-process ⇒ effectively always connected). Port the
  OBSERVABLE behaviors: a `connected` status the gating reads,
  hint-frame discrimination per §B.5, unknown-topic tolerance, and the
  catch-up sweep on EVERY (re)connect/resync. The ping and the
  hand-rolled backoff are WS-protocol legs — NO-PORT where the
  transport already provides the equivalent; record per leg. ⚠ Survey
  first: find v5's existing event-stream consumer in `apps/web`
  (the Green-Room/creation-progress frames already arrive somewhere —
  locate the service that subscribes, and whether it's SSE-persistent
  in both modes); extend it rather than opening a SECOND stream
  (v4 decision 2: one connection per tab).
- `lib/realtime/topic-map.ts` (91 lines): the topic switch of §B.3 →
  key prefixes; id-scoped narrowing (chats → detail/state/background;
  projects → same trio; characters → detail/prompts/photos;
  mountPoints → namespace-wide, no detail key exists); unknown topic →
  `[]`. `ALL_REALTIME_PREFIXES` drives the reconnect sweep.
- `hooks/useRealtime.ts` (93 lines): `useRealtimeConnected()`;
  `useRealtimeRefetchInterval(pollMs)` → `connected ? false : pollMs`
  (`false` passes through); `useRealtimeTopic(topic, onChange, id?)` —
  fires on topic match where `id` matches OR the event is
  collection-wide (no id); ALSO fires `onChange` on connect (`onOpen`).
  Angular twins: signals + effects, semantics 1:1; parity specs.
- `components/providers/realtime-provider.tsx` (58 lines) +
  `session-provider.tsx` (+3): mount + invalidate every mapped prefix on
  connect. v5 twin: app-bootstrap wiring in `app.config.ts`/shell.
- `hooks/useNow.ts` (207 lines): ONE timer per granularity however many
  subscribers; boundary-aligned firing (`granularity - (now %
  granularity) + 1` ms; `DAY_GRANULARITY_MS = 86_400_000` aligns to
  LOCAL midnight); sub-minute granularities pause while hidden
  (`HIDDEN_TAB_PAUSE_BELOW_MS`); an `enabled` flag (disabled neither
  subscribes nor re-renders); SSR-inert. v5 twin: a signal-based
  service; port v4's `__tests__/unit/hooks/useNow.test.tsx` semantics
  (137 lines) as the spec base.
- `lib/format-time.ts`: `formatRelativeDate` + `formatChatListDate` gain
  `nowMs: number = Date.now()` (pure parameterization, output
  unchanged); NEW `formatRelativeAge(ts, nowMs)` — `<2s` → `just now`,
  `<60s` → `${s}s ago`, else `${m}m ago`, floor at 0 (transcribe).

### v4 — the migrated sites (Phase-4 of the spec; port each from its hunks)

| v4 site | change | v5 twin (verified 2026-08-26) |
|---|---|---|
| `components/layout/queue-status-badges.tsx` | the whole rework above | `apps/web/src/app/layout/queue-status-badges.ts` (class `:52`, 5 s `setInterval` poll `:21,:110`, reads `activeByType` `:98-99`, `QUEUE_CHANGE_EVENT` listener `:68`, NavigationEnd refire `:72-77`) + `layout/queue-status.logic.ts` (`QUEUE_TYPES` `:34` — REPLACED by the kind-keyed chips; `notifyQueueChange` `:19` — RETIRED, see below) + mount at `layout/page-toolbar.ts:42` |
| `components/tools/tasks-queue/*` | gated fallback interval; **toggle relabeled "Fallback polling (5s)"** (same switch, honest name); `useNow` for ages | `apps/web/src/app/screens/settings/system/` tasks-queue family (`tasks-queue.api.ts` — `tasksQueueKeys:93`, `formatRelativeDate:75`) |
| `components/chat/ChatCard.tsx` | `useNow(DAY_GRANULARITY_MS)` for the Today→Yesterday rollover | the chat-card/date sites reading `formatChatListDate` — `screens/characters/view/tabs/character-conversation-card.ts:17,:165` (+ sweep other consumers) |
| `components/chat/MergeConversationModal.tsx` | `useNow` for the picker's ages | `apps/web/src/app/chat/merge-conversation-modal.ts` (already imports `formatRelativeDate` at `:18`) |
| `components/layout/autonomous-room-badges.tsx` | realtime-gated poll + `useNow` with the `enabled` flag (second hand only while a time-budgeted room runs; drops the bespoke 1 s interval) | the autonomous badges/card family (`apps/web/src/app/autonomous/`, `autonomous.logic.ts:286` `formatTime`) |
| `components/tools/autonomous-rooms-card.tsx` | gated poll | same family |
| `components/tools/memory-backfill-card.tsx`, `memory-regenerate-card.tsx`, `conversation-summary-regenerate-card.tsx` | `useRealtimeTopic('jobs', …)` as the live path, interval kept as fallback | `screens/settings/memory/…` (regenerate-card `:131`, summary-card `:90` are today's `notifyQueueChange` callers) |
| `hooks/useStoryBackground.ts` | BOTH loops gated (the 30 s sweep and the 3-minute active loop) | `screens/salon/story-background.api.ts:29` + its consumers |
| `app/salon/[id]/SalonView.tsx` | the avatar watch becomes DECLARATIVE state (push + fallback interval + timeout reading one "is a watch active" answer) | the Salon avatar-watch site (locate in `screens/salon/`) |
| `components/character/character-conversations-tab.tsx` | Scriptorium watch declarative + a NEW five-minute expiry (a render that never reached `embedded` used to poll forever) | `screens/characters/view/tabs/` conversations tab |
| `components/loading/StartupProgress.tsx` | KEEPS polling deliberately; `formatRelativeAge` moves to `lib/format-time.ts`; `useNow(1_000)` | `apps/web/src/app/startup/` |
| `components/tools/tasks-queue/README.md` | v4-doc | NO-PORT |

- **`notifyQueueChange` retirement:** v5's four caller sites
  (`conversation-summary-regenerate-card.ts:90`,
  `memory-regenerate-card.ts:131`, `embedding-profile-list.ts:310,340,369`,
  `embedding-profile-modal.ts:406`) switch to invalidating the jobs
  query key (v4's shape: the TanStack cache IS the signal). The
  `QUEUE_CHANGE_EVENT` window-event machinery and the NavigationEnd
  stop/refire go with it — but sweep ALL consumers before deleting
  (`queue-status-badges.spec.ts:116` covers the wake-a-stopped-poller
  case that no longer exists; rewrite the spec family to the new
  cadence semantics, don't weaken it).

### v5 — key-registry reality (the topic map's targets)

v5 has **no central `lib/query/keys.ts`**; keys are per-feature consts
(verified): `memoryKeys` (`memory/memory.api.ts:26`), `tasksQueueKeys`
(`tasks-queue.api.ts:93` — `all: ['tasksQueue']` only), `projectKeys`
(`screens/prospero/projects.api.ts:19`), `characterKeys`/`tagKeys`
(`screens/characters/characters.api.ts:40,:58`), plus ~20 more `*.api.ts`
key consts, AND raw inline arrays at several sites —
`chat/merge-conversation-modal.ts:259,268,371` (`['chats',…]`,
`['chat',…]`), `chat/select-llm-profile-dialog.ts:151`,
`chat/sidebar/chat-section.ts:410`. **The topic map targets v5's ACTUAL
spellings** (decision 8's twin): build the map over the existing consts,
create a `systemJobs`-style key const for the chips' new query, and for
the `chats` topic first SWEEP the raw spellings into a small exported
const (within this lane's ownership) so the map and the call sites
cannot drift — record the sweep's site list. A topic with no live v5
key family yet (e.g. `mountPoints` surfaces that don't query) maps to
whatever exists; document each row's target next to the row, v4-style.

## Tiered deliverables

**Tier 1 (must land):**

1. The chips rework, final state (§survey), on §Shared contract §A —
   including the pulse CSS (mind `check-qt-classes`:
   `qt-class-guard-false-positives.md`) and the transcribed chip
   metadata.
2. The realtime hub over the existing stream: `connected` status,
   §B.5 discrimination, unknown-topic tolerance, catch-up sweep on
   (re)connect + SSE id-gap + `quilltap://resync`; both host modes.
3. The topic map + the `useRealtimeRefetchInterval` /
   `useRealtimeTopic` twins, parity-specced.
4. The shared clock service (+ `enabled` flag, boundary alignment,
   hidden-tab pause) and the `format-time` changes
   (`nowMs` params, `formatRelativeAge`).
5. The tasks-queue migration incl. the toggle rename ("Fallback polling
   (5s)" — v4's exact string), and the chips/memory-cards
   `notifyQueueChange` retirement.
6. Parity specs + mutation proofs per §below; the deterministic e2e
   beats (chips render + pulse from mocked snapshots; a hint injected
   via the EventSource-injection idiom —
   `e2e-inject-wire-bytes-via-eventsource.md`).

**Tier 2 (should land):**

7. The remaining site migrations (story background, autonomous
   badges/card, Salon avatar watch declarative, conversations-tab watch
   + five-minute expiry, StartupProgress + `formatRelativeAge`,
   MergeConversationModal + ChatCard-twin day rollover).
8. A live e2e beat driving a real enqueue → chip lights without a
   manual refetch — **gated ACTIVATE-AT-UNIFY** (needs the server
   lane's hints; flip the gate constant at unification per the standing
   pattern).

**Tier 3 (explicit deferrals, loud):**

9. Any v4-migrated site whose v5 surface doesn't exist yet — record by
   name in the lane record (expected candidates: none known; verify).

## The differential/parity requirement, restated

This is a client lane: the oracle is **v4's client source at the pin**,
per the established v4-client parity pattern (P4.D96's vocative twin,
P4.D57's `isUserDrivenSeat` twins). Specifically:

- Transcription parity specs for: the chip metadata table, the topic
  map's switch semantics (adapted targets, v4 structure), the
  `formatRelativeAge` / `nowMs` math (drive both twins' edge inputs:
  the `<2s` boundary, the 59/60 s boundary, day-rollover at local
  midnight — TZ-pinned per the jest-TZ discipline),
  the backoff/gating semantics you keep.
- `useNow`: port v4's own test semantics (137 lines) — single shared
  timer per granularity, boundary alignment (+1 ms), pause/resume on
  visibility, `enabled` inertness. Beware
  `testbed-injectquery-needs-a-macrotask-flush.md` and
  `dom-rebuild-refires-mouseenter.md` for the chip pulse specs.
- The pulse logic: unit-spec the delta detection (advance → pulse;
  first read → no pulse; decrease/reset → no pulse; timer cleanup on
  destroy), mutation-proven (invert the delta test → exactly the pulse
  spec reds).
- Every retired poller: a spec asserting the OLD mechanism is gone
  (no `QUEUE_CHANGE_EVENT` listener, no bespoke interval) AND the new
  path fires — never delete a covering spec without replacing its
  assertion at equal strength.

## Fixtures

None (no DB fixtures; mocked HTTP snapshots live in the specs). The e2e
beats reuse the committed e2e fixture family unchanged.

## Shared contract (BINDING — identical text in `p4.d123-jobs-activity-server.md` and `p4.d124-realtime-server.md`)

**§A The jobs snapshot** (P4.D123 serves it; P4.D125 reads it):

1. `GET /api/v1/system/jobs` (and the dispatch verb behind it) answers,
   ALWAYS: `stats` (unchanged), `activeByKind`, `startedByKind`,
   `processor` (unchanged).
2. `activeByKind` and `startedByKind` are objects with EXACTLY the five
   keys, in this order: `memory`, `embedding`, `summary`, `danger`,
   `image`; integer values ≥ 0. `activeByKind` = PENDING+PROCESSING job
   rows mapped through the kind table PLUS in-flight registry counts.
   `startedByKind` = monotonic per-kind totals of ended spans ≥ 250 ms,
   since process boot (a fresh server answers zeros; the client treats it
   as a delta base, never absolute).
3. `activeByType` is present iff `includeByType=true` OR
   `includeJobs=true` (v4's `includeByType = param === 'true' ||
   includeJobs`); same shape as before this round.
4. The five kind ids above are the server↔client join; chip labels,
   titles, and CSS classes are client-side (P4.D125 transcribes v4's
   `ACTIVITY_CHIPS`, including the `image`→`qt-queue-badge-story`
   badgeClass quirk).

**§B The realtime hint** (P4.D124 emits it; P4.D125 consumes it):

1. A NEW `EventPayload` variant (working name
   `EventPayload::Realtime(RealtimeHint)`) rides v5's EXISTING event
   channel — the engine broadcast → SSE `GET /api/events`
   (`quilltap-web/src/events.rs`) in HTTP mode, → `quilltap://event`
   (`quilltap-tauri/src/events_pump.rs`) in the shell. **No new
   WebSocket endpoint** — this is a documented mechanism divergence from
   v4's `/api/v1/system/realtime/stream` socket, mandated by the locked
   transport-agnostic boundary ("streaming only ever on the Event
   channel"); record it in the round's divergence notes with this
   rationale.
2. Wire bytes (the `Event` flatten): `{"v":1,"topic":"<topic>"}` plus
   `"id":"<id>"` ONLY when scoped (omitted, never null — v4's spread)
   and `"at":<server-ms>`; no `chat_id`/`room_id`/`progress_id` keys.
   `at` is debugging-only — clients must not order, dedupe, or expire on
   it.
3. Topics, verbatim and closed for this round: `jobs`,
   `autonomousRooms`, `chats`, `projects`, `characters`, `mountPoints`.
   The client IGNORES unknown topics (an older tab must survive a newer
   server).
4. The server coalesces per `topic` / `topic:id` with a 250 ms
   trailing-edge debounce. Clients must tolerate BOTH missed hints (any
   (re)connect, SSE `id` gap, or Tauri `quilltap://resync` ⇒ invalidate
   every mapped prefix) and duplicate hints (invalidation is
   idempotent).
5. Client discrimination on the shared stream: a frame is a hint iff it
   carries both `topic` and `v` (chat/creation frames carry `type` /
   their own keys; the server lane verifies the untagged serde round
   trip stays unambiguous).
6. Fallback polling survives everywhere, gated on channel health: every
   migrated site keeps its original cadence wired but returns
   `false`/long while the channel is healthy (v4 decision 5). In the
   Tauri shell the pump is in-process — "connected" is true whenever
   the listener is attached.

## Ownership (BINDING — identical table in all three orders)

| Lane | Owns (exclusive) | Version bumps |
|---|---|---|
| P4.D123+P4.D124 (one stacked lane) | `crates/**` (quilltap-core, quilltap-web, quilltap-harness, quilltap-host, quilltap-tauri as needed), `harness/oracle/**`, `harness/tools/**` | core, harness, web; host/tauri only if touched |
| P4.D125 | `apps/web/**` (all SPA source, styles, specs) + its e2e specs under the repo's Playwright tree | SPA |

`CHANGELOG.md` + `status-log.md` append-only for everyone; nobody edits
`drift-ledger.md` from a lane. The unifier recounts version bumps (the
standing silent-auto-merge trap — playbook). Only P4.D125 and the unifier
run Playwright (`e2e-port-4319-is-repo-wide.md`).

## Verification gate (before declaring done)

- `npm run build` and `npm test` in `apps/web` (never a bare `ng` —
  `ng-test-defaults-to-watch-mode.md`; never sentinel an ng command —
  `background-gate-latency-fix.md` §FOREVER-WATCHER), zero failures,
  `check-qt-classes` clean.
- The full Playwright suite (this lane owns port 4319 during its run),
  zero unexplained skips; the ACTIVATE-AT-UNIFY beat committed gated.
- Every parity spec's mutation proofs run and verified applied.
- Per-commit rule: `.claude/commands/commit.md`. Lane record appended to
  `status-log.md`.
