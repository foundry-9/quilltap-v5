# Work order P4.D158 — the wire re-check, the Opus 5 sampling strip (`48f4b42ec`), the `0506517d3` neutrality sweep, the three packaging rows' measurement (`b52b996c1` / `6e1a64ea6` / `06658535f`), the three ratifications (`49f66f571` / `a0e6fb42a` / `2edd823c0`), and the `docs/v4/` mirror

**Status:** OPEN at **unit 3 only** (lane branch
`claude/p4-d158-wire-recheck-df0ce5`, worked 2026-09-05).

- **Unit 1 — LANDED.** The `^claude-opus-5(-|$)` entry in v4's position + the
  comment corrections; two new corpus rows (`boundary-opus-5`,
  `boundary-opus-5-thinking`) recorded at the pin, shown RED first on a literal
  `"temperature":0.5` and green after; corpus 343 → 347 with every
  pre-existing row byte-identical.
- **Unit 2 — LANDED (items 3, 4, 5).** Every provider corpus regenerated at
  `d883a5ee1`; the ONLY moving bytes are `x-stainless-package-version`
  7.4.0 → 7.10.0 (210 rows) and the openrouter `user-agent` 1.2.32 → 1.2.106
  (16). No body/url/method/refusal/parse result moved anywhere; anthropic and
  ollama byte-identical whole; all eleven manifests byte-identical. Zod 4.4.3 →
  4.5.4 read against v5's transcriptions — no v5 sentence and no v5 regex
  moves, with four locale arms (not three) and six `.length(n)` sites (not
  five) recorded, plus the reachable-but-unexercised `datetime()` tightening.
  `b52b996c1` / `6e1a64ea6` / `06658535f` **ratified NO-PORT on the
  measurement.**
- **Unit 3 — OPEN.** Blocked on the order's own §C: the bulk sweep is
  single-flight repo-wide and runs only after P4.D153–P4.D157 close, and all
  five were still mid-lane throughout. The exclusion list (38 families, from
  466), the driver self-test, the `--diff-filter=DR` check and the
  twelve-row pre-sweep attribution map are done and committed; the ready-to-run
  recipe is in the lane record. **The neutrality proof for `0506517d3` is still
  owed.**
- **Unit 4 — LANDED (items 7, 8, 9, 10).** `49f66f571` and `a0e6fb42a`
  ratified NO-PORT with hunk-level evidence; `2edd823c0`'s four blind spots are
  now corpus arms over the new committed `restore-archive-bag-keys.zip`, each
  mutation-proven on v5 source and reddening only itself (zero v5 source
  changed — v5 already carried all four); the `docs/v4/` mirror refreshed (21
  modified / 34 added / 0 deleted) and its 21 newly-documented `?action=`
  values read against v5 row by row (all served; no port owed); the §G help
  hunks banked.

_Original header:_ OPEN (ordered 2026-09-05, the `d883a5ee1` drift catch-up
round: P4.D153 ∥ P4.D154 ∥ P4.D155 ∥ P4.D156 ∥ P4.D157 ∥ P4.D158).

## Preamble

- **v4 oracle baseline: `0b0617fee`** (drift-ledger §1). **This lane's
  rows (ledger §3):** `48f4b42ec` PORT (one regex — v5 reproduces the bug),
  `b52b996c1` / `6e1a64ea6` / `06658535f` NO-PORT? each with a MEASUREMENT
  obligation (plugin-utils 2.6 `buildRequestBody`; `openai` 7.4 → 7.10,
  `@openrouter/sdk` 1.2.32 → 1.2.106, `zod` 4.4.3 → 4.5.4, fifteen plugin
  bundles rebuilt; the 2.6.1 tail), `0506517d3`'s NEUTRALITY obligation
  over the 249 files that are not its seven corrections (P4.D155 ports
  those), `49f66f571` / `a0e6fb42a` / `2edd823c0` NO-PORT? ratifiable with
  evidence (`2edd823c0` names four restore blind spots worth CORPUS ARMS),
  and `e9a9c538e`'s DOCS half (its About hunk is P4.D156's). Read each
  row's hunk evidence in the ledger, then the hunks.
- **Freshness probe at lane start** (drift-ledger §2): v4 on `main`, tree
  clean, `git log d883a5ee1..main` empty, `git log 3a76b17df..bugfix` empty.
  **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED** (§B): `/tmp/qt-v4-pin-p4d158-d883a5ee1`
  with all THREE symlink classes — the provider recorders load
  `plugins/dist/*/` and die without the third (`Cannot find module
  '@anthropic-ai/sdk'`), and the empty-file trap then reads as drift
  (ledger §5.1). ⚠ `6e1a64ea6` repaired v4's root `postcss` override —
  the pin's `node_modules` is a symlink to the live checkout's (already
  installed at HEAD), so no reinstall is needed; but prove the INSTALLED
  SDK versions with the `createRequire` probe before any regen (P4.D76's
  Tier 1 item 1; memory note `p4.d48-anthropic-sdk-wire-check-lane`).
- **Sibling lanes:** P4.D153–P4.D157 own the families in §A; **this
  lane's bulk sweep runs AFTER they close (§C)** and excludes their
  families by name. P4.D157's seven families will `run_failed` at the
  target pin by design — exclude them too. §A–§H + Ownership binding and
  identical across all six orders.
- **Worktree:** per the `carryout` skill (branch `claude/p4-d158-…`).
  `CHANGELOG`/`status-log.md` append-only. **No Playwright** (§E).

## Mandate

Prove or refute, by corpus, that v4's packaging churn moved the provider
wire (the P4.D33 / P4.D48 / P4.D76 method: regenerate every recorded
corpus at the new pin; **byte-identical outside the self-dating version
markers**, the anthropic rows byte-identical WHOLE unless `48f4b42ec`'s
row is the one moving); port the ONE known wire change (`48f4b42ec`: the
`^claude-opus-5(-|$)` sampling-rejected entry + its comment corrections)
with a corpus row that would have caught it; run the bulk neutrality sweep
at the pin over every non-sibling family (the `0506517d3` proof — the
P4.D129 method, whose predecessor found a real 10/76 divergence no family
could see); ratify the three NO-PORT? rows with evidence and turn
`2edd823c0`'s four named restore blind spots into corpus arms; read the
Zod 4.5 locale against v5's transcribed refusal sentences; refresh the
`docs/v4/` mirror at the pin; bank the help hunks (§G). **If a corpus byte
moves anywhere but the opus-5 row: STOP and escalate** with the family,
the bytes, and the responsible v4 hunk — absorbing an unplanned wire
change is a drift-port decision, not this lane's. Must NOT touch: any
`crates/quilltap-core/src` provider code other than the one regex entry;
any sibling-owned family or fixture; `lib.rs`.

## Survey-verified starting points (2026-09-05 — measured, ledger §5.3)

### Unit 1 — `48f4b42ec` (the Opus 5 sampling strip)
1. v4 `plugins/dist/qtap-plugin-anthropic/provider.ts`: ONE new entry
   `/^claude-opus-5(-|$)/` in `SAMPLING_PARAMS_REJECTED_MODELS`, placed
   after the sonnet-5 row and before opus-4-7, plus three comment
   corrections ("Opus 4.7+" → naming Opus 5). The one flag gates BOTH
   decisions in both compositions (sampling params AND the fixed-budget
   thinking branch) at `sendMessage` `:379` / `streamMessage` `:574`; the
   bundle rebuilt; plugin 1.0.54 → 1.0.55.
2. v5 `crates/quilltap-core/src/model/request_builder/anthropic.rs:31-45`:
   the `LazyLock<Vec<Regex>>` carries six patterns (`sonnet-5`, `opus-4-7`,
   `opus-4-8`, `fable-5`, `mythos-5`, `mythos-preview`) and NOT
   `^claude-opus-5(-|$)` — so any `claude-opus-5*` profile is sent
   `temperature`/`top_p`/`top_k` and a fixed thinking budget, which the API
   400s. Add the entry in v4's POSITION (after sonnet-5) and carry the
   comment corrections. **The corpus today has no opus-5 row** (measured:
   `claude-opus-4-6` ×30, `-4-7` ×2, `-4-8` ×2, `-4-8-20260215` ×2,
   `sonnet-5` ×2): add `boundary-opus-5` (plain) and `boundary-opus-5-
   thinking` (a `thinkingBudget`) to `harness/oracle/providers/
   record-request-envelopes.mjs:270-283`'s anthropic block, re-record at
   the pin, and assert red-first that v5 emits the params before the port
   and strips them after. The corpus is 343 rows; every pre-existing row
   must stay byte-identical (the anthropic SDK did not move this round —
   check the `x-stainless`/anthropic version markers are unchanged).
   ⚠ This is the model the planning session runs on — a real Friday
   profile is affected; 💸 the live send joins the dogfood queue.

### Unit 2 — the packaging trio (`b52b996c1`, `6e1a64ea6`, `06658535f`)
3. Corpora + recipes (all under `harness/oracle/providers/`, runnable at
   the pin from `$PIN/plugins/dist/<plugin>`): `regenerate-request-
   envelopes.sh` (→ `fixtures/request-envelopes/request-envelopes.recorded
   .ndjson`, 343 rows), `regenerate-google-wire.sh` (`google-wire` +
   `google-request`), `regenerate-response-bodies.sh`, `regenerate-stream-
   fixtures.sh`, `regenerate-tool-wire.sh`, `regenerate-image-fixtures.sh`,
   plus `record-moderation-wire.mjs` / `record-web-search-wire.mjs`.
   Consumers: `request_builder_equivalence.rs` (its header check is an
   explicit SUBSET — `x-stainless-*` moves are invisible to it, so the
   corpus DIFF is the measurement, not the test), `request_builder_google_
   wire_equivalence`, `request_builder_google_equivalence`,
   `response_parse_equivalence`, `tool_wire_call_site`, the stream families,
   `openrouter_sdk_pricing_equivalence` (its header recipe :22-43; BOTH jest
   overrides load-bearing — without `--transformIgnorePatterns` the suite
   passes with EMPTY scenarios). Expected: byte-identical outside the
   version-marker fields (`x-stainless-package-version` 7.4.0 → 7.10.0 on
   the openai-family rows, the openrouter UA `speakeasy-sdk/typescript
   1.2.32 → 1.2.106`); the plugin-utils 2.6 `buildRequestBody` claim ("the
   streaming keys are spread in position between `stop` and `user` so the
   serialized body stays byte-identical") is exactly the kind of sentence a
   corpus answers. Re-confirm the three recorded SDK refusals still refuse
   (memory note `v4-sdk-behavior-the-raw-wire-must-reproduce`).
4. **Zod 4.4.3 → 4.5.4 — MEASURED at ordering** (`npm pack zod@4.4.3` vs
   the installed 4.5.4, `v4/locales/en.js` diffed): the uuid regex is
   IDENTICAL (so the `zod_uuid` gates — `api/chat_outfits.rs:297 is_zod_uuid`,
   `api/brahma.rs:139`, the folded `z.uuid()` copies — need no change);
   three message arms moved: **(α)** `too_small`/`too_big` with
   `issue.exact` now render `expected … exactly N` instead of `>=`/`<=`
   (Zod's `.length(n)` — v4 has `z.string().length(64)` on `sha256` fields
   in `file.types.ts:37/:95`, `mount-index.types.ts:81/:159/:280`; v5
   transcribes NO `>=64 characters`/`<=64 characters` sentence anywhere
   (measured) — confirm by grep and record; if v5 ever answers a `.length`
   refusal it must use the new wording); **(β)** `invalid_type` names a
   non-finite number as `Infinity`/`-Infinity`/`NaN` (unreachable through
   JSON — record); **(γ)** `invalid_union` with `inclusive === false` →
   `Invalid input: more than one option matched` (v4 uses no `.xor`/
   `inclusive:false` — measured; record). So the expected outcome is
   NO v5 sentence moves; the PROOF is the settings/routes families
   regenerated at the pin in the bulk sweep (Unit 3) staying green — name
   them in the record (`settings_routes_equivalence`, `chat_settings_*`,
   `web_edge_body_parse_guard`'s families, P4.62's `validationError`
   envelope family).
5. `public/schemas/plugin-manifest.schema.json` re-emits `extendsTheme` as
   `"type": ["string","null"]` (a zod-4.5 JSON-Schema emission difference,
   theme manifests only) — v5's manifest generator reads plugin
   `manifest.json` files, not this schema: re-run `harness/oracle/
   providers/gen-provider-manifests.mjs` at the pin and diff
   `provider_manifest/manifests/*.json` (expected: version strings only;
   anything else is drift to report).

### Unit 3 — the `0506517d3` neutrality sweep
6. `python3 harness/tools/recipe_sweep.py --v4 $PIN --run-all --exclude
   <every §A family of P4.D153–P4.D157, comma-separated> --results
   harness/tools/sweep-results/2026-09-0X-d883a5ee1-p4.d158-neutrality.json`
   — single-flight, AFTER the siblings close (§C). Expected verdict: every
   run family green with oracles regenerated at the post-collapse tree;
   that IS the neutrality proof for those surfaces. Any red: check the
   ledger for which commit owns the surface (a red on a sibling's surface →
   hand it over, it is not yours); an unexplained red = the collapse was NOT
   neutral there — **escalate with the family, the differing bytes, and the
   responsible v4 hunk; do not patch v5 inside this lane.** Record the
   totals (run / green / excluded-by-§A / `nothing_to_run` / skipped with
   reason). Highest-risk rewrites by size (read their hunks BEFORE the
   sweep so a red is attributable at once): `lib/tools/almanack/render.ts`
   (1,060), `lib/pascal/tool-draft.ts` (342), `app/api/v1/chats/route.ts`
   (306), `chats/[id]/actions/documents.ts` + `wardrobe/transfers/route.ts`
   (281 each), `app/api/v1/documents/route.ts` (248), `doc-edit-handler.ts`
   (226), `state-handler.ts` (214), `message-finalizer.service.ts` +
   `answer-confirmation.service.ts` (144/143), the twelve 4.9 migration
   scripts onto `openEncryptedSqlite`/`addColumnIfMissing` (no DDL change —
   `generateDDL` untouched across all fifteen commits, measured; no D23
   re-dump is owed).

### Unit 4 — the three ratifications (+ `e9a9c538e`'s docs half)
7. `49f66f571`: two `NextResponse.json(x)` → `successResponse(x)` swaps;
   `lib/api/responses.ts:63` is a pass-through (measured) — ratify NO-PORT
   on the hunk + the two ported surfaces' payloads (P4.D85 `get-tags`,
   P4.D112 wardrobe archetypes) unchanged in the sweep.
8. `a0e6fb42a`: eighteen new `__tests__` files + a bundle rebuild that is a
   vendored-dependency re-inline only (inspected: `standardwebhooks`) —
   ratify NO-PORT; READ its commit body for where v4 says bugs 87/88/112/119
   and 89/90/100/102 are covered (useful when a lane looks for a v4-side pin
   under the obvious name and finds none) — one paragraph in the record.
9. `2edd823c0`: `restore-field-fidelity.test.ts` (+144) only — ratify
   NO-PORT for behaviour, and turn its four named blind spots into v5
   CORPUS ARMS (measured: NONE of the four keys appears in any v5
   restore/backup/import family or fixture builder today): (1)
   `chats.conciergeOverride = 'UNCENSORED'` restores as `'UNCENSORED'`,
   never narrowed to `'OFF'` (which would re-arm the classifier on a chat
   the operator ruled on — P4.D148's surface); (2) `chat_settings.
   cheapLLMSettings.allowCheapFallback = true` survives (its `false` default
   reads as a declined stand-in — P4.D138); (3) `image_profiles.parameters.
   loras` survives (the bag is unvalidated, so nothing downstream notices the
   key vanishing); (4) the `memoryRecall` instance-settings row keeps
   `perTurnConversationSummaries` (upserted by raw SQL, not a repository —
   P4.D95). Shape: a NEW committed archive (`restore-archive-bag-keys.zip`
   via a `build-restore-archive-bag-keys.ts` builder beside the others under
   `harness/oracle/fixtures/`) + four arms in `system_restore_equivalence`
   (`system-restore.test.ts` + the runner) comparing the restored cells
   within-tree AND cross-side; mutation: narrow `'UNCENSORED'` → `'OFF'` on
   restore → arm (1) reddens. v4's framing goes in the runner's header: *a
   new column announces itself with a migration; a new key in a JSON bag or a
   widened enum domain is invisible to every schema check.*
10. `e9a9c538e` docs half: refresh `docs/v4/` by the P4.61 recipe (mechanical
    `rsync` from the PIN's `docs/` — `.DS_Store` excluded, no `--delete`,
    plus `help/database-protection.md` from `help/`; `diff -rq` clean
    afterwards; 19 modified + 97 added last time — record this run's
    counts). The mirror is measurably behind the baseline already
    (`developer/API.md` differs from `0b0617fee`'s by 521 lines and from
    HEAD's by 720). Then READ the refreshed `developer/API.md`'s fourteen
    newly-documented `?action=` values against P4.67/P4.72's
    `query_param_semantics_equivalence` endpoint list — every value either
    served (name the row) or a recorded gap; one table in the record, no
    port. Bank the help hunks per §G.

## Tiered deliverables

**Tier 1 (must land):** Unit 1 whole (the entry + comments + the two new
corpus rows red-first); Unit 2 items 3–5 (the environment proof, every
corpus regenerated at the pin and committed, the field-level byte assertions,
the pricing family with the REAL SDK, the Zod record with the three arms
measured, the manifests diff); Unit 3 whole with totals; Unit 4 items 7–9's
ratification records with evidence + the four restore arms + the new
archive.
**Tier 2 (should land):** item 10 (the mirror refresh + the `?action=`
table + the §G bank).
**Tier 3 (explicit deferrals, loud):** none — this lane's only legitimate
outputs are proofs, a one-line port, ratifications with evidence, or an
escalation. An unexplained neutrality red or corpus byte is an ESCALATION,
not a deferral.

## The differential requirement (restated)

Unit 1: `request_builder_equivalence` at the pin with the two new anthropic
rows red-first (v5 emitting sampling params + a fixed thinking budget for
`claude-opus-5*` before the port), green after, every other row
byte-identical. Unit 2: the corpora's pre/post diffs are the deliverable —
record each file's differing FIELDS by name (version markers only) and the
anthropic rows' whole-row identity; the consuming families by name with
`--nocapture`, zero SKIP. Unit 3: the sweep artifact committed under
`harness/tools/sweep-results/`. Unit 4: the four restore arms mutation-proven
(one mutation each), the archive's manifest read as ground truth (ledger
§5.4's habit).

## Fixtures

- Consumes: every committed provider corpus (regenerated + committed by
  this lane), the restore-archive family (§A).
- Delivers: the regenerated corpora, `restore-archive-bag-keys.zip` + its
  builder, the sweep artifact, the refreshed `docs/v4/` mirror.

## Shared contracts (BINDING — identical text in all six orders)

**§A Family ownership for this round's regens.** Each family below is
regenerated and edited by ONE lane; P4.D158's bulk neutrality sweep
EXCLUDES every family in this section by name and hands any red in them to
the owning lane.

- **P4.D153:** `memory_injector_equivalence` (+ `harness/oracle/cases/
  memory-injector.ts`), `build_context_tier3_equivalence` (+ its builder
  `build-context-tier3.test.ts` and the `QT_FIXTURE_BC_*` pair),
  `carina_query_tier3_equivalence` (+ its fixture builder),
  `announcer_tier3_equivalence`.
- **P4.D154:** `message_context_leaves_equivalence` (+ `harness/oracle/
  cases/message-context-leaves.ts`), `orchestrator_tier3_equivalence` (+
  `orchestrator-tier3.test.ts` and the `QT_FIXTURE_ORCH_*` pair),
  `file_attachment_tier3_equivalence` (+ its fixture pair).
- **P4.D155:** `documents_routes_equivalence` (+ `documents-routes.test.ts`),
  `system_export_equivalence`, `answer_confirmation_tier3_equivalence`,
  `cheap_llm_fallback_equivalence`, `brahma_console_tier3_equivalence`,
  `brahma_console_routes_equivalence`, `brahma_orchestrator_tier3_equivalence`,
  every `pascal_*_equivalence` + `p4_6ay_workbench_wire_contract`, the
  self-inventory families, and whichever family the lane identifies as
  driving `cheap_llm::get_cheap_llm_provider` (named in its record).
- **P4.D156:** the CLI Tier R (`crates/quilltap-cli/tests/cli_differential.rs`)
  + `completion_behavior.rs`; the SPA unit specs under its paths.
- **P4.D157:** the seven families the dead-code sweep takes dark:
  `cheap_model_equivalence`, `model_selection_equivalence`,
  `llm_errors_equivalence`, `message_formatter_equivalence`,
  `post_office_host_equivalence`, `chat_timestamp_equivalence`,
  `token_estimation_equivalence` (+ their seven oracle cases).
- **P4.D158:** `request_builder_equivalence` + the committed request-envelopes
  corpus and its recorder, `request_builder_google_wire_equivalence`,
  `request_builder_google_equivalence`, `response_parse_equivalence`,
  `tool_wire_call_site`, the stream fixtures, `openrouter_sdk_pricing_equivalence`,
  the provider manifests generator + `provider_manifest/manifests/*.json`;
  the restore families for the `2edd823c0` arms (`system_restore_equivalence`,
  `system_restore_state`, `restore_vintage_state`,
  `system_restore_guards_equivalence`, the `build-restore-archive*` builders
  and any NEW committed archive); and the bulk neutrality sweep over every
  family NOT listed above.

**§B Pins.** The round's target baseline is **`d883a5ee1`** (v4 `main` HEAD
at ordering, 2026-09-05). Every lane regenerates from a LANE-UNIQUE detached
worktree pinned at `d883a5ee1` (`/tmp/qt-v4-pin-<order>-d883a5ee1`, the
three symlink classes, ledger §5.1) — with ONE exception: P4.D157's
frozen-family evidence regenerates at **`0b0617fee`** (the last sha at which
those seven cases link), from its own second pin. **PIN REQUIRED stands for
every lane until `/unify` moves the baseline** — the checkout's HEAD is the
target, but the checkout can go dirty mid-lane (§5.1), and a pin is the only
tree whose contents are known. Verify each pin by grepping a fresh NDJSON for
a marker only one tree has (e.g. `About another character` appears in a
`d883a5ee1` memory-injector oracle only once P4.D153's targeted cases exist,
and never in a `0b0617fee` one).

**§C Sweep serialization.** `recipe_sweep.py --run-all` is single-flight
repo-wide and belongs to P4.D158 alone; it runs AFTER P4.D153–P4.D157 have
closed their branches (execution arrangement in the round report). Lanes
153–157 run per-family `--run` regens only, and never against another
lane's §A families or their `/tmp` staging.

**§D Source-tree seams (who edits what shared file).**

- `crates/quilltap-core/src/services/mod.rs`: **only P4.D153** edits it (one
  line: `pub mod memory_subject;`). `crates/quilltap-core/src/lib.rs`:
  **nobody**. P4.D157 deletes FUNCTIONS and their tests inside files it owns
  but never removes a module declaration or a `pub use` in `lib.rs`; a module
  it empties is recorded for the unifier, not deleted.
- `services/message_context.rs` + `services/chat_files.rs` (the
  `MessageContextSeams` trait, its `Noop` and `Real` impls): **only
  P4.D154**. The trait gains ONE sibling method beside `load_lantern_images`
  for the USER-side loader (name it `load_user_attachments`; signature shape
  `(&self, file_ids: &[String], provider: &str) -> impl Future<Output =
  UserAttachmentLoad> + Send`, default no-op, `UserAttachmentLoad` a struct
  the lane defines beside `LanternLoad`).
- `db/characters_read.rs`: **only P4.D153** (`find_names_by_ids`, RAW path).
- `cheap_llm.rs`, `services/cheap_llm_exec.rs`: **only P4.D155**.
- `model/request_builder/anthropic.rs`: **only P4.D158** (one regex entry +
  the three comment corrections).
- SPA: `apps/web/src/app/pascal/**` → **P4.D155**; `apps/web/src/app/screens/**`
  and `apps/web/src/styles/**` → **P4.D156**. Nobody else edits `apps/web`.
- `crates/quilltap-cli/**` → **P4.D156** only.

**§E Playwright.** Port 4319 is P4.D156's for the round (it may run the
existing suite once at close; it authors no new beat unless its record says
why). Every other lane authors NO Playwright and runs none; the unifier runs
the full suite at the gate.

**§F The drift ledger.** Lanes never write `drift-ledger.md`. The fourteen
§3 rows were marked `ORDERED(p4.d15x)` at ordering; `/unify` marks them
ABSORBED / NO-PORT-RATIFIED when the baseline moves to `d883a5ee1`. The
fifteenth row (`15573c3a1`, bug 119) stays UNPROCESSED by the ledger's own
instruction — it belongs to the unported `p4.9k` surface.

**§G The `p4.9i2` help bank.** The help-file hunks in this drift
(`e288ae2ec` → `help/file-uploads.md` "A word on company"; `d883a5ee1` →
`help/memory-recall-relevance.md` "Whose Life Is It, Anyway?"; `e9a9c538e` →
four `help/` files incl. `homepage.md`'s In-Chat Navigation section and three
corrected `url:` values) are banked ONCE, by P4.D158, in its lane record under
a `p4.9i2 bank` heading; the unifier carries them into `phase-4.md`'s
candidate row. No lane ports help content.

**§H Versions.** Each lane bumps only the crates / the SPA it touches, per
`.claude/commands/commit.md` §6; the unifier recounts as base + total bumps
(the silent auto-merge trap). Base at ordering: core 0.0.776, harness 0.0.671,
web 0.0.114, host 0.0.96, cli 0.0.17, tauri 0.0.7, SPA 0.5.642.

## Ownership (BINDING — identical table in all six orders)

| Path | Owner |
|---|---|
| `crates/quilltap-core/src/memory_injector.rs`, NEW `services/memory_subject.rs` (+ the one `services/mod.rs` line), `db/characters_read.rs`, `services/build_context.rs`, `services/carina_query.rs`, `services/announcer/character_voiced.rs`; §A families for P4.D153 | P4.D153 |
| `crates/quilltap-core/src/services/message_context.rs`, `services/chat_files.rs`; §A families for P4.D154 | P4.D154 |
| `crates/quilltap-core/src/cheap_llm.rs`, `services/cheap_llm_exec.rs`, `api/documents.rs`, `services/brahma_console/**`, `services/qtap_export/**`, `tools/self_inventory.rs`, `crates/quilltap-core/src/pascal/**`, `apps/web/src/app/pascal/**`; §A families for P4.D155 | P4.D155 |
| `apps/web/src/app/screens/**`, `apps/web/src/styles/**`, `crates/quilltap-cli/**`; the CLI Tier R + `completion_behavior.rs` | P4.D156 |
| `crates/quilltap-core/src/cheap_model.rs`, `pricing.rs`, `services/llm_errors.rs`, `message_formatter.rs`, `services/host_notifications.rs`, `chat_timestamp.rs`, `token_estimation.rs` (function-level deletions/pins only); the seven §A families + their oracle cases | P4.D157 |
| `crates/quilltap-core/src/model/request_builder/anthropic.rs` (one regex line + comments); `harness/oracle/providers/**` + `harness/oracle/fixtures/request-envelopes/**`, `response-bodies/**`, the stream fixtures; `crates/quilltap-core/src/provider_manifest/manifests/*.json`; the restore-archive builders + any NEW committed archive + §A restore families; `docs/v4/**`; `harness/tools/sweep-results/<new artifact>` | P4.D158 |
| `docs/CHANGELOG.md`, `docs/developer/porting/status-log.md` | append-only, all lanes |
| `docs/developer/porting/drift-ledger.md`, `crates/quilltap-core/src/lib.rs` | NOBODY |

Every other path is unowned this round: a lane that believes it must touch
one STOPS and reports (the `carryout` rule 3), naming the file and the
sibling it suspects.

## Verification gate (before declaring done)

1. The sweep's full log captured (never `| tail`) and audited: totals in
   the lane record; zero unexplained SKIPs; zero unexplained reds; the
   artifact committed.
2. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D
   warnings` in BOTH feature sets; `cargo build --workspace`; `cargo test
   --workspace` (full log + sentinel) with the provider corpora's and the
   restore families' env vars set, each confirmed RUN, zero `SKIP:`.
3. The consuming provider families + `openrouter_sdk_pricing_equivalence`
   by name with `--nocapture`; `request_builder_equivalence`'s two new rows
   shown red before the regex entry and green after.
4. The three ratification records + the Zod measurement + the manifests
   diff + the mirror counts in the lane record, each with its evidence.
5. Commits per `.claude/commands/commit.md` (core for the one-line regex,
   harness for the corpora/arms; docs-only for the mirror); the lane record
   appended to `status-log.md`; this order's status header updated.
