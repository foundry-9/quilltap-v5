# Work order P4.9G5 — the backup / restore family

> **STATUS: OPEN — units 1–2 LANDED on `claude/p4-backup-restore-docs-6085f9`;
> units 3–5 (the whole restore side) NOT STARTED.** Lane 3 of 3 in the "finish
> P4.9G1" round (P4.9G3 ∥ P4.9G4 ∥ P4.9G5). **This is the round's largest
> lane** — run it in a dedicated worktree and land it unit-by-unit (see §Unit
> order).
>
> **Landed (see the P4.9G5 lane record in `status-log.md` for the full detail):**
> - Unit 1 — `collectUserData` + `createManifest` + the 38-file staging tree,
>   with the `system_backup_equivalence` archive-tree differential (2 cases).
>   ⚠ The `system-data-*` fixture family CHANGED (a row in every table the
>   collector dumps) — G3's and G4's oracles must be regenerated at unification.
> - Unit 2 — the zip, the `BackupHost` seam + its live host impl (single-use
>   30-minute temp store, injected clock), `systemBackupCreate`, and the byte
>   `GET /api/v1/system/backup/{id}` download leg. **Backup is live end-to-end**
>   against the SPA card already on main.
>
> **RESUME HERE — still OPEN:**
> - **Unit 3** — `parseBackupZip` + `legacy-migrations` + `previewRestore` +
>   `systemRestorePreview` + the octet-stream `?action=upload` leg.
> - **Unit 4** — `remapBackupData` + restore `new-account`.
> - **Unit 5** — restore `replace` (the §2 ACTIVATE-AT-UNIFY call site).
> - The `system_restore_equivalence` differential and its committed sanitized
>   `.zip` archive fixture do not exist yet.
> - **Tier 2's e2e beat was deliberately NOT written** (landing it obliges a
>   full Playwright run the lane could not run honestly). `backup create →
>   download` would walk today; `restore preview` cannot until unit 3 lands.
>
> `SystemRestorePreview` / `SystemRestoreExecute` keep the shared §3 refusal arm;
> `SystemBackupCreate` was removed from it.
>
> **v4 baseline: `e646f58b`** (v4 HEAD, clean tree, drift-verified 2026-07-24
> at round planning). **Drift-check at lane start** — `git log e646f58b..HEAD
> --oneline` in `~/source/quilltap-server`; if v4 moved, STOP and report before
> porting anything.
>
> **Siblings (run in parallel, disjoint ownership — see §Ownership):**
> - **P4.9G3** — delete-all + the REST remainders. **This lane DEPENDS on its
>   `delete_user_data` for restore's `replace` mode** — see Shared contract §2
>   for the binding signature and the ACTIVATE-AT-UNIFY marker you must write.
> - **P4.9G4** — the `.qtap` export/import family.
>
> This lane, plus its two siblings, discharges the OPEN remainder of
> `p4.9g1-data-system-server.md` (whose tasks-queue + jobs family and all
> sixteen §1 wire variants are already on main). P4.9G1 stays open as the
> historical record until all three land; **do not work P4.9G1 directly.**

## Mandate

Deliver the **Backup & Restore** server family — full-graph backup creation to a
staged zip with a single-use temp store and streaming download, plus restore
upload / preview / execute in **both** modes (`replace`, `new-account`) — as
dispatch verbs + v4-parity REST edges + the two byte-level web-edge legs,
differential-verified against v4's real `lib/backup/**`.

The SPA half is **already built and waiting on main** (P4.9G2 unit 7 —
`backup-dialog.ts`, `restore-dialog.ts` + `useRestoreData` port,
`backup-restore-card.ts`, `download-utils.ts`), so this lane's output goes live
the moment it lands. Read that unit's record in `status-log.md` first: it pins
which legs the client calls, the UI `import` → wire `new-account` map, the
filename fallback the client always exercises (the create response carries no
`filename`), and the fact that the card reloads the app on `restoreComplete`.

**Must NOT touch:** the delete-all family (P4.9G3: `services/delete_all.rs`,
`api/system_data.rs`), the export/import family (P4.9G4:
`services/qtap_export/**`, `services/quilltap_import/**`,
`quilltap-web/src/qtap_routes.rs`), and
`crates/quilltap-core/src/api/types.rs` (**FROZEN this round** — all sixteen §1
variants are already defined and pinned by `p4_9g1_wire_contract`; a needed
change is a STOP-and-report). §Ownership binds the rest.

## Survey-verified starting points (2026-07-24 @ v4 `e646f58b` — do not re-derive)

### v4 source map

| File | Lines | Role |
| --- | --- | --- |
| `lib/backup/backup-service.ts` | 765 | `collectUserData` :135, `createManifest` :432, `writeJsonFile` :492, `writeJsonArrayFile` :505, `cleanupDir` :529, `createBackup` :548; helpers `encodeEmbedding` :53, `dumpMountIndexTable` :72, `dumpInstanceSettings` :92, `countNpmPlugins` :392, `countUserInstalledThemes` :412 |
| `lib/backup/temporary-storage.ts` | 127 | `BACKUP_EXPIRY_MS = 30*60*1000` :29, `CLEANUP_INTERVAL_MS = 60*1000` :30, `storeTemporaryBackup` :92, `retrieveTemporaryBackup` :115 (**single-use — removes the entry on read**) |
| `lib/backup/types.ts` | 596 | `BackupData`, `BackupManifest`, `RestoreSummary` |
| `lib/backup/uuid-remapper.ts` | 181 | the `UuidRemapper` class — `remap` (memoized old→new), `remapArray` (non-array → `[]` + warn) |
| `lib/backup/restore-service.ts` | 41 | a barrel re-exporting the `restore/` modules |
| `lib/backup/restore/archive.ts` | 372 | `cleanupDir` :82, `extractZipToTemp` :99, `parseBackupZip` :135, `getFileFromExtractedBackup` :328, `countNpmPluginsInExtractedBackup` :363 |
| `lib/backup/restore/json-stream.ts` | 163 | disk-backed / streaming JSON readers |
| `lib/backup/restore/legacy-migrations.ts` | 105 | pure transforms folding pre-rework backup shapes (incl. outfit-presets → composite wardrobe items) |
| `lib/backup/restore/preview.ts` | 79 | `previewRestore` :20 — the **35-field** `RestoreSummary` (full field list at :29-77; note `userInstalledThemes: 0` is hard-coded and `warnings: []` is always empty) |
| `lib/backup/restore/uuid-remap.ts` | 452 | `MOUNT_POINT_SETTING_KEYS` :61, `remapBackupData` :70 — the ~40-entity field-list remap |
| `lib/backup/restore/restore.ts` | 901 | `restore` :35 — the orchestrator |
| `lib/backup/restore/delete-service.ts` | 349 | **P4.9G3's file — do not port it here** (see §2) |

### `createBackup` (:548) — the exact sequence

1. WAL checkpoint **all three partitions**: `runBackupCheckpoint(rawDb)`,
   `runLLMLogsBackupCheckpoint`, `runMountIndexBackupCheckpoint` (:556-570).
2. `collectUserData(userId)` (:135) → `createManifest` (:432).
3. `mkdtemp(os.tmpdir()/'quilltap-backup-')`; staging folder name
   `quilltap-backup-<new Date().toISOString().replace(/[:.]/g,'-')>` (:577-580).
4. Write **32 JSON array files** under `<staging>/data/` via
   `writeJsonArrayFile`, in this exact order (:590-632): `characters`, `chats`,
   `tags`, `connection-profiles`, `image-profiles`, `embedding-profiles`,
   `memories`, `files`, `prompt-templates`, `roleplay-templates`,
   `provider-models`, `projects`, `groups`, `llm-logs`, `plugin-configs`,
   `chat-settings`, `folders`, `wardrobe-items`, `character-plugin-data`,
   `conversation-annotations`, then the format-3 additions `chat-documents`,
   `instance-settings`, `embedding-status`, `conversation-chunks`,
   `tfidf-vocabularies`, `vector-index-metas`, `vector-entries`,
   `doc-mount-points`, `doc-mount-folders`, `doc-mount-files`,
   `doc-mount-file-links`, `doc-mount-chunks`, `doc-mount-documents`,
   `doc-mount-blobs`, `project-doc-mount-links`, `group-doc-mount-links`,
   `group-character-members`, `text-replacement-rules`. `outfit-presets.json`
   is **no longer written** (:632 comment).
5. User files → `<staging>/files/<file.storageKey>` one at a time, **warn-and-
   continue on failure** (:636-658).
6. Doc-mount blob bytes → `<staging>/mount-blobs/<blob.id>`, streamed one at a
   time, warn-and-continue (:665-687).
7. npm plugins → `<staging>/plugins/npm/<name>` (recursive copy, best-effort).
8. Zip; `storeTemporaryBackup(backupId, zipPath, userId)` holds
   `backupId → zipPath` in an **in-memory Map with a 30-minute TTL** and a
   1-minute sweep.

### `GET /api/v1/system/backup/[id]` (`route.ts:21`)

`retrieveTemporaryBackup` is **single-use** (removes the entry on read), the
response streams with `Content-Length`, and on stream close the handler
**unlinks the zip and rm's its temp dir** (:63-77).

### `POST /api/v1/system/restore` (`route.ts`, dispatch at :246)

- `?action=upload` → `handleUpload` :86. Streams the raw octet-stream body to
  `os.tmpdir()/quilltap-restore-<uuid>.zip` with explicit back-pressure
  (`writeStream.write` → await `'drain'`). Empty body → `'No request body'`.
  Returns `{uploadId, size}`. `pendingUploads` is an **in-memory Map,
  `UPLOAD_TTL_MS = 60*60*1000`** (:36), swept lazily by
  `cleanupExpiredUploads()` (:43) at the top of each request.
  **Quirk to carry faithfully and note:** `getPendingUpload(uploadId, userId)`
  (:56) takes `userId` and **never checks it** — it only validates the UUID
  shape and map presence.
- `?action=preview` → `handlePreview` :145 → `previewRestore(zipPath)`.
- default → `handleRestore` :186 → `restore(zipPath, mode)` with
  `mode: 'replace' | 'new-account'`. Returns
  `{characters, chats, messages, tags, files, memories, warnings[]}`. The temp
  file is removed in a `finally` (:238).

### `restore` (`restore/restore.ts:35`) — the phase order

Extract once (:45), then: **replace** → `deleteUserData` first (:52);
**new-account** → `remapBackupData` (:57). Then restore in dependency order
(:65-67 — every entity preserves its backup/remapped id via `CreateOptions.id`,
so cross-references are already correct), through the numbered phases:
1 tags (:69) · 2 connection profiles (:80 — **rename on name collision**, and
`apiKeyId` is deliberately NOT restored, :89) · 3 image profiles (:106) ·
4 embedding profiles (:117) · 5 files (:128 — read from the extracted dir using
**`parsedData.files`** for the on-disk lookup and **`data.files`** for the DB
row, since new-account remaps ids but not on-disk names; project-bound files go
to the project mount, project-less to Quilltap Uploads under `restored/`, not
`_general/`; **record post-bridge mime/size, not the backup's claim**, :167-172;
strip auto-generated + legacy fields) · 6 characters (:197) · 7 chats +
messages (:208) · 9 memories (:230 — strip legacy `personaId`) · 10 prompt
templates (:246, user-created only) · 11 roleplay templates (:263) ·
12 provider models (:279) · 13 projects (:292 — no `userId`) · 13a groups
(:306 — `groups.create` discards the incoming `officialMountPointId` and
provisions a fresh store; membership + additional links restore at 22h-i/22h-ii)
· 14 llm logs (:324) · 15 plugin configs (:342, **upsert**) · 16 chat settings
(:360) · 17 folders (:373) · **19 wardrobe items — DEFERRED to 22g** (:386-394;
vault mounts don't exist until 22a, and post-cutover backups restore as
`Wardrobe/*.md` documents via 22c–22e — only LEGACY backups use
`data.wardrobeItems`) · 20 outfit presets **REMOVED** (:396) · then the 22x
doc-mount phase family. Read the file top-to-bottom before porting; the
comments carry the invariants.

**Mechanism divergence (pre-approved):** v4 shells out to `unzip`; v5 uses a zip
crate. The DB-state differential is the equivalence proof, not the unzip
mechanics. Record it.

### v5 anchors

- **The refusal to replace** — `api/engine.rs:736-746` holds ONE contiguous
  "recognized but not yet available" arm covering all nine unlanded verbs. See
  Shared contract §3 for the region discipline that keeps three lanes out of
  each other's way.
- The `?action=` dispatch pattern lives in
  `quilltap-web/src/system_data_routes.rs` (GET :71, POST :132,
  `unknown_action` :57); this lane's routes are NEW paths, so they go in a new
  `quilltap-web/src/backup_routes.rs` registered in `lib.rs` `build_router`
  (:295-302 is the existing system block).
- Byte/stream route precedents: the qtap-target byte route (P4.6w) and the fs
  raw read (P4.6y). Host filesystem access: the `files_dir` thread and
  `safe_realpath` boundary check landed in P4.6bg (`services/` doc-edit path
  resolver) — reuse rather than re-derive.
- **The temp stores are host state, not core state** (P4.0: the host owns
  cadence and process-lifetime state). Both maps (30-min backup store, 1-hour
  upload store) want an erased seam on `EngineAssembly` with the host impl in
  `quilltap-host/src/{spine.rs,host.rs}` — pattern: `image_generation` (core
  `engine.rs:122-128`, host `spine.rs:2388-2435`, threading `host.rs:461,478,590`)
  and P4.9G1's own `JobPumpControl` (`api/system_data.rs`, host
  `quilltap-host/src/job_pump.rs`). **Inject the clock** so the TTLs are
  testable. A `None` seam → the loud not-assembled refusal.
- `services/file_storage.rs` holds the storage-key machinery the file staging
  and restore phases go through.
- Fixture: `crates/quilltap-web/tests/fixtures/system-data-{main,mount,llmlogs}.db`
  (committed) + builder `harness/oracle/fixtures/build-system-data-fixture.ts`
  and `harness/oracle/fixtures/system-data.json`.

## Unit order (land incrementally; each unit commits green)

1. `collectUserData` + `createManifest` + the staging tree (no zip yet) —
   provable by the archive-tree differential.
2. Zip + the temp store seam + `systemBackupCreate` + the download leg.
3. `parseBackupZip` + `legacy-migrations` + `previewRestore` +
   `systemRestorePreview` + the octet-stream upload leg.
4. `remapBackupData` (the ~40-entity field list) + restore `new-account`.
5. Restore `replace` — the §2 ACTIVATE-AT-UNIFY call site.

## Tiered deliverables

### Tier 1 — must land

1. **Backup create**: `services/backup/**` — the full-graph collect, the
   manifest, the 32-file staging tree in v4's exact order and filenames, the
   file + mount-blob + npm-plugin staging with v4's warn-and-continue error
   handling, the zip, and the 30-minute single-use temp store behind the host
   seam with an injected clock.
2. **`systemBackupCreate`** dispatch verb + **`GET /api/v1/system/backup/{id}`**
   — the single-use streaming download with `Content-Length` and the
   unlink-plus-rmdir on close.
3. **Restore upload/preview**: the octet-stream `?action=upload` leg (back-
   pressured write to a temp zip, the 1-hour TTL map with lazy cleanup, the
   userId-ignored `getPendingUpload` quirk carried and documented),
   `parseBackupZip` + the legacy migrations, and `systemRestorePreview` over
   `previewRestore`'s 35-field summary.
4. **Restore execute, both modes**: `systemRestoreExecute` over the full
   phase-ordered orchestrator. `new-account` includes the `UuidRemapper` +
   `remapBackupData` field list. `replace` is written per §2 with the
   ACTIVATE-AT-UNIFY marker and a loud typed refusal until the unifier wires it.
5. **The differentials** (fresh at `e646f58b`, run by name, zero SKIP):
   - **`system_backup_equivalence`** — v4's REAL `createBackup` vs the Rust one:
     diff **the manifest + the extracted archive tree** (relative paths, then
     file contents), **never the zip bytes** (archiver vs zip-crate framing
     differs by design). Normalize only the staging timestamp and the
     legitimately-nondeterministic manifest fields.
   - **`system_restore_equivalence`** — tier-2 DB-state diff after restoring
     the same archive, covering `mode: 'new-account'` (with the minted-UUID
     positional remap) **plus** the preview arm (assert preview writes nothing)
     and the malformed-archive arm. The `replace`-mode case is the unifier's
     (see §2 / §Fixtures).

### Tier 2 — should land

- A new e2e beat: backup create → download → restore preview, appended at the
  end of the main describe in
  `apps/web/e2e/settings-data-system-flow.spec.ts` (per §3). Seed anything it
  needs in `apps/web/e2e/global-setup.ts` (this lane's file). **Do not** make it
  execute a destructive restore against the shared e2e server — preview only,
  unless it runs after the delete-all describe.
- The npm-plugin and user-theme staging arms if the v5 instance has no
  equivalent directory: land them as a documented no-op with the evidence, not
  as silence.

### Tier 3 — explicit deferrals (loud, named)

- If a phase of `restore.ts` depends on a v5 subsystem that is itself unported,
  refuse **that phase** loudly and by name in the restore result's `warnings[]`
  **and** in a typed refusal — never restore a partial graph silently. Enumerate
  every such phase in the lane record.
- Anything else that cannot land ships as a `not_available`-style loud typed
  refusal, never a silent stub.
- **Landing units 1–3 with 4–5 refused is an acceptable outcome** if the lane
  runs out of budget — say so explicitly, leave the order OPEN with a resume
  list in its status header, and do not half-port the orchestrator.

## The differential requirement, restated

Every ported unit ships with an equivalence test driving v4's **REAL** code
(jest oracle → NDJSON → Rust diff; tier-2 structural DB diff for the restore
write path). Closest recipes, in the memory notes: **`jest-real-db-oracle`**
(`resetModules` + `doMock` past `jest.setup`; Node 24), **`oracle-jest-testtimeout`**
(`testTimeout=120000` — the backup/restore cases are the slowest in the repo),
**`oracle-regen-silent-stale-pass`** (an erroring builder leaves the OLD fixture
— grep the NDJSON after every regen), **`tool-handler-differential-minted-remap`**
(positional UUID/timestamp normalization for `new-account`),
**`project-store-fixture-needs-real-create`**, and
**`chain-depth-frozen-clock-artifact`** (tick the oracle clock if a frozen clock
collapses distinct reads). P4.9G1's `system_jobs_routes_equivalence` is the
in-repo exemplar over this same fixture; its full regen recipe is in
`status-log.md` under "Lane record — P4.9G1". Run every differential **by name**
with `--nocapture`, zero SKIP, per-case OK lines visible.

## Fixtures

- **Consumes** the committed `system-data-{main,mount,llmlogs}.db` family. The
  backup differential wants at least one row in every collected table and at
  least one staged file **and** one doc-mount blob; extend via
  `build-system-data-fixture.ts` + `system-data.json` if thin.
- **If you change that fixture, this lane owns regenerating and re-running
  `system_jobs_routes_equivalence`** (it rides the same family) — and must flag
  the change in its lane record so the unifier re-runs G3's and G4's
  differentials too.
- **Committed archive fixture:** ship a small, sanitized `.zip` backup produced
  by v4 (or by the oracle run) as the restore differential's input so restore is
  testable without first running backup. Sanitize per the
  `quilltap-fixture-sanitizer` conventions.
- **The unifier's follow-on** (state it in the lane record so it is not lost):
  after §2's call site is wired, add a `mode: 'replace'` case to
  `system_restore_equivalence` — same archive, same fixture copy, diff the
  post-restore DB state against v4's — and re-run the family by name.
- Restore writes: run each case against a **fresh copy** of the fixture, never
  the committed file. Journal files are never fixtures — delete stray
  `.db-journal` after any regen (the standing P4.15 rule).

## Shared contract (BINDING — identical in all three orders)

### §1 — the wire surface is FROZEN

All sixteen §1 CoreRequest variants are already on main (`api/types.rs:2293-2375`)
and pinned by `crates/quilltap-harness/tests/p4_9g1_wire_contract.rs`. The SPA
(P4.9G2, CLOSED) is fully built against them. **No lane may change a variant's
name, field name, or field type.** If a port reveals a genuine mismatch with
v4's route body, STOP and report — it is a round-level decision, not a lane one.

The web-edge-only legs (no dispatch verb; the SPA reaches them raw via
`apiUrl()`), assigned by lane:

| Leg | Lane |
| --- | --- |
| `POST /api/v1/system/tools?action=export` — the NDJSON `.qtap` stream | **G4** |
| `POST /api/v1/system/tools?action=import-preview\|import-execute` (multipart) | **G4** |
| `GET /api/v1/system/backup/{id}` — the single-use zip download | **G5** |
| `POST /api/v1/system/restore?action=upload` — octet-stream → `{uploadId}` | **G5** |

### §2 — the cross-lane Rust seam: `delete_user_data` (G3 delivers, G5 consumes)

**P4.9G3** delivers, in `crates/quilltap-core/src/services/delete_all.rs`,
exactly:

```rust
/// v4 `lib/backup/restore/delete-service.ts:90` `deleteUserData(userId)` —
/// the full per-user wipe, ALSO used by restore's `replace` mode.
pub async fn delete_user_data(db: &Db, user_id: &str) -> Result<(), DbError>;
```

**P4.9G5** consumes it as restore's `mode: 'replace'` pre-step (v4
`restore/restore.ts:52`). Because the two lanes run in parallel, **G5 writes
that call site as a loud typed refusal** carrying the marker comment

```rust
// ACTIVATE-AT-UNIFY (P4.9G5 ← P4.9G3 §2): replace mode calls
// services::delete_all::delete_user_data(&db, user_id).await?
```

and ships the `system_restore_equivalence` differential covering
`mode: 'new-account'` only. **The unifier** flips the call site and adds the
`replace`-mode case to that differential (both lanes' orders say so; the recipe
is in G5's §Fixtures). No other cross-lane Rust dependency exists in this round;
if a lane finds one, STOP and report rather than inventing a second seam.

### §3 — shared-file regions and the conflict discipline

Four files are unavoidably touched by more than one lane. Each lane confines
itself to a **labelled region** so the cherry-pick conflicts stay local and
mechanically resolvable:

1. **`crates/quilltap-core/src/api/engine.rs`** — the single "not yet
   available" arm at `:736-746`. Each lane (a) **deletes only its own variant
   names** from that arm's `|` list, and (b) appends its real arms immediately
   ABOVE the arm, wrapped in `// ── P4.9G<n> arms ──` … `// ── end P4.9G<n> ──`
   markers. When all three land the residual list is empty and the refusal arm
   is deleted — **the last lane to be cherry-picked removes it**; the unifier
   verifies no orphan remains.
2. **`crates/quilltap-web/src/system_data_routes.rs`** — the `?action=` match
   arms in `system_tools_get` (:71) and `system_tools_post` (:132). Each lane
   adds its own actions in a `// ── P4.9G<n> ──` labelled block at the END of
   each match, before `other => unknown_action(other)`.
3. **`crates/quilltap-web/src/lib.rs`** — `build_router`. Only **G5** registers
   new paths (`/api/v1/system/backup`, `/api/v1/system/backup/{id}`,
   `/api/v1/system/restore`); **G3** registers `/api/v1/system/jobs` and the
   unlock alias; **G4** registers nothing (its legs ride the existing
   `/api/v1/system/tools` route). Add `pub mod` lines at the end of the
   existing block.
4. **`apps/web/e2e/settings-data-system-flow.spec.ts`** — per-lane blocks only:
   **G3** owns the `DELETE_ALL_SERVER_LANDED` constant (`:36`) and the
   `Delete All Data (destructive; last)` describe (`:193`); **G4** owns the
   `export → import round-trip` test body (`:176`); **G5** appends a NEW test
   inside the main describe, at the end. No lane reformats or reorders the file.
   `apps/web/e2e/global-setup.ts` is **G5-exclusive**; a lane needing a seed
   there must say so in its lane record instead of editing it.

`crates/quilltap-core/src/services/mod.rs` is **APPEND-ONLY SHARED** — each lane
adds its own `mod` declaration lines; the unifier union-merges. `docs/CHANGELOG.md`
and `docs/developer/porting/status-log.md` are append-only for everyone.

## Ownership (BINDING — identical in all three orders)

| Lane | Owns (exclusive) | Bumps |
| --- | --- | --- |
| **P4.9G3** (delete-all + REST remainders) | `services/delete_all.rs` (new), `api/system_data.rs`, the delete + jobs-collection + unlock-alias edges, `system_delete_data_equivalence` + its oracle case, the e2e delete-all describe + its gate constant | core, harness, web |
| **P4.9G4** (`.qtap` export/import) | `services/qtap_export/**` (new), `services/quilltap_import/**` (the existing seed-subset module — extends it), new `api/system_qtap.rs` (name indicative), the export/import REST + streaming + multipart legs in a new `quilltap-web/src/qtap_routes.rs`, `system_export_equivalence` + `system_import_equivalence` + their oracle cases, the e2e export→import test body | core, harness, web |
| **P4.9G5** (backup/restore) | `services/backup/**` (new, incl. the restore + uuid-remap submodules), new `api/system_backup.rs` (name indicative), new `quilltap-web/src/backup_routes.rs` + the `lib.rs` backup/restore registrations, any new host seam in `quilltap-host/src/{spine.rs,host.rs}`, `system_backup_equivalence` + `system_restore_equivalence` + their oracle cases, `apps/web/e2e/global-setup.ts`, its new e2e beat | core, harness, web, host |

Shared/append-only: `api/engine.rs`, `system_data_routes.rs`, `lib.rs`,
`services/mod.rs`, the e2e spec (all per §3); `CHANGELOG` + `status-log.md`.
**FROZEN for everyone: `api/types.rs`.**

⚠ All three lanes bump `core`, `harness` and `web` off the same base.
**Identical bumps merge SILENTLY at cherry-pick** — the unifier RECOUNTS as
base + total bumps (the standing gotcha; do not try to pre-sequence).

## Verification gate (run before declaring done)

1. `cargo fmt --all --check`
2. `cargo clippy --workspace --all-targets -- -D warnings` — plain **AND**
   `--features quilltap-core/native-transport`
3. `TZ=UTC cargo test --workspace` with this lane's env vars — zero failures.
   Remember `cargo test --workspace` is **fail-fast per binary**; use
   `--no-fail-fast` to see real blast radius.
4. This lane's differentials **by name**, `--nocapture`, zero SKIP, per-case OK
   lines visible.
5. Oracles regenerated fresh from `~/source/quilltap-server` at `e646f58b`
   (drift-check FIRST; if v4 moved, STOP and report).
6. `cargo build --release` (the web bin and the host are affected).
7. If tier 2's e2e beat landed: full Playwright over a fresh `ng build` dist +
   rebuilt debug bins.
8. Per-commit rule: follow `.claude/commands/commit.md` (CHANGELOG entry +
   version bumps per §Ownership + the full gate on every commit).

**Disk:** export `CARGO_INCREMENTAL=0` for the whole lane. Three parallel
worktrees at default settings will not fit (`target/debug/incremental` alone hit
64 GB in one past lane); with it off a worktree target stays in the 10–20 GB
range. Check `df -h ~` before starting and again mid-lane. This lane also stages
zips and extracted trees under `os.tmpdir()` during tests — clean up after
failed runs.
