# Work order P4.D54 — the `f4955e0e` Salon + UI-polish SPA drift: bugs 29/30 (tool-card faces, whisper targets), the SPA halves of 22/36/37 (projection consumers, the `allowToolUse` binding, the AllLLMPauseModal port), bug 42 (toast animation), bugs 39/40 (stale-divergence retirement)

> **STATUS: CLOSED — UNIFIED on main (2026-08-06, the f4955e0e convergence
> round).** All seven tier-1 items landed (bugs 29/30/22/36/37/42 +
> the 39/40 comment retirements); tier-2 item 8 (the projection
> round-trip beat) authored gated and ACTIVATED at unification
> (`PROJECTION_ROUNDTRIP_SERVER_LANDED` flipped true over P4.D53's
> live projection); **tier-2 item 9 (the AllLLMPauseModal live-opener
> e2e beat) DEFERRED LOUD** — it needs a seeded paused, all-LLM,
> zero-USER-message chat (a fixture + walk item for the next round
> that touches the salon e2e fixtures); the modal + helpers are fully
> spec-pinned (v4's suite block-for-block). **The §3 unification
> review fixed:** the take-over roster's missing `defaultImage`
> avatar fallback (now `participantAvatar()`, the house idiom). Lane
> record: `status-log.md` → "Lane record — P4.D54".

Part of the `f4955e0e` drift catch-up round (v4's "bugs 8–43" batch).
This lane is `apps/web/**` ONLY. It absorbs the SPA halves of v4
`bd419ae9` (session 6) and `99d5fc7d` (session 7), plus `ea4dc011`
(session 9) minus bug 41 (the `content_disposition` lib widening,
P4.D53's). Its server counterpart is **P4.D53** — the §1 contract
below is BINDING between the two lanes, and this lane's projection
consumers assume P4.D53's emissions exist (they meet at unification;
until then, code against the §1 names and gate live e2e beats
accordingly).

**v4 baseline for THIS round: `f4955e0e`** (4.8.0-dev.175,
2026-08-06, tree clean at planning). Classification: "Round planned —
the f4955e0e found-bugs convergence round" in `status-log.md`. **The
baseline move is the UNIFIER's** — do NOT edit `CLAUDE.md` or
`phase-4.md` on the branch.

**Drift-check at lane start:** `git log f4955e0e..HEAD --oneline` +
`git status --short` in `~/source/quilltap-server`. If HEAD moved:
classify; STOP only if a new commit lands on files this order names.

**Siblings (same round):** **P4.D53 (server — §1 binding)**, P4.D51,
P4.D52, P4.D55, P4.43 (which owns the `P49H2A_MAINTENANCE_LANDED`
constant flip — do not touch it).

**Worktree note:** own branch, own checkout; no `git stash`/
`git worktree` games with sibling lanes.

## Mandate

Mirror v4's client-side fixes case-for-case: the tool-card
attribution helper (bug 29), the whisper-target "you" label (bug 30),
the projection consumers that retire v5's write-only-select
divergence (bug 22), the one `allowToolUse` binding (bug 36), the
full `AllLLMPauseModal` port with its opener (bug 37 — a 148-LOC
dialog plus pure helpers, not a mirror), the toast entry animation
(bug 42), and the retirement of the two stale "v4-side fix queued"
divergence records that bugs 39/40 discharge. Port v4's new unit
tests case-for-case as `ng` specs. Do NOT touch: any Rust crate,
`harness/**`, `dogfood-findings.md` (the unifier's sweep), or the
`P49H2A_MAINTENANCE_LANDED` constant (P4.43's).

## Survey-verified starting points (2026-08-06, fresh at `f4955e0e`)

### Bug 29 — user-initiated tool card wears the operator's face (v4 `99d5fc7d`)
- v4 `group-tool-messages.ts`: `readInitiatedBy` exported (`:9`); new
  `resolveToolRowAttributionMessage(message, i, messages)` (`:45-84`):
  `systemSender || participantId` → return as-is;
  `readInitiatedBy(content) === 'user'` → `{...message, role:'USER',
  participantId: null}`; else the positional borrow stopping at a
  USER boundary. `VirtualizedMessageList.tsx:236-240` swaps the
  inline IIFE for the helper. Pin: v4's
  `group-tool-messages.test.ts` additions.
- v5 mirror sites: `apps/web/src/app/chat/chat-view-model.ts:307-324`
  (`resolveToolAvatar` — the verbatim port of the OLD borrow, with
  v4's pre-fix comment; call site `:357` in `buildRenderItems`);
  `apps/web/src/app/chat/group-tool-messages.ts:16-23`
  (`readInitiatedBy` is PRIVATE here — export it or move the logic,
  following v4's shape); the card itself
  (`apps/web/src/app/chat/tool-message.ts`, P4.17) needs no change.
  Specs: `chat-view-model.spec.ts`, `group-tool-messages.spec.ts`.

### Bug 30 — "whispered to you" (v4 `99d5fc7d`)
- v4: new `resolveWhisperTargetLabel(targetId, participantNames,
  currentUserId)` in `whisper-visibility.ts:66-84` (`'you'` when
  self-targeted, else name-or-`'unknown'`); threaded
  `currentUserId` through SalonView → VirtualizedMessageList →
  MessageRow. Pin: v4's `whisper-visibility.test.ts`.
- v5 mirror: `apps/web/src/app/chat/message-row.ts:586-599`
  (`whisperTargets` computed — the `|| 'unknown'` map);
  `staffWhisperNames` (`:601-610`) reuses it, so the fix lands in
  both labels for free; template at `:142`. **No prop threading
  needed** — `message-row.ts:364` already takes the full
  `ChatDetail` input and `chat().user.id` is in scope
  (`core-contract.ts:2583`); document the simplification. Put the
  helper in `apps/web/src/app/chat/whisper-visibility.ts` for shape
  parity with v4. Specs: `whisper-visibility.spec.ts`,
  `message-row.spec.ts`.

### Bug 22, SPA half — the projection consumers (v4 `bd419ae9`)
- v5's divergence records to retire (the fields now arrive per §1):
  `apps/web/src/app/chat/sidebar/chat-section.ts:38-61` (the long
  doc-comment that IS the record of this v4 bug; local-signal seeding
  at `:378-379`, reads/writes `:409/:467/:492`) and
  `visibility-section.ts:19-20/:40-55/:249-251/:338/:351` (the
  `showThinking`/`answerConfirmationOverride` half);
  `screens/salon/salon-conversation.ts:1844-1848` ("v4's chat GET
  never returns `timelineMode`"). Re-rule all three to v4's post-fix
  shape: seed from the projected `ChatDetail` fields, drop the
  write-only-select divergence, keep whatever v4's client keeps of
  in-session state. `core-contract.ts:2610-2611` already declares
  `timelineMode?`/`alertCharactersOfLanternImages?` (fix their stale
  doc-comments); add `showThinking`, `answerConfirmationOverride`,
  `allLLMPauseTurnCount` per §1.

### Bug 36, SPA half — the one binding (v4 `bd419ae9`)
- `apps/web/src/app/chat/tools/chat-tool-settings-modal.ts:36-56`
  carries the P4.9E4B disposition comment ending "if v4 ever grows
  the projection, one binding turns it on". v4 grew it. Make the
  binding (`profileToolsDisabled` from
  `connectionProfile.allowToolUse === false`), rewrite the
  disposition comment, spec the warning-box arm.
  `core-contract.ts:2993` already declares the key.

### Bug 37, SPA half — the AllLLMPauseModal port (v4 `bd419ae9`)
- v5's tier-3 deferral block at
  `screens/salon/salon-conversation.ts:1259-1277` records WHY the
  modal was never ported: it was unreachable in v4 itself. v4 gave it
  an opener — the deferral is void. Port v4's
  `components/chat/AllLLMPauseModal.tsx` (148 LOC) case-for-case,
  plus its `nextPauseAt`-family pure helpers into TypeScript (the
  Rust twins live in `crates/quilltap-core/src/all_llm_pause.rs` —
  port from v4's TS source, not from the Rust), and v4's opener
  effect (`SalonView.tsx:1220-1231`: open when `chat?.isPaused &&
  participantsWithImpersonation.isAllLLM`; no SSE change — it rides
  the existing chain-complete refetch). Spec the helpers against
  v4's test vectors; an e2e beat needs an all-LLM cast reaching the
  pause threshold — take it only if affordable, else spec-level
  coverage and say so in the lane record (loud, named).

### Bug 42 — toast entry animation (v4 `ea4dc011`)
- v4 defined the previously-nonexistent `slideInUp` keyframes
  (`globals.css:300-309`: opacity 0→1, translateY 0.75rem→0), added
  `.app-toast` with the inline `animation: 'slideInUp 0.3s ease-out'`
  kept, and a `prefers-reduced-motion` `animation: none !important`
  guard (`:311-315`).
- v5 deliberately shipped NO animation (`_toast.css:19-26` records
  the dead-CSS finding — now discharged). Add the entry animation +
  the reduced-motion guard to `.qt-toast` (v5 convention precedents:
  `_variables.css:927`, `_content.css:94`, `_chat.css:3211/:3257`),
  rewrite the `_toast.css` finding note, and keep v5's class shape
  (no dead `animate-in` utilities — match the OBSERVABLE behavior).

### Bugs 39/40 — stale-divergence retirement (v4 `ea4dc011`)
- Bug 39 (`.qt-text-danger`): v5 fixed first; v4 converged. Retire
  the 20-line divergence header at
  `apps/web/src/styles/qt-components/_utilities.css:376-393` down to
  a plain rule comment. NOTHING mechanized fires (the session-9 doc's
  "corpus" claim is wrong — no `_utilities.css` corpus exists);
  record that correction.
- Bug 40 (search-dialog portal): v5 fixed first (dogfood #45); v4
  converged with `createPortal`. Rewrite the PORTAL divergence
  comment at `apps/web/src/app/search/search-dialog.ts:172-197` to a
  convergence note; the e2e pin
  (`e2e/page-toolbar-flow.spec.ts:212`) stays as-is.

## Tiered deliverables

### Tier 1 — must land
1. Bug 29: the exported `readInitiatedBy` +
   `resolveToolRowAttributionMessage` mirror in `chat-view-model.ts`,
   v4's test cases as specs.
2. Bug 30: `resolveWhisperTargetLabel` + the `whisperTargets` 'you'
   arm (+ the free `staffWhisperNames` fix), specs, and the
   documented no-prop-threading simplification.
3. Bug 22: the `core-contract.ts` §1 mirror + the three re-ruled
   consumer sites, specs updated.
4. Bug 36: the binding + spec.
5. Bug 37: the AllLLMPauseModal + helpers port, the opener effect,
   `allLLMPauseTurnCount` on `ChatDetail`, specs.
6. Bug 42: the toast animation + reduced-motion guard.
7. Bugs 39/40: the two comment retirements (+ the corpus-claim
   correction in the lane record).

### Tier 2 — should land
8. An e2e beat for the projection round-trip (set a controlled
   select in the sidebar, reload, assert it survives — a real server
   round-trip once unified with P4.D53; author it
   ACTIVATE-AT-UNIFY-gated on a named constant if the lane's own
   branch cannot run it live).
9. The AllLLMPauseModal e2e beat (see above — take or defer loudly).

### Tier 3 — explicit loud deferrals
- None planned beyond the conditional item 9. Anything else defers
  loud and named, never silently.

## The differential requirement, restated

This lane is SPA-only: no Rust oracle family moves here. Its
equivalence obligations are (a) v4's new unit tests ported
case-for-case as `ng` specs (`group-tool-messages.test.ts`,
`whisper-visibility.test.ts`, the AllLLMPauseModal/`nextPauseAt`
vectors, the enrichment-consumer arms), and (b) the §1 name-for-name
contract diff against P4.D53's emissions at unification. Where v4's
test asserts exact strings ('you', 'unknown', label text), the spec
asserts the same bytes.

## Fixtures

None owned. The e2e instance's salon fixtures are P4.D53's if they
move; any ACTIVATE-AT-UNIFY beat that needs seeded state names its
seed requirement in the spec header for the unifier
([[e2e-playwright-traps]] §7 — gate on a NAMED constant, not a
capability probe).

## §1 Shared contract (BINDING — identical text in P4.D53)

No `api/types.rs` change: every item is a JSON-body shape. The SPA
mirror in `apps/web/src/app/core/core-contract.ts` is P4.D54's;
this lane's server emissions and P4.D54's TS types must agree
name-for-name (the unifier diffs them):

1. **Chat GET body — five additive fields** (emitted by
   `assemble_chat_get`, all v4 `?? null` except the count):
   `timelineMode: string | null`,
   `alertCharactersOfLanternImages: boolean | null`,
   `showThinking: boolean | null`,
   `answerConfirmationOverride: string | null`,
   `allLLMPauseTurnCount: number` (default 0).
2. **`participants[].connectionProfile.allowToolUse: boolean`**
   (default true) — present in the chat GET AND in chat-PUT /
   participant-action response bodies.
3. **Behavioral, no key change:** impersonate/stop-impersonate now
   mutate `participants[].controlledBy` (user on start; llm on
   profile-less stop) and rewrite `chats.compiledIdentityStacks`;
   remove-participant's reply carries the POST-cleanup
   `impersonatingParticipantIds`.

Conflict rule: P4.D53's emissions land first at unification; THIS
lane's `core-contract.ts` additions must be purely additive mirrors
of the names above.

## Ownership (BINDING)

THIS lane owns, all under `apps/web/`: `src/app/chat/
chat-view-model.ts`, `group-tool-messages.ts`, `message-row.ts`,
`whisper-visibility.ts`, `tools/chat-tool-settings-modal.ts`,
`sidebar/chat-section.ts`, `sidebar/visibility-section.ts`,
`src/app/screens/salon/salon-conversation.ts`, the new
all-LLM-pause modal + helper files, `src/app/core/core-contract.ts`
(additive per §1), `src/app/search/search-dialog.ts` (comment),
`src/styles/qt-components/_toast.css`, `_utilities.css` (comment),
their spec files, and any new e2e spec it authors. It must NOT
touch: any Rust crate, `harness/**`, P4.43's constant,
`dogfood-findings.md`. `CHANGELOG` and `status-log.md` are
append-only union-merge. Version bump: the SPA version per
`.claude/commands/commit.md`; the unifier recounts.

## Verification gate (before declaring done)

`ng test` clean (all new specs in); `ng build` clean; the full
Playwright suite green with any new live beat active and any gated
beat behind its NAMED constant (read [[e2e-playwright-traps]] before
diagnosing any red); no Rust gate owed (no crate touched — but run
`cargo test -p quilltap-web` if any e2e fixture seeding changed
web-side test fixtures, which this order does not expect).
Per-commit rules: `.claude/commands/commit.md`. Lane record appended
to `status-log.md` before handoff.
