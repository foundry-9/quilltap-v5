# Work order P4.70 — image-generation fidelity (the whole tool-input schema, the `[Image LoRA]` log context) + the harness fixture debts (`system-data-*` widening, the `latest_chat` background arm, the leaves header)

**Status:** CLOSED — UNIFIED on main at the follow-ups round unification (2026-09-02). The §3 review fixed on the unify branch: the in-process `image_profile_generate` route now runs v4's OWN `generateImageSchema` gate (prompt 1..4000 UTF-16 units, `count` 1..10, after the 404, before the tool) — `count: 20` had fallen through to the TOOL's schema and answered its fixed sentence where v4 answers `Validation error`; two new arms in `image_generate_route_equivalence` (`generate_count_over_max`, `generate_prompt_empty`, oracle regenerated from the pin 7 → 9 rows, mutation-proven by reverting the gate); the leaves `.test.ts` header's stage rename; a `connectionProfiles >= 1` vacuity tripwire on two `system_import_state` arms.

_(The lane's own close header follows.)_

**Status:** **CLOSED — all five deliverables landed** (Tier 1 items 1–3, Tier 2
items 4–5, plus item 6's disposition), unified-ready on branch
`claude/p4.70-image-schema-fixture-debts` (4 commits). Gate green: fmt, clippy
BOTH feature sets, 485 test binaries / 2,709 tests / 0 failed / 1 ignored /
zero SKIP, release build. Ordered 2026-09-02 (the follow-ups round: P4.67 ∥
P4.68 ∥ P4.69 ∥ P4.70 ∥ P4.71); carried out 2026-09-02.

⚠ **v4 drifted DURING the lane** — `main` moved from `303288fb4` to
`02d4efa1b` ("fix(memory): the turn-blocking distillation asks for the
interactive budget (bug 115)"), a `lib/` change on an already-ported surface.
**Every regen in this lane ran from the pinned worktree at `6d2a50382`**
(ledger §5.1), so nothing here imported it; the unifier must `/driftcheck`
before the baseline moves.

**Three of the order's own premises were refuted by measurement** — see the
lane records in `status-log.md` for the evidence:

1. v5 already had ALL FIVE `[Image LoRA]` warn sites, not two; the CALLER
   SPREAD was the gap.
2. The `system-data-*` rebuild is impossible as specified — the builder mints
   unpinned vault mount-point ids that the archive extender and an oracle case
   hard-code, so a rebuild cannot byte-preserve the rows the same order
   requires. Migrated in place through v4's own `compareSchemas` +
   `generateAlterStatements` instead.
3. The `projects_routes` normalize plant already existed AND still cannot
   discriminate: the overlay chokepoint folds the value first. Pinned the
   chokepoint (four arms) and recorded the GET's line as defensive depth, as
   v4 has it.

## Preamble

- **v4 oracle baseline: `6d2a50382`** (drift-ledger §1, probe re-run
  2026-09-02 at ordering: v4 checkout on `main`, tree clean, both logs
  empty, §3 EMPTY). Not a drift lane.
- **Freshness probe at lane start** (drift-ledger §2). Pass = v4 checkout
  on `main`, tree clean, `git log 6d2a50382..main` empty and `git log
  3a76b17df..bugfix` empty. **Any mismatch: STOP and report.**
- **Regen rule: NO PIN NEEDED** (ledger §1 as of 2026-09-02); a failed
  probe flips it to §5.1's pinned worktree at `6d2a50382`. All regens
  through the sweep driver; **the driver never runs a recording stage and
  warns on tracked-fixture writes** — the `system-data-*` rebuild is a
  deliberate tracked-fixture write, run its builder by hand and commit the
  result (memory note `sweep-recipes-write-into-the-repo`).
- **⚠ Drift line (2026-09-02 evening `/driftcheck`, after ordering):** v4
  `main` moved ONE commit past the baseline — `303288fb4` "Choose the
  Concierge state on the New Chat form" (`4.9.0-dev.116`; ledger §3, an
  UNPROCESSED PORT-NEW row that NO lane in this round owns). **The regen
  rule is now PIN REQUIRED at `6d2a50382`** (ledger §5.1 — a lane-unique
  detached worktree, `recipe_sweep.py --run <family> --v4 "$PIN"`). The §2
  probe passes against the REWRITTEN §1: v4 checkout on `main`, tree
  clean, `git log 303288fb4..main` empty, `git log 3a76b17df..bugfix`
  empty. Resume on that; a further move is again a STOP.
- **Sibling lanes (this round):** P4.67 (REST-edge query semantics —
  **§D: it consumes the committed `system-data-*` pair at its base and does
  not regenerate it**), P4.68 (core consolidation — **§F: the
  `episodic-recall-*` pair is its**), P4.69 (SPA follow-ups — owns
  Playwright), P4.71 (host gateway — owns `model/image_dialects.rs` if a
  base-URL seam is needed there; this lane does not touch it). §Shared
  contracts / §Ownership are binding and identical across all five orders.
- **Worktree:** per the `carryout` skill. `CHANGELOG` / `status-log.md`
  append-only. **No Playwright**; no SPA surface.
- **Long gates:** `run_in_background: true` + full log + sentinel; NEVER a
  `pgrep` loop; NEVER `| tail`. Never edit source during a workspace gate.
  `/tmp` fixtures collide across parallel lanes — stage under a
  lane-unique dir (memory notes `tmp-fixtures-collide-across-parallel-
  lanes`, `tmp-fixture-clobber-by-a-sibling-lane`).

## Mandate

(1) Make v5's `generate_image` tool validate its input as v4's Zod
`safeParse` does — every field, every enum, the `llmNumber` string
coercion on `count`, the fixed refusal sentence whatever field failed —
red-first over `image_generation_tier3`. (2) Restore v4's `[Image LoRA]`
log context and the `style-options` anchor, capture-pinned. (3) Repair the
`image_gen_leaves` recipe header. (4) Widen the committed `system-data-*`
fixture to the baseline schema vintage so the connection-profile import
leg measures again, re-running every consumer. (5) Give
`projects_routes_equivalence` its `latest_chat` background GET arm and a
discriminating normalize row. Must NOT touch: `image_gen/huggingface_
lookup.rs` or the `list-models`/`options-schema` verbs (closed at the
D138 follow-up — re-run only), `model/image_dialects.rs` (P4.71),
`services/**` (P4.68), any `quilltap-web` file (P4.67), the
`episodic-recall-*` pair (P4.68), the SPA (P4.69).

## Survey-verified starting points (2026-09-02)

1. **v4's whole schema** (`lib/tools/image-generation-tool.ts:29-76`):
   `prompt` `.min(1).max(4000)`; `negativePrompt` `.max(1000).optional()`;
   `orientation` `enum(['portrait','landscape','square']).optional()`;
   `size` `enum(['1024x1024','1792x1024','1024x1792']).default('1024x1024')
   .optional()`; `style` `enum(['vivid','natural']).default('vivid')`;
   `quality` `enum(['standard','hd']).default('standard')`; `aspectRatio`
   `enum(['1:1','3:4','4:3','9:16','16:9']).optional()`; `count`
   **`llmNumber(z.number().int().min(1).max(10)).default(1).optional()`**
   (max is 10 — the candidate's "`count: 20`" row is one refusal shape,
   not the bound). `lib/tools/llm-number.ts:33 llmNumber`: a
   `z.preprocess` that touches ONLY strings — trimmed, `''` handed back
   untouched (so the validator refuses it), `Number(trimmed)` NaN → the
   original string handed back (refused with the type message), otherwise
   the number; `"1001"` fails `max` exactly as `1001` does and `"6.5"`
   fails `.int()`; `true`/`null`/`[]` fall through untouched and are
   refused. `validateImageGenerationInput` (`:128`) = `safeParse(input)
   .success ? data : null`; the handler (`lib/tools/handlers/
   image-generation-handler.ts:729-736`) answers ONE fixed pair on `null`
   whatever field failed: `error: 'Invalid input: prompt is required and
   must be a non-empty string'`, `message: 'Image generation tool received
   invalid parameters'`.
2. **v5 today.** `tools/generate_image.rs:141 validate_image_generation_
   input` checks the prompt length only (its own doc: "prompt required,
   non-empty, ≤4000 (UTF-16)"); `:152-176 apply_tool_input_schema_defaults`
   applies the four defaults and says in its own comment that "the
   enum/length VALIDATION half of `safeParse` is deliberately not
   reproduced … recorded rather than silently narrowed". `ImageGeneration
   ToolInput` (`:130-140`) types `count: Option<i64>`, the enums as
   `Option<String>` — so `count: 20` passes v5 and generates where v4
   refuses (the recorded divergence), `style: "bogus"` passes, and a
   `count: "2"` (a string the LLM emitted) or `count: 1.5` is refused
   somewhere in v5's TYPED decode with some other sentence — **find that
   decode site** (the tool-call args → `ImageGenerationToolInput` step,
   in `generate_image.rs` or the tool dispatcher) and make the whole
   schema, coercion included, answer v4's fixed pair. The handler's call
   at `:1560` is the seam.
3. **The tier-3 family.** `crates/quilltap-harness/tests/
   image_generation_tier3_equivalence.rs` over `harness/oracle/cases/
   image-generation.test.ts` drives v4's REAL handler with the provider
   mocked (recipe in its header; memory note `image/moderation/search`
   family recipes via `lane-records-index`). Add the refusal rows RED-FIRST
   (`count: 20`, `count: "2"` → accepted as 2, `count: 1.5`, `count:
   "6.5"`, `count: ""`, `count: true`, `style: "bogus"`, `quality: "ultra"`,
   `orientation: "wide"`, `aspectRatio: "2:3"`, `size: "512x512"`,
   `negativePrompt` of 1001 UTF-16 units, `prompt` of 4001) and record the
   pre-fix reds. ⚠ the `/tmp/qt-imggen-*` pair is shared with
   `image_generate_route_equivalence` (the round-2 unification's
   collision) — stage lane-unique.
4. **The `[Image LoRA]` log context.** v4 `lib/image-gen/lora-support.ts`
   warns at FIVE sites (`:92` not-a-list, `:121` out-of-range scale,
   `:139` dropped malformed entries, `:165` stripping — no support
   declared, `:178` capping to the limit), each spreading the caller's
   `{context, chatId, jobId, profileId}`; v5 `image_gen/lora_support.rs`
   has `LoraLogContext` (`:143`, "the log context v4 spreads into every
   `[Image LoRA]` line") but only TWO warn sites (`:216`, `:254`) —
   measure which of the five are missing and which present ones drop the
   spread. The `tools.generate_image.style-options` anchor: v4
   `image-generation-handler.ts:869 context: 'tools.generate_image.style-
   options'`; v5 `generate_image.rs:1893` says "v5 has no style-options
   surface on this path" — read v4's surrounding block and land the
   anchor where v5's equivalent call sits (log-only). Pins: the
   thread-scoped capture layer (memory notes
   `a-process-global-test-seam-must-be-thread-scoped`,
   `capture-layer-target-assert-is-a-prefix-match`,
   `coalescing-hides-its-own-mutation-proofs`), one test per line, each
   mutation reddening exactly one.
5. **The leaves header.** `crates/quilltap-harness/tests/
   image_gen_leaves_equivalence.rs:22-30` stages into the SHARED
   `/tmp/qt-oracle-stage` (`STAGE=/tmp/qt-oracle-stage`) — a sibling
   family's regen can clobber it mid-run. Rename to a family-unique stage
   (`/tmp/qt-oracle-stage-image-gen-leaves`), keep regen/run NDJSON paths
   character-identical (`recipe-header-conventions`), `--show` the family
   and read BOTH stages before believing it.
6. **`ImageModelListing.loraSupport`** — "no reader on either side": verify
   v4's client (`grep -rn loraSupport components app`) really reads only
   the `options-schema`/editor path; if v4 has no reader, record the field
   as carried-for-parity with the v4 line and move on (no v5 reader
   invented).
7. **The `system-data-*` vintage gap.** `harness/oracle/fixtures/
   build-system-data-fixture.ts` (header: built through v4's REAL repos;
   three files; widened at P4.D46) predates `connection_profiles.
   multiCharacterPrefill` (P4.63's and P4.D135's records: "every
   connection-profile import in the family fails identically on both
   sides — the import has measured nothing since bug 68"; the P4.D135
   chain columns "land in the same hole"). Measure the gap FIRST: diff the
   fixture's `connection_profiles` (and every other table) DDL against
   `crates/quilltap-core/src/services/provisioning/fresh_schema.json` at
   the baseline; list every missing column (memory notes
   `generatedll-and-migration-ddl-disagree-on-defaults`,
   `fixture-vintage-gap-which-columns-are-fatal`,
   `migration-vintage-fixture-goes-stale`). Rebuild through the builder at
   the baseline so the DDL is production-identical, with EVERY
   pre-existing row byte-preserved cell-by-cell (a census over the old vs
   new pair) and `system-data.json` regenerated by the shipped generator
   (memory note `byte-exact-static-data-transcription`). Then make
   `system_import_state`'s connection-profile leg DISCRIMINATING
   (mutation: break the import → red) and re-run the other eight consumers
   by name, zero SKIP, unedited: `system_export_equivalence`,
   `system_jobs_collection_equivalence`, `system_jobs_routes_equivalence`,
   `system_import_equivalence`, `system_backup_equivalence`,
   `system_delete_data_equivalence`, `system_body_guards_equivalence`,
   `files_body_guards_equivalence` (the last three live in
   `crates/quilltap-web/tests/`). **§D:** if a consumer must CHANGE to
   stay green, that is a finding — record it for the unifier; do not edit
   a sibling's family.
8. **`projects_routes_equivalence`.** `:459-469` drives `project_background_
   get` for IOTA/KAPPA only — neither carries `backgroundDisplayMode:
   'latest_chat'` with a chat holding a story background, so v4's
   `actions/background.ts` latest-chat branch is unexercised. Add the
   fixture shape (a project in that mode + a chat with a story-background
   file) to the family's builder and the GET arm; and a project row
   PLANTED with a retired mode (`'project'`) so the GET's own normalize
   line discriminates (the 2026-09-02 walk's four-integrity-field plant —
   `dogfood-findings.md` Standing notes: `contentSha256`/`plainTextLength`
   AND `sha256`/`fileSizeBytes` move with the content). Red-first: revert
   the normalize → the planted row's arm must redden.

## Tiered deliverables

**Tier 1 (must land):**

1. The whole tool-input schema in v5 (item 1/2), `llmNumber` coercion
   included, v4's fixed refusal pair whatever field failed, the four
   defaults preserved; `image_generation_tier3` grown red-first with the
   rows in item 3 (pre-fix reds recorded by name); `image_generate_route_
   equivalence` re-run.
2. The `[Image LoRA]` context + the `style-options` anchor (item 4),
   capture-pinned one line each.
3. The `system-data-*` widening (item 7) with the cell census, the
   discriminating import leg, and the nine consumers re-run.

**Tier 2 (should land):**

4. The `projects_routes_equivalence` arms (item 8).
5. The leaves header repair (item 5); the `loraSupport` disposition
   (item 6).

**Tier 3 (explicit deferrals, loud):** any v4 refusal whose sentence
depends on Zod's own path-specific message (v4 answers the ONE fixed pair
here, so none is expected — record any found); the image-profile modal's
client half (P4.69's).

## The differential requirement (restated)

Items 1/8 are red-first corpus work against v4's REAL handler/route
(`image_generation_tier3`, `projects_routes_equivalence`); item 4 is
log-only, so its proof is the capture layer (a differential cannot see a
log-only fix — memory note `differential-blind-to-a-log-only-fix`); item
7's proof is the consumers re-run over the widened pair PLUS the
formerly-vacuous leg going red under mutation (a green regen is not
coverage — memory note `green-regen-is-not-coverage`; grep the changed
bytes in the regenerated NDJSONs). Every mutation verified-applied then
reverted by file backup.

## Fixtures

DELIVERS the widened committed `system-data-{main,mount,llmlogs}.db` +
`system-data.json` (§D — every pre-existing row byte-preserved; the nine
consumers re-run by this lane; P4.67's three re-run again by the
unifier). Consumes the image tier-3 fixtures its families build
(lane-unique `/tmp` paths) and the `groups-projects` pair for item 8
(widen through its builder if the shape needs a committed row; say so).

## Shared contracts (binding, identical across all five orders)

- **§A — well-formed actions never move.** P4.67 may change the answer
  to `?action=` (empty), an UNKNOWN action, and DUPLICATED keys only. The
  status + body of every well-formed `?action=<served>` request and every
  single-valued query is byte-identical before and after (the existing
  web families are the pin). P4.69's SPA callers (`image-profiles.api.ts`
  `?action=options-schema|list-models`, the wardrobe/characters edges)
  therefore need NO change and P4.69 makes none.
- **§B — participant-status parsing has ONE home:**
  `quilltap_core::chat_predicates::participant_status_from_str` (+
  `is_participant_present` / `json_participant_is_present`). P4.68 owns
  the consolidation; no other lane adds a private status match, and no
  lane changes the canonical unknown-string rule (`None`/`"active"` →
  Active, `"silent"` → Silent, `"removed"` → Removed, anything else →
  Absent).
- **§C — danger styling is a client predicate.** `shouldShowDangerStyling
  (chat) = getConciergeState(chat) === 'flagged'` lives ONLY in the SPA
  (`chat/concierge-state.ts:127`, v4 `chat-override.ts:110`); P4.69 binds
  it to the message list; no server payload changes (the §A list wire of
  the `6d2a50382` round stands).
- **§D — the `system-data-*` committed fixture is P4.70's this round.**
  P4.70 widens it (additively — every existing row byte-preserved,
  `system-data.json` regenerated by the shipped builder) and re-runs all
  nine consuming families without editing them (except
  `system_import_state`'s discriminating leg, which P4.70 owns). P4.67
  consumes the committed pair at its base. The unifier re-runs P4.67's
  three consuming families over the widened pair before the gate.
- **§E — the gateway seam.** P4.71 threads `with_localhost_gateway` from
  the host into every provider construction site and adds the ONE missing
  seam (`model/completion_provider.rs`'s hard `None`). No other lane
  constructs a wire provider or touches `quilltap-host/src/spine.rs`,
  `providers.rs`, or `host.rs`; P4.68's executor census is core-only and
  RECORDS any host-side finding for P4.71/the unifier instead of editing.
- **§F — `precompute_equivalence` and the `episodic-recall-*` pair are
  P4.68's.** P4.70's fixture work is the `system-data-*` pair only; the
  two lanes' fixtures are disjoint by name.

## Ownership (binding, identical across all five orders)

| Area | Owner |
|---|---|
| `crates/quilltap-web/src/**` (every `*_routes.rs`, `log_file.rs`, the NEW shared query reader), `crates/quilltap-web/tests/**` (NEW `query_param_semantics_equivalence`; arms added to `system_body_guards_equivalence` / `files_body_guards_equivalence`); `quilltap-core`: `api/types.rs` (the `FileUpload.tags` widening ONLY), `api/system_data.rs` (the `jobs_enqueue` payload gate ONLY), `api/chat_media.rs` (the `chat_file_link` guard ONLY); harness: `system_jobs_collection_equivalence` (the `payload: []` arm), NEW `harness/oracle/cases/query-param-semantics.test.ts` | **P4.67** |
| `quilltap-core`: `chat_predicates.rs`, `enclave/announce.rs`, `services/{commonplace_notifications, user_identity_resolver, fold_episode_pass, turn_orchestrator, participant_resolver, message_finalizer, build_context, orchestrator, provider_failover, cheap_llm_exec}.rs`, `tools/{self_inventory, whisper}.rs`, `tools/doc_edit/shared.rs`, the NEW bare-executor census guard; harness: `precompute_equivalence` + `harness/oracle/cases/precompute.test.ts` + `harness/oracle/fixtures/build-episodic-recall-fixture.ts` + the committed `episodic-recall-*` pair (RE-RUN `recall_replay_equivalence`, `vault_conv_search_equivalence`), the failover / chain-walk families (`orchestrator_tier3`, the `provider_failover` family) + their oracle cases | **P4.68** |
| SPA: `chat/{message-list, message-row, streaming-message, tool-message}.ts` (+specs), `screens/salon/salon-conversation.ts`, `quick-hide/quick-hide.service.ts` (+spec), `screens/settings/images/image-profile-modal.ts` (+spec), the twelve `qt-range` hosts' SPECS (suffix pins only), `styles/qt-components/_chat.css` (only if a rule is missing); e2e: `salon-optimistic-bubble-reconcile.spec.ts`, `workspace-search-documents-flow.spec.ts`, `wardrobe-flow.spec.ts`, NEW danger-avatar beat, `global-setup.ts` if the transfer un-park needs it | **P4.69** |
| `quilltap-core`: `tools/generate_image.rs`, `image_gen/lora_support.rs`, `image_gen/params_builder.rs`; harness: `image_generation_tier3_equivalence`, `image_gen_leaves_equivalence`, `image_generate_route_equivalence` (re-run), `projects_routes_equivalence` (+ its oracle case), `system_import_state` (the discriminating leg), `harness/oracle/fixtures/build-system-data-fixture.ts` + the committed `system-data-{main,mount,llmlogs}.db` + `system-data.json`, `harness/oracle/cases/image-generation.test.ts`, `image-gen-leaves.test.ts`; the other eight `system-data` consumers RE-RUN, not edited | **P4.70** |
| `quilltap-host`: `env.rs`, `providers.rs`, `spine.rs`, `host.rs`, `avatar_preview.rs`, NEW `host_gateway.rs`; `quilltap-core`: `model/completion_provider.rs` (the gateway seam), `model/transport.rs` (doc only), `provider_manifest/rewrite.rs` (header + the pure resolver if placed here), `model/provider_models_api.rs`, `api/provider_actions.rs`, `model/image_dialects.rs` (a base-URL seam ONLY if v4 rewrites there — measure); harness: NEW `host_gateway_equivalence` + `harness/oracle/cases/host-gateway.test.ts`; `Dockerfile`, `docs/developer/running.md` | **P4.71** |
| Playwright (port 4319) | **P4.69's branch only**; every other lane's beats are authored gated (ACTIVATE-AT-UNIFY) |
| `CHANGELOG`, `status-log.md` | append-only, all lanes |
| `drift-ledger.md` | lanes never write it |


**Version bumps:** this lane bumps `quilltap-core` and `quilltap-harness`
per commit (base: core 0.0.750, harness 0.0.642); `quilltap-web` only if
a web test venue file must move (it should not — §D); the unifier
recounts as base + total bumps.

## Verification gate before declaring done

`cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
-D warnings` on BOTH feature sets; full `cargo test --workspace` with the
lane's oracle env vars (`run_in_background`, full log + sentinel, every
`test result:` line read); the touched + consuming families by name
through the sweep driver, zero SKIP, changed bytes grepped (`Invalid
input: prompt is required`, the refusal rows' names, the `[Image LoRA]`
lines, the new `connection_profiles` columns in the fixture census); the
mutation proofs verified-applied then reverted; `cargo build --workspace
--release`. No SPA run, no Playwright. Per-commit rule:
`.claude/commands/commit.md`. Lane record → `status-log.md` ("Lane record
— P4.70"); this file's status header updated at close.
