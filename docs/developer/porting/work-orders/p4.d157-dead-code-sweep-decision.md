# Work order P4.D157 — the `d4138b96b` dead-code sweep: a per-family DECISION for the seven oracle cases it takes dark (fourteen deleted v4 exports), the LoRA-bounds new-home check, and the vestigial v5 twins

**Status (unification, 2026-09-05):** CLOSED — UNIFIED on main at the
`d883a5ee1` drift catch-up round unification. Tier 1 + Tier 2 landed as the
lane's header below says: thirteen symbols (the order's "fourteen" was one
high — its own item-1 list counts thirteen), every one option (ii) DELETE,
seven families SPLIT with their surviving rows byte-identical, none frozen,
no sweep exemption. **The Tier-3 items the lane could not edit landed at the
unification wire:** the two dangling intra-doc links (`model/completion_
provider.rs`, `model/transport.rs`) now name the `LlmErrorKind` taxonomy /
the failover detectors and cite the sweep, and `lib.rs`'s module-doc prose no
longer advertises the deleted classifiers and gauges. The drift ledger's §1
count was corrected at the baseline move. The §3 review of the deletions
found nothing to fix (no remaining reference to any deleted name outside the
sweep's own citations; the LoRA bounds pinned at their new home).

_(The lane's own close header follows.)_

**Status:** **CLOSED** on the lane branch `claude/p4-dead-code-sweep-40b441`
(2026-09-05) — Tier 1 and Tier 2 both landed; Tier 3 is recorded, not built.
Awaiting `/unify`.

- **Tier 1 — landed whole.** The evidence pair is recorded verbatim in the
  lane record; a decision landed per SYMBOL. **The count is THIRTEEN, not
  fourteen** — the order's own item-1 enumeration lists thirteen and the
  measurement agrees; the prose in this order and in drift-ledger §1 is one
  high (the ledger is NOBODY's to edit this round — flagged for `/unify`).
  **All thirteen resolved to option (ii) DELETE: not one twin had a
  production caller in v5.** So option (i) FROZEN is used ZERO times and
  option (iii) ZERO times, no family is frozen, no `recipe_sweep.py`
  exemption was added, and **no family retired — all seven were SPLIT**, so
  the workspace test-binary count does not move. The LoRA server pin landed
  with two mutation proofs.
- **Tier 2 — landed as a table with ZERO deletions owed.** The census over
  the other 38 deleted names, cross-checked against v4's own
  `DEAD-CODE-REPORT.md` at the pin, found no zero-caller v5 twin: 29 names
  are absent from v5 entirely, 4 have a LIVE twin of something v4 kept, and
  3 are the PRIMITIVE v4 kept rather than the wrapper it deleted. No
  maintenance candidate is opened.
- **Tier 3 — recorded for the unifier, not built** (as ordered): three stale
  doc references in files this lane may not edit (`model/completion_provider.rs:137`,
  `model/transport.rs:206`, `lib.rs:78-79`/`:87`), each with suggested
  replacement text in the lane record. **No `lib.rs` `pub use` re-export of
  any deleted symbol exists** (only `pub mod` declarations), and **no module
  was emptied**.
- **One ownership call recorded:** item 5 is Tier 1 and names
  `image_gen/lora_support.rs`, which is outside the Ownership row; the
  additive edit landed as ordered rather than narrowing a Tier-1 deliverable
  silently. No sibling lane owns `image_gen/**`.

## Preamble

- **v4 oracle baseline: `0b0617fee`** (drift-ledger §1). **This lane's
  target commit is `d4138b96b`** — "chore(dead-code): v4.9 release sweep,
  second pass (checklist item 5)" (2026-09-04): three modules deleted whole
  (`lib/database/meta.ts`, `lib/sillytavern/persona.ts`,
  `hooks/useNavbarCollapse.ts` — zero v5 references, measured) plus 32
  unreferenced exports removed across 23 files. **The consequential half:
  fourteen of those exports are imported BY NAME by seven committed oracle
  cases**, so at any pin past `d4138b96b` those families do not diff wrong —
  they fail to LINK. Read the hunks (`git -C ~/source/quilltap-server show
  d4138b96b --stat`; then per deleted symbol), the memory note
  `a-v4-dead-code-sweep-breaks-oracle-imports` (the recipe + the ruling's
  three options), and v4's `docs/developer/DEAD-CODE-REPORT.md` at the pin.
- **Freshness probe at lane start** (drift-ledger §2): v4 on `main`, tree
  clean, `git log d883a5ee1..main` empty, `git log 3a76b17df..bugfix` empty.
  **Any mismatch: STOP and report.**
- **Regen rule: PIN REQUIRED — and this lane needs TWO pins** (§B):
  `/tmp/qt-v4-pin-p4d157-d883a5ee1` (to PROVE the seven fail to link at the
  target) and `/tmp/qt-v4-pin-p4d157-0b0617fee` (the last sha at which they
  link — the frozen-family evidence). Never the live checkout.
- **Sibling lanes:** P4.D158's bulk neutrality sweep EXCLUDES the seven
  families by name (§A) — an unexplained `run_failed` on them in its
  artifact is THIS lane's, not a sweep defect. P4.D155 owns `cheap_llm.rs`
  (NOT `cheap_model.rs`, which is this lane's — two different modules).
  P4.D156 owns `lora-list-editor.ts` (this lane only READS it for the
  bounds check). §A–§H + Ownership binding and identical across all six
  orders.
- **Worktree:** per the `carryout` skill (branch `claude/p4-d157-…`).
  `CHANGELOG`/`status-log.md` append-only. **No Playwright** (§E).

## Mandate

Make a RECORDED, EVIDENCED decision for each of the seven families — never
"let the regen fail and call it drift" (the ledger row's instruction) — and
land it: per family, exactly one of **(i)** keep the v5 twin and re-home the
family as FROZEN at `0b0617fee` (the recipe header pins that sha, the runner
says so, the sweep driver's `--v4` override is refused for it by name),
**(ii)** delete the v5 twin and retire the family (oracle case + runner +
any `lib.rs` re-export noted for the unifier), or **(iii)** prove the v5
twin is LIVE in v5 even though v4's is dead (a production caller outside
`#[cfg(test)]`), keep it under a UNIT pin transcribed from the frozen
oracle's rows, and retire the differential. Plus the one check
`d4138b96b`'s own unification row flagged: the LoRA scale bounds moved to
`lib/image-gen/lora-scale.ts` (the profile editor had a byte-identical copy;
the `lora-support.ts` re-export is gone) — v5's two transcriptions must
still equal the bounds in their NEW home. Must NOT touch: `lib.rs` (§D —
record any re-export that should go), any module declaration, any file
outside the seven twin modules + the seven families, `lora-list-editor.ts`
(P4.D156 owns it; hand it the comment fix through the record).

## Survey-verified starting points (2026-09-05 — measured, ledger §5.3)

1. **The fourteen symbols → seven cases → seven runners** (all
   `crates/quilltap-harness/tests/…_equivalence.rs`; env var in brackets):
   - `cases/cheap-model.ts` ← `isCheapModel`, `estimateModelCost` (from
     `lib/llm/cheap-llm`; `getCheapestModel` SURVIVES) → `cheap_model_
     equivalence` [`QT_ORACLE_CHEAP_MODEL`]; v5 `cheap_model.rs:62/:98`.
     `is_cheap_model` has ONE production caller (`cheap_model.rs:117`, inside
     `get_cheapest_model`'s ladder?) — read whether `:117` is production or
     the test module; `estimate_model_cost`'s only caller is a test (`:156`).
   - `cases/model-selection.ts` ← `getModelsUnderCost`, `calculateCostTier`,
     `calculateSavings` (`lib/llm/pricing`) → `model_selection_equivalence`
     [`QT_ORACLE_MODEL_SELECTION`]; v5 `pricing.rs:113/:128/:145` — ZERO
     callers outside their definitions (measured).
   - `cases/llm-errors.ts` ← `handleProviderError`, `getUserFriendlyError`
     (`lib/llm/errors`) → `llm_errors_equivalence` [`QT_ORACLE_LLM_ERRORS`];
     v5 `services/llm_errors.rs:242 handle_provider_error` (four callers, all
     at `:352-393` = the test module — verify) and `:293 user_friendly_error`
     (the differently-named twin; zero callers found).
   - `cases/message-formatter.ts` ← `buildMultiCharacterContextSection`
     (`lib/llm/message-formatter`) → `message_formatter_equivalence`
     [`QT_ORACLE_MESSAGE_FORMATTER`]; v5 `message_formatter.rs:364`, called
     from `services/host_notifications.rs:301/:315` — i.e. from the roster
     builders below, which are themselves dead (next bullet). The case ALSO
     imports surviving symbols and loads the provider registry — retiring
     ONE symbol's rows must not take the whole family; split the case.
   - `cases/post-office-host.ts` ← `buildMultiCharacterRosterContent`,
     `buildMultiCharacterRosterOpaqueContent` (`host-notifications/writer`)
     → `post_office_host_equivalence` [`QT_ORACLE_PO_HOST`]; v5
     `services/host_notifications.rs:297/:311`, called ONLY from
     `post_host_multi_character_roster_announcement` (`:1051`), which has
     **ZERO callers in v5** (measured across `crates/`) — v5's twins are as
     dead as v4's; `git -C ~/source/quilltap-server log -S
     buildMultiCharacterRosterContent` shows the export survived TWO earlier
     knip sweeps (`f0e86852a`, 4.6.0) before this one. The case ALSO drives
     many surviving writer exports — split, don't retire whole.
   - `cases/chat-timestamp.ts` ← `formatTimestampForSystemPrompt`
     (`lib/chat/timestamp-utils`) → `chat_timestamp_equivalence`
     [`QT_ORACLE_CHAT_TIMESTAMP`]; v5 `chat_timestamp.rs:740`, callers at
     `:1054/:1058` (the test module — verify). ⚠ `cases/system-prompt.ts`
     MENTIONS `chat-timestamp.ts` in comments only (it pins the clock the
     same way) — measured: no import; `system_prompt_equivalence` is NOT
     affected. The case also drives surviving symbols — split.
   - `cases/token-estimation.ts` ← `getContextUsagePercent`,
     `getContextWarningLevel` (`lib/tokens/token-counter`) →
     `token_estimation_equivalence` [`QT_ORACLE_TOKEN_ESTIMATION`]; v5
     `token_estimation.rs:138/:163`; `get_context_usage_percent` is called
     from `:164` = inside `get_context_warning_level` (zero external
     callers) — a dead pair. The case also drives surviving symbols — split.
   Re-run the recipe from the memory note FIRST (it is cheap) — the ledger's
   count is a measurement from 2026-09-04, and a transitive case-to-case
   import would widen it (none found today: grep the seven case files'
   importers under `harness/oracle/cases`).
2. **Confirm the failure mode before deciding:** regenerate ONE of the seven
   at the `d883a5ee1` pin and record the exact `tsx` error (a missing named
   export fails at module evaluation — the NDJSON is EMPTY, the "empty-file
   trap" in ledger §5.1); then at the `0b0617fee` pin (links, diffs green).
   That pair of runs is the evidence every decision cites.
3. **The decision rule (from the ruling, applied per symbol not per file):**
   a twin with a PRODUCTION caller in v5 → (iii) — prove the caller is
   outside `#[cfg(test)]` by line number, transcribe the frozen oracle's
   rows for that symbol into a unit test beside the twin, retire the rows
   from the differential (split the case). A twin with NO production caller
   → (ii) — delete the function + its test-module rows + the oracle rows;
   the differential shrinks (count guard adjusted downward, recorded). (i)
   FROZEN is the fallback ONLY where the twin is live AND its behaviour
   cannot be pinned without v4 (e.g. it depends on a registry the unit test
   cannot build) — expect zero or one such family; each frozen family gets a
   header line `FROZEN at 0b0617fee (v4 d4138b96b deleted <symbols>)` and a
   `recipe_sweep.py` exemption by name (the driver's exempt list —
   `--self-test` must stay green).
4. **Splitting a case:** the deleted symbols' rows come out into no file;
   the surviving rows keep their `kind`s and order so every other row stays
   byte-identical (diff the pre/post NDJSON on the surviving kinds: EMPTY);
   the runner's count guard moves by exactly the removed rows.
5. **The LoRA bounds check:** v4 `lib/image-gen/lora-scale.ts`
   `DEFAULT_LORA_SCALE = { min: 0, max: 2, default: 1, step: 0.05 }` +
   `resolveLoraScaleBounds` (step falls back to the default's). v5:
   `crates/quilltap-core/src/image_gen/lora_support.rs:37` and
   `apps/web/src/app/screens/settings/images/lora-list-editor.ts:36
   DEFAULT_SCALE` — both measured `{0, 2, 1, 0.05}` today; pin the server
   constant against the four literals (a unit assert) and hand P4.D156 the
   comment re-point (its file) via the record. `IMAGE_PROFILE_LORAS_KEY` is
   also in the deleted set — grep v5 for a twin (`image_profiles` `loras`
   key constant) and confirm it is a plain string literal, not a re-export
   from a module that lost it.
6. **The other deleted exports** (51 names in the recipe's output, e.g.
   `createUuidRemapper`, `calculateSha256`, `validateRequestBody`,
   `requireAuth`, `queueMemoriesSchema`, `setStateSchema`, `getContextStatus`,
   `failCreationProgress`…): none imported by any oracle case (measured), so
   NO family moves — but the vestigial-cruft ruling says a v5 twin of a
   now-dead v4 export is a candidate for removal ONLY if it is dead in v5
   too. Do the census cheaply (grep each name's snake_case twin for callers)
   and record a table; delete only zero-caller twins inside the seven files
   this lane owns; list the rest for a later maintenance order — do not
   widen ownership.

## Tiered deliverables

**Tier 1 (must land):** item 2's evidence pair; a decision per SYMBOL
(fourteen rows: option, evidence line numbers, what was deleted / frozen /
unit-pinned), landed; the seven families either retired, split-and-green at
the `d883a5ee1` pin, or frozen with the header + exemption; the LoRA server
pin (item 5).
**Tier 2 (should land):** item 6's census table over the 51 names, with the
zero-caller deletions inside owned files.
**Tier 3 (explicit deferrals, loud):** module-level deletions and `lib.rs`
re-export removals (recorded for the unifier — §D); twins outside the seven
owned files (a named maintenance candidate).

## The differential requirement (restated)

For every SURVIVING family: regenerated at the `d883a5ee1` pin through the
sweep driver, run by name, zero SKIP, with the surviving rows proven
byte-identical to the pre-split oracle. For every FROZEN family: regenerated
at `0b0617fee` through the driver with the exemption in place, green. For
every RETIRED symbol under (iii): the transcribed unit rows fail when the
twin's body is mutated (one mutation per symbol, named). The evidence pair
(item 2) is recorded verbatim in the lane record.

## Fixtures

- Consumes: none (all seven are pure tier-1 families; `message-formatter`
  loads the provider registry from the pin's `plugins/dist` — the third
  symlink class).
- Delivers: the split/retired oracle cases + runners; no committed `.db`.

## Shared contracts (BINDING — identical text in all six orders)

**§A Family ownership for this round's regens.** Each family below is
regenerated and edited by ONE lane; P4.D158's bulk neutrality sweep
EXCLUDES every family in this section by name and hands any red in them to
the owning lane.

- **P4.D153:** `memory_injector_equivalence` (+ `harness/oracle/cases/
  memory-injector.ts`), `build_context_tier3_equivalence` (+ its builder
  `build-context-tier3.test.ts` and the `QT_FIXTURE_BC_*` pair),
  `carina_query_tier3_equivalence` (+ its fixture builder),
  `announcer_tier3_equivalence`.
- **P4.D154:** `message_context_leaves_equivalence` (+ `harness/oracle/
  cases/message-context-leaves.ts`), `orchestrator_tier3_equivalence` (+
  `orchestrator-tier3.test.ts` and the `QT_FIXTURE_ORCH_*` pair),
  `file_attachment_tier3_equivalence` (+ its fixture pair).
- **P4.D155:** `documents_routes_equivalence` (+ `documents-routes.test.ts`),
  `system_export_equivalence`, `answer_confirmation_tier3_equivalence`,
  `cheap_llm_fallback_equivalence`, `brahma_console_tier3_equivalence`,
  `brahma_console_routes_equivalence`, `brahma_orchestrator_tier3_equivalence`,
  every `pascal_*_equivalence` + `p4_6ay_workbench_wire_contract`, the
  self-inventory families, and whichever family the lane identifies as
  driving `cheap_llm::get_cheap_llm_provider` (named in its record).
- **P4.D156:** the CLI Tier R (`crates/quilltap-cli/tests/cli_differential.rs`)
  + `completion_behavior.rs`; the SPA unit specs under its paths.
- **P4.D157:** the seven families the dead-code sweep takes dark:
  `cheap_model_equivalence`, `model_selection_equivalence`,
  `llm_errors_equivalence`, `message_formatter_equivalence`,
  `post_office_host_equivalence`, `chat_timestamp_equivalence`,
  `token_estimation_equivalence` (+ their seven oracle cases).
- **P4.D158:** `request_builder_equivalence` + the committed request-envelopes
  corpus and its recorder, `request_builder_google_wire_equivalence`,
  `request_builder_google_equivalence`, `response_parse_equivalence`,
  `tool_wire_call_site`, the stream fixtures, `openrouter_sdk_pricing_equivalence`,
  the provider manifests generator + `provider_manifest/manifests/*.json`;
  the restore families for the `2edd823c0` arms (`system_restore_equivalence`,
  `system_restore_state`, `restore_vintage_state`,
  `system_restore_guards_equivalence`, the `build-restore-archive*` builders
  and any NEW committed archive); and the bulk neutrality sweep over every
  family NOT listed above.

**§B Pins.** The round's target baseline is **`d883a5ee1`** (v4 `main` HEAD
at ordering, 2026-09-05). Every lane regenerates from a LANE-UNIQUE detached
worktree pinned at `d883a5ee1` (`/tmp/qt-v4-pin-<order>-d883a5ee1`, the
three symlink classes, ledger §5.1) — with ONE exception: P4.D157's
frozen-family evidence regenerates at **`0b0617fee`** (the last sha at which
those seven cases link), from its own second pin. **PIN REQUIRED stands for
every lane until `/unify` moves the baseline** — the checkout's HEAD is the
target, but the checkout can go dirty mid-lane (§5.1), and a pin is the only
tree whose contents are known. Verify each pin by grepping a fresh NDJSON for
a marker only one tree has (e.g. `About another character` appears in a
`d883a5ee1` memory-injector oracle only once P4.D153's targeted cases exist,
and never in a `0b0617fee` one).

**§C Sweep serialization.** `recipe_sweep.py --run-all` is single-flight
repo-wide and belongs to P4.D158 alone; it runs AFTER P4.D153–P4.D157 have
closed their branches (execution arrangement in the round report). Lanes
153–157 run per-family `--run` regens only, and never against another
lane's §A families or their `/tmp` staging.

**§D Source-tree seams (who edits what shared file).**

- `crates/quilltap-core/src/services/mod.rs`: **only P4.D153** edits it (one
  line: `pub mod memory_subject;`). `crates/quilltap-core/src/lib.rs`:
  **nobody**. P4.D157 deletes FUNCTIONS and their tests inside files it owns
  but never removes a module declaration or a `pub use` in `lib.rs`; a module
  it empties is recorded for the unifier, not deleted.
- `services/message_context.rs` + `services/chat_files.rs` (the
  `MessageContextSeams` trait, its `Noop` and `Real` impls): **only
  P4.D154**. The trait gains ONE sibling method beside `load_lantern_images`
  for the USER-side loader (name it `load_user_attachments`; signature shape
  `(&self, file_ids: &[String], provider: &str) -> impl Future<Output =
  UserAttachmentLoad> + Send`, default no-op, `UserAttachmentLoad` a struct
  the lane defines beside `LanternLoad`).
- `db/characters_read.rs`: **only P4.D153** (`find_names_by_ids`, RAW path).
- `cheap_llm.rs`, `services/cheap_llm_exec.rs`: **only P4.D155**.
- `model/request_builder/anthropic.rs`: **only P4.D158** (one regex entry +
  the three comment corrections).
- SPA: `apps/web/src/app/pascal/**` → **P4.D155**; `apps/web/src/app/screens/**`
  and `apps/web/src/styles/**` → **P4.D156**. Nobody else edits `apps/web`.
- `crates/quilltap-cli/**` → **P4.D156** only.

**§E Playwright.** Port 4319 is P4.D156's for the round (it may run the
existing suite once at close; it authors no new beat unless its record says
why). Every other lane authors NO Playwright and runs none; the unifier runs
the full suite at the gate.

**§F The drift ledger.** Lanes never write `drift-ledger.md`. The fourteen
§3 rows were marked `ORDERED(p4.d15x)` at ordering; `/unify` marks them
ABSORBED / NO-PORT-RATIFIED when the baseline moves to `d883a5ee1`. The
fifteenth row (`15573c3a1`, bug 119) stays UNPROCESSED by the ledger's own
instruction — it belongs to the unported `p4.9k` surface.

**§G The `p4.9i2` help bank.** The help-file hunks in this drift
(`e288ae2ec` → `help/file-uploads.md` "A word on company"; `d883a5ee1` →
`help/memory-recall-relevance.md` "Whose Life Is It, Anyway?"; `e9a9c538e` →
four `help/` files incl. `homepage.md`'s In-Chat Navigation section and three
corrected `url:` values) are banked ONCE, by P4.D158, in its lane record under
a `p4.9i2 bank` heading; the unifier carries them into `phase-4.md`'s
candidate row. No lane ports help content.

**§H Versions.** Each lane bumps only the crates / the SPA it touches, per
`.claude/commands/commit.md` §6; the unifier recounts as base + total bumps
(the silent auto-merge trap). Base at ordering: core 0.0.776, harness 0.0.671,
web 0.0.114, host 0.0.96, cli 0.0.17, tauri 0.0.7, SPA 0.5.642.

## Ownership (BINDING — identical table in all six orders)

| Path | Owner |
|---|---|
| `crates/quilltap-core/src/memory_injector.rs`, NEW `services/memory_subject.rs` (+ the one `services/mod.rs` line), `db/characters_read.rs`, `services/build_context.rs`, `services/carina_query.rs`, `services/announcer/character_voiced.rs`; §A families for P4.D153 | P4.D153 |
| `crates/quilltap-core/src/services/message_context.rs`, `services/chat_files.rs`; §A families for P4.D154 | P4.D154 |
| `crates/quilltap-core/src/cheap_llm.rs`, `services/cheap_llm_exec.rs`, `api/documents.rs`, `services/brahma_console/**`, `services/qtap_export/**`, `tools/self_inventory.rs`, `crates/quilltap-core/src/pascal/**`, `apps/web/src/app/pascal/**`; §A families for P4.D155 | P4.D155 |
| `apps/web/src/app/screens/**`, `apps/web/src/styles/**`, `crates/quilltap-cli/**`; the CLI Tier R + `completion_behavior.rs` | P4.D156 |
| `crates/quilltap-core/src/cheap_model.rs`, `pricing.rs`, `services/llm_errors.rs`, `message_formatter.rs`, `services/host_notifications.rs`, `chat_timestamp.rs`, `token_estimation.rs` (function-level deletions/pins only); the seven §A families + their oracle cases | P4.D157 |
| `crates/quilltap-core/src/model/request_builder/anthropic.rs` (one regex line + comments); `harness/oracle/providers/**` + `harness/oracle/fixtures/request-envelopes/**`, `response-bodies/**`, the stream fixtures; `crates/quilltap-core/src/provider_manifest/manifests/*.json`; the restore-archive builders + any NEW committed archive + §A restore families; `docs/v4/**`; `harness/tools/sweep-results/<new artifact>` | P4.D158 |
| `docs/CHANGELOG.md`, `docs/developer/porting/status-log.md` | append-only, all lanes |
| `docs/developer/porting/drift-ledger.md`, `crates/quilltap-core/src/lib.rs` | NOBODY |

Every other path is unowned this round: a lane that believes it must touch
one STOPS and reports (the `carryout` rule 3), naming the file and the
sibling it suspects.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D
   warnings` in BOTH feature sets (a deleted function's orphaned test or
   `use` is a clippy/`dead_code` red — fix, do not `allow`); `cargo build
   --workspace`.
2. `cargo test --workspace` (full log + sentinel, never `| tail`) with the
   surviving families' env vars set (fresh oracles at the right pin per
   family) — each confirmed RUN, zero `SKIP:`; the retired families'
   binaries GONE (the binary count moves down by exactly the retired
   number — record it).
3. `python3 harness/tools/recipe_sweep.py --self-test` zero failures after
   any exemption edit; `--run <family>` by name for each surviving/frozen
   family.
4. Commits per `.claude/commands/commit.md` (core + harness bumps); the
   lane record appended to `status-log.md` with the fourteen-row decision
   table; this order's status header updated.
