# Work order P4.D113 — the wardrobe-containers drift, client half: the container browser dialog, the pinned editor, the component-carry transfer prompts

**Status:** CLOSED (2026-08-25, lane branch
`claude/wardrobe-containers-spa-port-8780ff`; unified on
`unify/f6a10055-round`). Tier 1 items 1–4 and 6 LANDED (the container
module 1:1 + the verb router; the container browser with `canManage`; the
pinned editor — **the order's mis-target question answered YES, v5 had
v4's latent bug and this port fixes it**; `imagePrompt` on Duplicate; the
download rider); item 5 landed EXCEPT its response-fields half, which was
REFUTED by measurement — v4's client never reads
`componentsTransferred`/`unresolvedComponentIds` (re-verified by grep at
unification), so rendering them would be v5-invented UI; the copy+move
refusal is surfaced v4's way (the combination is unreachable from the UI).
Tier 2: beats (a)/(c) LIVE; beat (b) armed at unification
(`P4D112_TRANSFER_COMPONENTS_LANDED` → true) but SELF-PARKS on the
committed fixture's missing General store — **widening the characters-*
e2e fixture with a shared container is the recorded follow-up**. The §2
contract was folded into `core-contract.ts` at unification and the
dispatch cast retired. One loud out-of-folder edit: the
`workspace-flow.spec.ts:253` selector rename follow (no owner collision).
Deferrals: the Prospero manager duplication RECORDED (v4's own "second
door"); help deltas → `p4.9i2`. Lane record: `status-log.md` → "P4.D113".

## Preamble

- **v4 baseline for this round: `f6a10055`** (v4 main, HEAD at planning
  2026-08-25, tree CLEAN, checkout on `main`). The round absorbs the FOUR
  commits past `0ba942b1`: `af1bc479` (gallery downloads — mostly sibling
  P4.D114, but its wardrobe-dialog hunk belongs HERE), `c93ec7ff` (bug 98 —
  sibling P4.D114), `d7263f39` + `f6a10055` (the wardrobe container feature
  pair — this lane's client half; sibling P4.D112's server half). `bugfix`
  at planning: HEAD `3a76b17d`, nothing unabsorbed beyond the tests-only
  `009c49b2`.
- **Drift-check at lane start.** `git log f6a10055..main --oneline` AND
  `git diff main bugfix -- lib/ app/ packages/`. If v4 moved past
  `f6a10055`, **STOP** and report. This lane's oracle is v4's CLIENT code
  (transcription parity specs + behavior reads) — read it at the pin;
  build a worktree pin only if the checkout dirties or moves.
- **Sibling lanes:** P4.D112 (`p4.d112-wardrobe-containers-server.md` —
  meets this lane ONLY at the §Shared contract; its verbs may not exist on
  your branch, so live beats that need them are **gated
  ACTIVATE-AT-UNIFY**) and P4.D114 (`p4.d114-downloads-bug98.md` — fully
  disjoint). §Ownership below is the binding split (identical in all
  three orders).
- **Worktree:** per the `carryout` skill; `npm ci` in the worktree's
  `apps/web`, never `cp -a` node_modules (NG0203 — see the memory note).
  `docs/CHANGELOG.md` and `status-log.md` append-only.

## Mandate

Port the client half of v4's wardrobe-containers feature pair: the
dialog's **top menu becomes a container selector** listing every place a
wardrobe item can live (each character, Quilltap General, each project,
each group — matching the transfer-destination roster), **browsing a shared
container grants the full kebab** (Edit, star, Duplicate, Move, Copy,
Delete) plus in-place creation via the `canManage` predicate, the **item
editor is pinned to the browsed container** (creates POST into it, edits
PUT back to it — v4 fixed a latent bug where any shared edit targeted
Quilltap General; measure whether v5's editor shares it), **Duplicate
preserves the Portrait Cue** (`imagePrompt`), the transfer dialog **hides
the item's known home** from the destination list and gains the
**component prompt** (moving an outfit offers move / copy / leave its
same-container components; copying offers copy / don't; copy+move is
refused), and the `af1bc479` rider: the wardrobe avatar-preview download
goes through the shared `core/download-utils.ts`.

**Must NOT touch:** anything in `crates/` (P4.D112/P4.D114); any SPA file
outside `apps/web/src/app/wardrobe/**` and `apps/web/e2e/wardrobe-flow.spec.ts`
(P4.D114 owns the photos/scriptorium/generate-image/home surfaces); the
Prospero project-wardrobe screens (`apps/web/src/app/screens/prospero/**`)
— v4's commit did not touch its project-wardrobe manager either; if the
container browser makes part of it redundant, RECORD that, don't refactor.

## Survey-verified starting points (fresh survey 2026-08-25, at `f6a10055`)

### v4 — read whole: `git show d7263f39` and `git show f6a10055`

- `lib/wardrobe/wardrobe-container.ts` (new, 73 lines, client-safe):
  `WardrobeContainerScope`, `WardrobeContainer { scope, id }`,
  `GENERAL_CONTAINER`, `encodeWardrobeContainer` (`` `${scope}:${id ?? ''}` ``),
  `decodeWardrobeContainer` (null on mangled), `sameWardrobeContainer`,
  `wardrobeCollectionUrl` / `wardrobeItemUrl` (the four REST families —
  in v5 these become the dispatch-verb router, see §Adaptation).
  **Transcribe 1:1** with a parity spec (the byte-exact static-data
  discipline).
- `lib/hooks/use-wardrobe-container-items.ts` (new, 98 lines): the
  shared-container loader (list + refresh) the dialog uses when browsing a
  non-character container. In v5 this becomes a loader in
  `wardrobe.api.ts` / the dialog service.
- `components/wardrobe/wardrobe-control-dialog.tsx` (334-line rework):
  the top selector now lists containers (characters + General + projects +
  groups); character view keeps the merged four-tier read; shared-container
  view lists that container's items with full kebab + in-place creation;
  Duplicate adds `imagePrompt: item.imagePrompt ?? null` to the payload.
- `components/wardrobe/wardrobe-item-row.tsx`: new optional
  `canManage?: (item) => boolean` predicate replacing the
  `isShared = !item.characterId` rule — full kebab iff manageable, else
  Move/Copy only + the `· shared` badge; default falls back to the
  character-view rule.
- `components/wardrobe/wardrobe-item-editor.tsx`: new
  `container` / `containerLabel` props; a non-character container pins the
  editor (creates/edits route to `wardrobeCollectionUrl`/`wardrobeItemUrl`
  of THAT container; the "Add to" selector is replaced by a destination
  note; the isDefault helper text gains a group arm: `'Worn by default by
  every character in this group'`); component-picker candidates become the
  container's items + General archetypes (General alone when it IS the
  container).
- `components/wardrobe/WardrobeTransferDialog.tsx` (78 + 52 lines):
  accepts the item's known home and hides it from the destination list;
  when the item is an outfit, a components radio appears (move: Move
  components / Copy components / Leave them; copy: Copy components /
  Don't); sends `source` (when browsing a shared container) and
  `components` on the POST; surfaces `componentsTransferred` /
  `unresolvedComponentIds` from the response.
- The `af1bc479` wardrobe hunk: the avatar-preview download in
  `wardrobe-control-dialog.tsx` switches from a hand-rolled anchor to
  `triggerDownload` from `lib/download-utils`.

### v5 — the counterparts (all under `apps/web/src/app/wardrobe/`)

- `wardrobe-control-dialog.ts` (1310 lines) — `WardrobeControlDialogInner`
  / `WardrobeControlDialog` / `WardrobeTabView`. **The header row today is
  a character `<select>` ONLY** (~line 167, "Character selector
  (v4 :887-918)") — no container picker exists. Four-tier merged read:
  `loadCharacterWardrobeItems` in `wardrobe.api.ts:130` (personal >
  group [`characterWardrobeList` with `scope:'group'`] > project >
  General), `dialogProjectId` from input or `chatGet(chatId).projectId`.
- `wardrobe-item-editor.ts` (775) — measure its current edit routing
  BEFORE porting: does a shared item's edit PUT target Quilltap General
  (v4's latent bug, likely transcribed faithfully)?
- `wardrobe-item-row.ts` (329) — has the `isShared`-style rule to replace
  with `canManage`.
- `wardrobe-transfer-dialog.ts` (288) — exports `encodeDestination` /
  `decodeDestination`; Destination `<select>` optgroups General /
  Projects / Groups / Users over `wardrobeTransferDestinations`;
  `wardrobeTransferApply` at :267.
- Also in-folder (touch only as the port requires): `wardrobe.api.ts`,
  `wardrobe-dialog.service.ts`, `avatar-generation-pane.ts` (the likely
  v5 home of the avatar-preview download), `item-editor/
  wardrobe-component-picker.ts`, `dissolve-bundles.ts`, `outfit-store.ts`.
- Shared download util (owned by nobody this round — consume, don't
  edit): `apps/web/src/app/core/download-utils.ts` —
  `triggerBlobDownload(blob, filename)` / `triggerUrlDownload(path,
  filename)`. The Tauri-native save arm is a recorded non-goal (the
  util's own header).
- e2e: `apps/web/e2e/wardrobe-flow.spec.ts` (669 lines — create/default/
  compose, hairdo, preview, staging + `set_all`, the bug-61 race beat).
- Other dialog hosts to leave working:
  `screens/characters/view/tabs/wardrobe-tab.ts`,
  `screens/prospero/wardrobe/project-wardrobe-manager.ts`.

### Adaptation notes (v5 mechanics)

- v4's `wardrobeCollectionUrl`/`wardrobeItemUrl` return REST URLs; v5's
  mutations ride dispatch verbs (`characterWardrobe*`, `projectWardrobe*`,
  the new `groupWardrobe*`, and the General-tier `wardrobe*` verbs).
  Transcribe the container module's types/encode/decode/same 1:1, and
  implement the URL helpers as a **verb router** (container → the right
  dispatch call) in `wardrobe.api.ts` — record the mechanism divergence in
  the component header as previous SPA lanes do.
- The React→Angular porting memos apply: `porting-a-react-component-to-
  angular` (bare props / `booleanAttribute` / interpolation spacing),
  `angular-custom-element-host-is-inline` (any new host element needs its
  display class), `dom-rebuild-refires-mouseenter` (the dialog rebuilds
  rows — keep `@for` track stable), `angular-select-cannot-mirror-react-
  controlled-value` (the new container `<select>` — assign in an
  afterRenderEffect if it must follow state).
- `form-rebuilt-json-bag-drops-unrendered-keys`: the Duplicate payload
  now carries `imagePrompt` — check no other key is silently dropped
  while you're in that builder.

## Tiered deliverables

**Tier 1 (must land):**

1. `wardrobe-container.ts` transcribed 1:1 + parity spec (encode/decode/
   same round-trips, mangled-value nulls, the general-needs-no-id rule).
2. The dialog's container selector + shared-container browsing (list via
   the container loader; full kebab via `canManage`; in-place creation;
   `· shared` badge semantics). Character view's merged behavior
   unchanged (guard with a spec).
3. The pinned item editor (container routing, destination note, the
   group-arm helper text, component-picker candidate scoping) — with the
   mis-target measurement recorded (does current v5 PUT shared edits to
   General? if yes, this port FIXES it — say so in the lane record).
4. Duplicate preserves `imagePrompt` (payload spec pin).
5. The transfer dialog: known-home hiding, the components radio pair,
   `source`/`components` on the wire, the copy+move refusal surfaced,
   response fields rendered. Wire-shape spec against the §Shared contract.
6. The `af1bc479` rider: avatar-preview download through
   `triggerBlobDownload`.

**Tier 2 (should land):**

7. e2e beats in `wardrobe-flow.spec.ts`: (a) open the container selector,
   browse Quilltap General, create + edit an item in place; (b) a
   composite move with "Move components" asserting the components appear
   at the destination and vanish from the source; (c) the copy+move
   refusal. Beats needing `groupWardrobe*` or the new transfer fields are
   **gated ACTIVATE-AT-UNIFY** (`P4D113_SERVER_LANDED`-style constant,
   flipped by the unifier); beats expressible against existing verbs
   (General/project tiers use existing CRUD) go live NOW.

**Tier 3 (explicit deferrals — loud):**

- Group-tier in-place browsing beats stay gated until unification (the
  server verbs are P4.D112's).
- The Prospero project-wardrobe manager's potential convergence onto the
  container browser: RECORD only.
- `help/wardrobe.md` / `help/groups.md` / `help/project-wardrobe.md`
  deltas → the `p4.9i2` bank.

## The differential requirement, restated

Client-half lanes prove against v4's CLIENT code: transcription parity
specs for `wardrobe-container.ts` (character-for-character, like the
`isUserDrivenSeat` twins), wire-shape specs for every new request body
field and response read, and behavior specs mirroring v4's dialog logic
read at the pin. Mutation-prove the load-bearing ones (e.g. break
`canManage` fallback → spec reds; drop `imagePrompt` from Duplicate →
spec reds). The e2e beats close the loop to persisted state
(`dogfood-verify-through-persisted-state`).

## Fixtures

- Consumes the e2e seed fixture (`apps/web/e2e/support/` — the salon/
  wardrobe seeds). If a beat needs a seeded group with a wardrobe item,
  extend the seed through the API/UI, never SQL against store-overlay
  properties (`store-overlay-properties-cannot-be-sql-seeded`).
- Owns no oracle fixtures.

## Shared contract (BINDING — identical text in P4.D112 and P4.D113)

1. **Five dispatch verbs** (serde camelCase over the Request enum),
   mirroring the `ProjectWardrobe*` field shapes exactly:
   - `groupWardrobeList { groupId }` → `{ mountPointId, wardrobeItems }`
   - `groupWardrobeCreate { groupId, item }` → `{ wardrobeItem }`
   - `groupWardrobeGet { groupId, itemId }` → `{ wardrobeItem }`
   - `groupWardrobeUpdate { groupId, itemId, item }` → `{ wardrobeItem }`
   - `groupWardrobeDelete { groupId, itemId }` → `{ success: true }`
   Error bodies: v4's — 404 `Group`, 404 `Group wardrobe item`, 400 with
   the schema's message on create/update.
2. **Transfers body additions** on `wardrobeTransferApply`:
   `source?: { scope: 'character'|'project'|'group'|'general', id?: string }`,
   `components?: 'move'|'copy'|'none'`. Legacy `sourceCharacterId` /
   `sourceProjectId` keep working; at least one of
   `sourceCharacterId`/`source` is required (v4's refine sentence).
3. **Transfers response additions:** `componentsTransferred: number`
   (always) and `unresolvedComponentIds: string[]` (only when non-empty).
4. **The client-safe container module** (P4.D113 transcribes v4's
   `lib/wardrobe/wardrobe-container.ts` 1:1): scope strings
   `'character'|'general'|'project'|'group'`, `encodeWardrobeContainer` =
   `` `${scope}:${id ?? ''}` ``. The server never sees this encoding — it is
   client-only — but the scope-string spellings above are shared with the
   transfers `source.scope` field and MUST NOT drift.

## Ownership (identical table in all three orders)

| Lane | Owns (exclusive) |
|---|---|
| P4.D112 | `crates/quilltap-core/src/{api/types.rs, api/engine.rs, api/groups.rs, api/wardrobe.rs, services/wardrobe_transfers.rs, vault_overlay.rs, db/vault_wardrobe_write.rs, db/vault_wardrobe_public.rs, db/archetype_wardrobe.rs, db/vault_read_overlay.rs}`; `crates/quilltap-web/src/wardrobe_routes.rs` + its tests; harness wardrobe/vault/groups families + their oracle cases + fixture builders. **Bumps `quilltap-core` + `quilltap-harness` versions.** |
| P4.D113 | `apps/web/src/app/wardrobe/**` (the whole folder, incl. `wardrobe.api.ts` and the new `wardrobe-container.ts`), `apps/web/e2e/wardrobe-flow.spec.ts`. **Bumps the SPA version.** |
| P4.D114 | `crates/quilltap-web/src/files_routes.rs` + `crates/quilltap-web/tests/binary_routes.rs`; `crates/quilltap-core/src/api/projects.rs`; `crates/quilltap-harness/tests/projects_routes_equivalence.rs` + its oracle case/fixture; SPA: `apps/web/src/app/screens/photos/**`, `apps/web/src/app/screens/scriptorium/file-detail-row.ts`, `apps/web/src/app/screens/generate-image/**`, `apps/web/src/app/images/image-gallery.ts`, `apps/web/src/app/screens/home/quick-actions-row.ts` (+ their specs), `apps/web/e2e/{photos-flow,scriptorium-flow,generate-image-flow,home-flow}.spec.ts`, a new clipboard util under `apps/web/src/app/core/` if needed. **Bumps `quilltap-web` version ONLY** — its core/harness/SPA edits ride un-bumped; the unifier recounts. |
| Everyone | `docs/CHANGELOG.md`, `docs/developer/porting/status-log.md` — append-only. |

## Verification gate (before declaring done)

- `npm test` (the wrapper — never a bare `ng`; `--filter` matches TEST
  NAMES, not paths) and `npm run build` clean in `apps/web`.
- Playwright: the wardrobe spec green including the new live beats; the
  full suite green (run in the worktree with its own `npm ci`).
- The parity/wire specs mutation-proven as listed.
- No crate touched → no cargo gate needed; if that changes, STOP —
  ownership was violated.
- Per-commit rule: `.claude/commands/commit.md`.
