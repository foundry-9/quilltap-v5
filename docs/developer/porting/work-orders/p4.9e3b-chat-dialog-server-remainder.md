# Work order P4.9E3B — the chat-dialog server remainder (tools inventory, export, search-replace, message reattribute, llm_choose, timestampConfig)

> **STATUS: CLOSED — UNIFIED on main (2026-07-27).** Nothing OPEN. At the
> unification the one verb this lane added beyond the frozen §1 —
> `ChatGroupStores` — was mirrored into the SPA's `core-contract.ts` so both
> sides describe the same surface; **it has no client consumer yet**, its only
> caller being the `LibraryFilePickerModal` P4.9E3C deferred by name. All four
> of E3C's ACTIVATE-AT-UNIFY beats flipped live over this lane's verbs and pass.
> Round record: `status-log.md`.
>
> The lane-close header follows for history:
> **STATUS: CLOSED — all seven tier-1 units + both tier-2 items landed
> (2026-07-27, branch `claude/chat-dialog-server-porting-d64442`, five
> commits).** Every §1 verb is LIVE (no in-flight refusals remain); the two
> §1 REST edges serve; the new committed `chat-dialogs-{main,mount}.db`
> family carries six differential families (all green over fresh `e8a49597`
> oracles, mutation-tested). Tier 2: `group-stores` PORTED (a new
> `ChatGroupStores` dispatch verb — an audit-mandated addition beyond the
> frozen six, recorded for the unifier + E3C); `attach-mount-file` DEFERRED
> loudly by name (the vision-LLM describe seam — the web edge refuses it
> instead of falling through to the multipart parser). `photo-albums`
> verified present. The lane record (status-log.md) carries the regen
> recipes, the stop-impersonate v4 bug, and the deferral list. Lane 2 of 4
> in the "embedding repair + chat-dialog family" round. Server-only;
> `apps/web/**` untouched.
>
> **v4 baseline for this round: `e8a49597`** (v4 HEAD, 2026-07-27,
> 4.8.0-dev.108 — REVISED mid-planning: the file that was dirty at survey time,
> `app/api/v1/chats/[id]/custom-tools/route.ts`, landed as the Bug 5 fix while
> these orders were being written). That one commit is pascal-surface drift
> owned by the round's fourth lane, **P4.D24**. **No chat-dialog oracle
> imports the drifted file (verified at planning)** — this lane's surveyed v4
> sites are all elsewhere and were re-verified unchanged by the commit's stat —
> so this lane regenerates its oracles straight from the checkout at
> `e8a49597`. P4.D24 owns the baseline-paragraph move; do not edit
> `CLAUDE.md`/`phase-4.md` baselines.
>
> **Drift-check at lane start**: in `~/source/quilltap-server` run
> `git log e8a49597..HEAD --oneline` and `git status --short`. If v4 moved or
> the tree is dirty, **STOP and report before porting anything.** Pin a
> detached worktree only on drift/dirty (`oracle-regen-pinned-v4-worktree`).
>
> **Siblings (run in parallel, disjoint ownership — see §Ownership):**
> - **P4.6BL** — the EMBEDDING_GENERATE job handler
>   (`p4.6bl-embedding-generate-handler.md`). Also appends to `engine.rs`,
>   `services/mod.rs` and `quilltap-host/src/spine.rs` — the §2 discipline is
>   binding on both lanes. It owns the `engine.rs:2661–2668` refusal arms; do
>   not touch them.
> - **P4.9E3C** — the ChatModals dialog SPA
>   (`p4.9e3c-chat-dialogs-spa.md`). It consumes **every §1 verb and REST edge
>   in this order** — the §1 contract is written verbatim in both orders and
>   is BINDING.
> - **P4.D24** — the `e8a49597` operator-perspective drift re-port
>   (`p4.d24-operator-perspective-drift.md`). Owns `api/custom_tools.rs` and
>   the pascal route family. No dependency either way.
>
> **Worktree note:** run on your own branch in your own checkout. Do **not**
> use `git stash` or `git worktree` inside the v5 repo (standing rule,
> `CLAUDE.md`).

## Mandate

Land every server half the `p4.9e3` dialog family still lacks, so the sibling
SPA lane's dialogs sit on live verbs rather than refusals:

1. **The tool inventory** — v4's `GET /api/v1/tools`
   (`app/api/v1/tools/route.ts`, 727 LOC), the reason `ChatToolSettingsModal`
   and `RunToolModal` refuse by name today. This is the round's largest single
   cost.
2. **Chat export** — the SillyTavern-JSONL `?action=export` arm (the one
   Organize-section item whose server half is genuinely missing).
3. **The search-replace family** — v4's standalone
   `POST /api/v1/search-replace` route (preview + execute, chat AND character
   scope, messages AND memories). v5 has only the DB leaf.
4. **Per-message reattribution** — v4's
   `POST /api/v1/messages/{id}?action=reattribute` (NOT `bulk-reattribute`;
   a different verb the m6 table mis-annotates).
5. **The `outfit-summary` read** — a P4.9f1 named deferral the merge modal's
   step 2 needs.
6. **The two `llm_choose` refusals** — closed by a host-side cheap-LLM driver
   on the `HostRegenerateTitleRunner` pattern (the logic is ALREADY ported;
   this is a plumbing job — see the survey).
7. **The `TimestampConfigSchema` write normalization** — deferred twice; this
   lane owns both straddled files now, so it lands here.

**Must NOT touch:** `apps/web/**` (P4.9E3C's); `services/embedding_*`,
`db/embedding_status.rs`, `db/conversation_chunks.rs`, `db/doc_mount_chunks.rs`,
`db/help_docs.rs`, and the `engine.rs:2661–2668` refusal arms (P4.6BL's).

## Survey-verified starting points (2026-07-27 — do not re-derive)

> Surveyed at v4 `c1507f47` / v5 `d2097503`. Several planning premises were
> corrected — read the ⚠ flags.

### 1. The tools inventory route — `app/api/v1/tools/route.ts` (727 LOC exactly)

| Region | Lines |
| --- | --- |
| Imports (49 tool-definition symbols) | :16–65 |
| `BUILT_IN_TOOL_SCHEMAS` map (consulted only when `includeSchemas=true`) | :70–108 |
| `BUILT_IN_TOOLS` array — **the inventory data, 288 lines, ~44 entries** | :113–401 |
| `AvailableTool` interface | :403–422 |
| `GET` handler | :429–727 |

Query params (:431–432): `chatId` (optional — adds per-tool `available` /
`unavailableReason`), `includeSchemas=true` (optional — attaches OpenAI-format
`parameters`). Response: `successResponse({ tools, count })` (:719–722); entry
shape `{ id, name, description, source: 'built-in'|'plugin', category?,
pluginName?, subgroupId?, subgroupDisplayName?, available?,
unavailableReason?, userInvocable?, parameters? }`.

Built-in categories: `utility, media, search, project, help, wardrobe,
documents, photos`. Entry ids in order (:115–400): `ask_carina, send_mail,
list_email, generate_image, search, search_web, project_info, help_search,
help_settings, help_navigate, rng, run_custom, state, self_inventory, whisper,
wardrobe_list/read/create/update/archive/wear/take_off,
doc_read_file/write_file/str_replace/insert_text/grep/list_files/
read_frontmatter/update_frontmatter/read_heading/update_heading/move_file/
copy_file/delete_file/create_folder/delete_folder, keep_image, list_images,
attach_image`. `request_full_context` and `submit_final_response` are excluded
by name (:574).

What it reads: `repos.chats.findById` (:454–463 — participants,
`imageProfileId`, `projectId`); `repos.connections.findById` (:468–471 —
`allowWebSearch`); `repos.characters.findById` (:485–488 —
`canDressThemselves`/`canCreateOutfits`);
`repos.projectDocMountLinks.findByProjectId` (:502 — `hasDocumentStores`);
`repos.pluginConfigs.findByUserId` (:524–527); **`toolRegistry.getAllPlugins()`**
(:540 — `lib/plugins/tool-registry.ts`, 668 LOC +
`lib/plugins/interfaces/tool-plugin.ts`, 347 LOC), plus a second pass for
`getToolHierarchy` (:616–639); `isWebSearchConfigured()` (:665). The
availability switch is :648–715 (nine arms; the `doc_*` arm requires BOTH a
project and linked doc stores).

⚠ **v5's `tools/executor.rs:90–151` `BUILT_IN_TOOLS` (55 names) is NOT this
inventory** — it carries internal-only names v4's route excludes
(`request_full_context`, `submit_final_response`) plus ~15 others, and has no
display names, descriptions or categories. Build the inventory table fresh
from v4's :113–401; do not reuse the executor constant.

⚠ **v5 has no plugin runtime at all.** Determine at lane start whether the v4
checkout's registry returns plugins on a default install (the oracle worktree
recipe symlinks `plugins/dist/*`, so it may). If the oracle output carries
plugin entries, pin the divergence explicitly (the `EXPECTED_DIVERGENCES`
precedent, `system_import_equivalence.rs`) rather than silently filtering —
v5 returns built-ins only, and the lane record names the plugin arm as the
standing deferral it already is.

Client consumers (context for the response shape, not this lane's):
`ChatToolSettingsModal.tsx:50` (`?chatId=…`, no schemas),
`RunToolModal.tsx:80` (`?chatId=…&includeSchemas=true`),
`ProjectToolSettingsModal.tsx:43` (no chatId at all — the no-chat arm must
work).

### 2. Chat export — an arm on the chat GET handler, ONE format only

- Handler: `app/api/v1/chats/[id]/handlers/get.ts:46–127` (82 lines of a
  576-LOC file). **SillyTavern JSONL only** — no json/markdown/text switch.
- Formatter: `exportSTChatAsJSONL` at `lib/sillytavern/chat.ts:222` (file is
  248 LOC).
- Reads: `chats.findById` (:48), `getMessages` filtered `type === 'message'`
  (:53–54), `characters.findByIds` over every CHARACTER participant (:68–72);
  a `participantId → characterName` map (:74–78) with broken-vault fallback to
  the primary name.
- Response: raw body, `Content-Type: application/x-ndjson`,
  `Content-Disposition: attachment;
  filename="{primaryCharacter.name}_chat_{chat.createdAt.getTime()}.jsonl"`
  (:114–122). Failure arms: `notFound('Chat')`, `notFound('No character in
  chat')` :61, `notFound('Character')` :82, `serverError('Failed to export
  chat')` :125.
- v4's client is a bare `window.location.href` navigation
  (`ChatSidebar.tsx:1510–1512`) — **no dialog exists**; the v5 need is a byte
  route, and v5 already has the natural home: the chats GET `?action=` fan-out
  in `crates/quilltap-web/src/wardrobe_routes.rs:183–210`, which today serves
  `outfit` and refuses `outfit-summary` by name (:204–207).

### 3. The search-replace family

- Route: `app/api/v1/search-replace/route.ts` (**106 LOC**) — a standalone
  collection route. `execute` → :33–61, `preview` → :67–91; missing/unknown
  action → `badRequest('Action parameter required: execute or preview')`
  (:104).
- Body schema :18–27: `scope` is a discriminated union on `type`
  (`{type:'chat', chatId: uuid}` | `{type:'character', characterId: uuid}`),
  plus `searchText` (min 1), `replaceText`, `includeMessages` (prefault true),
  `includeMemories` (prefault true).
- Service: `lib/search-replace/search-replace-service.ts` (**234 LOC**) —
  `getSearchReplacePreview` :75, `executeSearchReplace` :128. **The scope
  union covers characters as well as chats, and memories as well as
  messages.**
- Repository leaf: `lib/database/repositories/chats-search.ops.ts` (248 LOC).
  **v5 already has this leaf ported**: `db/chats_search.rs` (289 LOC —
  `count_messages_with_text` :103, `find_messages_with_text` :124,
  `search_messages_global` :153, `replace_in_messages` :222), currently
  called only by `chats_search_equivalence.rs`. The port is the service +
  verbs over an existing leaf.

### 4. Per-message reattribute — `app/api/v1/messages/[id]/route.ts`

Schema :31–33: `{ newParticipantId: z.uuid() }`. Handler
(`handleReattributeAction`, ~:336–395): find the message across the user's
chats (`notFound('Message')`); validate the target participant is in the chat
(`badRequest('Target participant not found in chat')`); **delete every memory
sourced from the message** via `findBySourceMessageId` +
`deleteMemoryWithVector` (failures logged and swallowed, counted); update only
the row's `participantId`; bump the chat's `updatedAt` via an empty
`chats.update`; respond with the updated message + `memoriesDeleted`.

### 5. `outfit-summary`

v4: `app/api/v1/chats/[id]/actions/outfit.ts:100` (`handleGetOutfitSummary` —
per-character equipped outfit with resolved item titles), dispatched at
`handlers/get.ts:144–146`. v5: the fan-out refuses it by name
(`wardrobe_routes.rs:204–207`, citing this exact v4 site). Small port; the
merge modal's step 2 consumes it.

### 6. The two `llm_choose` paths — plumbing, not logic

**The executor is ALREADY ported and live on chat-create:**
`services/outfit_selections.rs` — `apply_outfit_selections` :333, the
`llm_choose` branch :352–353, `async fn apply_llm_choose` :424; the
`ChatCreateSpine` builds its per-call logging cheap executor at
`quilltap-host/src/spine.rs:1610`.

The two refusals to close:

- **add-participant** — `services/chat_participants.rs:1233`
  (`apply_outfit_for_added_participant`), refusal :1269–1279: a
  `tracing::warn!`, **no write, request still succeeds**.
- **merge-conversation** — `services/chat_merge.rs:552–565`: a hard
  `Response::error(ErrorKind::Internal, …)` that **aborts the whole merge**.

⚠ v4 does neither: on ANY failure both call sites fall back to
`resolveDefaultOutfit` and never throw
(`lib/wardrobe/apply-outfit-selections.ts:212–330`, warn + fall through).
Unify both v5 sites to that shape when the driver lands.

⚠ The two call sites have **different default modes**: add-participant
defaults to `'default'` when the selection is absent
(`actions/participants.ts:186–189`, no `sourceChatId` so `previous_chat`
degenerates to `default`); merge defaults to `'previous_chat'`
(`lib/chat/apply-chat-merge.ts:237–241`, context includes `sourceChatId`).

v4's prompt half: `lib/memory/cheap-llm-tasks/outfit-selection.ts` (194 LOC;
`OUTFIT_SELECTION_PROMPT` :20–36, `chooseLLMOutfit` :52) — check what
`apply_llm_choose` already covers before porting anything twice.

**The driver template is `HostRegenerateTitleRunner`** (this exact family,
P4.9E3A): trait `services/chat_admin.rs:677`; `EngineAssembly` field
`engine.rs:216` (doc :211–215); `ReadyEngine` :424; accessor
`ready_regenerate_title` :3983; host impl `spine.rs:612–626` with the per-call
logging executor :623–625; products field :2613–2614; wiring :2834–2840.
`ChatCreateDriver` (`api/chat_create.rs:43–45`, host `spine.rs:1679–1690`) is
the heavier pattern — prefer the small runner.

### 7. `TimestampConfigSchema` — the probe-verified spec

Schema: `lib/schemas/settings.types.ts:76–95`, nine keys in declaration order:
`mode` (default `'NONE'`), `format` (default `'FRIENDLY'`), `customFormat`
(nullable-optional), `useFictionalTime` (default false),
`fictionalBaseTimestamp` (n-o), `fictionalBaseRealTime` (n-o), `autoPrepend`
(default true), `timezone` (n-o), `intervalMinutes` (int min 1, default 15).

⚠ **The re-parse is the REPOSITORY-WRITE one, not a request-level one.**
`chatUpdateRequestSchema` has no `timestampConfig` key at all; the parse fires
in `base.repository.ts:119–130` via `chats.repository.ts:60`
(`ChatMetadataBaseSchema`, `chat.types.ts:1004`), reached from `_create`
(:233) and `update` (:278–286). Only chat-CREATE accepts `timestampConfig` in
a request body (`chats/route.ts:120`, wrapped in
`ensureFictionalBaseRealTime` :1119–1120).

The P4.d18 probe pinned (status-log.md:35673–35759, probe :35700–35731):
schema-declaration key order; unknown keys stripped; the five keyed defaults
materialized on `{}`; explicit `null` survives while absent stays absent;
**bad values REJECT (v4 answers 400)**; and the parse runs at the repository
write, which is what produces the observed slot-5 `fictionalBaseRealTime`
placement. The owed follow-up as written: port the normalization at the
chats **write and update** with its 400 arm. Until then a partial config
saved from the SPA lands missing v4's defaults.

### Where to append on the boundary

`api/types.rs` is 3292 LOC; `Request` closes at :2611 with the last region
`P4.9E3A` at :2514–2610 — **append the §1 region immediately after :2610**.
`Response` closes at :2934; last region `P4.9E3A` (`ChatAdmin(Value)`)
:2919–2932 — append after :2932. Both E1A and E3A used the
`serde_json::Value` passthrough shape because v4's handlers each return a
differently-shaped literal; follow it. The dispatch boundary carries no
per-verb status (types.rs:2928–2930) — byte legs and download headers belong
at the quilltap-web edge, exactly as `characters_routes.rs:373–391` does.

### ⚠ A v4 bug found during the survey — record, don't inherit

v4's stop-impersonate is **unreachable from its own client**: the client sends
`DELETE ?action=stop-impersonate` (`useImpersonation.ts:94, :121`) but the
action is registered only on the POST map (`handlers/post.ts:129`), and the
DELETE handler hard-rejects unknown actions (`handlers/delete.ts:32–35`). Not
in v4's `found-bugs.md`. v5's `ChatStopImpersonate` (types.rs:282,
engine.rs:1297) already models it correctly as one verb — **nothing to do in
this lane** except record it in the lane record so the human can report it to
the v4 side.

## Tiered deliverables

### Tier 1 — must land

1. **`ToolsList`** — a new service/api module owning the v4-faithful inventory
   table (fresh transcription of route.ts:113–401 + the schemas map — the
   `byte-exact-static-data-transcription` memory note applies: consider a
   checked-in generator/dump), the nine-arm availability switch, the
   userInvocable exclusions, and the plugin arm's explicit disposition. REST
   edge: `GET /api/v1/tools` in a new `quilltap-web` route (query params
   `chatId`, `includeSchemas` — names pinned in §1). Differential:
   `tools_inventory_equivalence` driving v4's REAL route over a committed
   fixture — minimum: no-chat arm, chat arm with and without a project +
   doc stores, `includeSchemas` on/off, a character with wardrobe flags off
   (the wardrobe availability arms), web-search unconfigured.
2. **`ChatExport`** — the JSONL formatter ported byte-for-byte
   (`lib/sillytavern/chat.ts:222`), the dispatch verb, and the byte leg on
   the existing chats GET fan-out (`wardrobe_routes.rs`) at v4's exact URL
   (`?action=export`) with v4's exact `Content-Type` and filename. All four
   failure arms. Differential: byte-exact NDJSON + filename over a
   multi-character chat (attribution map), a broken-vault participant
   (fallback name), and a no-character chat (the refusal arm).
3. **`ChatOutfitSummary`** — port `handleGetOutfitSummary` (outfit.ts:100),
   replace the named refusal in the fan-out. Differential cases over the new
   fixture (an equipped outfit with resolvable titles + a bare character).
4. **`SearchReplacePreview` / `SearchReplaceExecute`** — port
   `search-replace-service.ts` over the existing `db/chats_search.rs` leaf +
   whatever the character/memories halves need. Differential (tier-2 DB
   diff): both actions × both scope types × memories on/off × a no-match
   case, plus the unknown-action rejection.
5. **`MessageReattribute`** — port the handler (find-across-chats, in-chat
   target validation, the memory-deletion sweep with swallowed failures, the
   single-row update, the `updatedAt` bump). Differential: happy path with
   memories to delete, target-not-in-chat, missing message.
6. **The `llm_choose` driver** — one new host seam on the
   `HostRegenerateTitleRunner` template, threaded into BOTH call sites;
   v4's fall-back-to-default-on-failure semantics at both; the two distinct
   default modes preserved. Differential: tier-3 (canned cheap-LLM completion
   injected both sides — `tier3-completion-oracle`) driving add-participant
   and merge with `llm_choose`, including a failure case that must fall back
   to the default outfit rather than error.
7. **The `TimestampConfigSchema` normalization** at the chats repository
   write AND update (`db/chats.rs` + the `api/salon.rs` `chat_update` path),
   with the 400 arm. Extend the existing chat-timestamp differential cases
   with non-schema-shaped configs (scrambled key order, unknown keys,
   partial bags, explicit nulls, a bad value → 400) — the P4.d18 capstone
   cases deliberately sent schema-shaped configs; stop doing that.
   ⚠ The Zod `.nullable().optional()` three-state needs
   `Option<Option<…>>` (`p4.6an-dangerous-content-parse-gap`), and JSON
   column key order follows the schema declaration
   (`json-column-key-order`).

### Tier 2 — should land

8. `attach-mount-file` (`POST /chats/{id}/files?action=attach-mount-file`) —
   a standing named deferral from P4.6ah that `LibraryFilePickerModal` needs
   for its mount-file leg. Audit its size first; if it stays deferred, the
   SPA lane's picker degrades that leg loudly and this order's record says
   so.
9. An audit that `group-stores` (`GET /chats/{id}?action=group-stores`) and
   `photo-albums` reads the picker consumes exist in v5 (photo-albums landed
   P4.6ab; group-stores — verify), porting whichever small read is missing.

### Tier 3 — explicit deferrals (loud, named — never silent stubs)

- The plugin arm of the tools inventory (v5 has no plugin runtime — an
  existing standing reality; name it in the response-shaping code and the
  lane record).
- Anything in tier 2 that turns out oversized — refuse by name with the v4
  file:line.

## The differential requirement, restated

Every ported unit ships with an equivalence test driving v4's **real** code.
This lane is **tier 2** (structural DB diff / response diff) for items 1–5
and 7, and **tier 3** (canned completion both sides) for item 6.

- **Closest recipes:** `chat_admin_routes_equivalence` (route-level tier-2
  over a committed family, this exact surface),
  `wardrobe_routes_equivalence` (the outfit family),
  `system_export_equivalence` (byte-exact NDJSON export),
  `tier3-completion-oracle` + the `chat_create` llm_choose coverage for
  item 6.
- **Memory notes that apply:** `oracle-regen-silent-stale-pass`,
  `harness-corpus-shape-constants-rot`, `oracle-fire-and-forget-tail`,
  `oracle-jest-testtimeout`, `chain-depth-frozen-clock-artifact`,
  `project-store-fixture-needs-real-create`, `jest-real-db-oracle`.
- **`TZ=UTC` is load-bearing** wherever v4 formats a date (the export
  filename embeds `createdAt.getTime()` — millisecond-exact, so the fixture
  pins it).

## Fixtures

- **Delivers:** a new committed `chat-dialogs-{main,mount}.db` family built by
  v4's REAL repo `create` calls: a multi-character chat with messages and
  memories sourced from specific messages (reattribute + search-replace), a
  character vault with searchable text and memories (the character scope), an
  equipped outfit with resolvable item titles, a chat with a project + linked
  doc stores and one without (the tools availability arms), and a
  wardrobe-flags-off character.
- **Consumes:** nothing another lane owns. **Do not widen existing committed
  fixtures** — escalate to the unifier if you think you must.

## Shared contract (BINDING)

### §1 — the wire surface (written verbatim and identically in P4.9E3B and P4.9E3C)

Added to `Request` in `crates/quilltap-core/src/api/types.rs`
(`#[serde(tag = "type", rename_all = "camelCase")]`), in a new labelled
`// === P4.9E3B: the chat-dialog server remainder (§1, frozen) ===` region
appended immediately after the `P4.9E3A` region (after :2610).

```rust
/// The tool inventory (v4 `GET /api/v1/tools`, app/api/v1/tools/route.ts:429).
#[serde(rename_all = "camelCase")]
ToolsList {
    #[serde(default)] chat_id: Option<String>,
    #[serde(default)] include_schemas: Option<bool>,
},
/// Chat transcript export as SillyTavern JSONL (v4 `GET …/chats/{id}?action=export`).
#[serde(rename_all = "camelCase")]
ChatExport { chat_id: String },
/// Per-character equipped-outfit summary (v4 `GET …/chats/{id}?action=outfit-summary`).
#[serde(rename_all = "camelCase")]
ChatOutfitSummary { chat_id: String },
/// Search & replace dry-run (v4 `POST /api/v1/search-replace?action=preview`).
/// `scope` is v4's discriminated union: {"type":"chat","chatId":…} |
/// {"type":"character","characterId":…}. The two include flags default TRUE
/// (v4 prefaults) — absent means true, not false.
#[serde(rename_all = "camelCase")]
SearchReplacePreview {
    scope: serde_json::Value,
    search_text: String,
    replace_text: String,
    #[serde(default)] include_messages: Option<bool>,
    #[serde(default)] include_memories: Option<bool>,
},
/// Search & replace execute (v4 `POST /api/v1/search-replace?action=execute`).
#[serde(rename_all = "camelCase")]
SearchReplaceExecute {
    scope: serde_json::Value,
    search_text: String,
    replace_text: String,
    #[serde(default)] include_messages: Option<bool>,
    #[serde(default)] include_memories: Option<bool>,
},
/// Re-attribute ONE message (v4 `POST /api/v1/messages/{id}?action=reattribute`).
/// Also deletes every memory sourced from the message (v4 route.ts:342+).
#[serde(rename_all = "camelCase")]
MessageReattribute { message_id: String, new_participant_id: String },
```

**Binding notes.**
- **Response bodies are NOT pinned here.** This lane defines its own response
  types (the `serde_json::Value` passthrough precedent); the §1 freeze is the
  request shapes plus the two REST edges below.
- **Two REST edges are part of the contract** (the SPA navigates to them
  directly): `GET /api/v1/tools?chatId=…&includeSchemas=true` (query-param
  names exactly as written) and `GET /api/v1/chats/{chat_id}?action=export`
  (a byte download: `application/x-ndjson` +
  `Content-Disposition: attachment; filename="{name}_chat_{ms}.jsonl"`).
- `scope` stays `serde_json::Value` because v4's union is discriminated on
  `type`, which collides with the enum's own serde tag if lifted; the service
  validates it against v4's schema exactly (uuid checks included).
- If any shape must change, it is a cross-lane escalation — record it for the
  unifier, do not change it unilaterally.

### §2 — shared files and the conflict discipline (identical in P4.6BL)

| File | Discipline |
| --- | --- |
| `crates/quilltap-core/src/api/types.rs` | Append the §1 region after `P4.9E3A` (:2610) and the Response region after :2932. Do not edit existing variants. P4.6BL does not touch this file. |
| `crates/quilltap-core/src/api/engine.rs` | Append dispatch arms + the driver's `EngineAssembly`/`ReadyEngine` fields and accessor in labelled `// === P4.9E3B ===` regions. Do NOT touch :2661–2668 (P4.6BL's refusal arms). |
| `crates/quilltap-core/src/services/mod.rs` | Append `pub mod` lines within your own labelled region. |
| `crates/quilltap-host/src/spine.rs` | Append the driver impl + products wiring in a labelled `// === P4.9E3B ===` region; P4.6BL appends its own handler region — do not reorder either. |

`CHANGELOG` and `docs/developer/porting/status-log.md` are append-only for
everyone and union-merge at unification. If you must EDIT a line another lane
owns, stop and record a cross-lane escalation.

## Ownership (BINDING — identical in all three orders)

| Lane | Owns exclusively |
| --- | --- |
| **P4.6BL** | `services/embedding_generate_job.rs`, `db/embedding_status.rs`, the `update_embedding` additions to `db/conversation_chunks.rs`/`db/doc_mount_chunks.rs`/`db/help_docs.rs`, the memory-service files for its tier-2 un-refusals, `engine.rs:2661–2668`, its spine region, `quilltap-harness/tests/embedding_generate_*`, `harness/oracle/{cases,fixtures}/embedding-generate-*`, `embedding-generate-{main,mount}.db` |
| **P4.9E3B** | the new tools-inventory module(s), `services/search_replace.rs` (new), the export + outfit-summary + message-reattribute service code, `services/chat_participants.rs`, `services/chat_merge.rs`, `services/outfit_selections.rs`, `db/chats.rs`, `api/salon.rs`, `crates/quilltap-web/src/wardrobe_routes.rs` + its new `tools_routes.rs`, its §1 regions, its spine region, `quilltap-harness/tests/{tools_inventory,chat_export,search_replace,message_reattribute}_*`, `harness/oracle/{cases,fixtures}/chat-dialogs-*`, `chat-dialogs-{main,mount}.db` |
| **P4.9E3C** | **all of `apps/web/**`** — no other lane may touch it |
| **P4.D24** | `api/custom_tools.rs`, `pascal_custom_tools_route_equivalence` + its oracle case/fixture builder, the `CLAUDE.md`/`phase-4.md` baseline paragraphs |

**Version bumps.** This lane bumps `quilltap-core`, `quilltap-harness`,
`quilltap-host`, and `quilltap-web`. Concurrent bumps are expected; the
unifier recounts as `base + total bumps` (`parallel-round-playbook`).

> Per `.claude/commands/commit.md` every commit carries a version bump for the
> crate(s) it touches.

## Verification gate (run before declaring done)

1. `cargo fmt --all --check`
2. `cargo clippy --workspace --all-targets` **and** the second feature set the
   most recent round record ran
3. `cargo test --workspace --no-fail-fast`
4. **Your own differentials BY NAME with `--nocapture`, env vars set, zero
   SKIP lines**
5. **Re-run by name every family your writes can reach** — at minimum
   `chat_admin_routes_equivalence`, `chat_cast_routes_equivalence`,
   `chats_tier2_equivalence`, `chat_settings_tier2_equivalence`,
   `salon_mutations_equivalence`, `wardrobe_routes_equivalence`,
   `chats_search_equivalence`, and the chat-timestamp family — over oracles
   regenerated fresh at `e8a49597`. The timestampConfig normalization in
   particular changes what the chats write path persists; expect and
   investigate any tripwire.
6. `cargo build --release`
7. **No SPA run is owed** — this lane must not touch `apps/web/**`.

Per-commit rule: follow `.claude/commands/commit.md`.

## Records to leave behind

- A lane record in `status-log.md`: what landed, every deferral by name with
  its v4 file:line, the fixture recipe, each differential's case count and
  what it proves, the plugin-arm disposition, and the two llm_choose default
  modes as reproduced.
- Update this order's status header; update `m6-screen-parity.md` only for
  rows whose SERVER half this lane changes (the rows stay MISSING until the
  UI lands — say so).
- Record the stop-impersonate v4 bug (survey above) for the human to carry to
  the v4 side; do NOT fix v4.
