# Work order P4.9I2A — the help/HelpChat SERVER lane: the shipped help tree + the boot-time sync wire, the help-docs read verbs, the help-chats dispatch family, and the help-chat orchestrator

**Status:** LANE COMPLETE — ready for `/unify` (2026-09-05; branch
`claude/p4-9i2a-help-server-porting-575b4a`, 9 commits — 8 units + the closing gate record). Tier 1 items 1–7 and
Tier 2 items 8–10 LANDED; Tier 3: nothing deferred (no refusal arms needed
beyond the driver-less `HelpChatSend` NAMED refusal, which is design). Unit
records + the divergence list: `status-log.md` → "P4.9I2A — the help/HelpChat
SERVER lane". The unifier marks the `p4.9i2` bank sections named there and
folds the §B wire types. (Ordered 2026-09-05, the `p4.9i2` help/HelpChat round:
P4.9I2A ∥ P4.9I2B ∥ P4.76 ∥ P4.77.)

## Preamble

- **v4 oracle baseline: `d883a5ee1`** (drift-ledger §1, "fix(memory): name
  the subject of a memory about someone else (bug 122)", v4 `4.9.0-dev.135`).
  v4 `main` HEAD is `c2232cd9a`, four commits past — **every one of them
  docs/version-only** (the 4.9.0 release cut; ledger §3, all four rows
  `ORDERED(P4.77)` for the mechanical ratification). There is NO code drift to
  absorb in this lane. `lib/`, `app/`, `packages/`, `plugins/` are
  byte-identical between the baseline and HEAD (measured, ledger §1).
- **Freshness probe at lane start** (ledger §2): the checkout sits on
  **`bugfix`** (recorded — do NOT switch it; v4 is the human's active repo),
  tree clean, `git -C ~/source/quilltap-server log --oneline c2232cd9a..main`
  empty, `git … log --oneline 2b49f51aa..bugfix` empty. **Any mismatch: STOP
  and report** — never `/driftcheck` from a lane.
- **Regen rule: PIN REQUIRED at `d883a5ee1`** (ledger §1 — HEAD is past the
  baseline AND the checkout is on the wrong branch; the code is identical but
  the rule applies as written, and the pinned tree's `package.json` reads
  `4.9.0-dev.135` where `bugfix` reads `4.9.1-bugfix.0`). Every regen and
  every fixture build runs from **`/tmp/qt-v4-pin-p49i2a-d883a5ee1`** (all
  three symlink classes, ledger §5.1) through the sweep driver
  (`harness/tools/recipe_sweep.py --v4 <pin> --run <family>`), and **every
  `cwd`-relative read of v4's `help/` tree — the content oracle below — runs
  with `cwd = the pin**, because v4 captures `HELP_DIR = join(process.cwd(),
  'help')` at module load (`harness/oracle/cases/help-sync-ensure.test.ts`
  header). ⚠ The oracle `node_modules` are the LIVE tree (ledger §1's standing
  hazard) — `zod` is 4.5.4; nothing in this lane transcribes Zod beyond the
  five small body schemas surveyed below.
- **Sibling lanes:** P4.9I2B (the HelpChat SPA — §B binds you together; it
  touches nothing under `crates/**`), P4.76 (the `/api/v1/images?action=
  generate` remainder — a Rust lane; §S names the fenced files you share),
  P4.77 (maintenance — harness guard + docs + a test-helper consolidation
  that must NOT touch this lane's new files). §B, §S and the Ownership table
  are binding and identical across the orders that carry them.
- **Worktree:** per the `carryout` skill (branch `claude/p4-9i2a-…`). Edit by
  absolute worktree path (memory note `edit-inside-the-worktree-not-main`);
  every `cd ~/source/quilltap-server && …` resets the tool cwd to MAIN — `cd
  <worktree>` before every cargo command (memory note
  `p4.9i1a-brahma-server-lane`). `CHANGELOG`/`status-log.md` append-only.
  **No Playwright** (§S.5). Per-worktree cargo targets run 10–20 GB — clean
  merged targets first (memory note `parallel-lane-disk-exhaustion`).

## Mandate

Port v4's help/HelpChat **server half** whole — the last unported vertical of
any size — so that the SPA lane's Guide and Ask tabs have a real backend and
the help documentation actually exists on a v5 instance:

1. **Ship the help content.** v5 has NO `help/` tree at all: the host walker
   (`quilltap-host/src/host.rs:544` → `files_store::load_help_source_files`)
   walks `<cwd>/help`, which does not exist in this repo, so every
   `EMBEDDING_REINDEX_ALL` since P4.6BM has synced an EMPTY tree and every
   `help_search` call on a fresh v5 instance reads zero rows. Vendor v4's
   `help/` (120 files, 1.6 MB, at the pin) into the repo root, embed it in the
   binary at compile time, and make that ONE embedded table the source for
   both the boot ensure and the reindex-all handler.
2. **Wire `ensure_help_docs_synced` at boot** — the deferral its own doc
   comment names ("no production caller"); with it the P4.D77 upgrade
   BACKFILL becomes reachable for the first time.
3. **The help-docs read verbs** v4's `app/api/v1/help-docs/**` serves (list,
   `?action=chat-count`, `?action=search&q=`, `{id}`) — the P4.D77 bank's
   half (a), surveyed there and re-surveyed below.
4. **The help-chats dispatch family** (`app/api/v1/help-chats/**`: list,
   `?action=eligibility`, create, get, rename, `?action=update-context`,
   delete, messages, send) + `verify_help_chat` + the REST edges.
5. **The help-chat orchestrator** (`lib/services/help-chat/
   orchestrator.service.ts`, 645 LOC) with its two pure neighbours
   (`lib/help-chat/context-resolver.ts`, `system-prompt-builder.ts`), on a
   new host driver seam mirroring `BrahmaConsoleSendDriver`. 💸 **The send is
   LIVE and costs real money once wired** (the Brahma precedent — flag it in
   the lane record as P4.9I1A did).

Every unit ships with its differential against v4's REAL code (the
requirement restated below). **Must NOT touch:** anything under `apps/web/`
(P4.9I2B's), `api/images.rs` / `images_routes.rs` / `model/image.rs` /
`tools/generate_image.rs` / `api/files.rs`'s upload path (P4.76's), the
already-ported help substrate's PUBLIC surface (`db/help_docs.rs`,
`db/help_doc_chunks.rs`, `db/help_search.rs`, `services/help_doc_sync.rs`,
`services/help_doc_chunking.rs`, `tools/help.rs`, `tools/help_search.rs` —
extend by NEW functions only; never rewrite a ported one), the Brahma
orchestrator (share by REUSE, never by editing `services/brahma_console/**`),
and the 19 `CaptureLayer` test copies P4.77 consolidates (write your own
capture tests against whatever helper your tree has; the unifier folds).

## Survey-verified starting points (2026-09-05 @ v4 `d883a5ee1` — do not re-derive)

### The v4 surface, by file

| v4 file | LOC | Port target | Notes |
| --- | --- | --- | --- |
| `help/*.md` | 120 files, 1.6 MB | **vendor at the pin** (`help/` at the v5 repo root) | front matter is `url:` (+ optional `title:`/`tags:`); `url` shapes: 14× `/salon/:id`, 12× `/aurora`, 7× `/settings?tab=system`, 6× `/salon`, 6× `/prospero/:id`, 5× `/settings?tab=chat`, 5× `/files`, 4× `/profile`, 3× `/aurora/:id/edit`, 3× `/`; **23 files carry a `:param`**; **three are wildcards `url: *`** (`search.md`, `sidebar.md`, `width-toggle.md`) |
| `lib/help-search.ts` | 284 | `db/help_search.rs` already ports `search`; **`listDocuments` / `getDocument(idOrSlug)` / `getAllDocuments` are NEW** (read verbs) | v4 keeps an in-process `documents` cache filled by `loadFromDatabase()` (which FIRST calls `ensureHelpDocsSynced()`); v5 reads the table per call — **record the divergence** (no process cache; `invalidate()` has no twin) |
| `lib/help/help-doc-sync.ts` | 499 | ported (`services/help_doc_sync.rs`) — **wire the caller** | `HELP_DIR = join(process.cwd(), 'help')` at module load |
| `lib/help/help-doc-slug.ts` | 22 | ported (`help_doc_slug.rs`) | |
| `lib/help-chat/context-resolver.ts` | 202 | NEW `services/help_chat/context_resolver.rs` | six strategies, see below |
| `lib/help-chat/system-prompt-builder.ts` | 140 | NEW `services/help_chat/system_prompt.rs` | byte-exact; reuses `templates::process_template`, `system_prompt::build_identity_reinforcement`, `system_prompt::first_active_scenario_content` |
| `lib/services/help-chat/orchestrator.service.ts` | 645 | NEW `services/help_chat/orchestrator.rs` | the Brahma orchestrator (`services/brahma_console/orchestrator.rs`) is the v5 shape to mirror |
| `lib/services/help-chat/types.ts` | 29 | — | `HelpChatSendOptions { content, fileIds? }`; `HelpChatEligibleCharacter` declares `supportsImages` **which the eligibility route never emits** (and the client type omits) — a dead field, NO-PORT with this line as evidence |
| `app/api/v1/help-docs/route.ts` | 161 | NEW `api/help_docs.rs` + `quilltap-web/src/help_routes.rs` | |
| `app/api/v1/help-docs/[id]/route.ts` | 60 | ″ | |
| `app/api/v1/help-chats/route.ts` | 247 | NEW `api/help_chats.rs` + `help_routes.rs` | |
| `app/api/v1/help-chats/[id]/route.ts` | 235 | ″ | |
| `app/api/v1/help-chats/[id]/messages/route.ts` | 126 | ″ | |
| `lib/help-guide/categories.ts` | 195 | **SPA-side** (P4.9I2B) — not yours | |
| `lib/tools/help-*.ts`, `handlers/help-*.ts` | — | ported (`tools/help.rs`, `tools/help_search.rs`) | `tool_build.rs:420` `help_tools_enabled` gates all three |

**Where help chats live:** the `chats` table with `chatType = 'help'` and
`helpPageUrl` — both columns already in `fresh_schema.json` (line 10) and
round-tripped by `db/chats.rs`; `chat_predicates::is_help_like_chat_type`
already excludes them from Salon lists and routes the title job to
`titleHelpChat` (`services/context_summary.rs:889`). **No schema change.**

### The route tables (verbatim from the shipped files)

**`GET /api/v1/help-docs`** (`route.ts:20-36`) → `{ documents: [{id, slug,
title, path, url}] }` (`listDocuments`); catch → `serverError('Failed to load
help documents')`. Logs `[HelpDocs] Listed help documents { documentCount }`.

**`GET /api/v1/help-docs?action=chat-count`** (`:117-134`) →
`{ count }` over `repos.chats.findByUserId` filtered `!chat.chatType ||
chat.chatType === 'salon'`; catch → `serverError('Failed to get chat count')`.

**`GET /api/v1/help-docs?action=search&q=`** (`:38-115`) → `{ matches:
[{slug, titleHit, snippet}] }`. `query = (q ?? '').trim()`; **`query.length <
2` → `{matches: []}`** (JS `.length` = UTF-16 units); `needle =
query.toLowerCase()`; per doc `titleHit = title.toLowerCase().includes(needle)`,
`contentIndex = content.toLowerCase().indexOf(needle)`; drop when neither;
`snippet = contentIndex >= 0 ? buildSnippet(content, contentIndex,
query.length) : null`; sort `Number(b.titleHit) - Number(a.titleHit)` (a
STABLE sort — JS `Array.prototype.sort` is stable; keep document order within
each half). `buildSnippet`: `start = max(0, idx - 30)`, `end = min(len, idx +
qlen + 160)` (the deliberately lopsided `SNIPPET_LEAD 30` / `SNIPPET_TRAIL
160` — v4's why-comment at `:38-45`, carry it), slice, `/```+/g → ' '`,
`/[*_`#|>]/g → ''`, `/\s+/g → ' '`, `.trim()`, then `…` prepended if `start >
0` and appended if `end < len`. ⚠ All indices are UTF-16 units and the
`toLowerCase` is JS's — use the `jsstr` helpers (`to_lowercase` is already
byte-identical to JS per Phase 1; slicing needs a UTF-16-unit slice). Note
the ORDER of the two regex passes matters (the fence pass runs before the
single-char pass). Unknown `?action=` → **falls through to the list** (this
route is one of v4's default-serving shapes — memory note
`v4-has-three-action-dispatch-shapes`; pin the arm). Logs `[HelpDocs] Guide
text search { query, matchCount }`; catch → `serverError('Failed to search
help documents')`.

**`GET /api/v1/help-docs/{id}`** (`[id]/route.ts:24-59`): `docId` = the last
path segment; `getDocument(idOrSlug)` matches `doc.id === x || doc.slug === x`;
miss → `notFound('Help document')` + warn `[HelpDoc] Document not found`;
hit → `{ document: {id, title, path, url, content} }` (NO slug in this body)
+ info `[HelpDoc] Document retrieved { documentId, title }`; catch →
`serverError('Failed to get help document')`.

**`GET /api/v1/help-chats`** (`route.ts:40-73`) → `{ chats: [{id, title,
updatedAt, participants: enriched, messageCount, helpPageUrl}] }`: all the
user's chats filtered `chatType === 'help'`, sorted `new Date(b.updatedAt) -
new Date(a.updatedAt)` (a JS comparator over `getTime()` — memory note
`js-nan-comparator-is-not-a-total-order`; the fixture must carry distinct
timestamps and one arm may plant an unparseable one), participants through
`enrichParticipantSummary` (v5 `services/chat_enrichment.rs::
enrich_participant_summary`), `messageCount = getMessages(id).length`,
`helpPageUrl: chat.helpPageUrl || null`.

**`GET /api/v1/help-chats?action=eligibility`** (`:78-119`) → `{ eligible,
characters, reasons }`: help chars = `defaultHelpToolsEnabled === true`;
tool-capable profiles = `allowToolUse !== false`; per char
`hasToolCapable = defaultConnectionProfileId ? some(p.id === it) :
toolCapable.length > 0`; avatar = `charAny.avatarUrl || null`, else
`files.findByLinkedTo(char.id)` → the first tagged `avatar` else `images[0]`
→ `/api/v1/files/${id}`; row shape `{id, name, avatarUrl,
defaultHelpToolsEnabled: true, connectionProfileId:
defaultConnectionProfileId || null, hasToolCapableProfile}`; `eligible =
some(hasToolCapableProfile)`; `reasons`: `'No characters have help tools
enabled'` when none, `'No tool-capable connection profiles available'` when
some but none capable. Unknown `?action=` → `badRequest('Unknown action:
${action}. Available actions: eligibility')` (the middleware-envelope shape —
`isValidAction`).

**`POST /api/v1/help-chats`** (`:124-217`): body Zod `{ characterIds:
z.array(z.string().uuid()).min(1, 'At least one character is required'),
pageUrl: z.string() }` via **`.parse` (uncaught)** → memory note
`v4-refusal-envelope-parse-vs-safeparse` — MEASURE the envelope at the pin,
never assume. Then per id `findById` → **`notFound('Character')` on the FIRST
miss** (before the help-enabled check — the guard ORDER is the `a6870c5a`
class; pin it), `badRequest('At least one character must have help tools
enabled')` when none has `defaultHelpToolsEnabled`; participants built in
order `{id: uuid, type: 'CHARACTER', characterId, controlledBy: 'llm',
connectionProfileId: char.defaultConnectionProfileId || null, imageProfileId:
null, displayOrder: i, isActive: true, createdAt: now, updatedAt: now}`;
`repos.chats.create({… title: `Help: ${firstChar.name}`, contextSummary:
null, tags: [], roleplayTemplateId: null, timestampConfig: null,
messageCount: 0, lastMessageAt: null, lastRenameCheckInterchange: 0,
projectId: null, disabledTools: [], disabledToolGroups: [], imageProfileId:
null, chatType: 'help', helpPageUrl})` — ⚠ **v4's create echoes the INPUT
literal, so its six explicit nulls survive in the 201 body while a v5
re-read omits them** (memory note `p4.9i1a-brahma-server-lane`: re-inject
exactly those keys); then one SYSTEM message `Help chat initiated for page:
${pageUrl}` (`attachments: []`); response `created({ chat: {...chat,
participants: enriched} })` (201). Logs `Help chat created { chatId, userId,
characterCount, pageUrl }`.

**`GET /api/v1/help-chats/{id}`** (`[id]/route.ts:69-91`) → `{ chat:
{...chat, participants: enriched, messageCount} }`. **`verifyHelpChat`**
(`:44-60`): `findById` miss → `notFound('Help chat')`; `chatType !== 'help'`
→ the SAME 404 — ⚠ note v4's `verifyHelpChat` never checks `userId` despite
its doc comment (single-user instance; reproduce, record).

**`PATCH /api/v1/help-chats/{id}`** (no action, `:98-123`): Zod `{ title:
z.string().min(1, 'Title is required') }` `.parse`; `chats.update(id, {title,
isManuallyRenamed: true})`; null → `serverError('Failed to update help
chat')`; → `{ chat: updated }`. **`?action=update-context`** (`:128-164`):
Zod `{ pageUrl: z.string().min(1, 'Page URL is required') }`;
`chats.update(id, {helpPageUrl})`; null → `serverError('Failed to update help
chat context')`; then one SYSTEM message `[System: User navigated to
${pageUrl}]`; → `{ chat: updated }`. Unknown action → `badRequest('Unknown
action: ${action}. Available actions: update-context')`. ⚠ verify-then-parse
ORDER on both (the 404 gate runs before the body parse — `:106`/`:136`).

**`DELETE /api/v1/help-chats/{id}`** (`:169-190`): verify; `chats.delete` false
→ `serverError('Failed to delete help chat')`; → `messageResponse('Help chat
deleted successfully')`.

**`GET /api/v1/help-chats/{id}/messages`** (`messages/route.ts:92-105`) →
`{ messages }` (raw `getMessages`). **`POST …/messages`** (`:60-86`): verify;
Zod `{ content: z.string().min(1, 'Message content is required'), fileIds:
z.array(z.string().uuid()).optional() }` `.parse`; `handleHelpChatMessage` →
an SSE stream. ⚠ **`fileIds` is accepted and then IGNORED** — the orchestrator
reads only `options.content` and saves the user message with `attachments:
[]` (`orchestrator.service.ts:84-92`); pin it as accepted-ignored, do not
invent an attachment path.

### The orchestrator, anatomically (`orchestrator.service.ts`)

- `handleHelpChatMessage` (`:65-186`): load chat → `Chat not found` /
  `Unauthorized` (`chat.userId !== userId`) / `Not a help chat`; save the USER
  message; `activeParticipants = participants.filter(isParticipantPresent(p.
  status) && controlledBy === 'llm').sort(displayOrder)`; none → `No active
  help characters in chat`; **`pageUrl = chat.helpPageUrl || '/'` →
  `resolveAllHelpContentForUrl` → `primary = [0] || null`, `additional =
  slice(1)`**; then **each participant SEQUENTIALLY**, with `turnStart
  {participantId, characterName: char?.name || 'Unknown', chainDepth: i}`
  before every participant AFTER the first and `turnComplete {participantId,
  messageId: id || '', chainDepth: i}` after it (multi-character only); a
  per-participant failure emits `error {message, errorType:
  'processing_error', details: ''}` and CONTINUES; multi-character ends with
  `chainComplete {reason: 'cycle_complete', nextSpeakerId: null, chainDepth:
  n-1}`; then `triggerAsyncTasks` (fire-and-forget) and close; a top-level
  failure emits `error … 'fatal_error'` and closes.
- `processHelpResponse` (`:189-586`): character `Character not found`;
  `No connection profile for help character` / `Connection profile not
  found`; **api key via `resolveConnectionProfileApiKey`** — on failure throw
  **`'No API key configured'`** / **`'API key not found'`** (the P4.D93 bank:
  the SHORT sentences, distinct from both Brahma sites; v5 twin
  `services::api_key_service::resolve_connection_profile_api_key`); user
  character = `{ name: user.name || 'User', description: '' }` when the user
  row exists, else null; `otherCharacterNames` from `findById` per other
  participant (misses skipped); **`buildTools(profile, null, null, userId,
  null, false, [], [], agentModeEnabled=true, isMultiCharacter, helpTools=
  true)`** — the v5 `tool_build` input with `help_tools_enabled: true`,
  agent mode ON, no image profile, no project, nothing disabled;
  `simple-json → text-block` downgrade (`:243-249`) exactly as Brahma;
  text-block instructions via `determineTextBlockToolOptions(null, false,
  isMulti, false, true, false, false)` else native instructions when tools
  exist; **`maxAgentTurns = 10`** (`:274` — NOT Brahma's 25 and NOT the
  chat setting; a lane-local constant) with `buildAgentModeInstructions(10)`
  appended to the tool instructions (`\n\n`-joined, or alone); the system
  prompt from `buildHelpChatSystemPrompt`; **history = the chat's messages
  with SYSTEM rows DROPPED** (`:296-321` forwards only USER/ASSISTANT/TOOL
  as `{role: user|assistant|tool, content}` — so the `Help chat initiated…`
  and `[System: User navigated…]` rows are transcript-only); `effectiveTools
  = (!textBlock && native) ? tools : []`; the loop `while (turn <= 10)` with
  the force-final message at `turn === 10`, streaming `content` chunks,
  native (`detectToolCallsInResponse`) or text-block (`hasTextBlockMarkers`
  → `parseTextBlocksFromResponse`) detection, the `submit_final_response`
  arm + the JSON-text fallback (`extractSubmitFinalResponseFromText`), the
  duplicate-call guard **`MAX_DUPLICATE_TOOL_CALLS = 2`** with its nudge
  sentence (`:451`, byte-exact incl. `${duplicateCount + 1}` and the
  `toolDataReminder` tail — read the hunk), assistant tool-call message
  persisted, `processToolCalls` (per-tool `status` frames inside it) +
  `saveToolMessages`; after the loop `stripCharacterNamePrefix` +
  `extractSubmitFinalResponseFromText` on the full response, the assistant
  message saved, `estimateMessageCost`, the `done` frame `{done: true,
  messageId, usage, toolsExecuted: agentTurnCount > 1, provider, modelName,
  …}` and `trackMessageTokenUsage`. Every v5 twin named above exists
  (`agent_mode.rs`, `message_formatter.rs`, `brahma_console::
  normalize_tool_call_signature`, `pseudo_tool.rs`, `chat_events.rs`'s
  `ChatEvent::turn_start`/… frames, `services/chat_enrichment.rs`).
- **`triggerAsyncTasks` (`:588-645`) — DIFFERENT from Brahma:** gated on
  `chatSettings.cheapLLMSettings` present AND the FIRST participant's
  `connectionProfileId` resolving; then **BOTH** `triggerContextSummaryCheck`
  (auto re-title) **AND `triggerTurnMemoryExtraction`** — help chats DO
  extract memories (Brahma never does). Each is `.catch`-warned
  (`Failed to trigger context summary check` / `… memory extraction` /
  `… async tasks`). The v5 twins are the Salon spine's triggers; the tier-3
  corpus must SEE the enqueues (`background_jobs` as comparands — the
  fixture must carry the table, memory note: the archive fixture's blindness).
- **`resolveHelpContentForUrl` (`context-resolver.ts:23-129`):** over
  `listDocuments()` — (1) `urlQuery` set → exact `doc.url === url`; (2)
  path-exact ignoring query, `!doc.url.includes(':')` — and if `urlQuery`
  set, prefer among docs with the same path AND a query whose every param
  matches, the one with the MOST params (`'query'`), else `'exact'`; (3)
  pattern (`:` in `doc.url`, `matchUrlPattern` on the path half; the most
  `/`-segments wins — a JS `sort` tie keeps insertion order); (4) prefix
  (`docPath !== '*' && urlPath.startsWith(docPath + '/')`, longest `doc.url`
  wins); (5) wildcard (`url === '*'`, the FIRST); (6) fallback `url === '/'`;
  else null. **`resolveAllHelpContentForUrl` (`:176-201`)** = primary + every
  wildcard doc **except where `doc.id === primary.url`** — ⚠ an id-vs-url
  comparison that is NEVER true, so when the primary IS a wildcard doc it is
  pushed TWICE. Reproduce faithfully, pin with a corpus row, and record it as
  a **candidate upstream filing** (the port never fixes v4's bugs silently).
- **`buildHelpChatSystemPrompt` (`system-prompt-builder.ts:31-139`):** parts
  joined `\n\n` then `.trim()`: the `## Character Identity` preamble through
  `processTemplate`; the fixed `## Help Assistant Role` block (byte-copy —
  it names `help_search, help_settings, help_navigate` and the
  `help_navigate` MUST-call rule); tool instructions (templated); `##
  Character Personality` (second-person wrapper — the bug-88 wording); `##
  Character Pronouns` (subject/object/possessive); the tool-reinforcement
  sentence (only when tool instructions exist); `## Current Page Context`
  (`**title**`, `` URL: `url` ``, `### Page Documentation` + content); each
  additional context as `### Additional Context: title`; `## User Character`;
  `## Other Help Characters` (`', '`-joined); `buildIdentityReinforcement`.
  The template context: `{char, user, description, personality, scenario:
  firstActiveScenarioContent(scenarios), persona: userCharacter.description}`.

### The v5 side today

- `services/help_doc_sync.rs` (sync + `ensure_help_docs_synced` +
  `backfill_help_doc_chunks`; **no production caller for the ensure**),
  `services/help_doc_chunking.rs`, `db/help_docs.rs` (`HelpDocsRepository`:
  upsert/find_all/…), `db/help_doc_chunks.rs`, `db/help_search.rs`
  (`semantic_search`/`keyword_search` + the seam note), `help_doc_slug.rs`,
  `tools/help.rs` (`help_settings`/`help_navigate`), `tools/help_search.rs`.
- `quilltap-host/src/files_store.rs:304` `load_help_source_files(cwd)` — v4's
  `findMarkdownFiles` order reproduced; the ONLY caller is `host.rs:544`
  (the `EMBEDDING_REINDEX_ALL` registration).
- `api/types.rs` has NO help variants; `core-contract.ts` carries the
  `'help'` `chatType` literal only (`:2627`, `:2911`).
- The compile-time asset precedent: `services/quilltap_import/
  seed_assets.rs` embeds `assets/first-startup/**` via `include_str!`/
  `include_bytes!` ("a native binary carries them"). The 120-file help tree
  wants the same idea at scale — see Tier 1 item 1.
- The Brahma precedent for everything else: `services/brahma_console/
  orchestrator.rs` (the loop shape, the `EventSink` frames, the writer
  discipline), `api/brahma.rs` (verb handlers), `quilltap-web/src/
  brahma_routes.rs` (the unwrap-to-raw-body edges), `quilltap-host/src/
  spine.rs:2112` (`BrahmaConsoleSendDriver` impl; `run_brahma_send` `:1569`;
  the registry field `:3017`), and the families `brahma_console_routes_
  equivalence` / `brahma_orchestrator_tier3_equivalence` with their oracle
  cases `harness/oracle/cases/brahma-console-routes.test.ts` /
  `brahma-orchestrator-tier3.test.ts`.
- Committed help oracle cases already in the tree: `help-doc-chunking.ts`,
  `help-doc-slug.ts`, `help-docs-tier2.ts`, `help-docs-upsert-tier2.ts`,
  `help-sync-ensure.test.ts` (the jest real-DB recipe, chdir-per-scenario),
  `help-sync-guards.ts`, `help-sync-tier2.ts`, `help-tools.ts`.

## Tiered deliverables

### Tier 1 — must land

1. **The shipped help tree.** (a) `help/` at the v5 repo root, byte-identical
   to the pin (`diff -r "$PIN/help" help` empty — put that diff in the lane
   record). (b) A compile-time embedding in `quilltap-host` — preferred: a
   `build.rs` that walks `../../help` in exactly `find_markdown_files`' order
   and emits a `&[(&str /*rel_path*/, &str /*content*/)]` table via
   `include_str!` (the `quilltap-sqlite3mc-sys` crate is the workspace's
   build.rs precedent; keep the generated file out of the source tree). The
   `rel_path` is v4's `relative(process.cwd(), filePath)` — `help/<name>.md`.
   (c) `files_store::load_help_source_files` keeps the fs walk for the
   differential fixture trees, and gains an `embedded_help_source_files()`
   sibling; **`host.rs:544` switches to the embedded table** so reindex-all
   and the boot ensure share ONE source. (d) A harness guard
   (`help_tree_embed_guard`) asserting the embedded table equals the on-disk
   `help/` (path set, order, bytes) — a stale build cannot pass. Bump the
   sys crate? NO — the pinned `quilltap-sqlite3mc-sys` is never bumped; the
   host crate carries the build.rs.
2. **The boot-time ensure.** Call `ensure_help_docs_synced` from the host
   after unlock (the P4.D140/P4.D152 boot-ensure sites in `host.rs` are the
   placement precedent; it must run AFTER the partitions open and BEFORE the
   job pump starts, since the sync enqueues embedding jobs). v4 is LAZY
   (first read of any help route/tool through `loadFromDatabase()`); v5 is
   EAGER — record the divergence (observable only as timing/log lines; the
   rows are identical). Wiring pin: a host test that boots a fresh instance
   and reads `help_docs` = 120 rows, a second boot writes nothing (the
   `content_hash` short-circuit), and a **mutation** (the call removed) reads
   0. This is what finally makes the P4.D77 backfill reachable — say so in
   the lane record with the backfill's own test still green.
3. **The help-docs read verbs** — `Request::HelpDocsList`,
   `HelpDocsChatCount`, `HelpDocsSearch { q }`, `HelpDocGet { id_or_slug }`
   (§B names) in `api/types.rs` (append-only inside a `// === P4.9I2A ===`
   fence, §S.1), handlers in a NEW `api/help_docs.rs`, `engine.rs` wiring
   (fenced), the `Response::HelpDocs(Value)` family variant, and the REST
   edges in a NEW `quilltap-web/src/help_routes.rs` at v4's paths with
   v4's raw bodies (unwrap the dispatch envelope — the P4.6ah rule the
   Brahma edges follow), registered in `lib.rs` (§S.1). The pure snippet +
   search mapper live in a NEW `services/help_chat/guide_search.rs` (or
   `db/help_search.rs`'s NEW functions — your call, extend never rewrite).
4. **The help-chats dispatch family** — the nine §B verbs in
   `api/help_chats.rs` (+ `verify_help_chat`), the `Response::HelpChat(Value)`
   / `Response::HelpChatSend(Value)` variants, the REST edges in
   `help_routes.rs` (`GET|POST /api/v1/help-chats`, `?action=eligibility`,
   `GET|PATCH|DELETE /api/v1/help-chats/{id}`, `?action=update-context`,
   `GET|POST /api/v1/help-chats/{id}/messages`). Body decoding goes THROUGH
   the `Request` enum (memory note `edge-must-decode-through-the-request-
   enum`); the five Zod schemas' refusal envelopes are MEASURED at the pin
   and pinned per arm; unknown-`?action=` answers v4's `Unknown action: …`
   sentence on the two envelope-shaped routes; the query-param reader is the
   ONE shared reader P4.67/P4.72 landed (FIRST wins; memory note
   `query-param-first-vs-last-wins`).
5. **The two pure modules** — `services/help_chat/context_resolver.rs`
   (`resolve_help_content_for_url` / `resolve_all_help_content_for_url` /
   `match_url_pattern`, over a `&[HelpDocListing]` the caller reads — keep
   the DB read OUTSIDE so the tier-1 family can drive it from a corpus) and
   `services/help_chat/system_prompt.rs`, each carrying v4's why-comments.
6. **The orchestrator** — `services/help_chat/orchestrator.rs`
   (`handle_help_chat_message` + `process_help_response` +
   `trigger_async_tasks`), generic over the same seams the Brahma
   orchestrator consumes (streaming provider / tool runner / tool detector /
   `CostTracker`) PLUS the two Salon triggers for the async tail; frames on
   the Event channel scope-tagged by `chatId` (§B); the writer discipline
   (each persist its own short `Db::write`, never held across an await).
   Carry the constants: `maxAgentTurns = 10`, `MAX_DUPLICATE_TOOL_CALLS = 2`,
   the nudge sentence, the error sentences, the three api-key/profile
   sentences. The `HelpChatSend` verb dispatches to a NEW
   `HelpChatSendDriver` seam (`api/help_chats.rs` trait; `spine.rs` impl;
   registry field beside `brahma_console_send`) — the P4.9I1A precedent;
   **flag the live seam in the lane record (💸)**. A `HelpChatSend` with no
   driver registered answers a NAMED typed refusal, never a silent no-op.
7. **The differentials** (restated below): the content oracle, the two
   routes families, the three tier-1 pure families, the tier-3 orchestrator
   family, and the four wiring pins.

### Tier 2 — should land

8. Port v4's `__tests__/unit/lib/help-chat/context-resolver.test.ts` (368
   LOC), `system-prompt-builder.test.ts` (237), `match-url-pattern.test.ts`
   (89) and `lib/services/help-chat/orchestrator.test.ts` (328) case-for-case
   where they are pure/mockable (the guards, the sentences, the frame
   sequence for the multi-character path).
9. The `[HelpDocs]`/`[HelpDoc]`/`Help chat …` log lines at v4's levels with
   capture-layer presence pins (memory note
   `differential-blind-to-a-log-only-fix`; the handler-logging inventory
   `docs/developer/porting/handler-logging-inventory.md` gains the two new
   route families as rows).
10. `docs/v4/` mirror: nothing to refresh here (P4.77 owns the mirror this
    round); but phase-4.md's five `p4.9i2` bank sections (lines ~5718, ~7449,
    ~7484 + the m6 §1.6 row-11 notes) are DISCHARGED by item 1 — name each in
    the lane record so the unifier can mark them.

### Tier 3 — explicit refusal-arm deferrals

- **None expected.** `fileIds` on send is accepted-ignored BY v4 (pinned,
  not deferred). If a text-block-mode edge the Brahma loop never exercised
  surfaces, land it as a loud, named, typed refusal and record it — never a
  silent stub.

## The differential requirement (restated)

Every ported unit ships with an equivalence test driving v4's REAL code —
jest/tsx oracle → NDJSON → Rust diff, per tier. Run every family BY NAME
through the sweep driver from the pin, `--nocapture`, and grep the log for
`SKIP:` (zero). Recipes to copy, not reinvent:

- **The content oracle — `help_tree_equivalence` (tier 2, NEW).** Drive v4's
  REAL `syncHelpDocs` (via `ensureHelpDocsSynced` on an EMPTY `help_docs`)
  over the PINNED `help/` tree with `cwd = $PIN` (the `help-sync-ensure.
  test.ts` jest recipe — resetModules + chdir per scenario; the
  `ensureProcessorRunning` mock), dump `help_docs` (title/path/url/content/
  contentHash — path-keyed, ids normalized) and `help_doc_chunks`
  (docId-remapped, chunkIndex/heading/content), and diff against v5's
  `sync_help_docs` over the EMBEDDED table. This proves the vendored bytes,
  the front-matter parser and the chunker over all 120 real files at once.
  A mutation that edits ONE byte of one vendored file must redden it.
- **`help_docs_routes_equivalence` (tier 2, NEW)** over the NEW committed
  `help-chat-{main,mount}.db` pair (Fixtures below): list / chat-count (three
  chat types seeded) / get-by-id + get-by-slug + 404 / unknown-action-falls-
  through / search: a title hit, a content-only hit, both, neither, the
  `< 2` arm (a one-char query AND a one-astral-char query — ONE code point,
  TWO UTF-16 units: v4 does NOT short-circuit it), a fence-stripping snippet,
  an interior slice with both `…`, a leading slice with only the trailing
  `…`, an emphasis/heading/pipe-stripping row, a `>`-quoted paragraph. The
  oracle drives v4's REAL route handlers (`brahma-console-routes.test.ts` is
  the real-DB jest-route idiom; memory note `jest-real-db-oracle`).
- **`help_snippet_equivalence` (tier 1, NEW)** — `buildSnippet` + the search
  mapper alone over a corpus of ≥ 20 rows (the UTF-16 arms, the regex ORDER
  arm, the stable-sort arm with three docs where title-hit order differs
  from document order).
- **`help_context_resolver_equivalence` (tier 1, NEW)** — v4's REAL
  `resolveHelpContentForUrl` / `resolveAllHelpContentForUrl` with
  `@/lib/help-search` mocked to a corpus document list (the mock shape at
  `__tests__/unit/lib/help-chat/context-resolver.test.ts:23-27`), ≥ 25 URL
  rows covering all six strategies, the query-specificity tie, the pattern
  segment-count tie (insertion order), the prefix longest-wins, `'/'` as
  input, a URL with no match at all (null), and **the duplicate-wildcard
  quirk** (primary IS a wildcard → pushed twice). Emit `{matchType, id}` per
  row and the full list for the `All` variant.
- **`help_system_prompt_equivalence` (tier 1, NEW)** — `buildHelpChatSystemPrompt`
  over ≥ 12 option combinations (no personality / personality with
  `{{char}}`+`{{user}}` / pronouns / tool instructions present vs absent —
  which toggles the reinforcement sentence / page context / two additional
  contexts / user character null vs present / other names 0, 1, 3), byte-
  exact.
- **`help_chats_routes_equivalence` (tier 2, NEW)** over the same fixture:
  every route table row above incl. the guard-order arms (missing character
  404 BEFORE the help-enabled 400; verify-then-parse on rename/update-
  context), the six-null create echo, the SYSTEM rows written by create and
  update-context (rowid-ordered projection — memory note
  `a-sorted-dump-cannot-see-position`), eligibility's four arms, the
  `Unknown action` sentences, and the Zod refusals (`characterIds: []`, a
  non-uuid, `title: ''`, `pageUrl: ''`, `content: ''`, a wrong-type body) —
  envelopes MEASURED. Cover `verify_help_chat`'s two 404 arms with a salon
  chat id and a missing id.
- **`help_chat_orchestrator_tier3_equivalence` (tier 3, NEW)** — mocked-LLM
  per `brahma-orchestrator-tier3.test.ts` + memory notes
  `tier3-completion-oracle` (rule-match + record canned rows; replay in
  Rust), `keyed-canned-provider-cannot-count-calls` (diff the ordered
  `recordings`), `oracle-fire-and-forget-tail` (drain the async tail before
  the next case), `chain-depth-frozen-clock-artifact`. Comparands: the
  ordered frame sequence, the chat's message rows (tool rows included), the
  chat's cost/token columns, and `background_jobs` (the two async-tail
  enqueues — the fixture MUST carry the table). Arms: single character, no
  tool; two characters (turn frames + `chainComplete`); a native
  `help_search` turn (the SPA reads its result for suggested links — the
  frame's `toolResult.result` shape is a comparand); a native
  `help_navigate` turn; a text-block turn on a profile whose manifest has no
  native tools; the duplicate-call guard (nudge row); the
  `submit_final_response` arm + the JSON-text fallback; the 10-turn cap; a
  participant whose profile lacks a key (`No API key configured` as the
  per-participant `error` frame, the OTHER participant still answering); a
  chat whose `helpPageUrl` is null (→ `'/'`); the async tail SUPPRESSED when
  `cheapLLMSettings` is absent.
- **Wiring pins (unit tier):** the embed guard; the boot ensure (item 2);
  the driver refusal; and the reindex-all handler reading the embedded
  table (a mutation restoring the cwd walk on a cwd without `help/` must
  redden it).

## Fixtures

- **This lane DELIVERS** `crates/quilltap-harness/fixtures/help-chat-
  {main,mount}.db` (fixture-sanitizer conventions; the `brahma-*` pair is
  the model): one user with `cheapLLMSettings` set; three characters — two
  with `defaultHelpToolsEnabled = 1` (one with a `defaultConnectionProfileId`,
  one without; one with a linked `avatar`-tagged file, one relying on
  `images[0]`, one with neither) and one with it null; three connection
  profiles (one `allowToolUse = 0`, one tool-capable with a native-tools
  manifest, one on a no-native-tools provider for the text-block arm); ≥ 12
  `help_docs` rows seeded with REAL vendored files chosen to cover every
  resolver strategy (`/`, `*` ×2, `/salon`, `/salon/:id`, `/settings?tab=chat`,
  `/settings?tab=system`, `/aurora`, `/aurora/:id/edit`, `/files`, one
  `title:`-front-matter file, one H1-derived title) + their chunks; ≥ 2 help
  chats with history (incl. tool rows and a SYSTEM row), one salon chat, one
  brahma chat (the list and chat-count filters); distinct `updatedAt`s; the
  `background_jobs` table present and empty. Document the seeds in the
  fixture's `.meta.json`.
- **Consumes** the vendored `help/` (yours) — no other lane's fixture.
- You own regenerating every oracle that consumes the pair. The
  `help-sync-*`/`help-docs-*` families' committed fixtures are untouched.

## §B — the help wire contract (BINDING — verbatim-identical in P4.9I2A and P4.9I2B)

**Dispatch verbs** (Rust `Request` variant / SPA wire type name / fields on
the wire in camelCase / v4 anchor):

| Rust variant | SPA type | Fields | v4 anchor |
| --- | --- | --- | --- |
| `HelpDocsList` | `helpDocsList` | — | `help-docs/route.ts:20-36` |
| `HelpDocsChatCount` | `helpDocsChatCount` | — | `:117-134` |
| `HelpDocsSearch` | `helpDocsSearch` | `q` | `:38-115` |
| `HelpDocGet` | `helpDocGet` | `id` (a DB id OR a slug) | `help-docs/[id]/route.ts:24-59` |
| `HelpChatList` | `helpChatList` | — | `help-chats/route.ts:40-73` |
| `HelpChatEligibility` | `helpChatEligibility` | — | `:78-119` |
| `HelpChatCreate` | `helpChatCreate` | `characterIds`, `pageUrl` | `:124-217` |
| `HelpChatGet` | `helpChatGet` | `chatId` | `[id]/route.ts:69-91` |
| `HelpChatRename` | `helpChatRename` | `chatId`, `title` | `:98-123` |
| `HelpChatUpdateContext` | `helpChatUpdateContext` | `chatId`, `pageUrl` | `:128-164` |
| `HelpChatDelete` | `helpChatDelete` | `chatId` | `:169-190` |
| `HelpChatMessages` | `helpChatMessages` | `chatId` | `[id]/messages/route.ts:92-105` |
| `HelpChatSend` | `helpChatSend` | `chatId`, `content`, `fileIds?` (accepted-ignored, as v4) | `:60-86` |

- **Response DTOs mirror v4's route JSON name-for-name** — both lanes derive
  them from the v4 handlers at the anchors; the tier-2 differentials are the
  arbiter. A derivation disagreement between the lanes is a unifier-level
  event, not something either lane patches around. Notable shapes: the
  document LIST row is `{id, slug, title, path, url}`; the single document is
  `{id, title, path, url, content}` (no slug); search rows are `{slug,
  titleHit, snippet}`; eligibility is `{eligible, characters: [{id, name,
  avatarUrl, defaultHelpToolsEnabled, connectionProfileId,
  hasToolCapableProfile}], reasons}`; the help-chat list row is `{id, title,
  updatedAt, participants, messageCount, helpPageUrl}`.
- **Send semantics:** the dispatch reply is the typed result (`{messageId}` —
  the id of the LAST persisted assistant message, or `null` when none was
  produced); the frames ride the global Event channel scope-tagged by
  `chatId`, exactly like `ChatSend`/`BrahmaConsoleSend`: `content`,
  `status`, `toolsDetected`, `toolResult` (batch-relative `index`), `done`,
  `error {error, errorType: 'processing_error' | 'fatal_error', details:
  ''}`, and — multi-character only — `turnStart` / `turnComplete` (from the
  SECOND participant on, `chainDepth = i`) and one `chainComplete {reason:
  'cycle_complete', nextSpeakerId: null, chainDepth: n-1}`. The SPA reads
  `toolResult` frames named `help_navigate` (→ navigation links) and
  `help_search` (→ suggested links) exactly as v4's
  `useHelpChatStreaming.ts:176-231` does; the server does not pre-digest
  them.
- **REST edges** (quilltap-web, v4-path-faithful): `GET /api/v1/help-docs`,
  `?action=chat-count`, `?action=search&q=`; `GET /api/v1/help-docs/{id}`;
  `GET|POST /api/v1/help-chats` (+ `?action=eligibility` on GET);
  `GET|PATCH|DELETE /api/v1/help-chats/{id}` (+ `?action=update-context` on
  PATCH); `GET|POST /api/v1/help-chats/{id}/messages`. `POST /help-chats`
  answers 201; `POST …/messages` returns the send reply body.
- **Constants:** create's title is exactly `` `Help: ${firstChar.name}` ``;
  create writes the SYSTEM row `Help chat initiated for page: ${pageUrl}`;
  update-context writes `[System: User navigated to ${pageUrl}]`; delete
  answers `Help chat deleted successfully`; `verify_help_chat` 404s
  (`Help chat`) unless the row exists AND `chatType === 'help'`; the
  eligibility `reasons` sentences are `No characters have help tools
  enabled` / `No tool-capable connection profiles available`; the search
  short-circuit is `q.trim().length < 2` in UTF-16 units; `SNIPPET_LEAD =
  30`, `SNIPPET_TRAIL = 160`; `maxAgentTurns = 10`. The SPA's storage keys
  (`quilltap:help-chat-selected-characters`, `quilltap:help-chat-last-id`,
  `quilltap:help-tab`) are SPA-only and never reach the wire.
- The SPA lane declares its wire types lane-locally in
  `apps/web/src/app/help/help-wire.ts`; the UNIFIER folds them into
  `core-contract.ts` and runs the name-for-name wire diff against
  `api/types.rs` (§S.3).

## §S — the round-wide meeting points (BINDING — identical in all four orders)

1. **Fenced-append shared files.** `crates/quilltap-core/src/api/types.rs`,
   `crates/quilltap-core/src/api/engine.rs`, and
   `crates/quilltap-host/src/host.rs` are edited by BOTH Rust lanes
   (P4.9I2A, P4.76) — each lane appends ONLY inside its own fence
   (`// === P4.9I2A ===` / `// === P4.76 ===`, opened once per file, new
   variants/arms/registrations added at the END of the relevant block,
   never interleaved with existing lines). `crates/quilltap-web/src/lib.rs`
   is P4.9I2A's alone (P4.76 registers nothing new — `?action=generate` is
   an action on an existing edge). `crates/quilltap-host/src/spine.rs` is
   P4.9I2A's alone. `Cargo.toml`/`Cargo.lock` at the workspace root: version
   lines only (§S.4); a dependency add is a STOP-and-flag. P4.77 touches
   none of these.
2. **Frozen files.** `docs/developer/porting/drift-ledger.md` (lanes never
   write it), `apps/web/src/app/core/core-contract.ts` (folded at unify —
   §S.3), `apps/web/src/app/shell/shell.ts` (UNIFIER-ONLY: the P4.9I2B
   `<qt-help-entry />` mount lands there at unification, BEFORE
   `<qt-brahma-entry />` — v4's footer order `sidebar-footer.tsx:203` then
   `:213`), `apps/web/src/app/workspace/workspace-contract.ts` (v4's help is
   a dialog, not a tab kind — no registry row).
3. **`core-contract.ts` folds at unify.** P4.9I2B declares the §B DTOs in
   `apps/web/src/app/help/help-wire.ts`; the unifier folds them and runs the
   name-for-name wire diff against `api/types.rs`.
4. **Version bumps.** P4.9I2A: `quilltap-core`, `quilltap-harness`,
   `quilltap-web`, `quilltap-host` per commit. P4.76: `quilltap-core`,
   `quilltap-harness`, `quilltap-web`, and `quilltap-host` (its seam).
   P4.77: `quilltap-harness` (+ `quilltap-core` for the test helper). P4.9I2B:
   `apps/web/package.json` per commit. The unifier RECOUNTS — identical
   bumps merge silently (memory note `commit-checklist`; the "silent
   auto-merge trap" in the playbook).
5. **e2e.** Only P4.9I2B runs Playwright and it adds NEW spec files only
   (`help-*.spec.ts`), with beats that depend on the server verbs or the
   shell mount shipped as guarded ACTIVATE-AT-UNIFY probes that report their
   guarded state loudly. The three other lanes: `git diff main -- apps/web/`
   empty. Port 4319 is repo-wide — one Playwright at a time.
6. **Append-only for everyone:** `docs/CHANGELOG.md`,
   `docs/developer/porting/status-log.md`.
7. **The regen rule for every lane:** PIN REQUIRED at `d883a5ee1`, a
   LANE-UNIQUE pin path (`/tmp/qt-v4-pin-<order>-d883a5ee1`), every regen
   through `recipe_sweep.py --v4 <pin>`; the checkout stays on `bugfix`
   untouched. The unifier moves the baseline to `c2232cd9a` (a pure
   ratification, P4.77's evidence) and the rule returns to PIN NOT REQUIRED
   only once the human has put the checkout back on `main`.
8. **The `p4.9i2` bank discharge:** P4.9I2A's vendored `help/` at the pin
   discharges every banked help-copy note (phase-4.md's five `### p4.9i2
   bank` sections and the m6 §1.6 row-11 notes); P4.9I2B's Guide tab
   discharges the P4.D77 bank's half (b). The unifier marks them.

## Ownership (BINDING — identical table in all four orders)

| Lane | Owns (edit freely) | Fenced-append (§S.1) | Must not touch |
| --- | --- | --- | --- |
| **P4.9I2A** | `help/**` (new, vendored); `crates/quilltap-host/build.rs` + `Cargo.toml` (host crate only) + `src/files_store.rs` + a new `src/help_content.rs`; `crates/quilltap-core/src/services/help_chat/**` (new); `crates/quilltap-core/src/api/{help_docs,help_chats}.rs` (new); NEW functions appended to `db/help_search.rs` / `db/help_docs.rs`; `crates/quilltap-web/src/help_routes.rs` (new) + `lib.rs`; `crates/quilltap-host/src/spine.rs`; new harness families + oracle cases + the `help-chat-*` fixture; `handler-logging-inventory.md` rows; this order's header | `api/types.rs`, `api/engine.rs`, `host.rs` | `apps/web/**`; P4.76's files; `services/brahma_console/**`; the 19 `CaptureLayer` sites |
| **P4.9I2B** | `apps/web/src/app/help/**` (new); `_surfaces.css`'s `qt-help-*` block (extend only); new e2e specs + `e2e/support/seed-help-fixture.ts`; `apps/web/package.json`; this order's header | — | `crates/**`; `shell.ts`; `core-contract.ts`; `workspace-contract.ts`; any existing spec |
| **P4.76** | `crates/quilltap-core/src/api/images.rs`; `api/files.rs` (the upload path: `save_file_entry` + `FileUpload.tags`); `model/image.rs`; `tools/generate_image.rs` (visibility widening only); `crates/quilltap-web/src/{images_routes,files_routes}.rs`; a new host image-generation seam file; `crates/quilltap-harness/tests/{images_routes_equivalence,activity_span_sites_guard,web_edge_body_parse_guard}.rs` (+ a new generate family); `harness/oracle/cases/images-*.ts`; the `images-*` fixture; this order's header | `api/types.rs`, `api/engine.rs`, `host.rs` | `apps/web/**`; P4.9I2A's files; `lib.rs`; `spine.rs` |
| **P4.77** | `crates/quilltap-harness/tests/zod_version_guard.rs` (new); `docs/v4/releases/4.9.0.md`; a new core test-support module + the 19 named `CaptureLayer` sites (listed at lane start, none in another lane's files); `crates/quilltap-core/src/pascal/custom_tools.rs` (`render_template` log lines only); this order's header | — | everything in the other three lanes' columns; `api/types.rs`; `engine.rs`; `host.rs` |

Everyone: `docs/CHANGELOG.md` + `status-log.md` append-only; the drift ledger
frozen; `core-contract.ts`/`shell.ts` frozen.

## Verification gate (before declaring done)

1. `cargo fmt --all --check`; `cargo clippy --workspace --all-targets --
   -D warnings` in BOTH feature sets (default + `quilltap-core/native-
   transport`); `cargo build --workspace`; the release build.
2. `cargo test --workspace` (full log captured to a file + a sentinel — never
   `| tail`; memory notes `background-gate-latency-fix`,
   `cargo-test-workspace-is-fail-fast`, `a-gate-piped-through-tail-
   undercounts-binaries`) with this lane's env block: every new
   `QT_ORACLE_HELP_*` + `QT_FIXTURE_HELP_CHAT_*` variable, the existing
   help families' variables (`help-sync-*`, `help-docs-*`, `help-tools`),
   and the Brahma pair (unchanged code, shared seams — a neutrality check);
   grep the log for `SKIP:` — zero for every family the block names.
3. Every family above by NAME through `recipe_sweep.py --v4 /tmp/qt-v4-pin-
   p49i2a-d883a5ee1 --run <family>`, oracles fresh at the pin, zero SKIP;
   for the content oracle, `diff -r "$PIN/help" help` empty in the log.
4. The mutation proofs named above (one-byte help edit; the ensure call
   removed; the cwd walk restored; the duplicate-wildcard guard; the
   guard-order swap on create; the six-null echo), each named in the lane
   record with the arm it reddened.
5. `git diff main -- apps/web/` empty.
6. Commits per `.claude/commands/commit.md` (core + harness + web + host
   bumps; CHANGELOG every commit); the lane record appended to
   `status-log.md` (with the 💸 live-send flag and the divergence list:
   eager sync, no process cache, the accepted-ignored `fileIds`, the
   duplicate-wildcard reproduction + upstream candidate, the userId-less
   verify); this order's status header updated.
