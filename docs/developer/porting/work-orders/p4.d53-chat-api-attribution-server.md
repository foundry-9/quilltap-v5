# Work order P4.D53 — the `f4955e0e` chat-API + attribution server drift: bugs 22/23/24/25/27/36/37 (projections, participant updates, impersonation), bug 28 (staff announcement attribution), bug 10's per-chat annotations sweep, bug 41 (`Content-Disposition` ext-value)

> **STATUS: CLOSED — UNIFIED on main (2026-08-06, the f4955e0e convergence
> round).** All nine tier-1 bugs landed (41/10/28 + the coupled
> 22/37/36/27/25/23/24 bundle); no tier-3 deferrals; `api/types.rs`
> never opened. The salon fixture regen seeded the five projection
> fields non-default + the `allowToolUse:false` profile; the
> salon-mutations oracle drives stop-impersonate through the REAL
> DELETE route; `markdown_transcript`'s `EXPECTED_DIVERGENCES` is
> empty (machinery kept wired). **⚠ THE BUG-27 RULING CONFLICT stands
> flagged for the human:** v5 mirrors v4's `controlledBy` flips per
> the port discipline; the finding-#39 post-5.0 design (which forbade
> exactly that) needs a fresh ruling — flagged in
> `dogfood-findings.md`. Lane record: `status-log.md` → "Lane record —
> P4.D53".

Part of the `f4955e0e` drift catch-up round (v4's "bugs 8–43" batch).
This lane absorbs the RUST side of v4 `bd419ae9` (session 6) and
`99d5fc7d`'s lib half (session 7's bug 28), plus two re-binned items:
bug 10's per-chat `conversation_annotations` sweep (from session 2 —
this lane owns `db/chats.rs` and the chats families) and bug 41's
`content_disposition` widening (from session 9 — the helper is
core-side and its corpus family is Rust-owned). The SPA halves of
sessions 6/7/9 are P4.D54's; the §1 contract below is BINDING between
the two lanes.

**v4 baseline for THIS round: `f4955e0e`** (4.8.0-dev.175, 2026-08-06,
tree clean at planning). Classification: "Round planned — the
f4955e0e found-bugs convergence round" in `status-log.md`. **The
baseline move is the UNIFIER's** — do NOT edit `CLAUDE.md` or
`phase-4.md` on the branch.

**Drift-check at lane start:** `git log f4955e0e..HEAD --oneline` +
`git status --short` in `~/source/quilltap-server`. If HEAD moved:
classify; STOP only if a new commit lands on files this order names;
otherwise regenerate from a worktree PINNED at `f4955e0e`
(`oracle-regen-pinned-v4-worktree`). All chat-cast/salon oracle
regens run `TZ=UTC` per their headers.

**Siblings (same round):** P4.D51 (guards/backup/mount), P4.D52
(scriptorium/memory/almanack), **P4.D54 (the SPA lane — §1 below is
binding between you)**, P4.D55 (providers), P4.43
(memory-maintenance remainder).

**Worktree note:** own branch, own checkout; no `git stash`/
`git worktree` games with sibling lanes.

## Mandate

Mirror v4's chat-API state fixes faithfully: the five new chat-GET
projection fields, the `controlledBy`-patch fall-through, the
post-cleanup remove-participant reply, the impersonation
`controlledBy` flips with identity recompiles, `allowToolUse` on the
enriched connection profile, the per-chat annotations sweep, the
staff-announcement attribution (a genuine FIX in v5 too, per the
human's 2026-08-02 ruling on finding #54 — not merely a mirror), and
the widened `Content-Disposition` ext-value escape. Rewrite the
salon-mutations oracle case for the POST→DELETE stop-impersonate move
(a hard blocker for regenerating that family). Extend the fixtures
that are currently blind to every one of these arms. Do NOT touch:
`apps/web/**` (P4.D54's — including `core-contract.ts`; the SPA
mirror of §1 is theirs), P4.D51's files, P4.D52's, P4.D55's, P4.43's,
`api/types.rs` (FROZEN all round — every wire change here is inside
JSON bodies, not typed variants).

## Survey-verified starting points (2026-08-06, fresh at `f4955e0e`)

### Bugs 22 + 37 — the chat GET projection (v4 `bd419ae9`) — FAITHFUL
- v4 `handlers/get.ts:566-569`: four new keys, each `?? null` —
  `timelineMode, alertCharactersOfLanternImages, showThinking,
  answerConfirmationOverride` — after `turnSkippingEnabled`; and
  `:549` `allLLMPauseTurnCount: chatMetadata.allLLMPauseTurnCount ?? 0`
  (note: `isPaused` was ALREADY projected — the commit message
  overstates).
- v5: `api/salon.rs` `assemble_chat_get` (`:206-457`; the emit block
  `:337-455`; `turnSkippingEnabled` at `:401-404`, `isPaused` at
  `:361`). All five values are already READ (`db/chats_read.rs:59-76`
  selects; materialized at `:251`, `:278`, `:314`;
  `allLLMPauseTurnCount` at `:189` as f64 — the harness's
  `canon_numbers` collapses integer-valued floats). Projection-only
  change.
- **Fixture blindness (the P4.D24 lesson):** `salon.json` sets NONE of
  the five (nor a non-zero pause count) — both sides would emit
  null/0 and a wrong VALUE would pass. Extend `salon.json` +
  `build-salon-fixture.ts` with non-default values per field and
  regenerate the committed `salon-{main,mount}.db` (this lane owns
  them; re-run every family over them by name — salon reads,
  mutations, and any sibling the fixture feeds).

### Bug 23 — `controlledBy` patch early return (v4 `bd419ae9`) — FAITHFUL
- v4 `helpers.ts:196-202`: the early `findById`-and-return DELETED;
  the patch now falls through to the status/`isActive` back-compat
  sync and the `compileAllIdentityStacks` tail.
- v5 ported the early return verbatim:
  `services/chat_participants.rs:796-801` (step 6), with the standing
  ruling in the module header `:29-36` ("a v4 quirk this port
  reproduces deliberately") — rewrite BOTH. Steps 7/8 (`:804-930`)
  become reachable; `compile_all_identity_stacks` lives at
  `services/system_prompt_compiler.rs:207`.
- Family: `chat_cast_routes_equivalence` (72 cases). The pinned case
  `update_controlled_by_with_status_early_return`
  (`chat_cast_routes_equivalence.rs:873`; oracle
  `chat-cast-routes.test.ts:529-533`) is now mis-named — re-name it
  to the post-fix semantics when regenerating. **The family dumps the
  `chats` table**, so every `update_controlled_by_*` case's dump moves
  (`compiledIdentityStacks` is rewritten), not just the response.

### Bug 24 — remove-participant returns the post-cleanup chat (v4 `bd419ae9`) — FAITHFUL
- v4 `actions/participants.ts:572-599`: capture the cleanup
  `repos.chats.update` result into `finalChat`; respond with it.
- v5: `api/chat_cast.rs:462-498` performs the cleanup write and
  DISCARDS the result (the ported comment at `:462-467` says so);
  `:512` responds with the pre-cleanup chat. Change the response to
  the post-cleanup row.
- Cases: `remove_impersonating_promotes` / `remove_impersonating_
  not_typist` + the `("remove_ok","bag_remove_participant",false)`
  entrance pair.

### Bug 25 — stop-impersonate POST → DELETE (v4 `bd419ae9`) — CONVERGED, oracle-side work only
- v5's typed `ChatStopImpersonate` verb already models it correctly
  (v4's ledger: "Converged"). NO v5 source change.
- **⚠ HARD BLOCKER for regen:** `harness/oracle/cases/
  salon-mutations.test.ts:223` drives `stop_impersonate` via
  `chatPost` `?action=stop-impersonate`, which at the new HEAD answers
  **400 Unknown action**. Rewrite the case to import `DELETE` from
  `@/app/api/v1/chats/[id]/route` (a new `chatDelete` method arm in
  the case runner) BEFORE regenerating `QT_ORACLE_SALON_MUTATIONS`.

### Bug 27 — impersonation flips `controlledBy` (v4 `bd419ae9`) — FAITHFUL, with a ruling flag
- v4 `actions/participants.ts`: `handleImpersonate` writes
  `{controlledBy:'user'}` BEFORE `addImpersonation` (`:61`), then a
  non-fatal `compileAllIdentityStacks` try/warn (`:69-77`);
  `handleStopImpersonate` — the `newConnectionProfileId` branch gains
  `?? updatedChat`, and a NEW else branch writes
  `{controlledBy:'llm'}` when no profile is supplied (`:120-131`),
  plus the mirror recompile (`:133-141`). `findActiveUserParticipant`
  itself untouched.
- v5: `api/salon.rs` `chat_impersonate` (`:1621-1665` — no
  `controlledBy` write, no recompile) and `chat_stop_impersonate`
  (`:1668-1750` — profile branch writes `controlledBy:'llm'` at
  `:1707-1726`; NO else arm; no recompile). Mirror v4 exactly,
  including the try/warn non-fatality.
- **⚠ RULING CONFLICT — mirror now, flag for re-ruling.**
  `dogfood-findings.md:1056-1081` records a POST-5.0 v5-divergence
  design for finding #39 whose explicit instruction is "Do NOT mutate
  the participant's `controlledBy`". v4's shipped fix does exactly
  that. The port discipline (v5 follows v4) governs the PRESENT: land
  v4's behavior faithfully. The #39 design note is about a FUTURE
  multi-turn-impersonation feature and its premise has moved — do not
  edit `dogfood-findings.md` (the unifier's sweep flags it for the
  human's re-ruling); state the conflict prominently in your lane
  record.
- Families: `salon_mutations_equivalence` (diffs response + `chats` +
  `chat_messages` dumps — the participants JSON and
  `compiledIdentityStacks` changes land there);
  `turn_pause_filters_equivalence` gains the v5 twin of v4's new
  turn-manager case ("honours a formerly-LLM character once
  impersonation flips it to user-controlled");
  `chats_impersonation_tier2` (repo layer, neutrality).

### Bug 36 — `allowToolUse` on the enriched profile (v4 `bd419ae9`) — FAITHFUL
- v4 `chat-enrichment.service.ts:118-120` + `:380`
  (`allowToolUse: profile.allowToolUse ?? true`) and the
  `helpers.ts:83` mirror.
- v5: `services/chat_enrichment.rs:191-200`
  (`EnrichedConnectionProfile { id, name, provider, model_name,
  api_key }`) + `get_connection_profile` (`:322-347`). The column is
  already read everywhere (`db/connection_profiles.rs:196` etc.,
  schema default 1).
- **Fixture blindness:** no fixture anywhere carries an
  `allowToolUse: false` profile — the `false` arm (the one that makes
  the SPA warning box render) would never be proven. Seed one in the
  salon fixture regeneration (same move as bugs 22/37).
- The SPA binding is P4.D54's; the key is §1's.

### Bug 28 — staff-signed announcements attributed (v4 `99d5fc7d`) — BOTH-APPS FIX (finding #54)
- v4 `announcement-attribution.ts`: `AnnouncerAttributable` gains
  `opaqueContent?/systemSender?/systemKind?` (`:37-40`);
  `resolveAnnouncerName(announcer, names, systemSender?)` — announcer
  branches nested, then `if (systemSender) return
  staffDisplayName(systemSender).trim() || null` (`:57-72`); the gate
  `m.systemKind === 'announcement' ? m.systemSender : undefined`
  (`:114`); the `prefix()` closure with the idempotency guard applied
  to `content` AND to string `opaqueContent` (`:117-127`). Caller
  unchanged.
- v5: `announcement_attribution.rs` — widen the trait (`:72-80`; note
  `with_content` currently rewrites only `content`; the opaque write
  needs its own path), the resolver (`:88-111`), the pass
  (`:139-159`), and BOTH impls: the `Value` impl (`:165-179`, what the
  differential drives) and the production `WhisperMessage` impl
  (`services/message_context.rs:123-140` — the struct already carries
  `opaque_content`/`system_sender`/`system_kind`).
  `staff_display_names.rs:51` already exists. **Ordering invariant:**
  step A2 (`message_context.rs:602-640`) must prefix `opaqueContent`
  BEFORE `normalize_whisper_roles` (`:327-359`) swaps it into the
  body at `:344-348` — v4's order; carry the why-comment.
- **Corpus blindness:** the `announcement-attribution.ts` oracle
  corpus (~26 records, pure NDJSON, no DB) has ZERO staff arms. Add:
  staff-signed announcement (`systemSender: 'host'` / `'suparna'`), a
  staff whisper with a NON-announcement `systemKind` (must NOT be
  tagged), an `opaqueContent` arm, and an idempotency re-run arm. The
  record schema grows with the trait — the family's coverage-by-shape
  assertion moves with it.
- In-situ neutrality re-runs: `build_context_tier3`,
  `message_context_leaves`, `announcer_tier3`, the post-office whisper
  arms (fixtures carry no staff ad-hoc announcements today — expect
  output-neutral; verify, and note which fixtures would need seeds for
  in-situ proof if any family's comparands do move).

### Bug 10, per-chat half (v4 `3bb664f0`) — NEW SURFACE
- v4 `chats.repository.ts:336-350`: chat delete sweeps
  `conversationAnnotations.deleteAllForChat(id)` in try/catch→warn.
- v5: `db/chats.rs:1253-1262` deletes only `chats` + `chat_messages`;
  `db/conversation_annotations.rs` has `delete`/`delete_annotation`
  but no `delete_all_for_chat`. Add both halves; prove as state
  through `chats_tier2_equivalence` (a delete op over a fixture
  carrying annotations — seed if absent) and re-run the
  conversation-annotations families for neutrality.

### Bug 41 — `Content-Disposition` ext-value (v4 `ea4dc011`) — v4 went FURTHER than v5
- v4 `content-disposition.ts:10`: `encodeExtValue =
  encodeURIComponent(value).replace(/['()*!]/g, %XX-upper)`. v5's
  `content_disposition.rs:72-74` escapes only `'` (the dogfood-#46
  fix). Widen to v4's exact set; `encode_uri_component` itself stays
  JS-faithful (`api/ui_search.rs:42` is a second consumer wanting JS
  semantics — don't touch it).
- **Pin that trips:** `markdown_transcript_equivalence.rs:39-42`
  `EXPECTED_DIVERGENCES` (`ascii-apostrophe-with-non-ascii`) — the
  `divergence_vanished` arm (`:222-230`) fires on regen because both
  sides now emit `%27`. Retire it.
- **Corpus blindness:** none of the ten disposition vectors carries
  `( ) * !` beside a non-ASCII char — the newly-converged four-char
  set is untested. Add a vector (v4's own test asserts
  `filename*=UTF-8''a%27%28%29%2A%21%E2%80%94b.txt`).
  `chat_export_equivalence` (`:99`, `:193`) and the almanack
  disposition check stay green (ASCII titles) — re-run for neutrality.

## Tiered deliverables

### Tier 1 — must land
1. The five-field GET projection + the salon fixture extension +
   regeneration (bugs 22/37/36 in one fixture move), with
   `salon_reads_equivalence` proving non-default VALUES.
2. The `controlledBy` fall-through (bug 23) + the module-header
   re-rule + the chat-cast regen with the renamed case.
3. The post-cleanup remove reply (bug 24).
4. The salon-mutations oracle DELETE rewrite (bug 25) — a
   prerequisite for every regen of that family this round.
5. The impersonation flips + recompiles (bug 27), the
   turn-pause-filters twin case, and the ruling-conflict flag in the
   lane record.
6. `allowToolUse` on the enriched profile (bug 36), with the
   `false`-profile fixture arm.
7. The staff-attribution fix end-to-end (bug 28): trait + resolver +
   gate + dual prefix + both impls + the staff corpus arms +
   neutrality re-runs.
8. The per-chat annotations sweep (bug 10) with its state proof.
9. The `encode_ext_value` widening (bug 41) + the retired divergence +
   the new vector.

### Tier 2 — should land
10. The stale-comment sweep in this lane's files (the
    `chat_participants.rs` ruling header, the `chat_cast.rs`
    pre-cleanup comment, the salon.rs projection notes). Do NOT edit
    `dogfood-findings.md` — the unifier sweeps it once for the round.

### Tier 3 — explicit loud deferrals
- None planned. (The `AllLLMPauseModal` SPA port that bug 37 unlocks
  is P4.D54's, not a deferral of yours.)

## The differential requirement, restated

Families: `salon_reads_equivalence` (`QT_ORACLE_SALON_READS`),
`salon_mutations_equivalence` (`QT_ORACLE_SALON_MUTATIONS`),
`chat_cast_routes_equivalence` (`QT_ORACLE_CHAT_CAST`, 72 cases —
expect broad dump movement from `compiledIdentityStacks`),
`chats_tier2` (`QT_ORACLE_CHATS`), `chats_read` / `chats_participants_
tier2` / `chats_impersonation_tier2` (neutrality),
`connection_profiles_tier2` (neutrality),
`announcement_attribution_equivalence`
(`QT_ORACLE_ANNOUNCEMENT_ATTRIBUTION` — pure, tsx-driven),
`turn_pause_filters_equivalence` (`QT_ORACLE_TURN_PAUSE_FILTERS`),
`conversation_annotations_tier2` (+ upsert sibling, neutrality),
`markdown_transcript_equivalence` (`QT_ORACLE_MARKDOWN_TRANSCRIPT`),
`chat_export_equivalence` (neutrality), `build_context_tier3` /
`message_context_leaves` / `announcer_tier3` (neutrality). All from
v4's REAL code at the `f4955e0e` pin, TZ=UTC where the headers say,
BY NAME with `--nocapture`, zero SKIP, fresh-baseline markers grepped
([[oracle-regen-silent-stale-pass]]); recipes stay runnable
([[harness-recipes-are-runnable]]); changed arms mutation-proven (the
D24 rule); committed-fixture rule per
[[sweep-recipes-write-into-the-repo]].

## Fixtures

This lane owns and regenerates: `harness/oracle/fixtures/salon.json`
+ `build-salon-fixture.ts` + the COMMITTED
`crates/quilltap-web/tests/fixtures/salon-{main,mount}.db` (ONE
regeneration covering bugs 22/36/37's seeds; re-run EVERY family over
the salon fixtures by name — the P4.D50-era list includes the salon
tier-3 set; enumerate what actually reads them and record it), and
`chat-cast-{main,mount}.db` + spec + builder if bug 23/24 coverage
needs seeds (verify first — the 72 cases may already suffice). It
must NOT touch P4.D51's, P4.D52's, or P4.D55's fixtures.

## §1 Shared contract (BINDING — identical text in P4.D54)

No `api/types.rs` change: every item is a JSON-body shape. The SPA
mirror in `apps/web/src/app/core/core-contract.ts` is P4.D54's;
this lane's server emissions and P4.D54's TS types must agree
name-for-name (the unifier diffs them):

1. **Chat GET body — five additive fields** (emitted by
   `assemble_chat_get`, all v4 `?? null` except the count):
   `timelineMode: string | null`,
   `alertCharactersOfLanternImages: boolean | null`,
   `showThinking: boolean | null`,
   `answerConfirmationOverride: string | null`,
   `allLLMPauseTurnCount: number` (default 0).
2. **`participants[].connectionProfile.allowToolUse: boolean`**
   (default true) — present in the chat GET AND in chat-PUT /
   participant-action response bodies.
3. **Behavioral, no key change:** impersonate/stop-impersonate now
   mutate `participants[].controlledBy` (user on start; llm on
   profile-less stop) and rewrite `chats.compiledIdentityStacks`;
   remove-participant's reply carries the POST-cleanup
   `impersonatingParticipantIds`.

Conflict rule: this lane's emissions land first at unification;
P4.D54's `core-contract.ts` additions must be purely additive
mirrors of the names above.

## Ownership (BINDING)

THIS lane owns: `crates/quilltap-core/src/api/salon.rs`,
`api/chat_cast.rs`, `services/chat_participants.rs`,
`services/chat_enrichment.rs`,
`crates/quilltap-core/src/announcement_attribution.rs`,
`services/message_context.rs`, `db/chats.rs`,
`db/conversation_annotations.rs`,
`crates/quilltap-core/src/content_disposition.rs`, and the
harness/oracle files of its families (incl.
`markdown_transcript_equivalence.rs` and the salon-mutations case
runner). It must NOT touch: `apps/web/**`, P4.D51's files, P4.D52's,
P4.D55's, P4.43's, `api/types.rs`, `api/engine.rs`. `CHANGELOG` and
`status-log.md` are append-only union-merge. Version bumps per
`.claude/commands/commit.md`; the unifier recounts.

## Verification gate (before declaring done)

`cargo fmt --all --check`; clippy both feature sets `-D warnings`;
`cargo test --workspace --no-fail-fast` with the lane's env block
([[cargo-test-workspace-is-fail-fast]]); the lane's differentials BY
NAME with `--nocapture`, zero SKIP, over fresh `f4955e0e` oracles
(TZ=UTC where required); no SPA gate owed (no `apps/web` change —
P4.D54 runs the SPA gates and the joint e2e proof rides
unification). Per-commit rules: `.claude/commands/commit.md`. Lane
record appended to `status-log.md` before handoff — including the
bug-27 ruling-conflict flag, stated loudly.
