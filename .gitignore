# Rust
/target
**/*.rs.bk
# Cargo.lock IS committed — this repo produces binaries (CLI, Tauri). Do not ignore it.

# Probe scratch
qt-probe-*.db

# Node (frontend / oracle, added later)
node_modules
dist
.next

# Angular SPA (apps/web) build artifacts
apps/web/node_modules
apps/web/dist
apps/web/.angular
apps/web/playwright-report
apps/web/test-results
test-results
playwright-report
apps/web/e2e/.artifacts

# Real instance data — NEVER commit a database or a pepper
fixtures/*.db
*.pepper
.env
.env.*

# OS / editor
.DS_Store
__pycache__/
