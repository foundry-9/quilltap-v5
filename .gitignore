# Rust
/target
**/*.rs.bk
# Cargo.lock IS committed — this repo produces binaries (CLI, Tauri). Do not ignore it.

# Probe scratch
qt-probe-*.db

# Node (frontend / oracle, added later)
node_modules
dist
.next

# Real instance data — NEVER commit a database or a pepper
fixtures/*.db
*.pepper
.env
.env.*

# OS / editor
.DS_Store
